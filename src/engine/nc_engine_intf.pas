unit nc_engine_intf;

{$codepage utf8}
{$mode delphiunicode}
{$H+}

interface

uses
    SysUtils,
    Math,
    Classes,
    nc_io_compat,
    Generics.Collections,
    Generics.Defaults,
    nc_platform_compat,
    nc_types,
    nc_candidate_presentation,
    nc_prefix_completion_policy,
    nc_shortcut,
    nc_dictionary_intf,
    nc_local_repair_guard,
    nc_dictionary_sqlite,
    nc_document_context_model,
    nc_pinyin_parser,
    nc_shuangpin_decoder,
    nc_config;

type
    TncInMemoryDictionary = class(TncDictionaryProvider)
    private
        m_map: TDictionary<string, TArray<string>>;
        procedure add_entry(const pinyin: string; const items: array of string);
    public
        constructor create;
        destructor Destroy; override;
        function lookup(const pinyin: string; out results: TncCandidateList): Boolean; override;
    end;

    TncConfirmedSegment = record
        text: string;
        pinyin: string;
        input_code: string;
        canonical_pinyin: string;
        explicit_choice: Boolean;
        literal_choice: Boolean;
        fuzzy_choice: Boolean;
    end;

    TncInferredPathCacheValue = record
        path_text: string;
        score: Integer;
        segment_count: Integer;
    end;

    TncCharLmScoreMode = (clsm_full, clsm_suffix, clsm_context,
        clsm_short_context, clsm_span, clsm_reverse);

    TncSearchBudgetMode = (
        sbm_production,
        sbm_wall_clock,
        sbm_deterministic
    );

    TncLongPathStructureCacheValue = record
        segment_count: Integer;
        single_segment_count: Integer;
        max_segment_units: Integer;
        min_segment_units: Integer;
        segment_units_square_sum: Integer;
        first_segment_units: Integer;
        last_segment_units: Integer;
        nonprimary_multi_char_state: Byte;
    end;

    TncExactLexicalFeatureCacheValue = record
        selected_rank: Integer;
        selected_weight: Integer;
        top_weight: Integer;
    end;

    TncTargetRecallDebug = record
        found: Boolean;
        raw_rank: Integer;
        complete_rank: Integer;
        raw_candidate_count: Integer;
        complete_candidate_count: Integer;
        score: Integer;
        segment_path: string;
        final_state_found: Boolean;
        final_state_rank: Integer;
        final_state_count: Integer;
        max_generated_syllable: Integer;
        max_retained_syllable: Integer;
        chain_generated_syllable: Integer;
        chain_prebeam_syllable: Integer;
        chain_beam_syllable: Integer;
        full_path_generated_syllable: Integer;
        full_path_retained_syllable: Integer;
        final_state_source_mask: Integer;
        chain_final_state_rank: Integer;
        light_final_state_rank: Integer;
        lattice_final_state_rank: Integer;
        oracle_final_state_rank: Integer;
        full_final_state_rank: Integer;
        chain_build_attempted: Boolean;
        chain_build_accepted: Boolean;
        chain_top_score: Integer;
        chain_min_score: Integer;
        chain_search_status: Integer;
        candidate_stage_mask: Integer;
        input_syllable_count: Integer;
        full_path_budget_exhausted: Boolean;
    end;

    TncLongBeamStateDebug = record
        position: Integer;
        input_syllable_count: Integer;
        first_chunk_query_key: string;
        original_rank: Integer;
        base_score: Integer;
        char_lm_score: Integer;
        word_lm_head_bonus: Integer;
        word_lm_full_bonus: Integer;
        word_lm_boundary_count: Integer;
        word_lm_min_bonus: Integer;
        word_lm_max_bonus: Integer;
        word_lm_first_bonus: Integer;
        word_lm_last_bonus: Integer;
        word_lm_supported_count: Integer;
        word_lm_strong_count: Integer;
        word_lm_trigram_count: Integer;
        word_lm_zero_count: Integer;
        segments: Integer;
        single_segments: Integer;
        first_chunk_units: Integer;
        first_chunk_exact_rank: Integer;
        first_chunk_raw_weight: Integer;
        first_chunk_top_weight: Integer;
        first_chunk_weight_gap: Integer;
        first_chunk_effective_score: Integer;
        first_chunk_edge_score: Int64;
        first_chunk_source_user: Boolean;
        first_chunk_baseline_option: Boolean;
        anchor_units: Integer;
        has_anchor: Boolean;
        baseline_lineage: Boolean;
        baseline_selected: Boolean;
        selected: Boolean;
        target: Boolean;
        text: string;
        segment_path: string;
    end;

    TncLongExactEdgeStageDebug = record
        position: Integer;
        input_syllable_count: Integer;
        chunk_units: Integer;
        exact_rank: Integer;
        raw_weight: Integer;
        top_weight: Integer;
        effective_score: Integer;
        edge_score: Int64;
        source_user: Boolean;
        baseline_option: Boolean;
        target: Boolean;
        stage_mask: Integer;
        query_key: string;
        text: string;
    end;

    TncLongRetainedExactEdge = record
        position: Integer;
        chunk_units: Integer;
        exact_rank: Integer;
        raw_weight: Integer;
        top_weight: Integer;
        confidence: Int64;
        query_key: string;
        text: string;
    end;

    TncLongChainCandidateDebug = record
        original_rank: Integer;
        final_rank: Integer;
        order_score: Integer;
        first_stage_score: Integer;
        second_stage_score: Int64;
        promotion_rank: Integer;
        base_score: Integer;
        char_lm_score: Integer;
        char_lm_suffix_score: Integer;
        char_lm_context_score: Integer;
        char_lm_context_gain: Integer;
        char_lm_per_unit: Integer;
        word_lm_bonus: Integer;
        word_lm_per_boundary: Integer;
        word_lm_boundary_count: Integer;
        word_lm_boundary_min: Integer;
        word_lm_boundary_max: Integer;
        word_lm_boundary_first: Integer;
        word_lm_boundary_last: Integer;
        word_lm_supported_ratio: Integer;
        word_lm_strong_ratio: Integer;
        word_lm_trigram_ratio: Integer;
        word_lm_zero_count: Integer;
        lexical_weight_sum: Integer;
        lexical_weight_min: Integer;
        lexical_weight_max: Integer;
        lexical_weight_mean: Integer;
        lexical_weight_per_unit: Integer;
        lexical_known_ratio: Integer;
        lexical_top_ratio: Integer;
        lexical_rank_sum: Integer;
        lexical_rank_max: Integer;
        lexical_margin_sum: Integer;
        lexical_margin_min: Integer;
        first_segment_weight: Integer;
        last_segment_weight: Integer;
        segments: Integer;
        single_segments: Integer;
        multi_segments: Integer;
        max_segment_units: Integer;
        min_segment_units: Integer;
        segment_units_square_sum: Integer;
        first_segment_units: Integer;
        last_segment_units: Integer;
        anchor_units: Integer;
        has_anchor: Boolean;
        baseline_lineage: Boolean;
        retained_exact_edge_count: Integer;
        retained_exact_edge_position: Integer;
        retained_exact_edge_units: Integer;
        retained_exact_edge_rank: Integer;
        retained_exact_edge_top_weight: Integer;
        retained_exact_edge_confidence: Int64;
        has_left_context: Boolean;
        query_path_bonus: Integer;
        query_path_penalty: Integer;
        score_per_segment: Integer;
        target: Boolean;
        text: string;
        segment_path: string;
    end;

    TncLongChainCandidateDebugArray = TArray<TncLongChainCandidateDebug>;

    TncLongFinalCandidateDebug = record
        legacy_rank: Integer;
        final_rank: Integer;
        legacy_top: Boolean;
        chain_rank: Integer;
        chain_present: Boolean;
        candidate_score: Integer;
        dict_weight: Integer;
        has_dict_weight: Boolean;
        source_user: Boolean;
        source_chain: Boolean;
        source_pattern: Boolean;
        source_redup: Boolean;
        source_local_rerank: Boolean;
        source_rule_fallback: Boolean;
        source_direct_generation: Boolean;
        direct_generation_rank: Integer;
        direct_generation_score: Single;
        direct_generation_score_delta: Single;
        direct_generation_existing_rank: Integer;
        chain_first_stage_score: Integer;
        chain_second_stage_score: Int64;
        chain_score_gap: Int64;
        complete_match: Boolean;
        partial_match: Boolean;
        text_units: Integer;
        comment_length: Integer;
        unit_delta: Integer;
        path_available: Boolean;
        path_confidence_score: Integer;
        path_confidence_tier: Integer;
        path_segments: Integer;
        path_single_segments: Integer;
        path_max_segment_units: Integer;
        char_lm_score: Integer;
        char_lm_suffix_score: Integer;
        char_lm_context_score: Integer;
        char_lm_context_gain: Integer;
        has_left_context: Boolean;
        query_choice_bonus: Integer;
        latest_query_choice: Boolean;
        query_path_bonus: Integer;
        query_path_penalty: Integer;
        word_lm_bonus: Integer;
        word_lm_boundary_count: Integer;
        word_lm_boundary_min: Integer;
        word_lm_boundary_max: Integer;
        word_lm_boundary_first: Integer;
        word_lm_boundary_last: Integer;
        word_lm_supported_ratio: Integer;
        word_lm_strong_ratio: Integer;
        word_lm_trigram_ratio: Integer;
        word_lm_zero_count: Integer;
        input_syllable_count: Integer;
        score_per_unit: Integer;
        dict_weight_per_unit: Integer;
        complete_user: Boolean;
        complete_dictionary: Boolean;
        complete_chain: Boolean;
        complete_pool_present: Boolean;
        complete_pool_source_kind: Integer;
        complete_pool_rank: Integer;
        complete_pool_seed_rank: Integer;
        complete_pool_original: Boolean;
        complete_pool_substitutions: Integer;
        complete_pool_changed_position: Integer;
        complete_pool_anchor_present: Boolean;
        complete_pool_anchor_start: Integer;
        complete_pool_anchor_units: Integer;
        complete_pool_anchor_exact_rank: Integer;
        complete_pool_anchor_source_weight: Integer;
        complete_pool_anchor_replacement_weight: Integer;
        complete_pool_anchor_top_weight: Integer;
        complete_pool_anchor_weight_gain: Integer;
        complete_pool_pair_evidence: Integer;
        complete_pool_proper_name_confidence: Integer;
        complete_pool_signature_support: Integer;
        complete_pool_consensus_support: Integer;
        complete_pool_consensus_seed_count: Integer;
        complete_pool_consensus_support_mean: Integer;
        complete_pool_consensus_support_min: Integer;
        complete_pool_consensus_majority_units: Integer;
        complete_pool_consensus_unanimous_units: Integer;
        complete_pool_consensus_nearest_distance: Integer;
        complete_pool_consensus_mean_distance: Integer;
        complete_pool_consensus_changed_support: Integer;
        complete_pool_consensus_changed_top_match: Boolean;
        complete_pool_local_pairwise_score: Integer;
        complete_pool_edge_model_anchor_count: Integer;
        complete_pool_edge_model_score_total: Integer;
        complete_pool_edge_model_score_max: Integer;
        complete_pool_edge_model_word_count: Integer;
        complete_pool_edge_model_word_score_total: Integer;
        complete_pool_edge_model_word_score_min: Integer;
        complete_pool_edge_model_word_score_max: Integer;
        complete_pool_edge_model_word_score_mean: Integer;
        exact_edge_shadow_rank: Integer;
        exact_edge_auditor_score: Int64;
        exact_edge_auditor_threshold: Int64;
        exact_edge_auditor_decision: Integer;
        exact_edge_fallback_auditor_score: Int64;
        exact_edge_fallback_auditor_threshold: Int64;
        exact_edge_fallback_auditor_decision: Integer;
        exact_edge_pre_auditor_rank: Integer;
        ranker_score: Int64;
        abstain_score: Int64;
        ranker_applied: Boolean;
        top2_pairwise_score: Int64;
        top2_pairwise_eligible: Boolean;
        top2_pairwise_swapped: Boolean;
        exact_anchor_pairwise_score: Int64;
        exact_anchor_pairwise_eligible: Boolean;
        exact_anchor_pairwise_moved: Boolean;
        exact_anchor_pairwise_target_rank: Integer;
        exact_anchor_top_local_word_lm: array[0..8] of Integer;
        exact_anchor_candidate_local_word_lm: array[0..8] of Integer;
        complete_pool_final_pairwise_score: Int64;
        complete_pool_final_pairwise_eligible: Boolean;
        complete_pool_final_pairwise_promoted: Boolean;
        complete_pool_final_pairwise_current_rank: Integer;
        complete_pool_post_static_difference_score: Int64;
        complete_pool_post_static_difference_eligible: Boolean;
        complete_pool_post_static_difference_promoted: Boolean;
        complete_pool_post_static_difference_current_rank: Integer;
        local_difference_score: Int64;
        local_difference_promoted: Boolean;
        local_difference_current_rank: Integer;
        local_different_units: Integer;
        local_different_runs: Integer;
        local_max_different_run: Integer;
        local_same_prefix_units: Integer;
        local_same_suffix_units: Integer;
        local_difference_span_units: Integer;
        local_top_lm_r0: Integer;
        local_top_lm_r1: Integer;
        local_top_lm_r2: Integer;
        local_top_lm_r3: Integer;
        local_candidate_lm_r0: Integer;
        local_candidate_lm_r1: Integer;
        local_candidate_lm_r2: Integer;
        local_candidate_lm_r3: Integer;
        ranking_top1_votes: Integer;
        ranking_top2_votes: Integer;
        ranking_first_top_stage: Integer;
        ranking_last_top_stage: Integer;
        ranking_top_entry_count: Integer;
        ranking_pre_settled_top: Boolean;
        unified_top2_arbiter_score: Double;
        unified_top2_arbiter_family: Integer;
        unified_top2_arbiter_swapped: Boolean;
        unified_top2_arbiter_features: TArray<Double>;
        target: Boolean;
        text: string;
        comment: string;
        segment_path: string;
    end;

    TncLongFinalCandidateDebugArray = TArray<TncLongFinalCandidateDebug>;

    TncLongNeuralCompletionRequest = record
        query_prefix: string;
        query_syllables: string;
        context_text: string;
        phonetic_only: Boolean;
        top1_text: string;
        top1_path: string;
        top1_anchor_path: string;
        top2_text: string;
        top2_path: string;
        top2_anchor_path: string;
    end;

    TncLongNeuralCompletionCandidate = record
        suffix_text: string;
        suffix_pinyin_path: string;
        suffix_path: string;
        base_rank: Integer;
        replace_units: Integer;
        score: Single;
    end;

    TncLongNeuralCompletionCandidateArray =
        TArray<TncLongNeuralCompletionCandidate>;

    TncLongNeuralCompletionResult = record
        suffix_text: string;
        suffix_pinyin_path: string;
        suffix_path: string;
        base_rank: Integer;
        replace_units: Integer;
        confidence: Single;
        candidates: TncLongNeuralCompletionCandidateArray;
    end;

    TncLongGeneratedCandidate = record
        text: string;
        normalized_score: Single;
        rank: Integer;
    end;

    TncLongGeneratedCandidateArray = TArray<TncLongGeneratedCandidate>;

    IncLongNeuralReranker = interface
        ['{E3B83F1A-8746-4E9D-8130-79E0EB9840D8}']
        function should_generate(const query_text: string;
            const candidates: TncLongFinalCandidateDebugArray): Boolean;
        function try_generate(const query_text: string;
            out candidates: TncLongGeneratedCandidateArray): Boolean;
        function try_select(const query_text: string;
            const candidates: TncLongFinalCandidateDebugArray;
            out selected_index: Integer): Boolean;
    end;

    IncLongLocalRepair = interface
        ['{C94752D8-309D-4F6D-9990-4CC9B38D4473}']
        procedure set_document_context(const document_key, preceding_text: string);
        function try_repair(const query_text, draft_text: string;
            const document_key, preceding_text: string;
            out repaired_text, aligned_pinyin: string;
            out minimum_word_ratio: Double): Boolean;
    end;

    IncLongLocalRepairPolicy = interface
        ['{BB45A804-567C-4FE5-AC84-4439A7AFF353}']
        function allows_no_context_refinement: Boolean;
    end;

    IncLongJointRepair = interface
        ['{2064E122-3F7B-4475-B5C6-FCC98AD63949}']
        function joint_ready: Boolean;
        function try_finalize(const dictionary: TncDictionaryProvider;
            const query_text, draft, path, current, second, aligned_pinyin: string;
            const document_key, preceding_text: string;
            out selected: TncValidatedRepairPath): Boolean;
    end;

    TncLocalRepairGuardDebug = record
        query_text, draft, proposal, segment_path, aligned_pinyin, guarded: string;
        invoked, accepted: Boolean;
    end;

    TncLongRankingStageDebug = record
        stage_name: string;
        top1_text: string;
        top2_text: string;
    end;

    TncLongRankingStageDebugArray = TArray<TncLongRankingStageDebug>;

    TncLongChainRuntimeCandidate = record
        text: string;
        segment_path: string;
        feature_query_key: string;
        feature_context_key: string;
        rank: Integer;
        original_rank: Integer;
        first_stage_score: Integer;
        second_stage_score: Int64;
        features_valid: Boolean;
        char_lm_score: Integer;
        char_lm_suffix_score: Integer;
        char_lm_context_score: Integer;
        path_segments: Integer;
        path_single_segments: Integer;
        path_max_segment_units: Integer;
        query_path_bonus: Integer;
        query_path_penalty: Integer;
        word_lm_bonus: Integer;
        word_lm_boundary_count: Integer;
        word_lm_boundary_min: Integer;
        word_lm_boundary_max: Integer;
        word_lm_boundary_first: Integer;
        word_lm_boundary_last: Integer;
        word_lm_supported_ratio: Integer;
        word_lm_strong_ratio: Integer;
        word_lm_trigram_ratio: Integer;
        word_lm_zero_count: Integer;
        retained_exact_edge_count: Integer;
        retained_exact_edge_position: Integer;
        retained_exact_edge_units: Integer;
        retained_exact_edge_rank: Integer;
        retained_exact_edge_top_weight: Integer;
        retained_exact_edge_confidence: Int64;
    end;

    TncLongChainRuntimeCandidateArray = TArray<TncLongChainRuntimeCandidate>;

    TncLongCompletePoolSource = (
        lcps_visible,
        lcps_chain,
        lcps_existing,
        lcps_local_repair,
        lcps_exact_pair,
        lcps_proper_name,
        lcps_exact_lattice,
        lcps_exact_edge_chain
    );

    TncLongCompletePoolRuntimeCandidate = record
        candidate: TncCandidate;
        source_index: Integer;
        segment_path: string;
        segmentation_signature: string;
        source_kind: TncLongCompletePoolSource;
        seed_rank: Integer;
        seed_visible_index: Integer;
        original: Boolean;
        substitutions: Integer;
        changed_position: Integer;
        source_char_weight: Integer;
        replacement_char_weight: Integer;
        anchor_start: Integer;
        anchor_units: Integer;
        anchor_exact_rank: Integer;
        anchor_top_weight: Integer;
        edge_retention_score: Integer;
        char_lm_score: Integer;
        char_lm_suffix_score: Integer;
        char_lm_context_score: Integer;
        word_lm_bonus: Integer;
        word_lm_boundary_count: Integer;
        word_lm_boundary_min: Integer;
        word_lm_boundary_max: Integer;
        word_lm_boundary_first: Integer;
        word_lm_boundary_last: Integer;
        word_lm_supported_ratio: Integer;
        word_lm_strong_ratio: Integer;
        word_lm_trigram_ratio: Integer;
        word_lm_zero_count: Integer;
        pair_evidence: Integer;
        proper_name_confidence: Integer;
        signature_support: Integer;
        consensus_support: Integer;
        consensus_seed_count: Integer;
        consensus_support_mean: Double;
        consensus_support_min: Double;
        consensus_majority_units: Integer;
        consensus_unanimous_units: Integer;
        consensus_nearest_distance: Integer;
        consensus_mean_distance: Double;
        consensus_changed_support: Double;
        consensus_changed_top_match: Integer;
        local_pairwise_score: Integer;
        local_pairwise_insert_rank: Integer;
        pool_rank: Integer;
        visible_evidence: Boolean;
        edge_model_anchor_count: Integer;
        edge_model_score_total: Integer;
        edge_model_score_max: Integer;
        edge_model_word_count: Integer;
        edge_model_word_score_total: Integer;
        edge_model_word_score_min: Integer;
        edge_model_word_score_max: Integer;
        edge_model_word_score_mean: Integer;
    end;

    TncLongCompletePoolRuntimeCandidateArray =
        TArray<TncLongCompletePoolRuntimeCandidate>;

    TncLongBeamStateDebugArray = TArray<TncLongBeamStateDebug>;
    TncLongExactEdgeStageDebugArray = TArray<TncLongExactEdgeStageDebug>;
    TncLongRetainedExactEdgeArray = TArray<TncLongRetainedExactEdge>;

    TncEngine = class
    private
        m_config: TncEngineConfig;
        m_composition_text: string;
        m_composition_display_text: string;
        m_candidates: TncCandidateList;
        m_one_key_completion: TncOneKeyCompletion;
        m_tab_projection_shown: TncOneKeyCompletion;
        m_tab_projection_query, m_tab_projection_document: string;
        m_tab_projection_raw_text, m_tab_projection_raw_path: string;
        m_debug_lexical_one_key_completion: TncOneKeyCompletion;
        m_debug_long_one_key_completion_pool: TncOneKeyCompletionList;
        m_debug_capture_long_one_key_completion_pool: Boolean;
        m_one_key_completion_query_prefix: string;
        m_one_key_completion_score: Integer;
        m_long_neural_completion_request: TncLongNeuralCompletionRequest;
        m_has_long_neural_completion_request: Boolean;
        m_long_neural_completion_prefix_locked: Boolean;
        m_completion_feedback_origin_prefix: string;
        m_allow_one_key_completion_lookup: Boolean;
        m_visible_candidates_cache: TncCandidateList;
        m_visible_candidate_source_indices_cache: TArray<Integer>;
        m_visible_candidates_cache_composition_text: string;
        m_visible_candidates_cache_lookup_key: string;
        m_visible_candidates_cache_page_index: Integer;
        m_visible_candidates_cache_page_size: Integer;
        m_visible_candidates_cache_valid: Boolean;
        m_long_visible_candidate_pool_cache: TncCandidateList;
        m_long_visible_candidate_pool_source_indices_cache: TArray<Integer>;
        m_long_visible_candidate_pool_cache_key: string;
        m_long_visible_candidate_pool_source_signature: UInt64;
        m_long_visible_candidate_pool_cache_valid: Boolean;
        m_long_local_rerank_generated_text: string;
        m_long_local_rerank_composition_text: string;
        m_long_local_rerank_lookup_key: string;
        m_forced_visible_top_candidate: TncCandidate;
        m_forced_visible_top_composition_text: string;
        m_forced_visible_top_lookup_key: string;
        m_has_forced_visible_top_candidate: Boolean;
        m_forced_visible_top_from_incremental_lm_prefix: Boolean;
        m_dictionary: TncDictionaryProvider;
        m_cached_dictionary_simplified: TncDictionaryProvider;
        m_cached_dictionary_traditional: TncDictionaryProvider;
        m_defer_optional_dictionary_models: Boolean;
        m_configured_dictionary_path_simplified: string;
        m_configured_dictionary_path_traditional: string;
        m_configured_user_dictionary_path: string;
        m_dictionary_paths_configured: Boolean;
        m_candidate_navigation_started: Boolean;
        m_dictionary_path: string;
        m_dictionary_write_time: TDateTime;
        m_user_dictionary_path: string;
        m_user_dictionary_write_time: TDateTime;
        m_last_dictionary_reload_check_tick: UInt64;
        m_left_context: string;
        m_external_left_context: string;
        m_document_context_model: TncDocumentContextModel;
        m_segment_left_context: string;
        m_context_pairs: TDictionary<string, Integer>;
        m_context_order: TQueue<string>;
        m_phrase_context_pairs: TDictionary<string, Integer>;
        m_phrase_context_order: TQueue<string>;
        m_phrase_context_last_seen: TDictionary<string, Int64>;
        m_session_text_counts: TDictionary<string, Integer>;
        m_session_text_last_seen: TDictionary<string, Int64>;
        m_session_text_order: TQueue<string>;
        m_session_query_choice_counts: TDictionary<string, Integer>;
        m_session_query_choice_last_seen: TDictionary<string, Int64>;
        m_session_query_choice_order: TQueue<string>;
        m_session_query_latest_text: TDictionary<string, string>;
        m_session_query_path_choice_counts: TDictionary<string, Integer>;
        m_session_query_path_choice_last_seen: TDictionary<string, Int64>;
        m_session_query_path_choice_order: TQueue<string>;
        m_session_query_path_penalty_counts: TDictionary<string, Integer>;
        m_session_query_path_penalty_last_seen: TDictionary<string, Int64>;
        m_session_query_path_penalty_order: TQueue<string>;
        m_session_ranked_query_paths: TDictionary<string, string>;
        m_session_ranked_query_path_scores: TDictionary<string, Integer>;
        m_session_ranked_query_path_order: TQueue<string>;
        m_session_context_query_choice_counts: TDictionary<string, Integer>;
        m_session_context_query_choice_last_seen: TDictionary<string, Int64>;
        m_session_context_query_choice_order: TQueue<string>;
        m_session_context_query_latest_text: TDictionary<string, string>;
        m_session_commit_serial: Int64;
        m_last_output_commit_text: string;
        m_prev_output_commit_text: string;
        m_context_db_bonus_cache_key: string;
        m_context_db_bonus_cache: TDictionary<string, Integer>;
        m_lookup_text_unit_count_cache: TDictionary<string, Integer>;
        m_lookup_text_units_cache: TDictionary<string, TArray<string>>;
        m_lookup_session_bonus_cache: TDictionary<string, Integer>;
        m_lookup_query_bonus_cache: TDictionary<string, Integer>;
        m_lookup_query_latest_text_cache: TDictionary<string, string>;
        m_lookup_query_path_bonus_cache: TDictionary<string, Integer>;
        m_lookup_context_query_bonus_cache: TDictionary<string, Integer>;
        m_lookup_context_query_latest_bonus_cache: TDictionary<string, Integer>;
        m_lookup_phrase_context_bonus_cache: TDictionary<string, Integer>;
        m_lookup_text_context_bonus_cache: TDictionary<string, Integer>;
        m_lookup_context_bonus_cache: TDictionary<string, Integer>;
        m_lookup_segment_path_context_bonus_cache: TDictionary<string, Integer>;
        m_lookup_segment_path_preference_cache: TDictionary<string, Integer>;
        m_lookup_candidate_path_confidence_cache: TDictionary<string, Integer>;
        m_lookup_single_char_match_cache: TDictionary<string, Integer>;
        m_lookup_inferred_segment_path_cache: TDictionary<string, TncInferredPathCacheValue>;
        m_lookup_char_lm_full_score_cache: TDictionary<string, Integer>;
        m_lookup_char_lm_suffix_score_cache: TDictionary<string, Integer>;
        m_lookup_char_lm_context_score_cache: TDictionary<string, Integer>;
        m_lookup_char_lm_span_score_cache: TDictionary<string, Integer>;
        m_lookup_char_lm_reverse_score_cache: TDictionary<string, Integer>;
        m_lookup_long_path_structure_cache: TDictionary<string, TncLongPathStructureCacheValue>;
        m_lookup_exact_lexical_feature_cache: TDictionary<string, TncExactLexicalFeatureCacheValue>;
        m_lookup_display_feature_cache: TDictionary<string, Integer>;
        m_lookup_admin_place_alias_cache: TDictionary<string, Boolean>;
        m_full_pinyin_key_cache: TDictionary<string, Boolean>;
        m_effective_pinyin_parse_cache: TDictionary<string, TncPinyinParseResult>;
        m_build_lookup_cache: TDictionary<string, TncCandidateList>;
        m_long_local_single_options_cache: TDictionary<string, TncCandidateList>;
        m_current_segment_path_map: TDictionary<string, string>;
        m_current_segment_path_score_map: TDictionary<string, Integer>;
        m_current_segment_path_query_prefix_map: TDictionary<string, string>;
        m_candidate_segment_paths: TArray<string>;
        m_short_two_single_pair_query: string;
        m_short_two_single_pair_texts: TArray<string>;
        m_short_three_exact_pair_query: string;
        m_short_three_exact_pair_texts: TArray<string>;
        m_short_three_char_pair_path: string;
        m_short_three_char_pair_evidence: Integer;
        m_short_exact_lexicon_completion_query: string;
        m_short_exact_lexicon_completion_texts: TArray<string>;
        m_short_exact_lexicon_completion_candidates: TncCandidateList;
        m_short_exact_pair_tail_query: string;
        m_short_exact_pair_tail_candidates: TncCandidateList;
        m_pending_commit_text: string;
        m_pending_commit_remaining: string;
        m_pending_commit_remaining_input_code: string;
        m_has_pending_commit: Boolean;
        m_pending_commit_allow_learning: Boolean;
        m_pending_commit_learning_locked: Boolean;
        m_pending_commit_explicit_choice: Boolean;
        m_pending_commit_literal_choice: Boolean;
        m_pending_commit_fuzzy_choice: Boolean;
        m_pending_commit_fuzzy_text: string;
        m_pending_commit_segment_path: string;
        m_pending_commit_query_key: string;
        m_last_debug_commit_segment_path: string;
        m_last_lookup_key: string;
        m_last_lookup_normalized_from: string;
        m_last_lookup_syllable_count: Integer;
        m_last_three_syllable_partial_preference_kind: Integer;
        m_last_three_syllable_head_exact_text: string;
        m_last_three_syllable_head_strength: Integer;
        m_last_three_syllable_tail_strength: Integer;
        m_last_three_syllable_first_single_strength: Integer;
        m_last_three_syllable_last_single_strength: Integer;
        m_last_three_syllable_head_path_bonus: Integer;
        m_last_three_syllable_tail_path_bonus: Integer;
        m_last_three_syllable_partial_debug_info: string;
        m_last_full_path_debug_info: string;
        m_last_lookup_debug_extra: string;
        m_last_lookup_timing_info: string;
        m_last_lookup_all_initial_fast: Boolean;
        m_last_lookup_literal_user_fast: Boolean;
        m_last_lookup_prefix_partial_fast: Boolean;
        m_last_lookup_short312_head_fast: Boolean;
        m_last_lookup_incremental_partial_reuse: Boolean;
        m_last_ranked_query_key: string;
        m_last_ranked_top_path: string;
        m_last_candidates_incrementally_reusable: Boolean;
        m_composition_built_incrementally: Boolean;
        m_runtime_chain_text: string;
        m_runtime_common_pattern_text: string;
        m_runtime_redup_text: string;
        m_disable_subspan_standalone_oracle: Integer;
        m_debug_target_recall_text: string;
        m_debug_target_final_state_found: Boolean;
        m_debug_target_final_state_rank: Integer;
        m_debug_target_final_state_count: Integer;
        m_debug_target_max_generated_syllable: Integer;
        m_debug_target_max_retained_syllable: Integer;
        m_debug_target_chain_generated_syllable: Integer;
        m_debug_target_chain_prebeam_syllable: Integer;
        m_debug_target_chain_beam_syllable: Integer;
        m_debug_target_full_path_generated_syllable: Integer;
        m_debug_target_full_path_retained_syllable: Integer;
        m_debug_target_final_state_source_mask: Integer;
        m_debug_target_chain_final_state_rank: Integer;
        m_debug_target_light_final_state_rank: Integer;
        m_debug_target_lattice_final_state_rank: Integer;
        m_debug_target_oracle_final_state_rank: Integer;
        m_debug_target_full_final_state_rank: Integer;
        m_debug_target_chain_build_attempted: Boolean;
        m_debug_target_chain_build_accepted: Boolean;
        m_debug_target_chain_top_score: Integer;
        m_debug_target_chain_min_score: Integer;
        m_debug_target_chain_search_status: Integer;
        m_debug_target_candidate_stage_mask: Integer;
        m_debug_target_full_path_budget_exhausted: Boolean;
        m_debug_capture_long_beam_states: Boolean;
        m_debug_capture_long_final_candidates: Boolean;
        m_debug_expand_long_beam_states: Boolean;
        m_debug_long_beam_states: TncLongBeamStateDebugArray;
        m_debug_long_exact_edge_stages: TncLongExactEdgeStageDebugArray;
        m_debug_long_exact_edge_stage_index: TDictionary<string, Integer>;
        m_debug_long_chain_candidates: TncLongChainCandidateDebugArray;
        m_debug_long_final_candidates: TncLongFinalCandidateDebugArray;
        m_debug_capture_long_ranking_stages: Boolean;
        m_debug_long_ranking_stages: TncLongRankingStageDebugArray;
        m_runtime_long_chain_candidates: TncLongChainRuntimeCandidateArray;
        m_runtime_long_complete_pool_candidates:
            TncLongCompletePoolRuntimeCandidateArray;
        m_runtime_long_retained_exact_edges: TncLongRetainedExactEdgeArray;
        m_long_neural_reranker: IncLongNeuralReranker;
        m_long_local_repair: IncLongLocalRepair;
        m_long_local_repair_policy: IncLongLocalRepairPolicy;
        m_long_joint_repair: IncLongJointRepair;
        m_local_repair_query_key, m_local_repair_text, m_local_repair_draft: string;
        m_local_repair_original_path: string;
        m_local_repair_baseline_text: string;
        m_local_repair_validated: TncValidatedRepairPath;
        m_repaired_completion_query_key: string;
        m_debug_capture_local_repair: Boolean;
        m_debug_local_repair_guard: TncLocalRepairGuardDebug;
        m_long_complete_pool_pairwise_text: string;
        m_long_complete_pool_pairwise_insert_rank: Integer;
        m_debug_long_chain_beam_width: Integer;
        m_debug_long_chain_prebeam_width: Integer;
        m_debug_long_chain_option_limit: Integer;
        m_debug_long_chain_probe_limit: Integer;
        m_debug_long_chain_ranker_profile: Integer;
        m_debug_long_final_ranker_profile: Integer;
        m_debug_disable_long_top2_pairwise_swap: Boolean;
        m_search_budget_mode: TncSearchBudgetMode;
        m_search_budget_scale_percent: Integer;
        m_single_quote_open: Boolean;
        m_double_quote_open: Boolean;
        m_page_index: Integer;
        m_selected_index: Integer;
        m_confirmed_text: string;
        m_confirmed_explicit_choice: Boolean;
        m_recent_partial_prefix_text: string;
        m_prev_committed_text: string;
        m_prev_committed_pinyin: string;
        m_last_committed_text: string;
        m_last_committed_pinyin: string;
        m_confirmed_segments: TList<TncConfirmedSegment>;
        function is_alpha_key(const key_code: Word; const key_state: TncKeyState; out normalized_char: Char;
            out display_char: Char): Boolean;
        function get_candidate_limit: Integer;
        function get_candidate_page_size: Integer;
        function get_total_candidate_limit: Integer;
        function is_fuzzy_pinyin_active: Boolean; inline;
        function create_dictionary_from_config: TncDictionaryProvider;
        function take_cached_dictionary_provider(const variant: TncDictionaryVariant;
            const base_path: string; const user_path: string): TncDictionaryProvider;
        procedure store_current_dictionary_provider(const previous_config: TncEngineConfig);
        procedure clear_cached_dictionary_providers;
        procedure inherit_search_budget_policy_to(const engine: TncEngine);
        function search_budget_should_stop(const start_tick: UInt64;
            const work_count: Int64; const work_limit: Int64;
            const timed_budget_ms: UInt64;
            const emergency_budget_ms: UInt64;
            const apply_configured_policy: Boolean = True): Boolean;
        procedure free_dictionary_provider(var provider: TncDictionaryProvider);
        function get_dictionary_path_for_variant(
            const variant: TncDictionaryVariant): string;
        function get_configured_user_dictionary_path: string;
        function get_active_dictionary_path: string;
        function create_dictionary_from_paths(const base_path, user_path: string): TncDictionaryProvider;
        function get_dictionary_write_time(const path: string): TDateTime;
        function visible_candidates_cache_is_current(
            const page_size: Integer): Boolean;
        procedure cache_visible_candidate_page(const candidates: TncCandidateList;
            const source_indices: TArray<Integer>; const page_size: Integer);
        function get_candidate_state_signature: UInt64;
        function get_long_visible_candidate_pool_cache_key(
            const page_size: Integer): string;
        function long_visible_candidate_pool_cache_is_current(
            const page_size: Integer): Boolean;
        function get_current_page_candidate_count(
            const page_size: Integer): Integer;
        function get_page_count_internal(const page_size: Integer): Integer;
        procedure normalize_page_and_selection;
        procedure apply_visible_local_repair(var candidates: TncCandidateList;
            var source_indices: TArray<Integer>; const expected_units: Integer);
        function has_validated_completion_prefix: Boolean;
        function has_current_validated_completion_prefix: Boolean;
        function project_validated_prefix_completion(
            const completion: TncOneKeyCompletion): TncOneKeyCompletion;
        function get_one_key_completion_for_commit: TncOneKeyCompletion;
        procedure refresh_validated_prefix_completion;
        function get_source_rank(const source: TncCandidateSource): Integer;
        function get_context_variants(const context_text: string): TArray<string>;
        function get_session_text_bonus(const candidate_text: string): Integer;
        function build_session_query_choice_key(const query_key: string; const candidate_text: string): string;
        function build_session_query_path_choice_key(const query_key: string; const encoded_path: string): string;
        function build_context_query_scope_key(const context_text: string; const query_key: string): string;
        function build_context_query_choice_key(const context_text: string; const query_key: string;
            const candidate_text: string): string;
        function get_session_query_bonus(const candidate_text: string): Integer;
        function get_session_query_path_bonus(const query_key: string; const encoded_path: string): Integer;
        function get_session_query_path_penalty(const query_key: string; const encoded_path: string): Integer;
        function get_session_query_path_prefix_bonus(const query_key: string; const encoded_path: string): Integer;
        function get_session_query_path_prefix_penalty(const query_key: string; const encoded_path: string): Integer;
        function get_session_ranked_query_path_bonus(const encoded_path: string): Integer;
        function get_persistent_query_path_prefix_support(const encoded_path: string): Integer;
        function get_context_query_bonus(const candidate_text: string): Integer;
        function get_context_query_latest_bonus(const candidate_text: string): Integer;
        function is_latest_session_query_choice(const candidate_text: string): Boolean;
        function get_phrase_context_bonus(const candidate_text: string): Integer;
        function get_text_context_bonus(const candidate_text: string): Integer;
        function get_context_bonus(const candidate_text: string): Integer;
        function get_segment_path_preference_score(const encoded_path: string): Integer;
        function get_incremental_path_stability_bonus_for_path(const encoded_path: string): Integer;
        function get_incremental_path_stability_bonus(const candidate: TncCandidate): Integer;
        function get_segment_path_support_score(const candidate: TncCandidate): Integer;
        procedure get_recent_path_context_seed(out prev_prev_text: string; out prev_text: string);
        function get_segment_path_context_bonus(const candidate: TncCandidate): Integer;
        function get_candidate_debug_summary(const candidate: TncCandidate): string;
        function get_punctuation_char(const key_code: Word; const key_state: TncKeyState; out out_char: Char): Boolean;
        function get_effective_punctuation_full_width: Boolean;
        function map_full_width_char(const input_char: Char): string;
        function map_punctuation_char(const input_char: Char): string;
        function get_direct_ascii_commit_text(const key_code: Word; const key_state: TncKeyState;
            out out_text: string): Boolean;
        function get_raw_composition_commit_text: string;
        function get_candidate_text_unit_count(const text: string): Integer;
        function try_get_fixed_pronoun_tail_query_parts(const query_text: string;
            const expected_unit_count: Integer; out head_pinyin: string;
            out head_text: string; out tail_key: string;
            out tail_units: Integer): Boolean;
        function try_get_short_particle_tail_query_parts(const query_text: string;
            out tail_key: string; out tail_text: string;
            out head_key: string): Boolean;
        function is_nonlexicon_long_segment_path_candidate(const candidate: TncCandidate): Boolean;
        function is_runtime_chain_candidate(const candidate: TncCandidate): Boolean;
        function is_runtime_common_pattern_candidate(const candidate: TncCandidate): Boolean;
        function is_runtime_redup_candidate(const candidate: TncCandidate): Boolean;
        function get_runtime_candidate_kind(const candidate: TncCandidate): string;
        function get_candidate_path_confidence_score(const candidate: TncCandidate): Integer;
        function get_candidate_path_confidence_tier(const candidate: TncCandidate): Integer;
        function get_candidate_confidence_rank(const candidate: TncCandidate): Integer;
        function get_front_row_confidence_bonus(const candidate: TncCandidate): Integer;
        function get_multi_syllable_intent_layer(const candidate: TncCandidate): Integer;
        function get_candidate_segment_path_score_hint(const candidate: TncCandidate): Integer;
        function match_single_char_candidate_for_syllable(const syllable_text: string;
            const unit_text: string; out out_preferred: Boolean): Boolean;
        function is_weak_single_char_chain_candidate_for_query(const query_key: string;
            const candidate: TncCandidate): Boolean;
        function is_problematic_single_char_chain_candidate_for_query(const query_key: string;
            const candidate: TncCandidate): Boolean;
        function has_competing_exact_phrase_candidate(const selected_text: string): Boolean;
        function is_runtime_constructed_phrase_friendly(const text: string): Boolean;
        function get_rank_score(const candidate: TncCandidate): Integer;
        procedure sort_candidates(var candidates: TncCandidateList);
        procedure clear_lookup_bonus_caches;
        function is_low_evidence_admin_place_alias_cached(
            const pinyin: string; const text: string): Boolean;
        function get_cached_char_lm_scores(const texts: TArray<string>;
            out scores: TArray<Integer>; const score_mode: TncCharLmScoreMode;
            const left_context: string): Boolean;
        function get_cached_long_path_structure(const encoded_path: string;
            out value: TncLongPathStructureCacheValue): Boolean;
        function get_cached_query_segment_path_bonus(const query_key: string;
            const encoded_path: string): Integer;
        function get_cached_query_segment_path_penalty(const query_key: string;
            const encoded_path: string): Integer;
        function get_short_four_two_exact_path_evidence(
            const query_key: string; const text_value: string;
            out encoded_path: string; out evidence_score: Integer): Boolean;
        function get_short_four_transition_particle_path_evidence(
            const query_key: string; const text_value: string;
            out encoded_path: string; out evidence_score: Integer): Boolean;
        function is_current_short_two_single_pair_candidate_supported(
            const query_key: string; const candidate_text: string): Boolean;
        function is_current_short_three_exact_pair_candidate_supported(
            const query_key: string; const candidate_text: string): Boolean;
        function is_current_short_exact_lexicon_completion(
            const query_key: string; const candidate_text: string): Boolean;
        procedure clear_short_exact_pair_tail_handoff;
        procedure prepare_short_exact_pair_tail_handoff(
            const full_query: string; const selected_text: string;
            const remaining_pinyin: string);
        procedure apply_short_exact_pair_tail_handoff;
        function get_cached_query_latest_choice_text(
            const query_key: string): string;
        procedure clear_session_learning_after_user_removal;
        function build_candidate_identity_key(const candidate_text: string; const comment_text: string): string;
        procedure clear_segment_path_tracking;
        procedure remember_segment_path_for_candidate(const candidate_text: string; const comment_text: string;
            const encoded_path: string; const path_score_hint: Integer = Low(Integer));
        procedure remember_segment_path_score_hint_for_candidate(const candidate_text: string;
            const comment_text: string; const path_score_hint: Integer);
        procedure remember_segment_path_query_prefix(const encoded_path: string; const query_prefix: string);
        function get_query_prefix_for_segment_path(const encoded_path: string): string;
        function get_segment_path_for_candidate(const candidate: TncCandidate;
            const candidate_index: Integer = -1): string;
        function infer_segment_path_for_query_text_with_score(
            const query_text: string; const selected_text: string;
            out out_score: Integer; out out_segment_count: Integer;
            const allow_any_single_char: Boolean = False): string;
        function infer_segment_path_for_selected_text_with_score(
            const selected_text: string; out out_score: Integer;
            out out_segment_count: Integer): string;
        function infer_segment_path_for_selected_text(const selected_text: string): string;
        procedure refresh_candidate_segment_paths;
        procedure note_ranked_top_candidate;
        function normalize_pinyin_text(const input_text: string): string;
        function is_shuangpin_input: Boolean;
        procedure rebuild_shuangpin_composition_from_input_code;
        function get_shuangpin_remaining_input_code_for_partial(
            const selected_text: string): string;
        function is_valid_pinyin_syllable(const syllable: string): Boolean;
        function is_full_pinyin_key(const value: string): Boolean;
        function get_effective_compact_pinyin_syllables(const input_text: string;
            const allow_relaxed_split: Boolean = False): TncPinyinParseResult;
        function get_effective_compact_pinyin_unit_count(const input_text: string;
            const allow_relaxed_split: Boolean = False): Integer;
        function normalize_adjacent_swap_typo(const value: string): string;
        function split_text_units(const input_text: string): TArray<string>;
        function is_common_surname_text(const value: string): Boolean;
        function merge_candidate_lists(const primary_candidates: TncCandidateList;
            const secondary_candidates: TncCandidateList; const max_candidates: Integer): TncCandidateList;
        procedure build_candidates;
        procedure build_candidates_core;
        procedure clear_one_key_completion;
        procedure clear_one_key_completion_feedback_target;
        procedure record_one_key_completion_feedback_target(
            const full_pinyin, text: string);
        function try_refresh_document_copy_completion(
            const compact_query: string;
            const previous_completion: TncOneKeyCompletion;
            out completion: TncOneKeyCompletion;
            out completion_score: Integer): Boolean;
        function try_refresh_long_one_key_completion(
            const syllables: TncPinyinParseResult;
            const compact_query: string;
            const previous_completion: TncOneKeyCompletion;
            out completion: TncOneKeyCompletion;
            out completion_score: Integer): Boolean;
        procedure refresh_one_key_completion;
        function accept_one_key_completion: Boolean;
        procedure apply_long_complete_candidate_pool(
            var candidates: TncCandidateList;
            var source_indices: TArray<Integer>;
            const normalized_query: string;
            const query_syllables: TncPinyinParseResult;
            const expected_units: Integer);
        procedure apply_long_final_visible_candidate_ranking(
            var candidates: TncCandidateList;
            var source_indices: TArray<Integer>;
            const unified_pool_final: Boolean = False;
            const exact_edge_baseline_top1: string = '';
            const exact_edge_baseline_top2: string = '';
            const run_host_neural_reranker: Boolean = False);
        function build_segment_candidates(out out_candidates: TncCandidateList;
            const include_full_path: Boolean; out out_path_search_elapsed_ms: Int64;
            const allow_relaxed_missing_apostrophe: Boolean = False): Boolean;
        function build_pinyin_comment(const input_text: string;
            const allow_relaxed_missing_apostrophe: Boolean = False): string;
        procedure update_segment_left_context;
        procedure push_confirmed_segment(const text: string; const pinyin: string;
            const input_code: string; const canonical_pinyin: string = '';
            const explicit_choice: Boolean = False;
            const literal_choice: Boolean = False;
            const fuzzy_choice: Boolean = False);
        function pop_confirmed_segment(out out_segment: TncConfirmedSegment): Boolean;
        procedure rebuild_confirmed_text;
        function rollback_last_segment: Boolean;
        procedure apply_partial_commit(const selected_text: string; const remaining_pinyin: string;
            const segment_path: string = ''; const explicit_choice: Boolean = False;
            const remaining_input_code: string = '';
            const literal_choice: Boolean = False;
            const fuzzy_choice: Boolean = False);
        procedure update_left_context(const committed_text: string);
        procedure record_context_pair(const left_text: string; const committed_text: string);
        procedure note_session_commit(const text: string);
        procedure note_session_query_choice(const query_key: string; const text: string);
        procedure note_session_query_path_choice(const query_key: string; const encoded_path: string);
        procedure note_session_query_path_penalty(const query_key: string; const encoded_path: string);
        procedure note_session_ranked_query_path(const query_key: string; const encoded_path: string;
            const path_confidence_score: Integer);
        procedure note_session_context_query_choice(const context_text: string; const query_key: string;
            const text: string);
        procedure track_phrase_context_key(const phrase_key: string; const current_serial: Int64);
        procedure note_output_phrase_context(const committed_text: string);
        procedure note_segment_path_context(const encoded_path: string);
        procedure set_pending_commit(const text: string; const remaining_pinyin: string = '';
            const allow_learning: Boolean = True; const segment_path: string = '';
            const explicit_choice: Boolean = False; const remaining_input_code: string = '';
            const literal_choice: Boolean = False;
            const fuzzy_choice: Boolean = False;
            const fuzzy_choice_text: string = '';
            const learning_locked: Boolean = False);
        procedure clear_pending_commit;
        procedure update_dictionary_state;
        procedure invalidate_dictionary_lookup_caches;
        procedure toggle_input_mode;
        procedure reset_debug_target_recall_metrics;
        function is_debug_target_recall_prefix(const text: string): Boolean;
        procedure debug_note_long_exact_edge_stage(const position: Integer;
            const input_syllable_count: Integer; const query_key: string;
            const text: string; const chunk_units: Integer;
            const exact_rank: Integer; const raw_weight: Integer;
            const top_weight: Integer; const effective_score: Integer;
            const edge_score: Int64; const source_user: Boolean;
            const baseline_option: Boolean; const stage_mask: Integer);
        procedure debug_mark_long_exact_path_complete_pool(
            const segment_path: string);
    public
        constructor create(const config: TncEngineConfig;
            const defer_optional_dictionary_models: Boolean = False;
            const allow_one_key_completion_lookup: Boolean = True;
            const initialize_dictionary: Boolean = True;
            const initial_dictionary: TncDictionaryProvider = nil);
        destructor Destroy; override;
        procedure reset(const preserve_document_context: Boolean = False);
        procedure update_config(const config: TncEngineConfig);
        procedure configure_dictionary_paths(
            const simplified_dictionary_path: string;
            const traditional_dictionary_path: string;
            const user_dictionary_path: string);
        procedure set_dictionary_provider(const dictionary: TncDictionaryProvider);
        procedure set_long_neural_reranker(
            const reranker: IncLongNeuralReranker);
        function detach_dictionary_provider: TncDictionaryProvider;
        procedure adopt_ready_dictionary_provider(
            const dictionary: TncDictionaryProvider);
        function dictionary_models_deferred: Boolean;
        function rebuild_candidates_after_dictionary_upgrade: Boolean;
        function try_upgrade_dictionary_from(const ready_engine: TncEngine;
            out candidates_rebuilt: Boolean): Boolean;
        procedure reload_dictionary_if_needed;
        procedure prewarm_dictionary_caches;
        procedure set_external_left_context(const left_context: string;
            const document_key: string = '';
            const document_snapshot: string = '');
        function debug_query_one_key_completion(const query_text: string;
            const left_context: string): TncOneKeyCompletion;
        function debug_query_one_key_completion_candidates(
            const query_text: string;
            out completions: TncOneKeyCompletionList): Boolean;
        function debug_get_long_one_key_completion_pool(
            out completions: TncOneKeyCompletionList): Boolean;
        function debug_get_lexical_one_key_completion(
            out completion: TncOneKeyCompletion): Boolean;
        procedure debug_enable_long_one_key_completion_pool_capture(
            const enabled: Boolean);
        function debug_get_ranked_candidates(
            out candidates: TncCandidateList): Boolean;
        function debug_score_one_key_completion_candidates(
            const query_text: string; const left_context: string;
            out completions: TncOneKeyCompletionList;
            out char_lm_scores: TArray<Integer>;
            out reverse_char_lm_scores: TArray<Integer>;
            out prefix1_lm_scores: TArray<Integer>;
            out prefix2_lm_scores: TArray<Integer>): Boolean;
        function debug_score_one_key_completion_pool(
            const completions: TncOneKeyCompletionList;
            out char_lm_scores: TArray<Integer>;
            out suffix_lm_scores: TArray<Integer>;
            out reverse_char_lm_scores: TArray<Integer>;
            out path_lm_scores: TArray<Integer>): Boolean;
        procedure debug_set_target_recall_text(const text: string);
        procedure debug_enable_long_beam_state_capture(const enabled: Boolean);
        procedure debug_enable_long_final_candidate_capture(
            const enabled: Boolean);
        procedure debug_enable_long_beam_state_expansion(
            const enabled: Boolean);
        procedure debug_enable_long_ranking_stage_capture(
            const enabled: Boolean);
        procedure debug_set_long_chain_beam_widths(const beam_width: Integer;
            const prebeam_width: Integer);
        procedure debug_set_long_chain_option_limits(const option_limit: Integer;
            const probe_limit: Integer);
        procedure debug_set_long_chain_ranker_profile(const profile: Integer);
        procedure debug_set_long_final_ranker_profile(const profile: Integer);
        procedure debug_disable_long_top2_pairwise_swap(
            const disabled: Boolean);
        procedure debug_set_search_budget_policy(
            const mode: TncSearchBudgetMode;
            const scale_percent: Integer = 100);
        procedure debug_set_composition_text(const text: string);
        function process_key(const key_code: Word; const key_state: TncKeyState): Boolean;
        function get_candidates: TncCandidateList;
        function get_one_key_completion: TncOneKeyCompletion;
        function get_long_neural_completion_request(
            out request: TncLongNeuralCompletionRequest): Boolean;
        function get_prefetch_long_neural_completion_request(
            out request: TncLongNeuralCompletionRequest): Boolean;
        function apply_long_neural_completion(
            const request: TncLongNeuralCompletionRequest;
            const completion_result: TncLongNeuralCompletionResult): Boolean;
        function get_page_index: Integer;
        function get_page_count: Integer;
        function get_selected_index: Integer;
        function next_page: Boolean;
        function prev_page: Boolean;
        function get_composition_text: string;
        function get_preedit_text: string;
        function get_last_lookup_key: string;
        function get_display_text: string;
        function get_confirmed_length: Integer;
        function get_lookup_perf_info: string;
        function get_lookup_debug_info: string;
        function get_debug_last_output_commit_text: string;
        function get_debug_phrase_context_pair_count(const left_text: string; const candidate_text: string): Integer;
        function get_debug_last_commit_segment_path: string;
        function get_debug_candidate_segment_path(const candidate_index: Integer): string;
        procedure debug_enable_local_repair_capture(const enabled: Boolean);
        function debug_get_local_repair_guard: TncLocalRepairGuardDebug;
        function get_debug_target_recall(const target_text: string): TncTargetRecallDebug;
        function get_debug_long_beam_states: TncLongBeamStateDebugArray;
        function get_debug_long_exact_edge_stages:
            TncLongExactEdgeStageDebugArray;
        function get_debug_long_retained_exact_edges:
            TncLongRetainedExactEdgeArray;
        function get_debug_long_chain_candidates:
            TncLongChainCandidateDebugArray;
        function get_debug_long_final_candidates:
            TncLongFinalCandidateDebugArray;
        function get_debug_long_ranking_stages:
            TncLongRankingStageDebugArray;
        function get_debug_pending_commit_segment_path: string;
        function get_dictionary_debug_info: string;
        function should_handle_key(const key_code: Word; const key_state: TncKeyState): Boolean;
        function commit_text(out out_text: string): Boolean;
        function remove_user_candidate(const pinyin: string; const text: string): Boolean; overload;
        function remove_user_candidate(const pinyin: string; const text: string;
            const preserve_candidate: TncCandidate; const has_preserve_candidate: Boolean): Boolean; overload;
        function clear_user_dictionary: Boolean;
        function dictionary_base_ready: Boolean;
        function dictionary_user_ready: Boolean;
        function is_user_or_literal_entry(const pinyin: string;
            const text: string): Boolean;
        procedure notify_user_dictionary_cleared;
        property config: TncEngineConfig read m_config write update_config;
    end;

function short_nocontext_large_weight_gap_blocks_promotion(
    const baseline_weight: Integer; const challenger_weight: Integer;
    const baseline_has_protected_name_shape: Boolean): Boolean;
function short_residual_exact_weight_is_eligible(
    const candidate_weight: Integer;
    const is_baseline: Boolean): Boolean;
function long_exact_chunk_weight_is_eligible(
    const candidate_weight: Integer;
    const has_dict_weight: Boolean): Boolean;
function apply_long_top2_pairwise_index_swap(
    var ordered_indices: TArray<Integer>;
    const second_position: Integer;
    const eligible: Boolean;
    const score: Int64;
    const threshold: Int64): Boolean;
function apply_long_exact_anchor_pairwise_index_move(
    var ordered_indices: TArray<Integer>;
    const anchor_position: Integer;
    const score: Double;
    const strict_threshold: Double;
    const second_threshold: Double): Integer;
function nc_search_budget_should_stop(const mode: TncSearchBudgetMode;
    const elapsed_ms: UInt64; const work_count: Int64;
    const work_limit: Int64; const timed_budget_ms: UInt64;
    const emergency_budget_ms: UInt64;
    const scale_percent: Integer): Boolean;

implementation

uses
    nc_long_search_ranker_model,
    nc_long_edge_retention_model,
    nc_long_exact_edge_lattice_ranker_model,
    nc_long_exact_edge_candidate_auditor_model,
    nc_long_exact_edge_fallback_auditor_model,
    nc_long_second_stage_ranker_model,
    nc_long_pairwise_residual_model,
    nc_long_final_ranker_model,
    nc_long_final_abstain_model,
    nc_long_complete_pool_ranker_model,
    nc_long_complete_pool_abstain_model,
    nc_long_top2_pairwise_swap_model,
    nc_long_top2_pairwise_text_model,
    nc_long_top2_pairwise_difference_model,
    nc_long_exact_anchor_pairwise_model,
    nc_long_bidirectional_difference_model,
    nc_long_second_slot_bidirectional_model,
    nc_long_visible_pairwise_residual_model,
    nc_long_complete_pool_difference_model,
    nc_long_complete_pool_bidirectional_model,
    nc_long_settled_top2_residual_model,
    nc_long_unified_top2_arbiter_model,
    nc_long_unified_top2_evidence_model,
    nc_long_unified_top2_text_model,
    nc_long_second_slot_recovery_gate_model,
    nc_long_second_slot_selector_model,
    nc_long_post_second_slot_gate_model,
    nc_long_local_difference_residual_model,
    nc_long_local_pairwise_pool_model,
    nc_long_local_reranker_model,
    nc_long_local_residual_model,
    nc_short_context_reranker_model,
    nc_short_context_residual_model,
    nc_short_context_boundary_residual_model,
    nc_short_context_difference_model,
    nc_short_context_top2_difference_model,
    nc_short_context_expanded_evidence,
    nc_short_nocontext_residual_model,
    nc_one_key_completion_ranker_model,
    nc_one_key_completion_difference_model,
    nc_one_key_completion_topk_model,
    nc_one_key_completion_ncgpt_model,
    nc_one_key_completion_ncgpt_sparse_audit_model,
    nc_tab_repair_projection;

const
    c_suppress_nonlexicon_complete_long_candidates = True;
    c_long_sentence_exact_min_syllables = 3;
    c_long_sentence_complete_candidate_display_limit = 2;
    c_long_postprocess_emergency_budget_ms = 320;
    c_fast_repair_promote_margin = 6000;
    c_short_nocontext_protected_baseline_min_weight = 1000;
    c_short_residual_visibility_only_max_weight = 0;

function trim_left_context_to_sentence(const value: string;
    const max_length: Integer): string; forward;

function nc_search_budget_should_stop(const mode: TncSearchBudgetMode;
    const elapsed_ms: UInt64; const work_count: Int64;
    const work_limit: Int64; const timed_budget_ms: UInt64;
    const emergency_budget_ms: UInt64;
    const scale_percent: Integer): Boolean;
var
    normalized_scale: Integer;
    scaled_budget_ms: UInt64;
begin
    normalized_scale := EnsureRange(scale_percent, 25, 1000);
    case mode of
        sbm_deterministic:
            begin
                Exit((work_limit > 0) and (work_count >= work_limit));
            end;
        sbm_wall_clock:
            begin
                if (work_limit > 0) and (work_count >= work_limit) then
                begin
                    Exit(True);
                end;
                if timed_budget_ms = 0 then
                begin
                    Exit(False);
                end;
                scaled_budget_ms :=
                    (timed_budget_ms * UInt64(normalized_scale) + 99) div 100;
                Exit(elapsed_ms >= scaled_budget_ms);
            end;
    end;

    if (work_limit > 0) and (work_count >= work_limit) then
    begin
        Exit(True);
    end;
    if emergency_budget_ms = 0 then
    begin
        Exit(False);
    end;
    scaled_budget_ms :=
        (emergency_budget_ms * UInt64(normalized_scale) + 99) div 100;
    Result := elapsed_ms >= scaled_budget_ms;
end;

function short_residual_exact_weight_is_eligible(
    const candidate_weight: Integer;
    const is_baseline: Boolean): Boolean;
begin
    Result := is_baseline or
        (candidate_weight > c_short_residual_visibility_only_max_weight);
end;

function long_exact_chunk_weight_is_eligible(
    const candidate_weight: Integer;
    const has_dict_weight: Boolean): Boolean;
begin
    Result := (not has_dict_weight) or (candidate_weight > 0);
end;

function short_nocontext_large_weight_gap_blocks_promotion(
    const baseline_weight: Integer; const challenger_weight: Integer;
    const baseline_has_protected_name_shape: Boolean): Boolean;
begin
    Result := baseline_has_protected_name_shape and
        (baseline_weight >=
        c_short_nocontext_protected_baseline_min_weight) and
        (challenger_weight >= 0) and
        (challenger_weight < baseline_weight) and
        (Int64(challenger_weight) * 2 < Int64(baseline_weight));
end;

function apply_long_top2_pairwise_index_swap(
    var ordered_indices: TArray<Integer>;
    const second_position: Integer;
    const eligible: Boolean;
    const score: Int64;
    const threshold: Int64): Boolean;
var
    first_index: Integer;
begin
    Result := eligible and (Length(ordered_indices) >= 2) and
        (second_position > 0) and
        (second_position < Length(ordered_indices)) and
        (score >= threshold);
    if not Result then
    begin
        Exit;
    end;

    first_index := ordered_indices[0];
    ordered_indices[0] := ordered_indices[second_position];
    ordered_indices[second_position] := first_index;
end;

function apply_long_exact_anchor_pairwise_index_move(
    var ordered_indices: TArray<Integer>;
    const anchor_position: Integer;
    const score: Double;
    const strict_threshold: Double;
    const second_threshold: Double): Integer;
var
    target_position: Integer;
    candidate_index: Integer;
    position: Integer;
begin
    Result := -1;
    if (Length(ordered_indices) < 2) or (anchor_position <= 0) or
        (anchor_position >= Length(ordered_indices)) then
    begin
        Exit;
    end;
    if score >= strict_threshold then
    begin
        target_position := 0;
    end
    else if score >= second_threshold then
    begin
        target_position := 1;
    end
    else
    begin
        Exit;
    end;
    if anchor_position <= target_position then
    begin
        Exit;
    end;

    candidate_index := ordered_indices[anchor_position];
    for position := anchor_position downto target_position + 1 do
    begin
        ordered_indices[position] := ordered_indices[position - 1];
    end;
    ordered_indices[target_position] := candidate_index;
    Result := target_position;
end;

type
    TncSegmentPathState = record
        text: string;
        score: Integer;
        path_preference_score: Integer;
        path_confidence_score: Integer;
        source: TncCandidateSource;
        has_multi_segment: Boolean;
        uses_prefix_completion: Boolean;
        prev_text: string;
        prev_pinyin_text: string;
        prev_prev_text: string;
        path_text: string;
        fuzzy_cost: Integer;
        fuzzy_rules: TncFuzzyPinyinRules;
    end;

    TncLongLocalRerankPoolItem = record
        text: string;
        seed_rank: Integer;
        seed_visible_index: Integer;
        original: Boolean;
        substitutions: Integer;
        changed_position: Integer;
        source_char_weight: Integer;
        replacement_char_weight: Integer;
        char_lm_score: Integer;
        pool_rank: Integer;
        consensus_seed_count: Integer;
        consensus_support_mean: Double;
        consensus_support_min: Double;
        consensus_majority_units: Integer;
        consensus_unanimous_units: Integer;
        consensus_nearest_distance: Integer;
        consensus_mean_distance: Double;
        consensus_changed_support: Double;
        consensus_changed_top_match: Integer;
        model_score: Double;
    end;

const
    c_default_page_size = 9;
    c_default_total_limit = 256;
    c_candidate_total_expand_factor = 16;
    c_candidate_total_limit_max = 256;
    c_user_score_bonus = 1000;
    c_partial_candidate_score_penalty = 260;
    c_left_context_max_len = 64;
    c_context_model_max_len = 20;
    c_context_history_limit = 200;
    c_phrase_context_history_limit = 256;
    c_segment_path_separator = #3;
    c_context_score_bonus = 80;
    c_context_score_bonus_max = 400;
    c_session_text_history_limit = 256;
    c_session_query_history_limit = 320;
    c_session_query_path_history_limit = 384;
    c_session_ranked_query_path_history_limit = 192;
    c_session_context_query_history_limit = 384;
    c_long_sentence_full_path_min_syllables = 5;
    c_long_sentence_head_only_bypass_min_syllables = 8;
    c_full_width_offset = $FEE0;
    c_segment_surname_bonus = 110;
    c_session_query_path_separator = #4;

function get_encoded_path_segment_count_local(const encoded_path: string): Integer;
var
    idx: Integer;
    trimmed_path: string;
begin
    trimmed_path := Trim(encoded_path);
    if trimmed_path = '' then
    begin
        Exit(0);
    end;

    Result := 1;
    for idx := 1 to Length(trimmed_path) do
    begin
        if trimmed_path[idx] = c_segment_path_separator then
        begin
            Inc(Result);
        end;
    end;
end;

constructor TncInMemoryDictionary.create;
begin
    inherited create;
    m_map := TDictionary<string, TArray<string>>.Create;

    add_entry('ni', ['ni']);
    add_entry('hao', ['hao']);
    add_entry('nihao', ['ni_hao']);
end;

destructor TncInMemoryDictionary.Destroy;
begin
    m_map.Free;
    inherited Destroy;
end;

procedure TncInMemoryDictionary.add_entry(const pinyin: string; const items: array of string);
var
    values: TArray<string>;
    i: Integer;
begin
    SetLength(values, Length(items));
    for i := 0 to High(items) do
    begin
        values[i] := items[i];
    end;

    m_map.AddOrSetValue(pinyin, values);
end;

function TncInMemoryDictionary.lookup(const pinyin: string; out results: TncCandidateList): Boolean;
var
    values: TArray<string>;
    i: Integer;
    score_base: Integer;
begin
    SetLength(results, 0);
    Result := m_map.TryGetValue(pinyin, values);
    if not Result then
    begin
        Exit(False);
    end;

    SetLength(results, Length(values));
    score_base := Length(values);
    for i := 0 to High(values) do
    begin
        results[i].text := values[i];
        results[i].comment := '';
        results[i].score := score_base - i;
        results[i].source := cs_rule;
        results[i].has_dict_weight := True;
        results[i].dict_weight := results[i].score;
    end;
end;

constructor TncEngine.create(const config: TncEngineConfig;
    const defer_optional_dictionary_models: Boolean = False;
    const allow_one_key_completion_lookup: Boolean = True;
    const initialize_dictionary: Boolean = True;
    const initial_dictionary: TncDictionaryProvider = nil);
begin
    inherited create;
    m_long_neural_reranker := nil;
    m_config := config;
    m_defer_optional_dictionary_models :=
        defer_optional_dictionary_models;
    m_allow_one_key_completion_lookup := allow_one_key_completion_lookup;
    nc_normalize_shortcut_config(m_config.shortcuts);
    m_config.one_key_completion_key :=
        nc_normalize_one_key_completion_key(
        m_config.one_key_completion_key);
    m_config.candidate_page_key_scheme :=
        nc_resolve_candidate_page_key_scheme(
        m_config.candidate_page_key_scheme,
        m_config.one_key_completion_key);
    m_composition_text := '';
    m_composition_display_text := '';
    clear_one_key_completion;
    clear_one_key_completion_feedback_target;
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_query_key := '';
    m_last_lookup_key := '';
    m_last_lookup_normalized_from := '';
    m_last_lookup_syllable_count := 0;
    m_last_three_syllable_partial_preference_kind := 0;
    m_last_three_syllable_head_exact_text := '';
    m_last_three_syllable_head_strength := 0;
    m_last_three_syllable_tail_strength := 0;
    m_last_three_syllable_first_single_strength := 0;
    m_last_three_syllable_last_single_strength := 0;
    m_last_three_syllable_head_path_bonus := 0;
    m_last_three_syllable_tail_path_bonus := 0;
    m_last_three_syllable_partial_debug_info := '';
    m_last_full_path_debug_info := '';
    m_last_lookup_debug_extra := '';
    m_last_lookup_timing_info := '';
    m_last_lookup_all_initial_fast := False;
    m_last_lookup_prefix_partial_fast := False;
    m_last_lookup_short312_head_fast := False;
    m_last_lookup_incremental_partial_reuse := False;
    m_last_ranked_query_key := '';
    m_last_ranked_top_path := '';
    m_last_candidates_incrementally_reusable := True;
    m_composition_built_incrementally := False;
    // Ranked paths are speculative ordering state from the current composition,
    // not user-confirmed learning. Keep them from leaking into the next query.
    if m_session_ranked_query_paths <> nil then
    begin
        m_session_ranked_query_paths.Clear;
    end;
    if m_session_ranked_query_path_scores <> nil then
    begin
        m_session_ranked_query_path_scores.Clear;
    end;
    if m_session_ranked_query_path_order <> nil then
    begin
        m_session_ranked_query_path_order.Clear;
    end;
    m_runtime_chain_text := '';
    m_runtime_common_pattern_text := '';
    m_runtime_redup_text := '';
    m_debug_target_recall_text := '';
    m_debug_capture_long_beam_states := False;
    m_debug_capture_long_final_candidates := False;
    m_debug_capture_long_ranking_stages := False;
    m_debug_expand_long_beam_states := False;
    SetLength(m_debug_long_beam_states, 0);
    SetLength(m_debug_long_exact_edge_stages, 0);
    if m_debug_long_exact_edge_stage_index <> nil then
    begin
        m_debug_long_exact_edge_stage_index.Clear;
    end;
    SetLength(m_debug_long_chain_candidates, 0);
    SetLength(m_debug_long_final_candidates, 0);
    SetLength(m_debug_long_ranking_stages, 0);
    SetLength(m_runtime_long_chain_candidates, 0);
    SetLength(m_runtime_long_complete_pool_candidates, 0);
    SetLength(m_runtime_long_retained_exact_edges, 0);
    m_long_complete_pool_pairwise_text := '';
    m_long_complete_pool_pairwise_insert_rank := -1;
    m_debug_long_chain_beam_width := 0;
    m_debug_long_chain_prebeam_width := 0;
    m_debug_long_chain_option_limit := 0;
    m_debug_long_chain_probe_limit := 0;
    m_debug_long_chain_ranker_profile := -1;
    m_debug_long_final_ranker_profile := -1;
    m_debug_disable_long_top2_pairwise_swap := False;
    m_search_budget_mode := sbm_production;
    m_search_budget_scale_percent := 100;
    reset_debug_target_recall_metrics;
    m_disable_subspan_standalone_oracle := 0;
    m_single_quote_open := False;
    m_double_quote_open := False;
    m_page_index := 0;
    m_selected_index := 0;
    m_confirmed_text := '';
    m_confirmed_explicit_choice := False;
    m_recent_partial_prefix_text := '';
    m_prev_committed_text := '';
    m_prev_committed_pinyin := '';
    m_last_committed_text := '';
    m_last_committed_pinyin := '';
    m_dictionary := nil;
    m_cached_dictionary_simplified := nil;
    m_cached_dictionary_traditional := nil;
    m_configured_dictionary_path_simplified := '';
    m_configured_dictionary_path_traditional := '';
    m_configured_user_dictionary_path := '';
    m_dictionary_paths_configured := False;
    m_dictionary_path := '';
    m_dictionary_write_time := 0;
    m_user_dictionary_path := '';
    m_user_dictionary_write_time := 0;
    m_last_dictionary_reload_check_tick := 0;
    m_left_context := '';
    m_external_left_context := '';
    m_segment_left_context := '';
    m_document_context_model := TncDocumentContextModel.create;
    m_confirmed_segments := TList<TncConfirmedSegment>.Create;
    m_context_pairs := TDictionary<string, Integer>.Create;
    m_context_order := TQueue<string>.Create;
    m_phrase_context_pairs := TDictionary<string, Integer>.Create;
    m_phrase_context_order := TQueue<string>.Create;
    m_phrase_context_last_seen := TDictionary<string, Int64>.Create;
    m_session_text_counts := TDictionary<string, Integer>.Create;
    m_session_text_last_seen := TDictionary<string, Int64>.Create;
    m_session_text_order := TQueue<string>.Create;
    m_session_query_choice_counts := TDictionary<string, Integer>.Create;
    m_session_query_choice_last_seen := TDictionary<string, Int64>.Create;
    m_session_query_choice_order := TQueue<string>.Create;
    m_session_query_latest_text := TDictionary<string, string>.Create;
    m_session_query_path_choice_counts := TDictionary<string, Integer>.Create;
    m_session_query_path_choice_last_seen := TDictionary<string, Int64>.Create;
    m_session_query_path_choice_order := TQueue<string>.Create;
    m_session_query_path_penalty_counts := TDictionary<string, Integer>.Create;
    m_session_query_path_penalty_last_seen := TDictionary<string, Int64>.Create;
    m_session_query_path_penalty_order := TQueue<string>.Create;
    m_session_ranked_query_paths := TDictionary<string, string>.Create;
    m_session_ranked_query_path_scores := TDictionary<string, Integer>.Create;
    m_session_ranked_query_path_order := TQueue<string>.Create;
    m_session_context_query_choice_counts := TDictionary<string, Integer>.Create;
    m_session_context_query_choice_last_seen := TDictionary<string, Int64>.Create;
    m_session_context_query_choice_order := TQueue<string>.Create;
    m_session_context_query_latest_text := TDictionary<string, string>.Create;
    m_session_commit_serial := 0;
    m_last_output_commit_text := '';
    m_prev_output_commit_text := '';
    m_context_db_bonus_cache_key := '';
    m_context_db_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_text_unit_count_cache := TDictionary<string, Integer>.Create;
    m_lookup_text_units_cache := TDictionary<string, TArray<string>>.Create;
    m_lookup_session_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_query_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_query_latest_text_cache := TDictionary<string, string>.Create;
    m_lookup_query_path_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_context_query_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_context_query_latest_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_phrase_context_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_text_context_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_context_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_segment_path_context_bonus_cache := TDictionary<string, Integer>.Create;
    m_lookup_segment_path_preference_cache := TDictionary<string, Integer>.Create;
    m_lookup_candidate_path_confidence_cache := TDictionary<string, Integer>.Create;
    m_lookup_single_char_match_cache := TDictionary<string, Integer>.Create;
    m_lookup_inferred_segment_path_cache :=
        TDictionary<string, TncInferredPathCacheValue>.Create;
    m_lookup_char_lm_full_score_cache := TDictionary<string, Integer>.Create;
    m_lookup_char_lm_suffix_score_cache := TDictionary<string, Integer>.Create;
    m_lookup_char_lm_context_score_cache := TDictionary<string, Integer>.Create;
    m_lookup_char_lm_span_score_cache := TDictionary<string, Integer>.Create;
    m_lookup_char_lm_reverse_score_cache := TDictionary<string, Integer>.Create;
    m_lookup_long_path_structure_cache :=
        TDictionary<string, TncLongPathStructureCacheValue>.Create;
    m_lookup_exact_lexical_feature_cache :=
        TDictionary<string, TncExactLexicalFeatureCacheValue>.Create;
    m_lookup_display_feature_cache := TDictionary<string, Integer>.Create;
    m_lookup_admin_place_alias_cache := TDictionary<string, Boolean>.Create;
    m_full_pinyin_key_cache := TDictionary<string, Boolean>.Create;
    m_effective_pinyin_parse_cache :=
        TDictionary<string, TncPinyinParseResult>.Create;
    m_build_lookup_cache := TDictionary<string, TncCandidateList>.Create;
    m_long_local_single_options_cache :=
        TDictionary<string, TncCandidateList>.Create;
    m_current_segment_path_map := TDictionary<string, string>.Create;
    m_current_segment_path_score_map := TDictionary<string, Integer>.Create;
    m_current_segment_path_query_prefix_map := TDictionary<string, string>.Create;
    m_debug_long_exact_edge_stage_index :=
        TDictionary<string, Integer>.Create;
    SetLength(m_candidate_segment_paths, 0);
    m_short_three_exact_pair_query := '';
    SetLength(m_short_three_exact_pair_texts, 0);
    m_short_three_char_pair_path := '';
    m_short_three_char_pair_evidence := 0;
    m_short_exact_lexicon_completion_query := '';
    SetLength(m_short_exact_lexicon_completion_texts, 0);
    SetLength(m_short_exact_lexicon_completion_candidates, 0);
    m_short_exact_pair_tail_query := '';
    SetLength(m_short_exact_pair_tail_candidates, 0);
    m_pending_commit_segment_path := '';
    m_pending_commit_query_key := '';
    SetLength(m_candidates, 0);
    SetLength(m_visible_candidates_cache, 0);
    SetLength(m_visible_candidate_source_indices_cache, 0);
    m_visible_candidates_cache_valid := False;
    SetLength(m_long_visible_candidate_pool_cache, 0);
    SetLength(m_long_visible_candidate_pool_source_indices_cache, 0);
    m_long_visible_candidate_pool_cache_key := '';
    m_long_visible_candidate_pool_source_signature := 0;
    m_long_visible_candidate_pool_cache_valid := False;
    m_long_local_rerank_generated_text := '';
    m_long_local_rerank_composition_text := '';
    m_long_local_rerank_lookup_key := '';
    m_has_forced_visible_top_candidate := False;
    m_forced_visible_top_from_incremental_lm_prefix := False;
    m_forced_visible_top_candidate.text := '';
    m_forced_visible_top_candidate.comment := '';
    m_forced_visible_top_candidate.score := 0;
    m_forced_visible_top_candidate.source := cs_rule;
    m_forced_visible_top_candidate.has_dict_weight := False;
    m_forced_visible_top_candidate.dict_weight := 0;
    m_forced_visible_top_composition_text := '';
    m_forced_visible_top_lookup_key := '';
    if initial_dictionary <> nil then
        set_dictionary_provider(initial_dictionary)
    else if initialize_dictionary then
    begin
        set_dictionary_provider(create_dictionary_from_config);
    end;
end;

destructor TncEngine.Destroy;
begin
    m_long_neural_reranker := nil;
    if m_document_context_model <> nil then
    begin
        m_document_context_model.Free;
        m_document_context_model := nil;
    end;
    if m_dictionary <> nil then
    begin
        m_dictionary.Free;
        m_dictionary := nil;
    end;
    clear_cached_dictionary_providers;

    if m_context_order <> nil then
    begin
        m_context_order.Free;
        m_context_order := nil;
    end;

    if m_phrase_context_order <> nil then
    begin
        m_phrase_context_order.Free;
        m_phrase_context_order := nil;
    end;

    if m_phrase_context_last_seen <> nil then
    begin
        m_phrase_context_last_seen.Free;
        m_phrase_context_last_seen := nil;
    end;

    if m_session_text_order <> nil then
    begin
        m_session_text_order.Free;
        m_session_text_order := nil;
    end;
    if m_session_query_choice_order <> nil then
    begin
        m_session_query_choice_order.Free;
        m_session_query_choice_order := nil;
    end;
    if m_session_query_path_choice_order <> nil then
    begin
        m_session_query_path_choice_order.Free;
        m_session_query_path_choice_order := nil;
    end;
    if m_session_query_path_penalty_order <> nil then
    begin
        m_session_query_path_penalty_order.Free;
        m_session_query_path_penalty_order := nil;
    end;
    if m_session_ranked_query_path_order <> nil then
    begin
        m_session_ranked_query_path_order.Free;
        m_session_ranked_query_path_order := nil;
    end;

    if m_session_text_last_seen <> nil then
    begin
        m_session_text_last_seen.Free;
        m_session_text_last_seen := nil;
    end;
    if m_session_query_choice_last_seen <> nil then
    begin
        m_session_query_choice_last_seen.Free;
        m_session_query_choice_last_seen := nil;
    end;
    if m_session_query_latest_text <> nil then
    begin
        m_session_query_latest_text.Free;
        m_session_query_latest_text := nil;
    end;
    if m_session_query_path_choice_last_seen <> nil then
    begin
        m_session_query_path_choice_last_seen.Free;
        m_session_query_path_choice_last_seen := nil;
    end;
    if m_session_query_path_penalty_last_seen <> nil then
    begin
        m_session_query_path_penalty_last_seen.Free;
        m_session_query_path_penalty_last_seen := nil;
    end;
    if m_session_ranked_query_path_scores <> nil then
    begin
        m_session_ranked_query_path_scores.Free;
        m_session_ranked_query_path_scores := nil;
    end;
    if m_session_ranked_query_paths <> nil then
    begin
        m_session_ranked_query_paths.Free;
        m_session_ranked_query_paths := nil;
    end;
    if m_session_context_query_choice_order <> nil then
    begin
        m_session_context_query_choice_order.Free;
        m_session_context_query_choice_order := nil;
    end;
    if m_session_context_query_latest_text <> nil then
    begin
        m_session_context_query_latest_text.Free;
        m_session_context_query_latest_text := nil;
    end;
    if m_session_context_query_choice_last_seen <> nil then
    begin
        m_session_context_query_choice_last_seen.Free;
        m_session_context_query_choice_last_seen := nil;
    end;
    if m_session_context_query_choice_counts <> nil then
    begin
        m_session_context_query_choice_counts.Free;
        m_session_context_query_choice_counts := nil;
    end;

    if m_context_db_bonus_cache <> nil then
    begin
        m_context_db_bonus_cache.Free;
        m_context_db_bonus_cache := nil;
    end;
    m_context_db_bonus_cache_key := '';

    if m_lookup_text_unit_count_cache <> nil then
    begin
        m_lookup_text_unit_count_cache.Free;
        m_lookup_text_unit_count_cache := nil;
    end;
    if m_lookup_text_units_cache <> nil then
    begin
        m_lookup_text_units_cache.Free;
        m_lookup_text_units_cache := nil;
    end;
    if m_lookup_session_bonus_cache <> nil then
    begin
        m_lookup_session_bonus_cache.Free;
        m_lookup_session_bonus_cache := nil;
    end;
    if m_lookup_query_bonus_cache <> nil then
    begin
        m_lookup_query_bonus_cache.Free;
        m_lookup_query_bonus_cache := nil;
    end;
    if m_lookup_query_latest_text_cache <> nil then
    begin
        m_lookup_query_latest_text_cache.Free;
        m_lookup_query_latest_text_cache := nil;
    end;
    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.Free;
        m_lookup_query_path_bonus_cache := nil;
    end;
    if m_lookup_context_query_bonus_cache <> nil then
    begin
        m_lookup_context_query_bonus_cache.Free;
        m_lookup_context_query_bonus_cache := nil;
    end;
    if m_lookup_context_query_latest_bonus_cache <> nil then
    begin
        m_lookup_context_query_latest_bonus_cache.Free;
        m_lookup_context_query_latest_bonus_cache := nil;
    end;
    if m_lookup_phrase_context_bonus_cache <> nil then
    begin
        m_lookup_phrase_context_bonus_cache.Free;
        m_lookup_phrase_context_bonus_cache := nil;
    end;
    if m_lookup_text_context_bonus_cache <> nil then
    begin
        m_lookup_text_context_bonus_cache.Free;
        m_lookup_text_context_bonus_cache := nil;
    end;
    if m_lookup_context_bonus_cache <> nil then
    begin
        m_lookup_context_bonus_cache.Free;
        m_lookup_context_bonus_cache := nil;
    end;
    if m_lookup_segment_path_context_bonus_cache <> nil then
    begin
        m_lookup_segment_path_context_bonus_cache.Free;
        m_lookup_segment_path_context_bonus_cache := nil;
    end;
    if m_lookup_segment_path_preference_cache <> nil then
    begin
        m_lookup_segment_path_preference_cache.Free;
        m_lookup_segment_path_preference_cache := nil;
    end;
    if m_lookup_candidate_path_confidence_cache <> nil then
    begin
        m_lookup_candidate_path_confidence_cache.Free;
        m_lookup_candidate_path_confidence_cache := nil;
    end;
    if m_lookup_single_char_match_cache <> nil then
    begin
        m_lookup_single_char_match_cache.Free;
        m_lookup_single_char_match_cache := nil;
    end;
    if m_lookup_inferred_segment_path_cache <> nil then
    begin
        m_lookup_inferred_segment_path_cache.Free;
        m_lookup_inferred_segment_path_cache := nil;
    end;
    if m_lookup_char_lm_full_score_cache <> nil then
    begin
        m_lookup_char_lm_full_score_cache.Free;
        m_lookup_char_lm_full_score_cache := nil;
    end;
    if m_lookup_char_lm_suffix_score_cache <> nil then
    begin
        m_lookup_char_lm_suffix_score_cache.Free;
        m_lookup_char_lm_suffix_score_cache := nil;
    end;
    if m_lookup_char_lm_context_score_cache <> nil then
    begin
        m_lookup_char_lm_context_score_cache.Free;
        m_lookup_char_lm_context_score_cache := nil;
    end;
    if m_lookup_char_lm_span_score_cache <> nil then
    begin
        m_lookup_char_lm_span_score_cache.Free;
        m_lookup_char_lm_span_score_cache := nil;
    end;
    if m_lookup_char_lm_reverse_score_cache <> nil then
    begin
        m_lookup_char_lm_reverse_score_cache.Free;
        m_lookup_char_lm_reverse_score_cache := nil;
    end;
    if m_lookup_long_path_structure_cache <> nil then
    begin
        m_lookup_long_path_structure_cache.Free;
        m_lookup_long_path_structure_cache := nil;
    end;
    if m_lookup_exact_lexical_feature_cache <> nil then
    begin
        m_lookup_exact_lexical_feature_cache.Free;
        m_lookup_exact_lexical_feature_cache := nil;
    end;
    if m_lookup_display_feature_cache <> nil then
    begin
        m_lookup_display_feature_cache.Free;
        m_lookup_display_feature_cache := nil;
    end;
    if m_lookup_admin_place_alias_cache <> nil then
    begin
        m_lookup_admin_place_alias_cache.Free;
        m_lookup_admin_place_alias_cache := nil;
    end;
    if m_full_pinyin_key_cache <> nil then
    begin
        m_full_pinyin_key_cache.Free;
        m_full_pinyin_key_cache := nil;
    end;
    if m_effective_pinyin_parse_cache <> nil then
    begin
        m_effective_pinyin_parse_cache.Free;
        m_effective_pinyin_parse_cache := nil;
    end;
    if m_build_lookup_cache <> nil then
    begin
        m_build_lookup_cache.Free;
        m_build_lookup_cache := nil;
    end;
    if m_long_local_single_options_cache <> nil then
    begin
        m_long_local_single_options_cache.Free;
        m_long_local_single_options_cache := nil;
    end;
    if m_current_segment_path_map <> nil then
    begin
        m_current_segment_path_map.Free;
        m_current_segment_path_map := nil;
    end;
    if m_current_segment_path_score_map <> nil then
    begin
        m_current_segment_path_score_map.Free;
        m_current_segment_path_score_map := nil;
    end;
    if m_current_segment_path_query_prefix_map <> nil then
    begin
        m_current_segment_path_query_prefix_map.Free;
        m_current_segment_path_query_prefix_map := nil;
    end;
    if m_debug_long_exact_edge_stage_index <> nil then
    begin
        m_debug_long_exact_edge_stage_index.Free;
        m_debug_long_exact_edge_stage_index := nil;
    end;
    SetLength(m_candidate_segment_paths, 0);

    if m_confirmed_segments <> nil then
    begin
        m_confirmed_segments.Free;
        m_confirmed_segments := nil;
    end;

    if m_context_pairs <> nil then
    begin
        m_context_pairs.Free;
        m_context_pairs := nil;
    end;

    if m_phrase_context_pairs <> nil then
    begin
        m_phrase_context_pairs.Free;
        m_phrase_context_pairs := nil;
    end;

    if m_session_text_counts <> nil then
    begin
        m_session_text_counts.Free;
        m_session_text_counts := nil;
    end;
    if m_session_query_choice_counts <> nil then
    begin
        m_session_query_choice_counts.Free;
        m_session_query_choice_counts := nil;
    end;
    if m_session_query_path_choice_counts <> nil then
    begin
        m_session_query_path_choice_counts.Free;
        m_session_query_path_choice_counts := nil;
    end;
    if m_session_query_path_penalty_counts <> nil then
    begin
        m_session_query_path_penalty_counts.Free;
        m_session_query_path_penalty_counts := nil;
    end;

    inherited Destroy;
end;

procedure TncEngine.clear_lookup_bonus_caches;
begin
    // Text unit counts are immutable. Keep this bounded cache across keystrokes;
    // clearing it here made every long-sentence display pass rescan the same
    // UTF-16 strings dozens of times.
    if m_lookup_session_bonus_cache <> nil then
    begin
        m_lookup_session_bonus_cache.Clear;
    end;
    if m_lookup_query_bonus_cache <> nil then
    begin
        m_lookup_query_bonus_cache.Clear;
    end;
    if m_lookup_query_latest_text_cache <> nil then
    begin
        m_lookup_query_latest_text_cache.Clear;
    end;
    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.Clear;
    end;
    if m_lookup_context_query_bonus_cache <> nil then
    begin
        m_lookup_context_query_bonus_cache.Clear;
    end;
    if m_lookup_context_query_latest_bonus_cache <> nil then
    begin
        m_lookup_context_query_latest_bonus_cache.Clear;
    end;
    if m_lookup_phrase_context_bonus_cache <> nil then
    begin
        m_lookup_phrase_context_bonus_cache.Clear;
    end;
    if m_lookup_text_context_bonus_cache <> nil then
    begin
        m_lookup_text_context_bonus_cache.Clear;
    end;
    if m_lookup_context_bonus_cache <> nil then
    begin
        m_lookup_context_bonus_cache.Clear;
    end;
    if m_lookup_segment_path_context_bonus_cache <> nil then
    begin
        m_lookup_segment_path_context_bonus_cache.Clear;
    end;
    if m_lookup_segment_path_preference_cache <> nil then
    begin
        m_lookup_segment_path_preference_cache.Clear;
    end;
    if m_lookup_candidate_path_confidence_cache <> nil then
    begin
        m_lookup_candidate_path_confidence_cache.Clear;
    end;
    if m_lookup_single_char_match_cache <> nil then
    begin
        m_lookup_single_char_match_cache.Clear;
    end;
    if m_lookup_inferred_segment_path_cache <> nil then
    begin
        m_lookup_inferred_segment_path_cache.Clear;
    end;
    if m_lookup_char_lm_full_score_cache <> nil then
    begin
        m_lookup_char_lm_full_score_cache.Clear;
    end;
    if m_lookup_char_lm_suffix_score_cache <> nil then
    begin
        m_lookup_char_lm_suffix_score_cache.Clear;
    end;
    if m_lookup_char_lm_context_score_cache <> nil then
    begin
        m_lookup_char_lm_context_score_cache.Clear;
    end;
    if m_lookup_char_lm_span_score_cache <> nil then
    begin
        m_lookup_char_lm_span_score_cache.Clear;
    end;
    if m_lookup_char_lm_reverse_score_cache <> nil then
    begin
        m_lookup_char_lm_reverse_score_cache.Clear;
    end;
    if m_lookup_long_path_structure_cache <> nil then
    begin
        m_lookup_long_path_structure_cache.Clear;
    end;
    if m_lookup_exact_lexical_feature_cache <> nil then
    begin
        m_lookup_exact_lexical_feature_cache.Clear;
    end;
    if m_lookup_display_feature_cache <> nil then
    begin
        m_lookup_display_feature_cache.Clear;
    end;
    if m_lookup_admin_place_alias_cache <> nil then
    begin
        m_lookup_admin_place_alias_cache.Clear;
    end;
end;

function TncEngine.is_low_evidence_admin_place_alias_cached(
    const pinyin: string; const text: string): Boolean;
var
    cache_key: string;
begin
    Result := False;
    if (m_dictionary = nil) or (pinyin = '') or (text = '') then
    begin
        Exit;
    end;

    cache_key := normalize_pinyin_text(pinyin) + #1 + Trim(text);
    if (m_lookup_admin_place_alias_cache <> nil) and
        m_lookup_admin_place_alias_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := m_dictionary.is_low_evidence_admin_place_alias_user_entry(
        pinyin, text, '', 0, 0);
    if m_lookup_admin_place_alias_cache <> nil then
    begin
        m_lookup_admin_place_alias_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_cached_char_lm_scores(const texts: TArray<string>;
    out scores: TArray<Integer>; const score_mode: TncCharLmScoreMode;
    const left_context: string): Boolean;
var
    cache: TDictionary<string, Integer>;
    cache_key: string;
    context_key: string;
    idx: Integer;
    missing_count: Integer;
    missing_texts: TArray<string>;
    missing_score_index_by_input: TArray<Integer>;
    missing_scores: TArray<Integer>;
    missing_idx: Integer;
    missing_slot: Integer;
    normalized_missing_text: string;
    provider_ok: Boolean;
begin
    SetLength(scores, Length(texts));
    Result := False;
    if (m_dictionary = nil) or (Length(texts) = 0) then
    begin
        Exit;
    end;

    case score_mode of
        clsm_full:
            cache := m_lookup_char_lm_full_score_cache;
        clsm_suffix:
            cache := m_lookup_char_lm_suffix_score_cache;
        clsm_context, clsm_short_context:
            cache := m_lookup_char_lm_context_score_cache;
        clsm_span:
            cache := m_lookup_char_lm_span_score_cache;
        clsm_reverse:
            cache := m_lookup_char_lm_reverse_score_cache;
    else
        cache := nil;
    end;
    if cache = nil then
    begin
        case score_mode of
            clsm_full:
                provider_ok := m_dictionary.get_char_lm_text_scores(
                    texts, scores);
            clsm_suffix:
                provider_ok := m_dictionary.get_char_lm_suffix_scores(
                    texts, scores);
            clsm_context:
                provider_ok := m_dictionary.get_char_lm_continuation_scores(
                    left_context, texts, scores);
            clsm_short_context:
                provider_ok := m_dictionary.get_char_lm_short_context_scores(
                    left_context, texts, scores);
            clsm_span:
                provider_ok := m_dictionary.get_char_lm_span_scores(
                    texts, scores);
            clsm_reverse:
                provider_ok := m_dictionary.get_char_reverse_lm_suffix_scores(
                    texts, scores);
        else
            provider_ok := False;
        end;
        Exit(provider_ok and (Length(scores) = Length(texts)));
    end;

    context_key := '';
    if score_mode in [clsm_context, clsm_short_context] then
    begin
        context_key := Trim(left_context) + #1;
    end;
    SetLength(missing_texts, 0);
    SetLength(missing_score_index_by_input, 0);
    missing_count := 0;
    for idx := 0 to High(texts) do
    begin
        cache_key := context_key + Trim(texts[idx]);
        if not cache.TryGetValue(cache_key, scores[idx]) then
        begin
            if missing_count = 0 then
            begin
                SetLength(missing_texts, Length(texts));
                SetLength(missing_score_index_by_input, Length(texts));
            end;
            normalized_missing_text := Trim(texts[idx]);
            missing_slot := -1;
            for missing_idx := 0 to missing_count - 1 do
            begin
                if missing_texts[missing_idx] = normalized_missing_text then
                begin
                    missing_slot := missing_idx;
                    Break;
                end;
            end;
            if missing_slot < 0 then
            begin
                missing_slot := missing_count;
                missing_texts[missing_count] := normalized_missing_text;
                Inc(missing_count);
            end;
            missing_score_index_by_input[idx] := missing_slot + 1;
        end;
    end;
    if missing_count = 0 then
    begin
        Exit(True);
    end;

    SetLength(missing_texts, missing_count);
    case score_mode of
        clsm_full:
            provider_ok := m_dictionary.get_char_lm_text_scores(
                missing_texts, missing_scores);
        clsm_suffix:
            provider_ok := m_dictionary.get_char_lm_suffix_scores(
                missing_texts, missing_scores);
        clsm_context:
            provider_ok := m_dictionary.get_char_lm_continuation_scores(
                left_context, missing_texts, missing_scores);
        clsm_short_context:
            provider_ok := m_dictionary.get_char_lm_short_context_scores(
                left_context, missing_texts, missing_scores);
        clsm_span:
            provider_ok := m_dictionary.get_char_lm_span_scores(
                missing_texts, missing_scores);
        clsm_reverse:
            provider_ok := m_dictionary.get_char_reverse_lm_suffix_scores(
                missing_texts, missing_scores);
    else
        provider_ok := False;
    end;
    if (not provider_ok) or (Length(missing_scores) <> missing_count) then
    begin
        Exit(False);
    end;

    for idx := 0 to High(texts) do
    begin
        missing_slot := missing_score_index_by_input[idx] - 1;
        if missing_slot >= 0 then
        begin
            scores[idx] := missing_scores[missing_slot];
        end;
    end;
    for idx := 0 to missing_count - 1 do
    begin
        cache_key := context_key + Trim(missing_texts[idx]);
        cache.AddOrSetValue(cache_key, missing_scores[idx]);
    end;
    Result := True;
end;

function TncEngine.get_cached_long_path_structure(const encoded_path: string;
    out value: TncLongPathStructureCacheValue): Boolean;
var
    cache_key: string;
    part: string;
    part_units: Integer;
    parts: TArray<string>;
begin
    FillChar(value, SizeOf(value), 0);
    cache_key := Trim(encoded_path);
    if cache_key = '' then
    begin
        Exit(False);
    end;
    if (m_lookup_long_path_structure_cache <> nil) and
        m_lookup_long_path_structure_cache.TryGetValue(cache_key, value) then
    begin
        Exit(True);
    end;

    value.min_segment_units := MaxInt;
    parts := cache_key.Split([c_segment_path_separator]);
    for part in parts do
    begin
        part_units := get_candidate_text_unit_count(Trim(part));
        if part_units <= 0 then
        begin
            Continue;
        end;
        Inc(value.segment_count);
        if part_units = 1 then
        begin
            Inc(value.single_segment_count);
        end;
        value.max_segment_units := Max(value.max_segment_units, part_units);
        value.min_segment_units := Min(value.min_segment_units, part_units);
        Inc(value.segment_units_square_sum, part_units * part_units);
        if value.first_segment_units = 0 then
        begin
            value.first_segment_units := part_units;
        end;
        value.last_segment_units := part_units;
    end;
    if value.min_segment_units = MaxInt then
    begin
        value.min_segment_units := 0;
    end;
    if m_lookup_long_path_structure_cache <> nil then
    begin
        m_lookup_long_path_structure_cache.AddOrSetValue(cache_key, value);
    end;
    Result := value.segment_count > 0;
end;

function TncEngine.get_cached_query_segment_path_bonus(const query_key: string;
    const encoded_path: string): Integer;
var
    cache_key: string;
begin
    Result := 0;
    if m_dictionary = nil then
    begin
        Exit;
    end;
    cache_key := 'DB' + #1 + LowerCase(Trim(query_key)) + #1 +
        Trim(encoded_path);
    if (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;
    Result := m_dictionary.get_query_segment_path_bonus(query_key,
        encoded_path);
    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_cached_query_segment_path_penalty(
    const query_key: string; const encoded_path: string): Integer;
var
    cache_key: string;
begin
    Result := 0;
    if m_dictionary = nil then
    begin
        Exit;
    end;
    cache_key := 'DN' + #1 + LowerCase(Trim(query_key)) + #1 +
        Trim(encoded_path);
    if (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;
    Result := m_dictionary.get_query_segment_path_penalty(query_key,
        encoded_path);
    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_short_four_two_exact_path_evidence(
    const query_key: string; const text_value: string;
    out encoded_path: string; out evidence_score: Integer): Boolean;
const
    c_min_lm_raw_weight = 390;
    c_min_query_path_raw_weight = 420;
    c_min_corroborated_raw_weight = 360;
    c_min_aspect_pair_weight = 800;
    c_aspect_evidence = 360;
    c_model_evidence_cap = 1560;
    c_query_path_evidence_cap = 420;
var
    normalized_query: string;
    normalized_text: string;
    cache_key: string;
    cached_evidence: Integer;
    syllables: TncPinyinParseResult;
    units: TArray<string>;
    head_key: string;
    tail_key: string;
    head_text: string;
    tail_text: string;
    head_results: TncCandidateList;
    tail_results: TncCandidateList;
    head_weight: Integer;
    tail_weight: Integer;
    model_paths: TncPairPathEvidenceList;
    model_idx: Integer;
    query_path_raw_weight: Integer;
    lm_raw_weight: Integer;
    model_supported: Boolean;
    path_penalty: Integer;

    function query_path_evidence(const raw_weight: Integer): Integer;
    begin
        Result := 0;
        if raw_weight <= 0 then
        begin
            Exit;
        end;
        Result := (raw_weight * 3) div 5;
        if raw_weight >= 420 then
        begin
            Inc(Result, 28);
        end;
        if raw_weight >= 620 then
        begin
            Inc(Result, 42);
        end;
        if raw_weight >= 820 then
        begin
            Inc(Result, 54);
        end;
        Result := Min(c_query_path_evidence_cap, Result);
    end;

    function build_query_span_key(const start_idx: Integer;
        const span_len: Integer): string;
    var
        idx: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (span_len <= 0) or
            (start_idx + span_len > Length(syllables)) then
        begin
            Exit;
        end;
        for idx := start_idx to start_idx + span_len - 1 do
        begin
            Result := Result + normalize_pinyin_text(syllables[idx].text);
        end;
    end;

    function load_exact_results(const key_value: string;
        out results_value: TncCandidateList): Boolean;
    begin
        SetLength(results_value, 0);
        Result := (m_dictionary <> nil) and
            m_dictionary.lookup_exact_full_pinyin(key_value, results_value);
        if (not Result) and (m_dictionary <> nil) then
        begin
            Result := m_dictionary.lookup(key_value, results_value);
        end;
    end;

    function candidate_weight(const candidate_value: TncCandidate): Integer;
    begin
        Result := candidate_value.score;
        if candidate_value.has_dict_weight and
            (candidate_value.dict_weight > Result) then
        begin
            Result := candidate_value.dict_weight;
        end;
        if candidate_value.source = cs_user then
        begin
            Result := Max(Result, 4096);
        end;
        Result := Max(0, Min(4096, Result));
    end;

    function find_exact_pair(const key_value: string;
        const exact_text: string; const results_value: TncCandidateList;
        out exact_weight: Integer): Boolean;
    var
        idx: Integer;
        candidate_text: string;
    begin
        Result := False;
        exact_weight := -1;
        for idx := 0 to High(results_value) do
        begin
            candidate_text := Trim(results_value[idx].text);
            if (Trim(results_value[idx].comment) <> '') or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (not SameText(candidate_text, exact_text)) then
            begin
                Continue;
            end;
            exact_weight := Max(exact_weight,
                candidate_weight(results_value[idx]));
            Result := True;
        end;
        if Result and m_dictionary.is_user_entry(key_value, exact_text) then
        begin
            exact_weight := Max(exact_weight, 4096);
        end;
    end;

    function has_productive_aspect_tail: Boolean;
    var
        last_pinyin: string;
    begin
        Result := False;
        if (Length(units) <> 4) or (units[3] = '') or
            (Length(syllables) <> 4) then
        begin
            Exit;
        end;
        last_pinyin := normalize_pinyin_text(syllables[3].text);
        Result := ((last_pinyin = 'le') and
            SameText(units[3], string(Char($4E86)))) or
            ((last_pinyin = 'zhe') and
            SameText(units[3], string(Char($7740)))) or
            ((last_pinyin = 'guo') and
            SameText(units[3], string(Char($8FC7))));
    end;

    procedure cache_evidence;
    begin
        if m_lookup_display_feature_cache <> nil then
        begin
            m_lookup_display_feature_cache.AddOrSetValue(cache_key,
                evidence_score);
        end;
    end;
begin
    Result := False;
    encoded_path := '';
    evidence_score := 0;
    if m_dictionary = nil then
    begin
        Exit;
    end;

    normalized_query := normalize_pinyin_text(query_key);
    normalized_text := Trim(text_value);
    syllables := get_effective_compact_pinyin_syllables(normalized_query);
    units := split_text_units(normalized_text);
    if (Length(syllables) <> 4) or (Length(units) <> 4) then
    begin
        Exit;
    end;

    head_key := build_query_span_key(0, 2);
    tail_key := build_query_span_key(2, 2);
    head_text := units[0] + units[1];
    tail_text := units[2] + units[3];
    encoded_path := head_text + c_segment_path_separator + tail_text;
    cache_key := 'S42E' + #1 + normalized_query + #1 + normalized_text;
    if (m_lookup_display_feature_cache <> nil) and
        m_lookup_display_feature_cache.TryGetValue(cache_key,
        cached_evidence) then
    begin
        evidence_score := cached_evidence;
        Exit(evidence_score > 0);
    end;

    if (not load_exact_results(head_key, head_results)) or
        (not load_exact_results(tail_key, tail_results)) or
        (not find_exact_pair(head_key, head_text, head_results,
        head_weight)) or
        (not find_exact_pair(tail_key, tail_text, tail_results,
        tail_weight)) then
    begin
        cache_evidence;
        Exit;
    end;

    query_path_raw_weight := 0;
    lm_raw_weight := 0;
    if m_dictionary.get_exact_pair_path_evidence(normalized_query,
        model_paths) then
    begin
        for model_idx := 0 to High(model_paths) do
        begin
            if SameText(Trim(model_paths[model_idx].encoded_path),
                encoded_path) then
            begin
                query_path_raw_weight := Max(query_path_raw_weight,
                    model_paths[model_idx].query_path_weight);
                lm_raw_weight := Max(lm_raw_weight,
                    model_paths[model_idx].lm_transition_weight);
            end;
        end;
    end;

    model_supported :=
        (lm_raw_weight >= c_min_lm_raw_weight) or
        (query_path_raw_weight >= c_min_query_path_raw_weight) or
        ((lm_raw_weight >= c_min_corroborated_raw_weight) and
        (query_path_raw_weight >= c_min_corroborated_raw_weight));
    if model_supported then
    begin
        if (lm_raw_weight >= c_min_lm_raw_weight) or
            ((lm_raw_weight >= c_min_corroborated_raw_weight) and
            (query_path_raw_weight >= c_min_corroborated_raw_weight)) then
        begin
            evidence_score := Min(c_model_evidence_cap,
                3 * lm_raw_weight);
        end;
        if query_path_raw_weight >= c_min_query_path_raw_weight then
        begin
            evidence_score := Max(evidence_score,
                query_path_evidence(query_path_raw_weight));
        end;
    end;

    if (head_weight >= c_min_aspect_pair_weight) and
        (tail_weight >= c_min_aspect_pair_weight) and
        has_productive_aspect_tail then
    begin
        evidence_score := Max(evidence_score, c_aspect_evidence);
    end;

    path_penalty := Max(0, get_cached_query_segment_path_penalty(
        normalized_query, encoded_path));
    if path_penalty > 0 then
    begin
        evidence_score := Max(0, evidence_score - path_penalty);
    end;
    cache_evidence;
    Result := evidence_score > 0;
end;

function TncEngine.get_short_four_transition_particle_path_evidence(
    const query_key: string; const text_value: string;
    out encoded_path: string; out evidence_score: Integer): Boolean;
const
    c_min_lm_raw_weight = 390;
    c_min_query_path_raw_weight = 420;
    c_min_corroborated_raw_weight = 360;
    c_model_evidence_cap = 1560;
    c_query_path_evidence_cap = 420;
var
    normalized_query: string;
    normalized_text: string;
    evidence_cache_key: string;
    path_cache_key: string;
    cached_evidence: Integer;
    syllables: TncPinyinParseResult;
    units: TArray<string>;
    prefix_query: string;
    prefix_text: string;
    particle_key: string;
    particle_text: string;
    model_paths: TncPairPathEvidenceList;
    model_idx: Integer;
    parts: TArray<string>;
    first_units: Integer;
    second_units: Integer;
    first_key: string;
    second_key: string;
    candidate_path: string;
    candidate_evidence: Integer;
    path_penalty: Integer;

    procedure cache_result;
    begin
        if m_lookup_display_feature_cache <> nil then
        begin
            m_lookup_display_feature_cache.AddOrSetValue(evidence_cache_key,
                evidence_score);
        end;
        if (evidence_score > 0) and (encoded_path <> '') and
            (m_lookup_query_latest_text_cache <> nil) then
        begin
            m_lookup_query_latest_text_cache.AddOrSetValue(path_cache_key,
                encoded_path);
        end;
    end;

    function build_query_span_key(const start_idx: Integer;
        const span_len: Integer): string;
    var
        idx: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (span_len <= 0) or
            (start_idx + span_len > Length(syllables)) then
        begin
            Exit;
        end;
        for idx := start_idx to start_idx + span_len - 1 do
        begin
            Result := Result + normalize_pinyin_text(syllables[idx].text);
        end;
    end;

    function query_path_evidence(const raw_weight: Integer): Integer;
    begin
        Result := 0;
        if raw_weight <= 0 then
        begin
            Exit;
        end;
        Result := (raw_weight * 3) div 5;
        if raw_weight >= 420 then
        begin
            Inc(Result, 28);
        end;
        if raw_weight >= 620 then
        begin
            Inc(Result, 42);
        end;
        if raw_weight >= 820 then
        begin
            Inc(Result, 54);
        end;
        Result := Min(c_query_path_evidence_cap, Result);
    end;

    function try_get_particle_text(const pinyin_value: string;
        const text_value_local: string; out normalized_particle: string): Boolean;
    begin
        normalized_particle := normalize_pinyin_text(pinyin_value);
        Result :=
            ((normalized_particle = 'de') and
            SameText(text_value_local, string(Char($7684)))) or
            ((normalized_particle = 'le') and
            SameText(text_value_local, string(Char($4E86)))) or
            ((normalized_particle = 'zhe') and
            SameText(text_value_local, string(Char($7740)))) or
            ((normalized_particle = 'la') and
            SameText(text_value_local, string(Char($5566)))) or
            ((normalized_particle = 'ba') and
            SameText(text_value_local, string(Char($5427)))) or
            ((normalized_particle = 'ma') and
            SameText(text_value_local, string(Char($5417)))) or
            ((normalized_particle = 'a') and
            SameText(text_value_local, string(Char($554A))));
    end;

    function exact_segment_exists(const key_value: string;
        const exact_text: string; const expected_units: Integer): Boolean;
    var
        exact_results: TncCandidateList;
        exact_idx: Integer;
        candidate_text: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (key_value = '') or (exact_text = '') or
            (get_candidate_text_unit_count(exact_text) <> expected_units) then
        begin
            Exit;
        end;
        if m_dictionary.is_base_entry(key_value, exact_text) or
            m_dictionary.is_user_entry(key_value, exact_text) then
        begin
            Exit(True);
        end;
        if not m_dictionary.lookup_exact_full_pinyin(key_value,
            exact_results) then
        begin
            Exit;
        end;
        for exact_idx := 0 to High(exact_results) do
        begin
            candidate_text := Trim(exact_results[exact_idx].text);
            if (Trim(exact_results[exact_idx].comment) = '') and
                SameText(candidate_text, exact_text) and
                (get_candidate_text_unit_count(candidate_text) =
                expected_units) and
                ((exact_results[exact_idx].source = cs_user) or
                exact_results[exact_idx].has_dict_weight) then
            begin
                Exit(True);
            end;
        end;
    end;
begin
    Result := False;
    encoded_path := '';
    evidence_score := 0;
    if m_dictionary = nil then
    begin
        Exit;
    end;

    normalized_query := normalize_pinyin_text(query_key);
    normalized_text := Trim(text_value);
    syllables := get_effective_compact_pinyin_syllables(normalized_query);
    units := split_text_units(normalized_text);
    if (Length(syllables) <> 4) or (Length(units) <> 4) then
    begin
        Exit;
    end;

    evidence_cache_key := 'S43E' + #1 + normalized_query + #1 +
        normalized_text;
    path_cache_key := 'S43P' + #1 + normalized_query + #1 + normalized_text;
    if (m_lookup_display_feature_cache <> nil) and
        m_lookup_display_feature_cache.TryGetValue(evidence_cache_key,
        cached_evidence) then
    begin
        evidence_score := cached_evidence;
        if (evidence_score > 0) and
            (m_lookup_query_latest_text_cache <> nil) then
        begin
            m_lookup_query_latest_text_cache.TryGetValue(path_cache_key,
                encoded_path);
        end;
        Exit(evidence_score > 0);
    end;

    particle_text := units[3];
    if not try_get_particle_text(syllables[3].text, particle_text,
        particle_key) then
    begin
        cache_result;
        Exit;
    end;
    if not exact_segment_exists(particle_key, particle_text, 1) then
    begin
        cache_result;
        Exit;
    end;

    prefix_query := build_query_span_key(0, 3);
    prefix_text := units[0] + units[1] + units[2];
    if (prefix_query = '') or (prefix_text = '') or
        (not m_dictionary.get_exact_pair_path_evidence(prefix_query,
        model_paths)) then
    begin
        cache_result;
        Exit;
    end;

    for model_idx := 0 to High(model_paths) do
    begin
        candidate_path := Trim(model_paths[model_idx].encoded_path);
        if get_encoded_path_segment_count_local(candidate_path) <> 2 then
        begin
            Continue;
        end;
        parts := candidate_path.Split([c_segment_path_separator]);
        if Length(parts) <> 2 then
        begin
            Continue;
        end;
        parts[0] := Trim(parts[0]);
        parts[1] := Trim(parts[1]);
        first_units := get_candidate_text_unit_count(parts[0]);
        second_units := get_candidate_text_unit_count(parts[1]);
        if (first_units < 1) or (first_units > 2) or
            (second_units < 1) or (second_units > 2) or
            (first_units + second_units <> 3) or
            (not SameText(parts[0] + parts[1], prefix_text)) then
        begin
            Continue;
        end;

        first_key := build_query_span_key(0, first_units);
        second_key := build_query_span_key(first_units, second_units);
        if (not exact_segment_exists(first_key, parts[0], first_units)) or
            (not exact_segment_exists(second_key, parts[1], second_units)) then
        begin
            Continue;
        end;

        candidate_evidence := 0;
        if (model_paths[model_idx].lm_transition_weight >=
            c_min_lm_raw_weight) or
            ((model_paths[model_idx].lm_transition_weight >=
            c_min_corroborated_raw_weight) and
            (model_paths[model_idx].query_path_weight >=
            c_min_corroborated_raw_weight)) then
        begin
            candidate_evidence := Min(c_model_evidence_cap,
                3 * model_paths[model_idx].lm_transition_weight);
        end;
        if model_paths[model_idx].query_path_weight >=
            c_min_query_path_raw_weight then
        begin
            candidate_evidence := Max(candidate_evidence,
                query_path_evidence(
                model_paths[model_idx].query_path_weight));
        end;
        if candidate_evidence <= 0 then
        begin
            Continue;
        end;

        path_penalty := Max(0, get_cached_query_segment_path_penalty(
            prefix_query, candidate_path));
        candidate_evidence := Max(0, candidate_evidence - path_penalty);
        if candidate_evidence > evidence_score then
        begin
            evidence_score := candidate_evidence;
            encoded_path := candidate_path + c_segment_path_separator +
                particle_text;
        end;
    end;

    cache_result;
    Result := evidence_score > 0;
end;

function TncEngine.is_current_short_two_single_pair_candidate_supported(
    const query_key: string; const candidate_text: string): Boolean;
var
    idx: Integer;
    normalized_query: string;
    normalized_text: string;
begin
    Result := False;
    normalized_query := normalize_pinyin_text(query_key);
    normalized_text := Trim(candidate_text);
    if (normalized_query = '') or (normalized_text = '') or
        (not SameText(normalized_query, m_short_two_single_pair_query)) then
    begin
        Exit;
    end;

    for idx := 0 to High(m_short_two_single_pair_texts) do
    begin
        if SameText(Trim(m_short_two_single_pair_texts[idx]), normalized_text) then
        begin
            Exit(True);
        end;
    end;
end;

function TncEngine.is_current_short_three_exact_pair_candidate_supported(
    const query_key: string; const candidate_text: string): Boolean;
var
    idx: Integer;
    normalized_query: string;
    normalized_text: string;
begin
    Result := False;
    normalized_query := normalize_pinyin_text(query_key);
    normalized_text := Trim(candidate_text);
    if (normalized_query = '') or (normalized_text = '') or
        (not SameText(normalized_query, m_short_three_exact_pair_query)) then
    begin
        Exit;
    end;

    for idx := 0 to High(m_short_three_exact_pair_texts) do
    begin
        if SameText(Trim(m_short_three_exact_pair_texts[idx]), normalized_text) then
        begin
            Exit(True);
        end;
    end;
end;

function TncEngine.is_current_short_exact_lexicon_completion(
    const query_key: string; const candidate_text: string): Boolean;
var
    idx: Integer;
    normalized_query: string;
    normalized_text: string;
begin
    Result := False;
    normalized_query := normalize_pinyin_text(query_key);
    normalized_text := Trim(candidate_text);
    if (normalized_query = '') or (normalized_text = '') or
        (not SameText(normalized_query,
        m_short_exact_lexicon_completion_query)) then
    begin
        Exit;
    end;

    for idx := 0 to High(m_short_exact_lexicon_completion_texts) do
    begin
        if SameText(Trim(m_short_exact_lexicon_completion_texts[idx]),
            normalized_text) then
        begin
            Exit(True);
        end;
    end;
end;

procedure TncEngine.clear_short_exact_pair_tail_handoff;
begin
    m_short_exact_pair_tail_query := '';
    SetLength(m_short_exact_pair_tail_candidates, 0);
end;

procedure TncEngine.prepare_short_exact_pair_tail_handoff(
    const full_query: string; const selected_text: string;
    const remaining_pinyin: string);
var
    normalized_full_query: string;
    normalized_remaining: string;
    selected_prefix: string;
    pair_text: string;
    tail_text: string;
    remaining_syllables: TncPinyinParseResult;
    exact_tail_candidates: TncCandidateList;
    pair_idx: Integer;
    candidate_idx: Integer;
    stored_idx: Integer;
    duplicate: Boolean;
begin
    clear_short_exact_pair_tail_handoff;
    if m_dictionary = nil then
    begin
        Exit;
    end;

    normalized_full_query := normalize_pinyin_text(full_query);
    normalized_remaining := normalize_pinyin_text(remaining_pinyin);
    selected_prefix := Trim(selected_text);
    if (normalized_full_query = '') or (normalized_remaining = '') or
        (selected_prefix = '') or
        (not SameText(normalized_full_query, m_short_three_exact_pair_query)) then
    begin
        Exit;
    end;

    remaining_syllables := get_effective_compact_pinyin_syllables(
        normalized_remaining);
    if (Length(remaining_syllables) <= 0) or
        (not m_dictionary.lookup_isolated_exact_component(
        normalized_remaining, exact_tail_candidates)) then
    begin
        Exit;
    end;

    for pair_idx := 0 to High(m_short_three_exact_pair_texts) do
    begin
        pair_text := Trim(m_short_three_exact_pair_texts[pair_idx]);
        if (Length(pair_text) <= Length(selected_prefix)) or
            (not SameText(Copy(pair_text, 1, Length(selected_prefix)),
            selected_prefix)) then
        begin
            Continue;
        end;

        tail_text := Copy(pair_text, Length(selected_prefix) + 1,
            MaxInt);
        if (tail_text = '') or
            (get_candidate_text_unit_count(tail_text) <>
            Length(remaining_syllables)) then
        begin
            Continue;
        end;

        for candidate_idx := 0 to High(exact_tail_candidates) do
        begin
            if (Trim(exact_tail_candidates[candidate_idx].comment) <> '') or
                (not SameText(Trim(exact_tail_candidates[candidate_idx].text),
                tail_text)) then
            begin
                Continue;
            end;

            duplicate := False;
            for stored_idx := 0 to High(m_short_exact_pair_tail_candidates) do
            begin
                if SameText(Trim(m_short_exact_pair_tail_candidates[
                    stored_idx].text), tail_text) then
                begin
                    duplicate := True;
                    Break;
                end;
            end;
            if not duplicate then
            begin
                stored_idx := Length(m_short_exact_pair_tail_candidates);
                SetLength(m_short_exact_pair_tail_candidates, stored_idx + 1);
                m_short_exact_pair_tail_candidates[stored_idx] :=
                    exact_tail_candidates[candidate_idx];
            end;
            Break;
        end;
    end;

    if Length(m_short_exact_pair_tail_candidates) > 0 then
    begin
        m_short_exact_pair_tail_query := normalized_remaining;
    end;
end;

procedure TncEngine.apply_short_exact_pair_tail_handoff;
var
    target_idx: Integer;
    candidate_idx: Integer;
    move_idx: Integer;
    insert_idx: Integer;
    total_limit: Integer;
    picked: TncCandidate;
    changed: Boolean;
begin
    if (m_short_exact_pair_tail_query = '') or
        (Length(m_short_exact_pair_tail_candidates) = 0) or
        (not SameText(normalize_pinyin_text(m_composition_text),
        m_short_exact_pair_tail_query)) then
    begin
        Exit;
    end;

    changed := False;
    for target_idx := High(m_short_exact_pair_tail_candidates) downto 0 do
    begin
        candidate_idx := -1;
        for move_idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[move_idx].comment) = '') and
                SameText(Trim(m_candidates[move_idx].text),
                Trim(m_short_exact_pair_tail_candidates[target_idx].text)) then
            begin
                candidate_idx := move_idx;
                Break;
            end;
        end;

        picked := m_short_exact_pair_tail_candidates[target_idx];
        if candidate_idx >= 0 then
        begin
            picked := m_candidates[candidate_idx];
            if picked.source = cs_user then
            begin
                Continue;
            end;
            for move_idx := candidate_idx to High(m_candidates) - 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx + 1];
            end;
            SetLength(m_candidates, Length(m_candidates) - 1);
        end;

        insert_idx := 0;
        while (insert_idx < Length(m_candidates)) and
            (m_candidates[insert_idx].source = cs_user) and
            (Trim(m_candidates[insert_idx].comment) = '') do
        begin
            Inc(insert_idx);
        end;

        SetLength(m_candidates, Length(m_candidates) + 1);
        for move_idx := High(m_candidates) downto insert_idx + 1 do
        begin
            m_candidates[move_idx] := m_candidates[move_idx - 1];
        end;
        m_candidates[insert_idx] := picked;
        changed := True;
    end;

    if not changed then
    begin
        Exit;
    end;

    total_limit := get_total_candidate_limit;
    if (total_limit > 0) and (Length(m_candidates) > total_limit) then
    begin
        SetLength(m_candidates, total_limit);
    end;
    m_page_index := 0;
    m_selected_index := 0;
    m_last_candidates_incrementally_reusable := False;
    m_visible_candidates_cache_valid := False;
    SetLength(m_visible_candidates_cache, 0);
    SetLength(m_visible_candidate_source_indices_cache, 0);
    refresh_candidate_segment_paths;
    normalize_page_and_selection;
end;

function TncEngine.get_cached_query_latest_choice_text(
    const query_key: string): string;
var
    cache_key: string;
begin
    Result := '';
    cache_key := Trim(query_key);
    if (m_dictionary = nil) or (cache_key = '') then
    begin
        Exit;
    end;
    if (m_lookup_query_latest_text_cache <> nil) and
        m_lookup_query_latest_text_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;
    Result := Trim(m_dictionary.get_query_latest_choice_text(cache_key));
    if m_lookup_query_latest_text_cache <> nil then
    begin
        m_lookup_query_latest_text_cache.AddOrSetValue(cache_key, Result);
    end;
end;

procedure TncEngine.clear_session_learning_after_user_removal;
begin
    // A user deletion is an explicit negative signal. Query/latest/path/context
    // session caches can otherwise resurrect the removed phrase immediately.
    if m_context_pairs <> nil then
    begin
        m_context_pairs.Clear;
    end;
    if m_context_order <> nil then
    begin
        m_context_order.Clear;
    end;
    if m_phrase_context_pairs <> nil then
    begin
        m_phrase_context_pairs.Clear;
    end;
    if m_phrase_context_order <> nil then
    begin
        m_phrase_context_order.Clear;
    end;
    if m_phrase_context_last_seen <> nil then
    begin
        m_phrase_context_last_seen.Clear;
    end;
    if m_context_db_bonus_cache <> nil then
    begin
        m_context_db_bonus_cache.Clear;
        m_context_db_bonus_cache_key := '';
    end;

    if m_session_text_counts <> nil then
    begin
        m_session_text_counts.Clear;
    end;
    if m_session_text_last_seen <> nil then
    begin
        m_session_text_last_seen.Clear;
    end;
    if m_session_text_order <> nil then
    begin
        m_session_text_order.Clear;
    end;

    if m_session_query_choice_counts <> nil then
    begin
        m_session_query_choice_counts.Clear;
    end;
    if m_session_query_choice_last_seen <> nil then
    begin
        m_session_query_choice_last_seen.Clear;
    end;
    if m_session_query_choice_order <> nil then
    begin
        m_session_query_choice_order.Clear;
    end;
    if m_session_query_latest_text <> nil then
    begin
        m_session_query_latest_text.Clear;
    end;

    if m_session_query_path_choice_counts <> nil then
    begin
        m_session_query_path_choice_counts.Clear;
    end;
    if m_session_query_path_choice_last_seen <> nil then
    begin
        m_session_query_path_choice_last_seen.Clear;
    end;
    if m_session_query_path_choice_order <> nil then
    begin
        m_session_query_path_choice_order.Clear;
    end;
    if m_session_ranked_query_paths <> nil then
    begin
        m_session_ranked_query_paths.Clear;
    end;
    if m_session_ranked_query_path_scores <> nil then
    begin
        m_session_ranked_query_path_scores.Clear;
    end;
    if m_session_ranked_query_path_order <> nil then
    begin
        m_session_ranked_query_path_order.Clear;
    end;

    if m_session_context_query_choice_counts <> nil then
    begin
        m_session_context_query_choice_counts.Clear;
    end;
    if m_session_context_query_choice_last_seen <> nil then
    begin
        m_session_context_query_choice_last_seen.Clear;
    end;
    if m_session_context_query_choice_order <> nil then
    begin
        m_session_context_query_choice_order.Clear;
    end;
    if m_session_context_query_latest_text <> nil then
    begin
        m_session_context_query_latest_text.Clear;
    end;

    clear_lookup_bonus_caches;
end;

function TncEngine.build_candidate_identity_key(const candidate_text: string; const comment_text: string): string;
begin
    Result := LowerCase(Trim(candidate_text)) + #1 +
        LowerCase(Trim(comment_text));
end;

procedure TncEngine.clear_segment_path_tracking;
begin
    m_pending_commit_segment_path := '';
    m_pending_commit_query_key := '';
    SetLength(m_candidate_segment_paths, 0);
    if m_current_segment_path_map <> nil then
    begin
        m_current_segment_path_map.Clear;
    end;
    if m_current_segment_path_score_map <> nil then
    begin
        m_current_segment_path_score_map.Clear;
    end;
    if m_current_segment_path_query_prefix_map <> nil then
    begin
        m_current_segment_path_query_prefix_map.Clear;
    end;
end;

procedure TncEngine.remember_segment_path_for_candidate(const candidate_text: string; const comment_text: string;
    const encoded_path: string; const path_score_hint: Integer = Low(Integer));
var
    key: string;
    existing_path: string;
    existing_preference: Integer;
    existing_score_hint: Integer;
    new_preference: Integer;
    existing_segment_count: Integer;
    new_segment_count: Integer;
begin
    if (encoded_path = '') or (m_current_segment_path_map = nil) then
    begin
        Exit;
    end;

    key := build_candidate_identity_key(candidate_text, comment_text);
    if key = '' then
    begin
        Exit;
    end;

    if m_current_segment_path_map.TryGetValue(key, existing_path) then
    begin
        remember_segment_path_score_hint_for_candidate(candidate_text, comment_text, path_score_hint);

        // The same visible candidate text can be reached through multiple segment paths.
        // Keep the path with fewer segments so phrase-context learning records the more
        // natural chunk boundary instead of over-splitting the phrase.
        existing_preference := get_segment_path_preference_score(existing_path) +
            get_incremental_path_stability_bonus_for_path(existing_path);
        new_preference := get_segment_path_preference_score(encoded_path) +
            get_incremental_path_stability_bonus_for_path(encoded_path);
        existing_segment_count := get_encoded_path_segment_count_local(existing_path);
        new_segment_count := get_encoded_path_segment_count_local(encoded_path);

        if (existing_segment_count > 1) and (new_segment_count <= 1) and
            (new_preference <= existing_preference + 240) then
        begin
            Exit;
        end;

        if (new_preference > existing_preference + 24) or
            ((Abs(new_preference - existing_preference) <= 24) and
            (path_score_hint > Low(Integer)) and
            (m_current_segment_path_score_map <> nil) and
            m_current_segment_path_score_map.TryGetValue(key, existing_score_hint) and
            (path_score_hint > existing_score_hint + 48)) or
            ((new_preference = existing_preference) and
            (get_encoded_path_segment_count_local(encoded_path) < get_encoded_path_segment_count_local(existing_path))) or
            ((new_preference = existing_preference) and
            (get_encoded_path_segment_count_local(encoded_path) = get_encoded_path_segment_count_local(existing_path)) and
            (Length(encoded_path) < Length(existing_path))) then
        begin
            m_current_segment_path_map.AddOrSetValue(key, encoded_path);
            remember_segment_path_score_hint_for_candidate(candidate_text, comment_text, path_score_hint);
        end;
        Exit;
    end;

    m_current_segment_path_map.Add(key, encoded_path);
    remember_segment_path_score_hint_for_candidate(candidate_text, comment_text, path_score_hint);
end;

procedure TncEngine.remember_segment_path_score_hint_for_candidate(const candidate_text: string;
    const comment_text: string; const path_score_hint: Integer);
var
    existing_score_hint: Integer;
    key: string;
begin
    if (m_current_segment_path_score_map = nil) or (path_score_hint <= Low(Integer)) then
    begin
        Exit;
    end;

    key := build_candidate_identity_key(candidate_text, comment_text);
    if key = '' then
    begin
        Exit;
    end;

    if (not m_current_segment_path_score_map.TryGetValue(key, existing_score_hint)) or
        (path_score_hint > existing_score_hint) then
    begin
        m_current_segment_path_score_map.AddOrSetValue(key, path_score_hint);
    end;
end;

procedure TncEngine.remember_segment_path_query_prefix(const encoded_path: string; const query_prefix: string);
var
    normalized_path: string;
    normalized_query: string;
    existing_query: string;
begin
    normalized_path := Trim(encoded_path);
    normalized_query := normalize_pinyin_text(query_prefix);
    if (normalized_path = '') or (normalized_query = '') or
        (get_encoded_path_segment_count_local(normalized_path) <= 1) or
        (m_current_segment_path_query_prefix_map = nil) then
    begin
        Exit;
    end;

    if m_current_segment_path_query_prefix_map.TryGetValue(normalized_path, existing_query) then
    begin
        if (existing_query = '') or (Length(normalized_query) > Length(existing_query)) then
        begin
            m_current_segment_path_query_prefix_map.AddOrSetValue(normalized_path, normalized_query);
        end;
        Exit;
    end;

    m_current_segment_path_query_prefix_map.Add(normalized_path, normalized_query);
end;

function TncEngine.get_query_prefix_for_segment_path(const encoded_path: string): string;
var
    normalized_path: string;
begin
    Result := '';
    normalized_path := Trim(encoded_path);
    if (normalized_path = '') or (m_current_segment_path_query_prefix_map = nil) then
    begin
        Exit;
    end;

    if not m_current_segment_path_query_prefix_map.TryGetValue(normalized_path, Result) then
    begin
        Result := '';
    end;
end;

function TncEngine.get_segment_path_for_candidate(const candidate: TncCandidate;
    const candidate_index: Integer = -1): string;
var
    key: string;
begin
    Result := '';
    if (candidate_index >= 0) and (candidate_index < Length(m_candidate_segment_paths)) then
    begin
        Result := m_candidate_segment_paths[candidate_index];
        if Result <> '' then
        begin
            Exit;
        end;
    end;

    if m_current_segment_path_map = nil then
    begin
        Exit;
    end;

    key := build_candidate_identity_key(candidate.text, candidate.comment);
    if (key = '') or (not m_current_segment_path_map.TryGetValue(key, Result)) then
    begin
        Result := '';
    end;
end;

function TncEngine.infer_segment_path_for_query_text_with_score(
    const query_text: string; const selected_text: string;
    out out_score: Integer; out out_segment_count: Integer;
    const allow_any_single_char: Boolean): string;
type
    TncInferredPathState = record
        valid: Boolean;
        score: Integer;
        segment_count: Integer;
        path_text: string;
    end;
    const
        c_infer_segment_max_syllables = 40;
        c_infer_segment_word_max_syllables = 5;
        c_infer_segment_lookup_probe_limit = 48;
        c_infer_segment_rank_step = 240;
var
    syllables: TncPinyinParseResult;
    target_units: TArray<string>;
    lookup_cache: TDictionary<string, TncCandidateList>;
    memo: TDictionary<string, TncInferredPathState>;
    normalized_query_text: string;
    trimmed_selected_text: string;
    inferred_cache_key: string;
    inferred_cache_value: TncInferredPathCacheValue;
    function build_syllable_key(const start_index: Integer; const syllable_count: Integer): string;
    var
        idx: Integer;
        end_index: Integer;
    begin
        Result := '';
        if (syllable_count <= 0) or (start_index < 0) or (start_index > High(syllables)) then
        begin
            Exit;
        end;

        end_index := start_index + syllable_count - 1;
        if end_index > High(syllables) then
        begin
            end_index := High(syllables);
        end;

        for idx := start_index to end_index do
        begin
            Result := Result + syllables[idx].text;
        end;
    end;

    function lookup_cached(const pinyin_key: string; out out_results: TncCandidateList): Boolean;
    var
        build_cache_key: string;
        full_results: TncCandidateList;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        if lookup_cache.TryGetValue(pinyin_key, out_results) then
        begin
            Exit(Length(out_results) > 0);
        end;

        build_cache_key := '#exact#' + pinyin_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(build_cache_key, full_results) then
        begin
            out_results := Copy(full_results, 0, Length(full_results));
            if Length(out_results) > c_infer_segment_lookup_probe_limit then
            begin
                SetLength(out_results, c_infer_segment_lookup_probe_limit);
            end;
            lookup_cache.AddOrSetValue(pinyin_key, out_results);
            Exit(Length(out_results) > 0);
        end;

        if not m_dictionary.lookup_exact_full_pinyin(pinyin_key, full_results) then
        begin
            SetLength(full_results, 0);
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(build_cache_key, full_results);
        end;
        out_results := Copy(full_results, 0, Length(full_results));
        if Length(out_results) > c_infer_segment_lookup_probe_limit then
        begin
            SetLength(out_results, c_infer_segment_lookup_probe_limit);
        end;
        lookup_cache.AddOrSetValue(pinyin_key, out_results);
        Result := Length(out_results) > 0;
    end;

    function is_better_state(const candidate_state: TncInferredPathState;
        const current_state: TncInferredPathState): Boolean;
    const
        c_segment_count_tiebreak_margin = 480;
    var
        candidate_multi: Boolean;
        current_multi: Boolean;
    begin
        if not candidate_state.valid then
        begin
            Exit(False);
        end;
        if not current_state.valid then
        begin
            Exit(True);
        end;

        candidate_multi := candidate_state.segment_count > 1;
        current_multi := current_state.segment_count > 1;
        if candidate_multi <> current_multi then
        begin
            Exit(candidate_multi);
        end;

        if candidate_state.score > current_state.score +
            c_segment_count_tiebreak_margin then
        begin
            Exit(True);
        end;
        if current_state.score > candidate_state.score +
            c_segment_count_tiebreak_margin then
        begin
            Exit(False);
        end;

        if candidate_state.segment_count <> current_state.segment_count then
        begin
            Exit(candidate_state.segment_count < current_state.segment_count);
        end;

        if candidate_state.score <> current_state.score then
        begin
            Exit(candidate_state.score > current_state.score);
        end;

        Result := Length(candidate_state.path_text) < Length(current_state.path_text);
    end;

    function matches_target_units(const candidate_text: string; const unit_index: Integer;
        out matched_units: Integer): Boolean;
    var
        candidate_units: TArray<string>;
        idx: Integer;
    begin
        Result := False;
        matched_units := 0;
        if candidate_text = '' then
        begin
            Exit;
        end;

        candidate_units := split_text_units(candidate_text);
        matched_units := Length(candidate_units);
        if (matched_units <= 0) or (unit_index + matched_units > Length(target_units)) then
        begin
            matched_units := 0;
            Exit;
        end;

        for idx := 0 to matched_units - 1 do
        begin
            if candidate_units[idx] <> target_units[unit_index + idx] then
            begin
                matched_units := 0;
                Exit(False);
            end;
        end;

        Result := True;
    end;

    function get_first_segment_text_local(const encoded_path: string): string;
    var
        separator_pos: Integer;
    begin
        Result := Trim(encoded_path);
        if Result = '' then
        begin
            Exit;
        end;

        separator_pos := Pos(c_segment_path_separator, Result);
        if separator_pos > 0 then
        begin
            Result := Trim(Copy(Result, 1, separator_pos - 1));
        end;
    end;

    function is_allowed_infer_single_char_local(
        const candidate_text: string): Boolean;
    begin
        Result := False;
        if Length(candidate_text) <> 1 then
        begin
            Exit;
        end;

        case Ord(candidate_text[1]) of
            $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
            $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $53EA, $628A, $88AB,
            $8BA9, $7ED9, $5411, $5BF9, $8DDF, $540C, $4ECE, $4E0E, $4E8E, $5C31,
            $4E5F, $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2,
            $5C06, $5230, $7684, $4E86, $7740, $8FC7, $4F46, $867D, $800C, $5E76,
            $5374, $4E14:
                Result := True;
        end;
    end;

    function is_modifier_infer_single_char_local(
        const candidate_text: string): Boolean;
    begin
        Result := False;
        if Length(candidate_text) <> 1 then
        begin
            Exit;
        end;

        case Ord(candidate_text[1]) of
            $662F, $7684, $4E86, $5728, $548C, $4E0D, $518D, $9AD8,
            $5F88, $592A, $6700, $66F4, $6CA1, $8981, $4F1A, $80FD,
            $628A, $88AB, $8BA9, $7ED9, $5982, $8001, $5C0F, $5927,
            $5168, $8D85, $771F, $597D, $800C, $5E76:
                Result := True;
        end;
    end;

    function solve(const syllable_index: Integer; const unit_index: Integer): TncInferredPathState;
    var
        cache_key: string;
        segment_len: Integer;
        next_syllable_index: Integer;
        pinyin_key: string;
        lookup_results: TncCandidateList;
        candidate_index: Integer;
        candidate: TncCandidate;
        matched_units: Integer;
        tail_state: TncInferredPathState;
        candidate_state: TncInferredPathState;
        first_tail_segment_text: string;
        candidate_weight: Integer;
        best_segment_weight: Integer;
        scan_index: Integer;
        scan_candidate: TncCandidate;
        scan_text: string;
        scan_weight: Integer;
        context_bonus: Integer;
    begin
        Result.valid := False;
        Result.score := 0;
        Result.segment_count := 0;
        Result.path_text := '';

        if (syllable_index = Length(syllables)) and (unit_index = Length(target_units)) then
        begin
            Result.valid := True;
            Exit;
        end;

        if (syllable_index >= Length(syllables)) or (unit_index >= Length(target_units)) then
        begin
            Exit;
        end;

        cache_key := IntToStr(syllable_index) + '#' + IntToStr(unit_index);
        if memo.TryGetValue(cache_key, Result) then
        begin
            Exit;
        end;

        for segment_len := 1 to c_infer_segment_word_max_syllables do
        begin
            next_syllable_index := syllable_index + segment_len;
            if next_syllable_index > Length(syllables) then
            begin
                Break;
            end;

            pinyin_key := build_syllable_key(syllable_index, segment_len);
            if not lookup_cached(pinyin_key, lookup_results) then
            begin
                Continue;
            end;

            best_segment_weight := Low(Integer);
            for scan_index := 0 to High(lookup_results) do
            begin
                if scan_index >= c_infer_segment_lookup_probe_limit then
                begin
                    Break;
                end;
                scan_candidate := lookup_results[scan_index];
                if scan_candidate.comment <> '' then
                begin
                    Continue;
                end;
                scan_text := Trim(scan_candidate.text);
                if (scan_text = '') or
                    (get_candidate_text_unit_count(scan_text) <> segment_len) then
                begin
                    Continue;
                end;
                scan_weight := scan_candidate.score;
                if scan_candidate.has_dict_weight and
                    (scan_candidate.dict_weight > scan_weight) then
                begin
                    scan_weight := scan_candidate.dict_weight;
                end;
                if scan_weight > best_segment_weight then
                begin
                    best_segment_weight := scan_weight;
                end;
            end;

            for candidate_index := 0 to High(lookup_results) do
            begin
                candidate := lookup_results[candidate_index];
                if candidate.comment <> '' then
                begin
                    Continue;
                end;

                if not matches_target_units(candidate.text, unit_index, matched_units) then
                begin
                    Continue;
                end;
                if (matched_units = 1) and (not allow_any_single_char) and
                    (not is_allowed_infer_single_char_local(candidate.text)) then
                begin
                    Continue;
                end;

                candidate_weight := candidate.score;
                if candidate.has_dict_weight and
                    (candidate.dict_weight > candidate_weight) then
                begin
                    candidate_weight := candidate.dict_weight;
                end;

                tail_state := solve(next_syllable_index, unit_index + matched_units);
                if not tail_state.valid then
                begin
                    Continue;
                end;

                candidate_state.valid := True;
                candidate_state.score := candidate.score + tail_state.score +
                    ((12 - Min(candidate_index, 11)) * c_infer_segment_rank_step);
                if matched_units >= 2 then
                begin
                    Inc(candidate_state.score, matched_units * 1000);
                end
                else if is_modifier_infer_single_char_local(candidate.text) then
                begin
                    Inc(candidate_state.score, 600);
                end
                else
                begin
                    Dec(candidate_state.score, 900);
                end;
                first_tail_segment_text := get_first_segment_text_local(
                    tail_state.path_text);
                if (m_dictionary <> nil) and (first_tail_segment_text <> '') then
                begin
                    context_bonus := m_dictionary.get_context_bonus(candidate.text,
                        first_tail_segment_text);
                    if (best_segment_weight <> Low(Integer)) and
                        (best_segment_weight - candidate_weight >= 240) then
                    begin
                        context_bonus := Min(context_bonus, 240);
                    end;
                    Inc(candidate_state.score,
                        context_bonus * 4);
                end;
                candidate_state.segment_count := tail_state.segment_count + 1;
                candidate_state.path_text := candidate.text;
                if tail_state.path_text <> '' then
                begin
                    candidate_state.path_text := candidate_state.path_text + c_segment_path_separator +
                        tail_state.path_text;
                end;

                if is_better_state(candidate_state, Result) then
                begin
                    Result := candidate_state;
                end;
            end;
        end;

        memo.AddOrSetValue(cache_key, Result);
    end;
var
    inferred_state: TncInferredPathState;
begin
    Result := '';
    out_score := 0;
    out_segment_count := 0;
    if (m_dictionary = nil) or (selected_text = '') or (query_text = '') then
    begin
        Exit;
    end;

    normalized_query_text := normalize_pinyin_text(query_text);
    trimmed_selected_text := Trim(selected_text);
    if (normalized_query_text = '') or (trimmed_selected_text = '') then
    begin
        Exit;
    end;

    inferred_cache_key := normalized_query_text + #1 + trimmed_selected_text;
    if allow_any_single_char then
    begin
        inferred_cache_key := inferred_cache_key + #1 + 'all-single';
    end;
    if (m_lookup_inferred_segment_path_cache <> nil) and
        m_lookup_inferred_segment_path_cache.TryGetValue(inferred_cache_key,
        inferred_cache_value) then
    begin
        Result := inferred_cache_value.path_text;
        out_score := inferred_cache_value.score;
        out_segment_count := inferred_cache_value.segment_count;
        Exit;
    end;

    lookup_cache := TDictionary<string, TncCandidateList>.Create;
    memo := TDictionary<string, TncInferredPathState>.Create;
    try
        syllables := get_effective_compact_pinyin_syllables(normalized_query_text);
        if (Length(syllables) <= 1) or (Length(syllables) > c_infer_segment_max_syllables) then
        begin
            Exit;
        end;

        target_units := split_text_units(trimmed_selected_text);
        if Length(target_units) <= 1 then
        begin
            Exit;
        end;

        inferred_state := solve(0, 0);
        if inferred_state.valid and (inferred_state.segment_count > 1) then
        begin
            Result := inferred_state.path_text;
            out_score := inferred_state.score;
            out_segment_count := inferred_state.segment_count;
        end;
    finally
        memo.Free;
        lookup_cache.Free;
    end;

    if m_lookup_inferred_segment_path_cache <> nil then
    begin
        inferred_cache_value.path_text := Result;
        inferred_cache_value.score := out_score;
        inferred_cache_value.segment_count := out_segment_count;
        m_lookup_inferred_segment_path_cache.AddOrSetValue(inferred_cache_key,
            inferred_cache_value);
    end;
end;

function TncEngine.infer_segment_path_for_selected_text_with_score(
    const selected_text: string; out out_score: Integer;
    out out_segment_count: Integer): string;
begin
    Result := infer_segment_path_for_query_text_with_score(m_composition_text,
        selected_text, out_score, out_segment_count);
end;

function TncEngine.infer_segment_path_for_selected_text(const selected_text: string): string;
var
    ignored_score: Integer;
    ignored_segment_count: Integer;
begin
    Result := infer_segment_path_for_selected_text_with_score(selected_text,
        ignored_score, ignored_segment_count);
end;

procedure TncEngine.refresh_candidate_segment_paths;
var
    idx: Integer;
    key: string;
    in_idx: Integer;
    out_idx: Integer;
    normalized_query: string;
    candidate_text: string;
    query_unit_count: Integer;
    candidate_paths: TArray<string>;

    function candidate_has_backed_complete_segment_path_local(
        const candidate_value: TncCandidate;
        const path_text: string): Boolean;
    var
        path_parts: TArray<string>;
        path_idx: Integer;
        joined_text: string;
        segment_text: string;
    begin
        Result := False;
        if (candidate_value.source <> cs_rule) or
            (Trim(candidate_value.comment) <> '') or
            (query_unit_count <= 0) or
            (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
            query_unit_count) then
        begin
            Exit;
        end;

        if get_encoded_path_segment_count_local(path_text) <= 1 then
        begin
            Exit;
        end;

        path_parts := path_text.Split([c_segment_path_separator]);
        joined_text := '';
        for path_idx := 0 to High(path_parts) do
        begin
            segment_text := Trim(path_parts[path_idx]);
            if segment_text = '' then
            begin
                Exit;
            end;
            joined_text := joined_text + segment_text;
        end;

        Result := SameText(joined_text, Trim(candidate_value.text));
    end;
begin
    normalized_query := normalize_pinyin_text(m_last_lookup_key);
    if (m_dictionary <> nil) and (normalized_query <> '') and
        (Length(m_candidates) > 0) then
    begin
        SetLength(candidate_paths, Length(m_candidates));
        for idx := 0 to High(m_candidates) do
        begin
            if idx < Length(m_candidate_segment_paths) then
            begin
                candidate_paths[idx] := m_candidate_segment_paths[idx];
            end
            else
            begin
                candidate_paths[idx] := '';
            end;
        end;

        query_unit_count := get_effective_compact_pinyin_unit_count(
            normalized_query);
        out_idx := 0;
        for in_idx := 0 to High(m_candidates) do
        begin
            candidate_text := Trim(m_candidates[in_idx].text);
            if (candidate_text <> '') and
                (Trim(m_candidates[in_idx].comment) = '') and
                (not is_current_short_exact_lexicon_completion(
                normalized_query, candidate_text)) and
                (m_dictionary.get_candidate_penalty(normalized_query,
                candidate_text) > 0) and
                (not m_dictionary.is_base_entry(normalized_query,
                candidate_text)) and
                (not candidate_has_backed_complete_segment_path_local(
                m_candidates[in_idx], candidate_paths[in_idx])) then
            begin
                Continue;
            end;

            if out_idx <> in_idx then
            begin
                m_candidates[out_idx] := m_candidates[in_idx];
                candidate_paths[out_idx] := candidate_paths[in_idx];
            end;
            Inc(out_idx);
        end;
        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            SetLength(candidate_paths, out_idx);
        end;
        m_candidate_segment_paths := Copy(candidate_paths);
    end;

    if Length(m_candidate_segment_paths) <> Length(m_candidates) then
    begin
        SetLength(m_candidate_segment_paths, Length(m_candidates));
    end;
    if (Length(m_candidates) = 0) or (m_current_segment_path_map = nil) or
        (m_current_segment_path_map.Count <= 0) then
    begin
        Exit;
    end;

    for idx := 0 to High(m_candidates) do
    begin
        key := build_candidate_identity_key(m_candidates[idx].text, m_candidates[idx].comment);
        if (key <> '') and m_current_segment_path_map.TryGetValue(key, m_candidate_segment_paths[idx]) then
        begin
            Continue;
        end;
    end;
end;

procedure TncEngine.note_ranked_top_candidate;
var
    path_confidence_score: Integer;
    previous_query: string;
    normalized_query: string;
    top_path: string;
begin
    previous_query := normalize_pinyin_text(m_last_ranked_query_key);
    normalized_query := normalize_pinyin_text(m_last_lookup_key);

    if normalized_query = '' then
    begin
        m_last_ranked_query_key := '';
        m_last_ranked_top_path := '';
        Exit;
    end;

    m_last_ranked_query_key := normalized_query;

    if Length(m_candidates) = 0 then
    begin
        if (previous_query = '') or
            (Copy(normalized_query, 1, Length(previous_query)) <> previous_query) then
        begin
            m_last_ranked_top_path := '';
        end;
        Exit;
    end;

    top_path := '';
    if m_candidates[0].comment = '' then
    begin
        top_path := get_segment_path_for_candidate(m_candidates[0], 0);
    end;

    if get_encoded_path_segment_count_local(top_path) <= 1 then
    begin
        if (previous_query = '') or
            (Copy(normalized_query, 1, Length(previous_query)) <> previous_query) then
        begin
            m_last_ranked_top_path := '';
        end;
        Exit;
    end;

    m_last_ranked_top_path := top_path;
    path_confidence_score := get_candidate_path_confidence_score(m_candidates[0]);
    if path_confidence_score >= 180 then
    begin
        note_session_ranked_query_path(normalized_query, top_path, path_confidence_score);
    end;
end;

procedure TncEngine.reset(const preserve_document_context: Boolean);
begin
    clear_one_key_completion;
    clear_one_key_completion_feedback_target;
    m_composition_text := '';
    m_composition_display_text := '';
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_segment_path := '';
    m_pending_commit_query_key := '';
    m_last_lookup_key := '';
    m_last_lookup_normalized_from := '';
    m_last_lookup_syllable_count := 0;
    m_last_three_syllable_partial_preference_kind := 0;
    m_last_three_syllable_head_exact_text := '';
    m_last_three_syllable_head_strength := 0;
    m_last_three_syllable_tail_strength := 0;
    m_last_three_syllable_first_single_strength := 0;
    m_last_three_syllable_last_single_strength := 0;
    m_last_three_syllable_head_path_bonus := 0;
    m_last_three_syllable_tail_path_bonus := 0;
    m_last_three_syllable_partial_debug_info := '';
    m_last_full_path_debug_info := '';
    m_last_lookup_debug_extra := '';
    m_last_lookup_timing_info := '';
    m_last_lookup_all_initial_fast := False;
    m_last_lookup_prefix_partial_fast := False;
    m_last_lookup_short312_head_fast := False;
    m_last_lookup_incremental_partial_reuse := False;
    m_last_ranked_query_key := '';
    m_last_ranked_top_path := '';
    m_last_candidates_incrementally_reusable := True;
    m_composition_built_incrementally := False;
    m_runtime_chain_text := '';
    m_candidate_navigation_started := False;
    m_runtime_common_pattern_text := '';
    m_runtime_redup_text := '';
    SetLength(m_runtime_long_chain_candidates, 0);
    SetLength(m_runtime_long_complete_pool_candidates, 0);
    SetLength(m_runtime_long_retained_exact_edges, 0);
    m_long_complete_pool_pairwise_text := '';
    m_long_complete_pool_pairwise_insert_rank := -1;
    m_debug_target_recall_text := '';
    reset_debug_target_recall_metrics;
    m_page_index := 0;
    m_selected_index := 0;
    m_confirmed_text := '';
    m_confirmed_explicit_choice := False;
    m_recent_partial_prefix_text := '';
    m_external_left_context := '';
    if (m_document_context_model <> nil) and
        (not preserve_document_context) then
    begin
        m_document_context_model.clear;
        if m_long_local_repair <> nil then
            m_long_local_repair.set_document_context('', '');
    end;
    m_segment_left_context := '';
    m_local_repair_query_key := '';
    m_local_repair_text := '';
    m_local_repair_draft := '';
    m_local_repair_original_path := '';
    m_local_repair_baseline_text := '';
    m_local_repair_validated := Default(TncValidatedRepairPath);
    m_context_db_bonus_cache_key := '';
    SetLength(m_candidates, 0);
    SetLength(m_visible_candidates_cache, 0);
    SetLength(m_visible_candidate_source_indices_cache, 0);
    m_visible_candidates_cache_valid := False;
    SetLength(m_long_visible_candidate_pool_cache, 0);
    SetLength(m_long_visible_candidate_pool_source_indices_cache, 0);
    m_long_visible_candidate_pool_cache_key := '';
    m_long_visible_candidate_pool_source_signature := 0;
    m_long_visible_candidate_pool_cache_valid := False;
    m_long_local_rerank_generated_text := '';
    m_long_local_rerank_composition_text := '';
    m_long_local_rerank_lookup_key := '';
    m_short_two_single_pair_query := '';
    SetLength(m_short_two_single_pair_texts, 0);
    m_short_three_exact_pair_query := '';
    SetLength(m_short_three_exact_pair_texts, 0);
    m_short_three_char_pair_path := '';
    m_short_three_char_pair_evidence := 0;
    m_short_exact_lexicon_completion_query := '';
    SetLength(m_short_exact_lexicon_completion_texts, 0);
    SetLength(m_short_exact_lexicon_completion_candidates, 0);
    clear_short_exact_pair_tail_handoff;
    m_has_forced_visible_top_candidate := False;
    m_forced_visible_top_from_incremental_lm_prefix := False;
    m_forced_visible_top_composition_text := '';
    m_forced_visible_top_lookup_key := '';
    if m_confirmed_segments <> nil then
    begin
        m_confirmed_segments.Clear;
    end;
    if m_context_db_bonus_cache <> nil then
    begin
        m_context_db_bonus_cache.Clear;
    end;
    if m_build_lookup_cache <> nil then
    begin
        m_build_lookup_cache.Clear;
    end;
    if m_effective_pinyin_parse_cache <> nil then
    begin
        m_effective_pinyin_parse_cache.Clear;
    end;
    clear_segment_path_tracking;
    clear_lookup_bonus_caches;
    if m_session_ranked_query_paths <> nil then
    begin
        m_session_ranked_query_paths.Clear;
    end;
    if m_session_ranked_query_path_scores <> nil then
    begin
        m_session_ranked_query_path_scores.Clear;
    end;
    if m_session_ranked_query_path_order <> nil then
    begin
        m_session_ranked_query_path_order.Clear;
    end;
end;

procedure TncEngine.reset_debug_target_recall_metrics;
begin
    m_debug_target_final_state_found := False;
    m_debug_target_final_state_rank := 0;
    m_debug_target_final_state_count := 0;
    m_debug_target_max_generated_syllable := 0;
    m_debug_target_max_retained_syllable := 0;
    m_debug_target_chain_generated_syllable := 0;
    m_debug_target_chain_prebeam_syllable := 0;
    m_debug_target_chain_beam_syllable := 0;
    m_debug_target_full_path_generated_syllable := 0;
    m_debug_target_full_path_retained_syllable := 0;
    m_debug_target_final_state_source_mask := 0;
    m_debug_target_chain_final_state_rank := 0;
    m_debug_target_light_final_state_rank := 0;
    m_debug_target_lattice_final_state_rank := 0;
    m_debug_target_oracle_final_state_rank := 0;
    m_debug_target_full_final_state_rank := 0;
    m_debug_target_chain_build_attempted := False;
    m_debug_target_chain_build_accepted := False;
    m_debug_target_chain_top_score := 0;
    m_debug_target_chain_min_score := 0;
    m_debug_target_chain_search_status := 0;
    m_debug_target_candidate_stage_mask := 0;
    m_debug_target_full_path_budget_exhausted := False;
    SetLength(m_debug_long_beam_states, 0);
    SetLength(m_debug_long_exact_edge_stages, 0);
    if m_debug_long_exact_edge_stage_index <> nil then
    begin
        m_debug_long_exact_edge_stage_index.Clear;
    end;
    SetLength(m_debug_long_chain_candidates, 0);
    SetLength(m_debug_long_final_candidates, 0);
    SetLength(m_debug_long_ranking_stages, 0);
end;

function TncEngine.is_debug_target_recall_prefix(const text: string): Boolean;
var
    normalized_text: string;
begin
    normalized_text := Trim(text);
    Result := (m_debug_target_recall_text <> '') and
        (normalized_text <> '') and
        (Length(normalized_text) <= Length(m_debug_target_recall_text)) and
        SameText(normalized_text,
            Copy(m_debug_target_recall_text, 1, Length(normalized_text)));
end;

procedure TncEngine.debug_note_long_exact_edge_stage(const position: Integer;
    const input_syllable_count: Integer; const query_key: string;
    const text: string; const chunk_units: Integer;
    const exact_rank: Integer; const raw_weight: Integer;
    const top_weight: Integer; const effective_score: Integer;
    const edge_score: Int64; const source_user: Boolean;
    const baseline_option: Boolean; const stage_mask: Integer);
var
    normalized_text: string;
    key: string;
    idx: Integer;
    item: TncLongExactEdgeStageDebug;
begin
    if (not m_debug_capture_long_beam_states) or
        (m_debug_target_recall_text = '') or (position < 0) or
        (chunk_units <= 0) or (exact_rank <= 0) or (stage_mask = 0) then
    begin
        Exit;
    end;
    normalized_text := Trim(text);
    if normalized_text = '' then
    begin
        Exit;
    end;
    key := IntToStr(position) + #1 + IntToStr(chunk_units) + #1 +
        LowerCase(normalized_text);
    if (m_debug_long_exact_edge_stage_index <> nil) and
        m_debug_long_exact_edge_stage_index.TryGetValue(key, idx) then
    begin
        item := m_debug_long_exact_edge_stages[idx];
        item.stage_mask := item.stage_mask or stage_mask;
        item.baseline_option := item.baseline_option or baseline_option;
        item.source_user := item.source_user or source_user;
        item.target := item.target or SameText(normalized_text,
            Copy(m_debug_target_recall_text, position + 1,
            Length(normalized_text)));
        if (item.exact_rank <= 0) or (exact_rank < item.exact_rank) then
        begin
            item.exact_rank := exact_rank;
            item.raw_weight := raw_weight;
            item.top_weight := top_weight;
            item.effective_score := effective_score;
            item.edge_score := edge_score;
            item.query_key := query_key;
        end;
        m_debug_long_exact_edge_stages[idx] := item;
        Exit;
    end;

    item := Default(TncLongExactEdgeStageDebug);
    item.position := position;
    item.input_syllable_count := input_syllable_count;
    item.chunk_units := chunk_units;
    item.exact_rank := exact_rank;
    item.raw_weight := raw_weight;
    item.top_weight := top_weight;
    item.effective_score := effective_score;
    item.edge_score := edge_score;
    item.source_user := source_user;
    item.baseline_option := baseline_option;
    item.target := SameText(normalized_text,
        Copy(m_debug_target_recall_text, position + 1,
        Length(normalized_text)));
    item.stage_mask := stage_mask;
    item.query_key := query_key;
    item.text := normalized_text;
    idx := Length(m_debug_long_exact_edge_stages);
    SetLength(m_debug_long_exact_edge_stages, idx + 1);
    m_debug_long_exact_edge_stages[idx] := item;
    if m_debug_long_exact_edge_stage_index <> nil then
    begin
        m_debug_long_exact_edge_stage_index.Add(key, idx);
    end;
end;

procedure TncEngine.debug_mark_long_exact_path_complete_pool(
    const segment_path: string);
const
    c_complete_pool_stage = 8;
var
    parts: TArray<string>;
    part: string;
    normalized_part: string;
    position: Integer;
    chunk_units: Integer;
    key: string;
    idx: Integer;
    item: TncLongExactEdgeStageDebug;
begin
    if (not m_debug_capture_long_beam_states) or
        (m_debug_long_exact_edge_stage_index = nil) or
        (Trim(segment_path) = '') then
    begin
        Exit;
    end;
    parts := segment_path.Split([c_segment_path_separator]);
    position := 0;
    for part in parts do
    begin
        normalized_part := Trim(part);
        chunk_units := get_candidate_text_unit_count(normalized_part);
        if chunk_units <= 0 then
        begin
            Continue;
        end;
        key := IntToStr(position) + #1 + IntToStr(chunk_units) + #1 +
            LowerCase(normalized_part);
        if m_debug_long_exact_edge_stage_index.TryGetValue(key, idx) then
        begin
            item := m_debug_long_exact_edge_stages[idx];
            item.stage_mask := item.stage_mask or c_complete_pool_stage;
            m_debug_long_exact_edge_stages[idx] := item;
        end;
        Inc(position, chunk_units);
    end;
end;

procedure TncEngine.debug_set_target_recall_text(const text: string);
begin
    m_debug_target_recall_text := Trim(text);
    reset_debug_target_recall_metrics;
end;

procedure TncEngine.debug_enable_long_beam_state_capture(
    const enabled: Boolean);
begin
    m_debug_capture_long_beam_states := enabled;
    // Beam reports are the training/audit surface for exact-edge recall.  The
    // expanded option set must stay diagnostic-only until a trained model has
    // passed the independent-corpus and production benchmark gates.
    m_debug_expand_long_beam_states := enabled;
    SetLength(m_debug_long_beam_states, 0);
    SetLength(m_debug_long_exact_edge_stages, 0);
    if m_debug_long_exact_edge_stage_index <> nil then
    begin
        m_debug_long_exact_edge_stage_index.Clear;
    end;
end;

procedure TncEngine.debug_enable_long_beam_state_expansion(
    const enabled: Boolean);
begin
    m_debug_expand_long_beam_states := enabled;
end;

procedure TncEngine.debug_enable_long_final_candidate_capture(
    const enabled: Boolean);
begin
    // Copy the already-ranked runtime pool without expanding search or
    // retaining every beam state. This keeps Oracle/training data faithful to
    // production candidate generation.
    m_debug_capture_long_final_candidates := enabled;
end;

procedure TncEngine.debug_enable_long_ranking_stage_capture(
    const enabled: Boolean);
begin
    m_debug_capture_long_ranking_stages := enabled;
    SetLength(m_debug_long_ranking_stages, 0);
end;

procedure TncEngine.debug_set_long_chain_beam_widths(
    const beam_width: Integer; const prebeam_width: Integer);
begin
    if beam_width > 0 then
    begin
        m_debug_long_chain_beam_width := Max(2, Min(64, beam_width));
    end
    else
    begin
        m_debug_long_chain_beam_width := 0;
    end;
    if prebeam_width > 0 then
    begin
        m_debug_long_chain_prebeam_width := Max(2, Min(128, prebeam_width));
    end
    else
    begin
        m_debug_long_chain_prebeam_width := 0;
    end;
    if (m_debug_long_chain_beam_width > 0) and
        (m_debug_long_chain_prebeam_width > 0) and
        (m_debug_long_chain_prebeam_width < m_debug_long_chain_beam_width) then
    begin
        m_debug_long_chain_prebeam_width := m_debug_long_chain_beam_width;
    end;
end;

procedure TncEngine.debug_set_long_chain_option_limits(
    const option_limit: Integer; const probe_limit: Integer);
begin
    if option_limit > 0 then
    begin
        m_debug_long_chain_option_limit := Max(2, Min(32, option_limit));
    end
    else
    begin
        m_debug_long_chain_option_limit := 0;
    end;
    if probe_limit > 0 then
    begin
        m_debug_long_chain_probe_limit := Max(4, Min(64, probe_limit));
    end
    else
    begin
        m_debug_long_chain_probe_limit := 0;
    end;
end;

procedure TncEngine.debug_set_long_chain_ranker_profile(
    const profile: Integer);
begin
    m_debug_long_chain_ranker_profile := Max(-1, Min(2, profile));
end;

procedure TncEngine.debug_set_long_final_ranker_profile(
    const profile: Integer);
begin
    m_debug_long_final_ranker_profile := Max(-1, Min(2, profile));
end;

procedure TncEngine.debug_disable_long_top2_pairwise_swap(
    const disabled: Boolean);
begin
    m_debug_disable_long_top2_pairwise_swap := disabled;
end;

procedure TncEngine.debug_set_search_budget_policy(
    const mode: TncSearchBudgetMode; const scale_percent: Integer);
begin
    m_search_budget_mode := mode;
    m_search_budget_scale_percent := EnsureRange(scale_percent, 25, 1000);
end;

procedure TncEngine.inherit_search_budget_policy_to(const engine: TncEngine);
begin
    if engine = nil then
    begin
        Exit;
    end;
    engine.m_search_budget_mode := m_search_budget_mode;
    engine.m_search_budget_scale_percent := m_search_budget_scale_percent;
end;

function TncEngine.search_budget_should_stop(const start_tick: UInt64;
    const work_count: Int64; const work_limit: Int64;
    const timed_budget_ms: UInt64;
    const emergency_budget_ms: UInt64;
    const apply_configured_policy: Boolean): Boolean;
var
    elapsed_ms: UInt64;
begin
    // Deterministic quality runs must never fall back to elapsed-time gates.
    // Some nested short-window searches bypass the production policy, but
    // their work limits still provide a stable bound in deterministic mode.
    if m_search_budget_mode = sbm_deterministic then
    begin
        Exit(nc_search_budget_should_stop(m_search_budget_mode, 0,
            work_count, work_limit, timed_budget_ms,
            emergency_budget_ms, m_search_budget_scale_percent));
    end;
    if not apply_configured_policy then
    begin
        if (start_tick = 0) or (timed_budget_ms = 0) then
        begin
            elapsed_ms := 0;
        end
        else
        begin
            elapsed_ms := nc_monotonic_tick_ms - start_tick;
        end;
        Exit(nc_search_budget_should_stop(sbm_wall_clock, elapsed_ms,
            work_count, 0, timed_budget_ms, 0, 100));
    end;
    { Direct whole-query evaluation is already bounded by work counters. A
      wall-clock cutoff here makes benchmark accuracy depend on machine load.
      Interactive incremental input keeps the production emergency cutoff. }
    if (m_search_budget_mode = sbm_production) and
        (not m_composition_built_incrementally) then
    begin
        Exit(nc_search_budget_should_stop(sbm_deterministic, 0,
            work_count, work_limit, timed_budget_ms,
            emergency_budget_ms, m_search_budget_scale_percent));
    end;
    if (work_limit > 0) and (work_count >= work_limit) then
    begin
        Exit(True);
    end;
    if ((m_search_budget_mode = sbm_wall_clock) and
        (timed_budget_ms = 0)) or
        ((m_search_budget_mode = sbm_production) and
        (emergency_budget_ms = 0)) then
    begin
        Exit(False);
    end;
    if start_tick = 0 then
    begin
        elapsed_ms := 0;
    end
    else
    begin
        elapsed_ms := nc_monotonic_tick_ms - start_tick;
    end;
    Result := nc_search_budget_should_stop(m_search_budget_mode,
        elapsed_ms, work_count, work_limit, timed_budget_ms,
        emergency_budget_ms, m_search_budget_scale_percent);
end;

procedure TncEngine.debug_set_composition_text(const text: string);
begin
    reload_dictionary_if_needed;
    m_composition_built_incrementally := False;
    m_composition_display_text := text;
    if is_shuangpin_input then
    begin
        rebuild_shuangpin_composition_from_input_code;
    end
    else
    begin
        m_composition_text := text;
    end;
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_segment_path := '';
    m_pending_commit_query_key := '';
    m_page_index := 0;
    m_selected_index := 0;
    build_candidates;
    normalize_page_and_selection;
end;

function TncEngine.debug_query_one_key_completion(const query_text: string;
    const left_context: string): TncOneKeyCompletion;
begin
    reload_dictionary_if_needed;
    m_composition_built_incrementally := False;
    m_composition_display_text := LowerCase(Trim(query_text));
    m_composition_text := m_composition_display_text;
    m_confirmed_text := '';
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    set_external_left_context(left_context);
    refresh_one_key_completion;
    Result := m_one_key_completion;
end;

function TncEngine.debug_query_one_key_completion_candidates(
    const query_text: string;
    out completions: TncOneKeyCompletionList): Boolean;
begin
    SetLength(completions, 0);
    Result := (m_dictionary <> nil) and
        m_dictionary.lookup_one_key_completions(query_text, completions);
end;

function TncEngine.debug_get_long_one_key_completion_pool(
    out completions: TncOneKeyCompletionList): Boolean;
begin
    completions := Copy(m_debug_long_one_key_completion_pool);
    Result := Length(completions) > 0;
end;

function TncEngine.debug_get_lexical_one_key_completion(
    out completion: TncOneKeyCompletion): Boolean;
begin
    completion := m_debug_lexical_one_key_completion;
    Result := Trim(completion.text) <> '';
end;

procedure TncEngine.debug_enable_long_one_key_completion_pool_capture(
    const enabled: Boolean);
begin
    m_debug_capture_long_one_key_completion_pool := enabled;
    if not enabled then
    begin
        SetLength(m_debug_long_one_key_completion_pool, 0);
    end;
end;

function TncEngine.debug_get_ranked_candidates(
    out candidates: TncCandidateList): Boolean;
begin
    candidates := Copy(m_candidates);
    Result := Length(candidates) > 0;
end;

function TncEngine.debug_score_one_key_completion_candidates(
    const query_text: string; const left_context: string;
    out completions: TncOneKeyCompletionList;
    out char_lm_scores: TArray<Integer>;
    out reverse_char_lm_scores: TArray<Integer>;
    out prefix1_lm_scores: TArray<Integer>;
    out prefix2_lm_scores: TArray<Integer>): Boolean;
const
    c_completion_context_length = 12;
var
    completion_texts: TArray<string>;
    prefix_texts: TArray<string>;
    context_value: string;
    idx: Integer;
begin
    SetLength(completions, 0);
    SetLength(char_lm_scores, 0);
    SetLength(reverse_char_lm_scores, 0);
    SetLength(prefix1_lm_scores, 0);
    SetLength(prefix2_lm_scores, 0);
    Result := (m_dictionary <> nil) and
        m_dictionary.lookup_one_key_completions(query_text, completions);
    if not Result then
    begin
        Exit;
    end;

    SetLength(completion_texts, Length(completions));
    for idx := 0 to High(completions) do
    begin
        completion_texts[idx] := completions[idx].text;
    end;
    context_value := trim_left_context_to_sentence(left_context,
        c_completion_context_length);
    if not get_cached_char_lm_scores(completion_texts, char_lm_scores,
        clsm_context, context_value) then
    begin
        SetLength(char_lm_scores, Length(completions));
    end;
    if not get_cached_char_lm_scores(completion_texts,
        reverse_char_lm_scores, clsm_reverse, '') then
    begin
        SetLength(reverse_char_lm_scores, Length(completions));
    end;

    SetLength(prefix_texts, Length(completions) * 2);
    for idx := 0 to High(completions) do
    begin
        prefix_texts[idx * 2] := Copy(completions[idx].text, 1, 1);
        prefix_texts[idx * 2 + 1] := Copy(completions[idx].text, 1, 2);
    end;
    if get_cached_char_lm_scores(prefix_texts, prefix1_lm_scores,
        clsm_context, context_value) and
        (Length(prefix1_lm_scores) = Length(prefix_texts)) then
    begin
        SetLength(prefix2_lm_scores, Length(completions));
        for idx := 0 to High(completions) do
        begin
            prefix2_lm_scores[idx] := prefix1_lm_scores[idx * 2 + 1];
            prefix1_lm_scores[idx] := prefix1_lm_scores[idx * 2];
        end;
        SetLength(prefix1_lm_scores, Length(completions));
    end
    else
    begin
        SetLength(prefix1_lm_scores, Length(completions));
        SetLength(prefix2_lm_scores, Length(completions));
    end;
end;

function TncEngine.debug_score_one_key_completion_pool(
    const completions: TncOneKeyCompletionList;
    out char_lm_scores: TArray<Integer>;
    out suffix_lm_scores: TArray<Integer>;
    out reverse_char_lm_scores: TArray<Integer>;
    out path_lm_scores: TArray<Integer>): Boolean;
var
    texts: TArray<string>;
    idx: Integer;
    query_key: string;
begin
    SetLength(char_lm_scores, Length(completions));
    SetLength(suffix_lm_scores, Length(completions));
    SetLength(reverse_char_lm_scores, Length(completions));
    SetLength(path_lm_scores, Length(completions));
    Result := (m_dictionary <> nil) and (Length(completions) > 0);
    if not Result then
    begin
        Exit;
    end;
    SetLength(texts, Length(completions));
    for idx := 0 to High(completions) do
    begin
        texts[idx] := completions[idx].text;
    end;
    if not get_cached_char_lm_scores(texts, char_lm_scores,
        clsm_full, '') then
    begin
        SetLength(char_lm_scores, Length(completions));
    end;
    if not get_cached_char_lm_scores(texts, suffix_lm_scores,
        clsm_suffix, '') then
    begin
        SetLength(suffix_lm_scores, Length(completions));
    end;
    if not get_cached_char_lm_scores(texts, reverse_char_lm_scores,
        clsm_reverse, '') then
    begin
        SetLength(reverse_char_lm_scores, Length(completions));
    end;
    for idx := 0 to High(completions) do
    begin
        query_key := normalize_pinyin_text(completions[idx].full_pinyin);
        if (query_key <> '') and (completions[idx].path_text <> '') then
        begin
            path_lm_scores[idx] := m_dictionary.get_lm_transition_bonus(
                query_key, completions[idx].path_text);
        end;
    end;
end;

procedure TncEngine.update_config(const config: TncEngineConfig);
var
    previous_config: TncEngineConfig;
    dictionary_changed: Boolean;
    input_scheme_changed: Boolean;
    fuzzy_pinyin_changed: Boolean;
    previous_page_size: Integer;
begin
    previous_config := m_config;
    previous_page_size := get_candidate_page_size;
    dictionary_changed := m_config.dictionary_variant <> config.dictionary_variant;
    input_scheme_changed := m_config.pinyin_input_scheme <> config.pinyin_input_scheme;
    fuzzy_pinyin_changed :=
        (m_config.fuzzy_pinyin_enabled <> config.fuzzy_pinyin_enabled) or
        (m_config.fuzzy_pinyin_rules <> config.fuzzy_pinyin_rules);
    m_config := config;
    nc_normalize_shortcut_config(m_config.shortcuts);
    m_config.one_key_completion_key :=
        nc_normalize_one_key_completion_key(
        m_config.one_key_completion_key);
    m_config.candidate_page_key_scheme :=
        nc_resolve_candidate_page_key_scheme(
        m_config.candidate_page_key_scheme,
        m_config.one_key_completion_key);
    if input_scheme_changed then
    begin
        reset;
    end;
    if previous_page_size <> get_candidate_page_size then
    begin
        m_page_index := 0;
        m_selected_index := 0;
        SetLength(m_visible_candidates_cache, 0);
        SetLength(m_visible_candidate_source_indices_cache, 0);
        m_visible_candidates_cache_valid := False;
    end;
    if m_dictionary <> nil then
    begin
        m_dictionary.set_debug_mode(m_config.debug_mode);
        m_dictionary.set_fuzzy_pinyin_config(
            m_config.fuzzy_pinyin_enabled, m_config.fuzzy_pinyin_rules);
    end;
    if dictionary_changed then
    begin
        store_current_dictionary_provider(previous_config);
        set_dictionary_provider(create_dictionary_from_config);
    end;
    if fuzzy_pinyin_changed and (not dictionary_changed) then
    begin
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.Clear;
        end;
        if m_composition_text <> '' then
        begin
            build_candidates;
            normalize_page_and_selection;
        end;
    end;
end;

procedure TncEngine.configure_dictionary_paths(
    const simplified_dictionary_path: string;
    const traditional_dictionary_path: string;
    const user_dictionary_path: string);
begin
    if m_dictionary_paths_configured and
        SameText(m_configured_dictionary_path_simplified,
        simplified_dictionary_path) and
        SameText(m_configured_dictionary_path_traditional,
        traditional_dictionary_path) and
        SameText(m_configured_user_dictionary_path,
        user_dictionary_path) then
    begin
        Exit;
    end;

    clear_cached_dictionary_providers;
    m_configured_dictionary_path_simplified := simplified_dictionary_path;
    m_configured_dictionary_path_traditional := traditional_dictionary_path;
    m_configured_user_dictionary_path := user_dictionary_path;
    m_dictionary_paths_configured := True;
end;

procedure TncEngine.set_dictionary_provider(const dictionary: TncDictionaryProvider);
begin
    if m_dictionary = dictionary then
    begin
        Exit;
    end;

    if m_dictionary <> nil then
    begin
        m_dictionary.Free;
    end;

    m_dictionary := dictionary;
    if m_dictionary is TncSqliteDictionary then
        TncSqliteDictionary(m_dictionary).set_user_dictionary_variant(
            m_config.dictionary_variant);
    if m_dictionary <> nil then
    begin
        m_dictionary.set_debug_mode(m_config.debug_mode);
        m_dictionary.set_fuzzy_pinyin_config(
            m_config.fuzzy_pinyin_enabled, m_config.fuzzy_pinyin_rules);
    end;
    invalidate_dictionary_lookup_caches;
    m_last_dictionary_reload_check_tick := 0;
    update_dictionary_state;
end;

procedure TncEngine.invalidate_dictionary_lookup_caches;
begin
    m_context_db_bonus_cache_key := '';
    if m_context_db_bonus_cache <> nil then
    begin
        m_context_db_bonus_cache.Clear;
    end;
    if m_build_lookup_cache <> nil then
    begin
        m_build_lookup_cache.Clear;
    end;
    if m_long_local_single_options_cache <> nil then
    begin
        m_long_local_single_options_cache.Clear;
    end;
    if m_effective_pinyin_parse_cache <> nil then
    begin
        m_effective_pinyin_parse_cache.Clear;
    end;
    clear_lookup_bonus_caches;
end;

procedure TncEngine.set_long_neural_reranker(
    const reranker: IncLongNeuralReranker);
begin
    m_long_neural_reranker := reranker;
    m_long_local_repair := nil;
    m_long_local_repair_policy := nil;
    m_long_joint_repair := nil;
    m_local_repair_query_key := '';
    m_local_repair_text := '';
    m_local_repair_draft := '';
    m_local_repair_original_path := '';
    m_local_repair_baseline_text := '';
    m_local_repair_validated := Default(TncValidatedRepairPath);
    Supports(reranker, IncLongLocalRepair, m_long_local_repair);
    Supports(reranker, IncLongLocalRepairPolicy, m_long_local_repair_policy);
    Supports(reranker, IncLongJointRepair, m_long_joint_repair);
    if (m_long_local_repair <> nil) and (m_document_context_model <> nil) then
    begin
        m_long_local_repair.set_document_context(
            m_document_context_model.document_key,
            m_document_context_model.semantic_tail);
    end;
end;

function TncEngine.detach_dictionary_provider: TncDictionaryProvider;
begin
    Result := m_dictionary;
    m_dictionary := nil;
    m_dictionary_path := '';
    m_dictionary_write_time := 0;
    m_user_dictionary_path := '';
    m_user_dictionary_write_time := 0;
    m_last_dictionary_reload_check_tick := 0;
end;

procedure TncEngine.adopt_ready_dictionary_provider(
    const dictionary: TncDictionaryProvider);
begin
    m_defer_optional_dictionary_models := False;
    set_dictionary_provider(dictionary);
end;

function TncEngine.dictionary_models_deferred: Boolean;
begin
    Result := m_defer_optional_dictionary_models;
end;

function TncEngine.rebuild_candidates_after_dictionary_upgrade: Boolean;
begin
    Result := m_composition_text <> '';
    if not Result then
    begin
        Exit;
    end;
    build_candidates;
    normalize_page_and_selection;
end;

function TncEngine.try_upgrade_dictionary_from(const ready_engine: TncEngine;
    out candidates_rebuilt: Boolean): Boolean;
begin
    candidates_rebuilt := False;
    Result := False;
    if (ready_engine = nil) or (ready_engine = Self) or
        (ready_engine.m_dictionary = nil) or
        ready_engine.dictionary_models_deferred or
        (not dictionary_models_deferred) then Exit;
    // A background handoff must not replace numbered choices already being
    // browsed, even after the user has returned to page one. Keep the standby
    // provider owned by ready_engine until the composition is edited or ended.
    if (m_composition_text <> '') and m_candidate_navigation_started then Exit;
    adopt_ready_dictionary_provider(ready_engine.detach_dictionary_provider);
    candidates_rebuilt := rebuild_candidates_after_dictionary_upgrade;
    Result := True;
end;

procedure TncEngine.free_dictionary_provider(var provider: TncDictionaryProvider);
begin
    if provider <> nil then
    begin
        provider.Free;
        provider := nil;
    end;
end;

procedure TncEngine.clear_cached_dictionary_providers;
begin
    free_dictionary_provider(m_cached_dictionary_simplified);
    free_dictionary_provider(m_cached_dictionary_traditional);
end;

function TncEngine.take_cached_dictionary_provider(const variant: TncDictionaryVariant;
    const base_path: string; const user_path: string): TncDictionaryProvider;
var
    cached_provider: TncDictionaryProvider;
    sqlite_dict: TncSqliteDictionary;
begin
    Result := nil;
    if variant = dv_traditional then
    begin
        cached_provider := m_cached_dictionary_traditional;
    end
    else
    begin
        cached_provider := m_cached_dictionary_simplified;
    end;

    if cached_provider = nil then
    begin
        Exit;
    end;

    if not (cached_provider is TncSqliteDictionary) then
    begin
        if variant = dv_traditional then
        begin
            free_dictionary_provider(m_cached_dictionary_traditional);
        end
        else
        begin
            free_dictionary_provider(m_cached_dictionary_simplified);
        end;
        Exit;
    end;

    sqlite_dict := TncSqliteDictionary(cached_provider);
    if SameText(sqlite_dict.db_path, base_path) and SameText(sqlite_dict.user_db_path, user_path) then
    begin
        Result := cached_provider;
        if variant = dv_traditional then
        begin
            m_cached_dictionary_traditional := nil;
        end
        else
        begin
            m_cached_dictionary_simplified := nil;
        end;
        Exit;
    end;

    if variant = dv_traditional then
    begin
        free_dictionary_provider(m_cached_dictionary_traditional);
    end
    else
    begin
        free_dictionary_provider(m_cached_dictionary_simplified);
    end;
end;

procedure TncEngine.store_current_dictionary_provider(const previous_config: TncEngineConfig);
var
    cache_target: ^TncDictionaryProvider;
    sqlite_dict: TncSqliteDictionary;
    expected_base_path: string;
begin
    if m_dictionary = nil then
    begin
        Exit;
    end;

    if previous_config.dictionary_variant = dv_traditional then
    begin
        cache_target := @m_cached_dictionary_traditional;
        expected_base_path := get_dictionary_path_for_variant(
            dv_traditional);
    end
    else
    begin
        cache_target := @m_cached_dictionary_simplified;
        expected_base_path := get_dictionary_path_for_variant(
            dv_simplified);
    end;

    if not (m_dictionary is TncSqliteDictionary) then
    begin
        free_dictionary_provider(m_dictionary);
        Exit;
    end;

    sqlite_dict := TncSqliteDictionary(m_dictionary);
    if (not SameText(sqlite_dict.db_path, expected_base_path)) or
        (not SameText(sqlite_dict.user_db_path,
        get_configured_user_dictionary_path)) then
    begin
        free_dictionary_provider(m_dictionary);
        Exit;
    end;

    if cache_target^ <> nil then
    begin
        free_dictionary_provider(cache_target^);
    end;
    cache_target^ := m_dictionary;
    m_dictionary := nil;
end;

function TncEngine.is_alpha_key(const key_code: Word; const key_state: TncKeyState; out normalized_char: Char;
    out display_char: Char): Boolean;
begin
    if (key_code >= Ord('A')) and (key_code <= Ord('Z')) then
    begin
        normalized_char := Char(key_code + Ord('a') - Ord('A'));
        if key_state.shift_down xor key_state.caps_lock then
        begin
            display_char := Char(key_code);
        end
        else
        begin
            display_char := normalized_char;
        end;
        Result := True;
        Exit;
    end;

    Result := False;
end;

function TncEngine.get_candidate_limit: Integer;
begin
    Result := c_default_page_size;
end;

function TncEngine.is_fuzzy_pinyin_active: Boolean;
begin
    Result := m_config.fuzzy_pinyin_enabled and
        (m_config.fuzzy_pinyin_rules <> []);
end;

function TncEngine.get_candidate_page_size: Integer;
begin
    Result := m_config.candidate_page_size;
    if (Result < c_min_candidate_page_size) or
        (Result > c_max_candidate_page_size) then
    begin
        Result := c_default_candidate_page_size;
    end;
end;

function TncEngine.get_total_candidate_limit: Integer;
begin
    Result := c_default_total_limit;
    if get_candidate_limit > Result then
    begin
        Result := get_candidate_limit;
    end;
end;

function TncEngine.create_dictionary_from_config: TncDictionaryProvider;
var
    base_path: string;
    user_path: string;
begin
    base_path := get_active_dictionary_path;
    user_path := get_configured_user_dictionary_path;
    if base_path <> '' then
    begin
        Result := take_cached_dictionary_provider(m_config.dictionary_variant, base_path, user_path);
        if Result <> nil then
        begin
            Result.set_debug_mode(m_config.debug_mode);
            Exit;
        end;
    end;
    Result := create_dictionary_from_paths(base_path, user_path);
end;

function TncEngine.create_dictionary_from_paths(const base_path,
    user_path: string): TncDictionaryProvider;
var
    sqlite_dict: TncSqliteDictionary;
begin
    if (base_path <> '') or (user_path <> '') then
    begin
        sqlite_dict := TncSqliteDictionary.create(base_path, user_path);
        sqlite_dict.set_user_dictionary_variant(m_config.dictionary_variant);
        if (m_defer_optional_dictionary_models and
            sqlite_dict.open_deferred) or
            ((not m_defer_optional_dictionary_models) and sqlite_dict.open) then
        begin
            sqlite_dict.set_debug_mode(m_config.debug_mode);
            Result := sqlite_dict;
            Exit;
        end;

        sqlite_dict.Free;
    end;

    Result := TncInMemoryDictionary.create;
end;

function TncEngine.get_dictionary_path_for_variant(
    const variant: TncDictionaryVariant): string;
begin
    if variant = dv_traditional then
    begin
        Result := m_configured_dictionary_path_traditional;
        if (not m_dictionary_paths_configured) and (Result = '') then
            Result := get_default_dictionary_path_traditional;
    end
    else
    begin
        Result := m_configured_dictionary_path_simplified;
        if (not m_dictionary_paths_configured) and (Result = '') then
            Result := get_default_dictionary_path_simplified;
    end;
end;

function TncEngine.get_configured_user_dictionary_path: string;
begin
    Result := m_configured_user_dictionary_path;
    if (not m_dictionary_paths_configured) and (Result = '') then
        Result := get_default_user_dictionary_path;
end;

function TncEngine.get_active_dictionary_path: string;
begin
    Result := get_dictionary_path_for_variant(m_config.dictionary_variant);
end;

function TncEngine.get_dictionary_write_time(const path: string): TDateTime;
begin
    if (path <> '') and FileExists(path) then
    begin
        Result := TncFile.GetLastWriteTime(path);
    end
    else
    begin
        Result := 0;
    end;
end;

procedure TncEngine.update_dictionary_state;
var
    sqlite_dict: TncSqliteDictionary;
begin
    if m_dictionary is TncSqliteDictionary then
    begin
        sqlite_dict := TncSqliteDictionary(m_dictionary);
        m_dictionary_path := sqlite_dict.db_path;
        m_dictionary_write_time := get_dictionary_write_time(m_dictionary_path);
        m_user_dictionary_path := sqlite_dict.user_db_path;
        m_user_dictionary_write_time := get_dictionary_write_time(m_user_dictionary_path);
    end
    else
    begin
        m_dictionary_path := '';
        m_dictionary_write_time := 0;
        m_user_dictionary_path := '';
        m_user_dictionary_write_time := 0;
    end;
end;

procedure TncEngine.reload_dictionary_if_needed;
const
    c_dictionary_reload_check_interval_ms = 1500;
var
    current_write_time: TDateTime;
    user_write_time: TDateTime;
    now_tick: UInt64;
begin
    if (m_dictionary_path = '') and (m_user_dictionary_path = '') then
    begin
        Exit;
    end;

    now_tick := nc_monotonic_tick_ms;
    if (m_last_dictionary_reload_check_tick <> 0) and
        ((now_tick - m_last_dictionary_reload_check_tick) < c_dictionary_reload_check_interval_ms) then
    begin
        Exit;
    end;
    m_last_dictionary_reload_check_tick := now_tick;

    if m_dictionary_path <> '' then
    begin
        current_write_time := get_dictionary_write_time(m_dictionary_path);
    end
    else
    begin
        current_write_time := 0;
    end;

    if m_user_dictionary_path <> '' then
    begin
        user_write_time := get_dictionary_write_time(m_user_dictionary_path);
    end
    else
    begin
        user_write_time := 0;
    end;

    if current_write_time <> m_dictionary_write_time then
    begin
        set_dictionary_provider(create_dictionary_from_paths(m_dictionary_path,
            m_user_dictionary_path));
        Exit;
    end;

    if (user_write_time <> m_user_dictionary_write_time) and
        (m_dictionary is TncSqliteDictionary) and
        TncSqliteDictionary(m_dictionary).reload_user_dictionary then
    begin
        invalidate_dictionary_lookup_caches;
        m_user_dictionary_write_time :=
            get_dictionary_write_time(m_user_dictionary_path);
    end;
end;

procedure TncEngine.prewarm_dictionary_caches;
var
    sqlite_dict: TncSqliteDictionary;
    alt_variant: TncDictionaryVariant;
    alt_base_path: string;
    alt_dict: TncSqliteDictionary;
begin
    if not (m_dictionary is TncSqliteDictionary) then
    begin
        Exit;
    end;

    sqlite_dict := TncSqliteDictionary(m_dictionary);
    sqlite_dict.prewarm_short_lookup_caches;

    if m_config.dictionary_variant = dv_traditional then
    begin
        alt_variant := dv_simplified;
        alt_base_path := get_dictionary_path_for_variant(dv_simplified);
        if (m_cached_dictionary_simplified <> nil) or
            (alt_base_path = '') or (not FileExists(alt_base_path)) then
        begin
            Exit;
        end;
    end
    else
    begin
        alt_variant := dv_traditional;
        alt_base_path := get_dictionary_path_for_variant(dv_traditional);
        if (m_cached_dictionary_traditional <> nil) or
            (alt_base_path = '') or (not FileExists(alt_base_path)) then
        begin
            Exit;
        end;
    end;

    // The prewarmed alternate variant shares the same user dictionary. Do not
    // prune while opening it, because simplified-only rows such as ci -> U+8BCD
    // look invalid against the traditional base dictionary, and vice versa.
    alt_dict := TncSqliteDictionary.Create(alt_base_path,
        get_configured_user_dictionary_path, False);
    alt_dict.set_user_dictionary_variant(alt_variant);
    if alt_dict.open then
    begin
        alt_dict.set_debug_mode(m_config.debug_mode);
        alt_dict.prewarm_short_lookup_caches;
        if alt_variant = dv_traditional then
        begin
            free_dictionary_provider(m_cached_dictionary_traditional);
            m_cached_dictionary_traditional := alt_dict;
        end
        else
        begin
            free_dictionary_provider(m_cached_dictionary_simplified);
            m_cached_dictionary_simplified := alt_dict;
        end;
    end
    else
    begin
        alt_dict.Free;
    end;
end;

function trim_left_context_to_sentence(const value: string;
    const max_length: Integer): string;
var
    idx: Integer;
    start_idx: Integer;
begin
    Result := Trim(value);
    start_idx := 1;
    for idx := Length(Result) downto 1 do
    begin
        case Ord(Result[idx]) of
            $000A, $000D, $0021, $003B, $003F, $3002, $FF01, $FF1B,
            $FF1F:
                begin
                    start_idx := idx + 1;
                    Break;
                end;
        end;
    end;
    if start_idx > 1 then
    begin
        Result := Trim(Copy(Result, start_idx, MaxInt));
    end;
    if (max_length > 0) and (Length(Result) > max_length) then
    begin
        Result := Copy(Result, Length(Result) - max_length + 1, max_length);
    end;
end;

function context_model_tail(const value: string): string;
begin
    Result := trim_left_context_to_sentence(value, c_context_model_max_len);
end;

procedure TncEngine.set_external_left_context(const left_context: string;
    const document_key: string; const document_snapshot: string);
var
    next_context: string;
    next_document_snapshot: string;
begin
    if m_document_context_model <> nil then
    begin
        next_document_snapshot := document_snapshot;
        if (document_key <> '') and (next_document_snapshot = '') then
        begin
            next_document_snapshot := left_context;
        end;
        m_document_context_model.set_snapshot(document_key,
            next_document_snapshot);
        if m_long_local_repair <> nil then
        begin
            m_long_local_repair.set_document_context(
                m_document_context_model.document_key,
                m_document_context_model.semantic_tail);
        end;
    end;
    next_context := trim_left_context_to_sentence(left_context,
        c_left_context_max_len);
    if m_external_left_context <> next_context then
    begin
        m_external_left_context := next_context;
        if m_lookup_context_query_bonus_cache <> nil then
        begin
            m_lookup_context_query_bonus_cache.Clear;
        end;
        if m_lookup_context_query_latest_bonus_cache <> nil then
        begin
            m_lookup_context_query_latest_bonus_cache.Clear;
        end;
    end;
    if m_lookup_context_bonus_cache <> nil then
    begin
        m_lookup_context_bonus_cache.Clear;
    end;
    if m_lookup_segment_path_context_bonus_cache <> nil then
    begin
        m_lookup_segment_path_context_bonus_cache.Clear;
    end;
end;

function is_relaxed_no_initial_syllable_text(const token_text: string): Boolean;
begin
    Result :=
        SameText(token_text, 'ang') or
        SameText(token_text, 'eng') or
        SameText(token_text, 'ai') or
        SameText(token_text, 'an') or
        SameText(token_text, 'ao') or
        SameText(token_text, 'ei') or
        SameText(token_text, 'en') or
        SameText(token_text, 'er') or
        SameText(token_text, 'ou') or
        SameText(token_text, 'a') or
        SameText(token_text, 'e') or
        SameText(token_text, 'o');
end;

function try_split_relaxed_missing_apostrophe_token(const parser: TncPinyinParser; const token_text: string;
    out out_left: string; out out_right: string): Boolean;
var
    split_idx: Integer;
    left_text: string;
    right_text: string;
    left_parts: TncPinyinParseResult;
    right_parts: TncPinyinParseResult;
begin
    Result := False;
    out_left := '';
    out_right := '';
    if (parser = nil) or (token_text = '') or (Length(token_text) < 3) or
        (Pos('''', token_text) > 0) then
    begin
        Exit;
    end;

    for split_idx := 2 to Length(token_text) - 1 do
    begin
        left_text := Copy(token_text, 1, split_idx);
        right_text := Copy(token_text, split_idx + 1, MaxInt);
        if not is_relaxed_no_initial_syllable_text(right_text) then
        begin
            Continue;
        end;

        left_parts := parser.parse(left_text);
        right_parts := parser.parse(right_text);
        if (Length(right_text) = 1) and (Length(left_text) > 3) and
            (Length(left_parts) < 2) then
        begin
            Continue;
        end;
        if (Length(left_parts) = 1) and (Length(right_parts) = 1) and
            SameText(left_parts[0].text, left_text) and
            SameText(right_parts[0].text, right_text) then
        begin
            out_left := left_text;
            out_right := right_text;
            Result := True;
            Exit;
        end;
    end;
end;

function detect_relaxed_missing_apostrophe_boundary_shift(const input_text: string): Boolean;
var
    parser: TncPinyinParser;
    primary_parts: TncPinyinParseResult;
    part_idx: Integer;
    left_text: string;
    right_text: string;
    combined_text: string;
begin
    Result := False;
    if (input_text = '') or (Pos('''', input_text) > 0) then
    begin
        Exit;
    end;

    parser := TncPinyinParser.Create;
    try
        primary_parts := parser.parse(input_text);
        if Length(primary_parts) < 2 then
        begin
            Exit;
        end;

        for part_idx := 0 to High(primary_parts) - 1 do
        begin
            combined_text := primary_parts[part_idx].text + primary_parts[part_idx + 1].text;
            if try_split_relaxed_missing_apostrophe_token(parser, combined_text, left_text, right_text) and
                (Length(left_text) > Length(primary_parts[part_idx].text)) then
            begin
                Exit(True);
            end;
        end;
    finally
        parser.Free;
    end;
end;

function parse_pinyin_with_relaxed_missing_apostrophe(const input_text: string): TncPinyinParseResult;
var
    parser: TncPinyinParser;
    primary_parts: TncPinyinParseResult;
    expanded_parts: TncPinyinParseResult;
    part_idx: Integer;
    out_idx: Integer;
    left_text: string;
    right_text: string;
    combined_text: string;
    changed: Boolean;

    procedure append_part(const text: string; const start_index: Integer);
    begin
        if text = '' then
        begin
            Exit;
        end;
        SetLength(expanded_parts, out_idx + 1);
        expanded_parts[out_idx].text := text;
        expanded_parts[out_idx].start_index := start_index;
        expanded_parts[out_idx].length := Length(text);
        Inc(out_idx);
    end;
begin
    parser := TncPinyinParser.Create;
    try
        primary_parts := parser.parse(input_text);
        if (Length(primary_parts) = 0) or (Pos('''', input_text) > 0) then
        begin
            Result := primary_parts;
            Exit;
        end;

        SetLength(expanded_parts, 0);
        out_idx := 0;
        changed := False;
        part_idx := 0;
        while part_idx <= High(primary_parts) do
        begin
            if try_split_relaxed_missing_apostrophe_token(parser, primary_parts[part_idx].text, left_text, right_text) then
            begin
                append_part(left_text, primary_parts[part_idx].start_index);
                append_part(right_text, primary_parts[part_idx].start_index + Length(left_text));
                changed := True;
                Inc(part_idx);
                Continue;
            end;

            if part_idx < High(primary_parts) then
            begin
                combined_text := primary_parts[part_idx].text + primary_parts[part_idx + 1].text;
                if try_split_relaxed_missing_apostrophe_token(parser, combined_text, left_text, right_text) and
                    (Length(left_text) > Length(primary_parts[part_idx].text)) then
                begin
                    append_part(left_text, primary_parts[part_idx].start_index);
                    append_part(right_text, primary_parts[part_idx].start_index + Length(left_text));
                    changed := True;
                    Inc(part_idx, 2);
                    Continue;
                end;
            end;

            append_part(primary_parts[part_idx].text, primary_parts[part_idx].start_index);
            Inc(part_idx);
        end;

        if changed or (Length(expanded_parts) > Length(primary_parts)) then
        begin
            Result := expanded_parts;
        end
        else
        begin
            Result := primary_parts;
        end;
    finally
        parser.Free;
    end;
end;

procedure TncEngine.clear_one_key_completion;
begin
    m_repaired_completion_query_key := '';
    m_tab_projection_shown := Default(TncOneKeyCompletion);
    m_tab_projection_query := ''; m_tab_projection_document := '';
    m_tab_projection_raw_text := ''; m_tab_projection_raw_path := '';
    m_one_key_completion := Default(TncOneKeyCompletion);
    m_debug_lexical_one_key_completion := Default(TncOneKeyCompletion);
    SetLength(m_debug_long_one_key_completion_pool, 0);
    m_one_key_completion_query_prefix := '';
    m_one_key_completion_score := Low(Integer);
    m_long_neural_completion_request :=
        Default(TncLongNeuralCompletionRequest);
    m_has_long_neural_completion_request := False;
    m_long_neural_completion_prefix_locked := False;
end;

procedure TncEngine.clear_one_key_completion_feedback_target;
begin
    m_completion_feedback_origin_prefix := '';
end;

procedure TncEngine.record_one_key_completion_feedback_target(
    const full_pinyin, text: string);
var
    target_pinyin: string;
    target_text: string;
begin
    target_pinyin := normalize_pinyin_text(full_pinyin);
    target_text := Trim(text);
    if (m_dictionary <> nil) and
        (m_completion_feedback_origin_prefix <> '') and
        (target_pinyin <> '') and (target_text <> '') and
        (Length(target_pinyin) >
        Length(m_completion_feedback_origin_prefix)) and
        target_pinyin.StartsWith(m_completion_feedback_origin_prefix, True) then
    begin
        m_dictionary.record_one_key_completion_accept(
            m_completion_feedback_origin_prefix, target_pinyin, target_text);
    end;
    clear_one_key_completion_feedback_target;
end;

function TncEngine.try_refresh_document_copy_completion(
    const compact_query: string;
    const previous_completion: TncOneKeyCompletion;
    out completion: TncOneKeyCompletion;
    out completion_score: Integer): Boolean;
const
    c_raw_candidate_limit = 24;
    c_max_exact_segments = 3;
    c_max_anchor_units = 8;
    c_max_suffix_units = 8;
    c_min_evidence = 700;
    c_min_winner_margin = 88;
    c_top_candidate_bonus = 96;
    c_second_candidate_penalty = 32;
    c_long_anchor_bonus = 112;
    c_repeat_bonus = 72;
    c_hysteresis_bonus = 192;
type
    TncDocumentScoredCompletion = record
        value: TncOneKeyCompletion;
        score: Integer;
        context_width: Integer;
        occurrence_count: Integer;
    end;
var
    scored: TList<TncDocumentScoredCompletion>;
    by_text: TDictionary<string, Integer>;
    raw_values: TncDocumentContinuationList;
    resolved: TncExactTextPath;
    anchor_resolved: TncExactTextPath;
    item: TncDocumentScoredCompletion;
    existing: TncDocumentScoredCompletion;
    candidate: TncCandidate;
    base_text: string;
    base_path: string;
    anchor_full_pinyin: string;
    full_text: string;
    feedback_score: Integer;
    path_score: Integer;
    path_segments: Integer;
    existing_idx: Integer;
    best_idx: Integer;
    second_idx: Integer;
    idx: Integer;

    procedure collect_for_candidate(const rank: Integer);
    var
        raw_idx: Integer;
    begin
        if (rank < 0) or (rank > High(m_candidates)) then
        begin
            Exit;
        end;
        candidate := m_candidates[rank];
        base_text := Trim(candidate.text);
        if (base_text = '') or (Trim(candidate.comment) <> '') or
            (get_candidate_text_unit_count(base_text) < 2) or
            (not m_document_context_model.lookup_continuations(base_text,
            c_raw_candidate_limit, raw_values)) then
        begin
            Exit;
        end;
        base_path := get_segment_path_for_candidate(candidate, rank);
        if base_path = '' then
        begin
            base_path := infer_segment_path_for_query_text_with_score(
                m_composition_text, base_text, path_score, path_segments,
                True);
        end;
        if base_path = '' then
        begin
            base_path := base_text;
        end;
        anchor_full_pinyin := compact_query;
        anchor_resolved := Default(TncExactTextPath);
        if (get_candidate_text_unit_count(base_text) <=
            c_max_anchor_units) and
            m_dictionary.resolve_exact_text_prefix(base_text,
            c_max_exact_segments, c_max_anchor_units, anchor_resolved) and
            SameText(anchor_resolved.text, base_text) and
            normalize_pinyin_text(anchor_resolved.full_pinyin).StartsWith(
            compact_query, True) then
        begin
            // The typed query can end in a partial syllable. Keep the full
            // anchor spelling so continuing that syllable is not learned as
            // a rejection of the document-local completion.
            anchor_full_pinyin := normalize_pinyin_text(
                anchor_resolved.full_pinyin);
        end;

        for raw_idx := 0 to High(raw_values) do
        begin
            if (raw_values[raw_idx].context_width < 3) and
                (raw_values[raw_idx].occurrence_count < 2) then
            begin
                Continue;
            end;
            if not m_dictionary.resolve_exact_text_prefix(
                raw_values[raw_idx].suffix_text,
                c_max_exact_segments, c_max_suffix_units, resolved) then
            begin
                Continue;
            end;
            feedback_score :=
                m_document_context_model.completion_feedback_score(
                raw_values[raw_idx].anchor_text, resolved.text);
            // A long continuation copied from a single occurrence needs a
            // correspondingly strong anchor. This prevents a short shared
            // tail from attaching an unrelated multi-word continuation.
            if (resolved.unit_count > 4) and
                (raw_values[raw_idx].context_width < 6) and
                (raw_values[raw_idx].occurrence_count < 2) and
                (feedback_score <= 0) then
            begin
                Continue;
            end;
            full_text := base_text + resolved.text;
            if (resolved.unit_count < 2) or (full_text = base_text) then
            begin
                Continue;
            end;

            item := Default(TncDocumentScoredCompletion);
            item.context_width := raw_values[raw_idx].context_width;
            item.occurrence_count := raw_values[raw_idx].occurrence_count;
            item.value.text := full_text;
            item.value.full_pinyin := anchor_full_pinyin +
                resolved.full_pinyin;
            item.value.path_text := base_path + #3 + resolved.path_text;
            item.value.weight := resolved.weight;
            item.value.document_score := raw_values[raw_idx].evidence;
            item.value.source_count := raw_values[raw_idx].occurrence_count;
            item.value.path_score := resolved.unit_count * 64;
            item.value.prefix_anchored := True;
            item.value.anchor_text := base_text;
            item.value.suffix_text := resolved.text;
            // For document-copy completions anchor_path is the exact
            // character suffix used by the in-memory index. It is not written
            // to the persistent long-completion feedback table.
            item.value.anchor_path := raw_values[raw_idx].anchor_text;
            item.value.source := okcs_document_copy;
            item.score := raw_values[raw_idx].evidence +
                resolved.unit_count * 44 +
                Max(-120, Min(240, resolved.weight div 6)) +
                feedback_score;
            if rank = 0 then
            begin
                Inc(item.score, c_top_candidate_bonus);
            end
            else
            begin
                Dec(item.score, c_second_candidate_penalty);
            end;
            if item.context_width >= 6 then
            begin
                Inc(item.score, c_long_anchor_bonus);
            end;
            if item.occurrence_count >= 2 then
            begin
                Inc(item.score, Min(4, item.occurrence_count - 1) *
                    c_repeat_bonus);
            end;
            if (previous_completion.source = okcs_document_copy) and
                SameText(previous_completion.text, item.value.text) and
                SameText(previous_completion.full_pinyin,
                item.value.full_pinyin) then
            begin
                Inc(item.score, c_hysteresis_bonus);
            end;

            if by_text.TryGetValue(item.value.text, existing_idx) then
            begin
                existing := scored[existing_idx];
                if item.score > existing.score then
                begin
                    scored[existing_idx] := item;
                end
                else if SameText(existing.value.suffix_text,
                    item.value.suffix_text) then
                begin
                    Inc(existing.score, 56);
                    existing.occurrence_count := Max(
                        existing.occurrence_count, item.occurrence_count);
                    scored[existing_idx] := existing;
                end;
                Continue;
            end;
            by_text.Add(item.value.text, scored.Count);
            scored.Add(item);
        end;
    end;

begin
    completion := Default(TncOneKeyCompletion);
    completion_score := Low(Integer);
    Result := False;
    if (m_document_context_model = nil) or
        (not m_document_context_model.has_context) or
        (m_dictionary = nil) or (compact_query = '') or
        (Length(m_candidates) <= 0) then
    begin
        Exit;
    end;

    scored := TList<TncDocumentScoredCompletion>.Create;
    by_text := TDictionary<string, Integer>.Create;
    try
        collect_for_candidate(0);
        if Length(m_candidates) > 1 then
        begin
            collect_for_candidate(1);
        end;
        best_idx := -1;
        second_idx := -1;
        for idx := 0 to scored.Count - 1 do
        begin
            if (best_idx < 0) or (scored[idx].score > scored[best_idx].score) then
            begin
                second_idx := best_idx;
                best_idx := idx;
            end
            else if (second_idx < 0) or
                (scored[idx].score > scored[second_idx].score) then
            begin
                second_idx := idx;
            end;
        end;
        if best_idx < 0 then
        begin
            Exit;
        end;
        item := scored[best_idx];
        if item.score < c_min_evidence then
        begin
            Exit;
        end;
        if (item.context_width < 4) and (item.occurrence_count < 2) and
            (m_document_context_model.completion_feedback_score(
            item.value.anchor_path, item.value.suffix_text) <= 0) then
        begin
            Exit;
        end;
        if (second_idx >= 0) and
            (item.score - scored[second_idx].score < c_min_winner_margin) and
            (not ((previous_completion.source = okcs_document_copy) and
            SameText(previous_completion.text, item.value.text))) then
        begin
            Exit;
        end;
        completion := item.value;
        completion_score := item.score;
        Result := True;
    finally
        by_text.Free;
        scored.Free;
    end;
end;

function TncEngine.has_validated_completion_prefix: Boolean;
begin
    Result := ((GetEnvironmentVariable('CASSOTIS_TAB_REPAIRED_PREFIX') = '1')
        ) and has_current_validated_completion_prefix;
    if Result and (GetEnvironmentVariable('CASSOTIS_TAB_REPAIR_TRUST') = '1') and
        (m_local_repair_validated.lm_gain < 80) then Result := False;
end;

function TncEngine.has_current_validated_completion_prefix: Boolean;
var key: string;
begin
    Result := False;
    if (not m_allow_one_key_completion_lookup) or (m_dictionary = nil) or
        (m_config.input_mode <> im_chinese) or m_has_pending_commit or
        (m_long_local_repair = nil) or (m_page_index <> 0) or
        (m_config.pinyin_input_scheme <> pis_full_pinyin) or
        (m_config.dictionary_variant <> dv_simplified) or
        m_config.fuzzy_pinyin_enabled or (m_confirmed_text <> '') or
        not m_visible_candidates_cache_valid or
        (m_visible_candidates_cache_composition_text <> m_composition_text) or
        (m_visible_candidates_cache_lookup_key <> m_last_lookup_key) or
        (m_visible_candidates_cache_page_index <> 0) or
        (Length(m_visible_candidates_cache) = 0) or
        (Length(m_visible_candidates_cache) <> Length(m_visible_candidate_source_indices_cache)) or
        (Length(m_local_repair_text) < 6) or (Length(m_local_repair_text) > 40) or
        (m_local_repair_text = m_local_repair_draft) or
        (m_visible_candidates_cache[0].text <> m_local_repair_text) or
        (m_visible_candidates_cache[0].comment <> '') or
        (m_visible_candidates_cache[0].source = cs_user) or
        not m_local_repair_validated.exact_path or
        (m_local_repair_validated.text <> m_local_repair_text) or
        (m_local_repair_validated.segment_path = '') then Exit;
    key := m_composition_text + #0 + m_last_lookup_key + #0;
    if m_document_context_model <> nil then
        key := key + m_document_context_model.document_key + #0 +
            m_document_context_model.semantic_tail
    else key := key + #0;
    Result := key = m_local_repair_query_key;
end;

function TncEngine.project_validated_prefix_completion(
    const completion: TncOneKeyCompletion): TncOneKeyCompletion;
var
    syllables: TncPinyinParseResult;
    aligned_query, query: string;
    i: Integer;
begin
    Result := completion;
    if not (completion.source in [okcs_long_transition, okcs_long_neural]) or
        (completion.text = '') or
        (GetEnvironmentVariable('CASSOTIS_TAB_FINAL_REPAIR') = '0') or
        (completion.anchor_text <> m_local_repair_draft) or
        not has_current_validated_completion_prefix then Exit;
    syllables := get_effective_compact_pinyin_syllables(m_composition_text, False);
    aligned_query := ''; query := '';
    for i := 0 to High(syllables) do
    begin
        if i > 0 then aligned_query := aligned_query + #3;
        aligned_query := aligned_query + normalize_pinyin_text(syllables[i].text);
        query := query + normalize_pinyin_text(syllables[i].text);
    end;
    if query <> normalize_pinyin_text(m_composition_text) then Exit;
    nc_project_repaired_tab_prefix(m_local_repair_draft, query, aligned_query, m_local_repair_validated,
        completion, Result);
end;


procedure TncEngine.refresh_validated_prefix_completion;
var
    previous, completion: TncOneKeyCompletion;
    syllables: TncPinyinParseResult;
    query_key: string;
    score: Integer;
begin
    if not has_validated_completion_prefix then Exit;
    // Lexical/user completion is a separate priority lane. A repaired sentence
    // only replaces the old long-continuation request, never lexical ranking.
    if m_one_key_completion.source in [okcs_user_exact, okcs_base_exact, okcs_transition] then Exit;
    previous := m_one_key_completion;
    query_key := normalize_pinyin_text(m_composition_text);
    syllables := get_effective_compact_pinyin_syllables(m_composition_text, False);
    clear_one_key_completion;
    if (previous.source = okcs_document_copy) and
        previous.text.StartsWith(m_local_repair_text) and
        previous.full_pinyin.StartsWith(query_key) then
    begin
        m_one_key_completion := previous;
        m_one_key_completion_query_prefix := query_key;
        Exit;
    end;
    if try_refresh_long_one_key_completion(syllables, query_key, previous, completion, score) then
    begin
        m_one_key_completion := completion;
        m_one_key_completion_query_prefix := query_key;
        m_one_key_completion_score := score;
    end;
end;

function TncEngine.try_refresh_long_one_key_completion(
    const syllables: TncPinyinParseResult;
    const compact_query: string;
    const previous_completion: TncOneKeyCompletion;
    out completion: TncOneKeyCompletion;
    out completion_score: Integer): Boolean;
const
    c_min_long_syllables = 4;
    c_max_visible_scan = 12;
    c_max_anchor_segments = 3;
    c_min_evidence = 560;
    c_min_source_count = 5;
    c_context_length = 12;
    c_char_lm_divisor = 4;
    c_context_contradiction_tolerance = 96;
    c_feedback_accept_step = 96;
    c_feedback_reject_step = 88;
    c_hysteresis_bonus = 72;
    c_min_winner_margin = 18;
type
    TncLongCompletionPathAnalysis = record
        valid: Boolean;
        segments: TArray<string>;
        exact_segments: TArray<Boolean>;
    end;
    TncLongCompletionAnchor = record
        path_text: string;
        text: string;
        segment_count: Integer;
    end;
    TncLongCompletionScoredItem = record
        value: TncLongOneKeyCompletion;
        anchor: TncLongCompletionAnchor;
        score: Integer;
    end;
var
    completion_candidates: TncCandidateList;
    completion_sources: TArray<Integer>;
    corrected_prefix: Boolean;
    corrected_path: string;
    top_index: Integer;
    second_index: Integer;
    top_path: string;
    second_path: string;
    top1_anchor_path: string;
    top2_anchor_path: string;
    query_syllable_text: string;
    top_analysis: TncLongCompletionPathAnalysis;
    second_analysis: TncLongCompletionPathAnalysis;
    anchors: TArray<TncLongCompletionAnchor>;
    anchor: TncLongCompletionAnchor;
    anchor_results: TncLongOneKeyCompletionList;
    items: TArray<TncLongCompletionScoredItem>;
    item: TncLongCompletionScoredItem;
    completion_texts: TArray<string>;
    char_lm_scores: TArray<Integer>;
    anchor_lm_texts: TArray<string>;
    anchor_lm_scores: TArray<Integer>;
    document_context_bonus: Integer;
    context_value: string;
    top1_uncontested: Boolean;
    request_phonetic_only: Boolean;
    context_lm_ok: Boolean;
    anchor_segment_count: Integer;
    anchor_idx: Integer;
    result_idx: Integer;
    item_idx: Integer;
    existing_idx: Integer;
    best_idx: Integer;
    second_best_idx: Integer;
    best_score: Integer;
    second_best_score: Integer;
    suffix_compact_pinyin: string;

    function completion_path(const candidate_idx: Integer): string;
    var source_idx: Integer;
    begin
        if corrected_prefix and (candidate_idx = 0) and (corrected_path <> '') then
            Exit(corrected_path);
        source_idx := candidate_idx;
        if candidate_idx < Length(completion_sources) then
            source_idx := completion_sources[candidate_idx];
        Result := get_segment_path_for_candidate(completion_candidates[candidate_idx], source_idx);
    end;

    procedure adopt_corrected_prefix;
    var
        visible: TncCandidateList;
        parts: TArray<string>;
        unit_idx: Integer;
    begin
        // Tab consumes the settled visible result; it must never trigger an
        // earlier candidate-ranking pass while build_candidates is running.
        if not has_validated_completion_prefix then Exit;
        visible := m_visible_candidates_cache;
        if Length(visible[0].text) <> Length(syllables) then Exit;
        parts := m_local_repair_validated.aligned_pinyin.Split([#3], TStringSplitOptions.ExcludeEmpty);
        if Length(parts) <> Length(syllables) then Exit;
        for unit_idx := 0 to High(parts) do
            if parts[unit_idx] <> normalize_pinyin_text(syllables[unit_idx].text) then Exit;
        // Text, alignment and resegmented exact path are produced by the same
        // guard invocation. Never splice corrected text into old word lengths.
        corrected_path := m_local_repair_validated.segment_path;
        completion_candidates := visible;
        completion_sources := Copy(m_visible_candidate_source_indices_cache);
        corrected_prefix := True;
    end;

    function build_query_syllable_text: string;
    var
        syllable_idx: Integer;
        syllable_text: string;
    begin
        Result := '';
        for syllable_idx := 0 to High(syllables) do
        begin
            syllable_text := normalize_pinyin_text(
                syllables[syllable_idx].text);
            if syllable_text = '' then
            begin
                Result := '';
                Exit;
            end;
            if Result <> '' then
            begin
                Result := Result + '''';
            end;
            Result := Result + syllable_text;
        end;
    end;

    function is_complete_candidate(const candidate_idx: Integer): Boolean;
    begin
        Result := (candidate_idx >= 0) and
            (candidate_idx < Length(completion_candidates)) and
            (Trim(completion_candidates[candidate_idx].text) <> '') and
            (Trim(completion_candidates[candidate_idx].comment) = '') and
            (get_candidate_text_unit_count(
            Trim(completion_candidates[candidate_idx].text)) = Length(syllables));
    end;

    function query_key_for_span(const start_idx, unit_count: Integer): string;
    var
        syllable_idx: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (unit_count <= 0) or
            (start_idx + unit_count > Length(syllables)) then
        begin
            Exit;
        end;
        for syllable_idx := start_idx to start_idx + unit_count - 1 do
        begin
            Result := Result + syllables[syllable_idx].text;
        end;
        Result := normalize_pinyin_text(Result);
    end;

    function analyze_candidate_path(const candidate_idx: Integer;
        out analysis: TncLongCompletionPathAnalysis): Boolean;
    var
        encoded_path: string;
        plain_text: string;
        segment_text: string;
        segment_query: string;
        segment_units: Integer;
        segment_idx: Integer;
        syllable_pos: Integer;
    begin
        analysis := Default(TncLongCompletionPathAnalysis);
        Result := False;
        if not is_complete_candidate(candidate_idx) then
        begin
            Exit;
        end;
        encoded_path := Trim(completion_path(candidate_idx));
        if encoded_path = '' then
        begin
            Exit;
        end;
        analysis.segments := encoded_path.Split([#3],
            TStringSplitOptions.ExcludeEmpty);
        if Length(analysis.segments) = 0 then
        begin
            Exit;
        end;
        SetLength(analysis.exact_segments, Length(analysis.segments));
        plain_text := '';
        syllable_pos := 0;
        for segment_idx := 0 to High(analysis.segments) do
        begin
            segment_text := Trim(analysis.segments[segment_idx]);
            segment_units := get_candidate_text_unit_count(segment_text);
            if (segment_text = '') or (segment_units <= 0) or
                (syllable_pos + segment_units > Length(syllables)) then
            begin
                Exit;
            end;
            segment_query := query_key_for_span(syllable_pos, segment_units);
            analysis.exact_segments[segment_idx] :=
                (segment_query <> '') and
                m_dictionary.is_base_entry(segment_query, segment_text);
            plain_text := plain_text + segment_text;
            Inc(syllable_pos, segment_units);
        end;
        analysis.valid := (syllable_pos = Length(syllables)) and
            SameText(plain_text, Trim(completion_candidates[candidate_idx].text));
        Result := analysis.valid;
    end;

    function exact_suffix_path(const analysis: TncLongCompletionPathAnalysis;
        const segment_count: Integer; out path_text: string): Boolean;
    var
        segment_idx: Integer;
        start_idx: Integer;
    begin
        path_text := '';
        Result := analysis.valid and (segment_count >= 1) and
            (segment_count <= Length(analysis.segments)) and
            (Length(analysis.exact_segments) = Length(analysis.segments));
        if not Result then
        begin
            Exit;
        end;
        start_idx := Length(analysis.segments) - segment_count;
        for segment_idx := start_idx to High(analysis.segments) do
        begin
            if not analysis.exact_segments[segment_idx] then
            begin
                Result := False;
                Exit;
            end;
            if path_text <> '' then
            begin
                path_text := path_text + #3;
            end;
            path_text := path_text + analysis.segments[segment_idx];
        end;
    end;

    function exact_anchor_path_matches_query_tail(
        const path_text: string): Boolean;
    var
        path_segments: TArray<string>;
        segment_text: string;
        segment_units: Integer;
        total_units: Integer;
        syllable_pos: Integer;
        segment_idx: Integer;
        segment_query: string;
    begin
        Result := False;
        path_segments := path_text.Split([#3],
            TStringSplitOptions.ExcludeEmpty);
        if Length(path_segments) = 0 then
        begin
            Exit;
        end;
        total_units := 0;
        for segment_text in path_segments do
        begin
            segment_units := get_candidate_text_unit_count(
                Trim(segment_text));
            if segment_units <= 0 then
            begin
                Exit;
            end;
            Inc(total_units, segment_units);
        end;
        syllable_pos := Length(syllables) - total_units;
        if syllable_pos < 0 then
        begin
            Exit;
        end;
        for segment_idx := 0 to High(path_segments) do
        begin
            segment_text := Trim(path_segments[segment_idx]);
            segment_units := get_candidate_text_unit_count(segment_text);
            segment_query := query_key_for_span(syllable_pos,
                segment_units);
            if (segment_query = '') or
                (not m_dictionary.is_base_entry(segment_query,
                segment_text)) then
            begin
                Exit;
            end;
            Inc(syllable_pos, segment_units);
        end;
        Result := syllable_pos = Length(syllables);
    end;

    function analysis_ends_with_path(
        const analysis: TncLongCompletionPathAnalysis;
        const path_text: string): Boolean;
    var
        expected_segments: TArray<string>;
        expected_idx: Integer;
        actual_idx: Integer;
    begin
        Result := False;
        if (not analysis.valid) or (path_text = '') then
        begin
            Exit;
        end;
        expected_segments := path_text.Split([#3],
            TStringSplitOptions.ExcludeEmpty);
        if (Length(expected_segments) = 0) or
            (Length(expected_segments) > Length(analysis.segments)) then
        begin
            Exit;
        end;
        actual_idx := Length(analysis.segments) - Length(expected_segments);
        for expected_idx := 0 to High(expected_segments) do
        begin
            if (not analysis.exact_segments[actual_idx + expected_idx]) or
                (not SameText(expected_segments[expected_idx],
                analysis.segments[actual_idx + expected_idx])) then
            begin
                Exit;
            end;
        end;
        Result := True;
    end;

    function shared_anchor_has_stable_prefix(
        const path_text: string): Boolean;
    var
        top_units: TArray<string>;
        second_units: TArray<string>;
        anchor_units: TArray<string>;
        anchor_text: string;
        top_prefix_count: Integer;
        second_prefix_count: Integer;
        common_prefix_count: Integer;
        required_prefix_count: Integer;
    begin
        Result := False;
        if (second_index < 0) or
            (not analysis_ends_with_path(second_analysis, path_text)) then
        begin
            Exit;
        end;

        anchor_text := StringReplace(path_text, #3, '', [rfReplaceAll]);
        top_units := split_text_units(Trim(completion_candidates[top_index].text));
        second_units := split_text_units(
            Trim(completion_candidates[second_index].text));
        anchor_units := split_text_units(anchor_text);
        if (Length(anchor_units) = 0) or
            (Length(anchor_units) > Length(top_units)) or
            (Length(anchor_units) > Length(second_units)) then
        begin
            Exit;
        end;

        top_prefix_count := Length(top_units) - Length(anchor_units);
        second_prefix_count := Length(second_units) - Length(anchor_units);
        if (top_prefix_count <= 0) or (second_prefix_count <= 0) then
        begin
            Result := SameText(Trim(completion_candidates[top_index].text),
                Trim(completion_candidates[second_index].text));
            Exit;
        end;

        required_prefix_count := Min(2,
            Min(top_prefix_count, second_prefix_count));
        common_prefix_count := 0;
        while (common_prefix_count < top_prefix_count) and
            (common_prefix_count < second_prefix_count) and
            SameText(top_units[common_prefix_count],
            second_units[common_prefix_count]) do
        begin
            Inc(common_prefix_count);
        end;
        Result := common_prefix_count >= required_prefix_count;
    end;

    procedure append_anchor(const path_text, anchor_text: string;
        const segment_count: Integer);
    var
        idx: Integer;
    begin
        for idx := 0 to High(anchors) do
        begin
            if SameText(anchors[idx].path_text, path_text) and
                SameText(anchors[idx].text, anchor_text) then
            begin
                Exit;
            end;
        end;
        SetLength(anchors, Length(anchors) + 1);
        anchors[High(anchors)].path_text := path_text;
        anchors[High(anchors)].text := anchor_text;
        anchors[High(anchors)].segment_count := segment_count;
    end;

    function same_completion(const left_item: TncLongCompletionScoredItem;
        const right_value: TncLongOneKeyCompletion): Boolean;
    begin
        Result := SameText(left_item.value.suffix_text,
            right_value.suffix_text) and
            SameText(normalize_pinyin_text(left_item.value.suffix_pinyin),
            normalize_pinyin_text(right_value.suffix_pinyin));
    end;

    procedure capture_scored_completion_pool;
    var
        used: TArray<Boolean>;
        ranked_idx: Integer;
        scan_idx: Integer;
        selected_idx: Integer;
        suffix_key: string;
        debug_completion: TncOneKeyCompletion;
    begin
        SetLength(m_debug_long_one_key_completion_pool,
            Min(32, Length(items)));
        SetLength(used, Length(items));
        for ranked_idx := 0 to High(m_debug_long_one_key_completion_pool) do
        begin
            selected_idx := -1;
            for scan_idx := 0 to High(items) do
            begin
                if used[scan_idx] then
                begin
                    Continue;
                end;
                if (selected_idx < 0) or
                    (items[scan_idx].score > items[selected_idx].score) or
                    ((items[scan_idx].score = items[selected_idx].score) and
                    (CompareText(items[scan_idx].value.suffix_text,
                    items[selected_idx].value.suffix_text) < 0)) then
                begin
                    selected_idx := scan_idx;
                end;
            end;
            if selected_idx < 0 then
            begin
                SetLength(m_debug_long_one_key_completion_pool, ranked_idx);
                Break;
            end;
            used[selected_idx] := True;
            suffix_key := normalize_pinyin_text(
                items[selected_idx].value.suffix_pinyin);
            debug_completion := Default(TncOneKeyCompletion);
            debug_completion.anchor_text := Trim(completion_candidates[top_index].text);
            debug_completion.suffix_text :=
                items[selected_idx].value.suffix_text;
            debug_completion.anchor_path :=
                items[selected_idx].anchor.path_text;
            debug_completion.text := debug_completion.anchor_text +
                debug_completion.suffix_text;
            debug_completion.full_pinyin := compact_query + suffix_key;
            debug_completion.path_text := top_path + #3 +
                items[selected_idx].value.suffix_path;
            debug_completion.weight := items[selected_idx].score;
            debug_completion.source_count :=
                items[selected_idx].value.source_count;
            debug_completion.feedback_count :=
                items[selected_idx].value.feedback_count;
            debug_completion.feedback_reject_count :=
                items[selected_idx].value.feedback_reject_count;
            debug_completion.prefix_anchored := True;
            debug_completion.source := okcs_long_transition;
            m_debug_long_one_key_completion_pool[ranked_idx] :=
                debug_completion;
        end;
    end;
begin
    completion := Default(TncOneKeyCompletion);
    completion_score := Low(Integer);
    Result := False;
    completion_candidates := m_candidates;
    completion_sources := nil;
    corrected_prefix := False;
    corrected_path := '';
    if m_dictionary <> nil then adopt_corrected_prefix;
    // A failed alignment check must not silently revive the obsolete draft.
    if has_validated_completion_prefix and (not corrected_prefix)
        then Exit;
    if (m_dictionary = nil) or
        (Length(syllables) < c_min_long_syllables) or
        (compact_query = '') or (Length(completion_candidates) = 0) or
        (not is_complete_candidate(0)) then
    begin
        Exit;
    end;

    top_index := 0;
    second_index := -1;
    for item_idx := 1 to Min(High(completion_candidates), c_max_visible_scan - 1) do
    begin
        if is_complete_candidate(item_idx) then
        begin
            second_index := item_idx;
            Break;
        end;
    end;
    if not analyze_candidate_path(top_index, top_analysis) then
    begin
        Exit;
    end;
    if Length(top_analysis.segments) < 2 then
    begin
        Exit;
    end;
    top_path := Trim(completion_path(top_index));
    if top_path = '' then
    begin
        Exit;
    end;
    second_analysis := Default(TncLongCompletionPathAnalysis);
    second_path := '';
    if second_index >= 0 then
    begin
        if analyze_candidate_path(second_index, second_analysis) then
        begin
            second_path := Trim(completion_path(second_index));
        end;
    end;

    // Ordinary paths still need agreement. The separate experiment accepts a
    // fully validated repaired prefix only with additional local LM support.
    top1_uncontested := second_index < 0;
    if corrected_prefix and (GetEnvironmentVariable('CASSOTIS_TAB_REPAIR_TRUST') = '1') and
        (m_local_repair_validated.lm_gain >= 80) then top1_uncontested := True;

    top1_anchor_path := '';
    for anchor_segment_count := Min(c_max_anchor_segments,
        Length(top_analysis.segments)) downto 1 do
    begin
        if exact_suffix_path(top_analysis, anchor_segment_count,
            anchor.path_text) and (top1_uncontested or
            shared_anchor_has_stable_prefix(anchor.path_text)) then
        begin
            top1_anchor_path := anchor.path_text;
            Break;
        end;
    end;
    top2_anchor_path := '';
    if second_analysis.valid then
    begin
        for anchor_segment_count := Min(c_max_anchor_segments,
            Length(second_analysis.segments)) downto 1 do
        begin
            if exact_suffix_path(second_analysis, anchor_segment_count,
                anchor.path_text) and
                shared_anchor_has_stable_prefix(anchor.path_text) then
            begin
                top2_anchor_path := anchor.path_text;
                Break;
            end;
        end;
    end;
    query_syllable_text := build_query_syllable_text;
    // Text continuation needs a stable exact suffix.  Phonetic repair is
    // specifically intended for cases where that suffix was decoded
    // incorrectly, so it may run without one while keeping text recall off.
    request_phonetic_only := False;
    if (top1_anchor_path = '') and (query_syllable_text <> '') and
        (Length(syllables) >= 5) then
    begin
        request_phonetic_only := True;
        top1_anchor_path := top_path;
        if second_path <> '' then
        begin
            top2_anchor_path := second_path;
        end;
    end;
    if (top1_anchor_path <> '') and (query_syllable_text <> '') then
    begin
        context_value := m_left_context;
        if m_segment_left_context <> '' then
        begin
            context_value := m_segment_left_context;
        end
        else if m_external_left_context <> '' then
        begin
            context_value := m_external_left_context;
        end;
        m_long_neural_completion_request :=
            Default(TncLongNeuralCompletionRequest);
        m_long_neural_completion_request.phonetic_only :=
            request_phonetic_only;
        m_long_neural_completion_request.query_prefix := compact_query;
        m_long_neural_completion_request.query_syllables :=
            query_syllable_text;
        m_long_neural_completion_request.context_text :=
            trim_left_context_to_sentence(context_value, c_context_length);
        m_long_neural_completion_request.top1_text :=
            Trim(completion_candidates[top_index].text);
        m_long_neural_completion_request.top1_path := top_path;
        m_long_neural_completion_request.top1_anchor_path :=
            top1_anchor_path;
        if (second_index >= 0) and (top2_anchor_path <> '') then
        begin
            m_long_neural_completion_request.top2_text :=
                Trim(completion_candidates[second_index].text);
            m_long_neural_completion_request.top2_path := second_path;
            m_long_neural_completion_request.top2_anchor_path :=
                top2_anchor_path;
        end;
        m_has_long_neural_completion_request := True;
        m_long_neural_completion_prefix_locked := corrected_prefix;
    end;

    SetLength(anchors, 0);
    for anchor_segment_count := Min(c_max_anchor_segments,
        Length(top_analysis.segments)) downto 1 do
    begin
        if not exact_suffix_path(top_analysis, anchor_segment_count,
            anchor.path_text) then
        begin
            Continue;
        end;
        anchor.segment_count := anchor_segment_count;
        if shared_anchor_has_stable_prefix(anchor.path_text) or
            top1_uncontested then
        begin
            anchor.text := StringReplace(anchor.path_text, #3, '',
                [rfReplaceAll]);
            append_anchor(anchor.path_text, anchor.text,
                anchor.segment_count);
        end;
    end;
    if Length(anchors) = 0 then
    begin
        Exit;
    end;

    SetLength(items, 0);
    for anchor_idx := 0 to High(anchors) do
    begin
        SetLength(anchor_results, 0);
        if anchors[anchor_idx].path_text <> '' then
        begin
            m_dictionary.lookup_long_one_key_completions(
                anchors[anchor_idx].path_text, anchor_results);
        end;
        if Length(anchor_results) = 0 then
        begin
            Continue;
        end;
        for result_idx := 0 to High(anchor_results) do
        begin
            if (anchor_results[result_idx].evidence < c_min_evidence) or
                (anchor_results[result_idx].source_count <
                c_min_source_count) or
                (Trim(anchor_results[result_idx].suffix_text) = '') or
                (not exact_anchor_path_matches_query_tail(
                anchor_results[result_idx].anchor_path)) or
                (anchor_results[result_idx].feedback_reject_count >
                anchor_results[result_idx].feedback_count + 6) then
            begin
                Continue;
            end;

            existing_idx := -1;
            for item_idx := 0 to High(items) do
            begin
                if same_completion(items[item_idx],
                    anchor_results[result_idx]) then
                begin
                    existing_idx := item_idx;
                    Break;
                end;
            end;
            item.value := Default(TncLongOneKeyCompletion);
            item.anchor.path_text := '';
            item.anchor.segment_count := 0;
            item.score := 0;
            item.value := anchor_results[result_idx];
            item.anchor := anchors[anchor_idx];
            if Trim(item.value.anchor_path) <> '' then
            begin
                item.anchor.path_text := item.value.anchor_path;
            end;
            item.score := (item.value.evidence * 8) +
                (Min(item.value.source_count, 64) * 3) +
                (item.anchor.segment_count * 24) +
                (Min(item.value.feedback_count, 8) *
                c_feedback_accept_step) -
                (Min(item.value.feedback_reject_count, 8) *
                c_feedback_reject_step);
            if existing_idx < 0 then
            begin
                SetLength(items, Length(items) + 1);
                items[High(items)] := item;
            end
            else if item.score > items[existing_idx].score then
            begin
                items[existing_idx] := item;
            end;
        end;
    end;
    if Length(items) = 0 then
    begin
        Exit;
    end;

    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end
    else if m_external_left_context <> '' then
    begin
        context_value := m_external_left_context;
    end;
    context_value := trim_left_context_to_sentence(context_value +
        Trim(completion_candidates[top_index].text), c_context_length);
    SetLength(completion_texts, Length(items));
    for item_idx := 0 to High(items) do
    begin
        completion_texts[item_idx] := items[item_idx].value.suffix_text;
    end;
    context_lm_ok := get_cached_char_lm_scores(completion_texts,
        char_lm_scores, clsm_context, context_value);
    if not context_lm_ok then
    begin
        SetLength(char_lm_scores, Length(items));
    end;
    for item_idx := 0 to High(items) do
    begin
        if item_idx < Length(char_lm_scores) then
        begin
            Inc(items[item_idx].score,
                char_lm_scores[item_idx] div c_char_lm_divisor);
        end;
        if (m_document_context_model <> nil) and
            m_document_context_model.has_context then
        begin
            document_context_bonus := m_document_context_model.score_text(
                items[item_idx].anchor.text +
                items[item_idx].value.suffix_text);
            Inc(items[item_idx].score, document_context_bonus div 8);
        end;
        if (previous_completion.source = okcs_long_transition) and
            SameText(previous_completion.anchor_path,
            items[item_idx].anchor.path_text) and
            SameText(previous_completion.suffix_text,
            items[item_idx].value.suffix_text) and
            previous_completion.full_pinyin.StartsWith(compact_query,
            True) then
        begin
            Inc(items[item_idx].score, c_hysteresis_bonus);
        end;
    end;

    if m_debug_capture_long_one_key_completion_pool then
    begin
        capture_scored_completion_pool;
    end;

    best_idx := -1;
    second_best_idx := -1;
    best_score := Low(Integer);
    second_best_score := Low(Integer);
    for item_idx := 0 to High(items) do
    begin
        if (best_idx < 0) or (items[item_idx].score > best_score) then
        begin
            second_best_idx := best_idx;
            second_best_score := best_score;
            best_idx := item_idx;
            best_score := items[item_idx].score;
        end
        else if (second_best_idx < 0) or
            (items[item_idx].score > second_best_score) then
        begin
            second_best_idx := item_idx;
            second_best_score := items[item_idx].score;
        end;
    end;

    if (best_idx < 0) or ((second_best_idx >= 0) and
        (best_score - second_best_score < c_min_winner_margin)) then
    begin
        Exit;
    end;

    if context_lm_ok and (best_idx < Length(char_lm_scores)) then
    begin
        SetLength(anchor_lm_texts, 1);
        anchor_lm_texts[0] := items[best_idx].value.suffix_text;
        if get_cached_char_lm_scores(anchor_lm_texts, anchor_lm_scores,
            clsm_context, items[best_idx].anchor.text) and
            (Length(anchor_lm_scores) = 1) and
            (char_lm_scores[best_idx] +
            c_context_contradiction_tolerance < anchor_lm_scores[0]) then
        begin
            Exit;
        end;
    end;

    suffix_compact_pinyin := normalize_pinyin_text(
        items[best_idx].value.suffix_pinyin);
    if suffix_compact_pinyin = '' then
    begin
        Exit;
    end;
    completion := Default(TncOneKeyCompletion);
    completion.anchor_text :=
        Trim(completion_candidates[top_index].text);
    completion.suffix_text :=
        items[best_idx].value.suffix_text;
    completion.anchor_path :=
        items[best_idx].anchor.path_text;
    completion.text := completion.anchor_text +
        completion.suffix_text;
    completion.full_pinyin := compact_query +
        suffix_compact_pinyin;
    completion.path_text := top_path + #3 +
        items[best_idx].value.suffix_path;
    completion.weight := items[best_idx].value.evidence;
    completion.source_count :=
        items[best_idx].value.source_count;
    completion.feedback_count :=
        items[best_idx].value.feedback_count;
    completion.feedback_reject_count :=
        items[best_idx].value.feedback_reject_count;
    completion.prefix_anchored := True;
    completion.source := okcs_long_transition;
    completion_score := best_score;
    Result := True;
end;

procedure TncEngine.refresh_one_key_completion;
const
    c_completion_pool_limit = 32;
    c_completion_context_length = 12;
    c_completion_feedback_base = 96;
    c_completion_feedback_step = 48;
    c_completion_feedback_cap = 480;
    c_completion_reject_base = 72;
    c_completion_reject_step = 32;
    c_completion_reject_cap = 320;
    c_completion_transition_lm_divisor = 3;
    c_completion_transition_evidence_floor = 420;
    c_completion_transition_evidence_divisor = 4;
    c_completion_popularity_hot_threshold = 700;
    c_completion_popularity_warm_threshold = 480;
    c_completion_popularity_score_multiplier = 4;
    c_completion_path_score_divisor = 2;
    c_completion_hot_anchor_bonus = 220;
    c_completion_warm_anchor_bonus = 120;
    c_completion_cold_anchor_bonus = 48;
    c_completion_exact_anchor_over_unanchored_bonus = 128;
    c_completion_cold_unanchored_vertical_penalty = 220;
    c_completion_cold_vertical_context_lm_margin = 0;
    c_completion_hot_transition_penalty = 1200;
    c_completion_warm_transition_penalty = 1100;
    c_completion_cold_vertical_transition_penalty = 500;
    c_completion_generic_transition_penalty = 1000;
    c_completion_char_lm_divisor = 4;
    c_completion_long_tail_penalty = 18;
    c_completion_no_context_transition_evidence_margin = 24;
    c_completion_no_context_margin = 32;
    c_completion_context_margin = 18;
    c_completion_hysteresis_margin = 96;
    c_completion_no_context_switch_margin = 144;
    c_completion_context_switch_margin = 88;
    c_completion_hot_no_context_switch_margin = 180;
    c_completion_hot_context_switch_margin = 140;
    c_completion_warm_no_context_switch_margin = 112;
    c_completion_warm_context_switch_margin = 80;
    c_completion_cold_vertical_no_context_switch_margin = 40;
    c_completion_cold_vertical_context_switch_margin = 24;
    c_completion_generic_no_context_switch_margin = 80;
    c_completion_generic_context_switch_margin = 56;
    c_completion_context_model_margin = 1.0;
    c_completion_context_residual_margin = 2.7;
    c_completion_calibrated_margin = 1.5;
    c_completion_difference_hysteresis_margin = 3.0;
    c_completion_topk_hysteresis_margin = 0.0;
    c_completion_competition_top_k = 5;
    c_completion_competition_score_scale = 1000;
var
    query_text: string;
    compact_query: string;
    syllables: TncPinyinParseResult;
    completions: TncOneKeyCompletionList;
    shuangpin_decode: TncShuangpinDecodeResult;
    previous_completion: TncOneKeyCompletion;
    previous_query_prefix: string;
    context_value: string;
    completion_texts: TArray<string>;
    char_lm_scores: TArray<Integer>;
    scores: TArray<Integer>;
    eligible: TArray<Boolean>;
    has_char_lm: Boolean;
    has_base_completion: Boolean;
    has_anchored_base: Boolean;
    has_feedback: Boolean;
    has_popularity_prior: Boolean;
    eligible_count: Integer;
    baseline_idx: Integer;
    best_idx: Integer;
    second_idx: Integer;
    previous_idx: Integer;
    idx: Integer;
    units: Integer;
    remaining_units: Integer;
    feedback_bonus: Integer;
    reject_penalty: Integer;
    document_context_bonus: Integer;
    transition_bonus: Integer;
    transition_penalty: Integer;
    anchor_bonus: Integer;
    required_margin: Integer;
    top_margin: Integer;
    model_baseline_idx: Integer;
    model_best_idx: Integer;
    model_best_lm_idx: Integer;
    model_candidate_count: Integer;
    model_rank: Integer;
    model_idx: Integer;
    model_baseline_score: Double;
    model_best_score: Double;
    model_candidate_score: Double;
    model_current_score: Double;
    residual_best_idx: Integer;
    residual_best_score: Double;
    residual_candidate_score: Double;
    residual_current_score: Double;
    calibrated_best_idx: Integer;
    calibrated_best_score: Double;
    calibrated_candidate_score: Double;
    calibrated_current_score: Double;
    previous_compatible: Boolean;
    previous_present: Boolean;
    transition_challenge_allowed: Boolean;
    stronger_general_idx: Integer;
    selected_lm_per_unit: Integer;
    general_lm_per_unit: Integer;
    difference_best_idx: Integer;
    difference_idx: Integer;
    difference_score: Double;
    difference_best_score: Double;
    difference_threshold: Double;
    difference_category: TncOneKeyCompletionDifferenceCategory;
    topk_best_idx: Integer;
    topk_idx: Integer;
    topk_score: Double;
    topk_best_score: Double;
    topk_current_score: Double;
    topk_threshold: Double;
    topk_category: TncOneKeyCompletionDifferenceCategory;
    competition_evidence: TncOneKeyCompletionCompetitionEvidenceList;
    competition_width: Integer;
    competition_candidate_idx: Integer;
    competition_candidate_count: Integer;
    competition_best_idx: Integer;
    competition_best_score: Integer;
    competition_current_score: Integer;
    competition_candidate_score: Integer;
    competition_margin: Integer;
    competition_ratio_left: Integer;
    competition_ratio_right: Integer;
    competition_has_evidence: Boolean;
    pair_audit: TncOneKeyCompletionPairAudit;
    pair_candidate_idx: Integer;
    pair_candidate_count: Integer;
    pair_best_idx: Integer;
    pair_best_width: Integer;
    pair_best_confidence: Integer;
    pair_best_observations: Integer;
    pair_confidence: Integer;
    pair_observations: Integer;
    ncgpt_candidate_features:
        array of TncOneKeyCompletionNcgptCandidateFeatures;
    ncgpt_pair_features: TncOneKeyCompletionNcgptPairFeatures;
    ncgpt_needed_indices: TArray<Integer>;
    ncgpt_stage1_features: TncShortContextDifferenceFeatures;
    ncgpt_best_idx: Integer;
    ncgpt_incumbent_idx: Integer;
    ncgpt_primary_idx: Integer;
    ncgpt_candidate_idx: Integer;
    ncgpt_best_score: Double;
    ncgpt_primary_score: Double;
    ncgpt_candidate_score: Double;
    ncgpt_threshold: Double;
    ncgpt_primary_threshold: Double;
    ncgpt_category: TncOneKeyCompletionDifferenceCategory;
    ncgpt_primary_category: TncOneKeyCompletionDifferenceCategory;
    ncgpt_common_prefix: Integer;
    ncgpt_common_suffix: Integer;
    ncgpt_prior_score: Double;
    ncgpt_candidate_scores: TArray<Double>;
    ncgpt_candidate_thresholds: TArray<Double>;
    ncgpt_candidate_categories:
        TArray<TncOneKeyCompletionDifferenceCategory>;
    ncgpt_candidate_scored: TArray<Boolean>;
    ncgpt_fallback_idx: Integer;
    ncgpt_fallback_score: Double;
    ncgpt_fallback_candidate_score: Double;
    has_completion_lookup: Boolean;
    long_completion: TncOneKeyCompletion;
    long_completion_score: Integer;
    has_long_completion: Boolean;
    document_completion: TncOneKeyCompletion;
    document_completion_score: Integer;
    has_document_completion: Boolean;

    procedure apply_long_completion;
    begin
        if not has_long_completion then
        begin
            Exit;
        end;
        if Trim(m_one_key_completion.text) <> '' then
        begin
            m_debug_lexical_one_key_completion := m_one_key_completion;
        end;
        // The first refactoring step deliberately preserves the established
        // source priority. The candidate is now retained independently so a
        // length-calibrated cross-source arbiter can compare it with the
        // settled lexical winner without changing candidate generation.
        m_one_key_completion := long_completion;
        m_one_key_completion_query_prefix := compact_query;
        m_one_key_completion_score := long_completion_score;
    end;

    function competition_score_for_candidate(const candidate_idx: Integer;
        const context_width: Integer; out evidence_score: Integer): Boolean;
    var
        evidence_idx: Integer;
        candidate_pinyin: string;
    begin
        evidence_score := 0;
        Result := False;
        if (candidate_idx < 0) or (candidate_idx > High(completions)) then
        begin
            Exit;
        end;
        candidate_pinyin := normalize_pinyin_text(
            completions[candidate_idx].full_pinyin);
        for evidence_idx := 0 to High(competition_evidence) do
        begin
            if (competition_evidence[evidence_idx].context_width <>
                context_width) or
                (not SameText(competition_evidence[evidence_idx].text,
                completions[candidate_idx].text)) or
                (not SameText(normalize_pinyin_text(
                competition_evidence[evidence_idx].full_pinyin),
                candidate_pinyin)) then
            begin
                Continue;
            end;
            if (not Result) or
                (competition_evidence[evidence_idx].evidence_score >
                evidence_score) then
            begin
                evidence_score :=
                    competition_evidence[evidence_idx].evidence_score;
                Result := True;
            end;
        end;
    end;

    function has_stronger_general_anchor(const cold_idx: Integer): Boolean;
    var
        candidate_idx: Integer;
    begin
        Result := False;
        if (cold_idx < 0) or (cold_idx > High(completions)) then
        begin
            Exit;
        end;
        for candidate_idx := 0 to High(completions) do
        begin
            if (candidate_idx = cold_idx) or
                (completions[candidate_idx].source <> okcs_base_exact) or
                (not completions[candidate_idx].has_popularity_prior) or
                (not completions[candidate_idx].prefix_anchored) or
                (completions[candidate_idx].vertical_layer_kind <> 0) then
            begin
                Continue;
            end;
            if (completions[candidate_idx].popularity_prior >=
                completions[cold_idx].popularity_prior + 24) and
                (completions[candidate_idx].corpus_score >
                completions[cold_idx].corpus_score) then
            begin
                Result := True;
                Exit;
            end;
        end;
    end;

    function find_best_stronger_general_anchor(
        const cold_idx: Integer): Integer;
    var
        candidate_idx: Integer;
    begin
        Result := -1;
        if (cold_idx < 0) or (cold_idx > High(completions)) then
        begin
            Exit;
        end;
        for candidate_idx := 0 to High(completions) do
        begin
            if (candidate_idx = cold_idx) or
                (candidate_idx > High(eligible)) or
                (not eligible[candidate_idx]) or
                (completions[candidate_idx].source <> okcs_base_exact) or
                (not completions[candidate_idx].has_popularity_prior) or
                (not completions[candidate_idx].prefix_anchored) or
                (completions[candidate_idx].vertical_layer_kind <> 0) or
                (completions[candidate_idx].popularity_prior <
                completions[cold_idx].popularity_prior + 24) or
                (completions[candidate_idx].corpus_score <=
                completions[cold_idx].corpus_score) then
            begin
                Continue;
            end;
            if (Result < 0) or (scores[candidate_idx] > scores[Result]) or
                ((scores[candidate_idx] = scores[Result]) and
                (candidate_idx < Result)) then
            begin
                Result := candidate_idx;
            end;
        end;
    end;

    procedure get_common_completion_fragment_units(const left_text,
        right_text: string; out common_prefix, common_suffix: Integer);
    var
        left_units: TArray<string>;
        right_units: TArray<string>;
        compare_limit: Integer;
    begin
        common_prefix := 0;
        common_suffix := 0;
        left_units := split_text_units(left_text);
        right_units := split_text_units(right_text);
        compare_limit := Min(Length(left_units), Length(right_units));
        while (common_prefix < compare_limit) and
            (left_units[common_prefix] = right_units[common_prefix]) do
        begin
            Inc(common_prefix);
        end;
        compare_limit := compare_limit - common_prefix;
        while (common_suffix < compare_limit) and
            (left_units[High(left_units) - common_suffix] =
            right_units[High(right_units) - common_suffix]) do
        begin
            Inc(common_suffix);
        end;
    end;

    procedure fill_ncgpt_completion_candidate_features(
        const candidate_idx: Integer;
        out features: TncOneKeyCompletionNcgptCandidateFeatures);
    var
        item: TncOneKeyCompletion;
    begin
        FillChar(features, SizeOf(features), 0);
        item := completions[candidate_idx];
        features[0] := item.weight;
        features[1] := item.popularity_prior;
        features[2] := item.corpus_score;
        features[3] := item.document_score;
        features[4] := item.source_count;
        features[5] := item.path_score;
        features[6] := item.vertical_penalty;
        features[7] := item.vertical_layer_kind;
        features[8] := Ord(item.prefix_anchored);
        features[9] := item.feedback_count;
        features[10] := item.feedback_reject_count;
        features[11] := char_lm_scores[candidate_idx];

        ncgpt_stage1_features := Default(TncShortContextDifferenceFeatures);
        short_context_difference_fill_stage1_evidence(context_value,
            completions[0].text, item.text, ncgpt_stage1_features);
        features[12] := ncgpt_stage1_features.pair_log_odds;
        features[13] := ncgpt_stage1_features.pair_log_total;
        features[14] := ncgpt_stage1_features.pair_context_log_odds;
        features[15] := ncgpt_stage1_features.pair_context_log_total;
        features[16] := ncgpt_stage1_features.pair_context_width;
        features[17] := ncgpt_stage1_features.mined_log_odds;
        features[18] := ncgpt_stage1_features.mined_log_total;
        features[19] := ncgpt_stage1_features.mined_context_width;
        features[20] := ncgpt_stage1_features.novel_log_odds;
        features[21] := ncgpt_stage1_features.chat_log_odds;
        features[22] := ncgpt_stage1_features.formal_log_odds;
        features[23] := ncgpt_stage1_features.total_log_odds;
        features[24] := candidate_idx + 1;
        features[25] := Ord(item.source = okcs_user_exact);
        features[26] := Ord(item.source = okcs_base_exact);
        features[27] := Ord(item.source = okcs_transition);
        features[28] := get_candidate_text_unit_count(item.text);
        features[29] := Length(item.full_pinyin);
        features[30] := Max(0, Length(item.full_pinyin) -
            Length(compact_query));
        features[31] := Length(compact_query);
        features[32] := get_candidate_text_unit_count(context_value);
        features[33] := Length(syllables);
    end;

    function prepare_ncgpt_completion_features: Boolean;
    var
        feature_idx: Integer;
        completion_idx: Integer;
        category: TncOneKeyCompletionDifferenceCategory;
        threshold: Double;
    begin
        Result := False;
        if (context_value = '') or (not has_char_lm) or
            (Length(completions) < 2) then
        begin
            Exit;
        end;

        // Only materialize the settled incumbent and categories whose
        // independently calibrated threshold permits a challenge. Most
        // completion groups therefore avoid all extra LM/evidence work.
        SetLength(ncgpt_needed_indices, 1);
        ncgpt_needed_indices[0] := best_idx;
        for completion_idx := 0 to High(completions) do
        begin
            if (completion_idx = best_idx) or
                (not eligible[completion_idx]) or
                (completions[best_idx].feedback_count >
                completions[completion_idx].feedback_count) then
            begin
                Continue;
            end;
            category := one_key_completion_difference_category(
                completions[best_idx], completions[completion_idx]);
            threshold := one_key_completion_ncgpt_threshold(category);
            if threshold >= 1.0E29 then
            begin
                Continue;
            end;
            SetLength(ncgpt_needed_indices,
                Length(ncgpt_needed_indices) + 1);
            ncgpt_needed_indices[High(ncgpt_needed_indices)] := completion_idx;
        end;
        if Length(ncgpt_needed_indices) < 2 then
        begin
            Exit;
        end;

        SetLength(ncgpt_candidate_features, Length(completions));
        for feature_idx := 0 to High(ncgpt_needed_indices) do
        begin
            completion_idx := ncgpt_needed_indices[feature_idx];
            fill_ncgpt_completion_candidate_features(completion_idx,
                ncgpt_candidate_features[completion_idx]);
        end;
        Result := True;
    end;
begin
    previous_completion := m_one_key_completion;
    previous_query_prefix := m_one_key_completion_query_prefix;
    clear_one_key_completion;
    if (not m_allow_one_key_completion_lookup) or (m_dictionary = nil) or
        (m_config.input_mode <> im_chinese) or
        (Trim(m_confirmed_text) <> '') or (Trim(m_composition_text) = '') or
        m_has_pending_commit then
    begin
        Exit;
    end;

    query_text := LowerCase(Trim(m_composition_text));
    if is_shuangpin_input then
    begin
        shuangpin_decode := nc_decode_shuangpin(
            m_config.pinyin_input_scheme, m_composition_display_text);
        if (not shuangpin_decode.valid) or shuangpin_decode.has_pending_key then
        begin
            Exit;
        end;
    end;
    // Completion lookup also accepts a partial final syllable after at least
    // two parsed units. The dictionary reuses the nearest completed-syllable
    // index and strictly filters every result by the complete typed key.
    syllables := get_effective_compact_pinyin_syllables(query_text, False);
    if Length(syllables) < 2 then
    begin
        Exit;
    end;
    compact_query := normalize_pinyin_text(query_text);
    if (m_completion_feedback_origin_prefix <> '') and
        (not compact_query.StartsWith(
        m_completion_feedback_origin_prefix, True)) then
    begin
        clear_one_key_completion_feedback_target;
    end;
    if (previous_completion.source in
        [okcs_long_transition, okcs_long_neural]) and
        (previous_query_prefix <> '') and
        compact_query.StartsWith(previous_query_prefix, True) and
        (Length(compact_query) > Length(previous_query_prefix)) and
        (not previous_completion.full_pinyin.StartsWith(compact_query,
        True)) and (previous_completion.anchor_path <> '') and
        (previous_completion.suffix_text <> '') then
    begin
        m_dictionary.record_long_one_key_completion_reject(
            previous_completion.anchor_path,
            previous_completion.suffix_text);
    end;
    if (previous_completion.source = okcs_document_copy) and
        (m_document_context_model <> nil) and
        (previous_query_prefix <> '') and
        compact_query.StartsWith(previous_query_prefix, True) and
        (Length(compact_query) > Length(previous_query_prefix)) and
        (not previous_completion.full_pinyin.StartsWith(compact_query,
        True)) and (previous_completion.anchor_path <> '') and
        (previous_completion.suffix_text <> '') then
    begin
        m_document_context_model.record_completion_feedback(
            previous_completion.anchor_path,
            previous_completion.suffix_text, False);
    end;
    // Continuing beyond the previously suggested full spelling is an
    // unambiguous local rejection of that prefix-to-completion relation. It is
    // learned separately from ordinary user words and recorded only once,
    // because the rejected completion is no longer current on the next key.
    if (previous_completion.source in [okcs_base_exact, okcs_transition]) and
        (previous_query_prefix <> '') and
        compact_query.StartsWith(previous_query_prefix, True) and
        (Length(compact_query) > Length(previous_query_prefix)) and
        (not previous_completion.full_pinyin.StartsWith(compact_query, True)) then
    begin
        m_dictionary.record_one_key_completion_reject(
            previous_query_prefix, previous_completion.full_pinyin,
            previous_completion.text);
        m_completion_feedback_origin_prefix := previous_query_prefix;
    end;

    SetLength(completions, 0);
    has_completion_lookup :=
        m_dictionary.lookup_one_key_completions(query_text, completions) and
        (Length(completions) > 0);

    // User completions are already reduced to their best relation by the
    // dictionary and remain an absolute priority class.
    if has_completion_lookup and
        (completions[0].source = okcs_user_exact) then
    begin
        m_one_key_completion := completions[0];
        m_one_key_completion_query_prefix := compact_query;
        m_one_key_completion_score := MaxInt;
        Exit;
    end;

    document_completion := Default(TncOneKeyCompletion);
    document_completion_score := Low(Integer);
    has_document_completion := try_refresh_document_copy_completion(
        compact_query, previous_completion, document_completion,
        document_completion_score);
    if has_document_completion and has_validated_completion_prefix and
        (not document_completion.text.StartsWith(m_local_repair_text)) then
        has_document_completion := False;
    if has_document_completion then
    begin
        m_one_key_completion := document_completion;
        m_one_key_completion_query_prefix := compact_query;
        m_one_key_completion_score := document_completion_score;
        Exit;
    end;

    long_completion := Default(TncOneKeyCompletion);
    long_completion_score := Low(Integer);
    has_long_completion := try_refresh_long_one_key_completion(syllables,
        compact_query, previous_completion, long_completion,
        long_completion_score);
    if has_long_completion then
    begin
        apply_long_completion;
        if not m_debug_capture_long_one_key_completion_pool then
        begin
            // Production preserves the established long-completion priority
            // and must not pay for lexical scoring that will be overwritten.
            // Oracle runs explicitly enable pool capture and continue below so
            // all sources remain available to offline training diagnostics.
            Exit;
        end;
    end;
    if not has_completion_lookup then
    begin
        Exit;
    end;

    previous_compatible :=
        (previous_completion.source in [okcs_base_exact, okcs_transition]) and
        (Trim(previous_completion.text) <> '') and
        (previous_query_prefix <> '') and
        compact_query.StartsWith(previous_query_prefix, True) and
        (Length(previous_completion.full_pinyin) > Length(compact_query)) and
        previous_completion.full_pinyin.StartsWith(compact_query, True);
    if previous_compatible then
    begin
        previous_present := False;
        for idx := 0 to High(completions) do
        begin
            if SameText(completions[idx].text, previous_completion.text) and
                SameText(completions[idx].full_pinyin,
                previous_completion.full_pinyin) then
            begin
                previous_present := True;
                Break;
            end;
        end;
        if not previous_present then
        begin
            previous_completion.prefix_anchored := False;
            if Length(completions) < c_completion_pool_limit then
            begin
                SetLength(completions, Length(completions) + 1);
                completions[High(completions)] := previous_completion;
            end
            else
            begin
                // Hysteresis may retain the previous key's completion, but it
                // must not expand the fixed-work Top-32 ranking pool.
                completions[High(completions)] := previous_completion;
            end;
        end;
    end;

    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end
    else if m_external_left_context <> '' then
    begin
        context_value := m_external_left_context;
    end;
    context_value := trim_left_context_to_sentence(context_value,
        c_completion_context_length);

    has_base_completion := completions[0].source = okcs_base_exact;
    has_anchored_base := False;
    has_feedback := False;
    has_popularity_prior := False;
    for idx := 0 to High(completions) do
    begin
        if (completions[idx].source = okcs_base_exact) and
            completions[idx].prefix_anchored then
        begin
            has_anchored_base := True;
        end;
        if completions[idx].feedback_count > 0 then
        begin
            has_feedback := True;
        end;
        if completions[idx].has_popularity_prior then
        begin
            has_popularity_prior := True;
        end;
    end;

    // Databases built before the completion-prior index retain their legacy
    // deterministic behavior. New databases use the same unified scorer with
    // or without left context.
    if (not has_popularity_prior) and (context_value = '') and
        (not has_feedback) then
    begin
        if (completions[0].source = okcs_transition) and
            (Length(completions) > 1) and
            (completions[1].source = okcs_transition) and
            (completions[0].weight - completions[1].weight <
            c_completion_no_context_transition_evidence_margin) then
        begin
            Exit;
        end;
        m_one_key_completion := completions[0];
        m_one_key_completion_query_prefix := compact_query;
        m_one_key_completion_score := completions[0].weight;
        apply_long_completion;
        Exit;
    end;

    SetLength(completion_texts, Length(completions));
    SetLength(scores, Length(completions));
    SetLength(eligible, Length(completions));
    eligible_count := 0;
    for idx := 0 to High(completions) do
    begin
        completion_texts[idx] := completions[idx].text;
        if context_value = '' then
        begin
            eligible[idx] := has_popularity_prior or (idx = 0) or
                (completions[idx].feedback_count > 0);
        end
        else if has_base_completion then
        begin
            if has_popularity_prior then
            begin
                // Reliable base and transition Top-K candidates share one
                // ranking pool. The offline transition index already enforces
                // exact components and multi-source evidence.
                eligible[idx] := True;
            end
            else if completions[idx].source = okcs_base_exact then
            begin
                // Once an exact word boundary is available, keep all such
                // base alternatives for context ranking and suppress weaker
                // ordinary-prefix variants. Preserve index zero as the
                // established fallback even if it is unanchored.
                eligible[idx] := (not has_anchored_base) or
                    completions[idx].prefix_anchored or (idx = 0);
            end
            else
            begin
                // A transition may challenge an ordinary base prefix only
                // when its first exact segment ends at the typed boundary.
                eligible[idx] := (not has_anchored_base) and
                    completions[idx].prefix_anchored;
            end;
            eligible[idx] := eligible[idx] or
                (completions[idx].feedback_count > 0) or
                (previous_compatible and
                SameText(completions[idx].text, previous_completion.text) and
                SameText(completions[idx].full_pinyin,
                previous_completion.full_pinyin));
        end
        else
        begin
            eligible[idx] := True;
        end;
        if eligible[idx] then
        begin
            Inc(eligible_count);
        end;
    end;
    has_char_lm := (eligible_count > 1) and
        get_cached_char_lm_scores(completion_texts,
        char_lm_scores, clsm_context, context_value) and
        (Length(char_lm_scores) = Length(completions));

    best_idx := -1;
    second_idx := -1;
    previous_idx := -1;
    baseline_idx := -1;
    for idx := 0 to High(completions) do
    begin
        if not eligible[idx] then
        begin
            scores[idx] := Low(Integer);
            Continue;
        end;
        if baseline_idx < 0 then
        begin
            baseline_idx := idx;
        end;

        if has_popularity_prior and
            (completions[idx].source = okcs_base_exact) and
            completions[idx].has_popularity_prior then
        begin
            scores[idx] := completions[idx].weight +
                (completions[idx].popularity_prior *
                c_completion_popularity_score_multiplier);
        end
        else if has_popularity_prior and
            (completions[idx].source = okcs_transition) then
        begin
            if (baseline_idx >= 0) and
                (completions[baseline_idx].source = okcs_base_exact) then
            begin
                if completions[baseline_idx].has_popularity_prior and
                    (completions[baseline_idx].popularity_prior >=
                    c_completion_popularity_hot_threshold) and
                    (completions[baseline_idx].source_count >= 2) then
                begin
                    transition_penalty := c_completion_hot_transition_penalty;
                end
                else if completions[baseline_idx].has_popularity_prior and
                    (completions[baseline_idx].popularity_prior >=
                    c_completion_popularity_warm_threshold) then
                begin
                    transition_penalty := c_completion_warm_transition_penalty;
                end
                else if completions[baseline_idx].has_popularity_prior and
                    (completions[baseline_idx].vertical_layer_kind > 0) and
                    (completions[baseline_idx].popularity_prior < 300) then
                begin
                    transition_penalty :=
                        c_completion_cold_vertical_transition_penalty;
                end
                else
                begin
                    transition_penalty :=
                        c_completion_generic_transition_penalty;
                end;
                scores[idx] := completions[baseline_idx].weight -
                    transition_penalty +
                    (Max(0, completions[idx].weight -
                    c_completion_transition_evidence_floor) div
                    c_completion_transition_evidence_divisor);
            end
            else
            begin
                scores[idx] := completions[idx].weight;
            end;
        end
        else if (completions[idx].source = okcs_transition) and
            (completions[0].source = okcs_base_exact) then
        begin
            // Transition evidence and lexicon weight have different scales.
            // Treat evidence as a bounded advantage relative to the legacy
            // base choice instead of comparing the raw integers directly.
            scores[idx] := completions[0].weight - 320 +
                (Max(0, completions[idx].weight -
                c_completion_transition_evidence_floor) div 4);
        end
        else
        begin
            scores[idx] := completions[idx].weight;
        end;
        if (completions[idx].source = okcs_base_exact) and
            completions[idx].has_popularity_prior and
            (not completions[idx].prefix_anchored) and
            (completions[idx].vertical_layer_kind > 0) and
            ((context_value = '') or
            (completions[idx].vertical_layer_kind = 3)) and
            (completions[idx].popularity_prior < 300) and
            (completions[idx].corpus_score = 0) and
            (completions[idx].document_score = 0) and
            (completions[idx].path_score = 0) and
            has_stronger_general_anchor(idx) then
        begin
            // A cold vertical phrase must not win only because its raw weight
            // is high when a general exact-boundary completion has stronger
            // corpus evidence. Context may still restore it below when the
            // normalized language evidence is genuinely stronger.
            Dec(scores[idx], c_completion_cold_unanchored_vertical_penalty);
        end;
        if (completions[idx].source = okcs_base_exact) and
            completions[idx].prefix_anchored then
        begin
            if has_popularity_prior and
                completions[idx].has_popularity_prior then
            begin
                if (completions[idx].popularity_prior >=
                    c_completion_popularity_hot_threshold) and
                    (completions[idx].source_count >= 2) then
                begin
                    anchor_bonus := c_completion_hot_anchor_bonus;
                end
                else if completions[idx].popularity_prior >=
                    c_completion_popularity_warm_threshold then
                begin
                    anchor_bonus := c_completion_warm_anchor_bonus;
                end
                else
                begin
                    anchor_bonus := c_completion_cold_anchor_bonus;
                end;
                if (context_value = '') and
                    (get_candidate_text_unit_count(completions[idx].text) =
                    Length(syllables) + 1) and (baseline_idx >= 0) and
                    (completions[baseline_idx].source = okcs_base_exact) and
                    (not completions[baseline_idx].prefix_anchored) then
                begin
                    Inc(anchor_bonus,
                        c_completion_exact_anchor_over_unanchored_bonus);
                end;
                Inc(scores[idx], anchor_bonus);
            end
            else
            begin
                scores[idx] := completions[idx].weight * 2;
            end;
        end
        else if (completions[idx].source = okcs_transition) and
            (Trim(completions[idx].path_text) <> '') then
        begin
            transition_bonus := m_dictionary.get_lm_transition_bonus(
                completions[idx].full_pinyin,
                completions[idx].path_text);
            if transition_bonus > 0 then
            begin
                Inc(scores[idx], transition_bonus div
                    c_completion_transition_lm_divisor);
            end;
        end;

        units := Max(1, get_candidate_text_unit_count(
            completions[idx].text));
        remaining_units := units - Length(syllables);
        if (completions[idx].source = okcs_base_exact) and
            (completions[idx].path_score > 0) and
            (remaining_units >= 1) and (remaining_units <= 2) then
        begin
            // Internal word-path cohesion is useful for local completion, but
            // must not make a much longer phrase override a valid short word.
            Inc(scores[idx], completions[idx].path_score div
                c_completion_path_score_divisor);
        end;
        if has_char_lm then
        begin
            Inc(scores[idx], (char_lm_scores[idx] div units) div
                c_completion_char_lm_divisor);
        end;
        if (m_document_context_model <> nil) and
            m_document_context_model.has_context then
        begin
            document_context_bonus :=
                m_document_context_model.score_text(completions[idx].text);
            Inc(scores[idx], document_context_bonus div 8);
        end;

        if completions[idx].feedback_count > 0 then
        begin
            feedback_bonus := c_completion_feedback_base +
                ((Min(completions[idx].feedback_count, 9) - 1) *
                c_completion_feedback_step);
            feedback_bonus := Min(feedback_bonus,
                c_completion_feedback_cap);
            Inc(scores[idx], feedback_bonus);
        end;
        if completions[idx].feedback_reject_count > 0 then
        begin
            reject_penalty := c_completion_reject_base +
                ((Min(completions[idx].feedback_reject_count, 9) - 1) *
                c_completion_reject_step);
            reject_penalty := Min(reject_penalty, c_completion_reject_cap);
            Dec(scores[idx], reject_penalty);
        end;

        if remaining_units > 2 then
        begin
            Dec(scores[idx], (remaining_units - 2) *
                c_completion_long_tail_penalty);
        end;

        if SameText(completions[idx].text, previous_completion.text) and
            SameText(completions[idx].full_pinyin,
            previous_completion.full_pinyin) then
        begin
            previous_idx := idx;
        end;

        if (best_idx < 0) or (scores[idx] > scores[best_idx]) or
            ((scores[idx] = scores[best_idx]) and
            (CompareText(completions[idx].text,
            completions[best_idx].text) < 0)) then
        begin
            second_idx := best_idx;
            best_idx := idx;
        end
        else if (second_idx < 0) or
            (scores[idx] > scores[second_idx]) or
            ((scores[idx] = scores[second_idx]) and
            (CompareText(completions[idx].text,
            completions[second_idx].text) < 0)) then
        begin
            second_idx := idx;
        end;
    end;

    if best_idx < 0 then
    begin
        Exit;
    end;

    // Preserve the deterministic legacy choice unless a challenger has
    // genuinely strong evidence. This makes Top-K additive rather than
    // allowing a weak LM fluctuation to rewrite every existing completion.
    if (baseline_idx >= 0) and (best_idx <> baseline_idx) and
        (completions[best_idx].feedback_count <= 0) then
    begin
        if has_popularity_prior and
            (completions[baseline_idx].source = okcs_base_exact) and
            completions[baseline_idx].has_popularity_prior then
        begin
            if (completions[baseline_idx].popularity_prior >=
                c_completion_popularity_hot_threshold) and
                (completions[baseline_idx].source_count >= 2) then
            begin
                if context_value = '' then
                begin
                    required_margin :=
                        c_completion_hot_no_context_switch_margin;
                end
                else
                begin
                    required_margin := c_completion_hot_context_switch_margin;
                end;
            end
            else if completions[baseline_idx].popularity_prior >=
                c_completion_popularity_warm_threshold then
            begin
                if context_value = '' then
                begin
                    required_margin :=
                        c_completion_warm_no_context_switch_margin;
                end
                else
                begin
                    required_margin := c_completion_warm_context_switch_margin;
                end;
            end
            else if (completions[baseline_idx].vertical_layer_kind > 0) and
                (completions[baseline_idx].popularity_prior < 300) then
            begin
                if context_value = '' then
                begin
                    required_margin :=
                        c_completion_cold_vertical_no_context_switch_margin;
                end
                else
                begin
                    required_margin :=
                        c_completion_cold_vertical_context_switch_margin;
                end;
            end
            else if context_value = '' then
            begin
                required_margin :=
                    c_completion_generic_no_context_switch_margin;
            end
            else
            begin
                required_margin := c_completion_generic_context_switch_margin;
            end;
        end
        else if context_value = '' then
        begin
            required_margin := c_completion_no_context_switch_margin;
        end
        else
        begin
            required_margin := c_completion_context_switch_margin;
        end;
        if scores[best_idx] - scores[baseline_idx] < required_margin then
        begin
            best_idx := baseline_idx;
        end;
    end;

    // Completion candidates are longer than the ordinary short-word choices,
    // but their leading characters and the left-context boundary use the same
    // independently trained evidence. Apply that model only within the base
    // lexicon class; user feedback and transition recall keep their own rules.
    if (context_value <> '') and has_char_lm and has_base_completion and
        (best_idx >= 0) and
        (completions[best_idx].source = okcs_base_exact) then
    begin
        model_baseline_idx := -1;
        model_best_lm_idx := -1;
        model_candidate_count := 0;
        for model_idx := 0 to High(completions) do
        begin
            if eligible[model_idx] and
                (completions[model_idx].source = okcs_base_exact) then
            begin
                if model_baseline_idx < 0 then
                begin
                    model_baseline_idx := model_idx;
                end;
                if (model_best_lm_idx < 0) or
                    (char_lm_scores[model_idx] >
                    char_lm_scores[model_best_lm_idx]) then
                begin
                    model_best_lm_idx := model_idx;
                end;
                Inc(model_candidate_count);
            end;
        end;
        if model_candidate_count > 1 then
        begin
            model_best_idx := model_baseline_idx;
            model_rank := 1;
            model_baseline_score := short_context_reranker_score(
                context_value, completions[model_baseline_idx].text,
                model_rank, char_lm_scores[model_baseline_idx],
                completions[model_baseline_idx].weight,
                scores[model_baseline_idx], True, 0, 0,
                char_lm_scores[model_baseline_idx] -
                char_lm_scores[model_best_lm_idx],
                model_baseline_idx = model_best_lm_idx,
                model_candidate_count, Length(context_value),
                Length(completions[model_baseline_idx].text));
            model_best_score := model_baseline_score;
            model_current_score := model_baseline_score;
            for model_idx := 0 to High(completions) do
            begin
                if (not eligible[model_idx]) or
                    (completions[model_idx].source <> okcs_base_exact) then
                begin
                    Continue;
                end;
                if model_idx = model_baseline_idx then
                begin
                    Continue;
                end;
                Inc(model_rank);
                model_candidate_score := short_context_reranker_score(
                    context_value, completions[model_idx].text, model_rank,
                    char_lm_scores[model_idx], completions[model_idx].weight,
                    scores[model_idx], False,
                    char_lm_scores[model_idx] -
                    char_lm_scores[model_baseline_idx],
                    completions[model_idx].weight -
                    completions[model_baseline_idx].weight,
                    char_lm_scores[model_idx] -
                    char_lm_scores[model_best_lm_idx],
                    model_idx = model_best_lm_idx, model_candidate_count,
                    Length(context_value), Length(completions[model_idx].text));
                if model_idx = best_idx then
                begin
                    model_current_score := model_candidate_score;
                end;
                if model_candidate_score > model_best_score then
                begin
                    model_best_idx := model_idx;
                    model_best_score := model_candidate_score;
                end;
            end;
            if (model_best_idx <> best_idx) and
                (model_best_score - model_current_score >=
                c_completion_context_model_margin) then
            begin
                best_idx := model_best_idx;
            end;

            residual_best_idx := best_idx;
            residual_current_score := short_context_residual_score(
                context_value, completions[best_idx].text, 1,
                char_lm_scores[best_idx], completions[best_idx].weight,
                scores[best_idx], True, 0, 0,
                char_lm_scores[best_idx] -
                char_lm_scores[model_best_lm_idx],
                best_idx = model_best_lm_idx, model_candidate_count,
                Length(context_value), Length(completions[best_idx].text));
            residual_best_score := residual_current_score;
            model_rank := 0;
            for model_idx := 0 to High(completions) do
            begin
                if (not eligible[model_idx]) or
                    (completions[model_idx].source <> okcs_base_exact) then
                begin
                    Continue;
                end;
                Inc(model_rank);
                if model_idx = best_idx then
                begin
                    Continue;
                end;
                residual_candidate_score := short_context_residual_score(
                    context_value, completions[model_idx].text, model_rank,
                    char_lm_scores[model_idx], completions[model_idx].weight,
                    scores[model_idx], False,
                    char_lm_scores[model_idx] - char_lm_scores[best_idx],
                    completions[model_idx].weight -
                    completions[best_idx].weight,
                    char_lm_scores[model_idx] -
                    char_lm_scores[model_best_lm_idx],
                    model_idx = model_best_lm_idx, model_candidate_count,
                    Length(context_value), Length(completions[model_idx].text));
                if residual_candidate_score > residual_best_score then
                begin
                    residual_best_idx := model_idx;
                    residual_best_score := residual_candidate_score;
                end;
            end;
            if (residual_best_idx <> best_idx) and
                (residual_best_score - residual_current_score >=
                c_completion_context_residual_margin) then
            begin
                best_idx := residual_best_idx;
            end;

        end;
    end;

    // A completion-specific pairwise calibration resolves only strong base
    // exact disagreements. It runs before hysteresis, so small score changes
    // cannot make a compatible prompt jump while the user keeps typing.
    if (context_value <> '') and has_char_lm and (best_idx >= 0) and
        (completions[best_idx].source = okcs_base_exact) then
    begin
        calibrated_best_idx := best_idx;
        calibrated_current_score := one_key_completion_calibrated_score(
            completions[best_idx].corpus_score,
            completions[best_idx].path_score,
            completions[best_idx].vertical_penalty,
            completions[best_idx].vertical_layer_kind,
            char_lm_scores[best_idx],
            get_candidate_text_unit_count(completions[best_idx].text),
            Length(syllables));
        calibrated_best_score := calibrated_current_score;
        for model_idx := 0 to High(completions) do
        begin
            if (not eligible[model_idx]) or
                (completions[model_idx].source <> okcs_base_exact) or
                (model_idx = best_idx) then
            begin
                Continue;
            end;
            calibrated_candidate_score :=
                one_key_completion_calibrated_score(
                completions[model_idx].corpus_score,
                completions[model_idx].path_score,
                completions[model_idx].vertical_penalty,
                completions[model_idx].vertical_layer_kind,
                char_lm_scores[model_idx],
                get_candidate_text_unit_count(completions[model_idx].text),
                Length(syllables));
            if calibrated_candidate_score > calibrated_best_score then
            begin
                calibrated_best_idx := model_idx;
                calibrated_best_score := calibrated_candidate_score;
            end;
        end;
        if (calibrated_best_idx <> best_idx) and
            (calibrated_best_score - calibrated_current_score >=
            c_completion_calibrated_margin) then
        begin
            best_idx := calibrated_best_idx;
        end;
    end;

    // Keep a compatible suggestion stable while the user continues typing.
    // A challenger must win by a visible margin before replacing it.
    if (previous_idx >= 0) and (previous_query_prefix <> '') and
        compact_query.StartsWith(previous_query_prefix, True) and
        completions[previous_idx].full_pinyin.StartsWith(compact_query,
        True) and (best_idx <> previous_idx) and
        (scores[best_idx] - scores[previous_idx] <
        c_completion_hysteresis_margin) then
    begin
        best_idx := previous_idx;
    end;

    // Reused short-word models can prefer a short specialist phrase merely
    // because its raw LM sum contains fewer characters. Do not let that
    // replace a better-supported general completion unless the specialist
    // phrase also wins after normalizing the context LM score by text length.
    // A relation accepted repeatedly remains an explicit user preference.
    if (context_value <> '') and has_char_lm and (best_idx >= 0) and
        (completions[best_idx].source = okcs_base_exact) and
        completions[best_idx].has_popularity_prior and
        (completions[best_idx].vertical_layer_kind = 3) and
        (completions[best_idx].popularity_prior < 300) and
        (completions[best_idx].feedback_count < 2) then
    begin
        stronger_general_idx := find_best_stronger_general_anchor(best_idx);
        if stronger_general_idx >= 0 then
        begin
            selected_lm_per_unit := char_lm_scores[best_idx] div
                Max(1, get_candidate_text_unit_count(
                completions[best_idx].text));
            general_lm_per_unit := char_lm_scores[stronger_general_idx] div
                Max(1, get_candidate_text_unit_count(
                completions[stronger_general_idx].text));
            if selected_lm_per_unit - general_lm_per_unit <
                c_completion_cold_vertical_context_lm_margin then
            begin
                best_idx := stronger_general_idx;
            end;
        end;
    end;

    // Keep every reliable transition in the internal pool, but only let it
    // replace an existing base completion when that base is demonstrably
    // cold. Apply this after hysteresis so a previous transition cannot
    // bypass the same evidence gate on the next keystroke.
    if (best_idx >= 0) and (baseline_idx >= 0) and
        (completions[best_idx].source = okcs_transition) and
        (completions[baseline_idx].source = okcs_base_exact) and
        (completions[best_idx].feedback_count <= 0) then
    begin
        transition_challenge_allowed :=
            completions[baseline_idx].has_popularity_prior and
            (((completions[baseline_idx].vertical_layer_kind > 0) and
            (completions[baseline_idx].popularity_prior < 300)) or
            (completions[baseline_idx].popularity_prior < 120));
        if not transition_challenge_allowed then
        begin
            best_idx := baseline_idx;
        end;
    end;

    // Resolve the settled Top-K in one completion-specific comparison stage.
    // The model was trained on benchmark-excluded novel/chat candidate dumps
    // and can abstain independently for hot exact words, cold vertical words,
    // and transition candidates. It never changes the candidate pool.
    if (context_value <> '') and has_char_lm and (best_idx >= 0) and
        (eligible_count > 1) then
    begin
        difference_best_idx := -1;
        difference_best_score := -MaxDouble;
        for difference_idx := 0 to High(completions) do
        begin
            if (difference_idx = best_idx) or
                (not eligible[difference_idx]) then
            begin
                Continue;
            end;
            // An explicitly accepted relation is only challenged by an equal
            // or stronger relation-local preference.
            if completions[best_idx].feedback_count >
                completions[difference_idx].feedback_count then
            begin
                Continue;
            end;
            difference_score := one_key_completion_difference_score(
                context_value, compact_query, completions[difference_idx],
                completions[best_idx], char_lm_scores[difference_idx],
                char_lm_scores[best_idx], Length(syllables));
            if (difference_best_idx < 0) or
                (difference_score > difference_best_score) then
            begin
                difference_best_idx := difference_idx;
                difference_best_score := difference_score;
            end;
        end;
        if difference_best_idx >= 0 then
        begin
            difference_category := one_key_completion_difference_category(
                completions[best_idx], completions[difference_best_idx]);
            difference_threshold := one_key_completion_difference_threshold(
                difference_category);
            if difference_best_score >= difference_threshold then
            begin
                // The previous prompt already survived the ordinary
                // hysteresis stage. A learned challenger needs a little extra
                // confidence to replace it on the next key.
                if (best_idx <> previous_idx) or
                    (difference_best_score >= difference_threshold +
                    c_completion_difference_hysteresis_margin) then
                begin
                    best_idx := difference_best_idx;
                end;
            end;
        end;
    end;

    // The final completion-only listwise model sees the settled internal
    // Top-K as one ranking group. It was trained on independent novel, chat,
    // and general Chinese corpora and uses conservative ambiguity-specific
    // abstention thresholds. No candidates are generated at this stage.
    if (context_value <> '') and has_char_lm and (best_idx >= 0) and
        (eligible_count > 1) then
    begin
        topk_best_idx := best_idx;
        topk_current_score := one_key_completion_topk_score(
            context_value, compact_query, completions[best_idx],
            char_lm_scores[best_idx], Length(syllables), best_idx + 1);
        topk_best_score := topk_current_score;
        for topk_idx := 0 to High(completions) do
        begin
            if (topk_idx = best_idx) or (not eligible[topk_idx]) then
            begin
                Continue;
            end;
            if completions[best_idx].feedback_count >
                completions[topk_idx].feedback_count then
            begin
                Continue;
            end;
            topk_score := one_key_completion_topk_score(
                context_value, compact_query, completions[topk_idx],
                char_lm_scores[topk_idx], Length(syllables), topk_idx + 1);
            if topk_score > topk_best_score then
            begin
                topk_best_idx := topk_idx;
                topk_best_score := topk_score;
            end;
        end;
        if topk_best_idx <> best_idx then
        begin
            topk_category := one_key_completion_difference_category(
                completions[best_idx], completions[topk_best_idx]);
            topk_threshold := one_key_completion_topk_threshold(topk_category);
            if (best_idx = previous_idx) and (previous_idx >= 0) then
            begin
                topk_threshold := topk_threshold +
                    c_completion_topk_hysteresis_margin;
            end;
            if topk_best_score - topk_current_score >= topk_threshold then
            begin
                best_idx := topk_best_idx;
            end;
        end;
    end;

    // best_idx may have changed through the baseline guard or hysteresis.
    // Recompute its actual runner-up before applying ambiguity abstention.
    second_idx := -1;
    for idx := 0 to High(completions) do
    begin
        if (idx = best_idx) or (not eligible[idx]) then
        begin
            Continue;
        end;
        if (second_idx < 0) or (scores[idx] > scores[second_idx]) or
            ((scores[idx] = scores[second_idx]) and
            (CompareText(completions[idx].text,
            completions[second_idx].text) < 0)) then
        begin
            second_idx := idx;
        end;
    end;

    // Transition completions are deliberately conservative: when their lead
    // is ambiguous, retain the best base completion or abstain if no base
    // alternative exists. User feedback has already been handled above.
    if (second_idx >= 0) and
        (completions[best_idx].source = okcs_transition) and
        (best_idx <> previous_idx) then
    begin
        if context_value = '' then
        begin
            required_margin := c_completion_no_context_margin;
        end
        else
        begin
            required_margin := c_completion_context_margin;
        end;
        top_margin := scores[best_idx] - scores[second_idx];
        if (top_margin < required_margin) and
            (completions[best_idx].feedback_count <= 0) then
        begin
            if (baseline_idx >= 0) and
                (completions[baseline_idx].source = okcs_base_exact) then
            begin
                best_idx := baseline_idx;
            end
            else
            begin
                Exit;
            end;
        end;
    end;

    // Audit the final choice against corpus evidence keyed by the actual
    // context suffix, typed prefix, completion spelling, and completion text.
    // The most specific available context is decisive: weak evidence abstains
    // instead of falling through to a broader prior. Only the settled internal
    // Top-5 is considered, so this stage cannot generate or expose candidates.
    if (context_value <> '') and (best_idx >= 0) and (eligible_count > 1) and
        m_dictionary.lookup_one_key_completion_competition(compact_query,
        context_value, competition_evidence) then
    begin
        for competition_width := 4 downto 0 do
        begin
            competition_has_evidence := False;
            competition_best_idx := -1;
            competition_best_score := 0;
            competition_candidate_count := 0;
            for competition_candidate_idx := 0 to High(completions) do
            begin
                if not eligible[competition_candidate_idx] then
                begin
                    Continue;
                end;
                Inc(competition_candidate_count);
                if competition_candidate_count >
                    c_completion_competition_top_k then
                begin
                    Break;
                end;
                if not competition_score_for_candidate(
                    competition_candidate_idx, competition_width,
                    competition_candidate_score) then
                begin
                    Continue;
                end;
                competition_has_evidence := True;
                if (competition_best_idx < 0) or
                    (competition_candidate_score > competition_best_score) or
                    ((competition_candidate_score = competition_best_score) and
                    (competition_candidate_idx < competition_best_idx)) then
                begin
                    competition_best_idx := competition_candidate_idx;
                    competition_best_score := competition_candidate_score;
                end;
            end;
            if not competition_has_evidence then
            begin
                Continue;
            end;

            competition_current_score := 0;
            competition_score_for_candidate(best_idx, competition_width,
                competition_current_score);
            if (competition_best_idx = best_idx) or
                (competition_best_idx < 0) or
                (completions[best_idx].feedback_count >
                completions[competition_best_idx].feedback_count) then
            begin
                Break;
            end;

            case competition_width of
                2, 3, 4:
                begin
                    competition_margin :=
                        c_completion_competition_score_scale div 2;
                    competition_ratio_left := 4;
                    competition_ratio_right := 5;
                end;
                1:
                begin
                    competition_margin :=
                        c_completion_competition_score_scale;
                    competition_ratio_left := 2;
                    competition_ratio_right := 3;
                end;
            else
                competition_margin :=
                    c_completion_competition_score_scale * 2;
                competition_ratio_left := 4;
                competition_ratio_right := 5;
            end;
            if (competition_best_score - competition_current_score >=
                competition_margin) and
                ((competition_best_score +
                c_completion_competition_score_scale div 4) *
                competition_ratio_left >=
                (competition_current_score +
                c_completion_competition_score_scale div 4) *
                competition_ratio_right) then
            begin
                best_idx := competition_best_idx;
            end;
            Break;
        end;
    end;

    // The final pair audit compares the settled choice with the existing
    // internal Top-5. It can switch only when independently trained evidence
    // strongly prefers that exact ordered pair in the matching context. It
    // never generates a candidate and never overrides a stronger user choice.
    if (best_idx >= 0) and (eligible_count > 1) then
    begin
        pair_best_idx := -1;
        pair_best_width := -1;
        pair_best_confidence := -1;
        pair_best_observations := -1;
        pair_candidate_count := 0;
        for pair_candidate_idx := 0 to High(completions) do
        begin
            if not eligible[pair_candidate_idx] then
            begin
                Continue;
            end;
            Inc(pair_candidate_count);
            if pair_candidate_count > c_completion_competition_top_k then
            begin
                Break;
            end;
            if (pair_candidate_idx = best_idx) or
                (completions[best_idx].feedback_count >
                completions[pair_candidate_idx].feedback_count) or
                (not m_dictionary.lookup_one_key_completion_pair_audit(
                compact_query, context_value,
                completions[best_idx].full_pinyin,
                completions[best_idx].text,
                completions[pair_candidate_idx].full_pinyin,
                completions[pair_candidate_idx].text, pair_audit)) or
                (pair_audit.decision <> -1) then
            begin
                Continue;
            end;
            pair_confidence := 1000 - pair_audit.confidence_milli;
            pair_observations := pair_audit.keep_count +
                pair_audit.switch_count;
            if (pair_best_idx < 0) or
                (pair_audit.context_width > pair_best_width) or
                ((pair_audit.context_width = pair_best_width) and
                (pair_confidence > pair_best_confidence)) or
                ((pair_audit.context_width = pair_best_width) and
                (pair_confidence = pair_best_confidence) and
                (pair_observations > pair_best_observations)) then
            begin
                pair_best_idx := pair_candidate_idx;
                pair_best_width := pair_audit.context_width;
                pair_best_confidence := pair_confidence;
                pair_best_observations := pair_observations;
            end;
        end;
        if pair_best_idx >= 0 then
        begin
            best_idx := pair_best_idx;
        end;
    end;

    // nc_gpt is used only offline. The generated trees and sparse priors
    // below are static distillations of its hard-pair decisions. The primary
    // prior audits the tree choice; a stricter fallback may rescue another
    // existing Top-K item only when the primary path keeps the incumbent.
    if (best_idx >= 0) and (eligible_count > 1) and
        prepare_ncgpt_completion_features then
    begin
        ncgpt_incumbent_idx := best_idx;
        ncgpt_best_idx := -1;
        ncgpt_best_score := -1.0E30;
        ncgpt_primary_idx := -1;
        ncgpt_primary_score := -1.0E30;
        SetLength(ncgpt_candidate_scores, Length(completions));
        SetLength(ncgpt_candidate_thresholds, Length(completions));
        SetLength(ncgpt_candidate_categories, Length(completions));
        SetLength(ncgpt_candidate_scored, Length(completions));
        for ncgpt_candidate_idx := 0 to High(completions) do
        begin
            if (ncgpt_candidate_idx = ncgpt_incumbent_idx) or
                (not eligible[ncgpt_candidate_idx]) or
                (completions[ncgpt_incumbent_idx].feedback_count >
                completions[ncgpt_candidate_idx].feedback_count) then
            begin
                Continue;
            end;
            ncgpt_category := one_key_completion_difference_category(
                completions[ncgpt_incumbent_idx],
                completions[ncgpt_candidate_idx]);
            ncgpt_threshold := one_key_completion_ncgpt_threshold(
                ncgpt_category);
            if ncgpt_threshold >= 1.0E29 then
            begin
                Continue;
            end;
            get_common_completion_fragment_units(
                completions[ncgpt_incumbent_idx].text,
                completions[ncgpt_candidate_idx].text,
                ncgpt_common_prefix, ncgpt_common_suffix);
            build_one_key_completion_ncgpt_pair_features(
                ncgpt_candidate_features[ncgpt_candidate_idx],
                ncgpt_candidate_features[ncgpt_incumbent_idx],
                ncgpt_common_prefix, ncgpt_common_suffix,
                get_candidate_text_unit_count(
                completions[ncgpt_candidate_idx].text) -
                get_candidate_text_unit_count(
                completions[ncgpt_incumbent_idx].text),
                Length(completions[ncgpt_candidate_idx].full_pinyin) -
                Length(completions[ncgpt_incumbent_idx].full_pinyin),
                completions[ncgpt_candidate_idx].source =
                completions[ncgpt_incumbent_idx].source,
                (completions[ncgpt_candidate_idx].source =
                okcs_base_exact) and
                (completions[ncgpt_incumbent_idx].source = okcs_transition),
                (completions[ncgpt_candidate_idx].source =
                okcs_transition) and
                (completions[ncgpt_incumbent_idx].source = okcs_base_exact),
                ncgpt_pair_features);
            ncgpt_candidate_score := one_key_completion_ncgpt_score(
                ncgpt_pair_features);
            ncgpt_candidate_scores[ncgpt_candidate_idx] :=
                ncgpt_candidate_score;
            ncgpt_candidate_thresholds[ncgpt_candidate_idx] :=
                ncgpt_threshold;
            ncgpt_candidate_categories[ncgpt_candidate_idx] :=
                ncgpt_category;
            ncgpt_candidate_scored[ncgpt_candidate_idx] := True;
            if (ncgpt_primary_idx < 0) or
                (ncgpt_candidate_score > ncgpt_primary_score) or
                ((ncgpt_candidate_score = ncgpt_primary_score) and
                (ncgpt_candidate_idx < ncgpt_primary_idx)) then
            begin
                ncgpt_primary_idx := ncgpt_candidate_idx;
                ncgpt_primary_score := ncgpt_candidate_score;
                ncgpt_primary_threshold := ncgpt_threshold;
                ncgpt_primary_category := ncgpt_category;
            end;
            if (ncgpt_candidate_score >= ncgpt_threshold) and
                ((ncgpt_best_idx < 0) or
                (ncgpt_candidate_score > ncgpt_best_score) or
                ((ncgpt_candidate_score = ncgpt_best_score) and
                (ncgpt_candidate_idx < ncgpt_best_idx))) then
            begin
                ncgpt_best_idx := ncgpt_candidate_idx;
                ncgpt_best_score := ncgpt_candidate_score;
            end;
        end;
        if ncgpt_best_idx >= 0 then
        begin
            best_idx := ncgpt_best_idx;
        end
        else
        begin
            best_idx := ncgpt_incumbent_idx;
        end;

        if (ncgpt_primary_idx >= 0) and
            lookup_one_key_completion_ncgpt_primary_prior(
            context_value, compact_query,
            completions[ncgpt_incumbent_idx],
            completions[ncgpt_primary_idx], ncgpt_primary_category,
            ncgpt_primary_score, ncgpt_primary_threshold,
            ncgpt_prior_score) then
        begin
            if (ncgpt_prior_score >=
                one_key_completion_ncgpt_primary_approve_threshold(
                ncgpt_primary_category)) and
                (ncgpt_primary_score >= ncgpt_primary_threshold -
                c_one_key_completion_ncgpt_primary_expansion_margin) then
            begin
                best_idx := ncgpt_primary_idx;
            end
            else if (best_idx = ncgpt_primary_idx) and
                (ncgpt_prior_score <=
                one_key_completion_ncgpt_primary_veto_threshold(
                ncgpt_primary_category)) then
            begin
                best_idx := ncgpt_incumbent_idx;
            end;
        end;

        if best_idx = ncgpt_incumbent_idx then
        begin
            ncgpt_fallback_idx := -1;
            ncgpt_fallback_score := -1.0E30;
            for ncgpt_candidate_idx := 0 to High(completions) do
            begin
                if (not ncgpt_candidate_scored[ncgpt_candidate_idx]) or
                    (ncgpt_candidate_scores[ncgpt_candidate_idx] <
                    ncgpt_candidate_thresholds[ncgpt_candidate_idx] -
                    c_one_key_completion_ncgpt_fallback_expansion_margin) or
                    (not lookup_one_key_completion_ncgpt_fallback_prior(
                    context_value, compact_query,
                    completions[ncgpt_incumbent_idx],
                    completions[ncgpt_candidate_idx],
                    ncgpt_candidate_categories[ncgpt_candidate_idx],
                    ncgpt_candidate_scores[ncgpt_candidate_idx],
                    ncgpt_candidate_thresholds[ncgpt_candidate_idx],
                    ncgpt_prior_score)) or (ncgpt_prior_score <= 0.0) then
                begin
                    Continue;
                end;
                ncgpt_fallback_candidate_score := ncgpt_prior_score +
                    c_one_key_completion_ncgpt_fallback_tree_weight *
                    (ncgpt_candidate_scores[ncgpt_candidate_idx] -
                    ncgpt_candidate_thresholds[ncgpt_candidate_idx]);
                if (ncgpt_fallback_idx < 0) or
                    (ncgpt_fallback_candidate_score >
                    ncgpt_fallback_score) or
                    ((ncgpt_fallback_candidate_score =
                    ncgpt_fallback_score) and
                    (ncgpt_candidate_scores[ncgpt_candidate_idx] >
                    ncgpt_candidate_scores[ncgpt_fallback_idx])) or
                    ((ncgpt_fallback_candidate_score =
                    ncgpt_fallback_score) and
                    (ncgpt_candidate_scores[ncgpt_candidate_idx] =
                    ncgpt_candidate_scores[ncgpt_fallback_idx]) and
                    (ncgpt_candidate_idx < ncgpt_fallback_idx)) then
                begin
                    ncgpt_fallback_idx := ncgpt_candidate_idx;
                    ncgpt_fallback_score :=
                        ncgpt_fallback_candidate_score;
                end;
            end;
            if (ncgpt_fallback_idx >= 0) and
                (ncgpt_fallback_score >=
                c_one_key_completion_ncgpt_fallback_threshold) then
            begin
                best_idx := ncgpt_fallback_idx;
            end;
        end;
    end;

    m_one_key_completion := completions[best_idx];
    m_one_key_completion_query_prefix := compact_query;
    m_one_key_completion_score := scores[best_idx];
    apply_long_completion;
end;

function TncEngine.get_one_key_completion_for_commit: TncOneKeyCompletion;
var document_key: string;
begin
    document_key := '';
    if m_document_context_model <> nil then document_key := m_document_context_model.document_key;
    // A refreshed context must not change the text of an already shown Tab hint.
    // Keep this snapshot separate from raw ranking and feedback state.
    if (m_tab_projection_shown.text <> '') and (m_config.input_mode = im_chinese) and
        (m_page_index = 0) and (m_tab_projection_query = m_composition_text) and
        (m_tab_projection_document = document_key) and
        (m_tab_projection_raw_text = m_one_key_completion.text) and
        (m_tab_projection_raw_path = m_one_key_completion.path_text) and
        (m_tab_projection_shown.full_pinyin = m_one_key_completion.full_pinyin) and
        (m_tab_projection_shown.anchor_path = m_one_key_completion.anchor_path) and
        (m_tab_projection_shown.suffix_text = m_one_key_completion.suffix_text) and
        (m_tab_projection_shown.source = m_one_key_completion.source) then
        Exit(m_tab_projection_shown);
    Result := project_validated_prefix_completion(m_one_key_completion);
end;

function TncEngine.accept_one_key_completion: Boolean;
var
    completion_text: string;
    completion_pinyin: string;
    completion: TncOneKeyCompletion;
begin
    completion := get_one_key_completion_for_commit;
    completion_text := Trim(completion.text);
    completion_pinyin := normalize_pinyin_text(
        completion.full_pinyin);
    Result := completion_text <> '';
    if not Result then
    begin
        Exit;
    end;

    if (m_one_key_completion.source = okcs_document_copy) and
        (m_document_context_model <> nil) and
        (m_one_key_completion.anchor_path <> '') and
        (m_one_key_completion.suffix_text <> '') then
    begin
        m_document_context_model.record_completion_feedback(
            m_one_key_completion.anchor_path,
            m_one_key_completion.suffix_text, True);
        clear_one_key_completion_feedback_target;
    end
    else if (m_dictionary <> nil) and
        (m_one_key_completion.source in
        [okcs_long_transition, okcs_long_neural]) and
        (m_one_key_completion.anchor_path <> '') and
        (m_one_key_completion.suffix_text <> '') then
    begin
        m_dictionary.record_long_one_key_completion_accept(
            m_one_key_completion.anchor_path,
            m_one_key_completion.suffix_text);
        clear_one_key_completion_feedback_target;
    end
    else if (m_dictionary <> nil) and
        (not (m_one_key_completion.source in
        [okcs_long_transition, okcs_long_neural])) and
        (m_one_key_completion_query_prefix <> '') and
        (completion_pinyin <> '') then
    begin
        m_dictionary.record_one_key_completion_accept(
            m_one_key_completion_query_prefix, completion_pinyin,
            completion_text);
    end;
    if m_one_key_completion.source in
        [okcs_long_transition, okcs_long_neural,
        okcs_document_copy] then
    begin
        clear_one_key_completion_feedback_target;
    end
    else if SameText(m_completion_feedback_origin_prefix,
        m_one_key_completion_query_prefix) then
    begin
        clear_one_key_completion_feedback_target;
    end
    else
    begin
        record_one_key_completion_feedback_target(completion_pinyin,
            completion_text);
    end;

    // The prefix is intentionally incomplete, so never persist it as the
    // pronunciation of the completed lexicon word. Keep the real full pinyin
    // for later context bookkeeping instead of associating the text with the
    // typed prefix.
    set_pending_commit(completion_text, '', False, '', True, '', False,
        False, '', True);
    if completion_pinyin <> '' then
    begin
        m_pending_commit_query_key := completion_pinyin;
    end;
    clear_one_key_completion;
end;

procedure TncEngine.build_candidates;
const
    c_completion_emergency_skip_ms = 250;
var
    build_started_at: UInt64;
begin
    build_started_at := nc_monotonic_tick_ms;
    build_candidates_core;

    // Candidate visibility is more important than the optional Tab hint during
    // real incremental typing. Direct whole-query evaluation uses fixed work so
    // that machine load cannot change later cases in the same benchmark run.
    if (m_search_budget_mode = sbm_production) and
        m_composition_built_incrementally and
        (nc_monotonic_tick_ms - build_started_at >= c_completion_emergency_skip_ms) then
    begin
        clear_one_key_completion;
        Exit;
    end;
    refresh_one_key_completion;
end;

{$IFDEF FPC}
{$OPTIMIZATION NOCSE}
{$ENDIF}
procedure TncEngine.build_candidates_core;
const
    c_long_prefast_catchup_char_lm_threshold = -6500;
type
    TncSubspanTextOptions = TArray<string>;
    TncSubspanWeightOptions = TArray<Integer>;
    TncSubspanTextOptionRow = TArray<TncSubspanTextOptions>;
    TncSubspanWeightOptionRow = TArray<TncSubspanWeightOptions>;
var
    raw_candidates: TncCandidateList;
    literal_user_candidates: TncCandidateList;
    segment_candidates: TncCandidateList;
    full_lookup_candidates: TncCandidateList;
    lookup_cache: TDictionary<string, TncCandidateList>;
    preferred_exact_candidate_cache: TDictionary<string, TncCandidate>;
    preferred_exact_syllable_count_cache: TDictionary<string, Integer>;
    preferred_exact_state_cache: TDictionary<string, Byte>;
    preferred_query_phrase_bonus_cache: TDictionary<string, Integer>;
    preferred_query_phrase_text_cache: TDictionary<string, string>;
    preferred_query_phrase_text_bonus_cache: TDictionary<string, Integer>;
    learned_query_choice_override_cache: TDictionary<string, Byte>;
    stronger_query_choice_competitor_cache: TDictionary<string, Byte>;
    limit: Integer;
    fallback_comment: string;
    has_raw_candidates: Boolean;
    has_segment_candidates: Boolean;
    raw_from_dictionary: Boolean;
    trailing_prefix_rescue_applied: Boolean;
    has_strong_long_sentence_prediction: Boolean;
    final_has_strong_long_sentence_prediction: Boolean;
    lookup_text: string;
    has_multi_syllable_input: Boolean;
    has_internal_dangling_initial: Boolean;
    has_safe_trailing_initial_typing_state: Boolean;
    has_raw_safe_trailing_initial_typing_state: Boolean;
    all_initial_compact_query: Boolean;
    head_only_multi_syllable: Boolean;
    input_syllable_count: Integer;
    all_initial_tail: string;
    interactive_head_key: string;
    interactive_query_text: string;
    interactive_tail_key: string;
    interactive_best_idx: Integer;
    interactive_best_score: Integer;
    interactive_score: Integer;
    interactive_fixed_head_pinyin: string;
    interactive_fixed_head_text: string;
    interactive_fixed_tail_key: string;
    interactive_fixed_tail_units: Integer;
    interactive_tail_candidates: TncCandidateList;
    interactive_tail_text: string;
    interactive_best_tail_text: string;
    interactive_tail_score: Integer;
    interactive_head_candidates: TncCandidateList;
    interactive_preserved_candidate: TncCandidate;
    previous_lookup_key: string;
    previous_candidates: TncCandidateList;
    previous_visible_candidates: TncCandidateList;
    previous_short_three_exact_pair_query: string;
    previous_short_three_exact_pair_texts: TArray<string>;
    short_four_preserved_prefix_candidate: TncCandidate;
    incremental_partial_candidates: TncCandidateList;
    incremental_partial_reuse_applied: Boolean;
    multi_syllable_cap_limit: Integer;
    normalized_lookup_text: string;
    repeated_two_syllable_query: Boolean;
    single_char_partial_min_count: Integer;
    runtime_phrase_added: Boolean;
    runtime_redup_added: Boolean;
    raw_candidates_seeded_from_runtime_only: Boolean;
    relaxed_missing_apostrophe_comment: string;
    has_relaxed_missing_apostrophe_partial: Boolean;
    has_relaxed_missing_apostrophe_boundary_shift: Boolean;
    lookup_cache_hits: Integer;
    lookup_cache_misses: Integer;
    lookup_elapsed_ms: Int64;
    segment_elapsed_ms: Int64;
    runtime_elapsed_ms: Int64;
    post_elapsed_ms: Int64;
    sort_elapsed_ms: Int64;
    path_search_elapsed_ms: Int64;
    short4_has_strong_exact: Boolean;
    short_four_model_evidence_applied: Boolean;
    short4_prefix_candidates_ready: Boolean;
    short4_is_particle_tail: Boolean;
    short4_tail_key: string;
    short4_tail_text: string;
    short4_head_key: string;
    short4_standalone_particle_tail: Boolean;
    short4_tail_syllables: TncPinyinParseResult;
    short4_tail_unit_key: string;
    total_start_tick: UInt64;
    phase_start_tick: UInt64;
    allow_relaxed_missing_apostrophe: Boolean;
    compact_runtime_candidate: TncCandidate;
    direct_preferred_candidate: TncCandidate;
    typo_exact_candidate: TncCandidate;
    has_long_direct_preferred_candidate: Boolean;
    direct_chain_candidate: TncCandidate;
    direct_chain_encoded_path: string;
    top_complete_chain_candidates: TncCandidateList;
    top_complete_chain_encoded_paths: TArray<string>;
    top_complete_chain_applied: Boolean;
    stable_prefix_candidate: TncCandidate;
    head_tail_complete_candidate: TncCandidate;
    head_tail_complete_encoded_path: string;
    supported_complete_candidate: TncCandidate;
    supported_complete_encoded_path: string;
    direct_three_syllable_one_plus_two_candidate: TncCandidate;
    direct_three_syllable_one_plus_two_encoded_path: string;
    direct_three_syllable_one_plus_two_registered: Boolean;
    direct_three_syllable_two_plus_one_candidate: TncCandidate;
    direct_three_syllable_two_plus_one_encoded_path: string;
    fast_chain_seed_candidate: TncCandidate;
    fast_chain_complete_candidate: TncCandidate;
    has_stable_prefix_candidate: Boolean;
    stable_prefix_candidate_evaluated: Boolean;
    ranked_directional_repair_applied: Boolean;
    fragment_prefix_candidate: TncCandidate;
    head_partial_candidate: TncCandidate;
    short_exact_preserved_candidate: TncCandidate;
    has_short_exact_preserved_candidate: Boolean;
    has_long_direct_chain_candidate: Boolean;
    compact_runtime_candidates: TncCandidateList;
    relaxed_segment_candidates: TncCandidateList;
    explicit_apostrophe_aligned_candidates: TncCandidateList;
    explicit_apostrophe_exact_candidates: TncCandidateList;
    lightweight_sentence_candidates: TncCandidateList;
    exact_lattice_backup_candidates: TncCandidateList;
    strict_oracle_sentence_candidates: TncCandidateList;
    explicit_apostrophe_prefix_candidates: TncCandidateList;
    has_explicit_apostrophe_input: Boolean;
    explicit_apostrophe_lookup_text: string;
    confirmed_prefix_boundary_partial_preferred: Boolean;
    explicit_apostrophe_query_syllables: TncPinyinParseResult;
    explicit_apostrophe_query_parsed: Boolean;
    explicit_apostrophe_exact_candidates_loaded: Boolean;
    lightweight_sentence_applied: Boolean;
    delayed_long_decode_mode: Boolean;
    ultra_long_partial_shortfast_applied: Boolean;
    normalized_typo_long_query_fast_path: Boolean;
    short_dangling_prefix_query: Boolean;
    short_full_exact_query: Boolean;
    long_full_exact_query: Boolean;
    latest_query_choice_text: string;
    exact_head_partial_candidates: TncCandidateList;
    fixed_wo_tail_candidates: TncCandidateList;
    short_particle_tail_candidates: TncCandidateList;
    full_query_exact_promoted: Boolean;
    exact_subspan_cache_prepared: Boolean;
    exact_subspan_cache_syllables: TncPinyinParseResult;
    exact_subspan_cache_states: TArray<TArray<Byte>>;
    exact_subspan_cache_scores: TArray<TArray<Integer>>;
    exact_subspan_cache_segments: TArray<TArray<Integer>>;
    exact_subspan_cache_anchor_units: TArray<TArray<Integer>>;
    exact_subspan_cache_has_anchor: TArray<TArray<Boolean>>;
    exact_subspan_cache_texts: TArray<TArray<string>>;
    exact_subspan_cache_paths: TArray<TArray<string>>;
    exact_subspan_single_cache_prepared: TArray<Boolean>;
    exact_subspan_single_cache_texts: TArray<TArray<string>>;
    exact_subspan_single_cache_weights: TArray<TArray<Integer>>;
    exact_subspan_fixed_weight_cache_states: TArray<Byte>;
    exact_subspan_fixed_weight_cache_values: TArray<Integer>;
    exact_subspan_chunk_cache_prepared: TArray<TArray<Boolean>>;
    exact_subspan_chunk_cache_texts: TArray<TncSubspanTextOptionRow>;
    exact_subspan_chunk_cache_weights: TArray<TncSubspanWeightOptionRow>;
    subspan_oracle_states: TDictionary<string, Integer>;
    subspan_oracle_texts: TDictionary<string, string>;
    subspan_oracle_scores: TDictionary<string, Integer>;
    subspan_oracle_engine: TncEngine;
    subspan_oracle_config: TncEngineConfig;
    subspan_oracle_budget_remaining: Integer;
    subquery_sentence_promoted: Boolean;
    subquery_sentence_missed: Boolean;
    strict_oracle_sentence_built: Boolean;
    strict_oracle_sentence_ok: Boolean;
    resolved_sentence_candidate_built: Boolean;
    resolved_sentence_candidate_ok: Boolean;
    resolved_sentence_candidate_cached: TncCandidate;
    resolved_sentence_path_cached: string;
    ctxalt_miss_debug_count: Integer;
    extendable_same_tail_cache_valid: Boolean;
    extendable_same_tail_cache_key: string;
    extendable_same_tail_cache_expected_count: Integer;
    extendable_same_tail_cache_value: Boolean;
    short_extendable_tail_cache_valid: Boolean;
    short_extendable_tail_cache_value: Boolean;
    skip_three_syllable_contextual_completion: Boolean;
    long_path_exact_lookup_filter_depth: Integer;
    ultra_long_exact_chunk_chain_miss_cached: Boolean;
    prefast_char_lm_texts: TArray<string>;
    prefast_char_lm_scores: TArray<Integer>;
    short_two_single_pair_prepared: Boolean;
    short_two_single_pair_candidates: TncCandidateList;
    short_two_single_pair_paths: TArray<string>;
    short_two_single_pair_rank_scores: TArray<Integer>;
    short_three_exact_pair_prepared: Boolean;
    short_three_exact_pair_candidates: TncCandidateList;
    short_three_exact_pair_paths: TArray<string>;
    short_three_exact_pair_rank_scores: TArray<Integer>;

    function get_sentence_text_prefix_bonus_local(
        const prefix_text: string): Integer;
    begin
        Result := 0;
        if m_dictionary = nil then
        begin
            Exit;
        end;
        Result := m_dictionary.get_base_text_prefix_bonus(prefix_text);
    end;

    procedure clear_candidate_comments(var candidates: TncCandidateList);
    var
        idx: Integer;
    begin
        for idx := 0 to High(candidates) do
        begin
            candidates[idx].comment := '';
        end;
    end;

    procedure promote_longest_partial_prefix_candidate_local(
        var candidates: TncCandidateList);
    var
        idx: Integer;
        best_idx: Integer;
        best_units: Integer;
        units: Integer;
        picked: TncCandidate;
    begin
        best_idx := -1;
        best_units := 0;
        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) = '' then
            begin
                Continue;
            end;

            units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if (units < 2) or (units < best_units) then
            begin
                Continue;
            end;
            if (units = best_units) and (best_idx >= 0) and
                (candidates[idx].score <= candidates[best_idx].score) then
            begin
                Continue;
            end;

            best_idx := idx;
            best_units := units;
        end;

        if best_idx <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_idx];
        while best_idx > 0 do
        begin
            candidates[best_idx] := candidates[best_idx - 1];
            Dec(best_idx);
        end;
        candidates[0] := picked;
    end;

    procedure finalize_lookup_timing_info; forward;
    procedure finalize_modal_tail_sentence_candidate(
        var candidates: TncCandidateList); forward;
    function build_strict_chunk_oracle_sentence_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean; forward;
    function should_trim_strong_long_sentence_candidates_local(
        const candidates: TncCandidateList): Boolean; forward;
    procedure trim_sorted_candidates_for_strong_long_sentence(
        var candidates: TncCandidateList); forward;
    procedure sort_candidates_lightweight(
        var candidates: TncCandidateList); forward;
    procedure ensure_incomplete_tail_partial_precedes_completed_extension(
        var candidates: TncCandidateList); forward;
    procedure filter_self_composed_complete_candidates_when_normal_complete_exists; forward;
    procedure filter_short_query_nonlexicon_complete_noise(
        var candidates: TncCandidateList); forward;
    function build_exact_leading_prefix_partial_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean; forward;
    function build_fuzzy_short_query_recall_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean; forward;
    function top_complete_candidate_is_reliable_local(
        const candidates: TncCandidateList): Boolean; forward;
    function has_extendable_trailing_prefix_query_text(
        const pinyin_key: string): Boolean; forward;
    procedure ensure_best_fixed_boundary_exact_chain_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_simple_fixed_boundary_chain_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_short_full_query_exact_cluster_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_high_quality_complete_candidate_in_top2(
        var candidates: TncCandidateList); forward;
    procedure ensure_bounded_single_char_partial_pool(
        var candidates: TncCandidateList); forward;
    procedure ensure_best_first_syllable_single_partial_visible(
        var candidates: TncCandidateList; const requested_target_index: Integer); forward;
    procedure ensure_top_complete_candidate_prefix_partials_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_long_sentence_prefix_partial_visible(
        var candidates: TncCandidateList; const visible_limit: Integer); forward;
    procedure demote_low_evidence_admin_place_alias_top_local(
        var candidates: TncCandidateList); forward;
    procedure filter_non_bmp_single_char_candidates(
        var candidates: TncCandidateList); forward;
    procedure ensure_two_syllable_quantity_phrase_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_multi_char_partial_precedes_single_char_partial(
        var candidates: TncCandidateList); forward;
    function dictionary_lookup_cached(const pinyin_key: string;
        out out_results: TncCandidateList): Boolean; forward;

    function lookup_exact_full_pinyin_cached_local(const pinyin_key: string;
        out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
        cached_results: TncCandidateList;
        phase_tick: UInt64;

        procedure filter_visibility_only_path_entries_local;
        var
            read_idx: Integer;
            write_idx: Integer;
        begin
            if long_path_exact_lookup_filter_depth <= 0 then
            begin
                Exit;
            end;

            write_idx := 0;
            for read_idx := 0 to High(out_results) do
            begin
                if not long_exact_chunk_weight_is_eligible(
                    out_results[read_idx].dict_weight,
                    out_results[read_idx].has_dict_weight) then
                begin
                    Continue;
                end;
                if write_idx <> read_idx then
                begin
                    out_results[write_idx] := out_results[read_idx];
                end;
                Inc(write_idx);
            end;
            SetLength(out_results, write_idx);
        end;

    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        cache_key := '#exact#' + pinyin_key;
        if (lookup_cache <> nil) and lookup_cache.TryGetValue(cache_key,
            cached_results) then
        begin
            Inc(lookup_cache_hits);
            out_results := Copy(cached_results, 0, Length(cached_results));
            filter_visibility_only_path_entries_local;
            Exit(Length(out_results) > 0);
        end;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, cached_results) then
        begin
            Inc(lookup_cache_hits);
            out_results := Copy(cached_results, 0, Length(cached_results));
            filter_visibility_only_path_entries_local;
            if lookup_cache <> nil then
            begin
                lookup_cache.AddOrSetValue(cache_key,
                    Copy(cached_results, 0, Length(cached_results)));
            end;
            Exit(Length(out_results) > 0);
        end;

        Inc(lookup_cache_misses);
        phase_tick := nc_monotonic_tick_ms;
        if not m_dictionary.lookup_exact_full_pinyin(pinyin_key,
            out_results) then
        begin
            SetLength(out_results, 0);
        end;
        Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_tick));
        if lookup_cache <> nil then
        begin
            lookup_cache.AddOrSetValue(cache_key,
                Copy(out_results, 0, Length(out_results)));
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key,
                Copy(out_results, 0, Length(out_results)));
        end;
        filter_visibility_only_path_entries_local;
        Result := Length(out_results) > 0;
    end;

    function direct_exact_lookup_has_text_local(const pinyin_key: string;
        const target_text: string; const target_units: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (pinyin_key = '') or
            (target_text = '') or
            (not lookup_exact_full_pinyin_cached_local(pinyin_key,
            exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if not long_exact_chunk_weight_is_eligible(
                exact_results_local[exact_idx_local].dict_weight,
                exact_results_local[exact_idx_local].has_dict_weight) then
            begin
                Continue;
            end;
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                SameText(exact_text_local, target_text) and
                (get_candidate_text_unit_count(exact_text_local) =
                target_units) then
            begin
                Exit(True);
            end;
        end;
    end;

    function direct_base_exact_lookup_has_text_local(const pinyin_key: string;
        const target_text: string; const target_units: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (pinyin_key = '') or
            (target_text = '') or
            (not lookup_exact_full_pinyin_cached_local(pinyin_key,
            exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (exact_results_local[exact_idx_local].source = cs_rule) and
                (Trim(exact_results_local[exact_idx_local].comment) = '') and
                SameText(exact_text_local, target_text) and
                (get_candidate_text_unit_count(exact_text_local) =
                target_units) then
            begin
                Exit(True);
            end;
        end;
    end;

    function direct_full_query_entry_has_text_local(const pinyin_key: string;
        const target_text: string): Boolean;
    begin
        Result := False;
        if (m_dictionary = nil) or (pinyin_key = '') or (target_text = '') then
        begin
            Exit;
        end;

        Result := m_dictionary.is_base_entry(pinyin_key, target_text) or
            m_dictionary.is_user_entry(pinyin_key, target_text);
    end;

    function direct_exact_primary_has_text_local(const pinyin_key: string;
        const target_text: string; const target_units: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (pinyin_key = '') or
            (target_text = '') or
            (not lookup_exact_full_pinyin_cached_local(pinyin_key,
            exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                (exact_text_local <> '') and
                (get_candidate_text_unit_count(exact_text_local) =
                target_units) then
            begin
                Exit(SameText(exact_text_local, target_text));
            end;
        end;
    end;

    function short_four_span_query_key_local(
        const query_syllables_local: TncPinyinParseResult;
        const start_idx: Integer; const span_len: Integer): string;
    var
        syllable_idx_local: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (span_len <= 0) or
            (start_idx + span_len > Length(query_syllables_local)) then
        begin
            Exit;
        end;

        for syllable_idx_local := start_idx to start_idx + span_len - 1 do
        begin
            Result := Result + normalize_pinyin_text(
                query_syllables_local[syllable_idx_local].text);
        end;
    end;

    function short_four_two_exact_path_is_supported_local(
        const query_syllables_local: TncPinyinParseResult;
        const text_value: string): Boolean;
    var
        query_key_local: string;
        encoded_path_local: string;
        evidence_score_local: Integer;
    begin
        query_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, Length(query_syllables_local));
        if query_key_local = '' then
        begin
            Exit(False);
        end;
        Result := get_short_four_two_exact_path_evidence(query_key_local,
            text_value, encoded_path_local, evidence_score_local);
    end;

    procedure ensure_supported_short_four_two_exact_path_visible_local(
        const query_syllables_local: TncPinyinParseResult;
        var candidates_local: TncCandidateList);
    const
        c_aspect_probe_limit = 8;
        c_strong_path_top_evidence = 1000;
    var
        query_key_local: string;
        head_key_local: string;
        tail_key_local: string;
        head_results_local: TncCandidateList;
        tail_results_local: TncCandidateList;
        model_paths_local: TncPairPathEvidenceList;
        model_idx_local: Integer;
        head_idx_local: Integer;
        tail_idx_local: Integer;
        best_text_local: string;
        best_score_local: Integer;
        best_evidence_local: Integer;
        existing_idx_local: Integer;
        candidate_idx_local: Integer;
        picked_local: TncCandidate;
        has_full_query_exact_local: Boolean;
        has_full_query_user_exact_local: Boolean;
        full_query_exact_results_local: TncCandidateList;

        function candidate_weight_local(const candidate_value: TncCandidate): Integer;
        begin
            Result := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;
        end;

        function find_exact_candidate_local(
            const results_value: TncCandidateList; const text_value: string;
            out candidate_value: TncCandidate): Boolean;
        var
            idx_local: Integer;
        begin
            Result := False;
            FillChar(candidate_value, SizeOf(candidate_value), 0);
            for idx_local := 0 to High(results_value) do
            begin
                if (Trim(results_value[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(results_value[idx_local].text)) = 2) and
                    SameText(Trim(results_value[idx_local].text),
                    Trim(text_value)) then
                begin
                    candidate_value := results_value[idx_local];
                    Exit(True);
                end;
            end;
        end;

        function find_full_query_exact_candidate_local(
            const text_value: string; out candidate_value: TncCandidate): Boolean;
        var
            idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            FillChar(candidate_value, SizeOf(candidate_value), 0);
            for idx_local := 0 to High(full_query_exact_results_local) do
            begin
                exact_text_local := Trim(
                    full_query_exact_results_local[idx_local].text);
                if (Trim(full_query_exact_results_local[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(exact_text_local) = 4) and
                    SameText(exact_text_local, Trim(text_value)) then
                begin
                    candidate_value := full_query_exact_results_local[idx_local];
                    Exit(True);
                end;
            end;
        end;

        procedure consider_path_local(const path_value: string);
        var
            parts_local: TArray<string>;
            head_candidate_local: TncCandidate;
            tail_candidate_local: TncCandidate;
            head_text_local: string;
            tail_text_local: string;
            combined_text_local: string;
            combined_score_local: Integer;
            context_bonus_local: Integer;
            evidence_score_local: Integer;
            evidence_path_local: string;
            existing_idx: Integer;
            idx_local: Integer;
            local_candidate: TncCandidate;
            full_exact_candidate_local: TncCandidate;
        begin
            if get_encoded_path_segment_count_local(path_value) <> 2 then
            begin
                Exit;
            end;
            parts_local := path_value.Split([c_segment_path_separator]);
            if Length(parts_local) <> 2 then
            begin
                Exit;
            end;
            head_text_local := Trim(parts_local[0]);
            tail_text_local := Trim(parts_local[1]);
            if (get_candidate_text_unit_count(head_text_local) <> 2) or
                (get_candidate_text_unit_count(tail_text_local) <> 2) or
                (not find_exact_candidate_local(head_results_local,
                head_text_local, head_candidate_local)) or
                (not find_exact_candidate_local(tail_results_local,
                tail_text_local, tail_candidate_local)) then
            begin
                Exit;
            end;

            combined_text_local := head_text_local + tail_text_local;
            if has_full_query_exact_local and
                (not find_full_query_exact_candidate_local(
                combined_text_local, full_exact_candidate_local)) then
            begin
                Exit;
            end;
            if not get_short_four_two_exact_path_evidence(query_key_local,
                combined_text_local, evidence_path_local,
                evidence_score_local) then
            begin
                Exit;
            end;
            if has_full_query_exact_local and
                (evidence_score_local < c_strong_path_top_evidence) then
            begin
                Exit;
            end;

            context_bonus_local := Max(0, Min(400,
                m_dictionary.get_context_bonus(head_text_local,
                tail_text_local)));
            combined_score_local :=
                (candidate_weight_local(head_candidate_local) +
                candidate_weight_local(tail_candidate_local)) div 2 +
                evidence_score_local + context_bonus_local;

            existing_idx := -1;
            for idx_local := 0 to High(candidates_local) do
            begin
                if (Trim(candidates_local[idx_local].comment) = '') and
                    SameText(Trim(candidates_local[idx_local].text),
                    combined_text_local) then
                begin
                    existing_idx := idx_local;
                    Break;
                end;
            end;
            if existing_idx >= 0 then
            begin
                local_candidate := candidates_local[existing_idx];
                local_candidate.score := Max(local_candidate.score,
                    combined_score_local);
                if not has_full_query_exact_local then
                begin
                    local_candidate.display_kind := cdk_lm_compound;
                end;
                candidates_local[existing_idx] := local_candidate;
            end
            else
            begin
                if has_full_query_exact_local then
                begin
                    local_candidate := full_exact_candidate_local;
                end
                else
                begin
                    FillChar(local_candidate, SizeOf(local_candidate), 0);
                    local_candidate.text := combined_text_local;
                    local_candidate.comment := '';
                    local_candidate.source := cs_rule;
                    local_candidate.has_dict_weight := False;
                    local_candidate.dict_weight := 0;
                    local_candidate.display_kind := cdk_lm_compound;
                end;
                local_candidate.score := combined_score_local;
                SetLength(candidates_local, Length(candidates_local) + 1);
                candidates_local[High(candidates_local)] := local_candidate;
            end;
            remember_segment_path_for_candidate(combined_text_local, '',
                evidence_path_local, combined_score_local);

            if (best_text_local = '') or
                (combined_score_local > best_score_local) or
                ((combined_score_local = best_score_local) and
                (CompareText(combined_text_local, best_text_local) < 0)) then
            begin
                best_text_local := combined_text_local;
                best_score_local := combined_score_local;
                best_evidence_local := evidence_score_local;
            end;
        end;
    begin
        if (m_dictionary = nil) or (Length(query_syllables_local) <> 4) then
        begin
            Exit;
        end;

        head_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, 2);
        tail_key_local := short_four_span_query_key_local(
            query_syllables_local, 2, 2);
        if (not lookup_exact_full_pinyin_cached_local(head_key_local,
            head_results_local)) or
            (not lookup_exact_full_pinyin_cached_local(tail_key_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        query_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, Length(query_syllables_local));
        SetLength(full_query_exact_results_local, 0);
        has_full_query_exact_local :=
            lookup_exact_full_pinyin_cached_local(query_key_local,
            full_query_exact_results_local) and
            (Length(full_query_exact_results_local) > 0);
        has_full_query_user_exact_local := False;
        if has_full_query_exact_local then
        begin
            for candidate_idx_local := 0 to High(
                full_query_exact_results_local) do
            begin
                if (full_query_exact_results_local[candidate_idx_local].source =
                    cs_user) and
                    (Trim(full_query_exact_results_local[
                    candidate_idx_local].comment) = '') then
                begin
                    has_full_query_user_exact_local := True;
                    Break;
                end;
            end;
        end;
        if has_full_query_user_exact_local then
        begin
            Exit;
        end;
        best_text_local := '';
        best_score_local := Low(Integer);
        best_evidence_local := 0;

        if m_dictionary.get_exact_pair_path_evidence(query_key_local,
            model_paths_local) then
        begin
            for model_idx_local := 0 to High(model_paths_local) do
            begin
                consider_path_local(model_paths_local[model_idx_local].encoded_path);
            end;
        end;

        // Productive aspect tails are a grammatical exception, not arbitrary
        // 2+2 composition. Keep their bounded fallback independent of the LM.
        if (not has_full_query_exact_local) and
            (SameText(normalize_pinyin_text(query_syllables_local[3].text),
            'le') or
            SameText(normalize_pinyin_text(query_syllables_local[3].text),
            'zhe') or
            SameText(normalize_pinyin_text(query_syllables_local[3].text),
            'guo')) then
        begin
            for head_idx_local := 0 to Min(c_aspect_probe_limit - 1,
                High(head_results_local)) do
            begin
                if (Trim(head_results_local[head_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(
                    Trim(head_results_local[head_idx_local].text)) <> 2) then
                begin
                    Continue;
                end;
                for tail_idx_local := 0 to Min(c_aspect_probe_limit - 1,
                    High(tail_results_local)) do
                begin
                    if (Trim(tail_results_local[tail_idx_local].comment) <> '') or
                        (get_candidate_text_unit_count(
                        Trim(tail_results_local[tail_idx_local].text)) <> 2) then
                    begin
                        Continue;
                    end;
                    consider_path_local(
                        Trim(head_results_local[head_idx_local].text) +
                        c_segment_path_separator +
                        Trim(tail_results_local[tail_idx_local].text));
                end;
            end;
        end;

        if best_text_local = '' then
        begin
            Exit;
        end;

        if SameText(query_key_local, lookup_text) then
            short_four_model_evidence_applied := True;
        sort_candidates_lightweight(candidates_local);
        if (((not has_full_query_exact_local) and
            (best_evidence_local > 0)) or
            (has_full_query_exact_local and
            (best_evidence_local >= c_strong_path_top_evidence) and
            find_full_query_exact_candidate_local(best_text_local,
            picked_local))) then
        begin
            existing_idx_local := -1;
            for candidate_idx_local := 0 to High(candidates_local) do
            begin
                if (Trim(candidates_local[candidate_idx_local].comment) = '') and
                    SameText(Trim(candidates_local[candidate_idx_local].text),
                    best_text_local) then
                begin
                    existing_idx_local := candidate_idx_local;
                    Break;
                end;
            end;
            if existing_idx_local > 0 then
            begin
                picked_local := candidates_local[existing_idx_local];
                for candidate_idx_local := existing_idx_local downto 1 do
                begin
                    candidates_local[candidate_idx_local] :=
                        candidates_local[candidate_idx_local - 1];
                end;
                candidates_local[0] := picked_local;
            end;
        end;
    end;

    procedure ensure_supported_short_four_two_prefix_partial_visible_local(
        const query_syllables_local: TncPinyinParseResult;
        var candidates_local: TncCandidateList);
    const
        c_prefix_syllable_count = 4;
        c_max_query_syllables = 8;
        c_remaining_syllable_penalty = 80;
    var
        prefix_syllables_local: TncPinyinParseResult;
        prefix_candidates_local: TncCandidateList;
        prefix_candidate_local: TncCandidate;
        prefix_query_local: string;
        remaining_pinyin_local: string;
        evidence_path_local: string;
        evidence_score_local: Integer;
        prefix_idx_local: Integer;
        candidate_idx_local: Integer;
        syllable_idx_local: Integer;
        existing_idx_local: Integer;
        remaining_count_local: Integer;
    begin
        if (m_dictionary = nil) or
            (Length(query_syllables_local) <= c_prefix_syllable_count) or
            (Length(query_syllables_local) > c_max_query_syllables) or
            has_explicit_apostrophe_input then
        begin
            Exit;
        end;

        SetLength(prefix_syllables_local, c_prefix_syllable_count);
        prefix_query_local := '';
        for syllable_idx_local := 0 to c_prefix_syllable_count - 1 do
        begin
            prefix_syllables_local[syllable_idx_local] :=
                query_syllables_local[syllable_idx_local];
            prefix_query_local := prefix_query_local + normalize_pinyin_text(
                query_syllables_local[syllable_idx_local].text);
        end;

        remaining_pinyin_local := '';
        for syllable_idx_local := c_prefix_syllable_count to
            High(query_syllables_local) do
        begin
            remaining_pinyin_local := remaining_pinyin_local +
                normalize_pinyin_text(
                query_syllables_local[syllable_idx_local].text);
        end;
        if (prefix_query_local = '') or (remaining_pinyin_local = '') then
        begin
            Exit;
        end;

        SetLength(prefix_candidates_local, 0);
        ensure_supported_short_four_two_exact_path_visible_local(
            prefix_syllables_local, prefix_candidates_local);
        for prefix_idx_local := 0 to High(prefix_candidates_local) do
        begin
            prefix_candidate_local := prefix_candidates_local[prefix_idx_local];
            if (Trim(prefix_candidate_local.comment) <> '') or
                (prefix_candidate_local.display_kind <> cdk_lm_compound) or
                (get_candidate_text_unit_count(
                Trim(prefix_candidate_local.text)) <>
                c_prefix_syllable_count) or
                (not get_short_four_two_exact_path_evidence(
                prefix_query_local, Trim(prefix_candidate_local.text),
                evidence_path_local, evidence_score_local)) then
            begin
                Continue;
            end;

            existing_idx_local := -1;
            for candidate_idx_local := 0 to High(candidates_local) do
            begin
                if SameText(Trim(candidates_local[candidate_idx_local].text),
                    Trim(prefix_candidate_local.text)) and
                    SameText(normalize_pinyin_text(Trim(
                    candidates_local[candidate_idx_local].comment)),
                    remaining_pinyin_local) then
                begin
                    existing_idx_local := candidate_idx_local;
                    Break;
                end;
            end;

            remaining_count_local := Length(query_syllables_local) -
                c_prefix_syllable_count;
            prefix_candidate_local.comment := remaining_pinyin_local;
            prefix_candidate_local.score := Max(1,
                prefix_candidate_local.score -
                (remaining_count_local * c_remaining_syllable_penalty));
            prefix_candidate_local.source := cs_rule;
            prefix_candidate_local.has_dict_weight := False;
            prefix_candidate_local.dict_weight := 0;
            prefix_candidate_local.display_kind := cdk_lm_compound;
            if existing_idx_local >= 0 then
            begin
                if prefix_candidate_local.score >
                    candidates_local[existing_idx_local].score then
                begin
                    candidates_local[existing_idx_local].score :=
                        prefix_candidate_local.score;
                end;
                candidates_local[existing_idx_local].source := cs_rule;
                candidates_local[existing_idx_local].has_dict_weight := False;
                candidates_local[existing_idx_local].dict_weight := 0;
                candidates_local[existing_idx_local].display_kind :=
                    cdk_lm_compound;
            end
            else
            begin
                SetLength(candidates_local, Length(candidates_local) + 1);
                candidates_local[High(candidates_local)] :=
                    prefix_candidate_local;
            end;
            remember_segment_path_for_candidate(
                Trim(prefix_candidate_local.text), remaining_pinyin_local,
                evidence_path_local, prefix_candidate_local.score);
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' s42prefix=1';
            end;
            Break;
        end;
    end;

    procedure ensure_supported_short_four_transition_particle_path_visible_local(
        const query_syllables_local: TncPinyinParseResult;
        var candidates_local: TncCandidateList);
    const
        c_generated_complete_base_score = 16000;
        c_max_generated_candidates = 3;
    var
        query_key_local: string;
        prefix_key_local: string;
        particle_key_local: string;
        particle_text_local: string;
        model_paths_local: TncPairPathEvidenceList;
        full_exact_results_local: TncCandidateList;
        model_idx_local: Integer;
        candidate_idx_local: Integer;
        existing_idx_local: Integer;
        move_idx_local: Integer;
        generated_count_local: Integer;
        evidence_score_local: Integer;
        best_evidence_local: Integer;
        generated_score_local: Integer;
        generated_text_local: string;
        evidence_path_local: string;
        model_path_local: string;
        path_parts_local: TArray<string>;
        generated_candidate_local: TncCandidate;
        picked_candidate_local: TncCandidate;
        best_text_local: string;

        function try_get_particle_text_local: Boolean;
        begin
            particle_key_local := normalize_pinyin_text(
                query_syllables_local[3].text);
            particle_text_local := '';
            if particle_key_local = 'de' then
            begin
                particle_text_local := string(Char($7684));
            end
            else if particle_key_local = 'le' then
            begin
                particle_text_local := string(Char($4E86));
            end
            else if particle_key_local = 'zhe' then
            begin
                particle_text_local := string(Char($7740));
            end
            else if particle_key_local = 'la' then
            begin
                particle_text_local := string(Char($5566));
            end
            else if particle_key_local = 'ba' then
            begin
                particle_text_local := string(Char($5427));
            end
            else if particle_key_local = 'ma' then
            begin
                particle_text_local := string(Char($5417));
            end
            else if particle_key_local = 'a' then
            begin
                particle_text_local := string(Char($554A));
            end;
            Result := particle_text_local <> '';
        end;

        function has_full_query_exact_local: Boolean;
        var
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                full_exact_results_local) then
            begin
                Exit;
            end;
            for exact_idx_local := 0 to High(full_exact_results_local) do
            begin
                exact_text_local := Trim(
                    full_exact_results_local[exact_idx_local].text);
                if (Trim(full_exact_results_local[exact_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(exact_text_local) = 4) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (m_dictionary = nil) or (Length(query_syllables_local) <> 4) or
            has_explicit_apostrophe_input or
            (not try_get_particle_text_local) then
        begin
            Exit;
        end;

        query_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, 4);
        prefix_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, 3);
        if (query_key_local = '') or (prefix_key_local = '') or
            has_full_query_exact_local or
            (not m_dictionary.get_exact_pair_path_evidence(prefix_key_local,
            model_paths_local)) then
        begin
            Exit;
        end;

        generated_count_local := 0;
        best_evidence_local := 0;
        best_text_local := '';
        for model_idx_local := 0 to High(model_paths_local) do
        begin
            if generated_count_local >= c_max_generated_candidates then
            begin
                Break;
            end;
            model_path_local := Trim(
                model_paths_local[model_idx_local].encoded_path);
            path_parts_local := model_path_local.Split(
                [c_segment_path_separator]);
            if Length(path_parts_local) <> 2 then
            begin
                Continue;
            end;
            generated_text_local := Trim(path_parts_local[0]) +
                Trim(path_parts_local[1]) + particle_text_local;
            if not get_short_four_transition_particle_path_evidence(
                query_key_local, generated_text_local, evidence_path_local,
                evidence_score_local) then
            begin
                Continue;
            end;

            generated_score_local := c_generated_complete_base_score +
                Min(1560, evidence_score_local);
            existing_idx_local := -1;
            for candidate_idx_local := 0 to High(candidates_local) do
            begin
                if (Trim(candidates_local[candidate_idx_local].comment) = '') and
                    SameText(Trim(candidates_local[candidate_idx_local].text),
                    generated_text_local) then
                begin
                    existing_idx_local := candidate_idx_local;
                    Break;
                end;
            end;

            if existing_idx_local >= 0 then
            begin
                generated_candidate_local :=
                    candidates_local[existing_idx_local];
                generated_candidate_local.score := Max(
                    generated_candidate_local.score, generated_score_local);
                generated_candidate_local.display_kind := cdk_lm_compound;
                candidates_local[existing_idx_local] :=
                    generated_candidate_local;
            end
            else
            begin
                FillChar(generated_candidate_local,
                    SizeOf(generated_candidate_local), 0);
                generated_candidate_local.text := generated_text_local;
                generated_candidate_local.comment := '';
                generated_candidate_local.source := cs_rule;
                generated_candidate_local.score := generated_score_local;
                generated_candidate_local.has_dict_weight := False;
                generated_candidate_local.dict_weight := 0;
                generated_candidate_local.display_kind := cdk_lm_compound;
                SetLength(candidates_local, Length(candidates_local) + 1);
                candidates_local[High(candidates_local)] :=
                    generated_candidate_local;
            end;
            remember_segment_path_for_candidate(generated_text_local, '',
                evidence_path_local, generated_score_local);
            Inc(generated_count_local);

            if (best_text_local = '') or
                (evidence_score_local > best_evidence_local) then
            begin
                best_text_local := generated_text_local;
                best_evidence_local := evidence_score_local;
            end;
        end;

        if best_text_local = '' then
        begin
            Exit;
        end;
        sort_candidates_lightweight(candidates_local);
        existing_idx_local := -1;
        for candidate_idx_local := 0 to High(candidates_local) do
        begin
            if (Trim(candidates_local[candidate_idx_local].comment) = '') and
                SameText(Trim(candidates_local[candidate_idx_local].text),
                best_text_local) then
            begin
                existing_idx_local := candidate_idx_local;
                Break;
            end;
        end;
        if existing_idx_local > 0 then
        begin
            picked_candidate_local := candidates_local[existing_idx_local];
            for move_idx_local := existing_idx_local downto 1 do
            begin
                candidates_local[move_idx_local] :=
                    candidates_local[move_idx_local - 1];
            end;
            candidates_local[0] := picked_candidate_local;
        end;
    end;

    function is_productive_short_function_head_text_local(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 2 then
        begin
            Exit;
        end;

        if SameText(text_value, string(Char($6709)) + string(Char($70B9))) or

            SameText(text_value, string(Char($4E00)) + string(Char($70B9))) or
            SameText(text_value, string(Char($4E0D)) + string(Char($662F))) or

            SameText(text_value, string(Char($4E0D)) + string(Char($4F1A))) or

            SameText(text_value, string(Char($4E0D)) + string(Char($80FD))) or

            SameText(text_value, string(Char($4E0D)) + string(Char($8981))) or

            SameText(text_value, string(Char($6CA1)) + string(Char($6709))) or

            SameText(text_value, string(Char($53EF)) + string(Char($4EE5))) or

            SameText(text_value, string(Char($5E94)) + string(Char($8BE5))) or

            SameText(text_value, string(Char($9700)) + string(Char($8981))) then
        begin
            Exit(True);
        end;
    end;

    function is_one_plus_two_supported_head_single_local(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit;
        end;

        case Ord(text_value[1]) of
            $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
            $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
            $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
            $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
            $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
            $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F,
            $7B2C, $5148, $6211, $4F60, $60A8:
                Result := True;
        end;
    end;

    function is_unsupported_short_four_ab_cd_text_local(
        const text_value: string): Boolean;
    var
        units_local: TArray<string>;
        query_syllables_local: TncPinyinParseResult;
        first_unit_local: string;
        head_text_local: string;
        head_three_text_local: string;
        middle_text_local: string;
        tail_text_local: string;
        head_key_local: string;
        head_three_key_local: string;
        middle_key_local: string;
        tail_key_local: string;
        transition_particle_path_local: string;
        transition_particle_evidence_local: Integer;

        function is_short_particle_tail_unit_local(const pinyin_key_value: string;
            const text_value: string): Boolean;
        begin
            Result := ((pinyin_key_value = 'de') and
                SameText(text_value, string(Char($7684)))) or
                ((pinyin_key_value = 'le') and
                SameText(text_value, string(Char($4E86)))) or
                ((pinyin_key_value = 'zhe') and
                SameText(text_value, string(Char($7740)))) or
                ((pinyin_key_value = 'la') and
                SameText(text_value, string(Char($5566)))) or
                ((pinyin_key_value = 'ba') and
                SameText(text_value, string(Char($5427)))) or
                ((pinyin_key_value = 'ma') and
                SameText(text_value, string(Char($5417)))) or
                ((pinyin_key_value = 'a') and
                SameText(text_value, string(Char($554A))));
        end;

        function is_short_pronoun_unit_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1:
                    Result := True;
            end;
        end;

        function is_short_connector_unit_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $548C, $4E0E, $8DDF, $540C:
                    Result := True;
            end;
        end;

        function prefix_lookup_has_complete_text_local: Boolean;
        var
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or
                (not m_dictionary.lookup_full_pinyin_prefix(lookup_text,
                prefix_results_local)) then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                    prefix_results_local[prefix_idx_local].has_dict_weight and
                    (prefix_results_local[prefix_idx_local].dict_weight > 0) and
                    SameText(prefix_text_local, text_value) and
                    (get_candidate_text_unit_count(prefix_text_local) = 4) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (input_syllable_count <> 4) or
            (not is_full_pinyin_key(lookup_text)) or (text_value = '') then
        begin
            Exit;
        end;

        units_local := split_text_units(text_value);
        if Length(units_local) <> 4 then
        begin
            Exit;
        end;

        if direct_full_query_entry_has_text_local(lookup_text, text_value) then
        begin
            Exit;
        end;

        if prefix_lookup_has_complete_text_local then
        begin
            Exit;
        end;

        query_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(query_syllables_local) <> 4 then
        begin
            Exit;
        end;

        if short_four_two_exact_path_is_supported_local(
            query_syllables_local, text_value) then
        begin
            Exit;
        end;

        if get_short_four_transition_particle_path_evidence(lookup_text,
            text_value, transition_particle_path_local,
            transition_particle_evidence_local) then
        begin
            Exit;
        end;

        head_text_local := units_local[0] + units_local[1];
        tail_text_local := units_local[2] + units_local[3];
        head_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, 2);
        tail_key_local := short_four_span_query_key_local(
            query_syllables_local, 2, 2);

        if is_productive_short_function_head_text_local(head_text_local) and
            direct_exact_lookup_has_text_local(tail_key_local,
            tail_text_local, 2) then
        begin
            Exit;
        end;

        if direct_exact_lookup_has_text_local(head_key_local,
            head_text_local, 2) and
            is_short_pronoun_unit_local(units_local[2]) and
            is_short_connector_unit_local(units_local[3]) then
        begin
            Exit;
        end;

        if is_short_pronoun_unit_local(units_local[0]) and
            is_short_connector_unit_local(units_local[1]) and
            direct_exact_lookup_has_text_local(tail_key_local,
            tail_text_local, 2) then
        begin
            Exit;
        end;

        first_unit_local := units_local[0];
        middle_text_local := units_local[1] + units_local[2];
        middle_key_local := short_four_span_query_key_local(
            query_syllables_local, 1, 2);
        if is_one_plus_two_supported_head_single_local(first_unit_local) and
            direct_exact_lookup_has_text_local(middle_key_local,
            middle_text_local, 2) and
            is_short_particle_tail_unit_local(query_syllables_local[3].text,
            units_local[3]) then
        begin
            Exit;
        end;

        tail_text_local := units_local[1] + units_local[2] + units_local[3];
        tail_key_local := short_four_span_query_key_local(
            query_syllables_local, 1, 3);
        if is_one_plus_two_supported_head_single_local(first_unit_local) and
            direct_exact_lookup_has_text_local(tail_key_local,
            tail_text_local, 3) then
        begin
            Exit;
        end;

        head_three_text_local := units_local[0] + units_local[1] +
            units_local[2];
        head_three_key_local := short_four_span_query_key_local(
            query_syllables_local, 0, 3);
        if direct_exact_lookup_has_text_local(head_three_key_local,
            head_three_text_local, 3) and
            is_short_particle_tail_unit_local(query_syllables_local[3].text,
            units_local[3]) then
        begin
            Exit;
        end;

        Result := True;
    end;

    procedure filter_unsupported_short_four_ab_cd_complete_candidates_local(
        var candidates: TncCandidateList);
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
    begin
        if (input_syllable_count <> 4) or (m_dictionary = nil) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        ensure_supported_short_four_two_exact_path_visible_local(
            get_effective_compact_pinyin_syllables(lookup_text), candidates);
        ensure_supported_short_four_transition_particle_path_visible_local(
            get_effective_compact_pinyin_syllables(lookup_text), candidates);
        if Length(candidates) = 0 then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(candidates) do
        begin
            candidate_text_local := Trim(candidates[in_idx_local].text);
            if (Trim(candidates[in_idx_local].comment) = '') and
                (candidate_text_local <> '') and
                (candidates[in_idx_local].source <> cs_user) and
                (not (candidates[in_idx_local].has_dict_weight and
                (candidates[in_idx_local].dict_weight > 0))) and
                (get_candidate_text_unit_count(candidate_text_local) =
                input_syllable_count) and
                is_unsupported_short_four_ab_cd_text_local(
                candidate_text_local) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                candidates[out_idx_local] := candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    function is_unsupported_short_four_ab_c_partial_candidate_local(
        const candidate: TncCandidate): Boolean;
    var
        units_local: TArray<string>;
        query_syllables_local: TncPinyinParseResult;
        head_text_local: string;
        tail_text_local: string;
        tail_key_local: string;
        prefix_text_local: string;
        prefix_key_local: string;
        normalized_comment_local: string;
        comment_unit_count_local: Integer;
        unit_idx_local: Integer;

        function candidate_has_allowed_one_plus_two_prefix_support_local:
            Boolean;
        begin
            Result := False;
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            head_text_local := Trim(units_local[0]);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            tail_key_local := short_four_span_query_key_local(
                query_syllables_local, 1, 2);
            Result := (head_text_local <> '') and (tail_text_local <> '') and
                (tail_key_local <> '') and
                is_one_plus_two_supported_head_single_local(head_text_local) and
                direct_exact_lookup_has_text_local(tail_key_local,
                tail_text_local, 2);
        end;

    begin
        Result := False;
        if (m_dictionary = nil) or (input_syllable_count <> 4) or
            (not is_full_pinyin_key(lookup_text)) or
            (Trim(candidate.comment) = '') then
        begin
            Exit;
        end;

        normalized_comment_local := normalize_pinyin_text(Trim(candidate.comment));
        comment_unit_count_local := get_effective_compact_pinyin_unit_count(
            normalized_comment_local);
        if comment_unit_count_local <= 0 then
        begin
            Exit;
        end;

        units_local := split_text_units(Trim(candidate.text));
        if (Length(units_local) < 2) or (Length(units_local) > 3) then
        begin
            Exit;
        end;
        if Length(units_local) + comment_unit_count_local <> input_syllable_count
        then
        begin
            Exit;
        end;

        query_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(query_syllables_local) <> 4 then
        begin
            Exit;
        end;

        prefix_text_local := '';
        for unit_idx_local := 0 to High(units_local) do
        begin
            prefix_text_local := prefix_text_local + units_local[unit_idx_local];
        end;
        prefix_key_local := short_four_span_query_key_local(query_syllables_local,
            0, Length(units_local));
        if (prefix_key_local <> '') and (prefix_text_local <> '') and
            ((candidate.source = cs_user) or
            (candidate.has_dict_weight and (candidate.dict_weight > 0))) then
        begin
            Exit;
        end;

        if Length(units_local) <> 3 then
        begin
            Exit(True);
        end;

        if candidate_has_allowed_one_plus_two_prefix_support_local then
        begin
            Exit;
        end;

        Result := True;
    end;

    procedure filter_unsupported_short_four_ab_c_partial_candidates_local(
        var candidates: TncCandidateList);
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
    begin
        if (Length(candidates) = 0) or (input_syllable_count <> 4) or
            (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(candidates) do
        begin
            if is_unsupported_short_four_ab_c_partial_candidate_local(
                candidates[in_idx_local]) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                candidates[out_idx_local] := candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    procedure promote_long_full_query_exact_over_chain_candidate_local(
        var candidates: TncCandidateList; const chain_score_value: Integer);
    const
        c_exact_probe_limit = 12;
        c_exact_over_chain_bonus = 30000;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_text_local: string;
        candidate_rank_local: Integer;
        existing_idx_local: Integer;
        move_idx_local: Integer;
        picked_candidate_local: TncCandidate;
    begin
        if (m_dictionary = nil) or (Length(candidates) = 0) or
            (input_syllable_count < 5) or (not is_full_pinyin_key(lookup_text)) or
            (not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local)) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if exact_idx_local >= c_exact_probe_limit then
            begin
                Break;
            end;
            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            if exact_results_local[exact_idx_local].has_dict_weight then
            begin
                candidate_rank_local := exact_results_local[exact_idx_local].dict_weight;
            end
            else
            begin
                candidate_rank_local := exact_results_local[exact_idx_local].score;
            end;
            Inc(candidate_rank_local, get_session_query_bonus(candidate_text_local));
            Inc(candidate_rank_local, m_dictionary.get_query_choice_bonus(
                lookup_text, candidate_text_local) * 20);
            if is_latest_session_query_choice(candidate_text_local) then
            begin
                Inc(candidate_rank_local, 200000);
            end;
            if exact_results_local[exact_idx_local].source = cs_user then
            begin
                Inc(candidate_rank_local, 2048);
            end;

            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := exact_idx_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' lfq=[none n=%d]', [Length(exact_results_local)]);
            end;
            Exit;
        end;

        picked_candidate_local := exact_results_local[best_idx_local];
        picked_candidate_local.text := Trim(picked_candidate_local.text);
        picked_candidate_local.comment := '';
        picked_candidate_local.score := Max(picked_candidate_local.score,
            chain_score_value + c_exact_over_chain_bonus);

        existing_idx_local := -1;
        for move_idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[move_idx_local].comment) = '') and
                SameText(Trim(candidates[move_idx_local].text),
                picked_candidate_local.text) then
            begin
                existing_idx_local := move_idx_local;
                Break;
            end;
        end;

        if existing_idx_local > 0 then
        begin
            candidates[existing_idx_local].score := Max(
                candidates[existing_idx_local].score,
                picked_candidate_local.score);
            picked_candidate_local := candidates[existing_idx_local];
            for move_idx_local := existing_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_candidate_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' lfq=[move idx=%d rank=%d]', [existing_idx_local,
                    best_rank_local]);
            end;
        end
        else if existing_idx_local < 0 then
        begin
            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_candidate_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' lfq=[insert rank=%d]', [best_rank_local]);
            end;
        end
        else
        begin
            candidates[0].score := Max(candidates[0].score,
                picked_candidate_local.score);
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' lfq=[top rank=%d]', [best_rank_local]);
            end;
        end;
    end;

    function has_strong_short_exact_complete_lookup_local(
        const min_weight: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
        exact_weight_local: Integer;
    begin
        Result := False;
        if (not has_multi_syllable_input) or (input_syllable_count <= 1) or
            (not is_full_pinyin_key(lookup_text)) or
            (not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (exact_text_local = '') or
                (get_candidate_text_unit_count(exact_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            if exact_results_local[exact_idx_local].source = cs_user then
            begin
                Exit(True);
            end;

            if exact_results_local[exact_idx_local].has_dict_weight then
            begin
                exact_weight_local := exact_results_local[exact_idx_local].dict_weight;
            end
            else
            begin
                exact_weight_local := exact_results_local[exact_idx_local].score;
            end;
            if exact_weight_local >= min_weight then
            begin
                Exit(True);
            end;
        end;
    end;

    function build_two_syllable_single_head_prefix_partial_candidates_local(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_single_limit = c_candidate_total_limit_max;
        c_prefix_remaining_penalty = 40;
    var
        parser_local: TncPinyinParser;
        parsed_local: TncPinyinParseResult;
        exact_query_results_local: TncCandidateList;
        head_key_local: string;
        tail_key_local: string;
        head_results_local: TncCandidateList;
        result_idx_local: Integer;
        accepted_count_local: Integer;
        candidate_local: TncCandidate;
        candidate_text_local: string;
        base_score_local: Integer;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (m_dictionary = nil) or (query_text = '') then
        begin
            Exit;
        end;
        if lookup_exact_full_pinyin_cached_local(query_text,
            exact_query_results_local) then
        begin
            for result_idx_local := 0 to High(exact_query_results_local) do
            begin
                if Trim(exact_query_results_local[result_idx_local].comment) = '' then
                begin
                    Exit;
                end;
            end;
        end;

        head_key_local := '';
        tail_key_local := '';
        parser_local := TncPinyinParser.Create;
        try
            parsed_local := parser_local.parse(query_text);
            if Length(parsed_local) = 2 then
            begin
                head_key_local := normalize_pinyin_text(parsed_local[0].text);
                tail_key_local := normalize_pinyin_text(parsed_local[1].text);
            end
            else if (Length(parsed_local) = 1) and
                try_split_relaxed_missing_apostrophe_token(parser_local,
                query_text, head_key_local, tail_key_local) then
            begin
                head_key_local := normalize_pinyin_text(head_key_local);
                tail_key_local := normalize_pinyin_text(tail_key_local);
            end;
        finally
            parser_local.Free;
        end;

        if (head_key_local = '') or (tail_key_local = '') or
            (not is_full_pinyin_key(head_key_local)) or
            ((not is_full_pinyin_key(tail_key_local)) and
            (not is_relaxed_no_initial_syllable_text(tail_key_local))) then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(head_key_local,
            head_results_local) then
        begin
            Exit;
        end;

        accepted_count_local := 0;
        for result_idx_local := 0 to High(head_results_local) do
        begin
            if accepted_count_local >= c_prefix_single_limit then
            begin
                Break;
            end;
            if Trim(head_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(head_results_local[result_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <> 1) then
            begin
                Continue;
            end;

            candidate_local := head_results_local[result_idx_local];
            candidate_local.text := candidate_text_local;
            candidate_local.comment := tail_key_local;
            base_score_local := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > base_score_local) then
            begin
                base_score_local := candidate_local.dict_weight;
            end;
            candidate_local.score := base_score_local -
                c_prefix_remaining_penalty;
            candidate_local.dict_weight := base_score_local;
            candidate_local.has_dict_weight := True;

            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate_local;
            Inc(accepted_count_local);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function has_strong_exact_complete_lookup_weight_local(
        const min_weight: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
        exact_weight_local: Integer;
    begin
        Result := False;
        if (not has_multi_syllable_input) or (input_syllable_count <= 1) or
            (not is_full_pinyin_key(lookup_text)) or
            (not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (exact_text_local = '') or
                (get_candidate_text_unit_count(exact_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            exact_weight_local := exact_results_local[exact_idx_local].score;
            if exact_results_local[exact_idx_local].has_dict_weight and
                (exact_results_local[exact_idx_local].dict_weight >
                exact_weight_local) then
            begin
                exact_weight_local := exact_results_local[exact_idx_local].dict_weight;
            end;
            if exact_weight_local >= min_weight then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure promote_preferred_three_syllable_one_plus_two_partial_local(
        var candidates: TncCandidateList);
    const
        c_min_tail_weight_local = 520;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_score_local: Integer;
        candidate_score_local: Integer;
        best_two_plus_one_weight_local: Integer;
        move_idx_local: Integer;
        picked_local: TncCandidate;

        function is_allowed_one_plus_two_prefix_text_local(
            const value: string): Boolean;
        begin
            Result := (value = string(Char($6211))) or

                (value = string(Char($4E00))) or

                (value = string(Char($4E0D))) or

                (value = string(Char($518D))) or

                (value = string(Char($5728))) or

                (value = string(Char($9AD8))) or

                (value = string(Char($5F88))) or

                (value = string(Char($592A))) or

                (value = string(Char($6700))) or

                (value = string(Char($66F4))) or

                (value = string(Char($53C8))) or

                (value = string(Char($4E5F))) or

                (value = string(Char($5DF2))) or

                (value = string(Char($5C06))) or

                (value = string(Char($53EF))) or

                (value = string(Char($4F1A))) or

                (value = string(Char($80FD))) or

                (value = string(Char($8981))) or

                (value = string(Char($60F3))) or

                (value = string(Char($6CA1))) or

                (value = string(Char($65E0))) or

                (value = string(Char($975E))) or

                (value = string(Char($672A))) or

                (value = string(Char($4ECD))) or

                (value = string(Char($90FD))) or

                (value = string(Char($8FD8))) or

                (value = string(Char($624D))) or

                (value = string(Char($5C31))) or

                (value = string(Char($53EA))) or

                (value = string(Char($522B))) or

                (value = string(Char($591A))) or

                (value = string(Char($5C11))) or

                (value = string(Char($5168))) or

                (value = string(Char($8D85))) or

                (value = string(Char($771F))) or

                (value = string(Char($633A))) or

                (value = string(Char($8F83))) or

                (value = string(Char($9887))) or

                (value = string(Char($6781))) or

                (value = string(Char($628A))) or

                (value = string(Char($88AB))) or

                (value = string(Char($8BA9))) or

                (value = string(Char($7ED9))) or

                (value = string(Char($5411))) or

                (value = string(Char($5BF9))) or

                (value = string(Char($4ECE))) or

                (value = string(Char($4E8E))) or

                (value = string(Char($548C))) or

                (value = string(Char($4E0E))) or

                (value = string(Char($4F46))) or

                (value = string(Char($867D))) or

                (value = string(Char($800C))) or

                (value = string(Char($662F))) or

                (value = string(Char($7684))) or

                (value = string(Char($4E86)));

        end;

        function has_high_quality_two_syllable_tail_local(
            const tail_key: string): Boolean;
        var
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
            tail_text_local: string;
            tail_weight_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (tail_key = '') or
                (not is_full_pinyin_key(tail_key)) or
                (not m_dictionary.lookup(tail_key, tail_results_local)) then
            begin
                Exit;
            end;

            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if tail_idx_local >= 24 then
                begin
                    Break;
                end;
                if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                if get_candidate_text_unit_count(tail_text_local) <> 2 then
                begin
                    Continue;
                end;
                tail_weight_local := tail_results_local[tail_idx_local].score;
                if tail_results_local[tail_idx_local].has_dict_weight and
                    (tail_results_local[tail_idx_local].dict_weight >
                    tail_weight_local) then
                begin
                    tail_weight_local := tail_results_local[tail_idx_local].dict_weight;
                end;
                if (tail_results_local[tail_idx_local].source = cs_user) or
                    (tail_weight_local >= c_min_tail_weight_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function should_promote_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := False;
            if (Trim(candidate.comment) = '') or
                (get_candidate_text_unit_count(Trim(candidate.text)) <> 1) or
                (get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate.comment)) <> 2) then
            begin
                Exit;
            end;

            if (m_last_three_syllable_partial_preference_kind = 1) and
                (m_last_three_syllable_head_strength <= 0) then
            begin
                Exit(True);
            end;

            Result := (best_two_plus_one_weight_local < 1000) and
                is_allowed_one_plus_two_prefix_text_local(
                Trim(candidate.text)) and
                has_high_quality_two_syllable_tail_local(
                normalize_pinyin_text(candidate.comment));
        end;

    begin
        if (Length(candidates) = 0) or
            (input_syllable_count <> 3) or
            has_safe_trailing_initial_typing_state then
        begin
            Exit;
        end;

        for idx_local := 0 to High(candidates) do
        begin
            if (candidates[idx_local].source = cs_user) and
                (Trim(candidates[idx_local].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) =
                input_syllable_count) then
            begin
                if idx_local > 0 then
                begin
                    picked_local := candidates[idx_local];
                    if picked_local.score <= candidates[0].score then
                    begin
                        picked_local.score := candidates[0].score + 1;
                    end;
                    for move_idx_local := idx_local downto 1 do
                    begin
                        candidates[move_idx_local] := candidates[move_idx_local - 1];
                    end;
                    candidates[0] := picked_local;
                end;
                Exit;
            end;
        end;

        best_two_plus_one_weight_local := 0;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) <> '') and
                (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) = 2) and
                (get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidates[idx_local].comment)) = 1) then
            begin
                candidate_score_local := candidates[idx_local].score;
                if candidates[idx_local].has_dict_weight then
                begin
                    candidate_score_local := candidates[idx_local].dict_weight;
                end;
                if candidate_score_local > best_two_plus_one_weight_local then
                begin
                    best_two_plus_one_weight_local := candidate_score_local;
                end;
            end;
        end;

        best_idx_local := -1;
        best_score_local := Low(Integer);
        for idx_local := 0 to High(candidates) do
        begin
            if not should_promote_candidate_local(candidates[idx_local]) then
            begin
                Continue;
            end;

            candidate_score_local := candidates[idx_local].score;
            if candidates[idx_local].has_dict_weight and
                (candidates[idx_local].dict_weight > candidate_score_local) then
            begin
                candidate_score_local := candidates[idx_local].dict_weight;
            end;
            if candidate_score_local > best_score_local then
            begin
                best_score_local := candidate_score_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        picked_local := candidates[best_idx_local];
        if picked_local.score <= candidates[0].score then
        begin
            picked_local.score := candidates[0].score + 1;
        end;
        for move_idx_local := best_idx_local downto 1 do
        begin
            candidates[move_idx_local] := candidates[move_idx_local - 1];
        end;
        candidates[0] := picked_local;
    end;

    function try_pick_complete_candidate_from_list_local(
        const source_candidates: TncCandidateList;
        out out_candidate: TncCandidate): Boolean;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        score_local: Integer;
        best_score_local: Integer;
        text_local: string;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        best_idx_local := -1;
        best_score_local := Low(Integer);
        for idx_local := 0 to High(source_candidates) do
        begin
            if Trim(source_candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;

            text_local := Trim(source_candidates[idx_local].text);
            if (text_local = '') or
                (get_candidate_text_unit_count(text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            score_local := source_candidates[idx_local].score;
            if source_candidates[idx_local].has_dict_weight and
                (source_candidates[idx_local].dict_weight > score_local) then
            begin
                score_local := source_candidates[idx_local].dict_weight;
            end;
            if (best_idx_local < 0) or (score_local > best_score_local) then
            begin
                best_idx_local := idx_local;
                best_score_local := score_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            Exit;
        end;

        out_candidate := source_candidates[best_idx_local];
        out_candidate.comment := '';
        Result := True;
    end;

    function predictive_prefix_units_allowed_for_lookup_local(
        const units_value: Integer): Boolean;
    var
        query_units_local: Integer;
    begin
        Result := False;
        query_units_local := get_effective_compact_pinyin_unit_count(lookup_text);
        if query_units_local <= 0 then
        begin
            query_units_local := input_syllable_count;
        end;

        if (units_value <= 0) or (query_units_local <= 0) then
        begin
            Exit;
        end;

        if (query_units_local <= 3) and (units_value = query_units_local) then
        begin
            Exit(True);
        end;

        if (query_units_local = 4) and (units_value = 3) then
        begin
            Exit(True);
        end;
    end;

    function try_pick_predictive_prefix_candidate_from_list_local(
        const source_candidates: TncCandidateList;
        out out_candidate: TncCandidate): Boolean;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        score_local: Integer;
        best_score_local: Integer;
        text_local: string;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        best_idx_local := -1;
        best_score_local := Low(Integer);
        for idx_local := 0 to High(source_candidates) do
        begin
            if Trim(source_candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;

            text_local := Trim(source_candidates[idx_local].text);
            if (text_local = '') or
                (not predictive_prefix_units_allowed_for_lookup_local(
                get_candidate_text_unit_count(text_local))) then
            begin
                Continue;
            end;

            score_local := source_candidates[idx_local].score;
            if source_candidates[idx_local].has_dict_weight and
                (source_candidates[idx_local].dict_weight > score_local) then
            begin
                score_local := source_candidates[idx_local].dict_weight;
            end;
            if (best_idx_local < 0) or (score_local > best_score_local) then
            begin
                best_idx_local := idx_local;
                best_score_local := score_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            Exit;
        end;

        out_candidate := source_candidates[best_idx_local];
        out_candidate.comment := '';
        Result := True;
    end;

    function build_single_incomplete_prefix_lookup_candidates_local(
        out out_candidates: TncCandidateList): Boolean;
    var
        prefix_candidates_local: TncCandidateList;
        idx_local: Integer;
        out_idx_local: Integer;
        query_local: string;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        query_local := normalize_pinyin_text(lookup_text);
        if (m_dictionary = nil) or has_explicit_apostrophe_input or
            (input_syllable_count <> 1) or (Length(query_local) < 2) or
            is_full_pinyin_key(query_local) then
        begin
            Exit;
        end;

        if not m_dictionary.lookup_full_pinyin_prefix(query_local,
            prefix_candidates_local) then
        begin
            Exit;
        end;

        SetLength(out_candidates, Length(prefix_candidates_local));
        out_idx_local := 0;
        for idx_local := 0 to High(prefix_candidates_local) do
        begin
            if (Trim(prefix_candidates_local[idx_local].text) = '') or
                (Trim(prefix_candidates_local[idx_local].comment) <> '') then
            begin
                Continue;
            end;

            out_candidates[out_idx_local] := prefix_candidates_local[idx_local];
            out_candidates[out_idx_local].comment := '';
            Inc(out_idx_local);
        end;

        SetLength(out_candidates, out_idx_local);
        Result := out_idx_local > 0;
    end;

    procedure ensure_strict_oracle_sentence_candidates_prepared;
    const
        c_strict_oracle_max_syllables = 24;
    var
        debug_idx: Integer;
        debug_text: string;
    begin
        if strict_oracle_sentence_built then
        begin
            Exit;
        end;

        strict_oracle_sentence_built := True;
        strict_oracle_sentence_ok := False;
        SetLength(strict_oracle_sentence_candidates, 0);
        if (not has_multi_syllable_input) or
            (input_syllable_count < 10) or
            (input_syllable_count > c_strict_oracle_max_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            all_initial_compact_query or
            has_internal_dangling_initial or
            has_explicit_apostrophe_input then
        begin
            Exit;
        end;

        Inc(m_disable_subspan_standalone_oracle);
        try
            strict_oracle_sentence_ok :=
                build_strict_chunk_oracle_sentence_candidates(lookup_text,
                strict_oracle_sentence_candidates);
        finally
            Dec(m_disable_subspan_standalone_oracle);
        end;
        if m_config.debug_mode and strict_oracle_sentence_ok then
        begin
            debug_text := '';
            for debug_idx := 0 to Min(2, High(strict_oracle_sentence_candidates)) do
            begin
                debug_text := debug_text + Format(' {%d:%s}',
                    [debug_idx + 1,
                    Trim(strict_oracle_sentence_candidates[debug_idx].text)]);
            end;
            if debug_text <> '' then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' oracle=' + debug_text;
            end;
        end;
    end;

    function explicit_apostrophe_exact_text_matches_local(
        const text_value: string): Boolean;
    var
        idx_local: Integer;
    begin
        Result := False;
        if (not has_explicit_apostrophe_input) or
            (explicit_apostrophe_lookup_text = '') or (m_dictionary = nil) then
        begin
            Exit;
        end;

        if not explicit_apostrophe_exact_candidates_loaded then
        begin
            explicit_apostrophe_exact_candidates_loaded := True;
            if not m_dictionary.lookup(explicit_apostrophe_lookup_text,
                explicit_apostrophe_exact_candidates) then
            begin
                SetLength(explicit_apostrophe_exact_candidates, 0);
            end;
        end;

        for idx_local := 0 to High(explicit_apostrophe_exact_candidates) do
        begin
            if (Trim(explicit_apostrophe_exact_candidates[idx_local].comment) = '') and
                SameText(Trim(explicit_apostrophe_exact_candidates[idx_local].text),
                Trim(text_value)) then
            begin
                Exit(True);
            end;
        end;
    end;

    function explicit_apostrophe_complete_text_matches_boundary_local(
        const text_value: string): Boolean;
    var
        parser_local: TncPinyinParser;
        parts_local: TncPinyinParseResult;
        units_local: TArray<string>;
        idx_local: Integer;
    begin
        Result := False;
        if (not has_explicit_apostrophe_input) or (input_syllable_count <= 1) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        { A strict explicit-boundary dictionary hit is authoritative even when
          a character has a lexical reading that does not exist in isolation,
          such as lu'an / 六安.  Constructed candidates still pass through
          the per-character alignment checks below. }
        if explicit_apostrophe_exact_text_matches_local(text_value) then
        begin
            Exit(True);
        end;

        units_local := split_text_units(Trim(text_value));
        if Length(units_local) <> input_syllable_count then
        begin
            Exit;
        end;

        parser_local := TncPinyinParser.Create;
        try
            parts_local := parser_local.parse(m_composition_text);
        finally
            parser_local.Free;
        end;

        if Length(parts_local) <> input_syllable_count then
        begin
            Exit;
        end;

        for idx_local := 0 to input_syllable_count - 1 do
        begin
            if not m_dictionary.single_char_matches_pinyin(
                normalize_pinyin_text(Trim(parts_local[idx_local].text)),
                units_local[idx_local]) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure ensure_explicit_apostrophe_aligned_complete_visible_local(
        var candidates: TncCandidateList);
    var
        lookup_results_local: TncCandidateList;
        result_idx_local: Integer;
        existing_idx_local: Integer;
        idx_local: Integer;
        picked_local: TncCandidate;

        procedure consider_candidates_local(const source_candidates: TncCandidateList);
        var
            local_idx: Integer;
            local_text: string;
        begin
            for local_idx := 0 to High(source_candidates) do
            begin
                if Trim(source_candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(source_candidates[local_idx].text);
                if (local_text = '') or
                    (not explicit_apostrophe_complete_text_matches_boundary_local(
                    local_text)) then
                begin
                    Continue;
                end;

                result_idx_local := local_idx;
                picked_local := source_candidates[local_idx];
                Exit;
            end;
        end;
    begin
        if (not has_explicit_apostrophe_input) or (input_syllable_count <= 1) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        result_idx_local := -1;
        FillChar(picked_local, SizeOf(picked_local), 0);
        if (explicit_apostrophe_lookup_text <> '') and
            m_dictionary.lookup(explicit_apostrophe_lookup_text,
            lookup_results_local) then
        begin
            consider_candidates_local(lookup_results_local);
        end;
        if (result_idx_local < 0) and
            (explicit_apostrophe_lookup_text <> '') and
            m_dictionary.lookup_exact_full_pinyin(explicit_apostrophe_lookup_text,
            lookup_results_local) then
        begin
            consider_candidates_local(lookup_results_local);
        end;
        if (result_idx_local < 0) and
            m_dictionary.lookup(lookup_text, lookup_results_local) then
        begin
            consider_candidates_local(lookup_results_local);
        end;
        if (result_idx_local < 0) and
            m_dictionary.lookup_exact_full_pinyin(lookup_text, lookup_results_local) then
        begin
            consider_candidates_local(lookup_results_local);
        end;
        if result_idx_local < 0 then
        begin
            Exit;
        end;

        picked_local.comment := '';
        if picked_local.score < 2048 then
        begin
            picked_local.score := picked_local.score + 4096;
        end;

        existing_idx_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text),
                Trim(picked_local.text)) then
            begin
                existing_idx_local := idx_local;
                Break;
            end;
        end;

        if existing_idx_local = 0 then
        begin
            if candidates[0].score < picked_local.score then
            begin
                candidates[0].score := picked_local.score;
            end;
            Exit;
        end;

        if existing_idx_local > 0 then
        begin
            picked_local := candidates[existing_idx_local];
            for idx_local := existing_idx_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := picked_local;
            Exit;
        end;

        SetLength(candidates, Length(candidates) + 1);
        for idx_local := High(candidates) downto 1 do
        begin
            candidates[idx_local] := candidates[idx_local - 1];
        end;
        candidates[0] := picked_local;
    end;

    procedure filter_explicit_apostrophe_output_complete_local(
        var candidates: TncCandidateList);
    var
        idx_local: Integer;
        out_idx_local: Integer;
        parts_local: TncPinyinParseResult;
        parts_parsed_local: Boolean;
        function ensure_parts_local: Boolean;
        var
            parser_local: TncPinyinParser;
        begin
            if not parts_parsed_local then
            begin
                parts_parsed_local := True;
                parser_local := TncPinyinParser.Create;
                try
                    parts_local := parser_local.parse(m_composition_text);
                finally
                    parser_local.Free;
                end;
            end;
            Result := Length(parts_local) = input_syllable_count;
        end;

        function contains_non_ascii_text_local(const value: string): Boolean;
        var
            ch: Char;
        begin
            Result := False;
            for ch in value do
            begin
                if Ord(ch) > $7F then
                begin
                    Exit(True);
                end;
            end;
        end;

        function partial_candidate_matches_boundary_local(
            const candidate: TncCandidate): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            expected_tail_local: string;
            is_preferred_local: Boolean;
        begin
            Result := False;
            if Trim(candidate.comment) = '' then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate.text));
            if (Length(text_units_local) <= 0) or
                (Length(text_units_local) >= input_syllable_count) then
            begin
                Exit;
            end;

            if not ensure_parts_local then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(text_units_local) do
            begin
                if not match_single_char_candidate_for_syllable(
                    Trim(parts_local[unit_idx_local].text),
                    text_units_local[unit_idx_local],
                    is_preferred_local) then
                begin
                    Exit;
                end;
            end;

            expected_tail_local := '';
            for unit_idx_local := Length(text_units_local) to input_syllable_count - 1 do
            begin
                expected_tail_local := expected_tail_local +
                    normalize_pinyin_text(parts_local[unit_idx_local].text);
            end;

            Result := (expected_tail_local <> '') and
                SameText(normalize_pinyin_text(Trim(candidate.comment)),
                expected_tail_local);
        end;
    begin
        if (not has_explicit_apostrophe_input) or (input_syllable_count <= 1) then
        begin
            Exit;
        end;

        parts_parsed_local := False;
        SetLength(parts_local, 0);
        out_idx_local := 0;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) =
                input_syllable_count) and
                (not explicit_apostrophe_complete_text_matches_boundary_local(
                candidates[idx_local].text)) then
            begin
                Continue;
            end;

            if (Trim(candidates[idx_local].comment) <> '') and
                contains_non_ascii_text_local(Trim(candidates[idx_local].text)) and
                (not partial_candidate_matches_boundary_local(candidates[idx_local])) then
            begin
                Continue;
            end;

            if out_idx_local <> idx_local then
            begin
                candidates[out_idx_local] := candidates[idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    procedure dedupe_visible_candidates_local(var candidates: TncCandidateList;
        out out_removed_count: Integer);
    var
        seen_local: TDictionary<string, Integer>;
        list_local: TList<TncCandidate>;
        idx_local: Integer;
        existing_idx_local: Integer;
        key_local: string;
        candidate_local: TncCandidate;
        existing_local: TncCandidate;

        procedure merge_duplicate_local(var target: TncCandidate;
            const duplicate: TncCandidate);
        begin
            if duplicate.score > target.score then
            begin
                target.score := duplicate.score;
            end;

            if duplicate.has_dict_weight and
                ((not target.has_dict_weight) or
                (duplicate.dict_weight > target.dict_weight)) then
            begin
                target.has_dict_weight := True;
                target.dict_weight := duplicate.dict_weight;
            end;

            if get_source_rank(duplicate.source) < get_source_rank(target.source) then
            begin
                target.source := duplicate.source;
            end;
        end;
    begin
        out_removed_count := 0;
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        seen_local := TDictionary<string, Integer>.Create;
        list_local := TList<TncCandidate>.Create;
        try
            for idx_local := 0 to High(candidates) do
            begin
                key_local := build_candidate_identity_key(candidates[idx_local].text,
                    candidates[idx_local].comment);
                if key_local = '' then
                begin
                    list_local.Add(candidates[idx_local]);
                    Continue;
                end;

                if seen_local.TryGetValue(key_local, existing_idx_local) then
                begin
                    if (existing_idx_local >= 0) and
                        (existing_idx_local < list_local.Count) then
                    begin
                        existing_local := list_local[existing_idx_local];
                        merge_duplicate_local(existing_local, candidates[idx_local]);
                        list_local[existing_idx_local] := existing_local;
                    end;
                    Inc(out_removed_count);
                    Continue;
                end;

                candidate_local := candidates[idx_local];
                list_local.Add(candidate_local);
                seen_local.Add(key_local, list_local.Count - 1);
            end;

            if out_removed_count <= 0 then
            begin
                Exit;
            end;

            SetLength(candidates, list_local.Count);
            for idx_local := 0 to list_local.Count - 1 do
            begin
                candidates[idx_local] := list_local[idx_local];
            end;
        finally
            list_local.Free;
            seen_local.Free;
        end;
    end;

    procedure filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
        var candidates: TncCandidateList; var removed_count: Integer);
    var
        relaxed_parts_local: TncPinyinParseResult;
        relaxed_head_key_local: string;
        relaxed_tail_key_local: string;
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_units_local: TArray<string>;

        function load_relaxed_parts_local: Boolean;
        begin
            relaxed_parts_local := parse_pinyin_with_relaxed_missing_apostrophe(
                m_composition_text);
            if Length(relaxed_parts_local) <> 2 then
            begin
                relaxed_parts_local := parse_pinyin_with_relaxed_missing_apostrophe(
                    lookup_text);
            end;
            Result := Length(relaxed_parts_local) = 2;
        end;

        function has_aligned_relaxed_boundary_partial_local: Boolean;
        var
            candidate_idx_local: Integer;
            partial_units_local: TArray<string>;
        begin
            Result := False;
            for candidate_idx_local := 0 to High(candidates) do
            begin
                if not SameText(normalize_pinyin_text(
                    Trim(candidates[candidate_idx_local].comment)),
                    relaxed_tail_key_local) then
                begin
                    Continue;
                end;

                partial_units_local := split_text_units(
                    Trim(candidates[candidate_idx_local].text));
                if (Length(partial_units_local) = 1) and
                    m_dictionary.single_char_matches_pinyin(
                    relaxed_head_key_local, partial_units_local[0]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function complete_units_match_relaxed_boundary_local(
            const units_value: TArray<string>): Boolean;
        begin
            Result := (Length(units_value) = 2) and
                m_dictionary.single_char_matches_pinyin(relaxed_head_key_local,
                units_value[0]) and
                m_dictionary.single_char_matches_pinyin(relaxed_tail_key_local,
                units_value[1]);
        end;

        function has_non_user_full_query_exact_support_local(
            const text_value: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            text_key_local: string;
        begin
            Result := False;
            text_key_local := Trim(text_value);
            if (text_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    text_key_local) and
                    (exact_results_local[exact_idx_local].source <> cs_user) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (m_dictionary = nil) or (input_syllable_count <> 2) or
            (Length(candidates) <= 1) or (not load_relaxed_parts_local) then
        begin
            Exit;
        end;

        relaxed_head_key_local := normalize_pinyin_text(
            relaxed_parts_local[0].text);
        relaxed_tail_key_local := normalize_pinyin_text(
            relaxed_parts_local[1].text);
        if (relaxed_head_key_local = '') or (relaxed_tail_key_local = '') or
            (not is_relaxed_no_initial_syllable_text(relaxed_tail_key_local)) or
            (not has_aligned_relaxed_boundary_partial_local) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(candidates) do
        begin
            if (candidates[in_idx_local].source = cs_user) and
                (Trim(candidates[in_idx_local].comment) = '') then
            begin
                candidate_units_local := split_text_units(
                    Trim(candidates[in_idx_local].text));
                if (Length(candidate_units_local) = 2) and
                    (not complete_units_match_relaxed_boundary_local(
                    candidate_units_local)) and
                    (not has_non_user_full_query_exact_support_local(
                    candidates[in_idx_local].text)) then
                begin
                    Inc(removed_count);
                    Continue;
                end;
            end;

            if out_idx_local <> in_idx_local then
            begin
                candidates[out_idx_local] := candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    function debug_target_candidate_present_local(
        const candidates: TncCandidateList): Boolean;
    var
        debug_idx_local: Integer;
    begin
        Result := False;
        if m_debug_target_recall_text = '' then
        begin
            Exit;
        end;
        for debug_idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[debug_idx_local].comment) = '') and
                SameText(Trim(candidates[debug_idx_local].text),
                m_debug_target_recall_text) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure mark_debug_target_candidate_stage_local(const stage_bit: Integer;
        const candidates: TncCandidateList);
    begin
        if debug_target_candidate_present_local(candidates) then
        begin
            m_debug_target_candidate_stage_mask :=
                m_debug_target_candidate_stage_mask or stage_bit;
        end;
    end;

    procedure finalize_candidates_and_prepare_exit(const debug_extra: string);
    var
        filtered_constructed_count_local: Integer;
        filtered_misaligned_complete_count_local: Integer;
        deduped_candidate_count_local: Integer;
        best_complete_exact_text_local: string;
        strong_tail_completion_text_local: string;
        strong_tail_completion_score_local: Integer;

        function get_exact_query_user_choice_priority_local(
            const candidate_text: string): Integer;
        var
            normalized_query_local: string;
            latest_text_local: string;
            choice_bonus_local: Integer;
            text_key_local: string;
        begin
            Result := 0;
            text_key_local := Trim(candidate_text);
            if (text_key_local = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            if normalized_query_local = '' then
            begin
                Exit;
            end;

            if is_latest_session_query_choice(text_key_local) then
            begin
                Inc(Result, 200000);
            end;

            latest_text_local := get_cached_query_latest_choice_text(
                normalized_query_local);
            if (latest_text_local <> '') and SameText(text_key_local,
                latest_text_local) then
            begin
                Inc(Result, 200000);
            end;

            choice_bonus_local := m_dictionary.get_query_choice_bonus(
                normalized_query_local, text_key_local);
            if choice_bonus_local > 0 then
            begin
                Inc(Result, 50000 + (choice_bonus_local * 20));
            end;
        end;

        function is_protected_user_choice_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (candidate.source = cs_user) and
                (Trim(candidate.comment) = '') and
                (get_exact_query_user_choice_priority_local(candidate.text) > 0);
        end;

        function is_complete_single_char_chain_text_local(
            const text_value: string): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            unit_idx_local: Integer;
            is_preferred_local: Boolean;
        begin
            Result := False;
            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            units_local := split_text_units(Trim(text_value));
            if (Length(syllables_local) <> input_syllable_count) or
                (Length(units_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(units_local) do
            begin
                if (get_candidate_text_unit_count(units_local[unit_idx_local]) <> 1) or
                    (not match_single_char_candidate_for_syllable(
                    syllables_local[unit_idx_local].text,
                    units_local[unit_idx_local], is_preferred_local)) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function has_complete_exact_phrase_support_local(
            const text_value: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            text_key_local: string;
        begin
            Result := False;
            text_key_local := Trim(text_value);
            if (text_key_local = '') or (m_dictionary = nil) or
                (lookup_text = '') or (input_syllable_count < 2) or
                (not is_full_pinyin_key(lookup_text)) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    text_key_local) and
                    (get_candidate_text_unit_count(Trim(
                    exact_results_local[exact_idx_local].text)) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_reliable_complete_support_text_local(
            const text_value: string; out out_support_score_local: Integer): Boolean;
        var
            support_segments_local: Integer;
            support_path_local: string;
            text_key_local: string;

            function has_lexicon_longer_prefix_support_local: Boolean;
            var
                prefix_results_local: TncCandidateList;
                prefix_idx_local: Integer;
                text_units_local: TArray<string>;
                prefix_text_local: string;
                unit_idx_local: Integer;
            begin
                Result := False;
                if (m_dictionary = nil) or (lookup_text = '') then
                begin
                    Exit;
                end;

                if not m_dictionary.lookup_full_pinyin_prefix(lookup_text,
                    prefix_results_local) then
                begin
                    Exit;
                end;

                for prefix_idx_local := 0 to High(prefix_results_local) do
                begin
                    if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    text_units_local := split_text_units(
                        Trim(prefix_results_local[prefix_idx_local].text));
                    if Length(text_units_local) <= input_syllable_count then
                    begin
                        Continue;
                    end;

                    prefix_text_local := '';
                    for unit_idx_local := 0 to input_syllable_count - 1 do
                    begin
                        prefix_text_local := prefix_text_local +
                            text_units_local[unit_idx_local];
                    end;

                    if SameText(prefix_text_local, text_key_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            out_support_score_local := Low(Integer);
            text_key_local := Trim(text_value);
            if (text_key_local = '') or (input_syllable_count < 2) or
                (get_candidate_text_unit_count(text_key_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if has_lexicon_longer_prefix_support_local then
            begin
                out_support_score_local := 4096;
                Exit(True);
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, text_key_local, out_support_score_local,
                support_segments_local);
            if (support_path_local = '') or (out_support_score_local < 4096) then
            begin
                Exit;
            end;
            Result := True;
        end;

        function get_complete_exact_query_weight_for_text_local(
            const text_value: string): Integer;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_weight_local: Integer;
        begin
            Result := -1;
            if (m_dictionary = nil) or (Trim(text_value) = '') or
                (lookup_text = '') or (not is_full_pinyin_key(lookup_text)) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local = '') or
                    (not SameText(exact_text_local, Trim(text_value))) then
                begin
                    Continue;
                end;

                exact_weight_local := exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight >
                    exact_weight_local) then
                begin
                    exact_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end;

                if exact_weight_local > Result then
                begin
                    Result := exact_weight_local;
                end;
            end;
        end;

        function is_complete_single_char_chain_candidate_local(
            const candidate: TncCandidate): Boolean;
        var
            candidate_weight_local: Integer;
        begin
            Result := False;
            if (candidate.comment <> '') or (input_syllable_count < 2) or
                (get_candidate_text_unit_count(candidate.text) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if has_complete_exact_phrase_support_local(candidate.text) then
            begin
                Exit(False);
            end;

            candidate_weight_local := 0;
            if candidate.has_dict_weight then
            begin
                candidate_weight_local := candidate.dict_weight;
            end;
            if (candidate.source = cs_rule) and candidate.has_dict_weight and
                (candidate_weight_local >= 2500) then
            begin
                Exit(False);
            end;

            Result := is_complete_single_char_chain_text_local(candidate.text);
        end;

        function try_get_best_complete_exact_phrase_candidate_local(
            out out_candidate: TncCandidate; out out_text: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_weight_local: Integer;
            exact_support_score_local: Integer;
            exact_support_segments_local: Integer;
            exact_support_path_local: string;
            exact_choice_priority_local: Integer;
            best_idx_local: Integer;
            best_weight_local: Integer;
            function build_chunk_text_local(const units_local: TArray<string>;
                const start_idx_local: Integer; const unit_count_local: Integer): string;
            var
                unit_idx_local: Integer;
            begin
                Result := '';
                if (unit_count_local <= 0) or (start_idx_local < 0) or
                    (start_idx_local + unit_count_local - 1 > High(units_local)) then
                begin
                    Exit;
                end;

                for unit_idx_local := start_idx_local to
                    start_idx_local + unit_count_local - 1 do
                begin
                    Result := Result + units_local[unit_idx_local];
                end;
            end;

            function get_fixed_head_match_bonus_local(
                const text_value_local: string): Integer;
            begin
                Result := 0;
            end;

            function get_exact_subspan_alignment_bonus_local(
                const text_value_local: string): Integer;
            var
                units_local: TArray<string>;
                syllables_local: TncPinyinParseResult;
                start_idx_local: Integer;
                span_units_local: Integer;
                query_idx_local: Integer;
                query_key_local: string;
                chunk_text_local: string;
                subspan_results_local: TncCandidateList;
                result_idx_local: Integer;
                best_subspan_text_local: string;
                best_subspan_weight_local: Integer;
                candidate_weight_local: Integer;
            begin
                Result := 0;
                units_local := split_text_units(Trim(text_value_local));
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (Length(units_local) <> input_syllable_count) or
                    (Length(syllables_local) <> input_syllable_count) or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                for start_idx_local := 0 to input_syllable_count - 1 do
                begin
                    for span_units_local := 2 to Min(5,
                        input_syllable_count - start_idx_local) do
                    begin
                        query_key_local := '';
                        for query_idx_local := start_idx_local to
                            start_idx_local + span_units_local - 1 do
                        begin
                            query_key_local := query_key_local +
                                normalize_pinyin_text(
                                syllables_local[query_idx_local].text);
                        end;
                        if (query_key_local = '') or
                            (not m_dictionary.lookup_exact_full_pinyin(
                            query_key_local, subspan_results_local)) then
                        begin
                            Continue;
                        end;

                        best_subspan_text_local := '';
                        best_subspan_weight_local := Low(Integer);
                        for result_idx_local := 0 to Min(5,
                            High(subspan_results_local)) do
                        begin
                            if Trim(subspan_results_local[result_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            if get_candidate_text_unit_count(
                                Trim(subspan_results_local[result_idx_local].text)) <>
                                span_units_local then
                            begin
                                Continue;
                            end;

                            if subspan_results_local[result_idx_local].has_dict_weight then
                            begin
                                candidate_weight_local :=
                                    subspan_results_local[result_idx_local].dict_weight;
                            end
                            else
                            begin
                                candidate_weight_local :=
                                    subspan_results_local[result_idx_local].score;
                            end;
                            if candidate_weight_local > best_subspan_weight_local then
                            begin
                                best_subspan_weight_local := candidate_weight_local;
                                best_subspan_text_local := Trim(
                                    subspan_results_local[result_idx_local].text);
                            end;
                        end;
                        if best_subspan_text_local = '' then
                        begin
                            Continue;
                        end;

                        chunk_text_local := build_chunk_text_local(units_local,
                            start_idx_local, span_units_local);
                        if chunk_text_local = '' then
                        begin
                            Continue;
                        end;

                        if SameText(chunk_text_local, best_subspan_text_local) then
                        begin
                            case span_units_local of
                                2:
                                    Inc(Result, 1200);
                                3:
                                    Inc(Result, 4800);
                                4:
                                    Inc(Result, 12000);
                            else
                                Inc(Result, 18000);
                            end;
                        end
                        else if (span_units_local >= 4) or
                            (best_subspan_weight_local >= 12000) then
                        begin
                            case span_units_local of
                                2:
                                    Dec(Result, 1600);
                                3:
                                    Dec(Result, 6400);
                                4:
                                    Dec(Result, 18000);
                            else
                                Dec(Result, 24000);
                            end;
                        end;
                    end;
                end;
            end;
        begin
            Result := False;
            out_text := '';
            best_idx_local := -1;
            best_weight_local := Low(Integer);
            if (m_dictionary = nil) or (lookup_text = '') or
                (input_syllable_count < 2) or
                (not is_full_pinyin_key(lookup_text)) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local = '') or
                    (get_candidate_text_unit_count(exact_text_local) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                if not (exact_results_local[exact_idx_local].has_dict_weight or
                    (exact_results_local[exact_idx_local].source = cs_user)) then
                begin
                    Continue;
                end;

                if (m_dictionary.get_candidate_penalty(lookup_text,
                    exact_text_local) > 0) and
                    ((exact_results_local[exact_idx_local].source = cs_user) or
                    (not m_dictionary.is_base_entry(lookup_text,
                    exact_text_local))) then
                begin
                    Continue;
                end;

                if exact_results_local[exact_idx_local].has_dict_weight then
                begin
                    exact_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end
                else
                begin
                    exact_weight_local :=
                        exact_results_local[exact_idx_local].score;
                end;
                if (strong_tail_completion_text_local <> '') and
                    (not SameText(exact_text_local,
                    strong_tail_completion_text_local)) and
                    (exact_weight_local <= 300) then
                begin
                    Continue;
                end;
                exact_support_path_local :=
                    infer_segment_path_for_query_text_with_score(
                    lookup_text, exact_text_local, exact_support_score_local,
                    exact_support_segments_local);
                if (exact_support_path_local <> '') and
                    (exact_support_score_local > 0) then
                begin
                    Inc(exact_weight_local, exact_support_score_local);
                    if exact_support_segments_local >= 3 then
                    begin
                        Inc(exact_weight_local, 12000);
                    end;
                end;
                exact_choice_priority_local :=
                    get_exact_query_user_choice_priority_local(exact_text_local);
                if (exact_choice_priority_local > 0) or
                    (not ((exact_results_local[exact_idx_local].source = cs_user) and
                    (exact_weight_local <= 1) and
                    is_complete_single_char_chain_text_local(exact_text_local))) then
                begin
                    Inc(exact_weight_local, exact_choice_priority_local);
                end;
                Inc(exact_weight_local,
                    get_fixed_head_match_bonus_local(exact_text_local));
                Inc(exact_weight_local,
                    get_exact_subspan_alignment_bonus_local(exact_text_local));

                if (best_idx_local < 0) or
                    (exact_weight_local > best_weight_local) then
                begin
                    best_idx_local := exact_idx_local;
                    best_weight_local := exact_weight_local;
                end;
            end;

            if best_idx_local >= 0 then
            begin
                out_candidate := exact_results_local[best_idx_local];
                out_candidate.comment := '';
                out_text := Trim(out_candidate.text);
                Result := out_text <> '';
            end;
        end;

        procedure ensure_best_complete_exact_phrase_visible_local(
            var candidates: TncCandidateList);
        var
            best_candidate_local: TncCandidate;
            idx_local: Integer;
            move_idx_local: Integer;
            existing_idx_local: Integer;
            best_exact_weight_local: Integer;

            function has_reliable_complete_support_local(
                const candidate_local: TncCandidate;
                out out_support_score_local: Integer): Boolean;
            var
                support_segments_local: Integer;
                support_path_local: string;
                candidate_text_local: string;
            begin
                Result := False;
                out_support_score_local := Low(Integer);
                candidate_text_local := Trim(candidate_local.text);
                if (candidate_text_local = '') or
                    (Trim(candidate_local.comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    input_syllable_count) or
                    is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, candidate_local) then
                begin
                    Exit;
                end;

                support_path_local := infer_segment_path_for_query_text_with_score(
                    lookup_text, candidate_text_local, out_support_score_local,
                    support_segments_local);
                if (support_path_local = '') or (out_support_score_local < 4096) then
                begin
                    Exit;
                end;

                Result := True;
            end;

            function get_fixed_head_match_count_local(
                const text_value_local: string): Integer;
            begin
                Result := 0;
            end;

            function is_number_single_text_local(
                const text_value_local: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(text_value_local) <> 1 then
                begin
                    Exit;
                end;

                case Ord(text_value_local[1]) of
                    $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                    $4E03, $516B, $4E5D, $5341:
                        Result := True;
                end;
            end;

            function is_ordinal_head_text_local(
                const text_value_local: string): Boolean;
            var
                units_local: TArray<string>;
            begin
                units_local := split_text_units(Trim(text_value_local));
                if Length(units_local) <= 0 then
                begin
                    Exit(False);
                end;
                Result := (Length(units_local) >= 2) and
                    SameText(units_local[0], string(Char($7B2C)));
            end;

            function is_quantity_tail_single_text_local(
                const text_value_local: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(text_value_local) <> 1 then
                begin
                    Exit;
                end;

                case Ord(text_value_local[1]) of
                    $5B57, $9879, $6761, $4E2A, $70B9, $79CD, $7C7B,
                    $6B21, $6B65, $8F6E, $7AE0, $8282, $9875, $6BB5,
                    $884C, $5217, $5C42:
                        Result := True;
                end;
            end;

            function is_structured_quantity_candidate_local(
                const candidate_local: TncCandidate): Boolean;
            var
                encoded_path_local: string;
                segment_parts_local: TArray<string>;
                inferred_score_local: Integer;
                inferred_segment_count_local: Integer;
                head_text_local: string;
                tail_text_local: string;

                function is_number_quantity_phrase_text_local(
                    const text_value_local: string): Boolean;
                var
                    units_local: TArray<string>;
                begin
                    units_local := split_text_units(Trim(text_value_local));
                    if Length(units_local) <> 2 then
                    begin
                        Exit(False);
                    end;
                    Result := is_number_single_text_local(units_local[0]) and
                        is_quantity_tail_single_text_local(units_local[1]);
                end;
            begin
                Result := False;
                if Trim(candidate_local.comment) <> '' then
                begin
                    Exit;
                end;

                encoded_path_local := get_segment_path_for_candidate(
                    candidate_local);
                if encoded_path_local = '' then
                begin
                    encoded_path_local := infer_segment_path_for_query_text_with_score(
                        lookup_text, Trim(candidate_local.text),
                        inferred_score_local, inferred_segment_count_local);
                end;
                segment_parts_local := encoded_path_local.Split(
                    [c_segment_path_separator]);
                if Length(segment_parts_local) <> 2 then
                begin
                    Exit;
                end;

                head_text_local := Trim(segment_parts_local[0]);
                tail_text_local := Trim(segment_parts_local[1]);
                Result :=
                    (is_quantity_tail_single_text_local(tail_text_local) and
                    (is_number_single_text_local(head_text_local) or
                    is_ordinal_head_text_local(head_text_local))) or
                    (SameText(head_text_local, string(Char($7B2C))) and
                    is_number_quantity_phrase_text_local(tail_text_local));
            end;

            function should_keep_existing_complete_chain_first_local: Boolean;
            var
                current_weight_local: Integer;
                current_support_score_local: Integer;
                best_support_score_local: Integer;
                current_head_match_local: Integer;
                best_head_match_local: Integer;
            begin
                Result := False;
                if (Length(candidates) = 0) or
                    (Trim(candidates[0].comment) <> '') or
                    SameText(Trim(candidates[0].text),
                    best_complete_exact_text_local) or
                    (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                    input_syllable_count) then
                begin
                    Exit;
                end;

                if best_candidate_local.has_dict_weight then
                begin
                    best_exact_weight_local := best_candidate_local.dict_weight;
                end
                else if (best_candidate_local.source = cs_user) and
                    (not is_latest_session_query_choice(
                    best_complete_exact_text_local)) then
                begin
                    best_exact_weight_local := 0;
                end
                else
                begin
                    best_exact_weight_local := best_candidate_local.score;
                end;
                if ((best_candidate_local.source = cs_user) or
                    best_candidate_local.has_dict_weight) and
                    (candidates[0].source <> cs_user) and
                    (not candidates[0].has_dict_weight) then
                begin
                    Exit(False);
                end;
                if (input_syllable_count <= 3) and
                    (candidates[0].source <> cs_user) and
                    (m_dictionary <> nil) and
                    (not m_dictionary.is_base_entry(lookup_text,
                    Trim(candidates[0].text))) then
                begin
                    if is_structured_quantity_candidate_local(candidates[0]) then
                    begin
                        current_weight_local := candidates[0].score;
                        if candidates[0].has_dict_weight and
                            (candidates[0].dict_weight > current_weight_local) then
                        begin
                            current_weight_local := candidates[0].dict_weight;
                        end;
                        if current_weight_local > best_exact_weight_local then
                        begin
                            Exit(True);
                        end;
                    end;
                    Exit(False);
                end;
                if best_exact_weight_local >= 700 then
                begin
                    current_head_match_local := get_fixed_head_match_count_local(
                        candidates[0].text);
                    best_head_match_local := get_fixed_head_match_count_local(
                        best_complete_exact_text_local);
                    if current_head_match_local > best_head_match_local then
                    begin
                        Exit(True);
                    end;
                    if has_reliable_complete_support_local(candidates[0],
                        current_support_score_local) then
                    begin
                        if (not has_reliable_complete_support_local(
                            best_candidate_local, best_support_score_local)) or
                            (current_support_score_local + 2048 >=
                            best_support_score_local) then
                        begin
                            Exit(True);
                        end;
                    end;
                    Exit;
                end;

                current_weight_local := candidates[0].score;
                if candidates[0].has_dict_weight and
                    (candidates[0].dict_weight > current_weight_local) then
                begin
                    current_weight_local := candidates[0].dict_weight;
                end;
                Result := current_weight_local > best_exact_weight_local;
            end;
        begin
            best_complete_exact_text_local := '';
            if not try_get_best_complete_exact_phrase_candidate_local(
                best_candidate_local, best_complete_exact_text_local) then
            begin
                Exit;
            end;

            if (Length(candidates) > 0) and
                SameText(Trim(candidates[0].text),
                best_complete_exact_text_local) and
                (Trim(candidates[0].comment) = '') then
            begin
                Exit;
            end;

            if should_keep_existing_complete_chain_first_local then
            begin
                Exit;
            end;

            existing_idx_local := -1;
            for idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx_local].comment) = '') and
                    SameText(Trim(candidates[idx_local].text),
                    best_complete_exact_text_local) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end;

            if existing_idx_local >= 0 then
            begin
                best_candidate_local := candidates[existing_idx_local];
                for move_idx_local := existing_idx_local downto 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local - 1];
                end;
                candidates[0] := best_candidate_local;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := best_candidate_local;
        end;

        procedure ensure_best_tail_exact_completion_visible_local(
            var candidates: TncCandidateList);
        const
            c_tail_probe_limit = 12;
        var
            idx_local: Integer;
            tail_idx_local: Integer;
            existing_idx_local: Integer;
            move_idx_local: Integer;
            prefix_text_local: string;
            tail_key_local: string;
            tail_syllable_count_local: Integer;
            prefix_unit_count_local: Integer;
            tail_results_local: TncCandidateList;
            tail_text_local: string;
            tail_weight_local: Integer;
            best_tail_text_local: string;
            best_tail_weight_local: Integer;
            completion_candidate_local: TncCandidate;
            completion_text_local: string;
            completion_score_local: Integer;

            function has_stronger_complete_exact_phrase_local(
                const completion_text: string): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := False;
                if (m_dictionary = nil) or (Trim(completion_text) = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(lookup_text,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (exact_text_local = '') or
                        SameText(exact_text_local, Trim(completion_text)) or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        input_syllable_count) then
                    begin
                        Continue;
                    end;

                    if exact_results_local[exact_idx_local].has_dict_weight then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        exact_weight_local := exact_results_local[exact_idx_local].score;
                    end;

                    if exact_weight_local >= 500 then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            strong_tail_completion_text_local := '';
            strong_tail_completion_score_local := 0;
            if (Length(candidates) = 0) or (m_dictionary = nil) or
                (input_syllable_count < 3) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(candidates) do
            begin
                prefix_text_local := Trim(candidates[idx_local].text);
                tail_key_local := normalize_pinyin_text(
                    Trim(candidates[idx_local].comment));
                if (prefix_text_local = '') or (tail_key_local = '') or
                    (not is_full_pinyin_key(tail_key_local)) then
                begin
                    Continue;
                end;

                prefix_unit_count_local :=
                    get_candidate_text_unit_count(prefix_text_local);
                tail_syllable_count_local :=
                    get_effective_compact_pinyin_unit_count(tail_key_local);
                if (prefix_unit_count_local < 2) or
                    (tail_syllable_count_local < 1) or
                    (tail_syllable_count_local > 4) or
                    (prefix_unit_count_local + tail_syllable_count_local <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                    tail_results_local) then
                begin
                    Continue;
                end;

                best_tail_text_local := '';
                best_tail_weight_local := Low(Integer);
                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    if tail_idx_local >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                    if (tail_text_local = '') or
                        (get_candidate_text_unit_count(tail_text_local) <>
                        tail_syllable_count_local) then
                    begin
                        Continue;
                    end;

                    tail_weight_local := tail_results_local[tail_idx_local].score;
                    if tail_results_local[tail_idx_local].has_dict_weight and
                        (tail_results_local[tail_idx_local].dict_weight >
                        tail_weight_local) then
                    begin
                        tail_weight_local :=
                            tail_results_local[tail_idx_local].dict_weight;
                    end;

                    if tail_weight_local > best_tail_weight_local then
                    begin
                        best_tail_weight_local := tail_weight_local;
                        best_tail_text_local := tail_text_local;
                    end;
                end;

                if (best_tail_text_local = '') or
                    (best_tail_weight_local < 260) then
                begin
                    Continue;
                end;

                completion_text_local := prefix_text_local + best_tail_text_local;
                completion_score_local := candidates[idx_local].score +
                    best_tail_weight_local + 50000 +
                    (tail_syllable_count_local * 1200);
                if completion_score_local > strong_tail_completion_score_local then
                begin
                    strong_tail_completion_score_local := completion_score_local;
                    strong_tail_completion_text_local := completion_text_local;
                    FillChar(completion_candidate_local,
                        SizeOf(completion_candidate_local), 0);
                    completion_candidate_local.text := completion_text_local;
                    completion_candidate_local.comment := '';
                    completion_candidate_local.score := completion_score_local;
                    completion_candidate_local.source := cs_rule;
                    completion_candidate_local.has_dict_weight := True;
                    completion_candidate_local.dict_weight :=
                        completion_score_local;
                end;
            end;

            if strong_tail_completion_text_local = '' then
            begin
                Exit;
            end;

            if has_stronger_complete_exact_phrase_local(
                strong_tail_completion_text_local) then
            begin
                strong_tail_completion_text_local := '';
                strong_tail_completion_score_local := 0;
                Exit;
            end;

            existing_idx_local := -1;
            for idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx_local].comment) = '') and
                    SameText(Trim(candidates[idx_local].text),
                    strong_tail_completion_text_local) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end;

            if existing_idx_local >= 0 then
            begin
                completion_candidate_local := candidates[existing_idx_local];
                completion_candidate_local.score := Max(
                    completion_candidate_local.score,
                    strong_tail_completion_score_local);
                completion_candidate_local.has_dict_weight := True;
                completion_candidate_local.dict_weight := Max(
                    completion_candidate_local.dict_weight,
                    completion_candidate_local.score);
                for move_idx_local := existing_idx_local downto 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local - 1];
                end;
                candidates[0] := completion_candidate_local;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := completion_candidate_local;
        end;

        function try_build_learned_exact_query_choice_candidate_local(
            out out_candidate: TncCandidate): Boolean;
        var
            normalized_query_local: string;
            learned_text_local: string;
            session_text_local: string;
            latest_from_session_local: Boolean;
            latest_from_dictionary_local: Boolean;
            trusted_latest_choice_local: Boolean;
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            unit_idx_local: Integer;
            is_preferred_local: Boolean;
            choice_priority_local: Integer;
            learned_source_local: TncCandidateSource;
            learned_has_dict_weight_local: Boolean;
            learned_dict_weight_local: Integer;

            function get_exact_weight_for_text_local(
                const target_text: string): Integer;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := -1;
                if (m_dictionary = nil) or (Trim(target_text) = '') or
                    (not lookup_exact_full_pinyin_cached_local(
                    normalized_query_local, exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (exact_text_local = '') or
                        (not SameText(exact_text_local, Trim(target_text))) then
                    begin
                        Continue;
                    end;

                    if exact_results_local[exact_idx_local].has_dict_weight then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        exact_weight_local := exact_results_local[exact_idx_local].score;
                    end;

                    if exact_weight_local > Result then
                    begin
                        Result := exact_weight_local;
                    end;
                end;
            end;

            function get_best_competing_exact_weight_local: Integer;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := -1;
                if (m_dictionary = nil) or
                    (not lookup_exact_full_pinyin_cached_local(
                    normalized_query_local, exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (exact_text_local = '') or
                        SameText(exact_text_local, learned_text_local) or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        input_syllable_count) then
                    begin
                        Continue;
                    end;

                    exact_weight_local := exact_results_local[exact_idx_local].score;
                    if exact_results_local[exact_idx_local].has_dict_weight and
                        (exact_results_local[exact_idx_local].dict_weight >
                        exact_weight_local) then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end;

                    if exact_weight_local > Result then
                    begin
                        Result := exact_weight_local;
                    end;
                end;
            end;

            function weak_learned_choice_is_blocked_by_strong_exact_local: Boolean;
            const
                c_strong_exact_protection_min = 260;
                c_strong_exact_protection_gap = 700;
                c_weak_learned_exact_max = 80;
            var
                learned_exact_weight_local: Integer;
                competing_exact_weight_local: Integer;
            begin
                Result := False;
                learned_exact_weight_local :=
                    get_exact_weight_for_text_local(learned_text_local);
                competing_exact_weight_local :=
                    get_best_competing_exact_weight_local;
                if (learned_exact_weight_local >= 0) and
                    (learned_exact_weight_local <= c_weak_learned_exact_max) and
                    (competing_exact_weight_local >= c_strong_exact_protection_min) and
                    (competing_exact_weight_local >= learned_exact_weight_local +
                    c_strong_exact_protection_gap) then
                begin
                    Result := True;
                end;
            end;

        begin
            Result := False;
            FillChar(out_candidate, SizeOf(out_candidate), 0);

            if (lookup_text = '') or (input_syllable_count < 2) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            learned_text_local := '';
            latest_from_session_local := False;
            latest_from_dictionary_local := False;
            if (m_session_query_latest_text <> nil) and
                m_session_query_latest_text.TryGetValue(normalized_query_local,
                session_text_local) then
            begin
                learned_text_local := Trim(session_text_local);
                latest_from_session_local := learned_text_local <> '';
            end;
            if (learned_text_local = '') and (m_dictionary <> nil) then
            begin
                learned_text_local := get_cached_query_latest_choice_text(
                    normalized_query_local);
                latest_from_dictionary_local := learned_text_local <> '';
            end;
            trusted_latest_choice_local := latest_from_session_local or
                latest_from_dictionary_local;

            if (learned_text_local = '') or
                (get_candidate_text_unit_count(learned_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if (m_dictionary <> nil) and
                (m_dictionary.get_candidate_penalty(normalized_query_local,
                learned_text_local) > 0) and
                (not m_dictionary.is_base_entry(normalized_query_local,
                learned_text_local)) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalized_query_local);
            units_local := split_text_units(learned_text_local);
            if (Length(syllables_local) <> input_syllable_count) or
                (Length(units_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(units_local) do
            begin
                if (get_candidate_text_unit_count(units_local[unit_idx_local]) <> 1) or
                    (not match_single_char_candidate_for_syllable(
                    syllables_local[unit_idx_local].text,
                    units_local[unit_idx_local], is_preferred_local)) then
                begin
                    Exit;
                end;
            end;

            choice_priority_local :=
                get_exact_query_user_choice_priority_local(learned_text_local);
            if latest_from_session_local then
            begin
                Inc(choice_priority_local, 200000);
            end;
            if choice_priority_local <= 0 then
            begin
                choice_priority_local := get_session_query_bonus(
                    learned_text_local);
            end;
            if choice_priority_local <= 0 then
            begin
                Exit;
            end;

            if latest_from_dictionary_local and (not latest_from_session_local) and
                (m_dictionary <> nil) and
                is_low_evidence_admin_place_alias_cached(
                normalized_query_local, learned_text_local) then
            begin
                Exit;
            end;
            if (not trusted_latest_choice_local) and
                weak_learned_choice_is_blocked_by_strong_exact_local then
            begin
                Exit;
            end;
            if (strong_tail_completion_text_local <> '') and
                (not trusted_latest_choice_local) and
                (not SameText(learned_text_local,
                strong_tail_completion_text_local)) and
                (get_exact_weight_for_text_local(learned_text_local) <= 300) then
            begin
                Exit;
            end;

            learned_source_local := cs_user;
            learned_has_dict_weight_local := False;
            learned_dict_weight_local := 0;
            if (m_dictionary <> nil) and
                m_dictionary.is_base_entry(normalized_query_local,
                learned_text_local) then
            begin
                learned_source_local := cs_rule;
                learned_dict_weight_local :=
                    get_exact_weight_for_text_local(learned_text_local);
                learned_has_dict_weight_local := learned_dict_weight_local > 0;
            end;

            out_candidate.text := learned_text_local;
            out_candidate.comment := '';
            out_candidate.source := learned_source_local;
            out_candidate.has_dict_weight := learned_has_dict_weight_local;
            out_candidate.score := 50000 + choice_priority_local;
            if out_candidate.score < 120000 then
            begin
                out_candidate.score := 120000;
            end;
            out_candidate.dict_weight := learned_dict_weight_local;
            Result := True;
        end;

        procedure ensure_learned_exact_query_choice_visible_local(
            var candidates: TncCandidateList);
        var
            learned_candidate_local: TncCandidate;
            existing_candidate_local: TncCandidate;
            idx_local: Integer;
            existing_idx_local: Integer;
            move_idx_local: Integer;
        begin
            if not try_build_learned_exact_query_choice_candidate_local(
                learned_candidate_local) then
            begin
                Exit;
            end;

            if Length(candidates) > 0 then
            begin
                if learned_candidate_local.score <= candidates[0].score then
                begin
                    learned_candidate_local.score := candidates[0].score + 4096;
                end;
                if SameText(Trim(candidates[0].text),
                    learned_candidate_local.text) and
                    (Trim(candidates[0].comment) = '') then
                begin
                    if candidates[0].score < learned_candidate_local.score then
                    begin
                        candidates[0].score := learned_candidate_local.score;
                    end;
                    candidates[0].source := learned_candidate_local.source;
                    if learned_candidate_local.has_dict_weight then
                    begin
                        candidates[0].has_dict_weight := True;
                        candidates[0].dict_weight := Max(candidates[0].dict_weight,
                            learned_candidate_local.dict_weight);
                    end;
                    Exit;
                end;
            end;

            existing_idx_local := -1;
            for idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx_local].comment) = '') and
                    SameText(Trim(candidates[idx_local].text),
                    learned_candidate_local.text) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end;

            if existing_idx_local >= 0 then
            begin
                existing_candidate_local := candidates[existing_idx_local];
                if existing_candidate_local.score > learned_candidate_local.score then
                begin
                    learned_candidate_local.score :=
                        existing_candidate_local.score;
                end;
                if existing_candidate_local.has_dict_weight then
                begin
                    learned_candidate_local.dict_weight := Max(
                        learned_candidate_local.dict_weight,
                        existing_candidate_local.dict_weight);
                    learned_candidate_local.has_dict_weight := True;
                end;
                if Length(candidates) > 0 then
                begin
                    learned_candidate_local.score := Max(
                        learned_candidate_local.score,
                        candidates[0].score + 4096);
                end;
                for move_idx_local := existing_idx_local downto 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local - 1];
                end;
                candidates[0] := learned_candidate_local;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := learned_candidate_local;
        end;

        procedure filter_constructed_complete_candidates_with_exact_phrase_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            filtered_support_score_local: Integer;

            function has_multi_segment_candidate_path_local(
                const candidate: TncCandidate): Boolean;
            var
                encoded_path_local: string;
            begin
                Result := False;
                encoded_path_local := get_segment_path_for_candidate(candidate);
                if encoded_path_local = '' then
                begin
                    Exit;
                end;
                Result := get_encoded_path_segment_count_local(
                    encoded_path_local) > 1;
            end;

            function weak_complete_exact_is_blocked_by_tail_local(
                const candidate: TncCandidate): Boolean;
            var
                blocked_text_local: string;
                blocked_support_score_local: Integer;
            begin
                Result := False;
                blocked_text_local := Trim(candidate.text);
                if (strong_tail_completion_text_local = '') or
                    (blocked_text_local = '') or
                    (Trim(candidate.comment) <> '') or
                    SameText(blocked_text_local,
                    strong_tail_completion_text_local) or
                    (get_candidate_text_unit_count(blocked_text_local) <>
                    input_syllable_count) then
                begin
                    Exit;
                end;

                if candidate.source = cs_user then
                begin
                    Exit(not is_protected_user_choice_candidate_local(candidate));
                end;

                if (strong_tail_completion_text_local <> '') and
                    (candidate.source <> cs_user) and
                    (not candidate.has_dict_weight) then
                begin
                    Exit(True);
                end;

                if has_reliable_complete_support_text_local(blocked_text_local,
                    blocked_support_score_local) then
                begin
                    Exit(False);
                end;

                if has_multi_segment_candidate_path_local(candidate) then
                begin
                    Exit(False);
                end;

                Result := get_complete_exact_query_weight_for_text_local(
                    blocked_text_local) <= 300;
            end;
        begin
            filtered_constructed_count_local := 0;
            if (Length(candidates) = 0) or (input_syllable_count < 2) or
                ((best_complete_exact_text_local = '') and
                (strong_tail_completion_text_local = '')) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if is_fuzzy_pinyin_active and
                    (candidates[in_idx_local].fuzzy_cost > 0) and
                    (Trim(candidates[in_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) then
                begin
                    if out_idx_local <> in_idx_local then
                    begin
                        candidates[out_idx_local] := candidates[in_idx_local];
                    end;
                    Inc(out_idx_local);
                    Continue;
                end;
                if (Trim(candidates[in_idx_local].comment) = '') and
                    ((((best_complete_exact_text_local <> '') and
                    SameText(candidate_text_local,
                    best_complete_exact_text_local) and
                    (not weak_complete_exact_is_blocked_by_tail_local(
                    candidates[in_idx_local]))) or
                    ((strong_tail_completion_text_local <> '') and
                    SameText(candidate_text_local,
                    strong_tail_completion_text_local)))) then
                begin
                    if out_idx_local <> in_idx_local then
                    begin
                        candidates[out_idx_local] := candidates[in_idx_local];
                    end;
                    Inc(out_idx_local);
                    Continue;
                end;

                if is_complete_single_char_chain_candidate_local(
                    candidates[in_idx_local]) then
                begin
                    if not is_protected_user_choice_candidate_local(
                        candidates[in_idx_local]) then
                    begin
                        Inc(filtered_constructed_count_local);
                        Continue;
                    end;
                end;

                if (best_complete_exact_text_local <> '') and
                    (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (candidates[in_idx_local].source <> cs_user) and
                    (not candidates[in_idx_local].has_dict_weight) and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (not SameText(candidate_text_local,
                    best_complete_exact_text_local)) and
                    ((strong_tail_completion_text_local = '') or
                    (not SameText(candidate_text_local,
                    strong_tail_completion_text_local))) then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if (best_complete_exact_text_local <> '') and
                    (Trim(candidates[in_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (not SameText(candidate_text_local,
                    best_complete_exact_text_local)) and
                    ((strong_tail_completion_text_local = '') or
                    (not SameText(candidate_text_local,
                    strong_tail_completion_text_local))) and
                    (get_complete_exact_query_weight_for_text_local(
                    candidate_text_local) < 0) and
                    ((not has_multi_segment_candidate_path_local(
                    candidates[in_idx_local])) or
                    (not candidates[in_idx_local].has_dict_weight)) and
                    (not has_reliable_complete_support_text_local(
                    candidate_text_local, filtered_support_score_local)) and
                    (not is_protected_user_choice_candidate_local(
                    candidates[in_idx_local])) then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if weak_complete_exact_is_blocked_by_tail_local(
                    candidates[in_idx_local]) then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure filter_unsupported_nonlexicon_complete_candidates_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            candidate_is_unsupported_complete_local: Boolean;

            function span_query_key_local(const syllables_value:
                TncPinyinParseResult; const start_idx: Integer;
                const span_len: Integer): string;
            var
                syllable_idx_local: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (span_len <= 0) or
                    (start_idx + span_len > Length(syllables_value)) then
                begin
                    Exit;
                end;

                for syllable_idx_local := start_idx to start_idx + span_len - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_value[syllable_idx_local].text);
                end;
            end;

            function span_has_exact_text_local(const query_key: string;
                const target_text: string; const target_units: Integer): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := False;
                if (query_key = '') or (target_text = '') or
                    (m_dictionary = nil) or
                    (not lookup_exact_full_pinyin_cached_local(query_key,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(exact_text_local, target_text) and
                        (get_candidate_text_unit_count(exact_text_local) =
                        target_units) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function unsupported_four_syllable_ab_cd_runtime_complete_local(
                const text_value: string): Boolean;
            var
                syllables_local: TncPinyinParseResult;
                units_local: TArray<string>;
                head_text_local: string;
                tail_text_local: string;
                head_query_local: string;
                tail_query_local: string;
            begin
                Result := False;
                if (input_syllable_count <> 4) or (m_dictionary = nil) then
                begin
                    Exit;
                end;

                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                units_local := split_text_units(text_value);
                if (Length(syllables_local) <> 4) or (Length(units_local) <> 4) then
                begin
                    Exit;
                end;

                head_text_local := units_local[0] + units_local[1];
                tail_text_local := units_local[2] + units_local[3];
                head_query_local := span_query_key_local(syllables_local, 0, 2);
                tail_query_local := span_query_key_local(syllables_local, 2, 2);

                if not (span_has_exact_text_local(head_query_local,
                    head_text_local, 2) and
                    span_has_exact_text_local(tail_query_local,
                    tail_text_local, 2)) then
                begin
                    Exit;
                end;

                Result := True;
            end;
        begin
            if (Length(candidates) = 0) or (input_syllable_count < 3) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                candidate_is_unsupported_complete_local :=
                    (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    ((not is_fuzzy_pinyin_active) or
                    (candidates[in_idx_local].fuzzy_cost <= 0)) and
                    (candidates[in_idx_local].source <> cs_user) and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (get_candidate_text_unit_count(candidate_text_local) >= 3) and
                    (not has_complete_exact_phrase_support_local(
                    candidate_text_local));
                if candidate_is_unsupported_complete_local and
                    ((input_syllable_count = 3) or
                    (not candidates[in_idx_local].has_dict_weight) or
                    unsupported_four_syllable_ab_cd_runtime_complete_local(
                    candidate_text_local)) then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure ensure_complete_phrase_head_partial_visible_local(
            var candidates: TncCandidateList);
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_text_local: string;
            remaining_pinyin_local: string;
            head_results_local: TncCandidateList;
            head_candidate_local: TncCandidate;
            idx_local: Integer;
            existing_idx_local: Integer;
            target_idx_local: Integer;
            move_idx_local: Integer;
            found_local: Boolean;
        begin
            if (Length(candidates) = 0) or (input_syllable_count < 2) or
                (Trim(candidates[0].comment) <> '') or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) < 2 then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidates[0].text));
            if Length(units_local) < 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(units_local[0]);
            if (head_text_local = '') or
                (get_candidate_text_unit_count(head_text_local) <> 1) then
            begin
                Exit;
            end;

            remaining_pinyin_local := '';
            for idx_local := 1 to High(syllables_local) do
            begin
                remaining_pinyin_local := remaining_pinyin_local +
                    syllables_local[idx_local].text;
            end;
            if remaining_pinyin_local = '' then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(syllables_local[0].text,
                head_results_local) then
            begin
                Exit;
            end;

            found_local := False;
            FillChar(head_candidate_local, SizeOf(head_candidate_local), 0);
            for idx_local := 0 to High(head_results_local) do
            begin
                if (Trim(head_results_local[idx_local].comment) <> '') or
                    (Trim(head_results_local[idx_local].text) <>
                    head_text_local) or
                    (get_candidate_text_unit_count(Trim(
                    head_results_local[idx_local].text)) <> 1) then
                begin
                    Continue;
                end;

                head_candidate_local := head_results_local[idx_local];
                found_local := True;
                Break;
            end;
            if not found_local then
            begin
                Exit;
            end;

            head_candidate_local.comment := remaining_pinyin_local;
            if head_candidate_local.score >= candidates[0].score then
            begin
                head_candidate_local.score := candidates[0].score - 64;
            end;
            if head_candidate_local.has_dict_weight and
                (head_candidate_local.dict_weight >= candidates[0].score) then
            begin
                head_candidate_local.dict_weight := head_candidate_local.score;
            end;

            existing_idx_local := -1;
            for idx_local := 0 to High(candidates) do
            begin
                if SameText(Trim(candidates[idx_local].text),
                    head_candidate_local.text) and
                    SameText(Trim(candidates[idx_local].comment),
                    head_candidate_local.comment) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end;

            target_idx_local := 1;
            for idx_local := 1 to High(candidates) do
            begin
                if (Trim(candidates[idx_local].comment) <> '') and
                    (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) >= 2) then
                begin
                    target_idx_local := idx_local + 1;
                    Break;
                end;
            end;
            if existing_idx_local = target_idx_local then
            begin
                Exit;
            end;

            if existing_idx_local >= 0 then
            begin
                head_candidate_local := candidates[existing_idx_local];
                if existing_idx_local < target_idx_local then
                begin
                    // Removing an earlier item shifts the insertion point left.
                    Dec(target_idx_local);
                    for move_idx_local := existing_idx_local to
                        target_idx_local - 1 do
                    begin
                        candidates[move_idx_local] :=
                            candidates[move_idx_local + 1];
                    end;
                end
                else
                begin
                    for move_idx_local := existing_idx_local downto
                        target_idx_local + 1 do
                    begin
                        candidates[move_idx_local] :=
                            candidates[move_idx_local - 1];
                    end;
                end;
                candidates[target_idx_local] := head_candidate_local;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto target_idx_local + 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[target_idx_local] := head_candidate_local;
        end;

        procedure ensure_single_syllable_best_exact_visible_local(
            var candidates: TncCandidateList);
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            best_idx_local: Integer;
            existing_idx_local: Integer;
            move_idx_local: Integer;
            target_idx_local: Integer;
            best_weight_local: Integer;
            exact_weight_local: Integer;
            exact_text_local: string;
            exact_candidate_local: TncCandidate;
        begin
            if (Length(candidates) = 0) or (input_syllable_count <> 1) or
                (not is_full_pinyin_key(lookup_text)) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_weight_local := Low(Integer);
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    (exact_text_local = '') or
                    (get_candidate_text_unit_count(exact_text_local) <> 1) then
                begin
                    Continue;
                end;

                exact_weight_local := exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight >
                    exact_weight_local) then
                begin
                    exact_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end;

                if exact_weight_local > best_weight_local then
                begin
                    best_weight_local := exact_weight_local;
                    best_idx_local := exact_idx_local;
                end;
            end;

            if best_idx_local < 0 then
            begin
                Exit;
            end;

            exact_candidate_local := exact_results_local[best_idx_local];
            exact_candidate_local.comment := '';
            existing_idx_local := -1;
            for exact_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[exact_idx_local].comment) = '') and
                    SameText(Trim(candidates[exact_idx_local].text),
                    exact_candidate_local.text) then
                begin
                    existing_idx_local := exact_idx_local;
                    Break;
                end;
            end;

            if existing_idx_local = 0 then
            begin
                Exit;
            end;

            target_idx_local := 1;
            if Length(candidates) = 1 then
            begin
                target_idx_local := 0;
            end;

            if existing_idx_local >= 0 then
            begin
                if existing_idx_local <= c_default_page_size - 1 then
                begin
                    Exit;
                end;

                for move_idx_local := existing_idx_local downto target_idx_local + 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local - 1];
                end;
                candidates[target_idx_local] := exact_candidate_local;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto target_idx_local + 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[target_idx_local] := exact_candidate_local;
        end;

        procedure filter_misaligned_complete_candidates_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            query_syllables_local: TncPinyinParseResult;

            function complete_candidate_matches_query_units_local(
                const candidate: TncCandidate; const text_value: string): Boolean;
            var
                candidate_units_local: TArray<string>;
                unit_idx_local: Integer;
                exact_results_local: TncCandidateList;
                encoded_path_local: string;
                inferred_path_local: string;
                inferred_score_local: Integer;
                inferred_segments_local: Integer;
                transition_particle_path_local: string;
                transition_particle_evidence_local: Integer;

                function full_query_exact_matches_text_local: Boolean;
                var
                    local_idx: Integer;
                begin
                    Result := False;
                    if (m_dictionary = nil) or (Trim(text_value) = '') or
                        (not lookup_exact_full_pinyin_cached_local(lookup_text,
                        exact_results_local)) then
                    begin
                        Exit;
                    end;

                    for local_idx := 0 to High(exact_results_local) do
                    begin
                        if (Trim(exact_results_local[local_idx].comment) = '') and
                            SameText(Trim(exact_results_local[local_idx].text),
                            text_value) and
                            (get_candidate_text_unit_count(Trim(
                            exact_results_local[local_idx].text)) =
                            input_syllable_count) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                function build_segment_query_key_local(
                    const start_idx_local: Integer;
                    const count_local: Integer): string;
                var
                    query_idx_local: Integer;
                begin
                    Result := '';
                    if (start_idx_local < 0) or (count_local <= 0) or
                        (start_idx_local + count_local >
                        Length(query_syllables_local)) then
                    begin
                        Exit;
                    end;

                    for query_idx_local := start_idx_local to
                        start_idx_local + count_local - 1 do
                    begin
                        Result := Result + normalize_pinyin_text(
                            query_syllables_local[query_idx_local].text);
                    end;
                end;

                function segment_matches_query_local(
                    const start_idx_local: Integer;
                    const segment_value_local: string;
                    const segment_units_count_local: Integer): Boolean;
                var
                    segment_key_local: string;
                    segment_results_local: TncCandidateList;
                    segment_result_idx_local: Integer;

                    function structural_single_segment_matches_query_local(
                        const key_value_local: string;
                        const text_value_local: string): Boolean;
                    begin
                        Result := False;
                        if get_candidate_text_unit_count(text_value_local) <> 1 then
                        begin
                            Exit;
                        end;

                        if ((key_value_local = 'yi') and
                            (text_value_local = string(Char($4E00)))) or
                            (((key_value_local = 'er') or
                            (key_value_local = 'liang')) and
                            ((text_value_local = string(Char($4E8C))) or
                            (text_value_local = string(Char($4E24))))) or
                            ((key_value_local = 'san') and
                            (text_value_local = string(Char($4E09)))) or
                            ((key_value_local = 'si') and
                            (text_value_local = string(Char($56DB)))) or
                            ((key_value_local = 'wu') and
                            (text_value_local = string(Char($4E94)))) or
                            ((key_value_local = 'liu') and
                            (text_value_local = string(Char($516D)))) or
                            ((key_value_local = 'qi') and
                            (text_value_local = string(Char($4E03)))) or
                            ((key_value_local = 'ba') and
                            (text_value_local = string(Char($516B)))) or
                            ((key_value_local = 'jiu') and
                            (text_value_local = string(Char($4E5D)))) or
                            ((key_value_local = 'shi') and
                            (text_value_local = string(Char($5341)))) or
                            ((key_value_local = 'di') and
                            (text_value_local = string(Char($7B2C)))) or
                            ((key_value_local = 'zi') and
                            (text_value_local = string(Char($5B57)))) or
                            ((key_value_local = 'xiang') and
                            (text_value_local = string(Char($9879)))) or
                            ((key_value_local = 'ci') and
                            (text_value_local = string(Char($6B21)))) or
                            ((key_value_local = 'ge') and
                            (text_value_local = string(Char($4E2A)))) or
                            ((key_value_local = 'tiao') and
                            (text_value_local = string(Char($6761)))) or
                            ((key_value_local = 'zhang') and
                            (text_value_local = string(Char($5F20)))) or
                            ((key_value_local = 'dian') and
                            (text_value_local = string(Char($70B9)))) or
                            ((key_value_local = 'bu') and
                            (text_value_local = string(Char($6B65)))) or
                            ((key_value_local = 'lun') and
                            (text_value_local = string(Char($8F6E)))) or
                            ((key_value_local = 'jie') and
                            (text_value_local = string(Char($8282)))) or
                            ((key_value_local = 'ye') and
                            (text_value_local = string(Char($9875)))) or
                            ((key_value_local = 'duan') and
                            (text_value_local = string(Char($6BB5)))) or
                            ((key_value_local = 'hang') and
                            (text_value_local = string(Char($884C)))) or
                            ((key_value_local = 'lie') and
                            (text_value_local = string(Char($5217)))) then
                        begin
                            Exit(True);
                        end;
                    end;
                begin
                    Result := False;
                    if (segment_value_local = '') or
                        (segment_units_count_local <= 0) then
                    begin
                        Exit;
                    end;

                    segment_key_local := build_segment_query_key_local(
                        start_idx_local, segment_units_count_local);
                    if segment_key_local = '' then
                    begin
                        Exit;
                    end;

                    if segment_units_count_local = 1 then
                    begin
                        if structural_single_segment_matches_query_local(
                            segment_key_local, segment_value_local) then
                        begin
                            Exit(True);
                        end;
                        Exit(m_dictionary.single_char_matches_pinyin(
                            segment_key_local, segment_value_local));
                    end;

                    if not lookup_exact_full_pinyin_cached_local(
                        segment_key_local, segment_results_local) then
                    begin
                        Exit(False);
                    end;

                    for segment_result_idx_local := 0 to
                        High(segment_results_local) do
                    begin
                        if Trim(segment_results_local[segment_result_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if SameText(Trim(
                            segment_results_local[segment_result_idx_local].text),
                            segment_value_local) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                function encoded_path_matches_query_local(
                    const path_value_local: string): Boolean;
                var
                    local_path_segments: TArray<string>;
                    local_segment_idx: Integer;
                    local_segment_text: string;
                    local_segment_units: Integer;
                    local_query_offset: Integer;
                begin
                    Result := False;
                    if path_value_local = '' then
                    begin
                        Exit;
                    end;

                    local_path_segments := path_value_local.Split(
                        [c_segment_path_separator]);
                    local_query_offset := 0;
                    for local_segment_idx := 0 to High(local_path_segments) do
                    begin
                        local_segment_text := Trim(
                            local_path_segments[local_segment_idx]);
                        if local_segment_text = '' then
                        begin
                            Continue;
                        end;
                        local_segment_units := get_candidate_text_unit_count(
                            local_segment_text);
                        if (local_segment_units <= 0) or
                            (local_query_offset + local_segment_units >
                            input_syllable_count) or
                            (not segment_matches_query_local(local_query_offset,
                            local_segment_text, local_segment_units)) then
                        begin
                            Exit(False);
                        end;
                        Inc(local_query_offset, local_segment_units);
                    end;
                    Result := local_query_offset = input_syllable_count;
                end;
            begin
                Result := True;
                if is_fuzzy_pinyin_active and
                    (candidate.fuzzy_cost > 0) then
                begin
                    Exit;
                end;
                if (m_dictionary = nil) or (candidate.source = cs_user) or
                    (Trim(candidate.comment) <> '') or
                    (get_candidate_text_unit_count(text_value) <>
                    input_syllable_count) then
                begin
                    Exit;
                end;

                if Length(query_syllables_local) <> input_syllable_count then
                begin
                    Exit;
                end;

                if (candidate.source = cs_rule) and candidate.has_dict_weight and
                    (candidate.dict_weight > 0) then
                begin
                    Exit(True);
                end;

                if full_query_exact_matches_text_local then
                begin
                    Exit(True);
                end;

                if (input_syllable_count = 4) and
                    get_short_four_transition_particle_path_evidence(
                    lookup_text, text_value,
                    transition_particle_path_local,
                    transition_particle_evidence_local) then
                begin
                    Exit(True);
                end;

                if (candidate.source = cs_rule) and candidate.has_dict_weight and
                    (candidate.score >= 20000) then
                begin
                    Exit(True);
                end;

                encoded_path_local := get_segment_path_for_candidate(candidate);
                if encoded_path_local = '' then
                begin
                    inferred_path_local :=
                        infer_segment_path_for_query_text_with_score(lookup_text,
                        text_value, inferred_score_local,
                        inferred_segments_local);
                    if (inferred_path_local <> '') and
                        encoded_path_matches_query_local(inferred_path_local) then
                    begin
                        remember_segment_path_for_candidate(text_value, '',
                            inferred_path_local, candidate.score);
                        Exit(True);
                    end;
                end;

                if encoded_path_local <> '' then
                begin
                    if encoded_path_matches_query_local(encoded_path_local) then
                    begin
                        Exit(True);
                    end;

                    inferred_path_local :=
                        infer_segment_path_for_query_text_with_score(lookup_text,
                        text_value, inferred_score_local,
                        inferred_segments_local);
                    if (inferred_path_local <> '') and
                        (not SameText(inferred_path_local, encoded_path_local)) and
                        encoded_path_matches_query_local(inferred_path_local) then
                    begin
                        remember_segment_path_for_candidate(text_value, '',
                            inferred_path_local, candidate.score);
                        Exit(True);
                    end;

                    Exit(False);
                end;

                candidate_units_local := split_text_units(text_value);
                if Length(candidate_units_local) <> input_syllable_count then
                begin
                    Exit(False);
                end;

                for unit_idx_local := 0 to High(candidate_units_local) do
                begin
                    if (get_candidate_text_unit_count(
                        candidate_units_local[unit_idx_local]) <> 1) or
                        (not m_dictionary.single_char_matches_pinyin(
                        normalize_pinyin_text(
                        query_syllables_local[unit_idx_local].text),
                        candidate_units_local[unit_idx_local])) then
                    begin
                        Exit(False);
                    end;
                end;
            end;
        begin
            filtered_misaligned_complete_count_local := 0;
            if (Length(candidates) = 0) or (input_syllable_count <= 1) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    input_syllable_count) then
                begin
                    Inc(filtered_misaligned_complete_count_local);
                    Continue;
                end;
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (not complete_candidate_matches_query_units_local(
                    candidates[in_idx_local], candidate_text_local)) then
                begin
                    Inc(filtered_misaligned_complete_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure filter_rejected_user_candidate_texts_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            penalty_local: Integer;
        begin
            if (m_dictionary = nil) or (lookup_text = '') or
                (Length(candidates) = 0) then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if (candidate_text_local <> '') and
                    (Trim(candidates[in_idx_local].comment) = '') then
                begin
                    penalty_local := m_dictionary.get_candidate_penalty(
                        lookup_text, candidate_text_local);
                    if (penalty_local > 0) and
                        ((candidates[in_idx_local].source = cs_user) or
                        (not m_dictionary.is_base_entry(lookup_text,
                        candidate_text_local))) then
                    begin
                        Continue;
                    end;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;

            if out_idx_local < Length(candidates) then
            begin
                SetLength(candidates, out_idx_local);
            end;
        end;

        procedure ensure_lexicon_longer_prefix_complete_visible_local(
            var candidates: TncCandidateList);
        const
            c_prefix_total_limit = 6;
            c_prefix_base_bonus = 2100;
            c_prefix_unit_bonus = 260;
            c_prefix_complete_bonus = 980;
            c_prefix_remaining_penalty = 90;
        var
            prefix_results_local: TncCandidateList;
            prefix_candidates_local: TncCandidateList;
            prefix_idx_local: Integer;
            accepted_total_local: Integer;
            candidate_text_local: string;
            prefix_text_local: string;
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            candidate_local: TncCandidate;
            base_score_local: Integer;

            function is_safe_longer_prefix_suffix_local(const units: TArray<string>;
                const suffix_start_idx: Integer): Boolean;
            var
                suffix_head: string;
            begin
                Result := False;
                if (suffix_start_idx < 0) or (suffix_start_idx > High(units)) then
                begin
                    Exit;
                end;

                suffix_head := units[suffix_start_idx];
                if get_candidate_text_unit_count(suffix_head) <> 1 then
                begin
                    Exit;
                end;

                Result :=
                    (suffix_head = string(Char($533A))) or
                    (suffix_head = string(Char($5E02))) or
                    (suffix_head = string(Char($7701))) or
                    (suffix_head = string(Char($53BF))) or
                    (suffix_head = string(Char($9547))) or
                    (suffix_head = string(Char($4E61))) or
                    (suffix_head = string(Char($8DEF))) or
                    (suffix_head = string(Char($8857))) or
                    (suffix_head = string(Char($9053))) or
                    (suffix_head = string(Char($4E0B))) or
                    (suffix_head = string(Char($4E0A))) or
                    (suffix_head = string(Char($4E2D))) or
                    (suffix_head = string(Char($5185))) or
                    (suffix_head = string(Char($5916))) or
                    (suffix_head = string(Char($524D))) or
                    (suffix_head = string(Char($540E))) or
                    (suffix_head = string(Char($4E1C))) or
                    (suffix_head = string(Char($897F))) or
                    (suffix_head = string(Char($5357))) or
                    (suffix_head = string(Char($5317))) or
                    (suffix_head = string(Char($8FB9))) or
                    (suffix_head = string(Char($9762))) or
                    (suffix_head = string(Char($7248))) or
                    (suffix_head = string(Char($53F7))) or
                    (suffix_head = string(Char($5E97)));
            end;
        begin
            if (m_dictionary = nil) or (lookup_text = '') or
                (input_syllable_count < 2) or (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_full_pinyin_prefix(lookup_text,
                prefix_results_local) then
            begin
                Exit;
            end;

            SetLength(prefix_candidates_local, 0);
            accepted_total_local := 0;
            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if accepted_total_local >= c_prefix_total_limit then
                begin
                    Break;
                end;
                if (prefix_results_local[prefix_idx_local].source = cs_user) or
                    (Trim(prefix_results_local[prefix_idx_local].comment) <> '') then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if candidate_text_local = '' then
                begin
                    Continue;
                end;

                text_units_local := split_text_units(candidate_text_local);
                if Length(text_units_local) <= input_syllable_count then
                begin
                    Continue;
                end;

                prefix_text_local := '';
                for unit_idx_local := 0 to input_syllable_count - 1 do
                begin
                    prefix_text_local := prefix_text_local +
                        text_units_local[unit_idx_local];
                end;

                if (prefix_text_local = '') or
                    m_dictionary.is_base_entry(lookup_text, prefix_text_local) then
                begin
                    Continue;
                end;
                if not is_safe_longer_prefix_suffix_local(text_units_local,
                    input_syllable_count) then
                begin
                    Continue;
                end;

                candidate_local := prefix_results_local[prefix_idx_local];
                candidate_local.text := prefix_text_local;
                candidate_local.comment := '';
                base_score_local := candidate_local.score;
                if candidate_local.has_dict_weight and
                    (candidate_local.dict_weight > base_score_local) then
                begin
                    base_score_local := candidate_local.dict_weight;
                end;
                if base_score_local < 0 then
                begin
                    base_score_local := 0;
                end;

                candidate_local.score := base_score_local + c_prefix_base_bonus +
                    (input_syllable_count * c_prefix_unit_bonus) +
                    c_prefix_complete_bonus -
                    ((Length(text_units_local) - input_syllable_count) *
                    c_prefix_remaining_penalty);
                candidate_local.dict_weight := base_score_local;
                candidate_local.has_dict_weight := True;

                SetLength(prefix_candidates_local,
                    Length(prefix_candidates_local) + 1);
                prefix_candidates_local[High(prefix_candidates_local)] :=
                    candidate_local;
                Inc(accepted_total_local);
            end;

            if Length(prefix_candidates_local) <= 0 then
            begin
                Exit;
            end;

            candidates := merge_candidate_lists(prefix_candidates_local,
                candidates, 0);
        end;

        procedure note_finalize_debug_elapsed_local(const phase_name: string;
            const phase_start_tick_local: UInt64);
        var
            elapsed_ms_local: UInt64;
        begin
            if not m_config.debug_mode then
            begin
                Exit;
            end;

            elapsed_ms_local := nc_monotonic_tick_ms - phase_start_tick_local;
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' %s=%d', [phase_name, elapsed_ms_local]);
        end;

        procedure ensure_final_supported_complete_candidate_local(
            var candidates: TncCandidateList);
        const
            c_probe_limit = 8;
            c_promote_margin = 1024;
            c_support_weight = 12;
        var
            idx_local: Integer;
            best_idx_local: Integer;
            top_score_local: Integer;
            best_score_local: Integer;
            candidate_score_local: Integer;
            picked_local: TncCandidate;

            function get_fixed_head_bonus_local(
                const text_value_local: string): Integer;
            begin
                Result := 0;
            end;

            function try_get_bridge_prefix_for_syllable_local(
                const syllable_text_local: string; out out_text_local: string): Boolean;
            var
                normalized_local: string;
            begin
                Result := True;
                out_text_local := '';
                normalized_local := normalize_pinyin_text(syllable_text_local);
                if normalized_local = 'bu' then
                    out_text_local := string(Char($4E0D))
                else if normalized_local = 'zai' then
                    out_text_local := string(Char($5728))
                else if normalized_local = 'you' then
                    out_text_local := string(Char($6709))
                else if normalized_local = 'ye' then
                    out_text_local := string(Char($4E5F))
                else if normalized_local = 'hai' then
                    out_text_local := string(Char($8FD8))
                else if normalized_local = 'dou' then
                    out_text_local := string(Char($90FD))
                else if normalized_local = 'jiu' then
                    out_text_local := string(Char($5C31))
                else if normalized_local = 'de' then
                    out_text_local := string(Char($7684))
                else if normalized_local = 'dui' then
                    out_text_local := string(Char($5BF9))
                else if normalized_local = 'dao' then
                    out_text_local := string(Char($5230))
                else if normalized_local = 'cong' then
                    out_text_local := string(Char($4ECE))
                else if normalized_local = 'he' then
                    out_text_local := string(Char($548C))
                else if normalized_local = 'yu' then
                    out_text_local := string(Char($4E0E))
                else if normalized_local = 'ba' then
                    out_text_local := string(Char($628A))
                else if normalized_local = 'bei' then
                    out_text_local := string(Char($88AB))
                else if normalized_local = 'gei' then
                    out_text_local := string(Char($7ED9))
                else if normalized_local = 'rang' then
                    out_text_local := string(Char($8BA9))
                else if normalized_local = 'suo' then
                    out_text_local := string(Char($6240))
                else if normalized_local = 'shi' then
                    out_text_local := string(Char($662F))
                else if normalized_local = 'yi' then
                    out_text_local := string(Char($4E00))
                else
                    Result := False;
            end;

            function is_directional_location_phrase_text_local(
                const text_value_local: string): Boolean;
            var
                text_units_local: TArray<string>;
                head_text_local: string;
                tail_text_local: string;
            begin
                Result := False;
                text_units_local := split_text_units(Trim(text_value_local));
                if Length(text_units_local) <> 2 then
                begin
                    Exit;
                end;

                head_text_local := text_units_local[0];
                tail_text_local := text_units_local[1];
                if (get_candidate_text_unit_count(head_text_local) <> 1) or
                    (get_candidate_text_unit_count(tail_text_local) <> 1) then
                begin
                    Exit;
                end;

                case Ord(head_text_local[1]) of
                    $5317, $5357, $4E1C, $897F, $524D, $540E, $4E0A, $4E0B,
                    $91CC, $5185, $5916, $5DE6, $53F3:
                        case Ord(tail_text_local[1]) of
                            $9762, $8FB9, $4FA7, $65B9, $90E8, $7AEF, $5934:
                                Result := True;
                        end;
                end;
            end;

            function get_final_complete_score_local(
                const candidate_local: TncCandidate): Integer;
            var
                candidate_text_local: string;
                support_score_local: Integer;
                support_segments_local: Integer;
                support_path_local: string;
                units_local: TArray<string>;
                syllables_local: TncPinyinParseResult;
                start_idx_local: Integer;
                span_units_local: Integer;
                query_idx_local: Integer;
                query_key_local: string;
                chunk_text_local: string;
                exact_results_local: TncCandidateList;
                result_idx_local: Integer;
                best_exact_text_local: string;
                best_exact_weight_local: Integer;
                chunk_exact_weight_local: Integer;
                candidate_weight_local: Integer;
                candidate_contextual_weight_local: Integer;
                has_direction_context_local: Boolean;
                bridge_prefix_text_local: string;
                bridge_tail_units_local: Integer;
                bridge_tail_key_local: string;
                bridge_tail_text_local: string;
                bridge_tail_weight_local: Integer;
                bridge_span_text_local: string;

                function is_place_context_unit_local(
                    const unit_value_local: string): Boolean;
                begin
                    Result := (unit_value_local = string(Char($5BB6))) or
                        (unit_value_local = string(Char($5E97))) or
                        (unit_value_local = string(Char($5C4B))) or
                        (unit_value_local = string(Char($697C))) or
                        (unit_value_local = string(Char($57CE))) or
                        (unit_value_local = string(Char($6751))) or
                        (unit_value_local = string(Char($53BF))) or
                        (unit_value_local = string(Char($5E02))) or
                        (unit_value_local = string(Char($6821))) or
                        (unit_value_local = string(Char($9662))) or
                        (unit_value_local = string(Char($5382))) or
                        (unit_value_local = string(Char($9986))) or
                        (unit_value_local = string(Char($7AD9))) or
                        (unit_value_local = string(Char($5C40))) or
                        (unit_value_local = string(Char($6240))) or
                        (unit_value_local = string(Char($56ED))) or
                        (unit_value_local = string(Char($533A))) or
                        (unit_value_local = string(Char($5708))) or
                        (unit_value_local = string(Char($8DEF))) or
                        (unit_value_local = string(Char($8857))) or
                        (unit_value_local = string(Char($95E8))) or
                        (unit_value_local = string(Char($5BA4))) or
                        (unit_value_local = string(Char($623F))) or
                        (unit_value_local = string(Char($5385))) or
                        (unit_value_local = string(Char($573A))) or
                        (unit_value_local = string(Char($53F8)));
                end;

                function has_directional_location_span_context_local(
                    const local_start_idx: Integer;
                    const local_span_units: Integer): Boolean;
                var
                    next_key_local: string;
                begin
                    Result := False;
                    if (local_span_units <> 2) or (local_start_idx <= 0) or
                        (local_start_idx > High(units_local)) or
                        (not is_place_context_unit_local(
                        units_local[local_start_idx - 1])) then
                    begin
                        Exit;
                    end;

                    if local_start_idx + local_span_units >=
                        Length(syllables_local) then
                    begin
                        Exit(True);
                    end;

                    next_key_local := normalize_pinyin_text(
                        syllables_local[local_start_idx +
                        local_span_units].text);
                    Result := SameText(next_key_local, 'de');
                end;

                function get_directional_contextual_weight_local(
                    const text_value_local: string;
                    const raw_weight_local: Integer;
                    const has_direction_context_value_local: Boolean): Integer;
                const
                    c_final_directional_context_bonus = 12000;
                begin
                    Result := raw_weight_local;
                    if has_direction_context_value_local and
                        is_directional_location_phrase_text_local(
                        text_value_local) then
                    begin
                        Inc(Result, c_final_directional_context_bonus);
                    end;
                end;
            begin
                Result := Low(Integer);
                if Trim(candidate_local.comment) <> '' then
                begin
                    Exit;
                end;

                candidate_text_local := Trim(candidate_local.text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    input_syllable_count) or
                    is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, candidate_local) then
                begin
                    Exit;
                end;

                Result := get_rank_score(candidate_local);
                support_path_local := infer_segment_path_for_query_text_with_score(
                    lookup_text, candidate_text_local, support_score_local,
                    support_segments_local);
                if support_path_local <> '' then
                begin
                    Inc(Result, support_score_local * c_support_weight);
                    if support_segments_local > 0 then
                    begin
                        Inc(Result, 8192 - (support_segments_local * 384));
                    end;
                end
                else
                begin
                    Dec(Result, 4096);
                end;
                Inc(Result, get_fixed_head_bonus_local(candidate_text_local));

                units_local := split_text_units(candidate_text_local);
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (Length(units_local) = input_syllable_count) and
                    (Length(syllables_local) = input_syllable_count) and
                    (m_dictionary <> nil) then
                begin
                    for start_idx_local := 1 to input_syllable_count - 3 do
                    begin
                        if has_directional_location_span_context_local(
                            start_idx_local, 2) and
                            is_directional_location_phrase_text_local(
                            units_local[start_idx_local] +
                            units_local[start_idx_local + 1]) then
                        begin
                            Inc(Result, 18000);
                            Break;
                        end;
                    end;

                    for start_idx_local := 0 to input_syllable_count - 1 do
                    begin
                        for span_units_local := 2 to Min(4,
                            input_syllable_count - start_idx_local) do
                        begin
                            query_key_local := '';
                            for query_idx_local := start_idx_local to
                                start_idx_local + span_units_local - 1 do
                            begin
                                query_key_local := query_key_local +
                                    normalize_pinyin_text(
                                    syllables_local[query_idx_local].text);
                            end;
                            if (query_key_local = '') or
                                (not m_dictionary.lookup_exact_full_pinyin(
                                query_key_local, exact_results_local)) then
                            begin
                                Continue;
                            end;

                            chunk_text_local := '';
                            for query_idx_local := start_idx_local to
                                start_idx_local + span_units_local - 1 do
                            begin
                                chunk_text_local := chunk_text_local +
                                    units_local[query_idx_local];
                            end;

                            best_exact_text_local := '';
                            best_exact_weight_local := Low(Integer);
                            chunk_exact_weight_local := Low(Integer);
                            has_direction_context_local :=
                                has_directional_location_span_context_local(
                                start_idx_local, span_units_local);
                            for result_idx_local := 0 to Min(5,
                                High(exact_results_local)) do
                            begin
                                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                if get_candidate_text_unit_count(
                                    Trim(exact_results_local[result_idx_local].text)) <>
                                    span_units_local then
                                begin
                                    Continue;
                                end;

                                if exact_results_local[result_idx_local].has_dict_weight then
                                begin
                                    candidate_weight_local :=
                                        exact_results_local[result_idx_local].dict_weight;
                                end
                                else
                                begin
                                    candidate_weight_local :=
                                        exact_results_local[result_idx_local].score;
                                end;
                                candidate_contextual_weight_local :=
                                    get_directional_contextual_weight_local(
                                    Trim(exact_results_local[result_idx_local].text),
                                    candidate_weight_local,
                                    has_direction_context_local);
                                if candidate_contextual_weight_local >
                                    best_exact_weight_local then
                                begin
                                    best_exact_weight_local :=
                                        candidate_contextual_weight_local;
                                    best_exact_text_local := Trim(
                                        exact_results_local[result_idx_local].text);
                                end;
                                if SameText(Trim(exact_results_local[result_idx_local].text),
                                    chunk_text_local) and
                                    (candidate_contextual_weight_local >
                                    chunk_exact_weight_local) then
                                begin
                                    chunk_exact_weight_local :=
                                        candidate_contextual_weight_local;
                                end;
                            end;
                            if best_exact_text_local = '' then
                            begin
                                Continue;
                            end;

                            if SameText(chunk_text_local, best_exact_text_local) then
                            begin
                                case span_units_local of
                                    2:
                                        Inc(Result, 3200);
                                    3:
                                        Inc(Result, 7200);
                                else
                                    Inc(Result, 12000);
                                end;
                            end
                            else if best_exact_weight_local >= 1200 then
                            begin
                                case span_units_local of
                                    2:
                                        Dec(Result, 3600);
                                    3:
                                        Dec(Result, 8800);
                                else
                                    Dec(Result, 14000);
                                end;
                            end;

                            if (span_units_local = 2) and
                                (chunk_exact_weight_local > Low(Integer)) and
                                is_directional_location_phrase_text_local(
                                chunk_text_local) and
                                has_direction_context_local then
                            begin
                                Inc(Result, 9600 +
                                    Min(2400, chunk_exact_weight_local div 2));
                            end;
                        end;
                    end;

                    for start_idx_local := 0 to input_syllable_count - 3 do
                    begin
                        if not try_get_bridge_prefix_for_syllable_local(
                            syllables_local[start_idx_local].text,
                            bridge_prefix_text_local) then
                        begin
                            Continue;
                        end;
                        for bridge_tail_units_local := 2 to Min(4,
                            input_syllable_count - start_idx_local - 1) do
                        begin
                            bridge_tail_key_local := '';
                            for query_idx_local := start_idx_local + 1 to
                                start_idx_local + bridge_tail_units_local do
                            begin
                                bridge_tail_key_local := bridge_tail_key_local +
                                    normalize_pinyin_text(
                                    syllables_local[query_idx_local].text);
                            end;
                            if (bridge_tail_key_local = '') or
                                (not m_dictionary.lookup_exact_full_pinyin(
                                bridge_tail_key_local, exact_results_local)) then
                            begin
                                Continue;
                            end;

                            bridge_tail_text_local := '';
                            bridge_tail_weight_local := Low(Integer);
                            for result_idx_local := 0 to Min(5,
                                High(exact_results_local)) do
                            begin
                                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                if get_candidate_text_unit_count(
                                    Trim(exact_results_local[result_idx_local].text)) <>
                                    bridge_tail_units_local then
                                begin
                                    Continue;
                                end;
                                if exact_results_local[result_idx_local].has_dict_weight then
                                begin
                                    candidate_weight_local :=
                                        exact_results_local[result_idx_local].dict_weight;
                                end
                                else
                                begin
                                    candidate_weight_local :=
                                        exact_results_local[result_idx_local].score;
                                end;
                                if candidate_weight_local > bridge_tail_weight_local then
                                begin
                                    bridge_tail_weight_local := candidate_weight_local;
                                    bridge_tail_text_local := Trim(
                                        exact_results_local[result_idx_local].text);
                                end;
                            end;
                            if (bridge_tail_text_local = '') or
                                (bridge_tail_weight_local < 500) then
                            begin
                                Continue;
                            end;

                            bridge_span_text_local := '';
                            for query_idx_local := start_idx_local to
                                start_idx_local + bridge_tail_units_local do
                            begin
                                bridge_span_text_local := bridge_span_text_local +
                                    units_local[query_idx_local];
                            end;
                            if SameText(bridge_span_text_local,
                                bridge_prefix_text_local + bridge_tail_text_local) then
                            begin
                                Inc(Result, 9000 + bridge_tail_units_local * 1800);
                            end
                            else
                            begin
                                Dec(Result, 9000 + bridge_tail_units_local * 1800);
                            end;
                        end;
                    end;
                end;
            end;
        begin
            if (Length(candidates) <= 1) or (input_syllable_count < 4) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            top_score_local := get_final_complete_score_local(candidates[0]);
            best_idx_local := -1;
            best_score_local := top_score_local;
            for idx_local := 1 to Min(c_probe_limit - 1, High(candidates)) do
            begin
                candidate_score_local := get_final_complete_score_local(
                    candidates[idx_local]);
                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_idx_local := idx_local;
                end;
            end;

            if (best_idx_local <= 0) or
                (best_score_local <= top_score_local + c_promote_margin) then
            begin
                Exit;
            end;

            picked_local := candidates[best_idx_local];
            for idx_local := best_idx_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := picked_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' ffinal=[promote text=%s score=%d top=%d]',
                    [Trim(picked_local.text), best_score_local, top_score_local]);
            end;
        end;

        procedure promote_highest_scored_complete_candidate_local(
            var candidates: TncCandidateList);
        const
            c_probe_limit = 8;
            c_promote_margin = 512;
        var
            idx_local: Integer;
            best_idx_local: Integer;
            best_score_local: Integer;
            top_score_local: Integer;
            adjusted_score_local: Integer;
            picked_local: TncCandidate;

            function is_place_context_unit_for_candidate_local(
                const unit_value_local: string): Boolean;
            begin
                Result := (unit_value_local = string(Char($5BB6))) or
                    (unit_value_local = string(Char($5E97))) or
                    (unit_value_local = string(Char($5C4B))) or
                    (unit_value_local = string(Char($697C))) or
                    (unit_value_local = string(Char($57CE))) or
                    (unit_value_local = string(Char($6751))) or
                    (unit_value_local = string(Char($53BF))) or
                    (unit_value_local = string(Char($5E02))) or
                    (unit_value_local = string(Char($6821))) or
                    (unit_value_local = string(Char($9662))) or
                    (unit_value_local = string(Char($5382))) or
                    (unit_value_local = string(Char($9986))) or
                    (unit_value_local = string(Char($7AD9))) or
                    (unit_value_local = string(Char($5C40))) or
                    (unit_value_local = string(Char($6240))) or
                    (unit_value_local = string(Char($56ED))) or
                    (unit_value_local = string(Char($533A))) or
                    (unit_value_local = string(Char($5708))) or
                    (unit_value_local = string(Char($8DEF))) or
                    (unit_value_local = string(Char($8857))) or
                    (unit_value_local = string(Char($95E8))) or
                    (unit_value_local = string(Char($5BA4))) or
                    (unit_value_local = string(Char($623F))) or
                    (unit_value_local = string(Char($5385))) or
                    (unit_value_local = string(Char($573A))) or
                    (unit_value_local = string(Char($53F8)));
            end;

            function exact_span_contains_text_local(const local_start_idx: Integer;
                const local_span_units: Integer; const text_value_local: string):
                Boolean;
            var
                syllables_local: TncPinyinParseResult;
                query_idx_local: Integer;
                query_key_local: string;
                exact_results_local: TncCandidateList;
                result_idx_local: Integer;
            begin
                Result := False;
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (local_start_idx < 0) or (local_span_units <= 0) or
                    (local_start_idx + local_span_units >
                    Length(syllables_local)) then
                begin
                    Exit;
                end;

                query_key_local := '';
                for query_idx_local := local_start_idx to
                    local_start_idx + local_span_units - 1 do
                begin
                    query_key_local := query_key_local + normalize_pinyin_text(
                        syllables_local[query_idx_local].text);
                end;
                if (query_key_local = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for result_idx_local := 0 to High(exact_results_local) do
                begin
                    if Trim(exact_results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if SameText(Trim(exact_results_local[result_idx_local].text),
                        text_value_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function is_directional_location_phrase_for_candidate_local(
                const text_value_local: string): Boolean;
            var
                units_local: TArray<string>;
                first_unit_local: string;
                second_unit_local: string;
            begin
                Result := False;
                units_local := split_text_units(Trim(text_value_local));
                if Length(units_local) <> 2 then
                begin
                    Exit;
                end;

                first_unit_local := units_local[0];
                second_unit_local := units_local[1];
                if not (SameText(first_unit_local, string(Char($5317))) or
                    SameText(first_unit_local, string(Char($5357))) or
                    SameText(first_unit_local, string(Char($4E1C))) or
                    SameText(first_unit_local, string(Char($897F))) or
                    SameText(first_unit_local, string(Char($524D))) or
                    SameText(first_unit_local, string(Char($540E))) or
                    SameText(first_unit_local, string(Char($4E0A))) or
                    SameText(first_unit_local, string(Char($4E0B))) or
                    SameText(first_unit_local, string(Char($91CC))) or
                    SameText(first_unit_local, string(Char($5185))) or
                    SameText(first_unit_local, string(Char($5916))) or
                    SameText(first_unit_local, string(Char($5DE6))) or
                    SameText(first_unit_local, string(Char($53F3)))) then
                begin
                    Exit;
                end;

                Result := SameText(second_unit_local, string(Char($9762))) or
                    SameText(second_unit_local, string(Char($8FB9))) or
                    SameText(second_unit_local, string(Char($4FA7))) or
                    SameText(second_unit_local, string(Char($65B9))) or
                    SameText(second_unit_local, string(Char($90E8)));
            end;

            function get_directional_context_promote_bonus_local(
                const candidate: TncCandidate): Integer;
            const
                c_directional_context_candidate_bonus = 12000;
            var
                candidate_text_local: string;
                units_local: TArray<string>;
                syllables_local: TncPinyinParseResult;
                start_idx_local: Integer;
                span_text_local: string;
                next_key_local: string;
            begin
                Result := 0;
                if (m_dictionary = nil) or (Trim(candidate.comment) <> '') then
                begin
                    Exit;
                end;

                candidate_text_local := Trim(candidate.text);
                units_local := split_text_units(candidate_text_local);
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (Length(units_local) <> input_syllable_count) or
                    (Length(syllables_local) <> input_syllable_count) then
                begin
                    Exit;
                end;

                for start_idx_local := 1 to input_syllable_count - 3 do
                begin
                    if not is_place_context_unit_for_candidate_local(
                        units_local[start_idx_local - 1]) then
                    begin
                        Continue;
                    end;
                    next_key_local := normalize_pinyin_text(
                        syllables_local[start_idx_local + 2].text);
                    if not SameText(next_key_local, 'de') then
                    begin
                        Continue;
                    end;
                    span_text_local := units_local[start_idx_local] +
                        units_local[start_idx_local + 1];
                    if is_directional_location_phrase_for_candidate_local(
                        span_text_local) and
                        exact_span_contains_text_local(start_idx_local, 2,
                        span_text_local) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                Format(' dirctxpromote=[text=%s span=%s idx=%d]',
                                [candidate_text_local, span_text_local,
                                start_idx_local]);
                        end;
                        Exit(c_directional_context_candidate_bonus);
                    end;
                end;
            end;
        begin
            if (Length(candidates) <= 1) or (input_syllable_count < 4) then
            begin
                Exit;
            end;
            best_idx_local := -1;
            best_score_local := Low(Integer);
            for idx_local := 0 to Min(c_probe_limit - 1, High(candidates)) do
            begin
                if (Trim(candidates[idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) <>
                    input_syllable_count) or
                    (is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, candidates[idx_local]) and
                    (get_segment_path_for_candidate(candidates[idx_local]) = '')) then
                begin
                    Continue;
                end;
                adjusted_score_local := candidates[idx_local].score +
                    get_directional_context_promote_bonus_local(
                    candidates[idx_local]);
                if (best_idx_local < 0) or
                    (adjusted_score_local > best_score_local) then
                begin
                    best_idx_local := idx_local;
                    best_score_local := adjusted_score_local;
                end;
            end;
            if best_idx_local <= 0 then
            begin
                Exit;
            end;
            top_score_local := candidates[0].score +
                get_directional_context_promote_bonus_local(candidates[0]);
            if best_score_local <= top_score_local + c_promote_margin then
            begin
                Exit;
            end;

            picked_local := candidates[best_idx_local];
            for idx_local := best_idx_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := picked_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' rawscorepromote=[text=%s score=%d top=%d]',
                    [Trim(picked_local.text), best_score_local, top_score_local]);
            end;
        end;

        procedure filter_empty_text_candidates_local(var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
        begin
            if Length(candidates) = 0 then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                if Trim(candidates[in_idx_local].text) = '' then
                begin
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure filter_exact_single_syllable_prefix_partials_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            full_results_local: TncCandidateList;
            full_idx_local: Integer;
            has_full_multi_anchor_local: Boolean;
            has_plain_single_local: Boolean;

            function is_full_multi_anchor_candidate_local(
                const candidate: TncCandidate): Boolean;
            var
                candidate_text_local: string;
                anchor_idx_local: Integer;
            begin
                Result := False;
                if (not has_full_multi_anchor_local) or
                    (Trim(candidate.comment) <> '') then
                begin
                    Exit;
                end;

                candidate_text_local := Trim(candidate.text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <= 1) then
                begin
                    Exit;
                end;

                for anchor_idx_local := 0 to High(full_results_local) do
                begin
                    if (Trim(full_results_local[anchor_idx_local].comment) = '') and
                        SameText(Trim(full_results_local[anchor_idx_local].text),
                        candidate_text_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (Length(candidates) = 0) or (input_syllable_count <> 1) then
            begin
                Exit;
            end;

            has_full_multi_anchor_local := False;
            if (m_dictionary <> nil) and (lookup_text <> '') and
                m_dictionary.lookup(lookup_text, full_results_local) then
            begin
                for full_idx_local := 0 to High(full_results_local) do
                begin
                    if (Trim(full_results_local[full_idx_local].comment) = '') and
                        (get_candidate_text_unit_count(Trim(
                        full_results_local[full_idx_local].text)) > 1) then
                    begin
                        has_full_multi_anchor_local := True;
                        Break;
                    end;
                end;
            end;

            has_plain_single_local := False;
            for in_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(
                    candidates[in_idx_local].text)) = 1) then
                begin
                    has_plain_single_local := True;
                    Break;
                end;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                if Trim(candidates[in_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(
                    candidates[in_idx_local].text)) <> 1 then
                begin
                    if has_plain_single_local and
                        (not is_full_multi_anchor_candidate_local(
                        candidates[in_idx_local])) then
                    begin
                        Continue;
                    end;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        function should_use_fast_two_syllable_complete_exit_local: Boolean;
        var
            top_text_local: string;
        begin
            Result := False;
            if (input_syllable_count <> 2) or head_only_multi_syllable or
                (Length(m_candidates) = 0) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            if Trim(m_candidates[0].comment) <> '' then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            Result := (top_text_local <> '') and
                (get_candidate_text_unit_count(top_text_local) = 2) and
                has_complete_exact_phrase_support_local(top_text_local);
        end;

        procedure filter_two_syllable_fast_complete_candidates_local(
            var candidates: TncCandidateList);
        var
            exact_results_local: TncCandidateList;
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;

            function exact_complete_text_exists_local(
                const text_value: string): Boolean;
            var
                exact_idx_local: Integer;
            begin
                Result := False;
                if text_value = '' then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(Trim(exact_results_local[exact_idx_local].text),
                        text_value) and
                        (get_candidate_text_unit_count(Trim(
                        exact_results_local[exact_idx_local].text)) = 2) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (Length(candidates) = 0) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if candidate_text_local = '' then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if Trim(candidates[in_idx_local].comment) <> '' then
                begin
                    if out_idx_local <> in_idx_local then
                    begin
                        candidates[out_idx_local] := candidates[in_idx_local];
                    end;
                    Inc(out_idx_local);
                    Continue;
                end;

                if get_candidate_text_unit_count(candidate_text_local) <> 2 then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if (candidates[in_idx_local].source <> cs_user) and
                    (not exact_complete_text_exists_local(candidate_text_local)) then
                begin
                    Inc(filtered_constructed_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;
    begin
        mark_debug_target_candidate_stage_local(2, m_candidates);
        filtered_misaligned_complete_count_local := 0;
        deduped_candidate_count_local := 0;
        best_complete_exact_text_local := '';
        strong_tail_completion_text_local := '';
        strong_tail_completion_score_local := 0;
        if should_use_fast_two_syllable_complete_exit_local then
        begin
            finalize_lookup_timing_info;
            phase_start_tick := nc_monotonic_tick_ms;
            filter_empty_text_candidates_local(m_candidates);
            filter_two_syllable_fast_complete_candidates_local(m_candidates);
            note_finalize_debug_elapsed_local('s2filter', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_learned_exact_query_choice_visible_local(m_candidates);
            note_finalize_debug_elapsed_local('s2learn', phase_start_tick);
            filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
                m_candidates, filtered_misaligned_complete_count_local);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            note_finalize_debug_elapsed_local('s2exact', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            dedupe_visible_candidates_local(m_candidates,
                deduped_candidate_count_local);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            note_finalize_debug_elapsed_local('s2partial', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            filter_non_bmp_single_char_candidates(m_candidates);
            filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
                m_candidates, filtered_misaligned_complete_count_local);
            ensure_two_syllable_quantity_phrase_candidate_visible(m_candidates);
            filter_explicit_apostrophe_output_complete_local(m_candidates);
            filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
                m_candidates, filtered_misaligned_complete_count_local);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            note_finalize_debug_elapsed_local('s2final', phase_start_tick);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra +
                    ' short2finalfast=1';
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
                if filtered_constructed_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' filter_constructed=%d',
                        [filtered_constructed_count_local]);
                end;
                if deduped_candidate_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' dedupe=%d', [deduped_candidate_count_local]);
                end;
            end;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;

        phase_start_tick := nc_monotonic_tick_ms;
        ensure_explicit_apostrophe_aligned_complete_visible_local(m_candidates);
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        if (not head_only_multi_syllable) and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 4) and
            (input_syllable_count <= 6) then
        begin
            ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
        end;
        finalize_modal_tail_sentence_candidate(m_candidates);
        note_finalize_debug_elapsed_local('tfin_pre', phase_start_tick);
        finalize_lookup_timing_info;
        phase_start_tick := nc_monotonic_tick_ms;
        if (not head_only_multi_syllable) and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 4) and
            (input_syllable_count <= 6) then
        begin
            ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
        end;
        finalize_modal_tail_sentence_candidate(m_candidates);
        ensure_best_tail_exact_completion_visible_local(m_candidates);
        ensure_best_complete_exact_phrase_visible_local(m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        ensure_learned_exact_query_choice_visible_local(m_candidates);
        if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 4) then
        begin
            ensure_incomplete_tail_partial_precedes_completed_extension(
                m_candidates);
        end;
        note_finalize_debug_elapsed_local('tfin_exact', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        filter_empty_text_candidates_local(m_candidates);
        filter_misaligned_complete_candidates_local(m_candidates);
        mark_debug_target_candidate_stage_local(4, m_candidates);
        filter_constructed_complete_candidates_with_exact_phrase_local(
            m_candidates);
        mark_debug_target_candidate_stage_local(8, m_candidates);
        ensure_complete_phrase_head_partial_visible_local(m_candidates);
        ensure_single_syllable_best_exact_visible_local(m_candidates);
        filter_rejected_user_candidate_texts_local(m_candidates);
        ensure_lexicon_longer_prefix_complete_visible_local(m_candidates);
        ensure_best_complete_exact_phrase_visible_local(m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        dedupe_visible_candidates_local(m_candidates,
            deduped_candidate_count_local);
        promote_highest_scored_complete_candidate_local(m_candidates);
        ensure_final_supported_complete_candidate_local(m_candidates);
        if should_trim_strong_long_sentence_candidates_local(m_candidates) then
        begin
            trim_sorted_candidates_for_strong_long_sentence(m_candidates);
        end;
        mark_debug_target_candidate_stage_local(16, m_candidates);
        ensure_high_quality_complete_candidate_in_top2(m_candidates);
        ensure_best_complete_exact_phrase_visible_local(m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        ensure_bounded_single_char_partial_pool(m_candidates);
        ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
        ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
        ensure_long_sentence_prefix_partial_visible(m_candidates,
            get_candidate_limit);
        demote_low_evidence_admin_place_alias_top_local(m_candidates);
        filter_non_bmp_single_char_candidates(m_candidates);
        filter_exact_single_syllable_prefix_partials_local(m_candidates);
        promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
        ensure_learned_exact_query_choice_visible_local(m_candidates);
        filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
            m_candidates, filtered_misaligned_complete_count_local);
        if input_syllable_count = 2 then
        begin
            ensure_two_syllable_quantity_phrase_candidate_visible(m_candidates);
        end;
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
            m_candidates, filtered_misaligned_complete_count_local);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        if (Length(m_candidates) = 0) and has_multi_syllable_input and
            is_full_pinyin_key(lookup_text) and (input_syllable_count <= 6) then
        begin
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
            end;
            ensure_complete_phrase_head_partial_visible_local(m_candidates);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
        end;
        note_finalize_debug_elapsed_local('tfin_filter', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        mark_debug_target_candidate_stage_local(32, m_candidates);
        note_finalize_debug_elapsed_local('tfin_path', phase_start_tick);
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if m_last_three_syllable_partial_preference_kind > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' b3pref=%d',
                    [m_last_three_syllable_partial_preference_kind]);
            end;
            if m_last_three_syllable_partial_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_three_syllable_partial_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            if filtered_constructed_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' filter_constructed=%d',
                    [filtered_constructed_count_local]);
            end;
            if filtered_misaligned_complete_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' filter_misaligned=%d',
                    [filtered_misaligned_complete_count_local]);
            end;
            if deduped_candidate_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' dedupe=%d', [deduped_candidate_count_local]);
            end;
        end;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    procedure finalize_stable_consensus_candidates_and_prepare_exit(
        const debug_extra: string);
    var
        stable_top_text: string;
        stable_top_candidate: TncCandidate;
        stable_top_complete: Boolean;
        stable_top_index: Integer;
        stable_top_idx: Integer;
    begin
        mark_debug_target_candidate_stage_local(64, m_candidates);
        stable_top_text := '';
        FillChar(stable_top_candidate, SizeOf(stable_top_candidate), 0);
        stable_top_complete := (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            input_syllable_count);
        if stable_top_complete then
        begin
            stable_top_text := Trim(m_candidates[0].text);
            stable_top_candidate := m_candidates[0];
        end;

        finalize_modal_tail_sentence_candidate(m_candidates);
        filter_self_composed_complete_candidates_when_normal_complete_exists;
        mark_debug_target_candidate_stage_local(128, m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        sort_candidates_lightweight(m_candidates);
        if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 4) then
        begin
            ensure_incomplete_tail_partial_precedes_completed_extension(
                m_candidates);
        end;
        if should_trim_strong_long_sentence_candidates_local(m_candidates) then
        begin
            trim_sorted_candidates_for_strong_long_sentence(m_candidates);
        end;
        mark_debug_target_candidate_stage_local(256, m_candidates);
        promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        if stable_top_complete and (stable_top_text <> '') and
            (Length(m_candidates) > 0) and
            (not ((Trim(m_candidates[0].comment) = '') and
            SameText(Trim(m_candidates[0].text), stable_top_text))) then
        begin
            stable_top_index := -1;
            for stable_top_idx := 1 to High(m_candidates) do
            begin
                if (Trim(m_candidates[stable_top_idx].comment) = '') and
                    SameText(Trim(m_candidates[stable_top_idx].text),
                    stable_top_text) then
                begin
                    stable_top_index := stable_top_idx;
                    stable_top_candidate := m_candidates[stable_top_idx];
                    Break;
                end;
            end;
            if stable_top_index > 0 then
            begin
                for stable_top_idx := stable_top_index downto 1 do
                begin
                    m_candidates[stable_top_idx] :=
                        m_candidates[stable_top_idx - 1];
                end;
                m_candidates[0] := stable_top_candidate;
            end;
        end;
        finalize_lookup_timing_info;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        mark_debug_target_candidate_stage_local(512, m_candidates);
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
        end;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    procedure ensure_best_natural_prefix_candidate_visible_local(
        var candidates: TncCandidateList);
    const
        c_natural_prefix_scan_limit = 48;
    var
        query_syllables_local: TncPinyinParseResult;
        best_idx_local: Integer;
        best_quality_local: Integer;
        best_units_local: Integer;
        idx_local: Integer;
        candidate_quality_local: Integer;
        candidate_units_local: Integer;
        top_quality_local: Integer;
        top_units_local: Integer;
        moved_candidate_local: TncCandidate;
        tail_key_local: string;
        has_natural_candidate_local: Boolean;
        top_is_natural_local: Boolean;
        stable_single_keys_local: TArray<string>;
        stable_single_values_local: TArray<string>;
        stable_single_cache_count_local: Integer;
        strong_phrase_cache_local: TArray<Integer>;

        function build_syllable_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_len <= 0) or
                (local_start_idx + local_len > Length(query_syllables_local)) then
            begin
                Exit;
            end;
            for syllable_idx_local := local_start_idx to
                local_start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    query_syllables_local[syllable_idx_local].text);
            end;
        end;

        function get_stable_single_text_local(const syllable_text: string): string;
        var
            normalized_syllable_local: string;
            lookup_results_local: TncCandidateList;
            idx_local: Integer;
            candidate_text_local: string;
            cache_idx_local: Integer;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            for cache_idx_local := 0 to stable_single_cache_count_local - 1 do
            begin
                if SameText(stable_single_keys_local[cache_idx_local],
                    normalized_syllable_local) then
                begin
                    Result := stable_single_values_local[cache_idx_local];
                    Exit;
                end;
            end;

            if (normalized_syllable_local = '') or (m_dictionary = nil) or
                (not m_dictionary.lookup(normalized_syllable_local,
                lookup_results_local)) then
            begin
                SetLength(stable_single_keys_local,
                    stable_single_cache_count_local + 1);
                SetLength(stable_single_values_local,
                    stable_single_cache_count_local + 1);
                stable_single_keys_local[stable_single_cache_count_local] :=
                    normalized_syllable_local;
                stable_single_values_local[stable_single_cache_count_local] := '';
                Inc(stable_single_cache_count_local);
                Exit;
            end;

            for idx_local := 0 to High(lookup_results_local) do
            begin
                if Trim(lookup_results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(lookup_results_local[idx_local].text);
                if (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) = 1) then
                begin
                    Result := candidate_text_local;
                    SetLength(stable_single_keys_local,
                        stable_single_cache_count_local + 1);
                    SetLength(stable_single_values_local,
                        stable_single_cache_count_local + 1);
                    stable_single_keys_local[stable_single_cache_count_local] :=
                        normalized_syllable_local;
                    stable_single_values_local[stable_single_cache_count_local] :=
                        Result;
                    Inc(stable_single_cache_count_local);
                    Exit;
                end;
            end;
            SetLength(stable_single_keys_local, stable_single_cache_count_local + 1);
            SetLength(stable_single_values_local,
                stable_single_cache_count_local + 1);
            stable_single_keys_local[stable_single_cache_count_local] :=
                normalized_syllable_local;
            stable_single_values_local[stable_single_cache_count_local] := '';
            Inc(stable_single_cache_count_local);
        end;

        procedure set_strong_phrase_cache_local(const local_len: Integer;
            const local_value: Boolean);
        begin
            if (local_len >= 0) and (local_len < Length(strong_phrase_cache_local)) then
            begin
                if local_value then
                begin
                    strong_phrase_cache_local[local_len] := 2;
                end
                else
                begin
                    strong_phrase_cache_local[local_len] := 1;
                end;
            end;
        end;

        function has_strong_phrase_for_span_local(const local_len: Integer): Boolean;
        var
            local_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            if (local_len >= 0) and (local_len < Length(strong_phrase_cache_local)) then
            begin
                if strong_phrase_cache_local[local_len] = 1 then
                begin
                    Exit(False);
                end;
                if strong_phrase_cache_local[local_len] = 2 then
                begin
                    Exit(True);
                end;
            end;

            if local_len < 2 then
            begin
                set_strong_phrase_cache_local(local_len, False);
                Exit;
            end;

            local_key := build_syllable_key_local(0, local_len);
            if local_key = '' then
            begin
                set_strong_phrase_cache_local(local_len, False);
                Exit;
            end;

            if (m_dictionary <> nil) and
                (m_dictionary.lookup_exact_full_pinyin(local_key, local_results) or
                m_dictionary.lookup(local_key, local_results)) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(local_results[local_idx].text);
                    if get_candidate_text_unit_count(candidate_text_local) =
                        local_len then
                    begin
                        set_strong_phrase_cache_local(local_len, True);
                        Exit(True);
                    end;
                end;
            end;
            set_strong_phrase_cache_local(local_len, False);
        end;

        function score_candidate_local(const candidate_local: TncCandidate;
            out out_units_local: Integer): Integer;
        var
            candidate_text_local: string;
            candidate_units_list_local: TArray<string>;
            anchor_len_local: Integer;
            anchor_key_local: string;
            anchor_results_local: TncCandidateList;
            anchor_units_local: TArray<string>;
            split_idx_local: Integer;
            mismatch_local: Boolean;
            stable_text_local: string;
            effective_score_local: Integer;
            anchor_idx_local: Integer;
            best_anchor_text_local: string;
            occupied_units_local: TArray<Boolean>;
            covered_units_local: Integer;
            span_len_local: Integer;
            start_idx_local: Integer;
            query_span_key_local: string;
            span_text_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_score_local: Integer;
            best_exact_score_local: Integer;

            function build_query_span_key_local(const local_start_idx: Integer;
                const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (local_start_idx < 0) or (local_span_len <= 0) or
                    (local_start_idx + local_span_len > Length(query_syllables_local)) then
                begin
                    Exit;
                end;
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        query_syllables_local[local_idx].text);
                end;
            end;

            function join_units_local(const local_start_idx: Integer;
                const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + candidate_units_list_local[local_idx];
                end;
            end;

            function any_occupied_local(const local_start_idx: Integer;
                const local_span_len: Integer): Boolean;
            var
                local_idx: Integer;
            begin
                Result := False;
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    if occupied_units_local[local_idx] then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := Low(Integer);
            out_units_local := 0;
            candidate_text_local := Trim(candidate_local.text);
            if candidate_text_local = '' then
            begin
                Exit;
            end;

            candidate_units_list_local := split_text_units(candidate_text_local);
            out_units_local := Length(candidate_units_list_local);
            if (out_units_local <= 0) or (out_units_local > input_syllable_count) then
            begin
                Exit(Low(Integer));
            end;

            tail_key_local := normalize_pinyin_text(Trim(candidate_local.comment));
            if (tail_key_local <> '') and (not is_full_pinyin_key(tail_key_local)) then
            begin
                Exit(Low(Integer));
            end;

            Result := 0;
            for anchor_len_local := 2 to Min(3, out_units_local) do
            begin
                anchor_key_local := build_syllable_key_local(0, anchor_len_local);
                if anchor_key_local = '' then
                begin
                    Continue;
                end;

                best_anchor_text_local := '';
                if (m_dictionary = nil) or
                    (not (m_dictionary.lookup_exact_full_pinyin(anchor_key_local,
                    anchor_results_local) or
                    m_dictionary.lookup(anchor_key_local, anchor_results_local))) then
                begin
                    Continue;
                end;

                for anchor_idx_local := 0 to High(anchor_results_local) do
                begin
                    if Trim(anchor_results_local[anchor_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    best_anchor_text_local := Trim(
                        anchor_results_local[anchor_idx_local].text);
                    if get_candidate_text_unit_count(best_anchor_text_local) =
                        anchor_len_local then
                    begin
                        Break;
                    end;
                    best_anchor_text_local := '';
                end;
                if best_anchor_text_local = '' then
                begin
                    Continue;
                end;

                anchor_units_local := split_text_units(Trim(best_anchor_text_local));
                if Length(anchor_units_local) <> anchor_len_local then
                begin
                    Continue;
                end;

                mismatch_local := False;
                for split_idx_local := 0 to anchor_len_local - 1 do
                begin
                    if not SameText(candidate_units_list_local[split_idx_local],
                        anchor_units_local[split_idx_local]) then
                    begin
                        mismatch_local := True;
                        Break;
                    end;
                end;
                if mismatch_local then
                begin
                    Dec(Result, 7000);
                end
                else
                begin
                    Inc(Result, 5200);
                end;
                Break;
            end;

            for split_idx_local := 1 to out_units_local - 1 do
            begin
                stable_text_local := get_stable_single_text_local(
                    query_syllables_local[split_idx_local].text);
                if stable_text_local = '' then
                begin
                    Continue;
                end;
                if not has_strong_phrase_for_span_local(split_idx_local) then
                begin
                    Continue;
                end;
                if SameText(candidate_units_list_local[split_idx_local],
                    stable_text_local) then
                begin
                    Inc(Result, 2800);
                end
                else
                begin
                    Dec(Result, 4200);
                end;
            end;

            SetLength(occupied_units_local, out_units_local);
            covered_units_local := 0;
            for span_len_local := Min(6, out_units_local) downto 2 do
            begin
                for start_idx_local := 0 to out_units_local - span_len_local do
                begin
                    if any_occupied_local(start_idx_local, span_len_local) then
                    begin
                        Continue;
                    end;

                    query_span_key_local := build_query_span_key_local(
                        start_idx_local, span_len_local);
                    if query_span_key_local = '' then
                    begin
                        Continue;
                    end;
                    span_text_local := join_units_local(start_idx_local,
                        span_len_local);
                    if span_text_local = '' then
                    begin
                        Continue;
                    end;

                    exact_score_local := Low(Integer);
                    best_exact_score_local := Low(Integer);
                    if (m_dictionary <> nil) and
                        lookup_exact_full_pinyin_cached_local(
                        query_span_key_local, exact_results_local) then
                    begin
                        for exact_idx_local := 0 to High(exact_results_local) do
                        begin
                            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            exact_text_local := Trim(
                                exact_results_local[exact_idx_local].text);
                            if (exact_text_local = '') or
                                (get_candidate_text_unit_count(exact_text_local) <>
                                span_len_local) then
                            begin
                                Continue;
                            end;
                            if exact_results_local[exact_idx_local].score >
                                best_exact_score_local then
                            begin
                                best_exact_score_local :=
                                    exact_results_local[exact_idx_local].score;
                            end;
                            if SameText(exact_text_local, span_text_local) then
                            begin
                                exact_score_local :=
                                    exact_results_local[exact_idx_local].score;
                            end;
                        end;
                    end;

                    if (exact_score_local = Low(Integer)) or
                        ((span_len_local = 2) and (exact_score_local < 4096)) then
                    begin
                        if (span_len_local >= 3) and
                            (best_exact_score_local >= 8192) then
                        begin
                            Dec(Result, span_len_local * 2200);
                        end;
                        Continue;
                    end;

                    for split_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_units_local[split_idx_local] := True;
                    end;
                    Inc(covered_units_local, span_len_local);
                    Inc(Result, span_len_local * 1500);
                    Inc(Result, Min(1600, Max(0, exact_score_local div 8)));
                end;
            end;

            if covered_units_local >= Max(4, out_units_local - 2) then
            begin
                Inc(Result, 3200);
            end;

            if tail_key_local = '' then
            begin
                Inc(Result, 1200);
            end
            else
            begin
                Inc(Result, 800);
            end;

            Inc(Result, out_units_local * 1200);
            effective_score_local := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > effective_score_local) then
            begin
                effective_score_local := candidate_local.dict_weight;
            end;
            Inc(Result, Min(1800, Max(0, effective_score_local div 16)));
        end;

        function candidate_is_natural_local(const candidate_local: TncCandidate;
            out out_units_local: Integer): Boolean;
        var
            candidate_text_local: string;
            candidate_units_list_local: TArray<string>;
            anchor_syllables_local: Integer;
            anchor_key_local: string;
            anchor_results_local: TncCandidateList;
            anchor_idx_local: Integer;
            anchor_text_local: string;
            anchor_units_local: TArray<string>;
            unit_idx_local: Integer;
            stable_text_local: string;
        begin
            Result := False;
            out_units_local := 0;
            candidate_text_local := Trim(candidate_local.text);
            if candidate_text_local = '' then
            begin
                Exit;
            end;

            candidate_units_list_local := split_text_units(candidate_text_local);
            out_units_local := Length(candidate_units_list_local);
            if (out_units_local <= 0) or (out_units_local > input_syllable_count) then
            begin
                Exit;
            end;

            tail_key_local := normalize_pinyin_text(Trim(candidate_local.comment));
            if (tail_key_local <> '') and (not is_full_pinyin_key(tail_key_local)) then
            begin
                Exit;
            end;

            for anchor_syllables_local := 2 to Min(3, out_units_local) do
            begin
                anchor_key_local := build_syllable_key_local(0, anchor_syllables_local);
                if (anchor_key_local = '') or (m_dictionary = nil) then
                begin
                    Continue;
                end;
                if not (m_dictionary.lookup_exact_full_pinyin(anchor_key_local,
                    anchor_results_local) or
                    m_dictionary.lookup(anchor_key_local, anchor_results_local)) then
                begin
                    Continue;
                end;

                anchor_text_local := '';
                for anchor_idx_local := 0 to High(anchor_results_local) do
                begin
                    if Trim(anchor_results_local[anchor_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    anchor_text_local := Trim(anchor_results_local[anchor_idx_local].text);
                    if get_candidate_text_unit_count(anchor_text_local) =
                        anchor_syllables_local then
                    begin
                        Break;
                    end;
                    anchor_text_local := '';
                end;
                if anchor_text_local = '' then
                begin
                    Continue;
                end;

                anchor_units_local := split_text_units(anchor_text_local);
                if Length(anchor_units_local) <> anchor_syllables_local then
                begin
                    Continue;
                end;

                for unit_idx_local := 0 to anchor_syllables_local - 1 do
                begin
                    if not SameText(candidate_units_list_local[unit_idx_local],
                        anchor_units_local[unit_idx_local]) then
                    begin
                        Exit(False);
                    end;
                end;
                Break;
            end;

            for unit_idx_local := 1 to out_units_local - 1 do
            begin
                stable_text_local := get_stable_single_text_local(
                    query_syllables_local[unit_idx_local].text);
                if (stable_text_local = '') or
                    (not has_strong_phrase_for_span_local(unit_idx_local)) then
                begin
                    Continue;
                end;
                if not SameText(candidate_units_list_local[unit_idx_local],
                    stable_text_local) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;
    begin
        if (Length(candidates) <= 1) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        if Length(candidates) > c_natural_prefix_scan_limit then
        begin
            Exit;
        end;
        if input_syllable_count > 10 then
        begin
            Exit;
        end;
        if input_syllable_count <= 8 then
        begin
            { Keep this repair pass out of short/medium queries. It is a
              defensive postprocess, not the primary decoder, and can be
              expensive on noisy candidate sets. }
            Exit;
        end;

        query_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(query_syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        stable_single_cache_count_local := 0;
        SetLength(stable_single_keys_local, 0);
        SetLength(stable_single_values_local, 0);
        SetLength(strong_phrase_cache_local, input_syllable_count + 1);

        top_quality_local := score_candidate_local(candidates[0], top_units_local);
        top_is_natural_local := candidate_is_natural_local(candidates[0],
            top_units_local);
        best_idx_local := -1;
        best_quality_local := top_quality_local;
        best_units_local := top_units_local;
        has_natural_candidate_local := top_is_natural_local;
        for idx_local := 1 to Min(High(candidates),
            c_natural_prefix_scan_limit - 1) do
        begin
            candidate_quality_local := score_candidate_local(candidates[idx_local],
                candidate_units_local);
            if candidate_quality_local = Low(Integer) then
            begin
                Continue;
            end;

            if candidate_is_natural_local(candidates[idx_local],
                candidate_units_local) then
            begin
                if (not has_natural_candidate_local) or
                    (candidate_quality_local > best_quality_local + 2400) or
                    (((candidate_quality_local + 2400) >= best_quality_local) and
                    (candidate_units_local > best_units_local)) or
                    ((candidate_units_local = best_units_local) and
                    (candidate_quality_local > best_quality_local)) then
                begin
                    best_idx_local := idx_local;
                    best_quality_local := candidate_quality_local;
                    best_units_local := candidate_units_local;
                    has_natural_candidate_local := True;
                end;
            end
            else if (not has_natural_candidate_local) and
                ((candidate_quality_local > best_quality_local) or
                ((candidate_quality_local = best_quality_local) and
                (candidate_units_local > best_units_local))) then
            begin
                best_idx_local := idx_local;
                best_quality_local := candidate_quality_local;
                best_units_local := candidate_units_local;
            end;
        end;

        if (best_idx_local > 0) and
            ((has_natural_candidate_local and
            ((not top_is_natural_local) or
            (best_quality_local > top_quality_local + 2400) or
            (((best_quality_local + 2400) >= top_quality_local) and
            (best_units_local > top_units_local)) or
            ((best_units_local = top_units_local) and
            (best_quality_local > top_quality_local)))) or
            ((not has_natural_candidate_local) and
            ((best_quality_local >= top_quality_local + 2400) or
            (top_quality_local < 0)))) then
        begin
            moved_candidate_local := candidates[best_idx_local];
            while best_idx_local > 0 do
            begin
                candidates[best_idx_local] := candidates[best_idx_local - 1];
                Dec(best_idx_local);
            end;
            candidates[0] := moved_candidate_local;
        end;
    end;

    function promote_stable_prefix_partial_candidate_local(
        var candidates: TncCandidateList; const min_head_units: Integer): Boolean; forward;
    function has_strong_top_complete_candidate_for_postprocess_lock_local(
        const candidates: TncCandidateList): Boolean; forward;
    function is_natural_predictive_segment_path_local(
        const encoded_path: string): Boolean; forward;
    procedure ensure_reliable_full_query_exact_complete_candidate_fast_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_full_query_exact_complete_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_exact_span_rank_repair_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_three_syllable_prefix_one_plus_two_complete_candidates_visible(
        var candidates: TncCandidateList); forward;
    procedure ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
        var candidates: TncCandidateList); forward;
    procedure prepare_short_two_single_pair_candidates_local; forward;
    procedure ensure_short_two_single_pair_candidates_visible_local(
        var candidates: TncCandidateList); forward;
    procedure prepare_short_three_exact_pair_candidates_local; forward;
    function is_short_three_exact_pair_candidate_supported_local(
        const candidate_text: string): Boolean; forward;
    procedure ensure_short_three_exact_pair_candidates_visible_local(
        var candidates: TncCandidateList); forward;
    procedure promote_short_unsupported_complete_prefix_partial(
        var candidates: TncCandidateList); forward;
    procedure promote_full_query_exact_complete_phrase_local(
        var candidates: TncCandidateList); forward;
    procedure finalize_preserved_candidates_and_prepare_exit(
        const debug_extra: string; const preserved_top: TncCandidate;
        const allow_exact_span_repair: Boolean = True); forward;
    function build_fixed_wo_tail_dictionary_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean; forward;
    function build_short_particle_tail_dictionary_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean; forward;
    function build_short_particle_tail_from_prefix_partials_fast_local(
        const query_text: string; const prefix_candidates: TncCandidateList;
        out out_candidates: TncCandidateList): Boolean; forward;
    function has_extendable_same_syllable_tail_local(const pinyin_key: string;
        const expected_syllable_count: Integer): Boolean; forward;
    function has_short_extendable_tail_prefix_local: Boolean; forward;

    procedure finalize_prefix_partial_candidates_and_prepare_exit(
        const debug_extra: string; const allow_tail_resolution: Boolean = False;
        const allow_expensive_prefix_rerank: Boolean = True;
        const allow_dangling_initial_prediction: Boolean = True);
    var
        fuzzy_short_candidates_local: TncCandidateList;
        filtered_prefix_misaligned_count_local: Integer;
        deduped_prefix_candidate_count_local: Integer;
        top_partial_head_units_local: Integer;
        phase_start_tick_local: UInt64;
        fast_stable_prefix_partial_exit_local: Boolean;

        function skip_expensive_short_prefix_rerank_local: Boolean;
        begin
            Result := not allow_expensive_prefix_rerank;
        end;

        procedure note_prefix_phase_elapsed_local(const phase_name: string;
            const started_at: UInt64);
        var
            elapsed_local: UInt64;
        begin
            if not m_config.debug_mode then
            begin
                Exit;
            end;
            elapsed_local := nc_monotonic_tick_ms - started_at;
            if elapsed_local > 0 then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' %s=%d', [phase_name, elapsed_local]);
            end;
        end;

        procedure ensure_short_particle_tail_from_prefix_partial_visible_local(
            var candidates: TncCandidateList);
        const
            c_particle_tail_min_head_score = 80;
            c_particle_tail_visible_bonus = 160;
            c_particle_tail_unit_bonus = 24;
            c_particle_tail_weak_bonus = 8;
        var
            normalized_query_local: string;
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_syllables_local: TncPinyinParseResult;
            head_units_local: Integer;
        head_results_local: TncCandidateList;
        candidate_idx_local: Integer;
        head_idx_local: Integer;
        existing_idx_local: Integer;
        head_text_local: string;
            partial_tail_local: string;
            completion_local: TncCandidate;
            completion_candidates_local: TncCandidateList;
            score_local: Integer;
            completion_score_local: Integer;
        exact_head_matches_local: Boolean;
        changed_local: Boolean;

            function text_ends_with_tail_particle_local(
                const text_value: string; const tail_text_value: string): Boolean;
            var
                trimmed_text_local: string;
                last_char_local: string;
            begin
                Result := False;
                trimmed_text_local := Trim(text_value);
                if (trimmed_text_local = '') or (tail_text_value = '') then
                begin
                    Exit;
                end;
                last_char_local := Copy(trimmed_text_local,
                    Length(trimmed_text_local), 1);
                Result := SameText(last_char_local, tail_text_value);
            end;

            function derived_particle_tail_score_local(
                const base_score: Integer): Integer;
            begin
                Result := base_score;
                if Result < 0 then
                begin
                    Result := 0;
                end;
                if Result < c_particle_tail_min_head_score then
                begin
                    Inc(Result, c_particle_tail_weak_bonus);
                    Exit;
                end;

                Inc(Result, c_particle_tail_visible_bonus +
                    (head_units_local * c_particle_tail_unit_bonus));
            end;
        begin
            if (Length(candidates) = 0) or (m_dictionary = nil) then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            if not try_get_short_particle_tail_query_parts(
                normalized_query_local, tail_key_local, tail_text_local,
                head_key_local) then
            begin
                Exit;
            end;
            if head_key_local = '' then
            begin
                Exit;
            end;

            head_syllables_local := get_effective_compact_pinyin_syllables(
                head_key_local);
            head_units_local := Length(head_syllables_local);
            if (head_units_local < 2) or (head_units_local > 3) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(head_key_local,
                head_results_local) then
            begin
                Exit;
            end;

            SetLength(completion_candidates_local, 0);
            changed_local := False;
            for candidate_idx_local := 0 to High(candidates) do
            begin
                partial_tail_local := normalize_pinyin_text(
                    Trim(candidates[candidate_idx_local].comment));
                if partial_tail_local <> tail_key_local then
                begin
                    Continue;
                end;

                head_text_local := Trim(candidates[candidate_idx_local].text);
                if (head_text_local = '') or
                    (get_candidate_text_unit_count(head_text_local) <>
                    head_units_local) or
                    text_ends_with_tail_particle_local(head_text_local,
                    tail_text_local) then
                begin
                    Continue;
                end;

                exact_head_matches_local := False;
                for head_idx_local := 0 to High(head_results_local) do
                begin
                    if (Trim(head_results_local[head_idx_local].comment) = '') and
                        SameText(Trim(head_results_local[head_idx_local].text),
                        head_text_local) then
                    begin
                        exact_head_matches_local := True;
                        Break;
                    end;
                end;
                if not exact_head_matches_local then
                begin
                    Continue;
                end;

                score_local := candidates[candidate_idx_local].score;
                if candidates[candidate_idx_local].has_dict_weight and
                    (candidates[candidate_idx_local].dict_weight > score_local) then
                begin
                    score_local := candidates[candidate_idx_local].dict_weight;
                end;
                completion_score_local :=
                    derived_particle_tail_score_local(score_local);

                for existing_idx_local := 0 to High(candidates) do
                begin
                    if (Trim(candidates[existing_idx_local].comment) = '') and
                        SameText(Trim(candidates[existing_idx_local].text),
                        head_text_local + tail_text_local) then
                    begin
                        if candidates[existing_idx_local].score <
                            completion_score_local then
                        begin
                            candidates[existing_idx_local].score :=
                                completion_score_local;
                            changed_local := True;
                        end;
                        if candidates[existing_idx_local].dict_weight <
                            score_local then
                        begin
                            candidates[existing_idx_local].dict_weight :=
                                score_local;
                            candidates[existing_idx_local].has_dict_weight := True;
                            changed_local := True;
                        end;
                        exact_head_matches_local := False;
                        Break;
                    end;
                end;
                if not exact_head_matches_local then
                begin
                    Continue;
                end;

                FillChar(completion_local, SizeOf(completion_local), 0);
                completion_local.text := head_text_local + tail_text_local;
                completion_local.comment := '';
                completion_local.source := cs_rule;
                completion_local.score := completion_score_local;
                completion_local.has_dict_weight := True;
                completion_local.dict_weight := score_local;

                SetLength(completion_candidates_local,
                    Length(completion_candidates_local) + 1);
                completion_candidates_local[High(completion_candidates_local)] :=
                    completion_local;
            end;

            if Length(completion_candidates_local) = 0 then
            begin
                if changed_local then
                begin
                    sort_candidates_lightweight(candidates);
                end;
                Exit;
            end;

            candidates := merge_candidate_lists(completion_candidates_local,
                candidates, 0);
            sort_candidates_lightweight(candidates);
        end;

        procedure filter_misaligned_complete_candidates_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            query_syllables_local: TncPinyinParseResult;

            function short_particle_tail_dictionary_matches_text_local(
                const text_value: string): Boolean;
            var
                particle_candidates_local: TncCandidateList;
                particle_idx_local: Integer;
            begin
                Result := False;
                if not build_short_particle_tail_dictionary_candidates(
                    lookup_text, particle_candidates_local) then
                begin
                    Exit;
                end;

                for particle_idx_local := 0 to High(particle_candidates_local) do
                begin
                    if (Trim(particle_candidates_local[
                        particle_idx_local].comment) = '') and
                        SameText(Trim(particle_candidates_local[
                        particle_idx_local].text), text_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function complete_candidate_matches_query_units_local(
                const candidate: TncCandidate; const text_value: string): Boolean;
            var
                candidate_units_local: TArray<string>;
                unit_idx_local: Integer;
                exact_results_local: TncCandidateList;
                transition_particle_path_local: string;
                transition_particle_evidence_local: Integer;

                function full_query_exact_matches_text_local: Boolean;
                var
                    local_idx: Integer;
                begin
                    Result := False;
                    if (m_dictionary = nil) or (Trim(text_value) = '') or
                        (not lookup_exact_full_pinyin_cached_local(lookup_text,
                        exact_results_local)) then
                    begin
                        Exit;
                    end;

                    for local_idx := 0 to High(exact_results_local) do
                    begin
                        if (Trim(exact_results_local[local_idx].comment) = '') and
                            SameText(Trim(exact_results_local[local_idx].text),
                            text_value) and
                            (get_candidate_text_unit_count(Trim(
                            exact_results_local[local_idx].text)) =
                            input_syllable_count) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                function fixed_wo_tail_dictionary_matches_text_local: Boolean;
                var
                    head_pinyin_local: string;
                    head_text_local: string;
                    tail_key_local: string;
                    tail_units_local: Integer;
                    tail_text_local: string;
                begin
                    Result := False;
                    if not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                        input_syllable_count, head_pinyin_local,
                        head_text_local, tail_key_local, tail_units_local) then
                    begin
                        Exit;
                    end;

                    if (get_candidate_text_unit_count(text_value) <>
                        input_syllable_count) or
                        (Copy(text_value, 1, 1) <> head_text_local) then
                    begin
                        Exit;
                    end;

                    tail_text_local := Copy(text_value, 2, MaxInt);
                    if (tail_key_local = '') or (tail_text_local = '') then
                    begin
                        Exit;
                    end;

                    Result := m_dictionary.is_base_entry(tail_key_local,
                        tail_text_local) or m_dictionary.is_user_entry(
                        tail_key_local, tail_text_local);
                end;

                function compact_interrogative_location_matches_text_local:
                    Boolean;
                var
                    normalized_query_local: string;
                begin
                    normalized_query_local := LowerCase(Trim(lookup_text));
                    Result := (((normalized_query_local = 'naer') or
                        (normalized_query_local = 'ner')) and
                        SameText(text_value, string(Char($54EA)) +
                        string(Char($513F)))) or
                        (((normalized_query_local = 'qunaer') or
                        (normalized_query_local = 'qner')) and
                        SameText(text_value, string(Char($53BB)) +
                        string(Char($54EA)) + string(Char($513F)))) or
                        ((normalized_query_local = 'nli') and
                        SameText(text_value, string(Char($54EA)) +
                        string(Char($91CC)))) or
                        ((normalized_query_local = 'qnli') and
                        SameText(text_value, string(Char($53BB)) +
                        string(Char($54EA)) + string(Char($91CC))));
                end;

            begin
                Result := True;
                if is_fuzzy_pinyin_active and
                    (candidate.fuzzy_cost > 0) then
                begin
                    Exit;
                end;
                if (m_dictionary = nil) or (candidate.source = cs_user) or
                    (Trim(candidate.comment) <> '') or
                    ((get_candidate_text_unit_count(text_value) <>
                    input_syllable_count) and
                    (not short_particle_tail_dictionary_matches_text_local(
                    text_value))) then
                begin
                    Exit;
                end;

                if Length(query_syllables_local) <> input_syllable_count then
                begin
                    Exit;
                end;

                if full_query_exact_matches_text_local then
                begin
                    Exit(True);
                end;

                if (input_syllable_count = 4) and
                    short_four_two_exact_path_is_supported_local(
                    query_syllables_local, text_value) then
                begin
                    Exit(True);
                end;

                if (input_syllable_count = 4) and
                    get_short_four_transition_particle_path_evidence(
                    lookup_text, text_value,
                    transition_particle_path_local,
                    transition_particle_evidence_local) then
                begin
                    Exit(True);
                end;

                if fixed_wo_tail_dictionary_matches_text_local then
                begin
                    Exit(True);
                end;

                if short_particle_tail_dictionary_matches_text_local(
                    text_value) then
                begin
                    Exit(True);
                end;

                if compact_interrogative_location_matches_text_local then
                begin
                    Exit(True);
                end;

                candidate_units_local := split_text_units(text_value);
                if Length(candidate_units_local) <> input_syllable_count then
                begin
                    Exit(False);
                end;

                for unit_idx_local := 0 to High(candidate_units_local) do
                begin
                    if (get_candidate_text_unit_count(
                        candidate_units_local[unit_idx_local]) <> 1) or
                        (not m_dictionary.single_char_matches_pinyin(
                        normalize_pinyin_text(
                        query_syllables_local[unit_idx_local].text),
                        candidate_units_local[unit_idx_local])) then
                    begin
                        Exit(False);
                    end;
                end;
            end;
        begin
            filtered_prefix_misaligned_count_local := 0;
            if (Length(candidates) = 0) or (input_syllable_count <= 0) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    input_syllable_count) and
                    (not short_particle_tail_dictionary_matches_text_local(
                    candidate_text_local)) then
                begin
                    Inc(filtered_prefix_misaligned_count_local);
                    Continue;
                end;
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (not complete_candidate_matches_query_units_local(
                    candidates[in_idx_local], candidate_text_local)) then
                begin
                    Inc(filtered_prefix_misaligned_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure filter_interior_predictive_prefix_candidates_local(
            var candidates: TncCandidateList);
        var
            query_syllables_local: TncPinyinParseResult;
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            exact_head_phrase_cache_local: TDictionary<string, TncCandidateList>;

            function fixed_pronoun_tail_dictionary_matches_text_local(
                const text_value: string): Boolean;
            var
                head_pinyin_local: string;
                head_text_local: string;
                tail_key_local: string;
                tail_units_local: Integer;
                tail_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or
                    (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                    input_syllable_count, head_pinyin_local,
                    head_text_local, tail_key_local, tail_units_local)) or
                    (head_pinyin_local = '') then
                begin
                    Exit;
                end;

                if (get_candidate_text_unit_count(text_value) <>
                    input_syllable_count) or
                    (Copy(text_value, 1, 1) <> head_text_local) then
                begin
                    Exit;
                end;

                tail_text_local := Copy(text_value, 2, MaxInt);
                if (tail_key_local = '') or (tail_text_local = '') or
                    (get_candidate_text_unit_count(tail_text_local) <>
                    tail_units_local) then
                begin
                    Exit;
                end;

                Result := m_dictionary.is_base_entry(tail_key_local,
                    tail_text_local) or m_dictionary.is_user_entry(
                    tail_key_local, tail_text_local);
            end;

            function lookup_exact_head_phrase_cached_local(
                const head_key: string; out out_results: TncCandidateList): Boolean;
            begin
                SetLength(out_results, 0);
                if (head_key = '') or (m_dictionary = nil) then
                begin
                    Exit(False);
                end;

                if (exact_head_phrase_cache_local <> nil) and
                    exact_head_phrase_cache_local.TryGetValue(head_key, out_results) then
                begin
                    Exit(Length(out_results) > 0);
                end;

                if not m_dictionary.lookup(head_key, out_results) then
                begin
                    SetLength(out_results, 0);
                end;

                if exact_head_phrase_cache_local <> nil then
                begin
                    exact_head_phrase_cache_local.AddOrSetValue(head_key,
                        out_results);
                end;
                Result := Length(out_results) > 0;
            end;

            function has_interior_alignment_mismatch_local(
                const candidate: TncCandidate; const text_value: string): Boolean;
            var
                candidate_units_local: TArray<string>;
                unit_idx_inner: Integer;
                syllable_key_local: string;
                matched_unit_count_local: Integer;
                last_required_exact_unit_local: Integer;
                transition_particle_path_local: string;
                transition_particle_evidence_local: Integer;

                function has_exact_head_phrase_match_local: Boolean;
                var
                    head_key_local: string;
                    head_idx_local: Integer;
                    head_results_local: TncCandidateList;
                begin
                    Result := False;
                    head_key_local := '';
                    for head_idx_local := 0 to matched_unit_count_local - 1 do
                    begin
                        head_key_local := head_key_local + normalize_pinyin_text(
                            query_syllables_local[head_idx_local].text);
                    end;
                    if (head_key_local = '') or
                        (not lookup_exact_head_phrase_cached_local(head_key_local,
                        head_results_local)) then
                    begin
                        Exit;
                    end;
                    for head_idx_local := 0 to High(head_results_local) do
                    begin
                        if Trim(head_results_local[head_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if SameText(Trim(head_results_local[head_idx_local].text),
                            text_value) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            begin
                Result := False;
                if is_fuzzy_pinyin_active and (candidate.fuzzy_cost > 0) then
                begin
                    Exit;
                end;
                candidate_units_local := split_text_units(text_value);
                matched_unit_count_local := Length(candidate_units_local);
                if (Length(query_syllables_local) <> input_syllable_count) or
                    (matched_unit_count_local <= 0) or
                    (matched_unit_count_local > input_syllable_count) then
                begin
                    Exit;
                end;

                if (input_syllable_count = 4) and
                    short_four_two_exact_path_is_supported_local(
                    query_syllables_local, text_value) then
                begin
                    Exit(False);
                end;

                if (input_syllable_count = 4) and
                    get_short_four_transition_particle_path_evidence(
                    lookup_text, text_value,
                    transition_particle_path_local,
                    transition_particle_evidence_local) then
                begin
                    Exit(False);
                end;

                if has_exact_head_phrase_match_local then
                begin
                    Exit;
                end;

                if fixed_pronoun_tail_dictionary_matches_text_local(
                    text_value) then
                begin
                    Exit;
                end;

                if Trim(candidate.comment) = '' then
                begin
                    last_required_exact_unit_local := matched_unit_count_local - 2;
                end
                else
                begin
                    last_required_exact_unit_local := matched_unit_count_local - 1;
                end;

                for unit_idx_inner := 0 to last_required_exact_unit_local do
                begin
                    syllable_key_local := normalize_pinyin_text(
                        query_syllables_local[unit_idx_inner].text);
                    if (syllable_key_local = '') or
                        (not m_dictionary.single_char_matches_pinyin(
                        syllable_key_local,
                        candidate_units_local[unit_idx_inner])) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (m_dictionary = nil) or (Length(candidates) = 0) or
                (input_syllable_count <= 1) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            if Length(query_syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;

            exact_head_phrase_cache_local :=
                TDictionary<string, TncCandidateList>.Create;
            try
                out_idx_local := 0;
                for in_idx_local := 0 to High(candidates) do
                begin
                    candidate_text_local := Trim(candidates[in_idx_local].text);
                    if (candidates[in_idx_local].source <> cs_user) and
                        (candidate_text_local <> '') and
                        has_interior_alignment_mismatch_local(candidates[in_idx_local],
                        candidate_text_local) then
                    begin
                        Continue;
                    end;

                    if out_idx_local <> in_idx_local then
                    begin
                        candidates[out_idx_local] := candidates[in_idx_local];
                    end;
                    Inc(out_idx_local);
                end;
            finally
                exact_head_phrase_cache_local.Free;
            end;
            SetLength(candidates, out_idx_local);
        end;

        procedure finalize_lookup_timing_info_lightweight_local;
        var
            total_elapsed_ms_local: Int64;
        begin
            total_elapsed_ms_local := Int64(nc_monotonic_tick_ms - total_start_tick);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
                [lookup_elapsed_ms, segment_elapsed_ms,
                path_search_elapsed_ms, runtime_elapsed_ms, post_elapsed_ms,
                sort_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                total_elapsed_ms_local]);
        end;

        function has_strong_complete_top_local: Boolean;
        var
            top_text_local: string;
        begin
            Result := False;
            if Length(m_candidates) = 0 then
            begin
                Exit;
            end;

            if Trim(m_candidates[0].comment) <> '' then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            Result := (m_candidates[0].source = cs_user) or
                m_candidates[0].has_dict_weight;
        end;

        procedure filter_unsupported_three_syllable_complete_candidates_local(
            var candidates: TncCandidateList);
        var
            in_idx_local: Integer;
            out_idx_local: Integer;
            candidate_text_local: string;
            exact_results_local: TncCandidateList;

            function has_exact_full_query_support_local(
                const text_value: string): Boolean;
            var
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (Trim(text_value) = '') or
                    (not lookup_exact_full_pinyin_cached_local(lookup_text,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(exact_text_local, Trim(text_value)) and
                        (get_candidate_text_unit_count(exact_text_local) =
                        input_syllable_count) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function has_fixed_pronoun_tail_support_local(
                const text_value: string): Boolean;
            var
                head_pinyin_local: string;
                head_text_local: string;
                tail_key_local: string;
                tail_units_local: Integer;
                tail_text_local: string;
                tail_results_local: TncCandidateList;
                tail_idx_local: Integer;
                tail_candidate_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or
                    (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                    input_syllable_count, head_pinyin_local,
                    head_text_local, tail_key_local, tail_units_local)) or
                    (head_pinyin_local = '') then
                begin
                    Exit;
                end;

                if (get_candidate_text_unit_count(text_value) <>
                    input_syllable_count) or
                    (Copy(text_value, 1, 1) <> head_text_local) then
                begin
                    Exit;
                end;

                tail_text_local := Copy(text_value, 2, MaxInt);
                if (tail_key_local = '') or (tail_text_local = '') or
                    (get_candidate_text_unit_count(tail_text_local) <>
                    tail_units_local) or
                    (not lookup_exact_full_pinyin_cached_local(tail_key_local,
                    tail_results_local)) then
                begin
                    Exit;
                end;

                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    tail_candidate_text_local :=
                        Trim(tail_results_local[tail_idx_local].text);
                    if SameText(tail_candidate_text_local, tail_text_local) and
                        (get_candidate_text_unit_count(tail_candidate_text_local) =
                        tail_units_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (Length(candidates) = 0) or (input_syllable_count <> 3) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for in_idx_local := 0 to High(candidates) do
            begin
                candidate_text_local := Trim(candidates[in_idx_local].text);
                if (Trim(candidates[in_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (candidates[in_idx_local].source <> cs_user) and
                    (not (is_fuzzy_pinyin_active and
                    (candidates[in_idx_local].fuzzy_cost > 0))) and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    input_syllable_count) and
                    (not has_exact_full_query_support_local(
                    candidate_text_local)) and
                    (not has_fixed_pronoun_tail_support_local(
                    candidate_text_local)) and
                    (not is_short_three_exact_pair_candidate_supported_local(
                    candidate_text_local)) then
                begin
                    Inc(filtered_prefix_misaligned_count_local);
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    candidates[out_idx_local] := candidates[in_idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;

        function build_fixed_wo_tail_candidates_for_prefix_final_local(
            out out_candidates: TncCandidateList): Boolean;
        const
            c_tail_probe_limit = 8;
            c_fixed_wo_tail_base_bonus = 6400;
            c_fixed_wo_tail_unit_bonus = 920;
        var
            head_pinyin_local: string;
            head_text_local: string;
            normalized_query_local: string;
            tail_key_local: string;
            tail_units_local: Integer;
            tail_results_local: TncCandidateList;
            seen_local: TDictionary<string, Byte>;
            accepted_before_local: Integer;
            exact_accepted_local: Boolean;

            procedure append_tail_candidates_local(
                const candidates_local: TncCandidateList);
            var
                local_idx: Integer;
                candidate_local: TncCandidate;
                tail_text_local: string;
                tail_text_units_local: Integer;
                tail_score_local: Integer;
                seen_key_local: string;
            begin
                for local_idx := 0 to High(candidates_local) do
                begin
                    if local_idx >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(candidates_local[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    tail_text_local := Trim(candidates_local[local_idx].text);
                    tail_text_units_local :=
                        get_candidate_text_unit_count(tail_text_local);
                    if (tail_text_local = '') or (tail_text_units_local < 2) or
                        (tail_text_units_local > 3) then
                    begin
                        Continue;
                    end;
                    if (not m_dictionary.is_base_entry(tail_key_local,
                        tail_text_local)) and
                        (not m_dictionary.is_user_entry(tail_key_local,
                        tail_text_local)) then
                    begin
                        Continue;
                    end;

                    seen_key_local := tail_text_local;
                    if seen_local.ContainsKey(seen_key_local) then
                    begin
                        Continue;
                    end;

                    tail_score_local := candidates_local[local_idx].score;
                    if candidates_local[local_idx].has_dict_weight then
                    begin
                        tail_score_local := candidates_local[local_idx].dict_weight;
                    end;
                    if candidates_local[local_idx].source = cs_user then
                    begin
                        Inc(tail_score_local, c_user_score_bonus);
                    end;

                    FillChar(candidate_local, SizeOf(candidate_local), 0);
                    candidate_local.text := head_text_local + tail_text_local;
                    candidate_local.comment := '';
                    candidate_local.source := cs_rule;
                    candidate_local.score := tail_score_local +
                        c_fixed_wo_tail_base_bonus +
                        (tail_text_units_local * c_fixed_wo_tail_unit_bonus);
                    candidate_local.has_dict_weight := True;
                    candidate_local.dict_weight := candidate_local.score;
                    remember_segment_path_for_candidate(candidate_local.text, '',
                        head_text_local + c_segment_path_separator +
                        tail_text_local, candidate_local.score);

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate_local;
                    seen_local.AddOrSetValue(seen_key_local, 1);
                end;
            end;
        begin
            SetLength(out_candidates, 0);
            Result := False;
            if m_dictionary = nil then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            if not try_get_fixed_pronoun_tail_query_parts(
                normalized_query_local, input_syllable_count,
                head_pinyin_local, head_text_local, tail_key_local,
                tail_units_local) then
            begin
                Exit;
            end;
            if tail_key_local = '' then
            begin
                Exit;
            end;

            seen_local := TDictionary<string, Byte>.Create;
            try
                exact_accepted_local := False;
                if lookup_exact_full_pinyin_cached_local(tail_key_local,
                    tail_results_local) then
                begin
                    accepted_before_local := Length(out_candidates);
                    append_tail_candidates_local(tail_results_local);
                    exact_accepted_local := Length(out_candidates) >
                        accepted_before_local;
                end;
                if (not (exact_accepted_local and
                    is_full_pinyin_key(tail_key_local))) and
                    m_dictionary.lookup(tail_key_local, tail_results_local) then
                begin
                    append_tail_candidates_local(tail_results_local);
                end;
            finally
                seen_local.Free;
            end;

            sort_candidates_lightweight(out_candidates);
            Result := Length(out_candidates) > 0;
        end;

        procedure suppress_non_fixed_wo_single_head_partials_for_prefix_final_local(
            var candidates: TncCandidateList);
        var
            has_fixed_wo_tail_candidate: Boolean;
            head_pinyin_local: string;
            head_text_local: string;
            tail_key_local: string;
            tail_units_local: Integer;
            idx: Integer;
            out_idx: Integer;
            candidate_text: string;
            candidate_units: TArray<string>;
        begin
            if (Length(candidates) = 0) or
                (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                input_syllable_count, head_pinyin_local, head_text_local,
                tail_key_local, tail_units_local)) then
            begin
                Exit;
            end;

            has_fixed_wo_tail_candidate := False;
            for idx := 0 to High(candidates) do
            begin
                candidate_text := Trim(candidates[idx].text);
                if (Trim(candidates[idx].comment) = '') and
                    (get_candidate_text_unit_count(candidate_text) >= 2) and
                    (Copy(candidate_text, 1, 1) = head_text_local) then
                begin
                    has_fixed_wo_tail_candidate := True;
                    Break;
                end;
            end;
            if not has_fixed_wo_tail_candidate then
            begin
                Exit;
            end;

            out_idx := 0;
            for idx := 0 to High(candidates) do
            begin
                candidate_text := Trim(candidates[idx].text);
                candidate_units := split_text_units(candidate_text);
                if (Trim(candidates[idx].comment) <> '') and
                    (Length(candidate_units) >= 2) and
                    (Trim(candidate_units[0]) <> head_text_local) and
                    (m_dictionary <> nil) and
                    m_dictionary.single_char_matches_pinyin(head_pinyin_local,
                    Trim(candidate_units[0])) then
                begin
                    Continue;
                end;

                if out_idx <> idx then
                begin
                    candidates[out_idx] := candidates[idx];
                end;
                Inc(out_idx);
            end;
            if out_idx <> Length(candidates) then
            begin
                SetLength(candidates, out_idx);
            end;
        end;

        procedure ensure_fixed_head_tail_complete_from_visible_partial_local(
            var candidates: TncCandidateList);
        const
            c_tail_probe_limit = 8;
            c_fixed_head_tail_base_bonus = 6400;
            c_fixed_head_tail_unit_bonus = 920;
        var
            head_pinyin_local: string;
            head_text_local: string;
            tail_key_local: string;
            tail_units_local: Integer;
            idx_local: Integer;
            tail_idx_local: Integer;
            tail_results_local: TncCandidateList;
            additions_local: TncCandidateList;
            candidate_local: TncCandidate;
            tail_text_local: string;
            complete_text_local: string;
            tail_text_units_local: Integer;
            tail_score_local: Integer;
            seen_local: TDictionary<string, Byte>;
        begin
            if (Length(candidates) = 0) or (m_dictionary = nil) or
                (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                input_syllable_count, head_pinyin_local, head_text_local,
                tail_key_local, tail_units_local)) or (head_text_local = '') or
                (tail_key_local = '') then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                tail_results_local) then
            begin
                Exit;
            end;

            SetLength(additions_local, 0);
            seen_local := TDictionary<string, Byte>.Create;
            try
                for idx_local := 0 to High(candidates) do
                begin
                    if Trim(candidates[idx_local].comment) = '' then
                    begin
                        seen_local.AddOrSetValue(Trim(candidates[idx_local].text),
                            1);
                    end;
                end;

                for idx_local := 0 to High(candidates) do
                begin
                    if (Trim(candidates[idx_local].text) <> head_text_local) or
                        (normalize_pinyin_text(Trim(candidates[idx_local].comment)) <>
                        tail_key_local) then
                    begin
                        Continue;
                    end;

                    for tail_idx_local := 0 to High(tail_results_local) do
                    begin
                        if tail_idx_local >= c_tail_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;

                        tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                        tail_text_units_local :=
                            get_candidate_text_unit_count(tail_text_local);
                        if (tail_text_local = '') or (tail_text_units_local < 2) or
                            (tail_text_units_local > 3) then
                        begin
                            Continue;
                        end;
                        if (not m_dictionary.is_base_entry(tail_key_local,
                            tail_text_local)) and
                            (not m_dictionary.is_user_entry(tail_key_local,
                            tail_text_local)) then
                        begin
                            Continue;
                        end;

                        complete_text_local := head_text_local + tail_text_local;
                        if seen_local.ContainsKey(complete_text_local) then
                        begin
                            Continue;
                        end;

                        tail_score_local := tail_results_local[tail_idx_local].score;
                        if tail_results_local[tail_idx_local].has_dict_weight then
                        begin
                            tail_score_local :=
                                tail_results_local[tail_idx_local].dict_weight;
                        end;
                        if tail_results_local[tail_idx_local].source = cs_user then
                        begin
                            Inc(tail_score_local, c_user_score_bonus);
                        end;

                        FillChar(candidate_local, SizeOf(candidate_local), 0);
                        candidate_local.text := complete_text_local;
                        candidate_local.comment := '';
                        candidate_local.source := cs_rule;
                        candidate_local.score := tail_score_local +
                            c_fixed_head_tail_base_bonus +
                            (tail_text_units_local * c_fixed_head_tail_unit_bonus);
                        candidate_local.has_dict_weight := True;
                        candidate_local.dict_weight := candidate_local.score;
                        remember_segment_path_for_candidate(candidate_local.text, '',
                            head_text_local + c_segment_path_separator +
                            tail_text_local, candidate_local.score);

                        SetLength(additions_local, Length(additions_local) + 1);
                        additions_local[High(additions_local)] := candidate_local;
                        seen_local.AddOrSetValue(complete_text_local, 1);
                    end;
                end;
            finally
                seen_local.Free;
            end;

            if Length(additions_local) = 0 then
            begin
                Exit;
            end;

            candidates := merge_candidate_lists(additions_local, candidates, 0);
            sort_candidates_lightweight(candidates);
            suppress_non_fixed_wo_single_head_partials_for_prefix_final_local(
                candidates);
        end;

        function should_use_fast_stable_prefix_partial_exit_local: Boolean;
        var
            top_text_local: string;
            top_comment_local: string;
            top_text_units_local: Integer;
            top_comment_units_local: Integer;
        begin
            Result := False;
            if allow_tail_resolution or (input_syllable_count <> 3) or
                (Length(m_candidates) = 0) then
            begin
                Exit;
            end;

            top_comment_local := normalize_pinyin_text(
                Trim(m_candidates[0].comment));
            if top_comment_local = '' then
            begin
                Exit;
            end;

            if not (has_short_extendable_tail_prefix_local or
                has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count)) then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            top_text_units_local := get_candidate_text_unit_count(top_text_local);
            top_comment_units_local :=
                get_effective_compact_pinyin_unit_count(top_comment_local);
            if (top_text_units_local < 2) or (top_comment_units_local <= 0) or
                (top_text_units_local + top_comment_units_local <>
                input_syllable_count) then
            begin
                Exit;
            end;

            Result := (m_candidates[0].source = cs_user) or
                m_candidates[0].has_dict_weight;
        end;

        function candidate_has_fast_four_syllable_two_phrase_support_local(
            const candidate: TncCandidate): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            head_text_local: string;
            tail_text_local: string;

            function build_key_local(const start_idx: Integer;
                const count_local: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (count_local <= 0) or
                    (start_idx + count_local > Length(syllables_local)) then
                begin
                    Exit;
                end;
                for local_idx := start_idx to start_idx + count_local - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[local_idx].text);
                end;
            end;

            function exact_phrase_text_exists_local(const key_value: string;
                const text_value: string): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
            begin
                Result := False;
                if (key_value = '') or (text_value = '') or
                    (get_candidate_text_unit_count(text_value) <> 2) or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                if m_dictionary.is_base_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;

                if not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(Trim(exact_results_local[exact_idx_local].text),
                        text_value) and
                        (get_candidate_text_unit_count(Trim(
                        exact_results_local[exact_idx_local].text)) = 2) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (input_syllable_count <> 4) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> 4 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> 4 then
            begin
                Exit;
            end;

            head_key_local := build_key_local(0, 2);
            tail_key_local := build_key_local(2, 2);
            head_text_local := units_local[0] + units_local[1];
            tail_text_local := units_local[2] + units_local[3];
            Result := exact_phrase_text_exists_local(head_key_local,
                head_text_local) and exact_phrase_text_exists_local(
                tail_key_local, tail_text_local);
        end;

        function should_use_fast_complete_short_split_exit_local: Boolean;
        var
            candidate_idx_local: Integer;
            move_idx_local: Integer;
            picked_local: TncCandidate;

            function candidate_has_fast_two_phrase_path_local(
                const candidate: TncCandidate): Boolean;
            var
                path_local: string;
                segments_local: TArray<string>;
            begin
                path_local := get_segment_path_for_candidate(candidate);
                if path_local = '' then
                begin
                    Exit(candidate_has_fast_four_syllable_two_phrase_support_local(
                        candidate));
                end;

                segments_local := path_local.Split([c_segment_path_separator]);
                if (Length(segments_local) = 2) and
                    (get_candidate_text_unit_count(Trim(segments_local[0])) = 2) and
                    (get_candidate_text_unit_count(Trim(segments_local[1])) = 2) then
                begin
                    Exit(True);
                end;

                // Session learning can remember a valid 2+2 phrase as a more
                // granular path (for example AB/C/D).  The fast exit should be
                // based on text support, not only on the stored path shape.
                Result := candidate_has_fast_four_syllable_two_phrase_support_local(
                    candidate);
            end;

            function candidate_can_use_fast_exit_local(
                const candidate: TncCandidate): Boolean;
            var
                text_local: string;
            begin
                Result := False;
                if Trim(candidate.comment) <> '' then
                begin
                    Exit;
                end;

                text_local := Trim(candidate.text);
                Result := (text_local <> '') and
                    (get_candidate_text_unit_count(text_local) =
                    input_syllable_count) and
                    candidate_has_fast_two_phrase_path_local(candidate);
            end;
        begin
            Result := False;
            if allow_tail_resolution or (input_syllable_count <> 4) or
                (Length(m_candidates) = 0) then
            begin
                Exit;
            end;

            if candidate_can_use_fast_exit_local(m_candidates[0]) then
            begin
                Exit(True);
            end;
            for candidate_idx_local := 1 to Min(High(m_candidates), 31) do
            begin
                if not candidate_can_use_fast_exit_local(
                    m_candidates[candidate_idx_local]) then
                begin
                    Continue;
                end;

                picked_local := m_candidates[candidate_idx_local];
                move_idx_local := candidate_idx_local;
                while move_idx_local > 0 do
                begin
                    m_candidates[move_idx_local] :=
                        m_candidates[move_idx_local - 1];
                    Dec(move_idx_local);
                end;
                m_candidates[0] := picked_local;
                Exit(True);
            end;
        end;

        procedure ensure_exact_prefix_phrase_partials_before_other_partials_local(
            var candidates: TncCandidateList);
        var
            target_idx_local: Integer;
            scan_idx_local: Integer;
            picked_local: TncCandidate;

            function is_exact_prefix_phrase_partial_local(
                const candidate: TncCandidate): Boolean;
            var
                prefix_key_local: string;
                prefix_text_local: string;
                prefix_units_count_local: Integer;
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                normalized_query_local: string;
                normalized_comment_local: string;
                comment_units_count_local: Integer;
            begin
                Result := False;
                if (m_dictionary = nil) or (Trim(candidate.comment) = '') then
                begin
                    Exit;
                end;
                if Pos(c_segment_path_separator,
                    get_segment_path_for_candidate(candidate)) > 0 then
                begin
                    Exit;
                end;

                prefix_text_local := Trim(candidate.text);
                prefix_units_count_local :=
                    get_candidate_text_unit_count(prefix_text_local);
                if prefix_units_count_local < 2 then
                begin
                    Exit;
                end;

                normalized_query_local := normalize_pinyin_text(lookup_text);
                normalized_comment_local :=
                    normalize_pinyin_text(candidate.comment);
                if (normalized_query_local = '') or
                    (normalized_comment_local = '') or
                    (Length(normalized_query_local) <=
                    Length(normalized_comment_local)) or
                    (Copy(normalized_query_local,
                    Length(normalized_query_local) -
                    Length(normalized_comment_local) + 1,
                    Length(normalized_comment_local)) <>
                    normalized_comment_local) then
                begin
                    Exit;
                end;

                prefix_key_local := Copy(normalized_query_local, 1,
                    Length(normalized_query_local) -
                    Length(normalized_comment_local));
                comment_units_count_local :=
                    input_syllable_count - prefix_units_count_local;
                if (prefix_key_local = '') or
                    (get_effective_compact_pinyin_unit_count(prefix_key_local) <>
                    prefix_units_count_local) or
                    (comment_units_count_local <= 0) then
                begin
                    Exit;
                end;

                if (input_syllable_count = 4) and
                    (prefix_units_count_local = 2) and
                    (comment_units_count_local = 2) and
                    (Length(normalized_comment_local) > 2) and
                    candidate.has_dict_weight then
                begin
                    Exit(True);
                end;

                if m_dictionary.is_base_entry(prefix_key_local,
                    prefix_text_local) then
                begin
                    Exit(True);
                end;

                if not lookup_exact_full_pinyin_cached_local(prefix_key_local,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(Trim(exact_results_local[exact_idx_local].text),
                        prefix_text_local) and
                        (get_candidate_text_unit_count(Trim(
                        exact_results_local[exact_idx_local].text)) =
                        prefix_units_count_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if Length(candidates) <= 1 then
            begin
                Exit;
            end;

            target_idx_local := 0;
            while (target_idx_local <= High(candidates)) and
                (Trim(candidates[target_idx_local].comment) = '') do
            begin
                Inc(target_idx_local);
            end;

            scan_idx_local := target_idx_local;
            while scan_idx_local <= High(candidates) do
            begin
                if not is_exact_prefix_phrase_partial_local(
                    candidates[scan_idx_local]) then
                begin
                    Inc(scan_idx_local);
                    Continue;
                end;

                picked_local := candidates[scan_idx_local];
                while scan_idx_local > target_idx_local do
                begin
                    candidates[scan_idx_local] := candidates[scan_idx_local - 1];
                    Dec(scan_idx_local);
                end;
                candidates[target_idx_local] := picked_local;
                Inc(target_idx_local);
                scan_idx_local := target_idx_local + 1;
            end;
        end;

        procedure ensure_balanced_prefix_phrase_partials_first_local(
            var candidates: TncCandidateList);
        var
            target_idx_local: Integer;
            scan_idx_local: Integer;
            picked_local: TncCandidate;

            function is_balanced_prefix_phrase_partial_local(
                const candidate: TncCandidate): Boolean;
            begin
                Result := (input_syllable_count = 4) and
                    (Trim(candidate.comment) <> '') and
                    candidate.has_dict_weight and
                    (Length(Trim(candidate.text)) > 1) and
                    (Length(normalize_pinyin_text(candidate.comment)) > 2) and
                    (get_candidate_text_unit_count(Trim(candidate.text)) <= 2);
            end;
        begin
            if Length(candidates) <= 1 then
            begin
                Exit;
            end;

            target_idx_local := 0;
            while (target_idx_local <= High(candidates)) and
                (Trim(candidates[target_idx_local].comment) = '') do
            begin
                Inc(target_idx_local);
            end;

            scan_idx_local := target_idx_local;
            while scan_idx_local <= High(candidates) do
            begin
                if not is_balanced_prefix_phrase_partial_local(
                    candidates[scan_idx_local]) then
                begin
                    Inc(scan_idx_local);
                    Continue;
                end;

                picked_local := candidates[scan_idx_local];
                while scan_idx_local > target_idx_local do
                begin
                    candidates[scan_idx_local] := candidates[scan_idx_local - 1];
                    Dec(scan_idx_local);
                end;
                candidates[target_idx_local] := picked_local;
                Inc(target_idx_local);
                scan_idx_local := target_idx_local + 1;
            end;
        end;

        procedure ensure_prefix_phrase_partials_before_single_partials_local(
            var candidates: TncCandidateList);
        var
            target_idx_local: Integer;
            scan_idx_local: Integer;
            first_single_idx_local: Integer;
            picked_local: TncCandidate;

            function is_partial_single_local(
                const candidate: TncCandidate): Boolean;
            begin
                Result := (Trim(candidate.comment) <> '') and
                    (get_candidate_text_unit_count(Trim(candidate.text)) = 1);
            end;

            function is_partial_phrase_local(
                const candidate: TncCandidate): Boolean;
            begin
                Result := (Trim(candidate.comment) <> '') and
                    (get_candidate_text_unit_count(Trim(candidate.text)) >= 2);
            end;
        begin
            if Length(candidates) <= 1 then
            begin
                Exit;
            end;

            first_single_idx_local := -1;
            for scan_idx_local := 0 to High(candidates) do
            begin
                if is_partial_single_local(candidates[scan_idx_local]) then
                begin
                    first_single_idx_local := scan_idx_local;
                    Break;
                end;
            end;
            if first_single_idx_local < 0 then
            begin
                Exit;
            end;

            target_idx_local := 0;
            while (target_idx_local <= High(candidates)) and
                (Trim(candidates[target_idx_local].comment) = '') do
            begin
                Inc(target_idx_local);
            end;

            scan_idx_local := target_idx_local;
            while scan_idx_local <= High(candidates) do
            begin
                if (scan_idx_local < first_single_idx_local) or
                    (not is_partial_phrase_local(candidates[scan_idx_local])) then
                begin
                    Inc(scan_idx_local);
                    Continue;
                end;

                picked_local := candidates[scan_idx_local];
                while scan_idx_local > target_idx_local do
                begin
                    candidates[scan_idx_local] := candidates[scan_idx_local - 1];
                    Dec(scan_idx_local);
                end;
                candidates[target_idx_local] := picked_local;
                Inc(target_idx_local);
                if target_idx_local > first_single_idx_local then
                begin
                    first_single_idx_local := target_idx_local;
                end;
                scan_idx_local := target_idx_local + 1;
            end;
        end;

        procedure stable_order_prefix_partials_by_matched_units_local(
            var candidates: TncCandidateList);
        var
            block_start_local: Integer;
            block_end_local: Integer;
            scan_idx_local: Integer;
            insert_idx_local: Integer;
            picked_local: TncCandidate;
            picked_units_local: Integer;

            function partial_prefix_units_local(
                const candidate: TncCandidate): Integer;
            var
                text_units_local: Integer;
                tail_units_local: Integer;
            begin
                text_units_local := get_candidate_text_unit_count(
                    Trim(candidate.text));
                tail_units_local := get_effective_compact_pinyin_unit_count(
                    candidate.comment);
                if (Trim(candidate.comment) = '') or
                    (text_units_local <= 0) or (tail_units_local <= 0) or
                    (text_units_local + tail_units_local <>
                    input_syllable_count) then
                begin
                    Exit(0);
                end;
                Result := text_units_local;
            end;
        begin
            if Length(candidates) <= 1 then
            begin
                Exit;
            end;

            // Keep complete candidates untouched, then order every aligned
            // prefix block by consumed syllables (3, 2, 1 for a four-syllable
            // query). Equal-length prefixes retain the established ranking.
            block_start_local := 0;
            while block_start_local <= High(candidates) do
            begin
                while (block_start_local <= High(candidates)) and
                    (partial_prefix_units_local(
                    candidates[block_start_local]) <= 0) do
                begin
                    Inc(block_start_local);
                end;
                if block_start_local > High(candidates) then
                begin
                    Break;
                end;

                block_end_local := block_start_local;
                while (block_end_local <= High(candidates)) and
                    (partial_prefix_units_local(
                    candidates[block_end_local]) > 0) do
                begin
                    Inc(block_end_local);
                end;
                Dec(block_end_local);

                scan_idx_local := block_start_local + 1;
                while scan_idx_local <= block_end_local do
                begin
                    picked_local := candidates[scan_idx_local];
                    picked_units_local := partial_prefix_units_local(
                        picked_local);
                    insert_idx_local := scan_idx_local;
                    while (insert_idx_local > block_start_local) and
                        (picked_units_local > partial_prefix_units_local(
                        candidates[insert_idx_local - 1])) do
                    begin
                        candidates[insert_idx_local] :=
                            candidates[insert_idx_local - 1];
                        Dec(insert_idx_local);
                    end;
                    candidates[insert_idx_local] := picked_local;
                    Inc(scan_idx_local);
                end;

                block_start_local := block_end_local + 1;
            end;
        end;

        procedure promote_supported_prefix_phrase_partial_over_unstable_complete_local(
            var candidates: TncCandidateList);
        var
            syllables_local: TncPinyinParseResult;
            top_text_local: string;
            best_idx_local: Integer;
            best_rank_local: Integer;
            idx_local: Integer;
            move_idx_local: Integer;
            picked_local: TncCandidate;

            function build_key_local(const start_idx: Integer;
                const count_local: Integer): string;
            var
                key_idx_local: Integer;
            begin
                Result := '';
                if (count_local <= 0) or (start_idx < 0) or
                    (start_idx + count_local > Length(syllables_local)) then
                begin
                    Exit;
                end;

                for key_idx_local := start_idx to start_idx + count_local - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[key_idx_local].text);
                end;
            end;

            function exact_text_exists_local(const key_value: string;
                const text_value: string; const expected_units_local: Integer): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (key_value = '') or
                    (text_value = '') then
                begin
                    Exit;
                end;

                if m_dictionary.is_base_entry(key_value, text_value) or
                    m_dictionary.is_user_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;

                if not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(exact_text_local, text_value) and
                        (get_candidate_text_unit_count(exact_text_local) =
                        expected_units_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function is_supported_prefix_partial_local(
                const candidate: TncCandidate; out out_rank: Integer): Boolean;
            var
                prefix_text_local: string;
                tail_key_local: string;
                prefix_units_local: Integer;
                tail_units_local: Integer;
                prefix_key_local: string;
            begin
                Result := False;
                out_rank := Low(Integer);
                if Trim(candidate.comment) = '' then
                begin
                    Exit;
                end;

                prefix_text_local := Trim(candidate.text);
                tail_key_local := normalize_pinyin_text(Trim(candidate.comment));
                prefix_units_local := get_candidate_text_unit_count(
                    prefix_text_local);
                tail_units_local :=
                    get_effective_compact_pinyin_unit_count(tail_key_local);
                if (prefix_text_local = '') or (tail_key_local = '') or
                    (prefix_units_local < 2) or
                    (prefix_units_local >= input_syllable_count) or
                    (tail_units_local <= 0) or
                    (prefix_units_local + tail_units_local <>
                    input_syllable_count) then
                begin
                    Exit;
                end;

                prefix_key_local := build_key_local(0, prefix_units_local);
                if not exact_text_exists_local(prefix_key_local,
                    prefix_text_local, prefix_units_local) then
                begin
                    Exit;
                end;

                out_rank := prefix_units_local * 100000;
                if candidate.source = cs_user then
                begin
                    Inc(out_rank, 50000);
                end;
                if candidate.has_dict_weight then
                begin
                    Inc(out_rank, candidate.dict_weight);
                end
                else
                begin
                    Inc(out_rank, candidate.score);
                end;
                Result := True;
            end;
        var
            rank_local: Integer;
        begin
            if (Length(candidates) <= 1) or (m_dictionary = nil) or
                (input_syllable_count < 3) or (input_syllable_count > 5) or
                (not is_full_pinyin_key(lookup_text)) or
                (Trim(candidates[0].comment) <> '') then
            begin
                Exit;
            end;

            top_text_local := Trim(candidates[0].text);
            if (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if exact_text_exists_local(lookup_text, top_text_local,
                input_syllable_count) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_rank_local := Low(Integer);
            for idx_local := 1 to High(candidates) do
            begin
                if is_supported_prefix_partial_local(candidates[idx_local],
                    rank_local) and (rank_local > best_rank_local) then
                begin
                    best_idx_local := idx_local;
                    best_rank_local := rank_local;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            picked_local := candidates[best_idx_local];
            if picked_local.score <= candidates[0].score then
            begin
                picked_local.score := candidates[0].score + 1;
            end;
            for move_idx_local := best_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
        end;

        procedure ensure_dangling_initial_prediction_visible_local(
            var candidates: TncCandidateList);
        var
            idx_local: Integer;
            target_idx_local: Integer;
            move_idx_local: Integer;
            seed_text_local: string;
            tail_key_local: string;
            predicted_text_local: string;
            predicted_candidate_local: TncCandidate;
            seed_units_local: Integer;

            function is_single_initial_fragment_local(
                const value: string): Boolean;
            begin
                Result := (Length(value) = 1) and
                    CharInSet(value[1], ['b', 'p', 'm', 'f', 'd', 't', 'n',
                    'l', 'g', 'k', 'h', 'j', 'q', 'x', 'z', 'c', 's', 'r',
                    'y', 'w']);
            end;

            function get_common_initial_prediction_local(
                const value: string): string;
            begin
                Result := '';
                if SameText(value, 'w') then
                begin
                    Result := string(Char($6211));
                end
                else if SameText(value, 'h') then
                begin
                    Result := string(Char($548C));
                end
                else if SameText(value, 'q') then
                begin
                    Result := string(Char($53BB));
                end
                else if SameText(value, 'y') then
                begin
                    Result := string(Char($4E00));
                end;
            end;

            function has_candidate_local(const candidate_text: string;
                const candidate_comment: string): Boolean;
            var
                local_idx: Integer;
            begin
                Result := False;
                for local_idx := 0 to High(candidates) do
                begin
                    if SameText(Trim(candidates[local_idx].text),
                        candidate_text) and
                        SameText(Trim(candidates[local_idx].comment),
                        candidate_comment) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (Length(candidates) = 0) or (input_syllable_count < 3) then
            begin
                Exit;
            end;

            for idx_local := 0 to Min(2, High(candidates)) do
            begin
                seed_text_local := Trim(candidates[idx_local].text);
                tail_key_local := normalize_pinyin_text(
                    Trim(candidates[idx_local].comment));
                seed_units_local := get_candidate_text_unit_count(seed_text_local);
                if (seed_text_local = '') or
                    (not is_single_initial_fragment_local(tail_key_local)) or
                    (seed_units_local + 1 <> input_syllable_count) or
                    (seed_units_local > 2) then
                begin
                    Continue;
                end;

                predicted_text_local :=
                    get_common_initial_prediction_local(tail_key_local);
                if (predicted_text_local = '') or
                    has_candidate_local(seed_text_local + predicted_text_local,
                    '') then
                begin
                    Exit;
                end;

                predicted_candidate_local := candidates[idx_local];
                predicted_candidate_local.text := seed_text_local +
                    predicted_text_local;
                predicted_candidate_local.comment := '';
                predicted_candidate_local.score := Max(1,
                    candidates[idx_local].score - 2);
                predicted_candidate_local.source := cs_rule;
                predicted_candidate_local.has_dict_weight := True;
                predicted_candidate_local.dict_weight :=
                    predicted_candidate_local.score;
                target_idx_local := Min(idx_local + 1, Length(candidates));
                SetLength(candidates, Length(candidates) + 1);
                for move_idx_local := High(candidates) downto
                    target_idx_local + 1 do
                begin
                    candidates[move_idx_local] :=
                        candidates[move_idx_local - 1];
                end;
                candidates[target_idx_local] := predicted_candidate_local;
                Exit;
            end;
        end;

        function has_complete_short_particle_tail_candidate_visible_local: Boolean;
        var
            tail_key_local: string;
            tail_text_local: string;
            head_key_local: string;
            candidate_idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            if not try_get_short_particle_tail_query_parts(lookup_text,
                tail_key_local, tail_text_local, head_key_local) then
            begin
                Exit;
            end;
            if tail_text_local = '' then
            begin
                Exit;
            end;
            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                candidate_text_local := Trim(m_candidates[candidate_idx_local].text);
                if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) >= 2) and
                    SameText(Copy(candidate_text_local,
                    Length(candidate_text_local), 1), tail_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure finalize_short_particle_tail_fast_local(
            const debug_extra_value: string);
        var
            best_partial_score_local: Integer;
            candidate_idx_local: Integer;
            particle_idx_local: Integer;
            particle_text_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_key_local: string;
            candidate_text_local: string;
        begin
            best_partial_score_local := Low(Integer);
            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[candidate_idx_local].comment) <> '' then
                begin
                    best_partial_score_local := Max(best_partial_score_local,
                        m_candidates[candidate_idx_local].score);
                end;
            end;
            if (best_partial_score_local > Low(Integer)) and
                (Length(short_particle_tail_candidates) > 0) then
            begin
                for particle_idx_local := 0 to High(short_particle_tail_candidates) do
                begin
                    if Trim(short_particle_tail_candidates[
                        particle_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    particle_text_local := Trim(short_particle_tail_candidates[
                        particle_idx_local].text);
                    if particle_text_local = '' then
                    begin
                        Continue;
                    end;
                    for candidate_idx_local := 0 to High(m_candidates) do
                    begin
                        if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                            SameText(Trim(m_candidates[candidate_idx_local].text),
                            particle_text_local) and
                            (m_candidates[candidate_idx_local].score <=
                            best_partial_score_local) then
                        begin
                            m_candidates[candidate_idx_local].score :=
                                best_partial_score_local + 64;
                            Break;
                        end;
                    end;
                end;
            end;
            if (best_partial_score_local > Low(Integer)) and
                try_get_short_particle_tail_query_parts(lookup_text,
                tail_key_local, tail_text_local, head_key_local) and
                (tail_text_local <> '') then
            begin
                for candidate_idx_local := 0 to High(m_candidates) do
                begin
                    candidate_text_local := Trim(m_candidates[candidate_idx_local].text);
                    if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                        (candidate_text_local <> '') and
                        (get_candidate_text_unit_count(candidate_text_local) >= 2) and
                        SameText(Copy(candidate_text_local,
                        Length(candidate_text_local), 1), tail_text_local) and
                        (m_candidates[candidate_idx_local].score <=
                        best_partial_score_local) then
                    begin
                        m_candidates[candidate_idx_local].score :=
                            best_partial_score_local + 64;
                    end;
                end;
            end;
            sort_candidates_lightweight(m_candidates);
            filter_misaligned_complete_candidates_local(m_candidates);
            filter_interior_predictive_prefix_candidates_local(m_candidates);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            dedupe_visible_candidates_local(m_candidates,
                deduped_prefix_candidate_count_local);
            filter_non_bmp_single_char_candidates(m_candidates);
            if m_last_lookup_prefix_partial_fast and
                (input_syllable_count = 4) then
            begin
                stable_order_prefix_partials_by_matched_units_local(
                    m_candidates);
            end;
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra_value +
                    ' shortparticletailfast=1';
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if deduped_prefix_candidate_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' dedupe=%d',
                        [deduped_prefix_candidate_count_local]);
                end;
            end;
            finalize_lookup_timing_info_lightweight_local;
            m_page_index := 0;
            m_selected_index := 0;
        end;
    begin
        m_last_lookup_prefix_partial_fast :=
            (Pos('shortprefixraw=1', debug_extra) > 0) or
            (Pos('short2prefixraw=1', debug_extra) > 0) or
            (Pos('short4prefixraw=1', debug_extra) > 0);
        filtered_prefix_misaligned_count_local := 0;
        deduped_prefix_candidate_count_local := 0;
        if build_fuzzy_short_query_recall_candidates(lookup_text,
            fuzzy_short_candidates_local) then
        begin
            m_candidates := merge_candidate_lists(m_candidates,
                fuzzy_short_candidates_local, 0);
            sort_candidates_lightweight(m_candidates);
        end;
        ensure_fixed_head_tail_complete_from_visible_partial_local(m_candidates);
        if build_short_particle_tail_dictionary_candidates(lookup_text,
            short_particle_tail_candidates) then
        begin
            m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                m_candidates, 0);
            sort_candidates_lightweight(m_candidates);
            filter_misaligned_complete_candidates_local(m_candidates);
            filter_interior_predictive_prefix_candidates_local(m_candidates);
        end;
        ensure_short_particle_tail_from_prefix_partial_visible_local(m_candidates);
        if (input_syllable_count in [3, 4]) and
            ((Length(short_particle_tail_candidates) > 0) or
            has_complete_short_particle_tail_candidate_visible_local) then
        begin
            if input_syllable_count = 4 then
            begin
                filter_unsupported_short_four_ab_cd_complete_candidates_local(
                    m_candidates);
            end;
            finalize_short_particle_tail_fast_local(debug_extra);
            Exit;
        end;
        if (input_syllable_count = 3) and
            build_short_particle_tail_from_prefix_partials_fast_local(lookup_text,
            m_candidates, short_particle_tail_candidates) then
        begin
            m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                m_candidates, 0);
            finalize_short_particle_tail_fast_local(debug_extra);
            Exit;
        end;
        if allow_tail_resolution then
        begin
            finalize_lookup_timing_info;
        end
        else
        begin
            finalize_lookup_timing_info_lightweight_local;
        end;
        if should_use_fast_complete_short_split_exit_local then
        begin
            filter_interior_predictive_prefix_candidates_local(m_candidates);
            finalize_modal_tail_sentence_candidate(m_candidates);
            dedupe_visible_candidates_local(m_candidates,
                deduped_prefix_candidate_count_local);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_balanced_prefix_phrase_partials_first_local(m_candidates);
            ensure_exact_prefix_phrase_partials_before_other_partials_local(
                m_candidates);
            ensure_prefix_phrase_partials_before_single_partials_local(
                m_candidates);
            filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
                m_candidates, filtered_prefix_misaligned_count_local);
            if build_fixed_wo_tail_candidates_for_prefix_final_local(
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
                suppress_non_fixed_wo_single_head_partials_for_prefix_final_local(
                    m_candidates);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
                filter_misaligned_complete_candidates_local(m_candidates);
                filter_interior_predictive_prefix_candidates_local(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
            end;
            ensure_short_particle_tail_from_prefix_partial_visible_local(m_candidates);
            filter_unsupported_short_four_ab_cd_complete_candidates_local(
                m_candidates);
            filter_unsupported_short_four_ab_c_partial_candidates_local(
                m_candidates);
            ensure_short_two_single_pair_candidates_visible_local(m_candidates);
            ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
            dedupe_visible_candidates_local(m_candidates,
                deduped_prefix_candidate_count_local);
            filter_explicit_apostrophe_output_complete_local(m_candidates);
            filter_non_bmp_single_char_candidates(m_candidates);
            if m_last_lookup_prefix_partial_fast and
                (input_syllable_count = 4) then
            begin
                stable_order_prefix_partials_by_matched_units_local(
                    m_candidates);
            end;
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra +
                    ' shortcompletefast=1';
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if deduped_prefix_candidate_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' dedupe=%d',
                        [deduped_prefix_candidate_count_local]);
                end;
            end;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        filter_misaligned_complete_candidates_local(m_candidates);
        filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
            m_candidates, filtered_prefix_misaligned_count_local);
        filter_unsupported_three_syllable_complete_candidates_local(m_candidates);
        filter_unsupported_short_four_ab_cd_complete_candidates_local(
            m_candidates);
        filter_unsupported_short_four_ab_c_partial_candidates_local(
            m_candidates);
        if (input_syllable_count in [3, 4]) and
            ((Length(short_particle_tail_candidates) > 0) or
            has_complete_short_particle_tail_candidate_visible_local) then
        begin
            if input_syllable_count = 4 then
            begin
                filter_unsupported_short_four_ab_cd_complete_candidates_local(
                    m_candidates);
            end;
            finalize_short_particle_tail_fast_local(debug_extra);
            Exit;
        end;
        if build_fixed_wo_tail_candidates_for_prefix_final_local(
            fixed_wo_tail_candidates) then
        begin
            m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                m_candidates, 0);
            sort_candidates_lightweight(m_candidates);
            suppress_non_fixed_wo_single_head_partials_for_prefix_final_local(
                m_candidates);
        end;
        dedupe_visible_candidates_local(m_candidates,
            deduped_prefix_candidate_count_local);
        fast_stable_prefix_partial_exit_local :=
            should_use_fast_stable_prefix_partial_exit_local;
        if fast_stable_prefix_partial_exit_local then
        begin
            phase_start_tick_local := nc_monotonic_tick_ms;
            if input_syllable_count = 3 then
            begin
                ensure_three_syllable_prefix_one_plus_two_complete_candidates_visible(
                    m_candidates);
                filter_self_composed_complete_candidates_when_normal_complete_exists;
                dedupe_visible_candidates_local(m_candidates,
                    deduped_prefix_candidate_count_local);
            end;
            note_prefix_phase_elapsed_local('pfquick3', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            if allow_dangling_initial_prediction then
            begin
                ensure_dangling_initial_prediction_visible_local(m_candidates);
            end;
            promote_full_query_exact_complete_phrase_local(m_candidates);
            promote_short_unsupported_complete_prefix_partial(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            note_prefix_phase_elapsed_local('pfquickvis', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            filter_explicit_apostrophe_output_complete_local(m_candidates);
            note_prefix_phase_elapsed_local('pfquickapo', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            filter_non_bmp_single_char_candidates(m_candidates);
            note_prefix_phase_elapsed_local('pfquickbmp', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
            promote_supported_prefix_phrase_partial_over_unstable_complete_local(
                m_candidates);
            filter_unsupported_three_syllable_complete_candidates_local(m_candidates);
            filter_misaligned_complete_candidates_local(m_candidates);
            filter_interior_predictive_prefix_candidates_local(m_candidates);
            if build_fixed_wo_tail_candidates_for_prefix_final_local(
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
                suppress_non_fixed_wo_single_head_partials_for_prefix_final_local(
                    m_candidates);
                filter_misaligned_complete_candidates_local(m_candidates);
                filter_interior_predictive_prefix_candidates_local(m_candidates);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
                filter_misaligned_complete_candidates_local(m_candidates);
                filter_interior_predictive_prefix_candidates_local(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
            end;
            ensure_short_particle_tail_from_prefix_partial_visible_local(m_candidates);
            filter_unsupported_short_four_ab_cd_complete_candidates_local(
                m_candidates);
            filter_unsupported_short_four_ab_c_partial_candidates_local(
                m_candidates);
            ensure_short_two_single_pair_candidates_visible_local(m_candidates);
            ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
            dedupe_visible_candidates_local(m_candidates,
                deduped_prefix_candidate_count_local);
            note_prefix_phase_elapsed_local('pfquick312', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            refresh_candidate_segment_paths;
            note_prefix_phase_elapsed_local('pfquickrefresh', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            note_ranked_top_candidate;
            note_prefix_phase_elapsed_local('pfquickrank', phase_start_tick_local);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra +
                    ' prefixpartialfast=1';
                if m_last_three_syllable_partial_preference_kind > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' b3pref=%d',
                        [m_last_three_syllable_partial_preference_kind]);
                end;
                if m_last_three_syllable_partial_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_three_syllable_partial_debug_info;
                end;
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
                if filtered_prefix_misaligned_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' filter_prefix_misaligned=%d',
                        [filtered_prefix_misaligned_count_local]);
                end;
                if deduped_prefix_candidate_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' dedupe=%d',
                        [deduped_prefix_candidate_count_local]);
                end;
            end;
            finalize_lookup_timing_info_lightweight_local;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if not skip_expensive_short_prefix_rerank_local then
        begin
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            note_prefix_phase_elapsed_local('pf321', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_three_syllable_prefix_one_plus_two_complete_candidates_visible(
                m_candidates);
            note_prefix_phase_elapsed_local('pf123', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_reliable_full_query_exact_complete_candidate_fast_visible(
                m_candidates);
            note_prefix_phase_elapsed_local('pffqfast', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            filter_self_composed_complete_candidates_when_normal_complete_exists;
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            note_prefix_phase_elapsed_local('pfself', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_exact_span_rank_repair_candidate_visible(m_candidates);
            note_prefix_phase_elapsed_local('pfexactspan', phase_start_tick_local);
            phase_start_tick_local := nc_monotonic_tick_ms;
            sort_candidates(m_candidates);
            note_prefix_phase_elapsed_local('pfsort', phase_start_tick_local);
            if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 4) then
            begin
                phase_start_tick_local := nc_monotonic_tick_ms;
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);
                note_prefix_phase_elapsed_local('pftailpartial',
                    phase_start_tick_local);
            end;
        end;
        if (not skip_expensive_short_prefix_rerank_local) and
            (not has_strong_complete_top_local) then
        begin
            phase_start_tick_local := nc_monotonic_tick_ms;
            promote_stable_prefix_partial_candidate_local(m_candidates, 2);
            note_prefix_phase_elapsed_local('pfstable',
                phase_start_tick_local);
        end;
        top_partial_head_units_local := 0;
        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) <> '') then
        begin
            top_partial_head_units_local := get_candidate_text_unit_count(
                Trim(m_candidates[0].text));
        end;
        if (not skip_expensive_short_prefix_rerank_local) and
            ((Length(m_candidates) = 0) or
            ((Trim(m_candidates[0].comment) <> '') and
            (top_partial_head_units_local < 2)) or
            is_problematic_single_char_chain_candidate_for_query(lookup_text,
            m_candidates[0])) then
        begin
            phase_start_tick_local := nc_monotonic_tick_ms;
            promote_stable_prefix_partial_candidate_local(m_candidates, 2);
            note_prefix_phase_elapsed_local('pfstable2',
                phase_start_tick_local);
        end;
        if not skip_expensive_short_prefix_rerank_local then
        begin
            phase_start_tick_local := nc_monotonic_tick_ms;
            ensure_best_natural_prefix_candidate_visible_local(m_candidates);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            note_prefix_phase_elapsed_local('pfnatural',
                phase_start_tick_local);
        end;
        phase_start_tick_local := nc_monotonic_tick_ms;
        refresh_candidate_segment_paths;
        note_prefix_phase_elapsed_local('pfrefresh1', phase_start_tick_local);
        phase_start_tick_local := nc_monotonic_tick_ms;
        ensure_high_quality_complete_candidate_in_top2(m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        ensure_bounded_single_char_partial_pool(m_candidates);
        ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
        demote_low_evidence_admin_place_alias_top_local(m_candidates);
        if allow_dangling_initial_prediction then
        begin
            ensure_dangling_initial_prediction_visible_local(m_candidates);
        end;
        promote_full_query_exact_complete_phrase_local(m_candidates);
        promote_short_unsupported_complete_prefix_partial(m_candidates);
        ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
        filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
            m_candidates, filtered_prefix_misaligned_count_local);
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        filter_self_composed_complete_candidates_when_normal_complete_exists;
        filter_non_bmp_single_char_candidates(m_candidates);
        promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
        promote_supported_prefix_phrase_partial_over_unstable_complete_local(
            m_candidates);
        filter_user_completes_misaligned_with_relaxed_no_initial_tail_local(
            m_candidates, filtered_prefix_misaligned_count_local);
        ensure_short_particle_tail_from_prefix_partial_visible_local(m_candidates);
        filter_unsupported_short_four_ab_cd_complete_candidates_local(
            m_candidates);
        filter_unsupported_short_four_ab_c_partial_candidates_local(
            m_candidates);
        ensure_short_two_single_pair_candidates_visible_local(m_candidates);
        ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
        if m_last_lookup_prefix_partial_fast and
            (input_syllable_count = 4) then
        begin
            stable_order_prefix_partials_by_matched_units_local(
                m_candidates);
        end;
        note_prefix_phase_elapsed_local('pfquality', phase_start_tick_local);
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if m_last_three_syllable_partial_preference_kind > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' b3pref=%d',
                    [m_last_three_syllable_partial_preference_kind]);
            end;
            if m_last_three_syllable_partial_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_three_syllable_partial_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            if filtered_prefix_misaligned_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' filter_prefix_misaligned=%d',
                    [filtered_prefix_misaligned_count_local]);
            end;
            if deduped_prefix_candidate_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' dedupe=%d',
                    [deduped_prefix_candidate_count_local]);
            end;
        end;
        phase_start_tick_local := nc_monotonic_tick_ms;
        refresh_candidate_segment_paths;
        note_prefix_phase_elapsed_local('pfrefresh2', phase_start_tick_local);
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    procedure ensure_complete_long_top_tail_partial_second_local;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
        top_units_local: TArray<string>;
        syllables_local: TncPinyinParseResult;
        prefix_text_local: string;
        tail_key_local: string;
        idx_local: Integer;
        move_idx_local: Integer;
        existing_idx_local: Integer;
        target_idx_local: Integer;
        partial_candidate_local: TncCandidate;
        function is_full_complete_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) =
                input_syllable_count);
        end;
        function prefix_partial_insert_index_local(
            const skip_index: Integer): Integer;
        var
            scan_idx_local: Integer;
            complete_count_local: Integer;
        begin
            Result := 0;
            complete_count_local := 0;
            for scan_idx_local := 0 to High(m_candidates) do
            begin
                if scan_idx_local = skip_index then
                begin
                    Continue;
                end;

                if is_full_complete_candidate_local(m_candidates[scan_idx_local]) then
                begin
                    Inc(complete_count_local);
                    Result := scan_idx_local + 1;
                    if complete_count_local >= 2 then
                    begin
                        Break;
                    end;
                end;
            end;
            if (skip_index >= 0) and (skip_index < Result) then
            begin
                Dec(Result);
            end;
            if Result < 1 then
            begin
                Result := 1;
            end;
        end;
        procedure move_candidate_to_target_local(const from_index: Integer;
            const to_index: Integer);
        var
            local_move_idx: Integer;
            moved_candidate_local: TncCandidate;
            moved_path_local: string;
        begin
            if (from_index < 0) or (from_index >= Length(m_candidates)) or
                (to_index < 0) or (to_index >= Length(m_candidates)) or
                (from_index = to_index) then
            begin
                Exit;
            end;

            moved_candidate_local := m_candidates[from_index];
            moved_path_local := '';
            if from_index < Length(m_candidate_segment_paths) then
            begin
                moved_path_local := m_candidate_segment_paths[from_index];
            end;

            if from_index > to_index then
            begin
                for local_move_idx := from_index downto to_index + 1 do
                begin
                    m_candidates[local_move_idx] :=
                        m_candidates[local_move_idx - 1];
                    if local_move_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[local_move_idx] :=
                            m_candidate_segment_paths[local_move_idx - 1];
                    end;
                end;
            end
            else
            begin
                for local_move_idx := from_index to to_index - 1 do
                begin
                    m_candidates[local_move_idx] :=
                        m_candidates[local_move_idx + 1];
                    if local_move_idx < Length(m_candidate_segment_paths) - 1 then
                    begin
                        m_candidate_segment_paths[local_move_idx] :=
                            m_candidate_segment_paths[local_move_idx + 1];
                    end;
                end;
            end;

            m_candidates[to_index] := moved_candidate_local;
            if to_index < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[to_index] := moved_path_local;
            end;
        end;
    begin
        if (input_syllable_count < 6) or (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        top_text_local := Trim(top_candidate_local.text);
        if (top_text_local = '') or (Trim(top_candidate_local.comment) <> '') or
            (get_candidate_text_unit_count(top_text_local) <>
            input_syllable_count) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(top_text_local);
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if (Length(top_units_local) <> input_syllable_count) or
            (Length(syllables_local) <> input_syllable_count) then
        begin
            Exit;
        end;

        prefix_text_local := '';
        for idx_local := 0 to High(top_units_local) - 1 do
        begin
            prefix_text_local := prefix_text_local + top_units_local[idx_local];
        end;
        tail_key_local := normalize_pinyin_text(
            Trim(syllables_local[High(syllables_local)].text));
        if (prefix_text_local = '') or (tail_key_local = '') then
        begin
            Exit;
        end;

        existing_idx_local := -1;
        for idx_local := 0 to High(m_candidates) do
        begin
            if SameText(Trim(m_candidates[idx_local].text), prefix_text_local) and
                SameText(normalize_pinyin_text(Trim(m_candidates[idx_local].comment)),
                tail_key_local) then
            begin
                existing_idx_local := idx_local;
                Break;
            end;
        end;

        target_idx_local := prefix_partial_insert_index_local(
            existing_idx_local);
        if target_idx_local > High(m_candidates) then
        begin
            target_idx_local := High(m_candidates);
        end;

        if existing_idx_local = target_idx_local then
        begin
            Exit;
        end;
        if existing_idx_local >= 0 then
        begin
            move_candidate_to_target_local(existing_idx_local,
                target_idx_local);
            Exit;
        end;

        partial_candidate_local := top_candidate_local;
        partial_candidate_local.text := prefix_text_local;
        partial_candidate_local.comment := tail_key_local;
        partial_candidate_local.score := Max(0, top_candidate_local.score - 80);
        partial_candidate_local.has_dict_weight := False;
        partial_candidate_local.dict_weight := 0;

        SetLength(m_candidates, Length(m_candidates) + 1);
        if Length(m_candidate_segment_paths) < Length(m_candidates) then
        begin
            SetLength(m_candidate_segment_paths, Length(m_candidates));
        end;
        if target_idx_local > High(m_candidates) then
        begin
            target_idx_local := High(m_candidates);
        end;
        for move_idx_local := High(m_candidates) downto target_idx_local + 1 do
        begin
            m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
            if move_idx_local < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[move_idx_local] :=
                    m_candidate_segment_paths[move_idx_local - 1];
            end;
        end;
        m_candidates[target_idx_local] := partial_candidate_local;
        if target_idx_local < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[target_idx_local] := '';
        end;
    end;

    procedure finalize_direct_preferred_candidates_and_prepare_exit(
        const debug_extra: string);
    var
        deduped_direct_candidate_count_local: Integer;
        direct_top_candidate_local: TncCandidate;
        direct_top_text_local: string;
        direct_top_valid_local: Boolean;
        direct_top_syllables_local: TncPinyinParseResult;

        procedure finalize_lookup_timing_info_lightweight_local;
        var
            total_elapsed_ms_local: Int64;
        begin
            total_elapsed_ms_local := Int64(nc_monotonic_tick_ms - total_start_tick);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
                [lookup_elapsed_ms, segment_elapsed_ms,
                path_search_elapsed_ms, runtime_elapsed_ms, post_elapsed_ms,
                sort_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                total_elapsed_ms_local]);
        end;

        function build_direct_top_query_key_local(const start_idx: Integer;
            const span_len: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            if (span_len <= 0) or (start_idx < 0) or
                (start_idx + span_len > Length(direct_top_syllables_local)) then
            begin
                Exit;
            end;

            for syllable_idx_local := start_idx to start_idx + span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    direct_top_syllables_local[syllable_idx_local].text);
            end;
        end;

        function get_direct_top_exact_span_weight_local(const start_idx: Integer;
            const span_len: Integer; const span_text: string): Integer;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            weight_local: Integer;
        begin
            Result := 0;
            if (span_text = '') or (span_len < 2) or (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key_local := build_direct_top_query_key_local(start_idx,
                span_len);
            if (query_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if not SameText(Trim(exact_results_local[exact_idx_local].text),
                    span_text) then
                begin
                    Continue;
                end;

                weight_local := exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight >
                    weight_local) then
                begin
                    weight_local := exact_results_local[exact_idx_local].dict_weight;
                end;
                if weight_local > Result then
                begin
                    Result := weight_local;
                end;
            end;
        end;

        function candidate_rewrites_direct_top_exact_span_local(
            const candidate_text: string): Boolean;
        const
            c_min_protected_exact_span_weight = 700;
            c_max_protected_exact_span_units = 6;
        var
            direct_units_local: TArray<string>;
            candidate_units_local: TArray<string>;
            start_idx_local: Integer;
            span_len_local: Integer;
            unit_idx_local: Integer;
            direct_span_text_local: string;
            candidate_span_text_local: string;
        begin
            Result := False;
            if (not direct_top_valid_local) or (candidate_text = '') or
                SameText(candidate_text, direct_top_text_local) then
            begin
                Exit;
            end;

            direct_units_local := split_text_units(direct_top_text_local);
            candidate_units_local := split_text_units(candidate_text);
            if (Length(direct_units_local) <> input_syllable_count) or
                (Length(candidate_units_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            for start_idx_local := 0 to input_syllable_count - 1 do
            begin
                for span_len_local := 2 to Min(c_max_protected_exact_span_units,
                    input_syllable_count - start_idx_local) do
                begin
                    direct_span_text_local := '';
                    candidate_span_text_local := '';
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        direct_span_text_local := direct_span_text_local +
                            direct_units_local[unit_idx_local];
                        candidate_span_text_local := candidate_span_text_local +
                            candidate_units_local[unit_idx_local];
                    end;

                    if SameText(direct_span_text_local,
                        candidate_span_text_local) then
                    begin
                        Continue;
                    end;

                    if get_direct_top_exact_span_weight_local(start_idx_local,
                        span_len_local, direct_span_text_local) >=
                        c_min_protected_exact_span_weight then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        procedure restore_direct_top_candidate_local;
        var
            idx_local: Integer;
            existing_idx_local: Integer;
        begin
            if not direct_top_valid_local then
            begin
                Exit;
            end;

            existing_idx_local := -1;
            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) =
                    Trim(direct_top_candidate_local.comment)) and
                    SameText(Trim(m_candidates[idx_local].text),
                    direct_top_text_local) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end;

            if existing_idx_local = 0 then
            begin
                Exit;
            end;

            if existing_idx_local > 0 then
            begin
                direct_top_candidate_local := m_candidates[existing_idx_local];
                for idx_local := existing_idx_local downto 1 do
                begin
                    m_candidates[idx_local] := m_candidates[idx_local - 1];
                end;
                m_candidates[0] := direct_top_candidate_local;
            end
            else
            begin
                SetLength(m_candidates, Length(m_candidates) + 1);
                for idx_local := High(m_candidates) downto 1 do
                begin
                    m_candidates[idx_local] := m_candidates[idx_local - 1];
                end;
                m_candidates[0] := direct_top_candidate_local;
            end;
        end;

        function can_use_lightweight_long_direct_finalize_local: Boolean;
        var
            direct_path_local: string;
        begin
            Result := False;
            if (not direct_top_valid_local) or (input_syllable_count < 9) or
                all_initial_compact_query or has_internal_dangling_initial then
            begin
                Exit;
            end;

            direct_path_local := get_segment_path_for_candidate(
                direct_top_candidate_local);
            Result := get_encoded_path_segment_count_local(direct_path_local) > 1;
        end;

    begin
        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) <> '') then
        begin
            finalize_prefix_partial_candidates_and_prepare_exit(debug_extra,
                False);
            Exit;
        end;

        deduped_direct_candidate_count_local := 0;
        direct_top_valid_local := False;
        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            input_syllable_count) then
        begin
            direct_top_candidate_local := m_candidates[0];
            direct_top_text_local := Trim(m_candidates[0].text);
            direct_top_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            direct_top_valid_local :=
                Length(direct_top_syllables_local) = input_syllable_count;
        end;
        finalize_lookup_timing_info_lightweight_local;
        if can_use_lightweight_long_direct_finalize_local then
        begin
            dedupe_visible_candidates_local(m_candidates,
                deduped_direct_candidate_count_local);
            restore_direct_top_candidate_local;
            ensure_long_sentence_prefix_partial_visible(m_candidates,
                get_candidate_limit);
            ensure_complete_long_top_tail_partial_second_local;
            if Length(m_candidates) > get_total_candidate_limit then
            begin
                SetLength(m_candidates, get_total_candidate_limit);
            end;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra;
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
                if deduped_direct_candidate_count_local > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' dedupe=%d', [deduped_direct_candidate_count_local]);
                end;
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    ' directlite=1';
            end;
            refresh_candidate_segment_paths;
            m_visible_candidates_cache_valid := False;
            SetLength(m_visible_candidates_cache, 0);
            SetLength(m_visible_candidate_source_indices_cache, 0);
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        ensure_bounded_single_char_partial_pool(m_candidates);
        dedupe_visible_candidates_local(m_candidates,
            deduped_direct_candidate_count_local);
        ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
        ensure_long_sentence_prefix_partial_visible(m_candidates,
            get_candidate_limit);
        promote_full_query_exact_complete_phrase_local(m_candidates);
        promote_short_unsupported_complete_prefix_partial(m_candidates);
        ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
        filter_self_composed_complete_candidates_when_normal_complete_exists;
        promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        if (Length(m_candidates) > 0) and
            candidate_rewrites_direct_top_exact_span_local(
            Trim(m_candidates[0].text)) then
        begin
            restore_direct_top_candidate_local;
        end;
        finalize_modal_tail_sentence_candidate(m_candidates);
        dedupe_visible_candidates_local(m_candidates,
            deduped_direct_candidate_count_local);
        ensure_long_sentence_prefix_partial_visible(m_candidates,
            get_candidate_limit);
        ensure_complete_long_top_tail_partial_second_local;
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            if deduped_direct_candidate_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' dedupe=%d', [deduped_direct_candidate_count_local]);
            end;
        end;
        refresh_candidate_segment_paths;
        m_visible_candidates_cache_valid := False;
        SetLength(m_visible_candidates_cache, 0);
        SetLength(m_visible_candidate_source_indices_cache, 0);
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    procedure finalize_fast_prefix_candidates_and_prepare_exit(
        const debug_extra: string);
    var
        candidate_idx_local: Integer;
        has_prefix_partial_candidate_local: Boolean;
    begin
        has_prefix_partial_candidate_local := False;
        for candidate_idx_local := 0 to High(m_candidates) do
        begin
            if Trim(m_candidates[candidate_idx_local].comment) <> '' then
            begin
                has_prefix_partial_candidate_local := True;
                Break;
            end;
        end;

        if has_prefix_partial_candidate_local then
        begin
            finalize_prefix_partial_candidates_and_prepare_exit(debug_extra,
                True);
            Exit;
        end;

        finalize_lookup_timing_info;
        filter_self_composed_complete_candidates_when_normal_complete_exists;
        promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
        filter_explicit_apostrophe_output_complete_local(m_candidates);
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if m_last_three_syllable_partial_preference_kind > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' b3pref=%d',
                    [m_last_three_syllable_partial_preference_kind]);
            end;
            if m_last_three_syllable_partial_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_three_syllable_partial_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
        end;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    procedure finalize_all_initial_compact_candidates_and_prepare_exit(
        const debug_extra: string);
    const
        c_max_all_initial_visible_candidates = 36;
    var
        deduped_count_local: Integer;
        total_elapsed_ms_local: Int64;
    begin
        m_last_lookup_all_initial_fast := True;
        total_elapsed_ms_local := Int64(nc_monotonic_tick_ms - total_start_tick);
        m_last_lookup_timing_info := Format(
            'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
            [lookup_elapsed_ms, segment_elapsed_ms, path_search_elapsed_ms,
            runtime_elapsed_ms, post_elapsed_ms, sort_elapsed_ms,
            lookup_cache_hits, lookup_cache_misses, total_elapsed_ms_local]);
        dedupe_visible_candidates_local(m_candidates, deduped_count_local);
        if Length(m_candidates) > c_max_all_initial_visible_candidates then
        begin
            SetLength(m_candidates, c_max_all_initial_visible_candidates);
        end;
        if Length(m_candidate_segment_paths) > Length(m_candidates) then
        begin
            SetLength(m_candidate_segment_paths, Length(m_candidates));
        end;

        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if deduped_count_local > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' dedupe=%d', [deduped_count_local]);
            end;
        end;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    function try_finalize_fast_prefix_candidates_and_prepare_exit_local(
        const debug_extra: string): Boolean;
    begin
        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            input_syllable_count) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' fpfast=[defer-complete]';
            end;
            Exit(False);
        end;

        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) <> '') and
            (not is_full_pinyin_key(lookup_text)) and
            has_safe_trailing_initial_typing_state then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' fpfast=[preserve-partial]';
            end;
            finalize_preserved_candidates_and_prepare_exit(debug_extra,
                m_candidates[0], False);
            Exit(True);
        end;

        finalize_fast_prefix_candidates_and_prepare_exit(debug_extra);
        Exit(True);
    end;

    procedure finalize_preserved_candidates_and_prepare_exit(
        const debug_extra: string; const preserved_top: TncCandidate;
        const allow_exact_span_repair: Boolean = True);
    var
        top_to_preserve: TncCandidate;
        total_elapsed_ms: Int64;
        preserved_phase_start_tick: UInt64;
        deduped_preserved_candidate_count: Integer;
        preserved_lite_skip_debug_local: string;

        procedure note_preserved_phase_elapsed_local(const phase_name: string;
            const started_at: UInt64);
        var
            elapsed_local: UInt64;
        begin
            if not m_config.debug_mode then
            begin
                Exit;
            end;
            elapsed_local := nc_monotonic_tick_ms - started_at;
            if elapsed_local > 0 then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' %s=%d', [phase_name, elapsed_local]);
            end;
        end;

        function can_use_trailing_prefix_preserved_fast_finalize_local: Boolean;
        var
            trailing_syllables_local: TncPinyinParseResult;
            last_trailing_text_local: string;
        begin
            Result := False;
            if (input_syllable_count < 9) or all_initial_compact_query or
                has_internal_dangling_initial then
            begin
                Exit;
            end;

            if input_syllable_count = 9 then
            begin
                trailing_syllables_local :=
                    get_effective_compact_pinyin_syllables(lookup_text);
                if Length(trailing_syllables_local) <= 0 then
                begin
                    Exit;
                end;
                last_trailing_text_local := Trim(
                    trailing_syllables_local[High(trailing_syllables_local)].text);
                if SameText(last_trailing_text_local, 'q') then
                begin
                    Exit;
                end;
            end;

            Result := has_extendable_trailing_prefix_query_text(lookup_text);
        end;

        function top_complete_path_has_only_multi_segments_local: Boolean;
        var
            top_text_local: string;
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            part_idx_local: Integer;
        begin
            Result := False;
            if Length(m_candidates) = 0 then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (Trim(m_candidates[0].comment) <> '') or
                (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            encoded_path_local := get_segment_path_for_candidate(
                m_candidates[0], 0);
            if get_encoded_path_segment_count_local(encoded_path_local) <= 1 then
            begin
                Exit;
            end;

            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            for part_idx_local := 0 to High(segment_parts_local) do
            begin
                if get_candidate_text_unit_count(
                    Trim(segment_parts_local[part_idx_local])) < 2 then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function top_complete_path_is_backed_local: Boolean;
        var
            top_text_local: string;
            encoded_path_local: string;
        begin
            Result := False;
            if Length(m_candidates) = 0 then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (Trim(m_candidates[0].comment) <> '') or
                (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            encoded_path_local := get_segment_path_for_candidate(
                m_candidates[0], 0);
            Result := get_encoded_path_segment_count_local(encoded_path_local) > 1;
        end;

        function top_complete_is_exact_query_local: Boolean;
        var
            top_text_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (lookup_text = '') or
                (Length(m_candidates) = 0) then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (Trim(m_candidates[0].comment) <> '') or
                (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(lookup_text, top_text_local) then
            begin
                Exit(True);
            end;

            if not m_dictionary.lookup_exact_full_pinyin(lookup_text,
                exact_results_local) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    top_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_close_complete_alternative_for_quality_repair_local: Boolean;
        const
            c_close_complete_score_gap = 5200;
        var
            top_text_local: string;
            alt_text_local: string;
            top_score_local: Integer;
            alt_score_local: Integer;
            alt_idx_local: Integer;
        begin
            Result := False;
            if (Length(m_candidates) < 2) or (input_syllable_count < 7) then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (Trim(m_candidates[0].comment) <> '') or
                (top_text_local = '') or
                (get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            top_score_local := m_candidates[0].score;
            if m_candidates[0].has_dict_weight and
                (m_candidates[0].dict_weight > top_score_local) then
            begin
                top_score_local := m_candidates[0].dict_weight;
            end;

            for alt_idx_local := 1 to Min(4, High(m_candidates)) do
            begin
                alt_text_local := Trim(m_candidates[alt_idx_local].text);
                if (Trim(m_candidates[alt_idx_local].comment) <> '') or
                    (alt_text_local = '') or SameText(alt_text_local,
                    top_text_local) or
                    (get_candidate_text_unit_count(alt_text_local) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                alt_score_local := m_candidates[alt_idx_local].score;
                if m_candidates[alt_idx_local].has_dict_weight and
                    (m_candidates[alt_idx_local].dict_weight > alt_score_local) then
                begin
                    alt_score_local := m_candidates[alt_idx_local].dict_weight;
                end;

                if top_score_local - alt_score_local <= c_close_complete_score_gap then
                begin
                    Exit(True);
                end;
            end;
        end;

        function can_skip_preserved_quality_repair_local: Boolean;
        var
            preserved_text_local: string;
        begin
            Result := False;
            if allow_exact_span_repair or all_initial_compact_query or
                has_internal_dangling_initial then
            begin
                Exit;
            end;

            if input_syllable_count >= 7 then
            begin
                Exit(not has_close_complete_alternative_for_quality_repair_local);
            end;

            if (input_syllable_count <= 6) and
                (top_complete_path_is_backed_local or
                top_complete_path_has_only_multi_segments_local or
                top_complete_is_exact_query_local) then
            begin
                Exit(True);
            end;

            preserved_text_local := Trim(top_to_preserve.text);
            Result := (input_syllable_count >= 5) and
                (top_complete_path_is_backed_local or
                (not is_full_pinyin_key(lookup_text)) or
                has_extendable_trailing_prefix_query_text(lookup_text) or
                has_raw_safe_trailing_initial_typing_state or
                ((preserved_text_local <> '') and
                (get_candidate_text_unit_count(preserved_text_local) <
                input_syllable_count)));
        end;

        function can_skip_preserved_exact_cluster_repair_local: Boolean;
        var
            preserved_text_local: string;
        begin
            preserved_text_local := Trim(top_to_preserve.text);
            Result := (not allow_exact_span_repair) and
                (input_syllable_count <= 3) and
                (Pos('short312head=1', debug_extra) > 0);
            if Result then
            begin
                Exit;
            end;

            Result := (not allow_exact_span_repair) and
                (input_syllable_count >= 7) and
                (not all_initial_compact_query) and
                (not has_internal_dangling_initial);
            if Result then
            begin
                Exit;
            end;

            Result := (not allow_exact_span_repair) and
                (input_syllable_count >= 4) and
                (not all_initial_compact_query) and
                (not has_internal_dangling_initial) and
                (top_complete_path_is_backed_local or
                ((preserved_text_local <> '') and
                (get_candidate_text_unit_count(preserved_text_local) <
                input_syllable_count)));
            if Result then
            begin
                Exit;
            end;

            Result := (not allow_exact_span_repair) and
                (input_syllable_count >= 5) and
                (not all_initial_compact_query) and
                (not has_internal_dangling_initial) and
                (top_complete_path_is_backed_local or
                (not is_full_pinyin_key(lookup_text)) or
                has_extendable_trailing_prefix_query_text(lookup_text) or
                has_raw_safe_trailing_initial_typing_state or
                ((preserved_text_local <> '') and
                (get_candidate_text_unit_count(preserved_text_local) <
                input_syllable_count)));
        end;

        function try_complete_preserved_single_syllable_tail(
            const seed_candidate: TncCandidate; out out_candidate: TncCandidate): Boolean;
        const
            c_tail_probe_limit = 18;
        var
            tail_key: string;
            tail_results: TncCandidateList;
            tail_idx: Integer;
            tail_text: string;
            tail_score: Integer;
            best_text: string;
            best_score: Integer;
            is_preferred: Boolean;
            candidate_path: string;
            seed_units: Integer;
            seed_base_score: Integer;

            function can_complete_tail_key_local(const value: string): Boolean;
            begin
                Result := SameText(value, 'wo') or SameText(value, 'he') or
                    SameText(value, 'yi') or SameText(value, 'yu') or
                    SameText(value, 'you') or SameText(value, 'yao') or
                    SameText(value, 'shi') or SameText(value, 'zai') or
                    SameText(value, 'bu') or SameText(value, 'le') or
                    SameText(value, 'hai') or SameText(value, 'hen') or
                    SameText(value, 'hui') or SameText(value, 'neng') or
                    SameText(value, 'qu') or SameText(value, 'lai') or
                    SameText(value, 'gei') or SameText(value, 'de') or
                    SameText(value, 'ba') or SameText(value, 'ma') or
                    SameText(value, 'ne') or SameText(value, 'a') or
                    SameText(value, 'ya') or SameText(value, 'la');
            end;

            function is_short_particle_tail_key_local(const value: string): Boolean;
            begin
                Result := SameText(value, 'de') or SameText(value, 'le') or
                    SameText(value, 'ba') or SameText(value, 'ma') or
                    SameText(value, 'la') or SameText(value, 'a');
            end;

            function derived_particle_tail_score_local(const base_score: Integer): Integer;
            const
                c_particle_tail_min_head_score = 80;
                c_particle_tail_visible_bonus = 160;
                c_particle_tail_unit_bonus = 24;
                c_particle_tail_weak_bonus = 8;
            begin
                Result := base_score;
                if Result < 0 then
                begin
                    Result := 0;
                end;
                if Result < c_particle_tail_min_head_score then
                begin
                    Inc(Result, c_particle_tail_weak_bonus);
                    Exit;
                end;

                Inc(Result, c_particle_tail_visible_bonus +
                    (seed_units * c_particle_tail_unit_bonus));
            end;
        begin
            Result := False;
            out_candidate := seed_candidate;
            tail_key := normalize_pinyin_text(Trim(seed_candidate.comment));
            seed_units := get_candidate_text_unit_count(Trim(seed_candidate.text));
            if (Trim(seed_candidate.text) = '') or (tail_key = '') or
                (not is_full_pinyin_key(tail_key)) or
                (get_effective_compact_pinyin_unit_count(tail_key) <> 1) or
                (not can_complete_tail_key_local(tail_key)) or
                (seed_units <> input_syllable_count - 1) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(tail_key, tail_results) then
            begin
                Exit;
            end;

            best_text := '';
            best_score := Low(Integer);
            for tail_idx := 0 to High(tail_results) do
            begin
                if tail_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                tail_text := Trim(tail_results[tail_idx].text);
                if (get_candidate_text_unit_count(tail_text) <> 1) or
                    (not match_single_char_candidate_for_syllable(
                    tail_key, tail_text, is_preferred)) then
                begin
                    Continue;
                end;

                tail_score := tail_results[tail_idx].score;
                if tail_results[tail_idx].has_dict_weight and
                    (tail_results[tail_idx].dict_weight > tail_score) then
                begin
                    tail_score := tail_results[tail_idx].dict_weight;
                end;
                if is_preferred then
                begin
                    Inc(tail_score, 90);
                end;
                if tail_score > best_score then
                begin
                    best_score := tail_score;
                    best_text := tail_text;
                end;
            end;

            if best_text = '' then
            begin
                Exit;
            end;

            out_candidate.text := Trim(seed_candidate.text) + best_text;
            out_candidate.comment := '';
            seed_base_score := seed_candidate.score;
            if seed_candidate.has_dict_weight and
                (seed_candidate.dict_weight > 0) then
            begin
                seed_base_score := seed_candidate.dict_weight;
            end;
            if is_short_particle_tail_key_local(tail_key) then
            begin
                out_candidate.score :=
                    derived_particle_tail_score_local(seed_base_score);
                out_candidate.has_dict_weight := seed_candidate.has_dict_weight;
                out_candidate.dict_weight := seed_base_score;
            end
            else
            begin
                out_candidate.score := seed_candidate.score + 880;
                out_candidate.has_dict_weight := seed_candidate.has_dict_weight;
                out_candidate.dict_weight := seed_base_score;
            end;

            candidate_path := get_segment_path_for_candidate(seed_candidate);
            if candidate_path <> '' then
            begin
                candidate_path := candidate_path + c_segment_path_separator +
                    best_text;
            end
            else
            begin
                candidate_path := Trim(seed_candidate.text) +
                    c_segment_path_separator + best_text;
            end;
            remember_segment_path_for_candidate(out_candidate.text, '',
                candidate_path, out_candidate.score);
            Result := True;
    end;

        procedure ensure_preserved_dangling_initial_variants_visible(
            var candidates: TncCandidateList; const seed_candidate: TncCandidate);
        var
            tail_key: string;
            prefix_candidate: TncCandidate;
            partial_candidate: TncCandidate;
            predicted_candidate: TncCandidate;
            predicted_text: string;
            seed_text: string;
            candidate_path: string;
            seed_units: Integer;
            prefix_index: Integer;

            function is_single_initial_fragment_local(const value: string): Boolean;
            begin
                Result := (Length(value) = 1) and
                    CharInSet(value[1], ['b', 'p', 'm', 'f', 'd', 't', 'n',
                    'l', 'g', 'k', 'h', 'j', 'q', 'x', 'z', 'c', 's', 'r',
                    'y', 'w']);
            end;

            function get_common_initial_prediction_local(
                const value: string): string;
            begin
                Result := '';
                if SameText(value, 'w') then
                begin
                    Result := string(Char($6211));
                end
                else if SameText(value, 'h') then
                begin
                    Result := string(Char($548C));
                end
                else if SameText(value, 'q') then
                begin
                    Result := string(Char($53BB));
                end
                else if SameText(value, 'y') then
                begin
                    Result := string(Char($4E00));
                end;
            end;

            function has_candidate_local(const candidate_text: string;
                const candidate_comment: string): Boolean;
            var
                local_idx: Integer;
            begin
                Result := False;
                for local_idx := 0 to High(candidates) do
                begin
                    if SameText(Trim(candidates[local_idx].text), candidate_text) and
                        SameText(Trim(candidates[local_idx].comment),
                        candidate_comment) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function find_candidate_index_local(const candidate_text: string;
                const candidate_comment: string): Integer;
            var
                local_idx: Integer;
            begin
                Result := -1;
                for local_idx := 0 to High(candidates) do
                begin
                    if SameText(Trim(candidates[local_idx].text), candidate_text) and
                        SameText(Trim(candidates[local_idx].comment),
                        candidate_comment) then
                    begin
                        Exit(local_idx);
                    end;
                end;
            end;

            procedure insert_candidate_local(const candidate: TncCandidate;
                const target_index: Integer);
            var
                local_target: Integer;
                move_idx: Integer;
            begin
                local_target := target_index;
                if local_target < 0 then
                begin
                    local_target := 0;
                end;
                if local_target > Length(candidates) then
                begin
                    local_target := Length(candidates);
                end;

                SetLength(candidates, Length(candidates) + 1);
                for move_idx := High(candidates) downto local_target + 1 do
                begin
                    candidates[move_idx] := candidates[move_idx - 1];
                end;
                candidates[local_target] := candidate;
            end;

            procedure move_candidate_local(const candidate_index: Integer;
                const target_index: Integer);
            var
                local_target: Integer;
                move_idx: Integer;
                picked_candidate: TncCandidate;
            begin
                if (candidate_index < 0) or
                    (candidate_index > High(candidates)) then
                begin
                    Exit;
                end;
                local_target := target_index;
                if local_target < 0 then
                begin
                    local_target := 0;
                end;
                if local_target > High(candidates) then
                begin
                    local_target := High(candidates);
                end;
                if candidate_index = local_target then
                begin
                    Exit;
                end;

                picked_candidate := candidates[candidate_index];
                if candidate_index > local_target then
                begin
                    for move_idx := candidate_index downto local_target + 1 do
                    begin
                        candidates[move_idx] := candidates[move_idx - 1];
                    end;
                end
                else
                begin
                    for move_idx := candidate_index to local_target - 1 do
                    begin
                        candidates[move_idx] := candidates[move_idx + 1];
                    end;
                end;
                candidates[local_target] := picked_candidate;
            end;

        begin
            tail_key := normalize_pinyin_text(Trim(seed_candidate.comment));
            seed_text := Trim(seed_candidate.text);
            seed_units := get_candidate_text_unit_count(seed_text);
            if (Length(candidates) = 0) or (seed_text = '') or
                (not is_single_initial_fragment_local(tail_key)) or
                (seed_units <= 0) or (seed_units > input_syllable_count) then
            begin
                Exit;
            end;

            prefix_index := find_candidate_index_local(seed_text, '');
            if prefix_index >= 0 then
            begin
                if seed_units >= 3 then
                begin
                    move_candidate_local(prefix_index, 0);
                end;
            end
            else
            begin
                prefix_candidate := seed_candidate;
                prefix_candidate.comment := '';
                prefix_candidate.score := seed_candidate.score - 1;
                if seed_units >= 3 then
                begin
                    prefix_candidate.score := seed_candidate.score + 1;
                    insert_candidate_local(prefix_candidate, 0);
                end
                else
                begin
                    insert_candidate_local(prefix_candidate, 1);
                end;
            end;

            prefix_index := find_candidate_index_local(seed_text, tail_key);
            if prefix_index >= 0 then
            begin
                if seed_units >= 6 then
                begin
                    move_candidate_local(prefix_index, 0);
                end
                else
                begin
                    move_candidate_local(prefix_index, Min(1, High(candidates)));
                end;
            end
            else
            begin
                partial_candidate := seed_candidate;
                if seed_units >= 6 then
                begin
                    insert_candidate_local(partial_candidate, 0);
                end
                else
                begin
                    insert_candidate_local(partial_candidate,
                        Min(1, Length(candidates)));
                end;
            end;

            predicted_text := get_common_initial_prediction_local(tail_key);
            if (predicted_text = '') or
                (seed_units + 1 <> input_syllable_count) or
                has_candidate_local(seed_text + predicted_text, '') then
            begin
                Exit;
            end;

            predicted_candidate := seed_candidate;
            predicted_candidate.text := seed_text + predicted_text;
            predicted_candidate.comment := '';
            predicted_candidate.score := seed_candidate.score - 2;
            predicted_candidate.has_dict_weight := True;
            predicted_candidate.dict_weight := predicted_candidate.score;
            candidate_path := get_segment_path_for_candidate(seed_candidate);
            if candidate_path <> '' then
            begin
                candidate_path := candidate_path + c_segment_path_separator +
                    predicted_text;
            end
            else
            begin
                candidate_path := seed_text + c_segment_path_separator +
                    predicted_text;
            end;
            remember_segment_path_for_candidate(predicted_candidate.text, '',
                candidate_path, predicted_candidate.score);
            insert_candidate_local(predicted_candidate, Min(2, Length(candidates)));
        end;

        function can_use_lightweight_preserved_sort_local: Boolean;
        begin
            Result := False;
            preserved_lite_skip_debug_local := '';
            if input_syllable_count < 7 then
            begin
                if (input_syllable_count >= 3) and
                    (not allow_exact_span_repair) then
                begin
                    Exit(True);
                end;
                if (input_syllable_count >= 3) and
                    (top_complete_path_is_backed_local or
                    top_complete_is_exact_query_local) then
                begin
                    Exit(True);
                end;
                preserved_lite_skip_debug_local := Format(' pslite_skip=short%d',
                    [input_syllable_count]);
                Exit;
            end;

            Result := Length(m_candidates) > 0;
            if not Result then
            begin
                preserved_lite_skip_debug_local := ' pslite_skip=empty';
            end;
        end;

        function preserved_candidate_effective_score_local(
            const candidate_local: TncCandidate): Integer;
        begin
            Result := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > Result) then
            begin
                Result := candidate_local.dict_weight;
            end;
            if candidate_local.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function can_skip_high_confidence_preserved_sort_local: Boolean;
        begin
            Result := (not allow_exact_span_repair) and
                (input_syllable_count >= 5) and
                (Length(m_candidates) > 0) and
                top_complete_path_is_backed_local and
                (preserved_candidate_effective_score_local(top_to_preserve) >=
                50000);
        end;

        procedure restore_strong_pathful_preserved_top_local;
        const
            c_preserved_top_min_gap = -8000;
        var
            preserved_text_local: string;
            preserved_comment_local: string;
            preserved_path_local: string;
            preserved_score_local: Integer;
            current_score_local: Integer;
            candidate_idx_local: Integer;
            move_idx_local: Integer;
            moved_candidate_local: TncCandidate;
            moved_path_local: string;

            function preserved_path_has_visible_better_segment_local(
                const current_top_text_local: string): Boolean;
            const
                c_probe_limit = 6;
            var
                parser_local: TncPinyinParser;
                syllables_local: TncPinyinParseResult;
                path_segments_local: TArray<string>;
                segment_idx_local: Integer;
                segment_text_local: string;
                segment_units_local: Integer;
                syllable_pos_local: Integer;
                key_idx_local: Integer;
                segment_key_local: string;
                ranked_results_local: TncCandidateList;
                ranked_idx_local: Integer;
                ranked_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (lookup_text = '') or
                    (current_top_text_local = '') or
                    (SameText(current_top_text_local, preserved_text_local)) or
                    (top_to_preserve.source = cs_user) or
                    is_latest_session_query_choice(preserved_text_local) or
                    m_dictionary.is_base_entry(lookup_text,
                    preserved_text_local) or
                    (m_dictionary.get_query_choice_bonus(lookup_text,
                    preserved_text_local) > 0) then
                begin
                    Exit;
                end;

                path_segments_local := preserved_path_local.Split(
                    [c_segment_path_separator]);
                if Length(path_segments_local) <= 1 then
                begin
                    Exit;
                end;

                parser_local := TncPinyinParser.Create;
                try
                    syllables_local := parser_local.parse(lookup_text);
                finally
                    parser_local.Free;
                end;
                if Length(syllables_local) <> input_syllable_count then
                begin
                    Exit;
                end;

                syllable_pos_local := 0;
                for segment_idx_local := 0 to High(path_segments_local) do
                begin
                    segment_text_local := Trim(path_segments_local[segment_idx_local]);
                    segment_units_local := get_candidate_text_unit_count(
                        segment_text_local);
                    if (segment_text_local = '') or (segment_units_local <= 0) or
                        (syllable_pos_local + segment_units_local >
                        Length(syllables_local)) then
                    begin
                        Exit;
                    end;

                    if segment_units_local >= 2 then
                    begin
                        segment_key_local := '';
                        for key_idx_local := syllable_pos_local to
                            syllable_pos_local + segment_units_local - 1 do
                        begin
                            segment_key_local := segment_key_local +
                                normalize_pinyin_text(
                                syllables_local[key_idx_local].text);
                        end;

                        if (segment_key_local <> '') and
                            m_dictionary.lookup(segment_key_local,
                            ranked_results_local) then
                        begin
                            for ranked_idx_local := 0 to
                                Min(High(ranked_results_local),
                                c_probe_limit - 1) do
                            begin
                                ranked_text_local := Trim(
                                    ranked_results_local[ranked_idx_local].text);
                                if (Trim(ranked_results_local[ranked_idx_local].comment) <> '') or
                                    (ranked_text_local = '') or
                                    (get_candidate_text_unit_count(
                                    ranked_text_local) <> segment_units_local) then
                                begin
                                    Continue;
                                end;

                                if not SameText(ranked_text_local,
                                    segment_text_local) then
                                begin
                                    Exit(True);
                                end;
                                Break;
                            end;
                        end;
                    end;

                    Inc(syllable_pos_local, segment_units_local);
                end;
            end;
        begin
            preserved_text_local := Trim(top_to_preserve.text);
            preserved_comment_local := Trim(top_to_preserve.comment);
            if (preserved_text_local = '') or
                (preserved_comment_local <> '') or
                (get_candidate_text_unit_count(preserved_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            preserved_path_local := get_segment_path_for_candidate(
                top_to_preserve);
            if get_encoded_path_segment_count_local(preserved_path_local) <= 1 then
            begin
                Exit;
            end;

            preserved_score_local := preserved_candidate_effective_score_local(
                top_to_preserve);
            if Length(m_candidates) > 0 then
            begin
                current_score_local := preserved_candidate_effective_score_local(
                    m_candidates[0]);
                if SameText(Trim(m_candidates[0].text), preserved_text_local) and
                    SameText(Trim(m_candidates[0].comment),
                    preserved_comment_local) then
                begin
                    Exit;
                end;
                if preserved_score_local < current_score_local +
                    c_preserved_top_min_gap then
                begin
                    Exit;
                end;
                if (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                    input_syllable_count) and
                    preserved_path_has_visible_better_segment_local(
                    Trim(m_candidates[0].text)) then
                begin
                    Exit;
                end;
            end;

            candidate_idx_local := -1;
            for move_idx_local := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[move_idx_local].text),
                    preserved_text_local) and
                    SameText(Trim(m_candidates[move_idx_local].comment),
                    preserved_comment_local) then
                begin
                    candidate_idx_local := move_idx_local;
                    Break;
                end;
            end;

            if candidate_idx_local < 0 then
            begin
                SetLength(m_candidates, Length(m_candidates) + 1);
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(m_candidates));
                end;
                for move_idx_local := High(m_candidates) downto 1 do
                begin
                    m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
                    if move_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[move_idx_local] :=
                            m_candidate_segment_paths[move_idx_local - 1];
                    end;
                end;
                m_candidates[0] := top_to_preserve;
                if Length(m_candidate_segment_paths) > 0 then
                begin
                    m_candidate_segment_paths[0] := preserved_path_local;
                end;
                Exit;
            end;

            if candidate_idx_local = 0 then
            begin
                Exit;
            end;

            moved_candidate_local := m_candidates[candidate_idx_local];
            moved_path_local := '';
            if candidate_idx_local < Length(m_candidate_segment_paths) then
            begin
                moved_path_local := m_candidate_segment_paths[candidate_idx_local];
            end;
            if moved_path_local = '' then
            begin
                moved_path_local := preserved_path_local;
            end;

            for move_idx_local := candidate_idx_local downto 1 do
            begin
                m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
                if move_idx_local < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx_local] :=
                        m_candidate_segment_paths[move_idx_local - 1];
                end;
            end;
            m_candidates[0] := moved_candidate_local;
            if Length(m_candidate_segment_paths) > 0 then
            begin
                m_candidate_segment_paths[0] := moved_path_local;
            end;
        end;

        procedure promote_higher_scored_complete_after_preserved_local;
        const
            c_probe_limit = 6;
            c_min_score_gain = 12000;
        var
            best_idx_local: Integer;
            best_score_local: Integer;
            candidate_idx_local: Integer;
            candidate_score_local: Integer;
            move_idx_local: Integer;
            moved_candidate_local: TncCandidate;
            moved_path_local: string;

            function is_complete_candidate_local(
                const candidate_local: TncCandidate): Boolean;
            begin
                Result := (Trim(candidate_local.comment) = '') and
                    (get_candidate_text_unit_count(Trim(candidate_local.text)) =
                    input_syllable_count);
            end;
        begin
            if (not allow_exact_span_repair) or (input_syllable_count < 6) or
                (Length(m_candidates) < 2) or (top_to_preserve.source = cs_user) or
                (not is_complete_candidate_local(m_candidates[0])) then
            begin
                Exit;
            end;

            best_idx_local := 0;
            best_score_local := preserved_candidate_effective_score_local(
                m_candidates[0]);
            for candidate_idx_local := 1 to Min(c_probe_limit - 1,
                High(m_candidates)) do
            begin
                if not is_complete_candidate_local(
                    m_candidates[candidate_idx_local]) then
                begin
                    Continue;
                end;

                candidate_score_local := preserved_candidate_effective_score_local(
                    m_candidates[candidate_idx_local]);
                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_idx_local := candidate_idx_local;
                end;
            end;

            if (best_idx_local <= 0) or
                (best_score_local < preserved_candidate_effective_score_local(
                m_candidates[0]) + c_min_score_gain) then
            begin
                Exit;
            end;

            if Length(m_candidate_segment_paths) < Length(m_candidates) then
            begin
                SetLength(m_candidate_segment_paths, Length(m_candidates));
            end;

            moved_candidate_local := m_candidates[best_idx_local];
            moved_path_local := '';
            if best_idx_local < Length(m_candidate_segment_paths) then
            begin
                moved_path_local := m_candidate_segment_paths[best_idx_local];
            end;

            for move_idx_local := best_idx_local downto 1 do
            begin
                m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
                if move_idx_local < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx_local] :=
                        m_candidate_segment_paths[move_idx_local - 1];
                end;
            end;

            m_candidates[0] := moved_candidate_local;
            if Length(m_candidate_segment_paths) > 0 then
            begin
                m_candidate_segment_paths[0] := moved_path_local;
            end;
        end;

    begin
        top_to_preserve := preserved_top;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' [ps-tail]';
        end;
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        try_complete_preserved_single_syllable_tail(preserved_top, top_to_preserve);
        note_preserved_phase_elapsed_local('pstail1',
            preserved_phase_start_tick);
        if Length(m_candidates) = 0 then
        begin
            SetLength(m_candidates, 1);
        end;
        m_candidates[0] := top_to_preserve;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' [ps-dangling]';
        end;
        ensure_preserved_dangling_initial_variants_visible(m_candidates,
            top_to_preserve);
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' [ps-chain]';
        end;
        ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
        ensure_supported_short_four_two_prefix_partial_visible_local(
            get_effective_compact_pinyin_syllables(lookup_text),
            m_candidates);
        if can_use_trailing_prefix_preserved_fast_finalize_local then
        begin
            dedupe_visible_candidates_local(m_candidates,
                deduped_preserved_candidate_count);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            total_elapsed_ms := Int64(nc_monotonic_tick_ms - total_start_tick);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
                [lookup_elapsed_ms, segment_elapsed_ms, path_search_elapsed_ms,
                runtime_elapsed_ms, post_elapsed_ms, sort_elapsed_ms,
                lookup_cache_hits, lookup_cache_misses, total_elapsed_ms]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := debug_extra + ' preservedtailfast=1';
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
            end;
            refresh_candidate_segment_paths;
            m_visible_candidates_cache_valid := False;
            SetLength(m_visible_candidates_cache, 0);
            SetLength(m_visible_candidate_source_indices_cache, 0);
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' [ps123]';
        end;
        ensure_three_syllable_prefix_one_plus_two_complete_candidates_visible(
            m_candidates);
        note_preserved_phase_elapsed_local('ps123', preserved_phase_start_tick);
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' [ps-fqfast]';
        end;
        ensure_reliable_full_query_exact_complete_candidate_fast_visible(
            m_candidates);
        note_preserved_phase_elapsed_local('psfqfast',
            preserved_phase_start_tick);
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        filter_self_composed_complete_candidates_when_normal_complete_exists;
        note_preserved_phase_elapsed_local('psself',
            preserved_phase_start_tick);
        if allow_exact_span_repair then
        begin
            preserved_phase_start_tick := nc_monotonic_tick_ms;
            ensure_exact_span_rank_repair_candidate_visible(m_candidates);
            note_preserved_phase_elapsed_local('psexactspan',
                preserved_phase_start_tick);
        end;
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if not can_skip_preserved_exact_cluster_repair_local then
        begin
            ensure_short_full_query_exact_cluster_visible(m_candidates);
        end;
        note_preserved_phase_elapsed_local('psfqexact',
            preserved_phase_start_tick);
        dedupe_visible_candidates_local(m_candidates,
            deduped_preserved_candidate_count);
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if can_skip_high_confidence_preserved_sort_local then
        begin
            note_preserved_phase_elapsed_local('pssortskip',
                preserved_phase_start_tick);
        end
        else if can_use_lightweight_preserved_sort_local then
        begin
            sort_candidates_lightweight(m_candidates);
            if should_trim_strong_long_sentence_candidates_local(m_candidates) then
            begin
                trim_sorted_candidates_for_strong_long_sentence(m_candidates);
            end;
            note_preserved_phase_elapsed_local('pssortlite',
                preserved_phase_start_tick);
        end
        else
        begin
            sort_candidates(m_candidates);
            note_preserved_phase_elapsed_local('pssort',
                preserved_phase_start_tick);
        end;
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if not can_skip_preserved_exact_cluster_repair_local then
        begin
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
        end;
        note_preserved_phase_elapsed_local('psfqexact2',
            preserved_phase_start_tick);
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        refresh_candidate_segment_paths;
        note_preserved_phase_elapsed_local('psrefresh1',
            preserved_phase_start_tick);
        preserved_phase_start_tick := nc_monotonic_tick_ms;
        if ((input_syllable_count <= 3) and (not allow_exact_span_repair)) or
            can_skip_preserved_quality_repair_local then
        begin
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            restore_strong_pathful_preserved_top_local;
            ensure_long_sentence_prefix_partial_visible(m_candidates,
                get_candidate_limit);
            note_preserved_phase_elapsed_local('psqualityskip',
                preserved_phase_start_tick);
        end
        else
        begin
            ensure_high_quality_complete_candidate_in_top2(m_candidates);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_top_complete_candidate_prefix_partials_visible(m_candidates);
            demote_low_evidence_admin_place_alias_top_local(m_candidates);
            promote_full_query_exact_complete_phrase_local(m_candidates);
            promote_short_unsupported_complete_prefix_partial(m_candidates);
            ensure_preserved_dangling_initial_variants_visible(m_candidates,
                top_to_preserve);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            promote_preferred_three_syllable_one_plus_two_partial_local(m_candidates);
            restore_strong_pathful_preserved_top_local;
            ensure_long_sentence_prefix_partial_visible(m_candidates,
                get_candidate_limit);
            filter_self_composed_complete_candidates_when_normal_complete_exists;
            note_preserved_phase_elapsed_local('psquality',
                preserved_phase_start_tick);
        end;
        if build_fixed_wo_tail_dictionary_candidates(lookup_text,
            fixed_wo_tail_candidates) then
        begin
            m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                m_candidates, 0);
            sort_candidates_lightweight(m_candidates);
            m_candidates[0] := top_to_preserve;
        end;
        if build_short_particle_tail_dictionary_candidates(lookup_text,
            short_particle_tail_candidates) then
        begin
            m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                m_candidates, 0);
            sort_candidates_lightweight(m_candidates);
            m_candidates[0] := top_to_preserve;
        end;
        promote_higher_scored_complete_after_preserved_local;
        total_elapsed_ms := Int64(nc_monotonic_tick_ms - total_start_tick);
        m_last_lookup_timing_info := Format(
            'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
            [lookup_elapsed_ms, segment_elapsed_ms, path_search_elapsed_ms,
            runtime_elapsed_ms, post_elapsed_ms, sort_elapsed_ms,
            lookup_cache_hits, lookup_cache_misses, total_elapsed_ms]);
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := debug_extra;
            if preserved_lite_skip_debug_local <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    preserved_lite_skip_debug_local;
            end;
            if m_last_three_syllable_partial_preference_kind > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' b3pref=%d',
                    [m_last_three_syllable_partial_preference_kind]);
            end;
            if m_last_three_syllable_partial_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_three_syllable_partial_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
        end;
        refresh_candidate_segment_paths;
        m_visible_candidates_cache_valid := False;
        SetLength(m_visible_candidates_cache, 0);
        SetLength(m_visible_candidate_source_indices_cache, 0);
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

    function is_short_mixed_head_only_fast_anchor_query: Boolean;
    begin
        Result := head_only_multi_syllable and
            (input_syllable_count >= 2) and
            (input_syllable_count <= 2) and
            (not is_full_pinyin_key(lookup_text)) and
            (not has_internal_dangling_initial) and
            (not all_initial_compact_query);
    end;

    function has_multi_char_dictionary_anchor(const candidates: TncCandidateList): Boolean;
    var
        idx: Integer;
    begin
        Result := False;
        for idx := 0 to High(candidates) do
        begin
            if Length(candidates[idx].text) > 1 then
            begin
                Exit(True);
            end;
        end;
    end;

    function try_resolve_best_exact_subspan_local(const start_idx: Integer;
        const span_syllables: Integer; out out_text: string; out out_score: Integer;
        out out_segments: Integer; out out_path: string; out out_has_anchor: Boolean;
        out out_anchor_units: Integer): Boolean; forward;

    function get_preferred_query_phrase_bonus_local(const query_key: string;
        const candidate_text: string): Integer; forward;

    function try_get_preferred_query_phrase_text_local(const query_key: string;
        out out_text: string; out out_bonus: Integer): Boolean; forward;

    function has_short_exact_complete_anchor(
        const candidates: TncCandidateList): Boolean;
    var
        idx: Integer;
    begin
        Result := False;
        if (input_syllable_count < 2) or (input_syllable_count > 8) or
            ((not is_full_pinyin_key(lookup_text)) and
            (not is_short_mixed_head_only_fast_anchor_query)) then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) <> '') or
                (not candidates[idx].has_dict_weight) then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            Exit(True);
        end;
    end;

    procedure promote_short_exact_raw_weight_leader_local(
        var candidates: TncCandidateList);
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        rank_local: Integer;
        picked_local: TncCandidate;

        function is_complete_exact_local(const candidate: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            Result := False;
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;
            if not (candidate.has_dict_weight or (candidate.source = cs_user)) then
            begin
                Exit;
            end;

            text_local := Trim(candidate.text);
            Result := (text_local <> '') and
                (get_candidate_text_unit_count(text_local) = input_syllable_count);
        end;

        function get_exact_rank_local(const candidate: TncCandidate): Integer;
        begin
            if candidate.has_dict_weight then
            begin
                Result := candidate.dict_weight;
            end
            else
            begin
                Result := candidate.score;
            end;

            if candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx_local := 0 to High(candidates) do
        begin
            if not is_complete_exact_local(candidates[idx_local]) then
            begin
                Continue;
            end;

            rank_local := get_exact_rank_local(candidates[idx_local]);
            if (best_idx_local < 0) or (rank_local > best_rank_local) or
                ((rank_local = best_rank_local) and
                (candidates[idx_local].score > candidates[best_idx_local].score)) then
            begin
                best_idx_local := idx_local;
                best_rank_local := rank_local;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        picked_local := candidates[best_idx_local];
        for idx_local := best_idx_local downto 1 do
        begin
            candidates[idx_local] := candidates[idx_local - 1];
        end;
        candidates[0] := picked_local;
    end;

    function seed_short_full_exact_resolved_candidate_local(
        var io_candidates: TncCandidateList): Boolean;
    var
        resolved_text_local: string;
        resolved_path_local: string;
        resolved_score_local: Integer;
        resolved_segments_local: Integer;
        resolved_anchor_units_local: Integer;
        resolved_has_anchor_local: Boolean;
        support_score_local: Integer;
        support_segments_local: Integer;
        support_path_local: string;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
        existing_index_local: Integer;
        idx_local: Integer;
        seeded_candidate_local: TncCandidate;

        function has_precise_extendable_tail_local: Boolean;
        var
            syllables_local: TncPinyinParseResult;
            tail_text_local: string;
            suffix_ch_local: Char;
            extended_tail_local: string;
        begin
            Result := False;
            if not is_full_pinyin_key(lookup_text) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) = 0 then
            begin
                Exit;
            end;

            tail_text_local := normalize_pinyin_text(
                syllables_local[High(syllables_local)].text);
            if tail_text_local = '' then
            begin
                Exit;
            end;

            for suffix_ch_local := 'a' to 'z' do
            begin
                extended_tail_local := tail_text_local + suffix_ch_local;
                if is_full_pinyin_key(extended_tail_local) and
                    (get_effective_compact_pinyin_unit_count(
                    extended_tail_local) = 1) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_covering_partial_anchor_local: Boolean;
        var
            local_idx: Integer;
            partial_units_local: Integer;
            comment_units_local: Integer;
        begin
            Result := False;
            for local_idx := 0 to High(io_candidates) do
            begin
                if Trim(io_candidates[local_idx].comment) = '' then
                begin
                    Continue;
                end;

                partial_units_local := get_candidate_text_unit_count(
                    Trim(io_candidates[local_idx].text));
                if partial_units_local <= 0 then
                begin
                    Continue;
                end;

                comment_units_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(io_candidates[local_idx].comment)));
                if (comment_units_local > 0) and
                    (partial_units_local + comment_units_local =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        if (not short_full_exact_query) or (m_dictionary = nil) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 2) or (input_syllable_count > 2) then
        begin
            Exit;
        end;

        if not try_resolve_best_exact_subspan_local(0, input_syllable_count,
            resolved_text_local, resolved_score_local, resolved_segments_local,
            resolved_path_local, resolved_has_anchor_local,
            resolved_anchor_units_local) then
        begin
            Exit;
        end;

        if (Trim(resolved_text_local) = '') or
            (get_candidate_text_unit_count(Trim(resolved_text_local)) <>
            input_syllable_count) then
        begin
            Exit;
        end;

        if has_precise_extendable_tail_local and
            has_covering_partial_anchor_local and
            (resolved_score_local < 4096) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' sfx=[skip weak_prefix text=%s]',
                    [Trim(resolved_text_local)]);
            end;
            Exit;
        end;

        if try_get_preferred_query_phrase_text_local(lookup_text,
            preferred_text_local, preferred_bonus_local) and
            (Trim(preferred_text_local) <> '') and
            (not SameText(Trim(preferred_text_local), Trim(resolved_text_local))) and
            (get_candidate_text_unit_count(Trim(preferred_text_local)) =
            input_syllable_count) then
        begin
            resolved_text_local := Trim(preferred_text_local);
            Inc(resolved_score_local, preferred_bonus_local);
            resolved_path_local := '';
        end;

        support_path_local := '';
        support_score_local := 0;
        support_segments_local := 0;
        if Trim(resolved_path_local) = '' then
        begin
            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(resolved_text_local), support_score_local,
                support_segments_local);
            if Trim(support_path_local) <> '' then
            begin
                resolved_path_local := support_path_local;
            end;
        end;
        if Trim(resolved_path_local) = '' then
        begin
            Exit;
        end;
        if (m_dictionary <> nil) and
            is_low_evidence_admin_place_alias_cached(
            lookup_text, Trim(resolved_text_local)) then
        begin
            Exit;
        end;

        existing_index_local := -1;
        for idx_local := 0 to High(io_candidates) do
        begin
            if (Trim(io_candidates[idx_local].comment) = '') and
                SameText(Trim(io_candidates[idx_local].text),
                Trim(resolved_text_local)) then
            begin
                existing_index_local := idx_local;
                Break;
            end;
        end;

        if existing_index_local = 0 then
        begin
            if not io_candidates[0].has_dict_weight then
            begin
                io_candidates[0].has_dict_weight := True;
                io_candidates[0].dict_weight := Max(io_candidates[0].score,
                    resolved_score_local);
            end;
            remember_segment_path_for_candidate(Trim(io_candidates[0].text), '',
                resolved_path_local, io_candidates[0].dict_weight);
            Exit(True);
        end;

        if existing_index_local > 0 then
        begin
            seeded_candidate_local := io_candidates[existing_index_local];
            if not seeded_candidate_local.has_dict_weight then
            begin
                seeded_candidate_local.has_dict_weight := True;
                seeded_candidate_local.dict_weight := Max(
                    seeded_candidate_local.score, resolved_score_local);
            end;
            for idx_local := existing_index_local downto 1 do
            begin
                io_candidates[idx_local] := io_candidates[idx_local - 1];
            end;
            io_candidates[0] := seeded_candidate_local;
            remember_segment_path_for_candidate(Trim(io_candidates[0].text), '',
                resolved_path_local, io_candidates[0].dict_weight);
            Exit(True);
        end;

        FillChar(seeded_candidate_local, SizeOf(seeded_candidate_local), 0);
        seeded_candidate_local.text := Trim(resolved_text_local);
        seeded_candidate_local.comment := '';
        seeded_candidate_local.score := Max(resolved_score_local, 2048);
        seeded_candidate_local.source := cs_rule;
        seeded_candidate_local.has_dict_weight := True;
        seeded_candidate_local.dict_weight := seeded_candidate_local.score;

        SetLength(io_candidates, Length(io_candidates) + 1);
        for idx_local := High(io_candidates) downto 1 do
        begin
            io_candidates[idx_local] := io_candidates[idx_local - 1];
        end;
        io_candidates[0] := seeded_candidate_local;
        remember_segment_path_for_candidate(Trim(io_candidates[0].text), '',
            resolved_path_local, io_candidates[0].dict_weight);
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' sfx=[seed text=%s seg=%d]',
                [Trim(resolved_text_local), resolved_segments_local]);
        end;
        Result := True;
    end;

    function get_fixed_resolved_boundary_char_local(
        const syllable_text: string): string;
    var
        normalized_syllable: string;
    begin
        Result := '';
        normalized_syllable := normalize_pinyin_text(syllable_text);
        if (normalized_syllable = 'ta') or
            (normalized_syllable = 'qing') or
            (normalized_syllable = 'bei') then
        begin
            Exit;
        end;
        if normalized_syllable = 'dan' then
        begin
            Result := string(Char($4F46));
        end
        else if normalized_syllable = 'sui' then
        begin
            Result := string(Char($867D));
        end
        else if normalized_syllable = 'wo' then
        begin
            Result := string(Char($6211));
        end
        else if normalized_syllable = 'ni' then
        begin
            Result := string(Char($4F60));
        end
        else if normalized_syllable = 'ta' then
        begin
            Result := string(Char($4ED6));
        end
        else if normalized_syllable = 'he' then
        begin
            Result := string(Char($548C));
        end
        else if normalized_syllable = 'qu' then
        begin
            Result := string(Char($53BB));
        end
        else if normalized_syllable = 'shi' then
        begin
            Result := string(Char($662F));
        end
        else if normalized_syllable = 'yi' then
        begin
            Result := string(Char($4E00));
        end
        else if normalized_syllable = 'ye' then
        begin
            Result := string(Char($4E5F));
        end
        else if normalized_syllable = 'hui' then
        begin
            Result := string(Char($4F1A));
        end
        else if normalized_syllable = 'ba' then
        begin
            Result := string(Char($628A));
        end
        else if normalized_syllable = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_syllable = 'zai' then
        begin
            Result := string(Char($5728));
        end
        else if normalized_syllable = 'zhe' then
        begin
            Result := string(Char($8FD9));
        end
        else if normalized_syllable = 'que' then
        begin
            Result := string(Char($5374));
        end
        else if normalized_syllable = 'bing' then
        begin
            Result := string(Char($5E76));
        end
        else if normalized_syllable = 'qie' then
        begin
            Result := string(Char($4E14));
        end;
    end;

    function build_resolved_sentence_candidate_local(
        out out_candidate: TncCandidate; out out_path: string): Boolean;
    const
        c_min_syllables = 6;
        c_max_chunk_syllables = 5;
        c_probe_limit = 4;
        c_single_probe_limit = 6;
        c_single_weight_floor = 120;
        c_fixed_single_score = 1200;
        c_single_penalty = 1600;
        c_chunk_length_bonus = 1400;
        c_chunk_anchor_bonus = 3600;
        c_join_penalty = 220;
        c_context_scale = 4;
        c_prefix_scale = 2;
    type
        TResolvedSentenceState = record
            valid: Boolean;
            score: Integer;
            segments: Integer;
            text: string;
            path: string;
            first_chunk: string;
        end;
    var
        syllables_local: TncPinyinParseResult;
        states_local: array of TResolvedSentenceState;
        pos_local: Integer;
        seg_len_local: Integer;
        next_pos_local: Integer;
        fixed_text_local: string;
        query_key_local: string;
        exact_results_local: TncCandidateList;
        candidate_idx_local: Integer;
        candidate_text_local: string;
        candidate_score_local: Integer;
        candidate_has_anchor_local: Boolean;
        candidate_anchor_units_local: Integer;
        candidate_local: TncCandidate;
        reach_pos_local: Integer;
        preferred_text_local: string;
        preferred_bonus_local: Integer;

        function lookup_exact_resolved_cached_local(const pinyin_key: string;
            out out_results: TncCandidateList): Boolean;
        begin
            Result := lookup_exact_full_pinyin_cached_local(pinyin_key,
                out_results);
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to
                start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function candidate_respects_fixed_boundaries_local(
            const start_idx: Integer; const syllable_count_local: Integer;
            const text_value: string): Boolean;
        var
            units_local: TArray<string>;
            local_idx: Integer;
            fixed_char_local: string;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;

            if syllable_count_local > 1 then
            begin
                Exit(True);
            end;

            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <> syllable_count_local then
            begin
                Exit(False);
            end;

            for local_idx := 0 to syllable_count_local - 1 do
            begin
                fixed_char_local := get_fixed_resolved_boundary_char_local(
                    syllables_local[start_idx + local_idx].text);
                if (fixed_char_local <> '') and
                    (not SameText(units_local[local_idx], fixed_char_local)) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function get_effective_weight_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and
                (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
        end;

        function is_better_state_local(const left_state: TResolvedSentenceState;
            const right_state: TResolvedSentenceState): Boolean;
        begin
            Result := False;
            if not left_state.valid then
            begin
                Exit(False);
            end;
            if not right_state.valid then
            begin
                Exit(True);
            end;
            if left_state.score > right_state.score then
            begin
                Exit(True);
            end;
            if left_state.score < right_state.score then
            begin
                Exit(False);
            end;
            if get_candidate_text_unit_count(left_state.first_chunk) >
                get_candidate_text_unit_count(right_state.first_chunk) then
            begin
                Exit(True);
            end;
            if get_candidate_text_unit_count(left_state.first_chunk) <
                get_candidate_text_unit_count(right_state.first_chunk) then
            begin
                Exit(False);
            end;
            if left_state.segments < right_state.segments then
            begin
                Exit(True);
            end;
            if left_state.segments > right_state.segments then
            begin
                Exit(False);
            end;
            if (left_state.text <> '') and
                ((right_state.text = '') or
                (CompareText(left_state.text, right_state.text) < 0)) then
            begin
                Exit(True);
            end;
        end;

        function path_has_multi_char_chunk_local(const encoded_path: string): Boolean;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
        begin
            Result := False;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                if get_candidate_text_unit_count(
                    Trim(path_parts_local[part_idx_local])) >= 2 then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_modifier_prefix_single_local(const candidate_text: string): Boolean;
        begin
            Result := False;
            if Length(candidate_text) <> 1 then
            begin
                Exit;
            end;

            case Ord(candidate_text[1]) of
                $4E0D, $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1,
                $8981, $4F1A, $80FD, $53EF, $88AB, $628A, $8BA9, $7ED9,
                $5BF9, $5728, $4EE5, $6709, $65E0, $5982, $8001, $5C0F,
                $5927, $5168, $8D85, $771F, $597D, $800C, $5E76:
                    Result := True;
            end;
        end;

        procedure try_update_state_local(const start_idx: Integer;
            const syllable_count_local: Integer; const chunk_text: string;
            const chunk_path: string; const base_score: Integer;
            const has_anchor_local: Boolean; const anchor_units_local: Integer);
        var
            local_state: TResolvedSentenceState;
            next_state_local: TResolvedSentenceState;
            total_score_local: Integer;
            total_path_local: string;
            local_pair_units: Integer;
            local_pair_key: string;
            local_pair_text: string;
        begin
            if (chunk_text = '') or
                (get_candidate_text_unit_count(Trim(chunk_text)) <>
                syllable_count_local) then
            begin
                Exit;
            end;

            next_pos_local := start_idx + syllable_count_local;
            if (next_pos_local < input_syllable_count) and
                (not states_local[next_pos_local].valid) then
            begin
                Exit;
            end;

            total_score_local := base_score +
                (syllable_count_local * c_chunk_length_bonus);
            if has_anchor_local then
            begin
                Inc(total_score_local, c_chunk_anchor_bonus +
                    (anchor_units_local * 192));
            end;
            if syllable_count_local = 1 then
            begin
                Dec(total_score_local, c_single_penalty);
            end;

            local_state.valid := True;
            local_state.segments := 1;
            local_state.first_chunk := Trim(chunk_text);
            local_state.text := Trim(chunk_text);
            if Trim(chunk_path) <> '' then
            begin
                total_path_local := Trim(chunk_path);
            end
            else
            begin
                total_path_local := Trim(chunk_text);
            end;
            local_state.path := total_path_local;

            if next_pos_local < input_syllable_count then
            begin
                next_state_local := states_local[next_pos_local];
                Inc(total_score_local, next_state_local.score);
                Dec(total_score_local, c_join_penalty);
                local_state.segments := local_state.segments +
                    next_state_local.segments;
                local_state.text := local_state.text + next_state_local.text;
                if next_state_local.path <> '' then
                begin
                    local_state.path := local_state.path + c_segment_path_separator +
                        next_state_local.path;
                end;
                if get_candidate_text_unit_count(next_state_local.first_chunk) >= 2 then
                begin
                    if syllable_count_local = 1 then
                    begin
                        if is_modifier_prefix_single_local(Trim(chunk_text)) then
                        begin
                            Inc(total_score_local, 900);
                        end
                        else
                        begin
                            Dec(total_score_local, 1400);
                        end;
                    end;
                end
                else if (syllable_count_local >= 2) and
                    (get_candidate_text_unit_count(next_state_local.first_chunk) = 1) then
                begin
                    Inc(total_score_local, 900);
                end;
                if (m_dictionary <> nil) and
                    (next_state_local.first_chunk <> '') then
                begin
                    Inc(total_score_local,
                        m_dictionary.get_context_bonus(
                        Trim(chunk_text), next_state_local.first_chunk) *
                        c_context_scale);
                    Inc(total_score_local,
                        get_sentence_text_prefix_bonus_local(
                        Trim(chunk_text) + next_state_local.first_chunk) *
                        c_prefix_scale);

                    local_pair_units := syllable_count_local +
                        get_candidate_text_unit_count(next_state_local.first_chunk);
                    if (local_pair_units > syllable_count_local) and
                        (local_pair_units <= 6) then
                    begin
                        local_pair_key := build_query_key_local(start_idx,
                            local_pair_units);
                        local_pair_text := Trim(chunk_text) +
                            next_state_local.first_chunk;
                        Inc(total_score_local,
                            get_preferred_query_phrase_bonus_local(
                            local_pair_key, local_pair_text));
                    end;
                end;
            end;

            if start_idx = 0 then
            begin
                Inc(total_score_local,
                    get_preferred_query_phrase_bonus_local(lookup_text,
                    local_state.text));
            end;

            local_state.score := total_score_local;
            if is_better_state_local(local_state, states_local[start_idx]) then
            begin
                states_local[start_idx] := local_state;
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_path := '';
        if (m_dictionary = nil) or (input_syllable_count < c_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            (m_disable_subspan_standalone_oracle > 0) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text,
            m_last_lookup_normalized_from <> '');
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(states_local, input_syllable_count + 1);
        states_local[0].valid := True;
        states_local[input_syllable_count].score := 0;
        states_local[input_syllable_count].segments := 0;
        states_local[input_syllable_count].text := '';
        states_local[input_syllable_count].path := '';
        states_local[input_syllable_count].first_chunk := '';

        for pos_local := input_syllable_count - 1 downto 0 do
        begin
            fixed_text_local := get_fixed_resolved_boundary_char_local(
                syllables_local[pos_local].text);
            if fixed_text_local <> '' then
            begin
                candidate_score_local := c_fixed_single_score;
                query_key_local := build_query_key_local(pos_local, 1);
                if lookup_exact_resolved_cached_local(query_key_local,
                    exact_results_local) then
                begin
                    for candidate_idx_local := 0 to High(exact_results_local) do
                    begin
                        if candidate_idx_local >= c_probe_limit then
                        begin
                            Break;
                        end;
                        if (Trim(exact_results_local[candidate_idx_local].comment) = '') and
                            SameText(Trim(exact_results_local[candidate_idx_local].text),
                            fixed_text_local) then
                        begin
                            candidate_score_local := Max(candidate_score_local,
                                get_effective_weight_local(
                                exact_results_local[candidate_idx_local]));
                        end;
                    end;
                end;
                try_update_state_local(pos_local, 1, fixed_text_local,
                    fixed_text_local, candidate_score_local, False, 0);
            end
            else
            begin
                query_key_local := build_query_key_local(pos_local, 1);
                if (query_key_local <> '') and
                    lookup_exact_resolved_cached_local(query_key_local,
                    exact_results_local) then
                begin
                    for candidate_idx_local := 0 to High(exact_results_local) do
                    begin
                        if candidate_idx_local >= c_single_probe_limit then
                        begin
                            Break;
                        end;
                        candidate_local := exact_results_local[candidate_idx_local];
                        if Trim(candidate_local.comment) <> '' then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(candidate_local.text);
                        if get_candidate_text_unit_count(candidate_text_local) <> 1 then
                        begin
                            Continue;
                        end;

                        candidate_score_local := get_effective_weight_local(
                            candidate_local);
                        if candidate_score_local < c_single_weight_floor then
                        begin
                            Continue;
                        end;

                        Dec(candidate_score_local, candidate_idx_local * 24);
                        try_update_state_local(pos_local, 1,
                            candidate_text_local, candidate_text_local,
                            candidate_score_local, False, 0);
                    end;
                end;
            end;

            for seg_len_local := 2 to Min(c_max_chunk_syllables,
                input_syllable_count - pos_local) do
            begin
                query_key_local := build_query_key_local(pos_local,
                    seg_len_local);

                if query_key_local = '' then
                begin
                    Continue;
                end;

                preferred_text_local := '';
                preferred_bonus_local := 0;
                if try_get_preferred_query_phrase_text_local(query_key_local,
                    preferred_text_local, preferred_bonus_local) and
                    (get_candidate_text_unit_count(preferred_text_local) =
                    seg_len_local) and
                    candidate_respects_fixed_boundaries_local(pos_local,
                    seg_len_local, preferred_text_local) then
                begin
                    candidate_score_local := preferred_bonus_local +
                        (seg_len_local * c_chunk_length_bonus) + 2048;
                    candidate_has_anchor_local := seg_len_local >= 3;
                    if candidate_has_anchor_local then
                    begin
                        candidate_anchor_units_local := seg_len_local;
                    end
                    else
                    begin
                        candidate_anchor_units_local := 0;
                    end;
                    try_update_state_local(pos_local, seg_len_local,
                        preferred_text_local, preferred_text_local,
                        candidate_score_local, candidate_has_anchor_local,
                        candidate_anchor_units_local);
                end;

                if not lookup_exact_resolved_cached_local(query_key_local,
                    exact_results_local) then
                begin
                    Continue;
                end;

                for candidate_idx_local := 0 to High(exact_results_local) do
                begin
                    if candidate_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    candidate_local := exact_results_local[candidate_idx_local];
                    if Trim(candidate_local.comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(candidate_local) or
                        is_runtime_common_pattern_candidate(candidate_local) or
                        is_runtime_redup_candidate(candidate_local) then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(candidate_local.text);
                    if (get_candidate_text_unit_count(candidate_text_local) <>
                        seg_len_local) or
                        (not candidate_respects_fixed_boundaries_local(pos_local,
                        seg_len_local, candidate_text_local)) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := get_effective_weight_local(
                        candidate_local);
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(query_key_local,
                        candidate_text_local));
                    if seg_len_local >= 4 then
                    begin
                        Inc(candidate_score_local, 2048);
                    end;
                    if seg_len_local >= 5 then
                    begin
                        Inc(candidate_score_local, 2048);
                    end;
                    if seg_len_local >= 3 then
                    begin
                        Inc(candidate_score_local,
                            get_sentence_text_prefix_bonus_local(
                            candidate_text_local) * c_prefix_scale);
                    end;

                    candidate_has_anchor_local := seg_len_local >= 3;
                    if candidate_has_anchor_local then
                    begin
                        candidate_anchor_units_local := seg_len_local;
                    end
                    else
                    begin
                        candidate_anchor_units_local := 0;
                    end;

                    try_update_state_local(pos_local, seg_len_local,
                        candidate_text_local, candidate_text_local,
                        candidate_score_local, candidate_has_anchor_local,
                        candidate_anchor_units_local);
                end;
            end;
        end;

        if (not states_local[0].valid) or
            (get_candidate_text_unit_count(Trim(states_local[0].text)) <>
            input_syllable_count) then
        begin
            if m_config.debug_mode then
            begin
                reach_pos_local := 0;
                for pos_local := input_syllable_count downto 0 do
                begin
                    if states_local[pos_local].valid then
                    begin
                        reach_pos_local := pos_local;
                        Break;
                    end;
                end;
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vrsmiss=[reach=%d/%d text=%s]',
                    [reach_pos_local, input_syllable_count,
                    Trim(states_local[reach_pos_local].text)]);
            end;
            Exit;
        end;
        if not path_has_multi_char_chunk_local(states_local[0].path) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' vrs=[skip all_single]';
            end;
            Exit;
        end;

        out_candidate.text := Trim(states_local[0].text);
        out_candidate.comment := '';
        out_candidate.score := states_local[0].score;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        out_path := Trim(states_local[0].path);
        Result := out_candidate.text <> '';
    end;

    function try_get_resolved_sentence_candidate_cached_local(
        out out_candidate: TncCandidate; out out_path: string): Boolean;
    var
        resolved_segments_local: Integer;
        resolved_anchor_units_local: Integer;
        resolved_has_anchor_local: Boolean;
    begin
        if resolved_sentence_candidate_built then
        begin
            out_candidate := resolved_sentence_candidate_cached;
            out_path := resolved_sentence_path_cached;
            Exit(resolved_sentence_candidate_ok);
        end;

        resolved_sentence_candidate_built := True;
        resolved_sentence_candidate_ok := False;
        FillChar(resolved_sentence_candidate_cached,
            SizeOf(resolved_sentence_candidate_cached), 0);
        resolved_sentence_path_cached := '';
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_path := '';

        if try_resolve_best_exact_subspan_local(0, input_syllable_count,
            out_candidate.text, out_candidate.score,
            resolved_segments_local, out_path,
            resolved_has_anchor_local, resolved_anchor_units_local) then
        begin
            out_candidate.comment := '';
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := False;
            out_candidate.dict_weight := 0;
            resolved_sentence_candidate_ok := True;
        end
        else if build_resolved_sentence_candidate_local(out_candidate,
            out_path) then
        begin
            resolved_sentence_candidate_ok := True;
        end;

        if resolved_sentence_candidate_ok then
        begin
            resolved_sentence_candidate_cached := out_candidate;
            resolved_sentence_path_cached := out_path;
        end;
        Result := resolved_sentence_candidate_ok;
    end;

    function ensure_best_resolved_sentence_candidate_visible(
        var candidates: TncCandidateList): Boolean;
    const
        c_promote_margin = 2048;
    var
        resolved_candidate_local: TncCandidate;
        resolved_path_local: string;
        idx_local: Integer;
        existing_index_local: Integer;
        top_score_local: Integer;
        resolved_score_local: Integer;
        support_score_local: Integer;
        support_segments_local: Integer;
        support_path_local: string;
        top_text_local: string;
        top_is_base_entry_local: Boolean;
        resolved_multi_chunks_local: Integer;
        quality_syllables_local: TncPinyinParseResult;
        top_path_local: string;
        top_quality_local: Integer;
        resolved_quality_local: Integer;

        function count_multi_char_path_chunks_local(const encoded_path: string): Integer;
        var
            parts_local: TArray<string>;
            part_idx_local: Integer;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;
            parts_local := encoded_path.Split([c_segment_path_separator]);
            for part_idx_local := 0 to High(parts_local) do
            begin
                if get_candidate_text_unit_count(Trim(parts_local[part_idx_local])) >= 2 then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function is_modifier_single_text_for_resolved_quality_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $6211, $4F60, $4ED6, $5979,
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5927, $5168, $8D85, $771F,
                $597D, $5982, $633A, $8F83, $6781, $800C, $5E76:
                    Result := True;
            end;
        end;

        function build_resolved_quality_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(quality_syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    quality_syllables_local[key_idx_local].text);
            end;
        end;

        function get_resolved_quality_exact_weight_local(
            const start_idx_local: Integer; const count_local: Integer;
            const expected_text_local: string): Integer;
        var
            key_local: string;
            results_local: TncCandidateList;
            idx_local: Integer;
            text_local: string;
            weight_local: Integer;
        begin
            Result := Low(Integer);
            key_local := build_resolved_quality_key_local(start_idx_local,
                count_local);
            if (key_local = '') or
                (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(key_local,
                results_local)) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(results_local) do
            begin
                if idx_local >= 12 then
                begin
                    Break;
                end;
                if Trim(results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                text_local := Trim(results_local[idx_local].text);
                if (text_local = '') or
                    (get_candidate_text_unit_count(text_local) <> count_local) or
                    ((expected_text_local <> '') and
                    (not SameText(text_local, expected_text_local))) then
                begin
                    Continue;
                end;

                weight_local := results_local[idx_local].score;
                if results_local[idx_local].has_dict_weight and
                    (results_local[idx_local].dict_weight > weight_local) then
                begin
                    weight_local := results_local[idx_local].dict_weight;
                end;
                if weight_local > Result then
                begin
                    Result := weight_local;
                end;
            end;
        end;

        function get_resolved_path_quality_local(const encoded_path: string): Integer;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            pos_local: Integer;
            segment_text_local: string;
            unit_count_local: Integer;
            exact_weight_local: Integer;
        begin
            Result := 0;
            if (Trim(encoded_path) = '') or (Length(quality_syllables_local) = 0) then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            pos_local := 0;
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                unit_count_local := get_candidate_text_unit_count(segment_text_local);
                if (unit_count_local <= 0) or
                    (pos_local + unit_count_local > Length(quality_syllables_local)) then
                begin
                    Exit;
                end;

                exact_weight_local := get_resolved_quality_exact_weight_local(
                    pos_local, unit_count_local, segment_text_local);
                if exact_weight_local > Low(Integer) div 4 then
                begin
                    Inc(Result, Min(20000, exact_weight_local * 4));
                    if (unit_count_local >= 2) and
                        (exact_weight_local >= 700) then
                    begin
                        Inc(Result, unit_count_local * 2200);
                    end;
                    if (unit_count_local >= 2) and
                        (exact_weight_local < 520) then
                    begin
                        Dec(Result, 3000 + unit_count_local * 1000);
                    end;
                    if (unit_count_local >= 4) and
                        (exact_weight_local >= 700) then
                    begin
                        Inc(Result, 8000 + (unit_count_local - 4) * 2200);
                    end;
                end
                else if unit_count_local >= 2 then
                begin
                    Dec(Result, 12000 + unit_count_local * 2500);
                end
                else if is_modifier_single_text_for_resolved_quality_local(
                    segment_text_local) then
                begin
                    Inc(Result, 900);
                end
                else
                begin
                    Dec(Result, 500);
                end;

                Inc(pos_local, unit_count_local);
            end;
        end;
    begin
        Result := False;
        FillChar(resolved_candidate_local, SizeOf(resolved_candidate_local), 0);
        resolved_path_local := '';
        if not try_get_resolved_sentence_candidate_cached_local(
            resolved_candidate_local, resolved_path_local) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' vrs=[miss]';
            end;
            Exit;
        end;

        support_score_local := 0;
        support_segments_local := 0;
        support_path_local := '';
        if Trim(resolved_path_local) = '' then
        begin
            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(resolved_candidate_local.text),
                support_score_local, support_segments_local);
            if Trim(support_path_local) <> '' then
            begin
                resolved_path_local := Trim(support_path_local);
            end;
        end;
        if Trim(resolved_path_local) <> '' then
        begin
            remember_segment_path_for_candidate(Trim(resolved_candidate_local.text),
                '', Trim(resolved_path_local),
                resolved_candidate_local.score + support_score_local);
        end;

        existing_index_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text),
                Trim(resolved_candidate_local.text)) then
            begin
                existing_index_local := idx_local;
                Break;
            end;
        end;

        resolved_score_local := resolved_candidate_local.score +
            support_score_local;
        resolved_multi_chunks_local :=
            count_multi_char_path_chunks_local(resolved_path_local);
        quality_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        resolved_quality_local := get_resolved_path_quality_local(
            resolved_path_local) + resolved_score_local div 32;
        if (Length(candidates) > 0) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            top_text_local := Trim(candidates[0].text);
            top_is_base_entry_local := (m_dictionary <> nil) and
                m_dictionary.is_base_entry(lookup_text, top_text_local);
            if (not is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidates[0])) and
                (top_is_base_entry_local or
                (resolved_multi_chunks_local < 3)) and
                (existing_index_local <> 0) then
            begin
                top_score_local := get_rank_score(candidates[0]);
                support_score_local := 0;
                support_segments_local := 0;
                if infer_segment_path_for_query_text_with_score(lookup_text,
                    top_text_local, support_score_local,
                    support_segments_local) <> '' then
                begin
                    Inc(top_score_local, support_score_local);
                end;
                top_path_local := get_segment_path_for_candidate(candidates[0], 0);
                if Trim(top_path_local) = '' then
                begin
                    top_path_local := infer_segment_path_for_query_text_with_score(
                        lookup_text, top_text_local, support_score_local,
                        support_segments_local);
                end;
                top_quality_local := get_resolved_path_quality_local(
                    top_path_local) + top_score_local div 32;
                if (resolved_score_local <= top_score_local + c_promote_margin) and
                    (resolved_quality_local <= top_quality_local + 3600) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' vrs=[keep resolved=%s toptext=%s score=%d top=%d rq=%d tq=%d]',
                            [Trim(resolved_candidate_local.text),
                            Trim(candidates[0].text), resolved_score_local,
                            top_score_local, resolved_quality_local,
                            top_quality_local]);
                    end;
                    Exit(False);
                end;
            end;
        end;

        if existing_index_local = 0 then
        begin
            Exit(True);
        end;

        if (Length(candidates) > 0) and
            (resolved_candidate_local.score <= candidates[0].score) then
        begin
            resolved_candidate_local.score := candidates[0].score + 1024;
            resolved_candidate_local.has_dict_weight := True;
            resolved_candidate_local.dict_weight := resolved_candidate_local.score;
        end;

        if existing_index_local > 0 then
        begin
            resolved_candidate_local := candidates[existing_index_local];
            if (Length(candidates) > 0) and
                (resolved_candidate_local.score <= candidates[0].score) then
            begin
                resolved_candidate_local.score := candidates[0].score + 1024;
                resolved_candidate_local.has_dict_weight := True;
                resolved_candidate_local.dict_weight := resolved_candidate_local.score;
            end;
            for idx_local := existing_index_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := resolved_candidate_local;
        end
        else
        begin
            SetLength(candidates, Length(candidates) + 1);
            for idx_local := High(candidates) downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := resolved_candidate_local;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vrs=[promote text=%s score=%d]',
                [Trim(candidates[0].text), resolved_score_local]);
        end;
        Result := True;
    end;

    function ensure_best_resolved_sentence_candidate_visible_aggressive(
        var candidates: TncCandidateList): Boolean;
    var
        resolved_candidate_local: TncCandidate;
        resolved_path_local: string;
        idx_local: Integer;
        existing_index_local: Integer;
        support_score_local: Integer;
        support_segments_local: Integer;
        support_path_local: string;
    begin
        Result := False;
        if (input_syllable_count < 8) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        if not build_resolved_sentence_candidate_local(
            resolved_candidate_local, resolved_path_local) then
        begin
            Exit;
        end;

        support_path_local := infer_segment_path_for_query_text_with_score(
            lookup_text, Trim(resolved_candidate_local.text),
            support_score_local, support_segments_local);
        if (Trim(resolved_path_local) = '') and
            (Trim(support_path_local) <> '') then
        begin
            resolved_path_local := Trim(support_path_local);
        end;
        if Trim(resolved_path_local) <> '' then
        begin
            remember_segment_path_for_candidate(Trim(resolved_candidate_local.text),
                '', Trim(resolved_path_local),
                resolved_candidate_local.score + support_score_local);
        end;

        existing_index_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text),
                Trim(resolved_candidate_local.text)) then
            begin
                existing_index_local := idx_local;
                Break;
            end;
        end;

        if existing_index_local = 0 then
        begin
            Exit(True);
        end;

        if existing_index_local > 0 then
        begin
            resolved_candidate_local := candidates[existing_index_local];
            for idx_local := existing_index_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := resolved_candidate_local;
        end
        else
        begin
            SetLength(candidates, Length(candidates) + 1);
            for idx_local := High(candidates) downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := resolved_candidate_local;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vra=[promote text=%s]', [Trim(candidates[0].text)]);
        end;
        Result := True;
    end;

    procedure ensure_best_late_exact_chunk_consensus_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_min_syllables = 8;
        c_min_window_syllables = 3;
        c_max_window_syllables = 6;
        c_probe_limit = 4;
        c_candidate_limit = 3;
        c_complete_rank_scale = 64;
        c_match_unit_weight = 640;
        c_mismatch_unit_weight = 896;
        c_fixed_match_bonus = 2048;
        c_fixed_mismatch_penalty = 4096;
        c_promote_margin = 512;
    var
        syllables_local: TncPinyinParseResult;
        cached_chunk_texts_local: TDictionary<string, string>;
        cached_chunk_scores_local: TDictionary<string, Integer>;
        cached_chunk_states_local: TDictionary<string, Byte>;
        candidate_units_local: array of TArray<string>;
        complete_indexes_local: TArray<Integer>;
        idx_local: Integer;
        candidate_idx_local: Integer;
        top_eval_score_local: Integer;
        best_eval_score_local: Integer;
        best_index_local: Integer;
        top_index_local: Integer;
        eval_score_local: Integer;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;
            for local_idx := start_idx to
                start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function window_has_fixed_boundary_local(const start_idx: Integer;
            const syllable_count_local: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;
            for local_idx := 0 to syllable_count_local - 1 do
            begin
                if get_fixed_resolved_boundary_char_local(
                    syllables_local[start_idx + local_idx].text) <> '' then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_respects_fixed_boundaries_local(
            const start_idx: Integer; const syllable_count_local: Integer;
            const text_value: string): Boolean;
        var
            units_local: TArray<string>;
            local_idx: Integer;
            fixed_char_local: string;
        begin
            if syllable_count_local > 1 then
            begin
                Exit(True);
            end;

            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <> syllable_count_local then
            begin
                Exit(False);
            end;
            for local_idx := 0 to syllable_count_local - 1 do
            begin
                fixed_char_local := get_fixed_resolved_boundary_char_local(
                    syllables_local[start_idx + local_idx].text);
                if (fixed_char_local <> '') and
                    (not SameText(units_local[local_idx], fixed_char_local)) then
                begin
                    Exit(False);
                end;
            end;
            Result := True;
        end;

        function get_effective_weight_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and
                (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
        end;

        function try_get_best_exact_chunk_local(const start_idx: Integer;
            const syllable_count_local: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            query_key_local: string;
            cached_state_local: Byte;
            exact_results_local: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
            best_text_local: string;
            best_score_local: Integer;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            query_key_local := build_query_key_local(start_idx,
                syllable_count_local);
            if (query_key_local = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            if cached_chunk_states_local.TryGetValue(query_key_local,
                cached_state_local) then
            begin
                if cached_state_local = 1 then
                begin
                    cached_chunk_texts_local.TryGetValue(query_key_local, out_text);
                    cached_chunk_scores_local.TryGetValue(query_key_local, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            best_text_local := '';
            best_score_local := Low(Integer);
            if (syllable_count_local >= 3) and
                try_resolve_best_exact_subspan_local(start_idx,
                syllable_count_local, resolved_text_local,
                resolved_score_local, resolved_segments_local,
                resolved_path_local, resolved_has_anchor_local,
                resolved_anchor_units_local) and
                (resolved_text_local <> '') and
                candidate_respects_fixed_boundaries_local(start_idx,
                syllable_count_local, resolved_text_local) then
            begin
                best_text_local := resolved_text_local;
                best_score_local := resolved_score_local;
                if resolved_segments_local <= 1 then
                begin
                    Inc(best_score_local, 2048);
                end;
                if resolved_has_anchor_local then
                begin
                    Inc(best_score_local, 1024 + (resolved_anchor_units_local * 96));
                end;
            end;

            if m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_results_local) then
            begin
                for local_idx := 0 to High(exact_results_local) do
                begin
                    if local_idx >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(exact_results_local[local_idx].text);
                    if (get_candidate_text_unit_count(candidate_text_local) <>
                        syllable_count_local) or
                        (not candidate_respects_fixed_boundaries_local(start_idx,
                        syllable_count_local, candidate_text_local)) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := get_effective_weight_local(
                        exact_results_local[local_idx]);
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(query_key_local,
                        candidate_text_local));
                    if syllable_count_local >= 4 then
                    begin
                        Inc(candidate_score_local, 1024);
                    end;
                    if syllable_count_local >= 5 then
                    begin
                        Inc(candidate_score_local, 1024);
                    end;

                    if candidate_score_local > best_score_local then
                    begin
                        best_score_local := candidate_score_local;
                        best_text_local := candidate_text_local;
                    end;
                end;
            end;

            if best_text_local <> '' then
            begin
                cached_chunk_states_local.AddOrSetValue(query_key_local, 1);
                cached_chunk_texts_local.AddOrSetValue(query_key_local,
                    best_text_local);
                cached_chunk_scores_local.AddOrSetValue(query_key_local,
                    best_score_local);
                out_text := best_text_local;
                out_score := best_score_local;
                Exit(True);
            end;

            cached_chunk_states_local.AddOrSetValue(query_key_local, 2);
        end;

        function extract_candidate_window_local(const units_local: TArray<string>;
            const start_idx: Integer; const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (Length(units_local) <> input_syllable_count) or
                (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local > Length(units_local)) then
            begin
                Exit;
            end;
            for local_idx := start_idx to
                start_idx + syllable_count_local - 1 do
            begin
                Result := Result + units_local[local_idx];
            end;
        end;

        function evaluate_complete_candidate_local(
            const candidate_index_local: Integer): Integer;
        var
            start_idx_local: Integer;
            syllable_count_part_local: Integer;
            chunk_text_local: string;
            chunk_score_local: Integer;
            candidate_chunk_local: string;
            fixed_boundary_local: Boolean;
        begin
            Result := get_rank_score(candidates[candidate_index_local]) div
                c_complete_rank_scale;
            for start_idx_local := 0 to input_syllable_count - 1 do
            begin
                for syllable_count_part_local := c_min_window_syllables to
                    Min(c_max_window_syllables,
                    input_syllable_count - start_idx_local) do
                begin
                    if not try_get_best_exact_chunk_local(start_idx_local,
                        syllable_count_part_local, chunk_text_local,
                        chunk_score_local) then
                    begin
                        Continue;
                    end;
                    candidate_chunk_local := extract_candidate_window_local(
                        candidate_units_local[candidate_index_local],
                        start_idx_local, syllable_count_part_local);
                    if candidate_chunk_local = '' then
                    begin
                        Continue;
                    end;
                    fixed_boundary_local := window_has_fixed_boundary_local(
                        start_idx_local, syllable_count_part_local);
                    if SameText(candidate_chunk_local, chunk_text_local) then
                    begin
                        Inc(Result, syllable_count_part_local *
                            c_match_unit_weight);
                        if fixed_boundary_local then
                        begin
                            Inc(Result, c_fixed_match_bonus);
                        end;
                    end
                    else
                    begin
                        Dec(Result, syllable_count_part_local *
                            c_mismatch_unit_weight);
                        if fixed_boundary_local then
                        begin
                            Dec(Result, c_fixed_mismatch_penalty);
                        end;
                    end;
                end;
            end;
        end;

        procedure promote_existing_candidate_local(const existing_index_local: Integer);
        var
            picked_local: TncCandidate;
            local_idx: Integer;
        begin
            if existing_index_local <= 0 then
            begin
                Exit;
            end;
            picked_local := candidates[existing_index_local];
            for local_idx := existing_index_local downto 1 do
            begin
                candidates[local_idx] := candidates[local_idx - 1];
            end;
            candidates[0] := picked_local;
        end;
    begin
        if (Length(candidates) <= 1) or (m_dictionary = nil) or
            (input_syllable_count < c_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        cached_chunk_texts_local := TDictionary<string, string>.Create;
        cached_chunk_scores_local := TDictionary<string, Integer>.Create;
        cached_chunk_states_local := TDictionary<string, Byte>.Create;
        try
            SetLength(candidate_units_local, Length(candidates));
            SetLength(complete_indexes_local, 0);
            for idx_local := 0 to High(candidates) do
            begin
                candidate_units_local[idx_local] := split_text_units(
                    Trim(candidates[idx_local].text));
                if (Trim(candidates[idx_local].comment) = '') and
                    (Length(candidate_units_local[idx_local]) =
                    input_syllable_count) then
                begin
                    SetLength(complete_indexes_local,
                        Length(complete_indexes_local) + 1);
                    complete_indexes_local[High(complete_indexes_local)] := idx_local;
                    if Length(complete_indexes_local) >= c_candidate_limit then
                    begin
                        Break;
                    end;
                end;
            end;

            if Length(complete_indexes_local) <= 1 then
            begin
                Exit;
            end;

            best_index_local := complete_indexes_local[0];
            top_index_local := complete_indexes_local[0];
            best_eval_score_local := evaluate_complete_candidate_local(
                best_index_local);
            top_eval_score_local := best_eval_score_local;
            for idx_local := 1 to High(complete_indexes_local) do
            begin
                candidate_idx_local := complete_indexes_local[idx_local];
                eval_score_local := evaluate_complete_candidate_local(
                    candidate_idx_local);
                if eval_score_local > best_eval_score_local then
                begin
                    best_eval_score_local := eval_score_local;
                    best_index_local := candidate_idx_local;
                end;
            end;

            if (best_index_local > 0) and
                (best_eval_score_local >= top_eval_score_local +
                c_promote_margin) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' vlc=[promote text=%s score=%d top=%d]',
                        [Trim(candidates[best_index_local].text),
                        best_eval_score_local, top_eval_score_local]);
                end;
                promote_existing_candidate_local(best_index_local);
            end
            else if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vlc=[keep text=%s score=%d top=%d]',
                    [Trim(candidates[top_index_local].text),
                    best_eval_score_local, top_eval_score_local]);
            end;
        finally
            cached_chunk_states_local.Free;
            cached_chunk_scores_local.Free;
            cached_chunk_texts_local.Free;
        end;
    end;

    function try_lookup_preferred_exact_phrase_candidate_local(
        const query_key: string; out out_candidate: TncCandidate;
        out out_syllable_count: Integer): Boolean;
    const
        c_min_preferred_phrase_syllables = 2;
        c_max_preferred_phrase_syllables = 5;
        c_min_preferred_phrase_weight = 80;
        c_regular_lookup_preferred_source_bonus = 0;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        exact_results_local: TncCandidateList;
        lookup_results_local: TncCandidateList;
        candidate_pool_local: TncCandidateList;
        candidate_pool_source_bonuses_local: TArray<Integer>;
        cache_key_local: string;
        phase_start_tick_local: UInt64;
        idx_local: Integer;
        candidate_text_local: string;
        candidate_units_local: Integer;
        candidate_weight_local: Integer;
        best_idx_local: Integer;
        best_weight_local: Integer;
        best_raw_weight_local: Integer;
        cached_state_local: Byte;
        has_valid_exact_candidate_local: Boolean;

        function get_candidate_effective_weight_local(const local_candidate: TncCandidate;
            const local_source_bonus: Integer): Integer;
        begin
            if is_fuzzy_pinyin_active and
                (local_candidate.fuzzy_cost > 0) then
            begin
                Result := local_candidate.score;
            end
            else if local_candidate.has_dict_weight then
            begin
                Result := local_candidate.dict_weight;
            end
            else
            begin
                Result := local_candidate.score;
            end;
            Inc(Result, local_source_bonus);
        end;

        procedure append_candidate_pool_local(const source_candidates: TncCandidateList;
            const source_bonus: Integer);
        var
            source_idx_local: Integer;
            existing_idx_local: Integer;
            source_text_local: string;
            duplicate_found_local: Boolean;
            source_weight_local: Integer;
            existing_weight_local: Integer;
        begin
            for source_idx_local := 0 to High(source_candidates) do
            begin
                source_text_local := Trim(source_candidates[source_idx_local].text);
                if source_text_local = '' then
                begin
                    Continue;
                end;

                duplicate_found_local := False;
                source_weight_local := get_candidate_effective_weight_local(
                    source_candidates[source_idx_local], source_bonus);
                for existing_idx_local := 0 to High(candidate_pool_local) do
                begin
                    if SameText(Trim(candidate_pool_local[existing_idx_local].text),
                        source_text_local) then
                    begin
                        duplicate_found_local := True;
                        existing_weight_local := get_candidate_effective_weight_local(
                            candidate_pool_local[existing_idx_local],
                            candidate_pool_source_bonuses_local[existing_idx_local]);
                        if source_weight_local > existing_weight_local then
                        begin
                            candidate_pool_local[existing_idx_local] :=
                                source_candidates[source_idx_local];
                            candidate_pool_source_bonuses_local[existing_idx_local] :=
                                source_bonus;
                        end;
                        Break;
                    end;
                end;
                if duplicate_found_local then
                begin
                    Continue;
                end;

                SetLength(candidate_pool_local,
                    Length(candidate_pool_local) + 1);
                SetLength(candidate_pool_source_bonuses_local,
                    Length(candidate_pool_source_bonuses_local) + 1);
                candidate_pool_local[High(candidate_pool_local)] :=
                    source_candidates[source_idx_local];
                candidate_pool_source_bonuses_local[
                    High(candidate_pool_source_bonuses_local)] := source_bonus;
            end;
        end;

        function build_sub_query_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[key_idx_local].text);
            end;
        end;

        function get_best_exact_weight_for_split_local(
            const split_query_key: string; const split_syllable_count: Integer;
            out out_weight: Integer): Boolean;
        var
            split_results_local: TncCandidateList;
            split_cache_key_local: string;
            split_phase_start_tick_local: UInt64;
            split_idx_local: Integer;
            split_text_local: string;
            split_weight_local: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (split_query_key = '') or (split_syllable_count <= 0) then
            begin
                Exit;
            end;

            split_cache_key_local := '#preferred_split#' + split_query_key;
            if (lookup_cache <> nil) and
                lookup_cache.TryGetValue(split_cache_key_local,
                split_results_local) then
            begin
                Inc(lookup_cache_hits);
            end
            else
            begin
                Inc(lookup_cache_misses);
                split_phase_start_tick_local := nc_monotonic_tick_ms;
                if not m_dictionary.lookup_exact_full_pinyin(split_query_key,
                    split_results_local) then
                begin
                    SetLength(split_results_local, 0);
                end;
                Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms -
                    split_phase_start_tick_local));
                if lookup_cache <> nil then
                begin
                    lookup_cache.AddOrSetValue(split_cache_key_local,
                        split_results_local);
                end;
            end;

            for split_idx_local := 0 to High(split_results_local) do
            begin
                if Trim(split_results_local[split_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                split_text_local := Trim(split_results_local[split_idx_local].text);
                if (split_text_local = '') or
                    (get_candidate_text_unit_count(split_text_local) <>
                    split_syllable_count) then
                begin
                    Continue;
                end;
                if split_results_local[split_idx_local].has_dict_weight then
                begin
                    split_weight_local :=
                        split_results_local[split_idx_local].dict_weight;
                end
                else
                begin
                    split_weight_local := split_results_local[split_idx_local].score;
                end;
                if split_weight_local > out_weight then
                begin
                    out_weight := split_weight_local;
                    Result := True;
                end;
            end;
        end;

        function has_much_stronger_split_local(
            const exact_weight_local: Integer): Boolean;
        const
            c_exact_phrase_protection_weight = 260;
            c_split_override_margin = 700;
            c_split_strong_chunk_weight = 360;
        var
            split_pos_local: Integer;
            left_key_local: string;
            right_key_local: string;
            left_weight_local: Integer;
            right_weight_local: Integer;
            split_weight_local: Integer;
        begin
            Result := False;
            if out_syllable_count < 3 then
            begin
                Exit;
            end;
            if exact_weight_local >= c_exact_phrase_protection_weight then
            begin
                Exit;
            end;

            for split_pos_local := 1 to out_syllable_count - 1 do
            begin
                left_key_local := build_sub_query_key_local(0,
                    split_pos_local);
                right_key_local := build_sub_query_key_local(split_pos_local,
                    out_syllable_count - split_pos_local);
                if (not get_best_exact_weight_for_split_local(left_key_local,
                    split_pos_local, left_weight_local)) or
                    (not get_best_exact_weight_for_split_local(right_key_local,
                    out_syllable_count - split_pos_local,
                    right_weight_local)) then
                begin
                    Continue;
                end;

                split_weight_local := left_weight_local + right_weight_local;
                if (Max(left_weight_local, right_weight_local) >=
                    c_split_strong_chunk_weight) and
                    (split_weight_local >= exact_weight_local +
                    c_split_override_margin) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        out_syllable_count := 0;

        normalized_query := normalize_pinyin_text(query_key);
        if (normalized_query = '') or (m_dictionary = nil) or
            (not is_full_pinyin_key(normalized_query)) then
        begin
            Exit;
        end;

        if (preferred_exact_state_cache <> nil) and
            preferred_exact_state_cache.TryGetValue(normalized_query,
            cached_state_local) then
        begin
            if (preferred_exact_syllable_count_cache <> nil) and
                (not preferred_exact_syllable_count_cache.TryGetValue(
                normalized_query, out_syllable_count)) then
            begin
                out_syllable_count := 0;
            end;
            if cached_state_local <> 0 then
            begin
                if (preferred_exact_candidate_cache <> nil) and
                    preferred_exact_candidate_cache.TryGetValue(
                    normalized_query, out_candidate) then
                begin
                    Exit(True);
                end;
            end;
            Exit(False);
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalized_query);
        out_syllable_count := Length(syllables_local);
        if (out_syllable_count < c_min_preferred_phrase_syllables) or
            (out_syllable_count > c_max_preferred_phrase_syllables) then
        begin
            Exit;
        end;

        cache_key_local := '#preferred_exact#' + normalized_query;
        if (lookup_cache <> nil) and
            lookup_cache.TryGetValue(cache_key_local, exact_results_local) then
        begin
            Inc(lookup_cache_hits);
        end
        else
        begin
            Inc(lookup_cache_misses);
            phase_start_tick_local := nc_monotonic_tick_ms;
            if not m_dictionary.lookup_exact_full_pinyin(normalized_query,
                exact_results_local) then
            begin
                SetLength(exact_results_local, 0);
            end;
            Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms -
                phase_start_tick_local));
            if lookup_cache <> nil then
            begin
                lookup_cache.AddOrSetValue(cache_key_local,
                    exact_results_local);
            end;
        end;

        cache_key_local := '#preferred_lookup#' + normalized_query;
        if (lookup_cache <> nil) and
            lookup_cache.TryGetValue(cache_key_local, lookup_results_local) then
        begin
            Inc(lookup_cache_hits);
        end
        else
        begin
            Inc(lookup_cache_misses);
            phase_start_tick_local := nc_monotonic_tick_ms;
            if not m_dictionary.lookup(normalized_query, lookup_results_local) then
            begin
                SetLength(lookup_results_local, 0);
            end;
            Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms -
                phase_start_tick_local));
            if lookup_cache <> nil then
            begin
                lookup_cache.AddOrSetValue(cache_key_local,
                    lookup_results_local);
            end;
        end;

        has_valid_exact_candidate_local := False;
        for idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(exact_results_local[idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                out_syllable_count) then
            begin
                Continue;
            end;
            if exact_results_local[idx_local].has_dict_weight then
            begin
                candidate_weight_local := exact_results_local[idx_local].dict_weight;
            end
            else
            begin
                candidate_weight_local := exact_results_local[idx_local].score;
            end;
            if candidate_weight_local >= c_min_preferred_phrase_weight then
            begin
                has_valid_exact_candidate_local := True;
                Break;
            end;
        end;

        SetLength(candidate_pool_local, 0);
        SetLength(candidate_pool_source_bonuses_local, 0);
        if has_valid_exact_candidate_local then
        begin
            append_candidate_pool_local(exact_results_local, 0);
        end
        else if out_syllable_count <= 2 then
        begin
            append_candidate_pool_local(lookup_results_local,
                c_regular_lookup_preferred_source_bonus);
        end
        else
        begin
            append_candidate_pool_local(lookup_results_local, 0);
        end;

        best_idx_local := -1;
        best_weight_local := Low(Integer);
        best_raw_weight_local := 0;
        for idx_local := 0 to High(candidate_pool_local) do
        begin
            if Trim(candidate_pool_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(candidate_pool_local[idx_local].text);
            if candidate_text_local = '' then
            begin
                Continue;
            end;
            candidate_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            if candidate_units_local <> out_syllable_count then
            begin
                Continue;
            end;

            if candidate_pool_local[idx_local].has_dict_weight then
            begin
                candidate_weight_local := candidate_pool_local[idx_local].dict_weight;
            end
            else
            begin
                candidate_weight_local := candidate_pool_local[idx_local].score;
            end;
            Inc(candidate_weight_local,
                candidate_pool_source_bonuses_local[idx_local]);
            if candidate_weight_local < c_min_preferred_phrase_weight then
            begin
                Continue;
            end;

            if candidate_weight_local > best_weight_local then
            begin
                best_weight_local := candidate_weight_local;
                best_raw_weight_local := candidate_weight_local -
                    candidate_pool_source_bonuses_local[idx_local];
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local >= 0 then
        begin
            if not has_much_stronger_split_local(best_raw_weight_local) then
            begin
                out_candidate := candidate_pool_local[best_idx_local];
                Result := True;
            end;
        end;

        if (preferred_exact_state_cache <> nil) then
        begin
            preferred_exact_state_cache.AddOrSetValue(normalized_query,
                Byte(Ord(Result)));
            if preferred_exact_syllable_count_cache <> nil then
            begin
                preferred_exact_syllable_count_cache.AddOrSetValue(
                    normalized_query, out_syllable_count);
            end;
            if Result and (preferred_exact_candidate_cache <> nil) then
            begin
                preferred_exact_candidate_cache.AddOrSetValue(normalized_query,
                    out_candidate);
            end;
        end;
    end;

    function has_learned_query_choice_override_local(const query_key: string;
        const candidate_text: string): Boolean;
    var
        normalized_query_local: string;
        text_key_local: string;
        latest_text_local: string;
        cache_key_local: string;
        cached_state_local: Byte;

        function get_exact_weight_for_text_local(
            const target_text: string): Integer;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            candidate_weight_local: Integer;
        begin
            Result := -1;
            if (m_dictionary = nil) or (Trim(target_text) = '') or
                (not m_dictionary.lookup_exact_full_pinyin(normalized_query_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    (not SameText(Trim(exact_results_local[exact_idx_local].text),
                    Trim(target_text))) then
                begin
                    Continue;
                end;

                if exact_results_local[exact_idx_local].has_dict_weight then
                begin
                    candidate_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end
                else
                begin
                    candidate_weight_local :=
                        exact_results_local[exact_idx_local].score;
                end;
                if candidate_weight_local > Result then
                begin
                    Result := candidate_weight_local;
                end;
            end;
        end;

        function learned_choice_should_override_local(
            const local_latest_text: string): Boolean;
        const
            c_strong_exact_protection_min = 260;
            c_strong_exact_protection_gap = 700;
            c_weak_latest_exact_max = 80;
        var
            candidate_weight_local: Integer;
            latest_weight_local: Integer;
        begin
            Result := (Trim(local_latest_text) <> '') and
                (not SameText(text_key_local, Trim(local_latest_text)));
            if not Result then
            begin
                Exit;
            end;

            candidate_weight_local := get_exact_weight_for_text_local(text_key_local);
            latest_weight_local := get_exact_weight_for_text_local(local_latest_text);
            if (candidate_weight_local >= c_strong_exact_protection_min) and
                (latest_weight_local >= 0) and
                (latest_weight_local <= c_weak_latest_exact_max) and
                (candidate_weight_local >= latest_weight_local +
                c_strong_exact_protection_gap) then
            begin
                Result := False;
            end;
        end;
    begin
        Result := False;
        normalized_query_local := normalize_pinyin_text(query_key);
        text_key_local := Trim(candidate_text);
        if (normalized_query_local = '') or (text_key_local = '') then
        begin
            Exit;
        end;

        cache_key_local := normalized_query_local + #1 + text_key_local;
        if (learned_query_choice_override_cache <> nil) and
            learned_query_choice_override_cache.TryGetValue(cache_key_local,
            cached_state_local) then
        begin
            Exit(cached_state_local <> 0);
        end;

        if (m_session_query_latest_text <> nil) and
            m_session_query_latest_text.TryGetValue(normalized_query_local,
            latest_text_local) and (Trim(latest_text_local) <> '') then
        begin
            Result := learned_choice_should_override_local(latest_text_local);
            if learned_query_choice_override_cache <> nil then
            begin
                learned_query_choice_override_cache.AddOrSetValue(
                    cache_key_local, Byte(Ord(Result)));
            end;
            Exit;
        end;

        if m_dictionary = nil then
        begin
            Exit;
        end;

        latest_text_local := '';
        if (m_lookup_query_latest_text_cache <> nil) and
            m_lookup_query_latest_text_cache.TryGetValue(normalized_query_local,
            latest_text_local) then
        begin
        end
        else
        begin
            latest_text_local := get_cached_query_latest_choice_text(
                normalized_query_local);
            if m_lookup_query_latest_text_cache <> nil then
            begin
                m_lookup_query_latest_text_cache.AddOrSetValue(
                    normalized_query_local, latest_text_local);
            end;
        end;

        Result := learned_choice_should_override_local(latest_text_local);
        if learned_query_choice_override_cache <> nil then
        begin
            learned_query_choice_override_cache.AddOrSetValue(cache_key_local,
                Byte(Ord(Result)));
        end;
    end;

    function has_stronger_query_choice_bonus_competitor_local(
        const query_key: string; const candidate_text: string): Boolean;
    var
        normalized_query_local: string;
        text_key_local: string;
        exact_results_local: TncCandidateList;
        cache_key_local: string;
        phase_start_tick_local: UInt64;
        idx_local: Integer;
        competitor_text_local: string;
        candidate_bonus_local: Integer;
        competitor_bonus_local: Integer;
        candidate_units_local: Integer;
        cached_state_local: Byte;
    begin
        Result := False;
        normalized_query_local := normalize_pinyin_text(query_key);
        text_key_local := Trim(candidate_text);
        if (normalized_query_local = '') or (text_key_local = '') or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        cache_key_local := normalized_query_local + #1 + text_key_local;
        if (stronger_query_choice_competitor_cache <> nil) and
            stronger_query_choice_competitor_cache.TryGetValue(cache_key_local,
            cached_state_local) then
        begin
            Exit(cached_state_local <> 0);
        end;

        candidate_bonus_local := m_dictionary.get_query_choice_bonus(
            normalized_query_local, text_key_local);
        candidate_units_local := get_candidate_text_unit_count(text_key_local);

        cache_key_local := '#choice_override#' + normalized_query_local;
        if (lookup_cache <> nil) and
            lookup_cache.TryGetValue(cache_key_local, exact_results_local) then
        begin
            Inc(lookup_cache_hits);
        end
        else
        begin
            Inc(lookup_cache_misses);
            phase_start_tick_local := nc_monotonic_tick_ms;
            if not m_dictionary.lookup_exact_full_pinyin(normalized_query_local,
                exact_results_local) then
            begin
                SetLength(exact_results_local, 0);
            end;
            Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms -
                phase_start_tick_local));
            if lookup_cache <> nil then
            begin
                lookup_cache.AddOrSetValue(cache_key_local,
                    exact_results_local);
            end;
        end;

        for idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            competitor_text_local := Trim(exact_results_local[idx_local].text);
            if (competitor_text_local = '') or
                SameText(competitor_text_local, text_key_local) or
                (get_candidate_text_unit_count(competitor_text_local) <>
                candidate_units_local) then
            begin
                Continue;
            end;

            competitor_bonus_local := m_dictionary.get_query_choice_bonus(
                normalized_query_local, competitor_text_local);
            if competitor_bonus_local > candidate_bonus_local then
            begin
                Result := True;
                Break;
            end;
        end;
        if stronger_query_choice_competitor_cache <> nil then
        begin
            stronger_query_choice_competitor_cache.AddOrSetValue(
                normalized_query_local + #1 + text_key_local, Byte(Ord(Result)));
        end;
    end;

    function should_allow_preferred_query_phrase_local(
        const normalized_query_local: string): Boolean;
    begin
        Result := False;
        if (normalized_query_local = '') or (m_dictionary = nil) or
            (input_syllable_count < 2) or (input_syllable_count > 4) or
            (not is_full_pinyin_key(normalized_query_local)) then
        begin
            Exit;
        end;

        Result := SameText(normalized_query_local,
            normalize_pinyin_text(lookup_text));
    end;

    function get_preferred_query_phrase_bonus_local(const query_key: string;
        const candidate_text: string): Integer;
    var
        normalized_query_local: string;
        text_key_local: string;
        cache_key_local: string;
        cached_bonus_local: Integer;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
    begin
        Result := 0;
        normalized_query_local := normalize_pinyin_text(query_key);
        text_key_local := Trim(candidate_text);
        if (normalized_query_local = '') or (text_key_local = '') or
            (not should_allow_preferred_query_phrase_local(
            normalized_query_local)) then
        begin
            Exit;
        end;

        cache_key_local := normalized_query_local + #1 + text_key_local;
        if (preferred_query_phrase_bonus_cache <> nil) and
            preferred_query_phrase_bonus_cache.TryGetValue(cache_key_local,
            cached_bonus_local) then
        begin
            Exit(cached_bonus_local);
        end;

        if try_get_preferred_query_phrase_text_local(normalized_query_local,
            preferred_text_local, preferred_bonus_local) and
            SameText(text_key_local, preferred_text_local) then
        begin
            Result := preferred_bonus_local;
        end;

        preferred_query_phrase_bonus_cache.AddOrSetValue(cache_key_local, Result);
    end;
    procedure apply_preferred_query_phrase_bonus_for_query(const query_key: string;
        var candidates: TncCandidateList);
    var
        idx: Integer;
        bonus: Integer;
    begin
        if normalize_pinyin_text(query_key) = '' then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            bonus := get_preferred_query_phrase_bonus_local(query_key,
                Trim(candidates[idx].text));
            if bonus <> 0 then
            begin
                Inc(candidates[idx].score, bonus);
            end;
        end;
    end;

    function try_get_preferred_query_phrase_text_local(const query_key: string;
        out out_text: string; out out_bonus: Integer): Boolean;
    const
        c_no_preferred_phrase_text = #0;
    var
        normalized_query_local: string;
        cached_text_local: string;
        latest_text_local: string;
        latest_from_session_local: Boolean;
    begin
        out_text := '';
        out_bonus := 0;
        latest_from_session_local := False;
        normalized_query_local := normalize_pinyin_text(query_key);
        if (normalized_query_local = '') or
            (not should_allow_preferred_query_phrase_local(
            normalized_query_local)) then
        begin
            Result := False;
            Exit;
        end;

        if (preferred_query_phrase_text_cache <> nil) and
            preferred_query_phrase_text_cache.TryGetValue(normalized_query_local,
            cached_text_local) then
        begin
            if SameText(cached_text_local, c_no_preferred_phrase_text) then
            begin
                Result := False;
                Exit;
            end;
            out_text := cached_text_local;
            out_bonus := 0;
            if preferred_query_phrase_text_bonus_cache <> nil then
            begin
                preferred_query_phrase_text_bonus_cache.TryGetValue(
                    normalized_query_local, out_bonus);
            end;
            Result := True;
            Exit;
        end;

        latest_text_local := '';
        if (m_session_query_latest_text <> nil) and
            m_session_query_latest_text.TryGetValue(normalized_query_local,
            latest_text_local) then
        begin
            latest_from_session_local := True;
        end
        else if (m_lookup_query_latest_text_cache <> nil) and
            m_lookup_query_latest_text_cache.TryGetValue(normalized_query_local,
            latest_text_local) then
        begin
        end
        else
        begin
            latest_text_local := get_cached_query_latest_choice_text(
                normalized_query_local);
            if m_lookup_query_latest_text_cache <> nil then
            begin
                m_lookup_query_latest_text_cache.AddOrSetValue(
                    normalized_query_local, latest_text_local);
            end;
        end;

        out_text := Trim(latest_text_local);
        out_bonus := 0;
        Result := (out_text <> '') and
            (get_candidate_text_unit_count(out_text) = input_syllable_count) and
            (get_candidate_text_unit_count(out_text) <= 4);
        if Result and (not latest_from_session_local) and (m_dictionary <> nil) and
            is_low_evidence_admin_place_alias_cached(
            normalized_query_local, out_text) then
        begin
            Result := False;
        end;
        if Result then
        begin
            out_bonus := m_dictionary.get_query_choice_bonus(
                normalized_query_local, out_text);
            Result := out_bonus > 0;
        end;
        if Result then
        begin
            Result := not has_stronger_query_choice_bonus_competitor_local(
                normalized_query_local, out_text);
        end;

        if not Result then
        begin
            preferred_query_phrase_text_cache.AddOrSetValue(
                normalized_query_local, c_no_preferred_phrase_text);
            preferred_query_phrase_text_bonus_cache.AddOrSetValue(
                normalized_query_local, 0);
            Exit;
        end;

        preferred_query_phrase_text_cache.AddOrSetValue(normalized_query_local,
            out_text);
        preferred_query_phrase_text_bonus_cache.AddOrSetValue(
            normalized_query_local, out_bonus);
    end;
    procedure ensure_preferred_subspan_repair_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_min_syllables = 5;
        c_max_span = 6;
        c_exact_min_span = 3;
        c_exact_min_weight = 320;
        c_repair_base_bonus = 50000;
    var
        syllables_local: TncPinyinParseResult;
        top_units_local: TArray<string>;
        repaired_units_local: TArray<string>;
        occupied_units_local: TArray<Boolean>;
        start_idx_local: Integer;
        span_len_local: Integer;
        unit_idx_local: Integer;
        query_key_local: string;
        current_text_local: string;
        repair_text_local: string;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
        preferred_weight_estimate_local: Integer;
        repair_score_local: Integer;
        total_repair_score_local: Integer;
        exact_results_local: TncCandidateList;
        exact_weight_local: Integer;
        repaired_text_local: string;
        repaired_candidate_local: TncCandidate;
        existing_idx_local: Integer;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(syllables_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function join_units_local(const units_local: TArray<string>;
            const local_start_idx: Integer; const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(units_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + units_local[local_idx];
            end;
        end;

        function get_repair_fixed_boundary_char_local(
            const local_syllable_text: string): string;
        var
            local_key: string;
        begin
            Result := '';
            local_key := normalize_pinyin_text(local_syllable_text);
            if local_key = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if local_key = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if local_key = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if local_key = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if local_key = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if local_key = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if local_key = 'shuo' then
            begin
                Result := string(Char($8BF4));
            end
            else if local_key = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if local_key = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if local_key = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if local_key = 'yu' then
            begin
                Result := string(Char($4E0E));
            end
            else if local_key = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if local_key = 'le' then
            begin
                Result := string(Char($4E86));
            end
            else if local_key = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if local_key = 'hui' then
            begin
                Result := string(Char($4F1A));
            end;
        end;

        function span_has_matching_fixed_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const current_text: string): Boolean;
        var
            current_units: TArray<string>;
            local_idx: Integer;
            expected_text: string;
        begin
            Result := False;
            current_units := split_text_units(current_text);
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (Length(current_units) <> local_span_len) or
                (local_start_idx + local_span_len > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := 0 to local_span_len - 1 do
            begin
                expected_text := get_repair_fixed_boundary_char_local(
                    syllables_local[local_start_idx + local_idx].text);
                if (expected_text <> '') and
                    SameText(current_units[local_idx], expected_text) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function any_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := True;
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(occupied_units_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if occupied_units_local[local_idx] then
                begin
                    Exit(True);
                end;
            end;
            Result := False;
        end;

        procedure mark_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer);
        var
            local_idx: Integer;
        begin
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                occupied_units_local[local_idx] := True;
            end;
        end;

        function get_modal_tail_text_for_key_local(
            const tail_key_local: string): string;
        begin
            Result := '';
            if tail_key_local = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if tail_key_local = 'ma' then
            begin
                Result := string(Char($5417));
            end
            else if tail_key_local = 'ne' then
            begin
                Result := string(Char($5462));
            end
            else if tail_key_local = 'a' then
            begin
                Result := string(Char($554A));
            end
            else if tail_key_local = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if tail_key_local = 'la' then
            begin
                Result := string(Char($5566));
            end
            else if tail_key_local = 'le' then
            begin
                Result := string(Char($4E86));
            end
            else if tail_key_local = 'wa' then
            begin
                Result := string(Char($54C7));
            end;
        end;

        function has_exact_phrase_text_for_key_local(const local_query_key: string;
            const local_text: string; const local_units: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_candidate_text: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (local_query_key = '') or
                (local_text = '') or (local_units <= 0) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(local_query_key,
                local_results) then
            begin
                if not m_dictionary.lookup(local_query_key, local_results) then
                begin
                    Exit;
                end;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= 8 then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_candidate_text := Trim(local_results[local_idx].text);
                if SameText(local_candidate_text, local_text) and
                    (get_candidate_text_unit_count(local_candidate_text) =
                    local_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_exact_phrase_text_only_for_key_local(
            const local_query_key: string; const local_text: string;
            const local_units: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_candidate_text: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (local_query_key = '') or
                (local_text = '') or (local_units <= 0) or
                (not m_dictionary.lookup_exact_full_pinyin(local_query_key,
                local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= 8 then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_candidate_text := Trim(local_results[local_idx].text);
                if SameText(local_candidate_text, local_text) and
                    (get_candidate_text_unit_count(local_candidate_text) =
                    local_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function current_span_has_path_bonus_protection_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const current_text: string): Boolean;
        var
            segment_path: string;
            segment_parts: TArray<string>;
            segment_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            unit_pos: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (current_text = '') or
                (local_span_len <= 0) then
            begin
                Exit;
            end;

            segment_path := get_segment_path_for_candidate(candidates[0], 0);
            if (segment_path = '') or
                (get_cached_query_segment_path_bonus(lookup_text,
                segment_path) <= 0) then
            begin
                Exit;
            end;

            segment_parts := segment_path.Split([c_segment_path_separator]);
            unit_pos := 0;
            for segment_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[segment_idx]);
                if segment_text = '' then
                begin
                    Continue;
                end;
                segment_units := get_candidate_text_unit_count(segment_text);
                if (unit_pos = local_start_idx) and
                    (segment_units = local_span_len) and
                    SameText(segment_text, current_text) then
                begin
                    Exit(True);
                end;
                Inc(unit_pos, segment_units);
            end;
        end;

        function should_keep_final_modal_tail_span_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const current_text: string): Boolean;
        var
            current_units: TArray<string>;
            prefix_text: string;
            prefix_key: string;
            tail_key: string;
            expected_tail_text: string;
            local_idx: Integer;
        begin
            Result := False;
            if (local_span_len < 2) or
                (local_start_idx + local_span_len <> input_syllable_count) then
            begin
                Exit;
            end;

            current_units := split_text_units(current_text);
            if Length(current_units) <> local_span_len then
            begin
                Exit;
            end;

            tail_key := normalize_pinyin_text(
                syllables_local[local_start_idx + local_span_len - 1].text);
            expected_tail_text := get_modal_tail_text_for_key_local(tail_key);
            if (expected_tail_text = '') or
                (not SameText(current_units[High(current_units)],
                expected_tail_text)) then
            begin
                Exit;
            end;

            prefix_key := build_query_key_local(local_start_idx,
                local_span_len - 1);
            prefix_text := '';
            for local_idx := 0 to High(current_units) - 1 do
            begin
                prefix_text := prefix_text + current_units[local_idx];
            end;

            Result := has_exact_phrase_text_for_key_local(prefix_key,
                prefix_text, local_span_len - 1) or
                (get_preferred_query_phrase_bonus_local(prefix_key,
                prefix_text) > 0);
        end;

        function try_get_exact_repair_text_local(const local_start_idx: Integer;
            const local_query_key: string; const local_span_len: Integer;
            const current_text: string; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if ((local_span_len < c_exact_min_span) and
                (local_span_len <> 2)) or
                (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(local_query_key,
                exact_results_local)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(exact_results_local) do
            begin
                if local_idx >= 4 then
                begin
                    Break;
                end;
                if Trim(exact_results_local[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_text := Trim(exact_results_local[local_idx].text);
                if (local_text = '') or SameText(local_text, current_text) or
                    (get_candidate_text_unit_count(local_text) <> local_span_len) then
                begin
                    if (local_idx = 0) and SameText(local_text, current_text) then
                    begin
                        Exit(False);
                    end;
                    Continue;
                end;

                exact_weight_local := exact_results_local[local_idx].score;
                if exact_results_local[local_idx].has_dict_weight and
                    (exact_results_local[local_idx].dict_weight >
                    exact_weight_local) then
                begin
                    exact_weight_local := exact_results_local[local_idx].dict_weight;
                end;
                if ((local_span_len = 2) and (exact_weight_local < 800)) or
                    ((local_span_len <> 2) and
                    (exact_weight_local < c_exact_min_weight)) then
                begin
                    Continue;
                end;
                if (local_span_len = 2) and (exact_weight_local < 800) and
                    span_has_matching_fixed_boundary_local(local_start_idx,
                    local_span_len, current_text) then
                begin
                    Continue;
                end;

                out_text := local_text;
                out_score := exact_weight_local +
                    ((4 - local_idx) * 256) + (local_span_len * 512);
                Exit(True);
            end;
        end;

        function try_get_split_preferred_repair_text_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const current_text: string; out out_text: string;
            out out_score: Integer): Boolean;
        var
            split_len_local: Integer;
            left_query_key: string;
            right_query_key: string;
            left_text: string;
            right_text: string;
            left_bonus: Integer;
            right_bonus: Integer;
            combined_text: string;
            combined_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (local_span_len < 4) or
                (local_start_idx < 0) or
                (local_start_idx + local_span_len > input_syllable_count) then
            begin
                Exit;
            end;

            for split_len_local := 2 to local_span_len - 2 do
            begin
                left_query_key := build_query_key_local(local_start_idx,
                    split_len_local);
                right_query_key := build_query_key_local(
                    local_start_idx + split_len_local,
                    local_span_len - split_len_local);
                if (not try_get_preferred_query_phrase_text_local(
                    left_query_key, left_text, left_bonus)) or
                    (not try_get_preferred_query_phrase_text_local(
                    right_query_key, right_text, right_bonus)) or
                    (get_candidate_text_unit_count(left_text) <>
                    split_len_local) or
                    (get_candidate_text_unit_count(right_text) <>
                    local_span_len - split_len_local) then
                begin
                    Continue;
                end;

                combined_text := left_text + right_text;
                if SameText(combined_text, current_text) or
                    (get_candidate_text_unit_count(combined_text) <>
                    local_span_len) then
                begin
                    Continue;
                end;

                combined_score := left_bonus + right_bonus +
                    (local_span_len * 768);
                if (not Result) or (combined_score > out_score) then
                begin
                    Result := True;
                    out_text := combined_text;
                    out_score := combined_score;
                end;
            end;
        end;

        function should_leave_weak_same_span_open_for_following_phrase_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const local_preferred_bonus: Integer): Boolean;
        var
            next_query_key: string;
            next_text: string;
            next_bonus: Integer;
            current_weight_estimate: Integer;
            next_weight_estimate: Integer;
        begin
            Result := False;
            if (local_span_len <> 2) or
                (local_start_idx + local_span_len >= input_syllable_count) then
            begin
                Exit;
            end;

            current_weight_estimate := local_preferred_bonus - 1800 -
                (local_span_len * 180);
            if current_weight_estimate >= 420 then
            begin
                Exit;
            end;

            next_query_key := build_query_key_local(local_start_idx + 1, 2);
            if (next_query_key = '') or
                (not try_get_preferred_query_phrase_text_local(next_query_key,
                next_text, next_bonus)) or
                (get_candidate_text_unit_count(next_text) <> 2) then
            begin
                Exit;
            end;

            next_weight_estimate := next_bonus - 1800 - (2 * 180);
            Result := next_weight_estimate >= 800;
        end;

        function try_apply_overlap_single_then_phrase_repair_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const local_preferred_bonus: Integer; out out_single_text: string;
            out out_single_score: Integer): Boolean;
        var
            single_results: TncCandidateList;
            single_idx: Integer;
            single_text: string;
            single_score: Integer;
            next_query_key: string;
            next_text: string;
            next_bonus: Integer;
            current_weight_estimate: Integer;
            next_weight_estimate: Integer;
        begin
            Result := False;
            out_single_text := '';
            out_single_score := 0;
            if (local_span_len <> 2) or
                (local_start_idx + local_span_len >= input_syllable_count) then
            begin
                Exit;
            end;

            current_weight_estimate := local_preferred_bonus - 1800 -
                (local_span_len * 180);
            if current_weight_estimate >= 420 then
            begin
                Exit;
            end;

            next_query_key := build_query_key_local(local_start_idx + 1, 2);
            if (next_query_key = '') or
                (not try_get_preferred_query_phrase_text_local(next_query_key,
                next_text, next_bonus)) or
                (get_candidate_text_unit_count(next_text) <> 2) then
            begin
                Exit;
            end;
            next_weight_estimate := next_bonus - 1800 - (2 * 180);
            if next_weight_estimate < 800 then
            begin
                Exit;
            end;

            if not m_dictionary.lookup(
                normalize_pinyin_text(syllables_local[local_start_idx].text),
                single_results) then
            begin
                Exit;
            end;

            for single_idx := 0 to High(single_results) do
            begin
                single_text := Trim(single_results[single_idx].text);
                if get_candidate_text_unit_count(single_text) <> 1 then
                begin
                    Continue;
                end;
                single_score := single_results[single_idx].score;
                if single_results[single_idx].has_dict_weight and
                    (single_results[single_idx].dict_weight > single_score) then
                begin
                    single_score := single_results[single_idx].dict_weight;
                end;
                if SameText(normalize_pinyin_text(
                    syllables_local[local_start_idx].text), 'yu') and
                    (single_text = string(Char($4E0E))) then
                begin
                    Inc(single_score, 160);
                end;
                if (out_single_text = '') or (single_score > out_single_score) then
                begin
                    out_single_text := single_text;
                    out_single_score := single_score;
                end;
            end;

            Result := out_single_text <> '';
        end;

        function build_repaired_text_local: string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(repaired_units_local) do
            begin
                Result := Result + repaired_units_local[local_idx];
            end;
        end;

        procedure promote_repaired_candidate_local;
        var
            local_idx: Integer;
        begin
            repaired_text_local := build_repaired_text_local;
            if (repaired_text_local = '') or
                SameText(repaired_text_local, Trim(candidates[0].text)) or
                (get_candidate_text_unit_count(repaired_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            existing_idx_local := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text),
                    repaired_text_local) then
                begin
                    existing_idx_local := local_idx;
                    Break;
                end;
            end;

            if existing_idx_local = 0 then
            begin
                Exit;
            end;

            if existing_idx_local > 0 then
            begin
                repaired_candidate_local := candidates[existing_idx_local];
            end
            else
            begin
                repaired_candidate_local := candidates[0];
                repaired_candidate_local.text := repaired_text_local;
                repaired_candidate_local.comment := '';
                repaired_candidate_local.source := cs_rule;
            end;
            repaired_candidate_local.score := Max(
                repaired_candidate_local.score + c_repair_base_bonus +
                total_repair_score_local,
                candidates[0].score + c_repair_base_bonus +
                total_repair_score_local);

            if existing_idx_local > 0 then
            begin
                for local_idx := existing_idx_local downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
                candidates[0] := repaired_candidate_local;
            end
            else
            begin
                SetLength(candidates, Length(candidates) + 1);
                for local_idx := High(candidates) downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
                candidates[0] := repaired_candidate_local;
            end;

            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' prefrepair=[text=%s score=%d]',
                    [repaired_text_local, total_repair_score_local]);
            end;
        end;
    begin
        // The span rank repair path has repeatedly promoted locally valid but
        // globally worse chains (for example a single + two-char technical word
        // split beating two ordinary two-char words). Keep the repair disabled
        // until it can prove end-to-end quality on corpus tests.
        Exit;
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count < c_min_syllables) or
            (Trim(candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
            input_syllable_count) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(candidates[0].text));
        if Length(top_units_local) <> input_syllable_count then
        begin
            Exit;
        end;

        repaired_units_local := Copy(top_units_local);
        SetLength(occupied_units_local, input_syllable_count);
        total_repair_score_local := 0;

        for span_len_local := c_max_span downto 2 do
        begin
            for start_idx_local := 0 to input_syllable_count - span_len_local do
            begin
                if any_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_len_local);
                current_text_local := join_units_local(repaired_units_local,
                    start_idx_local, span_len_local);
                repair_text_local := '';
                repair_score_local := 0;
                preferred_text_local := '';
                preferred_bonus_local := 0;

                if should_keep_final_modal_tail_span_local(start_idx_local,
                    span_len_local, current_text_local) then
                begin
                    mark_occupied_local(start_idx_local, span_len_local);
                    Continue;
                end;

                if try_get_preferred_query_phrase_text_local(query_key_local,
                    preferred_text_local, preferred_bonus_local) and
                    (get_candidate_text_unit_count(preferred_text_local) =
                    span_len_local) then
                begin
                    preferred_weight_estimate_local := preferred_bonus_local -
                        1800 - (span_len_local * 180);
                    if (not SameText(preferred_text_local,
                        current_text_local)) and
                        (has_exact_phrase_text_only_for_key_local(
                        query_key_local, current_text_local,
                        span_len_local) or
                        current_span_has_path_bonus_protection_local(
                        start_idx_local, span_len_local,
                        current_text_local)) then
                    begin
                        mark_occupied_local(start_idx_local, span_len_local);
                        Continue;
                    end;
                    if (span_len_local = 2) and
                        (preferred_weight_estimate_local < 800) and
                        (not SameText(preferred_text_local,
                        current_text_local)) and
                        span_has_matching_fixed_boundary_local(start_idx_local,
                        span_len_local, current_text_local) then
                    begin
                        Continue;
                    end;
                    if (not SameText(preferred_text_local, current_text_local)) and
                        try_apply_overlap_single_then_phrase_repair_local(
                        start_idx_local, span_len_local, preferred_bonus_local,
                        repair_text_local, repair_score_local) then
                    begin
                        repaired_units_local[start_idx_local] := repair_text_local;
                        mark_occupied_local(start_idx_local, 1);
                        Inc(total_repair_score_local, repair_score_local +
                            (span_len_local * 512));
                        Continue;
                    end;
                    if SameText(preferred_text_local, current_text_local) then
                    begin
                        if should_leave_weak_same_span_open_for_following_phrase_local(
                            start_idx_local, span_len_local,
                            preferred_bonus_local) then
                        begin
                            Continue;
                        end;
                        mark_occupied_local(start_idx_local, span_len_local);
                        Continue;
                    end
                    else
                    begin
                        repair_text_local := preferred_text_local;
                        repair_score_local := preferred_bonus_local +
                            (span_len_local * 1024);
                    end;
                end
                else if not try_get_exact_repair_text_local(start_idx_local,
                    query_key_local, span_len_local, current_text_local,
                    repair_text_local, repair_score_local) and
                    not try_get_split_preferred_repair_text_local(
                    start_idx_local, span_len_local, current_text_local,
                    repair_text_local, repair_score_local) then
                begin
                    Continue;
                end;

                if (repair_text_local = '') or
                    (get_candidate_text_unit_count(repair_text_local) <>
                    span_len_local) then
                begin
                    Continue;
                end;

                for unit_idx_local := 0 to span_len_local - 1 do
                begin
                    repaired_units_local[start_idx_local + unit_idx_local] :=
                        split_text_units(repair_text_local)[unit_idx_local];
                end;
                mark_occupied_local(start_idx_local, span_len_local);
                Inc(total_repair_score_local, repair_score_local);
            end;
        end;

        if total_repair_score_local > 0 then
        begin
            promote_repaired_candidate_local;
        end;
    end;

    procedure ensure_exact_span_rank_repair_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_min_syllables = 5;
        c_max_span = 5;
        c_probe_limit = 10;
        c_min_replace_weight = 520;
        c_min_chain_phrase_weight = 450;
        c_exact_replace_margin = 180;
        c_strong_split_override_weight = 20000;
        c_repair_base_bonus = 52000;
        c_weak_fixed_head_exact_weight = 720;
    var
        syllables_local: TncPinyinParseResult;
        top_units_local: TArray<string>;
        repaired_units_local: TArray<string>;
        occupied_units_local: TArray<Boolean>;
        span_len_local: Integer;
        start_idx_local: Integer;
        total_repair_score_local: Integer;
        repaired_text_local: string;
        repaired_candidate_local: TncCandidate;
        existing_idx_local: Integer;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(syllables_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function join_units_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(repaired_units_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + repaired_units_local[local_idx];
            end;
        end;

        function any_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := True;
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(occupied_units_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if occupied_units_local[local_idx] then
                begin
                    Exit(True);
                end;
            end;
            Result := False;
        end;

        procedure mark_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer);
        var
            local_idx: Integer;
        begin
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                occupied_units_local[local_idx] := True;
            end;
        end;

        function get_effective_candidate_weight_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if Result < 0 then
            begin
                Result := 0;
            end;
        end;

        function try_pick_best_exact_span_text_local(const query_key_local: string;
            const span_len_value: Integer; const current_text_value: string;
            const local_start_idx_value: Integer;
            out out_text: string; out out_weight: Integer;
            out out_current_weight: Integer): Boolean;
        var
            exact_results_local: TncCandidateList;
            ranked_lookup_results_local: TncCandidateList;
            local_idx: Integer;
            lookup_idx_local: Integer;
            candidate_text_local: string;
            ranked_text_local: string;
            candidate_weight_local: Integer;
            candidate_rank_weight_local: Integer;
            ranked_weight_local: Integer;
            query_choice_bonus_local: Integer;

            function should_skip_weak_fixed_head_candidate_local(
                const candidate_text_value: string;
                const candidate_weight_value: Integer;
                const candidate_source_value: TncCandidateSource): Boolean;
            var
                fixed_head_text_local: string;
                current_units_local: TArray<string>;
                candidate_units_local: TArray<string>;
            begin
                Result := False;
                if (span_len_value <> 2) or
                    (candidate_source_value = cs_user) or
                    (candidate_weight_value >= c_weak_fixed_head_exact_weight) then
                begin
                    Exit;
                end;

                fixed_head_text_local := get_fixed_resolved_boundary_char_local(
                    syllables_local[local_start_idx_value].text);
                if fixed_head_text_local = '' then
                begin
                    Exit;
                end;

                current_units_local := split_text_units(
                    Trim(current_text_value));
                candidate_units_local := split_text_units(
                    Trim(candidate_text_value));
                if (Length(current_units_local) <> span_len_value) or
                    (Length(candidate_units_local) <> span_len_value) or
                    (not SameText(current_units_local[0],
                    fixed_head_text_local)) then
                begin
                    Exit;
                end;

                Result := not SameText(candidate_units_local[0],
                    fixed_head_text_local);
            end;

            function get_primary_visible_lookup_text_local: string;
            var
                ranked_idx_local: Integer;
                ranked_units_local: Integer;
            begin
                Result := '';
                if Length(ranked_lookup_results_local) = 0 then
                begin
                    Exit;
                end;

                for ranked_idx_local := 0 to Min(High(ranked_lookup_results_local),
                    c_probe_limit - 1) do
                begin
                    if Trim(ranked_lookup_results_local[ranked_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    ranked_text_local := Trim(
                        ranked_lookup_results_local[ranked_idx_local].text);
                    ranked_units_local :=
                        get_candidate_text_unit_count(ranked_text_local);
                    if (ranked_text_local <> '') and
                        (ranked_units_local = span_len_value) then
                    begin
                        Exit(ranked_text_local);
                    end;
                end;
            end;

            function get_ranked_lookup_score_local(const text_value: string): Integer;
            var
                ranked_idx_local: Integer;
            begin
                Result := Low(Integer);
                if (text_value = '') or (Length(ranked_lookup_results_local) = 0) then
                begin
                    Exit;
                end;

                for ranked_idx_local := 0 to Min(High(ranked_lookup_results_local),
                    c_probe_limit - 1) do
                begin
                    if Trim(ranked_lookup_results_local[ranked_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    ranked_text_local := Trim(
                        ranked_lookup_results_local[ranked_idx_local].text);
                    if (ranked_text_local = '') or
                        (get_candidate_text_unit_count(ranked_text_local) <>
                        span_len_value) or
                        (not SameText(ranked_text_local, text_value)) then
                    begin
                        Continue;
                    end;

                    ranked_weight_local := get_effective_candidate_weight_local(
                        ranked_lookup_results_local[ranked_idx_local]);
                    // The visible exact lookup order already contains the
                    // lexicon/user ranking policy for this span. Respect it
                    // during repair so raw weights do not flip a better
                    // visible phrase, e.g. a common homophone selected by
                    // normal lookup.
                    Result := ranked_weight_local +
                        ((c_probe_limit - ranked_idx_local) * 640);
                    Exit;
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_current_weight := Low(Integer);
            ranked_text_local := '';

            if (m_dictionary = nil) or (query_key_local = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup(query_key_local,
                ranked_lookup_results_local) then
            begin
                ranked_lookup_results_local := nil;
            end;
            ranked_text_local := get_primary_visible_lookup_text_local;

            for local_idx := 0 to High(exact_results_local) do
            begin
                if local_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(exact_results_local[local_idx].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    span_len_value) then
                begin
                    Continue;
                end;

                candidate_weight_local :=
                    get_effective_candidate_weight_local(
                    exact_results_local[local_idx]);
                if should_skip_weak_fixed_head_candidate_local(
                    candidate_text_local, candidate_weight_local,
                    exact_results_local[local_idx].source) then
                begin
                    Continue;
                end;
                query_choice_bonus_local :=
                    m_dictionary.get_query_choice_bonus(query_key_local,
                    candidate_text_local);
                if (ranked_text_local <> '') and
                    SameText(current_text_value, ranked_text_local) and
                    (not SameText(candidate_text_local, ranked_text_local)) and
                    (query_choice_bonus_local <= 0) and
                    (not ((exact_results_local[local_idx].source = cs_user) and
                    m_dictionary.is_user_entry(query_key_local,
                    candidate_text_local))) then
                begin
                    Continue;
                end;
                if query_choice_bonus_local > 0 then
                begin
                    Inc(candidate_weight_local, query_choice_bonus_local * 8);
                end;
                if (span_len_value >= 2) and
                    (not m_dictionary.is_base_entry(query_key_local,
                    candidate_text_local)) and
                    (not ((exact_results_local[local_idx].source = cs_user) and
                    m_dictionary.is_user_entry(query_key_local,
                    candidate_text_local))) then
                begin
                    candidate_weight_local := Min(candidate_weight_local,
                        240 + span_len_value * 80);
                end;
                lookup_idx_local := get_ranked_lookup_score_local(
                    candidate_text_local);
                if lookup_idx_local > candidate_weight_local then
                begin
                    candidate_weight_local := lookup_idx_local;
                end;
                if (span_len_value >= 2) and
                    (not m_dictionary.is_base_entry(query_key_local,
                    candidate_text_local)) and
                    (not ((exact_results_local[local_idx].source = cs_user) and
                    m_dictionary.is_user_entry(query_key_local,
                    candidate_text_local))) then
                begin
                    candidate_weight_local := Min(candidate_weight_local,
                        240 + span_len_value * 80);
                end;

                if SameText(candidate_text_local, current_text_value) and
                    (candidate_weight_local > out_current_weight) then
                begin
                    out_current_weight := candidate_weight_local;
                end;

                candidate_rank_weight_local := candidate_weight_local +
                    ((c_probe_limit - local_idx) * 32);
                if (out_text = '') or
                    (candidate_rank_weight_local > out_weight) then
                begin
                    out_text := candidate_text_local;
                    out_weight := candidate_rank_weight_local;
                    Result := True;
                end;
            end;
        end;

        function should_replace_span_local(const best_text_value: string;
            const best_weight_value: Integer; const current_text_value: string;
            const current_weight_value: Integer): Boolean;
        var
            best_units_local: Integer;
        begin
            Result := False;
            if (best_text_value = '') or
                SameText(best_text_value, current_text_value) or
                (get_candidate_text_unit_count(best_text_value) <= 1) then
            begin
                Exit;
            end;

            best_units_local := get_candidate_text_unit_count(best_text_value);
            if current_weight_value <= Low(Integer) div 4 then
            begin
                if best_units_local >= 4 then
                begin
                    Exit(best_weight_value >= 220);
                end;
                Exit(best_weight_value >= c_min_replace_weight);
            end;

            Result := best_weight_value >=
                current_weight_value + c_exact_replace_margin;
        end;

        function is_modifier_single_text_for_repair_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168, $8D85,
                $771F, $597D, $5982, $633A, $8F83, $6781, $800C, $5E76,
                $6240, $4E0A, $4E0B, $4E00, $4E14, $5730, $5148:
                    Result := True;
            end;
        end;

        function is_modifier_tail_syllable_for_repair_local(
            const local_idx: Integer): Boolean;
        var
            syllable_text_local: string;
        begin
            Result := False;
            if (local_idx < 0) or (local_idx >= Length(syllables_local)) then
            begin
                Exit;
            end;

            syllable_text_local := normalize_pinyin_text(
                syllables_local[local_idx].text);
            Result := (syllable_text_local = 'shi') or
                (syllable_text_local = 'de') or
                (syllable_text_local = 'le') or
                (syllable_text_local = 'zai') or
                (syllable_text_local = 'he') or
                (syllable_text_local = 'yu') or
                (syllable_text_local = 'ye') or
                (syllable_text_local = 'you') or
                (syllable_text_local = 'jiu') or
                (syllable_text_local = 'dou') or
                (syllable_text_local = 'hai') or
                (syllable_text_local = 'cai') or
                (syllable_text_local = 'ba') or
                (syllable_text_local = 'bei') or
                (syllable_text_local = 'rang') or
                (syllable_text_local = 'gei') or
                (syllable_text_local = 'dui') or
                (syllable_text_local = 'cong') or
                (syllable_text_local = 'xiang') or
                (syllable_text_local = 'yao') or
                (syllable_text_local = 'hui') or
                (syllable_text_local = 'neng') or
                (syllable_text_local = 'ke') or
                (syllable_text_local = 'jiang') or
                (syllable_text_local = 'yi') or
                (syllable_text_local = 'bu') or
                (syllable_text_local = 'zui') or
                (syllable_text_local = 'geng') or
                (syllable_text_local = 'hen') or
                (syllable_text_local = 'tai');
        end;

        function has_following_exact_phrase_for_repair_local(
            const local_start_idx: Integer): Boolean;
        var
            follow_len_local: Integer;
            follow_query_key_local: string;
            follow_results_local: TncCandidateList;
            follow_idx_local: Integer;
            follow_text_local: string;
            follow_weight_local: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or
                (local_start_idx >= input_syllable_count) then
            begin
                Exit;
            end;

            for follow_len_local := 2 to
                Min(c_max_span, input_syllable_count - local_start_idx) do
            begin
                follow_query_key_local := build_query_key_local(local_start_idx,
                    follow_len_local);
                if (follow_query_key_local = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(
                    follow_query_key_local, follow_results_local)) then
                begin
                    Continue;
                end;

                for follow_idx_local := 0 to High(follow_results_local) do
                begin
                    if follow_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(follow_results_local[follow_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    follow_text_local := Trim(
                        follow_results_local[follow_idx_local].text);
                    if get_candidate_text_unit_count(follow_text_local) <>
                        follow_len_local then
                    begin
                        Continue;
                    end;
                    follow_weight_local :=
                        get_effective_candidate_weight_local(
                        follow_results_local[follow_idx_local]);
                    if follow_weight_local >= c_min_replace_weight then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function should_preserve_modifier_plus_phrase_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            local_units: TArray<string>;
        begin
            Result := False;
            if (local_span_len < 2) or
                (local_start_idx + 1 >= input_syllable_count) then
            begin
                Exit;
            end;
            local_units := split_text_units(join_units_local(local_start_idx,
                local_span_len));
            if (Length(local_units) <= 0) or
                (not is_modifier_single_text_for_repair_local(local_units[0])) then
            begin
                Exit;
            end;

            Result := has_following_exact_phrase_for_repair_local(
                local_start_idx + 1);
        end;

        function has_preceding_exact_phrase_for_repair_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            phrase_len_local: Integer;
            phrase_query_key_local: string;
            phrase_results_local: TncCandidateList;
            phrase_idx_local: Integer;
            phrase_text_local: string;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or (local_span_len < 2) then
            begin
                Exit;
            end;

            for phrase_len_local := 2 to Min(c_max_span, local_span_len - 1) do
            begin
                phrase_query_key_local := build_query_key_local(local_start_idx,
                    phrase_len_local);
                phrase_text_local := join_units_local(local_start_idx,
                    phrase_len_local);
                if (phrase_query_key_local = '') or (phrase_text_local = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(
                    phrase_query_key_local, phrase_results_local)) then
                begin
                    Continue;
                end;

                for phrase_idx_local := 0 to High(phrase_results_local) do
                begin
                    if phrase_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(phrase_results_local[phrase_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(
                        phrase_results_local[phrase_idx_local].text);
                    if not SameText(candidate_text_local, phrase_text_local) then
                    begin
                        Continue;
                    end;
                    candidate_weight_local := get_effective_candidate_weight_local(
                        phrase_results_local[phrase_idx_local]);
                    if candidate_weight_local >= c_min_replace_weight then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function should_preserve_phrase_plus_modifier_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            local_units: TArray<string>;
        begin
            Result := False;
            if local_span_len < 2 then
            begin
                Exit;
            end;
            local_units := split_text_units(join_units_local(local_start_idx,
                local_span_len));
            if (Length(local_units) <> local_span_len) or
                (not is_modifier_single_text_for_repair_local(
                local_units[High(local_units)])) then
            begin
                Exit;
            end;

            // A multi-character prefix followed by a function/modifier character
            // is a plausible word-boundary split even when the prefix is a name
            // or a low-frequency phrase not present as a base lexicon entry.
            // The caller still allows replacements that extend that prefix
            // (e.g. "宸ョ▼鏄? -> "宸ョ▼甯?).
            if (local_span_len >= 3) and
                (get_candidate_text_unit_count(join_units_local(local_start_idx,
                local_span_len - 1)) >= 2) then
            begin
                Exit(True);
            end;

            Result := has_preceding_exact_phrase_for_repair_local(
                local_start_idx, local_span_len);
        end;

        function replacement_extends_current_prefix_local(
            const best_text_value: string; const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            prefix_units_local: TArray<string>;
            best_units_local: TArray<string>;
            unit_idx_local: Integer;
        begin
            Result := False;
            if (best_text_value = '') or (local_span_len < 3) then
            begin
                Exit;
            end;

            prefix_units_local := split_text_units(join_units_local(
                local_start_idx, local_span_len - 1));
            best_units_local := split_text_units(best_text_value);
            if (Length(prefix_units_local) < 2) or
                (Length(best_units_local) <> local_span_len) or
                (Length(prefix_units_local) >= Length(best_units_local)) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(prefix_units_local) do
            begin
                if prefix_units_local[unit_idx_local] <>
                    best_units_local[unit_idx_local] then
                begin
                    Exit(False);
                end;
            end;
            Result := True;
        end;

        function try_get_exact_text_weight_for_repair_local(
            const query_key_value: string; const text_value: string;
            out out_weight: Integer): Boolean;
        const
            c_split_probe_limit = 48;
        var
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            out_weight := 0;
            if (query_key_value = '') or (text_value = '') or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(query_key_value, text_value) then
            begin
                out_weight := 1000;
                Exit(True);
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key_value,
                exact_results_local) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if result_idx_local >= c_split_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[result_idx_local].text);
                if SameText(candidate_text_local, text_value) then
                begin
                    out_weight := get_effective_candidate_weight_local(
                        exact_results_local[result_idx_local]);
                    Exit(True);
                end;
            end;
        end;

        function current_span_has_strong_exact_split_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            split_len_local: Integer;
            left_key_local: string;
            right_key_local: string;
            left_text_local: string;
            right_text_local: string;
            left_weight_local: Integer;
            right_weight_local: Integer;
        begin
            Result := False;
            if local_span_len < 4 then
            begin
                Exit;
            end;

            for split_len_local := 2 to local_span_len - 2 do
            begin
                left_key_local := build_query_key_local(local_start_idx,
                    split_len_local);
                right_key_local := build_query_key_local(local_start_idx +
                    split_len_local, local_span_len - split_len_local);
                left_text_local := join_units_local(local_start_idx,
                    split_len_local);
                right_text_local := join_units_local(local_start_idx +
                    split_len_local, local_span_len - split_len_local);

                if try_get_exact_text_weight_for_repair_local(left_key_local,
                    left_text_local, left_weight_local) and
                    try_get_exact_text_weight_for_repair_local(right_key_local,
                    right_text_local, right_weight_local) and
                    (left_weight_local >= c_min_replace_weight) and
                    (right_weight_local >= c_min_replace_weight) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function range_has_strong_exact_phrase_for_repair_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            range_query_key_local: string;
            range_results_local: TncCandidateList;
            range_idx_local: Integer;
            range_text_local: string;
            range_weight_local: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or (local_span_len < 2) or
                (local_start_idx + local_span_len > input_syllable_count) then
            begin
                Exit;
            end;

            range_query_key_local := build_query_key_local(local_start_idx,
                local_span_len);
            if (range_query_key_local = '') or
                (not m_dictionary.lookup_exact_full_pinyin(range_query_key_local,
                range_results_local)) then
            begin
                Exit;
            end;

            for range_idx_local := 0 to High(range_results_local) do
            begin
                if range_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(range_results_local[range_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                range_text_local := Trim(range_results_local[range_idx_local].text);
                if get_candidate_text_unit_count(range_text_local) <>
                    local_span_len then
                begin
                    Continue;
                end;
                range_weight_local := get_effective_candidate_weight_local(
                    range_results_local[range_idx_local]);
                if range_weight_local >= c_min_chain_phrase_weight then
                begin
                    Exit(True);
                end;
            end;
        end;

        function range_has_strong_exact_phrase_chain_for_repair_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            left_len_local: Integer;
            right_len_local: Integer;
        begin
            Result := False;
            if local_span_len < 2 then
            begin
                Exit;
            end;

            if range_has_strong_exact_phrase_for_repair_local(local_start_idx,
                local_span_len) then
            begin
                Exit(True);
            end;

            for left_len_local := 2 to local_span_len - 2 do
            begin
                right_len_local := local_span_len - left_len_local;
                if range_has_strong_exact_phrase_for_repair_local(local_start_idx,
                    left_len_local) and
                    range_has_strong_exact_phrase_chain_for_repair_local(
                    local_start_idx + left_len_local, right_len_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_following_strong_phrase_chain_for_repair_local(
            const local_start_idx: Integer): Boolean;
        var
            follow_len_local: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or
                (local_start_idx >= input_syllable_count) then
            begin
                Exit;
            end;

            for follow_len_local := Min(c_max_span,
                input_syllable_count - local_start_idx) downto 2 do
            begin
                if range_has_strong_exact_phrase_chain_for_repair_local(
                    local_start_idx, follow_len_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function current_unit_is_inside_exact_phrase_for_repair_local(
            const local_unit_idx: Integer): Boolean;
        var
            phrase_start_local: Integer;
            phrase_len_local: Integer;
            phrase_key_local: string;
            phrase_text_local: string;
            phrase_weight_local: Integer;
        begin
            Result := False;
            if (local_unit_idx < 0) or
                (local_unit_idx >= input_syllable_count) then
            begin
                Exit;
            end;

            for phrase_len_local := 2 to c_max_span do
            begin
                for phrase_start_local :=
                    Max(0, local_unit_idx - phrase_len_local + 1) to
                    Min(local_unit_idx, input_syllable_count - phrase_len_local) do
                begin
                    phrase_key_local := build_query_key_local(phrase_start_local,
                        phrase_len_local);
                    phrase_text_local := join_units_local(phrase_start_local,
                        phrase_len_local);
                    if (phrase_key_local = '') or (phrase_text_local = '') then
                    begin
                        Continue;
                    end;

                    if try_get_exact_text_weight_for_repair_local(
                        phrase_key_local, phrase_text_local,
                        phrase_weight_local) and
                        (phrase_weight_local >= c_min_chain_phrase_weight) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function replacement_preserves_current_prefix_local(
            const replacement_text_value: string; const local_start_idx: Integer;
            const local_prefix_len: Integer): Boolean;
        var
            prefix_units_local: TArray<string>;
            replacement_units_local: TArray<string>;
            unit_idx_local: Integer;
        begin
            Result := False;
            if (replacement_text_value = '') or (local_prefix_len < 2) then
            begin
                Exit;
            end;

            prefix_units_local := split_text_units(join_units_local(
                local_start_idx, local_prefix_len));
            replacement_units_local := split_text_units(replacement_text_value);
            if (Length(prefix_units_local) <> local_prefix_len) or
                (Length(replacement_units_local) <= local_prefix_len) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(prefix_units_local) do
            begin
                if prefix_units_local[unit_idx_local] <>
                    replacement_units_local[unit_idx_local] then
                begin
                    Exit(False);
                end;
            end;
            Result := True;
        end;

        function should_preserve_strong_prefix_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const replacement_text_value: string): Boolean;
        var
            prefix_len_local: Integer;
            prefix_query_key_local: string;
            prefix_text_local: string;
            prefix_weight_local: Integer;
            replacement_query_key_local: string;
        begin
            Result := False;
            if (local_span_len < 3) or (replacement_text_value = '') or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            replacement_query_key_local := build_query_key_local(local_start_idx,
                local_span_len);
            if (replacement_query_key_local <> '') and
                m_dictionary.is_base_entry(replacement_query_key_local,
                replacement_text_value) then
            begin
                Exit;
            end;

            for prefix_len_local := 2 to Min(c_max_span, local_span_len - 1) do
            begin
                prefix_query_key_local := build_query_key_local(local_start_idx,
                    prefix_len_local);
                prefix_text_local := join_units_local(local_start_idx,
                    prefix_len_local);
                if (prefix_query_key_local = '') or (prefix_text_local = '') then
                begin
                    Continue;
                end;

                if try_get_exact_text_weight_for_repair_local(
                    prefix_query_key_local, prefix_text_local,
                    prefix_weight_local) and
                    (prefix_weight_local >= c_min_replace_weight) and
                    ((has_following_exact_phrase_for_repair_local(
                    local_start_idx + prefix_len_local)) or
                    (has_following_strong_phrase_chain_for_repair_local(
                    local_start_idx + prefix_len_local))) and
                    (not replacement_preserves_current_prefix_local(
                    replacement_text_value, local_start_idx,
                    prefix_len_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function replacement_preserves_current_suffix_local(
            const replacement_text_value: string; const local_start_idx: Integer;
            const local_suffix_start_offset: Integer; const local_suffix_len: Integer): Boolean;
        var
            replacement_units_local: TArray<string>;
            suffix_units_local: TArray<string>;
            unit_idx_local: Integer;
        begin
            Result := False;
            if (replacement_text_value = '') or (local_suffix_start_offset <= 0) or
                (local_suffix_len < 2) then
            begin
                Exit;
            end;

            replacement_units_local := split_text_units(replacement_text_value);
            suffix_units_local := split_text_units(join_units_local(
                local_start_idx + local_suffix_start_offset, local_suffix_len));
            if (Length(replacement_units_local) <> local_suffix_start_offset +
                local_suffix_len) or (Length(suffix_units_local) <> local_suffix_len) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(suffix_units_local) do
            begin
                if suffix_units_local[unit_idx_local] <>
                    replacement_units_local[local_suffix_start_offset + unit_idx_local] then
                begin
                    Exit(False);
                end;
            end;
            Result := True;
        end;

        function should_preserve_single_plus_phrase_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const replacement_text_value: string): Boolean;
        var
            suffix_len_local: Integer;
            suffix_query_key_local: string;
            suffix_text_local: string;
            suffix_weight_local: Integer;
        begin
            Result := False;
            if (local_span_len < 3) or (replacement_text_value = '') or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            suffix_len_local := local_span_len - 1;
            suffix_query_key_local := build_query_key_local(local_start_idx + 1,
                suffix_len_local);
            suffix_text_local := join_units_local(local_start_idx + 1,
                suffix_len_local);
            if (suffix_query_key_local = '') or (suffix_text_local = '') then
            begin
                Exit;
            end;

            if try_get_exact_text_weight_for_repair_local(suffix_query_key_local,
                suffix_text_local, suffix_weight_local) and
                (suffix_weight_local >= c_min_replace_weight) and
                (not replacement_preserves_current_suffix_local(
                replacement_text_value, local_start_idx, 1, suffix_len_local)) then
            begin
                Exit(True);
            end;
        end;

        function should_preserve_top_path_exact_segment_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const replacement_text_value: string): Boolean;
        var
            path_parts_local: TArray<string>;
            part_units_local: TArray<string>;
            replacement_units_local: TArray<string>;
            part_idx_local: Integer;
            segment_start_local: Integer;
            segment_len_local: Integer;
            segment_end_local: Integer;
            span_end_local: Integer;
            segment_query_key_local: string;
            segment_text_local: string;
            segment_weight_local: Integer;
            unit_idx_local: Integer;
            replacement_offset_local: Integer;
            top_path_local: string;
            replacement_dict_weight_known_local: Boolean;
            replacement_dict_weight_available_local: Boolean;
            replacement_dict_weight_local: Integer;

            function try_get_exact_dict_weight_local(const query_key_value: string;
                const text_value: string; out out_weight: Integer): Boolean;
            const
                c_probe_limit = 48;
            var
                exact_results_local: TncCandidateList;
                result_idx_local: Integer;
                candidate_text_local: string;
            begin
                Result := False;
                out_weight := 0;
                if (query_key_value = '') or (text_value = '') or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                if not m_dictionary.lookup_exact_full_pinyin(query_key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for result_idx_local := 0 to High(exact_results_local) do
                begin
                    if result_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(
                        exact_results_local[result_idx_local].text);
                    if SameText(candidate_text_local, text_value) then
                    begin
                        out_weight := exact_results_local[result_idx_local].dict_weight;
                        Exit(True);
                    end;
                end;
            end;

            procedure ensure_replacement_dict_weight_local;
            var
                replacement_query_key_local: string;
            begin
                if not replacement_dict_weight_known_local then
                begin
                    replacement_dict_weight_known_local := True;
                    replacement_dict_weight_available_local := False;
                    replacement_dict_weight_local := 0;
                    replacement_query_key_local := build_query_key_local(
                        local_start_idx, local_span_len);
                    if replacement_query_key_local <> '' then
                    begin
                        replacement_dict_weight_available_local :=
                            try_get_exact_dict_weight_local(
                            replacement_query_key_local, replacement_text_value,
                            replacement_dict_weight_local);
                    end;
                end;
            end;

            function replacement_exact_span_can_override_segment_local(
                const local_segment_weight: Integer): Boolean;
            const
                c_override_margin = 120;
            begin
                ensure_replacement_dict_weight_local;

                Result := replacement_dict_weight_available_local and
                    (replacement_dict_weight_local >= c_min_replace_weight) and
                    (replacement_dict_weight_local >= local_segment_weight +
                    c_override_margin);
            end;

            function replacement_can_resegment_tail_with_following_phrase_local(
                const local_segment_start: Integer; const local_segment_end: Integer;
                const local_segment_len: Integer;
                const local_segment_weight: Integer): Boolean;
            const
                c_min_following_phrase_weight = 780;

                function has_high_weight_following_phrase_local: Boolean;
                var
                    follow_start_local: Integer;
                    follow_len_local: Integer;
                    follow_key_local: string;
                    follow_text_local: string;
                    follow_weight_local: Integer;
                begin
                    Result := False;
                    follow_start_local := local_start_idx + local_span_len;
                    if follow_start_local >= input_syllable_count then
                    begin
                        Exit;
                    end;

                    for follow_len_local := 2 to Min(c_max_span,
                        input_syllable_count - follow_start_local) do
                    begin
                        follow_key_local := build_query_key_local(
                            follow_start_local, follow_len_local);
                        follow_text_local := join_units_local(follow_start_local,
                            follow_len_local);
                        if (follow_key_local = '') or
                            (follow_text_local = '') then
                        begin
                            Continue;
                        end;

                        if try_get_exact_dict_weight_local(
                            follow_key_local, follow_text_local,
                            follow_weight_local) and
                            (follow_weight_local >=
                            c_min_following_phrase_weight) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            begin
                Result := False;
                if (local_segment_len <> 2) or
                    (local_start_idx <> local_segment_end) or
                    (local_segment_start >= local_start_idx) then
                begin
                    Exit;
                end;

                ensure_replacement_dict_weight_local;

                Result := replacement_dict_weight_available_local and
                    (replacement_dict_weight_local >= c_min_replace_weight) and
                    (replacement_dict_weight_local >= local_segment_weight) and
                    has_high_weight_following_phrase_local;
            end;
        begin
            Result := False;
            if (local_span_len < 2) or (replacement_text_value = '') or
                (Length(candidates) = 0) then
            begin
                Exit;
            end;

            top_path_local := get_segment_path_for_candidate(candidates[0], 0);
            if top_path_local = '' then
            begin
                Exit;
            end;

            replacement_units_local := split_text_units(replacement_text_value);
            if Length(replacement_units_local) <> local_span_len then
            begin
                Exit;
            end;
            replacement_dict_weight_known_local := False;
            replacement_dict_weight_available_local := False;
            replacement_dict_weight_local := 0;

            span_end_local := local_start_idx + local_span_len - 1;
            segment_start_local := 0;
            path_parts_local := top_path_local.Split([c_segment_path_separator]);
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                part_units_local := split_text_units(segment_text_local);
                segment_len_local := Length(part_units_local);
                if segment_len_local <= 0 then
                begin
                    Continue;
                end;

                segment_end_local := segment_start_local + segment_len_local - 1;
                if (segment_end_local >= local_start_idx) and
                    (segment_start_local <= span_end_local) and
                    (segment_len_local >= 2) then
                begin
                    segment_query_key_local := build_query_key_local(
                        segment_start_local, segment_len_local);
                    if (segment_query_key_local <> '') and
                        try_get_exact_dict_weight_local(
                        segment_query_key_local, segment_text_local,
                        segment_weight_local) and
                        (segment_weight_local >= c_min_chain_phrase_weight) then
                    begin
                        if (segment_len_local >= 3) and
                            (local_start_idx > segment_start_local) and
                            (local_start_idx <= segment_end_local) then
                        begin
                            if (local_start_idx <> segment_end_local) or
                                (not replacement_exact_span_can_override_segment_local(
                                segment_weight_local)) then
                            begin
                                Exit(True);
                            end;
                        end;

                        if (segment_start_local < local_start_idx) or
                            (segment_end_local > span_end_local) then
                        begin
                            if replacement_exact_span_can_override_segment_local(
                                segment_weight_local) or
                                replacement_can_resegment_tail_with_following_phrase_local(
                                segment_start_local, segment_end_local,
                                segment_len_local, segment_weight_local) then
                            begin
                                Continue;
                            end;
                            Exit(True);
                        end;

                        replacement_offset_local := segment_start_local - local_start_idx;
                        for unit_idx_local := 0 to High(part_units_local) do
                        begin
                            if replacement_units_local[
                                replacement_offset_local + unit_idx_local] <>
                                part_units_local[unit_idx_local] then
                            begin
                                if replacement_exact_span_can_override_segment_local(
                                    segment_weight_local) then
                                begin
                                    Continue;
                                end;
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                Inc(segment_start_local, segment_len_local);
            end;
        end;

        function has_preceding_exact_phrase_ending_at_repair_local(
            const local_end_idx: Integer): Boolean;
        var
            phrase_len_local: Integer;
            phrase_start_local: Integer;
            phrase_query_key_local: string;
            phrase_text_local: string;
            phrase_results_local: TncCandidateList;
            phrase_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            if (local_end_idx <= 0) or (local_end_idx >= input_syllable_count) then
            begin
                Exit;
            end;

            for phrase_len_local := 2 to Min(c_max_span, local_end_idx + 1) do
            begin
                phrase_start_local := local_end_idx - phrase_len_local + 1;
                phrase_query_key_local := build_query_key_local(phrase_start_local,
                    phrase_len_local);
                phrase_text_local := join_units_local(phrase_start_local,
                    phrase_len_local);
                if (phrase_query_key_local = '') or (phrase_text_local = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(
                    phrase_query_key_local, phrase_results_local)) then
                begin
                    Continue;
                end;

                for phrase_idx_local := 0 to High(phrase_results_local) do
                begin
                    if phrase_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(phrase_results_local[phrase_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(
                        phrase_results_local[phrase_idx_local].text);
                    if not SameText(candidate_text_local, phrase_text_local) then
                    begin
                        Continue;
                    end;
                    candidate_weight_local := get_effective_candidate_weight_local(
                        phrase_results_local[phrase_idx_local]);
                    if candidate_weight_local >= c_min_replace_weight then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function try_pick_split_single_span_text_local(
            const local_start_idx: Integer; const current_text_value: string;
            const current_weight_value: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        var
            unit_idx_local: Integer;
            single_query_key_local: string;
            single_results_local: TncCandidateList;
            single_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_single_text_local: string;
            best_single_weight_local: Integer;
            combined_text_local: string;
            combined_weight_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (local_start_idx < 0) or
                (local_start_idx + 2 > input_syllable_count) or
                (get_candidate_text_unit_count(current_text_value) <> 2) or
                (current_weight_value >= c_min_replace_weight) or
                ((not has_following_exact_phrase_for_repair_local(
                local_start_idx + 2)) and
                (not has_following_strong_phrase_chain_for_repair_local(
                local_start_idx + 2)) and
                (not has_preceding_exact_phrase_ending_at_repair_local(
                local_start_idx - 1))) then
            begin
                Exit;
            end;

            combined_text_local := '';
            combined_weight_local := 0;
            for unit_idx_local := 0 to 1 do
            begin
                single_query_key_local := build_query_key_local(
                    local_start_idx + unit_idx_local, 1);
                if (single_query_key_local = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(
                    single_query_key_local, single_results_local)) then
                begin
                    Exit;
                end;

                best_single_text_local := '';
                best_single_weight_local := 0;
                for single_idx_local := 0 to High(single_results_local) do
                begin
                    if single_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(single_results_local[single_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(
                        single_results_local[single_idx_local].text);
                    if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                        (not m_dictionary.single_char_matches_pinyin(
                        single_query_key_local, candidate_text_local)) then
                    begin
                        Continue;
                    end;
                    candidate_weight_local := get_effective_candidate_weight_local(
                        single_results_local[single_idx_local]);
                    if candidate_weight_local < 780 then
                    begin
                        Continue;
                    end;
                    if (best_single_text_local = '') or
                        (candidate_weight_local > best_single_weight_local) then
                    begin
                        best_single_text_local := candidate_text_local;
                        best_single_weight_local := candidate_weight_local;
                    end;
                end;

                if best_single_text_local = '' then
                begin
                    Exit;
                end;
                if not is_modifier_single_text_for_repair_local(
                    best_single_text_local) then
                begin
                    Exit;
                end;
                combined_text_local := combined_text_local + best_single_text_local;
                Inc(combined_weight_local, best_single_weight_local);
            end;

            if SameText(combined_text_local, current_text_value) or
                (combined_weight_local < current_weight_value + 280) then
            begin
                Exit;
            end;

            out_text := combined_text_local;
            out_weight := combined_weight_local + 512;
            Result := True;
        end;

        function try_pick_modifier_single_repair_local(
            const local_idx: Integer; const current_text_value: string;
            out out_text: string; out out_weight: Integer): Boolean;
        var
            single_query_key_local: string;
            single_results_local: TncCandidateList;
            single_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            ranked_weight_local: Integer;
            current_is_modifier_local: Boolean;
            has_preceding_local: Boolean;
            has_following_exact_local: Boolean;
            has_following_chain_local: Boolean;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (local_idx < 0) or (local_idx >= input_syllable_count) or
                (current_text_value = '') then
            begin
                Exit;
            end;

            current_is_modifier_local :=
                is_modifier_single_text_for_repair_local(current_text_value);
            has_preceding_local :=
                has_preceding_exact_phrase_ending_at_repair_local(local_idx);
            has_following_exact_local :=
                has_following_exact_phrase_for_repair_local(local_idx + 1);
            has_following_chain_local := False;
            if not has_following_exact_local then
            begin
                has_following_chain_local :=
                    has_following_strong_phrase_chain_for_repair_local(
                    local_idx + 1);
            end;

            if ((not has_following_exact_local) and
                (not has_following_chain_local)) or
                (current_is_modifier_local and (not has_preceding_local)) then
            begin
                Exit;
            end;

            single_query_key_local := build_query_key_local(local_idx, 1);
            if (single_query_key_local = '') or
                (not m_dictionary.lookup_exact_full_pinyin(single_query_key_local,
                single_results_local)) then
            begin
                Exit;
            end;

            for single_idx_local := 0 to High(single_results_local) do
            begin
                if single_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(single_results_local[single_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(single_results_local[single_idx_local].text);
                if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                    (not is_modifier_single_text_for_repair_local(candidate_text_local)) or
                    (not m_dictionary.single_char_matches_pinyin(
                    single_query_key_local, candidate_text_local)) then
                begin
                    Continue;
                end;
                candidate_weight_local := get_effective_candidate_weight_local(
                    single_results_local[single_idx_local]);
                ranked_weight_local := candidate_weight_local +
                    ((c_probe_limit - single_idx_local) * 32);
                if has_preceding_local and
                    (has_following_exact_local or has_following_chain_local) and
                    ((candidate_text_local = string(Char($4E0E))) or
                    (candidate_text_local = string(Char($548C)))) then
                begin
                    Inc(ranked_weight_local, 260);
                end;
                if (out_text = '') or (ranked_weight_local > out_weight) then
                begin
                    out_text := candidate_text_local;
                    out_weight := ranked_weight_local;
                    Result := True;
                end;
            end;
        end;

        procedure apply_span_repair_local(const local_start_idx: Integer;
            const local_span_len: Integer; const replacement_text: string);
        var
            replacement_units: TArray<string>;
            local_idx: Integer;
        begin
            replacement_units := split_text_units(replacement_text);
            if Length(replacement_units) <> local_span_len then
            begin
                Exit;
            end;
            for local_idx := 0 to local_span_len - 1 do
            begin
                repaired_units_local[local_start_idx + local_idx] :=
                    replacement_units[local_idx];
            end;
            mark_occupied_local(local_start_idx, local_span_len);
        end;

        function build_repaired_text_local: string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(repaired_units_local) do
            begin
                Result := Result + repaired_units_local[local_idx];
            end;
        end;

        function build_repaired_segment_path_local: string;
        var
            local_idx: Integer;
            span_len_local: Integer;
            chosen_len_local: Integer;
            segment_text_local: string;
            segment_query_key_local: string;
            segment_weight_local: Integer;
        begin
            Result := '';
            local_idx := 0;
            while local_idx <= High(repaired_units_local) do
            begin
                chosen_len_local := 1;
                for span_len_local := Min(c_max_span,
                    Length(repaired_units_local) - local_idx) downto 2 do
                begin
                    segment_text_local := join_units_local(local_idx,
                        span_len_local);
                    segment_query_key_local := build_query_key_local(local_idx,
                        span_len_local);
                    if try_get_exact_text_weight_for_repair_local(
                        segment_query_key_local, segment_text_local,
                        segment_weight_local) and
                        (segment_weight_local >= c_min_chain_phrase_weight) then
                    begin
                        chosen_len_local := span_len_local;
                        Break;
                    end;
                end;

                segment_text_local := join_units_local(local_idx,
                    chosen_len_local);
                if Result <> '' then
                begin
                    Result := Result + c_segment_path_separator;
                end;
                Result := Result + segment_text_local;
                Inc(local_idx, chosen_len_local);
            end;
        end;

        procedure promote_repaired_candidate_local;
        var
            local_idx: Integer;
            candidate_count_before_local: Integer;
            path_count_matches_local: Boolean;
            repaired_path_local: string;
        begin
            repaired_text_local := build_repaired_text_local;
            if (repaired_text_local = '') or
                SameText(repaired_text_local, Trim(candidates[0].text)) or
                (get_candidate_text_unit_count(repaired_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            existing_idx_local := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text),
                    repaired_text_local) then
                begin
                    existing_idx_local := local_idx;
                    Break;
                end;
            end;

            if existing_idx_local = 0 then
            begin
                Exit;
            end;

            if existing_idx_local > 0 then
            begin
                repaired_candidate_local := candidates[existing_idx_local];
            end
            else
            begin
                repaired_candidate_local := candidates[0];
                repaired_candidate_local.text := repaired_text_local;
                repaired_candidate_local.comment := '';
                repaired_candidate_local.source := cs_rule;
            end;
            repaired_candidate_local.score := Max(
                repaired_candidate_local.score + c_repair_base_bonus +
                total_repair_score_local,
                candidates[0].score + c_repair_base_bonus +
                total_repair_score_local);
            repaired_candidate_local.has_dict_weight := True;
            repaired_candidate_local.dict_weight := repaired_candidate_local.score;
            repaired_path_local := build_repaired_segment_path_local;
            if repaired_path_local <> '' then
            begin
                remember_segment_path_for_candidate(repaired_text_local, '',
                    repaired_path_local, repaired_candidate_local.score);
            end;

            if existing_idx_local > 0 then
            begin
                path_count_matches_local := Length(m_candidate_segment_paths) =
                    Length(candidates);
                for local_idx := existing_idx_local downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                    if path_count_matches_local then
                    begin
                        m_candidate_segment_paths[local_idx] :=
                            m_candidate_segment_paths[local_idx - 1];
                    end;
                end;
                candidates[0] := repaired_candidate_local;
                if path_count_matches_local then
                begin
                    m_candidate_segment_paths[0] := repaired_path_local;
                end;
            end
            else
            begin
                candidate_count_before_local := Length(candidates);
                path_count_matches_local := Length(m_candidate_segment_paths) =
                    candidate_count_before_local;
                SetLength(candidates, Length(candidates) + 1);
                if path_count_matches_local then
                begin
                    SetLength(m_candidate_segment_paths, Length(candidates));
                end;
                for local_idx := High(candidates) downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                    if path_count_matches_local then
                    begin
                        m_candidate_segment_paths[local_idx] :=
                            m_candidate_segment_paths[local_idx - 1];
                    end;
                end;
                candidates[0] := repaired_candidate_local;
                if path_count_matches_local then
                begin
                    m_candidate_segment_paths[0] := repaired_path_local;
                end;
            end;

            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' exactrepair=[text=%s score=%d]',
                    [repaired_text_local, total_repair_score_local]);
            end;
        end;

    var
        query_key_local: string;
        current_text_local: string;
        best_text_local: string;
        best_weight_local: Integer;
        current_weight_local: Integer;
        split_text_local: string;
        split_weight_local: Integer;
        modifier_text_local: string;
        modifier_weight_local: Integer;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count < c_min_syllables) or
            (Trim(candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
            input_syllable_count) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(candidates[0].text));
        if Length(top_units_local) <> input_syllable_count then
        begin
            Exit;
        end;

        repaired_units_local := Copy(top_units_local);
        SetLength(occupied_units_local, input_syllable_count);
        total_repair_score_local := 0;

        for span_len_local := Min(c_max_span, input_syllable_count) downto 2 do
        begin
            for start_idx_local := 0 to input_syllable_count - span_len_local do
            begin
                if any_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_len_local);
                current_text_local := join_units_local(start_idx_local,
                    span_len_local);
                if (query_key_local = '') or (current_text_local = '') or
                    (not try_pick_best_exact_span_text_local(query_key_local,
                    span_len_local, current_text_local, start_idx_local,
                    best_text_local, best_weight_local,
                    current_weight_local)) then
                begin
                    Continue;
                end;

                if try_pick_split_single_span_text_local(start_idx_local,
                    current_text_local, current_weight_local,
                    split_text_local, split_weight_local) then
                begin
                    best_text_local := split_text_local;
                    best_weight_local := split_weight_local;
                end;

                if SameText(best_text_local, current_text_local) then
                begin
                    Continue;
                end;

                if (span_len_local >= 3) and
                    is_modifier_tail_syllable_for_repair_local(
                    start_idx_local + span_len_local - 1) and
                    (not replacement_extends_current_prefix_local(best_text_local,
                    start_idx_local, span_len_local)) then
                begin
                    Continue;
                end;

                if should_preserve_strong_prefix_boundary_local(
                    start_idx_local, span_len_local, best_text_local) then
                begin
                    Continue;
                end;

                if should_preserve_single_plus_phrase_boundary_local(
                    start_idx_local, span_len_local, best_text_local) then
                begin
                    Continue;
                end;

                if should_preserve_top_path_exact_segment_local(
                    start_idx_local, span_len_local, best_text_local) then
                begin
                    Continue;
                end;

                if should_preserve_phrase_plus_modifier_boundary_local(
                    start_idx_local, span_len_local) and
                    (not replacement_extends_current_prefix_local(best_text_local,
                    start_idx_local, span_len_local)) then
                begin
                    Continue;
                end;

                if should_preserve_modifier_plus_phrase_boundary_local(
                    start_idx_local, span_len_local) or
                    (current_span_has_strong_exact_split_local(start_idx_local,
                    span_len_local) and
                    (best_weight_local < c_strong_split_override_weight)) or
                    (not should_replace_span_local(best_text_local,
                    best_weight_local, current_text_local,
                    current_weight_local)) then
                begin
                    Continue;
                end;

                apply_span_repair_local(start_idx_local, span_len_local,
                    best_text_local);
                Inc(total_repair_score_local,
                    best_weight_local + (span_len_local * 768));
            end;
        end;

        for start_idx_local := 0 to input_syllable_count - 1 do
        begin
            if any_occupied_local(start_idx_local, 1) then
            begin
                Continue;
            end;
            if current_unit_is_inside_exact_phrase_for_repair_local(
                start_idx_local) then
            begin
                Continue;
            end;
            current_text_local := join_units_local(start_idx_local, 1);
            if try_pick_modifier_single_repair_local(start_idx_local,
                current_text_local, modifier_text_local, modifier_weight_local) and
                (not SameText(modifier_text_local, current_text_local)) then
            begin
                apply_span_repair_local(start_idx_local, 1,
                    modifier_text_local);
                Inc(total_repair_score_local, modifier_weight_local + 512);
            end;
        end;

        if total_repair_score_local > 0 then
        begin
            promote_repaired_candidate_local;
        end;
    end;

    procedure ensure_high_quality_complete_candidate_in_top2(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 32;
        c_max_path_segment_units = 5;
        c_promote_to_second_margin = 900;
        c_promote_to_first_margin = 2400;
    var
        syllables_local: TncPinyinParseResult;
        expected_units_local: Integer;
        best_idx_local: Integer;
        best_quality_local: Integer;
        idx_local: Integer;
        candidate_quality_local: Integer;
        top_quality_local: Integer;
        second_quality_local: Integer;
        moved_candidate_local: TncCandidate;
        best_two_text_cache_local: TArray<string>;
        best_two_weight_cache_local: TArray<Integer>;
        best_two_known_cache_local: TArray<Boolean>;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len >
                Length(syllables_local)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function is_complete_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) =
                expected_units_local);
        end;

        function get_exact_segment_quality_local(
            const query_key_value: string; const text_value: string;
            const segment_units_value: Integer): Integer;
        var
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            weight_local: Integer;
        begin
            Result := -900 - (segment_units_value * 220);
            if (query_key_value = '') or (text_value = '') or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key_value,
                exact_results_local) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if result_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[result_idx_local].text);
                if not SameText(candidate_text_local, text_value) then
                begin
                    Continue;
                end;

                weight_local := exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    weight_local) then
                begin
                    weight_local := exact_results_local[result_idx_local].dict_weight;
                end;

                Result := 900 + Min(weight_local, 2400) +
                    ((c_probe_limit - result_idx_local) * 140) +
                    (segment_units_value * 180);
                if segment_units_value >= 3 then
                begin
                    Inc(Result, 520);
                end;
                if segment_units_value >= 4 then
                begin
                    Inc(Result, 2400);
                end;
                Exit;
            end;
        end;

        function try_get_best_exact_two_unit_text_local(
            const local_start_idx: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (local_start_idx < 0) or
                (local_start_idx + 2 > Length(syllables_local)) or
                (local_start_idx >= Length(best_two_known_cache_local)) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if best_two_known_cache_local[local_start_idx] then
            begin
                out_text := best_two_text_cache_local[local_start_idx];
                out_weight := best_two_weight_cache_local[local_start_idx];
                Exit(out_text <> '');
            end;

            best_two_known_cache_local[local_start_idx] := True;
            query_key_local := build_query_key_local(local_start_idx, 2);
            if (query_key_local = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            best_score_local := Low(Integer);
            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if result_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[result_idx_local].text);
                if get_candidate_text_unit_count(candidate_text_local) <> 2 then
                begin
                    Continue;
                end;

                candidate_weight_local :=
                    exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        exact_results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_weight_local,
                    (c_probe_limit - result_idx_local) * 32);

                if candidate_weight_local > best_score_local then
                begin
                    best_score_local := candidate_weight_local;
                    best_two_text_cache_local[local_start_idx] :=
                        candidate_text_local;
                    best_two_weight_cache_local[local_start_idx] :=
                        candidate_weight_local;
                end;
            end;

            out_text := best_two_text_cache_local[local_start_idx];
            out_weight := best_two_weight_cache_local[local_start_idx];
            Result := out_text <> '';
        end;

        function try_get_best_exact_span_text_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (local_start_idx < 0) or (local_span_len < 2) or
                (local_start_idx + local_span_len > Length(syllables_local)) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key_local := build_query_key_local(local_start_idx,
                local_span_len);
            if (query_key_local = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            best_score_local := Low(Integer);
            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if result_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[result_idx_local].text);
                if get_candidate_text_unit_count(candidate_text_local) <>
                    local_span_len then
                begin
                    Continue;
                end;

                candidate_weight_local :=
                    exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        exact_results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_weight_local,
                    (c_probe_limit - result_idx_local) * 32);

                if candidate_weight_local > best_score_local then
                begin
                    best_score_local := candidate_weight_local;
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                    Result := True;
                end;
            end;
        end;

        function is_function_or_modifier_single_quality_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168, $8D85,
                $771F, $597D, $5982, $633A, $8F83, $6781, $800C, $5E76:
                    Result := True;
            end;
        end;

        function is_directional_location_phrase_text_local(
            const text_value: string): Boolean;
        var
            text_units_local: TArray<string>;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            text_units_local := split_text_units(Trim(text_value));
            if Length(text_units_local) <> 2 then
            begin
                Exit;
            end;

            head_text_local := text_units_local[0];
            tail_text_local := text_units_local[1];
            if (get_candidate_text_unit_count(head_text_local) <> 1) or
                (get_candidate_text_unit_count(tail_text_local) <> 1) then
            begin
                Exit;
            end;

            case Ord(head_text_local[1]) of
                $5317, $5357, $4E1C, $897F, $524D, $540E, $4E0A, $4E0B,
                $91CC, $5185, $5916, $5DE6, $53F3:
                    case Ord(tail_text_local[1]) of
                        $9762, $8FB9, $4FA7, $65B9, $90E8, $7AEF, $5934:
                            Result := True;
                    end;
            end;
        end;

        function get_quantity_tail_text_local(
            const syllable_text: string): string;
        var
            normalized_syllable_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if normalized_syllable_local = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable_local = 'ge' then
            begin
                Result := string(Char($4E2A));
            end
            else if normalized_syllable_local = 'tiao' then
            begin
                Result := string(Char($6761));
            end
            else if normalized_syllable_local = 'zhang' then
            begin
                Result := string(Char($5F20));
            end
            else if normalized_syllable_local = 'ben' then
            begin
                Result := string(Char($672C));
            end
            else if normalized_syllable_local = 'pian' then
            begin
                Result := string(Char($7247));
            end
            else if normalized_syllable_local = 'bu' then
            begin
                Result := string(Char($6B65));
            end
            else if normalized_syllable_local = 'lun' then
            begin
                Result := string(Char($8F6E));
            end
            else if normalized_syllable_local = 'hui' then
            begin
                Result := string(Char($56DE));
            end
            else if normalized_syllable_local = 'xia' then
            begin
                Result := string(Char($4E0B));
            end;
        end;

        function get_fixed_quantity_alignment_score_local(
            const candidate: TncCandidate): Integer;
        var
            candidate_units_local: TArray<string>;
            syllable_idx_local: Integer;
            expected_tail_text_local: string;
        begin
            Result := 0;
            if not is_complete_candidate_local(candidate) then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate.text));
            if Length(candidate_units_local) <> expected_units_local then
            begin
                Exit;
            end;

            for syllable_idx_local := 0 to expected_units_local - 2 do
            begin
                if syllable_idx_local < expected_units_local - 3 then
                begin
                    Continue;
                end;
                if normalize_pinyin_text(syllables_local[syllable_idx_local].text) <>
                    'yi' then
                begin
                    Continue;
                end;

                expected_tail_text_local := get_quantity_tail_text_local(
                    syllables_local[syllable_idx_local + 1].text);
                if expected_tail_text_local = '' then
                begin
                    Continue;
                end;

                if SameText(Trim(candidate_units_local[syllable_idx_local]),
                    string(Char($4E00))) and
                    SameText(Trim(candidate_units_local[syllable_idx_local + 1]),
                    expected_tail_text_local) then
                begin
                    Inc(Result, 22000);
                end
                else
                begin
                    Dec(Result, 22000);
                end;
            end;
        end;

        function get_first_text_unit_local(const value: string): string;
        var
            units_local: TArray<string>;
        begin
            Result := '';
            units_local := split_text_units(Trim(value));
            if Length(units_local) > 0 then
            begin
                Result := units_local[0];
            end;
        end;

        function get_last_text_unit_local(const value: string): string;
        var
            units_local: TArray<string>;
        begin
            Result := '';
            units_local := split_text_units(Trim(value));
            if Length(units_local) > 0 then
            begin
                Result := units_local[High(units_local)];
            end;
        end;

        function is_likely_verbal_following_segment_local(
            const text_value: string): Boolean;
        var
            first_unit_local: string;
        begin
            Result := False;
            first_unit_local := get_first_text_unit_local(text_value);
            if first_unit_local = '' then
            begin
                Exit;
            end;

            case Ord(first_unit_local[1]) of
                $7EE7, $94BB, $5C55, $89E3, $53D1, $8FD0, $5904, $4F7F,
                $6267, $63D0, $6295, $9009, $6210, $8FDB, $6539, $8BAD,
                $6D4B, $5B66, $5DE5, $505A, $5199, $770B, $8BF4, $8BFB,
                $95EE, $8D70, $53BB, $6765, $5750, $5E2E, $627E, $5E26,
                $4FDD, $53D8, $8F6C, $63A5, $89C2, $6CE8, $6D41, $8F93:
                    Result := True;
            end;
        end;

        function get_de_particle_context_score_local(
            const previous_segment_text: string; const current_segment_text: string;
            const next_segment_text: string): Integer;
        var
            current_first_unit_local: string;
            current_last_unit_local: string;
            previous_last_unit_local: string;
            next_units_local: Integer;
            next_is_verbal_local: Boolean;
        begin
            Result := 0;
            if current_segment_text = '' then
            begin
                Exit;
            end;

            current_first_unit_local := get_first_text_unit_local(
                current_segment_text);
            current_last_unit_local := get_last_text_unit_local(
                current_segment_text);
            if (current_last_unit_local <> string(Char($7684))) and
                (current_last_unit_local <> string(Char($5730))) then
            begin
                Exit;
            end;

            previous_last_unit_local := get_last_text_unit_local(
                previous_segment_text);
            next_units_local := get_candidate_text_unit_count(
                next_segment_text);
            next_is_verbal_local :=
                is_likely_verbal_following_segment_local(next_segment_text);

            if current_last_unit_local = string(Char($5730)) then
            begin
                if SameText(previous_last_unit_local, string(Char($822C))) or
                    SameText(current_first_unit_local, string(Char($822C))) then
                begin
                    Inc(Result, 6000);
                end;
                if next_segment_text <> '' then
                begin
                    if next_is_verbal_local then
                    begin
                        Inc(Result, 9000);
                    end
                    else if next_units_local >= 2 then
                    begin
                        Dec(Result, 14000);
                    end;
                end;
            end
            else if current_last_unit_local = string(Char($7684)) then
            begin
                if (SameText(previous_last_unit_local, string(Char($822C))) or
                    SameText(current_first_unit_local, string(Char($822C)))) and
                    next_is_verbal_local then
                begin
                    Dec(Result, 14000);
                end
                else if (next_segment_text <> '') and (next_units_local >= 2) and
                    (not next_is_verbal_local) then
                begin
                    Inc(Result, 9000);
                end;
            end;
        end;

        function get_indexed_segment_path_local(
            const candidate: TncCandidate; const candidate_index: Integer): string;
        begin
            Result := '';
            if (candidate_index >= 0) and
                (candidate_index < Length(m_candidate_segment_paths)) then
            begin
                Result := m_candidate_segment_paths[candidate_index];
                Exit;
            end;
            Result := get_segment_path_for_candidate(candidate);
        end;

        function top_complete_path_is_stable_without_repair: Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_text_local: string;
            next_segment_text_local: string;
            previous_segment_text_local: string;
            segment_units_local: Integer;
            previous_was_single_local: Boolean;
            multi_segment_count_local: Integer;
            effective_score_local: Integer;
            particle_context_score_local: Integer;
        begin
            Result := False;
            if (Length(candidates) = 0) or
                (not is_complete_candidate_local(candidates[0])) then
            begin
                Exit;
            end;
            if get_fixed_quantity_alignment_score_local(candidates[0]) < 0 then
            begin
                Exit;
            end;

            effective_score_local := candidates[0].score;
            if candidates[0].has_dict_weight and
                (candidates[0].dict_weight > effective_score_local) then
            begin
                effective_score_local := candidates[0].dict_weight;
            end;
            if (candidates[0].source <> cs_user) and
                (effective_score_local < 50000) then
            begin
                Exit;
            end;

            encoded_path_local := get_indexed_segment_path_local(candidates[0], 0);
            if get_encoded_path_segment_count_local(encoded_path_local) <= 1 then
            begin
                Exit;
            end;

            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            previous_was_single_local := False;
            multi_segment_count_local := 0;
            previous_segment_text_local := '';
            for part_idx_local := 0 to High(segment_parts_local) do
            begin
                segment_text_local := Trim(segment_parts_local[part_idx_local]);
                next_segment_text_local := '';
                if part_idx_local < High(segment_parts_local) then
                begin
                    next_segment_text_local :=
                        Trim(segment_parts_local[part_idx_local + 1]);
                end;
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if segment_units_local <= 0 then
                begin
                    Exit;
                end;

                particle_context_score_local :=
                    get_de_particle_context_score_local(
                    previous_segment_text_local, segment_text_local,
                    next_segment_text_local);
                if particle_context_score_local < -8000 then
                begin
                    Exit;
                end;

                if segment_units_local = 1 then
                begin
                    if previous_was_single_local or
                        (not is_function_or_modifier_single_quality_local(
                        segment_text_local)) then
                    begin
                        Exit;
                    end;
                    previous_was_single_local := True;
                    previous_segment_text_local := segment_text_local;
                    Continue;
                end;

                previous_was_single_local := False;
                Inc(multi_segment_count_local);
                previous_segment_text_local := segment_text_local;
            end;

            Result := multi_segment_count_local >= 2;
        end;

        function get_candidate_path_quality_local(
            const candidate: TncCandidate; const candidate_index: Integer): Integer;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            segment_start_local: Integer;
            query_key_local: string;
            prev_segment_text_local: string;
            prev_prev_segment_text_local: string;
            prev_single_text_local: string;
            prev_single_start_local: Integer;
            prev_was_single_local: Boolean;
            pair_text_local: string;
            best_pair_text_local: string;
            best_pair_weight_local: Integer;
            next_segment_text_local: string;
            next_segment_units_local: Integer;
            combined_text_local: string;
            combined_units_local: Integer;
            best_span_text_local: string;
            best_span_weight_local: Integer;
            next_segment_units_text_local: TArray<string>;
        begin
            Result := 0;
            if not is_complete_candidate_local(candidate) then
            begin
                Exit(Low(Integer) div 4);
            end;

            if candidate.source = cs_user then
            begin
                Inc(Result, 5000);
            end;
            Inc(Result, get_fixed_quantity_alignment_score_local(candidate));

            if (m_dictionary <> nil) and
                m_dictionary.is_base_entry(lookup_text, Trim(candidate.text)) then
            begin
                Inc(Result, 12000);
            end;

            encoded_path_local := get_indexed_segment_path_local(candidate,
                candidate_index);
            if get_encoded_path_segment_count_local(encoded_path_local) <= 1 then
            begin
                Dec(Result, 12000);
                Exit;
            end;

            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            segment_start_local := 0;
            prev_segment_text_local := '';
            prev_prev_segment_text_local := '';
            prev_single_text_local := '';
            prev_single_start_local := -1;
            prev_was_single_local := False;
            for part_idx_local := 0 to High(segment_parts_local) do
            begin
                segment_text_local := Trim(segment_parts_local[part_idx_local]);
                segment_units_local :=
                    get_candidate_text_unit_count(segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) or
                    (segment_start_local + segment_units_local >
                    expected_units_local) then
                begin
                    Dec(Result, 16000);
                    Exit;
                end;

                next_segment_text_local := '';
                if part_idx_local < High(segment_parts_local) then
                begin
                    next_segment_text_local :=
                        Trim(segment_parts_local[part_idx_local + 1]);
                end;
                Inc(Result, get_de_particle_context_score_local(
                    prev_segment_text_local, segment_text_local,
                    next_segment_text_local));

                if segment_units_local > c_max_path_segment_units then
                begin
                    Dec(Result, 6000 + (segment_units_local * 400));
                end;

                if (segment_units_local = 1) and
                    (part_idx_local < High(segment_parts_local)) then
                begin
                    next_segment_units_local := get_candidate_text_unit_count(
                        next_segment_text_local);
                    combined_units_local := segment_units_local +
                        next_segment_units_local;
                    if (next_segment_text_local <> '') and
                        (combined_units_local >= 3) and
                        (combined_units_local <= 4) and
                        try_get_best_exact_span_text_local(segment_start_local,
                        combined_units_local, best_span_text_local,
                        best_span_weight_local) then
                    begin
                        combined_text_local := segment_text_local +
                            next_segment_text_local;
                        if (best_span_weight_local >= 900) and
                            (not SameText(best_span_text_local,
                            combined_text_local)) then
                        begin
                            Dec(Result, 9000 + Min(9000,
                                best_span_weight_local));
                        end;
                    end;

                    // Penalize an off-by-one split such as A/BC when AB is a
                    // strong phrase. This keeps complete long-sentence paths
                    // from preferring self-composed tails over phrase-aligned
                    // paths.
                    if next_segment_units_local = 2 then
                    begin
                        next_segment_units_text_local := split_text_units(
                            next_segment_text_local);
                        if (Length(next_segment_units_text_local) = 2) and
                            try_get_best_exact_two_unit_text_local(
                            segment_start_local, best_pair_text_local,
                            best_pair_weight_local) then
                        begin
                            pair_text_local := segment_text_local +
                                Trim(next_segment_units_text_local[0]);
                            if (best_pair_weight_local >= 520) and
                                (not SameText(best_pair_text_local,
                                pair_text_local)) then
                            begin
                                Dec(Result, 36000 +
                                    Min(16000, best_pair_weight_local * 4));
                            end;
                        end;
                    end;
                end;

                query_key_local := build_query_key_local(segment_start_local,
                    segment_units_local);
                Inc(Result, get_exact_segment_quality_local(query_key_local,
                    segment_text_local, segment_units_local));
                if (segment_units_local = 2) and
                    is_directional_location_phrase_text_local(
                    segment_text_local) and
                    (prev_segment_text_local <> '') and
                    (get_candidate_text_unit_count(prev_segment_text_local) >=
                    2) then
                begin
                    Inc(Result, 9600);
                end;
                if (segment_units_local = 2) and
                    try_get_best_exact_two_unit_text_local(segment_start_local,
                    best_pair_text_local, best_pair_weight_local) and
                    SameText(best_pair_text_local, segment_text_local) then
                begin
                    Inc(Result, 1800 + Min(1800, best_pair_weight_local div 2));
                end;
                if prev_segment_text_local <> '' then
                begin
                    Inc(Result, m_dictionary.get_context_bonus(
                        prev_segment_text_local, segment_text_local) * 4);
                    if prev_prev_segment_text_local <> '' then
                    begin
                        Inc(Result, m_dictionary.get_context_trigram_bonus(
                            prev_prev_segment_text_local,
                            prev_segment_text_local, segment_text_local) * 6);
                    end;
                end;
                if segment_units_local = 1 then
                begin
                    if prev_was_single_local and
                        (prev_single_start_local + 1 = segment_start_local) then
                    begin
                        pair_text_local := prev_single_text_local +
                            segment_text_local;
                        if try_get_best_exact_two_unit_text_local(
                            prev_single_start_local, best_pair_text_local,
                            best_pair_weight_local) then
                        begin
                            if SameText(best_pair_text_local, pair_text_local) then
                            begin
                                Inc(Result, 520 +
                                    Min(1200, best_pair_weight_local div 3));
                            end
                            else
                            begin
                                Dec(Result, 42000 +
                                    Min(18000, best_pair_weight_local * 4));
                            end;
                        end
                        else
                        begin
                            Dec(Result, 760);
                        end;
                    end;
                    if is_function_or_modifier_single_quality_local(
                        segment_text_local) then
                    begin
                        Inc(Result, 960);
                    end
                    else
                    begin
                        Dec(Result, 520);
                    end;
                    prev_was_single_local := True;
                    prev_single_text_local := segment_text_local;
                    prev_single_start_local := segment_start_local;
                end
                else
                begin
                    prev_was_single_local := False;
                    prev_single_text_local := '';
                    prev_single_start_local := -1;
                end;
                Inc(segment_start_local, segment_units_local);
                prev_prev_segment_text_local := prev_segment_text_local;
                prev_segment_text_local := segment_text_local;
            end;

            if segment_start_local <> expected_units_local then
            begin
                Dec(Result, 16000);
            end;
        end;

        procedure move_candidate_local(const from_idx: Integer;
            const to_idx: Integer);
        var
            local_idx: Integer;
            src_idx_local: Integer;
            candidate_count_local: Integer;
            moved_path_local: string;
            old_candidates_local: TncCandidateList;
            old_paths_local: TArray<string>;
        begin
            if (from_idx <= to_idx) or (from_idx < 0) or
                (from_idx > High(candidates)) or (to_idx < 0) then
            begin
                Exit;
            end;

            candidate_count_local := Length(candidates);
            moved_candidate_local := candidates[from_idx];
            moved_path_local := '';
            if from_idx < Length(m_candidate_segment_paths) then
            begin
                moved_path_local := m_candidate_segment_paths[from_idx];
            end;

            old_candidates_local := Copy(candidates);
            old_paths_local := Copy(m_candidate_segment_paths);
            SetLength(candidates, candidate_count_local);
            if Length(m_candidate_segment_paths) = candidate_count_local then
            begin
                SetLength(m_candidate_segment_paths, candidate_count_local);
            end;

            src_idx_local := 0;
            for local_idx := 0 to candidate_count_local - 1 do
            begin
                if local_idx = to_idx then
                begin
                    candidates[local_idx] := moved_candidate_local;
                    if local_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[local_idx] := moved_path_local;
                    end;
                    Continue;
                end;

                if src_idx_local = from_idx then
                begin
                    Inc(src_idx_local);
                end;
                if src_idx_local >= candidate_count_local then
                begin
                    Break;
                end;

                candidates[local_idx] := old_candidates_local[src_idx_local];
                if local_idx < Length(m_candidate_segment_paths) then
                begin
                    if src_idx_local < Length(old_paths_local) then
                    begin
                        m_candidate_segment_paths[local_idx] :=
                            old_paths_local[src_idx_local];
                    end
                    else
                    begin
                        m_candidate_segment_paths[local_idx] := '';
                    end;
                end;
                Inc(src_idx_local);
            end;
        end;

        procedure promote_best_fixed_quantity_tail_candidate_local;
        var
            local_idx: Integer;
            best_idx_quantity_local: Integer;
            best_score_quantity_local: Integer;
            candidate_alignment_local: Integer;
            candidate_score_local: Integer;
            top_alignment_local: Integer;
            top_score_local: Integer;
        begin
            best_idx_quantity_local := -1;
            best_score_quantity_local := Low(Integer);
            for local_idx := 0 to Min(8, High(candidates)) do
            begin
                candidate_alignment_local :=
                    get_fixed_quantity_alignment_score_local(candidates[local_idx]);
                if candidate_alignment_local <= 0 then
                begin
                    Continue;
                end;

                candidate_score_local := candidates[local_idx].score +
                    candidate_alignment_local;
                if candidate_score_local > best_score_quantity_local then
                begin
                    best_score_quantity_local := candidate_score_local;
                    best_idx_quantity_local := local_idx;
                end;
            end;

            if best_idx_quantity_local <= 0 then
            begin
                Exit;
            end;

            top_alignment_local :=
                get_fixed_quantity_alignment_score_local(candidates[0]);
            top_score_local := candidates[0].score + top_alignment_local;
            if (top_alignment_local > 0) and
                (top_score_local >= best_score_quantity_local - 2000) then
            begin
                Exit;
            end;

            if best_score_quantity_local < top_score_local + 8000 then
            begin
                Exit;
            end;

            move_candidate_local(best_idx_quantity_local, 0);
            m_candidates := candidates;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' qfixqty=[idx=%d score=%d text=%s]',
                    [best_idx_quantity_local, best_score_quantity_local -
                    top_score_local, Trim(candidates[0].text)]);
            end;
        end;

        procedure promote_best_scored_complete_candidate_local;
        const
            c_complete_score_promote_margin = 256;
        var
            local_idx: Integer;
            best_idx_local: Integer;
            best_score_local: Integer;
            candidate_score_local: Integer;
        begin
            if (Length(candidates) < 2) or
                (not is_complete_candidate_local(candidates[0])) then
            begin
                Exit;
            end;

            best_idx_local := 0;
            best_score_local := candidates[0].score;
            if candidates[0].has_dict_weight and
                (candidates[0].dict_weight > best_score_local) then
            begin
                best_score_local := candidates[0].dict_weight;
            end;

            for local_idx := 1 to Min(8, High(candidates)) do
            begin
                if not is_complete_candidate_local(candidates[local_idx]) then
                begin
                    Continue;
                end;
                candidate_score_local := candidates[local_idx].score;
                if candidates[local_idx].has_dict_weight and
                    (candidates[local_idx].dict_weight > candidate_score_local) then
                begin
                    candidate_score_local := candidates[local_idx].dict_weight;
                end;
                if candidate_score_local >
                    best_score_local + c_complete_score_promote_margin then
                begin
                    best_idx_local := local_idx;
                    best_score_local := candidate_score_local;
                end;
            end;

            if best_idx_local > 0 then
            begin
                move_candidate_local(best_idx_local, 0);
                m_candidates := candidates;
            end;
        end;

        begin
            if (Length(candidates) < 2) or (m_dictionary = nil) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            expected_units_local := input_syllable_count;
            if expected_units_local < 4 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) <> expected_units_local then
            begin
                Exit;
            end;
        SetLength(best_two_text_cache_local, expected_units_local);
        SetLength(best_two_weight_cache_local, expected_units_local);
        SetLength(best_two_known_cache_local, expected_units_local);
        promote_best_scored_complete_candidate_local;

        // A path that looks locally stable can still lose to a better complete
        // candidate already visible in top2/topN. Keep the quality comparison
        // active so long-sentence ranking can promote those candidates without
        // widening the search space.
        if is_complete_candidate_local(candidates[0]) and
            m_dictionary.is_base_entry(lookup_text, Trim(candidates[0].text)) then
        begin
            Exit;
        end;

        top_quality_local := get_candidate_path_quality_local(candidates[0], 0);
        second_quality_local := Low(Integer) div 4;
        if Length(candidates) > 1 then
        begin
            second_quality_local := get_candidate_path_quality_local(
                candidates[1], 1);
        end;

        best_idx_local := -1;
        best_quality_local := Low(Integer) div 4;
        for idx_local := 1 to Min(8, High(candidates)) do
        begin
            candidate_quality_local :=
                get_candidate_path_quality_local(candidates[idx_local],
                idx_local);
            if candidate_quality_local > best_quality_local then
            begin
                best_quality_local := candidate_quality_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            Exit;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' qeval=[idx=%d top=%d best=%d diff=%d text=%s]',
                [best_idx_local, top_quality_local, best_quality_local,
                best_quality_local - top_quality_local,
                Trim(candidates[best_idx_local].text)]);
        end;

        if best_quality_local >= top_quality_local + c_promote_to_first_margin then
        begin
            move_candidate_local(best_idx_local, 0);
            m_candidates := candidates;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' qpromote=[to=1 idx=%d q=%d text=%s]',
                    [best_idx_local, best_quality_local - top_quality_local,
                    Trim(candidates[0].text)]);
            end;
        end
        else if (best_idx_local > 1) and
            (best_quality_local >= second_quality_local +
            c_promote_to_second_margin) then
        begin
            move_candidate_local(best_idx_local, 1);
            m_candidates := candidates;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' qpromote=[to=2 idx=%d q=%d text=%s]',
                    [best_idx_local, best_quality_local - second_quality_local,
                    Trim(candidates[1].text)]);
            end;
        end;
        promote_best_fixed_quantity_tail_candidate_local;
    end;

    function get_preferred_exact_subspan_bonus_local(const query_key: string;
        const candidate_text: string): Integer;
    begin
        Result := 0;
    end;

    function try_resolve_best_exact_subspan_local(const start_idx: Integer;
        const span_syllables: Integer; out out_text: string; out out_score: Integer;
        out out_segments: Integer; out out_path: string; out out_has_anchor: Boolean;
        out out_anchor_units: Integer): Boolean;
    const
        c_state_unknown = 0;
        c_state_miss = 1;
        c_state_hit = 2;
        c_probe_limit = 8;
        c_span_max_syllables = 8;
        c_fixed_single_bonus = 1800;
        c_chunk_length_bonus = 1200;
        c_chunk_anchor_bonus = 3200;
        c_weak_fixed_head_exact_weight = 720;
        var
            syllable_count_local: Integer;
            oracle_preferred_text: string;
            oracle_preferred_bonus: Integer;
            standalone_promote_bonus_local: Integer;

        function get_fixed_boundary_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end;
        end;

        procedure prepare_exact_subspan_cache_local;
        var
            local_idx: Integer;
        begin
            if exact_subspan_cache_prepared then
            begin
                Exit;
            end;

            exact_subspan_cache_prepared := True;
            exact_subspan_cache_syllables := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(exact_subspan_cache_syllables) <> input_syllable_count then
            begin
                SetLength(exact_subspan_cache_syllables, 0);
                Exit;
            end;

            syllable_count_local := Length(exact_subspan_cache_syllables);
            SetLength(exact_subspan_cache_states, syllable_count_local);
            SetLength(exact_subspan_cache_scores, syllable_count_local);
            SetLength(exact_subspan_cache_segments, syllable_count_local);
            SetLength(exact_subspan_cache_anchor_units, syllable_count_local);
            SetLength(exact_subspan_cache_has_anchor, syllable_count_local);
            SetLength(exact_subspan_cache_texts, syllable_count_local);
            SetLength(exact_subspan_cache_paths, syllable_count_local);
            SetLength(exact_subspan_single_cache_prepared, syllable_count_local);
            SetLength(exact_subspan_single_cache_texts, syllable_count_local);
            SetLength(exact_subspan_single_cache_weights, syllable_count_local);
            SetLength(exact_subspan_fixed_weight_cache_states,
                syllable_count_local);
            SetLength(exact_subspan_fixed_weight_cache_values,
                syllable_count_local);
            SetLength(exact_subspan_chunk_cache_prepared, syllable_count_local);
            SetLength(exact_subspan_chunk_cache_texts, syllable_count_local);
            SetLength(exact_subspan_chunk_cache_weights, syllable_count_local);
            for local_idx := 0 to syllable_count_local - 1 do
            begin
                SetLength(exact_subspan_cache_states[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_scores[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_segments[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_anchor_units[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_has_anchor[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_texts[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_cache_paths[local_idx], c_span_max_syllables + 1);
                SetLength(exact_subspan_chunk_cache_prepared[local_idx],
                    c_span_max_syllables + 1);
                SetLength(exact_subspan_chunk_cache_texts[local_idx],
                    c_span_max_syllables + 1);
                SetLength(exact_subspan_chunk_cache_weights[local_idx],
                    c_span_max_syllables + 1);
            end;
        end;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_syllables: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_syllables <= 0) or
                (local_start_idx + local_span_syllables - 1 > High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_span_syllables - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(exact_subspan_cache_syllables[local_idx].text);
            end;
        end;

        function subspan_has_fixed_boundary_local(const local_start_idx: Integer;
            const local_span_syllables: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or (local_span_syllables <= 0) or
                (local_start_idx + local_span_syllables - 1 >
                High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_span_syllables - 1 do
            begin
                if get_fixed_boundary_single_char_local(
                    exact_subspan_cache_syllables[local_idx].text) <> '' then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_valid_cjk_phrase_text_local(const value: string;
            const expected_units: Integer): Boolean;
        var
            units: TArray<string>;
            unit_idx: Integer;
            codepoint: Integer;

            function try_get_codepoint_local(const unit_text: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if unit_text = '' then
                begin
                    Exit;
                end;

                if Length(unit_text) = 1 then
                begin
                    out_codepoint := Ord(unit_text[1]);
                    Exit(True);
                end;

                if (Length(unit_text) = 2) and
                    (Ord(unit_text[1]) >= $D800) and
                    (Ord(unit_text[1]) <= $DBFF) and
                    (Ord(unit_text[2]) >= $DC00) and
                    (Ord(unit_text[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                        (Ord(unit_text[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if Trim(value) = '' then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(units) do
            begin
                if (not try_get_codepoint_local(units[unit_idx], codepoint)) or
                    (codepoint < $4E00) or (codepoint > $9FFF) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function try_get_best_subspan_standalone_query_candidate_local(
            const query_key_local: string; const expected_syllables: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        const
            c_subspan_oracle_probe_limit = 8;
        var
            cached_state_local: Integer;
            snapshot_candidates_local: TncCandidateList;
            snapshot_scores_local: TArray<Integer>;
            candidate_idx_local: Integer;
            candidate_local: TncCandidate;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_score_local: Integer;
            best_text_local: string;
            best_score_local: Integer;
            preferred_exact_bonus_local: Integer;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            preferred_text_local := '';
            preferred_bonus_local := 0;
            if (query_key_local = '') or
                (not is_full_pinyin_key(query_key_local)) or
                (expected_syllables < 3) or (expected_syllables > 8) then
            begin
                Exit;
            end;
            if m_disable_subspan_standalone_oracle > 0 then
            begin
                Exit;
            end;
            try_get_preferred_query_phrase_text_local(query_key_local,
                preferred_text_local, preferred_bonus_local);

            if subspan_oracle_states = nil then
            begin
                subspan_oracle_states := TDictionary<string, Integer>.Create;
                subspan_oracle_texts := TDictionary<string, string>.Create;
                subspan_oracle_scores := TDictionary<string, Integer>.Create;
            end;

            if subspan_oracle_states.TryGetValue(query_key_local,
                cached_state_local) then
            begin
                if cached_state_local = 1 then
                begin
                    subspan_oracle_texts.TryGetValue(query_key_local, out_text);
                    subspan_oracle_scores.TryGetValue(query_key_local, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            if subspan_oracle_budget_remaining <= 0 then
            begin
                Exit;
            end;

            if subspan_oracle_engine = nil then
            begin
                subspan_oracle_config := m_config;
                subspan_oracle_config.debug_mode := False;
                subspan_oracle_config.max_candidates := 6;
                subspan_oracle_engine := TncEngine.Create(
                    subspan_oracle_config, False, False);
                inherit_search_budget_policy_to(subspan_oracle_engine);
            end;

            Dec(subspan_oracle_budget_remaining);
            subspan_oracle_states.AddOrSetValue(query_key_local, 3);
            subspan_oracle_engine.m_disable_subspan_standalone_oracle :=
                m_disable_subspan_standalone_oracle + 1;
            subspan_oracle_engine.reset;
            subspan_oracle_engine.m_composition_text := query_key_local;
            subspan_oracle_engine.build_candidates;

            SetLength(snapshot_candidates_local,
                Length(subspan_oracle_engine.m_candidates));
            SetLength(snapshot_scores_local,
                Length(subspan_oracle_engine.m_candidates));
            for candidate_idx_local := 0 to High(subspan_oracle_engine.m_candidates) do
            begin
                snapshot_candidates_local[candidate_idx_local] :=
                    subspan_oracle_engine.m_candidates[candidate_idx_local];
                snapshot_scores_local[candidate_idx_local] :=
                    subspan_oracle_engine.get_rank_score(
                    subspan_oracle_engine.m_candidates[candidate_idx_local]);
            end;

            best_text_local := '';
            best_score_local := Low(Integer);
            for candidate_idx_local := 0 to High(snapshot_candidates_local) do
            begin
                if candidate_idx_local >= c_subspan_oracle_probe_limit then
                begin
                    Break;
                end;

                candidate_local := snapshot_candidates_local[candidate_idx_local];
                if Trim(candidate_local.comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(candidate_local.text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if (candidate_units_local <> expected_syllables) or
                    (not is_valid_cjk_phrase_text_local(candidate_text_local,
                    expected_syllables)) then
                begin
                    Continue;
                end;

                candidate_score_local :=
                    snapshot_scores_local[candidate_idx_local] +
                    get_preferred_query_phrase_bonus_local(query_key_local,
                    candidate_text_local);
                preferred_exact_bonus_local :=
                    get_preferred_exact_subspan_bonus_local(query_key_local,
                    candidate_text_local);
                if preferred_exact_bonus_local > 0 then
                begin
                    Inc(candidate_score_local,
                        preferred_exact_bonus_local + 12000);
                end;
                support_path_local := infer_segment_path_for_query_text_with_score(
                    query_key_local, candidate_text_local, support_score_local,
                    support_segments_local);
                if support_path_local <> '' then
                begin
                    Inc(candidate_score_local, support_score_local);
                    Inc(candidate_score_local,
                        Max(0, 2048 - (support_segments_local * 128)));
                end
                else
                begin
                    Dec(candidate_score_local, 2048);
                end;

                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_text_local := candidate_text_local;
                end;

                // The child engine has already applied the full candidate
                // ordering rules.  Do not let a later synthetic candidate with
                // an inflated internal rank score override the child top
                // complete result when resolving a sentence subspan.
                if candidate_idx_local = 0 then
                begin
                    Break;
                end;
            end;

            if best_text_local <> '' then
            begin
                out_text := best_text_local;
                out_score := best_score_local;
                subspan_oracle_states.AddOrSetValue(query_key_local, 1);
                subspan_oracle_texts.AddOrSetValue(query_key_local, out_text);
                subspan_oracle_scores.AddOrSetValue(query_key_local, out_score);
                Exit(True);
            end;

            subspan_oracle_states.AddOrSetValue(query_key_local, 2);
        end;

        function try_get_best_direct_exact_candidate_local(
            const local_start_idx: Integer; const local_span_syllables: Integer;
            out resolved_text: string; out resolved_score: Integer;
            out resolved_has_anchor: Boolean; out resolved_anchor_units: Integer;
            out resolved_exact_bonus: Integer): Boolean;
        const
            c_direct_probe_limit = 8;
            c_direct_rank_step = 240;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            exact_only_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_units_local: Integer;
            base_weight_local: Integer;
            best_score_local: Integer;
            best_text_local: string;
            best_anchor_units_local: Integer;
            best_has_anchor_local: Boolean;
            exact_bonus_local: Integer;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            preferred_base_weight_local: Integer;

            function preferred_phrase_matches_local: Boolean;
            begin
                Result := is_valid_cjk_phrase_text_local(preferred_text_local,
                    local_span_syllables);
            end;
        begin
            Result := False;
            resolved_text := '';
            resolved_score := 0;
            resolved_has_anchor := False;
            resolved_anchor_units := -1;
            resolved_exact_bonus := 0;
            if (local_span_syllables <= 1) then
            begin
                Exit;
            end;

            query_key_local := build_query_key_local(local_start_idx,
                local_span_syllables);
            if query_key_local = '' then
            begin
                Exit;
            end;

            preferred_text_local := '';
            preferred_bonus_local := 0;
            best_score_local := Low(Integer);
            best_text_local := '';
            best_anchor_units_local := -1;
            best_has_anchor_local := False;
            if try_get_preferred_query_phrase_text_local(query_key_local,
                preferred_text_local, preferred_bonus_local) and
                preferred_phrase_matches_local then
            begin
                preferred_base_weight_local := preferred_bonus_local +
                    (c_direct_probe_limit * c_direct_rank_step) + 12000;
                if local_span_syllables >= 3 then
                begin
                    Inc(preferred_base_weight_local, 12000);
                end;
                best_score_local := preferred_base_weight_local;
                best_text_local := preferred_text_local;
                best_has_anchor_local := local_span_syllables >= 3;
                if best_has_anchor_local then
                begin
                    best_anchor_units_local := local_span_syllables;
                    resolved_exact_bonus := Max(resolved_exact_bonus,
                        preferred_bonus_local);
                end
                else
                begin
                    best_anchor_units_local := 0;
                end;
                Result := True;
            end;

            SetLength(exact_results_local, 0);
            if lookup_exact_full_pinyin_cached_local(query_key_local,
                exact_only_results_local) then
            begin
                for result_idx_local := 0 to High(exact_only_results_local) do
                begin
                    SetLength(exact_results_local, Length(exact_results_local) + 1);
                    exact_results_local[High(exact_results_local)] :=
                        exact_only_results_local[result_idx_local];
                end;
            end;

            if Length(exact_results_local) = 0 then
            begin
                if not Result then
                begin
                    Exit;
                end;
            end;

            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if result_idx_local >= c_direct_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(exact_results_local[result_idx_local]) or
                    is_runtime_common_pattern_candidate(exact_results_local[result_idx_local]) or
                    is_runtime_redup_candidate(exact_results_local[result_idx_local]) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(exact_results_local[result_idx_local].text);
                candidate_units_local := get_candidate_text_unit_count(candidate_text_local);
                if candidate_units_local <> local_span_syllables then
                begin
                    Continue;
                end;
                if not is_valid_cjk_phrase_text_local(candidate_text_local,
                    local_span_syllables) then
                begin
                    Continue;
                end;

                base_weight_local := exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight then
                begin
                    if local_span_syllables <= 2 then
                    begin
                        base_weight_local :=
                            exact_results_local[result_idx_local].dict_weight;
                    end
                    else if (base_weight_local <= 0) or
                        (exact_results_local[result_idx_local].dict_weight >
                        base_weight_local) then
                    begin
                        base_weight_local :=
                            exact_results_local[result_idx_local].dict_weight;
                    end;
                end;
                Inc(base_weight_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                exact_bonus_local := get_preferred_exact_subspan_bonus_local(
                    query_key_local, candidate_text_local);
                Inc(base_weight_local, exact_bonus_local);
                if (preferred_text_local <> '') and
                    SameText(candidate_text_local, preferred_text_local) then
                begin
                    Inc(base_weight_local, 12000);
                    if local_span_syllables >= 3 then
                    begin
                        Inc(base_weight_local, 12000);
                    end;
                end;
                Inc(base_weight_local,
                    (c_direct_probe_limit - result_idx_local) * c_direct_rank_step);
                if (local_span_syllables >= 3) and (exact_bonus_local > 0) then
                begin
                    Inc(base_weight_local, 12000);
                end;

                resolved_score := base_weight_local +
                    (local_span_syllables * local_span_syllables * c_chunk_length_bonus);
                resolved_has_anchor := local_span_syllables >= 3;
                if resolved_has_anchor then
                begin
                    Inc(resolved_score, c_chunk_anchor_bonus);
                    resolved_anchor_units := local_span_syllables;
                end
                else
                begin
                    resolved_anchor_units := 0;
                end;

                if (not Result) or (resolved_score > best_score_local) or
                    ((resolved_score = best_score_local) and
                    (CompareText(candidate_text_local, best_text_local) < 0)) then
                begin
                    Result := True;
                    best_score_local := resolved_score;
                    best_text_local := candidate_text_local;
                    best_has_anchor_local := resolved_has_anchor;
                    best_anchor_units_local := resolved_anchor_units;
                    resolved_exact_bonus := exact_bonus_local;
                end;
            end;

            if not Result then
            begin
                Exit;
            end;

            resolved_text := best_text_local;
            resolved_score := best_score_local;
            resolved_has_anchor := best_has_anchor_local;
            resolved_anchor_units := best_anchor_units_local;
        end;

        function try_get_best_three_syllable_fixed_boundary_compound_local(
            const local_start_idx: Integer; out resolved_text: string;
            out resolved_score: Integer; out resolved_has_anchor: Boolean;
            out resolved_anchor_units: Integer): Boolean;
        const
            c_compound_probe_limit = 8;
            c_fixed_compound_bonus = 4096;
            c_fixed_boundary_tail_bonus = 1800;
        var
            local_query_key: string;
            head_query_key: string;
            tail_query_key: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_weight_local: Integer;
            fixed_head_text_local: string;
            fixed_tail_text_local: string;
            fixed_head_weight_local: Integer;
            fixed_tail_weight_local: Integer;
            pair_text_local: string;
            pair_bonus_local: Integer;

            function normalize_compound_weight_local(
                const raw_weight: Integer): Integer;
            begin
                Result := raw_weight;
                if Result > 240 then
                begin
                    Result := 240 + ((Result - 240) div 10);
                end;
            end;

            function get_effective_weight_local(
                const candidate: TncCandidate): Integer;
            begin
                Result := candidate.score;
                if candidate.has_dict_weight and
                    (candidate.dict_weight > Result) then
                begin
                    Result := candidate.dict_weight;
                end;
            end;

            function try_get_fixed_single_weight_local(
                const local_fixed_start_idx: Integer; const expected_text: string;
                out out_weight: Integer): Boolean;
            var
                local_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_weight: Integer;
            begin
                Result := False;
                out_weight := 0;
                if (expected_text = '') or (m_dictionary = nil) then
                begin
                    Exit;
                end;
                local_key := normalize_pinyin_text(
                    exact_subspan_cache_syllables[local_fixed_start_idx].text);
                if (local_key = '') or
                    (not lookup_exact_full_pinyin_cached_local(local_key,
                    local_results)) then
                begin
                    Exit;
                end;
                for local_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if Trim(local_results[local_idx].text) <> expected_text then
                    begin
                        Continue;
                    end;
                    candidate_weight := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight > candidate_weight) then
                    begin
                        candidate_weight := local_results[local_idx].dict_weight;
                    end;
                    if candidate_weight > out_weight then
                    begin
                        out_weight := candidate_weight;
                    end;
                end;
                Result := out_weight > 0;
            end;

            procedure consider_pair_local(const left_text: string;
                const left_weight: Integer; const right_text: string;
                const right_weight: Integer);
            var
                combined_text_local: string;
                combined_score_local: Integer;
            begin
                if (left_text = '') or (right_text = '') then
                begin
                    Exit;
                end;
                combined_text_local := left_text + right_text;
                if (get_candidate_text_unit_count(combined_text_local) <> 3) or
                    (not is_valid_cjk_phrase_text_local(combined_text_local, 3)) then
                begin
                    Exit;
                end;

                combined_score_local :=
                    normalize_compound_weight_local(
                    left_weight + right_weight + c_fixed_compound_bonus) +
                    (3 * 3 * c_chunk_length_bonus) + c_chunk_anchor_bonus;
                if m_dictionary <> nil then
                begin
                    Inc(combined_score_local,
                        m_dictionary.get_context_bonus(left_text, right_text));
                    Inc(combined_score_local,
                        m_dictionary.get_base_text_prefix_bonus(
                        combined_text_local));
                end;
                Inc(combined_score_local,
                    get_preferred_exact_subspan_bonus_local(local_query_key,
                    combined_text_local));
                Inc(combined_score_local,
                    get_preferred_query_phrase_bonus_local(local_query_key,
                    combined_text_local));

                if (not Result) or (combined_score_local > resolved_score) or
                    ((combined_score_local = resolved_score) and
                    (CompareText(combined_text_local, resolved_text) < 0)) then
                begin
                    Result := True;
                    resolved_text := combined_text_local;
                    resolved_score := combined_score_local;
                    resolved_has_anchor := True;
                    resolved_anchor_units := 3;
                end;
            end;
        begin
            Result := False;
            resolved_text := '';
            resolved_score := 0;
            resolved_has_anchor := False;
            resolved_anchor_units := 0;
            if (local_start_idx < 0) or
                (local_start_idx + 2 > High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            local_query_key := build_query_key_local(local_start_idx, 3);
            if local_query_key = '' then
            begin
                Exit;
            end;

            fixed_tail_text_local := get_fixed_boundary_single_char_local(
                exact_subspan_cache_syllables[local_start_idx + 2].text);
            if fixed_tail_text_local <> '' then
            begin
                if not try_get_fixed_single_weight_local(local_start_idx + 2,
                    fixed_tail_text_local, fixed_tail_weight_local) then
                begin
                    fixed_tail_weight_local := 520;
                end;
                Inc(fixed_tail_weight_local, c_fixed_boundary_tail_bonus);
                tail_query_key := normalize_pinyin_text(
                    exact_subspan_cache_syllables[local_start_idx + 2].text);
                Inc(fixed_tail_weight_local,
                    get_preferred_query_phrase_bonus_local(tail_query_key,
                    fixed_tail_text_local));

                head_query_key := build_query_key_local(local_start_idx, 2);
                if (head_query_key <> '') and
                    lookup_exact_full_pinyin_cached_local(head_query_key,
                    exact_results_local) then
                begin
                    for exact_idx_local := 0 to High(exact_results_local) do
                    begin
                        if exact_idx_local >= c_compound_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if is_runtime_chain_candidate(exact_results_local[exact_idx_local]) or
                            is_runtime_common_pattern_candidate(exact_results_local[exact_idx_local]) or
                            is_runtime_redup_candidate(exact_results_local[exact_idx_local]) then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(
                            exact_results_local[exact_idx_local].text);
                        candidate_units_local := get_candidate_text_unit_count(
                            candidate_text_local);
                        if (candidate_units_local <> 2) or
                            (not is_valid_cjk_phrase_text_local(
                            candidate_text_local, 2)) then
                        begin
                            Continue;
                        end;

                        candidate_weight_local :=
                            get_effective_weight_local(
                            exact_results_local[exact_idx_local]);
                        Inc(candidate_weight_local,
                            get_preferred_exact_subspan_bonus_local(
                            head_query_key, candidate_text_local));
                        Inc(candidate_weight_local,
                            get_preferred_query_phrase_bonus_local(
                            head_query_key, candidate_text_local));
                        consider_pair_local(candidate_text_local,
                            candidate_weight_local, fixed_tail_text_local,
                            fixed_tail_weight_local);
                    end;
                end;
            end;

            fixed_head_text_local := get_fixed_boundary_single_char_local(
                exact_subspan_cache_syllables[local_start_idx].text);
            if fixed_head_text_local <> '' then
            begin
                if not try_get_fixed_single_weight_local(local_start_idx,
                    fixed_head_text_local, fixed_head_weight_local) then
                begin
                    fixed_head_weight_local := 520;
                end;
                Inc(fixed_head_weight_local, c_fixed_boundary_tail_bonus);
                head_query_key := normalize_pinyin_text(
                    exact_subspan_cache_syllables[local_start_idx].text);
                Inc(fixed_head_weight_local,
                    get_preferred_query_phrase_bonus_local(head_query_key,
                    fixed_head_text_local));

                tail_query_key := build_query_key_local(local_start_idx + 1, 2);
                if (tail_query_key <> '') and
                    lookup_exact_full_pinyin_cached_local(tail_query_key,
                    exact_results_local) then
                begin
                    for exact_idx_local := 0 to High(exact_results_local) do
                    begin
                        if exact_idx_local >= c_compound_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if is_runtime_chain_candidate(exact_results_local[exact_idx_local]) or
                            is_runtime_common_pattern_candidate(exact_results_local[exact_idx_local]) or
                            is_runtime_redup_candidate(exact_results_local[exact_idx_local]) then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(
                            exact_results_local[exact_idx_local].text);
                        candidate_units_local := get_candidate_text_unit_count(
                            candidate_text_local);
                        if (candidate_units_local <> 2) or
                            (not is_valid_cjk_phrase_text_local(
                            candidate_text_local, 2)) then
                        begin
                            Continue;
                        end;

                        candidate_weight_local :=
                            get_effective_weight_local(
                            exact_results_local[exact_idx_local]);
                        Inc(candidate_weight_local,
                            get_preferred_exact_subspan_bonus_local(
                            tail_query_key, candidate_text_local));
                        Inc(candidate_weight_local,
                            get_preferred_query_phrase_bonus_local(
                            tail_query_key, candidate_text_local));
                        consider_pair_local(fixed_head_text_local,
                            fixed_head_weight_local, candidate_text_local,
                            candidate_weight_local);
                    end;
                end;
            end;

            if (not Result) and
                try_get_preferred_query_phrase_text_local(local_query_key,
                pair_text_local, pair_bonus_local) and
                is_valid_cjk_phrase_text_local(pair_text_local, 3) then
            begin
                resolved_text := pair_text_local;
                resolved_score :=
                    normalize_compound_weight_local(
                    pair_bonus_local + c_fixed_compound_bonus) +
                    (3 * 3 * c_chunk_length_bonus) + c_chunk_anchor_bonus;
                resolved_has_anchor := True;
                resolved_anchor_units := 3;
                Result := True;
            end;
        end;

        function get_candidate_effective_weight_local(
            const candidate: TncCandidate; const chunk_syllables: Integer): Integer;
        begin
            Result := candidate.score;
            if ((not is_fuzzy_pinyin_active) or
                (candidate.fuzzy_cost <= 0)) and candidate.has_dict_weight then
            begin
                if chunk_syllables <= 2 then
                begin
                    Result := candidate.dict_weight;
                end
                else if (Result <= 0) or (candidate.dict_weight > Result) then
                begin
                    Result := candidate.dict_weight;
                end;
            end;
        end;

        function get_first_meaningful_path_segment_local(
            const encoded_path: string): string;
        var
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
        begin
            Result := '';
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                if segment_text = '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(segment_text) >= 2 then
                begin
                    Exit(segment_text);
                end;
                if Result = '' then
                begin
                    Result := segment_text;
                end;
            end;
        end;

        function get_suffix_context_bonus_local(const chunk_text: string;
            const suffix_path: string): Integer;
        var
            meaningful_segment: string;
        begin
            Result := 0;
            if (m_dictionary = nil) or (chunk_text = '') or (suffix_path = '') then
            begin
                Exit;
            end;
            meaningful_segment := get_first_meaningful_path_segment_local(suffix_path);
            if meaningful_segment <> '' then
            begin
                Result := m_dictionary.get_context_bonus(chunk_text, meaningful_segment);
            end;
        end;

        function get_suffix_base_prefix_bonus_local(const chunk_text: string;
            const suffix_path: string): Integer;
        var
            meaningful_segment: string;
            prefix_text: string;
        begin
            Result := 0;
            if (m_dictionary = nil) or (chunk_text = '') or (suffix_path = '') then
            begin
                Exit;
            end;

            meaningful_segment := get_first_meaningful_path_segment_local(suffix_path);
            if meaningful_segment = '' then
            begin
                Exit;
            end;

            prefix_text := chunk_text + meaningful_segment;
            if (get_candidate_text_unit_count(prefix_text) < 2) or
                (get_candidate_text_unit_count(prefix_text) > 6) then
            begin
                Exit;
            end;
            Result := m_dictionary.get_base_text_prefix_bonus(prefix_text);
        end;

        function get_first_path_text_unit_local(const encoded_path: string): string;
        var
            parts: TArray<string>;
            units: TArray<string>;
            part_idx: Integer;
            segment_text: string;
        begin
            Result := '';
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                if segment_text = '' then
                begin
                    Continue;
                end;
                units := split_text_units(segment_text);
                if Length(units) > 0 then
                begin
                    Exit(units[0]);
                end;
            end;
        end;

        function get_first_path_segment_text_local(
            const encoded_path: string): string;
        var
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
        begin
            Result := '';
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                if segment_text <> '' then
                begin
                    Exit(segment_text);
                end;
            end;
        end;

        function is_structural_particle_text_local(const value: string): Boolean;
        begin
            Result := (value = string(Char($7684))) or
                (value = string(Char($5F97))) or
                (value = string(Char($5730)));
        end;

        function try_get_best_exact_two_unit_text_local(
            const local_start_idx: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        const
            c_two_unit_probe_limit = 12;
        var
            query_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            ranked_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (m_dictionary = nil) or (local_start_idx < 0) or
                (local_start_idx + 1 > High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            query_key := build_query_key_local(local_start_idx, 2);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                local_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_two_unit_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(local_results[result_idx]) or
                    is_runtime_common_pattern_candidate(local_results[result_idx]) or
                    is_runtime_redup_candidate(local_results[result_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[result_idx].text);
                if (get_candidate_text_unit_count(candidate_text) <> 2) or
                    (not is_valid_cjk_phrase_text_local(candidate_text, 2)) then
                begin
                    Continue;
                end;

                candidate_weight := get_candidate_effective_weight_local(
                    local_results[result_idx], 2);
                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));
                Inc(candidate_weight, get_preferred_exact_subspan_bonus_local(
                    query_key, candidate_text));
                ranked_weight := candidate_weight +
                    ((c_two_unit_probe_limit - result_idx) * 96);
                if (not Result) or (ranked_weight > out_weight) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_weight := ranked_weight;
                end;
            end;
        end;

        function try_get_exact_single_char_weight_local(const local_start_idx: Integer;
            const expected_text: string; out out_weight: Integer): Boolean;
        var
            query_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_weight: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (expected_text = '') or (m_dictionary = nil) or
                (local_start_idx < 0) or
                (local_start_idx > High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            if exact_subspan_fixed_weight_cache_states[local_start_idx] = 2 then
            begin
                out_weight := exact_subspan_fixed_weight_cache_values[
                    local_start_idx];
                Exit(True);
            end;
            if exact_subspan_fixed_weight_cache_states[local_start_idx] = 1 then
            begin
                Exit(False);
            end;

            query_key := normalize_pinyin_text(
                exact_subspan_cache_syllables[local_start_idx].text);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                local_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if Trim(local_results[result_idx].text) <> expected_text then
                begin
                    Continue;
                end;

                candidate_weight := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[result_idx].dict_weight;
                end;
                if candidate_weight > out_weight then
                begin
                    out_weight := candidate_weight;
                end;
            end;

            Result := out_weight > 0;
            if Result then
            begin
                exact_subspan_fixed_weight_cache_states[local_start_idx] := 2;
                exact_subspan_fixed_weight_cache_values[local_start_idx] :=
                    out_weight;
            end
            else
            begin
                exact_subspan_fixed_weight_cache_states[local_start_idx] := 1;
            end;
        end;

        function get_exact_span_coverage_quality_local(
            const local_start_idx: Integer; const local_span_syllables: Integer;
            const text_value: string): Integer;
        const
            c_quality_probe_limit = 16;
            c_quality_max_span = 5;
        var
            units_local: TArray<string>;
            occupied_local: TArray<Boolean>;
            offset_local: Integer;
            span_len_local: Integer;
            query_key_local: string;
            span_text_local: string;
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;

            function range_is_occupied_local(const range_start: Integer;
                const range_len: Integer): Boolean;
            var
                range_idx: Integer;
            begin
                Result := True;
                if (range_start < 0) or (range_len <= 0) or
                    (range_start + range_len > Length(occupied_local)) then
                begin
                    Exit;
                end;

                for range_idx := range_start to range_start + range_len - 1 do
                begin
                    if occupied_local[range_idx] then
                    begin
                        Exit;
                    end;
                end;
                Result := False;
            end;

            procedure mark_range_occupied_local(const range_start: Integer;
                const range_len: Integer);
            var
                range_idx: Integer;
            begin
                if (range_start < 0) or (range_len <= 0) or
                    (range_start + range_len > Length(occupied_local)) then
                begin
                    Exit;
                end;

                for range_idx := range_start to range_start + range_len - 1 do
                begin
                    occupied_local[range_idx] := True;
                end;
            end;

            function join_units_range_local(const range_start: Integer;
                const range_len: Integer): string;
            var
                range_idx: Integer;
            begin
                Result := '';
                if (range_start < 0) or (range_len <= 0) or
                    (range_start + range_len > Length(units_local)) then
                begin
                    Exit;
                end;

                for range_idx := range_start to range_start + range_len - 1 do
                begin
                    Result := Result + units_local[range_idx];
                end;
            end;

        begin
            Result := 0;
            units_local := split_text_units(Trim(text_value));
            if (local_span_syllables < 2) or
                (Length(units_local) <> local_span_syllables) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            SetLength(occupied_local, local_span_syllables);
            for span_len_local := Min(c_quality_max_span,
                local_span_syllables) downto 2 do
            begin
                for offset_local := 0 to local_span_syllables - span_len_local do
                begin
                    if range_is_occupied_local(offset_local, span_len_local) then
                    begin
                        Continue;
                    end;

                    query_key_local := build_query_key_local(
                        local_start_idx + offset_local, span_len_local);
                    span_text_local := join_units_range_local(offset_local,
                        span_len_local);
                    if (query_key_local = '') or (span_text_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(query_key_local,
                        exact_results_local)) then
                    begin
                        Continue;
                    end;

                    for result_idx_local := 0 to High(exact_results_local) do
                    begin
                        if result_idx_local >= c_quality_probe_limit then
                        begin
                            Break;
                        end;
                        candidate_text_local :=
                            Trim(exact_results_local[result_idx_local].text);
                        if (Trim(exact_results_local[result_idx_local].comment) <> '') or
                            (not SameText(candidate_text_local, span_text_local)) or
                            (get_candidate_text_unit_count(candidate_text_local) <>
                            span_len_local) then
                        begin
                            Continue;
                        end;

                        candidate_weight_local :=
                            get_candidate_effective_weight_local(
                            exact_results_local[result_idx_local], span_len_local);
                        Inc(Result, (span_len_local * span_len_local * 1800) +
                            Min(12000, candidate_weight_local * 6) +
                            ((c_quality_probe_limit - result_idx_local) * 80));
                        mark_range_occupied_local(offset_local, span_len_local);
                        Break;
                    end;
                end;
            end;
        end;

        procedure collect_exact_single_char_candidates_local(
            const local_start_idx: Integer; const max_options: Integer;
            out out_texts: TArray<string>; out out_weights: TArray<Integer>);
        const
            c_single_probe_limit = 16;
        var
            query_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            candidate_is_preferred: Boolean;
            existing_idx: Integer;
            insert_idx: Integer;
            swap_text: string;
            swap_weight: Integer;
        begin
            SetLength(out_texts, 0);
            SetLength(out_weights, 0);
            if (max_options <= 0) or (m_dictionary = nil) or
                (local_start_idx < 0) or
                (local_start_idx > High(exact_subspan_cache_syllables)) then
            begin
                Exit;
            end;

            if exact_subspan_single_cache_prepared[local_start_idx] then
            begin
                out_texts := exact_subspan_single_cache_texts[local_start_idx];
                out_weights := exact_subspan_single_cache_weights[local_start_idx];
                Exit;
            end;

            query_key := normalize_pinyin_text(
                exact_subspan_cache_syllables[local_start_idx].text);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                local_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_single_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[result_idx].text);
                if (get_candidate_text_unit_count(candidate_text) <> 1) or
                    (not is_valid_cjk_phrase_text_local(candidate_text, 1)) then
                begin
                    Continue;
                end;
                if not match_single_char_candidate_for_syllable(
                    exact_subspan_cache_syllables[local_start_idx].text,
                    candidate_text, candidate_is_preferred) then
                begin
                    Continue;
                end;

                candidate_weight := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[result_idx].dict_weight;
                end;
                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));
                if candidate_is_preferred then
                begin
                    Inc(candidate_weight, 256);
                end;

                existing_idx := -1;
                for insert_idx := 0 to High(out_texts) do
                begin
                    if SameText(out_texts[insert_idx], candidate_text) then
                    begin
                        existing_idx := insert_idx;
                        Break;
                    end;
                end;

                if existing_idx >= 0 then
                begin
                    if candidate_weight <= out_weights[existing_idx] then
                    begin
                        Continue;
                    end;
                    out_weights[existing_idx] := candidate_weight;
                    insert_idx := existing_idx;
                end
                else
                begin
                    if Length(out_texts) < max_options then
                    begin
                        SetLength(out_texts, Length(out_texts) + 1);
                        SetLength(out_weights, Length(out_weights) + 1);
                        insert_idx := High(out_texts);
                        out_texts[insert_idx] := candidate_text;
                        out_weights[insert_idx] := candidate_weight;
                    end
                    else
                    begin
                        insert_idx := High(out_texts);
                        if (candidate_weight < out_weights[insert_idx]) or
                            ((candidate_weight = out_weights[insert_idx]) and
                            (CompareText(candidate_text, out_texts[insert_idx]) >= 0)) then
                        begin
                            Continue;
                        end;
                        out_texts[insert_idx] := candidate_text;
                        out_weights[insert_idx] := candidate_weight;
                    end;
                end;

                while (insert_idx > 0) and
                    ((out_weights[insert_idx] > out_weights[insert_idx - 1]) or
                    ((out_weights[insert_idx] = out_weights[insert_idx - 1]) and
                    (CompareText(out_texts[insert_idx], out_texts[insert_idx - 1]) < 0))) do
                begin
                    swap_text := out_texts[insert_idx - 1];
                    out_texts[insert_idx - 1] := out_texts[insert_idx];
                    out_texts[insert_idx] := swap_text;
                    swap_weight := out_weights[insert_idx - 1];
                    out_weights[insert_idx - 1] := out_weights[insert_idx];
                    out_weights[insert_idx] := swap_weight;
                    Dec(insert_idx);
                end;
            end;
            exact_subspan_single_cache_prepared[local_start_idx] := True;
            exact_subspan_single_cache_texts[local_start_idx] := out_texts;
            exact_subspan_single_cache_weights[local_start_idx] := out_weights;
        end;

        function normalize_long_sentence_chunk_weight_local(
            const raw_weight: Integer; const chunk_syllables_local: Integer): Integer;
        begin
            Result := raw_weight;
            case chunk_syllables_local of
                1:
                    if Result > 180 then
                    begin
                        Result := 180 + ((Result - 180) div 12);
                    end;
                2:
                    if Result > 240 then
                    begin
                        Result := 240 + ((Result - 240) div 10);
                    end;
            end;
        end;

        function is_preferred_phrase_match_local(const local_start_idx: Integer;
            const local_span_syllables: Integer; const preferred_text: string): Boolean;
        var
            preferred_units: TArray<string>;
            preferred_unit_idx: Integer;
            preferred_is_preferred: Boolean;
        begin
            Result := False;
            preferred_units := split_text_units(preferred_text);
            if Length(preferred_units) <> local_span_syllables then
            begin
                Exit;
            end;

            for preferred_unit_idx := 0 to High(preferred_units) do
            begin
                if not match_single_char_candidate_for_syllable(
                    exact_subspan_cache_syllables[local_start_idx + preferred_unit_idx].text,
                    preferred_units[preferred_unit_idx], preferred_is_preferred) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        procedure store_cache_result_local(const local_start_idx: Integer;
            const local_span_syllables: Integer; const resolved: Boolean;
            const resolved_text: string; const resolved_score: Integer;
            const resolved_segments: Integer; const resolved_path: string;
            const resolved_has_anchor: Boolean; const resolved_anchor_units: Integer);
        begin
            if (not exact_subspan_cache_prepared) or
                (m_disable_subspan_standalone_oracle > 0) then
            begin
                Exit;
            end;
            exact_subspan_cache_states[local_start_idx][local_span_syllables] :=
                IfThen(resolved, c_state_hit, c_state_miss);
            if resolved then
            begin
                exact_subspan_cache_scores[local_start_idx][local_span_syllables] :=
                    resolved_score;
                exact_subspan_cache_segments[local_start_idx][local_span_syllables] :=
                    resolved_segments;
                exact_subspan_cache_anchor_units[local_start_idx][local_span_syllables] :=
                    resolved_anchor_units;
                exact_subspan_cache_has_anchor[local_start_idx][local_span_syllables] :=
                    resolved_has_anchor;
                exact_subspan_cache_texts[local_start_idx][local_span_syllables] :=
                    resolved_text;
                exact_subspan_cache_paths[local_start_idx][local_span_syllables] :=
                    resolved_path;
            end;
        end;

        var
            best_scores: TArray<Integer>;
            best_segments: TArray<Integer>;
            best_anchor_units: TArray<Integer>;
            best_has_anchor: TArray<Boolean>;
        best_texts: TArray<string>;
        best_paths: TArray<string>;
        pos: Integer;
        global_idx: Integer;
        chunk_syllables: Integer;
        next_pos: Integer;
        query_key: string;
        candidate_text: string;
        candidate_units: Integer;
        fixed_text: string;
        fixed_weight: Integer;
        single_text_options: TArray<string>;
        single_weight_options: TArray<Integer>;
        single_option_idx: Integer;
        chunk_text_options: TArray<string>;
        chunk_weight_options: TArray<Integer>;
        chunk_option_idx: Integer;
        preferred_text: string;
        preferred_bonus: Integer;
        total_score: Integer;
        total_segments: Integer;
        total_anchor_units: Integer;
        total_has_anchor: Boolean;
        combined_text: string;
        combined_path: string;
        better_candidate: Boolean;
        local_has_anchor: Boolean;
        direct_text: string;
            direct_score: Integer;
            direct_has_anchor: Boolean;
            direct_anchor_units: Integer;
        direct_exact_bonus: Integer;
        direct_units: TArray<string>;
        best_units: TArray<string>;
        same_first_unit_local: Boolean;
        oracle_span_quality_local: Integer;
        current_span_quality_local: Integer;
        boundary_resolved_text: string;
        boundary_resolved_score: Integer;
        boundary_resolved_has_anchor: Boolean;
        boundary_resolved_anchor_units: Integer;
        boundary_preferred_bonus: Integer;
        current_preferred_bonus_local: Integer;
        suffix_first_unit_local: string;
        suffix_first_segment_local: string;
        suffix_first_segment_units_local: Integer;
        expected_pair_text_local: string;
        actual_pair_text_local: string;
        expected_pair_weight_local: Integer;
        boundary_resolved_local: Boolean;

        function get_first_text_unit_for_guard_local(const text_value: string): string;
        var
            text_units_local: TArray<string>;
        begin
            Result := '';
            text_units_local := split_text_units(Trim(text_value));
            if Length(text_units_local) > 0 then
            begin
                Result := text_units_local[0];
            end;
        end;

        function should_skip_weak_fixed_head_exact_local(
            const local_start_idx: Integer; const local_chunk_syllables: Integer;
            const candidate_text_value: string; const candidate_weight_value: Integer;
            const candidate_source_value: TncCandidateSource): Boolean;
        var
            fixed_head_text_local: string;
            candidate_head_text_local: string;
        begin
            Result := False;
            if (local_chunk_syllables <> 2) or
                (local_start_idx < 0) or
                (local_start_idx > High(exact_subspan_cache_syllables)) or
                (candidate_source_value = cs_user) or
                (candidate_weight_value >= c_weak_fixed_head_exact_weight) then
            begin
                Exit;
            end;

            fixed_head_text_local := get_fixed_boundary_single_char_local(
                exact_subspan_cache_syllables[local_start_idx].text);
            if fixed_head_text_local = '' then
            begin
                Exit;
            end;

            candidate_head_text_local :=
                get_first_text_unit_for_guard_local(candidate_text_value);
            Result := (candidate_head_text_local <> '') and
                (not SameText(candidate_head_text_local, fixed_head_text_local));
        end;

        procedure try_update_with_candidate(const local_pos: Integer;
            const local_chunk_syllables: Integer; const chunk_text: string;
            const base_weight: Integer);
        begin
            candidate_text := Trim(chunk_text);
            if candidate_text = '' then
            begin
                Exit;
            end;
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <> local_chunk_syllables then
            begin
                Exit;
            end;

            next_pos := local_pos + local_chunk_syllables;
            total_score := base_weight +
                (local_chunk_syllables * local_chunk_syllables * c_chunk_length_bonus);
            local_has_anchor := local_chunk_syllables >= 3;
            if local_has_anchor then
            begin
                Inc(total_score, c_chunk_anchor_bonus);
            end;

            total_segments := 1;
            total_has_anchor := local_has_anchor;
            if local_has_anchor then
            begin
                total_anchor_units := local_chunk_syllables;
            end
            else
            begin
                total_anchor_units := 0;
            end;
            combined_text := candidate_text;
            combined_path := candidate_text;

            if next_pos < span_syllables then
            begin
                Inc(total_score, best_scores[next_pos]);
                Inc(total_score, get_suffix_context_bonus_local(candidate_text,
                    best_paths[next_pos]));
                Inc(total_score, get_suffix_base_prefix_bonus_local(candidate_text,
                    best_paths[next_pos]));
                Inc(total_segments, best_segments[next_pos]);
                if best_has_anchor[next_pos] then
                begin
                    total_has_anchor := True;
                    if best_anchor_units[next_pos] > total_anchor_units then
                    begin
                        total_anchor_units := best_anchor_units[next_pos];
                    end;
                end;
                combined_text := combined_text + best_texts[next_pos];
                if best_paths[next_pos] <> '' then
                begin
                    combined_path := combined_path + c_segment_path_separator +
                        best_paths[next_pos];
                end;
            end;

            if (local_chunk_syllables = 1) and
                (next_pos < span_syllables) and
                (best_paths[next_pos] <> '') then
            begin
                suffix_first_unit_local :=
                    get_first_path_text_unit_local(best_paths[next_pos]);
                if (suffix_first_unit_local <> '') and
                    try_get_best_exact_two_unit_text_local(start_idx + local_pos,
                    expected_pair_text_local, expected_pair_weight_local) then
                begin
                    actual_pair_text_local := candidate_text +
                        suffix_first_unit_local;
                    if SameText(expected_pair_text_local,
                        actual_pair_text_local) then
                    begin
                        Inc(total_score, 1200 +
                            Min(4800, expected_pair_weight_local div 4));
                    end
                else
                begin
                    suffix_first_segment_local :=
                        get_first_path_segment_text_local(best_paths[next_pos]);
                    suffix_first_segment_units_local :=
                        get_candidate_text_unit_count(suffix_first_segment_local);
                    if is_structural_particle_text_local(candidate_text) and
                        (suffix_first_segment_units_local >= 2) then
                    begin
                        Inc(total_score, 1800);
                    end
                    else
                    begin
                        Dec(total_score, 24000 +
                            Min(48000, expected_pair_weight_local * 8));
                    end;
                end;
            end;
            end;

            better_candidate := False;
            if total_score > best_scores[local_pos] then
            begin
                better_candidate := True;
            end
            else if total_score = best_scores[local_pos] then
            begin
                if total_has_anchor and (not best_has_anchor[local_pos]) then
                begin
                    better_candidate := True;
                end
                else if total_has_anchor = best_has_anchor[local_pos] then
                begin
                    if total_anchor_units > best_anchor_units[local_pos] then
                    begin
                        better_candidate := True;
                    end
                    else if total_anchor_units = best_anchor_units[local_pos] then
                    begin
                        if total_segments < best_segments[local_pos] then
                        begin
                            better_candidate := True;
                        end
                        else if (total_segments = best_segments[local_pos]) and
                            ((best_texts[local_pos] = '') or
                            (CompareText(combined_text, best_texts[local_pos]) < 0)) then
                        begin
                            better_candidate := True;
                        end;
                    end;
                end;
            end;

            if better_candidate then
            begin
                best_scores[local_pos] := total_score;
                best_segments[local_pos] := total_segments;
                best_anchor_units[local_pos] := total_anchor_units;
                best_has_anchor[local_pos] := total_has_anchor;
                best_texts[local_pos] := combined_text;
                best_paths[local_pos] := combined_path;
            end;
        end;

        procedure collect_exact_chunk_candidates_local(
            const local_start_idx: Integer;
            const local_chunk_syllables: Integer;
            out out_texts: TArray<string>; out out_weights: TArray<Integer>);
        var
            query_key_local: string;
            local_results: TncCandidateList;
            local_result_idx: Integer;
            candidate_text_local: string;
            raw_weight_local: Integer;
            exact_bonus_local: Integer;
            option_idx_local: Integer;
        begin
            SetLength(out_texts, 0);
            SetLength(out_weights, 0);
            if (m_dictionary = nil) or (local_start_idx < 0) or
                (local_start_idx > High(exact_subspan_cache_syllables)) or
                (local_chunk_syllables < 2) or
                (local_chunk_syllables > c_span_max_syllables) then
            begin
                Exit;
            end;

            if exact_subspan_chunk_cache_prepared[local_start_idx][
                local_chunk_syllables] then
            begin
                out_texts := exact_subspan_chunk_cache_texts[
                    local_start_idx][local_chunk_syllables];
                out_weights := exact_subspan_chunk_cache_weights[
                    local_start_idx][local_chunk_syllables];
                Exit;
            end;
            exact_subspan_chunk_cache_prepared[local_start_idx][
                local_chunk_syllables] := True;

            query_key_local := build_query_key_local(local_start_idx,
                local_chunk_syllables);
            if query_key_local = '' then
            begin
                Exit;
            end;
            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                if (local_chunk_syllables > 2) or
                    (not m_dictionary.lookup(query_key_local, local_results)) then
                begin
                    Exit;
                end;
            end;

            for local_result_idx := 0 to High(local_results) do
            begin
                if local_result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(local_results[local_result_idx]) or
                    is_runtime_common_pattern_candidate(
                    local_results[local_result_idx]) or
                    is_runtime_redup_candidate(local_results[local_result_idx]) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(
                    local_results[local_result_idx].text);
                if get_candidate_text_unit_count(candidate_text_local) <>
                    local_chunk_syllables then
                begin
                    Continue;
                end;
                if not is_valid_cjk_phrase_text_local(candidate_text_local,
                    local_chunk_syllables) then
                begin
                    Continue;
                end;

                raw_weight_local := get_candidate_effective_weight_local(
                    local_results[local_result_idx], local_chunk_syllables);
                exact_bonus_local := get_preferred_exact_subspan_bonus_local(
                    query_key_local, candidate_text_local);
                Inc(raw_weight_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                if should_skip_weak_fixed_head_exact_local(local_start_idx,
                    local_chunk_syllables, candidate_text_local,
                    raw_weight_local, local_results[local_result_idx].source) then
                begin
                    Continue;
                end;
                Inc(raw_weight_local, exact_bonus_local);
                if (local_chunk_syllables >= 3) and
                    (exact_bonus_local > 0) then
                begin
                    Inc(raw_weight_local, 12000);
                end;
                raw_weight_local := normalize_long_sentence_chunk_weight_local(
                    raw_weight_local, local_chunk_syllables);

                option_idx_local := Length(out_texts);
                SetLength(out_texts, option_idx_local + 1);
                SetLength(out_weights, option_idx_local + 1);
                out_texts[option_idx_local] := candidate_text_local;
                out_weights[option_idx_local] := raw_weight_local;
            end;

            exact_subspan_chunk_cache_texts[local_start_idx][
                local_chunk_syllables] := out_texts;
            exact_subspan_chunk_cache_weights[local_start_idx][
                local_chunk_syllables] := out_weights;
        end;

    begin
        out_text := '';
        out_score := 0;
        out_segments := 0;
        out_path := '';
        out_has_anchor := False;
        out_anchor_units := -1;
        if (span_syllables <= 0) or (span_syllables > c_span_max_syllables) then
        begin
            Exit(False);
        end;
        if (start_idx < 0) or
            (start_idx + span_syllables - 1 >= input_syllable_count) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit(False);
        end;

        prepare_exact_subspan_cache_local;
        if not exact_subspan_cache_prepared then
        begin
            Exit(False);
        end;
        if (start_idx > High(exact_subspan_cache_syllables)) or
            (exact_subspan_cache_states = nil) then
        begin
            Exit(False);
        end;

        if exact_subspan_cache_states[start_idx][span_syllables] = c_state_hit then
        begin
            out_text := exact_subspan_cache_texts[start_idx][span_syllables];
            out_score := exact_subspan_cache_scores[start_idx][span_syllables];
            out_segments := exact_subspan_cache_segments[start_idx][span_syllables];
            out_path := exact_subspan_cache_paths[start_idx][span_syllables];
            out_has_anchor := exact_subspan_cache_has_anchor[start_idx][span_syllables];
            out_anchor_units := exact_subspan_cache_anchor_units[start_idx][span_syllables];
            Exit(True);
        end;
        if exact_subspan_cache_states[start_idx][span_syllables] = c_state_miss then
        begin
            Exit(False);
        end;

        SetLength(best_scores, span_syllables + 1);
        SetLength(best_segments, span_syllables + 1);
        SetLength(best_anchor_units, span_syllables + 1);
        SetLength(best_has_anchor, span_syllables + 1);
        SetLength(best_texts, span_syllables + 1);
        SetLength(best_paths, span_syllables + 1);
        for pos := 0 to span_syllables do
        begin
            best_scores[pos] := Low(Integer);
            best_segments[pos] := MaxInt;
            best_anchor_units[pos] := -1;
            best_has_anchor[pos] := False;
            best_texts[pos] := '';
            best_paths[pos] := '';
        end;
        best_scores[span_syllables] := 0;
        best_segments[span_syllables] := 0;
        best_anchor_units[span_syllables] := -1;
        best_has_anchor[span_syllables] := False;
        best_texts[span_syllables] := '';
        best_paths[span_syllables] := '';

        for pos := span_syllables - 1 downto 0 do
        begin
            global_idx := start_idx + pos;
            fixed_text := get_fixed_boundary_single_char_local(
                exact_subspan_cache_syllables[global_idx].text);
            if (fixed_text <> '') and ((pos + 1 = span_syllables) or
                (best_scores[pos + 1] > Low(Integer))) then
            begin
                if not try_get_exact_single_char_weight_local(global_idx, fixed_text,
                    fixed_weight) then
                begin
                    fixed_weight := 520;
                end;
                Inc(fixed_weight, get_preferred_query_phrase_bonus_local(
                    normalize_pinyin_text(exact_subspan_cache_syllables[global_idx].text),
                    fixed_text));
                try_update_with_candidate(pos, 1, fixed_text,
                    normalize_long_sentence_chunk_weight_local(
                    fixed_weight + c_fixed_single_bonus, 1));
            end;

            if ((pos + 1 = span_syllables) or (best_scores[pos + 1] > Low(Integer))) then
            begin
                collect_exact_single_char_candidates_local(global_idx, 6,
                    single_text_options, single_weight_options);
                for single_option_idx := 0 to High(single_text_options) do
                begin
                    if SameText(single_text_options[single_option_idx], fixed_text) then
                    begin
                        Continue;
                    end;
                    try_update_with_candidate(pos, 1,
                        single_text_options[single_option_idx],
                        normalize_long_sentence_chunk_weight_local(
                        single_weight_options[single_option_idx], 1));
                end;
            end;

            for chunk_syllables := 2 to Min(c_span_max_syllables, span_syllables - pos) do
            begin
                next_pos := pos + chunk_syllables;
                if (next_pos < span_syllables) and
                    (best_scores[next_pos] = Low(Integer)) then
                begin
                    Continue;
                end;

                query_key := build_query_key_local(global_idx, chunk_syllables);
                if query_key = '' then
                begin
                    Continue;
                end;

                preferred_text := '';
                preferred_bonus := 0;
                if try_get_preferred_query_phrase_text_local(query_key, preferred_text,
                    preferred_bonus) and
                    is_valid_cjk_phrase_text_local(preferred_text,
                    chunk_syllables) then
                begin
                    try_update_with_candidate(pos, chunk_syllables, preferred_text,
                        preferred_bonus + 12000);
                end;

                collect_exact_chunk_candidates_local(global_idx,
                    chunk_syllables, chunk_text_options,
                    chunk_weight_options);
                for chunk_option_idx := 0 to High(chunk_text_options) do
                begin
                    try_update_with_candidate(pos, chunk_syllables,
                        chunk_text_options[chunk_option_idx],
                        chunk_weight_options[chunk_option_idx]);
                end;
            end;
        end;

        if (best_scores[0] = Low(Integer)) or (best_texts[0] = '') then
        begin
            store_cache_result_local(start_idx, span_syllables, False, '', 0, 0, '',
                False, -1);
            Exit(False);
        end;
        if try_get_best_direct_exact_candidate_local(start_idx, span_syllables,
            direct_text, direct_score, direct_has_anchor, direct_anchor_units,
            direct_exact_bonus) and
            (direct_text <> '') and
            ((best_segments[0] > 1) or
            ((direct_exact_bonus > 0) and (not SameText(direct_text, best_texts[0])))) then
        begin
            same_first_unit_local := False;
            if span_syllables = 2 then
            begin
                direct_units := split_text_units(direct_text);
                best_units := split_text_units(best_texts[0]);
                same_first_unit_local := (Length(direct_units) > 0) and
                    (Length(best_units) > 0) and
                    SameText(direct_units[0], best_units[0]);
            end;

            if SameText(direct_text, best_texts[0]) then
            begin
                // Keep the DP result when it already resolves to the same text.
            end
            else if (direct_exact_bonus > 0) or
                ((span_syllables = 2) and
                (best_segments[0] > 1) and
                (direct_score >= best_scores[0] - 512) and
                same_first_unit_local) or
                ((best_segments[0] >= 3) and
                (direct_score >= best_scores[0] + 2048)) then
            begin
                best_texts[0] := direct_text;
                best_scores[0] := direct_score;
                best_segments[0] := 1;
                best_has_anchor[0] := direct_has_anchor;
                best_anchor_units[0] := direct_anchor_units;
                best_paths[0] := direct_text;
            end;
        end;
        query_key := build_query_key_local(start_idx, span_syllables);

        boundary_resolved_local := False;
        if span_syllables = 3 then
        begin
            boundary_resolved_local :=
                try_get_best_three_syllable_fixed_boundary_compound_local(
                start_idx, boundary_resolved_text, boundary_resolved_score,
                boundary_resolved_has_anchor, boundary_resolved_anchor_units);
        end;
        if boundary_resolved_local and
            (boundary_resolved_text <> '') then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' tfix3=[text=%s score=%d]',
                    [boundary_resolved_text, boundary_resolved_score]);
            end;
            current_preferred_bonus_local := 0;
            if (query_key <> '') and (best_texts[0] <> '') then
            begin
                current_preferred_bonus_local :=
                    get_preferred_exact_subspan_bonus_local(query_key,
                    best_texts[0]);
            end;
            boundary_preferred_bonus := 0;
            if query_key <> '' then
            begin
                boundary_preferred_bonus :=
                    get_preferred_exact_subspan_bonus_local(query_key,
                    boundary_resolved_text);
            end;

            if (best_texts[0] = '') or
                SameText(boundary_resolved_text, best_texts[0]) or
                ((best_segments[0] > 1) and
                (boundary_resolved_score >= best_scores[0] - 1024)) or
                ((not best_has_anchor[0]) and
                (boundary_resolved_score >= best_scores[0] - 512)) or
                ((boundary_preferred_bonus > current_preferred_bonus_local) and
                (boundary_resolved_score >= best_scores[0] - 2048)) or
                (boundary_resolved_score > best_scores[0] + 2048) then
            begin
                best_texts[0] := boundary_resolved_text;
                best_scores[0] := Max(best_scores[0], boundary_resolved_score);
                best_segments[0] := 1;
                best_has_anchor[0] := boundary_resolved_has_anchor;
                best_anchor_units[0] := Max(best_anchor_units[0],
                    boundary_resolved_anchor_units);
                best_paths[0] := boundary_resolved_text;
            end;
        end;

        oracle_preferred_text := '';
        oracle_preferred_bonus := 0;
        if query_key <> '' then
        begin
            oracle_preferred_text := '';
            oracle_preferred_bonus := 0;
            try_get_preferred_query_phrase_text_local(query_key,
                oracle_preferred_text, oracle_preferred_bonus);
            standalone_promote_bonus_local := 4096;
            if (span_syllables >= 4) and
                (start_idx + span_syllables >= input_syllable_count - 1) then
            begin
                standalone_promote_bonus_local := 49152;
            end
            else if span_syllables >= 6 then
            begin
                standalone_promote_bonus_local := 32768;
            end
            else if subspan_has_fixed_boundary_local(start_idx, span_syllables) then
            begin
                standalone_promote_bonus_local := 16384;
            end;
            if ((((span_syllables >= 4) and
                (start_idx + span_syllables >= input_syllable_count - 1)) or
                (span_syllables >= 6) or
                subspan_has_fixed_boundary_local(start_idx, span_syllables)) and
                try_get_best_subspan_standalone_query_candidate_local(query_key,
                span_syllables, direct_text, direct_score)) and
                (direct_text <> '') then
            begin
                direct_exact_bonus := get_preferred_exact_subspan_bonus_local(
                    query_key, direct_text);
                oracle_span_quality_local :=
                    get_exact_span_coverage_quality_local(start_idx,
                    span_syllables, direct_text);
                current_span_quality_local :=
                    get_exact_span_coverage_quality_local(start_idx,
                    span_syllables, best_texts[0]);
                if (direct_exact_bonus <= 0) and
                    (current_span_quality_local > 0) and
                    (current_span_quality_local >=
                    oracle_span_quality_local + 1800) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' oraclekeep=[curq=%d oraq=%d]',
                            [current_span_quality_local,
                            oracle_span_quality_local]);
                    end;
                end
                else if (best_texts[0] = '') or
                    SameText(direct_text, best_texts[0]) or
                    (direct_exact_bonus > 0) or
                    (best_segments[0] > 1) or
                    ((span_syllables >= 4) and
                    (direct_score + standalone_promote_bonus_local >=
                    best_scores[0] - 512)) or
                    (direct_score + standalone_promote_bonus_local >=
                    best_scores[0] + 512) or
                    ((not best_has_anchor[0]) and
                    (direct_score + standalone_promote_bonus_local >=
                    best_scores[0] - 1024)) then
                begin
                    best_texts[0] := direct_text;
                    best_scores[0] := Max(best_scores[0],
                        direct_score + standalone_promote_bonus_local);
                    best_segments[0] := 1;
                    best_has_anchor[0] := True;
                    best_anchor_units[0] := span_syllables;
                    best_paths[0] := direct_text;
                end;
            end;
        end;

        out_text := best_texts[0];
        out_score := best_scores[0];
        out_segments := best_segments[0];
        out_path := best_paths[0];
        out_has_anchor := best_has_anchor[0];
        out_anchor_units := best_anchor_units[0];
        store_cache_result_local(start_idx, span_syllables, True, out_text, out_score,
            out_segments, out_path, out_has_anchor, out_anchor_units);
        Result := True;
    end;

    procedure promote_preferred_query_phrase_candidate_for_query(
        const query_key: string; var candidates: TncCandidateList);
    var
        idx: Integer;
        best_idx: Integer;
        best_bonus: Integer;
        current_top_bonus: Integer;
        best_is_complete: Boolean;
        current_is_complete: Boolean;
        tmp: TncCandidate;
        candidate_text: string;
        preferred_text: string;
        preferred_bonus: Integer;
        preferred_units: TArray<string>;
        query_syllables: TncPinyinParseResult;
        can_insert_preferred: Boolean;
    begin
        if (Length(candidates) = 0) or (query_key = '') then
        begin
            Exit;
        end;

        preferred_text := '';
        preferred_bonus := 0;
        query_syllables := get_effective_compact_pinyin_syllables(query_key);
        can_insert_preferred :=
            try_get_preferred_query_phrase_text_local(query_key,
            preferred_text, preferred_bonus);
        if can_insert_preferred then
        begin
            preferred_units := split_text_units(preferred_text);
            can_insert_preferred :=
                (Length(query_syllables) > 0) and
                (Length(preferred_units) = Length(query_syllables)) and
                is_full_pinyin_key(query_key);
        end;

        best_idx := -1;
        best_bonus := 0;
        best_is_complete := False;
        current_top_bonus := get_preferred_query_phrase_bonus_local(query_key,
            Trim(candidates[0].text));

        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text := Trim(candidates[idx].text);
            if candidate_text = '' then
            begin
                Continue;
            end;

            current_is_complete := Pos('/', candidate_text) = 0;
            if idx = 0 then
            begin
                best_is_complete := current_is_complete;
            end;

            if get_preferred_query_phrase_bonus_local(query_key, candidate_text) >
                best_bonus then
            begin
                best_idx := idx;
                best_bonus := get_preferred_query_phrase_bonus_local(query_key,
                    candidate_text);
                best_is_complete := current_is_complete;
                Continue;
            end;

            if (best_idx >= 0) and
                (get_preferred_query_phrase_bonus_local(query_key, candidate_text) =
                best_bonus) and (best_bonus > 0) then
            begin
                if current_is_complete and (not best_is_complete) then
                begin
                    best_idx := idx;
                    best_is_complete := True;
                end
                else if current_is_complete = best_is_complete then
                begin
                    if get_candidate_text_unit_count(candidate_text) >
                        get_candidate_text_unit_count(Trim(candidates[best_idx].text)) then
                    begin
                        best_idx := idx;
                    end;
                end;
            end;
        end;

        if (best_idx < 0) and can_insert_preferred and (preferred_bonus > 0) then
        begin
            if SameText(Trim(candidates[0].text), preferred_text) then
            begin
                Exit;
            end;

            FillChar(tmp, SizeOf(tmp), 0);
            tmp.text := preferred_text;
            tmp.comment := '';
            tmp.source := cs_rule;
            tmp.has_dict_weight := False;
            tmp.dict_weight := 0;
            tmp.score := Max(candidates[0].score + 2048,
                preferred_bonus + 50000);

            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := tmp;
            Exit;
        end;

        if (best_idx <= 0) or (best_bonus <= 0) then
        begin
            Exit;
        end;

        if best_bonus < current_top_bonus then
        begin
            Exit;
        end;

        tmp := candidates[best_idx];
        for idx := best_idx downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := tmp;
    end;

    function should_use_delayed_long_decode_mode_local: Boolean;
    begin
        Result := m_config.enable_segment_candidates and has_multi_syllable_input and
            (input_syllable_count >= c_long_sentence_full_path_min_syllables) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input);
    end;

    function has_extendable_same_syllable_tail_local(const pinyin_key: string;
        const expected_syllable_count: Integer): Boolean;
    var
        suffix_ch: Char;
        extended_key: string;
    begin
        Result := False;
        if extendable_same_tail_cache_valid and
            SameText(extendable_same_tail_cache_key, pinyin_key) and
            (extendable_same_tail_cache_expected_count = expected_syllable_count) then
        begin
            Exit(extendable_same_tail_cache_value);
        end;

        extendable_same_tail_cache_valid := True;
        extendable_same_tail_cache_key := pinyin_key;
        extendable_same_tail_cache_expected_count := expected_syllable_count;
        extendable_same_tail_cache_value := False;

        if (pinyin_key = '') or (expected_syllable_count < 4) then
        begin
            Exit;
        end;

        for suffix_ch := 'a' to 'z' do
        begin
            extended_key := pinyin_key + suffix_ch;
            if get_effective_compact_pinyin_unit_count(extended_key) =
                expected_syllable_count then
            begin
                extendable_same_tail_cache_value := True;
                Exit(True);
            end;
        end;
    end;

    function has_precise_extendable_tail_syllable_local(
        const pinyin_key: string): Boolean;
    var
        syllables_local: TncPinyinParseResult;
        tail_text_local: string;
        suffix_ch_local: Char;
        extended_tail_local: string;
    begin
        Result := False;
        if (pinyin_key = '') or (not is_full_pinyin_key(pinyin_key)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(pinyin_key);
        if Length(syllables_local) = 0 then
        begin
            Exit;
        end;

        tail_text_local := normalize_pinyin_text(
            syllables_local[High(syllables_local)].text);
        if tail_text_local = '' then
        begin
            Exit;
        end;

        for suffix_ch_local := 'a' to 'z' do
        begin
            extended_tail_local := tail_text_local + suffix_ch_local;
            if is_full_pinyin_key(extended_tail_local) and
                (get_effective_compact_pinyin_unit_count(
                extended_tail_local) = 1) then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_short_extendable_tail_prefix_local: Boolean;
    var
        syllables_local: TncPinyinParseResult;
        tail_text_local: string;
        suffix_ch_local: Char;
        extended_key_local: string;
    begin
        Result := False;
        if short_extendable_tail_cache_valid then
        begin
            Exit(short_extendable_tail_cache_value);
        end;

        short_extendable_tail_cache_valid := True;
        short_extendable_tail_cache_value := False;

        if has_safe_trailing_initial_typing_state then
        begin
            short_extendable_tail_cache_value := True;
            Exit(True);
        end;

        if (not is_full_pinyin_key(lookup_text)) or
            (lookup_text = '') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <= 0 then
        begin
            Exit;
        end;

        tail_text_local := normalize_pinyin_text(
            syllables_local[High(syllables_local)].text);
        if (tail_text_local = '') or (Length(tail_text_local) > 2) then
        begin
            Exit;
        end;

        for suffix_ch_local := 'a' to 'z' do
        begin
            extended_key_local := lookup_text + suffix_ch_local;
            if get_effective_compact_pinyin_unit_count(extended_key_local) =
                Length(syllables_local) then
            begin
                short_extendable_tail_cache_value := True;
                Exit(True);
            end;
        end;
    end;

    function has_complete_exact_tail_phrase_local(
        const tail_syllable_count: Integer): Boolean;
    const
        c_min_tail_exact_weight_for_no_defer = 700;
    var
        syllables_local: TncPinyinParseResult;
        tail_key_local: string;
        idx_local: Integer;
        exact_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
        candidate_weight_local: Integer;
    begin
        Result := False;
        if (m_dictionary = nil) or (tail_syllable_count < 2) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < tail_syllable_count then
        begin
            Exit;
        end;

        tail_key_local := '';
        for idx_local := Length(syllables_local) - tail_syllable_count to
            High(syllables_local) do
        begin
            tail_key_local := tail_key_local +
                normalize_pinyin_text(syllables_local[idx_local].text);
        end;
        if tail_key_local = '' then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(tail_key_local,
            exact_results_local) then
        begin
            Exit;
        end;

        for result_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(exact_results_local[result_idx_local].text);
            if (candidate_text_local <> '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                tail_syllable_count) then
            begin
                if (not m_dictionary.is_base_entry(tail_key_local,
                    candidate_text_local)) and
                    (not m_dictionary.is_user_entry(tail_key_local,
                    candidate_text_local)) then
                begin
                    Continue;
                end;
                if is_problematic_single_char_chain_candidate_for_query(
                    tail_key_local, exact_results_local[result_idx_local]) then
                begin
                    Continue;
                end;
                candidate_weight_local := exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        exact_results_local[result_idx_local].dict_weight;
                end;
                if candidate_weight_local >= c_min_tail_exact_weight_for_no_defer then
                begin
                    Exit(True);
                end;
            end;
        end;
    end;

    function is_place_context_tail_unit_local(
        const unit_value_local: string): Boolean;
    begin
        Result := False;
        if Length(unit_value_local) <> 1 then
        begin
            Exit;
        end;
        case Ord(unit_value_local[1]) of
            $5BB6, $5E97, $5C4B, $697C, $57CE, $6751, $53BF, $5E02,
            $6821, $9662, $5382, $9986, $7AD9, $5C40, $6240, $56ED,
            $533A, $5708, $8DEF, $8857, $95E8, $5BA4, $623F, $5385,
            $573A, $53F8:
                Result := True;
        end;
    end;

    function has_contextual_directional_exact_tail_candidate_local: Boolean;
    const
        c_context_probe_limit = 8;
    var
        syllables_local: TncPinyinParseResult;
        tail_key_local: string;
        tail_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
        has_directional_tail_local: Boolean;
        back_len_local: Integer;
        context_key_local: string;
        context_results_local: TncCandidateList;
        context_text_local: string;
        context_units_local: TArray<string>;
        context_idx_local: Integer;
        syllable_idx_local: Integer;

        function is_directional_locative_text_local(
            const text_value_local: string): Boolean;
        var
            units_local: TArray<string>;
            head_unit_local: string;
            tail_unit_local: string;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value_local));
            if Length(units_local) <> 2 then
            begin
                Exit;
            end;
            head_unit_local := units_local[0];
            tail_unit_local := units_local[1];
            Result := ((head_unit_local = string(Char($4E1C))) or
                (head_unit_local = string(Char($5357))) or
                (head_unit_local = string(Char($897F))) or
                (head_unit_local = string(Char($5317))) or
                (head_unit_local = string(Char($4E0A))) or
                (head_unit_local = string(Char($4E0B))) or
                (head_unit_local = string(Char($524D))) or
                (head_unit_local = string(Char($540E))) or
                (head_unit_local = string(Char($91CC))) or
                (head_unit_local = string(Char($5916))) or
                (head_unit_local = string(Char($5DE6))) or
                (head_unit_local = string(Char($53F3)))) and
                ((tail_unit_local = string(Char($9762))) or
                (tail_unit_local = string(Char($8FB9))) or
                (tail_unit_local = string(Char($65B9))) or
                (tail_unit_local = string(Char($4FA7))) or
                (tail_unit_local = string(Char($90E8))));
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;

        tail_key_local := '';
        for syllable_idx_local := Length(syllables_local) - 2 to
            High(syllables_local) do
        begin
            tail_key_local := tail_key_local + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
        end;
        if (tail_key_local = '') or
            (not lookup_exact_full_pinyin_cached_local(tail_key_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        has_directional_tail_local := False;
        for result_idx_local := 0 to Min(c_context_probe_limit - 1,
            High(tail_results_local)) do
        begin
            candidate_text_local := Trim(tail_results_local[result_idx_local].text);
            if (Trim(tail_results_local[result_idx_local].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) = 2) and
                is_directional_locative_text_local(candidate_text_local) then
            begin
                has_directional_tail_local := True;
                Break;
            end;
        end;
        if not has_directional_tail_local then
        begin
            Exit;
        end;

        for back_len_local := 1 to Min(4, Length(syllables_local) - 2) do
        begin
            context_key_local := '';
            for syllable_idx_local := Length(syllables_local) - 2 -
                back_len_local to Length(syllables_local) - 3 do
            begin
                context_key_local := context_key_local + normalize_pinyin_text(
                    syllables_local[syllable_idx_local].text);
            end;
            if (context_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(context_key_local,
                context_results_local)) then
            begin
                Continue;
            end;
            for context_idx_local := 0 to Min(c_context_probe_limit - 1,
                High(context_results_local)) do
            begin
                if Trim(context_results_local[context_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                context_text_local := Trim(
                    context_results_local[context_idx_local].text);
                context_units_local := split_text_units(context_text_local);
                if (Length(context_units_local) > 0) and
                    is_place_context_tail_unit_local(
                    context_units_local[High(context_units_local)]) then
                begin
                    Exit(True);
                end;
            end;
        end;
    end;

    function has_reliable_exact_tail_candidate_local(
        const tail_syllable_count: Integer): Boolean;
    const
        c_min_reliable_tail_weight = 560;
    var
        syllables_local: TncPinyinParseResult;
        tail_key_local: string;
        idx_local: Integer;
        exact_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
        candidate_weight_local: Integer;
    begin
        Result := False;
        if (m_dictionary = nil) or (tail_syllable_count < 2) or
            (tail_syllable_count > 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < tail_syllable_count then
        begin
            Exit;
        end;

        tail_key_local := '';
        for idx_local := Length(syllables_local) - tail_syllable_count to
            High(syllables_local) do
        begin
            tail_key_local := tail_key_local + normalize_pinyin_text(
                syllables_local[idx_local].text);
        end;
        if (tail_key_local = '') or
            (not lookup_exact_full_pinyin_cached_local(tail_key_local,
            exact_results_local)) then
        begin
            Exit;
        end;

        for result_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(
                exact_results_local[result_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                tail_syllable_count) then
            begin
                Continue;
            end;
            candidate_weight_local := exact_results_local[result_idx_local].score;
            if exact_results_local[result_idx_local].has_dict_weight and
                (exact_results_local[result_idx_local].dict_weight >
                candidate_weight_local) then
            begin
                candidate_weight_local :=
                    exact_results_local[result_idx_local].dict_weight;
            end;
            Inc(candidate_weight_local, get_preferred_query_phrase_bonus_local(
                tail_key_local, candidate_text_local));
            if candidate_weight_local >= c_min_reliable_tail_weight then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_preferred_tail_phrase_local(
        const tail_syllable_count: Integer): Boolean;
    var
        syllables_local: TncPinyinParseResult;
        tail_key_local: string;
        idx_local: Integer;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
    begin
        Result := False;
        if (tail_syllable_count < 2) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < tail_syllable_count then
        begin
            Exit;
        end;

        tail_key_local := '';
        for idx_local := Length(syllables_local) - tail_syllable_count to
            High(syllables_local) do
        begin
            tail_key_local := tail_key_local +
                normalize_pinyin_text(syllables_local[idx_local].text);
        end;

        Result := (tail_key_local <> '') and
            try_get_preferred_query_phrase_text_local(tail_key_local,
            preferred_text_local, preferred_bonus_local) and
            (Length(split_text_units(preferred_text_local)) =
            tail_syllable_count);
    end;

    function should_defer_exact_chain_for_extendable_tail_local: Boolean;
    begin
        Result := has_multi_syllable_input and (input_syllable_count >= 7) and
            is_full_pinyin_key(lookup_text) and (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            (not has_preferred_tail_phrase_local(2)) and
            (not has_complete_exact_tail_phrase_local(2)) and
            (not has_preferred_tail_phrase_local(3)) and
            (not has_complete_exact_tail_phrase_local(3));
    end;

    function has_complete_top_candidate_for_fast_exit_local(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_text: string;
        candidate_path: string;
        top_candidate_local: TncCandidate;
        support_score_local: Integer;
        support_segments_local: Integer;
        inferred_path_local: string;
    begin
        Result := False;
        if (Length(candidates) = 0) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        if Trim(candidates[0].comment) <> '' then
        begin
            Exit;
        end;

        candidate_text := Trim(candidates[0].text);
        if (candidate_text = '') or
            (get_candidate_text_unit_count(candidate_text) <> input_syllable_count) then
        begin
            Exit;
        end;

        if input_syllable_count <= 4 then
        begin
            Exit(True);
        end;

        top_candidate_local := candidates[0];
        if is_problematic_single_char_chain_candidate_for_query(lookup_text,
            top_candidate_local) then
        begin
            Exit(False);
        end;

        if top_candidate_local.source = cs_user then
        begin
            Exit(True);
        end;

        if (m_dictionary <> nil) and
            m_dictionary.is_base_entry(lookup_text, candidate_text) then
        begin
            Exit(True);
        end;

        candidate_path := Trim(get_segment_path_for_candidate(top_candidate_local));
        if input_syllable_count >= 8 then
        begin
            inferred_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, support_score_local,
                support_segments_local);
            Exit(((inferred_path_local <> '') and (support_segments_local > 1) and
                (support_score_local > -960)) or
                ((candidate_path <> '') and
                (get_encoded_path_segment_count_local(candidate_path) > 1)));
        end;

        Result := (candidate_path <> '') and
            (Pos(c_segment_path_separator, candidate_path) > 0);
    end;

    function top_candidate_has_natural_complete_segment_path_local(
        const candidates: TncCandidateList): Boolean;
    var
        top_candidate_local: TncCandidate;
        candidate_text: string;
        encoded_path: string;
        support_score_local: Integer;
        support_segments_local: Integer;
        inferred_path_local: string;
    begin
        Result := False;
        if (Length(candidates) = 0) or
            (Trim(candidates[0].comment) <> '') then
        begin
            Exit;
        end;

        top_candidate_local := candidates[0];
        candidate_text := Trim(candidates[0].text);
        if (candidate_text = '') or
            (get_candidate_text_unit_count(candidate_text) <> input_syllable_count) then
        begin
            Exit;
        end;

        if is_problematic_single_char_chain_candidate_for_query(lookup_text,
            top_candidate_local) then
        begin
            Exit;
        end;

        if top_candidate_local.source = cs_user then
        begin
            Exit(True);
        end;

        if (m_dictionary <> nil) and
            m_dictionary.is_base_entry(lookup_text, candidate_text) then
        begin
            Exit(True);
        end;

        encoded_path := get_segment_path_for_candidate(candidates[0]);
        if (Trim(encoded_path) <> '') and
            (get_encoded_path_segment_count_local(encoded_path) > 1) then
        begin
            Exit(True);
        end;

        inferred_path_local := infer_segment_path_for_query_text_with_score(
            lookup_text, candidate_text, support_score_local,
            support_segments_local);
        if inferred_path_local = '' then
        begin
            Exit(False);
        end;

        if input_syllable_count <= 4 then
        begin
            Exit(support_score_local > -960);
        end;

        Result := (support_segments_local > 1) and
            (support_score_local > -960);
    end;

    function top_partial_tail_has_multi_syllable_local(
        const candidates: TncCandidateList): Boolean;
    var
        tail_key_local: string;
    begin
        Result := False;
        if Length(candidates) = 0 then
        begin
            Exit;
        end;

        tail_key_local := normalize_pinyin_text(Trim(candidates[0].comment));
        Result := (tail_key_local <> '') and
            (get_effective_compact_pinyin_unit_count(tail_key_local) >= 2);
    end;

    function top_candidate_is_stable_non_modal_partial_local(
        const candidates: TncCandidateList): Boolean;
    var
        top_text_local: string;
        top_comment_local: string;
        text_units_local: Integer;
        comment_units_local: Integer;

        function is_modal_tail_key_local(const value: string): Boolean;
        begin
            Result := (value = 'ba') or (value = 'ma') or
                (value = 'ne') or (value = 'a') or
                (value = 'ya') or (value = 'la') or
                (value = 'le') or (value = 'wa');
        end;
    begin
        Result := False;
        if Length(candidates) = 0 then
        begin
            Exit;
        end;

        top_text_local := Trim(candidates[0].text);
        top_comment_local := normalize_pinyin_text(Trim(candidates[0].comment));
        if (top_text_local = '') or (top_comment_local = '') or
            is_modal_tail_key_local(top_comment_local) then
        begin
            Exit;
        end;

        text_units_local := get_candidate_text_unit_count(top_text_local);
        comment_units_local := get_effective_compact_pinyin_unit_count(top_comment_local);
        Result := (text_units_local >= 2) and (comment_units_local > 0) and
            (text_units_local + comment_units_local = input_syllable_count);
    end;

    procedure filter_overlong_partial_candidates_for_current_query(
        var candidates: TncCandidateList);
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
    begin
        if (Length(candidates) = 0) or (input_syllable_count <= 0) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(candidates) do
        begin
            candidate_text_local := Trim(candidates[in_idx_local].text);
            if (Trim(candidates[in_idx_local].comment) <> '') and
                (candidate_text_local <> '') and
                (get_candidate_text_unit_count(candidate_text_local) >
                input_syllable_count) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                candidates[out_idx_local] := candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    procedure filter_single_char_partial_candidates_for_current_query(
        var candidates: TncCandidateList);
    const
        c_min_phrase_prefix_weight = 500;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
        candidate_units_local: Integer;
        candidate_weight_local: Integer;
    begin
        if Length(candidates) = 0 then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(candidates) do
        begin
            candidate_text_local := Trim(candidates[in_idx_local].text);
            if (Trim(candidates[in_idx_local].comment) <> '') and
                (candidate_text_local <> '') then
            begin
                candidate_units_local :=
                    get_candidate_text_unit_count(candidate_text_local);
                if candidate_units_local < 2 then
                begin
                    Continue;
                end;

                candidate_weight_local := candidates[in_idx_local].score;
                if candidates[in_idx_local].has_dict_weight then
                begin
                    candidate_weight_local :=
                        candidates[in_idx_local].dict_weight;
                end;
                if (candidates[in_idx_local].source <> cs_user) and
                    (candidate_weight_local < c_min_phrase_prefix_weight) then
                begin
                    Continue;
                end;
            end;

            if out_idx_local <> in_idx_local then
            begin
                candidates[out_idx_local] := candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;
        SetLength(candidates, out_idx_local);
    end;

    function has_full_query_exact_complete_candidate_local: Boolean;
    var
        exact_results_local: TncCandidateList;
        idx_local: Integer;
        text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (lookup_text = '') or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local) then
        begin
            Exit;
        end;

        for idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            text_local := Trim(exact_results_local[idx_local].text);
            if (text_local <> '') and
                (get_candidate_text_unit_count(text_local) =
                input_syllable_count) then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_strong_full_query_exact_complete_candidate_local: Boolean;
    const
        c_min_strong_complete_weight = 700;
    var
        exact_results_local: TncCandidateList;
        idx_local: Integer;
        text_local: string;
        candidate_weight_local: Integer;
    begin
        Result := False;
        if (m_dictionary = nil) or (lookup_text = '') then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local) then
        begin
            Exit;
        end;

        for idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            text_local := Trim(exact_results_local[idx_local].text);
            if (text_local = '') or
                (get_candidate_text_unit_count(text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;
            if (not m_dictionary.is_base_entry(lookup_text, text_local)) and
                (not m_dictionary.is_user_entry(lookup_text, text_local)) then
            begin
                Continue;
            end;

            candidate_weight_local := exact_results_local[idx_local].score;
            if exact_results_local[idx_local].has_dict_weight and
                (exact_results_local[idx_local].dict_weight >
                candidate_weight_local) then
            begin
                candidate_weight_local := exact_results_local[idx_local].dict_weight;
            end;
            if candidate_weight_local >= c_min_strong_complete_weight then
            begin
                Exit(True);
            end;
        end;
    end;

    function try_get_best_complete_exact_candidate_local(
        const candidates: TncCandidateList; out out_candidate: TncCandidate): Boolean;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_score_local: Integer;
        candidate_score_local: Integer;
        candidate_text_local: string;
    begin
        Result := False;
        best_idx_local := -1;
        best_score_local := Low(Integer);
        for idx_local := 0 to High(candidates) do
        begin
            if Trim(candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(candidates[idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            candidate_score_local := candidates[idx_local].score;
            if candidates[idx_local].has_dict_weight and
                (candidates[idx_local].dict_weight > candidate_score_local) then
            begin
                candidate_score_local := candidates[idx_local].dict_weight;
            end;

            if (best_idx_local < 0) or
                (candidate_score_local > best_score_local) then
            begin
                best_idx_local := idx_local;
                best_score_local := candidate_score_local;
            end;
        end;

        if best_idx_local >= 0 then
        begin
            out_candidate := candidates[best_idx_local];
            Result := True;
        end;
    end;

    procedure promote_candidate_to_front_local(var candidates: TncCandidateList;
        const preferred_candidate: TncCandidate);
    var
        idx_local: Integer;
        target_idx_local: Integer;
        front_candidate_local: TncCandidate;
    begin
        if Length(candidates) = 0 then
        begin
            SetLength(candidates, 1);
            candidates[0] := preferred_candidate;
            Exit;
        end;

        target_idx_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if SameText(Trim(candidates[idx_local].text),
                Trim(preferred_candidate.text)) and
                SameText(Trim(candidates[idx_local].comment),
                Trim(preferred_candidate.comment)) then
            begin
                target_idx_local := idx_local;
                Break;
            end;
        end;

        if target_idx_local = 0 then
        begin
            Exit;
        end;

        if target_idx_local > 0 then
        begin
            front_candidate_local := candidates[0];
            candidates[0] := candidates[target_idx_local];
            candidates[target_idx_local] := front_candidate_local;
            Exit;
        end;

        SetLength(candidates, Length(candidates) + 1);
        for idx_local := High(candidates) downto 1 do
        begin
            candidates[idx_local] := candidates[idx_local - 1];
        end;
        candidates[0] := preferred_candidate;
    end;

    function get_directional_locative_span_bonus_local(const text_value: string): Integer;
    const
        c_directional_locative_bonus = 180;
    var
        units_local: TArray<string>;
        head_unit_local: string;
        tail_unit_local: string;
        function is_direction_unit_local(const unit_value: string): Boolean;
        begin
            Result := (unit_value = string(Char($4E1C))) or
                (unit_value = string(Char($5357))) or
                (unit_value = string(Char($897F))) or
                (unit_value = string(Char($5317))) or
                (unit_value = string(Char($4E0A))) or
                (unit_value = string(Char($4E0B))) or
                (unit_value = string(Char($524D))) or
                (unit_value = string(Char($540E))) or
                (unit_value = string(Char($91CC))) or
                (unit_value = string(Char($5916))) or
                (unit_value = string(Char($5DE6))) or
                (unit_value = string(Char($53F3)));
        end;
        function is_locative_tail_unit_local(const unit_value: string): Boolean;
        begin
            Result := (unit_value = string(Char($9762))) or
                (unit_value = string(Char($8FB9))) or
                (unit_value = string(Char($65B9))) or
                (unit_value = string(Char($4FA7))) or
                (unit_value = string(Char($90E8)));
        end;
    begin
        Result := 0;
        units_local := split_text_units(Trim(text_value));
        if Length(units_local) <> 2 then
        begin
            Exit;
        end;

        head_unit_local := Trim(units_local[0]);
        tail_unit_local := Trim(units_local[1]);
        if is_direction_unit_local(head_unit_local) and
            is_locative_tail_unit_local(tail_unit_local) then
        begin
            Result := c_directional_locative_bonus;
        end;
    end;

    procedure promote_multi_syllable_partial_without_full_exact_local(
        var candidates: TncCandidateList);
    const
        c_strong_partial_head_weight = 900;
    var
        idx_local: Integer;
        move_idx_local: Integer;
        tail_key_local: string;
        head_weight_local: Integer;
        picked_local: TncCandidate;
    begin
        if (Length(candidates) <= 1) or
            has_full_query_exact_complete_candidate_local then
        begin
            Exit;
        end;

        for idx_local := 0 to High(candidates) do
        begin
            tail_key_local := normalize_pinyin_text(
                Trim(candidates[idx_local].comment));
            if (tail_key_local = '') or
                (get_effective_compact_pinyin_unit_count(tail_key_local) < 2) or
                (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) < 2) then
            begin
                Continue;
            end;

            if candidates[idx_local].has_dict_weight then
            begin
                head_weight_local := candidates[idx_local].dict_weight;
            end
            else
            begin
                head_weight_local := candidates[idx_local].score;
            end;
            if head_weight_local >= c_strong_partial_head_weight then
            begin
                Continue;
            end;

            if idx_local = 0 then
            begin
                Exit;
            end;

            picked_local := candidates[idx_local];
            for move_idx_local := idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
            Exit;
        end;
    end;

    procedure promote_full_query_exact_complete_phrase_local(
        var candidates: TncCandidateList);
    var
        exact_results_local: TncCandidateList;
        idx_local: Integer;
        best_idx_local: Integer;
        best_weight_local: Integer;
        best_visible_idx_local: Integer;
        best_visible_weight_local: Integer;
        candidate_text_local: string;
        candidate_weight_local: Integer;
        existing_idx_local: Integer;
        move_idx_local: Integer;
        picked_local: TncCandidate;
        normalized_query_local: string;
        latest_choice_text_local: string;
        query_syllables_local: TncPinyinParseResult;

        function get_exact_candidate_weight_local(
            const candidate: TncCandidate; const candidate_text: string): Integer;
        var
            choice_bonus_local: Integer;
        begin
            if candidate.has_dict_weight then
            begin
                Result := candidate.dict_weight;
            end
            else
            begin
                Result := candidate.score;
            end;

            if Result < 0 then
            begin
                Result := 0;
            end;

            choice_bonus_local := get_session_query_bonus(candidate_text);
            if (normalized_query_local <> '') and (m_dictionary <> nil) then
            begin
                choice_bonus_local := Max(choice_bonus_local,
                    m_dictionary.get_query_choice_bonus(normalized_query_local,
                    candidate_text));
            end;

            if choice_bonus_local > 0 then
            begin
                Inc(Result, 50000 + (choice_bonus_local * 20));
            end;
            if is_latest_session_query_choice(candidate_text) or
                ((latest_choice_text_local <> '') and SameText(candidate_text,
                latest_choice_text_local)) then
            begin
                Inc(Result, 200000);
            end;

            if candidate.source = cs_user then
            begin
                Inc(Result, 2048);
            end;
        end;

        function get_visible_candidate_weight_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := get_rank_score(candidate);
            Inc(Result, Max(0, get_segment_path_support_score(candidate)));
            Inc(Result, Max(0, get_candidate_path_confidence_score(candidate)) * 32);
        end;

        function build_segment_query_key_local(const segment_start: Integer;
            const segment_units: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            if (segment_start < 0) or (segment_units <= 0) or
                (segment_start + segment_units > Length(query_syllables_local)) then
            begin
                Exit;
            end;

            for syllable_idx_local := segment_start to
                segment_start + segment_units - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    query_syllables_local[syllable_idx_local].text);
            end;
        end;

        function segment_text_is_visible_primary_local(
            const query_key_value: string; const segment_text_value: string;
            const segment_units_value: Integer): Boolean;
        var
            lookup_results_local: TncCandidateList;
            lookup_idx_local: Integer;
            lookup_text_local: string;
        begin
            Result := True;
            if (query_key_value = '') or (segment_text_value = '') or
                (segment_units_value <= 1) or (m_dictionary = nil) or
                (not m_dictionary.lookup(query_key_value, lookup_results_local)) then
            begin
                Exit;
            end;

            for lookup_idx_local := 0 to High(lookup_results_local) do
            begin
                if Trim(lookup_results_local[lookup_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                lookup_text_local := Trim(
                    lookup_results_local[lookup_idx_local].text);
                if (lookup_text_local = '') or
                    (get_candidate_text_unit_count(lookup_text_local) <>
                    segment_units_value) then
                begin
                    Continue;
                end;
                Exit(SameText(lookup_text_local, segment_text_value));
            end;
        end;

        function path_has_nonprimary_multi_char_segment_local(
            const encoded_path_value: string): Boolean;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_start_local: Integer;
            segment_units_local: Integer;
            segment_text_local: string;
            query_key_local: string;
        begin
            Result := False;
            if (encoded_path_value = '') or
                (get_encoded_path_segment_count_local(encoded_path_value) <= 1) or
                (Length(query_syllables_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            path_parts_local := encoded_path_value.Split([c_segment_path_separator]);
            segment_start_local := 0;
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                segment_units_local :=
                    get_candidate_text_unit_count(segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) then
                begin
                    Exit;
                end;

                if segment_units_local > 1 then
                begin
                    query_key_local := build_segment_query_key_local(
                        segment_start_local, segment_units_local);
                    if not segment_text_is_visible_primary_local(query_key_local,
                        segment_text_local, segment_units_local) then
                    begin
                        Exit(True);
                    end;
                end;
                Inc(segment_start_local, segment_units_local);
            end;
        end;

        function candidate_has_nonprimary_path_local(
            const candidate: TncCandidate; const candidate_index: Integer): Boolean;
        var
            encoded_path_local: string;
        begin
            encoded_path_local := get_segment_path_for_candidate(candidate,
                candidate_index);
            Result := path_has_nonprimary_multi_char_segment_local(
                encoded_path_local);
        end;

        function visible_text_has_nonprimary_path_local(
            const text_value: string): Boolean;
        var
            visible_idx_local: Integer;
        begin
            Result := False;
            if text_value = '' then
            begin
                Exit;
            end;

            for visible_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[visible_idx_local].comment) = '') and
                    SameText(Trim(candidates[visible_idx_local].text),
                    text_value) then
                begin
                    Exit(candidate_has_nonprimary_path_local(
                        candidates[visible_idx_local], visible_idx_local));
                end;
            end;
        end;

        function has_explicit_query_choice_local(
            const candidate: TncCandidate; const candidate_text: string): Boolean;
        begin
            Result := (candidate.source = cs_user) or
                is_latest_session_query_choice(candidate_text) or
                ((latest_choice_text_local <> '') and SameText(candidate_text,
                latest_choice_text_local)) or
                ((normalized_query_local <> '') and (m_dictionary <> nil) and
                (m_dictionary.get_query_choice_bonus(normalized_query_local,
                candidate_text) > 0));
        end;
    begin
        if (m_dictionary = nil) or (lookup_text = '') or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 4) then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local) then
        begin
            Exit;
        end;

        normalized_query_local := normalize_pinyin_text(lookup_text);
        latest_choice_text_local := '';
        if normalized_query_local <> '' then
        begin
            latest_choice_text_local := get_cached_query_latest_choice_text(
                normalized_query_local);
        end;
        query_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);

        best_visible_idx_local := -1;
        best_visible_weight_local := Low(Integer);
        for idx_local := 0 to High(candidates) do
        begin
            if Trim(candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(candidates[idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;
            candidate_weight_local := get_visible_candidate_weight_local(
                candidates[idx_local]);
            if candidate_has_nonprimary_path_local(candidates[idx_local],
                idx_local) then
            begin
                Dec(candidate_weight_local, 240000);
            end;
            if candidate_weight_local > best_visible_weight_local then
            begin
                best_visible_weight_local := candidate_weight_local;
                best_visible_idx_local := idx_local;
            end;
        end;

        best_idx_local := -1;
        best_weight_local := Low(Integer);
        for idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(exact_results_local[idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;
            candidate_weight_local := get_exact_candidate_weight_local(
                exact_results_local[idx_local], candidate_text_local);
            if visible_text_has_nonprimary_path_local(candidate_text_local) and
                (not has_explicit_query_choice_local(
                exact_results_local[idx_local], candidate_text_local)) then
            begin
                Continue;
            end;
            if candidate_weight_local > best_weight_local then
            begin
                best_weight_local := candidate_weight_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            if best_visible_idx_local < 0 then
            begin
                Exit;
            end;
            candidate_text_local := Trim(candidates[best_visible_idx_local].text);
        end
        else if ((exact_results_local[best_idx_local].source <> cs_user) or
            (best_weight_local >= best_visible_weight_local + 512)) then
        begin
            candidate_text_local := Trim(exact_results_local[best_idx_local].text);
        end
        else if best_visible_idx_local >= 0 then
        begin
            candidate_text_local := Trim(candidates[best_visible_idx_local].text);
        end
        else
        begin
            candidate_text_local := Trim(exact_results_local[best_idx_local].text);
        end;
        if (Length(candidates) > 0) and
            SameText(Trim(candidates[0].text), candidate_text_local) and
            (Trim(candidates[0].comment) = '') then
        begin
            Exit;
        end;

        existing_idx_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text), candidate_text_local) then
            begin
                existing_idx_local := idx_local;
                Break;
            end;
        end;

        if existing_idx_local >= 0 then
        begin
            picked_local := candidates[existing_idx_local];
        end
        else
        begin
            picked_local := exact_results_local[best_idx_local];
            picked_local.comment := '';
            if picked_local.score < 0 then
            begin
                picked_local.score := 0;
            end;
        end;

        if existing_idx_local > 0 then
        begin
            for move_idx_local := existing_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
        end
        else if existing_idx_local < 0 then
        begin
            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
        end;
    end;

    function promote_stable_prefix_partial_candidate_local(
        var candidates: TncCandidateList; const min_head_units: Integer): Boolean;
    const
        c_prefix_head_unit_bonus = 64;
        c_prefix_tail_unit_penalty = 32;
        c_prefix_support_rank_max_syllables = 6;
        c_prefix_support_rank_max_candidates = 24;
    var
        idx_local: Integer;
        move_idx_local: Integer;
        picked_idx_local: Integer;
        head_units_local: Integer;
        tail_units_local: Integer;
        head_syllables_local: Integer;
        tail_key_local: string;
        head_query_key_local: string;
        picked_local: TncCandidate;
        best_head_units_local: Integer;
        best_score_local: Integer;
        best_rank_score_local: Integer;
        candidate_score_local: Integer;
        candidate_rank_score_local: Integer;
        best_reliable_local: Boolean;
        candidate_reliable_local: Boolean;
        query_syllables_local: TncPinyinParseResult;
        support_segments_local: Integer;
        support_path_local: string;
        rank_debug_local: string;

        function build_query_prefix_key_local(
            const prefix_syllable_count: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            if (prefix_syllable_count <= 0) or
                (prefix_syllable_count > Length(query_syllables_local)) then
            begin
                Exit;
            end;

            for syllable_idx_local := 0 to prefix_syllable_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    query_syllables_local[syllable_idx_local].text);
            end;
        end;

        function get_stable_single_text_local(const syllable_text: string): string;
        var
            normalized_syllable_local: string;
            lookup_results_local: TncCandidateList;
            idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if (normalized_syllable_local = '') or (m_dictionary = nil) or
                (not m_dictionary.lookup(normalized_syllable_local,
                lookup_results_local)) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(lookup_results_local) do
            begin
                if Trim(lookup_results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(lookup_results_local[idx_local].text);
                if (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) = 1) then
                begin
                    Result := candidate_text_local;
                    Exit;
                end;
            end;
        end;

        function has_strong_phrase_for_head_local(
            const prefix_syllable_count: Integer): Boolean;
        var
            prefix_key_local: string;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_text_local: string;
        begin
            Result := False;
            if (prefix_syllable_count < 2) or (m_dictionary = nil) or
                (Length(query_syllables_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            prefix_key_local := build_query_prefix_key_local(prefix_syllable_count);
            if prefix_key_local = '' then
            begin
                Exit;
            end;

            if not (m_dictionary.lookup_exact_full_pinyin(prefix_key_local,
                prefix_results_local) or
                m_dictionary.lookup(prefix_key_local, prefix_results_local)) then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if get_candidate_text_unit_count(prefix_text_local) =
                    prefix_syllable_count then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_natural_prefix_adjustment_local(
            const candidate_text: string; const head_units: Integer): Integer;
        var
            candidate_units_local: TArray<string>;
            anchor_syllables_local: Integer;
            anchor_key_local: string;
            anchor_results_local: TncCandidateList;
            anchor_idx_local: Integer;
            anchor_text_local: string;
            anchor_units_local: TArray<string>;
            unit_idx_local: Integer;
            mismatch_local: Boolean;
            stable_text_local: string;
            occupied_units_local: TArray<Boolean>;
            covered_units_local: Integer;
            span_len_local: Integer;
            start_idx_local: Integer;
            query_span_key_local: string;
            span_text_local: string;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_score_local: Integer;
            best_exact_score_local: Integer;

            function build_query_span_key_local(const local_start_idx: Integer;
                const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (local_start_idx < 0) or (local_span_len <= 0) or
                    (local_start_idx + local_span_len > Length(query_syllables_local)) then
                begin
                    Exit;
                end;
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        query_syllables_local[local_idx].text);
                end;
            end;

            function join_units_local(const local_start_idx: Integer;
                const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + candidate_units_local[local_idx];
                end;
            end;

            function any_occupied_local(const local_start_idx: Integer;
                const local_span_len: Integer): Boolean;
            var
                local_idx: Integer;
            begin
                Result := False;
                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    if occupied_units_local[local_idx] then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := 0;
            if Length(query_syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;
            candidate_units_local := split_text_units(Trim(candidate_text));
            if Length(candidate_units_local) <> head_units then
            begin
                Exit;
            end;

            for anchor_syllables_local := 2 to Min(3, head_units) do
            begin
                anchor_key_local := build_query_prefix_key_local(
                    anchor_syllables_local);
                if (anchor_key_local = '') or (m_dictionary = nil) then
                begin
                    Continue;
                end;
                if not (m_dictionary.lookup_exact_full_pinyin(anchor_key_local,
                    anchor_results_local) or
                    m_dictionary.lookup(anchor_key_local, anchor_results_local)) then
                begin
                    Continue;
                end;

                anchor_text_local := '';
                for anchor_idx_local := 0 to High(anchor_results_local) do
                begin
                    if Trim(anchor_results_local[anchor_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    anchor_text_local := Trim(anchor_results_local[anchor_idx_local].text);
                    if get_candidate_text_unit_count(anchor_text_local) =
                        anchor_syllables_local then
                    begin
                        Break;
                    end;
                    anchor_text_local := '';
                end;
                if anchor_text_local = '' then
                begin
                    Continue;
                end;

                anchor_units_local := split_text_units(anchor_text_local);
                if Length(anchor_units_local) <> anchor_syllables_local then
                begin
                    Continue;
                end;

                mismatch_local := False;
                for unit_idx_local := 0 to anchor_syllables_local - 1 do
                begin
                    if not SameText(candidate_units_local[unit_idx_local],
                        anchor_units_local[unit_idx_local]) then
                    begin
                        mismatch_local := True;
                        Break;
                    end;
                end;
                if mismatch_local then
                begin
                    Dec(Result, 7000);
                end
                else
                begin
                    Inc(Result, 5200);
                end;
                Break;
            end;

            for unit_idx_local := 1 to head_units - 1 do
            begin
                stable_text_local := get_stable_single_text_local(
                    query_syllables_local[unit_idx_local].text);
                if (stable_text_local = '') or
                    (not has_strong_phrase_for_head_local(unit_idx_local)) then
                begin
                    Continue;
                end;
                if SameText(candidate_units_local[unit_idx_local],
                    stable_text_local) then
                begin
                    Inc(Result, 2800);
                end
                else
                begin
                    Dec(Result, 4200);
                end;
            end;

            SetLength(occupied_units_local, head_units);
            covered_units_local := 0;
            for span_len_local := Min(6, head_units) downto 2 do
            begin
                for start_idx_local := 0 to head_units - span_len_local do
                begin
                    if any_occupied_local(start_idx_local, span_len_local) then
                    begin
                        Continue;
                    end;

                    query_span_key_local := build_query_span_key_local(
                        start_idx_local, span_len_local);
                    if query_span_key_local = '' then
                    begin
                        Continue;
                    end;
                    span_text_local := join_units_local(start_idx_local,
                        span_len_local);
                    if span_text_local = '' then
                    begin
                        Continue;
                    end;

                    if try_get_preferred_query_phrase_text_local(
                        query_span_key_local, preferred_text_local,
                        preferred_bonus_local) then
                    begin
                        if not SameText(span_text_local, preferred_text_local) then
                        begin
                            if span_len_local >= 3 then
                            begin
                                Dec(Result, span_len_local * 2400);
                            end;
                            Continue;
                        end;

                        for unit_idx_local := start_idx_local to
                            start_idx_local + span_len_local - 1 do
                        begin
                            occupied_units_local[unit_idx_local] := True;
                        end;
                        Inc(covered_units_local, span_len_local);
                        Inc(Result, span_len_local * 2200);
                        Inc(Result, Min(2400, Max(0, preferred_bonus_local)));
                        Continue;
                    end;

                    exact_score_local := Low(Integer);
                    best_exact_score_local := Low(Integer);
                    if (m_dictionary <> nil) and
                        lookup_exact_full_pinyin_cached_local(
                        query_span_key_local, exact_results_local) then
                    begin
                        for exact_idx_local := 0 to High(exact_results_local) do
                        begin
                            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            exact_text_local := Trim(
                                exact_results_local[exact_idx_local].text);
                            if (exact_text_local = '') or
                                (get_candidate_text_unit_count(exact_text_local) <>
                                span_len_local) then
                            begin
                                Continue;
                            end;
                            if exact_results_local[exact_idx_local].score >
                                best_exact_score_local then
                            begin
                                best_exact_score_local :=
                                    exact_results_local[exact_idx_local].score;
                            end;
                            if SameText(exact_text_local, span_text_local) then
                            begin
                                exact_score_local :=
                                    exact_results_local[exact_idx_local].score;
                            end;
                        end;
                    end;

                    if (exact_score_local = Low(Integer)) or
                        ((span_len_local = 2) and (exact_score_local < 4096)) then
                    begin
                        if (span_len_local >= 3) and
                            (best_exact_score_local >= 8192) then
                        begin
                            Dec(Result, span_len_local * 2200);
                        end;
                        Continue;
                    end;

                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_units_local[unit_idx_local] := True;
                    end;
                    Inc(covered_units_local, span_len_local);
                    Inc(Result, span_len_local * 1500);
                    Inc(Result, Min(1600, Max(0, exact_score_local div 8)));
                end;
            end;

            if covered_units_local >= Max(4, head_units - 2) then
            begin
                Inc(Result, 3200);
            end;
        end;
    begin
        Result := False;
        picked_idx_local := -1;
        best_head_units_local := -1;
        best_score_local := Low(Integer);
        best_rank_score_local := Low(Integer);
        best_reliable_local := False;
        if Length(candidates) = 0 then
        begin
            Exit;
        end;
        rank_debug_local := '';
        if is_full_pinyin_key(lookup_text) then
        begin
            query_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
        end
        else
        begin
            SetLength(query_syllables_local, 0);
        end;

        for idx_local := 0 to High(candidates) do
        begin
            tail_key_local := normalize_pinyin_text(Trim(candidates[idx_local].comment));
            if tail_key_local = '' then
            begin
                Continue;
            end;

            head_units_local :=
                get_candidate_text_unit_count(Trim(candidates[idx_local].text));
            if head_units_local < min_head_units then
            begin
                Continue;
            end;

            tail_units_local := get_effective_compact_pinyin_unit_count(
                tail_key_local);
            if (tail_units_local <= 0) and has_safe_trailing_initial_typing_state then
            begin
                tail_units_local := 1;
            end;
            if tail_units_local <= 0 then
            begin
                Continue;
            end;
            head_syllables_local := input_syllable_count - tail_units_local;
            if head_syllables_local <= 0 then
            begin
                Continue;
            end;

            if (input_syllable_count >= 8) and (head_units_local < 3) then
            begin
                Continue;
            end;

            candidate_score_local := candidates[idx_local].score;
            if candidates[idx_local].has_dict_weight and
                (candidates[idx_local].dict_weight > candidate_score_local) then
            begin
                candidate_score_local := candidates[idx_local].dict_weight;
            end;
            candidate_reliable_local :=
                (candidates[idx_local].has_dict_weight and
                (candidates[idx_local].dict_weight > 0)) or
                (candidates[idx_local].source = cs_user);

            candidate_rank_score_local := candidate_score_local;
            if (input_syllable_count <= c_prefix_support_rank_max_syllables) and
                (Length(candidates) <= c_prefix_support_rank_max_candidates) and
                (Length(query_syllables_local) = input_syllable_count) then
            begin
                head_query_key_local := build_query_prefix_key_local(
                    head_syllables_local);
                if head_query_key_local <> '' then
                begin
                    support_path_local := infer_segment_path_for_query_text_with_score(
                        head_query_key_local, Trim(candidates[idx_local].text),
                        candidate_score_local, support_segments_local);
                    if (support_path_local <> '') and
                        (candidate_score_local > candidate_rank_score_local) then
                    begin
                        candidate_rank_score_local := candidate_score_local;
                        candidate_reliable_local := True;
                    end;
                end;
            end;
            candidate_rank_score_local := candidate_rank_score_local +
                (head_units_local * c_prefix_head_unit_bonus) -
                (tail_units_local * c_prefix_tail_unit_penalty);
            Inc(candidate_rank_score_local,
                get_natural_prefix_adjustment_local(
                Trim(candidates[idx_local].text), head_units_local));
            if m_config.debug_mode and (idx_local <= 5) then
            begin
                rank_debug_local := rank_debug_local + Format(
                    ' prank[%d]=%s/%s hs=%d ts=%d rank=%d raw=%d rel=%d',
                    [idx_local, Trim(candidates[idx_local].text),
                    Trim(candidates[idx_local].comment), head_units_local,
                    tail_units_local, candidate_rank_score_local,
                    candidate_score_local, Ord(candidate_reliable_local)]);
            end;

            if ((candidate_reliable_local = best_reliable_local) and
                ((candidate_rank_score_local > best_rank_score_local) or
                ((candidate_rank_score_local = best_rank_score_local) and
                (head_units_local > best_head_units_local)) or
                ((candidate_rank_score_local = best_rank_score_local) and
                (head_units_local = best_head_units_local) and
                (candidate_score_local > best_score_local)))) or
                (candidate_reliable_local and (not best_reliable_local) and
                (candidate_rank_score_local + 96 >= best_rank_score_local)) or
                ((not candidate_reliable_local) and best_reliable_local and
                (candidate_rank_score_local > best_rank_score_local + 256)) then
            begin
                picked_idx_local := idx_local;
                best_head_units_local := head_units_local;
                best_score_local := candidate_score_local;
                best_rank_score_local := candidate_rank_score_local;
                best_reliable_local := candidate_reliable_local;
            end;
        end;

        if picked_idx_local < 0 then
        begin
            Exit;
        end;

        if picked_idx_local > 0 then
        begin
            picked_local := candidates[picked_idx_local];
            for move_idx_local := picked_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
        end;
        if m_config.debug_mode and (rank_debug_local <> '') then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                rank_debug_local;
        end;
        Result := True;
    end;

    function prepare_prefix_partial_fast_exit_local(
        var candidates: TncCandidateList; const min_head_units: Integer): Boolean;
    begin
        if input_syllable_count >= 8 then
        begin
            Result := False;
            Exit;
        end;
        if has_full_query_exact_complete_candidate_local then
        begin
            Result := False;
            Exit;
        end;
        Result := promote_stable_prefix_partial_candidate_local(candidates,
            min_head_units);
        if Result and (Length(candidates) > 0) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) and
            (not has_strong_top_complete_candidate_for_postprocess_lock_local(
            candidates)) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' pfast=[defer-weak-complete]';
            end;
            Result := False;
        end;
    end;

    function top_candidate_has_preferred_subspan_coverage_for_fast_exit_local(
        const candidates: TncCandidateList): Boolean;
    const
        c_max_span = 6;
    var
        syllables_local: TncPinyinParseResult;
        top_units_local: TArray<string>;
        occupied_units_local: TArray<Boolean>;
        covered_units_local: Integer;
        required_units_local: Integer;
        start_idx_local: Integer;
        span_len_local: Integer;
        unit_idx_local: Integer;
        query_key_local: string;
        span_text_local: string;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
        has_unmatched_preferred_anchor_local: Boolean;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function join_units_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + top_units_local[local_idx];
            end;
        end;

        function any_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if occupied_units_local[local_idx] then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_fixed_boundary_single_local(const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'ta') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'qu') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'ba') or
                (normalized_syllable_local = 'yi') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'zai') or
                (normalized_syllable_local = 'le') or
                (normalized_syllable_local = 'zhe') or
                (normalized_syllable_local = 'hui');
        end;

        function should_preoccupy_fixed_boundary_single_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'zai');
        end;
    begin
        Result := False;
        if (not has_complete_top_candidate_for_fast_exit_local(candidates)) or
            (input_syllable_count < 7) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        top_units_local := split_text_units(Trim(candidates[0].text));
        if (Length(syllables_local) <> input_syllable_count) or
            (Length(top_units_local) <> input_syllable_count) then
        begin
            Exit;
        end;

        SetLength(occupied_units_local, input_syllable_count);
        covered_units_local := 0;
        has_unmatched_preferred_anchor_local := False;
        for span_len_local := c_max_span downto 2 do
        begin
            for start_idx_local := 0 to input_syllable_count - span_len_local do
            begin
                if any_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_len_local);
                if not try_get_preferred_query_phrase_text_local(query_key_local,
                    preferred_text_local, preferred_bonus_local) then
                begin
                    Continue;
                end;

                span_text_local := join_units_local(start_idx_local,
                    span_len_local);
                if not SameText(span_text_local, preferred_text_local) then
                begin
                    if span_len_local >= 4 then
                    begin
                        has_unmatched_preferred_anchor_local := True;
                    end;
                    Continue;
                end;

                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    occupied_units_local[unit_idx_local] := True;
                end;
                Inc(covered_units_local, span_len_local);
            end;
        end;

        for unit_idx_local := 0 to input_syllable_count - 1 do
        begin
            if (not occupied_units_local[unit_idx_local]) and
                has_fixed_boundary_single_local(syllables_local[unit_idx_local].text) then
            begin
                occupied_units_local[unit_idx_local] := True;
                Inc(covered_units_local);
            end;
        end;

        required_units_local := Min(input_syllable_count,
            Max(8, input_syllable_count div 2));
        Result := (not has_unmatched_preferred_anchor_local) and
            (covered_units_local >= required_units_local);
    end;

    function candidate_has_preferred_prefix_coverage_for_fast_exit_local(
        const candidate_local: TncCandidate): Boolean;
    const
        c_max_span = 6;
        c_exact_two_span_min_score = 4096;
    var
        syllables_local: TncPinyinParseResult;
        top_units_local: TArray<string>;
        occupied_units_local: TArray<Boolean>;
        covered_units_local: Integer;
        required_units_local: Integer;
        start_idx_local: Integer;
        span_len_local: Integer;
        unit_idx_local: Integer;
        prefix_unit_count_local: Integer;
        comment_units_local: Integer;
        candidate_text_local: string;
        candidate_comment_local: string;
        query_key_local: string;
        span_text_local: string;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
        has_unmatched_preferred_anchor_local: Boolean;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function join_units_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + top_units_local[local_idx];
            end;
        end;

        function any_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if occupied_units_local[local_idx] then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_fixed_boundary_single_local(const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'ta') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'qu') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'ba') or
                (normalized_syllable_local = 'yi') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'zai') or
                (normalized_syllable_local = 'le') or
                (normalized_syllable_local = 'zhe') or
                (normalized_syllable_local = 'hui');
        end;

        function span_matches_exact_phrase_local(const query_key_local: string;
            const span_text_local: string; const span_units_local: Integer): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key_local = '') or
                (span_text_local = '') or (span_units_local < 2) then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                exact_results_local) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local = '') or
                    (not SameText(exact_text_local, span_text_local)) or
                    (get_candidate_text_unit_count(exact_text_local) <>
                    span_units_local) then
                begin
                    Continue;
                end;
                if (span_units_local >= 3) or
                    (exact_results_local[exact_idx_local].score >=
                    c_exact_two_span_min_score) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        if (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 8) then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_local.text);
        candidate_comment_local := normalize_pinyin_text(
            Trim(candidate_local.comment));
        if (candidate_text_local = '') or (candidate_comment_local = '') or
            (not is_full_pinyin_key(candidate_comment_local)) then
        begin
            Exit;
        end;
        if (candidate_comment_local = 'ba') or (candidate_comment_local = 'ma') or
            (candidate_comment_local = 'ne') or (candidate_comment_local = 'a') or
            (candidate_comment_local = 'ya') or (candidate_comment_local = 'la') or
            (candidate_comment_local = 'le') or (candidate_comment_local = 'wa') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        top_units_local := split_text_units(candidate_text_local);
        prefix_unit_count_local := Length(top_units_local);
        comment_units_local := get_effective_compact_pinyin_unit_count(
            candidate_comment_local);
        if (Length(syllables_local) <> input_syllable_count) or
            (prefix_unit_count_local < 3) or (comment_units_local <> 1) or
            (prefix_unit_count_local + comment_units_local <> input_syllable_count) then
        begin
            Exit;
        end;

        SetLength(occupied_units_local, prefix_unit_count_local);
        covered_units_local := 0;
        has_unmatched_preferred_anchor_local := False;
        for span_len_local := Min(c_max_span, prefix_unit_count_local) downto 2 do
        begin
            for start_idx_local := 0 to
                prefix_unit_count_local - span_len_local do
            begin
                if any_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_len_local);
                span_text_local := join_units_local(start_idx_local,
                    span_len_local);
                if try_get_preferred_query_phrase_text_local(query_key_local,
                    preferred_text_local, preferred_bonus_local) then
                begin
                    if not SameText(span_text_local, preferred_text_local) then
                    begin
                        if span_len_local >= 4 then
                        begin
                            has_unmatched_preferred_anchor_local := True;
                        end;
                        Continue;
                    end;
                end
                else if not span_matches_exact_phrase_local(query_key_local,
                    span_text_local, span_len_local) then
                begin
                    Continue;
                end;

                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    occupied_units_local[unit_idx_local] := True;
                end;
                Inc(covered_units_local, span_len_local);
            end;
        end;

        for unit_idx_local := 0 to prefix_unit_count_local - 1 do
        begin
            if (not occupied_units_local[unit_idx_local]) and
                has_fixed_boundary_single_local(syllables_local[unit_idx_local].text) then
            begin
                occupied_units_local[unit_idx_local] := True;
                Inc(covered_units_local);
            end;
        end;

        required_units_local := Min(prefix_unit_count_local,
            Max(7, prefix_unit_count_local - 1));
        Result := (not has_unmatched_preferred_anchor_local) and
            (covered_units_local >= required_units_local);
    end;

    function top_partial_candidate_has_preferred_prefix_coverage_for_fast_exit_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := (Length(candidates) > 0) and
            candidate_has_preferred_prefix_coverage_for_fast_exit_local(
            candidates[0]);
    end;

    function query_has_preferred_subspan_coverage_for_light_path_local(
        const include_fixed_boundary_singles: Boolean): Boolean;
    const
        c_max_span = 6;
    var
        syllables_local: TncPinyinParseResult;
        occupied_units_local: TArray<Boolean>;
        covered_units_local: Integer;
        required_units_local: Integer;
        start_idx_local: Integer;
        span_len_local: Integer;
        unit_idx_local: Integer;
        query_key_local: string;
        preferred_text_local: string;
        preferred_bonus_local: Integer;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function any_occupied_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if occupied_units_local[local_idx] then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_fixed_boundary_single_local(const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'ta') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'qu') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'ba') or
                (normalized_syllable_local = 'yi') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'zai') or
                (normalized_syllable_local = 'le') or
                (normalized_syllable_local = 'zhe') or
                (normalized_syllable_local = 'hui');
        end;

        function should_preoccupy_fixed_boundary_single_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'zai');
        end;
    begin
        Result := False;
        if (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 7) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(occupied_units_local, input_syllable_count);
        covered_units_local := 0;
        if include_fixed_boundary_singles then
        begin
            for unit_idx_local := 0 to input_syllable_count - 1 do
            begin
                if should_preoccupy_fixed_boundary_single_local(
                    syllables_local[unit_idx_local].text) then
                begin
                    occupied_units_local[unit_idx_local] := True;
                    Inc(covered_units_local);
                end;
            end;
        end;
        for span_len_local := c_max_span downto 2 do
        begin
            for start_idx_local := 0 to input_syllable_count - span_len_local do
            begin
                if any_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_len_local);
                if not try_get_preferred_query_phrase_text_local(query_key_local,
                    preferred_text_local, preferred_bonus_local) then
                begin
                    Continue;
                end;

                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    occupied_units_local[unit_idx_local] := True;
                end;
                Inc(covered_units_local, span_len_local);
            end;
        end;

        if include_fixed_boundary_singles then
        begin
            for unit_idx_local := 0 to input_syllable_count - 1 do
            begin
                if (not occupied_units_local[unit_idx_local]) and
                    has_fixed_boundary_single_local(syllables_local[unit_idx_local].text) then
                begin
                    occupied_units_local[unit_idx_local] := True;
                    Inc(covered_units_local);
                end;
            end;
        end;

        required_units_local := Min(input_syllable_count,
            Max(8, input_syllable_count div 2));
        Result := covered_units_local >= required_units_local;
    end;

    function try_build_direct_preferred_subspan_sentence_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    const
        c_max_span = 6;
    var
        syllables_local: TncPinyinParseResult;
        pos_local: Integer;
        span_len_local: Integer;
        preferred_text_local: string;
        preferred_bonus_local: Integer;
        preferred_units_local: TArray<string>;
        exact_results_local: TncCandidateList;
        exact_tail_text_local: string;
        phrase_count_local: Integer;
        strong_single_count_local: Integer;
        remaining_count_local: Integer;
        score_local: Integer;
        path_local: string;
        single_text_local: string;
        single_score_local: Integer;
        preferred_span_component_count_local: Integer;
        current_span_component_count_local: Integer;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        procedure append_segment_local(const segment_text: string);
        begin
            out_candidate.text := out_candidate.text + segment_text;
            if path_local <> '' then
            begin
                path_local := path_local + c_segment_path_separator;
            end;
            path_local := path_local + segment_text;
        end;

        function try_build_single_char_tail_text_local(
            const local_start_idx: Integer; const local_count: Integer;
            out out_tail_text: string; out out_tail_score: Integer): Boolean;
        var
            local_idx: Integer;
            local_results: TncCandidateList;
            candidate_idx: Integer;
            candidate_text: string;
            best_text: string;
            best_score: Integer;
            candidate_score: Integer;
            preferred_fixed_text: string;

            function get_preferred_direct_single_text_local(
                const local_syllable: string): string;
            var
                normalized_local: string;
            begin
                Result := '';
                normalized_local := normalize_pinyin_text(local_syllable);
                if normalized_local = 'ba' then
                begin
                    Result := string(Char($628A));
                end
                else if normalized_local = 'hui' then
                begin
                    Result := string(Char($4F1A));
                end
                else if normalized_local = 'de' then
                begin
                    Result := string(Char($7684));
                end
                else if normalized_local = 'yi' then
                begin
                    Result := string(Char($4E00));
                end
                else if normalized_local = 'qian' then
                begin
                    Result := string(Char($524D));
                end
                else if normalized_local = 'liang' then
                begin
                    Result := string(Char($4E24));
                end
                else if normalized_local = 'nian' then
                begin
                    Result := string(Char($5E74));
                end
                else if normalized_local = 'suo' then
                begin
                    Result := string(Char($7F29));
                end;
            end;
        begin
            Result := False;
            out_tail_text := '';
            out_tail_score := 0;
            if (m_dictionary = nil) or (local_count <= 0) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to local_start_idx + local_count - 1 do
            begin
                if not m_dictionary.lookup(
                    normalize_pinyin_text(syllables_local[local_idx].text),
                    local_results) then
                begin
                    Exit(False);
                end;

                best_text := '';
                best_score := Low(Integer);
                preferred_fixed_text := get_preferred_direct_single_text_local(
                    syllables_local[local_idx].text);
                for candidate_idx := 0 to High(local_results) do
                begin
                    candidate_text := Trim(local_results[candidate_idx].text);
                    if Length(split_text_units(candidate_text)) <> 1 then
                    begin
                        Continue;
                    end;
                    candidate_score := local_results[candidate_idx].score;
                    if local_results[candidate_idx].source = cs_user then
                    begin
                        Inc(candidate_score, c_user_score_bonus);
                    end;
                    if SameText(normalize_pinyin_text(syllables_local[local_idx].text),
                        'yu') and (candidate_text = string(Char($4E0E))) then
                    begin
                        Inc(candidate_score, 160);
                    end;
                    if (preferred_fixed_text <> '') and
                        SameText(candidate_text, preferred_fixed_text) then
                    begin
                        Inc(candidate_score, 3600);
                    end;
                    if candidate_score > best_score then
                    begin
                        best_score := candidate_score;
                        best_text := candidate_text;
                    end;
                end;

                if best_text = '' then
                begin
                    Exit(False);
                end;

                out_tail_text := out_tail_text + best_text;
                Inc(out_tail_score, Max(0, best_score));
            end;

            Result := out_tail_text <> '';
        end;

        function is_direct_boundary_single_syllable_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'shuo') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'ba') or
                (normalized_syllable_local = 'hui') or
                (normalized_syllable_local = 'yi') or
                (normalized_syllable_local = 'qian') or
                (normalized_syllable_local = 'liang') or
                (normalized_syllable_local = 'nian') or
                (normalized_syllable_local = 'suo') or
                (normalized_syllable_local = 'yu') or
                (normalized_syllable_local = 'zai') or
                (normalized_syllable_local = 'le');
        end;

        function get_direct_fixed_tail_single_local(
            const syllable_text: string): string;
        var
            normalized_syllable_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if normalized_syllable_local = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable_local = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable_local = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable_local = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable_local = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable_local = 'le' then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable_local = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable_local = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable_local = 'qian' then
            begin
                Result := string(Char($524D));
            end
            else if normalized_syllable_local = 'liang' then
            begin
                Result := string(Char($4E24));
            end
            else if normalized_syllable_local = 'nian' then
            begin
                Result := string(Char($5E74));
            end
            else if normalized_syllable_local = 'suo' then
            begin
                Result := string(Char($7F29));
            end
            else if normalized_syllable_local = 'ma' then
            begin
                Result := string(Char($5417));
            end
            else if normalized_syllable_local = 'ne' then
            begin
                Result := string(Char($5462));
            end
            else if normalized_syllable_local = 'a' then
            begin
                Result := string(Char($554A));
            end
            else if normalized_syllable_local = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if normalized_syllable_local = 'la' then
            begin
                Result := string(Char($5566));
            end
            else if normalized_syllable_local = 'wa' then
            begin
                Result := string(Char($54C7));
            end;
        end;

        function is_direct_modal_tail_syllable_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'ba') or
                (normalized_syllable_local = 'ma') or
                (normalized_syllable_local = 'ne') or
                (normalized_syllable_local = 'a') or
                (normalized_syllable_local = 'ya') or
                (normalized_syllable_local = 'la') or
                (normalized_syllable_local = 'le') or
                (normalized_syllable_local = 'wa');
        end;

        function is_direct_quantity_tail_unit_local(const unit_text: string): Boolean;
        var
            codepoint_local: Integer;
        begin
            Result := False;
            if unit_text = '' then
            begin
                Exit;
            end;
            if Length(unit_text) = 1 then
            begin
                codepoint_local := Ord(unit_text[1]);
            end
            else if (Length(unit_text) = 2) and
                (Ord(unit_text[1]) >= $D800) and
                (Ord(unit_text[1]) <= $DBFF) and
                (Ord(unit_text[2]) >= $DC00) and
                (Ord(unit_text[2]) <= $DFFF) then
            begin
                codepoint_local := ((Ord(unit_text[1]) - $D800) shl 10) +
                    (Ord(unit_text[2]) - $DC00) + $10000;
            end
            else
            begin
                Exit;
            end;

            case codepoint_local of
                $4E2A, $4E9B, $70B9, $4E0B, $6B21, $56DE, $904D,
                $5929, $5E74, $8F6E, $6B65, $6761, $6839, $6279,
                $5339, $90E8, $53F0, $628A, $526F, $5BF9, $53CC,
                $74F6, $6876, $680B, $5E62, $7247, $723F, $76D8,
                $53EA:
                    Result := True;
            end;
        end;

        function try_get_direct_quantity_phrase_before_modal_local(
            const local_start_idx: Integer; out out_text: string;
            out out_bonus: Integer): Boolean;
        var
            local_query_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text: string;
            candidate_units: TArray<string>;
            candidate_weight: Integer;
            candidate_bonus: Integer;
            fixed_first_text: string;
        begin
            Result := False;
            out_text := '';
            out_bonus := 0;
            if (m_dictionary = nil) or (local_start_idx < 0) or
                (local_start_idx + 2 <> input_syllable_count - 1) or
                (not is_direct_modal_tail_syllable_local(
                syllables_local[local_start_idx + 2].text)) then
            begin
                Exit;
            end;

            fixed_first_text := get_direct_fixed_tail_single_local(
                syllables_local[local_start_idx].text);
            if fixed_first_text = '' then
            begin
                Exit;
            end;

            local_query_key := build_query_key_local(local_start_idx, 2);
            if (local_query_key = '') or
                ((not m_dictionary.lookup_exact_full_pinyin(local_query_key,
                local_results)) and
                (not m_dictionary.lookup(local_query_key, local_results))) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(local_results[local_idx].text);
                candidate_units := split_text_units(candidate_text);
                if (Length(candidate_units) <> 2) or
                    (not SameText(candidate_units[0], fixed_first_text)) or
                    (not is_direct_quantity_tail_unit_local(candidate_units[1])) then
                begin
                    Continue;
                end;

                candidate_weight := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[local_idx].dict_weight;
                end;
                candidate_bonus := 1800 + Min(2000, Max(0, candidate_weight)) +
                    (2 * 180) + 4200;
                if (not Result) or (candidate_bonus > out_bonus) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_bonus := candidate_bonus;
                end;
            end;
        end;

        function try_get_preferred_span_local(const local_start_idx: Integer;
            const local_max_span: Integer; out out_span_len: Integer;
            out out_text: string; out out_bonus: Integer): Boolean;
        var
            local_span_len: Integer;
            local_query_key: string;
            local_units: TArray<string>;

            function should_skip_final_modal_compound_span_local(
                const current_span_len: Integer; const current_text: string): Boolean;
            var
                current_units_local: TArray<string>;
                expected_tail_text_local: string;
                prefix_query_key_local: string;
                prefix_text_local: string;
                prefix_bonus_local: Integer;
            begin
                Result := False;
                if (current_span_len < 2) or
                    (local_start_idx + current_span_len <> input_syllable_count) or
                    (not is_direct_modal_tail_syllable_local(
                    syllables_local[local_start_idx + current_span_len - 1].text)) then
                begin
                    Exit;
                end;

                expected_tail_text_local := get_direct_fixed_tail_single_local(
                    syllables_local[local_start_idx + current_span_len - 1].text);
                current_units_local := split_text_units(current_text);
                if (expected_tail_text_local = '') or
                    (Length(current_units_local) <> current_span_len) or
                    SameText(current_units_local[High(current_units_local)],
                    expected_tail_text_local) then
                begin
                    Exit;
                end;

                if (current_span_len = 3) and
                    try_get_direct_quantity_phrase_before_modal_local(
                    local_start_idx, prefix_text_local, prefix_bonus_local) then
                begin
                    Exit(True);
                end;

                prefix_query_key_local := build_query_key_local(local_start_idx,
                    current_span_len - 1);
                Result := try_get_preferred_query_phrase_text_local(
                    prefix_query_key_local, prefix_text_local,
                    prefix_bonus_local) and
                    (Length(split_text_units(prefix_text_local)) =
                    current_span_len - 1);
            end;

            function should_leave_following_anchor_open_local(
                const current_span_len: Integer; const current_text: string): Boolean;
            var
                fixed_head_text_local: string;
                fixed_head_score_local: Integer;
                current_units_local: TArray<string>;
                next_span_len_local: Integer;
                next_query_key_local: string;
                next_text_local: string;
                next_bonus_local: Integer;
                next_units_local: TArray<string>;
            begin
                Result := False;
                if (current_span_len < 2) or
                    (local_start_idx >= input_syllable_count - 1) or
                    (not is_direct_boundary_single_syllable_local(
                    syllables_local[local_start_idx].text)) then
                begin
                    Exit;
                end;

                if not try_build_single_char_tail_text_local(local_start_idx, 1,
                    fixed_head_text_local, fixed_head_score_local) then
                begin
                    Exit;
                end;

                current_units_local := split_text_units(current_text);
                if (Length(current_units_local) <> current_span_len) or
                    (Length(current_units_local) = 0) or
                    (not SameText(current_units_local[0], fixed_head_text_local)) then
                begin
                    Exit;
                end;

                next_span_len_local := Min(c_max_span,
                    input_syllable_count - local_start_idx - 1);
                while next_span_len_local >= 2 do
                begin
                    next_query_key_local := build_query_key_local(
                        local_start_idx + 1, next_span_len_local);
                    if try_get_preferred_query_phrase_text_local(
                        next_query_key_local, next_text_local,
                        next_bonus_local) then
                    begin
                        next_units_local := split_text_units(next_text_local);
                        if Length(next_units_local) = next_span_len_local then
                        begin
                            Exit(True);
                        end;
                    end;
                    Dec(next_span_len_local);
                end;
            end;

            function should_skip_temporal_year_crossing_span_local(
                const current_span_len: Integer): Boolean;
            var
                current_syllable_local: string;
                next_syllable_local: string;
                next_next_syllable_local: string;
            begin
                Result := False;
                if (current_span_len < 2) or
                    (local_start_idx + 2 >= input_syllable_count) then
                begin
                    Exit;
                end;

                current_syllable_local := normalize_pinyin_text(
                    syllables_local[local_start_idx].text);
                next_syllable_local := normalize_pinyin_text(
                    syllables_local[local_start_idx + 1].text);
                next_next_syllable_local := normalize_pinyin_text(
                    syllables_local[local_start_idx + 2].text);
                Result := (current_syllable_local = 'qian') and
                    (next_syllable_local = 'liang') and
                    (next_next_syllable_local = 'nian');
            end;

            function try_get_split_preferred_span_text_local(
                const current_span_len: Integer; out out_split_text: string;
                out out_split_bonus: Integer): Boolean;
            var
                split_len_local: Integer;
                left_key_local: string;
                right_key_local: string;
                left_text_local: string;
                right_text_local: string;
                left_bonus_local: Integer;
                right_bonus_local: Integer;
                candidate_text_local: string;
                candidate_bonus_local: Integer;
            begin
                Result := False;
                out_split_text := '';
                out_split_bonus := 0;
                if current_span_len < 4 then
                begin
                    Exit;
                end;

                for split_len_local := 2 to current_span_len - 2 do
                begin
                    if (split_len_local > 4) or
                        (current_span_len - split_len_local > 4) then
                    begin
                        Continue;
                    end;

                    left_key_local := build_query_key_local(local_start_idx,
                        split_len_local);
                    right_key_local := build_query_key_local(
                        local_start_idx + split_len_local,
                        current_span_len - split_len_local);
                    if (not try_get_preferred_query_phrase_text_local(
                        left_key_local, left_text_local, left_bonus_local)) or
                        (not try_get_preferred_query_phrase_text_local(
                        right_key_local, right_text_local, right_bonus_local)) then
                    begin
                        Continue;
                    end;
                    if (Length(split_text_units(left_text_local)) <>
                        split_len_local) or
                        (Length(split_text_units(right_text_local)) <>
                        current_span_len - split_len_local) then
                    begin
                        Continue;
                    end;

                    candidate_text_local := left_text_local + right_text_local;
                    candidate_bonus_local := left_bonus_local + right_bonus_local +
                        1400 + current_span_len * 120;
                    if (not Result) or (candidate_bonus_local > out_split_bonus) then
                    begin
                        Result := True;
                        out_split_text := candidate_text_local;
                        out_split_bonus := candidate_bonus_local;
                    end;
                end;
            end;
        begin
            Result := False;
            out_span_len := 0;
            out_text := '';
            out_bonus := 0;
            preferred_span_component_count_local := 0;
            local_span_len := Min(local_max_span,
                input_syllable_count - local_start_idx);
            while local_span_len >= 2 do
            begin
                if (local_span_len = 2) and
                    try_get_direct_quantity_phrase_before_modal_local(
                    local_start_idx, out_text, out_bonus) then
                begin
                    out_span_len := local_span_len;
                    preferred_span_component_count_local := 1;
                    Exit(True);
                end;

                local_query_key := build_query_key_local(local_start_idx,
                    local_span_len);
                if try_get_preferred_query_phrase_text_local(local_query_key,
                    out_text, out_bonus) then
                begin
                    local_units := split_text_units(out_text);
                    if Length(local_units) = local_span_len then
                    begin
                        if should_skip_final_modal_compound_span_local(
                            local_span_len, out_text) then
                        begin
                            Dec(local_span_len);
                            Continue;
                        end;
                        if should_skip_temporal_year_crossing_span_local(
                            local_span_len) then
                        begin
                            Dec(local_span_len);
                            Continue;
                        end;
                        if should_leave_following_anchor_open_local(
                            local_span_len, out_text) then
                        begin
                            Dec(local_span_len);
                            Continue;
                        end;
                        out_span_len := local_span_len;
                        preferred_span_component_count_local := 1;
                        Exit(True);
                    end;
                end;
                Dec(local_span_len);
            end;
        end;

        procedure prefer_path_supported_tail_span_local(
            const local_start_idx: Integer; const local_span_len: Integer;
            const current_path: string; var io_text: string; var io_bonus: Integer);
        var
            local_query_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            candidate_bonus: Integer;
            candidate_path: string;
            candidate_path_bonus: Integer;
            best_total: Integer;
            candidate_total: Integer;

            function split_last_segment_path_variant_local(
                const path_value: string; const tail_text: string): string;
            var
                sep_idx: Integer;
                scan_idx: Integer;
                prefix_value: string;
                last_segment: string;
                last_units: TArray<string>;
                unit_idx: Integer;
                head_segment: string;
            begin
                Result := '';
                sep_idx := 0;
                for scan_idx := Length(path_value) downto 1 do
                begin
                    if path_value[scan_idx] = c_segment_path_separator then
                    begin
                        sep_idx := scan_idx;
                        Break;
                    end;
                end;
                if sep_idx > 0 then
                begin
                    prefix_value := Copy(path_value, 1, sep_idx - 1);
                    last_segment := Copy(path_value, sep_idx + 1,
                        Length(path_value) - sep_idx);
                end
                else
                begin
                    prefix_value := '';
                    last_segment := path_value;
                end;

                last_units := split_text_units(last_segment);
                if Length(last_units) < 2 then
                begin
                    Exit;
                end;

                head_segment := '';
                for unit_idx := 0 to High(last_units) - 1 do
                begin
                    head_segment := head_segment + last_units[unit_idx];
                end;
                if (head_segment = '') or (last_units[High(last_units)] = '') then
                begin
                    Exit;
                end;

                if prefix_value <> '' then
                begin
                    Result := prefix_value + c_segment_path_separator;
                end;
                Result := Result + head_segment + c_segment_path_separator +
                    last_units[High(last_units)] + c_segment_path_separator +
                    tail_text;
            end;
        begin
            if (m_dictionary = nil) or (current_path = '') or
                (local_start_idx + local_span_len <> input_syllable_count) then
            begin
                Exit;
            end;

            local_query_key := build_query_key_local(local_start_idx,
                local_span_len);
            if (local_query_key = '') or
                ((not m_dictionary.lookup_exact_full_pinyin(local_query_key,
                local_results)) and
                (not m_dictionary.lookup(local_query_key, local_results))) then
            begin
                Exit;
            end;

            best_total := io_bonus + get_cached_query_segment_path_bonus(
                lookup_text, current_path + c_segment_path_separator + io_text);
            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(local_results[local_idx].text);
                if (candidate_text = '') or
                    (Length(split_text_units(candidate_text)) <> local_span_len) then
                begin
                    Continue;
                end;

                candidate_weight := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[local_idx].dict_weight;
                end;
                candidate_bonus := 1800 + Min(2000, Max(0, candidate_weight)) +
                    local_span_len * 180;
                candidate_path := current_path + c_segment_path_separator +
                    candidate_text;
                candidate_path_bonus := get_cached_query_segment_path_bonus(
                    lookup_text, candidate_path);
                candidate_path := split_last_segment_path_variant_local(
                    current_path, candidate_text);
                if candidate_path <> '' then
                begin
                    candidate_path_bonus := Max(candidate_path_bonus,
                        get_cached_query_segment_path_bonus(lookup_text,
                        candidate_path));
                end;
                candidate_total := candidate_bonus + candidate_path_bonus;
                if candidate_total > best_total then
                begin
                    best_total := candidate_total;
                    io_text := candidate_text;
                    io_bonus := candidate_bonus;
                end;
            end;
        end;

        function should_prefer_single_then_phrase_local(
            const local_pos: Integer; const current_span_len: Integer;
            const current_bonus: Integer; out out_single_text: string;
            out out_single_score: Integer): Boolean;
        var
            next_span_len_local: Integer;
            next_text_local: string;
            next_bonus_local: Integer;
            current_score_local: Integer;
            current_weight_estimate: Integer;
            split_score_local: Integer;
            normalized_current_syllable: string;
        begin
            Result := False;
            out_single_text := '';
            out_single_score := 0;
            if (local_pos < 0) or (local_pos >= input_syllable_count - 1) or
                (current_span_len <> 2) or
                (not is_direct_boundary_single_syllable_local(
                syllables_local[local_pos].text)) then
            begin
                Exit;
            end;

            if not try_build_single_char_tail_text_local(local_pos, 1,
                out_single_text, out_single_score) then
            begin
                Exit;
            end;

            if not try_get_preferred_span_local(local_pos + 1, c_max_span,
                next_span_len_local, next_text_local, next_bonus_local) then
            begin
                Exit;
            end;

            normalized_current_syllable := normalize_pinyin_text(
                syllables_local[local_pos].text);
            current_weight_estimate := current_bonus - 1800 -
                current_span_len * 180;
            if (normalized_current_syllable = 'yu') and
                (current_weight_estimate >= 450) then
            begin
                Exit;
            end;

            current_score_local := current_bonus + (current_span_len * 320);
            split_score_local := 700 + out_single_score + next_bonus_local +
                (next_span_len_local * 320);
            Result := split_score_local >= current_score_local + 900;
        end;

        function should_prefer_two_singles_then_phrase_local(
            const local_pos: Integer; const current_span_len: Integer;
            const current_bonus: Integer; out out_single_text: string;
            out out_single_score: Integer): Boolean;
        var
            next_span_len_local: Integer;
            next_text_local: string;
            next_bonus_local: Integer;
            current_score_local: Integer;
            current_weight_estimate: Integer;
            split_score_local: Integer;
            normalized_current_syllable: string;
        begin
            Result := False;
            out_single_text := '';
            out_single_score := 0;
            if (local_pos < 0) or (local_pos >= input_syllable_count - 2) or
                (current_span_len <> 2) then
            begin
                Exit;
            end;

            if (not is_direct_boundary_single_syllable_local(
                syllables_local[local_pos].text)) and
                (normalize_pinyin_text(syllables_local[local_pos].text) <> 'zhi') then
            begin
                Exit;
            end;

            if not try_build_single_char_tail_text_local(local_pos, 2,
                out_single_text, out_single_score) then
            begin
                Exit;
            end;

            if not try_get_preferred_span_local(local_pos + 2, c_max_span,
                next_span_len_local, next_text_local, next_bonus_local) then
            begin
                Exit;
            end;

            current_weight_estimate := current_bonus - 1800 - current_span_len * 180;
            normalized_current_syllable := normalize_pinyin_text(
                syllables_local[local_pos].text);
            if (normalized_current_syllable = 'yu') and
                (current_weight_estimate >= 450) then
            begin
                Exit;
            end;
            if (current_weight_estimate >= 760) and (out_single_score < 1600) then
            begin
                Exit;
            end;

            current_score_local := current_bonus + (current_span_len * 320);
            split_score_local := 900 + out_single_score + next_bonus_local +
                (next_span_len_local * 320);
            Result := split_score_local >= current_score_local + 900;
        end;

    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        Result := False;
        if (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 6) or all_initial_compact_query or
            (has_internal_dangling_initial and
            (not normalized_typo_long_query_fast_path)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        pos_local := 0;
        phrase_count_local := 0;
        strong_single_count_local := 0;
        score_local := 50000 + (input_syllable_count * 640);
        path_local := '';
        while pos_local < input_syllable_count do
        begin
        if try_get_preferred_span_local(pos_local, c_max_span,
            span_len_local, preferred_text_local,
            preferred_bonus_local) then
        begin
            prefer_path_supported_tail_span_local(pos_local,
                span_len_local, path_local, preferred_text_local,
                preferred_bonus_local);
                current_span_component_count_local :=
                    Max(1, preferred_span_component_count_local);
                if should_prefer_single_then_phrase_local(pos_local,
                    span_len_local, preferred_bonus_local,
                    single_text_local, single_score_local) then
                begin
                    append_segment_local(single_text_local);
                    Inc(score_local, 700 + single_score_local);
                    Inc(strong_single_count_local);
                    Inc(pos_local);
                    Continue;
                end;
                if should_prefer_two_singles_then_phrase_local(pos_local,
                    span_len_local, preferred_bonus_local,
                    single_text_local, single_score_local) then
                begin
                    append_segment_local(single_text_local);
                    Inc(score_local, 900 + single_score_local);
                    Inc(strong_single_count_local, 2);
                    Inc(pos_local, 2);
                    Continue;
                end;

                preferred_units_local := split_text_units(preferred_text_local);
                if Length(preferred_units_local) = span_len_local then
                begin
                    append_segment_local(preferred_text_local);
                    Inc(score_local, preferred_bonus_local +
                        (span_len_local * 320));
                    Inc(phrase_count_local, current_span_component_count_local);
                    Inc(pos_local, span_len_local);
                    Continue;
                end;
            end;

            span_len_local := 0;
            if span_len_local < 2 then
            begin
                remaining_count_local := input_syllable_count - pos_local;
                if (phrase_count_local >= 2) and (remaining_count_local = 1) and
                    (not is_direct_boundary_single_syllable_local(
                    syllables_local[pos_local].text)) and
                    (not is_direct_modal_tail_syllable_local(
                    syllables_local[pos_local].text)) then
                begin
                    out_candidate.comment := build_query_key_local(pos_local,
                        remaining_count_local);
                    out_candidate.score := score_local + 400;
                    out_candidate.source := cs_rule;
                    out_candidate.has_dict_weight := True;
                    out_candidate.dict_weight := out_candidate.score;
                    remember_segment_path_for_candidate(out_candidate.text,
                        out_candidate.comment, path_local, out_candidate.score);
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' directpartial=[%s/%s]',
                            [out_candidate.text, out_candidate.comment]);
                    end;
                    Result := Trim(out_candidate.text) <> '';
                    Exit;
                end;

                if (phrase_count_local >= 2) and (remaining_count_local = 1) and
                    try_build_single_char_tail_text_local(pos_local, 1,
                    exact_tail_text_local, preferred_bonus_local) then
                begin
                    append_segment_local(exact_tail_text_local);
                    Inc(score_local, 700 + preferred_bonus_local);
                    if is_direct_boundary_single_syllable_local(
                        syllables_local[pos_local].text) then
                    begin
                        Inc(strong_single_count_local);
                    end;
                    Inc(pos_local);
                    Continue;
                end;

                if ((phrase_count_local >= 3) or
                    (phrase_count_local + strong_single_count_local >= 4)) and
                    (remaining_count_local <= 3) and
                    (m_dictionary <> nil) and
                    m_dictionary.lookup_exact_full_pinyin(
                    build_query_key_local(pos_local, remaining_count_local),
                    exact_results_local) and
                    (Length(exact_results_local) > 0) and
                    (Trim(exact_results_local[0].text) <> '') then
                begin
                    exact_tail_text_local := Trim(exact_results_local[0].text);
                    if Length(split_text_units(exact_tail_text_local)) =
                        remaining_count_local then
                    begin
                        append_segment_local(exact_tail_text_local);
                        Inc(score_local, 1200 + (remaining_count_local * 240));
                        Inc(pos_local, remaining_count_local);
                        Continue;
                    end;
                end;

                if (phrase_count_local >= 1) and (remaining_count_local > 1) and
                    try_build_single_char_tail_text_local(pos_local, 1,
                    exact_tail_text_local, preferred_bonus_local) then
                begin
                    append_segment_local(exact_tail_text_local);
                    Inc(score_local, 700 + preferred_bonus_local);
                    if is_direct_boundary_single_syllable_local(
                        syllables_local[pos_local].text) then
                    begin
                        Inc(strong_single_count_local);
                    end;
                    Inc(pos_local);
                    Continue;
                end;

                if ((phrase_count_local >= 3) or
                    (phrase_count_local + strong_single_count_local >= 4)) and
                    (remaining_count_local <= 3) and
                    (m_dictionary <> nil) and
                    m_dictionary.lookup_exact_full_pinyin(
                    build_query_key_local(pos_local, remaining_count_local),
                    exact_results_local) and
                    (Length(exact_results_local) > 0) and
                    (Trim(exact_results_local[0].text) <> '') then
                begin
                    exact_tail_text_local := Trim(exact_results_local[0].text);
                    if Length(split_text_units(exact_tail_text_local)) =
                        remaining_count_local then
                    begin
                        append_segment_local(exact_tail_text_local);
                        Inc(score_local, 1200 + (remaining_count_local * 240));
                        Inc(pos_local, remaining_count_local);
                    end
                    else if try_build_single_char_tail_text_local(pos_local,
                        remaining_count_local, exact_tail_text_local,
                        preferred_bonus_local) then
                    begin
                        append_segment_local(exact_tail_text_local);
                        Inc(score_local, 900 + (remaining_count_local * 180) +
                            (preferred_bonus_local div Max(1,
                            remaining_count_local)));
                        Inc(strong_single_count_local, remaining_count_local);
                        Inc(pos_local, remaining_count_local);
                    end
                    else
                    begin
                        Exit;
                    end;
                end
                else if ((phrase_count_local >= 3) or
                    (phrase_count_local + strong_single_count_local >= 4)) and
                    (remaining_count_local <= 3) and
                    try_build_single_char_tail_text_local(pos_local,
                    remaining_count_local, exact_tail_text_local,
                    preferred_bonus_local) then
                begin
                    append_segment_local(exact_tail_text_local);
                    Inc(score_local, 900 + (remaining_count_local * 180) +
                        (preferred_bonus_local div Max(1,
                        remaining_count_local)));
                    Inc(strong_single_count_local, remaining_count_local);
                    Inc(pos_local, remaining_count_local);
                end
                else
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' directfail=[pos=%d phrase=%d strong=%d remain=%d]',
                            [pos_local, phrase_count_local,
                            strong_single_count_local, remaining_count_local]);
                    end;
                    Exit;
                end;
            end;
        end;

        if (phrase_count_local < 3) and
            ((phrase_count_local < 2) or
            (phrase_count_local + strong_single_count_local < 4)) and
            (not ((input_syllable_count >= 7) and
            (phrase_count_local >= 2) and (strong_single_count_local >= 1))) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' directfail=[final phrase=%d strong=%d]',
                    [phrase_count_local, strong_single_count_local]);
            end;
            Exit;
        end;

        out_candidate.comment := '';
        out_candidate.score := score_local;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        remember_segment_path_for_candidate(out_candidate.text, '',
            path_local, out_candidate.score);
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' directpath=[%s]', [path_local]);
        end;
        Result := Trim(out_candidate.text) <> '';
    end;

    procedure sort_candidates_lightweight(var candidates: TncCandidateList);
    var
        left_idx: Integer;
        left_units: Integer;
        has_exact_complete_phrase_candidate: Boolean;
        lightweight_query_syllables: TncPinyinParseResult;
        lightweight_query_prepared: Boolean;
        best_two_syllable_exact_texts: TArray<string>;
        best_two_syllable_exact_weights: TArray<Integer>;
        best_two_syllable_exact_known: TArray<Boolean>;
        best_three_syllable_exact_texts: TArray<string>;
        best_three_syllable_exact_weights: TArray<Integer>;
        best_three_syllable_exact_known: TArray<Boolean>;
        unit_cache: TArray<Integer>;
        rank_cache: TArray<Integer>;
        exact_complete_texts: TArray<string>;
        exact_complete_texts_prepared: Boolean;
        short_particle_tail_completion_texts: TArray<string>;
        short_particle_tail_completion_texts_prepared: Boolean;
        latest_query_choice_text: string;
        latest_query_choice_prepared: Boolean;
        order_cache: TArray<Integer>;
        sorted_candidates: TncCandidateList;
        text_cache: TArray<string>;
        right_idx: Integer;
        temp_order: Integer;

        function compare_lightweight_order(const left_order: Integer;
            const right_order: Integer): Integer;
        begin
            if rank_cache[left_order] > rank_cache[right_order] then
            begin
                Exit(-1);
            end;
            if rank_cache[left_order] < rank_cache[right_order] then
            begin
                Exit(1);
            end;
            if unit_cache[left_order] > unit_cache[right_order] then
            begin
                Exit(-1);
            end;
            if unit_cache[left_order] < unit_cache[right_order] then
            begin
                Exit(1);
            end;
            Result := CompareText(text_cache[left_order],
                text_cache[right_order]);
        end;

        function get_display_text_unit_count(const text: string): Integer;
        var
            idx: Integer;
            trimmed: string;
            codepoint: Integer;
        begin
            Result := 0;
            trimmed := Trim(text);
            idx := 1;
            while idx <= Length(trimmed) do
            begin
                codepoint := Ord(trimmed[idx]);
                if (codepoint >= $D800) and (codepoint <= $DBFF) and
                    (idx < Length(trimmed)) and
                    (Ord(trimmed[idx + 1]) >= $DC00) and
                    (Ord(trimmed[idx + 1]) <= $DFFF) then
                begin
                    Inc(idx);
                end;
                Inc(Result);
                Inc(idx);
            end;
        end;

        function is_latest_query_choice_lightweight_local(
            const candidate_text: string): Boolean;
        begin
            Result := False;
            if (lookup_text = '') or (Trim(candidate_text) = '') then
            begin
                Exit;
            end;

            if not latest_query_choice_prepared then
            begin
                latest_query_choice_prepared := True;
                latest_query_choice_text := '';
                if (m_session_query_latest_text <> nil) and
                    m_session_query_latest_text.TryGetValue(lookup_text,
                    latest_query_choice_text) then
                begin
                end
                else if m_dictionary <> nil then
                begin
                    latest_query_choice_text :=
                        get_cached_query_latest_choice_text(lookup_text);
                end;
            end;

            Result := (latest_query_choice_text <> '') and
                SameText(Trim(candidate_text), latest_query_choice_text);
        end;

        function get_short_particle_tail_text_lightweight_local(
            const tail_key_value: string): string;
        var
            normalized_tail_key_local: string;
        begin
            Result := '';
            normalized_tail_key_local :=
                normalize_pinyin_text(Trim(tail_key_value));
            if normalized_tail_key_local = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_tail_key_local = 'le' then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_tail_key_local = 'zhe' then
            begin
                Result := string(Char($7740));
            end
            else if normalized_tail_key_local = 'la' then
            begin
                Result := string(Char($5566));
            end
            else if normalized_tail_key_local = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_tail_key_local = 'ma' then
            begin
                Result := string(Char($5417));
            end
            else if normalized_tail_key_local = 'a' then
            begin
                Result := string(Char($554A));
            end;
        end;

        function text_ends_with_tail_particle_lightweight_local(
            const text_value: string; const tail_text_value: string): Boolean;
        var
            trimmed_text_local: string;
            last_char_local: string;
        begin
            Result := False;
            trimmed_text_local := Trim(text_value);
            if (trimmed_text_local = '') or (tail_text_value = '') then
            begin
                Exit;
            end;
            last_char_local := Copy(trimmed_text_local,
                Length(trimmed_text_local), 1);
            Result := SameText(last_char_local, tail_text_value);
        end;

        procedure prepare_short_particle_tail_completion_texts_local;
        var
            normalized_query_local: string;
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_syllables_local: TncPinyinParseResult;
            head_results_local: TncCandidateList;
            exact_full_results_local: TncCandidateList;
            result_idx_local: Integer;
            exact_idx_local: Integer;
            accepted_count_local: Integer;
            head_text_local: string;
            query_units_local: Integer;
        begin
            if short_particle_tail_completion_texts_prepared then
            begin
                Exit;
            end;

            short_particle_tail_completion_texts_prepared := True;
            SetLength(short_particle_tail_completion_texts, 0);
            if (m_dictionary = nil) or (lookup_text = '') or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            if not try_get_short_particle_tail_query_parts(
                normalized_query_local, tail_key_local, tail_text_local,
                head_key_local) then
            begin
                Exit;
            end;
            if (tail_text_local = '') or (head_key_local = '') then
            begin
                Exit;
            end;

            if lookup_exact_full_pinyin_cached_local(normalized_query_local,
                exact_full_results_local) then
            begin
                query_units_local := Length(
                    get_effective_compact_pinyin_syllables(
                    normalized_query_local));
                for exact_idx_local := 0 to High(exact_full_results_local) do
                begin
                    if (Trim(exact_full_results_local[exact_idx_local].comment) =
                        '') and
                        (Trim(exact_full_results_local[exact_idx_local].text) <>
                        '') and
                        (get_display_text_unit_count(
                        Trim(exact_full_results_local[exact_idx_local].text)) =
                        query_units_local) then
                    begin
                        Exit;
                    end;
                end;
            end;

            head_syllables_local :=
                get_effective_compact_pinyin_syllables(head_key_local);
            if (Length(head_syllables_local) < 2) or
                (Length(head_syllables_local) > 3) or
                (not lookup_exact_full_pinyin_cached_local(head_key_local,
                head_results_local)) then
            begin
                Exit;
            end;

            accepted_count_local := 0;
            for result_idx_local := 0 to High(head_results_local) do
            begin
                if Trim(head_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                head_text_local := Trim(head_results_local[result_idx_local].text);
                if (head_text_local = '') or
                    (get_display_text_unit_count(head_text_local) <>
                    Length(head_syllables_local)) or
                    text_ends_with_tail_particle_lightweight_local(
                    head_text_local, tail_text_local) then
                begin
                    Continue;
                end;

                SetLength(short_particle_tail_completion_texts,
                    accepted_count_local + 1);
                short_particle_tail_completion_texts[accepted_count_local] :=
                    head_text_local + tail_text_local;
                Inc(accepted_count_local);
            end;
        end;

        function is_short_particle_tail_completion_text_lightweight_local(
            const text_value: string): Boolean;
        var
            text_idx_local: Integer;
            trimmed_text_local: string;
        begin
            Result := False;
            prepare_short_particle_tail_completion_texts_local;
            trimmed_text_local := Trim(text_value);
            if trimmed_text_local = '' then
            begin
                Exit;
            end;
            for text_idx_local := 0 to High(short_particle_tail_completion_texts) do
            begin
                if SameText(trimmed_text_local,
                    short_particle_tail_completion_texts[text_idx_local]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure promote_short_particle_tail_completion_over_partial_local;
        var
            partial_idx_local: Integer;
            complete_idx_local: Integer;
            move_idx_local: Integer;
            partial_text_local: string;
            tail_text_local: string;
            complete_text_local: string;
            moved_candidate_local: TncCandidate;
        begin
            for partial_idx_local := 0 to High(candidates) do
            begin
                if Trim(candidates[partial_idx_local].comment) = '' then
                begin
                    Continue;
                end;

                partial_text_local := Trim(candidates[partial_idx_local].text);
                if (get_display_text_unit_count(partial_text_local) < 2) or
                    (get_display_text_unit_count(partial_text_local) > 3) or
                    text_ends_with_tail_particle_lightweight_local(
                    partial_text_local, tail_text_local) then
                begin
                    Continue;
                end;

                tail_text_local := get_short_particle_tail_text_lightweight_local(
                    candidates[partial_idx_local].comment);
                if tail_text_local = '' then
                begin
                    Continue;
                end;

                complete_text_local := partial_text_local + tail_text_local;
                for complete_idx_local := partial_idx_local + 1 to High(candidates) do
                begin
                    if (Trim(candidates[complete_idx_local].comment) = '') and
                        SameText(Trim(candidates[complete_idx_local].text),
                        complete_text_local) then
                    begin
                        moved_candidate_local := candidates[complete_idx_local];
                        for move_idx_local := complete_idx_local downto
                            partial_idx_local + 1 do
                        begin
                            candidates[move_idx_local] :=
                                candidates[move_idx_local - 1];
                        end;
                        candidates[partial_idx_local] := moved_candidate_local;
                        Break;
                    end;
                end;
            end;
        end;

        procedure prepare_exact_complete_texts_local;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_count_local: Integer;
        begin
            if exact_complete_texts_prepared then
            begin
                Exit;
            end;

            exact_complete_texts_prepared := True;
            SetLength(exact_complete_texts, 0);
            if (m_dictionary = nil) or (input_syllable_count < 2) or
                (not is_full_pinyin_key(lookup_text)) or
                (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            exact_count_local := 0;
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local = '') or
                    (get_display_text_unit_count(exact_text_local) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                SetLength(exact_complete_texts, exact_count_local + 1);
                exact_complete_texts[exact_count_local] := exact_text_local;
                Inc(exact_count_local);
            end;
        end;

        function is_exact_complete_text_local(const text_value: string): Boolean;
        var
            exact_idx_local: Integer;
            trimmed_text_local: string;
        begin
            Result := False;
            prepare_exact_complete_texts_local;
            trimmed_text_local := Trim(text_value);
            if trimmed_text_local = '' then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_complete_texts) do
            begin
                if SameText(trimmed_text_local,
                    exact_complete_texts[exact_idx_local]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_complete_exact_phrase_candidate(const candidate: TncCandidate;
            const text_units: Integer): Boolean;
        begin
            Result := (input_syllable_count >= 2) and
                (candidate.comment = '') and
                (text_units = input_syllable_count) and
                (not is_runtime_chain_candidate(candidate)) and
                (not is_runtime_common_pattern_candidate(candidate)) and
                (not is_runtime_redup_candidate(candidate)) and
                ((candidate.source = cs_user) or
                is_exact_complete_text_local(candidate.text));
        end;

        function is_complete_composed_phrase_candidate(const candidate: TncCandidate;
            const text_units: Integer): Boolean;
        begin
            Result := (input_syllable_count >= 2) and
                (candidate.comment = '') and
                (get_candidate_segment_path_score_hint(candidate) > Low(Integer)) and
                (not candidate.has_dict_weight) and
                (candidate.source <> cs_user);
        end;

        function get_fixed_sentence_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end;
        end;

        function has_lightweight_context_source_local: Boolean;
        begin
            Result := Trim(m_left_context) <> '';
            if Trim(m_segment_left_context) <> '' then
            begin
                Exit(True);
            end;
            if (Trim(m_segment_left_context) = '') and
                (Trim(m_external_left_context) <> '') then
            begin
                Exit(True);
            end;
        end;

        procedure prepare_lightweight_query_cache;
        var
            parser: TncPinyinParser;
            idx: Integer;
        begin
            if lightweight_query_prepared then
            begin
                Exit;
            end;
            lightweight_query_prepared := True;
            SetLength(lightweight_query_syllables, 0);
            if normalize_pinyin_text(m_last_lookup_key) = '' then
            begin
                Exit;
            end;
            parser := TncPinyinParser.Create;
            try
                lightweight_query_syllables := parser.parse(normalize_pinyin_text(m_last_lookup_key));
            finally
                parser.Free;
            end;

            if Length(lightweight_query_syllables) <> input_syllable_count then
            begin
                SetLength(lightweight_query_syllables, 0);
                Exit;
            end;

            for idx := 0 to High(lightweight_query_syllables) do
            begin
                if Length(Trim(lightweight_query_syllables[idx].text)) <= 1 then
                begin
                    SetLength(lightweight_query_syllables, 0);
                    Exit;
                end;
            end;

            if Length(lightweight_query_syllables) >= 3 then
            begin
                SetLength(best_three_syllable_exact_texts, Length(lightweight_query_syllables) - 2);
                SetLength(best_three_syllable_exact_weights, Length(lightweight_query_syllables) - 2);
                SetLength(best_three_syllable_exact_known, Length(lightweight_query_syllables) - 2);
            end;
            if Length(lightweight_query_syllables) >= 2 then
            begin
                SetLength(best_two_syllable_exact_texts, Length(lightweight_query_syllables) - 1);
                SetLength(best_two_syllable_exact_weights, Length(lightweight_query_syllables) - 1);
                SetLength(best_two_syllable_exact_known, Length(lightweight_query_syllables) - 1);
            end;
        end;

        function build_lightweight_query_key(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            idx: Integer;
        begin
            Result := '';
            prepare_lightweight_query_cache;
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(lightweight_query_syllables)) then
            begin
                Exit;
            end;
            for idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(lightweight_query_syllables[idx].text);
            end;
        end;

        function try_get_best_exact_two_syllable_text_local(const start_idx: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            query_key: string;
            exact_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            weight_value: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            prepare_lightweight_query_cache;
            if (start_idx < 0) or (start_idx > High(best_two_syllable_exact_known)) then
            begin
                Exit;
            end;

            if best_two_syllable_exact_known[start_idx] then
            begin
                out_text := best_two_syllable_exact_texts[start_idx];
                out_weight := best_two_syllable_exact_weights[start_idx];
                Exit(out_text <> '');
            end;

            best_two_syllable_exact_known[start_idx] := True;
            query_key := build_lightweight_query_key(start_idx, 2);
            // The sentence DP already probes these exact spans. Reuse that
            // cache instead of issuing another general lookup per boundary.
            if (query_key = '') or (m_dictionary = nil) or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                exact_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(exact_results) do
            begin
                if result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[result_idx].source = cs_user) or
                    (not exact_results[result_idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[result_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                    is_runtime_redup_candidate(exact_results[result_idx]) then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results[result_idx].text);
                if get_candidate_text_unit_count(candidate_text) <> 2 then
                begin
                    Continue;
                end;
                weight_value := exact_results[result_idx].score;
                if (weight_value <= 0) and exact_results[result_idx].has_dict_weight then
                begin
                    weight_value := exact_results[result_idx].dict_weight;
                end;
                if weight_value > out_weight then
                begin
                    out_text := candidate_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;

            if Result then
            begin
                best_two_syllable_exact_texts[start_idx] := out_text;
                best_two_syllable_exact_weights[start_idx] := out_weight;
            end;
        end;

        function try_get_best_exact_three_syllable_text_local(const start_idx: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            query_key: string;
            exact_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            weight_value: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            prepare_lightweight_query_cache;
            if (start_idx < 0) or (start_idx > High(best_three_syllable_exact_known)) then
            begin
                Exit;
            end;

            if best_three_syllable_exact_known[start_idx] then
            begin
                out_text := best_three_syllable_exact_texts[start_idx];
                out_weight := best_three_syllable_exact_weights[start_idx];
                Exit(out_text <> '');
            end;

            best_three_syllable_exact_known[start_idx] := True;
            query_key := build_lightweight_query_key(start_idx, 3);
            if (query_key = '') or (m_dictionary = nil) or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                exact_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(exact_results) do
            begin
                if result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[result_idx].source = cs_user) or
                    (not exact_results[result_idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[result_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                    is_runtime_redup_candidate(exact_results[result_idx]) then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results[result_idx].text);
                if get_candidate_text_unit_count(candidate_text) <> 3 then
                begin
                    Continue;
                end;
                weight_value := exact_results[result_idx].score;
                if (weight_value <= 0) and exact_results[result_idx].has_dict_weight then
                begin
                    weight_value := exact_results[result_idx].dict_weight;
                end;
                if weight_value > out_weight then
                begin
                    out_text := candidate_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;

            if Result then
            begin
                best_three_syllable_exact_texts[start_idx] := out_text;
                best_three_syllable_exact_weights[start_idx] := out_weight;
            end;
        end;

        function get_lightweight_sentence_path_adjustment(const candidate: TncCandidate;
            const text_units: Integer): Integer;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            segment_idx: Integer;
            syllable_idx: Integer;
            segment_text: string;
            next_segment_text: string;
            segment_units: Integer;
            next_segment_units: Integer;
            fixed_left: string;
            fixed_right: string;
            fixed_third: string;
            combined_text: string;
            exact_two_text: string;
            exact_two_weight: Integer;
            exact_three_text: string;
            exact_three_weight: Integer;
            current_units: TArray<string>;
            next_units: TArray<string>;
            boundary_pair_start: Integer;
            boundary_pair_text: string;
        begin
            Result := 0;
            if (candidate.comment <> '') or (candidate.source = cs_user) or
                (text_units < 2) then
            begin
                Exit;
            end;

            prepare_lightweight_query_cache;
            if Length(lightweight_query_syllables) <> input_syllable_count then
            begin
                Exit;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            syllable_idx := 0;
            for segment_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[segment_idx]);
                current_units := split_text_units(segment_text);
                segment_units := Length(current_units);
                if segment_units <= 0 then
                begin
                    Exit;
                end;

                if segment_idx < High(segment_parts) then
                begin
                    next_segment_text := Trim(segment_parts[segment_idx + 1]);
                    next_units := split_text_units(next_segment_text);
                    next_segment_units := Length(next_units);
                    if (next_segment_units > 0) and
                        (syllable_idx + segment_units < Length(lightweight_query_syllables)) then
                    begin
                        boundary_pair_start := syllable_idx + segment_units - 1;
                        boundary_pair_text := current_units[High(current_units)] +
                            next_units[0];
                        exact_two_text := '';
                        exact_two_weight := 0;
                        if try_get_best_exact_two_syllable_text_local(
                            boundary_pair_start, exact_two_text, exact_two_weight) and
                            (exact_two_weight >= 700) then
                        begin
                            if SameText(boundary_pair_text, exact_two_text) then
                            begin
                                Inc(Result, 420);
                            end
                            else
                            begin
                                Dec(Result, 1800 + Min(1600, exact_two_weight div 2));
                            end;
                        end;
                    end;
                end;

                if (segment_units = 2) and (syllable_idx + 1 <= High(lightweight_query_syllables)) then
                begin
                    exact_two_text := '';
                    exact_two_weight := 0;
                    if try_get_best_exact_two_syllable_text_local(
                        syllable_idx, exact_two_text, exact_two_weight) then
                    begin
                        if segment_text = exact_two_text then
                        begin
                            Inc(Result, 720);
                        end
                        else if SameText(segment_text,
                            get_fixed_sentence_single_char_local(
                            lightweight_query_syllables[syllable_idx].text) +
                            get_fixed_sentence_single_char_local(
                            lightweight_query_syllables[syllable_idx + 1].text)) then
                        begin
                            Dec(Result, 1600);
                        end;
                    end;

                    fixed_left := get_fixed_sentence_single_char_local(
                        lightweight_query_syllables[syllable_idx].text);
                    fixed_right := get_fixed_sentence_single_char_local(
                        lightweight_query_syllables[syllable_idx + 1].text);
                    if (fixed_left <> '') and (fixed_right <> '') and
                        SameText(segment_text, fixed_left + fixed_right) and
                        (exact_two_text <> '') and
                        (not SameText(segment_text, exact_two_text)) then
                    begin
                        Dec(Result, 1600);
                    end;

                    if (segment_idx < High(segment_parts)) and
                        (syllable_idx + 2 <= High(lightweight_query_syllables)) then
                    begin
                        next_segment_text := Trim(segment_parts[segment_idx + 1]);
                        next_segment_units := get_candidate_text_unit_count(next_segment_text);
                        if next_segment_units >= 1 then
                        begin
                            combined_text := segment_text + split_text_units(next_segment_text)[0];
                            fixed_third := get_fixed_sentence_single_char_local(
                                lightweight_query_syllables[syllable_idx + 2].text);
                            exact_three_text := '';
                            exact_three_weight := 0;
                            if try_get_best_exact_three_syllable_text_local(
                                syllable_idx, exact_three_text, exact_three_weight) then
                            begin
                                if combined_text = exact_three_text then
                                begin
                                    Inc(Result, 520);
                                end
                                else if (fixed_left <> '') and (fixed_right <> '') and
                                    (fixed_third <> '') and
                                    SameText(combined_text,
                                    fixed_left + fixed_right + fixed_third) then
                                begin
                                    Dec(Result, 880);
                                end;
                            end;
                        end;
                    end;
                end;
                Inc(syllable_idx, segment_units);
            end;
        end;

        function get_long_dict_prefix_partial_adjustment(const candidate: TncCandidate;
            const text_units: Integer): Integer;
        var
            remaining_syllables: Integer;
            next_syllable_key: string;
        begin
            Result := 0;
            if (candidate.comment = '') or (not candidate.has_dict_weight) or
                (candidate.source = cs_user) or (text_units < 4) or
                (input_syllable_count < 6) then
            begin
                Exit;
            end;

            prepare_lightweight_query_cache;
            if Length(lightweight_query_syllables) <> input_syllable_count then
            begin
                Exit;
            end;

            remaining_syllables := input_syllable_count - text_units;
            if remaining_syllables <= 0 then
            begin
                Exit;
            end;

            Result := 5600 + (text_units * 520) -
                (remaining_syllables * 80) +
                Min(1600, candidate.dict_weight div 2);

            if text_units <= High(lightweight_query_syllables) then
            begin
                next_syllable_key := normalize_pinyin_text(
                    lightweight_query_syllables[text_units].text);
                if get_fixed_sentence_single_char_local(next_syllable_key) <> '' then
                begin
                    Inc(Result, 2200);
                end;
            end;

            if has_exact_complete_phrase_candidate then
            begin
                Result := Result div 3;
            end;
        end;

        function get_suspicious_swallowed_fixed_boundary_penalty(
            const candidate: TncCandidate; const text_units: Integer): Integer;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            segment_idx: Integer;
            syllable_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            fixed_left: string;
            fixed_right: string;
            exact_two_text: string;
            exact_three_text: string;
            exact_two_weight: Integer;
            exact_three_weight: Integer;
            follow_two_text: string;
            follow_three_text: string;
            follow_two_weight: Integer;
            follow_three_weight: Integer;
            has_preceding_phrase: Boolean;
            has_following_phrase: Boolean;
        begin
            Result := 0;
            if candidate.has_dict_weight or (candidate.source = cs_user) or
                (text_units < 2) then
            begin
                Exit;
            end;

            prepare_lightweight_query_cache;
            if Length(lightweight_query_syllables) <> input_syllable_count then
            begin
                Exit;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            syllable_idx := 0;
            for segment_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[segment_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_units = 2) and
                    (syllable_idx + 1 <= High(lightweight_query_syllables)) then
                begin
                    fixed_left := get_fixed_sentence_single_char_local(
                        lightweight_query_syllables[syllable_idx].text);
                    fixed_right := get_fixed_sentence_single_char_local(
                        lightweight_query_syllables[syllable_idx + 1].text);

                    has_preceding_phrase := False;
                    exact_two_text := '';
                    exact_two_weight := 0;
                    if (syllable_idx >= 1) and
                        try_get_best_exact_two_syllable_text_local(
                        syllable_idx - 1, exact_two_text, exact_two_weight) then
                    begin
                        has_preceding_phrase := True;
                    end;
                    exact_three_text := '';
                    exact_three_weight := 0;
                    if (syllable_idx >= 2) and
                        try_get_best_exact_three_syllable_text_local(
                        syllable_idx - 2, exact_three_text, exact_three_weight) then
                    begin
                        has_preceding_phrase := True;
                    end;

                    has_following_phrase := False;
                    follow_two_text := '';
                    follow_two_weight := 0;
                    if (syllable_idx + 2 <= High(lightweight_query_syllables)) and
                        try_get_best_exact_two_syllable_text_local(
                        syllable_idx + 1, follow_two_text, follow_two_weight) then
                    begin
                        has_following_phrase := True;
                    end;
                    follow_three_text := '';
                    follow_three_weight := 0;
                    if (syllable_idx + 3 <= High(lightweight_query_syllables)) and
                        try_get_best_exact_three_syllable_text_local(
                        syllable_idx + 1, follow_three_text, follow_three_weight) then
                    begin
                        has_following_phrase := True;
                    end;

                    if (fixed_left <> '') and has_following_phrase then
                    begin
                        Inc(Result, 2400);
                    end;
                    if (fixed_right <> '') and has_preceding_phrase and
                        ((syllable_idx + 2 > High(lightweight_query_syllables)) or
                        has_following_phrase) then
                    begin
                        Inc(Result, 2400);
                    end;
                end;
                Inc(syllable_idx, segment_units);
            end;
        end;

        function get_complete_sentence_modal_tail_adjustment(
            const candidate: TncCandidate; const text_units: Integer): Integer;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            last_segment_text: string;
            expected_text: string;
            last_syllable_key: string;
        begin
            Result := 0;
            if (candidate.comment <> '') or (text_units < 4) then
            begin
                Exit;
            end;

            prepare_lightweight_query_cache;
            if Length(lightweight_query_syllables) <> input_syllable_count then
            begin
                Exit;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            if Length(segment_parts) <= 1 then
            begin
                Exit;
            end;

            last_segment_text := Trim(segment_parts[High(segment_parts)]);
            if get_candidate_text_unit_count(last_segment_text) <> 1 then
            begin
                Exit;
            end;

            last_syllable_key := normalize_pinyin_text(
                lightweight_query_syllables[High(lightweight_query_syllables)].text);
            expected_text := '';
            if last_syllable_key = 'ba' then
            begin
                expected_text := string(Char($5427));
            end
            else if last_syllable_key = 'ma' then
            begin
                expected_text := string(Char($5417));
            end
            else if last_syllable_key = 'ne' then
            begin
                expected_text := string(Char($5462));
            end
            else if last_syllable_key = 'a' then
            begin
                expected_text := string(Char($554A));
            end
            else if last_syllable_key = 'ya' then
            begin
                expected_text := string(Char($5440));
            end
            else if last_syllable_key = 'la' then
            begin
                expected_text := string(Char($5566));
            end
            else if last_syllable_key = 'le' then
            begin
                expected_text := string(Char($4E86));
            end
            else if last_syllable_key = 'wa' then
            begin
                expected_text := string(Char($54C7));
            end;

            if expected_text = '' then
            begin
                Exit;
            end;

            if last_segment_text = expected_text then
            begin
                Exit(980);
            end;

            Exit(-920);
        end;

        function is_short_repeated_initial_query_lightweight_local(
            const query_value: string): Boolean;
        const
            c_initial_letters = 'bpmfdtnlgkhjqxzcsryw';
        var
            normalized_query_local: string;
            idx_local: Integer;
        begin
            Result := False;
            normalized_query_local := normalize_pinyin_text(Trim(query_value));
            if (Length(normalized_query_local) < 2) or
                (Length(normalized_query_local) > 3) or
                (Pos(string(normalized_query_local[1]), c_initial_letters) <= 0) then
            begin
                Exit;
            end;

            for idx_local := 2 to Length(normalized_query_local) do
            begin
                if normalized_query_local[idx_local] <> normalized_query_local[1] then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function get_repeated_initial_reduplicated_rank_bonus_local(
            const candidate: TncCandidate; const text_units: Integer): Integer;
        const
            c_repeated_text_bonus = 2200;
            c_repeated_tail_bonus = 1700;
        var
            normalized_query_local: string;
            text_units_local: TArray<string>;
            idx_local: Integer;
            first_unit_local: string;
            is_repeated_text_local: Boolean;
            is_repeated_tail_local: Boolean;
        begin
            Result := 0;
            normalized_query_local := normalize_pinyin_text(Trim(lookup_text));
            if (candidate.comment <> '') or
                ((not candidate.has_dict_weight) and
                (candidate.source <> cs_user)) or
                is_runtime_chain_candidate(candidate) or
                is_runtime_common_pattern_candidate(candidate) or
                is_runtime_redup_candidate(candidate) or
                (not is_short_repeated_initial_query_lightweight_local(
                normalized_query_local)) or
                (text_units <> Length(normalized_query_local)) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate.text));
            if (Length(text_units_local) <> text_units) or
                (Length(text_units_local) = 0) then
            begin
                Exit;
            end;

            first_unit_local := text_units_local[0];
            if first_unit_local = '' then
            begin
                Exit;
            end;

            is_repeated_text_local := True;
            for idx_local := 1 to High(text_units_local) do
            begin
                if not SameText(text_units_local[idx_local], first_unit_local) then
                begin
                    is_repeated_text_local := False;
                    Break;
                end;
            end;
            is_repeated_tail_local := (text_units = 3) and
                SameText(text_units_local[1], text_units_local[2]);
            if not (is_repeated_text_local or is_repeated_tail_local) then
            begin
                Exit;
            end;

            if is_repeated_text_local then
            begin
                Result := c_repeated_text_bonus;
            end
            else
            begin
                Result := c_repeated_tail_bonus;
            end;
            if candidate.has_dict_weight then
            begin
                Inc(Result, Min(1800, candidate.dict_weight));
            end;
        end;

        function get_lightweight_rank(const candidate: TncCandidate;
            const text_units: Integer): Integer;
        var
            context_query_latest_bonus_local: Integer;
            remaining_comment_units_local: Integer;
        begin
            Result := candidate.score;
            if input_syllable_count <= 3 then
            begin
                if (input_syllable_count >= 2) or
                    (Length(candidates) <= c_default_page_size * 5) then
                begin
                    Inc(Result, get_context_bonus(candidate.text));
                end;
                if candidate.comment = '' then
                begin
                    if input_syllable_count >= 2 then
                    begin
                        Inc(Result, get_session_query_bonus(candidate.text));
                    end;
                    Inc(Result, get_session_text_bonus(candidate.text));
                end;
            end;
            if (candidate.comment = '') and
                (input_syllable_count <= 3) then
            begin
                context_query_latest_bonus_local :=
                    get_context_query_latest_bonus(candidate.text);
                if context_query_latest_bonus_local > 0 then
                begin
                    Inc(Result, 13000 + context_query_latest_bonus_local);
                end;
            end;
            if (candidate.comment = '') and
                is_short_particle_tail_completion_text_lightweight_local(
                candidate.text) then
            begin
                Inc(Result, 7200);
            end;
            if (candidate.comment = '') and
                is_latest_query_choice_lightweight_local(candidate.text) then
            begin
                Inc(Result, 12000);
            end;
            if (candidate.comment = '') and is_full_pinyin_key(lookup_text) and
                (input_syllable_count > 0) and
                (text_units > input_syllable_count) and
                (not is_short_particle_tail_completion_text_lightweight_local(
                candidate.text)) then
            begin
                Dec(Result, 20000 + (text_units - input_syllable_count) * 1600);
            end;
            if candidate.source = cs_user then
            begin
                Inc(Result, 1200);
            end;
            if candidate.comment = '' then
            begin
                Inc(Result, 320);
                if (input_syllable_count >= 4) and (text_units = input_syllable_count) then
                begin
                    Inc(Result, 1400);
                end
                else if text_units >= 2 then
                begin
                    Inc(Result, 220);
                end;
            end
            else if text_units >= 2 then
            begin
                Inc(Result, 640);
            end
            else if (input_syllable_count >= 3) then
            begin
                Dec(Result, 960);
            end;

            if (input_syllable_count >= 8) and (candidate.comment <> '') then
            begin
                remaining_comment_units_local :=
                    get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(candidate.comment));
                if remaining_comment_units_local >= 3 then
                begin
                    Dec(Result, 60000 + remaining_comment_units_local * 3000);
                end;
            end;

            if candidate.has_dict_weight then
            begin
                Inc(Result, Min(360, candidate.dict_weight div 3));
            end;
            Inc(Result, get_repeated_initial_reduplicated_rank_bonus_local(
                candidate, text_units));

            if is_complete_exact_phrase_candidate(candidate, text_units) then
            begin
                Inc(Result, 7200);
                if candidate.has_dict_weight then
                begin
                    Inc(Result, Min(2400, candidate.dict_weight div 2));
                end;
            end
            else if has_exact_complete_phrase_candidate and
                (candidate.comment = '') and
                (text_units = input_syllable_count) and
                (candidate.source <> cs_user) then
            begin
                Dec(Result, 4600);
            end
            else if has_exact_complete_phrase_candidate and
                (input_syllable_count <= 6) and
                is_complete_composed_phrase_candidate(candidate, text_units) then
            begin
                Dec(Result, 1800);
            end;

            Inc(Result, get_lightweight_sentence_path_adjustment(candidate, text_units));
            Inc(Result, get_long_dict_prefix_partial_adjustment(candidate, text_units));
            Dec(Result, get_suspicious_swallowed_fixed_boundary_penalty(candidate, text_units));
            Inc(Result, get_complete_sentence_modal_tail_adjustment(candidate, text_units));
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        // These flags belong to this invocation. Reading an old stack value
        // skips preparation and changes ranking between identical queries.
        lightweight_query_prepared := False;
        exact_complete_texts_prepared := False;
        has_exact_complete_phrase_candidate := False;
        latest_query_choice_text := '';
        latest_query_choice_prepared := False;
        short_particle_tail_completion_texts_prepared := False;
        SetLength(short_particle_tail_completion_texts, 0);
        for left_idx := 0 to High(candidates) do
        begin
            left_units := get_display_text_unit_count(candidates[left_idx].text);
            if is_complete_exact_phrase_candidate(candidates[left_idx], left_units) then
            begin
                has_exact_complete_phrase_candidate := True;
                Break;
            end;
        end;

        SetLength(unit_cache, Length(candidates));
        SetLength(rank_cache, Length(candidates));
        SetLength(text_cache, Length(candidates));
        for left_idx := 0 to High(candidates) do
        begin
            text_cache[left_idx] := candidates[left_idx].text;
            unit_cache[left_idx] := get_display_text_unit_count(
                text_cache[left_idx]);
            rank_cache[left_idx] := get_lightweight_rank(candidates[left_idx],
                unit_cache[left_idx]);
        end;

        SetLength(order_cache, Length(candidates));
        for left_idx := 0 to High(order_cache) do
        begin
            order_cache[left_idx] := left_idx;
        end;
        for left_idx := 1 to High(order_cache) do
        begin
            temp_order := order_cache[left_idx];
            right_idx := left_idx - 1;
            while (right_idx >= 0) and
                (compare_lightweight_order(order_cache[right_idx],
                temp_order) > 0) do
            begin
                order_cache[right_idx + 1] := order_cache[right_idx];
                Dec(right_idx);
            end;
            order_cache[right_idx + 1] := temp_order;
        end;

        SetLength(sorted_candidates, Length(candidates));
        for left_idx := 0 to High(order_cache) do
        begin
            sorted_candidates[left_idx] := candidates[order_cache[left_idx]];
        end;
        candidates := sorted_candidates;
        promote_short_particle_tail_completion_over_partial_local;

    end;

    procedure filter_short_dictionary_hits_for_explicit_apostrophe(var candidates: TncCandidateList;
        const min_char_count: Integer);
    var
        idx: Integer;
        out_idx: Integer;
        text_units: TArray<string>;
    begin
        if min_char_count <= 1 then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            text_units := split_text_units(Trim(candidates[idx].text));
            if Length(text_units) >= min_char_count then
            begin
                if out_idx <> idx then
                begin
                    candidates[out_idx] := candidates[idx];
                end;
                Inc(out_idx);
            end;
        end;
        SetLength(candidates, out_idx);
    end;

    procedure filter_explicit_apostrophe_single_char_complete_candidates(var candidates: TncCandidateList);
    var
        idx: Integer;
        out_idx: Integer;
        text_units: TArray<string>;
    begin
        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            text_units := split_text_units(Trim(candidates[idx].text));
            if (candidates[idx].comment = '') and (Length(text_units) = 1) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                candidates[out_idx] := candidates[idx];
            end;
            Inc(out_idx);
        end;
        SetLength(candidates, out_idx);
    end;

    function ensure_explicit_apostrophe_query_syllables_parsed: Boolean;
    var
        parser_local: TncPinyinParser;
        tokens_local: TArray<string>;
        token_idx_local: Integer;
        part_idx_local: Integer;
        out_idx_local: Integer;
        compact_offset_local: Integer;
        token_text_local: string;
        token_parts_local: TncPinyinParseResult;
    begin
        Result := False;
        if (not has_explicit_apostrophe_input) or (input_syllable_count <= 1) then
        begin
            Exit;
        end;

        if not explicit_apostrophe_query_parsed then
        begin
            explicit_apostrophe_query_parsed := True;
            parser_local := TncPinyinParser.Create;
            try
                SetLength(explicit_apostrophe_query_syllables, 0);
                tokens_local := m_composition_text.Split(['''']);
                compact_offset_local := 0;
                for token_idx_local := 0 to High(tokens_local) do
                begin
                    token_text_local := normalize_pinyin_text(tokens_local[token_idx_local]);
                    if token_text_local = '' then
                    begin
                        SetLength(explicit_apostrophe_query_syllables, 0);
                        Break;
                    end;

                    token_parts_local := parser_local.parse(token_text_local);
                    if Length(token_parts_local) = 0 then
                    begin
                        SetLength(explicit_apostrophe_query_syllables, 0);
                        Break;
                    end;

                    out_idx_local := Length(explicit_apostrophe_query_syllables);
                    SetLength(explicit_apostrophe_query_syllables,
                        out_idx_local + Length(token_parts_local));
                    for part_idx_local := 0 to High(token_parts_local) do
                    begin
                        explicit_apostrophe_query_syllables[out_idx_local +
                            part_idx_local] := token_parts_local[part_idx_local];
                        explicit_apostrophe_query_syllables[out_idx_local +
                            part_idx_local].text := normalize_pinyin_text(
                            token_parts_local[part_idx_local].text);
                        explicit_apostrophe_query_syllables[out_idx_local +
                            part_idx_local].start_index :=
                            compact_offset_local +
                            token_parts_local[part_idx_local].start_index;
                    end;
                    Inc(compact_offset_local, Length(token_text_local));
                end;
            finally
                parser_local.Free;
            end;
        end;

        Result := Length(explicit_apostrophe_query_syllables) = input_syllable_count;
    end;

    function matches_explicit_apostrophe_unit_alignment(const candidate_text: string): Boolean;
    var
        text_units: TArray<string>;
        idx: Integer;
        is_preferred: Boolean;
    begin
        Result := False;
        if (not has_explicit_apostrophe_input) or (input_syllable_count <= 1) then
        begin
            Exit;
        end;

        if explicit_apostrophe_exact_text_matches_local(candidate_text) then
        begin
            Exit(True);
        end;

        text_units := split_text_units(Trim(candidate_text));
        if Length(text_units) <> input_syllable_count then
        begin
            Exit;
        end;

        if not ensure_explicit_apostrophe_query_syllables_parsed then
        begin
            Exit;
        end;

        for idx := 0 to input_syllable_count - 1 do
        begin
            if not match_single_char_candidate_for_syllable(
                Trim(explicit_apostrophe_query_syllables[idx].text),
                text_units[idx],
                is_preferred) then
            begin
                Exit(False);
            end;
        end;

        Result := True;
    end;

    procedure filter_complete_candidates_for_explicit_apostrophe_boundary(
        var candidates: TncCandidateList; const aligned_candidates: TncCandidateList);
    var
        allowed_complete_texts: TDictionary<string, Byte>;
        idx: Integer;
        out_idx: Integer;
        key: string;
        candidate_text_units: Integer;
        encoded_path: string;
        has_aligned_boundary_candidate: Boolean;
        function contains_non_ascii_text(const value: string): Boolean;
        var
            ch: Char;
        begin
            Result := False;
            for ch in value do
            begin
                if Ord(ch) > $7F then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        allowed_complete_texts := TDictionary<string, Byte>.Create;
        try
            has_aligned_boundary_candidate := has_explicit_apostrophe_input and
                (input_syllable_count > 1);
            for idx := 0 to High(aligned_candidates) do
            begin
                if aligned_candidates[idx].comment <> '' then
                begin
                    if contains_non_ascii_text(Trim(aligned_candidates[idx].text)) then
                    begin
                        has_aligned_boundary_candidate := True;
                    end;
                    Continue;
                end;

                candidate_text_units := get_candidate_text_unit_count(Trim(aligned_candidates[idx].text));
                if candidate_text_units < 2 then
                begin
                    Continue;
                end;

                if matches_explicit_apostrophe_unit_alignment(
                    aligned_candidates[idx].text) then
                begin
                    has_aligned_boundary_candidate := True;
                    key := LowerCase(Trim(aligned_candidates[idx].text));
                    if (key <> '') and
                        (not allowed_complete_texts.ContainsKey(key)) then
                    begin
                        allowed_complete_texts.Add(key, 1);
                    end;
                    Continue;
                end;

                // A segment path alone is not evidence for an explicit apostrophe
                // boundary. For example jin'an must not keep jinan words such as
                // 娴庡崡 just because they can be split into two display units.
                encoded_path := get_segment_path_for_candidate(aligned_candidates[idx]);
                if get_encoded_path_segment_count_local(encoded_path) >= input_syllable_count then
                begin
                    Continue;
                end;
            end;

            if not has_aligned_boundary_candidate then
            begin
                Exit;
            end;

            out_idx := 0;
            for idx := 0 to High(candidates) do
            begin
                if candidates[idx].comment <> '' then
                begin
                    if out_idx <> idx then
                    begin
                        candidates[out_idx] := candidates[idx];
                    end;
                    Inc(out_idx);
                    Continue;
                end;

                candidate_text_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
                if candidate_text_units < 2 then
                begin
                    if out_idx <> idx then
                    begin
                        candidates[out_idx] := candidates[idx];
                    end;
                    Inc(out_idx);
                    Continue;
                end;

                key := LowerCase(Trim(candidates[idx].text));
                if (key <> '') and allowed_complete_texts.ContainsKey(key) then
                begin
                    if out_idx <> idx then
                    begin
                        candidates[out_idx] := candidates[idx];
                    end;
                    Inc(out_idx);
                    Continue;
                end;

                if matches_explicit_apostrophe_unit_alignment(candidates[idx].text) then
                begin
                    if out_idx <> idx then
                    begin
                        candidates[out_idx] := candidates[idx];
                    end;
                    Inc(out_idx);
                end;
            end;
            SetLength(candidates, out_idx);
        finally
            allowed_complete_texts.Free;
        end;
    end;

    procedure prefer_relaxed_missing_apostrophe_boundary_partial(var candidates: TncCandidateList);
    var
        preferred_index: Integer;
        target_index: Integer;
        idx: Integer;
        move_idx: Integer;
        complete_index: Integer;
        preferred_candidate: TncCandidate;
        complete_candidate: TncCandidate;
        preferred_comment: string;
        primary_comment: string;

        function build_partial_tail_comment(const input_text: string;
            const allow_relaxed_split: Boolean): string;
        var
            parser: TncPinyinParser;
            parts: TncPinyinParseResult;
            part_idx: Integer;
        begin
            Result := '';
            if input_text = '' then
            begin
                Exit;
            end;

            parser := TncPinyinParser.Create;
            try
                if allow_relaxed_split then
                begin
                    parts := parse_pinyin_with_relaxed_missing_apostrophe(input_text);
                end
                else
                begin
                    parts := parser.parse(input_text);
                end;
            finally
                parser.Free;
            end;

            if Length(parts) <= 1 then
            begin
                Exit;
            end;

            for part_idx := 1 to High(parts) do
            begin
                Result := Result + parts[part_idx].text;
            end;
        end;

        function is_complete_multi_char_anchor(const candidate: TncCandidate): Boolean;
        var
            candidate_text_units: Integer;
        begin
            Result := False;
            if candidate.comment <> '' then
            begin
                Exit;
            end;

            candidate_text_units := get_candidate_text_unit_count(Trim(candidate.text));
            if candidate_text_units < 2 then
            begin
                Exit;
            end;

            if candidate.has_dict_weight or (candidate.source = cs_user) then
            begin
                Exit(True);
            end;

            Result := (candidate.source = cs_rule) and
                (not is_runtime_chain_candidate(candidate)) and
                (not is_runtime_common_pattern_candidate(candidate)) and
                (not is_runtime_redup_candidate(candidate));
        end;

        function is_single_bmp_cjk_text_local(const value: string): Boolean;
        var
            unit_text: string;
            codepoint: Integer;
        begin
            Result := False;
            unit_text := Trim(value);
            if Length(unit_text) = 1 then
            begin
                codepoint := Ord(unit_text[1]);
            end
            else
            begin
                Exit;
            end;
            Result := (codepoint >= $4E00) and (codepoint <= $9FFF);
        end;

        function try_build_complete_boundary_candidate_local(
            const partial_candidate: TncCandidate;
            out out_candidate: TncCandidate): Boolean;
        var
            tail_lookup: TncCandidateList;
            tail_idx: Integer;
            tail_text: string;
            tail_score: Integer;
            best_tail_text: string;
            best_tail_score: Integer;
        begin
            FillChar(out_candidate, SizeOf(out_candidate), 0);
            Result := False;
            if (m_dictionary = nil) or (Trim(partial_candidate.text) = '') or
                (not is_relaxed_no_initial_syllable_text(preferred_comment)) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup(preferred_comment, tail_lookup) then
            begin
                Exit;
            end;

            best_tail_text := '';
            best_tail_score := Low(Integer);
            for tail_idx := 0 to High(tail_lookup) do
            begin
                if Trim(tail_lookup[tail_idx].comment) <> '' then
                begin
                    Continue;
                end;
                tail_text := Trim(tail_lookup[tail_idx].text);
                if not is_single_bmp_cjk_text_local(tail_text) then
                begin
                    Continue;
                end;
                tail_score := tail_lookup[tail_idx].score;
                if tail_lookup[tail_idx].has_dict_weight and
                    (tail_lookup[tail_idx].dict_weight > tail_score) then
                begin
                    tail_score := tail_lookup[tail_idx].dict_weight;
                end;
                if tail_score > best_tail_score then
                begin
                    best_tail_text := tail_text;
                    best_tail_score := tail_score;
                end;
            end;

            if best_tail_text = '' then
            begin
                Exit;
            end;

            out_candidate := partial_candidate;
            out_candidate.text := Trim(partial_candidate.text) + best_tail_text;
            out_candidate.comment := '';
            out_candidate.score := Max(partial_candidate.score,
                best_tail_score) + 68000;
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
            Result := True;
        end;

        procedure filter_misaligned_no_initial_tail_completes_local;
        var
            parts_local: TncPinyinParseResult;
            idx_local: Integer;
            out_idx_local: Integer;
            units_local: TArray<string>;
            first_preferred_local: Boolean;
            second_preferred_local: Boolean;
        begin
            if (input_syllable_count <> 2) or
                (not is_relaxed_no_initial_syllable_text(preferred_comment)) then
            begin
                Exit;
            end;

            parts_local := parse_pinyin_with_relaxed_missing_apostrophe(
                m_composition_text);
            if Length(parts_local) <> 2 then
            begin
                parts_local := parse_pinyin_with_relaxed_missing_apostrophe(
                    lookup_text);
            end;
            if Length(parts_local) <> 2 then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for idx_local := 0 to High(candidates) do
            begin
                if Trim(candidates[idx_local].comment) = '' then
                begin
                    units_local := split_text_units(
                        Trim(candidates[idx_local].text));
                    if Length(units_local) = 2 then
                    begin
                        if (not match_single_char_candidate_for_syllable(
                            normalize_pinyin_text(parts_local[0].text),
                            units_local[0], first_preferred_local)) or
                            (not match_single_char_candidate_for_syllable(
                            normalize_pinyin_text(parts_local[1].text),
                            units_local[1], second_preferred_local)) then
                        begin
                            Continue;
                        end;
                    end;
                end;

                if out_idx_local <> idx_local then
                begin
                    candidates[out_idx_local] := candidates[idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        preferred_comment := build_partial_tail_comment(m_composition_text, True);
        if preferred_comment = '' then
        begin
            preferred_comment := build_partial_tail_comment(lookup_text, True);
        end;
        primary_comment := build_partial_tail_comment(m_composition_text, False);
        if primary_comment = '' then
        begin
            primary_comment := build_partial_tail_comment(lookup_text, False);
        end;

        if preferred_comment = '' then
        begin
            Exit;
        end;
        if SameText(preferred_comment, primary_comment) and
            (not is_relaxed_no_initial_syllable_text(preferred_comment)) then
        begin
            Exit;
        end;

        filter_misaligned_no_initial_tail_completes_local;

        preferred_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if SameText(candidates[idx].comment, preferred_comment) then
            begin
                preferred_index := idx;
                Break;
            end;
        end;

        if preferred_index < 0 then
        begin
            Exit;
        end;

        if input_syllable_count = 2 then
        begin
            target_index := 0;
        end
        else
        begin
            target_index := 0;
            while (target_index <= High(candidates)) and
                is_complete_multi_char_anchor(candidates[target_index]) do
            begin
                Inc(target_index);
            end;
        end;

        if (target_index > High(candidates)) or (preferred_index <= target_index) then
        begin
            Exit;
        end;

        preferred_candidate := candidates[preferred_index];
        if (input_syllable_count = 2) and
            try_build_complete_boundary_candidate_local(preferred_candidate,
            complete_candidate) then
        begin
            complete_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if SameText(Trim(candidates[idx].text),
                    Trim(complete_candidate.text)) and
                    (Trim(candidates[idx].comment) = '') then
                begin
                    complete_index := idx;
                    Break;
                end;
            end;

            if complete_index >= 0 then
            begin
                candidates[complete_index].score := Max(
                    candidates[complete_index].score, complete_candidate.score);
                candidates[complete_index].has_dict_weight := True;
                candidates[complete_index].dict_weight := Max(
                    candidates[complete_index].dict_weight,
                    candidates[complete_index].score);
                complete_candidate := candidates[complete_index];
                for move_idx := complete_index downto 1 do
                begin
                    candidates[move_idx] := candidates[move_idx - 1];
                end;
                candidates[0] := complete_candidate;
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            for move_idx := High(candidates) downto 1 do
            begin
                candidates[move_idx] := candidates[move_idx - 1];
            end;
            candidates[0] := complete_candidate;
            Exit;
        end;

        if input_syllable_count = 2 then
        begin
            preferred_candidate.score := Max(preferred_candidate.score, 64000);
            preferred_candidate.has_dict_weight := True;
            preferred_candidate.dict_weight := Max(preferred_candidate.dict_weight,
                preferred_candidate.score);
        end;
        for idx := preferred_index downto target_index + 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[target_index] := preferred_candidate;
    end;

    procedure ensure_relaxed_missing_apostrophe_head_partial_visible(
        var candidates: TncCandidateList);
    var
        parts: TncPinyinParseResult;
        head_key: string;
        remaining_pinyin: string;
        head_lookup: TncCandidateList;
        part_idx: Integer;
        candidate_idx: Integer;
        existing_idx: Integer;
        target_idx: Integer;
        insert_idx: Integer;
        best_idx: Integer;
        best_score: Integer;
        source_item: TncCandidate;
        partial_item: TncCandidate;
        move_idx: Integer;

        function is_bmp_cjk_single_char_local(const candidate: TncCandidate): Boolean;
        var
            unit_text: string;
            codepoint: Integer;
        begin
            Result := False;
            unit_text := Trim(candidate.text);
            if Length(unit_text) = 1 then
            begin
                codepoint := Ord(unit_text[1]);
            end
            else if (Length(unit_text) = 2) and
                (Ord(unit_text[1]) >= $D800) and (Ord(unit_text[1]) <= $DBFF) and
                (Ord(unit_text[2]) >= $DC00) and (Ord(unit_text[2]) <= $DFFF) then
            begin
                codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                    (Ord(unit_text[2]) - $DC00) + $10000;
            end
            else
            begin
                Exit;
            end;
            Result := (codepoint >= $4E00) and (codepoint <= $9FFF);
        end;
    begin
        if (m_dictionary = nil) or
            (not has_relaxed_missing_apostrophe_partial) or
            ((not has_relaxed_missing_apostrophe_boundary_shift) and
            has_multi_syllable_input) then
        begin
            Exit;
        end;

        parts := parse_pinyin_with_relaxed_missing_apostrophe(m_composition_text);
        if Length(parts) <= 1 then
        begin
            parts := parse_pinyin_with_relaxed_missing_apostrophe(lookup_text);
        end;
        if Length(parts) <= 1 then
        begin
            Exit;
        end;

        head_key := normalize_pinyin_text(parts[0].text);
        remaining_pinyin := '';
        for part_idx := 1 to High(parts) do
        begin
            remaining_pinyin := remaining_pinyin + normalize_pinyin_text(parts[part_idx].text);
        end;
        if (head_key = '') or (remaining_pinyin = '') then
        begin
            Exit;
        end;

        existing_idx := -1;
        for candidate_idx := 0 to High(candidates) do
        begin
            if SameText(Trim(candidates[candidate_idx].comment), remaining_pinyin) and
                (get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) = 1) then
            begin
                existing_idx := candidate_idx;
                Break;
            end;
        end;
        if existing_idx >= 0 then
        begin
            Exit;
        end;

        if not m_dictionary.lookup(head_key, head_lookup) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_score := Low(Integer);
        for candidate_idx := 0 to High(head_lookup) do
        begin
            source_item := head_lookup[candidate_idx];
            if (Trim(source_item.comment) <> '') or
                (not is_bmp_cjk_single_char_local(source_item)) then
            begin
                Continue;
            end;
            if source_item.score > best_score then
            begin
                best_idx := candidate_idx;
                best_score := source_item.score;
            end;
        end;
        if best_idx < 0 then
        begin
            Exit;
        end;

        partial_item := head_lookup[best_idx];
        partial_item.comment := remaining_pinyin;
        Inc(partial_item.score, 64);

        target_idx := -1;
        for candidate_idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[candidate_idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) > 1) then
            begin
                target_idx := candidate_idx + 1;
                Break;
            end;
        end;
        if target_idx < 0 then
        begin
            target_idx := Min(Length(candidates), 1);
        end;

        SetLength(candidates, Length(candidates) + 1);
        insert_idx := Min(target_idx, High(candidates));
        for move_idx := High(candidates) downto insert_idx + 1 do
        begin
            candidates[move_idx] := candidates[move_idx - 1];
        end;
        candidates[insert_idx] := partial_item;
    end;

    procedure filter_long_query_nonlexicon_complete_candidates(var candidates: TncCandidateList);
    var
        idx: Integer;
        out_idx: Integer;
        candidate_text_units: Integer;
        segment_path: string;
        has_protected_full_complete: Boolean;
    begin
        if (input_syllable_count < 2) or
            (not c_suppress_nonlexicon_complete_long_candidates) then
        begin
            Exit;
        end;

        has_protected_full_complete := False;
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment = '') and
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) =
                input_syllable_count) and
                (candidates[idx].has_dict_weight or (candidates[idx].source = cs_user)) then
            begin
                has_protected_full_complete := True;
                Break;
            end;
        end;

        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            candidate_text_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if (candidates[idx].comment <> '') or (candidate_text_units < 2) then
            begin
                if out_idx <> idx then
                begin
                    candidates[out_idx] := candidates[idx];
                end;
                Inc(out_idx);
                Continue;
            end;

            if candidates[idx].has_dict_weight or (candidates[idx].source = cs_user) then
            begin
                if out_idx <> idx then
                begin
                    candidates[out_idx] := candidates[idx];
                end;
                Inc(out_idx);
                Continue;
            end;

            if is_fuzzy_pinyin_active and
                (candidates[idx].fuzzy_cost > 0) and
                (candidate_text_units = input_syllable_count) then
            begin
                if out_idx <> idx then
                begin
                    candidates[out_idx] := candidates[idx];
                end;
                Inc(out_idx);
                Continue;
            end;

            if has_protected_full_complete and
                (candidate_text_units = input_syllable_count) then
            begin
                Continue;
            end;

            if input_syllable_count >= c_long_sentence_exact_min_syllables then
            begin
                segment_path := get_segment_path_for_candidate(candidates[idx]);
                if (get_encoded_path_segment_count_local(segment_path) > 1) and
                    (not is_runtime_chain_candidate(candidates[idx])) and
                    (not is_runtime_common_pattern_candidate(candidates[idx])) and
                    (not is_runtime_redup_candidate(candidates[idx])) and
                    ((input_syllable_count >= c_long_sentence_full_path_min_syllables) or
                    (is_full_pinyin_key(lookup_text) and
                    is_natural_predictive_segment_path_local(segment_path))) then
                begin
                    if out_idx <> idx then
                    begin
                        candidates[out_idx] := candidates[idx];
                    end;
                    Inc(out_idx);
                    Continue;
                end;
            end;
        end;
        SetLength(candidates, out_idx);
    end;

    function should_enable_long_sentence_full_path_search_local: Boolean;
    begin
        Result := m_config.enable_segment_candidates and has_multi_syllable_input and
            (input_syllable_count >= c_long_sentence_full_path_min_syllables) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not (has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count) and (input_syllable_count < 10)));
    end;

    function should_enable_long_sentence_exact_search_local: Boolean;
    begin
        Result := m_config.enable_segment_candidates and has_multi_syllable_input and
            (input_syllable_count >= c_long_sentence_exact_min_syllables) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial);
    end;

    function has_exact_complete_phrase_candidate_for_syllable_count(
        const candidates: TncCandidateList;
        const expected_syllables: Integer): Boolean; forward;
    function has_natural_complete_segment_path_candidate_for_syllable_count(
        const candidates: TncCandidateList;
        const expected_syllables: Integer): Boolean; forward;
    function has_useful_long_sentence_prediction_candidate(
        const candidates: TncCandidateList): Boolean; forward;
    function has_strong_delayed_long_decode_candidate(
        const candidates: TncCandidateList): Boolean; forward;

    function has_ultra_long_extendable_partial_anchor(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_idx: Integer;
        candidate_text: string;
        candidate_comment: string;
        text_units: Integer;
        comment_units: Integer;
        min_anchor_units: Integer;
    begin
        Result := False;
        if input_syllable_count < 12 then
        begin
            Exit;
        end;

        min_anchor_units := input_syllable_count div 2;
        if min_anchor_units < 4 then
        begin
            min_anchor_units := 4;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            candidate_text := Trim(candidates[candidate_idx].text);
            candidate_comment := Trim(candidates[candidate_idx].comment);
            if (candidate_text = '') or (candidate_comment = '') then
            begin
                Continue;
            end;

            if not (candidates[candidate_idx].has_dict_weight or
                (candidates[candidate_idx].source = cs_user)) then
            begin
                Continue;
            end;

            text_units := get_candidate_text_unit_count(candidate_text);
            if text_units < min_anchor_units then
            begin
                Continue;
            end;

            comment_units := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate_comment));
            if comment_units < 4 then
            begin
                Continue;
            end;

            if text_units + comment_units < input_syllable_count - 1 then
            begin
                Continue;
            end;

            Exit(True);
        end;
    end;

    function should_short_circuit_ultra_long_partial_query_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := delayed_long_decode_mode and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 12) and
            (not normalized_typo_long_query_fast_path) and
            (not has_exact_complete_phrase_candidate_for_syllable_count(
                candidates, input_syllable_count)) and
            (not has_natural_complete_segment_path_candidate_for_syllable_count(
                candidates, input_syllable_count)) and
            (not has_strong_delayed_long_decode_candidate(candidates)) and
            has_ultra_long_extendable_partial_anchor(candidates);
    end;

    function should_record_subquery_sentence_miss_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 12) and
            (Length(candidates) > 0) and
            (Trim(candidates[0].comment) <> '') and
            (not has_exact_complete_phrase_candidate_for_syllable_count(
            candidates, input_syllable_count)) and
            (not has_natural_complete_segment_path_candidate_for_syllable_count(
            candidates, input_syllable_count));
    end;

    function should_skip_repeated_subquery_sentence_solver_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := subquery_sentence_missed and
            should_record_subquery_sentence_miss_local(candidates);
    end;

    function should_skip_expensive_subquery_sentence_solver_local(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_idx_local: Integer;
    begin
        if input_syllable_count > 24 then
        begin
            for candidate_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[candidate_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(
                    candidates[candidate_idx_local].text)) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        Result := (input_syllable_count >= 12) and
            should_record_subquery_sentence_miss_local(candidates) and
            (not has_strong_delayed_long_decode_candidate(candidates));
    end;

    function should_skip_expensive_long_partial_repair_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := should_skip_expensive_subquery_sentence_solver_local(
            candidates);
    end;

    function has_strong_long_sentence_prediction_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := has_useful_long_sentence_prediction_candidate(candidates) or
            (should_enable_long_sentence_exact_search_local and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 10) and
            has_exact_complete_phrase_candidate_for_syllable_count(
                candidates, input_syllable_count));
    end;

    function should_trim_strong_long_sentence_candidates_local(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_idx: Integer;
    begin
        Result := False;
        if (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 8) or
            (Length(candidates) <= get_candidate_limit * 2) then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if candidates[candidate_idx].comment <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            Exit(True);
        end;
    end;

    function is_single_initial_token_local(const token_text: string): Boolean;
    var
        ch: Char;
    begin
        Result := False;
        if Length(token_text) <> 1 then
        begin
            Exit;
        end;

        ch := token_text[1];
        if (ch >= 'A') and (ch <= 'Z') then
        begin
            ch := Chr(Ord(ch) + 32);
        end;
        Result := CharInSet(ch, ['b', 'p', 'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h', 'j', 'q', 'x',
            'r', 'z', 'c', 's', 'y', 'w']);
    end;

    function detect_internal_dangling_initial(const text: string): Boolean;
    var
        parser: TncPinyinParser;
        parts: TncPinyinParseResult;
        idx: Integer;
        all_parts_are_initials: Boolean;
    begin
        Result := False;
        if text = '' then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            parts := parser.parse(text);
        finally
            parser.Free;
        end;

        if Length(parts) <= 1 then
        begin
            Exit;
        end;

        all_parts_are_initials := True;
        for idx := 0 to High(parts) do
        begin
            if not is_single_initial_token_local(parts[idx].text) then
            begin
                all_parts_are_initials := False;
                Break;
            end;
        end;
        if all_parts_are_initials and (Length(parts) <= 4) then
        begin
            Exit;
        end;

        // Pattern like "cha + g + n" usually means malformed/typo input.
        // In this case segment fusion is often noisy and should not override
        // direct lookup/typo recovery results.
        for idx := 0 to High(parts) - 1 do
        begin
            if is_single_initial_token_local(parts[idx].text) then
            begin
                Result := True;
                Exit;
            end;
        end;
    end;

    function detect_safe_trailing_initial_typing_state(const text: string): Boolean;
    var
        normalized_text: string;
        parser: TncPinyinParser;
        parts: TncPinyinParseResult;
        prefix_text: string;
        suffix_start: Integer;
        suffix_count: Integer;
        idx: Integer;
        tail_cluster: string;
    begin
        Result := False;
        if text = '' then
        begin
            Exit;
        end;

        normalized_text := normalize_pinyin_text(text);
        if is_full_pinyin_key(normalized_text) then
        begin
            Exit;
        end;

        if Length(normalized_text) >= 3 then
        begin
            prefix_text := Copy(normalized_text, 1, Length(normalized_text) - 1);
            if is_single_initial_token_local(Copy(normalized_text, Length(normalized_text), 1)) and
                is_full_pinyin_key(prefix_text) then
            begin
                Exit(True);
            end;
        end;
        if Length(normalized_text) >= 4 then
        begin
            tail_cluster := Copy(normalized_text, Length(normalized_text) - 1, 2);
            prefix_text := Copy(normalized_text, 1, Length(normalized_text) - 2);
            if ((tail_cluster = 'zh') or (tail_cluster = 'ch') or (tail_cluster = 'sh')) and
                is_full_pinyin_key(prefix_text) then
            begin
                Exit(True);
            end;
        end;

        parser := TncPinyinParser.create;
        try
            parts := parser.parse(text);
        finally
            parser.Free;
        end;

        if Length(parts) <= 1 then
        begin
            Exit;
        end;

        suffix_start := Length(parts);
        while (suffix_start > 0) and is_single_initial_token_local(parts[suffix_start - 1].text) do
        begin
            Dec(suffix_start);
        end;

        suffix_count := Length(parts) - suffix_start;
        if (suffix_count <= 0) or (suffix_count > 2) or (suffix_start <= 0) then
        begin
            Exit;
        end;

        for idx := 0 to suffix_start - 1 do
        begin
            if is_single_initial_token_local(parts[idx].text) then
            begin
                Exit;
            end;
        end;

        if suffix_count = 1 then
        begin
            Result := True;
            Exit;
        end;

        tail_cluster := LowerCase(parts[High(parts) - 1].text + parts[High(parts)].text);
        Result := (tail_cluster = 'zh') or (tail_cluster = 'ch') or (tail_cluster = 'sh');
    end;

    function detect_raw_safe_trailing_initial_typing_state(
        const normalized_text: string; const current_syllable_count: Integer): Boolean;
    var
        prefix_text: string;
        prefix_syllable_count: Integer;
        tail_text: string;
    begin
        Result := False;
        if (normalized_text = '') or (current_syllable_count <= 1) or
            (Length(normalized_text) < 3) then
        begin
            Exit;
        end;

        if is_full_pinyin_key(normalized_text) and
            (get_effective_compact_pinyin_unit_count(normalized_text) =
            current_syllable_count) then
        begin
            Exit;
        end;

        tail_text := Copy(normalized_text, Length(normalized_text), 1);
        if not is_single_initial_token_local(tail_text) then
        begin
            Exit;
        end;

        prefix_text := Copy(normalized_text, 1, Length(normalized_text) - 1);
        if prefix_text = '' then
        begin
            Exit;
        end;

        if is_full_pinyin_key(prefix_text) then
        begin
            Exit(True);
        end;

        prefix_syllable_count := get_effective_compact_pinyin_unit_count(
            prefix_text);
        Result := prefix_syllable_count = current_syllable_count - 1;
    end;

    function detect_all_initial_compact_query(const text: string): Boolean;
    const
        c_all_initial_compact_query_syllable_min = 5;
    var
        parser: TncPinyinParser;
        parts: TncPinyinParseResult;
        idx: Integer;
    begin
        Result := False;
        if text = '' then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            parts := parser.parse(text);
        finally
            parser.Free;
        end;

        if Length(parts) < c_all_initial_compact_query_syllable_min then
        begin
            Exit;
        end;

        for idx := 0 to High(parts) do
        begin
            if not is_single_initial_token_local(parts[idx].text) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    function is_compact_all_single_initial_query_text(const text: string;
        const min_len: Integer): Boolean;
    var
        idx: Integer;
    begin
        Result := False;
        if Length(text) < min_len then
        begin
            Exit;
        end;

        for idx := 1 to Length(text) do
        begin
            if not is_single_initial_token_local(Copy(text, idx, 1)) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    function is_short_repeated_initial_compact_query_text_local(
        const text: string): Boolean;
    var
        normalized_text_local: string;
        idx: Integer;
    begin
        Result := False;
        normalized_text_local := normalize_pinyin_text(Trim(text));
        if (Length(normalized_text_local) < 2) or
            (Length(normalized_text_local) > 3) then
        begin
            Exit;
        end;

        for idx := 1 to Length(normalized_text_local) do
        begin
            if not is_single_initial_token_local(Copy(normalized_text_local, idx, 1)) then
            begin
                Exit;
            end;
            if (idx > 1) and
                (normalized_text_local[idx] <> normalized_text_local[1]) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure ensure_partial_fallback_visible(var candidates: TncCandidateList; const visible_limit: Integer);
    var
        idx: Integer;
        partial_index: Integer;
        partial_candidate: TncCandidate;
    begin
        if (visible_limit <= 0) or (Length(candidates) <= visible_limit) then
        begin
            Exit;
        end;

        for idx := 0 to visible_limit - 1 do
        begin
            if candidates[idx].comment <> '' then
            begin
                Exit;
            end;
        end;

        partial_index := -1;
        for idx := visible_limit to High(candidates) do
        begin
            if candidates[idx].comment <> '' then
            begin
                partial_index := idx;
                Break;
            end;
        end;

        if partial_index < 0 then
        begin
            Exit;
        end;

        partial_candidate := candidates[partial_index];
        for idx := partial_index downto visible_limit do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[visible_limit - 1] := partial_candidate;
    end;

    function is_strong_exact_partial_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    begin
        Result := (candidate.comment <> '') and
            (get_candidate_text_unit_count(Trim(candidate.text)) >= 2) and
            (candidate.has_dict_weight or (candidate.source = cs_user) or
            ((candidate.source = cs_rule) and
            (not is_runtime_chain_candidate(candidate)) and
            (not is_runtime_common_pattern_candidate(candidate)) and
            (not is_runtime_redup_candidate(candidate))));
    end;

    function is_supported_head_phrase_partial_for_visibility(
        const candidate: TncCandidate): Boolean;
    const
        c_supported_head_phrase_first_single_margin_local = 180;
        c_supported_head_phrase_tail_ratio_pct_local = 80;
    var
        candidate_text: string;
        partial_comment_syllables: Integer;
    begin
        Result := False;
        if (m_last_lookup_syllable_count <> 3) or (candidate.comment = '') then
        begin
            Exit;
        end;

        candidate_text := Trim(candidate.text);
        if (candidate_text = '') or
            (get_candidate_text_unit_count(candidate_text) < 2) then
        begin
            Exit;
        end;

        partial_comment_syllables := get_effective_compact_pinyin_unit_count(
            normalize_pinyin_text(candidate.comment));
        if partial_comment_syllables <> 1 then
        begin
            Exit;
        end;

        if (m_last_three_syllable_head_exact_text = '') or
            (candidate_text <> m_last_three_syllable_head_exact_text) then
        begin
            Exit;
        end;

        if not is_strong_exact_partial_candidate_for_visibility(candidate) then
        begin
            Exit;
        end;

        if m_last_three_syllable_head_strength <= 0 then
        begin
            Exit;
        end;
        if m_last_three_syllable_head_strength >=
            (m_last_three_syllable_first_single_strength +
            c_supported_head_phrase_first_single_margin_local) then
        begin
            Exit(True);
        end;

        if m_last_three_syllable_head_path_bonus > 0 then
        begin
            Exit(True);
        end;

        if m_last_three_syllable_tail_strength <= 0 then
        begin
            Exit(True);
        end;

        Result := (m_last_three_syllable_head_strength * 100) >=
            (m_last_three_syllable_tail_strength *
            c_supported_head_phrase_tail_ratio_pct_local);
    end;

    procedure ensure_strong_two_plus_one_partial_visible(var candidates: TncCandidateList;
        const visible_limit: Integer);
    var
        idx: Integer;
        partial_index: Integer;
        fallback_partial_index: Integer;
        target_index: Integer;
        partial_candidate: TncCandidate;
    begin
        if (m_last_lookup_syllable_count <> 3) or
            (visible_limit <= 0) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        for idx := 0 to Min(visible_limit - 1, High(candidates)) do
        begin
            if is_supported_head_phrase_partial_for_visibility(candidates[idx]) then
            begin
                Exit;
            end;
            if (m_last_three_syllable_partial_preference_kind = 1) and
                is_strong_exact_partial_candidate_for_visibility(candidates[idx]) then
            begin
                Exit;
            end;
        end;

        partial_index := -1;
        fallback_partial_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if is_supported_head_phrase_partial_for_visibility(candidates[idx]) then
            begin
                partial_index := idx;
                Break;
            end;
            if (fallback_partial_index < 0) and
                (m_last_three_syllable_partial_preference_kind = 1) and
                is_strong_exact_partial_candidate_for_visibility(candidates[idx]) then
            begin
                fallback_partial_index := idx;
            end;
        end;

        if partial_index < 0 then
        begin
            partial_index := fallback_partial_index;
        end;

        if partial_index < 0 then
        begin
            Exit;
        end;

        target_index := 2;
        if target_index >= visible_limit then
        begin
            target_index := visible_limit - 1;
        end;
        if target_index >= Length(candidates) then
        begin
            target_index := Length(candidates) - 1;
        end;
        if (target_index < 0) or (partial_index <= target_index) then
        begin
            Exit;
        end;

        partial_candidate := candidates[partial_index];
        for idx := partial_index downto target_index + 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[target_index] := partial_candidate;
    end;

    function is_long_sentence_prefix_partial_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    var
        partial_comment_syllables: Integer;
        partial_text_units: Integer;
        segment_path: string;
    begin
        Result := False;
        if input_syllable_count < c_long_sentence_exact_min_syllables then
        begin
            Exit;
        end;

        if Trim(candidate.comment) = '' then
        begin
            Exit;
        end;

        partial_text_units := get_candidate_text_unit_count(Trim(candidate.text));
        if partial_text_units < 2 then
        begin
            Exit;
        end;

        partial_comment_syllables := get_effective_compact_pinyin_unit_count(
            normalize_pinyin_text(candidate.comment));
        if partial_comment_syllables <= 0 then
        begin
            Exit;
        end;

        if partial_text_units + partial_comment_syllables <> input_syllable_count then
        begin
            Exit;
        end;

        segment_path := get_segment_path_for_candidate(candidate);
        if (partial_text_units >= 3) and
            is_strong_exact_partial_candidate_for_visibility(candidate) then
        begin
            Exit(True);
        end;

        if get_encoded_path_segment_count_local(segment_path) <= 1 then
        begin
            Exit;
        end;

        Result := True;
    end;

    function has_long_sentence_extendable_tail_for_visibility_local: Boolean;
    var
        effective_syllables: TncPinyinParseResult;
        local_syllable_count: Integer;
        last_syllable_text: string;
        suffix_ch: Char;
        extended_key: string;
    begin
        Result := False;
        if lookup_text = '' then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        local_syllable_count := Length(effective_syllables);
        if local_syllable_count < c_long_sentence_exact_min_syllables then
        begin
            Exit;
        end;

        last_syllable_text := Trim(effective_syllables[High(effective_syllables)].text);
        if last_syllable_text = '' then
        begin
            Exit;
        end;

        for suffix_ch := 'a' to 'z' do
        begin
            extended_key := lookup_text + suffix_ch;
            if get_effective_compact_pinyin_unit_count(extended_key) = local_syllable_count then
            begin
                Exit(True);
            end;
        end;
    end;

    function is_predictive_tail_single_char_text_codepoint_for_visibility_local(
        const value: string): Boolean;
    begin
        Result := False;
        if Length(value) <> 1 then
        begin
            Exit;
        end;

        case Ord(value[1]) of
            $4E00, $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
            $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $628A, $88AB, $8BA9,
            $7ED9, $5411, $8DDF, $540C, $4ECE, $4E0E, $5C31, $4E5F, $90FD, $8FD8,
            $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2, $5C06, $5230, $4E86,
            $7740, $8FC7:
                Result := True;
        end;
    end;

    function is_predictive_bridge_single_char_text_codepoint_for_visibility_local(
        const value: string): Boolean;
    begin
        Result := False;
        if Length(value) <> 1 then
        begin
            Exit;
        end;

        case Ord(value[1]) of
            $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765, $5728,
            $662F, $6709, $8981, $60F3, $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9,
            $5411, $8DDF, $540C, $4ECE, $4E0E, $5C31, $4E5F, $90FD, $8FD8, $53C8,
            $518D, $4E0D, $5F88, $592A, $6700, $5DF2, $5C06, $5230:
                Result := True;
        end;
    end;

    function is_natural_predictive_segment_path_local(const encoded_path: string): Boolean;
    var
        segment_parts: TArray<string>;
        segment_idx: Integer;
        trailing_single_start: Integer;
        segment_text: string;
        segment_units: Integer;
        prev_units: Integer;
        next_units: Integer;
    begin
        Result := True;
        if Trim(encoded_path) = '' then
        begin
            Exit;
        end;

        segment_parts := encoded_path.Split([c_segment_path_separator]);
        if Length(segment_parts) <= 1 then
        begin
            Exit;
        end;

        trailing_single_start := Length(segment_parts);
        while trailing_single_start > 0 do
        begin
            if get_candidate_text_unit_count(
                Trim(segment_parts[trailing_single_start - 1])) <> 1 then
            begin
                Break;
            end;
            Dec(trailing_single_start);
        end;

        for segment_idx := trailing_single_start to High(segment_parts) do
        begin
            segment_text := Trim(segment_parts[segment_idx]);
            if not is_predictive_tail_single_char_text_codepoint_for_visibility_local(segment_text) then
            begin
                Exit(False);
            end;
        end;

        for segment_idx := 1 to High(segment_parts) - 1 do
        begin
            if segment_idx >= trailing_single_start then
            begin
                Break;
            end;

            segment_text := Trim(segment_parts[segment_idx]);
            segment_units := get_candidate_text_unit_count(segment_text);
            if segment_units <> 1 then
            begin
                Continue;
            end;

            prev_units := get_candidate_text_unit_count(Trim(segment_parts[segment_idx - 1]));
            next_units := get_candidate_text_unit_count(Trim(segment_parts[segment_idx + 1]));
            if (prev_units >= 2) and (next_units >= 2) and
                (not is_predictive_bridge_single_char_text_codepoint_for_visibility_local(segment_text)) then
            begin
                Exit(False);
            end;
        end;
    end;

    procedure ensure_long_sentence_prefix_partial_visible(var candidates: TncCandidateList;
        const visible_limit: Integer);
    var
        best_index: Integer;
        best_units: Integer;
        best_score: Integer;
        current_units: Integer;
        idx: Integer;
        picked: TncCandidate;
        function build_tail_syllable_key_local(
            const tail_unit_count: Integer): string;
        var
            local_syllables: TncPinyinParseResult;
            local_idx: Integer;
        begin
            Result := '';
            if tail_unit_count <= 0 then
            begin
                Exit;
            end;

            local_syllables := get_effective_compact_pinyin_syllables(lookup_text);
            if (Length(local_syllables) <> input_syllable_count) or
                (tail_unit_count > Length(local_syllables)) then
            begin
                Exit;
            end;

            for local_idx := Length(local_syllables) - tail_unit_count to
                High(local_syllables) do
            begin
                Result := Result + normalize_pinyin_text(
                    Trim(local_syllables[local_idx].text));
            end;
        end;
        function build_prefix_path_local(const parts: TArray<string>;
            const last_prefix_index: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if last_prefix_index < 0 then
            begin
                Exit;
            end;

            for local_idx := 0 to last_prefix_index do
            begin
                if Result <> '' then
                begin
                    Result := Result + c_segment_path_separator;
                end;
                Result := Result + Trim(parts[local_idx]);
            end;
        end;
        procedure move_candidate_to_index_local(const from_index: Integer;
            const to_index: Integer; const fallback_path: string);
        var
            move_idx: Integer;
            moved_candidate: TncCandidate;
            moved_path: string;
        begin
            if (from_index < 0) or (from_index >= Length(candidates)) or
                (to_index < 0) or (to_index >= Length(candidates)) or
                (from_index = to_index) then
            begin
                Exit;
            end;

            moved_candidate := candidates[from_index];
            moved_path := '';
            if from_index < Length(m_candidate_segment_paths) then
            begin
                moved_path := m_candidate_segment_paths[from_index];
            end;
            if moved_path = '' then
            begin
                moved_path := fallback_path;
            end;

            if from_index < to_index then
            begin
                for move_idx := from_index to to_index - 1 do
                begin
                    candidates[move_idx] := candidates[move_idx + 1];
                    if move_idx < Length(m_candidate_segment_paths) - 1 then
                    begin
                        m_candidate_segment_paths[move_idx] :=
                            m_candidate_segment_paths[move_idx + 1];
                    end;
                end;
            end
            else
            begin
                for move_idx := from_index downto to_index + 1 do
                begin
                    candidates[move_idx] := candidates[move_idx - 1];
                    if move_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[move_idx] :=
                            m_candidate_segment_paths[move_idx - 1];
                    end;
                end;
            end;
            candidates[to_index] := moved_candidate;
            if to_index < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[to_index] := moved_path;
            end;
        end;
        function is_full_complete_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) =
                input_syllable_count);
        end;
        function prefix_partial_insert_index_local(
            const skip_index: Integer): Integer;
        var
            scan_idx: Integer;
        begin
            Result := 0;
            for scan_idx := 0 to High(candidates) do
            begin
                if scan_idx = skip_index then
                begin
                    Continue;
                end;
                if not is_full_complete_candidate_local(candidates[scan_idx]) then
                begin
                    Break;
                end;
                Inc(Result);
            end;
            if Result < 1 then
            begin
                Result := 1;
            end;
        end;
        function ensure_top_complete_derived_prefix_partial_local: Boolean;
        var
            top_candidate: TncCandidate;
            top_path: string;
            path_parts: TArray<string>;
            prefix_text: string;
            prefix_path: string;
            tail_text: string;
            tail_key: string;
            tail_units: Integer;
            prefix_idx: Integer;
            inferred_path_score: Integer;
            inferred_path_segments: Integer;
            top_units: TArray<string>;
            top_unit_idx: Integer;
            function insert_prefix_partial_local(const new_prefix_text: string;
                const new_tail_key: string; const new_prefix_path: string): Boolean;
            var
                local_insert_index: Integer;
                local_existing_index: Integer;
                local_candidate_idx: Integer;
                local_move_idx: Integer;
                local_partial_candidate: TncCandidate;
            begin
                Result := False;
                if (Trim(new_prefix_text) = '') or
                    (Trim(new_tail_key) = '') or
                    (get_candidate_text_unit_count(Trim(new_prefix_text)) < 3) then
                begin
                    Exit;
                end;

                local_existing_index := -1;
                for local_candidate_idx := 0 to High(candidates) do
                begin
                    if SameText(Trim(candidates[local_candidate_idx].text),
                        Trim(new_prefix_text)) and
                        SameText(normalize_pinyin_text(Trim(
                        candidates[local_candidate_idx].comment)),
                        normalize_pinyin_text(Trim(new_tail_key))) then
                    begin
                        local_existing_index := local_candidate_idx;
                        Break;
                    end;
                end;

                local_insert_index := prefix_partial_insert_index_local(
                    local_existing_index);
                if local_insert_index < 1 then
                begin
                    local_insert_index := 1;
                end;

                if local_existing_index >= 0 then
                begin
                    if local_insert_index > High(candidates) then
                    begin
                        local_insert_index := High(candidates);
                    end;
                    if local_existing_index <> local_insert_index then
                    begin
                        move_candidate_to_index_local(local_existing_index,
                            local_insert_index, new_prefix_path);
                    end;
                    Exit(True);
                end;

                FillChar(local_partial_candidate,
                    SizeOf(local_partial_candidate), 0);
                local_partial_candidate.text := Trim(new_prefix_text);
                local_partial_candidate.comment := normalize_pinyin_text(
                    Trim(new_tail_key));
                local_partial_candidate.score := Max(0, top_candidate.score - 80);
                local_partial_candidate.source := top_candidate.source;
                local_partial_candidate.has_dict_weight := False;
                local_partial_candidate.dict_weight := 0;

                SetLength(candidates, Length(candidates) + 1);
                if Length(m_candidate_segment_paths) < Length(candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(candidates));
                end;
                if local_insert_index > High(candidates) then
                begin
                    local_insert_index := High(candidates);
                end;
                for local_move_idx := High(candidates) downto
                    local_insert_index + 1 do
                begin
                    candidates[local_move_idx] := candidates[local_move_idx - 1];
                    if local_move_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[local_move_idx] :=
                            m_candidate_segment_paths[local_move_idx - 1];
                    end;
                end;
                candidates[local_insert_index] := local_partial_candidate;
                if local_insert_index < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[local_insert_index] :=
                        Trim(new_prefix_path);
                end;
                Result := True;
            end;
        begin
            Result := False;
            if (Length(candidates) = 0) or (visible_limit <= 1) then
            begin
                Exit;
            end;

            top_candidate := candidates[0];
            if (Trim(top_candidate.comment) <> '') or
                (get_candidate_text_unit_count(Trim(top_candidate.text)) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            top_path := get_segment_path_for_candidate(top_candidate, 0);
            if Trim(top_path) = '' then
            begin
                top_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(top_candidate.text), inferred_path_score,
                    inferred_path_segments);
                if get_encoded_path_segment_count_local(top_path) > 1 then
                begin
                    remember_segment_path_for_candidate(Trim(top_candidate.text),
                        '', top_path, inferred_path_score);
                end;
            end;
            if get_encoded_path_segment_count_local(top_path) <= 1 then
            begin
                top_units := split_text_units(Trim(top_candidate.text));
                if Length(top_units) = input_syllable_count then
                begin
                    prefix_text := '';
                    for top_unit_idx := 0 to High(top_units) - 1 do
                    begin
                        prefix_text := prefix_text + top_units[top_unit_idx];
                    end;
                    tail_key := build_tail_syllable_key_local(1);
                    Exit(insert_prefix_partial_local(prefix_text, tail_key, ''));
                end;
                Exit(False);
            end;

            path_parts := top_path.Split([c_segment_path_separator]);
            if Length(path_parts) <= 1 then
            begin
                Exit;
            end;

            tail_text := Trim(path_parts[High(path_parts)]);
            tail_units := get_candidate_text_unit_count(tail_text);
            if (tail_text = '') or (tail_units <= 0) or
                (tail_units >= input_syllable_count) then
            begin
                Exit;
            end;

            tail_key := build_tail_syllable_key_local(tail_units);
            if tail_key = '' then
            begin
                Exit;
            end;

            prefix_text := '';
            for prefix_idx := 0 to High(path_parts) - 1 do
            begin
                prefix_text := prefix_text + Trim(path_parts[prefix_idx]);
            end;
            if (prefix_text = '') or
                (get_candidate_text_unit_count(prefix_text) < 3) or
                (get_candidate_text_unit_count(prefix_text) + tail_units <>
                input_syllable_count) then
            begin
                Exit;
            end;

            prefix_path := build_prefix_path_local(path_parts,
                High(path_parts) - 1);
            Result := insert_prefix_partial_local(prefix_text, tail_key,
                prefix_path);
        end;
        function is_strong_long_exact_prefix_partial_local(
            const candidate: TncCandidate): Boolean;
        var
            local_text_units: Integer;
            local_comment_syllables: Integer;
        begin
            Result := False;
            if Trim(candidate.comment) = '' then
            begin
                Exit;
            end;

            local_text_units := get_candidate_text_unit_count(Trim(candidate.text));
            if local_text_units < 3 then
            begin
                Exit;
            end;

            local_comment_syllables := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate.comment));
            if local_comment_syllables <= 0 then
            begin
                Exit;
            end;

            if local_text_units + local_comment_syllables <> input_syllable_count then
            begin
                Exit;
            end;

            Result := is_strong_exact_partial_candidate_for_visibility(candidate);
        end;
    begin
        if (Length(candidates) = 0) or (visible_limit <= 0) then
        begin
            Exit;
        end;

        if (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) = input_syllable_count) then
        begin
            if ensure_top_complete_derived_prefix_partial_local then
            begin
                Exit;
            end;

            if get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[0])) > 1 then
            begin
                Exit;
            end;
        end;

        if not has_long_sentence_extendable_tail_for_visibility_local then
        begin
            Exit;
        end;

        if is_full_pinyin_key(lookup_text) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) >= 2) and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) >=
            input_syllable_count - 1) and
            (candidates[0].has_dict_weight or
            (get_encoded_path_segment_count_local(
            get_segment_path_for_candidate(candidates[0])) <= 1)) then
        begin
            Exit;
        end;

        if is_long_sentence_prefix_partial_candidate_for_visibility(candidates[0]) then
        begin
            Exit;
        end;

        best_index := -1;
        best_units := -1;
        best_score := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if not is_long_sentence_prefix_partial_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;

            current_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if (current_units > best_units) or
                ((current_units = best_units) and (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_units := current_units;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index < 0 then
        begin
            for idx := 0 to High(candidates) do
            begin
                if not is_strong_long_exact_prefix_partial_local(candidates[idx]) then
                begin
                    Continue;
                end;

                current_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
                if (current_units > best_units) or
                    ((current_units = best_units) and
                    (candidates[idx].score > best_score)) then
                begin
                    best_index := idx;
                    best_units := current_units;
                    best_score := candidates[idx].score;
                end;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    function is_long_sentence_exact_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    var
        exact_text_units: Integer;
        segment_path: string;
    begin
        Result := False;
        if input_syllable_count < c_long_sentence_exact_min_syllables then
        begin
            Exit;
        end;

        if Trim(candidate.comment) <> '' then
        begin
            Exit;
        end;

        exact_text_units := get_candidate_text_unit_count(Trim(candidate.text));
        if exact_text_units <> input_syllable_count then
        begin
            Exit;
        end;

        segment_path := get_segment_path_for_candidate(candidate);
        if get_encoded_path_segment_count_local(segment_path) <= 0 then
        begin
            Exit;
        end;

        Result := True;
    end;

    function is_long_sentence_partial_exact_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    var
        segment_path: string;
        segment_parts: TArray<string>;
    begin
        Result := False;
        if not is_long_sentence_exact_candidate_for_visibility(candidate) then
        begin
            Exit;
        end;

        segment_path := get_segment_path_for_candidate(candidate);
        if Trim(segment_path) = '' then
        begin
            Exit;
        end;
        segment_parts := segment_path.Split([c_segment_path_separator]);
        if Length(segment_parts) <= 1 then
        begin
            Exit;
        end;
        if get_candidate_text_unit_count(Trim(segment_parts[High(segment_parts)])) <> 1 then
        begin
            Exit;
        end;
        if not is_natural_predictive_segment_path_local(segment_path) then
        begin
            Exit;
        end;

        Result := True;
    end;

    procedure ensure_long_sentence_exact_candidate_visible(var candidates: TncCandidateList);
    var
        best_index: Integer;
        best_score: Integer;
        idx: Integer;
        picked: TncCandidate;
        debug_seen_count: Integer;
        debug_first_seen: string;
        debug_full_length_count: Integer;
        debug_first_full_text: string;
        debug_first_full_segments: Integer;
        debug_candidate_segments: Integer;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        debug_seen_count := 0;
        debug_first_seen := '';
        debug_full_length_count := 0;
        debug_first_full_text := '';
        debug_first_full_segments := 0;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) =
                input_syllable_count) then
            begin
                Inc(debug_full_length_count);
                if debug_first_full_text = '' then
                begin
                    debug_first_full_text := Trim(candidates[idx].text);
                    debug_first_full_segments := get_encoded_path_segment_count_local(
                        get_segment_path_for_candidate(candidates[idx]));
                end;
            end;

            if not is_long_sentence_exact_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;

            Inc(debug_seen_count);
            if debug_first_seen = '' then
            begin
                debug_first_seen := Trim(candidates[idx].text);
            end;

            if candidates[idx].score > best_score then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index <= 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' texact=[none seen=%d first=%s full=%d firstfull=%s seg=%d]',
                    [debug_seen_count, debug_first_seen, debug_full_length_count,
                    debug_first_full_text, debug_first_full_segments]);
            end;
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            debug_candidate_segments := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(picked));
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' texact=[idx=%d text=%s seg=%d]',
                [best_index, Trim(picked.text), debug_candidate_segments]);
        end;
    end;

    procedure ensure_best_full_length_pathless_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_pathless_exact_visibility_margin = 480;
    var
        best_index: Integer;
        best_score: Integer;
        best_segment_count: Integer;
        best_path_confidence: Integer;
        best_path_tier: Integer;
        top_units: Integer;
        idx: Integer;
        picked: TncCandidate;
        top_path_segments: Integer;
        top_path_confidence: Integer;
        candidate_path_segments: Integer;
        candidate_path_confidence: Integer;
        candidate_path_tier: Integer;
    begin
        if (Length(candidates) <= 1) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
        (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        top_units := get_candidate_text_unit_count(Trim(candidates[0].text));
        top_path_segments := get_encoded_path_segment_count_local(
            get_segment_path_for_candidate(candidates[0]));
        top_path_confidence := get_candidate_path_confidence_score(candidates[0]);
        if (Trim(candidates[0].comment) <> '') or
            (top_units <> input_syllable_count) then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        best_segment_count := MaxInt;
        best_path_confidence := Low(Integer);
        best_path_tier := -1;
        for idx := 1 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            if is_runtime_chain_candidate(candidates[idx]) or
                is_runtime_common_pattern_candidate(candidates[idx]) or
                is_runtime_redup_candidate(candidates[idx]) then
            begin
                Continue;
            end;

            candidate_path_segments := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[idx]));
            if candidate_path_segments <= 1 then
            begin
                Continue;
            end;
            candidate_path_confidence := get_candidate_path_confidence_score(
                candidates[idx]);
            candidate_path_tier := get_candidate_path_confidence_tier(
                candidates[idx]);

            if (top_path_segments > 1) and
                (candidate_path_segments >= top_path_segments) and
                (candidate_path_confidence <= top_path_confidence + 64) then
            begin
                Continue;
            end;
            if (candidate_path_segments * 3) > input_syllable_count then
            begin
                Continue;
            end;
            if (top_path_segments <= 1) and
                (candidate_path_tier <= 0) and
                (candidate_path_confidence < 640) then
            begin
                Continue;
            end;

            if (top_path_segments > 1) and
                (candidates[idx].score + c_pathless_exact_visibility_margin <
                candidates[0].score) then
            begin
                Continue;
            end;

            if (best_index < 0) or
                (candidate_path_tier > best_path_tier) or
                ((candidate_path_tier = best_path_tier) and
                (candidate_path_confidence > best_path_confidence)) or
                ((candidate_path_tier = best_path_tier) and
                (candidate_path_confidence = best_path_confidence) and
                (candidate_path_segments < best_segment_count)) or
                ((candidate_path_tier = best_path_tier) and
                (candidate_path_confidence = best_path_confidence) and
                (candidate_path_segments = best_segment_count) and
                (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
                best_segment_count := candidate_path_segments;
                best_path_confidence := candidate_path_confidence;
                best_path_tier := candidate_path_tier;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure infer_paths_for_front_full_length_complete_candidates(
        var candidates: TncCandidateList);
    const
        c_infer_front_limit = 6;
    var
        idx: Integer;
        inferred_score: Integer;
        inferred_segment_count: Integer;
        inferred_path: string;
        candidate_text: string;
    begin
        if (Length(candidates) = 0) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        for idx := 0 to Min(c_infer_front_limit - 1, High(candidates)) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text := Trim(candidates[idx].text);
            if (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            if get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[idx])) > 1 then
            begin
                Continue;
            end;

            inferred_path := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, inferred_score, inferred_segment_count);
            if (inferred_path = '') or (inferred_segment_count <= 1) then
            begin
                Continue;
            end;

            remember_segment_path_for_candidate(candidate_text, '',
                inferred_path, candidates[idx].score + Min(2400, inferred_score div 4));
        end;
    end;

    procedure ensure_best_inferred_full_length_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_infer_front_limit = 6;
        c_infer_promote_margin = 0;
    var
        idx: Integer;
        best_index: Integer;
        best_total_score: Integer;
        best_segment_count: Integer;
        best_path: string;
        current_total_score: Integer;
        current_inferred_score: Integer;
        current_segment_count: Integer;
        current_path: string;
        candidate_text: string;
        picked: TncCandidate;

        function build_total_score_local(const candidate_score: Integer;
            const inferred_score_local: Integer;
            const segment_count_local: Integer): Integer;
        begin
            Result := (inferred_score_local * 2) + (candidate_score div 10) -
                (segment_count_local * 64);
        end;
    begin
        if (Length(candidates) <= 1) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_index := -1;
        best_total_score := Low(Integer);
        best_segment_count := MaxInt;
        best_path := '';
        for idx := 0 to Min(c_infer_front_limit - 1, High(candidates)) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text := Trim(candidates[idx].text);
            if (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <>
                input_syllable_count) then
            begin
                Continue;
            end;

            current_path := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, current_inferred_score,
                current_segment_count);
            if (current_path = '') or (current_segment_count <= 1) then
            begin
                Continue;
            end;

            remember_segment_path_for_candidate(candidate_text, '',
                current_path, candidates[idx].score + Min(2400,
                current_inferred_score div 4));
            current_total_score := build_total_score_local(candidates[idx].score,
                current_inferred_score, current_segment_count);

            if (best_index < 0) or
                (current_total_score > best_total_score) or
                ((current_total_score = best_total_score) and
                (current_segment_count < best_segment_count)) then
            begin
                best_index := idx;
                best_total_score := current_total_score;
                best_segment_count := current_segment_count;
                best_path := current_path;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        current_path := infer_segment_path_for_query_text_with_score(
            lookup_text, Trim(candidates[0].text), current_inferred_score,
            current_segment_count);
        if (current_path <> '') and (current_segment_count > 1) then
        begin
            current_total_score := build_total_score_local(candidates[0].score,
                current_inferred_score, current_segment_count);
            if best_total_score < current_total_score + c_infer_promote_margin then
            begin
                Exit;
            end;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    function is_exact_dict_complete_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    begin
        Result := (Trim(candidate.comment) = '') and candidate.has_dict_weight;
    end;

    procedure ensure_exact_dict_complete_candidate_visible(var candidates: TncCandidateList);
    const
        c_explicit_query_choice_min_bonus = 200;
    var
        best_index: Integer;
        best_score: Integer;
        best_dict_weight: Integer;
        idx: Integer;
        picked: TncCandidate;
        normalized_query: string;
        top_text: string;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 2) or
            (input_syllable_count > 4) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        if is_exact_dict_complete_candidate_for_visibility(candidates[0]) then
        begin
            Exit;
        end;
        top_text := Trim(candidates[0].text);
        normalized_query := normalize_pinyin_text(lookup_text);
        if (top_text <> '') and (candidates[0].source = cs_user) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count) and
            ((is_latest_session_query_choice(top_text)) or
            ((m_dictionary <> nil) and
            (m_dictionary.get_query_choice_bonus(normalized_query, top_text) >=
            c_explicit_query_choice_min_bonus))) then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        best_dict_weight := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if not is_exact_dict_complete_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;

            if (candidates[idx].dict_weight > best_dict_weight) or
                ((candidates[idx].dict_weight = best_dict_weight) and
                (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
                best_dict_weight := candidates[idx].dict_weight;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_short_full_query_exact_cluster_visible(
        var candidates: TncCandidateList);
    const
        c_max_exact_cluster = 3;
        c_exact_probe_limit = 12;
    var
        exact_results: TncCandidateList;
        exact_candidates: TncCandidateList;
        exact_idx: Integer;
        insert_idx: Integer;
        existing_idx: Integer;
        candidate: TncCandidate;
        candidate_text: string;

        function get_query_choice_priority_local(
            const candidate_text_value: string): Integer;
        var
            normalized_query_local: string;
            latest_text_local: string;
            choice_bonus_local: Integer;
            text_key_local: string;
        begin
            Result := 0;
            text_key_local := Trim(candidate_text_value);
            if text_key_local = '' then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(lookup_text);
            if normalized_query_local = '' then
            begin
                Exit;
            end;

            if is_latest_session_query_choice(text_key_local) then
            begin
                Inc(Result, 200000);
            end;

            if m_dictionary <> nil then
            begin
                latest_text_local := get_cached_query_latest_choice_text(
                    normalized_query_local);
                if (latest_text_local <> '') and
                    SameText(text_key_local, latest_text_local) then
                begin
                    Inc(Result, 200000);
                end;

                choice_bonus_local := m_dictionary.get_query_choice_bonus(
                    normalized_query_local, text_key_local);
                if choice_bonus_local > 0 then
                begin
                    Inc(Result, 50000 + (choice_bonus_local * 20));
                end;
            end;
        end;

        function candidate_effective_weight_local(
            const item: TncCandidate): Integer;
        begin
            Result := item.score;
            if item.has_dict_weight and (item.dict_weight > Result) then
            begin
                Result := item.dict_weight;
            end;
            Inc(Result, get_query_choice_priority_local(item.text));
        end;

        function is_eligible_exact_candidate(
            const item: TncCandidate): Boolean;
        var
            text_value: string;
        begin
            text_value := Trim(item.text);
            Result := (text_value <> '') and (Trim(item.comment) = '') and
                (get_candidate_text_unit_count(text_value) =
                input_syllable_count);
            if Result and has_explicit_apostrophe_input then
            begin
                Result := matches_explicit_apostrophe_unit_alignment(
                    text_value);
            end;
        end;

        function exact_text_already_selected(const text_value: string): Boolean;
        var
            idx_local: Integer;
        begin
            Result := False;
            for idx_local := 0 to High(exact_candidates) do
            begin
                if SameText(Trim(exact_candidates[idx_local].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function same_candidate_text_and_comment(const left_value,
            right_value: TncCandidate): Boolean;
        begin
            Result := SameText(Trim(left_value.text), Trim(right_value.text)) and
                SameText(Trim(left_value.comment), Trim(right_value.comment));
        end;

        procedure sort_exact_candidates_by_weight;
        var
            left_idx: Integer;
            right_idx: Integer;
            tmp_candidate: TncCandidate;
        begin
            for left_idx := 0 to High(exact_candidates) - 1 do
            begin
                for right_idx := left_idx + 1 to High(exact_candidates) do
                begin
                    if candidate_effective_weight_local(exact_candidates[right_idx]) >
                        candidate_effective_weight_local(exact_candidates[left_idx]) then
                    begin
                        tmp_candidate := exact_candidates[left_idx];
                        exact_candidates[left_idx] := exact_candidates[right_idx];
                        exact_candidates[right_idx] := tmp_candidate;
                    end;
                end;
            end;
        end;
        function find_existing_candidate_from_index(
            const item: TncCandidate; const start_idx: Integer): Integer;
        var
            idx_local: Integer;
        begin
            Result := -1;
            for idx_local := Max(0, start_idx) to High(candidates) do
            begin
                if same_candidate_text_and_comment(candidates[idx_local],
                    item) then
                begin
                    Exit(idx_local);
                end;
            end;
        end;

        procedure remove_candidate_at(var list: TncCandidateList;
            const remove_idx: Integer);
        var
            idx_local: Integer;
        begin
            if (remove_idx < 0) or (remove_idx > High(list)) then
            begin
                Exit;
            end;

            for idx_local := remove_idx to High(list) - 1 do
            begin
                list[idx_local] := list[idx_local + 1];
            end;
            SetLength(list, Length(list) - 1);
        end;

        procedure insert_candidate_at(var list: TncCandidateList;
            const target_idx: Integer; const item: TncCandidate);
        var
            idx_local: Integer;
            safe_idx: Integer;
        begin
            safe_idx := target_idx;
            if safe_idx < 0 then
            begin
                safe_idx := 0;
            end;
            if safe_idx > Length(list) then
            begin
                safe_idx := Length(list);
            end;

            SetLength(list, Length(list) + 1);
            for idx_local := High(list) downto safe_idx + 1 do
            begin
                list[idx_local] := list[idx_local - 1];
            end;
            list[safe_idx] := item;
        end;
    begin
        if (m_dictionary = nil) or (input_syllable_count < 2) or
            (input_syllable_count > 4) or
            ((not has_explicit_apostrophe_input) and
            (not is_full_pinyin_key(lookup_text))) then
        begin
            Exit;
        end;
        if has_explicit_apostrophe_input then
        begin
            exact_results := Copy(candidates, 0, Length(candidates));
        end
        else if not lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results) then
        begin
            Exit;
        end;

        SetLength(exact_candidates, 0);
        for exact_idx := 0 to High(exact_results) do
        begin
            if exact_idx >= c_exact_probe_limit then
            begin
                Break;
            end;

            if not is_eligible_exact_candidate(exact_results[exact_idx]) then
            begin
                Continue;
            end;

            candidate_text := Trim(exact_results[exact_idx].text);
            if exact_text_already_selected(candidate_text) then
            begin
                Continue;
            end;

            SetLength(exact_candidates, Length(exact_candidates) + 1);
            exact_candidates[High(exact_candidates)] := exact_results[exact_idx];
            exact_candidates[High(exact_candidates)].text := candidate_text;
            exact_candidates[High(exact_candidates)].comment := '';
        end;

        if Length(exact_candidates) = 0 then
        begin
            Exit;
        end;

        sort_exact_candidates_by_weight;
        if Length(exact_candidates) > Min(c_max_exact_cluster,
            get_candidate_limit) then
        begin
            SetLength(exact_candidates, Min(c_max_exact_cluster,
                get_candidate_limit));
        end;
        insert_idx := 0;
        if Length(candidates) > 0 then
        begin
            if same_candidate_text_and_comment(candidates[0],
                exact_candidates[0]) then
            begin
                insert_idx := 1;
            end
            else if (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count) and
                (get_query_choice_priority_local(candidates[0].text) > 0) and
                (get_query_choice_priority_local(candidates[0].text) >=
                get_query_choice_priority_local(exact_candidates[0].text)) then
            begin
                insert_idx := 1;
            end
            else
            begin
                insert_idx := 0;
            end;
        end;

        for exact_idx := High(exact_candidates) downto 0 do
        begin
            candidate := exact_candidates[exact_idx];
            if (Length(candidates) > 0) and
                same_candidate_text_and_comment(candidates[0], candidate) then
            begin
                Continue;
            end;

            existing_idx := find_existing_candidate_from_index(candidate, 1);
            if existing_idx >= 0 then
            begin
                candidate := candidates[existing_idx];
                remove_candidate_at(candidates, existing_idx);
            end;

            if insert_idx > Length(candidates) then
            begin
                insert_idx := Length(candidates);
            end;
            insert_candidate_at(candidates, insert_idx, candidate);
        end;
    end;

    procedure ensure_full_query_exact_complete_candidate_visible(
        var candidates: TncCandidateList);
    type
        TncFullQueryChunkWindow = record
            start_idx: Integer;
            span_syllables: Integer;
            resolved_text: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_has_anchor: Boolean;
            window_weight: Integer;
        end;
    var
        exact_results: TncCandidateList;
        oracle_candidates: TncCandidateList;
        best_candidate: TncCandidate;
        best_text: string;
        best_weight: Integer;
        best_score: Integer;
        best_phrase_bonus: Integer;
        best_support_score: Integer;
        best_chunk_alignment_score: Integer;
        best_rerank_score: Integer;
        existing_index: Integer;
        idx: Integer;
        weight_value: Integer;
        phrase_bonus: Integer;
        preferred_candidate: TncCandidate;
        support_score: Integer;
        top_first_unit: string;
        top_requires_first_unit_match: Boolean;
        best_path_confidence_score: Integer;
        current_path_confidence_score: Integer;
        best_path_preference_score: Integer;
        current_path_preference_score: Integer;
        best_head_boundary_score: Integer;
        current_head_boundary_score: Integer;
        current_chunk_alignment_score: Integer;
        current_rerank_score: Integer;
        debug_full_candidate_summary: string;
        debug_full_candidate_count: Integer;
        effective_syllables: TncPinyinParseResult;
        last_syllable_text: string;
        best_exact_backed_local: Boolean;
        trust_exact_best_local: Boolean;
        query_choice_bonus: Integer;
        adjusted_score: Integer;
        chunk_windows_prepared: Boolean;
        chunk_windows: TArray<TncFullQueryChunkWindow>;

        function get_candidate_weight_local(
            const candidate_local: TncCandidate): Integer;
        begin
            Result := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > Result) then
            begin
                Result := candidate_local.dict_weight;
            end;
        end;

        function get_first_text_unit_local(const value: string): string;
        var
            units: TArray<string>;
        begin
            Result := '';
            units := split_text_units(Trim(value));
            if Length(units) > 0 then
            begin
                Result := units[0];
            end;
        end;

        function has_exact_single_char_tail_local(const value: string): Boolean;
        const
            c_tail_probe_limit = 12;
        var
            local_value: string;
            local_results: TncCandidateList;
            local_idx: Integer;
        begin
            Result := False;
            local_value := normalize_pinyin_text(value);
            if (local_value = '') or (m_dictionary = nil) or
                (not m_dictionary.lookup(local_value, local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(local_results[local_idx].text)) = 1 then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_get_best_covering_partial_anchor_index_local(
            out out_index_local: Integer): Boolean;
        var
            local_idx: Integer;
            partial_units_local: Integer;
            comment_units_local: Integer;
            candidate_weight_local: Integer;
            best_units_local: Integer;
            best_weight_local: Integer;
        begin
            out_index_local := -1;
            best_units_local := -1;
            best_weight_local := Low(Integer);
            for local_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_idx].comment) = '' then
                begin
                    Continue;
                end;

                partial_units_local := get_candidate_text_unit_count(
                    Trim(candidates[local_idx].text));
                if partial_units_local <= 0 then
                begin
                    Continue;
                end;

                comment_units_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(candidates[local_idx].comment)));
                if (comment_units_local <= 0) or
                    (partial_units_local + comment_units_local <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                candidate_weight_local := get_candidate_weight_local(
                    candidates[local_idx]);
                if (partial_units_local > best_units_local) or
                    ((partial_units_local = best_units_local) and
                    (candidate_weight_local > best_weight_local)) then
                begin
                    out_index_local := local_idx;
                    best_units_local := partial_units_local;
                    best_weight_local := candidate_weight_local;
                end;
            end;
            Result := out_index_local >= 0;
        end;

        procedure promote_partial_anchor_to_front_local;
        var
            partial_index_local: Integer;
            picked_candidate_local: TncCandidate;
        begin
            if not try_get_best_covering_partial_anchor_index_local(
                partial_index_local) or (partial_index_local <= 0) then
            begin
                Exit;
            end;

            picked_candidate_local := candidates[partial_index_local];
            while partial_index_local > 0 do
            begin
                candidates[partial_index_local] :=
                    candidates[partial_index_local - 1];
                Dec(partial_index_local);
            end;
            candidates[0] := picked_candidate_local;
        end;

        function should_defer_weak_exact_complete_for_prefix_query_local: Boolean;
        var
            partial_index_local: Integer;
        begin
            Result := False;
            if not try_get_best_covering_partial_anchor_index_local(
                partial_index_local) then
            begin
                Exit;
            end;

            if (best_support_score > 0) or (best_path_confidence_score > 0) or
                (best_weight >= 4096) or (best_phrase_bonus >= 260) then
            begin
                Exit;
            end;

            Result := (not is_full_pinyin_key(lookup_text)) or
                has_internal_dangling_initial or head_only_multi_syllable or
                has_short_extendable_tail_prefix_local or
                has_precise_extendable_tail_syllable_local(lookup_text);
        end;

        function get_fixed_boundary_single_char_for_query_syllable_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'bei' then
            begin
                Result := string(Char($88AB));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'jiu' then
            begin
                Result := string(Char($5C31));
            end
            else if normalized_syllable = 'bu' then
            begin
                Result := string(Char($4E0D));
            end;
        end;

        function get_fixed_boundary_head_score_local(
            const candidate_text: string): Integer;
        var
            syllables_local: TncPinyinParseResult;
            text_units_local: TArray<string>;
            head_idx: Integer;
            expected_char: string;
        begin
            Result := 0;
            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            text_units_local := split_text_units(Trim(candidate_text));
            for head_idx := 0 to Min(Min(High(syllables_local), High(text_units_local)), 3) do
            begin
                expected_char := get_fixed_boundary_single_char_for_query_syllable_local(
                    syllables_local[head_idx].text);
                if expected_char = '' then
                begin
                    Continue;
                end;
                if SameText(text_units_local[head_idx], expected_char) then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function has_material_support_advantage(const candidate_support: Integer;
            const current_support: Integer): Boolean;
        var
            support_margin: Integer;
        begin
            if input_syllable_count <= 4 then
            begin
                support_margin := 128;
            end
            else if input_syllable_count <= 6 then
            begin
                support_margin := 256;
            end
            else
            begin
                support_margin := 512;
            end;
            Result := candidate_support > current_support + support_margin;
        end;

        function get_common_prefix_unit_count_local(const left_text: string;
            const right_text: string): Integer;
        var
            left_units: TArray<string>;
            right_units: TArray<string>;
            unit_idx: Integer;
        begin
            Result := 0;
            left_units := split_text_units(Trim(left_text));
            right_units := split_text_units(Trim(right_text));
            for unit_idx := 0 to Min(High(left_units), High(right_units)) do
            begin
                if not SameText(left_units[unit_idx], right_units[unit_idx]) then
                begin
                    Break;
                end;
                Inc(Result);
            end;
        end;

        function build_text_from_units_local(const text_units: TArray<string>;
            const start_unit: Integer; const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (unit_count <= 0) or (start_unit < 0) or
                (start_unit + unit_count - 1 > High(text_units)) then
            begin
                Exit;
            end;

            for unit_idx := start_unit to start_unit + unit_count - 1 do
            begin
                Result := Result + text_units[unit_idx];
            end;
        end;

        procedure prepare_full_query_chunk_windows_local;
        const
            c_chunk_min_syllables = 2;
            c_chunk_max_syllables = 5;
            c_chunk_full_query_max_syllables = 24;
        var
            start_idx_local: Integer;
            span_syllables_local: Integer;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
            window_weight_local: Integer;
            window_idx_local: Integer;
            window_resolved_local: Boolean;
            max_chunk_syllables_local: Integer;
        begin
            if chunk_windows_prepared then
            begin
                Exit;
            end;
            chunk_windows_prepared := True;
            SetLength(chunk_windows, 0);
            if (not is_full_pinyin_key(lookup_text)) or
                (input_syllable_count < 3) or
                (input_syllable_count > c_chunk_full_query_max_syllables) or
                (m_disable_subspan_standalone_oracle > 0) then
            begin
                Exit;
            end;

            max_chunk_syllables_local := c_chunk_max_syllables;

            for start_idx_local := 0 to input_syllable_count -
                c_chunk_min_syllables do
            begin
                for span_syllables_local := c_chunk_min_syllables to
                    Min(max_chunk_syllables_local,
                    input_syllable_count - start_idx_local) do
                begin
                    window_resolved_local := try_resolve_best_exact_subspan_local(
                        start_idx_local, span_syllables_local,
                        resolved_text_local, resolved_score_local,
                        resolved_segments_local, resolved_path_local,
                        resolved_has_anchor_local,
                        resolved_anchor_units_local);
                    if not window_resolved_local then
                    begin
                        Continue;
                    end;
                    if resolved_text_local = '' then
                    begin
                        Continue;
                    end;

                    window_weight_local := Min(4096,
                        Max(512, resolved_score_local div 8));
                    Inc(window_weight_local, span_syllables_local * 96);
                    if resolved_has_anchor_local then
                    begin
                        Inc(window_weight_local, 256);
                    end;
                    if start_idx_local + span_syllables_local =
                        input_syllable_count then
                    begin
                        Inc(window_weight_local, window_weight_local * 2);
                        if span_syllables_local <= 2 then
                        begin
                            Inc(window_weight_local, window_weight_local * 2);
                        end
                        else if span_syllables_local = 3 then
                        begin
                            Inc(window_weight_local, window_weight_local);
                        end;
                    end
                    else if start_idx_local + span_syllables_local =
                        input_syllable_count - 1 then
                    begin
                        Inc(window_weight_local, window_weight_local div 2);
                    end;

                    window_idx_local := Length(chunk_windows);
                    SetLength(chunk_windows, window_idx_local + 1);
                    chunk_windows[window_idx_local].start_idx := start_idx_local;
                    chunk_windows[window_idx_local].span_syllables :=
                        span_syllables_local;
                    chunk_windows[window_idx_local].resolved_text :=
                        resolved_text_local;
                    chunk_windows[window_idx_local].resolved_score :=
                        resolved_score_local;
                    chunk_windows[window_idx_local].resolved_segments :=
                        resolved_segments_local;
                    chunk_windows[window_idx_local].resolved_has_anchor :=
                        resolved_has_anchor_local;
                    chunk_windows[window_idx_local].window_weight :=
                        window_weight_local;
                end;
            end;
        end;

        function get_full_query_chunk_alignment_score_local(
            const candidate_text: string): Integer;
        var
            candidate_units_local: TArray<string>;
            candidate_chunk_text_local: string;
            window_idx_local: Integer;
            window_local: TncFullQueryChunkWindow;
        begin
            Result := 0;
            if (not is_full_pinyin_key(lookup_text)) or
                (input_syllable_count < 3) or
                (m_disable_subspan_standalone_oracle > 0) then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate_text));
            if Length(candidate_units_local) <> input_syllable_count then
            begin
                Exit;
            end;

            prepare_full_query_chunk_windows_local;
            for window_idx_local := 0 to High(chunk_windows) do
            begin
                window_local := chunk_windows[window_idx_local];
                candidate_chunk_text_local := build_text_from_units_local(
                    candidate_units_local, window_local.start_idx,
                    window_local.span_syllables);
                if candidate_chunk_text_local = '' then
                begin
                    Continue;
                end;

                if SameText(candidate_chunk_text_local,
                    window_local.resolved_text) then
                begin
                    Inc(Result, window_local.window_weight);
                    if (window_local.start_idx + window_local.span_syllables =
                        input_syllable_count) and
                        (window_local.span_syllables = 2) and
                        ((window_local.resolved_segments <= 1) or
                        (window_local.resolved_score >= 4096)) then
                    begin
                        Inc(Result, window_local.window_weight * 3);
                    end;
                    if (window_local.span_syllables = 2) and
                        ((window_local.resolved_segments <= 1) or
                        (window_local.resolved_score >= 4096)) then
                    begin
                        Inc(Result, window_local.window_weight div 2);
                    end;
                end
                else if (window_local.start_idx +
                    window_local.span_syllables =
                    input_syllable_count) and
                    (window_local.span_syllables = 2) and
                    ((window_local.resolved_segments <= 1) or
                    (window_local.resolved_score >= 4096)) then
                begin
                    Dec(Result, window_local.window_weight * 6);
                end
                else if (window_local.span_syllables = 2) and
                    ((window_local.resolved_segments <= 1) or
                    (window_local.resolved_score >= 4096)) then
                begin
                    Dec(Result, window_local.window_weight);
                end
                else if (window_local.span_syllables >= 4) or
                    window_local.resolved_has_anchor then
                begin
                    Dec(Result, window_local.window_weight div 2);
                end
                else if window_local.window_weight >= 1536 then
                begin
                    Dec(Result, window_local.window_weight div 3);
                end;
            end;
        end;

        function get_full_query_chunk_alignment_score_fast_local(
            const candidate_text: string; const candidate_support: Integer): Integer;
        begin
            if (input_syllable_count >= 10) and (candidate_support > -960) then
            begin
                Exit(0);
            end;

            Result := get_full_query_chunk_alignment_score_local(candidate_text);
        end;

        function allow_supportless_override_local(
            const candidate_support: Integer; const current_support: Integer;
            const candidate_chunk_alignment: Integer;
            const current_chunk_alignment: Integer;
            const candidate_rerank_score: Integer;
            const current_rerank_score: Integer): Boolean;
        begin
            Result := True;
            if (candidate_support <= -960) and (current_support > -960) then
            begin
                Result := False;
                Exit;
            end;
            if candidate_support > -960 then
            begin
                Exit;
            end;
            if current_support > -960 then
            begin
                Result := (candidate_chunk_alignment >
                    current_chunk_alignment + 4096) and
                    (candidate_rerank_score > current_rerank_score + 12000);
                Exit;
            end;

            Result := (candidate_chunk_alignment > current_chunk_alignment + 1600) and
                (candidate_rerank_score > current_rerank_score + 2400);
        end;

        function has_material_chunk_alignment_advantage_local(
            const candidate_chunk_alignment: Integer;
            const current_chunk_alignment: Integer;
            const candidate_support: Integer;
            const current_support: Integer): Boolean;
        begin
            Result := False;
            if candidate_chunk_alignment <= current_chunk_alignment + 2048 then
            begin
                Exit;
            end;
            if has_material_support_advantage(current_support, candidate_support) and
                (candidate_chunk_alignment <= current_chunk_alignment + 4096) then
            begin
                Exit;
            end;
            Result := True;
        end;

        function has_material_rerank_advantage_local(
            const candidate_rerank_score: Integer;
            const current_rerank_score: Integer;
            const candidate_support: Integer;
            const current_support: Integer): Boolean;
        begin
            Result := False;
            if (candidate_support <= -960) and (current_support > -960) then
            begin
                Exit;
            end;
            if candidate_rerank_score <= current_rerank_score + 1024 then
            begin
                Exit;
            end;

            if (input_syllable_count >= 8) and
                (candidate_support > -960) and (current_support > -960) and
                (candidate_rerank_score > current_rerank_score + 2048) and
                (current_support <= candidate_support + 4096) then
            begin
                Result := True;
                Exit;
            end;

            if has_material_support_advantage(current_support, candidate_support) and
                (candidate_rerank_score <= current_rerank_score + 6000) then
            begin
                Exit;
            end;

            Result := True;
        end;

        function allow_support_priority_override_local(
            const candidate_support: Integer;
            const current_support: Integer;
            const candidate_rerank_score: Integer;
            const current_rerank_score: Integer): Boolean;
        begin
            // Support still matters, but it must not blindly outrank a much
            // stronger full-query rerank. Otherwise frequent but misaligned
            // paths such as "鑷充簬/鍙兘" overpower better exact chunk chains.
            if current_support <= -960 then
            begin
                Exit(True);
            end;

            if input_syllable_count >= 4 then
            begin
                Result := candidate_rerank_score >= current_rerank_score + 256;
            end
            else
            begin
                Result := candidate_rerank_score >= current_rerank_score - 2048;
            end;
        end;

        function allow_path_priority_override_local(
            const candidate_rerank_score: Integer;
            const current_rerank_score: Integer;
            const candidate_support: Integer;
            const current_support: Integer): Boolean;
        begin
            Result := not has_material_rerank_advantage_local(
                current_rerank_score, candidate_rerank_score,
                current_support, candidate_support);
        end;

        function build_full_query_rerank_score_local(
            const candidate_score: Integer;
            const chunk_alignment_score: Integer;
            const support_score_value: Integer;
            const head_boundary_score_value: Integer): Integer;
        var
            capped_score_local: Integer;
        begin
            if support_score_value > -960 then
            begin
                capped_score_local := Min(candidate_score, 24000);
                if candidate_score > 24000 then
                begin
                    Inc(capped_score_local, Min(12000,
                        (candidate_score - 24000) div 2));
                end;
            end
            else
            begin
                capped_score_local := Min(candidate_score, 12000);
            end;

            Result := capped_score_local + (chunk_alignment_score * 2) +
                (head_boundary_score_value * 8192);
            if (support_score_value <= -960) and
                (head_boundary_score_value <= 0) then
            begin
                Dec(Result, 12000);
            end;
        end;

        function prefer_long_same_prefix_score_override_local(
            const candidate_text: string; const candidate_support: Integer;
            const candidate_score: Integer; const current_text: string;
            const current_support: Integer; const current_score: Integer): Boolean;
        const
            c_support_tolerance_local = 8192;
            c_score_margin_local = 1200;
            c_large_score_margin_local = 6000;
        begin
            Result := False;
            if input_syllable_count < 4 then
            begin
                Exit;
            end;
            if get_common_prefix_unit_count_local(candidate_text, current_text) < 1 then
            begin
                Exit;
            end;
            if candidate_score <= current_score + c_score_margin_local then
            begin
                Exit;
            end;

            if (candidate_support > -960) and (current_support > -960) and
                (candidate_support + c_support_tolerance_local < current_support) and
                (candidate_score <= current_score + c_large_score_margin_local) then
            begin
                Exit;
            end;

            Result := True;
        end;

        function get_full_query_support_score_local(
            const candidate_text: string): Integer;
        var
            support_path_local: string;
            support_score_local: Integer;
            support_segments_local: Integer;
        begin
            if (candidate_text = '') or (input_syllable_count < 3) then
            begin
                Exit(0);
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, support_score_local,
                support_segments_local);
            if support_path_local <> '' then
            begin
                Result := support_score_local + (support_segments_local * 320);
            end
            else
            begin
                Result := -960;
            end;
        end;

        function get_full_query_choice_bonus_local(
            const candidate_text: string): Integer;
        begin
            Result := get_session_query_bonus(candidate_text);
            if is_latest_session_query_choice(candidate_text) then
            begin
                Inc(Result, 1200);
            end;
        end;

        function has_material_exact_score_advantage_local(
            const candidate_weight: Integer; const current_weight: Integer;
            const candidate_adjusted_score: Integer;
            const current_adjusted_score: Integer;
            const candidate_support: Integer; const current_support: Integer): Boolean;
        var
            required_margin_local: Integer;
        begin
            Result := False;
            if candidate_adjusted_score <= current_adjusted_score then
            begin
                Exit;
            end;

            required_margin_local := 1024;
            if input_syllable_count >= 6 then
            begin
                required_margin_local := 2048;
            end;
            if input_syllable_count >= 10 then
            begin
                Inc(required_margin_local, 1024);
            end;

            if (candidate_support > -960) and (current_support > -960) and
                (current_support > candidate_support) then
            begin
                Inc(required_margin_local, Min(4096,
                    (current_support - candidate_support) div 2));
            end;

            if candidate_adjusted_score < current_adjusted_score +
                required_margin_local then
            begin
                Exit;
            end;

            Result := True;
        end;

        function should_trust_exact_best_local: Boolean;
        begin
            Result := best_exact_backed_local and (best_support_score > -960);
            if not Result then
            begin
                Exit;
            end;

            if best_chunk_alignment_score >= input_syllable_count * 3072 then
            begin
                Exit(True);
            end;

            // Fast long-query chunk scoring intentionally returns 0 for strong
            // supported exact candidates. Do not force those cases down the
            // expensive fallback/oracle path solely because chunk alignment was
            // skipped.
            if input_syllable_count >= 10 then
            begin
                Result := (best_support_score >= input_syllable_count * 1024) and
                    (best_score >= 12000);
                Exit;
            end;

            Result := False;
        end;

        function try_build_preferred_query_phrase_candidate_local(
            out out_candidate: TncCandidate): Boolean;
        var
            preferred_text: string;
            preferred_bonus_local: Integer;
            syllables_local: TncPinyinParseResult;
            text_units_local: TArray<string>;
            local_idx: Integer;
            local_is_preferred: Boolean;
            preferred_score: Integer;
        begin
            FillChar(out_candidate, SizeOf(out_candidate), 0);
            Result := False;
            if not try_get_preferred_query_phrase_text_local(lookup_text,
                preferred_text, preferred_bonus_local) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            text_units_local := split_text_units(preferred_text);
            if (Length(syllables_local) <> input_syllable_count) or
                (Length(text_units_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(text_units_local) do
            begin
                if not match_single_char_candidate_for_syllable(
                    syllables_local[local_idx].text, text_units_local[local_idx],
                    local_is_preferred) then
                begin
                    Exit(False);
                end;
            end;

            preferred_score := preferred_bonus_local + 2048;
            if Length(candidates) > 0 then
            begin
                preferred_score := Max(preferred_score, candidates[0].score + 2048);
            end;

            out_candidate.text := preferred_text;
            out_candidate.comment := '';
            out_candidate.score := preferred_score;
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := False;
            out_candidate.dict_weight := 0;
            remember_segment_path_for_candidate(out_candidate.text, '',
                out_candidate.text, out_candidate.score);
            Result := True;
        end;

        procedure maybe_promote_best_supported_full_candidate_local;
        var
            local_idx: Integer;
            local_candidate: TncCandidate;
            local_text: string;
            local_support_score: Integer;
            local_chunk_alignment_score: Integer;
            local_rerank_score: Integer;
            local_head_boundary_score: Integer;
            local_best_found: Boolean;
            local_best_candidate: TncCandidate;
            local_best_text: string;
            local_best_support_score: Integer;
            local_best_chunk_alignment_score: Integer;
            local_best_rerank_score: Integer;
            local_best_head_boundary_score: Integer;
        begin
            if (not is_full_pinyin_key(lookup_text)) or
                (input_syllable_count < 4) then
            begin
                Exit;
            end;

            local_best_found := False;
            local_best_text := '';
            local_best_support_score := Low(Integer);
            local_best_chunk_alignment_score := Low(Integer);
            local_best_rerank_score := Low(Integer);
            local_best_head_boundary_score := Low(Integer);

            for local_idx := 0 to High(candidates) do
            begin
                local_candidate := candidates[local_idx];
                if Trim(local_candidate.comment) <> '' then
                begin
                    Continue;
                end;
                local_text := Trim(local_candidate.text);
                if get_candidate_text_unit_count(local_text) <> input_syllable_count then
                begin
                    Continue;
                end;

                local_support_score := get_full_query_support_score_local(local_text);
                if local_support_score <= -960 then
                begin
                    Continue;
                end;

                local_head_boundary_score :=
                    get_fixed_boundary_head_score_local(local_text);
                local_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(local_text,
                    local_support_score);
                local_rerank_score := build_full_query_rerank_score_local(
                    local_candidate.score, local_chunk_alignment_score,
                    local_support_score, local_head_boundary_score);

                if (not local_best_found) or
                    has_material_chunk_alignment_advantage_local(
                    local_chunk_alignment_score,
                    local_best_chunk_alignment_score, local_support_score,
                    local_best_support_score) or
                    has_material_rerank_advantage_local(local_rerank_score,
                    local_best_rerank_score, local_support_score,
                    local_best_support_score) or
                    ((local_rerank_score = local_best_rerank_score) and
                    (local_head_boundary_score > local_best_head_boundary_score)) or
                    ((local_rerank_score = local_best_rerank_score) and
                    (local_head_boundary_score = local_best_head_boundary_score) and
                    has_material_support_advantage(local_support_score,
                    local_best_support_score)) then
                begin
                    local_best_found := True;
                    local_best_candidate := local_candidate;
                    local_best_text := local_text;
                    local_best_support_score := local_support_score;
                    local_best_chunk_alignment_score := local_chunk_alignment_score;
                    local_best_rerank_score := local_rerank_score;
                    local_best_head_boundary_score := local_head_boundary_score;
                end;
            end;

            if not local_best_found then
            begin
                Exit;
            end;

            if (best_support_score <= -960) and
                (input_syllable_count >= 8) and
                (local_best_support_score >= 4096) then
            begin
                best_candidate := local_best_candidate;
                best_text := local_best_text;
                best_weight := local_best_candidate.dict_weight;
                best_score := local_best_candidate.score;
                best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_text);
                best_support_score := local_best_support_score;
                best_chunk_alignment_score := local_best_chunk_alignment_score;
                best_rerank_score := local_best_rerank_score;
                best_path_confidence_score :=
                    get_candidate_path_confidence_score(local_best_candidate);
                best_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(local_best_candidate));
                best_head_boundary_score := local_best_head_boundary_score;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' fqvfix=[supported text=%s]',
                        [best_text]);
                end;
            end
            else if (best_support_score <= -960) and
                (best_rerank_score < local_best_rerank_score + 12000) and
                (best_chunk_alignment_score <
                local_best_chunk_alignment_score + 4096) then
            begin
                best_candidate := local_best_candidate;
                best_text := local_best_text;
                best_weight := local_best_candidate.dict_weight;
                best_score := local_best_candidate.score;
                best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_text);
                best_support_score := local_best_support_score;
                best_chunk_alignment_score := local_best_chunk_alignment_score;
                best_rerank_score := local_best_rerank_score;
                best_path_confidence_score :=
                    get_candidate_path_confidence_score(local_best_candidate);
                best_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(local_best_candidate));
                best_head_boundary_score := local_best_head_boundary_score;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' fqvfix=[supported text=%s]',
                        [best_text]);
                end;
            end
            else if ((not has_material_support_advantage(best_support_score,
                local_best_support_score)) or
                ((local_best_chunk_alignment_score >
                best_chunk_alignment_score + 8192) and
                (local_best_rerank_score > best_rerank_score + 24000))) and
                has_material_rerank_advantage_local(local_best_rerank_score,
                best_rerank_score, local_best_support_score,
                best_support_score) then
            begin
                best_candidate := local_best_candidate;
                best_text := local_best_text;
                best_weight := local_best_candidate.dict_weight;
                best_score := local_best_candidate.score;
                best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_text);
                best_support_score := local_best_support_score;
                best_chunk_alignment_score := local_best_chunk_alignment_score;
                best_rerank_score := local_best_rerank_score;
                best_path_confidence_score :=
                    get_candidate_path_confidence_score(local_best_candidate);
                best_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(local_best_candidate));
                best_head_boundary_score := local_best_head_boundary_score;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' fqvfix=[rerank text=%s]',
                        [best_text]);
                end;
            end;
        end;

        procedure maybe_promote_best_rerank_complete_candidate_local;
        const
            c_rerank_force_margin = 12000;
        var
            local_idx: Integer;
            local_candidate: TncCandidate;
            local_text: string;
            local_support_score: Integer;
            local_chunk_alignment_score: Integer;
            local_rerank_score: Integer;
            local_head_boundary_score: Integer;
            local_path_confidence_score: Integer;
            local_path_preference_score: Integer;
            local_best_idx: Integer;
            local_best_candidate: TncCandidate;
            local_best_text: string;
            local_best_support_score: Integer;
            local_best_chunk_alignment_score: Integer;
            local_best_rerank_score: Integer;
            local_best_head_boundary_score: Integer;
            local_best_path_confidence_score: Integer;
            local_best_path_preference_score: Integer;
        begin
            if (best_text = '') or (best_support_score > -960) or
                (input_syllable_count < 4) then
            begin
                Exit;
            end;

            local_best_idx := -1;
            local_best_text := best_text;
            local_best_support_score := best_support_score;
            local_best_chunk_alignment_score := best_chunk_alignment_score;
            local_best_rerank_score := best_rerank_score;
            local_best_head_boundary_score := best_head_boundary_score;
            local_best_path_confidence_score := best_path_confidence_score;
            local_best_path_preference_score := best_path_preference_score;

            for local_idx := 0 to High(candidates) do
            begin
                local_candidate := candidates[local_idx];
                if Trim(local_candidate.comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_candidate.text);
                if get_candidate_text_unit_count(local_text) <> input_syllable_count then
                begin
                    Continue;
                end;

                local_head_boundary_score := get_fixed_boundary_head_score_local(
                    local_text);
                if top_requires_first_unit_match and
                    (not SameText(get_first_text_unit_local(local_text),
                    top_first_unit)) and (local_head_boundary_score < 2) then
                begin
                    Continue;
                end;

                local_support_score := get_full_query_support_score_local(local_text);
                local_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(local_text,
                    local_support_score);
                local_rerank_score := build_full_query_rerank_score_local(
                    local_candidate.score, local_chunk_alignment_score,
                    local_support_score, local_head_boundary_score);
                local_path_confidence_score :=
                    get_candidate_path_confidence_score(local_candidate);
                local_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(local_candidate, local_idx));

                if local_rerank_score > local_best_rerank_score then
                begin
                    local_best_idx := local_idx;
                    local_best_candidate := local_candidate;
                    local_best_text := local_text;
                    local_best_support_score := local_support_score;
                    local_best_chunk_alignment_score :=
                        local_chunk_alignment_score;
                    local_best_rerank_score := local_rerank_score;
                    local_best_head_boundary_score := local_head_boundary_score;
                    local_best_path_confidence_score :=
                        local_path_confidence_score;
                    local_best_path_preference_score :=
                        local_path_preference_score;
                end;
            end;

            if (local_best_idx >= 0) and
                (local_best_rerank_score >= best_rerank_score +
                c_rerank_force_margin) then
            begin
                best_candidate := local_best_candidate;
                best_text := local_best_text;
                if local_best_candidate.has_dict_weight then
                begin
                    best_weight := local_best_candidate.dict_weight;
                end
                else
                begin
                    best_weight := local_best_candidate.score;
                end;
                best_score := local_best_candidate.score;
                best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_text);
                best_support_score := local_best_support_score;
                best_chunk_alignment_score := local_best_chunk_alignment_score;
                best_rerank_score := local_best_rerank_score;
                best_head_boundary_score := local_best_head_boundary_score;
                best_path_confidence_score := local_best_path_confidence_score;
                best_path_preference_score := local_best_path_preference_score;
            end;
        end;

        procedure ensure_best_front_complete_chunk_rerank_candidate_local;
        const
            c_front_limit = 6;
            c_chunk_margin = 4096;
            c_rerank_margin = 6000;
        var
            top_text_local: string;
            top_support_local: Integer;
            top_chunk_local: Integer;
            top_rerank_local: Integer;
            local_idx: Integer;
            local_text: string;
            local_support_score: Integer;
            local_chunk_alignment_score: Integer;
            local_rerank_score: Integer;
            best_idx_local: Integer;
            best_chunk_local: Integer;
            best_rerank_local: Integer;
            picked_candidate: TncCandidate;
        begin
            if (Length(candidates) <= 1) or (best_text = '') then
            begin
                Exit;
            end;

            top_text_local := Trim(best_text);
            if get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count then
            begin
                Exit;
            end;

            top_support_local := best_support_score;
            top_chunk_local := best_chunk_alignment_score;
            top_rerank_local := best_rerank_score;

            best_idx_local := -1;
            best_chunk_local := top_chunk_local;
            best_rerank_local := top_rerank_local;
            for local_idx := 0 to Min(c_front_limit - 1, High(candidates)) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(candidates[local_idx].text);
                if get_candidate_text_unit_count(local_text) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                if SameText(local_text, top_text_local) then
                begin
                    Continue;
                end;

                local_support_score := get_full_query_support_score_local(local_text);
                local_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(local_text,
                    local_support_score);
                local_rerank_score := build_full_query_rerank_score_local(
                    candidates[local_idx].score, local_chunk_alignment_score,
                    local_support_score,
                    get_fixed_boundary_head_score_local(local_text));

                if (top_support_local > -960) and
                    (local_support_score <= -960) then
                begin
                    Continue;
                end;

                if has_material_support_advantage(top_support_local,
                    local_support_score) and
                    (local_chunk_alignment_score <= top_chunk_local +
                    c_chunk_margin) then
                begin
                    Continue;
                end;

                if (local_chunk_alignment_score <= top_chunk_local +
                    c_chunk_margin) or
                    (local_rerank_score <= top_rerank_local + c_rerank_margin) then
                begin
                    Continue;
                end;

                if (best_idx_local < 0) or
                    (local_chunk_alignment_score > best_chunk_local) or
                    ((local_chunk_alignment_score = best_chunk_local) and
                    (local_rerank_score > best_rerank_local)) then
                begin
                    best_idx_local := local_idx;
                    best_chunk_local := local_chunk_alignment_score;
                    best_rerank_local := local_rerank_score;
                end;
            end;

            if best_idx_local < 0 then
            begin
                Exit;
            end;

            picked_candidate := candidates[best_idx_local];
            best_candidate := picked_candidate;
            best_text := Trim(picked_candidate.text);
            best_weight := picked_candidate.dict_weight;
            best_score := picked_candidate.score;
            best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, best_text);
            best_head_boundary_score := get_fixed_boundary_head_score_local(
                best_text);
            best_support_score := get_full_query_support_score_local(best_text);
            best_chunk_alignment_score :=
                get_full_query_chunk_alignment_score_fast_local(best_text,
                best_support_score);
            best_rerank_score := build_full_query_rerank_score_local(
                picked_candidate.score, best_chunk_alignment_score,
                best_support_score, best_head_boundary_score);
            best_path_confidence_score := get_candidate_path_confidence_score(
                picked_candidate);
            best_path_preference_score := get_segment_path_preference_score(
                get_segment_path_for_candidate(picked_candidate, best_idx_local));
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvfix=[front text=%s]',
                    [best_text]);
            end;
        end;

        procedure ensure_best_nearby_variant_complete_candidate_visible_local;
        const
            c_front_limit = 6;
            c_nearby_variant_max_syllables = 24;
            c_support_margin = 1024;
            c_chunk_margin = 4096;
            c_rerank_margin = 4096;
        var
            top_units: TArray<string>;
            top_text_local: string;
            top_support_local: Integer;
            top_chunk_local: Integer;
            top_rerank_local: Integer;
            top_problematic_local: Boolean;
            local_idx: Integer;
            local_text: string;
            local_units: TArray<string>;
            local_support_score: Integer;
            local_chunk_alignment_score: Integer;
            local_rerank_score: Integer;
            local_problematic_score: Boolean;
            window_start_local: Integer;
            window_span_local: Integer;
            top_window_text_local: string;
            local_window_text_local: string;
            resolved_window_text_local: string;
            resolved_window_score_local: Integer;
            resolved_window_segments_local: Integer;
            resolved_window_path_local: string;
            resolved_window_anchor_units_local: Integer;
            resolved_window_has_anchor_local: Boolean;
            diff_start: Integer;
            diff_end: Integer;
            diff_count: Integer;
            unit_idx: Integer;
            best_idx_local: Integer;
            best_support_local: Integer;
            best_chunk_local: Integer;
            best_rerank_local: Integer;
            picked_candidate: TncCandidate;
        begin
            if (Length(candidates) <= 1) or (best_text = '') or
                (input_syllable_count > c_nearby_variant_max_syllables) then
            begin
                Exit;
            end;

            top_text_local := Trim(best_text);
            if get_candidate_text_unit_count(top_text_local) <>
                input_syllable_count then
            begin
                Exit;
            end;

            top_units := split_text_units(top_text_local);
            if Length(top_units) <> input_syllable_count then
            begin
                Exit;
            end;

            top_support_local := best_support_score;
            top_chunk_local := best_chunk_alignment_score;
            top_rerank_local := best_rerank_score;
            top_problematic_local :=
                is_problematic_single_char_chain_candidate_for_query(
                lookup_text, best_candidate);

            if top_support_local > -960 then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_support_local := top_support_local;
            best_chunk_local := top_chunk_local;
            best_rerank_local := top_rerank_local;
            for local_idx := 0 to Min(c_front_limit - 1, High(candidates)) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(candidates[local_idx].text);
                if get_candidate_text_unit_count(local_text) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                if SameText(local_text, top_text_local) then
                begin
                    Continue;
                end;

                local_units := split_text_units(local_text);
                if Length(local_units) <> Length(top_units) then
                begin
                    Continue;
                end;

                diff_start := -1;
                diff_end := -1;
                diff_count := 0;
                for unit_idx := 0 to High(top_units) do
                begin
                    if SameText(top_units[unit_idx], local_units[unit_idx]) then
                    begin
                        Continue;
                    end;

                    if diff_start < 0 then
                    begin
                        diff_start := unit_idx;
                    end;
                    diff_end := unit_idx;
                    Inc(diff_count);
                end;

                if (diff_count <= 0) or (diff_count > 2) or
                    ((diff_end - diff_start + 1) > 2) then
                begin
                    Continue;
                end;

                local_support_score := get_full_query_support_score_local(local_text);
                local_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(local_text,
                    local_support_score);
                local_rerank_score := build_full_query_rerank_score_local(
                    candidates[local_idx].score, local_chunk_alignment_score,
                    local_support_score,
                    get_fixed_boundary_head_score_local(local_text));
                local_problematic_score :=
                    is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, candidates[local_idx]);
                window_start_local := diff_start;
                window_span_local := Max(2, diff_end - diff_start + 1);
                if window_start_local + window_span_local > input_syllable_count then
                begin
                    window_start_local := Max(0, input_syllable_count -
                        window_span_local);
                end;
                top_window_text_local := build_text_from_units_local(top_units,
                    window_start_local, window_span_local);
                local_window_text_local := build_text_from_units_local(local_units,
                    window_start_local, window_span_local);
                resolved_window_text_local := '';
                if try_resolve_best_exact_subspan_local(window_start_local,
                    window_span_local, resolved_window_text_local,
                    resolved_window_score_local, resolved_window_segments_local,
                    resolved_window_path_local, resolved_window_has_anchor_local,
                    resolved_window_anchor_units_local) and
                    (resolved_window_text_local <> '') and
                    SameText(local_window_text_local, resolved_window_text_local) and
                    (not SameText(top_window_text_local, resolved_window_text_local)) then
                begin
                    if (local_support_score <= -960) and
                        (top_support_local > -960) then
                    begin
                        Continue;
                    end;

                    if (local_support_score <= top_support_local) and
                        (local_chunk_alignment_score + 4096 <
                        top_chunk_local) then
                    begin
                        Continue;
                    end;

                    if (not top_problematic_local) and
                        (not has_material_support_advantage(
                        local_support_score, top_support_local)) and
                        (not has_material_chunk_alignment_advantage_local(
                        local_chunk_alignment_score, top_chunk_local,
                        local_support_score, top_support_local)) and
                        (not has_material_rerank_advantage_local(
                        local_rerank_score, top_rerank_local,
                        local_support_score, top_support_local)) then
                    begin
                        Continue;
                    end;

                    best_idx_local := local_idx;
                    best_support_local := local_support_score;
                    best_chunk_local := local_chunk_alignment_score;
                    best_rerank_local := local_rerank_score;
                    Continue;
                end;

                if not (((local_support_score > top_support_local +
                    c_support_margin) and
                    (local_chunk_alignment_score >= top_chunk_local - 2048) and
                    (local_rerank_score >= top_rerank_local - 8192)) or
                    ((local_chunk_alignment_score > top_chunk_local +
                    c_chunk_margin) and
                    (local_rerank_score > top_rerank_local +
                    c_rerank_margin)) or
                    (top_problematic_local and
                    (not local_problematic_score) and
                    (local_chunk_alignment_score >= top_chunk_local - 2048) and
                    (local_rerank_score >= top_rerank_local - 16000)) or
                    ((local_rerank_score > top_rerank_local + 12000) and
                    ((local_chunk_alignment_score >= top_chunk_local - 2048) or
                    (local_support_score > top_support_local +
                    c_support_margin)))) then
                begin
                    Continue;
                end;

                if (best_idx_local < 0) or
                    (local_chunk_alignment_score > best_chunk_local) or
                    ((local_chunk_alignment_score = best_chunk_local) and
                    (local_rerank_score > best_rerank_local)) or
                    ((local_chunk_alignment_score = best_chunk_local) and
                    (local_rerank_score = best_rerank_local) and
                    (local_support_score > best_support_local)) then
                begin
                    best_idx_local := local_idx;
                    best_support_local := local_support_score;
                    best_chunk_local := local_chunk_alignment_score;
                    best_rerank_local := local_rerank_score;
                end;
            end;

            if best_idx_local < 0 then
            begin
                Exit;
            end;

            picked_candidate := candidates[best_idx_local];
            best_candidate := picked_candidate;
            best_text := Trim(picked_candidate.text);
            best_weight := picked_candidate.dict_weight;
            best_score := picked_candidate.score;
            best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, best_text);
            best_head_boundary_score := get_fixed_boundary_head_score_local(
                best_text);
            best_support_score := get_full_query_support_score_local(best_text);
            best_chunk_alignment_score :=
                get_full_query_chunk_alignment_score_fast_local(best_text,
                best_support_score);
            best_rerank_score := build_full_query_rerank_score_local(
                picked_candidate.score, best_chunk_alignment_score,
                best_support_score, best_head_boundary_score);
            best_path_confidence_score := get_candidate_path_confidence_score(
                picked_candidate);
            best_path_preference_score := get_segment_path_preference_score(
                get_segment_path_for_candidate(picked_candidate, best_idx_local));
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvfix=[near text=%s]',
                    [best_text]);
            end;
        end;

        procedure try_promote_full_query_subspan_resolved_candidate_local;
        const
            c_max_short_query_syllables = 7;
        var
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
            resolved_support_score_local: Integer;
            resolved_chunk_alignment_score_local: Integer;
            resolved_rerank_score_local: Integer;
            resolved_head_boundary_score_local: Integer;
            resolved_phrase_bonus_local: Integer;
            local_idx: Integer;
            promote_margin_local: Integer;
            resolved_candidate_local: TncCandidate;
        begin
            if (best_text = '') or (input_syllable_count < 3) or
                (input_syllable_count > c_max_short_query_syllables) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            if not try_resolve_best_exact_subspan_local(0, input_syllable_count,
                resolved_text_local, resolved_score_local,
                resolved_segments_local, resolved_path_local,
                resolved_has_anchor_local, resolved_anchor_units_local) then
            begin
                Exit;
            end;
            if (resolved_text_local = '') or SameText(resolved_text_local,
                best_text) or
                (get_candidate_text_unit_count(resolved_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if (resolved_segments_local <= 1) and
                (not resolved_has_anchor_local) and
                (input_syllable_count > 4) then
            begin
                Exit;
            end;

            resolved_support_score_local := get_full_query_support_score_local(
                resolved_text_local);
            if (resolved_support_score_local <= -960) and
                (best_support_score > -960) then
            begin
                Exit;
            end;

            resolved_head_boundary_score_local :=
                get_fixed_boundary_head_score_local(resolved_text_local);
            resolved_chunk_alignment_score_local :=
                get_full_query_chunk_alignment_score_fast_local(
                resolved_text_local, resolved_support_score_local);
            resolved_rerank_score_local := Max(resolved_score_local,
                build_full_query_rerank_score_local(resolved_score_local,
                resolved_chunk_alignment_score_local,
                resolved_support_score_local,
                resolved_head_boundary_score_local));
            resolved_phrase_bonus_local :=
                get_preferred_query_phrase_bonus_local(lookup_text,
                resolved_text_local);

            if input_syllable_count <= 4 then
            begin
                promote_margin_local := 512;
            end
            else
            begin
                promote_margin_local := 1024;
            end;

            if not ((resolved_support_score_local >
                best_support_score + 128) or
                (resolved_rerank_score_local >=
                best_rerank_score + promote_margin_local) or
                ((resolved_head_boundary_score_local >
                best_head_boundary_score) and
                (resolved_rerank_score_local >= best_rerank_score - 512)) or
                ((input_syllable_count <= 4) and
                (resolved_chunk_alignment_score_local >
                best_chunk_alignment_score + 1024) and
                (resolved_rerank_score_local >= best_rerank_score - 1024)) or
                ((best_support_score <= -960) and
                (resolved_rerank_score_local >= best_rerank_score))) then
            begin
                Exit;
            end;

            FillChar(resolved_candidate_local, SizeOf(resolved_candidate_local), 0);
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text),
                    resolved_text_local) then
                begin
                    resolved_candidate_local := candidates[local_idx];
                    Break;
                end;
            end;
            if resolved_candidate_local.text = '' then
            begin
                for local_idx := 0 to High(exact_results) do
                begin
                    if (Trim(exact_results[local_idx].comment) = '') and
                        SameText(Trim(exact_results[local_idx].text),
                        resolved_text_local) then
                    begin
                        resolved_candidate_local := exact_results[local_idx];
                        Break;
                    end;
                end;
            end;
            if resolved_candidate_local.text = '' then
            begin
                resolved_candidate_local.text := resolved_text_local;
                resolved_candidate_local.comment := '';
                resolved_candidate_local.source := cs_rule;
                resolved_candidate_local.has_dict_weight := False;
                resolved_candidate_local.dict_weight := 0;
                resolved_candidate_local.score := resolved_score_local;
            end;

            if resolved_candidate_local.score < resolved_score_local then
            begin
                resolved_candidate_local.score := resolved_score_local;
            end;

            best_candidate := resolved_candidate_local;
            best_text := resolved_text_local;
            best_weight := resolved_candidate_local.dict_weight;
            best_score := resolved_candidate_local.score;
            best_phrase_bonus := resolved_phrase_bonus_local;
            best_support_score := resolved_support_score_local;
            best_chunk_alignment_score := resolved_chunk_alignment_score_local;
            best_rerank_score := resolved_rerank_score_local;
            best_head_boundary_score := resolved_head_boundary_score_local;
            best_path_confidence_score := 0;
            best_path_preference_score := 0;
            best_exact_backed_local := resolved_support_score_local > -960;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvfix=[subspan text=%s seg=%d score=%d]',
                    [best_text, resolved_segments_local,
                    resolved_rerank_score_local]);
            end;
        end;

        procedure maybe_promote_fixed_boundary_variant_candidate_local;
        const
            c_front_limit = 6;
            c_chunk_slack = 4096;
            c_rerank_slack = 12000;
        var
            top_units_local: TArray<string>;
            local_units_local: TArray<string>;
            local_idx: Integer;
            local_text_local: string;
            local_support_score_local: Integer;
            local_chunk_alignment_score_local: Integer;
            local_rerank_score_local: Integer;
            local_boundary_score_local: Integer;
            best_idx_local: Integer;
            best_boundary_score_local: Integer;
            best_chunk_local: Integer;
            best_rerank_local: Integer;
            picked_candidate_local: TncCandidate;

            function get_boundary_score_local(
                const units_local: TArray<string>): Integer;
            var
                unit_idx_local: Integer;
                fixed_text_local: string;
                normalized_syllable_local: string;
                match_bonus_local: Integer;
                mismatch_penalty_local: Integer;
            begin
                Result := 0;
                if Length(units_local) <> input_syllable_count then
                begin
                    Exit(Low(Integer));
                end;

                for unit_idx_local := 0 to input_syllable_count - 1 do
                begin
                    fixed_text_local := get_fixed_boundary_single_char_for_query_syllable_local(
                        effective_syllables[unit_idx_local].text);
                    if fixed_text_local = '' then
                    begin
                        Continue;
                    end;

                    normalized_syllable_local := normalize_pinyin_text(
                        effective_syllables[unit_idx_local].text);
                    if (normalized_syllable_local = 'er') or
                        (normalized_syllable_local = 'yu') then
                    begin
                        match_bonus_local := 480;
                        mismatch_penalty_local := 720;
                    end
                    else if normalized_syllable_local = 'de' then
                    begin
                        match_bonus_local := 800;
                        mismatch_penalty_local := 1200;
                    end
                    else
                    begin
                        match_bonus_local := 1600;
                        mismatch_penalty_local := 2400;
                    end;

                    if SameText(Trim(units_local[unit_idx_local]),
                        fixed_text_local) then
                    begin
                        Inc(Result, match_bonus_local);
                    end
                    else
                    begin
                        Dec(Result, mismatch_penalty_local);
                    end;
                end;
            end;
        begin
            if best_text = '' then
            begin
                Exit;
            end;
            if (input_syllable_count >= 8) and (best_support_score > -960) then
            begin
                Exit;
            end;
            if get_candidate_text_unit_count(best_text) <> input_syllable_count then
            begin
                Exit;
            end;

            top_units_local := split_text_units(best_text);
            if Length(top_units_local) <> input_syllable_count then
            begin
                Exit;
            end;

            best_boundary_score_local := get_boundary_score_local(top_units_local);
            best_idx_local := -1;
            best_chunk_local := best_chunk_alignment_score;
            best_rerank_local := best_rerank_score;
            for local_idx := 0 to Min(c_front_limit - 1, High(candidates)) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text_local := Trim(candidates[local_idx].text);
                if SameText(local_text_local, best_text) or
                    (get_candidate_text_unit_count(local_text_local) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                local_units_local := split_text_units(local_text_local);
                if Length(local_units_local) <> input_syllable_count then
                begin
                    Continue;
                end;

                local_boundary_score_local := get_boundary_score_local(
                    local_units_local);
                if local_boundary_score_local <= best_boundary_score_local then
                begin
                    Continue;
                end;

                local_support_score_local := get_full_query_support_score_local(
                    local_text_local);
                local_chunk_alignment_score_local :=
                    get_full_query_chunk_alignment_score_fast_local(
                    local_text_local, local_support_score_local);
                local_rerank_score_local := build_full_query_rerank_score_local(
                    candidates[local_idx].score, local_chunk_alignment_score_local,
                    local_support_score_local, local_boundary_score_local);

                if (best_support_score > -960) and
                    (local_support_score_local <= -960) then
                begin
                    Continue;
                end;
                if (best_support_score > -960) and
                    (local_support_score_local + 4096 < best_support_score) then
                begin
                    Continue;
                end;

                if has_material_support_advantage(best_support_score,
                    local_support_score_local) and
                    (local_boundary_score_local < best_boundary_score_local + 3200) then
                begin
                    Continue;
                end;

                if (local_chunk_alignment_score_local + c_chunk_slack <
                    best_chunk_alignment_score) and
                    (local_rerank_score_local + c_rerank_slack <
                    best_rerank_score) then
                begin
                    Continue;
                end;

                if (best_idx_local < 0) or
                    (local_boundary_score_local > best_boundary_score_local) or
                    ((local_boundary_score_local = best_boundary_score_local) and
                    (local_chunk_alignment_score_local > best_chunk_local)) or
                    ((local_boundary_score_local = best_boundary_score_local) and
                    (local_chunk_alignment_score_local = best_chunk_local) and
                    (local_rerank_score_local > best_rerank_local)) then
                begin
                    best_idx_local := local_idx;
                    best_boundary_score_local := local_boundary_score_local;
                    best_chunk_local := local_chunk_alignment_score_local;
                    best_rerank_local := local_rerank_score_local;
                end;
            end;

            if best_idx_local < 0 then
            begin
                Exit;
            end;

            picked_candidate_local := candidates[best_idx_local];
            best_candidate := picked_candidate_local;
            best_text := Trim(picked_candidate_local.text);
            best_weight := picked_candidate_local.dict_weight;
            best_score := picked_candidate_local.score;
            best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, best_text);
            best_head_boundary_score := get_fixed_boundary_head_score_local(
                best_text);
            best_support_score := get_full_query_support_score_local(best_text);
            best_chunk_alignment_score :=
                get_full_query_chunk_alignment_score_fast_local(best_text,
                best_support_score);
            best_rerank_score := build_full_query_rerank_score_local(
                picked_candidate_local.score, best_chunk_alignment_score,
                best_support_score, best_head_boundary_score);
            best_path_confidence_score := get_candidate_path_confidence_score(
                picked_candidate_local);
            best_path_preference_score := get_segment_path_preference_score(
                get_segment_path_for_candidate(picked_candidate_local,
                best_idx_local));
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvfix=[boundary text=%s]', [best_text]);
            end;
        end;

        procedure maybe_promote_modal_tail_complete_candidate_local;
        const
            c_front_limit = 8;
        var
            expected_tail_text_local: string;
            best_idx_local: Integer;
            local_idx: Integer;
            local_text_local: string;
            local_units_local: TArray<string>;
            picked_candidate_local: TncCandidate;
        begin
            if (input_syllable_count < 3) or
                (not SameText(normalize_pinyin_text(
                effective_syllables[High(effective_syllables) - 1].text), 'le')) then
            begin
                Exit;
            end;

            expected_tail_text_local := '';
            local_text_local := LowerCase(normalize_pinyin_text(
                effective_syllables[High(effective_syllables)].text));
            if local_text_local = 'ba' then
            begin
                expected_tail_text_local := string(Char($5427));
            end
            else if local_text_local = 'ma' then
            begin
                expected_tail_text_local := string(Char($5417));
            end
            else if local_text_local = 'ne' then
            begin
                expected_tail_text_local := string(Char($5462));
            end
            else if local_text_local = 'a' then
            begin
                expected_tail_text_local := string(Char($554A));
            end
            else if local_text_local = 'ya' then
            begin
                expected_tail_text_local := string(Char($5440));
            end
            else if local_text_local = 'la' then
            begin
                expected_tail_text_local := string(Char($5566));
            end
            else if local_text_local = 'wa' then
            begin
                expected_tail_text_local := string(Char($54C7));
            end;
            if expected_tail_text_local = '' then
            begin
                Exit;
            end;

            best_idx_local := -1;
            for local_idx := 0 to Min(c_front_limit - 1, High(candidates)) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text_local := Trim(candidates[local_idx].text);
                if get_candidate_text_unit_count(local_text_local) <>
                    input_syllable_count then
                begin
                    Continue;
                end;

                local_units_local := split_text_units(local_text_local);
                if (Length(local_units_local) < 2) or
                    (not SameText(Trim(local_units_local[High(local_units_local) - 1]),
                    string(Char($4E86)))) or
                    (not SameText(Trim(local_units_local[High(local_units_local)]),
                    expected_tail_text_local)) then
                begin
                    Continue;
                end;

                if (best_idx_local < 0) or
                    (candidates[local_idx].score > candidates[best_idx_local].score) then
                begin
                    best_idx_local := local_idx;
                end;
            end;

            if (best_idx_local < 0) or SameText(Trim(candidates[best_idx_local].text),
                best_text) then
            begin
                Exit;
            end;

            picked_candidate_local := candidates[best_idx_local];
            best_candidate := picked_candidate_local;
            best_text := Trim(picked_candidate_local.text);
            best_weight := picked_candidate_local.dict_weight;
            best_score := picked_candidate_local.score;
            best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, best_text);
            best_head_boundary_score := get_fixed_boundary_head_score_local(
                best_text);
            best_support_score := get_full_query_support_score_local(best_text);
            best_chunk_alignment_score :=
                get_full_query_chunk_alignment_score_fast_local(best_text,
                best_support_score);
            best_rerank_score := build_full_query_rerank_score_local(
                picked_candidate_local.score, best_chunk_alignment_score,
                best_support_score, best_head_boundary_score);
            best_path_confidence_score := get_candidate_path_confidence_score(
                picked_candidate_local);
            best_path_preference_score := get_segment_path_preference_score(
                get_segment_path_for_candidate(picked_candidate_local,
                best_idx_local));
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvfix=[modal text=%s]', [best_text]);
            end;
        end;

        procedure maybe_promote_high_score_complete_candidate_local;
        const
            c_front_limit = 6;
            c_score_margin = 24000;
            c_chunk_tolerance = 12000;
            c_rerank_tolerance = 24000;
        var
            local_idx: Integer;
            local_candidate: TncCandidate;
            local_text: string;
            local_support_score: Integer;
            local_chunk_alignment_score: Integer;
            local_rerank_score: Integer;
            local_head_boundary_score: Integer;
            picked_candidate_local: TncCandidate;
        begin
            if (best_text = '') or (Length(candidates) <= 1) then
            begin
                Exit;
            end;

            for local_idx := 0 to Min(c_front_limit - 1, High(candidates)) do
            begin
                local_candidate := candidates[local_idx];
                if Trim(local_candidate.comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_candidate.text);
                if SameText(local_text, best_text) or
                    (get_candidate_text_unit_count(local_text) <>
                    input_syllable_count) or
                    (local_candidate.score < best_score + c_score_margin) then
                begin
                    Continue;
                end;

                local_support_score := get_full_query_support_score_local(
                    local_text);
                local_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(local_text,
                    local_support_score);
                local_head_boundary_score := get_fixed_boundary_head_score_local(
                    local_text);
                local_rerank_score := build_full_query_rerank_score_local(
                    local_candidate.score, local_chunk_alignment_score,
                    local_support_score, local_head_boundary_score);
                if local_support_score <= -960 then
                begin
                    if best_support_score > -960 then
                    begin
                        Continue;
                    end;
                    if (local_chunk_alignment_score <
                        best_chunk_alignment_score + 24000) or
                        (local_rerank_score <
                        best_rerank_score + 36000) then
                    begin
                        Continue;
                    end;
                end;
                if (local_chunk_alignment_score + c_chunk_tolerance <
                    best_chunk_alignment_score) and
                    (local_rerank_score + c_rerank_tolerance <
                    best_rerank_score) then
                begin
                    Continue;
                end;

                picked_candidate_local := local_candidate;
                best_candidate := picked_candidate_local;
                best_text := local_text;
                if picked_candidate_local.has_dict_weight then
                begin
                    best_weight := picked_candidate_local.dict_weight;
                end
                else
                begin
                    best_weight := picked_candidate_local.score;
                end;
                best_score := picked_candidate_local.score;
                best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_text);
                best_support_score := local_support_score;
                best_chunk_alignment_score := local_chunk_alignment_score;
                best_rerank_score := local_rerank_score;
                best_head_boundary_score := get_fixed_boundary_head_score_local(
                    best_text);
                best_path_confidence_score := get_candidate_path_confidence_score(
                    picked_candidate_local);
                best_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(picked_candidate_local,
                    local_idx));
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' fqvfix=[score text=%s]', [best_text]);
                end;
                Exit;
            end;
        end;
    begin
        chunk_windows_prepared := False;
        SetLength(chunk_windows, 0);
        if full_query_exact_promoted and (Length(candidates) > 0) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            Exit;
        end;

        if (Length(candidates) <= 1) or (input_syllable_count < 2) or
            ((m_dictionary = nil) and (Length(candidates) <= 1)) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;
        last_syllable_text := Trim(effective_syllables[High(effective_syllables)].text);
        if (input_syllable_count >= 6) and (last_syllable_text <> '') and
            (not has_exact_single_char_tail_local(last_syllable_text)) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' fqv=[skip incomplete_tail]';
            end;
            Exit;
        end;

        best_text := '';
            best_weight := Low(Integer);
            best_score := Low(Integer);
            best_phrase_bonus := Low(Integer);
            best_support_score := Low(Integer);
            best_chunk_alignment_score := Low(Integer);
            best_rerank_score := Low(Integer);
            best_path_confidence_score := Low(Integer);
            best_path_preference_score := Low(Integer);
            best_head_boundary_score := Low(Integer);
            best_exact_backed_local := False;
            debug_full_candidate_summary := '';
            debug_full_candidate_count := 0;
            top_first_unit := get_first_text_unit_local(candidates[0].text);
            top_requires_first_unit_match := (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count) and (top_first_unit <> '') and
                (input_syllable_count >= 4);
            if (m_dictionary <> nil) and
                lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results) then
            begin
            if input_syllable_count <= 2 then
            begin
                for idx := 0 to High(exact_results) do
                begin
                    if Trim(exact_results[idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(Trim(exact_results[idx].text)) <>
                        input_syllable_count then
                    begin
                        Continue;
                    end;
                    if has_explicit_apostrophe_input and
                        (not matches_explicit_apostrophe_unit_alignment(
                        exact_results[idx].text)) then
                    begin
                        Continue;
                    end;
                    if top_requires_first_unit_match and
                        (not SameText(get_first_text_unit_local(
                        exact_results[idx].text), top_first_unit)) then
                    begin
                        Continue;
                    end;

                    if exact_results[idx].has_dict_weight then
                    begin
                        weight_value := exact_results[idx].dict_weight;
                    end
                    else
                    begin
                        weight_value := exact_results[idx].score;
                    end;
                    Inc(weight_value, get_preferred_query_phrase_bonus_local(
                        lookup_text, Trim(exact_results[idx].text)));
                    query_choice_bonus := get_full_query_choice_bonus_local(
                        Trim(exact_results[idx].text));
                    Inc(weight_value, query_choice_bonus);
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' fqvshort=[idx=%d text=%s w=%d]',
                            [idx, Trim(exact_results[idx].text), weight_value]);
                    end;
                    if (best_text = '') or (weight_value > best_weight) or
                        ((weight_value = best_weight) and
                        (CompareText(Trim(exact_results[idx].text), best_text) < 0)) then
                    begin
                        best_candidate := exact_results[idx];
                        best_text := Trim(exact_results[idx].text);
                        best_weight := weight_value;
                        best_score := exact_results[idx].score +
                            query_choice_bonus;
                        best_phrase_bonus :=
                            get_preferred_query_phrase_bonus_local(lookup_text,
                            best_text);
                        best_support_score := 0;
                        best_chunk_alignment_score := input_syllable_count * 4096;
                        best_rerank_score := weight_value;
                        best_path_confidence_score := 0;
                        best_path_preference_score := 0;
                        best_head_boundary_score :=
                            get_fixed_boundary_head_score_local(best_text);
                        best_exact_backed_local := True;
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info := m_last_full_path_debug_info +
                                Format(' fqvlock=[exact text=%s]',
                                [best_text]);
                        end;
                    end;
                end;
            end;
            if (input_syllable_count > 2) or (best_text = '') then
            begin
                for idx := 0 to High(exact_results) do
                begin
                    if Trim(exact_results[idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(Trim(exact_results[idx].text)) <>
                        input_syllable_count then
                    begin
                        Continue;
                    end;
                    if has_explicit_apostrophe_input and
                        (not matches_explicit_apostrophe_unit_alignment(
                        exact_results[idx].text)) then
                    begin
                        Continue;
                    end;
                    if top_requires_first_unit_match and
                        (not SameText(get_first_text_unit_local(
                        exact_results[idx].text), top_first_unit)) then
                    begin
                        Continue;
                    end;

                    if exact_results[idx].has_dict_weight then
                    begin
                        weight_value := exact_results[idx].dict_weight;
                    end
                    else
                    begin
                        weight_value := exact_results[idx].score;
                    end;
                    query_choice_bonus := get_full_query_choice_bonus_local(
                        Trim(exact_results[idx].text));
                    adjusted_score := exact_results[idx].score +
                        query_choice_bonus;
                    Inc(weight_value, query_choice_bonus);

                    support_score := get_full_query_support_score_local(
                        Trim(exact_results[idx].text));
                    current_head_boundary_score := get_fixed_boundary_head_score_local(
                        Trim(exact_results[idx].text));
                    current_chunk_alignment_score :=
                        get_full_query_chunk_alignment_score_fast_local(
                        Trim(exact_results[idx].text), support_score);
                    current_rerank_score := build_full_query_rerank_score_local(
                        adjusted_score, current_chunk_alignment_score,
                        support_score, current_head_boundary_score);
                    if (best_text = '') or
                        has_material_exact_score_advantage_local(weight_value,
                        best_weight, adjusted_score, best_score, support_score,
                        best_support_score) or
                        ((not has_material_exact_score_advantage_local(
                        best_weight, weight_value, best_score, adjusted_score,
                        best_support_score, support_score)) and
                        has_material_support_advantage(support_score,
                        best_support_score) and
                        allow_support_priority_override_local(support_score,
                        best_support_score, current_rerank_score,
                        best_rerank_score)) or
                        has_material_chunk_alignment_advantage_local(
                        current_chunk_alignment_score,
                        best_chunk_alignment_score, support_score,
                        best_support_score) or
                        prefer_long_same_prefix_score_override_local(
                        Trim(exact_results[idx].text), support_score,
                        current_rerank_score, best_text, best_support_score,
                        best_rerank_score) or
                        has_material_rerank_advantage_local(
                        current_rerank_score, best_rerank_score, support_score,
                        best_support_score) or
                        (allow_supportless_override_local(support_score,
                        best_support_score, current_chunk_alignment_score,
                        best_chunk_alignment_score, current_rerank_score,
                        best_rerank_score) and
                        (current_chunk_alignment_score >
                        best_chunk_alignment_score + 1600)) or
                        ((not has_material_support_advantage(best_support_score,
                        support_score)) and
                        allow_path_priority_override_local(current_rerank_score,
                        best_rerank_score, support_score, best_support_score) and
                        (weight_value > best_weight)) or
                        ((not has_material_support_advantage(best_support_score,
                        support_score)) and
                        allow_path_priority_override_local(current_rerank_score,
                        best_rerank_score, support_score, best_support_score) and
                        (weight_value = best_weight) and
                        (current_rerank_score > best_rerank_score)) then
                    begin
                        best_candidate := exact_results[idx];
                        best_text := Trim(exact_results[idx].text);
                        best_weight := weight_value;
                        best_score := adjusted_score;
                        best_phrase_bonus :=
                            get_preferred_query_phrase_bonus_local(lookup_text,
                            best_text);
                        best_support_score := support_score;
                        best_chunk_alignment_score := current_chunk_alignment_score;
                        best_rerank_score := current_rerank_score;
                        best_path_confidence_score := 0;
                        best_path_preference_score := 0;
                        best_head_boundary_score := current_head_boundary_score;
                        best_exact_backed_local := support_score > -960;
                        if m_config.debug_mode and best_exact_backed_local then
                        begin
                            m_last_full_path_debug_info := m_last_full_path_debug_info +
                                Format(' fqvlock=[exact text=%s]',
                                [best_text]);
                        end;
                    end;
                end;
            end;
        end;
        try_promote_full_query_subspan_resolved_candidate_local;

        trust_exact_best_local := should_trust_exact_best_local;

        if not trust_exact_best_local then
        begin
            for idx := 0 to High(candidates) do
            begin
                if Trim(candidates[idx].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                if has_explicit_apostrophe_input and
                    (not matches_explicit_apostrophe_unit_alignment(
                    candidates[idx].text)) then
                begin
                    Continue;
                end;
                current_head_boundary_score := get_fixed_boundary_head_score_local(
                    Trim(candidates[idx].text));
                if top_requires_first_unit_match and
                    (not SameText(get_first_text_unit_local(candidates[idx].text),
                    top_first_unit)) and (current_head_boundary_score < 2) then
                begin
                    Continue;
                end;

                phrase_bonus := get_preferred_query_phrase_bonus_local(lookup_text,
                    Trim(candidates[idx].text));
                support_score := get_full_query_support_score_local(
                    Trim(candidates[idx].text));
                current_chunk_alignment_score :=
                    get_full_query_chunk_alignment_score_fast_local(
                    Trim(candidates[idx].text), support_score);
                current_rerank_score := build_full_query_rerank_score_local(
                    candidates[idx].score, current_chunk_alignment_score,
                    support_score, current_head_boundary_score);
                current_path_confidence_score :=
                    get_candidate_path_confidence_score(candidates[idx]);
                current_path_preference_score := get_segment_path_preference_score(
                    get_segment_path_for_candidate(candidates[idx], idx));
                if m_config.debug_mode and (debug_full_candidate_count < 4) then
                begin
                    debug_full_candidate_summary := debug_full_candidate_summary +
                        Format(' {%s s=%d sc=%d ch=%d rr=%d hb=%d pc=%d pp=%d}',
                        [Trim(candidates[idx].text), support_score,
                        candidates[idx].score, current_chunk_alignment_score,
                        current_rerank_score, current_head_boundary_score,
                        current_path_confidence_score,
                        current_path_preference_score]);
                    Inc(debug_full_candidate_count);
                end;

                if best_exact_backed_local and (best_support_score > -960) and
                    (not ((input_syllable_count >= 8) and
                    (support_score > -960) and
                    (best_support_score <= support_score + 4096) and
                    (current_rerank_score > best_rerank_score + 2048))) and
                    (not has_material_support_advantage(support_score,
                    best_support_score)) and
                    (current_head_boundary_score <= best_head_boundary_score) and
                    (current_chunk_alignment_score <=
                    best_chunk_alignment_score + 4096) and
                    (current_rerank_score <= best_rerank_score + 12000) then
                begin
                    Continue;
                end;

                if (best_text = '') or
                    ((current_head_boundary_score >= 2) and
                    (current_head_boundary_score > best_head_boundary_score)) or
                    has_material_support_advantage(support_score,
                    best_support_score) and
                    allow_support_priority_override_local(support_score,
                    best_support_score, current_rerank_score,
                    best_rerank_score) or
                    has_material_chunk_alignment_advantage_local(
                    current_chunk_alignment_score,
                    best_chunk_alignment_score, support_score,
                    best_support_score) or
                    prefer_long_same_prefix_score_override_local(
                    Trim(candidates[idx].text), support_score, current_rerank_score,
                    best_text, best_support_score, best_rerank_score) or
                    has_material_rerank_advantage_local(current_rerank_score,
                    best_rerank_score, support_score, best_support_score) or
                    (allow_supportless_override_local(support_score,
                    best_support_score, current_chunk_alignment_score,
                    best_chunk_alignment_score, current_rerank_score,
                    best_rerank_score) and
                    (current_chunk_alignment_score >
                    best_chunk_alignment_score + 1600)) or
                    ((not has_material_support_advantage(best_support_score,
                    support_score)) and
                    allow_supportless_override_local(support_score,
                    best_support_score, current_chunk_alignment_score,
                    best_chunk_alignment_score, current_rerank_score,
                    best_rerank_score) and
                    allow_path_priority_override_local(current_rerank_score,
                    best_rerank_score, support_score, best_support_score) and
                    (current_path_confidence_score > best_path_confidence_score)) or
                    ((not has_material_support_advantage(best_support_score,
                    support_score)) and
                    allow_supportless_override_local(support_score,
                    best_support_score, current_chunk_alignment_score,
                    best_chunk_alignment_score, current_rerank_score,
                    best_rerank_score) and
                    allow_path_priority_override_local(current_rerank_score,
                    best_rerank_score, support_score, best_support_score) and
                    (current_path_confidence_score = best_path_confidence_score) and
                    (current_path_preference_score > best_path_preference_score)) or
                    ((not has_material_support_advantage(best_support_score,
                    support_score)) and
                    allow_supportless_override_local(support_score,
                    best_support_score, current_chunk_alignment_score,
                    best_chunk_alignment_score, current_rerank_score,
                    best_rerank_score) and
                    allow_path_priority_override_local(current_rerank_score,
                    best_rerank_score, support_score, best_support_score) and
                    (current_path_confidence_score = best_path_confidence_score) and
                    (current_path_preference_score = best_path_preference_score) and
                    (current_rerank_score > best_rerank_score)) then
                begin
                    best_candidate := candidates[idx];
                    best_text := Trim(candidates[idx].text);
                    best_weight := candidates[idx].dict_weight;
                    best_score := candidates[idx].score;
                    best_phrase_bonus := phrase_bonus;
                    best_support_score := support_score;
                    best_chunk_alignment_score := current_chunk_alignment_score;
                    best_rerank_score := current_rerank_score;
                    best_path_confidence_score := current_path_confidence_score;
                    best_path_preference_score := current_path_preference_score;
                    best_head_boundary_score := current_head_boundary_score;
                    best_exact_backed_local := False;
                end;
            end;
        end;
        if (input_syllable_count <= 3) and
            try_build_preferred_query_phrase_candidate_local(preferred_candidate) and
            ((best_text = '') or
            (get_preferred_query_phrase_bonus_local(lookup_text, best_text) <
            get_preferred_query_phrase_bonus_local(lookup_text,
            preferred_candidate.text))) then
        begin
            best_candidate := preferred_candidate;
            best_text := Trim(preferred_candidate.text);
            best_weight := preferred_candidate.dict_weight;
            best_score := preferred_candidate.score;
            best_chunk_alignment_score := 0;
            best_rerank_score := preferred_candidate.score;
        end;

        if (not trust_exact_best_local) and
            (not ((best_text <> '') and (best_support_score >= 4096))) and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 4) then
        begin
            ensure_strict_oracle_sentence_candidates_prepared;
            if strict_oracle_sentence_ok then
            begin
                oracle_candidates := strict_oracle_sentence_candidates;
                for idx := 0 to High(oracle_candidates) do
                begin
                    if Trim(oracle_candidates[idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(
                        Trim(oracle_candidates[idx].text)) <> input_syllable_count then
                    begin
                        Continue;
                    end;
                    if top_requires_first_unit_match and
                        (best_support_score > -960) and
                        (not SameText(get_first_text_unit_local(
                        oracle_candidates[idx].text), top_first_unit)) then
                    begin
                        Continue;
                    end;

                    support_score := get_full_query_support_score_local(
                        Trim(oracle_candidates[idx].text));
                    current_head_boundary_score :=
                        get_fixed_boundary_head_score_local(
                        Trim(oracle_candidates[idx].text));
                    current_chunk_alignment_score :=
                        get_full_query_chunk_alignment_score_fast_local(
                        Trim(oracle_candidates[idx].text), support_score);
                    current_rerank_score := build_full_query_rerank_score_local(
                        oracle_candidates[idx].score,
                        current_chunk_alignment_score, support_score,
                        current_head_boundary_score);
                    current_path_confidence_score :=
                        get_candidate_path_confidence_score(oracle_candidates[idx]);
                    current_path_preference_score :=
                        get_segment_path_preference_score(
                        get_segment_path_for_candidate(oracle_candidates[idx], idx));

                    if best_exact_backed_local and (best_support_score > -960) and
                        (not ((input_syllable_count >= 8) and
                        (support_score > -960) and
                        (best_support_score <= support_score + 4096) and
                        (current_rerank_score > best_rerank_score + 2048))) and
                        (not has_material_support_advantage(support_score,
                        best_support_score)) and
                        (current_head_boundary_score <= best_head_boundary_score) and
                        (current_chunk_alignment_score <=
                        best_chunk_alignment_score + 4096) and
                        (current_rerank_score <= best_rerank_score + 12000) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                Format(' fqvlock=[skip_oracle best=%s over=%s]',
                                [best_text, Trim(oracle_candidates[idx].text)]);
                        end;
                        Continue;
                    end;

                    if (best_text = '') or
                        ((current_head_boundary_score >= 2) and
                        (current_head_boundary_score >
                        best_head_boundary_score)) or
                        has_material_support_advantage(support_score,
                        best_support_score) and
                        allow_support_priority_override_local(support_score,
                        best_support_score, current_rerank_score,
                        best_rerank_score) or
                        has_material_chunk_alignment_advantage_local(
                        current_chunk_alignment_score,
                        best_chunk_alignment_score, support_score,
                        best_support_score) or
                        prefer_long_same_prefix_score_override_local(
                        Trim(oracle_candidates[idx].text), support_score,
                        current_rerank_score, best_text,
                        best_support_score, best_rerank_score) or
                        has_material_rerank_advantage_local(
                        current_rerank_score, best_rerank_score,
                        support_score, best_support_score) or
                        (allow_supportless_override_local(support_score,
                        best_support_score, current_chunk_alignment_score,
                        best_chunk_alignment_score, current_rerank_score,
                        best_rerank_score) and
                        (current_chunk_alignment_score >
                        best_chunk_alignment_score + 1600)) or
                        ((not has_material_support_advantage(best_support_score,
                        support_score)) and
                        allow_supportless_override_local(support_score,
                        best_support_score, current_chunk_alignment_score,
                        best_chunk_alignment_score, current_rerank_score,
                        best_rerank_score) and
                        allow_path_priority_override_local(current_rerank_score,
                        best_rerank_score, support_score, best_support_score) and
                        (current_path_confidence_score >
                        best_path_confidence_score)) or
                        ((not has_material_support_advantage(best_support_score,
                        support_score)) and
                        allow_supportless_override_local(support_score,
                        best_support_score, current_chunk_alignment_score,
                        best_chunk_alignment_score, current_rerank_score,
                        best_rerank_score) and
                        allow_path_priority_override_local(current_rerank_score,
                        best_rerank_score, support_score, best_support_score) and
                        (current_path_confidence_score =
                        best_path_confidence_score) and
                        (current_path_preference_score >
                        best_path_preference_score)) or
                        ((not has_material_support_advantage(best_support_score,
                        support_score)) and
                        allow_supportless_override_local(support_score,
                        best_support_score, current_chunk_alignment_score,
                        best_chunk_alignment_score, current_rerank_score,
                        best_rerank_score) and
                        allow_path_priority_override_local(current_rerank_score,
                        best_rerank_score, support_score, best_support_score) and
                        (current_path_confidence_score =
                        best_path_confidence_score) and
                        (current_path_preference_score =
                        best_path_preference_score) and
                        (current_rerank_score > best_rerank_score)) then
                    begin
                        best_candidate := oracle_candidates[idx];
                        best_text := Trim(oracle_candidates[idx].text);
                        best_weight := oracle_candidates[idx].dict_weight;
                        best_score := oracle_candidates[idx].score;
                        best_phrase_bonus := get_preferred_query_phrase_bonus_local(
                            lookup_text, best_text);
                        best_support_score := support_score;
                        best_chunk_alignment_score := current_chunk_alignment_score;
                        best_rerank_score := current_rerank_score;
                        best_path_confidence_score :=
                            current_path_confidence_score;
                        best_path_preference_score :=
                            current_path_preference_score;
                        best_head_boundary_score := current_head_boundary_score;
                        best_exact_backed_local := False;
                    end;
                end;
            end;
        end;
        if not trust_exact_best_local then
        begin
            maybe_promote_best_supported_full_candidate_local;
            maybe_promote_best_rerank_complete_candidate_local;
            ensure_best_front_complete_chunk_rerank_candidate_local;
            ensure_best_nearby_variant_complete_candidate_visible_local;
            maybe_promote_fixed_boundary_variant_candidate_local;
            maybe_promote_modal_tail_complete_candidate_local;
            maybe_promote_high_score_complete_candidate_local;
        end;

        if best_text = '' then
        begin
            Exit;
        end;

        if should_defer_weak_exact_complete_for_prefix_query_local then
        begin
            promote_partial_anchor_to_front_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqv=[defer weak_prefix text=%s]', [best_text]);
            end;
            Exit;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' fqv=[best text=%s support=%d phrase=%d score=%d chunk=%d rerank=%d pconf=%d ppref=%d]',
                [best_text, best_support_score, best_phrase_bonus, best_score,
                best_chunk_alignment_score, best_rerank_score,
                best_path_confidence_score, best_path_preference_score]);
            if debug_full_candidate_summary <> '' then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqvcands=%s', [debug_full_candidate_summary]);
            end;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), best_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index = 0 then
        begin
            full_query_exact_promoted := True;
            Exit;
        end;

        if existing_index > 0 then
        begin
            best_candidate := candidates[existing_index];
            if best_candidate.score < best_score then
            begin
                best_candidate.score := best_score;
            end;
            if best_candidate.has_dict_weight and (best_candidate.dict_weight < best_weight) then
            begin
                best_candidate.dict_weight := best_weight;
            end;
        end
        else
        begin
            existing_index := Length(candidates);
            SetLength(candidates, existing_index + 1);
        end;

        best_candidate.comment := '';
        for idx := existing_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := best_candidate;
        full_query_exact_promoted := True;
    end;

    procedure ensure_best_long_exact_subspan_sentence_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_min_query_syllables = 7;
        c_max_chunk_syllables = 8;
        c_chunk_cover_bonus = 2600;
        c_anchor_bonus = 3600;
        c_segment_penalty = 2400;
        c_strong_chunk_bonus = 6000;
        c_cross_boundary_swallow_penalty = 36000;
        c_short_chunk_under_strong_penalty = 4000;
        c_short_exact_variant_limit = 8;
    var
        dp_scores: TArray<Integer>;
        dp_segments: TArray<Integer>;
        dp_anchor_units: TArray<Integer>;
        dp_has_anchor: TArray<Boolean>;
        dp_texts: TArray<string>;
        dp_paths: TArray<string>;
        pos: Integer;
        chunk_syllables: Integer;
        next_pos: Integer;
        chunk_text: string;
        chunk_path: string;
        combined_text: string;
        combined_path: string;
        top_text: string;
        best_text_local: string;
        chunk_score: Integer;
        chunk_segments: Integer;
        chunk_anchor_units: Integer;
        total_score: Integer;
        total_segments: Integer;
        total_anchor_units: Integer;
        best_score_local: Integer;
        top_support_local: Integer;
        top_support_segments_local: Integer;
        best_support_local: Integer;
        best_support_segments_local: Integer;
        idx: Integer;
        existing_index: Integer;
        local_has_anchor: Boolean;
        total_has_anchor: Boolean;
        better_candidate: Boolean;
        picked_candidate: TncCandidate;
        top_support_path_local: string;
        best_support_path_local: string;
        pre_ok_local: array[1..c_max_chunk_syllables] of Boolean;
        pre_text_local: array[1..c_max_chunk_syllables] of string;
        pre_path_local: array[1..c_max_chunk_syllables] of string;
        pre_score_local: array[1..c_max_chunk_syllables] of Integer;
        pre_segments_local: array[1..c_max_chunk_syllables] of Integer;
        pre_anchor_units_local: array[1..c_max_chunk_syllables] of Integer;
        pre_has_anchor_local: array[1..c_max_chunk_syllables] of Boolean;
        max_chunk_local: Integer;
        strong_chunk_len_local: Integer;
        effective_syllables_local: TncPinyinParseResult;
        oracle_top_text: string;
        oracle_existing_index: Integer;
        oracle_idx: Integer;
        top_exact_match_count_local: Integer;
        top_exact_mismatch_count_local: Integer;
        top_exact_strong_match_count_local: Integer;
        top_exact_strong_mismatch_count_local: Integer;

        function get_cross_boundary_swallow_penalty_local(
            const local_pos: Integer; const local_chunk_syllables: Integer;
            const local_chunk_text: string): Integer;
        var
            local_chunk_units: TArray<string>;
            head_text_local: string;
            head_path_local: string;
            head_score_local: Integer;
            head_segments_local: Integer;
            head_anchor_units_local: Integer;
            head_has_anchor_local: Boolean;
            head_units_local: TArray<string>;
            unit_idx_local: Integer;
            overlap_start_local: Integer;
            overlap_syllables_local: Integer;
            overlap_max_local: Integer;
            overlap_text_local: string;
            overlap_path_local: string;
            overlap_score_local: Integer;
            overlap_segments_local: Integer;
            overlap_anchor_units_local: Integer;
            overlap_has_anchor_local: Boolean;
            overlap_units_local: TArray<string>;
        begin
            Result := 0;
            if (local_chunk_syllables < 5) or
                (local_pos + local_chunk_syllables >= input_syllable_count) then
            begin
                Exit;
            end;

            local_chunk_units := split_text_units(Trim(local_chunk_text));
            if Length(local_chunk_units) <> local_chunk_syllables then
            begin
                Exit;
            end;

            if not try_resolve_best_exact_subspan_local(local_pos,
                local_chunk_syllables - 1, head_text_local, head_score_local,
                head_segments_local, head_path_local, head_has_anchor_local,
                head_anchor_units_local) then
            begin
                Exit;
            end;
            head_units_local := split_text_units(Trim(head_text_local));
            if Length(head_units_local) <> local_chunk_syllables - 1 then
            begin
                Exit;
            end;
            for unit_idx_local := 0 to High(head_units_local) do
            begin
                if not SameText(head_units_local[unit_idx_local],
                    local_chunk_units[unit_idx_local]) then
                begin
                    Exit;
                end;
            end;
            if (head_score_local < 12000) and
                ((not head_has_anchor_local) or (head_segments_local > 2)) then
            begin
                Exit;
            end;

            overlap_start_local := local_pos + local_chunk_syllables - 1;
            overlap_max_local := Min(c_max_chunk_syllables,
                input_syllable_count - overlap_start_local);
            if overlap_max_local < 3 then
            begin
                Exit;
            end;

            for overlap_syllables_local := 3 to overlap_max_local do
            begin
                if not try_resolve_best_exact_subspan_local(overlap_start_local,
                    overlap_syllables_local, overlap_text_local,
                    overlap_score_local, overlap_segments_local,
                    overlap_path_local, overlap_has_anchor_local,
                    overlap_anchor_units_local) then
                begin
                    Continue;
                end;
                overlap_units_local := split_text_units(Trim(overlap_text_local));
                if Length(overlap_units_local) <> overlap_syllables_local then
                begin
                    Continue;
                end;
                if SameText(overlap_units_local[0],
                    local_chunk_units[High(local_chunk_units)]) then
                begin
                    Continue;
                end;
                if (overlap_score_local < 12000) and
                    (not overlap_has_anchor_local) then
                begin
                    Continue;
                end;
                Exit(c_cross_boundary_swallow_penalty +
                    overlap_syllables_local * 1200);
            end;
        end;

        function build_window_query_key_local(const local_pos: Integer;
            const local_chunk_syllables: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_pos < 0) or (local_chunk_syllables <= 0) or
                (local_pos + local_chunk_syllables - 1 >
                High(effective_syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := local_pos to
                local_pos + local_chunk_syllables - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables_local[local_idx].text);
            end;
        end;

        function is_valid_cjk_phrase_text_outer_local(const value: string;
            const expected_units: Integer): Boolean;
        var
            units: TArray<string>;
            unit_idx: Integer;
            codepoint: Integer;

            function try_get_codepoint_local(const unit_text: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if unit_text = '' then
                begin
                    Exit;
                end;

                if Length(unit_text) = 1 then
                begin
                    out_codepoint := Ord(unit_text[1]);
                    Exit(True);
                end;

                if (Length(unit_text) = 2) and
                    (Ord(unit_text[1]) >= $D800) and
                    (Ord(unit_text[1]) <= $DBFF) and
                    (Ord(unit_text[2]) >= $DC00) and
                    (Ord(unit_text[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                        (Ord(unit_text[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if Trim(value) = '' then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(units) do
            begin
                if (not try_get_codepoint_local(units[unit_idx], codepoint)) or
                    (codepoint < $4E00) or (codepoint > $9FFF) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        procedure get_window_consistency_stats_for_text_local(
            const candidate_text_local: string; out out_match_count: Integer;
            out out_mismatch_count: Integer; out out_strong_match_count: Integer;
            out out_strong_mismatch_count: Integer);
        var
            candidate_units_local: TArray<string>;
            local_start_idx: Integer;
            local_span_syllables: Integer;
            local_chunk_text: string;
            local_resolved_text: string;
            local_resolved_path: string;
            local_resolved_score: Integer;
            local_resolved_segments: Integer;
            local_resolved_anchor_units: Integer;
            local_resolved_has_anchor: Boolean;
            strong_window_local: Boolean;

            function build_text_from_units_local(const text_units: TArray<string>;
                const start_unit: Integer; const unit_count: Integer): string;
            var
                unit_idx: Integer;
            begin
                Result := '';
                if (unit_count <= 0) or (start_unit < 0) or
                    (start_unit + unit_count - 1 > High(text_units)) then
                begin
                    Exit;
                end;

                for unit_idx := start_unit to start_unit + unit_count - 1 do
                begin
                    Result := Result + text_units[unit_idx];
                end;
            end;
        begin
            out_match_count := 0;
            out_mismatch_count := 0;
            out_strong_match_count := 0;
            out_strong_mismatch_count := 0;
            candidate_units_local := split_text_units(Trim(candidate_text_local));
            if Length(candidate_units_local) <> input_syllable_count then
            begin
                Exit;
            end;

            for local_start_idx := 0 to input_syllable_count - 2 do
            begin
                for local_span_syllables := 2 to
                    Min(5, input_syllable_count - local_start_idx) do
                begin
                    if not try_resolve_best_exact_subspan_local(local_start_idx,
                        local_span_syllables, local_resolved_text,
                        local_resolved_score, local_resolved_segments,
                        local_resolved_path, local_resolved_has_anchor,
                        local_resolved_anchor_units) or
                        (local_resolved_text = '') then
                    begin
                        Continue;
                    end;

                    local_chunk_text := build_text_from_units_local(
                        candidate_units_local, local_start_idx,
                        local_span_syllables);
                    if local_chunk_text = '' then
                    begin
                        Continue;
                    end;

                    strong_window_local := (local_span_syllables >= 3) or
                        local_resolved_has_anchor or
                        (local_resolved_score >= 4096);
                    if SameText(local_chunk_text, local_resolved_text) then
                    begin
                        Inc(out_match_count);
                        if strong_window_local then
                        begin
                            Inc(out_strong_match_count);
                        end;
                    end
                    else if strong_window_local then
                    begin
                        Inc(out_mismatch_count);
                        if (local_resolved_score >= 12000) or
                            local_resolved_has_anchor or
                            (local_span_syllables >= 4) then
                        begin
                            Inc(out_strong_mismatch_count);
                        end;
                    end;
                end;
            end;
        end;

        procedure try_update_candidate_state_local(const local_pos: Integer;
            const local_chunk_syllables: Integer; const local_chunk_text: string;
            const local_chunk_score: Integer; const local_chunk_segments: Integer;
            const local_has_anchor_value: Boolean;
            const local_chunk_anchor_units: Integer;
            const local_strong_chunk_len: Integer;
            const apply_short_chunk_penalty: Boolean);
        begin
            next_pos := local_pos + local_chunk_syllables;
            if (next_pos < input_syllable_count) and
                (dp_scores[next_pos] = Low(Integer)) then
            begin
                Exit;
            end;

            total_score := local_chunk_score +
                (local_chunk_syllables * local_chunk_syllables *
                c_chunk_cover_bonus) -
                Max(0, local_chunk_segments - 1) * c_segment_penalty;
            if local_has_anchor_value or (local_chunk_syllables >= 3) then
            begin
                Inc(total_score, c_anchor_bonus);
            end;
            if (local_strong_chunk_len >= 4) and
                (local_chunk_syllables = local_strong_chunk_len) then
            begin
                Inc(total_score, c_strong_chunk_bonus);
            end;
            if apply_short_chunk_penalty then
            begin
                Dec(total_score, c_short_chunk_under_strong_penalty);
            end;
            Dec(total_score, get_cross_boundary_swallow_penalty_local(local_pos,
                local_chunk_syllables, local_chunk_text));
            total_segments := local_chunk_segments;
            total_has_anchor := local_has_anchor_value;
            total_anchor_units := local_chunk_anchor_units;
            if total_anchor_units < 0 then
            begin
                total_anchor_units := 0;
            end;
            combined_text := Trim(local_chunk_text);
            combined_path := combined_text;

            if next_pos < input_syllable_count then
            begin
                Inc(total_score, dp_scores[next_pos]);
                Inc(total_segments, dp_segments[next_pos]);
                if dp_has_anchor[next_pos] then
                begin
                    total_has_anchor := True;
                    if dp_anchor_units[next_pos] > total_anchor_units then
                    begin
                        total_anchor_units := dp_anchor_units[next_pos];
                    end;
                end;
                combined_text := combined_text + dp_texts[next_pos];
                if (combined_path <> '') and (dp_paths[next_pos] <> '') then
                begin
                    combined_path := combined_path + c_segment_path_separator +
                        dp_paths[next_pos];
                end
                else if dp_paths[next_pos] <> '' then
                begin
                    combined_path := dp_paths[next_pos];
                end;
            end;

            better_candidate := False;
            if total_score > dp_scores[local_pos] then
            begin
                better_candidate := True;
            end
            else if total_score = dp_scores[local_pos] then
            begin
                if total_has_anchor and (not dp_has_anchor[local_pos]) then
                begin
                    better_candidate := True;
                end
                else if total_has_anchor = dp_has_anchor[local_pos] then
                begin
                    if total_anchor_units > dp_anchor_units[local_pos] then
                    begin
                        better_candidate := True;
                    end
                    else if total_anchor_units = dp_anchor_units[local_pos] then
                    begin
                        if total_segments < dp_segments[local_pos] then
                        begin
                            better_candidate := True;
                        end
                        else if (total_segments = dp_segments[local_pos]) and
                            ((dp_texts[local_pos] = '') or
                            (CompareText(combined_text, dp_texts[local_pos]) < 0)) then
                        begin
                            better_candidate := True;
                        end;
                    end;
                end;
            end;

            if better_candidate then
            begin
                dp_scores[local_pos] := total_score;
                dp_segments[local_pos] := total_segments;
                dp_anchor_units[local_pos] := total_anchor_units;
                dp_has_anchor[local_pos] := total_has_anchor;
                dp_texts[local_pos] := combined_text;
                dp_paths[local_pos] := combined_path;
            end;
        end;

        procedure try_update_short_exact_variants_local(const local_pos: Integer;
            const local_chunk_syllables: Integer;
            const local_strong_chunk_len: Integer;
            const skip_text: string);
        var
            query_key_local: string;
            lookup_results_local: TncCandidateList;
            exact_only_results_local: TncCandidateList;
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            merged_idx_local: Integer;
            duplicate_found_local: Boolean;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_weight_local: Integer;
            exact_bonus_local: Integer;
            probe_limit_local: Integer;
        begin
            if (local_chunk_syllables <= 0) or (local_chunk_syllables > 2) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key_local := build_window_query_key_local(local_pos,
                local_chunk_syllables);
            if query_key_local = '' then
            begin
                Exit;
            end;

            SetLength(exact_results_local, 0);
            if m_dictionary.lookup(query_key_local, lookup_results_local) then
            begin
                SetLength(exact_results_local, Length(lookup_results_local));
                for merged_idx_local := 0 to High(lookup_results_local) do
                begin
                    exact_results_local[merged_idx_local] :=
                        lookup_results_local[merged_idx_local];
                end;
            end;

            if m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_only_results_local) then
            begin
                for result_idx_local := 0 to High(exact_only_results_local) do
                begin
                    duplicate_found_local := False;
                    for merged_idx_local := 0 to High(exact_results_local) do
                    begin
                        if SameText(Trim(exact_results_local[merged_idx_local].text),
                            Trim(exact_only_results_local[result_idx_local].text)) and
                            SameText(Trim(exact_results_local[merged_idx_local].comment),
                            Trim(exact_only_results_local[result_idx_local].comment)) then
                        begin
                            duplicate_found_local := True;
                            Break;
                        end;
                    end;
                    if duplicate_found_local then
                    begin
                        Continue;
                    end;
                    SetLength(exact_results_local, Length(exact_results_local) + 1);
                    exact_results_local[High(exact_results_local)] :=
                        exact_only_results_local[result_idx_local];
                end;
            end;

            if Length(exact_results_local) = 0 then
            begin
                Exit;
            end;

            probe_limit_local := Min(c_short_exact_variant_limit,
                Length(exact_results_local));
            for result_idx_local := 0 to probe_limit_local - 1 do
            begin
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(exact_results_local[result_idx_local]) or
                    is_runtime_common_pattern_candidate(exact_results_local[result_idx_local]) or
                    is_runtime_redup_candidate(exact_results_local[result_idx_local]) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(exact_results_local[result_idx_local].text);
                if SameText(candidate_text_local, Trim(skip_text)) then
                begin
                    Continue;
                end;
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if candidate_units_local <> local_chunk_syllables then
                begin
                    Continue;
                end;
                if not is_valid_cjk_phrase_text_outer_local(candidate_text_local,
                    local_chunk_syllables) then
                begin
                    Continue;
                end;

                candidate_weight_local := exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        exact_results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_weight_local,
                    (probe_limit_local - result_idx_local) * 240);
                Inc(candidate_weight_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                exact_bonus_local := get_preferred_exact_subspan_bonus_local(
                    query_key_local, candidate_text_local);
                Inc(candidate_weight_local, exact_bonus_local);

                try_update_candidate_state_local(local_pos, local_chunk_syllables,
                    candidate_text_local, candidate_weight_local, 1, False, 0,
                    local_strong_chunk_len,
                    (local_strong_chunk_len >= 4) and
                    (local_chunk_syllables < 3));
            end;
        end;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < c_min_query_syllables) then
        begin
            Exit;
        end;

        top_text := Trim(candidates[0].text);
        if (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count) then
        begin
            top_support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, top_text, top_support_local,
                top_support_segments_local);
            if top_support_path_local <> '' then
            begin
                get_window_consistency_stats_for_text_local(top_text,
                    top_exact_match_count_local,
                    top_exact_mismatch_count_local,
                    top_exact_strong_match_count_local,
                    top_exact_strong_mismatch_count_local);
                if (top_exact_strong_mismatch_count_local = 0) and
                    ((top_exact_strong_match_count_local >= 2) or
                    ((top_exact_strong_match_count_local >= 1) and
                    (top_exact_mismatch_count_local = 0))) then
                begin
                    Exit;
                end;
            end;
        end;

        effective_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(effective_syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(dp_scores, input_syllable_count + 1);
        SetLength(dp_segments, input_syllable_count + 1);
        SetLength(dp_anchor_units, input_syllable_count + 1);
        SetLength(dp_has_anchor, input_syllable_count + 1);
        SetLength(dp_texts, input_syllable_count + 1);
        SetLength(dp_paths, input_syllable_count + 1);
        for pos := 0 to input_syllable_count do
        begin
            dp_scores[pos] := Low(Integer);
            dp_segments[pos] := MaxInt;
            dp_anchor_units[pos] := -1;
            dp_has_anchor[pos] := False;
            dp_texts[pos] := '';
            dp_paths[pos] := '';
        end;
        dp_scores[input_syllable_count] := 0;
        dp_segments[input_syllable_count] := 0;
        dp_anchor_units[input_syllable_count] := -1;

        for pos := input_syllable_count - 1 downto 0 do
        begin
            max_chunk_local := Min(c_max_chunk_syllables,
                input_syllable_count - pos);
            strong_chunk_len_local := 0;
            for chunk_syllables := 1 to max_chunk_local do
            begin
                pre_ok_local[chunk_syllables] :=
                    try_resolve_best_exact_subspan_local(pos, chunk_syllables,
                    pre_text_local[chunk_syllables],
                    pre_score_local[chunk_syllables],
                    pre_segments_local[chunk_syllables],
                    pre_path_local[chunk_syllables],
                    pre_has_anchor_local[chunk_syllables],
                    pre_anchor_units_local[chunk_syllables]);
                if (not pre_ok_local[chunk_syllables]) or
                    (Trim(pre_text_local[chunk_syllables]) = '') or
                    (get_candidate_text_unit_count(
                    Trim(pre_text_local[chunk_syllables])) <> chunk_syllables) then
                begin
                    pre_ok_local[chunk_syllables] := False;
                    Continue;
                end;
                if chunk_syllables >= 4 then
                begin
                    strong_chunk_len_local := Max(strong_chunk_len_local,
                        chunk_syllables);
                end;
            end;

            for chunk_syllables := 1 to max_chunk_local do
            begin
                if not pre_ok_local[chunk_syllables] then
                begin
                    Continue;
                end;
                chunk_text := pre_text_local[chunk_syllables];
                chunk_score := pre_score_local[chunk_syllables];
                chunk_segments := pre_segments_local[chunk_syllables];
                chunk_path := pre_path_local[chunk_syllables];
                local_has_anchor := pre_has_anchor_local[chunk_syllables];
                chunk_anchor_units := pre_anchor_units_local[chunk_syllables];

                try_update_candidate_state_local(pos, chunk_syllables,
                    chunk_text, chunk_score, chunk_segments, local_has_anchor,
                    chunk_anchor_units, strong_chunk_len_local,
                    (strong_chunk_len_local >= 4) and
                    (chunk_syllables < 3));
            end;
        end;

        best_text_local := Trim(dp_texts[0]);
        best_score_local := dp_scores[0];
        if m_config.debug_mode and (best_text_local <> '') then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' ldptry=[text=%s seg=%d score=%d]',
                [best_text_local, dp_segments[0], best_score_local]);
        end;
        if (best_score_local = Low(Integer)) or (best_text_local = '') or
            (get_candidate_text_unit_count(best_text_local) <> input_syllable_count) then
        begin
            Exit;
        end;

        oracle_top_text := '';
        oracle_existing_index := -1;
        ensure_strict_oracle_sentence_candidates_prepared;
        if strict_oracle_sentence_ok and
            (Length(strict_oracle_sentence_candidates) > 0) then
        begin
            for oracle_idx := 0 to Min(3,
                High(strict_oracle_sentence_candidates)) do
            begin
                if Trim(strict_oracle_sentence_candidates[oracle_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(
                    Trim(strict_oracle_sentence_candidates[oracle_idx].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                oracle_top_text := Trim(
                    strict_oracle_sentence_candidates[oracle_idx].text);
                Break;
            end;
        end;
        if oracle_top_text <> '' then
        begin
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), oracle_top_text) then
                begin
                    oracle_existing_index := idx;
                    Break;
                end;
            end;
        end;

        top_text := Trim(candidates[0].text);
        if SameText(top_text, best_text_local) then
        begin
            Exit;
        end;

        best_support_path_local := infer_segment_path_for_query_text_with_score(
            lookup_text, best_text_local, best_support_local,
            best_support_segments_local);

        if (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count) then
        begin
            top_support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, top_text, top_support_local,
                top_support_segments_local);
        end;

        existing_index := -1;
        FillChar(picked_candidate, SizeOf(picked_candidate), 0);
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), best_text_local) then
            begin
                existing_index := idx;
                picked_candidate := candidates[idx];
                Break;
            end;
        end;

        if existing_index < 0 then
        begin
            picked_candidate.text := best_text_local;
            picked_candidate.comment := '';
            picked_candidate.score := Max(best_score_local, 4096);
            picked_candidate.source := cs_rule;
            picked_candidate.has_dict_weight := True;
            picked_candidate.dict_weight := picked_candidate.score;
            existing_index := Length(candidates);
            SetLength(candidates, existing_index + 1);
        end
        else if picked_candidate.score < best_score_local then
        begin
            picked_candidate.score := best_score_local;
        end;

        for idx := existing_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked_candidate;
        remember_segment_path_for_candidate(best_text_local, '', dp_paths[0],
            Max(candidates[0].dict_weight, best_score_local));
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' ldp=[text=%s seg=%d score=%d sup=%d path=%s]',
                [best_text_local, dp_segments[0], best_score_local,
                best_support_local, best_support_path_local]);
        end;
    end;

    procedure ensure_best_exact_tail_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_tail_probe_limit = 8;
    var
        effective_syllables: TncPinyinParseResult;
        tail_syllables: Integer;
        tail_key: string;
        candidate_text: string;
        candidate_units: Integer;
        weight_value: Integer;
        best_tail_text: string;
        best_tail_score: Integer;
        idx: Integer;
        picked: TncCandidate;
        head_syllable_count: Integer;
        head_key: string;
        head_results: TncCandidateList;
        head_text: string;
        head_score: Integer;
        combined_text: string;
        existing_index: Integer;

        function ends_with_text_local(const full_value: string;
            const suffix_value: string): Boolean;
        begin
            Result := (full_value <> '') and (suffix_value <> '') and
                (Length(suffix_value) <= Length(full_value)) and
                SameText(Copy(full_value,
                Length(full_value) - Length(suffix_value) + 1, Length(suffix_value)),
                suffix_value);
        end;

        function build_tail_query_key_local(const tail_syllable_count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (tail_syllable_count <= 0) or
                (tail_syllable_count > Length(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := Length(effective_syllables) - tail_syllable_count
                to High(effective_syllables) do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function build_head_query_key_local(const head_syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (head_syllable_count_local <= 0) or
                (head_syllable_count_local > Length(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := 0 to head_syllable_count_local - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function try_get_best_exact_tail_text_local(
            const tail_syllable_count: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_result_idx: Integer;
            preferred_text: string;
            preferred_bonus: Integer;
            preferred_units: TArray<string>;
            preferred_start_idx: Integer;
            preferred_unit_idx: Integer;
            preferred_is_preferred: Boolean;
            preferred_match_ok: Boolean;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);

            if (tail_syllable_count >= 2) and (tail_syllable_count <= 5) and
                try_resolve_best_exact_subspan_local(
                input_syllable_count - tail_syllable_count, tail_syllable_count,
                resolved_text_local, resolved_score_local,
                resolved_segments_local, resolved_path_local,
                resolved_has_anchor_local, resolved_anchor_units_local) then
            begin
                out_text := resolved_text_local;
                out_score := resolved_score_local;
                Exit(True);
            end;

            tail_key := build_tail_query_key_local(tail_syllable_count);
            if (tail_key = '') or
                (not dictionary_lookup_cached(tail_key, local_results)) then
            begin
                Exit;
            end;

            for local_result_idx := 0 to High(local_results) do
            begin
                if local_result_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(local_results[local_result_idx]) or
                    is_runtime_common_pattern_candidate(local_results[local_result_idx]) or
                    is_runtime_redup_candidate(local_results[local_result_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[local_result_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> tail_syllable_count then
                begin
                    Continue;
                end;

                weight_value := local_results[local_result_idx].score;
                if (weight_value <= 0) and local_results[local_result_idx].has_dict_weight then
                begin
                    weight_value := local_results[local_result_idx].dict_weight;
                end;
                Inc(weight_value, get_preferred_query_phrase_bonus_local(
                    tail_key, candidate_text));

                if weight_value > out_score then
                begin
                    out_score := weight_value;
                    out_text := candidate_text;
                    Result := True;
                end;
            end;

            if try_get_preferred_query_phrase_text_local(tail_key, preferred_text,
                preferred_bonus) then
            begin
                preferred_units := split_text_units(preferred_text);
                if Length(preferred_units) = tail_syllable_count then
                begin
                    preferred_start_idx := Length(effective_syllables) - tail_syllable_count;
                    preferred_match_ok := True;
                    for preferred_unit_idx := 0 to High(preferred_units) do
                    begin
                        if not match_single_char_candidate_for_syllable(
                            effective_syllables[preferred_start_idx + preferred_unit_idx].text,
                            preferred_units[preferred_unit_idx], preferred_is_preferred) then
                        begin
                            preferred_match_ok := False;
                            Break;
                        end;
                    end;

                    if preferred_match_ok and (preferred_bonus > out_score) then
                    begin
                        out_score := preferred_bonus;
                        out_text := preferred_text;
                        Result := True;
                    end;
                end;
            end;
        end;

        function try_get_best_head_complete_text_local(
            const head_syllable_count_local: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_head_idx: Integer;
            fixed_text: string;
            fixed_score: Integer;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;

            function get_fixed_single_char_for_head_local(
                const query_key_local: string): string;
            begin
                Result := '';
                if query_key_local = 'de' then
                begin
                    Result := string(Char($7684));
                end
                else if query_key_local = 'yu' then
                begin
                    Result := string(Char($4E8E));
                end
                else if query_key_local = 'zai' then
                begin
                    Result := string(Char($5728));
                end
                else if query_key_local = 'shi' then
                begin
                    Result := string(Char($662F));
                end
                else if query_key_local = 'ba' then
                begin
                    Result := string(Char($628A));
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);

            if (head_syllable_count_local >= 2) and
                (head_syllable_count_local <= 5) and
                try_resolve_best_exact_subspan_local(0,
                head_syllable_count_local, resolved_text_local,
                resolved_score_local, resolved_segments_local,
                resolved_path_local, resolved_has_anchor_local,
                resolved_anchor_units_local) then
            begin
                out_text := resolved_text_local;
                out_score := resolved_score_local;
                Exit(True);
            end;

            head_key := build_head_query_key_local(head_syllable_count_local);
            if (head_syllable_count_local = 1) and (head_key <> '') then
            begin
                fixed_text := get_fixed_single_char_for_head_local(head_key);
                if fixed_text <> '' then
                begin
                    fixed_score := 520 +
                        get_preferred_query_phrase_bonus_local(head_key, fixed_text);
                    out_text := fixed_text;
                    out_score := fixed_score;
                    Result := True;
                end;
            end;

            if (head_key <> '') and
                dictionary_lookup_cached(head_key, head_results) then
            begin
                for local_head_idx := 0 to High(head_results) do
                begin
                    if local_head_idx >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(head_results[local_head_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[local_head_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if candidate_units <> head_syllable_count_local then
                    begin
                        Continue;
                    end;

                    weight_value := head_results[local_head_idx].score;
                    if (weight_value <= 0) and head_results[local_head_idx].has_dict_weight then
                    begin
                        weight_value := head_results[local_head_idx].dict_weight;
                    end;
                    Inc(weight_value, get_preferred_query_phrase_bonus_local(
                        head_key, candidate_text));
                    if weight_value > out_score then
                    begin
                        out_score := weight_value;
                        out_text := candidate_text;
                        Result := True;
                    end;
                end;
            end;

            if Result then
            begin
                Exit;
            end;

            for local_head_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_head_idx].comment) <> '' then
                begin
                    if not SameText(normalize_pinyin_text(
                        Trim(candidates[local_head_idx].comment)), tail_key) then
                    begin
                        Continue;
                    end;
                end;

                candidate_text := Trim(candidates[local_head_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> head_syllable_count_local then
                begin
                    Continue;
                end;

                weight_value := candidates[local_head_idx].score +
                    get_preferred_query_phrase_bonus_local(head_key, candidate_text);
                if weight_value > out_score then
                begin
                    out_score := weight_value;
                    out_text := candidate_text;
                    Result := True;
                end;
            end;
        end;

        function try_get_best_visible_complete_candidate_with_tail_local(
            const tail_text_local: string; out out_index: Integer): Boolean;
        var
            local_idx: Integer;
            local_score: Integer;
            local_best_score: Integer;
            local_units: Integer;
        begin
            Result := False;
            out_index := -1;
            local_best_score := Low(Integer);
            if tail_text_local = '' then
            begin
                Exit;
            end;

            for local_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_units := get_candidate_text_unit_count(Trim(candidates[local_idx].text));
                if local_units <> input_syllable_count then
                begin
                    Continue;
                end;
                if not ends_with_text_local(Trim(candidates[local_idx].text), tail_text_local) then
                begin
                    Continue;
                end;

                local_score := candidates[local_idx].score;
                if (local_score > local_best_score) or
                    ((local_score = local_best_score) and (out_index < 0)) then
                begin
                    out_index := local_idx;
                    local_best_score := local_score;
                    Result := True;
                end;
            end;
        end;
    begin
        if (Length(candidates) <= 1) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(lookup_text));
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        for tail_syllables := Min(4, input_syllable_count - 1) downto 1 do
        begin
            if not try_get_best_exact_tail_text_local(
                tail_syllables, best_tail_text, best_tail_score) then
            begin
                Continue;
            end;

            head_syllable_count := input_syllable_count - tail_syllables;
            if not try_get_best_head_complete_text_local(
                head_syllable_count, head_text, head_score) then
            begin
                if try_get_best_visible_complete_candidate_with_tail_local(
                    best_tail_text, existing_index) then
                begin
                    if existing_index = 0 then
                    begin
                        Exit;
                    end;

                    picked := candidates[existing_index];
                    for idx := existing_index downto 1 do
                    begin
                        candidates[idx] := candidates[idx - 1];
                    end;
                    candidates[0] := picked;
                    Exit;
                end;
                Continue;
            end;

            combined_text := head_text + best_tail_text;
            existing_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), combined_text) then
                begin
                    existing_index := idx;
                    Break;
                end;
            end;

            if existing_index = 0 then
            begin
                Exit;
            end
            else if existing_index > 0 then
            begin
                picked := candidates[existing_index];
                for idx := existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
                Exit;
            end;

            picked.text := combined_text;
            picked.comment := '';
            picked.score := head_score + best_tail_score + 960;
            picked.source := cs_rule;
            picked.has_dict_weight := False;
            picked.dict_weight := 0;
            remember_segment_path_for_candidate(picked.text, '', head_text +
                c_segment_path_separator + best_tail_text, picked.score);

            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
            Exit;
        end;
    end;

    procedure ensure_best_short_exact_prefix_tail_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
        c_prefix_tail_bonus = 4096;
        c_quantity_tail_bonus = 4200;
        c_fixed_tail_bonus = 2600;
        c_exact_tail_span_bonus = 16384;
        c_weak_direct_exact_split_weight = 300;
        c_compound_tail_candidate_window = 1400;
        c_compound_tail_bonus_cap = 1800;
    var
        syllables_local: TncPinyinParseResult;
        syllable_count_local: Integer;
        tail_len: Integer;
        head_len: Integer;
        best_text: string;
        best_path: string;
        best_score: Integer;
        head_text: string;
        head_score: Integer;
        head_reliable: Boolean;
        tail_text: string;
        tail_score: Integer;
        tail_reliable: Boolean;
        tail_probe_idx: Integer;
        combined_text: string;
        combined_score: Integer;
        existing_index: Integer;
        idx: Integer;
        picked: TncCandidate;
        tail_is_fixed_single: Boolean;
        preferred_two_tail_text: string;
        preferred_two_tail_score: Integer;
        best_tail_len: Integer;
        best_tail_is_fixed_single: Boolean;
        function_head_text: string;
        function_head_path: string;
        function_head_score: Integer;
        has_function_head_candidate: Boolean;
        pronoun_connector_tail_text: string;
        pronoun_connector_tail_path: string;
        pronoun_connector_tail_score: Integer;
        has_pronoun_connector_tail_candidate: Boolean;
        best_is_pronoun_connector_tail: Boolean;
        inferred_path_score: Integer;
        inferred_path_segment_count: Integer;

        function can_use_four_syllable_compound_tail_local(
            const head_value: string; const tail_value: string;
            const tail_span_len: Integer): Boolean;
        begin
            Result := True;
            if (syllable_count_local <> 4) or (tail_span_len <> 2) then
            begin
                Exit;
            end;

            Result := short_four_two_exact_path_is_supported_local(
                syllables_local, head_value + tail_value);
        end;

        function try_get_protective_full_exact_candidate_local(
            out out_candidate: TncCandidate; out out_weight: Integer): Boolean;
        var
            local_results: TncCandidateList;
            fallback_results: TncCandidateList;
            result_idx: Integer;
            fallback_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            duplicate_found: Boolean;
        begin
            Result := False;
            out_weight := Low(Integer);
            FillChar(out_candidate, SizeOf(out_candidate), 0);

            if lookup_text = '' then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(lookup_text,
                local_results) then
            begin
                SetLength(local_results, 0);
            end;
            if m_dictionary.lookup(lookup_text, fallback_results) then
            begin
                for fallback_idx := 0 to High(fallback_results) do
                begin
                    duplicate_found := False;
                    for result_idx := 0 to High(local_results) do
                    begin
                        if SameText(Trim(local_results[result_idx].text),
                            Trim(fallback_results[fallback_idx].text)) and
                            SameText(Trim(local_results[result_idx].comment),
                            Trim(fallback_results[fallback_idx].comment)) then
                        begin
                            duplicate_found := True;
                            Break;
                        end;
                    end;
                    if duplicate_found then
                    begin
                        Continue;
                    end;
                    SetLength(local_results, Length(local_results) + 1);
                    local_results[High(local_results)] :=
                        fallback_results[fallback_idx];
                end;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(local_results[result_idx].text);
                if (candidate_text = '') or
                    (get_candidate_text_unit_count(candidate_text) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                candidate_weight := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[result_idx].dict_weight;
                end;
                if (local_results[result_idx].source = cs_user) and
                    (candidate_weight < c_weak_direct_exact_split_weight) then
                begin
                    Continue;
                end;
                if (input_syllable_count < 4) and
                    (candidate_weight < c_weak_direct_exact_split_weight) then
                begin
                    Continue;
                end;

                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    lookup_text, candidate_text));
                if (not Result) or (candidate_weight > out_weight) then
                begin
                    Result := True;
                    out_candidate := local_results[result_idx];
                    out_weight := candidate_weight;
                end;
            end;
        end;

        function has_weak_direct_full_exact_candidate_local: Boolean;
        var
            local_results: TncCandidateList;
            fallback_results: TncCandidateList;
            result_idx: Integer;
            fallback_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            duplicate_found: Boolean;
        begin
            Result := False;
            if lookup_text = '' then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(lookup_text,
                local_results) then
            begin
                SetLength(local_results, 0);
            end;
            if m_dictionary.lookup(lookup_text, fallback_results) then
            begin
                for fallback_idx := 0 to High(fallback_results) do
                begin
                    duplicate_found := False;
                    for result_idx := 0 to High(local_results) do
                    begin
                        if SameText(Trim(local_results[result_idx].text),
                            Trim(fallback_results[fallback_idx].text)) and
                            SameText(Trim(local_results[result_idx].comment),
                            Trim(fallback_results[fallback_idx].comment)) then
                        begin
                            duplicate_found := True;
                            Break;
                        end;
                    end;
                    if duplicate_found then
                    begin
                        Continue;
                    end;
                    SetLength(local_results, Length(local_results) + 1);
                    local_results[High(local_results)] :=
                        fallback_results[fallback_idx];
                end;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(local_results[result_idx].text);
                if (candidate_text = '') or
                    (get_candidate_text_unit_count(candidate_text) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                candidate_weight := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := local_results[result_idx].dict_weight;
                end;
                if candidate_weight < c_weak_direct_exact_split_weight then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_covering_partial_anchor_local: Boolean;
        var
            local_idx: Integer;
            partial_text_units: Integer;
            partial_comment_units: Integer;
        begin
            Result := False;
            for local_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_idx].comment) = '' then
                begin
                    Continue;
                end;

                partial_text_units := get_candidate_text_unit_count(
                    Trim(candidates[local_idx].text));
                if partial_text_units < 2 then
                begin
                    Continue;
                end;

                partial_comment_units := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(candidates[local_idx].comment)));
                if (partial_comment_units > 0) and
                    (partial_text_units + partial_comment_units = input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure promote_or_insert_protective_full_exact_candidate_local(
            const exact_candidate: TncCandidate; const exact_weight: Integer);
        var
            local_idx: Integer;
            existing_index: Integer;
            picked_local: TncCandidate;
        begin
            existing_index := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text),
                    Trim(exact_candidate.text)) then
                begin
                    existing_index := local_idx;
                    Break;
                end;
            end;

            if existing_index = 0 then
            begin
                if candidates[0].score < exact_weight then
                begin
                    candidates[0].score := exact_weight;
                end;
                if candidates[0].has_dict_weight and
                    (candidates[0].dict_weight < exact_weight) then
                begin
                    candidates[0].dict_weight := exact_weight;
                end;
                Exit;
            end;

            if existing_index > 0 then
            begin
                picked_local := candidates[existing_index];
                if picked_local.score < exact_weight then
                begin
                    picked_local.score := exact_weight;
                end;
                if picked_local.has_dict_weight and
                    (picked_local.dict_weight < exact_weight) then
                begin
                    picked_local.dict_weight := exact_weight;
                end;
                for local_idx := existing_index downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
                candidates[0] := picked_local;
                Exit;
            end;

            picked_local := exact_candidate;
            if picked_local.score < exact_weight then
            begin
                picked_local.score := exact_weight;
            end;
            if picked_local.has_dict_weight and
                (picked_local.dict_weight < exact_weight) then
            begin
                picked_local.dict_weight := exact_weight;
            end;
            SetLength(candidates, Length(candidates) + 1);
            for local_idx := High(candidates) downto 1 do
            begin
                candidates[local_idx] := candidates[local_idx - 1];
            end;
            candidates[0] := picked_local;
        end;

        function build_query_key_local(const start_idx: Integer;
            const len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (len <= 0) or
                (start_idx + len > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_candidate_weight_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
        end;

        function get_fixed_tail_single_local(const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'ma' then
            begin
                Result := string(Char($5417));
            end
            else if normalized_syllable = 'ne' then
            begin
                Result := string(Char($5462));
            end
            else if normalized_syllable = 'a' then
            begin
                Result := string(Char($554A));
            end
            else if normalized_syllable = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if normalized_syllable = 'la' then
            begin
                Result := string(Char($5566));
            end
            else if normalized_syllable = 'wa' then
            begin
                Result := string(Char($54C7));
            end;
        end;

        function is_modal_tail_syllable_local(const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'ba') or
                (normalized_syllable = 'ma') or
                (normalized_syllable = 'ne') or
                (normalized_syllable = 'a') or
                (normalized_syllable = 'ya') or
                (normalized_syllable = 'la') or
                (normalized_syllable = 'le') or
                (normalized_syllable = 'liao') or
                (normalized_syllable = 'wa');
        end;

        function is_function_head_single_text_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
                $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F, $7684, $4E86,
                $7B2C, $6211, $4F60, $60A8, $5148:
                    Result := True;
            end;
        end;

        function is_quantity_tail_unit_local(const unit_text: string): Boolean;
        var
            codepoint: Integer;
            function try_get_codepoint_local(const value: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if value = '' then
                begin
                    Exit;
                end;

                if Length(value) = 1 then
                begin
                    out_codepoint := Ord(value[1]);
                    Exit(True);
                end;

                if (Length(value) = 2) and
                    (Ord(value[1]) >= $D800) and (Ord(value[1]) <= $DBFF) and
                    (Ord(value[2]) >= $DC00) and (Ord(value[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(value[1]) - $D800) shl 10) +
                        (Ord(value[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if not try_get_codepoint_local(unit_text, codepoint) then
            begin
                Exit;
            end;

            case codepoint of
                $4E2A, $4E9B, $70B9, $4E0B, $6B21, $56DE, $904D,
                $5929, $5E74, $8F6E, $6B65, $6761, $6839, $6279,
                $5339, $90E8, $53F0, $628A, $526F, $5BF9, $53CC,
                $74F6, $6876, $680B, $5E62, $7247, $723F, $76D8,
                $53EA:
                    Result := True;
            end;
        end;

        function text_first_unit_local(const value: string): string;
        var
            units: TArray<string>;
        begin
            Result := '';
            units := split_text_units(value);
            if Length(units) > 0 then
            begin
                Result := units[0];
            end;
        end;

        function text_second_unit_local(const value: string): string;
        var
            units: TArray<string>;
        begin
            Result := '';
            units := split_text_units(value);
            if Length(units) > 1 then
            begin
                Result := units[1];
            end;
        end;

        function try_get_best_exact_phrase_local(const start_idx: Integer;
            const len: Integer; const prefer_quantity_tail: Boolean;
            const prefer_compound_tail: Boolean;
            out out_text: string; out out_score: Integer;
            out out_reliable: Boolean): Boolean;
        var
            query_key: string;
            results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
            direct_weight: Integer;
            best_direct_weight: Integer;
            fixed_first: string;
            first_unit: string;
            second_unit: string;
            split_len: Integer;
            left_text: string;
            right_text: string;
            left_score: Integer;
            right_score: Integer;
            left_reliable: Boolean;
            right_reliable: Boolean;
            compound_tail_support: Integer;

            function has_reliable_dict_weight_local(
                const candidate: TncCandidate): Boolean;
            begin
                Result := candidate.has_dict_weight and
                    (candidate.dict_weight > 0) and
                    ((candidate.score <= 0) or
                    (candidate.dict_weight <= candidate.score * 4));
            end;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);
            out_reliable := False;
            best_direct_weight := Low(Integer);
            query_key := build_query_key_local(start_idx, len);
            if query_key = '' then
            begin
                Exit;
            end;
            if not lookup_exact_full_pinyin_cached_local(query_key, results) then
            begin
                if len >= 3 then
                begin
                    // Exact lookup already falls back to lookup() for legacy
                    // providers.  Avoid probing the same long key again; split
                    // the span into cached exact subspans instead.
                if len = 3 then
                begin
                    for split_len := 2 downto 1 do
                    begin
                        if try_get_best_exact_phrase_local(start_idx,
                            split_len, False, False, left_text, left_score,
                            left_reliable) and
                            try_get_best_exact_phrase_local(start_idx + split_len,
                            len - split_len, False, False, right_text, right_score,
                            right_reliable) then
                        begin
                            candidate_text := left_text + right_text;
                            candidate_score := left_score + right_score +
                                c_prefix_tail_bonus + (len * len * 256);
                            if m_dictionary <> nil then
                            begin
                                Inc(candidate_score, m_dictionary.get_context_bonus(
                                    left_text, right_text));
                            end;
                            if (not Result) or (candidate_score > out_score) or
                                ((candidate_score = out_score) and
                                (CompareText(candidate_text, out_text) < 0)) then
                            begin
                                Result := True;
                                out_text := candidate_text;
                                out_score := candidate_score;
                                out_reliable := left_reliable and right_reliable;
                            end;
                        end;
                    end;
                end
                else
                begin
                        for split_len := len - 1 downto 2 do
                        begin
                            if try_get_best_exact_phrase_local(start_idx,
                                split_len, False, False, left_text, left_score,
                                left_reliable) and
                            try_get_best_exact_phrase_local(start_idx + split_len,
                            len - split_len, False, False, right_text, right_score,
                            right_reliable) then
                        begin
                            candidate_text := left_text + right_text;
                            candidate_score := left_score + right_score +
                                c_prefix_tail_bonus + (len * len * 256);
                            if m_dictionary <> nil then
                            begin
                                Inc(candidate_score, m_dictionary.get_context_bonus(
                                    left_text, right_text));
                            end;
                            if (not Result) or (candidate_score > out_score) or
                                ((candidate_score = out_score) and
                                (CompareText(candidate_text, out_text) < 0)) then
                            begin
                                Result := True;
                                out_text := candidate_text;
                                out_score := candidate_score;
                                out_reliable := left_reliable and right_reliable;
                            end;
                        end;
                    end;
                end;
                Exit;
            end;

                if not m_dictionary.lookup(query_key, results) then
                begin
                    Exit;
                end;
            end;

            fixed_first := '';
            if len >= 2 then
            begin
                fixed_first := get_fixed_tail_single_local(
                    syllables_local[start_idx].text);
            end;

            for result_idx := 0 to High(results) do
            begin
                if result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(results[result_idx].text);
                if (candidate_text = '') or
                    (get_candidate_text_unit_count(candidate_text) <> len) then
                begin
                    Continue;
                end;
                if (len >= 2) and (results[result_idx].source <> cs_user) and
                    (not has_reliable_dict_weight_local(results[result_idx])) then
                begin
                    Continue;
                end;

                candidate_score := get_candidate_weight_local(results[result_idx]);
                if results[result_idx].has_dict_weight and
                    (results[result_idx].dict_weight > 0) then
                begin
                    direct_weight := results[result_idx].dict_weight;
                end
                else
                begin
                    direct_weight := candidate_score;
                end;
                if (results[result_idx].source = cs_user) and
                    (direct_weight >= c_weak_direct_exact_split_weight) then
                begin
                    direct_weight := High(Integer);
                end;
                if direct_weight > best_direct_weight then
                begin
                    best_direct_weight := direct_weight;
                end;
                Inc(candidate_score, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));
                Inc(candidate_score, (c_probe_limit - result_idx) * 96);

                if prefer_quantity_tail and (fixed_first <> '') then
                begin
                    first_unit := text_first_unit_local(candidate_text);
                    second_unit := text_second_unit_local(candidate_text);
                    if SameText(first_unit, fixed_first) and
                        is_quantity_tail_unit_local(second_unit) then
                    begin
                        Inc(candidate_score, c_quantity_tail_bonus);
                    end;
                end;
                if prefer_compound_tail and (len = 2) and
                    (m_dictionary <> nil) and
                    (direct_weight < best_direct_weight) and
                    ((best_direct_weight = Low(Integer)) or
                    ((direct_weight + c_compound_tail_candidate_window) >=
                    best_direct_weight)) then
                begin
                    compound_tail_support :=
                        m_dictionary.get_compound_tail_support(candidate_text);
                    if compound_tail_support > 0 then
                    begin
                        Inc(candidate_score,
                            Min(c_compound_tail_bonus_cap,
                            compound_tail_support));
                    end;
                end;

                if (not Result) or (candidate_score > out_score) or
                    ((candidate_score = out_score) and
                    (CompareText(candidate_text, out_text) < 0)) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_score := candidate_score;
                    out_reliable := (len = 1) or
                        (results[result_idx].source = cs_user) or
                        has_reliable_dict_weight_local(results[result_idx]);
                end;
            end;

            if (len >= 3) and ((not Result) or
                (best_direct_weight < c_weak_direct_exact_split_weight)) then
            begin
                if len = 3 then
                begin
                    for split_len := 2 downto 1 do
                    begin
                        if try_get_best_exact_phrase_local(start_idx,
                            split_len, False, False, left_text, left_score,
                            left_reliable) and
                            try_get_best_exact_phrase_local(start_idx + split_len,
                            len - split_len, False, False, right_text, right_score,
                            right_reliable) then
                        begin
                            candidate_text := left_text + right_text;
                            candidate_score := left_score + right_score +
                                c_prefix_tail_bonus + (len * len * 256);
                            if m_dictionary <> nil then
                            begin
                                Inc(candidate_score, m_dictionary.get_context_bonus(
                                    left_text, right_text));
                            end;
                            if (not Result) or (candidate_score > out_score) or
                                ((candidate_score = out_score) and
                                (CompareText(candidate_text, out_text) < 0)) then
                            begin
                                Result := True;
                                out_text := candidate_text;
                                out_score := candidate_score;
                                out_reliable := left_reliable and right_reliable;
                            end;
                        end;
                    end;
                end
                else
                begin
                    for split_len := len - 1 downto 2 do
                    begin
                        if try_get_best_exact_phrase_local(start_idx,
                            split_len, False, False, left_text, left_score,
                            left_reliable) and
                            try_get_best_exact_phrase_local(start_idx + split_len,
                            len - split_len, False, False, right_text, right_score,
                            right_reliable) then
                        begin
                            candidate_text := left_text + right_text;
                            candidate_score := left_score + right_score +
                                c_prefix_tail_bonus + (len * len * 256);
                            if m_dictionary <> nil then
                            begin
                                Inc(candidate_score, m_dictionary.get_context_bonus(
                                    left_text, right_text));
                            end;
                            if (not Result) or (candidate_score > out_score) or
                                ((candidate_score = out_score) and
                                (CompareText(candidate_text, out_text) < 0)) then
                            begin
                                Result := True;
                                out_text := candidate_text;
                                out_score := candidate_score;
                                out_reliable := left_reliable and right_reliable;
                            end;
                        end;
                    end;
                end;
            end;
        end;

        function try_get_fixed_single_tail_local(const start_idx: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            query_key: string;
            results: TncCandidateList;
            result_idx: Integer;
            candidate_score: Integer;
        begin
            Result := False;
            out_text := get_fixed_tail_single_local(syllables_local[start_idx].text);
            out_score := 0;
            if out_text = '' then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, 1);
            if (query_key <> '') and
                lookup_exact_full_pinyin_cached_local(query_key, results) then
            begin
                for result_idx := 0 to High(results) do
                begin
                    if result_idx >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if (Trim(results[result_idx].comment) <> '') or
                        (not SameText(Trim(results[result_idx].text), out_text)) then
                    begin
                        Continue;
                    end;
                    candidate_score := get_candidate_weight_local(results[result_idx]);
                    if candidate_score > out_score then
                    begin
                        out_score := candidate_score;
                    end;
                end;
            end;

            if out_score <= 0 then
            begin
                out_score := 520;
            end;
            Inc(out_score, c_fixed_tail_bonus);
            Result := True;
        end;

        function try_get_function_head_single_local(const start_idx: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            query_key: string;
            results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            query_key := build_query_key_local(start_idx, 1);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key, results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(results) do
            begin
                if result_idx >= c_probe_limit * 4 then
                begin
                    Break;
                end;
                if Trim(results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(results[result_idx].text);
                if not is_function_head_single_text_local(candidate_text) then
                begin
                    Continue;
                end;

                candidate_score := get_candidate_weight_local(results[result_idx]);
                Inc(candidate_score, (c_probe_limit * 4 - result_idx) * 48);
                if (not Result) or (candidate_score > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_score := candidate_score;
                end;
            end;
        end;

        function path_has_unsupported_single_segment_local(
            const encoded_path: string): Boolean;
        var
            parts: TArray<string>;
            part_idx: Integer;
            part_text: string;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                part_text := Trim(parts[part_idx]);
                if get_candidate_text_unit_count(part_text) <> 1 then
                begin
                    Continue;
                end;
                if is_function_head_single_text_local(part_text) or
                    is_quantity_tail_unit_local(part_text) then
                begin
                    Continue;
                end;
                Exit(True);
            end;
        end;

        function path_has_nonbase_two_plus_single_segment_local(
            const encoded_path: string): Boolean;
        var
            parts: TArray<string>;
            part_idx: Integer;
            syllable_pos: Integer;
            part_units: Integer;
            part_text: string;
            part_key: string;
            head_key_value: string;
            tail_key_value: string;
            head_text_value: string;
            tail_text_value: string;
            unit_texts: TArray<string>;

            function exact_span_has_text_local(const start_idx: Integer;
                const len: Integer; const text_value: string;
                const require_reliable: Boolean): Boolean;
            var
                key_value: string;
                exact_results_value: TncCandidateList;
                exact_idx_value: Integer;
                exact_text_value: string;
            begin
                Result := False;
                key_value := build_query_key_local(start_idx, len);
                if (key_value = '') or (text_value = '') or
                    (not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_value)) then
                begin
                    Exit;
                end;

                for exact_idx_value := 0 to High(exact_results_value) do
                begin
                    if Trim(exact_results_value[exact_idx_value].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_value := Trim(
                        exact_results_value[exact_idx_value].text);
                    if SameText(exact_text_value, text_value) and
                        (get_candidate_text_unit_count(exact_text_value) = len) then
                    begin
                        if require_reliable and
                            (exact_results_value[exact_idx_value].source <> cs_user) and
                            ((not exact_results_value[exact_idx_value].has_dict_weight) or
                            (exact_results_value[exact_idx_value].dict_weight <= 0)) then
                        begin
                            Continue;
                        end;
                        Exit(True);
                    end;
                end;
            end;

            function is_allowed_single_tail_text_local(
                const text_value: string): Boolean;
            begin
                Result := is_modal_tail_syllable_local(tail_key_value) or
                    is_function_head_single_text_local(text_value);
            end;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            syllable_pos := 0;
            for part_idx := 0 to High(parts) do
            begin
                part_text := Trim(parts[part_idx]);
                part_units := get_candidate_text_unit_count(part_text);
                if part_units <= 0 then
                begin
                    Continue;
                end;
                if syllable_pos + part_units > syllable_count_local then
                begin
                    Exit;
                end;

                if part_units = 3 then
                begin
                    part_key := build_query_key_local(syllable_pos, 3);
                    if (part_key <> '') and
                        (not m_dictionary.is_user_entry(part_key, part_text)) and
                        (not exact_span_has_text_local(syllable_pos, 3,
                        part_text, True)) then
                    begin
                        unit_texts := split_text_units(part_text);
                        if Length(unit_texts) = 3 then
                        begin
                            head_key_value := build_query_key_local(
                                syllable_pos, 2);
                            tail_key_value := build_query_key_local(
                                syllable_pos + 2, 1);
                            head_text_value := unit_texts[0] + unit_texts[1];
                            tail_text_value := unit_texts[2];
                            if (not is_allowed_single_tail_text_local(
                                tail_text_value)) and
                                exact_span_has_text_local(syllable_pos, 2,
                                head_text_value, False) and
                                (m_dictionary.single_char_matches_pinyin(
                                tail_key_value, tail_text_value) or
                                exact_span_has_text_local(syllable_pos + 2, 1,
                                tail_text_value, False)) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                Inc(syllable_pos, part_units);
            end;
        end;

        function try_get_function_head_remainder_candidate_local(
            out out_text: string; out out_path: string;
            out out_score: Integer): Boolean;
        var
            head_single_text: string;
            tail_phrase_text: string;
            head_single_score: Integer;
            tail_phrase_score: Integer;
            tail_reliable_local: Boolean;
            tail_span_len: Integer;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := Low(Integer);
            if syllable_count_local < 4 then
            begin
                Exit;
            end;

            tail_span_len := syllable_count_local - 1;
            if (not try_get_function_head_single_local(0, head_single_text,
                head_single_score)) or
                (not try_get_best_exact_phrase_local(1, tail_span_len, False,
                False, tail_phrase_text, tail_phrase_score,
                tail_reliable_local)) or (not tail_reliable_local) then
            begin
                Exit;
            end;

            out_text := head_single_text + tail_phrase_text;
            if get_candidate_text_unit_count(out_text) <> input_syllable_count then
            begin
                Exit;
            end;

            out_path := head_single_text + c_segment_path_separator +
                tail_phrase_text;
            out_score := head_single_score + tail_phrase_score +
                c_prefix_tail_bonus + (tail_span_len * tail_span_len * 512);
            Inc(out_score, m_dictionary.get_context_bonus(head_single_text,
                tail_phrase_text));
            Result := True;
        end;

        function is_pronoun_single_text_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1, $4FFA:
                    Result := True;
            end;
        end;

        function is_connector_single_text_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $8DDF, $548C, $4E0E, $540C:
                    Result := True;
            end;
        end;

        function try_get_best_role_single_local(const start_idx: Integer;
            const require_pronoun: Boolean; out out_text: string;
            out out_score: Integer): Boolean;
        var
            query_key: string;
            results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);
            query_key := build_query_key_local(start_idx, 1);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key, results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(results) do
            begin
                if result_idx >= c_probe_limit * 4 then
                begin
                    Break;
                end;
                if Trim(results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(results[result_idx].text);
                if (require_pronoun and
                    (not is_pronoun_single_text_local(candidate_text))) or
                    ((not require_pronoun) and
                    (not is_connector_single_text_local(candidate_text))) then
                begin
                    Continue;
                end;

                candidate_score := get_candidate_weight_local(results[result_idx]);
                Inc(candidate_score, (c_probe_limit * 4 - result_idx) * 48);
                if (not Result) or (candidate_score > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_score := candidate_score;
                end;
            end;
        end;

        function try_get_pronoun_connector_tail_candidate_local(
            out out_text: string; out out_path: string;
            out out_score: Integer): Boolean;
        var
            pronoun_text: string;
            connector_text: string;
            exact_tail_text: string;
            pronoun_score: Integer;
            connector_score: Integer;
            exact_tail_score: Integer;
            exact_tail_key: string;
            exact_tail_results: TncCandidateList;
            exact_tail_idx: Integer;
            exact_tail_candidate_text: string;
            exact_tail_candidate_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := Low(Integer);
            if (syllable_count_local < 4) or (syllable_count_local > 6) then
            begin
                Exit;
            end;

            if not try_get_best_role_single_local(0, True, pronoun_text,
                pronoun_score) then
            begin
                Exit;
            end;
            if not try_get_best_role_single_local(1, False, connector_text,
                connector_score) then
            begin
                Exit;
            end;
            exact_tail_key := build_query_key_local(2,
                syllable_count_local - 2);
            if (exact_tail_key = '') or
                (not lookup_exact_full_pinyin_cached_local(exact_tail_key,
                exact_tail_results)) then
            begin
                Exit;
            end;
            exact_tail_text := '';
            exact_tail_score := Low(Integer);
            for exact_tail_idx := 0 to High(exact_tail_results) do
            begin
                if exact_tail_idx >= c_probe_limit * 4 then
                begin
                    Break;
                end;
                if Trim(exact_tail_results[exact_tail_idx].comment) <> '' then
                begin
                    Continue;
                end;
                exact_tail_candidate_text := Trim(
                    exact_tail_results[exact_tail_idx].text);
                if (exact_tail_candidate_text = '') or
                    (get_candidate_text_unit_count(exact_tail_candidate_text) <>
                    syllable_count_local - 2) then
                begin
                    Continue;
                end;
                exact_tail_candidate_score := get_candidate_weight_local(
                    exact_tail_results[exact_tail_idx]);
                if (exact_tail_results[exact_tail_idx].source <> cs_user) and
                    (exact_tail_candidate_score <
                    c_weak_direct_exact_split_weight) then
                begin
                    Continue;
                end;
                Inc(exact_tail_candidate_score,
                    (c_probe_limit * 4 - exact_tail_idx) * 32);
                if (exact_tail_text = '') or
                    (exact_tail_candidate_score > exact_tail_score) then
                begin
                    exact_tail_text := exact_tail_candidate_text;
                    exact_tail_score := exact_tail_candidate_score;
                end;
            end;
            if exact_tail_text = '' then
            begin
                Exit;
            end;

            out_text := pronoun_text + connector_text + exact_tail_text;
            if get_candidate_text_unit_count(out_text) <> syllable_count_local then
            begin
                Exit;
            end;

            out_path := pronoun_text + c_segment_path_separator +
                connector_text + c_segment_path_separator + exact_tail_text;
            out_score := pronoun_score + connector_score + exact_tail_score +
                c_prefix_tail_bonus +
                ((syllable_count_local - 3) * c_exact_tail_span_bonus);
            Inc(out_score, m_dictionary.get_context_bonus(pronoun_text,
                connector_text));
            Inc(out_score, m_dictionary.get_context_bonus(connector_text,
                exact_tail_text));
            Result := True;
        end;

        procedure remember_best_candidate_path_local(const candidate_text: string;
            const candidate_score: Integer);
        var
            candidate_key_local: string;
        begin
            remember_segment_path_for_candidate(candidate_text, '', best_path,
                candidate_score);
            if (not best_is_pronoun_connector_tail) or
                (m_current_segment_path_map = nil) then
            begin
                Exit;
            end;

            // Every segment in this path was independently verified above.  Do
            // not let an earlier inferred path with fewer, non-exact segments
            // replace the structurally valid pronoun/connector/tail boundary.
            candidate_key_local := build_candidate_identity_key(candidate_text, '');
            if candidate_key_local <> '' then
            begin
                m_current_segment_path_map.AddOrSetValue(candidate_key_local,
                    best_path);
                remember_segment_path_score_hint_for_candidate(candidate_text,
                    '', candidate_score);
            end;
        end;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count < 3) or (input_syllable_count > 8) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(syllables_local);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;
        has_pronoun_connector_tail_candidate :=
            try_get_pronoun_connector_tail_candidate_local(
            pronoun_connector_tail_text, pronoun_connector_tail_path,
            pronoun_connector_tail_score);
        if (syllable_count_local = 4) and has_covering_partial_anchor_local and
            (not has_pronoun_connector_tail_candidate) and
            (not is_modal_tail_syllable_local(
            syllables_local[High(syllables_local)].text)) then
        begin
            // Four-syllable prefix typing often already has a stable head/tail
            // partial such as "瀹屽叏/buzhi".  Do not spend a full exact-span
            // search only to discard the result below.
            Exit;
        end;
        if (syllable_count_local >= 4) and
            SameText(normalize_pinyin_text(syllables_local[0].text), 'yi') then
        begin
            // Avoid building speculative "yi + ..." short-chain candidates for
            // longer input.  They are often ambiguous and can dominate the
            // normal exact-prefix path while adding multiple exact span probes.
            Exit;
        end;

        if try_get_protective_full_exact_candidate_local(picked,
            best_score) then
        begin
            promote_or_insert_protective_full_exact_candidate_local(picked,
                best_score);
            Exit;
        end;

        if (syllable_count_local = 3) and
            (not has_weak_direct_full_exact_candidate_local) then
        begin
            Exit;
        end;

        best_text := '';
        best_path := '';
        best_score := Low(Integer);
        best_tail_len := 0;
        best_tail_is_fixed_single := False;
        best_is_pronoun_connector_tail := False;
        if has_pronoun_connector_tail_candidate then
        begin
            best_text := pronoun_connector_tail_text;
            best_path := pronoun_connector_tail_path;
            best_score := pronoun_connector_tail_score;
            best_tail_len := syllable_count_local - 2;
            best_is_pronoun_connector_tail := True;
        end;
        has_function_head_candidate :=
            try_get_function_head_remainder_candidate_local(function_head_text,
            function_head_path, function_head_score);
        for tail_probe_idx := 0 to Min(2, syllable_count_local - 2) - 1 do
        begin
            if syllable_count_local = 4 then
            begin
                tail_len := 2 - tail_probe_idx;
            end
            else
            begin
                tail_len := tail_probe_idx + 1;
            end;
            tail_is_fixed_single := False;
            head_len := syllable_count_local - tail_len;
            if not try_get_best_exact_phrase_local(0, head_len,
                (tail_len = 1) and
                is_modal_tail_syllable_local(syllables_local[head_len].text),
                False,
                head_text, head_score, head_reliable) then
            begin
                Continue;
            end;

            if tail_len = 1 then
            begin
                if (head_len >= 1) and
                    try_get_best_exact_phrase_local(head_len - 1, 2, True, False,
                    preferred_two_tail_text, preferred_two_tail_score,
                    tail_reliable) and
                    SameText(text_first_unit_local(preferred_two_tail_text),
                    get_fixed_tail_single_local(syllables_local[head_len - 1].text)) and
                    is_quantity_tail_unit_local(
                    text_second_unit_local(preferred_two_tail_text)) then
                begin
                    Continue;
                end;

                tail_is_fixed_single := try_get_fixed_single_tail_local(
                    head_len, tail_text, tail_score);
                tail_reliable := tail_is_fixed_single;
                if (not tail_is_fixed_single) and
                    not try_get_best_exact_phrase_local(head_len, tail_len,
                    False, False, tail_text, tail_score, tail_reliable) then
                begin
                    Continue;
                end;
            end
            else if not try_get_best_exact_phrase_local(head_len, tail_len,
                True, (syllable_count_local = 4) and (tail_len = 2),
                tail_text, tail_score, tail_reliable) then
            begin
                Continue;
            end;

            combined_text := head_text + tail_text;
            if get_candidate_text_unit_count(combined_text) <>
                input_syllable_count then
            begin
                Continue;
            end;
            if not can_use_four_syllable_compound_tail_local(head_text,
                tail_text, tail_len) then
            begin
                Continue;
            end;

            combined_score := head_score + tail_score + c_prefix_tail_bonus +
                (head_len * head_len * 512);
            if tail_len > 1 then
            begin
                Inc(combined_score, (tail_len - 1) * c_exact_tail_span_bonus);
            end;
            Inc(combined_score, m_dictionary.get_context_bonus(head_text,
                tail_text));
            if (not best_is_pronoun_connector_tail) and
                (combined_score > best_score) then
            begin
                best_score := combined_score;
                best_text := combined_text;
                best_path := head_text + c_segment_path_separator + tail_text;
                best_tail_len := tail_len;
                best_tail_is_fixed_single := tail_is_fixed_single;
                best_is_pronoun_connector_tail := False;
            end;
            if (syllable_count_local = 4) and (tail_len = 2) and
                head_reliable and tail_reliable and (best_text <> '') then
            begin
                Break;
            end;
            if tail_is_fixed_single and (best_text <> '') then
            begin
                Break;
            end;
        end;

        if best_text = '' then
        begin
            if has_function_head_candidate then
            begin
                best_text := function_head_text;
                best_path := function_head_path;
                best_score := function_head_score;
                best_tail_len := syllable_count_local - 1;
                best_tail_is_fixed_single := False;
                best_is_pronoun_connector_tail := False;
            end
            else
            begin
                Exit;
            end;
        end
        else if has_function_head_candidate then
        begin
            best_path := infer_segment_path_for_query_text_with_score(
                lookup_text, best_text, inferred_path_score,
                inferred_path_segment_count);
            if (not best_is_pronoun_connector_tail) and
                ((syllable_count_local >= 5) or
                path_has_unsupported_single_segment_local(best_path) or
                (function_head_score > best_score)) then
            begin
                best_text := function_head_text;
                best_path := function_head_path;
                best_score := function_head_score;
                best_tail_len := syllable_count_local - 1;
                best_tail_is_fixed_single := False;
                best_is_pronoun_connector_tail := False;
            end;
        end;

        if (best_tail_len = 1) and (not best_tail_is_fixed_single) and
            has_covering_partial_anchor_local then
        begin
            Exit;
        end;
        if (not best_is_pronoun_connector_tail) and
            path_has_nonbase_two_plus_single_segment_local(best_path) then
        begin
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), best_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index = 0 then
        begin
            remember_best_candidate_path_local(best_text,
                Max(candidates[0].score, best_score));
            Exit;
        end;

        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
            picked.score := Max(Max(candidates[0].score + 2048,
                picked.score), best_score);
            remember_best_candidate_path_local(picked.text, picked.score);
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end
        else
        begin
            FillChar(picked, SizeOf(picked), 0);
            picked.text := best_text;
            picked.comment := '';
            picked.score := Max(candidates[0].score + 2048, best_score);
            picked.source := cs_rule;
            picked.has_dict_weight := False;
            picked.dict_weight := 0;
            remember_best_candidate_path_local(picked.text, picked.score);
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' sptail=[text=%s score=%d]', [best_text, best_score]);
        end;
    end;

    procedure ensure_two_syllable_quantity_phrase_candidate_visible(
        var candidates: TncCandidateList);
    var
        syllables_local: TncPinyinParseResult;
        head_text: string;
        tail_text: string;
        combined_text: string;
        existing_index: Integer;
        target_index: Integer;
        idx: Integer;
        picked: TncCandidate;

        function get_number_head_text_local(const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if (normalized_syllable = 'er') or (normalized_syllable = 'liang') then
            begin
                Result := string(Char($4E8C));
            end
            else if normalized_syllable = 'san' then
            begin
                Result := string(Char($4E09));
            end
            else if normalized_syllable = 'si' then
            begin
                Result := string(Char($56DB));
            end
            else if normalized_syllable = 'wu' then
            begin
                Result := string(Char($4E94));
            end
            else if normalized_syllable = 'liu' then
            begin
                Result := string(Char($516D));
            end
            else if normalized_syllable = 'qi' then
            begin
                Result := string(Char($4E03));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($516B));
            end
            else if normalized_syllable = 'jiu' then
            begin
                Result := string(Char($4E5D));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($5341));
            end;
        end;

        function get_quantity_tail_text_local(const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'zi' then
            begin
                Result := string(Char($5B57));
            end
            else if normalized_syllable = 'xiang' then
            begin
                Result := string(Char($9879));
            end
            else if normalized_syllable = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable = 'ge' then
            begin
                Result := string(Char($4E2A));
            end
            else if normalized_syllable = 'tiao' then
            begin
                Result := string(Char($6761));
            end
            else if normalized_syllable = 'zhang' then
            begin
                Result := string(Char($5F20));
            end
            else if normalized_syllable = 'dian' then
            begin
                Result := string(Char($70B9));
            end
            else if normalized_syllable = 'bu' then
            begin
                Result := string(Char($6B65));
            end
            else if normalized_syllable = 'lun' then
            begin
                Result := string(Char($8F6E));
            end
            else if normalized_syllable = 'jie' then
            begin
                Result := string(Char($8282));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($9875));
            end
            else if normalized_syllable = 'duan' then
            begin
                Result := string(Char($6BB5));
            end
            else if normalized_syllable = 'hang' then
            begin
                Result := string(Char($884C));
            end
            else if normalized_syllable = 'lie' then
            begin
                Result := string(Char($5217));
            end;
        end;

        function is_protected_complete_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            candidate_text: string;
        begin
            Result := False;
            candidate_text := Trim(candidate_value.text);
            if (candidate_text = '') or (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(candidate_text) <>
                input_syllable_count) then
            begin
                Exit;
            end;
            Exit(True);
        end;

        function get_quantity_insert_index_local: Integer;
        begin
            Result := 0;
            while (Result <= High(candidates)) and
                is_protected_complete_exact_candidate_local(candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count <> 2) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> 2 then
        begin
            Exit;
        end;

        head_text := get_number_head_text_local(syllables_local[0].text);
        tail_text := get_quantity_tail_text_local(syllables_local[1].text);
        if (head_text = '') or (tail_text = '') then
        begin
            Exit;
        end;

        combined_text := head_text + tail_text;
        if SameText(Trim(candidates[0].text), combined_text) and
            (Trim(candidates[0].comment) = '') then
        begin
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), combined_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;
        target_index := get_quantity_insert_index_local;

        if existing_index > target_index then
        begin
            picked := candidates[existing_index];
            for idx := existing_index downto target_index + 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[target_index] := picked;
            existing_index := target_index;
        end
        else if existing_index < 0 then
        begin
            FillChar(picked, SizeOf(picked), 0);
            picked.text := combined_text;
            picked.comment := '';
            picked.score := 0;
            picked.source := cs_rule;
            picked.has_dict_weight := False;
            picked.dict_weight := 0;
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto target_index + 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[target_index] := picked;
            existing_index := target_index;
        end;

        if existing_index >= 0 then
        begin
            remember_segment_path_for_candidate(combined_text, '',
                head_text + c_segment_path_separator + tail_text,
                candidates[existing_index].score);
        end;
    end;

    procedure ensure_best_short_natural_split_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_max_span_local = 3;
        c_probe_limit_local = 8;
        c_min_user_direct_weight_local = 300;
        c_invalid_score_local = Low(Integer) div 4;
    var
        syllables_local: TncPinyinParseResult;
        syllable_count_local: Integer;
        scores: TArray<Integer>;
        texts: TArray<string>;
        paths: TArray<string>;
        last_segments: TArray<string>;
        segment_counts: TArray<Integer>;
        pos: Integer;
        span_len: Integer;
        next_pos: Integer;
        result_idx: Integer;
        results: TncCandidateList;
        query_key: string;
        candidate_text: string;
        candidate_weight: Integer;
        segment_score: Integer;
        next_score: Integer;
        best_text: string;
        best_path: string;
        existing_index: Integer;
        idx: Integer;
        top_text: string;
        top_weight: Integer;
        top_complete: Boolean;
        picked: TncCandidate;

        function build_query_key_local(const start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := start_idx to start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_candidate_weight_local(const candidate: TncCandidate): Integer;
        begin
            if candidate.has_dict_weight and (candidate.dict_weight > 0) then
            begin
                Result := candidate.dict_weight;
                Exit;
            end;
            Result := candidate.score;
        end;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count < 2) or (input_syllable_count > 6) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(syllables_local);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(scores, syllable_count_local + 1);
        SetLength(texts, syllable_count_local + 1);
        SetLength(paths, syllable_count_local + 1);
        SetLength(last_segments, syllable_count_local + 1);
        SetLength(segment_counts, syllable_count_local + 1);
        for idx := 0 to syllable_count_local do
        begin
            scores[idx] := c_invalid_score_local;
            texts[idx] := '';
            paths[idx] := '';
            last_segments[idx] := '';
            segment_counts[idx] := 0;
        end;
        scores[0] := 0;

        for pos := 0 to syllable_count_local - 1 do
        begin
            if scores[pos] <= c_invalid_score_local div 2 then
            begin
                Continue;
            end;

            for span_len := 1 to Min(c_max_span_local,
                syllable_count_local - pos) do
            begin
                if (pos = 0) and (span_len = syllable_count_local) then
                begin
                    Continue;
                end;
                query_key := build_query_key_local(pos, span_len);
                if (query_key = '') or (not m_dictionary.lookup(query_key,
                    results)) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(results) do
                begin
                    if result_idx >= c_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(results[result_idx].text);
                    if (candidate_text = '') or
                        (get_candidate_text_unit_count(candidate_text) <>
                        span_len) then
                    begin
                        Continue;
                    end;

                    candidate_weight := get_candidate_weight_local(
                        results[result_idx]);
                    if (Trim(results[result_idx].comment) = '') and
                        (m_dictionary <> nil) then
                    begin
                        Inc(candidate_weight,
                            m_dictionary.get_query_choice_bonus(query_key,
                            candidate_text) * 8);
                    end;
                    if (span_len >= 2) and
                        (not m_dictionary.is_base_entry(query_key,
                        candidate_text)) and
                        (not ((results[result_idx].source = cs_user) and
                        m_dictionary.is_user_entry(query_key,
                        candidate_text))) then
                    begin
                        candidate_weight := Min(candidate_weight,
                            240 + span_len * 80);
                    end;
                    if (results[result_idx].source = cs_user) and
                        (candidate_weight < c_min_user_direct_weight_local) then
                    begin
                        Continue;
                    end;

                    segment_score := candidate_weight + span_len * 512;
                    if span_len > 1 then
                    begin
                        Inc(segment_score, 1024);
                    end;
                    Inc(segment_score,
                        (c_probe_limit_local - result_idx) * 24);
                    if last_segments[pos] <> '' then
                    begin
                        Inc(segment_score, m_dictionary.get_context_bonus(
                            last_segments[pos], candidate_text));
                    end;

                    next_pos := pos + span_len;
                    next_score := scores[pos] + segment_score;
                    if next_score > scores[next_pos] then
                    begin
                        scores[next_pos] := next_score;
                        texts[next_pos] := texts[pos] + candidate_text;
                        if paths[pos] <> '' then
                        begin
                            paths[next_pos] := paths[pos] +
                                c_segment_path_separator + candidate_text;
                        end
                        else
                        begin
                            paths[next_pos] := candidate_text;
                        end;
                        last_segments[next_pos] := candidate_text;
                        segment_counts[next_pos] := segment_counts[pos] + 1;
                    end;
                end;
            end;
        end;

        if (scores[syllable_count_local] <= c_invalid_score_local div 2) or
            (segment_counts[syllable_count_local] < 2) then
        begin
            Exit;
        end;

        best_text := texts[syllable_count_local];
        best_path := paths[syllable_count_local];
        if (best_text = '') or
            (get_candidate_text_unit_count(best_text) <> input_syllable_count) then
        begin
            Exit;
        end;

        top_text := Trim(candidates[0].text);
        top_complete := (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count);
        top_weight := get_candidate_weight_local(candidates[0]);
        if top_complete and SameText(top_text, best_text) then
        begin
            remember_segment_path_for_candidate(best_text, '', best_path,
                Max(candidates[0].score, scores[syllable_count_local]));
            Exit;
        end;
        if top_complete and (candidates[0].source <> cs_user) and
            (top_weight >= 1800) then
        begin
            Exit;
        end;
        if top_complete and
            (scores[syllable_count_local] < top_weight + 512) then
        begin
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), best_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index = 0 then
        begin
            Exit;
        end;
        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
            picked.score := Max(picked.score, candidates[0].score + 1024);
            picked.has_dict_weight := True;
            picked.dict_weight := Max(picked.dict_weight,
                scores[syllable_count_local]);
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end
        else
        begin
            FillChar(picked, SizeOf(picked), 0);
            picked.text := best_text;
            picked.comment := '';
            picked.score := Max(scores[syllable_count_local],
                candidates[0].score + 1024);
            picked.source := cs_rule;
            picked.has_dict_weight := True;
            picked.dict_weight := scores[syllable_count_local];
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end;
        remember_segment_path_for_candidate(best_text, '', best_path,
            scores[syllable_count_local]);
    end;

    procedure ensure_reliable_full_query_exact_complete_candidate_fast_visible(
        var candidates: TncCandidateList);
    const
        c_min_exact_weight = 700;
        c_min_three_syllable_exact_weight = 520;
        c_min_four_syllable_exact_weight = 520;
        c_supported_split_exact_margin = 512;
    var
        exact_results: TncCandidateList;
        best_idx: Integer;
        best_weight: Integer;
        min_exact_weight: Integer;
        best_candidate: TncCandidate;
        idx: Integer;
        existing_idx: Integer;
        candidate_text: string;
        candidate_weight: Integer;
        exact_support_score: Integer;
        exact_support_segments: Integer;
        top_support_score: Integer;
        top_support_segments: Integer;
        exact_rank: Integer;
        top_rank: Integer;
        exact_path: string;
        top_path: string;
        top_path_hint: string;
        exact_path_hint: string;
        top_text: string;

        function is_modifier_single_text_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $662F, $7684, $4E86, $5728, $548C, $4E0D, $518D, $9AD8,
                $5F88, $592A, $6700, $66F4, $6CA1, $8981, $4F1A, $80FD,
                $628A, $88AB, $8BA9, $7ED9, $5982, $8001, $5C0F, $5927,
                $5168, $8D85, $771F, $597D, $53C8, $4E5F, $5DF2, $5C06,
                $53EF, $60F3, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $65B0, $633A,
                $8F83, $9887, $6781, $5411, $5BF9, $4ECE, $4E8E, $4E0E,
                $800C, $5E76:
                    Result := True;
            end;
        end;

        function get_modifier_head_tail_path_bonus_local(
            const encoded_path: string): Integer;
        var
            path_parts: TArray<string>;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := 0;
            if encoded_path = '' then
            begin
                Exit;
            end;

            path_parts := encoded_path.Split([c_segment_path_separator]);
            if Length(path_parts) <> 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(path_parts[0]);
            tail_text_local := Trim(path_parts[1]);
            if is_modifier_single_text_local(head_text_local) and
                (get_candidate_text_unit_count(tail_text_local) >= 2) then
            begin
                Result := 3500;
            end;
        end;

        function build_support_rank_local(const encoded_path: string;
            const support_score: Integer; const support_segments: Integer;
            const candidate_score: Integer): Integer;
        begin
            if (encoded_path = '') or (support_segments <= 0) then
            begin
                Exit(candidate_score div 8 - 4096);
            end;

            Result := support_score + candidate_score div 32 -
                support_segments * 120 +
                get_modifier_head_tail_path_bonus_local(encoded_path);
        end;

        function build_support_rank_with_hint_local(const encoded_path: string;
            const path_hint: string; const support_score: Integer;
            const support_segments: Integer; const candidate_score: Integer): Integer;
        var
            encoded_bonus: Integer;
            hint_bonus: Integer;
        begin
            encoded_bonus := get_modifier_head_tail_path_bonus_local(encoded_path);
            hint_bonus := get_modifier_head_tail_path_bonus_local(path_hint);
            Result := build_support_rank_local(encoded_path, support_score,
                support_segments, candidate_score);
            if hint_bonus > encoded_bonus then
            begin
                Inc(Result, hint_bonus - encoded_bonus);
            end;
        end;

        function is_sentence_particle_unit_local(const unit_value: string): Boolean;
        begin
            Result := (unit_value = string(Char($4E86))) or
                (unit_value = string(Char($7740))) or

                (unit_value = string(Char($8FC7))) or
                (unit_value = string(Char($5417))) or
                (unit_value = string(Char($561B))) or
                (unit_value = string(Char($5427))) or
                (unit_value = string(Char($5462))) or
                (unit_value = string(Char($554A))) or
                (unit_value = string(Char($5440))) or

                (unit_value = string(Char($5566))) or
                (unit_value = string(Char($7684))) or
                (unit_value = string(Char($5730))) or
                (unit_value = string(Char($5F97)));
        end;

        function should_keep_supported_split_over_full_exact_local(
            const encoded_path: string; const path_hint: string;
            const exact_weight: Integer): Boolean;
        var
            local_path: string;
            path_parts: TArray<string>;
            tail_units: TArray<string>;
        begin
            Result := False;
            if input_syllable_count <> 3 then
            begin
                Exit;
            end;

            local_path := encoded_path;
            if local_path = '' then
            begin
                local_path := path_hint;
            end;
            if local_path = '' then
            begin
                Exit;
            end;

            path_parts := local_path.Split([c_segment_path_separator]);
            if Length(path_parts) <> 2 then
            begin
                Exit;
            end;
            if (get_candidate_text_unit_count(Trim(path_parts[0])) <> 2) then
            begin
                Exit;
            end;

            tail_units := split_text_units(Trim(path_parts[1]));
            if Length(tail_units) <> 1 then
            begin
                Exit;
            end;

            // Keep natural AB + sentence-particle parses (e.g. "鎴愬姛/浜?)
            // from being displaced by rare full-span technical nouns. Other
            // splits such as "宸ョ▼/鏄? or "鍙樻垚/鏂归潰" should not suppress an
            // exact full dictionary phrase.
            Result := is_sentence_particle_unit_local(tail_units[0]) and
                (exact_weight < 1600);
        end;
    begin
        if (m_dictionary = nil) or (input_syllable_count < 3) or
            (input_syllable_count > 8) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        min_exact_weight := c_min_exact_weight;
        if input_syllable_count = 3 then
        begin
            min_exact_weight := c_min_three_syllable_exact_weight;
        end
        else if input_syllable_count = 4 then
        begin
            min_exact_weight := c_min_four_syllable_exact_weight;
        end;

        if not lookup_exact_full_pinyin_cached_local(lookup_text, exact_results) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_weight := Low(Integer);
        for idx := 0 to High(exact_results) do
        begin
            if Trim(exact_results[idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text := Trim(exact_results[idx].text);
            if get_candidate_text_unit_count(candidate_text) <> input_syllable_count then
            begin
                Continue;
            end;

            if exact_results[idx].has_dict_weight then
            begin
                candidate_weight := exact_results[idx].dict_weight;
            end
            else
            begin
                candidate_weight := exact_results[idx].score;
            end;
            if (exact_results[idx].source <> cs_user) and
                (candidate_weight < min_exact_weight) then
            begin
                Continue;
            end;
            Inc(candidate_weight,
                get_preferred_query_phrase_bonus_local(lookup_text,
                candidate_text));

            if (best_idx < 0) or (candidate_weight > best_weight) then
            begin
                best_idx := idx;
                best_weight := candidate_weight;
            end;
        end;

        if best_idx < 0 then
        begin
            Exit;
        end;

        best_candidate := exact_results[best_idx];
        best_candidate.comment := '';
        if best_candidate.score < best_weight then
        begin
            best_candidate.score := best_weight;
        end;
        if not best_candidate.has_dict_weight then
        begin
            best_candidate.has_dict_weight := True;
            best_candidate.dict_weight := best_weight;
        end;
        if (Length(candidates) > 0) and
            (best_candidate.source <> cs_user) then
        begin
            top_text := Trim(candidates[0].text);
            if (top_text <> '') and (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(top_text) = input_syllable_count) and
                (not SameText(top_text, Trim(best_candidate.text))) then
            begin
                top_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, top_text, top_support_score, top_support_segments);
                exact_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(best_candidate.text), exact_support_score,
                    exact_support_segments);
                top_path_hint := get_segment_path_for_candidate(candidates[0], 0);
                exact_path_hint := get_segment_path_for_candidate(best_candidate);
                top_rank := build_support_rank_with_hint_local(top_path,
                    top_path_hint, top_support_score, top_support_segments,
                    candidates[0].score);
                exact_rank := build_support_rank_with_hint_local(exact_path,
                    exact_path_hint, exact_support_score, exact_support_segments,
                    best_candidate.score);
                if (top_path <> '') and
                    (top_rank > exact_rank + c_supported_split_exact_margin) and
                    should_keep_supported_split_over_full_exact_local(top_path,
                    top_path_hint, best_weight) then
                begin
                    Exit;
                end;
            end;
        end;

        if (Length(candidates) > 0) and
            (not SameText(Trim(candidates[0].text),
            Trim(best_candidate.text))) then
        begin
            best_candidate.score := Max(best_candidate.score,
                candidates[0].score + 2048);
            if best_candidate.dict_weight < best_weight then
            begin
                best_candidate.dict_weight := best_weight;
            end;
        end;

        existing_idx := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), Trim(best_candidate.text)) then
            begin
                existing_idx := idx;
                Break;
            end;
        end;

        if existing_idx = 0 then
        begin
            if candidates[0].score < best_candidate.score then
            begin
                candidates[0].score := best_candidate.score;
            end;
            Exit;
        end;

        if existing_idx > 0 then
        begin
            for idx := existing_idx downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := best_candidate;
        end
        else
        begin
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := best_candidate;
        end;

    end;

    procedure prepare_short_two_single_pair_candidates_local;
    const
        c_min_lm_raw_weight = 420;
        c_max_visible_pair_candidates = 2;
        c_generated_complete_base_score = 15000;
    var
        syllables_local: TncPinyinParseResult;
        left_results_local: TncCandidateList;
        right_results_local: TncCandidateList;
        full_exact_results_local: TncCandidateList;
        model_paths_local: TncPairPathEvidenceList;
        accepted_texts_local: TArray<string>;
        accepted_paths_local: TArray<string>;
        accepted_ranks_local: TArray<Integer>;
        parts_local: TArray<string>;
        left_key_local: string;
        right_key_local: string;
        left_text_local: string;
        right_text_local: string;
        combined_text_local: string;
        path_local: string;
        evidence_idx_local: Integer;
        accepted_idx_local: Integer;
        scan_idx_local: Integer;
        left_weight_local: Integer;
        right_weight_local: Integer;
        rank_local: Integer;
        best_rank_local: Integer;
        temp_text_local: string;
        temp_rank_local: Integer;
        candidate_local: TncCandidate;

        function exact_single_weight_local(const results_value: TncCandidateList;
            const text_value: string): Integer;
        var
            idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := -1;
            for idx_local := 0 to High(results_value) do
            begin
                candidate_text_local := Trim(results_value[idx_local].text);
                if (Trim(results_value[idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                    (not SameText(candidate_text_local, text_value)) then
                begin
                    Continue;
                end;
                Result := Max(Result, results_value[idx_local].score);
                if results_value[idx_local].has_dict_weight then
                begin
                    Result := Max(Result, results_value[idx_local].dict_weight);
                end;
            end;
        end;

        function is_full_query_exact_text_local(const text_value: string): Boolean;
        var
            idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            for idx_local := 0 to High(full_exact_results_local) do
            begin
                candidate_text_local := Trim(full_exact_results_local[idx_local].text);
                if (Trim(full_exact_results_local[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(candidate_text_local) = 2) and
                    SameText(candidate_text_local, text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure add_candidate_local(const text_value: string;
            const path_value: string; const rank_value: Integer);
        var
            idx_local: Integer;
        begin
            for idx_local := 0 to High(accepted_texts_local) do
            begin
                if SameText(accepted_texts_local[idx_local], text_value) then
                begin
                    if rank_value > accepted_ranks_local[idx_local] then
                    begin
                        accepted_paths_local[idx_local] := path_value;
                        accepted_ranks_local[idx_local] := rank_value;
                    end;
                    Exit;
                end;
            end;
            idx_local := Length(accepted_texts_local);
            SetLength(accepted_texts_local, idx_local + 1);
            SetLength(accepted_paths_local, idx_local + 1);
            SetLength(accepted_ranks_local, idx_local + 1);
            accepted_texts_local[idx_local] := text_value;
            accepted_paths_local[idx_local] := path_value;
            accepted_ranks_local[idx_local] := rank_value;
        end;
    begin
        if short_two_single_pair_prepared then
        begin
            Exit;
        end;
        short_two_single_pair_prepared := True;
        SetLength(short_two_single_pair_candidates, 0);
        SetLength(short_two_single_pair_paths, 0);
        SetLength(short_two_single_pair_rank_scores, 0);
        if m_config.debug_mode and (input_syllable_count = 2) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t2probe=[apostrophe=%d full=%d]',
                [Ord(has_explicit_apostrophe_input),
                Ord(is_full_pinyin_key(lookup_text))]);
        end;

        if (m_dictionary = nil) or (input_syllable_count <> 2) or
            has_explicit_apostrophe_input or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(lookup_text));
        if Length(syllables_local) <> 2 then
        begin
            Exit;
        end;

        left_key_local := normalize_pinyin_text(syllables_local[0].text);
        right_key_local := normalize_pinyin_text(syllables_local[1].text);
        if (left_key_local = '') or (right_key_local = '') or
            (not lookup_exact_full_pinyin_cached_local(left_key_local,
            left_results_local)) or
            (not lookup_exact_full_pinyin_cached_local(right_key_local,
            right_results_local)) then
        begin
            Exit;
        end;
        SetLength(full_exact_results_local, 0);
        lookup_exact_full_pinyin_cached_local(lookup_text,
            full_exact_results_local);
        SetLength(model_paths_local, 0);
        if not m_dictionary.get_exact_pair_path_evidence(lookup_text,
            model_paths_local) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' t2evidence=0';
            end;
            Exit;
        end;

        SetLength(accepted_texts_local, 0);
        SetLength(accepted_paths_local, 0);
        SetLength(accepted_ranks_local, 0);
        for evidence_idx_local := 0 to High(model_paths_local) do
        begin
            if model_paths_local[evidence_idx_local].lm_transition_weight <
                c_min_lm_raw_weight then
            begin
                Continue;
            end;
            path_local := Trim(model_paths_local[evidence_idx_local].encoded_path);
            parts_local := path_local.Split([c_segment_path_separator]);
            if (Length(parts_local) <> 2) or
                (get_candidate_text_unit_count(Trim(parts_local[0])) <> 1) or
                (get_candidate_text_unit_count(Trim(parts_local[1])) <> 1) then
            begin
                Continue;
            end;
            left_text_local := Trim(parts_local[0]);
            right_text_local := Trim(parts_local[1]);
            combined_text_local := left_text_local + right_text_local;
            if is_full_query_exact_text_local(combined_text_local) then
            begin
                Continue;
            end;
            left_weight_local := exact_single_weight_local(left_results_local,
                left_text_local);
            right_weight_local := exact_single_weight_local(right_results_local,
                right_text_local);
            if (left_weight_local < 0) or (right_weight_local < 0) then
            begin
                Continue;
            end;
            rank_local := model_paths_local[evidence_idx_local].lm_transition_weight *
                4 + Min(4096, left_weight_local) + Min(4096,
                right_weight_local);
            add_candidate_local(combined_text_local, path_local, rank_local);
        end;
        if Length(accepted_texts_local) = 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' t2accepted=0 evidence=%d',
                    [Length(model_paths_local)]);
            end;
            Exit;
        end;

        for accepted_idx_local := 0 to High(accepted_texts_local) - 1 do
        begin
            for scan_idx_local := accepted_idx_local + 1 to
                High(accepted_texts_local) do
            begin
                if accepted_ranks_local[scan_idx_local] <=
                    accepted_ranks_local[accepted_idx_local] then
                begin
                    Continue;
                end;
                temp_text_local := accepted_texts_local[accepted_idx_local];
                accepted_texts_local[accepted_idx_local] :=
                    accepted_texts_local[scan_idx_local];
                accepted_texts_local[scan_idx_local] := temp_text_local;
                temp_text_local := accepted_paths_local[accepted_idx_local];
                accepted_paths_local[accepted_idx_local] :=
                    accepted_paths_local[scan_idx_local];
                accepted_paths_local[scan_idx_local] := temp_text_local;
                temp_rank_local := accepted_ranks_local[accepted_idx_local];
                accepted_ranks_local[accepted_idx_local] :=
                    accepted_ranks_local[scan_idx_local];
                accepted_ranks_local[scan_idx_local] := temp_rank_local;
            end;
        end;
        if Length(accepted_texts_local) > c_max_visible_pair_candidates then
        begin
            SetLength(accepted_texts_local, c_max_visible_pair_candidates);
            SetLength(accepted_paths_local, c_max_visible_pair_candidates);
            SetLength(accepted_ranks_local, c_max_visible_pair_candidates);
        end;

        best_rank_local := accepted_ranks_local[0];
        SetLength(short_two_single_pair_candidates,
            Length(accepted_texts_local));
        SetLength(short_two_single_pair_paths, Length(accepted_texts_local));
        SetLength(short_two_single_pair_rank_scores,
            Length(accepted_texts_local));
        for accepted_idx_local := 0 to High(accepted_texts_local) do
        begin
            FillChar(candidate_local, SizeOf(candidate_local), 0);
            candidate_local.text := accepted_texts_local[accepted_idx_local];
            candidate_local.comment := '';
            candidate_local.score := c_generated_complete_base_score -
                Min(2000, Max(0, best_rank_local -
                accepted_ranks_local[accepted_idx_local]));
            candidate_local.source := cs_rule;
            candidate_local.has_dict_weight := False;
            candidate_local.dict_weight := 0;
            candidate_local.display_kind := cdk_lm_compound;
            short_two_single_pair_candidates[accepted_idx_local] :=
                candidate_local;
            short_two_single_pair_paths[accepted_idx_local] :=
                accepted_paths_local[accepted_idx_local];
            short_two_single_pair_rank_scores[accepted_idx_local] :=
                accepted_ranks_local[accepted_idx_local];
        end;

        m_short_two_single_pair_query := normalize_pinyin_text(lookup_text);
        SetLength(m_short_two_single_pair_texts,
            Length(short_two_single_pair_candidates));
        for accepted_idx_local := 0 to
            High(short_two_single_pair_candidates) do
        begin
            m_short_two_single_pair_texts[accepted_idx_local] :=
                Trim(short_two_single_pair_candidates[accepted_idx_local].text);
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t2single=[n=%d top=%s rank=%d]',
                [Length(short_two_single_pair_candidates),
                Trim(short_two_single_pair_candidates[0].text),
                short_two_single_pair_rank_scores[0]]);
        end;
    end;

    procedure ensure_short_two_single_pair_candidates_visible_local(
        var candidates: TncCandidateList);
    var
        exact_results_local: TncCandidateList;
        candidate_idx_local: Integer;
        accepted_idx_local: Integer;
        move_idx_local: Integer;
        insert_idx_local: Integer;
        picked_local: TncCandidate;

        function is_full_query_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <> 2) then
            begin
                Exit;
            end;
            if candidate_value.source = cs_user then
            begin
                Exit(True);
            end;
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(exact_text_local) = 2) and
                    SameText(exact_text_local, Trim(candidate_value.text)) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        prepare_short_two_single_pair_candidates_local;
        if Length(short_two_single_pair_candidates) = 0 then
        begin
            Exit;
        end;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t2visible-before=%d', [Length(candidates)]);
        end;

        SetLength(exact_results_local, 0);
        lookup_exact_full_pinyin_cached_local(lookup_text,
            exact_results_local);
        for accepted_idx_local := 0 to
            High(short_two_single_pair_candidates) do
        begin
            candidate_idx_local := -1;
            for move_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[move_idx_local].comment) = '') and
                    SameText(Trim(candidates[move_idx_local].text),
                    Trim(short_two_single_pair_candidates[
                    accepted_idx_local].text)) then
                begin
                    candidate_idx_local := move_idx_local;
                    Break;
                end;
            end;
            if candidate_idx_local >= 0 then
            begin
                for move_idx_local := candidate_idx_local to
                    High(candidates) - 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local + 1];
                end;
                SetLength(candidates, Length(candidates) - 1);
            end;
        end;

        insert_idx_local := 0;
        for candidate_idx_local := 0 to High(candidates) do
        begin
            if is_full_query_exact_candidate_local(
                candidates[candidate_idx_local]) then
            begin
                insert_idx_local := candidate_idx_local + 1;
            end;
        end;

        for accepted_idx_local := 0 to
            High(short_two_single_pair_candidates) do
        begin
            picked_local := short_two_single_pair_candidates[
                accepted_idx_local];
            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto
                insert_idx_local + 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[insert_idx_local] := picked_local;
            remember_segment_path_for_candidate(picked_local.text, '',
                short_two_single_pair_paths[accepted_idx_local],
                picked_local.score);
            Inc(insert_idx_local);
        end;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t2visible-after=%d', [Length(candidates)]);
        end;
    end;

    procedure prepare_short_three_exact_pair_candidates_local;
    const
        c_segment_probe_limit = 16;
        c_pair_prebeam_per_split = 8;
        c_max_visible_pair_candidates = 3;
        c_min_lm_raw_weight = 390;
        c_min_query_path_raw_weight = 420;
        c_min_corroborated_raw_weight = 360;
        c_model_evidence_cap = 1560;
        c_generated_complete_base_score = 16000;
    var
        syllables_local: TncPinyinParseResult;
        full_exact_results_local: TncCandidateList;
        head_results_local: TncCandidateList;
        tail_results_local: TncCandidateList;
        model_paths_local: TncPairPathEvidenceList;
        pair_texts_local: TArray<string>;
        pair_paths_local: TArray<string>;
        pair_splits_local: TArray<Integer>;
        pair_head_weights_local: TArray<Integer>;
        pair_tail_weights_local: TArray<Integer>;
        pair_model_evidence_local: TArray<Integer>;
        pair_pre_ranks_local: TArray<Integer>;
        pair_final_ranks_local: TArray<Integer>;
        accepted_indices_local: TArray<Integer>;
        head_key_local: string;
        tail_key_local: string;
        head_text_local: string;
        tail_text_local: string;
        pair_text_local: string;
        pair_path_local: string;
        split_local: Integer;
        head_idx_local: Integer;
        tail_idx_local: Integer;
        pair_idx_local: Integer;
        accepted_idx_local: Integer;
        best_rank_local: Integer;
        model_evidence_local: Integer;
        has_usable_model_evidence_local: Boolean;
        lexical_rank_local: Integer;
        display_score_local: Integer;
        candidate_local: TncCandidate;

        function build_span_key_local(const start_idx: Integer;
            const span_len: Integer): string;
        var
            idx_local: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (span_len <= 0) or
                (start_idx + span_len > Length(syllables_local)) then
            begin
                Exit;
            end;
            for idx_local := start_idx to start_idx + span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[idx_local].text);
            end;
        end;

        function candidate_weight_local(const candidate_value: TncCandidate): Integer;
        begin
            Result := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;
            if candidate_value.source = cs_user then
            begin
                Result := Max(Result, 4096);
            end;
            Result := Max(0, Min(4096, Result));
        end;

        function has_full_query_exact_local: Boolean;
        var
            idx_local: Integer;
            text_local: string;
        begin
            Result := False;
            if not lookup_exact_full_pinyin_cached_local(lookup_text,
                full_exact_results_local) then
            begin
                Exit;
            end;
            for idx_local := 0 to High(full_exact_results_local) do
            begin
                text_local := Trim(full_exact_results_local[idx_local].text);
                if (Trim(full_exact_results_local[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(text_local) = 3) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure collect_exact_segments_local(const key_value: string;
            const expected_units_value: Integer; out results_value: TncCandidateList);
        var
            raw_results_local: TncCandidateList;
            raw_idx_local: Integer;
            result_idx_local: Integer;
            existing_idx_local: Integer;
            text_local: string;
        begin
            SetLength(results_value, 0);
            if (key_value = '') or
                (not m_dictionary.lookup_isolated_exact_component(key_value,
                raw_results_local)) then
            begin
                Exit;
            end;

            for raw_idx_local := 0 to High(raw_results_local) do
            begin
                if Length(results_value) >= c_segment_probe_limit then
                begin
                    Break;
                end;
                text_local := Trim(raw_results_local[raw_idx_local].text);
                if (Trim(raw_results_local[raw_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(text_local) <>
                    expected_units_value) or
                    ((raw_results_local[raw_idx_local].source <> cs_user) and
                    (not raw_results_local[raw_idx_local].has_dict_weight) and
                    (not m_dictionary.is_base_entry(key_value, text_local))) then
                begin
                    Continue;
                end;

                existing_idx_local := -1;
                for result_idx_local := 0 to High(results_value) do
                begin
                    if SameText(Trim(results_value[result_idx_local].text),
                        text_local) then
                    begin
                        existing_idx_local := result_idx_local;
                        Break;
                    end;
                end;
                if existing_idx_local >= 0 then
                begin
                    if candidate_weight_local(raw_results_local[raw_idx_local]) >
                        candidate_weight_local(results_value[existing_idx_local]) then
                    begin
                        results_value[existing_idx_local] :=
                            raw_results_local[raw_idx_local];
                    end;
                    Continue;
                end;

                result_idx_local := Length(results_value);
                SetLength(results_value, result_idx_local + 1);
                results_value[result_idx_local] := raw_results_local[raw_idx_local];
            end;
        end;

        function get_model_evidence_local(const path_value: string): Integer;
        var
            idx_local: Integer;
            query_path_raw_local: Integer;
            lm_raw_local: Integer;
            path_penalty_local: Integer;
        begin
            Result := 0;
            query_path_raw_local := 0;
            lm_raw_local := 0;
            for idx_local := 0 to High(model_paths_local) do
            begin
                if SameText(Trim(model_paths_local[idx_local].encoded_path),
                    path_value) then
                begin
                    query_path_raw_local := Max(query_path_raw_local,
                        model_paths_local[idx_local].query_path_weight);
                    lm_raw_local := Max(lm_raw_local,
                        model_paths_local[idx_local].lm_transition_weight);
                end;
            end;

            if (lm_raw_local >= c_min_lm_raw_weight) or
                ((lm_raw_local >= c_min_corroborated_raw_weight) and
                (query_path_raw_local >= c_min_corroborated_raw_weight)) then
            begin
                Result := Min(c_model_evidence_cap, 3 * lm_raw_local);
            end;
            if query_path_raw_local >= c_min_query_path_raw_weight then
            begin
                Result := Max(Result, Min(420,
                    (query_path_raw_local * 3) div 5 + 28));
            end;

            if Result = 0 then Exit;
            path_penalty_local := Max(0, get_cached_query_segment_path_penalty(
                lookup_text, path_value));
            if path_penalty_local > 0 then
            begin
                Result := Max(0, Result - path_penalty_local);
            end;
        end;

        procedure add_pair_prebeam_local(const split_value: Integer;
            const head_candidate_value: TncCandidate;
            const tail_candidate_value: TncCandidate);
        var
            idx_local: Integer;
            same_split_count_local: Integer;
            worst_idx_local: Integer;
            worst_rank_local: Integer;
            head_weight_local: Integer;
            tail_weight_local: Integer;
            pre_rank_local: Integer;

            procedure write_pair_local(const target_idx: Integer);
            begin
                pair_texts_local[target_idx] := pair_text_local;
                pair_paths_local[target_idx] := pair_path_local;
                pair_splits_local[target_idx] := split_value;
                pair_head_weights_local[target_idx] := head_weight_local;
                pair_tail_weights_local[target_idx] := tail_weight_local;
                pair_model_evidence_local[target_idx] := model_evidence_local;
                pair_pre_ranks_local[target_idx] := pre_rank_local;
            end;
        begin
            head_text_local := Trim(head_candidate_value.text);
            tail_text_local := Trim(tail_candidate_value.text);
            if (head_text_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;
            pair_text_local := head_text_local + tail_text_local;
            pair_path_local := head_text_local + c_segment_path_separator +
                tail_text_local;
            head_weight_local := candidate_weight_local(head_candidate_value);
            tail_weight_local := candidate_weight_local(tail_candidate_value);
            model_evidence_local := get_model_evidence_local(pair_path_local);
            pre_rank_local := head_weight_local + tail_weight_local +
                model_evidence_local * 2;

            for idx_local := 0 to High(pair_texts_local) do
            begin
                if SameText(pair_texts_local[idx_local], pair_text_local) then
                begin
                    if pre_rank_local > pair_pre_ranks_local[idx_local] then
                    begin
                        write_pair_local(idx_local);
                    end;
                    Exit;
                end;
            end;

            same_split_count_local := 0;
            worst_idx_local := -1;
            worst_rank_local := High(Integer);
            for idx_local := 0 to High(pair_texts_local) do
            begin
                if pair_splits_local[idx_local] <> split_value then
                begin
                    Continue;
                end;
                Inc(same_split_count_local);
                if pair_pre_ranks_local[idx_local] < worst_rank_local then
                begin
                    worst_rank_local := pair_pre_ranks_local[idx_local];
                    worst_idx_local := idx_local;
                end;
            end;

            if same_split_count_local >= c_pair_prebeam_per_split then
            begin
                if (worst_idx_local >= 0) and
                    (pre_rank_local > worst_rank_local) then
                begin
                    write_pair_local(worst_idx_local);
                end;
                Exit;
            end;

            idx_local := Length(pair_texts_local);
            SetLength(pair_texts_local, idx_local + 1);
            SetLength(pair_paths_local, idx_local + 1);
            SetLength(pair_splits_local, idx_local + 1);
            SetLength(pair_head_weights_local, idx_local + 1);
            SetLength(pair_tail_weights_local, idx_local + 1);
            SetLength(pair_model_evidence_local, idx_local + 1);
            SetLength(pair_pre_ranks_local, idx_local + 1);
            write_pair_local(idx_local);
        end;

        procedure add_accepted_index_local(const pair_index_value: Integer);
        var
            idx_local: Integer;
        begin
            if (pair_index_value < 0) or
                (pair_index_value > High(pair_texts_local)) then
            begin
                Exit;
            end;
            for idx_local := 0 to High(accepted_indices_local) do
            begin
                if SameText(pair_texts_local[accepted_indices_local[idx_local]],
                    pair_texts_local[pair_index_value]) then
                begin
                    if pair_final_ranks_local[pair_index_value] >
                        pair_final_ranks_local[accepted_indices_local[idx_local]] then
                    begin
                        accepted_indices_local[idx_local] := pair_index_value;
                    end;
                    Exit;
                end;
            end;
            idx_local := Length(accepted_indices_local);
            SetLength(accepted_indices_local, idx_local + 1);
            accepted_indices_local[idx_local] := pair_index_value;
        end;

        procedure sort_accepted_indices_local;
        var
            left_idx_local: Integer;
            right_idx_local: Integer;
            temp_idx_local: Integer;
        begin
            for left_idx_local := 0 to High(accepted_indices_local) - 1 do
            begin
                for right_idx_local := left_idx_local + 1 to
                    High(accepted_indices_local) do
                begin
                    if pair_final_ranks_local[
                        accepted_indices_local[right_idx_local]] >
                        pair_final_ranks_local[
                        accepted_indices_local[left_idx_local]] then
                    begin
                        temp_idx_local := accepted_indices_local[left_idx_local];
                        accepted_indices_local[left_idx_local] :=
                            accepted_indices_local[right_idx_local];
                        accepted_indices_local[right_idx_local] := temp_idx_local;
                    end;
                end;
            end;
        end;

        procedure consider_attested_char_pair_local;
        const
            c_min_component_weight = 80;
            c_min_trigram_score = -3072;
            c_min_particle_bigram_score = -6144;
            c_min_conditional_score = -12288;
            c_min_competitor_margin = 1024;
            // Below the display-stage dictionary-exact override threshold.
            c_char_evidence = 350;
        var
            ngrams_local: TArray<string>;
            observed_scores_local: TArray<Integer>;
            text_scores_local: TArray<Integer>;
            context_scores_local: TArray<Integer>;
            units_local: TArray<string>;
            idx_local, best_idx_local: Integer;
            score_local, best_score_local, next_score_local: Integer;
            particle_key_local, particle_text_local, particle_head_local: string;
            left_context_local: string;
            particle_query_local, attested_local: Boolean;

            function path_is_attested_local(const index: Integer): Boolean;
            begin
                Result := observed_scores_local[index * 3] >= c_min_trigram_score;
                if (not Result) and particle_query_local and
                    (pair_splits_local[index] = 2) and
                    (Copy(pair_texts_local[index], Length(pair_texts_local[index]),
                    1) = particle_text_local) then
                    Result := observed_scores_local[index * 3 + 1] >=
                        c_min_particle_bigram_score;
            end;
        begin
            // This is a bounded fallback for missing word transitions, not a
            // replacement for them. Backoff probability alone cannot admit a path.
            if has_usable_model_evidence_local or
                (Length(pair_texts_local) < 2) then Exit;
            SetLength(ngrams_local, Length(pair_texts_local) * 3);
            for idx_local := 0 to High(pair_texts_local) do
            begin
                units_local := split_text_units(pair_texts_local[idx_local]);
                if Length(units_local) <> 3 then Exit;
                ngrams_local[idx_local * 3] := pair_texts_local[idx_local];
                ngrams_local[idx_local * 3 + 1] := units_local[1] + units_local[2];
                ngrams_local[idx_local * 3 + 2] := units_local[0];
            end;
            if (not m_dictionary.get_char_lm_attested_scores(ngrams_local,
                observed_scores_local)) or
                (Length(observed_scores_local) <> Length(ngrams_local)) then Exit;
            particle_query_local := try_get_short_particle_tail_query_parts(
                lookup_text, particle_key_local, particle_text_local,
                particle_head_local);
            attested_local := False;
            for idx_local := 0 to High(pair_texts_local) do
                if path_is_attested_local(idx_local) then
                begin
                    attested_local := True;
                    Break;
                end;
            if not attested_local then Exit;
            if (not m_dictionary.get_char_lm_continuation_scores('', pair_texts_local,
                text_scores_local)) or
                (Length(text_scores_local) <> Length(pair_texts_local)) then Exit;
            best_idx_local := -1;
            best_score_local := Low(Integer);
            next_score_local := Low(Integer);
            for idx_local := 0 to High(pair_texts_local) do
            begin
                if observed_scores_local[idx_local * 3 + 2] = Low(Integer) then Continue;
                // Rank whole phrases, without an artificial sentence end. The
                // scorer returns a per-character mean; all paths have three units.
                score_local := 3 * text_scores_local[idx_local];
                if score_local > best_score_local then
                begin
                    next_score_local := best_score_local;
                    best_score_local := score_local;
                    best_idx_local := idx_local;
                end
                else next_score_local := Max(next_score_local, score_local);
            end;
            if m_config.debug_mode and (best_idx_local >= 0) then
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' [s3char=%s score=%d next=%d obs=%d/%d w=%d/%d]',
                    [pair_texts_local[best_idx_local], best_score_local,
                    next_score_local, observed_scores_local[best_idx_local * 3],
                    observed_scores_local[best_idx_local * 3 + 1],
                    pair_head_weights_local[best_idx_local],
                    pair_tail_weights_local[best_idx_local]]);
            if (best_idx_local < 0) or (next_score_local = Low(Integer)) or
                (best_score_local - observed_scores_local[
                best_idx_local * 3 + 2] < c_min_conditional_score) or
                (best_score_local - next_score_local < c_min_competitor_margin) or
                (pair_head_weights_local[best_idx_local] < c_min_component_weight) or
                (pair_tail_weights_local[best_idx_local] < c_min_component_weight) or
                (get_cached_query_segment_path_penalty(lookup_text,
                pair_paths_local[best_idx_local]) > 0) then Exit;

            if not path_is_attested_local(best_idx_local) then Exit;

            // A context-free phrase preference must not overrule a conflicting
            // preceding context. Leave that decision to the contextual ranker.
            left_context_local := Trim(m_segment_left_context);
            if left_context_local = '' then
                left_context_local := Trim(m_external_left_context);
            if left_context_local = '' then
                left_context_local := Trim(m_left_context);
            left_context_local := context_model_tail(left_context_local);
            if left_context_local <> '' then
            begin
                if (not get_cached_char_lm_scores(pair_texts_local,
                    context_scores_local, clsm_context, left_context_local)) or
                    (Length(context_scores_local) <> Length(pair_texts_local)) then Exit;
                for idx_local := 0 to High(pair_texts_local) do
                    if (idx_local <> best_idx_local) and
                        (3 * (Int64(context_scores_local[best_idx_local]) -
                        context_scores_local[idx_local]) < c_min_competitor_margin) then
                    begin
                        if m_config.debug_mode then
                            m_last_full_path_debug_info := m_last_full_path_debug_info +
                                ' [s3char-context-abstain]';
                        Exit;
                    end;
            end;

            pair_model_evidence_local[best_idx_local] := 3 * c_char_evidence;
            m_short_three_char_pair_path := pair_paths_local[best_idx_local];
            m_short_three_char_pair_evidence := c_char_evidence;
        end;
    begin
        if short_three_exact_pair_prepared then
        begin
            Exit;
        end;
        short_three_exact_pair_prepared := True;
        SetLength(short_three_exact_pair_candidates, 0);
        SetLength(short_three_exact_pair_paths, 0);
        SetLength(short_three_exact_pair_rank_scores, 0);
        m_short_three_char_pair_path := '';
        m_short_three_char_pair_evidence := 0;

        if (m_dictionary = nil) or (input_syllable_count <> 3) or
            has_explicit_apostrophe_input or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(lookup_text));
        if (Length(syllables_local) <> 3) or has_full_query_exact_local then
        begin
            Exit;
        end;

        SetLength(model_paths_local, 0);
        m_dictionary.get_exact_pair_path_evidence(lookup_text,
            model_paths_local);
        has_usable_model_evidence_local := False;
        for pair_idx_local := 0 to High(model_paths_local) do
        begin
            if (model_paths_local[pair_idx_local].lm_transition_weight >=
                c_min_lm_raw_weight) or
                (model_paths_local[pair_idx_local].query_path_weight >=
                c_min_query_path_raw_weight) or
                ((model_paths_local[pair_idx_local].lm_transition_weight >=
                c_min_corroborated_raw_weight) and
                (model_paths_local[pair_idx_local].query_path_weight >=
                c_min_corroborated_raw_weight)) then
            begin
                has_usable_model_evidence_local := True;
                Break;
            end;
        end;
        SetLength(pair_texts_local, 0);
        SetLength(pair_paths_local, 0);
        SetLength(pair_splits_local, 0);
        SetLength(pair_head_weights_local, 0);
        SetLength(pair_tail_weights_local, 0);
        SetLength(pair_model_evidence_local, 0);
        SetLength(pair_pre_ranks_local, 0);

        for split_local := 1 to 2 do
        begin
            head_key_local := build_span_key_local(0, split_local);
            tail_key_local := build_span_key_local(split_local,
                3 - split_local);
            collect_exact_segments_local(head_key_local, split_local,
                head_results_local);
            collect_exact_segments_local(tail_key_local, 3 - split_local,
                tail_results_local);
            for head_idx_local := 0 to High(head_results_local) do
            begin
                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    add_pair_prebeam_local(split_local,
                        head_results_local[head_idx_local],
                        tail_results_local[tail_idx_local]);
                end;
            end;
        end;
        if Length(pair_texts_local) = 0 then
        begin
            Exit;
        end;

        consider_attested_char_pair_local;

        SetLength(pair_final_ranks_local, Length(pair_texts_local));
        SetLength(accepted_indices_local, 0);
        for pair_idx_local := 0 to High(pair_texts_local) do
        begin
            lexical_rank_local := pair_head_weights_local[pair_idx_local] +
                pair_tail_weights_local[pair_idx_local];
            pair_final_ranks_local[pair_idx_local] := lexical_rank_local +
                pair_model_evidence_local[pair_idx_local] * 2;
            if pair_model_evidence_local[pair_idx_local] > 0 then
            begin
                add_accepted_index_local(pair_idx_local);
            end;
        end;
        if Length(accepted_indices_local) = 0 then
        begin
            Exit;
        end;
        sort_accepted_indices_local;
        if Length(accepted_indices_local) > c_max_visible_pair_candidates then
        begin
            SetLength(accepted_indices_local, c_max_visible_pair_candidates);
        end;

        best_rank_local := pair_final_ranks_local[accepted_indices_local[0]];
        for accepted_idx_local := 0 to High(accepted_indices_local) do
        begin
            pair_idx_local := accepted_indices_local[accepted_idx_local];
            display_score_local := c_generated_complete_base_score -
                Min(3000, Max(0, best_rank_local -
                pair_final_ranks_local[pair_idx_local]));
            FillChar(candidate_local, SizeOf(candidate_local), 0);
            candidate_local.text := pair_texts_local[pair_idx_local];
            candidate_local.comment := '';
            candidate_local.score := display_score_local;
            candidate_local.source := cs_rule;
            candidate_local.has_dict_weight := False;
            candidate_local.dict_weight := 0;
            candidate_local.display_kind := cdk_lm_compound;

            SetLength(short_three_exact_pair_candidates,
                accepted_idx_local + 1);
            SetLength(short_three_exact_pair_paths, accepted_idx_local + 1);
            SetLength(short_three_exact_pair_rank_scores,
                accepted_idx_local + 1);
            short_three_exact_pair_candidates[accepted_idx_local] :=
                candidate_local;
            short_three_exact_pair_paths[accepted_idx_local] :=
                pair_paths_local[pair_idx_local];
            short_three_exact_pair_rank_scores[accepted_idx_local] :=
                pair_final_ranks_local[pair_idx_local];
        end;

        m_short_three_exact_pair_query := normalize_pinyin_text(lookup_text);
        SetLength(m_short_three_exact_pair_texts,
            Length(short_three_exact_pair_candidates));
        for accepted_idx_local := 0 to
            High(short_three_exact_pair_candidates) do
        begin
            m_short_three_exact_pair_texts[accepted_idx_local] :=
                Trim(short_three_exact_pair_candidates[accepted_idx_local].text);
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t3pair=[n=%d top=%s rank=%d]',
                [Length(short_three_exact_pair_candidates),
                Trim(short_three_exact_pair_candidates[0].text),
                short_three_exact_pair_rank_scores[0]]);
        end;
    end;

    function is_short_three_exact_pair_candidate_supported_local(
        const candidate_text: string): Boolean;
    begin
        Result := short_three_exact_pair_prepared and
            is_current_short_three_exact_pair_candidate_supported(lookup_text,
            candidate_text);
    end;

    procedure ensure_short_three_exact_pair_candidates_visible_local(
        var candidates: TncCandidateList);
    var
        accepted_idx_local: Integer;
        candidate_idx_local: Integer;
        move_idx_local: Integer;
        picked_local: TncCandidate;
    begin
        prepare_short_three_exact_pair_candidates_local;
        if Length(short_three_exact_pair_candidates) = 0 then
        begin
            Exit;
        end;

        for accepted_idx_local := High(short_three_exact_pair_candidates)
            downto 0 do
        begin
            candidate_idx_local := -1;
            for move_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[move_idx_local].comment) = '') and
                    SameText(Trim(candidates[move_idx_local].text),
                    Trim(short_three_exact_pair_candidates[
                    accepted_idx_local].text)) then
                begin
                    candidate_idx_local := move_idx_local;
                    Break;
                end;
            end;
            if candidate_idx_local >= 0 then
            begin
                for move_idx_local := candidate_idx_local to
                    High(candidates) - 1 do
                begin
                    candidates[move_idx_local] := candidates[move_idx_local + 1];
                end;
                SetLength(candidates, Length(candidates) - 1);
            end;

            picked_local := short_three_exact_pair_candidates[
                accepted_idx_local];
            SetLength(candidates, Length(candidates) + 1);
            for move_idx_local := High(candidates) downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := picked_local;
            remember_segment_path_for_candidate(picked_local.text, '',
                short_three_exact_pair_paths[accepted_idx_local],
                picked_local.score);
        end;
    end;

    procedure ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_tail_probe_limit = 48;
        c_context_scale = 4;
        c_preferred_tail_bonus = 96;
        c_complete_promote_bonus = 1152;
        c_reliable_competing_exact_weight = 520;
    var
        effective_syllables: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        tail_results: TncCandidateList;
        head_texts: TArray<string>;
        head_scores: TArray<Integer>;
        best_tail_text: string;
        best_prefix_text: string;
        best_path_text: string;
        combined_text: string;
        candidate_combined_text: string;
        candidate_text: string;
        resolved_text_local: string;
        resolved_path_local: string;
        resolved_has_anchor_local: Boolean;
        best_tail_score: Integer;
        candidate_score: Integer;
        context_bonus: Integer;
        prefix_popularity: Integer;
        query_bonus: Integer;
        exact_bonus: Integer;
        preferred_exact_bonus: Integer;
        idx: Integer;
        existing_index: Integer;
        candidate_units: Integer;
        resolved_segments_local: Integer;
        resolved_anchor_units_local: Integer;
        is_preferred: Boolean;
        picked: TncCandidate;
        best_tail_context_bonus: Integer;
        prefix_results: TncCandidateList;
        head_option_idx: Integer;
        ordinal_tail_allowed: Boolean;

        function build_syllable_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function build_prefix_text_local(const text_value: string;
            const unit_count_local: Integer): string;
        var
            local_units: TArray<string>;
            local_idx: Integer;
        begin
            Result := '';
            local_units := split_text_units(Trim(text_value));
            if Length(local_units) < unit_count_local then
            begin
                Exit;
            end;
            for local_idx := 0 to unit_count_local - 1 do
            begin
                Result := Result + local_units[local_idx];
            end;
        end;

        function get_candidate_score_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if Result < 0 then
            begin
                Result := 0;
            end;
        end;

        function get_exact_query_text_bonus_local(const candidate_text_local: string): Integer;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (lookup_text = '') or
                (candidate_text_local = '') then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(lookup_text, local_results) then
            begin
                Exit;
            end;
            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(local_results[local_idx].text), candidate_text_local) then
                begin
                    Exit(3072);
                end;
            end;
        end;

        function has_competing_exact_full_phrase_local(
            const candidate_text_local: string): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
            local_choice_priority: Integer;
            best_choice_priority: Integer;
            best_text: string;
            best_score: Integer;
            normalized_query: string;

            function is_low_evidence_single_char_chain_local(
                const local_candidate: TncCandidate; const local_text_value: string): Boolean;
            var
                local_units: TArray<string>;
                local_unit_idx: Integer;
                local_preferred: Boolean;
                local_weight: Integer;
            begin
                Result := False;
                if local_candidate.source <> cs_user then
                begin
                    Exit;
                end;

                local_weight := local_candidate.dict_weight;
                if local_candidate.score > local_weight then
                begin
                    local_weight := local_candidate.score;
                end;
                if local_weight > 1 then
                begin
                    Exit;
                end;

                local_units := split_text_units(Trim(local_text_value));
                if Length(local_units) <> input_syllable_count then
                begin
                    Exit;
                end;

                for local_unit_idx := 0 to High(local_units) do
                begin
                    if (get_candidate_text_unit_count(local_units[local_unit_idx]) <> 1) or
                        (not match_single_char_candidate_for_syllable(
                        effective_syllables[local_unit_idx].text,
                        local_units[local_unit_idx], local_preferred)) then
                    begin
                        Exit;
                    end;
                end;

                Result := True;
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (lookup_text = '') or
                (Trim(candidate_text_local) = '') then
            begin
                Exit;
            end;

            normalized_query := normalize_pinyin_text(lookup_text);
            if not m_dictionary.lookup_exact_full_pinyin(lookup_text,
                local_results) then
            begin
                Exit;
            end;

            best_text := '';
            best_score := Low(Integer);
            best_choice_priority := 0;
            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if get_candidate_text_unit_count(local_text) <> input_syllable_count then
                begin
                    Continue;
                end;

                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end
                else if local_score < 0 then
                begin
                    local_score := 0;
                end;

                local_choice_priority := 0;
                if not is_low_evidence_single_char_chain_local(
                    local_results[local_idx], local_text) then
                begin
                    if is_latest_session_query_choice(local_text) then
                    begin
                        Inc(local_choice_priority, 200000);
                    end;
                    if m_dictionary.get_query_choice_bonus(normalized_query,
                        local_text) > 0 then
                    begin
                        Inc(local_choice_priority, 50000 +
                            (m_dictionary.get_query_choice_bonus(
                            normalized_query, local_text) * 20));
                    end;
                    Inc(local_score, local_choice_priority);
                end;

                if (best_text = '') or (local_score > best_score) then
                begin
                    best_text := local_text;
                    best_score := local_score;
                    best_choice_priority := local_choice_priority;
                end;
            end;

            Result := (best_text <> '') and
                (not SameText(best_text, Trim(candidate_text_local))) and
                ((best_score >= c_reliable_competing_exact_weight) or
                (best_choice_priority > 0));
        end;

        procedure add_head_option_local(const text_value: string;
            const score_value: Integer);
        var
            option_idx: Integer;
            insert_idx: Integer;
            tmp_text: string;
            tmp_score: Integer;
        begin
            if text_value = '' then
            begin
                Exit;
            end;
            for option_idx := 0 to High(head_texts) do
            begin
                if SameText(head_texts[option_idx], text_value) then
                begin
                    if score_value > head_scores[option_idx] then
                    begin
                        head_scores[option_idx] := score_value;
                    end;
                    Exit;
                end;
            end;

            insert_idx := Length(head_texts);
            SetLength(head_texts, insert_idx + 1);
            SetLength(head_scores, insert_idx + 1);
            head_texts[insert_idx] := text_value;
            head_scores[insert_idx] := score_value;

            while (insert_idx > 0) and
                (head_scores[insert_idx] > head_scores[insert_idx - 1]) do
            begin
                tmp_text := head_texts[insert_idx - 1];
                head_texts[insert_idx - 1] := head_texts[insert_idx];
                head_texts[insert_idx] := tmp_text;
                tmp_score := head_scores[insert_idx - 1];
                head_scores[insert_idx - 1] := head_scores[insert_idx];
                head_scores[insert_idx] := tmp_score;
                Dec(insert_idx);
            end;

            if Length(head_texts) > 6 then
            begin
                SetLength(head_texts, 6);
                SetLength(head_scores, 6);
            end;
        end;

        function collect_head_phrase_options_local: Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
        begin
            SetLength(head_texts, 0);
            SetLength(head_scores, 0);

            if try_resolve_best_exact_subspan_local(0, 2, resolved_text_local,
                local_score, resolved_segments_local, resolved_path_local,
                resolved_has_anchor_local, resolved_anchor_units_local) and
                (get_candidate_text_unit_count(Trim(resolved_text_local)) = 2) and
                m_dictionary.is_base_entry(head_key, Trim(resolved_text_local)) then
            begin
                if resolved_has_anchor_local then
                begin
                    Inc(local_score, resolved_anchor_units_local * 192);
                end;
                if resolved_segments_local > 1 then
                begin
                    Dec(local_score, (resolved_segments_local - 1) * 96);
                end;
                add_head_option_local(Trim(resolved_text_local), local_score);
            end;

            if m_dictionary.lookup(head_key, local_results) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= 12 then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    local_text := Trim(local_results[local_idx].text);
                    if get_candidate_text_unit_count(local_text) <> 2 then
                    begin
                        Continue;
                    end;

                    local_score := get_candidate_score_local(local_results[local_idx]) +
                        get_preferred_query_phrase_bonus_local(head_key, local_text);
                    add_head_option_local(local_text, local_score);
                end;
            end;
            Result := Length(head_texts) > 0;
        end;

        function has_high_quality_one_plus_two_split_local: Boolean;
        const
            c_prefix_probe_limit = 24;
            c_tail_probe_limit = 24;
            c_min_tail_weight = 520;
        var
            prefix_key_local: string;
            tail_key_local: string;
            prefix_results_local: TncCandidateList;
            tail_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            tail_idx_local: Integer;
            prefix_text_local: string;
            tail_text_local: string;
            tail_weight_local: Integer;
            is_preferred_local: Boolean;

            function is_allowed_prefix_head_text_local(const value: string): Boolean;
            begin
                Result := (value = string(Char($6211))) or

                    (value = string(Char($4E00))) or

                    (value = string(Char($7B2C))) or

                    (value = string(Char($4E0D))) or

                    (value = string(Char($518D))) or

                    (value = string(Char($5728))) or

                    (value = string(Char($9AD8))) or

                    (value = string(Char($5F88))) or

                    (value = string(Char($592A))) or

                    (value = string(Char($6700))) or

                    (value = string(Char($66F4))) or

                    (value = string(Char($53C8))) or

                    (value = string(Char($4E5F))) or

                    (value = string(Char($5DF2))) or

                    (value = string(Char($5C06))) or

                    (value = string(Char($53EF))) or

                    (value = string(Char($4F1A))) or

                    (value = string(Char($80FD))) or

                    (value = string(Char($8981))) or

                    (value = string(Char($60F3))) or

                    (value = string(Char($6CA1))) or

                    (value = string(Char($65E0))) or

                    (value = string(Char($975E))) or

                    (value = string(Char($672A))) or

                    (value = string(Char($4ECD))) or

                    (value = string(Char($90FD))) or

                    (value = string(Char($8FD8))) or

                    (value = string(Char($624D))) or

                    (value = string(Char($5C31))) or

                    (value = string(Char($53EA))) or

                    (value = string(Char($522B))) or

                    (value = string(Char($591A))) or

                    (value = string(Char($5C11))) or

                    (value = string(Char($5168))) or

                    (value = string(Char($8D85))) or

                    (value = string(Char($771F))) or

                    (value = string(Char($633A))) or

                    (value = string(Char($8F83))) or

                    (value = string(Char($9887))) or

                    (value = string(Char($6781))) or

                    (value = string(Char($628A))) or

                    (value = string(Char($88AB))) or

                    (value = string(Char($8BA9))) or

                    (value = string(Char($7ED9))) or

                    (value = string(Char($5411))) or

                    (value = string(Char($5BF9))) or

                    (value = string(Char($4ECE))) or

                    (value = string(Char($4E8E))) or

                    (value = string(Char($548C))) or

                    (value = string(Char($4E0E))) or

                    (value = string(Char($662F))) or

                    (value = string(Char($7684))) or

                    (value = string(Char($4E86)));

            end;
        begin
            Result := False;
            prefix_key_local := build_syllable_query_key_local(0, 1);
            tail_key_local := build_syllable_query_key_local(1, 2);
            if (prefix_key_local = '') or (tail_key_local = '') or
                (not m_dictionary.lookup(prefix_key_local, prefix_results_local)) or
                (not m_dictionary.lookup(tail_key_local, tail_results_local)) then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if prefix_idx_local >= c_prefix_probe_limit then
                begin
                    Break;
                end;
                prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if (Trim(prefix_results_local[prefix_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(prefix_text_local) <> 1) or
                    (not is_allowed_prefix_head_text_local(prefix_text_local)) or
                    (not match_single_char_candidate_for_syllable(prefix_key_local,
                    prefix_text_local, is_preferred_local)) then
                begin
                    Continue;
                end;

                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    if tail_idx_local >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                    tail_weight_local := get_candidate_score_local(
                        tail_results_local[tail_idx_local]);
                    if (Trim(tail_results_local[tail_idx_local].comment) = '') and
                        (get_candidate_text_unit_count(tail_text_local) = 2) and
                        ((tail_results_local[tail_idx_local].source = cs_user) or
                        (tail_weight_local >= c_min_tail_weight)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function is_allowed_two_plus_one_tail_text_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $4E86, $7684, $7740, $8FC7, $5417, $561B, $5427, $5462,
                $554A, $5440, $5566, $5F97, $5730, $4E0A, $4E0B, $91CC,
                $5185, $5916, $524D, $540E, $4E2D, $95F4, $8FB9, $65C1:
                    Result := True;
            end;
        end;

        function is_ordinal_head_text_local(const text_value: string): Boolean;
        var
            units_local: TArray<string>;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <= 0 then
            begin
                Exit;
            end;
            Result := SameText(units_local[0], string(Char($7B2C)));
        end;

        function is_ordinal_tail_text_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $9879, $6761, $4E2A, $70B9, $79CD, $7C7B, $6B21, $6B65,
                $8F6E, $7AE0, $8282, $9875, $6BB5, $884C, $5217, $5C42:
                    Result := True;
            end;
        end;

        function try_get_best_prefix_derived_complete_text_local(
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_idx: Integer;
            prefix_text_local: string;
            prefix_score_local: Integer;
            prefix_popularity_local: Integer;
            syllable_idx: Integer;
            prefix_units_local: TArray<string>;
            prefix_scores_local: TDictionary<string, Integer>;
            aggregated_score_local: Integer;
            existing_prefix_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);
            if not m_dictionary.lookup_full_pinyin_prefix(lookup_text, prefix_results) then
            begin
                Exit;
            end;

            prefix_scores_local := TDictionary<string, Integer>.Create;
            try
                for local_idx := 0 to High(prefix_results) do
                begin
                    if local_idx >= 48 then
                    begin
                        Break;
                    end;
                    if Trim(prefix_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(prefix_results[local_idx]) or
                        is_runtime_common_pattern_candidate(prefix_results[local_idx]) or
                        is_runtime_redup_candidate(prefix_results[local_idx]) then
                    begin
                        Continue;
                    end;

                    prefix_text_local := build_prefix_text_local(prefix_results[local_idx].text, 3);
                    if get_candidate_text_unit_count(prefix_text_local) <> 3 then
                    begin
                        Continue;
                    end;
                    prefix_units_local := split_text_units(prefix_text_local);
                    if Length(prefix_units_local) <> 3 then
                    begin
                        Continue;
                    end;

                    for syllable_idx := 0 to 2 do
                    begin
                        if not match_single_char_candidate_for_syllable(
                            effective_syllables[syllable_idx].text,
                            prefix_units_local[syllable_idx], is_preferred) then
                        begin
                            prefix_text_local := '';
                            Break;
                        end;
                    end;
                    if prefix_text_local = '' then
                    begin
                        Continue;
                    end;

                    prefix_popularity_local := 0;
                    if m_dictionary is TncSqliteDictionary then
                    begin
                        prefix_popularity_local := TncSqliteDictionary(m_dictionary).
                            get_prefix_popularity_hint(prefix_text_local);
                    end;

                    prefix_score_local := 1024 +
                        Min(256, Max(0, get_candidate_score_local(prefix_results[local_idx])) div 8) +
                        get_preferred_query_phrase_bonus_local(lookup_text, prefix_text_local);
                    if prefix_popularity_local > 0 then
                    begin
                        Inc(prefix_score_local, Round(Ln(1.0 + prefix_popularity_local) * 96.0));
                    end;

                    aggregated_score_local := prefix_score_local;
                    if prefix_scores_local.TryGetValue(prefix_text_local,
                        existing_prefix_score_local) then
                    begin
                        Inc(aggregated_score_local, existing_prefix_score_local);
                    end;
                    prefix_scores_local.AddOrSetValue(prefix_text_local, aggregated_score_local);

                    if aggregated_score_local > out_score then
                    begin
                        out_score := aggregated_score_local;
                        out_text := prefix_text_local;
                        Result := True;
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info := m_last_full_path_debug_info +
                                Format(' t321pick=[text=%s pop=%d score=%d agg=%d]',
                                [prefix_text_local, prefix_popularity_local,
                                prefix_score_local, aggregated_score_local]);
                        end;
                    end;
                end;
            finally
                prefix_scores_local.Free;
            end;
        end;
    begin
        ensure_short_three_exact_pair_candidates_visible_local(candidates);
        if Length(short_three_exact_pair_candidates) > 0 then
        begin
            Exit;
        end;

        if (Length(candidates) = 0) or (input_syllable_count <> 3) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        if skip_three_syllable_contextual_completion then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(lookup_text));
        if Length(effective_syllables) <> 3 then
        begin
            Exit;
        end;

        head_key := build_syllable_query_key_local(0, 2);
        tail_key := build_syllable_query_key_local(2, 1);
        if (head_key = '') or (tail_key = '') then
        begin
            Exit;
        end;

        if (m_last_three_syllable_partial_preference_kind <> 2) and
            has_high_quality_one_plus_two_split_local then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' t321=[skip312 key=%s]', [lookup_text]);
            end;
            Exit;
        end;

        if not collect_head_phrase_options_local then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' t321=[nohead key=%s]', [head_key]);
            end;
            Exit;
        end;

        if not m_dictionary.lookup(tail_key, tail_results) then
        begin
            SetLength(tail_results, 0);
        end;

        combined_text := '';
        best_tail_text := '';
        best_path_text := '';
        best_tail_score := Low(Integer);
        best_tail_context_bonus := 0;
        for idx := 0 to High(tail_results) do
        begin
            if idx >= c_tail_probe_limit then
            begin
                Break;
            end;
            if Trim(tail_results[idx].comment) <> '' then
            begin
                Continue;
            end;
            if is_runtime_chain_candidate(tail_results[idx]) or
                is_runtime_common_pattern_candidate(tail_results[idx]) or
                is_runtime_redup_candidate(tail_results[idx]) then
            begin
                Continue;
            end;

            candidate_text := Trim(tail_results[idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <> 1 then
            begin
                Continue;
            end;
            if not match_single_char_candidate_for_syllable(tail_key, candidate_text,
                is_preferred) then
            begin
                Continue;
            end;

            for head_option_idx := 0 to High(head_texts) do
            begin
                context_bonus := m_dictionary.get_context_bonus(
                    head_texts[head_option_idx], candidate_text);
                prefix_popularity := 0;
                candidate_combined_text := head_texts[head_option_idx] + candidate_text;
                if m_dictionary is TncSqliteDictionary then
                begin
                    prefix_popularity := TncSqliteDictionary(m_dictionary).
                        get_prefix_popularity_hint(candidate_combined_text);
                end;
                exact_bonus := get_exact_query_text_bonus_local(candidate_combined_text);
                query_bonus := get_preferred_query_phrase_bonus_local(lookup_text,
                    candidate_combined_text);
                preferred_exact_bonus := get_preferred_exact_subspan_bonus_local(
                    lookup_text, candidate_combined_text);
                ordinal_tail_allowed :=
                    is_ordinal_head_text_local(head_texts[head_option_idx]) and
                    is_ordinal_tail_text_local(candidate_text);
                if (not is_allowed_two_plus_one_tail_text_local(candidate_text)) and
                    (not ordinal_tail_allowed) and
                    (exact_bonus <= 0) and (query_bonus <= 0) and
                    (preferred_exact_bonus <= 0) then
                begin
                    Continue;
                end;
                if (context_bonus <= 0) and (prefix_popularity <= 0) and
                    (exact_bonus <= 0) and (query_bonus <= 0) and
                    (preferred_exact_bonus <= 0) and (not is_preferred) and
                    (not ordinal_tail_allowed) then
                begin
                    Continue;
                end;

                candidate_score := get_candidate_score_local(tail_results[idx]) +
                    (context_bonus * c_context_scale) +
                    Round(Ln(1.0 + prefix_popularity) * 256.0) +
                    query_bonus + exact_bonus + preferred_exact_bonus;
                if is_preferred then
                begin
                    Inc(candidate_score, c_preferred_tail_bonus);
                end;
                if ordinal_tail_allowed then
                begin
                    Inc(candidate_score, 1800);
                end;
                if (context_bonus <= 0) and (prefix_popularity <= 0) and
                    (exact_bonus <= 0) and (query_bonus <= 0) and
                    (preferred_exact_bonus <= 0) and (not ordinal_tail_allowed) then
                begin
                    Dec(candidate_score, 384);
                end;

                candidate_score := head_scores[head_option_idx] +
                    candidate_score + c_complete_promote_bonus;
                if candidate_score > best_tail_score then
                begin
                    best_tail_score := candidate_score;
                    best_tail_text := candidate_combined_text;
                    best_path_text := head_texts[head_option_idx] +
                        c_segment_path_separator + candidate_text;
                    best_tail_context_bonus := context_bonus;
                end;
            end;
        end;

        best_prefix_text := '';
        if try_get_best_prefix_derived_complete_text_local(best_prefix_text, candidate_score) and
            ((best_tail_text = '') or
            (candidate_score > best_tail_score)) then
        begin
            combined_text := best_prefix_text;
            best_tail_text := '';
            best_path_text := '';
            best_tail_score := candidate_score;
            candidate_score := best_tail_score;
        end
        else if best_tail_text <> '' then
        begin
            combined_text := best_tail_text;
            candidate_score := best_tail_score;
        end;

        if combined_text = '' then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' t321=[notail head=%s tail=%s]', [head_key, tail_key]);
            end;
            Exit;
        end;

        if has_competing_exact_full_phrase_local(combined_text) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' t321=[skip exact=%s text=%s]',
                    [lookup_text, combined_text]);
            end;
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), combined_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index = 0 then
        begin
            if candidates[0].score < candidate_score then
            begin
                candidates[0].score := candidate_score;
            end;
            Exit;
        end;

        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
            if picked.score < candidate_score then
            begin
                picked.score := candidate_score;
            end;
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
            Exit;
        end;

        picked.text := combined_text;
        picked.comment := '';
        picked.score := candidate_score;
        picked.source := cs_rule;
        picked.has_dict_weight := True;
        picked.dict_weight := Max(1, Min(candidate_score, 50000));
        if best_path_text <> '' then
        begin
            remember_segment_path_for_candidate(picked.text, '', best_path_text,
                picked.score);
        end;

        SetLength(candidates, Length(candidates) + 1);
        for idx := High(candidates) downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t321=[promote text=%s head=%s tail=%s ctx=%d score=%d]',
                [Trim(picked.text), head_key, best_tail_text, best_tail_context_bonus,
                picked.score]);
        end;
    end;

    procedure ensure_three_syllable_prefix_one_plus_two_complete_candidates_visible(
        var candidates: TncCandidateList);
    const
        c_head_probe_limit = 24;
        c_tail_probe_limit = 24;
        c_max_inserted_candidates = 3;
        c_min_tail_weight = 360;
        c_full_exact_skip_weight = 520;
        c_complete_promote_bonus = 2048;
        c_function_tail_phrase_bonus = 14000;
    type
        TPrefixTailCandidate = record
            candidate: TncCandidate;
            rank_score: Integer;
        end;
    var
        effective_syllables: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        head_results: TncCandidateList;
        tail_results: TncCandidateList;
        built_candidates: TArray<TPrefixTailCandidate>;
        head_idx: Integer;
        tail_idx: Integer;
        insert_idx: Integer;
        idx: Integer;
        move_idx: Integer;
        head_text: string;
        tail_text: string;
        combined_text: string;
        head_weight: Integer;
        tail_weight: Integer;
        context_bonus: Integer;
        query_bonus: Integer;
        combined_score: Integer;
        is_preferred: Boolean;

        function build_syllable_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function get_candidate_weight_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if Result < 0 then
            begin
                Result := 0;
            end;
        end;

        function is_allowed_prefix_head_text_local(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $9887, $628A, $88AB, $8BA9,
                $7ED9, $5411, $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F,
                $7684, $4E86, $7B2C:
                    Result := True;
            end;
            Exit;
        end;

        function is_pronoun_prefix_head_text_local(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $6211, $4F60, $60A8:
                    Result := True;
            end;
        end;

        function is_allowed_one_plus_two_head_text_local(
            const value: string): Boolean;
        begin
            Result := is_allowed_prefix_head_text_local(value) or
                is_pronoun_prefix_head_text_local(value);
        end;

        function get_min_tail_weight_for_head_local(
            const head_value: string): Integer;
        begin
            if is_pronoun_prefix_head_text_local(head_value) then
            begin
                Result := 500;
            end
            else
            begin
                Result := c_min_tail_weight;
            end;
        end;

        function is_number_quantity_tail_text_local(const value: string): Boolean;
        var
            units_local: TArray<string>;
        begin
            units_local := split_text_units(Trim(value));
            if Length(units_local) <> 2 then
            begin
                Exit(False);
            end;

            case Ord(units_local[0][1]) of
                $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                $4E03, $516B, $4E5D, $5341:
                    ;
            else
                Exit(False);
            end;

            case Ord(units_local[1][1]) of
                $5B57, $9879, $6761, $4E2A, $70B9, $79CD, $7C7B,
                $6B21, $6B65, $8F6E, $7AE0, $8282, $9875, $6BB5,
                $884C, $5217, $5C42:
                    Result := True;
            else
                Result := False;
            end;
        end;

        function is_chinese_number_text_unit_local(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                $4E03, $516B, $4E5D, $5341, $767E, $5343, $4E07,
                $4EBF:
                    Result := True;
            end;
        end;

        function get_function_tail_quality_penalty_local(
            const head_value: string; const tail_value: string): Integer;
        var
            units_local: TArray<string>;
        begin
            Result := 0;
            if SameText(head_value, string(Char($7B2C))) then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(tail_value));
            if Length(units_local) <> 2 then
            begin
                Exit;
            end;

            if is_chinese_number_text_unit_local(units_local[0]) and
                is_chinese_number_text_unit_local(units_local[1]) then
            begin
                Inc(Result, 6000);
            end;
            if SameText(units_local[0], units_local[1]) then
            begin
                Inc(Result, 3000);
            end;
        end;

        (*
            Result := (value = string(Char($5982))) or
                (value = string(Char($4E0D))) or
                (value = string(Char($518D))) or
                (value = string(Char($5728))) or
                (value = string(Char($9AD8))) or
                (value = string(Char($5F88))) or
                (value = string(Char($592A))) or
                (value = string(Char($6700))) or

                (value = string(Char($66F4))) or
                (value = string(Char($53C8))) or
                (value = string(Char($4E5F))) or
                (value = string(Char($5DF2))) or
                (value = string(Char($5C06))) or
                (value = string(Char($53EF))) or
                (value = string(Char($4F1A))) or
                (value = string(Char($80FD))) or
                (value = string(Char($8981))) or
                (value = string(Char($60F3))) or
                (value = string(Char($6CA1))) or
                (value = string(Char($65E0))) or
                (value = string(Char($975E))) or
                (value = string(Char($672A))) or
                (value = string(Char($4ECD))) or
                (value = string(Char($90FD))) or
                (value = string(Char($8FD8))) or
                (value = string(Char($624D))) or
                (value = string(Char($5C31))) or
                (value = string(Char($53EA))) or
                (value = string(Char($522B))) or
                (value = string(Char($591A))) or
                (value = string(Char($5C11))) or
                (value = string(Char($65B0))) or
                (value = string(Char($8001))) or
                (value = string(Char($5C0F))) or
                (value = string(Char($5927))) or
                (value = string(Char($5168))) or
                (value = string(Char($8D85))) or
                (value = string(Char($771F))) or
                (value = string(Char($597D))) or
                (value = string(Char($633A))) or
                (value = string(Char($8F83))) or
                (value = string(Char($9887))) or
                (value = string(Char($6781))) or
                (value = string(Char($628A))) or
                (value = string(Char($88AB))) or
                (value = string(Char($8BA9))) or
                (value = string(Char($7ED9))) or
                (value = string(Char($5411))) or
                (value = string(Char($5BF9))) or
                (value = string(Char($4ECE))) or
                (value = string(Char($4E8E))) or
                (value = string(Char($548C))) or
                (value = string(Char($4E0E)));
        end;

        *)
        function has_reliable_full_exact_candidate_local: Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_weight_local: Integer;
        begin
            Result := False;
            if not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results_local) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if get_candidate_text_unit_count(exact_text_local) <> 3 then
                begin
                    Continue;
                end;
                if exact_results_local[exact_idx_local].source = cs_user then
                begin
                    Exit(True);
                end;
                exact_weight_local :=
                    get_candidate_weight_local(exact_results_local[exact_idx_local]);
                if exact_weight_local >= c_full_exact_skip_weight then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_existing_complete_text_local(const text_value: string): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text), text_value) then
                begin
                    Exit(True);
                end;
            end;
            for local_idx := 0 to High(built_candidates) do
            begin
                if SameText(Trim(built_candidates[local_idx].candidate.text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_high_quality_tail_candidate_local(
            const candidate: TncCandidate; const text_value: string;
            const weight_value: Integer; const head_value: string): Boolean;
        begin
            Result := (Trim(candidate.comment) = '') and
                (get_candidate_text_unit_count(text_value) = 2) and
                ((candidate.source = cs_user) or candidate.has_dict_weight) and
                ((candidate.source = cs_user) or
                (weight_value >= get_min_tail_weight_for_head_local(
                head_value)));
        end;

        procedure add_built_candidate_local(const text_value: string;
            const score_value: Integer; const head_value: string;
            const tail_value: string);
        var
            local_idx: Integer;
            move_idx: Integer;
            local_candidate: TPrefixTailCandidate;
        begin
            if text_value = '' then
            begin
                Exit;
            end;

            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text), text_value) then
                begin
                    if candidates[local_idx].score < score_value then
                    begin
                        candidates[local_idx].score := score_value;
                        candidates[local_idx].has_dict_weight := True;
                        candidates[local_idx].dict_weight := Max(
                            candidates[local_idx].dict_weight,
                            Min(score_value, 50000));
                        remember_segment_path_for_candidate(text_value, '',
                            head_value + c_segment_path_separator + tail_value,
                            score_value);
                    end;
                    move_idx := local_idx;
                    while (move_idx > 0) and
                        (candidates[move_idx].score >
                        candidates[move_idx - 1].score) do
                    begin
                        local_candidate.candidate := candidates[move_idx - 1];
                        candidates[move_idx - 1] := candidates[move_idx];
                        candidates[move_idx] := local_candidate.candidate;
                        Dec(move_idx);
                    end;
                    Exit;
                end;
            end;

            for local_idx := 0 to High(built_candidates) do
            begin
                if SameText(Trim(built_candidates[local_idx].candidate.text),
                    text_value) then
                begin
                    if built_candidates[local_idx].rank_score < score_value then
                    begin
                        built_candidates[local_idx].candidate.score := score_value;
                        built_candidates[local_idx].candidate.has_dict_weight := True;
                        built_candidates[local_idx].candidate.dict_weight := Max(1,
                            Min(score_value, 50000));
                        built_candidates[local_idx].rank_score := score_value;
                        remember_segment_path_for_candidate(text_value, '',
                            head_value + c_segment_path_separator + tail_value,
                            score_value);
                    end;
                    Exit;
                end;
            end;

            FillChar(local_candidate, SizeOf(local_candidate), 0);
            local_candidate.candidate.text := text_value;
            local_candidate.candidate.comment := '';
            local_candidate.candidate.score := score_value;
            local_candidate.candidate.source := cs_rule;
            local_candidate.candidate.has_dict_weight := True;
            local_candidate.candidate.dict_weight := Max(1, Min(score_value, 50000));
            local_candidate.rank_score := score_value;

            local_idx := Length(built_candidates);
            SetLength(built_candidates, local_idx + 1);
            built_candidates[local_idx] := local_candidate;
            while (local_idx > 0) and
                (built_candidates[local_idx].rank_score >
                built_candidates[local_idx - 1].rank_score) do
            begin
                local_candidate := built_candidates[local_idx - 1];
                built_candidates[local_idx - 1] := built_candidates[local_idx];
                built_candidates[local_idx] := local_candidate;
                Dec(local_idx);
            end;

            if Length(built_candidates) > c_max_inserted_candidates then
            begin
                SetLength(built_candidates, c_max_inserted_candidates);
            end;

            for move_idx := 0 to High(built_candidates) do
            begin
                if SameText(Trim(built_candidates[move_idx].candidate.text),
                    text_value) then
                begin
                    remember_segment_path_for_candidate(text_value, '',
                        head_value + c_segment_path_separator + tail_value,
                        score_value);
                    Break;
                end;
            end;
        end;

        procedure promote_existing_one_plus_two_split_candidates_local;
        const
            c_existing_split_bonus = 6000;
        var
            local_idx: Integer;
            head_lookup_idx: Integer;
            tail_lookup_idx: Integer;
            move_idx_local: Integer;
            current_text_local: string;
            units_local: TArray<string>;
            head_text_local: string;
            tail_text_local: string;
            head_weight_local: Integer;
            tail_weight_local: Integer;
            current_weight_local: Integer;
            split_score_local: Integer;
            matched_head_local: Boolean;
            matched_tail_local: Boolean;
            preferred_head_local: Boolean;
            temp_candidate_local: TncCandidate;
        begin
            for local_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                current_text_local := Trim(candidates[local_idx].text);
                if get_candidate_text_unit_count(current_text_local) <> 3 then
                begin
                    Continue;
                end;

                units_local := split_text_units(current_text_local);
                if Length(units_local) <> 3 then
                begin
                    Continue;
                end;
                head_text_local := units_local[0];
                tail_text_local := units_local[1] + units_local[2];
                if not is_allowed_one_plus_two_head_text_local(
                    head_text_local) then
                begin
                    Continue;
                end;

                matched_head_local := False;
                head_weight_local := 0;
                for head_lookup_idx := 0 to High(head_results) do
                begin
                    if head_lookup_idx >= c_head_probe_limit then
                    begin
                        Break;
                    end;
                    if (Trim(head_results[head_lookup_idx].comment) <> '') or
                        (not SameText(Trim(head_results[head_lookup_idx].text),
                        head_text_local)) or
                        (not match_single_char_candidate_for_syllable(head_key,
                        head_text_local, preferred_head_local)) then
                    begin
                        Continue;
                    end;
                    matched_head_local := True;
                    head_weight_local :=
                        get_candidate_weight_local(head_results[head_lookup_idx]);
                    if preferred_head_local then
                    begin
                        Inc(head_weight_local, 96);
                    end;
                    Break;
                end;
                if not matched_head_local then
                begin
                    Continue;
                end;

                matched_tail_local := False;
                tail_weight_local := 0;
                for tail_lookup_idx := 0 to High(tail_results) do
                begin
                    if tail_lookup_idx >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    if not SameText(Trim(tail_results[tail_lookup_idx].text),
                        tail_text_local) then
                    begin
                        Continue;
                    end;
                    tail_weight_local :=
                        get_candidate_weight_local(tail_results[tail_lookup_idx]);
                    if is_high_quality_tail_candidate_local(
                        tail_results[tail_lookup_idx], tail_text_local,
                        tail_weight_local, head_text_local) then
                    begin
                        matched_tail_local := True;
                    end;
                    Break;
                end;
                if not matched_tail_local then
                begin
                    Continue;
                end;

                current_weight_local := get_candidate_weight_local(
                    candidates[local_idx]);
                split_score_local := Max(current_weight_local +
                    c_existing_split_bonus, (tail_weight_local * 3) +
                    head_weight_local + c_complete_promote_bonus +
                    c_function_tail_phrase_bonus);
                if candidates[local_idx].score < split_score_local then
                begin
                    candidates[local_idx].score := split_score_local;
                    candidates[local_idx].has_dict_weight := True;
                    candidates[local_idx].dict_weight := Max(
                        candidates[local_idx].dict_weight,
                        Min(split_score_local, 50000));
                    remember_segment_path_for_candidate(current_text_local, '',
                        head_text_local + c_segment_path_separator +
                        tail_text_local, split_score_local);
                end;

                move_idx_local := local_idx;
                while (move_idx_local > 0) and
                    (candidates[move_idx_local].score >
                    candidates[move_idx_local - 1].score) do
                begin
                    temp_candidate_local := candidates[move_idx_local - 1];
                    candidates[move_idx_local - 1] := candidates[move_idx_local];
                    candidates[move_idx_local] := temp_candidate_local;
                    Dec(move_idx_local);
                end;
            end;
        end;
    begin
        // Generic A/BC completion is allowed only when both pieces are exact
        // entries and the trained language model supplies clear pair evidence.
        ensure_short_three_exact_pair_candidates_visible_local(candidates);
        Exit;

        if (Length(candidates) = 0) or (input_syllable_count <> 3) or
            (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(lookup_text));
        if Length(effective_syllables) <> 3 then
        begin
            Exit;
        end;

        head_key := build_syllable_query_key_local(0, 1);
        tail_key := build_syllable_query_key_local(1, 2);
        if (head_key = '') or (tail_key = '') then
        begin
            Exit;
        end;
        if SameText(head_key, 'yi') then
        begin
            // "yi" is too ambiguous as a one-character A/BC head.  Let exact
            // phrases and normal prefix paths handle it instead of probing
            // arbitrary "yi + two-char word" combinations on every keystroke.
            Exit;
        end;

        if (not lookup_exact_full_pinyin_cached_local(head_key, head_results)) or
            (not lookup_exact_full_pinyin_cached_local(tail_key, tail_results)) then
        begin
            Exit;
        end;

        SetLength(built_candidates, 0);
        promote_existing_one_plus_two_split_candidates_local;
        for head_idx := 0 to High(head_results) do
        begin
            if head_idx >= c_head_probe_limit then
            begin
                Break;
            end;
            if Trim(head_results[head_idx].comment) <> '' then
            begin
                Continue;
            end;
            head_text := Trim(head_results[head_idx].text);
            if (get_candidate_text_unit_count(head_text) <> 1) or
                (not is_allowed_one_plus_two_head_text_local(head_text)) or
                (not match_single_char_candidate_for_syllable(head_key, head_text,
                is_preferred)) then
            begin
                Continue;
            end;

            head_weight := get_candidate_weight_local(head_results[head_idx]);
            if is_preferred then
            begin
                Inc(head_weight, 96);
            end;

            for tail_idx := 0 to High(tail_results) do
            begin
                if tail_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                tail_text := Trim(tail_results[tail_idx].text);
                tail_weight := get_candidate_weight_local(tail_results[tail_idx]);
                if not is_high_quality_tail_candidate_local(
                    tail_results[tail_idx], tail_text, tail_weight,
                    head_text) then
                begin
                    Continue;
                end;

                combined_text := head_text + tail_text;
                context_bonus := m_dictionary.get_context_bonus(head_text, tail_text);
                query_bonus := get_preferred_query_phrase_bonus_local(lookup_text,
                    combined_text);
                combined_score := (tail_weight * 3) + head_weight +
                    (context_bonus * 4) + query_bonus + c_complete_promote_bonus +
                    c_function_tail_phrase_bonus;
                Dec(combined_score, get_function_tail_quality_penalty_local(
                    head_text, tail_text));
                if SameText(head_text, string(Char($7B2C))) and
                    is_number_quantity_tail_text_local(tail_text) then
                begin
                    Inc(combined_score, 20000);
                end;
                add_built_candidate_local(combined_text, combined_score,
                    head_text, tail_text);
            end;
        end;

        if Length(built_candidates) = 0 then
        begin
            Exit;
        end;

        for insert_idx := High(built_candidates) downto 0 do
        begin
            SetLength(candidates, Length(candidates) + 1);
            idx := 0;
            while (idx < High(candidates)) and
                (candidates[idx].score >=
                built_candidates[insert_idx].candidate.score) do
            begin
                Inc(idx);
            end;
            for move_idx := High(candidates) downto idx + 1 do
            begin
                candidates[move_idx] := candidates[move_idx - 1];
            end;
            candidates[idx] := built_candidates[insert_idx].candidate;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' t312=[n=%d top=%s head=%s tail=%s]',
                [Length(built_candidates), Trim(candidates[0].text),
                head_key, tail_key]);
        end;
    end;

    procedure ensure_best_visible_partial_completion_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_visible_probe_limit = 12;
        c_completion_probe_limit = 6;
        c_completion_promote_bonus = 2048;
        c_completion_tail_max_syllables = 4;
    var
        completion_states: TDictionary<string, Integer>;
        completion_texts: TDictionary<string, string>;
        completion_scores: TDictionary<string, Integer>;
        best_index: Integer;
        existing_index: Integer;
        idx: Integer;
        picked: TncCandidate;

        function get_fixed_boundary_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'jiang' then
            begin
                Result := string(Char($5C06));
            end
            else if normalized_syllable = 'rang' then
            begin
                Result := string(Char($8BA9));
            end
            else if normalized_syllable = 'dui' then
            begin
                Result := string(Char($5BF9));
            end
            else if normalized_syllable = 'dao' then
            begin
                Result := string(Char($5230));
            end
            else if normalized_syllable = 'cong' then
            begin
                Result := string(Char($4ECE));
            end
            else if normalized_syllable = 'you' then
            begin
                Result := string(Char($6709));
            end
            else if normalized_syllable = 'deng' then
            begin
                Result := string(Char($7B49));
            end
            else if normalized_syllable = 'hai' then
            begin
                Result := string(Char($8FD8));
            end
            else if normalized_syllable = 'mai' then
            begin
                Result := string(Char($4E70));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable = 'ji' then
            begin
                Result := string(Char($65E2));
            end
            else if normalized_syllable = 'ban' then
            begin
                Result := string(Char($822C));
            end;
        end;

        function is_boundary_tail_comment_local(const tail_query_key: string): Boolean;
        var
            tail_syllables: TncPinyinParseResult;
            first_tail: string;
        begin
            Result := False;
            if tail_query_key = '' then
            begin
                Exit;
            end;

            tail_syllables := get_effective_compact_pinyin_syllables(
                tail_query_key);
            if Length(tail_syllables) = 0 then
            begin
                Exit;
            end;

            first_tail := normalize_pinyin_text(tail_syllables[0].text);
            Result := (first_tail = 'de') or (first_tail = 'le') or
                (first_tail = 'liao') or
                (first_tail = 'ma') or (first_tail = 'ba') or
                (first_tail = 'ne') or (first_tail = 'a') or
                (first_tail = 'ya') or (first_tail = 'la') or
                (first_tail = 'wa');
        end;

        function is_valid_cjk_phrase_text_local(const value: string;
            const expected_units: Integer): Boolean;
        var
            units: TArray<string>;
            unit_idx: Integer;
            codepoint: Integer;

            function try_get_codepoint_local(const unit_text: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if unit_text = '' then
                begin
                    Exit;
                end;

                if Length(unit_text) = 1 then
                begin
                    out_codepoint := Ord(unit_text[1]);
                    Exit(True);
                end;

                if (Length(unit_text) = 2) and
                    (Ord(unit_text[1]) >= $D800) and
                    (Ord(unit_text[1]) <= $DBFF) and
                    (Ord(unit_text[2]) >= $DC00) and
                    (Ord(unit_text[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                        (Ord(unit_text[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if Trim(value) = '' then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(units) do
            begin
                if (not try_get_codepoint_local(units[unit_idx], codepoint)) or
                    (codepoint < $4E00) or (codepoint > $9FFF) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function try_find_query_key_span_local(const query_key: string;
            const expected_syllables: Integer; const prefer_suffix: Boolean;
            out out_start_idx: Integer): Boolean;
        var
            query_syllables_local: TncPinyinParseResult;
            lookup_syllables_local: TncPinyinParseResult;
            start_idx_local: Integer;

            function matches_at_local(const candidate_start_idx: Integer): Boolean;
            var
                local_unit_idx: Integer;
            begin
                Result := False;
                if (candidate_start_idx < 0) or
                    (candidate_start_idx + expected_syllables - 1 >
                    High(lookup_syllables_local)) then
                begin
                    Exit;
                end;

                for local_unit_idx := 0 to expected_syllables - 1 do
                begin
                    if not SameText(normalize_pinyin_text(
                        lookup_syllables_local[candidate_start_idx + local_unit_idx].text),
                        normalize_pinyin_text(query_syllables_local[local_unit_idx].text)) then
                    begin
                        Exit(False);
                    end;
                end;

                Result := True;
            end;
        begin
            Result := False;
            out_start_idx := -1;
            if (query_key = '') or (expected_syllables <= 0) or
                (not is_full_pinyin_key(query_key)) or
                (not is_full_pinyin_key(lookup_text)) or
                (input_syllable_count < expected_syllables) then
            begin
                Exit;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(query_key);
            if Length(query_syllables_local) <> expected_syllables then
            begin
                Exit;
            end;

            lookup_syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(lookup_syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;

            if prefer_suffix then
            begin
                start_idx_local := input_syllable_count - expected_syllables;
                if matches_at_local(start_idx_local) then
                begin
                    out_start_idx := start_idx_local;
                    Exit(True);
                end;
            end;

            for start_idx_local := 0 to input_syllable_count - expected_syllables do
            begin
                if prefer_suffix and
                    (start_idx_local = input_syllable_count - expected_syllables) then
                begin
                    Continue;
                end;
                if matches_at_local(start_idx_local) then
                begin
                    out_start_idx := start_idx_local;
                    Exit(True);
                end;
            end;
        end;

        function try_get_best_completion_local(const query_key: string;
            const expected_syllables: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            cached_state: Integer;
            local_results: TncCandidateList;
            snapshot_candidates: TncCandidateList;
            snapshot_scores: TArray<Integer>;
            local_idx: Integer;
            candidate: TncCandidate;
            candidate_text: string;
            candidate_score: Integer;
            candidate_units: Integer;
            preferred_text: string;
            best_complete_text: string;
            best_complete_score: Integer;
            best_partial_text: string;
            best_partial_score: Integer;
            best_partial_comment: string;
            best_split_text: string;
            best_split_score: Integer;
            comment_key: string;
            comment_syllables: TncPinyinParseResult;
            tail_text: string;
            tail_score: Integer;
            combined_text: string;
            combined_score: Integer;
            context_bonus: Integer;
            query_syllables: TncPinyinParseResult;
            split_idx: Integer;
            left_key: string;
            right_key: string;
            left_text: string;
            left_score: Integer;
            right_text: string;
            right_score: Integer;
            aligned_start_idx: Integer;
            aligned_segments: Integer;
            aligned_path: string;
            aligned_has_anchor: Boolean;
            aligned_anchor_units: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (query_key = '') or (expected_syllables <= 0) or
                (not is_full_pinyin_key(query_key)) then
            begin
                Exit;
            end;

            if completion_states.TryGetValue(query_key, cached_state) then
            begin
                if cached_state = 1 then
                begin
                    completion_texts.TryGetValue(query_key, out_text);
                    completion_scores.TryGetValue(query_key, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            if expected_syllables = 1 then
            begin
                preferred_text := get_fixed_boundary_single_char_local(query_key);
                if (m_dictionary <> nil) and
                    m_dictionary.lookup_exact_full_pinyin(query_key, local_results) then
                begin
                    for local_idx := 0 to High(local_results) do
                    begin
                        if local_idx >= c_completion_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(local_results[local_idx].comment) <> '' then
                        begin
                            Continue;
                        end;
                        candidate_text := Trim(local_results[local_idx].text);
                        if (get_candidate_text_unit_count(candidate_text) <> 1) or
                            (not is_valid_cjk_phrase_text_local(candidate_text, 1)) then
                        begin
                            Continue;
                        end;

                        candidate_score := local_results[local_idx].score;
                        if local_results[local_idx].has_dict_weight and
                            (local_results[local_idx].dict_weight > candidate_score) then
                        begin
                            candidate_score := local_results[local_idx].dict_weight;
                        end;
                        Inc(candidate_score, get_preferred_query_phrase_bonus_local(
                            query_key, candidate_text));
                        if (preferred_text <> '') and
                            SameText(candidate_text, preferred_text) then
                        begin
                            Inc(candidate_score, 256);
                        end;

                        if (not Result) or (candidate_score > out_score) then
                        begin
                            Result := True;
                            out_text := candidate_text;
                            out_score := candidate_score;
                        end;
                    end;
                end;

                if (not Result) and (preferred_text <> '') then
                begin
                    out_text := preferred_text;
                    out_score := 1500;
                    Result := True;
                end;

                if Result then
                begin
                    completion_states.AddOrSetValue(query_key, 1);
                    completion_texts.AddOrSetValue(query_key, out_text);
                    completion_scores.AddOrSetValue(query_key, out_score);
                end
                else
                begin
                    completion_states.AddOrSetValue(query_key, 2);
                end;
                Exit(Result);
            end;

            if try_find_query_key_span_local(query_key, expected_syllables, True,
                aligned_start_idx) and
                try_resolve_best_exact_subspan_local(aligned_start_idx,
                expected_syllables, out_text, out_score, aligned_segments,
                aligned_path, aligned_has_anchor, aligned_anchor_units) then
            begin
                completion_states.AddOrSetValue(query_key, 1);
                completion_texts.AddOrSetValue(query_key, out_text);
                completion_scores.AddOrSetValue(query_key, out_score);
                Exit(True);
            end;

            completion_states.AddOrSetValue(query_key, 3);

            SetLength(snapshot_candidates, 0);
            SetLength(snapshot_scores, 0);

            best_complete_text := '';
            best_complete_score := Low(Integer);
            best_partial_text := '';
            best_partial_score := Low(Integer);
            best_partial_comment := '';
            best_split_text := '';
            best_split_score := Low(Integer);

            for local_idx := 0 to High(snapshot_candidates) do
            begin
                if local_idx >= c_completion_probe_limit then
                begin
                    Break;
                end;

                candidate := snapshot_candidates[local_idx];
                candidate_text := Trim(candidate.text);
                if candidate_text = '' then
                begin
                    Continue;
                end;

                candidate_score := snapshot_scores[local_idx] +
                    get_preferred_query_phrase_bonus_local(query_key, candidate_text);
                candidate_units := get_candidate_text_unit_count(candidate_text);

                if Trim(candidate.comment) = '' then
                begin
                    if (candidate_units = expected_syllables) and
                        is_valid_cjk_phrase_text_local(candidate_text,
                        expected_syllables) and
                        (candidate_score > best_complete_score) then
                    begin
                        best_complete_text := candidate_text;
                        best_complete_score := candidate_score;
                    end;
                    Continue;
                end;

                comment_key := normalize_pinyin_text(candidate.comment);
                if (comment_key = '') or (not is_full_pinyin_key(comment_key)) then
                begin
                    Continue;
                end;

                comment_syllables := get_effective_compact_pinyin_syllables(comment_key);
                if (Length(comment_syllables) = 0) or
                    (candidate_units + Length(comment_syllables) <> expected_syllables) then
                begin
                    Continue;
                end;

                if not try_get_best_completion_local(comment_key,
                    Length(comment_syllables), tail_text, tail_score) then
                begin
                    Continue;
                end;

                combined_text := candidate_text + tail_text;
                if not is_valid_cjk_phrase_text_local(combined_text, expected_syllables) then
                begin
                    Continue;
                end;

                combined_score := candidate_score + tail_score;
                if m_dictionary <> nil then
                begin
                    context_bonus := m_dictionary.get_context_bonus(candidate_text,
                        tail_text);
                    Inc(combined_score, context_bonus);
                end;
                if is_boundary_tail_comment_local(comment_key) then
                begin
                    Inc(combined_score, 1024);
                end;

                if combined_score > best_partial_score then
                begin
                    best_partial_text := combined_text;
                    best_partial_score := combined_score;
                    best_partial_comment := comment_key;
                end;
            end;

            query_syllables := get_effective_compact_pinyin_syllables(query_key);
            if Length(query_syllables) = expected_syllables then
            begin
                for split_idx := 1 to expected_syllables - 1 do
                begin
                    left_key := '';
                    for local_idx := 0 to split_idx - 1 do
                    begin
                        left_key := left_key +
                            normalize_pinyin_text(query_syllables[local_idx].text);
                    end;

                    right_key := '';
                    for local_idx := split_idx to High(query_syllables) do
                    begin
                        right_key := right_key +
                            normalize_pinyin_text(query_syllables[local_idx].text);
                    end;

                    if (left_key = '') or (right_key = '') then
                    begin
                        Continue;
                    end;

                    if not try_get_best_completion_local(left_key, split_idx,
                        left_text, left_score) then
                    begin
                        Continue;
                    end;
                    if not try_get_best_completion_local(right_key,
                        expected_syllables - split_idx, right_text,
                        right_score) then
                    begin
                        Continue;
                    end;

                    combined_text := left_text + right_text;
                    if not is_valid_cjk_phrase_text_local(combined_text, expected_syllables) then
                    begin
                        Continue;
                    end;

                    combined_score := left_score + right_score - 96;
                    if m_dictionary <> nil then
                    begin
                        context_bonus := m_dictionary.get_context_bonus(left_text,
                            right_text);
                        Inc(combined_score, context_bonus);
                    end;

                    if combined_score > best_split_score then
                    begin
                        best_split_text := combined_text;
                        best_split_score := combined_score;
                    end;
                end;
            end;

            if best_complete_text <> '' then
            begin
                out_text := best_complete_text;
                out_score := best_complete_score;
                if (best_partial_text <> '') and
                    ((is_boundary_tail_comment_local(best_partial_comment) and
                    (best_partial_score > best_complete_score - 256)) or
                    (best_partial_score > best_complete_score + 512)) then
                begin
                    out_text := best_partial_text;
                    out_score := best_partial_score;
                end;
            end;

            if (best_split_text <> '') and
                ((out_text = '') or (best_split_score > out_score - 64)) then
            begin
                out_text := best_split_text;
                out_score := best_split_score;
            end;

            if (out_text = '') and (best_partial_text <> '') then
            begin
                out_text := best_partial_text;
                out_score := best_partial_score;
            end;

            if out_text <> '' then
            begin
                completion_states.AddOrSetValue(query_key, 1);
                completion_texts.AddOrSetValue(query_key, out_text);
                completion_scores.AddOrSetValue(query_key, out_score);
                Exit(True);
            end;

            completion_states.AddOrSetValue(query_key, 2);
        end;

        function try_get_best_completed_visible_candidate_local(
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_idx: Integer;
            candidate: TncCandidate;
            prefix_text: string;
            prefix_units: Integer;
            comment_key: string;
            comment_syllables: TncPinyinParseResult;
            tail_text: string;
            tail_score: Integer;
            combined_text: string;
            combined_score: Integer;
            context_bonus: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;

            for local_idx := 0 to High(candidates) do
            begin
                if local_idx >= c_visible_probe_limit then
                begin
                    Break;
                end;

                candidate := candidates[local_idx];
                if Trim(candidate.comment) = '' then
                begin
                    Continue;
                end;

                prefix_text := Trim(candidate.text);
                prefix_units := get_candidate_text_unit_count(prefix_text);
                if prefix_units <= 0 then
                begin
                    Continue;
                end;

                comment_key := normalize_pinyin_text(candidate.comment);
                if (comment_key = '') or (not is_full_pinyin_key(comment_key)) then
                begin
                    Continue;
                end;

                comment_syllables := get_effective_compact_pinyin_syllables(
                    comment_key);
                if (Length(comment_syllables) = 0) or
                    (Length(comment_syllables) > c_completion_tail_max_syllables) or
                    (prefix_units + Length(comment_syllables) <> input_syllable_count) then
                begin
                    Continue;
                end;

                if not try_get_best_completion_local(comment_key,
                    Length(comment_syllables), tail_text, tail_score) then
                begin
                    Continue;
                end;

                combined_text := prefix_text + tail_text;
                if not is_valid_cjk_phrase_text_local(combined_text, input_syllable_count) then
                begin
                    Continue;
                end;

                combined_score := get_rank_score(candidate) + tail_score;
                if m_dictionary <> nil then
                begin
                    context_bonus := m_dictionary.get_context_bonus(prefix_text,
                        tail_text);
                    Inc(combined_score, context_bonus);
                end;
                if is_boundary_tail_comment_local(comment_key) then
                begin
                    Inc(combined_score, 1024);
                end;

                if (not Result) or (combined_score > out_score) or
                    ((combined_score = out_score) and
                    (CompareText(combined_text, out_text) < 0)) then
                begin
                    Result := True;
                    out_text := combined_text;
                    out_score := combined_score;
                    best_index := local_idx;
                end;
            end;
        end;
    var
        combined_text: string;
        combined_score: Integer;
        top_rank: Integer;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 6) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        if (input_syllable_count <= 10) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            Exit;
        end;

        completion_states := TDictionary<string, Integer>.Create;
        completion_texts := TDictionary<string, string>.Create;
        completion_scores := TDictionary<string, Integer>.Create;
        best_index := -1;
        try
            if not try_get_best_completed_visible_candidate_local(combined_text,
                combined_score) then
            begin
                Exit;
            end;

            existing_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), combined_text) then
                begin
                    existing_index := idx;
                    Break;
                end;
            end;

            if existing_index = 0 then
            begin
                Exit;
            end;

            top_rank := get_rank_score(candidates[0]);
            if existing_index > 0 then
            begin
                if (best_index >= 0) and
                    (best_index < Length(candidates)) and
                    (get_rank_score(candidates[best_index]) + c_completion_promote_bonus <
                    top_rank) and
                    (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                    input_syllable_count) then
                begin
                    Exit;
                end;

                picked := candidates[existing_index];
                for idx := existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
                Exit;
            end;

            if (best_index >= 0) and (best_index < Length(candidates)) and
                (get_rank_score(candidates[best_index]) + c_completion_promote_bonus <
                top_rank) and
                (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count) then
            begin
                Exit;
            end;

            FillChar(picked, SizeOf(picked), 0);
            picked.text := combined_text;
            picked.comment := '';
            picked.score := Max(top_rank + c_completion_promote_bonus, combined_score);
            picked.source := cs_rule;
            picked.has_dict_weight := False;
            picked.dict_weight := 0;

            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        finally
            completion_scores.Free;
            completion_texts.Free;
            completion_states.Free;
        end;
    end;

    procedure ensure_best_visible_complete_sentence_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_visible_probe_limit = 8;
        c_exact_probe_limit = 8;
        c_promote_margin = 512;
        c_rank_score_cap = 8192;
    var
        effective_syllables: TncPinyinParseResult;
        query_key_cache: TArray<TArray<string>>;
        loaded_queries: TDictionary<string, Byte>;
        exact_rank_cache: TDictionary<string, Integer>;
        exact_top_text_cache: TDictionary<string, string>;
        best_index: Integer;
        best_score: Integer;
        top_score: Integer;
        idx: Integer;
        candidate_score: Integer;
        picked: TncCandidate;
        query_start_idx: Integer;
        query_span_idx: Integer;
        accumulated_query_key: string;

        procedure ensure_query_loaded_local(const query_key: string;
            const expected_units: Integer; const start_idx: Integer);
        var
            local_idx: Integer;
            cache_key: string;
            candidate_text: string;
            exact_results: TncCandidateList;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
            has_resolved_local: Boolean;
            raw_rank_local: Integer;
        begin
            if (query_key = '') or loaded_queries.ContainsKey(query_key) then
            begin
                Exit;
            end;

            loaded_queries.Add(query_key, 1);
            has_resolved_local := False;
            if (start_idx >= 0) and (expected_units >= 3) and
                (expected_units <= 5) and
                try_resolve_best_exact_subspan_local(start_idx, expected_units,
                resolved_text_local, resolved_score_local,
                resolved_segments_local, resolved_path_local,
                resolved_has_anchor_local, resolved_anchor_units_local) and
                (resolved_text_local <> '') then
            begin
                has_resolved_local := True;
                exact_top_text_cache.AddOrSetValue(query_key,
                    resolved_text_local);
                cache_key := query_key + '|' + resolved_text_local;
                exact_rank_cache.AddOrSetValue(cache_key, 0);
            end;

            if exact_top_text_cache.ContainsKey(query_key) then
            begin
                Exit;
            end;

            if (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(query_key,
                exact_results)) then
            begin
                Exit;
            end;

            raw_rank_local := 0;
            for local_idx := 0 to High(exact_results) do
            begin
                if local_idx >= c_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[local_idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;
                if has_resolved_local and
                    SameText(candidate_text, resolved_text_local) then
                begin
                    Continue;
                end;
                if not exact_top_text_cache.ContainsKey(query_key) then
                begin
                    exact_top_text_cache.Add(query_key, candidate_text);
                end;

                cache_key := query_key + '|' + candidate_text;
                if not exact_rank_cache.ContainsKey(cache_key) then
                begin
                    if has_resolved_local then
                    begin
                        exact_rank_cache.Add(cache_key, raw_rank_local + 1);
                    end
                    else
                    begin
                        exact_rank_cache.Add(cache_key, raw_rank_local);
                    end;
                end;
                Inc(raw_rank_local);
            end;
        end;

        function get_exact_support_bonus_local(const query_key: string;
            const candidate_text: string; const start_idx: Integer): Integer;
        var
            cache_key: string;
            rank_value: Integer;
        begin
            Result := 0;
            if (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            Inc(Result, get_preferred_query_phrase_bonus_local(query_key,
                candidate_text));
            Inc(Result, get_preferred_exact_subspan_bonus_local(query_key,
                candidate_text));

            ensure_query_loaded_local(query_key,
                get_candidate_text_unit_count(candidate_text), start_idx);
            cache_key := query_key + '|' + candidate_text;
            if not exact_rank_cache.TryGetValue(cache_key, rank_value) then
            begin
                Exit;
            end;

            case rank_value of
                0:
                    Inc(Result, 1100);
                1:
                    Inc(Result, 800);
                2, 3:
                    Inc(Result, 480);
                4, 5:
                    Inc(Result, 240);
            else
                Inc(Result, 120);
            end;
        end;

        function get_exact_mismatch_penalty_local(const query_key: string;
            const candidate_text: string; const span_units: Integer;
            const start_idx: Integer): Integer;
        var
            cache_key: string;
            rank_value: Integer;
            top_text: string;
        begin
            Result := 0;
            if (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            ensure_query_loaded_local(query_key, span_units, start_idx);
            cache_key := query_key + '|' + candidate_text;
            if exact_rank_cache.TryGetValue(cache_key, rank_value) then
            begin
                case rank_value of
                    0:
                        Exit(0);
                    1:
                        Exit(-96);
                    2, 3:
                        Exit(-192);
                    4, 5:
                        Exit(-320);
                else
                    Exit(-480);
                end;
            end;

            if not exact_top_text_cache.TryGetValue(query_key, top_text) or
                (top_text = '') or SameText(top_text, candidate_text) then
            begin
                Exit;
            end;

            case span_units of
                2:
                    Result := -1400;
                3:
                    Result := -1100;
                4, 5:
                    Result := -900;
            else
                Result := -600;
            end;
        end;

        function get_visible_complete_candidate_score_local(
            const candidate: TncCandidate): Integer;
        var
            candidate_text: string;
            units: TArray<string>;
            start_idx: Integer;
            span_units: Integer;
            query_key: string;
            chunk_text: string;
            problematic_penalty: Integer;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate.text);
            if candidate_text = '' then
            begin
                Exit;
            end;

            units := split_text_units(candidate_text);
            if Length(units) <> input_syllable_count then
            begin
                Exit;
            end;

            Result := Min(get_rank_score(candidate), c_rank_score_cap);
            problematic_penalty := 0;
            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                problematic_penalty := 16000;
            end;

            for start_idx := 0 to input_syllable_count - 1 do
            begin
                chunk_text := '';
                for span_units := 1 to Min(5,
                    input_syllable_count - start_idx) do
                begin
                    chunk_text := chunk_text + units[start_idx + span_units - 1];
                    if span_units < 2 then
                    begin
                        Continue;
                    end;
                    query_key := query_key_cache[start_idx][span_units];
                    if query_key = '' then
                    begin
                        Continue;
                    end;
                    if chunk_text = '' then
                    begin
                        Continue;
                    end;
                    Inc(Result, get_exact_support_bonus_local(query_key,
                        chunk_text, start_idx));
                    Inc(Result, get_exact_mismatch_penalty_local(query_key,
                        chunk_text, span_units, start_idx));
                end;
            end;
            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, support_score_local,
                support_segments_local);
            if support_path_local <> '' then
            begin
                Inc(Result, support_score_local + (support_segments_local * 320));
            end;
            Dec(Result, problematic_penalty);
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 8) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) or
            (m_disable_subspan_standalone_oracle > 0) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(query_key_cache, input_syllable_count);
        for query_start_idx := 0 to input_syllable_count - 1 do
        begin
            SetLength(query_key_cache[query_start_idx],
                Min(5, input_syllable_count - query_start_idx) + 1);
            accumulated_query_key := '';
            for query_span_idx := 1 to Min(5,
                input_syllable_count - query_start_idx) do
            begin
                accumulated_query_key := accumulated_query_key +
                    normalize_pinyin_text(effective_syllables[
                    query_start_idx + query_span_idx - 1].text);
                query_key_cache[query_start_idx][query_span_idx] :=
                    accumulated_query_key;
            end;
        end;

        loaded_queries := TDictionary<string, Byte>.Create;
        exact_rank_cache := TDictionary<string, Integer>.Create;
        exact_top_text_cache := TDictionary<string, string>.Create;
        best_index := -1;
        best_score := Low(Integer);
        top_score := Low(Integer);
        try
            if (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count) then
            begin
                top_score := get_visible_complete_candidate_score_local(
                    candidates[0]);
                best_index := 0;
                best_score := top_score;
            end;

            for idx := 1 to High(candidates) do
            begin
                if idx >= c_visible_probe_limit then
                begin
                    Break;
                end;
                if (Trim(candidates[idx].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                candidate_score := get_visible_complete_candidate_score_local(
                    candidates[idx]);
                if candidate_score > best_score then
                begin
                    best_index := idx;
                    best_score := candidate_score;
                end;
            end;

            if best_index <= 0 then
            begin
                Exit;
            end;

            if (top_score <> Low(Integer)) and
                (best_score <= top_score + c_promote_margin) then
            begin
                Exit;
            end;

            picked := candidates[best_index];
            for idx := best_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vfull=[promote text=%s score=%d]',
                    [Trim(picked.text), best_score]);
            end;
        finally
            exact_top_text_cache.Free;
            exact_rank_cache.Free;
            loaded_queries.Free;
        end;
    end;

    procedure ensure_best_supported_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
        c_exact_probe_limit = 8;
        c_support_weight = 16;
        c_rank_cap = 2048;
        c_problematic_penalty = 24000;
        c_rank_regression_tolerance = 512;
    var
        effective_syllables: TncPinyinParseResult;
        query_key_cache: TArray<TArray<string>>;
        loaded_queries: TDictionary<string, Byte>;
        exact_rank_cache: TDictionary<string, Integer>;
        exact_top_text_cache: TDictionary<string, string>;
        idx: Integer;
        best_idx: Integer;
        top_score: Integer;
        best_score: Integer;
        candidate_score: Integer;
        top_rank: Integer;
        best_rank: Integer;
        candidate_rank: Integer;
        promote_margin: Integer;
        picked: TncCandidate;
        oracle_rank_cache: TDictionary<string, Integer>;
        oracle_top_text: string;
        oracle_rank_local: Integer;
        oracle_idx: Integer;
        query_start_idx: Integer;
        query_span_idx: Integer;
        accumulated_query_key: string;

        procedure ensure_query_loaded_local(const query_key: string;
            const expected_units: Integer; const start_idx: Integer);
        var
            local_idx: Integer;
            cache_key: string;
            candidate_text: string;
            exact_results: TncCandidateList;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
            has_resolved_local: Boolean;
            raw_rank_local: Integer;
        begin
            if (query_key = '') or loaded_queries.ContainsKey(query_key) then
            begin
                Exit;
            end;

            loaded_queries.Add(query_key, 1);
            has_resolved_local := False;
            if (start_idx >= 0) and (expected_units >= 3) and
                (expected_units <= 5) and
                try_resolve_best_exact_subspan_local(start_idx, expected_units,
                resolved_text_local, resolved_score_local,
                resolved_segments_local, resolved_path_local,
                resolved_has_anchor_local, resolved_anchor_units_local) and
                (resolved_text_local <> '') then
            begin
                has_resolved_local := True;
                exact_top_text_cache.AddOrSetValue(query_key,
                    resolved_text_local);
                cache_key := query_key + '|' + resolved_text_local;
                exact_rank_cache.AddOrSetValue(cache_key, 0);
            end;

            if exact_top_text_cache.ContainsKey(query_key) then
            begin
                Exit;
            end;

            if (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(query_key,
                exact_results)) then
            begin
                Exit;
            end;

            raw_rank_local := 0;
            for local_idx := 0 to High(exact_results) do
            begin
                if local_idx >= c_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[local_idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;
                if has_resolved_local and
                    SameText(candidate_text, resolved_text_local) then
                begin
                    Continue;
                end;
                if not exact_top_text_cache.ContainsKey(query_key) then
                begin
                    exact_top_text_cache.Add(query_key, candidate_text);
                end;

                cache_key := query_key + '|' + candidate_text;
                if not exact_rank_cache.ContainsKey(cache_key) then
                begin
                    if has_resolved_local then
                    begin
                        exact_rank_cache.Add(cache_key, raw_rank_local + 1);
                    end
                    else
                    begin
                        exact_rank_cache.Add(cache_key, raw_rank_local);
                    end;
                end;
                Inc(raw_rank_local);
            end;
        end;

        function get_exact_support_bonus_local(const query_key: string;
            const candidate_text: string; const start_idx: Integer): Integer;
        var
            cache_key: string;
            rank_value: Integer;
        begin
            Result := 0;
            if (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            Inc(Result, get_preferred_query_phrase_bonus_local(query_key,
                candidate_text));
            Inc(Result, get_preferred_exact_subspan_bonus_local(query_key,
                candidate_text));

            ensure_query_loaded_local(query_key,
                get_candidate_text_unit_count(candidate_text), start_idx);
            cache_key := query_key + '|' + candidate_text;
            if not exact_rank_cache.TryGetValue(cache_key, rank_value) then
            begin
                Exit;
            end;

            case rank_value of
                0:
                    Inc(Result, 1100);
                1:
                    Inc(Result, 800);
                2, 3:
                    Inc(Result, 480);
                4, 5:
                    Inc(Result, 240);
            else
                Inc(Result, 120);
            end;
        end;

        function get_exact_mismatch_penalty_local(const query_key: string;
            const candidate_text: string; const span_units: Integer;
            const start_idx: Integer): Integer;
        var
            cache_key: string;
            rank_value: Integer;
            top_text: string;
        begin
            Result := 0;
            if (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            ensure_query_loaded_local(query_key, span_units, start_idx);
            cache_key := query_key + '|' + candidate_text;
            if exact_rank_cache.TryGetValue(cache_key, rank_value) then
            begin
                case rank_value of
                    0:
                        Exit(0);
                    1:
                        Exit(-96);
                    2, 3:
                        Exit(-192);
                    4, 5:
                        Exit(-320);
                else
                    Exit(-480);
                end;
            end;

            if not exact_top_text_cache.TryGetValue(query_key, top_text) or
                (top_text = '') or SameText(top_text, candidate_text) then
            begin
                Exit;
            end;

            case span_units of
                2:
                    Result := -1400;
                3:
                    Result := -1100;
                4, 5, 6:
                    Result := -900;
            else
                Result := -600;
            end;
        end;

        function get_supported_complete_candidate_score_local(
            const candidate: TncCandidate): Integer;
        var
            candidate_text: string;
            units: TArray<string>;
            start_idx: Integer;
            span_units: Integer;
            query_key: string;
            chunk_text: string;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
            resolved_text_local: string;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;

            function get_resolved_subspan_alignment_adjustment_local(
                const local_start_idx: Integer; const local_span_units: Integer;
                const local_chunk_text: string): Integer;
            begin
                Result := 0;
                if (local_span_units < 3) or (local_chunk_text = '') then
                begin
                    Exit;
                end;

                if not try_resolve_best_exact_subspan_local(local_start_idx,
                    local_span_units, resolved_text_local, resolved_score_local,
                    resolved_segments_local, resolved_path_local,
                    resolved_has_anchor_local, resolved_anchor_units_local) or
                    (resolved_text_local = '') then
                begin
                    Exit;
                end;

                if (local_span_units = 3) and (not resolved_has_anchor_local) and
                    (resolved_score_local < 12000) then
                begin
                    Exit;
                end;

                if SameText(local_chunk_text, resolved_text_local) then
                begin
                    case local_span_units of
                        3:
                            Result := 4096;
                        4:
                            Result := 16384;
                    else
                        Result := 24576;
                    end;
                    if resolved_has_anchor_local then
                    begin
                        Inc(Result, 2048);
                    end;
                    if resolved_segments_local <= 1 then
                    begin
                        Inc(Result, 1024);
                    end;
                end
                else
                begin
                    case local_span_units of
                        3:
                            Result := -8192;
                        4:
                            Result := -24576;
                    else
                        Result := -32768;
                    end;
                    if resolved_has_anchor_local or
                        (resolved_score_local >= 16000) then
                    begin
                        Dec(Result, 4096);
                    end;
                end;
            end;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate.text);
            if (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            units := split_text_units(candidate_text);
            if Length(units) <> input_syllable_count then
            begin
                Exit;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, support_score_local,
                support_segments_local);
            if support_path_local = '' then
            begin
                support_score_local := -960;
            end;

            Result := (support_score_local * c_support_weight) +
                Min(get_rank_score(candidate), c_rank_cap);
            for start_idx := 0 to input_syllable_count - 1 do
            begin
                chunk_text := '';
                for span_units := 1 to Min(6, input_syllable_count -
                    start_idx) do
                begin
                    chunk_text := chunk_text + units[start_idx + span_units - 1];
                    if span_units < 2 then
                    begin
                        Continue;
                    end;
                    query_key := query_key_cache[start_idx][span_units];
                    if query_key = '' then
                    begin
                        Continue;
                    end;
                    if chunk_text = '' then
                    begin
                        Continue;
                    end;
                    Inc(Result, get_exact_support_bonus_local(query_key,
                        chunk_text, start_idx));
                    Inc(Result, get_exact_mismatch_penalty_local(query_key,
                        chunk_text, span_units, start_idx));
                    Inc(Result, get_resolved_subspan_alignment_adjustment_local(
                        start_idx, span_units, chunk_text));
                end;
            end;
            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                Dec(Result, c_problematic_penalty);
            end;
            if oracle_rank_cache.TryGetValue(candidate_text, oracle_rank_local) then
            begin
                Inc(Result, 6000 - (oracle_rank_local * 1500));
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 3) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        if input_syllable_count <= 4 then
        begin
            promote_margin := 256;
        end
        else if input_syllable_count <= 7 then
        begin
            promote_margin := 512;
        end
        else
        begin
            promote_margin := 1024;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(query_key_cache, input_syllable_count);
        for query_start_idx := 0 to input_syllable_count - 1 do
        begin
            SetLength(query_key_cache[query_start_idx],
                Min(6, input_syllable_count - query_start_idx) + 1);
            accumulated_query_key := '';
            for query_span_idx := 1 to Min(6,
                input_syllable_count - query_start_idx) do
            begin
                accumulated_query_key := accumulated_query_key +
                    normalize_pinyin_text(effective_syllables[
                    query_start_idx + query_span_idx - 1].text);
                query_key_cache[query_start_idx][query_span_idx] :=
                    accumulated_query_key;
            end;
        end;

        loaded_queries := TDictionary<string, Byte>.Create;
        exact_rank_cache := TDictionary<string, Integer>.Create;
        exact_top_text_cache := TDictionary<string, string>.Create;
        oracle_rank_cache := TDictionary<string, Integer>.Create;
        oracle_top_text := '';
        try
            ensure_strict_oracle_sentence_candidates_prepared;
            if strict_oracle_sentence_ok and
                (Length(strict_oracle_sentence_candidates) > 0) then
            begin
                for oracle_idx := 0 to Min(3,
                    High(strict_oracle_sentence_candidates)) do
                begin
                    if Trim(strict_oracle_sentence_candidates[oracle_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(
                        Trim(strict_oracle_sentence_candidates[oracle_idx].text)) <>
                        input_syllable_count then
                    begin
                        Continue;
                    end;
                    if oracle_top_text = '' then
                    begin
                        oracle_top_text := Trim(
                            strict_oracle_sentence_candidates[oracle_idx].text);
                    end;
                    if not oracle_rank_cache.ContainsKey(
                        Trim(strict_oracle_sentence_candidates[oracle_idx].text)) then
                    begin
                        oracle_rank_cache.Add(
                            Trim(strict_oracle_sentence_candidates[oracle_idx].text),
                            oracle_idx);
                    end;
                end;
            end;

            top_score := get_supported_complete_candidate_score_local(
                candidates[0]);
            top_rank := get_rank_score(candidates[0]);
            best_idx := -1;
            best_score := top_score;
            best_rank := top_rank;

            for idx := 1 to Min(c_probe_limit - 1, High(candidates)) do
            begin
                candidate_score := get_supported_complete_candidate_score_local(
                    candidates[idx]);
                candidate_rank := get_rank_score(candidates[idx]);
                if candidate_score > best_score then
                begin
                    best_score := candidate_score;
                    best_idx := idx;
                    best_rank := candidate_rank;
                end;
            end;

            if (best_idx <= 0) or
                (best_score <= top_score + promote_margin) or
                (best_rank + c_rank_regression_tolerance < top_rank) then
            begin
                Exit;
            end;

            picked := candidates[best_idx];
            for idx := best_idx downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vsup=[promote text=%s score=%d top=%d]',
                    [Trim(picked.text), best_score, top_score]);
            end;
        finally
            oracle_rank_cache.Free;
            exact_top_text_cache.Free;
            exact_rank_cache.Free;
            loaded_queries.Free;
        end;
    end;

    procedure ensure_strong_supported_complete_candidate_locked_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
        c_min_support_score = 4096;
        c_promote_margin = 512;
    var
        idx: Integer;
        best_idx: Integer;
        best_score: Integer;
        top_score: Integer;
        candidate_score: Integer;
        picked: TncCandidate;

        function get_lock_score_local(const candidate: TncCandidate): Integer;
        var
            candidate_text_local: string;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
            encoded_path_local: string;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) or
                is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                Exit;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text_local, support_score_local,
                support_segments_local);
            if (support_path_local = '') or
                (support_score_local < c_min_support_score) then
            begin
                Exit;
            end;

            Result := (support_score_local * 32) +
                Min(get_rank_score(candidate), 4096);
            if support_segments_local <= 4 then
            begin
                Inc(Result, 8192 - (support_segments_local * 512));
            end
            else
            begin
                Dec(Result, (support_segments_local - 4) * 768);
            end;

            encoded_path_local := get_segment_path_for_candidate(candidate);
            if (encoded_path_local <> '') and
                is_natural_predictive_segment_path_local(encoded_path_local) then
            begin
                Inc(Result, 4096);
            end;
            if candidate.has_dict_weight or (candidate.source = cs_user) then
            begin
                Inc(Result, 4096);
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        top_score := get_lock_score_local(candidates[0]);
        best_idx := -1;
        best_score := top_score;
        for idx := 1 to Min(c_probe_limit - 1, High(candidates)) do
        begin
            candidate_score := get_lock_score_local(candidates[idx]);
            if candidate_score > best_score then
            begin
                best_score := candidate_score;
                best_idx := idx;
            end;
        end;

        if (best_idx <= 0) or (best_score <= top_score + c_promote_margin) then
        begin
            Exit;
        end;

        picked := candidates[best_idx];
        for idx := best_idx downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vlock=[promote text=%s score=%d top=%d]',
                [Trim(picked.text), best_score, top_score]);
        end;
    end;

    procedure get_exact_window_consistency_stats_local(
        const candidate_text: string; const min_span: Integer;
        const max_span: Integer; out out_match_count: Integer;
        out out_mismatch_count: Integer; out out_strong_match_count: Integer;
        out out_strong_mismatch_count: Integer);
    var
        candidate_units_local: TArray<string>;
        start_idx_local: Integer;
        span_syllables_local: Integer;
        candidate_chunk_text_local: string;
        resolved_text_local: string;
        resolved_path_local: string;
        resolved_score_local: Integer;
        resolved_segments_local: Integer;
        resolved_anchor_units_local: Integer;
        resolved_has_anchor_local: Boolean;
        strong_window_local: Boolean;

        function build_text_from_units_local(const text_units: TArray<string>;
            const start_unit: Integer; const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (unit_count <= 0) or (start_unit < 0) or
                (start_unit + unit_count - 1 > High(text_units)) then
            begin
                Exit;
            end;

            for unit_idx := start_unit to start_unit + unit_count - 1 do
            begin
                Result := Result + text_units[unit_idx];
            end;
        end;
    begin
        out_match_count := 0;
        out_mismatch_count := 0;
        out_strong_match_count := 0;
        out_strong_mismatch_count := 0;
        candidate_units_local := split_text_units(Trim(candidate_text));
        if Length(candidate_units_local) <> input_syllable_count then
        begin
            Exit;
        end;

        for start_idx_local := 0 to
            input_syllable_count - Max(2, min_span) do
        begin
            for span_syllables_local := Max(2, min_span) to
                Min(max_span, input_syllable_count - start_idx_local) do
            begin
                if not try_resolve_best_exact_subspan_local(start_idx_local,
                    span_syllables_local, resolved_text_local,
                    resolved_score_local, resolved_segments_local,
                    resolved_path_local, resolved_has_anchor_local,
                    resolved_anchor_units_local) or
                    (resolved_text_local = '') then
                begin
                    Continue;
                end;

                candidate_chunk_text_local := build_text_from_units_local(
                    candidate_units_local, start_idx_local,
                    span_syllables_local);
                if candidate_chunk_text_local = '' then
                begin
                    Continue;
                end;

                strong_window_local := (span_syllables_local >= 3) or
                    resolved_has_anchor_local or (resolved_score_local >= 4096);
                if SameText(candidate_chunk_text_local, resolved_text_local) then
                begin
                    Inc(out_match_count);
                    if strong_window_local then
                    begin
                        Inc(out_strong_match_count);
                    end;
                end
                else if strong_window_local then
                begin
                    Inc(out_mismatch_count);
                    if (resolved_score_local >= 12000) or
                        resolved_has_anchor_local or
                        (span_syllables_local >= 4) then
                    begin
                        Inc(out_strong_mismatch_count);
                    end;
                end;
            end;
        end;
    end;

    function has_strong_top_complete_candidate_for_postprocess_lock_local(
        const candidates: TncCandidateList): Boolean;
    const
        c_min_support_score = 4096;
        c_relaxed_support_score = -960;
        c_max_support_segments = 8;
        c_exact_probe_limit = 6;
        c_visible_probe_limit = 8;
        c_visible_rank_override_margin = 256;
        c_visible_support_override_margin = 1024;
    var
        top_candidate: TncCandidate;
        top_text: string;
        support_score_local: Integer;
        support_segments_local: Integer;
        support_path_local: string;
        encoded_path_local: string;
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_match_count_local: Integer;
        exact_mismatch_count_local: Integer;
        exact_strong_match_count_local: Integer;
        exact_strong_mismatch_count_local: Integer;
        top_rank_local: Integer;
        visible_idx_local: Integer;
        visible_candidate_local: TncCandidate;
        visible_text_local: string;
        visible_support_score_local: Integer;
        visible_support_segments_local: Integer;
        visible_support_path_local: string;
        visible_encoded_path_local: string;
        visible_rank_local: Integer;
        has_natural_path_backing_local: Boolean;

        function build_chunk_text_local(const units_local: TArray<string>;
            const start_idx_local: Integer; const unit_count_local: Integer): string;
        var
            unit_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (unit_count_local <= 0) or
                (start_idx_local + unit_count_local - 1 > High(units_local)) then
            begin
                Exit;
            end;

            for unit_idx_local := start_idx_local to
                start_idx_local + unit_count_local - 1 do
            begin
                Result := Result + units_local[unit_idx_local];
            end;
        end;

        function has_ambiguous_exact_subspan_local(
            const text_value_local: string): Boolean;
        const
            c_ambiguous_span_max = 3;
            c_ambiguous_weight_margin = 256;
            c_ambiguous_weight_ratio_pct = 74;
        var
            units_local: TArray<string>;
            syllables_local: TncPinyinParseResult;
            start_idx_local: Integer;
            span_units_local: Integer;
            query_idx_local: Integer;
            query_key_local: string;
            current_chunk_local: string;
            local_results_local: TncCandidateList;
            result_idx_local: Integer;
            filtered_rank_local: Integer;
            matched_rank_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            top_weight_local: Integer;
            second_weight_local: Integer;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value_local));
            syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            if (Length(units_local) <> input_syllable_count) or
                (Length(syllables_local) <> input_syllable_count) then
            begin
                Exit;
            end;

            for start_idx_local := 0 to input_syllable_count - 2 do
            begin
                for span_units_local := 2 to Min(c_ambiguous_span_max,
                    input_syllable_count - start_idx_local) do
                begin
                    query_key_local := '';
                    for query_idx_local := start_idx_local to
                        start_idx_local + span_units_local - 1 do
                    begin
                        query_key_local := query_key_local +
                            normalize_pinyin_text(
                            syllables_local[query_idx_local].text);
                    end;
                    if (query_key_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(
                        query_key_local, local_results_local)) then
                    begin
                        Continue;
                    end;

                    current_chunk_local := build_chunk_text_local(
                        units_local, start_idx_local, span_units_local);
                    matched_rank_local := -1;
                    filtered_rank_local := 0;
                    top_weight_local := Low(Integer);
                    second_weight_local := Low(Integer);

                    for result_idx_local := 0 to Min(c_exact_probe_limit - 1,
                        High(local_results_local)) do
                    begin
                        if Trim(local_results_local[result_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        candidate_text_local := Trim(
                            local_results_local[result_idx_local].text);
                        if get_candidate_text_unit_count(candidate_text_local) <>
                            span_units_local then
                        begin
                            Continue;
                        end;

                        candidate_weight_local :=
                            local_results_local[result_idx_local].score;
                        if local_results_local[result_idx_local].has_dict_weight and
                            (local_results_local[result_idx_local].dict_weight >
                            candidate_weight_local) then
                        begin
                            candidate_weight_local :=
                                local_results_local[result_idx_local].dict_weight;
                        end;

                        if filtered_rank_local = 0 then
                        begin
                            top_weight_local := candidate_weight_local;
                        end
                        else if candidate_weight_local > second_weight_local then
                        begin
                            second_weight_local := candidate_weight_local;
                        end;

                        if SameText(candidate_text_local,
                            current_chunk_local) then
                        begin
                            matched_rank_local := filtered_rank_local;
                        end;
                        Inc(filtered_rank_local);
                    end;

                    if (matched_rank_local = 0) and (second_weight_local > 0) and
                        (((top_weight_local - second_weight_local) <=
                        c_ambiguous_weight_margin) or
                        ((second_weight_local * 100) >=
                        (top_weight_local * c_ambiguous_weight_ratio_pct))) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;
    begin
        Result := False;
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        top_candidate := candidates[0];
        if Trim(top_candidate.comment) <> '' then
        begin
            Exit;
        end;

        top_text := Trim(top_candidate.text);
        if (top_text = '') or
            (get_candidate_text_unit_count(top_text) <> input_syllable_count) or
            is_problematic_single_char_chain_candidate_for_query(lookup_text,
            top_candidate) then
        begin
            Exit;
        end;

        support_score_local := Low(Integer);
        support_segments_local := MaxInt;
        support_path_local := infer_segment_path_for_query_text_with_score(
            lookup_text, top_text, support_score_local,
            support_segments_local);
        encoded_path_local := get_segment_path_for_candidate(top_candidate);
        has_natural_path_backing_local :=
            (support_path_local <> '') or
            ((Trim(encoded_path_local) <> '') and
            (get_encoded_path_segment_count_local(encoded_path_local) > 1) and
            is_natural_predictive_segment_path_local(encoded_path_local));
        if (support_path_local = '') and
            ((Trim(encoded_path_local) = '') or
            (get_encoded_path_segment_count_local(encoded_path_local) <= 1) or
            (not is_natural_predictive_segment_path_local(encoded_path_local))) then
        begin
            Exit;
        end;
        if (support_path_local <> '') and
            (support_score_local < c_min_support_score) and
            (not ((input_syllable_count >= 8) and
            (support_segments_local <= c_max_support_segments) and
            (support_score_local > c_relaxed_support_score) and
            (Trim(encoded_path_local) <> '') and
            is_natural_predictive_segment_path_local(encoded_path_local))) then
        begin
            Exit;
        end;

        get_exact_window_consistency_stats_local(top_text, 2, 5,
            exact_match_count_local, exact_mismatch_count_local,
            exact_strong_match_count_local, exact_strong_mismatch_count_local);
        if exact_strong_mismatch_count_local > 0 then
        begin
            Exit(False);
        end;
        if (exact_mismatch_count_local > 0) and
            (exact_strong_match_count_local = 0) then
        begin
            Exit(False);
        end;
        if has_ambiguous_exact_subspan_local(top_text) then
        begin
            Exit(False);
        end;

        top_rank_local := get_rank_score(top_candidate);
        for visible_idx_local := 1 to Min(c_visible_probe_limit - 1,
            High(candidates)) do
        begin
            visible_candidate_local := candidates[visible_idx_local];
            if Trim(visible_candidate_local.comment) <> '' then
            begin
                Continue;
            end;

            visible_text_local := Trim(visible_candidate_local.text);
            if (visible_text_local = '') or SameText(visible_text_local, top_text) or
                (get_candidate_text_unit_count(visible_text_local) <>
                input_syllable_count) or
                is_problematic_single_char_chain_candidate_for_query(
                lookup_text, visible_candidate_local) then
            begin
                Continue;
            end;

            visible_rank_local := get_rank_score(visible_candidate_local);
            if visible_rank_local < top_rank_local + c_visible_rank_override_margin then
            begin
                Continue;
            end;

            visible_support_score_local := Low(Integer);
            visible_support_segments_local := MaxInt;
            visible_support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, visible_text_local, visible_support_score_local,
                visible_support_segments_local);
            visible_encoded_path_local := get_segment_path_for_candidate(
                visible_candidate_local);
            if (visible_support_path_local = '') and
                ((Trim(visible_encoded_path_local) = '') or
                (get_encoded_path_segment_count_local(visible_encoded_path_local) <= 1) or
                (not is_natural_predictive_segment_path_local(
                visible_encoded_path_local))) then
            begin
                Continue;
            end;

            if (support_path_local = '') and (visible_support_path_local <> '') then
            begin
                Exit(False);
            end;
            if (visible_support_path_local <> '') and
                (visible_support_score_local >
                support_score_local + c_visible_support_override_margin) then
            begin
                Exit(False);
            end;
        end;

        if top_candidate.source = cs_user then
        begin
            Exit(True);
        end;

        if top_candidate.has_dict_weight then
        begin
            Exit(has_natural_path_backing_local and
                (((support_path_local <> '') and
                (support_score_local >= c_min_support_score)) or
                ((support_path_local <> '') and
                (support_segments_local <= c_max_support_segments) and
                (exact_strong_match_count_local >= 2)) or
                ((support_path_local <> '') and
                (support_segments_local <= c_max_support_segments) and
                (exact_strong_match_count_local >= 1) and
                (exact_mismatch_count_local = 0))));
        end;

        if (support_segments_local <= c_max_support_segments) and
            (encoded_path_local <> '') and
            is_natural_predictive_segment_path_local(encoded_path_local) then
        begin
            Exit(True);
        end;

        if (m_dictionary <> nil) and
            m_dictionary.lookup_exact_full_pinyin(lookup_text,
            exact_results_local) then
        begin
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if exact_idx_local >= c_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(exact_results_local[exact_idx_local].text),
                    top_text) then
                begin
                    Exit((support_path_local <> '') and
                        ((exact_strong_match_count_local >= 1) or
                        (exact_mismatch_count_local = 0)));
                end;
            end;
        end;

        Result := (support_segments_local <= c_max_support_segments) and
            ((exact_strong_match_count_local >= 2) or
            ((exact_strong_match_count_local >= 1) and
            (exact_mismatch_count_local = 0)));
    end;

    procedure ensure_best_exact_window_consensus_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 4;
        c_window_min_syllables = 2;
        c_window_max_syllables = 6;
        c_promote_margin = 512;
        c_score_keep_margin = 24000;
        c_consensus_force_margin = 20000;
        c_supportless_force_margin = 16000;
        c_support_bonus_divisor = 4;
        c_problematic_penalty = 24000;
        c_consensus_budget_ms = 24;
        // Direct/offline decoding gets more room, but must remain bounded.
        c_consensus_direct_budget_ms = 96;
        c_consensus_work_limit = 512;
    type
        TConsensusWindowCacheEntry = record
            text: string;
            path: string;
            score: Integer;
            segments: Integer;
            anchor_units: Integer;
            has_anchor: Boolean;
            found: Boolean;
        end;
    var
        effective_syllables: TncPinyinParseResult;
        top_score: Integer;
        best_score: Integer;
        best_idx: Integer;
        idx: Integer;
        candidate_score: Integer;
        picked: TncCandidate;
        debug_summary: string;
        top_support_score_local: Integer;
        top_support_segments_local: Integer;
        picked_support_score_local: Integer;
        picked_support_segments_local: Integer;
        top_support_path_local: string;
        picked_support_path_local: string;
        oracle_rank_cache: TDictionary<string, Integer>;
        oracle_top_text: string;
        oracle_rank_local: Integer;
        oracle_idx: Integer;
        best_from_oracle_local: Boolean;
        oracle_candidate_local: TncCandidate;
        best_oracle_candidate_local: TncCandidate;
        best_supported_visible_score_local: Integer;
        best_supported_visible_idx_local: Integer;
        best_supported_visible_text_local: string;
        oracle_support_score_local: Integer;
        oracle_support_segments_local: Integer;
        oracle_support_path_local: string;
        consensus_start_tick: UInt64;
        consensus_work_count: Int64;
        consensus_budget_logged: Boolean;
        consensus_window_cache: TDictionary<string, TConsensusWindowCacheEntry>;

        function consensus_budget_exceeded_local: Boolean;
        var
            budget_ms_local: UInt64;
            budget_start_tick_local: UInt64;
            emergency_budget_ms_local: UInt64;
        begin
            if m_composition_built_incrementally then
            begin
                budget_ms_local := c_consensus_budget_ms;
            end
            else
            begin
                budget_ms_local := c_consensus_direct_budget_ms;
            end;
            budget_start_tick_local := consensus_start_tick;
            emergency_budget_ms_local := budget_ms_local * 4;
            if m_search_budget_mode = sbm_production then
            begin
                budget_start_tick_local := total_start_tick;
                emergency_budget_ms_local :=
                    c_long_postprocess_emergency_budget_ms;
            end;
            Result := search_budget_should_stop(budget_start_tick_local,
                consensus_work_count, c_consensus_work_limit,
                budget_ms_local, emergency_budget_ms_local,
                input_syllable_count >=
                c_long_sentence_full_path_min_syllables);
            if Result and m_config.debug_mode and
                (not consensus_budget_logged) then
            begin
                consensus_budget_logged := True;
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vcons=[budget mode=%d work=%d]',
                    [Ord(m_search_budget_mode), consensus_work_count]);
            end;
        end;

        function try_resolve_window_cached_local(const start_idx: Integer;
            const span_syllables: Integer; out out_text: string;
            out out_score: Integer; out out_segments: Integer;
            out out_path: string; out out_has_anchor: Boolean;
            out out_anchor_units: Integer): Boolean;
        var
            cache_key: string;
            cache_entry: TConsensusWindowCacheEntry;
        begin
            out_text := '';
            out_score := 0;
            out_segments := 0;
            out_path := '';
            out_has_anchor := False;
            out_anchor_units := 0;
            if consensus_budget_exceeded_local then
            begin
                Exit(False);
            end;
            cache_key := IntToStr(start_idx) + ':' + IntToStr(span_syllables);
            if (consensus_window_cache <> nil) and
                consensus_window_cache.TryGetValue(cache_key, cache_entry) then
            begin
                out_text := cache_entry.text;
                out_score := cache_entry.score;
                out_segments := cache_entry.segments;
                out_path := cache_entry.path;
                out_has_anchor := cache_entry.has_anchor;
                out_anchor_units := cache_entry.anchor_units;
                Exit(cache_entry.found);
            end;

            Result := try_resolve_best_exact_subspan_local(start_idx,
                span_syllables, out_text, out_score, out_segments,
                out_path, out_has_anchor, out_anchor_units) and
                (out_text <> '');
            Inc(consensus_work_count);
            if consensus_window_cache <> nil then
            begin
                cache_entry.text := out_text;
                cache_entry.score := out_score;
                cache_entry.segments := out_segments;
                cache_entry.path := out_path;
                cache_entry.has_anchor := out_has_anchor;
                cache_entry.anchor_units := out_anchor_units;
                cache_entry.found := Result;
                consensus_window_cache.AddOrSetValue(cache_key, cache_entry);
            end;
        end;

        function get_supported_keep_margin_local: Integer;
        begin
            if input_syllable_count >= 10 then
            begin
                Exit(64000);
            end;
            if input_syllable_count >= 8 then
            begin
                Exit(24000);
            end;
            Result := 12000;
        end;

        function is_visible_complete_candidate_text_local(
            const text_value: string): Boolean;
        var
            visible_idx_local: Integer;
        begin
            Result := False;
            if text_value = '' then
            begin
                Exit;
            end;

            for visible_idx_local := 0 to Min(c_probe_limit - 1,
                High(candidates)) do
            begin
                if Trim(candidates[visible_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(
                    Trim(candidates[visible_idx_local].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                if SameText(Trim(candidates[visible_idx_local].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function build_text_from_units_local(const text_units: TArray<string>;
            const start_unit: Integer; const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (unit_count <= 0) or (start_unit < 0) or
                (start_unit + unit_count - 1 > High(text_units)) then
            begin
                Exit;
            end;

            for unit_idx := start_unit to start_unit + unit_count - 1 do
            begin
                Result := Result + text_units[unit_idx];
            end;
        end;

        function get_window_consensus_score_local(
            const candidate: TncCandidate): Integer;
        var
            candidate_text: string;
            candidate_units: TArray<string>;
            start_idx: Integer;
            span_syllables: Integer;
            candidate_chunk_text: string;
            resolved_text: string;
            resolved_path: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            window_weight: Integer;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
            head_boundary_score_local: Integer;
            candidate_rank_bonus_local: Integer;

            function get_fixed_boundary_single_char_for_window_local(
                const syllable_text: string): string;
            var
                normalized_syllable: string;
            begin
                Result := '';
                normalized_syllable := normalize_pinyin_text(syllable_text);
                if (normalized_syllable = 'ta') or
                    (normalized_syllable = 'qing') or
                    (normalized_syllable = 'bei') then
                begin
                    Exit;
                end;
                if normalized_syllable = 'dan' then
                begin
                    Result := string(Char($4F46));
                end
                else if normalized_syllable = 'sui' then
                begin
                    Result := string(Char($867D));
                end
                else if normalized_syllable = 'er' then
                begin
                    Result := string(Char($800C));
                end
                else if normalized_syllable = 'wo' then
                begin
                    Result := string(Char($6211));
                end
                else if normalized_syllable = 'ni' then
                begin
                    Result := string(Char($4F60));
                end
                else if normalized_syllable = 'ta' then
                begin
                    Result := string(Char($4ED6));
                end
                else if normalized_syllable = 'he' then
                begin
                    Result := string(Char($548C));
                end
                else if normalized_syllable = 'qu' then
                begin
                    Result := string(Char($53BB));
                end
                else if normalized_syllable = 'shi' then
                begin
                    Result := string(Char($662F));
                end
                else if normalized_syllable = 'ye' then
                begin
                    Result := string(Char($4E5F));
                end
                else if normalized_syllable = 'yu' then
                begin
                    Result := string(Char($4E8E));
                end
                else if normalized_syllable = 'de' then
                begin
                    Result := string(Char($7684));
                end
                else if normalized_syllable = 'zai' then
                begin
                    Result := string(Char($5728));
                end
                else if normalized_syllable = 'ba' then
                begin
                    Result := string(Char($628A));
                end
                else if normalized_syllable = 'bei' then
                begin
                    Result := string(Char($88AB));
                end
                else if normalized_syllable = 'bing' then
                begin
                    Result := string(Char($5E76));
                end
                else if normalized_syllable = 'que' then
                begin
                    Result := string(Char($5374));
                end
                else if normalized_syllable = 'jiu' then
                begin
                    Result := string(Char($5C31));
                end
                else if normalized_syllable = 'bu' then
                begin
                    Result := string(Char($4E0D));
                end;
            end;

            function get_fixed_boundary_head_score_for_window_local(
                const candidate_text_local: string): Integer;
            var
                syllables_local: TncPinyinParseResult;
                text_units_local: TArray<string>;
                head_idx: Integer;
                expected_char: string;
            begin
                Result := 0;
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                text_units_local := split_text_units(
                    Trim(candidate_text_local));
                for head_idx := 0 to Min(
                    Min(High(syllables_local), High(text_units_local)), 3) do
                begin
                    expected_char :=
                        get_fixed_boundary_single_char_for_window_local(
                        syllables_local[head_idx].text);
                    if expected_char = '' then
                    begin
                        Continue;
                    end;
                    if SameText(text_units_local[head_idx], expected_char) then
                    begin
                        Inc(Result);
                    end;
                end;
            end;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate.text);
            if candidate_text = '' then
            begin
                Exit;
            end;

            candidate_units := split_text_units(candidate_text);
            if Length(candidate_units) <> input_syllable_count then
            begin
                Exit;
            end;
            if consensus_budget_exceeded_local then
            begin
                Exit;
            end;

            Result := 0;
            for start_idx := 0 to input_syllable_count - c_window_min_syllables do
            begin
                for span_syllables := c_window_min_syllables to
                    Min(c_window_max_syllables,
                    input_syllable_count - start_idx) do
                begin
                    if consensus_budget_exceeded_local then
                    begin
                        Exit(Low(Integer));
                    end;
                    if not try_resolve_window_cached_local(start_idx,
                        span_syllables, resolved_text, resolved_score,
                        resolved_segments, resolved_path, resolved_has_anchor,
                        resolved_anchor_units) then
                    begin
                        Continue;
                    end;

                    candidate_chunk_text := build_text_from_units_local(
                        candidate_units, start_idx, span_syllables);
                    if candidate_chunk_text = '' then
                    begin
                        Continue;
                    end;

                    window_weight := Min(4096, Max(512,
                        resolved_score div 8));
                    Inc(window_weight, span_syllables * 96);
                    if resolved_has_anchor then
                    begin
                        Inc(window_weight, 256);
                    end;
                    if start_idx + span_syllables = input_syllable_count then
                    begin
                        Inc(window_weight, window_weight * 4);
                    end
                    else if start_idx + span_syllables >=
                        input_syllable_count - 1 then
                    begin
                        Inc(window_weight, window_weight);
                    end;

                    if SameText(candidate_chunk_text, resolved_text) then
                    begin
                        Inc(Result, window_weight);
                        if span_syllables >= 4 then
                        begin
                            Inc(Result, window_weight * 3);
                        end
                        else if (span_syllables = 3) and
                            (resolved_has_anchor or (resolved_score >= 12000)) then
                        begin
                            Inc(Result, window_weight);
                        end;
                        if (span_syllables = 2) and
                            ((resolved_segments <= 1) or
                            (resolved_score >= 4096)) then
                        begin
                            Inc(Result, window_weight div 2);
                        end;
                    end
                    else if (span_syllables = 2) and
                        ((resolved_segments <= 1) or
                        (resolved_score >= 4096)) then
                    begin
                        Dec(Result, Min(1536, Max(256, window_weight div 4)));
                    end
                    else if span_syllables >= 4 then
                    begin
                        Dec(Result, window_weight * 4);
                    end
                    else if resolved_has_anchor or
                        (resolved_score >= 12000) then
                    begin
                        Dec(Result, window_weight * 2);
                    end
                    else if window_weight >= 1536 then
                    begin
                        Dec(Result, window_weight div 3);
                    end;
                end;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text, support_score_local,
                support_segments_local);
            if support_path_local <> '' then
            begin
                Inc(Result, support_score_local div c_support_bonus_divisor);
                Inc(Result, support_segments_local * 160);
            end
            else
            begin
                Dec(Result, 1024);
            end;

            head_boundary_score_local :=
                get_fixed_boundary_head_score_for_window_local(candidate_text);
            if head_boundary_score_local > 0 then
            begin
                Inc(Result, head_boundary_score_local * 12000);
            end
            else if input_syllable_count >= 6 then
            begin
                Dec(Result, 12000);
            end;

            candidate_rank_bonus_local :=
                Min(24000, Max(0, get_rank_score(candidate)));
            if candidate_rank_bonus_local > 0 then
            begin
                Inc(Result, candidate_rank_bonus_local);
                if support_path_local <> '' then
                begin
                    Inc(Result, candidate_rank_bonus_local div 2);
                end;
            end;

            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                Dec(Result, c_problematic_penalty);
            end;
            if oracle_rank_cache.TryGetValue(candidate_text, oracle_rank_local) then
            begin
                Inc(Result, 16000 - (oracle_rank_local * 4000));
            end
            else if oracle_top_text <> '' then
            begin
                Dec(Result, 8000);
            end;
        end;
    begin
        consensus_start_tick := nc_monotonic_tick_ms;
        consensus_work_count := 0;
        consensus_budget_logged := False;
        consensus_window_cache := nil;
        if (Length(candidates) <= 1) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) or
            (m_disable_subspan_standalone_oracle > 0) then
        begin
            Exit;
        end;

        if has_strong_top_complete_candidate_for_postprocess_lock_local(
            candidates) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' vcons=[locked]';
            end;
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        oracle_rank_cache := TDictionary<string, Integer>.Create;
        consensus_window_cache :=
            TDictionary<string, TConsensusWindowCacheEntry>.Create;
        oracle_top_text := '';
        try
            ensure_strict_oracle_sentence_candidates_prepared;
            if strict_oracle_sentence_ok and
                (Length(strict_oracle_sentence_candidates) > 0) then
            begin
                for oracle_idx := 0 to Min(3,
                    High(strict_oracle_sentence_candidates)) do
                begin
                    if Trim(strict_oracle_sentence_candidates[oracle_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(
                        Trim(strict_oracle_sentence_candidates[oracle_idx].text)) <>
                        input_syllable_count then
                    begin
                        Continue;
                    end;
                    if oracle_top_text = '' then
                    begin
                        oracle_top_text := Trim(
                            strict_oracle_sentence_candidates[oracle_idx].text);
                    end;
                    if not oracle_rank_cache.ContainsKey(
                        Trim(strict_oracle_sentence_candidates[oracle_idx].text)) then
                    begin
                        oracle_rank_cache.Add(
                            Trim(strict_oracle_sentence_candidates[oracle_idx].text),
                            oracle_idx);
                    end;
                end;
            end;

            top_score := get_window_consensus_score_local(candidates[0]);
            best_idx := -1;
            best_score := top_score;
            best_from_oracle_local := False;
            best_supported_visible_score_local := Low(Integer);
            best_supported_visible_idx_local := -1;
            best_supported_visible_text_local := '';
            top_support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(candidates[0].text), top_support_score_local,
                top_support_segments_local);
            if top_support_path_local <> '' then
            begin
                best_supported_visible_score_local := top_score;
                best_supported_visible_idx_local := 0;
                best_supported_visible_text_local := Trim(candidates[0].text);
            end;
            if m_config.debug_mode then
            begin
                debug_summary := Format(' {top=%s s=%d}',
                    [Trim(candidates[0].text), top_score]);
            end;

            for idx := 1 to Min(c_probe_limit - 1, High(candidates)) do
            begin
                if consensus_budget_exceeded_local then
                begin
                    Break;
                end;
                candidate_score := get_window_consensus_score_local(candidates[idx]);
                if m_config.debug_mode then
                begin
                    debug_summary := debug_summary + Format(' {%s s=%d}',
                        [Trim(candidates[idx].text), candidate_score]);
                end;
                if candidate_score > best_score then
                begin
                    best_score := candidate_score;
                    best_idx := idx;
                    best_from_oracle_local := False;
                end;
                picked_support_path_local := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(candidates[idx].text),
                    picked_support_score_local, picked_support_segments_local);
                if (picked_support_path_local <> '') and
                    (candidate_score > best_supported_visible_score_local) then
                begin
                    best_supported_visible_score_local := candidate_score;
                    best_supported_visible_idx_local := idx;
                    best_supported_visible_text_local := Trim(candidates[idx].text);
                end;
            end;

            if strict_oracle_sentence_ok and
                (Length(strict_oracle_sentence_candidates) > 0) then
            begin
                for oracle_idx := 0 to Min(2,
                    High(strict_oracle_sentence_candidates)) do
                begin
                    if consensus_budget_exceeded_local then
                    begin
                        Break;
                    end;
                    oracle_candidate_local :=
                        strict_oracle_sentence_candidates[oracle_idx];
                    if Trim(oracle_candidate_local.comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(
                        Trim(oracle_candidate_local.text)) <>
                        input_syllable_count then
                    begin
                        Continue;
                    end;
                    if is_visible_complete_candidate_text_local(
                        Trim(oracle_candidate_local.text)) then
                    begin
                        Continue;
                    end;

                    candidate_score := get_window_consensus_score_local(
                        oracle_candidate_local);
                    oracle_support_path_local :=
                        infer_segment_path_for_query_text_with_score(
                        lookup_text, Trim(oracle_candidate_local.text),
                        oracle_support_score_local,
                        oracle_support_segments_local);
                    if (oracle_support_path_local = '') and
                        (best_supported_visible_score_local > Low(Integer)) and
                        (candidate_score <=
                        best_supported_visible_score_local +
                        get_supported_keep_margin_local) then
                    begin
                        Continue;
                    end;
                    if m_config.debug_mode then
                    begin
                        debug_summary := debug_summary + Format(' {oracle:%s s=%d}',
                            [Trim(oracle_candidate_local.text), candidate_score]);
                    end;
                    if candidate_score > best_score then
                    begin
                        best_score := candidate_score;
                        best_idx := -1;
                        best_from_oracle_local := True;
                        best_oracle_candidate_local := oracle_candidate_local;
                    end;
                end;
            end;

            if best_from_oracle_local and
                (best_supported_visible_idx_local >= 0) and
                (best_supported_visible_score_local +
                get_supported_keep_margin_local >= best_score) then
            begin
                best_from_oracle_local := False;
                best_idx := best_supported_visible_idx_local;
                best_score := best_supported_visible_score_local;
            end;

            if ((not best_from_oracle_local) and (best_idx <= 0)) or
                (best_score <= top_score + c_promote_margin) then
            begin
                if m_config.debug_mode and (debug_summary <> '') then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' vcons=[keep top=%d best=%d]%s',
                        [top_score, best_score, debug_summary]);
                end;
                Exit;
            end;

            if best_from_oracle_local then
            begin
                picked := best_oracle_candidate_local;
            end
            else
            begin
                picked := candidates[best_idx];
            end;
            if (candidates[0].score >= picked.score + c_score_keep_margin) and
                (best_score < top_score + c_consensus_force_margin) then
            begin
                if m_config.debug_mode and (debug_summary <> '') then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' vcons=[keep-score top=%d best=%d]%s',
                        [top_score, best_score, debug_summary]);
                end;
                Exit;
            end;
            picked_support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(picked.text), picked_support_score_local,
                picked_support_segments_local);
            if (top_support_path_local <> '') and
                (picked_support_path_local = '') then
            begin
                if best_score < top_score + c_supportless_force_margin then
                begin
                    if m_config.debug_mode and (debug_summary <> '') then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' vcons=[keep-supported top=%d best=%d]%s',
                            [top_score, best_score, debug_summary]);
                    end;
                    Exit;
                end;
            end;
            if best_from_oracle_local then
            begin
                SetLength(candidates, Length(candidates) + 1);
                for idx := High(candidates) downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
            end
            else
            begin
                for idx := best_idx downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
            end;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vcons=[promote text=%s score=%d top=%d ext=%d]',
                    [Trim(picked.text), best_score, top_score,
                    Ord(best_from_oracle_local)]);
                if debug_summary <> '' then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        debug_summary;
                end;
            end;
        finally
            consensus_window_cache.Free;
            oracle_rank_cache.Free;
        end;
    end;

    procedure ensure_best_exact_window_repair_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 3;
        c_window_min_syllables = 2;
        c_window_max_syllables = 6;
        c_variant_limit = 4;
        c_variant_exact_probe_limit = 4;
        c_repair_pass_limit = 2;
        c_promote_score_margin = 4096;
        c_score_support_weight = 16;
        c_problematic_penalty = 24000;
        c_repair_budget_ms = 24;
        // Direct/offline decoding gets more room, but must remain bounded.
        c_repair_direct_budget_ms = 96;
        { Bound expensive exact-window probes by deterministic work before the
          query-wide wall-clock emergency guard becomes relevant. }
        c_repair_work_limit = 512;
    type
        TWindowVariantCacheEntry = record
            texts: TArray<string>;
            weights: TArray<Integer>;
            anchor_units: TArray<Integer>;
            found: Boolean;
        end;
    var
        effective_syllables: TncPinyinParseResult;
        best_score: Integer;
        top_score: Integer;
        probe_idx: Integer;
        repaired_candidate: TncCandidate;
        local_candidate: TncCandidate;
        local_score: Integer;
        pass_idx: Integer;
        move_idx: Integer;
        repair_start_tick: UInt64;
        repair_work_count: Int64;
        repair_budget_logged: Boolean;
        variant_cache: TDictionary<string, TWindowVariantCacheEntry>;

        function repair_budget_exceeded_local: Boolean;
        var
            budget_ms_local: UInt64;
            budget_start_tick_local: UInt64;
            emergency_budget_ms_local: UInt64;
        begin
            if m_composition_built_incrementally then
            begin
                budget_ms_local := c_repair_budget_ms;
            end
            else
            begin
                budget_ms_local := c_repair_direct_budget_ms;
            end;
            budget_start_tick_local := repair_start_tick;
            emergency_budget_ms_local := budget_ms_local * 4;
            if m_search_budget_mode = sbm_production then
            begin
                budget_start_tick_local := total_start_tick;
                emergency_budget_ms_local :=
                    c_long_postprocess_emergency_budget_ms;
            end;
            Result := search_budget_should_stop(budget_start_tick_local,
                repair_work_count, c_repair_work_limit, budget_ms_local,
                emergency_budget_ms_local,
                input_syllable_count >=
                c_long_sentence_full_path_min_syllables);
            if Result and m_config.debug_mode and (not repair_budget_logged) then
            begin
                repair_budget_logged := True;
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vrepair=[budget mode=%d work=%d]',
                    [Ord(m_search_budget_mode), repair_work_count]);
            end;
        end;

        function get_support_margin_local: Integer;
        begin
            if input_syllable_count <= 4 then
            begin
                Exit(128);
            end;
            if input_syllable_count <= 6 then
            begin
                Exit(256);
            end;
            Result := 512;
        end;

        function build_query_key_local(const start_unit: Integer;
            const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (start_unit < 0) or (unit_count <= 0) or
                (start_unit + unit_count - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for unit_idx := start_unit to start_unit + unit_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    effective_syllables[unit_idx].text);
            end;
        end;

        function build_text_from_units_local(const units: TArray<string>;
            const start_unit: Integer; const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (start_unit < 0) or (unit_count <= 0) or
                (start_unit + unit_count - 1 > High(units)) then
            begin
                Exit;
            end;

            for unit_idx := start_unit to start_unit + unit_count - 1 do
            begin
                Result := Result + units[unit_idx];
            end;
        end;

        function replace_window_text_local(const units: TArray<string>;
            const start_unit: Integer; const unit_count: Integer;
            const replacement_text: string): string;
        var
            unit_idx: Integer;
            replacement_units: TArray<string>;
            replacement_idx: Integer;
        begin
            Result := '';
            replacement_units := split_text_units(Trim(replacement_text));
            if Length(replacement_units) <> unit_count then
            begin
                Exit;
            end;

            unit_idx := 0;
            while unit_idx <= High(units) do
            begin
                if unit_idx = start_unit then
                begin
                    for replacement_idx := 0 to High(replacement_units) do
                    begin
                        Result := Result + replacement_units[replacement_idx];
                    end;
                    Inc(unit_idx, unit_count - 1);
                end
                else
                begin
                    Result := Result + units[unit_idx];
                end;
                Inc(unit_idx);
            end;
        end;

        function get_supported_sentence_score_local(const candidate: TncCandidate;
            out support_score_local: Integer): Integer;
        var
            candidate_text_local: string;
            support_segments_local: Integer;
            support_path_local: string;
            exact_match_count_local: Integer;
            exact_mismatch_count_local: Integer;
            exact_strong_match_count_local: Integer;
            exact_strong_mismatch_count_local: Integer;
        begin
            Result := Low(Integer);
            support_score_local := -960;
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text_local, support_score_local,
                support_segments_local);
            if support_path_local = '' then
            begin
                support_score_local := -960;
            end;

            get_exact_window_consistency_stats_local(candidate_text_local, 2, 5,
                exact_match_count_local, exact_mismatch_count_local,
                exact_strong_match_count_local,
                exact_strong_mismatch_count_local);
            Result := (exact_match_count_local * 1024) +
                (exact_strong_match_count_local * 12288) -
                (exact_mismatch_count_local * 2048) -
                (exact_strong_mismatch_count_local * 24576) +
                Min(get_rank_score(candidate), 2048);
            if support_path_local <> '' then
            begin
                Inc(Result, (support_score_local * c_score_support_weight) +
                    Max(0, 3072 - (support_segments_local * 384)));
            end
            else
            begin
                Dec(Result, 2048);
            end;
            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                Dec(Result, c_problematic_penalty);
            end;
        end;

        procedure append_window_variant_local(var texts: TArray<string>;
            var weights: TArray<Integer>; var anchor_units: TArray<Integer>;
            const candidate_text: string; const candidate_weight: Integer;
            const candidate_anchor_units: Integer);
        var
            local_idx: Integer;
            insert_idx: Integer;
            swap_text: string;
            swap_weight: Integer;
            swap_anchor_units: Integer;
        begin
            if candidate_text = '' then
            begin
                Exit;
            end;

            for local_idx := 0 to High(texts) do
            begin
                if SameText(texts[local_idx], candidate_text) then
                begin
                    if candidate_weight <= weights[local_idx] then
                    begin
                        Exit;
                    end;
                    weights[local_idx] := candidate_weight;
                    anchor_units[local_idx] := candidate_anchor_units;
                    insert_idx := local_idx;
                    while (insert_idx > 0) and
                        ((weights[insert_idx] > weights[insert_idx - 1]) or
                        ((weights[insert_idx] = weights[insert_idx - 1]) and
                        (CompareText(texts[insert_idx],
                        texts[insert_idx - 1]) < 0))) do
                    begin
                        swap_text := texts[insert_idx - 1];
                        texts[insert_idx - 1] := texts[insert_idx];
                        texts[insert_idx] := swap_text;
                        swap_weight := weights[insert_idx - 1];
                        weights[insert_idx - 1] := weights[insert_idx];
                        weights[insert_idx] := swap_weight;
                        swap_anchor_units := anchor_units[insert_idx - 1];
                        anchor_units[insert_idx - 1] := anchor_units[insert_idx];
                        anchor_units[insert_idx] := swap_anchor_units;
                        Dec(insert_idx);
                    end;
                    Exit;
                end;
            end;

            SetLength(texts, Length(texts) + 1);
            SetLength(weights, Length(weights) + 1);
            SetLength(anchor_units, Length(anchor_units) + 1);
            insert_idx := High(texts);
            texts[insert_idx] := candidate_text;
            weights[insert_idx] := candidate_weight;
            anchor_units[insert_idx] := candidate_anchor_units;
            while (insert_idx > 0) and
                ((weights[insert_idx] > weights[insert_idx - 1]) or
                ((weights[insert_idx] = weights[insert_idx - 1]) and
                (CompareText(texts[insert_idx], texts[insert_idx - 1]) < 0))) do
            begin
                swap_text := texts[insert_idx - 1];
                texts[insert_idx - 1] := texts[insert_idx];
                texts[insert_idx] := swap_text;
                swap_weight := weights[insert_idx - 1];
                weights[insert_idx - 1] := weights[insert_idx];
                weights[insert_idx] := swap_weight;
                swap_anchor_units := anchor_units[insert_idx - 1];
                anchor_units[insert_idx - 1] := anchor_units[insert_idx];
                anchor_units[insert_idx] := swap_anchor_units;
                Dec(insert_idx);
            end;

            if Length(texts) > c_variant_limit then
            begin
                SetLength(texts, c_variant_limit);
                SetLength(weights, c_variant_limit);
                SetLength(anchor_units, c_variant_limit);
            end;
        end;

        function collect_window_variant_texts_local(const start_unit: Integer;
            const unit_count: Integer; out out_texts: TArray<string>;
            out out_weights: TArray<Integer>;
            out out_anchor_units: TArray<Integer>): Boolean;
        var
            cache_key: string;
            cache_entry: TWindowVariantCacheEntry;
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            resolved_text: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_path: string;
            resolved_has_anchor: Boolean;
            resolved_anchor_units: Integer;
            split_idx: Integer;
            left_texts: TArray<string>;
            left_weights: TArray<Integer>;
            left_anchor_units: TArray<Integer>;
            right_texts: TArray<string>;
            right_weights: TArray<Integer>;
            right_anchor_units: TArray<Integer>;
            left_idx: Integer;
            right_idx: Integer;

            function collect_direct_subspan_variants_local(
                const local_start_unit: Integer; const local_unit_count: Integer;
                const max_local_options: Integer; out local_texts: TArray<string>;
                out local_weights: TArray<Integer>;
                out local_anchor_units: TArray<Integer>): Boolean;
            var
                local_query_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                local_text: string;
                local_weight: Integer;
                local_resolved_text: string;
                local_resolved_score: Integer;
                local_resolved_segments: Integer;
                local_resolved_path: string;
                local_resolved_has_anchor: Boolean;
                local_resolved_anchor_units: Integer;
            begin
                SetLength(local_texts, 0);
                SetLength(local_weights, 0);
                SetLength(local_anchor_units, 0);
                if repair_budget_exceeded_local then
                begin
                    Exit(False);
                end;
                if (local_start_unit < 0) or (local_unit_count <= 0) then
                begin
                    Exit(False);
                end;
                Inc(repair_work_count);

                local_query_key := build_query_key_local(local_start_unit,
                    local_unit_count);
                if (local_query_key <> '') and
                    m_dictionary.lookup_exact_full_pinyin(local_query_key,
                    local_results) then
                begin
                    for local_idx := 0 to High(local_results) do
                    begin
                        if local_idx >= max_local_options then
                        begin
                            Break;
                        end;
                        if Trim(local_results[local_idx].comment) <> '' then
                        begin
                            Continue;
                        end;
                        local_text := Trim(local_results[local_idx].text);
                        if get_candidate_text_unit_count(local_text) <>
                            local_unit_count then
                        begin
                            Continue;
                        end;
                        local_weight := local_results[local_idx].score;
                        if local_results[local_idx].has_dict_weight and
                            (local_results[local_idx].dict_weight >
                            local_weight) then
                        begin
                            local_weight := local_results[local_idx].dict_weight;
                        end;
                        append_window_variant_local(local_texts, local_weights,
                            local_anchor_units, local_text, local_weight,
                            IfThen(local_unit_count >= 3, local_unit_count, 0));
                    end;
                end;

                if (local_unit_count >= 2) and
                    (not repair_budget_exceeded_local) and
                    try_resolve_best_exact_subspan_local(local_start_unit,
                    local_unit_count, local_resolved_text, local_resolved_score,
                    local_resolved_segments, local_resolved_path,
                    local_resolved_has_anchor, local_resolved_anchor_units) then
                begin
                    append_window_variant_local(local_texts, local_weights,
                        local_anchor_units, Trim(local_resolved_text),
                        local_resolved_score, local_resolved_anchor_units);
                end;
                Result := Length(local_texts) > 0;
            end;
        begin
            SetLength(out_texts, 0);
            SetLength(out_weights, 0);
            SetLength(out_anchor_units, 0);
            if repair_budget_exceeded_local then
            begin
                Exit(False);
            end;
            if (start_unit < 0) or (unit_count <= 0) then
            begin
                Exit(False);
            end;

            cache_key := IntToStr(start_unit) + ':' + IntToStr(unit_count);
            if (variant_cache <> nil) and
                variant_cache.TryGetValue(cache_key, cache_entry) then
            begin
                out_texts := Copy(cache_entry.texts);
                out_weights := Copy(cache_entry.weights);
                out_anchor_units := Copy(cache_entry.anchor_units);
                Exit(cache_entry.found);
            end;
            Inc(repair_work_count);

            query_key := build_query_key_local(start_unit, unit_count);
            if (query_key <> '') and
                m_dictionary.lookup_exact_full_pinyin(query_key, exact_results) then
            begin
                for exact_idx := 0 to High(exact_results) do
                begin
                    if exact_idx >= c_variant_exact_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results[exact_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(exact_results[exact_idx].text);
                    if get_candidate_text_unit_count(candidate_text_local) <>
                        unit_count then
                    begin
                        Continue;
                    end;
                    candidate_weight_local := exact_results[exact_idx].score;
                    if exact_results[exact_idx].has_dict_weight and
                        (exact_results[exact_idx].dict_weight >
                        candidate_weight_local) then
                    begin
                        candidate_weight_local :=
                            exact_results[exact_idx].dict_weight;
                    end;
                    append_window_variant_local(out_texts, out_weights,
                        out_anchor_units, candidate_text_local,
                        candidate_weight_local, IfThen(unit_count >= 3,
                        unit_count, 0));
                end;
            end;

            if (not repair_budget_exceeded_local) and
                try_resolve_best_exact_subspan_local(start_unit, unit_count,
                resolved_text, resolved_score, resolved_segments, resolved_path,
                resolved_has_anchor, resolved_anchor_units) then
            begin
                append_window_variant_local(out_texts, out_weights,
                    out_anchor_units, Trim(resolved_text), resolved_score,
                    resolved_anchor_units);
            end;

            if unit_count >= 3 then
            begin
                for split_idx := 1 to unit_count - 1 do
                begin
                    if repair_budget_exceeded_local then
                    begin
                        Break;
                    end;
                    if not collect_direct_subspan_variants_local(start_unit,
                        split_idx, 3, left_texts, left_weights,
                        left_anchor_units) then
                    begin
                        Continue;
                    end;
                    if not collect_direct_subspan_variants_local(
                        start_unit + split_idx, unit_count - split_idx, 3,
                        right_texts, right_weights, right_anchor_units) then
                    begin
                        Continue;
                    end;

                    for left_idx := 0 to High(left_texts) do
                    begin
                        for right_idx := 0 to High(right_texts) do
                        begin
                            append_window_variant_local(out_texts, out_weights,
                                out_anchor_units,
                                left_texts[left_idx] + right_texts[right_idx],
                                left_weights[left_idx] +
                                right_weights[right_idx] + 256,
                                Max(left_anchor_units[left_idx],
                                right_anchor_units[right_idx]));
                        end;
                    end;
                end;
            end;
            Result := Length(out_texts) > 0;
            if variant_cache <> nil then
            begin
                cache_entry.texts := Copy(out_texts);
                cache_entry.weights := Copy(out_weights);
                cache_entry.anchor_units := Copy(out_anchor_units);
                cache_entry.found := Result;
                variant_cache.AddOrSetValue(cache_key, cache_entry);
            end;
        end;

        function get_window_alignment_score_local(
            const candidate_text_local: string): Integer;
        var
            candidate_units_local: TArray<string>;
            start_idx_local: Integer;
            span_syllables_local: Integer;
            variant_texts_local: TArray<string>;
            variant_weights_local: TArray<Integer>;
            variant_anchor_units_local: TArray<Integer>;
            current_window_text_local: string;
            variant_idx_local: Integer;
            matched_idx_local: Integer;
        begin
            Result := Low(Integer);
            candidate_units_local := split_text_units(Trim(candidate_text_local));
            if Length(candidate_units_local) <> input_syllable_count then
            begin
                Exit;
            end;
            if repair_budget_exceeded_local then
            begin
                Exit;
            end;

            Result := 0;
            for start_idx_local := 0 to
                input_syllable_count - c_window_min_syllables do
            begin
                for span_syllables_local := c_window_min_syllables to
                    Min(c_window_max_syllables,
                    input_syllable_count - start_idx_local) do
                begin
                    if repair_budget_exceeded_local then
                    begin
                        Exit(Low(Integer));
                    end;
                    if not collect_window_variant_texts_local(start_idx_local,
                        span_syllables_local, variant_texts_local,
                        variant_weights_local, variant_anchor_units_local) then
                    begin
                        Continue;
                    end;

                    current_window_text_local := build_text_from_units_local(
                        candidate_units_local, start_idx_local,
                        span_syllables_local);
                    matched_idx_local := -1;
                    for variant_idx_local := 0 to High(variant_texts_local) do
                    begin
                        if SameText(current_window_text_local,
                            variant_texts_local[variant_idx_local]) then
                        begin
                            matched_idx_local := variant_idx_local;
                            Break;
                        end;
                    end;

                    case matched_idx_local of
                        0:
                            begin
                                Inc(Result, 4096 + span_syllables_local * 512);
                                if span_syllables_local >= 4 then
                                begin
                                    Inc(Result, 4096 + span_syllables_local * 256);
                                end;
                            end;
                        1:
                            begin
                                Inc(Result, 2048 + span_syllables_local * 256);
                                if span_syllables_local >= 4 then
                                begin
                                    Inc(Result, 1536 + span_syllables_local * 128);
                                end;
                            end;
                        2:
                            begin
                                Inc(Result, 1024 + span_syllables_local * 128);
                                if span_syllables_local >= 4 then
                                begin
                                    Inc(Result, 768 + span_syllables_local * 96);
                                end;
                            end;
                    else
                        if (Length(variant_texts_local) > 0) and
                            ((span_syllables_local >= 3) or
                            (variant_anchor_units_local[0] >= 3) or
                            (variant_weights_local[0] >= 1200)) then
                        begin
                            if span_syllables_local >= 4 then
                            begin
                                Dec(Result, 2048 + span_syllables_local * 256);
                            end
                            else if span_syllables_local = 3 then
                            begin
                                Dec(Result, 1536 + span_syllables_local * 160);
                            end
                            else
                            begin
                                Dec(Result, 384 + span_syllables_local * 64);
                            end;
                        end;
                    end;
                end;
            end;
        end;

        function get_local_repair_bonus_local(const unit_count: Integer;
            const candidate_weight: Integer;
            const candidate_anchor_units: Integer): Integer;
        begin
            Result := Min(8192, Max(768, candidate_weight div 2)) +
                (unit_count * 320);
            if candidate_anchor_units >= 3 then
            begin
                Inc(Result, 1024);
            end;
        end;

        function try_find_best_repair_for_candidate_local(
            const seed_candidate: TncCandidate; out out_candidate: TncCandidate;
            out out_score: Integer): Boolean;
        var
            seed_text: string;
            seed_units: TArray<string>;
            seed_score: Integer;
            start_idx: Integer;
            span_syllables: Integer;
            current_window_text: string;
            repaired_window_text: string;
            repaired_text: string;
            variant_texts: TArray<string>;
            variant_weights: TArray<Integer>;
            variant_anchor_units: TArray<Integer>;
            variant_idx: Integer;
            repaired_support_score: Integer;
            repaired_support_segments: Integer;
            repaired_support_path: string;
            local_candidate_local: TncCandidate;
            local_best_score: Integer;
            alignment_score_local: Integer;
        begin
            Result := False;
            out_score := Low(Integer);
            FillChar(out_candidate, SizeOf(out_candidate), 0);
            if repair_budget_exceeded_local then
            begin
                Exit;
            end;

            seed_text := Trim(seed_candidate.text);
            seed_units := split_text_units(seed_text);
            if Length(seed_units) <> input_syllable_count then
            begin
                Exit;
            end;

            seed_score := get_supported_sentence_score_local(seed_candidate,
                repaired_support_score);
            alignment_score_local := get_window_alignment_score_local(seed_text);
            if alignment_score_local <> Low(Integer) then
            begin
                Inc(seed_score, alignment_score_local * 8);
            end;
            local_best_score := seed_score;

            for start_idx := 0 to input_syllable_count - c_window_min_syllables do
            begin
                for span_syllables := c_window_min_syllables to
                    Min(c_window_max_syllables,
                    input_syllable_count - start_idx) do
                begin
                    if repair_budget_exceeded_local then
                    begin
                        Exit;
                    end;
                    if not collect_window_variant_texts_local(start_idx,
                        span_syllables, variant_texts, variant_weights,
                        variant_anchor_units) then
                    begin
                        Continue;
                    end;

                    current_window_text := build_text_from_units_local(
                        seed_units, start_idx, span_syllables);
                    for variant_idx := 0 to High(variant_texts) do
                    begin
                        repaired_window_text := Trim(variant_texts[variant_idx]);
                        if (repaired_window_text = '') or SameText(
                            repaired_window_text, current_window_text) then
                        begin
                            Continue;
                        end;

                        repaired_text := replace_window_text_local(seed_units,
                            start_idx, span_syllables, repaired_window_text);
                        if (repaired_text = '') or SameText(repaired_text,
                            seed_text) then
                        begin
                            Continue;
                        end;

                        FillChar(local_candidate_local,
                            SizeOf(local_candidate_local), 0);
                        local_candidate_local.text := repaired_text;
                        local_candidate_local.comment := '';
                        local_candidate_local.score := seed_candidate.score +
                            1024 + Min(4096, Max(512,
                            variant_weights[variant_idx] div 8));
                        local_candidate_local.source := cs_rule;
                        local_candidate_local.has_dict_weight := False;
                        local_candidate_local.dict_weight := 0;

                        out_score := get_supported_sentence_score_local(
                            local_candidate_local, repaired_support_score);
                        alignment_score_local :=
                            get_window_alignment_score_local(repaired_text);
                        repaired_support_path :=
                            infer_segment_path_for_query_text_with_score(
                            lookup_text, repaired_text, repaired_support_score,
                            repaired_support_segments);
                        if repaired_support_path = '' then
                        begin
                            Dec(out_score, 4096);
                        end;

                        if alignment_score_local <> Low(Integer) then
                        begin
                            Inc(out_score, alignment_score_local * 8);
                        end;

                        Inc(out_score, get_local_repair_bonus_local(
                            span_syllables, variant_weights[variant_idx],
                            variant_anchor_units[variant_idx]));
                        if out_score <= local_best_score +
                            get_support_margin_local then
                        begin
                            Continue;
                        end;

                        out_candidate := local_candidate_local;
                        local_best_score := out_score;
                        Result := True;
                    end;
                end;
            end;

            if Result then
            begin
                out_score := local_best_score;
            end;
        end;
    begin
        repair_start_tick := nc_monotonic_tick_ms;
        repair_work_count := 0;
        repair_budget_logged := False;
        variant_cache := nil;
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        if has_strong_top_complete_candidate_for_postprocess_lock_local(
            candidates) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' vrepair=[locked]';
            end;
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        variant_cache := TDictionary<string, TWindowVariantCacheEntry>.Create;
        try
        if (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            top_score := get_supported_sentence_score_local(candidates[0],
                local_score);
        end
        else
        begin
            top_score := Low(Integer);
        end;

        best_score := top_score;
        FillChar(repaired_candidate, SizeOf(repaired_candidate), 0);
        for probe_idx := 0 to Min(c_probe_limit - 1, High(candidates)) do
        begin
            if not try_find_best_repair_for_candidate_local(candidates[probe_idx],
                local_candidate, local_score) then
            begin
                Continue;
            end;
            if local_score <= best_score +
                IfThen(input_syllable_count <= 4, 1024,
                IfThen(input_syllable_count <= 6, 2048,
                c_promote_score_margin)) then
            begin
                Continue;
            end;

            repaired_candidate := local_candidate;
            best_score := local_score;
        end;

        if Trim(repaired_candidate.text) = '' then
        begin
            Exit;
        end;

        for pass_idx := 2 to c_repair_pass_limit do
        begin
            if not try_find_best_repair_for_candidate_local(repaired_candidate,
                local_candidate, local_score) then
            begin
                Break;
            end;
            if local_score <= best_score + get_support_margin_local then
            begin
                Break;
            end;

            repaired_candidate := local_candidate;
            best_score := local_score;
        end;

        SetLength(candidates, Length(candidates) + 1);
        for move_idx := High(candidates) downto 1 do
        begin
            candidates[move_idx] := candidates[move_idx - 1];
        end;
        candidates[0] := repaired_candidate;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vrepair=[promote text=%s score=%d]',
                [Trim(repaired_candidate.text), best_score]);
        end;
        finally
            variant_cache.Free;
        end;
    end;

    procedure ensure_best_oracle_backed_exact_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_visible_probe_limit = 8;
        c_exact_probe_limit = 8;
        c_oracle_rank_bonus_base = 18000;
        c_oracle_rank_bonus_step = 4000;
        c_oracle_score_scale = 3;
        c_support_scale = 1;
        c_support_segment_bonus = 320;
        c_no_support_penalty = 4096;
        c_problematic_penalty = 24000;
        c_oracle_top_exact_bonus = 24000;
        c_oracle_near_top_exact_bonus = 12000;
        c_non_oracle_exact_penalty = 12000;
    var
        exact_results: TncCandidateList;
        oracle_text_scores: TDictionary<string, Integer>;
        oracle_text_ranks: TDictionary<string, Integer>;
        visible_texts: TDictionary<string, Byte>;
        idx: Integer;
        exact_idx: Integer;
        existing_index: Integer;
        top_score: Integer;
        best_score: Integer;
        candidate_score: Integer;
        top_is_complete: Boolean;
        picked: TncCandidate;
        top_text: string;
        candidate_text: string;
        support_score_local: Integer;
        support_segments_local: Integer;
        support_path_local: string;
        oracle_score_local: Integer;
        oracle_rank_local: Integer;
        exact_candidate: TncCandidate;
        promote_margin: Integer;

        function get_candidate_total_score_local(
            const candidate: TncCandidate): Integer;
        var
            local_text: string;
            local_support_score: Integer;
            local_support_segments: Integer;
            local_support_path: string;
            local_oracle_score: Integer;
            local_oracle_rank: Integer;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            local_text := Trim(candidate.text);
            if (local_text = '') or
                (get_candidate_text_unit_count(local_text) <> input_syllable_count) then
            begin
                Exit;
            end;

            Result := Min(get_rank_score(candidate), 32768);
            local_support_path := infer_segment_path_for_query_text_with_score(
                lookup_text, local_text, local_support_score,
                local_support_segments);
            if local_support_path <> '' then
            begin
                Inc(Result, local_support_score * c_support_scale);
                Inc(Result, local_support_segments * c_support_segment_bonus);
            end
            else
            begin
                Dec(Result, c_no_support_penalty);
            end;

            if oracle_text_scores.TryGetValue(local_text, local_oracle_score) then
            begin
                Inc(Result, local_oracle_score * c_oracle_score_scale);
                if oracle_text_ranks.TryGetValue(local_text, local_oracle_rank) then
                begin
                    Inc(Result, Max(0, c_oracle_rank_bonus_base -
                        (local_oracle_rank * c_oracle_rank_bonus_step)));
                    if local_oracle_rank = 0 then
                    begin
                        Inc(Result, c_oracle_top_exact_bonus);
                    end
                    else if local_oracle_rank = 1 then
                    begin
                        Inc(Result, c_oracle_near_top_exact_bonus);
                    end;
                end;
            end;
            if (Length(exact_results) > 0) and
                (not oracle_text_scores.ContainsKey(local_text)) then
            begin
                Dec(Result, c_non_oracle_exact_penalty);
            end;

            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate) then
            begin
                Dec(Result, c_problematic_penalty);
            end;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 3) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' voracle=[skip pre len=%d syll=%d fp=%d dict=%d]',
                    [Length(candidates), input_syllable_count,
                    Ord(is_full_pinyin_key(lookup_text)),
                    Ord(m_dictionary <> nil)]);
            end;
            Exit;
        end;

        if input_syllable_count <= 4 then
        begin
            promote_margin := 256;
        end
        else if input_syllable_count <= 7 then
        begin
            promote_margin := 512;
        end
        else
        begin
            promote_margin := 2048;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' voracle=[enter]';
        end;
        ensure_strict_oracle_sentence_candidates_prepared;
        if not strict_oracle_sentence_ok then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' voracle=[skip oracle]';
            end;
            Exit;
        end;
        if not m_dictionary.lookup_exact_full_pinyin(lookup_text, exact_results) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' voracle=[no exact]';
            end;
            Exit;
        end;
        if Length(exact_results) = 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' voracle=[empty exact]';
            end;
            Exit;
        end;

        oracle_text_scores := TDictionary<string, Integer>.Create;
        oracle_text_ranks := TDictionary<string, Integer>.Create;
        visible_texts := TDictionary<string, Byte>.Create;
        try
            for idx := 0 to Min(c_visible_probe_limit - 1, High(candidates)) do
            begin
                candidate_text := Trim(candidates[idx].text);
                if candidate_text <> '' then
                begin
                    visible_texts.AddOrSetValue(candidate_text, 1);
                end;
            end;

            for idx := 0 to Min(c_exact_probe_limit - 1,
                High(strict_oracle_sentence_candidates)) do
            begin
                candidate_text := Trim(strict_oracle_sentence_candidates[idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;
                oracle_text_scores.AddOrSetValue(candidate_text,
                    strict_oracle_sentence_candidates[idx].score);
                oracle_text_ranks.AddOrSetValue(candidate_text, idx);
            end;

            top_is_complete := (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count);
            top_text := Trim(candidates[0].text);
            if top_is_complete then
            begin
                top_score := get_candidate_total_score_local(candidates[0]);
                best_score := top_score;
                picked := candidates[0];
            end
            else
            begin
                top_score := Low(Integer);
                best_score := Low(Integer);
                FillChar(picked, SizeOf(picked), 0);
            end;

            for idx := 0 to Min(c_visible_probe_limit - 1, High(candidates)) do
            begin
                candidate_score := get_candidate_total_score_local(candidates[idx]);
                if candidate_score > best_score then
                begin
                    best_score := candidate_score;
                    picked := candidates[idx];
                end;
            end;

            for exact_idx := 0 to Min(c_exact_probe_limit - 1, High(exact_results)) do
            begin
                exact_candidate := exact_results[exact_idx];
                if Trim(exact_candidate.comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_candidate.text);
                if (candidate_text = '') or
                    (get_candidate_text_unit_count(candidate_text) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;
                if visible_texts.ContainsKey(candidate_text) then
                begin
                    Continue;
                end;

                candidate_score := get_candidate_total_score_local(exact_candidate);
                if candidate_score > best_score then
                begin
                    best_score := candidate_score;
                    picked := exact_candidate;
                end;
            end;

            if (best_score = Low(Integer)) or
                ((top_score <> Low(Integer)) and
                (best_score <= top_score + promote_margin)) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' voracle=[keep top=%s ts=%d best=%s bs=%d]',
                        [top_text, top_score, Trim(picked.text), best_score]);
                end;
                Exit;
            end;

            existing_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), Trim(picked.text)) then
                begin
                    existing_index := idx;
                    Break;
                end;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(picked.text), support_score_local,
                support_segments_local);
            if support_path_local <> '' then
            begin
                remember_segment_path_for_candidate(Trim(picked.text), '',
                    support_path_local, picked.score + Min(2400,
                    support_score_local div 4));
            end;

            if existing_index > 0 then
            begin
                for idx := existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
            end
            else if existing_index < 0 then
            begin
                SetLength(candidates, Length(candidates) + 1);
                for idx := High(candidates) downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
                candidates[0] := picked;
            end;

            if m_config.debug_mode then
            begin
                oracle_score_local := 0;
                oracle_rank_local := -1;
                oracle_text_scores.TryGetValue(Trim(picked.text), oracle_score_local);
                oracle_text_ranks.TryGetValue(Trim(picked.text), oracle_rank_local);
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' voracle=[promote text=%s score=%d top=%d os=%d or=%d]',
                    [Trim(picked.text), best_score, top_score, oracle_score_local,
                    oracle_rank_local]);
            end;
        finally
            visible_texts.Free;
            oracle_text_ranks.Free;
            oracle_text_scores.Free;
        end;
    end;

    procedure ensure_strict_oracle_exact_top_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_oracle_probe_limit = 6;
        c_exact_probe_limit = 12;
    var
        exact_results: TncCandidateList;
        oracle_text: string;
        top_text: string;
        idx: Integer;
        exact_idx: Integer;
        existing_index: Integer;
        picked: TncCandidate;
        top_is_complete: Boolean;
        top_is_exact: Boolean;

        function is_exact_complete_text_local(const text_value: string): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if text_value = '' then
            begin
                Exit;
            end;
            for local_idx := 0 to Min(c_exact_probe_limit - 1, High(exact_results)) do
            begin
                if Trim(exact_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(exact_results[local_idx].text), text_value) and
                    (get_candidate_text_unit_count(Trim(exact_results[local_idx].text)) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        ensure_strict_oracle_sentence_candidates_prepared;
        if (not strict_oracle_sentence_ok) or
            (Length(strict_oracle_sentence_candidates) = 0) then
        begin
            Exit;
        end;
        if not m_dictionary.lookup_exact_full_pinyin(lookup_text, exact_results) then
        begin
            Exit;
        end;

        oracle_text := '';
        for idx := 0 to Min(c_oracle_probe_limit - 1,
            High(strict_oracle_sentence_candidates)) do
        begin
            if Trim(strict_oracle_sentence_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(
                Trim(strict_oracle_sentence_candidates[idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            if is_exact_complete_text_local(
                Trim(strict_oracle_sentence_candidates[idx].text)) then
            begin
                oracle_text := Trim(strict_oracle_sentence_candidates[idx].text);
                Break;
            end;
        end;
        if oracle_text = '' then
        begin
            Exit;
        end;

        top_text := Trim(candidates[0].text);
        top_is_complete := (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count);
        top_is_exact := top_is_complete and is_exact_complete_text_local(top_text);
        if SameText(top_text, oracle_text) or top_is_exact then
        begin
            Exit;
        end;

        if (not top_is_complete) and
            (not is_problematic_single_char_chain_candidate_for_query(
            lookup_text, candidates[0])) then
        begin
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), oracle_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end
        else if existing_index < 0 then
        begin
            for exact_idx := 0 to Min(c_exact_probe_limit - 1, High(exact_results)) do
            begin
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(exact_results[exact_idx].text), oracle_text) then
                begin
                    picked := exact_results[exact_idx];
                    SetLength(candidates, Length(candidates) + 1);
                    for idx := High(candidates) downto 1 do
                    begin
                        candidates[idx] := candidates[idx - 1];
                    end;
                    candidates[0] := picked;
                    Break;
                end;
            end;
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' voraclex=[promote text=%s top=%s]', [oracle_text, top_text]);
        end;
    end;

    function ensure_visible_strict_oracle_top_complete_candidate_visible(
        var candidates: TncCandidateList): Boolean;
    const
        c_oracle_probe_limit = 6;
    var
        oracle_text: string;
        idx: Integer;
        existing_index: Integer;
        picked: TncCandidate;
        exact_results_local: TncCandidateList;
    begin
        Result := False;
        if (Length(candidates) = 0) or (input_syllable_count < 10) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        if not m_dictionary.lookup_exact_full_pinyin(lookup_text,
            exact_results_local) then
        begin
            Exit;
        end;
        if Length(exact_results_local) = 0 then
        begin
            Exit;
        end;

        ensure_strict_oracle_sentence_candidates_prepared;
        if (not strict_oracle_sentence_ok) or
            (Length(strict_oracle_sentence_candidates) = 0) then
        begin
            Exit;
        end;

        oracle_text := '';
        for idx := 0 to Min(c_oracle_probe_limit - 1,
            High(strict_oracle_sentence_candidates)) do
        begin
            if Trim(strict_oracle_sentence_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(
                Trim(strict_oracle_sentence_candidates[idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            oracle_text := Trim(strict_oracle_sentence_candidates[idx].text);
            Break;
        end;
        if oracle_text = '' then
        begin
            Exit;
        end;

        if SameText(Trim(candidates[0].text), oracle_text) and
            (Trim(candidates[0].comment) = '') then
        begin
            Exit(True);
        end;

        existing_index := -1;
        for idx := 1 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), oracle_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;
        if existing_index <= 0 then
        begin
            Exit(False);
        end;

        picked := candidates[existing_index];
        for idx := existing_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vtoporacle=[promote text=%s]', [Trim(picked.text)]);
        end;
        Result := True;
    end;

    function ensure_strict_oracle_complete_candidate_visible(
        var candidates: TncCandidateList): Boolean;
    const
        c_oracle_probe_limit = 6;
    var
        oracle_text: string;
        top_text: string;
        oracle_path: string;
        top_path: string;
        oracle_support_score: Integer;
        top_support_score: Integer;
        oracle_support_segments: Integer;
        top_support_segments: Integer;
        oracle_idx: Integer;
        idx: Integer;
        existing_index: Integer;
        picked: TncCandidate;
        top_is_complete: Boolean;
        top_problematic: Boolean;
        should_promote: Boolean;
    begin
        Result := False;
        if (Length(candidates) = 0) or (input_syllable_count < 10) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        ensure_strict_oracle_sentence_candidates_prepared;
        if (not strict_oracle_sentence_ok) or
            (Length(strict_oracle_sentence_candidates) = 0) then
        begin
            Exit;
        end;

        oracle_text := '';
        oracle_idx := -1;
        for idx := 0 to Min(c_oracle_probe_limit - 1,
            High(strict_oracle_sentence_candidates)) do
        begin
            if Trim(strict_oracle_sentence_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(
                Trim(strict_oracle_sentence_candidates[idx].text)) <>
                input_syllable_count then
            begin
                Continue;
            end;
            oracle_text := Trim(strict_oracle_sentence_candidates[idx].text);
            oracle_idx := idx;
            Break;
        end;
        if oracle_text = '' then
        begin
            Exit;
        end;

        top_text := Trim(candidates[0].text);
        if SameText(top_text, oracle_text) and
            (Trim(candidates[0].comment) = '') then
        begin
            Exit(True);
        end;

        top_is_complete := (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(top_text) = input_syllable_count);
        top_problematic := (not top_is_complete) or
            is_problematic_single_char_chain_candidate_for_query(lookup_text,
            candidates[0]);

        oracle_support_score := Low(Integer);
        oracle_support_segments := 0;
        oracle_path := infer_segment_path_for_query_text_with_score(lookup_text,
            oracle_text, oracle_support_score, oracle_support_segments);
        top_support_score := Low(Integer);
        top_support_segments := 0;
        top_path := '';
        if top_is_complete then
        begin
            top_path := infer_segment_path_for_query_text_with_score(lookup_text,
                top_text, top_support_score, top_support_segments);
        end;

        should_promote := top_problematic or (top_path = '');
        if (not should_promote) and (oracle_path <> '') then
        begin
            should_promote := (top_support_score = Low(Integer)) or
                (oracle_support_score >= top_support_score + 2048) or
                ((input_syllable_count >= 10) and
                (oracle_support_segments > top_support_segments) and
                (oracle_support_score + 1024 >= top_support_score));
        end;
        if not should_promote then
        begin
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), oracle_text) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end
        else if existing_index < 0 then
        begin
            picked := strict_oracle_sentence_candidates[oracle_idx];
            picked.comment := '';
            if get_rank_score(picked) < get_rank_score(candidates[0]) + 1024 then
            begin
                picked.score := candidates[0].score + 1024;
            end;
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end;

        if oracle_path <> '' then
        begin
            remember_segment_path_for_candidate(oracle_text, '', oracle_path,
                candidates[0].score + Min(2400, Max(0,
                oracle_support_score div 4)));
        end;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vstrictoracle=[promote text=%s top=%s os=%d ts=%d]',
                [oracle_text, top_text, oracle_support_score,
                top_support_score]);
        end;
        Result := True;
    end;

    procedure ensure_best_exact_chunk_chain_complete_candidate_visible(
        var candidates: TncCandidateList;
        const force_fast_validation: Boolean = False;
        const allow_fast_repair: Boolean = True;
        const allow_incremental_tail_repair: Boolean = False);
    const
        c_chain_probe_limit = 8;
        c_fast_visible_probe_limit = 24;
        c_fast_visible_metric_threshold = 14000;
        c_chain_max_chunk_syllables = 6;
        c_chain_min_anchor_syllables = 2;
        c_chain_length_bonus = 2000;
        c_chain_anchor_bonus = 3200;
        c_chain_lattice_max_chunk_syllables = 8;
        c_chain_lattice_baseline_beam_width = 6;
        c_chain_lattice_beam_width = 8;
        c_chain_lattice_baseline_prebeam_width = 12;
        c_chain_lattice_baseline_long_prebeam_width = 20;
        c_chain_lattice_prebeam_width = 16;
        c_chain_lattice_long_prebeam_width = 28;
        c_chain_lattice_long_prebeam_min_syllables = 13;
        c_chain_lattice_lm_reserved_slots = 1;
        c_chain_lattice_exact_edge_reserved_slots = 0;
        c_chain_lattice_exact_edge_side_lane_enabled = False;
        c_chain_lattice_exact_edge_min_units = 3;
        c_chain_lattice_exact_edge_min_model_score = 2.5;
        c_chain_lattice_exact_edge_probe_limit = 8;
        c_chain_lattice_lm_min_bonus = 360;
        c_chain_lattice_lm_min_gain = 120;
        c_chain_lattice_lm_max_base_gap = 90000;
        c_chain_lattice_probe_limit = 3;
        c_chain_lattice_merge_bonus = 512;
        c_chain_lattice_fixed_single_bonus = 768;
        c_chain_standalone_option_limit = 6;
        c_chain_lattice_option_limit = 4;
        c_chain_option_probe_limit = 16;
        c_chain_search_status_guard_rejected = 1;
        c_chain_search_status_short_query = 2;
        c_chain_search_status_trusted_complete_exit = 3;
        c_chain_search_status_visible_complete_exit = 4;
        c_chain_search_status_cached_miss_exit = 5;
        c_chain_search_status_syllable_mismatch = 6;
        c_chain_search_status_fast_search = 7;
        c_chain_search_status_ultra_long_fast_miss = 8;
        c_chain_search_status_fallback_search = 9;
        c_incremental_fast_direct_min_gap = 256;
    type
        TExactChunkChainState = record
            text: string;
            path: string;
            first_chunk_text: string;
            first_chunk_query_key: string;
            first_chunk_exact_rank: Integer;
            first_chunk_raw_weight: Integer;
            first_chunk_top_weight: Integer;
            first_chunk_effective_score: Integer;
            first_chunk_edge_score: Int64;
            first_chunk_exact_edge_model_score: Double;
            first_chunk_source_user: Boolean;
            first_chunk_baseline_option: Boolean;
            score: Integer;
            segments: Integer;
            has_anchor: Boolean;
            anchor_units: Integer;
            baseline_lineage: Boolean;
            retained_exact_edge_count: Integer;
            retained_exact_edge_position: Integer;
            retained_exact_edge_units: Integer;
            retained_exact_edge_rank: Integer;
            retained_exact_edge_top_weight: Integer;
            retained_exact_edge_confidence: Int64;
            ranker_original_rank: Integer;
            ranker_first_stage_score: Integer;
            ranker_second_stage_score: Int64;
            ranker_model_ordered: Boolean;
            ranker_features_valid: Boolean;
            ranker_feature_context_key: string;
            ranker_features: TncLongSecondStageFeatures;
        end;
        TExactChunkChainStateArray = TArray<TExactChunkChainState>;
        TLongWordLmBoundaryStats = record
            boundary_count: Integer;
            min_bonus: Integer;
            max_bonus: Integer;
            first_bonus: Integer;
            last_bonus: Integer;
            supported_count: Integer;
            strong_count: Integer;
            trigram_count: Integer;
            zero_count: Integer;
        end;
        TExactChunkChainOption = record
            text: string;
            path: string;
            query_key: string;
            score: Integer;
            exact_rank: Integer;
            raw_weight: Integer;
            top_weight: Integer;
            edge_retention_score: Int64;
            edge_retention_features: TncLongEdgeRetentionFeatures;
            exact_edge_model_score: Double;
            source_user: Boolean;
            direct_exact: Boolean;
            edge_expanded_only: Boolean;
            baseline_option: Boolean;
            segments: Integer;
            has_anchor: Boolean;
            anchor_units: Integer;
        end;
        TExactChunkChainOptionArray = TArray<TExactChunkChainOption>;
    var
        effective_syllables: TncPinyinParseResult;
        best_scores: TArray<Integer>;
        best_texts: TArray<string>;
        best_paths: TArray<string>;
        best_segments: TArray<Integer>;
        best_has_anchor: TArray<Boolean>;
        best_anchor_units: TArray<Integer>;
        syllable_count_local: Integer;
        idx: Integer;
        existing_index: Integer;
        best_visible_index: Integer;
        picked: TncCandidate;
        top_path_segment_count: Integer;
        should_promote: Boolean;
        preferred_phrase_bonus: Integer;
        top_preferred_phrase_bonus: Integer;
        best_visible_metric: Integer;
        current_metric_local: Integer;
        xchain_metric_local: Integer;
        current_support_local: Integer;
        current_path_preference_local: Integer;
        support_path_local: string;
        current_candidate_text_local: string;
        best_visible_problematic_local: Boolean;
        xchain_problematic_local: Boolean;
        problematic_candidate_local: TncCandidate;
        standalone_chunk_states: TDictionary<string, Integer>;
        standalone_chunk_texts: TDictionary<string, string>;
        standalone_chunk_scores: TDictionary<string, Integer>;
        standalone_chunk_options: TDictionary<string, TExactChunkChainOptionArray>;
        standalone_chunk_engine: TncEngine;
        standalone_chunk_config: TncEngineConfig;
        standalone_chunk_budget_remaining: Integer;
        lattice_state_map: array of TExactChunkChainStateArray;
        lattice_text: string;
        lattice_path: string;
        lattice_score: Integer;
        lattice_segments: Integer;
        lattice_has_anchor: Boolean;
        lattice_anchor_units: Integer;
        fast_text: string;
        fast_path: string;
        fast_score: Integer;
        fast_segments: Integer;
        fast_states: TExactChunkChainStateArray;
        fast_state_idx: Integer;
        fast_state_to_upsert: TExactChunkChainState;
        fast_repaired_state: TExactChunkChainState;
        fast_repair_inserted: Boolean;
        fast_model_order_active: Boolean;
        fast_build_ok: Boolean;
        fast_phase_tick: UInt64;
        suppress_subspan_oracle_local: Boolean;
        use_direct_chunk_lattice_only: Boolean;
        runtime_exact_edge_probe_collection_local: Boolean;

        function use_exact_edge_ranker_expansion_local: Boolean;
        begin
            Result := m_debug_expand_long_beam_states or
                runtime_exact_edge_probe_collection_local;
        end;

        function get_chain_beam_width_local: Integer;
        begin
            Result := m_debug_long_chain_beam_width;
            if Result <= 0 then
            begin
                Result := c_chain_lattice_beam_width;
            end;
        end;

        procedure record_chain_search_status_local(const status_value: Integer);
        begin
            if m_debug_target_recall_text <> '' then
            begin
                m_debug_target_chain_search_status := status_value;
            end;
        end;

        function get_chain_prebeam_width_local: Integer;
        begin
            Result := m_debug_long_chain_prebeam_width;
            if Result <= 0 then
            begin
                if syllable_count_local >=
                    c_chain_lattice_long_prebeam_min_syllables then
                begin
                    Result := c_chain_lattice_long_prebeam_width;
                end
                else
                begin
                    Result := c_chain_lattice_prebeam_width;
                end;
            end;
            Result := Max(Result, get_chain_beam_width_local);
        end;

        function get_chain_baseline_prebeam_width_local: Integer;
        begin
            if syllable_count_local >=
                c_chain_lattice_long_prebeam_min_syllables then
            begin
                Result := c_chain_lattice_baseline_long_prebeam_width;
            end
            else
            begin
                Result := c_chain_lattice_baseline_prebeam_width;
            end;
            Result := Min(Result, get_chain_prebeam_width_local);
            Result := Max(Result, Min(c_chain_lattice_baseline_beam_width,
                get_chain_beam_width_local));
        end;

        function get_chain_char_base_slots_local: Integer;
        begin
            // Let the character LM choose every retained long-chain state.
            // Base-ranked states still participate in the same pool, but no
            // slot is reserved before the LM can compare complete prefixes.
            Result := 0;
        end;

        function get_chain_option_limit_local: Integer;
        begin
            Result := m_debug_long_chain_option_limit;
            if Result <= 0 then
            begin
                Result := c_chain_lattice_option_limit;
                if syllable_count_local >=
                    c_chain_lattice_long_prebeam_min_syllables then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function get_chain_probe_limit_local: Integer;
        begin
            Result := m_debug_long_chain_probe_limit;
            if Result <= 0 then
            begin
                Result := c_chain_option_probe_limit;
            end;
            Result := Max(Result, get_chain_option_limit_local);
        end;

        function try_fast_visible_complete_exit_local: Boolean;
        var
            local_idx: Integer;
            best_idx_local: Integer;
            best_metric_local: Integer;
            top_metric_local: Integer;
            current_metric_local: Integer;
            local_support_score: Integer;
            local_path_preference: Integer;
            local_support_path: string;
            local_candidate_text: string;
            local_problematic_candidate: TncCandidate;
            best_path_local: string;
            picked_local: TncCandidate;
            top_is_complete_local: Boolean;

            function is_place_context_unit_for_fast_visible_local(
                const unit_value_local: string): Boolean;
            begin
                Result := (unit_value_local = string(Char($5BB6))) or
                    (unit_value_local = string(Char($5E97))) or
                    (unit_value_local = string(Char($5C4B))) or
                    (unit_value_local = string(Char($697C))) or
                    (unit_value_local = string(Char($57CE))) or
                    (unit_value_local = string(Char($6751))) or
                    (unit_value_local = string(Char($53BF))) or
                    (unit_value_local = string(Char($5E02))) or
                    (unit_value_local = string(Char($6821))) or
                    (unit_value_local = string(Char($9662))) or
                    (unit_value_local = string(Char($5382))) or
                    (unit_value_local = string(Char($9986))) or
                    (unit_value_local = string(Char($7AD9))) or
                    (unit_value_local = string(Char($5C40))) or
                    (unit_value_local = string(Char($6240))) or
                    (unit_value_local = string(Char($56ED))) or
                    (unit_value_local = string(Char($533A))) or
                    (unit_value_local = string(Char($5708))) or
                    (unit_value_local = string(Char($8DEF))) or
                    (unit_value_local = string(Char($8857))) or
                    (unit_value_local = string(Char($95E8))) or
                    (unit_value_local = string(Char($5BA4))) or
                    (unit_value_local = string(Char($623F))) or
                    (unit_value_local = string(Char($5385))) or
                    (unit_value_local = string(Char($573A))) or
                    (unit_value_local = string(Char($53F8)));
            end;

            function is_directional_location_phrase_for_fast_visible_local(
                const text_value_local: string): Boolean;
            var
                units_local: TArray<string>;
                first_unit_local: string;
                second_unit_local: string;
            begin
                Result := False;
                units_local := split_text_units(Trim(text_value_local));
                if Length(units_local) <> 2 then
                begin
                    Exit;
                end;

                first_unit_local := units_local[0];
                second_unit_local := units_local[1];
                if not (SameText(first_unit_local, string(Char($5317))) or
                    SameText(first_unit_local, string(Char($5357))) or
                    SameText(first_unit_local, string(Char($4E1C))) or
                    SameText(first_unit_local, string(Char($897F))) or
                    SameText(first_unit_local, string(Char($524D))) or
                    SameText(first_unit_local, string(Char($540E))) or
                    SameText(first_unit_local, string(Char($4E0A))) or
                    SameText(first_unit_local, string(Char($4E0B))) or
                    SameText(first_unit_local, string(Char($91CC))) or
                    SameText(first_unit_local, string(Char($5185))) or
                    SameText(first_unit_local, string(Char($5916))) or
                    SameText(first_unit_local, string(Char($5DE6))) or
                    SameText(first_unit_local, string(Char($53F3)))) then
                begin
                    Exit;
                end;

                Result := SameText(second_unit_local, string(Char($9762))) or
                    SameText(second_unit_local, string(Char($8FB9))) or
                    SameText(second_unit_local, string(Char($4FA7))) or
                    SameText(second_unit_local, string(Char($65B9))) or
                    SameText(second_unit_local, string(Char($90E8)));
            end;

            function get_directional_context_metric_bonus_local(
                const text_value_local: string): Integer;
            const
                c_directional_context_metric_bonus = 18000;
            var
                units_local: TArray<string>;
                syllables_local: TncPinyinParseResult;
                start_idx_local: Integer;
                span_text_local: string;
                next_key_local: string;
            begin
                Result := 0;
                units_local := split_text_units(Trim(text_value_local));
                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (Length(units_local) <> input_syllable_count) or
                    (Length(syllables_local) <> input_syllable_count) then
                begin
                    Exit;
                end;

                for start_idx_local := 1 to input_syllable_count - 3 do
                begin
                    if not is_place_context_unit_for_fast_visible_local(
                        units_local[start_idx_local - 1]) then
                    begin
                        Continue;
                    end;
                    next_key_local := normalize_pinyin_text(
                        syllables_local[start_idx_local + 2].text);
                    if not SameText(next_key_local, 'de') then
                    begin
                        Continue;
                    end;
                    span_text_local := units_local[start_idx_local] +
                        units_local[start_idx_local + 1];
                    if is_directional_location_phrase_for_fast_visible_local(
                        span_text_local) then
                    begin
                        Exit(c_directional_context_metric_bonus);
                    end;
                end;
            end;

            function is_quantity_tail_key_fast_visible_local(
                const key_local: string): Boolean;
            begin
                Result := SameText(key_local, 'ci') or
                    SameText(key_local, 'ge') or SameText(key_local, 'tiao') or
                    SameText(key_local, 'zhang') or SameText(key_local, 'ben') or
                    SameText(key_local, 'pian') or SameText(key_local, 'bu') or
                    SameText(key_local, 'lun') or SameText(key_local, 'hui') or
                    SameText(key_local, 'xia');
            end;

            function get_fixed_quantity_text_fast_visible_local(
                const key_local: string): string;
            begin
                Result := '';
                if SameText(key_local, 'yi') then
                begin
                    Result := string(Char($4E00));
                end
                else if SameText(key_local, 'ci') then
                begin
                    Result := string(Char($6B21));
                end
                else if SameText(key_local, 'ge') then
                begin
                    Result := string(Char($4E2A));
                end
                else if SameText(key_local, 'tiao') then
                begin
                    Result := string(Char($6761));
                end
                else if SameText(key_local, 'zhang') then
                begin
                    Result := string(Char($5F20));
                end
                else if SameText(key_local, 'ben') then
                begin
                    Result := string(Char($672C));
                end
                else if SameText(key_local, 'pian') then
                begin
                    Result := string(Char($7BC7));
                end
                else if SameText(key_local, 'bu') then
                begin
                    Result := string(Char($90E8));
                end
                else if SameText(key_local, 'lun') then
                begin
                    Result := string(Char($8F6E));
                end
                else if SameText(key_local, 'hui') then
                begin
                    Result := string(Char($56DE));
                end
                else if SameText(key_local, 'xia') then
                begin
                    Result := string(Char($4E0B));
                end
                else if SameText(key_local, 'ba') then
                begin
                    Result := string(Char($5427));
                end;
            end;

            function has_quantity_modal_tail_mismatch_fast_visible_local(
                const text_value_local: string): Boolean;
            var
                local_units: TArray<string>;
                local_syllables: TncPinyinParseResult;
                local_idx: Integer;
                key0: string;
                key1: string;
                key2: string;
                expected_text: string;
            begin
                Result := False;
                local_units := split_text_units(Trim(text_value_local));
                local_syllables := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if (Length(local_units) <> input_syllable_count) or
                    (Length(local_syllables) <> input_syllable_count) then
                begin
                    Exit;
                end;

                for local_idx := 0 to input_syllable_count - 2 do
                begin
                    key0 := normalize_pinyin_text(local_syllables[local_idx].text);
                    key1 := normalize_pinyin_text(local_syllables[local_idx + 1].text);
                    if (not SameText(key0, 'yi')) or
                        (not is_quantity_tail_key_fast_visible_local(key1)) then
                    begin
                        Continue;
                    end;

                    expected_text := get_fixed_quantity_text_fast_visible_local(key0) +
                        get_fixed_quantity_text_fast_visible_local(key1);
                    if (expected_text <> '') and (not SameText(local_units[local_idx] +
                        local_units[local_idx + 1], expected_text)) then
                    begin
                        Exit(True);
                    end;

                    if local_idx + 2 >= input_syllable_count then
                    begin
                        Continue;
                    end;
                    key2 := normalize_pinyin_text(
                        local_syllables[local_idx + 2].text);
                    if not SameText(key2, 'ba') then
                    begin
                        Continue;
                    end;
                    expected_text := expected_text +
                        get_fixed_quantity_text_fast_visible_local(key2);
                    if (expected_text <> '') and (not SameText(local_units[local_idx] +
                        local_units[local_idx + 1] + local_units[local_idx + 2],
                        expected_text)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            if input_syllable_count < 6 then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_metric_local := Low(Integer);
            best_path_local := '';
            for local_idx := 0 to Min(c_fast_visible_probe_limit - 1,
                High(candidates)) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_candidate_text := Trim(candidates[local_idx].text);
                if (local_candidate_text = '') or
                    (get_candidate_text_unit_count(local_candidate_text) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;

                local_path_preference := 0;
                local_support_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, local_candidate_text, local_support_score,
                    local_path_preference);
                if local_support_path = '' then
                begin
                    Continue;
                end;

                current_metric_local := local_support_score +
                    get_preferred_query_phrase_bonus_local(lookup_text,
                    local_candidate_text);
                Inc(current_metric_local,
                    get_directional_context_metric_bonus_local(
                    local_candidate_text));
                FillChar(local_problematic_candidate,
                    SizeOf(local_problematic_candidate), 0);
                local_problematic_candidate.text := local_candidate_text;
                local_problematic_candidate.comment := '';
                if is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, local_problematic_candidate) then
                begin
                    Dec(current_metric_local, 1536);
                end;

                if current_metric_local > best_metric_local then
                begin
                    best_metric_local := current_metric_local;
                    best_idx_local := local_idx;
                    best_path_local := local_support_path;
                end;
            end;

            if (best_idx_local < 0) or
                (best_metric_local < c_fast_visible_metric_threshold) then
            begin
                Exit;
            end;

            top_is_complete_local := (Trim(candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(candidates[0].text)) =
                input_syllable_count);
            if top_is_complete_local then
            begin
                local_path_preference := 0;
                top_metric_local := get_preferred_query_phrase_bonus_local(
                    lookup_text, Trim(candidates[0].text));
                local_support_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(candidates[0].text), local_support_score,
                    local_path_preference);
                if local_support_path <> '' then
                begin
                    Inc(top_metric_local, local_support_score);
                    Inc(top_metric_local,
                        get_directional_context_metric_bonus_local(
                        Trim(candidates[0].text)));
                end
                else
                begin
                    top_metric_local := Low(Integer);
                end;
            end
            else
            begin
                top_metric_local := Low(Integer);
            end;

            if (best_idx_local = 0) and
                (top_metric_local >= c_fast_visible_metric_threshold) and
                (not has_quantity_modal_tail_mismatch_fast_visible_local(
                Trim(candidates[0].text))) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchain=[fast-visible keep text=%s metric=%d]',
                        [Trim(candidates[0].text), top_metric_local]);
                end;
                Exit(True);
            end;

            if (best_idx_local > 0) and
                (not has_quantity_modal_tail_mismatch_fast_visible_local(
                Trim(candidates[best_idx_local].text))) and
                ((not top_is_complete_local) or
                (best_metric_local > top_metric_local + 1024)) then
            begin
                picked_local := candidates[best_idx_local];
                for local_idx := best_idx_local downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
                candidates[0] := picked_local;
                if best_path_local <> '' then
                begin
                    remember_segment_path_for_candidate(
                        Trim(candidates[0].text), '', best_path_local,
                        candidates[0].score);
                end;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchain=[fast-visible promote text=%s metric=%d]',
                        [Trim(candidates[0].text), best_metric_local]);
                end;
                Exit(True);
            end;
        end;

        function get_path_segment_count_local(const encoded_path: string): Integer;
        var
            local_parts: TArray<string>;
            local_idx: Integer;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            local_parts := encoded_path.Split([c_segment_path_separator]);
            for local_idx := 0 to High(local_parts) do
            begin
                if Trim(local_parts[local_idx]) <> '' then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function is_trusted_top_complete_candidate_local(
            const local_candidates: TncCandidateList): Boolean;
        const
            c_min_support_score_local = 8192;
        var
            local_candidate: TncCandidate;
            local_candidate_text: string;
            local_support_score: Integer;
            local_path_preference: Integer;
            local_support_path: string;
        begin
            Result := False;
            if (Length(local_candidates) = 0) or
                (input_syllable_count < 4) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            local_candidate := local_candidates[0];
            if Trim(local_candidate.comment) <> '' then
            begin
                Exit;
            end;

            local_candidate_text := Trim(local_candidate.text);
            if (local_candidate_text = '') or
                (get_candidate_text_unit_count(local_candidate_text) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            if local_candidate.source = cs_user then
            begin
                Exit(True);
            end;
            if local_candidate.has_dict_weight and
                (input_syllable_count <= 4) then
            begin
                Exit(True);
            end;

            if is_problematic_single_char_chain_candidate_for_query(
                lookup_text, local_candidate) then
            begin
                Exit;
            end;

            local_support_path := infer_segment_path_for_query_text_with_score(
                lookup_text, local_candidate_text, local_support_score,
                local_path_preference);
            Result := (local_support_path <> '') and
                (local_support_score >= c_min_support_score_local);
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_part: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_part - 1 do
            begin
                Result := Result + normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function get_exact_chunk_length_bonus_local(
            const syllable_count_part: Integer): Integer;
        begin
            if syllable_count_part <= c_chain_max_chunk_syllables then
            begin
                Exit(syllable_count_part * syllable_count_part *
                    c_chain_length_bonus);
            end;

            Result := (c_chain_max_chunk_syllables * c_chain_max_chunk_syllables *
                c_chain_length_bonus) +
                ((syllable_count_part - c_chain_max_chunk_syllables) * 900);
        end;

        function get_exact_chunk_effective_weight_local(
            const local_candidate: TncCandidate): Integer;
        var
            local_units: Integer;
        begin
            if local_candidate.has_dict_weight then
            begin
                Result := local_candidate.dict_weight;
            end
            else
            begin
                Result := local_candidate.score;
            end;

            local_units := get_candidate_text_unit_count(Trim(local_candidate.text));
            case local_units of
                1:
                    if Result > 180 then
                    begin
                        Result := 180 + ((Result - 180) div 12);
                    end;
                2:
                    if Result > 240 then
                    begin
                        Result := 240 + ((Result - 240) div 10);
                    end;
                3:
                    if Result > 300 then
                    begin
                        Result := 300 + ((Result - 300) div 24);
                    end;
                4:
                    if Result > 360 then
                    begin
                        Result := 360 + ((Result - 360) div 32);
                    end;
            else
                if Result > 420 then
                begin
                    Result := 420 + ((Result - 420) div 40);
                end;
            end;
        end;

        function get_exact_chunk_raw_weight_local(
            const local_candidate: TncCandidate): Integer;
        begin
            if local_candidate.has_dict_weight then
            begin
                Result := local_candidate.dict_weight;
            end
            else
            begin
                Result := local_candidate.score;
            end;
        end;

        function get_last_text_unit_local(const value: string): string;
        var
            units_local: TArray<string>;
        begin
            Result := '';
            units_local := split_text_units(Trim(value));
            if Length(units_local) > 0 then
            begin
                Result := units_local[High(units_local)];
            end;
        end;

        function get_first_text_unit_context_local(const value: string): string;
        var
            trimmed_local: string;
        begin
            Result := '';
            trimmed_local := Trim(value);
            if Length(trimmed_local) > 0 then
            begin
                Result := trimmed_local[1];
            end;
        end;

        function get_last_text_unit_context_local(const value: string): string;
        var
            trimmed_local: string;
        begin
            Result := '';
            trimmed_local := Trim(value);
            if Length(trimmed_local) > 0 then
            begin
                Result := trimmed_local[Length(trimmed_local)];
            end;
        end;

        function is_text_unit_code_context_local(const value: string;
            const code_local: Integer): Boolean;
        begin
            Result := (Length(value) = 1) and (Ord(value[1]) = code_local);
        end;

        function is_predicate_head_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4E0D, $4E5F, $4F1A, $4F4F, $505A, $518D, $51FA, $5230,
                $53BB, $53D1, $53D8, $53D6, $542C, $56DE, $5728, $5B66,
                $5F00, $5F97, $60F3, $6210, $6253, $627E, $628A, $62FF,
                $6301, $63A5, $63D0, $6539, $653E, $6536, $662F, $6709,
                $6765, $6CA1, $7528, $770B, $7740, $7814, $79BB, $7EE7,
                $7ED9, $80FD, $8981, $89C1, $8BA9, $8BB2, $8BB0, $8BF4,
                $8D70, $8DD1, $8F93, $8F6C, $8FC7, $9001, $9009, $95EE,
                $5199, $597D, $591A, $5FEB, $6162, $6E05, $51C6, $786E,
                $660E, $96BE, $987A, $7A33, $5BB9, $8054, $88AB, $5FD0,
                $76B1, $8D62, $9501:
                    Result := True;
            end;
        end;

        function is_de_adverbial_predicate_head_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $505A, $51FA, $5230, $53BB, $53D1, $53D8, $53D6, $547C,
                $542C, $56DE, $5B66, $5F00, $60F3, $6210, $6253, $627E,
                $62FF, $6301, $63A5, $63D0, $6539, $653E, $6765,
                $7528, $770B, $7740, $7814, $79BB, $7EE7, $7ED9, $80E1,
                $89C1, $8BA9, $8BB2, $8BB0, $8BF4, $8D70, $8DD1, $8F93,
                $8F6C, $8FC7, $9001, $9009, $95EE, $5199, $5C06, $964D,
                $9610, $9996, $6572, $653B, $878D, $642C, $4FEE, $963B,
                $8054, $88AB, $5FD0, $76B1, $8D62, $9501, $5229, $7A33,
                $8131, $6563, $5B8C, $5BA3, $5E94, $79F0, $8EBA, $70B9, $8FC5,
                $9010, $6DF1, $8DF3, $63E1, $6D12, $8FFD:
                    Result := True;
            end;
        end;

        function is_result_complement_head_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4E0D, $4E86, $5230, $51FA, $597D, $5B8C, $5F00, $5F88,
                $5FEB, $6162, $660E, $6709, $6E05, $719F, $767D, $76F4, $771F,
                $7A33, $7EA2, $8001, $8D77, $8FDC, $8FD1, $900F, $9519,
                $9AD8:
                    Result := True;
            end;
        end;

        function is_location_or_measure_head_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4E0A, $4E0B, $4E2D, $4E2A, $4E9B, $4EF6, $4EEC, $4F4D,
                $5143, $5165, $5185, $5317, $5357, $4E1C, $897F, $5916,
                $524D, $540E, $56FD, $5730, $573A,
                $57CE, $5B66, $5BB6, $5C42, $5C4B, $5DE5, $5E02, $5E74,
                $65B9, $65F6, $6708, $672C, $6761, $679A, $6B21, $7247,
                $79CD, $7C7B, $7EC4, $8005, $91CC, $95F4, $9879:
                    Result := True;
            end;
        end;

        function is_de_adverbial_left_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $6025, $822C, $5F85, $987E, $7EC6, $5927, $5C0F, $9AD8,
                $4F4E, $5FEB, $6162, $6DF1, $6D45, $8F7B, $91CD, $9759,
                $9ED8, $6781, $529B, $5FC3, $610F, $7136, $65AD, $7EED,
                $76F4, $660E, $786E, $6E05, $695A, $771F, $6084, $5174,
                $597D, $4E3B, $529F,
                $82E6, $4E25, $5BC6, $5B9A, $7A33, $51C6, $6EE1, $575A,
                $51B3, $6027, $7ECF, $5916, $89C9, $51C0, $901F, $987A,
                $631A, $6FC0, $594B, $548C, $7981, $601D, $8BB6, $7545,
                $8352, $56F4, $6599, $8D45, $5947, $6B63:
                    Result := True;
            end;
        end;

        function is_yi_preposition_suffix_head_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4E0D, $4FBF, $521B, $5F62, $68A6, $5BF9, $6211, $4F60,
                $4ED6, $5979, $5B83, $6B64, $540C:
                    Result := True;
            end;
        end;

        function is_nominal_or_pronoun_head_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4EBA, $4EEC, $4EF6, $4F4D, $4F60, $4ED6, $516C, $5176,
                $5404, $5927, $5979, $5B83, $5BA2, $5BB6, $5C42, $5DE5,
                $5E02, $6280, $6570, $672C, $6761, $6B64, $6BCF, $7CFB,
                $8005, $884C, $9879, $8BE5, $8FD9, $90A3, $94F6:
                    Result := True;
            end;
        end;

        function is_wei_degree_left_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $66F4, $8F83, $6700, $8D8A, $6108, $5C24, $7A0D, $9887:
                    Result := True;
            end;
        end;

        function is_bound_yu_preposition_left_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4F4D, $5173, $5BF9, $7528, $5C5E, $7B49, $6E90, $57FA,
                $5904, $51FA, $6025, $7126, $9274, $81F3, $4FBF, $5229,
                $7531, $9650:
                    Result := True;
            end;
        end;

        function is_predicate_yu_preposition_left_unit_context_local(
            const unit_local: string): Boolean;
        begin
            Result := False;
            if Length(unit_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_local[1]) of
                $4E50, $4FBF, $5229, $5584, $5FD9, $6025, $75B2:
                    Result := True;
            end;
        end;

        function get_function_particle_suffix_context_adjustment_local(
            const chunk_last_syllable_local: string; const chunk_text_local: string;
            const suffix_text_local: string): Integer;
        var
            last_unit_local: string;
            suffix_first_unit_local: string;
            last_syllable_local: string;
            predicate_suffix_local: Boolean;
            de_adverbial_suffix_local: Boolean;
            complement_suffix_local: Boolean;
            location_suffix_local: Boolean;
            chunk_units_local: TArray<string>;
            chunk_left_unit_local: string;
        begin
            Result := 0;
            if (chunk_last_syllable_local = '') or (chunk_text_local = '') or
                (suffix_text_local = '') then
            begin
                Exit;
            end;

            last_unit_local := get_last_text_unit_context_local(chunk_text_local);
            if (last_unit_local = '') or
                ((not is_text_unit_code_context_local(last_unit_local, $7684)) and
                (not is_text_unit_code_context_local(last_unit_local, $5730)) and
                (not is_text_unit_code_context_local(last_unit_local, $5F97)) and
                (not is_text_unit_code_context_local(last_unit_local, $518D)) and
                (not is_text_unit_code_context_local(last_unit_local, $5728)) and
                (not is_text_unit_code_context_local(last_unit_local, $4E00)) and
                (not is_text_unit_code_context_local(last_unit_local, $4EE5)) and
                (not is_text_unit_code_context_local(last_unit_local, $7740)) and
                (not is_text_unit_code_context_local(last_unit_local, $8FD9)) and
                (not is_text_unit_code_context_local(last_unit_local, $4E3A)) and
                (not is_text_unit_code_context_local(last_unit_local, $672A)) and
                (not is_text_unit_code_context_local(last_unit_local, $4E0E)) and
                (not is_text_unit_code_context_local(last_unit_local, $4E8E))) then
            begin
                Exit;
            end;

            last_syllable_local := normalize_pinyin_text(
                chunk_last_syllable_local);

            suffix_first_unit_local :=
                get_first_text_unit_context_local(suffix_text_local);
            predicate_suffix_local :=
                is_predicate_head_unit_context_local(suffix_first_unit_local);
            de_adverbial_suffix_local :=
                is_de_adverbial_predicate_head_unit_context_local(
                suffix_first_unit_local);
            complement_suffix_local :=
                is_result_complement_head_unit_context_local(
                suffix_first_unit_local);
            location_suffix_local :=
                is_location_or_measure_head_unit_context_local(
                suffix_first_unit_local);
            chunk_left_unit_local := '';
            chunk_units_local := split_text_units(Trim(chunk_text_local));
            if Length(chunk_units_local) >= 2 then
            begin
                chunk_left_unit_local := chunk_units_local[
                    High(chunk_units_local) - 1];
            end;

            if last_syllable_local = 'de' then
            begin
                if is_text_unit_code_context_local(last_unit_local, $5730) then
                begin
                    if de_adverbial_suffix_local then
                    begin
                        Inc(Result, 16000);
                    end
                    else
                    begin
                        Dec(Result, 82000);
                    end;
                end
                else if is_text_unit_code_context_local(last_unit_local, $5F97) then
                begin
                    if complement_suffix_local then
                    begin
                        Inc(Result, 12000);
                    end
                    else
                    begin
                        Dec(Result, 52000);
                    end;
                end
                else if is_text_unit_code_context_local(last_unit_local, $7684) then
                begin
                    if de_adverbial_suffix_local and (not location_suffix_local) then
                    begin
                        if is_de_adverbial_left_unit_context_local(
                            chunk_left_unit_local) then
                        begin
                            Dec(Result, 26000);
                        end;
                    end
                    else
                    begin
                        Inc(Result, 1500);
                    end;
                end;
            end
            else if last_syllable_local = 'zai' then
            begin
                if is_text_unit_code_context_local(last_unit_local, $518D) and
                    predicate_suffix_local then
                begin
                    Inc(Result, 6000);
                end
                else if is_text_unit_code_context_local(last_unit_local, $5728) and
                    location_suffix_local then
                begin
                    Inc(Result, 4500);
                end;
            end
            else if last_syllable_local = 'yi' then
            begin
                if is_text_unit_code_context_local(last_unit_local, $4EE5) and
                    ((suffix_first_unit_local = string(Char($4E3A))) or
                    (suffix_first_unit_local = string(Char($53CA))) or
                    (suffix_first_unit_local = string(Char($540E))) or
                    (suffix_first_unit_local = string(Char($524D))) or
                    (suffix_first_unit_local = string(Char($6765))) or
                    (suffix_first_unit_local = string(Char($4E0A))) or
                    (suffix_first_unit_local = string(Char($4E0B))) or
                    is_yi_preposition_suffix_head_context_local(
                    suffix_first_unit_local)) then
                begin
                    Inc(Result, 5000);
                end
                else if is_text_unit_code_context_local(last_unit_local, $4E00) and
                    is_yi_preposition_suffix_head_context_local(
                    suffix_first_unit_local) then
                begin
                    Dec(Result, 4500);
                end
                else if is_text_unit_code_context_local(last_unit_local, $4E00) and
                    location_suffix_local then
                begin
                    Inc(Result, 2500);
                end;
            end
            else if last_syllable_local = 'zhe' then
            begin
                if is_text_unit_code_context_local(last_unit_local, $8FD9) and
                    location_suffix_local then
                begin
                    Inc(Result, 6000);
                end
                else if is_text_unit_code_context_local(last_unit_local, $7740) and
                    predicate_suffix_local then
                begin
                    Inc(Result, 2500);
                end;
            end
            else if last_syllable_local = 'wei' then
            begin
                if is_wei_degree_left_unit_context_local(chunk_left_unit_local) and
                    is_de_adverbial_left_unit_context_local(
                    suffix_first_unit_local) then
                begin
                    if is_text_unit_code_context_local(last_unit_local, $4E3A) then
                    begin
                        Inc(Result, 14000);
                    end
                    else if is_text_unit_code_context_local(last_unit_local, $672A) then
                    begin
                        Dec(Result, 120000);
                    end;
                end
                else if is_nominal_or_pronoun_head_unit_context_local(
                    suffix_first_unit_local) then
                begin
                    if is_text_unit_code_context_local(last_unit_local, $4E3A) then
                    begin
                        Inc(Result, 12000);
                    end
                    else if is_text_unit_code_context_local(last_unit_local, $672A) then
                    begin
                        Dec(Result, 18000);
                    end;
                end
                else if predicate_suffix_local then
                begin
                    if is_text_unit_code_context_local(last_unit_local, $672A) then
                    begin
                        Inc(Result, 5000);
                    end
                    else if is_text_unit_code_context_local(last_unit_local, $4E3A) then
                    begin
                        Dec(Result, 5000);
                    end;
                end;
            end
            else if last_syllable_local = 'yu' then
            begin
                if is_bound_yu_preposition_left_unit_context_local(
                    chunk_left_unit_local) then
                begin
                    if is_text_unit_code_context_local(last_unit_local, $4E8E) then
                    begin
                        if predicate_suffix_local and
                            (not is_text_unit_code_context_local(
                            suffix_first_unit_local, $662F)) and
                            (not location_suffix_local) and
                            (not is_predicate_yu_preposition_left_unit_context_local(
                            chunk_left_unit_local)) then
                        begin
                            Dec(Result, 6000);
                        end
                        else
                        begin
                            Inc(Result, 3600);
                        end;
                        if predicate_suffix_local and
                            (not is_text_unit_code_context_local(
                            suffix_first_unit_local, $662F)) and
                            is_predicate_yu_preposition_left_unit_context_local(
                            chunk_left_unit_local) then
                        begin
                            Inc(Result, 16000);
                        end;
                    end
                    else if is_text_unit_code_context_local(last_unit_local,
                        $4E0E) then
                    begin
                        Dec(Result, 30000);
                    end;
                end
                else if is_text_unit_code_context_local(last_unit_local, $4E8E) and
                    is_text_unit_code_context_local(suffix_first_unit_local,
                    $662F) then
                begin
                    Inc(Result, 5000);
                end
                else if (not predicate_suffix_local) and (not location_suffix_local) then
                begin
                    if is_text_unit_code_context_local(last_unit_local, $4E0E) then
                    begin
                        Inc(Result, 6000);
                    end
                    else if is_text_unit_code_context_local(last_unit_local, $4E8E) then
                    begin
                        Dec(Result, 5000);
                    end;
                end
                else if is_text_unit_code_context_local(last_unit_local, $4E8E) and
                    (predicate_suffix_local or location_suffix_local) then
                begin
                    Inc(Result, 2200);
                end;
            end
        end;

        function get_function_particle_prefix_context_adjustment_local(
            const left_text_local: string; const suffix_text_local: string): Integer;
        var
            left_last_unit_local: string;
            suffix_units_local: TArray<string>;
            particle_unit_local: string;
            next_unit_local: string;
        begin
            Result := 0;
            if (left_text_local = '') or (suffix_text_local = '') then
            begin
                Exit;
            end;

            left_last_unit_local := get_last_text_unit_context_local(left_text_local);
            suffix_units_local := split_text_units(Trim(suffix_text_local));
            if (left_last_unit_local = '') or (Length(suffix_units_local) = 0) then
            begin
                Exit;
            end;

            particle_unit_local := suffix_units_local[0];
            next_unit_local := '';
            if Length(suffix_units_local) >= 2 then
            begin
                next_unit_local := suffix_units_local[1];
            end;

            if is_text_unit_code_context_local(particle_unit_local, $5730) then
            begin
                if is_de_adverbial_left_unit_context_local(left_last_unit_local) and
                    is_de_adverbial_predicate_head_unit_context_local(
                    next_unit_local) then
                begin
                    Inc(Result, 18000);
                end
                else
                begin
                    Dec(Result, 120000);
                end;
            end
            else if is_text_unit_code_context_local(particle_unit_local, $5F97) then
            begin
                if is_predicate_head_unit_context_local(left_last_unit_local) and
                    is_result_complement_head_unit_context_local(next_unit_local) then
                begin
                    Inc(Result, 14000);
                end
                else
                begin
                    Dec(Result, 90000);
                end;
            end
            else if (is_text_unit_code_context_local(particle_unit_local, $4E3A) or
                is_text_unit_code_context_local(particle_unit_local, $672A)) then
            begin
                if is_wei_degree_left_unit_context_local(left_last_unit_local) and
                    is_de_adverbial_left_unit_context_local(next_unit_local) then
                begin
                    if is_text_unit_code_context_local(particle_unit_local, $4E3A) then
                    begin
                        Inc(Result, 14000);
                    end
                    else
                    begin
                        Dec(Result, 120000);
                    end;
                end;
            end;
        end;

        function starts_with_context_text_unit_local(const value_local: string;
            const code_local: Integer): Boolean;
        var
            first_unit_local: string;
        begin
            first_unit_local := get_first_text_unit_context_local(value_local);
            Result := is_text_unit_code_context_local(first_unit_local, code_local);
        end;

        function try_get_function_particle_boundary_alternative_local(
            const syllable_text_local: string; const current_text_local: string;
            const suffix_text_local: string; out out_text_local: string;
            out out_adjustment_local: Integer): Boolean;
        var
            normalized_syllable_local: string;
            suffix_first_unit_local: string;
            current_units_local: TArray<string>;
            prefix_text_local: string;
            left_unit_local: string;
            unit_idx_local: Integer;
            replacement_text_local: string;
        begin
            Result := False;
            out_text_local := '';
            out_adjustment_local := 0;
            if (current_text_local = '') or (suffix_text_local = '') or
                (get_candidate_text_unit_count(current_text_local) < 1) then
            begin
                Exit;
            end;

            normalized_syllable_local := normalize_pinyin_text(syllable_text_local);
            if normalized_syllable_local <> 'de' then
            begin
                Exit;
            end;

            suffix_first_unit_local :=
                get_first_text_unit_context_local(suffix_text_local);
            replacement_text_local := '';
            current_units_local := split_text_units(Trim(current_text_local));
            if Length(current_units_local) = 0 then
            begin
                Exit;
            end;
            left_unit_local := '';
            if Length(current_units_local) >= 2 then
            begin
                left_unit_local := current_units_local[High(current_units_local) - 1];
            end;

            if normalized_syllable_local = 'de' then
            begin
                if is_de_adverbial_predicate_head_unit_context_local(
                    suffix_first_unit_local) then
                begin
                    replacement_text_local := string(Char($5730));
                    out_adjustment_local := 42000;
                end
                else if is_result_complement_head_unit_context_local(
                    suffix_first_unit_local) then
                begin
                    replacement_text_local := string(Char($5F97));
                    out_adjustment_local := 18000;
                end;
            end;
            if replacement_text_local = '' then
            begin
                Exit;
            end;

            if Length(current_units_local) < 2 then
            begin
                Exit;
            end;

            if (replacement_text_local = string(Char($5730))) and
                (not is_de_adverbial_left_unit_context_local(left_unit_local)) then
            begin
                Exit;
            end;
            if (replacement_text_local = string(Char($5F97))) and
                (not is_predicate_head_unit_context_local(left_unit_local)) then
            begin
                Exit;
            end;

            if SameText(current_units_local[High(current_units_local)],
                replacement_text_local) then
            begin
                Exit;
            end;

            prefix_text_local := '';
            for unit_idx_local := 0 to High(current_units_local) - 1 do
            begin
                prefix_text_local := prefix_text_local +
                    current_units_local[unit_idx_local];
            end;
            out_text_local := prefix_text_local + replacement_text_local;
            Result := out_text_local <> '';
        end;

        function try_get_function_particle_suffix_head_alternative_local(
            const left_text_local: string; const suffix_text_local: string;
            const suffix_path_local: string; const suffix_first_chunk_text_local: string;
            const suffix_first_chunk_query_key_local: string;
            out out_suffix_text_local: string; out out_suffix_path_local: string;
            out out_adjustment_local: Integer): Boolean;
        var
            normalized_key_local: string;
            left_last_unit_local: string;
            suffix_units_local: TArray<string>;
            particle_unit_local: string;
            next_unit_local: string;
            replacement_text_local: string;
            unit_idx_local: Integer;
            separator_pos_local: Integer;
        begin
            Result := False;
            out_suffix_text_local := '';
            out_suffix_path_local := '';
            out_adjustment_local := 0;

            normalized_key_local := normalize_pinyin_text(
                suffix_first_chunk_query_key_local);
            if (left_text_local = '') or (suffix_text_local = '') or
                (suffix_first_chunk_text_local = '') then
            begin
                Exit;
            end;

            suffix_units_local := split_text_units(Trim(suffix_text_local));
            if Length(suffix_units_local) < 2 then
            begin
                Exit;
            end;

            particle_unit_local := suffix_units_local[0];
            left_last_unit_local := get_last_text_unit_context_local(left_text_local);
            next_unit_local := suffix_units_local[1];
            replacement_text_local := '';

            if is_text_unit_code_context_local(particle_unit_local, $7684) then
            begin
                if (normalized_key_local <> 'de') and
                    (not is_text_unit_code_context_local(
                    get_first_text_unit_context_local(suffix_first_chunk_text_local),
                    $7684)) then
                begin
                    Exit;
                end;

                if is_de_adverbial_left_unit_context_local(left_last_unit_local) and
                    is_de_adverbial_predicate_head_unit_context_local(
                    next_unit_local) then
                begin
                    replacement_text_local := string(Char($5730));
                    out_adjustment_local := 22000;
                end
                else if is_predicate_head_unit_context_local(left_last_unit_local) and
                    is_result_complement_head_unit_context_local(next_unit_local) then
                begin
                    replacement_text_local := string(Char($5F97));
                    out_adjustment_local := 18000;
                end;
            end
            else if (normalized_key_local = 'yu') and
                (is_text_unit_code_context_local(particle_unit_local, $4E0E) or
                is_text_unit_code_context_local(particle_unit_local, $4E8E)) then
            begin
                if is_text_unit_code_context_local(particle_unit_local, $4E0E) and
                    is_text_unit_code_context_local(next_unit_local, $662F) then
                begin
                    replacement_text_local := string(Char($4E8E));
                    out_adjustment_local := 52000;
                end
                else if is_bound_yu_preposition_left_unit_context_local(
                    left_last_unit_local) and
                    (not is_text_unit_code_context_local(particle_unit_local,
                    $4E8E)) then
                begin
                    replacement_text_local := string(Char($4E8E));
                    out_adjustment_local := 42000;
                end
                else if (not is_bound_yu_preposition_left_unit_context_local(
                    left_last_unit_local)) and
                    is_text_unit_code_context_local(particle_unit_local,
                    $4E8E) and
                    is_nominal_or_pronoun_head_unit_context_local(
                    next_unit_local) then
                begin
                    replacement_text_local := string(Char($4E0E));
                    out_adjustment_local := 16000;
                end;
            end
            else if (normalized_key_local = 'wei') and
                (is_text_unit_code_context_local(particle_unit_local, $4E3A) or
                is_text_unit_code_context_local(particle_unit_local, $672A)) then
            begin
                if is_wei_degree_left_unit_context_local(left_last_unit_local) and
                    is_de_adverbial_left_unit_context_local(next_unit_local) and
                    is_text_unit_code_context_local(particle_unit_local, $672A) then
                begin
                    replacement_text_local := string(Char($4E3A));
                    out_adjustment_local := 36000;
                end;
            end
            else
            begin
                Exit;
            end;

            if replacement_text_local = '' then
            begin
                if m_config.debug_mode and (ctxalt_miss_debug_count < 8) then
                begin
                    Inc(ctxalt_miss_debug_count);
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' ctxalt_miss=[left=%s next=%s key=%s suffix=%s]',
                        [left_last_unit_local, next_unit_local,
                        normalized_key_local, suffix_text_local]);
                end;
                Exit;
            end;

            out_suffix_text_local := replacement_text_local;
            for unit_idx_local := 1 to High(suffix_units_local) do
            begin
                out_suffix_text_local := out_suffix_text_local +
                    suffix_units_local[unit_idx_local];
            end;

            out_suffix_path_local := replacement_text_local;
            separator_pos_local := Pos(c_segment_path_separator, suffix_path_local);
            if separator_pos_local > 0 then
            begin
                out_suffix_path_local := out_suffix_path_local +
                    Copy(suffix_path_local, separator_pos_local, MaxInt);
            end;

            Result := out_suffix_text_local <> '';
        end;

        function has_particle_tail_mismatch_local(const query_key_local: string;
            const text_local: string; const raw_weight_local: Integer): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            last_syllable_local: string;
            last_unit_local: string;
        begin
            Result := False;
            syllables_local := get_effective_compact_pinyin_syllables(
                query_key_local);
            if Length(syllables_local) = 0 then
            begin
                Exit;
            end;

            last_syllable_local := normalize_pinyin_text(
                syllables_local[High(syllables_local)].text);
            if (last_syllable_local <> 'de') and (last_syllable_local <> 'le') then
            begin
                Exit;
            end;

            last_unit_local := get_last_text_unit_local(text_local);
            if last_syllable_local = 'de' then
            begin
                Result := (last_unit_local <> string(Char($7684))) and
                    (last_unit_local <> string(Char($5730))) and
                    (last_unit_local <> string(Char($5F97))) and
                    (raw_weight_local < 900);
            end
            else if last_syllable_local = 'le' then
            begin
                Result := (last_unit_local <> string(Char($4E86))) and
                    (last_unit_local <> string(Char($4E50))) and
                    (raw_weight_local < 900);
            end;
        end;

        function is_nonbase_two_plus_single_exact_chunk_local(
            const query_key_local: string; const chunk_len_local: Integer;
            const text_local: string): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            tail_syllable_local: string;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (chunk_len_local <> 3) then
            begin
                Exit;
            end;
            if m_dictionary.is_base_entry(query_key_local, text_local) or
                m_dictionary.is_user_entry(query_key_local, text_local) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                query_key_local);
            units_local := split_text_units(Trim(text_local));
            if (Length(syllables_local) <> 3) or (Length(units_local) <> 3) then
            begin
                Exit;
            end;

            tail_syllable_local := normalize_pinyin_text(
                syllables_local[2].text);
            if (tail_syllable_local = 'de') or (tail_syllable_local = 'le') or
                (tail_syllable_local = 'la') or (tail_syllable_local = 'ba') or
                (tail_syllable_local = 'ma') then
            begin
                Exit;
            end;

            head_key_local := normalize_pinyin_text(syllables_local[0].text) +
                normalize_pinyin_text(syllables_local[1].text);
            tail_key_local := tail_syllable_local;
            head_text_local := units_local[0] + units_local[1];
            tail_text_local := units_local[2];

            Result := direct_exact_lookup_has_text_local(head_key_local,
                head_text_local, 2) and
                (direct_exact_lookup_has_text_local(tail_key_local,
                tail_text_local, 1) or
                m_dictionary.single_char_matches_pinyin(tail_key_local,
                tail_text_local));
        end;

        function is_reliable_fast_exact_chunk_local(
            const query_key_local: string; const chunk_len_local: Integer;
            const text_local: string; const candidate_local: TncCandidate): Boolean;
        var
            raw_weight_local: Integer;
            preferred_bonus_local: Integer;
            min_weight_local: Integer;
        begin
            Result := True;
            if not long_exact_chunk_weight_is_eligible(
                candidate_local.dict_weight, candidate_local.has_dict_weight) then
            begin
                Exit(False);
            end;
            if chunk_len_local <= 1 then
            begin
                Exit;
            end;

            if is_nonbase_two_plus_single_exact_chunk_local(query_key_local,
                chunk_len_local, text_local) then
            begin
                Exit(False);
            end;

            raw_weight_local := get_exact_chunk_raw_weight_local(candidate_local);
            if has_particle_tail_mismatch_local(query_key_local, text_local,
                raw_weight_local) then
            begin
                Exit(False);
            end;

            preferred_bonus_local := get_preferred_query_phrase_bonus_local(
                query_key_local, text_local);
            case chunk_len_local of
                2:
                    min_weight_local := 240;
                3:
                    min_weight_local := 260;
                4:
                    min_weight_local := 220;
            else
                min_weight_local := 200;
            end;

            if (raw_weight_local < min_weight_local) and
                (preferred_bonus_local <= 0) then
            begin
                Result := False;
            end;
        end;

        function has_high_quality_exact_phrase_for_key_local(
            const query_key_local: string; const expected_units_local: Integer;
            const min_weight_local: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            if (query_key_local = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;
            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                Exit;
            end;
            for local_idx := 0 to Min(High(local_results), c_chain_probe_limit - 1) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_text := Trim(local_results[local_idx].text);
                if (get_candidate_text_unit_count(local_text) = expected_units_local) and
                    (get_exact_chunk_raw_weight_local(local_results[local_idx]) >=
                    min_weight_local) and
                    is_reliable_fast_exact_chunk_local(query_key_local,
                    expected_units_local, local_text, local_results[local_idx]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_high_quality_exact_phrase_with_prefix_for_key_local(
            const query_key_local: string; const expected_units_local: Integer;
            const min_weight_local: Integer; const prefix_text_local: string): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            if (query_key_local = '') or (prefix_text_local = '') or
                (m_dictionary = nil) then
            begin
                Exit;
            end;
            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                Exit;
            end;
            for local_idx := 0 to Min(High(local_results), c_chain_probe_limit - 1) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_text := Trim(local_results[local_idx].text);
                if (get_candidate_text_unit_count(local_text) = expected_units_local) and
                    (Copy(local_text, 1, Length(prefix_text_local)) =
                    prefix_text_local) and
                    (get_exact_chunk_raw_weight_local(local_results[local_idx]) >=
                    min_weight_local) and
                    is_reliable_fast_exact_chunk_local(query_key_local,
                    expected_units_local, local_text, local_results[local_idx]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_modifier_prefix_bridge_exempt_local(
            const prefix_text_local: string): Boolean;
        begin
            Result := (prefix_text_local = string(Char($4E0D))) or
                (prefix_text_local = string(Char($518D))) or
                (prefix_text_local = string(Char($9AD8))) or
                (prefix_text_local = string(Char($5F88))) or
                (prefix_text_local = string(Char($66F4))) or
                (prefix_text_local = string(Char($6700))) or
                (prefix_text_local = string(Char($592A))) or
                (prefix_text_local = string(Char($8981))) or
                (prefix_text_local = string(Char($80FD))) or
                (prefix_text_local = string(Char($6CA1))) or
                (prefix_text_local = string(Char($65E0))) or
                (prefix_text_local = string(Char($975E))) or
                (prefix_text_local = string(Char($53EF))) or
                (prefix_text_local = string(Char($4F1A))) or
                (prefix_text_local = string(Char($5E94))) or
                (prefix_text_local = string(Char($5C06))) or
                (prefix_text_local = string(Char($672A))) or
                (prefix_text_local = string(Char($5DF2))) or
                (prefix_text_local = string(Char($6B63))) or
                (prefix_text_local = string(Char($53C8))) or
                (prefix_text_local = string(Char($4E5F))) or
                (prefix_text_local = string(Char($8FD8))) or
                (prefix_text_local = string(Char($90FD))) or
                (prefix_text_local = string(Char($5C31))) or
                (prefix_text_local = string(Char($522B))) or
                (prefix_text_local = string(Char($6709))) or
                (prefix_text_local = string(Char($5BF9))) or
                (prefix_text_local = string(Char($5230))) or
                (prefix_text_local = string(Char($4ECE))) or
                (prefix_text_local = string(Char($548C))) or
                (prefix_text_local = string(Char($4E0E))) or
                (prefix_text_local = string(Char($4E8E))) or
                (prefix_text_local = string(Char($628A))) or
                (prefix_text_local = string(Char($88AB))) or
                (prefix_text_local = string(Char($7ED9))) or
                (prefix_text_local = string(Char($8BA9))) or
                (prefix_text_local = string(Char($5148))) or
                (prefix_text_local = string(Char($800C))) or
                (prefix_text_local = string(Char($5728))) or
                (prefix_text_local = string(Char($7684))) or
                (prefix_text_local = string(Char($5730))) or
                (prefix_text_local = string(Char($5F97))) or
                (prefix_text_local = string(Char($6240))) or
                (prefix_text_local = string(Char($662F))) or
                (prefix_text_local = string(Char($53EA))) or
                (prefix_text_local = string(Char($624D))) or
                (prefix_text_local = string(Char($5E76))) or
                (prefix_text_local = string(Char($4E14))) or
                (prefix_text_local = string(Char($4E3A)));
        end;

        function is_directional_location_phrase_text_local(
            const text_local: string): Boolean;
        var
            units_local: TArray<string>;
            first_unit_local: string;
            second_unit_local: string;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_local));
            if Length(units_local) <> 2 then
            begin
                Exit;
            end;

            first_unit_local := units_local[0];
            second_unit_local := units_local[1];

            if SameText(text_local, string(Char($9644)) + string(Char($8FD1))) or
                SameText(text_local, string(Char($5BF9)) + string(Char($9762))) or
                SameText(text_local, string(Char($65C1)) + string(Char($8FB9))) or
                SameText(text_local, string(Char($5468)) + string(Char($8FB9))) then
            begin
                Exit(True);
            end;

            if not (SameText(first_unit_local, string(Char($5317))) or
                SameText(first_unit_local, string(Char($5357))) or
                SameText(first_unit_local, string(Char($4E1C))) or
                SameText(first_unit_local, string(Char($897F))) or
                SameText(first_unit_local, string(Char($524D))) or
                SameText(first_unit_local, string(Char($540E))) or
                SameText(first_unit_local, string(Char($4E0A))) or
                SameText(first_unit_local, string(Char($4E0B))) or
                SameText(first_unit_local, string(Char($91CC))) or
                SameText(first_unit_local, string(Char($5185))) or
                SameText(first_unit_local, string(Char($5916))) or
                SameText(first_unit_local, string(Char($5DE6))) or
                SameText(first_unit_local, string(Char($53F3)))) then
            begin
                Exit;
            end;

            Result := SameText(second_unit_local, string(Char($9762))) or
                SameText(second_unit_local, string(Char($8FB9))) or
                SameText(second_unit_local, string(Char($4FA7))) or
                SameText(second_unit_local, string(Char($65B9))) or
                SameText(second_unit_local, string(Char($90E8))) or
                SameText(second_unit_local, string(Char($7AEF))) or
                SameText(second_unit_local, string(Char($5934)));
        end;

        function get_exact_chunk_semantic_bonus_local(
            const text_local: string): Integer;
        begin
            Result := 0;
            if is_directional_location_phrase_text_local(text_local) then
            begin
                Inc(Result, 3600);
            end;
        end;

        function get_adjacent_exact_chunk_context_bonus_local(
            const left_text_local: string; const right_text_local: string): Integer;
        begin
            Result := 0;
            if (get_candidate_text_unit_count(Trim(left_text_local)) >= 2) and
                is_directional_location_phrase_text_local(right_text_local) then
            begin
                Inc(Result, 7200);
            end;
        end;

        function is_strong_modifier_prefix_local(
            const prefix_text_local: string): Boolean;
        begin
            Result := (prefix_text_local = string(Char($4E0D))) or
                (prefix_text_local = string(Char($518D))) or
                (prefix_text_local = string(Char($9AD8))) or
                (prefix_text_local = string(Char($5F88))) or
                (prefix_text_local = string(Char($66F4))) or
                (prefix_text_local = string(Char($6700))) or
                (prefix_text_local = string(Char($592A))) or
                (prefix_text_local = string(Char($8981))) or
                (prefix_text_local = string(Char($80FD))) or
                (prefix_text_local = string(Char($6CA1))) or
                (prefix_text_local = string(Char($65E0))) or
                (prefix_text_local = string(Char($975E))) or
                (prefix_text_local = string(Char($53EF))) or
                (prefix_text_local = string(Char($4F1A))) or
                (prefix_text_local = string(Char($5E94))) or
                (prefix_text_local = string(Char($5C06))) or
                (prefix_text_local = string(Char($672A))) or
                (prefix_text_local = string(Char($5DF2))) or
                (prefix_text_local = string(Char($6B63))) or
                (prefix_text_local = string(Char($53C8))) or
                (prefix_text_local = string(Char($4E5F))) or
                (prefix_text_local = string(Char($8FD8))) or
                (prefix_text_local = string(Char($90FD))) or
                (prefix_text_local = string(Char($5C31))) or
                (prefix_text_local = string(Char($522B))) or
                (prefix_text_local = string(Char($6709))) or
                (prefix_text_local = string(Char($5728))) or
                (prefix_text_local = string(Char($6240))) or
                (prefix_text_local = string(Char($5BF9))) or
                (prefix_text_local = string(Char($4ECE))) or
                (prefix_text_local = string(Char($4E8E))) or
                (prefix_text_local = string(Char($4E0E))) or
                (prefix_text_local = string(Char($548C))) or
                (prefix_text_local = string(Char($628A))) or
                (prefix_text_local = string(Char($88AB))) or
                (prefix_text_local = string(Char($7ED9))) or
                (prefix_text_local = string(Char($8BA9))) or
                (prefix_text_local = string(Char($800C))) or
                (prefix_text_local = string(Char($7684)));
        end;

        function is_boundary_tail_comment_local(const tail_query_key: string): Boolean;
        var
            tail_syllables_local: TncPinyinParseResult;
            first_tail_local: string;
        begin
            Result := False;
            if tail_query_key = '' then
            begin
                Exit;
            end;

            tail_syllables_local := get_effective_compact_pinyin_syllables(
                tail_query_key);
            if Length(tail_syllables_local) = 0 then
            begin
                Exit;
            end;

            first_tail_local := normalize_pinyin_text(tail_syllables_local[0].text);
            Result := (first_tail_local = 'de') or (first_tail_local = 'le') or
                (first_tail_local = 'ma') or (first_tail_local = 'ba') or
                (first_tail_local = 'ne') or (first_tail_local = 'a') or
                (first_tail_local = 'ya') or (first_tail_local = 'la') or
                (first_tail_local = 'wa');
        end;

        function get_fixed_boundary_single_char_for_standalone_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'jiang' then
            begin
                Result := string(Char($5C06));
            end
            else if normalized_syllable = 'rang' then
            begin
                Result := string(Char($8BA9));
            end
            else if normalized_syllable = 'gei' then
            begin
                Result := string(Char($7ED9));
            end
            else if normalized_syllable = 'bei' then
            begin
                Result := string(Char($88AB));
            end
            else if normalized_syllable = 'dui' then
            begin
                Result := string(Char($5BF9));
            end
            else if normalized_syllable = 'dao' then
            begin
                Result := string(Char($5230));
            end
            else if normalized_syllable = 'cong' then
            begin
                Result := string(Char($4ECE));
            end
            else if normalized_syllable = 'you' then
            begin
                Result := string(Char($7531));
            end
            else if normalized_syllable = 'deng' then
            begin
                Result := string(Char($7B49));
            end
            else if normalized_syllable = 'hai' then
            begin
                Result := string(Char($8FD8));
            end
            else if normalized_syllable = 'suo' then
            begin
                Result := string(Char($6240));
            end
            else if normalized_syllable = 'wei' then
            begin
                Result := string(Char($4E3A));
            end
            else if normalized_syllable = 'mai' then
            begin
                Result := string(Char($4E70));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end;
        end;

        function get_context_free_single_char_penalty_local(
            const query_key_local: string; const candidate_text_local: string): Integer;
        var
            normalized_query_key_local: string;
            glue_text_local: string;
        begin
            Result := 0;
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <> 1) then
            begin
                Exit;
            end;

            normalized_query_key_local := normalize_pinyin_text(query_key_local);
            if normalized_query_key_local = '' then
            begin
                Exit;
            end;

            glue_text_local := get_fixed_boundary_single_char_for_standalone_local(
                normalized_query_key_local);
            if (glue_text_local = '') or
                (not SameText(candidate_text_local, glue_text_local)) then
            begin
                Exit;
            end;

            if (normalized_query_key_local = 'shi') or
                (normalized_query_key_local = 'ye') or
                (normalized_query_key_local = 'yu') or
                (normalized_query_key_local = 'hui') or
                (normalized_query_key_local = 'ba') or
                (normalized_query_key_local = 'de') or
                (normalized_query_key_local = 'liao') or
                (normalized_query_key_local = 'zai') or
                (normalized_query_key_local = 'zhe') or
                (normalized_query_key_local = 'que') or
                (normalized_query_key_local = 'bing') or
                (normalized_query_key_local = 'qie') or
                (normalized_query_key_local = 'jiang') or
                (normalized_query_key_local = 'rang') or
                (normalized_query_key_local = 'dui') or
                (normalized_query_key_local = 'dao') or
                (normalized_query_key_local = 'cong') or
                (normalized_query_key_local = 'yi') or
                (normalized_query_key_local = 'you') or
                (normalized_query_key_local = 'er') or
                (normalized_query_key_local = 'he') or
                (normalized_query_key_local = 'ke') then
            begin
                Result := 4096;
            end;
        end;

        function has_exact_query_text_match_local(const query_key_local: string;
            const candidate_text_local: string): Boolean;
        var
            local_results_local: TncCandidateList;
            local_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key_local = '') or
                (candidate_text_local = '') then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results_local) then
            begin
                Exit;
            end;

            for local_idx_local := 0 to High(local_results_local) do
            begin
                if local_idx_local >= c_chain_probe_limit * 3 then
                begin
                    Break;
                end;
                if Trim(local_results_local[local_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    local_results_local[local_idx_local].dict_weight,
                    local_results_local[local_idx_local].has_dict_weight) then
                begin
                    Continue;
                end;
                if SameText(Trim(local_results_local[local_idx_local].text),
                    candidate_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_exact_query_text_bonus_local(const query_key_local: string;
            const candidate_text_local: string): Integer;
        begin
            if has_exact_query_text_match_local(query_key_local,
                candidate_text_local) then
            begin
                Exit(3072);
            end;
            Result := 0;
        end;

        function get_long_exact_phrase_cohesion_bonus_local(
            const syllable_count_part: Integer): Integer;
        begin
            Result := 0;
            if syllable_count_part >= 3 then
            begin
                Result := (syllable_count_part - 2) * 3600;
            end;
        end;

        function get_modifier_prefix_phrase_bonus_local(
            const syllable_count_part: Integer;
            const candidate_text_local: string): Integer;
        var
            text_units_local: TArray<string>;
            first_unit_local: string;
        begin
            Result := 0;
            if (syllable_count_part <> 3) or (candidate_text_local = '') then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_text_local));
            if Length(text_units_local) <> syllable_count_part then
            begin
                Exit;
            end;

            first_unit_local := text_units_local[0];
            if SameText(first_unit_local, string(Char($4E0D))) or
                SameText(first_unit_local, string(Char($518D))) or
                SameText(first_unit_local, string(Char($9AD8))) or
                SameText(first_unit_local, string(Char($5F88))) or
                SameText(first_unit_local, string(Char($66F4))) or
                SameText(first_unit_local, string(Char($6700))) or

                SameText(first_unit_local, string(Char($592A))) or
                SameText(first_unit_local, string(Char($8981))) or
                SameText(first_unit_local, string(Char($80FD))) or
                SameText(first_unit_local, string(Char($53EF))) or
                SameText(first_unit_local, string(Char($4F1A))) or
                SameText(first_unit_local, string(Char($5E94))) or
                SameText(first_unit_local, string(Char($5DF2))) or
                SameText(first_unit_local, string(Char($5C06))) or
                SameText(first_unit_local, string(Char($6B63))) or
                SameText(first_unit_local, string(Char($53C8))) or
                SameText(first_unit_local, string(Char($4E5F))) or
                SameText(first_unit_local, string(Char($8FD8))) or
                SameText(first_unit_local, string(Char($90FD))) or
                SameText(first_unit_local, string(Char($5C31))) or
                SameText(first_unit_local, string(Char($522B))) or
                SameText(first_unit_local, string(Char($975E))) or
                SameText(first_unit_local, string(Char($6CA1))) or
                SameText(first_unit_local, string(Char($65E0))) or
                SameText(first_unit_local, string(Char($672A))) then
            begin
                Result := 7200;
            end;
            if SameText(first_unit_local, string(Char($4F46))) or
                SameText(first_unit_local, string(Char($800C))) or
                SameText(first_unit_local, string(Char($6709))) or
                SameText(first_unit_local, string(Char($5728))) or
                SameText(first_unit_local, string(Char($5BF9))) or
                SameText(first_unit_local, string(Char($5230))) or
                SameText(first_unit_local, string(Char($4ECE))) or
                SameText(first_unit_local, string(Char($548C))) or
                SameText(first_unit_local, string(Char($4E0E))) or
                SameText(first_unit_local, string(Char($4E8E))) or
                SameText(first_unit_local, string(Char($628A))) or
                SameText(first_unit_local, string(Char($88AB))) or
                SameText(first_unit_local, string(Char($7ED9))) or
                SameText(first_unit_local, string(Char($8BA9))) or
                SameText(first_unit_local, string(Char($7684))) or
                SameText(first_unit_local, string(Char($6240))) or

                SameText(first_unit_local, string(Char($662F))) or
                SameText(first_unit_local, string(Char($4E00))) then

            begin
                Result := Max(Result, 7200);
            end;
        end;

        function try_get_modifier_prefix_single_text_local(
            const syllable_text: string; out out_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            Result := True;
            out_text := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'bu' then
            begin
                out_text := string(Char($4E0D));
            end
            else if normalized_syllable = 'zai' then
            begin
                out_text := string(Char($5728));
            end
            else if normalized_syllable = 'gao' then
            begin
                out_text := string(Char($9AD8));
            end
            else if normalized_syllable = 'hen' then
            begin
                out_text := string(Char($5F88));
            end
            else if normalized_syllable = 'geng' then
            begin
                out_text := string(Char($66F4));
            end
            else if normalized_syllable = 'zui' then
            begin
                out_text := string(Char($6700));
            end
            else if normalized_syllable = 'tai' then
            begin
                out_text := string(Char($592A));
            end
            else if normalized_syllable = 'yao' then
            begin
                out_text := string(Char($8981));
            end
            else if normalized_syllable = 'neng' then
            begin
                out_text := string(Char($80FD));
            end
            else if normalized_syllable = 'ke' then
            begin
                out_text := string(Char($53EF));
            end
            else if normalized_syllable = 'hui' then
            begin
                out_text := string(Char($4F1A));
            end
            else if normalized_syllable = 'ying' then
            begin
                out_text := string(Char($5E94));
            end
            else if normalized_syllable = 'yi' then
            begin
                out_text := string(Char($4E00));
            end
            else if normalized_syllable = 'jiang' then
            begin
                out_text := string(Char($5C06));
            end
            else if normalized_syllable = 'zheng' then
            begin
                out_text := string(Char($6B63));
            end
            else if normalized_syllable = 'you' then
            begin
                out_text := string(Char($6709));
            end
            else if normalized_syllable = 'dan' then
            begin
                out_text := string(Char($4F46));
            end
            else if normalized_syllable = 'er' then
            begin
                out_text := string(Char($800C));
            end
            else if normalized_syllable = 'ye' then
            begin
                out_text := string(Char($4E5F));
            end
            else if normalized_syllable = 'hai' then
            begin
                out_text := string(Char($8FD8));
            end
            else if normalized_syllable = 'dou' then
            begin
                out_text := string(Char($90FD));
            end
            else if normalized_syllable = 'jiu' then
            begin
                out_text := string(Char($5C31));
            end
            else if normalized_syllable = 'bie' then
            begin
                out_text := string(Char($522B));
            end
            else if normalized_syllable = 'fei' then
            begin
                out_text := string(Char($975E));
            end
            else if normalized_syllable = 'mei' then
            begin
                out_text := string(Char($6CA1));
            end
            else if normalized_syllable = 'wu' then
            begin
                out_text := string(Char($65E0));
            end
            else if normalized_syllable = 'wei' then
            begin
                Result := False;
            end
            else if normalized_syllable = 'dui' then
            begin
                out_text := string(Char($5BF9));
            end
            else if normalized_syllable = 'dao' then
            begin
                out_text := string(Char($5230));
            end
            else if normalized_syllable = 'cong' then
            begin
                out_text := string(Char($4ECE));
            end
            else if normalized_syllable = 'he' then
            begin
                out_text := string(Char($548C));
            end
            else if normalized_syllable = 'yu' then
            begin
                out_text := string(Char($4E8E));
            end
            else if normalized_syllable = 'ba' then
            begin
                out_text := string(Char($628A));
            end
            else if normalized_syllable = 'bei' then
            begin
                out_text := string(Char($88AB));
            end
            else if normalized_syllable = 'gei' then
            begin
                out_text := string(Char($7ED9));
            end
            else if normalized_syllable = 'rang' then
            begin
                out_text := string(Char($8BA9));
            end
            else if normalized_syllable = 'de' then
            begin
                out_text := string(Char($7684));
            end
            else if normalized_syllable = 'suo' then
            begin
                out_text := string(Char($6240));
            end
            else if normalized_syllable = 'shi' then
            begin
                out_text := string(Char($662F));
            end
            else if normalized_syllable = 'zhi' then
            begin
                out_text := string(Char($53EA));
            end
            else if normalized_syllable = 'cai' then
            begin
                out_text := string(Char($624D));
            end
            else if normalized_syllable = 'xiang' then
            begin
                out_text := string(Char($50CF));
            end
            else if normalized_syllable = 'bing' then
            begin
                out_text := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                out_text := string(Char($4E14));
            end
            else
            begin
                Result := False;
            end;
        end;

        function is_valid_standalone_cjk_phrase_text_local(const value: string;
            const expected_units: Integer): Boolean;
        var
            units: TArray<string>;
            unit_idx: Integer;
            codepoint: Integer;

            function try_get_codepoint_local(const unit_text: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if unit_text = '' then
                begin
                    Exit;
                end;

                if Length(unit_text) = 1 then
                begin
                    out_codepoint := Ord(unit_text[1]);
                    Exit(True);
                end;

                if (Length(unit_text) = 2) and
                    (Ord(unit_text[1]) >= $D800) and
                    (Ord(unit_text[1]) <= $DBFF) and
                    (Ord(unit_text[2]) >= $DC00) and
                    (Ord(unit_text[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                        (Ord(unit_text[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if Trim(value) = '' then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(units) do
            begin
                if (not try_get_codepoint_local(units[unit_idx], codepoint)) or
                    (codepoint < $4E00) or (codepoint > $9FFF) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function try_get_best_single_char_for_query_key_local(
            const query_key_local: string; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (query_key_local = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_chain_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    local_results[local_idx].dict_weight,
                    local_results[local_idx].has_dict_weight) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(local_results[local_idx].text);
                if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                    (not is_valid_standalone_cjk_phrase_text_local(
                    candidate_text_local, 1)) then
                begin
                    Continue;
                end;

                if local_results[local_idx].has_dict_weight then
                begin
                    candidate_score_local := local_results[local_idx].dict_weight;
                end
                else
                begin
                    candidate_score_local := local_results[local_idx].score;
                end;
                Inc(candidate_score_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                Dec(candidate_score_local, get_context_free_single_char_penalty_local(
                    query_key_local, candidate_text_local));

                if (not Result) or (candidate_score_local > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text_local;
                    out_score := candidate_score_local;
                end;
            end;
        end;

        function try_get_best_exact_phrase_for_query_key_local(
            const query_key_local: string; const expected_units: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (query_key_local = '') or (expected_units <= 0) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_chain_lattice_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    local_results[local_idx].dict_weight,
                    local_results[local_idx].has_dict_weight) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(local_results[local_idx].text);
                if (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) or
                    (not is_valid_standalone_cjk_phrase_text_local(
                    candidate_text_local, expected_units)) then
                begin
                    Continue;
                end;

                candidate_score_local :=
                    get_exact_chunk_effective_weight_local(local_results[local_idx]);
                Inc(candidate_score_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                Inc(candidate_score_local, get_exact_query_text_bonus_local(
                    query_key_local, candidate_text_local));
                Inc(candidate_score_local, get_exact_chunk_semantic_bonus_local(
                    candidate_text_local));

                if (not Result) or (candidate_score_local > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text_local;
                    out_score := candidate_score_local;
                end;
            end;
        end;

        function get_exact_chunk_boundary_adjustment_local(
            const start_idx: Integer; const syllable_count_part: Integer;
            const candidate_text_local: string): Integer;
        var
            text_units_local: TArray<string>;
            expected_char_local: string;
            syllable_key_local: string;
            last_idx_local: Integer;
        begin
            Result := 0;
            if (syllable_count_part <= 0) or (candidate_text_local = '') then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_text_local));
            if Length(text_units_local) <> syllable_count_part then
            begin
                Exit;
            end;

            expected_char_local := get_fixed_boundary_single_char_for_standalone_local(
                effective_syllables[start_idx].text);
            if expected_char_local <> '' then
            begin
                syllable_key_local := normalize_pinyin_text(
                    effective_syllables[start_idx].text);
                if (syllable_key_local = 'de') and
                    SameText(text_units_local[0], string(Char($5730))) then
                begin
                    Dec(Result, 16000);
                end
            end;

            if syllable_count_part <= 1 then
            begin
                Exit;
            end;

            last_idx_local := start_idx + syllable_count_part - 1;
            expected_char_local := get_fixed_boundary_single_char_for_standalone_local(
                effective_syllables[last_idx_local].text);
            if expected_char_local <> '' then
            begin
                syllable_key_local := normalize_pinyin_text(
                    effective_syllables[last_idx_local].text);
                if (syllable_key_local = 'de') and
                    SameText(text_units_local[High(text_units_local)],
                    string(Char($5730))) then
                begin
                    Dec(Result, 16000);
                end
            end;
        end;

        function try_resolve_three_syllable_split_option_local(
            const start_idx: Integer; out out_option: TExactChunkChainOption): Boolean;
        var
            head_key_local: string;
            tail_key_local: string;
            combined_score_local: Integer;

            function should_defer_pair_started_inside_previous_exact_local(
                const pair_units_start: Integer; const pair_text_local: string): Boolean;
            var
                previous_key_local: string;
                expected_text_local: string;
                pair_units_local: TArray<string>;
            begin
                Result := False;
                if (pair_units_start <= 0) or
                    (pair_units_start + 1 >= syllable_count_local) or
                    (pair_text_local = '') then
                begin
                    Exit;
                end;

                if not try_get_modifier_prefix_single_text_local(
                    effective_syllables[pair_units_start + 1].text,
                    expected_text_local) then
                begin
                    Exit;
                end;

                pair_units_local := split_text_units(Trim(pair_text_local));
                if (Length(pair_units_local) <> 2) or
                    SameText(pair_units_local[1], expected_text_local) then
                begin
                    Exit;
                end;

                previous_key_local := build_query_key_local(pair_units_start - 1, 2);
                Result := has_high_quality_exact_phrase_for_key_local(
                    previous_key_local, 2, 700);
            end;

            procedure consider_split_local(const left_key: string; const left_units: Integer;
                const right_key: string; const right_units: Integer);
            var
                left_text_local: string;
                right_text_local: string;
                left_score_local: Integer;
                right_score_local: Integer;
                option_local: TExactChunkChainOption;
                fixed_text_local: string;
                local_results: TncCandidateList;
                local_idx: Integer;
            begin
                if left_units = 1 then
                begin
                    if (right_units = 2) and
                        try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx].text, fixed_text_local) then
                    begin
                        left_text_local := fixed_text_local;
                        left_score_local := 1200;
                        if lookup_exact_full_pinyin_cached_local(left_key,
                            local_results) then
                        begin
                            for local_idx := 0 to High(local_results) do
                            begin
                                if local_idx >= c_chain_probe_limit then
                                begin
                                    Break;
                                end;
                                if SameText(Trim(local_results[local_idx].text),
                                    left_text_local) then
                                begin
                                    left_score_local :=
                                        get_exact_chunk_effective_weight_local(
                                        local_results[local_idx]);
                                    Break;
                                end;
                            end;
                        end;
                    end
                    else if not try_get_best_single_char_for_query_key_local(
                        left_key, left_text_local, left_score_local) then
                    begin
                        Exit;
                    end;
                end
                else if not try_get_best_exact_phrase_for_query_key_local(left_key,
                    left_units, left_text_local, left_score_local) then
                begin
                    Exit;
                end;
                if right_units = 1 then
                begin
                    fixed_text_local :=
                        get_fixed_boundary_single_char_for_standalone_local(
                        effective_syllables[start_idx + left_units].text);
                    if fixed_text_local <> '' then
                    begin
                        right_text_local := fixed_text_local;
                        right_score_local := 1200;
                        if lookup_exact_full_pinyin_cached_local(right_key,
                            local_results) then
                        begin
                            for local_idx := 0 to High(local_results) do
                            begin
                                if local_idx >= c_chain_probe_limit then
                                begin
                                    Break;
                                end;
                                if SameText(Trim(local_results[local_idx].text),
                                    right_text_local) then
                                begin
                                    right_score_local :=
                                        get_exact_chunk_effective_weight_local(
                                        local_results[local_idx]);
                                    Break;
                                end;
                            end;
                        end;
                    end
                    else if not try_get_best_single_char_for_query_key_local(right_key,
                        right_text_local, right_score_local) then
                    begin
                        Exit;
                    end;
                end
                else if not try_get_best_exact_phrase_for_query_key_local(right_key,
                    right_units, right_text_local, right_score_local) then
                begin
                    Exit;
                end;

                if (left_text_local = '') or (right_text_local = '') then
                begin
                    Exit;
                end;
                if (left_units = 2) and
                    should_defer_pair_started_inside_previous_exact_local(
                    start_idx, left_text_local) then
                begin
                    Exit;
                end;
                if (left_units = 1) and (right_units = 2) and
                    (not is_modifier_prefix_bridge_exempt_local(left_text_local)) then
                begin
                    Exit;
                end;

                combined_score_local := left_score_local + right_score_local +
                    c_chain_lattice_merge_bonus + get_exact_chunk_length_bonus_local(3) +
                    c_chain_anchor_bonus;
                if (left_units = 2) and (right_units = 1) then
                begin
                    Inc(combined_score_local, 2200);
                    if left_score_local >= 700 then
                    begin
                        Inc(combined_score_local, 1600);
                    end;
                end
                else if (left_units = 1) and (right_units = 2) then
                begin
                    Inc(combined_score_local, 2400);
                    if is_strong_modifier_prefix_local(left_text_local) and
                        (right_score_local >= 700) then
                    begin
                        Inc(combined_score_local, 5200);
                    end;
                end;
                if m_dictionary <> nil then
                begin
                    Inc(combined_score_local, m_dictionary.get_context_bonus(
                        left_text_local, right_text_local));
                end;

                option_local := Default(TExactChunkChainOption);
                option_local.text := left_text_local + right_text_local;
                option_local.path := left_text_local + c_segment_path_separator +
                    right_text_local;
                option_local.query_key := left_key + right_key;
                option_local.score := combined_score_local;
                option_local.segments := 2;
                option_local.has_anchor := True;
                option_local.anchor_units := 2;

                if (not Result) or (option_local.score > out_option.score) or
                    ((option_local.score = out_option.score) and
                    (option_local.segments < out_option.segments)) or
                    ((option_local.score = out_option.score) and
                    (option_local.segments = out_option.segments) and
                    (CompareText(option_local.text, out_option.text) < 0)) then
                begin
                    Result := True;
                    out_option := option_local;
                end;
            end;
        begin
            Result := False;
            out_option := Default(TExactChunkChainOption);
            if (start_idx < 0) or (start_idx + 2 > High(effective_syllables)) then
            begin
                Exit;
            end;

            head_key_local := build_query_key_local(start_idx, 2);
            tail_key_local := build_query_key_local(start_idx + 2, 1);
            consider_split_local(head_key_local, 2, tail_key_local, 1);

            head_key_local := build_query_key_local(start_idx, 1);
            tail_key_local := build_query_key_local(start_idx + 1, 2);
            consider_split_local(head_key_local, 1, tail_key_local, 2);
        end;

        function is_exact_chunk_option_better_local(
            const left_option: TExactChunkChainOption;
            const right_option: TExactChunkChainOption): Boolean;
        begin
            Result := False;
            if left_option.score > right_option.score then
            begin
                Exit(True);
            end;
            if left_option.score < right_option.score then
            begin
                Exit(False);
            end;
            if left_option.has_anchor and (not right_option.has_anchor) then
            begin
                Exit(True);
            end;
            if (not left_option.has_anchor) and right_option.has_anchor then
            begin
                Exit(False);
            end;
            if left_option.anchor_units > right_option.anchor_units then
            begin
                Exit(True);
            end;
            if left_option.anchor_units < right_option.anchor_units then
            begin
                Exit(False);
            end;
            if left_option.segments < right_option.segments then
            begin
                Exit(True);
            end;
            if left_option.segments > right_option.segments then
            begin
                Exit(False);
            end;
            if (left_option.text <> '') and
                ((right_option.text = '') or
                (CompareText(left_option.text, right_option.text) < 0)) then
            begin
                Exit(True);
            end;
        end;

        procedure sort_and_trim_exact_chunk_options_local(
            var options: TExactChunkChainOptionArray; const max_count: Integer);
        var
            option_idx: Integer;
            insert_idx: Integer;
            existing_option: TExactChunkChainOption;
        begin
            for option_idx := 1 to High(options) do
            begin
                for insert_idx := option_idx downto 1 do
                begin
                    if not is_exact_chunk_option_better_local(options[insert_idx],
                        options[insert_idx - 1]) then
                    begin
                        Break;
                    end;
                    existing_option := options[insert_idx - 1];
                    options[insert_idx - 1] := options[insert_idx];
                    options[insert_idx] := existing_option;
                end;
            end;

            if (max_count > 0) and (Length(options) > max_count) then
            begin
                SetLength(options, max_count);
            end;
        end;

        function try_collect_standalone_query_candidates_local(
            const query_key_local: string; const expected_syllables: Integer;
            out out_options: TExactChunkChainOptionArray): Boolean;
        var
            cached_state: Integer;
            cached_options_local: TExactChunkChainOptionArray;
            snapshot_candidates_local: TncCandidateList;
            snapshot_scores_local: TArray<Integer>;
            candidate_idx_local: Integer;
            candidate_local: TncCandidate;
            candidate_text_local: string;
            candidate_score_local: Integer;
            candidate_units_local: Integer;
            best_complete_text_local: string;
            best_complete_score_local: Integer;
            best_partial_text_local: string;
            best_partial_score_local: Integer;
            best_partial_comment_local: string;
            best_split_text_local: string;
            best_split_score_local: Integer;
            comment_key_local: string;
            comment_syllables_local: TncPinyinParseResult;
            tail_text_local: string;
            tail_score_local: Integer;
            combined_text_local: string;
            combined_score_local: Integer;
            context_bonus_local: Integer;
            query_syllables_local: TncPinyinParseResult;
            split_idx_local: Integer;
            left_key_local: string;
            right_key_local: string;
            left_text_local: string;
            left_score_local: Integer;
            right_text_local: string;
            right_score_local: Integer;
            left_options_local: TExactChunkChainOptionArray;
            right_options_local: TExactChunkChainOptionArray;
            left_option_idx_local: Integer;
            right_option_idx_local: Integer;
            local_idx: Integer;
            local_results: TncCandidateList;
            infer_score_local: Integer;
            infer_segments_local: Integer;
            inferred_path_local: string;
            local_option: TExactChunkChainOption;
            best_partial_option_local: TExactChunkChainOption;
            best_split_option_local: TExactChunkChainOption;
            has_best_partial_option_local: Boolean;
            has_best_split_option_local: Boolean;
            fixed_single_text_local: string;
        const
            c_standalone_split_option_probe_limit = 6;
            c_standalone_single_char_option_limit = 6;
            procedure add_standalone_option_local(
                const candidate_option: TExactChunkChainOption);
            var
                option_idx_local: Integer;
            begin
                if candidate_option.text = '' then
                begin
                    Exit;
                end;
                for option_idx_local := 0 to High(out_options) do
                begin
                    if SameText(out_options[option_idx_local].text,
                        candidate_option.text) then
                    begin
                        if is_exact_chunk_option_better_local(candidate_option,
                            out_options[option_idx_local]) then
                        begin
                            out_options[option_idx_local] := candidate_option;
                        end;
                        Exit;
                    end;
                end;
                SetLength(out_options, Length(out_options) + 1);
                out_options[High(out_options)] := candidate_option;
            end;
        begin
            Result := False;
            SetLength(out_options, 0);
            if (query_key_local = '') or (not is_full_pinyin_key(query_key_local)) or
                (input_syllable_count < 8) or
                (expected_syllables <= 0) or
                (expected_syllables > c_chain_lattice_max_chunk_syllables) then
            begin
                Exit;
            end;

            if expected_syllables = 1 then
            begin
                fixed_single_text_local :=
                    get_fixed_boundary_single_char_for_standalone_local(
                    query_key_local);
                if fixed_single_text_local <> '' then
                begin
                    candidate_score_local := 0;
                    if lookup_exact_full_pinyin_cached_local(query_key_local,
                        local_results) then
                    begin
                        for local_idx := 0 to High(local_results) do
                        begin
                            if local_idx >= c_chain_probe_limit then
                            begin
                                Break;
                            end;
                            if SameText(Trim(local_results[local_idx].text),
                                fixed_single_text_local) then
                            begin
                                candidate_score_local :=
                                    get_exact_chunk_effective_weight_local(
                                    local_results[local_idx]);
                                Break;
                            end;
                        end;
                    end;
                    if candidate_score_local <= 0 then
                    begin
                        candidate_score_local := 1200;
                    end;
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(
                        query_key_local, fixed_single_text_local));
                    Inc(candidate_score_local,
                        get_exact_query_text_bonus_local(query_key_local,
                        fixed_single_text_local));
                    Inc(candidate_score_local, c_chain_lattice_fixed_single_bonus);

                    local_option := Default(TExactChunkChainOption);
                    local_option.text := fixed_single_text_local;
                    local_option.path := fixed_single_text_local;
                    local_option.query_key := query_key_local;
                    local_option.score := candidate_score_local;
                    local_option.segments := 1;
                    local_option.has_anchor := True;
                    local_option.anchor_units := 1;
                    add_standalone_option_local(local_option);
                    Exit(Length(out_options) > 0);
                end;

                if lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    for local_idx := 0 to High(local_results) do
                    begin
                        if local_idx >= c_chain_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(local_results[local_idx].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not long_exact_chunk_weight_is_eligible(
                            local_results[local_idx].dict_weight,
                            local_results[local_idx].has_dict_weight) then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(local_results[local_idx].text);
                        if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                            (not is_valid_standalone_cjk_phrase_text_local(
                            candidate_text_local, 1)) then
                        begin
                            Continue;
                        end;

                        if local_results[local_idx].has_dict_weight then
                        begin
                            candidate_score_local :=
                                local_results[local_idx].dict_weight;
                        end
                        else
                        begin
                            candidate_score_local := local_results[local_idx].score;
                        end;
                        Inc(candidate_score_local,
                            get_preferred_query_phrase_bonus_local(
                            query_key_local, candidate_text_local));
                        Inc(candidate_score_local,
                            get_exact_query_text_bonus_local(query_key_local,
                            candidate_text_local));
                        Dec(candidate_score_local,
                            get_context_free_single_char_penalty_local(
                            query_key_local, candidate_text_local));

                        local_option := Default(TExactChunkChainOption);
                        local_option.text := candidate_text_local;
                        local_option.path := candidate_text_local;
                        local_option.query_key := query_key_local;
                        local_option.score := candidate_score_local;
                        local_option.segments := 1;
                        local_option.has_anchor := True;
                        local_option.anchor_units := 1;
                        add_standalone_option_local(local_option);
                    end;
                end;
                sort_and_trim_exact_chunk_options_local(out_options,
                    c_standalone_single_char_option_limit);
                Exit(Length(out_options) > 0);
            end;

            if expected_syllables = 2 then
            begin
                if lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    for local_idx := 0 to High(local_results) do
                    begin
                        if local_idx >= c_chain_lattice_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(local_results[local_idx].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not long_exact_chunk_weight_is_eligible(
                            local_results[local_idx].dict_weight,
                            local_results[local_idx].has_dict_weight) then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(local_results[local_idx].text);
                        if (get_candidate_text_unit_count(candidate_text_local) <> 2) or
                            (not is_valid_standalone_cjk_phrase_text_local(
                            candidate_text_local, 2)) then
                        begin
                            Continue;
                        end;

                        candidate_score_local :=
                            get_exact_chunk_effective_weight_local(
                            local_results[local_idx]);
                        Inc(candidate_score_local,
                            get_preferred_query_phrase_bonus_local(
                            query_key_local, candidate_text_local));
                        Inc(candidate_score_local,
                            get_exact_query_text_bonus_local(query_key_local,
                            candidate_text_local));

                        local_option := Default(TExactChunkChainOption);
                        local_option.text := candidate_text_local;
                        local_option.path := candidate_text_local;
                        local_option.query_key := query_key_local;
                        local_option.score := candidate_score_local;
                        local_option.segments := 1;
                        local_option.has_anchor := True;
                        local_option.anchor_units := 2;
                        add_standalone_option_local(local_option);
                    end;
                end;
                sort_and_trim_exact_chunk_options_local(out_options,
                    c_chain_standalone_option_limit);
                Exit(Length(out_options) > 0);
            end;

            if standalone_chunk_states.TryGetValue(query_key_local, cached_state) then
            begin
                if cached_state = 1 then
                begin
                    if standalone_chunk_options.TryGetValue(query_key_local,
                        cached_options_local) then
                    begin
                        out_options := Copy(cached_options_local);
                        Exit(Length(out_options) > 0);
                    end;
                    standalone_chunk_texts.TryGetValue(query_key_local,
                        candidate_text_local);
                    standalone_chunk_scores.TryGetValue(query_key_local,
                        candidate_score_local);
                    if candidate_text_local <> '' then
                    begin
                        SetLength(out_options, 1);
                        out_options[0] := Default(TExactChunkChainOption);
                        out_options[0].text := candidate_text_local;
                        out_options[0].path := candidate_text_local;
                        out_options[0].query_key := query_key_local;
                        out_options[0].score := candidate_score_local;
                        out_options[0].segments := 1;
                        out_options[0].has_anchor :=
                            expected_syllables >= c_chain_min_anchor_syllables;
                        if out_options[0].has_anchor then
                        begin
                            out_options[0].anchor_units := expected_syllables;
                        end
                        else
                        begin
                            out_options[0].anchor_units := 0;
                        end;
                        Exit(True);
                    end;
                    Exit(False);
                end;
                Exit(False);
            end;

            if standalone_chunk_budget_remaining <= 0 then
            begin
                Exit;
            end;
            Dec(standalone_chunk_budget_remaining);

            if standalone_chunk_engine = nil then
            begin
                standalone_chunk_config := m_config;
                standalone_chunk_config.debug_mode := False;
                standalone_chunk_engine := TncEngine.Create(
                    standalone_chunk_config, False, False);
                inherit_search_budget_policy_to(standalone_chunk_engine);
            end;

            standalone_chunk_states.AddOrSetValue(query_key_local, 3);
            standalone_chunk_engine.m_disable_subspan_standalone_oracle :=
                m_disable_subspan_standalone_oracle + 1;
            standalone_chunk_engine.m_composition_text := query_key_local;
            standalone_chunk_engine.build_candidates;
            SetLength(snapshot_candidates_local,
                Length(standalone_chunk_engine.m_candidates));
            SetLength(snapshot_scores_local,
                Length(standalone_chunk_engine.m_candidates));
            for candidate_idx_local := 0 to High(standalone_chunk_engine.m_candidates) do
            begin
                snapshot_candidates_local[candidate_idx_local] :=
                    standalone_chunk_engine.m_candidates[candidate_idx_local];
                snapshot_scores_local[candidate_idx_local] :=
                    standalone_chunk_engine.get_rank_score(
                    standalone_chunk_engine.m_candidates[candidate_idx_local]);
            end;

            best_complete_text_local := '';
            best_complete_score_local := Low(Integer);
            best_partial_text_local := '';
            best_partial_score_local := Low(Integer);
            best_partial_comment_local := '';
            best_split_text_local := '';
            best_split_score_local := Low(Integer);
            has_best_partial_option_local := False;
            has_best_split_option_local := False;

            if lookup_exact_full_pinyin_cached_local(query_key_local,
                local_results) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_probe_limit * 3 then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results[local_idx].dict_weight,
                        local_results[local_idx].has_dict_weight) then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if (get_candidate_text_unit_count(candidate_text_local) <>
                        expected_syllables) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, expected_syllables)) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight >
                        candidate_score_local) then
                    begin
                        candidate_score_local := local_results[local_idx].dict_weight;
                    end;
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(query_key_local,
                        candidate_text_local));
                    Inc(candidate_score_local,
                        get_exact_query_text_bonus_local(query_key_local,
                        candidate_text_local));
                    Inc(candidate_score_local,
                        get_exact_chunk_semantic_bonus_local(candidate_text_local));

                    if candidate_score_local > best_complete_score_local then
                    begin
                        best_complete_text_local := candidate_text_local;
                        best_complete_score_local := candidate_score_local;
                    end;

                    local_option := Default(TExactChunkChainOption);
                    local_option.text := candidate_text_local;
                    local_option.path := candidate_text_local;
                    local_option.query_key := query_key_local;
                    local_option.score := candidate_score_local;
                    local_option.segments := 1;
                    local_option.has_anchor :=
                        expected_syllables >= c_chain_min_anchor_syllables;
                    if local_option.has_anchor then
                    begin
                        local_option.anchor_units := expected_syllables;
                    end
                    else
                    begin
                        local_option.anchor_units := 0;
                    end;
                    add_standalone_option_local(local_option);
                end;
            end;

            for candidate_idx_local := 0 to High(snapshot_candidates_local) do
            begin
                if candidate_idx_local >= c_chain_probe_limit then
                begin
                    Break;
                end;

                candidate_local := snapshot_candidates_local[candidate_idx_local];
                candidate_text_local := Trim(candidate_local.text);
                if candidate_text_local = '' then
                begin
                    Continue;
                end;

                if Trim(candidate_local.comment) = '' then
                begin
                    if not long_exact_chunk_weight_is_eligible(
                        candidate_local.dict_weight,
                        candidate_local.has_dict_weight) then
                    begin
                        Continue;
                    end;
                    candidate_score_local :=
                        get_exact_chunk_effective_weight_local(candidate_local);
                end
                else
                begin
                    candidate_score_local := snapshot_scores_local[
                        candidate_idx_local];
                end;
                Inc(candidate_score_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, candidate_text_local));
                candidate_units_local :=
                    get_candidate_text_unit_count(candidate_text_local);

                if Trim(candidate_local.comment) = '' then
                begin
                    if (candidate_units_local <> expected_syllables) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, expected_syllables)) then
                    begin
                        Continue;
                    end;

                    inferred_path_local :=
                        infer_segment_path_for_query_text_with_score(
                        query_key_local, candidate_text_local, infer_score_local,
                        infer_segments_local);
                    if inferred_path_local <> '' then
                    begin
                        Inc(candidate_score_local, infer_score_local);
                        if infer_segments_local > 1 then
                        begin
                            Inc(candidate_score_local,
                                infer_segments_local * 160);
                        end;
                    end
                    else if expected_syllables >= 3 then
                    begin
                        Dec(candidate_score_local, 1536);
                    end;

                    if candidate_score_local > best_complete_score_local then
                    begin
                        best_complete_text_local := candidate_text_local;
                        best_complete_score_local := candidate_score_local;
                    end;
                    local_option := Default(TExactChunkChainOption);
                    local_option.text := candidate_text_local;
                    local_option.path := candidate_text_local;
                    local_option.query_key := query_key_local;
                    local_option.score := candidate_score_local;
                    local_option.segments := 1;
                    local_option.has_anchor :=
                        expected_syllables >= c_chain_min_anchor_syllables;
                    if local_option.has_anchor then
                    begin
                        local_option.anchor_units := expected_syllables;
                    end
                    else
                    begin
                        local_option.anchor_units := 0;
                    end;
                    add_standalone_option_local(local_option);
                    Continue;
                end;

                comment_key_local := normalize_pinyin_text(candidate_local.comment);
                if (comment_key_local = '') or
                    (not is_full_pinyin_key(comment_key_local)) or
                    (not is_boundary_tail_comment_local(comment_key_local)) then
                begin
                    Continue;
                end;

                comment_syllables_local := get_effective_compact_pinyin_syllables(
                    comment_key_local);
                if (Length(comment_syllables_local) = 0) or
                    (candidate_units_local + Length(comment_syllables_local) <>
                    expected_syllables) then
                begin
                    Continue;
                end;

                if not try_collect_standalone_query_candidates_local(comment_key_local,
                    Length(comment_syllables_local), right_options_local) or
                    (Length(right_options_local) = 0) then
                begin
                    Continue;
                end;
                tail_text_local := right_options_local[0].text;
                tail_score_local := right_options_local[0].score;

                combined_text_local := candidate_text_local + tail_text_local;
                if not is_valid_standalone_cjk_phrase_text_local(
                    combined_text_local, expected_syllables) then
                begin
                    Continue;
                end;

                combined_score_local := candidate_score_local + tail_score_local;
                if m_dictionary <> nil then
                begin
                    context_bonus_local := m_dictionary.get_context_bonus(
                        candidate_text_local, tail_text_local);
                    Inc(combined_score_local, context_bonus_local);
                end;
                Inc(combined_score_local,
                    get_preferred_query_phrase_bonus_local(query_key_local,
                    combined_text_local));
                Inc(combined_score_local,
                    get_exact_query_text_bonus_local(query_key_local,
                    combined_text_local));
                Inc(combined_score_local, 1024);

                if combined_score_local > best_partial_score_local then
                begin
                    best_partial_text_local := combined_text_local;
                    best_partial_score_local := combined_score_local;
                    best_partial_comment_local := comment_key_local;
                end;
                local_option := Default(TExactChunkChainOption);
                local_option.text := combined_text_local;
                local_option.path := combined_text_local;
                local_option.query_key := query_key_local;
                local_option.score := combined_score_local;
                local_option.segments := 1;
                local_option.has_anchor :=
                    expected_syllables >= c_chain_min_anchor_syllables;
                if local_option.has_anchor then
                begin
                    local_option.anchor_units := expected_syllables;
                end
                else
                begin
                    local_option.anchor_units := 0;
                end;
                if (not has_best_partial_option_local) or
                    is_exact_chunk_option_better_local(local_option,
                    best_partial_option_local) then
                begin
                    best_partial_option_local := local_option;
                    has_best_partial_option_local := True;
                end;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(
                query_key_local);
            if Length(query_syllables_local) = expected_syllables then
            begin
                for split_idx_local := 1 to expected_syllables - 1 do
                begin
                    left_key_local := '';
                    for local_idx := 0 to split_idx_local - 1 do
                    begin
                        left_key_local := left_key_local + normalize_pinyin_text(
                            query_syllables_local[local_idx].text);
                    end;

                    right_key_local := '';
                    for local_idx := split_idx_local to High(query_syllables_local) do
                    begin
                        right_key_local := right_key_local + normalize_pinyin_text(
                            query_syllables_local[local_idx].text);
                    end;

                    if (left_key_local = '') or (right_key_local = '') then
                    begin
                        Continue;
                    end;

                    if not try_collect_standalone_query_candidates_local(
                        left_key_local, split_idx_local, left_options_local) or
                        (Length(left_options_local) = 0) then
                    begin
                        Continue;
                    end;
                    if not try_collect_standalone_query_candidates_local(
                        right_key_local, expected_syllables - split_idx_local,
                        right_options_local) or
                        (Length(right_options_local) = 0) then
                    begin
                        Continue;
                    end;
                    for left_option_idx_local := 0 to
                        Min(High(left_options_local),
                        c_standalone_split_option_probe_limit - 1) do
                    begin
                        left_text_local := left_options_local[left_option_idx_local].text;
                        left_score_local :=
                            left_options_local[left_option_idx_local].score;
                        for right_option_idx_local := 0 to
                            Min(High(right_options_local),
                            c_standalone_split_option_probe_limit - 1) do
                        begin
                            right_text_local :=
                                right_options_local[right_option_idx_local].text;
                            right_score_local :=
                                right_options_local[right_option_idx_local].score;

                            if (expected_syllables >= 4) and
                                (not ((split_idx_local = 1) and
                                (expected_syllables - split_idx_local = 3) and
                                (get_modifier_prefix_phrase_bonus_local(3,
                                right_text_local) > 0))) then
                            begin
                                Continue;
                            end;

                            combined_text_local := left_text_local + right_text_local;
                            if not is_valid_standalone_cjk_phrase_text_local(
                                combined_text_local, expected_syllables) then
                            begin
                                Continue;
                            end;

                            combined_score_local := left_score_local +
                                right_score_local - 96;
                            if m_dictionary <> nil then
                            begin
                                context_bonus_local := m_dictionary.get_context_bonus(
                                    left_text_local, right_text_local);
                                Inc(combined_score_local, context_bonus_local);
                            end;
                            Inc(combined_score_local,
                                get_preferred_query_phrase_bonus_local(
                                query_key_local, combined_text_local));
                            Inc(combined_score_local,
                                get_exact_query_text_bonus_local(
                                query_key_local, combined_text_local));
                            inferred_path_local :=
                                infer_segment_path_for_query_text_with_score(
                                query_key_local, combined_text_local,
                                infer_score_local, infer_segments_local);
                            if inferred_path_local <> '' then
                            begin
                                Inc(combined_score_local, infer_score_local);
                                if infer_segments_local > 1 then
                                begin
                                    Inc(combined_score_local,
                                        infer_segments_local * 160);
                                end;
                            end;

                            if combined_score_local > best_split_score_local then
                            begin
                                best_split_text_local := combined_text_local;
                                best_split_score_local := combined_score_local;
                            end;
                            local_option := Default(TExactChunkChainOption);
                            local_option.text := combined_text_local;
                            local_option.path :=
                                left_options_local[left_option_idx_local].path;
                            if right_options_local[right_option_idx_local].path <> '' then
                            begin
                                if local_option.path <> '' then
                                begin
                                    local_option.path := local_option.path +
                                        c_segment_path_separator;
                                end;
                                local_option.path := local_option.path +
                                    right_options_local[right_option_idx_local].path;
                            end;
                            local_option.query_key := query_key_local;
                            local_option.score := combined_score_local;
                            local_option.segments :=
                                left_options_local[left_option_idx_local].segments +
                                right_options_local[right_option_idx_local].segments;
                            local_option.has_anchor :=
                                left_options_local[left_option_idx_local].has_anchor or
                                right_options_local[right_option_idx_local].has_anchor;
                            local_option.anchor_units :=
                                left_options_local[left_option_idx_local].anchor_units;
                            if right_options_local[right_option_idx_local].anchor_units >
                                local_option.anchor_units then
                            begin
                                local_option.anchor_units :=
                                    right_options_local[right_option_idx_local].anchor_units;
                            end;
                            if (not has_best_split_option_local) or
                                is_exact_chunk_option_better_local(local_option,
                                best_split_option_local) then
                            begin
                                best_split_option_local := local_option;
                                has_best_split_option_local := True;
                            end;
                        end;
                    end;
                end;
            end;

            if has_best_partial_option_local then
            begin
                if (best_complete_text_local <> '') and
                    is_boundary_tail_comment_local(best_partial_comment_local) and
                    (best_partial_score_local > best_complete_score_local - 256) then
                begin
                    add_standalone_option_local(best_partial_option_local);
                end
                else if best_complete_text_local = '' then
                begin
                    add_standalone_option_local(best_partial_option_local);
                end;
            end;

            if has_best_split_option_local then
            begin
                add_standalone_option_local(best_split_option_local);
            end;

            sort_and_trim_exact_chunk_options_local(out_options,
                c_chain_standalone_option_limit);

            if Length(out_options) > 0 then
            begin
                standalone_chunk_states.AddOrSetValue(query_key_local, 1);
                standalone_chunk_options.AddOrSetValue(query_key_local,
                    Copy(out_options));
                standalone_chunk_texts.AddOrSetValue(query_key_local,
                    out_options[0].text);
                standalone_chunk_scores.AddOrSetValue(query_key_local,
                    out_options[0].score);
                Exit(True);
            end;

            standalone_chunk_states.AddOrSetValue(query_key_local, 2);
        end;

        function try_get_best_standalone_query_candidate_local(
            const query_key_local: string; const expected_syllables: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_options: TExactChunkChainOptionArray;
        begin
            Result := try_collect_standalone_query_candidates_local(query_key_local,
                expected_syllables, local_options) and (Length(local_options) > 0);
            if Result then
            begin
                out_text := local_options[0].text;
                out_score := local_options[0].score;
            end
            else
            begin
                out_text := '';
                out_score := 0;
            end;
        end;

        function try_get_standalone_chunk_candidate_local(const start_idx: Integer;
            const syllable_count_part: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        begin
            out_text := '';
            out_score := 0;
            // Arbitrary recursive standalone chunks are intentionally disabled.
            // Sentence paths should be assembled from exact lexicon/user chunks,
            // explicit function-word bridges, and single-character fallbacks.
            Result := False;
        end;

        procedure try_update_best_from_position(const start_idx: Integer);
        var
            chunk_syllables: Integer;
            next_idx: Integer;
            resolved_text: string;
            resolved_path: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            total_score: Integer;
            total_segments: Integer;
            total_anchor_units: Integer;
            total_has_anchor: Boolean;
            combined_text: string;
            combined_path: string;
            better_candidate: Boolean;
            standalone_text: string;
            standalone_score: Integer;
            standalone_min_score: Integer;
        begin
            for chunk_syllables := 1 to Min(c_chain_max_chunk_syllables,
                syllable_count_local - start_idx) do
            begin
                next_idx := start_idx + chunk_syllables;
                if (next_idx < syllable_count_local) and
                    (best_scores[next_idx] = Low(Integer)) then
                begin
                    Continue;
                end;

                resolved_text := '';
                resolved_path := '';
                resolved_score := 0;
                resolved_segments := 0;
                resolved_anchor_units := -1;
                resolved_has_anchor := False;

                if (chunk_syllables >= 2) and
                    try_get_standalone_chunk_candidate_local(start_idx,
                    chunk_syllables, standalone_text, standalone_score) and
                    (standalone_text <> '') then
                begin
                    resolved_text := standalone_text;
                    resolved_path := standalone_text;
                    resolved_segments := 1;
                    resolved_has_anchor := chunk_syllables >= c_chain_min_anchor_syllables;
                    if resolved_has_anchor then
                    begin
                        resolved_anchor_units := chunk_syllables;
                    end
                    else
                    begin
                        resolved_anchor_units := 0;
                    end;
                    resolved_score := standalone_score +
                        (chunk_syllables * chunk_syllables * c_chain_length_bonus);
                    if resolved_has_anchor then
                    begin
                        Inc(resolved_score, c_chain_anchor_bonus);
                    end;
                end
                else if not try_resolve_best_exact_subspan_local(start_idx,
                    chunk_syllables, resolved_text, resolved_score,
                    resolved_segments, resolved_path, resolved_has_anchor,
                    resolved_anchor_units) then
                begin
                    Continue;
                end;

                if (chunk_syllables >= 2) and
                    (resolved_segments > 1) and
                    try_get_standalone_chunk_candidate_local(start_idx,
                    chunk_syllables, standalone_text, standalone_score) and
                    (standalone_text <> '') then
                begin
                    standalone_min_score := standalone_score +
                        (chunk_syllables * chunk_syllables * c_chain_length_bonus);
                    if chunk_syllables >= c_chain_min_anchor_syllables then
                    begin
                        Inc(standalone_min_score, c_chain_anchor_bonus);
                    end;
                    resolved_text := standalone_text;
                    resolved_score := Max(resolved_score + 2048, standalone_min_score);
                    resolved_segments := 1;
                    resolved_path := standalone_text;
                    resolved_has_anchor := chunk_syllables >= c_chain_min_anchor_syllables;
                    if resolved_has_anchor then
                    begin
                        resolved_anchor_units := chunk_syllables;
                    end
                    else
                    begin
                        resolved_anchor_units := 0;
                    end;
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' xoracle=[key=%s text=%s score=%d]',
                            [build_query_key_local(start_idx, chunk_syllables),
                            standalone_text, standalone_min_score]);
                    end;
                end;

                total_score := resolved_score;
                total_segments := resolved_segments;
                total_has_anchor := resolved_has_anchor;
                total_anchor_units := resolved_anchor_units;
                combined_text := resolved_text;
                combined_path := resolved_path;

                if next_idx < syllable_count_local then
                begin
                    Inc(total_score, best_scores[next_idx]);
                    Inc(total_segments, best_segments[next_idx]);
                    if best_has_anchor[next_idx] then
                    begin
                        total_has_anchor := True;
                        if best_anchor_units[next_idx] > total_anchor_units then
                        begin
                            total_anchor_units := best_anchor_units[next_idx];
                        end;
                    end;
                    combined_text := combined_text + best_texts[next_idx];
                    if best_paths[next_idx] <> '' then
                    begin
                        if combined_path <> '' then
                        begin
                            combined_path := combined_path + c_segment_path_separator;
                        end;
                        combined_path := combined_path + best_paths[next_idx];
                    end;
                end;

                better_candidate := False;
                if total_score > best_scores[start_idx] then
                begin
                    better_candidate := True;
                end
                else if total_score = best_scores[start_idx] then
                begin
                    if total_has_anchor and (not best_has_anchor[start_idx]) then
                    begin
                        better_candidate := True;
                    end
                    else if total_has_anchor = best_has_anchor[start_idx] then
                    begin
                        if total_anchor_units > best_anchor_units[start_idx] then
                        begin
                            better_candidate := True;
                        end
                        else if total_anchor_units = best_anchor_units[start_idx] then
                        begin
                            if total_segments < best_segments[start_idx] then
                            begin
                                better_candidate := True;
                            end
                            else if (total_segments = best_segments[start_idx]) and
                                (combined_text <> '') and
                                ((best_texts[start_idx] = '') or
                                (CompareText(combined_text, best_texts[start_idx]) < 0)) then
                            begin
                                better_candidate := True;
                            end;
                        end;
                    end;
                end;

                if better_candidate then
                begin
                    best_scores[start_idx] := total_score;
                    best_texts[start_idx] := combined_text;
                    best_paths[start_idx] := combined_path;
                    best_segments[start_idx] := total_segments;
                    best_has_anchor[start_idx] := total_has_anchor;
                    best_anchor_units[start_idx] := total_anchor_units;
                end;
            end;
        end;

        function is_exact_chunk_chain_state_better_local(
            const left_state: TExactChunkChainState;
            const right_state: TExactChunkChainState): Boolean;
        begin
            Result := False;
            if left_state.score > right_state.score then
            begin
                Exit(True);
            end;
            if left_state.score < right_state.score then
            begin
                Exit(False);
            end;
            if left_state.has_anchor and (not right_state.has_anchor) then
            begin
                Exit(True);
            end;
            if (not left_state.has_anchor) and right_state.has_anchor then
            begin
                Exit(False);
            end;
            if left_state.anchor_units > right_state.anchor_units then
            begin
                Exit(True);
            end;
            if left_state.anchor_units < right_state.anchor_units then
            begin
                Exit(False);
            end;
            if left_state.segments < right_state.segments then
            begin
                Exit(True);
            end;
            if left_state.segments > right_state.segments then
            begin
                Exit(False);
            end;
            if (left_state.text <> '') and
                ((right_state.text = '') or
                (CompareText(left_state.text, right_state.text) < 0)) then
            begin
                Exit(True);
            end;
        end;

        function get_exact_chunk_chain_state_lm_bonus_local(
            const state_inner: TExactChunkChainState;
            const state_start_pos: Integer;
            out stats_inner: TLongWordLmBoundaryStats): Integer;
        var
            path_segments_inner: TArray<string>;
            segment_idx_inner: Integer;
            query_offset_inner: Integer;
            current_text_inner: string;
            next_text_inner: string;
            previous_text_inner: string;
            current_units_inner: Integer;
            next_units_inner: Integer;
            previous_units_inner: Integer;
            transition_query_inner: string;
            bigram_bonus_inner: Integer;
            trigram_bonus_inner: Integer;
            boundary_bonus_inner: Integer;
            boundary_count_inner: Integer;
            total_bonus_inner: Integer;
        begin
            Result := 0;
            stats_inner := Default(TLongWordLmBoundaryStats);
            stats_inner.min_bonus := MaxInt;
            if (m_dictionary = nil) or (syllable_count_local <= 5) or
                (state_start_pos < 0) or (state_inner.path = '') then
            begin
                Exit;
            end;

            path_segments_inner := state_inner.path.Split(
                [c_segment_path_separator]);
            if Length(path_segments_inner) < 2 then
            begin
                Exit;
            end;

            query_offset_inner := state_start_pos;
            previous_text_inner := '';
            previous_units_inner := 0;
            boundary_count_inner := 0;
            total_bonus_inner := 0;
            for segment_idx_inner := 0 to High(path_segments_inner) - 1 do
            begin
                current_text_inner := Trim(path_segments_inner[segment_idx_inner]);
                next_text_inner := Trim(path_segments_inner[segment_idx_inner + 1]);
                current_units_inner := get_candidate_text_unit_count(
                    current_text_inner);
                next_units_inner := get_candidate_text_unit_count(next_text_inner);
                if (current_text_inner = '') or (next_text_inner = '') or
                    (current_units_inner <= 0) or (next_units_inner <= 0) or
                    (query_offset_inner + current_units_inner + next_units_inner >
                    syllable_count_local) then
                begin
                    Exit(0);
                end;

                transition_query_inner := build_query_key_local(
                    query_offset_inner, current_units_inner + next_units_inner);
                bigram_bonus_inner := m_dictionary.get_lm_transition_bonus(
                    transition_query_inner, current_text_inner +
                    c_segment_path_separator + next_text_inner);
                trigram_bonus_inner := 0;
                if (segment_idx_inner > 0) and
                    (previous_text_inner <> '') and (previous_units_inner > 0) then
                begin
                    transition_query_inner := build_query_key_local(
                        query_offset_inner - previous_units_inner,
                        previous_units_inner + current_units_inner +
                        next_units_inner);
                    trigram_bonus_inner := m_dictionary.get_lm_transition_bonus(
                        transition_query_inner, previous_text_inner +
                        c_segment_path_separator + current_text_inner +
                        c_segment_path_separator + next_text_inner);
                end;

                boundary_bonus_inner := Max(bigram_bonus_inner,
                    trigram_bonus_inner);
                if boundary_count_inner = 0 then
                begin
                    stats_inner.first_bonus := boundary_bonus_inner;
                end;
                stats_inner.last_bonus := boundary_bonus_inner;
                stats_inner.min_bonus := Min(stats_inner.min_bonus,
                    boundary_bonus_inner);
                stats_inner.max_bonus := Max(stats_inner.max_bonus,
                    boundary_bonus_inner);
                if boundary_bonus_inner > 0 then
                begin
                    Inc(stats_inner.supported_count);
                end
                else
                begin
                    Inc(stats_inner.zero_count);
                end;
                if boundary_bonus_inner >= c_chain_lattice_lm_min_bonus then
                begin
                    Inc(stats_inner.strong_count);
                end;
                if trigram_bonus_inner > 0 then
                begin
                    Inc(stats_inner.trigram_count);
                end;
                Inc(total_bonus_inner, boundary_bonus_inner);
                Inc(boundary_count_inner);
                previous_text_inner := current_text_inner;
                previous_units_inner := current_units_inner;
                Inc(query_offset_inner, current_units_inner);
            end;

            if boundary_count_inner > 0 then
            begin
                stats_inner.boundary_count := boundary_count_inner;
                Result := total_bonus_inner div boundary_count_inner;
            end;
            if stats_inner.min_bonus = MaxInt then
            begin
                stats_inner.min_bonus := 0;
            end;
        end;

        function get_exact_chunk_chain_state_head_lm_bonus_local(
            const state_inner: TExactChunkChainState;
            const state_start_pos: Integer): Integer;
        var
            path_segments_inner: TArray<string>;
            first_text_inner: string;
            second_text_inner: string;
            third_text_inner: string;
            first_units_inner: Integer;
            second_units_inner: Integer;
            third_units_inner: Integer;
            transition_query_inner: string;
            bigram_bonus_inner: Integer;
            trigram_bonus_inner: Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (syllable_count_local <= 5) or
                (state_start_pos < 0) or (state_inner.path = '') then
            begin
                Exit;
            end;

            path_segments_inner := state_inner.path.Split(
                [c_segment_path_separator]);
            if Length(path_segments_inner) < 2 then
            begin
                Exit;
            end;

            first_text_inner := Trim(path_segments_inner[0]);
            second_text_inner := Trim(path_segments_inner[1]);
            first_units_inner := get_candidate_text_unit_count(first_text_inner);
            second_units_inner := get_candidate_text_unit_count(second_text_inner);
            if (first_text_inner = '') or (second_text_inner = '') or
                (first_units_inner <= 0) or (second_units_inner <= 0) or
                (state_start_pos + first_units_inner + second_units_inner >
                syllable_count_local) then
            begin
                Exit;
            end;

            transition_query_inner := build_query_key_local(state_start_pos,
                first_units_inner + second_units_inner);
            bigram_bonus_inner := m_dictionary.get_lm_transition_bonus(
                transition_query_inner, first_text_inner +
                c_segment_path_separator + second_text_inner);
            trigram_bonus_inner := 0;
            if Length(path_segments_inner) >= 3 then
            begin
                third_text_inner := Trim(path_segments_inner[2]);
                third_units_inner := get_candidate_text_unit_count(third_text_inner);
                if (third_text_inner <> '') and (third_units_inner > 0) and
                    (state_start_pos + first_units_inner + second_units_inner +
                    third_units_inner <= syllable_count_local) then
                begin
                    transition_query_inner := build_query_key_local(
                        state_start_pos, first_units_inner + second_units_inner +
                        third_units_inner);
                    trigram_bonus_inner := m_dictionary.get_lm_transition_bonus(
                        transition_query_inner, first_text_inner +
                        c_segment_path_separator + second_text_inner +
                        c_segment_path_separator + third_text_inner);
                end;
            end;
            Result := Max(bigram_bonus_inner, trigram_bonus_inner);
        end;

        procedure consider_exact_chunk_chain_state_local(
            var states: TExactChunkChainStateArray;
            const candidate_state: TExactChunkChainState;
            const state_start_pos: Integer;
            const state_limit: Integer = 0);
        var
            state_idx: Integer;
            insert_idx: Integer;
            existing_state: TExactChunkChainState;
            target_suffix: string;
            target_suffix_units: Integer;
            candidate_is_target_suffix: Boolean;
            lm_idx: Integer;
            lm_best_idx: Integer;
            lm_tail_idx: Integer;
            lm_best_bonus: Integer;
            lm_tail_bonus: Integer;
            lm_current_bonus: Integer;
            lm_base_gap: Integer;
            effective_state_limit: Integer;
            beam_width_local: Integer;
            prebeam_width_local: Integer;
            baseline_prebeam_width_local: Integer;
            baseline_count_local: Integer;
            removal_idx_local: Integer;
            shift_idx_local: Integer;
            reposition_replaced_state_local: Boolean;
        begin
            if candidate_state.text = '' then
            begin
                Exit;
            end;

            candidate_is_target_suffix := False;
            target_suffix_units := 0;
            beam_width_local := get_chain_beam_width_local;
            prebeam_width_local := get_chain_prebeam_width_local;
            effective_state_limit := state_limit;
            if effective_state_limit <= 0 then
            begin
                effective_state_limit := beam_width_local;
            end;
            if (m_debug_target_recall_text <> '') and
                (state_start_pos >= 0) then
            begin
                target_suffix := Copy(m_debug_target_recall_text,
                    state_start_pos + 1, MaxInt);
                candidate_is_target_suffix := SameText(
                    Trim(candidate_state.text), target_suffix);
                if candidate_is_target_suffix then
                begin
                    target_suffix_units := get_candidate_text_unit_count(
                        target_suffix);
                    m_debug_target_max_generated_syllable := Max(
                        m_debug_target_max_generated_syllable,
                        target_suffix_units);
                    m_debug_target_chain_generated_syllable := Max(
                        m_debug_target_chain_generated_syllable,
                        target_suffix_units);
                end;
            end;

            for state_idx := 0 to High(states) do
            begin
                if SameText(states[state_idx].text, candidate_state.text) then
                begin
                    reposition_replaced_state_local := False;
                    if candidate_state.baseline_lineage and
                        (not states[state_idx].baseline_lineage) then
                    begin
                        states[state_idx] := candidate_state;
                        reposition_replaced_state_local := True;
                    end
                    else if states[state_idx].baseline_lineage and
                        (not candidate_state.baseline_lineage) then
                    begin
                        { Preserve the exact baseline continuation. }
                    end
                    else if is_exact_chunk_chain_state_better_local(
                        candidate_state, states[state_idx]) then
                    begin
                        states[state_idx] := candidate_state;
                    end;
                    if reposition_replaced_state_local then
                    begin
                        insert_idx := state_idx;
                        while (insert_idx > 0) and
                            is_exact_chunk_chain_state_better_local(
                            states[insert_idx], states[insert_idx - 1]) do
                        begin
                            existing_state := states[insert_idx - 1];
                            states[insert_idx - 1] := states[insert_idx];
                            states[insert_idx] := existing_state;
                            Dec(insert_idx);
                        end;
                        while (insert_idx < High(states)) and
                            is_exact_chunk_chain_state_better_local(
                            states[insert_idx + 1], states[insert_idx]) do
                        begin
                            existing_state := states[insert_idx + 1];
                            states[insert_idx + 1] := states[insert_idx];
                            states[insert_idx] := existing_state;
                            Inc(insert_idx);
                        end;
                    end;
                    if candidate_is_target_suffix then
                    begin
                        m_debug_target_max_retained_syllable := Max(
                            m_debug_target_max_retained_syllable,
                            get_candidate_text_unit_count(target_suffix));
                        if effective_state_limit = prebeam_width_local then
                        begin
                            m_debug_target_chain_prebeam_syllable := Max(
                                m_debug_target_chain_prebeam_syllable,
                                target_suffix_units);
                        end
                        else if effective_state_limit = beam_width_local then
                        begin
                            m_debug_target_chain_beam_syllable := Max(
                                m_debug_target_chain_beam_syllable,
                                target_suffix_units);
                        end;
                    end;
                    Exit;
                end;
            end;

            SetLength(states, Length(states) + 1);
            states[High(states)] := candidate_state;
            for insert_idx := High(states) downto 1 do
            begin
                if not is_exact_chunk_chain_state_better_local(states[insert_idx],
                    states[insert_idx - 1]) then
                begin
                    Break;
                end;
                existing_state := states[insert_idx - 1];
                states[insert_idx - 1] := states[insert_idx];
                states[insert_idx] := existing_state;
            end;

            if Length(states) > effective_state_limit then
            begin
                if (effective_state_limit = beam_width_local) and
                    (state_start_pos > 0) then
                begin
                    lm_best_idx := -1;
                    lm_best_bonus := 0;
                    lm_tail_idx := beam_width_local -
                        c_chain_lattice_lm_reserved_slots;
                    lm_tail_bonus :=
                        get_exact_chunk_chain_state_head_lm_bonus_local(
                        states[lm_tail_idx], state_start_pos);
                    for lm_idx := beam_width_local to High(states) do
                    begin
                        lm_current_bonus :=
                            get_exact_chunk_chain_state_head_lm_bonus_local(
                            states[lm_idx], state_start_pos);
                        if lm_current_bonus > lm_best_bonus then
                        begin
                            lm_best_bonus := lm_current_bonus;
                            lm_best_idx := lm_idx;
                        end;
                    end;

                    if (lm_best_idx >= beam_width_local) and
                        (lm_best_bonus >= c_chain_lattice_lm_min_bonus) and
                        (lm_best_bonus >= lm_tail_bonus +
                        c_chain_lattice_lm_min_gain) then
                    begin
                        lm_base_gap := states[lm_tail_idx].score -
                            states[lm_best_idx].score;
                        if lm_base_gap <= c_chain_lattice_lm_max_base_gap then
                        begin
                            states[lm_tail_idx] := states[lm_best_idx];
                        end;
                    end;
                end;
                removal_idx_local := High(states);
                if (effective_state_limit = prebeam_width_local) and
                    (prebeam_width_local >
                    get_chain_baseline_prebeam_width_local) then
                begin
                    baseline_prebeam_width_local :=
                        get_chain_baseline_prebeam_width_local;
                    baseline_count_local := 0;
                    for state_idx := 0 to High(states) do
                    begin
                        if states[state_idx].baseline_lineage then
                        begin
                            Inc(baseline_count_local);
                        end;
                    end;
                    if baseline_count_local <= baseline_prebeam_width_local then
                    begin
                        removal_idx_local := -1;
                        for state_idx := High(states) downto 0 do
                        begin
                            if not states[state_idx].baseline_lineage then
                            begin
                                removal_idx_local := state_idx;
                                Break;
                            end;
                        end;
                        if removal_idx_local < 0 then
                        begin
                            removal_idx_local := High(states);
                        end;
                    end;
                end;
                for shift_idx_local := removal_idx_local to
                    High(states) - 1 do
                begin
                    states[shift_idx_local] := states[shift_idx_local + 1];
                end;
                SetLength(states, Length(states) - 1);
            end;

            if candidate_is_target_suffix then
            begin
                for state_idx := 0 to High(states) do
                begin
                    if SameText(Trim(states[state_idx].text), target_suffix) then
                    begin
                        m_debug_target_max_retained_syllable := Max(
                            m_debug_target_max_retained_syllable,
                            get_candidate_text_unit_count(target_suffix));
                        if effective_state_limit = prebeam_width_local then
                        begin
                            m_debug_target_chain_prebeam_syllable := Max(
                                m_debug_target_chain_prebeam_syllable,
                                target_suffix_units);
                        end
                        else if effective_state_limit = beam_width_local then
                        begin
                            m_debug_target_chain_beam_syllable := Max(
                                m_debug_target_chain_beam_syllable,
                                target_suffix_units);
                        end;
                        Break;
                    end;
                end;
            end;
        end;

        procedure consider_exact_chunk_option_local(
            var options: TExactChunkChainOptionArray;
            const candidate_option: TExactChunkChainOption);
        var
            option_idx: Integer;
        begin
            if candidate_option.text = '' then
            begin
                Exit;
            end;

            for option_idx := 0 to High(options) do
            begin
                if SameText(options[option_idx].text, candidate_option.text) then
                begin
                    if candidate_option.score > options[option_idx].score then
                    begin
                        options[option_idx] := candidate_option;
                    end;
                    Exit;
                end;
            end;

            SetLength(options, Length(options) + 1);
            options[High(options)] := candidate_option;
        end;

        function has_exact_chunk_merge_bonus_local(const left_query_key: string;
            const right_query_key: string; const combined_text: string;
            const expected_units: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            if (left_query_key = '') or (right_query_key = '') or
                (combined_text = '') or (expected_units <= 0) then
            begin
                Exit;
            end;

            if not lookup_exact_full_pinyin_cached_local(left_query_key +
                right_query_key, local_results) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_chain_lattice_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    local_results[local_idx].dict_weight,
                    local_results[local_idx].has_dict_weight) then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if (local_text = '') or
                    (get_candidate_text_unit_count(local_text) <> expected_units) then
                begin
                    Continue;
                end;

                if SameText(local_text, combined_text) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_collect_exact_chunk_options_local(const start_idx: Integer;
            const syllable_count_part: Integer;
            out options: TExactChunkChainOptionArray): Boolean;
        var
            query_key_local: string;
            local_results: TncCandidateList;
            ranked_lookup_results_local: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
            ranked_score_local: Integer;
            local_option: TExactChunkChainOption;
            fixed_single_text_local: string;

                function is_place_noun_unit_for_chunk_context_local(
                    const unit_value: string): Boolean;
                begin
                    Result := (unit_value = string(Char($5BB6))) or
                        (unit_value = string(Char($5E97))) or
                        (unit_value = string(Char($5C4B))) or
                        (unit_value = string(Char($697C))) or
                        (unit_value = string(Char($57CE))) or
                        (unit_value = string(Char($6751))) or
                        (unit_value = string(Char($53BF))) or
                        (unit_value = string(Char($5E02))) or
                        (unit_value = string(Char($6821))) or
                        (unit_value = string(Char($9662))) or
                        (unit_value = string(Char($5382))) or
                        (unit_value = string(Char($9986))) or
                        (unit_value = string(Char($7AD9))) or
                        (unit_value = string(Char($5C40))) or
                        (unit_value = string(Char($6240))) or
                        (unit_value = string(Char($56ED))) or
                        (unit_value = string(Char($533A))) or
                        (unit_value = string(Char($5708))) or
                        (unit_value = string(Char($8DEF))) or
                        (unit_value = string(Char($8857))) or
                        (unit_value = string(Char($95E8))) or
                        (unit_value = string(Char($5BA4))) or
                        (unit_value = string(Char($623F))) or
                        (unit_value = string(Char($5385))) or
                        (unit_value = string(Char($573A))) or
                        (unit_value = string(Char($53F8)));
                end;

                function has_place_context_before_chunk_local: Boolean;
                const
                    c_context_probe_limit = 8;
                var
                    back_len_local: Integer;
                    context_key_local: string;
                    context_results_local: TncCandidateList;
                    result_idx_local: Integer;
                    context_text_local: string;
                    context_units_local: TArray<string>;
                begin
                    Result := False;
                    if start_idx <= 0 then
                    begin
                        Exit;
                    end;

                    for back_len_local := 1 to Min(4, start_idx) do
                    begin
                        context_key_local := build_query_key_local(start_idx -
                            back_len_local, back_len_local);
                        if (context_key_local = '') or
                            (not lookup_exact_full_pinyin_cached_local(
                            context_key_local, context_results_local)) then
                        begin
                            Continue;
                        end;

                        for result_idx_local := 0 to High(context_results_local) do
                        begin
                            if result_idx_local >= c_context_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(context_results_local[result_idx_local].comment) <>
                                '' then
                            begin
                                Continue;
                            end;

                            context_text_local := Trim(
                                context_results_local[result_idx_local].text);
                            context_units_local := split_text_units(
                                context_text_local);
                            if (Length(context_units_local) > 0) and
                                is_place_noun_unit_for_chunk_context_local(
                                context_units_local[High(context_units_local)]) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                function has_directional_locative_chunk_context_local: Boolean;
                var
                    next_key_local: string;
                begin
                    Result := False;
                    if (syllable_count_part <> 2) or
                        (not has_place_context_before_chunk_local) then
                    begin
                        Exit;
                    end;

                    if start_idx + syllable_count_part >= syllable_count_local then
                    begin
                        Exit(True);
                    end;

                    next_key_local := normalize_pinyin_text(
                        effective_syllables[start_idx +
                        syllable_count_part].text);
                    Result := SameText(next_key_local, 'de');
                end;

                function get_directional_locative_chunk_bonus_local(
                    const text_value_local: string): Integer;
                const
                    c_chunk_directional_context_bonus = 12000;
                begin
                    Result := 0;
                    if (get_directional_locative_span_bonus_local(
                        text_value_local) > 0) and
                        has_directional_locative_chunk_context_local then
                    begin
                        Result := c_chunk_directional_context_bonus;
                    end;
                end;

                function should_defer_pair_to_modifier_tail_exact_local(
                    const pair_text_local: string): Boolean;
                var
                    prefix_text_local: string;
                    tail_key_local: string;
                    pair_units_local: TArray<string>;
                begin
                    Result := False;
                    if (syllable_count_part <> 2) or
                        (start_idx + 2 >= syllable_count_local) or
                        (pair_text_local = '') then
                    begin
                        Exit;
                    end;

                    if not try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx].text, prefix_text_local) then
                    begin
                        Exit;
                    end;

                    pair_units_local := split_text_units(Trim(pair_text_local));
                    if (Length(pair_units_local) <> 2) or
                        SameText(pair_units_local[0], prefix_text_local) then
                    begin
                        Exit;
                    end;

                    tail_key_local := build_query_key_local(start_idx + 1, 2);
                    Result := has_high_quality_exact_phrase_for_key_local(
                        tail_key_local, 2, 700);
                end;

            function is_deferable_function_tail_single_local(
                const text_value_local: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(text_value_local) <> 1 then
                begin
                    Exit;
                end;

                case Ord(text_value_local[1]) of
                    $7684, $5730, $5F97, $4E86, $7740, $8FC7:
                        Result := True;
                end;
            end;

            function should_defer_chunk_to_head_plus_function_tail_local(
                const chunk_text_local: string): Boolean;
            var
                chunk_units_local: TArray<string>;
                tail_text_local: string;
                head_key_local: string;
            begin
                Result := False;
                if (syllable_count_part < 3) or
                    (syllable_count_part > 4) or (chunk_text_local = '') then
                begin
                    Exit;
                end;

                chunk_units_local := split_text_units(Trim(chunk_text_local));
                if Length(chunk_units_local) <> syllable_count_part then
                begin
                    Exit;
                end;

                if not try_get_modifier_prefix_single_text_local(
                    effective_syllables[start_idx + syllable_count_part - 1].text,
                    tail_text_local) then
                begin
                    tail_text_local :=
                        get_fixed_boundary_single_char_for_standalone_local(
                        effective_syllables[start_idx + syllable_count_part - 1].text);
                end;

                if (tail_text_local = '') or
                    (not is_deferable_function_tail_single_local(tail_text_local)) or
                    (not SameText(chunk_units_local[High(chunk_units_local)],
                    tail_text_local)) then
                begin
                    Exit;
                end;

                head_key_local := build_query_key_local(start_idx,
                    syllable_count_part - 1);
                Result := has_high_quality_exact_phrase_for_key_local(
                    head_key_local, syllable_count_part - 1, 700);
            end;

            function should_defer_pair_started_inside_previous_exact_local(
                const pair_text_local: string): Boolean;
            var
                previous_key_local: string;
                expected_text_local: string;
                pair_units_local: TArray<string>;
            begin
                Result := False;
                if (syllable_count_part <> 2) or (start_idx <= 0) or
                    (start_idx + 1 >= syllable_count_local) or
                    (pair_text_local = '') then
                begin
                    Exit;
                end;

                if not try_get_modifier_prefix_single_text_local(
                    effective_syllables[start_idx + 1].text,
                    expected_text_local) then
                begin
                    Exit;
                end;

                pair_units_local := split_text_units(Trim(pair_text_local));
                if (Length(pair_units_local) <> 2) or
                    SameText(pair_units_local[1], expected_text_local) then
                begin
                    Exit;
                end;

                previous_key_local := build_query_key_local(start_idx - 1, 2);
                Result := has_high_quality_exact_phrase_for_key_local(
                    previous_key_local, 2, 700);
            end;

            function get_ranked_lookup_score_for_text_local(
                const text_value_local: string): Integer;
            var
                ranked_idx_local: Integer;
                ranked_text_local: string;
            begin
                Result := Low(Integer);
                if (text_value_local = '') or
                    (Length(ranked_lookup_results_local) = 0) then
                begin
                    Exit;
                end;

                for ranked_idx_local := 0 to Min(High(ranked_lookup_results_local),
                    c_chain_probe_limit * 4 - 1) do
                begin
                    if Trim(ranked_lookup_results_local[ranked_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    ranked_text_local := Trim(
                        ranked_lookup_results_local[ranked_idx_local].text);
                    if (ranked_text_local = '') or
                        (get_candidate_text_unit_count(ranked_text_local) <>
                        syllable_count_part) or
                        (not SameText(ranked_text_local, text_value_local)) then
                    begin
                        Continue;
                    end;

                    Result := get_exact_chunk_effective_weight_local(
                        ranked_lookup_results_local[ranked_idx_local]) +
                        ((c_chain_probe_limit * 4 - ranked_idx_local) * 512);
                    Exit;
                end;
            end;

            procedure add_fixed_single_option_local(const single_text_local: string;
                const score_penalty_local: Integer);
            var
                scan_idx_local: Integer;
                single_score_local: Integer;
                single_option_local: TExactChunkChainOption;
            begin
                if single_text_local = '' then
                begin
                    Exit;
                end;

                single_score_local := 1200;
                if lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    for scan_idx_local := 0 to High(local_results) do
                    begin
                        if scan_idx_local >= c_chain_probe_limit then
                        begin
                            Break;
                        end;
                        if SameText(Trim(local_results[scan_idx_local].text),
                            single_text_local) then
                        begin
                            single_score_local :=
                                get_exact_chunk_effective_weight_local(
                                local_results[scan_idx_local]);
                            Break;
                        end;
                    end;
                end;

                Inc(single_score_local, get_preferred_query_phrase_bonus_local(
                    query_key_local, single_text_local));
                Inc(single_score_local, get_exact_query_text_bonus_local(
                    query_key_local, single_text_local));
                Dec(single_score_local, score_penalty_local);

                single_option_local := Default(TExactChunkChainOption);
                single_option_local.text := single_text_local;
                single_option_local.path := single_text_local;
                single_option_local.query_key := query_key_local;
                single_option_local.score := single_score_local +
                    c_chain_lattice_fixed_single_bonus;
                single_option_local.segments := 1;
                single_option_local.has_anchor := True;
                single_option_local.anchor_units := 1;
                consider_exact_chunk_option_local(options, single_option_local);
            end;
        begin
            Result := False;
            SetLength(options, 0);
            query_key_local := build_query_key_local(start_idx, syllable_count_part);
            if query_key_local = '' then
            begin
                Exit;
            end;

            if syllable_count_part = 1 then
            begin
                fixed_single_text_local :=
                    get_fixed_boundary_single_char_for_standalone_local(
                    effective_syllables[start_idx].text);
                if fixed_single_text_local <> '' then
                begin
                    add_fixed_single_option_local(fixed_single_text_local, 0);
                end;
                Exit(Length(options) > 0);
            end;

            if (input_syllable_count <= 6) and (m_dictionary <> nil) and
                (not m_dictionary.lookup(query_key_local,
                ranked_lookup_results_local)) then
            begin
                ranked_lookup_results_local := nil;
            end;

            if lookup_exact_full_pinyin_cached_local(query_key_local, local_results) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_probe_limit * 4 then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results[local_idx].dict_weight,
                        local_results[local_idx].has_dict_weight) then
                    begin
                        Continue;
                    end;

                    local_text := Trim(local_results[local_idx].text);
                    if (local_text = '') or
                        (get_candidate_text_unit_count(local_text) <>
                        syllable_count_part) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        local_text, syllable_count_part)) then
                    begin
                        Continue;
                    end;

                    if should_defer_pair_to_modifier_tail_exact_local(local_text) then
                    begin
                        Continue;
                    end;
                    if should_defer_chunk_to_head_plus_function_tail_local(
                        local_text) then
                    begin
                        Continue;
                    end;
                    if should_defer_pair_started_inside_previous_exact_local(
                        local_text) then
                    begin
                        Continue;
                    end;

                    local_score := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight > local_score) then
                    begin
                        local_score := local_results[local_idx].dict_weight;
                    end;
                    if syllable_count_part <= 2 then
                    begin
                        case local_idx of
                            0:
                                Inc(local_score, 4096);
                            1:
                                Inc(local_score, 1536);
                            2, 3:
                                Dec(local_score, 1024);
                        else
                            Dec(local_score, 2048);
                        end;
                    end
                    else
                    begin
                        if local_idx = 0 then
                        begin
                            Inc(local_score, 7200);
                        end;
                        case local_idx of
                            0:
                                Inc(local_score, 6144);
                            1:
                                Inc(local_score, 3072);
                            2, 3:
                                Inc(local_score, 1536);
                        else
                            Inc(local_score, 768);
                        end;
                    end;
                    ranked_score_local :=
                        get_ranked_lookup_score_for_text_local(local_text);
                    if ranked_score_local > local_score then
                    begin
                        local_score := ranked_score_local;
                    end;
                    Inc(local_score, get_preferred_query_phrase_bonus_local(
                        query_key_local, local_text));
                    Inc(local_score, get_exact_chunk_length_bonus_local(
                        syllable_count_part));
                    Inc(local_score, get_long_exact_phrase_cohesion_bonus_local(
                        syllable_count_part));
                    Inc(local_score, get_modifier_prefix_phrase_bonus_local(
                        syllable_count_part, local_text));
                    Inc(local_score, get_exact_chunk_boundary_adjustment_local(
                        start_idx, syllable_count_part, local_text));
                    Inc(local_score, get_exact_chunk_semantic_bonus_local(local_text));
                    Inc(local_score, get_directional_locative_chunk_bonus_local(
                        local_text));
                    if syllable_count_part >= c_chain_min_anchor_syllables then
                    begin
                        Inc(local_score, c_chain_anchor_bonus);
                    end;
                    local_option := Default(TExactChunkChainOption);
                    local_option.text := local_text;
                    local_option.path := local_text;
                    local_option.query_key := query_key_local;
                    local_option.score := local_score;
                    local_option.segments := 1;
                    local_option.has_anchor :=
                        syllable_count_part >= c_chain_min_anchor_syllables;
                    if local_option.has_anchor then
                    begin
                        local_option.anchor_units := syllable_count_part;
                    end
                    else
                    begin
                        local_option.anchor_units := 0;
                    end;
                    consider_exact_chunk_option_local(options, local_option);
                end;
            end;

            if (Length(options) = 0) and (syllable_count_part = 3) and
                try_resolve_three_syllable_split_option_local(start_idx,
                local_option) then
            begin
                consider_exact_chunk_option_local(options, local_option);
            end;

            // Do not synthesize arbitrary 3-4 syllable chunks here. The exact
            // chain may combine lexicon/user words and intentional 3-syllable
            // AB/C or A/BC splits, but recursive standalone chunks create
            // invalid phrases such as "鐨勪細鎺ヨ" and dominate real paths.

            if m_config.debug_mode and (Length(options) > 0) then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xopt=[key=%s n=%d', [query_key_local, Length(options)]);
                for local_idx := 0 to Min(High(options), 2) do
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' {%d:%s s=%d seg=%d}',
                        [local_idx, options[local_idx].text, options[local_idx].score,
                        options[local_idx].segments]);
                end;
                m_last_full_path_debug_info := m_last_full_path_debug_info + ']';
            end;

            Result := Length(options) > 0;
        end;

        function try_build_exact_chunk_lattice_local(out out_text: string;
            out out_path: string; out out_score: Integer;
            out out_segments: Integer; out out_has_anchor: Boolean;
            out out_anchor_units: Integer): Boolean;
        var
            start_idx: Integer;
            next_idx: Integer;
            chunk_syllables: Integer;
            option_idx: Integer;
            state_idx: Integer;
            options: TExactChunkChainOptionArray;
            suffix_states: TExactChunkChainStateArray;
            candidate_state: TExactChunkChainState;
            combined_score: Integer;
            combined_text: string;
            combined_path: string;
            combined_anchor_units: Integer;
            combined_has_anchor: Boolean;
            combined_segments: Integer;
            option_units_local: TArray<string>;
            option_raw_local: Integer;
            option_last_syllable_local: string;
            suffix_first_units_local: Integer;
            function_context_adjust_local: Integer;
            prefix_context_adjust_local: Integer;
            alternative_text_local: string;
            alternative_suffix_path_local: string;
            alternative_adjust_local: Integer;
            alternative_score_local: Integer;
            alternative_state_local: TExactChunkChainState;
            function get_lattice_exact_phrase_raw_weight_local(
                const query_key_local: string; const expected_units_local: Integer;
                const text_local: string): Integer;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                local_text: string;
            begin
                Result := 0;
                if (query_key_local = '') or (text_local = '') then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    local_text := Trim(local_results[local_idx].text);
                    if (get_candidate_text_unit_count(local_text) =
                        expected_units_local) and SameText(local_text, text_local) then
                    begin
                        Exit(get_exact_chunk_raw_weight_local(
                            local_results[local_idx]));
                    end;
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := Low(Integer);
            out_segments := MaxInt;
            out_has_anchor := False;
            out_anchor_units := -1;

            SetLength(lattice_state_map, syllable_count_local + 1);
            SetLength(lattice_state_map[syllable_count_local], 1);
            lattice_state_map[syllable_count_local][0] :=
                Default(TExactChunkChainState);
            lattice_state_map[syllable_count_local][0].score := 0;
            lattice_state_map[syllable_count_local][0].segments := 0;
            lattice_state_map[syllable_count_local][0].anchor_units := -1;

            for start_idx := syllable_count_local - 1 downto 0 do
            begin
                SetLength(lattice_state_map[start_idx], 0);
                for chunk_syllables := 1 to Min(c_chain_lattice_max_chunk_syllables,
                    syllable_count_local - start_idx) do
                begin
                    next_idx := start_idx + chunk_syllables;
                    if (next_idx < syllable_count_local) and
                        (Length(lattice_state_map[next_idx]) = 0) then
                    begin
                        Continue;
                    end;

                    if not try_collect_exact_chunk_options_local(start_idx,
                        chunk_syllables, options) then
                    begin
                        Continue;
                    end;

                    if next_idx < syllable_count_local then
                    begin
                        suffix_states := lattice_state_map[next_idx];
                    end
                    else
                    begin
                        suffix_states := lattice_state_map[syllable_count_local];
                    end;

                    for option_idx := 0 to High(options) do
                    begin
                        for state_idx := 0 to High(suffix_states) do
                        begin
                            combined_score := options[option_idx].score +
                                suffix_states[state_idx].score;
                            if (suffix_states[state_idx].first_chunk_text <> '') and
                                (m_dictionary <> nil) then
                            begin
                                Inc(combined_score, m_dictionary.get_context_bonus(
                                    options[option_idx].text,
                                    suffix_states[state_idx].first_chunk_text));
                                Inc(combined_score,
                                    get_adjacent_exact_chunk_context_bonus_local(
                                    options[option_idx].text,
                                    suffix_states[state_idx].first_chunk_text));
                                if has_exact_chunk_merge_bonus_local(
                                    options[option_idx].query_key,
                                    suffix_states[state_idx].first_chunk_query_key,
                                    options[option_idx].text +
                                    suffix_states[state_idx].first_chunk_text,
                                    get_candidate_text_unit_count(
                                    options[option_idx].text) +
                                    get_candidate_text_unit_count(
                                    suffix_states[state_idx].first_chunk_text)) then
                                begin
                                    Inc(combined_score, c_chain_lattice_merge_bonus);
                                end;
                            end;
                            function_context_adjust_local := 0;
                            if suffix_states[state_idx].first_chunk_text <> '' then
                            begin
                                function_context_adjust_local :=
                                    get_function_particle_suffix_context_adjustment_local(
                                    effective_syllables[start_idx + chunk_syllables -
                                    1].text,
                                    options[option_idx].text,
                                    suffix_states[state_idx].first_chunk_text);
                                Inc(combined_score, function_context_adjust_local);
                                if starts_with_context_text_unit_local(
                                    suffix_states[state_idx].text, $5730) or
                                    starts_with_context_text_unit_local(
                                    suffix_states[state_idx].text, $5F97) or
                                    starts_with_context_text_unit_local(
                                    suffix_states[state_idx].text, $4E3A) or
                                    starts_with_context_text_unit_local(
                                    suffix_states[state_idx].text, $672A) then
                                begin
                                    prefix_context_adjust_local :=
                                        get_function_particle_prefix_context_adjustment_local(
                                        options[option_idx].text,
                                        suffix_states[state_idx].text);
                                    if prefix_context_adjust_local <= -90000 then
                                    begin
                                        Continue;
                                    end;
                                    Inc(combined_score, prefix_context_adjust_local);
                                end;
                            end;

                            suffix_first_units_local := get_candidate_text_unit_count(
                                Trim(suffix_states[state_idx].first_chunk_text));
                            option_units_local := split_text_units(
                                Trim(options[option_idx].text));
                            if (Length(option_units_local) = chunk_syllables) and
                                (chunk_syllables >= 2) and
                                (suffix_first_units_local >= 2) and
                                ((SameText(option_units_local[
                                High(option_units_local)], string(Char($5730)))) or
                                (SameText(option_units_local[
                                High(option_units_local)], string(Char($5F97))))) then
                            begin
                                option_last_syllable_local := normalize_pinyin_text(
                                    effective_syllables[start_idx + chunk_syllables -
                                    1].text);
                                option_raw_local := get_lattice_exact_phrase_raw_weight_local(
                                    options[option_idx].query_key, chunk_syllables,
                                    options[option_idx].text);
                                if (option_last_syllable_local = 'de') and
                                    ((option_raw_local <= 0) or
                                    (option_raw_local < 700)) then
                                begin
                                    Dec(combined_score, 30000);
                                end;
                            end;

                            combined_text := options[option_idx].text +
                                suffix_states[state_idx].text;
                            combined_path := options[option_idx].path;
                            if suffix_states[state_idx].path <> '' then
                            begin
                                if combined_path <> '' then
                                begin
                                    combined_path := combined_path +
                                        c_segment_path_separator;
                                end;
                                combined_path := combined_path +
                                    suffix_states[state_idx].path;
                            end;
                            combined_segments := options[option_idx].segments +
                                suffix_states[state_idx].segments;
                            combined_has_anchor := options[option_idx].has_anchor or
                                suffix_states[state_idx].has_anchor;
                            combined_anchor_units := options[option_idx].anchor_units;
                            if suffix_states[state_idx].anchor_units >
                                combined_anchor_units then
                            begin
                                combined_anchor_units :=
                                    suffix_states[state_idx].anchor_units;
                            end;

                            candidate_state := Default(TExactChunkChainState);
                            candidate_state.text := combined_text;
                            candidate_state.path := combined_path;
                            candidate_state.first_chunk_text :=
                                options[option_idx].text;
                            candidate_state.first_chunk_query_key :=
                                options[option_idx].query_key;
                            candidate_state.score := combined_score;
                            candidate_state.segments := combined_segments;
                            candidate_state.has_anchor := combined_has_anchor;
                            candidate_state.anchor_units := combined_anchor_units;
                            consider_exact_chunk_chain_state_local(
                                lattice_state_map[start_idx], candidate_state,
                                start_idx);

                            if try_get_function_particle_suffix_head_alternative_local(
                                options[option_idx].text,
                                suffix_states[state_idx].text,
                                suffix_states[state_idx].path,
                                suffix_states[state_idx].first_chunk_text,
                                suffix_states[state_idx].first_chunk_query_key,
                                alternative_text_local,
                                alternative_suffix_path_local,
                                alternative_adjust_local) then
                            begin
                                alternative_state_local := candidate_state;
                                alternative_state_local.text :=
                                    options[option_idx].text +
                                    alternative_text_local;
                                alternative_state_local.path :=
                                    options[option_idx].path;
                                if alternative_suffix_path_local <> '' then
                                begin
                                    if alternative_state_local.path <> '' then
                                    begin
                                        alternative_state_local.path :=
                                            alternative_state_local.path +
                                            c_segment_path_separator;
                                    end;
                                    alternative_state_local.path :=
                                        alternative_state_local.path +
                                        alternative_suffix_path_local;
                                end;
                                alternative_state_local.score :=
                                    combined_score + alternative_adjust_local;
                                consider_exact_chunk_chain_state_local(
                                    lattice_state_map[start_idx],
                                    alternative_state_local, start_idx);
                            end;

                            if (options[option_idx].segments = 1) and
                                (suffix_states[state_idx].first_chunk_text <> '') and
                                try_get_function_particle_boundary_alternative_local(
                                effective_syllables[start_idx + chunk_syllables -
                                1].text,
                                options[option_idx].text,
                                suffix_states[state_idx].first_chunk_text,
                                alternative_text_local, alternative_adjust_local) then
                            begin
                                alternative_score_local := combined_score -
                                    function_context_adjust_local +
                                    alternative_adjust_local;
                                alternative_state_local := candidate_state;
                                alternative_state_local.text := alternative_text_local +
                                    suffix_states[state_idx].text;
                                alternative_state_local.path := alternative_text_local;
                                if suffix_states[state_idx].path <> '' then
                                begin
                                    alternative_state_local.path :=
                                        alternative_state_local.path +
                                        c_segment_path_separator +
                                        suffix_states[state_idx].path;
                                end;
                                alternative_state_local.first_chunk_text :=
                                    alternative_text_local;
                                alternative_state_local.score :=
                                    alternative_score_local;
                                consider_exact_chunk_chain_state_local(
                                    lattice_state_map[start_idx],
                                    alternative_state_local, start_idx);
                            end;
                        end;
                    end;
                end;
            end;

            if Length(lattice_state_map[0]) = 0 then
            begin
                Exit;
            end;

            out_text := lattice_state_map[0][0].text;
            out_path := lattice_state_map[0][0].path;
            out_score := lattice_state_map[0][0].score;
            out_segments := lattice_state_map[0][0].segments;
            out_has_anchor := lattice_state_map[0][0].has_anchor;
            out_anchor_units := lattice_state_map[0][0].anchor_units;
            Result := out_text <> '';
        end;

        function try_build_fast_dictionary_sentence_candidate_local(
            out out_text: string; out out_path: string; out out_score: Integer;
            out out_segments: Integer;
            out out_states: TExactChunkChainStateArray): Boolean;
        const
            c_fast_max_chunk_syllables = 5;
            c_fast_split_probe_limit = 6;
            c_fast_length_bonus = 2300;
            c_fast_fixed_single_bonus = 1200;
            c_fast_segment_penalty = 720;
            c_fast_completion_bonus = 2200;
        var
            state_map_local: array of TExactChunkChainStateArray;
            retained_state_map_local: array of TExactChunkChainStateArray;
            baseline_candidates_local: TExactChunkChainStateArray;
            legacy_candidates_local: TExactChunkChainStateArray;
            edge_probe_candidates_local: TExactChunkChainStateArray;
            exact_edge_model_candidates_local:
                TExactChunkChainStateArray;
            pos_local: Integer;
            chunk_len_local: Integer;
            next_pos_local: Integer;
            options_local: TExactChunkChainOptionArray;
            probe_options_local: TExactChunkChainOptionArray;
            option_idx_local: Integer;
            suffix_idx_local: Integer;
            suffix_count_local: Integer;
            debug_state_idx_local: Integer;
            candidate_state_local: TExactChunkChainState;
            suffix_state_local: TExactChunkChainState;
            base_state_local: TExactChunkChainState;
            head_two_key_local: string;
            head_two_text_local: string;
            head_two_raw_local: Integer;
            suffix_first_raw_local: Integer;
            suffix_first_units_local: Integer;
            current_pair_text_local: string;
            natural_modifier_phrase_boundary_local: Boolean;
            state_debug_text_local: string;
            function_context_adjust_local: Integer;
            prefix_context_adjust_local: Integer;
            alternative_text_local: string;
            alternative_suffix_path_local: string;
            alternative_adjust_local: Integer;
            alternative_score_local: Integer;
            alternative_state_local: TExactChunkChainState;
            greedy_state_local: TExactChunkChainState;
            greedy_state_found_local: Boolean;
            greedy_state_present_local: Boolean;
            prefix_state_local: TExactChunkChainState;
            prefix_state_found_local: Boolean;
            prefix_state_present_local: Boolean;
            retained_slot_states_local: TExactChunkChainStateArray;
            fast_seed_requires_dp_local: Boolean;
            minimum_score_local: Integer;
            baseline_acceptance_score_local: Integer;
            baseline_acceptance_found_local: Boolean;
            exact_edge_reserved_slots_local: Integer;

            function try_get_sentence_final_modal_particle_text_fast_local(
                const syllable_key_local: string; out out_text_local: string): Boolean;
            var
                normalized_key_local: string;
            begin
                out_text_local := '';
                normalized_key_local := normalize_pinyin_text(syllable_key_local);
                if normalized_key_local = 'ba' then
                begin
                    out_text_local := string(Char($5427));
                end
                else if normalized_key_local = 'ma' then
                begin
                    out_text_local := string(Char($5417));
                end
                else if normalized_key_local = 'ne' then
                begin
                    out_text_local := string(Char($5462));
                end
                else if normalized_key_local = 'a' then
                begin
                    out_text_local := string(Char($554A));
                end
                else if normalized_key_local = 'ya' then
                begin
                    out_text_local := string(Char($5440));
                end
                else if normalized_key_local = 'la' then
                begin
                    out_text_local := string(Char($5566));
                end
                else if normalized_key_local = 'le' then
                begin
                    out_text_local := string(Char($4E86));
                end
                else if normalized_key_local = 'wa' then
                begin
                    out_text_local := string(Char($54C7));
                end;
                Result := out_text_local <> '';
            end;

            function try_build_prefix_exact_plus_short_tail_local(
                out out_state_local: TExactChunkChainState): Boolean;
            const
                c_tail_max_syllables = 4;
                c_min_prefix_syllables = 2;
                c_prefix_probe_limit = 6;
                c_tail_probe_limit = 8;
            var
                tail_len_local: Integer;
                prefix_len_local: Integer;
                prefix_key_local: string;
                tail_key_local: string;
                prefix_results_local: TncCandidateList;
                tail_results_local: TncCandidateList;
                tail_len_iter_local: Integer;
                prefix_idx_iter_local: Integer;
                tail_idx_iter_local: Integer;
                prefix_idx_local: Integer;
                tail_idx_local: Integer;
                prefix_candidate_local: TncCandidate;
                prefix_text_local: string;
                tail_text_local: string;
                prefix_score_local: Integer;
                tail_score_local: Integer;
                combined_text_local: string;
                combined_path_local: string;
                best_state_local: TExactChunkChainState;
                has_best_local: Boolean;
                resolved_prefix_text_local: string;
                resolved_prefix_path_local: string;
                resolved_prefix_score_local: Integer;
                resolved_prefix_segments_local: Integer;
                resolved_prefix_anchor_units_local: Integer;
                resolved_prefix_has_anchor_local: Boolean;
                resolved_tail_text_local: string;
                resolved_tail_path_local: string;
                resolved_tail_score_local: Integer;
                resolved_tail_segments_local: Integer;
                resolved_tail_anchor_units_local: Integer;
                resolved_tail_has_anchor_local: Boolean;
                tail_path_local: string;
                tail_segments_local: Integer;
                tail_anchor_units_local: Integer;
                tail_has_anchor_local: Boolean;

                function candidate_effective_score_local(
                    const candidate_local: TncCandidate): Integer;
                begin
                    Result := candidate_local.score;
                    if candidate_local.has_dict_weight and
                        (candidate_local.dict_weight > Result) then
                    begin
                        Result := candidate_local.dict_weight;
                    end;
                end;

                function resolved_path_has_multi_exact_chunk_local(
                    const span_start_local: Integer;
                    const span_units_local: Integer;
                    const path_value_local: string): Boolean;
                var
                    parts_local: TArray<string>;
                    part_idx_local: Integer;
                    segment_text_local: string;
                    segment_units_local: Integer;
                    segment_pos_local: Integer;
                    segment_key_local: string;
                    segment_results_local: TncCandidateList;
                    segment_idx_local: Integer;
                    has_multi_exact_local: Boolean;
                begin
                    Result := False;
                    has_multi_exact_local := False;
                    segment_pos_local := span_start_local;
                    if (Trim(path_value_local) = '') or
                        (span_start_local < 0) or (span_units_local <= 0) then
                    begin
                        Exit;
                    end;

                    parts_local := path_value_local.Split(
                        [c_segment_path_separator]);
                    for part_idx_local := 0 to High(parts_local) do
                    begin
                        segment_text_local := Trim(parts_local[part_idx_local]);
                        segment_units_local := get_candidate_text_unit_count(
                            segment_text_local);
                        if (segment_text_local = '') or
                            (segment_units_local <= 0) or
                            (segment_pos_local + segment_units_local >
                            span_start_local + span_units_local) then
                        begin
                            Exit(False);
                        end;

                        if segment_units_local >= 2 then
                        begin
                            segment_key_local := build_query_key_local(
                                segment_pos_local, segment_units_local);
                            if (segment_key_local <> '') and
                                lookup_exact_full_pinyin_cached_local(
                                segment_key_local, segment_results_local) then
                            begin
                                for segment_idx_local := 0 to Min(
                                    c_tail_probe_limit - 1,
                                    High(segment_results_local)) do
                                begin
                                    if SameText(Trim(segment_results_local[
                                        segment_idx_local].text),
                                        segment_text_local) then
                                    begin
                                        has_multi_exact_local := True;
                                        Break;
                                    end;
                                end;
                            end;
                        end;
                        Inc(segment_pos_local, segment_units_local);
                    end;
                    Result := has_multi_exact_local and
                        (segment_pos_local = span_start_local + span_units_local);
                end;

                function try_get_tail_text_local(const tail_units_local: Integer;
                    out out_text_local: string; out out_score_local: Integer): Boolean;
                var
                    fixed_text_local: string;
                begin
                    Result := False;
                    out_text_local := '';
                    out_score_local := 0;
                    tail_path_local := '';
                    tail_segments_local := 0;
                    tail_anchor_units_local := 0;
                    tail_has_anchor_local := False;
                    tail_key_local := build_query_key_local(prefix_len_local,
                        tail_units_local);
                    if tail_key_local = '' then
                    begin
                        Exit;
                    end;

                    if tail_units_local = 1 then
                    begin
                        if (prefix_len_local + tail_units_local =
                            syllable_count_local) and
                            try_get_sentence_final_modal_particle_text_fast_local(
                            effective_syllables[prefix_len_local].text,
                            fixed_text_local) then
                        begin
                            { Sentence-final particles should use their modal
                              form instead of the ordinary object/preposition
                              single for the same pinyin, e.g. ba -> 鍚? }
                        end
                        else
                        begin
                            fixed_text_local :=
                                get_fixed_boundary_single_char_for_standalone_local(
                                effective_syllables[prefix_len_local].text);
                        end;
                        if fixed_text_local <> '' then
                        begin
                            out_text_local := fixed_text_local;
                            tail_path_local := fixed_text_local;
                            tail_segments_local := 1;
                            out_score_local := 1200 + c_fast_fixed_single_bonus;
                            if lookup_exact_full_pinyin_cached_local(tail_key_local,
                                tail_results_local) then
                            begin
                                tail_idx_iter_local := 0;
                                while tail_idx_iter_local <= High(tail_results_local) do
                                begin
                                    tail_idx_local := tail_idx_iter_local;
                                    if tail_idx_local >= c_tail_probe_limit then
                                    begin
                                        Break;
                                    end;
                                    if SameText(Trim(tail_results_local[
                                        tail_idx_local].text), fixed_text_local) then
                                    begin
                                        out_score_local := Max(out_score_local,
                                            candidate_effective_score_local(
                                            tail_results_local[tail_idx_local]) +
                                            c_fast_fixed_single_bonus);
                                        Break;
                                    end;
                                    Inc(tail_idx_iter_local);
                                end;
                            end;
                            Exit(True);
                        end;
                        if try_get_best_single_char_for_query_key_local(tail_key_local,
                            out_text_local, out_score_local) then
                        begin
                            tail_path_local := out_text_local;
                            tail_segments_local := 1;
                            Inc(out_score_local, c_fast_fixed_single_bonus);
                            Exit(True);
                        end;
                        Exit;
                    end;

                    if lookup_exact_full_pinyin_cached_local(tail_key_local,
                        tail_results_local) then
                    begin
                        tail_idx_iter_local := 0;
                        while tail_idx_iter_local <= High(tail_results_local) do
                        begin
                            tail_idx_local := tail_idx_iter_local;
                            if tail_idx_local >= c_tail_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                            begin
                                Inc(tail_idx_iter_local);
                                Continue;
                            end;
                            tail_text_local := Trim(
                                tail_results_local[tail_idx_local].text);
                            if (get_candidate_text_unit_count(tail_text_local) <>
                                tail_units_local) or
                                (not is_valid_standalone_cjk_phrase_text_local(
                                tail_text_local, tail_units_local)) or
                                (not is_reliable_fast_exact_chunk_local(
                                tail_key_local, tail_units_local,
                                tail_text_local,
                                tail_results_local[tail_idx_local])) then
                            begin
                                Inc(tail_idx_iter_local);
                                Continue;
                            end;
                            out_text_local := tail_text_local;
                            tail_path_local := tail_text_local;
                            tail_segments_local := 1;
                            tail_has_anchor_local := tail_units_local >= 3;
                            if tail_has_anchor_local then
                            begin
                                tail_anchor_units_local := tail_units_local;
                            end;
                            out_score_local := candidate_effective_score_local(
                                tail_results_local[tail_idx_local]) +
                                get_exact_chunk_length_bonus_local(
                                tail_units_local);
                            Exit(True);
                        end;
                    end;

                    if (tail_units_local >= 2) and
                        try_resolve_best_exact_subspan_local(prefix_len_local,
                        tail_units_local, resolved_tail_text_local,
                        resolved_tail_score_local, resolved_tail_segments_local,
                        resolved_tail_path_local, resolved_tail_has_anchor_local,
                        resolved_tail_anchor_units_local) and
                        (resolved_tail_segments_local > 1) and
                        (get_candidate_text_unit_count(resolved_tail_text_local) =
                        tail_units_local) and
                        is_valid_standalone_cjk_phrase_text_local(
                        resolved_tail_text_local, tail_units_local) and
                        resolved_path_has_multi_exact_chunk_local(prefix_len_local,
                        tail_units_local, resolved_tail_path_local) then
                    begin
                        out_text_local := resolved_tail_text_local;
                        out_score_local := resolved_tail_score_local;
                        tail_path_local := resolved_tail_path_local;
                        tail_segments_local := resolved_tail_segments_local;
                        tail_has_anchor_local := resolved_tail_has_anchor_local;
                        tail_anchor_units_local :=
                            resolved_tail_anchor_units_local;
                        Exit(True);
                    end;
                end;

                procedure consider_prefix_candidate_local(
                    const candidate_local: TncCandidate);
                begin
                    prefix_text_local := Trim(candidate_local.text);
                    if (prefix_text_local = '') or
                        (Trim(candidate_local.comment) <> '') or
                        (get_candidate_text_unit_count(prefix_text_local) <>
                        prefix_len_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        prefix_text_local, prefix_len_local)) then
                    begin
                        Exit;
                    end;

                    prefix_score_local := candidate_effective_score_local(
                        candidate_local);
                    if prefix_score_local < 600 then
                    begin
                        Exit;
                    end;

                    if not try_get_tail_text_local(tail_len_local, tail_text_local,
                        tail_score_local) then
                    begin
                        Exit;
                    end;

                    combined_text_local := prefix_text_local + tail_text_local;
                    if get_candidate_text_unit_count(combined_text_local) <>
                        syllable_count_local then
                    begin
                        Exit;
                    end;

                    combined_path_local := prefix_text_local +
                        c_segment_path_separator + tail_path_local;
                    best_state_local := Default(TExactChunkChainState);
                    best_state_local.text := combined_text_local;
                    best_state_local.path := combined_path_local;
                    best_state_local.first_chunk_text := prefix_text_local;
                    best_state_local.first_chunk_query_key := prefix_key_local;
                    best_state_local.score := prefix_score_local + tail_score_local +
                        c_fast_completion_bonus + (prefix_len_local *
                        prefix_len_local * c_fast_length_bonus);
                    best_state_local.segments := 1 + tail_segments_local;
                    best_state_local.has_anchor := True;
                    best_state_local.anchor_units := Max(prefix_len_local,
                        tail_anchor_units_local);
                    has_best_local := True;
                end;

                procedure consider_resolved_prefix_candidate_local;
                begin
                    if (tail_len_local <> 1) or (prefix_len_local < 4) then
                    begin
                        Exit;
                    end;

                    if not try_resolve_best_exact_subspan_local(0,
                        prefix_len_local, resolved_prefix_text_local,
                        resolved_prefix_score_local,
                        resolved_prefix_segments_local,
                        resolved_prefix_path_local,
                        resolved_prefix_has_anchor_local,
                        resolved_prefix_anchor_units_local) then
                    begin
                        Exit;
                    end;

                    resolved_prefix_text_local := Trim(resolved_prefix_text_local);
                    if (resolved_prefix_text_local = '') or
                        (resolved_prefix_segments_local <= 1) or
                        (get_candidate_text_unit_count(resolved_prefix_text_local) <>
                        prefix_len_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        resolved_prefix_text_local, prefix_len_local)) or
                        (not resolved_path_has_multi_exact_chunk_local(0,
                        prefix_len_local, resolved_prefix_path_local)) then
                    begin
                        Exit;
                    end;

                    if not try_get_tail_text_local(tail_len_local, tail_text_local,
                        tail_score_local) then
                    begin
                        Exit;
                    end;

                    combined_text_local := resolved_prefix_text_local +
                        tail_text_local;
                    if get_candidate_text_unit_count(combined_text_local) <>
                        syllable_count_local then
                    begin
                        Exit;
                    end;

                    combined_path_local := resolved_prefix_path_local +
                        c_segment_path_separator + tail_path_local;
                    best_state_local := Default(TExactChunkChainState);
                    best_state_local.text := combined_text_local;
                    best_state_local.path := combined_path_local;
                    best_state_local.first_chunk_text := resolved_prefix_text_local;
                    best_state_local.first_chunk_query_key := prefix_key_local;
                    best_state_local.score := resolved_prefix_score_local +
                        tail_score_local + c_fast_completion_bonus +
                        (prefix_len_local * prefix_len_local *
                        c_fast_length_bonus);
                    best_state_local.segments := resolved_prefix_segments_local +
                        tail_segments_local;
                    best_state_local.has_anchor := True;
                    best_state_local.anchor_units := Max(prefix_len_local,
                        resolved_prefix_anchor_units_local);
                    has_best_local := True;
                end;
            begin
                Result := False;
                out_state_local := Default(TExactChunkChainState);
                if (syllable_count_local < c_min_prefix_syllables + 1) or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                tail_len_iter_local := 1;
                while tail_len_iter_local <= Min(c_tail_max_syllables,
                    syllable_count_local - c_min_prefix_syllables) do
                begin
                    tail_len_local := tail_len_iter_local;
                    prefix_len_local := syllable_count_local - tail_len_local;
                    if prefix_len_local < c_min_prefix_syllables then
                    begin
                        Inc(tail_len_iter_local);
                        Continue;
                    end;
                    prefix_key_local := build_query_key_local(0, prefix_len_local);
                    if prefix_key_local = '' then
                    begin
                        Inc(tail_len_iter_local);
                        Continue;
                    end;

                    has_best_local := False;
                    if lookup_exact_full_pinyin_cached_local(prefix_key_local,
                        prefix_results_local) then
                    begin
                        prefix_idx_iter_local := 0;
                        while prefix_idx_iter_local <= High(prefix_results_local) do
                        begin
                            prefix_idx_local := prefix_idx_iter_local;
                            if prefix_idx_local >= c_prefix_probe_limit then
                            begin
                                Break;
                            end;
                            prefix_candidate_local :=
                                prefix_results_local[prefix_idx_local];
                            consider_prefix_candidate_local(prefix_candidate_local);
                            if has_best_local then
                            begin
                                out_state_local := best_state_local;
                                Exit(True);
                            end;
                            Inc(prefix_idx_iter_local);
                        end;
                    end;
                    Inc(tail_len_iter_local);
                end;

                tail_len_local := 1;
                prefix_len_local := syllable_count_local - tail_len_local;
                prefix_key_local := build_query_key_local(0, prefix_len_local);
                has_best_local := False;
                consider_resolved_prefix_candidate_local;
                if has_best_local then
                begin
                    out_state_local := best_state_local;
                    Exit(True);
                end;
            end;

            function try_build_unambiguous_multi_exact_chain_local(
                out out_state_local: TExactChunkChainState): Boolean;
            const
                c_min_chunk_raw_weight = 500;
                c_min_unambiguous_chunk_raw_weight = 180;
                c_strong_chunk_raw_weight = 50000;
                c_clear_short_chunk_raw_floor = 700;
                c_clear_short_chunk_raw_gap = 96;
                c_clear_short_chunk_score_gap = 64;
                c_ambiguous_raw_gap = 700;
                c_short_ambiguous_raw_ratio_pct = 150;
                c_ambiguous_score_gap = 1600;
            var
                local_pos: Integer;
                local_chunk_len: Integer;
                local_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                local_text: string;
                local_raw_weight: Integer;
                local_min_raw_weight: Integer;
                local_score: Integer;
                local_path_bonus: Integer;
                local_candidate_path: string;
                local_expanded_candidate_path: string;
                scan_idx: Integer;
                scan_text: string;
                scan_raw_weight: Integer;
                best_direct_raw_weight: Integer;
                tail_support_bonus: Integer;
                best_text: string;
                best_key: string;
                best_raw_weight: Integer;
                best_score: Integer;
                second_raw_weight: Integer;
                second_score: Integer;
                fixed_single_text: string;
                fixed_single_count: Integer;
                picked: Boolean;
                first_chunk: string;
                first_key: string;

                function is_unambiguous_splittable_tail_local(
                    const text_value_local: string): Boolean;
                begin
                    Result := (text_value_local = string(Char($53BB))) or
                        (text_value_local = string(Char($7684))) or
                        (text_value_local = string(Char($4E86))) or
                        (text_value_local = string(Char($5427))) or
                        (text_value_local = string(Char($5417)));
                end;

                function expand_unambiguous_function_tail_path_local(
                    const path_value_local: string): string;
                var
                    parts_local: TArray<string>;
                    units_local: TArray<string>;
                    part_idx_local: Integer;
                    unit_idx_local: Integer;
                    segment_text_local: string;
                    head_text_local: string;
                    tail_text_local: string;
                begin
                    Result := '';
                    if Trim(path_value_local) = '' then
                    begin
                        Exit;
                    end;

                    parts_local := path_value_local.Split(
                        [c_segment_path_separator]);
                    for part_idx_local := 0 to High(parts_local) do
                    begin
                        segment_text_local := Trim(parts_local[part_idx_local]);
                        if segment_text_local = '' then
                        begin
                            Continue;
                        end;

                        units_local := split_text_units(segment_text_local);
                        if (Length(units_local) >= 3) and
                            is_unambiguous_splittable_tail_local(
                            units_local[High(units_local)]) then
                        begin
                            head_text_local := '';
                            for unit_idx_local := 0 to High(units_local) - 1 do
                            begin
                                head_text_local := head_text_local +
                                    units_local[unit_idx_local];
                            end;
                            tail_text_local := units_local[High(units_local)];
                            if Result <> '' then
                            begin
                                Result := Result + c_segment_path_separator;
                            end;
                            Result := Result + head_text_local +
                                c_segment_path_separator + tail_text_local;
                        end
                        else
                        begin
                            if Result <> '' then
                            begin
                                Result := Result + c_segment_path_separator;
                            end;
                            Result := Result + segment_text_local;
                        end;
                    end;
                end;

                function has_reliable_following_exact_chunk_local(
                    const from_pos: Integer): Boolean;
                var
                    probe_len: Integer;
                    probe_key: string;
                    probe_results: TncCandidateList;
                    probe_idx: Integer;
                    probe_text: string;
                begin
                    Result := False;
                    if from_pos >= syllable_count_local then
                    begin
                        Exit(True);
                    end;
                    if from_pos < 0 then
                    begin
                        Exit;
                    end;

                    for probe_len := Min(3, syllable_count_local - from_pos) downto 2 do
                    begin
                        probe_key := build_query_key_local(from_pos, probe_len);
                        if (probe_key = '') or
                            (not lookup_exact_full_pinyin_cached_local(probe_key,
                            probe_results)) then
                        begin
                            Continue;
                        end;

                        for probe_idx := 0 to High(probe_results) do
                        begin
                            if probe_idx >= get_chain_probe_limit_local then
                            begin
                                Break;
                            end;
                            if Trim(probe_results[probe_idx].comment) <> '' then
                            begin
                                Continue;
                            end;
                            probe_text := Trim(probe_results[probe_idx].text);
                            if (get_candidate_text_unit_count(probe_text) =
                                probe_len) and
                                is_valid_standalone_cjk_phrase_text_local(
                                probe_text, probe_len) and
                                is_reliable_fast_exact_chunk_local(probe_key,
                                probe_len, probe_text, probe_results[probe_idx]) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                function should_allow_long_fixed_single_bridge_local(
                    const syllable_text_local: string;
                    const fixed_text_local: string): Boolean;
                var
                    normalized_syllable_local: string;
                begin
                    Result := False;
                    normalized_syllable_local :=
                        normalize_pinyin_text(syllable_text_local);
                    if (fixed_text_local = '') or (normalized_syllable_local = '') then
                    begin
                        Exit;
                    end;

                    if normalized_syllable_local = 'yi' then
                    begin
                        // Avoid restoring the old "涓€/宕庡矕" style split.
                        Exit;
                    end;

                    Result := (local_pos > 0) and (fixed_single_count < 2) and
                        has_reliable_following_exact_chunk_local(local_pos + 1);
                end;

                function should_allow_long_tail_single_completion_local: Boolean;
                begin
                    Result := (local_pos > 0) and
                        (syllable_count_local - local_pos <= 3);
                end;

                function should_defer_pair_to_modifier_tail_exact_local(
                    const pair_text_local: string): Boolean;
                var
                    prefix_text_local: string;
                    tail_key_local: string;
                    pair_units_local: TArray<string>;
                begin
                    Result := False;
                    if (local_chunk_len <> 2) or
                        (local_pos + 2 >= syllable_count_local) or
                        (pair_text_local = '') then
                    begin
                        Exit;
                    end;

                    if not try_get_modifier_prefix_single_text_local(
                        effective_syllables[local_pos].text, prefix_text_local) then
                    begin
                        Exit;
                    end;

                    pair_units_local := split_text_units(Trim(pair_text_local));
                    if (Length(pair_units_local) <> 2) or
                        SameText(pair_units_local[0], prefix_text_local) then
                    begin
                        Exit;
                    end;

                    tail_key_local := build_query_key_local(local_pos + 1, 2);
                    Result := has_high_quality_exact_phrase_for_key_local(
                        tail_key_local, 2, 700);
                end;
            begin
                Result := False;
                out_state_local := Default(TExactChunkChainState);
                if (syllable_count_local < 6) or (m_dictionary = nil) then
                begin
                    Exit;
                end;

                local_pos := 0;
                first_chunk := '';
                first_key := '';
                fixed_single_count := 0;
                while local_pos < syllable_count_local do
                begin
                    picked := False;
                    best_text := '';
                    best_key := '';
                    best_score := Low(Integer);

                    for local_chunk_len := Min(c_fast_max_chunk_syllables,
                        syllable_count_local - local_pos) downto 2 do
                    begin
                        local_key := build_query_key_local(local_pos,
                            local_chunk_len);
                        if (local_key = '') or
                            (not lookup_exact_full_pinyin_cached_local(local_key,
                            local_results)) then
                        begin
                            Continue;
                        end;

                        best_text := '';
                        best_key := '';
                        best_raw_weight := Low(Integer);
                        best_score := Low(Integer);
                        second_raw_weight := Low(Integer);
                        second_score := Low(Integer);
                        best_direct_raw_weight := 0;

                        if (local_chunk_len = 2) and (local_pos > 0) and
                            (local_pos + local_chunk_len = syllable_count_local) then
                        begin
                            for scan_idx := 0 to High(local_results) do
                            begin
                                if scan_idx >= get_chain_probe_limit_local then
                                begin
                                    Break;
                                end;
                                if Trim(local_results[scan_idx].comment) <> '' then
                                begin
                                    Continue;
                                end;

                                scan_text := Trim(local_results[scan_idx].text);
                                if (scan_text = '') or
                                    (get_candidate_text_unit_count(scan_text) <>
                                    local_chunk_len) or
                                    (not is_valid_standalone_cjk_phrase_text_local(
                                    scan_text, local_chunk_len)) then
                                begin
                                    Continue;
                                end;

                                scan_raw_weight := get_exact_chunk_raw_weight_local(
                                    local_results[scan_idx]);
                                if scan_raw_weight > best_direct_raw_weight then
                                begin
                                    best_direct_raw_weight := scan_raw_weight;
                                end;
                            end;
                        end;

                        for local_idx := 0 to High(local_results) do
                        begin
                            if local_idx >= get_chain_probe_limit_local then
                            begin
                                Break;
                            end;
                            if Trim(local_results[local_idx].comment) <> '' then
                            begin
                                Continue;
                            end;

                            local_text := Trim(local_results[local_idx].text);
                            if (local_text = '') or
                                (get_candidate_text_unit_count(local_text) <>
                                local_chunk_len) or
                                (not is_valid_standalone_cjk_phrase_text_local(
                                local_text, local_chunk_len)) or
                                (not is_reliable_fast_exact_chunk_local(local_key,
                                local_chunk_len, local_text,
                                local_results[local_idx])) then
                            begin
                                Continue;
                            end;

                            local_raw_weight := get_exact_chunk_raw_weight_local(
                                local_results[local_idx]);
                            if get_exact_chunk_boundary_adjustment_local(
                                local_pos, local_chunk_len, local_text) <= -12000 then
                            begin
                                Continue;
                            end;
                            if should_defer_pair_to_modifier_tail_exact_local(
                                local_text) then
                            begin
                                Continue;
                            end;
                            tail_support_bonus := 0;
                            if (local_chunk_len = 2) and (local_pos > 0) and
                                (local_pos + local_chunk_len =
                                syllable_count_local) and
                                (best_direct_raw_weight > 0) and
                                (local_raw_weight < best_direct_raw_weight) and
                                ((local_raw_weight + 1400) >=
                                best_direct_raw_weight) then
                            begin
                                tail_support_bonus :=
                                    m_dictionary.get_compound_tail_support(
                                    local_text);
                                if (tail_support_bonus <= 0) or
                                    ((best_direct_raw_weight >= 900) and
                                    (tail_support_bonus < 2000)) then
                                begin
                                    tail_support_bonus := 0;
                                end
                                else
                                begin
                                    tail_support_bonus := Min(1800,
                                        tail_support_bonus);
                                end;
                            end;
                            local_min_raw_weight := c_min_chunk_raw_weight;
                            if syllable_count_local <= 6 then
                            begin
                                local_min_raw_weight := 450;
                            end;
                            if local_chunk_len >= 4 then
                            begin
                                local_min_raw_weight := 220;
                            end;
                            if Length(local_results) = 1 then
                            begin
                                local_min_raw_weight := Min(local_min_raw_weight,
                                    c_min_unambiguous_chunk_raw_weight);
                            end;
                            if (local_raw_weight < local_min_raw_weight) and
                                (tail_support_bonus <= 0) then
                            begin
                                Continue;
                            end;
                            local_score := get_exact_chunk_effective_weight_local(
                                local_results[local_idx]) +
                                get_exact_chunk_length_bonus_local(
                                local_chunk_len) +
                                (get_chain_probe_limit_local - local_idx) * 96;
                            Inc(local_score, get_exact_chunk_boundary_adjustment_local(
                                local_pos, local_chunk_len, local_text));
                            if tail_support_bonus > 0 then
                            begin
                                Inc(local_score, tail_support_bonus);
                            end;
                            if (local_pos + local_chunk_len =
                                syllable_count_local) and
                                (out_state_local.path <> '') then
                            begin
                                local_candidate_path := out_state_local.path +
                                    c_segment_path_separator + local_text;
                                local_path_bonus := Max(0,
                                    get_cached_query_segment_path_bonus(
                                    lookup_text, local_candidate_path));
                                local_expanded_candidate_path :=
                                    expand_unambiguous_function_tail_path_local(
                                    local_candidate_path);
                                if (local_expanded_candidate_path <> '') and
                                    (not SameText(local_expanded_candidate_path,
                                    local_candidate_path)) then
                                begin
                                    local_path_bonus := Max(local_path_bonus,
                                        Max(0,
                                        get_cached_query_segment_path_bonus(
                                        lookup_text,
                                        local_expanded_candidate_path)));
                                end;
                                Inc(local_path_bonus, Max(0,
                                    get_session_query_path_bonus(lookup_text,
                                    local_candidate_path)));
                                Inc(local_path_bonus, Max(0,
                                    get_session_ranked_query_path_bonus(
                                    local_candidate_path)));
                                if local_path_bonus > 0 then
                                begin
                                    Inc(local_score, Min(1600,
                                        local_path_bonus * 2));
                                end;
                            end;

                            if local_score > best_score then
                            begin
                                second_raw_weight := best_raw_weight;
                                second_score := best_score;
                                best_text := local_text;
                                best_key := local_key;
                                best_raw_weight := local_raw_weight;
                                best_score := local_score;
                            end
                            else if local_score > second_score then
                            begin
                                second_raw_weight := local_raw_weight;
                                second_score := local_score;
                            end;
                        end;

                        if (best_text <> '') and
                            ((best_raw_weight >= c_strong_chunk_raw_weight) or
                            ((local_chunk_len <= 3) and
                            (best_raw_weight >= c_clear_short_chunk_raw_floor) and
                            (best_raw_weight >= second_raw_weight +
                            c_clear_short_chunk_raw_gap) and
                            (best_score >= second_score +
                            c_clear_short_chunk_score_gap)) or
                            (second_score = Low(Integer)) or
                            (best_raw_weight >= second_raw_weight +
                            c_ambiguous_raw_gap) or
                            ((syllable_count_local <= 6) and
                            (best_raw_weight > 0) and
                            (second_raw_weight > 0) and
                            (best_raw_weight * 100 >= second_raw_weight *
                            c_short_ambiguous_raw_ratio_pct)) or
                            (best_score >= second_score +
                            c_ambiguous_score_gap)) then
                        begin
                            picked := True;
                            Break;
                        end;
                    end;

                    if (not picked) and (syllable_count_local <= 6) then
                    begin
                        local_chunk_len := 1;
                        local_key := build_query_key_local(local_pos, 1);
                        if (local_pos + 1 = syllable_count_local) and
                            try_get_sentence_final_modal_particle_text_fast_local(
                            effective_syllables[local_pos].text,
                            fixed_single_text) then
                        begin
                        end
                        else if not try_get_modifier_prefix_single_text_local(
                            effective_syllables[local_pos].text,
                            fixed_single_text) then
                        begin
                            fixed_single_text :=
                                get_fixed_boundary_single_char_for_standalone_local(
                                effective_syllables[local_pos].text);
                        end;
                        if fixed_single_text <> '' then
                        begin
                            best_text := fixed_single_text;
                            best_key := local_key;
                            best_score := 1200 + c_fast_fixed_single_bonus;
                            if lookup_exact_full_pinyin_cached_local(local_key,
                                local_results) then
                            begin
                                for local_idx := 0 to High(local_results) do
                                begin
                                    if local_idx >= get_chain_probe_limit_local then
                                    begin
                                        Break;
                                    end;
                                    if SameText(Trim(local_results[local_idx].text),
                                        fixed_single_text) then
                                    begin
                                        best_score := Max(best_score,
                                            get_exact_chunk_effective_weight_local(
                                            local_results[local_idx]) +
                                            c_fast_fixed_single_bonus);
                                        Break;
                                    end;
                                end;
                            end;
                            picked := True;
                        end
                        else if try_get_best_single_char_for_query_key_local(
                            local_key, best_text, best_score) then
                        begin
                            best_key := local_key;
                            Dec(best_score, 900);
                            picked := True;
                        end;
                    end;

                    if (not picked) and (syllable_count_local > 6) then
                    begin
                        local_chunk_len := 1;
                        local_key := build_query_key_local(local_pos, 1);
                        if (local_pos + 1 = syllable_count_local) and
                            try_get_sentence_final_modal_particle_text_fast_local(
                            effective_syllables[local_pos].text,
                            fixed_single_text) then
                        begin
                        end
                        else if not try_get_modifier_prefix_single_text_local(
                            effective_syllables[local_pos].text,
                            fixed_single_text) then
                        begin
                            fixed_single_text :=
                                get_fixed_boundary_single_char_for_standalone_local(
                                effective_syllables[local_pos].text);
                        end;
                        if should_allow_long_fixed_single_bridge_local(
                            effective_syllables[local_pos].text,
                            fixed_single_text) then
                        begin
                            best_text := fixed_single_text;
                            best_key := local_key;
                            best_score := 900 + c_fast_fixed_single_bonus;
                            if lookup_exact_full_pinyin_cached_local(local_key,
                                local_results) then
                            begin
                                for local_idx := 0 to High(local_results) do
                                begin
                                    if local_idx >= get_chain_probe_limit_local then
                                    begin
                                        Break;
                                    end;
                                    if SameText(Trim(local_results[local_idx].text),
                                        fixed_single_text) then
                                    begin
                                        best_score := Max(best_score,
                                            get_exact_chunk_effective_weight_local(
                                            local_results[local_idx]) +
                                            c_fast_fixed_single_bonus);
                                        Break;
                                    end;
                                end;
                            end;
                            Inc(fixed_single_count);
                            picked := True;
                        end;
                        if (not picked) and
                            should_allow_long_tail_single_completion_local then
                        begin
                            if fixed_single_text <> '' then
                            begin
                                best_text := fixed_single_text;
                                best_key := local_key;
                                best_score := 600 + c_fast_fixed_single_bonus;
                                picked := True;
                            end
                            else if try_get_best_single_char_for_query_key_local(
                                local_key, best_text, best_score) then
                            begin
                                best_key := local_key;
                                Dec(best_score, 500);
                                picked := True;
                            end;
                            if picked then
                            begin
                                Inc(fixed_single_count);
                            end;
                        end;
                    end;

                    if not picked then
                    begin
                        Exit(False);
                    end;

                    if first_chunk = '' then
                    begin
                        first_chunk := best_text;
                        first_key := best_key;
                    end;
                    out_state_local.text := out_state_local.text + best_text;
                    if out_state_local.path <> '' then
                    begin
                        out_state_local.path := out_state_local.path +
                            c_segment_path_separator;
                    end;
                    out_state_local.path := out_state_local.path + best_text;
                    Inc(out_state_local.score, best_score -
                        c_fast_segment_penalty);
                    Inc(out_state_local.segments);
                    if local_chunk_len > out_state_local.anchor_units then
                    begin
                        out_state_local.anchor_units := local_chunk_len;
                    end;
                    Inc(local_pos, local_chunk_len);
                end;

                if (out_state_local.text = '') or
                    (out_state_local.segments < 2) or
                    (get_candidate_text_unit_count(out_state_local.text) <>
                    syllable_count_local) then
                begin
                    Exit(False);
                end;

                out_state_local.first_chunk_text := first_chunk;
                out_state_local.first_chunk_query_key := first_key;
                out_state_local.has_anchor := True;
                Inc(out_state_local.score, c_fast_completion_bonus);
                Result := True;
            end;

            procedure add_fast_option_local(
                var local_options: TExactChunkChainOptionArray;
                var legacy_options: TExactChunkChainOptionArray;
                const local_option: TExactChunkChainOption);
            var
                candidate_option_local: TExactChunkChainOption;
                rebuilt_options_local: TExactChunkChainOptionArray;
                insert_idx_local: Integer;
                option_idx_local: Integer;
                existing_idx_local: Integer;
                existing_option_local: TExactChunkChainOption;
                duplicate_found_local: Boolean;
            begin
                if local_option.text = '' then
                begin
                    Exit;
                end;

                if not use_exact_edge_ranker_expansion_local then
                begin
                    candidate_option_local := local_option;
                    candidate_option_local.baseline_option := True;
                    SetLength(local_options, Length(local_options) + 1);
                    local_options[High(local_options)] :=
                        candidate_option_local;
                    for insert_idx_local := High(local_options) downto 1 do
                    begin
                        if not is_exact_chunk_option_better_local(
                            local_options[insert_idx_local],
                            local_options[insert_idx_local - 1]) then
                        begin
                            Break;
                        end;
                        existing_option_local :=
                            local_options[insert_idx_local - 1];
                        local_options[insert_idx_local - 1] :=
                            local_options[insert_idx_local];
                        local_options[insert_idx_local] :=
                            existing_option_local;
                    end;
                    if Length(local_options) >
                        get_chain_option_limit_local then
                    begin
                        SetLength(local_options,
                            get_chain_option_limit_local);
                    end;
                    Exit;
                end;

                if not local_option.edge_expanded_only then
                begin
                    candidate_option_local := local_option;
                    candidate_option_local.baseline_option := True;
                    SetLength(legacy_options, Length(legacy_options) + 1);
                    legacy_options[High(legacy_options)] :=
                        candidate_option_local;
                    for insert_idx_local := High(legacy_options) downto 1 do
                    begin
                        if not is_exact_chunk_option_better_local(
                            legacy_options[insert_idx_local],
                            legacy_options[insert_idx_local - 1]) then
                        begin
                            Break;
                        end;
                        existing_option_local :=
                            legacy_options[insert_idx_local - 1];
                        legacy_options[insert_idx_local - 1] :=
                            legacy_options[insert_idx_local];
                        legacy_options[insert_idx_local] :=
                            existing_option_local;
                    end;
                    if Length(legacy_options) >
                        get_chain_option_limit_local then
                    begin
                        SetLength(legacy_options,
                            get_chain_option_limit_local);
                    end;
                end;

                candidate_option_local := local_option;
                candidate_option_local.baseline_option := False;
                if candidate_option_local.direct_exact and
                    (candidate_option_local.exact_rank > 0) then
                begin
                    candidate_option_local.edge_retention_score :=
                        long_edge_retention_score(
                        candidate_option_local.exact_rank,
                        candidate_option_local.raw_weight,
                        candidate_option_local.top_weight,
                        candidate_option_local.score,
                        get_candidate_text_unit_count(
                        candidate_option_local.text),
                        pos_local, syllable_count_local,
                        candidate_option_local.source_user);
                    build_long_edge_retention_features(
                        candidate_option_local.exact_rank,
                        candidate_option_local.raw_weight,
                        candidate_option_local.top_weight,
                        candidate_option_local.score,
                        get_candidate_text_unit_count(
                        candidate_option_local.text),
                        pos_local, syllable_count_local,
                        candidate_option_local.source_user,
                        candidate_option_local.edge_retention_features);
                end;

                duplicate_found_local := False;
                for existing_idx_local := 0 to High(local_options) do
                begin
                    if SameText(local_options[existing_idx_local].text,
                        candidate_option_local.text) and
                        SameText(local_options[existing_idx_local].path,
                        candidate_option_local.path) then
                    begin
                        duplicate_found_local := True;
                        if is_exact_chunk_option_better_local(
                            candidate_option_local,
                            local_options[existing_idx_local]) then
                        begin
                            local_options[existing_idx_local] :=
                                candidate_option_local;
                        end;
                        Break;
                    end;
                end;
                if not duplicate_found_local then
                begin
                    SetLength(local_options, Length(local_options) + 1);
                    local_options[High(local_options)] := candidate_option_local;
                end;
                for option_idx_local := 1 to High(local_options) do
                begin
                    insert_idx_local := option_idx_local;
                    while (insert_idx_local > 0) and
                        is_exact_chunk_option_better_local(
                        local_options[insert_idx_local],
                        local_options[insert_idx_local - 1]) do
                    begin
                        existing_option_local :=
                            local_options[insert_idx_local - 1];
                        local_options[insert_idx_local - 1] :=
                            local_options[insert_idx_local];
                        local_options[insert_idx_local] :=
                            existing_option_local;
                        Dec(insert_idx_local);
                    end;
                end;

                rebuilt_options_local := Copy(legacy_options, 0,
                    Length(legacy_options));
                for option_idx_local := 0 to
                    High(rebuilt_options_local) do
                begin
                    existing_option_local :=
                        rebuilt_options_local[option_idx_local];
                    existing_option_local.baseline_option := True;
                    rebuilt_options_local[option_idx_local] :=
                        existing_option_local;
                end;

                if use_exact_edge_ranker_expansion_local then
                begin
                    { Training capture needs every probed exact edge. Normal
                      search deliberately keeps the legacy option set until an
                      independently validated edge model can retain extra
                      states without displacing existing paths. }
                    for option_idx_local := 0 to High(local_options) do
                    begin
                        if not local_options[option_idx_local].direct_exact then
                        begin
                            Continue;
                        end;
                        duplicate_found_local := False;
                        for existing_idx_local := 0 to
                            High(rebuilt_options_local) do
                        begin
                            if SameText(rebuilt_options_local[
                                existing_idx_local].text,
                                local_options[option_idx_local].text) and
                                SameText(rebuilt_options_local[
                                existing_idx_local].path,
                                local_options[option_idx_local].path) then
                            begin
                                duplicate_found_local := True;
                                Break;
                            end;
                        end;
                        if duplicate_found_local then
                        begin
                            Continue;
                        end;
                        existing_option_local :=
                            local_options[option_idx_local];
                        existing_option_local.baseline_option := False;
                        SetLength(rebuilt_options_local,
                            Length(rebuilt_options_local) + 1);
                        rebuilt_options_local[
                            High(rebuilt_options_local)] :=
                            existing_option_local;
                    end;
                end;
                local_options := rebuilt_options_local;
            end;

            procedure score_fast_options_local(
                var local_options: TExactChunkChainOptionArray);
            var
                option_idx_local: Integer;
                option_value_local: TExactChunkChainOption;
                exact_edge_features_local:
                    TncLongExactEdgeLatticeFeatures;
                exact_edge_score_local: Integer;
            begin
                for option_idx_local := 0 to High(local_options) do
                begin
                    option_value_local := local_options[option_idx_local];
                    if option_value_local.direct_exact and
                        (option_value_local.exact_rank > 0) and
                        (not option_value_local.source_user) and
                        (get_candidate_text_unit_count(
                        option_value_local.text) >=
                        c_chain_lattice_exact_edge_min_units) then
                    begin
                        option_value_local.edge_retention_score :=
                            long_edge_retention_score(
                            option_value_local.exact_rank,
                            option_value_local.raw_weight,
                            option_value_local.top_weight,
                            option_value_local.score,
                            get_candidate_text_unit_count(
                            option_value_local.text),
                            pos_local, syllable_count_local, False);
                        if option_value_local.edge_retention_score >
                            High(Integer) then
                        begin
                            exact_edge_score_local := High(Integer);
                        end
                        else if option_value_local.edge_retention_score <
                            Low(Integer) then
                        begin
                            exact_edge_score_local := Low(Integer);
                        end
                        else
                        begin
                            exact_edge_score_local :=
                                option_value_local.edge_retention_score;
                        end;
                        build_long_exact_edge_lattice_features(
                            option_value_local.query_key,
                            option_value_local.text,
                            get_candidate_text_unit_count(
                            option_value_local.text),
                            option_value_local.exact_rank,
                            option_value_local.raw_weight,
                            option_value_local.top_weight,
                            option_value_local.score,
                            exact_edge_score_local,
                            pos_local, syllable_count_local,
                            option_value_local.baseline_option,
                            exact_edge_features_local);
                        option_value_local.exact_edge_model_score :=
                            long_exact_edge_lattice_ranker_score(
                            exact_edge_features_local);
                        local_options[option_idx_local] := option_value_local;
                    end;
                end;
            end;

            function is_splittable_function_tail_fast_local(
                const text_value_local: string): Boolean;
            begin
                Result := (text_value_local = string(Char($53BB))) or
                    (text_value_local = string(Char($7684))) or
                    (text_value_local = string(Char($4E86))) or
                    (text_value_local = string(Char($5427))) or
                    (text_value_local = string(Char($5417)));
            end;

            function expand_function_tail_path_fast_local(
                const path_value_local: string): string;
            var
                parts_local: TArray<string>;
                units_local: TArray<string>;
                part_idx_local: Integer;
                unit_idx_local: Integer;
                segment_text_local: string;
                head_text_local: string;
                tail_text_local: string;
            begin
                Result := '';
                if Trim(path_value_local) = '' then
                begin
                    Exit;
                end;

                parts_local := path_value_local.Split(
                    [c_segment_path_separator]);
                for part_idx_local := 0 to High(parts_local) do
                begin
                    segment_text_local := Trim(parts_local[part_idx_local]);
                    if segment_text_local = '' then
                    begin
                        Continue;
                    end;

                    units_local := split_text_units(segment_text_local);
                    if (Length(units_local) >= 3) and
                        is_splittable_function_tail_fast_local(
                        units_local[High(units_local)]) then
                    begin
                        head_text_local := '';
                        for unit_idx_local := 0 to High(units_local) - 1 do
                        begin
                            head_text_local := head_text_local +
                                units_local[unit_idx_local];
                        end;
                        tail_text_local := units_local[High(units_local)];
                        if Result <> '' then
                        begin
                            Result := Result + c_segment_path_separator;
                        end;
                        Result := Result + head_text_local +
                            c_segment_path_separator + tail_text_local;
                    end
                    else
                    begin
                        if Result <> '' then
                        begin
                            Result := Result + c_segment_path_separator;
                        end;
                        Result := Result + segment_text_local;
                    end;
                end;
            end;

            function get_fast_full_path_adjustment_local(
                const path_value_local: string): Integer;
            const
                c_fast_path_bonus_cap = 1600;
                c_fast_path_bonus_scale = 2;
                c_fast_path_penalty_cap = 240;
            var
                bonus_local: Integer;
                expanded_bonus_local: Integer;
                penalty_local: Integer;
                expanded_path_local: string;
            begin
                Result := 0;
                if (m_dictionary = nil) or (lookup_text = '') or
                    (path_value_local = '') then
                begin
                    Exit;
                end;

                bonus_local := Max(0,
                    get_cached_query_segment_path_bonus(lookup_text,
                    path_value_local));
                Inc(bonus_local, Max(0, get_session_query_path_bonus(
                    lookup_text, path_value_local)));
                Inc(bonus_local, Max(0,
                    get_session_ranked_query_path_bonus(path_value_local)));

                expanded_path_local := expand_function_tail_path_fast_local(
                    path_value_local);
                if (expanded_path_local <> '') and
                    (not SameText(expanded_path_local, path_value_local)) then
                begin
                    expanded_bonus_local := Max(0,
                        get_cached_query_segment_path_bonus(lookup_text,
                        expanded_path_local));
                    Inc(expanded_bonus_local, Max(0,
                        get_session_query_path_bonus(lookup_text,
                        expanded_path_local)));
                    Inc(expanded_bonus_local, Max(0,
                        get_session_ranked_query_path_bonus(
                        expanded_path_local)));
                    if expanded_bonus_local > bonus_local then
                    begin
                        bonus_local := expanded_bonus_local;
                    end;
                end;

                penalty_local := Max(0,
                    get_cached_query_segment_path_penalty(lookup_text,
                    path_value_local));
                Inc(penalty_local, Max(0, get_session_query_path_penalty(
                    lookup_text, path_value_local)));

                Result := Min(c_fast_path_bonus_cap,
                    bonus_local * c_fast_path_bonus_scale) -
                    Min(c_fast_path_penalty_cap, penalty_local);
            end;

            function try_get_strong_fast_exact_phrase_text_local(
                const query_key_inner: string; const expected_units_inner: Integer;
                const min_raw_weight_inner: Integer; out out_text_inner: string): Boolean;
            var
                lookup_results_inner: TncCandidateList;
                lookup_idx_inner: Integer;
                text_inner: string;
                raw_weight_inner: Integer;
                score_inner: Integer;
                best_score_inner: Integer;
            begin
                Result := False;
                out_text_inner := '';
                best_score_inner := Low(Integer);
                if (query_key_inner = '') or (expected_units_inner < 2) or
                    (expected_units_inner > 5) then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_inner,
                    lookup_results_inner) then
                begin
                    Exit;
                end;

                for lookup_idx_inner := 0 to High(lookup_results_inner) do
                begin
                    if lookup_idx_inner >= get_chain_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results_inner[lookup_idx_inner].comment) <> '' then
                    begin
                        Continue;
                    end;

                    text_inner := Trim(lookup_results_inner[lookup_idx_inner].text);
                    if (text_inner = '') or
                        (get_candidate_text_unit_count(text_inner) <>
                        expected_units_inner) or
                        (not is_valid_standalone_cjk_phrase_text_local(text_inner,
                        expected_units_inner)) then
                    begin
                        Continue;
                    end;

                    raw_weight_inner := get_exact_chunk_raw_weight_local(
                        lookup_results_inner[lookup_idx_inner]);
                    if raw_weight_inner < min_raw_weight_inner then
                    begin
                        Continue;
                    end;
                    if not is_reliable_fast_exact_chunk_local(query_key_inner,
                        expected_units_inner, text_inner,
                        lookup_results_inner[lookup_idx_inner]) then
                    begin
                        Continue;
                    end;

                    score_inner := get_exact_chunk_effective_weight_local(
                        lookup_results_inner[lookup_idx_inner]);
                    if (not Result) or (score_inner > best_score_inner) then
                    begin
                        Result := True;
                        best_score_inner := score_inner;
                        out_text_inner := text_inner;
                    end;
                end;
            end;

            function is_natural_modifier_phrase_boundary_fast_local(
                const prefix_text_inner: string; const tail_offset_inner: Integer;
                const tail_units_inner: Integer; const tail_text_inner: string;
                const min_tail_raw_weight_inner: Integer): Boolean;
            var
                tail_key_inner: string;
                strong_tail_text_inner: string;
            begin
                Result := False;
                if (prefix_text_inner = '') or (tail_text_inner = '') or
                    (tail_units_inner < 2) or
                    (not is_modifier_prefix_bridge_exempt_local(
                    prefix_text_inner)) then
                begin
                    Exit;
                end;

                tail_key_inner := build_query_key_local(tail_offset_inner,
                    tail_units_inner);
                Result := try_get_strong_fast_exact_phrase_text_local(
                    tail_key_inner, tail_units_inner,
                    min_tail_raw_weight_inner, strong_tail_text_inner) and
                    SameText(strong_tail_text_inner, Trim(tail_text_inner));
            end;

            function get_fast_exact_phrase_raw_weight_local(
                const query_key_inner: string; const expected_units_inner: Integer;
                const text_inner_value: string): Integer;
            var
                lookup_results_inner: TncCandidateList;
                lookup_idx_inner: Integer;
                lookup_text_inner: string;
                raw_weight_inner: Integer;
            begin
                Result := 0;
                if (query_key_inner = '') or (text_inner_value = '') or
                    (expected_units_inner < 2) then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_inner,
                    lookup_results_inner) then
                begin
                    Exit;
                end;

                for lookup_idx_inner := 0 to High(lookup_results_inner) do
                begin
                    if lookup_idx_inner >= get_chain_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results_inner[lookup_idx_inner].comment) <> '' then
                    begin
                        Continue;
                    end;

                    lookup_text_inner := Trim(lookup_results_inner[lookup_idx_inner].text);
                    if (get_candidate_text_unit_count(lookup_text_inner) <>
                        expected_units_inner) or
                        (not SameText(lookup_text_inner, text_inner_value)) then
                    begin
                        Continue;
                    end;

                    raw_weight_inner := get_exact_chunk_raw_weight_local(
                        lookup_results_inner[lookup_idx_inner]);
                    if raw_weight_inner > Result then
                    begin
                        Result := raw_weight_inner;
                    end;
                end;
            end;

            function get_first_text_unit_fast_local(const value_inner: string): string;
            var
                units_inner: TArray<string>;
            begin
                Result := '';
                units_inner := split_text_units(Trim(value_inner));
                if Length(units_inner) > 0 then
                begin
                    Result := units_inner[0];
                end;
            end;

            function build_leading_span_text_fast_local(const value_inner: string;
                const unit_count_inner: Integer): string;
            var
                units_inner: TArray<string>;
                unit_idx_inner: Integer;
            begin
                Result := '';
                if unit_count_inner <= 0 then
                begin
                    Exit;
                end;

                units_inner := split_text_units(Trim(value_inner));
                if Length(units_inner) < unit_count_inner then
                begin
                    Exit;
                end;

                for unit_idx_inner := 0 to unit_count_inner - 1 do
                begin
                    Result := Result + units_inner[unit_idx_inner];
                end;
            end;

            function build_span_text_fast_local(const value_inner: string;
                const start_idx_inner: Integer; const unit_count_inner: Integer): string;
            var
                units_inner: TArray<string>;
                unit_idx_inner: Integer;
            begin
                Result := '';
                if (start_idx_inner < 0) or (unit_count_inner <= 0) then
                begin
                    Exit;
                end;

                units_inner := split_text_units(Trim(value_inner));
                if start_idx_inner + unit_count_inner > Length(units_inner) then
                begin
                    Exit;
                end;

                for unit_idx_inner := start_idx_inner to
                    start_idx_inner + unit_count_inner - 1 do
                begin
                    Result := Result + units_inner[unit_idx_inner];
                end;
            end;

            function text_starts_with_fast_local(const value_inner: string;
                const prefix_inner: string): Boolean;
            begin
                Result := (prefix_inner <> '') and
                    SameText(Copy(value_inner, 1, Length(prefix_inner)),
                    prefix_inner);
            end;

            function should_accept_greedy_exact_chain_state_local(
                const state_inner: TExactChunkChainState): Boolean;
            const
                c_best_exact_raw_margin = 420;
            var
                path_segments_inner: TArray<string>;
                segment_idx_inner: Integer;
                query_offset_inner: Integer;
                segment_text_inner: string;
                segment_units_inner: Integer;
                query_key_inner: string;
                lookup_results_inner: TncCandidateList;
                lookup_idx_inner: Integer;
                lookup_text_inner: string;
                lookup_raw_inner: Integer;
                selected_raw_inner: Integer;
                best_raw_inner: Integer;
                best_text_inner: string;
                prefix_text_inner: string;
                tail_key_inner: string;
                tail_text_inner: string;
                candidate_span_inner: string;
                preferred_span_inner: string;
                next_segment_text_inner: string;
                next_segment_units_inner: Integer;
                next_key_inner: string;
                next_raw_inner: Integer;
            begin
                Result := False;
                if (state_inner.path = '') or (m_dictionary = nil) then
                begin
                    Exit;
                end;

                path_segments_inner := state_inner.path.Split(
                    [c_segment_path_separator]);
                query_offset_inner := 0;
                for segment_idx_inner := 0 to High(path_segments_inner) do
                begin
                    segment_text_inner := Trim(path_segments_inner[segment_idx_inner]);
                    segment_units_inner := get_candidate_text_unit_count(
                        segment_text_inner);
                    if (segment_text_inner = '') or (segment_units_inner <= 0) or
                        (query_offset_inner + segment_units_inner >
                        syllable_count_local) then
                    begin
                        Exit;
                    end;

                    if segment_units_inner > 1 then
                    begin
                        query_key_inner := build_query_key_local(query_offset_inner,
                            segment_units_inner);
                        if (query_key_inner = '') or
                            (not lookup_exact_full_pinyin_cached_local(query_key_inner,
                            lookup_results_inner)) then
                        begin
                            Exit;
                        end;

                        selected_raw_inner := 0;
                        best_raw_inner := 0;
                        best_text_inner := '';
                        for lookup_idx_inner := 0 to High(lookup_results_inner) do
                        begin
                            if lookup_idx_inner >= get_chain_probe_limit_local then
                            begin
                                Break;
                            end;
                            if Trim(lookup_results_inner[lookup_idx_inner].comment) <> '' then
                            begin
                                Continue;
                            end;
                            lookup_text_inner := Trim(
                                lookup_results_inner[lookup_idx_inner].text);
                            if (get_candidate_text_unit_count(lookup_text_inner) <>
                                segment_units_inner) or
                                (not is_valid_standalone_cjk_phrase_text_local(
                                lookup_text_inner, segment_units_inner)) then
                            begin
                                Continue;
                            end;
                            lookup_raw_inner := get_exact_chunk_raw_weight_local(
                                lookup_results_inner[lookup_idx_inner]);
                            if lookup_raw_inner > best_raw_inner then
                            begin
                                best_raw_inner := lookup_raw_inner;
                                best_text_inner := lookup_text_inner;
                            end;
                            if SameText(lookup_text_inner, segment_text_inner) then
                            begin
                                selected_raw_inner := lookup_raw_inner;
                            end;
                        end;

                        if selected_raw_inner <= 0 then
                        begin
                            Exit;
                        end;
                        if (best_raw_inner >= selected_raw_inner +
                            c_best_exact_raw_margin) and
                            (best_text_inner <> '') and
                            (not SameText(best_text_inner, segment_text_inner)) then
                        begin
                            Exit;
                        end;
                    end;

                    if (segment_units_inner = 1) and
                        (segment_idx_inner < High(path_segments_inner)) and
                        (query_offset_inner + 2 <= syllable_count_local) then
                    begin
                        query_key_inner := build_query_key_local(query_offset_inner,
                            2);
                        if try_get_strong_fast_exact_phrase_text_local(
                            query_key_inner, 2, 700, best_text_inner) then
                        begin
                            next_segment_text_inner := Trim(
                                path_segments_inner[segment_idx_inner + 1]);
                            next_segment_units_inner :=
                                get_candidate_text_unit_count(
                                next_segment_text_inner);
                            candidate_span_inner := segment_text_inner +
                                get_first_text_unit_fast_local(
                                next_segment_text_inner);
                            if (candidate_span_inner <> '') and
                                (not SameText(candidate_span_inner,
                                best_text_inner)) then
                            begin
                                best_raw_inner :=
                                    get_fast_exact_phrase_raw_weight_local(
                                    query_key_inner, 2, best_text_inner);
                                next_raw_inner := 0;
                                if next_segment_units_inner >= 2 then
                                begin
                                    next_key_inner := build_query_key_local(
                                        query_offset_inner + 1,
                                        next_segment_units_inner);
                                    next_raw_inner :=
                                        get_fast_exact_phrase_raw_weight_local(
                                        next_key_inner, next_segment_units_inner,
                                        next_segment_text_inner);
                                end;
                                if next_raw_inner < best_raw_inner + 700 then
                                begin
                                    Exit;
                                end;
                            end;
                        end;
                    end;

                    if (segment_units_inner = 2) and
                        (segment_idx_inner < High(path_segments_inner)) then
                    begin
                        prefix_text_inner := '';
                        if (not try_get_modifier_prefix_single_text_local(
                            effective_syllables[query_offset_inner].text,
                            prefix_text_inner)) or (prefix_text_inner = '') then
                        begin
                            prefix_text_inner :=
                                get_fixed_boundary_single_char_for_standalone_local(
                                effective_syllables[query_offset_inner].text);
                        end;

                        if (prefix_text_inner <> '') and
                            SameText(get_first_text_unit_fast_local(segment_text_inner),
                            prefix_text_inner) and
                            (query_offset_inner + 3 <= syllable_count_local) then
                        begin
                            tail_key_inner := build_query_key_local(
                                query_offset_inner + 1, 2);
                            if try_get_strong_fast_exact_phrase_text_local(
                                tail_key_inner, 2, 800, tail_text_inner) then
                            begin
                                candidate_span_inner := build_span_text_fast_local(
                                    state_inner.text, query_offset_inner, 3);
                                preferred_span_inner := prefix_text_inner +
                                    tail_text_inner;
                                if (candidate_span_inner <> '') and
                                    (not SameText(candidate_span_inner,
                                    preferred_span_inner)) then
                                begin
                                    Exit;
                                end;
                            end;
                        end;
                    end;

                    Inc(query_offset_inner, segment_units_inner);
                end;

                Result := query_offset_inner = syllable_count_local;
            end;

            function fast_seed_lacks_lm_support_local(
                const state_inner: TExactChunkChainState): Boolean;
            var
                lm_stats_inner: TLongWordLmBoundaryStats;
                lm_bonus_inner: Integer;
            begin
                Result := False;
                if (input_syllable_count < 7) or
                    (input_syllable_count > 8) or
                    (state_inner.segments < 2) then
                begin
                    Exit;
                end;

                lm_bonus_inner := get_exact_chunk_chain_state_lm_bonus_local(
                    state_inner, 0, lm_stats_inner);
                Result := (lm_bonus_inner = 0) and
                    (lm_stats_inner.boundary_count > 0) and
                    (lm_stats_inner.supported_count = 0);
            end;

            function get_competing_boundary_penalty_fast_local(
                const candidate_text_from_pos_inner: string; const pos_inner: Integer;
                const chunk_len_inner: Integer;
                const option_inner: TExactChunkChainOption;
                const suffix_state_inner: TExactChunkChainState): Integer;
            const
                c_bad_two_plus_tail_penalty = 72000;
                c_cut_head_exact_penalty = 96000;
                c_strong_exact_span_penalty = 128000;
                c_strong_exact_span_min_weight = 900;
                c_strong_exact_span_margin = 180;
            var
                prefix_text_inner: string;
                tail_units_inner: Integer;
                tail_key_inner: string;
                tail_text_inner: string;
                preferred_text_inner: string;
                current_span_inner: string;
                head_text_inner: string;
                head_key_inner: string;
                head_raw_inner: Integer;
                current_raw_inner: Integer;
                suffix_first_units_inner: Integer;
                span_units_inner: Integer;
                left_units_inner: Integer;
                right_units_inner: Integer;
                span_start_abs_inner: Integer;
                span_left_text_inner: string;
                option_units_inner: TArray<string>;
                option_raw_inner: Integer;
                option_last_syllable_inner: string;
            begin
                Result := 0;
                if (candidate_text_from_pos_inner = '') or
                    (suffix_state_inner.first_chunk_text = '') then
                begin
                    Exit;
                end;

                // Prefer strong exact spans over cross-boundary homophone paths.
                // This keeps long-sentence DP from selecting a sequence of weak
                // exact chunks that cuts through an available high-quality phrase.
                for span_units_inner := 3 to Min(5,
                    syllable_count_local - pos_inner) do
                begin
                    if span_units_inner <= chunk_len_inner then
                    begin
                        Continue;
                    end;
                    head_key_inner := build_query_key_local(pos_inner,
                        span_units_inner);
                    if not try_get_strong_fast_exact_phrase_text_local(
                        head_key_inner, span_units_inner,
                        c_strong_exact_span_min_weight, head_text_inner) then
                    begin
                        Continue;
                    end;

                    current_span_inner := build_leading_span_text_fast_local(
                        candidate_text_from_pos_inner, span_units_inner);
                    if (current_span_inner = '') or
                        SameText(current_span_inner, head_text_inner) then
                    begin
                        Continue;
                    end;

                    head_raw_inner := get_fast_exact_phrase_raw_weight_local(
                        head_key_inner, span_units_inner, head_text_inner);
                    current_raw_inner := get_fast_exact_phrase_raw_weight_local(
                        head_key_inner, span_units_inner, current_span_inner);
                    if (head_raw_inner >= c_strong_exact_span_min_weight) and
                        ((current_raw_inner <= 0) or
                        (head_raw_inner >= current_raw_inner +
                        c_strong_exact_span_margin)) then
                    begin
                        Inc(Result, c_strong_exact_span_penalty);
                    end;
                end;
                if (option_inner.segments > 1) and
                    (chunk_len_inner > 2) and
                    (pos_inner + 2 <= syllable_count_local) and
                    try_get_strong_fast_exact_phrase_text_local(
                    build_query_key_local(pos_inner, 2), 2, 900,
                    head_text_inner) then
                begin
                    current_span_inner := build_leading_span_text_fast_local(
                        candidate_text_from_pos_inner, 2);
                    if (current_span_inner <> '') and
                        (not SameText(current_span_inner, head_text_inner)) then
                    begin
                        Inc(Result, c_cut_head_exact_penalty);
                    end;
                end;

                if chunk_len_inner = 2 then
                begin
                    prefix_text_inner := '';
                    if (not try_get_modifier_prefix_single_text_local(
                        effective_syllables[pos_inner].text, prefix_text_inner)) or
                        (prefix_text_inner = '') then
                    begin
                        prefix_text_inner :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[pos_inner].text);
                    end;

                    if (prefix_text_inner <> '') and
                        (not ((option_inner.segments = 1) and
                        try_get_strong_fast_exact_phrase_text_local(
                        option_inner.query_key, 2, 900, head_text_inner) and
                        SameText(head_text_inner, option_inner.text))) then
                    begin
                        if not SameText(get_first_text_unit_fast_local(
                            option_inner.text), prefix_text_inner) then
                        begin
                            for tail_units_inner := 2 to 3 do
                            begin
                                if pos_inner + 1 + tail_units_inner >
                                    syllable_count_local then
                                begin
                                    Continue;
                                end;
                                tail_key_inner := build_query_key_local(pos_inner + 1,
                                    tail_units_inner);
                                if try_get_strong_fast_exact_phrase_text_local(
                                    tail_key_inner, tail_units_inner, 800,
                                    tail_text_inner) then
                                begin
                                    preferred_text_inner := prefix_text_inner +
                                        tail_text_inner;
                                    current_span_inner :=
                                        build_leading_span_text_fast_local(
                                        candidate_text_from_pos_inner,
                                        1 + tail_units_inner);
                                    if (current_span_inner <> '') and
                                        (not SameText(current_span_inner,
                                        preferred_text_inner)) then
                                    begin
                                        Inc(Result, c_bad_two_plus_tail_penalty);
                                    end;
                                end;
                            end;
                        end
                        else
                        begin
                            tail_key_inner := build_query_key_local(pos_inner + 1, 2);
                            if try_get_strong_fast_exact_phrase_text_local(
                                tail_key_inner, 2, 800, tail_text_inner) then
                            begin
                                preferred_text_inner := prefix_text_inner +
                                    tail_text_inner;
                                current_span_inner :=
                                    build_leading_span_text_fast_local(
                                    candidate_text_from_pos_inner, 3);
                                if (current_span_inner <> '') and
                                    (not SameText(current_span_inner,
                                    preferred_text_inner)) then
                                begin
                                    Inc(Result, c_bad_two_plus_tail_penalty);
                                end;
                            end;
                        end;
                    end;

                    if (pos_inner + 3 <= syllable_count_local) and
                        try_get_strong_fast_exact_phrase_text_local(
                        build_query_key_local(pos_inner, 3), 3, 900,
                        head_text_inner) and
                        text_starts_with_fast_local(head_text_inner,
                        option_inner.text) then
                    begin
                        current_span_inner := build_leading_span_text_fast_local(
                            candidate_text_from_pos_inner, 3);
                        if (current_span_inner <> '') and
                            (not SameText(current_span_inner, head_text_inner)) then
                        begin
                            Inc(Result, c_cut_head_exact_penalty);
                        end;
                    end;
                end
                else if chunk_len_inner = 1 then
                begin
                    suffix_first_units_inner := get_candidate_text_unit_count(
                        Trim(suffix_state_inner.first_chunk_text));
                    if (suffix_first_units_inner >= 1) and
                        (pos_inner + 2 <= syllable_count_local) and
                        try_get_strong_fast_exact_phrase_text_local(
                        build_query_key_local(pos_inner, 2), 2, 700,
                        head_text_inner) and
                        text_starts_with_fast_local(head_text_inner,
                        option_inner.text) then
                    begin
                        current_span_inner := build_leading_span_text_fast_local(
                            candidate_text_from_pos_inner, 2);
                        if (current_span_inner <> '') and
                            (not SameText(current_span_inner, head_text_inner)) then
                        begin
                            tail_text_inner := '';
                            if (suffix_first_units_inner >= 2) and
                                is_modifier_prefix_bridge_exempt_local(
                                option_inner.text) and
                                try_get_strong_fast_exact_phrase_text_local(
                                build_query_key_local(pos_inner + 1,
                                suffix_first_units_inner),
                                suffix_first_units_inner, 700,
                                tail_text_inner) and
                                SameText(tail_text_inner,
                                Trim(suffix_state_inner.first_chunk_text)) then
                            begin
                                { A function single followed by a strong phrase
                                  is a natural boundary; do not let a weak
                                  cross-boundary exact pair suppress it. }
                            end
                            else
                            begin
                                Inc(Result, c_cut_head_exact_penalty);
                            end;
                        end;
                    end;
                end;

                if chunk_len_inner >= 2 then
                begin
                    suffix_first_units_inner := get_candidate_text_unit_count(
                        Trim(suffix_state_inner.first_chunk_text));
                    option_units_inner := split_text_units(Trim(option_inner.text));
                    if (Length(option_units_inner) = chunk_len_inner) and
                        (suffix_first_units_inner >= 2) and
                        ((SameText(option_units_inner[High(option_units_inner)],
                        string(Char($5730)))) or
                        (SameText(option_units_inner[High(option_units_inner)],
                        string(Char($5F97))))) then
                    begin
                        option_last_syllable_inner := normalize_pinyin_text(
                            effective_syllables[pos_inner + chunk_len_inner - 1].text);
                        option_raw_inner := get_fast_exact_phrase_raw_weight_local(
                            option_inner.query_key, chunk_len_inner,
                            option_inner.text);
                        if (option_last_syllable_inner = 'de') and
                            ((option_raw_inner <= 0) or (option_raw_inner < 700)) then
                        begin
                            Inc(Result, 30000);
                        end;
                    end;

                    suffix_first_units_inner := get_candidate_text_unit_count(
                        Trim(suffix_state_inner.first_chunk_text));
                    for span_units_inner := 3 to 4 do
                    begin
                        for left_units_inner := 2 to
                            Min(chunk_len_inner, span_units_inner - 1) do
                        begin
                            right_units_inner := span_units_inner - left_units_inner;
                            if (right_units_inner <= 0) or
                                (right_units_inner > suffix_first_units_inner) then
                            begin
                                Continue;
                            end;
                            span_start_abs_inner := pos_inner + chunk_len_inner -
                                left_units_inner;
                            if (span_start_abs_inner < pos_inner) or
                                (span_start_abs_inner + span_units_inner >
                                syllable_count_local) then
                            begin
                                Continue;
                            end;

                            if not try_get_strong_fast_exact_phrase_text_local(
                                build_query_key_local(span_start_abs_inner,
                                span_units_inner), span_units_inner, 900,
                                head_text_inner) then
                            begin
                                Continue;
                            end;

                            current_span_inner := build_span_text_fast_local(
                                candidate_text_from_pos_inner,
                                chunk_len_inner - left_units_inner,
                                span_units_inner);
                            span_left_text_inner := build_span_text_fast_local(
                                candidate_text_from_pos_inner,
                                chunk_len_inner - left_units_inner,
                                left_units_inner);
                            if (current_span_inner <> '') and
                                (span_left_text_inner <> '') and
                                text_starts_with_fast_local(head_text_inner,
                                span_left_text_inner) and
                                (not SameText(current_span_inner,
                                head_text_inner)) then
                            begin
                                Inc(Result, c_cut_head_exact_penalty);
                            end;
                        end;
                    end;
                end;
            end;

            function get_fast_state_order_score_local(
                const state_inner: TExactChunkChainState;
                out word_lm_bonus_inner: Integer;
                out word_lm_stats_inner: TLongWordLmBoundaryStats): Integer;
            const
                c_cross_boundary_exact_head_penalty = 90000;
                c_single_segment_penalty = 1600;
                c_multi_segment_bonus = 1200;
            var
                path_segments_inner: TArray<string>;
                segment_idx_inner: Integer;
                query_offset_inner: Integer;
                current_text_inner: string;
                next_text_inner: string;
                current_units_inner: Integer;
                next_units_inner: Integer;
                exact_head_text_inner: string;
                pair_key_inner: string;
                current_pair_text_inner: string;
            begin
                word_lm_bonus_inner := 0;
                word_lm_stats_inner := Default(TLongWordLmBoundaryStats);
                Result := state_inner.score;
                if state_inner.path = '' then
                begin
                    Exit;
                end;

                path_segments_inner := state_inner.path.Split(
                    [c_segment_path_separator]);
                query_offset_inner := 0;
                for segment_idx_inner := 0 to High(path_segments_inner) do
                begin
                    current_text_inner := Trim(path_segments_inner[segment_idx_inner]);
                    current_units_inner := get_candidate_text_unit_count(
                        current_text_inner);
                    if (current_text_inner = '') or (current_units_inner <= 0) or
                        (query_offset_inner + current_units_inner >
                        syllable_count_local) then
                    begin
                        Exit;
                    end;

                    if current_units_inner = 1 then
                    begin
                        Dec(Result, c_single_segment_penalty);
                    end
                    else
                    begin
                        Inc(Result, current_units_inner * c_multi_segment_bonus);
                    end;

                    if (segment_idx_inner < High(path_segments_inner)) and
                        (current_units_inner = 1) and
                        (query_offset_inner + 2 <= syllable_count_local) then
                    begin
                        next_text_inner := Trim(
                            path_segments_inner[segment_idx_inner + 1]);
                        next_units_inner := get_candidate_text_unit_count(
                            next_text_inner);
                        if (next_text_inner <> '') and (next_units_inner = 1) and
                            is_modifier_prefix_bridge_exempt_local(
                            current_text_inner) and
                            is_modifier_prefix_bridge_exempt_local(
                            next_text_inner) then
                        begin
                            current_pair_text_inner := current_text_inner +
                                get_first_text_unit_fast_local(next_text_inner);
                            pair_key_inner := build_query_key_local(
                                query_offset_inner, 2);
                            if (pair_key_inner = '') or
                                (not try_get_strong_fast_exact_phrase_text_local(
                                pair_key_inner, 2, 700, exact_head_text_inner)) or
                                SameText(current_pair_text_inner,
                                exact_head_text_inner) then
                            begin
                                Inc(Result, 90000);
                            end
                            else
                            begin
                                Dec(Result, c_cross_boundary_exact_head_penalty);
                            end;
                        end;
                        if (next_text_inner <> '') and (next_units_inner > 0) and
                            try_get_strong_fast_exact_phrase_text_local(
                            build_query_key_local(query_offset_inner, 2), 2, 700,
                            exact_head_text_inner) and
                            text_starts_with_fast_local(exact_head_text_inner,
                            current_text_inner) then
                        begin
                            current_pair_text_inner := current_text_inner +
                                get_first_text_unit_fast_local(next_text_inner);
                            if (current_pair_text_inner <> '') and
                                (not SameText(current_pair_text_inner,
                                exact_head_text_inner)) then
                            begin
                                if is_natural_modifier_phrase_boundary_fast_local(
                                    current_text_inner, query_offset_inner + 1,
                                    next_units_inner, next_text_inner, 400) then
                                begin
                                    { A function/adverb single followed by a
                                      strong exact phrase is a valid boundary,
                                      e.g. 鍏?鎻愪氦; do not let a competing
                                      cross-boundary two-syllable word erase it. }
                                end
                                else
                                begin
                                    Dec(Result,
                                        c_cross_boundary_exact_head_penalty);
                                end;
                            end
                            else if next_units_inner > 1 then
                            begin
                                Dec(Result,
                                    c_cross_boundary_exact_head_penalty div 2);
                            end;
                        end;
                    end;

                    Inc(query_offset_inner, current_units_inner);
                end;

                word_lm_bonus_inner :=
                    get_exact_chunk_chain_state_lm_bonus_local(state_inner, 0,
                    word_lm_stats_inner);
                Inc(Result, word_lm_bonus_inner);
            end;

            function get_long_chain_ranker_profile_local: Integer;
            const
                c_default_long_chain_ranker_profile = 2;
            begin
                if m_debug_long_chain_ranker_profile >= 0 then
                begin
                    Exit(m_debug_long_chain_ranker_profile);
                end;
                Result := c_default_long_chain_ranker_profile;
            end;

            procedure get_long_chain_structure_stats_local(
                const state_inner: TExactChunkChainState;
                out single_segments_inner: Integer;
                out max_segment_units_inner: Integer);
            var
                structure_inner: TncLongPathStructureCacheValue;
            begin
                single_segments_inner := 0;
                max_segment_units_inner := 0;
                if get_cached_long_path_structure(state_inner.path,
                    structure_inner) then
                begin
                    single_segments_inner :=
                        structure_inner.single_segment_count;
                    max_segment_units_inner :=
                        structure_inner.max_segment_units;
                end;
            end;

            procedure get_long_ranker_char_scores_local(
                const state_texts_inner: TArray<string>;
                out full_scores_inner: TArray<Integer>;
                out suffix_scores_inner: TArray<Integer>;
                out context_scores_inner: TArray<Integer>;
                out full_scores_ok_inner: Boolean;
                out has_left_context_inner: Boolean;
                out left_context_inner: string);
            var
                idx_inner: Integer;
                previous_previous_text_inner: string;
                previous_text_inner: string;
            begin
                SetLength(full_scores_inner, Length(state_texts_inner));
                SetLength(suffix_scores_inner, Length(state_texts_inner));
                SetLength(context_scores_inner, Length(state_texts_inner));
                full_scores_ok_inner := False;
                has_left_context_inner := False;
                left_context_inner := '';
                if (m_dictionary = nil) or (Length(state_texts_inner) = 0) then
                begin
                    Exit;
                end;

                full_scores_ok_inner :=
                    get_cached_char_lm_scores(state_texts_inner,
                    full_scores_inner, clsm_full, '') and
                    (Length(full_scores_inner) = Length(state_texts_inner));
                if not full_scores_ok_inner then
                begin
                    SetLength(full_scores_inner, Length(state_texts_inner));
                    Exit;
                end;

                if (not get_cached_char_lm_scores(state_texts_inner,
                    suffix_scores_inner, clsm_suffix, '')) or
                    (Length(suffix_scores_inner) <> Length(state_texts_inner)) then
                begin
                    suffix_scores_inner := Copy(full_scores_inner);
                end;

                get_recent_path_context_seed(previous_previous_text_inner,
                    previous_text_inner);
                left_context_inner := Trim(previous_previous_text_inner) +
                    Trim(previous_text_inner);
                has_left_context_inner := left_context_inner <> '';
                if has_left_context_inner and
                    get_cached_char_lm_scores(state_texts_inner,
                    context_scores_inner, clsm_context,
                    left_context_inner) and
                    (Length(context_scores_inner) = Length(state_texts_inner)) then
                begin
                    Exit;
                end;

                context_scores_inner := Copy(suffix_scores_inner);
                if not has_left_context_inner then
                begin
                    for idx_inner := 0 to High(context_scores_inner) do
                    begin
                        context_scores_inner[idx_inner] :=
                            suffix_scores_inner[idx_inner];
                    end;
                end;
            end;

            procedure build_long_second_stage_features_local(
                const state_inner: TExactChunkChainState;
                const original_rank_inner: Integer;
                const first_stage_score_inner: Integer;
                const char_lm_score_inner: Integer;
                const char_lm_suffix_score_inner: Integer;
                const char_lm_context_score_inner: Integer;
                const has_left_context_inner: Boolean;
                const word_lm_bonus_inner: Integer;
                const word_lm_stats_inner: TLongWordLmBoundaryStats;
                const single_segments_inner: Integer;
                const max_segment_units_inner: Integer;
                out features_inner: TncLongSecondStageFeatures);
            const
                c_unknown_lexical_rank = 65;
            var
                parts_inner: TArray<string>;
                part_inner: string;
                part_text_inner: string;
                part_units_inner: Integer;
                part_idx_inner: Integer;
                query_offset_inner: Integer;
                query_key_inner: string;
                lookup_results_inner: TncCandidateList;
                lookup_idx_inner: Integer;
                eligible_rank_inner: Integer;
                selected_rank_inner: Integer;
                selected_weight_inner: Integer;
                top_weight_inner: Integer;
                raw_weight_inner: Integer;
                lookup_text_inner: string;
                known_segments_inner: Integer;
                top_segments_inner: Integer;
                margin_inner: Integer;
                path_structure_inner: TncLongPathStructureCacheValue;
                path_structure_cached_inner: Boolean;
                lexical_cache_key_inner: string;
                lexical_cache_value_inner: TncExactLexicalFeatureCacheValue;
                lexical_cache_hit_inner: Boolean;
            begin
                FillChar(features_inner, SizeOf(features_inner), 0);
                features_inner.first_stage_score := first_stage_score_inner;
                features_inner.base_score := state_inner.score;
                features_inner.char_lm_score := char_lm_score_inner;
                features_inner.char_lm_suffix_score :=
                    char_lm_suffix_score_inner;
                features_inner.char_lm_context_score :=
                    char_lm_context_score_inner;
                features_inner.char_lm_context_gain :=
                    char_lm_context_score_inner - char_lm_suffix_score_inner;
                if syllable_count_local > 0 then
                begin
                    features_inner.char_lm_per_unit :=
                        char_lm_score_inner div syllable_count_local;
                end;
                features_inner.word_lm_bonus := word_lm_bonus_inner;
                if state_inner.segments > 1 then
                begin
                    features_inner.word_lm_per_boundary :=
                        word_lm_bonus_inner div (state_inner.segments - 1);
                end;
                features_inner.word_lm_boundary_count :=
                    word_lm_stats_inner.boundary_count;
                features_inner.word_lm_boundary_min :=
                    word_lm_stats_inner.min_bonus;
                features_inner.word_lm_boundary_max :=
                    word_lm_stats_inner.max_bonus;
                features_inner.word_lm_boundary_first :=
                    word_lm_stats_inner.first_bonus;
                features_inner.word_lm_boundary_last :=
                    word_lm_stats_inner.last_bonus;
                features_inner.word_lm_zero_count :=
                    word_lm_stats_inner.zero_count;
                if word_lm_stats_inner.boundary_count > 0 then
                begin
                    features_inner.word_lm_supported_ratio :=
                        word_lm_stats_inner.supported_count * 1000 div
                        word_lm_stats_inner.boundary_count;
                    features_inner.word_lm_strong_ratio :=
                        word_lm_stats_inner.strong_count * 1000 div
                        word_lm_stats_inner.boundary_count;
                    features_inner.word_lm_trigram_ratio :=
                        word_lm_stats_inner.trigram_count * 1000 div
                        word_lm_stats_inner.boundary_count;
                end;
                features_inner.segments := state_inner.segments;
                features_inner.single_segments := single_segments_inner;
                features_inner.multi_segments := Max(0,
                    state_inner.segments - single_segments_inner);
                features_inner.max_segment_units := max_segment_units_inner;
                features_inner.min_segment_units := MaxInt;
                features_inner.anchor_units := state_inner.anchor_units;
                features_inner.has_anchor := state_inner.has_anchor;
                features_inner.baseline_lineage :=
                    state_inner.baseline_lineage;
                features_inner.original_rank := original_rank_inner;
                features_inner.input_syllable_count := syllable_count_local;
                features_inner.has_left_context := has_left_context_inner;
                if state_inner.segments > 0 then
                begin
                    features_inner.score_per_segment :=
                        state_inner.score div state_inner.segments;
                end;
                if (m_dictionary <> nil) and (lookup_text <> '') and
                    (state_inner.path <> '') then
                begin
                    features_inner.query_path_bonus :=
                        get_cached_query_segment_path_bonus(
                        lookup_text, state_inner.path);
                    features_inner.query_path_penalty :=
                        get_cached_query_segment_path_penalty(
                        lookup_text, state_inner.path);
                end;

                features_inner.lexical_weight_min := MaxInt;
                features_inner.lexical_margin_min := MaxInt;
                features_inner.min_segment_units := MaxInt;
                path_structure_cached_inner :=
                    get_cached_long_path_structure(state_inner.path,
                    path_structure_inner);
                if path_structure_cached_inner then
                begin
                    features_inner.min_segment_units :=
                        path_structure_inner.min_segment_units;
                    features_inner.segment_units_square_sum :=
                        path_structure_inner.segment_units_square_sum;
                    features_inner.first_segment_units :=
                        path_structure_inner.first_segment_units;
                    features_inner.last_segment_units :=
                        path_structure_inner.last_segment_units;
                end;
                parts_inner := state_inner.path.Split(
                    [c_segment_path_separator]);
                query_offset_inner := 0;
                known_segments_inner := 0;
                top_segments_inner := 0;
                for part_idx_inner := 0 to High(parts_inner) do
                begin
                    part_inner := parts_inner[part_idx_inner];
                    part_text_inner := Trim(part_inner);
                    part_units_inner := get_candidate_text_unit_count(
                        part_text_inner);
                    if part_units_inner <= 0 then
                    begin
                        Continue;
                    end;

                    if (not path_structure_cached_inner) and
                        (features_inner.first_segment_units = 0) then
                    begin
                        features_inner.first_segment_units := part_units_inner;
                    end;
                    if not path_structure_cached_inner then
                    begin
                        features_inner.last_segment_units := part_units_inner;
                        features_inner.min_segment_units := Min(
                            features_inner.min_segment_units, part_units_inner);
                        Inc(features_inner.segment_units_square_sum,
                            part_units_inner * part_units_inner);
                    end;

                    selected_weight_inner := 0;
                    selected_rank_inner := 0;
                    top_weight_inner := 0;
                    eligible_rank_inner := 0;
                    query_key_inner := build_query_key_local(
                        query_offset_inner, part_units_inner);
                    lexical_cache_key_inner := query_key_inner + #1 +
                        part_text_inner;
                    lexical_cache_hit_inner :=
                        (m_lookup_exact_lexical_feature_cache <> nil) and
                        m_lookup_exact_lexical_feature_cache.TryGetValue(
                        lexical_cache_key_inner, lexical_cache_value_inner);
                    if lexical_cache_hit_inner then
                    begin
                        selected_rank_inner :=
                            lexical_cache_value_inner.selected_rank;
                        selected_weight_inner :=
                            lexical_cache_value_inner.selected_weight;
                        top_weight_inner := lexical_cache_value_inner.top_weight;
                    end
                    else
                    begin
                        if (query_key_inner <> '') and
                            lookup_exact_full_pinyin_cached_local(query_key_inner,
                            lookup_results_inner) then
                        begin
                            for lookup_idx_inner := 0 to
                                High(lookup_results_inner) do
                            begin
                                if eligible_rank_inner >=
                                    c_unknown_lexical_rank - 1 then
                                begin
                                    Break;
                                end;
                                if Trim(lookup_results_inner[
                                    lookup_idx_inner].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                lookup_text_inner := Trim(lookup_results_inner[
                                    lookup_idx_inner].text);
                                if get_candidate_text_unit_count(
                                    lookup_text_inner) <> part_units_inner then
                                begin
                                    Continue;
                                end;
                                Inc(eligible_rank_inner);
                                raw_weight_inner :=
                                    get_exact_chunk_raw_weight_local(
                                    lookup_results_inner[lookup_idx_inner]);
                                top_weight_inner := Max(top_weight_inner,
                                    raw_weight_inner);
                                if (selected_rank_inner = 0) and
                                    SameText(lookup_text_inner,
                                    part_text_inner) then
                                begin
                                    selected_rank_inner := eligible_rank_inner;
                                    selected_weight_inner := raw_weight_inner;
                                end;
                            end;
                        end;
                        if m_lookup_exact_lexical_feature_cache <> nil then
                        begin
                            lexical_cache_value_inner.selected_rank :=
                                selected_rank_inner;
                            lexical_cache_value_inner.selected_weight :=
                                selected_weight_inner;
                            lexical_cache_value_inner.top_weight :=
                                top_weight_inner;
                            m_lookup_exact_lexical_feature_cache.AddOrSetValue(
                                lexical_cache_key_inner,
                                lexical_cache_value_inner);
                        end;
                    end;

                    if selected_rank_inner = 0 then
                    begin
                        selected_rank_inner := c_unknown_lexical_rank;
                    end
                    else
                    begin
                        Inc(known_segments_inner);
                        if selected_weight_inner >= top_weight_inner then
                        begin
                            Inc(top_segments_inner);
                        end;
                    end;
                    margin_inner := selected_weight_inner - top_weight_inner;
                    Inc(features_inner.lexical_weight_sum,
                        selected_weight_inner);
                    if selected_weight_inner <
                        features_inner.lexical_weight_min then
                    begin
                        features_inner.lexical_weight_min :=
                            selected_weight_inner;
                    end;
                    features_inner.lexical_weight_max := Max(
                        features_inner.lexical_weight_max,
                        selected_weight_inner);
                    Inc(features_inner.lexical_rank_sum,
                        selected_rank_inner);
                    features_inner.lexical_rank_max := Max(
                        features_inner.lexical_rank_max,
                        selected_rank_inner);
                    Inc(features_inner.lexical_margin_sum, margin_inner);
                    features_inner.lexical_margin_min := Min(
                        features_inner.lexical_margin_min, margin_inner);
                    if part_idx_inner = 0 then
                    begin
                        features_inner.first_segment_weight :=
                            selected_weight_inner;
                    end;
                    features_inner.last_segment_weight :=
                        selected_weight_inner;
                    Inc(query_offset_inner, part_units_inner);
                end;

                if features_inner.min_segment_units = MaxInt then
                begin
                    features_inner.min_segment_units := 0;
                end;
                if features_inner.lexical_weight_min = MaxInt then
                begin
                    features_inner.lexical_weight_min := 0;
                end;
                if features_inner.lexical_margin_min = MaxInt then
                begin
                    features_inner.lexical_margin_min := 0;
                end;
                if state_inner.segments > 0 then
                begin
                    features_inner.lexical_weight_mean :=
                        features_inner.lexical_weight_sum div
                        state_inner.segments;
                    features_inner.lexical_known_ratio :=
                        known_segments_inner * 1000 div state_inner.segments;
                    features_inner.lexical_top_ratio :=
                        top_segments_inner * 1000 div state_inner.segments;
                end;
                if syllable_count_local > 0 then
                begin
                    features_inner.lexical_weight_per_unit :=
                        features_inner.lexical_weight_sum div
                        syllable_count_local;
                end;
            end;

            function get_long_chain_ranker_correction_local(
                const state_inner: TExactChunkChainState;
                const char_lm_score_inner: Integer;
                const word_lm_bonus_inner: Integer;
                const profile_inner: Integer;
                const single_segments_inner: Integer;
                const max_segment_units_inner: Integer): Integer;
            var
                correction_inner: Int64;
                correction_cap_inner: Integer;
            begin
                Result := 0;
                if (profile_inner <= 0) or (syllable_count_local <= 5) then
                begin
                    Exit;
                end;

                { Keep the first-stage feature contract identical for every
                  learned second-stage profile. The LambdaRank training rows
                  use this bounded baseline; applying the retired profile-2
                  linear coefficients here would shift both first_stage_score
                  and original_rank before the tree model sees them. }
                correction_inner := Int64(char_lm_score_inner) * 15 +
                    Int64(word_lm_bonus_inner) * 44 -
                    Int64(state_inner.score) * 202 div 1000 +
                    Int64(state_inner.segments) * 1097 +
                    Int64(single_segments_inner) * 2564 +
                    Int64(max_segment_units_inner) * 8698 +
                    Int64(state_inner.anchor_units) * 8634;
                correction_cap_inner := 15000;

                if correction_inner > correction_cap_inner then
                begin
                    correction_inner := correction_cap_inner;
                end
                else if correction_inner < -correction_cap_inner then
                begin
                    correction_inner := -correction_cap_inner;
                end;
                Result := Integer(correction_inner);
            end;

            function get_long_second_stage_score_local(
                const profile_inner: Integer;
                const features_inner: TncLongSecondStageFeatures): Int64;
            begin
                Result := features_inner.first_stage_score;
                if (profile_inner < 2) or (syllable_count_local <= 5) then
                begin
                    Exit;
                end;
                Result := long_second_stage_ranker_score(features_inner);
            end;

            procedure sort_fast_states_by_order_score_local(
                var states_inner: TExactChunkChainStateArray);
            var
                idx_inner: Integer;
                insert_idx_inner: Integer;
                baseline_count_inner: Integer;
                extra_count_inner: Integer;
                ordered_count_inner: Integer;
                baseline_indices_inner: TArray<Integer>;
                extra_indices_inner: TArray<Integer>;
                ordered_states_inner: TExactChunkChainStateArray;
                swap_state_inner: TExactChunkChainState;
                current_index_inner: Integer;
                first_stage_score_inner: Integer;
                first_stage_scores_inner: TArray<Integer>;
                first_stage_ranks_inner: TArray<Integer>;
                second_stage_scores_inner: TArray<Int64>;
                word_lm_bonuses_inner: TArray<Integer>;
                word_lm_stats_values_inner: TArray<TLongWordLmBoundaryStats>;
                single_segment_counts_inner: TArray<Integer>;
                max_segment_units_values_inner: TArray<Integer>;
                feature_values_inner: TArray<TncLongSecondStageFeatures>;
                state_texts_inner: TArray<string>;
                char_scores_inner: TArray<Integer>;
                char_suffix_scores_inner: TArray<Integer>;
                char_context_scores_inner: TArray<Integer>;
                ranker_profile_inner: Integer;
                char_scores_ok_inner: Boolean;
                has_left_context_inner: Boolean;
                left_context_inner: string;
                compare_idx_inner: Integer;
                pairwise_features_inner: TncLongPairwiseResidualFeatures;
                pairwise_score_inner: Int64;
                best_pairwise_score_inner: Int64;
                best_pairwise_index_inner: Integer;

                function is_extra_index_better_inner(
                    const left_inner: Integer;
                    const right_inner: Integer): Boolean;
                begin
                    Result := second_stage_scores_inner[left_inner] >
                        second_stage_scores_inner[right_inner];
                    if second_stage_scores_inner[left_inner] =
                        second_stage_scores_inner[right_inner] then
                    begin
                        Result := first_stage_scores_inner[left_inner] >
                            first_stage_scores_inner[right_inner];
                    end;
                end;

                function is_model_state_better_inner(
                    const left_inner: TExactChunkChainState;
                    const right_inner: TExactChunkChainState): Boolean;
                begin
                    Result := left_inner.ranker_second_stage_score >
                        right_inner.ranker_second_stage_score;
                    if left_inner.ranker_second_stage_score =
                        right_inner.ranker_second_stage_score then
                    begin
                        Result := left_inner.ranker_original_rank <
                            right_inner.ranker_original_rank;
                    end;
                    if (left_inner.ranker_second_stage_score =
                        right_inner.ranker_second_stage_score) and
                        (left_inner.ranker_original_rank =
                        right_inner.ranker_original_rank) then
                    begin
                        Result := CompareText(left_inner.text,
                            right_inner.text) < 0;
                    end;
                end;
            begin
                if Length(states_inner) = 0 then
                begin
                    Exit;
                end;
                SetLength(first_stage_scores_inner, Length(states_inner));
                SetLength(first_stage_ranks_inner, Length(states_inner));
                SetLength(second_stage_scores_inner, Length(states_inner));
                SetLength(word_lm_bonuses_inner, Length(states_inner));
                SetLength(word_lm_stats_values_inner, Length(states_inner));
                SetLength(single_segment_counts_inner, Length(states_inner));
                SetLength(max_segment_units_values_inner,
                    Length(states_inner));
                SetLength(feature_values_inner, Length(states_inner));
                ranker_profile_inner := get_long_chain_ranker_profile_local;
                char_scores_ok_inner := False;
                has_left_context_inner := False;
                if (ranker_profile_inner > 0) and
                    (syllable_count_local > 5) and (m_dictionary <> nil) then
                begin
                    SetLength(state_texts_inner, Length(states_inner));
                    for idx_inner := 0 to High(states_inner) do
                    begin
                        state_texts_inner[idx_inner] :=
                            Trim(states_inner[idx_inner].text);
                    end;
                    get_long_ranker_char_scores_local(state_texts_inner,
                        char_scores_inner, char_suffix_scores_inner,
                        char_context_scores_inner, char_scores_ok_inner,
                        has_left_context_inner, left_context_inner);
                end;
                for idx_inner := 0 to High(states_inner) do
                begin
                    states_inner[idx_inner].ranker_model_ordered := False;
                    states_inner[idx_inner].ranker_features_valid := False;
                    states_inner[idx_inner].ranker_feature_context_key := '';
                    get_long_chain_structure_stats_local(
                        states_inner[idx_inner],
                        single_segment_counts_inner[idx_inner],
                        max_segment_units_values_inner[idx_inner]);
                    first_stage_score_inner :=
                        get_fast_state_order_score_local(states_inner[idx_inner],
                        word_lm_bonuses_inner[idx_inner],
                        word_lm_stats_values_inner[idx_inner]);
                    if char_scores_ok_inner then
                    begin
                        Inc(first_stage_score_inner,
                            get_long_chain_ranker_correction_local(
                            states_inner[idx_inner],
                            char_scores_inner[idx_inner],
                            word_lm_bonuses_inner[idx_inner],
                            ranker_profile_inner,
                            single_segment_counts_inner[idx_inner],
                            max_segment_units_values_inner[idx_inner]));
                    end;

                    first_stage_scores_inner[idx_inner] :=
                        first_stage_score_inner;
                end;

                for idx_inner := 0 to High(states_inner) do
                begin
                    first_stage_ranks_inner[idx_inner] := 1;
                    for compare_idx_inner := 0 to High(states_inner) do
                    begin
                        if (first_stage_scores_inner[compare_idx_inner] >
                            first_stage_scores_inner[idx_inner]) or
                            ((first_stage_scores_inner[compare_idx_inner] =
                            first_stage_scores_inner[idx_inner]) and
                            (compare_idx_inner < idx_inner)) then
                        begin
                            Inc(first_stage_ranks_inner[idx_inner]);
                        end;
                    end;
                end;

                for idx_inner := 0 to High(states_inner) do
                begin
                    if char_scores_ok_inner then
                    begin
                        build_long_second_stage_features_local(
                            states_inner[idx_inner],
                            first_stage_ranks_inner[idx_inner],
                            first_stage_scores_inner[idx_inner],
                            char_scores_inner[idx_inner],
                            char_suffix_scores_inner[idx_inner],
                            char_context_scores_inner[idx_inner],
                            has_left_context_inner,
                            word_lm_bonuses_inner[idx_inner],
                            word_lm_stats_values_inner[idx_inner],
                            single_segment_counts_inner[idx_inner],
                            max_segment_units_values_inner[idx_inner],
                            feature_values_inner[idx_inner]);
                        second_stage_scores_inner[idx_inner] :=
                            get_long_second_stage_score_local(
                            ranker_profile_inner,
                            feature_values_inner[idx_inner]);
                        states_inner[idx_inner].ranker_features :=
                            feature_values_inner[idx_inner];
                        states_inner[idx_inner].ranker_features_valid := True;
                        states_inner[idx_inner].ranker_feature_context_key :=
                            left_context_inner;
                    end
                    else
                    begin
                        second_stage_scores_inner[idx_inner] :=
                            first_stage_scores_inner[idx_inner];
                    end;
                    states_inner[idx_inner].ranker_original_rank :=
                        first_stage_ranks_inner[idx_inner];
                    states_inner[idx_inner].ranker_first_stage_score :=
                        first_stage_scores_inner[idx_inner];
                    states_inner[idx_inner].ranker_second_stage_score :=
                        second_stage_scores_inner[idx_inner];
                end;

                if char_scores_ok_inner and (ranker_profile_inner >= 2) then
                begin
                    ordered_states_inner := Copy(states_inner);
                    for idx_inner := 1 to High(ordered_states_inner) do
                    begin
                        current_index_inner := idx_inner;
                        while (current_index_inner > 0) and
                            is_model_state_better_inner(
                            ordered_states_inner[current_index_inner],
                            ordered_states_inner[current_index_inner - 1]) do
                        begin
                            swap_state_inner :=
                                ordered_states_inner[current_index_inner - 1];
                            ordered_states_inner[current_index_inner - 1] :=
                                ordered_states_inner[current_index_inner];
                            ordered_states_inner[current_index_inner] :=
                                swap_state_inner;
                            Dec(current_index_inner);
                        end;
                    end;

                    { The residual is deliberately restricted to the final
                      long-chain order. It reuses already computed features
                      and may promote only one challenger, so it cannot widen
                      search or affect short/exact input. }
                    best_pairwise_index_inner := -1;
                    best_pairwise_score_inner := Low(Int64);
                    for idx_inner := 1 to High(ordered_states_inner) do
                    begin
                        build_long_pairwise_residual_features(
                            ordered_states_inner[idx_inner].ranker_features,
                            ordered_states_inner[0].ranker_features,
                            idx_inner + 1,
                            ordered_states_inner[idx_inner].
                                ranker_second_stage_score,
                            ordered_states_inner[0].
                                ranker_second_stage_score,
                            pairwise_features_inner);
                        pairwise_score_inner :=
                            long_pairwise_residual_score(
                            pairwise_features_inner);
                        if pairwise_score_inner >
                            best_pairwise_score_inner then
                        begin
                            best_pairwise_score_inner :=
                                pairwise_score_inner;
                            best_pairwise_index_inner := idx_inner;
                        end;
                    end;
                    if (best_pairwise_index_inner > 0) and
                        (best_pairwise_score_inner >=
                        c_long_pairwise_residual_promotion_threshold) then
                    begin
                        swap_state_inner :=
                            ordered_states_inner[best_pairwise_index_inner];
                        for idx_inner := best_pairwise_index_inner downto 1 do
                        begin
                            ordered_states_inner[idx_inner] :=
                                ordered_states_inner[idx_inner - 1];
                        end;
                        ordered_states_inner[0] := swap_state_inner;
                    end;
                    for idx_inner := 0 to High(ordered_states_inner) do
                    begin
                        ordered_states_inner[idx_inner].ranker_model_ordered :=
                            True;
                    end;
                    states_inner := ordered_states_inner;
                    Exit;
                end;

                baseline_count_inner := 0;
                extra_count_inner := 0;
                for idx_inner := 0 to High(states_inner) do
                begin
                    if states_inner[idx_inner].baseline_lineage then
                    begin
                        Inc(baseline_count_inner);
                    end;
                    if not states_inner[idx_inner].baseline_lineage then
                    begin
                        Inc(extra_count_inner);
                    end;
                end;
                SetLength(baseline_indices_inner, baseline_count_inner);
                SetLength(extra_indices_inner, extra_count_inner);
                baseline_count_inner := 0;
                extra_count_inner := 0;
                for idx_inner := 0 to High(states_inner) do
                begin
                    if states_inner[idx_inner].baseline_lineage then
                    begin
                        baseline_indices_inner[baseline_count_inner] := idx_inner;
                        Inc(baseline_count_inner);
                    end;
                    if not states_inner[idx_inner].baseline_lineage then
                    begin
                        extra_indices_inner[extra_count_inner] := idx_inner;
                        Inc(extra_count_inner);
                    end;
                end;

                { The baseline pool is the exact legacy six-state beam. Keep
                  its first-stage order byte-for-byte unless a separately
                  calibrated promotion gate accepts one expanded state. }
                for idx_inner := 1 to High(baseline_indices_inner) do
                begin
                    current_index_inner := baseline_indices_inner[idx_inner];
                    insert_idx_inner := idx_inner - 1;
                    while (insert_idx_inner >= 0) and
                        (first_stage_scores_inner[
                        baseline_indices_inner[insert_idx_inner]] <
                        first_stage_scores_inner[current_index_inner]) do
                    begin
                        baseline_indices_inner[insert_idx_inner + 1] :=
                            baseline_indices_inner[insert_idx_inner];
                        Dec(insert_idx_inner);
                    end;
                    baseline_indices_inner[insert_idx_inner + 1] :=
                        current_index_inner;
                end;
                for idx_inner := 1 to High(extra_indices_inner) do
                begin
                    current_index_inner := extra_indices_inner[idx_inner];
                    insert_idx_inner := idx_inner - 1;
                    while (insert_idx_inner >= 0) and
                        is_extra_index_better_inner(current_index_inner,
                        extra_indices_inner[insert_idx_inner]) do
                    begin
                        extra_indices_inner[insert_idx_inner + 1] :=
                            extra_indices_inner[insert_idx_inner];
                        Dec(insert_idx_inner);
                    end;
                    extra_indices_inner[insert_idx_inner + 1] :=
                        current_index_inner;
                end;

                SetLength(ordered_states_inner, Length(states_inner));
                ordered_count_inner := 0;
                for idx_inner := 0 to High(baseline_indices_inner) do
                begin
                    ordered_states_inner[ordered_count_inner] :=
                        states_inner[baseline_indices_inner[idx_inner]];
                    Inc(ordered_count_inner);
                end;
                for idx_inner := 0 to High(extra_indices_inner) do
                begin
                    ordered_states_inner[ordered_count_inner] :=
                        states_inner[extra_indices_inner[idx_inner]];
                    Inc(ordered_count_inner);
                end;
                SetLength(ordered_states_inner, ordered_count_inner);
                states_inner := ordered_states_inner;
            end;

            procedure capture_final_chain_candidates_local(
                const states_inner: TExactChunkChainStateArray);
            var
                state_texts_inner: TArray<string>;
                char_scores_inner: TArray<Integer>;
                char_suffix_scores_inner: TArray<Integer>;
                char_context_scores_inner: TArray<Integer>;
                captured_inner: TncLongChainCandidateDebugArray;
                state_idx_inner: Integer;
                existing_has_target_inner: Boolean;
                captured_has_target_inner: Boolean;
                char_scores_ok_inner: Boolean;
                has_left_context_inner: Boolean;
                left_context_inner: string;
                word_lm_bonus_inner: Integer;
                word_lm_stats_inner: TLongWordLmBoundaryStats;
                baseline_count_inner: Integer;
                features_inner: TncLongSecondStageFeatures;
            begin
                SetLength(m_runtime_long_chain_candidates,
                    Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    m_runtime_long_chain_candidates[state_idx_inner].text :=
                        Trim(states_inner[state_idx_inner].text);
                    m_runtime_long_chain_candidates[state_idx_inner].segment_path :=
                        states_inner[state_idx_inner].path;
                    m_runtime_long_chain_candidates[state_idx_inner].rank :=
                        state_idx_inner + 1;
                    m_runtime_long_chain_candidates[state_idx_inner].original_rank :=
                        states_inner[state_idx_inner].ranker_original_rank;
                    m_runtime_long_chain_candidates[state_idx_inner].first_stage_score :=
                        states_inner[state_idx_inner].ranker_first_stage_score;
                    m_runtime_long_chain_candidates[state_idx_inner].second_stage_score :=
                        states_inner[state_idx_inner].ranker_second_stage_score;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_count :=
                        states_inner[state_idx_inner].retained_exact_edge_count;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_position :=
                        states_inner[state_idx_inner].retained_exact_edge_position;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_units :=
                        states_inner[state_idx_inner].retained_exact_edge_units;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_rank :=
                        states_inner[state_idx_inner].retained_exact_edge_rank;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_top_weight :=
                        states_inner[state_idx_inner].retained_exact_edge_top_weight;
                    m_runtime_long_chain_candidates[state_idx_inner].retained_exact_edge_confidence :=
                        states_inner[state_idx_inner].retained_exact_edge_confidence;
                    m_runtime_long_chain_candidates[state_idx_inner].feature_query_key :=
                        lookup_text;
                    m_runtime_long_chain_candidates[state_idx_inner].feature_context_key :=
                        states_inner[state_idx_inner].ranker_feature_context_key;
                    m_runtime_long_chain_candidates[state_idx_inner].features_valid :=
                        states_inner[state_idx_inner].ranker_features_valid;
                    if states_inner[state_idx_inner].ranker_features_valid then
                    begin
                        m_runtime_long_chain_candidates[state_idx_inner].char_lm_score :=
                            states_inner[state_idx_inner].ranker_features.char_lm_score;
                        m_runtime_long_chain_candidates[state_idx_inner].char_lm_suffix_score :=
                            states_inner[state_idx_inner].ranker_features.char_lm_suffix_score;
                        m_runtime_long_chain_candidates[state_idx_inner].char_lm_context_score :=
                            states_inner[state_idx_inner].ranker_features.char_lm_context_score;
                        m_runtime_long_chain_candidates[state_idx_inner].path_segments :=
                            states_inner[state_idx_inner].ranker_features.segments;
                        m_runtime_long_chain_candidates[state_idx_inner].path_single_segments :=
                            states_inner[state_idx_inner].ranker_features.single_segments;
                        m_runtime_long_chain_candidates[state_idx_inner].path_max_segment_units :=
                            states_inner[state_idx_inner].ranker_features.max_segment_units;
                        m_runtime_long_chain_candidates[state_idx_inner].query_path_bonus :=
                            states_inner[state_idx_inner].ranker_features.query_path_bonus;
                        m_runtime_long_chain_candidates[state_idx_inner].query_path_penalty :=
                            states_inner[state_idx_inner].ranker_features.query_path_penalty;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_bonus :=
                            states_inner[state_idx_inner].ranker_features.word_lm_bonus;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_boundary_count :=
                            states_inner[state_idx_inner].ranker_features.word_lm_boundary_count;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_boundary_min :=
                            states_inner[state_idx_inner].ranker_features.word_lm_boundary_min;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_boundary_max :=
                            states_inner[state_idx_inner].ranker_features.word_lm_boundary_max;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_boundary_first :=
                            states_inner[state_idx_inner].ranker_features.word_lm_boundary_first;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_boundary_last :=
                            states_inner[state_idx_inner].ranker_features.word_lm_boundary_last;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_supported_ratio :=
                            states_inner[state_idx_inner].ranker_features.word_lm_supported_ratio;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_strong_ratio :=
                            states_inner[state_idx_inner].ranker_features.word_lm_strong_ratio;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_trigram_ratio :=
                            states_inner[state_idx_inner].ranker_features.word_lm_trigram_ratio;
                        m_runtime_long_chain_candidates[state_idx_inner].word_lm_zero_count :=
                            states_inner[state_idx_inner].ranker_features.word_lm_zero_count;
                    end;
                end;

                if (m_debug_target_recall_text = '') or
                    (Length(states_inner) = 0) then
                begin
                    Exit;
                end;

                SetLength(state_texts_inner, Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    state_texts_inner[state_idx_inner] :=
                        Trim(states_inner[state_idx_inner].text);
                end;
                get_long_ranker_char_scores_local(state_texts_inner,
                    char_scores_inner, char_suffix_scores_inner,
                    char_context_scores_inner, char_scores_ok_inner,
                    has_left_context_inner, left_context_inner);
                if not char_scores_ok_inner then
                begin
                    has_left_context_inner := False;
                end;

                captured_has_target_inner := False;
                baseline_count_inner := 0;
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    if states_inner[state_idx_inner].baseline_lineage then
                    begin
                        Inc(baseline_count_inner);
                    end;
                end;
                SetLength(captured_inner, Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    captured_inner[state_idx_inner].original_rank :=
                        states_inner[state_idx_inner].ranker_original_rank;
                    captured_inner[state_idx_inner].final_rank :=
                        state_idx_inner + 1;
                    get_long_chain_structure_stats_local(
                        states_inner[state_idx_inner],
                        captured_inner[state_idx_inner].single_segments,
                        captured_inner[state_idx_inner].max_segment_units);
                    get_fast_state_order_score_local(
                        states_inner[state_idx_inner], word_lm_bonus_inner,
                        word_lm_stats_inner);
                    build_long_second_stage_features_local(
                        states_inner[state_idx_inner],
                        states_inner[state_idx_inner].ranker_original_rank,
                        states_inner[state_idx_inner].ranker_first_stage_score,
                        char_scores_inner[state_idx_inner],
                        char_suffix_scores_inner[state_idx_inner],
                        char_context_scores_inner[state_idx_inner],
                        has_left_context_inner, word_lm_bonus_inner,
                        word_lm_stats_inner,
                        captured_inner[state_idx_inner].single_segments,
                        captured_inner[state_idx_inner].max_segment_units,
                        features_inner);
                    captured_inner[state_idx_inner].first_stage_score :=
                        states_inner[state_idx_inner].ranker_first_stage_score;
                    captured_inner[state_idx_inner].second_stage_score :=
                        states_inner[state_idx_inner].ranker_second_stage_score;
                    captured_inner[state_idx_inner].order_score :=
                        captured_inner[state_idx_inner].first_stage_score;
                    captured_inner[state_idx_inner].base_score :=
                        features_inner.base_score;
                    captured_inner[state_idx_inner].char_lm_score :=
                        features_inner.char_lm_score;
                    captured_inner[state_idx_inner].char_lm_suffix_score :=
                        features_inner.char_lm_suffix_score;
                    captured_inner[state_idx_inner].char_lm_context_score :=
                        features_inner.char_lm_context_score;
                    captured_inner[state_idx_inner].char_lm_context_gain :=
                        features_inner.char_lm_context_gain;
                    captured_inner[state_idx_inner].char_lm_per_unit :=
                        features_inner.char_lm_per_unit;
                    captured_inner[state_idx_inner].word_lm_bonus :=
                        features_inner.word_lm_bonus;
                    captured_inner[state_idx_inner].word_lm_per_boundary :=
                        features_inner.word_lm_per_boundary;
                    captured_inner[state_idx_inner].word_lm_boundary_count :=
                        features_inner.word_lm_boundary_count;
                    captured_inner[state_idx_inner].word_lm_boundary_min :=
                        features_inner.word_lm_boundary_min;
                    captured_inner[state_idx_inner].word_lm_boundary_max :=
                        features_inner.word_lm_boundary_max;
                    captured_inner[state_idx_inner].word_lm_boundary_first :=
                        features_inner.word_lm_boundary_first;
                    captured_inner[state_idx_inner].word_lm_boundary_last :=
                        features_inner.word_lm_boundary_last;
                    captured_inner[state_idx_inner].word_lm_supported_ratio :=
                        features_inner.word_lm_supported_ratio;
                    captured_inner[state_idx_inner].word_lm_strong_ratio :=
                        features_inner.word_lm_strong_ratio;
                    captured_inner[state_idx_inner].word_lm_trigram_ratio :=
                        features_inner.word_lm_trigram_ratio;
                    captured_inner[state_idx_inner].word_lm_zero_count :=
                        features_inner.word_lm_zero_count;
                    captured_inner[state_idx_inner].lexical_weight_sum :=
                        features_inner.lexical_weight_sum;
                    captured_inner[state_idx_inner].lexical_weight_min :=
                        features_inner.lexical_weight_min;
                    captured_inner[state_idx_inner].lexical_weight_max :=
                        features_inner.lexical_weight_max;
                    captured_inner[state_idx_inner].lexical_weight_mean :=
                        features_inner.lexical_weight_mean;
                    captured_inner[state_idx_inner].lexical_weight_per_unit :=
                        features_inner.lexical_weight_per_unit;
                    captured_inner[state_idx_inner].lexical_known_ratio :=
                        features_inner.lexical_known_ratio;
                    captured_inner[state_idx_inner].lexical_top_ratio :=
                        features_inner.lexical_top_ratio;
                    captured_inner[state_idx_inner].lexical_rank_sum :=
                        features_inner.lexical_rank_sum;
                    captured_inner[state_idx_inner].lexical_rank_max :=
                        features_inner.lexical_rank_max;
                    captured_inner[state_idx_inner].lexical_margin_sum :=
                        features_inner.lexical_margin_sum;
                    captured_inner[state_idx_inner].lexical_margin_min :=
                        features_inner.lexical_margin_min;
                    captured_inner[state_idx_inner].first_segment_weight :=
                        features_inner.first_segment_weight;
                    captured_inner[state_idx_inner].last_segment_weight :=
                        features_inner.last_segment_weight;
                    captured_inner[state_idx_inner].segments :=
                        features_inner.segments;
                    captured_inner[state_idx_inner].multi_segments :=
                        features_inner.multi_segments;
                    captured_inner[state_idx_inner].min_segment_units :=
                        features_inner.min_segment_units;
                    captured_inner[state_idx_inner].segment_units_square_sum :=
                        features_inner.segment_units_square_sum;
                    captured_inner[state_idx_inner].first_segment_units :=
                        features_inner.first_segment_units;
                    captured_inner[state_idx_inner].last_segment_units :=
                        features_inner.last_segment_units;
                    captured_inner[state_idx_inner].anchor_units :=
                        features_inner.anchor_units;
                    captured_inner[state_idx_inner].has_anchor :=
                        features_inner.has_anchor;
                    captured_inner[state_idx_inner].baseline_lineage :=
                        features_inner.baseline_lineage;
                    captured_inner[state_idx_inner].retained_exact_edge_count :=
                        states_inner[state_idx_inner].retained_exact_edge_count;
                    captured_inner[state_idx_inner].retained_exact_edge_position :=
                        states_inner[state_idx_inner].retained_exact_edge_position;
                    captured_inner[state_idx_inner].retained_exact_edge_units :=
                        states_inner[state_idx_inner].retained_exact_edge_units;
                    captured_inner[state_idx_inner].retained_exact_edge_rank :=
                        states_inner[state_idx_inner].retained_exact_edge_rank;
                    captured_inner[state_idx_inner].retained_exact_edge_top_weight :=
                        states_inner[state_idx_inner].retained_exact_edge_top_weight;
                    captured_inner[state_idx_inner].retained_exact_edge_confidence :=
                        states_inner[state_idx_inner].retained_exact_edge_confidence;
                    captured_inner[state_idx_inner].has_left_context :=
                        features_inner.has_left_context;
                    captured_inner[state_idx_inner].query_path_bonus :=
                        features_inner.query_path_bonus;
                    captured_inner[state_idx_inner].query_path_penalty :=
                        features_inner.query_path_penalty;
                    captured_inner[state_idx_inner].score_per_segment :=
                        features_inner.score_per_segment;
                    captured_inner[state_idx_inner].promotion_rank := 0;
                    if (not states_inner[state_idx_inner].baseline_lineage) and
                        (state_idx_inner < baseline_count_inner) then
                    begin
                        captured_inner[state_idx_inner].promotion_rank :=
                            state_idx_inner + 1;
                    end;
                    captured_inner[state_idx_inner].target := SameText(
                        state_texts_inner[state_idx_inner],
                        m_debug_target_recall_text);
                    captured_inner[state_idx_inner].text :=
                        state_texts_inner[state_idx_inner];
                    captured_inner[state_idx_inner].segment_path :=
                        states_inner[state_idx_inner].path;
                    if captured_inner[state_idx_inner].target then
                    begin
                        captured_has_target_inner := True;
                    end;
                end;

                existing_has_target_inner := False;
                for state_idx_inner := 0 to
                    High(m_debug_long_chain_candidates) do
                begin
                    if m_debug_long_chain_candidates[state_idx_inner].target then
                    begin
                        existing_has_target_inner := True;
                        Break;
                    end;
                end;
                if existing_has_target_inner and
                    (not captured_has_target_inner) then
                begin
                    Exit;
                end;
                m_debug_long_chain_candidates := captured_inner;
            end;

            procedure remember_retained_exact_edge_local(
                const position_inner: Integer;
                const state_inner: TExactChunkChainState;
                const confidence_inner: Int64);
            var
                edge_idx_inner: Integer;
                edge_units_inner: Integer;
                item_inner: TncLongRetainedExactEdge;
            begin
                edge_units_inner := get_candidate_text_unit_count(
                    state_inner.first_chunk_text);
                if (position_inner < 0) or (edge_units_inner < 2) or
                    (edge_units_inner > c_chain_lattice_max_chunk_syllables) or
                    (state_inner.first_chunk_exact_rank <= 0) or
                    (Trim(state_inner.first_chunk_query_key) = '') or
                    (Trim(state_inner.first_chunk_text) = '') then
                begin
                    Exit;
                end;
                for edge_idx_inner := 0 to
                    High(m_runtime_long_retained_exact_edges) do
                begin
                    if m_runtime_long_retained_exact_edges[
                        edge_idx_inner].position <> position_inner then
                    begin
                        Continue;
                    end;
                    if confidence_inner <= m_runtime_long_retained_exact_edges[
                        edge_idx_inner].confidence then
                    begin
                        Exit;
                    end;
                    item_inner := m_runtime_long_retained_exact_edges[
                        edge_idx_inner];
                    item_inner.chunk_units := edge_units_inner;
                    item_inner.exact_rank :=
                        state_inner.first_chunk_exact_rank;
                    item_inner.raw_weight :=
                        state_inner.first_chunk_raw_weight;
                    item_inner.top_weight :=
                        state_inner.first_chunk_top_weight;
                    item_inner.confidence := confidence_inner;
                    item_inner.query_key :=
                        state_inner.first_chunk_query_key;
                    item_inner.text := state_inner.first_chunk_text;
                    m_runtime_long_retained_exact_edges[
                        edge_idx_inner] := item_inner;
                    Exit;
                end;
                item_inner := Default(TncLongRetainedExactEdge);
                item_inner.position := position_inner;
                item_inner.chunk_units := edge_units_inner;
                item_inner.exact_rank := state_inner.first_chunk_exact_rank;
                item_inner.raw_weight := state_inner.first_chunk_raw_weight;
                item_inner.top_weight := state_inner.first_chunk_top_weight;
                item_inner.confidence := confidence_inner;
                item_inner.query_key := state_inner.first_chunk_query_key;
                item_inner.text := state_inner.first_chunk_text;
                SetLength(m_runtime_long_retained_exact_edges,
                    Length(m_runtime_long_retained_exact_edges) + 1);
                m_runtime_long_retained_exact_edges[
                    High(m_runtime_long_retained_exact_edges)] := item_inner;
            end;

            procedure prune_baseline_fast_states_by_char_lm_local(
                var states_inner: TExactChunkChainStateArray;
                const state_start_pos_inner: Integer);
            const
                c_char_lm_original_rank_penalty = 20;
            var
                state_texts_inner: TArray<string>;
                char_scores_inner: TArray<Integer>;
                selected_inner: TArray<Boolean>;
                kept_states_inner: TExactChunkChainStateArray;
                state_idx_inner: Integer;
                keep_idx_inner: Integer;
                base_keep_inner: Integer;
                best_idx_inner: Integer;
                best_score_inner: Integer;
                current_score_inner: Integer;
                insert_idx_inner: Integer;
                swap_state_inner: TExactChunkChainState;
                char_scores_ok_inner: Boolean;
                beam_width_inner: Integer;
            begin
                beam_width_inner := Min(c_chain_lattice_baseline_beam_width,
                    Length(states_inner));
                if beam_width_inner <= 0 then
                begin
                    Exit;
                end;
                if Length(states_inner) <= beam_width_inner then
                begin
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        states_inner[state_idx_inner].baseline_lineage := True;
                    end;
                    Exit;
                end;

                SetLength(state_texts_inner, Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    state_texts_inner[state_idx_inner] :=
                        Trim(states_inner[state_idx_inner].text);
                end;
                char_scores_ok_inner := False;
                if m_dictionary <> nil then
                begin
                    if state_start_pos_inner > 0 then
                    begin
                        char_scores_ok_inner :=
                            get_cached_char_lm_scores(state_texts_inner,
                            char_scores_inner, clsm_suffix, '');
                    end
                    else
                    begin
                        char_scores_ok_inner :=
                            get_cached_char_lm_scores(state_texts_inner,
                            char_scores_inner, clsm_full, '');
                    end;
                end;
                if (not char_scores_ok_inner) or
                    (Length(char_scores_inner) <> Length(states_inner)) then
                begin
                    SetLength(states_inner, beam_width_inner);
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        states_inner[state_idx_inner].baseline_lineage := True;
                    end;
                    Exit;
                end;

                SetLength(selected_inner, Length(states_inner));
                SetLength(kept_states_inner, beam_width_inner);
                keep_idx_inner := 0;
                base_keep_inner := Min(get_chain_char_base_slots_local,
                    beam_width_inner);
                for state_idx_inner := 0 to base_keep_inner - 1 do
                begin
                    kept_states_inner[keep_idx_inner] :=
                        states_inner[state_idx_inner];
                    kept_states_inner[keep_idx_inner].baseline_lineage := True;
                    selected_inner[state_idx_inner] := True;
                    Inc(keep_idx_inner);
                end;
                while keep_idx_inner < beam_width_inner do
                begin
                    best_idx_inner := -1;
                    best_score_inner := Low(Integer);
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if selected_inner[state_idx_inner] then
                        begin
                            Continue;
                        end;
                        current_score_inner := char_scores_inner[state_idx_inner] -
                            state_idx_inner * c_char_lm_original_rank_penalty;
                        if (best_idx_inner < 0) or
                            (current_score_inner > best_score_inner) then
                        begin
                            best_idx_inner := state_idx_inner;
                            best_score_inner := current_score_inner;
                        end;
                    end;
                    if best_idx_inner < 0 then
                    begin
                        Break;
                    end;
                    kept_states_inner[keep_idx_inner] := states_inner[best_idx_inner];
                    kept_states_inner[keep_idx_inner].baseline_lineage := True;
                    selected_inner[best_idx_inner] := True;
                    Inc(keep_idx_inner);
                end;
                SetLength(kept_states_inner, keep_idx_inner);

                for state_idx_inner := 1 to High(kept_states_inner) do
                begin
                    insert_idx_inner := state_idx_inner;
                    while (insert_idx_inner > 0) and
                        is_exact_chunk_chain_state_better_local(
                        kept_states_inner[insert_idx_inner],
                        kept_states_inner[insert_idx_inner - 1]) do
                    begin
                        swap_state_inner := kept_states_inner[insert_idx_inner - 1];
                        kept_states_inner[insert_idx_inner - 1] :=
                            kept_states_inner[insert_idx_inner];
                        kept_states_inner[insert_idx_inner] := swap_state_inner;
                        Dec(insert_idx_inner);
                    end;
                end;
                states_inner := kept_states_inner;
            end;

            procedure merge_baseline_and_expanded_fast_states_local(
                var expanded_states_inner: TExactChunkChainStateArray;
                const baseline_states_inner: TExactChunkChainStateArray);
            var
                merged_states_inner: TExactChunkChainStateArray;
                state_idx_inner: Integer;
                merged_idx_inner: Integer;
                insert_idx_inner: Integer;
                swap_state_inner: TExactChunkChainState;
                duplicate_inner: Boolean;
                beam_width_inner: Integer;
            begin
                beam_width_inner := get_chain_beam_width_local;
                SetLength(merged_states_inner, beam_width_inner);
                merged_idx_inner := 0;
                for state_idx_inner := 0 to High(baseline_states_inner) do
                begin
                    if merged_idx_inner >= beam_width_inner then
                    begin
                        Break;
                    end;
                    merged_states_inner[merged_idx_inner] :=
                        baseline_states_inner[state_idx_inner];
                    merged_states_inner[merged_idx_inner].baseline_lineage := True;
                    Inc(merged_idx_inner);
                end;

                for state_idx_inner := 0 to High(expanded_states_inner) do
                begin
                    if merged_idx_inner >= beam_width_inner then
                    begin
                        Break;
                    end;
                    duplicate_inner := False;
                    for insert_idx_inner := 0 to merged_idx_inner - 1 do
                    begin
                        if SameText(merged_states_inner[insert_idx_inner].text,
                            expanded_states_inner[state_idx_inner].text) then
                        begin
                            duplicate_inner := True;
                            Break;
                        end;
                    end;
                    if duplicate_inner then
                    begin
                        Continue;
                    end;
                    merged_states_inner[merged_idx_inner] :=
                        expanded_states_inner[state_idx_inner];
                    merged_states_inner[merged_idx_inner].baseline_lineage := False;
                    Inc(merged_idx_inner);
                end;
                SetLength(merged_states_inner, merged_idx_inner);

                for state_idx_inner := 1 to High(merged_states_inner) do
                begin
                    insert_idx_inner := state_idx_inner;
                    while (insert_idx_inner > 0) and
                        is_exact_chunk_chain_state_better_local(
                        merged_states_inner[insert_idx_inner],
                        merged_states_inner[insert_idx_inner - 1]) do
                    begin
                        swap_state_inner := merged_states_inner[insert_idx_inner - 1];
                        merged_states_inner[insert_idx_inner - 1] :=
                            merged_states_inner[insert_idx_inner];
                        merged_states_inner[insert_idx_inner] := swap_state_inner;
                        Dec(insert_idx_inner);
                    end;
                end;
                expanded_states_inner := merged_states_inner;
            end;

            procedure prune_fast_states_by_char_lm_local(
                var states_inner: TExactChunkChainStateArray;
                const state_start_pos_inner: Integer;
                const baseline_states_inner: TExactChunkChainStateArray;
                const enable_edge_probe_inner: Boolean);
            const
                c_char_lm_original_rank_penalty = 20;
            var
                state_texts_inner: TArray<string>;
                char_scores_inner: TArray<Integer>;
                scored_char_scores_inner: TArray<Integer>;
                selected_inner: TArray<Boolean>;
                kept_states_inner: TExactChunkChainStateArray;
                state_idx_inner: Integer;
                keep_idx_inner: Integer;
                base_keep_inner: Integer;
                best_idx_inner: Integer;
                best_score_inner: Integer;
                current_score_inner: Integer;
                baseline_keep_inner: Integer;
                legacy_keep_width_inner: Integer;
                best_model_score_inner: Int64;
                current_model_score_inner: Int64;
                insert_idx_inner: Integer;
                swap_state_inner: TExactChunkChainState;
                char_scores_ok_inner: Boolean;
                score_start_idx_inner: Integer;
                beam_width_inner: Integer;
                effective_keep_width_inner: Integer;
                baseline_prebeam_width_inner: Integer;
                baseline_candidate_count_inner: Integer;
                baseline_ranks_inner: TArray<Integer>;
                shared_features_valid_inner: TArray<Boolean>;
                shared_single_segments_inner: TArray<Integer>;
                shared_head_lm_bonus_inner: TArray<Integer>;
                shared_full_lm_bonus_inner: TArray<Integer>;
                shared_lm_stats_inner: TArray<TLongWordLmBoundaryStats>;

                function get_structure_signature_inner(
                    const state_inner: TExactChunkChainState): string;
                var
                    signature_parts_inner: TArray<string>;
                    signature_part_inner: string;
                begin
                    Result := '';
                    signature_parts_inner := state_inner.path.Split(
                        [c_segment_path_separator]);
                    for signature_part_inner in signature_parts_inner do
                    begin
                        if Trim(signature_part_inner) = '' then
                        begin
                            Continue;
                        end;
                        if Result <> '' then
                        begin
                            Result := Result + '-';
                        end;
                        Result := Result + IntToStr(
                            get_candidate_text_unit_count(
                            Trim(signature_part_inner)));
                    end;
                    if Result = '' then
                    begin
                        Result := IntToStr(get_candidate_text_unit_count(
                            Trim(state_inner.text)));
                    end;
                end;

                function has_selected_structure_inner(
                    const signature_inner: string): Boolean;
                var
                    selected_idx_inner: Integer;
                begin
                    Result := False;
                    for selected_idx_inner := 0 to High(states_inner) do
                    begin
                        if selected_inner[selected_idx_inner] and
                            SameText(get_structure_signature_inner(
                            states_inner[selected_idx_inner]),
                            signature_inner) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                procedure ensure_shared_state_features_inner(
                    const state_idx_score_inner: Integer);
                var
                    score_parts_inner: TArray<string>;
                    score_part_inner: string;
                    single_segments_score_inner: Integer;
                begin
                    if Length(shared_features_valid_inner) <>
                        Length(states_inner) then
                    begin
                        SetLength(shared_features_valid_inner,
                            Length(states_inner));
                        SetLength(shared_single_segments_inner,
                            Length(states_inner));
                        SetLength(shared_head_lm_bonus_inner,
                            Length(states_inner));
                        SetLength(shared_full_lm_bonus_inner,
                            Length(states_inner));
                        SetLength(shared_lm_stats_inner,
                            Length(states_inner));
                    end;
                    if shared_features_valid_inner[state_idx_score_inner] then
                    begin
                        Exit;
                    end;
                    single_segments_score_inner := 0;
                    score_parts_inner := states_inner[
                        state_idx_score_inner].path.Split(
                        [c_segment_path_separator]);
                    for score_part_inner in score_parts_inner do
                    begin
                        if get_candidate_text_unit_count(
                            Trim(score_part_inner)) = 1 then
                        begin
                            Inc(single_segments_score_inner);
                        end;
                    end;
                    shared_single_segments_inner[state_idx_score_inner] :=
                        single_segments_score_inner;
                    shared_lm_stats_inner[state_idx_score_inner] :=
                        Default(TLongWordLmBoundaryStats);
                    shared_full_lm_bonus_inner[state_idx_score_inner] :=
                        get_exact_chunk_chain_state_lm_bonus_local(
                        states_inner[state_idx_score_inner],
                        state_start_pos_inner,
                        shared_lm_stats_inner[state_idx_score_inner]);
                    if shared_lm_stats_inner[
                        state_idx_score_inner].min_bonus = MaxInt then
                    begin
                        shared_lm_stats_inner[
                            state_idx_score_inner].min_bonus := 0;
                    end;
                    shared_head_lm_bonus_inner[state_idx_score_inner] :=
                        get_exact_chunk_chain_state_head_lm_bonus_local(
                        states_inner[state_idx_score_inner],
                        state_start_pos_inner);
                    shared_features_valid_inner[state_idx_score_inner] := True;
                end;

                function get_search_model_score_inner(
                    const state_idx_score_inner: Integer;
                    const original_rank_inner: Integer): Int64;
                var
                    rank_inner: Integer;
                begin
                    ensure_shared_state_features_inner(state_idx_score_inner);
                    rank_inner := original_rank_inner;
                    if rank_inner <= 0 then
                    begin
                        rank_inner := state_idx_score_inner + 1;
                    end;
                    Result := long_search_ranker_score(
                        char_scores_inner[state_idx_score_inner],
                        rank_inner,
                        states_inner[state_idx_score_inner].score,
                        shared_head_lm_bonus_inner[state_idx_score_inner],
                        shared_full_lm_bonus_inner[state_idx_score_inner],
                        shared_lm_stats_inner[state_idx_score_inner].boundary_count,
                        shared_lm_stats_inner[state_idx_score_inner].min_bonus,
                        shared_lm_stats_inner[state_idx_score_inner].max_bonus,
                        shared_lm_stats_inner[state_idx_score_inner].first_bonus,
                        shared_lm_stats_inner[state_idx_score_inner].last_bonus,
                        shared_lm_stats_inner[state_idx_score_inner].supported_count,
                        shared_lm_stats_inner[state_idx_score_inner].strong_count,
                        shared_lm_stats_inner[state_idx_score_inner].trigram_count,
                        shared_lm_stats_inner[state_idx_score_inner].zero_count,
                        states_inner[state_idx_score_inner].segments,
                        shared_single_segments_inner[state_idx_score_inner],
                        get_candidate_text_unit_count(states_inner[
                        state_idx_score_inner].first_chunk_text),
                        states_inner[state_idx_score_inner].anchor_units,
                        states_inner[state_idx_score_inner].has_anchor);
                end;

                function select_best_char_lm_state_inner(
                    const baseline_only_inner: Boolean): Boolean;
                var
                    candidate_idx_inner: Integer;
                begin
                    Result := False;
                    best_idx_inner := -1;
                    best_score_inner := Low(Integer);
                    for candidate_idx_inner := 0 to High(states_inner) do
                    begin
                        if selected_inner[candidate_idx_inner] or
                            (baseline_only_inner and
                            (not states_inner[
                            candidate_idx_inner].baseline_lineage)) then
                        begin
                            Continue;
                        end;
                        current_score_inner := char_scores_inner[
                            candidate_idx_inner];
                        if baseline_only_inner then
                        begin
                            current_score_inner := current_score_inner -
                                (baseline_ranks_inner[candidate_idx_inner] - 1) *
                                c_char_lm_original_rank_penalty;
                        end
                        else
                        begin
                            current_score_inner := current_score_inner -
                                candidate_idx_inner *
                                c_char_lm_original_rank_penalty;
                        end;
                        if (best_idx_inner < 0) or
                            (current_score_inner > best_score_inner) then
                        begin
                            best_idx_inner := candidate_idx_inner;
                            best_score_inner := current_score_inner;
                        end;
                    end;
                    if best_idx_inner < 0 then
                    begin
                        Exit;
                    end;
                    kept_states_inner[keep_idx_inner] :=
                        states_inner[best_idx_inner];
                    kept_states_inner[keep_idx_inner].baseline_lineage :=
                        baseline_only_inner;
                    selected_inner[best_idx_inner] := True;
                    Inc(keep_idx_inner);
                    Result := True;
                end;

                procedure note_target_stage_inner(const after_beam: Boolean);
                var
                    note_idx_inner: Integer;
                    note_target_suffix_inner: string;
                    note_target_units_inner: Integer;
                begin
                    if m_debug_target_recall_text = '' then
                    begin
                        Exit;
                    end;
                    note_target_suffix_inner := Copy(m_debug_target_recall_text,
                        state_start_pos_inner + 1, MaxInt);
                    note_target_units_inner := get_candidate_text_unit_count(
                        note_target_suffix_inner);
                    for note_idx_inner := 0 to High(states_inner) do
                    begin
                        if SameText(Trim(states_inner[note_idx_inner].text),
                            note_target_suffix_inner) then
                        begin
                            if after_beam then
                            begin
                                m_debug_target_chain_beam_syllable := Max(
                                    m_debug_target_chain_beam_syllable,
                                    note_target_units_inner);
                            end
                            else
                            begin
                                m_debug_target_chain_prebeam_syllable := Max(
                                    m_debug_target_chain_prebeam_syllable,
                                    note_target_units_inner);
                            end;
                            Exit;
                        end;
                    end;
                end;

                procedure capture_beam_states_inner;
                var
                    capture_base_inner: Integer;
                    capture_idx_inner: Integer;
                    capture_baseline_idx_inner: Integer;
                    capture_item_inner: TncLongBeamStateDebug;
                    capture_target_suffix_inner: string;
                    capture_parts_inner: TArray<string>;
                    capture_part_inner: string;
                    capture_lm_stats_inner: TLongWordLmBoundaryStats;
                begin
                    if (not m_debug_capture_long_beam_states) or
                        (m_debug_target_recall_text = '') then
                    begin
                        Exit;
                    end;

                    capture_target_suffix_inner := Copy(
                        m_debug_target_recall_text,
                        state_start_pos_inner + 1, MaxInt);
                    capture_base_inner := Length(m_debug_long_beam_states);
                    SetLength(m_debug_long_beam_states, capture_base_inner +
                        Length(states_inner));
                    for capture_idx_inner := 0 to High(states_inner) do
                    begin
                        capture_item_inner.position := state_start_pos_inner;
                        capture_item_inner.input_syllable_count :=
                            syllable_count_local;
                        capture_item_inner.first_chunk_query_key :=
                            states_inner[capture_idx_inner].first_chunk_query_key;
                        capture_item_inner.original_rank := capture_idx_inner + 1;
                        capture_item_inner.base_score :=
                            states_inner[capture_idx_inner].score;
                        capture_item_inner.char_lm_score :=
                            char_scores_inner[capture_idx_inner];
                        capture_item_inner.word_lm_head_bonus :=
                            get_exact_chunk_chain_state_head_lm_bonus_local(
                            states_inner[capture_idx_inner],
                            state_start_pos_inner);
                        capture_item_inner.word_lm_full_bonus :=
                            get_exact_chunk_chain_state_lm_bonus_local(
                            states_inner[capture_idx_inner],
                            state_start_pos_inner, capture_lm_stats_inner);
                        capture_item_inner.word_lm_boundary_count :=
                            capture_lm_stats_inner.boundary_count;
                        capture_item_inner.word_lm_min_bonus :=
                            capture_lm_stats_inner.min_bonus;
                        if capture_item_inner.word_lm_min_bonus = MaxInt then
                        begin
                            capture_item_inner.word_lm_min_bonus := 0;
                        end;
                        capture_item_inner.word_lm_max_bonus :=
                            capture_lm_stats_inner.max_bonus;
                        capture_item_inner.word_lm_first_bonus :=
                            capture_lm_stats_inner.first_bonus;
                        capture_item_inner.word_lm_last_bonus :=
                            capture_lm_stats_inner.last_bonus;
                        capture_item_inner.word_lm_supported_count :=
                            capture_lm_stats_inner.supported_count;
                        capture_item_inner.word_lm_strong_count :=
                            capture_lm_stats_inner.strong_count;
                        capture_item_inner.word_lm_trigram_count :=
                            capture_lm_stats_inner.trigram_count;
                        capture_item_inner.word_lm_zero_count :=
                            capture_lm_stats_inner.zero_count;
                        capture_item_inner.segments :=
                            states_inner[capture_idx_inner].segments;
                        capture_item_inner.single_segments := 0;
                        capture_parts_inner :=
                            states_inner[capture_idx_inner].path.Split(
                            [c_segment_path_separator]);
                        for capture_part_inner in capture_parts_inner do
                        begin
                            if get_candidate_text_unit_count(
                                Trim(capture_part_inner)) = 1 then
                            begin
                                Inc(capture_item_inner.single_segments);
                            end;
                        end;
                        capture_item_inner.first_chunk_units :=
                            get_candidate_text_unit_count(
                            states_inner[capture_idx_inner].first_chunk_text);
                        capture_item_inner.first_chunk_exact_rank :=
                            states_inner[capture_idx_inner].first_chunk_exact_rank;
                        capture_item_inner.first_chunk_raw_weight :=
                            states_inner[capture_idx_inner].first_chunk_raw_weight;
                        capture_item_inner.first_chunk_top_weight :=
                            states_inner[capture_idx_inner].first_chunk_top_weight;
                        capture_item_inner.first_chunk_weight_gap := Max(0,
                            capture_item_inner.first_chunk_top_weight -
                            capture_item_inner.first_chunk_raw_weight);
                        capture_item_inner.first_chunk_effective_score :=
                            states_inner[capture_idx_inner].first_chunk_effective_score;
                        capture_item_inner.first_chunk_edge_score :=
                            states_inner[capture_idx_inner].first_chunk_edge_score;
                        capture_item_inner.first_chunk_source_user :=
                            states_inner[capture_idx_inner].first_chunk_source_user;
                        capture_item_inner.first_chunk_baseline_option :=
                            states_inner[capture_idx_inner].first_chunk_baseline_option;
                        capture_item_inner.anchor_units :=
                            states_inner[capture_idx_inner].anchor_units;
                        capture_item_inner.has_anchor :=
                            states_inner[capture_idx_inner].has_anchor;
                        capture_item_inner.baseline_lineage :=
                            states_inner[capture_idx_inner].baseline_lineage;
                        capture_item_inner.baseline_selected := False;
                        for capture_baseline_idx_inner := 0 to
                            High(baseline_states_inner) do
                        begin
                            if SameText(baseline_states_inner[
                                capture_baseline_idx_inner].text,
                                states_inner[capture_idx_inner].text) and
                                SameText(baseline_states_inner[
                                capture_baseline_idx_inner].path,
                                states_inner[capture_idx_inner].path) then
                            begin
                                capture_item_inner.baseline_selected := True;
                                Break;
                            end;
                        end;
                        capture_item_inner.selected :=
                            selected_inner[capture_idx_inner];
                        capture_item_inner.target := SameText(
                            Trim(states_inner[capture_idx_inner].text),
                            capture_target_suffix_inner);
                        capture_item_inner.text :=
                            Trim(states_inner[capture_idx_inner].text);
                        capture_item_inner.segment_path :=
                            states_inner[capture_idx_inner].path;
                        debug_note_long_exact_edge_stage(
                            state_start_pos_inner, syllable_count_local,
                            states_inner[capture_idx_inner].first_chunk_query_key,
                            states_inner[capture_idx_inner].first_chunk_text,
                            capture_item_inner.first_chunk_units,
                            capture_item_inner.first_chunk_exact_rank,
                            capture_item_inner.first_chunk_raw_weight,
                            capture_item_inner.first_chunk_top_weight,
                            capture_item_inner.first_chunk_effective_score,
                            capture_item_inner.first_chunk_edge_score,
                            capture_item_inner.first_chunk_source_user,
                            capture_item_inner.first_chunk_baseline_option,
                            2 or (Ord(capture_item_inner.selected) * 4));
                        m_debug_long_beam_states[capture_base_inner +
                            capture_idx_inner] := capture_item_inner;
                    end;
                end;
            begin
                beam_width_inner := get_chain_beam_width_local;
                note_target_stage_inner(False);

                { A state can descend from an old-beam suffix without having
                  survived the old prebeam at this position. Restrict lineage
                  to the exact old prebeam prefix before reproducing its
                  six-state character-LM selection. }
                baseline_prebeam_width_inner :=
                    get_chain_baseline_prebeam_width_local;
                baseline_candidate_count_inner := 0;
                SetLength(baseline_ranks_inner, Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    if not states_inner[state_idx_inner].baseline_lineage then
                    begin
                        Continue;
                    end;
                    Inc(baseline_candidate_count_inner);
                    if baseline_candidate_count_inner >
                        baseline_prebeam_width_inner then
                    begin
                        states_inner[state_idx_inner].baseline_lineage := False;
                    end
                    else
                    begin
                        baseline_ranks_inner[state_idx_inner] :=
                            baseline_candidate_count_inner;
                    end;
                end;
                baseline_candidate_count_inner := Min(
                    baseline_candidate_count_inner,
                    baseline_prebeam_width_inner);
                effective_keep_width_inner := Min(beam_width_inner,
                    Length(states_inner));
                if (Length(states_inner) <= beam_width_inner) and
                    (baseline_candidate_count_inner <= Min(
                    c_chain_lattice_baseline_beam_width,
                    effective_keep_width_inner)) and
                    (not enable_edge_probe_inner) then
                begin
                    note_target_stage_inner(True);
                    Exit;
                end;

                base_keep_inner := get_chain_char_base_slots_local;
                score_start_idx_inner := base_keep_inner;
                if enable_edge_probe_inner then
                begin
                    score_start_idx_inner := 0;
                end;
                SetLength(state_texts_inner, Length(states_inner) -
                    score_start_idx_inner);
                for state_idx_inner := score_start_idx_inner to
                    High(states_inner) do
                begin
                    state_texts_inner[state_idx_inner - score_start_idx_inner] :=
                        Trim(states_inner[state_idx_inner].text);
                end;
                char_scores_ok_inner := False;
                if m_dictionary <> nil then
                begin
                    if state_start_pos_inner > 0 then
                    begin
                        char_scores_ok_inner :=
                            get_cached_char_lm_scores(state_texts_inner,
                            scored_char_scores_inner, clsm_suffix, '');
                    end
                    else
                    begin
                        char_scores_ok_inner :=
                            get_cached_char_lm_scores(state_texts_inner,
                            scored_char_scores_inner, clsm_full, '');
                    end;
                end;
                if (not char_scores_ok_inner) or
                    (Length(scored_char_scores_inner) <>
                    Length(state_texts_inner)) then
                begin
                    SetLength(selected_inner, Length(states_inner));
                    SetLength(kept_states_inner, effective_keep_width_inner);
                    keep_idx_inner := 0;
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if states_inner[state_idx_inner].baseline_lineage and
                            (keep_idx_inner < Min(
                            c_chain_lattice_baseline_beam_width,
                            effective_keep_width_inner)) then
                        begin
                            kept_states_inner[keep_idx_inner] :=
                                states_inner[state_idx_inner];
                            selected_inner[state_idx_inner] := True;
                            Inc(keep_idx_inner);
                        end;
                    end;
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if keep_idx_inner >= effective_keep_width_inner then
                        begin
                            Break;
                        end;
                        if not selected_inner[state_idx_inner] then
                        begin
                            kept_states_inner[keep_idx_inner] :=
                                states_inner[state_idx_inner];
                            kept_states_inner[keep_idx_inner].baseline_lineage :=
                                False;
                            Inc(keep_idx_inner);
                        end;
                    end;
                    SetLength(kept_states_inner, keep_idx_inner);
                    states_inner := kept_states_inner;
                    note_target_stage_inner(True);
                    Exit;
                end;
                SetLength(char_scores_inner, Length(states_inner));
                for state_idx_inner := score_start_idx_inner to
                    High(states_inner) do
                begin
                    char_scores_inner[state_idx_inner] :=
                        scored_char_scores_inner[state_idx_inner -
                        score_start_idx_inner];
                end;

                SetLength(selected_inner, Length(states_inner));
                SetLength(kept_states_inner, effective_keep_width_inner);
                keep_idx_inner := 0;
                for state_idx_inner := 0 to base_keep_inner - 1 do
                begin
                    kept_states_inner[keep_idx_inner] := states_inner[state_idx_inner];
                    selected_inner[state_idx_inner] := True;
                    Inc(keep_idx_inner);
                end;

                // Keep the character-LM, search-model and structure slots
                // intact. Exact-edge recall runs in its bounded side lattice.
                baseline_keep_inner := Min(
                    c_chain_lattice_baseline_beam_width,
                    effective_keep_width_inner);
                while keep_idx_inner < baseline_keep_inner do
                begin
                    if not select_best_char_lm_state_inner(True) then
                    begin
                        Break;
                    end;
                end;

                if keep_idx_inner < effective_keep_width_inner then
                begin
                    best_idx_inner := -1;
                    best_model_score_inner := Low(Int64);
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if selected_inner[state_idx_inner] or
                            (enable_edge_probe_inner and
                            (not states_inner[
                            state_idx_inner].baseline_lineage)) then
                        begin
                            Continue;
                        end;
                        current_model_score_inner :=
                            get_search_model_score_inner(state_idx_inner,
                            IfThen(enable_edge_probe_inner,
                            baseline_ranks_inner[state_idx_inner],
                            state_idx_inner + 1));
                        if (best_idx_inner < 0) or
                            (current_model_score_inner >
                            best_model_score_inner) then
                        begin
                            best_idx_inner := state_idx_inner;
                            best_model_score_inner :=
                                current_model_score_inner;
                        end;
                    end;
                    if best_idx_inner >= 0 then
                    begin
                        kept_states_inner[keep_idx_inner] :=
                            states_inner[best_idx_inner];
                        kept_states_inner[keep_idx_inner].baseline_lineage :=
                            False;
                        selected_inner[best_idx_inner] := True;
                        Inc(keep_idx_inner);
                    end;
                end;

                if keep_idx_inner < effective_keep_width_inner then
                begin
                    best_idx_inner := -1;
                    best_score_inner := Low(Integer);
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if selected_inner[state_idx_inner] or
                            (enable_edge_probe_inner and
                            (not states_inner[
                            state_idx_inner].baseline_lineage)) or
                            has_selected_structure_inner(
                            get_structure_signature_inner(
                            states_inner[state_idx_inner])) then
                        begin
                            Continue;
                        end;
                        if enable_edge_probe_inner then
                        begin
                            current_score_inner := char_scores_inner[
                                state_idx_inner] -
                                (baseline_ranks_inner[state_idx_inner] - 1) *
                                c_char_lm_original_rank_penalty;
                        end
                        else
                        begin
                            current_score_inner := char_scores_inner[
                                state_idx_inner] - state_idx_inner *
                                c_char_lm_original_rank_penalty;
                        end;
                        if (best_idx_inner < 0) or
                            (current_score_inner > best_score_inner) then
                        begin
                            best_idx_inner := state_idx_inner;
                            best_score_inner := current_score_inner;
                        end;
                    end;
                    if best_idx_inner >= 0 then
                    begin
                        kept_states_inner[keep_idx_inner] :=
                            states_inner[best_idx_inner];
                        kept_states_inner[keep_idx_inner].baseline_lineage :=
                            False;
                        selected_inner[best_idx_inner] := True;
                        Inc(keep_idx_inner);
                    end;
                end;

                legacy_keep_width_inner := effective_keep_width_inner;
                while keep_idx_inner < legacy_keep_width_inner do
                begin
                    if not select_best_char_lm_state_inner(
                        enable_edge_probe_inner) then
                    begin
                        Break;
                    end;
                end;

                capture_beam_states_inner;
                SetLength(kept_states_inner, keep_idx_inner);

                for state_idx_inner := 1 to High(kept_states_inner) do
                begin
                    insert_idx_inner := state_idx_inner;
                    while (insert_idx_inner > 0) and
                        is_exact_chunk_chain_state_better_local(
                        kept_states_inner[insert_idx_inner],
                        kept_states_inner[insert_idx_inner - 1]) do
                    begin
                        swap_state_inner := kept_states_inner[insert_idx_inner - 1];
                        kept_states_inner[insert_idx_inner - 1] :=
                            kept_states_inner[insert_idx_inner];
                        kept_states_inner[insert_idx_inner] := swap_state_inner;
                        Dec(insert_idx_inner);
                    end;
                end;
                states_inner := kept_states_inner;
                note_target_stage_inner(True);
            end;

            function try_collect_fast_options_local(const start_idx_local: Integer;
                const chunk_len_inner: Integer;
                out out_options_local: TExactChunkChainOptionArray): Boolean;
            var
                query_key_inner: string;
                lookup_results_inner: TncCandidateList;
                ranked_lookup_results_inner: TncCandidateList;
                lookup_idx_inner: Integer;
                text_inner: string;
                score_inner: Integer;
                ranked_score_inner: Integer;
                raw_weight_inner: Integer;
                best_direct_raw_weight_inner: Integer;
                option_inner: TExactChunkChainOption;
                prefix_text_inner: string;
                tail_key_inner: string;
                tail_lookup_results_inner: TncCandidateList;
                tail_idx_inner: Integer;
                tail_text_inner: string;
                tail_units_inner: Integer;
                tail_raw_weight_inner: Integer;
                head_key_inner: string;
                head_lookup_results_inner: TncCandidateList;
                head_idx_inner: Integer;
                head_text_inner: string;
                head_has_strong_exact_inner: Boolean;
                prefix_score_inner: Integer;
                prefix_is_modifier_inner: Boolean;
                split_len_inner: Integer;
                left_key_inner: string;
                right_key_inner: string;
                left_lookup_results_inner: TncCandidateList;
                right_lookup_results_inner: TncCandidateList;
                left_idx_inner: Integer;
                right_idx_inner: Integer;
                left_text_inner: string;
                right_text_inner: string;
                split_path_inner: string;
                split_score_inner: Integer;
                split_path_bonus_inner: Integer;
                split_path_penalty_inner: Integer;
                split_lm_bonus_inner: Integer;
                normalized_single_key_inner: string;
                legacy_options_inner: TExactChunkChainOptionArray;

                function finish_options_inner: Boolean;
                begin
                    score_fast_options_local(out_options_local);
                    Result := Length(out_options_local) > 0;
                end;

                function is_place_noun_unit_for_fast_context_inner(
                    const unit_value_inner: string): Boolean;
                begin
                    Result := (unit_value_inner = string(Char($5BB6))) or
                        (unit_value_inner = string(Char($5E97))) or
                        (unit_value_inner = string(Char($5C4B))) or
                        (unit_value_inner = string(Char($697C))) or
                        (unit_value_inner = string(Char($57CE))) or
                        (unit_value_inner = string(Char($6751))) or
                        (unit_value_inner = string(Char($53BF))) or
                        (unit_value_inner = string(Char($5E02))) or
                        (unit_value_inner = string(Char($6821))) or
                        (unit_value_inner = string(Char($9662))) or
                        (unit_value_inner = string(Char($5382))) or
                        (unit_value_inner = string(Char($9986))) or
                        (unit_value_inner = string(Char($7AD9))) or
                        (unit_value_inner = string(Char($5C40))) or
                        (unit_value_inner = string(Char($6240))) or
                        (unit_value_inner = string(Char($56ED))) or
                        (unit_value_inner = string(Char($533A))) or
                        (unit_value_inner = string(Char($5708))) or
                        (unit_value_inner = string(Char($8DEF))) or
                        (unit_value_inner = string(Char($8857))) or
                        (unit_value_inner = string(Char($95E8))) or
                        (unit_value_inner = string(Char($5BA4))) or
                        (unit_value_inner = string(Char($623F))) or
                        (unit_value_inner = string(Char($5385))) or
                        (unit_value_inner = string(Char($573A))) or
                        (unit_value_inner = string(Char($53F8)));
                end;

                function has_place_context_before_fast_chunk_inner: Boolean;
                const
                    c_context_probe_limit = 8;
                var
                    back_len_inner: Integer;
                    context_key_inner: string;
                    context_results_inner: TncCandidateList;
                    context_idx_inner: Integer;
                    context_text_inner: string;
                    context_units_inner: TArray<string>;
                begin
                    Result := False;
                    if start_idx_local <= 0 then
                    begin
                        Exit;
                    end;

                    for back_len_inner := 1 to Min(4, start_idx_local) do
                    begin
                        context_key_inner := build_query_key_local(start_idx_local -
                            back_len_inner, back_len_inner);
                        if (context_key_inner = '') or
                            (not lookup_exact_full_pinyin_cached_local(
                            context_key_inner, context_results_inner)) then
                        begin
                            Continue;
                        end;

                        for context_idx_inner := 0 to High(context_results_inner) do
                        begin
                            if context_idx_inner >= c_context_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(context_results_inner[context_idx_inner].comment) <>
                                '' then
                            begin
                                Continue;
                            end;

                            context_text_inner := Trim(
                                context_results_inner[context_idx_inner].text);
                            context_units_inner := split_text_units(
                                context_text_inner);
                            if (Length(context_units_inner) > 0) and
                                is_place_noun_unit_for_fast_context_inner(
                                context_units_inner[High(context_units_inner)]) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                function has_directional_locative_fast_context_inner: Boolean;
                var
                    next_key_inner: string;
                begin
                    Result := False;
                    if (chunk_len_inner <> 2) or
                        (not has_place_context_before_fast_chunk_inner) then
                    begin
                        Exit;
                    end;

                    if start_idx_local + chunk_len_inner >= syllable_count_local then
                    begin
                        Exit(True);
                    end;

                    next_key_inner := normalize_pinyin_text(
                        effective_syllables[start_idx_local +
                        chunk_len_inner].text);
                    Result := SameText(next_key_inner, 'de');
                end;

                function get_directional_locative_fast_bonus_inner(
                    const text_value_inner: string): Integer;
                const
                    c_fast_directional_context_bonus = 12000;
                begin
                    Result := 0;
                    if (get_directional_locative_span_bonus_local(
                        text_value_inner) > 0) and
                        has_directional_locative_fast_context_inner then
                    begin
                        Result := c_fast_directional_context_bonus;
                    end;
                end;

                function try_get_stable_bridge_single_text_inner(
                    const syllable_text_inner: string;
                    out out_text_inner: string): Boolean;
                begin
                    Result := try_get_modifier_prefix_single_text_local(
                        syllable_text_inner, out_text_inner);
                    if Result and (out_text_inner <> '') then
                    begin
                        Exit;
                    end;

                    out_text_inner :=
                        get_fixed_boundary_single_char_for_standalone_local(
                        syllable_text_inner);
                    Result := out_text_inner <> '';
                end;

                function get_function_single_pair_exact_penalty_inner(
                    const candidate_text_inner: string): Integer;
                var
                    first_single_inner: string;
                    second_single_inner: string;
                    stable_pair_inner: string;

                    function is_number_single_text_inner(
                        const text_value_inner: string): Boolean;
                    begin
                        Result := False;
                        if get_candidate_text_unit_count(text_value_inner) <> 1 then
                        begin
                            Exit;
                        end;

                        case Ord(text_value_inner[1]) of
                            $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                            $4E03, $516B, $4E5D, $5341:
                                Result := True;
                        end;
                    end;

                    function is_quantity_tail_single_text_inner(
                        const text_value_inner: string): Boolean;
                    begin
                        Result := False;
                        if get_candidate_text_unit_count(text_value_inner) <> 1 then
                        begin
                            Exit;
                        end;

                        case Ord(text_value_inner[1]) of
                            $5B57, $9879, $6761, $4E2A, $70B9, $79CD, $7C7B,
                            $6B21, $6B65, $8F6E, $7AE0, $8282, $9875, $6BB5,
                            $884C, $5217, $5C42:
                                Result := True;
                        end;
                    end;

                begin
                    Result := 0;
                    if (chunk_len_inner <> 2) or
                        (candidate_text_inner = '') or
                        (start_idx_local + 1 >= syllable_count_local) then
                    begin
                        Exit;
                    end;

                    if (not try_get_stable_bridge_single_text_inner(
                        effective_syllables[start_idx_local].text,
                        first_single_inner)) or
                        (not try_get_stable_bridge_single_text_inner(
                        effective_syllables[start_idx_local + 1].text,
                        second_single_inner)) then
                    begin
                        Exit;
                    end;

                    if (not ((is_modifier_prefix_bridge_exempt_local(
                        first_single_inner) and
                        is_modifier_prefix_bridge_exempt_local(second_single_inner)) or
                        (is_number_single_text_inner(first_single_inner) and
                        is_quantity_tail_single_text_inner(second_single_inner)))) then
                    begin
                        Exit;
                    end;

                    stable_pair_inner := first_single_inner + second_single_inner;
                    if (stable_pair_inner <> '') and
                        (not SameText(candidate_text_inner, stable_pair_inner)) then
                    begin
                        Result := 42000;
                    end;
                end;

                function get_ranked_lookup_score_for_text_inner(
                    const text_value_inner: string): Integer;
                var
                    ranked_idx_inner: Integer;
                    ranked_text_inner: string;
                begin
                    Result := Low(Integer);
                    if (text_value_inner = '') or
                        (Length(ranked_lookup_results_inner) = 0) then
                    begin
                        Exit;
                    end;

                    for ranked_idx_inner := 0 to
                        Min(High(ranked_lookup_results_inner),
                        get_chain_probe_limit_local - 1) do
                    begin
                        if Trim(ranked_lookup_results_inner[ranked_idx_inner].comment) <> '' then
                        begin
                            Continue;
                        end;
                        ranked_text_inner := Trim(
                            ranked_lookup_results_inner[ranked_idx_inner].text);
                        if (ranked_text_inner = '') or
                            (get_candidate_text_unit_count(ranked_text_inner) <>
                            chunk_len_inner) or
                            (not SameText(ranked_text_inner, text_value_inner)) then
                        begin
                            Continue;
                        end;

                        Result := get_exact_chunk_effective_weight_local(
                            ranked_lookup_results_inner[ranked_idx_inner]) +
                            ((get_chain_probe_limit_local - ranked_idx_inner) * 384);
                        Exit;
                    end;
                end;

                function get_best_direct_raw_weight_inner: Integer;
                var
                    scan_idx_inner: Integer;
                    scan_text_inner: string;
                    scan_raw_inner: Integer;
                begin
                    if best_direct_raw_weight_inner <> Low(Integer) then
                    begin
                        Exit(best_direct_raw_weight_inner);
                    end;

                    best_direct_raw_weight_inner := 0;
                    for scan_idx_inner := 0 to High(lookup_results_inner) do
                    begin
                        if scan_idx_inner >= get_chain_probe_limit_local then
                        begin
                            Break;
                        end;
                        if Trim(lookup_results_inner[scan_idx_inner].comment) <> '' then
                        begin
                            Continue;
                        end;

                        scan_text_inner := Trim(
                            lookup_results_inner[scan_idx_inner].text);
                        if (scan_text_inner = '') or
                            (get_candidate_text_unit_count(scan_text_inner) <>
                            chunk_len_inner) or
                            (not is_valid_standalone_cjk_phrase_text_local(
                            scan_text_inner, chunk_len_inner)) then
                        begin
                            Continue;
                        end;

                        scan_raw_inner := get_exact_chunk_raw_weight_local(
                            lookup_results_inner[scan_idx_inner]);
                        if scan_raw_inner > best_direct_raw_weight_inner then
                        begin
                            best_direct_raw_weight_inner := scan_raw_inner;
                        end;
                    end;
                    Result := best_direct_raw_weight_inner;
                end;

                function get_final_compound_tail_support_bonus_inner(
                    const text_value_inner: string;
                    const raw_weight_value_inner: Integer): Integer;
                const
                    c_strong_direct_tail_weight = 900;
                    c_tail_support_candidate_window = 1400;
                    c_tail_support_bonus_cap = 1800;
                var
                    best_raw_inner: Integer;
                    support_inner: Integer;
                begin
                    Result := 0;
                    if (chunk_len_inner <> 2) or (start_idx_local <= 0) or
                        (start_idx_local + chunk_len_inner <>
                        syllable_count_local) or (m_dictionary = nil) or
                        (text_value_inner = '') or (raw_weight_value_inner <= 0) then
                    begin
                        Exit;
                    end;

                    best_raw_inner := get_best_direct_raw_weight_inner;
                    if (best_raw_inner <= 0) or
                        (raw_weight_value_inner >= best_raw_inner) or
                        ((raw_weight_value_inner +
                        c_tail_support_candidate_window) < best_raw_inner) then
                    begin
                        Exit;
                    end;

                    support_inner :=
                        m_dictionary.get_compound_tail_support(text_value_inner);
                    if (support_inner <= 0) or
                        ((best_raw_inner >= c_strong_direct_tail_weight) and
                        (support_inner < 2000)) then
                    begin
                        Exit;
                    end;

                    Result := Min(c_tail_support_bonus_cap, support_inner);
                end;

                function is_function_or_modifier_single_quality_inner(
                    const text_value_inner: string): Boolean;
                begin
                    Result := False;
                    if get_candidate_text_unit_count(text_value_inner) <> 1 then
                    begin
                        Exit;
                    end;

                    case Ord(text_value_inner[1]) of
                        $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C,
                        $4E0D, $518D, $9AD8, $5F88, $592A, $6700, $66F4,
                        $6CA1, $8981, $4F1A, $80FD, $628A, $88AB, $8BA9,
                        $7ED9, $5BF9, $4ECE, $5411, $4E0E, $90FD, $8FD8,
                        $624D, $5C31, $53EA, $522B, $5DF2, $5C06, $53EF,
                        $60F3, $65E0, $975E, $672A, $4ECD, $591A, $5C11,
                        $65B0, $8001, $5C0F, $5927, $5168, $8D85, $771F,
                        $597D, $5982, $633A, $8F83, $6781, $800C, $5E76:
                            Result := True;
                    end;
                end;

                function should_defer_pair_to_modifier_tail_exact_inner(
                    const pair_text_inner: string;
                    const pair_raw_weight_inner: Integer): Boolean;
                var
                    prefix_text_inner_local: string;
                    tail_key_inner_local: string;
                    tail_text_inner_local: string;
                    pair_units_inner: TArray<string>;
                begin
                    Result := False;
                    if (chunk_len_inner <> 2) or
                        (start_idx_local + 2 >= syllable_count_local) or
                        (pair_text_inner = '') then
                    begin
                        Exit;
                    end;

                    if not try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx_local].text,
                        prefix_text_inner_local) then
                    begin
                        prefix_text_inner_local :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[start_idx_local].text);
                        if (prefix_text_inner_local = '') or
                            (not is_function_or_modifier_single_quality_inner(
                            prefix_text_inner_local)) then
                        begin
                            Exit;
                        end;
                    end;

                    pair_units_inner := split_text_units(Trim(pair_text_inner));
                    if Length(pair_units_inner) <> 2 then
                    begin
                        Exit;
                    end;

                    tail_key_inner_local := build_query_key_local(start_idx_local + 1, 2);
                    if not try_get_strong_fast_exact_phrase_text_local(
                        tail_key_inner_local, 2, 700, tail_text_inner_local) then
                    begin
                        Exit;
                    end;

                    if SameText(pair_units_inner[0], prefix_text_inner_local) then
                    begin
                        Result := not SameText(pair_units_inner[1],
                            get_first_text_unit_fast_local(tail_text_inner_local));
                    end
                    else
                    begin
                        Result := pair_raw_weight_inner < 900;
                    end;
                end;

                function is_deferable_function_tail_single_inner(
                    const text_value_inner: string): Boolean;
                begin
                    Result := False;
                    if get_candidate_text_unit_count(text_value_inner) <> 1 then
                    begin
                        Exit;
                    end;

                    case Ord(text_value_inner[1]) of
                        $7684, $5730, $5F97, $4E86, $7740, $8FC7:
                            Result := True;
                    end;
                end;

                function should_defer_chunk_to_head_plus_function_tail_inner(
                    const chunk_text_inner: string;
                    const chunk_raw_weight_inner: Integer): Boolean;
                const
                    c_head_exact_min_raw = 700;
                    c_tail_chunk_strength_margin = 180;
                var
                    chunk_units_inner: TArray<string>;
                    tail_text_inner_local: string;
                    head_key_inner_local: string;
                    head_text_inner_local: string;
                    head_raw_weight_inner_local: Integer;
                begin
                    Result := False;
                    if (chunk_len_inner < 3) or (chunk_len_inner > 4) or
                        (chunk_text_inner = '') then
                    begin
                        Exit;
                    end;

                    chunk_units_inner := split_text_units(Trim(chunk_text_inner));
                    if Length(chunk_units_inner) <> chunk_len_inner then
                    begin
                        Exit;
                    end;

                    if not try_get_stable_bridge_single_text_inner(
                        effective_syllables[start_idx_local + chunk_len_inner - 1].text,
                        tail_text_inner_local) then
                    begin
                        Exit;
                    end;

                    if (not is_deferable_function_tail_single_inner(
                        tail_text_inner_local)) or
                        (not SameText(chunk_units_inner[High(chunk_units_inner)],
                        tail_text_inner_local)) then
                    begin
                        Exit;
                    end;

                    head_key_inner_local := build_query_key_local(start_idx_local,
                        chunk_len_inner - 1);
                    if not try_get_strong_fast_exact_phrase_text_local(
                        head_key_inner_local, chunk_len_inner - 1,
                        c_head_exact_min_raw, head_text_inner_local) then
                    begin
                        Exit;
                    end;
                    if text_starts_with_fast_local(chunk_text_inner,
                        head_text_inner_local) then
                    begin
                        Exit;
                    end;

                    { Prefer a strong head exact plus a function tail over a
                      competing exact chunk that only wins by absorbing the
                      tail, e.g. head+de/di/de/le/zhe/guo boundaries. }
                    head_raw_weight_inner_local :=
                        get_fast_exact_phrase_raw_weight_local(
                        head_key_inner_local, chunk_len_inner - 1,
                        head_text_inner_local);
                    Result := (head_raw_weight_inner_local > 0) and
                        ((chunk_raw_weight_inner <= 0) or
                        (chunk_raw_weight_inner <=
                        head_raw_weight_inner_local +
                        c_tail_chunk_strength_margin));
                end;

                procedure add_fixed_single_option_inner(
                    const single_text_value_inner: string;
                    const score_penalty_value_inner: Integer);
                var
                    scan_idx_inner_local: Integer;
                    single_score_inner: Integer;
                    single_rank_inner: Integer;
                    single_raw_weight_inner: Integer;
                    single_top_weight_inner: Integer;
                    scan_raw_weight_inner: Integer;
                    single_source_user_inner: Boolean;
                    single_option_inner: TExactChunkChainOption;
                begin
                    if single_text_value_inner = '' then
                    begin
                        Exit;
                    end;

                    single_score_inner := 1200;
                    single_rank_inner := 0;
                    single_raw_weight_inner := 0;
                    single_top_weight_inner := 0;
                    single_source_user_inner := False;
                    if lookup_exact_full_pinyin_cached_local(query_key_inner,
                        lookup_results_inner) then
                    begin
                        for scan_idx_inner_local := 0 to
                            High(lookup_results_inner) do
                        begin
                            if scan_idx_inner_local >= get_chain_probe_limit_local then
                            begin
                                Break;
                            end;
                            scan_raw_weight_inner := 0;
                            if use_exact_edge_ranker_expansion_local then
                            begin
                                if Trim(lookup_results_inner[
                                    scan_idx_inner_local].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                if get_candidate_text_unit_count(Trim(
                                    lookup_results_inner[
                                    scan_idx_inner_local].text)) <> 1 then
                                begin
                                    Continue;
                                end;
                                scan_raw_weight_inner :=
                                    get_exact_chunk_raw_weight_local(
                                    lookup_results_inner[scan_idx_inner_local]);
                                single_top_weight_inner := Max(
                                    single_top_weight_inner,
                                    scan_raw_weight_inner);
                            end;
                            if SameText(Trim(lookup_results_inner[
                                scan_idx_inner_local].text),
                                single_text_value_inner) then
                            begin
                                single_score_inner :=
                                    get_exact_chunk_effective_weight_local(
                                    lookup_results_inner[scan_idx_inner_local]);
                                if use_exact_edge_ranker_expansion_local then
                                begin
                                    single_rank_inner :=
                                        scan_idx_inner_local + 1;
                                    single_raw_weight_inner :=
                                        scan_raw_weight_inner;
                                    single_source_user_inner :=
                                        lookup_results_inner[
                                        scan_idx_inner_local].source = cs_user;
                                end
                                else
                                begin
                                    Break;
                                end;
                            end;
                        end;
                    end;
                    Inc(single_score_inner, get_preferred_query_phrase_bonus_local(
                        query_key_inner, single_text_value_inner));
                    if (start_idx_local + chunk_len_inner = syllable_count_local) then
                    begin
                        Inc(single_score_inner, 36000);
                    end;
                    Inc(single_score_inner, c_fast_fixed_single_bonus);
                    Dec(single_score_inner, score_penalty_value_inner);

                    single_option_inner := Default(TExactChunkChainOption);
                    single_option_inner.text := single_text_value_inner;
                    single_option_inner.path := single_text_value_inner;
                    single_option_inner.query_key := query_key_inner;
                    single_option_inner.score := single_score_inner;
                    single_option_inner.exact_rank := single_rank_inner;
                    single_option_inner.raw_weight := single_raw_weight_inner;
                    single_option_inner.top_weight := single_top_weight_inner;
                    single_option_inner.source_user :=
                        single_source_user_inner;
                    single_option_inner.direct_exact := single_rank_inner > 0;
                    single_option_inner.segments := 1;
                    single_option_inner.has_anchor := True;
                    single_option_inner.anchor_units := 1;
                    add_fast_option_local(out_options_local,
                        legacy_options_inner, single_option_inner);
                end;

                procedure add_single_exact_edge_candidates_inner(
                    const protected_text_inner: string);
                var
                    edge_idx_inner: Integer;
                    edge_text_inner: string;
                    edge_score_inner: Integer;
                    edge_raw_inner: Integer;
                    edge_top_inner: Integer;
                    edge_option_inner: TExactChunkChainOption;
                begin
                    if not lookup_exact_full_pinyin_cached_local(query_key_inner,
                        lookup_results_inner) then
                    begin
                        Exit;
                    end;
                    edge_top_inner := 0;
                    for edge_idx_inner := 0 to Min(
                        get_chain_probe_limit_local - 1,
                        High(lookup_results_inner)) do
                    begin
                        if Trim(lookup_results_inner[edge_idx_inner].comment) <> '' then
                        begin
                            Continue;
                        end;
                        edge_text_inner := Trim(
                            lookup_results_inner[edge_idx_inner].text);
                        if (get_candidate_text_unit_count(edge_text_inner) <> 1) or
                            (not is_valid_standalone_cjk_phrase_text_local(
                            edge_text_inner, 1)) then
                        begin
                            Continue;
                        end;
                        edge_raw_inner := get_exact_chunk_raw_weight_local(
                            lookup_results_inner[edge_idx_inner]);
                        edge_top_inner := Max(edge_top_inner, edge_raw_inner);
                    end;

                    for edge_idx_inner := 0 to Min(
                        get_chain_probe_limit_local - 1,
                        High(lookup_results_inner)) do
                    begin
                        if Trim(lookup_results_inner[edge_idx_inner].comment) <> '' then
                        begin
                            Continue;
                        end;
                        edge_text_inner := Trim(
                            lookup_results_inner[edge_idx_inner].text);
                        if SameText(edge_text_inner, protected_text_inner) or
                            (get_candidate_text_unit_count(edge_text_inner) <> 1) or
                            (not is_valid_standalone_cjk_phrase_text_local(
                            edge_text_inner, 1)) then
                        begin
                            Continue;
                        end;
                        edge_raw_inner := get_exact_chunk_raw_weight_local(
                            lookup_results_inner[edge_idx_inner]);
                        edge_score_inner := get_exact_chunk_effective_weight_local(
                            lookup_results_inner[edge_idx_inner]);
                        Inc(edge_score_inner,
                            get_preferred_query_phrase_bonus_local(
                            query_key_inner, edge_text_inner));
                        Dec(edge_score_inner,
                            get_context_free_single_char_penalty_local(
                            query_key_inner, edge_text_inner));
                        if start_idx_local + chunk_len_inner =
                            syllable_count_local then
                        begin
                            Dec(edge_score_inner, 24000);
                        end;

                        edge_option_inner := Default(TExactChunkChainOption);
                        edge_option_inner.text := edge_text_inner;
                        edge_option_inner.path := edge_text_inner;
                        edge_option_inner.query_key := query_key_inner;
                        edge_option_inner.score := edge_score_inner;
                        edge_option_inner.exact_rank := edge_idx_inner + 1;
                        edge_option_inner.raw_weight := edge_raw_inner;
                        edge_option_inner.top_weight := edge_top_inner;
                        edge_option_inner.source_user :=
                            lookup_results_inner[edge_idx_inner].source = cs_user;
                        edge_option_inner.direct_exact := True;
                        edge_option_inner.edge_expanded_only := True;
                        edge_option_inner.segments := 1;
                        edge_option_inner.has_anchor := False;
                        edge_option_inner.anchor_units := 0;
                        add_fast_option_local(out_options_local,
                            legacy_options_inner, edge_option_inner);
                    end;
                end;

            begin
                Result := False;
                SetLength(out_options_local, 0);
                SetLength(legacy_options_inner, 0);
                best_direct_raw_weight_inner := Low(Integer);
                query_key_inner := build_query_key_local(start_idx_local,
                    chunk_len_inner);
                if query_key_inner = '' then
                begin
                    Exit;
                end;

                if chunk_len_inner = 1 then
                begin
                    if (start_idx_local + chunk_len_inner =
                        syllable_count_local) and
                        try_get_sentence_final_modal_particle_text_fast_local(
                        effective_syllables[start_idx_local].text,
                        text_inner) then
                    begin
                        { See the short-tail path above: final particles need a
                          sentence-final single, not the generic boundary one. }
                    end
                    else if not try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx_local].text, text_inner) then
                    begin
                        text_inner :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[start_idx_local].text);
                    end;
                    normalized_single_key_inner := normalize_pinyin_text(
                        query_key_inner);
                    if text_inner = '' then
                    begin
                        if not try_get_best_single_char_for_query_key_local(
                            query_key_inner, text_inner, score_inner) then
                        begin
                            Exit;
                        end;
                        if score_inner > 120 then
                        begin
                            score_inner := 120;
                        end;
                        Dec(score_inner, 900);

                        option_inner := Default(TExactChunkChainOption);
                        option_inner.text := text_inner;
                        option_inner.path := text_inner;
                        option_inner.query_key := query_key_inner;
                        option_inner.score := score_inner;
                        option_inner.segments := 1;
                        option_inner.has_anchor := False;
                        option_inner.anchor_units := 0;
                        add_fast_option_local(out_options_local,
                            legacy_options_inner, option_inner);
                        if normalized_single_key_inner = 'wei' then
                        begin
                            if not SameText(text_inner, string(Char($672A))) then
                            begin
                                add_fixed_single_option_inner(string(Char($672A)),
                                    1800);
                            end;
                            if not SameText(text_inner, string(Char($4E3A))) then
                            begin
                                add_fixed_single_option_inner(string(Char($4E3A)),
                                    2400);
                            end;
                        end
                        else if (normalized_single_key_inner = 'yu') and
                            (not SameText(text_inner, string(Char($4E0E)))) then
                        begin
                            add_fixed_single_option_inner(string(Char($4E0E)),
                                2200);
                        end;
                        if use_exact_edge_ranker_expansion_local then
                        begin
                            add_single_exact_edge_candidates_inner(text_inner);
                        end;
                        Exit(finish_options_inner);
                    end;

                    add_fixed_single_option_inner(text_inner, 0);
                    if normalized_single_key_inner = 'wei' then
                    begin
                        if not SameText(text_inner, string(Char($672A))) then
                        begin
                            add_fixed_single_option_inner(string(Char($672A)),
                                1800);
                        end;
                        if not SameText(text_inner, string(Char($4E3A))) then
                        begin
                            add_fixed_single_option_inner(string(Char($4E3A)),
                                2400);
                        end;
                    end
                    else if (normalized_single_key_inner = 'yu') and
                        (not SameText(text_inner, string(Char($4E0E)))) then
                    begin
                        add_fixed_single_option_inner(string(Char($4E0E)), 2200);
                    end;
                    if use_exact_edge_ranker_expansion_local then
                    begin
                        add_single_exact_edge_candidates_inner(text_inner);
                    end;
                    Exit(finish_options_inner);
                end;

                if (input_syllable_count <= 6) and (m_dictionary <> nil) and
                    (not m_dictionary.lookup(query_key_inner,
                    ranked_lookup_results_inner)) then
                begin
                    ranked_lookup_results_inner := nil;
                end;

                if chunk_len_inner = 3 then
                begin
                    prefix_text_inner := '';
                    prefix_is_modifier_inner := try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx_local].text,
                        prefix_text_inner);
                    if not prefix_is_modifier_inner then
                    begin
                        prefix_text_inner :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[start_idx_local].text);
                        if (prefix_text_inner = '') or
                            (not is_function_or_modifier_single_quality_inner(
                            prefix_text_inner)) then
                        begin
                            prefix_text_inner := '';
                        end;
                    end;

                    if prefix_text_inner <> '' then
                    begin
                        head_has_strong_exact_inner := False;
                        head_key_inner := build_query_key_local(start_idx_local, 2);
                        if (head_key_inner <> '') and
                            lookup_exact_full_pinyin_cached_local(head_key_inner,
                            head_lookup_results_inner) then
                        begin
                            for head_idx_inner := 0 to High(head_lookup_results_inner) do
                            begin
                                if head_idx_inner >= get_chain_probe_limit_local then
                                begin
                                    Break;
                                end;
                                if Trim(head_lookup_results_inner[head_idx_inner].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                head_text_inner := Trim(
                                    head_lookup_results_inner[head_idx_inner].text);
                                if (get_candidate_text_unit_count(head_text_inner) = 2) and
                                    is_valid_standalone_cjk_phrase_text_local(
                                    head_text_inner, 2) and
                                    (((head_lookup_results_inner[head_idx_inner].has_dict_weight) and
                                    (head_lookup_results_inner[head_idx_inner].dict_weight >= 700)) or
                                    (head_lookup_results_inner[head_idx_inner].score >= 700)) then
                                begin
                                    head_has_strong_exact_inner := True;
                                    Break;
                                end;
                            end;
                        end;

                        tail_key_inner := build_query_key_local(start_idx_local + 1, 2);
                        if ((not head_has_strong_exact_inner) or
                            (not SameText(prefix_text_inner, string(Char($9AD8))))) and
                            (tail_key_inner <> '') and
                            lookup_exact_full_pinyin_cached_local(tail_key_inner,
                            tail_lookup_results_inner) then
                        begin
                            for tail_idx_inner := 0 to High(tail_lookup_results_inner) do
                            begin
                                if tail_idx_inner >= get_chain_probe_limit_local then
                                begin
                                    Break;
                                end;
                                if Trim(tail_lookup_results_inner[tail_idx_inner].comment) <> '' then
                                begin
                                    Continue;
                                end;

                                tail_text_inner := Trim(
                                    tail_lookup_results_inner[tail_idx_inner].text);
                                if (get_candidate_text_unit_count(tail_text_inner) <> 2) or
                                    (not is_valid_standalone_cjk_phrase_text_local(
                                    tail_text_inner, 2)) or
                                    (not is_reliable_fast_exact_chunk_local(
                                    tail_key_inner, 2, tail_text_inner,
                                    tail_lookup_results_inner[tail_idx_inner])) then
                                begin
                                    Continue;
                                end;

                                text_inner := prefix_text_inner + tail_text_inner;
                                score_inner := get_exact_chunk_effective_weight_local(
                                    tail_lookup_results_inner[tail_idx_inner]);
                                Inc(score_inner, get_preferred_exact_subspan_bonus_local(
                                    query_key_inner, text_inner));
                                Inc(score_inner, get_preferred_query_phrase_bonus_local(
                                    tail_key_inner, tail_text_inner));
                                Inc(score_inner, get_exact_query_text_bonus_local(
                                    tail_key_inner, tail_text_inner));
                                Inc(score_inner, get_exact_chunk_semantic_bonus_local(
                                    tail_text_inner));
                                Inc(score_inner, get_modifier_prefix_phrase_bonus_local(
                                    chunk_len_inner, text_inner));
                                Inc(score_inner, chunk_len_inner * chunk_len_inner *
                                    c_fast_length_bonus);
                                Inc(score_inner, c_fast_fixed_single_bonus);
                                Inc(score_inner, (get_chain_probe_limit_local - tail_idx_inner) * 128);
                                Inc(score_inner, c_chain_anchor_bonus);

                                option_inner := Default(TExactChunkChainOption);
                                option_inner.text := text_inner;
                                option_inner.path := prefix_text_inner +
                                    c_segment_path_separator + tail_text_inner;
                                option_inner.query_key := query_key_inner;
                                option_inner.score := score_inner;
                                option_inner.segments := 2;
                                option_inner.has_anchor := True;
                                option_inner.anchor_units := 2;
                                add_fast_option_local(out_options_local,
                                    legacy_options_inner, option_inner);
                            end;
                        end;
                    end;
                end;

                if (chunk_len_inner >= 4) and (chunk_len_inner <= 5) then
                begin
                    prefix_text_inner := '';
                    prefix_score_inner := 0;
                    prefix_is_modifier_inner := try_get_modifier_prefix_single_text_local(
                        effective_syllables[start_idx_local].text,
                        prefix_text_inner);
                    if not prefix_is_modifier_inner then
                    begin
                        prefix_text_inner :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[start_idx_local].text);
                        if prefix_text_inner <> '' then
                        begin
                            prefix_score_inner := 1200;
                        end
                        else
                        begin
                            prefix_text_inner := '';
                        end;
                    end
                    else
                    begin
                        prefix_score_inner := 1200;
                    end;

                    if (prefix_text_inner <> '') and
                        ((chunk_len_inner > 3) or (not prefix_is_modifier_inner)) then
                    begin
                        tail_units_inner := chunk_len_inner - 1;
                        tail_key_inner := build_query_key_local(start_idx_local + 1,
                            tail_units_inner);
                        if (tail_key_inner <> '') and
                            lookup_exact_full_pinyin_cached_local(tail_key_inner,
                            tail_lookup_results_inner) then
                        begin
                            for tail_idx_inner := 0 to High(tail_lookup_results_inner) do
                            begin
                                if tail_idx_inner >= get_chain_probe_limit_local then
                                begin
                                    Break;
                                end;
                                if Trim(tail_lookup_results_inner[tail_idx_inner].comment) <> '' then
                                begin
                                    Continue;
                                end;

                                tail_text_inner := Trim(
                                    tail_lookup_results_inner[tail_idx_inner].text);
                                if (get_candidate_text_unit_count(tail_text_inner) <>
                                    tail_units_inner) or
                                    (not is_valid_standalone_cjk_phrase_text_local(
                                    tail_text_inner, tail_units_inner)) or
                                    (not is_reliable_fast_exact_chunk_local(
                                    tail_key_inner, tail_units_inner,
                                    tail_text_inner,
                                    tail_lookup_results_inner[tail_idx_inner])) then
                                begin
                                    Continue;
                                end;

                                tail_raw_weight_inner := get_exact_chunk_raw_weight_local(
                                    tail_lookup_results_inner[tail_idx_inner]);
                                if tail_raw_weight_inner < 760 then
                                begin
                                    Continue;
                                end;

                                text_inner := prefix_text_inner + tail_text_inner;
                                score_inner := get_exact_chunk_effective_weight_local(
                                    tail_lookup_results_inner[tail_idx_inner]);
                                Inc(score_inner, Min(1600, Max(0,
                                    prefix_score_inner)));
                                Inc(score_inner, get_preferred_exact_subspan_bonus_local(
                                    query_key_inner, text_inner));
                                Inc(score_inner, get_preferred_query_phrase_bonus_local(
                                    tail_key_inner, tail_text_inner));
                                Inc(score_inner, get_exact_query_text_bonus_local(
                                    tail_key_inner, tail_text_inner));
                                Inc(score_inner, get_exact_chunk_semantic_bonus_local(
                                    tail_text_inner));
                                Inc(score_inner, get_modifier_prefix_phrase_bonus_local(
                                    chunk_len_inner, text_inner));
                                Inc(score_inner, chunk_len_inner * chunk_len_inner *
                                    c_fast_length_bonus);
                                Inc(score_inner, c_fast_fixed_single_bonus);
                                Inc(score_inner, (get_chain_probe_limit_local - tail_idx_inner) * 128);
                                Inc(score_inner, c_chain_anchor_bonus);

                                option_inner := Default(TExactChunkChainOption);
                                option_inner.text := text_inner;
                                option_inner.path := prefix_text_inner +
                                    c_segment_path_separator + tail_text_inner;
                                option_inner.query_key := query_key_inner;
                                option_inner.score := score_inner;
                                option_inner.segments := 2;
                                option_inner.has_anchor := True;
                                option_inner.anchor_units := chunk_len_inner;
                                add_fast_option_local(out_options_local,
                                    legacy_options_inner, option_inner);
                            end;
                        end;
                    end;
                end;

                // Keep proven exact-word pairs available to the long lattice.
                // For ultra-long input this is deliberately restricted to 2+2
                // pairs with strong offline LM evidence, so arbitrary exact
                // words do not become a general-purpose composition path.
                if (chunk_len_inner >= 4) and (chunk_len_inner <= 5) and
                    ((syllable_count_local <= 12) or
                    (chunk_len_inner = 4)) and (m_dictionary <> nil) then
                begin
                    for split_len_inner := 2 to chunk_len_inner - 2 do
                    begin
                        if (syllable_count_local > 12) and
                            (split_len_inner <> 2) then
                        begin
                            Continue;
                        end;
                        left_key_inner := build_query_key_local(start_idx_local,
                            split_len_inner);
                        right_key_inner := build_query_key_local(start_idx_local +
                            split_len_inner, chunk_len_inner - split_len_inner);
                        if (left_key_inner = '') or (right_key_inner = '') or
                            (not lookup_exact_full_pinyin_cached_local(left_key_inner,
                            left_lookup_results_inner)) or
                            (not lookup_exact_full_pinyin_cached_local(right_key_inner,
                            right_lookup_results_inner)) then
                        begin
                            Continue;
                        end;

                        for left_idx_inner := 0 to High(left_lookup_results_inner) do
                        begin
                            if left_idx_inner >= c_fast_split_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(left_lookup_results_inner[left_idx_inner].comment) <> '' then
                            begin
                                Continue;
                            end;

                            left_text_inner := Trim(
                                left_lookup_results_inner[left_idx_inner].text);
                            if (get_candidate_text_unit_count(left_text_inner) <>
                                split_len_inner) or
                                (not is_valid_standalone_cjk_phrase_text_local(
                                left_text_inner, split_len_inner)) or
                                (not is_reliable_fast_exact_chunk_local(
                                left_key_inner, split_len_inner, left_text_inner,
                                left_lookup_results_inner[left_idx_inner])) then
                            begin
                                Continue;
                            end;

                            for right_idx_inner := 0 to High(right_lookup_results_inner) do
                            begin
                                if right_idx_inner >= c_fast_split_probe_limit then
                                begin
                                    Break;
                                end;
                                if Trim(right_lookup_results_inner[right_idx_inner].comment) <> '' then
                                begin
                                    Continue;
                                end;

                                right_text_inner := Trim(
                                    right_lookup_results_inner[right_idx_inner].text);
                                if (get_candidate_text_unit_count(right_text_inner) <>
                                    chunk_len_inner - split_len_inner) or
                                    (not is_valid_standalone_cjk_phrase_text_local(
                                    right_text_inner,
                                    chunk_len_inner - split_len_inner)) or
                                    (not is_reliable_fast_exact_chunk_local(
                                    right_key_inner,
                                    chunk_len_inner - split_len_inner,
                                    right_text_inner,
                                    right_lookup_results_inner[right_idx_inner])) then
                                begin
                                    Continue;
                                end;

                                split_path_inner := left_text_inner +
                                    c_segment_path_separator + right_text_inner;
                                split_path_bonus_inner := Max(0,
                                    get_cached_query_segment_path_bonus(
                                    query_key_inner, split_path_inner));
                                split_path_penalty_inner := Max(0,
                                    get_cached_query_segment_path_penalty(
                                    query_key_inner, split_path_inner));
                                split_lm_bonus_inner := Max(0,
                                    m_dictionary.get_lm_transition_bonus(
                                    query_key_inner, split_path_inner));
                                if (split_path_bonus_inner <=
                                    split_path_penalty_inner) and
                                    (split_lm_bonus_inner <
                                    c_chain_lattice_lm_min_bonus) then
                                begin
                                    Continue;
                                end;

                                split_score_inner :=
                                    get_exact_chunk_effective_weight_local(
                                    left_lookup_results_inner[left_idx_inner]) +
                                    get_exact_chunk_effective_weight_local(
                                    right_lookup_results_inner[right_idx_inner]);
                                Inc(split_score_inner,
                                    Min(2600, split_path_bonus_inner * 4));
                                Inc(split_score_inner,
                                    Min(2400, split_lm_bonus_inner * 4));
                                Dec(split_score_inner,
                                    Min(1800, split_path_penalty_inner * 4));
                                Inc(split_score_inner,
                                    get_preferred_query_phrase_bonus_local(
                                    query_key_inner,
                                    left_text_inner + right_text_inner));
                                Inc(split_score_inner,
                                    get_exact_query_text_bonus_local(
                                    query_key_inner,
                                    left_text_inner + right_text_inner));
                                Inc(split_score_inner, get_exact_chunk_semantic_bonus_local(
                                    left_text_inner + right_text_inner));
                                Inc(split_score_inner, chunk_len_inner *
                                    chunk_len_inner * c_fast_length_bonus);
                                Inc(split_score_inner, c_chain_anchor_bonus);
                                Inc(split_score_inner,
                                    (get_chain_probe_limit_local - left_idx_inner) * 96);
                                Inc(split_score_inner,
                                    (get_chain_probe_limit_local - right_idx_inner) * 96);

                                option_inner := Default(TExactChunkChainOption);
                                option_inner.text := left_text_inner + right_text_inner;
                                option_inner.path := split_path_inner;
                                option_inner.query_key := query_key_inner;
                                option_inner.score := split_score_inner;
                                option_inner.segments := 2;
                                option_inner.has_anchor := True;
                                option_inner.anchor_units := chunk_len_inner;
                                add_fast_option_local(out_options_local,
                                    legacy_options_inner, option_inner);
                            end;
                        end;
                    end;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_inner,
                    lookup_results_inner) then
                begin
                    Exit(finish_options_inner);
                end;

                for lookup_idx_inner := 0 to High(lookup_results_inner) do
                begin
                    if lookup_idx_inner >= get_chain_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results_inner[lookup_idx_inner].comment) <> '' then
                    begin
                        Continue;
                    end;

                    text_inner := Trim(lookup_results_inner[lookup_idx_inner].text);
                    if (get_candidate_text_unit_count(text_inner) <> chunk_len_inner) or
                        (not is_valid_standalone_cjk_phrase_text_local(text_inner,
                        chunk_len_inner)) or
                        (not is_reliable_fast_exact_chunk_local(query_key_inner,
                        chunk_len_inner, text_inner,
                        lookup_results_inner[lookup_idx_inner])) then
                    begin
                        Continue;
                    end;
                    raw_weight_inner := get_exact_chunk_raw_weight_local(
                        lookup_results_inner[lookup_idx_inner]);
                    if should_defer_pair_to_modifier_tail_exact_inner(text_inner,
                        raw_weight_inner) then
                    begin
                        Continue;
                    end;
                    if should_defer_chunk_to_head_plus_function_tail_inner(
                        text_inner, raw_weight_inner) then
                    begin
                        Continue;
                    end;

                    score_inner := get_exact_chunk_effective_weight_local(
                        lookup_results_inner[lookup_idx_inner]);
                    ranked_score_inner :=
                        get_ranked_lookup_score_for_text_inner(text_inner);
                    if ranked_score_inner > score_inner then
                    begin
                        score_inner := ranked_score_inner;
                    end;
                    Inc(score_inner, get_preferred_exact_subspan_bonus_local(
                        query_key_inner, text_inner));
                    Inc(score_inner, get_preferred_query_phrase_bonus_local(
                        query_key_inner, text_inner));
                    Inc(score_inner, get_exact_query_text_bonus_local(query_key_inner,
                        text_inner));
                    Inc(score_inner, get_long_exact_phrase_cohesion_bonus_local(
                        chunk_len_inner));
                    Inc(score_inner, get_modifier_prefix_phrase_bonus_local(
                        chunk_len_inner, text_inner));
                    Inc(score_inner, get_exact_chunk_boundary_adjustment_local(
                        start_idx_local, chunk_len_inner, text_inner));
                    Inc(score_inner, get_exact_chunk_semantic_bonus_local(text_inner));
                    Inc(score_inner, get_directional_locative_fast_bonus_inner(
                        text_inner));
                    Inc(score_inner, get_final_compound_tail_support_bonus_inner(
                        text_inner, raw_weight_inner));
                    Dec(score_inner,
                        get_function_single_pair_exact_penalty_inner(text_inner));
                    if (lookup_idx_inner = 0) and (chunk_len_inner >= 3) then
                    begin
                        Inc(score_inner, 7200);
                    end;
                    Inc(score_inner, chunk_len_inner * chunk_len_inner *
                        c_fast_length_bonus);
                    Inc(score_inner, (get_chain_probe_limit_local - lookup_idx_inner) * 192);
                    if chunk_len_inner >= c_chain_min_anchor_syllables then
                    begin
                        Inc(score_inner, c_chain_anchor_bonus);
                    end;

                    option_inner := Default(TExactChunkChainOption);
                    option_inner.text := text_inner;
                    option_inner.path := text_inner;
                    option_inner.query_key := query_key_inner;
                    option_inner.score := score_inner;
                    if use_exact_edge_ranker_expansion_local then
                    begin
                        option_inner.exact_rank := lookup_idx_inner + 1;
                        option_inner.raw_weight := raw_weight_inner;
                        option_inner.top_weight :=
                            get_best_direct_raw_weight_inner;
                        option_inner.source_user :=
                            lookup_results_inner[lookup_idx_inner].source = cs_user;
                        option_inner.direct_exact := True;
                    end;
                    option_inner.segments := 1;
                    option_inner.has_anchor :=
                        chunk_len_inner >= c_chain_min_anchor_syllables;
                    option_inner.anchor_units := chunk_len_inner;
                    add_fast_option_local(out_options_local,
                        legacy_options_inner, option_inner);
                end;
                Result := finish_options_inner;
            end;

            procedure consider_generated_fast_state_local(
                const generated_state_inner: TExactChunkChainState);
            begin
                debug_note_long_exact_edge_stage(pos_local,
                    syllable_count_local,
                    generated_state_inner.first_chunk_query_key,
                    generated_state_inner.first_chunk_text,
                    get_candidate_text_unit_count(
                    generated_state_inner.first_chunk_text),
                    generated_state_inner.first_chunk_exact_rank,
                    generated_state_inner.first_chunk_raw_weight,
                    generated_state_inner.first_chunk_top_weight,
                    generated_state_inner.first_chunk_effective_score,
                    generated_state_inner.first_chunk_edge_score,
                    generated_state_inner.first_chunk_source_user,
                    generated_state_inner.first_chunk_baseline_option, 1);
                if (not m_debug_expand_long_beam_states) and
                    (exact_edge_reserved_slots_local > 0) and
                    ((not generated_state_inner.first_chunk_baseline_option) or
                    (generated_state_inner.retained_exact_edge_count > 0)) then
                begin
                    consider_exact_chunk_chain_state_local(
                        edge_probe_candidates_local,
                        generated_state_inner, pos_local,
                        get_chain_prebeam_width_local);
                    Exit;
                end;
                consider_exact_chunk_chain_state_local(
                    state_map_local[pos_local], generated_state_inner,
                    pos_local, get_chain_prebeam_width_local);
                if generated_state_inner.baseline_lineage then
                begin
                    consider_exact_chunk_chain_state_local(
                        baseline_candidates_local, generated_state_inner,
                        pos_local, get_chain_baseline_prebeam_width_local);
                end;
            end;

            procedure collect_runtime_exact_edge_probe_states_local(
                const start_pos_inner: Integer;
                const chunk_len_inner: Integer;
                const next_pos_inner: Integer;
                const probe_options_inner: TExactChunkChainOptionArray);
            var
                probe_idx_inner: Integer;
                probe_state_inner: TExactChunkChainState;
                probe_suffix_inner: TExactChunkChainState;
                context_adjust_inner: Integer;

                procedure retain_model_probe_inner(
                    const value_inner: TExactChunkChainState);
                var
                    existing_idx_inner: Integer;
                    insert_idx_inner: Integer;
                    swap_inner: TExactChunkChainState;
                begin
                    for existing_idx_inner := 0 to
                        High(exact_edge_model_candidates_local) do
                    begin
                        if SameText(exact_edge_model_candidates_local[
                            existing_idx_inner].first_chunk_query_key,
                            value_inner.first_chunk_query_key) and
                            SameText(Trim(exact_edge_model_candidates_local[
                            existing_idx_inner].first_chunk_text),
                            Trim(value_inner.first_chunk_text)) and
                            SameText(exact_edge_model_candidates_local[
                            existing_idx_inner].path, value_inner.path) then
                        begin
                            if value_inner.score >
                                exact_edge_model_candidates_local[
                                existing_idx_inner].score then
                            begin
                                exact_edge_model_candidates_local[
                                    existing_idx_inner] := value_inner;
                            end;
                            Exit;
                        end;
                    end;
                    SetLength(exact_edge_model_candidates_local,
                        Length(exact_edge_model_candidates_local) + 1);
                    exact_edge_model_candidates_local[
                        High(exact_edge_model_candidates_local)] :=
                        value_inner;
                    insert_idx_inner :=
                        High(exact_edge_model_candidates_local);
                    while insert_idx_inner > 0 do
                    begin
                        if exact_edge_model_candidates_local[
                            insert_idx_inner - 1].first_chunk_exact_edge_model_score >
                            exact_edge_model_candidates_local[
                            insert_idx_inner].first_chunk_exact_edge_model_score then
                        begin
                            Break;
                        end;
                        if (exact_edge_model_candidates_local[
                            insert_idx_inner - 1].first_chunk_exact_edge_model_score =
                            exact_edge_model_candidates_local[
                            insert_idx_inner].first_chunk_exact_edge_model_score) and
                            (exact_edge_model_candidates_local[
                            insert_idx_inner - 1].score >=
                            exact_edge_model_candidates_local[
                            insert_idx_inner].score) then
                        begin
                            Break;
                        end;
                        swap_inner := exact_edge_model_candidates_local[
                            insert_idx_inner - 1];
                        exact_edge_model_candidates_local[
                            insert_idx_inner - 1] :=
                            exact_edge_model_candidates_local[
                            insert_idx_inner];
                        exact_edge_model_candidates_local[
                            insert_idx_inner] := swap_inner;
                        Dec(insert_idx_inner);
                    end;
                    if Length(exact_edge_model_candidates_local) >
                        c_chain_lattice_exact_edge_probe_limit then
                    begin
                        SetLength(exact_edge_model_candidates_local,
                            c_chain_lattice_exact_edge_probe_limit);
                    end;
                end;
            begin
                if m_debug_expand_long_beam_states or
                    (exact_edge_reserved_slots_local <= 0) or
                    (chunk_len_inner <
                    c_chain_lattice_exact_edge_min_units) or
                    (Length(state_map_local[next_pos_inner]) <= 0) then
                begin
                    Exit;
                end;
                probe_suffix_inner := state_map_local[next_pos_inner][0];
                for probe_idx_inner := 0 to High(probe_options_inner) do
                begin
                    if (not probe_options_inner[
                        probe_idx_inner].direct_exact) or
                        probe_options_inner[probe_idx_inner].source_user or
                        (probe_options_inner[
                        probe_idx_inner].exact_edge_model_score <
                        c_chain_lattice_exact_edge_min_model_score) then
                    begin
                        Continue;
                    end;
                    probe_state_inner := Default(TExactChunkChainState);
                    probe_state_inner.text := probe_options_inner[
                        probe_idx_inner].text + probe_suffix_inner.text;
                    probe_state_inner.path := probe_options_inner[
                        probe_idx_inner].path;
                    if probe_suffix_inner.path <> '' then
                    begin
                        probe_state_inner.path := probe_state_inner.path +
                            c_segment_path_separator +
                            probe_suffix_inner.path;
                    end;
                    probe_state_inner.first_chunk_text :=
                        probe_options_inner[probe_idx_inner].text;
                    probe_state_inner.first_chunk_query_key :=
                        probe_options_inner[probe_idx_inner].query_key;
                    probe_state_inner.first_chunk_exact_rank :=
                        probe_options_inner[probe_idx_inner].exact_rank;
                    probe_state_inner.first_chunk_raw_weight :=
                        probe_options_inner[probe_idx_inner].raw_weight;
                    probe_state_inner.first_chunk_top_weight :=
                        probe_options_inner[probe_idx_inner].top_weight;
                    probe_state_inner.first_chunk_effective_score :=
                        probe_options_inner[probe_idx_inner].score;
                    probe_state_inner.first_chunk_edge_score :=
                        probe_options_inner[
                        probe_idx_inner].edge_retention_score;
                    probe_state_inner.first_chunk_exact_edge_model_score :=
                        probe_options_inner[
                        probe_idx_inner].exact_edge_model_score;
                    probe_state_inner.first_chunk_source_user := False;
                    probe_state_inner.first_chunk_baseline_option :=
                        probe_options_inner[
                        probe_idx_inner].baseline_option;
                    probe_state_inner.score := probe_options_inner[
                        probe_idx_inner].score + probe_suffix_inner.score;
                    Dec(probe_state_inner.score,
                        get_competing_boundary_penalty_fast_local(
                        probe_state_inner.text, start_pos_inner,
                        chunk_len_inner,
                        probe_options_inner[probe_idx_inner],
                        probe_suffix_inner));
                    if probe_suffix_inner.first_chunk_text <> '' then
                    begin
                        Inc(probe_state_inner.score,
                            m_dictionary.get_context_bonus(
                            probe_options_inner[probe_idx_inner].text,
                            probe_suffix_inner.first_chunk_text));
                        Inc(probe_state_inner.score,
                            get_adjacent_exact_chunk_context_bonus_local(
                            probe_options_inner[probe_idx_inner].text,
                            probe_suffix_inner.first_chunk_text));
                        context_adjust_inner :=
                            get_function_particle_suffix_context_adjustment_local(
                            effective_syllables[start_pos_inner +
                            chunk_len_inner - 1].text,
                            probe_options_inner[probe_idx_inner].text,
                            probe_suffix_inner.first_chunk_text);
                        Inc(probe_state_inner.score,
                            context_adjust_inner);
                    end;
                    probe_state_inner.segments :=
                        probe_options_inner[probe_idx_inner].segments +
                        probe_suffix_inner.segments;
                    probe_state_inner.has_anchor :=
                        probe_options_inner[probe_idx_inner].has_anchor or
                        probe_suffix_inner.has_anchor;
                    probe_state_inner.anchor_units := Max(
                        probe_options_inner[probe_idx_inner].anchor_units,
                        probe_suffix_inner.anchor_units);
                    probe_state_inner.retained_exact_edge_count :=
                        probe_suffix_inner.retained_exact_edge_count;
                    probe_state_inner.retained_exact_edge_position :=
                        probe_suffix_inner.retained_exact_edge_position;
                    probe_state_inner.retained_exact_edge_units :=
                        probe_suffix_inner.retained_exact_edge_units;
                    probe_state_inner.retained_exact_edge_rank :=
                        probe_suffix_inner.retained_exact_edge_rank;
                    probe_state_inner.retained_exact_edge_top_weight :=
                        probe_suffix_inner.retained_exact_edge_top_weight;
                    probe_state_inner.retained_exact_edge_confidence :=
                        probe_suffix_inner.retained_exact_edge_confidence;
                    probe_state_inner.baseline_lineage := False;
                    debug_note_long_exact_edge_stage(start_pos_inner,
                        syllable_count_local,
                        probe_state_inner.first_chunk_query_key,
                        probe_state_inner.first_chunk_text,
                        chunk_len_inner,
                        probe_state_inner.first_chunk_exact_rank,
                        probe_state_inner.first_chunk_raw_weight,
                        probe_state_inner.first_chunk_top_weight,
                        probe_state_inner.first_chunk_effective_score,
                        probe_state_inner.first_chunk_edge_score,
                        False, False, 1);
                    retain_model_probe_inner(probe_state_inner);
                end;
            end;

            function try_get_retained_edge_for_position_local(
                const position_inner: Integer;
                out edge_inner: TncLongRetainedExactEdge): Boolean;
            var
                edge_idx_inner: Integer;
            begin
                Result := False;
                edge_inner := Default(TncLongRetainedExactEdge);
                for edge_idx_inner := 0 to
                    High(m_runtime_long_retained_exact_edges) do
                begin
                    if m_runtime_long_retained_exact_edges[
                        edge_idx_inner].position = position_inner then
                    begin
                        edge_inner := m_runtime_long_retained_exact_edges[
                            edge_idx_inner];
                        Exit(True);
                    end;
                end;
            end;

            procedure mark_state_with_retained_edge_local(
                var state_inner: TExactChunkChainState;
                const edge_inner: TncLongRetainedExactEdge);
            begin
                if (state_inner.retained_exact_edge_count <= 0) or
                    (edge_inner.confidence >
                    state_inner.retained_exact_edge_confidence) then
                begin
                    state_inner.retained_exact_edge_position :=
                        edge_inner.position;
                    state_inner.retained_exact_edge_units :=
                        edge_inner.chunk_units;
                    state_inner.retained_exact_edge_rank :=
                        edge_inner.exact_rank;
                    state_inner.retained_exact_edge_top_weight :=
                        edge_inner.top_weight;
                    state_inner.retained_exact_edge_confidence :=
                        edge_inner.confidence;
                end;
                Inc(state_inner.retained_exact_edge_count);
                state_inner.baseline_lineage := False;
            end;

            function retained_slot_state_is_better_local(
                const candidate_inner: TExactChunkChainState;
                const current_inner: TExactChunkChainState;
                const state_start_pos_inner: Integer): Boolean;
            var
                candidate_lm_inner: Integer;
                current_lm_inner: Integer;
                candidate_stats_inner: TLongWordLmBoundaryStats;
                current_stats_inner: TLongWordLmBoundaryStats;
                candidate_confidence_inner: Int64;
                current_confidence_inner: Int64;
                candidate_score_inner: Int64;
                current_score_inner: Int64;

                function bounded_confidence_inner(
                    const value_inner: Int64): Int64;
                begin
                    Result := value_inner div 8;
                    if Result < -48000 then
                    begin
                        Result := -48000;
                    end
                    else if Result > 96000 then
                    begin
                        Result := 96000;
                    end;
                end;
            begin
                candidate_lm_inner := get_exact_chunk_chain_state_lm_bonus_local(
                    candidate_inner, state_start_pos_inner,
                    candidate_stats_inner);
                current_lm_inner := get_exact_chunk_chain_state_lm_bonus_local(
                    current_inner, state_start_pos_inner, current_stats_inner);
                candidate_confidence_inner := bounded_confidence_inner(
                    candidate_inner.retained_exact_edge_confidence);
                current_confidence_inner := bounded_confidence_inner(
                    current_inner.retained_exact_edge_confidence);
                candidate_score_inner := Int64(candidate_inner.score) +
                    candidate_lm_inner + candidate_confidence_inner;
                current_score_inner := Int64(current_inner.score) +
                    current_lm_inner + current_confidence_inner;
                Result := candidate_score_inner > current_score_inner;
                if candidate_score_inner = current_score_inner then
                begin
                    Result := candidate_inner.retained_exact_edge_count >
                        current_inner.retained_exact_edge_count;
                end;
                if (candidate_score_inner = current_score_inner) and
                    (candidate_inner.retained_exact_edge_count =
                    current_inner.retained_exact_edge_count) then
                begin
                    Result := is_exact_chunk_chain_state_better_local(
                        candidate_inner, current_inner);
                end;
            end;

            procedure choose_retained_exact_edge_slots_local(
                const position_inner: Integer;
                const propagated_states_inner:
                    TExactChunkChainStateArray;
                const model_states_inner: TExactChunkChainStateArray;
                out states_inner: TExactChunkChainStateArray);
            var
                retained_edge_inner: TncLongRetainedExactEdge;
                source_idx_inner: Integer;
                candidate_inner: TExactChunkChainState;

                procedure consider_side_state_inner(
                    const value_inner: TExactChunkChainState);
                var
                    side_idx_inner: Integer;
                    insert_idx_inner: Integer;
                    swap_inner: TExactChunkChainState;
                begin
                    for side_idx_inner := 0 to High(states_inner) do
                    begin
                        if SameText(Trim(states_inner[side_idx_inner].text),
                            Trim(value_inner.text)) and
                            SameText(states_inner[side_idx_inner].path,
                            value_inner.path) then
                        begin
                            if retained_slot_state_is_better_local(
                                value_inner, states_inner[side_idx_inner],
                                position_inner) then
                            begin
                                states_inner[side_idx_inner] := value_inner;
                            end;
                            Exit;
                        end;
                    end;
                    SetLength(states_inner, Length(states_inner) + 1);
                    states_inner[High(states_inner)] := value_inner;
                    insert_idx_inner := High(states_inner);
                    while (insert_idx_inner > 0) and
                        retained_slot_state_is_better_local(
                        states_inner[insert_idx_inner],
                        states_inner[insert_idx_inner - 1],
                        position_inner) do
                    begin
                        swap_inner := states_inner[insert_idx_inner - 1];
                        states_inner[insert_idx_inner - 1] :=
                            states_inner[insert_idx_inner];
                        states_inner[insert_idx_inner] := swap_inner;
                        Dec(insert_idx_inner);
                    end;
                    if Length(states_inner) >
                        exact_edge_reserved_slots_local then
                    begin
                        SetLength(states_inner,
                            exact_edge_reserved_slots_local);
                    end;
                end;
            begin
                SetLength(states_inner, 0);
                for source_idx_inner := 0 to
                    High(propagated_states_inner) do
                begin
                    candidate_inner :=
                        propagated_states_inner[source_idx_inner];
                    if candidate_inner.retained_exact_edge_count > 0 then
                    begin
                        consider_side_state_inner(candidate_inner);
                    end;
                end;
                if Length(model_states_inner) <= 0 then
                begin
                    Exit;
                end;
                retained_edge_inner := Default(TncLongRetainedExactEdge);
                retained_edge_inner.position := position_inner;
                retained_edge_inner.chunk_units :=
                    get_candidate_text_unit_count(
                    model_states_inner[0].first_chunk_text);
                retained_edge_inner.exact_rank :=
                    model_states_inner[0].first_chunk_exact_rank;
                retained_edge_inner.raw_weight :=
                    model_states_inner[0].first_chunk_raw_weight;
                retained_edge_inner.top_weight :=
                    model_states_inner[0].first_chunk_top_weight;
                retained_edge_inner.confidence := Round(
                    model_states_inner[0].first_chunk_exact_edge_model_score *
                    100000.0) + Int64(retained_edge_inner.chunk_units) *
                    1000 - Int64(Max(0,
                    retained_edge_inner.exact_rank - 1)) * 100;
                retained_edge_inner.query_key :=
                    model_states_inner[0].first_chunk_query_key;
                retained_edge_inner.text :=
                    model_states_inner[0].first_chunk_text;
                if (retained_edge_inner.chunk_units <
                    c_chain_lattice_exact_edge_min_units) or
                    (model_states_inner[
                    0].first_chunk_exact_edge_model_score <
                    c_chain_lattice_exact_edge_min_model_score) then
                begin
                    Exit;
                end;
                for source_idx_inner := 0 to High(model_states_inner) do
                begin
                    candidate_inner := model_states_inner[source_idx_inner];
                    if (candidate_inner.first_chunk_exact_rank <= 0) or
                        candidate_inner.first_chunk_source_user or
                        (not SameText(candidate_inner.first_chunk_query_key,
                        retained_edge_inner.query_key)) or
                        (not SameText(Trim(candidate_inner.first_chunk_text),
                        Trim(retained_edge_inner.text))) then
                    begin
                        Continue;
                    end;
                    mark_state_with_retained_edge_local(candidate_inner,
                        retained_edge_inner);
                    consider_side_state_inner(candidate_inner);
                end;
                remember_retained_exact_edge_local(position_inner,
                    model_states_inner[0], retained_edge_inner.confidence);
            end;

            procedure append_retained_exact_edge_slots_local(
                var states_inner: TExactChunkChainStateArray;
                const side_states_inner: TExactChunkChainStateArray);
            var
                side_idx_inner: Integer;
                state_idx_inner: Integer;
                duplicate_inner: Boolean;
            begin
                for side_idx_inner := 0 to Min(
                    exact_edge_reserved_slots_local - 1,
                    High(side_states_inner)) do
                begin
                    duplicate_inner := False;
                    for state_idx_inner := 0 to High(states_inner) do
                    begin
                        if SameText(Trim(states_inner[state_idx_inner].text),
                            Trim(side_states_inner[side_idx_inner].text)) and
                            SameText(states_inner[state_idx_inner].path,
                            side_states_inner[side_idx_inner].path) then
                        begin
                            duplicate_inner := True;
                            Break;
                        end;
                    end;
                    if duplicate_inner then
                    begin
                        Continue;
                    end;
                    SetLength(states_inner, Length(states_inner) + 1);
                    states_inner[High(states_inner)] :=
                        side_states_inner[side_idx_inner];
                end;
            end;

            procedure sort_final_states_with_retained_slot_local(
                var states_inner: TExactChunkChainStateArray);
            var
                normal_states_inner: TExactChunkChainStateArray;
                side_states_inner: TExactChunkChainStateArray;
                state_idx_inner: Integer;
                normal_count_inner: Integer;
                side_count_inner: Integer;
            begin
                normal_count_inner := 0;
                side_count_inner := 0;
                SetLength(normal_states_inner, Length(states_inner));
                SetLength(side_states_inner, Length(states_inner));
                for state_idx_inner := 0 to High(states_inner) do
                begin
                    if states_inner[
                        state_idx_inner].retained_exact_edge_count > 0 then
                    begin
                        side_states_inner[side_count_inner] :=
                            states_inner[state_idx_inner];
                        Inc(side_count_inner);
                    end
                    else
                    begin
                        normal_states_inner[normal_count_inner] :=
                            states_inner[state_idx_inner];
                        Inc(normal_count_inner);
                    end;
                end;
                SetLength(normal_states_inner, normal_count_inner);
                SetLength(side_states_inner, side_count_inner);
                sort_fast_states_by_order_score_local(normal_states_inner);
                if side_count_inner > 0 then
                begin
                    sort_fast_states_by_order_score_local(side_states_inner);
                    side_count_inner := Min(side_count_inner,
                        exact_edge_reserved_slots_local);
                    SetLength(normal_states_inner, normal_count_inner +
                        side_count_inner);
                    for state_idx_inner := 0 to side_count_inner - 1 do
                    begin
                        normal_states_inner[normal_count_inner +
                            state_idx_inner] := side_states_inner[
                            state_idx_inner];
                    end;
                end;
                states_inner := normal_states_inner;
            end;

        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := 0;
            out_segments := 0;
            exact_edge_reserved_slots_local :=
                c_chain_lattice_exact_edge_reserved_slots;
            SetLength(out_states, 0);
            if (input_syllable_count < 6) or (input_syllable_count > 40) then
            begin
                Exit;
            end;
            if m_debug_target_recall_text <> '' then
            begin
                m_debug_target_chain_build_attempted := True;
            end;

            greedy_state_found_local := (input_syllable_count >= 6) and
                try_build_unambiguous_multi_exact_chain_local(greedy_state_local) and
                should_accept_greedy_exact_chain_state_local(greedy_state_local);
            if greedy_state_found_local then
            begin
                greedy_state_local.baseline_lineage := True;
            end;

            prefix_state_found_local := (input_syllable_count <= 9) and
                try_build_prefix_exact_plus_short_tail_local(prefix_state_local) and
                should_accept_greedy_exact_chain_state_local(prefix_state_local);
            if prefix_state_found_local then
            begin
                prefix_state_local.baseline_lineage := True;
            end;

            fast_seed_requires_dp_local :=
                (greedy_state_found_local and
                fast_seed_lacks_lm_support_local(greedy_state_local)) or
                (prefix_state_found_local and
                fast_seed_lacks_lm_support_local(prefix_state_local));
            if (not fast_seed_requires_dp_local) and
                (not (greedy_state_found_local and prefix_state_found_local and
                (not SameText(Trim(greedy_state_local.text),
                Trim(prefix_state_local.text))))) then
            begin
                if greedy_state_found_local then
                begin
                    out_text := greedy_state_local.text;
                    out_path := greedy_state_local.path;
                    out_score := greedy_state_local.score;
                    out_segments := greedy_state_local.segments;
                    SetLength(out_states, 1);
                    out_states[0] := greedy_state_local;
                    Exit(True);
                end;
                if prefix_state_found_local then
                begin
                    out_text := prefix_state_local.text;
                    out_path := prefix_state_local.path;
                    out_score := prefix_state_local.score;
                    out_segments := prefix_state_local.segments;
                    SetLength(out_states, 1);
                    out_states[0] := prefix_state_local;
                    Exit(True);
                end;
            end;

            SetLength(state_map_local, syllable_count_local + 1);
            SetLength(retained_state_map_local, syllable_count_local + 1);
            for pos_local := 0 to syllable_count_local do
            begin
                SetLength(state_map_local[pos_local], 0);
                SetLength(retained_state_map_local[pos_local], 0);
            end;
            base_state_local := Default(TExactChunkChainState);
            base_state_local.score := 0;
            base_state_local.segments := 0;
            base_state_local.anchor_units := -1;
            base_state_local.baseline_lineage := True;
            SetLength(state_map_local[syllable_count_local], 1);
            state_map_local[syllable_count_local][0] := base_state_local;

            for pos_local := syllable_count_local - 1 downto 0 do
            begin
                SetLength(baseline_candidates_local, 0);
                SetLength(edge_probe_candidates_local, 0);
                SetLength(exact_edge_model_candidates_local, 0);
                for chunk_len_local := 1 to Min(c_fast_max_chunk_syllables,
                    syllable_count_local - pos_local) do
                begin
                    next_pos_local := pos_local + chunk_len_local;
                    if Length(state_map_local[next_pos_local]) = 0 then
                    begin
                        Continue;
                    end;
                    if not try_collect_fast_options_local(pos_local,
                        chunk_len_local, options_local) then
                    begin
                        Continue;
                    end;

                    for option_idx_local := 0 to High(options_local) do
                    begin
                        suffix_count_local :=
                            Length(state_map_local[next_pos_local]);
                        if c_chain_lattice_exact_edge_side_lane_enabled then
                        begin
                            Inc(suffix_count_local,
                                Length(retained_state_map_local[
                                next_pos_local]));
                        end;
                        for suffix_idx_local := 0 to
                            suffix_count_local - 1 do
                        begin
                            if suffix_idx_local <
                                Length(state_map_local[next_pos_local]) then
                            begin
                                suffix_state_local :=
                                    state_map_local[next_pos_local][
                                    suffix_idx_local];
                            end
                            else
                            begin
                                suffix_state_local :=
                                    retained_state_map_local[next_pos_local][
                                    suffix_idx_local - Length(
                                    state_map_local[next_pos_local])];
                            end;
                            candidate_state_local := Default(TExactChunkChainState);
                            candidate_state_local.text :=
                                options_local[option_idx_local].text +
                                suffix_state_local.text;
                            candidate_state_local.path :=
                                options_local[option_idx_local].path;
                            if suffix_state_local.path <> '' then
                            begin
                                if candidate_state_local.path <> '' then
                                begin
                                    candidate_state_local.path :=
                                        candidate_state_local.path +
                                        c_segment_path_separator;
                                end;
                                candidate_state_local.path := candidate_state_local.path +
                                    suffix_state_local.path;
                            end;
                            candidate_state_local.first_chunk_text :=
                                options_local[option_idx_local].text;
                            candidate_state_local.first_chunk_query_key :=
                                options_local[option_idx_local].query_key;
                            candidate_state_local.first_chunk_exact_rank :=
                                options_local[option_idx_local].exact_rank;
                            candidate_state_local.first_chunk_raw_weight :=
                                options_local[option_idx_local].raw_weight;
                            candidate_state_local.first_chunk_top_weight :=
                                options_local[option_idx_local].top_weight;
                            candidate_state_local.first_chunk_effective_score :=
                                options_local[option_idx_local].score;
                            candidate_state_local.first_chunk_edge_score :=
                                options_local[option_idx_local].edge_retention_score;
                            candidate_state_local.first_chunk_exact_edge_model_score :=
                                options_local[
                                option_idx_local].exact_edge_model_score;
                            candidate_state_local.first_chunk_source_user :=
                                options_local[option_idx_local].source_user;
                            candidate_state_local.first_chunk_baseline_option :=
                                options_local[option_idx_local].baseline_option;
                            candidate_state_local.score :=
                                options_local[option_idx_local].score +
                                suffix_state_local.score;
                            Dec(candidate_state_local.score,
                                get_competing_boundary_penalty_fast_local(
                                candidate_state_local.text, pos_local,
                                chunk_len_local, options_local[option_idx_local],
                                suffix_state_local));
                            function_context_adjust_local := 0;
                            if suffix_state_local.first_chunk_text <> '' then
                            begin
                                Inc(candidate_state_local.score,
                                    m_dictionary.get_context_bonus(
                                    options_local[option_idx_local].text,
                                    suffix_state_local.first_chunk_text));
                                Inc(candidate_state_local.score,
                                    get_adjacent_exact_chunk_context_bonus_local(
                                    options_local[option_idx_local].text,
                                    suffix_state_local.first_chunk_text));
                                function_context_adjust_local :=
                                    get_function_particle_suffix_context_adjustment_local(
                                    effective_syllables[pos_local + chunk_len_local -
                                    1].text,
                                    options_local[option_idx_local].text,
                                    suffix_state_local.first_chunk_text);
                                Inc(candidate_state_local.score,
                                    function_context_adjust_local);
                                if starts_with_context_text_unit_local(
                                    suffix_state_local.text, $5730) or
                                    starts_with_context_text_unit_local(
                                    suffix_state_local.text, $5F97) or
                                    starts_with_context_text_unit_local(
                                    suffix_state_local.text, $4E3A) or
                                    starts_with_context_text_unit_local(
                                    suffix_state_local.text, $672A) then
                                begin
                                    prefix_context_adjust_local :=
                                        get_function_particle_prefix_context_adjustment_local(
                                        options_local[option_idx_local].text,
                                        suffix_state_local.text);
                                    if prefix_context_adjust_local <= -90000 then
                                    begin
                                        Continue;
                                    end;
                                    Inc(candidate_state_local.score,
                                        prefix_context_adjust_local);
                                end;
                            end;
                            if (chunk_len_local = 1) and
                                (suffix_state_local.first_chunk_text <> '') and
                                is_modifier_prefix_bridge_exempt_local(
                                options_local[option_idx_local].text) and
                                (get_candidate_text_unit_count(
                                suffix_state_local.first_chunk_text) >= 2) then
                            begin
                                Inc(candidate_state_local.score, 7600);
                            end;
                            if (chunk_len_local = 1) and
                                (next_pos_local < syllable_count_local) then
                            begin
                                suffix_first_units_local :=
                                    get_candidate_text_unit_count(
                                    suffix_state_local.first_chunk_text);
                                natural_modifier_phrase_boundary_local :=
                                    is_natural_modifier_phrase_boundary_fast_local(
                                    options_local[option_idx_local].text,
                                    next_pos_local, suffix_first_units_local,
                                    suffix_state_local.first_chunk_text, 400);
                                head_two_key_local := build_query_key_local(pos_local, 2);
                                if try_get_strong_fast_exact_phrase_text_local(
                                    head_two_key_local, 2, 700,
                                    head_two_text_local) then
                                begin
                                    current_pair_text_local :=
                                        options_local[option_idx_local].text +
                                        get_first_text_unit_fast_local(
                                        suffix_state_local.first_chunk_text);
                                    if (current_pair_text_local <> '') and
                                        (not SameText(current_pair_text_local,
                                        head_two_text_local)) then
                                    begin
                                        head_two_raw_local :=
                                            get_fast_exact_phrase_raw_weight_local(
                                            head_two_key_local, 2,
                                            head_two_text_local);
                                        suffix_first_raw_local := 0;
                                        if (suffix_first_units_local >= 2) and
                                            (suffix_state_local.first_chunk_query_key
                                            <> '') then
                                        begin
                                            suffix_first_raw_local :=
                                                get_fast_exact_phrase_raw_weight_local(
                                                suffix_state_local.first_chunk_query_key,
                                                suffix_first_units_local,
                                                suffix_state_local.first_chunk_text);
                                        end;
                                        if natural_modifier_phrase_boundary_local then
                                        begin
                                            { Natural function-single + phrase
                                              boundary, e.g. 鍏?鎻愪氦. }
                                        end
                                        else if suffix_first_raw_local >=
                                            head_two_raw_local + 700 then
                                        begin
                                            Dec(candidate_state_local.score, 12000);
                                        end
                                        else
                                        begin
                                            Dec(candidate_state_local.score, 72000);
                                        end;
                                    end;
                                end;
                                if (not natural_modifier_phrase_boundary_local) and
                                    has_high_quality_exact_phrase_with_prefix_for_key_local(
                                    head_two_key_local, 2, 240,
                                    options_local[option_idx_local].text) and
                                    (not is_strong_modifier_prefix_local(
                                    options_local[option_idx_local].text)) then
                                begin
                                    Dec(candidate_state_local.score, 36000);
                                end
                                else if (not natural_modifier_phrase_boundary_local) and
                                    has_high_quality_exact_phrase_for_key_local(
                                    head_two_key_local, 2, 240) and
                                    ((not is_modifier_prefix_bridge_exempt_local(
                                    options_local[option_idx_local].text)) or
                                    (not has_high_quality_exact_phrase_with_prefix_for_key_local(
                                    head_two_key_local, 2, 240,
                                    options_local[option_idx_local].text))) then
                                begin
                                    Dec(candidate_state_local.score, 30000);
                                end;
                            end;
                            candidate_state_local.segments :=
                                options_local[option_idx_local].segments +
                                suffix_state_local.segments;
                            candidate_state_local.has_anchor :=
                                options_local[option_idx_local].has_anchor or
                                suffix_state_local.has_anchor;
                            candidate_state_local.anchor_units :=
                                options_local[option_idx_local].anchor_units;
                            if suffix_state_local.anchor_units >
                                candidate_state_local.anchor_units then
                            begin
                                candidate_state_local.anchor_units :=
                                    suffix_state_local.anchor_units;
                            end;
                            candidate_state_local.retained_exact_edge_count :=
                                suffix_state_local.retained_exact_edge_count;
                            candidate_state_local.retained_exact_edge_position :=
                                suffix_state_local.retained_exact_edge_position;
                            candidate_state_local.retained_exact_edge_units :=
                                suffix_state_local.retained_exact_edge_units;
                            candidate_state_local.retained_exact_edge_rank :=
                                suffix_state_local.retained_exact_edge_rank;
                            candidate_state_local.retained_exact_edge_top_weight :=
                                suffix_state_local.retained_exact_edge_top_weight;
                            candidate_state_local.retained_exact_edge_confidence :=
                                suffix_state_local.retained_exact_edge_confidence;
                            candidate_state_local.baseline_lineage :=
                                suffix_state_local.baseline_lineage and
                                options_local[option_idx_local].baseline_option;
                            if pos_local = 0 then
                            begin
                                Inc(candidate_state_local.score,
                                    get_fast_full_path_adjustment_local(
                                    candidate_state_local.path));
                            end;
                            consider_generated_fast_state_local(
                                candidate_state_local);

                            if try_get_function_particle_suffix_head_alternative_local(
                                options_local[option_idx_local].text,
                                suffix_state_local.text,
                                suffix_state_local.path,
                                suffix_state_local.first_chunk_text,
                                suffix_state_local.first_chunk_query_key,
                                alternative_text_local,
                                alternative_suffix_path_local,
                                alternative_adjust_local) then
                            begin
                                alternative_state_local := candidate_state_local;
                                if pos_local = 0 then
                                begin
                                    Dec(alternative_state_local.score,
                                        get_fast_full_path_adjustment_local(
                                        alternative_state_local.path));
                                end;
                                alternative_state_local.text :=
                                    options_local[option_idx_local].text +
                                    alternative_text_local;
                                alternative_state_local.path :=
                                    options_local[option_idx_local].path;
                                if alternative_suffix_path_local <> '' then
                                begin
                                    if alternative_state_local.path <> '' then
                                    begin
                                        alternative_state_local.path :=
                                            alternative_state_local.path +
                                            c_segment_path_separator;
                                    end;
                                    alternative_state_local.path :=
                                        alternative_state_local.path +
                                        alternative_suffix_path_local;
                                end;
                                alternative_state_local.score :=
                                    alternative_state_local.score +
                                    alternative_adjust_local;
                                if pos_local = 0 then
                                begin
                                    Inc(alternative_state_local.score,
                                        get_fast_full_path_adjustment_local(
                                        alternative_state_local.path));
                                end;
                                consider_generated_fast_state_local(
                                    alternative_state_local);
                            end;

                            if (options_local[option_idx_local].segments = 1) and
                                (suffix_state_local.first_chunk_text <> '') and
                                try_get_function_particle_boundary_alternative_local(
                                effective_syllables[pos_local + chunk_len_local -
                                1].text,
                                options_local[option_idx_local].text,
                                suffix_state_local.first_chunk_text,
                                alternative_text_local, alternative_adjust_local) then
                            begin
                                alternative_state_local := candidate_state_local;
                                if pos_local = 0 then
                                begin
                                    Dec(alternative_state_local.score,
                                        get_fast_full_path_adjustment_local(
                                        alternative_state_local.path));
                                end;
                                alternative_score_local :=
                                    alternative_state_local.score -
                                    function_context_adjust_local +
                                    alternative_adjust_local;
                                alternative_state_local.text :=
                                    alternative_text_local +
                                    suffix_state_local.text;
                                alternative_state_local.path := alternative_text_local;
                                if suffix_state_local.path <> '' then
                                begin
                                    alternative_state_local.path :=
                                        alternative_state_local.path +
                                        c_segment_path_separator +
                                        suffix_state_local.path;
                                end;
                                alternative_state_local.first_chunk_text :=
                                    alternative_text_local;
                                alternative_state_local.score :=
                                    alternative_score_local;
                                if pos_local = 0 then
                                begin
                                    Inc(alternative_state_local.score,
                                        get_fast_full_path_adjustment_local(
                                        alternative_state_local.path));
                                end;
                                consider_generated_fast_state_local(
                                    alternative_state_local);
                            end;
                        end;
                    end;
                    if (not m_debug_expand_long_beam_states) and
                        (exact_edge_reserved_slots_local > 0) then
                    begin
                        runtime_exact_edge_probe_collection_local := True;
                        try
                            if try_collect_fast_options_local(pos_local,
                                chunk_len_local, probe_options_local) then
                            begin
                                collect_runtime_exact_edge_probe_states_local(
                                    pos_local, chunk_len_local,
                                    next_pos_local, probe_options_local);
                            end;
                        finally
                            runtime_exact_edge_probe_collection_local := False;
                        end;
                    end;
                end;
                legacy_candidates_local := Copy(baseline_candidates_local,
                    0, Length(baseline_candidates_local));
                prune_baseline_fast_states_by_char_lm_local(
                    baseline_candidates_local, pos_local);
                if m_debug_expand_long_beam_states then
                begin
                    prune_fast_states_by_char_lm_local(
                        state_map_local[pos_local], pos_local,
                        baseline_candidates_local, True);
                    merge_baseline_and_expanded_fast_states_local(
                        state_map_local[pos_local],
                        baseline_candidates_local);
                end
                else if (exact_edge_reserved_slots_local > 0) and
                    ((Length(edge_probe_candidates_local) > 0) or
                    (Length(exact_edge_model_candidates_local) > 0)) then
                begin
                    choose_retained_exact_edge_slots_local(pos_local,
                        edge_probe_candidates_local,
                        exact_edge_model_candidates_local,
                        retained_slot_states_local);
                    prune_fast_states_by_char_lm_local(
                        legacy_candidates_local, pos_local,
                        baseline_candidates_local, False);
                    state_map_local[pos_local] :=
                        legacy_candidates_local;
                    merge_baseline_and_expanded_fast_states_local(
                        state_map_local[pos_local],
                        baseline_candidates_local);
                    if c_chain_lattice_exact_edge_side_lane_enabled and
                        (Length(retained_slot_states_local) > 0) then
                    begin
                        retained_state_map_local[pos_local] :=
                            Copy(retained_slot_states_local, 0,
                            Length(retained_slot_states_local));
                    end;
                end
                else
                begin
                    prune_fast_states_by_char_lm_local(
                        state_map_local[pos_local], pos_local,
                        baseline_candidates_local, False);
                    merge_baseline_and_expanded_fast_states_local(
                        state_map_local[pos_local],
                        baseline_candidates_local);
                end;
            end;

            if greedy_state_found_local then
            begin
                greedy_state_present_local := False;
                for debug_state_idx_local := 0 to High(state_map_local[0]) do
                begin
                    if SameText(Trim(state_map_local[0][debug_state_idx_local].text),
                        Trim(greedy_state_local.text)) then
                    begin
                        greedy_state_present_local := True;
                        Break;
                    end;
                end;
                if not greedy_state_present_local then
                begin
                    SetLength(state_map_local[0], Length(state_map_local[0]) + 1);
                    state_map_local[0][High(state_map_local[0])] :=
                        greedy_state_local;
                end;
            end;
            if prefix_state_found_local then
            begin
                prefix_state_present_local := False;
                for debug_state_idx_local := 0 to High(state_map_local[0]) do
                begin
                    if SameText(Trim(state_map_local[0][debug_state_idx_local].text),
                        Trim(prefix_state_local.text)) then
                    begin
                        prefix_state_present_local := True;
                        Break;
                    end;
                end;
                if not prefix_state_present_local then
                begin
                    SetLength(state_map_local[0], Length(state_map_local[0]) + 1);
                    state_map_local[0][High(state_map_local[0])] :=
                        prefix_state_local;
                end;
            end;

            if c_chain_lattice_exact_edge_side_lane_enabled and
                (Length(retained_state_map_local[0]) > 0) then
            begin
                append_retained_exact_edge_slots_local(state_map_local[0],
                    retained_state_map_local[0]);
            end;
            sort_final_states_with_retained_slot_local(state_map_local[0]);
            capture_final_chain_candidates_local(state_map_local[0]);

            baseline_acceptance_score_local := Low(Integer);
            baseline_acceptance_found_local := False;
            for debug_state_idx_local := 0 to High(state_map_local[0]) do
            begin
                if state_map_local[0][debug_state_idx_local].baseline_lineage then
                begin
                    baseline_acceptance_score_local := Max(
                        baseline_acceptance_score_local,
                        state_map_local[0][debug_state_idx_local].score);
                    baseline_acceptance_found_local := True;
                end;
            end;

            if m_debug_target_recall_text <> '' then
            begin
                m_debug_target_final_state_count := Max(
                    m_debug_target_final_state_count,
                    Length(state_map_local[0]));
                for debug_state_idx_local := 0 to High(state_map_local[0]) do
                begin
                    if SameText(Trim(state_map_local[0][debug_state_idx_local].text),
                        m_debug_target_recall_text) then
                    begin
                        m_debug_target_final_state_source_mask :=
                            m_debug_target_final_state_source_mask or 1;
                        if (m_debug_target_chain_final_state_rank = 0) or
                            (debug_state_idx_local + 1 <
                            m_debug_target_chain_final_state_rank) then
                        begin
                            m_debug_target_chain_final_state_rank :=
                                debug_state_idx_local + 1;
                        end;
                        if (not m_debug_target_final_state_found) or
                            (debug_state_idx_local + 1 <
                            m_debug_target_final_state_rank) then
                        begin
                            m_debug_target_final_state_found := True;
                            m_debug_target_final_state_rank :=
                                debug_state_idx_local + 1;
                        end;
                    end;
                end;
            end;

            if (Length(state_map_local[0]) = 0) or
                (state_map_local[0][0].text = '') or
                (get_candidate_text_unit_count(state_map_local[0][0].text) <>
                input_syllable_count) then
            begin
                if m_config.debug_mode then
                begin
                    state_debug_text_local := '';
                    for pos_local := 0 to syllable_count_local do
                    begin
                        if pos_local >= 8 then
                        begin
                            state_debug_text_local := state_debug_text_local +
                                ' ...';
                            Break;
                        end;
                        if Length(state_map_local[pos_local]) > 0 then
                        begin
                            state_debug_text_local := state_debug_text_local +
                                Format(' %d:%s/%d', [pos_local,
                                state_map_local[pos_local][0].text,
                                state_map_local[pos_local][0].score]);
                        end;
                    end;
                    if Length(state_map_local[0]) > 0 then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' xfastdp_miss=[score=%d text=%s first=%s fixed=%s]',
                            [state_map_local[0][0].score,
                            state_map_local[0][0].text,
                            effective_syllables[0].text,
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[0].text)]) +
                            state_debug_text_local;
                    end
                    else
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' xfastdp_miss=[score=%d text=%s first=%s fixed=%s]',
                            [Low(Integer), '', effective_syllables[0].text,
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[0].text)]) +
                            state_debug_text_local;
                    end;
                end;
                Exit;
            end;

            out_states := Copy(state_map_local[0]);
            out_text := state_map_local[0][0].text;
            out_path := state_map_local[0][0].path;
            out_score := state_map_local[0][0].score;
            out_segments := state_map_local[0][0].segments;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xfastdp_probe=[text=%s score=%d seg=%d]',
                    [out_text, out_score, out_segments]);
            end;
            if input_syllable_count >= 12 then
            begin
                minimum_score_local := input_syllable_count * 4600;
            end
            else if input_syllable_count >= 9 then
            begin
                minimum_score_local := input_syllable_count * 4900;
            end
            else
            begin
                minimum_score_local := input_syllable_count * 5200;
            end;
            { Expanded states may be promoted in the returned candidate list,
              but they must not suppress the legacy fallback search. Keep the
              old six-state beam as the acceptance authority. }
            Result := baseline_acceptance_found_local and
                (baseline_acceptance_score_local >= minimum_score_local);
            if m_debug_target_recall_text <> '' then
            begin
                m_debug_target_chain_build_accepted := Result;
                m_debug_target_chain_top_score := baseline_acceptance_score_local;
                m_debug_target_chain_min_score := minimum_score_local;
            end;
        end;

        function try_repair_state_with_covering_exact_pairs_local(
            const source_state: TExactChunkChainState;
            out repaired_state: TExactChunkChainState;
            const incremental_tail_only: Boolean = False): Boolean;
        const
            c_min_covering_pair_exact_weight = 240;
            c_min_single_chain_triple_exact_weight = 180;
            c_repair_bonus = 9000;
            c_same_span_exact_min_gap = 300;
            c_same_span_exact_min_target_weight = 700;
            c_same_span_exact_probe_limit = c_chain_probe_limit * 4;
        var
            path_segments_local: TArray<string>;
            segment_idx_local: Integer;
            query_offset_local: Integer;
            current_text_local: string;
            current_units_local: Integer;
            next_text_local: string;
            next_units_local: Integer;
            third_text_local: string;
            third_units_local: Integer;
            fourth_text_local: string;
            fourth_units_local: Integer;
            combined_key_local: string;
            combined_text_local: string;
            best_exact_text_local: string;
            best_exact_score_local: Integer;
            repair_count_local: Integer;
            repair_probe_budget_local: Integer;
            repaired_text_local: string;
            repaired_path_local: string;
            chained_repaired_text_local: string;
            chained_repaired_path_local: string;
            repair_used_prefix_tail_shift_local: Boolean;
            repair_used_boundary_shift_local: Boolean;

            procedure append_repaired_segment_local(const segment_text_local: string);
            begin
                if segment_text_local = '' then
                begin
                    Exit;
                end;
                repaired_text_local := repaired_text_local + segment_text_local;
                if repaired_path_local <> '' then
                begin
                    repaired_path_local := repaired_path_local +
                        c_segment_path_separator;
                end;
                repaired_path_local := repaired_path_local + segment_text_local;
            end;

            function try_get_same_span_better_exact_segment_local(
                const segment_query_offset_local: Integer;
                const segment_units_local: Integer;
                const current_segment_text_local: string;
                out out_segment_text_local: string;
                out out_segment_score_local: Integer): Boolean;
            const
                c_same_span_directional_context_bonus = 12000;
                c_same_span_directional_min_weight = 300;
                c_same_span_context_margin = 120;
                c_same_span_unsupported_min_target_weight = 520;
                c_same_span_reliable_source_min_weight = 240;
            var
                query_key_local: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_text_local: string;
                candidate_raw_weight_local: Integer;
                candidate_score_local: Integer;
                candidate_adjusted_weight_local: Integer;
                current_raw_weight_local: Integer;
                current_adjusted_weight_local: Integer;
                best_raw_weight_local: Integer;
                best_adjusted_weight_local: Integer;
                best_score_local: Integer;
                best_text_local: string;
                has_directional_context_local: Boolean;

                function is_place_noun_unit_for_direction_context_local(
                    const unit_value: string): Boolean;
                begin
                    Result := (unit_value = string(Char($5BB6))) or
                        (unit_value = string(Char($5E97))) or
                        (unit_value = string(Char($5C4B))) or
                        (unit_value = string(Char($697C))) or
                        (unit_value = string(Char($57CE))) or
                        (unit_value = string(Char($6751))) or
                        (unit_value = string(Char($53BF))) or
                        (unit_value = string(Char($5E02))) or
                        (unit_value = string(Char($6821))) or
                        (unit_value = string(Char($9662))) or
                        (unit_value = string(Char($5382))) or
                        (unit_value = string(Char($9986))) or
                        (unit_value = string(Char($7AD9))) or
                        (unit_value = string(Char($5C40))) or
                        (unit_value = string(Char($6240))) or
                        (unit_value = string(Char($56ED))) or
                        (unit_value = string(Char($533A))) or
                        (unit_value = string(Char($5708))) or
                        (unit_value = string(Char($8DEF))) or
                        (unit_value = string(Char($8857))) or
                        (unit_value = string(Char($95E8))) or
                        (unit_value = string(Char($5BA4))) or
                        (unit_value = string(Char($623F))) or
                        (unit_value = string(Char($5385))) or
                        (unit_value = string(Char($573A))) or
                        (unit_value = string(Char($53F8)));
                end;

                function has_place_context_before_same_span_local: Boolean;
                const
                    c_context_probe_limit = 8;
                var
                    back_len_local: Integer;
                    context_key_local: string;
                    context_results_local: TncCandidateList;
                    result_idx_local: Integer;
                    context_text_local: string;
                    context_units_local: TArray<string>;
                begin
                    Result := False;
                    if segment_query_offset_local <= 0 then
                    begin
                        Exit;
                    end;

                    for back_len_local := 1 to Min(4,
                        segment_query_offset_local) do
                    begin
                        context_key_local := build_query_key_local(
                            segment_query_offset_local - back_len_local,
                            back_len_local);
                        if (context_key_local = '') or
                            (not lookup_exact_full_pinyin_cached_local(
                            context_key_local, context_results_local)) then
                        begin
                            Continue;
                        end;

                        for result_idx_local := 0 to High(context_results_local) do
                        begin
                            if result_idx_local >= c_context_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(context_results_local[result_idx_local].comment) <>
                                '' then
                            begin
                                Continue;
                            end;

                            context_text_local := Trim(
                                context_results_local[result_idx_local].text);
                            context_units_local := split_text_units(
                                context_text_local);
                            if (Length(context_units_local) > 0) and
                                is_place_noun_unit_for_direction_context_local(
                                context_units_local[High(context_units_local)]) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                function has_directional_same_span_context_local: Boolean;
                var
                    next_key_local: string;
                begin
                    Result := False;
                    if (segment_units_local <> 2) or
                        (not has_place_context_before_same_span_local) then
                    begin
                        Exit;
                    end;

                    if segment_query_offset_local + segment_units_local >=
                        syllable_count_local then
                    begin
                        Exit(True);
                    end;

                    next_key_local := normalize_pinyin_text(
                        effective_syllables[segment_query_offset_local +
                        segment_units_local].text);
                    Result := SameText(next_key_local, 'de');
                end;

                function is_directional_locative_text_for_same_span_local(
                    const text_value_local: string): Boolean;
                begin
                    Result := get_directional_locative_span_bonus_local(
                        text_value_local) > 0;
                end;

                function has_function_particle_before_same_span_local: Boolean;
                var
                    source_units_local: TArray<string>;
                    previous_unit_local: string;
                begin
                    Result := False;
                    source_units_local := split_text_units(source_state.text);
                    if (segment_query_offset_local <= 0) or
                        (segment_query_offset_local > High(source_units_local)) then
                    begin
                        Exit;
                    end;

                    previous_unit_local :=
                        source_units_local[segment_query_offset_local - 1];
                    Result := SameText(previous_unit_local, string(Char($7684))) or
                        SameText(previous_unit_local, string(Char($5730))) or
                        SameText(previous_unit_local, string(Char($5F97))) or
                        SameText(previous_unit_local, string(Char($4E4B)));
                end;

                function is_temporal_process_same_span_text_local(
                    const text_value_local: string): Boolean;
                begin
                    Result :=
                        SameText(text_value_local, string(Char($65F6)) +
                        string(Char($95F4))) or
                        SameText(text_value_local, string(Char($65F6)) +
                        string(Char($5019))) or
                        SameText(text_value_local, string(Char($671F)) +
                        string(Char($95F4))) or
                        SameText(text_value_local, string(Char($8FC7)) +
                        string(Char($7A0B))) or
                        SameText(text_value_local, string(Char($9636)) +
                        string(Char($6BB5))) or
                        SameText(text_value_local, string(Char($65F6)) +
                        string(Char($523B))) or
                        SameText(text_value_local, string(Char($77AC)) +
                        string(Char($95F4))) or
                        SameText(text_value_local, string(Char($65E9)) +
                        string(Char($6668))) or
                        SameText(text_value_local, string(Char($591C)) +
                        string(Char($665A))) or
                        SameText(text_value_local, string(Char($4E0A)) +
                        string(Char($5348))) or
                        SameText(text_value_local, string(Char($4E0B)) +
                        string(Char($5348))) or
                        SameText(text_value_local, string(Char($4E2D)) +
                        string(Char($5348))) or
                        SameText(text_value_local, string(Char($5E74)) +
                        string(Char($4EE3))) or
                        SameText(text_value_local, string(Char($65F6)) +
                        string(Char($4EE3)));
                end;

                function get_same_span_adjusted_weight_local(
                    const text_value_local: string;
                    const raw_weight_value_local: Integer): Integer;
                begin
                    Result := raw_weight_value_local;
                    if has_directional_context_local and
                        is_directional_locative_text_for_same_span_local(
                        text_value_local) then
                    begin
                        Inc(Result, c_same_span_directional_context_bonus);
                    end;
                    if (segment_query_offset_local > 0) and
                        is_temporal_process_same_span_text_local(
                        text_value_local) and
                        has_function_particle_before_same_span_local then
                    begin
                        Inc(Result, 4200);
                    end;
                end;
            begin
                Result := False;
                out_segment_text_local := '';
                out_segment_score_local := 0;
                if (segment_query_offset_local < 0) or
                    (segment_units_local < 2) or (segment_units_local > 4) or
                    (current_segment_text_local = '') or
                    (segment_query_offset_local + segment_units_local >
                    syllable_count_local) then
                begin
                    Exit;
                end;

                query_key_local := build_query_key_local(
                    segment_query_offset_local, segment_units_local);
                if (query_key_local = '') or
                    (not lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results)) then
                begin
                    Exit;
                end;

                current_raw_weight_local := 0;
                current_adjusted_weight_local := 0;
                best_raw_weight_local := 0;
                best_adjusted_weight_local := 0;
                best_score_local := Low(Integer);
                best_text_local := '';
                has_directional_context_local :=
                    has_directional_same_span_context_local;
                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_same_span_exact_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        segment_units_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, segment_units_local)) then
                    begin
                        Continue;
                    end;

                    candidate_raw_weight_local := get_exact_chunk_raw_weight_local(
                        local_results[local_idx]);
                    if SameText(candidate_text_local, current_segment_text_local) then
                    begin
                        if candidate_raw_weight_local > current_raw_weight_local then
                        begin
                            current_raw_weight_local := candidate_raw_weight_local;
                            current_adjusted_weight_local :=
                                get_same_span_adjusted_weight_local(
                                candidate_text_local, candidate_raw_weight_local);
                        end;
                        Continue;
                    end;

                    if has_particle_tail_mismatch_local(query_key_local,
                        candidate_text_local, candidate_raw_weight_local) or
                        (not is_reliable_fast_exact_chunk_local(query_key_local,
                        segment_units_local, candidate_text_local,
                        local_results[local_idx])) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := get_exact_chunk_effective_weight_local(
                        local_results[local_idx]);
                    candidate_adjusted_weight_local :=
                        get_same_span_adjusted_weight_local(candidate_text_local,
                        candidate_raw_weight_local);
                    if has_directional_context_local and
                        is_directional_locative_text_for_same_span_local(
                        candidate_text_local) then
                    begin
                        Inc(candidate_score_local,
                            c_same_span_directional_context_bonus);
                    end;
                    if (candidate_adjusted_weight_local >
                        best_adjusted_weight_local) or
                        ((candidate_adjusted_weight_local =
                        best_adjusted_weight_local) and
                        (candidate_raw_weight_local > best_raw_weight_local)) or
                        ((candidate_adjusted_weight_local =
                        best_adjusted_weight_local) and
                        (candidate_raw_weight_local = best_raw_weight_local) and
                        (candidate_score_local > best_score_local)) then
                    begin
                        best_raw_weight_local := candidate_raw_weight_local;
                        best_adjusted_weight_local :=
                            candidate_adjusted_weight_local;
                        best_score_local := candidate_score_local;
                        best_text_local := candidate_text_local;
                    end;
                end;

                if best_text_local = '' then
                begin
                    Exit;
                end;

                if current_raw_weight_local <= 0 then
                begin
                    if best_raw_weight_local <
                        c_same_span_unsupported_min_target_weight then
                    begin
                        Exit;
                    end;

                    out_segment_text_local := best_text_local;
                    out_segment_score_local := best_score_local;
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' xsamespan_unsupported=[%s>%s raw=%d]',
                            [current_segment_text_local, best_text_local,
                            best_raw_weight_local]);
                    end;
                    Exit(True);
                end;

                if (not has_directional_context_local) and
                    (best_adjusted_weight_local = best_raw_weight_local) and
                    (current_adjusted_weight_local = current_raw_weight_local) then
                begin
                    Exit;
                end;

                if has_directional_context_local then
                begin
                    if (best_raw_weight_local < c_same_span_directional_min_weight) or
                        (best_adjusted_weight_local <
                        current_adjusted_weight_local +
                        c_same_span_context_margin) then
                    begin
                        Exit;
                    end;
                end
                else if (best_adjusted_weight_local <> best_raw_weight_local) or
                    (current_adjusted_weight_local <> current_raw_weight_local) then
                begin
                    if (best_raw_weight_local < c_same_span_directional_min_weight) or
                        (best_adjusted_weight_local <
                        current_adjusted_weight_local +
                        c_same_span_context_margin) then
                    begin
                        Exit;
                    end;
                end
                else
                begin
                    Exit;
                end;

                out_segment_text_local := best_text_local;
                out_segment_score_local := best_score_local;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xsamespan=[%s>%s raw=%d/%d]',
                        [current_segment_text_local, best_text_local,
                        current_raw_weight_local, best_raw_weight_local]);
                    if has_directional_context_local then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' adj=%d/%d',
                            [current_adjusted_weight_local,
                            best_adjusted_weight_local]);
                    end;
                end;
                Result := True;
            end;

            function try_repair_same_span_exact_segments_local(
                const source_path_local: string;
                out out_text_local: string; out out_path_local: string;
                out out_score_bonus_local: Integer;
                out out_repair_count_local: Integer): Boolean;
            var
                segments_local: TArray<string>;
                segment_idx_local: Integer;
                segment_text_local: string;
                segment_units_local: Integer;
                query_offset_inner: Integer;
                replacement_text_local: string;
                replacement_score_local: Integer;
                local_text_accum: string;
                local_path_accum: string;

                procedure append_same_span_segment_local(
                    const segment_value_local: string);
                begin
                    if segment_value_local = '' then
                    begin
                        Exit;
                    end;
                    local_text_accum := local_text_accum + segment_value_local;
                    if local_path_accum <> '' then
                    begin
                        local_path_accum := local_path_accum +
                            c_segment_path_separator;
                    end;
                    local_path_accum := local_path_accum + segment_value_local;
                end;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                out_score_bonus_local := 0;
                out_repair_count_local := 0;
                local_text_accum := '';
                local_path_accum := '';
                if source_path_local = '' then
                begin
                    Exit;
                end;

                segments_local := source_path_local.Split([c_segment_path_separator]);
                if Length(segments_local) = 0 then
                begin
                    Exit;
                end;

                query_offset_inner := 0;
                for segment_idx_local := 0 to High(segments_local) do
                begin
                    segment_text_local := Trim(segments_local[segment_idx_local]);
                    segment_units_local := get_candidate_text_unit_count(
                        segment_text_local);
                    if (segment_text_local = '') or (segment_units_local <= 0) or
                        (query_offset_inner + segment_units_local >
                        syllable_count_local) then
                    begin
                        Exit(False);
                    end;

                    if try_get_same_span_better_exact_segment_local(
                        query_offset_inner, segment_units_local, segment_text_local,
                        replacement_text_local, replacement_score_local) then
                    begin
                        append_same_span_segment_local(replacement_text_local);
                        Inc(out_score_bonus_local, Max(0, replacement_score_local));
                        Inc(out_repair_count_local);
                    end
                    else
                    begin
                        append_same_span_segment_local(segment_text_local);
                    end;

                    Inc(query_offset_inner, segment_units_local);
                end;

                if (query_offset_inner <> syllable_count_local) or
                    (out_repair_count_local <= 0) or
                    SameText(local_text_accum, source_state.text) then
                begin
                    Exit(False);
                end;

                out_text_local := local_text_accum;
                out_path_local := local_path_accum;
                Result := True;
            end;

            function repaired_text_rewrites_protected_source_exact_span_local: Boolean;
            const
                c_min_protected_exact_span_weight = 240;
                c_max_protected_exact_span_units = 6;
            var
                source_units_local: TArray<string>;
                repaired_units_local: TArray<string>;
                start_idx_local: Integer;
                span_len_local: Integer;
                unit_idx_local: Integer;
                query_key_local: string;
                source_span_text_local: string;
                repaired_span_text_local: string;
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_weight_local: Integer;

                function build_unit_span_text_local(const units_local: TArray<string>;
                    const start_local: Integer; const len_local: Integer): string;
                var
                    span_unit_idx_local: Integer;
                begin
                    Result := '';
                    if (start_local < 0) or (len_local <= 0) or
                        (start_local + len_local > Length(units_local)) then
                    begin
                        Exit;
                    end;

                    for span_unit_idx_local := start_local to start_local + len_local - 1 do
                    begin
                        Result := Result + units_local[span_unit_idx_local];
                    end;
                end;

                function get_exact_span_weight_for_text_local(
                    const span_start_local: Integer; const span_len_value_local: Integer;
                    const span_text_value_local: string): Integer;
                var
                    span_key_local: string;
                    span_results_local: TncCandidateList;
                    span_idx_local: Integer;
                    span_weight_local: Integer;
                begin
                    Result := 0;
                    if (span_text_value_local = '') or
                        (span_start_local < 0) or (span_len_value_local <= 0) or
                        (span_start_local + span_len_value_local >
                        syllable_count_local) then
                    begin
                        Exit;
                    end;

                    span_key_local := build_query_key_local(span_start_local,
                        span_len_value_local);
                    if (span_key_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(span_key_local,
                        span_results_local)) then
                    begin
                        Exit;
                    end;

                    for span_idx_local := 0 to High(span_results_local) do
                    begin
                        if Trim(span_results_local[span_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not SameText(Trim(span_results_local[span_idx_local].text),
                            span_text_value_local) then
                        begin
                            Continue;
                        end;

                        span_weight_local := get_exact_chunk_raw_weight_local(
                            span_results_local[span_idx_local]);
                        if span_weight_local > Result then
                        begin
                            Result := span_weight_local;
                        end;
                    end;
                end;

                function repaired_span_has_comparable_exact_support_local(
                    const protected_start_local: Integer;
                    const protected_len_local: Integer;
                    const protected_weight_local: Integer): Boolean;
                const
                    c_covering_exact_weight_slack = 120;
                    c_covering_longer_exact_weight_slack = 360;
                    c_covering_longer_exact_override_min_weight = 260;
                    c_shifted_tail_exact_weight_slack = 160;
                    c_shifted_tail_strong_weight = 900;
                    c_shifted_tail_reliable_weight = 520;
                var
                    repaired_same_span_text_local: string;
                    cover_start_local: Integer;
                    cover_len_local: Integer;
                    cover_end_local: Integer;
                    protected_end_local: Integer;
                    repaired_cover_text_local: string;
                    covering_weight_local: Integer;
                    shifted_tail_text_local: string;
                    shifted_tail_weight_local: Integer;
                    shifted_head_text_local: string;
                    shifted_head_weight_local: Integer;
                    shifted_head_tail_text_local: string;
                    shifted_head_tail_fixed_local: string;
                    expected_shift_prefix_text_local: string;
                    bridge_left_text_local: string;
                    bridge_left_weight_local: Integer;
                    covered_unit_idx_local: Integer;
                    covered_unit_weight_local: Integer;
                    covered_unit_total_weight_local: Integer;
                    covered_unit_min_weight_local: Integer;

                    function is_shift_prefix_unit_local(
                        const unit_value_local: string): Boolean;
                    begin
                        Result :=
                            SameText(unit_value_local, string(Char($7684))) or
                            SameText(unit_value_local, string(Char($5730))) or
                            SameText(unit_value_local, string(Char($5F97))) or
                            SameText(unit_value_local, string(Char($5BF9))) or
                            SameText(unit_value_local, string(Char($548C))) or
                            SameText(unit_value_local, string(Char($4E0E))) or
                            SameText(unit_value_local, string(Char($50CF)));
                    end;

                    function get_best_repaired_exact_covering_unit_weight_local(
                        const unit_idx_value_local: Integer): Integer;
                    var
                        local_cover_start: Integer;
                        local_cover_len: Integer;
                        local_cover_end: Integer;
                        local_cover_text: string;
                        local_cover_weight: Integer;
                    begin
                        Result := 0;
                        if (unit_idx_value_local < 0) or
                            (unit_idx_value_local >= syllable_count_local) then
                        begin
                            Exit;
                        end;

                        for local_cover_start := Max(0,
                            unit_idx_value_local -
                            c_max_protected_exact_span_units + 1) to
                            unit_idx_value_local do
                        begin
                            for local_cover_len := 2 to
                                Min(c_max_protected_exact_span_units,
                                syllable_count_local - local_cover_start) do
                            begin
                                local_cover_end := local_cover_start +
                                    local_cover_len;
                                if (local_cover_end <= unit_idx_value_local) or
                                    (local_cover_start > unit_idx_value_local) then
                                begin
                                    Continue;
                                end;

                                local_cover_text := build_unit_span_text_local(
                                    repaired_units_local, local_cover_start,
                                    local_cover_len);
                                local_cover_weight :=
                                    get_exact_span_weight_for_text_local(
                                    local_cover_start, local_cover_len,
                                    local_cover_text);
                                if local_cover_weight > Result then
                                begin
                                    Result := local_cover_weight;
                                end;
                            end;
                        end;
                    end;
                begin
                    Result := False;
                    repaired_same_span_text_local := build_unit_span_text_local(
                        repaired_units_local, protected_start_local,
                        protected_len_local);

                    protected_end_local := protected_start_local +
                        protected_len_local;
                    for cover_start_local := Max(0,
                        protected_start_local - c_max_protected_exact_span_units +
                        protected_len_local) to protected_start_local do
                    begin
                        for cover_len_local := protected_len_local + 1 to
                            Min(c_max_protected_exact_span_units,
                            syllable_count_local - cover_start_local) do
                        begin
                            cover_end_local := cover_start_local + cover_len_local;
                            if (cover_start_local > protected_start_local) or
                                (cover_end_local < protected_end_local) then
                            begin
                                Continue;
                            end;

                            repaired_cover_text_local := build_unit_span_text_local(
                                repaired_units_local, cover_start_local,
                                cover_len_local);
                            covering_weight_local :=
                                get_exact_span_weight_for_text_local(
                                cover_start_local, cover_len_local,
                                repaired_cover_text_local);
                            if (cover_len_local > protected_len_local) and
                                (covering_weight_local >=
                                c_covering_longer_exact_override_min_weight) then
                            begin
                                Exit(True);
                            end;
                            if (covering_weight_local > 0) and
                                (((cover_len_local > protected_len_local) and
                                (covering_weight_local +
                                c_covering_longer_exact_weight_slack >=
                                protected_weight_local)) or
                                ((cover_len_local <= protected_len_local) and
                                (covering_weight_local +
                                c_covering_exact_weight_slack >=
                                protected_weight_local))) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;

                    covered_unit_total_weight_local := 0;
                    covered_unit_min_weight_local := MaxInt;
                    for covered_unit_idx_local := protected_start_local to
                        protected_end_local - 1 do
                    begin
                        covered_unit_weight_local :=
                            get_best_repaired_exact_covering_unit_weight_local(
                            covered_unit_idx_local);
                        if covered_unit_weight_local <
                            c_covering_longer_exact_override_min_weight then
                        begin
                            covered_unit_total_weight_local := 0;
                            Break;
                        end;
                        Inc(covered_unit_total_weight_local,
                            covered_unit_weight_local);
                        covered_unit_min_weight_local := Min(
                            covered_unit_min_weight_local,
                            covered_unit_weight_local);
                    end;
                    if (covered_unit_total_weight_local > 0) and
                        (covered_unit_min_weight_local >=
                        c_covering_longer_exact_override_min_weight) and
                        ((covered_unit_total_weight_local div
                        Max(1, protected_len_local) +
                        c_covering_longer_exact_weight_slack >=
                        protected_weight_local) or
                        (covered_unit_total_weight_local >=
                        protected_weight_local)) then
                    begin
                        Exit(True);
                    end;

                    if (protected_len_local = 2) and
                        (protected_start_local > 0) and
                        (protected_start_local + 2 < syllable_count_local) then
                    begin
                        bridge_left_text_local := build_unit_span_text_local(
                            repaired_units_local, protected_start_local - 1, 2);
                        bridge_left_weight_local :=
                            get_exact_span_weight_for_text_local(
                            protected_start_local - 1, 2,
                            bridge_left_text_local);
                        shifted_tail_text_local := build_unit_span_text_local(
                            repaired_units_local, protected_start_local + 1, 2);
                        shifted_tail_weight_local :=
                            get_exact_span_weight_for_text_local(
                            protected_start_local + 1, 2,
                            shifted_tail_text_local);
                        if (bridge_left_weight_local >= 450) and
                            (shifted_tail_weight_local >=
                            c_shifted_tail_reliable_weight) and
                            ((shifted_tail_weight_local >=
                            c_shifted_tail_strong_weight) or
                            (bridge_left_weight_local + shifted_tail_weight_local +
                            c_covering_longer_exact_weight_slack >=
                            protected_weight_local)) then
                        begin
                            Exit(True);
                        end;
                    end;

                    if (protected_len_local >= 2) and
                        (protected_start_local + protected_len_local <
                        syllable_count_local) and
                        ((try_get_modifier_prefix_single_text_local(
                        effective_syllables[protected_start_local].text,
                        expected_shift_prefix_text_local) and
                        SameText(repaired_units_local[protected_start_local],
                        expected_shift_prefix_text_local)) or
                        is_shift_prefix_unit_local(
                        repaired_units_local[protected_start_local]) or
                        SameText(repaired_units_local[protected_start_local],
                        get_fixed_boundary_single_char_for_standalone_local(
                        effective_syllables[protected_start_local].text))) then
                    begin
                        shifted_tail_text_local := build_unit_span_text_local(
                            repaired_units_local, protected_start_local + 1,
                            protected_len_local);
                        shifted_tail_weight_local :=
                            get_exact_span_weight_for_text_local(
                            protected_start_local + 1, protected_len_local,
                            shifted_tail_text_local);
                        if (shifted_tail_weight_local > 0) and
                            ((shifted_tail_weight_local +
                            c_shifted_tail_exact_weight_slack >=
                            protected_weight_local) or
                            ((shifted_tail_weight_local >=
                            c_shifted_tail_strong_weight) and
                            (protected_weight_local <=
                            shifted_tail_weight_local +
                            c_covering_longer_exact_weight_slack)) or
                            (shifted_tail_weight_local >=
                            c_shifted_tail_reliable_weight)) then
                        begin
                            Exit(True);
                        end;
                    end;

                    if (protected_len_local >= 2) and
                        (protected_start_local > 0) and
                        (protected_start_local + protected_len_local <=
                        syllable_count_local) then
                    begin
                        shifted_head_text_local := build_unit_span_text_local(
                            repaired_units_local, protected_start_local - 1,
                            protected_len_local);
                        shifted_head_weight_local :=
                            get_exact_span_weight_for_text_local(
                            protected_start_local - 1, protected_len_local,
                            shifted_head_text_local);
                        shifted_head_tail_text_local :=
                            repaired_units_local[protected_start_local +
                            protected_len_local - 1];
                        shifted_head_tail_fixed_local :=
                            get_fixed_boundary_single_char_for_standalone_local(
                            effective_syllables[protected_start_local +
                            protected_len_local - 1].text);
                        if (shifted_head_weight_local > 0) and
                            (shifted_head_tail_fixed_local <> '') and
                            SameText(shifted_head_tail_text_local,
                            shifted_head_tail_fixed_local) and
                            ((shifted_head_weight_local +
                            c_shifted_tail_exact_weight_slack >=
                            protected_weight_local) or
                            (shifted_head_weight_local >=
                            c_shifted_tail_strong_weight) or
                            (shifted_head_weight_local >=
                            c_shifted_tail_reliable_weight)) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            begin
                Result := False;
                source_units_local := split_text_units(source_state.text);
                repaired_units_local := split_text_units(repaired_text_local);
                if (Length(source_units_local) <> syllable_count_local) or
                    (Length(repaired_units_local) <> syllable_count_local) then
                begin
                    Exit;
                end;

                for start_idx_local := 0 to syllable_count_local - 1 do
                begin
                    for span_len_local := 2 to Min(c_max_protected_exact_span_units,
                        syllable_count_local - start_idx_local) do
                    begin
                        source_span_text_local := '';
                        repaired_span_text_local := '';
                        for unit_idx_local := start_idx_local to
                            start_idx_local + span_len_local - 1 do
                        begin
                            source_span_text_local := source_span_text_local +
                                source_units_local[unit_idx_local];
                            repaired_span_text_local := repaired_span_text_local +
                                repaired_units_local[unit_idx_local];
                        end;

                        if SameText(source_span_text_local,
                            repaired_span_text_local) then
                        begin
                            Continue;
                        end;

                        query_key_local := build_query_key_local(start_idx_local,
                            span_len_local);
                        if (query_key_local = '') or
                            (not lookup_exact_full_pinyin_cached_local(
                            query_key_local, exact_results_local)) then
                        begin
                            Continue;
                        end;

                        for exact_idx_local := 0 to High(exact_results_local) do
                        begin
                            if Trim(exact_results_local[exact_idx_local].comment) <>
                                '' then
                            begin
                                Continue;
                            end;
                            if not SameText(Trim(exact_results_local[
                                exact_idx_local].text), source_span_text_local) then
                            begin
                                Continue;
                            end;

                            exact_weight_local := get_exact_chunk_raw_weight_local(
                                exact_results_local[exact_idx_local]);
                            if (exact_weight_local >=
                                c_min_protected_exact_span_weight) and
                                is_reliable_fast_exact_chunk_local(query_key_local,
                                span_len_local, source_span_text_local,
                                exact_results_local[exact_idx_local]) then
                            begin
                                if repaired_span_has_comparable_exact_support_local(
                                    start_idx_local, span_len_local,
                                    exact_weight_local) then
                                begin
                                    Continue;
                                end;
                                Exit(True);
                            end;
                        end;
                    end;
                end;
            end;

            function repaired_text_rewrites_directional_place_span_local: Boolean;
            var
                source_units_local: TArray<string>;
                repaired_units_local: TArray<string>;
                unit_idx_local: Integer;
                source_span_local: string;
                repaired_span_local: string;

                function is_place_context_unit_local(
                    const unit_value_local: string): Boolean;
                begin
                    Result := (unit_value_local = string(Char($5BB6))) or
                        (unit_value_local = string(Char($5E97))) or
                        (unit_value_local = string(Char($5C4B))) or
                        (unit_value_local = string(Char($697C))) or
                        (unit_value_local = string(Char($57CE))) or
                        (unit_value_local = string(Char($6751))) or
                        (unit_value_local = string(Char($53BF))) or
                        (unit_value_local = string(Char($5E02))) or
                        (unit_value_local = string(Char($6821))) or
                        (unit_value_local = string(Char($9662))) or
                        (unit_value_local = string(Char($5382))) or
                        (unit_value_local = string(Char($9986))) or
                        (unit_value_local = string(Char($7AD9))) or
                        (unit_value_local = string(Char($5C40))) or
                        (unit_value_local = string(Char($6240))) or
                        (unit_value_local = string(Char($56ED))) or
                        (unit_value_local = string(Char($533A))) or
                        (unit_value_local = string(Char($5708))) or
                        (unit_value_local = string(Char($8DEF))) or
                        (unit_value_local = string(Char($8857))) or
                        (unit_value_local = string(Char($95E8))) or
                        (unit_value_local = string(Char($5BA4))) or
                        (unit_value_local = string(Char($623F))) or
                        (unit_value_local = string(Char($5385))) or
                        (unit_value_local = string(Char($573A))) or
                        (unit_value_local = string(Char($53F8)));
                end;

                function has_directional_context_local(
                    const span_start_local: Integer): Boolean;
                var
                    next_key_local: string;
                begin
                    Result := False;
                    if (span_start_local <= 0) or
                        (span_start_local + 1 >= syllable_count_local) or
                        (not is_place_context_unit_local(
                        source_units_local[span_start_local - 1])) then
                    begin
                        Exit;
                    end;

                    if span_start_local + 2 >= syllable_count_local then
                    begin
                        Exit(True);
                    end;

                    next_key_local := normalize_pinyin_text(
                        effective_syllables[span_start_local + 2].text);
                    Result := SameText(next_key_local, 'de');
                end;

            begin
                Result := False;
                source_units_local := split_text_units(source_state.text);
                repaired_units_local := split_text_units(repaired_text_local);
                if (Length(source_units_local) <> syllable_count_local) or
                    (Length(repaired_units_local) <> syllable_count_local) then
                begin
                    Exit;
                end;

                for unit_idx_local := 1 to syllable_count_local - 3 do
                begin
                    if not has_directional_context_local(unit_idx_local) then
                    begin
                        Continue;
                    end;

                    source_span_local := source_units_local[unit_idx_local] +
                        source_units_local[unit_idx_local + 1];
                    repaired_span_local := repaired_units_local[unit_idx_local] +
                        repaired_units_local[unit_idx_local + 1];
                    if SameText(source_span_local, repaired_span_local) then
                    begin
                        Continue;
                    end;

                    if (get_directional_locative_span_bonus_local(
                        source_span_local) > 0) and
                        (get_directional_locative_span_bonus_local(
                        repaired_span_local) <= 0) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                Format(' dirctxrepairreject=[%s>%s]',
                                [source_span_local, repaired_span_local]);
                        end;
                        Exit(True);
                    end;
                end;
            end;

            function try_get_better_exact_text_local(const query_key_local: string;
                const expected_units_local: Integer;
                const current_span_text_local: string; const min_weight_local: Integer;
                out out_text_local: string; out out_score_local: Integer): Boolean;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_text_local: string;
                candidate_score_local: Integer;
                candidate_weight_local: Integer;
                best_weight_local: Integer;
            begin
                Result := False;
                out_text_local := '';
                out_score_local := 0;
                best_weight_local := 0;
                if (query_key_local = '') or (expected_units_local < 2) or
                    (expected_units_local > 5) or
                    (current_span_text_local = '') then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_lattice_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if (candidate_text_local = '') or
                        SameText(candidate_text_local, current_span_text_local) or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        expected_units_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, expected_units_local)) then
                    begin
                        Continue;
                    end;

                    candidate_weight_local := get_exact_chunk_raw_weight_local(
                        local_results[local_idx]);
                    if candidate_weight_local < min_weight_local then
                    begin
                        Continue;
                    end;
                    if has_particle_tail_mismatch_local(query_key_local,
                        candidate_text_local, candidate_weight_local) then
                    begin
                        Continue;
                    end;
                    if (min_weight_local >= 260) and
                        (min_weight_local < 50000) and
                        (not is_reliable_fast_exact_chunk_local(query_key_local,
                        expected_units_local, candidate_text_local,
                        local_results[local_idx])) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := get_exact_chunk_effective_weight_local(
                        local_results[local_idx]);
                    Inc(candidate_score_local, get_preferred_query_phrase_bonus_local(
                        query_key_local, candidate_text_local));
                    Inc(candidate_score_local, get_exact_query_text_bonus_local(
                        query_key_local, candidate_text_local));
                    Inc(candidate_score_local, get_exact_chunk_semantic_bonus_local(
                        candidate_text_local));

                    if (not Result) or
                        (candidate_score_local > out_score_local) or
                        ((candidate_score_local = out_score_local) and
                        (candidate_weight_local > best_weight_local)) then
                    begin
                        Result := True;
                        out_text_local := candidate_text_local;
                        out_score_local := candidate_score_local;
                        best_weight_local := candidate_weight_local;
                    end;
                end;
            end;

            function try_get_best_exact_text_local(const query_key_local: string;
                const expected_units_local: Integer; const min_weight_local: Integer;
                out out_text_local: string; out out_score_local: Integer): Boolean;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_text_local: string;
                candidate_score_local: Integer;
                candidate_weight_local: Integer;
                best_weight_local: Integer;
            begin
                Result := False;
                out_text_local := '';
                out_score_local := 0;
                best_weight_local := 0;
                if (query_key_local = '') or (expected_units_local < 2) or
                    (expected_units_local > 3) then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_lattice_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        expected_units_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, expected_units_local)) then
                    begin
                        Continue;
                    end;

                    candidate_weight_local := get_exact_chunk_raw_weight_local(
                        local_results[local_idx]);
                    if candidate_weight_local < min_weight_local then
                    begin
                        Continue;
                    end;
                    if has_particle_tail_mismatch_local(query_key_local,
                        candidate_text_local, candidate_weight_local) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := get_exact_chunk_effective_weight_local(
                        local_results[local_idx]);
                    Inc(candidate_score_local, get_preferred_query_phrase_bonus_local(
                        query_key_local, candidate_text_local));
                    Inc(candidate_score_local, get_exact_query_text_bonus_local(
                        query_key_local, candidate_text_local));
                    Inc(candidate_score_local, get_exact_chunk_semantic_bonus_local(
                        candidate_text_local));

                    if (not Result) or
                        (candidate_score_local > out_score_local) or
                        ((candidate_score_local = out_score_local) and
                        (candidate_weight_local > best_weight_local)) then
                    begin
                        Result := True;
                        out_text_local := candidate_text_local;
                        out_score_local := candidate_score_local;
                        best_weight_local := candidate_weight_local;
                    end;
                end;
            end;

            function try_get_top_exact_text_local(const query_key_local: string;
                const expected_units_local: Integer; const min_weight_local: Integer;
                out out_text_local: string; out out_score_local: Integer): Boolean;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_text_local: string;
                candidate_weight_local: Integer;
            begin
                Result := False;
                out_text_local := '';
                out_score_local := 0;
                if (query_key_local = '') or (expected_units_local < 2) or
                    (expected_units_local > 4) then
                begin
                    Exit;
                end;

                if not lookup_exact_full_pinyin_cached_local(query_key_local,
                    local_results) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_chain_lattice_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        expected_units_local) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        candidate_text_local, expected_units_local)) then
                    begin
                        Continue;
                    end;

                    candidate_weight_local := get_exact_chunk_raw_weight_local(
                        local_results[local_idx]);
                    if candidate_weight_local < min_weight_local then
                    begin
                        Continue;
                    end;
                    if has_particle_tail_mismatch_local(query_key_local,
                        candidate_text_local, candidate_weight_local) then
                    begin
                        Continue;
                    end;
                    if (min_weight_local >= 260) and
                        (not is_reliable_fast_exact_chunk_local(query_key_local,
                        expected_units_local, candidate_text_local,
                        local_results[local_idx])) then
                    begin
                        Continue;
                    end;

                    out_text_local := candidate_text_local;
                    out_score_local := candidate_weight_local;
                    Exit(True);
                end;
            end;

            function is_quantity_tail_key_local(
                const key_local: string): Boolean;
            begin
                Result := SameText(key_local, 'ci') or
                    SameText(key_local, 'ge') or
                    SameText(key_local, 'tiao') or
                    SameText(key_local, 'zhang') or
                    SameText(key_local, 'ben') or
                    SameText(key_local, 'pian') or
                    SameText(key_local, 'bu') or
                    SameText(key_local, 'lun') or
                    SameText(key_local, 'hui') or
                    SameText(key_local, 'xia');
            end;

            function get_quantity_fixed_text_local(
                const key_local: string): string;
            begin
                Result := '';
                if SameText(key_local, 'yi') then
                begin
                    Result := string(Char($4E00));
                end
                else if SameText(key_local, 'ci') then
                begin
                    Result := string(Char($6B21));
                end
                else if SameText(key_local, 'ge') then
                begin
                    Result := string(Char($4E2A));
                end
                else if SameText(key_local, 'tiao') then
                begin
                    Result := string(Char($6761));
                end
                else if SameText(key_local, 'zhang') then
                begin
                    Result := string(Char($5F20));
                end
                else if SameText(key_local, 'ben') then
                begin
                    Result := string(Char($672C));
                end
                else if SameText(key_local, 'pian') then
                begin
                    Result := string(Char($7BC7));
                end
                else if SameText(key_local, 'bu') then
                begin
                    Result := string(Char($90E8));
                end
                else if SameText(key_local, 'lun') then
                begin
                    Result := string(Char($8F6E));
                end
                else if SameText(key_local, 'hui') then
                begin
                    Result := string(Char($56DE));
                end
                else if SameText(key_local, 'xia') then
                begin
                    Result := string(Char($4E0B));
                end
                else if SameText(key_local, 'ba') then
                begin
                    Result := string(Char($5427));
                end;
            end;

            function try_repair_quantity_fixed_segment_local(
                const segment_query_offset_local: Integer;
                const segment_units_local: Integer;
                const segment_text_local: string;
                out out_text_local: string;
                out out_path_local: string): Boolean;
            var
                segment_units_text_local: TArray<string>;
                key_values_local: TArray<string>;
                unit_idx_local: Integer;
                fixed_text_local: string;
                has_quantity_context_local: Boolean;
                has_mismatch_local: Boolean;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_units_local < 2) or (segment_units_local > 3) or
                    (segment_query_offset_local < 0) or
                    (segment_query_offset_local + segment_units_local >
                    syllable_count_local) then
                begin
                    Exit;
                end;

                segment_units_text_local := split_text_units(segment_text_local);
                if Length(segment_units_text_local) <> segment_units_local then
                begin
                    Exit;
                end;

                SetLength(key_values_local, segment_units_local);
                for unit_idx_local := 0 to segment_units_local - 1 do
                begin
                    key_values_local[unit_idx_local] := normalize_pinyin_text(
                        effective_syllables[
                        segment_query_offset_local + unit_idx_local].text);
                end;

                has_quantity_context_local := False;
                for unit_idx_local := 0 to segment_units_local - 2 do
                begin
                    if SameText(key_values_local[unit_idx_local], 'yi') and
                        is_quantity_tail_key_local(
                        key_values_local[unit_idx_local + 1]) then
                    begin
                        has_quantity_context_local := True;
                        Break;
                    end;
                end;
                if (not has_quantity_context_local) and
                    SameText(key_values_local[segment_units_local - 1], 'ba') and
                    (segment_units_local >= 2) and
                    (is_quantity_tail_key_local(
                    key_values_local[segment_units_local - 2]) or
                    SameText(key_values_local[0], 'yi')) then
                begin
                    has_quantity_context_local := True;
                end;
                if not has_quantity_context_local then
                begin
                    Exit;
                end;

                has_mismatch_local := False;
                for unit_idx_local := 0 to segment_units_local - 1 do
                begin
                    fixed_text_local := get_quantity_fixed_text_local(
                        key_values_local[unit_idx_local]);
                    if fixed_text_local = '' then
                    begin
                        Exit;
                    end;
                    out_text_local := out_text_local + fixed_text_local;
                    if out_path_local <> '' then
                    begin
                        out_path_local := out_path_local +
                            c_segment_path_separator;
                    end;
                    out_path_local := out_path_local + fixed_text_local;
                    if not SameText(segment_units_text_local[unit_idx_local],
                        fixed_text_local) then
                    begin
                        has_mismatch_local := True;
                    end;
                end;

                Result := has_mismatch_local and (out_text_local <> '');
            end;

            function get_particle_tail_fixed_text_local(
                const key_local: string): string;
            begin
                Result := '';
                if SameText(key_local, 'de') then
                begin
                    Result := string(Char($7684));
                end
                else if SameText(key_local, 'le') then
                begin
                    Result := string(Char($4E86));
                end;
            end;

            function get_covering_fixed_single_text_local(
                const key_local: string): string;
            begin
                Result := get_fixed_boundary_single_char_for_standalone_local(
                    key_local);
                if Result <> '' then
                begin
                    Exit;
                end;

                if SameText(key_local, 'qu') then
                begin
                    Result := string(Char($53BB));
                end
                else if SameText(key_local, 'dui') then
                begin
                    Result := string(Char($5BF9));
                end
                else if SameText(key_local, 'qie') then
                begin
                    Result := string(Char($4E14));
                end
                else if SameText(key_local, 'bei') then
                begin
                    Result := string(Char($88AB));
                end
                else if SameText(key_local, 'bing') then
                begin
                    Result := string(Char($5E76));
                end
                else if SameText(key_local, 'li') then
                begin
                    Result := string(Char($91CC));
                end
                else if SameText(key_local, 'geng') then
                begin
                    Result := string(Char($66F4));
                end
                else if SameText(key_local, 'zhong') then
                begin
                    Result := string(Char($4E2D));
                end
                else if SameText(key_local, 'shang') then
                begin
                    Result := string(Char($4E0A));
                end
                else if SameText(key_local, 'xia') then
                begin
                    Result := string(Char($4E0B));
                end
                else if SameText(key_local, 'nei') then
                begin
                    Result := string(Char($5185));
                end
                else if SameText(key_local, 'wai') then
                begin
                    Result := string(Char($5916));
                end
                else if SameText(key_local, 'qian') then
                begin
                    Result := string(Char($524D));
                end
                else if SameText(key_local, 'hou') then
                begin
                    Result := string(Char($540E));
                end
            end;

            function try_get_fixed_single_text_at_local(
                const unit_query_offset_local: Integer;
                out out_text_local: string): Boolean;
            var
                key_local: string;
            begin
                Result := False;
                out_text_local := '';
                if (unit_query_offset_local < 0) or
                    (unit_query_offset_local >= syllable_count_local) then
                begin
                    Exit;
                end;

                key_local := normalize_pinyin_text(
                    effective_syllables[unit_query_offset_local].text);
                out_text_local := get_covering_fixed_single_text_local(key_local);
                Result := out_text_local <> '';
            end;

            function try_get_particle_tail_text_at_local(
                const unit_query_offset_local: Integer;
                out out_text_local: string): Boolean;
            var
                key_local: string;
            begin
                Result := False;
                out_text_local := '';
                if (unit_query_offset_local < 0) or
                    (unit_query_offset_local >= syllable_count_local) then
                begin
                    Exit;
                end;

                key_local := normalize_pinyin_text(
                    effective_syllables[unit_query_offset_local].text);
                out_text_local := get_particle_tail_fixed_text_local(key_local);
                Result := out_text_local <> '';
            end;

            function try_repair_particle_tail_segment_local(
                const segment_query_offset_local: Integer;
                const segment_units_local: Integer;
                const segment_text_local: string;
                out out_text_local: string;
                out out_path_local: string): Boolean;
            var
                segment_units_text_local: TArray<string>;
                prefix_units_local: Integer;
                unit_idx_local: Integer;
                prefix_key_local: string;
                prefix_current_text_local: string;
                prefix_exact_text_local: string;
                prefix_exact_score_local: Integer;
                tail_key_local: string;
                tail_fixed_text_local: string;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_units_local < 3) or (segment_units_local > 4) or
                    (segment_query_offset_local < 0) or
                    (segment_query_offset_local + segment_units_local >
                    syllable_count_local) then
                begin
                    Exit;
                end;

                segment_units_text_local := split_text_units(segment_text_local);
                if Length(segment_units_text_local) <> segment_units_local then
                begin
                    Exit;
                end;

                tail_key_local := normalize_pinyin_text(effective_syllables[
                    segment_query_offset_local + segment_units_local - 1].text);
                tail_fixed_text_local := get_covering_fixed_single_text_local(
                    tail_key_local);
                if tail_fixed_text_local = '' then
                begin
                    tail_fixed_text_local := get_particle_tail_fixed_text_local(
                        tail_key_local);
                end;
                if tail_fixed_text_local = '' then
                begin
                    Exit;
                end;
                if SameText(segment_units_text_local[High(segment_units_text_local)],
                    tail_fixed_text_local) then
                begin
                    Exit;
                end;

                prefix_units_local := segment_units_local - 1;
                prefix_key_local := build_query_key_local(segment_query_offset_local,
                    prefix_units_local);
                prefix_current_text_local := '';
                for unit_idx_local := 0 to prefix_units_local - 1 do
                begin
                    prefix_current_text_local := prefix_current_text_local +
                        segment_units_text_local[unit_idx_local];
                end;

                if not try_get_best_exact_text_local(prefix_key_local,
                    prefix_units_local, c_min_covering_pair_exact_weight,
                    prefix_exact_text_local, prefix_exact_score_local) then
                begin
                    Exit;
                end;

                out_text_local := prefix_exact_text_local + tail_fixed_text_local;
                out_path_local := prefix_exact_text_local +
                    c_segment_path_separator + tail_fixed_text_local;
                Result := (out_text_local <> '') and
                    (not SameText(out_text_local, segment_text_local)) and
                    ((not SameText(prefix_exact_text_local,
                    prefix_current_text_local)) or
                    (not SameText(segment_units_text_local[
                    High(segment_units_text_local)], tail_fixed_text_local)));
            end;

            function try_repair_adjacent_split_phrase_local(
                const segment_query_offset_local: Integer;
                const left_units_local: Integer; const left_text_local: string;
                const right_units_local: Integer; const right_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                total_units_local: Integer;
                combined_text_value_local: string;
                exact_text_local: string;
                exact_score_local: Integer;
                fixed_text_local: string;
                head_fixed_text_local: string;
                tail_fixed_text_local: string;
                exact_key_local: string;

                function has_exact_text_for_span_local(
                    const span_offset_local: Integer; const span_units_local: Integer;
                    const span_text_local: string; const min_weight_local: Integer): Boolean;
                var
                    span_key_local: string;
                    span_results_local: TncCandidateList;
                    span_idx_local: Integer;
                    span_weight_local: Integer;
                    span_score_local: Integer;
                begin
                    Result := False;
                    if (span_text_local = '') or (span_units_local < 2) or
                        (span_offset_local < 0) or
                        (span_offset_local + span_units_local > syllable_count_local) then
                    begin
                        Exit;
                    end;

                    span_key_local := build_query_key_local(span_offset_local,
                        span_units_local);
                    if (span_key_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(span_key_local,
                        span_results_local)) then
                    begin
                        Exit;
                    end;

                    for span_idx_local := 0 to High(span_results_local) do
                    begin
                        if span_idx_local >= c_chain_lattice_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(span_results_local[span_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not SameText(Trim(span_results_local[span_idx_local].text),
                            span_text_local) then
                        begin
                            Continue;
                        end;

                        span_weight_local := get_exact_chunk_raw_weight_local(
                            span_results_local[span_idx_local]);
                        span_score_local := span_results_local[span_idx_local].score;
                        if (span_weight_local >= min_weight_local) or
                            (span_score_local >= min_weight_local * 2) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                function is_current_exact_plus_fixed_boundary_local: Boolean;
                var
                    current_fixed_text_local: string;
                begin
                    Result := False;
                    if (left_units_local < 2) or (right_units_local <> 1) then
                    begin
                        Exit;
                    end;

                    if (not try_get_fixed_single_text_at_local(
                        segment_query_offset_local + left_units_local,
                        current_fixed_text_local)) and
                        (not try_get_particle_tail_text_at_local(
                        segment_query_offset_local + left_units_local,
                        current_fixed_text_local)) then
                    begin
                        Exit;
                    end;

                    if (current_fixed_text_local = '') or
                        (not SameText(right_text_local, current_fixed_text_local)) then
                    begin
                        Exit;
                    end;

                    Result := has_exact_text_for_span_local(
                        segment_query_offset_local, left_units_local,
                        left_text_local, 500);
                end;

                function is_current_fixed_plus_exact_boundary_local: Boolean;
                var
                    current_fixed_text_local: string;
                begin
                    Result := False;
                    if (left_units_local <> 1) or (right_units_local < 2) then
                    begin
                        Exit;
                    end;

                    if not try_get_fixed_single_text_at_local(
                        segment_query_offset_local, current_fixed_text_local) then
                    begin
                        Exit;
                    end;

                    if (current_fixed_text_local = '') or
                        (not SameText(left_text_local, current_fixed_text_local)) then
                    begin
                        Exit;
                    end;

                    Result := has_exact_text_for_span_local(
                        segment_query_offset_local + 1, right_units_local,
                        right_text_local, 500);
                end;

                function try_split_exact_then_fixed_local(
                    const exact_units_local: Integer): Boolean;
                begin
                    Result := False;
                    if (exact_units_local < 2) or (exact_units_local > 3) or
                        (exact_units_local >= total_units_local) then
                    begin
                        Exit;
                    end;
                    if (not try_get_fixed_single_text_at_local(
                        segment_query_offset_local + exact_units_local,
                        fixed_text_local)) and
                        (not try_get_particle_tail_text_at_local(
                        segment_query_offset_local + exact_units_local,
                        fixed_text_local)) then
                    begin
                        Exit;
                    end;

                    exact_key_local := build_query_key_local(
                        segment_query_offset_local, exact_units_local);
                    if not try_get_best_exact_text_local(exact_key_local,
                        exact_units_local, c_min_covering_pair_exact_weight,
                        exact_text_local, exact_score_local) then
                    begin
                        Exit;
                    end;

                    out_text_local := exact_text_local + fixed_text_local;
                    out_path_local := exact_text_local +
                        c_segment_path_separator + fixed_text_local;
                    Result := (out_text_local <> '') and
                        (not SameText(out_text_local, combined_text_value_local));
                end;

                function try_split_fixed_then_exact_local(
                    const exact_units_local: Integer): Boolean;
                begin
                    Result := False;
                    if (exact_units_local < 2) or (exact_units_local > 3) or
                        (exact_units_local >= total_units_local) then
                    begin
                        Exit;
                    end;

                    if not try_get_fixed_single_text_at_local(
                        segment_query_offset_local, fixed_text_local) then
                    begin
                        Exit;
                    end;

                    exact_key_local := build_query_key_local(
                        segment_query_offset_local + 1, exact_units_local);
                    if not try_get_best_exact_text_local(exact_key_local,
                        exact_units_local, c_min_covering_pair_exact_weight,
                        exact_text_local, exact_score_local) then
                    begin
                        Exit;
                    end;

                    out_text_local := fixed_text_local + exact_text_local;
                    out_path_local := fixed_text_local +
                        c_segment_path_separator + exact_text_local;
                    Result := (out_text_local <> '') and
                        (not SameText(out_text_local, combined_text_value_local));
                end;

                function try_split_fixed_exact_fixed_local: Boolean;
                begin
                    Result := False;
                    if total_units_local <> 4 then
                    begin
                        Exit;
                    end;

                    if not try_get_fixed_single_text_at_local(
                        segment_query_offset_local, head_fixed_text_local) then
                    begin
                        Exit;
                    end;
                    if (not try_get_fixed_single_text_at_local(
                        segment_query_offset_local + 3,
                        tail_fixed_text_local)) and
                        (not try_get_particle_tail_text_at_local(
                        segment_query_offset_local + 3,
                        tail_fixed_text_local)) then
                    begin
                        Exit;
                    end;
                    if (head_fixed_text_local = '') or
                        (tail_fixed_text_local = '') then
                    begin
                        Exit;
                    end;

                    exact_key_local := build_query_key_local(
                        segment_query_offset_local + 1, 2);
                    if not try_get_best_exact_text_local(exact_key_local, 2,
                        c_min_covering_pair_exact_weight, exact_text_local,
                        exact_score_local) then
                    begin
                        Exit;
                    end;

                    out_text_local := head_fixed_text_local + exact_text_local +
                        tail_fixed_text_local;
                    out_path_local := head_fixed_text_local +
                        c_segment_path_separator + exact_text_local +
                        c_segment_path_separator + tail_fixed_text_local;
                    Result := (out_text_local <> '') and
                        (not SameText(out_text_local, combined_text_value_local));
                end;

            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                total_units_local := left_units_local + right_units_local;
                if (segment_query_offset_local < 0) or
                    (total_units_local < 3) or (total_units_local > 4) or
                    (segment_query_offset_local + total_units_local >
                    syllable_count_local) then
                begin
                    Exit;
                end;

                combined_text_value_local := left_text_local + right_text_local;
                if combined_text_value_local = '' then
                begin
                    Exit;
                end;

                if is_current_exact_plus_fixed_boundary_local then
                begin
                    Exit;
                end;
                if is_current_fixed_plus_exact_boundary_local then
                begin
                    Exit;
                end;

                if total_units_local = 3 then
                begin
                    if try_split_fixed_then_exact_local(2) then
                    begin
                        Exit(True);
                    end;
                    if try_split_exact_then_fixed_local(2) then
                    begin
                        Exit(True);
                    end;
                end
                else if total_units_local = 4 then
                begin
                    if try_split_fixed_exact_fixed_local then
                    begin
                        Exit(True);
                    end;
                    if try_split_fixed_then_exact_local(3) then
                    begin
                        Exit(True);
                    end;
                    if try_split_exact_then_fixed_local(3) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function try_repair_one_two_one_boundary_shift_local(
                const segment_query_offset_local: Integer;
                const left_single_text_local: string;
                const middle_units_local: Integer;
                const middle_text_local: string;
                const right_single_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                middle_units_text_local: TArray<string>;
                left_phrase_key_local: string;
                right_phrase_key_local: string;
                middle_key_local: string;
                left_phrase_text_local: string;
                right_phrase_text_local: string;
                middle_best_text_local: string;
                left_phrase_score_local: Integer;
                right_phrase_score_local: Integer;
                middle_score_local: Integer;
                shifted_text_local: string;
                original_text_local: string;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_query_offset_local < 0) or
                    (middle_units_local <> 2) or
                    (left_single_text_local = '') or
                    (middle_text_local = '') or
                    (right_single_text_local = '') or
                    (segment_query_offset_local + 4 > syllable_count_local) then
                begin
                    Exit;
                end;

                middle_units_text_local := split_text_units(middle_text_local);
                if Length(middle_units_text_local) <> 2 then
                begin
                    Exit;
                end;

                left_phrase_key_local := build_query_key_local(
                    segment_query_offset_local, 2);
                right_phrase_key_local := build_query_key_local(
                    segment_query_offset_local + 2, 2);
                if (not try_get_best_exact_text_local(left_phrase_key_local, 2,
                    c_min_covering_pair_exact_weight, left_phrase_text_local,
                    left_phrase_score_local)) or
                    (not try_get_best_exact_text_local(right_phrase_key_local, 2,
                    c_min_covering_pair_exact_weight, right_phrase_text_local,
                    right_phrase_score_local)) then
                begin
                    Exit;
                end;

                shifted_text_local := left_phrase_text_local +
                    right_phrase_text_local;
                original_text_local := left_single_text_local +
                    middle_text_local + right_single_text_local;
                if SameText(shifted_text_local, original_text_local) then
                begin
                    Exit;
                end;

                middle_score_local := 0;
                middle_key_local := build_query_key_local(
                    segment_query_offset_local + 1, 2);
                if try_get_best_exact_text_local(middle_key_local, 2, 0,
                    middle_best_text_local, middle_score_local) and
                    SameText(middle_best_text_local, middle_text_local) and
                    (left_phrase_score_local + right_phrase_score_local <
                    middle_score_local + c_min_covering_pair_exact_weight) then
                begin
                    Exit;
                end;

                out_text_local := shifted_text_local;
                out_path_local := left_phrase_text_local +
                    c_segment_path_separator + right_phrase_text_local;
                Result := out_text_local <> '';
            end;

            function try_repair_one_two_two_boundary_shift_local(
                const segment_query_offset_local: Integer;
                const left_single_text_local: string;
                const middle_units_local: Integer;
                const middle_text_local: string;
                const right_units_local: Integer;
                const right_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                left_phrase_key_local: string;
                right_phrase_key_local: string;
                left_phrase_text_local: string;
                right_phrase_text_local: string;
                left_phrase_score_local: Integer;
                right_phrase_score_local: Integer;
                original_text_local: string;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_query_offset_local < 0) or
                    (middle_units_local <> 2) or (right_units_local <> 2) or
                    (left_single_text_local = '') or (middle_text_local = '') or
                    (right_text_local = '') or
                    (segment_query_offset_local + 5 > syllable_count_local) then
                begin
                    Exit;
                end;

                left_phrase_key_local := build_query_key_local(
                    segment_query_offset_local, 2);
                right_phrase_key_local := build_query_key_local(
                    segment_query_offset_local + 2, 3);
                if (not try_get_top_exact_text_local(left_phrase_key_local, 2,
                    c_min_covering_pair_exact_weight, left_phrase_text_local,
                    left_phrase_score_local)) or
                    (not try_get_top_exact_text_local(right_phrase_key_local, 3,
                    800, right_phrase_text_local,
                    right_phrase_score_local)) then
                begin
                    Exit;
                end;

                original_text_local := left_single_text_local +
                    middle_text_local + right_text_local;
                out_text_local := left_phrase_text_local + right_phrase_text_local;
                if SameText(out_text_local, original_text_local) then
                begin
                    Exit;
                end;

                out_path_local := left_phrase_text_local +
                    c_segment_path_separator + right_phrase_text_local;
                Result := out_text_local <> '';
            end;

            function try_repair_two_one_one_boundary_shift_local(
                const segment_query_offset_local: Integer;
                const left_units_local: Integer;
                const left_text_local: string;
                const middle_single_text_local: string;
                const right_single_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                left_phrase_key_local: string;
                right_phrase_key_local: string;
                left_phrase_text_local: string;
                right_phrase_text_local: string;
                left_phrase_score_local: Integer;
                right_phrase_score_local: Integer;
                original_text_local: string;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_query_offset_local < 0) or
                    (left_units_local <> 2) or
                    (left_text_local = '') or
                    (middle_single_text_local = '') or
                    (right_single_text_local = '') or
                    (segment_query_offset_local + 4 > syllable_count_local) then
                begin
                    Exit;
                end;

                left_phrase_key_local := build_query_key_local(
                    segment_query_offset_local, 2);
                right_phrase_key_local := build_query_key_local(
                    segment_query_offset_local + 2, 2);
                if (not try_get_best_exact_text_local(left_phrase_key_local, 2,
                    c_min_covering_pair_exact_weight, left_phrase_text_local,
                    left_phrase_score_local)) or
                    (not try_get_best_exact_text_local(right_phrase_key_local, 2,
                    c_min_covering_pair_exact_weight, right_phrase_text_local,
                    right_phrase_score_local)) then
                begin
                    Exit;
                end;

                original_text_local := left_text_local + middle_single_text_local +
                    right_single_text_local;
                out_text_local := left_phrase_text_local + right_phrase_text_local;
                if SameText(out_text_local, original_text_local) then
                begin
                    Exit;
                end;

                out_path_local := left_phrase_text_local +
                    c_segment_path_separator + right_phrase_text_local;
                Result := out_text_local <> '';
            end;

            function try_repair_one_two_two_one_boundary_shift_local(
                const segment_query_offset_local: Integer;
                const left_single_text_local: string;
                const first_middle_text_local: string;
                const second_middle_text_local: string;
                const right_single_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                left_phrase_key_local: string;
                first_right_phrase_key_local: string;
                second_right_phrase_key_local: string;
                left_phrase_text_local: string;
                first_right_phrase_text_local: string;
                second_right_phrase_text_local: string;
                left_phrase_score_local: Integer;
                first_right_phrase_score_local: Integer;
                second_right_phrase_score_local: Integer;
                original_text_local: string;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_query_offset_local < 0) or
                    (left_single_text_local = '') or
                    (first_middle_text_local = '') or
                    (second_middle_text_local = '') or
                    (right_single_text_local = '') or
                    (segment_query_offset_local + 6 > syllable_count_local) then
                begin
                    Exit;
                end;

                left_phrase_key_local := build_query_key_local(
                    segment_query_offset_local, 2);
                first_right_phrase_key_local := build_query_key_local(
                    segment_query_offset_local + 2, 2);
                second_right_phrase_key_local := build_query_key_local(
                    segment_query_offset_local + 4, 2);

                if (not try_get_top_exact_text_local(left_phrase_key_local, 2,
                    800, left_phrase_text_local, left_phrase_score_local)) or
                    (not try_get_top_exact_text_local(first_right_phrase_key_local,
                    2, 800, first_right_phrase_text_local,
                    first_right_phrase_score_local)) or
                    (not try_get_top_exact_text_local(second_right_phrase_key_local,
                    2, 800, second_right_phrase_text_local,
                    second_right_phrase_score_local)) then
                begin
                    Exit;
                end;

                original_text_local := left_single_text_local +
                    first_middle_text_local + second_middle_text_local +
                    right_single_text_local;
                out_text_local := left_phrase_text_local +
                    first_right_phrase_text_local + second_right_phrase_text_local;
                if SameText(out_text_local, original_text_local) then
                begin
                    Exit;
                end;

                out_path_local := left_phrase_text_local +
                    c_segment_path_separator + first_right_phrase_text_local +
                    c_segment_path_separator + second_right_phrase_text_local;
                Result := out_text_local <> '';
            end;

            function is_structural_prefix_tail_shift_text_local(
                const prefix_text_value_local: string): Boolean;
            begin
                Result :=
                    SameText(prefix_text_value_local, string(Char($7684))) or
                    SameText(prefix_text_value_local, string(Char($5730))) or
                    SameText(prefix_text_value_local, string(Char($5F97))) or
                    SameText(prefix_text_value_local, string(Char($5BF9))) or
                    SameText(prefix_text_value_local, string(Char($548C))) or
                    SameText(prefix_text_value_local, string(Char($4E0E))) or
                    SameText(prefix_text_value_local, string(Char($50CF)));
            end;

            function try_repair_prefix_tail_exact_shift_local(
                const segment_query_offset_local: Integer;
                const total_units_local: Integer; const current_text_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            const
                c_min_tail_exact_weight = 500;
                c_min_fixed_prefix_tail_exact_weight = 800;
            var
                prefix_text_local: string;
                prefix_key_local: string;
                tail_key_local: string;
                tail_results_local: TncCandidateList;
                tail_idx_local: Integer;
                tail_text_local: string;
                tail_score_local: Integer;
                tail_weight_local: Integer;
                best_tail_text_local: string;
                best_tail_score_local: Integer;
                tail_units_local: TArray<string>;
                preferred_tail_head_local: string;
                fixed_tail_text_local: string;
                fixed_tail_path_local: string;
                prefix_is_de_particle_local: Boolean;
                prefix_is_connector_local: Boolean;
                prefix_is_fixed_boundary_local: Boolean;
                current_units_local: TArray<string>;
                current_tail_text_local: string;

                function current_tail_has_exact_support_local: Boolean;
                var
                    support_idx_local: Integer;
                    support_text_local: string;
                    support_weight_local: Integer;
                begin
                    Result := False;
                    if (current_tail_text_local = '') or
                        (Length(tail_results_local) = 0) then
                    begin
                        Exit;
                    end;

                    for support_idx_local := 0 to High(tail_results_local) do
                    begin
                        support_text_local := Trim(
                            tail_results_local[support_idx_local].text);
                        if not SameText(support_text_local,
                            current_tail_text_local) then
                        begin
                            Continue;
                        end;

                        support_weight_local := get_exact_chunk_raw_weight_local(
                            tail_results_local[support_idx_local]);
                        if support_weight_local > 0 then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                if (segment_query_offset_local < 0) or
                    (total_units_local < 3) or (total_units_local > 4) or
                    (segment_query_offset_local + total_units_local >
                    syllable_count_local) or (current_text_local = '') then
                begin
                    Exit;
                end;

                prefix_is_fixed_boundary_local := False;
                prefix_key_local := normalize_pinyin_text(
                    effective_syllables[segment_query_offset_local].text);
                if try_get_modifier_prefix_single_text_local(
                    effective_syllables[segment_query_offset_local].text,
                    prefix_text_local) then
                begin
                    if not is_structural_prefix_tail_shift_text_local(
                        prefix_text_local) then
                    begin
                        Exit;
                    end;
                end
                else
                begin
                    prefix_text_local := get_covering_fixed_single_text_local(
                        prefix_key_local);
                    if prefix_text_local = '' then
                    begin
                        Exit;
                    end;
                    prefix_is_fixed_boundary_local := True;
                end;
                prefix_is_de_particle_local :=
                    SameText(prefix_text_local, string(Char($7684))) or
                    SameText(prefix_text_local, string(Char($5730))) or
                    SameText(prefix_text_local, string(Char($5F97)));
                prefix_is_connector_local :=
                    SameText(prefix_text_local, string(Char($548C))) or
                    SameText(prefix_text_local, string(Char($4E0E)));

                tail_key_local := build_query_key_local(
                    segment_query_offset_local + 1, 2);
                if (tail_key_local = '') or
                    (not lookup_exact_full_pinyin_cached_local(tail_key_local,
                    tail_results_local)) then
                begin
                    Exit;
                end;
                if prefix_is_fixed_boundary_local then
                begin
                    current_units_local := split_text_units(current_text_local);
                    if Length(current_units_local) >= 3 then
                    begin
                        current_tail_text_local := current_units_local[1] +
                            current_units_local[2];
                    end
                    else
                    begin
                        current_tail_text_local := '';
                    end;
                    if current_tail_has_exact_support_local then
                    begin
                        Exit;
                    end;
                end;

                best_tail_text_local := '';
                best_tail_score_local := Low(Integer);
                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    if tail_idx_local >= c_chain_lattice_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                    if (tail_text_local = '') or
                        (get_candidate_text_unit_count(tail_text_local) <> 2) or
                        (not is_valid_standalone_cjk_phrase_text_local(
                        tail_text_local, 2)) then
                    begin
                        Continue;
                    end;

                    tail_weight_local := get_exact_chunk_raw_weight_local(
                        tail_results_local[tail_idx_local]);
                    if (tail_weight_local < c_min_tail_exact_weight) or
                        has_particle_tail_mismatch_local(tail_key_local,
                        tail_text_local, tail_weight_local) then
                    begin
                        Continue;
                    end;
                    if prefix_is_fixed_boundary_local and
                        (tail_weight_local <
                        c_min_fixed_prefix_tail_exact_weight) then
                    begin
                        Continue;
                    end;

                    tail_score_local := get_exact_chunk_effective_weight_local(
                        tail_results_local[tail_idx_local]);
                    tail_units_local := split_text_units(tail_text_local);
                    preferred_tail_head_local :=
                        get_fixed_boundary_single_char_for_standalone_local(
                        effective_syllables[segment_query_offset_local + 1].text);
                    if prefix_is_de_particle_local and
                        ((preferred_tail_head_local = '') or
                        (not SameText(tail_units_local[0],
                        preferred_tail_head_local))) then
                    begin
                        Continue;
                    end;
                    if prefix_is_connector_local and
                        (SameText(tail_units_local[1], string(Char($7684))) or
                        SameText(tail_units_local[1], string(Char($5730))) or
                        SameText(tail_units_local[1], string(Char($5F97)))) then
                    begin
                        Continue;
                    end;
                    if (Length(tail_units_local) = 2) and
                        (preferred_tail_head_local <> '') and
                        prefix_is_de_particle_local and
                        SameText(tail_units_local[0], preferred_tail_head_local) then
                    begin
                        Inc(tail_score_local, 640);
                    end;

                    if (best_tail_text_local = '') or
                        (tail_score_local > best_tail_score_local) then
                    begin
                        best_tail_text_local := tail_text_local;
                        best_tail_score_local := tail_score_local;
                    end;
                end;

                if best_tail_text_local = '' then
                begin
                    Exit;
                end;

                out_text_local := prefix_text_local + best_tail_text_local;
                out_path_local := prefix_text_local + c_segment_path_separator +
                    best_tail_text_local;

                if total_units_local = 4 then
                begin
                    if (not try_get_fixed_single_text_at_local(
                        segment_query_offset_local + 3, fixed_tail_text_local)) and
                        (not try_get_particle_tail_text_at_local(
                        segment_query_offset_local + 3, fixed_tail_text_local)) then
                    begin
                        Exit(False);
                    end;
                    if fixed_tail_text_local = '' then
                    begin
                        Exit(False);
                    end;
                    out_text_local := out_text_local + fixed_tail_text_local;
                    fixed_tail_path_local := fixed_tail_text_local;
                    out_path_local := out_path_local + c_segment_path_separator +
                        fixed_tail_path_local;
                end;

                Result := (out_text_local <> '') and
                    (not SameText(out_text_local, current_text_local));
            end;

            function try_repair_text_prefix_tail_shifts_local(
                const source_text_local: string; out out_text_local: string;
                out out_path_local: string): Boolean;
            var
                source_units_local: TArray<string>;
                unit_idx_local: Integer;
                current_window_local: string;
                repaired_window_text_local: string;
                repaired_window_path_local: string;

                procedure append_segment_local(const segment_text_local: string;
                    const segment_path_local: string);
                begin
                    if segment_text_local = '' then
                    begin
                        Exit;
                    end;
                    out_text_local := out_text_local + segment_text_local;
                    if out_path_local <> '' then
                    begin
                        out_path_local := out_path_local +
                            c_segment_path_separator;
                    end;
                    if segment_path_local <> '' then
                    begin
                        out_path_local := out_path_local + segment_path_local;
                    end
                    else
                    begin
                        out_path_local := out_path_local + segment_text_local;
                    end;
                end;

            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                source_units_local := split_text_units(source_text_local);
                if Length(source_units_local) <> syllable_count_local then
                begin
                    Exit;
                end;

                unit_idx_local := 0;
                while unit_idx_local < Length(source_units_local) do
                begin
                    if unit_idx_local + 4 <= Length(source_units_local) then
                    begin
                        current_window_local := source_units_local[unit_idx_local] +
                            source_units_local[unit_idx_local + 1] +
                            source_units_local[unit_idx_local + 2] +
                            source_units_local[unit_idx_local + 3];
                        if try_repair_prefix_tail_exact_shift_local(unit_idx_local,
                            4, current_window_local, repaired_window_text_local,
                            repaired_window_path_local) then
                        begin
                            append_segment_local(repaired_window_text_local,
                                repaired_window_path_local);
                            Inc(unit_idx_local, 4);
                            Result := True;
                            Continue;
                        end;
                    end;

                    if unit_idx_local + 3 <= Length(source_units_local) then
                    begin
                        current_window_local := source_units_local[unit_idx_local] +
                            source_units_local[unit_idx_local + 1] +
                            source_units_local[unit_idx_local + 2];
                        if try_repair_prefix_tail_exact_shift_local(unit_idx_local,
                            3, current_window_local, repaired_window_text_local,
                            repaired_window_path_local) then
                        begin
                            append_segment_local(repaired_window_text_local,
                                repaired_window_path_local);
                            Inc(unit_idx_local, 3);
                            Result := True;
                            Continue;
                        end;
                    end;

                    append_segment_local(source_units_local[unit_idx_local],
                        source_units_local[unit_idx_local]);
                    Inc(unit_idx_local);
                end;
            end;

            function try_repair_incremental_tail_prefix_shift_local(
                out out_text_local: string; out out_path_local: string): Boolean;
            const
                c_tail_units = 3;
            var
                source_units_local: TArray<string>;
                source_path_segments_local: TArray<string>;
                segment_idx_local: Integer;
                segment_text_local: string;
                segment_units_local: Integer;
                path_offset_local: Integer;
                tail_start_local: Integer;
                suffix_segment_count_local: Integer;
                prefix_text_local: string;
                prefix_path_local: string;
                current_tail_text_local: string;
                repaired_tail_text_local: string;
                repaired_tail_path_local: string;
                particle_text_local: string;
                tail_exact_key_local: string;
                tail_exact_score_local: Integer;
                unit_idx_local: Integer;
            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                source_units_local := split_text_units(Trim(source_state.text));
                if (Length(source_units_local) <> syllable_count_local) or
                    (Length(source_units_local) < c_tail_units) or
                    (Trim(source_state.path) = '') then
                begin
                    Exit;
                end;

                tail_start_local := Length(source_units_local) - c_tail_units;
                source_path_segments_local := source_state.path.Split(
                    [c_segment_path_separator]);
                path_offset_local := 0;
                suffix_segment_count_local := 0;
                prefix_path_local := '';
                for segment_idx_local := 0 to High(source_path_segments_local) do
                begin
                    segment_text_local := Trim(
                        source_path_segments_local[segment_idx_local]);
                    segment_units_local := get_candidate_text_unit_count(
                        segment_text_local);
                    if (segment_text_local = '') or (segment_units_local <= 0) or
                        (path_offset_local + segment_units_local >
                        Length(source_units_local)) then
                    begin
                        Exit(False);
                    end;

                    if path_offset_local < tail_start_local then
                    begin
                        { Incremental repair must not cut through an existing
                          exact segment. It only reconsiders the final boundary. }
                        if path_offset_local + segment_units_local >
                            tail_start_local then
                        begin
                            Exit(False);
                        end;
                        if prefix_path_local <> '' then
                        begin
                            prefix_path_local := prefix_path_local +
                                c_segment_path_separator;
                        end;
                        prefix_path_local := prefix_path_local + segment_text_local;
                    end
                    else
                    begin
                        Inc(suffix_segment_count_local);
                    end;
                    Inc(path_offset_local, segment_units_local);
                end;

                if (path_offset_local <> Length(source_units_local)) or
                    (suffix_segment_count_local < 2) then
                begin
                    Exit;
                end;

                prefix_text_local := '';
                for unit_idx_local := 0 to tail_start_local - 1 do
                begin
                    prefix_text_local := prefix_text_local +
                        source_units_local[unit_idx_local];
                end;
                current_tail_text_local := '';
                for unit_idx_local := tail_start_local to
                    High(source_units_local) do
                begin
                    current_tail_text_local := current_tail_text_local +
                        source_units_local[unit_idx_local];
                end;

                if not try_get_particle_tail_text_at_local(tail_start_local,
                    particle_text_local) then
                begin
                    Exit;
                end;
                tail_exact_key_local := build_query_key_local(
                    tail_start_local + 1, 2);
                if (tail_exact_key_local = '') or
                    (not try_get_best_exact_text_local(tail_exact_key_local, 2,
                    500, repaired_tail_text_local,
                    tail_exact_score_local)) then
                begin
                    Exit;
                end;
                repaired_tail_path_local := particle_text_local +
                    c_segment_path_separator + repaired_tail_text_local;
                repaired_tail_text_local := particle_text_local +
                    repaired_tail_text_local;
                if SameText(repaired_tail_text_local,
                    current_tail_text_local) then
                begin
                    Exit;
                end;

                out_text_local := prefix_text_local + repaired_tail_text_local;
                out_path_local := prefix_path_local;
                if (out_path_local <> '') and (repaired_tail_path_local <> '') then
                begin
                    out_path_local := out_path_local + c_segment_path_separator;
                end;
                out_path_local := out_path_local + repaired_tail_path_local;
                Result := (out_text_local <> '') and
                    (not SameText(out_text_local, source_state.text)) and
                    (get_candidate_text_unit_count(out_text_local) =
                    syllable_count_local);
            end;

            function try_repair_text_exact_span_splits_local(
                const source_text_local: string; const source_path_local: string;
                out out_text_local: string; out out_path_local: string): Boolean;
            var
                source_units_local: TArray<string>;
                path_segments_for_repair_local: TArray<string>;
                unit_idx_local: Integer;
                span_len_local: Integer;
                current_span_text_local: string;
                exact_text_local: string;
                span_repaired_local: Boolean;

                procedure append_segment_local(const segment_text_local: string);
                begin
                    if segment_text_local = '' then
                    begin
                        Exit;
                    end;
                    out_text_local := out_text_local + segment_text_local;
                    if out_path_local <> '' then
                    begin
                        out_path_local := out_path_local +
                            c_segment_path_separator;
                    end;
                    out_path_local := out_path_local + segment_text_local;
                end;

                function is_stable_sentence_single_unit_local(
                    const syllable_key_local: string;
                    const unit_text_local: string): Boolean;
                var
                    fixed_text_local: string;
                    modifier_text_local: string;
                begin
                    Result := False;
                    if (syllable_key_local = '') or
                        (get_candidate_text_unit_count(unit_text_local) <> 1) then
                    begin
                        Exit;
                    end;

                    fixed_text_local := get_covering_fixed_single_text_local(
                        syllable_key_local);
                    if (fixed_text_local <> '') and
                        SameText(fixed_text_local, unit_text_local) then
                    begin
                        Exit(True);
                    end;

                    if try_get_modifier_prefix_single_text_local(
                        syllable_key_local, modifier_text_local) and
                        SameText(modifier_text_local, unit_text_local) then
                    begin
                        Exit(True);
                    end;
                end;

                function span_crosses_repairable_path_boundary_local(
                    const span_start_local: Integer;
                    const span_len_value_local: Integer): Boolean;
                var
                    span_end_local: Integer;
                    path_idx_local: Integer;
                    path_offset_local: Integer;
                    path_units_local: Integer;
                    path_end_local: Integer;
                    covered_count_local: Integer;
                    has_multichar_segment_local: Boolean;
                    all_stable_single_segments_local: Boolean;
                    segment_text_local: string;
                    segment_key_local: string;
                    segment_exact_text_local: string;
                    segment_exact_score_local: Integer;
                    segment_units_local: TArray<string>;
                    span_key_local: string;
                    span_text_local: string;
                    span_has_exact_override_local: Boolean;
                    span_exact_text_local: string;
                    span_exact_score_local: Integer;

                    function span_exact_override_allows_current_boundary_local:
                        Boolean;
                    begin
                        Result := span_has_exact_override_local and
                            ((covered_count_local > 0) or
                            (path_end_local < span_end_local));
                    end;
                begin
                    Result := False;
                    if Trim(source_path_local) = '' then
                    begin
                        Exit;
                    end;

                    span_end_local := span_start_local + span_len_value_local;
                    span_key_local := build_query_key_local(span_start_local,
                        span_len_value_local);
                    span_text_local := '';
                    for path_idx_local := span_start_local to
                        span_start_local + span_len_value_local - 1 do
                    begin
                        span_text_local := span_text_local +
                            source_units_local[path_idx_local];
                    end;
                    span_has_exact_override_local :=
                        (span_len_value_local >= 3) and
                        try_get_better_exact_text_local(span_key_local,
                        span_len_value_local, span_text_local, 260,
                        span_exact_text_local, span_exact_score_local);

                    path_offset_local := 0;
                    covered_count_local := 0;
                    has_multichar_segment_local := False;
                    all_stable_single_segments_local := True;
                    for path_idx_local := 0 to High(path_segments_for_repair_local) do
                    begin
                        segment_text_local := Trim(
                            path_segments_for_repair_local[path_idx_local]);
                        path_units_local := get_candidate_text_unit_count(
                            segment_text_local);
                        if path_units_local <= 0 then
                        begin
                            Exit(False);
                        end;

                        path_end_local := path_offset_local + path_units_local;
                        if (path_offset_local < span_end_local) and
                            (path_end_local > span_start_local) then
                        begin
                            if (path_units_local >= 2) and
                                ((span_start_local > path_offset_local) or
                                (span_end_local < path_end_local)) then
                            begin
                                if path_units_local <= 3 then
                                begin
                                    segment_key_local := build_query_key_local(
                                        path_offset_local, path_units_local);
                                    if try_get_best_exact_text_local(
                                        segment_key_local, path_units_local,
                                        c_min_covering_pair_exact_weight,
                                        segment_exact_text_local,
                                        segment_exact_score_local) and
                                        SameText(segment_exact_text_local,
                                        segment_text_local) then
                                    begin
                                        if not span_exact_override_allows_current_boundary_local then
                                        begin
                                            Exit(False);
                                        end;
                                    end;
                                end;

                                if not ((span_start_local = path_offset_local + 1) and
                                    (span_end_local >= path_end_local) and
                                    (Length(split_text_units(segment_text_local)) =
                                    path_units_local)) then
                                begin
                                    if not span_exact_override_allows_current_boundary_local then
                                    begin
                                        Exit(False);
                                    end;
                                end;

                                if not span_exact_override_allows_current_boundary_local then
                                begin
                                    segment_units_local := split_text_units(
                                        segment_text_local);
                                    segment_key_local := build_query_key_local(
                                        path_offset_local, 1);
                                    if (Length(segment_units_local) = 0) or
                                        (not is_stable_sentence_single_unit_local(
                                        segment_key_local,
                                        segment_units_local[0])) then
                                    begin
                                        Exit(False);
                                    end;
                                end;
                            end;

                            Inc(covered_count_local);
                            if path_units_local >= 2 then
                            begin
                                has_multichar_segment_local := True;
                                all_stable_single_segments_local := False;
                            end
                            else
                            begin
                                segment_key_local := build_query_key_local(
                                    path_offset_local, 1);
                                if not is_stable_sentence_single_unit_local(
                                    segment_key_local, segment_text_local) then
                                begin
                                    all_stable_single_segments_local := False;
                                end;
                            end;
                        end;

                        path_offset_local := path_end_local;
                        if path_offset_local >= span_end_local then
                        begin
                            Break;
                        end;
                    end;

                    if covered_count_local < 2 then
                    begin
                        Exit(False);
                    end;

                    if (not has_multichar_segment_local) and
                        all_stable_single_segments_local and
                        (span_len_value_local >= 3) then
                    begin
                        if span_has_exact_override_local then
                        begin
                            Exit(True);
                        end;
                    end;

                    Result := has_multichar_segment_local or
                        (not all_stable_single_segments_local);
                end;

                function current_span_is_backed_path_segment_local(
                    const start_unit_idx_local: Integer;
                    const span_len_value_local: Integer;
                    const span_text_local: string): Boolean;
                var
                    local_path_idx: Integer;
                    local_path_offset: Integer;
                    local_segment_text: string;
                    local_segment_units: Integer;
                begin
                    Result := False;
                    if span_len_value_local <= 1 then
                    begin
                        Exit;
                    end;

                    local_path_offset := 0;
                    for local_path_idx := 0 to
                        High(path_segments_for_repair_local) do
                    begin
                        local_segment_text := Trim(
                            path_segments_for_repair_local[local_path_idx]);
                        local_segment_units := get_candidate_text_unit_count(
                            local_segment_text);
                        if local_segment_units <= 0 then
                        begin
                            Exit(False);
                        end;

                        if local_path_offset = start_unit_idx_local then
                        begin
                            if (local_segment_units = span_len_value_local) and
                                SameText(local_segment_text, span_text_local) then
                            begin
                                Result := True;
                            end;
                            Exit;
                        end;

                        Inc(local_path_offset, local_segment_units);
                        if local_path_offset > start_unit_idx_local then
                        begin
                            Exit;
                        end;
                    end;
                end;

                function try_find_exact_span_repair_at_unit_local(
                    const start_unit_idx_local: Integer;
                    const emit_debug_local: Boolean;
                    out out_span_len_local: Integer;
                    out out_exact_text_local: string): Boolean;
                var
                    probe_span_len_local: Integer;
                    probe_unit_idx_local: Integer;
                    probe_current_span_text_local: string;
                    probe_exact_key_local: string;
                    probe_exact_score_local: Integer;
                begin
                    Result := False;
                    out_span_len_local := 0;
                    out_exact_text_local := '';

                    for probe_span_len_local := Min(5,
                        Length(source_units_local) - start_unit_idx_local)
                        downto 2 do
                    begin
                        probe_current_span_text_local := '';
                        for probe_unit_idx_local := start_unit_idx_local to
                            start_unit_idx_local + probe_span_len_local - 1 do
                        begin
                            probe_current_span_text_local :=
                                probe_current_span_text_local +
                                source_units_local[probe_unit_idx_local];
                        end;

                        probe_exact_key_local := build_query_key_local(
                            start_unit_idx_local, probe_span_len_local);
                        if current_span_is_backed_path_segment_local(
                            start_unit_idx_local, probe_span_len_local,
                            probe_current_span_text_local) then
                        begin
                            Continue;
                        end;
                        if not span_crosses_repairable_path_boundary_local(
                            start_unit_idx_local, probe_span_len_local) then
                        begin
                            Continue;
                        end;
                        if try_get_better_exact_text_local(probe_exact_key_local,
                            probe_span_len_local, probe_current_span_text_local,
                            IfThen(probe_span_len_local >= 4, 700,
                            c_min_covering_pair_exact_weight),
                            out_exact_text_local, probe_exact_score_local) then
                        begin
                            out_span_len_local := probe_span_len_local;
                            Exit(True);
                        end;
                    end;
                end;

                function get_path_segment_starting_at_unit_local(
                    const start_unit_idx_local: Integer;
                    out out_segment_text_local: string;
                    out out_segment_units_local: Integer): Boolean;
                var
                    local_path_idx: Integer;
                    local_path_offset: Integer;
                    local_segment_text: string;
                    local_segment_units: Integer;
                begin
                    Result := False;
                    out_segment_text_local := '';
                    out_segment_units_local := 0;
                    local_path_offset := 0;
                    for local_path_idx := 0 to
                        High(path_segments_for_repair_local) do
                    begin
                        local_segment_text := Trim(
                            path_segments_for_repair_local[local_path_idx]);
                        local_segment_units :=
                            get_candidate_text_unit_count(local_segment_text);
                        if local_segment_units <= 0 then
                        begin
                            Exit(False);
                        end;
                        if local_path_offset = start_unit_idx_local then
                        begin
                            out_segment_text_local := local_segment_text;
                            out_segment_units_local := local_segment_units;
                            Exit(True);
                        end;
                        Inc(local_path_offset, local_segment_units);
                        if local_path_offset > start_unit_idx_local then
                        begin
                            Exit(False);
                        end;
                    end;
                end;

                function has_repair_start_inside_segment_local(
                    const segment_start_unit_local: Integer;
                    const segment_units_local: Integer): Boolean;
                var
                    scan_unit_idx_local: Integer;
                    ignored_span_len_local: Integer;
                    ignored_exact_text_local: string;
                begin
                    Result := False;
                    if segment_units_local <= 1 then
                    begin
                        Exit;
                    end;
                    for scan_unit_idx_local := segment_start_unit_local + 1 to
                        segment_start_unit_local + segment_units_local - 1 do
                    begin
                        if try_find_exact_span_repair_at_unit_local(
                            scan_unit_idx_local, False, ignored_span_len_local,
                            ignored_exact_text_local) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

            begin
                Result := False;
                out_text_local := '';
                out_path_local := '';
                source_units_local := split_text_units(source_text_local);
                if Length(source_units_local) <> syllable_count_local then
                begin
                    Exit;
                end;
                path_segments_for_repair_local := source_path_local.Split(
                    [c_segment_path_separator]);

                unit_idx_local := 0;
                while unit_idx_local < Length(source_units_local) do
                begin
                    span_repaired_local := False;
                    if try_find_exact_span_repair_at_unit_local(unit_idx_local,
                        True, span_len_local, exact_text_local) then
                    begin
                        append_segment_local(exact_text_local);
                        Inc(unit_idx_local, span_len_local);
                        span_repaired_local := True;
                        Result := True;
                    end;

                    if span_repaired_local then
                    begin
                        Continue;
                    end;

                    if get_path_segment_starting_at_unit_local(unit_idx_local,
                        current_span_text_local, span_len_local) and
                        (span_len_local > 1) and
                        (not has_repair_start_inside_segment_local(
                        unit_idx_local, span_len_local)) then
                    begin
                        append_segment_local(current_span_text_local);
                        Inc(unit_idx_local, span_len_local);
                        Continue;
                    end;

                    append_segment_local(source_units_local[unit_idx_local]);
                    Inc(unit_idx_local);
                end;
            end;

        begin
            Result := False;
            repaired_state := source_state;
            if source_state.text = '' then
            begin
                Exit;
            end;

            if incremental_tail_only then
            begin
                if not try_repair_incremental_tail_prefix_shift_local(
                    repaired_text_local, repaired_path_local) then
                begin
                    Exit;
                end;
                if repaired_text_rewrites_directional_place_span_local or
                    repaired_text_rewrites_protected_source_exact_span_local then
                begin
                    Exit;
                end;

                repaired_state.text := repaired_text_local;
                repaired_state.path := repaired_path_local;
                repaired_state.score := source_state.score + c_repair_bonus;
                repaired_state.segments := Max(1,
                    get_encoded_path_segment_count_local(repaired_path_local));
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xfasttailrepair=[%s>%s]',
                        [source_state.text, repaired_state.text]);
                end;
                Exit(True);
            end;

            if (source_state.path <> '') and
                try_repair_text_exact_span_splits_local(source_state.text,
                source_state.path, repaired_text_local,
                repaired_path_local) and
                (not SameText(repaired_text_local, source_state.text)) then
            begin
                chained_repaired_text_local := '';
                chained_repaired_path_local := '';
                if try_repair_text_prefix_tail_shifts_local(repaired_text_local,
                    chained_repaired_text_local, chained_repaired_path_local) and
                    (not SameText(chained_repaired_text_local,
                    repaired_text_local)) then
                begin
                    repaired_text_local := chained_repaired_text_local;
                    repaired_path_local := chained_repaired_path_local;
                end;

                if (not repaired_text_rewrites_directional_place_span_local) and
                    (not repaired_text_rewrites_protected_source_exact_span_local) then
                begin
                    repaired_state := source_state;
                    repaired_state.text := repaired_text_local;
                    repaired_state.path := repaired_path_local;
                    repaired_state.score := source_state.score + c_repair_bonus;
                    repaired_state.segments := Max(1, source_state.segments - 1);
                    Result := True;
                    Exit;
                end;
            end;

            if try_repair_text_prefix_tail_shifts_local(source_state.text,
                repaired_text_local, repaired_path_local) and
                (not SameText(repaired_text_local, source_state.text)) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xprefshift=[%s>%s]',
                        [source_state.text, repaired_text_local]);
                end;
                chained_repaired_text_local := '';
                chained_repaired_path_local := '';
                if try_repair_text_exact_span_splits_local(repaired_text_local,
                    repaired_path_local, chained_repaired_text_local,
                    chained_repaired_path_local) and
                    (not SameText(chained_repaired_text_local,
                    repaired_text_local)) then
                begin
                    repaired_text_local := chained_repaired_text_local;
                    repaired_path_local := chained_repaired_path_local;
                end;

                if (not repaired_text_rewrites_directional_place_span_local) and
                    (not repaired_text_rewrites_protected_source_exact_span_local) then
                begin
                    repaired_state := source_state;
                    repaired_state.text := repaired_text_local;
                    repaired_state.path := repaired_path_local;
                    repaired_state.score := source_state.score + c_repair_bonus;
                    repaired_state.segments := Max(1, source_state.segments - 1);
                    Result := True;
                    Exit;
                end;
            end;

            if source_state.path = '' then
            begin
                Exit;
            end;

            path_segments_local := source_state.path.Split(
                [c_segment_path_separator]);
            if Length(path_segments_local) < 2 then
            begin
                Exit;
            end;

            query_offset_local := 0;
            repair_count_local := 0;
            repair_probe_budget_local := 8;
            repaired_text_local := '';
            repaired_path_local := '';
            repair_used_prefix_tail_shift_local := False;
            repair_used_boundary_shift_local := False;
            segment_idx_local := 0;
            while segment_idx_local <= High(path_segments_local) do
            begin
                current_text_local := Trim(path_segments_local[segment_idx_local]);
                current_units_local := get_candidate_text_unit_count(
                    current_text_local);
                if (current_text_local = '') or (current_units_local <= 0) or
                    (query_offset_local + current_units_local >
                    syllable_count_local) then
                begin
                    Exit(False);
                end;

                if try_repair_quantity_fixed_segment_local(query_offset_local,
                    current_units_local, current_text_local,
                    best_exact_text_local, combined_text_local) then
                begin
                    repaired_text_local := repaired_text_local +
                        best_exact_text_local;
                    if repaired_path_local <> '' then
                    begin
                        repaired_path_local := repaired_path_local +
                            c_segment_path_separator;
                    end;
                    repaired_path_local := repaired_path_local +
                        combined_text_local;
                    Inc(query_offset_local, current_units_local);
                    Inc(segment_idx_local);
                    Inc(repair_count_local);
                    Continue;
                end;

                if try_repair_particle_tail_segment_local(query_offset_local,
                    current_units_local, current_text_local,
                    best_exact_text_local, combined_text_local) then
                begin
                    repaired_text_local := repaired_text_local +
                        best_exact_text_local;
                    if repaired_path_local <> '' then
                    begin
                        repaired_path_local := repaired_path_local +
                            c_segment_path_separator;
                    end;
                    repaired_path_local := repaired_path_local +
                        combined_text_local;
                    Inc(query_offset_local, current_units_local);
                    Inc(segment_idx_local);
                    Inc(repair_count_local);
                    Continue;
                end;

                if segment_idx_local < High(path_segments_local) then
                begin
                    next_text_local := Trim(
                        path_segments_local[segment_idx_local + 1]);
                    next_units_local := get_candidate_text_unit_count(
                        next_text_local);
                    if (segment_idx_local + 2 <= High(path_segments_local)) and
                        (current_units_local = 1) and
                        (next_units_local = 2) then
                    begin
                        third_text_local := Trim(
                            path_segments_local[segment_idx_local + 2]);
                        third_units_local := get_candidate_text_unit_count(
                            third_text_local);
                        if (third_units_local = 1) and
                            try_repair_one_two_one_boundary_shift_local(
                            query_offset_local, current_text_local,
                            next_units_local, next_text_local, third_text_local,
                            best_exact_text_local, combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, current_units_local +
                                next_units_local + third_units_local);
                            Inc(segment_idx_local, 3);
                            Inc(repair_count_local);
                            repair_used_boundary_shift_local := True;
                            Continue;
                        end;
                        if (third_units_local = 2) and
                            (segment_idx_local + 3 <= High(path_segments_local)) then
                        begin
                            fourth_text_local := Trim(
                                path_segments_local[segment_idx_local + 3]);
                            fourth_units_local := get_candidate_text_unit_count(
                                fourth_text_local);
                            if (fourth_units_local = 1) and
                                try_repair_one_two_two_one_boundary_shift_local(
                                query_offset_local, current_text_local,
                                next_text_local, third_text_local,
                                fourth_text_local, best_exact_text_local,
                                combined_text_local) then
                            begin
                                repaired_text_local := repaired_text_local +
                                    best_exact_text_local;
                                if repaired_path_local <> '' then
                                begin
                                    repaired_path_local := repaired_path_local +
                                        c_segment_path_separator;
                                end;
                                repaired_path_local := repaired_path_local +
                                    combined_text_local;
                                Inc(query_offset_local, current_units_local +
                                    next_units_local + third_units_local +
                                    fourth_units_local);
                                Inc(segment_idx_local, 4);
                                Inc(repair_count_local);
                                repair_used_boundary_shift_local := True;
                                Continue;
                            end;
                        end;
                        if (third_units_local = 2) and
                            try_repair_one_two_two_boundary_shift_local(
                            query_offset_local, current_text_local,
                            next_units_local, next_text_local, third_units_local,
                            third_text_local, best_exact_text_local,
                            combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, current_units_local +
                                next_units_local + third_units_local);
                            Inc(segment_idx_local, 3);
                            Inc(repair_count_local);
                            repair_used_boundary_shift_local := True;
                            Continue;
                        end;
                    end;
                    if (segment_idx_local + 1 <= High(path_segments_local)) and
                        ((current_units_local + next_units_local = 3) or
                        (current_units_local + next_units_local = 4)) then
                    begin
                        if (segment_idx_local + 2 <= High(path_segments_local)) and
                            (current_units_local = 2) and
                            (next_units_local = 1) then
                        begin
                            third_text_local := Trim(
                                path_segments_local[segment_idx_local + 2]);
                            third_units_local := get_candidate_text_unit_count(
                                third_text_local);
                            if (third_units_local = 1) and
                                try_repair_two_one_one_boundary_shift_local(
                                query_offset_local, current_units_local,
                                current_text_local, next_text_local,
                                third_text_local, best_exact_text_local,
                                combined_text_local) then
                            begin
                                repaired_text_local := repaired_text_local +
                                    best_exact_text_local;
                                if repaired_path_local <> '' then
                                begin
                                    repaired_path_local := repaired_path_local +
                                        c_segment_path_separator;
                                end;
                                repaired_path_local := repaired_path_local +
                                    combined_text_local;
                                Inc(query_offset_local, current_units_local +
                                    next_units_local + third_units_local);
                                Inc(segment_idx_local, 3);
                                Inc(repair_count_local);
                                repair_used_boundary_shift_local := True;
                                Continue;
                            end;
                        end;

                        combined_text_local := current_text_local + next_text_local;
                        if try_repair_adjacent_split_phrase_local(
                            query_offset_local, current_units_local,
                            current_text_local, next_units_local, next_text_local,
                            best_exact_text_local, combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, current_units_local +
                                next_units_local);
                            Inc(segment_idx_local, 2);
                            Inc(repair_count_local);
                            Continue;
                        end;
                        if try_repair_prefix_tail_exact_shift_local(
                            query_offset_local,
                            current_units_local + next_units_local,
                            combined_text_local, best_exact_text_local,
                            combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, current_units_local +
                                next_units_local);
                            Inc(segment_idx_local, 2);
                            Inc(repair_count_local);
                            repair_used_prefix_tail_shift_local := True;
                            Continue;
                        end;
                    end;
                    if (segment_idx_local + 2 <= High(path_segments_local)) and
                        (current_units_local = 1) and (next_units_local = 1) then
                    begin
                        third_text_local := Trim(
                            path_segments_local[segment_idx_local + 2]);
                        third_units_local := get_candidate_text_unit_count(
                            third_text_local);
                        if third_units_local = 1 then
                        begin
                            combined_text_local := current_text_local +
                                next_text_local + third_text_local;
                            if try_repair_adjacent_split_phrase_local(
                                query_offset_local, 2,
                                current_text_local + next_text_local, 1,
                                third_text_local, best_exact_text_local,
                                combined_text_local) then
                            begin
                                repaired_text_local := repaired_text_local +
                                    best_exact_text_local;
                                if repaired_path_local <> '' then
                                begin
                                    repaired_path_local := repaired_path_local +
                                        c_segment_path_separator;
                                end;
                                repaired_path_local := repaired_path_local +
                                    combined_text_local;
                                Inc(query_offset_local, 3);
                                Inc(segment_idx_local, 3);
                                Inc(repair_count_local);
                                Continue;
                            end;
                            if try_repair_prefix_tail_exact_shift_local(
                                query_offset_local, 3, combined_text_local,
                                best_exact_text_local, combined_text_local) then
                            begin
                                repaired_text_local := repaired_text_local +
                                    best_exact_text_local;
                                if repaired_path_local <> '' then
                                begin
                                    repaired_path_local := repaired_path_local +
                                        c_segment_path_separator;
                                end;
                                repaired_path_local := repaired_path_local +
                                    combined_text_local;
                                Inc(query_offset_local, 3);
                                Inc(segment_idx_local, 3);
                                Inc(repair_count_local);
                                repair_used_prefix_tail_shift_local := True;
                                Continue;
                            end;
                        end;
                    end;
                    if (next_text_local <> '') and (next_units_local > 0) and
                        ((current_units_local = 1) or (next_units_local = 1)) and
                        (current_units_local + next_units_local <= 3) and
                        (query_offset_local + current_units_local +
                        next_units_local <= syllable_count_local) then
                    begin
                        if try_repair_adjacent_split_phrase_local(
                            query_offset_local, current_units_local,
                            current_text_local, next_units_local, next_text_local,
                            best_exact_text_local, combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, current_units_local +
                                next_units_local);
                            Inc(segment_idx_local, 2);
                            Inc(repair_count_local);
                            Continue;
                        end;

                        combined_key_local := build_query_key_local(
                            query_offset_local, current_units_local +
                            next_units_local);
                        combined_text_local := current_text_local + next_text_local;
                        if repair_probe_budget_local <= 0 then
                        begin
                            append_repaired_segment_local(current_text_local);
                            Inc(query_offset_local, current_units_local);
                            Inc(segment_idx_local);
                            Continue;
                        end;
                        Dec(repair_probe_budget_local);
                        if (current_units_local = 1) and (next_units_local = 1) and
                            (segment_idx_local + 2 <= High(path_segments_local)) then
                        begin
                            third_text_local := Trim(
                                path_segments_local[segment_idx_local + 2]);
                            third_units_local := get_candidate_text_unit_count(
                                third_text_local);
                            if (third_text_local <> '') and
                                (third_units_local = 1) and
                                (query_offset_local + 3 <= syllable_count_local) then
                            begin
                                combined_key_local := build_query_key_local(
                                    query_offset_local, 3);
                                combined_text_local := current_text_local +
                                    next_text_local + third_text_local;
                                if try_get_better_exact_text_local(
                                    combined_key_local, 3, combined_text_local,
                                    c_min_single_chain_triple_exact_weight,
                                    best_exact_text_local,
                                    best_exact_score_local) then
                                begin
                                    append_repaired_segment_local(
                                        best_exact_text_local);
                                    Inc(query_offset_local, 3);
                                    Inc(segment_idx_local, 3);
                                    Inc(repair_count_local);
                                    Continue;
                                end;
                            end;
                        end;
                        if try_get_better_exact_text_local(combined_key_local,
                            current_units_local + next_units_local,
                            combined_text_local, c_min_covering_pair_exact_weight,
                            best_exact_text_local, best_exact_score_local) then
                        begin
                            append_repaired_segment_local(best_exact_text_local);
                            Inc(query_offset_local, current_units_local +
                                next_units_local);
                            Inc(segment_idx_local, 2);
                            Inc(repair_count_local);
                            Continue;
                        end;
                    end;
                    if (next_text_local <> '') and (next_units_local > 0) and
                        (current_units_local = 2) and (next_units_local = 2) and
                        (query_offset_local + 4 <= syllable_count_local) then
                    begin
                        if repair_probe_budget_local <= 0 then
                        begin
                            append_repaired_segment_local(current_text_local);
                            Inc(query_offset_local, current_units_local);
                            Inc(segment_idx_local);
                            Continue;
                        end;
                        Dec(repair_probe_budget_local);
                        if try_repair_adjacent_split_phrase_local(
                            query_offset_local, current_units_local,
                            current_text_local, next_units_local, next_text_local,
                            best_exact_text_local, combined_text_local) then
                        begin
                            repaired_text_local := repaired_text_local +
                                best_exact_text_local;
                            if repaired_path_local <> '' then
                            begin
                                repaired_path_local := repaired_path_local +
                                    c_segment_path_separator;
                            end;
                            repaired_path_local := repaired_path_local +
                                combined_text_local;
                            Inc(query_offset_local, 4);
                            Inc(segment_idx_local, 2);
                            Inc(repair_count_local);
                            Continue;
                        end;
                    end;
                end;

                append_repaired_segment_local(current_text_local);
                Inc(query_offset_local, current_units_local);
                Inc(segment_idx_local);
            end;

            if (repair_count_local <= 0) or
                (query_offset_local <> syllable_count_local) or
                SameText(repaired_text_local, source_state.text) then
            begin
                chained_repaired_text_local := '';
                chained_repaired_path_local := '';
                if try_repair_text_exact_span_splits_local(source_state.text,
                    source_state.path, chained_repaired_text_local,
                    chained_repaired_path_local) and
                    (not SameText(chained_repaired_text_local,
                    source_state.text)) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' xspansplit=[%s>%s]',
                            [source_state.text, chained_repaired_text_local]);
                    end;
                    repaired_text_local := chained_repaired_text_local;
                    repaired_path_local := chained_repaired_path_local;
                    repair_count_local := 1;
                end
                else
                begin
                    Exit(False);
                end;
            end
            else
            begin
                chained_repaired_text_local := '';
                chained_repaired_path_local := '';
                if try_repair_text_exact_span_splits_local(repaired_text_local,
                    repaired_path_local, chained_repaired_text_local,
                    chained_repaired_path_local) and
                    (not SameText(chained_repaired_text_local,
                    repaired_text_local)) then
                begin
                    repaired_text_local := chained_repaired_text_local;
                    repaired_path_local := chained_repaired_path_local;
                    Inc(repair_count_local);
                end;
            end;

            if repaired_text_rewrites_protected_source_exact_span_local then
            begin
                Exit(False);
            end;

            if (not repair_used_prefix_tail_shift_local) and
                (not repair_used_boundary_shift_local) and
                repaired_text_rewrites_directional_place_span_local then
            begin
                Exit(False);
            end;

            repaired_state := source_state;
            repaired_state.text := repaired_text_local;
            repaired_state.path := repaired_path_local;
            repaired_state.score := source_state.score +
                repair_count_local * c_repair_bonus;
            repaired_state.segments := Max(1,
                source_state.segments - repair_count_local);
            Result := True;
        end;

        function get_fast_state_path_exact_quality_local(
            const local_state: TExactChunkChainState): Integer;
        const
            c_path_exact_probe_limit = c_chain_probe_limit * 4;
            c_path_exact_weight_cap = 12000;
        var
            path_segments_local: TArray<string>;
            segment_idx_local: Integer;
            query_offset_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            query_key_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_weight_local: Integer;
            segment_is_exact_local: Boolean;
        begin
            Result := 0;
            if Trim(local_state.path) = '' then
            begin
                Exit(Low(Integer) div 4);
            end;

            path_segments_local := local_state.path.Split([c_segment_path_separator]);
            query_offset_local := 0;
            for segment_idx_local := 0 to High(path_segments_local) do
            begin
                segment_text_local := Trim(path_segments_local[segment_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) or
                    (query_offset_local + segment_units_local >
                    syllable_count_local) then
                begin
                    Exit(Low(Integer) div 4);
                end;

                query_key_local := build_query_key_local(query_offset_local,
                    segment_units_local);
                segment_is_exact_local := False;
                exact_weight_local := 0;
                if (query_key_local <> '') and
                    lookup_exact_full_pinyin_cached_local(query_key_local,
                    exact_results_local) then
                begin
                    for exact_idx_local := 0 to High(exact_results_local) do
                    begin
                        if exact_idx_local >= c_path_exact_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;

                        exact_text_local := Trim(
                            exact_results_local[exact_idx_local].text);
                        if SameText(exact_text_local, segment_text_local) and
                            (get_candidate_text_unit_count(exact_text_local) =
                            segment_units_local) then
                        begin
                            segment_is_exact_local := True;
                            exact_weight_local := Max(exact_weight_local,
                                get_exact_chunk_raw_weight_local(
                                exact_results_local[exact_idx_local]));
                            if is_reliable_fast_exact_chunk_local(query_key_local,
                                segment_units_local, segment_text_local,
                                exact_results_local[exact_idx_local]) then
                            begin
                                Inc(exact_weight_local, 180);
                            end;
                        end;
                    end;
                end;

                if segment_is_exact_local then
                begin
                    Inc(Result, segment_units_local * segment_units_local * 2200);
                    Inc(Result, Min(c_path_exact_weight_cap,
                        Max(0, exact_weight_local) * 6));
                    if segment_units_local >= 2 then
                    begin
                        Inc(Result, segment_units_local * 900);
                    end;
                end
                else if segment_units_local >= 2 then
                begin
                    Dec(Result, segment_units_local * segment_units_local * 2600 +
                        3600);
                end
                else
                begin
                    Dec(Result, 280);
                end;

                Inc(query_offset_local, segment_units_local);
            end;

            if query_offset_local <> syllable_count_local then
            begin
                Exit(Low(Integer) div 4);
            end;
            Dec(Result, Max(0, local_state.segments) * 120);
        end;

        function get_fast_reliable_exact_span_weight_local(
            const start_idx_local: Integer; const span_units_local: Integer;
            const span_text_local: string): Integer;
        const
            c_span_probe_limit = c_chain_probe_limit * 4;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := 0;
            if (span_text_local = '') or (span_units_local < 2) or
                (start_idx_local < 0) or
                (start_idx_local + span_units_local > syllable_count_local) then
            begin
                Exit;
            end;

            query_key_local := build_query_key_local(start_idx_local,
                span_units_local);
            if (query_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if exact_idx_local >= c_span_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if SameText(exact_text_local, span_text_local) and
                    (get_candidate_text_unit_count(exact_text_local) =
                    span_units_local) and
                    is_reliable_fast_exact_chunk_local(query_key_local,
                    span_units_local, span_text_local,
                    exact_results_local[exact_idx_local]) then
                begin
                    Result := Max(Result, get_exact_chunk_raw_weight_local(
                        exact_results_local[exact_idx_local]));
                end;
            end;
        end;

        function fast_repair_breaks_strong_source_exact_chain_local(
            const source_state_local: TExactChunkChainState;
            const repaired_state_local: TExactChunkChainState): Boolean;
        const
            c_strong_source_exact_weight = 700;
            c_same_span_slack = 160;
            c_source_pair_slack = 80;
        var
            source_units_local: TArray<string>;
            repaired_units_local: TArray<string>;
            path_segments_local: TArray<string>;
            segment_idx_local: Integer;
            query_offset_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            source_span_text_local: string;
            repaired_span_text_local: string;
            source_weight_local: Integer;
            repaired_weight_local: Integer;
            previous_lost_end_local: Integer;
            unit_idx_local: Integer;

            function build_units_range_text_local(const units_value: TArray<string>;
                const range_start: Integer; const range_len: Integer): string;
            var
                range_idx_local: Integer;
            begin
                Result := '';
                if (range_start < 0) or (range_len <= 0) or
                    (range_start + range_len > Length(units_value)) then
                begin
                    Exit;
                end;
                for range_idx_local := range_start to range_start + range_len - 1 do
                begin
                    Result := Result + units_value[range_idx_local];
                end;
            end;

            function get_fast_exact_span_weight_local(
                const start_idx_local: Integer; const span_units_local: Integer;
                const span_text_local: string): Integer;
            const
                c_span_probe_limit = c_chain_probe_limit * 4;
            var
                query_key_local: string;
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := 0;
                if (span_text_local = '') or (span_units_local < 2) or
                    (start_idx_local < 0) or
                    (start_idx_local + span_units_local > syllable_count_local) then
                begin
                    Exit;
                end;

                query_key_local := build_query_key_local(start_idx_local,
                    span_units_local);
                if (query_key_local = '') or
                    (not lookup_exact_full_pinyin_cached_local(query_key_local,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= c_span_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if SameText(exact_text_local, span_text_local) and
                        (get_candidate_text_unit_count(exact_text_local) =
                        span_units_local) then
                    begin
                        Result := Max(Result, get_exact_chunk_raw_weight_local(
                            exact_results_local[exact_idx_local]));
                    end;
                end;
            end;

        begin
            Result := False;
            source_units_local := split_text_units(Trim(source_state_local.text));
            repaired_units_local := split_text_units(Trim(repaired_state_local.text));
            if (Length(source_units_local) <> syllable_count_local) or
                (Length(repaired_units_local) <> syllable_count_local) or
                (Trim(source_state_local.path) = '') then
            begin
                Exit;
            end;

            path_segments_local := source_state_local.path.Split(
                [c_segment_path_separator]);
            query_offset_local := 0;
            previous_lost_end_local := -1;
            for segment_idx_local := 0 to High(path_segments_local) do
            begin
                segment_text_local := Trim(path_segments_local[segment_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) or
                    (query_offset_local + segment_units_local >
                    syllable_count_local) then
                begin
                    Exit(False);
                end;

                if segment_units_local >= 2 then
                begin
                    source_span_text_local := build_units_range_text_local(
                        source_units_local, query_offset_local,
                        segment_units_local);
                    if SameText(source_span_text_local, segment_text_local) then
                    begin
                        source_weight_local :=
                            get_fast_reliable_exact_span_weight_local(
                            query_offset_local, segment_units_local,
                            segment_text_local);
                        if source_weight_local >= c_strong_source_exact_weight then
                        begin
                            repaired_span_text_local := build_units_range_text_local(
                                repaired_units_local, query_offset_local,
                                segment_units_local);
                            if not SameText(repaired_span_text_local,
                                segment_text_local) then
                            begin
                                repaired_weight_local :=
                                    get_fast_reliable_exact_span_weight_local(
                                    query_offset_local, segment_units_local,
                                    repaired_span_text_local);
                                if repaired_weight_local + c_same_span_slack <
                                    source_weight_local then
                                begin
                                    if previous_lost_end_local = query_offset_local then
                                    begin
                                        Exit(True);
                                    end;
                                    previous_lost_end_local := query_offset_local +
                                        segment_units_local;
                                end;
                            end;
                        end;
                    end;
                end;

                Inc(query_offset_local, segment_units_local);
            end;

            if query_offset_local <> syllable_count_local then
            begin
                Exit(False);
            end;

            // Preserve strong two-character phrases that the fast DP already
            // decoded as adjacent single units. This prevents a later repair
            // pass from merging them into a weaker homophone solely because
            // the merged path has fewer segments.
            for unit_idx_local := 0 to syllable_count_local - 2 do
            begin
                source_span_text_local := build_units_range_text_local(
                    source_units_local, unit_idx_local, 2);
                repaired_span_text_local := build_units_range_text_local(
                    repaired_units_local, unit_idx_local, 2);
                if SameText(source_span_text_local, repaired_span_text_local) then
                begin
                    Continue;
                end;

                source_weight_local := get_fast_exact_span_weight_local(
                    unit_idx_local, 2, source_span_text_local);
                if source_weight_local < c_strong_source_exact_weight then
                begin
                    Continue;
                end;

                repaired_weight_local := get_fast_exact_span_weight_local(
                    unit_idx_local, 2, repaired_span_text_local);
                if repaired_weight_local + c_source_pair_slack <
                    source_weight_local then
                begin
                    Exit(True);
                end;
            end;
        end;

        function should_promote_fast_repair_state_local(
            const source_state_local: TExactChunkChainState;
            const repaired_state_local: TExactChunkChainState): Boolean;
        const
            c_min_repair_path_quality_gain = 800;
            c_min_long_exact_span_weight = 260;
        var
            source_quality_local: Integer;
            repaired_quality_local: Integer;
            source_long_exact_units_local: Integer;
            repaired_long_exact_units_local: Integer;

            function get_long_exact_units_local(
                const local_state: TExactChunkChainState): Integer;
            var
                local_segments: TArray<string>;
                local_segment_idx: Integer;
                local_query_offset: Integer;
                local_segment_text: string;
                local_segment_units: Integer;
                local_segment_weight: Integer;

                function get_raw_exact_span_weight_local(
                    const local_start: Integer; const local_len: Integer;
                    const local_text: string): Integer;
                var
                    local_key: string;
                    local_results: TncCandidateList;
                    local_idx: Integer;
                    local_candidate_text: string;
                begin
                    Result := 0;
                    local_key := build_query_key_local(local_start, local_len);
                    if (local_key = '') or
                        (not lookup_exact_full_pinyin_cached_local(local_key,
                        local_results)) then
                    begin
                        Exit;
                    end;

                    for local_idx := 0 to High(local_results) do
                    begin
                        if Trim(local_results[local_idx].comment) <> '' then
                        begin
                            Continue;
                        end;

                        local_candidate_text := Trim(local_results[local_idx].text);
                        if SameText(local_candidate_text, local_text) and
                            (get_candidate_text_unit_count(local_candidate_text) =
                            local_len) then
                        begin
                            Result := Max(Result,
                                get_exact_chunk_raw_weight_local(
                                local_results[local_idx]));
                        end;
                    end;
                end;
            begin
                Result := 0;
                if Trim(local_state.path) = '' then
                begin
                    Exit;
                end;

                local_segments := local_state.path.Split([c_segment_path_separator]);
                local_query_offset := 0;
                for local_segment_idx := 0 to High(local_segments) do
                begin
                    local_segment_text := Trim(local_segments[local_segment_idx]);
                    local_segment_units :=
                        get_candidate_text_unit_count(local_segment_text);
                    if (local_segment_text = '') or (local_segment_units <= 0) or
                        (local_query_offset + local_segment_units >
                        syllable_count_local) then
                    begin
                        Exit(0);
                    end;

                    if local_segment_units >= 3 then
                    begin
                        local_segment_weight := get_raw_exact_span_weight_local(
                            local_query_offset, local_segment_units,
                            local_segment_text);
                        if local_segment_weight >= c_min_long_exact_span_weight then
                        begin
                            Inc(Result, local_segment_units);
                        end;
                    end;

                    Inc(local_query_offset, local_segment_units);
                end;

                if local_query_offset <> syllable_count_local then
                begin
                    Result := 0;
                end;
            end;
        begin
            Result := False;
            if SameText(Trim(repaired_state_local.text),
                Trim(source_state_local.text)) then
            begin
                Exit;
            end;
            if repaired_state_local.score <= source_state_local.score +
                c_fast_repair_promote_margin then
            begin
                Exit;
            end;

            source_long_exact_units_local :=
                get_long_exact_units_local(source_state_local);
            repaired_long_exact_units_local :=
                get_long_exact_units_local(repaired_state_local);
            if (repaired_state_local.segments <= source_state_local.segments) and
                (repaired_long_exact_units_local >=
                source_long_exact_units_local + 3) then
            begin
                Exit(True);
            end;

            source_quality_local :=
                get_fast_state_path_exact_quality_local(source_state_local);
            repaired_quality_local :=
                get_fast_state_path_exact_quality_local(repaired_state_local);

            if fast_repair_breaks_strong_source_exact_chain_local(
                source_state_local, repaired_state_local) and
                (repaired_quality_local < source_quality_local +
                c_min_repair_path_quality_gain) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        ' xfastrepair_skip=[strong-exact-chain]';
                end;
                Exit;
            end;

            if repaired_quality_local < source_quality_local +
                c_min_repair_path_quality_gain then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xfastrepair_skip=[quality %d/%d]',
                        [source_quality_local, repaired_quality_local]);
                end;
                Exit;
            end;

            Result := True;
        end;

        procedure upsert_fast_state_candidate_local(
            const local_state: TExactChunkChainState; const target_rank: Integer);
        var
            local_existing_index: Integer;
            local_idx: Integer;
            local_insert_rank: Integer;
            local_candidate: TncCandidate;
            local_text: string;
            local_score: Integer;
        begin
            local_text := Trim(local_state.text);
            if (local_text = '') or
                (get_candidate_text_unit_count(local_text) <> input_syllable_count) then
            begin
                Exit;
            end;

            local_insert_rank := target_rank;
            if local_insert_rank < 0 then
            begin
                local_insert_rank := 0;
            end;
            if local_insert_rank > Length(candidates) then
            begin
                local_insert_rank := Length(candidates);
            end;

            local_score := local_state.score;
            if local_insert_rank = 0 then
            begin
                local_score := Max(local_score, candidates[0].score + 2048);
            end;

            local_existing_index := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text), local_text) then
                begin
                    local_existing_index := local_idx;
                    Break;
                end;
            end;

            if local_existing_index >= 0 then
            begin
                if local_existing_index <= local_insert_rank then
                begin
                    candidates[local_existing_index].score :=
                        Max(candidates[local_existing_index].score, local_score);
                    candidates[local_existing_index].has_dict_weight := True;
                    candidates[local_existing_index].dict_weight :=
                        Max(candidates[local_existing_index].dict_weight,
                        candidates[local_existing_index].score);
                    if local_state.path <> '' then
                    begin
                        remember_segment_path_for_candidate(local_text, '',
                            local_state.path, candidates[local_existing_index].score);
                    end;
                    if SameText(local_text, m_debug_target_recall_text) then
                    begin
                        m_debug_target_candidate_stage_mask :=
                            m_debug_target_candidate_stage_mask or 1;
                    end;
                    Exit;
                end;
                local_candidate := candidates[local_existing_index];
                for local_idx := local_existing_index to High(candidates) - 1 do
                begin
                    candidates[local_idx] := candidates[local_idx + 1];
                end;
                SetLength(candidates, Length(candidates) - 1);
            end
            else
            begin
                FillChar(local_candidate, SizeOf(local_candidate), 0);
                local_candidate.text := local_text;
            end;

            local_candidate.text := local_text;
            local_candidate.comment := '';
            local_candidate.source := cs_rule;
            local_candidate.has_dict_weight := True;
            local_candidate.dict_weight := local_state.score;
            local_candidate.score := local_score;
            local_candidate.dict_weight := local_score;

            SetLength(candidates, Length(candidates) + 1);
            for local_idx := High(candidates) downto local_insert_rank + 1 do
            begin
                candidates[local_idx] := candidates[local_idx - 1];
            end;
            candidates[local_insert_rank] := local_candidate;

            if local_state.path <> '' then
            begin
                remember_segment_path_for_candidate(local_text, '', local_state.path,
                    local_candidate.score);
            end;
            if SameText(local_text, m_debug_target_recall_text) then
            begin
                m_debug_target_candidate_stage_mask :=
                    m_debug_target_candidate_stage_mask or 1;
            end;
        end;

        procedure promote_best_fast_complete_candidate_by_score_local;
        const
            c_fast_complete_promote_margin = 256;
        var
            local_idx: Integer;
            best_idx_local: Integer;
            best_score_local: Integer;
            candidate_score_local: Integer;
            moved_candidate_local: TncCandidate;
        begin
            if Length(candidates) < 2 then
            begin
                Exit;
            end;
            if (Trim(candidates[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            best_idx_local := 0;
            best_score_local := candidates[0].score;
            for local_idx := 1 to Min(3, High(candidates)) do
            begin
                if (Trim(candidates[local_idx].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(candidates[local_idx].text)) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;
                candidate_score_local := candidates[local_idx].score;
                if candidate_score_local >
                    best_score_local + c_fast_complete_promote_margin then
                begin
                    best_idx_local := local_idx;
                    best_score_local := candidate_score_local;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            moved_candidate_local := candidates[best_idx_local];
            for local_idx := best_idx_local downto 1 do
            begin
                candidates[local_idx] := candidates[local_idx - 1];
            end;
            candidates[0] := moved_candidate_local;
        end;

        procedure force_fast_top_state_candidate_local(
            const local_state: TExactChunkChainState);
        var
            local_idx: Integer;
            existing_idx_local: Integer;
            local_candidate: TncCandidate;
            local_text: string;
        begin
            local_text := Trim(local_state.text);
            if (local_text = '') or
                (get_candidate_text_unit_count(local_text) <> input_syllable_count) then
            begin
                Exit;
            end;

            existing_idx_local := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[local_idx].comment) = '') and
                    SameText(Trim(candidates[local_idx].text), local_text) then
                begin
                    existing_idx_local := local_idx;
                    Break;
                end;
            end;

            if existing_idx_local >= 0 then
            begin
                local_candidate := candidates[existing_idx_local];
                for local_idx := existing_idx_local downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
            end
            else
            begin
                FillChar(local_candidate, SizeOf(local_candidate), 0);
                SetLength(candidates, Length(candidates) + 1);
                for local_idx := High(candidates) downto 1 do
                begin
                    candidates[local_idx] := candidates[local_idx - 1];
                end;
            end;

            local_candidate.text := local_text;
            local_candidate.comment := '';
            local_candidate.source := cs_rule;
            local_candidate.has_dict_weight := True;
            local_candidate.score := Max(local_candidate.score,
                local_state.score);
            local_candidate.dict_weight := Max(local_candidate.dict_weight,
                local_candidate.score);
            candidates[0] := local_candidate;
            if local_state.path <> '' then
            begin
                remember_segment_path_for_candidate(local_text, '',
                    local_state.path, local_candidate.score);
            end;
        end;

    begin
        { Fast search also reads this flag, before the fallback lattice starts. }
        runtime_exact_edge_probe_collection_local := False;
        if (Length(candidates) = 0) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            (m_dictionary = nil) then
        begin
            record_chain_search_status_local(c_chain_search_status_guard_rejected);
            Exit;
        end;

        if input_syllable_count <= 5 then
        begin
            record_chain_search_status_local(c_chain_search_status_short_query);
            Exit;
        end;

        if (not force_fast_validation) and
            (input_syllable_count >= 8) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) and
            is_trusted_top_complete_candidate_local(candidates) then
        begin
            record_chain_search_status_local(
                c_chain_search_status_trusted_complete_exit);
            Exit;
        end;

        if (not force_fast_validation) and
            try_fast_visible_complete_exit_local then
        begin
            record_chain_search_status_local(
                c_chain_search_status_visible_complete_exit);
            Exit;
        end;

        if (input_syllable_count > 20) and
            ultra_long_exact_chunk_chain_miss_cached then
        begin
            record_chain_search_status_local(
                c_chain_search_status_cached_miss_exit);
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' xfastdp=[cached-miss]';
            end;
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(effective_syllables);
        if syllable_count_local <> input_syllable_count then
        begin
            record_chain_search_status_local(
                c_chain_search_status_syllable_mismatch);
            Exit;
        end;

        fast_phase_tick := nc_monotonic_tick_ms;
        record_chain_search_status_local(c_chain_search_status_fast_search);
        Inc(long_path_exact_lookup_filter_depth);
        try
            fast_build_ok := try_build_fast_dictionary_sentence_candidate_local(
                fast_text, fast_path, fast_score, fast_segments, fast_states);
        finally
            Dec(long_path_exact_lookup_filter_depth);
        end;
        fast_model_order_active := (Length(fast_states) > 0) and
            fast_states[0].ranker_model_ordered;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' txfastdp=%d', [nc_monotonic_tick_ms - fast_phase_tick]);
        end;
        if fast_build_ok and (fast_text <> '') then
        begin
            if m_composition_built_incrementally and
                (not force_fast_validation) and
                (Length(fast_states) > 0) and
                ((Length(fast_states) = 1) or
                (fast_states[0].score >= fast_states[1].score +
                c_incremental_fast_direct_min_gap)) and
                (input_syllable_count >= 10) and
                (get_candidate_text_unit_count(Trim(fast_text)) =
                input_syllable_count) and
                (Trim(fast_path) <> '') and
                (fast_score >= input_syllable_count * 9000) and
                (fast_segments <= input_syllable_count - 3) then
            begin
                force_fast_top_state_candidate_local(fast_states[0]);
                mark_debug_target_candidate_stage_local(1024, candidates);
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xfastdirect=[text=%s score=%d seg=%d]',
                        [fast_text, fast_score, fast_segments]);
                end;
                Exit;
            end;
            fast_phase_tick := nc_monotonic_tick_ms;
            fast_repair_inserted := False;
            for fast_state_idx := 0 to Min(
                c_chain_lattice_baseline_beam_width - 1,
                High(fast_states)) do
            begin
                fast_state_to_upsert := fast_states[fast_state_idx];
                if fast_state_idx = 0 then
                begin
                    if (allow_fast_repair or allow_incremental_tail_repair) and
                        try_repair_state_with_covering_exact_pairs_local(
                        fast_states[fast_state_idx], fast_repaired_state,
                        allow_incremental_tail_repair and
                        (not allow_fast_repair)) and
                        should_promote_fast_repair_state_local(
                        fast_state_to_upsert, fast_repaired_state) then
                    begin
                        if fast_model_order_active then
                        begin
                            { The repair remains available, but it was not in
                              the ranked candidate set and must not bypass the
                              learned ordering. }
                            upsert_fast_state_candidate_local(
                                fast_state_to_upsert, 0);
                            upsert_fast_state_candidate_local(
                                fast_repaired_state, 1);
                        end
                        else
                        begin
                            upsert_fast_state_candidate_local(
                                fast_repaired_state, 0);
                            upsert_fast_state_candidate_local(
                                fast_state_to_upsert, 1);
                        end;
                        fast_repair_inserted := True;
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                Format(' xfastrepair=[%s score=%d]',
                                [fast_repaired_state.text,
                                fast_repaired_state.score]);
                        end;
                    end;
                    if not fast_repair_inserted then
                    begin
                        upsert_fast_state_candidate_local(fast_state_to_upsert, 0);
                    end;
                    Continue;
                end;
                upsert_fast_state_candidate_local(fast_state_to_upsert,
                    fast_state_idx + Ord(fast_repair_inserted));
            end;
            if Length(fast_states) > 0 then
            begin
                if fast_model_order_active then
                begin
                    force_fast_top_state_candidate_local(fast_states[0]);
                end
                else if fast_repair_inserted then
                begin
                    force_fast_top_state_candidate_local(fast_repaired_state);
                end
                else
                begin
                    force_fast_top_state_candidate_local(fast_states[0]);
                end;
            end;
            if not fast_model_order_active then
            begin
                promote_best_fast_complete_candidate_by_score_local;
            end;
            mark_debug_target_candidate_stage_local(1024, candidates);
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xfastdp=[text=%s score=%d seg=%d alts=%d]',
                    [fast_text, fast_score, fast_segments, Length(fast_states)]);
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' txfastup=%d', [nc_monotonic_tick_ms - fast_phase_tick]);
            end;
            Exit;
        end;

        if input_syllable_count > 20 then
        begin
            record_chain_search_status_local(
                c_chain_search_status_ultra_long_fast_miss);
            ultra_long_exact_chunk_chain_miss_cached := True;
            Exit;
        end;

        record_chain_search_status_local(c_chain_search_status_fallback_search);

        SetLength(best_scores, syllable_count_local + 1);
        SetLength(best_texts, syllable_count_local + 1);
        SetLength(best_paths, syllable_count_local + 1);
        SetLength(best_segments, syllable_count_local + 1);
        SetLength(best_has_anchor, syllable_count_local + 1);
        SetLength(best_anchor_units, syllable_count_local + 1);
        standalone_chunk_states := TDictionary<string, Integer>.Create;
        standalone_chunk_texts := TDictionary<string, string>.Create;
        standalone_chunk_scores := TDictionary<string, Integer>.Create;
        standalone_chunk_options :=
            TDictionary<string, TExactChunkChainOptionArray>.Create;
        standalone_chunk_engine := nil;
        standalone_chunk_budget_remaining := 12;
        use_direct_chunk_lattice_only := True;
        suppress_subspan_oracle_local := syllable_count_local <= 8;
        if suppress_subspan_oracle_local then
        begin
            Inc(m_disable_subspan_standalone_oracle);
        end;
        if syllable_count_local >= 12 then
        begin
            standalone_chunk_budget_remaining := 16;
        end;
        if syllable_count_local >= 18 then
        begin
            standalone_chunk_budget_remaining := 24;
        end;

        try
            for idx := 0 to syllable_count_local do
            begin
                best_scores[idx] := Low(Integer);
                best_texts[idx] := '';
                best_paths[idx] := '';
                best_segments[idx] := MaxInt;
                best_has_anchor[idx] := False;
                best_anchor_units[idx] := -1;
            end;

            best_scores[syllable_count_local] := 0;
            best_texts[syllable_count_local] := '';
            best_paths[syllable_count_local] := '';
            best_segments[syllable_count_local] := 0;
            best_has_anchor[syllable_count_local] := False;
            best_anchor_units[syllable_count_local] := -1;

            if not use_direct_chunk_lattice_only then
            begin
                for idx := syllable_count_local - 1 downto 0 do
                begin
                    try_update_best_from_position(idx);
                end;
            end;

            if (input_syllable_count >= 4) and
                try_build_exact_chunk_lattice_local(lattice_text, lattice_path,
                lattice_score, lattice_segments, lattice_has_anchor,
                lattice_anchor_units) and
                (lattice_text <> '') and
                (get_candidate_text_unit_count(lattice_text) = input_syllable_count) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xlattice=[text=%s score=%d seg=%d top=%s topscore=%d]',
                        [lattice_text, lattice_score, lattice_segments,
                        best_texts[0], best_scores[0]]);
                end;
                if (best_scores[0] = Low(Integer)) or
                    (lattice_score > best_scores[0]) or
                    ((lattice_score = best_scores[0]) and
                    lattice_has_anchor and (not best_has_anchor[0])) or
                    ((lattice_score = best_scores[0]) and
                    (lattice_has_anchor = best_has_anchor[0]) and
                    (lattice_anchor_units > best_anchor_units[0])) or
                    ((lattice_score = best_scores[0]) and
                    (lattice_anchor_units = best_anchor_units[0]) and
                    (lattice_segments < best_segments[0])) then
                begin
                    best_scores[0] := lattice_score;
                    best_texts[0] := lattice_text;
                    best_paths[0] := lattice_path;
                    best_segments[0] := lattice_segments;
                    best_has_anchor[0] := lattice_has_anchor;
                    best_anchor_units[0] := lattice_anchor_units;
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' xlattice=[promote text=%s seg=%d]',
                            [lattice_text, lattice_segments]);
                    end;
                end;
            end;

            preferred_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, best_texts[0]);
            current_path_preference_local := 0;
            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, best_texts[0], current_support_local,
                current_path_preference_local);
            xchain_metric_local := current_support_local + preferred_phrase_bonus;
            FillChar(problematic_candidate_local, SizeOf(problematic_candidate_local), 0);
            problematic_candidate_local.text := best_texts[0];
            problematic_candidate_local.comment := '';
            xchain_problematic_local :=
                is_problematic_single_char_chain_candidate_for_query(lookup_text,
                problematic_candidate_local);
            if xchain_problematic_local then
            begin
                Dec(xchain_metric_local, 1536);
            end;
            if support_path_local <> '' then
            begin
                best_paths[0] := support_path_local;
            end;

            if (best_scores[0] = Low(Integer)) or
                ((not best_has_anchor[0]) and (preferred_phrase_bonus <= 0)) or
                (best_texts[0] = '') or
                (get_candidate_text_unit_count(best_texts[0]) <> input_syllable_count) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchain=[miss text=%s anchor=%d seg=%d]',
                        [best_texts[0], Ord(best_has_anchor[0]), best_segments[0]]);
                end;
                Exit;
            end;

            best_visible_index := -1;
            best_visible_metric := Low(Integer);
            best_visible_problematic_local := False;
            for idx := 0 to High(candidates) do
            begin
                current_candidate_text_local := Trim(candidates[idx].text);
                if (Trim(candidates[idx].comment) <> '') or
                    (current_candidate_text_local = '') or
                    (get_candidate_text_unit_count(current_candidate_text_local) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;
                current_path_preference_local := 0;
                support_path_local := infer_segment_path_for_query_text_with_score(
                    lookup_text, current_candidate_text_local,
                    current_support_local, current_path_preference_local);
                current_metric_local := current_support_local +
                    get_preferred_query_phrase_bonus_local(lookup_text,
                    current_candidate_text_local);
                FillChar(problematic_candidate_local, SizeOf(problematic_candidate_local), 0);
                problematic_candidate_local.text := current_candidate_text_local;
                problematic_candidate_local.comment := '';
                if is_problematic_single_char_chain_candidate_for_query(lookup_text,
                    problematic_candidate_local) then
                begin
                    Dec(current_metric_local, 1536);
                end;
                if (best_visible_index < 0) or
                    (current_metric_local > best_visible_metric) or
                    ((current_metric_local = best_visible_metric) and
                    (CompareText(current_candidate_text_local,
                    Trim(candidates[best_visible_index].text)) < 0)) then
                begin
                    best_visible_index := idx;
                    best_visible_metric := current_metric_local;
                    best_visible_problematic_local :=
                        is_problematic_single_char_chain_candidate_for_query(
                        lookup_text, problematic_candidate_local);
                end;
            end;

            if (best_visible_index >= 0) and
                (not SameText(Trim(candidates[best_visible_index].text),
                best_texts[0])) and
                ((best_visible_metric > xchain_metric_local + 2048) or
                ((best_visible_metric = xchain_metric_local) and
                (not best_visible_problematic_local) and
                xchain_problematic_local)) then
            begin
                best_texts[0] := Trim(candidates[best_visible_index].text);
                preferred_phrase_bonus := get_preferred_query_phrase_bonus_local(
                    lookup_text, best_texts[0]);
                best_paths[0] := get_segment_path_for_candidate(
                    candidates[best_visible_index]);
                if best_paths[0] = '' then
                begin
                    current_path_preference_local := 0;
                    support_path_local := infer_segment_path_for_query_text_with_score(
                        lookup_text, best_texts[0], current_support_local,
                        current_path_preference_local);
                    best_paths[0] := support_path_local;
                end;
                best_segments[0] := get_path_segment_count_local(best_paths[0]);
                if best_segments[0] <= 0 then
                begin
                    best_segments[0] := 1;
                end;
                best_has_anchor[0] := True;
                best_anchor_units[0] := input_syllable_count;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchain=[prefer_visible text=%s metric=%d xmetric=%d]',
                        [best_texts[0], best_visible_metric, xchain_metric_local]);
                end;
            end;

            existing_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), best_texts[0]) then
                begin
                    existing_index := idx;
                    Break;
                end;
            end;

            if existing_index = 0 then
            begin
                Exit;
            end;

            top_path_segment_count := get_path_segment_count_local(
                get_segment_path_for_candidate(candidates[0]));
            top_preferred_phrase_bonus := get_preferred_query_phrase_bonus_local(
                lookup_text, Trim(candidates[0].text));
            should_promote := False;
            if Trim(candidates[0].comment) <> '' then
            begin
                should_promote := True;
            end
            else if get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                input_syllable_count then
            begin
                should_promote := True;
            end
            else if (top_path_segment_count > 0) and
                (best_segments[0] < top_path_segment_count) then
            begin
                should_promote := True;
            end
            else if (top_path_segment_count = 0) and best_has_anchor[0] and
                (best_segments[0] <= 2) then
            begin
                should_promote := True;
            end
            else if existing_index > 0 then
            begin
                should_promote := True;
            end
            else if preferred_phrase_bonus > top_preferred_phrase_bonus then
            begin
                should_promote := True;
            end
            else if best_scores[0] > candidates[0].score + 4096 then
            begin
                should_promote := True;
            end;

            if not should_promote then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchain=[skip text=%s seg=%d topseg=%d top=%s]',
                        [best_texts[0], best_segments[0], top_path_segment_count,
                        Trim(candidates[0].text)]);
                end;
                Exit;
            end;

            if existing_index > 0 then
            begin
                picked := candidates[existing_index];
                if picked.score < Max(candidates[0].score + 2048,
                    best_scores[0]) then
                begin
                    picked.score := Max(candidates[0].score + 2048,
                        best_scores[0]);
                end;
            end
            else
            begin
                FillChar(picked, SizeOf(picked), 0);
                picked.text := best_texts[0];
                picked.comment := '';
                picked.score := Max(candidates[0].score + 2048, best_scores[0]);
                picked.source := cs_rule;
                picked.has_dict_weight := True;
                picked.dict_weight := picked.score;
                remember_segment_path_for_candidate(picked.text, '',
                    best_paths[0], picked.score);
            end;

            if best_paths[0] <> '' then
            begin
                remember_segment_path_for_candidate(Trim(picked.text), '',
                    best_paths[0], picked.score);
            end;

            if existing_index > 0 then
            begin
                for idx := existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
            end
            else
            begin
                SetLength(candidates, Length(candidates) + 1);
                for idx := High(candidates) downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
            end;
            candidates[0] := picked;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xchain=[promote text=%s seg=%d]',
                    [best_texts[0], best_segments[0]]);
            end;
        finally
            if suppress_subspan_oracle_local then
            begin
                Dec(m_disable_subspan_standalone_oracle);
            end;
            standalone_chunk_engine.Free;
            standalone_chunk_options.Free;
            standalone_chunk_scores.Free;
            standalone_chunk_texts.Free;
            standalone_chunk_states.Free;
        end;
    end;

    procedure ensure_best_fixed_boundary_exact_chain_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
        c_max_chunk_syllables = 6;
        c_fixed_single_bonus = 1800;
        c_chunk_length_bonus = 900;
        c_prefix_probe_max = 6;
        c_prefix_probe_min = 3;
    var
        syllables_local: TncPinyinParseResult;
        syllable_count_local: Integer;
        best_scores: TArray<Integer>;
        best_texts: TArray<string>;
        best_paths: TArray<string>;
        best_segments: TArray<Integer>;
        idx: Integer;
        best_total_score: Integer;
        best_total_text: string;
        best_total_path: string;
        best_existing_index: Integer;
        prefix_count: Integer;
        local_text: string;
        local_path: string;
        local_score: Integer;
        local_resolved_segments: Integer;
        local_resolved_anchor_units: Integer;
        local_resolved_has_anchor: Boolean;
        top_is_suspicious: Boolean;
        picked: TncCandidate;
        fallback_fixed_text: string;
        fallback_fixed_weight: Integer;
        top_guard_path: string;
        top_guard_score: Integer;
        top_guard_segment_count: Integer;

        function get_fixed_sentence_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end;
        end;

        function get_directional_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'qian' then
            begin
                Result := string(Char($524D));
            end
            else if normalized_syllable = 'hou' then
            begin
                Result := string(Char($540E));
            end
            else if normalized_syllable = 'li' then
            begin
                Result := string(Char($91CC));
            end
            else if normalized_syllable = 'wai' then
            begin
                Result := string(Char($5916));
            end
            else if normalized_syllable = 'shang' then
            begin
                Result := string(Char($4E0A));
            end
            else if normalized_syllable = 'xia' then
            begin
                Result := string(Char($4E0B));
            end
            else if normalized_syllable = 'dong' then
            begin
                Result := string(Char($4E1C));
            end
            else if normalized_syllable = 'nan' then
            begin
                Result := string(Char($5357));
            end
            else if normalized_syllable = 'xi' then
            begin
                Result := string(Char($897F));
            end
            else if normalized_syllable = 'bei' then
            begin
                Result := string(Char($5317));
            end;
        end;

        function is_strong_sentence_boundary_single_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'dan') or
                (normalized_syllable = 'sui') or
                (normalized_syllable = 'er') or
                (normalized_syllable = 'shi') or
                (normalized_syllable = 'ye') or
                (normalized_syllable = 'yu') or
                (normalized_syllable = 'hui') or
                (normalized_syllable = 'ba') or
                (normalized_syllable = 'de') or
                (normalized_syllable = 'zai') or
                (normalized_syllable = 'zhe') or
                (normalized_syllable = 'que') or
                (normalized_syllable = 'bing') or
                (normalized_syllable = 'qie');
        end;

        function is_quantity_tail_syllable_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'ci') or
                (normalized_syllable = 'ge') or
                (normalized_syllable = 'tiao') or
                (normalized_syllable = 'zhang') or
                (normalized_syllable = 'ben') or
                (normalized_syllable = 'pian') or
                (normalized_syllable = 'bu') or
                (normalized_syllable = 'lun') or
                (normalized_syllable = 'hui') or
                (normalized_syllable = 'xia');
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_part: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part - 1 > High(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_part - 1 do
            begin
                Result := Result + normalize_pinyin_text(syllables_local[local_idx].text);
            end;
        end;

        function try_get_fixed_single_weight_local(const syllable_idx: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        var
            query_key: string;
            single_results: TncCandidateList;
            single_idx: Integer;
            candidate_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (syllable_idx < 0) or (syllable_idx > High(syllables_local)) then
            begin
                Exit;
            end;

            out_text := get_fixed_sentence_single_char_local(syllables_local[syllable_idx].text);
            if out_text = '' then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(syllables_local[syllable_idx].text);
            if (query_key <> '') and (m_dictionary <> nil) and
                m_dictionary.lookup_exact_full_pinyin(query_key, single_results) then
            begin
                for single_idx := 0 to High(single_results) do
                begin
                    if Trim(single_results[single_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if Trim(single_results[single_idx].text) <> out_text then
                    begin
                        Continue;
                    end;
                    candidate_weight := single_results[single_idx].score;
                    if single_results[single_idx].has_dict_weight and
                        (single_results[single_idx].dict_weight > candidate_weight) then
                    begin
                        candidate_weight := single_results[single_idx].dict_weight;
                    end;
                    if candidate_weight > out_weight then
                    begin
                        out_weight := candidate_weight;
                    end;
                end;
            end;

            if out_weight <= 0 then
            begin
                out_weight := 520;
            end;
            Result := True;
        end;

        function try_get_exact_weight_for_text_local(const start_idx: Integer;
            const chunk_syllables: Integer; const expected_text: string;
            out out_weight: Integer): Boolean;
        var
            query_key: string;
            exact_chunk_results: TncCandidateList;
            exact_idx: Integer;
            candidate_weight: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (expected_text = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, chunk_syllables);
            if (query_key = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key, exact_chunk_results)) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_chunk_results) do
            begin
                if Trim(exact_chunk_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if Trim(exact_chunk_results[exact_idx].text) <> expected_text then
                begin
                    Continue;
                end;

                candidate_weight := exact_chunk_results[exact_idx].score;
                if exact_chunk_results[exact_idx].has_dict_weight and
                    (exact_chunk_results[exact_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := exact_chunk_results[exact_idx].dict_weight;
                end;
                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    query_key, expected_text));
                if candidate_weight > out_weight then
                begin
                    out_weight := candidate_weight;
                end;
            end;

            Result := out_weight > 0;
        end;

        function try_get_top_direct_exact_text_local(const start_idx: Integer;
            const chunk_syllables: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        const
            c_direct_probe_limit = 6;
        var
            query_key: string;
            exact_chunk_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (chunk_syllables <= 0) or (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, chunk_syllables);
            if (query_key = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key,
                exact_chunk_results)) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_chunk_results) do
            begin
                if exact_idx >= c_direct_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_chunk_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_chunk_results[exact_idx].text);
                if get_candidate_text_unit_count(candidate_text) <> chunk_syllables then
                begin
                    Continue;
                end;

                candidate_weight := exact_chunk_results[exact_idx].score;
                if exact_chunk_results[exact_idx].has_dict_weight and
                    (exact_chunk_results[exact_idx].dict_weight > candidate_weight) then
                begin
                    candidate_weight := exact_chunk_results[exact_idx].dict_weight;
                end;
                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));
                if candidate_weight > out_weight then
                begin
                    out_text := candidate_text;
                    out_weight := candidate_weight;
                    Result := True;
                end;
            end;
        end;

        function try_get_best_following_phrase_weight_local(
            const start_idx: Integer; const min_syllables: Integer;
            out out_weight: Integer): Boolean;
        var
            follow_len: Integer;
            follow_text: string;
            follow_score: Integer;
            follow_segments: Integer;
            follow_path: string;
            follow_has_anchor: Boolean;
            follow_anchor_units: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (start_idx < 0) or (start_idx >= syllable_count_local) or
                (min_syllables < 2) then
            begin
                Exit;
            end;

            for follow_len := min_syllables to
                Min(c_max_chunk_syllables, syllable_count_local - start_idx) do
            begin
                if not try_resolve_best_exact_subspan_local(start_idx, follow_len,
                    follow_text, follow_score, follow_segments, follow_path,
                    follow_has_anchor, follow_anchor_units) then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(follow_text) < 2 then
                begin
                    Continue;
                end;
                if follow_score > out_weight then
                begin
                    out_weight := follow_score;
                    Result := True;
                end;
            end;
        end;

        function get_single_plus_following_phrase_penalty_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            single_text: string;
            single_weight: Integer;
            best_follow_weight: Integer;
            combined_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (segment_start < 0) or
                (segment_start + 2 >= syllable_count_local) then
            begin
                Exit;
            end;

            if not try_get_fixed_single_weight_local(segment_start, single_text,
                single_weight) then
            begin
                Exit;
            end;

            if not try_get_best_following_phrase_weight_local(segment_start + 1, 2,
                best_follow_weight) then
            begin
                Exit;
            end;

            combined_weight := single_weight + best_follow_weight;
            if is_strong_sentence_boundary_single_local(
                syllables_local[segment_start].text) then
            begin
                if combined_weight >= candidate_weight + 240 then
                begin
                    Exit(2400);
                end
                else if combined_weight >= candidate_weight + 80 then
                begin
                    Exit(1800);
                end
                else if combined_weight >= candidate_weight - 120 then
                begin
                    Exit(1200);
                end;
            end;

            if combined_weight >= candidate_weight + 520 then
            begin
                Exit(880);
            end
            else if combined_weight >= candidate_weight + 320 then
            begin
                Exit(640);
            end
            else if combined_weight >= candidate_weight + 180 then
            begin
                Exit(420);
            end;
        end;

        function get_first_text_unit_local(const text: string): string;
        var
            units: TArray<string>;
        begin
            Result := '';
            units := split_text_units(text);
            if Length(units) > 0 then
            begin
                Result := units[0];
            end;
        end;

        function get_first_path_segment_local(const encoded_path: string): string;
        var
            parts: TArray<string>;
            part_idx: Integer;
        begin
            Result := '';
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                Result := Trim(parts[part_idx]);
                if Result <> '' then
                begin
                    Exit;
                end;
            end;
            Result := '';
        end;

        function get_path_segment_at_local(const encoded_path: string;
            const segment_index: Integer): string;
        var
            parts: TArray<string>;
            part_idx: Integer;
            visible_idx: Integer;
            segment_text: string;
        begin
            Result := '';
            if (segment_index < 0) or (Trim(encoded_path) = '') then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            visible_idx := 0;
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                if segment_text = '' then
                begin
                    Continue;
                end;
                if visible_idx = segment_index then
                begin
                    Result := segment_text;
                    Exit;
                end;
                Inc(visible_idx);
            end;
        end;

        function get_first_meaningful_path_segment_local(
            const encoded_path: string): string;
        var
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
        begin
            Result := '';
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                if segment_text = '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(segment_text) >= 2 then
                begin
                    Result := segment_text;
                    Exit;
                end;
            end;

            Result := get_first_path_segment_local(encoded_path);
        end;

        function is_directional_surface_modifier_local(const text: string): Boolean;
        begin
            Result := SameText(Trim(text), string(Char($524D)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($540E)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($91CC)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($5916)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($4E0A)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($4E0B)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($4E1C)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($5357)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($897F)) + string(Char($9762))) or
                SameText(Trim(text), string(Char($5317)) + string(Char($9762)));
        end;

        function is_directional_single_unit_local(const text: string): Boolean;
        begin
            Result := SameText(Trim(text), string(Char($524D))) or
                SameText(Trim(text), string(Char($540E))) or
                SameText(Trim(text), string(Char($91CC))) or
                SameText(Trim(text), string(Char($5916))) or
                SameText(Trim(text), string(Char($4E0A))) or
                SameText(Trim(text), string(Char($4E0B))) or
                SameText(Trim(text), string(Char($4E1C))) or
                SameText(Trim(text), string(Char($5357))) or
                SameText(Trim(text), string(Char($897F))) or
                SameText(Trim(text), string(Char($5317)));
        end;

        function get_suffix_context_bonus_local(const chunk_text: string;
            const suffix_path: string): Integer;
        var
            first_segment: string;
            second_segment: string;
            meaningful_segment: string;
        begin
            Result := 0;
            if (chunk_text = '') or (suffix_path = '') then
            begin
                Exit;
            end;

            first_segment := get_first_path_segment_local(suffix_path);
            second_segment := get_path_segment_at_local(suffix_path, 1);
            meaningful_segment := get_first_meaningful_path_segment_local(suffix_path);

            if (m_dictionary <> nil) and (meaningful_segment <> '') then
            begin
                Inc(Result, m_dictionary.get_context_bonus(chunk_text,
                    meaningful_segment));
            end;

            if SameText(first_segment, string(Char($7684))) and
                is_directional_surface_modifier_local(chunk_text) then
            begin
                Inc(Result, 2200);
            end;

            if SameText(first_segment, string(Char($9762))) and
                SameText(second_segment, string(Char($7684))) and
                is_directional_single_unit_local(chunk_text) then
            begin
                Inc(Result, 2200);
            end;
        end;

        function get_suspicious_swallowed_fixed_chunk_penalty_local(
            const start_idx: Integer; const chunk_syllables: Integer;
            const chunk_text: string): Integer;
        var
            fixed_text: string;
            first_unit: string;
            chunk_units: TArray<string>;
            local_offset: Integer;
            direct_query_key: string;
            direct_results: TncCandidateList;
            direct_idx: Integer;
            direct_text: string;
            direct_first_unit: string;
        begin
            Result := 0;
            if chunk_syllables < 2 then
            begin
                Exit;
            end;

            if (start_idx < 0) or (start_idx >= syllable_count_local) or
                (start_idx + 1 >= syllable_count_local) then
            begin
                Exit;
            end;

            fixed_text := get_fixed_sentence_single_char_local(
                syllables_local[start_idx].text);
            if fixed_text = '' then
            begin
                Exit;
            end;

            if (best_scores[start_idx + 1] = Low(Integer)) or
                (best_texts[start_idx + 1] = '') then
            begin
                Exit;
            end;

            first_unit := get_first_text_unit_local(chunk_text);
            if first_unit = '' then
            begin
                Exit;
            end;

            chunk_units := split_text_units(chunk_text);
            if Length(chunk_units) = chunk_syllables then
            begin
                for local_offset := 1 to chunk_syllables - 1 do
                begin
                    fixed_text := get_fixed_sentence_single_char_local(
                        syllables_local[start_idx + local_offset].text);
                    if (fixed_text <> '') and
                        (not SameText(chunk_units[local_offset], fixed_text)) then
                    begin
                        Result := 3600 + (chunk_syllables * 2200);
                        Exit;
                    end;
                end;
            end;

            fixed_text := get_fixed_sentence_single_char_local(
                syllables_local[start_idx].text);
            if not SameText(first_unit, fixed_text) then
            begin
                Result := 3600 + (chunk_syllables * 2200);
                Exit;
            end;

            if m_dictionary = nil then
            begin
                Exit;
            end;

            direct_query_key := build_query_key_local(start_idx, 2);
            if (direct_query_key = '') or
                (not m_dictionary.lookup_exact_full_pinyin(direct_query_key,
                direct_results)) then
            begin
                Exit;
            end;

            for direct_idx := 0 to High(direct_results) do
            begin
                if direct_idx >= 6 then
                begin
                    Break;
                end;
                if Trim(direct_results[direct_idx].comment) <> '' then
                begin
                    Continue;
                end;

                direct_text := Trim(direct_results[direct_idx].text);
                if get_candidate_text_unit_count(direct_text) <> 2 then
                begin
                    Continue;
                end;

                direct_first_unit := get_first_text_unit_local(direct_text);
                if (direct_first_unit <> '') and
                    (not SameText(direct_first_unit, fixed_text)) then
                begin
                    Result := 6800 + (chunk_syllables * 2200);
                    Exit;
                end;
            end;
        end;

        function top_conflicts_with_fixed_boundary_chain_local(
            const top_text: string; const chain_text: string): Boolean;
        var
            top_units: TArray<string>;
            chain_units: TArray<string>;
            local_idx: Integer;
            fixed_text: string;
        begin
            Result := False;
            top_units := split_text_units(top_text);
            chain_units := split_text_units(chain_text);
            if (Length(top_units) < syllable_count_local) or
                (Length(chain_units) < syllable_count_local) then
            begin
                Exit;
            end;

            for local_idx := 0 to syllable_count_local - 1 do
            begin
                fixed_text := get_fixed_sentence_single_char_local(
                    syllables_local[local_idx].text);
                if (fixed_text <> '') and
                    SameText(chain_units[local_idx], fixed_text) and
                    (not SameText(top_units[local_idx], fixed_text)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function chain_rewrites_top_exact_segments_local(
            const top_path: string; const chain_text: string): Boolean;
        var
            top_segments: TArray<string>;
            chain_units: TArray<string>;
            segment_idx: Integer;
            unit_idx: Integer;
            query_offset: Integer;
            segment_text: string;
            segment_units: Integer;
            exact_weight: Integer;
            chain_span_text: string;
        begin
            Result := False;
            if (Trim(top_path) = '') or (Trim(chain_text) = '') then
            begin
                Exit;
            end;

            top_segments := top_path.Split([c_segment_path_separator]);
            chain_units := split_text_units(chain_text);
            if Length(chain_units) < syllable_count_local then
            begin
                Exit;
            end;

            query_offset := 0;
            for segment_idx := 0 to High(top_segments) do
            begin
                segment_text := Trim(top_segments[segment_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_text = '') or (segment_units <= 0) then
                begin
                    Continue;
                end;
                if query_offset + segment_units > syllable_count_local then
                begin
                    Exit;
                end;

                if (segment_units >= 2) and
                    try_get_exact_weight_for_text_local(query_offset,
                    segment_units, segment_text, exact_weight) and
                    (exact_weight >= 700) then
                begin
                    chain_span_text := '';
                    for unit_idx := query_offset to
                        query_offset + segment_units - 1 do
                    begin
                        chain_span_text := chain_span_text + chain_units[unit_idx];
                    end;
                    if (chain_span_text <> '') and
                        (not SameText(chain_span_text, segment_text)) then
                    begin
                        Exit(True);
                    end;
                end;

                Inc(query_offset, segment_units);
            end;
        end;

        function chain_rewrites_top_exact_text_spans_local(
            const top_text: string; const chain_text: string): Boolean;
        var
            top_units: TArray<string>;
            chain_units: TArray<string>;
            start_idx: Integer;
            span_len: Integer;
            unit_idx: Integer;
            exact_weight: Integer;
            top_span_text: string;
            chain_span_text: string;
        begin
            Result := False;
            top_units := split_text_units(top_text);
            chain_units := split_text_units(chain_text);
            if (Length(top_units) < syllable_count_local) or
                (Length(chain_units) < syllable_count_local) then
            begin
                Exit;
            end;

            for start_idx := 0 to syllable_count_local - 1 do
            begin
                for span_len := 2 to Min(c_max_chunk_syllables,
                    syllable_count_local - start_idx) do
                begin
                    top_span_text := '';
                    chain_span_text := '';
                    for unit_idx := start_idx to start_idx + span_len - 1 do
                    begin
                        top_span_text := top_span_text + top_units[unit_idx];
                        chain_span_text := chain_span_text + chain_units[unit_idx];
                    end;

                    if SameText(top_span_text, chain_span_text) then
                    begin
                        Continue;
                    end;

                    if try_get_exact_weight_for_text_local(start_idx, span_len,
                        top_span_text, exact_weight) and (exact_weight >= 700) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        procedure try_update_from_position(const start_idx: Integer);
        var
            fixed_text: string;
            fixed_weight: Integer;
            fixed_override_penalty: Integer;
            fixed_override_text: string;
            fixed_override_weight: Integer;
            directional_text: string;
            directional_phrase_text: string;
            directional_weight: Integer;
            chunk_syllables: Integer;
            next_idx: Integer;
            resolved_text: string;
            resolved_path: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            total_score: Integer;
            total_text: string;
            total_path: string;
            weak_phrase_penalty: Integer;
        begin
            if (start_idx + 2 < syllable_count_local) then
            begin
                directional_text := get_directional_single_char_local(
                    syllables_local[start_idx].text);
                if (directional_text <> '') and
                    SameText(normalize_pinyin_text(syllables_local[start_idx + 1].text), 'mian') and
                    SameText(get_fixed_sentence_single_char_local(
                    syllables_local[start_idx + 2].text), string(Char($7684))) and
                    (best_scores[start_idx + 2] > Low(Integer)) and
                    (best_texts[start_idx + 2] <> '') then
                begin
                    directional_phrase_text := directional_text + string(Char($9762));
                    directional_weight := 0;
                    if not try_get_exact_weight_for_text_local(start_idx, 2,
                        directional_phrase_text, directional_weight) then
                    begin
                        directional_weight := 680;
                    end;

                    total_score := directional_weight +
                        (2 * 2 * c_chunk_length_bonus) + 2600 + best_scores[start_idx + 2];
                    total_text := directional_phrase_text + best_texts[start_idx + 2];
                    total_path := directional_phrase_text;
                    if best_paths[start_idx + 2] <> '' then
                    begin
                        total_path := total_path + c_segment_path_separator +
                            best_paths[start_idx + 2];
                    end;

                    if (total_score > best_scores[start_idx]) or
                        ((total_score = best_scores[start_idx]) and
                        ((best_texts[start_idx] = '') or
                        (CompareText(total_text, best_texts[start_idx]) < 0))) then
                    begin
                        best_scores[start_idx] := total_score;
                        best_texts[start_idx] := total_text;
                        best_paths[start_idx] := total_path;
                        best_segments[start_idx] := 1 + best_segments[start_idx + 2];
                    end;
                end;
            end;

            if try_get_fixed_single_weight_local(start_idx, fixed_text, fixed_weight) then
            begin
                fixed_override_penalty := 0;
                if (start_idx + 1 < syllable_count_local) and
                    try_get_top_direct_exact_text_local(start_idx, 2,
                    fixed_override_text, fixed_override_weight) and
                    (get_first_text_unit_local(fixed_override_text) <> '') and
                    (not SameText(get_first_text_unit_local(fixed_override_text),
                    fixed_text)) then
                begin
                    if is_strong_sentence_boundary_single_local(
                        syllables_local[start_idx].text) then
                    begin
                        fixed_override_penalty := 6400;
                    end
                    else if SameText(fixed_text, string(Char($4E00))) and
                        is_quantity_tail_syllable_local(
                        syllables_local[start_idx + 1].text) then
                    begin
                        fixed_override_penalty := 0;
                    end
                    else
                    begin
                        fixed_override_penalty := 24000;
                    end;
                end;

                next_idx := start_idx + 1;
                if (next_idx = syllable_count_local) or
                    (best_scores[next_idx] > Low(Integer)) then
                begin
                    total_score := fixed_weight + c_fixed_single_bonus -
                        fixed_override_penalty;
                    total_text := fixed_text;
                    total_path := fixed_text;
                    if next_idx < syllable_count_local then
                    begin
                        Inc(total_score, best_scores[next_idx]);
                        Inc(total_score, get_suffix_context_bonus_local(
                            fixed_text, best_paths[next_idx]));
                        total_text := total_text + best_texts[next_idx];
                        if best_paths[next_idx] <> '' then
                        begin
                            total_path := total_path + c_segment_path_separator +
                                best_paths[next_idx];
                        end;
                    end;
                    if (total_score > best_scores[start_idx]) or
                        ((total_score = best_scores[start_idx]) and
                        ((best_texts[start_idx] = '') or
                        (CompareText(total_text, best_texts[start_idx]) < 0))) then
                    begin
                        best_scores[start_idx] := total_score;
                        best_texts[start_idx] := total_text;
                        best_paths[start_idx] := total_path;
                        best_segments[start_idx] := 1 +
                            IfThen(next_idx < syllable_count_local,
                            best_segments[next_idx], 0);
                    end;
                end;
            end;

            for chunk_syllables := 2 to Min(c_max_chunk_syllables,
                syllable_count_local - start_idx) do
            begin
                next_idx := start_idx + chunk_syllables;
                if (next_idx < syllable_count_local) and
                    (best_scores[next_idx] = Low(Integer)) then
                begin
                    Continue;
                end;

                if not try_resolve_best_exact_subspan_local(start_idx,
                    chunk_syllables, resolved_text, resolved_score,
                    resolved_segments, resolved_path, resolved_has_anchor,
                    resolved_anchor_units) then
                begin
                    Continue;
                end;

                total_score := resolved_score;
                Dec(total_score,
                    get_suspicious_swallowed_fixed_chunk_penalty_local(
                    start_idx, chunk_syllables, resolved_text));
                weak_phrase_penalty := get_single_plus_following_phrase_penalty_local(
                    start_idx, chunk_syllables, resolved_text, resolved_score);
                if weak_phrase_penalty > 0 then
                begin
                    Dec(total_score, weak_phrase_penalty);
                end;
                total_text := resolved_text;
                total_path := resolved_path;
                if next_idx < syllable_count_local then
                begin
                    Inc(total_score, best_scores[next_idx]);
                    total_text := total_text + best_texts[next_idx];
                    if best_paths[next_idx] <> '' then
                    begin
                        if total_path <> '' then
                        begin
                            total_path := total_path + c_segment_path_separator;
                        end;
                        total_path := total_path + best_paths[next_idx];
                    end;
                end;

                if (total_score > best_scores[start_idx]) or
                    ((total_score = best_scores[start_idx]) and
                    ((best_texts[start_idx] = '') or
                    (CompareText(total_text, best_texts[start_idx]) < 0))) then
                begin
                    best_scores[start_idx] := total_score;
                    best_texts[start_idx] := total_text;
                    best_paths[start_idx] := total_path;
                    best_segments[start_idx] := resolved_segments +
                        IfThen(next_idx < syllable_count_local,
                        best_segments[next_idx], 0);
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) or (m_dictionary = nil) then
        begin
            Exit;
        end;
        if has_strong_full_query_exact_complete_candidate_local then
        begin
            // A complete high-quality exact word must not be rewritten by
            // fixed-single stitching such as "yi" -> "涓€".
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(syllables_local);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(best_scores, syllable_count_local + 1);
        SetLength(best_texts, syllable_count_local + 1);
        SetLength(best_paths, syllable_count_local + 1);
        SetLength(best_segments, syllable_count_local + 1);
        for idx := 0 to syllable_count_local do
        begin
            best_scores[idx] := Low(Integer);
            best_texts[idx] := '';
            best_paths[idx] := '';
            best_segments[idx] := MaxInt;
        end;
        best_scores[syllable_count_local] := 0;
        best_texts[syllable_count_local] := '';
        best_paths[syllable_count_local] := '';
        best_segments[syllable_count_local] := 0;

        for idx := syllable_count_local - 1 downto 0 do
        begin
            try_update_from_position(idx);
        end;

        best_total_score := Low(Integer);
        best_total_text := '';
        best_total_path := '';
        best_existing_index := -1;

        if best_scores[0] > Low(Integer) then
        begin
            best_total_score := best_scores[0];
            best_total_text := best_texts[0];
            best_total_path := best_paths[0];
        end;

        for prefix_count := Min(c_prefix_probe_max, syllable_count_local - 2) downto
            c_prefix_probe_min do
        begin
            if (best_scores[prefix_count] = Low(Integer)) or (best_texts[prefix_count] = '') then
            begin
                Continue;
            end;

            if not try_resolve_best_exact_subspan_local(0, prefix_count, local_text,
                local_score, local_resolved_segments, local_path,
                local_resolved_has_anchor, local_resolved_anchor_units) then
            begin
                Continue;
            end;

            local_score := local_score + best_scores[prefix_count];
            local_text := local_text + best_texts[prefix_count];
            if best_paths[prefix_count] <> '' then
            begin
                if local_path <> '' then
                begin
                    local_path := local_path + c_segment_path_separator;
                end;
                local_path := local_path + best_paths[prefix_count];
            end;

            if local_score > best_total_score then
            begin
                best_total_score := local_score;
                best_total_text := local_text;
                best_total_path := local_path;
            end;
        end;

        if (best_total_text = '') or
            (get_candidate_text_unit_count(best_total_text) <> input_syllable_count) then
        begin
            if (best_total_text = '') and (syllable_count_local >= 2) and
                try_get_fixed_single_weight_local(0, fallback_fixed_text,
                fallback_fixed_weight) and
                (best_scores[1] > Low(Integer)) and (best_texts[1] <> '') then
            begin
                best_total_score := fallback_fixed_weight + c_fixed_single_bonus +
                    best_scores[1] + get_suffix_context_bonus_local(fallback_fixed_text,
                    best_paths[1]);
                best_total_text := fallback_fixed_text + best_texts[1];
                best_total_path := fallback_fixed_text;
                if best_paths[1] <> '' then
                begin
                    best_total_path := best_total_path + c_segment_path_separator +
                        best_paths[1];
                end;
            end;
        end;

        if (best_total_text = '') or
            (get_candidate_text_unit_count(best_total_text) <> input_syllable_count) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fxchain=[miss text=%s]', [best_total_text]);
            end;
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') and
                SameText(Trim(candidates[idx].text), best_total_text) then
            begin
                best_existing_index := idx;
                Break;
            end;
        end;

        if best_existing_index = 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fxchain=[keep text=%s]', [best_total_text]);
            end;
            Exit;
        end;

        top_is_suspicious := (Trim(candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidates[0].text)) <> input_syllable_count);
        if (not top_is_suspicious) and
            top_conflicts_with_fixed_boundary_chain_local(
            Trim(candidates[0].text), best_total_text) then
        begin
            top_is_suspicious := True;
        end;
        top_guard_path := get_segment_path_for_candidate(candidates[0]);
        if (top_guard_path = '') and (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            top_guard_path := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(candidates[0].text), top_guard_score,
                top_guard_segment_count);
        end;
        if chain_rewrites_top_exact_segments_local(top_guard_path,
            best_total_text) then
        begin
            Exit;
        end;
        if (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) and
            chain_rewrites_top_exact_text_spans_local(Trim(candidates[0].text),
            best_total_text) then
        begin
            Exit;
        end;
        if (not top_is_suspicious) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) = input_syllable_count) and
            (not candidates[0].has_dict_weight) and
            (not SameText(Trim(candidates[0].text), best_total_text)) and
            (best_total_score + 2048 >= candidates[0].score) then
        begin
            top_is_suspicious := True;
        end;
        if (best_existing_index > 0) or top_is_suspicious then
        begin
            if best_existing_index > 0 then
            begin
                picked := candidates[best_existing_index];
                picked.score := Max(Max(candidates[0].score + 2048, picked.score),
                    best_total_score);
                remember_segment_path_for_candidate(picked.text, '',
                    best_total_path, picked.score);
                for idx := best_existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx - 1];
                    end;
                end;
                candidates[0] := picked;
                if Length(m_candidate_segment_paths) > 0 then
                begin
                    m_candidate_segment_paths[0] := best_total_path;
                end;
            end
            else
            begin
                FillChar(picked, SizeOf(picked), 0);
                picked.text := best_total_text;
                picked.comment := '';
                picked.score := Max(candidates[0].score + 2048, best_total_score);
                picked.source := cs_rule;
                picked.has_dict_weight := True;
                picked.dict_weight := picked.score;
                remember_segment_path_for_candidate(picked.text, '', best_total_path,
                    picked.score);
                SetLength(candidates, Length(candidates) + 1);
                if Length(m_candidate_segment_paths) > 0 then
                begin
                    SetLength(m_candidate_segment_paths, Length(candidates));
                end;
                for idx := High(candidates) downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx - 1];
                    end;
                end;
                candidates[0] := picked;
                if Length(m_candidate_segment_paths) > 0 then
                begin
                    m_candidate_segment_paths[0] := best_total_path;
                end;
            end;

            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fxchain=[promote text=%s]', [best_total_text]);
            end;
        end;
    end;

    procedure ensure_simple_fixed_boundary_chain_candidate_visible(
        var candidates: TncCandidateList);
    var
        syllables_local: TncPinyinParseResult;
        syllable_count_local: Integer;
        pos_local: Integer;
        span_len_local: Integer;
        query_key_local: string;
        phrase_text_local: string;
        phrase_bonus_local: Integer;
        single_results_local: TncCandidateList;
        exact_results_local: TncCandidateList;
        single_text_local: string;
        built_text_local: string;
        built_path_local: string;
        top_units_local: TArray<string>;
        built_units_local: TArray<string>;
        fixed_text_local: string;
        idx_local: Integer;
        existing_idx_local: Integer;
        picked_local: TncCandidate;
        built_has_preferred_phrase_local: Boolean;
        built_has_fixed_single_local: Boolean;

        function build_query_key_local(const local_start_idx: Integer;
            const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > syllable_count_local) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_fixed_single_local(const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'shuo' then
            begin
                Result := string(Char($8BF4));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'ci' then
            begin
                Result := string(Char($6B21));
            end;
        end;

        function try_get_best_exact_phrase_text_local(
            const query_key: string; const local_span_len: Integer;
            out out_text: string): Boolean;
        const
            c_probe_limit = 8;
        var
            local_idx: Integer;
            candidate_text: string;
            candidate_weight: Integer;
            best_weight: Integer;
        begin
            Result := False;
            out_text := '';
            best_weight := Low(Integer);
            if (query_key = '') or (local_span_len <= 1) or
                (m_dictionary = nil) or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                exact_results_local)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(exact_results_local) do
            begin
                if local_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results_local[local_idx].text);
                if get_candidate_text_unit_count(candidate_text) <>
                    local_span_len then
                begin
                    Continue;
                end;

                candidate_weight := exact_results_local[local_idx].score;
                if exact_results_local[local_idx].has_dict_weight and
                    (exact_results_local[local_idx].dict_weight >
                    candidate_weight) then
                begin
                    candidate_weight := exact_results_local[local_idx].dict_weight;
                end;
                Inc(candidate_weight, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));
                if candidate_weight > best_weight then
                begin
                    best_weight := candidate_weight;
                    out_text := candidate_text;
                    Result := True;
                end;
            end;
        end;

        procedure append_segment_local(const segment_text: string);
        begin
            built_text_local := built_text_local + segment_text;
            if built_path_local <> '' then
            begin
                built_path_local := built_path_local + c_segment_path_separator;
            end;
            built_path_local := built_path_local + segment_text;
        end;

        function top_conflicts_with_built_fixed_boundary_local: Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if Length(candidates) = 0 then
            begin
                Exit;
            end;
            top_units_local := split_text_units(Trim(candidates[0].text));
            built_units_local := split_text_units(built_text_local);
            if (Length(top_units_local) < syllable_count_local) or
                (Length(built_units_local) < syllable_count_local) then
            begin
                Exit;
            end;
            for local_idx := 0 to syllable_count_local - 1 do
            begin
                fixed_text_local := get_fixed_single_local(
                    syllables_local[local_idx].text);
                if (fixed_text_local <> '') and
                    SameText(built_units_local[local_idx], fixed_text_local) and
                    (not SameText(top_units_local[local_idx], fixed_text_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function slice_units_local(const units: TArray<string>;
            const local_start_idx: Integer; const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(units)) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to local_start_idx + local_span_len - 1 do
            begin
                Result := Result + units[local_idx];
            end;
        end;

        function top_conflicts_with_built_preferred_phrase_local: Boolean;
        var
            local_start_idx: Integer;
            local_span_len: Integer;
            local_query_key: string;
            local_phrase_text: string;
            local_phrase_bonus: Integer;
            built_phrase_text: string;
            top_phrase_text: string;
        begin
            Result := False;
            if not built_has_preferred_phrase_local then
            begin
                Exit;
            end;

            top_units_local := split_text_units(Trim(candidates[0].text));
            built_units_local := split_text_units(built_text_local);
            if (Length(top_units_local) < syllable_count_local) or
                (Length(built_units_local) < syllable_count_local) then
            begin
                Exit;
            end;

            for local_span_len := 3 downto 2 do
            begin
                for local_start_idx := 0 to syllable_count_local - local_span_len do
                begin
                    local_query_key := build_query_key_local(local_start_idx,
                        local_span_len);
                    if not try_get_preferred_query_phrase_text_local(
                        local_query_key, local_phrase_text, local_phrase_bonus) then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(local_phrase_text) <>
                        local_span_len then
                    begin
                        Continue;
                    end;

                    built_phrase_text := slice_units_local(built_units_local,
                        local_start_idx, local_span_len);
                    if not SameText(built_phrase_text, local_phrase_text) then
                    begin
                        Continue;
                    end;

                    top_phrase_text := slice_units_local(top_units_local,
                        local_start_idx, local_span_len);
                    if (top_phrase_text <> '') and
                        (not SameText(top_phrase_text, local_phrase_text)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 4) or (input_syllable_count > 6) then
        begin
            Exit;
        end;
        if has_strong_full_query_exact_complete_candidate_local then
        begin
            // Fixed-boundary chains are only a fallback repair. They should
            // never outrank a complete high-quality exact dictionary hit.
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(syllables_local);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;

        pos_local := 0;
        built_text_local := '';
        built_path_local := '';
        built_has_preferred_phrase_local := False;
        built_has_fixed_single_local := False;
        while pos_local < syllable_count_local do
        begin
            fixed_text_local := get_fixed_single_local(
                syllables_local[pos_local].text);
            if fixed_text_local <> '' then
            begin
                append_segment_local(fixed_text_local);
                built_has_fixed_single_local := True;
                Inc(pos_local);
                Continue;
            end;

            phrase_text_local := '';
            span_len_local := Min(3, syllable_count_local - pos_local);
            while span_len_local >= 2 do
            begin
                query_key_local := build_query_key_local(pos_local,
                    span_len_local);
                if try_get_preferred_query_phrase_text_local(query_key_local,
                    phrase_text_local, phrase_bonus_local) and
                    (get_candidate_text_unit_count(phrase_text_local) =
                    span_len_local) then
                begin
                    Break;
                end;
                if try_get_best_exact_phrase_text_local(query_key_local,
                    span_len_local, phrase_text_local) then
                begin
                    Break;
                end;
                phrase_text_local := '';
                Dec(span_len_local);
            end;

            if phrase_text_local <> '' then
            begin
                append_segment_local(phrase_text_local);
                built_has_preferred_phrase_local := True;
                Inc(pos_local, get_candidate_text_unit_count(phrase_text_local));
                Continue;
            end;

            query_key_local := build_query_key_local(pos_local, 1);
            if (query_key_local = '') or
                (not m_dictionary.lookup(query_key_local, single_results_local)) or
                (Length(single_results_local) = 0) then
            begin
                Exit;
            end;
            single_text_local := Trim(single_results_local[0].text);
            if get_candidate_text_unit_count(single_text_local) <> 1 then
            begin
                Exit;
            end;
            append_segment_local(single_text_local);
            Inc(pos_local);
        end;

        if (built_text_local = '') or
            (not built_has_fixed_single_local) or
            (get_candidate_text_unit_count(built_text_local) <>
            input_syllable_count) or
            SameText(built_text_local, Trim(candidates[0].text)) or
            ((get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) and
            (not top_conflicts_with_built_fixed_boundary_local) and
            (not top_conflicts_with_built_preferred_phrase_local)) then
        begin
            Exit;
        end;

        existing_idx_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text), built_text_local) then
            begin
                existing_idx_local := idx_local;
                Break;
            end;
        end;

        if existing_idx_local > 0 then
        begin
            picked_local := candidates[existing_idx_local];
            for idx_local := existing_idx_local downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := picked_local;
        end
        else
        begin
            FillChar(picked_local, SizeOf(picked_local), 0);
            picked_local.text := built_text_local;
            picked_local.comment := '';
            picked_local.score := candidates[0].score + 4096;
            picked_local.source := cs_rule;
            picked_local.has_dict_weight := True;
            picked_local.dict_weight := picked_local.score;
            SetLength(candidates, Length(candidates) + 1);
            for idx_local := High(candidates) downto 1 do
            begin
                candidates[idx_local] := candidates[idx_local - 1];
            end;
            candidates[0] := picked_local;
        end;
        remember_segment_path_for_candidate(candidates[0].text, '',
            built_path_local, candidates[0].score);
    end;

    procedure ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(
        var candidates: TncCandidateList);
    const
        c_complete_visibility_margin = 2048;
    var
        query_syllables: TncPinyinParseResult;
        best_index: Integer;
        best_rank: Integer;
        best_segments: Integer;
        idx: Integer;
        text_units: Integer;
        path_segments: Integer;
        current_rank: Integer;
        picked: TncCandidate;

        function get_fixed_sentence_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'qing' then
            begin
                Result := string(Char($8BF7));
            end;
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(query_syllables)) then
            begin
                Exit;
            end;
            for local_idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(query_syllables[local_idx].text);
            end;
        end;

        function try_get_best_exact_two_syllable_text_local(const start_idx: Integer;
            out out_text: string): Boolean;
        const
            c_probe_limit = 8;
        var
            query_key: string;
            exact_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            best_weight: Integer;
            weight_value: Integer;
        begin
            Result := False;
            out_text := '';
            best_weight := Low(Integer);
            query_key := build_query_key_local(start_idx, 2);
            if (query_key = '') or (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(query_key, exact_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(exact_results) do
            begin
                if result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(exact_results[result_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                    is_runtime_redup_candidate(exact_results[result_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[result_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 2 then
                begin
                    Continue;
                end;

                weight_value := exact_results[result_idx].score;
                if exact_results[result_idx].has_dict_weight and
                    (exact_results[result_idx].dict_weight > weight_value) then
                begin
                    weight_value := exact_results[result_idx].dict_weight;
                end;
                Inc(weight_value, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));

                if (not Result) or (weight_value > best_weight) then
                begin
                    out_text := candidate_text;
                    best_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_path_quality_adjustment_local(
            const candidate: TncCandidate): Integer;
        var
            encoded_path: string;
            candidate_units_text: TArray<string>;
            segment_parts: TArray<string>;
            segment_idx: Integer;
            syllable_idx: Integer;
            segment_text: string;
            next_segment_text: string;
            segment_units: Integer;
            next_segment_units: Integer;
            exact_two_text: string;
            fixed_left: string;
            fixed_right: string;
            candidate_pair_text: string;
        begin
            Result := 0;

            candidate_units_text := split_text_units(Trim(candidate.text));
            if Length(candidate_units_text) = input_syllable_count then
            begin
                for syllable_idx := 0 to input_syllable_count - 2 do
                begin
                    fixed_left := get_fixed_sentence_single_char_local(
                        query_syllables[syllable_idx].text);
                    fixed_right := get_fixed_sentence_single_char_local(
                        query_syllables[syllable_idx + 1].text);
                    if (fixed_left = '') or (fixed_right = '') then
                    begin
                        Continue;
                    end;
                    if not try_get_best_exact_two_syllable_text_local(syllable_idx,
                        exact_two_text) then
                    begin
                        Continue;
                    end;

                    candidate_pair_text := candidate_units_text[syllable_idx] +
                        candidate_units_text[syllable_idx + 1];
                    if SameText(candidate_pair_text, exact_two_text) then
                    begin
                        Inc(Result, 12000);
                    end
                    else if SameText(candidate_pair_text, fixed_left + fixed_right) and
                        (not SameText(candidate_pair_text, exact_two_text)) then
                    begin
                        Dec(Result, 12000);
                    end;
                end;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            syllable_idx := 0;
            for segment_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[segment_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_units = 2) and
                    (syllable_idx + 1 <= High(query_syllables)) and
                    try_get_best_exact_two_syllable_text_local(syllable_idx,
                    exact_two_text) then
                begin
                    fixed_left := get_fixed_sentence_single_char_local(
                        query_syllables[syllable_idx].text);
                    fixed_right := get_fixed_sentence_single_char_local(
                        query_syllables[syllable_idx + 1].text);
                    if (fixed_left <> '') and (fixed_right <> '') and
                        SameText(segment_text, fixed_left + fixed_right) and
                        (not SameText(segment_text, exact_two_text)) then
                    begin
                        Dec(Result, 12000);
                    end;
                end;
                if (segment_units = 1) and (segment_idx < High(segment_parts)) then
                begin
                    next_segment_text := Trim(segment_parts[segment_idx + 1]);
                    next_segment_units := get_candidate_text_unit_count(next_segment_text);
                    if (next_segment_units = 1) and
                        (syllable_idx + 1 <= High(query_syllables)) and
                        try_get_best_exact_two_syllable_text_local(syllable_idx,
                        exact_two_text) and
                        (not SameText(segment_text + next_segment_text, exact_two_text)) then
                    begin
                        Dec(Result, 12000);
                    end;
                end;
                Inc(syllable_idx, segment_units);
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 4) or
            (Trim(candidates[0].comment) = '') or
            (not is_full_pinyin_key(normalize_pinyin_text(candidates[0].comment))) then
        begin
            Exit;
        end;

        query_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(query_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        best_index := -1;
        best_rank := Low(Integer);
        best_segments := MaxInt;
        for idx := 1 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            if is_runtime_chain_candidate(candidates[idx]) or
                is_runtime_common_pattern_candidate(candidates[idx]) or
                is_runtime_redup_candidate(candidates[idx]) then
            begin
                Continue;
            end;

            text_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if text_units <> input_syllable_count then
            begin
                Continue;
            end;

            path_segments := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[idx]));
            if (not candidates[idx].has_dict_weight) and
                (candidates[idx].source <> cs_user) and
                (path_segments <= 1) then
            begin
                Continue;
            end;

            current_rank := candidates[idx].score;
            if candidates[idx].has_dict_weight then
            begin
                Inc(current_rank, Min(1600, candidates[idx].dict_weight div 2));
            end;
            if path_segments > 1 then
            begin
                Inc(current_rank, 1200);
                if (path_segments * 3) <= input_syllable_count then
                begin
                    Inc(current_rank, 800);
                end;
            end;
            Inc(current_rank, get_path_quality_adjustment_local(candidates[idx]));

            if (best_index < 0) or (current_rank > best_rank) or
                ((current_rank = best_rank) and (path_segments < best_segments)) then
            begin
                best_index := idx;
                best_rank := current_rank;
                best_segments := path_segments;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        if best_rank < candidates[0].score + c_complete_visibility_margin then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_best_lookup_inferred_complete_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_lookup_inferred_margin = 240;
    var
        best_index: Integer;
        best_rank: Integer;
        top_rank: Integer;
        idx: Integer;
        infer_score: Integer;
        infer_segments: Integer;
        inferred_path: string;
        picked: TncCandidate;

        function get_lookup_inferred_rank_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := Low(Integer);
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;
            if get_candidate_text_unit_count(Trim(candidate.text)) <>
                input_syllable_count then
            begin
                Exit;
            end;

            inferred_path := infer_segment_path_for_selected_text_with_score(
                Trim(candidate.text), infer_score, infer_segments);
            if inferred_path = '' then
            begin
                Exit;
            end;

            Result := infer_score;
            Inc(Result, get_segment_path_preference_score(inferred_path));
            if infer_segments > 1 then
            begin
                Inc(Result, 600);
                if (infer_segments * 3) <= input_syllable_count then
                begin
                    Inc(Result, 400);
                end;
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        top_rank := get_lookup_inferred_rank_local(candidates[0]);
        if top_rank <= Low(Integer) div 4 then
        begin
            top_rank := 0;
        end;

        best_index := -1;
        best_rank := Low(Integer);
        for idx := 1 to High(candidates) do
        begin
            infer_score := get_lookup_inferred_rank_local(candidates[idx]);
            if infer_score > best_rank then
            begin
                best_rank := infer_score;
                best_index := idx;
            end;
        end;

        if (best_index <= 0) or (best_rank < top_rank + c_lookup_inferred_margin) then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' tli=[promote text=%s rank=%d top=%d]',
                [picked.text, best_rank, top_rank]);
        end;
    end;

    procedure ensure_best_exact_chunk_chain_partial_candidate_visible(
        var candidates: TncCandidateList);
    const
        c_chain_probe_limit = 8;
        c_chain_max_chunk_syllables = 5;
        c_chain_min_anchor_syllables = 3;
        c_chain_length_bonus = 1200;
        c_chain_anchor_bonus = 3200;
        c_chain_partial_bonus = 2048;
    var
        effective_syllables: TncPinyinParseResult;
        best_scores: TArray<Integer>;
        best_texts: TArray<string>;
        best_paths: TArray<string>;
        best_segments: TArray<Integer>;
        best_has_anchor: TArray<Boolean>;
        best_anchor_units: TArray<Integer>;
        syllable_count_local: Integer;
        idx: Integer;
        split_idx: Integer;
        best_split_idx: Integer;
        best_split_units: Integer;
        best_split_score: Integer;
        current_units: Integer;
        existing_index: Integer;
        complete_extension_index: Integer;
        picked: TncCandidate;
        top_units: Integer;
        top_is_strong_partial: Boolean;
        top_comment_key: string;
        best_comment_key: string;
        should_promote: Boolean;

        function is_text_prefix_local(const prefix_value: string;
            const full_value: string): Boolean;
        begin
            Result := (prefix_value <> '') and (full_value <> '') and
                (Length(prefix_value) <= Length(full_value)) and
                SameText(Copy(full_value, 1, Length(prefix_value)), prefix_value);
        end;

        function is_exact_tail_completion_local(const tail_key: string;
            const suffix_text: string): Boolean;
        var
            tail_results: TncCandidateList;
            tail_idx: Integer;
        begin
            Result := False;
            if (tail_key = '') or (suffix_text = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(tail_key, tail_results) then
            begin
                Exit;
            end;

            for tail_idx := 0 to High(tail_results) do
            begin
                if tail_idx >= c_chain_probe_limit then
                begin
                    Break;
                end;
                if Trim(tail_results[tail_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(tail_results[tail_idx].text), suffix_text) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_get_best_complete_extension_index_local(
            const partial_text: string; const tail_key: string;
            out out_index: Integer): Boolean;
        var
            local_idx: Integer;
            suffix_text: string;
            local_score: Integer;
            best_score_local: Integer;
        begin
            Result := False;
            out_index := -1;
            best_score_local := Low(Integer);
            if (partial_text = '') or (tail_key = '') then
            begin
                Exit;
            end;

            for local_idx := 0 to High(candidates) do
            begin
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(candidates[local_idx].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;
                if not is_text_prefix_local(partial_text,
                    Trim(candidates[local_idx].text)) then
                begin
                    Continue;
                end;

                suffix_text := Copy(Trim(candidates[local_idx].text),
                    Length(partial_text) + 1, MaxInt);
                if not is_exact_tail_completion_local(tail_key, suffix_text) then
                begin
                    Continue;
                end;

                local_score := candidates[local_idx].score;
                if (local_score > best_score_local) or
                    ((local_score = best_score_local) and (out_index < 0)) then
                begin
                    out_index := local_idx;
                    best_score_local := local_score;
                    Result := True;
                end;
            end;
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_part: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_part - 1 do
            begin
                Result := Result + normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        procedure try_update_best_from_position(const start_idx: Integer);
        var
            chunk_syllables: Integer;
            next_idx: Integer;
            query_key: string;
            exact_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
            total_score: Integer;
            total_segments: Integer;
            total_anchor_units: Integer;
            total_has_anchor: Boolean;
            combined_text: string;
            combined_path: string;
            chunk_score: Integer;
            better_candidate: Boolean;
        begin
            for chunk_syllables := 1 to Min(c_chain_max_chunk_syllables,
                syllable_count_local - start_idx) do
            begin
                next_idx := start_idx + chunk_syllables;
                if best_scores[start_idx] = Low(Integer) then
                begin
                    Break;
                end;

                query_key := build_query_key_local(start_idx, chunk_syllables);
                if (query_key = '') or (m_dictionary = nil) or
                    (not m_dictionary.lookup_exact_full_pinyin(query_key, exact_results)) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(exact_results) do
                begin
                    if result_idx >= c_chain_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(exact_results[result_idx]) or
                        is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                        is_runtime_redup_candidate(exact_results[result_idx]) then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(exact_results[result_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if candidate_units <> chunk_syllables then
                    begin
                        Continue;
                    end;

                    weight_value := exact_results[result_idx].score;
                    if exact_results[result_idx].has_dict_weight then
                    begin
                        if chunk_syllables <= 2 then
                        begin
                            weight_value := exact_results[result_idx].dict_weight;
                        end
                        else if (weight_value <= 0) or
                            (exact_results[result_idx].dict_weight > weight_value) then
                        begin
                            weight_value := exact_results[result_idx].dict_weight;
                        end;
                    end;
                    Inc(weight_value, get_preferred_query_phrase_bonus_local(
                        query_key, candidate_text));

                    chunk_score := weight_value +
                        (chunk_syllables * chunk_syllables * c_chain_length_bonus);
                    if chunk_syllables >= c_chain_min_anchor_syllables then
                    begin
                        Inc(chunk_score, c_chain_anchor_bonus);
                    end;

                    total_score := best_scores[start_idx] + chunk_score;
                    total_segments := best_segments[start_idx] + 1;
                    total_has_anchor := best_has_anchor[start_idx] or
                        (chunk_syllables >= c_chain_min_anchor_syllables);
                    total_anchor_units := best_anchor_units[start_idx];
                    if chunk_syllables >= c_chain_min_anchor_syllables then
                    begin
                        if chunk_syllables > total_anchor_units then
                        begin
                            total_anchor_units := chunk_syllables;
                        end;
                    end;

                    if best_texts[start_idx] <> '' then
                    begin
                        combined_text := best_texts[start_idx] + candidate_text;
                        combined_path := best_paths[start_idx] + c_segment_path_separator +
                            candidate_text;
                    end
                    else
                    begin
                        combined_text := candidate_text;
                        combined_path := candidate_text;
                    end;

                    better_candidate := False;
                    if total_score > best_scores[next_idx] then
                    begin
                        better_candidate := True;
                    end
                    else if total_score = best_scores[next_idx] then
                    begin
                        if total_has_anchor and (not best_has_anchor[next_idx]) then
                        begin
                            better_candidate := True;
                        end
                        else if total_has_anchor = best_has_anchor[next_idx] then
                        begin
                            if total_anchor_units > best_anchor_units[next_idx] then
                            begin
                                better_candidate := True;
                            end
                            else if total_anchor_units = best_anchor_units[next_idx] then
                            begin
                                if total_segments < best_segments[next_idx] then
                                begin
                                    better_candidate := True;
                                end
                                else if (total_segments = best_segments[next_idx]) and
                                    (combined_text <> '') and
                                    ((best_texts[next_idx] = '') or
                                    (CompareText(combined_text, best_texts[next_idx]) < 0)) then
                                begin
                                    better_candidate := True;
                                end;
                            end;
                        end;
                    end;

                    if better_candidate then
                    begin
                        best_scores[next_idx] := total_score;
                        best_texts[next_idx] := combined_text;
                        best_paths[next_idx] := combined_path;
                        best_segments[next_idx] := total_segments;
                        best_has_anchor[next_idx] := total_has_anchor;
                        best_anchor_units[next_idx] := total_anchor_units;
                    end;
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(effective_syllables);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(best_scores, syllable_count_local + 1);
        SetLength(best_texts, syllable_count_local + 1);
        SetLength(best_paths, syllable_count_local + 1);
        SetLength(best_segments, syllable_count_local + 1);
        SetLength(best_has_anchor, syllable_count_local + 1);
        SetLength(best_anchor_units, syllable_count_local + 1);

        for idx := 0 to syllable_count_local do
        begin
            best_scores[idx] := Low(Integer);
            best_texts[idx] := '';
            best_paths[idx] := '';
            best_segments[idx] := MaxInt;
            best_has_anchor[idx] := False;
            best_anchor_units[idx] := -1;
        end;

        best_scores[0] := 0;
        best_texts[0] := '';
        best_paths[0] := '';
        best_segments[0] := 0;
        best_has_anchor[0] := False;
        best_anchor_units[0] := -1;

        for idx := 0 to syllable_count_local - 1 do
        begin
            try_update_best_from_position(idx);
        end;

        best_split_idx := -1;
        best_split_units := -1;
        best_split_score := Low(Integer);
        best_comment_key := '';
        for split_idx := 2 to syllable_count_local - 1 do
        begin
            if (best_scores[split_idx] = Low(Integer)) or (best_texts[split_idx] = '') then
            begin
                Continue;
            end;

            best_comment_key := build_query_key_local(split_idx,
                syllable_count_local - split_idx);
            if (best_comment_key = '') or (not is_full_pinyin_key(best_comment_key)) then
            begin
                Continue;
            end;

            current_units := get_candidate_text_unit_count(best_texts[split_idx]);
            if current_units < 2 then
            begin
                Continue;
            end;
            if (not best_has_anchor[split_idx]) and (current_units < 3) then
            begin
                Continue;
            end;

            if (current_units > best_split_units) or
                ((current_units = best_split_units) and
                (best_scores[split_idx] > best_split_score)) then
            begin
                best_split_idx := split_idx;
                best_split_units := current_units;
                best_split_score := best_scores[split_idx];
            end;
        end;

        if best_split_idx < 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' xchainp=[miss]';
            end;
            Exit;
        end;

        best_comment_key := build_query_key_local(best_split_idx,
            syllable_count_local - best_split_idx);
        complete_extension_index := -1;
        if try_get_best_complete_extension_index_local(
            best_texts[best_split_idx], best_comment_key,
            complete_extension_index) then
        begin
            if complete_extension_index = 0 then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' xchainp=[keep complete text=%s]',
                        [Trim(candidates[0].text)]);
                end;
                Exit;
            end;

            picked := candidates[complete_extension_index];
            for idx := complete_extension_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xchainp=[prefer complete text=%s]',
                    [Trim(picked.text)]);
            end;
            Exit;
        end;

        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if SameText(Trim(candidates[idx].text), best_texts[best_split_idx]) and
                SameText(normalize_pinyin_text(candidates[idx].comment), best_comment_key) then
            begin
                existing_index := idx;
                Break;
            end;
        end;

        if existing_index = 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xchainp=[top text=%s comment=%s]',
                    [best_texts[best_split_idx], best_comment_key]);
            end;
            Exit;
        end;

        top_units := get_candidate_text_unit_count(Trim(candidates[0].text));
        top_comment_key := normalize_pinyin_text(candidates[0].comment);
        top_is_strong_partial := is_strong_exact_partial_candidate_for_visibility(
            candidates[0]);
        should_promote := False;
        if Trim(candidates[0].comment) = '' then
        begin
            should_promote := True;
        end
        else if (not top_is_strong_partial) then
        begin
            should_promote := True;
        end
        else if top_units < best_split_units then
        begin
            should_promote := True;
        end
        else if (top_units = best_split_units) and
            (top_comment_key <> best_comment_key) and
            (not candidates[0].has_dict_weight) then
        begin
            should_promote := True;
        end
        else if existing_index > 0 then
        begin
            should_promote := True;
        end;

        if not should_promote then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' xchainp=[skip text=%s comment=%s top=%s/%s]',
                    [best_texts[best_split_idx], best_comment_key,
                    Trim(candidates[0].text),
                    normalize_pinyin_text(candidates[0].comment)]);
            end;
            Exit;
        end;

        if existing_index > 0 then
        begin
            picked := candidates[existing_index];
        end
        else
        begin
            FillChar(picked, SizeOf(picked), 0);
            picked.text := best_texts[best_split_idx];
            picked.comment := best_comment_key;
            picked.score := Max(candidates[0].score + c_chain_partial_bonus,
                best_scores[best_split_idx] + c_chain_partial_bonus);
            picked.source := cs_rule;
            picked.has_dict_weight := False;
            picked.dict_weight := 0;
            remember_segment_path_for_candidate(picked.text, picked.comment,
                best_paths[best_split_idx], picked.score);
        end;

        if existing_index > 0 then
        begin
            for idx := existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
        end
        else
        begin
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' xchainp=[promote text=%s comment=%s]',
                [best_texts[best_split_idx], best_comment_key]);
        end;
    end;

    procedure ensure_best_subquery_sentence_candidate_visible(
        var candidates: TncCandidateList; out out_promoted: Boolean);
    const
        c_solver_min_syllables = 10;
        c_solver_max_chunk_syllables = 5;
        c_solver_beam_limit = 4;
        c_solver_beam_max_syllables = 12;
        c_solver_three_syllable_option_limit = 4;
        c_solver_length_bonus = 1200;
        c_solver_anchor_bonus = 3200;
        c_solver_promote_bonus = 4096;
        c_solver_probe_limit = 4;
        c_solver_work_limit = 32768;
    type
        TSubquerySolverBeamOption = record
            text: string;
            path: string;
            first_chunk: string;
            score: Integer;
            segments: Integer;
        end;
        TSubquerySolverBeamOptionArray = array of TSubquerySolverBeamOption;
        TStrictExactSentenceState = record
            valid: Boolean;
            score: Integer;
            segments: Integer;
            strong_chunks: Integer;
            single_chunks: Integer;
            anchor_units: Integer;
            text: string;
            path: string;
            first_chunk: string;
        end;
        TStrictExactSentenceStateArray = array of TStrictExactSentenceState;
    var
        effective_syllables: TncPinyinParseResult;
        syllable_count_local: Integer;
        best_scores: TArray<Integer>;
        best_segments: TArray<Integer>;
        best_texts: TArray<string>;
        best_paths: TArray<string>;
        best_first_chunks: TArray<string>;
        best_beam_options: TArray<TSubquerySolverBeamOptionArray>;
        idx: Integer;
        existing_index: Integer;
        picked: TncCandidate;
        top_path_segment_count: Integer;
        should_promote: Boolean;
        query_cache_states: TDictionary<string, Integer>;
        query_cache_texts: TDictionary<string, string>;
        query_cache_scores: TDictionary<string, Integer>;
        query_option_cache_states: TDictionary<string, Integer>;
        query_option_cache_options: TDictionary<string, TSubquerySolverBeamOptionArray>;
        query_engine: TncEngine;
        query_engine_config: TncEngineConfig;
        has_extendable_tail_local: Boolean;
        strict_solver_text_local: string;
        strict_solver_path_local: string;
        strict_solver_score_local: Integer;
        strict_solver_segments_local: Integer;

        solver_start_tick: UInt64;
        solver_budget_ms: UInt64;
        solver_work_count: Int64;
        solver_budget_exceeded: Boolean;

        function solver_time_budget_exceeded_local: Boolean;
        var
            timed_budget_ms_local: UInt64;
            emergency_budget_ms_local: UInt64;
        begin
            Result := solver_budget_exceeded;
            if Result then
            begin
                Exit;
            end;

            Inc(solver_work_count);
            if m_composition_built_incrementally then
            begin
                timed_budget_ms_local := solver_budget_ms;
                emergency_budget_ms_local := solver_budget_ms * 2;
            end
            else
            begin
                timed_budget_ms_local := 0;
                emergency_budget_ms_local := 0;
            end;
            if search_budget_should_stop(solver_start_tick,
                solver_work_count, c_solver_work_limit,
                timed_budget_ms_local, emergency_budget_ms_local) then
            begin
                solver_budget_exceeded := True;
                Result := True;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' tss_budget=[mode=%d work=%d]',
                        [Ord(m_search_budget_mode), solver_work_count]);
                end;
            end;
        end;

        function has_extendable_tail_query_local(const pinyin_key: string): Boolean;
        var
            local_syllables: TncPinyinParseResult;
            local_syllable_count: Integer;
            suffix_ch: Char;
            extended_key: string;
        begin
            Result := False;
            if pinyin_key = '' then
            begin
                Exit;
            end;

            local_syllables := get_effective_compact_pinyin_syllables(pinyin_key);
            local_syllable_count := Length(local_syllables);
            if local_syllable_count < 4 then
            begin
                Exit;
            end;

            for suffix_ch := 'a' to 'z' do
            begin
                extended_key := pinyin_key + suffix_ch;
                if get_effective_compact_pinyin_unit_count(extended_key) =
                    local_syllable_count then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_path_segment_count_local(const encoded_path: string): Integer;
        var
            parts: TArray<string>;
            part_idx: Integer;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(parts) do
            begin
                if Trim(parts[part_idx]) <> '' then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_part: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part - 1 > High(effective_syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_part - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    effective_syllables[local_idx].text);
            end;
        end;

        function get_fixed_boundary_single_char_for_solver_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'jiang' then
            begin
                Result := string(Char($5C06));
            end
            else if normalized_syllable = 'rang' then
            begin
                Result := string(Char($8BA9));
            end
            else if normalized_syllable = 'dui' then
            begin
                Result := string(Char($5BF9));
            end
            else if normalized_syllable = 'dao' then
            begin
                Result := string(Char($5230));
            end
            else if normalized_syllable = 'cong' then
            begin
                Result := string(Char($4ECE));
            end
            else if normalized_syllable = 'you' then
            begin
                Result := string(Char($7531));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end;
        end;

        function solver_segment_has_fixed_boundary_local(const start_idx: Integer;
            const syllable_count_part: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part > syllable_count_local) then
            begin
                Exit;
            end;

            for local_idx := 0 to syllable_count_part - 1 do
            begin
                if get_fixed_boundary_single_char_for_solver_local(
                    effective_syllables[start_idx + local_idx].text) <> '' then
                begin
                    Exit(True);
                end;
            end;
        end;

        function solver_candidate_respects_fixed_boundaries_local(
            const start_idx: Integer; const syllable_count_part: Integer;
            const candidate_text: string): Boolean;
        var
            text_units_local: TArray<string>;
            local_idx: Integer;
            fixed_text_local: string;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_part <= 0) or
                (start_idx + syllable_count_part > syllable_count_local) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_text));
            if Length(text_units_local) <> syllable_count_part then
            begin
                Exit(not solver_segment_has_fixed_boundary_local(start_idx,
                    syllable_count_part));
            end;

            for local_idx := 0 to syllable_count_part - 1 do
            begin
                fixed_text_local := get_fixed_boundary_single_char_for_solver_local(
                    effective_syllables[start_idx + local_idx].text);
                if (fixed_text_local <> '') and
                    (not SameText(text_units_local[local_idx], fixed_text_local)) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function get_context_free_single_char_penalty_solver_local(
            const pinyin_key: string; const candidate_text: string): Integer;
        var
            normalized_query_key_local: string;
            glue_text_local: string;
        begin
            Result := 0;
            if (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <> 1) then
            begin
                Exit;
            end;

            normalized_query_key_local := normalize_pinyin_text(pinyin_key);
            if normalized_query_key_local = '' then
            begin
                Exit;
            end;

            glue_text_local := get_fixed_boundary_single_char_for_solver_local(
                normalized_query_key_local);
            if (glue_text_local = '') or
                (not SameText(candidate_text, glue_text_local)) then
            begin
                Exit;
            end;

            if (normalized_query_key_local = 'shi') or
                (normalized_query_key_local = 'ye') or
                (normalized_query_key_local = 'yu') or
                (normalized_query_key_local = 'hui') or
                (normalized_query_key_local = 'ba') or
                (normalized_query_key_local = 'de') or
                (normalized_query_key_local = 'zai') or
                (normalized_query_key_local = 'zhe') or
                (normalized_query_key_local = 'que') or
                (normalized_query_key_local = 'bing') or
                (normalized_query_key_local = 'qie') or
                (normalized_query_key_local = 'jiang') or
                (normalized_query_key_local = 'rang') or
                (normalized_query_key_local = 'dui') or
                (normalized_query_key_local = 'dao') or
                (normalized_query_key_local = 'cong') or
                (normalized_query_key_local = 'you') or
                (normalized_query_key_local = 'er') or
                (normalized_query_key_local = 'he') or
                (normalized_query_key_local = 'ke') then
            begin
                Result := 1600;
            end;
        end;

        function has_exact_query_text_match_solver_local(const query_key: string;
            const candidate_text: string): Boolean;
        var
            local_results_local: TncCandidateList;
            result_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key,
                local_results_local) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(local_results_local) do
            begin
                if result_idx_local >= c_solver_probe_limit * 3 then
                begin
                    Break;
                end;
                if Trim(local_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(local_results_local[result_idx_local].text),
                    candidate_text) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_exact_query_text_bonus_solver_local(const query_key: string;
            const candidate_text: string): Integer;
        begin
            if has_exact_query_text_match_solver_local(query_key, candidate_text) then
            begin
                Exit(3072);
            end;
            Result := 0;
        end;

        function has_competing_exact_full_phrase_solver_local(
            const query_key: string; const candidate_text: string): Boolean;
        var
            local_results_local: TncCandidateList;
            local_syllables: TncPinyinParseResult;
            result_idx_local: Integer;
            exact_text_local: string;
            has_competing_exact: Boolean;
            candidate_is_exact: Boolean;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key = '') or
                (Trim(candidate_text) = '') then
            begin
                Exit;
            end;

            if m_dictionary.get_query_choice_bonus(query_key,
                candidate_text) > 0 then
            begin
                Exit;
            end;

            local_syllables := get_effective_compact_pinyin_syllables(query_key);
            if Length(local_syllables) <= 0 then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key,
                local_results_local) then
            begin
                Exit;
            end;

            has_competing_exact := False;
            candidate_is_exact := False;
            for result_idx_local := 0 to High(local_results_local) do
            begin
                if result_idx_local >= c_solver_probe_limit * 3 then
                begin
                    Break;
                end;
                if Trim(local_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                exact_text_local := Trim(local_results_local[result_idx_local].text);
                if get_candidate_text_unit_count(exact_text_local) <>
                    Length(local_syllables) then
                begin
                    Continue;
                end;

                if SameText(exact_text_local, Trim(candidate_text)) then
                begin
                    candidate_is_exact := True;
                end
                else
                begin
                    has_competing_exact := True;
                end;
            end;

            Result := has_competing_exact and (not candidate_is_exact);
        end;

        function get_trailing_context_free_single_char_penalty_solver_local(
            const query_key: string; const candidate_text: string): Integer;
        var
            query_syllables_local: TncPinyinParseResult;
            text_units_local: TArray<string>;
            tail_pinyin_local: string;
            tail_text_local: string;
        begin
            Result := 0;
            if (query_key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(query_key);
            if Length(query_syllables_local) < 2 then
            begin
                Exit;
            end;

            text_units_local := split_text_units(candidate_text);
            if Length(text_units_local) <> Length(query_syllables_local) then
            begin
                Exit;
            end;

            tail_pinyin_local := normalize_pinyin_text(
                query_syllables_local[High(query_syllables_local)].text);
            tail_text_local := text_units_local[High(text_units_local)];
            if SameText(tail_text_local,
                get_fixed_boundary_single_char_for_solver_local(
                tail_pinyin_local)) then
            begin
                Result := get_context_free_single_char_penalty_solver_local(
                    tail_pinyin_local, tail_text_local) * 2;
            end;
        end;

        function is_valid_solver_cjk_phrase_text_local(const value: string;
            const expected_units: Integer): Boolean;
        var
            units: TArray<string>;
            unit_idx: Integer;
            codepoint: Integer;

            function try_get_codepoint_local(const unit_text: string;
                out out_codepoint: Integer): Boolean;
            begin
                Result := False;
                out_codepoint := 0;
                if unit_text = '' then
                begin
                    Exit;
                end;

                if Length(unit_text) = 1 then
                begin
                    out_codepoint := Ord(unit_text[1]);
                    Exit(True);
                end;

                if (Length(unit_text) = 2) and
                    (Ord(unit_text[1]) >= $D800) and
                    (Ord(unit_text[1]) <= $DBFF) and
                    (Ord(unit_text[2]) >= $DC00) and
                    (Ord(unit_text[2]) <= $DFFF) then
                begin
                    out_codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                        (Ord(unit_text[2]) - $DC00) + $10000;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            if Trim(value) = '' then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(units) do
            begin
                if (not try_get_codepoint_local(units[unit_idx], codepoint)) or
                    (codepoint < $4E00) or (codepoint > $9FFF) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function is_solver_boundary_tail_local(const tail_query_key: string): Boolean;
        var
            tail_syllables: TncPinyinParseResult;
            first_tail: string;
        begin
            Result := False;
            if tail_query_key = '' then
            begin
                Exit;
            end;

            tail_syllables := get_effective_compact_pinyin_syllables(tail_query_key);
            if Length(tail_syllables) = 0 then
            begin
                Exit;
            end;

            first_tail := normalize_pinyin_text(tail_syllables[0].text);
            Result := (first_tail = 'de') or (first_tail = 'le') or
                (first_tail = 'liao') or
                (first_tail = 'ma') or (first_tail = 'ba') or
                (first_tail = 'ne') or (first_tail = 'a') or
                (first_tail = 'ya') or (first_tail = 'la') or
                (first_tail = 'wa');
        end;

        function get_first_solver_text_unit_local(const text: string): string;
        var
            units: TArray<string>;
        begin
            Result := '';
            units := split_text_units(text);
            if Length(units) > 0 then
            begin
                Result := units[0];
            end;
        end;

        function try_get_exact_single_char_candidate_local(const start_idx: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            query_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
            preferred_text: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;

            preferred_text := get_fixed_boundary_single_char_for_solver_local(
                effective_syllables[start_idx].text);
            query_key := normalize_pinyin_text(effective_syllables[start_idx].text);
            if (query_key = '') or (m_dictionary = nil) then
            begin
                if preferred_text <> '' then
                begin
                    out_text := preferred_text;
                    out_score := 1500;
                    Exit(True);
                end;
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key, local_results) then
            begin
                if preferred_text <> '' then
                begin
                    out_text := preferred_text;
                    out_score := 1500;
                    Exit(True);
                end;
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_solver_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(local_results[result_idx].text);
                if get_candidate_text_unit_count(candidate_text) <> 1 then
                begin
                    Continue;
                end;

                candidate_score := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_score) then
                begin
                    candidate_score := local_results[result_idx].dict_weight;
                end;
                Dec(candidate_score,
                    get_context_free_single_char_penalty_solver_local(query_key,
                    candidate_text));

                if (preferred_text <> '') and SameText(candidate_text, preferred_text) then
                begin
                    Inc(candidate_score, 1800);
                end;

                if (not Result) or (candidate_score > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_score := candidate_score;
                end;
            end;

            if (not Result) and (preferred_text <> '') then
            begin
                out_text := preferred_text;
                out_score := 1500;
                Result := True;
            end;
        end;

        function try_get_best_single_char_for_pinyin_key_local(
            const pinyin_key: string; out out_text: string;
            out out_score: Integer): Boolean;
        const
            c_single_probe_limit = 12;
        var
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (m_dictionary = nil) or (pinyin_key = '') then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(pinyin_key, local_results) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_single_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[result_idx].text);
                if (get_candidate_text_unit_count(candidate_text) <> 1) or
                    (not is_valid_solver_cjk_phrase_text_local(candidate_text, 1)) then
                begin
                    Continue;
                end;

                candidate_score := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_score) then
                begin
                    candidate_score := local_results[result_idx].dict_weight;
                end;
                Inc(candidate_score, get_preferred_query_phrase_bonus_local(
                    pinyin_key, candidate_text));
                Dec(candidate_score,
                    get_context_free_single_char_penalty_solver_local(pinyin_key,
                    candidate_text));

                if (not Result) or (candidate_score > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text;
                    out_score := candidate_score;
                end;
            end;
        end;

        function try_get_top_two_syllable_exact_text_local(const start_idx: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            query_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (start_idx < 0) or (start_idx + 1 > High(effective_syllables)) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, 2);
            if (query_key = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key, local_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_solver_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[result_idx].text);
                if (get_candidate_text_unit_count(candidate_text) <> 2) or
                    (not is_valid_solver_cjk_phrase_text_local(candidate_text, 2)) then
                begin
                    Continue;
                end;

                candidate_score := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_score) then
                begin
                    candidate_score := local_results[result_idx].dict_weight;
                end;
                Inc(candidate_score, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text));

                if candidate_score > out_score then
                begin
                    out_text := candidate_text;
                    out_score := candidate_score;
                    Result := True;
                end;
            end;
        end;

        function try_get_best_three_syllable_two_plus_one_candidate_local(
            const start_idx: Integer; const query_key: string;
            out out_text: string; out out_score: Integer): Boolean;
        const
            c_head_probe_limit_local = 6;
            c_tail_probe_limit_local = 16;
            c_context_scale_local = 4;
            c_preferred_tail_bonus_local = 96;
            c_complete_promote_bonus_local = 1152;
        var
            head_key_local: string;
            tail_key_local: string;
            head_results_local: TncCandidateList;
            tail_results_local: TncCandidateList;
            head_texts_local: TArray<string>;
            head_scores_local: TArray<Integer>;
            head_option_idx_local: Integer;
            candidate_text_local: string;
            candidate_combined_text_local: string;
            best_tail_text_local: string;
            best_tail_score_local: Integer;
            candidate_score_local: Integer;
            context_bonus_local: Integer;
            prefix_popularity_local: Integer;
            query_bonus_local: Integer;
            exact_bonus_local: Integer;
            idx_local: Integer;
            candidate_units_local: Integer;
            resolved_text_local_321: string;
            resolved_path_local_321: string;
            resolved_score_local_321: Integer;
            resolved_segments_local_321: Integer;
            resolved_anchor_units_local_321: Integer;
            resolved_has_anchor_local_321: Boolean;
            is_preferred_local: Boolean;
            debug_tail_info_local: string;

            procedure add_head_option_local(const text_value: string;
                const score_value: Integer);
            var
                option_idx_local: Integer;
                insert_idx_local: Integer;
                tmp_text_local: string;
                tmp_score_local: Integer;
            begin
                if text_value = '' then
                begin
                    Exit;
                end;
                for option_idx_local := 0 to High(head_texts_local) do
                begin
                    if SameText(head_texts_local[option_idx_local], text_value) then
                    begin
                        if score_value > head_scores_local[option_idx_local] then
                        begin
                            head_scores_local[option_idx_local] := score_value;
                        end;
                        Exit;
                    end;
                end;

                insert_idx_local := Length(head_texts_local);
                SetLength(head_texts_local, insert_idx_local + 1);
                SetLength(head_scores_local, insert_idx_local + 1);
                head_texts_local[insert_idx_local] := text_value;
                head_scores_local[insert_idx_local] := score_value;

                while (insert_idx_local > 0) and
                    (head_scores_local[insert_idx_local] >
                    head_scores_local[insert_idx_local - 1]) do
                begin
                    tmp_text_local := head_texts_local[insert_idx_local - 1];
                    head_texts_local[insert_idx_local - 1] :=
                        head_texts_local[insert_idx_local];
                    head_texts_local[insert_idx_local] := tmp_text_local;
                    tmp_score_local := head_scores_local[insert_idx_local - 1];
                    head_scores_local[insert_idx_local - 1] :=
                        head_scores_local[insert_idx_local];
                    head_scores_local[insert_idx_local] := tmp_score_local;
                    Dec(insert_idx_local);
                end;

                if Length(head_texts_local) > c_head_probe_limit_local then
                begin
                    SetLength(head_texts_local, c_head_probe_limit_local);
                    SetLength(head_scores_local, c_head_probe_limit_local);
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            SetLength(head_texts_local, 0);
            SetLength(head_scores_local, 0);
            debug_tail_info_local := '';

            if (start_idx < 0) or (start_idx + 2 > High(effective_syllables)) or
                (query_key = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            head_key_local := build_query_key_local(start_idx, 2);
            tail_key_local := build_query_key_local(start_idx + 2, 1);
            if (head_key_local = '') or (tail_key_local = '') then
            begin
                Exit;
            end;

            if try_resolve_best_exact_subspan_local(start_idx, 2, resolved_text_local_321,
                resolved_score_local_321, resolved_segments_local_321,
                resolved_path_local_321, resolved_has_anchor_local_321,
                resolved_anchor_units_local_321) and
                (get_candidate_text_unit_count(Trim(resolved_text_local_321)) = 2) and
                is_valid_solver_cjk_phrase_text_local(Trim(resolved_text_local_321), 2) then
            begin
                candidate_score_local := resolved_score_local_321;
                if resolved_has_anchor_local_321 then
                begin
                    Inc(candidate_score_local, resolved_anchor_units_local_321 * 192);
                end;
                if resolved_segments_local_321 > 1 then
                begin
                    Dec(candidate_score_local, (resolved_segments_local_321 - 1) * 96);
                end;
                add_head_option_local(Trim(resolved_text_local_321), candidate_score_local);
            end
            else if m_dictionary.lookup(head_key_local, head_results_local) then
            begin
                for idx_local := 0 to High(head_results_local) do
                begin
                    if idx_local >= c_head_probe_limit_local * 2 then
                    begin
                        Break;
                    end;
                    if Trim(head_results_local[idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(head_results_local[idx_local].text);
                    if (get_candidate_text_unit_count(candidate_text_local) <> 2) or
                        (not is_valid_solver_cjk_phrase_text_local(candidate_text_local, 2)) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := head_results_local[idx_local].score;
                    if head_results_local[idx_local].has_dict_weight and
                        (head_results_local[idx_local].dict_weight > candidate_score_local) then
                    begin
                        candidate_score_local := head_results_local[idx_local].dict_weight;
                    end;
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(head_key_local, candidate_text_local));
                    add_head_option_local(candidate_text_local, candidate_score_local);
                end;
            end;

            if Length(head_texts_local) = 0 then
            begin
                Exit;
            end;

            if not m_dictionary.lookup(tail_key_local, tail_results_local) then
            begin
                Exit;
            end;

            best_tail_text_local := '';
            best_tail_score_local := Low(Integer);
            for idx_local := 0 to High(tail_results_local) do
            begin
                if idx_local >= c_tail_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(tail_results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(tail_results_local[idx_local].text);
                candidate_units_local := get_candidate_text_unit_count(candidate_text_local);
                if (candidate_units_local <> 1) or
                    (not is_valid_solver_cjk_phrase_text_local(candidate_text_local, 1)) then
                begin
                    Continue;
                end;
                if not match_single_char_candidate_for_syllable(tail_key_local,
                    candidate_text_local, is_preferred_local) then
                begin
                    Continue;
                end;

                for head_option_idx_local := 0 to High(head_texts_local) do
                begin
                    candidate_combined_text_local :=
                        head_texts_local[head_option_idx_local] +
                        candidate_text_local;
                    if has_competing_exact_full_phrase_solver_local(query_key,
                        candidate_combined_text_local) then
                    begin
                        Continue;
                    end;

                    context_bonus_local := m_dictionary.get_context_bonus(
                        head_texts_local[head_option_idx_local], candidate_text_local);
                    prefix_popularity_local := 0;
                    if m_dictionary is TncSqliteDictionary then
                    begin
                        prefix_popularity_local := TncSqliteDictionary(m_dictionary).
                            get_prefix_popularity_hint(
                            candidate_combined_text_local);
                    end;
                    exact_bonus_local := get_exact_query_text_bonus_solver_local(query_key,
                        candidate_combined_text_local);
                    query_bonus_local := get_preferred_query_phrase_bonus_local(query_key,
                        candidate_combined_text_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) then
                    begin
                        if m_config.debug_mode and
                            (Length(debug_tail_info_local) < 256) then
                        begin
                            debug_tail_info_local := debug_tail_info_local + Format(
                                ' [%x c=%d p=%d e=%d b=%d]',
                                [Ord(candidate_text_local[1]), context_bonus_local,
                                prefix_popularity_local, exact_bonus_local,
                                Ord(SameText(candidate_text_local,
                                get_fixed_boundary_single_char_for_solver_local(
                                tail_key_local)))]);
                        end;
                        if SameText(candidate_text_local,
                            get_fixed_boundary_single_char_for_solver_local(
                            tail_key_local)) then
                        begin
                            Continue;
                        end;
                    end;

                    candidate_score_local := tail_results_local[idx_local].score;
                    if tail_results_local[idx_local].has_dict_weight and
                        (tail_results_local[idx_local].dict_weight > candidate_score_local) then
                    begin
                        candidate_score_local := tail_results_local[idx_local].dict_weight;
                    end;
                    Inc(candidate_score_local, context_bonus_local * c_context_scale_local);
                    if prefix_popularity_local > 0 then
                    begin
                        Inc(candidate_score_local,
                            Round(Ln(1.0 + prefix_popularity_local) * 256.0));
                    end;
                    Inc(candidate_score_local, query_bonus_local + exact_bonus_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) then
                    begin
                        Dec(candidate_score_local, 384);
                    end;
                    if is_preferred_local then
                    begin
                        Inc(candidate_score_local, c_preferred_tail_bonus_local);
                    end;
                    if m_config.debug_mode and (Length(debug_tail_info_local) < 512) then
                    begin
                        debug_tail_info_local := debug_tail_info_local + Format(
                            ' [%x s=%d]', [Ord(candidate_text_local[1]),
                            candidate_score_local]);
                    end;

                    candidate_score_local := head_scores_local[head_option_idx_local] +
                        candidate_score_local + c_complete_promote_bonus_local;
                    if not solver_candidate_respects_fixed_boundaries_local(
                        start_idx, 3, candidate_combined_text_local) then
                    begin
                        Continue;
                    end;
                    if candidate_score_local > best_tail_score_local then
                    begin
                        best_tail_score_local := candidate_score_local;
                        best_tail_text_local := candidate_combined_text_local;
                    end;
                end;
            end;

            if best_tail_text_local = '' then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' t321=[notail head=%s tail=%s dbg=%s]',
                        [head_key_local, tail_key_local, debug_tail_info_local]);
                end;
                Exit;
            end;

            out_text := best_tail_text_local;
            out_score := best_tail_score_local;
            Result := True;
        end;

        function collect_single_char_candidates_local(const pinyin_key: string;
            out out_texts: TArray<string>; out out_scores: TArray<Integer>): Boolean;
        const
            c_single_option_limit = 6;
            c_single_option_probe_limit = 12;
        var
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;

            procedure add_option_local(const text_value: string;
                const score_value: Integer);
            var
                option_idx: Integer;
                insert_idx: Integer;
                tmp_text: string;
                tmp_score: Integer;
            begin
                if text_value = '' then
                begin
                    Exit;
                end;
                for option_idx := 0 to High(out_texts) do
                begin
                    if SameText(out_texts[option_idx], text_value) then
                    begin
                        if score_value > out_scores[option_idx] then
                        begin
                            out_scores[option_idx] := score_value;
                        end;
                        Exit;
                    end;
                end;

                insert_idx := Length(out_texts);
                SetLength(out_texts, insert_idx + 1);
                SetLength(out_scores, insert_idx + 1);
                out_texts[insert_idx] := text_value;
                out_scores[insert_idx] := score_value;

                while (insert_idx > 0) and
                    (out_scores[insert_idx] > out_scores[insert_idx - 1]) do
                begin
                    tmp_text := out_texts[insert_idx - 1];
                    out_texts[insert_idx - 1] := out_texts[insert_idx];
                    out_texts[insert_idx] := tmp_text;
                    tmp_score := out_scores[insert_idx - 1];
                    out_scores[insert_idx - 1] := out_scores[insert_idx];
                    out_scores[insert_idx] := tmp_score;
                    Dec(insert_idx);
                end;

                if Length(out_texts) > c_single_option_limit then
                begin
                    SetLength(out_texts, c_single_option_limit);
                    SetLength(out_scores, c_single_option_limit);
                end;
            end;
        begin
            SetLength(out_texts, 0);
            SetLength(out_scores, 0);
            if (m_dictionary = nil) or (pinyin_key = '') then
            begin
                Exit(Length(out_texts) > 0);
            end;
            if not m_dictionary.lookup_exact_full_pinyin(pinyin_key, local_results) then
            begin
                Exit(Length(out_texts) > 0);
            end;

            for result_idx := 0 to High(local_results) do
            begin
                if result_idx >= c_single_option_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[result_idx].text);
                if (get_candidate_text_unit_count(candidate_text) <> 1) or
                    (not is_valid_solver_cjk_phrase_text_local(candidate_text, 1)) then
                begin
                    Continue;
                end;

                candidate_score := local_results[result_idx].score;
                if local_results[result_idx].has_dict_weight and
                    (local_results[result_idx].dict_weight > candidate_score) then
                begin
                    candidate_score := local_results[result_idx].dict_weight;
                end;
                Inc(candidate_score, get_preferred_query_phrase_bonus_local(
                    pinyin_key, candidate_text));
                Dec(candidate_score,
                    get_context_free_single_char_penalty_solver_local(pinyin_key,
                    candidate_text));
                add_option_local(candidate_text, candidate_score);
            end;

            Result := Length(out_texts) > 0;
        end;

        function try_get_best_two_syllable_contextual_single_char_combo_local(
            const start_idx: Integer; const query_key: string;
            out out_text: string; out out_score: Integer): Boolean;
        const
            c_combo_probe_penalty = 64;
            c_context_scale_local = 4;
        var
            left_key_local: string;
            right_key_local: string;
            left_option_texts_local: TArray<string>;
            left_option_scores_local: TArray<Integer>;
            right_option_texts_local: TArray<string>;
            right_option_scores_local: TArray<Integer>;
            left_idx_local: Integer;
            right_idx_local: Integer;
            combined_text_local: string;
            combined_score_local: Integer;
            context_bonus_local: Integer;
            prefix_popularity_local: Integer;
            query_bonus_local: Integer;
            exact_bonus_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);
            if (start_idx < 0) or (start_idx + 1 > High(effective_syllables)) or
                (query_key = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            left_key_local := build_query_key_local(start_idx, 1);
            right_key_local := build_query_key_local(start_idx + 1, 1);
            if (left_key_local = '') or (right_key_local = '') then
            begin
                Exit;
            end;

            if (not collect_single_char_candidates_local(left_key_local,
                left_option_texts_local, left_option_scores_local)) or
                (not collect_single_char_candidates_local(right_key_local,
                right_option_texts_local, right_option_scores_local)) then
            begin
                Exit;
            end;

            for left_idx_local := 0 to High(left_option_texts_local) do
            begin
                for right_idx_local := 0 to High(right_option_texts_local) do
                begin
                    combined_text_local := left_option_texts_local[left_idx_local] +
                        right_option_texts_local[right_idx_local];
                    if not is_valid_solver_cjk_phrase_text_local(combined_text_local, 2) then
                    begin
                        Continue;
                    end;
                    if not solver_candidate_respects_fixed_boundaries_local(
                        start_idx, 2, combined_text_local) then
                    begin
                        Continue;
                    end;

                    context_bonus_local := m_dictionary.get_context_bonus(
                        left_option_texts_local[left_idx_local],
                        right_option_texts_local[right_idx_local]);
                    prefix_popularity_local := 0;
                    if m_dictionary is TncSqliteDictionary then
                    begin
                        prefix_popularity_local := TncSqliteDictionary(m_dictionary).
                            get_prefix_popularity_hint(combined_text_local);
                    end;
                    exact_bonus_local := get_exact_query_text_bonus_solver_local(
                        query_key, combined_text_local);
                    query_bonus_local := get_preferred_query_phrase_bonus_local(
                        query_key, combined_text_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) and
                        SameText(right_option_texts_local[right_idx_local],
                        get_fixed_boundary_single_char_for_solver_local(right_key_local)) then
                    begin
                        Continue;
                    end;

                    combined_score_local := left_option_scores_local[left_idx_local] +
                        right_option_scores_local[right_idx_local] -
                        c_combo_probe_penalty;
                    Inc(combined_score_local, context_bonus_local *
                        c_context_scale_local);
                    if prefix_popularity_local > 0 then
                    begin
                        Inc(combined_score_local,
                            Round(Ln(1.0 + prefix_popularity_local) * 256.0));
                    end;
                    Inc(combined_score_local, exact_bonus_local + query_bonus_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) then
                    begin
                        Dec(combined_score_local, 192);
                    end;

                    if (not Result) or (combined_score_local > out_score) then
                    begin
                        Result := True;
                        out_text := combined_text_local;
                        out_score := combined_score_local;
                    end;
                end;
            end;
        end;

        function collect_two_syllable_contextual_options_local(
            const start_idx: Integer; const query_key: string;
            out out_texts: TArray<string>; out out_scores: TArray<Integer>): Boolean;
        const
            c_option_limit_local = 8;
            c_combo_probe_penalty = 64;
            c_context_scale_local = 4;
        var
            left_key_local: string;
            right_key_local: string;
            left_option_texts_local: TArray<string>;
            left_option_scores_local: TArray<Integer>;
            right_option_texts_local: TArray<string>;
            right_option_scores_local: TArray<Integer>;
            left_idx_local: Integer;
            right_idx_local: Integer;
            combined_text_local: string;
            combined_score_local: Integer;
            context_bonus_local: Integer;
            prefix_popularity_local: Integer;
            query_bonus_local: Integer;
            exact_bonus_local: Integer;

            procedure add_option_local(const text_value: string;
                const score_value: Integer);
            var
                option_idx_local: Integer;
                insert_idx_local: Integer;
                tmp_text_local: string;
                tmp_score_local: Integer;
            begin
                if text_value = '' then
                begin
                    Exit;
                end;
                for option_idx_local := 0 to High(out_texts) do
                begin
                    if SameText(out_texts[option_idx_local], text_value) then
                    begin
                        if score_value > out_scores[option_idx_local] then
                        begin
                            out_scores[option_idx_local] := score_value;
                        end;
                        Exit;
                    end;
                end;

                insert_idx_local := Length(out_texts);
                SetLength(out_texts, insert_idx_local + 1);
                SetLength(out_scores, insert_idx_local + 1);
                out_texts[insert_idx_local] := text_value;
                out_scores[insert_idx_local] := score_value;

                while (insert_idx_local > 0) and
                    (out_scores[insert_idx_local] > out_scores[insert_idx_local - 1]) do
                begin
                    tmp_text_local := out_texts[insert_idx_local - 1];
                    out_texts[insert_idx_local - 1] := out_texts[insert_idx_local];
                    out_texts[insert_idx_local] := tmp_text_local;
                    tmp_score_local := out_scores[insert_idx_local - 1];
                    out_scores[insert_idx_local - 1] := out_scores[insert_idx_local];
                    out_scores[insert_idx_local] := tmp_score_local;
                    Dec(insert_idx_local);
                end;

                if Length(out_texts) > c_option_limit_local then
                begin
                    SetLength(out_texts, c_option_limit_local);
                    SetLength(out_scores, c_option_limit_local);
                end;
            end;
        begin
            Result := False;
            SetLength(out_texts, 0);
            SetLength(out_scores, 0);
            if (start_idx < 0) or (start_idx + 1 > High(effective_syllables)) or
                (query_key = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            left_key_local := build_query_key_local(start_idx, 1);
            right_key_local := build_query_key_local(start_idx + 1, 1);
            if (left_key_local = '') or (right_key_local = '') then
            begin
                Exit;
            end;

            if (not collect_single_char_candidates_local(left_key_local,
                left_option_texts_local, left_option_scores_local)) or
                (not collect_single_char_candidates_local(right_key_local,
                right_option_texts_local, right_option_scores_local)) then
            begin
                Exit;
            end;

            for left_idx_local := 0 to High(left_option_texts_local) do
            begin
                for right_idx_local := 0 to High(right_option_texts_local) do
                begin
                    combined_text_local := left_option_texts_local[left_idx_local] +
                        right_option_texts_local[right_idx_local];
                    if not is_valid_solver_cjk_phrase_text_local(combined_text_local, 2) then
                    begin
                        Continue;
                    end;
                    if not solver_candidate_respects_fixed_boundaries_local(
                        start_idx, 2, combined_text_local) then
                    begin
                        Continue;
                    end;

                    context_bonus_local := m_dictionary.get_context_bonus(
                        left_option_texts_local[left_idx_local],
                        right_option_texts_local[right_idx_local]);
                    prefix_popularity_local := 0;
                    if m_dictionary is TncSqliteDictionary then
                    begin
                        prefix_popularity_local := TncSqliteDictionary(m_dictionary).
                            get_prefix_popularity_hint(combined_text_local);
                    end;
                    exact_bonus_local := get_exact_query_text_bonus_solver_local(
                        query_key, combined_text_local);
                    query_bonus_local := get_preferred_query_phrase_bonus_local(
                        query_key, combined_text_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) and
                        SameText(right_option_texts_local[right_idx_local],
                        get_fixed_boundary_single_char_for_solver_local(right_key_local)) then
                    begin
                        Continue;
                    end;

                    combined_score_local := left_option_scores_local[left_idx_local] +
                        right_option_scores_local[right_idx_local] -
                        c_combo_probe_penalty;
                    Inc(combined_score_local, context_bonus_local *
                        c_context_scale_local);
                    if prefix_popularity_local > 0 then
                    begin
                        Inc(combined_score_local,
                            Round(Ln(1.0 + prefix_popularity_local) * 256.0));
                    end;
                    Inc(combined_score_local, exact_bonus_local + query_bonus_local);
                    if (context_bonus_local <= 0) and (prefix_popularity_local <= 0) and
                        (exact_bonus_local <= 0) then
                    begin
                        Dec(combined_score_local, 192);
                    end;

                    add_option_local(combined_text_local, combined_score_local);
                end;
            end;

            Result := Length(out_texts) > 0;
        end;

        function try_get_best_query_key_candidate_local(const query_key: string;
            const expected_syllables: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            cached_state: Integer;
            snapshot_candidates: TncCandidateList;
            snapshot_scores: TArray<Integer>;
            candidate_idx: Integer;
            candidate: TncCandidate;
            candidate_text: string;
            candidate_score: Integer;
            candidate_units: Integer;
            best_complete_text: string;
            best_complete_score: Integer;
            best_partial_text: string;
            best_partial_score: Integer;
            best_partial_comment: string;
            best_split_text: string;
            best_split_score: Integer;
            comment_key: string;
            comment_syllables: TncPinyinParseResult;
            tail_text: string;
            tail_score: Integer;
            combined_text: string;
            combined_score: Integer;
            context_bonus: Integer;
            query_syllables_local: TncPinyinParseResult;
            split_idx: Integer;
            left_key: string;
            right_key: string;
            left_text: string;
            left_score: Integer;
            right_text: string;
            right_score: Integer;
            left_option_texts: TArray<string>;
            left_option_scores: TArray<Integer>;
            right_option_texts: TArray<string>;
            right_option_scores: TArray<Integer>;
            left_option_idx: Integer;
            right_option_idx: Integer;
            left_candidate_text: string;
            left_candidate_score: Integer;
            right_candidate_text: string;
            right_candidate_score: Integer;
            local_idx: Integer;
            infer_score: Integer;
            infer_segments: Integer;
            inferred_path: string;
            aligned_start_idx: Integer;
            aligned_has_anchor: Boolean;
            aligned_anchor_units: Integer;
            allow_nested_engine_local: Boolean;
            aligned_exact_text_local: string;
            aligned_exact_score_local: Integer;
            aligned_two_char_text_local: string;
            aligned_two_char_score_local: Integer;
            aligned_contextual_text_local: string;
            aligned_contextual_score_local: Integer;
            candidate_has_exact_match_local: Boolean;
            best_complete_locked_local: Boolean;

            function try_find_aligned_query_start_local: Boolean;
            var
                query_syllables_match_local: TncPinyinParseResult;
                lookup_syllables_match_local: TncPinyinParseResult;
                start_idx_match_local: Integer;
                unit_idx_match_local: Integer;
                matched_local: Boolean;
            begin
                Result := False;
                aligned_start_idx := -1;
                if (query_key = '') or (not is_full_pinyin_key(query_key)) or
                    (not is_full_pinyin_key(lookup_text)) or
                    (expected_syllables <= 0) or
                    (input_syllable_count < expected_syllables) then
                begin
                    Exit;
                end;

                query_syllables_match_local :=
                    get_effective_compact_pinyin_syllables(query_key);
                if Length(query_syllables_match_local) <> expected_syllables then
                begin
                    Exit;
                end;

                lookup_syllables_match_local :=
                    get_effective_compact_pinyin_syllables(lookup_text);
                if Length(lookup_syllables_match_local) <> input_syllable_count then
                begin
                    Exit;
                end;

                for start_idx_match_local := 0 to
                    input_syllable_count - expected_syllables do
                begin
                    matched_local := True;
                    for unit_idx_match_local := 0 to expected_syllables - 1 do
                    begin
                        if not SameText(normalize_pinyin_text(
                            lookup_syllables_match_local[start_idx_match_local +
                            unit_idx_match_local].text), normalize_pinyin_text(
                            query_syllables_match_local[unit_idx_match_local].text)) then
                        begin
                            matched_local := False;
                            Break;
                        end;
                    end;
                    if matched_local then
                    begin
                        aligned_start_idx := start_idx_match_local;
                        Exit(True);
                    end;
                end;
            end;

            function try_collect_aligned_exact_options_local(
                const option_start_idx_local: Integer;
                const option_query_key_local: string;
                const option_syllables_local: Integer;
                out out_texts_local: TArray<string>;
                out out_scores_local: TArray<Integer>): Boolean;
            var
                local_results_local: TncCandidateList;
                result_idx_local: Integer;
                candidate_text_local: string;
                candidate_units_local: Integer;
                candidate_score_local: Integer;
                insert_idx_local: Integer;
                option_idx_local: Integer;
                option_limit_local: Integer;
                tmp_text_local: string;
                tmp_score_local: Integer;
            begin
                Result := False;
                SetLength(out_texts_local, 0);
                SetLength(out_scores_local, 0);
                if (option_start_idx_local < 0) or (m_dictionary = nil) or
                    (option_query_key_local = '') or (option_syllables_local <= 1) then
                begin
                    Exit;
                end;

                if not m_dictionary.lookup_exact_full_pinyin(
                    option_query_key_local, local_results_local) then
                begin
                    Exit;
                end;

                if option_syllables_local >= 3 then
                begin
                    option_limit_local := 2;
                end
                else
                begin
                    option_limit_local := c_solver_beam_limit;
                end;

                for result_idx_local := 0 to High(local_results_local) do
                begin
                    if result_idx_local >= c_solver_probe_limit * 3 then
                    begin
                        Break;
                    end;
                    if Trim(local_results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results_local[result_idx_local].text);
                    candidate_units_local :=
                        get_candidate_text_unit_count(candidate_text_local);
                    if (candidate_units_local < 2) or
                        (candidate_units_local > option_syllables_local) or
                        (not is_valid_solver_cjk_phrase_text_local(
                        candidate_text_local, candidate_units_local)) then
                    begin
                        Continue;
                    end;
                    if (option_syllables_local >= 4) and
                        (candidate_units_local <> option_syllables_local) then
                    begin
                        Continue;
                    end;
                    if not solver_candidate_respects_fixed_boundaries_local(
                        option_start_idx_local, option_syllables_local,
                        candidate_text_local) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := local_results_local[result_idx_local].score;
                    if local_results_local[result_idx_local].has_dict_weight and
                        (local_results_local[result_idx_local].dict_weight >
                        candidate_score_local) then
                    begin
                        candidate_score_local :=
                            local_results_local[result_idx_local].dict_weight;
                    end;
                    Inc(candidate_score_local,
                        get_preferred_query_phrase_bonus_local(option_query_key_local,
                        candidate_text_local));
                    Inc(candidate_score_local,
                        get_preferred_exact_subspan_bonus_local(option_query_key_local,
                        candidate_text_local));
                    Inc(candidate_score_local,
                        get_exact_query_text_bonus_solver_local(option_query_key_local,
                        candidate_text_local));
                    Dec(candidate_score_local,
                        get_trailing_context_free_single_char_penalty_solver_local(
                        option_query_key_local, candidate_text_local));

                    for option_idx_local := 0 to High(out_texts_local) do
                    begin
                        if SameText(out_texts_local[option_idx_local],
                            candidate_text_local) then
                        begin
                            if candidate_score_local >
                                out_scores_local[option_idx_local] then
                            begin
                                out_scores_local[option_idx_local] :=
                                    candidate_score_local;
                            end;
                            candidate_text_local := '';
                            Break;
                        end;
                    end;
                    if candidate_text_local = '' then
                    begin
                        Continue;
                    end;

                    insert_idx_local := Length(out_texts_local);
                    SetLength(out_texts_local, insert_idx_local + 1);
                    SetLength(out_scores_local, insert_idx_local + 1);
                    out_texts_local[insert_idx_local] := candidate_text_local;
                    out_scores_local[insert_idx_local] := candidate_score_local;

                    while (insert_idx_local > 0) and
                        (out_scores_local[insert_idx_local] >
                        out_scores_local[insert_idx_local - 1]) do
                    begin
                        tmp_text_local := out_texts_local[insert_idx_local - 1];
                        out_texts_local[insert_idx_local - 1] :=
                            out_texts_local[insert_idx_local];
                        out_texts_local[insert_idx_local] := tmp_text_local;
                        tmp_score_local := out_scores_local[insert_idx_local - 1];
                        out_scores_local[insert_idx_local - 1] :=
                            out_scores_local[insert_idx_local];
                        out_scores_local[insert_idx_local] := tmp_score_local;
                        Dec(insert_idx_local);
                    end;

                    if Length(out_texts_local) > option_limit_local then
                    begin
                        SetLength(out_texts_local, option_limit_local);
                        SetLength(out_scores_local, option_limit_local);
                    end;
                end;

                if (option_syllables_local >= 3) and (Length(out_scores_local) > 1) and
                    (out_scores_local[0] >= out_scores_local[1] + 512) then
                begin
                    SetLength(out_texts_local, 1);
                    SetLength(out_scores_local, 1);
                end;

                Result := Length(out_texts_local) > 0;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            aligned_exact_text_local := '';
            aligned_exact_score_local := Low(Integer);
            aligned_two_char_text_local := '';
            aligned_two_char_score_local := Low(Integer);
            aligned_contextual_text_local := '';
            aligned_contextual_score_local := Low(Integer);
            best_complete_locked_local := False;
            if solver_time_budget_exceeded_local then
            begin
                Exit;
            end;
            if (query_key = '') or (not is_full_pinyin_key(query_key)) or
                (expected_syllables <= 0) or
                (expected_syllables > c_solver_max_chunk_syllables) then
            begin
                Exit;
            end;

            if expected_syllables = 1 then
            begin
                Exit(try_get_best_single_char_for_pinyin_key_local(query_key,
                    out_text, out_score));
            end;

            if query_cache_states.TryGetValue(query_key, cached_state) then
            begin
                if cached_state = 1 then
                begin
                    query_cache_texts.TryGetValue(query_key, out_text);
                    query_cache_scores.TryGetValue(query_key, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            best_complete_text := '';
            best_complete_score := Low(Integer);
            best_partial_text := '';
            best_partial_score := Low(Integer);
            best_partial_comment := '';
            best_split_text := '';
            best_split_score := Low(Integer);

            if try_find_aligned_query_start_local and
                try_resolve_best_exact_subspan_local(aligned_start_idx,
                expected_syllables, aligned_exact_text_local,
                aligned_exact_score_local, infer_segments, inferred_path,
                aligned_has_anchor, aligned_anchor_units) then
            begin
                if solver_candidate_respects_fixed_boundaries_local(
                    aligned_start_idx, expected_syllables, aligned_exact_text_local) then
                begin
                    best_complete_text := aligned_exact_text_local;
                    best_complete_score := aligned_exact_score_local -
                        get_trailing_context_free_single_char_penalty_solver_local(
                        query_key, aligned_exact_text_local);
                    best_complete_locked_local := True;
                end;
            end;
            if (expected_syllables = 2) and (aligned_start_idx >= 0) and
                try_get_best_two_syllable_contextual_single_char_combo_local(
                aligned_start_idx, query_key, aligned_two_char_text_local,
                aligned_two_char_score_local) and
                ((best_complete_text = '') or
                (aligned_two_char_score_local > best_complete_score)) then
            begin
                best_complete_text := aligned_two_char_text_local;
                best_complete_score := aligned_two_char_score_local;
                best_complete_locked_local := True;
            end;
            if (expected_syllables = 3) and (aligned_start_idx >= 0) and
                try_get_best_three_syllable_two_plus_one_candidate_local(
                aligned_start_idx, query_key, aligned_contextual_text_local,
                aligned_contextual_score_local) and
                ((best_complete_text = '') or
                (aligned_contextual_score_local > best_complete_score)) then
            begin
                best_complete_text := aligned_contextual_text_local;
                best_complete_score := aligned_contextual_score_local;
                best_complete_locked_local := True;
            end;

            allow_nested_engine_local := False;
            if allow_nested_engine_local then
            begin
                if query_engine = nil then
                begin
                    query_engine_config := m_config;
                    query_engine_config.debug_mode := False;
                    query_engine_config.max_candidates := 96;
                    query_engine := TncEngine.Create(query_engine_config,
                        False, False);
                    inherit_search_budget_policy_to(query_engine);
                    query_engine.m_disable_subspan_standalone_oracle :=
                        m_disable_subspan_standalone_oracle + 1;
                end;

                query_cache_states.AddOrSetValue(query_key, 3);
                query_engine.reset;
                query_engine.m_disable_subspan_standalone_oracle :=
                    m_disable_subspan_standalone_oracle + 1;
                query_engine.m_composition_text := query_key;
                query_engine.build_candidates;
                SetLength(snapshot_candidates, Length(query_engine.m_candidates));
                SetLength(snapshot_scores, Length(query_engine.m_candidates));
                for candidate_idx := 0 to High(query_engine.m_candidates) do
                begin
                    snapshot_candidates[candidate_idx] :=
                        query_engine.m_candidates[candidate_idx];
                    snapshot_scores[candidate_idx] := query_engine.get_rank_score(
                        query_engine.m_candidates[candidate_idx]);
                end;

                for candidate_idx := 0 to High(snapshot_candidates) do
                begin
                    if candidate_idx >= c_solver_probe_limit then
                    begin
                        Break;
                    end;

                    candidate := snapshot_candidates[candidate_idx];
                    candidate_text := Trim(candidate.text);
                    if candidate_text = '' then
                    begin
                        Continue;
                    end;

                    candidate_score := snapshot_scores[candidate_idx];
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if candidate_units <= 0 then
                    begin
                        Continue;
                    end;

                    if Trim(candidate.comment) = '' then
                    begin
                        if (candidate_units <> expected_syllables) or
                            (not is_valid_solver_cjk_phrase_text_local(
                            candidate_text, expected_syllables)) then
                        begin
                            Continue;
                        end;

                        inferred_path := infer_segment_path_for_query_text_with_score(
                            query_key, candidate_text, infer_score,
                            infer_segments);
                        if inferred_path <> '' then
                        begin
                            Inc(candidate_score, infer_score);
                            Inc(candidate_score, infer_segments * 320);
                        end
                        else if expected_syllables >= 4 then
                        begin
                            Dec(candidate_score, 960);
                        end;
                        Inc(candidate_score,
                            get_preferred_query_phrase_bonus_local(query_key,
                            candidate_text));
                        Inc(candidate_score,
                            get_exact_query_text_bonus_solver_local(query_key,
                            candidate_text));
                        Dec(candidate_score,
                            get_trailing_context_free_single_char_penalty_solver_local(
                            query_key, candidate_text));
                        candidate_has_exact_match_local :=
                            has_exact_query_text_match_solver_local(query_key,
                            candidate_text);
                        if best_complete_locked_local and
                            (expected_syllables <= 3) and
                            (best_complete_text <> '') and
                            (not SameText(candidate_text, best_complete_text)) and
                            (not candidate_has_exact_match_local) then
                        begin
                            Continue;
                        end;

                        if candidate_score > best_complete_score then
                        begin
                            best_complete_text := candidate_text;
                            best_complete_score := candidate_score;
                        end;
                        Continue;
                    end;

                    comment_key := normalize_pinyin_text(candidate.comment);
                    if (comment_key = '') or
                        (not is_full_pinyin_key(comment_key)) then
                    begin
                        Continue;
                    end;

                    comment_syllables :=
                        get_effective_compact_pinyin_syllables(comment_key);
                    if (Length(comment_syllables) = 0) or
                        (candidate_units + Length(comment_syllables) <>
                        expected_syllables) then
                    begin
                        Continue;
                    end;

                    if not try_get_best_query_key_candidate_local(comment_key,
                        Length(comment_syllables), tail_text, tail_score) then
                    begin
                        Continue;
                    end;

                    combined_text := candidate_text + tail_text;
                    if (aligned_start_idx >= 0) and
                        (not solver_candidate_respects_fixed_boundaries_local(
                        aligned_start_idx, expected_syllables, combined_text)) then
                    begin
                        Continue;
                    end;
                    combined_score := candidate_score + tail_score;
                    if m_dictionary <> nil then
                    begin
                        context_bonus := m_dictionary.get_context_bonus(
                            candidate_text, tail_text);
                        Inc(combined_score, context_bonus);
                    end;
                    Inc(combined_score,
                        get_preferred_query_phrase_bonus_local(query_key,
                        combined_text));
                    Inc(combined_score,
                        get_exact_query_text_bonus_solver_local(query_key,
                        combined_text));
                    if is_solver_boundary_tail_local(comment_key) then
                    begin
                        Inc(combined_score, 1024);
                    end;

                    if combined_score > best_partial_score then
                    begin
                        best_partial_text := combined_text;
                        best_partial_score := combined_score;
                        best_partial_comment := comment_key;
                    end;
                end;
            end;

            query_syllables_local := get_effective_compact_pinyin_syllables(query_key);
            if Length(query_syllables_local) = expected_syllables then
            begin
                for split_idx := 1 to expected_syllables - 1 do
                begin
                    left_key := '';
                    for local_idx := 0 to split_idx - 1 do
                    begin
                        left_key := left_key + normalize_pinyin_text(
                            query_syllables_local[local_idx].text);
                    end;

                    right_key := '';
                    for local_idx := split_idx to High(query_syllables_local) do
                    begin
                        right_key := right_key + normalize_pinyin_text(
                            query_syllables_local[local_idx].text);
                    end;

                    if (left_key = '') or (right_key = '') then
                    begin
                        Continue;
                    end;

                    SetLength(left_option_texts, 0);
                    SetLength(left_option_scores, 0);
                    SetLength(right_option_texts, 0);
                    SetLength(right_option_scores, 0);

                    if split_idx = 1 then
                    begin
                        if not collect_single_char_candidates_local(left_key,
                            left_option_texts, left_option_scores) then
                        begin
                            Continue;
                        end;
                    end
                    else if (aligned_start_idx >= 0) and
                        try_collect_aligned_exact_options_local(
                        aligned_start_idx, left_key, split_idx,
                        left_option_texts, left_option_scores) then
                    begin
                    end
                    else if try_get_best_query_key_candidate_local(left_key, split_idx,
                        left_text, left_score) then
                    begin
                        SetLength(left_option_texts, 1);
                        SetLength(left_option_scores, 1);
                        left_option_texts[0] := left_text;
                        left_option_scores[0] := left_score;
                    end
                    else
                    begin
                        Continue;
                    end;

                    if expected_syllables - split_idx = 1 then
                    begin
                        if not collect_single_char_candidates_local(right_key,
                            right_option_texts, right_option_scores) then
                        begin
                            Continue;
                        end;
                    end
                    else if (aligned_start_idx >= 0) and
                        try_collect_aligned_exact_options_local(
                        aligned_start_idx + split_idx, right_key,
                        expected_syllables - split_idx,
                        right_option_texts, right_option_scores) then
                    begin
                    end
                    else if try_get_best_query_key_candidate_local(right_key,
                        expected_syllables - split_idx, right_text, right_score) then
                    begin
                        SetLength(right_option_texts, 1);
                        SetLength(right_option_scores, 1);
                        right_option_texts[0] := right_text;
                        right_option_scores[0] := right_score;
                    end
                    else
                    begin
                        Continue;
                    end;

                    for left_option_idx := 0 to High(left_option_texts) do
                    begin
                        left_candidate_text := left_option_texts[left_option_idx];
                        left_candidate_score := left_option_scores[left_option_idx];
                        for right_option_idx := 0 to High(right_option_texts) do
                        begin
                            right_candidate_text :=
                                right_option_texts[right_option_idx];
                            right_candidate_score :=
                                right_option_scores[right_option_idx];

                            combined_text := left_candidate_text +
                                right_candidate_text;
                            if not is_valid_solver_cjk_phrase_text_local(combined_text,
                                expected_syllables) then
                            begin
                                Continue;
                            end;
                            if (aligned_start_idx >= 0) and
                                (not solver_candidate_respects_fixed_boundaries_local(
                                aligned_start_idx, expected_syllables,
                                combined_text)) then
                            begin
                                Continue;
                            end;

                            if (split_idx = 1) and
                                (get_exact_query_text_bonus_solver_local(query_key,
                                combined_text) = 0) and
                                (not SameText(left_candidate_text,
                                get_fixed_boundary_single_char_for_solver_local(
                                left_key))) then
                            begin
                                Continue;
                            end;

                            combined_score := left_candidate_score +
                                right_candidate_score - 96;
                            Inc(combined_score,
                                get_preferred_query_phrase_bonus_local(query_key,
                                combined_text));
                            Inc(combined_score,
                                get_exact_query_text_bonus_solver_local(query_key,
                                combined_text));
                            if m_dictionary <> nil then
                            begin
                                context_bonus := m_dictionary.get_context_bonus(
                                    left_candidate_text, right_candidate_text);
                                Inc(combined_score, context_bonus);
                            end;

                            inferred_path := infer_segment_path_for_query_text_with_score(
                                query_key, combined_text, infer_score,
                                infer_segments);
                            if inferred_path <> '' then
                            begin
                                Inc(combined_score, infer_score);
                                Inc(combined_score, infer_segments * 320);
                            end;

                            if combined_score > best_split_score then
                            begin
                                best_split_text := combined_text;
                                best_split_score := combined_score;
                            end;
                        end;
                    end;
                end;
            end;

            if best_complete_text <> '' then
            begin
                out_text := best_complete_text;
                out_score := best_complete_score;
                if (best_partial_text <> '') and
                    is_solver_boundary_tail_local(best_partial_comment) and
                    (best_partial_score > best_complete_score + 512) then
                begin
                    out_text := best_partial_text;
                    out_score := best_partial_score;
                end;
            end;

            if (best_split_text <> '') and
                ((out_text = '') or
                (best_split_score > out_score - 128) or
                ((expected_syllables >= 4) and
                (best_split_score + 256 >= out_score))) then
            begin
                out_text := best_split_text;
                out_score := best_split_score;
            end;

            if out_text <> '' then
            begin
                query_cache_states.AddOrSetValue(query_key, 1);
                query_cache_texts.AddOrSetValue(query_key, out_text);
                query_cache_scores.AddOrSetValue(query_key, out_score);
                Exit(True);
            end;

            if best_partial_text <> '' then
            begin
                out_text := best_partial_text;
                out_score := best_partial_score;
                query_cache_states.AddOrSetValue(query_key, 1);
                query_cache_texts.AddOrSetValue(query_key, out_text);
                query_cache_scores.AddOrSetValue(query_key, out_score);
                Exit(True);
            end;

            query_cache_states.AddOrSetValue(query_key, 2);
        end;

        function try_get_best_subquery_candidate_local(const start_idx: Integer;
            const syllable_count_part: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            query_key: string;
            resolved_path: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            resolved_units: Integer;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            allow_direct_exact_local: Boolean;
            contextual_two_text_local: string;
            contextual_two_score_local: Integer;
            contextual_text_local: string;
            contextual_score_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            preferred_text_local := '';
            preferred_bonus_local := 0;
            contextual_two_text_local := '';
            contextual_two_score_local := Low(Integer);
            contextual_text_local := '';
            contextual_score_local := Low(Integer);
            if solver_time_budget_exceeded_local then
            begin
                Exit;
            end;

            if syllable_count_part = 1 then
            begin
                Exit(try_get_exact_single_char_candidate_local(start_idx,
                    out_text, out_score));
            end;

            if syllable_count_part > c_solver_max_chunk_syllables then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, syllable_count_part);
            if query_key <> '' then
            begin
                try_get_preferred_query_phrase_text_local(query_key,
                    preferred_text_local, preferred_bonus_local);
            end;
            allow_direct_exact_local := (syllable_count_part <= 3) or
                (preferred_bonus_local > 0);

            if allow_direct_exact_local and
                try_resolve_best_exact_subspan_local(start_idx, syllable_count_part,
                out_text, resolved_score, resolved_segments, resolved_path,
                resolved_has_anchor, resolved_anchor_units) then
            begin
                resolved_units := get_candidate_text_unit_count(Trim(out_text));
                if (resolved_units >= 2) and
                    (resolved_units <= syllable_count_part) and
                    is_valid_solver_cjk_phrase_text_local(out_text,
                    resolved_units) then
                begin
                    out_score := resolved_score;
                    if resolved_has_anchor then
                    begin
                        Inc(out_score, resolved_anchor_units * 192);
                    end;
                    if resolved_segments > 1 then
                    begin
                        Dec(out_score, (resolved_segments - 1) * 96);
                    end;
                end;
            end;
            if (syllable_count_part = 2) and
                try_get_best_two_syllable_contextual_single_char_combo_local(
                start_idx, query_key, contextual_two_text_local,
                contextual_two_score_local) and
                ((out_text = '') or (contextual_two_score_local > out_score)) then
            begin
                out_text := contextual_two_text_local;
                out_score := contextual_two_score_local;
            end;
            if (syllable_count_part = 3) and
                try_get_best_three_syllable_two_plus_one_candidate_local(
                start_idx, query_key, contextual_text_local,
                contextual_score_local) and
                ((out_text = '') or (contextual_score_local > out_score)) then
            begin
                out_text := contextual_text_local;
                out_score := contextual_score_local;
            end;
            if out_text <> '' then
            begin
                Exit(True);
            end;

            if (query_key = '') or (not is_full_pinyin_key(query_key)) then
            begin
                Exit;
            end;

            Result := try_get_best_query_key_candidate_local(query_key,
                syllable_count_part, out_text, out_score);
        end;

        function is_solver_beam_option_better_local(
            const left_option: TSubquerySolverBeamOption;
            const right_option: TSubquerySolverBeamOption): Boolean;
        begin
            Result := False;
            if left_option.score > right_option.score then
            begin
                Exit(True);
            end;
            if left_option.score < right_option.score then
            begin
                Exit(False);
            end;
            if left_option.segments < right_option.segments then
            begin
                Exit(True);
            end;
            if left_option.segments > right_option.segments then
            begin
                Exit(False);
            end;
            if (left_option.text <> '') and
                ((right_option.text = '') or
                (CompareText(left_option.text, right_option.text) < 0)) then
            begin
                Exit(True);
            end;
        end;

        procedure sort_and_trim_solver_beam_options_local(
            var options: TSubquerySolverBeamOptionArray; const max_count: Integer);
        var
            option_idx: Integer;
            insert_idx: Integer;
            tmp_option: TSubquerySolverBeamOption;
        begin
            for option_idx := 1 to High(options) do
            begin
                for insert_idx := option_idx downto 1 do
                begin
                    if not is_solver_beam_option_better_local(options[insert_idx],
                        options[insert_idx - 1]) then
                    begin
                        Break;
                    end;
                    tmp_option := options[insert_idx - 1];
                    options[insert_idx - 1] := options[insert_idx];
                    options[insert_idx] := tmp_option;
                end;
            end;

            if (max_count > 0) and (Length(options) > max_count) then
            begin
                SetLength(options, max_count);
            end;
        end;

        procedure add_solver_beam_option_local(
            var options: TSubquerySolverBeamOptionArray;
            const candidate_option: TSubquerySolverBeamOption;
            const max_count: Integer);
        var
            option_idx: Integer;
        begin
            if candidate_option.text = '' then
            begin
                Exit;
            end;

            for option_idx := 0 to High(options) do
            begin
                if SameText(options[option_idx].text, candidate_option.text) then
                begin
                    if is_solver_beam_option_better_local(candidate_option,
                        options[option_idx]) then
                    begin
                        options[option_idx] := candidate_option;
                        sort_and_trim_solver_beam_options_local(options, max_count);
                    end;
                    Exit;
                end;
            end;

            SetLength(options, Length(options) + 1);
            options[High(options)] := candidate_option;
            sort_and_trim_solver_beam_options_local(options, max_count);
        end;

        function collect_exact_query_key_candidates_local(
            const start_idx_local: Integer; const query_key_local: string;
            const max_units_local: Integer;
            out out_texts_local: TArray<string>;
            out out_scores_local: TArray<Integer>): Boolean;
        var
            local_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
            candidate_units_local: Integer;
            insert_idx_local: Integer;
            tmp_text_local: string;
            tmp_score_local: Integer;
            option_idx_local: Integer;
            option_limit_local: Integer;
        begin
            Result := False;
            SetLength(out_texts_local, 0);
            SetLength(out_scores_local, 0);
            if (m_dictionary = nil) or (query_key_local = '') then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                local_results_local) then
            begin
                Exit;
            end;

            if max_units_local >= 3 then
            begin
                option_limit_local := 2;
            end
            else
            begin
                option_limit_local := c_solver_beam_limit;
            end;

            for result_idx_local := 0 to High(local_results_local) do
            begin
                if result_idx_local >= c_solver_probe_limit * 3 then
                begin
                    Break;
                end;
                if Trim(local_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(local_results_local[result_idx_local].text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if (candidate_units_local < 2) or
                    (candidate_units_local > max_units_local) or
                    (not is_valid_solver_cjk_phrase_text_local(
                    candidate_text_local, candidate_units_local)) then
                begin
                    Continue;
                end;
                if (max_units_local >= 4) and
                    (candidate_units_local <> max_units_local) then
                begin
                    Continue;
                end;
                if not solver_candidate_respects_fixed_boundaries_local(
                    start_idx_local, max_units_local, candidate_text_local) then
                begin
                    Continue;
                end;

                candidate_score_local := local_results_local[result_idx_local].score;
                if local_results_local[result_idx_local].has_dict_weight and
                    (local_results_local[result_idx_local].dict_weight >
                    candidate_score_local) then
                begin
                    candidate_score_local :=
                        local_results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_score_local,
                    get_preferred_query_phrase_bonus_local(query_key_local,
                    candidate_text_local));
                Inc(candidate_score_local,
                    get_preferred_exact_subspan_bonus_local(query_key_local,
                    candidate_text_local));
                Inc(candidate_score_local,
                    get_exact_query_text_bonus_solver_local(query_key_local,
                    candidate_text_local));
                Dec(candidate_score_local,
                    get_trailing_context_free_single_char_penalty_solver_local(
                    query_key_local, candidate_text_local));

                for option_idx_local := 0 to High(out_texts_local) do
                begin
                    if SameText(out_texts_local[option_idx_local], candidate_text_local) then
                    begin
                        if candidate_score_local > out_scores_local[option_idx_local] then
                        begin
                            out_scores_local[option_idx_local] := candidate_score_local;
                        end;
                        candidate_text_local := '';
                        Break;
                    end;
                end;
                if candidate_text_local = '' then
                begin
                    Continue;
                end;

                insert_idx_local := Length(out_texts_local);
                SetLength(out_texts_local, insert_idx_local + 1);
                SetLength(out_scores_local, insert_idx_local + 1);
                out_texts_local[insert_idx_local] := candidate_text_local;
                out_scores_local[insert_idx_local] := candidate_score_local;

                while (insert_idx_local > 0) and
                    (out_scores_local[insert_idx_local] >
                    out_scores_local[insert_idx_local - 1]) do
                begin
                    tmp_text_local := out_texts_local[insert_idx_local - 1];
                    out_texts_local[insert_idx_local - 1] :=
                        out_texts_local[insert_idx_local];
                    out_texts_local[insert_idx_local] := tmp_text_local;
                    tmp_score_local := out_scores_local[insert_idx_local - 1];
                    out_scores_local[insert_idx_local - 1] :=
                        out_scores_local[insert_idx_local];
                    out_scores_local[insert_idx_local] := tmp_score_local;
                    Dec(insert_idx_local);
                end;

                if Length(out_texts_local) > option_limit_local then
                begin
                    SetLength(out_texts_local, option_limit_local);
                    SetLength(out_scores_local, option_limit_local);
                end;
            end;

            if (max_units_local >= 3) and (Length(out_scores_local) > 1) and
                (out_scores_local[0] >= out_scores_local[1] + 512) then
            begin
                SetLength(out_texts_local, 1);
                SetLength(out_scores_local, 1);
            end;

            Result := Length(out_texts_local) > 0;
        end;

        function collect_subquery_candidate_options_local(const start_idx: Integer;
            const syllable_count_part: Integer;
            out out_options: TSubquerySolverBeamOptionArray): Boolean;
        var
            query_key_local: string;
            cached_state_local: Integer;
            cached_options_local: TSubquerySolverBeamOptionArray;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
            resolved_path_local: string;
            resolved_score_local: Integer;
            resolved_segments_local: Integer;
            resolved_anchor_units_local: Integer;
            resolved_has_anchor_local: Boolean;
            resolved_units_local: Integer;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            allow_direct_exact_local: Boolean;
            contextual_two_text_local: string;
            contextual_two_score_local: Integer;
            split_idx_local: Integer;
            left_key_local: string;
            right_key_local: string;
            left_text_local: string;
            right_text_local: string;
            left_score_local: Integer;
            right_score_local: Integer;
            left_option_texts_local: TArray<string>;
            left_option_scores_local: TArray<Integer>;
            right_option_texts_local: TArray<string>;
            right_option_scores_local: TArray<Integer>;
            left_option_idx_local: Integer;
            right_option_idx_local: Integer;
            combined_text_local: string;
            combined_score_local: Integer;
            contextual_text_local: string;
            contextual_score_local: Integer;
            infer_score_local: Integer;
            infer_segments_local: Integer;
            inferred_path_local: string;
            context_bonus_local: Integer;
            option_local: TSubquerySolverBeamOption;
            option_limit_local: Integer;
        begin
            Result := False;
            SetLength(out_options, 0);
            if syllable_count_part <= 0 then
            begin
                Exit;
            end;
            contextual_text_local := '';
            contextual_score_local := Low(Integer);
            contextual_two_text_local := '';
            contextual_two_score_local := Low(Integer);
            if syllable_count_part = 3 then
            begin
                option_limit_local := c_solver_three_syllable_option_limit;
            end
            else
            begin
                option_limit_local := c_solver_beam_limit;
            end;

            query_key_local := build_query_key_local(start_idx, syllable_count_part);
            if query_key_local = '' then
            begin
                Exit;
            end;

            if query_option_cache_states.TryGetValue(query_key_local,
                cached_state_local) then
            begin
                if cached_state_local = 1 then
                begin
                    if query_option_cache_options.TryGetValue(query_key_local,
                        cached_options_local) then
                    begin
                        out_options := Copy(cached_options_local);
                        Exit(Length(out_options) > 0);
                    end;
                    Exit(False);
                end;
                Exit(False);
            end;

            if syllable_count_part = 1 then
            begin
                if collect_single_char_candidates_local(query_key_local,
                    left_option_texts_local, left_option_scores_local) then
                begin
                    for left_option_idx_local := 0 to High(left_option_texts_local) do
                    begin
                        option_local := Default(TSubquerySolverBeamOption);
                        option_local.text := left_option_texts_local[
                            left_option_idx_local];
                        option_local.path := option_local.text;
                        option_local.first_chunk := option_local.text;
                        option_local.score := left_option_scores_local[
                            left_option_idx_local];
                        option_local.segments := 1;
                        add_solver_beam_option_local(out_options, option_local,
                            option_limit_local);
                    end;
                end;
                if Length(out_options) > 0 then
                begin
                    query_option_cache_states.AddOrSetValue(query_key_local, 1);
                    query_option_cache_options.AddOrSetValue(query_key_local,
                        Copy(out_options));
                    Exit(True);
                end;
                query_option_cache_states.AddOrSetValue(query_key_local, 2);
                Exit(False);
            end;

            if syllable_count_part > c_solver_max_chunk_syllables then
            begin
                Exit;
            end;

            preferred_text_local := '';
            preferred_bonus_local := 0;
            try_get_preferred_query_phrase_text_local(query_key_local,
                preferred_text_local, preferred_bonus_local);
            allow_direct_exact_local := (syllable_count_part <= 3) or
                (preferred_bonus_local > 0);

            if allow_direct_exact_local and
                try_resolve_best_exact_subspan_local(start_idx, syllable_count_part,
                candidate_text_local, resolved_score_local, resolved_segments_local,
                resolved_path_local, resolved_has_anchor_local,
                resolved_anchor_units_local) then
            begin
                resolved_units_local :=
                    get_candidate_text_unit_count(Trim(candidate_text_local));
                if (resolved_units_local >= 2) and
                    (resolved_units_local <= syllable_count_part) and
                    is_valid_solver_cjk_phrase_text_local(candidate_text_local,
                    resolved_units_local) and
                    solver_candidate_respects_fixed_boundaries_local(start_idx,
                    syllable_count_part, candidate_text_local) then
                begin
                    option_local := Default(TSubquerySolverBeamOption);
                    option_local.text := candidate_text_local;
                    option_local.path := candidate_text_local;
                    option_local.first_chunk := candidate_text_local;
                    option_local.score := resolved_score_local;
                    if resolved_has_anchor_local then
                    begin
                        Inc(option_local.score, resolved_anchor_units_local * 192);
                    end;
                    if resolved_segments_local > 1 then
                    begin
                        Dec(option_local.score, (resolved_segments_local - 1) * 96);
                    end;
                    option_local.segments := Max(1, resolved_segments_local);
                    add_solver_beam_option_local(out_options, option_local,
                        option_limit_local);
                end;
            end;
            if (syllable_count_part = 2) and
                collect_two_syllable_contextual_options_local(start_idx,
                query_key_local, left_option_texts_local, left_option_scores_local) then
            begin
                for result_idx_local := 0 to High(left_option_texts_local) do
                begin
                    option_local := Default(TSubquerySolverBeamOption);
                    option_local.text := left_option_texts_local[result_idx_local];
                    option_local.path := option_local.text;
                    option_local.first_chunk := option_local.text;
                    option_local.score := left_option_scores_local[result_idx_local];
                    option_local.segments := 1;
                    add_solver_beam_option_local(out_options, option_local,
                        option_limit_local);
                end;
            end
            else if (syllable_count_part = 2) and
                try_get_best_two_syllable_contextual_single_char_combo_local(
                start_idx, query_key_local, contextual_two_text_local,
                contextual_two_score_local) then
            begin
                option_local := Default(TSubquerySolverBeamOption);
                option_local.text := contextual_two_text_local;
                option_local.path := contextual_two_text_local;
                option_local.first_chunk := contextual_two_text_local;
                option_local.score := contextual_two_score_local;
                option_local.segments := 1;
                add_solver_beam_option_local(out_options, option_local,
                    option_limit_local);
            end;
            if (syllable_count_part = 3) and
                try_get_best_three_syllable_two_plus_one_candidate_local(
                start_idx, query_key_local, contextual_text_local,
                contextual_score_local) then
            begin
                option_local := Default(TSubquerySolverBeamOption);
                option_local.text := contextual_text_local;
                option_local.path := contextual_text_local;
                option_local.first_chunk := contextual_text_local;
                option_local.score := contextual_score_local;
                option_local.segments := 1;
                add_solver_beam_option_local(out_options, option_local,
                    option_limit_local);
            end;

            if collect_exact_query_key_candidates_local(start_idx, query_key_local,
                syllable_count_part, left_option_texts_local,
                left_option_scores_local) then
            begin
                for result_idx_local := 0 to High(left_option_texts_local) do
                begin
                    option_local := Default(TSubquerySolverBeamOption);
                    option_local.text := left_option_texts_local[result_idx_local];
                    option_local.path := option_local.text;
                    option_local.first_chunk := option_local.text;
                    option_local.score := left_option_scores_local[result_idx_local];
                    option_local.segments := 1;
                    add_solver_beam_option_local(out_options, option_local,
                        option_limit_local);
                end;
            end;
            if try_get_best_query_key_candidate_local(query_key_local,
                syllable_count_part, candidate_text_local, candidate_score_local) then
            begin
                option_local := Default(TSubquerySolverBeamOption);
                option_local.text := candidate_text_local;
                option_local.path := candidate_text_local;
                option_local.first_chunk := candidate_text_local;
                option_local.score := candidate_score_local;
                option_local.segments := 1;
                add_solver_beam_option_local(out_options, option_local,
                    option_limit_local);
            end;

            for split_idx_local := 1 to syllable_count_part - 1 do
            begin
                left_key_local := build_query_key_local(start_idx, split_idx_local);
                right_key_local := build_query_key_local(start_idx + split_idx_local,
                    syllable_count_part - split_idx_local);
                if (left_key_local = '') or (right_key_local = '') then
                begin
                    Continue;
                end;

                SetLength(left_option_texts_local, 0);
                SetLength(left_option_scores_local, 0);
                SetLength(right_option_texts_local, 0);
                SetLength(right_option_scores_local, 0);

                if split_idx_local = 1 then
                begin
                    if not collect_single_char_candidates_local(left_key_local,
                        left_option_texts_local, left_option_scores_local) then
                    begin
                        Continue;
                    end;
                end
                else if collect_exact_query_key_candidates_local(start_idx,
                    left_key_local,
                    split_idx_local, left_option_texts_local,
                    left_option_scores_local) then
                begin
                end
                else if try_get_best_query_key_candidate_local(left_key_local,
                    split_idx_local, left_text_local, left_score_local) then
                begin
                    SetLength(left_option_texts_local, 1);
                    SetLength(left_option_scores_local, 1);
                    left_option_texts_local[0] := left_text_local;
                    left_option_scores_local[0] := left_score_local;
                end
                else
                begin
                    Continue;
                end;

                if syllable_count_part - split_idx_local = 1 then
                begin
                    if not collect_single_char_candidates_local(right_key_local,
                        right_option_texts_local, right_option_scores_local) then
                    begin
                        Continue;
                    end;
                end
                else if collect_exact_query_key_candidates_local(
                    start_idx + split_idx_local, right_key_local,
                    syllable_count_part - split_idx_local, right_option_texts_local,
                    right_option_scores_local) then
                begin
                end
                else if try_get_best_query_key_candidate_local(right_key_local,
                    syllable_count_part - split_idx_local, right_text_local,
                    right_score_local) then
                begin
                    SetLength(right_option_texts_local, 1);
                    SetLength(right_option_scores_local, 1);
                    right_option_texts_local[0] := right_text_local;
                    right_option_scores_local[0] := right_score_local;
                end
                else
                begin
                    Continue;
                end;

                for left_option_idx_local := 0 to High(left_option_texts_local) do
                begin
                    for right_option_idx_local := 0 to High(right_option_texts_local) do
                    begin
                        combined_text_local := left_option_texts_local[
                            left_option_idx_local] + right_option_texts_local[
                            right_option_idx_local];
                        if not is_valid_solver_cjk_phrase_text_local(
                            combined_text_local, syllable_count_part) then
                        begin
                            Continue;
                        end;

                        if (split_idx_local = 1) and
                            (get_exact_query_text_bonus_solver_local(
                            query_key_local, combined_text_local) = 0) and
                            (not SameText(left_option_texts_local[
                            left_option_idx_local],
                            get_fixed_boundary_single_char_for_solver_local(
                            left_key_local))) then
                        begin
                            Continue;
                        end;

                        combined_score_local :=
                            left_option_scores_local[left_option_idx_local] +
                            right_option_scores_local[right_option_idx_local] - 96;
                        Inc(combined_score_local,
                            get_preferred_query_phrase_bonus_local(query_key_local,
                            combined_text_local));
                        Inc(combined_score_local,
                            get_exact_query_text_bonus_solver_local(
                            query_key_local, combined_text_local));
                        if m_dictionary <> nil then
                        begin
                            context_bonus_local := m_dictionary.get_context_bonus(
                                left_option_texts_local[left_option_idx_local],
                                right_option_texts_local[right_option_idx_local]);
                            Inc(combined_score_local, context_bonus_local);
                        end;
                        inferred_path_local :=
                            infer_segment_path_for_query_text_with_score(
                            query_key_local, combined_text_local,
                            infer_score_local, infer_segments_local);
                        if inferred_path_local <> '' then
                        begin
                            Inc(combined_score_local, infer_score_local);
                            Inc(combined_score_local, infer_segments_local * 320);
                        end;

                        option_local := Default(TSubquerySolverBeamOption);
                        option_local.text := combined_text_local;
                        option_local.path := combined_text_local;
                        option_local.first_chunk := combined_text_local;
                        option_local.score := combined_score_local;
                        option_local.segments := 2;
                        add_solver_beam_option_local(out_options, option_local,
                            option_limit_local);
                    end;
                end;
            end;

            Result := Length(out_options) > 0;
            if Result then
            begin
                query_option_cache_states.AddOrSetValue(query_key_local, 1);
                query_option_cache_options.AddOrSetValue(query_key_local,
                    Copy(out_options));
            end
            else
            begin
                query_option_cache_states.AddOrSetValue(query_key_local, 2);
            end;
        end;

        function get_solver_chunk_length_bonus_local(
            const chunk_syllables_local: Integer;
            const chunk_segments_local: Integer): Integer;
        begin
            if chunk_syllables_local <= 0 then
            begin
                Exit(0);
            end;

            if chunk_syllables_local <= 3 then
            begin
                Exit(chunk_syllables_local * chunk_syllables_local *
                    c_solver_length_bonus);
            end;

            Result := (3 * 3 * c_solver_length_bonus) +
                ((chunk_syllables_local - 3) * (c_solver_length_bonus div 2));
            if chunk_segments_local <= 1 then
            begin
                Dec(Result, (chunk_syllables_local - 3) *
                    (c_solver_length_bonus * 2));
            end;
            Result := Max(Result, chunk_syllables_local * 640);
        end;

        procedure maybe_promote_subquery_result_local(const result_text: string;
            const result_score: Integer; const result_segments: Integer;
            const result_path: string);
        var
            promote_existing_index_local: Integer;
            promote_idx_local: Integer;
            promote_top_path_segment_count_local: Integer;
            promote_should_promote_local: Boolean;
            promote_picked_local: TncCandidate;
        begin
            if (Trim(result_text) = '') or
                (get_candidate_text_unit_count(Trim(result_text)) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            promote_existing_index_local := -1;
            for promote_idx_local := 0 to High(candidates) do
            begin
                if (Trim(candidates[promote_idx_local].comment) = '') and
                    SameText(Trim(candidates[promote_idx_local].text),
                    result_text) then
                begin
                    promote_existing_index_local := promote_idx_local;
                    Break;
                end;
            end;

            if promote_existing_index_local = 0 then
            begin
                out_promoted := True;
                Exit;
            end;

            promote_top_path_segment_count_local := get_path_segment_count_local(
                get_segment_path_for_candidate(candidates[0]));
            promote_should_promote_local := False;
            if Trim(candidates[0].comment) <> '' then
            begin
                promote_should_promote_local := True;
            end
            else if get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                input_syllable_count then
            begin
                promote_should_promote_local := True;
            end
            else if (promote_top_path_segment_count_local > 0) and
                (result_segments < promote_top_path_segment_count_local) then
            begin
                promote_should_promote_local := True;
            end
            else if promote_existing_index_local > 0 then
            begin
                promote_should_promote_local := True;
            end
            else if not SameText(Trim(candidates[0].text), result_text) then
            begin
                promote_should_promote_local := True;
            end;

            if not promote_should_promote_local then
            begin
                Exit;
            end;

            if promote_existing_index_local > 0 then
            begin
                promote_picked_local := candidates[promote_existing_index_local];
                promote_picked_local.score := Max(promote_picked_local.score,
                    Max(candidates[0].score + c_solver_promote_bonus,
                    result_score + c_solver_promote_bonus));
                remember_segment_path_for_candidate(promote_picked_local.text,
                    promote_picked_local.comment, result_path,
                    promote_picked_local.score);
            end
            else
            begin
                FillChar(promote_picked_local, SizeOf(promote_picked_local), 0);
                promote_picked_local.text := result_text;
                promote_picked_local.comment := '';
                promote_picked_local.score := Max(candidates[0].score +
                    c_solver_promote_bonus, result_score +
                    c_solver_promote_bonus);
                promote_picked_local.source := cs_rule;
                promote_picked_local.has_dict_weight := False;
                promote_picked_local.dict_weight := 0;
                remember_segment_path_for_candidate(promote_picked_local.text, '',
                    result_path, promote_picked_local.score);
            end;

            if promote_existing_index_local > 0 then
            begin
                for promote_idx_local := promote_existing_index_local downto 1 do
                begin
                    candidates[promote_idx_local] :=
                        candidates[promote_idx_local - 1];
                end;
            end
            else
            begin
                SetLength(candidates, Length(candidates) + 1);
                for promote_idx_local := High(candidates) downto 1 do
                begin
                    candidates[promote_idx_local] :=
                        candidates[promote_idx_local - 1];
                end;
            end;
            candidates[0] := promote_picked_local;
            out_promoted := True;
        end;

        function try_build_strict_exact_window_sentence_candidate_local(
            out out_text: string; out out_score: Integer;
            out out_segments: Integer; out out_path: string): Boolean;
        const
            c_strict_max_chunk_syllables = 6;
            c_strict_single_penalty = 7800;
            c_strict_nonboundary_single_penalty = 4600;
            c_strict_join_penalty = 900;
            c_strict_context_scale = 6;
        var
            strict_states: TStrictExactSentenceStateArray;
            strict_pos: Integer;
            strict_len: Integer;
            strict_next_pos: Integer;
            strict_chunk_text: string;
            strict_chunk_path: string;
            strict_combined_text: string;
            strict_combined_path: string;
            strict_chunk_score: Integer;
            strict_total_score: Integer;
            strict_chunk_segments: Integer;
            strict_total_segments: Integer;
            strict_chunk_anchor_units: Integer;
            strict_total_anchor_units: Integer;
            strict_match_count_local: Integer;
            strict_mismatch_count_local: Integer;
            strict_strong_match_count_local: Integer;
            strict_strong_mismatch_count_local: Integer;
            strict_context_bonus_local: Integer;
            strict_has_anchor_local: Boolean;
            strict_is_single_local: Boolean;
            strict_is_strong_chunk_local: Boolean;
            strict_total_strong_chunks_local: Integer;
            strict_total_single_chunks_local: Integer;
            strict_candidate_better_local: Boolean;
            strict_fixed_text_local: string;

            function get_strict_chunk_bonus_local(
                const local_len: Integer;
                const local_has_anchor: Boolean;
                const local_anchor_units: Integer;
                const local_chunk_score: Integer): Integer;
            begin
                case local_len of
                    1:
                        Result := -c_strict_single_penalty;
                    2:
                        Result := 0;
                    3:
                        Result := 2800;
                    4:
                        Result := 7600;
                    5:
                        Result := 11200;
                    else
                        Result := 13600 + ((local_len - 6) * 1600);
                end;

                if local_has_anchor or (local_anchor_units >= 2) then
                begin
                    Inc(Result, 2200);
                end;

                if (local_len = 2) and (local_chunk_score < 18000) and
                    (not local_has_anchor) then
                begin
                    Dec(Result, 1600);
                end
                else if (local_len >= 4) and (local_chunk_score >= 28000) then
                begin
                    Inc(Result, 1400);
                end;
            end;

            function try_get_strict_chunk_local(const local_pos: Integer;
                const local_len: Integer; out local_text: string;
                out local_score: Integer; out local_segments: Integer;
                out local_path: string; out local_has_anchor: Boolean;
                out local_anchor_units: Integer;
                out local_is_strong_chunk: Boolean;
                out local_is_single: Boolean): Boolean;
            var
                local_query_key: string;
            begin
                Result := False;
                local_text := '';
                local_score := 0;
                local_segments := 1;
                local_path := '';
                local_has_anchor := False;
                local_anchor_units := 0;
                local_is_strong_chunk := False;
                local_is_single := False;

                if local_len <= 0 then
                begin
                    Exit;
                end;

                local_query_key := build_query_key_local(local_pos, local_len);
                if local_query_key = '' then
                begin
                    Exit;
                end;

                if local_len = 1 then
                begin
                    strict_fixed_text_local :=
                        get_fixed_boundary_single_char_for_solver_local(
                        effective_syllables[local_pos].text);
                    if strict_fixed_text_local = '' then
                    begin
                        Exit;
                    end;
                    if not try_get_exact_single_char_candidate_local(local_pos,
                        local_text, local_score) then
                    begin
                        local_text := strict_fixed_text_local;
                        local_score := 1500;
                    end;
                    if not SameText(local_text, strict_fixed_text_local) then
                    begin
                        local_text := strict_fixed_text_local;
                        local_score := Max(local_score, 1500);
                    end;
                    local_has_anchor := True;
                    local_anchor_units := Ord(local_has_anchor);
                    local_is_single := True;
                    if not is_valid_solver_cjk_phrase_text_local(local_text, 1) then
                    begin
                        Exit;
                    end;
                    local_is_strong_chunk := True;
                    local_path := local_text;
                    Exit(True);
                end;

                if not try_resolve_best_exact_subspan_local(local_pos, local_len,
                    local_text, local_score, local_segments, local_path,
                    local_has_anchor, local_anchor_units) then
                begin
                    Exit;
                end;
                if (not is_valid_solver_cjk_phrase_text_local(local_text, local_len)) or
                    (not solver_candidate_respects_fixed_boundaries_local(local_pos,
                    local_len, local_text)) then
                begin
                    Exit;
                end;

                Inc(local_score, get_exact_query_text_bonus_solver_local(
                    local_query_key, local_text));
                local_is_strong_chunk := (local_len >= 3) or local_has_anchor or
                    (local_score >= 22000);
                if local_path = '' then
                begin
                    local_path := local_text;
                end;
                Result := True;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            out_segments := 0;
            out_path := '';

            if syllable_count_local < c_solver_min_syllables then
            begin
                Exit;
            end;

            SetLength(strict_states, syllable_count_local + 1);
            strict_states[syllable_count_local].valid := True;
            strict_states[syllable_count_local].score := 0;
            strict_states[syllable_count_local].segments := 0;
            strict_states[syllable_count_local].strong_chunks := 0;
            strict_states[syllable_count_local].single_chunks := 0;
            strict_states[syllable_count_local].anchor_units := 0;
            strict_states[syllable_count_local].text := '';
            strict_states[syllable_count_local].path := '';
            strict_states[syllable_count_local].first_chunk := '';

            for strict_pos := syllable_count_local - 1 downto 0 do
            begin
                strict_states[strict_pos].valid := False;
                strict_states[strict_pos].score := Low(Integer);
                strict_states[strict_pos].segments := MaxInt;
                strict_states[strict_pos].strong_chunks := -1;
                strict_states[strict_pos].single_chunks := MaxInt;
                strict_states[strict_pos].anchor_units := 0;
                strict_states[strict_pos].text := '';
                strict_states[strict_pos].path := '';
                strict_states[strict_pos].first_chunk := '';

                for strict_len := 1 to Min(c_strict_max_chunk_syllables,
                    syllable_count_local - strict_pos) do
                begin
                    strict_next_pos := strict_pos + strict_len;
                    if (strict_next_pos < syllable_count_local) and
                        (not strict_states[strict_next_pos].valid) then
                    begin
                        Continue;
                    end;
                    if not try_get_strict_chunk_local(strict_pos, strict_len,
                        strict_chunk_text, strict_chunk_score,
                        strict_chunk_segments, strict_chunk_path,
                        strict_has_anchor_local, strict_chunk_anchor_units,
                        strict_is_strong_chunk_local, strict_is_single_local) then
                    begin
                        Continue;
                    end;

                    strict_total_score := strict_chunk_score +
                        get_strict_chunk_bonus_local(strict_len,
                        strict_has_anchor_local, strict_chunk_anchor_units,
                        strict_chunk_score);
                    strict_total_segments := strict_chunk_segments;
                    strict_total_strong_chunks_local :=
                        Ord(strict_is_strong_chunk_local);
                    strict_total_single_chunks_local :=
                        Ord(strict_is_single_local);
                    strict_total_anchor_units := strict_chunk_anchor_units;
                    strict_combined_text := strict_chunk_text;
                    strict_combined_path := strict_chunk_path;

                    if strict_next_pos < syllable_count_local then
                    begin
                        Inc(strict_total_score,
                            strict_states[strict_next_pos].score);
                        Inc(strict_total_score, -c_strict_join_penalty);
                        Inc(strict_total_segments,
                            strict_states[strict_next_pos].segments);
                        Inc(strict_total_strong_chunks_local,
                            strict_states[strict_next_pos].strong_chunks);
                        Inc(strict_total_single_chunks_local,
                            strict_states[strict_next_pos].single_chunks);
                        if strict_states[strict_next_pos].anchor_units >
                            strict_total_anchor_units then
                        begin
                            strict_total_anchor_units :=
                                strict_states[strict_next_pos].anchor_units;
                        end;
                        if (m_dictionary <> nil) and
                            (strict_states[strict_next_pos].first_chunk <> '') then
                        begin
                            strict_context_bonus_local :=
                                m_dictionary.get_context_bonus(
                                strict_chunk_text,
                                strict_states[strict_next_pos].first_chunk);
                            Inc(strict_total_score,
                                strict_context_bonus_local *
                                c_strict_context_scale);
                        end;
                        strict_combined_text := strict_chunk_text +
                            strict_states[strict_next_pos].text;
                        if strict_states[strict_next_pos].path <> '' then
                        begin
                            if strict_combined_path <> '' then
                            begin
                                strict_combined_path := strict_combined_path +
                                    c_segment_path_separator +
                                    strict_states[strict_next_pos].path;
                            end
                            else
                            begin
                                strict_combined_path :=
                                    strict_states[strict_next_pos].path;
                            end;
                        end;
                    end;

                    strict_candidate_better_local := False;
                    if strict_total_score > strict_states[strict_pos].score then
                    begin
                        strict_candidate_better_local := True;
                    end
                    else if strict_total_score = strict_states[strict_pos].score then
                    begin
                        if strict_total_strong_chunks_local >
                            strict_states[strict_pos].strong_chunks then
                        begin
                            strict_candidate_better_local := True;
                        end
                        else if strict_total_strong_chunks_local =
                            strict_states[strict_pos].strong_chunks then
                        begin
                            if strict_total_single_chunks_local <
                                strict_states[strict_pos].single_chunks then
                            begin
                                strict_candidate_better_local := True;
                            end
                            else if strict_total_single_chunks_local =
                                strict_states[strict_pos].single_chunks then
                            begin
                                if strict_total_segments <
                                    strict_states[strict_pos].segments then
                                begin
                                    strict_candidate_better_local := True;
                                end
                                else if (strict_total_segments =
                                    strict_states[strict_pos].segments) and
                                    ((strict_states[strict_pos].text = '') or
                                    (CompareText(strict_combined_text,
                                    strict_states[strict_pos].text) < 0)) then
                                begin
                                    strict_candidate_better_local := True;
                                end;
                            end;
                        end;
                    end;

                    if strict_candidate_better_local then
                    begin
                        strict_states[strict_pos].valid := True;
                        strict_states[strict_pos].score := strict_total_score;
                        strict_states[strict_pos].segments := strict_total_segments;
                        strict_states[strict_pos].strong_chunks :=
                            strict_total_strong_chunks_local;
                        strict_states[strict_pos].single_chunks :=
                            strict_total_single_chunks_local;
                        strict_states[strict_pos].anchor_units :=
                            strict_total_anchor_units;
                        strict_states[strict_pos].text := strict_combined_text;
                        strict_states[strict_pos].path := strict_combined_path;
                        strict_states[strict_pos].first_chunk := strict_chunk_text;
                    end;
                end;
            end;

            if (not strict_states[0].valid) or
                (strict_states[0].text = '') or
                (get_candidate_text_unit_count(strict_states[0].text) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            get_exact_window_consistency_stats_local(strict_states[0].text, 2, 5,
                strict_match_count_local, strict_mismatch_count_local,
                strict_strong_match_count_local,
                strict_strong_mismatch_count_local);
            if (strict_strong_mismatch_count_local > 0) or
                (strict_strong_match_count_local <
                Max(2, Min(6, input_syllable_count div 2))) or
                (strict_match_count_local <
                strict_mismatch_count_local + 2) or
                (strict_states[0].strong_chunks <
                Max(2, Min(5, input_syllable_count div 3))) or
                (strict_states[0].single_chunks >
                Max(2, input_syllable_count div 5)) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' tssx=[reject text=%s score=%d seg=%d sm=%d ssm=%d mm=%d sc=%d]',
                        [strict_states[0].text, strict_states[0].score,
                        strict_states[0].segments,
                        strict_strong_match_count_local,
                        strict_strong_mismatch_count_local,
                        strict_mismatch_count_local,
                        strict_states[0].single_chunks]);
                end;
                Exit;
            end;

            out_text := strict_states[0].text;
            out_score := strict_states[0].score +
                (strict_strong_match_count_local * 1024) -
                (strict_mismatch_count_local * 128);
            out_segments := strict_states[0].segments;
            out_path := strict_states[0].path;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' tssx=[best text=%s score=%d seg=%d sm=%d mm=%d sc=%d]',
                    [out_text, out_score, out_segments,
                    strict_strong_match_count_local,
                    strict_mismatch_count_local,
                    strict_states[0].single_chunks]);
            end;
            Result := True;
        end;

        procedure try_update_beam_from_position_local(const start_idx: Integer);
        var
            chunk_syllables_local: Integer;
            next_idx_local: Integer;
            chunk_options_local: TSubquerySolverBeamOptionArray;
            chunk_option_idx_local: Integer;
            suffix_option_idx_local: Integer;
            chunk_option_local: TSubquerySolverBeamOption;
            suffix_option_local: TSubquerySolverBeamOption;
            candidate_option_local: TSubquerySolverBeamOption;
            context_bonus_local: Integer;
            exact_two_text_local: string;
            exact_two_score_local: Integer;
            has_exact_two_local: Boolean;
        begin
            if solver_time_budget_exceeded_local then
            begin
                Exit;
            end;

            SetLength(best_beam_options[start_idx], 0);
            for chunk_syllables_local := 1 to Min(c_solver_max_chunk_syllables,
                syllable_count_local - start_idx) do
            begin
                if solver_time_budget_exceeded_local then
                begin
                    Break;
                end;

                next_idx_local := start_idx + chunk_syllables_local;
                if (next_idx_local < syllable_count_local) and
                    (Length(best_beam_options[next_idx_local]) = 0) then
                begin
                    Continue;
                end;

                if not collect_subquery_candidate_options_local(start_idx,
                    chunk_syllables_local, chunk_options_local) then
                begin
                    Continue;
                end;

                for chunk_option_idx_local := 0 to High(chunk_options_local) do
                begin
                    if solver_time_budget_exceeded_local then
                    begin
                        Break;
                    end;

                    chunk_option_local := chunk_options_local[chunk_option_idx_local];
                    chunk_option_local.first_chunk := chunk_option_local.text;
                    has_exact_two_local := (chunk_syllables_local = 1) and
                        try_get_top_two_syllable_exact_text_local(start_idx,
                        exact_two_text_local, exact_two_score_local);
                    if has_exact_two_local and
                        (get_first_solver_text_unit_local(exact_two_text_local) <> '') and
                        (not SameText(get_first_solver_text_unit_local(
                        exact_two_text_local), chunk_option_local.text)) then
                    begin
                        Dec(chunk_option_local.score, 12000);
                    end;
                    if has_exact_two_local and
                        (not SameText(chunk_option_local.text,
                        get_fixed_boundary_single_char_for_solver_local(
                        effective_syllables[start_idx].text))) then
                    begin
                        Dec(chunk_option_local.score, 4096);
                    end;
                    Inc(chunk_option_local.score,
                        get_solver_chunk_length_bonus_local(
                        chunk_syllables_local, chunk_option_local.segments));
                    if chunk_syllables_local >= 3 then
                    begin
                        Inc(chunk_option_local.score, c_solver_anchor_bonus);
                    end;

                    if next_idx_local >= syllable_count_local then
                    begin
                        add_solver_beam_option_local(best_beam_options[start_idx],
                            chunk_option_local, c_solver_beam_limit);
                        Continue;
                    end;

                    for suffix_option_idx_local := 0 to
                        High(best_beam_options[next_idx_local]) do
                    begin
                        suffix_option_local :=
                            best_beam_options[next_idx_local][suffix_option_idx_local];
                        candidate_option_local := Default(TSubquerySolverBeamOption);
                        candidate_option_local.text := chunk_option_local.text +
                            suffix_option_local.text;
                        candidate_option_local.path := chunk_option_local.path;
                        if suffix_option_local.path <> '' then
                        begin
                            if candidate_option_local.path <> '' then
                            begin
                                candidate_option_local.path :=
                                    candidate_option_local.path +
                                    c_segment_path_separator;
                            end;
                            candidate_option_local.path :=
                                candidate_option_local.path +
                                suffix_option_local.path;
                        end;
                        candidate_option_local.first_chunk :=
                            chunk_option_local.first_chunk;
                        candidate_option_local.score := chunk_option_local.score +
                            suffix_option_local.score;
                        candidate_option_local.segments :=
                            chunk_option_local.segments +
                            suffix_option_local.segments;
                        if (m_dictionary <> nil) and
                            (suffix_option_local.first_chunk <> '') then
                        begin
                            context_bonus_local := m_dictionary.get_context_bonus(
                                chunk_option_local.text,
                                suffix_option_local.first_chunk);
                            Inc(candidate_option_local.score, context_bonus_local);
                        end;
                        add_solver_beam_option_local(best_beam_options[start_idx],
                            candidate_option_local, c_solver_beam_limit);
                    end;
                end;
            end;
        end;

        procedure try_update_best_from_position(const start_idx: Integer);
        var
            chunk_syllables: Integer;
            next_idx: Integer;
            chunk_text: string;
            chunk_score: Integer;
            exact_two_text: string;
            exact_two_score: Integer;
            total_score: Integer;
            total_segments: Integer;
            combined_text: string;
            combined_path: string;
            better_candidate: Boolean;
            first_chunk_local: string;
            context_bonus: Integer;
            has_exact_two: Boolean;
        begin
            if solver_time_budget_exceeded_local then
            begin
                Exit;
            end;

            for chunk_syllables := 1 to Min(c_solver_max_chunk_syllables,
                syllable_count_local - start_idx) do
            begin
                if solver_time_budget_exceeded_local then
                begin
                    Break;
                end;

                next_idx := start_idx + chunk_syllables;
                if (next_idx < syllable_count_local) and
                    (best_scores[next_idx] = Low(Integer)) then
                begin
                    Continue;
                end;

                if not try_get_best_subquery_candidate_local(start_idx,
                    chunk_syllables, chunk_text, chunk_score) then
                begin
                    Continue;
                end;

                has_exact_two := (chunk_syllables = 1) and
                    try_get_top_two_syllable_exact_text_local(start_idx,
                    exact_two_text, exact_two_score);
                if has_exact_two and
                    (get_first_solver_text_unit_local(exact_two_text) <> '') and
                    (not SameText(get_first_solver_text_unit_local(exact_two_text),
                    chunk_text)) then
                begin
                    Dec(chunk_score, 12000);
                end;
                if has_exact_two and
                    (not SameText(chunk_text,
                    get_fixed_boundary_single_char_for_solver_local(
                    effective_syllables[start_idx].text))) then
                begin
                    Dec(chunk_score, 4096);
                end;

                total_score := chunk_score +
                    get_solver_chunk_length_bonus_local(chunk_syllables, 1);
                if chunk_syllables >= 3 then
                begin
                    Inc(total_score, c_solver_anchor_bonus);
                end;

                combined_text := chunk_text;
                combined_path := chunk_text;
                total_segments := 1;
                first_chunk_local := chunk_text;

                if next_idx < syllable_count_local then
                begin
                    Inc(total_score, best_scores[next_idx]);
                    if (m_dictionary <> nil) and (best_first_chunks[next_idx] <> '') then
                    begin
                        context_bonus := m_dictionary.get_context_bonus(chunk_text,
                            best_first_chunks[next_idx]);
                        Inc(total_score, context_bonus);
                    end;
                    Inc(total_segments, best_segments[next_idx]);
                    combined_text := combined_text + best_texts[next_idx];
                    if best_paths[next_idx] <> '' then
                    begin
                        combined_path := combined_path + c_segment_path_separator +
                            best_paths[next_idx];
                    end;
                end;

                better_candidate := False;
                if total_score > best_scores[start_idx] then
                begin
                    better_candidate := True;
                end
                else if total_score = best_scores[start_idx] then
                begin
                    if total_segments < best_segments[start_idx] then
                    begin
                        better_candidate := True;
                    end
                    else if (total_segments = best_segments[start_idx]) and
                        ((best_texts[start_idx] = '') or
                        (CompareText(combined_text, best_texts[start_idx]) < 0)) then
                    begin
                        better_candidate := True;
                    end;
                end;

                if better_candidate then
                begin
                    best_scores[start_idx] := total_score;
                    best_segments[start_idx] := total_segments;
                    best_texts[start_idx] := combined_text;
                    best_paths[start_idx] := combined_path;
                    best_first_chunks[start_idx] := first_chunk_local;
                end;
            end;
        end;
    begin
        out_promoted := False;
        if (Length(candidates) = 0) or
            (m_disable_subspan_standalone_oracle > 0) or
            (input_syllable_count < c_solver_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        syllable_count_local := Length(effective_syllables);
        if syllable_count_local <> input_syllable_count then
        begin
            Exit;
        end;

        solver_start_tick := nc_monotonic_tick_ms;
        solver_work_count := 0;
        solver_budget_exceeded := False;
        if syllable_count_local >= 18 then
        begin
            solver_budget_ms := 192;
        end
        else if syllable_count_local >= 14 then
        begin
            solver_budget_ms := 224;
        end
        else
        begin
            solver_budget_ms := 288;
        end;

        has_extendable_tail_local :=
            has_extendable_tail_query_local(lookup_text);
        if has_extendable_tail_local and m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' tss=[extendable_tail]';
        end;

        if try_build_strict_exact_window_sentence_candidate_local(
            strict_solver_text_local, strict_solver_score_local,
            strict_solver_segments_local, strict_solver_path_local) then
        begin
            maybe_promote_subquery_result_local(strict_solver_text_local,
                strict_solver_score_local, strict_solver_segments_local,
                strict_solver_path_local);
            if out_promoted then
            begin
                Exit;
            end;
        end;
        if solver_time_budget_exceeded_local then
        begin
            Exit;
        end;

        query_cache_states := TDictionary<string, Integer>.Create;
        query_cache_texts := TDictionary<string, string>.Create;
        query_cache_scores := TDictionary<string, Integer>.Create;
        query_option_cache_states := TDictionary<string, Integer>.Create;
        query_option_cache_options :=
            TDictionary<string, TSubquerySolverBeamOptionArray>.Create;
        query_engine := nil;
        try
            SetLength(best_scores, syllable_count_local + 1);
            SetLength(best_segments, syllable_count_local + 1);
            SetLength(best_texts, syllable_count_local + 1);
            SetLength(best_paths, syllable_count_local + 1);
            SetLength(best_first_chunks, syllable_count_local + 1);
            SetLength(best_beam_options, syllable_count_local + 1);

            for idx := 0 to syllable_count_local do
            begin
                best_scores[idx] := Low(Integer);
                best_segments[idx] := MaxInt;
                best_texts[idx] := '';
                best_paths[idx] := '';
                best_first_chunks[idx] := '';
            end;

            best_scores[syllable_count_local] := 0;
            best_segments[syllable_count_local] := 0;
            SetLength(best_beam_options[syllable_count_local], 1);
            best_beam_options[syllable_count_local][0] :=
                Default(TSubquerySolverBeamOption);
            best_beam_options[syllable_count_local][0].score := 0;
            best_beam_options[syllable_count_local][0].segments := 0;

            for idx := syllable_count_local - 1 downto 0 do
            begin
                if solver_time_budget_exceeded_local then
                begin
                    Break;
                end;
                try_update_best_from_position(idx);
            end;
            if (syllable_count_local <= c_solver_beam_max_syllables) and
                (not solver_time_budget_exceeded_local) and
                ((best_scores[0] = Low(Integer)) or (best_segments[0] >= 6)) then
            begin
                for idx := syllable_count_local - 1 downto 0 do
                begin
                    if solver_time_budget_exceeded_local then
                    begin
                        Break;
                    end;
                    try_update_beam_from_position_local(idx);
                end;

                if (Length(best_beam_options[0]) > 0) and
                    (best_beam_options[0][0].text <> '') and
                    (get_candidate_text_unit_count(best_beam_options[0][0].text) =
                    input_syllable_count) and
                    ((best_scores[0] = Low(Integer)) or
                    (best_beam_options[0][0].score > best_scores[0]) or
                    ((best_beam_options[0][0].score + 256 >= best_scores[0]) and
                    (best_beam_options[0][0].segments <= best_segments[0]))) then
                begin
                    best_scores[0] := best_beam_options[0][0].score;
                    best_segments[0] := best_beam_options[0][0].segments;
                    best_texts[0] := best_beam_options[0][0].text;
                    best_paths[0] := best_beam_options[0][0].path;
                    best_first_chunks[0] := best_beam_options[0][0].first_chunk;
                end;
            end;

            if (best_scores[0] = Low(Integer)) or
                (best_texts[0] = '') or
                (get_candidate_text_unit_count(best_texts[0]) <> input_syllable_count) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        ' tss=[miss]';
                end;
                Exit;
            end;

            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' tss=[best text=%s score=%d seg=%d]',
                    [best_texts[0], best_scores[0], best_segments[0]]);
                if Length(best_beam_options[0]) > 0 then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' tbeam=[best text=%s score=%d seg=%d]',
                        [best_beam_options[0][0].text,
                        best_beam_options[0][0].score,
                        best_beam_options[0][0].segments]);
                end;
            end;

            existing_index := -1;
            for idx := 0 to High(candidates) do
            begin
                if (Trim(candidates[idx].comment) = '') and
                    SameText(Trim(candidates[idx].text), best_texts[0]) then
                begin
                    existing_index := idx;
                    Break;
                end;
            end;

            if existing_index = 0 then
            begin
                out_promoted := True;
                Exit;
            end;

            top_path_segment_count := get_path_segment_count_local(
                get_segment_path_for_candidate(candidates[0]));
            should_promote := False;
            if Trim(candidates[0].comment) <> '' then
            begin
                should_promote := True;
            end
            else if get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                input_syllable_count then
            begin
                should_promote := True;
            end
            else if (top_path_segment_count > 0) and
                (best_segments[0] < top_path_segment_count) then
            begin
                should_promote := True;
            end
            else if existing_index > 0 then
            begin
                should_promote := True;
            end
            else if not SameText(Trim(candidates[0].text), best_texts[0]) then
            begin
                should_promote := True;
            end;

            if not should_promote then
            begin
                Exit;
            end;

            if existing_index > 0 then
            begin
                picked := candidates[existing_index];
            end
            else
            begin
                FillChar(picked, SizeOf(picked), 0);
                picked.text := best_texts[0];
                picked.comment := '';
                picked.score := Max(candidates[0].score + c_solver_promote_bonus,
                    best_scores[0] + c_solver_promote_bonus);
                picked.source := cs_rule;
                picked.has_dict_weight := False;
                picked.dict_weight := 0;
                remember_segment_path_for_candidate(picked.text, '',
                    best_paths[0], picked.score);
            end;

            if existing_index > 0 then
            begin
                for idx := existing_index downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
            end
            else
            begin
                SetLength(candidates, Length(candidates) + 1);
                for idx := High(candidates) downto 1 do
                begin
                    candidates[idx] := candidates[idx - 1];
                end;
            end;
            candidates[0] := picked;
            out_promoted := True;
        finally
            query_engine.Free;
            query_option_cache_options.Free;
            query_option_cache_states.Free;
            query_cache_scores.Free;
            query_cache_texts.Free;
            query_cache_states.Free;
        end;
    end;

    procedure ensure_multi_char_partial_precedes_single_char_partial(
        var candidates: TncCandidateList);
    var
        idx: Integer;
        first_single_index: Integer;
        target_index: Integer;
        picked: TncCandidate;

        function is_complete_multi_char_anchor_local(
            const candidate: TncCandidate): Boolean;
        var
            anchor_units: Integer;
        begin
            Result := False;
            if candidate.comment <> '' then
            begin
                Exit;
            end;

            anchor_units := get_candidate_text_unit_count(Trim(candidate.text));
            if anchor_units < 2 then
            begin
                Exit;
            end;

            if candidate.has_dict_weight or (candidate.source = cs_user) then
            begin
                Exit(True);
            end;

            Result := (candidate.source = cs_rule) and
                (not is_runtime_chain_candidate(candidate)) and
                (not is_runtime_common_pattern_candidate(candidate)) and
                (not is_runtime_redup_candidate(candidate));
        end;

        function is_strong_multi_char_partial_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (candidate.comment <> '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) >= 2) and
                (candidate.has_dict_weight or (candidate.source = cs_user) or
                ((candidate.source = cs_rule) and
                (not is_runtime_chain_candidate(candidate)) and
                (not is_runtime_common_pattern_candidate(candidate)) and
                (not is_runtime_redup_candidate(candidate))));
        end;

        function partial_prefix_units_local(
            const candidate: TncCandidate): Integer;
        var
            tail_units_local: Integer;
        begin
            Result := 0;
            if Trim(candidate.comment) = '' then
            begin
                Exit;
            end;

            Result := get_candidate_text_unit_count(Trim(candidate.text));
            tail_units_local := get_effective_compact_pinyin_unit_count(
                candidate.comment);
            if (Result <= 0) or (tail_units_local <= 0) or
                (Result + tail_units_local <> input_syllable_count) then
            begin
                Result := 0;
            end;
        end;

        function partial_prefix_order_before_local(
            const left: TncCandidate; const right: TncCandidate): Boolean;
        var
            left_units: Integer;
            right_units: Integer;
            left_base_entry: Boolean;
            right_base_entry: Boolean;
            left_weight: Integer;
            right_weight: Integer;

            function build_prefix_key_local(const prefix_units: Integer): string;
            var
                syllables_local: TncPinyinParseResult;
                syllable_idx_local: Integer;
            begin
                Result := '';
                if (prefix_units <= 0) or (lookup_text = '') then
                begin
                    Exit;
                end;

                syllables_local := get_effective_compact_pinyin_syllables(
                    lookup_text);
                if Length(syllables_local) < prefix_units then
                begin
                    Exit;
                end;

                for syllable_idx_local := 0 to prefix_units - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[syllable_idx_local].text);
                end;
            end;

            function is_exact_prefix_entry_local(
                const candidate: TncCandidate; const prefix_units: Integer): Boolean;
            var
                prefix_key_local: string;
            begin
                Result := False;
                if m_dictionary = nil then
                begin
                    Exit;
                end;

                prefix_key_local := build_prefix_key_local(prefix_units);
                Result := (prefix_key_local <> '') and
                    m_dictionary.is_base_entry(prefix_key_local,
                    Trim(candidate.text));
            end;

            function partial_prefix_weight_local(
                const candidate: TncCandidate): Integer;
            begin
                if candidate.has_dict_weight then
                begin
                    Result := candidate.dict_weight;
                end
                else
                begin
                    Result := candidate.score;
                end;
            end;
        begin
            left_units := partial_prefix_units_local(left);
            right_units := partial_prefix_units_local(right);
            if (left_units <= 0) or (right_units <= 0) then
            begin
                Exit(False);
            end;

            if left_units <> right_units then
            begin
                Exit(left_units > right_units);
            end;
            if left_units = 1 then
            begin
                Exit(False);
            end;

            left_base_entry := is_exact_prefix_entry_local(left, left_units);
            right_base_entry := is_exact_prefix_entry_local(right, right_units);
            if left_base_entry <> right_base_entry then
            begin
                Exit(left_base_entry);
            end;

            if left.source <> right.source then
            begin
                if left.source = cs_user then
                begin
                    Exit(True);
                end;
                if right.source = cs_user then
                begin
                    Exit(False);
                end;
            end;

            left_weight := partial_prefix_weight_local(left);
            right_weight := partial_prefix_weight_local(right);
            if left_weight <> right_weight then
            begin
                Exit(left_weight > right_weight);
            end;

            Result := left.score > right.score;
        end;

        procedure sort_consecutive_partial_blocks_by_prefix_units_local;
        var
            block_start: Integer;
            block_end: Integer;
            local_idx: Integer;
            insert_idx: Integer;
            picked_local: TncCandidate;
        begin
            block_start := 0;
            while block_start <= High(candidates) do
            begin
                while (block_start <= High(candidates)) and
                    (partial_prefix_units_local(candidates[block_start]) <= 0) do
                begin
                    Inc(block_start);
                end;
                if block_start > High(candidates) then
                begin
                    Break;
                end;

                block_end := block_start;
                while (block_end <= High(candidates)) and
                    (partial_prefix_units_local(candidates[block_end]) > 0) do
                begin
                    Inc(block_end);
                end;
                Dec(block_end);

                for local_idx := block_start + 1 to block_end do
                begin
                    picked_local := candidates[local_idx];
                    insert_idx := local_idx;
                    while (insert_idx > block_start) and
                        partial_prefix_order_before_local(picked_local,
                        candidates[insert_idx - 1]) do
                    begin
                        candidates[insert_idx] := candidates[insert_idx - 1];
                        Dec(insert_idx);
                    end;
                    candidates[insert_idx] := picked_local;
                end;

                block_start := block_end + 1;
            end;
        end;

    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 2) then
        begin
            Exit;
        end;

        sort_consecutive_partial_blocks_by_prefix_units_local;

        first_single_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (first_single_index < 0) and (candidates[idx].comment <> '') and
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) = 1) then
            begin
                first_single_index := idx;
            end;
        end;

        if first_single_index < 0 then
        begin
            Exit;
        end;

        target_index := 0;
        while (target_index <= High(candidates)) and
            is_complete_multi_char_anchor_local(candidates[target_index]) do
        begin
            Inc(target_index);
        end;

        while (target_index < first_single_index) and
            is_strong_multi_char_partial_local(candidates[target_index]) do
        begin
            Inc(target_index);
        end;

        if target_index > High(candidates) then
        begin
            Exit;
        end;

        idx := first_single_index + 1;
        while idx <= High(candidates) do
        begin
            if not is_strong_multi_char_partial_local(candidates[idx]) then
            begin
                Inc(idx);
                Continue;
            end;

            picked := candidates[idx];
            while idx > target_index do
            begin
                candidates[idx] := candidates[idx - 1];
                Dec(idx);
            end;
            candidates[target_index] := picked;
            Inc(target_index);
            if target_index > first_single_index then
            begin
                first_single_index := target_index;
            end;
            idx := target_index + 1;
        end;

        sort_consecutive_partial_blocks_by_prefix_units_local;
    end;

    function build_lexicon_longer_prefix_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean; forward;
    function build_raw_leading_prefix_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean; forward;
    function build_lightweight_sentence_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean; forward;

    procedure remove_partials_shadowed_by_complete_text_local(
        var candidates: TncCandidateList);
    var
        complete_texts_local: TDictionary<string, Byte>;
        idx_local: Integer;
        out_idx_local: Integer;
        text_key_local: string;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        complete_texts_local := TDictionary<string, Byte>.Create;
        try
            for idx_local := 0 to High(candidates) do
            begin
                text_key_local := LowerCase(Trim(candidates[idx_local].text));
                if (text_key_local <> '') and
                    (Trim(candidates[idx_local].comment) = '') then
                begin
                    complete_texts_local.AddOrSetValue(text_key_local, 1);
                end;
            end;

            if complete_texts_local.Count <= 0 then
            begin
                Exit;
            end;

            out_idx_local := 0;
            for idx_local := 0 to High(candidates) do
            begin
                text_key_local := LowerCase(Trim(candidates[idx_local].text));
                if (Trim(candidates[idx_local].comment) <> '') and
                    (text_key_local <> '') and
                    complete_texts_local.ContainsKey(text_key_local) then
                begin
                    Continue;
                end;

                if out_idx_local <> idx_local then
                begin
                    candidates[out_idx_local] := candidates[idx_local];
                end;
                Inc(out_idx_local);
            end;
            SetLength(candidates, out_idx_local);
        finally
            complete_texts_local.Free;
        end;
    end;

    procedure ensure_rich_head_single_char_partials_visible(
        var candidates: TncCandidateList);
    const
        c_target_single_count = 7;
    var
        target_index: Integer;
        pick_index: Integer;
        picked: TncCandidate;
        moved_count: Integer;
        best_phrase_index: Integer;
        best_phrase_units: Integer;
        best_phrase_score: Integer;
        candidate_units: Integer;
        top_phrase_units: Integer;
        visible_head_single_count: Integer;
        has_visible_secondary_head_partial: Boolean;
        supplemental_candidates: TncCandidateList;
        tail_prefix: string;
        parser: TncPinyinParser;
        raw_syllables: TncPinyinParseResult;
        top_clone: TncCandidate;

        function is_complete_multi_char_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (candidate.comment = '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) >= 2);
        end;

        function is_head_multi_char_partial_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (candidate.comment <> '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) >= 2);
        end;

        function is_head_single_char_partial_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (candidate.comment <> '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) = 1) and
                (get_effective_compact_pinyin_unit_count(candidate.comment) =
                input_syllable_count - 1);
        end;

        function is_top_anchor_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := is_complete_multi_char_candidate_local(candidate) or
                is_head_multi_char_partial_local(candidate);
        end;

        function is_text_prefix_local(const prefix_value: string;
            const full_value: string): Boolean;
        begin
            Result := (prefix_value <> '') and (full_value <> '') and
                (Length(prefix_value) <= Length(full_value)) and
                SameText(Copy(full_value, 1, Length(prefix_value)), prefix_value);
        end;

        function get_raw_query_tail_prefix_local: string;
        begin
            Result := '';
            parser := TncPinyinParser.Create;
            try
                raw_syllables := parser.parse(normalize_pinyin_text(lookup_text));
            finally
                parser.Free;
            end;

            if Length(raw_syllables) <= 0 then
            begin
                Exit;
            end;

            Result := normalize_pinyin_text(raw_syllables[High(raw_syllables)].text);
            if (Result = '') or is_full_pinyin_key(Result) then
            begin
                Result := '';
            end;
        end;

        function has_same_text_tail_partial_local(const text_value: string;
            const comment_value: string): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (Trim(text_value) = '') or (Trim(comment_value) = '') then
            begin
                Exit;
            end;

            for local_idx := 0 to High(candidates) do
            begin
                if SameText(Trim(candidates[local_idx].text), Trim(text_value)) and
                    SameText(Trim(candidates[local_idx].comment), Trim(comment_value)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_extendable_full_tail_query_local: Boolean;
        var
            effective_syllables: TncPinyinParseResult;
            local_syllable_count: Integer;
            suffix_ch: Char;
            extended_key: string;
        begin
            Result := False;
            if lookup_text = '' then
            begin
                Exit;
            end;

            effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
            local_syllable_count := Length(effective_syllables);
            if local_syllable_count < 4 then
            begin
                Exit;
            end;

            for suffix_ch := 'a' to 'z' do
            begin
                extended_key := lookup_text + suffix_ch;
                if get_effective_compact_pinyin_unit_count(extended_key) =
                    local_syllable_count then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure append_raw_head_prefix_partials_local;
        const
            c_raw_single_limit = c_candidate_total_limit_max;
            c_raw_phrase_limit = 3;
        var
            local_prefix_key: string;
            local_tail_comment: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            local_added: Integer;
            local_candidate: TncCandidate;
            local_units: Integer;
        begin
            if Length(raw_syllables) < 2 then
            begin
                Exit;
            end;

            local_prefix_key := normalize_pinyin_text(raw_syllables[0].text);
            local_tail_comment := '';
            for local_idx := 1 to High(raw_syllables) do
            begin
                local_tail_comment := local_tail_comment +
                    normalize_pinyin_text(raw_syllables[local_idx].text);
            end;

            if (m_dictionary <> nil) and (local_prefix_key <> '') and
                (local_tail_comment <> '') and
                m_dictionary.lookup(local_prefix_key, local_results) then
            begin
                local_added := 0;
                for local_idx := 0 to High(local_results) do
                begin
                    if local_added >= c_raw_single_limit then
                    begin
                        Break;
                    end;

                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    local_units := get_candidate_text_unit_count(
                        Trim(local_results[local_idx].text));
                    if local_units <> 1 then
                    begin
                        Continue;
                    end;

                    if has_same_text_tail_partial_local(local_results[local_idx].text,
                        local_tail_comment) then
                    begin
                        Continue;
                    end;

                    local_candidate := local_results[local_idx];
                    local_candidate.comment := local_tail_comment;
                    Inc(local_candidate.score, 420);
                    SetLength(candidates, Length(candidates) + 1);
                    candidates[High(candidates)] := local_candidate;
                    Inc(local_added);
                end;
            end;

            if Length(raw_syllables) < 3 then
            begin
                Exit;
            end;

            local_prefix_key := normalize_pinyin_text(raw_syllables[0].text) +
                normalize_pinyin_text(raw_syllables[1].text);
            local_tail_comment := '';
            for local_idx := 2 to High(raw_syllables) do
            begin
                local_tail_comment := local_tail_comment +
                    normalize_pinyin_text(raw_syllables[local_idx].text);
            end;

            if (m_dictionary <> nil) and (local_prefix_key <> '') and
                (local_tail_comment <> '') and
                m_dictionary.lookup(local_prefix_key, local_results) then
            begin
                local_added := 0;
                for local_idx := 0 to High(local_results) do
                begin
                    if local_added >= c_raw_phrase_limit then
                    begin
                        Break;
                    end;

                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    local_units := get_candidate_text_unit_count(
                        Trim(local_results[local_idx].text));
                    if local_units < 2 then
                    begin
                        Continue;
                    end;

                    if has_same_text_tail_partial_local(local_results[local_idx].text,
                        local_tail_comment) then
                    begin
                        Continue;
                    end;

                    local_candidate := local_results[local_idx];
                    local_candidate.comment := local_tail_comment;
                    Inc(local_candidate.score, 640);
                    SetLength(candidates, Length(candidates) + 1);
                    candidates[High(candidates)] := local_candidate;
                    Inc(local_added);
                end;
            end;
        end;

        function has_exact_full_multi_char_phrase_local(
            const pinyin_key: string): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (Trim(pinyin_key) = '') or
                (not m_dictionary.lookup_exact_full_pinyin(pinyin_key, local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if (Trim(local_results[local_idx].comment) = '') and
                    (get_candidate_text_unit_count(Trim(local_results[local_idx].text)) >= 2) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure append_extendable_full_tail_partial_local;
        var
            head_query_text: string;
            local_tail_comment: string;
            local_idx: Integer;
            head_candidates: TncCandidateList;
            best_head_idx: Integer;
            local_candidate: TncCandidate;
        begin
            if Length(raw_syllables) < 2 then
            begin
                Exit;
            end;
            if tail_prefix <> '' then
            begin
                Exit;
            end;
            if not has_extendable_full_tail_query_local then
            begin
                Exit;
            end;
            if has_exact_full_multi_char_phrase_local(lookup_text) then
            begin
                Exit;
            end;

            head_query_text := '';
            for local_idx := 0 to High(raw_syllables) - 1 do
            begin
                head_query_text := head_query_text +
                    normalize_pinyin_text(raw_syllables[local_idx].text);
            end;
            local_tail_comment := normalize_pinyin_text(
                raw_syllables[High(raw_syllables)].text);
            if (head_query_text = '') or (local_tail_comment = '') then
            begin
                Exit;
            end;

            if not build_lightweight_sentence_candidates(head_query_text, head_candidates) then
            begin
                Exit;
            end;

            best_head_idx := -1;
            for local_idx := 0 to High(head_candidates) do
            begin
                if (Trim(head_candidates[local_idx].comment) = '') and
                    (get_candidate_text_unit_count(Trim(head_candidates[local_idx].text)) >= 2) then
                begin
                    best_head_idx := local_idx;
                    Break;
                end;
            end;
            if best_head_idx < 0 then
            begin
                Exit;
            end;

            if has_same_text_tail_partial_local(head_candidates[best_head_idx].text,
                local_tail_comment) then
            begin
                Exit;
            end;

            local_candidate := head_candidates[best_head_idx];
            local_candidate.comment := local_tail_comment;
            Inc(local_candidate.score, 260);
            SetLength(candidates, Length(candidates) + 1);
            for local_idx := High(candidates) downto 1 do
            begin
                candidates[local_idx] := candidates[local_idx - 1];
            end;
            candidates[0] := local_candidate;
        end;

        procedure append_raw_exact_prefix_partial_local;
        const
            c_weak_exact_prefix_anchor_weight = 300;
        var
            prefix_count: Integer;
            local_idx: Integer;
            insert_idx: Integer;
            head_key: string;
            tail_comment_local: string;
            local_results: TncCandidateList;
            local_candidate: TncCandidate;
            local_weight: Integer;
        begin
            if Length(raw_syllables) < 3 then
            begin
                Exit;
            end;

            if (tail_prefix = '') and has_exact_full_multi_char_phrase_local(lookup_text) then
            begin
                Exit;
            end;

            for prefix_count := High(raw_syllables) downto 2 do
            begin
                head_key := '';
                for local_idx := 0 to prefix_count - 1 do
                begin
                    head_key := head_key + normalize_pinyin_text(raw_syllables[local_idx].text);
                end;

                tail_comment_local := '';
                for local_idx := prefix_count to High(raw_syllables) do
                begin
                    tail_comment_local := tail_comment_local +
                        normalize_pinyin_text(raw_syllables[local_idx].text);
                end;

                if (head_key = '') or (tail_comment_local = '') then
                begin
                    Continue;
                end;
                if (m_dictionary = nil) or
                    (not m_dictionary.lookup_exact_full_pinyin(head_key, local_results)) then
                begin
                    Continue;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if (Trim(local_results[local_idx].comment) <> '') or
                        (get_candidate_text_unit_count(Trim(local_results[local_idx].text)) < 2) then
                    begin
                        Continue;
                    end;

                    if has_same_text_tail_partial_local(local_results[local_idx].text,
                        tail_comment_local) then
                    begin
                        Break;
                    end;

                    local_candidate := local_results[local_idx];
                    local_weight := local_candidate.score;
                    if local_candidate.has_dict_weight and
                        (local_candidate.dict_weight > local_weight) then
                    begin
                        local_weight := local_candidate.dict_weight;
                    end;
                    if (local_candidate.source <> cs_user) and
                        (local_weight < c_weak_exact_prefix_anchor_weight) then
                    begin
                        Continue;
                    end;
                    local_candidate.comment := tail_comment_local;
                    Inc(local_candidate.score, 960 + (prefix_count * 80));
                    SetLength(candidates, Length(candidates) + 1);
                    for insert_idx := High(candidates) downto 1 do
                    begin
                        candidates[insert_idx] := candidates[insert_idx - 1];
                    end;
                    candidates[0] := local_candidate;
                    Exit;
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 3) then
        begin
            Exit;
        end;

        if not is_top_anchor_candidate_local(candidates[0]) then
        begin
            Exit;
        end;

        visible_head_single_count := 0;
        has_visible_secondary_head_partial := False;
        for pick_index := 1 to Min(High(candidates), c_target_single_count + 1) do
        begin
            if is_head_multi_char_partial_local(candidates[pick_index]) then
            begin
                has_visible_secondary_head_partial := True;
            end;
            if is_head_single_char_partial_local(candidates[pick_index]) then
            begin
                Inc(visible_head_single_count);
            end;
        end;

        if ((not has_visible_secondary_head_partial) or
            (visible_head_single_count < c_target_single_count)) and
            build_exact_leading_prefix_partial_candidates(lookup_text,
            supplemental_candidates) then
        begin
            candidates := merge_candidate_lists(candidates, supplemental_candidates, 0);
        end;
        if (visible_head_single_count < c_target_single_count) and
            build_raw_leading_prefix_partial_candidates(lookup_text,
            supplemental_candidates) then
        begin
            candidates := merge_candidate_lists(candidates, supplemental_candidates, 0);
        end;

        tail_prefix := get_raw_query_tail_prefix_local;
        if ((not has_visible_secondary_head_partial) or
            (visible_head_single_count < c_target_single_count)) and (tail_prefix <> '') then
        begin
            append_raw_head_prefix_partials_local;
        end;
        append_raw_exact_prefix_partial_local;
        append_extendable_full_tail_partial_local;

        if (tail_prefix <> '') and is_complete_multi_char_candidate_local(candidates[0]) and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) >= 2) and
            (not has_same_text_tail_partial_local(candidates[0].text, tail_prefix)) then
        begin
            top_clone := candidates[0];
            top_clone.comment := tail_prefix;
            Inc(top_clone.score, 240);
            SetLength(candidates, Length(candidates) + 1);
            for pick_index := High(candidates) downto 1 do
            begin
                candidates[pick_index] := candidates[pick_index - 1];
            end;
            candidates[0] := top_clone;
        end;

        best_phrase_index := -1;
        best_phrase_units := -1;
        best_phrase_score := Low(Integer);
        for pick_index := 1 to High(candidates) do
        begin
            if not is_head_multi_char_partial_local(candidates[pick_index]) then
            begin
                Continue;
            end;

            candidate_units := get_candidate_text_unit_count(Trim(candidates[pick_index].text));
            if (candidate_units > best_phrase_units) or
                ((candidate_units = best_phrase_units) and
                (candidates[pick_index].score > best_phrase_score)) then
            begin
                best_phrase_index := pick_index;
                best_phrase_units := candidate_units;
                best_phrase_score := candidates[pick_index].score;
            end;
        end;

        if best_phrase_index > 1 then
        begin
            picked := candidates[best_phrase_index];
            while best_phrase_index > 1 do
            begin
                candidates[best_phrase_index] := candidates[best_phrase_index - 1];
                Dec(best_phrase_index);
            end;
            candidates[1] := picked;
            best_phrase_index := 1;
        end;

        if (best_phrase_index > 0) and (tail_prefix <> '') and
            is_complete_multi_char_candidate_local(candidates[0]) then
        begin
            top_phrase_units := get_candidate_text_unit_count(Trim(candidates[0].text));
            if (best_phrase_units >= top_phrase_units - 1) and
                is_text_prefix_local(Trim(candidates[best_phrase_index].text),
                Trim(candidates[0].text)) then
            begin
                picked := candidates[best_phrase_index];
                while best_phrase_index > 0 do
                begin
                    candidates[best_phrase_index] := candidates[best_phrase_index - 1];
                    Dec(best_phrase_index);
                end;
                candidates[0] := picked;
            end;
        end;

        if (best_phrase_index > 0) and (Trim(candidates[0].comment) <> '') then
        begin
            top_phrase_units := get_candidate_text_unit_count(Trim(candidates[0].text));
            if best_phrase_units > top_phrase_units then
            begin
                picked := candidates[best_phrase_index];
                while best_phrase_index > 0 do
                begin
                    candidates[best_phrase_index] := candidates[best_phrase_index - 1];
                    Dec(best_phrase_index);
                end;
                candidates[0] := picked;
            end;
        end;

        moved_count := 0;
        for target_index := 2 to Min(High(candidates), c_target_single_count + 1) do
        begin
            if is_head_single_char_partial_local(candidates[target_index]) then
            begin
                Inc(moved_count);
                Continue;
            end;

            pick_index := target_index + 1;
            while pick_index <= High(candidates) do
            begin
                if is_head_single_char_partial_local(candidates[pick_index]) then
                begin
                    Break;
                end;
                Inc(pick_index);
            end;

            if (pick_index < 0) or (pick_index > High(candidates)) or
                (not is_head_single_char_partial_local(candidates[pick_index])) then
            begin
                Break;
            end;

            picked := candidates[pick_index];
            while pick_index > target_index do
            begin
                candidates[pick_index] := candidates[pick_index - 1];
                Dec(pick_index);
            end;
            candidates[target_index] := picked;
            Inc(moved_count);
        end;

        if moved_count = 0 then
        begin
            Exit;
        end;
    end;

    function is_lightweight_sentence_complete_candidate_for_visibility(
        const candidate: TncCandidate): Boolean;
    var
        candidate_text_units: Integer;
        encoded_path: string;
        path_segment_count: Integer;
    begin
        Result := False;
        if (candidate.comment <> '') or candidate.has_dict_weight or
            (candidate.source = cs_user) or (input_syllable_count < 5) then
        begin
            Exit;
        end;

        candidate_text_units := get_candidate_text_unit_count(Trim(candidate.text));
        if candidate_text_units < 4 then
        begin
            Exit;
        end;

        encoded_path := get_segment_path_for_candidate(candidate);
        path_segment_count := get_encoded_path_segment_count_local(encoded_path);
        if path_segment_count < 2 then
        begin
            Exit;
        end;

        Result := get_candidate_segment_path_score_hint(candidate) > Low(Integer);
    end;

    procedure ensure_best_lightweight_sentence_candidate_visible(var candidates: TncCandidateList);
    const
        c_lightweight_segment_tiebreak_margin = 160;
    var
        best_index: Integer;
        best_score_hint: Integer;
        top_score_hint: Integer;
        best_segment_count: Integer;
        top_segment_count: Integer;
        best_candidate_score: Integer;
        top_candidate_score: Integer;
        best_visibility_adjustment: Integer;
        top_visibility_adjustment: Integer;
        idx: Integer;
        picked: TncCandidate;
        visibility_syllables: TncPinyinParseResult;

        function get_fixed_bridge_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'qing' then
            begin
                Result := string(Char($8BF7));
            end;
        end;

        function build_visibility_query_key(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(visibility_syllables)) then
            begin
                Exit;
            end;
            for local_idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(visibility_syllables[local_idx].text);
            end;
        end;

        function try_get_best_exact_three_syllable_text_local(const start_idx: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            query_key: string;
            exact_results: TncCandidateList;
            result_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            query_key := build_visibility_query_key(start_idx, 3);
            if (query_key = '') or (m_dictionary = nil) or
                (not m_dictionary.lookup_exact_full_pinyin(query_key, exact_results)) then
            begin
                Exit;
            end;

            for result_idx := 0 to High(exact_results) do
            begin
                if result_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[result_idx].source = cs_user) or
                    (not exact_results[result_idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[result_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                    is_runtime_redup_candidate(exact_results[result_idx]) then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results[result_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 3 then
                begin
                    Continue;
                end;

                if exact_results[result_idx].has_dict_weight then
                begin
                    weight_value := exact_results[result_idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_results[result_idx].score;
                end;
                if weight_value > out_weight then
                begin
                    out_text := candidate_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_sentence_visibility_adjustment_local(
            const candidate: TncCandidate): Integer;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            segment_text: string;
            next_segment_text: string;
            segment_units: Integer;
            next_segment_units: Integer;
            segment_idx: Integer;
            syllable_idx: Integer;
            left_fixed: string;
            right_fixed: string;
            combined_text: string;
            exact_three_text: string;
            exact_three_weight: Integer;
        begin
            Result := 0;
            if candidate.comment <> '' then
            begin
                Exit;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            if Length(visibility_syllables) <> m_last_lookup_syllable_count then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            syllable_idx := 0;
            for segment_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[segment_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_units = 2) and
                    (syllable_idx + 1 <= High(visibility_syllables)) then
                begin
                    left_fixed := get_fixed_bridge_single_char_local(
                        visibility_syllables[syllable_idx].text);
                    right_fixed := get_fixed_bridge_single_char_local(
                        visibility_syllables[syllable_idx + 1].text);
                    if (left_fixed <> '') and (right_fixed <> '') and
                        (segment_text <> left_fixed + right_fixed) then
                    begin
                        Dec(Result, 1600);
                    end;

                    if (segment_idx < High(segment_parts)) and
                        (syllable_idx + 2 <= High(visibility_syllables)) then
                    begin
                        next_segment_text := Trim(segment_parts[segment_idx + 1]);
                        next_segment_units := get_candidate_text_unit_count(next_segment_text);
                        if next_segment_units >= 1 then
                        begin
                            combined_text := segment_text + split_text_units(next_segment_text)[0];
                            if try_get_best_exact_three_syllable_text_local(
                                syllable_idx, exact_three_text, exact_three_weight) and
                                (exact_three_weight >= 520) then
                            begin
                                if combined_text = exact_three_text then
                                begin
                                    Inc(Result, 520);
                                end
                                else
                                begin
                                    Dec(Result, 880);
                                end;
                            end;
                        end;
                    end;
                end;
                Inc(syllable_idx, segment_units);
            end;
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;

        Exit;

        visibility_syllables := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(m_last_lookup_key));

        best_index := -1;
        best_score_hint := Low(Integer);
        best_segment_count := MaxInt;
        best_candidate_score := Low(Integer);
        best_visibility_adjustment := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if not is_lightweight_sentence_complete_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;

            top_score_hint := get_candidate_segment_path_score_hint(candidates[idx]);
            top_segment_count := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[idx]));
            top_candidate_score := candidates[idx].score;
            top_visibility_adjustment := get_sentence_visibility_adjustment_local(candidates[idx]);
            if (best_index < 0) or
                (top_visibility_adjustment > best_visibility_adjustment) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                (top_candidate_score > best_candidate_score)) or
                ((Abs(top_candidate_score - best_candidate_score) <=
                c_lightweight_segment_tiebreak_margin) and
                (top_visibility_adjustment = best_visibility_adjustment) and
                (top_segment_count < best_segment_count)) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                (Abs(top_candidate_score - best_candidate_score) <=
                c_lightweight_segment_tiebreak_margin) and
                (top_segment_count = best_segment_count) and
                (top_score_hint > best_score_hint)) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                (top_candidate_score = best_candidate_score) and
                (top_segment_count = best_segment_count) and
                (top_score_hint > best_score_hint)) then
            begin
                best_score_hint := top_score_hint;
                best_segment_count := top_segment_count;
                best_candidate_score := top_candidate_score;
                best_visibility_adjustment := top_visibility_adjustment;
                best_index := idx;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        if is_lightweight_sentence_complete_candidate_for_visibility(candidates[0]) then
        begin
            top_score_hint := get_candidate_segment_path_score_hint(candidates[0]);
            top_segment_count := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[0]));
            top_candidate_score := candidates[0].score;
            top_visibility_adjustment := get_sentence_visibility_adjustment_local(candidates[0]);
            if (top_visibility_adjustment > best_visibility_adjustment) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                (top_candidate_score > best_candidate_score)) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                ((top_candidate_score + c_lightweight_segment_tiebreak_margin >=
                best_candidate_score) and
                (top_segment_count <= best_segment_count))) or
                ((top_visibility_adjustment = best_visibility_adjustment) and
                (top_candidate_score + c_lightweight_segment_tiebreak_margin >=
                best_candidate_score) and
                (top_segment_count = best_segment_count) and
                (top_score_hint >= best_score_hint)) then
            begin
                Exit;
            end;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(
        var candidates: TncCandidateList);
    var
        best_index: Integer;
        best_units: Integer;
        best_score: Integer;
        best_has_dict_weight: Boolean;
        idx: Integer;
        current_units: Integer;
        partial_comment_syllables: Integer;
        top_units: Integer;
        top_path_segments: Integer;
        top_is_suspicious_complete: Boolean;
        top_is_suspicious_partial: Boolean;
        picked: TncCandidate;
    begin
        if (Length(candidates) <= 1) or
            (input_syllable_count < 5) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_index := -1;
        best_units := -1;
        best_score := Low(Integer);
        best_has_dict_weight := False;
        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) = '' then
            begin
                Continue;
            end;
            if not is_full_pinyin_key(normalize_pinyin_text(candidates[idx].comment)) then
            begin
                Continue;
            end;
            if not is_strong_exact_partial_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;

            current_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if current_units < 2 then
            begin
                Continue;
            end;

            partial_comment_syllables := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidates[idx].comment));
            if current_units + partial_comment_syllables <> input_syllable_count then
            begin
                Continue;
            end;

            if (candidates[idx].has_dict_weight and (not best_has_dict_weight)) or
                ((candidates[idx].has_dict_weight = best_has_dict_weight) and
                (current_units > best_units)) or
                ((candidates[idx].has_dict_weight = best_has_dict_weight) and
                (current_units = best_units) and
                (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_units := current_units;
                best_score := candidates[idx].score;
                best_has_dict_weight := candidates[idx].has_dict_weight;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        top_units := get_candidate_text_unit_count(Trim(candidates[0].text));
        top_path_segments := get_encoded_path_segment_count_local(
            get_segment_path_for_candidate(candidates[0]));
        top_is_suspicious_complete := (Trim(candidates[0].comment) = '') and
            (top_units = input_syllable_count) and
            (not candidates[0].has_dict_weight) and
            (top_path_segments > 1);
        top_is_suspicious_partial := (Trim(candidates[0].comment) <> '') and
            is_full_pinyin_key(normalize_pinyin_text(candidates[0].comment)) and
            (not candidates[0].has_dict_weight);

        if not (top_is_suspicious_complete or top_is_suspicious_partial) then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_best_user_complete_candidate_visible(var candidates: TncCandidateList);
    var
        best_index: Integer;
        best_bonus: Integer;
        current_top_bonus: Integer;
        latest_query_choice_text: string;
        normalized_query: string;
        idx: Integer;
        local_bonus: Integer;
        picked: TncCandidate;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;
        if m_last_lookup_syllable_count < 2 then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(m_last_lookup_key);
        latest_query_choice_text := '';
        if (normalized_query <> '') and (m_dictionary <> nil) then
        begin
            latest_query_choice_text := get_cached_query_latest_choice_text(
                normalized_query);
        end;

        best_index := -1;
        best_bonus := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].source <> cs_user) or (candidates[idx].comment <> '') or
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                m_last_lookup_syllable_count) then
            begin
                Continue;
            end;
            if is_problematic_single_char_chain_candidate_for_query(
                normalized_query, candidates[idx]) then
            begin
                Continue;
            end;

            local_bonus := 0;
            if is_latest_session_query_choice(candidates[idx].text) then
            begin
                Inc(local_bonus, 800);
            end;
            if (latest_query_choice_text <> '') and
                SameText(Trim(candidates[idx].text), latest_query_choice_text) then
            begin
                Inc(local_bonus, 720);
            end;
            if (normalized_query <> '') and (m_dictionary <> nil) then
            begin
                Inc(local_bonus,
                    m_dictionary.get_query_choice_bonus(normalized_query, candidates[idx].text));
            end;

            if (best_index < 0) or (local_bonus > best_bonus) or
                ((local_bonus = best_bonus) and (candidates[idx].score > candidates[best_index].score)) then
            begin
                best_index := idx;
                best_bonus := local_bonus;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        current_top_bonus := 0;
        if (candidates[0].source = cs_user) and (candidates[0].comment = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) = m_last_lookup_syllable_count) then
        begin
            if is_latest_session_query_choice(candidates[0].text) then
            begin
                Inc(current_top_bonus, 800);
            end;
            if (latest_query_choice_text <> '') and
                SameText(Trim(candidates[0].text), latest_query_choice_text) then
            begin
                Inc(current_top_bonus, 720);
            end;
            if (normalized_query <> '') and (m_dictionary <> nil) then
            begin
                Inc(current_top_bonus,
                    m_dictionary.get_query_choice_bonus(normalized_query, candidates[0].text));
            end;
        end;

        if (best_bonus <= 0) and (candidates[0].source = cs_user) then
        begin
            Exit;
        end;
        if (best_bonus <= current_top_bonus) and (candidates[0].source = cs_user) then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_best_dict_exact_prefix_partial_visible(
        var candidates: TncCandidateList);
    var
        best_index: Integer;
        best_units: Integer;
        best_score: Integer;
        idx: Integer;
        current_units: Integer;
        picked: TncCandidate;
    begin
        if (Length(candidates) <= 1) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_index := -1;
        best_units := -1;
        best_score := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') or
                (not is_full_pinyin_key(normalize_pinyin_text(candidates[idx].comment))) or
                (not candidates[idx].has_dict_weight) then
            begin
                Continue;
            end;

            current_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if current_units < 2 then
            begin
                Continue;
            end;

            if (current_units > best_units) or
                ((current_units = best_units) and
                (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_units := current_units;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_strong_long_dict_prefix_partial_visible(
        var candidates: TncCandidateList);
    var
        syllables_local: TncPinyinParseResult;
        best_index: Integer;
        best_rank: Integer;
        best_synthetic_valid: Boolean;
        best_synthetic: TncCandidate;
        idx: Integer;
        current_rank: Integer;
        text_units: Integer;
        remaining_syllables: Integer;
        top_rank: Integer;
        top_is_suspicious: Boolean;
        picked: TncCandidate;
        prefix_count: Integer;
        prefix_key: string;
        tail_comment: string;
        tail_idx: Integer;
        exact_results: TncCandidateList;
        result_idx: Integer;
        candidate_text: string;
        candidate_units: Integer;
        weight_value: Integer;

        function get_fixed_sentence_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'qing' then
            begin
                Result := string(Char($8BF7));
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (input_syllable_count < 6) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        best_index := -1;
        best_rank := Low(Integer);
        best_synthetic_valid := False;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) = '') or
                (not is_full_pinyin_key(normalize_pinyin_text(candidates[idx].comment))) or
                (not candidates[idx].has_dict_weight) then
            begin
                Continue;
            end;

            text_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if (text_units < 4) or (text_units >= input_syllable_count) then
            begin
                Continue;
            end;

            remaining_syllables := input_syllable_count - text_units;
            current_rank := candidates[idx].score +
                Min(1600, candidates[idx].dict_weight div 2) +
                (text_units * 520) - (remaining_syllables * 80);
            if (text_units <= High(syllables_local)) and
                (get_fixed_sentence_single_char_local(syllables_local[text_units].text) <> '') then
            begin
                Inc(current_rank, 2600);
            end;

            if (best_index < 0) or (current_rank > best_rank) then
            begin
                best_index := idx;
                best_rank := current_rank;
            end;
        end;

        if best_index < 0 then
        begin
            for prefix_count := Min(6, input_syllable_count - 2) downto 4 do
            begin
                prefix_key := '';
                for tail_idx := 0 to prefix_count - 1 do
                begin
                    prefix_key := prefix_key +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if prefix_key = '' then
                begin
                    Continue;
                end;

                tail_comment := '';
                for tail_idx := prefix_count to High(syllables_local) do
                begin
                    tail_comment := tail_comment +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if tail_comment = '' then
                begin
                    Continue;
                end;

                if not m_dictionary.lookup_exact_full_pinyin(prefix_key, exact_results) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(exact_results) do
                begin
                    if Trim(exact_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(exact_results[result_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if candidate_units <> prefix_count then
                    begin
                        Continue;
                    end;

                    weight_value := exact_results[result_idx].score;
                    if exact_results[result_idx].has_dict_weight and
                        (exact_results[result_idx].dict_weight > weight_value) then
                    begin
                        weight_value := exact_results[result_idx].dict_weight;
                    end;

                    current_rank := weight_value + (candidate_units * 520) -
                        ((input_syllable_count - candidate_units) * 80) +
                        Min(1600, weight_value div 2);
                    if (candidate_units <= High(syllables_local)) and
                        (get_fixed_sentence_single_char_local(
                        syllables_local[candidate_units].text) <> '') then
                    begin
                        Inc(current_rank, 2600);
                    end;

                    if current_rank > best_rank then
                    begin
                        best_rank := current_rank;
                        FillChar(best_synthetic, SizeOf(best_synthetic), 0);
                        best_synthetic.text := candidate_text;
                        best_synthetic.comment := tail_comment;
                        best_synthetic.score := weight_value + 920 +
                            (candidate_units * 72) -
                            ((input_syllable_count - candidate_units) * 26);
                        best_synthetic.source := exact_results[result_idx].source;
                        best_synthetic.has_dict_weight := True;
                        best_synthetic.dict_weight := weight_value;
                        best_synthetic_valid := True;
                    end;
                end;
            end;
        end;

        if (best_index < 0) and (not best_synthetic_valid) then
        begin
            Exit;
        end;

        top_rank := candidates[0].score;
        top_is_suspicious := (Trim(candidates[0].comment) <> '') and
            (not candidates[0].has_dict_weight);
        if (not top_is_suspicious) and (best_rank <= top_rank + 2400) then
        begin
            Exit;
        end;

        if best_index > 0 then
        begin
            picked := candidates[best_index];
            for idx := best_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := picked;
        end
        else if best_synthetic_valid then
        begin
            SetLength(candidates, Length(candidates) + 1);
            for idx := High(candidates) downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := best_synthetic;
        end;
    end;

    procedure filter_short_query_nonlexicon_complete_noise(var candidates: TncCandidateList);
    var
        idx: Integer;
        out_idx: Integer;
        candidate_text_units: Integer;
        has_short_complete_anchor: Boolean;
        partial_comment_syllables: Integer;
        normalized_query_local: string;

        function get_display_codepoint_count(const text: string): Integer;
        var
            pos_idx: Integer;
            codepoint: Integer;
            trimmed: string;
        begin
            Result := 0;
            trimmed := Trim(text);
            pos_idx := 1;
            while pos_idx <= Length(trimmed) do
            begin
                codepoint := Ord(trimmed[pos_idx]);
                if (codepoint >= $D800) and (codepoint <= $DBFF) and
                    (pos_idx < Length(trimmed)) and
                    (Ord(trimmed[pos_idx + 1]) >= $DC00) and
                    (Ord(trimmed[pos_idx + 1]) <= $DFFF) then
                begin
                    Inc(pos_idx);
                end;
                Inc(Result);
                Inc(pos_idx);
            end;
        end;

        function has_exact_text_for_key_local(const query_key_local: string;
            const text_value_local: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key_local = '') or
                (Trim(text_value_local) = '') then
            begin
                Exit;
            end;
            if not m_dictionary.lookup_exact_full_pinyin(query_key_local,
                exact_results_local) then
            begin
                Exit;
            end;
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    Trim(text_value_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function join_units_local(const units_local: TArray<string>;
            const first_idx_local: Integer; const last_idx_local: Integer): string;
        var
            unit_idx_local: Integer;
        begin
            Result := '';
            if (first_idx_local < 0) or (last_idx_local > High(units_local)) or
                (first_idx_local > last_idx_local) then
            begin
                Exit;
            end;
            for unit_idx_local := first_idx_local to last_idx_local do
            begin
                Result := Result + units_local[unit_idx_local];
            end;
        end;

        function candidate_has_full_query_support_local(
            const candidate: TncCandidate; const text_value_local: string): Boolean;
        begin
            Result := False;
            if (m_dictionary = nil) or (lookup_text = '') or
                (Trim(text_value_local) = '') then
            begin
                Exit;
            end;
            Result := ((candidate.source = cs_user) and
                m_dictionary.is_user_entry(lookup_text, text_value_local)) or
                has_exact_text_for_key_local(lookup_text, text_value_local);
        end;

        function matches_fixed_prefix_rule_local(const query_prefix_local: string;
            const text_prefix_local: string; const text_value_local: string): Boolean;
        var
            units_local: TArray<string>;
            tail_key_local: string;
            tail_text_local: string;
        begin
            Result := False;
            if (normalized_query_local = '') or
                (Copy(normalized_query_local, 1, Length(query_prefix_local)) <>
                query_prefix_local) then
            begin
                Exit;
            end;
            units_local := split_text_units(text_value_local);
            if (Length(units_local) < 2) or
                (not SameText(units_local[0], text_prefix_local)) then
            begin
                Exit;
            end;
            tail_key_local := Copy(normalized_query_local,
                Length(query_prefix_local) + 1, MaxInt);
            tail_text_local := join_units_local(units_local, 1,
                High(units_local));
            Result := has_exact_text_for_key_local(tail_key_local, tail_text_local);
        end;

        function matches_particle_suffix_rule_local(const query_suffix_local: string;
            const text_suffix_local: string; const text_value_local: string): Boolean;
        var
            units_local: TArray<string>;
            head_key_local: string;
            head_text_local: string;
        begin
            Result := False;
            if (normalized_query_local = '') or
                (Length(normalized_query_local) <= Length(query_suffix_local)) or
                (Copy(normalized_query_local,
                Length(normalized_query_local) - Length(query_suffix_local) + 1,
                Length(query_suffix_local)) <> query_suffix_local) then
            begin
                Exit;
            end;
            units_local := split_text_units(text_value_local);
            if (Length(units_local) < 2) or
                (not SameText(units_local[High(units_local)], text_suffix_local)) then
            begin
                Exit;
            end;
            head_key_local := Copy(normalized_query_local, 1,
                Length(normalized_query_local) - Length(query_suffix_local));
            head_text_local := join_units_local(units_local, 0,
                High(units_local) - 1);
            Result := has_exact_text_for_key_local(head_key_local, head_text_local);
        end;

        function is_allowed_short_rule_complete_local(
            const text_value_local: string): Boolean;
        begin
            Result := matches_fixed_prefix_rule_local('wo', string(Char($6211)),
                text_value_local) or
                matches_fixed_prefix_rule_local('ni', string(Char($4F60)),
                text_value_local) or
                matches_fixed_prefix_rule_local('zui', string(Char($6700)),
                text_value_local) or
                matches_fixed_prefix_rule_local('bu', string(Char($4E0D)),
                text_value_local) or
                matches_fixed_prefix_rule_local('hen', string(Char($5F88)),
                text_value_local) or
                matches_fixed_prefix_rule_local('jiu', string(Char($5C31)),
                text_value_local) or
                matches_particle_suffix_rule_local('de', string(Char($7684)),
                text_value_local) or
                matches_particle_suffix_rule_local('le', string(Char($4E86)),
                text_value_local) or
                matches_particle_suffix_rule_local('la', string(Char($5566)),
                text_value_local) or
                matches_particle_suffix_rule_local('ma', string(Char($5417)),
                text_value_local) or
                matches_particle_suffix_rule_local('ba', string(Char($5427)),
                text_value_local) or
                ((normalized_query_local = 'dema') and
                SameText(text_value_local, string(Char($7684)) +
                string(Char($5417)))) or
                ((normalized_query_local = 'deba') and
                SameText(text_value_local, string(Char($7684)) +
                string(Char($5427)))) or
                ((normalized_query_local = 'dele') and
                SameText(text_value_local, string(Char($7684)) +
                string(Char($4E86)))) or
                ((normalized_query_local = 'leba') and
                SameText(text_value_local, string(Char($4E86)) +
                string(Char($5427)))) or
                ((normalized_query_local = 'lema') and
                SameText(text_value_local, string(Char($4E86)) +
                string(Char($5417))));
        end;

        function should_drop_short_complete_local(
            const candidate: TncCandidate): Boolean;
        var
            text_value_local: string;
            text_units_local: Integer;
        begin
            Result := False;
            if (input_syllable_count < 3) or (input_syllable_count > 6) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;
            text_value_local := Trim(candidate.text);
            text_units_local := get_display_codepoint_count(text_value_local);
            if (text_value_local = '') or (text_units_local <> input_syllable_count) then
            begin
                Exit;
            end;
            if candidate_has_full_query_support_local(candidate, text_value_local) or
                is_allowed_short_rule_complete_local(text_value_local) then
            begin
                Exit;
            end;
            Result := True;
        end;

    begin
        if (Length(candidates) = 0) or (input_syllable_count < 2) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        normalized_query_local := normalize_pinyin_text(lookup_text);

        has_short_complete_anchor := False;
        for idx := 0 to High(candidates) do
        begin
            candidate_text_units := get_display_codepoint_count(candidates[idx].text);
            if (Trim(candidates[idx].comment) = '') and
                (candidate_text_units >= 2) and (candidate_text_units <= 4) and
                (candidates[idx].has_dict_weight or (candidates[idx].source = cs_user)) then
            begin
                has_short_complete_anchor := True;
                Break;
            end;
            if (Trim(candidates[idx].comment) <> '') and
                (candidate_text_units >= 2) and (candidate_text_units <= 4) and
                (candidates[idx].source in [cs_rule, cs_user]) then
            begin
                partial_comment_syllables := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(candidates[idx].comment)));
                if (partial_comment_syllables > 0) and
                    (candidate_text_units + partial_comment_syllables = input_syllable_count) then
                begin
                    has_short_complete_anchor := True;
                    Break;
                end;
            end;
        end;
        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            candidate_text_units := get_display_codepoint_count(candidates[idx].text);
            if should_drop_short_complete_local(candidates[idx]) then
            begin
                Continue;
            end;
            if has_short_complete_anchor and (candidates[idx].comment = '') and
                (candidate_text_units >= 2) and (candidate_text_units <= 4) and
                (not candidates[idx].has_dict_weight) and
                (candidates[idx].source <> cs_user) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                candidates[out_idx] := candidates[idx];
            end;
            Inc(out_idx);
        end;
        SetLength(candidates, out_idx);
    end;

    procedure ensure_long_sentence_partial_exact_candidate_visible(
        var candidates: TncCandidateList);
    var
        best_index: Integer;
        best_score: Integer;
        idx: Integer;
        picked: TncCandidate;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < c_long_sentence_exact_min_syllables) or
            is_full_pinyin_key(lookup_text) or
            (not has_long_sentence_extendable_tail_for_visibility_local) then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if not is_long_sentence_partial_exact_candidate_for_visibility(candidates[idx]) then
            begin
                Continue;
            end;
            if candidates[idx].score > best_score then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_short_exact_tail_completion_visible(var candidates: TncCandidateList);
        function is_short_exact_tail_completion_seed_candidate(
            const candidate: TncCandidate): Boolean;
        var
            partial_text_units: Integer;
        begin
            Result := False;
            if Trim(candidate.comment) = '' then
            begin
                Exit;
            end;

            partial_text_units := get_candidate_text_unit_count(Trim(candidate.text));
            if partial_text_units < 2 then
            begin
                Exit;
            end;

            if partial_text_units <> input_syllable_count - 1 then
            begin
                Exit;
            end;

            if get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate.comment)) <> 1 then
            begin
                Exit;
            end;

            Result := True;
        end;
        function can_extend_single_syllable_prefix(const value: string): Boolean;
        var
            effective_parts: TncPinyinParseResult;
            local_syllable_count: Integer;
            suffix_ch: Char;
            extended_key: string;
        begin
            Result := False;
            if value = '' then
            begin
                Exit;
            end;

            effective_parts := get_effective_compact_pinyin_syllables(value);
            local_syllable_count := Length(effective_parts);
            if local_syllable_count <> 1 then
            begin
                Exit;
            end;

            for suffix_ch := 'a' to 'z' do
            begin
                extended_key := value + suffix_ch;
                if get_effective_compact_pinyin_unit_count(extended_key) = 1 then
                begin
                    Exit(True);
                end;
            end;
        end;
        procedure get_prefix_tail_context(const partial_candidate: TncCandidate;
            out out_prev_prev_text: string; out out_prev_text: string);
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            segment_count: Integer;
        begin
            out_prev_prev_text := '';
            out_prev_text := '';
            encoded_path := get_segment_path_for_candidate(partial_candidate);
            if Trim(encoded_path) <> '' then
            begin
                segment_parts := encoded_path.Split([c_segment_path_separator]);
                segment_count := Length(segment_parts);
                if segment_count >= 1 then
                begin
                    out_prev_text := Trim(segment_parts[segment_count - 1]);
                end;
                if segment_count >= 2 then
                begin
                    out_prev_prev_text := Trim(segment_parts[segment_count - 2]);
                end;
            end;

            if out_prev_text = '' then
            begin
                out_prev_text := Trim(partial_candidate.text);
            end;
        end;
        function lookup_exact_tail_cached(const pinyin_key: string;
            out out_results: TncCandidateList): Boolean;
        begin
            Result := lookup_exact_full_pinyin_cached_local(pinyin_key,
                out_results);
        end;
        function is_preferred_prefix_single_char(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E00, $6211, $4F60, $4ED6, $5979, $5B83, $548C, $53BB, $6765, $5728,
                $662F, $6709, $8981, $60F3, $4F1A, $80FD, $628A, $88AB, $8BA9,
                $7ED9, $5411, $5BF9, $4E8E, $8DDF, $540C, $4ECE, $4E0E, $5C31,
                $4E5F, $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A, $6700,
                $5DF2, $5C06, $5230, $7740, $8FC7, $4E86:
                    Result := True;
            end;
        end;
        function get_prefix_completion_priority(const value: string): Integer;
        begin
            if value = 'wo' then Exit(2000);
            if value = 'he' then Exit(1980);
            if value = 'yi' then Exit(1960);
            if value = 'you' then Exit(1940);
            if value = 'yao' then Exit(1930);
            if value = 'shi' then Exit(1920);
            if value = 'zai' then Exit(1910);
            if value = 'bu' then Exit(1900);
            if value = 'le' then Exit(1890);
            if value = 'hai' then Exit(1880);
            if value = 'hen' then Exit(1870);
            if value = 'jiu' then Exit(1860);
            if value = 'hui' then Exit(1850);
            if value = 'neng' then Exit(1840);
            if value = 'qu' then Exit(1830);
            if value = 'lai' then Exit(1820);
            if value = 'men' then Exit(1810);
            if value = 'gei' then Exit(1800);
            if value = 'bei' then Exit(1790);
            if value = 'ba' then Exit(1780);
            if value = 'de' then Exit(1770);

            Result := 1000 - Length(value);
        end;
        procedure sort_completion_list_by_priority(var completion_list: TList<string>);
        var
            item_idx: Integer;
            insert_idx: Integer;
            item_value: string;

            function compare_values(const left_value: string;
                const right_value: string): Integer;
            var
                left_priority: Integer;
                right_priority: Integer;
            begin
                left_priority := get_prefix_completion_priority(left_value);
                right_priority := get_prefix_completion_priority(right_value);
                Result := right_priority - left_priority;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := Length(left_value) - Length(right_value);
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := CompareText(left_value, right_value);
            end;

        begin
            if (completion_list = nil) or (completion_list.Count <= 1) then
            begin
                Exit;
            end;

            for item_idx := 1 to completion_list.Count - 1 do
            begin
                item_value := completion_list[item_idx];
                insert_idx := item_idx - 1;
                while (insert_idx >= 0) and
                    (compare_values(completion_list[insert_idx], item_value) > 0) do
                begin
                    completion_list[insert_idx + 1] :=
                        completion_list[insert_idx];
                    Dec(insert_idx);
                end;
                completion_list[insert_idx + 1] := item_value;
            end;
        end;
        function is_prefix_initial_final_compatible(const initial_value: string;
            const final_value: string): Boolean;
        begin
            if (initial_value = 'zh') or (initial_value = 'ch') or (initial_value = 'sh') or
                (initial_value = 'r') or (initial_value = 'z') or (initial_value = 'c') or
                (initial_value = 's') then
            begin
                if (final_value = 'ia') or (final_value = 'in') or (final_value = 'ing') or (final_value = 'iu') or
                    (final_value = 'ie') or (final_value = 'ian') or (final_value = 'iang') or
                    (final_value = 'iao') or (final_value = 'iong') or
                    (final_value = 'ue') or (final_value = 've') or (final_value = 'van') or
                    (final_value = 'vn') then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;
        procedure add_single_syllable_completion(const value: string;
            var completion_list: TList<string>);
        begin
            if (value = '') or (completion_list = nil) then
            begin
                Exit;
            end;
            if completion_list.IndexOf(value) >= 0 then
            begin
                Exit;
            end;
            completion_list.Add(value);
        end;
        procedure collect_single_syllable_completions(const prefix_value: string;
            var completion_list: TList<string>);
        const
            c_priority_completion_keys: array[0..20] of string = (
                'wo', 'he', 'yi', 'you', 'yao', 'shi', 'zai', 'bu', 'le', 'hai',
                'hen', 'jiu', 'hui', 'neng', 'qu', 'lai', 'men', 'gei', 'bei', 'ba',
                'de'
            );
            c_prefix_completion_initials: array[0..22] of string = (
                'zh', 'ch', 'sh',
                'b', 'p', 'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h',
                'j', 'q', 'x', 'r', 'z', 'c', 's', 'y', 'w'
            );
            c_prefix_completion_finals: array[0..35] of string = (
                'iang', 'iong', 'uang',
                'uai', 'uan', 'iao', 'ian', 'ing', 'ang', 'eng', 'ong',
                'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
                'ia', 'ie', 'in', 'iu', 'ua', 'ui', 'un', 'uo',
                've', 'van', 'vn', 'ue',
                'a', 'e', 'i', 'o', 'u', 'v'
            );
            c_prefix_completion_no_initial_finals: array[0..11] of string = (
                'ang', 'eng',
                'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
                'a', 'e', 'o'
            );
        var
            normalized_prefix: string;
            priority_idx: Integer;
            initial_idx: Integer;
            final_idx: Integer;
            initial_value: string;
            final_value: string;
            full_value: string;
            prefix_tail: string;
        begin
            normalized_prefix := normalize_pinyin_text(prefix_value);
            if (normalized_prefix = '') or (completion_list = nil) then
            begin
                Exit;
            end;

            if Length(normalized_prefix) = 1 then
            begin
                for priority_idx := Low(c_priority_completion_keys) to High(c_priority_completion_keys) do
                begin
                    full_value := c_priority_completion_keys[priority_idx];
                    if full_value.StartsWith(normalized_prefix) then
                    begin
                        add_single_syllable_completion(full_value, completion_list);
                    end;
                end;
                if completion_list.Count > 0 then
                begin
                    Exit;
                end;
            end;

            for initial_idx := Low(c_prefix_completion_initials) to High(c_prefix_completion_initials) do
            begin
                initial_value := c_prefix_completion_initials[initial_idx];
                for final_idx := Low(c_prefix_completion_finals) to High(c_prefix_completion_finals) do
                begin
                    final_value := c_prefix_completion_finals[final_idx];
                    if not is_prefix_initial_final_compatible(initial_value, final_value) then
                    begin
                        Continue;
                    end;
                    full_value := initial_value + final_value;
                    if full_value.StartsWith(normalized_prefix) then
                    begin
                        add_single_syllable_completion(full_value, completion_list);
                    end
                    else if normalized_prefix.StartsWith(initial_value) then
                    begin
                        prefix_tail := Copy(normalized_prefix, Length(initial_value) + 1, MaxInt);
                        if (prefix_tail <> '') and final_value.StartsWith(prefix_tail) then
                        begin
                            add_single_syllable_completion(full_value, completion_list);
                        end;
                    end;
                end;
            end;

            for final_idx := Low(c_prefix_completion_no_initial_finals) to High(c_prefix_completion_no_initial_finals) do
            begin
                final_value := c_prefix_completion_no_initial_finals[final_idx];
                if final_value.StartsWith(normalized_prefix) or normalized_prefix.StartsWith(final_value) then
                begin
                    add_single_syllable_completion(final_value, completion_list);
                end;
            end;
        end;
        function try_pick_fast_prefix_single_char_tail(
            const prefix_value: string; out out_text: string): Boolean;
        var
            local_completion_keys: TList<string>;
            local_probe_limit: Integer;
            local_completion_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            local_result_idx: Integer;
            local_candidate_text: string;
            local_is_preferred: Boolean;
            local_fallback_text: string;
        begin
            Result := False;
            out_text := '';
            local_fallback_text := '';
            if Length(normalize_pinyin_text(prefix_value)) > 1 then
            begin
                Exit;
            end;

            local_completion_keys := TList<string>.Create;
            try
                collect_single_syllable_completions(prefix_value, local_completion_keys);
                sort_completion_list_by_priority(local_completion_keys);
                if local_completion_keys.Count <= 0 then
                begin
                    Exit;
                end;

                local_probe_limit := Min(3, local_completion_keys.Count);
                for local_idx := 0 to local_probe_limit - 1 do
                begin
                    local_completion_key := local_completion_keys[local_idx];
                    if not lookup_exact_tail_cached(local_completion_key, local_results) then
                    begin
                        Continue;
                    end;

                    for local_result_idx := 0 to High(local_results) do
                    begin
                        if local_result_idx >= 16 then
                        begin
                            Break;
                        end;
                        local_candidate_text := Trim(local_results[local_result_idx].text);
                        if get_candidate_text_unit_count(local_candidate_text) <> 1 then
                        begin
                            Continue;
                        end;
                        if not match_single_char_candidate_for_syllable(
                            local_completion_key, local_candidate_text, local_is_preferred) then
                        begin
                            Continue;
                        end;
                        if local_fallback_text = '' then
                        begin
                            local_fallback_text := local_candidate_text;
                        end;
                        if is_preferred_prefix_single_char(local_candidate_text) then
                        begin
                            out_text := local_candidate_text;
                            Exit(True);
                        end;
                    end;
                end;

                if (local_fallback_text <> '') and
                    (Length(normalize_pinyin_text(prefix_value)) > 1) then
                begin
                    out_text := local_fallback_text;
                    Exit(True);
                end;
            finally
                local_completion_keys.Free;
            end;
        end;
    var
        best_partial_index: Integer;
        best_partial_score: Integer;
        best_partial: TncCandidate;
        tail_lookup: TncCandidateList;
        tail_single_candidate: TncCandidateList;
        tail_idx: Integer;
        tail_is_preferred: Boolean;
        tail_text: string;
        tail_fallback_text: string;
        tail_fallback_score: Integer;
        tail_prefix_completion_keys: TList<string>;
        tail_prefix_completion_key: string;
        tail_prefix_results: TncCandidateList;
        tail_prefix_idx: Integer;
        tail_prefix_candidate: TncCandidate;
        tail_prefix_prev_text: string;
        tail_prefix_prev_prev_text: string;
        tail_prefix_score: Integer;
        tail_prefix_best_score: Integer;
        tail_prefix_second_best_score: Integer;
        tail_prefix_has_allowed_candidate: Boolean;
        tail_prefix_is_preferred: Boolean;
        tail_prefix_probe_limit: Integer;
        combined: TncCandidate;
        combined_path: string;
        combined_base_score: Integer;

        function is_short_particle_tail_key_local(const value: string): Boolean;
        var
            normalized_value: string;
        begin
            normalized_value := normalize_pinyin_text(value);
            Result := SameText(normalized_value, 'de') or
                SameText(normalized_value, 'le') or
                SameText(normalized_value, 'zhe') or
                SameText(normalized_value, 'la') or
                SameText(normalized_value, 'ba') or
                SameText(normalized_value, 'ma') or
                SameText(normalized_value, 'a') or
                SameText(normalized_value, 'hao');
        end;

        function get_tail_completion_base_score_local(
            const partial_candidate: TncCandidate): Integer;
        var
            local_tail_key: string;
            local_tail_text: string;
            local_head_key: string;
            local_exact_results: TncCandidateList;
            local_exact_idx: Integer;
            local_exact_text: string;
            local_exact_weight: Integer;
            local_head_text: string;
            local_head_units: Integer;
        begin
            local_head_text := Trim(partial_candidate.text);
            local_head_units := get_candidate_text_unit_count(local_head_text);
            if is_short_particle_tail_key_local(partial_candidate.comment) and
                try_get_short_particle_tail_query_parts(lookup_text,
                local_tail_key, local_tail_text, local_head_key) and
                SameText(local_tail_key,
                normalize_pinyin_text(partial_candidate.comment)) and
                lookup_exact_tail_cached(local_head_key, local_exact_results) then
            begin
                Result := 0;
                for local_exact_idx := 0 to High(local_exact_results) do
                begin
                    local_exact_text :=
                        Trim(local_exact_results[local_exact_idx].text);
                    if (Trim(local_exact_results[local_exact_idx].comment) <> '') or
                        (not SameText(local_exact_text, local_head_text)) or
                        (get_candidate_text_unit_count(local_exact_text) <>
                        local_head_units) then
                    begin
                        Continue;
                    end;

                    local_exact_weight := local_exact_results[local_exact_idx].score;
                    if local_exact_results[local_exact_idx].has_dict_weight then
                    begin
                        local_exact_weight :=
                            local_exact_results[local_exact_idx].dict_weight;
                    end;
                    if local_exact_weight > Result then
                    begin
                        Result := local_exact_weight;
                    end;
                end;
                if Result > 0 then
                begin
                    if partial_candidate.source = cs_user then
                    begin
                        Inc(Result, c_user_score_bonus);
                    end;
                    Exit;
                end;
            end;

            Result := partial_candidate.score;
            if partial_candidate.has_dict_weight then
            begin
                Result := partial_candidate.dict_weight;
            end;
            if Result < 0 then
            begin
                Result := 0;
            end;
            if partial_candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function get_tail_completion_score_local(
            const partial_candidate: TncCandidate): Integer;
        const
            c_particle_tail_min_head_score = 80;
            c_particle_tail_visible_bonus = 160;
            c_particle_tail_unit_bonus = 24;
            c_particle_tail_weak_bonus = 8;
        var
            base_score_local: Integer;
            partial_units_local: Integer;
        begin
            base_score_local := get_tail_completion_base_score_local(
                partial_candidate);
            if is_short_particle_tail_key_local(partial_candidate.comment) then
            begin
                if base_score_local < c_particle_tail_min_head_score then
                begin
                    Exit(base_score_local + c_particle_tail_weak_bonus);
                end;

                partial_units_local := get_candidate_text_unit_count(
                    Trim(partial_candidate.text));
                Exit(base_score_local + c_particle_tail_visible_bonus +
                    (partial_units_local * c_particle_tail_unit_bonus));
            end;

            Result := base_score_local + 880;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < c_long_sentence_exact_min_syllables) then
        begin
            Exit;
        end;

        best_partial_index := -1;
        best_partial_score := Low(Integer);
        for tail_idx := 0 to High(candidates) do
        begin
            if not is_short_exact_tail_completion_seed_candidate(candidates[tail_idx]) then
            begin
                Continue;
            end;
            if candidates[tail_idx].score > best_partial_score then
            begin
                best_partial_index := tail_idx;
                best_partial_score := candidates[tail_idx].score;
            end;
        end;

        if best_partial_index < 0 then
        begin
            Exit;
        end;

        best_partial := candidates[best_partial_index];
        if (m_dictionary = nil) or
            (not lookup_exact_tail_cached(
            normalize_pinyin_text(best_partial.comment), tail_lookup)) then
        begin
            SetLength(tail_lookup, 0);
        end;

        tail_text := '';
        tail_fallback_text := '';
        tail_fallback_score := Low(Integer);
        for tail_idx := 0 to High(tail_lookup) do
        begin
            if get_candidate_text_unit_count(Trim(tail_lookup[tail_idx].text)) <> 1 then
            begin
                Continue;
            end;
            if not match_single_char_candidate_for_syllable(
                normalize_pinyin_text(best_partial.comment), Trim(tail_lookup[tail_idx].text),
                tail_is_preferred) then
            begin
                Continue;
            end;
            if not tail_is_preferred then
            begin
                if tail_lookup[tail_idx].score > tail_fallback_score then
                begin
                    tail_fallback_text := Trim(tail_lookup[tail_idx].text);
                    tail_fallback_score := tail_lookup[tail_idx].score;
                end;
                Continue;
            end;
            tail_text := Trim(tail_lookup[tail_idx].text);
            Break;
        end;

        if tail_text = '' then
        begin
            tail_text := tail_fallback_text;
        end;

        if tail_text = '' then
        begin
            if try_pick_fast_prefix_single_char_tail(best_partial.comment, tail_text) then
            begin
                // Fast path for one-letter trailing prefixes such as "w/h/y".
                // This avoids the much heavier context-scored completion scan.
            end
            else
            begin
            tail_prefix_completion_keys := TList<string>.Create;
            try
                collect_single_syllable_completions(best_partial.comment, tail_prefix_completion_keys);
                sort_completion_list_by_priority(tail_prefix_completion_keys);
                if Length(normalize_pinyin_text(best_partial.comment)) <= 1 then
                begin
                    tail_prefix_probe_limit := 4;
                end
                else
                begin
                    tail_prefix_probe_limit := 6;
                end;
                if can_extend_single_syllable_prefix(normalize_pinyin_text(best_partial.comment)) and
                    (tail_prefix_completion_keys.Count > 0) then
                begin
                    get_prefix_tail_context(best_partial, tail_prefix_prev_prev_text, tail_prefix_prev_text);
                    tail_prefix_has_allowed_candidate := False;
                    for tail_prefix_idx := 0 to tail_prefix_completion_keys.Count - 1 do
                    begin
                        if tail_prefix_idx >= tail_prefix_probe_limit then
                        begin
                            Break;
                        end;
                        tail_prefix_completion_key := tail_prefix_completion_keys[tail_prefix_idx];
                        if not lookup_exact_tail_cached(tail_prefix_completion_key, tail_prefix_results) then
                        begin
                            Continue;
                        end;
                        for tail_idx := 0 to High(tail_prefix_results) do
                        begin
                            if (get_candidate_text_unit_count(Trim(tail_prefix_results[tail_idx].text)) = 1) and
                                is_preferred_prefix_single_char(Trim(tail_prefix_results[tail_idx].text)) then
                            begin
                                tail_prefix_has_allowed_candidate := True;
                                Break;
                            end;
                        end;
                        if tail_prefix_has_allowed_candidate then
                        begin
                            Break;
                        end;
                    end;

                    tail_prefix_best_score := Low(Integer);
                    tail_prefix_second_best_score := Low(Integer);
                    for tail_prefix_idx := 0 to tail_prefix_completion_keys.Count - 1 do
                    begin
                        if tail_prefix_idx >= tail_prefix_probe_limit then
                        begin
                            Break;
                        end;
                        tail_prefix_completion_key := tail_prefix_completion_keys[tail_prefix_idx];
                        if not lookup_exact_tail_cached(tail_prefix_completion_key, tail_prefix_results) then
                        begin
                            Continue;
                        end;

                        for tail_idx := 0 to High(tail_prefix_results) do
                        begin
                            tail_prefix_candidate := tail_prefix_results[tail_idx];
                            if get_candidate_text_unit_count(Trim(tail_prefix_candidate.text)) <> 1 then
                            begin
                                Continue;
                            end;
                            if not match_single_char_candidate_for_syllable(
                                tail_prefix_completion_key, Trim(tail_prefix_candidate.text),
                                tail_prefix_is_preferred) then
                            begin
                                Continue;
                            end;
                            if tail_prefix_has_allowed_candidate and
                                (not is_preferred_prefix_single_char(Trim(tail_prefix_candidate.text))) then
                            begin
                                Continue;
                            end;

                            tail_prefix_score := tail_prefix_candidate.score;
                            if tail_prefix_candidate.has_dict_weight and
                                (tail_prefix_candidate.dict_weight > tail_prefix_score) then
                            begin
                                tail_prefix_score := tail_prefix_candidate.dict_weight;
                            end;
                            if tail_prefix_is_preferred then
                            begin
                                Inc(tail_prefix_score, 120);
                            end;
                            if is_preferred_prefix_single_char(Trim(tail_prefix_candidate.text)) then
                            begin
                                Inc(tail_prefix_score, 180);
                            end;
                            if (m_dictionary <> nil) and (tail_prefix_prev_text <> '') then
                            begin
                                Inc(tail_prefix_score, m_dictionary.get_context_bonus(
                                    tail_prefix_prev_text, Trim(tail_prefix_candidate.text)));
                                if tail_prefix_prev_prev_text <> '' then
                                begin
                                    Inc(tail_prefix_score, m_dictionary.get_context_trigram_bonus(
                                        tail_prefix_prev_prev_text, tail_prefix_prev_text,
                                        Trim(tail_prefix_candidate.text)));
                                end;
                            end;

                            if tail_prefix_score > tail_prefix_best_score then
                            begin
                                tail_prefix_second_best_score := tail_prefix_best_score;
                                tail_prefix_best_score := tail_prefix_score;
                                tail_text := Trim(tail_prefix_candidate.text);
                            end
                            else if tail_prefix_score > tail_prefix_second_best_score then
                            begin
                                tail_prefix_second_best_score := tail_prefix_score;
                            end;
                        end;
                    end;

                    if (tail_text <> '') and
                        (tail_prefix_second_best_score > Low(Integer) div 2) and
                        ((tail_prefix_best_score - tail_prefix_second_best_score) < 120) and
                        (Length(normalize_pinyin_text(best_partial.comment)) <= 1) then
                    begin
                        tail_text := '';
                    end;
                end;
            finally
                tail_prefix_completion_keys.Free;
            end;
            end;
        end;

        if tail_text = '' then
        begin
            Exit;
        end;

        combined.text := Trim(best_partial.text) + tail_text;
        combined.comment := '';
        combined.score := get_tail_completion_score_local(best_partial);
        combined.source := best_partial.source;
        combined.has_dict_weight := True;
        combined_base_score := get_tail_completion_base_score_local(best_partial);
        combined.dict_weight := combined_base_score;

        combined_path := get_segment_path_for_candidate(best_partial);
        if combined_path <> '' then
        begin
            combined_path := combined_path + c_segment_path_separator + tail_text;
        end
        else
        begin
            combined_path := Trim(best_partial.text) + c_segment_path_separator + tail_text;
        end;
        remember_segment_path_for_candidate(combined.text, '', combined_path, combined.score);
        SetLength(tail_single_candidate, 1);
        tail_single_candidate[0] := combined;
        candidates := merge_candidate_lists(candidates, tail_single_candidate, 0);
        sort_candidates_lightweight(candidates);
    end;

    procedure ensure_incomplete_tail_partial_precedes_completed_extension(
        var candidates: TncCandidateList);
    var
        top_units: Integer;
        idx: Integer;
        best_index: Integer;
        best_units: Integer;
        best_score: Integer;
        partial_units: Integer;
        picked: TncCandidate;

        function is_text_prefix_local(const prefix_value: string;
            const full_value: string): Boolean;
        begin
            Result := (prefix_value <> '') and (full_value <> '') and
                (Length(prefix_value) <= Length(full_value)) and
                SameText(Copy(full_value, 1, Length(prefix_value)), prefix_value);
        end;

        function candidate_completion_is_strong_local(
            const candidate_local: TncCandidate;
            const candidate_index_local: Integer): Boolean;
        var
            candidate_weight_local: Integer;
            encoded_path_local: string;
        begin
            Result := False;
            if Trim(candidate_local.comment) <> '' then
            begin
                Exit;
            end;

            if candidate_local.source = cs_user then
            begin
                Exit(True);
            end;

            encoded_path_local := get_segment_path_for_candidate(
                candidate_local, candidate_index_local);
            if get_encoded_path_segment_count_local(encoded_path_local) > 1 then
            begin
                Exit(True);
            end;

            candidate_weight_local := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > candidate_weight_local) then
            begin
                candidate_weight_local := candidate_local.dict_weight;
            end;
            Result := candidate_weight_local >= 4096;
        end;

        function has_visible_exact_completion_for_partial_local(
            const partial_text_local: string; const tail_comment_local: string): Boolean;
        var
            exact_tail_results_local: TncCandidateList;
            exact_tail_idx_local: Integer;
            candidate_idx_local: Integer;
            combined_text_local: string;
            normalized_tail_local: string;
        begin
            Result := False;
            normalized_tail_local := normalize_pinyin_text(tail_comment_local);
            if (partial_text_local = '') or (normalized_tail_local = '') or
                (not is_full_pinyin_key(normalized_tail_local)) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            if not m_dictionary.lookup_exact_full_pinyin(normalized_tail_local,
                exact_tail_results_local) then
            begin
                Exit;
            end;

            for exact_tail_idx_local := 0 to High(exact_tail_results_local) do
            begin
                if Trim(exact_tail_results_local[exact_tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                combined_text_local := partial_text_local +
                    Trim(exact_tail_results_local[exact_tail_idx_local].text);
                if combined_text_local = partial_text_local then
                begin
                    Continue;
                end;

                for candidate_idx_local := 0 to High(candidates) do
                begin
                    if (Trim(candidates[candidate_idx_local].comment) = '') and
                        SameText(Trim(candidates[candidate_idx_local].text),
                        combined_text_local) then
                    begin
                        if candidate_completion_is_strong_local(
                            candidates[candidate_idx_local],
                            candidate_idx_local) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' itail=[skip short]';
            end;
            Exit;
        end;
        if Trim(candidates[0].comment) <> '' then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' itail=[skip top-partial]';
            end;
            Exit;
        end;

        top_units := get_candidate_text_unit_count(Trim(candidates[0].text));
        if top_units < 3 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' itail=[skip top-units=%d]', [top_units]);
            end;
            Exit;
        end;

        best_index := -1;
        best_units := -1;
        best_score := Low(Integer);
        for idx := 1 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) = '' then
            begin
                Continue;
            end;

            partial_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if partial_units < 2 then
            begin
                Continue;
            end;
            if partial_units >= input_syllable_count then
            begin
                Continue;
            end;
            if partial_units < top_units - 1 then
            begin
                Continue;
            end;
            if has_visible_exact_completion_for_partial_local(
                Trim(candidates[idx].text), Trim(candidates[idx].comment)) then
            begin
                Continue;
            end;
            if not is_text_prefix_local(Trim(candidates[idx].text),
                Trim(candidates[0].text)) then
            begin
                Continue;
            end;

            if (partial_units > best_units) or
                ((partial_units = best_units) and (candidates[idx].score > best_score)) then
            begin
                best_index := idx;
                best_units := partial_units;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index <= 0 then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' itail=[skip no-partial]';
            end;
            Exit;
        end;

        picked := candidates[best_index];
        while best_index > 0 do
        begin
            candidates[best_index] := candidates[best_index - 1];
            Dec(best_index);
        end;
        candidates[0] := picked;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' itail=[promote text=%s comment=%s]',
                [Trim(candidates[0].text), Trim(candidates[0].comment)]);
        end;
    end;

    procedure ensure_complete_extension_precedes_prefix_partial(
        var candidates: TncCandidateList);
    var
        top_text: string;
        top_units: Integer;
        idx: Integer;
        best_index: Integer;
        best_score: Integer;
        best_shared_prefix_units: Integer;
        picked: TncCandidate;
        effective_syllables: TncPinyinParseResult;
        candidate_text: string;
        shared_prefix_units: Integer;
        suffix_text: string;

        function is_text_prefix_local(const prefix_value: string;
            const full_value: string): Boolean;
        begin
            Result := (prefix_value <> '') and (full_value <> '') and
                (Length(prefix_value) <= Length(full_value)) and
                SameText(Copy(full_value, 1, Length(prefix_value)), prefix_value);
        end;

        function get_shared_prefix_unit_count_local(const left_value: string;
            const right_value: string): Integer;
        var
            left_units: TArray<string>;
            right_units: TArray<string>;
            local_idx: Integer;
        begin
            Result := 0;
            left_units := split_text_units(Trim(left_value));
            right_units := split_text_units(Trim(right_value));
            for local_idx := 0 to Min(High(left_units), High(right_units)) do
            begin
                if not SameText(left_units[local_idx], right_units[local_idx]) then
                begin
                    Break;
                end;
                Inc(Result);
            end;
        end;

        function get_suffix_text_by_unit_count_local(const value: string;
            const prefix_units: Integer): string;
        var
            value_units: TArray<string>;
            local_idx: Integer;
        begin
            Result := '';
            value_units := split_text_units(Trim(value));
            if (prefix_units < 0) or (prefix_units > Length(value_units)) then
            begin
                Exit;
            end;
            for local_idx := prefix_units to High(value_units) do
            begin
                Result := Result + value_units[local_idx];
            end;
        end;

        function build_suffix_query_key_local(const prefix_units: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (prefix_units < 0) or (prefix_units >= Length(effective_syllables)) then
            begin
                Exit;
            end;
            for local_idx := prefix_units to High(effective_syllables) do
            begin
                Result := Result +
                    normalize_pinyin_text(effective_syllables[local_idx].text);
            end;
        end;

        function is_exact_tail_completion_local(const suffix_key: string;
            const suffix_value: string): Boolean;
        var
            tail_results: TncCandidateList;
            tail_idx: Integer;
        begin
            Result := False;
            if (suffix_key = '') or (suffix_value = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;
            if not m_dictionary.lookup_exact_full_pinyin(suffix_key, tail_results) then
            begin
                Exit;
            end;
            for tail_idx := 0 to High(tail_results) do
            begin
                if Trim(tail_results[tail_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if SameText(Trim(tail_results[tail_idx].text), suffix_value) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;
        if not is_full_pinyin_key(lookup_text) then
        begin
            Exit;
        end;
        if Trim(candidates[0].comment) = '' then
        begin
            Exit;
        end;
        if not is_full_pinyin_key(normalize_pinyin_text(candidates[0].comment)) then
        begin
            Exit;
        end;

        top_text := Trim(candidates[0].text);
        if top_text = '' then
        begin
            Exit;
        end;
        top_units := get_candidate_text_unit_count(top_text);
        if top_units < 2 then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(effective_syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        best_shared_prefix_units := -1;
        for idx := 1 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text := Trim(candidates[idx].text);
            if get_candidate_text_unit_count(candidate_text) <> input_syllable_count then
            begin
                Continue;
            end;

            shared_prefix_units := get_shared_prefix_unit_count_local(top_text,
                candidate_text);
            if shared_prefix_units < Max(2, top_units - 1) then
            begin
                Continue;
            end;

            if shared_prefix_units < top_units then
            begin
                suffix_text := get_suffix_text_by_unit_count_local(candidate_text,
                    shared_prefix_units);
                if not is_exact_tail_completion_local(
                    build_suffix_query_key_local(shared_prefix_units), suffix_text) then
                begin
                    Continue;
                end;
            end
            else if not is_text_prefix_local(top_text, candidate_text) then
            begin
                Continue;
            end;

            if (shared_prefix_units > best_shared_prefix_units) or
                ((shared_prefix_units = best_shared_prefix_units) and
                (candidates[idx].score > best_score)) or
                ((shared_prefix_units = best_shared_prefix_units) and
                (candidates[idx].score = best_score) and (best_index < 0)) then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
                best_shared_prefix_units := shared_prefix_units;
            end;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := picked;
    end;

    procedure ensure_exact_backed_same_comment_partial_visible(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
    var
        top_text: string;
        top_comment: string;
        top_units: Integer;
        idx: Integer;
        candidate_text: string;
        candidate_units: Integer;
        top_units_list: TArray<string>;
        candidate_units_list: TArray<string>;
        shared_prefix_units: Integer;
        best_index: Integer;
        best_dict_weight: Integer;
        picked: TncCandidate;
        unit_idx: Integer;
    begin
        if Length(candidates) <= 1 then
        begin
            Exit;
        end;
        if is_full_pinyin_key(lookup_text) then
        begin
            Exit;
        end;
        top_comment := Trim(candidates[0].comment);
        if top_comment = '' then
        begin
            Exit;
        end;
        top_text := Trim(candidates[0].text);
        top_units := get_candidate_text_unit_count(top_text);
        if top_units < 2 then
        begin
            Exit;
        end;
        top_units_list := split_text_units(top_text);
        if Length(top_units_list) <> top_units then
        begin
            Exit;
        end;

        best_index := -1;
        best_dict_weight := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(candidates)) do
        begin
            if not SameText(Trim(candidates[idx].comment), top_comment) then
            begin
                Continue;
            end;
            if not candidates[idx].has_dict_weight then
            begin
                Continue;
            end;

            candidate_text := Trim(candidates[idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <> top_units then
            begin
                Continue;
            end;
            candidate_units_list := split_text_units(candidate_text);
            if Length(candidate_units_list) <> top_units then
            begin
                Continue;
            end;

            shared_prefix_units := 0;
            for unit_idx := 0 to top_units - 1 do
            begin
                if not SameText(top_units_list[unit_idx],
                    candidate_units_list[unit_idx]) then
                begin
                    Break;
                end;
                Inc(shared_prefix_units);
            end;
            if shared_prefix_units < Max(1, top_units - 1) then
            begin
                Continue;
            end;
            if candidates[idx].dict_weight <= best_dict_weight then
            begin
                Continue;
            end;

            best_index := idx;
            best_dict_weight := candidates[idx].dict_weight;
        end;

        if best_index <= 0 then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        while best_index > 0 do
        begin
            candidates[best_index] := candidates[best_index - 1];
            Dec(best_index);
        end;
        candidates[0] := picked;
    end;

    function is_single_text_unit(const value: string): Boolean;
    begin
        if Length(value) = 1 then
        begin
            Result := True;
            Exit;
        end;

        Result := (Length(value) = 2) and
            (Ord(value[1]) >= $D800) and (Ord(value[1]) <= $DBFF) and
            (Ord(value[2]) >= $DC00) and (Ord(value[2]) <= $DFFF);
    end;

    function try_get_single_text_unit_codepoint(const value: string; out codepoint: Integer): Boolean;
    begin
        Result := False;
        codepoint := 0;
        if Length(value) = 1 then
        begin
            codepoint := Ord(value[1]);
            Result := True;
            Exit;
        end;

        if (Length(value) = 2) and
            (Ord(value[1]) >= $D800) and (Ord(value[1]) <= $DBFF) and
            (Ord(value[2]) >= $DC00) and (Ord(value[2]) <= $DFFF) then
        begin
            codepoint := ((Ord(value[1]) - $D800) shl 10) + (Ord(value[2]) - $DC00) + $10000;
            Result := True;
        end;
    end;

    function dictionary_lookup_cached(const pinyin_key: string; out out_results: TncCandidateList): Boolean;
    var
        build_cache_key: string;
        phase_start_tick: UInt64;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        if lookup_cache.TryGetValue(pinyin_key, out_results) then
        begin
            Inc(lookup_cache_hits);
            Exit(Length(out_results) > 0);
        end;

        build_cache_key := '#lookup#' + pinyin_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(build_cache_key, out_results) then
        begin
            Inc(lookup_cache_hits);
            lookup_cache.AddOrSetValue(pinyin_key, out_results);
            Exit(Length(out_results) > 0);
        end;

        Inc(lookup_cache_misses);
        if m_config.debug_mode and
            (Length(m_last_full_path_debug_info) < 768) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' lmiss=' + pinyin_key;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        if not m_dictionary.lookup(pinyin_key, out_results) then
        begin
            SetLength(out_results, 0);
        end;
        Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
        lookup_cache.AddOrSetValue(pinyin_key, out_results);
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(build_cache_key, out_results);
        end;
        Result := Length(out_results) > 0;
    end;

    function dictionary_exact_lookup_cached(const pinyin_key: string;
        out out_results: TncCandidateList): Boolean;
    begin
        Result := lookup_exact_full_pinyin_cached_local(pinyin_key,
            out_results);
    end;

    function dictionary_fuzzy_lattice_lookup_cached(const pinyin_key: string;
        out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') or
            (not m_config.fuzzy_pinyin_enabled) or
            (m_config.fuzzy_pinyin_rules = []) then
        begin
            Exit(False);
        end;

        cache_key := '#fuzzy-lattice#' + pinyin_key;
        if lookup_cache.TryGetValue(cache_key, out_results) then
        begin
            Exit(Length(out_results) > 0);
        end;
        if not m_dictionary.lookup_fuzzy_full_pinyin_bounded(pinyin_key,
            out_results, 1, 6, 4, 16) then
        begin
            SetLength(out_results, 0);
        end;
        lookup_cache.AddOrSetValue(cache_key, out_results);
        Result := Length(out_results) > 0;
    end;

    function try_build_explicit_apostrophe_lookup_seed_local(
        out out_results: TncCandidateList): Boolean;
    begin
        SetLength(out_results, 0);
        if (not has_explicit_apostrophe_input) or
            (not has_multi_syllable_input) or (input_syllable_count <= 1) or
            (explicit_apostrophe_lookup_text = '') then
        begin
            Exit(False);
        end;

        if not dictionary_lookup_cached(explicit_apostrophe_lookup_text,
            out_results) then
        begin
            Exit(False);
        end;

        filter_complete_candidates_for_explicit_apostrophe_boundary(out_results,
            out_results);
        filter_short_dictionary_hits_for_explicit_apostrophe(out_results,
            input_syllable_count);
        Result := Length(out_results) > 0;
    end;

    function try_build_three_syllable_pronoun_one_plus_two_complete_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_tail_min_weight = 360;
        c_tail_probe_limit = 24;
        c_preferred_head_bonus = 96;
        c_complete_bonus = 120000;
        c_strong_char_lm_score = -1200;
    var
        syllables_local: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        head_results: TncCandidateList;
        tail_results: TncCandidateList;
        result_idx: Integer;
        head_text: string;
        tail_text: string;
        best_head_text: string;
        best_tail_text: string;
        head_weight: Integer;
        tail_weight: Integer;
        best_head_weight: Integer;
        best_tail_weight: Integer;
        is_preferred_head: Boolean;
        combined_text: string;
        char_lm_texts: TArray<string>;
        char_lm_scores: TArray<Integer>;
        char_lm_supported: Boolean;

        function get_candidate_weight_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function is_one_plus_two_head_text_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(text_value[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
                $5BF9, $4ECE, $4E8E, $548C, $4E0E,
                $7B2C, $6211, $4F60, $60A8, $5148:
                    Result := True;
            end;
        end;

        function is_direct_complete_head_text_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(text_value[1]) of
                $6211, $4F60, $60A8, $6700:
                    Result := True;
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (not has_multi_syllable_input) or
            (not is_full_pinyin_key(lookup_text)) or
            all_initial_compact_query then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> 3 then
        begin
            Exit;
        end;

        head_key := normalize_pinyin_text(syllables_local[0].text);
        tail_key := normalize_pinyin_text(syllables_local[1].text) +
            normalize_pinyin_text(syllables_local[2].text);
        if SameText(head_key, 'yi') then
        begin
            // Avoid speculative "yi + BC" assembly.  It is both noisy and
            // expensive for common continuation input.
            Exit;
        end;
        if has_strong_exact_complete_lookup_weight_local(512) then
        begin
            Exit;
        end;
        if (head_key = '') or (tail_key = '') or
            (not dictionary_exact_lookup_cached(head_key, head_results)) or
            (not dictionary_exact_lookup_cached(tail_key, tail_results)) then
        begin
            Exit;
        end;

        best_head_text := '';
        best_head_weight := Low(Integer);
        for result_idx := 0 to High(head_results) do
        begin
            if Trim(head_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            head_text := Trim(head_results[result_idx].text);
            if (head_text = '') or (not is_one_plus_two_head_text_local(head_text)) or
                (not match_single_char_candidate_for_syllable(head_key, head_text,
                is_preferred_head)) then
            begin
                Continue;
            end;

            head_weight := get_candidate_weight_local(head_results[result_idx]);
            if is_preferred_head then
            begin
                Inc(head_weight, c_preferred_head_bonus);
            end;
            if head_weight > best_head_weight then
            begin
                best_head_weight := head_weight;
                best_head_text := head_text;
            end;
        end;
        if best_head_text = '' then
        begin
            Exit;
        end;

        best_tail_text := '';
        best_tail_weight := Low(Integer);
        for result_idx := 0 to High(tail_results) do
        begin
            if result_idx >= c_tail_probe_limit then
            begin
                Break;
            end;
            if Trim(tail_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            tail_text := Trim(tail_results[result_idx].text);
            if (tail_text = '') or
                (get_candidate_text_unit_count(tail_text) <> 2) then
            begin
                Continue;
            end;

            tail_weight := get_candidate_weight_local(tail_results[result_idx]);
            if tail_weight > best_tail_weight then
            begin
                best_tail_weight := tail_weight;
                best_tail_text := tail_text;
            end;
        end;
        if (best_tail_text = '') or (best_tail_weight < c_tail_min_weight) then
        begin
            Exit;
        end;

        combined_text := best_head_text + best_tail_text;
        char_lm_supported := False;
        if not is_direct_complete_head_text_local(best_head_text) then
        begin
            SetLength(char_lm_texts, 1);
            char_lm_texts[0] := combined_text;
            if get_cached_char_lm_scores(char_lm_texts, char_lm_scores,
                clsm_full, '') and (Length(char_lm_scores) = 1) and
                (char_lm_scores[0] >= c_strong_char_lm_score) then
            begin
                char_lm_supported := True;
            end;
        end;

        out_candidate.text := combined_text;
        out_candidate.comment := '';
        out_candidate.score := c_complete_bonus + best_head_weight + best_tail_weight;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;
        if char_lm_supported then
        begin
            out_candidate.display_kind := cdk_lm_compound;
        end;
        out_encoded_path := best_head_text + c_segment_path_separator +
            best_tail_text;
        remember_segment_path_for_candidate(out_candidate.text, '',
            out_encoded_path, out_candidate.score);
        Result := True;
    end;

    function try_build_three_syllable_two_plus_one_complete_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_head_min_weight = 512;
        c_tail_probe_limit = 24;
        c_preferred_tail_bonus = 96;
        c_complete_bonus = 120000;
    var
        syllables_local: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        head_results: TncCandidateList;
        tail_results: TncCandidateList;
        result_idx: Integer;
        head_text: string;
        tail_text: string;
        best_head_text: string;
        best_tail_text: string;
        head_weight: Integer;
        tail_weight: Integer;
        best_head_weight: Integer;
        best_tail_weight: Integer;
        is_preferred_tail: Boolean;

        function get_candidate_weight_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function is_ordinal_head_text_local(const text_value: string): Boolean;
        var
            units_local: TArray<string>;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <> 2 then
            begin
                Exit;
            end;
            Result := SameText(units_local[0], string(Char($7B2C)));
        end;

        function is_quantity_tail_text_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $9879, $6761, $4E2A, $70B9, $79CD, $7C7B, $6B21, $6B65,
                $8F6E, $7AE0, $8282, $9875, $6BB5, $884C, $5217, $5C42:
                    Result := True;
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (not has_multi_syllable_input) or
            (input_syllable_count <> 3) or (not is_full_pinyin_key(lookup_text)) or
            all_initial_compact_query or
            (has_internal_dangling_initial and
            (m_last_lookup_normalized_from = '')) then
        begin
            Exit;
        end;
        if has_strong_exact_complete_lookup_weight_local(512) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> 3 then
        begin
            Exit;
        end;

        head_key := normalize_pinyin_text(syllables_local[0].text) +
            normalize_pinyin_text(syllables_local[1].text);
        tail_key := normalize_pinyin_text(syllables_local[2].text);
        if (head_key = '') or (tail_key = '') or
            (not dictionary_exact_lookup_cached(head_key, head_results)) or
            (not dictionary_exact_lookup_cached(tail_key, tail_results)) then
        begin
            Exit;
        end;

        best_head_text := '';
        best_head_weight := Low(Integer);
        for result_idx := 0 to High(head_results) do
        begin
            if Trim(head_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            head_text := Trim(head_results[result_idx].text);
            if (head_text = '') or
                (get_candidate_text_unit_count(head_text) <> 2) then
            begin
                Continue;
            end;

            head_weight := get_candidate_weight_local(head_results[result_idx]);
            if is_ordinal_head_text_local(head_text) then
            begin
                Inc(head_weight, 4096);
            end;
            if head_weight > best_head_weight then
            begin
                best_head_weight := head_weight;
                best_head_text := head_text;
            end;
        end;
        if (best_head_text = '') or (best_head_weight < c_head_min_weight) then
        begin
            Exit;
        end;

        best_tail_text := '';
        best_tail_weight := Low(Integer);
        for result_idx := 0 to High(tail_results) do
        begin
            if result_idx >= c_tail_probe_limit then
            begin
                Break;
            end;
            if Trim(tail_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            tail_text := Trim(tail_results[result_idx].text);
            if (tail_text = '') or
                (get_candidate_text_unit_count(tail_text) <> 1) or
                (not match_single_char_candidate_for_syllable(tail_key, tail_text,
                is_preferred_tail)) then
            begin
                Continue;
            end;

            tail_weight := get_candidate_weight_local(tail_results[result_idx]);
            if is_preferred_tail then
            begin
                Inc(tail_weight, c_preferred_tail_bonus);
            end;
            if (normalize_pinyin_text(tail_key) = 'de') and
                SameText(tail_text, string(Char($7684))) then
            begin
                Inc(tail_weight, 4096);
            end;
            if is_ordinal_head_text_local(best_head_text) and
                is_quantity_tail_text_local(tail_text) then
            begin
                Inc(tail_weight, 4096);
            end;
            if tail_weight > best_tail_weight then
            begin
                best_tail_weight := tail_weight;
                best_tail_text := tail_text;
            end;
        end;
        if best_tail_text = '' then
        begin
            Exit;
        end;

        out_candidate.text := best_head_text + best_tail_text;
        out_candidate.comment := '';
        out_candidate.score := c_complete_bonus + best_head_weight + best_tail_weight;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;
        out_encoded_path := best_head_text + c_segment_path_separator + best_tail_text;
        remember_segment_path_for_candidate(out_candidate.text, '',
            out_encoded_path, out_candidate.score);
        Result := True;
    end;

    function try_build_three_syllable_two_plus_one_prefix_continuation_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_head_min_weight = 512;
        c_tail_min_weight = 360;
        c_tail_probe_limit = 24;
        c_preferred_tail_bonus = 96;
        c_complete_bonus = 300000;
    var
        syllables_local: TncPinyinParseResult;
        head_key: string;
        third_key: string;
        tail_key: string;
        head_results: TncCandidateList;
        third_results: TncCandidateList;
        tail_results: TncCandidateList;
        result_idx: Integer;
        syllable_idx: Integer;
        first_key: string;
        remainder_key: string;
        head_text: string;
        third_text: string;
        tail_text: string;
        remainder_text: string;
        remainder_path: string;
        remainder_weight: Integer;
        best_head_text: string;
        best_third_text: string;
        best_tail_text: string;
        head_weight: Integer;
        third_weight: Integer;
        tail_weight: Integer;
        remainder_segment_count: Integer;
        best_head_weight: Integer;
        best_third_weight: Integer;
        best_tail_weight: Integer;
        tail_units: Integer;
        is_preferred_tail: Boolean;
        remainder_memo_state: TArray<Byte>;
        remainder_memo_text: TArray<string>;
        remainder_memo_path: TArray<string>;
        remainder_memo_weight: TArray<Integer>;
        remainder_memo_segments: TArray<Integer>;
        remainder_memo_first_units: TArray<Integer>;
        function_head_candidate: TncCandidate;
        function_head_path: string;
        has_function_head_candidate: Boolean;

        function restore_function_head_candidate_local: Boolean;
        begin
            Result := has_function_head_candidate;
            if Result then
            begin
                out_candidate := function_head_candidate;
                out_encoded_path := function_head_path;
            end;
        end;

        function get_candidate_weight_local(
            const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            if candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function has_exact_combined_tail_phrase_local: Boolean;
        var
            combined_tail_key_local: string;
            combined_tail_results_local: TncCandidateList;
            combined_tail_idx_local: Integer;
            combined_tail_text_local: string;
            combined_tail_weight_local: Integer;
        begin
            Result := False;
            combined_tail_key_local := third_key + tail_key;
            if (combined_tail_key_local = '') or
                (not dictionary_exact_lookup_cached(combined_tail_key_local,
                combined_tail_results_local)) then
            begin
                Exit;
            end;

            for combined_tail_idx_local := 0 to High(combined_tail_results_local) do
            begin
                if Trim(combined_tail_results_local[combined_tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                combined_tail_text_local := Trim(
                    combined_tail_results_local[combined_tail_idx_local].text);
                if (combined_tail_text_local = '') or
                    (get_candidate_text_unit_count(combined_tail_text_local) <>
                    tail_units + 1) then
                begin
                    Continue;
                end;

                combined_tail_weight_local :=
                    combined_tail_results_local[combined_tail_idx_local].score;
                if combined_tail_results_local[combined_tail_idx_local].has_dict_weight and
                    (combined_tail_results_local[combined_tail_idx_local].dict_weight >
                    combined_tail_weight_local) then
                begin
                    combined_tail_weight_local :=
                        combined_tail_results_local[combined_tail_idx_local].dict_weight;
                end;
                if combined_tail_weight_local >= c_tail_min_weight then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_function_head_text_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
                $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F, $7684, $4E86,
                $7B2C, $6211, $4F60, $60A8, $5148:
                    Result := True;
            end;
        end;

        function is_structural_particle_key_local(const value: string): Boolean;
        var
            normalized_value: string;
        begin
            normalized_value := normalize_pinyin_text(value);
            Result := (normalized_value = 'de') or (normalized_value = 'le') or
                (normalized_value = 'la') or (normalized_value = 'ba') or
                (normalized_value = 'ma');
        end;

        function is_structural_connector_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $548C, $4E0E, $8DDF, $540C, $5728:
                    Result := True;
            end;
        end;

        function is_nonbase_two_plus_single_head_local(
            const local_head_text: string; const local_third_text: string):
            Boolean;
        var
            combined_key_local: string;
            combined_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (local_head_text = '') or
                (local_third_text = '') or is_structural_particle_key_local(
                third_key) or is_structural_connector_text_local(
                local_third_text) then
            begin
                Exit;
            end;

            combined_key_local := head_key + third_key;
            combined_text_local := local_head_text + local_third_text;
            if get_candidate_text_unit_count(combined_text_local) <> 3 then
            begin
                Exit;
            end;

            Result := (not m_dictionary.is_base_entry(combined_key_local,
                combined_text_local)) and
                (not m_dictionary.is_user_entry(combined_key_local,
                combined_text_local));
        end;

        function build_syllable_key_range_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            range_idx_local: Integer;
        begin
            Result := '';
            if (count_local <= 0) or (start_idx_local < 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;

            for range_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[range_idx_local].text);
            end;
        end;

        function try_get_best_exact_segment_local(const start_idx_local: Integer;
            const count_local: Integer; out out_text_local: string;
            out out_weight_local: Integer): Boolean;
        var
            segment_key_local: string;
            segment_results_local: TncCandidateList;
            segment_idx_local: Integer;
            segment_text_local: string;
            segment_weight_local: Integer;
        begin
            Result := False;
            out_text_local := '';
            out_weight_local := Low(Integer);
            if count_local < 2 then
            begin
                Exit;
            end;

            segment_key_local := build_syllable_key_range_local(start_idx_local,
                count_local);
            if (segment_key_local = '') or
                (not dictionary_exact_lookup_cached(segment_key_local,
                segment_results_local)) then
            begin
                Exit;
            end;

            for segment_idx_local := 0 to High(segment_results_local) do
            begin
                if segment_idx_local >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(segment_results_local[segment_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                segment_text_local := Trim(
                    segment_results_local[segment_idx_local].text);
                if (segment_text_local = '') or
                    (get_candidate_text_unit_count(segment_text_local) <>
                    count_local) then
                begin
                    Continue;
                end;

                segment_weight_local := get_candidate_weight_local(
                    segment_results_local[segment_idx_local]);
                if segment_weight_local > out_weight_local then
                begin
                    out_weight_local := segment_weight_local;
                    out_text_local := segment_text_local;
                    Result := True;
                end;
            end;
        end;

        function try_build_remainder_exact_chain_local(out out_text_local: string;
            out out_path_local: string; out out_weight_local: Integer): Boolean;
        var
            segment_count_local: Integer;
            first_segment_units_local: Integer;

            function is_structural_particle_remainder_text_local(
                const value_local: string): Boolean;
            begin
                Result := (value_local = string(Char($7684))) or
                    (value_local = string(Char($5F97))) or
                    (value_local = string(Char($5730)));
            end;

            function try_get_single_remainder_segment_local(
                const start_idx_local: Integer; out out_text: string;
                out out_weight: Integer): Boolean;
            var
                single_key_local: string;
                single_results_local: TncCandidateList;
                single_idx_local: Integer;
                single_text_local: string;
                single_weight_local: Integer;

                function is_copula_tail_text_local(
                    const key_value: string; const text_value: string): Boolean;
                begin
                    Result := SameText(normalize_pinyin_text(key_value), 'shi') and
                        SameText(text_value, string(Char($662F)));
                end;
            begin
                Result := False;
                out_text := '';
                out_weight := Low(Integer);
                if (start_idx_local < 0) or
                    (start_idx_local >= Length(syllables_local)) then
                begin
                    Exit;
                end;

                single_key_local := normalize_pinyin_text(
                    syllables_local[start_idx_local].text);
                if not dictionary_exact_lookup_cached(single_key_local,
                    single_results_local) then
                begin
                    Exit;
                end;

                for single_idx_local := 0 to High(single_results_local) do
                begin
                    if single_idx_local >= c_tail_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(single_results_local[single_idx_local].comment) <>
                        '' then
                    begin
                        Continue;
                    end;
                    single_text_local := Trim(
                        single_results_local[single_idx_local].text);
                    if (single_text_local = '') or
                        (get_candidate_text_unit_count(single_text_local) <> 1) then
                    begin
                        Continue;
                    end;

                    single_weight_local := get_candidate_weight_local(
                        single_results_local[single_idx_local]);
                    if is_structural_particle_remainder_text_local(
                        single_text_local) then
                    begin
                        Inc(single_weight_local, 1024);
                    end;
                    if is_copula_tail_text_local(single_key_local,
                        single_text_local) then
                    begin
                        Inc(single_weight_local, 4096);
                    end;
                    if (not Result) or (single_weight_local > out_weight) then
                    begin
                        Result := True;
                        out_text := single_text_local;
                        out_weight := single_weight_local;
                    end;
                end;
            end;

            function try_build_remainder_from_pos_local(
                const pos_value_local: Integer; out out_text_value_local: string;
                out out_path_value_local: string; out out_weight_value_local: Integer;
                out out_segment_count_local: Integer;
                out out_first_segment_units_local: Integer): Boolean;
            var
                local_span: Integer;
                local_remaining: Integer;
                local_segment_text: string;
                local_segment_weight: Integer;
                local_suffix_text: string;
                local_suffix_path: string;
                local_suffix_weight: Integer;
                local_suffix_segments: Integer;
                local_suffix_first_units: Integer;
                local_candidate_text: string;
                local_candidate_path: string;
                local_candidate_weight: Integer;
                local_candidate_segments: Integer;
                local_candidate_first_units: Integer;
                local_fixed_text: string;
                local_fixed_weight: Integer;

                procedure consider_candidate_local(const text_value_local: string;
                    const path_value_local: string; const weight_value_local: Integer;
                    const segments_value_local: Integer;
                    const first_units_value_local: Integer);
                begin
                    if (text_value_local = '') or (path_value_local = '') or
                        (segments_value_local <= 0) then
                    begin
                        Exit;
                    end;
                    if (not Result) or
                        (weight_value_local > out_weight_value_local) then
                    begin
                        Result := True;
                        out_text_value_local := text_value_local;
                        out_path_value_local := path_value_local;
                        out_weight_value_local := weight_value_local;
                        out_segment_count_local := segments_value_local;
                        out_first_segment_units_local := first_units_value_local;
                    end;
                end;
            begin
                Result := False;
                out_text_value_local := '';
                out_path_value_local := '';
                out_weight_value_local := Low(Integer);
                out_segment_count_local := 0;
                out_first_segment_units_local := 0;

                if (pos_value_local >= 0) and
                    (pos_value_local < Length(remainder_memo_state)) and
                    (remainder_memo_state[pos_value_local] <> 0) then
                begin
                    Result := remainder_memo_state[pos_value_local] = 2;
                    if Result then
                    begin
                        out_text_value_local :=
                            remainder_memo_text[pos_value_local];
                        out_path_value_local :=
                            remainder_memo_path[pos_value_local];
                        out_weight_value_local :=
                            remainder_memo_weight[pos_value_local];
                        out_segment_count_local :=
                            remainder_memo_segments[pos_value_local];
                        out_first_segment_units_local :=
                            remainder_memo_first_units[pos_value_local];
                    end;
                    Exit;
                end;

                if pos_value_local >= Length(syllables_local) then
                begin
                    Result := True;
                    out_weight_value_local := 0;
                    remainder_memo_state[pos_value_local] := 2;
                    remainder_memo_weight[pos_value_local] := 0;
                    Exit;
                end;

                if try_get_single_remainder_segment_local(pos_value_local,
                    local_fixed_text, local_fixed_weight) and
                    ((pos_value_local = Length(syllables_local) - 1) or
                    is_structural_particle_remainder_text_local(
                    local_fixed_text)) and
                    try_build_remainder_from_pos_local(pos_value_local + 1,
                    local_suffix_text, local_suffix_path, local_suffix_weight,
                    local_suffix_segments, local_suffix_first_units) then
                begin
                    local_candidate_text := local_fixed_text + local_suffix_text;
                    local_candidate_path := local_fixed_text;
                    if local_suffix_path <> '' then
                    begin
                        local_candidate_path := local_candidate_path +
                            c_segment_path_separator + local_suffix_path;
                    end;
                    local_candidate_weight := local_fixed_weight +
                        local_suffix_weight;
                    if (pos_value_local < Length(syllables_local) - 1) and
                        (local_suffix_segments > 0) then
                    begin
                        Inc(local_candidate_weight, 4096);
                    end;
                    local_candidate_segments := 1 + local_suffix_segments;
                    local_candidate_first_units := 1;
                    consider_candidate_local(local_candidate_text,
                        local_candidate_path, local_candidate_weight,
                        local_candidate_segments, local_candidate_first_units);
                end;

                local_remaining := Length(syllables_local) - pos_value_local;
                for local_span := Min(4, local_remaining) downto 2 do
                begin
                    if not try_get_best_exact_segment_local(pos_value_local,
                        local_span, local_segment_text,
                        local_segment_weight) then
                    begin
                        Continue;
                    end;
                    if local_segment_weight < c_tail_min_weight then
                    begin
                        Continue;
                    end;
                    if not try_build_remainder_from_pos_local(
                        pos_value_local + local_span, local_suffix_text,
                        local_suffix_path, local_suffix_weight,
                        local_suffix_segments, local_suffix_first_units) then
                    begin
                        Continue;
                    end;

                    local_candidate_text := local_segment_text +
                        local_suffix_text;
                    local_candidate_path := local_segment_text;
                    if local_suffix_path <> '' then
                    begin
                        local_candidate_path := local_candidate_path +
                            c_segment_path_separator + local_suffix_path;
                    end;
                    local_candidate_weight := local_segment_weight +
                        local_suffix_weight;
                    if local_span >= 3 then
                    begin
                        Inc(local_candidate_weight, 256);
                    end;
                    local_candidate_segments := 1 + local_suffix_segments;
                    local_candidate_first_units := local_span;
                    consider_candidate_local(local_candidate_text,
                        local_candidate_path, local_candidate_weight,
                        local_candidate_segments, local_candidate_first_units);
                end;

                if Result then
                begin
                    remainder_memo_state[pos_value_local] := 2;
                    remainder_memo_text[pos_value_local] :=
                        out_text_value_local;
                    remainder_memo_path[pos_value_local] :=
                        out_path_value_local;
                    remainder_memo_weight[pos_value_local] :=
                        out_weight_value_local;
                    remainder_memo_segments[pos_value_local] :=
                        out_segment_count_local;
                    remainder_memo_first_units[pos_value_local] :=
                        out_first_segment_units_local;
                end
                else
                begin
                    remainder_memo_state[pos_value_local] := 1;
                end;
            end;
        begin
            Result := False;
            out_text_local := '';
            out_path_local := '';
            out_weight_local := 0;
            remainder_segment_count := 0;
            segment_count_local := 0;
            first_segment_units_local := 0;
            if not try_build_remainder_from_pos_local(1, out_text_local,
                out_path_local, out_weight_local, segment_count_local,
                first_segment_units_local) then
            begin
                Exit;
            end;

            if segment_count_local <= 0 then
            begin
                Exit;
            end;
            if (segment_count_local = 1) and
                (first_segment_units_local < input_syllable_count - 1) then
            begin
                Exit;
            end;
            remainder_segment_count := segment_count_local;
            Result := get_candidate_text_unit_count(out_text_local) =
                input_syllable_count - 1;
        end;

        function try_build_function_head_remainder_candidate_local: Boolean;
        var
            first_idx_local: Integer;
            first_text_local: string;
            first_weight_local: Integer;
            best_first_text_local: string;
            best_first_weight_local: Integer;
        begin
            Result := False;
            if (first_key = '') or (remainder_key = '') or
                (not dictionary_exact_lookup_cached(first_key, head_results)) then
            begin
                Exit;
            end;

            best_first_text_local := '';
            best_first_weight_local := Low(Integer);
            for first_idx_local := 0 to High(head_results) do
            begin
                if first_idx_local >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results[first_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                first_text_local := Trim(head_results[first_idx_local].text);
                if is_function_head_text_local(first_text_local) then
                begin
                    first_weight_local := get_candidate_weight_local(
                        head_results[first_idx_local]);
                    if first_weight_local > best_first_weight_local then
                    begin
                        best_first_weight_local := first_weight_local;
                        best_first_text_local := first_text_local;
                    end;
                end;
            end;
            if best_first_text_local = '' then
            begin
                Exit;
            end;

            if not try_build_remainder_exact_chain_local(remainder_text,
                remainder_path, remainder_weight) then
            begin
                Exit;
            end;

            if (input_syllable_count <= 4) and (remainder_segment_count <> 1) then
            begin
                Exit;
            end;

            out_candidate.text := best_first_text_local + remainder_text;
            out_candidate.comment := '';
            out_candidate.score := c_complete_bonus + best_first_weight_local +
                remainder_weight;
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
            out_encoded_path := best_first_text_local +
                c_segment_path_separator + remainder_path;
            Result := True;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (not has_multi_syllable_input) or
            (input_syllable_count < 4) or (input_syllable_count > 6) or
            (not is_full_pinyin_key(lookup_text)) or all_initial_compact_query or
            has_internal_dangling_initial then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(remainder_memo_state, Length(syllables_local) + 1);
        SetLength(remainder_memo_text, Length(syllables_local) + 1);
        SetLength(remainder_memo_path, Length(syllables_local) + 1);
        SetLength(remainder_memo_weight, Length(syllables_local) + 1);
        SetLength(remainder_memo_segments, Length(syllables_local) + 1);
        SetLength(remainder_memo_first_units, Length(syllables_local) + 1);

        first_key := normalize_pinyin_text(syllables_local[0].text);
        remainder_key := '';
        for syllable_idx := 1 to High(syllables_local) do
        begin
            remainder_key := remainder_key + normalize_pinyin_text(
                syllables_local[syllable_idx].text);
        end;
        has_function_head_candidate :=
            try_build_function_head_remainder_candidate_local;
        if has_function_head_candidate then
        begin
            // Keep the one-character function-head path as a fallback. A
            // longer exact head may still produce a better complete path.
            function_head_candidate := out_candidate;
            function_head_path := out_encoded_path;
        end;

        if input_syllable_count <= 4 then
        begin
            Exit(restore_function_head_candidate_local);
        end;

        head_key := normalize_pinyin_text(syllables_local[0].text) +
            normalize_pinyin_text(syllables_local[1].text);
        third_key := normalize_pinyin_text(syllables_local[2].text);
        tail_key := '';
        for syllable_idx := 3 to High(syllables_local) do
        begin
            tail_key := tail_key + normalize_pinyin_text(syllables_local[syllable_idx].text);
        end;
        tail_units := input_syllable_count - 3;
        if has_exact_combined_tail_phrase_local then
        begin
            Exit(restore_function_head_candidate_local);
        end;
        if (head_key = '') or (third_key = '') or (tail_key = '') or
            (not dictionary_exact_lookup_cached(head_key, head_results)) or
            (not dictionary_exact_lookup_cached(third_key, third_results)) or
            (not dictionary_exact_lookup_cached(tail_key, tail_results)) then
        begin
            Exit(restore_function_head_candidate_local);
        end;

        best_head_text := '';
        best_head_weight := Low(Integer);
        for result_idx := 0 to High(head_results) do
        begin
            if Trim(head_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            head_text := Trim(head_results[result_idx].text);
            if (head_text = '') or
                (get_candidate_text_unit_count(head_text) <> 2) then
            begin
                Continue;
            end;
            head_weight := get_candidate_weight_local(head_results[result_idx]);
            if head_weight > best_head_weight then
            begin
                best_head_weight := head_weight;
                best_head_text := head_text;
            end;
        end;
        if (best_head_text = '') or (best_head_weight < c_head_min_weight) then
        begin
            Exit(restore_function_head_candidate_local);
        end;

        best_third_text := '';
        best_third_weight := Low(Integer);
        for result_idx := 0 to High(third_results) do
        begin
            if result_idx >= c_tail_probe_limit then
            begin
                Break;
            end;
            if Trim(third_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            third_text := Trim(third_results[result_idx].text);
            if (third_text = '') or
                (get_candidate_text_unit_count(third_text) <> 1) or
                (not match_single_char_candidate_for_syllable(third_key, third_text,
                is_preferred_tail)) then
            begin
                Continue;
            end;
            third_weight := get_candidate_weight_local(third_results[result_idx]);
            if is_preferred_tail then
            begin
                Inc(third_weight, c_preferred_tail_bonus);
            end;
            if third_weight > best_third_weight then
            begin
                best_third_weight := third_weight;
                best_third_text := third_text;
            end;
        end;
        if best_third_text = '' then
        begin
            Exit(restore_function_head_candidate_local);
        end;
        if is_nonbase_two_plus_single_head_local(best_head_text,
            best_third_text) then
        begin
            Exit(restore_function_head_candidate_local);
        end;

        best_tail_text := '';
        best_tail_weight := Low(Integer);
        for result_idx := 0 to High(tail_results) do
        begin
            if result_idx >= c_tail_probe_limit then
            begin
                Break;
            end;
            if Trim(tail_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;
            tail_text := Trim(tail_results[result_idx].text);
            if (tail_text = '') or
                (get_candidate_text_unit_count(tail_text) <> tail_units) then
            begin
                Continue;
            end;
            tail_weight := get_candidate_weight_local(tail_results[result_idx]);
            if tail_weight > best_tail_weight then
            begin
                best_tail_weight := tail_weight;
                best_tail_text := tail_text;
            end;
        end;
        if (best_tail_text = '') or (best_tail_weight < c_tail_min_weight) then
        begin
            Exit(restore_function_head_candidate_local);
        end;

        out_candidate.text := best_head_text + best_third_text + best_tail_text;
        out_candidate.comment := '';
        out_candidate.score := c_complete_bonus + best_head_weight +
            best_third_weight + best_tail_weight;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;
        out_encoded_path := best_head_text + c_segment_path_separator +
            best_third_text + c_segment_path_separator + best_tail_text;
        if has_function_head_candidate and
            (function_head_candidate.score > out_candidate.score) then
        begin
            out_candidate := function_head_candidate;
            out_encoded_path := function_head_path;
        end;
        remember_segment_path_for_candidate(out_candidate.text, '',
            out_encoded_path, out_candidate.score);
        Result := True;
    end;

    function try_build_five_syllable_strong_exact_partition_candidate_local(
        out out_candidate: TncCandidate;
        out out_encoded_path: string): Boolean;
    const
        c_segment_probe_limit = 8;
        c_min_pair_lm_weight = 420;
        c_pair_bonus_cap = 1560;
        c_complete_bonus = 300000;
        c_pair_scale = 8;
    var
        syllables_local: TncPinyinParseResult;
        best_score_local: Int64;

        function build_key_local(const start_idx_local,
            count_local: Integer): string;
        var
            idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;
            for idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[idx_local].text);
            end;
        end;

        function collect_exact_segments_local(const start_idx_local,
            count_local: Integer; out values_local: TncCandidateList): Boolean;
        var
            key_local: string;
            raw_local: TncCandidateList;
            idx_local: Integer;
            text_local: string;
        begin
            SetLength(values_local, 0);
            key_local := build_key_local(start_idx_local, count_local);
            if (key_local = '') or
                (not dictionary_exact_lookup_cached(key_local, raw_local)) then
            begin
                Exit(False);
            end;
            for idx_local := 0 to High(raw_local) do
            begin
                if Length(values_local) >= c_segment_probe_limit then
                begin
                    Break;
                end;
                if Trim(raw_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                text_local := Trim(raw_local[idx_local].text);
                if (text_local = '') or
                    (get_candidate_text_unit_count(text_local) <> count_local) then
                begin
                    Continue;
                end;
                SetLength(values_local, Length(values_local) + 1);
                values_local[High(values_local)] := raw_local[idx_local];
            end;
            Result := Length(values_local) > 0;
        end;

        function segment_weight_local(const value_local: TncCandidate): Integer;
        begin
            Result := value_local.score;
            if value_local.has_dict_weight and
                (value_local.dict_weight > Result) then
            begin
                Result := value_local.dict_weight;
            end;
            Result := Min(Result, 4096);
        end;

        function pair_bonus_local(const evidence_local: TncPairPathEvidenceList;
            const encoded_path_local: string): Integer;
        var
            evidence_idx_local: Integer;
            lm_weight_local: Integer;
        begin
            Result := 0;
            for evidence_idx_local := 0 to High(evidence_local) do
            begin
                if not SameText(Trim(evidence_local[evidence_idx_local].encoded_path),
                    encoded_path_local) then
                begin
                    Continue;
                end;
                lm_weight_local := evidence_local[evidence_idx_local].lm_transition_weight;
                if lm_weight_local < c_min_pair_lm_weight then
                begin
                    Exit(0);
                end;
                Exit(Min(c_pair_bonus_cap, lm_weight_local * 3));
            end;
        end;

        procedure consider_partition_local(const first_units_local,
            second_units_local, third_units_local: Integer);
        var
            first_values_local: TncCandidateList;
            second_values_local: TncCandidateList;
            third_values_local: TncCandidateList;
            first_idx_local: Integer;
            second_idx_local: Integer;
            third_idx_local: Integer;
            first_text_local: string;
            second_text_local: string;
            third_text_local: string;
            first_pair_key_local: string;
            second_pair_key_local: string;
            first_pair_evidence_local: TncPairPathEvidenceList;
            second_pair_evidence_local: TncPairPathEvidenceList;
            pair_first_local: Integer;
            pair_second_local: Integer;
            score_local: Int64;
        begin
            if (first_units_local + second_units_local +
                third_units_local <> 5) or
                (not collect_exact_segments_local(0, first_units_local,
                first_values_local)) or
                (not collect_exact_segments_local(first_units_local,
                second_units_local, second_values_local)) or
                (not collect_exact_segments_local(first_units_local +
                second_units_local, third_units_local, third_values_local)) then
            begin
                Exit;
            end;
            first_pair_key_local := build_key_local(0,
                first_units_local + second_units_local);
            second_pair_key_local := build_key_local(first_units_local,
                second_units_local + third_units_local);
            if (not m_dictionary.get_exact_pair_path_evidence(
                first_pair_key_local, first_pair_evidence_local)) or
                (not m_dictionary.get_exact_pair_path_evidence(
                second_pair_key_local, second_pair_evidence_local)) then
            begin
                Exit;
            end;

            for first_idx_local := 0 to High(first_values_local) do
            begin
                first_text_local := Trim(first_values_local[first_idx_local].text);
                for second_idx_local := 0 to High(second_values_local) do
                begin
                    second_text_local := Trim(
                        second_values_local[second_idx_local].text);
                    pair_first_local := pair_bonus_local(
                        first_pair_evidence_local, first_text_local +
                        c_segment_path_separator + second_text_local);
                    if pair_first_local <= 0 then
                    begin
                        Continue;
                    end;
                    for third_idx_local := 0 to High(third_values_local) do
                    begin
                        third_text_local := Trim(
                            third_values_local[third_idx_local].text);
                        pair_second_local := pair_bonus_local(
                            second_pair_evidence_local, second_text_local +
                            c_segment_path_separator + third_text_local);
                        if pair_second_local <= 0 then
                        begin
                            Continue;
                        end;
                        score_local := c_complete_bonus +
                            segment_weight_local(
                            first_values_local[first_idx_local]) +
                            segment_weight_local(
                            second_values_local[second_idx_local]) +
                            segment_weight_local(
                            third_values_local[third_idx_local]) +
                            (Int64(pair_first_local + pair_second_local) *
                            c_pair_scale);
                        if score_local <= best_score_local then
                        begin
                            Continue;
                        end;
                        best_score_local := score_local;
                        FillChar(out_candidate, SizeOf(out_candidate), 0);
                        out_candidate.text := first_text_local +
                            second_text_local + third_text_local;
                        out_candidate.comment := '';
                        out_candidate.score := Integer(Min(Int64(MaxInt),
                            score_local));
                        out_candidate.source := cs_rule;
                        out_candidate.display_kind := cdk_lm_compound;
                        out_candidate.has_dict_weight := True;
                        out_candidate.dict_weight := out_candidate.score;
                        out_encoded_path := first_text_local +
                            c_segment_path_separator + second_text_local +
                            c_segment_path_separator + third_text_local;
                    end;
                end;
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (input_syllable_count <> 5) or
            all_initial_compact_query or has_internal_dangling_initial or
            (not is_full_pinyin_key(lookup_text)) or
            has_strong_exact_complete_lookup_weight_local(512) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> 5 then
        begin
            Exit;
        end;
        best_score_local := Low(Int64);
        consider_partition_local(2, 2, 1);
        consider_partition_local(1, 2, 2);
        consider_partition_local(2, 1, 2);
        Result := (out_encoded_path <> '') and
            (Trim(out_candidate.text) <> '');
        if Result then
        begin
            remember_segment_path_for_candidate(out_candidate.text, '',
                out_encoded_path, out_candidate.score);
        end;
    end;

    function dictionary_prefix_lookup_cached(const pinyin_key: string;
        out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
        phase_start_tick: UInt64;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        cache_key := '#prefix#' + pinyin_key;
        if lookup_cache.TryGetValue(cache_key, out_results) then
        begin
            Inc(lookup_cache_hits);
            Exit(Length(out_results) > 0);
        end;

        Inc(lookup_cache_misses);
        if m_config.debug_mode and
            (Length(m_last_full_path_debug_info) < 768) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' pmiss=' + pinyin_key;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        if not m_dictionary.lookup_full_pinyin_prefix(pinyin_key, out_results) then
        begin
            SetLength(out_results, 0);
        end;
        Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
        lookup_cache.AddOrSetValue(cache_key, out_results);
        Result := Length(out_results) > 0;
    end;

    function build_lightweight_sentence_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_light_sentence_beam_width = 12;
        c_light_sentence_long_beam_width = 4;
        c_light_sentence_ultra_long_beam_width = 2;
        c_light_sentence_emit_limit = 8;
        c_light_sentence_long_emit_limit = 4;
        c_light_sentence_ultra_long_emit_limit = 2;
        c_light_sentence_exact_top_n = 6;
        c_light_sentence_long_exact_top_n = 2;
        c_light_sentence_ultra_long_exact_top_n = 2;
        c_light_sentence_tail_completion_top_n = 6;
        c_light_sentence_additional_segment_penalty = 120;
        c_light_sentence_single_penalty = 180;
        c_light_sentence_multi_bonus = 280;
        c_light_sentence_tail_partial_bonus = 140;
        c_light_sentence_tail_prediction_bonus = 260;
        c_light_sentence_exact_rank_step = 220;
        c_light_sentence_extended_single_min_weight = 560;
        c_light_sentence_two_single_rescue_bonus = 440;
        c_light_sentence_two_single_rescue_margin = 120;
    type
        TLightSentenceState = record
            score: Integer;
            lexical_rank_score: Integer;
            text: string;
            path: string;
            prev_text: string;
            prev_prev_text: string;
            last_segment_syllable_count: Integer;
            fuzzy_cost: Integer;
            fuzzy_rules: TncFuzzyPinyinRules;
        end;
        TLightSentenceStateArray = TArray<TLightSentenceState>;
        TLightSentenceStaticAdjustment = record
            score_delta: Integer;
            reject_candidate: Boolean;
        end;
    var
        normalized_query: string;
        syllables: TncPinyinParseResult;
        head_syllable_count: Integer;
        max_segment_len: Integer;
        tail_prefix: string;
        state_map: array of TLightSentenceStateArray;
        final_states: TLightSentenceStateArray;
        exact_sentence_subspan_cache_prepared: Boolean;
        exact_sentence_subspan_cache_ready: array of array of Boolean;
        exact_sentence_subspan_cache_weights: array of array of Integer;
        exact_sentence_subspan_cache_anchor_units: array of array of Integer;
        exact_sentence_subspan_cache_texts: array of array of string;
        syllable_key_cache_keys: TArray<string>;
        syllable_key_cache_values: TArray<string>;
        syllable_key_cache_count: Integer;
        context_bonus_cache_keys: TArray<string>;
        context_bonus_cache_values: TArray<Integer>;
        context_bonus_cache_count: Integer;
        trigram_bonus_cache_keys: TArray<string>;
        trigram_bonus_cache_values: TArray<Integer>;
        trigram_bonus_cache_count: Integer;
        static_multi_adjustment_cache:
            TDictionary<string, TLightSentenceStaticAdjustment>;

        function build_syllable_query_key(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            idx: Integer;
            cache_idx: Integer;
            cache_key: string;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local > Length(syllables)) then
            begin
                Exit;
            end;

            cache_key := IntToStr(start_idx) + ':' +
                IntToStr(syllable_count_local);
            for cache_idx := 0 to syllable_key_cache_count - 1 do
            begin
                if syllable_key_cache_keys[cache_idx] = cache_key then
                begin
                    Exit(syllable_key_cache_values[cache_idx]);
                end;
            end;

            for idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(syllables[idx].text);
            end;
            SetLength(syllable_key_cache_keys, syllable_key_cache_count + 1);
            SetLength(syllable_key_cache_values, syllable_key_cache_count + 1);
            syllable_key_cache_keys[syllable_key_cache_count] := cache_key;
            syllable_key_cache_values[syllable_key_cache_count] := Result;
            Inc(syllable_key_cache_count);
        end;

        function get_light_sentence_beam_width_local: Integer;
        begin
            if head_syllable_count >= 16 then
            begin
                Exit(c_light_sentence_ultra_long_beam_width);
            end;

            if head_syllable_count >= 10 then
            begin
                Exit(c_light_sentence_long_beam_width);
            end;

            Result := c_light_sentence_beam_width;
        end;

        function get_light_sentence_emit_limit_local: Integer;
        begin
            if head_syllable_count >= 16 then
            begin
                Exit(c_light_sentence_ultra_long_emit_limit);
            end;

            if head_syllable_count >= 10 then
            begin
                Exit(c_light_sentence_long_emit_limit);
            end;

            Result := c_light_sentence_emit_limit;
        end;

        function get_light_sentence_exact_top_n_local: Integer;
        begin
            if head_syllable_count >= 16 then
            begin
                Exit(c_light_sentence_ultra_long_exact_top_n);
            end;

            if head_syllable_count >= 10 then
            begin
                Exit(c_light_sentence_long_exact_top_n);
            end;

            Result := c_light_sentence_exact_top_n;
        end;

        procedure reserve_fuzzy_lattice_edges_local(
            var candidates: TncCandidateList);
        const
            c_max_reserved_fuzzy_edges = 1;
        var
            prefix_limit: Integer;
            reserve_limit: Integer;
            fuzzy_in_prefix: Integer;
            target_idx: Integer;
            best_idx: Integer;
            idx: Integer;
            best_score: Integer;
            candidate_tmp: TncCandidate;
        begin
            if (not m_config.fuzzy_pinyin_enabled) or
                (m_config.fuzzy_pinyin_rules = []) or
                (Length(candidates) = 0) then
            begin
                Exit;
            end;

            prefix_limit := Min(get_light_sentence_exact_top_n_local,
                Length(candidates));
            if prefix_limit <= 0 then
            begin
                Exit;
            end;
            reserve_limit := Min(c_max_reserved_fuzzy_edges,
                Max(1, prefix_limit div 2));

            fuzzy_in_prefix := 0;
            for idx := 0 to prefix_limit - 1 do
            begin
                if candidates[idx].fuzzy_cost > 0 then
                begin
                    Inc(fuzzy_in_prefix);
                end;
            end;

            while fuzzy_in_prefix < reserve_limit do
            begin
                best_idx := -1;
                best_score := Low(Integer);
                for idx := prefix_limit to High(candidates) do
                begin
                    if (candidates[idx].fuzzy_cost > 0) and
                        (candidates[idx].score > best_score) then
                    begin
                        best_idx := idx;
                        best_score := candidates[idx].score;
                    end;
                end;
                if best_idx < 0 then
                begin
                    Break;
                end;

                target_idx := prefix_limit - 1;
                while (target_idx >= 0) and
                    (candidates[target_idx].fuzzy_cost > 0) do
                begin
                    Dec(target_idx);
                end;
                if target_idx < 0 then
                begin
                    Break;
                end;

                candidate_tmp := candidates[target_idx];
                candidates[target_idx] := candidates[best_idx];
                candidates[best_idx] := candidate_tmp;
                Inc(fuzzy_in_prefix);
            end;
        end;

        function get_sentence_context_bonus_scaled(const left_text: string;
            const right_text: string): Integer;
        var
            raw_bonus: Integer;
            cache_idx: Integer;
            cache_key: string;
        begin
            Result := 0;
            if (m_dictionary = nil) or (left_text = '') or (right_text = '') then
            begin
                Exit;
            end;

            cache_key := left_text + #9 + right_text;
            for cache_idx := 0 to context_bonus_cache_count - 1 do
            begin
                if context_bonus_cache_keys[cache_idx] = cache_key then
                begin
                    Exit(context_bonus_cache_values[cache_idx]);
                end;
            end;

            raw_bonus := m_dictionary.get_context_bonus(left_text, right_text);
            if raw_bonus > 0 then
            begin
                Result := raw_bonus * 4;
            end
            else
            begin
                Result := raw_bonus;
            end;
            SetLength(context_bonus_cache_keys, context_bonus_cache_count + 1);
            SetLength(context_bonus_cache_values, context_bonus_cache_count + 1);
            context_bonus_cache_keys[context_bonus_cache_count] := cache_key;
            context_bonus_cache_values[context_bonus_cache_count] := Result;
            Inc(context_bonus_cache_count);
        end;

        function get_sentence_trigram_bonus_scaled(const left_left_text: string;
            const left_text: string; const right_text: string): Integer;
        var
            raw_bonus: Integer;
            cache_idx: Integer;
            cache_key: string;
        begin
            Result := 0;
            if (m_dictionary = nil) or (left_left_text = '') or (left_text = '') or
                (right_text = '') then
            begin
                Exit;
            end;

            cache_key := left_left_text + #9 + left_text + #9 + right_text;
            for cache_idx := 0 to trigram_bonus_cache_count - 1 do
            begin
                if trigram_bonus_cache_keys[cache_idx] = cache_key then
                begin
                    Exit(trigram_bonus_cache_values[cache_idx]);
                end;
            end;

            raw_bonus := m_dictionary.get_context_trigram_bonus(left_left_text,
                left_text, right_text);
            if raw_bonus > 0 then
            begin
                Result := raw_bonus * 6;
            end
            else
            begin
                Result := raw_bonus;
            end;
            SetLength(trigram_bonus_cache_keys, trigram_bonus_cache_count + 1);
            SetLength(trigram_bonus_cache_values, trigram_bonus_cache_count + 1);
            trigram_bonus_cache_keys[trigram_bonus_cache_count] := cache_key;
            trigram_bonus_cache_values[trigram_bonus_cache_count] := Result;
            Inc(trigram_bonus_cache_count);
        end;

        function is_allowed_single_char_for_sentence(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
                $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $53EA, $628A, $88AB,
                $8BA9, $7ED9, $5411, $5BF9, $8DDF, $540C, $4ECE, $4E0E, $5C31, $4E5F,
                $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2, $5C06,
                $5230, $7684, $5730, $4E86, $7740, $8FC7, $6240, $4E0A, $4E0B,
                $4E00, $4E14, $5F97, $4E3A, $4E8E, $4E2A, $4EE5, $5176, $7528,
                $5404, $4E2D, $524D, $540E, $5185, $5916, $91CC, $8BE5, $6B64,
                $6BCF:
                    Result := True;
            end;
        end;

        function is_function_or_modifier_single_for_sentence(
            const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168, $8D85,
                $771F, $597D, $5982, $633A, $8F83, $6781, $800C, $5E76,
                $6240, $4E0A, $4E0B, $4E00, $4E14, $5730, $5F97, $4E3A,
                $4E2A, $4EE5, $5176, $7528, $5404, $4E2D, $524D, $540E,
                $5185, $5916, $91CC, $8BE5, $6B64, $6BCF:
                    Result := True;
            end;
        end;

        function is_ambiguous_sentence_function_syllable(
            const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'de') or
                (normalized_syllable = 'ta') or
                (normalized_syllable = 'wei') or
                (normalized_syllable = 'yu') or
                (normalized_syllable = 'yong') or
                (normalized_syllable = 'qi') or
                (normalized_syllable = 'shi') or
                (normalized_syllable = 'zhe') or
                (normalized_syllable = 'ge') or
                (normalized_syllable = 'na') or
                (normalized_syllable = 'mei') or
                (normalized_syllable = 'zai') or
                (normalized_syllable = 'you');
        end;

        function get_fixed_sentence_single_char_for_filter(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'ai' then
            begin
                Result := string(Char($5509));
            end;
        end;

        function is_preferred_sentence_single_char_candidate_local(
            const syllable_key: string; const value: string;
            const effective_weight: Integer): Boolean;
        const
            c_sentence_single_char_probe_limit = 8;
            c_sentence_single_char_preferred_max_rank = 4;
            c_sentence_single_char_top_ratio_pct = 85;
        var
            exact_single_results: TncCandidateList;
            idx: Integer;
            candidate_text: string;
            weight_value: Integer;
            top_weight_value: Integer;
            single_char_rank: Integer;
            fixed_text: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (syllable_key = '') or (value = '') or
                (Length(Trim(value)) <> 1) then
            begin
                Exit;
            end;

            fixed_text := get_fixed_sentence_single_char_for_filter(syllable_key);
            if fixed_text <> '' then
            begin
                if SameText(fixed_text, value) then
                begin
                    Exit(True);
                end;
                if not is_ambiguous_sentence_function_syllable(syllable_key) then
                begin
                    Exit(False);
                end;
            end;

            if not m_dictionary.single_char_matches_pinyin(syllable_key, value) then
            begin
                Exit(False);
            end;

            if not dictionary_exact_lookup_cached(syllable_key, exact_single_results) then
            begin
                Exit(True);
            end;

            top_weight_value := 0;
            single_char_rank := 0;
            for idx := 0 to High(exact_single_results) do
            begin
                if idx >= c_sentence_single_char_probe_limit then
                begin
                    Break;
                end;

                candidate_text := Trim(exact_single_results[idx].text);
                if (candidate_text = '') or (exact_single_results[idx].comment <> '') or
                    (get_candidate_text_unit_count(candidate_text) <> 1) then
                begin
                    Continue;
                end;

                Inc(single_char_rank);
                if exact_single_results[idx].has_dict_weight then
                begin
                    weight_value := exact_single_results[idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_single_results[idx].score;
                end;

                if top_weight_value <= 0 then
                begin
                    top_weight_value := weight_value;
                end;

                if not SameText(candidate_text, value) then
                begin
                    Continue;
                end;

                Exit((single_char_rank <= c_sentence_single_char_preferred_max_rank) or
                    (top_weight_value <= 0) or
                    ((effective_weight * 100) >=
                    (top_weight_value * c_sentence_single_char_top_ratio_pct)));
            end;
        end;

        function is_extended_allowed_single_char_for_sentence(
            const syllable_key: string; const value: string;
            const effective_weight: Integer): Boolean;
        var
            fixed_text: string;
        begin
            fixed_text := get_fixed_sentence_single_char_for_filter(syllable_key);
            if fixed_text <> '' then
            begin
                if SameText(fixed_text, value) then
                begin
                    Exit(True);
                end;
                if not is_ambiguous_sentence_function_syllable(syllable_key) then
                begin
                    Exit(False);
                end;
            end;

            Result := is_allowed_single_char_for_sentence(value);
            if Result then
            begin
                Exit;
            end;

            if head_syllable_count < c_long_sentence_full_path_min_syllables then
            begin
                Exit(False);
            end;
            if (Length(Trim(value)) <> 1) or
                (effective_weight < c_light_sentence_extended_single_min_weight) then
            begin
                Exit(False);
            end;

            Result := is_preferred_sentence_single_char_candidate_local(
                syllable_key, value, effective_weight);
        end;

        function get_fixed_sentence_single_char(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_syllable = 'ai' then
            begin
                Result := string(Char($5509));
            end;
        end;

        function sentence_subspan_has_fixed_boundary(
            const segment_start: Integer; const segment_syllables: Integer): Boolean;
        var
            unit_idx_local: Integer;
        begin
            Result := False;
            if (segment_start < 0) or (segment_syllables <= 0) or
                (segment_start + segment_syllables > head_syllable_count) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to segment_syllables - 1 do
            begin
                if get_fixed_sentence_single_char(
                    normalize_pinyin_text(
                    syllables[segment_start + unit_idx_local].text)) <> '' then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_fixed_boundary_alignment_adjustment(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string): Integer;
        const
            c_match_bonus = 6000;
            c_full_match_bonus = 6000;
            c_mismatch_penalty = 24000;
            c_edge_mismatch_penalty = 8000;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            expected_text_local: string;
            syllable_key_local: string;
            checked_count_local: Integer;
            match_count_local: Integer;
        begin
            Result := 0;
            if (segment_start < 0) or (segment_syllables <= 0) or
                (segment_start + segment_syllables > head_syllable_count) or
                (candidate_text = '') then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_text));
            if Length(text_units_local) <> segment_syllables then
            begin
                Exit;
            end;

            checked_count_local := 0;
            match_count_local := 0;
            for unit_idx_local := 0 to segment_syllables - 1 do
            begin
                syllable_key_local := normalize_pinyin_text(
                    syllables[segment_start + unit_idx_local].text);
                expected_text_local := get_fixed_sentence_single_char(
                    syllable_key_local);
                if expected_text_local = '' then
                begin
                    Continue;
                end;

                Inc(checked_count_local);
                if SameText(text_units_local[unit_idx_local],
                    expected_text_local) then
                begin
                    Inc(match_count_local);
                    Inc(Result, c_match_bonus);
                end
                else if is_ambiguous_sentence_function_syllable(syllable_key_local) and
                    is_extended_allowed_single_char_for_sentence(syllable_key_local,
                    text_units_local[unit_idx_local], 600) then
                begin
                    { Let long-mode context rank common ambiguous particles. }
                end
                else
                begin
                    Dec(Result, c_mismatch_penalty);
                    if (unit_idx_local = 0) or
                        (unit_idx_local = segment_syllables - 1) then
                    begin
                        Dec(Result, c_edge_mismatch_penalty);
                    end;
                end;
            end;

            if (checked_count_local >= 2) and
                (match_count_local = checked_count_local) then
            begin
                Inc(Result, c_full_match_bonus);
            end;
        end;

        function try_get_best_sentence_subspan_standalone_candidate_local(
            const query_key_local: string; const expected_syllables: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        const
            c_probe_limit = 6;
        var
            cached_state_local: Integer;
            snapshot_candidates_local: TncCandidateList;
            snapshot_scores_local: TArray<Integer>;
            candidate_idx_local: Integer;
            candidate_local: TncCandidate;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_score_local: Integer;
            best_text_local: string;
            best_score_local: Integer;
            preferred_bonus_local: Integer;
            preferred_exact_bonus_local: Integer;
            preferred_text_local: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            preferred_text_local := '';
            preferred_bonus_local := 0;
            if (query_key_local = '') or
                (not is_full_pinyin_key(query_key_local)) or
                (expected_syllables < 2) or (expected_syllables > 8) or
                (m_disable_subspan_standalone_oracle > 0) then
            begin
                Exit;
            end;

            try_get_preferred_query_phrase_text_local(query_key_local,
                preferred_text_local, preferred_bonus_local);

            if subspan_oracle_states = nil then
            begin
                subspan_oracle_states := TDictionary<string, Integer>.Create;
                subspan_oracle_texts := TDictionary<string, string>.Create;
                subspan_oracle_scores := TDictionary<string, Integer>.Create;
            end;

            if subspan_oracle_states.TryGetValue(query_key_local,
                cached_state_local) then
            begin
                if cached_state_local = 1 then
                begin
                    subspan_oracle_texts.TryGetValue(query_key_local, out_text);
                    subspan_oracle_scores.TryGetValue(query_key_local, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            if subspan_oracle_budget_remaining <= 0 then
            begin
                Exit;
            end;

            if subspan_oracle_engine = nil then
            begin
                subspan_oracle_config := m_config;
                subspan_oracle_config.debug_mode := False;
                subspan_oracle_config.max_candidates := 6;
                subspan_oracle_engine := TncEngine.Create(
                    subspan_oracle_config, False, False);
                inherit_search_budget_policy_to(subspan_oracle_engine);
            end;

            Dec(subspan_oracle_budget_remaining);
            subspan_oracle_states.AddOrSetValue(query_key_local, 3);
            subspan_oracle_engine.m_disable_subspan_standalone_oracle :=
                m_disable_subspan_standalone_oracle + 1;
            subspan_oracle_engine.reset;
            subspan_oracle_engine.m_composition_text := query_key_local;
            subspan_oracle_engine.build_candidates;

            SetLength(snapshot_candidates_local,
                Length(subspan_oracle_engine.m_candidates));
            SetLength(snapshot_scores_local,
                Length(subspan_oracle_engine.m_candidates));
            for candidate_idx_local := 0 to High(subspan_oracle_engine.m_candidates) do
            begin
                snapshot_candidates_local[candidate_idx_local] :=
                    subspan_oracle_engine.m_candidates[candidate_idx_local];
                snapshot_scores_local[candidate_idx_local] :=
                    subspan_oracle_engine.get_rank_score(
                    subspan_oracle_engine.m_candidates[candidate_idx_local]);
            end;

            best_text_local := '';
            best_score_local := Low(Integer);
            for candidate_idx_local := 0 to High(snapshot_candidates_local) do
            begin
                if candidate_idx_local >= c_probe_limit then
                begin
                    Break;
                end;

                candidate_local := snapshot_candidates_local[candidate_idx_local];
                if Trim(candidate_local.comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(candidate_local.text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if candidate_units_local <> expected_syllables then
                begin
                    Continue;
                end;

                candidate_score_local := snapshot_scores_local[candidate_idx_local] +
                    get_preferred_query_phrase_bonus_local(query_key_local,
                    candidate_text_local);
                preferred_exact_bonus_local :=
                    get_preferred_exact_subspan_bonus_local(query_key_local,
                    candidate_text_local);
                if preferred_exact_bonus_local > 0 then
                begin
                    Inc(candidate_score_local,
                        preferred_exact_bonus_local + 12000);
                end;

                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_text_local := candidate_text_local;
                end;
            end;

            if best_text_local <> '' then
            begin
                out_text := best_text_local;
                out_score := best_score_local;
                subspan_oracle_states.AddOrSetValue(query_key_local, 1);
                subspan_oracle_texts.AddOrSetValue(query_key_local, out_text);
                subspan_oracle_scores.AddOrSetValue(query_key_local, out_score);
                Exit(True);
            end;

            subspan_oracle_states.AddOrSetValue(query_key_local, 2);
        end;

        function try_resolve_best_exact_sentence_subspan(
            const start_idx: Integer; const span_syllables: Integer;
            out out_text: string; out out_weight: Integer;
            out out_anchor_units: Integer): Boolean;
        const
            c_subspan_max_syllables = 5;
            c_subspan_beam_width = 6;
            c_subspan_chunk_probe_limit = 6;
            c_subspan_single_penalty = 120;
            c_subspan_additional_segment_penalty = 80;
            c_subspan_multi_bonus = 240;
            c_subspan_anchor_bonus = 420;
            c_subspan_preferred_bonus = 320;
            c_subspan_rank_step = 240;
        type
            TSubspanState = record
                score: Integer;
                weight: Integer;
                text: string;
                prev_text: string;
                anchor_units: Integer;
            end;
            TSubspanStateArray = TArray<TSubspanState>;
        var
            local_states: array of TSubspanStateArray;

            procedure add_subspan_state(var states: TSubspanStateArray;
                const candidate_state: TSubspanState);
            var
                idx_local: Integer;
                insert_idx_local: Integer;
                tmp_state_local: TSubspanState;
            begin
                for idx_local := 0 to High(states) do
                begin
                    if states[idx_local].text = candidate_state.text then
                    begin
                        if candidate_state.score > states[idx_local].score then
                        begin
                            states[idx_local] := candidate_state;
                        end;
                        Exit;
                    end;
                end;

                insert_idx_local := Length(states);
                SetLength(states, insert_idx_local + 1);
                states[insert_idx_local] := candidate_state;
                while (insert_idx_local > 0) and
                    (states[insert_idx_local].score > states[insert_idx_local - 1].score) do
                begin
                    tmp_state_local := states[insert_idx_local - 1];
                    states[insert_idx_local - 1] := states[insert_idx_local];
                    states[insert_idx_local] := tmp_state_local;
                    Dec(insert_idx_local);
                end;

                if Length(states) > c_subspan_beam_width then
                begin
                    SetLength(states, c_subspan_beam_width);
                end;
            end;

        var
            local_pos: Integer;
            state_idx_local: Integer;
            base_state_local: TSubspanState;
            next_state_local: TSubspanState;
            syllable_key_local: string;
            fixed_text_local: string;
            single_results_local: TncCandidateList;
            exact_results_local: TncCandidateList;
            lookup_results_local: TncCandidateList;
            exact_only_results_local: TncCandidateList;
            exact_idx_local: Integer;
            merged_idx_local: Integer;
            chunk_len_local: Integer;
            query_key_local: string;
            candidate_text_local: string;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            effective_weight_local: Integer;
            candidate_units_local: Integer;
            next_pos_local: Integer;
            duplicate_found_local: Boolean;
            standalone_text_local: string;
            standalone_score_local: Integer;
            standalone_candidate_local: TncCandidate;
        begin
            out_text := '';
            out_weight := 0;
            out_anchor_units := 0;
            Result := False;
            if (span_syllables <= 0) or (span_syllables > c_subspan_max_syllables) or
                (start_idx < 0) or (start_idx + span_syllables > head_syllable_count) then
            begin
                Exit;
            end;

            if not exact_sentence_subspan_cache_prepared then
            begin
                SetLength(exact_sentence_subspan_cache_ready, head_syllable_count);
                SetLength(exact_sentence_subspan_cache_weights, head_syllable_count);
                SetLength(exact_sentence_subspan_cache_anchor_units, head_syllable_count);
                SetLength(exact_sentence_subspan_cache_texts, head_syllable_count);
                for local_pos := 0 to head_syllable_count - 1 do
                begin
                    SetLength(exact_sentence_subspan_cache_ready[local_pos],
                        c_subspan_max_syllables + 1);
                    SetLength(exact_sentence_subspan_cache_weights[local_pos],
                        c_subspan_max_syllables + 1);
                    SetLength(exact_sentence_subspan_cache_anchor_units[local_pos],
                        c_subspan_max_syllables + 1);
                    SetLength(exact_sentence_subspan_cache_texts[local_pos],
                        c_subspan_max_syllables + 1);
                end;
                exact_sentence_subspan_cache_prepared := True;
            end;

            if exact_sentence_subspan_cache_ready[start_idx][span_syllables] then
            begin
                out_text := exact_sentence_subspan_cache_texts[start_idx][span_syllables];
                out_weight := exact_sentence_subspan_cache_weights[start_idx][span_syllables];
                out_anchor_units := exact_sentence_subspan_cache_anchor_units[start_idx][span_syllables];
                Exit(out_text <> '');
            end;

            SetLength(local_states, span_syllables + 1);
            SetLength(local_states[0], 1);
            local_states[0][0].score := 0;
            local_states[0][0].weight := 0;
            local_states[0][0].text := '';
            local_states[0][0].prev_text := '';
            local_states[0][0].anchor_units := 0;

            for local_pos := 0 to span_syllables - 1 do
            begin
                for state_idx_local := 0 to High(local_states[local_pos]) do
                begin
                    base_state_local := local_states[local_pos][state_idx_local];
                    syllable_key_local := normalize_pinyin_text(
                        syllables[start_idx + local_pos].text);
                    if syllable_key_local = '' then
                    begin
                        Continue;
                    end;

                    fixed_text_local := get_fixed_sentence_single_char(syllable_key_local);
                    if fixed_text_local <> '' then
                    begin
                        next_state_local.score := base_state_local.score + 960 +
                            c_subspan_anchor_bonus - c_subspan_single_penalty;
                        next_state_local.weight := base_state_local.weight + 960;
                        if base_state_local.text <> '' then
                        begin
                            Dec(next_state_local.score, c_subspan_additional_segment_penalty);
                            if m_dictionary <> nil then
                                begin
                                    Inc(next_state_local.score,
                                        get_sentence_context_bonus_scaled(
                                        base_state_local.prev_text,
                                        fixed_text_local));
                                end;
                            next_state_local.text := base_state_local.text + fixed_text_local;
                        end
                        else
                        begin
                            next_state_local.text := fixed_text_local;
                        end;
                        next_state_local.prev_text := fixed_text_local;
                        next_state_local.anchor_units := base_state_local.anchor_units + 1;
                        add_subspan_state(local_states[local_pos + 1], next_state_local);
                    end;

                    if dictionary_exact_lookup_cached(syllable_key_local, single_results_local) then
                    begin
                        for exact_idx_local := 0 to High(single_results_local) do
                        begin
                            if exact_idx_local >= c_subspan_chunk_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(single_results_local[exact_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            candidate_text_local := Trim(single_results_local[exact_idx_local].text);
                            if get_candidate_text_unit_count(candidate_text_local) <> 1 then
                            begin
                                Continue;
                            end;
                            effective_weight_local := single_results_local[exact_idx_local].score;
                            if single_results_local[exact_idx_local].has_dict_weight and
                                (single_results_local[exact_idx_local].dict_weight >
                                effective_weight_local) then
                            begin
                                effective_weight_local :=
                                    single_results_local[exact_idx_local].dict_weight;
                            end;
                            if not is_extended_allowed_single_char_for_sentence(
                                syllable_key_local, candidate_text_local, effective_weight_local) then
                            begin
                                Continue;
                            end;

                            next_state_local.score := base_state_local.score +
                                effective_weight_local - c_subspan_single_penalty;
                            next_state_local.weight := base_state_local.weight +
                                effective_weight_local;
                            if base_state_local.text <> '' then
                            begin
                                Dec(next_state_local.score, c_subspan_additional_segment_penalty);
                                if m_dictionary <> nil then
                                begin
                                    Inc(next_state_local.score,
                                        get_sentence_context_bonus_scaled(
                                        base_state_local.prev_text,
                                        candidate_text_local));
                                end;
                                next_state_local.text := base_state_local.text + candidate_text_local;
                            end
                            else
                            begin
                                next_state_local.text := candidate_text_local;
                            end;
                            next_state_local.prev_text := candidate_text_local;
                            next_state_local.anchor_units := base_state_local.anchor_units;
                            add_subspan_state(local_states[local_pos + 1], next_state_local);
                        end;
                    end;

                    for chunk_len_local := 2 to Min(c_subspan_max_syllables,
                        span_syllables - local_pos) do
                    begin
                        query_key_local := build_syllable_query_key(start_idx + local_pos,
                            chunk_len_local);
                        if query_key_local = '' then
                        begin
                            Continue;
                        end;

                        SetLength(exact_results_local, 0);
                        if dictionary_exact_lookup_cached(query_key_local,
                            exact_only_results_local) then
                        begin
                            SetLength(exact_results_local,
                                Length(exact_only_results_local));
                            for exact_idx_local := 0 to High(exact_only_results_local) do
                            begin
                                exact_results_local[exact_idx_local] :=
                                    exact_only_results_local[exact_idx_local];
                            end;
                        end;

                        if (Length(exact_results_local) = 0) and
                            (not (m_dictionary is TncSqliteDictionary)) and
                            dictionary_lookup_cached(query_key_local,
                            lookup_results_local) then
                        begin
                            for merged_idx_local := 0 to High(lookup_results_local) do
                            begin
                                duplicate_found_local := False;
                                for exact_idx_local := 0 to High(exact_results_local) do
                                begin
                                    if SameText(Trim(exact_results_local[exact_idx_local].text),
                                        Trim(lookup_results_local[merged_idx_local].text)) and
                                        SameText(Trim(exact_results_local[exact_idx_local].comment),
                                        Trim(lookup_results_local[merged_idx_local].comment)) then
                                    begin
                                        duplicate_found_local := True;
                                        Break;
                                    end;
                                end;
                                if duplicate_found_local then
                                begin
                                    Continue;
                                end;
                                SetLength(exact_results_local, Length(exact_results_local) + 1);
                                exact_results_local[High(exact_results_local)] :=
                                    lookup_results_local[merged_idx_local];
                            end;
                        end;

                        if Length(exact_results_local) = 0 then
                        begin
                            Continue;
                        end;

                        if sentence_subspan_has_fixed_boundary(
                            start_idx + local_pos, chunk_len_local) and
                            try_get_best_sentence_subspan_standalone_candidate_local(
                            query_key_local, chunk_len_local,
                            standalone_text_local, standalone_score_local) and
                            (standalone_text_local <> '') then
                        begin
                            duplicate_found_local := False;
                            for merged_idx_local := 0 to High(exact_results_local) do
                            begin
                                if SameText(
                                    Trim(exact_results_local[merged_idx_local].text),
                                    standalone_text_local) then
                                begin
                                    duplicate_found_local := True;
                                    Break;
                                end;
                            end;

                            if not duplicate_found_local then
                            begin
                                FillChar(standalone_candidate_local,
                                    SizeOf(standalone_candidate_local), 0);
                                standalone_candidate_local.text := standalone_text_local;
                                standalone_candidate_local.comment := '';
                                standalone_candidate_local.score := standalone_score_local;
                                standalone_candidate_local.source := cs_rule;
                                standalone_candidate_local.has_dict_weight := True;
                                standalone_candidate_local.dict_weight :=
                                    standalone_score_local;
                                SetLength(exact_results_local,
                                    Length(exact_results_local) + 1);
                                for merged_idx_local := High(exact_results_local)
                                    downto 1 do
                                begin
                                    exact_results_local[merged_idx_local] :=
                                        exact_results_local[merged_idx_local - 1];
                                end;
                                exact_results_local[0] :=
                                    standalone_candidate_local;
                            end;
                        end;

                        preferred_text_local := '';
                        preferred_bonus_local := 0;
                        try_get_preferred_query_phrase_text_local(query_key_local,
                            preferred_text_local, preferred_bonus_local);
                        next_pos_local := local_pos + chunk_len_local;
                        for exact_idx_local := 0 to High(exact_results_local) do
                        begin
                            if exact_idx_local >= c_subspan_chunk_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            if is_runtime_chain_candidate(exact_results_local[exact_idx_local]) or
                                is_runtime_common_pattern_candidate(exact_results_local[exact_idx_local]) or
                                is_runtime_redup_candidate(exact_results_local[exact_idx_local]) then
                            begin
                                Continue;
                            end;

                            candidate_text_local := Trim(exact_results_local[exact_idx_local].text);
                            candidate_units_local := get_candidate_text_unit_count(
                                candidate_text_local);
                            if candidate_units_local <> chunk_len_local then
                            begin
                                Continue;
                            end;
                            effective_weight_local := exact_results_local[exact_idx_local].score;
                            if exact_results_local[exact_idx_local].has_dict_weight and
                                (exact_results_local[exact_idx_local].dict_weight >
                                effective_weight_local) then
                            begin
                                effective_weight_local :=
                                    exact_results_local[exact_idx_local].dict_weight;
                            end;

                            next_state_local.score := base_state_local.score +
                                effective_weight_local + c_subspan_multi_bonus +
                                ((c_subspan_chunk_probe_limit - exact_idx_local) *
                                c_subspan_rank_step) +
                                (chunk_len_local * 24);
                            next_state_local.weight := base_state_local.weight +
                                effective_weight_local;
                            if base_state_local.text <> '' then
                            begin
                                Dec(next_state_local.score, c_subspan_additional_segment_penalty);
                                if m_dictionary <> nil then
                                begin
                                    Inc(next_state_local.score,
                                        get_sentence_context_bonus_scaled(
                                        base_state_local.prev_text,
                                        candidate_text_local));
                                end;
                                next_state_local.text := base_state_local.text + candidate_text_local;
                            end
                            else
                            begin
                                next_state_local.text := candidate_text_local;
                            end;
                            if chunk_len_local >= 3 then
                            begin
                                Inc(next_state_local.score, c_subspan_anchor_bonus);
                            end;
                            Inc(next_state_local.score,
                                get_fixed_boundary_alignment_adjustment(
                                start_idx + local_pos, chunk_len_local,
                                candidate_text_local));
                            if (preferred_text_local <> '') and
                                SameText(candidate_text_local, preferred_text_local) then
                            begin
                                Inc(next_state_local.score,
                                    c_subspan_preferred_bonus + preferred_bonus_local);
                            end;
                            next_state_local.prev_text := candidate_text_local;
                            next_state_local.anchor_units := base_state_local.anchor_units +
                                Ord((chunk_len_local >= 3) or
                                ((preferred_text_local <> '') and
                                SameText(candidate_text_local, preferred_text_local)));
                            add_subspan_state(local_states[next_pos_local], next_state_local);
                        end;
                    end;
                end;
            end;

            if Length(local_states[span_syllables]) > 0 then
            begin
                out_text := local_states[span_syllables][0].text;
                out_weight := local_states[span_syllables][0].weight;
                out_anchor_units := local_states[span_syllables][0].anchor_units;
                Result := out_text <> '';
            end;

            exact_sentence_subspan_cache_ready[start_idx][span_syllables] := True;
            exact_sentence_subspan_cache_texts[start_idx][span_syllables] := out_text;
            exact_sentence_subspan_cache_weights[start_idx][span_syllables] := out_weight;
            exact_sentence_subspan_cache_anchor_units[start_idx][span_syllables] :=
                out_anchor_units;
        end;

        function get_sentence_final_modal_particle_adjustment(
            const syllable_key: string; const candidate_text: string;
            const is_final_segment: Boolean): Integer;
        var
            normalized_key: string;
            expected_text: string;
            normalized_text: string;
        begin
            Result := 0;
            if (not is_final_segment) or (candidate_text = '') then
            begin
                Exit;
            end;

            normalized_key := normalize_pinyin_text(syllable_key);
            expected_text := '';
            if normalized_key = 'ba' then
            begin
                expected_text := string(Char($5427));
            end
            else if normalized_key = 'ma' then
            begin
                expected_text := string(Char($5417));
            end
            else if normalized_key = 'ne' then
            begin
                expected_text := string(Char($5462));
            end
            else if normalized_key = 'a' then
            begin
                expected_text := string(Char($554A));
            end
            else if normalized_key = 'ya' then
            begin
                expected_text := string(Char($5440));
            end
            else if normalized_key = 'la' then
            begin
                expected_text := string(Char($5566));
            end
            else if normalized_key = 'wa' then
            begin
                expected_text := string(Char($54C7));
            end;

            if expected_text = '' then
            begin
                Exit;
            end;

            normalized_text := Trim(candidate_text);
            if normalized_text = expected_text then
            begin
                Exit(760);
            end;

            if get_candidate_text_unit_count(normalized_text) = 1 then
            begin
                Exit(-720);
            end;
        end;

        function get_same_span_exact_competitor_penalty(
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer; const candidate_rank: Integer;
            const best_text: string; const best_weight: Integer;
            const best_rank: Integer): Integer; forward;
        function get_weak_two_syllable_phrase_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;
        function get_exact_phrase_lookahead_bonus(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string): Integer; forward;
        function get_exact_phrase_lookbehind_bonus(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;
        function get_longer_exact_prefix_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;
        function get_single_plus_following_phrase_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;
        function get_fixed_boundary_follow_phrase_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;
        function get_exact_phrase_split_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string;
            const candidate_weight: Integer): Integer; forward;

        function get_static_multi_candidate_adjustment_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate: TncCandidate; const candidate_text: string;
            const candidate_weight: Integer; const candidate_rank: Integer;
            const best_text: string; const best_weight: Integer;
            const best_rank: Integer): TLightSentenceStaticAdjustment;
        var
            cache_key_local: string;
            penalty_local: Integer;
        begin
            cache_key_local := IntToStr(segment_start) + ':' +
                IntToStr(segment_syllables) + ':' + IntToStr(candidate_rank) + ':' +
                IntToStr(candidate_weight) + ':' + IntToStr(best_weight) + ':' +
                IntToStr(best_rank) + ':' + candidate_text + #9 + best_text;
            if static_multi_adjustment_cache.TryGetValue(cache_key_local,
                Result) then
            begin
                Exit;
            end;

            Result.score_delta := c_light_sentence_multi_bonus +
                (segment_syllables * 24);
            Result.reject_candidate := False;
            if (segment_syllables = 2) and
                (get_candidate_text_unit_count(candidate_text) = 2) and
                candidate.has_dict_weight then
            begin
                Inc(Result.score_delta, 900);
            end;
            if segment_syllables >= 3 then
            begin
                Inc(Result.score_delta, 560 +
                    ((segment_syllables - 3) * 160));
            end;
            if (segment_syllables = 3) and candidate.has_dict_weight and
                (candidate_weight >= 900) and
                (get_candidate_text_unit_count(candidate_text) =
                segment_syllables) then
            begin
                Inc(Result.score_delta, 6000);
            end;
            if (segment_syllables >= 4) and candidate.has_dict_weight and
                (get_candidate_text_unit_count(candidate_text) =
                segment_syllables) then
            begin
                Inc(Result.score_delta, 4200 +
                    ((segment_syllables - 4) * 1800));
            end;
            if (head_syllable_count >= c_long_sentence_full_path_min_syllables) and
                (segment_syllables >= 3) and
                (get_candidate_text_unit_count(candidate_text) =
                segment_syllables) and candidate.has_dict_weight and
                (candidate_weight >= 260) and (candidate_weight < 900) then
            begin
                Inc(Result.score_delta, 5800 +
                    ((segment_syllables - 3) * 900) +
                    Min(1200, candidate_weight));
            end;

            penalty_local := get_same_span_exact_competitor_penalty(
                segment_syllables, candidate_text, candidate_weight,
                candidate_rank, best_text, best_weight, best_rank);
            Dec(Result.score_delta, penalty_local);

            penalty_local := get_weak_two_syllable_phrase_penalty(
                segment_start, segment_syllables, candidate_text,
                candidate_weight);
            if (segment_syllables = 2) and
                (get_candidate_text_unit_count(candidate_text) = 2) and
                (penalty_local >= 760) and (candidate_weight <= 360) then
            begin
                Result.reject_candidate := True;
                static_multi_adjustment_cache.AddOrSetValue(cache_key_local,
                    Result);
                Exit;
            end;
            Dec(Result.score_delta, penalty_local);

            Dec(Result.score_delta, get_longer_exact_prefix_penalty(
                segment_start, segment_syllables, candidate_text,
                candidate_weight));
            Dec(Result.score_delta, get_exact_phrase_split_penalty(
                segment_start, segment_syllables, candidate_text,
                candidate_weight));
            Dec(Result.score_delta, get_single_plus_following_phrase_penalty(
                segment_start, segment_syllables, candidate_text,
                candidate_weight));
            Dec(Result.score_delta, get_fixed_boundary_follow_phrase_penalty(
                segment_start, segment_syllables, candidate_text,
                candidate_weight));
            Inc(Result.score_delta, get_fixed_boundary_alignment_adjustment(
                segment_start, segment_syllables, candidate_text));
            Inc(Result.score_delta, get_exact_phrase_lookahead_bonus(
                segment_start, segment_syllables, candidate_text));
            Inc(Result.score_delta, get_exact_phrase_lookbehind_bonus(
                segment_start, segment_syllables, candidate_text,
                candidate_weight));

            static_multi_adjustment_cache.AddOrSetValue(cache_key_local, Result);
        end;

        function try_get_best_two_syllable_exact_sentence_segment(
            const segment_start: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        var
            anchor_units_local: Integer;
        begin
            Result := try_resolve_best_exact_sentence_subspan(segment_start, 2,
                out_text, out_weight, anchor_units_local);
            if Result and (get_candidate_text_unit_count(out_text) <> 2) then
            begin
                out_text := '';
                out_weight := 0;
                Result := False;
            end;
        end;

        function has_strong_two_syllable_exact_sentence_segment(
            const segment_start: Integer): Boolean;
        var
            best_text: string;
            best_weight: Integer;
        begin
            Result := try_get_best_two_syllable_exact_sentence_segment(segment_start,
                best_text, best_weight);
            if Result then
            begin
                Result := best_weight >= 520;
            end;
        end;

        function should_skip_two_single_rescue_segment(
            const segment_start: Integer): Boolean;
        var
            left_fixed: string;
            right_fixed: string;
        begin
            Result := False;
            if (segment_start < 0) or (segment_start + 1 >= head_syllable_count) then
            begin
                Exit;
            end;

            if has_strong_two_syllable_exact_sentence_segment(segment_start) then
            begin
                Exit(True);
            end;

            left_fixed := get_fixed_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text));
            right_fixed := get_fixed_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start + 1].text));

            if (left_fixed <> '') and (right_fixed = '') and
                (segment_start + 2 < head_syllable_count) and
                has_strong_two_syllable_exact_sentence_segment(segment_start + 1) then
            begin
                Exit(True);
            end;
        end;

        function is_allowed_sentence_segment_candidate(const candidate: TncCandidate;
            const segment_start: Integer; const segment_syllables: Integer): Boolean;
        var
            text_value: string;
            text_units: Integer;
            effective_weight: Integer;
            normalized_syllable: string;
            fixed_text: string;
            left_fixed: string;
            right_fixed: string;
            best_exact_text: string;
            best_exact_weight: Integer;
            following_phrase_weight: Integer;
            following_len: Integer;
            following_text: string;
            following_anchor_units: Integer;
        begin
            Result := False;
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            text_value := Trim(candidate.text);
            if text_value = '' then
            begin
                Exit;
            end;

            if is_runtime_chain_candidate(candidate) or
                is_runtime_common_pattern_candidate(candidate) or
                is_runtime_redup_candidate(candidate) then
            begin
                Exit;
            end;

            text_units := get_candidate_text_unit_count(text_value);
            if segment_syllables <= 1 then
            begin
                if is_fuzzy_pinyin_active and
                    (candidate.fuzzy_cost > 0) then
                begin
                    // The dictionary already validated this text against the
                    // alternate canonical syllable. Rechecking it against
                    // the raw syllable would incorrectly discard the edge.
                    Exit(text_units = 1);
                end;
                effective_weight := candidate.score;
                if candidate.has_dict_weight and (candidate.dict_weight > effective_weight) then
                begin
                    effective_weight := candidate.dict_weight;
                end;
                normalized_syllable := normalize_pinyin_text(syllables[segment_start].text);
                fixed_text := get_fixed_sentence_single_char_for_filter(normalized_syllable);
                if has_strong_two_syllable_exact_sentence_segment(segment_start) then
                begin
                    following_phrase_weight := Low(Integer);
                    for following_len := 2 to
                        Min(max_segment_len, head_syllable_count -
                        (segment_start + 1)) do
                    begin
                        if try_resolve_best_exact_sentence_subspan(
                            segment_start + 1, following_len, following_text,
                            best_exact_weight, following_anchor_units) and
                            (get_candidate_text_unit_count(following_text) >= 2) and
                            (best_exact_weight > following_phrase_weight) then
                        begin
                            following_phrase_weight := best_exact_weight;
                        end;
                    end;
                    if (not is_function_or_modifier_single_for_sentence(text_value)) or
                        (following_phrase_weight < 700) then
                    begin
                        Exit(False);
                    end;
                end;
                Exit((text_units = 1) and
                    is_extended_allowed_single_char_for_sentence(
                        normalized_syllable,
                        text_value, effective_weight));
            end;

            if text_units >= 2 then
            begin
                if text_units <> segment_syllables then
                begin
                    Exit(False);
                end;

                if segment_syllables = 2 then
                begin
                    left_fixed := get_fixed_sentence_single_char(
                        normalize_pinyin_text(syllables[segment_start].text));
                    right_fixed := get_fixed_sentence_single_char(
                        normalize_pinyin_text(syllables[segment_start + 1].text));
                    if (left_fixed <> '') and (right_fixed <> '') and
                        SameText(text_value, left_fixed + right_fixed) and
                        try_get_best_two_syllable_exact_sentence_segment(segment_start,
                        best_exact_text, best_exact_weight) and
                        (not SameText(best_exact_text, text_value)) then
                    begin
                        Exit(False);
                    end;
                end;

                Exit(True);
            end;

            Result := False;
        end;

        function get_light_sentence_segment_effective_weight(
            const candidate: TncCandidate; const query_key: string;
            const candidate_text: string): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
            Inc(Result, get_preferred_query_phrase_bonus_local(query_key,
                candidate_text));
        end;

        function find_best_same_span_exact_segment(
            const exact_results_local: TncCandidateList;
            const segment_start: Integer; const segment_syllables: Integer;
            const query_key: string; out out_text: string; out out_weight: Integer;
            out out_rank: Integer): Boolean;
        var
            local_idx: Integer;
            local_text: string;
            local_units: Integer;
            local_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank := -1;
            if (segment_syllables < 2) or (query_key = '') then
            begin
                Exit;
            end;

            for local_idx := 0 to High(exact_results_local) do
            begin
                if local_idx >= get_light_sentence_exact_top_n_local then
                begin
                    Break;
                end;
                if not is_allowed_sentence_segment_candidate(
                    exact_results_local[local_idx], segment_start,
                    segment_syllables) then
                begin
                    Continue;
                end;

                local_text := Trim(exact_results_local[local_idx].text);
                local_units := get_candidate_text_unit_count(local_text);
                if local_units <> segment_syllables then
                begin
                    Continue;
                end;

                local_weight := get_light_sentence_segment_effective_weight(
                    exact_results_local[local_idx], query_key, local_text);
                if (not Result) or (local_weight > out_weight) then
                begin
                    out_text := local_text;
                    out_weight := local_weight;
                    out_rank := local_idx;
                    Result := True;
                end;
            end;
        end;

        function get_bridge_exact_single_context_bonus(
            const segment_start: Integer; const prev_text_value: string;
            const prev_syllables: Integer; const single_text_value: string): Integer;
        const
            c_min_bridge_weight = 520;
            c_base_bonus = 420;
            c_max_bonus = 2600;
            c_probe_limit = 12;
        var
            bridge_start: Integer;
            bridge_len: Integer;
            bridge_key: string;
            bridge_text: string;
            bridge_results: TncCandidateList;
            bridge_idx: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_weight_local: Integer;
        begin
            Result := 0;
            if (prev_syllables <= 0) or (prev_syllables > 4) or
                (segment_start <= 0) or (prev_text_value = '') or
                (single_text_value = '') or
                (get_candidate_text_unit_count(single_text_value) <> 1) or
                (not is_function_or_modifier_single_for_sentence(single_text_value)) then
            begin
                Exit;
            end;

            bridge_start := segment_start - prev_syllables;
            bridge_len := prev_syllables + 1;
            if (bridge_start < 0) or (bridge_len < 2) or (bridge_len > 5) then
            begin
                Exit;
            end;

            bridge_key := build_syllable_query_key(bridge_start, bridge_len);
            bridge_text := Trim(prev_text_value) + Trim(single_text_value);
            if (bridge_key = '') or (bridge_text = '') or
                (get_candidate_text_unit_count(bridge_text) <> bridge_len) or
                (not dictionary_exact_lookup_cached(bridge_key, bridge_results)) then
            begin
                Exit;
            end;

            best_weight_local := Low(Integer);
            for bridge_idx := 0 to High(bridge_results) do
            begin
                if bridge_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(bridge_results[bridge_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(bridge_results[bridge_idx].text);
                if not SameText(candidate_text_local, bridge_text) then
                begin
                    Continue;
                end;
                candidate_weight_local := get_light_sentence_segment_effective_weight(
                    bridge_results[bridge_idx], bridge_key, candidate_text_local);
                if candidate_weight_local > best_weight_local then
                begin
                    best_weight_local := candidate_weight_local;
                end;
            end;

            if best_weight_local >= c_min_bridge_weight then
            begin
                Result := Min(c_max_bonus, c_base_bonus + best_weight_local);
            end;
        end;

        function get_same_span_exact_competitor_penalty(
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer; const candidate_rank: Integer;
            const best_text: string; const best_weight: Integer;
            const best_rank: Integer): Integer;
        const
            c_min_best_weight = 700;
            c_min_clear_gap = 160;
            c_base_penalty = 2800;
            c_gap_scale = 80;
            c_rank_penalty = 960;
            c_strong_best_bonus = 2200;
            c_weak_candidate_bonus = 1800;
            c_max_penalty = 48000;
        var
            weight_gap: Integer;
        begin
            Result := 0;
            if (segment_syllables < 2) or (candidate_text = '') or
                (best_text = '') or SameText(candidate_text, best_text) or
                (best_weight < c_min_best_weight) or
                (get_candidate_text_unit_count(candidate_text) <>
                segment_syllables) then
            begin
                Exit;
            end;

            weight_gap := best_weight - candidate_weight;
            if weight_gap < c_min_clear_gap then
            begin
                Exit;
            end;

            Result := c_base_penalty + (weight_gap * c_gap_scale);
            if (best_rank >= 0) and (candidate_rank > best_rank) then
            begin
                Inc(Result, (candidate_rank - best_rank) * c_rank_penalty);
            end;
            if best_weight >= 1000 then
            begin
                Inc(Result, c_strong_best_bonus);
            end;
            if candidate_weight < c_min_best_weight then
            begin
                Inc(Result, c_weak_candidate_bonus);
            end;
            Result := Min(Result, c_max_penalty);
        end;

        function try_get_direct_exact_sentence_span_candidate(
            const segment_start: Integer; const segment_syllables: Integer;
            out out_text: string; out out_weight: Integer;
            out out_rank: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            span_key: string;
            span_results: TncCandidateList;
            span_idx: Integer;
            span_text: string;
            span_units: Integer;
            span_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank := -1;
            if (head_syllable_count < c_long_sentence_full_path_min_syllables) or
                (segment_start < 0) or (segment_syllables < 2) or
                (segment_syllables > 5) or
                (segment_start + segment_syllables > head_syllable_count) then
            begin
                Exit;
            end;

            span_key := build_syllable_query_key(segment_start, segment_syllables);
            if (span_key = '') or
                (not dictionary_exact_lookup_cached(span_key, span_results)) then
            begin
                Exit;
            end;

            for span_idx := 0 to High(span_results) do
            begin
                if span_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if (Trim(span_results[span_idx].comment) <> '') or
                    is_runtime_chain_candidate(span_results[span_idx]) or
                    is_runtime_common_pattern_candidate(span_results[span_idx]) or
                    is_runtime_redup_candidate(span_results[span_idx]) then
                begin
                    Continue;
                end;

                span_text := Trim(span_results[span_idx].text);
                span_units := get_candidate_text_unit_count(span_text);
                if span_units <> segment_syllables then
                begin
                    Continue;
                end;

                span_weight := get_light_sentence_segment_effective_weight(
                    span_results[span_idx], span_key, span_text);
                if (not Result) or (span_weight > out_weight) then
                begin
                    out_text := span_text;
                    out_weight := span_weight;
                    out_rank := span_idx;
                    Result := True;
                end;
            end;
        end;

        function get_direct_exact_merged_phrase_penalty_for_recent_transition(
            const base_state: TLightSentenceState; const current_pos: Integer;
            const current_seg_len: Integer; const current_text: string): Integer;
        var
            merged_start: Integer;
            merged_len: Integer;
            merged_text: string;
            merged_weight: Integer;
            merged_rank: Integer;
            split_text: string;
        begin
            Result := 0;
            if (base_state.last_segment_syllable_count < 2) or
                (current_seg_len <> 1) or (current_text = '') then
            begin
                Exit;
            end;

            merged_start := current_pos - base_state.last_segment_syllable_count;
            merged_len := base_state.last_segment_syllable_count + current_seg_len;
            if (merged_start < 0) or (merged_len < 3) or (merged_len > 5) or
                (not try_get_direct_exact_sentence_span_candidate(merged_start,
                merged_len, merged_text, merged_weight, merged_rank)) or
                (merged_weight < 260) then
            begin
                Exit;
            end;

            split_text := Trim(base_state.prev_text) + Trim(current_text);
            if SameText(split_text, merged_text) then
            begin
                Exit;
            end;

            Result := 5200 + ((merged_len - 3) * 900) +
                Min(1800, merged_weight);
            if merged_rank = 0 then
            begin
                Inc(Result, 1200);
            end;
        end;

        function has_exact_merged_phrase_for_recent_transition(
            const base_state: TLightSentenceState; const current_pos: Integer;
            const current_seg_len: Integer): Boolean;
        var
            merged_start: Integer;
            merged_len: Integer;
            merged_text: string;
            merged_weight: Integer;
            merged_units: Integer;
            merged_anchor_units: Integer;
        begin
            Result := False;
            if (base_state.last_segment_syllable_count < 2) or (current_seg_len <> 1) then
            begin
                Exit;
            end;

            merged_start := current_pos - base_state.last_segment_syllable_count;
            if merged_start < 0 then
            begin
                Exit;
            end;

            merged_len := base_state.last_segment_syllable_count + current_seg_len;
            if merged_len < 3 then
            begin
                Exit;
            end;
            if not try_resolve_best_exact_sentence_subspan(merged_start, merged_len,
                merged_text, merged_weight, merged_anchor_units) then
            begin
                Exit;
            end;
            merged_units := get_candidate_text_unit_count(Trim(merged_text));
            Result := merged_units >= 2;
        end;

        function has_exact_merged_phrase_for_single_bridge(
            const base_state: TLightSentenceState; const current_pos: Integer;
            const current_seg_len: Integer): Boolean;
        var
            merged_text: string;
            merged_weight: Integer;
            merged_units: Integer;
            merged_anchor_units: Integer;
        begin
            Result := False;
            if (base_state.last_segment_syllable_count <> 1) or (current_seg_len <> 1) then
            begin
                Exit;
            end;

            if current_pos <= 0 then
            begin
                Exit;
            end;
            if not try_resolve_best_exact_sentence_subspan(current_pos - 1, 2,
                merged_text, merged_weight, merged_anchor_units) then
            begin
                Exit;
            end;
            merged_units := get_candidate_text_unit_count(Trim(merged_text));
            Result := merged_units >= 2;
        end;

        function try_get_best_sentence_single_char(const syllable_key: string;
            out out_text: string; out out_weight: Integer): Boolean;
        const
            c_probe_limit = 6;
        var
            single_results: TncCandidateList;
            single_idx: Integer;
            single_text: string;
            weight_value: Integer;
            fixed_text: string;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if syllable_key = '' then
            begin
                Exit;
            end;

            fixed_text := get_fixed_sentence_single_char(syllable_key);
            if (fixed_text <> '') and
                (not is_ambiguous_sentence_function_syllable(syllable_key)) and
                is_extended_allowed_single_char_for_sentence(
                syllable_key, fixed_text, 960) then
            begin
                out_text := fixed_text;
                out_weight := 960;
                Exit(True);
            end;

            if not dictionary_exact_lookup_cached(syllable_key, single_results) then
            begin
                Exit;
            end;

            for single_idx := 0 to High(single_results) do
            begin
                if single_idx >= c_probe_limit then
                begin
                    Break;
                end;
                single_text := Trim(single_results[single_idx].text);
                if (single_results[single_idx].comment <> '') or
                    (get_candidate_text_unit_count(single_text) <> 1) then
                begin
                    Continue;
                end;
                if single_results[single_idx].has_dict_weight then
                begin
                    weight_value := single_results[single_idx].dict_weight;
                end
                else
                begin
                    weight_value := single_results[single_idx].score;
                end;
                if not is_extended_allowed_single_char_for_sentence(
                    syllable_key, single_text, weight_value) then
                begin
                    Continue;
                end;
                if weight_value > out_weight then
                begin
                    out_text := single_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function try_get_best_function_or_modifier_sentence_single_char(
            const syllable_key: string; out out_text: string;
            out out_weight: Integer): Boolean;
        const
            c_probe_limit = 12;
        var
            single_results: TncCandidateList;
            single_idx: Integer;
            single_text: string;
            weight_value: Integer;
            fixed_text: string;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if syllable_key = '' then
            begin
                Exit;
            end;

            fixed_text := get_fixed_sentence_single_char(syllable_key);
            if (fixed_text <> '') and
                (not is_ambiguous_sentence_function_syllable(syllable_key)) and
                is_function_or_modifier_single_for_sentence(fixed_text) then
            begin
                out_text := fixed_text;
                out_weight := 960;
                Exit(True);
            end;

            if not dictionary_exact_lookup_cached(syllable_key, single_results) then
            begin
                Exit;
            end;

            for single_idx := 0 to High(single_results) do
            begin
                if single_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(single_results[single_idx].comment) <> '' then
                begin
                    Continue;
                end;

                single_text := Trim(single_results[single_idx].text);
                if (get_candidate_text_unit_count(single_text) <> 1) or
                    (not is_function_or_modifier_single_for_sentence(single_text)) then
                begin
                    Continue;
                end;

                if single_results[single_idx].has_dict_weight then
                begin
                    weight_value := single_results[single_idx].dict_weight;
                end
                else
                begin
                    weight_value := single_results[single_idx].score;
                end;

                if weight_value > out_weight then
                begin
                    out_text := single_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_weak_two_syllable_phrase_penalty(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        const
            c_weak_phrase_weight_limit = 360;
        var
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            split_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (candidate_weight >= c_weak_phrase_weight_limit) or
                (segment_start < 0) or (segment_start + 1 > High(syllables)) then
            begin
                Exit;
            end;

            if (not try_get_best_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text), left_text, left_weight)) or
                (not try_get_best_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start + 1].text), right_text, right_weight)) then
            begin
                Exit;
            end;

            split_weight := left_weight + right_weight;
            if split_weight >= candidate_weight + 700 then
            begin
                Result := 980;
            end
            else if split_weight >= candidate_weight + 500 then
            begin
                Result := 760;
            end
            else if split_weight >= candidate_weight + 320 then
            begin
                Result := 520;
            end;
        end;

        function get_exact_phrase_lookahead_bonus(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string): Integer;
        var
            combined_text: string;
            combined_units: Integer;
            candidate_units: Integer;
            combined_weight: Integer;
            candidate_prefix: string;
            combined_anchor_units: Integer;
        begin
            Result := 0;
            if (segment_syllables < 2) or
                (segment_start < 0) or
                (segment_start + segment_syllables > High(syllables)) then
            begin
                Exit;
            end;

            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units < 2 then
            begin
                Exit;
            end;
            candidate_prefix := candidate_text;
            if not try_resolve_best_exact_sentence_subspan(segment_start,
                segment_syllables + 1, combined_text, combined_weight,
                combined_anchor_units) then
            begin
                Exit;
            end;
            combined_units := get_candidate_text_unit_count(combined_text);
            if (combined_units <> candidate_units + 1) or
                (Copy(combined_text, 1, Length(candidate_prefix)) <> candidate_prefix) then
            begin
                Exit;
            end;
            if combined_weight >= 760 then
            begin
                Exit(96);
            end
            else if combined_weight >= 560 then
            begin
                Exit(72);
            end
            else if combined_weight >= 360 then
            begin
                Exit(48);
            end;
        end;

        function ends_with_text_local(const full_value: string;
            const suffix_value: string): Boolean;
        begin
            Result := (full_value <> '') and (suffix_value <> '') and
                (Length(suffix_value) <= Length(full_value)) and
                SameText(Copy(full_value,
                Length(full_value) - Length(suffix_value) + 1, Length(suffix_value)),
                suffix_value);
        end;

        function get_exact_phrase_lookbehind_bonus(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        var
            total_len: Integer;
            span_start: Integer;
            combined_text: string;
            combined_weight: Integer;
            combined_anchor_units: Integer;
            candidate_units: Integer;
            combined_units: Integer;
            best_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <= 0) or (candidate_text = '') or
                (segment_start <= 0) then
            begin
                Exit;
            end;

            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <= 0 then
            begin
                Exit;
            end;

            best_weight := Low(Integer);
            for total_len := segment_syllables + 1 to
                Min(max_segment_len, segment_start + segment_syllables) do
            begin
                span_start := segment_start - (total_len - segment_syllables);
                if span_start < 0 then
                begin
                    Continue;
                end;
                if not try_resolve_best_exact_sentence_subspan(span_start,
                    total_len, combined_text, combined_weight,
                    combined_anchor_units) then
                begin
                    Continue;
                end;
                combined_units := get_candidate_text_unit_count(combined_text);
                if (combined_units <= candidate_units) or
                    (not ends_with_text_local(combined_text, candidate_text)) then
                begin
                    Continue;
                end;
                if combined_weight > best_weight then
                begin
                    best_weight := combined_weight;
                end;
            end;

            if best_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            if best_weight >= candidate_weight + 240 then
            begin
                Exit(240);
            end
            else if best_weight >= candidate_weight + 80 then
            begin
                Exit(180);
            end
            else if best_weight >= candidate_weight - 80 then
            begin
                Exit(120);
            end
            else if best_weight >= candidate_weight - 240 then
            begin
                Exit(72);
            end;
        end;

        function get_longer_exact_prefix_penalty(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        var
            longer_len: Integer;
            longer_text: string;
            longer_units: Integer;
            candidate_units: Integer;
            longer_weight: Integer;
            best_weight: Integer;
            longer_anchor_units: Integer;
        begin
            Result := 0;
            if (segment_syllables < 2) or
                (segment_start < 0) or
                (segment_start + segment_syllables >= head_syllable_count) then
            begin
                Exit;
            end;

            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units < 2 then
            begin
                Exit;
            end;

            best_weight := Low(Integer);
            for longer_len := segment_syllables + 1 to
                Min(max_segment_len, head_syllable_count - segment_start) do
            begin
                if not try_resolve_best_exact_sentence_subspan(segment_start,
                    longer_len, longer_text, longer_weight, longer_anchor_units) then
                begin
                    Continue;
                end;
                longer_units := get_candidate_text_unit_count(longer_text);
                if (longer_units <= candidate_units) or
                    (Copy(longer_text, 1, Length(candidate_text)) <> candidate_text) then
                begin
                    Continue;
                end;
                if longer_weight > best_weight then
                begin
                    best_weight := longer_weight;
                end;
            end;

            if best_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            if best_weight >= candidate_weight + 80 then
            begin
                Exit(920);
            end
            else if best_weight >= candidate_weight - 20 then
            begin
                Exit(680);
            end
            else if best_weight >= candidate_weight - 160 then
            begin
                Exit(420);
            end;
        end;

        function get_single_plus_following_phrase_penalty(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        const
            c_probe_limit = 6;
        var
            single_text: string;
            single_weight: Integer;
            follow_len: Integer;
            follow_key: string;
            follow_results: TncCandidateList;
            follow_idx: Integer;
            follow_text: string;
            follow_units: Integer;
            follow_weight: Integer;
            best_follow_weight: Integer;
            combined_weight: Integer;
            function_prefix: Boolean;
        begin
            Result := 0;
            function_prefix := False;
            if (segment_syllables <> 2) or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (segment_start < 0) or
                (segment_start + 2 >= head_syllable_count) then
            begin
                Exit;
            end;

            if try_get_best_function_or_modifier_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text),
                single_text, single_weight) then
            begin
                function_prefix := True;
            end
            else if not try_get_best_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text),
                single_text, single_weight) then
            begin
                Exit;
            end;

            if not function_prefix then
            begin
                function_prefix := is_function_or_modifier_single_for_sentence(
                    single_text);
            end;
            best_follow_weight := Low(Integer);
            for follow_len := 2 to Min(max_segment_len, head_syllable_count - (segment_start + 1)) do
            begin
                follow_key := build_syllable_query_key(segment_start + 1, follow_len);
                if (follow_key = '') or
                    (not dictionary_exact_lookup_cached(follow_key, follow_results)) then
                begin
                    Continue;
                end;

                for follow_idx := 0 to High(follow_results) do
                begin
                    if follow_idx >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(follow_results[follow_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not is_allowed_sentence_segment_candidate(
                        follow_results[follow_idx], segment_start + 1, follow_len) then
                    begin
                        Continue;
                    end;

                    follow_text := Trim(follow_results[follow_idx].text);
                    follow_units := get_candidate_text_unit_count(follow_text);
                    if follow_units < 2 then
                    begin
                        Continue;
                    end;

                    if follow_results[follow_idx].has_dict_weight then
                    begin
                        follow_weight := follow_results[follow_idx].dict_weight;
                    end
                    else
                    begin
                        follow_weight := follow_results[follow_idx].score;
                    end;

                    if follow_weight > best_follow_weight then
                    begin
                        best_follow_weight := follow_weight;
                    end;
                end;
            end;

            if best_follow_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            combined_weight := single_weight + best_follow_weight;
            if function_prefix then
            begin
                if combined_weight >= candidate_weight + 420 then
                begin
                    Exit(3200);
                end
                else if combined_weight >= candidate_weight + 220 then
                begin
                    Exit(2400);
                end
                else if combined_weight >= candidate_weight + 80 then
                begin
                    Exit(1600);
                end
                else if best_follow_weight >= 700 then
                begin
                    Exit(960);
                end;
            end
            else
            begin
                if combined_weight >= candidate_weight + 520 then
                begin
                    Exit(880);
                end
                else if combined_weight >= candidate_weight + 320 then
                begin
                    Exit(640);
                end
                else if combined_weight >= candidate_weight + 180 then
                begin
                    Exit(420);
                end;
            end;
        end;

        function try_get_best_following_sentence_phrase_weight(
            const start_idx: Integer; const min_syllables: Integer;
            out out_weight: Integer): Boolean;
        var
            follow_len: Integer;
            follow_text: string;
            follow_units: Integer;
            follow_weight: Integer;
            follow_anchor_units: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (start_idx < 0) or (start_idx >= head_syllable_count) or
                (min_syllables < 2) then
            begin
                Exit;
            end;

            for follow_len := min_syllables to
                Min(max_segment_len, head_syllable_count - start_idx) do
            begin
                if not try_resolve_best_exact_sentence_subspan(start_idx, follow_len,
                    follow_text, follow_weight, follow_anchor_units) then
                begin
                    Continue;
                end;
                follow_units := get_candidate_text_unit_count(follow_text);
                if follow_units < 2 then
                begin
                    Continue;
                end;
                if follow_weight > out_weight then
                begin
                    out_weight := follow_weight;
                    Result := True;
                end;
            end;
        end;

        function get_fixed_boundary_follow_phrase_penalty(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            left_key: string;
            right_key: string;
            left_fixed: string;
            right_fixed: string;
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            follow_weight: Integer;
            combined_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (segment_start < 0) or (segment_start + 1 >= head_syllable_count) then
            begin
                Exit;
            end;

            left_key := normalize_pinyin_text(syllables[segment_start].text);
            right_key := normalize_pinyin_text(syllables[segment_start + 1].text);
            left_fixed := get_fixed_sentence_single_char(left_key);
            right_fixed := get_fixed_sentence_single_char(right_key);

            if ((left_fixed <> '') or
                try_get_best_function_or_modifier_sentence_single_char(left_key,
                left_text, left_weight)) and
                ((left_text <> '') or
                try_get_best_sentence_single_char(left_key, left_text, left_weight)) and
                try_get_best_following_sentence_phrase_weight(segment_start + 1, 2,
                follow_weight) then
            begin
                combined_weight := left_weight + follow_weight;
                if combined_weight >= candidate_weight + 520 then
                begin
                    Exit(5200);
                end
                else if combined_weight >= candidate_weight + 320 then
                begin
                    Exit(3800);
                end
                else if combined_weight >= candidate_weight + 160 then
                begin
                    Exit(2600);
                end
                else if follow_weight >= 700 then
                begin
                    Exit(1680);
                end;
            end;

            if (right_fixed <> '') and (segment_start + 2 < head_syllable_count) and
                try_get_best_sentence_single_char(left_key, left_text, left_weight) and
                try_get_best_sentence_single_char(right_key, right_text, right_weight) and
                try_get_best_following_sentence_phrase_weight(segment_start + 2, 2,
                follow_weight) then
            begin
                combined_weight := left_weight + right_weight + follow_weight;
                if combined_weight >= candidate_weight + 640 then
                begin
                    Exit(3600);
                end
                else if combined_weight >= candidate_weight + 420 then
                begin
                    Exit(2600);
                end
                else if combined_weight >= candidate_weight + 220 then
                begin
                    Exit(1720);
                end;
            end;
        end;

        function get_exact_phrase_split_penalty(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        const
            c_anchor_unit_bonus = 320;
            c_edge_fixed_bonus = 260;
        var
            split_pos: Integer;
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            left_anchor_units: Integer;
            right_anchor_units: Integer;
            combined_text: string;
            combined_weight: Integer;
            best_split_weight: Integer;
            left_fixed: string;
            right_fixed: string;
        begin
            Result := 0;
            if (segment_syllables < 3) or
                (get_candidate_text_unit_count(candidate_text) < 2) or
                (segment_start < 0) or
                (segment_start + segment_syllables > head_syllable_count) then
            begin
                Exit;
            end;

            best_split_weight := Low(Integer);
            for split_pos := 1 to segment_syllables - 1 do
            begin
                if (not try_resolve_best_exact_sentence_subspan(segment_start,
                    split_pos, left_text, left_weight, left_anchor_units)) or
                    (not try_resolve_best_exact_sentence_subspan(
                    segment_start + split_pos,
                    segment_syllables - split_pos, right_text, right_weight,
                    right_anchor_units)) then
                begin
                    Continue;
                end;

                if (left_text = '') or (right_text = '') then
                begin
                    Continue;
                end;

                combined_text := left_text + right_text;
                if SameText(combined_text, candidate_text) then
                begin
                    Continue;
                end;

                combined_weight := left_weight + right_weight +
                    get_sentence_context_bonus_scaled(left_text, right_text) +
                    ((left_anchor_units + right_anchor_units) *
                    c_anchor_unit_bonus);

                left_fixed := get_fixed_sentence_single_char(
                    normalize_pinyin_text(syllables[segment_start].text));
                if (split_pos = 1) and (left_fixed <> '') and
                    SameText(left_text, left_fixed) then
                begin
                    Inc(combined_weight, c_edge_fixed_bonus);
                end;

                right_fixed := get_fixed_sentence_single_char(
                    normalize_pinyin_text(
                    syllables[segment_start + segment_syllables - 1].text));
                if (split_pos = segment_syllables - 1) and
                    (right_fixed <> '') and SameText(right_text, right_fixed) then
                begin
                    Inc(combined_weight, c_edge_fixed_bonus);
                end;

                if combined_weight > best_split_weight then
                begin
                    best_split_weight := combined_weight;
                end;
            end;

            if best_split_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            if (segment_syllables = 3) and (candidate_weight >= 900) then
            begin
                if best_split_weight >= candidate_weight + 2200 then
                begin
                    Exit(480);
                end;
                Exit(0);
            end;

            if best_split_weight >= candidate_weight + 960 then
            begin
                Exit(4200);
            end
            else if best_split_weight >= candidate_weight + 520 then
            begin
                Exit(2600);
            end
            else if best_split_weight >= candidate_weight + 220 then
            begin
                Exit(1400);
            end
            else if best_split_weight >= candidate_weight - 40 then
            begin
                Exit(720);
            end;
        end;

        function try_build_two_single_rescue_segment(const segment_start: Integer;
            out out_text: string; out out_weight: Integer;
            out out_rank_bonus: Integer): Boolean;
        var
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            left_fixed: string;
            right_fixed: string;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank_bonus := 0;
            if (segment_start < 0) or (segment_start + 1 >= head_syllable_count) or
                (head_syllable_count < c_long_sentence_full_path_min_syllables) then
            begin
                Exit;
            end;

            left_fixed := get_fixed_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text));
            right_fixed := get_fixed_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start + 1].text));
            if (left_fixed = '') and (right_fixed = '') then
            begin
                Exit;
            end;

            if should_skip_two_single_rescue_segment(segment_start) then
            begin
                Exit;
            end;

            if (not try_get_best_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start].text), left_text, left_weight)) or
                (not try_get_best_sentence_single_char(
                normalize_pinyin_text(syllables[segment_start + 1].text), right_text, right_weight)) then
            begin
                Exit;
            end;

            out_text := left_text + right_text;
            out_weight := left_weight + right_weight;
            out_rank_bonus := c_light_sentence_two_single_rescue_bonus;
            Result := get_candidate_text_unit_count(out_text) = 2;
        end;

        procedure add_state(var states: TLightSentenceStateArray;
            const candidate_state: TLightSentenceState);
        var
            idx: Integer;
            insert_idx: Integer;
            tmp_state: TLightSentenceState;
        begin
            if is_debug_target_recall_prefix(candidate_state.text) then
            begin
                m_debug_target_max_generated_syllable := Max(
                    m_debug_target_max_generated_syllable,
                    get_candidate_text_unit_count(candidate_state.text));
            end;

            for idx := 0 to High(states) do
            begin
                if states[idx].path = candidate_state.path then
                begin
                    if candidate_state.score > states[idx].score then
                    begin
                        states[idx] := candidate_state;
                    end;
                    Exit;
                end;
            end;

            insert_idx := Length(states);
            SetLength(states, insert_idx + 1);
            states[insert_idx] := candidate_state;
            while (insert_idx > 0) and (states[insert_idx].score > states[insert_idx - 1].score) do
            begin
                tmp_state := states[insert_idx - 1];
                states[insert_idx - 1] := states[insert_idx];
                states[insert_idx] := tmp_state;
                Dec(insert_idx);
            end;

            if Length(states) > get_light_sentence_beam_width_local then
            begin
                SetLength(states, get_light_sentence_beam_width_local);
            end;

            for idx := 0 to High(states) do
            begin
                if is_debug_target_recall_prefix(states[idx].text) then
                begin
                    m_debug_target_max_retained_syllable := Max(
                        m_debug_target_max_retained_syllable,
                        get_candidate_text_unit_count(states[idx].text));
                end;
            end;
        end;

        procedure append_light_candidate(const text_value: string; const comment_value: string;
            const path_value: string; const score_value: Integer;
            const path_score_hint_value: Integer;
            const fuzzy_cost_value: Integer = 0;
            const fuzzy_rules_value: TncFuzzyPinyinRules = []);
        var
            candidate: TncCandidate;
            next_index: Integer;
        begin
            if Trim(text_value) = '' then
            begin
                Exit;
            end;

            candidate := Default(TncCandidate);
            candidate.text := Trim(text_value);
            candidate.comment := Trim(comment_value);
            candidate.score := score_value;
            candidate.source := cs_rule;
            candidate.has_dict_weight := False;
            candidate.dict_weight := 0;
            candidate.fuzzy_cost := fuzzy_cost_value;
            candidate.fuzzy_rules := fuzzy_rules_value;

            next_index := Length(out_candidates);
            SetLength(out_candidates, next_index + 1);
            out_candidates[next_index] := candidate;
            remember_segment_path_for_candidate(candidate.text, candidate.comment,
                Trim(path_value), path_score_hint_value);
        end;

        procedure append_best_same_span_exact_segment_repair(
            const state_value: TLightSentenceState);
        const
            c_repair_min_gap = 160;
            c_repair_base_bonus = 1800;
            c_repair_gap_scale = 4;
            c_repair_max_gap_bonus = 8000;
        var
            path_parts: TArray<string>;
            repaired_parts: TArray<string>;
            part_idx: Integer;
            unit_pos: Integer;
            part_units: Integer;
            exact_results: TncCandidateList;
            query_key: string;
            current_text: string;
            best_text: string;
            repaired_text: string;
            repaired_path: string;
            current_weight: Integer;
            best_weight: Integer;
            best_rank: Integer;
            total_gap: Integer;
            repair_score: Integer;
            changed: Boolean;
            duplicate_idx: Integer;
            duplicate_found: Boolean;

            function get_candidate_weight_from_exact_results(
                const text_value: string; out out_weight: Integer): Boolean;
            var
                local_idx: Integer;
                local_text: string;
            begin
                Result := False;
                out_weight := 0;
                for local_idx := 0 to High(exact_results) do
                begin
                    if not is_allowed_sentence_segment_candidate(
                        exact_results[local_idx], unit_pos, part_units) then
                    begin
                        Continue;
                    end;
                    local_text := Trim(exact_results[local_idx].text);
                    if not SameText(local_text, text_value) then
                    begin
                        Continue;
                    end;
                    out_weight := get_light_sentence_segment_effective_weight(
                        exact_results[local_idx], query_key, local_text);
                    Exit(True);
                end;
            end;
        begin
            if (head_syllable_count < 5) or (Trim(state_value.path) = '') then
            begin
                Exit;
            end;

            path_parts := state_value.path.Split([c_segment_path_separator]);
            if Length(path_parts) <= 1 then
            begin
                Exit;
            end;
            SetLength(repaired_parts, Length(path_parts));
            for part_idx := 0 to High(path_parts) do
            begin
                repaired_parts[part_idx] := Trim(path_parts[part_idx]);
            end;

            unit_pos := 0;
            total_gap := 0;
            changed := False;
            for part_idx := 0 to High(repaired_parts) do
            begin
                current_text := Trim(repaired_parts[part_idx]);
                part_units := get_candidate_text_unit_count(current_text);
                if (current_text = '') or (part_units <= 0) or
                    (unit_pos + part_units > head_syllable_count) then
                begin
                    Exit;
                end;

                if part_units >= 2 then
                begin
                    query_key := build_syllable_query_key(unit_pos, part_units);
                    if (query_key <> '') and
                        dictionary_exact_lookup_cached(query_key, exact_results) and
                        find_best_same_span_exact_segment(exact_results, unit_pos,
                        part_units, query_key, best_text, best_weight,
                        best_rank) and (best_text <> '') and
                        (not SameText(best_text, current_text)) and
                        get_candidate_weight_from_exact_results(current_text,
                        current_weight) and
                        (best_weight >= current_weight + c_repair_min_gap) then
                    begin
                        repaired_parts[part_idx] := best_text;
                        Inc(total_gap, best_weight - current_weight);
                        changed := True;
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                Format(' spanrepair=[%s>%s gap=%d]',
                                [current_text, best_text,
                                best_weight - current_weight]);
                        end;
                    end;
                end;

                Inc(unit_pos, part_units);
            end;

            if (not changed) or (unit_pos <> head_syllable_count) then
            begin
                Exit;
            end;

            repaired_text := '';
            repaired_path := '';
            for part_idx := 0 to High(repaired_parts) do
            begin
                repaired_text := repaired_text + repaired_parts[part_idx];
                if repaired_path <> '' then
                begin
                    repaired_path := repaired_path + c_segment_path_separator;
                end;
                repaired_path := repaired_path + repaired_parts[part_idx];
            end;
            if (repaired_text = '') or SameText(repaired_text,
                Trim(state_value.text)) then
            begin
                Exit;
            end;

            repair_score := state_value.score + c_repair_base_bonus +
                Min(c_repair_max_gap_bonus, total_gap * c_repair_gap_scale);

            duplicate_found := False;
            for duplicate_idx := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[duplicate_idx].text),
                    repaired_text) and
                    (Trim(out_candidates[duplicate_idx].comment) = '') then
                begin
                    duplicate_found := True;
                    Break;
                end;
            end;
            if duplicate_found then
            begin
                if repair_score > out_candidates[duplicate_idx].score then
                begin
                    out_candidates[duplicate_idx].score := repair_score;
                    out_candidates[duplicate_idx].dict_weight := repair_score;
                    out_candidates[duplicate_idx].has_dict_weight := True;
                    remember_segment_path_for_candidate(repaired_text, '',
                        repaired_path, repair_score);
                end;
                Exit;
            end;

            append_light_candidate(repaired_text, '', repaired_path,
                repair_score, state_value.lexical_rank_score +
                c_repair_base_bonus + Min(c_repair_max_gap_bonus,
                total_gap * c_repair_gap_scale), state_value.fuzzy_cost,
                state_value.fuzzy_rules);
        end;

        procedure append_direct_exact_span_repair_candidate(
            const state_value: TLightSentenceState);
        const
            c_min_span_weight = 260;
            c_repair_base_bonus = 9000;
            c_repair_unit_bonus = 1400;
            c_repair_max_bonus = 24000;
        var
            source_units: TArray<string>;
            repaired_units: TArray<string>;
            pos: Integer;
            scan_len: Integer;
            best_len: Integer;
            best_text: string;
            best_weight: Integer;
            best_rank: Integer;
            exact_text: string;
            exact_weight: Integer;
            exact_rank: Integer;
            exact_units: TArray<string>;
            unit_idx: Integer;
            current_span_text: string;
            repaired_text: string;
            repaired_path: string;
            total_weight: Integer;
            total_units: Integer;
            replacement_count: Integer;
            repair_score: Integer;
            duplicate_idx: Integer;
            duplicate_found: Boolean;

            function join_source_units_local(const start_idx: Integer;
                const unit_count: Integer): string;
            var
                join_idx: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (unit_count <= 0) or
                    (start_idx + unit_count > Length(source_units)) then
                begin
                    Exit;
                end;
                for join_idx := start_idx to start_idx + unit_count - 1 do
                begin
                    Result := Result + source_units[join_idx];
                end;
            end;

            procedure append_path_segment_local(const segment_text: string);
            begin
                if Trim(segment_text) = '' then
                begin
                    Exit;
                end;
                if repaired_path <> '' then
                begin
                    repaired_path := repaired_path + c_segment_path_separator;
                end;
                repaired_path := repaired_path + segment_text;
            end;
        begin
            if (head_syllable_count < c_long_sentence_full_path_min_syllables) or
                (Trim(state_value.text) = '') then
            begin
                Exit;
            end;

            source_units := split_text_units(Trim(state_value.text));
            if Length(source_units) <> head_syllable_count then
            begin
                Exit;
            end;
            SetLength(repaired_units, Length(source_units));
            for unit_idx := 0 to High(source_units) do
            begin
                repaired_units[unit_idx] := source_units[unit_idx];
            end;

            pos := 0;
            total_weight := 0;
            total_units := 0;
            replacement_count := 0;
            repaired_path := '';
            while pos < head_syllable_count do
            begin
                best_len := 0;
                best_text := '';
                best_weight := 0;
                best_rank := -1;
                for scan_len := Min(5, head_syllable_count - pos) downto 3 do
                begin
                    if not try_get_direct_exact_sentence_span_candidate(pos,
                        scan_len, exact_text, exact_weight, exact_rank) then
                    begin
                        Continue;
                    end;
                    if (exact_weight < c_min_span_weight) or (exact_text = '') then
                    begin
                        Continue;
                    end;

                    exact_units := split_text_units(exact_text);
                    if Length(exact_units) <> scan_len then
                    begin
                        Continue;
                    end;

                    current_span_text := join_source_units_local(pos, scan_len);
                    if SameText(current_span_text, exact_text) then
                    begin
                        Continue;
                    end;

                    if (best_len <= 0) or
                        (scan_len > best_len) or
                        ((scan_len = best_len) and (exact_weight > best_weight)) then
                    begin
                        best_len := scan_len;
                        best_text := exact_text;
                        best_weight := exact_weight;
                        best_rank := exact_rank;
                    end;
                end;

                if best_len > 0 then
                begin
                    exact_units := split_text_units(best_text);
                    for unit_idx := 0 to best_len - 1 do
                    begin
                        repaired_units[pos + unit_idx] := exact_units[unit_idx];
                    end;
                    Inc(total_weight, best_weight);
                    Inc(total_units, best_len);
                    Inc(replacement_count);
                    if best_rank = 0 then
                    begin
                        Inc(total_weight, 300);
                    end;
                    append_path_segment_local(best_text);
                    Inc(pos, best_len);
                end
                else
                begin
                    append_path_segment_local(source_units[pos]);
                    Inc(pos);
                end;
            end;

            if replacement_count <= 0 then
            begin
                Exit;
            end;

            repaired_text := '';
            for unit_idx := 0 to High(repaired_units) do
            begin
                repaired_text := repaired_text + repaired_units[unit_idx];
            end;
            if (repaired_text = '') or SameText(repaired_text,
                Trim(state_value.text)) then
            begin
                Exit;
            end;

            repair_score := state_value.score + c_repair_base_bonus +
                Min(c_repair_max_bonus, (total_weight * 3) +
                (total_units * c_repair_unit_bonus));

            duplicate_found := False;
            for duplicate_idx := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[duplicate_idx].text),
                    repaired_text) and
                    (Trim(out_candidates[duplicate_idx].comment) = '') then
                begin
                    duplicate_found := True;
                    Break;
                end;
            end;
            if duplicate_found then
            begin
                if repair_score > out_candidates[duplicate_idx].score then
                begin
                    out_candidates[duplicate_idx].score := repair_score;
                    out_candidates[duplicate_idx].dict_weight := repair_score;
                    out_candidates[duplicate_idx].has_dict_weight := True;
                    remember_segment_path_for_candidate(repaired_text, '',
                        repaired_path, repair_score);
                end;
                Exit;
            end;

            append_light_candidate(repaired_text, '', repaired_path,
                repair_score, state_value.lexical_rank_score + c_repair_base_bonus +
                Min(c_repair_max_bonus, (total_weight * 3) +
                (total_units * c_repair_unit_bonus)), state_value.fuzzy_cost,
                state_value.fuzzy_rules);
        end;

        procedure append_function_phrase_split_repairs;
        const
            c_repair_probe_limit = 4;
            c_min_follow_phrase_weight = 700;
            c_repair_base_bonus = 6200;
        var
            source_idx: Integer;
            start_idx: Integer;
            follow_len: Integer;
            units: TArray<string>;
            repaired_units: TArray<string>;
            unit_idx: Integer;
            repair_text: string;
            repair_path: string;
            single_text: string;
            single_weight: Integer;
            phrase_text: string;
            phrase_weight: Integer;
            phrase_units: Integer;
            phrase_anchor_units: Integer;
            duplicate_idx: Integer;
            duplicate_found: Boolean;
            score_bonus: Integer;
            current_two_key: string;
            current_two_text: string;
            current_two_results: TncCandidateList;
            current_two_best_text: string;
            current_two_weight: Integer;
            current_two_rank: Integer;
        begin
            if Length(out_candidates) <= 0 then
            begin
                Exit;
            end;

            for source_idx := 0 to Min(c_repair_probe_limit - 1,
                High(out_candidates)) do
            begin
                if Trim(out_candidates[source_idx].comment) <> '' then
                begin
                    Continue;
                end;

                units := split_text_units(Trim(out_candidates[source_idx].text));
                if Length(units) <> head_syllable_count then
                begin
                    Continue;
                end;

                for start_idx := 0 to head_syllable_count - 3 do
                begin
                    if not try_get_best_function_or_modifier_sentence_single_char(
                        normalize_pinyin_text(syllables[start_idx].text),
                        single_text, single_weight) then
                    begin
                        Continue;
                    end;

                    for follow_len := 2 to Min(max_segment_len,
                        head_syllable_count - start_idx - 1) do
                    begin
                        if not try_resolve_best_exact_sentence_subspan(
                            start_idx + 1, follow_len, phrase_text,
                            phrase_weight, phrase_anchor_units) then
                        begin
                            Continue;
                        end;
                        phrase_units := get_candidate_text_unit_count(
                            Trim(phrase_text));
                        if (phrase_units <> follow_len) or
                            (phrase_weight < c_min_follow_phrase_weight) then
                        begin
                            Continue;
                        end;

                        if start_idx + 1 < Length(units) then
                        begin
                            current_two_key := build_syllable_query_key(start_idx, 2);
                            current_two_text := Trim(units[start_idx]) +
                                Trim(units[start_idx + 1]);
                            if (current_two_key <> '') and (current_two_text <> '') and
                                dictionary_exact_lookup_cached(current_two_key,
                                current_two_results) and
                                find_best_same_span_exact_segment(
                                current_two_results, start_idx, 2,
                                current_two_key, current_two_best_text,
                                current_two_weight, current_two_rank) and
                                SameText(current_two_best_text,
                                current_two_text) and
                                (current_two_weight >= phrase_weight + 400) then
                            begin
                                Continue;
                            end;
                        end;

                        SetLength(repaired_units, Length(units));
                        for unit_idx := 0 to High(units) do
                        begin
                            repaired_units[unit_idx] := units[unit_idx];
                        end;
                        repaired_units[start_idx] := single_text;
                        for unit_idx := 0 to phrase_units - 1 do
                        begin
                            repaired_units[start_idx + 1 + unit_idx] :=
                                Copy(phrase_text, unit_idx + 1, 1);
                        end;

                        repair_text := '';
                        for unit_idx := 0 to High(repaired_units) do
                        begin
                            repair_text := repair_text + repaired_units[unit_idx];
                        end;
                        if SameText(repair_text,
                            Trim(out_candidates[source_idx].text)) then
                        begin
                            Continue;
                        end;

                        duplicate_found := False;
                        for duplicate_idx := 0 to High(out_candidates) do
                        begin
                            if SameText(Trim(out_candidates[duplicate_idx].text),
                                repair_text) and
                                (Trim(out_candidates[duplicate_idx].comment) = '') then
                            begin
                                duplicate_found := True;
                                Break;
                            end;
                        end;
                        if duplicate_found then
                        begin
                            Continue;
                        end;

                        repair_path := '';
                        for unit_idx := 0 to start_idx - 1 do
                        begin
                            if repair_path <> '' then
                            begin
                                repair_path := repair_path + c_segment_path_separator;
                            end;
                            repair_path := repair_path + repaired_units[unit_idx];
                        end;
                        if repair_path <> '' then
                        begin
                            repair_path := repair_path + c_segment_path_separator;
                        end;
                        repair_path := repair_path + single_text +
                            c_segment_path_separator + phrase_text;
                        for unit_idx := start_idx + 1 + phrase_units to
                            High(repaired_units) do
                        begin
                            repair_path := repair_path + c_segment_path_separator +
                                repaired_units[unit_idx];
                        end;

                        score_bonus := c_repair_base_bonus +
                            Min(phrase_weight, 1800) + (phrase_anchor_units * 360);
                        append_light_candidate(repair_text, '', repair_path,
                            out_candidates[source_idx].score + score_bonus,
                            out_candidates[source_idx].score + score_bonus,
                            out_candidates[source_idx].fuzzy_cost,
                            out_candidates[source_idx].fuzzy_rules);
                    end;
                end;
            end;
        end;

        procedure append_intermediate_state_partials;
        const
            c_intermediate_partial_limit = 12;
            c_intermediate_partial_bonus = 220;
            c_fixed_boundary_partial_bonus = 560;
        var
            pos_local: Integer;
            state_idx_local: Integer;
            remaining_key: string;
            state_text: string;
            state_units: Integer;
            emitted_count: Integer;
            duplicate_found: Boolean;
            existing_idx: Integer;
            state_value: TLightSentenceState;
            last_fixed_text: string;
            last_syllable_key: string;
            score_bonus: Integer;
        begin
            if tail_prefix <> '' then
            begin
                Exit;
            end;

            emitted_count := 0;
            for pos_local := head_syllable_count - 1 downto 2 do
            begin
                remaining_key := build_syllable_query_key(pos_local,
                    head_syllable_count - pos_local);
                if remaining_key = '' then
                begin
                    Continue;
                end;

                for state_idx_local := 0 to Min(7, High(state_map[pos_local])) do
                begin
                    state_value := state_map[pos_local][state_idx_local];
                    state_text := Trim(state_value.text);
                    state_units := get_candidate_text_unit_count(state_text);
                    if state_units < 2 then
                    begin
                        Continue;
                    end;

                    duplicate_found := False;
                    for existing_idx := 0 to High(out_candidates) do
                    begin
                        if SameText(Trim(out_candidates[existing_idx].text), state_text) and
                            SameText(normalize_pinyin_text(out_candidates[existing_idx].comment),
                            remaining_key) then
                        begin
                            duplicate_found := True;
                            Break;
                        end;
                    end;
                    if duplicate_found then
                    begin
                        Continue;
                    end;

                    score_bonus := c_intermediate_partial_bonus +
                        ((head_syllable_count - pos_local) * 36);
                    if state_value.last_segment_syllable_count = 1 then
                    begin
                        last_syllable_key := normalize_pinyin_text(
                            syllables[pos_local - 1].text);
                        last_fixed_text := get_fixed_sentence_single_char(
                            last_syllable_key);
                        if (last_fixed_text <> '') and
                            SameText(Trim(state_value.prev_text), last_fixed_text) then
                        begin
                            Inc(score_bonus, c_fixed_boundary_partial_bonus);
                        end;
                    end;

                    append_light_candidate(state_text, remaining_key,
                        state_value.path, state_value.score + score_bonus,
                        state_value.lexical_rank_score + score_bonus,
                        state_value.fuzzy_cost, state_value.fuzzy_rules);
                    Inc(emitted_count);
                    if emitted_count >= c_intermediate_partial_limit then
                    begin
                        Exit;
                    end;
                end;
            end;
        end;

        function can_predict_one_letter_tail_local(const prefix_value: string): Boolean;
        begin
            Result := (Length(prefix_value) <> 1) or
                SameText(prefix_value, 'w') or SameText(prefix_value, 'h');
        end;

        function try_predict_tail_single_char(const prefix_value: string;
            const prev_prev_text: string; const prev_text: string;
            out out_text: string): Boolean;
        const
            c_tail_completion_keys: array[0..17] of string = (
                'wo', 'he', 'yi', 'yu', 'you', 'yao', 'shi', 'zai', 'bu',
                'le', 'hai', 'hen', 'hui', 'neng', 'qu', 'lai', 'gei', 'de'
            );
        var
            completion_idx: Integer;
            exact_results: TncCandidateList;
            result_idx: Integer;
            completion_key: string;
            candidate_text: string;
            candidate_score: Integer;
            best_score: Integer;
            second_score: Integer;
            is_preferred: Boolean;
        begin
            Result := False;
            out_text := '';
            if prefix_value = '' then
            begin
                Exit;
            end;
            if not can_predict_one_letter_tail_local(prefix_value) then
            begin
                Exit;
            end;

            best_score := Low(Integer);
            second_score := Low(Integer);
            for completion_idx := Low(c_tail_completion_keys) to High(c_tail_completion_keys) do
            begin
                completion_key := c_tail_completion_keys[completion_idx];
                if (Copy(completion_key, 1, Length(prefix_value)) <> prefix_value) then
                begin
                    Continue;
                end;
                if not dictionary_exact_lookup_cached(completion_key, exact_results) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(exact_results) do
                begin
                    if result_idx >= c_light_sentence_tail_completion_top_n then
                    begin
                        Break;
                    end;
                    candidate_text := Trim(exact_results[result_idx].text);
                    if (get_candidate_text_unit_count(candidate_text) <> 1) or
                        (not match_single_char_candidate_for_syllable(
                        completion_key, candidate_text, is_preferred)) then
                    begin
                        Continue;
                    end;

                    candidate_score := exact_results[result_idx].score;
                    if exact_results[result_idx].has_dict_weight and
                        (exact_results[result_idx].dict_weight > candidate_score) then
                    begin
                        candidate_score := exact_results[result_idx].dict_weight;
                    end;
                    if is_preferred then
                    begin
                        Inc(candidate_score, 60);
                    end;
                    if is_allowed_single_char_for_sentence(candidate_text) then
                    begin
                        Inc(candidate_score, 80);
                    end;
                    if (m_dictionary <> nil) and (prev_text <> '') then
                    begin
                        Inc(candidate_score, get_sentence_context_bonus_scaled(
                            prev_text, candidate_text));
                        if prev_prev_text <> '' then
                        begin
                            Inc(candidate_score, get_sentence_trigram_bonus_scaled(
                                prev_prev_text, prev_text, candidate_text));
                        end;
                    end;

                    if candidate_score > best_score then
                    begin
                        second_score := best_score;
                        best_score := candidate_score;
                        out_text := candidate_text;
                    end
                    else if candidate_score > second_score then
                    begin
                        second_score := candidate_score;
                    end;
                end;
            end;

            Result := (out_text <> '') and
                ((second_score <= Low(Integer) div 4) or ((best_score - second_score) >= 120));
            if not Result then
            begin
                out_text := '';
            end;
        end;

        procedure append_complete_single_syllable_tail_from_partial_states;
        const
            c_completion_limit = 8;
            c_completion_bonus = c_light_sentence_tail_prediction_bonus + 760;
            c_tail_exact_probe_limit = 18;
            function pick_full_syllable_tail_exact(
                const tail_key_value: string; const prev_prev_text_value: string;
                const prev_text_value: string; out out_text: string): Boolean;
            var
                exact_results: TncCandidateList;
                result_idx: Integer;
                candidate_text: string;
                candidate_score: Integer;
                best_score: Integer;
                is_preferred: Boolean;
                function can_complete_tail_key_local(const value: string): Boolean;
                begin
                    Result := SameText(value, 'wo') or SameText(value, 'he') or
                        SameText(value, 'yi') or SameText(value, 'yu') or
                        SameText(value, 'you') or SameText(value, 'yao') or
                        SameText(value, 'shi') or SameText(value, 'zai') or
                        SameText(value, 'bu') or SameText(value, 'le') or
                        SameText(value, 'hai') or SameText(value, 'hen') or
                        SameText(value, 'hui') or SameText(value, 'neng') or
                        SameText(value, 'qu') or SameText(value, 'lai') or
                        SameText(value, 'gei') or SameText(value, 'de') or
                        SameText(value, 'ba') or SameText(value, 'ma') or
                        SameText(value, 'ne') or SameText(value, 'a') or
                        SameText(value, 'ya') or SameText(value, 'la');
                end;

            begin
                Result := False;
                out_text := '';
                if (tail_key_value = '') or (not can_complete_tail_key_local(tail_key_value)) or
                    (not is_full_pinyin_key(tail_key_value)) or
                    (not dictionary_exact_lookup_cached(tail_key_value, exact_results)) then
                begin
                    Exit;
                end;

                best_score := Low(Integer);
                for result_idx := 0 to High(exact_results) do
                begin
                    if result_idx >= c_tail_exact_probe_limit then
                    begin
                        Break;
                    end;
                    candidate_text := Trim(exact_results[result_idx].text);
                    if (get_candidate_text_unit_count(candidate_text) <> 1) or
                        (not match_single_char_candidate_for_syllable(
                        tail_key_value, candidate_text, is_preferred)) then
                    begin
                        Continue;
                    end;

                    candidate_score := exact_results[result_idx].score;
                    if exact_results[result_idx].has_dict_weight and
                        (exact_results[result_idx].dict_weight > candidate_score) then
                    begin
                        candidate_score := exact_results[result_idx].dict_weight;
                    end;
                    if is_preferred then
                    begin
                        Inc(candidate_score, 90);
                    end;
                    if is_allowed_single_char_for_sentence(candidate_text) then
                    begin
                        Inc(candidate_score, 120);
                    end;
                    if (m_dictionary <> nil) and (prev_text_value <> '') then
                    begin
                        Inc(candidate_score, get_sentence_context_bonus_scaled(
                            prev_text_value, candidate_text));
                        if prev_prev_text_value <> '' then
                        begin
                            Inc(candidate_score, get_sentence_trigram_bonus_scaled(
                                prev_prev_text_value, prev_text_value, candidate_text));
                        end;
                    end;

                    if candidate_score > best_score then
                    begin
                        best_score := candidate_score;
                        out_text := candidate_text;
                        Result := True;
                    end;
                end;
            end;
        var
            pos_local: Integer;
            state_idx_local: Integer;
            emitted_count: Integer;
            existing_idx: Integer;
            tail_key: string;
            tail_text: string;
            completed_text: string;
            completed_path: string;
            state_value: TLightSentenceState;
            state_text: string;
            duplicate_found: Boolean;
        begin
            if tail_prefix <> '' then
            begin
                Exit;
            end;
            if head_syllable_count < 2 then
            begin
                Exit;
            end;

            pos_local := head_syllable_count - 1;
            if (pos_local < 1) or (pos_local > High(state_map)) then
            begin
                Exit;
            end;

            tail_key := build_syllable_query_key(pos_local, 1);
            if (tail_key = '') or (not is_full_pinyin_key(tail_key)) or
                (get_effective_compact_pinyin_unit_count(tail_key) <> 1) then
            begin
                Exit;
            end;

            emitted_count := 0;
            for state_idx_local := 0 to Min(7, High(state_map[pos_local])) do
            begin
                state_value := state_map[pos_local][state_idx_local];
                state_text := Trim(state_value.text);
                if get_candidate_text_unit_count(state_text) < 2 then
                begin
                    Continue;
                end;

                tail_text := '';
                if not try_predict_tail_single_char(tail_key, state_value.prev_prev_text,
                    state_value.prev_text, tail_text) and
                    (not pick_full_syllable_tail_exact(tail_key, state_value.prev_prev_text,
                    state_value.prev_text, tail_text)) then
                begin
                    Continue;
                end;

                completed_text := state_text + tail_text;
                duplicate_found := False;
                for existing_idx := 0 to High(out_candidates) do
                begin
                    if (Trim(out_candidates[existing_idx].comment) = '') and
                        SameText(Trim(out_candidates[existing_idx].text), completed_text) then
                    begin
                        duplicate_found := True;
                        Break;
                    end;
                end;
                if duplicate_found then
                begin
                    Continue;
                end;

                completed_path := state_value.path;
                if completed_path <> '' then
                begin
                    completed_path := completed_path + c_segment_path_separator + tail_text;
                end
                else
                begin
                    completed_path := completed_text;
                end;

                append_light_candidate(completed_text, '', completed_path,
                    state_value.score + c_completion_bonus,
                    state_value.lexical_rank_score + c_completion_bonus,
                    state_value.fuzzy_cost, state_value.fuzzy_rules);
                Inc(emitted_count);
                if emitted_count >= c_completion_limit then
                begin
                    Exit;
                end;
            end;
        end;

        var
            pos: Integer;
            state_idx: Integer;
        seg_len: Integer;
        next_pos: Integer;
        seg_key: string;
        exact_results: TncCandidateList;
        fuzzy_results: TncCandidateList;
        lookup_results: TncCandidateList;
        exact_lookup_results: TncCandidateList;
        result_idx: Integer;
        merged_result_idx: Integer;
        accepted_count: Integer;
        base_state: TLightSentenceState;
        candidate_state: TLightSentenceState;
        candidate_text: string;
        candidate_units: Integer;
        exact_effective_weight: Integer;
        weak_phrase_penalty: Integer;
        lookahead_bonus: Integer;
        tail_text: string;
        tail_partial_bonus: Integer;
        rescue_text: string;
            rescue_weight: Integer;
            rescue_rank_bonus: Integer;
            top_exact_effective_weight: Integer;
            exact_same_as_rescue: Boolean;
            lookbehind_bonus: Integer;
            duplicate_found: Boolean;
            following_phrase_weight: Integer;
            best_same_span_text: string;
            best_same_span_weight: Integer;
            best_same_span_rank: Integer;
            static_adjustment: TLightSentenceStaticAdjustment;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        exact_sentence_subspan_cache_prepared := False;
        syllable_key_cache_count := 0;
        context_bonus_cache_count := 0;
        trigram_bonus_cache_count := 0;
        if (m_dictionary = nil) or (not m_config.enable_segment_candidates) then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        syllables := get_effective_compact_pinyin_syllables(normalized_query);
        if Length(syllables) < 2 then
        begin
            Exit;
        end;

        head_syllable_count := Length(syllables);
        tail_prefix := '';
        if not is_full_pinyin_key(normalize_pinyin_text(syllables[High(syllables)].text)) then
        begin
            tail_prefix := normalize_pinyin_text(syllables[High(syllables)].text);
            Dec(head_syllable_count);
        end;

        if head_syllable_count < 2 then
        begin
            Exit;
        end;
        if (tail_prefix = '') and (head_syllable_count < 4) then
        begin
            Exit;
        end;

        if head_syllable_count >= 16 then
        begin
            max_segment_len := 4;
        end
        else if head_syllable_count >= 5 then
        begin
            max_segment_len := 5;
        end
        else
        begin
            max_segment_len := 3;
        end;

        static_multi_adjustment_cache :=
            TDictionary<string, TLightSentenceStaticAdjustment>.Create;
        try
        SetLength(state_map, head_syllable_count + 1);
        SetLength(state_map[0], 1);
        state_map[0][0].score := 0;
        state_map[0][0].lexical_rank_score := 0;
        state_map[0][0].text := '';
        state_map[0][0].path := '';
        state_map[0][0].prev_text := '';
        state_map[0][0].prev_prev_text := '';
        state_map[0][0].last_segment_syllable_count := 0;
        state_map[0][0].fuzzy_cost := 0;
        state_map[0][0].fuzzy_rules := [];

        for pos := 0 to head_syllable_count - 1 do
        begin
            for state_idx := 0 to High(state_map[pos]) do
            begin
                base_state := state_map[pos][state_idx];
                for seg_len := 1 to Min(max_segment_len, head_syllable_count - pos) do
                begin
                    seg_key := build_syllable_query_key(pos, seg_len);
                    SetLength(exact_results, 0);
                    if dictionary_exact_lookup_cached(seg_key, exact_lookup_results) then
                    begin
                        SetLength(exact_results, Length(exact_lookup_results));
                        for result_idx := 0 to High(exact_lookup_results) do
                        begin
                            exact_results[result_idx] :=
                            exact_lookup_results[result_idx];
                        end;
                    end;
                    if (seg_len <= 4) and
                        dictionary_fuzzy_lattice_lookup_cached(seg_key,
                        fuzzy_results) then
                    begin
                        exact_results := merge_candidate_lists(exact_results,
                            fuzzy_results, 0);
                        reserve_fuzzy_lattice_edges_local(exact_results);
                    end;

                    if (Length(exact_results) = 0) and
                        (not (m_dictionary is TncSqliteDictionary)) and
                        dictionary_lookup_cached(seg_key, lookup_results) then
                    begin
                        for result_idx := 0 to High(lookup_results) do
                        begin
                            duplicate_found := False;
                            for merged_result_idx := 0 to High(exact_results) do
                            begin
                                if SameText(Trim(exact_results[merged_result_idx].text),
                                    Trim(lookup_results[result_idx].text)) and
                                    SameText(Trim(exact_results[merged_result_idx].comment),
                                    Trim(lookup_results[result_idx].comment)) then
                                begin
                                    duplicate_found := True;
                                    Break;
                                end;
                            end;
                            if duplicate_found then
                            begin
                                Continue;
                            end;
                            SetLength(exact_results, Length(exact_results) + 1);
                            exact_results[High(exact_results)] :=
                                lookup_results[result_idx];
                        end;
                    end;

                    if Length(exact_results) = 0 then
                    begin
                        Continue;
                    end;

                    accepted_count := 0;
                    top_exact_effective_weight := 0;
                    exact_same_as_rescue := False;
                    next_pos := pos + seg_len;
                    find_best_same_span_exact_segment(exact_results, pos,
                        seg_len, seg_key, best_same_span_text,
                        best_same_span_weight, best_same_span_rank);
                    for result_idx := 0 to High(exact_results) do
                    begin
                        if accepted_count >= get_light_sentence_exact_top_n_local then
                        begin
                            Break;
                        end;
                        if not is_allowed_sentence_segment_candidate(
                            exact_results[result_idx], pos, seg_len) then
                        begin
                            Continue;
                        end;

                        candidate_text := Trim(exact_results[result_idx].text);
                        candidate_units := get_candidate_text_unit_count(candidate_text);
                        exact_effective_weight :=
                            get_light_sentence_segment_effective_weight(
                            exact_results[result_idx], seg_key, candidate_text);
                        if exact_effective_weight > top_exact_effective_weight then
                        begin
                            top_exact_effective_weight := exact_effective_weight;
                        end;
                        candidate_state.score := base_state.score +
                            Max(exact_results[result_idx].score,
                            exact_effective_weight);
                        Inc(candidate_state.score,
                            get_preferred_query_phrase_bonus_local(seg_key,
                            candidate_text));
                        candidate_state.lexical_rank_score := base_state.lexical_rank_score +
                            ((get_light_sentence_exact_top_n_local - result_idx) *
                            c_light_sentence_exact_rank_step);
                        Inc(candidate_state.score,
                            (get_light_sentence_exact_top_n_local - result_idx) *
                            c_light_sentence_exact_rank_step);
                        if base_state.text <> '' then
                        begin
                            Dec(candidate_state.score, c_light_sentence_additional_segment_penalty);
                        end;

                        if candidate_units >= 2 then
                        begin
                            static_adjustment :=
                                get_static_multi_candidate_adjustment_local(
                                pos, seg_len, exact_results[result_idx],
                                candidate_text, exact_effective_weight,
                                result_idx, best_same_span_text,
                                best_same_span_weight, best_same_span_rank);
                            if static_adjustment.reject_candidate then
                            begin
                                Continue;
                            end;
                            Inc(candidate_state.score,
                                static_adjustment.score_delta);
                        end
                        else
                        begin
                            Dec(candidate_state.score, c_light_sentence_single_penalty);
                            Inc(candidate_state.score,
                                get_bridge_exact_single_context_bonus(pos,
                                base_state.prev_text,
                                base_state.last_segment_syllable_count,
                                candidate_text));
                            if get_candidate_text_unit_count(base_state.prev_text) = 1 then
                            begin
                                Dec(candidate_state.score, 240);
                            end;
                            if get_candidate_text_unit_count(base_state.prev_prev_text) = 1 then
                            begin
                                Dec(candidate_state.score, 120);
                            end;
                            weak_phrase_penalty :=
                                get_direct_exact_merged_phrase_penalty_for_recent_transition(
                                base_state, pos, seg_len, candidate_text);
                            if weak_phrase_penalty > 0 then
                            begin
                                Dec(candidate_state.score, weak_phrase_penalty);
                            end
                            else if has_exact_merged_phrase_for_recent_transition(base_state, pos, seg_len) then
                            begin
                                Dec(candidate_state.score, 1680);
                            end;
                            if has_exact_merged_phrase_for_single_bridge(base_state, pos, seg_len) then
                            begin
                                Dec(candidate_state.score, 1120);
                            end;

                            if is_function_or_modifier_single_for_sentence(candidate_text) and
                                try_get_best_following_sentence_phrase_weight(pos + 1, 2,
                                following_phrase_weight) then
                            begin
                                if following_phrase_weight >= 1400 then
                                begin
                                    Inc(candidate_state.score, 4600);
                                end
                                else if following_phrase_weight >= 900 then
                                begin
                                    Inc(candidate_state.score, 3400);
                                end
                                else if following_phrase_weight >= 700 then
                                begin
                                    Inc(candidate_state.score, 2400);
                                end
                                else if following_phrase_weight >= 520 then
                                begin
                                    Inc(candidate_state.score, 1480);
                                end;
                            end;

                            lookbehind_bonus := get_exact_phrase_lookbehind_bonus(
                                pos, seg_len, candidate_text, exact_effective_weight);
                            if lookbehind_bonus > 0 then
                            begin
                                Inc(candidate_state.score, lookbehind_bonus);
                            end;
                        end;

                        if (m_dictionary <> nil) and (base_state.prev_text <> '') then
                        begin
                            Inc(candidate_state.score,
                                get_sentence_context_bonus_scaled(
                                base_state.prev_text, candidate_text));
                            if base_state.prev_prev_text <> '' then
                            begin
                                Inc(candidate_state.score,
                                    get_sentence_trigram_bonus_scaled(
                                    base_state.prev_prev_text,
                                    base_state.prev_text, candidate_text));
                            end;
                        end;

                        Inc(candidate_state.score,
                            get_sentence_final_modal_particle_adjustment(
                            seg_key, candidate_text, next_pos = head_syllable_count));

                        if base_state.text <> '' then
                        begin
                            candidate_state.text := base_state.text + candidate_text;
                            candidate_state.path := base_state.path + c_segment_path_separator + candidate_text;
                        end
                        else
                        begin
                            candidate_state.text := candidate_text;
                            candidate_state.path := candidate_text;
                        end;
                        candidate_state.prev_prev_text := base_state.prev_text;
                        candidate_state.prev_text := candidate_text;
                        candidate_state.last_segment_syllable_count := seg_len;
                        candidate_state.fuzzy_cost := base_state.fuzzy_cost +
                            exact_results[result_idx].fuzzy_cost;
                        candidate_state.fuzzy_rules := base_state.fuzzy_rules +
                            exact_results[result_idx].fuzzy_rules;
                        add_state(state_map[next_pos], candidate_state);
                        Inc(accepted_count);
                    end;

                    if (seg_len = 2) and
                        try_build_two_single_rescue_segment(pos, rescue_text, rescue_weight,
                        rescue_rank_bonus) and
                        (accepted_count <= 0) then
                    begin
                        for result_idx := 0 to Min(get_light_sentence_exact_top_n_local - 1,
                            High(exact_results)) do
                        begin
                            if SameText(Trim(exact_results[result_idx].text), rescue_text) then
                            begin
                                exact_same_as_rescue := True;
                                Break;
                            end;
                        end;

                        if not exact_same_as_rescue then
                        begin
                            candidate_state.score := base_state.score + rescue_weight;
                            candidate_state.lexical_rank_score := base_state.lexical_rank_score +
                                rescue_rank_bonus;
                            Inc(candidate_state.score, rescue_rank_bonus);
                            if base_state.text <> '' then
                            begin
                                Dec(candidate_state.score, c_light_sentence_additional_segment_penalty);
                            end;
                            Inc(candidate_state.score, c_light_sentence_multi_bonus + (seg_len * 24));

                            if (m_dictionary <> nil) and (base_state.prev_text <> '') then
                            begin
                                Inc(candidate_state.score,
                                    get_sentence_context_bonus_scaled(
                                    base_state.prev_text, rescue_text));
                                if base_state.prev_prev_text <> '' then
                                begin
                                    Inc(candidate_state.score,
                                        get_sentence_trigram_bonus_scaled(
                                        base_state.prev_prev_text,
                                        base_state.prev_text, rescue_text));
                                end;
                            end;

                            lookahead_bonus := get_exact_phrase_lookahead_bonus(
                                pos, seg_len, rescue_text);
                            if lookahead_bonus > 0 then
                            begin
                                Inc(candidate_state.score, lookahead_bonus);
                            end;

                            lookbehind_bonus := get_exact_phrase_lookbehind_bonus(
                                pos, seg_len, rescue_text, rescue_weight);
                            if lookbehind_bonus > 0 then
                            begin
                                Inc(candidate_state.score, lookbehind_bonus);
                            end;

                            if next_pos = head_syllable_count then
                            begin
                                Inc(candidate_state.score, 220);
                            end;
                            Inc(candidate_state.score,
                                get_fixed_boundary_alignment_adjustment(
                                pos, seg_len, rescue_text));

                            if base_state.text <> '' then
                            begin
                                candidate_state.text := base_state.text + rescue_text;
                                candidate_state.path := base_state.path + c_segment_path_separator +
                                    rescue_text;
                            end
                            else
                            begin
                                candidate_state.text := rescue_text;
                                candidate_state.path := rescue_text;
                            end;
                            candidate_state.prev_prev_text := base_state.prev_text;
                            candidate_state.prev_text := rescue_text;
                            candidate_state.last_segment_syllable_count := seg_len;
                            candidate_state.fuzzy_cost := base_state.fuzzy_cost;
                            candidate_state.fuzzy_rules := base_state.fuzzy_rules;
                            add_state(state_map[next_pos], candidate_state);
                        end;
                    end;
                end;
            end;
        end;

        final_states := state_map[head_syllable_count];
        if Length(final_states) <= 0 then
        begin
            Exit;
        end;

        if m_debug_target_recall_text <> '' then
        begin
            m_debug_target_final_state_count := Max(
                m_debug_target_final_state_count, Length(final_states));
            for state_idx := 0 to High(final_states) do
            begin
                if SameText(Trim(final_states[state_idx].text),
                    m_debug_target_recall_text) then
                begin
                    m_debug_target_final_state_source_mask :=
                        m_debug_target_final_state_source_mask or 2;
                    if (m_debug_target_light_final_state_rank = 0) or
                        (state_idx + 1 < m_debug_target_light_final_state_rank) then
                    begin
                        m_debug_target_light_final_state_rank := state_idx + 1;
                    end;
                    if (not m_debug_target_final_state_found) or
                        (state_idx + 1 < m_debug_target_final_state_rank) then
                    begin
                        m_debug_target_final_state_found := True;
                        m_debug_target_final_state_rank := state_idx + 1;
                    end;
                end;
            end;
        end;

        for state_idx := 0 to Min(get_light_sentence_emit_limit_local - 1, High(final_states)) do
        begin
            append_light_candidate(final_states[state_idx].text, '',
                final_states[state_idx].path, final_states[state_idx].score + 320,
                final_states[state_idx].lexical_rank_score,
                final_states[state_idx].fuzzy_cost,
                final_states[state_idx].fuzzy_rules);
            append_best_same_span_exact_segment_repair(final_states[state_idx]);
            append_direct_exact_span_repair_candidate(final_states[state_idx]);
        end;

        append_function_phrase_split_repairs;
        append_intermediate_state_partials;
        append_complete_single_syllable_tail_from_partial_states;

        if tail_prefix <> '' then
        begin
            tail_partial_bonus := c_light_sentence_tail_partial_bonus;
            if not can_predict_one_letter_tail_local(tail_prefix) then
            begin
                Inc(tail_partial_bonus, c_light_sentence_tail_prediction_bonus);
            end;
            append_light_candidate(final_states[0].text, tail_prefix,
                final_states[0].path, final_states[0].score + tail_partial_bonus,
                final_states[0].lexical_rank_score,
                final_states[0].fuzzy_cost, final_states[0].fuzzy_rules);

            tail_text := '';
            if try_predict_tail_single_char(tail_prefix, final_states[0].prev_prev_text,
                final_states[0].prev_text, tail_text) then
            begin
                append_light_candidate(final_states[0].text + tail_text, '',
                    final_states[0].path + c_segment_path_separator + tail_text,
                    final_states[0].score + c_light_sentence_tail_prediction_bonus,
                    final_states[0].lexical_rank_score + c_light_sentence_tail_prediction_bonus,
                    final_states[0].fuzzy_cost, final_states[0].fuzzy_rules);
            end;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
        finally
            static_multi_adjustment_cache.Free;
        end;
    end;

    function build_exact_lattice_sentence_backup_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_lattice_beam_width = 6;
        c_lattice_emit_limit = 2;
        c_lattice_max_segment_syllables = 5;
        c_lattice_exact_probe_limit = 4;
        c_lattice_segment_penalty = 260;
        c_lattice_single_penalty = 1250;
        c_lattice_multi_bonus = 460;
        c_lattice_long_phrase_bonus = 720;
        c_lattice_context_scale = 4;
        c_lattice_trigram_scale = 6;
        c_lattice_work_limit = 5000;
    type
        TExactLatticeState = record
            score: Integer;
            text: string;
            path: string;
            prev_text: string;
            prev_prev_text: string;
            segments: Integer;
            single_segments: Integer;
        end;
        TExactLatticeStateArray = TArray<TExactLatticeState>;
    var
        normalized_query_local: string;
        syllables_local: TncPinyinParseResult;
        state_map_local: array of TExactLatticeStateArray;
        final_states_local: TExactLatticeStateArray;
        syllable_count_local: Integer;
        start_tick_local: UInt64;
        lattice_work_count_local: Int64;

        function budget_exceeded_local: Boolean;
        var
            timed_budget_ms_local: UInt64;
            emergency_budget_ms_local: UInt64;
        begin
            if m_composition_built_incrementally then
            begin
                timed_budget_ms_local := 45;
                emergency_budget_ms_local := 180;
            end
            else
            begin
                timed_budget_ms_local := 0;
                emergency_budget_ms_local := 0;
            end;
            Result := search_budget_should_stop(start_tick_local,
                lattice_work_count_local, c_lattice_work_limit,
                timed_budget_ms_local, emergency_budget_ms_local);
        end;

        function build_key_local(const start_idx: Integer;
            const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (unit_count <= 0) or
                (start_idx + unit_count > syllable_count_local) then
            begin
                Exit;
            end;

            for unit_idx := start_idx to start_idx + unit_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[unit_idx].text);
            end;
        end;

        function get_candidate_weight_local(const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
        end;

        function normalize_segment_weight_local(const weight_value: Integer;
            const unit_count: Integer): Integer;
        begin
            Result := weight_value;
            if unit_count = 1 then
            begin
                if Result > 900 then
                begin
                    Result := 900 + ((Result - 900) div 8);
                end;
            end
            else if unit_count = 2 then
            begin
                if Result > 900 then
                begin
                    Result := 900 + ((Result - 900) div 5);
                end;
            end
            else if Result > 1800 then
            begin
                Result := 1800 + ((Result - 1800) div 3);
            end;
        end;

        function get_context_score_local(const left_text: string;
            const right_text: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (left_text = '') or (right_text = '') then
            begin
                Exit;
            end;
            Result := m_dictionary.get_context_bonus(left_text, right_text) *
                c_lattice_context_scale;
        end;

        function get_trigram_score_local(const prev_prev_text: string;
            const prev_text: string; const right_text: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (prev_prev_text = '') or
                (prev_text = '') or (right_text = '') then
            begin
                Exit;
            end;
            Result := m_dictionary.get_context_trigram_bonus(prev_prev_text,
                prev_text, right_text) * c_lattice_trigram_scale;
        end;

        procedure add_state_local(var states: TExactLatticeStateArray;
            const state_value: TExactLatticeState);
        var
            idx: Integer;
            insert_idx: Integer;
            temp_state: TExactLatticeState;
        begin
            if is_debug_target_recall_prefix(state_value.text) then
            begin
                m_debug_target_max_generated_syllable := Max(
                    m_debug_target_max_generated_syllable,
                    get_candidate_text_unit_count(state_value.text));
            end;

            for idx := 0 to High(states) do
            begin
                if SameText(states[idx].text, state_value.text) then
                begin
                    if state_value.score > states[idx].score then
                    begin
                        states[idx] := state_value;
                    end;
                    Exit;
                end;
            end;

            insert_idx := Length(states);
            SetLength(states, insert_idx + 1);
            states[insert_idx] := state_value;
            while (insert_idx > 0) and
                (states[insert_idx].score > states[insert_idx - 1].score) do
            begin
                temp_state := states[insert_idx - 1];
                states[insert_idx - 1] := states[insert_idx];
                states[insert_idx] := temp_state;
                Dec(insert_idx);
            end;

            if Length(states) > c_lattice_beam_width then
            begin
                SetLength(states, c_lattice_beam_width);
            end;

            for idx := 0 to High(states) do
            begin
                if is_debug_target_recall_prefix(states[idx].text) then
                begin
                    m_debug_target_max_retained_syllable := Max(
                        m_debug_target_max_retained_syllable,
                        get_candidate_text_unit_count(states[idx].text));
                end;
            end;
        end;

        procedure append_output_candidate_local(
            const state_value: TExactLatticeState);
        var
            next_idx: Integer;
        begin
            if Trim(state_value.text) = '' then
            begin
                Exit;
            end;
            for next_idx := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[next_idx].text),
                    Trim(state_value.text)) then
                begin
                    Exit;
                end;
            end;

            next_idx := Length(out_candidates);
            SetLength(out_candidates, next_idx + 1);
            out_candidates[next_idx].text := Trim(state_value.text);
            out_candidates[next_idx].comment := '';
            out_candidates[next_idx].score := state_value.score;
            out_candidates[next_idx].source := cs_rule;
            out_candidates[next_idx].has_dict_weight := False;
            out_candidates[next_idx].dict_weight := 0;
            remember_segment_path_for_candidate(out_candidates[next_idx].text,
                '', state_value.path, state_value.score);
        end;

        procedure extend_with_candidate_local(const base_state: TExactLatticeState;
            const pos_idx: Integer; const seg_len: Integer;
            const candidate: TncCandidate; const candidate_rank: Integer);
        var
            candidate_text: string;
            candidate_score: Integer;
            next_state: TExactLatticeState;
        begin
            candidate_text := Trim(candidate.text);
            if (candidate_text = '') or (Trim(candidate.comment) <> '') or
                (get_candidate_text_unit_count(candidate_text) <> seg_len) then
            begin
                Exit;
            end;
            Inc(lattice_work_count_local);

            candidate_score := normalize_segment_weight_local(
                get_candidate_weight_local(candidate), seg_len);
            Dec(candidate_score, candidate_rank * 180);
            Dec(candidate_score, c_lattice_segment_penalty);
            if seg_len = 1 then
            begin
                Dec(candidate_score, c_lattice_single_penalty);
            end
            else
            begin
                Inc(candidate_score, c_lattice_multi_bonus * seg_len);
                if seg_len >= 3 then
                begin
                    Inc(candidate_score, c_lattice_long_phrase_bonus);
                end;
            end;
            if candidate.source = cs_user then
            begin
                Inc(candidate_score, 2200);
            end;
            if pos_idx + seg_len = syllable_count_local then
            begin
                Inc(candidate_score, 420);
            end;
            Inc(candidate_score, get_context_score_local(base_state.prev_text,
                candidate_text));
            Inc(candidate_score, get_trigram_score_local(
                base_state.prev_prev_text, base_state.prev_text, candidate_text));

            next_state.score := base_state.score + candidate_score;
            if base_state.text <> '' then
            begin
                next_state.text := base_state.text + candidate_text;
                next_state.path := base_state.path + c_segment_path_separator +
                    candidate_text;
            end
            else
            begin
                next_state.text := candidate_text;
                next_state.path := candidate_text;
            end;
            next_state.prev_prev_text := base_state.prev_text;
            next_state.prev_text := candidate_text;
            next_state.segments := base_state.segments + 1;
            next_state.single_segments := base_state.single_segments;
            if seg_len = 1 then
            begin
                Inc(next_state.single_segments);
            end;
            if next_state.single_segments > (syllable_count_local div 2) + 1 then
            begin
                Dec(next_state.score,
                    (next_state.single_segments - ((syllable_count_local div 2) + 1)) *
                    900);
            end;
            add_state_local(state_map_local[pos_idx + seg_len], next_state);
        end;

        procedure sort_final_states_local(var states: TExactLatticeStateArray);
        var
            pass_idx: Integer;
            idx: Integer;
            temp_state: TExactLatticeState;
        begin
            for pass_idx := 0 to High(states) do
            begin
                for idx := 0 to High(states) - 1 do
                begin
                    if states[idx].score < states[idx + 1].score then
                    begin
                        temp_state := states[idx];
                        states[idx] := states[idx + 1];
                        states[idx + 1] := temp_state;
                    end;
                end;
            end;
        end;
    var
        pos_idx: Integer;
        state_idx: Integer;
        seg_len: Integer;
        query_key_local: string;
        exact_results_local: TncCandidateList;
        exact_idx: Integer;
        emit_idx: Integer;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if (m_dictionary = nil) or (Trim(query_text) = '') or
            (not is_full_pinyin_key(query_text)) then
        begin
            Exit;
        end;

        normalized_query_local := normalize_pinyin_text(query_text);
        syllables_local := get_effective_compact_pinyin_syllables(
            normalized_query_local);
        syllable_count_local := Length(syllables_local);
        if (syllable_count_local < 7) or (syllable_count_local > 40) then
        begin
            Exit;
        end;

        start_tick_local := nc_monotonic_tick_ms;
        lattice_work_count_local := 0;
        SetLength(state_map_local, syllable_count_local + 1);
        SetLength(state_map_local[0], 1);
        state_map_local[0][0].score := 0;
        state_map_local[0][0].text := '';
        state_map_local[0][0].path := '';
        state_map_local[0][0].prev_text := '';
        state_map_local[0][0].prev_prev_text := '';
        state_map_local[0][0].segments := 0;
        state_map_local[0][0].single_segments := 0;

        for pos_idx := 0 to syllable_count_local - 1 do
        begin
            if budget_exceeded_local then
            begin
                Break;
            end;
            if Length(state_map_local[pos_idx]) = 0 then
            begin
                Continue;
            end;

            for state_idx := 0 to High(state_map_local[pos_idx]) do
            begin
                for seg_len := Min(c_lattice_max_segment_syllables,
                    syllable_count_local - pos_idx) downto 1 do
                begin
                    query_key_local := build_key_local(pos_idx, seg_len);
                    if (query_key_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(
                        query_key_local, exact_results_local)) then
                    begin
                        Continue;
                    end;
                    for exact_idx := 0 to High(exact_results_local) do
                    begin
                        if budget_exceeded_local then
                        begin
                            Break;
                        end;
                        if exact_idx >= c_lattice_exact_probe_limit then
                        begin
                            Break;
                        end;
                        extend_with_candidate_local(
                            state_map_local[pos_idx][state_idx], pos_idx,
                            seg_len, exact_results_local[exact_idx], exact_idx);
                    end;
                end;
            end;
        end;

        final_states_local := state_map_local[syllable_count_local];
        if Length(final_states_local) = 0 then
        begin
            Exit;
        end;

        sort_final_states_local(final_states_local);
        if m_debug_target_recall_text <> '' then
        begin
            m_debug_target_final_state_count := Max(
                m_debug_target_final_state_count, Length(final_states_local));
            for emit_idx := 0 to High(final_states_local) do
            begin
                if SameText(Trim(final_states_local[emit_idx].text),
                    m_debug_target_recall_text) then
                begin
                    m_debug_target_final_state_source_mask :=
                        m_debug_target_final_state_source_mask or 4;
                    if (m_debug_target_lattice_final_state_rank = 0) or
                        (emit_idx + 1 < m_debug_target_lattice_final_state_rank) then
                    begin
                        m_debug_target_lattice_final_state_rank := emit_idx + 1;
                    end;
                    if (not m_debug_target_final_state_found) or
                        (emit_idx + 1 < m_debug_target_final_state_rank) then
                    begin
                        m_debug_target_final_state_found := True;
                        m_debug_target_final_state_rank := emit_idx + 1;
                    end;
                end;
            end;
        end;
        for emit_idx := 0 to Min(c_lattice_emit_limit - 1,
            High(final_states_local)) do
        begin
            append_output_candidate_local(final_states_local[emit_idx]);
        end;

        Result := Length(out_candidates) > 0;
    end;

    procedure ensure_exact_lattice_backup_second_visible(
        var candidates: TncCandidateList);
    var
        backup_idx: Integer;
        existing_idx: Integer;
        move_idx: Integer;
        backup_candidate_local: TncCandidate;
        second_text_local: string;
        top_text_local: string;
    begin
        if (Length(candidates) = 0) or
            (Length(exact_lattice_backup_candidates) = 0) or
            (input_syllable_count < 7) or (not is_full_pinyin_key(lookup_text)) or
            all_initial_compact_query or has_internal_dangling_initial then
        begin
            Exit;
        end;

        if Length(candidates) > 1 then
        begin
            second_text_local := Trim(candidates[1].text);
            if (Trim(candidates[1].comment) = '') and
                (second_text_local <> '') and
                (get_candidate_text_unit_count(second_text_local) =
                input_syllable_count) then
            begin
                Exit;
            end;
        end;

        top_text_local := Trim(candidates[0].text);
        for backup_idx := 0 to High(exact_lattice_backup_candidates) do
        begin
            backup_candidate_local := exact_lattice_backup_candidates[backup_idx];
            if Trim(backup_candidate_local.comment) <> '' then
            begin
                Continue;
            end;
            if SameText(Trim(backup_candidate_local.text), top_text_local) then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(backup_candidate_local.text)) <>
                input_syllable_count then
            begin
                Continue;
            end;

            existing_idx := -1;
            for move_idx := 0 to High(candidates) do
            begin
                if SameText(Trim(candidates[move_idx].text),
                    Trim(backup_candidate_local.text)) and
                    (Trim(candidates[move_idx].comment) = '') then
                begin
                    existing_idx := move_idx;
                    Break;
                end;
            end;

            if existing_idx = 1 then
            begin
                Exit;
            end;
            if existing_idx > 1 then
            begin
                backup_candidate_local := candidates[existing_idx];
                for move_idx := existing_idx downto 2 do
                begin
                    candidates[move_idx] := candidates[move_idx - 1];
                end;
                candidates[1] := backup_candidate_local;
            end
            else
            begin
                SetLength(candidates, Length(candidates) + 1);
                for move_idx := High(candidates) downto 2 do
                begin
                    candidates[move_idx] := candidates[move_idx - 1];
                end;
                candidates[1] := backup_candidate_local;
            end;

            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' elat2=[%s]', [Trim(backup_candidate_local.text)]);
            end;
            Exit;
        end;
    end;

    function build_strict_chunk_oracle_sentence_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_enable_nested_oracle: Boolean = False;
        c_oracle_beam_width = 10;
        c_oracle_emit_limit = 4;
        c_oracle_probe_limit = 6;
        c_oracle_max_segment_syllables = 5;
        c_oracle_single_penalty = 420;
        c_oracle_additional_segment_penalty = 140;
        c_oracle_multi_bonus = 360;
        c_oracle_rank_step = 260;
        c_oracle_final_complete_bonus = 480;
        c_oracle_anchor_bonus = 220;
        c_oracle_standalone_match_bonus = 3200;
        c_oracle_standalone_mismatch_penalty = 2400;
        c_oracle_budget_ms = 180;
        // Bound direct/offline work while allowing a wider search than input.
        c_oracle_direct_budget_ms = 720;
        c_oracle_work_limit = 16384;
    type
        TOracleState = record
            score: Integer;
            text: string;
            path: string;
            prev_text: string;
            prev_prev_text: string;
        end;
        TOracleStateArray = TArray<TOracleState>;
        TOracleSegmentOption = record
            text: string;
            path_text: string;
            score: Integer;
            rank: Integer;
        end;
        TOracleSegmentOptionArray = TArray<TOracleSegmentOption>;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        state_map: array of TOracleStateArray;
        final_states: TOracleStateArray;
        head_syllable_count_local: Integer;
        nested_option_states: TDictionary<string, Integer>;
        nested_option_cache: TDictionary<string, TOracleSegmentOptionArray>;
        nested_engine: TncEngine;
        nested_engine_config: TncEngineConfig;
        single_option_cache: TArray<TOracleSegmentOptionArray>;
        single_option_cache_prepared: TArray<Boolean>;
        merged_result_cache: TDictionary<string, TncCandidateList>;
        oracle_start_tick: UInt64;
        oracle_work_count: Int64;
        oracle_timed_out: Boolean;

        function oracle_budget_exhausted_local: Boolean;
        var
            budget_ms_local: UInt64;
        begin
            if m_composition_built_incrementally then
            begin
                budget_ms_local := c_oracle_budget_ms;
            end
            else
            begin
                budget_ms_local := c_oracle_direct_budget_ms;
            end;
            Inc(oracle_work_count);
            Result := search_budget_should_stop(oracle_start_tick,
                oracle_work_count, c_oracle_work_limit, budget_ms_local,
                budget_ms_local * 2,
                head_syllable_count_local >=
                c_long_sentence_full_path_min_syllables);
            if Result and (not oracle_timed_out) then
            begin
                oracle_timed_out := True;
                if m_debug_target_recall_text <> '' then
                begin
                    m_debug_target_full_path_budget_exhausted := True;
                end;
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' oracle_budget=[mode=%d work=%d elapsed=%d]',
                        [Ord(m_search_budget_mode), oracle_work_count,
                        nc_monotonic_tick_ms - oracle_start_tick]);
                end;
            end;
        end;

        function build_syllable_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            idx: Integer;
        begin
            Result := '';
            for idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(syllables_local[idx].text);
            end;
        end;

        function get_oracle_context_bonus_local(const left_text: string;
            const right_text: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (left_text = '') or (right_text = '') then
            begin
                Exit;
            end;

            Result := m_dictionary.get_context_bonus(left_text, right_text) * 4;
        end;

        function get_oracle_trigram_bonus_local(const left_left_text: string;
            const left_text: string; const right_text: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (left_left_text = '') or (left_text = '') or
                (right_text = '') then
            begin
                Exit;
            end;

            Result := m_dictionary.get_context_trigram_bonus(left_left_text,
                left_text, right_text) * 6;
        end;

        function get_fixed_oracle_single_char(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'dan' then
            begin
                Result := string(Char($4F46));
            end
            else if normalized_syllable = 'sui' then
            begin
                Result := string(Char($867D));
            end
            else if normalized_syllable = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'yu' then
            begin
                Result := string(Char($4E8E));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'bei' then
            begin
                Result := string(Char($88AB));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end;
        end;

        function segment_has_fixed_oracle_boundary_local(
            const start_idx: Integer; const syllable_count_local: Integer): Boolean;
        var
            unit_idx_local: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local > head_syllable_count_local) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to syllable_count_local - 1 do
            begin
                if get_fixed_oracle_single_char(
                    normalize_pinyin_text(
                    syllables_local[start_idx + unit_idx_local].text)) <> '' then
                begin
                    Exit(True);
                end;
            end;
        end;

        function oracle_candidate_respects_fixed_boundaries_local(
            const start_idx: Integer; const syllable_count_local: Integer;
            const text_value: string): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            expected_char_local: string;
        begin
            Result := False;
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local > head_syllable_count_local) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(text_value));
            if Length(text_units_local) <> syllable_count_local then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to syllable_count_local - 1 do
            begin
                expected_char_local := get_fixed_oracle_single_char(
                    normalize_pinyin_text(
                    syllables_local[start_idx + unit_idx_local].text));
                if (expected_char_local <> '') and
                    (not SameText(text_units_local[unit_idx_local],
                    expected_char_local)) then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function is_allowed_oracle_single_char(const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
                $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $53EA, $628A, $88AB,
                $8BA9, $7ED9, $5411, $5BF9, $8DDF, $540C, $4ECE, $4E0E, $4E8E, $5C31,
                $4E5F, $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2,
                $5C06, $5230, $7684, $4E86, $7740, $8FC7, $4F46, $867D, $800C, $5E76,
                $5374:
                    Result := True;
            end;
        end;

        function is_preferred_oracle_single_char_candidate_local(
            const syllable_key_local: string; const value: string;
            const effective_weight_local: Integer): Boolean;
        const
            c_oracle_single_probe_limit = 8;
            c_oracle_preferred_max_rank = 3;
            c_oracle_top_ratio_pct = 85;
        var
            exact_single_results_local: TncCandidateList;
            idx_local: Integer;
            candidate_text_local: string;
            weight_value_local: Integer;
            top_weight_value_local: Integer;
            single_char_rank_local: Integer;
            fixed_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (syllable_key_local = '') or
                (Length(Trim(value)) <> 1) then
            begin
                Exit;
            end;

            fixed_text_local := get_fixed_oracle_single_char(syllable_key_local);
            if fixed_text_local <> '' then
            begin
                Exit(SameText(fixed_text_local, value));
            end;

            if not m_dictionary.single_char_matches_pinyin(syllable_key_local,
                value) then
            begin
                Exit(False);
            end;

            if not dictionary_exact_lookup_cached(syllable_key_local,
                exact_single_results_local) then
            begin
                Exit(True);
            end;

            top_weight_value_local := 0;
            single_char_rank_local := 0;
            for idx_local := 0 to High(exact_single_results_local) do
            begin
                if idx_local >= c_oracle_single_probe_limit then
                begin
                    Break;
                end;

                candidate_text_local := Trim(exact_single_results_local[idx_local].text);
                if (candidate_text_local = '') or
                    (Trim(exact_single_results_local[idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <> 1) then
                begin
                    Continue;
                end;

                Inc(single_char_rank_local);
                if exact_single_results_local[idx_local].has_dict_weight then
                begin
                    weight_value_local :=
                        exact_single_results_local[idx_local].dict_weight;
                end
                else
                begin
                    weight_value_local := exact_single_results_local[idx_local].score;
                end;

                if top_weight_value_local <= 0 then
                begin
                    top_weight_value_local := weight_value_local;
                end;

                if not SameText(candidate_text_local, value) then
                begin
                    Continue;
                end;

                Exit((single_char_rank_local <= c_oracle_preferred_max_rank) or
                    (top_weight_value_local <= 0) or
                    ((effective_weight_local * 100) >=
                    (top_weight_value_local * c_oracle_top_ratio_pct)));
            end;
        end;

        function is_extended_allowed_oracle_single_char_local(
            const syllable_key_local: string; const value: string;
            const effective_weight_local: Integer): Boolean;
        const
            c_oracle_extended_single_min_weight = 300;
        var
            fixed_text_local: string;
        begin
            fixed_text_local := get_fixed_oracle_single_char(syllable_key_local);
            if fixed_text_local <> '' then
            begin
                Exit(SameText(fixed_text_local, value));
            end;

            Result := is_allowed_oracle_single_char(value);
            if Result then
            begin
                Exit;
            end;

            if head_syllable_count_local < 8 then
            begin
                Exit(False);
            end;
            if (Length(Trim(value)) <> 1) or
                (effective_weight_local < c_oracle_extended_single_min_weight) then
            begin
                Exit(False);
            end;

            Result := is_preferred_oracle_single_char_candidate_local(
                syllable_key_local, value, effective_weight_local);
        end;

        procedure get_oracle_single_options_local(const position_local: Integer;
            const syllable_key_local: string;
            out out_options: TOracleSegmentOptionArray);
        var
            exact_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            effective_weight_local: Integer;
            option_local: TOracleSegmentOption;
        begin
            SetLength(out_options, 0);
            if (position_local < 0) or
                (position_local > High(single_option_cache_prepared)) then
            begin
                Exit;
            end;
            if single_option_cache_prepared[position_local] then
            begin
                out_options := Copy(single_option_cache[position_local]);
                Exit;
            end;

            single_option_cache_prepared[position_local] := True;
            if not dictionary_exact_lookup_cached(syllable_key_local,
                exact_results_local) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to Min(c_oracle_probe_limit - 1,
                High(exact_results_local)) do
            begin
                if Trim(exact_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[result_idx_local].text);
                if get_candidate_text_unit_count(candidate_text_local) <> 1 then
                begin
                    Continue;
                end;

                effective_weight_local := exact_results_local[result_idx_local].score;
                if exact_results_local[result_idx_local].has_dict_weight and
                    (exact_results_local[result_idx_local].dict_weight >
                    effective_weight_local) then
                begin
                    effective_weight_local :=
                        exact_results_local[result_idx_local].dict_weight;
                end;
                if not is_extended_allowed_oracle_single_char_local(
                    syllable_key_local, candidate_text_local,
                    effective_weight_local) then
                begin
                    Continue;
                end;

                option_local := Default(TOracleSegmentOption);
                option_local.text := candidate_text_local;
                option_local.path_text := candidate_text_local;
                option_local.score := effective_weight_local;
                option_local.rank := result_idx_local;
                SetLength(out_options, Length(out_options) + 1);
                out_options[High(out_options)] := option_local;
            end;
            single_option_cache[position_local] := Copy(out_options);
        end;

        function normalize_oracle_segment_weight_local(const raw_weight: Integer;
            const syllable_count_local: Integer): Integer;
        begin
            Result := raw_weight;
            if syllable_count_local = 2 then
            begin
                if Result > 240 then
                begin
                    Result := 240 + ((Result - 240) div 10);
                end;
            end;
        end;

        function try_get_best_oracle_standalone_query_candidate_local(
            const query_key_local: string; const expected_syllables_local: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        const
            c_probe_limit_local = 8;
        var
            cached_state_local: Integer;
            snapshot_candidates_local: TncCandidateList;
            snapshot_scores_local: TArray<Integer>;
            candidate_idx_local: Integer;
            candidate_local: TncCandidate;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_score_local: Integer;
            best_text_local: string;
            best_score_local: Integer;
            preferred_exact_bonus_local: Integer;
            preferred_text_local: string;
            preferred_bonus_local: Integer;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            preferred_text_local := '';
            preferred_bonus_local := 0;
            if (query_key_local = '') or
                (not is_full_pinyin_key(query_key_local)) or
                (expected_syllables_local < 2) or
                (expected_syllables_local > 8) or
                (m_disable_subspan_standalone_oracle > 0) then
            begin
                Exit;
            end;

            try_get_preferred_query_phrase_text_local(query_key_local,
                preferred_text_local, preferred_bonus_local);

            if subspan_oracle_states = nil then
            begin
                subspan_oracle_states := TDictionary<string, Integer>.Create;
                subspan_oracle_texts := TDictionary<string, string>.Create;
                subspan_oracle_scores := TDictionary<string, Integer>.Create;
            end;

            if subspan_oracle_states.TryGetValue(query_key_local,
                cached_state_local) then
            begin
                if cached_state_local = 1 then
                begin
                    subspan_oracle_texts.TryGetValue(query_key_local, out_text);
                    subspan_oracle_scores.TryGetValue(query_key_local, out_score);
                    Exit(out_text <> '');
                end;
                Exit(False);
            end;

            if subspan_oracle_budget_remaining <= 0 then
            begin
                Exit;
            end;

            if subspan_oracle_engine = nil then
            begin
                subspan_oracle_config := m_config;
                subspan_oracle_config.debug_mode := False;
                subspan_oracle_config.max_candidates := 6;
                subspan_oracle_engine := TncEngine.Create(
                    subspan_oracle_config, False, False);
                inherit_search_budget_policy_to(subspan_oracle_engine);
            end;

            Dec(subspan_oracle_budget_remaining);
            subspan_oracle_states.AddOrSetValue(query_key_local, 3);
            subspan_oracle_engine.m_disable_subspan_standalone_oracle :=
                m_disable_subspan_standalone_oracle + 1;
            subspan_oracle_engine.reset;
            subspan_oracle_engine.m_composition_text := query_key_local;
            subspan_oracle_engine.build_candidates;

            SetLength(snapshot_candidates_local,
                Length(subspan_oracle_engine.m_candidates));
            SetLength(snapshot_scores_local,
                Length(subspan_oracle_engine.m_candidates));
            for candidate_idx_local := 0 to High(subspan_oracle_engine.m_candidates) do
            begin
                snapshot_candidates_local[candidate_idx_local] :=
                    subspan_oracle_engine.m_candidates[candidate_idx_local];
                snapshot_scores_local[candidate_idx_local] :=
                    subspan_oracle_engine.get_rank_score(
                    subspan_oracle_engine.m_candidates[candidate_idx_local]);
            end;

            best_text_local := '';
            best_score_local := Low(Integer);
            for candidate_idx_local := 0 to High(snapshot_candidates_local) do
            begin
                if candidate_idx_local >= c_probe_limit_local then
                begin
                    Break;
                end;

                candidate_local := snapshot_candidates_local[candidate_idx_local];
                if Trim(candidate_local.comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(candidate_local.text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if candidate_units_local <> expected_syllables_local then
                begin
                    Continue;
                end;

                candidate_score_local :=
                    snapshot_scores_local[candidate_idx_local] +
                    get_preferred_query_phrase_bonus_local(query_key_local,
                    candidate_text_local);
                preferred_exact_bonus_local :=
                    get_preferred_exact_subspan_bonus_local(query_key_local,
                    candidate_text_local);
                if preferred_exact_bonus_local > 0 then
                begin
                    Inc(candidate_score_local,
                        preferred_exact_bonus_local + 12000);
                end;
                support_path_local := infer_segment_path_for_query_text_with_score(
                    query_key_local, candidate_text_local, support_score_local,
                    support_segments_local);
                if support_path_local <> '' then
                begin
                    Inc(candidate_score_local, support_score_local);
                    Inc(candidate_score_local,
                        Max(0, 2048 - (support_segments_local * 128)));
                end
                else
                begin
                    Dec(candidate_score_local, 2048);
                end;

                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_text_local := candidate_text_local;
                end;
            end;

            if best_text_local <> '' then
            begin
                out_text := best_text_local;
                out_score := best_score_local;
                subspan_oracle_states.AddOrSetValue(query_key_local, 1);
                subspan_oracle_texts.AddOrSetValue(query_key_local, out_text);
                subspan_oracle_scores.AddOrSetValue(query_key_local, out_score);
                Exit(True);
            end;

            subspan_oracle_states.AddOrSetValue(query_key_local, 2);
        end;

        function is_oracle_segment_option_better_local(
            const left_option: TOracleSegmentOption;
            const right_option: TOracleSegmentOption): Boolean;
        begin
            Result := False;
            if left_option.score > right_option.score then
            begin
                Exit(True);
            end;
            if left_option.score < right_option.score then
            begin
                Exit(False);
            end;
            if left_option.rank < right_option.rank then
            begin
                Exit(True);
            end;
            if left_option.rank > right_option.rank then
            begin
                Exit(False);
            end;
            if (left_option.text <> '') and
                ((right_option.text = '') or
                (CompareText(left_option.text, right_option.text) < 0)) then
            begin
                Exit(True);
            end;
        end;

        procedure add_oracle_segment_option_local(
            var options: TOracleSegmentOptionArray;
            const option_value: TOracleSegmentOption);
        const
            c_nested_emit_limit = 4;
        var
            option_idx_local: Integer;
            insert_idx_local: Integer;
            tmp_option_local: TOracleSegmentOption;
        begin
            if Trim(option_value.text) = '' then
            begin
                Exit;
            end;

            for option_idx_local := 0 to High(options) do
            begin
                if SameText(options[option_idx_local].text, option_value.text) then
                begin
                    if is_oracle_segment_option_better_local(option_value,
                        options[option_idx_local]) then
                    begin
                        options[option_idx_local] := option_value;
                    end;
                    Exit;
                end;
            end;

            insert_idx_local := Length(options);
            SetLength(options, insert_idx_local + 1);
            options[insert_idx_local] := option_value;
            while (insert_idx_local > 0) and
                is_oracle_segment_option_better_local(options[insert_idx_local],
                options[insert_idx_local - 1]) do
            begin
                tmp_option_local := options[insert_idx_local - 1];
                options[insert_idx_local - 1] := options[insert_idx_local];
                options[insert_idx_local] := tmp_option_local;
                Dec(insert_idx_local);
            end;

            if Length(options) > c_nested_emit_limit then
            begin
                SetLength(options, c_nested_emit_limit);
            end;
        end;

        function collect_nested_oracle_segment_options_local(
            const query_key_local: string; const expected_units_local: Integer;
            out out_options: TOracleSegmentOptionArray): Boolean;
        const
            c_nested_probe_limit = 8;
        var
            cache_state_local: Integer;
            candidate_idx_local: Integer;
            nested_candidate_local: TncCandidate;
            candidate_text_local: string;
            candidate_units_local: Integer;
            candidate_rank_score_local: Integer;
            option_local: TOracleSegmentOption;
            path_text_local: string;
        begin
            SetLength(out_options, 0);
            if not c_enable_nested_oracle then
            begin
                Exit(False);
            end;
            if (query_key_local = '') or (expected_units_local < 2) then
            begin
                Exit(False);
            end;

            if nested_option_states.TryGetValue(query_key_local, cache_state_local) then
            begin
                if (cache_state_local = 1) and
                    nested_option_cache.TryGetValue(query_key_local, out_options) then
                begin
                    Exit(Length(out_options) > 0);
                end;
                Exit(False);
            end;

            if nested_engine = nil then
            begin
                nested_engine_config := m_config;
                nested_engine_config.debug_mode := False;
                nested_engine := TncEngine.Create(nested_engine_config,
                    False, False);
                inherit_search_budget_policy_to(nested_engine);
                nested_engine.m_disable_subspan_standalone_oracle :=
                    m_disable_subspan_standalone_oracle + 1;
            end;

            nested_engine.m_composition_text := query_key_local;
            nested_engine.build_candidates;

            for candidate_idx_local := 0 to High(nested_engine.m_candidates) do
            begin
                if candidate_idx_local >= c_nested_probe_limit then
                begin
                    Break;
                end;

                nested_candidate_local := nested_engine.m_candidates[candidate_idx_local];
                if Trim(nested_candidate_local.comment) <> '' then
                begin
                    Continue;
                end;
                if nested_engine.is_runtime_chain_candidate(nested_candidate_local) or
                    nested_engine.is_runtime_common_pattern_candidate(
                    nested_candidate_local) or
                    nested_engine.is_runtime_redup_candidate(nested_candidate_local) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(nested_candidate_local.text);
                if candidate_text_local = '' then
                begin
                    Continue;
                end;
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if candidate_units_local <> expected_units_local then
                begin
                    Continue;
                end;

                candidate_rank_score_local :=
                    nested_engine.get_rank_score(nested_candidate_local);
                if candidate_rank_score_local < 0 then
                begin
                    candidate_rank_score_local := 0;
                end;
                candidate_rank_score_local := normalize_oracle_segment_weight_local(
                    Max(120, candidate_rank_score_local div 48),
                    expected_units_local);
                Inc(candidate_rank_score_local,
                    (c_nested_probe_limit - candidate_idx_local) *
                    (c_oracle_rank_step div 2));
                if expected_units_local >= 3 then
                begin
                    Inc(candidate_rank_score_local, 96);
                end;

                path_text_local := nested_engine.get_segment_path_for_candidate(
                    nested_candidate_local, candidate_idx_local);
                if Trim(path_text_local) = '' then
                begin
                    path_text_local := candidate_text_local;
                end;

                option_local := Default(TOracleSegmentOption);
                option_local.text := candidate_text_local;
                option_local.path_text := path_text_local;
                option_local.score := candidate_rank_score_local;
                option_local.rank := candidate_idx_local;
                add_oracle_segment_option_local(out_options, option_local);
            end;

            if Length(out_options) > 0 then
            begin
                nested_option_states.AddOrSetValue(query_key_local, 1);
                nested_option_cache.AddOrSetValue(query_key_local,
                    Copy(out_options));
                Exit(True);
            end;

            nested_option_states.AddOrSetValue(query_key_local, 2);
            Result := False;
        end;

        procedure add_state(var states: TOracleStateArray;
            const candidate_state: TOracleState);
        var
            idx: Integer;
            insert_idx: Integer;
            tmp_state: TOracleState;
        begin
            if is_debug_target_recall_prefix(candidate_state.text) then
            begin
                m_debug_target_max_generated_syllable := Max(
                    m_debug_target_max_generated_syllable,
                    get_candidate_text_unit_count(candidate_state.text));
            end;

            for idx := 0 to High(states) do
            begin
                if states[idx].path = candidate_state.path then
                begin
                    if candidate_state.score > states[idx].score then
                    begin
                        states[idx] := candidate_state;
                    end;
                    Exit;
                end;
            end;

            insert_idx := Length(states);
            SetLength(states, insert_idx + 1);
            states[insert_idx] := candidate_state;
            while (insert_idx > 0) and
                (states[insert_idx].score > states[insert_idx - 1].score) do
            begin
                tmp_state := states[insert_idx - 1];
                states[insert_idx - 1] := states[insert_idx];
                states[insert_idx] := tmp_state;
                Dec(insert_idx);
            end;

            if Length(states) > c_oracle_beam_width then
            begin
                SetLength(states, c_oracle_beam_width);
            end;

            for idx := 0 to High(states) do
            begin
                if is_debug_target_recall_prefix(states[idx].text) then
                begin
                    m_debug_target_max_retained_syllable := Max(
                        m_debug_target_max_retained_syllable,
                        get_candidate_text_unit_count(states[idx].text));
                end;
            end;
        end;

        procedure append_oracle_candidate(const text_value: string;
            const path_value: string; const score_value: Integer);
        var
            candidate: TncCandidate;
            next_index: Integer;
        begin
            if Trim(text_value) = '' then
            begin
                Exit;
            end;

            candidate.text := Trim(text_value);
            candidate.comment := '';
            candidate.score := score_value;
            candidate.source := cs_rule;
            candidate.has_dict_weight := False;
            candidate.dict_weight := 0;

            next_index := Length(out_candidates);
            SetLength(out_candidates, next_index + 1);
            out_candidates[next_index] := candidate;
            remember_segment_path_for_candidate(candidate.text, '',
                Trim(path_value), score_value);
        end;

    var
        pos: Integer;
        seg_len: Integer;
        next_pos: Integer;
        state_idx: Integer;
        base_state: TOracleState;
        candidate_state: TOracleState;
        seg_key: string;
        fixed_text: string;
        single_options: TOracleSegmentOptionArray;
        single_option: TOracleSegmentOption;
        lookup_results: TncCandidateList;
        exact_results: TncCandidateList;
        merged_results: TncCandidateList;
        exact_idx: Integer;
        merged_idx: Integer;
        duplicate_found: Boolean;
        candidate_text: string;
        candidate_units: Integer;
        effective_weight: Integer;
        result_idx: Integer;
        resolved_text_local: string;
        resolved_score_local: Integer;
        resolved_segments_local: Integer;
        resolved_path_local: string;
        resolved_has_anchor_local: Boolean;
        resolved_anchor_units_local: Integer;
        resolved_chunk_text_local: string;
        resolved_chunk_valid_local: Boolean;
        prefer_resolved_chunk_local: Boolean;
        allow_raw_merged_local: Boolean;
        nested_segment_options_local: TOracleSegmentOptionArray;
        nested_option_idx_local: Integer;
        nested_option_local: TOracleSegmentOption;
        standalone_query_text_local: string;
        standalone_query_score_local: Integer;
        standalone_query_candidate_local: TncCandidate;
        standalone_query_valid_local: Boolean;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        nested_option_states := nil;
        nested_option_cache := nil;
        nested_engine := nil;
        merged_result_cache := nil;
        oracle_start_tick := nc_monotonic_tick_ms;
        oracle_work_count := 0;
        oracle_timed_out := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(normalized_query);
        head_syllable_count_local := Length(syllables_local);
        if head_syllable_count_local < 4 then
        begin
            Exit;
        end;
        SetLength(single_option_cache, head_syllable_count_local);
        SetLength(single_option_cache_prepared, head_syllable_count_local);

        if c_enable_nested_oracle then
        begin
            nested_option_states := TDictionary<string, Integer>.Create;
            nested_option_cache := TDictionary<string, TOracleSegmentOptionArray>.Create;
        end;
        merged_result_cache := TDictionary<string, TncCandidateList>.Create;
        try
        SetLength(state_map, head_syllable_count_local + 1);
        SetLength(state_map[0], 1);
        state_map[0][0].score := 0;
        state_map[0][0].text := '';
        state_map[0][0].path := '';
        state_map[0][0].prev_text := '';
        state_map[0][0].prev_prev_text := '';

        for pos := 0 to head_syllable_count_local - 1 do
        begin
            if oracle_budget_exhausted_local then
            begin
                Exit(False);
            end;
            for state_idx := 0 to High(state_map[pos]) do
            begin
                if oracle_budget_exhausted_local then
                begin
                    Exit(False);
                end;
                base_state := state_map[pos][state_idx];

                seg_key := normalize_pinyin_text(syllables_local[pos].text);
                fixed_text := get_fixed_oracle_single_char(seg_key);
                if fixed_text <> '' then
                begin
                    candidate_state.score := base_state.score + 960 -
                        c_oracle_single_penalty;
                    if base_state.text <> '' then
                    begin
                        Dec(candidate_state.score,
                            c_oracle_additional_segment_penalty);
                        Inc(candidate_state.score,
                            get_oracle_context_bonus_local(base_state.prev_text,
                            fixed_text));
                        if base_state.prev_prev_text <> '' then
                        begin
                            Inc(candidate_state.score,
                                get_oracle_trigram_bonus_local(
                                base_state.prev_prev_text,
                                base_state.prev_text, fixed_text));
                        end;
                        candidate_state.text := base_state.text + fixed_text;
                        candidate_state.path := base_state.path +
                            c_segment_path_separator + fixed_text;
                    end
                    else
                    begin
                        candidate_state.text := fixed_text;
                        candidate_state.path := fixed_text;
                    end;
                    candidate_state.prev_prev_text := base_state.prev_text;
                    candidate_state.prev_text := fixed_text;
                    add_state(state_map[pos + 1], candidate_state);
                end
                else
                begin
                    get_oracle_single_options_local(pos, seg_key,
                        single_options);
                    for result_idx := 0 to High(single_options) do
                    begin
                        single_option := single_options[result_idx];
                        candidate_text := single_option.text;
                        effective_weight := single_option.score;

                        candidate_state.score := base_state.score + effective_weight -
                            c_oracle_single_penalty;
                        if base_state.text <> '' then
                        begin
                            Dec(candidate_state.score,
                                c_oracle_additional_segment_penalty);
                            Inc(candidate_state.score,
                                get_oracle_context_bonus_local(base_state.prev_text,
                                candidate_text));
                            if base_state.prev_prev_text <> '' then
                            begin
                                Inc(candidate_state.score,
                                    get_oracle_trigram_bonus_local(
                                    base_state.prev_prev_text,
                                    base_state.prev_text, candidate_text));
                            end;
                            candidate_state.text := base_state.text + candidate_text;
                            candidate_state.path := base_state.path +
                                c_segment_path_separator + candidate_text;
                        end
                        else
                        begin
                            candidate_state.text := candidate_text;
                            candidate_state.path := candidate_text;
                        end;
                        candidate_state.prev_prev_text := base_state.prev_text;
                        candidate_state.prev_text := candidate_text;
                        add_state(state_map[pos + 1], candidate_state);
                    end;
                end;

                for seg_len := 2 to Min(c_oracle_max_segment_syllables,
                    head_syllable_count_local - pos) do
                begin
                    if oracle_budget_exhausted_local then
                    begin
                        Exit(False);
                    end;
                    seg_key := build_syllable_query_key_local(pos, seg_len);
                    if seg_key = '' then
                    begin
                        Continue;
                    end;

                    resolved_chunk_text_local := '';
                    resolved_chunk_valid_local := False;
                    if try_resolve_best_exact_subspan_local(pos, seg_len,
                        resolved_text_local, resolved_score_local,
                        resolved_segments_local, resolved_path_local,
                        resolved_has_anchor_local,
                        resolved_anchor_units_local) then
                    begin
                        candidate_text := Trim(resolved_text_local);
                        candidate_units := get_candidate_text_unit_count(
                            candidate_text);
                        if (candidate_text <> '') and
                            (candidate_units = seg_len) then
                        begin
                            prefer_resolved_chunk_local :=
                                (seg_len >= 3) or resolved_has_anchor_local or
                                (resolved_segments_local > 1);
                            if prefer_resolved_chunk_local then
                            begin
                                next_pos := pos + seg_len;
                                candidate_state.score := base_state.score +
                                    resolved_score_local + c_oracle_multi_bonus +
                                    (seg_len * c_oracle_rank_step);
                                if resolved_segments_local > 1 then
                                begin
                                    Dec(candidate_state.score,
                                        (resolved_segments_local - 1) *
                                        (c_oracle_additional_segment_penalty div 2));
                                end;
                                if base_state.text <> '' then
                                begin
                                    Dec(candidate_state.score,
                                        c_oracle_additional_segment_penalty);
                                    Inc(candidate_state.score,
                                        get_oracle_context_bonus_local(
                                        base_state.prev_text, candidate_text));
                                    if base_state.prev_prev_text <> '' then
                                    begin
                                        Inc(candidate_state.score,
                                            get_oracle_trigram_bonus_local(
                                            base_state.prev_prev_text,
                                            base_state.prev_text, candidate_text));
                                    end;
                                    candidate_state.text := base_state.text +
                                        candidate_text;
                                    if Trim(resolved_path_local) <> '' then
                                    begin
                                        candidate_state.path := base_state.path +
                                            c_segment_path_separator +
                                            Trim(resolved_path_local);
                                    end
                                    else
                                    begin
                                        candidate_state.path := base_state.path +
                                            c_segment_path_separator +
                                            candidate_text;
                                    end;
                                end
                                else
                                begin
                                    candidate_state.text := candidate_text;
                                    if Trim(resolved_path_local) <> '' then
                                    begin
                                        candidate_state.path := Trim(
                                            resolved_path_local);
                                    end
                                    else
                                    begin
                                        candidate_state.path := candidate_text;
                                    end;
                                end;
                                if resolved_has_anchor_local or
                                    (seg_len >= 3) then
                                begin
                                    Inc(candidate_state.score,
                                        c_oracle_anchor_bonus);
                                end;
                                candidate_state.prev_prev_text := base_state.prev_text;
                                candidate_state.prev_text := candidate_text;
                                add_state(state_map[next_pos], candidate_state);
                                resolved_chunk_text_local := candidate_text;
                                resolved_chunk_valid_local := True;
                            end;
                        end;
                    end;

                    allow_raw_merged_local := not
                        ((head_syllable_count_local >= 8) and
                        (resolved_chunk_valid_local or (seg_len >= 4)));
                    SetLength(merged_results, 0);
                    if allow_raw_merged_local then
                    begin
                        if merged_result_cache.TryGetValue(seg_key,
                            merged_results) then
                        begin
                            merged_results := Copy(merged_results);
                        end
                        else
                        begin
                            if dictionary_lookup_cached(seg_key,
                                lookup_results) then
                            begin
                                SetLength(merged_results,
                                    Length(lookup_results));
                                for merged_idx := 0 to High(lookup_results) do
                                begin
                                    merged_results[merged_idx] :=
                                        lookup_results[merged_idx];
                                end;
                            end;

                            if dictionary_exact_lookup_cached(seg_key,
                                exact_results) then
                            begin
                                for exact_idx := 0 to High(exact_results) do
                                begin
                                    duplicate_found := False;
                                    for merged_idx := 0 to High(
                                        merged_results) do
                                    begin
                                        if SameText(Trim(
                                            merged_results[merged_idx].text),
                                            Trim(exact_results[exact_idx].text)) and
                                            SameText(Trim(
                                            merged_results[merged_idx].comment),
                                            Trim(exact_results[exact_idx].comment)) then
                                        begin
                                            duplicate_found := True;
                                            Break;
                                        end;
                                    end;
                                    if duplicate_found then
                                    begin
                                        Continue;
                                    end;
                                    SetLength(merged_results,
                                        Length(merged_results) + 1);
                                    merged_results[High(merged_results)] :=
                                        exact_results[exact_idx];
                                end;
                            end;
                            merged_result_cache.AddOrSetValue(seg_key,
                                Copy(merged_results));
                        end;
                    end;

                    if Length(merged_results) <= 0 then
                    begin
                        SetLength(merged_results, 0);
                    end;

                    standalone_query_valid_local := False;
                    if (seg_len <= 5) and (head_syllable_count_local >= 6) and
                        try_get_best_oracle_standalone_query_candidate_local(
                        seg_key, seg_len, standalone_query_text_local,
                        standalone_query_score_local) and
                        (standalone_query_text_local <> '') then
                    begin
                        standalone_query_valid_local :=
                            oracle_candidate_respects_fixed_boundaries_local(
                            pos, seg_len, standalone_query_text_local);
                        if standalone_query_valid_local then
                        begin
                            duplicate_found := False;
                            for merged_idx := 0 to High(merged_results) do
                            begin
                                if SameText(
                                    Trim(merged_results[merged_idx].text),
                                    standalone_query_text_local) then
                                begin
                                    duplicate_found := True;
                                    Break;
                                end;
                            end;

                            if not duplicate_found then
                            begin
                                FillChar(standalone_query_candidate_local,
                                    SizeOf(standalone_query_candidate_local), 0);
                                standalone_query_candidate_local.text :=
                                    standalone_query_text_local;
                                standalone_query_candidate_local.comment := '';
                                standalone_query_candidate_local.score :=
                                    standalone_query_score_local;
                                standalone_query_candidate_local.source := cs_rule;
                                standalone_query_candidate_local.has_dict_weight := True;
                                standalone_query_candidate_local.dict_weight :=
                                    standalone_query_score_local;
                                SetLength(merged_results, Length(merged_results) + 1);
                                for merged_idx := High(merged_results) downto 1 do
                                begin
                                    merged_results[merged_idx] :=
                                        merged_results[merged_idx - 1];
                                end;
                                merged_results[0] :=
                                    standalone_query_candidate_local;
                            end;
                        end;
                    end;

                    if standalone_query_valid_local and
                        ((seg_len >= 4) or
                        (standalone_query_score_local >= 24000)) then
                    begin
                        allow_raw_merged_local := False;
                    end;

                    if standalone_query_valid_local then
                    begin
                        next_pos := pos + seg_len;
                        candidate_state.score := base_state.score +
                            standalone_query_score_local + c_oracle_multi_bonus +
                            (seg_len * c_oracle_rank_step) +
                            c_oracle_standalone_match_bonus;
                        if base_state.text <> '' then
                        begin
                            Dec(candidate_state.score,
                                c_oracle_additional_segment_penalty);
                            Inc(candidate_state.score,
                                get_oracle_context_bonus_local(
                                base_state.prev_text,
                                standalone_query_text_local));
                            if base_state.prev_prev_text <> '' then
                            begin
                                Inc(candidate_state.score,
                                    get_oracle_trigram_bonus_local(
                                    base_state.prev_prev_text,
                                    base_state.prev_text,
                                    standalone_query_text_local));
                            end;
                            candidate_state.text := base_state.text +
                                standalone_query_text_local;
                            candidate_state.path := base_state.path +
                                c_segment_path_separator +
                                standalone_query_text_local;
                        end
                        else
                        begin
                            candidate_state.text := standalone_query_text_local;
                            candidate_state.path := standalone_query_text_local;
                        end;
                        if seg_len >= 3 then
                        begin
                            Inc(candidate_state.score, c_oracle_anchor_bonus);
                        end;
                        candidate_state.prev_prev_text := base_state.prev_text;
                        candidate_state.prev_text := standalone_query_text_local;
                        add_state(state_map[next_pos], candidate_state);
                    end;

                    if c_enable_nested_oracle and
                        ((seg_len >= 3) or
                        segment_has_fixed_oracle_boundary_local(pos, seg_len)) and
                        collect_nested_oracle_segment_options_local(seg_key, seg_len,
                        nested_segment_options_local) then
                    begin
                        next_pos := pos + seg_len;
                        for nested_option_idx_local := 0 to High(
                            nested_segment_options_local) do
                        begin
                            nested_option_local := nested_segment_options_local[
                                nested_option_idx_local];
                            if Trim(nested_option_local.text) = '' then
                            begin
                                Continue;
                            end;
                            if resolved_chunk_valid_local and SameText(
                                nested_option_local.text,
                                resolved_chunk_text_local) then
                            begin
                                Continue;
                            end;

                            candidate_state.score := base_state.score +
                                nested_option_local.score + c_oracle_multi_bonus +
                                ((c_oracle_probe_limit - Min(
                                nested_option_local.rank, c_oracle_probe_limit - 1)) *
                                c_oracle_rank_step);
                            if base_state.text <> '' then
                            begin
                                Dec(candidate_state.score,
                                    c_oracle_additional_segment_penalty);
                                Inc(candidate_state.score,
                                    get_oracle_context_bonus_local(
                                    base_state.prev_text,
                                    nested_option_local.text));
                                if base_state.prev_prev_text <> '' then
                                begin
                                    Inc(candidate_state.score,
                                        get_oracle_trigram_bonus_local(
                                        base_state.prev_prev_text,
                                        base_state.prev_text,
                                        nested_option_local.text));
                                end;
                                candidate_state.text := base_state.text +
                                    nested_option_local.text;
                                candidate_state.path := base_state.path +
                                    c_segment_path_separator +
                                    nested_option_local.path_text;
                            end
                            else
                            begin
                                candidate_state.text := nested_option_local.text;
                                candidate_state.path := nested_option_local.path_text;
                            end;
                            if seg_len >= 3 then
                            begin
                                Inc(candidate_state.score, c_oracle_anchor_bonus);
                            end;
                            candidate_state.prev_prev_text := base_state.prev_text;
                            candidate_state.prev_text := nested_option_local.text;
                            add_state(state_map[next_pos], candidate_state);
                        end;
                    end;

                    if allow_raw_merged_local and (Length(merged_results) > 0) then
                    begin
                        next_pos := pos + seg_len;
                        for result_idx := 0 to Min(c_oracle_probe_limit - 1,
                            High(merged_results)) do
                        begin
                            if Trim(merged_results[result_idx].comment) <> '' then
                            begin
                                Continue;
                            end;
                            if is_runtime_chain_candidate(
                                merged_results[result_idx]) or
                                is_runtime_common_pattern_candidate(
                                merged_results[result_idx]) or
                                is_runtime_redup_candidate(
                                merged_results[result_idx]) then
                            begin
                                Continue;
                            end;

                            candidate_text := Trim(merged_results[result_idx].text);
                            candidate_units := get_candidate_text_unit_count(
                                candidate_text);
                            if candidate_units < 2 then
                            begin
                                Continue;
                            end;
                            if resolved_chunk_valid_local and
                                SameText(candidate_text, resolved_chunk_text_local) then
                            begin
                                Continue;
                            end;

                            effective_weight := merged_results[result_idx].score;
                            if merged_results[result_idx].has_dict_weight and
                                (merged_results[result_idx].dict_weight >
                                effective_weight) then
                            begin
                                effective_weight :=
                                    merged_results[result_idx].dict_weight;
                            end;
                            effective_weight :=
                                normalize_oracle_segment_weight_local(
                                effective_weight, seg_len);
                            Inc(effective_weight,
                                get_preferred_query_phrase_bonus_local(seg_key,
                                candidate_text));
                            if standalone_query_valid_local then
                            begin
                                if SameText(candidate_text,
                                    standalone_query_text_local) then
                                begin
                                    Inc(effective_weight,
                                        c_oracle_standalone_match_bonus);
                                end
                                else if seg_len <= 5 then
                                begin
                                    Dec(effective_weight,
                                        c_oracle_standalone_mismatch_penalty *
                                        IfThen(seg_len >= 4, 2, 1));
                                end;
                            end;

                            candidate_state.score := base_state.score +
                                effective_weight + c_oracle_multi_bonus +
                                ((c_oracle_probe_limit - result_idx) *
                                c_oracle_rank_step);
                            if base_state.text <> '' then
                            begin
                                Dec(candidate_state.score,
                                    c_oracle_additional_segment_penalty);
                                Inc(candidate_state.score,
                                    get_oracle_context_bonus_local(
                                    base_state.prev_text, candidate_text));
                                if base_state.prev_prev_text <> '' then
                                begin
                                    Inc(candidate_state.score,
                                        get_oracle_trigram_bonus_local(
                                        base_state.prev_prev_text,
                                        base_state.prev_text, candidate_text));
                                end;
                                candidate_state.text := base_state.text +
                                    candidate_text;
                                candidate_state.path := base_state.path +
                                    c_segment_path_separator + candidate_text;
                            end
                            else
                            begin
                                candidate_state.text := candidate_text;
                                candidate_state.path := candidate_text;
                            end;
                            if seg_len >= 3 then
                            begin
                                Inc(candidate_state.score,
                                    c_oracle_anchor_bonus);
                            end;
                            candidate_state.prev_prev_text := base_state.prev_text;
                            candidate_state.prev_text := candidate_text;
                            add_state(state_map[next_pos], candidate_state);
                        end;
                    end;
                end;
            end;
        end;

        final_states := state_map[head_syllable_count_local];
        if Length(final_states) <= 0 then
        begin
            Exit;
        end;

        if m_debug_target_recall_text <> '' then
        begin
            m_debug_target_final_state_count := Max(
                m_debug_target_final_state_count, Length(final_states));
            for state_idx := 0 to High(final_states) do
            begin
                if SameText(Trim(final_states[state_idx].text),
                    m_debug_target_recall_text) then
                begin
                    m_debug_target_final_state_source_mask :=
                        m_debug_target_final_state_source_mask or 8;
                    if (m_debug_target_oracle_final_state_rank = 0) or
                        (state_idx + 1 < m_debug_target_oracle_final_state_rank) then
                    begin
                        m_debug_target_oracle_final_state_rank := state_idx + 1;
                    end;
                    if (not m_debug_target_final_state_found) or
                        (state_idx + 1 < m_debug_target_final_state_rank) then
                    begin
                        m_debug_target_final_state_found := True;
                        m_debug_target_final_state_rank := state_idx + 1;
                    end;
                end;
            end;
        end;

        for state_idx := 0 to Min(c_oracle_emit_limit - 1, High(final_states)) do
        begin
            append_oracle_candidate(final_states[state_idx].text,
                final_states[state_idx].path,
                final_states[state_idx].score + c_oracle_final_complete_bonus);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
        finally
            merged_result_cache.Free;
            nested_engine.Free;
            nested_option_cache.Free;
            nested_option_states.Free;
        end;
    end;

    function get_prefix_partial_syllables_for_query(const query_text: string;
        out out_syllables: TncPinyinParseResult): Boolean;
    var
        normalized_query_local: string;
        explicit_compact_local: string;
        syllable_idx_local: Integer;
    begin
        normalized_query_local := normalize_pinyin_text(query_text);
        SetLength(out_syllables, 0);
        if normalized_query_local = '' then
        begin
            Exit(False);
        end;

        if has_explicit_apostrophe_input and
            SameText(normalize_pinyin_text(m_composition_text),
            normalized_query_local) and
            ensure_explicit_apostrophe_query_syllables_parsed then
        begin
            explicit_compact_local := '';
            SetLength(out_syllables, Length(explicit_apostrophe_query_syllables));
            for syllable_idx_local := 0 to
                High(explicit_apostrophe_query_syllables) do
            begin
                out_syllables[syllable_idx_local] :=
                    explicit_apostrophe_query_syllables[syllable_idx_local];
                out_syllables[syllable_idx_local].text := normalize_pinyin_text(
                    out_syllables[syllable_idx_local].text);
                explicit_compact_local := explicit_compact_local +
                    out_syllables[syllable_idx_local].text;
            end;

            if SameText(explicit_compact_local, normalized_query_local) then
            begin
                Exit(Length(out_syllables) > 0);
            end;
        end;

        out_syllables :=
            get_effective_compact_pinyin_syllables(normalized_query_local);
        Result := Length(out_syllables) > 0;
    end;

    function build_explicit_apostrophe_prefix_partial_candidates(
        out out_candidates: TncCandidateList): Boolean;
    const
        c_explicit_prefix_total_limit = 16;
        c_explicit_prefix_base_bonus = 1800;
        c_explicit_prefix_bonus_per_syllable = 180;
        c_explicit_prefix_remaining_penalty = 42;
    var
        syllables_local: TncPinyinParseResult;
        prefix_count: Integer;
        tail_idx: Integer;
        result_idx: Integer;
        prefix_key: string;
        tail_comment: string;
        raw_prefix_key: string;
        head_results: TncCandidateList;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        base_score: Integer;
        accepted_total: Integer;
        seen_key: string;
        seen: TDictionary<string, Byte>;

        function candidate_matches_explicit_prefix_local(
            const text_value: string): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            is_preferred_local: Boolean;
        begin
            Result := False;
            text_units_local := split_text_units(Trim(text_value));
            if Length(text_units_local) <> prefix_count then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to prefix_count - 1 do
            begin
                if not match_single_char_candidate_for_syllable(
                    normalize_pinyin_text(syllables_local[unit_idx_local].text),
                    text_units_local[unit_idx_local],
                    is_preferred_local) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function get_raw_prefix_key_for_count_local(
            const target_prefix_count: Integer): string;
        var
            target_compact_len_local: Integer;
            unit_idx_local: Integer;
            raw_idx_local: Integer;
            compact_seen_local: Integer;
            raw_char_local: Char;
        begin
            Result := '';
            target_compact_len_local := 0;
            for unit_idx_local := 0 to target_prefix_count - 1 do
            begin
                Inc(target_compact_len_local, Length(normalize_pinyin_text(
                    syllables_local[unit_idx_local].text)));
            end;
            if target_compact_len_local <= 0 then
            begin
                Exit;
            end;

            compact_seen_local := 0;
            for raw_idx_local := 1 to Length(m_composition_text) do
            begin
                raw_char_local := m_composition_text[raw_idx_local];
                Result := Result + raw_char_local;
                if raw_char_local <> '''' then
                begin
                    Inc(compact_seen_local);
                    if compact_seen_local >= target_compact_len_local then
                    begin
                        Break;
                    end;
                end;
            end;
            Result := LowerCase(Trim(Result));
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if (m_dictionary = nil) or (not has_explicit_apostrophe_input) or
            (not ensure_explicit_apostrophe_query_syllables_parsed) then
        begin
            Exit;
        end;

        syllables_local := explicit_apostrophe_query_syllables;
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            prefix_key := '';
            for prefix_count := 1 to High(syllables_local) do
            begin
                prefix_key := prefix_key +
                    normalize_pinyin_text(syllables_local[prefix_count - 1].text);
                if prefix_count < 2 then
                begin
                    Continue;
                end;

                tail_comment := '';
                for tail_idx := prefix_count to High(syllables_local) do
                begin
                    tail_comment := tail_comment +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if tail_comment = '' then
                begin
                    Continue;
                end;

                raw_prefix_key := get_raw_prefix_key_for_count_local(prefix_count);
                if ((raw_prefix_key = '') or
                    ((not dictionary_lookup_cached(raw_prefix_key, head_results)) and
                    (not dictionary_exact_lookup_cached(raw_prefix_key,
                    head_results)))) and
                    ((not dictionary_exact_lookup_cached(prefix_key,
                    head_results)) and
                    (not dictionary_lookup_cached(prefix_key, head_results))) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(head_results) do
                begin
                    if accepted_total >= c_explicit_prefix_total_limit then
                    begin
                        Break;
                    end;

                    if Trim(head_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[result_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if (candidate_text = '') or (candidate_units <> prefix_count) or
                        (not candidate_matches_explicit_prefix_local(candidate_text)) then
                    begin
                        Continue;
                    end;

                    seen_key := candidate_text + #9 + tail_comment;
                    if seen.ContainsKey(seen_key) then
                    begin
                        Continue;
                    end;

                    candidate := head_results[result_idx];
                    candidate.comment := tail_comment;
                    base_score := candidate.score;
                    if candidate.has_dict_weight and
                        (candidate.dict_weight > base_score) then
                    begin
                        base_score := candidate.dict_weight;
                    end;
                    candidate.score := base_score + c_explicit_prefix_base_bonus +
                        (prefix_count * c_explicit_prefix_bonus_per_syllable);
                    Dec(candidate.score,
                        (Length(syllables_local) - prefix_count) *
                        c_explicit_prefix_remaining_penalty);
                    candidate.dict_weight := base_score;
                    candidate.has_dict_weight := True;

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate;
                    seen.AddOrSetValue(seen_key, 1);
                    Inc(accepted_total);
                end;
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_exact_head_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_head_partial_limit = 6;
        c_head_partial_bonus = 160;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        head_key: string;
        tail_comment: string;
        idx: Integer;
        head_results: TncCandidateList;
        candidate: TncCandidate;
        accepted_count: Integer;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        if not get_prefix_partial_syllables_for_query(query_text,
            syllables_local) then
        begin
            Exit;
        end;
        if Length(syllables_local) < 2 then
        begin
            Exit;
        end;

        tail_comment := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        if (tail_comment = '') or (not is_full_pinyin_key(tail_comment)) then
        begin
            Exit;
        end;

        head_key := '';
        for idx := 0 to High(syllables_local) - 1 do
        begin
            head_key := head_key + normalize_pinyin_text(syllables_local[idx].text);
        end;
        if head_key = '' then
        begin
            Exit;
        end;
        if not dictionary_exact_lookup_cached(head_key, head_results) then
        begin
            Exit;
        end;

        accepted_count := 0;
        for idx := 0 to High(head_results) do
        begin
            if accepted_count >= c_head_partial_limit then
            begin
                Break;
            end;
            if Trim(head_results[idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate := head_results[idx];
            candidate.comment := tail_comment;
            Inc(candidate.score, c_head_partial_bonus);
            if candidate.dict_weight <= 0 then
            begin
                candidate.dict_weight := candidate.score;
            end;
            candidate.has_dict_weight := True;
            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate;
            Inc(accepted_count);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_lexicon_longer_prefix_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_total_limit = 8;
        c_prefix_base_bonus = 2100;
        c_prefix_unit_bonus = 260;
        c_prefix_tail_bonus = 320;
        c_prefix_complete_bonus = 980;
        c_prefix_remaining_penalty = 90;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        prefix_unit_count: Integer;
        tail_comment: string;
        prefix_results: TncCandidateList;
        accepted_total: Integer;
        candidate_text: string;
        text_units: TArray<string>;
        text_unit_count: Integer;
        prefix_text: string;
        candidate: TncCandidate;
        base_score: Integer;
        seen_key: string;
        seen: TDictionary<string, Byte>;

        function is_safe_longer_prefix_suffix_local(const units: TArray<string>;
            const suffix_start_idx: Integer): Boolean;
        var
            suffix_head: string;
        begin
            Result := False;
            if (suffix_start_idx < 0) or (suffix_start_idx > High(units)) then
            begin
                Exit;
            end;

            suffix_head := units[suffix_start_idx];
            if get_candidate_text_unit_count(suffix_head) <> 1 then
            begin
                Exit;
            end;

            Result :=
                (suffix_head = string(Char($533A))) or
                (suffix_head = string(Char($5E02))) or
                (suffix_head = string(Char($7701))) or
                (suffix_head = string(Char($53BF))) or
                (suffix_head = string(Char($9547))) or
                (suffix_head = string(Char($4E61))) or
                (suffix_head = string(Char($8DEF))) or
                (suffix_head = string(Char($8857))) or
                (suffix_head = string(Char($9053))) or
                (suffix_head = string(Char($4E0B))) or
                (suffix_head = string(Char($4E0A))) or
                (suffix_head = string(Char($4E2D))) or
                (suffix_head = string(Char($5185))) or
                (suffix_head = string(Char($5916))) or
                (suffix_head = string(Char($524D))) or
                (suffix_head = string(Char($540E))) or
                (suffix_head = string(Char($4E1C))) or
                (suffix_head = string(Char($897F))) or
                (suffix_head = string(Char($5357))) or
                (suffix_head = string(Char($5317))) or
                (suffix_head = string(Char($8FB9))) or
                (suffix_head = string(Char($9762))) or
                (suffix_head = string(Char($7248))) or
                (suffix_head = string(Char($53F7))) or
                (suffix_head = string(Char($5E97)));
        end;

        procedure append_longer_prefix_candidates_local(
            const local_prefix_unit_count: Integer;
            const local_tail_comment: string);
        var
            local_result_idx: Integer;
            local_unit_idx: Integer;
        begin
            if (local_prefix_unit_count < 1) or
                (local_prefix_unit_count > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_result_idx := 0 to High(prefix_results) do
            begin
                if accepted_total >= c_prefix_total_limit then
                begin
                    Break;
                end;

                if (prefix_results[local_result_idx].source = cs_user) or
                    (Trim(prefix_results[local_result_idx].comment) <> '') then
                begin
                    Continue;
                end;

                candidate_text := Trim(prefix_results[local_result_idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;

                text_units := split_text_units(candidate_text);
                text_unit_count := Length(text_units);
                if text_unit_count <= local_prefix_unit_count then
                begin
                    Continue;
                end;

                prefix_text := '';
                for local_unit_idx := 0 to local_prefix_unit_count - 1 do
                begin
                    prefix_text := prefix_text + text_units[local_unit_idx];
                end;
                if prefix_text = '' then
                begin
                    Continue;
                end;
                if (local_tail_comment = '') and
                    (not is_safe_longer_prefix_suffix_local(text_units,
                    local_prefix_unit_count)) then
                begin
                    Continue;
                end;
                if m_dictionary.is_base_entry(normalized_query, prefix_text) then
                begin
                    Continue;
                end;

                seen_key := prefix_text + #9 + local_tail_comment;
                if seen.ContainsKey(seen_key) then
                begin
                    Continue;
                end;

                candidate := prefix_results[local_result_idx];
                candidate.text := prefix_text;
                candidate.comment := local_tail_comment;
                base_score := candidate.score;
                if candidate.has_dict_weight and (candidate.dict_weight > base_score) then
                begin
                    base_score := candidate.dict_weight;
                end;
                if base_score < 0 then
                begin
                    base_score := 0;
                end;

                candidate.score := base_score + c_prefix_base_bonus +
                    (local_prefix_unit_count * c_prefix_unit_bonus) -
                    ((text_unit_count - local_prefix_unit_count) *
                    c_prefix_remaining_penalty);
                if local_tail_comment = '' then
                begin
                    Inc(candidate.score, c_prefix_complete_bonus);
                end
                else
                begin
                    Inc(candidate.score, c_prefix_tail_bonus);
                end;
                candidate.dict_weight := base_score;
                candidate.has_dict_weight := True;

                SetLength(out_candidates, Length(out_candidates) + 1);
                out_candidates[High(out_candidates)] := candidate;
                seen.AddOrSetValue(seen_key, 1);
                Inc(accepted_total);
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if (m_dictionary = nil) or (query_text = '') then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        if not get_prefix_partial_syllables_for_query(query_text,
            syllables_local) then
        begin
            Exit;
        end;
        if Length(syllables_local) < 2 then
        begin
            Exit;
        end;

        prefix_unit_count := Length(syllables_local);
        tail_comment := '';
        if not is_full_pinyin_key(
            normalize_pinyin_text(syllables_local[High(syllables_local)].text)) then
        begin
            Dec(prefix_unit_count);
            tail_comment := normalize_pinyin_text(
                syllables_local[High(syllables_local)].text);
        end;

        if prefix_unit_count < 2 then
        begin
            Exit;
        end;

        if not dictionary_prefix_lookup_cached(normalized_query, prefix_results) then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            append_longer_prefix_candidates_local(prefix_unit_count,
                tail_comment);

            if (tail_comment = '') and (Length(syllables_local) >= 2) then
            begin
                append_longer_prefix_candidates_local(
                    Length(syllables_local) - 1,
                    normalize_pinyin_text(
                    syllables_local[High(syllables_local)].text));
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_exact_leading_prefix_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_total_limit = c_candidate_total_limit_max;
        c_prefix_single_limit = c_candidate_total_limit_max;
        c_prefix_phrase_limit = 16;
        c_prefix_base_bonus = 220;
        c_prefix_bonus_per_syllable = 180;
        c_prefix_remaining_penalty = 42;
        c_prefix_max_syllables = 6;
        c_prefix_phrase_extra_bonus = 520;
        c_prefix_short_query_phrase_bonus = 260;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        prefix_count: Integer;
        tail_idx: Integer;
        result_idx: Integer;
        accepted_total: Integer;
        accepted_for_prefix: Integer;
        accepted_limit: Integer;
        prefix_key: string;
        tail_comment: string;
        head_results: TncCandidateList;
        prefix_lookup_results: TncCandidateList;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        lookup_idx: Integer;
        lookup_text: string;
        base_score: Integer;
        effective_prefix_units: Integer;
        seen_key: string;
        cache_key: string;
        cached_candidates: TncCandidateList;
        seen: TDictionary<string, Byte>;
        longer_prefix_candidates: TncCandidateList;

        function explicit_prefix_lookup_entry_local(
            const text_value: string): Boolean;
        var
            local_lookup_idx: Integer;
        begin
            Result := prefix_count < 2;
            if Result then
            begin
                Exit;
            end;

            for local_lookup_idx := 0 to High(prefix_lookup_results) do
            begin
                if (Trim(prefix_lookup_results[local_lookup_idx].comment) = '') and
                    SameText(Trim(prefix_lookup_results[local_lookup_idx].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function compact_prefix_matches_two_unit_candidate_local(
            const prefix_key_value: string; const text_value: string): Boolean;
        var
            text_units_local: TArray<string>;
            parser_local: TncPinyinParser;
            left_text_local: string;
            right_text_local: string;
            is_preferred_local: Boolean;
        begin
            Result := False;
            text_units_local := split_text_units(Trim(text_value));
            if Length(text_units_local) <> 2 then
            begin
                Exit;
            end;

            parser_local := TncPinyinParser.Create;
            try
                if not try_split_relaxed_missing_apostrophe_token(parser_local,
                    prefix_key_value, left_text_local, right_text_local) then
                begin
                    Exit;
                end;
            finally
                parser_local.Free;
            end;

            Result := match_single_char_candidate_for_syllable(left_text_local,
                text_units_local[0], is_preferred_local) and
                match_single_char_candidate_for_syllable(right_text_local,
                text_units_local[1], is_preferred_local);
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;
        cache_key := '#exactleadingprefix#' + normalized_query;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, cached_candidates) then
        begin
            out_candidates := Copy(cached_candidates, 0, Length(cached_candidates));
            Exit(Length(out_candidates) > 0);
        end;

        if not get_prefix_partial_syllables_for_query(query_text,
            syllables_local) then
        begin
            Exit;
        end;
        if Length(syllables_local) < 2 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            for prefix_count := Min(c_prefix_max_syllables,
                Length(syllables_local) - 1) downto 1 do
            begin
                // Reserve the bounded candidate pool for reliable phrase prefixes
                // before the usually much larger first-syllable character list.
                prefix_key := '';
                for tail_idx := 0 to prefix_count - 1 do
                begin
                    prefix_key := prefix_key +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if prefix_key = '' then
                begin
                    Continue;
                end;

                tail_comment := '';
                for tail_idx := prefix_count to High(syllables_local) do
                begin
                    tail_comment := tail_comment +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if tail_comment = '' then
                begin
                    Continue;
                end;

                if not dictionary_exact_lookup_cached(prefix_key,
                    head_results) then
                begin
                    if (m_dictionary is TncSqliteDictionary) or
                        (not dictionary_lookup_cached(prefix_key,
                        head_results)) then
                    begin
                        Continue;
                    end;
                end;
                prefix_lookup_results := head_results;

                if prefix_count = 1 then
                begin
                    accepted_limit := c_prefix_single_limit;
                end
                else
                begin
                    accepted_limit := c_prefix_phrase_limit;
                end;

                accepted_for_prefix := 0;
                for result_idx := 0 to High(head_results) do
                begin
                    if (accepted_total >= c_prefix_total_limit) or
                        (accepted_for_prefix >= accepted_limit) then
                    begin
                        Break;
                    end;
                    if Trim(head_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[result_idx].text);
                    if candidate_text = '' then
                    begin
                        Continue;
                    end;

                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    effective_prefix_units := prefix_count;
                    if prefix_count = 1 then
                    begin
                        if candidate_units <> 1 then
                        begin
                            if (tail_comment = '') or (candidate_units <> 2) or
                                (not compact_prefix_matches_two_unit_candidate_local(
                                prefix_key, candidate_text)) then
                            begin
                                Continue;
                            end;
                            effective_prefix_units := candidate_units;
                        end;
                    end
                    else if candidate_units < 2 then
                    begin
                        Continue;
                    end;
                    if not explicit_prefix_lookup_entry_local(candidate_text) then
                    begin
                        Continue;
                    end;

                    seen_key := candidate_text + #9 + tail_comment;
                    if seen.ContainsKey(seen_key) then
                    begin
                        Continue;
                    end;

                    candidate := head_results[result_idx];
                    candidate.comment := tail_comment;
                    base_score := candidate.score;
                    if candidate.has_dict_weight and
                        (candidate.dict_weight > base_score) then
                    begin
                        base_score := candidate.dict_weight;
                    end;
                    for lookup_idx := 0 to High(prefix_lookup_results) do
                    begin
                        if Trim(prefix_lookup_results[lookup_idx].comment) <> '' then
                        begin
                            Continue;
                        end;
                        lookup_text := Trim(prefix_lookup_results[lookup_idx].text);
                        if SameText(lookup_text, candidate_text) then
                        begin
                            base_score := prefix_lookup_results[lookup_idx].score;
                            if prefix_lookup_results[lookup_idx].has_dict_weight and
                                (prefix_lookup_results[lookup_idx].dict_weight >
                                base_score) then
                            begin
                                base_score :=
                                    prefix_lookup_results[lookup_idx].dict_weight;
                            end;
                            if lookup_idx = 0 then
                            begin
                                Inc(base_score, 720);
                            end;
                            Break;
                        end;
                    end;
                    candidate.score := base_score + c_prefix_base_bonus +
                        (effective_prefix_units * c_prefix_bonus_per_syllable);
                    if effective_prefix_units >= 2 then
                    begin
                        Inc(candidate.score, c_prefix_phrase_extra_bonus);
                        if Length(syllables_local) <= 4 then
                        begin
                            Inc(candidate.score, c_prefix_short_query_phrase_bonus);
                        end;
                    end;
                    Dec(candidate.score, (Length(syllables_local) - prefix_count) *
                        c_prefix_remaining_penalty);
                    candidate.dict_weight := base_score;
                    candidate.has_dict_weight := True;

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate;
                    seen.AddOrSetValue(seen_key, 1);
                    Inc(accepted_total);
                    Inc(accepted_for_prefix);
                end;
            end;
        finally
            seen.Free;
        end;

        if build_lexicon_longer_prefix_candidates(query_text,
            longer_prefix_candidates) then
        begin
            out_candidates := merge_candidate_lists(out_candidates,
                longer_prefix_candidates, 0);
        end;
        sort_candidates_lightweight(out_candidates);
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key,
                Copy(out_candidates, 0, Length(out_candidates)));
        end;
        Result := Length(out_candidates) > 0;
    end;

    function build_fuzzy_short_query_recall_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_total_limit = 40;
        c_full_limit = 16;
        c_single_prefix_limit = 16;
        c_phrase_prefix_limit = 8;
        c_prefix_base_bonus = 220;
        c_prefix_bonus_per_syllable = 180;
        c_prefix_phrase_extra_bonus = 520;
        c_prefix_short_query_phrase_bonus = 260;
        c_prefix_remaining_penalty = 42;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        fuzzy_results: TncCandidateList;
        seen: TDictionary<string, Byte>;
        prefix_count: Integer;
        syllable_idx: Integer;
        candidate_idx: Integer;
        accepted_for_prefix: Integer;
        accepted_limit: Integer;
        prefix_key: string;
        tail_comment: string;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        seen_key: string;

        procedure append_candidate_local(const candidate_value: TncCandidate;
            const comment_value: string; const expected_text_units: Integer;
            const add_prefix_score: Boolean);
        var
            local_candidate: TncCandidate;
            local_text: string;
            local_key: string;
        begin
            if Length(out_candidates) >= c_total_limit then
            begin
                Exit;
            end;

            local_candidate := candidate_value;
            local_text := Trim(local_candidate.text);
            if (local_text = '') or (Trim(local_candidate.comment) <> '') or
                (local_candidate.fuzzy_cost <= 0) or
                (get_candidate_text_unit_count(local_text) <>
                expected_text_units) then
            begin
                Exit;
            end;

            local_candidate.text := local_text;
            local_candidate.comment := comment_value;
            if add_prefix_score then
            begin
                Inc(local_candidate.score, c_prefix_base_bonus +
                    (expected_text_units * c_prefix_bonus_per_syllable));
                if expected_text_units >= 2 then
                begin
                    Inc(local_candidate.score, c_prefix_phrase_extra_bonus +
                        c_prefix_short_query_phrase_bonus);
                end;
                Dec(local_candidate.score,
                    (Length(syllables_local) - expected_text_units) *
                    c_prefix_remaining_penalty);
            end;

            local_key := LowerCase(local_text) + #1 +
                LowerCase(Trim(comment_value));
            if seen.ContainsKey(local_key) then
            begin
                Exit;
            end;

            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := local_candidate;
            seen.Add(local_key, 1);
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if (m_dictionary = nil) or (not is_fuzzy_pinyin_active) then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if (normalized_query = '') or
            (not get_prefix_partial_syllables_for_query(query_text,
            syllables_local)) or (Length(syllables_local) < 2) or
            (Length(syllables_local) > 4) then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            if m_dictionary.lookup_fuzzy_full_pinyin_bounded(normalized_query,
                fuzzy_results, 1, 6, 4, c_full_limit) then
            begin
                for candidate_idx := 0 to High(fuzzy_results) do
                begin
                    append_candidate_local(fuzzy_results[candidate_idx], '',
                        Length(syllables_local), False);
                end;
            end;

            { Explicit apostrophes carry authoritative syllable boundaries.
              The full fuzzy exact lookup above preserves them; do not rebuild
              prefix keys without those separators. }
            if Pos('''', normalized_query) = 0 then
            begin
                for prefix_count := Length(syllables_local) - 1 downto 1 do
                begin
                    prefix_key := '';
                    for syllable_idx := 0 to prefix_count - 1 do
                    begin
                        prefix_key := prefix_key + normalize_pinyin_text(
                            syllables_local[syllable_idx].text);
                    end;
                    tail_comment := '';
                    for syllable_idx := prefix_count to High(syllables_local) do
                    begin
                        tail_comment := tail_comment + normalize_pinyin_text(
                            syllables_local[syllable_idx].text);
                    end;
                    if (prefix_key = '') or (tail_comment = '') or
                        (not m_dictionary.lookup_fuzzy_full_pinyin_bounded(
                        prefix_key, fuzzy_results, 1, 6, 4,
                        c_single_prefix_limit)) then
                    begin
                        Continue;
                    end;

                    if prefix_count = 1 then
                    begin
                        accepted_limit := c_single_prefix_limit;
                    end
                    else
                    begin
                        accepted_limit := c_phrase_prefix_limit;
                    end;
                    accepted_for_prefix := 0;
                    for candidate_idx := 0 to High(fuzzy_results) do
                    begin
                        if (Length(out_candidates) >= c_total_limit) or
                            (accepted_for_prefix >= accepted_limit) then
                        begin
                            Break;
                        end;
                        candidate := fuzzy_results[candidate_idx];
                        candidate_text := Trim(candidate.text);
                        candidate_units := get_candidate_text_unit_count(
                            candidate_text);
                        seen_key := LowerCase(candidate_text) + #1 +
                            LowerCase(tail_comment);
                        if (candidate_text = '') or
                            (candidate_units <> prefix_count) or
                            seen.ContainsKey(seen_key) then
                        begin
                            Continue;
                        end;
                        append_candidate_local(candidate, tail_comment,
                            prefix_count, True);
                        Inc(accepted_for_prefix);
                    end;
                end;
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_lookup_leading_prefix_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_total_limit = 10;
        c_prefix_base_bonus = 180;
        c_prefix_bonus_per_syllable = 160;
        c_prefix_max_syllables = 6;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        prefix_count: Integer;
        tail_idx: Integer;
        result_idx: Integer;
        accepted_total: Integer;
        prefix_key: string;
        tail_comment: string;
        head_results: TncCandidateList;
        prefix_lookup_results: TncCandidateList;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        seen_key: string;
        seen: TDictionary<string, Byte>;

        function explicit_prefix_entry_local(const text_value: string): Boolean;
        var
            local_lookup_idx: Integer;
        begin
            Result := prefix_count < 2;
            if Result then
            begin
                Exit;
            end;

            for local_lookup_idx := 0 to High(prefix_lookup_results) do
            begin
                if (Trim(prefix_lookup_results[local_lookup_idx].comment) = '') and
                    SameText(Trim(prefix_lookup_results[local_lookup_idx].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        if not get_prefix_partial_syllables_for_query(query_text,
            syllables_local) then
        begin
            Exit;
        end;
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            for prefix_count := Min(c_prefix_max_syllables,
                Length(syllables_local) - 1) downto 2 do
            begin
                prefix_key := '';
                for tail_idx := 0 to prefix_count - 1 do
                begin
                    prefix_key := prefix_key +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if prefix_key = '' then
                begin
                    Continue;
                end;

                tail_comment := '';
                for tail_idx := prefix_count to High(syllables_local) do
                begin
                    tail_comment := tail_comment +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if tail_comment = '' then
                begin
                    Continue;
                end;

                if not dictionary_exact_lookup_cached(prefix_key, head_results) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(head_results) do
                begin
                    if accepted_total >= c_prefix_total_limit then
                    begin
                        Break;
                    end;
                    if Trim(head_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[result_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if (candidate_text = '') or (candidate_units <> prefix_count) then
                    begin
                        Continue;
                    end;
                    if not explicit_prefix_entry_local(candidate_text) then
                    begin
                        Continue;
                    end;

                    seen_key := candidate_text + #9 + tail_comment;
                    if seen.ContainsKey(seen_key) then
                    begin
                        Continue;
                    end;

                    candidate := head_results[result_idx];
                    candidate.comment := tail_comment;
                    Inc(candidate.score, c_prefix_base_bonus +
                        (prefix_count * c_prefix_bonus_per_syllable));
                    if candidate.dict_weight <= 0 then
                    begin
                        candidate.dict_weight := candidate.score;
                    end;
                    candidate.has_dict_weight := True;

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate;
                    seen.AddOrSetValue(seen_key, 1);
                    Inc(accepted_total);
                end;
            end;
        finally
            seen.Free;
        end;
        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_short_particle_prefix_partial_candidates_local(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    var
        normalized_query: string;
        parser: TncPinyinParser;
        syllables_local: TncPinyinParseResult;
        prefix_query: string;
        tail_comment: string;
        prefix_idx: Integer;
        candidate_idx: Integer;
        particle_candidates: TncCandidateList;
        candidate: TncCandidate;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        normalized_query := normalize_pinyin_text(query_text);
        if (normalized_query = '') or (not is_full_pinyin_key(normalized_query)) then
        begin
            Exit;
        end;

        parser := TncPinyinParser.Create;
        try
            syllables_local := parser.parse(normalized_query);
        finally
            parser.Free;
        end;
        if Length(syllables_local) <> 4 then
        begin
            Exit;
        end;

        prefix_query := '';
        for prefix_idx := 0 to High(syllables_local) - 1 do
        begin
            prefix_query := prefix_query + normalize_pinyin_text(
                syllables_local[prefix_idx].text);
        end;
        tail_comment := normalize_pinyin_text(
            syllables_local[High(syllables_local)].text);
        if (prefix_query = '') or (tail_comment = '') or
            (not build_short_particle_tail_dictionary_candidates(prefix_query,
            particle_candidates)) then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(particle_candidates) do
        begin
            candidate := particle_candidates[candidate_idx];
            if (Trim(candidate.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate.text)) <> 3) then
            begin
                Continue;
            end;
            candidate.comment := tail_comment;
            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate;
        end;
        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    procedure apply_selected_prefix_single_context_lm_local(
        var candidates: TncCandidateList);
    const
        c_max_context_candidates = 16;
        c_min_lm_lead = 1800;
        c_max_lexical_gap = 900;
        c_promotion_margin = 96;
        c_max_promotion_bonus = 1200;
    var
        context_value: string;
        normalized_query: string;
        latest_choice_text: string;
        candidate_texts: TArray<string>;
        candidate_indices: TArray<Integer>;
        lm_scores: TArray<Integer>;
        seen_texts: TDictionary<string, Byte>;
        idx: Integer;
        collected_idx: Integer;
        baseline_idx: Integer;
        best_idx: Integer;
        baseline_candidate_idx: Integer;
        best_candidate_idx: Integer;
        baseline_rank: Integer;
        best_rank: Integer;
        lexical_gap: Integer;
        lm_lead: Integer;
        promotion_bonus: Integer;
        candidate_text: string;

        function effective_candidate_rank_local(
            const candidate_value: TncCandidate): Integer;
        begin
            Result := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;
        end;
    begin
        // Standalone one-syllable input keeps the established lexical order.
        // Context is applied only to the remaining syllable after a partial
        // candidate was confirmed in the same composition.
        context_value := Trim(m_segment_left_context);
        normalized_query := normalize_pinyin_text(lookup_text);
        if (context_value = '') or (normalized_query = '') or
            (not is_full_pinyin_key(normalized_query)) or
            (m_dictionary = nil) or (Length(candidates) < 2) then
        begin
            Exit;
        end;

        seen_texts := TDictionary<string, Byte>.Create;
        try
            SetLength(candidate_texts, 0);
            SetLength(candidate_indices, 0);
            for idx := 0 to High(candidates) do
            begin
                if Length(candidate_texts) >= c_max_context_candidates then
                begin
                    Break;
                end;
                candidate_text := Trim(candidates[idx].text);
                if (Trim(candidates[idx].comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text) <> 1) or
                    (candidate_text = '') or seen_texts.ContainsKey(candidate_text) then
                begin
                    Continue;
                end;

                // Explicit query learning remains stronger than contextual LM.
                latest_choice_text := get_cached_query_latest_choice_text(
                    normalized_query);
                if (candidates[idx].source = cs_user) or
                    is_latest_session_query_choice(candidate_text) or
                    ((latest_choice_text <> '') and
                    SameText(latest_choice_text, candidate_text)) or
                    (m_dictionary.get_query_choice_bonus(normalized_query,
                    candidate_text) > 0) then
                begin
                    Exit;
                end;

                seen_texts.Add(candidate_text, 1);
                collected_idx := Length(candidate_texts);
                SetLength(candidate_texts, collected_idx + 1);
                SetLength(candidate_indices, collected_idx + 1);
                candidate_texts[collected_idx] := candidate_text;
                candidate_indices[collected_idx] := idx;
            end;
        finally
            seen_texts.Free;
        end;

        if (Length(candidate_texts) < 2) or
            (not get_cached_char_lm_scores(candidate_texts, lm_scores,
            clsm_context, context_value)) or
            (Length(lm_scores) <> Length(candidate_texts)) then
        begin
            Exit;
        end;

        baseline_idx := 0;
        best_idx := baseline_idx;
        for idx := 1 to High(candidate_texts) do
        begin
            if lm_scores[idx] > lm_scores[best_idx] then
            begin
                best_idx := idx;
            end;
        end;
        if best_idx = baseline_idx then
        begin
            Exit;
        end;

        lm_lead := lm_scores[best_idx] - lm_scores[baseline_idx];
        baseline_candidate_idx := candidate_indices[baseline_idx];
        best_candidate_idx := candidate_indices[best_idx];
        baseline_rank := effective_candidate_rank_local(
            candidates[baseline_candidate_idx]);
        best_rank := effective_candidate_rank_local(candidates[best_candidate_idx]);
        lexical_gap := baseline_rank - best_rank;
        if lexical_gap < 0 then
        begin
            lexical_gap := 0;
        end;
        if (lm_lead < c_min_lm_lead) or
            (lexical_gap > c_max_lexical_gap) then
        begin
            Exit;
        end;

        promotion_bonus := lexical_gap + c_promotion_margin;
        if promotion_bonus > c_max_promotion_bonus then
        begin
            promotion_bonus := c_max_promotion_bonus;
        end;
        Inc(candidates[best_candidate_idx].score, promotion_bonus);
        sort_candidates_lightweight(candidates);
        if m_config.debug_mode then
        begin
            if m_last_lookup_debug_extra <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
            end;
            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                Format('singlectx=%s:%d', [candidate_texts[best_idx], lm_lead]);
        end;
    end;

    function build_short_pinyin_prefix_completion_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_completion_visible_bonus = 620;
        c_prefix_completion_limit = 8;
    var
        normalized_query: string;
        prefix_results: TncCandidateList;
        syllables_local: TncPinyinParseResult;
        query_units: Integer;
        result_idx: Integer;
        accepted_count: Integer;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        base_score: Integer;

        function prefix_completion_alignment_allowed_local(
            const text_value: string): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            syllable_key_local: string;

            function raw_query_alignment_allowed_local: Boolean;
                function match_from_local(const unit_index: Integer;
                    const query_pos: Integer): Boolean;
                var
                    end_pos_local: Integer;
                    max_end_local: Integer;
                    remaining_units_local: Integer;
                    segment_key_local: string;
                begin
                    Result := False;
                    if unit_index = query_units - 1 then
                    begin
                        Result := query_pos <= Length(normalized_query);
                        Exit;
                    end;

                    remaining_units_local := query_units - unit_index - 1;
                    max_end_local := Length(normalized_query) - remaining_units_local;
                    for end_pos_local := query_pos to max_end_local do
                    begin
                        segment_key_local := Copy(normalized_query, query_pos,
                            end_pos_local - query_pos + 1);
                        if (segment_key_local <> '') and
                            is_full_pinyin_key(segment_key_local) and
                            m_dictionary.single_char_matches_pinyin(
                            segment_key_local, text_units_local[unit_index]) and
                            match_from_local(unit_index + 1, end_pos_local + 1) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            begin
                // The raw parser may split a multi-letter initial across units:
                // "rich" can become "ric/h" even though the intended alignment is
                // "ri/ch". Earlier units must still be full-pinyin exact matches;
                // only the final unit may remain a prefix.
                Result := match_from_local(0, 1);
            end;
        begin
            Result := False;
            text_units_local := split_text_units(text_value);
            if Length(text_units_local) <> query_units then
            begin
                Exit;
            end;

            if Length(syllables_local) = query_units then
            begin
                // Predictive prefix completion may abbreviate only the final
                // syllable.  Earlier syllables must already match exactly;
                // otherwise "zheji + geci" can incorrectly become
                // "zhejiang + geci".
                for unit_idx_local := 0 to query_units - 2 do
                begin
                    syllable_key_local := normalize_pinyin_text(
                        syllables_local[unit_idx_local].text);
                    if (syllable_key_local = '') or
                        (not m_dictionary.single_char_matches_pinyin(
                        syllable_key_local, text_units_local[unit_idx_local])) then
                    begin
                        Exit(raw_query_alignment_allowed_local);
                    end;
                end;

                Exit(True);
            end;

            Result := raw_query_alignment_allowed_local;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        query_units := input_syllable_count;
        if query_units <= 0 then
        begin
            query_units := get_effective_compact_pinyin_unit_count(normalized_query);
        end;
        if (query_units < 2) or (query_units > 4) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalized_query);
        if Length(syllables_local) <> query_units then
        begin
            Exit;
        end;

        if not dictionary_prefix_lookup_cached(normalized_query, prefix_results) then
        begin
            Exit;
        end;

        accepted_count := 0;
        for result_idx := 0 to High(prefix_results) do
        begin
            if accepted_count >= c_prefix_completion_limit then
            begin
                Break;
            end;

            if Trim(prefix_results[result_idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text := Trim(prefix_results[result_idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if (candidate_text = '') or (candidate_units <> query_units) then
            begin
                Continue;
            end;
            if not prefix_completion_alignment_allowed_local(candidate_text) then
            begin
                Continue;
            end;

            candidate := prefix_results[result_idx];
            base_score := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > base_score) then
            begin
                base_score := candidate.dict_weight;
            end;
            candidate.score := base_score + c_prefix_completion_visible_bonus;
            if candidate.dict_weight <= 0 then
            begin
                candidate.dict_weight := base_score;
            end;
            candidate.has_dict_weight := True;

            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate;
            Inc(accepted_count);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_fixed_wo_tail_dictionary_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_tail_probe_limit = 8;
        c_fixed_wo_tail_base_bonus = 6400;
        c_fixed_wo_tail_unit_bonus = 920;
    var
        head_pinyin: string;
        head_text: string;
        normalized_query: string;
        tail_key: string;
        tail_units: Integer;
        tail_results: TncCandidateList;
        seen: TDictionary<string, Byte>;
        accepted_before: Integer;
        exact_accepted: Boolean;

        procedure append_tail_candidates_local(const candidates: TncCandidateList);
        var
            local_idx: Integer;
            candidate: TncCandidate;
            tail_text: string;
            tail_text_units: Integer;
            tail_score: Integer;
            seen_key: string;
        begin
            for local_idx := 0 to High(candidates) do
            begin
                if local_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(candidates[local_idx]) or
                    is_runtime_common_pattern_candidate(candidates[local_idx]) or
                    is_runtime_redup_candidate(candidates[local_idx]) then
                begin
                    Continue;
                end;

                tail_text := Trim(candidates[local_idx].text);
                tail_text_units := get_candidate_text_unit_count(tail_text);
                if (tail_text = '') or (tail_text_units < 2) or
                    (tail_text_units > 3) then
                begin
                    Continue;
                end;
                if (not m_dictionary.is_base_entry(tail_key, tail_text)) and
                    (not m_dictionary.is_user_entry(tail_key, tail_text)) then
                begin
                    Continue;
                end;

                seen_key := tail_text;
                if seen.ContainsKey(seen_key) then
                begin
                    Continue;
                end;

                tail_score := candidates[local_idx].score;
                if candidates[local_idx].has_dict_weight then
                begin
                    tail_score := candidates[local_idx].dict_weight;
                end;
                if candidates[local_idx].source = cs_user then
                begin
                    Inc(tail_score, c_user_score_bonus);
                end;

                FillChar(candidate, SizeOf(candidate), 0);
                candidate.text := head_text + tail_text;
                candidate.comment := '';
                candidate.source := cs_rule;
                candidate.score := tail_score + c_fixed_wo_tail_base_bonus +
                    (tail_text_units * c_fixed_wo_tail_unit_bonus);
                candidate.has_dict_weight := True;
                candidate.dict_weight := candidate.score;
                remember_segment_path_for_candidate(candidate.text, '',
                    head_text + c_segment_path_separator + tail_text,
                    candidate.score);

                SetLength(out_candidates, Length(out_candidates) + 1);
                out_candidates[High(out_candidates)] := candidate;
                seen.AddOrSetValue(seen_key, 1);
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        if not try_get_fixed_pronoun_tail_query_parts(normalized_query,
            input_syllable_count, head_pinyin, head_text, tail_key,
            tail_units) then
        begin
            Exit;
        end;

        if tail_key = '' then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            exact_accepted := False;
            if dictionary_exact_lookup_cached(tail_key, tail_results) then
            begin
                accepted_before := Length(out_candidates);
                append_tail_candidates_local(tail_results);
                exact_accepted := Length(out_candidates) > accepted_before;
            end;
            if (not (exact_accepted and is_full_pinyin_key(tail_key))) and
                dictionary_lookup_cached(tail_key, tail_results) then
            begin
                append_tail_candidates_local(tail_results);
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_short_particle_tail_dictionary_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_head_probe_limit = 8;
        c_particle_tail_min_head_score = 80;
        c_particle_tail_visible_bonus = 160;
        c_particle_tail_unit_bonus = 24;
        c_particle_tail_weak_bonus = 8;
    var
        normalized_query: string;
        head_units: Integer;
        head_key: string;
        tail_key: string;
        tail_text: string;
        head_results: TncCandidateList;
        seen: TDictionary<string, Byte>;

        function text_ends_with_tail_particle_local(
            const text_value: string; const tail_text_value: string): Boolean;
        var
            trimmed_text_local: string;
            last_char_local: string;
        begin
            Result := False;
            trimmed_text_local := Trim(text_value);
            if (trimmed_text_local = '') or (tail_text_value = '') then
            begin
                Exit;
            end;
            last_char_local := Copy(trimmed_text_local,
                Length(trimmed_text_local), 1);
            Result := SameText(last_char_local, tail_text_value);
        end;

        function has_full_query_exact_candidate_local: Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_units_local: Integer;
        begin
            Result := False;
            if not dictionary_exact_lookup_cached(normalized_query,
                exact_results_local) then
            begin
                Exit;
            end;
            exact_units_local := Length(get_effective_compact_pinyin_syllables(
                normalized_query));
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    (Trim(exact_results_local[exact_idx_local].text) <> '') and
                    (get_candidate_text_unit_count(
                    Trim(exact_results_local[exact_idx_local].text)) =
                    exact_units_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function derived_particle_tail_score_local(
            const base_score: Integer): Integer;
        begin
            Result := base_score;
            if Result < 0 then
            begin
                Result := 0;
            end;
            if Result < c_particle_tail_min_head_score then
            begin
                Inc(Result, c_particle_tail_weak_bonus);
                Exit;
            end;

            Inc(Result, c_particle_tail_visible_bonus +
                (head_units * c_particle_tail_unit_bonus));
        end;

        procedure append_head_candidates_local(const candidates: TncCandidateList);
        var
            local_idx: Integer;
            candidate: TncCandidate;
            head_text: string;
            head_score: Integer;
            head_dict_weight: Integer;
            seen_key: string;
        begin
            for local_idx := 0 to High(candidates) do
            begin
                if local_idx >= c_head_probe_limit then
                begin
                    Break;
                end;
                if Trim(candidates[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(candidates[local_idx]) or
                    is_runtime_common_pattern_candidate(candidates[local_idx]) or
                    is_runtime_redup_candidate(candidates[local_idx]) then
                begin
                    Continue;
                end;

                head_text := Trim(candidates[local_idx].text);
                if (head_text = '') or
                    (get_candidate_text_unit_count(head_text) <> head_units) then
                begin
                    Continue;
                end;
                if text_ends_with_tail_particle_local(head_text, tail_text) then
                begin
                    Continue;
                end;

                seen_key := head_text + tail_text;
                if seen.ContainsKey(seen_key) then
                begin
                    Continue;
                end;

                head_score := candidates[local_idx].score;
                head_dict_weight := head_score;
                if candidates[local_idx].has_dict_weight and
                    (candidates[local_idx].dict_weight > head_score) then
                begin
                    head_score := candidates[local_idx].dict_weight;
                    head_dict_weight := candidates[local_idx].dict_weight;
                end;
                if candidates[local_idx].source = cs_user then
                begin
                    Inc(head_score, c_user_score_bonus);
                end;

                FillChar(candidate, SizeOf(candidate), 0);
                candidate.text := head_text + tail_text;
                candidate.comment := '';
                candidate.source := cs_rule;
                candidate.score :=
                    derived_particle_tail_score_local(head_score);
                candidate.has_dict_weight := True;
                candidate.dict_weight := head_dict_weight;

                SetLength(out_candidates, Length(out_candidates) + 1);
                out_candidates[High(out_candidates)] := candidate;
                seen.AddOrSetValue(seen_key, 1);
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        if not try_get_short_particle_tail_query_parts(normalized_query,
            tail_key, tail_text, head_key) then
        begin
            Exit;
        end;
        if head_key = '' then
        begin
            Exit;
        end;
        if has_full_query_exact_candidate_local and
            (not SameText(tail_key, 'a')) then
        begin
            Exit;
        end;

        head_units := get_effective_compact_pinyin_unit_count(head_key);
        if (head_units < 2) or (head_units > 3) then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            if dictionary_exact_lookup_cached(head_key, head_results) then
            begin
                append_head_candidates_local(head_results);
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_short_particle_tail_from_prefix_partials_fast_local(
        const query_text: string; const prefix_candidates: TncCandidateList;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_particle_tail_min_head_score = 80;
        c_particle_tail_visible_bonus = 160;
        c_particle_tail_unit_bonus = 24;
        c_particle_tail_weak_bonus = 8;
    var
        normalized_query: string;
        head_key: string;
        tail_key: string;
        tail_text: string;
        expected_head_units: Integer;
        idx: Integer;
        head_text: string;
        candidate: TncCandidate;
        base_weight: Integer;
        seen: TDictionary<string, Byte>;
        seen_key: string;

        function derived_particle_tail_score_local(const base_score: Integer;
            const unit_count: Integer): Integer;
        begin
            Result := base_score;
            if Result < 0 then
            begin
                Result := 0;
            end;
            if Result < c_particle_tail_min_head_score then
            begin
                Inc(Result, c_particle_tail_weak_bonus);
                Exit;
            end;

            Inc(Result, c_particle_tail_visible_bonus +
                (unit_count * c_particle_tail_unit_bonus));
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;

        normalized_query := normalize_pinyin_text(query_text);
        if not try_get_short_particle_tail_query_parts(normalized_query,
            tail_key, tail_text, head_key) then
        begin
            Exit;
        end;

        expected_head_units := get_effective_compact_pinyin_unit_count(
            head_key);
        if expected_head_units < 2 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            for idx := 0 to High(prefix_candidates) do
            begin
                if not SameText(normalize_pinyin_text(
                    Trim(prefix_candidates[idx].comment)), tail_key) then
                begin
                    Continue;
                end;

                head_text := Trim(prefix_candidates[idx].text);
                if (head_text = '') or
                    (get_candidate_text_unit_count(head_text) <>
                    expected_head_units) or
                    SameText(Copy(head_text, Length(head_text), 1),
                    tail_text) then
                begin
                    Continue;
                end;

                seen_key := head_text + tail_text;
                if seen.ContainsKey(seen_key) then
                begin
                    Continue;
                end;

                candidate := prefix_candidates[idx];
                base_weight := candidate.score;
                if candidate.has_dict_weight and (candidate.dict_weight > 0) then
                begin
                    base_weight := candidate.dict_weight;
                end;
                candidate.text := head_text + tail_text;
                candidate.comment := '';
                candidate.source := cs_rule;
                candidate.score := derived_particle_tail_score_local(
                    base_weight, expected_head_units);
                candidate.has_dict_weight := True;
                candidate.dict_weight := base_weight;

                SetLength(out_candidates, Length(out_candidates) + 1);
                out_candidates[High(out_candidates)] := candidate;
                seen.AddOrSetValue(seen_key, 1);
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    procedure suppress_non_fixed_wo_single_head_partials_local(
        var candidates: TncCandidateList);
    var
        has_fixed_wo_tail_candidate: Boolean;
        head_pinyin: string;
        head_text: string;
        tail_key: string;
        tail_units: Integer;
        idx: Integer;
        out_idx: Integer;
        candidate_text: string;
        candidate_units: TArray<string>;

        function is_exact_multi_char_prefix_local(const text_value: string;
            const unit_count: Integer): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            prefix_key_local: string;
            syllable_idx_local: Integer;
            exact_candidates_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (unit_count < 2) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            if Length(syllables_local) < unit_count then
            begin
                Exit;
            end;

            prefix_key_local := '';
            for syllable_idx_local := 0 to unit_count - 1 do
            begin
                prefix_key_local := prefix_key_local + normalize_pinyin_text(
                    syllables_local[syllable_idx_local].text);
            end;
            if prefix_key_local = '' then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(prefix_key_local,
                exact_candidates_local) then
            begin
                Exit;
            end;
            for exact_idx_local := 0 to High(exact_candidates_local) do
            begin
                if (Trim(exact_candidates_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_candidates_local[exact_idx_local].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or
            (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
            input_syllable_count, head_pinyin, head_text, tail_key,
            tail_units)) then
        begin
            Exit;
        end;

        has_fixed_wo_tail_candidate := False;
        for idx := 0 to High(candidates) do
        begin
            candidate_text := Trim(candidates[idx].text);
            if (Trim(candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text) >= 2) and
                (Copy(candidate_text, 1, 1) = head_text) then
            begin
                has_fixed_wo_tail_candidate := True;
                Break;
            end;
        end;
        if not has_fixed_wo_tail_candidate then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            candidate_text := Trim(candidates[idx].text);
            candidate_units := split_text_units(candidate_text);
            if (Trim(candidates[idx].comment) <> '') and
                (Length(candidate_units) >= 2) and
                (Trim(candidate_units[0]) <> head_text) and
                (m_dictionary <> nil) and
                m_dictionary.single_char_matches_pinyin(head_pinyin,
                Trim(candidate_units[0])) and
                (not is_exact_multi_char_prefix_local(candidate_text,
                Length(candidate_units))) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                candidates[out_idx] := candidates[idx];
            end;
            Inc(out_idx);
        end;
        if out_idx <> Length(candidates) then
        begin
            SetLength(candidates, out_idx);
        end;
    end;

    function build_long_exact_prefix_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_suffix_window = 4;
        c_prefix_head_window_max = 6;
        c_prefix_total_limit = 12;
        c_prefix_per_probe_limit = 3;
        c_prefix_base_bonus = 920;
        c_prefix_bonus_per_syllable = 72;
        c_prefix_remaining_penalty = 26;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        prefix_count: Integer;
        accepted_total: Integer;
        seen: TDictionary<string, Byte>;

        procedure try_append_prefix_count(const local_prefix_count: Integer);
        var
            local_tail_idx: Integer;
            local_result_idx: Integer;
            local_prefix_key: string;
            local_tail_comment: string;
            local_head_results: TncCandidateList;
            local_candidate: TncCandidate;
            local_candidate_text: string;
            local_candidate_units: Integer;
            local_score_value: Integer;
            local_seen_key: string;
            local_accepted_for_prefix: Integer;
        begin
            if (local_prefix_count < 4) or
                (local_prefix_count >= Length(syllables_local)) then
            begin
                Exit;
            end;

            local_prefix_key := '';
            for local_tail_idx := 0 to local_prefix_count - 1 do
            begin
                local_prefix_key := local_prefix_key +
                    normalize_pinyin_text(syllables_local[local_tail_idx].text);
            end;
            if local_prefix_key = '' then
            begin
                Exit;
            end;

            local_tail_comment := '';
            for local_tail_idx := local_prefix_count to High(syllables_local) do
            begin
                local_tail_comment := local_tail_comment +
                    normalize_pinyin_text(syllables_local[local_tail_idx].text);
            end;
            if local_tail_comment = '' then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(local_prefix_key, local_head_results) then
            begin
                Exit;
            end;

            local_accepted_for_prefix := 0;
            for local_result_idx := 0 to High(local_head_results) do
            begin
                if (accepted_total >= c_prefix_total_limit) or
                    (local_accepted_for_prefix >= c_prefix_per_probe_limit) then
                begin
                    Break;
                end;
                if Trim(local_head_results[local_result_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(local_head_results[local_result_idx]) or
                    is_runtime_common_pattern_candidate(local_head_results[local_result_idx]) or
                    is_runtime_redup_candidate(local_head_results[local_result_idx]) then
                begin
                    Continue;
                end;

                local_candidate_text := Trim(local_head_results[local_result_idx].text);
                if local_candidate_text = '' then
                begin
                    Continue;
                end;

                local_candidate_units := get_candidate_text_unit_count(local_candidate_text);
                if local_candidate_units <> local_prefix_count then
                begin
                    Continue;
                end;

                local_seen_key := local_candidate_text + #9 + local_tail_comment;
                if seen.ContainsKey(local_seen_key) then
                begin
                    Continue;
                end;

                local_candidate := local_head_results[local_result_idx];
                local_candidate.comment := local_tail_comment;
                local_score_value := local_candidate.score;
                if local_candidate.has_dict_weight and
                    (local_candidate.dict_weight > local_score_value) then
                begin
                    local_score_value := local_candidate.dict_weight;
                end;
                local_candidate.score := local_score_value + c_prefix_base_bonus +
                    (local_prefix_count * c_prefix_bonus_per_syllable) -
                    ((Length(syllables_local) - local_prefix_count) *
                    c_prefix_remaining_penalty);
                local_candidate.dict_weight := local_score_value;
                local_candidate.has_dict_weight := True;

                SetLength(out_candidates, Length(out_candidates) + 1);
                out_candidates[High(out_candidates)] := local_candidate;
                seen.AddOrSetValue(local_seen_key, 1);
                Inc(accepted_total);
                Inc(local_accepted_for_prefix);
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if (normalized_query = '') or (not is_full_pinyin_key(normalized_query)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(normalized_query);
        if Length(syllables_local) < 6 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            for prefix_count := Length(syllables_local) - 1 downto
                Max(4, Length(syllables_local) - c_prefix_suffix_window) do
            begin
                try_append_prefix_count(prefix_count);
            end;

            for prefix_count := Min(c_prefix_head_window_max,
                Length(syllables_local) - 2) downto 4 do
            begin
                if prefix_count >= Max(4, Length(syllables_local) - c_prefix_suffix_window) then
                begin
                    Continue;
                end;
                try_append_prefix_count(prefix_count);
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_raw_leading_prefix_partial_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_total_limit = c_candidate_total_limit_max;
        c_prefix_single_limit = c_candidate_total_limit_max;
        c_prefix_phrase_limit = 4;
        // A four-syllable raw-prefix page can otherwise hide a valid low-weight
        // exact prefix behind dozens of first-syllable character pages.
        c_four_syllable_prefix_phrase_limit = 16;
        c_prefix_base_bonus = 220;
        c_prefix_bonus_per_syllable = 180;
        c_prefix_remaining_penalty = 42;
        c_prefix_max_syllables = 3;
        c_prefix_phrase_extra_bonus = 520;
        c_prefix_short_query_phrase_bonus = 260;
    var
        normalized_query: string;
        parser: TncPinyinParser;
        syllables_local: TncPinyinParseResult;
        prefix_count: Integer;
        tail_idx: Integer;
        result_idx: Integer;
        accepted_total: Integer;
        accepted_for_prefix: Integer;
        accepted_limit: Integer;
        prefix_key: string;
        tail_comment: string;
        head_results: TncCandidateList;
        prefix_lookup_results: TncCandidateList;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        seen_key: string;
        seen: TDictionary<string, Byte>;

        procedure append_alternative_pair_prefixes;
        var
            boundary, shift, split_pos, idx, tail_idx: Integer;
            head_key, tail_key: string;
            heads, tails: TncCandidateList;
            item: TncCandidate;
            tail_is_exact: Boolean;
        begin
            if (Length(syllables_local) <> 4) or is_shuangpin_input or
                (Pos('''', normalized_query) > 0) then Exit;
            boundary := Length(syllables_local[0].text) +
                Length(syllables_local[1].text);
            // Adjacent n/g boundaries can hide an exact word (xin/an vs xi/nan).
            // Recover prefixes only; this must not authorize a new 2+2 compound.
            for shift := -1 to 1 do
            begin
                if shift = 0 then Continue;
                split_pos := boundary + shift;
                if (split_pos < 2) or (split_pos >= Length(normalized_query)) then Continue;
                if not CharInSet(normalized_query[Min(boundary, split_pos) + 1],
                    ['n', 'g']) then Continue;
                head_key := Copy(normalized_query, 1, split_pos);
                tail_key := Copy(normalized_query, split_pos + 1, MaxInt);
                if (not is_full_pinyin_key(head_key)) or
                    (not is_full_pinyin_key(tail_key)) or
                    (Length(get_effective_compact_pinyin_syllables(head_key)) <> 2) or
                    (Length(get_effective_compact_pinyin_syllables(tail_key)) <> 2) then Continue;
                if not dictionary_exact_lookup_cached(tail_key, tails) then Continue;
                tail_is_exact := False;
                for tail_idx := 0 to High(tails) do
                    if (tails[tail_idx].comment = '') and
                        (get_candidate_text_unit_count(tails[tail_idx].text) = 2) then
                    begin
                        tail_is_exact := True;
                        Break;
                    end;
                if (not tail_is_exact) or
                    (not dictionary_exact_lookup_cached(head_key, heads)) then Continue;
                for idx := 0 to Min(High(heads), c_four_syllable_prefix_phrase_limit - 1) do
                begin
                    if accepted_total >= c_prefix_total_limit then Break;
                    item := heads[idx];
                    if (item.comment <> '') or
                        (get_candidate_text_unit_count(item.text) <> 2) then Continue;
                    if seen.ContainsKey(item.text + #9 + tail_key) then Continue;
                    item.comment := tail_key;
                    Inc(item.score, c_prefix_base_bonus + 2 * c_prefix_bonus_per_syllable +
                        c_prefix_phrase_extra_bonus + c_prefix_short_query_phrase_bonus -
                        2 * c_prefix_remaining_penalty);
                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := item;
                    seen.AddOrSetValue(item.text + #9 + tail_key, 1);
                    Inc(accepted_total);
                end;
            end;
        end;

        function is_single_initial_tail_comment_local(const value: string): Boolean;
        var
            normalized_value: string;
        begin
            normalized_value := normalize_pinyin_text(value);
            Result := (Length(normalized_value) = 1) and
                CharInSet(normalized_value[1],
                ['b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n',
                'p', 'q', 'r', 's', 't', 'w', 'x', 'y', 'z']);
        end;

        function explicit_prefix_entry_local(const text_value: string): Boolean;
        var
            local_lookup_idx: Integer;
        begin
            Result := prefix_count < 2;
            if Result then
            begin
                Exit;
            end;

            for local_lookup_idx := 0 to High(prefix_lookup_results) do
            begin
                if (Trim(prefix_lookup_results[local_lookup_idx].comment) = '') and
                    SameText(Trim(prefix_lookup_results[local_lookup_idx].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        parser := TncPinyinParser.Create;
        try
            syllables_local := parser.parse(normalized_query);
        finally
            parser.Free;
        end;
        if Length(syllables_local) < 2 then
        begin
            Exit;
        end;

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            for prefix_count := Min(c_prefix_max_syllables,
                Length(syllables_local) - 1) downto 1 do
            begin
                if prefix_count = 1 then append_alternative_pair_prefixes;
                // Phrase prefixes must consume the bounded pool before the
                // much larger first-syllable character set.
                prefix_key := '';
                for tail_idx := 0 to prefix_count - 1 do
                begin
                    prefix_key := prefix_key +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if prefix_key = '' then
                begin
                    Continue;
                end;

                tail_comment := '';
                for tail_idx := prefix_count to High(syllables_local) do
                begin
                    tail_comment := tail_comment +
                        normalize_pinyin_text(syllables_local[tail_idx].text);
                end;
                if tail_comment = '' then
                begin
                    Continue;
                end;
                if (prefix_count >= 3) and
                    (prefix_count = Length(syllables_local) - 1) and
                    is_single_initial_tail_comment_local(tail_comment) then
                begin
                    Continue;
                end;

                if not dictionary_exact_lookup_cached(prefix_key, head_results) then
                begin
                    Continue;
                end;
                prefix_lookup_results := head_results;

                if prefix_count = 1 then
                begin
                    accepted_limit := c_prefix_single_limit;
                end
                else
                begin
                    if Length(syllables_local) = 4 then
                    begin
                        accepted_limit := c_four_syllable_prefix_phrase_limit;
                    end
                    else
                    begin
                        accepted_limit := c_prefix_phrase_limit;
                    end;
                end;

                accepted_for_prefix := 0;
                for result_idx := 0 to High(head_results) do
                begin
                    if (accepted_total >= c_prefix_total_limit) or
                        (accepted_for_prefix >= accepted_limit) then
                    begin
                        Break;
                    end;
                    if Trim(head_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[result_idx].text);
                    if candidate_text = '' then
                    begin
                        Continue;
                    end;

                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if prefix_count = 1 then
                    begin
                        if candidate_units <> 1 then
                        begin
                            Continue;
                        end;
                    end
                    else if candidate_units < 2 then
                    begin
                        Continue;
                    end;
                    if not explicit_prefix_entry_local(candidate_text) then
                    begin
                        Continue;
                    end;

                    seen_key := candidate_text + #9 + tail_comment;
                    if seen.ContainsKey(seen_key) then
                    begin
                        Continue;
                    end;

                    candidate := head_results[result_idx];
                    candidate.comment := tail_comment;
                    Inc(candidate.score, c_prefix_base_bonus +
                        (prefix_count * c_prefix_bonus_per_syllable));
                    if prefix_count >= 2 then
                    begin
                        Inc(candidate.score, c_prefix_phrase_extra_bonus);
                        if Length(syllables_local) <= 4 then
                        begin
                            Inc(candidate.score, c_prefix_short_query_phrase_bonus);
                        end;
                    end;
                    Dec(candidate.score, (Length(syllables_local) - prefix_count) *
                        c_prefix_remaining_penalty);

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate;
                    seen.AddOrSetValue(seen_key, 1);
                    Inc(accepted_total);
                    Inc(accepted_for_prefix);
                end;
            end;
        finally
            seen.Free;
        end;
        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    function build_string_prefix_exact_partial_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_prefix_total_limit = 12;
        c_prefix_probe_limit = 10;
        c_prefix_base_bonus = 760;
        c_prefix_unit_bonus = 260;
        c_tail_penalty = 18;
    var
        normalized_query: string;
        prefix_len: Integer;
        prefix_key: string;
        tail_comment: string;
        head_results: TncCandidateList;
        result_idx: Integer;
        accepted_total: Integer;
        candidate: TncCandidate;
        candidate_text: string;
        candidate_units: Integer;
        candidate_score: Integer;
        seen_key: string;
        cache_key: string;
        cached_candidates: TncCandidateList;
        seen: TDictionary<string, Byte>;
        protected_syllables: TncPinyinParseResult;

        function tail_is_single_initial_extension_local(
            const value: string): Boolean;
        var
            normalized_value: string;
        begin
            normalized_value := normalize_pinyin_text(value);
            Result := (Length(normalized_value) = 1) and
                CharInSet(normalized_value[1],
                ['b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n',
                'p', 'q', 'r', 's', 't', 'w', 'x', 'y', 'z']);
        end;

        function prefix_tail_alignment_allowed_local: Boolean;
        var
            part: TncPinyinSyllable;
        begin
            // A trailing initial must not split a completed syllable:
            // zhen+zheng+d is not zhen+zhen+gd. Alternate full-syllable
            // parses (jian+gei vs jiang+ei) are deliberately unaffected.
            for part in protected_syllables do
                if (prefix_len > part.start_index) and
                    (prefix_len < part.start_index + part.length) and
                    nc_is_canonical_pinyin_syllable(part.text) then
                    Exit(False);
            if is_full_pinyin_key(prefix_key) then
            begin
                Exit(True);
            end;

            // Non-full prefix matches are predictive completions for an
            // unfinished current syllable, such as "yongc" -> "yongchu".
            // Once additional non-initial letters have been typed, the prefix
            // no longer aligns with the user's input; otherwise "gengbuy"
            // incorrectly becomes a "gengb/uy" partial.
            Result := tail_is_single_initial_extension_local(tail_comment);
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        normalized_query := normalize_pinyin_text(query_text);
        if (m_dictionary = nil) or (normalized_query = '') then
        begin
            Exit;
        end;
        cache_key := '#stringprefixexact#' + normalized_query;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, cached_candidates) then
        begin
            out_candidates := Copy(cached_candidates, 0, Length(cached_candidates));
            Exit(Length(out_candidates) > 0);
        end;

        if has_raw_safe_trailing_initial_typing_state and
            SameText(normalized_query, lookup_text) then
            protected_syllables := get_effective_compact_pinyin_syllables(normalized_query);

        seen := TDictionary<string, Byte>.Create;
        try
            accepted_total := 0;
            for prefix_len := Length(normalized_query) - 1 downto 1 do
            begin
                if accepted_total >= c_prefix_total_limit then
                begin
                    Break;
                end;

                prefix_key := Copy(normalized_query, 1, prefix_len);
                tail_comment := Copy(normalized_query, prefix_len + 1,
                    Length(normalized_query) - prefix_len);
                if tail_comment = '' then
                begin
                    Continue;
                end;
                if not prefix_tail_alignment_allowed_local then
                begin
                    Continue;
                end;
                if not dictionary_exact_lookup_cached(prefix_key, head_results) then
                begin
                    Continue;
                end;

                for result_idx := 0 to High(head_results) do
                begin
                    if (accepted_total >= c_prefix_total_limit) or
                        (result_idx >= c_prefix_probe_limit) then
                    begin
                        Break;
                    end;
                    if Trim(head_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(head_results[result_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if (candidate_text = '') or (candidate_units <= 0) then
                    begin
                        Continue;
                    end;

                    seen_key := candidate_text + #9 + tail_comment;
                    if seen.ContainsKey(seen_key) then
                    begin
                        Continue;
                    end;

                    candidate := head_results[result_idx];
                    candidate.comment := tail_comment;
                    candidate_score := candidate.score;
                    if candidate.has_dict_weight and
                        (candidate.dict_weight > candidate_score) then
                    begin
                        candidate_score := candidate.dict_weight;
                    end;
                    candidate.score := candidate_score + c_prefix_base_bonus +
                        candidate_units * c_prefix_unit_bonus -
                        Length(tail_comment) * c_tail_penalty;
                    candidate.has_dict_weight := True;
                    candidate.dict_weight := candidate_score;

                    SetLength(out_candidates, Length(out_candidates) + 1);
                    out_candidates[High(out_candidates)] := candidate;
                    seen.AddOrSetValue(seen_key, 1);
                    Inc(accepted_total);
                end;
            end;
        finally
            seen.Free;
        end;

        sort_candidates_lightweight(out_candidates);
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key,
                Copy(out_candidates, 0, Length(out_candidates)));
        end;
        Result := Length(out_candidates) > 0;
    end;

    function build_adjacent_swap_prefix_partial_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    var
        normalized_query: string;
        swap_idx: Integer;
        swap_value: string;
        swap_char: Char;
    begin
        SetLength(out_candidates, 0);
        Result := False;

        normalized_query := normalize_pinyin_text(query_text);
        if (normalized_query = '') or (Length(normalized_query) < 5) or
            (Length(normalized_query) > 10) then
        begin
            Exit;
        end;

        for swap_idx := Length(normalized_query) - 1 downto 1 do
        begin
            if normalized_query[swap_idx] = normalized_query[swap_idx + 1] then
            begin
                Continue;
            end;

            swap_value := normalized_query;
            swap_char := swap_value[swap_idx];
            swap_value[swap_idx] := swap_value[swap_idx + 1];
            swap_value[swap_idx + 1] := swap_char;

            if build_string_prefix_exact_partial_candidates(swap_value,
                out_candidates) or
                build_exact_leading_prefix_partial_candidates(swap_value,
                out_candidates) or
                build_raw_leading_prefix_partial_candidates(swap_value,
                out_candidates) then
            begin
                Exit(True);
            end;
        end;
    end;

    function build_exact_head_modal_tail_complete_candidates(const query_text: string;
        out out_candidates: TncCandidateList): Boolean;
    const
        c_head_modal_complete_limit = 3;
        c_head_modal_complete_bonus = 1280;
    var
        normalized_query: string;
        syllables_local: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        tail_text: string;
        tail_units: TArray<string>;
        tail_syllable_count: Integer;
        idx: Integer;
        head_results: TncCandidateList;
        candidate: TncCandidate;
        accepted_count: Integer;
        score_value: Integer;
        path_hint: Integer;
        tail_path: string;

        function text_ends_with_tail_particle_local(
            const text_value: string; const tail_text_value: string): Boolean;
        var
            trimmed_text_local: string;
            last_char_local: string;
        begin
            Result := False;
            trimmed_text_local := Trim(text_value);
            if (trimmed_text_local = '') or (tail_text_value = '') then
            begin
                Exit;
            end;
            last_char_local := Copy(trimmed_text_local,
                Length(trimmed_text_local), 1);
            Result := SameText(last_char_local, tail_text_value);
        end;

        function has_full_query_exact_candidate_local: Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_units_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if not dictionary_exact_lookup_cached(normalized_query,
                exact_results_local) then
            begin
                Exit;
            end;
            exact_units_local := Length(syllables_local);
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local <> '') and
                    (get_candidate_text_unit_count(exact_text_local) =
                    exact_units_local) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if normalized_query = '' then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(normalized_query);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;
        if has_full_query_exact_candidate_local then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        tail_text := '';
        SetLength(tail_units, 0);
        tail_syllable_count := 0;
        if tail_key = 'ba' then
        begin
            tail_text := string(Char($5427));
        end
        else if tail_key = 'ma' then
        begin
            tail_text := string(Char($5417));
        end
        else if tail_key = 'ne' then
        begin
            tail_text := string(Char($5462));
        end
        else if tail_key = 'a' then
        begin
            tail_text := string(Char($554A));
        end
        else if tail_key = 'ya' then
        begin
            tail_text := string(Char($5440));
        end
        else if tail_key = 'la' then
        begin
            tail_text := string(Char($5566));
        end
        else if tail_key = 'le' then
        begin
            tail_text := string(Char($4E86));
        end
        else if tail_key = 'wa' then
        begin
            tail_text := string(Char($54C7));
        end;
        if tail_key = 'le' then
        begin
            tail_units := TArray<string>.Create(tail_text);
            tail_syllable_count := 1;
        end
        else if tail_text <> '' then
        begin
            if (Length(syllables_local) >= 2) and
                SameText(normalize_pinyin_text(
                syllables_local[High(syllables_local) - 1].text), 'le') then
            begin
                tail_units := TArray<string>.Create(string(Char($4E86)), tail_text);
                tail_syllable_count := 2;
            end
            else
            begin
                tail_units := TArray<string>.Create(tail_text);
                tail_syllable_count := 1;
            end;
        end;
        if tail_syllable_count <= 0 then
        begin
            Exit;
        end;

        head_key := '';
        for idx := 0 to High(syllables_local) - tail_syllable_count do
        begin
            head_key := head_key + normalize_pinyin_text(syllables_local[idx].text);
        end;
        if head_key = '' then
        begin
            Exit;
        end;

        if not dictionary_exact_lookup_cached(head_key, head_results) then
        begin
            Exit;
        end;

        accepted_count := 0;
        for idx := 0 to High(head_results) do
        begin
            if accepted_count >= c_head_modal_complete_limit then
            begin
                Break;
            end;
            if Trim(head_results[idx].comment) <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(head_results[idx].text)) <>
                (Length(syllables_local) - tail_syllable_count) then
            begin
                Continue;
            end;
            if text_ends_with_tail_particle_local(Trim(head_results[idx].text),
                tail_text) then
            begin
                Continue;
            end;

            candidate := head_results[idx];
            score_value := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > score_value) then
            begin
                score_value := candidate.dict_weight;
            end;

            candidate.text := Trim(candidate.text) + nc_join_strings('', tail_units);
            candidate.comment := '';
            candidate.source := cs_rule;
            candidate.has_dict_weight := False;
            candidate.dict_weight := 0;
            candidate.score := score_value + c_head_modal_complete_bonus;
            path_hint := score_value + c_head_modal_complete_bonus;
            tail_path := nc_join_strings(c_segment_path_separator, tail_units);
            remember_segment_path_for_candidate(candidate.text, '',
                Trim(head_results[idx].text) + c_segment_path_separator + tail_path,
                path_hint);

            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate;
            Inc(accepted_count);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := Length(out_candidates) > 0;
    end;

    procedure finalize_le_plus_modal_particle_sentence_candidate(
        var candidates: TncCandidateList);
    const
        c_le_modal_finalize_bonus = 1180;
    var
        syllables_local: TncPinyinParseResult;
        tail_key: string;
        expected_units: TArray<string>;
        expected_tail_text: string;
        expected_first_tail_text: string;
        idx: Integer;
        candidate_units: Integer;
        best_index: Integer;
        best_score: Integer;
        existing_index: Integer;
        preferred_existing_index: Integer;
        preferred_existing_score: Integer;
        preferred_partial_index: Integer;
        preferred_partial_score: Integer;
        preferred_le_tail_partial_index: Integer;
        preferred_le_tail_partial_score: Integer;
        segment_path: string;
        segment_parts: TArray<string>;
        corrected_candidate: TncCandidate;
        corrected_text: string;
        corrected_path: string;
        text_units: TArray<string>;
        part_idx: Integer;
        tail_idx: Integer;
        tail_ok: Boolean;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 5) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        expected_tail_text := '';
        if tail_key = 'ba' then
        begin
            expected_tail_text := string(Char($5427));
        end
        else if tail_key = 'ma' then
        begin
            expected_tail_text := string(Char($5417));
        end
        else if tail_key = 'ne' then
        begin
            expected_tail_text := string(Char($5462));
        end
        else if tail_key = 'a' then
        begin
            expected_tail_text := string(Char($554A));
        end
        else if tail_key = 'ya' then
        begin
            expected_tail_text := string(Char($5440));
        end
        else if tail_key = 'la' then
        begin
            expected_tail_text := string(Char($5566));
        end
        else if tail_key = 'wa' then
        begin
            expected_tail_text := string(Char($54C7));
        end;

        if (expected_tail_text = '') or
            (not SameText(normalize_pinyin_text(
            syllables_local[High(syllables_local) - 1].text), 'le')) then
        begin
            Exit;
        end;

        expected_units := TArray<string>.Create(string(Char($4E86)), expected_tail_text);
        expected_first_tail_text := expected_units[0];
        preferred_existing_index := -1;
        preferred_existing_score := Low(Integer);
        preferred_partial_index := -1;
        preferred_partial_score := Low(Integer);
        preferred_le_tail_partial_index := -1;
        preferred_le_tail_partial_score := Low(Integer);
        best_index := -1;
        best_score := Low(Integer);
        existing_index := -1;

        for idx := 0 to High(candidates) do
        begin
            candidate_units := get_candidate_text_unit_count(Trim(candidates[idx].text));

            if SameText(Trim(candidates[idx].comment), 'le' + tail_key) then
            begin
                if (candidate_units = input_syllable_count - 2) and
                    (candidates[idx].score > preferred_le_tail_partial_score) then
                begin
                    preferred_le_tail_partial_score := candidates[idx].score;
                    preferred_le_tail_partial_index := idx;
                end;
            end;

            if SameText(Trim(candidates[idx].comment), tail_key) then
            begin
                if candidate_units = input_syllable_count - 1 then
                begin
                    text_units := split_text_units(Trim(candidates[idx].text));
                    if (Length(text_units) > 0) and
                        SameText(Trim(text_units[High(text_units)]), expected_first_tail_text) and
                        (candidates[idx].score > preferred_partial_score) then
                    begin
                        preferred_partial_score := candidates[idx].score;
                        preferred_partial_index := idx;
                    end;
                end;
            end;

            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_units := get_candidate_text_unit_count(Trim(candidates[idx].text));
            if candidate_units = input_syllable_count then
            begin
                text_units := split_text_units(Trim(candidates[idx].text));
                if Length(text_units) >= 2 then
                begin
                    if SameText(Trim(text_units[High(text_units) - 1]), expected_units[0]) and
                        SameText(Trim(text_units[High(text_units)]), expected_units[1]) and
                        (candidates[idx].score > preferred_existing_score) then
                    begin
                        preferred_existing_score := candidates[idx].score;
                        preferred_existing_index := idx;
                    end;
                end;
            end;

            if candidate_units <> input_syllable_count then
            begin
                Continue;
            end;

            segment_path := get_segment_path_for_candidate(candidates[idx]);
            if get_encoded_path_segment_count_local(segment_path) <= 2 then
            begin
                Continue;
            end;
            segment_parts := segment_path.Split([c_segment_path_separator]);
            if Length(segment_parts) <= 2 then
            begin
                Continue;
            end;

            tail_ok := True;
            for tail_idx := 0 to 1 do
            begin
                if get_candidate_text_unit_count(
                    Trim(segment_parts[High(segment_parts) - 1 + tail_idx])) <> 1 then
                begin
                    tail_ok := False;
                    Break;
                end;
            end;
            if not tail_ok then
            begin
                Continue;
            end;

            corrected_text := '';
            corrected_path := '';
            for part_idx := 0 to High(segment_parts) - 2 do
            begin
                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                if corrected_path = '' then
                begin
                    corrected_path := Trim(segment_parts[part_idx]);
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        Trim(segment_parts[part_idx]);
                end;
            end;
            for tail_idx := 0 to High(expected_units) do
            begin
                corrected_text := corrected_text + expected_units[tail_idx];
                if corrected_path = '' then
                begin
                    corrected_path := expected_units[tail_idx];
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        expected_units[tail_idx];
                end;
            end;

            if SameText(corrected_text, Trim(candidates[idx].text)) then
            begin
                if (preferred_existing_index < 0) and
                    (candidates[idx].score > preferred_existing_score) then
                begin
                    preferred_existing_score := candidates[idx].score;
                    preferred_existing_index := idx;
                end;
                Continue;
            end;

            if existing_index < 0 then
            begin
                for part_idx := 0 to High(candidates) do
                begin
                    if (Trim(candidates[part_idx].comment) = '') and
                        SameText(Trim(candidates[part_idx].text), corrected_text) then
                    begin
                        existing_index := part_idx;
                        Break;
                    end;
                end;
            end;

            if candidates[idx].score > best_score then
            begin
                best_score := candidates[idx].score;
                best_index := idx;
            end;
        end;

        if preferred_le_tail_partial_index >= 0 then
        begin
            corrected_candidate := candidates[preferred_le_tail_partial_index];
            corrected_candidate.text := Trim(corrected_candidate.text) +
                expected_units[0] + expected_units[1];
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            corrected_candidate.has_dict_weight := False;
            corrected_candidate.dict_weight := 0;
            corrected_candidate.score := Max(corrected_candidate.score +
                c_le_modal_finalize_bonus, candidates[0].score + c_le_modal_finalize_bonus);
            segment_path := get_segment_path_for_candidate(
                candidates[preferred_le_tail_partial_index]);
            if segment_path = '' then
            begin
                corrected_path := Trim(candidates[preferred_le_tail_partial_index].text) +
                    c_segment_path_separator + expected_units[0] +
                    c_segment_path_separator + expected_units[1];
            end
            else
            begin
                corrected_path := segment_path + c_segment_path_separator +
                    expected_units[0] + c_segment_path_separator + expected_units[1];
            end;
            remember_segment_path_for_candidate(corrected_candidate.text, '',
                corrected_path, corrected_candidate.score);
            for idx := Length(candidates) - 1 downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := corrected_candidate;
            Exit;
        end;

        if preferred_partial_index >= 0 then
        begin
            corrected_candidate := candidates[preferred_partial_index];
            corrected_candidate.text := Trim(corrected_candidate.text) + expected_units[1];
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            corrected_candidate.has_dict_weight := False;
            corrected_candidate.dict_weight := 0;
            corrected_candidate.score := Max(corrected_candidate.score +
                c_le_modal_finalize_bonus, candidates[0].score + c_le_modal_finalize_bonus);
            segment_path := get_segment_path_for_candidate(candidates[preferred_partial_index]);
            if segment_path = '' then
            begin
                corrected_path := Trim(candidates[preferred_partial_index].text) +
                    c_segment_path_separator + expected_units[1];
            end
            else
            begin
                corrected_path := segment_path + c_segment_path_separator + expected_units[1];
            end;
            remember_segment_path_for_candidate(corrected_candidate.text, '',
                corrected_path, corrected_candidate.score);
            for idx := Length(candidates) - 1 downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := corrected_candidate;
            Exit;
        end;

        if preferred_existing_index > 0 then
        begin
            corrected_candidate := candidates[preferred_existing_index];
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            Inc(corrected_candidate.score, c_le_modal_finalize_bonus);
            for idx := preferred_existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := corrected_candidate;
            Exit;
        end;

        if best_index < 0 then
        begin
            Exit;
        end;

        if existing_index >= 0 then
        begin
            corrected_candidate := candidates[existing_index];
            Inc(corrected_candidate.score, c_le_modal_finalize_bonus);
            corrected_candidate.source := cs_rule;
            corrected_candidate.comment := '';
        end
        else
        begin
            corrected_candidate := candidates[best_index];
            segment_path := get_segment_path_for_candidate(candidates[best_index]);
            segment_parts := segment_path.Split([c_segment_path_separator]);
            corrected_text := '';
            corrected_path := '';
            for part_idx := 0 to High(segment_parts) - 2 do
            begin
                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                if corrected_path = '' then
                begin
                    corrected_path := Trim(segment_parts[part_idx]);
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        Trim(segment_parts[part_idx]);
                end;
            end;
            for tail_idx := 0 to High(expected_units) do
            begin
                corrected_text := corrected_text + expected_units[tail_idx];
                if corrected_path = '' then
                begin
                    corrected_path := expected_units[tail_idx];
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        expected_units[tail_idx];
                end;
            end;

            corrected_candidate.text := corrected_text;
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            corrected_candidate.has_dict_weight := False;
            corrected_candidate.dict_weight := 0;
            Inc(corrected_candidate.score, c_le_modal_finalize_bonus);
            remember_segment_path_for_candidate(corrected_candidate.text, '',
                corrected_path, corrected_candidate.score);
        end;

        for idx := Length(candidates) - 1 downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := corrected_candidate;
    end;

    function build_dangling_exact_head_chain_sentence_candidates(
        const query_text: string; out out_candidates: TncCandidateList): Boolean;
    const
        c_chain_max_depth = 8;
        c_chain_min_prefix_len = 3;
        c_chain_max_prefix_len = 14;
        c_chain_max_prefix_hits_per_depth = 6;
        c_chain_max_results_per_prefix = 2;
        c_chain_completion_bonus = 1200;
        c_chain_segment_penalty = 280;
        c_chain_prefix_len_bonus = 90;
    var
        normalized_query: string;
        best_scores: TDictionary<string, Integer>;

        procedure remember_complete_chain(const text_value: string;
            const path_value: string; const score_value: Integer);
        var
            previous_score: Integer;
            candidate: TncCandidate;
            idx: Integer;
            encoded_path: string;
        begin
            if (Trim(text_value) = '') or
                (get_candidate_text_unit_count(Trim(text_value)) <> input_syllable_count) then
            begin
                Exit;
            end;

            if best_scores.TryGetValue(text_value, previous_score) and
                (previous_score >= score_value) then
            begin
                Exit;
            end;
            best_scores.AddOrSetValue(text_value, score_value);

            encoded_path := path_value;
            if (encoded_path <> '') and (encoded_path[1] = c_segment_path_separator) then
            begin
                Delete(encoded_path, 1, 1);
            end;

            for idx := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[idx].text), Trim(text_value)) then
                begin
                    out_candidates[idx].score := score_value;
                    out_candidates[idx].comment := '';
                    out_candidates[idx].source := cs_rule;
                    out_candidates[idx].has_dict_weight := False;
                    out_candidates[idx].dict_weight := 0;
                    remember_segment_path_for_candidate(out_candidates[idx].text, '',
                        encoded_path, score_value);
                    Exit;
                end;
            end;

            candidate := Default(TncCandidate);
            candidate.text := text_value;
            candidate.comment := '';
            candidate.score := score_value;
            candidate.source := cs_rule;
            candidate.has_dict_weight := False;
            candidate.dict_weight := 0;
            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate;
            remember_segment_path_for_candidate(candidate.text, '', encoded_path,
                score_value);
        end;

        procedure build_chain_recursive(const remaining_query: string;
            const composed_text: string; const composed_path: string;
            const accumulated_score: Integer; const depth: Integer);
        var
            prefix_len: Integer;
            prefix_key: string;
            allow_nonfull_tail: Boolean;
            exact_results: TncCandidateList;
            result_idx: Integer;
            prefix_hit_count: Integer;
            accepted_count: Integer;
            piece_text: string;
            piece_score: Integer;
            piece_units: Integer;
            next_text: string;
            next_path: string;
            next_remaining: string;
        begin
            if remaining_query = '' then
            begin
                remember_complete_chain(composed_text, composed_path,
                    accumulated_score + c_chain_completion_bonus -
                    (depth * c_chain_segment_penalty));
                Exit;
            end;
            if depth >= c_chain_max_depth then
            begin
                Exit;
            end;

            prefix_hit_count := 0;
            for prefix_len := Min(c_chain_max_prefix_len, Length(remaining_query)) downto
                c_chain_min_prefix_len do
            begin
                prefix_key := Copy(remaining_query, 1, prefix_len);
                allow_nonfull_tail := prefix_len = Length(remaining_query);
                if (not is_full_pinyin_key(prefix_key)) and (not allow_nonfull_tail) then
                begin
                    Continue;
                end;
                if not dictionary_exact_lookup_cached(prefix_key, exact_results) then
                begin
                    Continue;
                end;

                accepted_count := 0;
                for result_idx := 0 to High(exact_results) do
                begin
                    if accepted_count >= c_chain_max_results_per_prefix then
                    begin
                        Break;
                    end;
                    if Trim(exact_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(exact_results[result_idx]) or
                        is_runtime_common_pattern_candidate(exact_results[result_idx]) or
                        is_runtime_redup_candidate(exact_results[result_idx]) then
                    begin
                        Continue;
                    end;

                    piece_text := Trim(exact_results[result_idx].text);
                    if piece_text = '' then
                    begin
                        Continue;
                    end;
                    piece_units := get_candidate_text_unit_count(piece_text);
                    if (depth = 0) and (piece_units = 1) and
                        (prefix_len < Length(remaining_query)) then
                    begin
                        Continue;
                    end;

                    next_remaining := Copy(remaining_query, prefix_len + 1, MaxInt);
                    piece_score := exact_results[result_idx].score;
                    if exact_results[result_idx].has_dict_weight and
                        (exact_results[result_idx].dict_weight > piece_score) then
                    begin
                        piece_score := exact_results[result_idx].dict_weight;
                    end;
                    if piece_score <= 0 then
                    begin
                        piece_score := 1;
                    end;

                    next_text := composed_text + piece_text;
                    next_path := composed_path + c_segment_path_separator + piece_text;
                    build_chain_recursive(next_remaining, next_text, next_path,
                        accumulated_score + piece_score +
                        (prefix_len * c_chain_prefix_len_bonus), depth + 1);
                    Inc(accepted_count);
                end;

                if accepted_count > 0 then
                begin
                    Inc(prefix_hit_count);
                    if prefix_hit_count >= c_chain_max_prefix_hits_per_depth then
                    begin
                        Break;
                    end;
                end;
            end;
        end;
    begin
        SetLength(out_candidates, 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(query_text);
        if (normalized_query = '') or (Length(normalized_query) < 8) then
        begin
            Exit;
        end;

        best_scores := TDictionary<string, Integer>.Create;
        try
            build_chain_recursive(normalized_query, '', '', 0, 0);
        finally
            best_scores.Free;
        end;

        if Length(out_candidates) = 0 then
        begin
            Exit(False);
        end;

        sort_candidates_lightweight(out_candidates);
        Result := True;
    end;

    procedure apply_shifted_boundary_longer_exact_penalty(
        var candidates: TncCandidateList);
    const
        c_exact_probe_limit = 8;
        c_shifted_boundary_penalty = 3600;
        c_min_shifted_left_weight = 520;
        c_min_shifted_follow_weight = 520;
    var
        syllables_local: TncPinyinParseResult;
        idx: Integer;
        segment_path: string;
        segment_parts: TArray<string>;
        segment_idx: Integer;
        syllable_idx: Integer;
        left_units: Integer;
        right_units: Integer;
        adjustment: Integer;
        shifted_left_weight: Integer;
        shifted_follow_weight: Integer;

        function build_query_key_local(const start_idx: Integer;
            const syllable_count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + syllable_count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(syllables_local[local_idx].text);
            end;
        end;

        function get_exact_candidate_rank_weight(
            const candidate: TncCandidate): Integer;
        begin
            Result := candidate.score;
            if candidate.has_dict_weight and (candidate.dict_weight > Result) then
            begin
                Result := candidate.dict_weight;
            end;
        end;

        function try_get_best_exact_phrase_weight_local(const start_idx: Integer;
            const syllable_count_local: Integer; out out_weight: Integer): Boolean;
        var
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (m_dictionary = nil) or
                (start_idx < 0) or (syllable_count_local <= 0) or
                (start_idx + syllable_count_local - 1 > High(syllables_local)) then
            begin
                Exit;
            end;

            query_key := build_query_key_local(start_idx, syllable_count_local);
            if (query_key = '') or
                (not m_dictionary.lookup_exact_full_pinyin(query_key, exact_results)) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_results) do
            begin
                if exact_idx >= c_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(exact_results[exact_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[exact_idx]) or
                    is_runtime_redup_candidate(exact_results[exact_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[exact_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> syllable_count_local then
                begin
                    Continue;
                end;

                weight_value := get_exact_candidate_rank_weight(exact_results[exact_idx]);
                if weight_value > out_weight then
                begin
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function try_get_best_shifted_follow_weight_local(
            const start_idx: Integer; out out_weight: Integer): Boolean;
        var
            probe_len: Integer;
            weight_value: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (start_idx < 0) or (start_idx > High(syllables_local)) then
            begin
                Exit;
            end;

            for probe_len := 1 to Min(3, Length(syllables_local) - start_idx) do
            begin
                if not try_get_best_exact_phrase_weight_local(start_idx,
                    probe_len, weight_value) then
                begin
                    Continue;
                end;
                if weight_value > out_weight then
                begin
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

    begin
        if (Length(candidates) = 0) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(m_last_lookup_key));
        if Length(syllables_local) <> m_last_lookup_syllable_count then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            segment_path := get_segment_path_for_candidate(candidates[idx]);
            if get_encoded_path_segment_count_local(segment_path) <= 1 then
            begin
                Continue;
            end;

            segment_parts := segment_path.Split([c_segment_path_separator]);
            syllable_idx := 0;
            adjustment := 0;
            for segment_idx := 0 to High(segment_parts) - 1 do
            begin
                left_units := get_candidate_text_unit_count(Trim(segment_parts[segment_idx]));
                right_units := get_candidate_text_unit_count(Trim(segment_parts[segment_idx + 1]));

                if (left_units >= 2) and (right_units >= 2) and
                    (syllable_idx + left_units + right_units - 1 <= High(syllables_local)) and
                    try_get_best_exact_phrase_weight_local(syllable_idx,
                    left_units + 1, shifted_left_weight) and
                    (shifted_left_weight >= c_min_shifted_left_weight) and
                    try_get_best_shifted_follow_weight_local(
                    syllable_idx + left_units + 1, shifted_follow_weight) and
                    (shifted_follow_weight >= c_min_shifted_follow_weight) then
                begin
                    Dec(adjustment, c_shifted_boundary_penalty);
                end;

                Inc(syllable_idx, left_units);
            end;

            if adjustment <> 0 then
            begin
                Inc(candidates[idx].score, adjustment);
            end;
        end;
    end;

    procedure apply_compact_long_phrase_bonus(var candidates: TncCandidateList);
    const
        c_compact_long_phrase_bonus = 760;
        c_compact_long_phrase_extra_per_unit = 120;
    var
        idx: Integer;
        encoded_path: string;
        segment_parts: TArray<string>;
        part_idx: Integer;
        segment_units: Integer;
        max_segment_units: Integer;
        path_segment_count: Integer;
    begin
        if (Length(candidates) = 0) or
            (input_syllable_count < c_long_sentence_exact_min_syllables) then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) <>
                input_syllable_count) or
                is_runtime_chain_candidate(candidates[idx]) or
                is_runtime_common_pattern_candidate(candidates[idx]) or
                is_runtime_redup_candidate(candidates[idx]) then
            begin
                Continue;
            end;

            encoded_path := get_segment_path_for_candidate(candidates[idx]);
            path_segment_count := get_encoded_path_segment_count_local(encoded_path);
            if path_segment_count <= 1 then
            begin
                Continue;
            end;

            max_segment_units := 0;
            segment_parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(segment_parts) do
            begin
                if Trim(segment_parts[part_idx]) = '' then
                begin
                    Continue;
                end;
                segment_units := get_candidate_text_unit_count(Trim(segment_parts[part_idx]));
                if segment_units > max_segment_units then
                begin
                    max_segment_units := segment_units;
                end;
            end;

            if max_segment_units < 3 then
            begin
                Continue;
            end;

            Inc(candidates[idx].score,
                c_compact_long_phrase_bonus +
                Max(0, max_segment_units - 3) * c_compact_long_phrase_extra_per_unit);
        end;
    end;

    procedure apply_preferred_sentence_phrase_bonus(
        var candidates: TncCandidateList);
    begin
        // Phrase preference is intentionally dictionary-driven. Avoid hard-coded
        // sentence substring bonuses here; short phrases should be represented in
        // the lexicon and resolved through exact chunk lookup.
    end;
    procedure ensure_modal_tail_sentence_candidate_visible(var candidates: TncCandidateList);
    const
        c_modal_tail_correction_bonus = 980;
    var
        syllables_local: TncPinyinParseResult;
        tail_key: string;
        expected_text: string;
        idx: Integer;
        best_index: Integer;
        best_score: Integer;
        existing_index: Integer;
        segment_path: string;
        corrected_path: string;
        corrected_text: string;
        segment_parts: TArray<string>;
        corrected_candidate: TncCandidate;
        part_idx: Integer;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        expected_text := '';
        if tail_key = 'ba' then
        begin
            expected_text := string(Char($5427));
        end
        else if tail_key = 'ma' then
        begin
            expected_text := string(Char($5417));
        end
        else if tail_key = 'ne' then
        begin
            expected_text := string(Char($5462));
        end
        else if tail_key = 'a' then
        begin
            expected_text := string(Char($554A));
        end
        else if tail_key = 'ya' then
        begin
            expected_text := string(Char($5440));
        end
        else if tail_key = 'la' then
        begin
            expected_text := string(Char($5566));
        end
        else if tail_key = 'le' then
        begin
            expected_text := string(Char($4E86));
        end
        else if tail_key = 'wa' then
        begin
            expected_text := string(Char($54C7));
        end;
        if expected_text = '' then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        existing_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidates[idx].text)) <> input_syllable_count) then
            begin
                Continue;
            end;

            segment_path := get_segment_path_for_candidate(candidates[idx]);
            if get_encoded_path_segment_count_local(segment_path) <= 1 then
            begin
                Continue;
            end;

            segment_parts := segment_path.Split([c_segment_path_separator]);
            if Length(segment_parts) <= 1 then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(segment_parts[High(segment_parts)])) <> 1 then
            begin
                Continue;
            end;

            corrected_text := '';
            corrected_path := '';
            for part_idx := 0 to High(segment_parts) - 1 do
            begin
                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                if corrected_path = '' then
                begin
                    corrected_path := Trim(segment_parts[part_idx]);
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        Trim(segment_parts[part_idx]);
                end;
            end;
            corrected_text := corrected_text + expected_text;
            if corrected_path = '' then
            begin
                corrected_path := expected_text;
            end
            else
            begin
                corrected_path := corrected_path + c_segment_path_separator + expected_text;
            end;

            if SameText(corrected_text, Trim(candidates[idx].text)) then
            begin
                if existing_index < 0 then
                begin
                    existing_index := idx;
                end;
                Continue;
            end;

            if existing_index < 0 then
            begin
                for part_idx := 0 to High(candidates) do
                begin
                    if (Trim(candidates[part_idx].comment) = '') and
                        SameText(Trim(candidates[part_idx].text), corrected_text) then
                    begin
                        existing_index := part_idx;
                        Break;
                    end;
                end;
            end;

            if candidates[idx].score > best_score then
            begin
                best_score := candidates[idx].score;
                best_index := idx;
            end;
        end;

        if best_index < 0 then
        begin
            Exit;
        end;

        segment_path := get_segment_path_for_candidate(candidates[best_index]);
        segment_parts := segment_path.Split([c_segment_path_separator]);
        corrected_text := '';
        corrected_path := '';
        for part_idx := 0 to High(segment_parts) - 1 do
        begin
            corrected_text := corrected_text + Trim(segment_parts[part_idx]);
            if corrected_path = '' then
            begin
                corrected_path := Trim(segment_parts[part_idx]);
            end
            else
            begin
                corrected_path := corrected_path + c_segment_path_separator +
                    Trim(segment_parts[part_idx]);
            end;
        end;
        corrected_text := corrected_text + expected_text;
        if corrected_path = '' then
        begin
            corrected_path := expected_text;
        end
        else
        begin
            corrected_path := corrected_path + c_segment_path_separator + expected_text;
        end;

        if existing_index >= 0 then
        begin
            Inc(candidates[existing_index].score, c_modal_tail_correction_bonus);
            candidates[existing_index].source := cs_rule;
            remember_segment_path_for_candidate(corrected_text, '',
                corrected_path, candidates[existing_index].score);
            Exit;
        end;

        corrected_candidate := candidates[best_index];
        corrected_candidate.text := corrected_text;
        corrected_candidate.comment := '';
        corrected_candidate.source := cs_rule;
        corrected_candidate.has_dict_weight := False;
        corrected_candidate.dict_weight := 0;
        Inc(corrected_candidate.score, c_modal_tail_correction_bonus);
        SetLength(candidates, Length(candidates) + 1);
        candidates[High(candidates)] := corrected_candidate;
        remember_segment_path_for_candidate(corrected_candidate.text, '',
            corrected_path, corrected_candidate.score);
    end;

    procedure finalize_modal_tail_sentence_candidate(var candidates: TncCandidateList);
    const
        c_modal_tail_finalize_bonus = 960;
    var
        syllables_local: TncPinyinParseResult;
        tail_key: string;
        expected_text: string;
        idx: Integer;
        candidate_units: Integer;
        best_index: Integer;
        best_score: Integer;
        existing_index: Integer;
        preferred_existing_index: Integer;
        preferred_existing_score: Integer;
        expanded_tail_index: Integer;
        expanded_tail_score: Integer;
        expanded_tail_text: string;
        expanded_tail_path: string;
        segment_path: string;
        segment_parts: TArray<string>;
        corrected_candidate: TncCandidate;
        corrected_text: string;
        corrected_path: string;
        part_idx: Integer;
        last_units: TArray<string>;
        last_segment_units: Integer;
        last_segment_start: Integer;
        tail_head_len: Integer;
        tail_head_key: string;
        tail_head_results: TncCandidateList;
        tail_head_idx: Integer;
        tail_head_text: string;
        tail_head_score: Integer;
        candidate_modal_score: Integer;
        fixed_quantity_modal_tail_text: string;
        fixed_quantity_modal_tail_units: TArray<string>;
        fixed_quantity_tail_score: Integer;
        fixed_quantity_tail_ready: Boolean;
        candidate_comment: string;
        candidate_text_value: string;

        function is_quantity_tail_unit_local(const unit_text: string): Boolean;
        begin
            Result :=
                SameText(unit_text, string(Char($6B21))) or
                SameText(unit_text, string(Char($4E2A))) or
                SameText(unit_text, string(Char($5929))) or
                SameText(unit_text, string(Char($65E5))) or
                SameText(unit_text, string(Char($8F6E))) or
                SameText(unit_text, string(Char($6B65))) or
                SameText(unit_text, string(Char($6761))) or
                SameText(unit_text, string(Char($6839))) or
                SameText(unit_text, string(Char($6279))) or
                SameText(unit_text, string(Char($5339))) or
                SameText(unit_text, string(Char($53F0))) or
                SameText(unit_text, string(Char($628A))) or
                SameText(unit_text, string(Char($526F))) or
                SameText(unit_text, string(Char($5BF9))) or
                SameText(unit_text, string(Char($53CC))) or
                SameText(unit_text, string(Char($74F6))) or
                SameText(unit_text, string(Char($6876))) or
                SameText(unit_text, string(Char($680B))) or
                SameText(unit_text, string(Char($5E62))) or
                SameText(unit_text, string(Char($7247))) or
                SameText(unit_text, string(Char($76D8))) or
                SameText(unit_text, string(Char($53EA)));
        end;

        function candidate_ends_with_units_local(const units: TArray<string>;
            const suffix_units: TArray<string>): Boolean;
        var
            suffix_idx: Integer;
            units_idx: Integer;
        begin
            Result := False;
            if (Length(units) < Length(suffix_units)) or
                (Length(suffix_units) = 0) then
            begin
                Exit;
            end;

            for suffix_idx := 0 to High(suffix_units) do
            begin
                units_idx := Length(units) - Length(suffix_units) + suffix_idx;
                if not SameText(Trim(units[units_idx]),
                    Trim(suffix_units[suffix_idx])) then
                begin
                    Exit;
                end;
            end;
            Result := True;
        end;

        function candidate_ends_with_fixed_quantity_head_local(
            const units: TArray<string>): Boolean;
        var
            suffix_len: Integer;
            suffix_idx: Integer;
            units_idx: Integer;
        begin
            Result := False;
            suffix_len := Length(fixed_quantity_modal_tail_units) - 1;
            if (suffix_len <= 0) or (Length(units) < suffix_len) then
            begin
                Exit;
            end;

            for suffix_idx := 0 to suffix_len - 1 do
            begin
                units_idx := Length(units) - suffix_len + suffix_idx;
                if not SameText(Trim(units[units_idx]),
                    Trim(fixed_quantity_modal_tail_units[suffix_idx])) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function concat_first_units_local(const units: TArray<string>;
            const unit_count: Integer): string;
        var
            unit_idx: Integer;
            safe_count: Integer;
        begin
            Result := '';
            safe_count := unit_count;
            if safe_count < 0 then
            begin
                safe_count := 0;
            end;
            if safe_count > Length(units) then
            begin
                safe_count := Length(units);
            end;

            for unit_idx := 0 to safe_count - 1 do
            begin
                Result := Result + Trim(units[unit_idx]);
            end;
        end;

        function try_get_fixed_quantity_modal_tail_local(out out_text: string;
            out out_score: Integer): Boolean;
        var
            quantity_key: string;
            quantity_results: TncCandidateList;
            quantity_idx: Integer;
            quantity_text: string;
            quantity_units: TArray<string>;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (tail_key <> 'ba') or (input_syllable_count < 3) or
                (not SameText(normalize_pinyin_text(
                syllables_local[input_syllable_count - 3].text), 'yi')) then
            begin
                Exit;
            end;

            quantity_key := 'yi' + normalize_pinyin_text(
                syllables_local[input_syllable_count - 2].text);
            if (quantity_key = 'yi') or
                (not dictionary_exact_lookup_cached(quantity_key,
                quantity_results)) then
            begin
                Exit;
            end;

            for quantity_idx := 0 to High(quantity_results) do
            begin
                if Trim(quantity_results[quantity_idx].comment) <> '' then
                begin
                    Continue;
                end;
                quantity_text := Trim(quantity_results[quantity_idx].text);
                quantity_units := split_text_units(quantity_text);
                if (Length(quantity_units) <> 2) or
                    (not SameText(Trim(quantity_units[0]),
                    string(Char($4E00)))) or
                    (not is_quantity_tail_unit_local(Trim(quantity_units[1]))) then
                begin
                    Continue;
                end;

                out_text := quantity_text + expected_text;
                out_score := quantity_results[quantity_idx].score;
                if quantity_results[quantity_idx].has_dict_weight and
                    (quantity_results[quantity_idx].dict_weight > out_score) then
                begin
                    out_score := quantity_results[quantity_idx].dict_weight;
                end;
                Exit(True);
            end;
        end;

        procedure remove_fixed_quantity_modal_tail_mismatches_local(
            var list: TncCandidateList);
        var
            remove_idx: Integer;
            shift_idx: Integer;
            units: TArray<string>;
        begin
            if not fixed_quantity_tail_ready then
            begin
                Exit;
            end;

            for remove_idx := High(list) downto 1 do
            begin
                if Trim(list[remove_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(list[remove_idx].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;

                units := split_text_units(Trim(list[remove_idx].text));
                if candidate_ends_with_units_local(units,
                    fixed_quantity_modal_tail_units) then
                begin
                    Continue;
                end;

                for shift_idx := remove_idx to High(list) - 1 do
                begin
                    list[shift_idx] := list[shift_idx + 1];
                end;
                SetLength(list, Length(list) - 1);
            end;
        end;

        function has_one_quantity_modal_tail_local(
            const units: TArray<string>): Boolean;
        begin
            Result := (Length(units) >= 3) and
                SameText(Trim(units[High(units)]), expected_text) and
                SameText(Trim(units[High(units) - 2]), string(Char($4E00))) and
                is_quantity_tail_unit_local(Trim(units[High(units) - 1]));
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        expected_text := '';
        if tail_key = 'ba' then
        begin
            expected_text := string(Char($5427));
        end
        else if tail_key = 'ma' then
        begin
            expected_text := string(Char($5417));
        end
        else if tail_key = 'ne' then
        begin
            expected_text := string(Char($5462));
        end
        else if tail_key = 'a' then
        begin
            expected_text := string(Char($554A));
        end
        else if tail_key = 'ya' then
        begin
            expected_text := string(Char($5440));
        end
        else if tail_key = 'la' then
        begin
            expected_text := string(Char($5566));
        end
        else if tail_key = 'le' then
        begin
            expected_text := string(Char($4E86));
        end
        else if tail_key = 'wa' then
        begin
            expected_text := string(Char($54C7));
        end;
        if expected_text = '' then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        existing_index := -1;
        preferred_existing_index := -1;
        preferred_existing_score := Low(Integer);
        expanded_tail_index := -1;
        expanded_tail_score := Low(Integer);
        expanded_tail_text := '';
        expanded_tail_path := '';
        fixed_quantity_tail_ready := try_get_fixed_quantity_modal_tail_local(
            fixed_quantity_modal_tail_text, fixed_quantity_tail_score);
        if fixed_quantity_tail_ready then
        begin
            fixed_quantity_modal_tail_units :=
                split_text_units(fixed_quantity_modal_tail_text);
        end
        else
        begin
            SetLength(fixed_quantity_modal_tail_units, 0);
        end;
        for idx := 0 to High(candidates) do
        begin
            candidate_comment := normalize_pinyin_text(Trim(candidates[idx].comment));
            candidate_text_value := Trim(candidates[idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text_value);
            if fixed_quantity_tail_ready and SameText(candidate_comment,
                tail_key) and (candidate_units = input_syllable_count - 1) then
            begin
                last_units := split_text_units(candidate_text_value);
                if candidate_ends_with_fixed_quantity_head_local(last_units) then
                begin
                    corrected_text := candidate_text_value + expected_text;
                    candidate_modal_score := candidates[idx].score +
                        c_modal_tail_finalize_bonus +
                        fixed_quantity_tail_score + 60000;
                    if candidate_modal_score > expanded_tail_score then
                    begin
                        expanded_tail_score := candidate_modal_score;
                        expanded_tail_index := idx;
                        expanded_tail_text := corrected_text;
                        expanded_tail_path := get_segment_path_for_candidate(
                            candidates[idx], idx);
                        if expanded_tail_path = '' then
                        begin
                            expanded_tail_path := candidate_text_value;
                        end;
                        expanded_tail_path := expanded_tail_path +
                            c_segment_path_separator + expected_text;
                    end;
                end;
            end;

            if candidate_comment <> '' then
            begin
                Continue;
            end;

            if candidate_units = input_syllable_count then
            begin
                last_units := split_text_units(candidate_text_value);
                if (Length(last_units) > 0) and
                    SameText(Trim(last_units[High(last_units)]), expected_text) then
                begin
                    if fixed_quantity_tail_ready and
                        (not candidate_ends_with_units_local(last_units,
                        fixed_quantity_modal_tail_units)) then
                    begin
                        Continue;
                    end;
                    candidate_modal_score := candidates[idx].score;
                    if has_one_quantity_modal_tail_local(last_units) then
                    begin
                        Inc(candidate_modal_score, 60000);
                    end;
                    if candidate_modal_score > preferred_existing_score then
                    begin
                        preferred_existing_score := candidate_modal_score;
                        preferred_existing_index := idx;
                    end;
                end;
            end;

            if fixed_quantity_tail_ready and
                (candidate_units = input_syllable_count) then
            begin
                last_units := split_text_units(Trim(candidates[idx].text));
                if not candidate_ends_with_units_local(last_units,
                    fixed_quantity_modal_tail_units) then
                begin
                    corrected_text := concat_first_units_local(last_units,
                        input_syllable_count -
                        Length(fixed_quantity_modal_tail_units)) +
                        fixed_quantity_modal_tail_text;
                    if not SameText(corrected_text,
                        candidate_text_value) then
                    begin
                        candidate_modal_score := candidates[idx].score +
                            c_modal_tail_finalize_bonus +
                            fixed_quantity_tail_score + 60000;
                        if candidate_modal_score > expanded_tail_score then
                        begin
                            expanded_tail_score := candidate_modal_score;
                            expanded_tail_index := idx;
                            expanded_tail_text := corrected_text;
                            expanded_tail_path := '';
                        end;
                    end;
                end;
            end;

            if candidate_units <> input_syllable_count then
            begin
                Continue;
            end;

            segment_path := get_segment_path_for_candidate(candidates[idx]);
            if get_encoded_path_segment_count_local(segment_path) <= 1 then
            begin
                Continue;
            end;
            segment_parts := segment_path.Split([c_segment_path_separator]);
            if Length(segment_parts) <= 1 then
            begin
                Continue;
            end;

            last_segment_units := get_candidate_text_unit_count(
                Trim(segment_parts[High(segment_parts)]));
            if last_segment_units > 1 then
            begin
                last_segment_start := input_syllable_count - last_segment_units;
                tail_head_len := last_segment_units - 1;
                if (last_segment_start >= 0) and (tail_head_len >= 1) and
                    (last_segment_start + tail_head_len <= High(syllables_local)) then
                begin
                    tail_head_key := '';
                    for tail_head_idx := last_segment_start to
                        last_segment_start + tail_head_len - 1 do
                    begin
                        tail_head_key := tail_head_key + normalize_pinyin_text(
                            syllables_local[tail_head_idx].text);
                    end;

                    if (tail_head_key <> '') and
                        dictionary_exact_lookup_cached(tail_head_key,
                        tail_head_results) then
                    begin
                        for tail_head_idx := 0 to High(tail_head_results) do
                        begin
                            if Trim(tail_head_results[tail_head_idx].comment) <> '' then
                            begin
                                Continue;
                            end;
                            tail_head_text := Trim(tail_head_results[tail_head_idx].text);
                            if get_candidate_text_unit_count(tail_head_text) <>
                                tail_head_len then
                            begin
                                Continue;
                            end;
                            tail_head_score := tail_head_results[tail_head_idx].score;
                            if tail_head_results[tail_head_idx].has_dict_weight and
                                (tail_head_results[tail_head_idx].dict_weight >
                                tail_head_score) then
                            begin
                                tail_head_score := tail_head_results[tail_head_idx].dict_weight;
                            end;

                            corrected_text := '';
                            corrected_path := '';
                            for part_idx := 0 to High(segment_parts) - 1 do
                            begin
                                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                                if corrected_path = '' then
                                begin
                                    corrected_path := Trim(segment_parts[part_idx]);
                                end
                                else
                                begin
                                    corrected_path := corrected_path + c_segment_path_separator +
                                        Trim(segment_parts[part_idx]);
                                end;
                            end;
                            corrected_text := corrected_text + tail_head_text + expected_text;
                            if corrected_path = '' then
                            begin
                                corrected_path := tail_head_text;
                            end
                            else
                            begin
                                corrected_path := corrected_path + c_segment_path_separator +
                                    tail_head_text;
                            end;
                            corrected_path := corrected_path + c_segment_path_separator +
                                expected_text;

                            candidate_modal_score := candidates[idx].score +
                                c_modal_tail_finalize_bonus + tail_head_score;
                            if (not SameText(corrected_text, Trim(candidates[idx].text))) and
                                (candidate_modal_score > expanded_tail_score) then
                            begin
                                expanded_tail_score := candidate_modal_score;
                                expanded_tail_index := idx;
                                expanded_tail_text := corrected_text;
                                expanded_tail_path := corrected_path;
                            end;
                            Break;
                        end;
                    end;
                end;
                Continue;
            end;

            if last_segment_units <> 1 then
            begin
                Continue;
            end;

            corrected_text := '';
            corrected_path := '';
            for part_idx := 0 to High(segment_parts) - 1 do
            begin
                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                if corrected_path = '' then
                begin
                    corrected_path := Trim(segment_parts[part_idx]);
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        Trim(segment_parts[part_idx]);
                end;
            end;
            corrected_text := corrected_text + expected_text;

            if SameText(corrected_text, Trim(candidates[idx].text)) then
            begin
                if existing_index < 0 then
                begin
                    existing_index := idx;
                end;
                Continue;
            end;

            if existing_index < 0 then
            begin
                for part_idx := 0 to High(candidates) do
                begin
                    if (Trim(candidates[part_idx].comment) = '') and
                        SameText(Trim(candidates[part_idx].text), corrected_text) then
                    begin
                        existing_index := part_idx;
                        Break;
                    end;
                end;
            end;

            if candidates[idx].score > best_score then
            begin
                best_score := candidates[idx].score;
                best_index := idx;
            end;
        end;

        if preferred_existing_index > 0 then
        begin
            corrected_candidate := candidates[preferred_existing_index];
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            Inc(corrected_candidate.score, c_modal_tail_finalize_bonus);
            for idx := preferred_existing_index downto 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[0] := corrected_candidate;
            remove_fixed_quantity_modal_tail_mismatches_local(candidates);
            Exit;
        end;

        if best_index < 0 then
        begin
            if expanded_tail_index < 0 then
            begin
                Exit;
            end;
        end;

        if expanded_tail_index >= 0 then
        begin
            corrected_candidate := candidates[expanded_tail_index];
            corrected_candidate.text := expanded_tail_text;
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            corrected_candidate.has_dict_weight := False;
            corrected_candidate.dict_weight := 0;
            corrected_candidate.score := expanded_tail_score;
            remember_segment_path_for_candidate(corrected_candidate.text, '',
                expanded_tail_path, corrected_candidate.score);
        end
        else if existing_index >= 0 then
        begin
            corrected_candidate := candidates[existing_index];
            Inc(corrected_candidate.score, c_modal_tail_finalize_bonus);
            corrected_candidate.source := cs_rule;
            corrected_candidate.comment := '';
            remove_fixed_quantity_modal_tail_mismatches_local(candidates);
        end
        else
        begin
            corrected_candidate := candidates[best_index];
            segment_path := get_segment_path_for_candidate(candidates[best_index]);
            segment_parts := segment_path.Split([c_segment_path_separator]);
            corrected_text := '';
            corrected_path := '';
            for part_idx := 0 to High(segment_parts) - 1 do
            begin
                corrected_text := corrected_text + Trim(segment_parts[part_idx]);
                if corrected_path = '' then
                begin
                    corrected_path := Trim(segment_parts[part_idx]);
                end
                else
                begin
                    corrected_path := corrected_path + c_segment_path_separator +
                        Trim(segment_parts[part_idx]);
                end;
            end;
            corrected_text := corrected_text + expected_text;
            if corrected_path = '' then
            begin
                corrected_path := expected_text;
            end
            else
            begin
                corrected_path := corrected_path + c_segment_path_separator + expected_text;
            end;

            corrected_candidate.text := corrected_text;
            corrected_candidate.comment := '';
            corrected_candidate.source := cs_rule;
            corrected_candidate.has_dict_weight := False;
            corrected_candidate.dict_weight := 0;
            Inc(corrected_candidate.score, c_modal_tail_finalize_bonus);
            corrected_candidate.comment := '';
            remember_segment_path_for_candidate(corrected_candidate.text, '',
                corrected_path, corrected_candidate.score);
        end;

        for idx := Length(candidates) - 1 downto 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[0] := corrected_candidate;
        remove_fixed_quantity_modal_tail_mismatches_local(candidates);
    end;

    function build_segment_candidates_timed(out out_candidates: TncCandidateList;
        const include_full_path: Boolean = True;
        const allow_relaxed_split: Boolean = False): Boolean;
    var
        phase_start_tick: UInt64;
        local_path_search_elapsed_ms: Int64;
    begin
        phase_start_tick := nc_monotonic_tick_ms;
        local_path_search_elapsed_ms := 0;
        Result := build_segment_candidates(out_candidates, include_full_path, local_path_search_elapsed_ms,
            allow_relaxed_split);
        Inc(segment_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
        Inc(path_search_elapsed_ms, local_path_search_elapsed_ms);
    end;

    procedure sort_candidates_timed(var candidates: TncCandidateList);
    var
        phase_start_tick: UInt64;
        elapsed_ms_local: UInt64;
    begin
        phase_start_tick := nc_monotonic_tick_ms;
        sort_candidates(candidates);
        elapsed_ms_local := nc_monotonic_tick_ms - phase_start_tick;
        Inc(sort_elapsed_ms, Int64(elapsed_ms_local));
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' sort=[n=%d ms=%d]',
                [Length(candidates), elapsed_ms_local]);
        end;
    end;

    procedure note_post_phase_elapsed(const phase_start_tick: UInt64);
    begin
        Inc(post_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
    end;

    procedure note_debug_helper_elapsed(const phase_name: string;
        const phase_start_tick: UInt64);
    var
        elapsed_ms: UInt64;
    begin
        if not m_config.debug_mode then
        begin
            Exit;
        end;

        elapsed_ms := nc_monotonic_tick_ms - phase_start_tick;
        m_last_full_path_debug_info := m_last_full_path_debug_info +
            Format(' %s=%d', [phase_name, elapsed_ms]);
    end;

    function has_complete_phrase_candidate_for_syllable_count(
        const candidates: TncCandidateList; const expected_syllables: Integer): Boolean;
    var
        candidate_idx: Integer;
    begin
        Result := False;
        if (not is_fuzzy_pinyin_active) or
            (expected_syllables <= 0) then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if candidates[candidate_idx].comment <> '' then
            begin
                Continue;
            end;
            if (get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <
                expected_syllables) or
                (get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) >
                expected_syllables + 1) then
            begin
                Continue;
            end;
            Exit(True);
        end;
    end;

    function has_fuzzy_complete_candidate_for_syllable_count(
        const candidates: TncCandidateList;
        const expected_syllables: Integer): Boolean;
    var
        candidate_idx: Integer;
    begin
        Result := False;
        if (not is_fuzzy_pinyin_active) or
            (expected_syllables <= 0) then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if (candidates[candidate_idx].fuzzy_cost > 0) and
                (Trim(candidates[candidate_idx].comment) = '') and
                (get_candidate_text_unit_count(
                Trim(candidates[candidate_idx].text)) = expected_syllables) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure reserve_fuzzy_complete_candidates_local(
        var candidates: TncCandidateList; const visible_limit: Integer;
        const reserve_count: Integer = 3);
    var
        prefix_limit: Integer;
        reserve_limit: Integer;
        fuzzy_in_prefix: Integer;
        source_idx: Integer;
        target_idx: Integer;
        idx: Integer;
        candidate_tmp: TncCandidate;

        function is_fuzzy_complete_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := (candidate_value.fuzzy_cost > 0) and
                (Trim(candidate_value.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate_value.text)) =
                input_syllable_count);
        end;
    begin
        if (not m_config.fuzzy_pinyin_enabled) or
            (m_config.fuzzy_pinyin_rules = []) or
            (visible_limit <= 0) or (reserve_count <= 0) or
            (Length(candidates) = 0) then
        begin
            Exit;
        end;

        prefix_limit := Min(visible_limit, Length(candidates));
        reserve_limit := Min(reserve_count, prefix_limit);
        fuzzy_in_prefix := 0;
        for idx := 0 to prefix_limit - 1 do
        begin
            if is_fuzzy_complete_local(candidates[idx]) then
            begin
                Inc(fuzzy_in_prefix);
            end;
        end;
        while fuzzy_in_prefix < reserve_limit do
        begin
            source_idx := -1;
            for idx := prefix_limit to High(candidates) do
            begin
                if is_fuzzy_complete_local(candidates[idx]) then
                begin
                    source_idx := idx;
                    Break;
                end;
            end;
            if source_idx < 0 then
            begin
                Break;
            end;

            target_idx := prefix_limit - 1;
            while (target_idx >= 0) and
                is_fuzzy_complete_local(candidates[target_idx]) do
            begin
                Dec(target_idx);
            end;
            if target_idx < 0 then
            begin
                Break;
            end;

            candidate_tmp := candidates[target_idx];
            candidates[target_idx] := candidates[source_idx];
            candidates[source_idx] := candidate_tmp;
            Inc(fuzzy_in_prefix);
        end;
    end;

    function has_reliable_complete_phrase_candidate_for_syllable_count(
        const candidates: TncCandidateList; const expected_syllables: Integer): Boolean;
    var
        candidate_idx: Integer;
        candidate_units: Integer;
    begin
        Result := False;
        if expected_syllables <= 0 then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if candidates[candidate_idx].comment <> '' then
            begin
                Continue;
            end;
            candidate_units := get_candidate_text_unit_count(Trim(candidates[candidate_idx].text));
            if (candidate_units < expected_syllables) or
                (candidate_units > expected_syllables + 1) then
            begin
                Continue;
            end;

            if candidates[candidate_idx].source <> cs_user then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_weak_user_complete_phrase_candidate_for_syllable_count(
        const candidates: TncCandidateList; const expected_syllables: Integer): Boolean;
    const
        c_max_weak_user_weight = 1200;
    var
        candidate_idx: Integer;
        candidate_weight: Integer;
    begin
        Result := False;
        if expected_syllables <= 0 then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if (candidates[candidate_idx].source <> cs_user) or
                (candidates[candidate_idx].comment <> '') then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <>
                expected_syllables then
            begin
                Continue;
            end;

            candidate_weight := candidates[candidate_idx].score;
            if candidates[candidate_idx].has_dict_weight then
            begin
                candidate_weight := candidates[candidate_idx].dict_weight;
            end;
            if candidate_weight <= c_max_weak_user_weight then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_exact_complete_phrase_candidate_for_syllable_count(
        const candidates: TncCandidateList; const expected_syllables: Integer): Boolean;
    var
        candidate_idx: Integer;
    begin
        Result := False;
        if expected_syllables <= 0 then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if candidates[candidate_idx].comment <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <>
                expected_syllables then
            begin
                Continue;
            end;
            if candidates[candidate_idx].has_dict_weight or
                (candidates[candidate_idx].source = cs_user) then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_natural_complete_segment_path_candidate_for_syllable_count(
        const candidates: TncCandidateList; const expected_syllables: Integer): Boolean;
    var
        candidate_idx: Integer;
        encoded_path: string;
    begin
        Result := False;
        if expected_syllables <= 0 then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            if candidates[candidate_idx].comment <> '' then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <>
                expected_syllables then
            begin
                Continue;
            end;

            encoded_path := get_segment_path_for_candidate(candidates[candidate_idx]);
            if (Trim(encoded_path) = '') or
                (get_encoded_path_segment_count_local(encoded_path) <= 1) or
                (not is_natural_predictive_segment_path_local(encoded_path)) then
            begin
                Continue;
            end;

            Exit(True);
        end;
    end;

    function is_trusted_top_complete_candidate_for_early_exit(
        const candidates: TncCandidateList): Boolean;
    var
        candidate: TncCandidate;
    begin
        Result := False;
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        candidate := candidates[0];
        if Trim(candidate.comment) <> '' then
        begin
            Exit;
        end;

        if (Trim(candidate.text) = '') or
            (get_candidate_text_unit_count(Trim(candidate.text)) <>
            input_syllable_count) then
        begin
            Exit;
        end;

        if candidate.source = cs_user then
        begin
            Exit(True);
        end;

        Result := has_strong_top_complete_candidate_for_postprocess_lock_local(
            candidates);
    end;

    function has_useful_long_sentence_prediction_candidate(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_idx: Integer;
        encoded_path: string;
        segment_count: Integer;
        comment_units: Integer;
        path_idx: Integer;
    begin
        Result := False;
        for candidate_idx := 0 to High(candidates) do
        begin
            encoded_path := get_segment_path_for_candidate(candidates[candidate_idx]);
            segment_count := 0;
            if Trim(encoded_path) <> '' then
            begin
                segment_count := 1;
                for path_idx := 1 to Length(encoded_path) do
                begin
                    if encoded_path[path_idx] = c_segment_path_separator then
                    begin
                        Inc(segment_count);
                    end;
                end;
            end;
            if segment_count <= 1 then
            begin
                Continue;
            end;
            if not is_natural_predictive_segment_path_local(encoded_path) then
            begin
                Continue;
            end;
            if Trim(candidates[candidate_idx].comment) = '' then
            begin
                if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) >=
                    Max(2, input_syllable_count - 1) then
                begin
                    Exit(True);
                end;
                Continue;
            end;

            comment_units := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidates[candidate_idx].comment));
            if (comment_units <= 1) and
                (get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) >=
                Max(2, input_syllable_count - 2)) then
            begin
                Exit(True);
            end;
        end;
    end;

    function has_strong_delayed_long_decode_candidate(
        const candidates: TncCandidateList): Boolean;
    var
        candidate_idx: Integer;
        text_units: Integer;
        comment_units: Integer;
        candidate: TncCandidate;
        segment_score_hint: Integer;
    begin
        Result := False;
        if has_useful_long_sentence_prediction_candidate(candidates) then
        begin
            Exit(True);
        end;

        for candidate_idx := 0 to High(candidates) do
        begin
            candidate := candidates[candidate_idx];
            text_units := get_candidate_text_unit_count(Trim(candidate.text));
            if text_units < 2 then
            begin
                Continue;
            end;

            segment_score_hint := get_candidate_segment_path_score_hint(candidate);
            if candidate.comment = '' then
            begin
                if (text_units >= Max(2, input_syllable_count - 1)) and
                    (candidate.has_dict_weight or (candidate.source = cs_user) or
                    (segment_score_hint > Low(Integer))) then
                begin
                    Exit(True);
                end;
                Continue;
            end;

            comment_units := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate.comment));
            if (comment_units <= 1) and
                (text_units >= Max(2, input_syllable_count - 2)) and
                (candidate.has_dict_weight or (candidate.source = cs_user) or
                (segment_score_hint > Low(Integer))) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure trim_candidates_for_delayed_long_decode(
        var candidates: TncCandidateList);
    const
        c_complete_limit = 6;
        c_partial_limit = 8;
        c_single_partial_limit = 3;
        c_user_limit = 6;
    var
        sorted_candidates: TncCandidateList;
        trimmed_candidates: TncCandidateList;
        idx: Integer;
        out_idx: Integer;
        text_units: Integer;
        user_count: Integer;
        complete_count: Integer;
        partial_count: Integer;
        single_partial_count: Integer;
        candidate: TncCandidate;
    begin
        if Length(candidates) <= 0 then
        begin
            Exit;
        end;

        sorted_candidates := candidates;
        sort_candidates_lightweight(sorted_candidates);
        SetLength(trimmed_candidates, 0);
        out_idx := 0;
        user_count := 0;
        complete_count := 0;
        partial_count := 0;
        single_partial_count := 0;

        for idx := 0 to High(sorted_candidates) do
        begin
            candidate := sorted_candidates[idx];
            text_units := get_candidate_text_unit_count(Trim(candidate.text));

            if candidate.source = cs_user then
            begin
                if user_count >= c_user_limit then
                begin
                    Continue;
                end;
                Inc(user_count);
            end
            else if candidate.comment = '' then
            begin
                if complete_count >= c_complete_limit then
                begin
                    Continue;
                end;
                Inc(complete_count);
            end
            else if text_units <= 1 then
            begin
                if single_partial_count >= c_single_partial_limit then
                begin
                    Continue;
                end;
                Inc(single_partial_count);
            end
            else
            begin
                if partial_count >= c_partial_limit then
                begin
                    Continue;
                end;
                Inc(partial_count);
            end;

            SetLength(trimmed_candidates, out_idx + 1);
            trimmed_candidates[out_idx] := candidate;
            Inc(out_idx);
        end;

        candidates := trimmed_candidates;
    end;

    procedure trim_sorted_candidates_for_strong_long_sentence(
        var candidates: TncCandidateList);
    const
        c_complete_limit = 8;
        c_partial_limit = 12;
        c_single_partial_limit = 6;
        c_user_limit = 6;
    var
        trimmed_candidates: TncCandidateList;
        idx: Integer;
        out_idx: Integer;
        text_units: Integer;
        user_count: Integer;
        complete_count: Integer;
        partial_count: Integer;
        single_partial_count: Integer;
        candidate: TncCandidate;
    begin
        if Length(candidates) <= 0 then
        begin
            Exit;
        end;

        SetLength(trimmed_candidates, 0);
        out_idx := 0;
        user_count := 0;
        complete_count := 0;
        partial_count := 0;
        single_partial_count := 0;

        for idx := 0 to High(candidates) do
        begin
            candidate := candidates[idx];
            text_units := get_candidate_text_unit_count(Trim(candidate.text));

            if candidate.source = cs_user then
            begin
                if user_count >= c_user_limit then
                begin
                    Continue;
                end;
                Inc(user_count);
            end
            else if candidate.comment = '' then
            begin
                if complete_count >= c_complete_limit then
                begin
                    Continue;
                end;
                Inc(complete_count);
            end
            else if text_units <= 1 then
            begin
                if single_partial_count >= c_single_partial_limit then
                begin
                    Continue;
                end;
                Inc(single_partial_count);
            end
            else
            begin
                if partial_count >= c_partial_limit then
                begin
                    Continue;
                end;
                Inc(partial_count);
            end;

            SetLength(trimmed_candidates, out_idx + 1);
            trimmed_candidates[out_idx] := candidate;
            Inc(out_idx);
        end;

        candidates := trimmed_candidates;
    end;

    function top_complete_candidate_is_reliable_local(
        const candidates: TncCandidateList): Boolean;
    var
        candidate: TncCandidate;
    begin
        Result := False;
        if (Length(candidates) = 0) or
            (Trim(candidates[0].comment) <> '') then
        begin
            Exit;
        end;

        candidate := candidates[0];
        if (Trim(candidate.text) = '') or
            (get_candidate_text_unit_count(Trim(candidate.text)) <>
            input_syllable_count) then
        begin
            Exit;
        end;

        if candidate.source = cs_user then
        begin
            Exit(True);
        end;

        Result := has_strong_top_complete_candidate_for_postprocess_lock_local(
            candidates);
    end;

    function has_trusted_complete_top_candidate_local(
        const candidates: TncCandidateList): Boolean;
    begin
        Result := has_complete_top_candidate_for_fast_exit_local(candidates) and
            top_complete_candidate_is_reliable_local(candidates);
    end;

    function top_complete_candidate_has_duplicate_support_local(
        const candidates: TncCandidateList): Boolean;
    var
        top_text_local: string;
        idx_local: Integer;
        same_count_local: Integer;
    begin
        Result := False;
        if Length(candidates) < 2 then
        begin
            Exit;
        end;

        top_text_local := Trim(candidates[0].text);
        if (Trim(candidates[0].comment) <> '') or (top_text_local = '') or
            (get_candidate_text_unit_count(top_text_local) <> input_syllable_count) or
            is_problematic_single_char_chain_candidate_for_query(lookup_text,
            candidates[0]) or
            (not top_complete_candidate_is_reliable_local(candidates)) then
        begin
            Exit;
        end;

        same_count_local := 0;
        for idx_local := 0 to Min(3, High(candidates)) do
        begin
            if Trim(candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;
            if SameText(Trim(candidates[idx_local].text), top_text_local) then
            begin
                Inc(same_count_local);
            end;
        end;
        Result := same_count_local >= 2;
    end;

    function has_strong_near_complete_prefix_partial_candidate_local(
        const candidates: TncCandidateList): Boolean;
    var
        top_text_local: string;
        partial_text_local: string;
        normalized_comment_local: string;
        idx_local: Integer;
        tail_units_local: Integer;
    begin
        Result := False;
        if (Length(candidates) = 0) or
            (Trim(candidates[0].comment) <> '') then
        begin
            Exit;
        end;

        top_text_local := Trim(candidates[0].text);
        if top_text_local = '' then
        begin
            Exit;
        end;

        for idx_local := 1 to Min(get_candidate_limit * 2, High(candidates)) do
        begin
            normalized_comment_local := normalize_pinyin_text(
                Trim(candidates[idx_local].comment));
            if (normalized_comment_local = '') or
                (not is_full_pinyin_key(normalized_comment_local)) then
            begin
                Continue;
            end;

            tail_units_local := get_effective_compact_pinyin_unit_count(
                normalized_comment_local);
            if (tail_units_local <= 0) or (tail_units_local > 1) then
            begin
                Continue;
            end;

            partial_text_local := Trim(candidates[idx_local].text);
            if (partial_text_local = '') or
                (get_candidate_text_unit_count(partial_text_local) <
                input_syllable_count - tail_units_local) or
                (Length(partial_text_local) > Length(top_text_local)) or
                (not SameText(Copy(top_text_local, 1, Length(partial_text_local)),
                partial_text_local)) then
            begin
                Continue;
            end;

            Exit(True);
        end;
    end;

    function try_finalize_stable_long_candidate_local(
        const stage_tag: string; const allow_prefix_partial: Boolean;
        const prefix_head_units: Integer = 0): Boolean;
    var
        min_prefix_units_local: Integer;
        top_units_local: Integer;
    begin
        Result := False;
        if not m_composition_built_incrementally then
        begin
            Exit;
        end;
        if (Length(m_candidates) = 0) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 8) then
        begin
            Exit;
        end;

        if has_strong_near_complete_prefix_partial_candidate_local(m_candidates) then
        begin
            ensure_incomplete_tail_partial_precedes_completed_extension(
                m_candidates);
        end;

        if allow_prefix_partial and
            ((not has_complete_phrase_candidate_for_syllable_count(
            m_candidates, input_syllable_count)) or
            has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) then
        begin
            ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(
                m_candidates);
            ensure_best_dict_exact_prefix_partial_visible(m_candidates);
            ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(
                m_candidates);
            ensure_incomplete_tail_partial_precedes_completed_extension(
                m_candidates);

            min_prefix_units_local := prefix_head_units;
            if min_prefix_units_local <= 0 then
            begin
                if input_syllable_count >= 12 then
                begin
                    min_prefix_units_local := input_syllable_count - 2;
                end
                else if input_syllable_count >= 9 then
                begin
                    min_prefix_units_local := input_syllable_count - 3;
                end
                else
                begin
                    min_prefix_units_local := 3;
                end;
            end;
            if min_prefix_units_local < 3 then
            begin
                min_prefix_units_local := 3;
            end;

            if prepare_prefix_partial_fast_exit_local(m_candidates,
                min_prefix_units_local) and
                top_candidate_is_stable_non_modal_partial_local(m_candidates) then
            begin
                top_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[0].text));
                if top_units_local >= min_prefix_units_local then
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d stableprefix=%s typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(delayed_long_decode_mode), stage_tag,
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted or long_full_exact_query),
                        Ord(all_initial_compact_query)]));
                    Exit(True);
                end;
            end;
        end;

        if (not has_short_extendable_tail_prefix_local) and
            (not has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            has_trusted_complete_top_candidate_local(m_candidates) then
        begin
            finalize_stable_consensus_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d stablefast=%s typofast=%d fqexact=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(lightweight_sentence_applied),
                Ord(delayed_long_decode_mode), stage_tag,
                Ord(normalized_typo_long_query_fast_path),
                Ord(full_query_exact_promoted or long_full_exact_query),
                Ord(all_initial_compact_query)]));
            Exit(True);
        end;

        if (not has_short_extendable_tail_prefix_local) and
            (not has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            top_complete_candidate_has_duplicate_support_local(m_candidates) then
        begin
            finalize_stable_consensus_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d stabledup=%s typofast=%d fqexact=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(lightweight_sentence_applied),
                Ord(delayed_long_decode_mode), stage_tag,
                Ord(normalized_typo_long_query_fast_path),
                Ord(full_query_exact_promoted or long_full_exact_query),
                Ord(all_initial_compact_query)]));
            Exit(True);
        end;
    end;

    function try_finalize_locked_long_postprocess_candidate_local(
        const stage_tag: string): Boolean;
    begin
        Result := False;
        if (Length(m_candidates) = 0) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 8) or
            has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count) then
        begin
            Exit;
        end;

        if has_strong_top_complete_candidate_for_postprocess_lock_local(
            m_candidates) or
            is_trusted_top_complete_candidate_for_early_exit(m_candidates) or
            top_complete_candidate_has_duplicate_support_local(m_candidates) then
        begin
            finalize_stable_consensus_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d stablelock=%s typofast=%d fqexact=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(lightweight_sentence_applied),
                Ord(delayed_long_decode_mode), stage_tag,
                Ord(normalized_typo_long_query_fast_path),
                Ord(full_query_exact_promoted or long_full_exact_query),
                Ord(all_initial_compact_query)]));
            Exit(True);
        end;
    end;

    function try_finalize_budgeted_long_postprocess_candidate_local(
        const stage_tag: string): Boolean;
    const
        c_long_postprocess_soft_budget_ms = 1200;
    var
        total_elapsed_ms_local: UInt64;
    begin
        Result := False;
        if (Length(m_candidates) = 0) or
            (not delayed_long_decode_mode) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 12) then
        begin
            Exit;
        end;

        total_elapsed_ms_local := nc_monotonic_tick_ms - total_start_tick;
        if not search_budget_should_stop(total_start_tick, 0, 0,
            c_long_postprocess_soft_budget_ms,
            c_long_postprocess_emergency_budget_ms) then
        begin
            Exit;
        end;

        if has_complete_top_candidate_for_fast_exit_local(m_candidates) or
            is_trusted_top_complete_candidate_for_early_exit(m_candidates) or
            has_exact_complete_phrase_candidate_for_syllable_count(
            m_candidates, input_syllable_count) or
            has_natural_complete_segment_path_candidate_for_syllable_count(
            m_candidates, input_syllable_count) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d budgetexit=%s elapsed=%d typofast=%d fqexact=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(lightweight_sentence_applied),
                Ord(delayed_long_decode_mode), stage_tag,
                Integer(total_elapsed_ms_local),
                Ord(normalized_typo_long_query_fast_path),
                Ord(full_query_exact_promoted or long_full_exact_query),
                Ord(all_initial_compact_query)]));
            Exit(True);
        end;
    end;

    procedure promote_best_visible_supported_complete_candidate_local(
        var candidates: TncCandidateList);
    const
        c_probe_limit = 8;
        c_promote_margin = 1024;
        c_support_weight = 12;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        top_score_local: Integer;
        best_score_local: Integer;
        candidate_score_local: Integer;
        picked_local: TncCandidate;
        exact_match_count_local: Integer;
        exact_mismatch_count_local: Integer;
        exact_strong_match_count_local: Integer;
        exact_strong_mismatch_count_local: Integer;

        function get_complete_candidate_score_local(
            const candidate_local: TncCandidate): Integer;
        var
            candidate_text_local: string;
            support_score_local: Integer;
            support_segments_local: Integer;
            support_path_local: string;
            match_count_local: Integer;
            mismatch_count_local: Integer;
            strong_match_count_local: Integer;
            strong_mismatch_count_local: Integer;
        begin
            Result := Low(Integer);
            if Trim(candidate_local.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate_local.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) or
                is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate_local) then
            begin
                Exit;
            end;

            Result := get_rank_score(candidate_local);
            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text_local, support_score_local,
                support_segments_local);
            if support_path_local <> '' then
            begin
                Inc(Result, support_score_local * c_support_weight);
                Inc(Result, Max(0, 4096 - (support_segments_local * 384)));
            end
            else
            begin
                Dec(Result, 4096);
            end;

            get_exact_window_consistency_stats_local(candidate_text_local, 2, 5,
                match_count_local, mismatch_count_local,
                strong_match_count_local, strong_mismatch_count_local);
            Inc(Result, match_count_local * 1024);
            Inc(Result, strong_match_count_local * 8192);
            Dec(Result, mismatch_count_local * 2048);
            Dec(Result, strong_mismatch_count_local * 16384);
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) then
        begin
            Exit;
        end;

        top_score_local := get_complete_candidate_score_local(candidates[0]);
        best_score_local := top_score_local;
        best_idx_local := 0;
        for idx_local := 1 to Min(c_probe_limit - 1, High(candidates)) do
        begin
            candidate_score_local :=
                get_complete_candidate_score_local(candidates[idx_local]);
            if candidate_score_local > best_score_local + c_promote_margin then
            begin
                best_score_local := candidate_score_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        picked_local := candidates[best_idx_local];
        for idx_local := best_idx_local downto 1 do
        begin
            candidates[idx_local] := candidates[idx_local - 1];
        end;
        candidates[0] := picked_local;

        if m_config.debug_mode then
        begin
            get_exact_window_consistency_stats_local(Trim(candidates[0].text), 2, 5,
                exact_match_count_local, exact_mismatch_count_local,
                exact_strong_match_count_local,
                exact_strong_mismatch_count_local);
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vpromote=[text=%s score=%d top=%d win=%d/%d]',
                [Trim(candidates[0].text), best_score_local, top_score_local,
                exact_match_count_local, exact_strong_match_count_local]);
        end;
    end;

    procedure prepare_visible_complete_candidates_for_fast_exit_local(
        var candidates: TncCandidateList);
    const
        c_resolved_sentence_max_syllables = 24;
    var
        local_phase_start_tick: UInt64;
    begin
        if m_composition_built_incrementally and delayed_long_decode_mode and
            (input_syllable_count > 10) and
            search_budget_should_stop(total_start_tick, 0, 0, 1200,
            c_long_postprocess_emergency_budget_ms) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tpvrs=[skip-budget]';
            end;
            Exit;
        end;

        if (input_syllable_count > c_resolved_sentence_max_syllables) and
            (Length(candidates) > 0) and
            (Trim(candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(candidates[0].text)) =
            input_syllable_count) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tpvrs=[skip-long-complete-postprocess]';
            end;
            Exit;
        end;

        local_phase_start_tick := nc_monotonic_tick_ms;
        if (input_syllable_count <= c_resolved_sentence_max_syllables) or
            (Length(candidates) = 0) or
            (Trim(candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
            input_syllable_count) then
        begin
            ensure_best_resolved_sentence_candidate_visible(candidates);
        end
        else if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                ' tpvrs=[skip-long-complete]';
        end;
        note_debug_helper_elapsed('tpvrs', local_phase_start_tick);
        if input_syllable_count > 10 then
        begin
            if (not m_composition_built_incrementally) or
                (not search_budget_should_stop(total_start_tick, 0, 0,
                700, c_long_postprocess_emergency_budget_ms)) then
            begin
                local_phase_start_tick := nc_monotonic_tick_ms;
                promote_best_visible_supported_complete_candidate_local(candidates);
                note_debug_helper_elapsed('tppromlg', local_phase_start_tick);
            end
            else if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tppromlg=[skip-budget]';
            end;
            Exit;
        end;
        local_phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_visible_complete_sentence_candidate_visible(candidates);
        note_debug_helper_elapsed('tpvfull', local_phase_start_tick);
        local_phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_supported_complete_candidate_visible(candidates);
        note_debug_helper_elapsed('tpvsup', local_phase_start_tick);
        local_phase_start_tick := nc_monotonic_tick_ms;
        promote_best_visible_supported_complete_candidate_local(candidates);
        note_debug_helper_elapsed('tpprom', local_phase_start_tick);
        local_phase_start_tick := nc_monotonic_tick_ms;
        ensure_strong_supported_complete_candidate_locked_visible(candidates);
        note_debug_helper_elapsed('tplock', local_phase_start_tick);
    end;

    function has_extendable_trailing_prefix_query_text(const pinyin_key: string): Boolean;
    var
        effective_syllables: TncPinyinParseResult;
        local_syllable_count: Integer;
        last_syllable_text: string;
        suffix_ch: Char;
        extended_key: string;
    begin
        Result := False;
        if pinyin_key = '' then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(pinyin_key);
        local_syllable_count := Length(effective_syllables);
        if local_syllable_count < 4 then
        begin
            Exit;
        end;

        last_syllable_text := Trim(effective_syllables[High(effective_syllables)].text);
        if last_syllable_text = '' then
        begin
            Exit;
        end;

        for suffix_ch := 'a' to 'z' do
        begin
            extended_key := pinyin_key + suffix_ch;
            if get_effective_compact_pinyin_unit_count(extended_key) = local_syllable_count then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure filter_self_composed_complete_candidates_when_normal_complete_exists;
    const
        c_weak_complete_weight_for_prefix_tail = 300;
        c_low_evidence_user_complete_weight = 8;
        c_low_weight_exact_allows_particle_split = 1600;
    var
        expected_units: Integer;
        idx: Integer;
        out_idx: Integer;
        has_normal_complete: Boolean;
        has_exact_full_complete: Boolean;
        has_one_plus_two_function_head_complete: Boolean;
        strongest_normal_complete_weight: Integer;
        strongest_exact_full_complete_weight: Integer;
        candidate_text: string;
        removed_count: Integer;
        normal_complete_texts: TDictionary<string, Byte>;
        exact_full_complete_texts: TDictionary<string, Byte>;
        exact_results_local: TncCandidateList;

        function segment_path_has_multi_char_segment_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            part_idx: Integer;
            inferred_score: Integer;
            inferred_segment_count: Integer;
        begin
            Result := False;
            encoded_path := get_segment_path_for_candidate(candidate);
            if encoded_path = '' then
            begin
                encoded_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(candidate.text), inferred_score,
                    inferred_segment_count);
            end;
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(segment_parts) do
            begin
                if get_candidate_text_unit_count(Trim(segment_parts[part_idx])) >= 2 then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_function_single_segment_text_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $4ED6, $5979,
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168, $8D85,
                $771F, $597D, $5982, $633A, $8F83, $6781, $800C, $5E76,
                $5374, $4E14, $5427:
                    Result := True;
            end;
        end;

        function is_one_plus_two_modifier_head_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
                $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F, $7684, $4E86,
                $7B2C, $5148:
                    Result := True;
            end;
        end;

        function is_one_plus_two_pronoun_head_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $60A8:
                    Result := True;
            end;
        end;

        function is_allowed_one_plus_two_head_segment_text_local(
            const value: string): Boolean;
        begin
            Result := is_one_plus_two_modifier_head_segment_text_local(value) or
                is_one_plus_two_pronoun_head_segment_text_local(value);
        end;

        function is_locative_tail_single_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E0A, $4E0B, $4E2D, $91CC, $5185, $5916, $524D, $540E,
                $65F6, $5904, $8FB9, $95F4, $65C1, $9762:
                    Result := True;
            end;
        end;

        function is_sentence_particle_single_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E86, $7740, $8FC7, $5417, $561B, $5427, $5462,
                $554A, $5440, $5566, $7684, $5730, $5F97:
                    Result := True;
            end;
        end;

        function is_number_single_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                $4E03, $516B, $4E5D, $5341:
                    Result := True;
            end;
        end;

        function is_ordinal_head_segment_text_local(
            const value: string): Boolean;
        var
            units_local: TArray<string>;
        begin
            units_local := split_text_units(Trim(value));
            if Length(units_local) <= 0 then
            begin
                Exit(False);
            end;
            Result := (Length(units_local) >= 2) and
                SameText(units_local[0], string(Char($7B2C)));
        end;

        function is_quantity_tail_single_segment_text_local(
            const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $5B57, $9879, $6761, $4E2A, $70B9, $79CD, $7C7B,
                $6B21, $6B65, $8F6E, $7AE0, $8282, $9875, $6BB5,
                $884C, $5217, $5C42:
                    Result := True;
            end;
        end;

        function segment_path_has_disallowed_single_segment_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            inferred_score: Integer;
            inferred_segment_count: Integer;

            function has_quantity_neighbor_local(
                const idx_local: Integer): Boolean;
            var
                previous_text_local: string;
                next_text_local: string;

                function is_number_quantity_phrase_text_local(
                    const text_value_local: string): Boolean;
                var
                    units_local: TArray<string>;
                begin
                    units_local := split_text_units(Trim(text_value_local));
                    if Length(units_local) <> 2 then
                    begin
                        Exit(False);
                    end;
                    Result := is_number_single_segment_text_local(units_local[0]) and
                        is_quantity_tail_single_segment_text_local(units_local[1]);
                end;
            begin
                Result := False;
                if (idx_local < 0) or (idx_local > High(segment_parts)) then
                begin
                    Exit;
                end;

                if (idx_local < High(segment_parts)) and
                    is_number_single_segment_text_local(
                    Trim(segment_parts[idx_local])) then
                begin
                    next_text_local := Trim(segment_parts[idx_local + 1]);
                    if is_quantity_tail_single_segment_text_local(next_text_local) then
                    begin
                        Exit(True);
                    end;
                end;

                if (idx_local = High(segment_parts)) and
                    is_quantity_tail_single_segment_text_local(
                    Trim(segment_parts[idx_local])) and (idx_local > 0) then
                begin
                    previous_text_local := Trim(segment_parts[idx_local - 1]);
                    Exit(is_number_single_segment_text_local(previous_text_local) or
                        is_ordinal_head_segment_text_local(previous_text_local));
                end;

                if (idx_local = 0) and
                    SameText(Trim(segment_parts[idx_local]),
                    string(Char($7B2C))) and
                    (idx_local < High(segment_parts)) then
                begin
                    next_text_local := Trim(segment_parts[idx_local + 1]);
                    Exit(is_number_quantity_phrase_text_local(next_text_local));
                end;
            end;
        begin
            Result := False;
            if expected_units > 4 then
            begin
                Exit;
            end;

            encoded_path := get_segment_path_for_candidate(candidate);
            if encoded_path = '' then
            begin
                encoded_path := infer_segment_path_for_query_text_with_score(
                    lookup_text, Trim(candidate.text), inferred_score,
                    inferred_segment_count);
            end;
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            for part_idx := 0 to High(segment_parts) do
            begin
                segment_text := Trim(segment_parts[part_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if segment_units <> 1 then
                begin
                    Continue;
                end;

                if has_quantity_neighbor_local(part_idx) then
                begin
                    Continue;
                end;

                if is_function_single_segment_text_local(segment_text) then
                begin
                    Continue;
                end;

                if (part_idx = High(segment_parts)) and
                    is_locative_tail_single_segment_text_local(segment_text) then
                begin
                    Continue;
                end;

                Exit(True);
            end;
        end;

        function candidate_has_exact_base_entry_local(
            const candidate: TncCandidate): Boolean;
        var
            candidate_text_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if m_dictionary = nil then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate.text);
            if candidate_text_local = '' then
            begin
                Exit;
            end;

            Result := m_dictionary.is_base_entry(lookup_text,
                candidate_text_local);
            if Result then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(lookup_text,
                exact_results_local) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    candidate_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_lexicon_longer_prefix_support_local(
            const candidate: TncCandidate): Boolean;
        var
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            candidate_text_local: string;
            prefix_text_local: string;
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (lookup_text = '') or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <> expected_units) then
            begin
                Exit;
            end;

            if not dictionary_prefix_lookup_cached(lookup_text,
                prefix_results_local) then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                text_units_local := split_text_units(
                    Trim(prefix_results_local[prefix_idx_local].text));
                if Length(text_units_local) <= expected_units then
                begin
                    Continue;
                end;

                prefix_text_local := '';
                for unit_idx_local := 0 to expected_units - 1 do
                begin
                    prefix_text_local := prefix_text_local +
                        text_units_local[unit_idx_local];
                end;

                if SameText(prefix_text_local, candidate_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_three_syllable_lexicon_split_support_local(
            const candidate: TncCandidate): Boolean;
        const
            c_min_two_char_head_split_weight = 520;
            c_min_two_char_tail_split_weight = 360;
            c_min_pronoun_two_char_tail_split_weight = 500;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            head_text_local: string;
            tail_text_local: string;
            tail_preferred_local: Boolean;

            function build_key_local(const start_idx: Integer;
                const count_local: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (count_local <= 0) or
                    (start_idx + count_local > Length(syllables_local)) then
                begin
                    Exit;
                end;
                for local_idx := start_idx to start_idx + count_local - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[local_idx].text);
                end;
            end;

            function two_char_text_has_base_support_local(
                const key_value: string; const text_value: string;
                const min_weight_value: Integer): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := False;
                if (key_value = '') or (text_value = '') or
                    (get_candidate_text_unit_count(text_value) <> 2) or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                if m_dictionary.is_base_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;

                if not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= 24 then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_local := Trim(
                        exact_results_local[exact_idx_local].text);
                    if not SameText(exact_text_local, text_value) then
                    begin
                        Continue;
                    end;

                    exact_weight_local :=
                        exact_results_local[exact_idx_local].score;
                    if exact_results_local[exact_idx_local].has_dict_weight and
                        (exact_results_local[exact_idx_local].dict_weight >
                        exact_weight_local) then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end;
                    Exit((exact_results_local[exact_idx_local].source = cs_user) or
                        (exact_weight_local >= min_weight_value));
                end;
            end;
        begin
            Result := False;
            if is_short_three_exact_pair_candidate_supported_local(
                Trim(candidate.text)) then
            begin
                Exit(True);
            end;
            if (m_dictionary = nil) or (expected_units <> 3) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> 3 then
            begin
                Exit;
            end;

            head_key_local := build_key_local(0, 2);
            tail_key_local := build_key_local(2, 1);
            head_text_local := units_local[0] + units_local[1];
            tail_text_local := units_local[2];
            if two_char_text_has_base_support_local(head_key_local,
                head_text_local, c_min_two_char_head_split_weight) and
                match_single_char_candidate_for_syllable(tail_key_local,
                tail_text_local, tail_preferred_local) and
                (tail_preferred_local or
                is_function_single_segment_text_local(tail_text_local) or
                is_locative_tail_single_segment_text_local(tail_text_local)) then
            begin
                Exit(True);
            end;









        end;

        function candidate_has_four_syllable_two_phrase_split_support_local(
            const candidate: TncCandidate): Boolean;
        const
            c_min_two_char_split_weight = 360;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_key_local: string;
            middle_key_local: string;
            particle_key_local: string;
            head_text_local: string;
            middle_text_local: string;
            particle_text_local: string;
            single_preferred_local: Boolean;

            function build_key_local(const start_idx: Integer;
                const count_local: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (count_local <= 0) or
                    (start_idx + count_local > Length(syllables_local)) then
                begin
                    Exit;
                end;
                for local_idx := start_idx to start_idx + count_local - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[local_idx].text);
                end;
            end;

            function two_char_text_has_base_support_local(
                const key_value: string; const text_value: string): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := False;
                if (key_value = '') or (text_value = '') or
                    (get_candidate_text_unit_count(text_value) <> 2) or
                    (m_dictionary = nil) then
                begin
                    Exit;
                end;

                if m_dictionary.is_base_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;

                if not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= 32 then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_local := Trim(
                        exact_results_local[exact_idx_local].text);
                    if not SameText(exact_text_local, text_value) then
                    begin
                        Continue;
                    end;

                    exact_weight_local :=
                        exact_results_local[exact_idx_local].score;
                    if exact_results_local[exact_idx_local].has_dict_weight and
                        (exact_results_local[exact_idx_local].dict_weight >
                        exact_weight_local) then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end;
                    Exit((exact_results_local[exact_idx_local].source = cs_user) or
                        (exact_weight_local >= c_min_two_char_split_weight));
                end;
            end;

        begin
            Result := False;
            if (m_dictionary = nil) or (expected_units < 4) or
                (expected_units > 5) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> 4 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> 4 then
            begin
                Exit;
            end;

            // Do not exempt arbitrary AB+CD runtime phrases in short mode.
            // Complete four-syllable candidates must be full exact entries,
            // user entries, or one of the explicit fixed-boundary patterns.
            head_key_local := build_key_local(0, 1);
            middle_key_local := build_key_local(1, 2);
            particle_key_local := build_key_local(3, 1);
            head_text_local := units_local[0];
            middle_text_local := units_local[1] + units_local[2];
            particle_text_local := units_local[3];

            Result :=
                (is_one_plus_two_modifier_head_segment_text_local(
                head_text_local) or is_one_plus_two_pronoun_head_segment_text_local(
                head_text_local)) and
                match_single_char_candidate_for_syllable(head_key_local,
                head_text_local, single_preferred_local) and
                two_char_text_has_base_support_local(middle_key_local,
                middle_text_local) and
                match_single_char_candidate_for_syllable(particle_key_local,
                particle_text_local, single_preferred_local) and
                is_sentence_particle_single_segment_text_local(
                particle_text_local);
        end;

        function get_candidate_segment_path_for_filter_local(
            const candidate: TncCandidate): string;
        var
            inferred_score_local: Integer;
            inferred_segment_count_local: Integer;
        begin
            Result := get_segment_path_for_candidate(candidate);
            if Result <> '' then
            begin
                Exit;
            end;

            Result := infer_segment_path_for_query_text_with_score(
                lookup_text, Trim(candidate.text), inferred_score_local,
                inferred_segment_count_local);
        end;

        function candidate_has_one_plus_two_function_head_split_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
        begin
            Result := False;
            if (expected_units <> 3) or (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if encoded_path_local = '' then
            begin
                Exit;
            end;
            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            if Length(segment_parts_local) <> 2 then
            begin
                Exit;
            end;

            Result :=
                (get_candidate_text_unit_count(Trim(segment_parts_local[0])) = 1) and
                is_allowed_one_plus_two_head_segment_text_local(
                Trim(segment_parts_local[0])) and
                (get_candidate_text_unit_count(Trim(segment_parts_local[1])) = 2);
        end;

        function candidate_has_one_plus_two_split_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
        begin
            Result := False;
            if (expected_units <> 3) or (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if encoded_path_local = '' then
            begin
                Exit;
            end;
            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            if Length(segment_parts_local) <> 2 then
            begin
                Exit;
            end;

            Result :=
                (get_candidate_text_unit_count(Trim(segment_parts_local[0])) = 1) and
                (get_candidate_text_unit_count(Trim(segment_parts_local[1])) = 2);
        end;

        function candidate_has_one_plus_two_text_split_local(
            const candidate: TncCandidate): Boolean;
        var
            syllables_local: TncPinyinParseResult;
            units_local: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
            ignored_preferred_local: Boolean;
        begin
            Result := False;
            if (m_dictionary = nil) or (expected_units <> 3) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> 3 then
            begin
                Exit;
            end;

            head_key_local := normalize_pinyin_text(syllables_local[0].text);
            tail_key_local := normalize_pinyin_text(syllables_local[1].text) +
                normalize_pinyin_text(syllables_local[2].text);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            if (head_key_local = '') or (tail_key_local = '') or
                (tail_text_local = '') or
                (not match_single_char_candidate_for_syllable(head_key_local,
                Trim(units_local[0]), ignored_preferred_local)) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(tail_key_local, tail_text_local) then
            begin
                Exit(True);
            end;

            if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                tail_results_local) then
            begin
                Exit;
            end;

            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if (Trim(tail_results_local[tail_idx_local].comment) = '') and
                    SameText(Trim(tail_results_local[tail_idx_local].text),
                    tail_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_fixed_pronoun_tail_split_support_local(
            const candidate: TncCandidate): Boolean;
        var
            units_local: TArray<string>;
            head_pinyin_local: string;
            head_text_local: string;
            tail_key_local: string;
            tail_units_local: Integer;
            tail_text_local: string;
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (Trim(candidate.comment) <> '') or
                (not try_get_fixed_pronoun_tail_query_parts(lookup_text,
                expected_units, head_pinyin_local, head_text_local,
                tail_key_local, tail_units_local)) then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;
            if (Length(units_local) < 3) or
                (not SameText(Trim(units_local[0]), head_text_local)) then
            begin
                Exit;
            end;

            tail_text_local := '';
            for tail_idx_local := 1 to High(units_local) do
            begin
                tail_text_local := tail_text_local + Trim(units_local[tail_idx_local]);
            end;
            if (tail_text_local = '') or
                (get_candidate_text_unit_count(tail_text_local) <>
                tail_units_local) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(tail_key_local, tail_text_local) then
            begin
                Exit(True);
            end;

            if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                tail_results_local) then
            begin
                Exit;
            end;
            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if (Trim(tail_results_local[tail_idx_local].comment) = '') and
                    SameText(Trim(tail_results_local[tail_idx_local].text),
                    tail_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_pronoun_connector_tail_split_support_local(
            const candidate: TncCandidate): Boolean;
        var
            units_local: TArray<string>;
            syllables_local: TncPinyinParseResult;
            exact_tail_results_local: TncCandidateList;
            exact_tail_idx_local: Integer;
            tail_unit_idx_local: Integer;
            pronoun_text_local: string;
            connector_text_local: string;
            tail_text_local: string;
            tail_key_local: string;
            ignored_preferred_local: Boolean;

            function is_pronoun_local(const value: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(value) <> 1 then
                begin
                    Exit;
                end;
                case Ord(value[1]) of
                    $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1, $4FFA:
                        Result := True;
                end;
            end;

            function is_connector_local(const value: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(value) <> 1 then
                begin
                    Exit;
                end;
                case Ord(value[1]) of
                    $8DDF, $548C, $4E0E, $540C:
                        Result := True;
                end;
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (expected_units < 4) or
                (expected_units > 6) or
                (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            // Validate the fixed boundary directly from the query and text.
            // The candidate path cache may legitimately retain another inferred
            // segmentation, but that must not hide a fully lexicon-backed tail.
            units_local := split_text_units(Trim(candidate.text));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;

            pronoun_text_local := Trim(units_local[0]);
            connector_text_local := Trim(units_local[1]);
            tail_text_local := '';
            for tail_unit_idx_local := 2 to High(units_local) do
            begin
                tail_text_local := tail_text_local + Trim(
                    units_local[tail_unit_idx_local]);
            end;
            if (not is_pronoun_local(pronoun_text_local)) or
                (not is_connector_local(connector_text_local)) or
                (get_candidate_text_unit_count(tail_text_local) <>
                expected_units - 2) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if (Length(syllables_local) <> expected_units) or
                (not match_single_char_candidate_for_syllable(
                normalize_pinyin_text(syllables_local[0].text),
                pronoun_text_local, ignored_preferred_local)) or
                (not match_single_char_candidate_for_syllable(
                normalize_pinyin_text(syllables_local[1].text),
                connector_text_local, ignored_preferred_local)) then
            begin
                Exit;
            end;

            tail_key_local := '';
            for tail_unit_idx_local := 2 to High(syllables_local) do
            begin
                tail_key_local := tail_key_local + normalize_pinyin_text(
                    syllables_local[tail_unit_idx_local].text);
            end;
            if m_dictionary.is_base_entry(tail_key_local, tail_text_local) or
                m_dictionary.is_user_entry(tail_key_local, tail_text_local) then
            begin
                Exit(True);
            end;
            if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                exact_tail_results_local) then
            begin
                Exit;
            end;
            for exact_tail_idx_local := 0 to High(exact_tail_results_local) do
            begin
                if (Trim(exact_tail_results_local[exact_tail_idx_local].comment) = '') and
                    SameText(Trim(exact_tail_results_local[
                    exact_tail_idx_local].text), tail_text_local) and
                    (get_candidate_text_unit_count(tail_text_local) =
                    expected_units - 2) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_two_plus_one_non_particle_tail_split_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            tail_text_local: string;
        begin
            Result := False;
            if (expected_units <> 3) or (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if encoded_path_local = '' then
            begin
                Exit;
            end;
            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            if Length(segment_parts_local) <> 2 then
            begin
                Exit;
            end;

            tail_text_local := Trim(segment_parts_local[1]);
            Result :=
                (get_candidate_text_unit_count(Trim(segment_parts_local[0])) = 2) and
                (get_candidate_text_unit_count(tail_text_local) = 1) and
                (not is_sentence_particle_single_segment_text_local(
                tail_text_local));
        end;

        function candidate_has_ordinal_quantity_two_plus_one_split_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            if (expected_units <> 3) or (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if encoded_path_local = '' then
            begin
                Exit;
            end;
            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            if Length(segment_parts_local) <> 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(segment_parts_local[0]);
            tail_text_local := Trim(segment_parts_local[1]);
            Result := is_ordinal_head_segment_text_local(head_text_local) and
                is_quantity_tail_single_segment_text_local(tail_text_local);
        end;

        function candidate_has_sentence_particle_tail_split_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            tail_text_local: string;
        begin
            Result := False;
            if Trim(candidate.comment) <> '' then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if encoded_path_local = '' then
            begin
                Exit;
            end;
            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            if Length(segment_parts_local) < 2 then
            begin
                Exit;
            end;

            tail_text_local := Trim(segment_parts_local[High(segment_parts_local)]);
            Result := (get_candidate_text_unit_count(tail_text_local) = 1) and
                is_sentence_particle_single_segment_text_local(tail_text_local);
        end;

        function is_complete_for_query_local(const candidate: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate.text)) = expected_units);
        end;

        function segment_path_has_nonbase_two_plus_single_segment_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path_local: string;
            segment_parts_local: TArray<string>;
            syllables_local: TncPinyinParseResult;
            part_idx_local: Integer;
            syllable_pos_local: Integer;
            segment_units_local: Integer;
            segment_text_local: string;
            segment_key_local: string;
            head_key_local: string;
            tail_key_local: string;
            head_text_local: string;
            tail_text_local: string;
            segment_unit_texts_local: TArray<string>;

            function build_span_key_local(const start_idx_local: Integer;
                const count_local: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (start_idx_local < 0) or (count_local <= 0) or
                    (start_idx_local + count_local > Length(syllables_local)) then
                begin
                    Exit;
                end;

                for local_idx := start_idx_local to
                    start_idx_local + count_local - 1 do
                begin
                    Result := Result + normalize_pinyin_text(
                        syllables_local[local_idx].text);
                end;
            end;

            function exact_lookup_has_text_local(const key_value: string;
                const text_value: string; const expected_units_value: Integer;
                const require_reliable: Boolean): Boolean;
            var
                exact_results_value: TncCandidateList;
                exact_idx_value: Integer;
                exact_text_value: string;
            begin
                Result := False;
                if (key_value = '') or (text_value = '') or
                    (expected_units_value <= 0) or
                    (not lookup_exact_full_pinyin_cached_local(key_value,
                    exact_results_value)) then
                begin
                    Exit;
                end;

                for exact_idx_value := 0 to High(exact_results_value) do
                begin
                    if Trim(exact_results_value[exact_idx_value].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_value := Trim(
                        exact_results_value[exact_idx_value].text);
                    if SameText(exact_text_value, text_value) and
                        (get_candidate_text_unit_count(exact_text_value) =
                        expected_units_value) then
                    begin
                        if require_reliable and
                            (exact_results_value[exact_idx_value].source <> cs_user) and
                            ((not exact_results_value[exact_idx_value].has_dict_weight) or
                            (exact_results_value[exact_idx_value].dict_weight <= 0)) then
                        begin
                            Continue;
                        end;
                        Exit(True);
                    end;
                end;
            end;

            function is_allowed_single_tail_text_local(
                const text_value: string): Boolean;
            begin
                Result := is_sentence_particle_single_segment_text_local(
                    text_value) or is_function_single_segment_text_local(
                    text_value);
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (not is_complete_for_query_local(candidate)) then
            begin
                Exit;
            end;

            encoded_path_local := get_candidate_segment_path_for_filter_local(
                candidate);
            if get_encoded_path_segment_count_local(encoded_path_local) <= 1 then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(syllables_local) <> expected_units then
            begin
                Exit;
            end;

            segment_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            syllable_pos_local := 0;
            for part_idx_local := 0 to High(segment_parts_local) do
            begin
                segment_text_local := Trim(segment_parts_local[part_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if segment_units_local <= 0 then
                begin
                    Continue;
                end;
                if syllable_pos_local + segment_units_local >
                    Length(syllables_local) then
                begin
                    Exit;
                end;

                if segment_units_local = 3 then
                begin
                    segment_key_local := build_span_key_local(
                        syllable_pos_local, 3);
                    if (segment_key_local <> '') and
                        (not m_dictionary.is_user_entry(segment_key_local,
                        segment_text_local)) and
                        (not exact_lookup_has_text_local(segment_key_local,
                        segment_text_local, 3, True)) then
                    begin
                        segment_unit_texts_local := split_text_units(
                            segment_text_local);
                        if Length(segment_unit_texts_local) = 3 then
                        begin
                            head_key_local := build_span_key_local(
                                syllable_pos_local, 2);
                            tail_key_local := build_span_key_local(
                                syllable_pos_local + 2, 1);
                            head_text_local := segment_unit_texts_local[0] +
                                segment_unit_texts_local[1];
                            tail_text_local := segment_unit_texts_local[2];
                            if (not is_allowed_single_tail_text_local(
                                tail_text_local)) and
                                exact_lookup_has_text_local(head_key_local,
                                head_text_local, 2, False) and
                                (m_dictionary.single_char_matches_pinyin(
                                tail_key_local, tail_text_local) or
                                exact_lookup_has_text_local(tail_key_local,
                                tail_text_local, 1, False)) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                Inc(syllable_pos_local, segment_units_local);
            end;
        end;

        function candidate_has_supported_full_entry_local(
            const candidate: TncCandidate): Boolean;
        const
            c_min_supported_exact_dict_weight = 160;
        var
            candidate_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (lookup_text = '') or
                (not is_complete_for_query_local(candidate)) then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate.text);
            Result := m_dictionary.is_base_entry(lookup_text,
                candidate_text_local) or
                ((candidate.source = cs_rule) and candidate.has_dict_weight and
                (candidate.dict_weight >= c_min_supported_exact_dict_weight) and
                candidate_has_exact_base_entry_local(candidate)) or
                ((candidate.source = cs_user) and
                m_dictionary.is_user_entry(lookup_text, candidate_text_local) and
                (not m_dictionary.should_suppress_exact_query_learning(
                    lookup_text, candidate_text_local)));
        end;

        function is_normal_complete_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := is_complete_for_query_local(candidate) and
                (candidate_has_supported_full_entry_local(candidate) or
                candidate_has_lexicon_longer_prefix_support_local(candidate) or
                candidate_has_three_syllable_lexicon_split_support_local(
                candidate) or
                candidate_has_fixed_pronoun_tail_split_support_local(candidate) or
                candidate_has_pronoun_connector_tail_split_support_local(candidate) or
                candidate_has_four_syllable_two_phrase_split_support_local(
                    candidate) or
                (candidate.has_dict_weight and
                segment_path_has_multi_char_segment_local(candidate) and
                (not segment_path_has_disallowed_single_segment_local(
                candidate)))) and
                (not is_problematic_single_char_chain_candidate_for_query(
                lookup_text, candidate));
        end;

        function is_polluted_complete_candidate_local(
            const candidate: TncCandidate): Boolean;
        var
            text_key_local: string;
        begin
            text_key_local := Trim(candidate.text);
            if (expected_units = 3) and is_complete_for_query_local(candidate) and
                (not candidate_has_supported_full_entry_local(candidate)) and
                candidate_has_two_plus_one_non_particle_tail_split_local(
                candidate) and
                (not is_short_three_exact_pair_candidate_supported_local(
                candidate.text)) and
                (not candidate_has_ordinal_quantity_two_plus_one_split_local(
                candidate)) then
            begin
                Exit(True);
            end;

            if is_complete_for_query_local(candidate) and
                candidate_has_fixed_pronoun_tail_split_support_local(candidate) then
            begin
                Exit(False);
            end;

            if is_complete_for_query_local(candidate) and
                candidate_has_pronoun_connector_tail_split_support_local(candidate) then
            begin
                Exit(False);
            end;

            if is_complete_for_query_local(candidate) and
                (not candidate_has_supported_full_entry_local(candidate)) and
                (candidate_has_one_plus_two_split_local(candidate) or
                candidate_has_one_plus_two_text_split_local(candidate)) and
                (not is_short_three_exact_pair_candidate_supported_local(
                candidate.text)) then
            begin
                Exit(True);
            end;

            if is_complete_for_query_local(candidate) and
                (not candidate_has_supported_full_entry_local(candidate)) and
                has_exact_full_complete and
                ((exact_full_complete_texts = nil) or
                (not exact_full_complete_texts.ContainsKey(text_key_local))) and
                (not candidate_has_three_syllable_lexicon_split_support_local(
                candidate)) and
                (not candidate_has_four_syllable_two_phrase_split_support_local(
                candidate)) and
                (not (candidate_has_sentence_particle_tail_split_local(candidate) and
                (strongest_exact_full_complete_weight <
                c_low_weight_exact_allows_particle_split))) then
            begin
                Exit(True);
            end;

            if is_complete_for_query_local(candidate) and
                (candidate.source = cs_rule) and candidate.has_dict_weight and
                segment_path_has_multi_char_segment_local(candidate) and
                (not segment_path_has_disallowed_single_segment_local(candidate)) then
            begin
                Exit(False);
            end;
            Result := is_complete_for_query_local(candidate) and
                ((normal_complete_texts = nil) or
                (not normal_complete_texts.ContainsKey(text_key_local))) and
                (not candidate_has_three_syllable_lexicon_split_support_local(
                candidate)) and
                (not candidate_has_four_syllable_two_phrase_split_support_local(
                candidate)) and
                ((not candidate_has_supported_full_entry_local(candidate)) and
                segment_path_has_disallowed_single_segment_local(candidate) or
                (((candidate.source = cs_user) and
                (not candidate_has_supported_full_entry_local(candidate)) and
                ((is_problematic_single_char_chain_candidate_for_query(lookup_text,
                candidate)) or
                (candidate.has_dict_weight and
                (candidate.dict_weight <= c_low_evidence_user_complete_weight) and
                (not candidate_has_exact_base_entry_local(candidate))))) or
                ((candidate.source = cs_rule) and
                (not candidate_has_exact_base_entry_local(candidate)) and
                ((not candidate.has_dict_weight) or
                (not segment_path_has_multi_char_segment_local(candidate))))));
        end;

        function has_strong_prefix_single_tail_segment_path_local(
            const candidate: TncCandidate): Boolean;
        var
            encoded_path: string;
            segment_parts: TArray<string>;
            part_idx: Integer;
            tail_text: string;
            has_multi_char_head_segment: Boolean;
        begin
            Result := False;
            if strongest_normal_complete_weight >= c_weak_complete_weight_for_prefix_tail then
            begin
                Exit;
            end;

            encoded_path := get_candidate_segment_path_for_filter_local(
                candidate);
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            if Length(segment_parts) <= 1 then
            begin
                Exit;
            end;

            has_multi_char_head_segment := False;
            for part_idx := 0 to High(segment_parts) - 1 do
            begin
                if get_candidate_text_unit_count(Trim(segment_parts[part_idx])) >= 2 then
                begin
                    has_multi_char_head_segment := True;
                    Break;
                end;
            end;
            if not has_multi_char_head_segment then
            begin
                Exit;
            end;

            tail_text := Trim(segment_parts[High(segment_parts)]);
            if (tail_text = '') or (get_candidate_text_unit_count(tail_text) <> 1) then
            begin
                Exit;
            end;
            Result := True;
        end;
    begin
        normal_complete_texts := nil;
        exact_full_complete_texts := nil;
        if (Length(m_candidates) = 0) or (not has_multi_syllable_input) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

            expected_units := input_syllable_count;
            if (expected_units < 2) or (expected_units > 5) then
            begin
                Exit;
            end;

        normal_complete_texts := TDictionary<string, Byte>.Create;
        exact_full_complete_texts := TDictionary<string, Byte>.Create;
        try
            has_normal_complete := False;
            has_exact_full_complete := False;
            has_one_plus_two_function_head_complete := False;
            strongest_normal_complete_weight := Low(Integer);
            strongest_exact_full_complete_weight := Low(Integer);
            if dictionary_exact_lookup_cached(lookup_text, exact_results_local) then
            begin
                for idx := 0 to High(exact_results_local) do
                begin
                    candidate_text := Trim(exact_results_local[idx].text);
                    if (Trim(exact_results_local[idx].comment) = '') and
                        (candidate_text <> '') and
                        (get_candidate_text_unit_count(candidate_text) =
                        expected_units) and
                        (m_dictionary.is_base_entry(lookup_text,
                        candidate_text) or
                        ((exact_results_local[idx].source = cs_rule) and
                        exact_results_local[idx].has_dict_weight and
                        (exact_results_local[idx].dict_weight >= 160)) or
                        ((exact_results_local[idx].source = cs_user) and
                        m_dictionary.is_user_entry(lookup_text,
                        candidate_text) and
                        (not m_dictionary.should_suppress_exact_query_learning(
                        lookup_text, candidate_text)))) then
                    begin
                        exact_full_complete_texts.AddOrSetValue(
                            candidate_text, 1);
                        has_exact_full_complete := True;
                        if (exact_results_local[idx].source = cs_user) and
                            m_dictionary.is_user_entry(lookup_text,
                            candidate_text) and
                            (not m_dictionary.should_suppress_exact_query_learning(
                            lookup_text, candidate_text)) then
                        begin
                            strongest_exact_full_complete_weight := High(Integer);
                        end
                        else if exact_results_local[idx].has_dict_weight and
                            (exact_results_local[idx].dict_weight >
                            strongest_exact_full_complete_weight) then
                        begin
                            strongest_exact_full_complete_weight :=
                                exact_results_local[idx].dict_weight;
                        end
                        else if (not exact_results_local[idx].has_dict_weight) and
                            (exact_results_local[idx].score >
                            strongest_exact_full_complete_weight) then
                        begin
                            strongest_exact_full_complete_weight :=
                                exact_results_local[idx].score;
                        end;
                    end;
                end;
            end;
            for idx := 0 to High(m_candidates) do
            begin
                if is_complete_for_query_local(m_candidates[idx]) and
                    candidate_has_supported_full_entry_local(m_candidates[idx]) then
                begin
                    exact_full_complete_texts.AddOrSetValue(
                        Trim(m_candidates[idx].text), 1);
                    has_exact_full_complete := True;
                    if (m_candidates[idx].source = cs_user) and
                        m_dictionary.is_user_entry(lookup_text,
                        Trim(m_candidates[idx].text)) and
                        (not m_dictionary.should_suppress_exact_query_learning(
                        lookup_text, Trim(m_candidates[idx].text))) then
                    begin
                        strongest_exact_full_complete_weight := High(Integer);
                    end
                    else if m_candidates[idx].dict_weight >
                        strongest_exact_full_complete_weight then
                    begin
                        strongest_exact_full_complete_weight :=
                            m_candidates[idx].dict_weight;
                    end
                    else if (not m_candidates[idx].has_dict_weight) and
                        (m_candidates[idx].score >
                        strongest_exact_full_complete_weight) then
                    begin
                        strongest_exact_full_complete_weight :=
                            m_candidates[idx].score;
                    end;
                end;

                if is_normal_complete_candidate_local(m_candidates[idx]) then
                begin
                    normal_complete_texts.AddOrSetValue(
                        Trim(m_candidates[idx].text), 1);
                    has_normal_complete := True;
                    if candidate_has_one_plus_two_function_head_split_local(
                        m_candidates[idx]) then
                    begin
                        has_one_plus_two_function_head_complete := True;
                    end;
                    if m_candidates[idx].source = cs_user then
                    begin
                        strongest_normal_complete_weight := High(Integer);
                    end
                    else if m_candidates[idx].dict_weight >
                        strongest_normal_complete_weight then
                    begin
                        strongest_normal_complete_weight := m_candidates[idx].dict_weight;
                    end;
                end;
            end;
            out_idx := 0;
            removed_count := 0;
            for idx := 0 to High(m_candidates) do
            begin
                { Fuzzy exacts and fuzzy lattice paths have already been
                  validated against lexicon edges using alternate syllables. }
                if is_fuzzy_pinyin_active and
                    (m_candidates[idx].fuzzy_cost > 0) and
                    is_complete_for_query_local(m_candidates[idx]) and
                    (((m_candidates[idx].source = cs_user) or
                    ((m_candidates[idx].source = cs_rule) and
                    m_candidates[idx].has_dict_weight)) or
                    segment_path_has_multi_char_segment_local(
                    m_candidates[idx])) then
                begin
                    if out_idx <> idx then
                    begin
                        m_candidates[out_idx] := m_candidates[idx];
                        if out_idx < Length(m_candidate_segment_paths) then
                        begin
                            if idx < Length(m_candidate_segment_paths) then
                            begin
                                m_candidate_segment_paths[out_idx] :=
                                    m_candidate_segment_paths[idx];
                            end
                            else
                            begin
                                m_candidate_segment_paths[out_idx] := '';
                            end;
                        end;
                    end;
                    Inc(out_idx);
                    Continue;
                end;

                if is_polluted_complete_candidate_local(m_candidates[idx]) and
                    ((not has_normal_complete) or
                    (not has_strong_prefix_single_tail_segment_path_local(
                    m_candidates[idx]))) then
                begin
                    Inc(removed_count);
                    Continue;
                end;

                if segment_path_has_nonbase_two_plus_single_segment_local(
                    m_candidates[idx]) and
                    (not is_short_three_exact_pair_candidate_supported_local(
                    m_candidates[idx].text)) then
                begin
                    Inc(removed_count);
                    Continue;
                end;

                if has_one_plus_two_function_head_complete and
                    candidate_has_two_plus_one_non_particle_tail_split_local(
                    m_candidates[idx]) and
                    (not is_short_three_exact_pair_candidate_supported_local(
                    m_candidates[idx].text)) and
                    (not candidate_has_ordinal_quantity_two_plus_one_split_local(
                    m_candidates[idx])) then
                begin
                    Inc(removed_count);
                    Continue;
                end;

                if out_idx <> idx then
                begin
                    m_candidates[out_idx] := m_candidates[idx];
                    if out_idx < Length(m_candidate_segment_paths) then
                    begin
                        if idx < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[out_idx] :=
                                m_candidate_segment_paths[idx];
                        end
                        else
                        begin
                            m_candidate_segment_paths[out_idx] := '';
                        end;
                    end;
                end;
                Inc(out_idx);
            end;

            if out_idx <> Length(m_candidates) then
            begin
                SetLength(m_candidates, out_idx);
                if Length(m_candidate_segment_paths) > out_idx then
                begin
                    SetLength(m_candidate_segment_paths, out_idx);
                end;
            end;

            if m_config.debug_mode and (removed_count > 0) then
            begin
                candidate_text := '';
                if Length(m_candidates) > 0 then
                begin
                    candidate_text := Trim(m_candidates[0].text);
                end;
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' selfdrop=[n=%d top=%s]', [removed_count,
                    candidate_text]);
            end;
        finally
            exact_full_complete_texts.Free;
            normal_complete_texts.Free;
        end;
    end;

    procedure finalize_lookup_timing_info;
    var
        total_elapsed_ms: Int64;
        function should_skip_heavy_finalize_promotions_local: Boolean;
        var
            top_candidate_local: TncCandidate;
        begin
            Result := False;
            if (not has_multi_syllable_input) or
                (input_syllable_count < 7) or
                (Length(m_candidates) = 0) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            top_candidate_local := m_candidates[0];
            if (Trim(top_candidate_local.comment) = '') then
            begin
                Result := top_candidate_local.has_dict_weight and
                    (top_candidate_local.dict_weight >= 50000) and
                    (get_candidate_text_unit_count(Trim(top_candidate_local.text)) =
                    input_syllable_count);
                Exit;
            end;

            Result :=
                (get_candidate_text_unit_count(Trim(top_candidate_local.text)) >=
                input_syllable_count - 1) and
                (Length(normalize_pinyin_text(Trim(top_candidate_local.comment))) <= 3);
        end;

        procedure promote_preferred_three_syllable_partial_shape_local;
        var
            local_idx: Integer;
            best_idx: Integer;
            best_score: Integer;
            picked_local: TncCandidate;
            text_units_local: Integer;
            comment_syllables_local: Integer;
        begin
            if (input_syllable_count <> 3) or
                (m_last_three_syllable_partial_preference_kind <> 1) or
                (Length(m_candidates) <= 1) then
            begin
                Exit;
            end;
            if (Trim(m_candidates[0].comment) = '') and
                (m_candidates[0].has_dict_weight or (m_candidates[0].source = cs_user)) then
            begin
                Exit;
            end;

            best_idx := -1;
            best_score := Low(Integer);
            for local_idx := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[local_idx].comment) = '' then
                begin
                    Continue;
                end;
                text_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[local_idx].text));
                comment_syllables_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(m_candidates[local_idx].comment)));
                if (text_units_local <> 1) or (comment_syllables_local <> 2) then
                begin
                    Continue;
                end;
                if (best_idx < 0) or (m_candidates[local_idx].score > best_score) then
                begin
                    best_idx := local_idx;
                    best_score := m_candidates[local_idx].score;
                end;
            end;
            if best_idx <= 0 then
            begin
                Exit;
            end;

            picked_local := m_candidates[best_idx];
            for local_idx := best_idx downto 1 do
            begin
                m_candidates[local_idx] := m_candidates[local_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        procedure promote_high_coverage_long_partial_local;
        var
            local_idx: Integer;
            best_idx: Integer;
            best_score: Integer;
            top_units: Integer;
            text_units_local: Integer;
            comment_syllables_local: Integer;
            picked_local: TncCandidate;
        begin
            if (input_syllable_count < 5) or (Length(m_candidates) <= 1) then
            begin
                Exit;
            end;
            top_units := get_candidate_text_unit_count(Trim(m_candidates[0].text));
            if (Trim(m_candidates[0].comment) = '') and
                (top_units >= input_syllable_count - 1) then
            begin
                Exit;
            end;

            best_idx := -1;
            best_score := Low(Integer);
            for local_idx := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[local_idx].comment) = '' then
                begin
                    Continue;
                end;
                text_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[local_idx].text));
                comment_syllables_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(m_candidates[local_idx].comment)));
                if (comment_syllables_local <= 0) or
                    (text_units_local + comment_syllables_local <> input_syllable_count) or
                    (text_units_local < input_syllable_count - 2) then
                begin
                    Continue;
                end;
                if (best_idx < 0) or (text_units_local >
                    get_candidate_text_unit_count(Trim(m_candidates[best_idx].text))) or
                    ((text_units_local =
                    get_candidate_text_unit_count(Trim(m_candidates[best_idx].text))) and
                    (m_candidates[local_idx].score > best_score)) then
                begin
                    best_idx := local_idx;
                    best_score := m_candidates[local_idx].score;
                end;
            end;
            if (best_idx <= 0) or
                ((get_candidate_text_unit_count(Trim(m_candidates[best_idx].text)) <=
                top_units + 1) and (m_candidates[best_idx].score <=
                m_candidates[0].score + 512)) then
            begin
                Exit;
            end;

            picked_local := m_candidates[best_idx];
            for local_idx := best_idx downto 1 do
            begin
                m_candidates[local_idx] := m_candidates[local_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        function final_fixed_single_for_pinyin_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if (normalized_syllable = 'le') or (normalized_syllable = 'liao') then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end;
        end;
        function final_fixed_single_for_prefix_local(
            const prefix_text: string): string;
        var
            normalized_prefix: string;
        begin
            Result := '';
            normalized_prefix := normalize_pinyin_text(prefix_text);
            if normalized_prefix = '' then
            begin
                Exit;
            end;
            if Pos(normalized_prefix, 'wo') = 1 then
            begin
                Result := string(Char($6211));
            end
            else if Pos(normalized_prefix, 'he') = 1 then
            begin
                Result := string(Char($548C));
            end
            else if Pos(normalized_prefix, 'yi') = 1 then
            begin
                Result := string(Char($4E00));
            end
            else if Pos(normalized_prefix, 'qu') = 1 then
            begin
                Result := string(Char($53BB));
            end
            else if Pos(normalized_prefix, 'le') = 1 then
            begin
                Result := string(Char($4E86));
            end
            else if Pos(normalized_prefix, 'ba') = 1 then
            begin
                Result := string(Char($5427));
            end
            else if Pos(normalized_prefix, 'de') = 1 then
            begin
                Result := string(Char($7684));
            end
            else if Pos(normalized_prefix, 'shi') = 1 then
            begin
                Result := string(Char($662F));
            end;
        end;
        function final_fixed_top_single_for_query_local: string;
        var
            normalized_query: string;
        begin
            Result := '';
            if input_syllable_count <> 1 then
            begin
                Exit;
            end;

            normalized_query := normalize_pinyin_text(lookup_text);
            if normalized_query = 'en' then
            begin
                Result := string(Char($55EF));
            end
            else if normalized_query = 'ba' then
            begin
                Result := string(Char($5427));
            end
            else if normalized_query = 'e' then
            begin
                Result := string(Char($5443));
            end
            else if normalized_query = 'o' then
            begin
                Result := string(Char($54E6));
            end
            else if normalized_query = 'ai' then
            begin
                Result := string(Char($5509));
            end
            else if normalized_query = 'ha' then
            begin
                Result := string(Char($54C8));
            end
            else if normalized_query = 'xi' then
            begin
                Result := string(Char($563B));
            end
            else if normalized_query = 'xing' then
            begin
                Result := string(Char($884C));
            end
            else if normalized_query = 'hao' then
            begin
                Result := string(Char($597D));
            end
            else if normalized_query = 'bang' then
            begin
                Result := string(Char($68D2));
            end
            else if normalized_query = 'ya' then
            begin
                Result := string(Char($5440));
            end
            else if normalized_query = 'qie' then
            begin
                Result := string(Char($5207));
            end
            else if normalized_query = 'ca' then
            begin
                Result := string(Char($64E6));
            end
            else if normalized_query = 'gun' then
            begin
                Result := string(Char($6EDA));
            end
            else if normalized_query = 'hei' then
            begin
                Result := string(Char($563F));
            end
            else if normalized_query = 'pi' then
            begin
                Result := string(Char($5C41));
            end
            else if normalized_query = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_query = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_query = 'er' then
            begin
                Result := string(Char($800C));
            end
            else if normalized_query = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_query = 'you' then
            begin
                Result := string(Char($6709));
            end
            else if normalized_query = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_query = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_query = 'qing' then
            begin
                Result := string(Char($8BF7));
            end;
        end;
        procedure ensure_fixed_top_single_char_visible_local;
        var
            expected_text: string;
            idx_local: Integer;
            move_idx: Integer;
            picked_local: TncCandidate;
        begin
            if (input_syllable_count <> 1) or (Length(m_candidates) = 0) then
            begin
                Exit;
            end;

            expected_text := final_fixed_top_single_for_query_local;
            if expected_text = '' then
            begin
                Exit;
            end;
            if (Trim(m_candidates[0].comment) = '') and
                (Trim(m_candidates[0].text) = expected_text) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    (Trim(m_candidates[idx_local].text) = expected_text) then
                begin
                    picked_local := m_candidates[idx_local];
                    picked_local.score := Max(picked_local.score,
                        m_candidates[0].score + 512);
                    picked_local.has_dict_weight := True;
                    picked_local.dict_weight := Max(picked_local.dict_weight,
                        picked_local.score);
                    for move_idx := idx_local downto 1 do
                    begin
                        m_candidates[move_idx] := m_candidates[move_idx - 1];
                    end;
                    m_candidates[0] := picked_local;
                    Exit;
                end;
            end;

            FillChar(picked_local, SizeOf(picked_local), 0);
            picked_local.text := expected_text;
            picked_local.comment := '';
            picked_local.score := m_candidates[0].score + 512;
            picked_local.source := cs_rule;
            picked_local.has_dict_weight := True;
            picked_local.dict_weight := picked_local.score;
            SetLength(m_candidates, Length(m_candidates) + 1);
            for move_idx := High(m_candidates) downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        function resolve_tail_text_for_partial_local(const tail_key: string;
            out out_tail_text: string; out out_tail_score: Integer): Boolean;
        var
            tail_results: TncCandidateList;
            tail_idx: Integer;
            candidate_text: string;
            candidate_score: Integer;
            syllables_local: TncPinyinParseResult;
            tail_syllable_count_local: Integer;
            unit_idx: Integer;
            fixed_text: string;

            function try_pick_exact_tail_local: Boolean;
            var
                exact_tail_idx_local: Integer;
                exact_candidate_text_local: string;
                exact_candidate_score_local: Integer;
            begin
                Result := False;
                if (tail_syllable_count_local <= 0) or
                    ((not lookup_exact_full_pinyin_cached_local(tail_key,
                    tail_results)) and
                    (not dictionary_lookup_cached(tail_key, tail_results))) then
                begin
                    Exit;
                end;

                for exact_tail_idx_local := 0 to High(tail_results) do
                begin
                    if Trim(tail_results[exact_tail_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_candidate_text_local :=
                        Trim(tail_results[exact_tail_idx_local].text);
                    if (exact_candidate_text_local = '') or
                        (get_candidate_text_unit_count(exact_candidate_text_local) <>
                        tail_syllable_count_local) then
                    begin
                        Continue;
                    end;
                    exact_candidate_score_local :=
                        tail_results[exact_tail_idx_local].score;
                    if tail_results[exact_tail_idx_local].has_dict_weight and
                        (tail_results[exact_tail_idx_local].dict_weight >
                        exact_candidate_score_local) then
                    begin
                        exact_candidate_score_local :=
                            tail_results[exact_tail_idx_local].dict_weight;
                    end;
                    out_tail_text := exact_candidate_text_local;
                    out_tail_score := exact_candidate_score_local;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            out_tail_text := '';
            out_tail_score := 0;
            if (m_dictionary = nil) or (tail_key = '') then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(tail_key);
            tail_syllable_count_local := Length(syllables_local);
            if is_full_pinyin_key(tail_key) and try_pick_exact_tail_local then
            begin
                Exit(True);
            end;

            if dictionary_prefix_lookup_cached(tail_key, tail_results) then
            begin
                for tail_idx := 0 to High(tail_results) do
                begin
                    if Trim(tail_results[tail_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text := Trim(tail_results[tail_idx].text);
                    if candidate_text = '' then
                    begin
                        Continue;
                    end;
                    candidate_score := tail_results[tail_idx].score;
                    if tail_results[tail_idx].has_dict_weight and
                        (tail_results[tail_idx].dict_weight > candidate_score) then
                    begin
                        candidate_score := tail_results[tail_idx].dict_weight;
                    end;
                    out_tail_text := candidate_text;
                    out_tail_score := candidate_score;
                    Exit(True);
                end;
            end;

            if (not is_full_pinyin_key(tail_key)) and try_pick_exact_tail_local then
            begin
                Exit(True);
            end;

            if Length(syllables_local) = 0 then
            begin
                fixed_text := final_fixed_single_for_prefix_local(tail_key);
                if fixed_text <> '' then
                begin
                    out_tail_text := fixed_text;
                    out_tail_score := 520;
                    Exit(True);
                end;
                Exit;
            end;

            for unit_idx := 0 to High(syllables_local) do
            begin
                fixed_text := final_fixed_single_for_pinyin_local(
                    syllables_local[unit_idx].text);
                if fixed_text = '' then
                begin
                    out_tail_text := '';
                    out_tail_score := 0;
                    Exit(False);
                end;
                out_tail_text := out_tail_text + fixed_text;
                Inc(out_tail_score, 520);
            end;
            Result := out_tail_text <> '';
        end;
        procedure promote_direct_complete_exact_candidate_local;
        var
            exact_results: TncCandidateList;
            exact_idx: Integer;
            existing_idx: Integer;
            move_idx: Integer;
            best_idx: Integer;
            best_weight: Integer;
            candidate_weight: Integer;
            candidate_text: string;
            top_units: Integer;
            picked_local: TncCandidate;

            function has_non_ascii_local(const value: string): Boolean;
            var
                char_idx: Integer;
            begin
                Result := False;
                for char_idx := 1 to Length(value) do
                begin
                    if Ord(value[char_idx]) > 127 then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (m_dictionary = nil) or (lookup_text = '') or
                (Length(m_candidates) = 0) or (input_syllable_count < 3) or
                (Pos('''', m_composition_text) > 0) then
            begin
                Exit;
            end;
            if (input_syllable_count >= 5) and
                top_candidate_has_natural_complete_segment_path_local(
                m_candidates) then
            begin
                Exit;
            end;
            if not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results) then
            begin
                if input_syllable_count >= 5 then
                begin
                    Exit;
                end;
                if not dictionary_lookup_cached(lookup_text, exact_results) then
                begin
                    Exit;
                end;
            end;
            if (Length(exact_results) = 0) and
                ((input_syllable_count >= 5) or
                (not dictionary_lookup_cached(lookup_text, exact_results))) then
            begin
                Exit;
            end;

            best_idx := -1;
            best_weight := Low(Integer);
            for exact_idx := 0 to High(exact_results) do
            begin
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results[exact_idx].text);
                if (candidate_text = '') or
                    (get_candidate_text_unit_count(candidate_text) <>
                    input_syllable_count) then
                begin
                    Continue;
                end;
                if (not has_non_ascii_local(candidate_text)) or
                    (exact_results[exact_idx].source = cs_user) or
                    exact_results[exact_idx].has_dict_weight then
                begin
                    Continue;
                end;
                candidate_weight := exact_results[exact_idx].score;
                if (best_idx < 0) or (candidate_weight > best_weight) then
                begin
                    best_idx := exact_idx;
                    best_weight := candidate_weight;
                end;
            end;
            if best_idx < 0 then
            begin
                Exit;
            end;

            top_units := get_candidate_text_unit_count(Trim(m_candidates[0].text));
            if (Trim(m_candidates[0].comment) = '') and
                (top_units = input_syllable_count) and
                (m_candidates[0].source = cs_rule) and
                (not m_candidates[0].has_dict_weight) then
            begin
                Exit;
            end;
            if (Trim(m_candidates[0].comment) = '') and
                (top_units >= input_syllable_count) and
                (m_candidates[0].has_dict_weight or
                (m_candidates[0].source = cs_user)) then
            begin
                Exit;
            end;

            existing_idx := -1;
            for exact_idx := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[exact_idx].comment) = '') and
                    SameText(Trim(m_candidates[exact_idx].text),
                    Trim(exact_results[best_idx].text)) then
                begin
                    existing_idx := exact_idx;
                    Break;
                end;
            end;

            if existing_idx = 0 then
            begin
                Exit;
            end;
            if existing_idx > 0 then
            begin
                picked_local := m_candidates[existing_idx];
                picked_local.score := Max(picked_local.score,
                    m_candidates[0].score + 128);
                picked_local.has_dict_weight := True;
                picked_local.dict_weight := Max(best_weight, picked_local.score);
                for move_idx := existing_idx downto 1 do
                begin
                    m_candidates[move_idx] := m_candidates[move_idx - 1];
                end;
                m_candidates[0] := picked_local;
                Exit;
            end;

            picked_local := exact_results[best_idx];
            picked_local.comment := '';
            picked_local.score := Max(picked_local.score,
                m_candidates[0].score + 128);
            picked_local.has_dict_weight := True;
            picked_local.dict_weight := Max(best_weight, picked_local.score);
            SetLength(m_candidates, Length(m_candidates) + 1);
            for move_idx := High(m_candidates) downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        procedure promote_resolved_high_coverage_partial_local;
        var
            idx_local: Integer;
            move_idx: Integer;
            best_idx: Integer;
            best_units: Integer;
            base_units: Integer;
            top_units: Integer;
            tail_units: Integer;
            tail_syllables: Integer;
            comment_key: string;
            tail_text: string;
            tail_score: Integer;
            generated_text: string;
            picked_local: TncCandidate;
            encoded_path: string;
        begin
            if (m_dictionary = nil) or (Length(m_candidates) = 0) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;
            if (input_syllable_count >= 5) and
                top_candidate_has_natural_complete_segment_path_local(
                m_candidates) then
            begin
                Exit;
            end;

            top_units := get_candidate_text_unit_count(Trim(m_candidates[0].text));
            best_idx := -1;
            best_units := top_units;
            FillChar(picked_local, SizeOf(picked_local), 0);
            for idx_local := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[idx_local].comment) = '' then
                begin
                    Continue;
                end;
                base_units := get_candidate_text_unit_count(
                    Trim(m_candidates[idx_local].text));
                if (base_units < Max(1, input_syllable_count - 2)) and
                    (base_units < top_units) then
                begin
                    Continue;
                end;

                comment_key := normalize_pinyin_text(
                    Trim(m_candidates[idx_local].comment));
                if not resolve_tail_text_for_partial_local(comment_key,
                    tail_text, tail_score) then
                begin
                    Continue;
                end;
                tail_units := get_candidate_text_unit_count(tail_text);
                tail_syllables := get_effective_compact_pinyin_unit_count(
                    comment_key);
                if tail_syllables <= 0 then
                begin
                    tail_syllables := tail_units;
                end;
                if (tail_syllables <= 0) or
                    (base_units <> input_syllable_count - tail_syllables) then
                begin
                    Continue;
                end;
                if (input_syllable_count < 4) and
                    is_full_pinyin_key(comment_key) and
                    (not SameText(comment_key, 'wo')) then
                begin
                    Continue;
                end;
                generated_text := Trim(m_candidates[idx_local].text) + tail_text;
                if (tail_units <= 0) or
                    (get_candidate_text_unit_count(generated_text) <= best_units) or
                    (get_candidate_text_unit_count(generated_text) >
                    input_syllable_count + Max(0, tail_units - tail_syllables)) then
                begin
                    Continue;
                end;

                best_idx := idx_local;
                best_units := get_candidate_text_unit_count(generated_text);
                picked_local := m_candidates[idx_local];
                picked_local.text := generated_text;
                picked_local.comment := '';
                picked_local.score := Max(picked_local.score + tail_score + 1536,
                    m_candidates[0].score + 1024);
                picked_local.has_dict_weight := True;
                picked_local.dict_weight := Max(picked_local.dict_weight,
                    picked_local.score);
                encoded_path := get_segment_path_for_candidate(
                    m_candidates[idx_local]);
                if Trim(encoded_path) = '' then
                begin
                    encoded_path := Trim(m_candidates[idx_local].text);
                end;
                remember_segment_path_for_candidate(picked_local.text, '',
                    encoded_path + c_segment_path_separator + tail_text,
                    picked_local.score);
            end;

            if best_idx < 0 then
            begin
                Exit;
            end;
            for move_idx := best_idx downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        procedure promote_aligned_head_phrase_partial_local;
        var
            idx_local: Integer;
            move_idx: Integer;
            best_idx: Integer;
            best_units: Integer;
            best_score: Integer;
            top_units: Integer;
            text_units_local: Integer;
            comment_syllables_local: Integer;
            consumed_syllables_local: Integer;
            picked_local: TncCandidate;
        begin
            if (Length(m_candidates) <= 1) or (input_syllable_count < 3) or
                (input_syllable_count > 5) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            top_units := get_candidate_text_unit_count(Trim(m_candidates[0].text));
            if (Trim(m_candidates[0].comment) = '') and
                (top_units >= input_syllable_count) and
                (m_candidates[0].has_dict_weight or
                (m_candidates[0].source = cs_user)) then
            begin
                Exit;
            end;

            best_idx := -1;
            best_units := -1;
            best_score := Low(Integer);
            for idx_local := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[idx_local].comment) = '' then
                begin
                    Continue;
                end;
                comment_syllables_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(m_candidates[idx_local].comment)));
                if comment_syllables_local <= 0 then
                begin
                    Continue;
                end;

                consumed_syllables_local := input_syllable_count -
                    comment_syllables_local;
                text_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[idx_local].text));
                if (text_units_local < 2) or
                    (text_units_local <> consumed_syllables_local) then
                begin
                    Continue;
                end;

                if (input_syllable_count = 3) and
                    (comment_syllables_local = 1) and
                    ((m_last_three_syllable_partial_preference_kind <> 1) or
                    (m_last_three_syllable_head_strength <= 0) or
                    (m_last_three_syllable_head_exact_text = '') or
                    (Trim(m_candidates[idx_local].text) <>
                    m_last_three_syllable_head_exact_text)) then
                begin
                    Continue;
                end;

                if (best_idx < 0) or (text_units_local > best_units) or
                    ((text_units_local = best_units) and
                    (m_candidates[idx_local].score > best_score)) then
                begin
                    best_idx := idx_local;
                    best_units := text_units_local;
                    best_score := m_candidates[idx_local].score;
                end;
            end;

            if best_idx <= 0 then
            begin
                Exit;
            end;

            if (Trim(m_candidates[0].comment) = '') and
                (top_units >= input_syllable_count) and
                (not m_candidates[0].has_dict_weight) and
                (m_candidates[0].source <> cs_user) and
                (best_units < input_syllable_count - 2) then
            begin
                Exit;
            end;

            picked_local := m_candidates[best_idx];
            for move_idx := best_idx downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        procedure ensure_exact_head_phrase_partial_visible_local;
        const
            c_insert_limit = 2;
        var
            syllables_local: TncPinyinParseResult;
            head_len: Integer;
            tail_len: Integer;
            local_idx: Integer;
            move_idx: Integer;
            insert_idx: Integer;
            existing_idx: Integer;
            result_idx: Integer;
            best_idx: Integer;
            best_weight: Integer;
            candidate_weight: Integer;
            candidate_text: string;
            head_key: string;
            tail_key: string;
            local_results: TncCandidateList;
            picked_local: TncCandidate;

            function build_key_local(const start_idx: Integer;
                const len: Integer): string;
            var
                key_idx: Integer;
            begin
                Result := '';
                for key_idx := start_idx to start_idx + len - 1 do
                begin
                    if (key_idx < 0) or (key_idx > High(syllables_local)) then
                    begin
                        Result := '';
                        Exit;
                    end;
                    Result := Result + normalize_pinyin_text(
                        syllables_local[key_idx].text);
                end;
            end;
        begin
            if (m_dictionary = nil) or (Length(m_candidates) = 0) or
                (input_syllable_count < 3) or
                (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;

            for tail_len := 1 to Min(2, input_syllable_count - 1) do
            begin
                head_len := input_syllable_count - tail_len;
                if head_len < 2 then
                begin
                    Continue;
                end;
                head_key := build_key_local(0, head_len);
                tail_key := build_key_local(head_len, tail_len);
                if (head_key = '') or (tail_key = '') then
                begin
                    Continue;
                end;
                if not lookup_exact_full_pinyin_cached_local(head_key,
                    local_results) then
                begin
                    if not dictionary_lookup_cached(head_key, local_results) then
                    begin
                        Continue;
                    end;
                end;
                if (Length(local_results) = 0) and
                    (not dictionary_lookup_cached(head_key, local_results)) then
                begin
                    Continue;
                end;

                best_idx := -1;
                best_weight := Low(Integer);
                for result_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text := Trim(local_results[result_idx].text);
                    if (candidate_text = '') or
                        (get_candidate_text_unit_count(candidate_text) <>
                        head_len) then
                    begin
                        Continue;
                    end;
                    candidate_weight := local_results[result_idx].score;
                    if local_results[result_idx].has_dict_weight and
                        (local_results[result_idx].dict_weight > candidate_weight) then
                    begin
                        candidate_weight := local_results[result_idx].dict_weight;
                    end;
                    if (best_idx < 0) or (candidate_weight > best_weight) then
                    begin
                        best_idx := result_idx;
                        best_weight := candidate_weight;
                    end;
                end;
                if best_idx < 0 then
                begin
                    Continue;
                end;

                existing_idx := -1;
                for local_idx := 0 to High(m_candidates) do
                begin
                    if SameText(Trim(m_candidates[local_idx].text),
                        Trim(local_results[best_idx].text)) and
                        SameText(Trim(m_candidates[local_idx].comment), tail_key) then
                    begin
                        existing_idx := local_idx;
                        Break;
                    end;
                end;

                insert_idx := 0;
                while (insert_idx < Length(m_candidates)) and
                    (insert_idx < c_insert_limit) and
                    (Trim(m_candidates[insert_idx].comment) = '') do
                begin
                    Inc(insert_idx);
                end;
                if insert_idx > c_insert_limit then
                begin
                    insert_idx := c_insert_limit;
                end;

                if (existing_idx >= 0) and (existing_idx <= insert_idx) then
                begin
                    Exit;
                end;

                if existing_idx >= 0 then
                begin
                    picked_local := m_candidates[existing_idx];
                    for move_idx := existing_idx downto insert_idx + 1 do
                    begin
                        m_candidates[move_idx] := m_candidates[move_idx - 1];
                    end;
                    m_candidates[insert_idx] := picked_local;
                    Exit;
                end;

                picked_local := local_results[best_idx];
                picked_local.comment := tail_key;
                picked_local.score := Max(picked_local.score, best_weight);
                SetLength(m_candidates, Length(m_candidates) + 1);
                for move_idx := High(m_candidates) downto insert_idx + 1 do
                begin
                    m_candidates[move_idx] := m_candidates[move_idx - 1];
                end;
                m_candidates[insert_idx] := picked_local;
                Exit;
            end;
        end;
        procedure promote_weak_two_syllable_head_single_tail_local;
        const
            c_weak_head_weight_max = 220;
        var
            syllables_local: TncPinyinParseResult;
            head_key: string;
            tail_key: string;
            local_results: TncCandidateList;
            result_idx: Integer;
            candidate_weight: Integer;
            best_head_weight: Integer;
            idx_local: Integer;
            move_idx: Integer;
            best_idx: Integer;
            best_score: Integer;
            text_units_local: Integer;
            comment_key: string;
            picked_local: TncCandidate;
        begin
            if (m_dictionary = nil) or (Length(m_candidates) <= 1) or
                (input_syllable_count <> 3) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) <> 3 then
            begin
                Exit;
            end;
            head_key := normalize_pinyin_text(syllables_local[0].text) +
                normalize_pinyin_text(syllables_local[1].text);
            tail_key := normalize_pinyin_text(syllables_local[1].text) +
                normalize_pinyin_text(syllables_local[2].text);
            if (head_key = '') or (tail_key = '') then
            begin
                Exit;
            end;

            best_head_weight := 0;
            if lookup_exact_full_pinyin_cached_local(head_key, local_results) or
                dictionary_lookup_cached(head_key, local_results) then
            begin
                for result_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(Trim(local_results[result_idx].text)) <> 2 then
                    begin
                        Continue;
                    end;
                    candidate_weight := local_results[result_idx].score;
                    if local_results[result_idx].has_dict_weight and
                        (local_results[result_idx].dict_weight > candidate_weight) then
                    begin
                        candidate_weight := local_results[result_idx].dict_weight;
                    end;
                    if candidate_weight > best_head_weight then
                    begin
                        best_head_weight := candidate_weight;
                    end;
                end;
            end;
            if (best_head_weight <= 0) or
                (best_head_weight > c_weak_head_weight_max) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) =
                    input_syllable_count) and
                    (m_candidates[idx_local].has_dict_weight or
                    (m_candidates[idx_local].source = cs_user)) then
                begin
                    Exit;
                end;
            end;

            best_idx := -1;
            best_score := Low(Integer);
            for idx_local := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[idx_local].comment) = '' then
                begin
                    Continue;
                end;
                text_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[idx_local].text));
                comment_key := normalize_pinyin_text(
                    Trim(m_candidates[idx_local].comment));
                if (text_units_local <> 1) or
                    (not SameText(comment_key, tail_key)) then
                begin
                    Continue;
                end;
                if (best_idx < 0) or (m_candidates[idx_local].score > best_score) then
                begin
                    best_idx := idx_local;
                    best_score := m_candidates[idx_local].score;
                end;
            end;
            if best_idx <= 0 then
            begin
                Exit;
            end;

            picked_local := m_candidates[best_idx];
            picked_local.score := Max(picked_local.score, m_candidates[0].score + 384);
            for move_idx := best_idx downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
        procedure promote_demonstrative_head_partial_local;
        var
            syllables_local: TncPinyinParseResult;
            head_text: string;
            tail_key: string;
            idx_local: Integer;
            out_idx: Integer;
            move_idx: Integer;
            existing_idx: Integer;
            picked_local: TncCandidate;
            units_local: TArray<string>;

            function is_friendly_tail_local(const value: string): Boolean;
            begin
                Result := SameText(value, 'ci') or SameText(value, 'hui') or
                    SameText(value, 'bian') or SameText(value, 'ge') or
                    SameText(value, 'zhong') or SameText(value, 'zhonglei') or
                    SameText(value, 'yang') or SameText(value, 'xie') or
                    SameText(value, 'yi') or SameText(value, 'yici') or
                    SameText(value, 'yihui') or SameText(value, 'yibian') or
                    SameText(value, 'yige') or SameText(value, 'yizhong') or
                    SameText(value, 'yizhonglei') or SameText(value, 'yiyang') or
                    SameText(value, 'yixie');
            end;
        begin
            if (Length(m_candidates) = 0) or (input_syllable_count < 2) or
                (input_syllable_count > 3) or (not is_full_pinyin_key(lookup_text)) then
            begin
                Exit;
            end;

            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            if Length(syllables_local) <> input_syllable_count then
            begin
                Exit;
            end;
            if SameText(normalize_pinyin_text(syllables_local[0].text), 'zhe') then
            begin
                head_text := string(Char($8FD9));
            end
            else if SameText(normalize_pinyin_text(syllables_local[0].text), 'na') or
                SameText(normalize_pinyin_text(syllables_local[0].text), 'nei') then
            begin
                head_text := string(Char($90A3));
            end
            else
            begin
                Exit;
            end;

            if input_syllable_count = 2 then
            begin
                tail_key := normalize_pinyin_text(syllables_local[1].text);
            end
            else
            begin
                if not SameText(normalize_pinyin_text(syllables_local[1].text), 'yi') then
                begin
                    Exit;
                end;
                tail_key := 'yi' + normalize_pinyin_text(syllables_local[2].text);
            end;
            if (tail_key = '') or (not is_friendly_tail_local(tail_key)) then
            begin
                Exit;
            end;

            out_idx := 0;
            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) =
                    input_syllable_count) then
                begin
                    units_local := split_text_units(Trim(m_candidates[idx_local].text));
                    if (Length(units_local) > 0) and (units_local[0] = head_text) then
                    begin
                        Continue;
                    end;
                end;
                if out_idx <> idx_local then
                begin
                    m_candidates[out_idx] := m_candidates[idx_local];
                end;
                Inc(out_idx);
            end;
            if out_idx <> Length(m_candidates) then
            begin
                SetLength(m_candidates, out_idx);
            end;

            existing_idx := -1;
            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].text) = head_text) and
                    SameText(normalize_pinyin_text(Trim(m_candidates[idx_local].comment)),
                    tail_key) then
                begin
                    existing_idx := idx_local;
                    Break;
                end;
            end;

            if existing_idx = 0 then
            begin
                Exit;
            end;
            if existing_idx > 0 then
            begin
                picked_local := m_candidates[existing_idx];
                picked_local.score := Max(picked_local.score,
                    m_candidates[0].score + 512);
                for move_idx := existing_idx downto 1 do
                begin
                    m_candidates[move_idx] := m_candidates[move_idx - 1];
                end;
                m_candidates[0] := picked_local;
                Exit;
            end;

            FillChar(picked_local, SizeOf(picked_local), 0);
            picked_local.text := head_text;
            picked_local.comment := tail_key;
            picked_local.score := 512;
            if Length(m_candidates) > 0 then
            begin
                picked_local.score := m_candidates[0].score + 512;
            end;
            picked_local.source := cs_rule;
            picked_local.has_dict_weight := False;
            picked_local.dict_weight := 0;
            SetLength(m_candidates, Length(m_candidates) + 1);
            for move_idx := High(m_candidates) downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;

        procedure ensure_exact_query_partial_candidate_visible_local;
        var
            exact_results_local: TncCandidateList;
            idx_local: Integer;
            move_idx: Integer;
            best_idx: Integer;
            existing_idx: Integer;
            candidate_text_units: Integer;
            comment_units: Integer;
            best_score_local: Integer;
            picked_local: TncCandidate;
        begin
            if (m_dictionary = nil) or (lookup_text = '') or
                (input_syllable_count < 2) or (input_syllable_count > 8) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) =
                    input_syllable_count) then
                begin
                    Exit;
                end;
            end;

            if not dictionary_lookup_cached(lookup_text, exact_results_local) then
            begin
                Exit;
            end;

            best_idx := -1;
            best_score_local := Low(Integer);
            for idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[idx_local].comment) = '' then
                begin
                    Continue;
                end;

                candidate_text_units := get_candidate_text_unit_count(
                    Trim(exact_results_local[idx_local].text));
                comment_units := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(exact_results_local[idx_local].comment)));
                if (candidate_text_units < 2) or (comment_units <= 0) or
                    (candidate_text_units + comment_units <> input_syllable_count) then
                begin
                    Continue;
                end;

                if exact_results_local[idx_local].score > best_score_local then
                begin
                    best_idx := idx_local;
                    best_score_local := exact_results_local[idx_local].score;
                end;
            end;

            if best_idx < 0 then
            begin
                Exit;
            end;

            existing_idx := -1;
            for idx_local := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[idx_local].text),
                    Trim(exact_results_local[best_idx].text)) and
                    SameText(Trim(m_candidates[idx_local].comment),
                    Trim(exact_results_local[best_idx].comment)) then
                begin
                    existing_idx := idx_local;
                    Break;
                end;
            end;

            if existing_idx = 0 then
            begin
                Exit;
            end;
            if existing_idx > 0 then
            begin
                picked_local := m_candidates[existing_idx];
                for move_idx := existing_idx downto 1 do
                begin
                    m_candidates[move_idx] := m_candidates[move_idx - 1];
                end;
                m_candidates[0] := picked_local;
                Exit;
            end;

            picked_local := exact_results_local[best_idx];
            if Length(m_candidates) > 0 then
            begin
                picked_local.score := Max(picked_local.score,
                    m_candidates[0].score + 512);
            end;
            SetLength(m_candidates, Length(m_candidates) + 1);
            for move_idx := High(m_candidates) downto 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
            end;
            m_candidates[0] := picked_local;
        end;
    begin
        if (input_syllable_count > 1) and
            (not should_skip_heavy_finalize_promotions_local) then
        begin
            ensure_explicit_apostrophe_aligned_complete_visible_local(m_candidates);
            filter_explicit_apostrophe_output_complete_local(m_candidates);
            promote_resolved_high_coverage_partial_local;
            ensure_exact_head_phrase_partial_visible_local;
            filter_self_composed_complete_candidates_when_normal_complete_exists;
            promote_preferred_three_syllable_partial_shape_local;
            promote_aligned_head_phrase_partial_local;
            promote_weak_two_syllable_head_single_tail_local;
            promote_high_coverage_long_partial_local;
            promote_direct_complete_exact_candidate_local;
            ensure_exact_query_partial_candidate_visible_local;
            promote_demonstrative_head_partial_local;
        end;
        ensure_fixed_top_single_char_visible_local;
        total_elapsed_ms := Int64(nc_monotonic_tick_ms - total_start_tick);
        m_last_lookup_timing_info := Format(
            'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
            [lookup_elapsed_ms, segment_elapsed_ms, path_search_elapsed_ms, runtime_elapsed_ms, post_elapsed_ms, sort_elapsed_ms,
            lookup_cache_hits, lookup_cache_misses, total_elapsed_ms]);
    end;

    function is_common_surname_text(const value: string): Boolean;
    var
        codepoint: Integer;
    begin
        Result := False;
        if not try_get_single_text_unit_codepoint(value, codepoint) then
        begin
            Exit;
        end;

        case codepoint of
            $8D75, $94B1, $5B59, $674E, $5468, $5434, $90D1, $738B, $51AF, $9648,
            $891A, $536B, $848B, $6C88, $97E9, $6768, $6731, $79E6, $8BB8, $4F55,
            $5415, $65BD, $5F20, $5B54, $66F9, $4E25, $534E, $91D1, $9B4F, $9676,
            $59DC, $621A, $8C22, $90B9, $55BB, $82CF, $6F58, $845B, $8303, $5F6D,
            $90CE, $9C81, $97E6, $9A6C, $82D7, $51E4, $65B9, $4FDE, $4EFB, $8881,
            $67F3, $9C8D, $53F2, $5510, $8D39, $5EC9, $5C91, $859B, $96F7, $8D3A,
            $502A, $6C64, $6ED5, $6BB7, $7F57, $6BD5, $90DD, $90AC, $5B89, $5E38,
            $4E50, $4E8E, $5085, $76AE, $9F50, $5EB7, $4F0D, $4F59, $5143, $987E,
            $5B5F, $5E73, $9EC4, $548C, $7A46, $8427, $5C39, $59DA, $90B5, $6E5B,
            $6C6A, $7941, $6BDB, $79B9, $72C4, $7C73, $660E, $81E7, $8BA1, $4F0F,
            $6210, $6234, $5B8B, $8305, $5E9E, $718A, $7EAA, $8212, $5C48, $9879,
            $795D, $8463, $6881, $675C, $962E, $84DD, $95F5, $5E2D, $5B63, $9EBB,
            $5F3A, $8D3E, $8DEF, $5A04, $5371, $6C5F, $7AE5, $989C, $90ED, $6885,
            $76DB, $6797, $949F, $5F90, $90B1, $9A86, $9AD8, $590F, $8521, $7530,
            $6A0A, $80E1, $51CC, $970D, $865E, $4E07, $67EF, $5362, $83AB, $623F,
            $7F2A, $89E3, $5E94, $4E01, $9093, $90C1, $5D14, $9F9A, $7A0B, $90A2,
            $88F4, $9646, $8363, $7FC1, $8340, $7F8A, $60E0, $7504, $5C01, $82AE,
            $50A8, $9773, $6BB5, $7126, $5DF4, $5F13, $7267, $8F66, $4FAF, $5B93,
            $84EC, $5168:
                Result := True;
        end;
    end;

    function is_preferred_partial_single_char_candidate(const candidate: TncCandidate): Boolean;
    const
        c_partial_preferred_min_weight = 120;
    var
        unit_text: string;
        codepoint: Integer;
        weight_value: Integer;
    begin
        Result := False;
        unit_text := Trim(candidate.text);
        if not try_get_single_text_unit_codepoint(unit_text, codepoint) then
        begin
            Exit;
        end;

        // Keep partial-leading single chars in common BMP CJK range to avoid
        // surfacing extremely rare code points as first choice for long input.
        if (codepoint < $4E00) or (codepoint > $9FFF) then
        begin
            Exit;
        end;

        if candidate.has_dict_weight then
        begin
            weight_value := candidate.dict_weight;
        end
        else
        begin
            weight_value := candidate.score;
        end;
        Result := weight_value >= c_partial_preferred_min_weight;
    end;

    function is_bmp_cjk_single_char_candidate(const candidate: TncCandidate): Boolean;
    var
        unit_text: string;
        codepoint: Integer;
    begin
        Result := False;
        unit_text := Trim(candidate.text);
        if not try_get_single_text_unit_codepoint(unit_text, codepoint) then
        begin
            Exit;
        end;

        Result := (codepoint >= $4E00) and (codepoint <= $9FFF);
    end;

    function get_text_unit_count(const text: string): Integer;
    var
        idx: Integer;
        codepoint: Integer;
    begin
        Result := 0;
        if text = '' then
        begin
            Exit;
        end;

        idx := 1;
        while idx <= Length(text) do
        begin
            codepoint := Ord(text[idx]);
            if (codepoint >= $D800) and (codepoint <= $DBFF) and (idx < Length(text)) then
            begin
                if (Ord(text[idx + 1]) >= $DC00) and (Ord(text[idx + 1]) <= $DFFF) then
                begin
                    Inc(idx);
                end;
            end;
            Inc(Result);
            Inc(idx);
        end;
    end;

    function get_input_syllable_count_for_text(const pinyin_text: string;
        const allow_relaxed_split: Boolean = False): Integer;
    begin
        Result := get_effective_compact_pinyin_unit_count(pinyin_text, allow_relaxed_split);
    end;

    function get_input_syllable_count: Integer;
    begin
        Result := get_input_syllable_count_for_text(m_composition_text);
    end;

    function is_repeated_two_syllable_query_text(const pinyin_text: string): Boolean;
    var
        parser: TncPinyinParser;
        syllables: TncPinyinParseResult;
        reconstructed: string;
    begin
        Result := False;
        if pinyin_text = '' then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            syllables := parser.parse(pinyin_text);
        finally
            parser.Free;
        end;

        if Length(syllables) <> 2 then
        begin
            Exit;
        end;

        if (syllables[0].text = '') or (syllables[1].text = '') then
        begin
            Exit;
        end;
        if not is_valid_pinyin_syllable(syllables[0].text) or
            (not is_valid_pinyin_syllable(syllables[1].text)) then
        begin
            Exit;
        end;

        reconstructed := syllables[0].text + syllables[1].text;
        Result := SameText(reconstructed, pinyin_text) and SameText(syllables[0].text, syllables[1].text);
    end;

    function is_two_unit_redup_text(const value: string): Boolean;
    var
        units: TArray<string>;
        trimmed: string;
    begin
        Result := False;
        trimmed := Trim(value);
        if trimmed = '' then
        begin
            Exit;
        end;

        units := split_text_units(trimmed);
        if Length(units) <> 2 then
        begin
            Exit;
        end;

        Result := units[0] = units[1];
    end;

    function is_runtime_constructed_tail_friendly(const tail_text: string): Boolean;
    var
        tail_codepoint: Integer;
    begin
        Result := False;
        if not try_get_single_text_unit_codepoint(Trim(tail_text), tail_codepoint) then
        begin
            Exit;
        end;

        case tail_codepoint of
            $4E2A, $4F4D, $6B21, $70B9, $4E9B, $79CD, $5929,
            $5E74, $6708, $91CC, $4E0B, $56DE, $904D, $58F0,
            $9762, $773C, $8FB9:
                Result := True;
        end;
    end;

        function try_get_runtime_function_head_expected_text(
            const syllable_text: string;
            out out_text: string
        ): Boolean;
    begin
        out_text := '';
        if SameText(syllable_text, 'zhe') then
        begin
            out_text := string(Char($8FD9));
        end
        else if SameText(syllable_text, 'na') or SameText(syllable_text, 'nei') then
        begin
            out_text := string(Char($90A3));
        end
        else if SameText(syllable_text, 'yi') then
        begin
            out_text := string(Char($4E00));
        end
        else if SameText(syllable_text, 'liang') then
        begin
            out_text := string(Char($4E24));
        end
        else if SameText(syllable_text, 'ji') then
        begin
            out_text := string(Char($51E0));
        end
        else if SameText(syllable_text, 'mei') then
        begin
            out_text := string(Char($6BCF));
        end;
        Result := out_text <> '';
    end;

        function try_get_interrogative_location_tail_expected_text(
            const syllable_text: string;
            out out_text: string
        ): Boolean;
        begin
            out_text := '';
            if SameText(syllable_text, 'li') then
            begin
                out_text := string(Char($91CC));
            end
            else if SameText(syllable_text, 'er') then
            begin
                out_text := string(Char($513F));
            end;
            Result := out_text <> '';
        end;

        function is_runtime_constructed_phrase_friendly(const text: string): Boolean;
        var
            units: TArray<string>;
            tail_codepoint: Integer;
    begin
        Result := False;
        units := split_text_units(Trim(text));
        if Length(units) <> 2 then
        begin
            Exit;
        end;

        if units[0] = units[1] then
        begin
            Result := True;
            Exit;
        end;

        if is_runtime_constructed_tail_friendly(units[1]) then
        begin
            Result := True;
            Exit;
        end;

        if (units[1] = string(Char($5427))) or
            (units[1] = string(Char($5417))) or
            (units[1] = string(Char($5462))) then
        begin
            Result := True;
            Exit;
        end;

        if not try_get_single_text_unit_codepoint(units[1], tail_codepoint) then
        begin
            Exit;
        end;

        case tail_codepoint of
            $4E2A, // 锟?
            $4F4D, // 锟?
            $6B21, // 锟?
            $70B9, // 锟?
            $4E9B, // 锟?
            $79CD, // 锟?
            $5929, // 锟?
            $5E74, // 锟?
            $6708, // 锟?
            $91CC, // 锟?
            $4E0B, // 锟?
            $56DE, // 锟?
            $904D, // 锟?
            $58F0, // 锟?
            $9762, // 锟?
            $773C: // 锟?
                Result := True;
        end;
    end;

    procedure ensure_redup_complete_candidate_visible(var candidates: TncCandidateList; const boundary_limit: Integer);
    var
        idx: Integer;
        best_index: Integer;
        best_score: Integer;
        target_index: Integer;
        effective_boundary: Integer;
        picked: TncCandidate;
    begin
        if (not repeated_two_syllable_query) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        effective_boundary := boundary_limit;
        if (effective_boundary <= 0) or (effective_boundary > Length(candidates)) then
        begin
            effective_boundary := Length(candidates);
        end;
        if effective_boundary <= 0 then
        begin
            Exit;
        end;

        best_index := -1;
        best_score := Low(Integer);
        for idx := 0 to High(candidates) do
        begin
            if candidates[idx].comment <> '' then
            begin
                Continue;
            end;
            if not is_two_unit_redup_text(candidates[idx].text) then
            begin
                Continue;
            end;

            if (best_index < 0) or (candidates[idx].score > best_score) then
            begin
                best_index := idx;
                best_score := candidates[idx].score;
            end;
        end;

        if best_index < 0 then
        begin
            Exit;
        end;

        target_index := 4;
        if target_index >= effective_boundary then
        begin
            target_index := effective_boundary - 1;
        end;
        if target_index < 0 then
        begin
            Exit;
        end;

        if best_index <= target_index then
        begin
            Exit;
        end;

        picked := candidates[best_index];
        for idx := best_index downto target_index + 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[target_index] := picked;
    end;

    procedure merge_head_only_full_lookup_candidates(var candidates: TncCandidateList;
        const pinyin_key: string);
    const
        c_head_only_full_non_user_limit = 4;
        c_head_only_full_non_user_penalty = 220;
        c_head_only_full_exact_long_bonus = 520;
    var
        candidate_text: string;
        filtered: TncCandidateList;
        text_units: Integer;
        non_user_count: Integer;
        input_syllables: Integer;
        idx: Integer;
        is_exact_long_full_match: Boolean;
    begin
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit;
        end;

        input_syllables := get_input_syllable_count_for_text(pinyin_key);
        if (input_syllables >= 4) and is_full_pinyin_key(pinyin_key) then
        begin
            if not dictionary_exact_lookup_cached(pinyin_key, full_lookup_candidates) then
            begin
                Exit;
            end;
        end
        else if not dictionary_lookup_cached(pinyin_key, full_lookup_candidates) then
        begin
            Exit;
        end;

        if Length(full_lookup_candidates) = 0 then
        begin
            Exit;
        end;

        clear_candidate_comments(full_lookup_candidates);
        SetLength(filtered, 0);
        non_user_count := 0;
        for idx := 0 to High(full_lookup_candidates) do
        begin
            candidate_text := Trim(full_lookup_candidates[idx].text);
            if candidate_text = '' then
            begin
                Continue;
            end;

            text_units := get_text_unit_count(candidate_text);
            if text_units < 2 then
            begin
                Continue;
            end;

            is_exact_long_full_match := (input_syllables >= 4) and
                (text_units = input_syllables);

            if is_exact_long_full_match then
            begin
                if (full_lookup_candidates[idx].source <> cs_user) and
                    (not full_lookup_candidates[idx].has_dict_weight) then
                begin
                    full_lookup_candidates[idx].has_dict_weight := True;
                    full_lookup_candidates[idx].dict_weight := full_lookup_candidates[idx].score;
                end;
                Inc(full_lookup_candidates[idx].score, c_head_only_full_exact_long_bonus);
                SetLength(filtered, Length(filtered) + 1);
                filtered[High(filtered)] := full_lookup_candidates[idx];
                Continue;
            end;

            if full_lookup_candidates[idx].source = cs_user then
            begin
                SetLength(filtered, Length(filtered) + 1);
                filtered[High(filtered)] := full_lookup_candidates[idx];
                Continue;
            end;

            // Keep only phrase-shaped full matches near input syllable length.
            if (input_syllables > 0) and ((text_units < input_syllables - 1) or (text_units > input_syllables + 1)) then
            begin
                Continue;
            end;

            if non_user_count >= c_head_only_full_non_user_limit then
            begin
                Continue;
            end;
            Inc(non_user_count);
            Dec(full_lookup_candidates[idx].score, c_head_only_full_non_user_penalty);
            SetLength(filtered, Length(filtered) + 1);
            filtered[High(filtered)] := full_lookup_candidates[idx];
        end;

        if Length(filtered) = 0 then
        begin
            Exit;
        end;

        candidates := merge_candidate_lists(candidates, filtered, 0);
    end;

    procedure merge_exact_long_full_lookup_candidates(var candidates: TncCandidateList;
        const pinyin_key: string);
    const
        c_exact_long_full_visible_bonus = 840;
        c_exact_long_full_limit = 6;
    var
        exact_matches: TncCandidateList;
        lookup_results: TncCandidateList;
        input_syllables: Integer;
        text_units: Integer;
        idx: Integer;
        candidate_text: string;
    begin
        if (m_dictionary = nil) or (pinyin_key = '') or
            (not c_suppress_nonlexicon_complete_long_candidates) then
        begin
            Exit;
        end;

        input_syllables := get_input_syllable_count_for_text(pinyin_key);
        if input_syllables < 3 then
        begin
            Exit;
        end;

        if is_full_pinyin_key(pinyin_key) then
        begin
            if not dictionary_exact_lookup_cached(pinyin_key, lookup_results) then
            begin
                Exit;
            end;
        end
        else if not dictionary_lookup_cached(pinyin_key, lookup_results) then
        begin
            Exit;
        end;

        SetLength(exact_matches, 0);
        for idx := 0 to High(lookup_results) do
        begin
            candidate_text := Trim(lookup_results[idx].text);
            if (candidate_text = '') or (lookup_results[idx].comment <> '') then
            begin
                Continue;
            end;

            text_units := get_text_unit_count(candidate_text);
            if (text_units <> input_syllables) or (text_units < 2) then
            begin
                Continue;
            end;

            if (lookup_results[idx].source <> cs_user) and
                (not lookup_results[idx].has_dict_weight) then
            begin
                lookup_results[idx].has_dict_weight := True;
                lookup_results[idx].dict_weight := lookup_results[idx].score;
            end;
            Inc(lookup_results[idx].score, c_exact_long_full_visible_bonus);
            SetLength(exact_matches, Length(exact_matches) + 1);
            exact_matches[High(exact_matches)] := lookup_results[idx];
        end;

        if Length(exact_matches) = 0 then
        begin
            Exit;
        end;

        clear_candidate_comments(exact_matches);
        sort_candidates_lightweight(exact_matches);
        if Length(exact_matches) > c_exact_long_full_limit then
        begin
            SetLength(exact_matches, c_exact_long_full_limit);
        end;
        candidates := merge_candidate_lists(candidates, exact_matches, 0);
    end;

    procedure merge_long_sentence_exact_candidates(var candidates: TncCandidateList;
        const pinyin_key: string);
    const
        c_long_sentence_state_limit = 48;
        c_long_sentence_single_top_n = 1;
        c_long_sentence_multi_top_n = 4;
        c_long_sentence_complete_non_user_limit = 4;
        c_long_sentence_partial_non_user_limit = 4;
        c_long_sentence_partial_remaining_limit = 6;
        c_long_sentence_max_segment_len = 3;
        c_long_sentence_single_penalty = 120;
        c_long_sentence_multi_bonus = 260;
        c_long_sentence_completion_bonus = 420;
        c_long_sentence_continuation_bonus = 40;
        c_long_sentence_candidate_visible_bonus = 1320;
        c_long_sentence_partial_visible_bonus = 980;
        c_long_sentence_partial_remaining_penalty = 132;
        c_long_sentence_partial_quadratic_penalty = 42;
        c_long_sentence_top_exact_bonus = 220;
        c_long_sentence_exact_rank_step = 52;
        c_long_sentence_bridge_bonus_low = 120;
        c_long_sentence_bridge_bonus_mid = 220;
        c_long_sentence_bridge_bonus_high = 340;
        c_long_sentence_bridge_bonus_top = 520;
        c_long_sentence_partial_tail_penalty = 140;
        c_long_sentence_phrase_query_latest_bonus = 420;
        c_long_sentence_single_query_latest_bonus = 180;
        c_long_sentence_protected_span_max_len = 4;
        c_long_sentence_protected_span_min_weight = 260;
    var
        exact_states: TArray<TList<TncSegmentPathState>>;
        exact_dedup: TArray<TDictionary<string, Integer>>;
        exact_sorted_states: TArray<TncSegmentPathState>;
        exact_bridge_bonus_cache: TDictionary<string, Integer>;
        exact_suffix_first_unit_cache: TDictionary<Integer, string>;
        exact_protected_span_text_cache: TDictionary<string, string>;
        exact_protected_span_weight_cache: TDictionary<string, Integer>;
        exact_protected_span_rank_cache: TDictionary<string, Integer>;
        exact_syllables: TncPinyinParseResult;
        seed_prev_prev_text: string;
        seed_prev_text: string;
        exact_state: TncSegmentPathState;
        exact_new_state: TncSegmentPathState;
        exact_existing_state: TncSegmentPathState;
        exact_lookup_results: TncCandidateList;
        exact_matches: TncCandidateList;
        exact_candidate: TncCandidate;
        exact_segment_text: string;
        exact_candidate_text: string;
        exact_key: string;
        exact_state_pos: Integer;
        exact_segment_len: Integer;
        exact_next_pos: Integer;
        exact_state_index: Integer;
        exact_candidate_index: Integer;
        exact_existing_state_index: Integer;
        exact_keep_count: Integer;
        exact_non_user_added: Integer;
        exact_candidate_limit: Integer;
        exact_units: Integer;
        exact_segment_count: Integer;
        exact_multi_char_segment_count: Integer;
        exact_effective_weight: Integer;
        exact_transition_bonus: Integer;
        exact_pair_bonus: Integer;
        exact_bridge_bonus: Integer;
        exact_rank_bonus: Integer;
        exact_path_bonus: Integer;
        exact_yu_context_adjustment: Integer;
        exact_span_adjustment: Integer;
        exact_matches_candidate: TncCandidate;
        exact_path_confidence: Integer;
        exact_idx: Integer;
        exact_debug_path: string;
        exact_remaining_pinyin: string;
        exact_remaining_syllables: Integer;
        exact_partial_score: Integer;
        query_allows_incomplete_tail: Boolean;
        query_extends_full_tail: Boolean;
        exact_segment_uses_prefix_lookup: Boolean;
        exact_required_multi_char_segment_count: Integer;
        exact_state_limit_local: Integer;
        exact_complete_non_user_limit_local: Integer;
        exact_partial_non_user_limit_local: Integer;
        exact_partial_remaining_limit_local: Integer;
        exact_prefix_single_candidate_limit_local: Integer;
        exact_prefix_multi_candidate_limit_local: Integer;
        exact_lightweight_incomplete_tail: Boolean;
        exact_top_effective_weight: Integer;
        exact_accepted_count: Integer;
        exact_rescue_weight: Integer;
        exact_rescue_rank_bonus: Integer;
        exact_rescue_text: string;
        exact_same_as_rescue: Boolean;
        exact_left_fixed_text: string;
        exact_right_fixed_text: string;
        exact_third_fixed_text: string;
        exact_best_two_syllable_text: string;
        exact_best_two_syllable_weight: Integer;
        exact_best_three_syllable_text: string;
        exact_best_three_syllable_weight: Integer;
        exact_phrase_bonus: Integer;
        exact_resolved_text: string;
        exact_resolved_weight: Integer;
        exact_resolved_rank_bonus: Integer;
        exact_resolved_inserted: Boolean;
        exact_resolved_candidate: TncCandidate;

        function merge_source_rank(const left: TncCandidateSource;
            const right: TncCandidateSource): TncCandidateSource;
        begin
            if (left = cs_user) or (right = cs_user) then
            begin
                Result := cs_user;
            end
            else
            begin
                Result := cs_rule;
            end;
        end;

        function build_state_key(const value: TncSegmentPathState): string;
        begin
            Result := LowerCase(Trim(value.text)) + #1 + LowerCase(Trim(value.prev_text)) +
                #1 + LowerCase(Trim(value.prev_pinyin_text)) + #1 +
                LowerCase(Trim(value.prev_prev_text)) + #1 +
                IntToStr(Ord(value.has_multi_segment));
        end;

        function get_encoded_path_segment_count_local(const encoded_path: string): Integer;
        var
            local_idx: Integer;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            Result := 1;
            for local_idx := 1 to Length(encoded_path) do
            begin
                if encoded_path[local_idx] = c_segment_path_separator then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function get_multi_char_segment_count(const encoded_path: string): Integer;
        var
            segment_parts: TArray<string>;
            part: string;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            for part in segment_parts do
            begin
                if get_candidate_text_unit_count(Trim(part)) >= 2 then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function build_exact_syllable_text(const start_index: Integer;
            const syllable_count: Integer): string;
        var
            local_idx: Integer;
            local_last_index: Integer;
        begin
            Result := '';
            if (start_index < 0) or (syllable_count <= 0) then
            begin
                Exit;
            end;

            local_last_index := start_index + syllable_count - 1;
            if local_last_index > High(exact_syllables) then
            begin
                Exit;
            end;

            for local_idx := start_index to local_last_index do
            begin
                Result := Result + exact_syllables[local_idx].text;
            end;
        end;

        function is_extendable_trailing_prefix_segment_local(
            const value: string): Boolean;
        var
            effective_parts: TncPinyinParseResult;
            local_syllable_count: Integer;
            local_last_syllable_text: string;
            suffix_ch: Char;
            extended_key: string;
        begin
            Result := False;
            if value = '' then
            begin
                Exit;
            end;

            effective_parts := get_effective_compact_pinyin_syllables(value);
            local_syllable_count := Length(effective_parts);
            if local_syllable_count <= 0 then
            begin
                Exit;
            end;

            local_last_syllable_text := Trim(effective_parts[High(effective_parts)].text);
            if local_last_syllable_text = '' then
            begin
                Exit;
            end;

            for suffix_ch := 'a' to 'z' do
            begin
                extended_key := value + suffix_ch;
                if get_effective_compact_pinyin_unit_count(extended_key) = local_syllable_count then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_long_sentence_allowed_single_char_text_local(const value: string): Boolean;
        begin
            Result := (value = string(Char($6211))) or
                (value = string(Char($4F60))) or
                (value = string(Char($4ED6))) or
                (value = string(Char($5979))) or
                (value = string(Char($5B83))) or
                (value = string(Char($4EEC))) or
                (value = string(Char($548C))) or
                (value = string(Char($53BB))) or
                (value = string(Char($6765))) or
                (value = string(Char($5728))) or
                (value = string(Char($662F))) or
                (value = string(Char($6709))) or
                (value = string(Char($8981))) or
                (value = string(Char($60F3))) or
                (value = string(Char($4F1A))) or
                (value = string(Char($80FD))) or
                (value = string(Char($53EA))) or
                (value = string(Char($628A))) or
                (value = string(Char($88AB))) or
                (value = string(Char($8BA9))) or
                (value = string(Char($7ED9))) or
                (value = string(Char($5411))) or
                (value = string(Char($5BF9))) or
                (value = string(Char($4E8E))) or
                (value = string(Char($4E3A))) or
                (value = string(Char($8DDF))) or
                (value = string(Char($540C))) or
                (value = string(Char($4ECE))) or
                (value = string(Char($4E0E))) or
                (value = string(Char($800C))) or
                (value = string(Char($5E76))) or
                (value = string(Char($5C31))) or
                (value = string(Char($4E5F))) or
                (value = string(Char($90FD))) or
                (value = string(Char($8FD8))) or
                (value = string(Char($53C8))) or
                (value = string(Char($518D))) or
                (value = string(Char($4E0D))) or
                (value = string(Char($5F88))) or
                (value = string(Char($592A))) or
                (value = string(Char($5730))) or
                (value = string(Char($5F97))) or
                (value = string(Char($7684))) or
                (value = string(Char($4EBA))) or
                (value = string(Char($4E2A))) or
                (value = string(Char($4E00))) or
                (value = string(Char($4EE5))) or
                (value = string(Char($5176))) or
                (value = string(Char($7528))) or
                (value = string(Char($5404))) or
                (value = string(Char($4E2D))) or
                (value = string(Char($4E0A))) or
                (value = string(Char($4E0B))) or
                (value = string(Char($524D))) or
                (value = string(Char($540E))) or
                (value = string(Char($5185))) or
                (value = string(Char($5916))) or
                (value = string(Char($91CC))) or
                (value = string(Char($8BE5))) or
                (value = string(Char($6B64))) or
                (value = string(Char($6BCF))) or
                (value = string(Char($7740))) or
                (value = string(Char($8FD9)));
        end;
        function is_long_sentence_allowed_single_char_text_codepoint_local(
            const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765, $5728,
                $662F, $6709, $8981, $60F3, $4F1A, $80FD, $53EA, $628A, $88AB,
                $8BA9, $7ED9, $5411, $5BF9, $4E8E, $8DDF, $540C, $4ECE, $4E0E,
                $5C31, $4E5F, $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A,
                $6700, $5DF2, $5C06, $5230, $800C, $5E76, $7684, $5730, $5F97,
                $7740, $8FC7, $4E86, $4E3A, $4E2A, $4E00, $4EE5, $5176, $7528,
                $5404, $4E2D, $4E0A, $4E0B, $524D, $540E, $5185, $5916, $91CC,
                $8BE5, $6B64, $6BCF:
                    Result := True;
            end;
        end;

        function is_long_sentence_ambiguous_function_syllable_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'de') or
                (normalized_syllable = 'ta') or
                (normalized_syllable = 'wei') or
                (normalized_syllable = 'yu') or
                (normalized_syllable = 'yong') or
                (normalized_syllable = 'qi') or
                (normalized_syllable = 'shi') or
                (normalized_syllable = 'zhe') or
                (normalized_syllable = 'ge') or
                (normalized_syllable = 'na') or
                (normalized_syllable = 'mei') or
                (normalized_syllable = 'zai') or
                (normalized_syllable = 'you');
        end;

        function get_fixed_long_sentence_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end;
        end;

        function has_strong_two_syllable_exact_segment_local(
            const segment_start: Integer): Boolean;
        const
            c_two_syllable_exact_probe_limit = 6;
        var
            query_key: string;
            exact_results: TncCandidateList;
            idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
        begin
            Result := False;
            if (segment_start < 0) or (segment_start + 1 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(exact_syllables[segment_start].text) +
                normalize_pinyin_text(exact_syllables[segment_start + 1].text);
            if (query_key = '') or (not dictionary_exact_lookup_cached(query_key, exact_results)) then
            begin
                Exit;
            end;

            for idx := 0 to High(exact_results) do
            begin
                if idx >= c_two_syllable_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[idx].source = cs_user) or
                    (not exact_results[idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[idx]) or
                    is_runtime_common_pattern_candidate(exact_results[idx]) or
                    is_runtime_redup_candidate(exact_results[idx]) then
                begin
                    Continue;
                end;
                candidate_text := Trim(exact_results[idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 2 then
                begin
                    Continue;
                end;
                Exit(True);
            end;
        end;

        function is_preferred_long_sentence_single_char_local(
            const syllable_text: string; const value: string;
            const effective_weight: Integer): Boolean;
        const
            c_long_sentence_single_char_probe_limit = 8;
            c_long_sentence_single_char_preferred_max_rank = 4;
            c_long_sentence_single_char_top_ratio_pct = 85;
        var
            exact_single_results: TncCandidateList;
            idx: Integer;
            candidate_text: string;
            weight_value: Integer;
            top_weight_value: Integer;
            single_char_rank: Integer;
            fixed_text: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (syllable_text = '') or (value = '') or
                (Length(Trim(value)) <> 1) then
            begin
                Exit;
            end;

            fixed_text := get_fixed_long_sentence_single_char_local(syllable_text);
            if fixed_text <> '' then
            begin
                if SameText(fixed_text, value) then
                begin
                    Exit(True);
                end;
                if not is_long_sentence_ambiguous_function_syllable_local(
                    syllable_text) then
                begin
                    Exit(False);
                end;
            end;

            if not m_dictionary.single_char_matches_pinyin(syllable_text, value) then
            begin
                Exit(False);
            end;

            if not dictionary_exact_lookup_cached(normalize_pinyin_text(syllable_text),
                exact_single_results) then
            begin
                Exit(True);
            end;

            top_weight_value := 0;
            single_char_rank := 0;
            for idx := 0 to High(exact_single_results) do
            begin
                if idx >= c_long_sentence_single_char_probe_limit then
                begin
                    Break;
                end;

                candidate_text := Trim(exact_single_results[idx].text);
                if (candidate_text = '') or (exact_single_results[idx].comment <> '') or
                    (get_candidate_text_unit_count(candidate_text) <> 1) then
                begin
                    Continue;
                end;

                Inc(single_char_rank);
                if exact_single_results[idx].has_dict_weight then
                begin
                    weight_value := exact_single_results[idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_single_results[idx].score;
                end;

                if top_weight_value <= 0 then
                begin
                    top_weight_value := weight_value;
                end;

                if not SameText(candidate_text, value) then
                begin
                    Continue;
                end;

                Exit((single_char_rank <= c_long_sentence_single_char_preferred_max_rank) or
                    (top_weight_value <= 0) or
                    ((effective_weight * 100) >=
                    (top_weight_value * c_long_sentence_single_char_top_ratio_pct)));
            end;
        end;

        function is_extended_long_sentence_allowed_single_char_candidate_local(
            const syllable_text: string; const value: string;
            const effective_weight: Integer): Boolean;
        var
            fixed_text: string;
        begin
            fixed_text := get_fixed_long_sentence_single_char_local(syllable_text);
            if fixed_text <> '' then
            begin
                if SameText(fixed_text, value) then
                begin
                    Exit(True);
                end;
                if not is_long_sentence_ambiguous_function_syllable_local(
                    syllable_text) then
                begin
                    Exit(False);
                end;
            end;

            Result := is_long_sentence_allowed_single_char_text_codepoint_local(value);
            if Result then
            begin
                Exit;
            end;

            if input_syllable_count < c_long_sentence_full_path_min_syllables then
            begin
                Exit(False);
            end;
            if (Length(Trim(value)) <> 1) or (effective_weight < 560) then
            begin
                Exit(False);
            end;

            Result := is_preferred_long_sentence_single_char_local(
                syllable_text, value, effective_weight);
        end;

        function try_get_best_context_extended_prefix_exact_phrase_local(
            const start_idx: Integer; const base_syllables: Integer;
            out best_text: string; out best_weight: Integer): Boolean;
        const
            c_context_extended_prefix_bonus = 1600;
            c_context_extended_prefix_max_chunk = 5;
        var
            extended_syllables: Integer;
            resolved_text: string;
            resolved_score: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            resolved_path: string;
            prefix_units: TArray<string>;
            prefix_text: string;
            unit_idx: Integer;
            weight_value: Integer;
        begin
            Result := False;
            best_text := '';
            best_weight := 0;
            if (base_syllables < 2) or (start_idx < 0) or
                (start_idx + base_syllables >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            extended_syllables := base_syllables + 1;
            if (extended_syllables > c_context_extended_prefix_max_chunk) or
                (start_idx + extended_syllables - 1 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            if not try_resolve_best_exact_subspan_local(start_idx,
                extended_syllables, resolved_text, resolved_score,
                resolved_segments, resolved_path, resolved_has_anchor,
                resolved_anchor_units) then
            begin
                Exit;
            end;

            prefix_units := split_text_units(Trim(resolved_text));
            if Length(prefix_units) < base_syllables then
            begin
                Exit;
            end;

            prefix_text := '';
            for unit_idx := 0 to base_syllables - 1 do
            begin
                prefix_text := prefix_text + prefix_units[unit_idx];
            end;
            if get_candidate_text_unit_count(prefix_text) <> base_syllables then
            begin
                Exit;
            end;

            weight_value := resolved_score + c_context_extended_prefix_bonus;
            if resolved_has_anchor then
            begin
                Inc(weight_value, resolved_anchor_units * 192);
            end;
            if resolved_segments > 1 then
            begin
                Dec(weight_value, (resolved_segments - 1) * 96);
            end;
            best_text := prefix_text;
            best_weight := weight_value;
            Result := True;
        end;

        function try_get_best_long_sentence_single_char_local(
            const syllable_text: string; out out_text: string;
            out out_weight: Integer): Boolean;
        const
            c_long_sentence_single_probe_limit = 8;
        var
            single_results: TncCandidateList;
            single_idx: Integer;
            single_text: string;
            weight_value: Integer;
            fixed_text: string;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if syllable_text = '' then
            begin
                Exit;
            end;

            fixed_text := get_fixed_long_sentence_single_char_local(syllable_text);
            if (fixed_text <> '') and
                (not is_long_sentence_ambiguous_function_syllable_local(
                syllable_text)) then
            begin
                out_text := fixed_text;
                out_weight := 960;
                Exit(True);
            end;

            if not dictionary_exact_lookup_cached(normalize_pinyin_text(syllable_text),
                single_results) then
            begin
                Exit;
            end;

            for single_idx := 0 to High(single_results) do
            begin
                if single_idx >= c_long_sentence_single_probe_limit then
                begin
                    Break;
                end;

                single_text := Trim(single_results[single_idx].text);
                if (single_text = '') or (single_results[single_idx].comment <> '') or
                    (get_candidate_text_unit_count(single_text) <> 1) then
                begin
                    Continue;
                end;

                if single_results[single_idx].has_dict_weight then
                begin
                    weight_value := single_results[single_idx].dict_weight;
                end
                else
                begin
                    weight_value := single_results[single_idx].score;
                end;

                if not is_extended_long_sentence_allowed_single_char_candidate_local(
                    syllable_text, single_text, weight_value) then
                begin
                    Continue;
                end;

                if weight_value > out_weight then
                begin
                    out_text := single_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_preferred_exact_phrase_bonus_local(const query_key: string;
            const candidate_text: string): Integer; forward;

        function try_get_best_two_syllable_exact_phrase_local(
            const start_idx: Integer; out best_text: string;
            out best_weight: Integer): Boolean;
        const
            c_two_syllable_exact_probe_limit = 6;
        var
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
        begin
            Result := False;
            best_text := '';
            best_weight := 0;
            if (start_idx < 0) or (start_idx + 1 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(exact_syllables[start_idx].text) +
                normalize_pinyin_text(exact_syllables[start_idx + 1].text);
            if query_key = '' then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(query_key, exact_results) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_results) do
            begin
                if exact_idx >= c_two_syllable_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[exact_idx].source = cs_user) or
                    (not exact_results[exact_idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[exact_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[exact_idx]) or
                    is_runtime_redup_candidate(exact_results[exact_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[exact_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 2 then
                begin
                    Continue;
                end;

                if exact_results[exact_idx].has_dict_weight then
                begin
                    weight_value := exact_results[exact_idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_results[exact_idx].score;
                end;

                Inc(weight_value, get_preferred_exact_phrase_bonus_local(
                    query_key, candidate_text));

                if (not Result) or (weight_value > best_weight) then
                begin
                    best_text := candidate_text;
                    best_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_preferred_exact_phrase_bonus_local(const query_key: string;
            const candidate_text: string): Integer;
        begin
            Result := 0;
        end;

        function try_get_best_three_syllable_exact_phrase_local(
            const start_idx: Integer; out best_text: string;
            out best_weight: Integer): Boolean;
        const
            c_three_syllable_exact_probe_limit = 8;
        var
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
            nested_text: string;
            nested_weight: Integer;
            resolved_segments: Integer;
            resolved_anchor_units: Integer;
            resolved_has_anchor: Boolean;
            resolved_path: string;
            extended_text: string;
            extended_weight: Integer;
        begin
            Result := False;
            best_text := '';
            best_weight := 0;
            if (start_idx < 0) or (start_idx + 2 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(exact_syllables[start_idx].text) +
                normalize_pinyin_text(exact_syllables[start_idx + 1].text) +
                normalize_pinyin_text(exact_syllables[start_idx + 2].text);
            if query_key = '' then
            begin
                Exit;
            end;

            if try_resolve_best_exact_subspan_local(start_idx, 3, nested_text,
                nested_weight, resolved_segments, resolved_path,
                resolved_has_anchor, resolved_anchor_units) and
                (get_candidate_text_unit_count(Trim(nested_text)) = 3) then
            begin
                best_text := Trim(nested_text);
                best_weight := nested_weight;
                if resolved_has_anchor then
                begin
                    Inc(best_weight, resolved_anchor_units * 192);
                end;
                if resolved_segments > 1 then
                begin
                    Dec(best_weight, (resolved_segments - 1) * 96);
                end;
                Inc(best_weight, get_preferred_exact_phrase_bonus_local(
                    query_key, best_text));
                Result := True;
            end;

            if try_get_best_context_extended_prefix_exact_phrase_local(
                start_idx, 3, extended_text, extended_weight) and
                ((not Result) or (extended_weight > best_weight)) then
            begin
                best_text := extended_text;
                best_weight := extended_weight;
                Result := True;
            end;

            if not dictionary_exact_lookup_cached(query_key, exact_results) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_results) do
            begin
                if exact_idx >= c_three_syllable_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (exact_results[exact_idx].source = cs_user) or
                    (not exact_results[exact_idx].has_dict_weight) or
                    is_runtime_chain_candidate(exact_results[exact_idx]) or
                    is_runtime_common_pattern_candidate(exact_results[exact_idx]) or
                    is_runtime_redup_candidate(exact_results[exact_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[exact_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 3 then
                begin
                    Continue;
                end;

                if exact_results[exact_idx].has_dict_weight then
                begin
                    weight_value := exact_results[exact_idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_results[exact_idx].score;
                end;

                Inc(weight_value, get_preferred_exact_phrase_bonus_local(
                    query_key, candidate_text));

                if (not Result) or (weight_value > best_weight) then
                begin
                    best_text := candidate_text;
                    best_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_longer_exact_prefix_penalty_local(const segment_start: Integer;
            const segment_syllables: Integer; const candidate_text: string;
            const candidate_weight: Integer): Integer;
        const
            c_probe_limit = 6;
        var
            longer_len: Integer;
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            longer_text: string;
            longer_units: Integer;
            candidate_units: Integer;
            weight_value: Integer;
            best_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables < 2) or
                (segment_start < 0) or
                (segment_start + segment_syllables >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units < 2 then
            begin
                Exit;
            end;

            best_weight := Low(Integer);
            for longer_len := segment_syllables + 1 to
                Min(c_long_sentence_max_segment_len, Length(exact_syllables) - segment_start) do
            begin
                query_key := build_exact_syllable_text(segment_start, longer_len);
                if (query_key = '') or
                    (not dictionary_exact_lookup_cached(query_key, exact_results)) then
                begin
                    Continue;
                end;

                for exact_idx := 0 to High(exact_results) do
                begin
                    if exact_idx >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results[exact_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if (exact_results[exact_idx].source = cs_user) or
                        (not exact_results[exact_idx].has_dict_weight) or
                        is_runtime_chain_candidate(exact_results[exact_idx]) or
                        is_runtime_common_pattern_candidate(exact_results[exact_idx]) or
                        is_runtime_redup_candidate(exact_results[exact_idx]) then
                    begin
                        Continue;
                    end;

                    longer_text := Trim(exact_results[exact_idx].text);
                    longer_units := get_candidate_text_unit_count(longer_text);
                    if (longer_units <= candidate_units) or
                        (Copy(longer_text, 1, Length(candidate_text)) <> candidate_text) then
                    begin
                        Continue;
                    end;

                    if exact_results[exact_idx].has_dict_weight then
                    begin
                        weight_value := exact_results[exact_idx].dict_weight;
                    end
                    else
                    begin
                        weight_value := exact_results[exact_idx].score;
                    end;

                    if weight_value > best_weight then
                    begin
                        best_weight := weight_value;
                    end;
                end;
            end;

            if best_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            if best_weight >= candidate_weight + 80 then
            begin
                Exit(920);
            end
            else if best_weight >= candidate_weight - 20 then
            begin
                Exit(680);
            end
            else if best_weight >= candidate_weight - 160 then
            begin
                Exit(420);
            end;
        end;

        function is_strong_sentence_boundary_single_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable: string;
        begin
            normalized_syllable := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable = 'shi') or
                (normalized_syllable = 'de') or
                (normalized_syllable = 'ba') or
                (normalized_syllable = 'zai') or
                (normalized_syllable = 'liao') or
                (normalized_syllable = 'dan') or
                (normalized_syllable = 'sui') or
                (normalized_syllable = 'er') or
                (normalized_syllable = 'que') or
                (normalized_syllable = 'bing') or
                (normalized_syllable = 'qie') or
                (normalized_syllable = 'yu');
        end;

        function try_get_best_sentence_phrase_weight_local(const query_key: string;
            const min_units: Integer; out best_text: string;
            out best_weight: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
            lookup_ok: Boolean;
        begin
            Result := False;
            best_text := '';
            best_weight := 0;
            if query_key = '' then
            begin
                Exit;
            end;

            { This helper scores dictionary phrase evidence for an already
              parsed full-pinyin span. Prefix, jianpin and typo expansion
              cannot produce stronger exact evidence here and made every
              missing lattice span run the full short-query pipeline. }
            lookup_ok := dictionary_exact_lookup_cached(query_key,
                local_results);
            if not lookup_ok then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (local_results[local_idx].source = cs_user) or
                    (not local_results[local_idx].has_dict_weight) or
                    is_runtime_chain_candidate(local_results[local_idx]) or
                    is_runtime_common_pattern_candidate(local_results[local_idx]) or
                    is_runtime_redup_candidate(local_results[local_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[local_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units < min_units then
                begin
                    Continue;
                end;

                if local_results[local_idx].has_dict_weight then
                begin
                    weight_value := local_results[local_idx].dict_weight;
                end
                else
                begin
                    weight_value := local_results[local_idx].score;
                end;

                Inc(weight_value, get_preferred_exact_phrase_bonus_local(
                    query_key, candidate_text));

                if (not Result) or (weight_value > best_weight) then
                begin
                    best_text := candidate_text;
                    best_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_single_plus_following_phrase_penalty_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            single_text: string;
            single_weight: Integer;
            follow_len: Integer;
            query_key: string;
            follow_text: string;
            best_follow_weight: Integer;
            combined_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or
                (get_candidate_text_unit_count(candidate_text) <> 2) or
                (segment_start < 0) or
                (segment_start + 2 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            if not try_get_best_long_sentence_single_char_local(
                exact_syllables[segment_start].text, single_text, single_weight) then
            begin
                Exit;
            end;

            best_follow_weight := Low(Integer);
            for follow_len := 2 to
                Min(c_long_sentence_max_segment_len, Length(exact_syllables) - (segment_start + 1)) do
            begin
                query_key := build_exact_syllable_text(segment_start + 1, follow_len);
                if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                    follow_text, best_follow_weight) then
                begin
                    Continue;
                end;
            end;

            if best_follow_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            combined_weight := single_weight + best_follow_weight;
            if is_strong_sentence_boundary_single_local(
                exact_syllables[segment_start].text) then
            begin
                if combined_weight >= candidate_weight + 240 then
                begin
                    Exit(2400);
                end
                else if combined_weight >= candidate_weight + 80 then
                begin
                    Exit(1800);
                end
                else if combined_weight >= candidate_weight - 120 then
                begin
                    Exit(1200);
                end;
            end;

            if combined_weight >= candidate_weight + 520 then
            begin
                Exit(880);
            end
            else if combined_weight >= candidate_weight + 320 then
            begin
                Exit(640);
            end
            else if combined_weight >= candidate_weight + 180 then
            begin
                Exit(420);
            end;
        end;

        function get_preferred_phrase_split_penalty_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            candidate_units: Integer;
            split_pos: Integer;
            left_query: string;
            right_query: string;
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            combined_text: string;
            combined_weight: Integer;
            best_combined_weight: Integer;
            left_bonus: Integer;
            right_bonus: Integer;
        begin
            Result := 0;
            if (segment_syllables < 4) or (segment_start < 0) or
                (segment_start + segment_syllables > Length(exact_syllables)) then
            begin
                Exit;
            end;

            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <> segment_syllables then
            begin
                Exit;
            end;

            best_combined_weight := Low(Integer);
            for split_pos := 2 to segment_syllables - 2 do
            begin
                left_query := build_exact_syllable_text(segment_start, split_pos);
                right_query := build_exact_syllable_text(segment_start + split_pos,
                    segment_syllables - split_pos);
                if (left_query = '') or (right_query = '') then
                begin
                    Continue;
                end;

                if not try_get_best_sentence_phrase_weight_local(left_query, 2,
                    left_text, left_weight) then
                begin
                    Continue;
                end;
                if not try_get_best_sentence_phrase_weight_local(right_query, 2,
                    right_text, right_weight) then
                begin
                    Continue;
                end;

                left_bonus := get_preferred_exact_phrase_bonus_local(left_query, left_text);
                right_bonus := get_preferred_exact_phrase_bonus_local(right_query, right_text);
                if (left_bonus <= 0) and (right_bonus <= 0) then
                begin
                    Continue;
                end;

                combined_text := left_text + right_text;
                if SameText(combined_text, candidate_text) then
                begin
                    Continue;
                end;

                combined_weight := left_weight + right_weight;
                if combined_weight > best_combined_weight then
                begin
                    best_combined_weight := combined_weight;
                end;
            end;

            if best_combined_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            if best_combined_weight >= candidate_weight + 320 then
            begin
                Exit(2200);
            end
            else if best_combined_weight >= candidate_weight + 140 then
            begin
                Exit(1400);
            end
            else if best_combined_weight >= candidate_weight - 40 then
            begin
                Exit(780);
            end;
        end;

        function get_preferred_phrase_plus_boundary_penalty_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            phrase_text: string;
            phrase_weight: Integer;
            boundary_text: string;
            boundary_weight: Integer;
            combined_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or (segment_start < 0) or
                (segment_start + 2 >= Length(exact_syllables)) or
                (not is_strong_sentence_boundary_single_local(
                exact_syllables[segment_start + 2].text)) then
            begin
                Exit;
            end;

            if not try_get_best_two_syllable_exact_phrase_local(segment_start,
                phrase_text, phrase_weight) then
            begin
                Exit;
            end;
            if SameText(phrase_text, candidate_text) then
            begin
                Exit;
            end;

            if not try_get_best_long_sentence_single_char_local(
                exact_syllables[segment_start + 2].text,
                boundary_text, boundary_weight) then
            begin
                Exit;
            end;

            combined_weight := phrase_weight + boundary_weight;
            if combined_weight >= candidate_weight + 220 then
            begin
                Exit(1800);
            end
            else if combined_weight >= candidate_weight + 80 then
            begin
                Exit(1200);
            end
            else if combined_weight >= candidate_weight - 80 then
            begin
                Exit(720);
            end;
        end;

        function try_get_best_prev_phrase_extension_local(
            const prev_pinyin_text: string; const prev_text: string;
            const bridge_syllable_text: string; out out_text: string;
            out out_weight: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            query_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            target_units: Integer;
            weight_value: Integer;
            lookup_ok: Boolean;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (prev_pinyin_text = '') or (prev_text = '') or
                (bridge_syllable_text = '') then
            begin
                Exit;
            end;

            target_units := get_candidate_text_unit_count(prev_text) + 1;
            if target_units <= 1 then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(prev_pinyin_text + bridge_syllable_text);
            if query_key = '' then
            begin
                Exit;
            end;

            lookup_ok := dictionary_exact_lookup_cached(query_key,
                local_results);
            if not lookup_ok then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if (local_results[local_idx].source = cs_user) or
                    (not local_results[local_idx].has_dict_weight) or
                    is_runtime_chain_candidate(local_results[local_idx]) or
                    is_runtime_common_pattern_candidate(local_results[local_idx]) or
                    is_runtime_redup_candidate(local_results[local_idx]) then
                begin
                    Continue;
                end;

                candidate_text := Trim(local_results[local_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if (candidate_units <> target_units) or
                    (Copy(candidate_text, 1, Length(prev_text)) <> prev_text) then
                begin
                    Continue;
                end;

                if local_results[local_idx].has_dict_weight then
                begin
                    weight_value := local_results[local_idx].dict_weight;
                end
                else
                begin
                    weight_value := local_results[local_idx].score;
                end;

                if (not Result) or (weight_value > out_weight) then
                begin
                    out_text := candidate_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;
        end;

        function get_prev_phrase_plus_boundary_single_penalty_local(
            const prev_pinyin_text: string; const prev_text: string;
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string; const candidate_weight: Integer): Integer;
        var
            extend_text: string;
            extend_weight: Integer;
            boundary_text: string;
            boundary_weight: Integer;
            follow_len: Integer;
            query_key: string;
            follow_text: string;
            best_follow_weight: Integer;
            combined_weight: Integer;
        begin
            Result := 0;
            if (segment_syllables <> 2) or
                (segment_start <= 0) or
                (segment_start + 1 >= Length(exact_syllables)) or
                (segment_start + 2 >= Length(exact_syllables)) or
                (prev_pinyin_text = '') or (prev_text = '') or
                (not is_strong_sentence_boundary_single_local(
                exact_syllables[segment_start + 1].text)) then
            begin
                Exit;
            end;

            if not try_get_best_prev_phrase_extension_local(prev_pinyin_text,
                prev_text, exact_syllables[segment_start].text,
                extend_text, extend_weight) then
            begin
                Exit;
            end;

            if not try_get_best_long_sentence_single_char_local(
                exact_syllables[segment_start + 1].text,
                boundary_text, boundary_weight) then
            begin
                Exit;
            end;

            best_follow_weight := Low(Integer);
            for follow_len := 2 to
                Min(c_long_sentence_max_segment_len,
                Length(exact_syllables) - (segment_start + 2)) do
            begin
                query_key := build_exact_syllable_text(segment_start + 2, follow_len);
                if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                    follow_text, combined_weight) then
                begin
                    Continue;
                end;
                if combined_weight > best_follow_weight then
                begin
                    best_follow_weight := combined_weight;
                end;
            end;

            if best_follow_weight <= Low(Integer) div 4 then
            begin
                Exit;
            end;

            combined_weight := extend_weight + boundary_weight + best_follow_weight;
            if combined_weight >= candidate_weight + 360 then
            begin
                Exit(2600);
            end
            else if combined_weight >= candidate_weight + 180 then
            begin
                Exit(2000);
            end
            else if combined_weight >= candidate_weight then
            begin
                Exit(1400);
            end;
        end;

        function try_build_two_single_rescue_segment_local(
            const segment_start: Integer; out out_text: string;
            out out_weight: Integer; out out_rank_bonus: Integer): Boolean;
            function try_get_best_two_syllable_exact_sentence_segment_local(
                const start_idx: Integer; out best_text: string;
                out best_weight: Integer): Boolean;
            const
                c_two_syllable_exact_probe_limit = 6;
            var
                query_key: string;
                exact_results: TncCandidateList;
                exact_idx: Integer;
                candidate_text: string;
                candidate_units: Integer;
                weight_value: Integer;
            begin
                Result := False;
                best_text := '';
                best_weight := 0;
                if (start_idx < 0) or (start_idx + 1 >= Length(exact_syllables)) then
                begin
                    Exit;
                end;

                query_key := normalize_pinyin_text(exact_syllables[start_idx].text) +
                    normalize_pinyin_text(exact_syllables[start_idx + 1].text);
                if (query_key = '') or
                    (not dictionary_exact_lookup_cached(query_key, exact_results)) then
                begin
                    Exit;
                end;

                for exact_idx := 0 to High(exact_results) do
                begin
                    if exact_idx >= c_two_syllable_exact_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_results[exact_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if (exact_results[exact_idx].source = cs_user) or
                        (not exact_results[exact_idx].has_dict_weight) or
                        is_runtime_chain_candidate(exact_results[exact_idx]) or
                        is_runtime_common_pattern_candidate(exact_results[exact_idx]) or
                        is_runtime_redup_candidate(exact_results[exact_idx]) then
                    begin
                        Continue;
                    end;

                    candidate_text := Trim(exact_results[exact_idx].text);
                    candidate_units := get_candidate_text_unit_count(candidate_text);
                    if candidate_units <> 2 then
                    begin
                        Continue;
                    end;

                    if exact_results[exact_idx].has_dict_weight then
                    begin
                        weight_value := exact_results[exact_idx].dict_weight;
                    end
                    else
                    begin
                        weight_value := exact_results[exact_idx].score;
                    end;

                    if (not Result) or (weight_value > best_weight) then
                    begin
                        best_text := candidate_text;
                        best_weight := weight_value;
                        Result := True;
                    end;
                end;
            end;

            function has_strong_two_syllable_exact_sentence_segment_local(
                const start_idx: Integer): Boolean;
            var
                best_text: string;
                best_weight: Integer;
            begin
                Result := try_get_best_two_syllable_exact_sentence_segment_local(
                    start_idx, best_text, best_weight);
            end;

            function should_skip_two_single_rescue_segment_local(
                const start_idx: Integer): Boolean;
            var
                left_fixed_local: string;
                right_fixed_local: string;
            begin
                Result := False;
                if (start_idx < 0) or (start_idx + 1 >= Length(exact_syllables)) then
            begin
                Exit;
            end;

            if has_strong_two_syllable_exact_sentence_segment_local(segment_start) then
            begin
                Exit(True);
            end;

            left_fixed_local := get_fixed_long_sentence_single_char_local(
                exact_syllables[start_idx].text);
            right_fixed_local := get_fixed_long_sentence_single_char_local(
                exact_syllables[start_idx + 1].text);

                if (left_fixed_local <> '') and (right_fixed_local = '') and
                    (start_idx + 2 < Length(exact_syllables)) and
                    has_strong_two_syllable_exact_sentence_segment_local(start_idx + 1) then
                begin
                    Exit(True);
                end;
            end;
        var
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            left_fixed: string;
            right_fixed: string;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank_bonus := 0;
            if (segment_start < 0) or (segment_start + 1 >= Length(exact_syllables)) or
                (Length(exact_syllables) < c_long_sentence_full_path_min_syllables) then
            begin
                Exit;
            end;

            left_fixed := get_fixed_long_sentence_single_char_local(
                exact_syllables[segment_start].text);
            right_fixed := get_fixed_long_sentence_single_char_local(
                exact_syllables[segment_start + 1].text);
            if (left_fixed = '') and (right_fixed = '') then
            begin
                Exit;
            end;

            if should_skip_two_single_rescue_segment_local(segment_start) then
            begin
                Exit;
            end;

            if (not try_get_best_long_sentence_single_char_local(
                exact_syllables[segment_start].text, left_text, left_weight)) or
                (not try_get_best_long_sentence_single_char_local(
                exact_syllables[segment_start + 1].text, right_text, right_weight)) then
            begin
                Exit;
            end;

            out_text := left_text + right_text;
            out_weight := left_weight + right_weight;
            out_rank_bonus := c_long_sentence_top_exact_bonus + 160;
            Result := get_candidate_text_unit_count(out_text) = 2;
        end;

        function try_build_preferred_split_exact_segment_local(
            const segment_start: Integer; const segment_syllables: Integer;
            out out_text: string; out out_weight: Integer;
            out out_rank_bonus: Integer): Boolean;
            function try_get_segment_piece_local(const start_idx: Integer;
                const syllable_count: Integer; out piece_text: string;
                out piece_weight: Integer; out anchored: Boolean): Boolean;
            var
                query_key: string;
            begin
                Result := False;
                piece_text := '';
                piece_weight := 0;
                anchored := False;
                if (syllable_count <= 0) or (start_idx < 0) or
                    (start_idx + syllable_count > Length(exact_syllables)) then
                begin
                    Exit;
                end;

                if syllable_count = 1 then
                begin
                    if not try_get_best_long_sentence_single_char_local(
                        exact_syllables[start_idx].text, piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                    anchored := is_strong_sentence_boundary_single_local(
                        exact_syllables[start_idx].text);
                    Exit(True);
                end;

                query_key := build_exact_syllable_text(start_idx, syllable_count);
                if query_key = '' then
                begin
                    Exit;
                end;

                if syllable_count = 2 then
                begin
                    if not try_get_best_two_syllable_exact_phrase_local(start_idx,
                        piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                end
                else if syllable_count = 3 then
                begin
                    if not try_get_best_three_syllable_exact_phrase_local(start_idx,
                        piece_text, piece_weight) then
                    begin
                        if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                            piece_text, piece_weight) then
                        begin
                            Exit;
                        end;
                    end;
                end
                else
                begin
                    if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                        piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                end;

                anchored := get_preferred_exact_phrase_bonus_local(query_key,
                    piece_text) > 0;
                Result := True;
            end;
        var
            best_text_local: string;
            best_weight_local: Integer;
            best_rank_bonus_local: Integer;
            query_key: string;
            direct_text: string;
            direct_weight: Integer;
            split_pos: Integer;
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            left_anchor: Boolean;
            right_anchor: Boolean;
            combined_text: string;
            combined_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank_bonus := 0;
            if (segment_syllables < 3) or (segment_syllables > 4) or
                (segment_start < 0) or
                (segment_start + segment_syllables > Length(exact_syllables)) then
            begin
                Exit;
            end;

            best_text_local := '';
            best_weight_local := 0;
            best_rank_bonus_local := 0;

            if segment_syllables = 3 then
            begin
                query_key := build_exact_syllable_text(segment_start, segment_syllables);
                if try_get_best_three_syllable_exact_phrase_local(segment_start,
                    direct_text, direct_weight) and
                    (get_preferred_exact_phrase_bonus_local(query_key, direct_text) > 0) then
                begin
                    best_text_local := direct_text;
                    best_weight_local := direct_weight;
                    best_rank_bonus_local := 2200;
                    Result := True;
                end;
            end;

            for split_pos := 1 to segment_syllables - 1 do
            begin
                if not try_get_segment_piece_local(segment_start, split_pos,
                    left_text, left_weight, left_anchor) then
                begin
                    Continue;
                end;
                if not try_get_segment_piece_local(segment_start + split_pos,
                    segment_syllables - split_pos, right_text, right_weight,
                    right_anchor) then
                begin
                    Continue;
                end;
                if (not left_anchor) and (not right_anchor) then
                begin
                    Continue;
                end;

                combined_text := left_text + right_text;
                combined_weight := left_weight + right_weight;
                if (not Result) or (combined_weight > best_weight_local) then
                begin
                    best_text_local := combined_text;
                    best_weight_local := combined_weight;
                    best_rank_bonus_local := 2400;
                    Result := True;
                end;
            end;

            if not Result then
            begin
                Exit;
            end;

            out_text := best_text_local;
            out_weight := best_weight_local;
            out_rank_bonus := best_rank_bonus_local;
        end;

        function try_get_best_resolved_exact_segment_candidate_local(
            const segment_start: Integer; const segment_syllables: Integer;
            out out_text: string; out out_weight: Integer;
            out out_rank_bonus: Integer): Boolean;
        var
            query_key: string;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            candidate_rank_bonus_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank_bonus := 0;
            if (segment_start < 0) or (segment_syllables <= 0) or
                (segment_start + segment_syllables > Length(exact_syllables)) then
            begin
                Exit;
            end;

            if segment_syllables = 1 then
            begin
                if try_get_best_long_sentence_single_char_local(
                    exact_syllables[segment_start].text,
                    candidate_text_local, candidate_weight_local) then
                begin
                    candidate_rank_bonus_local := c_long_sentence_top_exact_bonus;
                    if is_strong_sentence_boundary_single_local(
                        exact_syllables[segment_start].text) then
                    begin
                        Inc(candidate_rank_bonus_local, 260);
                    end;
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                    out_rank_bonus := candidate_rank_bonus_local;
                    Exit(True);
                end;
                Exit(False);
            end;

            if segment_syllables = 2 then
            begin
                if try_get_best_two_syllable_exact_phrase_local(segment_start,
                    candidate_text_local, candidate_weight_local) then
                begin
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                    out_rank_bonus := c_long_sentence_top_exact_bonus + 240;
                    Exit(True);
                end;
                Exit(False);
            end;

            if try_build_preferred_split_exact_segment_local(segment_start,
                segment_syllables, candidate_text_local, candidate_weight_local,
                candidate_rank_bonus_local) then
            begin
                out_text := candidate_text_local;
                out_weight := candidate_weight_local;
                out_rank_bonus := candidate_rank_bonus_local;
                Exit(True);
            end;

            if segment_syllables = 3 then
            begin
                if try_get_best_three_syllable_exact_phrase_local(segment_start,
                    candidate_text_local, candidate_weight_local) then
                begin
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                    out_rank_bonus := c_long_sentence_top_exact_bonus + 220;
                    Exit(True);
                end;
            end;

            query_key := build_exact_syllable_text(segment_start, segment_syllables);
            if (query_key <> '') and try_get_best_sentence_phrase_weight_local(
                query_key, 2, candidate_text_local, candidate_weight_local) then
            begin
                out_text := candidate_text_local;
                out_weight := candidate_weight_local;
                out_rank_bonus := c_long_sentence_top_exact_bonus + 160;
                Exit(True);
            end;
        end;

        function try_build_preferred_sentence_rescue_candidate_local(
            out out_candidate: TncCandidate): Boolean;
        const
            c_rescue_max_piece_syllables = 6;
            c_rescue_anchor_bonus = 960;
        type
            TPreferredSentenceState = record
                valid: Boolean;
                text: string;
                score: Integer;
                anchor_count: Integer;
            end;
            TPreferredSentenceStateArray = array of TPreferredSentenceState;
            function try_get_piece_local(const start_idx: Integer;
                const syllable_count: Integer; out piece_text: string;
                out piece_weight: Integer; out anchored: Boolean): Boolean;
            var
                query_key: string;
            begin
                Result := False;
                piece_text := '';
                piece_weight := 0;
                anchored := False;
                if (syllable_count <= 0) or (start_idx < 0) or
                    (start_idx + syllable_count > Length(exact_syllables)) then
                begin
                    Exit;
                end;

                if syllable_count = 1 then
                begin
                    if not try_get_best_long_sentence_single_char_local(
                        exact_syllables[start_idx].text, piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                    anchored := is_strong_sentence_boundary_single_local(
                        exact_syllables[start_idx].text);
                    Exit(True);
                end;

                query_key := build_exact_syllable_text(start_idx, syllable_count);
                if query_key = '' then
                begin
                    Exit;
                end;

                if syllable_count = 2 then
                begin
                    if not try_get_best_two_syllable_exact_phrase_local(start_idx,
                        piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                end
                else if syllable_count = 3 then
                begin
                    if not try_get_best_three_syllable_exact_phrase_local(start_idx,
                        piece_text, piece_weight) then
                    begin
                        if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                            piece_text, piece_weight) then
                        begin
                            Exit;
                        end;
                    end;
                end
                else
                begin
                    if not try_get_best_sentence_phrase_weight_local(query_key, 2,
                        piece_text, piece_weight) then
                    begin
                        Exit;
                    end;
                end;

                anchored := get_preferred_exact_phrase_bonus_local(query_key,
                    piece_text) > 0;
                Result := True;
            end;
        var
            states: TPreferredSentenceStateArray;
            pos: Integer;
            next_pos: Integer;
            piece_len: Integer;
            piece_text: string;
            piece_weight: Integer;
            piece_anchor: Boolean;
            next_score: Integer;
            next_text: string;
            total_syllables: Integer;
        begin
            Result := False;
            FillChar(out_candidate, SizeOf(out_candidate), 0);
            total_syllables := Length(exact_syllables);
            if total_syllables < 3 then
            begin
                Exit;
            end;

            SetLength(states, total_syllables + 1);
            states[0].valid := True;
            states[0].text := '';
            states[0].score := 0;
            states[0].anchor_count := 0;

            for pos := 0 to total_syllables - 1 do
            begin
                if not states[pos].valid then
                begin
                    Continue;
                end;

                for piece_len := 1 to Min(c_rescue_max_piece_syllables,
                    total_syllables - pos) do
                begin
                    if not try_get_piece_local(pos, piece_len, piece_text,
                        piece_weight, piece_anchor) then
                    begin
                        Continue;
                    end;

                    next_pos := pos + piece_len;
                    next_score := states[pos].score + piece_weight;
                    if piece_anchor then
                    begin
                        Inc(next_score, c_rescue_anchor_bonus);
                    end;
                    if piece_len = 1 then
                    begin
                        Dec(next_score, c_long_sentence_single_penalty);
                    end
                    else
                    begin
                        Inc(next_score,
                            c_long_sentence_multi_bonus + (piece_len * 24));
                    end;
                    if next_pos = total_syllables then
                    begin
                        Inc(next_score, c_long_sentence_completion_bonus);
                    end
                    else
                    begin
                        Inc(next_score, c_long_sentence_continuation_bonus);
                    end;

                    next_text := states[pos].text + piece_text;
                    if (not states[next_pos].valid) or
                        (next_score > states[next_pos].score) then
                    begin
                        states[next_pos].valid := True;
                        states[next_pos].text := next_text;
                        states[next_pos].score := next_score;
                        states[next_pos].anchor_count := states[pos].anchor_count +
                            Ord(piece_anchor);
                    end;
                end;
            end;

            if (not states[total_syllables].valid) or
                (states[total_syllables].anchor_count <= 0) then
            begin
                Exit;
            end;

            out_candidate.text := states[total_syllables].text;
            out_candidate.score := states[total_syllables].score;
            out_candidate.source := cs_rule;
            out_candidate.comment := '';
            Result := Trim(out_candidate.text) <> '';
        end;

        function is_predictive_tail_single_char_text_codepoint_local(
            const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $4E00, $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
                $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $628A, $88AB, $8BA9,
                $7ED9, $5411, $8DDF, $540C, $4ECE, $4E0E, $5C31, $4E5F, $90FD, $8FD8,
                $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2, $5C06, $5230, $4E86,
                $7740, $8FC7:
                    Result := True;
            end;
        end;

        function is_predictive_bridge_single_char_text_codepoint_local(
            const value: string): Boolean;
        begin
            Result := False;
            if Length(value) <> 1 then
            begin
                Exit;
            end;

            case Ord(value[1]) of
                $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765, $5728,
                $662F, $6709, $8981, $60F3, $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9,
                $5411, $8DDF, $540C, $4ECE, $4E0E, $5C31, $4E5F, $90FD, $8FD8, $53C8,
                $518D, $4E0D, $5F88, $592A, $6700, $5DF2, $5C06, $5230:
                    Result := True;
            end;
        end;

        function get_candidate_effective_weight_local(const local_candidate: TncCandidate): Integer;
        var
            local_units: Integer;
        begin
            Result := local_candidate.score;
            if Result <= 0 then
            begin
                if ((not is_fuzzy_pinyin_active) or
                    (local_candidate.fuzzy_cost <= 0)) and
                    local_candidate.has_dict_weight then
                begin
                    Result := local_candidate.dict_weight;
                end
                else
                begin
                    Result := 0;
                end;
            end
            else if ((not is_fuzzy_pinyin_active) or
                (local_candidate.fuzzy_cost <= 0)) and
                local_candidate.has_dict_weight then
            begin
                Result := Max(Result, local_candidate.dict_weight div 2);
            end;

            local_units := get_candidate_text_unit_count(Trim(local_candidate.text));
            case local_units of
                1:
                    if Result > 180 then
                    begin
                        Result := 180 + ((Result - 180) div 12);
                    end;
                2:
                    if Result > 240 then
                    begin
                        Result := 240 + ((Result - 240) div 10);
                    end;
            end;
        end;

        function get_candidate_raw_weight_local(const local_candidate: TncCandidate): Integer;
        begin
            if local_candidate.has_dict_weight then
            begin
                Result := local_candidate.dict_weight;
            end
            else
            begin
                Result := local_candidate.score;
            end;
        end;

        function exact_first_text_unit_local(const value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[1];
            end;
        end;

        function exact_last_text_unit_local(const value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[Length(trimmed_value)];
            end;
        end;

        function exact_unit_code_local(const value: string;
            const code_value: Integer): Boolean;
        begin
            Result := (Length(value) = 1) and (Ord(value[1]) = code_value);
        end;

        function is_exact_predicate_head_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $505A, $5230, $53BB, $544A, $547C, $56DE, $5BFB, $5F00,
                $60F3, $6210, $6253, $627E, $62FF, $6301, $63A5, $63D0,
                $6539, $653E, $6765, $7528, $770B, $7740, $7814, $79BB,
                $7EE7, $7ED9, $80E1, $89C1, $8BA9, $8BB2, $8BB0, $8BF4,
                $8D70, $8DD1, $8F93, $8F6C, $8FC7, $9001, $9009, $95EE,
                $5199, $5C06, $964D, $9610, $9996, $6572, $653B, $878D,
                $642C, $4FEE, $963B, $8054, $88AB, $5FD0, $76B1, $8D62,
                $9501, $5229, $7A33, $8131, $6563, $5BA3, $5E94, $79F0,
                $8EBA, $70B9, $8FC5, $9010, $6DF1, $8DF3, $63E1, $6D12,
                $8FFD:
                    Result := True;
            end;
        end;

        function is_exact_location_head_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4E0A, $4E0B, $4E2D, $4E2A, $4E9B, $4E1C, $4EF6, $4EEC,
                $4F4D, $5143, $5165, $5185, $5317, $5357, $524D, $540E,
                $56FD, $5730, $573A, $57CE, $5916, $5B66, $5BB6, $5C42,
                $5C4B, $5DE5, $5E02, $5E74, $65B9, $65F6, $6708, $672C,
                $6761, $679A, $6B21, $7247, $79CD, $7C7B, $7EC4, $8005,
                $897F, $91CC, $95F4, $9879:
                    Result := True;
            end;
        end;

        function is_exact_nominal_head_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4EBA, $4E8B, $4EF6, $516C, $5173, $5185, $5206, $529F,
                $539F, $56FE, $5730, $57CE, $58F0, $5B66, $5BB6, $5DE5,
                $5E02, $5E74, $5F62, $60C5, $6587, $65B9, $65F6, $6570,
                $670D, $672C, $673A, $6837, $6A21, $6B21, $6CD5, $7269,
                $7406, $7528, $7535, $76EE, $7CFB, $7ED3, $7F51, $80FD,
                $8272, $89C4, $8BA1, $8BED, $8D28, $8DEF, $8F6F, $95EE,
                $9762, $9879, $98CE:
                    Result := True;
            end;
        end;

        function is_exact_bound_yu_left_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4F4D, $5173, $5BF9, $7528, $5C5E, $7B49, $6E90, $57FA,
                $5904, $51FA, $6025, $7126, $9274, $81F3, $4FBF, $5229,
                $7531, $9650:
                    Result := True;
            end;
        end;

        function is_exact_predicate_yu_left_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4E50, $4FBF, $5229, $5584, $5FD9, $6025, $75B2:
                    Result := True;
            end;
        end;

        function get_exact_suffix_first_unit_for_context_local(
            const suffix_pos: Integer): string;
        const
            c_probe_limit = 4;
        var
            cached_text: string;
            span_len: Integer;
            query_key: string;
            lookup_results: TncCandidateList;
            single_results: TncCandidateList;
            result_idx: Integer;
            span_text: string;
            single_key: string;
        begin
            Result := '';
            if (suffix_pos < 0) or (suffix_pos >= Length(exact_syllables)) then
            begin
                Exit;
            end;
            if (exact_suffix_first_unit_cache <> nil) and
                exact_suffix_first_unit_cache.TryGetValue(suffix_pos,
                cached_text) then
            begin
                Exit(cached_text);
            end;

            for span_len := Min(4, Length(exact_syllables) - suffix_pos) downto 2 do
            begin
                query_key := build_exact_syllable_text(suffix_pos, span_len);
                if (query_key = '') or
                    (not dictionary_exact_lookup_cached(query_key, lookup_results)) then
                begin
                    Continue;
                end;
                for result_idx := 0 to High(lookup_results) do
                begin
                    if result_idx >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(lookup_results[result_idx]) or
                        is_runtime_common_pattern_candidate(
                        lookup_results[result_idx]) or
                        is_runtime_redup_candidate(lookup_results[result_idx]) then
                    begin
                        Continue;
                    end;
                    span_text := Trim(lookup_results[result_idx].text);
                    if get_candidate_text_unit_count(span_text) <> span_len then
                    begin
                        Continue;
                    end;
                    Result := exact_first_text_unit_local(span_text);
                    Break;
                end;
                if Result <> '' then
                begin
                    Break;
                end;
            end;

            if Result = '' then
            begin
                single_key := normalize_pinyin_text(
                    exact_syllables[suffix_pos].text);
                if (single_key <> '') and (m_dictionary <> nil) then
                begin
                    m_dictionary.lookup(single_key, single_results);
                    for result_idx := 0 to High(single_results) do
                    begin
                        if result_idx >= c_probe_limit then
                        begin
                            Break;
                        end;
                        span_text := Trim(single_results[result_idx].text);
                        if (Trim(single_results[result_idx].comment) = '') and
                            (get_candidate_text_unit_count(span_text) = 1) then
                        begin
                            Result := exact_first_text_unit_local(span_text);
                            Break;
                        end;
                    end;
                end;
            end;
            if exact_suffix_first_unit_cache <> nil then
            begin
                exact_suffix_first_unit_cache.AddOrSetValue(suffix_pos, Result);
            end;
        end;

        function get_exact_yu_context_adjustment_local(
            const segment_last_syllable: string; const candidate_text: string;
            const suffix_pos: Integer): Integer;
        var
            normalized_tail_syllable: string;
            candidate_last_unit: string;
            suffix_first_unit: string;
            suffix_is_predicate: Boolean;
            suffix_is_location: Boolean;
            suffix_is_nominal: Boolean;
        begin
            Result := 0;
            normalized_tail_syllable := normalize_pinyin_text(segment_last_syllable);
            candidate_last_unit := exact_last_text_unit_local(candidate_text);
            if candidate_last_unit = '' then
            begin
                Exit;
            end;

            suffix_first_unit := get_exact_suffix_first_unit_for_context_local(
                suffix_pos);
            suffix_is_predicate := is_exact_predicate_head_unit_local(
                suffix_first_unit);
            suffix_is_location := is_exact_location_head_unit_local(
                suffix_first_unit);
            suffix_is_nominal := suffix_is_location or
                is_exact_nominal_head_unit_local(suffix_first_unit);

            if normalized_tail_syllable = 'yu' then
            begin
                if exact_unit_code_local(candidate_last_unit, $4E8E) and
                    (suffix_is_location or suffix_is_nominal) then
                begin
                    Inc(Result, 2600);
                end
                else if exact_unit_code_local(candidate_last_unit, $4E0E) and
                    suffix_is_location then
                begin
                    Dec(Result, 2600);
                end;
            end
            else if normalized_tail_syllable = 'de' then
            begin
                if exact_unit_code_local(candidate_last_unit, $5730) then
                begin
                    if suffix_is_predicate then
                    begin
                        Inc(Result, 3200);
                    end
                    else if suffix_is_nominal then
                    begin
                        Dec(Result, 4200);
                    end;
                    if suffix_first_unit = '' then
                    begin
                        Dec(Result, 1600);
                    end;
                end
                else if exact_unit_code_local(candidate_last_unit, $7684) then
                begin
                    if suffix_is_predicate then
                    begin
                        Dec(Result, 2200);
                    end
                    else if suffix_is_nominal then
                    begin
                        Inc(Result, 2600);
                    end;
                    if suffix_first_unit = '' then
                    begin
                        Inc(Result, 1400);
                    end;
                end
                else if exact_unit_code_local(candidate_last_unit, $5F97) and
                    suffix_is_predicate then
                begin
                    Inc(Result, 2200);
                end;
            end
            else if normalized_tail_syllable = 'zai' then
            begin
                if exact_unit_code_local(candidate_last_unit, $5728) and
                    (suffix_is_location or suffix_is_nominal) then
                begin
                    Inc(Result, 2200);
                end
                else if exact_unit_code_local(candidate_last_unit, $518D) and
                    suffix_is_location then
                begin
                    Dec(Result, 2200);
                end;
            end
            else if normalized_tail_syllable = 'dao' then
            begin
                if exact_unit_code_local(candidate_last_unit, $5230) and
                    (suffix_is_location or suffix_is_predicate) then
                begin
                    Inc(Result, 1800);
                end
                else if exact_unit_code_local(candidate_last_unit, $9053) and
                    suffix_is_location then
                begin
                    Dec(Result, 1800);
                end;
            end;
        end;

        function try_get_protected_exact_span_local(const span_start: Integer;
            const span_len: Integer; out out_text: string; out out_weight: Integer;
            out out_rank: Integer): Boolean;
        const
            c_probe_limit = 8;
        var
            cache_key: string;
            query_key: string;
            span_results: TncCandidateList;
            span_idx: Integer;
            span_text: string;
            span_units: Integer;
            span_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_rank := 0;
            if (span_start < 0) or (span_len < 2) or
                (span_len > c_long_sentence_protected_span_max_len) or
                (span_start + span_len > Length(exact_syllables)) then
            begin
                Exit;
            end;

            cache_key := IntToStr(span_start) + ':' + IntToStr(span_len);
            if (exact_protected_span_weight_cache <> nil) and
                exact_protected_span_weight_cache.TryGetValue(cache_key, out_weight) then
            begin
                if out_weight <= 0 then
                begin
                    Exit(False);
                end;
                if exact_protected_span_text_cache <> nil then
                begin
                    exact_protected_span_text_cache.TryGetValue(cache_key, out_text);
                end;
                if exact_protected_span_rank_cache <> nil then
                begin
                    exact_protected_span_rank_cache.TryGetValue(cache_key, out_rank);
                end;
                Exit(out_text <> '');
            end;

            query_key := build_exact_syllable_text(span_start, span_len);
            if (query_key = '') or
                (not dictionary_exact_lookup_cached(query_key, span_results)) then
            begin
                if exact_protected_span_weight_cache <> nil then
                begin
                    exact_protected_span_weight_cache.AddOrSetValue(cache_key, -1);
                end;
                Exit;
            end;

            for span_idx := 0 to High(span_results) do
            begin
                if span_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(span_results[span_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(span_results[span_idx]) or
                    is_runtime_common_pattern_candidate(span_results[span_idx]) or
                    is_runtime_redup_candidate(span_results[span_idx]) then
                begin
                    Continue;
                end;

                span_text := Trim(span_results[span_idx].text);
                span_units := get_candidate_text_unit_count(span_text);
                if span_units <> span_len then
                begin
                    Continue;
                end;

                span_weight := get_candidate_raw_weight_local(span_results[span_idx]);
                if span_weight < c_long_sentence_protected_span_min_weight then
                begin
                    Continue;
                end;

                out_text := span_text;
                out_weight := span_weight;
                out_rank := span_idx + 1;
                Result := True;
                Break;
            end;

            if exact_protected_span_weight_cache <> nil then
            begin
                if Result then
                begin
                    exact_protected_span_weight_cache.AddOrSetValue(cache_key,
                        out_weight);
                    if exact_protected_span_text_cache <> nil then
                    begin
                        exact_protected_span_text_cache.AddOrSetValue(cache_key,
                            out_text);
                    end;
                    if exact_protected_span_rank_cache <> nil then
                    begin
                        exact_protected_span_rank_cache.AddOrSetValue(cache_key,
                            out_rank);
                    end;
                end
                else
                begin
                    exact_protected_span_weight_cache.AddOrSetValue(cache_key, -1);
                end;
            end;
        end;

        function get_protected_exact_span_adjustment_local(
            const segment_start: Integer; const segment_syllables: Integer;
            const candidate_text: string): Integer;
        var
            span_start: Integer;
            span_len: Integer;
            span_end: Integer;
            segment_end: Integer;
            protected_text: string;
            protected_weight: Integer;
            protected_rank: Integer;
            overlap_penalty: Integer;
            exact_bonus: Integer;

            function is_confident_protected_exact_span_local(
                const span_len_value: Integer; const span_weight_value: Integer;
                const span_rank_value: Integer): Boolean;
            begin
                Result := False;
                if span_rank_value <= 0 then
                begin
                    Exit;
                end;

                if span_rank_value = 1 then
                begin
                    case span_len_value of
                        2:
                            Result := span_weight_value >= 650;
                        3:
                            Result := span_weight_value >= 420;
                        4:
                            Result := span_weight_value >= 360;
                    end;
                end
                else if (span_rank_value = 2) and (span_len_value >= 3) then
                begin
                    Result := span_weight_value >= 900;
                end;
            end;

            function candidate_matches_protected_overlap_local(
                const protected_start: Integer; const protected_len: Integer;
                const protected_value: string): Boolean;
            var
                candidate_units: TArray<string>;
                protected_units: TArray<string>;
                overlap_start: Integer;
                overlap_end: Integer;
                unit_pos: Integer;
            begin
                Result := False;
                candidate_units := split_text_units(Trim(candidate_text));
                protected_units := split_text_units(Trim(protected_value));
                if (Length(candidate_units) <> segment_syllables) or
                    (Length(protected_units) <> protected_len) then
                begin
                    Exit;
                end;

                overlap_start := Max(segment_start, protected_start);
                overlap_end := Min(segment_start + segment_syllables,
                    protected_start + protected_len);
                if overlap_start >= overlap_end then
                begin
                    Exit(True);
                end;

                for unit_pos := overlap_start to overlap_end - 1 do
                begin
                    if not SameText(candidate_units[unit_pos - segment_start],
                        protected_units[unit_pos - protected_start]) then
                    begin
                        Exit(False);
                    end;
                end;
                Result := True;
            end;
        begin
            Result := 0;
            if (segment_start < 0) or (segment_syllables <= 0) or
                (candidate_text = '') then
            begin
                Exit;
            end;

            segment_end := segment_start + segment_syllables;

            if (segment_syllables >= 2) and
                try_get_protected_exact_span_local(segment_start, segment_syllables,
                protected_text, protected_weight, protected_rank) and
                is_confident_protected_exact_span_local(segment_syllables,
                protected_weight, protected_rank) and
                SameText(protected_text, candidate_text) then
            begin
                exact_bonus := 180 + (segment_syllables * 90) +
                    Min(620, protected_weight div 2);
                if protected_rank > 1 then
                begin
                    Dec(exact_bonus, (protected_rank - 1) * 160);
                end;
                if exact_bonus > 0 then
                begin
                    Inc(Result, exact_bonus);
                end;
            end;

            for span_start := Max(0, segment_start -
                (c_long_sentence_protected_span_max_len - 1)) to
                Min(segment_end - 1, Length(exact_syllables) - 1) do
            begin
                for span_len := 2 to c_long_sentence_protected_span_max_len do
                begin
                    span_end := span_start + span_len;
                    if span_end > Length(exact_syllables) then
                    begin
                        Break;
                    end;
                    if (span_start = segment_start) and
                        (span_len = segment_syllables) then
                    begin
                        Continue;
                    end;
                    if (span_start >= segment_end) or (segment_start >= span_end) then
                    begin
                        Continue;
                    end;
                    if (Min(segment_end, span_end) -
                        Max(segment_start, span_start)) <= 0 then
                    begin
                        Continue;
                    end;
                    if not try_get_protected_exact_span_local(span_start, span_len,
                        protected_text, protected_weight, protected_rank) then
                    begin
                        Continue;
                    end;
                    if not is_confident_protected_exact_span_local(span_len,
                        protected_weight, protected_rank) then
                    begin
                        Continue;
                    end;
                    if SameText(protected_text, candidate_text) then
                    begin
                        Continue;
                    end;
                    if candidate_matches_protected_overlap_local(span_start,
                        span_len, protected_text) then
                    begin
                        Continue;
                    end;

                    overlap_penalty := 260 + (span_len * 110) +
                        ((Min(segment_end, span_end) -
                        Max(segment_start, span_start)) * 180) +
                        Min(1200, protected_weight);
                    if protected_rank > 1 then
                    begin
                        Dec(overlap_penalty, (protected_rank - 1) * 260);
                    end;
                    if overlap_penalty > 0 then
                    begin
                        Dec(Result, overlap_penalty);
                    end;
                end;
            end;
        end;

        function get_exact_phrase_bridge_bonus_local(const left_pinyin: string;
            const left_text: string; const right_pinyin: string;
            const right_text: string): Integer;
        var
            cache_key: string;
            combined_pinyin: string;
            combined_text: string;
            bridge_results: TncCandidateList;
            bridge_idx: Integer;
            bridge_effective_weight: Integer;
        begin
            Result := 0;
            if (Trim(left_pinyin) = '') or (Trim(left_text) = '') or
                (Trim(right_pinyin) = '') or (Trim(right_text) = '') then
            begin
                Exit;
            end;

            combined_pinyin := normalize_pinyin_text(left_pinyin + right_pinyin);
            combined_text := Trim(left_text) + Trim(right_text);
            if (combined_pinyin = '') or (combined_text = '') then
            begin
                Exit;
            end;

            cache_key := combined_pinyin + #1 + combined_text;
            if (exact_bridge_bonus_cache <> nil) and
                exact_bridge_bonus_cache.TryGetValue(cache_key, Result) then
            begin
                Exit;
            end;

            if dictionary_exact_lookup_cached(combined_pinyin, bridge_results) then
            begin
                for bridge_idx := 0 to High(bridge_results) do
                begin
                    if bridge_results[bridge_idx].comment <> '' then
                    begin
                        Continue;
                    end;
                    if not SameText(Trim(bridge_results[bridge_idx].text), combined_text) then
                    begin
                        Continue;
                    end;

                    if bridge_results[bridge_idx].source = cs_user then
                    begin
                        Result := c_long_sentence_bridge_bonus_top;
                    end
                    else
                    begin
                        bridge_effective_weight := get_candidate_effective_weight_local(
                            bridge_results[bridge_idx]);
                        if bridge_effective_weight >= 760 then
                        begin
                            Result := c_long_sentence_bridge_bonus_top;
                        end
                        else if bridge_effective_weight >= 560 then
                        begin
                            Result := c_long_sentence_bridge_bonus_high;
                        end
                        else if bridge_effective_weight >= 360 then
                        begin
                            Result := c_long_sentence_bridge_bonus_mid;
                        end
                        else
                        begin
                            Result := c_long_sentence_bridge_bonus_low;
                        end;
                    end;
                    Break;
                end;
            end;

            if exact_bridge_bonus_cache <> nil then
            begin
                exact_bridge_bonus_cache.AddOrSetValue(cache_key, Result);
            end;
        end;

        function get_exact_query_choice_bonus_local(const query_key: string;
            const candidate_text: string): Integer;
        const
            c_single_query_base = 44;
            c_single_query_step = 34;
            c_single_query_cap = 176;
            c_multi_query_base = 96;
            c_multi_query_step = 56;
            c_multi_query_cap = 980;
            c_query_recent_latest = 900;
            c_query_recent_top = 180;
            c_query_recent_mid = 96;
            c_query_recent_tail = 52;
        var
            key: string;
            count: Integer;
            last_seen_serial: Int64;
            recent_bonus: Integer;
            serial_gap: Int64;
            text_units: Integer;
        begin
            Result := 0;
            key := build_session_query_choice_key(normalize_pinyin_text(query_key), candidate_text);
            if (key = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            count := 0;
            last_seen_serial := 0;
            serial_gap := High(Int64);
            if (m_session_query_choice_counts <> nil) and
                m_session_query_choice_counts.TryGetValue(key, count) and (count > 0) then
            begin
                if (m_session_query_choice_last_seen <> nil) and
                    m_session_query_choice_last_seen.TryGetValue(key, last_seen_serial) and
                    (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
                begin
                    serial_gap := m_session_commit_serial - last_seen_serial;
                end;

                recent_bonus := 0;
                if serial_gap = 0 then
                begin
                    recent_bonus := c_query_recent_latest;
                end
                else if serial_gap <= 1 then
                begin
                    recent_bonus := c_query_recent_top;
                end
                else if serial_gap <= 3 then
                begin
                    recent_bonus := c_query_recent_mid;
                end
                else if serial_gap <= 6 then
                begin
                    recent_bonus := c_query_recent_tail;
                end;

                text_units := get_candidate_text_unit_count(Trim(candidate_text));
                if text_units <= 1 then
                begin
                    Result := c_single_query_base + ((count - 1) * c_single_query_step) +
                        (recent_bonus div 3);
                    if count >= 3 then
                    begin
                        Inc(Result, 18);
                    end;
                    if Result > c_single_query_cap then
                    begin
                        Result := c_single_query_cap;
                    end;
                end
                else
                begin
                    Result := c_multi_query_base + ((count - 1) * c_multi_query_step) +
                        recent_bonus;
                    if (count >= 2) and (serial_gap <= 2) then
                    begin
                        Inc(Result, 56);
                    end;
                    if (count >= 3) and (serial_gap <= 4) then
                    begin
                        Inc(Result, 32);
                    end;
                    if Result > c_multi_query_cap then
                    begin
                        Result := c_multi_query_cap;
                    end;
                end;
                Exit;
            end;

            if m_dictionary <> nil then
            begin
                Result := m_dictionary.get_query_choice_bonus(normalize_pinyin_text(query_key),
                    candidate_text);
            end;
        end;

        function is_exact_latest_query_choice_local(const query_key: string;
            const candidate_text: string): Boolean;
        var
            latest_text: string;
            normalized_query: string;
        begin
            Result := False;
            normalized_query := normalize_pinyin_text(query_key);
            if (normalized_query = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            latest_text := '';
            if (m_session_query_latest_text <> nil) and
                m_session_query_latest_text.TryGetValue(normalized_query, latest_text) then
            begin
                Result := SameText(Trim(candidate_text), Trim(latest_text));
                if Result then
                begin
                    Exit;
                end;
            end;

            if m_dictionary <> nil then
            begin
                latest_text := get_cached_query_latest_choice_text(normalized_query);
                Result := SameText(Trim(candidate_text), latest_text);
            end;
        end;

        function get_exact_context_pair_bonus(const left_text: string;
            const candidate_text: string): Integer;
        const
            c_segment_pair_context_cap = 520;
            c_segment_pair_bonus_scale = 75;
        var
            pair_key: string;
            local_bonus: Integer;
            persistent_bonus: Integer;
            secondary_bonus: Integer;
            count: Integer;
        begin
            Result := 0;
            if (left_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            local_bonus := 0;
            persistent_bonus := 0;
            if m_context_pairs <> nil then
            begin
                pair_key := left_text + #1 + candidate_text;
                if m_context_pairs.TryGetValue(pair_key, count) and (count > 0) then
                begin
                    local_bonus := count * c_context_score_bonus;
                    if local_bonus > c_context_score_bonus_max then
                    begin
                        local_bonus := c_context_score_bonus_max;
                    end;
                end;
            end;

            if m_dictionary <> nil then
            begin
                persistent_bonus := m_dictionary.get_context_bonus(left_text, candidate_text);
            end;

            if local_bonus >= persistent_bonus then
            begin
                Result := local_bonus;
                secondary_bonus := persistent_bonus;
            end
            else
            begin
                Result := persistent_bonus;
                secondary_bonus := local_bonus;
            end;

            if secondary_bonus > 0 then
            begin
                Inc(Result, secondary_bonus div 2);
            end;
            if Result > c_segment_pair_context_cap then
            begin
                Result := c_segment_pair_context_cap;
            end;
            Result := (Result * c_segment_pair_bonus_scale) div 100;
        end;

        function get_phrase_trigram_transition_bonus(const prev_prev_text: string;
            const prev_text: string; const candidate_text: string): Integer;
        const
            c_segment_trigram_step = 96;
            c_segment_trigram_cap = 300;
            c_segment_trigram_recent_top = 140;
            c_segment_trigram_recent_mid = 84;
            c_segment_trigram_recent_tail = 40;
        var
            trigram_key: string;
            trigram_count: Integer;
            last_seen_serial: Int64;
            serial_gap: Int64;
        begin
            Result := 0;
            if (prev_prev_text = '') or (prev_text = '') or (candidate_text = '') or
                (m_phrase_context_pairs = nil) then
            begin
                Exit;
            end;

            trigram_key := prev_prev_text + #2 + prev_text + #1 + candidate_text;
            if (not m_phrase_context_pairs.TryGetValue(trigram_key, trigram_count)) or
                (trigram_count <= 0) then
            begin
                Exit;
            end;

            Result := trigram_count * c_segment_trigram_step;
            if Result > c_segment_trigram_cap then
            begin
                Result := c_segment_trigram_cap;
            end;

            if (m_phrase_context_last_seen <> nil) and
                m_phrase_context_last_seen.TryGetValue(trigram_key, last_seen_serial) and
                (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
            begin
                serial_gap := m_session_commit_serial - last_seen_serial;
                if serial_gap <= 1 then
                begin
                    Inc(Result, c_segment_trigram_recent_top);
                end
                else if serial_gap <= 3 then
                begin
                    Inc(Result, c_segment_trigram_recent_mid);
                end
                else if serial_gap <= 6 then
                begin
                    Inc(Result, c_segment_trigram_recent_tail);
                end;
            end;
        end;

        function get_compound_prev_trigram_bonus(const combined_prev_text: string;
            const candidate_text: string): Integer;
        var
            combined_units: TArray<string>;
            split_idx: Integer;
            unit_idx: Integer;
            left_part: string;
            right_part: string;
            local_session_bonus: Integer;
            local_persistent_bonus: Integer;
            local_secondary_bonus: Integer;
            local_bonus: Integer;
        begin
            Result := 0;
            if (combined_prev_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            combined_units := split_text_units(combined_prev_text);
            if Length(combined_units) < 2 then
            begin
                Exit;
            end;

            for split_idx := 1 to High(combined_units) do
            begin
                left_part := '';
                for unit_idx := 0 to split_idx - 1 do
                begin
                    left_part := left_part + combined_units[unit_idx];
                end;

                right_part := '';
                for unit_idx := split_idx to High(combined_units) do
                begin
                    right_part := right_part + combined_units[unit_idx];
                end;

                if (left_part = '') or (right_part = '') then
                begin
                    Continue;
                end;

                local_session_bonus := get_phrase_trigram_transition_bonus(
                    left_part, right_part, candidate_text);
                local_persistent_bonus := 0;
                if m_dictionary <> nil then
                begin
                    local_persistent_bonus := m_dictionary.get_context_trigram_bonus(
                        left_part, right_part, candidate_text);
                end;

                if local_session_bonus >= local_persistent_bonus then
                begin
                    local_bonus := local_session_bonus;
                    local_secondary_bonus := local_persistent_bonus;
                end
                else
                begin
                    local_bonus := local_persistent_bonus;
                    local_secondary_bonus := local_session_bonus;
                end;

                if local_secondary_bonus > 0 then
                begin
                    Inc(local_bonus, local_secondary_bonus div 2);
                end;

                if local_bonus > Result then
                begin
                    Result := local_bonus;
                end;
            end;
        end;

        function take_trailing_text_units_local(const value: string;
            const max_units: Integer): string;
        var
            units: TArray<string>;
            unit_idx: Integer;
            start_idx: Integer;
        begin
            Result := '';
            if (Trim(value) = '') or (max_units <= 0) then
            begin
                Exit;
            end;

            units := split_text_units(Trim(value));
            if Length(units) <= 0 then
            begin
                Exit;
            end;

            start_idx := Length(units) - max_units;
            if start_idx < 0 then
            begin
                start_idx := 0;
            end;

            for unit_idx := start_idx to High(units) do
            begin
                Result := Result + units[unit_idx];
            end;
        end;

        function get_exact_base_prefix_transition_bonus_local(
            const prev_prev_text: string; const prev_text: string;
            const candidate_text: string): Integer;
        const
            c_prefix_bonus_cap = 2200;
        var
            combined_text: string;
            raw_bonus: Integer;
            best_bonus: Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (Trim(prev_text) = '') or
                (Trim(candidate_text) = '') then
            begin
                Exit;
            end;

            best_bonus := 0;

            combined_text := take_trailing_text_units_local(
                Trim(prev_text) + Trim(candidate_text), 6);
            if get_candidate_text_unit_count(combined_text) >= 2 then
            begin
                raw_bonus := get_sentence_text_prefix_bonus_local(combined_text);
                if raw_bonus > best_bonus then
                begin
                    best_bonus := raw_bonus;
                end;
            end;

            if Trim(prev_prev_text) <> '' then
            begin
                combined_text := take_trailing_text_units_local(
                    Trim(prev_prev_text) + Trim(prev_text) + Trim(candidate_text), 6);
                if get_candidate_text_unit_count(combined_text) >= 2 then
                begin
                    raw_bonus := get_sentence_text_prefix_bonus_local(combined_text);
                    if raw_bonus > best_bonus then
                    begin
                        best_bonus := raw_bonus;
                    end;
                end;
            end;

            if best_bonus > 0 then
            begin
                Result := Min(c_prefix_bonus_cap, (best_bonus * 3) div 4);
            end;
        end;

        function get_exact_structural_transition_bonus_local(
            const prev_text: string; const candidate_text: string): Integer;
        var
            prev_last_unit: string;
            candidate_first_unit: string;
            candidate_is_predicate: Boolean;
            candidate_is_location: Boolean;
            candidate_is_nominal: Boolean;
        begin
            Result := 0;
            prev_last_unit := exact_last_text_unit_local(prev_text);
            candidate_first_unit := exact_first_text_unit_local(candidate_text);
            if (prev_last_unit = '') or (candidate_first_unit = '') then
            begin
                Exit;
            end;

            candidate_is_predicate := is_exact_predicate_head_unit_local(
                candidate_first_unit);
            candidate_is_location := is_exact_location_head_unit_local(
                candidate_first_unit);
            candidate_is_nominal := candidate_is_location or
                is_exact_nominal_head_unit_local(candidate_first_unit);

            if exact_unit_code_local(prev_last_unit, $4E8E) then
            begin
                if candidate_is_location or candidate_is_nominal then
                begin
                    Inc(Result, 5200);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $4E0E) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 2600);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $5728) then
            begin
                if candidate_is_location or candidate_is_nominal then
                begin
                    Inc(Result, 2800);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $518D) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 2400);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $5730) then
            begin
                if candidate_is_predicate then
                begin
                    Inc(Result, 4200);
                end
                else if candidate_is_nominal then
                begin
                    Dec(Result, 3600);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $7684) then
            begin
                if candidate_is_predicate then
                begin
                    Dec(Result, 1800);
                end
                else if candidate_is_nominal then
                begin
                    Inc(Result, 2600);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $5230) then
            begin
                if candidate_is_location or candidate_is_predicate then
                begin
                    Inc(Result, 2400);
                end;
            end
            else if exact_unit_code_local(prev_last_unit, $9053) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 1800);
                end;
            end;
        end;

        function get_path_transition_bonus(const prev_prev_text: string;
            const prev_text: string; const candidate_text: string): Integer;
        var
            pair_bonus: Integer;
            trigram_bonus: Integer;
            persistent_trigram_bonus: Integer;
            compound_prev_trigram_bonus: Integer;
            prefix_bonus: Integer;
            structural_bonus: Integer;
            secondary_bonus: Integer;
        begin
            Result := 0;
            if (prev_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            pair_bonus := get_exact_context_pair_bonus(prev_text, candidate_text);
            trigram_bonus := get_phrase_trigram_transition_bonus(prev_prev_text, prev_text,
                candidate_text);
            persistent_trigram_bonus := 0;
            if (prev_prev_text <> '') and (m_dictionary <> nil) then
            begin
                persistent_trigram_bonus := m_dictionary.get_context_trigram_bonus(
                    prev_prev_text, prev_text, candidate_text);
            end;
            compound_prev_trigram_bonus := 0;
            if prev_prev_text = '' then
            begin
                compound_prev_trigram_bonus := get_compound_prev_trigram_bonus(
                    prev_text, candidate_text);
            end;

            if trigram_bonus >= persistent_trigram_bonus then
            begin
                Result := trigram_bonus;
                secondary_bonus := persistent_trigram_bonus;
            end
            else
            begin
                Result := persistent_trigram_bonus;
                secondary_bonus := trigram_bonus;
            end;

            if secondary_bonus > 0 then
            begin
                Inc(Result, secondary_bonus div 2);
            end;
            if compound_prev_trigram_bonus > Result then
            begin
                Result := compound_prev_trigram_bonus;
            end;

            if Result > 0 then
            begin
                Inc(Result, pair_bonus div 2);
            end
            else
            begin
                Result := pair_bonus;
            end;

            prefix_bonus := get_exact_base_prefix_transition_bonus_local(
                prev_prev_text, prev_text, candidate_text);
            if prefix_bonus > 0 then
            begin
                if Result > 0 then
                begin
                    Inc(Result, prefix_bonus div 2);
                end
                else
                begin
                    Result := prefix_bonus;
                end;
            end;

            structural_bonus := get_exact_structural_transition_bonus_local(
                prev_text, candidate_text);
            if structural_bonus <> 0 then
            begin
                Inc(Result, structural_bonus);
            end;
        end;

        function compare_state(const left: TncSegmentPathState; const right: TncSegmentPathState): Integer;
        const
            c_near_tie_score_margin = 640;
        var
            left_segment_count: Integer;
            right_segment_count: Integer;
            text_compare: Integer;
        begin
            if Abs(left.score - right.score) <= c_near_tie_score_margin then
            begin
                if left.path_confidence_score <> right.path_confidence_score then
                begin
                    Result := right.path_confidence_score - left.path_confidence_score;
                    Exit;
                end;
            end;

            if Abs(left.score - right.score) > c_near_tie_score_margin then
            begin
                Result := right.score - left.score;
                Exit;
            end;

            if left.path_confidence_score <> right.path_confidence_score then
            begin
                Result := right.path_confidence_score - left.path_confidence_score;
                Exit;
            end;

            left_segment_count := get_encoded_path_segment_count_local(left.path_text);
            right_segment_count := get_encoded_path_segment_count_local(right.path_text);
            if left_segment_count <> right_segment_count then
            begin
                Result := left_segment_count - right_segment_count;
                Exit;
            end;

            if left.source <> right.source then
            begin
                if left.source = cs_user then
                begin
                    Exit(-1);
                end;
                if right.source = cs_user then
                begin
                    Exit(1);
                end;
            end;

            text_compare := CompareText(left.text, right.text);
            if text_compare <> 0 then
            begin
                Result := text_compare;
                Exit;
            end;

            Result := CompareText(left.path_text, right.path_text);
        end;

        procedure sort_state_array(var values: TArray<TncSegmentPathState>);
        var
            left_idx: Integer;
            right_idx: Integer;
            temp_state: TncSegmentPathState;
        begin
            if Length(values) <= 1 then
            begin
                Exit;
            end;

            for left_idx := 0 to High(values) - 1 do
            begin
                for right_idx := left_idx + 1 to High(values) do
                begin
                    if compare_state(values[left_idx], values[right_idx]) > 0 then
                    begin
                        temp_state := values[left_idx];
                        values[left_idx] := values[right_idx];
                        values[right_idx] := temp_state;
                    end;
                end;
            end;
        end;

        procedure append_exact_state(const position: Integer; const value: TncSegmentPathState);
        begin
            exact_key := build_state_key(value);
            if exact_dedup[position].TryGetValue(exact_key, exact_existing_state_index) then
            begin
                if (exact_existing_state_index >= 0) and
                    (exact_existing_state_index < exact_states[position].Count) then
                begin
                    exact_existing_state := exact_states[position][exact_existing_state_index];
                    if compare_state(value, exact_existing_state) < 0 then
                    begin
                        exact_existing_state := value;
                    end;
                    exact_existing_state.source := merge_source_rank(
                        exact_existing_state.source, value.source);
                    exact_states[position][exact_existing_state_index] := exact_existing_state;
                end;
                Exit;
            end;

            exact_states[position].Add(value);
            exact_dedup[position].Add(exact_key, exact_states[position].Count - 1);
        end;

        procedure trim_exact_state(const position: Integer);
        var
            idx: Integer;
        begin
            if exact_states[position].Count <= exact_state_limit_local then
            begin
                Exit;
            end;

            SetLength(exact_sorted_states, exact_states[position].Count);
            for idx := 0 to exact_states[position].Count - 1 do
            begin
                exact_sorted_states[idx] := exact_states[position][idx];
            end;
            sort_state_array(exact_sorted_states);
            exact_keep_count := exact_state_limit_local;

            exact_states[position].Clear;
            exact_dedup[position].Clear;
            for idx := 0 to exact_keep_count - 1 do
            begin
                exact_states[position].Add(exact_sorted_states[idx]);
                exact_dedup[position].Add(build_state_key(exact_sorted_states[idx]), idx);
            end;
        end;

    begin
        query_allows_incomplete_tail :=
            has_extendable_trailing_prefix_query_text(pinyin_key);
        query_extends_full_tail := query_allows_incomplete_tail and
            is_full_pinyin_key(pinyin_key);
        if (m_dictionary = nil) or (pinyin_key = '') or
            (not c_suppress_nonlexicon_complete_long_candidates) or
            ((not is_full_pinyin_key(pinyin_key)) and (not query_allows_incomplete_tail)) or
            all_initial_compact_query or has_internal_dangling_initial then
        begin
            Exit;
        end;

        exact_syllables := get_effective_compact_pinyin_syllables(pinyin_key,
            allow_relaxed_missing_apostrophe);
        if Length(exact_syllables) < c_long_sentence_exact_min_syllables then
        begin
            Exit;
        end;

        exact_lightweight_incomplete_tail := query_allows_incomplete_tail and
            (Length(exact_syllables) >= 10);
        exact_state_limit_local := c_long_sentence_state_limit;
        exact_complete_non_user_limit_local := c_long_sentence_complete_non_user_limit;
        exact_partial_non_user_limit_local := c_long_sentence_partial_non_user_limit;
        exact_partial_remaining_limit_local := c_long_sentence_partial_remaining_limit;
        exact_prefix_single_candidate_limit_local := 96;
        exact_prefix_multi_candidate_limit_local := Max(2, c_long_sentence_multi_top_n);
        if exact_lightweight_incomplete_tail then
        begin
            exact_state_limit_local := 24;
            exact_complete_non_user_limit_local := 3;
            exact_partial_non_user_limit_local := 2;
            exact_partial_remaining_limit_local := 3;
            exact_prefix_single_candidate_limit_local := 16;
            exact_prefix_multi_candidate_limit_local := 2;
        end;

        exact_bridge_bonus_cache := TDictionary<string, Integer>.Create;
        exact_suffix_first_unit_cache := TDictionary<Integer, string>.Create;
        exact_protected_span_text_cache := TDictionary<string, string>.Create;
        exact_protected_span_weight_cache := TDictionary<string, Integer>.Create;
        exact_protected_span_rank_cache := TDictionary<string, Integer>.Create;
        SetLength(exact_states, Length(exact_syllables) + 1);
        SetLength(exact_dedup, Length(exact_syllables) + 1);
        try
            for exact_state_pos := 0 to High(exact_states) do
            begin
                exact_states[exact_state_pos] := TList<TncSegmentPathState>.Create;
                exact_dedup[exact_state_pos] := TDictionary<string, Integer>.Create;
            end;

            exact_state.text := '';
            exact_state.score := 0;
            exact_state.path_preference_score := 0;
            exact_state.path_confidence_score := 0;
            exact_state.source := cs_rule;
            exact_state.has_multi_segment := False;
            exact_state.uses_prefix_completion := False;
            get_recent_path_context_seed(seed_prev_prev_text, seed_prev_text);
            exact_state.prev_prev_text := seed_prev_prev_text;
            exact_state.prev_text := seed_prev_text;
            exact_state.prev_pinyin_text := '';
            exact_state.path_text := '';
            exact_state.fuzzy_cost := 0;
            exact_state.fuzzy_rules := [];
            exact_states[0].Add(exact_state);
            exact_dedup[0].Add(build_state_key(exact_state), 0);

            for exact_state_pos := 0 to High(exact_syllables) do
            begin
                if exact_states[exact_state_pos].Count = 0 then
                begin
                    Continue;
                end;

                for exact_segment_len := 1 to c_long_sentence_max_segment_len do
                begin
                    if (exact_state_pos = 0) and (exact_segment_len = 1) then
                    begin
                        Continue;
                    end;

                    exact_next_pos := exact_state_pos + exact_segment_len;
                    if exact_next_pos > Length(exact_syllables) then
                    begin
                        Break;
                    end;

                    exact_segment_text := '';
                    for exact_idx := exact_state_pos to exact_next_pos - 1 do
                    begin
                        exact_segment_text := exact_segment_text + exact_syllables[exact_idx].text;
                    end;
                    if exact_segment_text = '' then
                    begin
                        Continue;
                    end;

                    exact_segment_uses_prefix_lookup := query_allows_incomplete_tail and
                        (exact_next_pos = Length(exact_syllables)) and
                        is_extendable_trailing_prefix_segment_local(exact_segment_text);

                    if exact_segment_uses_prefix_lookup then
                    begin
                        if not dictionary_prefix_lookup_cached(exact_segment_text,
                            exact_lookup_results) then
                        begin
                            Continue;
                        end;
                    end
                    else if exact_segment_len = 1 then
                    begin
                        if not dictionary_exact_lookup_cached(exact_segment_text,
                            exact_lookup_results) then
                        begin
                            Continue;
                        end;
                    end
                    else if exact_segment_len = 2 then
                    begin
                        if not dictionary_exact_lookup_cached(exact_segment_text,
                            exact_lookup_results) then
                        begin
                            Continue;
                        end;
                    end
                    else if not dictionary_exact_lookup_cached(exact_segment_text,
                        exact_lookup_results) then
                    begin
                        Continue;
                    end;

                    exact_resolved_inserted := False;
                    exact_resolved_text := '';
                    exact_resolved_weight := 0;
                    exact_resolved_rank_bonus := 0;
                    if (not exact_segment_uses_prefix_lookup) and
                        try_get_best_resolved_exact_segment_candidate_local(
                        exact_state_pos, exact_segment_len, exact_resolved_text,
                        exact_resolved_weight, exact_resolved_rank_bonus) then
                    begin
                        FillChar(exact_resolved_candidate,
                            SizeOf(exact_resolved_candidate), 0);
                        exact_resolved_candidate.text := exact_resolved_text;
                        exact_resolved_candidate.comment := '';
                        exact_resolved_candidate.score := exact_resolved_weight;
                        exact_resolved_candidate.source := cs_rule;
                        exact_resolved_candidate.has_dict_weight := True;
                        exact_resolved_candidate.dict_weight := exact_resolved_weight;
                        exact_resolved_inserted := True;
                        for exact_candidate_index := 0 to High(exact_lookup_results) do
                        begin
                            if SameText(Trim(exact_lookup_results[exact_candidate_index].text),
                                exact_resolved_candidate.text) then
                            begin
                                exact_resolved_inserted := False;
                                Break;
                            end;
                        end;
                        if exact_resolved_inserted then
                        begin
                            SetLength(exact_lookup_results, Length(exact_lookup_results) + 1);
                            for exact_candidate_index := High(exact_lookup_results) downto 1 do
                            begin
                                exact_lookup_results[exact_candidate_index] :=
                                    exact_lookup_results[exact_candidate_index - 1];
                            end;
                            exact_lookup_results[0] := exact_resolved_candidate;
                        end;
                    end;

                    if exact_segment_uses_prefix_lookup and (exact_segment_len = 1) then
                    begin
                        exact_candidate_limit := exact_prefix_single_candidate_limit_local;
                    end
                    else if exact_segment_uses_prefix_lookup then
                    begin
                        exact_candidate_limit := exact_prefix_multi_candidate_limit_local;
                    end
                    else if exact_segment_len = 1 then
                    begin
                        exact_candidate_limit := c_long_sentence_single_top_n;
                        if is_long_sentence_ambiguous_function_syllable_local(
                            exact_syllables[exact_state_pos].text) then
                        begin
                            exact_candidate_limit := 4;
                        end;
                    end
                    else if exact_segment_len >= 3 then
                    begin
                        exact_candidate_limit := Max(2, c_long_sentence_multi_top_n - 1);
                    end
                    else
                    begin
                        exact_candidate_limit := c_long_sentence_multi_top_n;
                    end;

                    for exact_state_index := 0 to exact_states[exact_state_pos].Count - 1 do
                    begin
                        exact_state := exact_states[exact_state_pos][exact_state_index];
                        exact_top_effective_weight := 0;
                        exact_accepted_count := 0;
                        for exact_candidate_index := 0 to High(exact_lookup_results) do
                        begin
                            if exact_candidate_index >= exact_candidate_limit then
                            begin
                                Break;
                            end;

                            exact_candidate := exact_lookup_results[exact_candidate_index];
                            exact_candidate_text := Trim(exact_candidate.text);
                            exact_effective_weight := 0;
                            if (exact_candidate.comment <> '') or (exact_candidate_text = '') then
                            begin
                                Continue;
                            end;

                            exact_units := get_candidate_text_unit_count(exact_candidate_text);
                            if exact_units <= 0 then
                            begin
                                Continue;
                            end;

                            if exact_segment_uses_prefix_lookup then
                            begin
                                if exact_segment_len = 1 then
                                begin
                                    if (exact_units <= 0) or (exact_units > 2) then
                                    begin
                                        Continue;
                                    end;
                                    if (exact_units = 1) and
                                        (not is_predictive_tail_single_char_text_codepoint_local(
                                        exact_candidate_text)) then
                                    begin
                                        Continue;
                                    end;
                                end
                                else if (exact_units < exact_segment_len) or
                                    (exact_units > exact_segment_len + 1) then
                                begin
                                    Continue;
                                end;
                            end
                            else if exact_segment_len = 1 then
                            begin
                                exact_effective_weight := get_candidate_effective_weight_local(exact_candidate);
                                if (exact_units <> 1) or
                                    (has_strong_two_syllable_exact_segment_local(exact_state_pos) and
                                    (not is_strong_sentence_boundary_single_local(
                                    exact_syllables[exact_state_pos].text)) and
                                    (not is_long_sentence_ambiguous_function_syllable_local(
                                    exact_syllables[exact_state_pos].text))) or
                                    (not is_extended_long_sentence_allowed_single_char_candidate_local(
                                    exact_syllables[exact_state_pos].text,
                                    exact_candidate_text, exact_effective_weight)) then
                                begin
                                    Continue;
                                end;
                            end
                            else
                            begin
                                if exact_units <> exact_segment_len then
                                begin
                                    Continue;
                                end;
                                if (exact_segment_len = 2) and
                                    (not exact_segment_uses_prefix_lookup) then
                                begin
                                    exact_left_fixed_text :=
                                        get_fixed_long_sentence_single_char_local(
                                        exact_syllables[exact_state_pos].text);
                                    exact_right_fixed_text :=
                                        get_fixed_long_sentence_single_char_local(
                                        exact_syllables[exact_state_pos + 1].text);
                                    if (exact_left_fixed_text <> '') and
                                        (exact_right_fixed_text <> '') and
                                        SameText(exact_candidate_text,
                                        exact_left_fixed_text +
                                        exact_right_fixed_text) and
                                        try_get_best_two_syllable_exact_phrase_local(
                                        exact_state_pos,
                                        exact_best_two_syllable_text,
                                        exact_best_two_syllable_weight) and
                                        (not SameText(exact_best_two_syllable_text,
                                        exact_candidate_text)) then
                                    begin
                                        Continue;
                                    end;
                                end;
                                if (exact_segment_len = 3) and
                                    (not exact_segment_uses_prefix_lookup) then
                                begin
                                    exact_left_fixed_text :=
                                        get_fixed_long_sentence_single_char_local(
                                        exact_syllables[exact_state_pos].text);
                                    exact_right_fixed_text :=
                                        get_fixed_long_sentence_single_char_local(
                                        exact_syllables[exact_state_pos + 1].text);
                                    exact_third_fixed_text :=
                                        get_fixed_long_sentence_single_char_local(
                                        exact_syllables[exact_state_pos + 2].text);
                                    if (exact_left_fixed_text <> '') and
                                        (exact_right_fixed_text <> '') and
                                        (exact_third_fixed_text <> '') and
                                        SameText(exact_candidate_text,
                                        exact_left_fixed_text +
                                        exact_right_fixed_text +
                                        exact_third_fixed_text) and
                                        try_get_best_three_syllable_exact_phrase_local(
                                        exact_state_pos,
                                        exact_best_three_syllable_text,
                                        exact_best_three_syllable_weight) and
                                        (not SameText(exact_best_three_syllable_text,
                                        exact_candidate_text)) then
                                    begin
                                        Continue;
                                    end;
                                end;
                            end;

                            exact_new_state.text := exact_state.text + exact_candidate_text;
                            exact_new_state.score := exact_state.score;
                            if (exact_segment_len <> 1) or exact_segment_uses_prefix_lookup then
                            begin
                                exact_effective_weight := get_candidate_effective_weight_local(exact_candidate);
                            end;
                            if exact_effective_weight > exact_top_effective_weight then
                            begin
                                exact_top_effective_weight := exact_effective_weight;
                            end;
                            Inc(exact_new_state.score, exact_effective_weight);

                            if exact_segment_uses_prefix_lookup and
                                (exact_units > exact_segment_len) then
                            begin
                                Inc(exact_new_state.score,
                                    Min(c_long_sentence_completion_bonus,
                                    (exact_units - exact_segment_len) *
                                    c_long_sentence_multi_bonus));
                            end;

                            exact_rank_bonus := 0;
                            if not exact_segment_uses_prefix_lookup then
                            begin
                                exact_rank_bonus := c_long_sentence_top_exact_bonus -
                                    (exact_candidate_index * c_long_sentence_exact_rank_step);
                                if exact_rank_bonus > 0 then
                                begin
                                    Inc(exact_new_state.score, exact_rank_bonus);
                                end;
                            end;

                            exact_path_bonus := get_exact_query_choice_bonus_local(
                                exact_segment_text, exact_candidate_text);
                            if exact_path_bonus <> 0 then
                            begin
                                Inc(exact_new_state.score, exact_path_bonus);
                            end;

                            exact_phrase_bonus := get_preferred_exact_phrase_bonus_local(
                                exact_segment_text, exact_candidate_text);
                            if exact_phrase_bonus <> 0 then
                            begin
                                Inc(exact_new_state.score, exact_phrase_bonus);
                            end;

                            exact_span_adjustment :=
                                get_protected_exact_span_adjustment_local(
                                exact_state_pos, exact_segment_len,
                                exact_candidate_text);
                            if exact_span_adjustment <> 0 then
                            begin
                                Inc(exact_new_state.score, exact_span_adjustment);
                            end;

                            exact_yu_context_adjustment :=
                                get_exact_yu_context_adjustment_local(
                                exact_syllables[exact_next_pos - 1].text,
                                exact_candidate_text, exact_next_pos);
                            if exact_yu_context_adjustment <> 0 then
                            begin
                                Inc(exact_new_state.score,
                                    exact_yu_context_adjustment);
                            end;

                            if is_exact_latest_query_choice_local(exact_segment_text,
                                exact_candidate_text) then
                            begin
                                if exact_segment_len = 1 then
                                begin
                                    Inc(exact_new_state.score,
                                        c_long_sentence_single_query_latest_bonus);
                                end
                                else
                                begin
                                    Inc(exact_new_state.score,
                                        c_long_sentence_phrase_query_latest_bonus);
                                end;
                            end;

                            exact_pair_bonus := 0;
                            exact_bridge_bonus := 0;

                            exact_transition_bonus := get_path_transition_bonus(
                                exact_state.prev_prev_text, exact_state.prev_text,
                                exact_candidate_text);
                            if exact_transition_bonus <> 0 then
                            begin
                                Inc(exact_new_state.score, exact_transition_bonus);
                            end;

                            if exact_state.prev_pinyin_text <> '' then
                            begin
                                if not (exact_lightweight_incomplete_tail and
                                    exact_segment_uses_prefix_lookup) then
                                begin
                                    exact_bridge_bonus := get_exact_phrase_bridge_bonus_local(
                                        exact_state.prev_pinyin_text, exact_state.prev_text,
                                        exact_segment_text, exact_candidate_text);
                                    if exact_bridge_bonus <> 0 then
                                    begin
                                        Inc(exact_new_state.score, exact_bridge_bonus);
                                    end;
                                end;
                            end;

                            if exact_segment_len = 1 then
                            begin
                                Dec(exact_new_state.score, c_long_sentence_single_penalty);
                            end
                            else
                            begin
                                Inc(exact_new_state.score,
                                    c_long_sentence_multi_bonus + (exact_segment_len * 24));

                                exact_path_bonus := get_longer_exact_prefix_penalty_local(
                                    exact_state_pos, exact_segment_len,
                                    exact_candidate_text, exact_effective_weight);
                                if exact_path_bonus > 0 then
                                begin
                                    Dec(exact_new_state.score, exact_path_bonus);
                                end;

                                exact_path_bonus := get_single_plus_following_phrase_penalty_local(
                                    exact_state_pos, exact_segment_len,
                                    exact_candidate_text, exact_effective_weight);
                                if exact_path_bonus > 0 then
                                begin
                                    Dec(exact_new_state.score, exact_path_bonus);
                                end;

                                exact_path_bonus := get_preferred_phrase_split_penalty_local(
                                    exact_state_pos, exact_segment_len,
                                    exact_candidate_text, exact_effective_weight);
                                if exact_path_bonus > 0 then
                                begin
                                    Dec(exact_new_state.score, exact_path_bonus);
                                end;

                                exact_path_bonus :=
                                    get_preferred_phrase_plus_boundary_penalty_local(
                                    exact_state_pos, exact_segment_len,
                                    exact_candidate_text, exact_effective_weight);
                                if exact_path_bonus > 0 then
                                begin
                                    Dec(exact_new_state.score, exact_path_bonus);
                                end;

                                exact_path_bonus :=
                                    get_prev_phrase_plus_boundary_single_penalty_local(
                                    exact_state.prev_pinyin_text, exact_state.prev_text,
                                    exact_state_pos, exact_segment_len,
                                    exact_candidate_text, exact_effective_weight);
                                if exact_path_bonus > 0 then
                                begin
                                    Dec(exact_new_state.score, exact_path_bonus);
                                end;
                            end;

                            if exact_next_pos = Length(exact_syllables) then
                            begin
                                Inc(exact_new_state.score, c_long_sentence_completion_bonus);
                            end
                            else
                            begin
                                Inc(exact_new_state.score, c_long_sentence_continuation_bonus);
                            end;
                            if exact_segment_uses_prefix_lookup then
                            begin
                                Dec(exact_new_state.score, c_long_sentence_partial_tail_penalty);
                            end;

                            if (exact_units = 1) and (exact_next_pos < Length(exact_syllables)) and
                                (get_candidate_text_unit_count(Trim(exact_state.prev_text)) >= 2) and
                                (not is_predictive_bridge_single_char_text_codepoint_local(
                                exact_candidate_text)) and
                                (exact_bridge_bonus <= 0) then
                            begin
                                Dec(exact_new_state.score, 900);
                            end;

                            exact_new_state.source := merge_source_rank(exact_state.source,
                                exact_candidate.source);
                            exact_new_state.fuzzy_cost := exact_state.fuzzy_cost +
                                exact_candidate.fuzzy_cost;
                            exact_new_state.fuzzy_rules := exact_state.fuzzy_rules +
                                exact_candidate.fuzzy_rules;
                            exact_new_state.prev_prev_text := exact_state.prev_text;
                            exact_new_state.prev_text := exact_candidate_text;
                            exact_new_state.prev_pinyin_text := exact_segment_text;
                            exact_new_state.path_text := exact_state.path_text;
                            if exact_new_state.path_text <> '' then
                            begin
                                exact_new_state.path_text := exact_new_state.path_text +
                                    c_segment_path_separator;
                            end;
                            exact_new_state.path_text := exact_new_state.path_text +
                                exact_candidate_text;
                            exact_new_state.has_multi_segment :=
                                get_encoded_path_segment_count_local(exact_new_state.path_text) > 1;
                            exact_new_state.uses_prefix_completion :=
                                exact_state.uses_prefix_completion or
                                (exact_segment_uses_prefix_lookup and
                                (exact_units > exact_segment_len));

                            exact_path_bonus := exact_transition_bonus + exact_pair_bonus +
                                exact_bridge_bonus + exact_rank_bonus +
                                exact_yu_context_adjustment +
                                exact_span_adjustment + (exact_phrase_bonus div 2);

                            exact_new_state.path_preference_score :=
                                exact_state.path_preference_score + exact_path_bonus;
                            exact_path_confidence := exact_new_state.path_preference_score;
                            if exact_path_confidence < 0 then
                            begin
                                exact_path_confidence := 0;
                            end;
                            exact_new_state.path_confidence_score := exact_path_confidence;
                            append_exact_state(exact_next_pos, exact_new_state);
                            Inc(exact_accepted_count);
                        end;

                        if (exact_segment_len = 2) and (not exact_segment_uses_prefix_lookup) and
                            try_build_two_single_rescue_segment_local(exact_state_pos,
                            exact_rescue_text, exact_rescue_weight,
                            exact_rescue_rank_bonus) and
                            (exact_accepted_count <= 0) then
                        begin
                            exact_same_as_rescue := False;
                            for exact_candidate_index := 0 to Min(exact_candidate_limit - 1,
                                High(exact_lookup_results)) do
                            begin
                                if SameText(Trim(exact_lookup_results[exact_candidate_index].text),
                                    exact_rescue_text) then
                                begin
                                    exact_same_as_rescue := True;
                                    Break;
                                end;
                            end;

                            if not exact_same_as_rescue then
                            begin
                                exact_new_state.text := exact_state.text + exact_rescue_text;
                                exact_new_state.score := exact_state.score + exact_rescue_weight;
                                Inc(exact_new_state.score, exact_rescue_rank_bonus);

                                exact_transition_bonus := get_path_transition_bonus(
                                    exact_state.prev_prev_text, exact_state.prev_text,
                                    exact_rescue_text);
                                if exact_transition_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_transition_bonus);
                                end;
                                exact_pair_bonus := get_exact_context_pair_bonus(
                                    exact_state.prev_text, exact_rescue_text);
                                if exact_pair_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_pair_bonus);
                                end;

                                Inc(exact_new_state.score,
                                    c_long_sentence_multi_bonus + (exact_segment_len * 24));
                                if exact_next_pos = Length(exact_syllables) then
                                begin
                                    Inc(exact_new_state.score, c_long_sentence_completion_bonus);
                                end
                                else
                                begin
                                    Inc(exact_new_state.score,
                                        c_long_sentence_continuation_bonus);
                                end;

                                exact_new_state.source := merge_source_rank(exact_state.source,
                                    cs_rule);
                                exact_new_state.fuzzy_cost := exact_state.fuzzy_cost;
                                exact_new_state.fuzzy_rules := exact_state.fuzzy_rules;
                                exact_new_state.prev_prev_text := exact_state.prev_text;
                                exact_new_state.prev_text := exact_rescue_text;
                                exact_new_state.prev_pinyin_text := exact_segment_text;
                                exact_new_state.path_text := exact_state.path_text;
                                if exact_new_state.path_text <> '' then
                                begin
                                    exact_new_state.path_text := exact_new_state.path_text +
                                        c_segment_path_separator;
                                end;
                                exact_new_state.path_text := exact_new_state.path_text +
                                    exact_rescue_text;
                                exact_new_state.has_multi_segment :=
                                    get_encoded_path_segment_count_local(
                                    exact_new_state.path_text) > 1;
                                exact_new_state.uses_prefix_completion :=
                                    exact_state.uses_prefix_completion;
                                exact_new_state.path_preference_score :=
                                    exact_state.path_preference_score +
                                    exact_transition_bonus + exact_pair_bonus +
                                    exact_rescue_rank_bonus;
                                exact_path_confidence :=
                                    exact_new_state.path_preference_score;
                                if exact_path_confidence < 0 then
                                begin
                                    exact_path_confidence := 0;
                                end;
                                exact_new_state.path_confidence_score :=
                                    exact_path_confidence;
                                append_exact_state(exact_next_pos, exact_new_state);
                            end;
                        end;

                        if (exact_segment_len >= 3) and (not exact_segment_uses_prefix_lookup) and
                            try_build_preferred_split_exact_segment_local(
                            exact_state_pos, exact_segment_len,
                            exact_rescue_text, exact_rescue_weight,
                            exact_rescue_rank_bonus) then
                        begin
                            exact_same_as_rescue := False;
                            for exact_candidate_index := 0 to Min(exact_candidate_limit - 1,
                                High(exact_lookup_results)) do
                            begin
                                if SameText(Trim(exact_lookup_results[exact_candidate_index].text),
                                    exact_rescue_text) then
                                begin
                                    exact_same_as_rescue := True;
                                    Break;
                                end;
                            end;

                            if not exact_same_as_rescue then
                            begin
                                exact_new_state.text := exact_state.text + exact_rescue_text;
                                exact_new_state.score := exact_state.score + exact_rescue_weight;
                                Inc(exact_new_state.score, exact_rescue_rank_bonus);

                                exact_transition_bonus := get_path_transition_bonus(
                                    exact_state.prev_prev_text, exact_state.prev_text,
                                    exact_rescue_text);
                                if exact_transition_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_transition_bonus);
                                end;
                                exact_pair_bonus := get_exact_context_pair_bonus(
                                    exact_state.prev_text, exact_rescue_text);
                                if exact_pair_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_pair_bonus);
                                end;

                                Inc(exact_new_state.score,
                                    c_long_sentence_multi_bonus + (exact_segment_len * 24));
                                if exact_next_pos = Length(exact_syllables) then
                                begin
                                    Inc(exact_new_state.score, c_long_sentence_completion_bonus);
                                end
                                else
                                begin
                                    Inc(exact_new_state.score,
                                        c_long_sentence_continuation_bonus);
                                end;

                                exact_new_state.source := merge_source_rank(exact_state.source,
                                    cs_rule);
                                exact_new_state.fuzzy_cost := exact_state.fuzzy_cost;
                                exact_new_state.fuzzy_rules := exact_state.fuzzy_rules;
                                exact_new_state.prev_prev_text := exact_state.prev_text;
                                exact_new_state.prev_text := exact_rescue_text;
                                exact_new_state.prev_pinyin_text := exact_segment_text;
                                exact_new_state.path_text := exact_state.path_text;
                                if exact_new_state.path_text <> '' then
                                begin
                                    exact_new_state.path_text := exact_new_state.path_text +
                                        c_segment_path_separator;
                                end;
                                exact_new_state.path_text := exact_new_state.path_text +
                                    exact_rescue_text;
                                exact_new_state.has_multi_segment :=
                                    get_encoded_path_segment_count_local(
                                    exact_new_state.path_text) > 1;
                                exact_new_state.uses_prefix_completion :=
                                    exact_state.uses_prefix_completion;
                                exact_new_state.path_preference_score :=
                                    exact_state.path_preference_score +
                                    exact_transition_bonus + exact_pair_bonus +
                                    exact_rescue_rank_bonus;
                                exact_path_confidence :=
                                    exact_new_state.path_preference_score;
                                if exact_path_confidence < 0 then
                                begin
                                    exact_path_confidence := 0;
                                end;
                                exact_new_state.path_confidence_score :=
                                    exact_path_confidence;
                                append_exact_state(exact_next_pos, exact_new_state);
                            end;
                        end;
                    end;

                    trim_exact_state(exact_next_pos);
                end;

            end;
            if exact_states[Length(exact_syllables)].Count <= 0 then
            begin
                Exit;
            end;

            SetLength(exact_sorted_states, exact_states[Length(exact_syllables)].Count);
            for exact_state_pos := 0 to exact_states[Length(exact_syllables)].Count - 1 do
            begin
                exact_sorted_states[exact_state_pos] :=
                    exact_states[Length(exact_syllables)][exact_state_pos];
                exact_path_bonus := get_session_query_path_bonus(pinyin_key,
                    exact_sorted_states[exact_state_pos].path_text);
                if m_dictionary <> nil then
                begin
                    Inc(exact_path_bonus, get_cached_query_segment_path_bonus(pinyin_key,
                        exact_sorted_states[exact_state_pos].path_text));
                    Dec(exact_path_bonus, get_cached_query_segment_path_penalty(
                        pinyin_key, exact_sorted_states[exact_state_pos].path_text));
                end;
                Inc(exact_path_bonus, get_persistent_query_path_prefix_support(
                    exact_sorted_states[exact_state_pos].path_text));
                if exact_path_bonus <> 0 then
                begin
                    Inc(exact_sorted_states[exact_state_pos].score, exact_path_bonus);
                    Inc(exact_sorted_states[exact_state_pos].path_preference_score,
                        exact_path_bonus);
                    exact_path_confidence := exact_sorted_states[exact_state_pos].path_confidence_score +
                        exact_path_bonus;
                    if exact_path_confidence < 0 then
                    begin
                        exact_path_confidence := 0;
                    end;
                    exact_sorted_states[exact_state_pos].path_confidence_score :=
                        exact_path_confidence;
                end;
            end;
            sort_state_array(exact_sorted_states);

            SetLength(exact_matches, 0);
            exact_non_user_added := 0;
            for exact_state_pos := 0 to High(exact_sorted_states) do
            begin
                exact_state := exact_sorted_states[exact_state_pos];
                if query_extends_full_tail and exact_state.uses_prefix_completion then
                begin
                    Continue;
                end;
                exact_segment_count := get_encoded_path_segment_count_local(exact_state.path_text);
                exact_multi_char_segment_count := get_multi_char_segment_count(exact_state.path_text);
                exact_units := get_candidate_text_unit_count(exact_state.text);
                if (exact_segment_count <= 1) or
                    (exact_units < Length(exact_syllables)) then
                begin
                    Continue;
                end;
                if (Length(exact_syllables) >= 8) and (exact_segment_count < 4) then
                begin
                    Continue;
                end;
                exact_required_multi_char_segment_count := 2;
                if Length(exact_syllables) < c_long_sentence_full_path_min_syllables then
                begin
                    exact_required_multi_char_segment_count := 1;
                end;
                if Length(exact_syllables) >= 8 then
                begin
                    exact_required_multi_char_segment_count := 3;
                end;
                if exact_multi_char_segment_count < exact_required_multi_char_segment_count then
                begin
                    Continue;
                end;
                if (exact_multi_char_segment_count * 2) < exact_segment_count then
                begin
                    Continue;
                end;
                if (exact_state.source <> cs_user) and
                    (exact_non_user_added >= exact_complete_non_user_limit_local) then
                begin
                    Continue;
                end;
                if not is_natural_predictive_segment_path_local(exact_state.path_text) then
                begin
                    Continue;
                end;

                SetLength(exact_matches, Length(exact_matches) + 1);
                exact_matches[High(exact_matches)].text := exact_state.text;
                exact_matches[High(exact_matches)].comment := '';
                exact_matches[High(exact_matches)].score :=
                    exact_state.score + c_long_sentence_candidate_visible_bonus;
                Inc(exact_matches[High(exact_matches)].score,
                    get_preferred_query_phrase_bonus_local(lookup_text,
                    exact_state.text));
                exact_matches[High(exact_matches)].source := exact_state.source;
                exact_matches[High(exact_matches)].has_dict_weight := False;
                exact_matches[High(exact_matches)].dict_weight := 0;
                exact_matches[High(exact_matches)].fuzzy_cost := exact_state.fuzzy_cost;
                exact_matches[High(exact_matches)].fuzzy_rules := exact_state.fuzzy_rules;
                remember_segment_path_for_candidate(exact_state.text, '',
                    exact_state.path_text, exact_matches[High(exact_matches)].score);
                if exact_state.source <> cs_user then
                begin
                    Inc(exact_non_user_added);
                end;
            end;

            if try_build_preferred_sentence_rescue_candidate_local(
                exact_matches_candidate) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        Format(' texact=[rescue text=%s score=%d]',
                        [exact_matches_candidate.text, exact_matches_candidate.score]);
                end;
                exact_same_as_rescue := False;
                for exact_idx := 0 to High(exact_matches) do
                begin
                    if SameText(Trim(exact_matches[exact_idx].text),
                        Trim(exact_matches_candidate.text)) and
                        (Trim(exact_matches[exact_idx].comment) = '') then
                    begin
                        exact_same_as_rescue := True;
                        Break;
                    end;
                end;

                if not exact_same_as_rescue then
                begin
                    SetLength(exact_matches, Length(exact_matches) + 1);
                    exact_matches[High(exact_matches)] := exact_matches_candidate;
                    Inc(exact_matches[High(exact_matches)].score,
                        c_long_sentence_candidate_visible_bonus);
                    remember_segment_path_for_candidate(
                        exact_matches[High(exact_matches)].text, '',
                        exact_matches[High(exact_matches)].text,
                        exact_matches[High(exact_matches)].score);
                end;
            end;

            for exact_state_pos := Length(exact_syllables) - 1 downto 2 do
            begin
                exact_remaining_syllables := Length(exact_syllables) - exact_state_pos;
                if (exact_remaining_syllables <= 0) or
                    (exact_remaining_syllables > exact_partial_remaining_limit_local) or
                    (exact_states[exact_state_pos].Count <= 0) then
                begin
                    Continue;
                end;

                exact_remaining_pinyin := build_exact_syllable_text(exact_state_pos,
                    exact_remaining_syllables);
                if exact_remaining_pinyin = '' then
                begin
                    Continue;
                end;

                SetLength(exact_sorted_states, exact_states[exact_state_pos].Count);
                for exact_idx := 0 to exact_states[exact_state_pos].Count - 1 do
                begin
                    exact_sorted_states[exact_idx] := exact_states[exact_state_pos][exact_idx];
                end;
                sort_state_array(exact_sorted_states);

                exact_non_user_added := 0;
                for exact_idx := 0 to High(exact_sorted_states) do
                begin
                    exact_state := exact_sorted_states[exact_idx];
                    exact_segment_count := get_encoded_path_segment_count_local(
                        exact_state.path_text);
                    if exact_segment_count <= 1 then
                    begin
                        Continue;
                    end;

                    exact_units := get_candidate_text_unit_count(exact_state.text);
                    if exact_units < 3 then
                    begin
                        Continue;
                    end;

                    exact_multi_char_segment_count :=
                        get_multi_char_segment_count(exact_state.path_text);
                    if exact_multi_char_segment_count <= 0 then
                    begin
                        Continue;
                    end;

                    if ((exact_multi_char_segment_count * 2) + 1) < exact_segment_count then
                    begin
                        Continue;
                    end;

                    if (exact_state.source <> cs_user) and
                        (exact_non_user_added >= exact_partial_non_user_limit_local) then
                    begin
                        Continue;
                    end;
                    if not is_natural_predictive_segment_path_local(exact_state.path_text) then
                    begin
                        Continue;
                    end;

                    exact_partial_score := exact_state.score +
                        c_long_sentence_partial_visible_bonus;
                    if exact_state.path_confidence_score > 0 then
                    begin
                        Inc(exact_partial_score,
                            Min(220, exact_state.path_confidence_score div 3));
                    end;
                    Dec(exact_partial_score,
                        c_long_sentence_partial_remaining_penalty * exact_remaining_syllables);
                    Dec(exact_partial_score,
                        c_long_sentence_partial_quadratic_penalty * exact_remaining_syllables *
                        (exact_remaining_syllables - 1));

                    SetLength(exact_matches, Length(exact_matches) + 1);
                    exact_matches[High(exact_matches)].text := exact_state.text;
                    exact_matches[High(exact_matches)].comment := exact_remaining_pinyin;
                    exact_matches[High(exact_matches)].score := exact_partial_score;
                    exact_matches[High(exact_matches)].source := exact_state.source;
                    exact_matches[High(exact_matches)].has_dict_weight := False;
                    exact_matches[High(exact_matches)].dict_weight := 0;
                    exact_matches[High(exact_matches)].fuzzy_cost := exact_state.fuzzy_cost;
                    exact_matches[High(exact_matches)].fuzzy_rules := exact_state.fuzzy_rules;
                    remember_segment_path_for_candidate(exact_state.text,
                        exact_remaining_pinyin, exact_state.path_text, exact_partial_score);

                    if exact_state.source <> cs_user then
                    begin
                        Inc(exact_non_user_added);
                    end;
                end;
            end;

            if Length(exact_matches) > 0 then
            begin
                candidates := merge_candidate_lists(candidates, exact_matches, 0);
                if (Length(candidates) > 1) and
                    (Trim(exact_matches[0].comment) = '') and
                    (get_candidate_text_unit_count(Trim(exact_matches[0].text)) =
                    Length(exact_syllables)) and
                    ((Trim(candidates[0].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(candidates[0].text)) <>
                    Length(exact_syllables)) or
                    is_problematic_single_char_chain_candidate_for_query(
                    pinyin_key, candidates[0])) then
                begin
                    for exact_idx := 0 to High(candidates) do
                    begin
                        if (Trim(candidates[exact_idx].comment) = '') and
                            SameText(Trim(candidates[exact_idx].text),
                            Trim(exact_matches[0].text)) then
                        begin
                            if exact_idx > 0 then
                            begin
                                exact_matches_candidate := candidates[exact_idx];
                                for exact_state_pos := exact_idx downto 1 do
                                begin
                                    candidates[exact_state_pos] :=
                                        candidates[exact_state_pos - 1];
                                end;
                                candidates[0] := exact_matches_candidate;
                                if m_config.debug_mode then
                                begin
                                    m_last_full_path_debug_info :=
                                        m_last_full_path_debug_info + Format(
                                        ' longmergepromote=[idx=%d text=%s]',
                                        [exact_idx, Trim(candidates[0].text)]);
                                end;
                            end;
                            Break;
                        end;
                    end;
                end;
                if m_config.debug_mode then
                begin
                    exact_debug_path := get_segment_path_for_candidate(exact_matches[0]);
                    m_last_full_path_debug_info := Format(
                        ' longmerge=[n=%d top=%s path=%s]',
                        [Length(exact_matches), exact_matches[0].text,
                        Copy(exact_debug_path, 1, 120)]);
                    for exact_idx := 0 to High(candidates) do
                    begin
                        if SameText(Trim(candidates[exact_idx].text),
                            Trim(exact_matches[0].text)) then
                        begin
                            m_last_full_path_debug_info := m_last_full_path_debug_info +
                                Format(' longmergekeep=[idx=%d comment=%s seg=%d path=%s]',
                                [exact_idx, Trim(candidates[exact_idx].comment),
                                get_encoded_path_segment_count_local(
                                get_segment_path_for_candidate(candidates[exact_idx])),
                                Copy(get_segment_path_for_candidate(
                                candidates[exact_idx]), 1, 120)]);
                            Break;
                        end;
                    end;
                end;
            end;
        finally
            exact_protected_span_rank_cache.Free;
            exact_protected_span_weight_cache.Free;
            exact_protected_span_text_cache.Free;
            exact_suffix_first_unit_cache.Free;
            exact_bridge_bonus_cache.Free;
            for exact_state_pos := 0 to High(exact_states) do
            begin
                if exact_states[exact_state_pos] <> nil then
                begin
                    exact_states[exact_state_pos].Free;
                end;
                if exact_dedup[exact_state_pos] <> nil then
                begin
                    exact_dedup[exact_state_pos].Free;
                end;
            end;
        end;
    end;

    function merge_incomplete_trailing_prefix_full_lookup_candidates(
        var candidates: TncCandidateList; const pinyin_key: string): Boolean;
    const
        c_incomplete_trailing_prefix_visible_bonus = 780;
        c_incomplete_trailing_prefix_limit = 6;
    var
        effective_syllables: TncPinyinParseResult;
        lookup_results: TncCandidateList;
        exact_same_key_results: TncCandidateList;
        exact_matches: TncCandidateList;
        probe_key: string;
        shortest_tail_probe_key: string;
        input_syllables: Integer;
        probe_syllables: Integer;
        text_units: Integer;
        candidate_text: string;
        last_syllable_text: string;
        tail_trim_chars: Integer;

        function has_exact_long_complete_candidate: Boolean;
        var
            candidate_idx: Integer;
        begin
            Result := False;
            for candidate_idx := 0 to High(candidates) do
            begin
                if candidates[candidate_idx].comment <> '' then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(candidates[candidate_idx].text)) <>
                    input_syllables then
                begin
                    Continue;
                end;
                Exit(True);
            end;
        end;

        function is_extendable_trailing_prefix_local: Boolean;
        var
            suffix_ch: Char;
            extended_key: string;
        begin
            Result := False;
            for suffix_ch := 'a' to 'z' do
            begin
                extended_key := pinyin_key + suffix_ch;
                if get_effective_compact_pinyin_unit_count(extended_key) = input_syllables then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_merge_probe_key(const value: string): Boolean;
        var
            candidate_idx: Integer;
        begin
            Result := False;
            if value = '' then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(value, lookup_results) then
            begin
                Exit;
            end;

            SetLength(exact_matches, 0);
            for candidate_idx := 0 to High(lookup_results) do
            begin
                candidate_text := Trim(lookup_results[candidate_idx].text);
                if (candidate_text = '') or (lookup_results[candidate_idx].comment <> '') then
                begin
                    Continue;
                end;

                text_units := get_text_unit_count(candidate_text);
                if (text_units > input_syllables) or (text_units < 2) then
                begin
                    Continue;
                end;

                if (lookup_results[candidate_idx].source <> cs_user) and
                    (not lookup_results[candidate_idx].has_dict_weight) then
                begin
                    lookup_results[candidate_idx].has_dict_weight := True;
                    lookup_results[candidate_idx].dict_weight := lookup_results[candidate_idx].score;
                end;
                Inc(lookup_results[candidate_idx].score,
                    c_incomplete_trailing_prefix_visible_bonus);
                SetLength(exact_matches, Length(exact_matches) + 1);
                exact_matches[High(exact_matches)] := lookup_results[candidate_idx];
            end;

            if Length(exact_matches) = 0 then
            begin
                Exit;
            end;

            clear_candidate_comments(exact_matches);
            sort_candidates_lightweight(exact_matches);
            if Length(exact_matches) > c_incomplete_trailing_prefix_limit then
            begin
                SetLength(exact_matches, c_incomplete_trailing_prefix_limit);
            end;
            candidates := merge_candidate_lists(candidates, exact_matches, 0);
            Result := True;
        end;

        function try_merge_prefix_probe_key(const value: string): Boolean;
        const
            c_exact_same_key_visible_bonus = 520;
        var
            candidate_idx: Integer;
            exact_idx: Integer;
            exact_same_key_found: Boolean;
        begin
            Result := False;
            if value = '' then
            begin
                Exit;
            end;

            exact_same_key_found :=
                dictionary_exact_lookup_cached(value, exact_same_key_results);
            SetLength(exact_matches, 0);
            if exact_same_key_found and (not is_full_pinyin_key(value)) then
            begin
                for exact_idx := 0 to High(exact_same_key_results) do
                begin
                    candidate_text := Trim(exact_same_key_results[exact_idx].text);
                    if (candidate_text = '') or
                        (Trim(exact_same_key_results[exact_idx].comment) <> '') then
                    begin
                        Continue;
                    end;

                    text_units := get_text_unit_count(candidate_text);
                    if text_units <> input_syllables then
                    begin
                        Continue;
                    end;

                    if (exact_same_key_results[exact_idx].source <> cs_user) and
                        (not exact_same_key_results[exact_idx].has_dict_weight) then
                    begin
                        exact_same_key_results[exact_idx].has_dict_weight := True;
                        exact_same_key_results[exact_idx].dict_weight :=
                            exact_same_key_results[exact_idx].score;
                    end;
                    Inc(exact_same_key_results[exact_idx].score,
                        c_incomplete_trailing_prefix_visible_bonus +
                        c_exact_same_key_visible_bonus);
                    SetLength(exact_matches, Length(exact_matches) + 1);
                    exact_matches[High(exact_matches)] :=
                        exact_same_key_results[exact_idx];
                end;
            end;

            if dictionary_prefix_lookup_cached(value, lookup_results) then
            begin
                for candidate_idx := 0 to High(lookup_results) do
                begin
                    candidate_text := Trim(lookup_results[candidate_idx].text);
                    if (candidate_text = '') or (lookup_results[candidate_idx].comment <> '') then
                    begin
                        Continue;
                    end;

                    if exact_same_key_found then
                    begin
                        for exact_idx := 0 to High(exact_same_key_results) do
                        begin
                            if (Trim(exact_same_key_results[exact_idx].comment) = '') and
                                SameText(Trim(exact_same_key_results[exact_idx].text),
                                candidate_text) then
                            begin
                                candidate_text := '';
                                Break;
                            end;
                        end;
                        if candidate_text = '' then
                        begin
                            Continue;
                        end;
                    end;

                    text_units := get_text_unit_count(candidate_text);
                    if (text_units > input_syllables + 1) or (text_units < 2) then
                    begin
                        Continue;
                    end;

                    if (lookup_results[candidate_idx].source <> cs_user) and
                        (not lookup_results[candidate_idx].has_dict_weight) then
                    begin
                        lookup_results[candidate_idx].has_dict_weight := True;
                        lookup_results[candidate_idx].dict_weight := lookup_results[candidate_idx].score;
                    end;
                    Inc(lookup_results[candidate_idx].score,
                        c_incomplete_trailing_prefix_visible_bonus);
                    SetLength(exact_matches, Length(exact_matches) + 1);
                    exact_matches[High(exact_matches)] := lookup_results[candidate_idx];
                end;
            end;

            if Length(exact_matches) = 0 then
            begin
                Exit;
            end;

            clear_candidate_comments(exact_matches);
            sort_candidates_lightweight(exact_matches);
            if Length(exact_matches) > c_incomplete_trailing_prefix_limit then
            begin
                SetLength(exact_matches, c_incomplete_trailing_prefix_limit);
            end;
            candidates := merge_candidate_lists(candidates, exact_matches, 0);
            Result := True;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (pinyin_key = '') or
            (not c_suppress_nonlexicon_complete_long_candidates) then
        begin
            Exit;
        end;

        effective_syllables := get_effective_compact_pinyin_syllables(pinyin_key);
        input_syllables := Length(effective_syllables);
        if input_syllables < 3 then
        begin
            Exit;
        end;
        if Length(effective_syllables) = 0 then
        begin
            Exit;
        end;

        last_syllable_text := Trim(effective_syllables[High(effective_syllables)].text);
        if (last_syllable_text = '') or
            ((input_syllables >= 4) and
            (not is_extendable_trailing_prefix_local)) then
        begin
            Exit;
        end;

        if has_exact_long_complete_candidate then
        begin
            Exit;
        end;

        if try_merge_prefix_probe_key(pinyin_key) then
        begin
            Exit(True);
        end;

        if input_syllables < 5 then
        begin
            Exit(False);
        end;

        if try_merge_probe_key(pinyin_key) then
        begin
            Exit(True);
        end;

        probe_key := pinyin_key;
        shortest_tail_probe_key := '';
        if Length(probe_key) > 1 then
        begin
            SetLength(probe_key, Length(probe_key) - 1);
            probe_syllables := get_effective_compact_pinyin_unit_count(probe_key);
            if probe_syllables = input_syllables then
            begin
                if try_merge_probe_key(probe_key) then
                begin
                    Exit(True);
                end;
            end;
        end;

        tail_trim_chars := Length(last_syllable_text) - 1;
        if tail_trim_chars <= 0 then
        begin
            Exit;
        end;

        if Length(pinyin_key) <= tail_trim_chars then
        begin
            Exit;
        end;

        shortest_tail_probe_key := Copy(pinyin_key, 1, Length(pinyin_key) - tail_trim_chars);
        if shortest_tail_probe_key = probe_key then
        begin
            Exit;
        end;

        probe_syllables := get_effective_compact_pinyin_unit_count(shortest_tail_probe_key);
        if probe_syllables <> input_syllables then
        begin
            Exit;
        end;

        Result := try_merge_probe_key(shortest_tail_probe_key);
    end;

    function try_build_best_single_char_chain(out out_candidate: TncCandidate): Boolean;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
    end;

    function try_build_runtime_chain_candidate(out out_candidate: TncCandidate): Boolean;
    const
        c_runtime_phrase_bonus = 220;
        c_runtime_phrase_per_syllable = 24;
        c_runtime_phrase_friendly_bonus = 150;
        c_runtime_phrase_long_bonus = 80;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        Exit;

        Result := try_build_best_single_char_chain(out_candidate);
        if not Result then
        begin
            Exit;
        end;

        if get_text_unit_count(out_candidate.text) <> input_syllable_count then
        begin
            Result := False;
            Exit;
        end;

        Inc(out_candidate.score, c_runtime_phrase_bonus + (input_syllable_count * c_runtime_phrase_per_syllable));
        if input_syllable_count >= 3 then
        begin
            Inc(out_candidate.score, c_runtime_phrase_long_bonus);
        end;
        if is_runtime_constructed_phrase_friendly(out_candidate.text) then
        begin
            Inc(out_candidate.score, c_runtime_phrase_friendly_bonus);
        end;
        out_candidate.comment := '';
        out_candidate.source := cs_rule;
    end;

    function try_build_runtime_redup_candidate(out out_candidate: TncCandidate): Boolean;
    const
        c_runtime_redup_bonus = 420;
    var
        parser: TncPinyinParser;
        syllables: TncPinyinParseResult;
        local_lookup: TncCandidateList;
        idx: Integer;
        best_index: Integer;
        best_rank: Integer;
        rank_score: Integer;
        chosen: TncCandidate;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        Exit;

        if (not repeated_two_syllable_query) or (m_dictionary = nil) then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            syllables := parser.parse(lookup_text);
        finally
            parser.Free;
        end;

        if Length(syllables) <> 2 then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(syllables[0].text, local_lookup) then
        begin
            Exit;
        end;

        best_index := -1;
        best_rank := Low(Integer);
        for idx := 0 to High(local_lookup) do
        begin
            chosen := local_lookup[idx];
            if not is_single_text_unit(Trim(chosen.text)) then
            begin
                Continue;
            end;

            rank_score := chosen.score;
            if chosen.source = cs_user then
            begin
                Inc(rank_score, c_user_score_bonus);
            end;
            if is_preferred_partial_single_char_candidate(chosen) then
            begin
                Inc(rank_score, 80);
            end;
            if rank_score > best_rank then
            begin
                best_rank := rank_score;
                best_index := idx;
            end;
        end;

        if best_index < 0 then
        begin
            Exit;
        end;

    end;

    function try_build_runtime_common_pattern_candidate(out out_candidate: TncCandidate): Boolean;
    const
        c_runtime_common_pattern_bonus = 560;
        c_runtime_common_pattern_step = 40;
    var
        parser: TncPinyinParser;
        syllables: TncPinyinParseResult;
        expected_units: TArray<string>;
        local_lookup: TncCandidateList;
        chosen: TncCandidate;
        unit_idx: Integer;
        best_rank: Integer;

        function syllable_equals(const left_value: string; const right_value: string): Boolean;
        begin
            Result := SameText(left_value, right_value);
        end;

        function pick_expected_single_char(
            const syllable_text: string;
            const expected_text: string;
            out out_single: TncCandidate
        ): Boolean;
        var
            candidate_idx: Integer;
            rank_score: Integer;
        begin
            Result := False;
            out_single.text := '';
            out_single.comment := '';
            out_single.score := 0;
            out_single.source := cs_rule;
            out_single.has_dict_weight := False;
            out_single.dict_weight := 0;

            if (syllable_text = '') or (expected_text = '') then
            begin
                Exit;
            end;
            if not dictionary_lookup_cached(syllable_text, local_lookup) then
            begin
                Exit;
            end;

            best_rank := Low(Integer);
            for candidate_idx := 0 to High(local_lookup) do
            begin
                if Trim(local_lookup[candidate_idx].text) <> expected_text then
                begin
                    Continue;
                end;
                if not is_single_text_unit(expected_text) then
                begin
                    Continue;
                end;

                rank_score := local_lookup[candidate_idx].score;
                if local_lookup[candidate_idx].source = cs_user then
                begin
                    Inc(rank_score, c_user_score_bonus);
                end;
                if rank_score > best_rank then
                begin
                    best_rank := rank_score;
                    out_single := local_lookup[candidate_idx];
                    Result := True;
                end;
            end;
        end;

        function pick_best_single_char(
            const syllable_text: string;
            out out_single: TncCandidate
        ): Boolean;
        var
            candidate_idx: Integer;
            pass_idx: Integer;
            best_idx: Integer;
            rank_score: Integer;
            prefer_common_single_char: Boolean;
        begin
            Result := False;
            out_single.text := '';
            out_single.comment := '';
            out_single.score := 0;
            out_single.source := cs_rule;
            out_single.has_dict_weight := False;
            out_single.dict_weight := 0;

            if syllable_text = '' then
            begin
                Exit;
            end;
            if not dictionary_lookup_cached(syllable_text, local_lookup) then
            begin
                Exit;
            end;

            for pass_idx := 0 to 1 do
            begin
                prefer_common_single_char := pass_idx = 0;
                best_idx := -1;
                best_rank := Low(Integer);
                for candidate_idx := 0 to High(local_lookup) do
                begin
                    if not is_single_text_unit(Trim(local_lookup[candidate_idx].text)) then
                    begin
                        Continue;
                    end;
                    if prefer_common_single_char and
                        (not is_preferred_partial_single_char_candidate(local_lookup[candidate_idx])) then
                    begin
                        Continue;
                    end;
                    if (not prefer_common_single_char) and
                        (not is_bmp_cjk_single_char_candidate(local_lookup[candidate_idx])) then
                    begin
                        Continue;
                    end;

                    rank_score := local_lookup[candidate_idx].score;
                    if local_lookup[candidate_idx].source = cs_user then
                    begin
                        Inc(rank_score, c_user_score_bonus);
                    end;
                    if rank_score > best_rank then
                    begin
                        best_rank := rank_score;
                        best_idx := candidate_idx;
                    end;
                end;
                if best_idx >= 0 then
                begin
                    out_single := local_lookup[best_idx];
                    Exit(True);
                end;
            end;
        end;

        function pick_best_constructed_phrase_head_single_char(
            const syllable_text: string;
            out out_single: TncCandidate
        ): Boolean;
        var
            candidate_idx: Integer;
            pass_idx: Integer;
            best_idx: Integer;
            best_rank: Integer;
            rank_score: Integer;
            prefer_common_single_char: Boolean;
            prefer_non_user: Boolean;
        begin
            Result := False;
            out_single.text := '';
            out_single.comment := '';
            out_single.score := 0;
            out_single.source := cs_rule;
            out_single.has_dict_weight := False;
            out_single.dict_weight := 0;

            if syllable_text = '' then
            begin
                Exit;
            end;
            if not dictionary_lookup_cached(syllable_text, local_lookup) then
            begin
                Exit;
            end;

            for pass_idx := 0 to 3 do
            begin
                prefer_non_user := pass_idx < 2;
                prefer_common_single_char := (pass_idx mod 2) = 0;
                best_idx := -1;
                best_rank := Low(Integer);
                for candidate_idx := 0 to High(local_lookup) do
                begin
                    if not is_single_text_unit(Trim(local_lookup[candidate_idx].text)) then
                    begin
                        Continue;
                    end;
                    if prefer_non_user and (local_lookup[candidate_idx].source = cs_user) then
                    begin
                        Continue;
                    end;
                    if prefer_common_single_char and
                        (not is_preferred_partial_single_char_candidate(local_lookup[candidate_idx])) then
                    begin
                        Continue;
                    end;
                    if (not prefer_common_single_char) and
                        (not is_bmp_cjk_single_char_candidate(local_lookup[candidate_idx])) then
                    begin
                        Continue;
                    end;

                    rank_score := local_lookup[candidate_idx].score;
                    if (not prefer_non_user) and (local_lookup[candidate_idx].source = cs_user) then
                    begin
                        Inc(rank_score, c_user_score_bonus);
                    end;
                    if rank_score > best_rank then
                    begin
                        best_rank := rank_score;
                        best_idx := candidate_idx;
                    end;
                end;
                if best_idx >= 0 then
                begin
                    out_single := local_lookup[best_idx];
                    Exit(True);
                end;
            end;
        end;

        function try_get_modal_particle_expected_text(
            const syllable_text: string;
            out out_text: string
        ): Boolean;
        begin
            out_text := '';
            if syllable_equals(syllable_text, 'ba') then
            begin
                out_text := string(Char($5427));
            end
            else if syllable_equals(syllable_text, 'ma') then
            begin
                out_text := string(Char($5417));
            end
            else if syllable_equals(syllable_text, 'ne') then
            begin
                out_text := string(Char($5462));
            end;
            Result := out_text <> '';
        end;

        function try_match_modal_particle_units(out out_units: TArray<string>): Boolean;
        var
            head_single: TncCandidate;
            particle_single: TncCandidate;
            particle_text: string;
        begin
            SetLength(out_units, 0);
            if Length(syllables) <> 2 then
            begin
                Exit(False);
            end;
            if not try_get_modal_particle_expected_text(syllables[1].text, particle_text) then
            begin
                Exit(False);
            end;
            if not pick_best_constructed_phrase_head_single_char(syllables[0].text, head_single) then
            begin
                Exit(False);
            end;
            if not pick_expected_single_char(syllables[1].text, particle_text, particle_single) then
            begin
                Exit(False);
            end;

            out_units := TArray<string>.Create(Trim(head_single.text), particle_text);
            Result := Length(out_units) = Length(syllables);
        end;

        function try_match_function_head_friendly_tail_units(out out_units: TArray<string>): Boolean;
        var
            head_text: string;
            head_single: TncCandidate;
            tail_single: TncCandidate;
        begin
            SetLength(out_units, 0);
            if Length(syllables) <> 2 then
            begin
                Exit(False);
            end;
            if not try_get_runtime_function_head_expected_text(syllables[0].text, head_text) then
            begin
                Exit(False);
            end;
            if not pick_expected_single_char(syllables[0].text, head_text, head_single) then
            begin
                Exit(False);
            end;
            if not pick_best_single_char(syllables[1].text, tail_single) then
            begin
                Exit(False);
            end;
            if not is_runtime_constructed_tail_friendly(tail_single.text) then
            begin
                Exit(False);
            end;

            out_units := TArray<string>.Create(head_text, Trim(tail_single.text));
            Result := Length(out_units) = Length(syllables);
        end;

        function try_match_expected_units(out out_units: TArray<string>): Boolean;
        var
            location_tail_text: string;
        begin
            SetLength(out_units, 0);
            if (Length(syllables) <> 2) and (Length(syllables) <> 3) then
            begin
                Exit(False);
            end;

            if try_match_function_head_friendly_tail_units(out_units) then
            begin
                Exit(True);
            end;

            if (Length(syllables) = 2) and
                (syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                try_get_interrogative_location_tail_expected_text(syllables[1].text, location_tail_text) then
            begin
                out_units := TArray<string>.Create(string(Char($54EA)), location_tail_text);
            end
            else if (Length(syllables) = 3) and syllable_equals(syllables[0].text, 'qu') and
                (syllable_equals(syllables[1].text, 'na') or syllable_equals(syllables[1].text, 'nei')) and
                try_get_interrogative_location_tail_expected_text(syllables[2].text, location_tail_text) then
            begin
                out_units := TArray<string>.Create(
                    string(Char($53BB)), string(Char($54EA)), location_tail_text);
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and
                ((syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                syllable_equals(syllables[1].text, 'ge')) then
            begin
                out_units := TArray<string>.Create(string(Char($90A3)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'yi') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E00)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'liang') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E24)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'ji') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($51E0)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'mei') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($6BCF)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'san') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E09)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'si') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($56DB)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'wu') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E94)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'liu') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($516D)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'qi') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E03)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'ba') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($516B)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'jiu') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($4E5D)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'shi') and
                syllable_equals(syllables[1].text, 'ge') then
            begin
                out_units := TArray<string>.Create(string(Char($5341)), string(Char($4E2A)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'xie') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($4E9B)));
            end
            else if (Length(syllables) = 2) and
                ((syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                syllable_equals(syllables[1].text, 'xie')) then
            begin
                out_units := TArray<string>.Create(string(Char($90A3)), string(Char($4E9B)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'yi') and
                syllable_equals(syllables[1].text, 'xie') then
            begin
                out_units := TArray<string>.Create(string(Char($4E00)), string(Char($4E9B)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'yi') and
                syllable_equals(syllables[1].text, 'dian') then
            begin
                out_units := TArray<string>.Create(string(Char($4E00)), string(Char($70B9)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'yi') and
                syllable_equals(syllables[1].text, 'xia') then
            begin
                out_units := TArray<string>.Create(string(Char($4E00)), string(Char($4E0B)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'yang') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($6837)));
            end
            else if (Length(syllables) = 2) and
                ((syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                syllable_equals(syllables[1].text, 'yang')) then
            begin
                out_units := TArray<string>.Create(string(Char($90A3)), string(Char($6837)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'me') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($4E48)));
            end
            else if (Length(syllables) = 2) and
                ((syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                syllable_equals(syllables[1].text, 'me')) then
            begin
                out_units := TArray<string>.Create(string(Char($90A3)), string(Char($4E48)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zen') and
                syllable_equals(syllables[1].text, 'me') then
            begin
                out_units := TArray<string>.Create(string(Char($600E)), string(Char($4E48)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'you') and
                syllable_equals(syllables[1].text, 'dian') then
            begin
                out_units := TArray<string>.Create(string(Char($6709)), string(Char($70B9)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'shen') and
                syllable_equals(syllables[1].text, 'me') then
            begin
                out_units := TArray<string>.Create(string(Char($4EC0)), string(Char($4E48)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'zhong') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($79CD)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'zhe') and
                syllable_equals(syllables[1].text, 'li') then
            begin
                out_units := TArray<string>.Create(string(Char($8FD9)), string(Char($91CC)));
            end
            else if (Length(syllables) = 2) and syllable_equals(syllables[0].text, 'yi') and
                syllable_equals(syllables[1].text, 'hou') then
            begin
                out_units := TArray<string>.Create(string(Char($4EE5)), string(Char($540E)));
            end
            else if (Length(syllables) = 2) and
                ((syllable_equals(syllables[0].text, 'na') or syllable_equals(syllables[0].text, 'nei')) and
                syllable_equals(syllables[1].text, 'zhong')) then
            begin
                out_units := TArray<string>.Create(string(Char($90A3)), string(Char($79CD)));
            end
            else if (Length(syllables) = 3) and syllable_equals(syllables[0].text, 'wei') and
                syllable_equals(syllables[1].text, 'shen') and syllable_equals(syllables[2].text, 'me') then
            begin
                out_units := TArray<string>.Create(
                    string(Char($4E3A)), string(Char($4EC0)), string(Char($4E48)));
            end
            else if (Length(syllables) = 3) and syllable_equals(syllables[0].text, 'zen') and
                syllable_equals(syllables[1].text, 'me') and syllable_equals(syllables[2].text, 'yang') then
            begin
                out_units := TArray<string>.Create(
                    string(Char($600E)), string(Char($4E48)), string(Char($6837)));
            end
            else
            begin
                Exit(False);
            end;

            Result := Length(out_units) = Length(syllables);
        end;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        Exit;

        if (m_dictionary = nil) or repeated_two_syllable_query then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            syllables := parser.parse(lookup_text);
        finally
            parser.Free;
        end;

        if not try_match_expected_units(expected_units) then
        begin
            if not try_match_modal_particle_units(expected_units) then
            begin
                Exit;
            end;
        end;

        for unit_idx := 0 to High(expected_units) do
        begin
            if not pick_expected_single_char(syllables[unit_idx].text, expected_units[unit_idx], chosen) then
            begin
                Exit;
            end;

            out_candidate.text := out_candidate.text + expected_units[unit_idx];
        end;

    end;

    function try_build_compact_interrogative_location_candidate(out out_candidate: TncCandidate): Boolean;
    const
        c_runtime_common_pattern_bonus = 560;
        c_runtime_common_pattern_step = 40;
    var
        expected_units: TArray<string>;
        expected_syllables: TArray<string>;
        local_lookup: TncCandidateList;
        chosen: TncCandidate;
        compact_unit_idx: Integer;
        best_rank_local: Integer;
        total_score: Integer;
        normalized_query: string;

        function try_pick_expected_single_char(const syllable_text: string;
            const expected_text: string; out out_single: TncCandidate): Boolean;
        var
            local_candidate_idx: Integer;
            local_rank_score: Integer;
        begin
            Result := False;
            out_single.text := '';
            out_single.comment := '';
            out_single.score := 0;
            out_single.source := cs_rule;
            out_single.has_dict_weight := False;
            out_single.dict_weight := 0;

            if (syllable_text = '') or (expected_text = '') then
            begin
                Exit;
            end;
            if not dictionary_lookup_cached(syllable_text, local_lookup) then
            begin
                Exit;
            end;

            best_rank_local := Low(Integer);
            for local_candidate_idx := 0 to High(local_lookup) do
            begin
                if Trim(local_lookup[local_candidate_idx].text) <> expected_text then
                begin
                    Continue;
                end;
                if not is_single_text_unit(expected_text) then
                begin
                    Continue;
                end;

                local_rank_score := local_lookup[local_candidate_idx].score;
                if local_lookup[local_candidate_idx].source = cs_user then
                begin
                    Inc(local_rank_score, c_user_score_bonus);
                end;
                if local_rank_score > best_rank_local then
                begin
                    best_rank_local := local_rank_score;
                    out_single := local_lookup[local_candidate_idx];
                    Result := True;
                end;
            end;
        end;

    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;

        if m_dictionary = nil then
        begin
            Exit;
        end;

        normalized_query := LowerCase(Trim(lookup_text));
        if normalized_query = 'ner' then
        begin
            expected_syllables := TArray<string>.Create('na', 'er');
            expected_units := TArray<string>.Create(string(Char($54EA)), string(Char($513F)));
        end
        else if normalized_query = 'naer' then
        begin
            expected_syllables := TArray<string>.Create('na', 'er');
            expected_units := TArray<string>.Create(string(Char($54EA)), string(Char($513F)));
        end
        else if normalized_query = 'qner' then
        begin
            expected_syllables := TArray<string>.Create('qu', 'na', 'er');
            expected_units := TArray<string>.Create(
                string(Char($53BB)), string(Char($54EA)), string(Char($513F)));
        end
        else if normalized_query = 'qunaer' then
        begin
            expected_syllables := TArray<string>.Create('qu', 'na', 'er');
            expected_units := TArray<string>.Create(
                string(Char($53BB)), string(Char($54EA)), string(Char($513F)));
        end
        else if normalized_query = 'nli' then
        begin
            expected_syllables := TArray<string>.Create('na', 'li');
            expected_units := TArray<string>.Create(string(Char($54EA)), string(Char($91CC)));
        end
        else if normalized_query = 'qnli' then
        begin
            expected_syllables := TArray<string>.Create('qu', 'na', 'li');
            expected_units := TArray<string>.Create(
                string(Char($53BB)), string(Char($54EA)), string(Char($91CC)));
        end
        else
        begin
            Exit;
        end;

        total_score := 0;
        for compact_unit_idx := 0 to High(expected_units) do
        begin
            if not try_pick_expected_single_char(expected_syllables[compact_unit_idx],
                expected_units[compact_unit_idx], chosen) then
            begin
                Exit(False);
            end;
            out_candidate.text := out_candidate.text + expected_units[compact_unit_idx];
            Inc(total_score, chosen.score);
        end;

        out_candidate.score := (total_score * 2) + c_runtime_common_pattern_bonus +
            (Length(expected_units) * c_runtime_common_pattern_step);
        out_candidate.comment := '';
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := total_score div Max(1, Length(expected_units));
        Result := True;
    end;

    procedure merge_runtime_constructed_candidates(var candidates: TncCandidateList;
        const allow_generic_chain: Boolean = True);
    begin
        // Runtime self-composed phrases are intentionally removed. Multi-syllable
        // complete candidates must come from dictionary/user entries; otherwise
        // the engine should expose prefix and single-character choices only.
        runtime_phrase_added := False;
        runtime_redup_added := False;
        m_runtime_chain_text := '';
        m_runtime_common_pattern_text := '';
        m_runtime_redup_text := '';
    end;

    procedure ensure_best_single_char_chain_visible(var candidates: TncCandidateList);
    var
        chain_candidate: TncCandidate;
        idx: Integer;
        chain_index: Integer;
        target_index: Integer;
        visible_limit: Integer;
    begin
        if not has_multi_syllable_input then
        begin
            Exit;
        end;

        if not try_build_best_single_char_chain(chain_candidate) then
        begin
            Exit;
        end;

        chain_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment = '') and (candidates[idx].text = chain_candidate.text) then
            begin
                chain_index := idx;
                Break;
            end;
        end;

        if chain_index < 0 then
        begin
            SetLength(candidates, Length(candidates) + 1);
            candidates[High(candidates)] := chain_candidate;
        end;

        sort_candidates(candidates);

        chain_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment = '') and (candidates[idx].text = chain_candidate.text) then
            begin
                chain_index := idx;
                Break;
            end;
        end;

        if chain_index < 0 then
        begin
            Exit;
        end;

        visible_limit := get_candidate_limit;
        if visible_limit <= 0 then
        begin
            Exit;
        end;

        if input_syllable_count <= 2 then
        begin
            if is_runtime_constructed_phrase_friendly(chain_candidate.text) then
            begin
                target_index := 2;
            end
            else
            begin
                target_index := 4;
            end;
        end
        else if input_syllable_count = 3 then
        begin
            target_index := 3;
        end
        else
        begin
            target_index := 2;
        end;
        if target_index >= visible_limit then
        begin
            target_index := visible_limit - 1;
        end;
        if target_index >= Length(candidates) then
        begin
            target_index := Length(candidates) - 1;
        end;
        if target_index < 0 then
        begin
            Exit;
        end;

        if chain_index > target_index then
        begin
            chain_candidate := candidates[chain_index];
            for idx := chain_index downto target_index + 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[target_index] := chain_candidate;
        end;
    end;

    procedure ensure_single_char_partial_visible(var candidates: TncCandidateList; const visible_limit: Integer;
        const minimum_count: Integer);
    var
        idx: Integer;
        current_visible: Integer;
        need_count: Integer;
        partial_index: Integer;
        target_index: Integer;
        moved_count: Integer;
        partial_candidate: TncCandidate;
        scan_limit: Integer;
    begin
        if (minimum_count <= 0) or (visible_limit <= 0) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        scan_limit := visible_limit;
        if scan_limit > Length(candidates) then
        begin
            scan_limit := Length(candidates);
        end;

        current_visible := 0;
        for idx := 0 to scan_limit - 1 do
        begin
            if (candidates[idx].comment <> '') and is_single_text_unit(Trim(candidates[idx].text)) then
            begin
                Inc(current_visible);
            end;
        end;

        if current_visible >= minimum_count then
        begin
            Exit;
        end;

        if Length(candidates) <= visible_limit then
        begin
            Exit;
        end;

        need_count := minimum_count - current_visible;
        moved_count := 0;
        while moved_count < need_count do
        begin
            partial_index := -1;
            for idx := visible_limit to High(candidates) do
            begin
                if (candidates[idx].comment <> '') and is_single_text_unit(Trim(candidates[idx].text)) then
                begin
                    partial_index := idx;
                    Break;
                end;
            end;

            if partial_index < 0 then
            begin
                Break;
            end;

            target_index := visible_limit - moved_count - 1;
            if target_index < 0 then
            begin
                Break;
            end;

            partial_candidate := candidates[partial_index];
            for idx := partial_index downto target_index + 1 do
            begin
                candidates[idx] := candidates[idx - 1];
            end;
            candidates[target_index] := partial_candidate;
            Inc(moved_count);
        end;
    end;

    function try_get_single_char_candidate_for_pinyin_key_local(const pinyin_key: string;
        const text_value: string; out out_candidate: TncCandidate;
        out out_strength: Integer): Boolean;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
        normalized_text_value: string;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        out_strength := 0;
        normalized_text_value := Trim(text_value);
        if (Trim(pinyin_key) = '') or (normalized_text_value = '') then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(Trim(pinyin_key), local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            if Trim(local_results[local_idx].text) <> normalized_text_value then
            begin
                Continue;
            end;

            if local_results[local_idx].has_dict_weight then
            begin
                local_weight := local_results[local_idx].dict_weight;
            end
            else
            begin
                local_weight := local_results[local_idx].score;
            end;
            if local_weight > out_strength then
            begin
                out_candidate := local_results[local_idx];
                out_strength := local_weight;
                Result := True;
            end;
        end;
    end;

    function try_get_demonstrative_head_expected_text_local(const syllable_text: string;
        out out_text: string): Boolean;
    begin
        out_text := '';
        if SameText(syllable_text, 'zhe') then
        begin
            out_text := string(Char($8FD9));
        end
        else if SameText(syllable_text, 'na') or SameText(syllable_text, 'nei') then
        begin
            out_text := string(Char($90A3));
        end;
        Result := out_text <> '';
    end;

    function is_demonstrative_function_head_syllable_local(const syllable_text: string): Boolean;
    begin
        Result := SameText(syllable_text, 'zhe') or
            SameText(syllable_text, 'na') or
            SameText(syllable_text, 'nei');
    end;

    function try_get_demonstrative_tail_expected_text_local(const syllable_text: string;
        out out_text: string): Boolean;
    begin
        out_text := '';
        if SameText(syllable_text, 'ge') then
        begin
            out_text := string(Char($4E2A));
        end
        else if SameText(syllable_text, 'xie') then
        begin
            out_text := string(Char($4E9B));
        end
        else if SameText(syllable_text, 'zhong') then
        begin
            out_text := string(Char($79CD));
        end
        else if SameText(syllable_text, 'li') then
        begin
            out_text := string(Char($91CC));
        end
        else if SameText(syllable_text, 'yang') then
        begin
            out_text := string(Char($6837));
        end
        else if SameText(syllable_text, 'me') then
        begin
            out_text := string(Char($4E48));
        end
        else if SameText(syllable_text, 'ci') then
        begin
            out_text := string(Char($6B21));
        end
        else if SameText(syllable_text, 'hui') then
        begin
            out_text := string(Char($56DE));
        end
        else if SameText(syllable_text, 'lun') then
        begin
            out_text := string(Char($8F6E));
        end
        else if SameText(syllable_text, 'lei') then
        begin
            out_text := string(Char($7C7B));
        end
        else if SameText(syllable_text, 'bian') then
        begin
            out_text := string(Char($8FB9));
        end
        else if SameText(syllable_text, 'mian') then
        begin
            out_text := string(Char($9762));
        end
        else if SameText(syllable_text, 'yan') then
        begin
            out_text := string(Char($773C));
        end
        else if SameText(syllable_text, 'tian') then
        begin
            out_text := string(Char($5929));
        end
        else if SameText(syllable_text, 'nian') then
        begin
            out_text := string(Char($5E74));
        end
        else if SameText(syllable_text, 'yue') then
        begin
            out_text := string(Char($6708));
        end
        else if SameText(syllable_text, 'xia') then
        begin
            out_text := string(Char($4E0B));
        end
        else if SameText(syllable_text, 'sheng') then
        begin
            out_text := string(Char($58F0));
        end
        else if SameText(syllable_text, 'dian') then
        begin
            out_text := string(Char($70B9));
        end
        else if SameText(syllable_text, 'wei') then
        begin
            out_text := string(Char($4F4D));
        end;
        Result := out_text <> '';
    end;

    function is_demonstrative_head_friendly_remaining_local(
        const parsed_syllables: TncPinyinParseResult; const start_idx: Integer
    ): Boolean;
    var
        dummy_text: string;
    begin
        Result := False;
        if (start_idx < 0) or (start_idx > High(parsed_syllables)) then
        begin
            Exit;
        end;

        if try_get_demonstrative_tail_expected_text_local(
            parsed_syllables[start_idx].text, dummy_text) then
        begin
            Exit(True);
        end;

        if (start_idx < High(parsed_syllables)) and
            SameText(parsed_syllables[start_idx].text, 'yi') and
            try_get_demonstrative_tail_expected_text_local(
            parsed_syllables[start_idx + 1].text, dummy_text) then
        begin
            Exit(True);
        end;
    end;

    function try_get_preferred_demonstrative_head_single_char_local(
        const parsed_syllables: TncPinyinParseResult; const start_idx: Integer;
        out out_candidate: TncCandidate; out out_strength: Integer
    ): Boolean;
    var
        expected_head_text: string;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        out_strength := 0;

        if (Length(parsed_syllables) <= 0) or
            (start_idx <= 0) or (start_idx > High(parsed_syllables)) or
            (not is_demonstrative_function_head_syllable_local(parsed_syllables[0].text)) or
            (not is_demonstrative_head_friendly_remaining_local(parsed_syllables, start_idx)) or
            (not try_get_demonstrative_head_expected_text_local(
            parsed_syllables[0].text, expected_head_text)) then
        begin
            Exit;
        end;

        Result := try_get_single_char_candidate_for_pinyin_key_local(
            parsed_syllables[0].text, expected_head_text, out_candidate, out_strength);
    end;

    function get_demonstrative_head_partial_score_adjustment_local(
        const parsed_syllables: TncPinyinParseResult; const candidate_text: string;
        const start_idx: Integer
    ): Integer;
    const
        c_demonstrative_head_partial_bonus_single_tail_local = 760;
        c_demonstrative_head_partial_bonus_yi_bridge_local = 680;
        c_demonstrative_wrong_head_partial_penalty_local = 160;
    var
        expected_head_text: string;
        normalized_candidate_text: string;
    begin
        Result := 0;
        normalized_candidate_text := Trim(candidate_text);
        if (Length(parsed_syllables) <= 0) or
            (start_idx <= 0) or (start_idx > High(parsed_syllables)) or
            (normalized_candidate_text = '') or
            (not is_demonstrative_function_head_syllable_local(parsed_syllables[0].text)) or
            (not is_demonstrative_head_friendly_remaining_local(parsed_syllables, start_idx)) or
            (not try_get_demonstrative_head_expected_text_local(
            parsed_syllables[0].text, expected_head_text)) then
        begin
            Exit;
        end;

        if normalized_candidate_text = expected_head_text then
        begin
            if (start_idx < High(parsed_syllables)) and
                SameText(parsed_syllables[start_idx].text, 'yi') then
            begin
                Result := c_demonstrative_head_partial_bonus_yi_bridge_local;
            end
            else
            begin
                Result := c_demonstrative_head_partial_bonus_single_tail_local;
            end;
        end
        else if is_single_text_unit(normalized_candidate_text) then
        begin
            Result := -c_demonstrative_wrong_head_partial_penalty_local;
        end;
    end;

    procedure ensure_forced_single_char_partial(var candidates: TncCandidateList);
    const
        c_forced_partial_penalty_per_syllable = 120;
        c_forced_partial_prefix_bonus = 80;
        // Keep a broader single-char fallback pool so medium-rank common chars
        // (e.g. "锟? under "shi") are not dropped too early.
        c_forced_partial_max_candidates = c_candidate_total_limit_max;
    var
        syllables: TncPinyinParseResult;
        first_syllable: string;
        remaining_pinyin: string;
        fallback_lookup: TncCandidateList;
        forced_list: TncCandidateList;
        forced_count: Integer;
        idx: Integer;
        pass_idx: Integer;
        source_item: TncCandidate;
        forced_item: TncCandidate;
        trailing_count: Integer;
        prefer_common_single_char: Boolean;

        function try_build_syllables_from_comment_local(
            const comment_text: string; out out_syllables: TncPinyinParseResult): Boolean;
        var
            scan_idx: Integer;
            token_start: Integer;
            token_text: string;
            item_count: Integer;

            procedure append_token_local(const value: string);
            begin
                if value = '' then
                begin
                    Exit;
                end;
                SetLength(out_syllables, item_count + 1);
                out_syllables[item_count].text := normalize_pinyin_text(value);
                out_syllables[item_count].start_index := 0;
                out_syllables[item_count].length := Length(value);
                Inc(item_count);
            end;
        begin
            SetLength(out_syllables, 0);
            Result := False;
            if Trim(comment_text) = '' then
            begin
                Exit;
            end;

            item_count := 0;
            token_start := 1;
            for scan_idx := 1 to Length(comment_text) + 1 do
            begin
                if (scan_idx > Length(comment_text)) or
                    (comment_text[scan_idx] = ' ') then
                begin
                    token_text := Trim(Copy(comment_text, token_start,
                        scan_idx - token_start));
                    append_token_local(token_text);
                    token_start := scan_idx + 1;
                end;
            end;

            Result := Length(out_syllables) > 1;
        end;
    begin
        if m_dictionary = nil then
        begin
            Exit;
        end;

        if fallback_comment <> '' then
        begin
            try_build_syllables_from_comment_local(fallback_comment, syllables);
        end;
        if Length(syllables) <= 1 then
        begin
            syllables := get_effective_compact_pinyin_syllables(m_composition_text,
                allow_relaxed_missing_apostrophe);
        end;

        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        first_syllable := normalize_pinyin_text(syllables[0].text);
        if first_syllable = '' then
        begin
            Exit;
        end;

        remaining_pinyin := '';
        for idx := 1 to High(syllables) do
        begin
            remaining_pinyin := remaining_pinyin + syllables[idx].text;
        end;
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(first_syllable, fallback_lookup) then
        begin
            Exit;
        end;

        trailing_count := Length(syllables) - 1;
        SetLength(forced_list, 0);
        forced_count := 0;
        for pass_idx := 0 to 1 do
        begin
            prefer_common_single_char := pass_idx = 0;
            for idx := 0 to High(fallback_lookup) do
            begin
                source_item := fallback_lookup[idx];
                if not is_single_text_unit(Trim(source_item.text)) then
                begin
                    Continue;
                end;
                if not is_bmp_cjk_single_char_candidate(source_item) then
                begin
                    Continue;
                end;
                if prefer_common_single_char then
                begin
                    if not is_preferred_partial_single_char_candidate(source_item) then
                    begin
                        Continue;
                    end;
                end
                else if is_preferred_partial_single_char_candidate(source_item) then
                begin
                    Continue;
                end;

                forced_item := source_item;
                forced_item.comment := remaining_pinyin;
                forced_item.score := source_item.score + c_forced_partial_prefix_bonus -
                    (trailing_count * c_forced_partial_penalty_per_syllable);
                Inc(forced_item.score, get_demonstrative_head_partial_score_adjustment_local(
                    syllables, forced_item.text, 1));

                SetLength(forced_list, forced_count + 1);
                forced_list[forced_count] := forced_item;
                Inc(forced_count);
                if forced_count >= c_forced_partial_max_candidates then
                begin
                    Break;
                end;
            end;

            if forced_count >= c_forced_partial_max_candidates then
            begin
                Break;
            end;
        end;

        if forced_count <= 0 then
        begin
            Exit;
        end;

        candidates := merge_candidate_lists(candidates, forced_list, 0);
    end;

    procedure ensure_fast_prefix_single_char_partials_visible(
        var candidates: TncCandidateList; const max_count: Integer);
    const
        c_fast_partial_penalty_per_syllable = 120;
        c_fast_partial_prefix_bonus = 80;
    var
        syllables: TncPinyinParseResult;
        first_syllable: string;
        remaining_pinyin: string;
        fallback_lookup: TncCandidateList;
        forced_list: TncCandidateList;
        forced_count: Integer;
        idx: Integer;
        pass_idx: Integer;
        source_item: TncCandidate;
        forced_item: TncCandidate;
        trailing_count: Integer;
        prefer_common_single_char: Boolean;

        function try_build_syllables_from_comment_local(
            const comment_text: string; out out_syllables: TncPinyinParseResult): Boolean;
        var
            scan_idx: Integer;
            token_start: Integer;
            token_text: string;
            item_count: Integer;

            procedure append_token_local(const value: string);
            begin
                if value = '' then
                begin
                    Exit;
                end;
                SetLength(out_syllables, item_count + 1);
                out_syllables[item_count].text := normalize_pinyin_text(value);
                out_syllables[item_count].start_index := 0;
                out_syllables[item_count].length := Length(value);
                Inc(item_count);
            end;
        begin
            SetLength(out_syllables, 0);
            Result := False;
            if Trim(comment_text) = '' then
            begin
                Exit;
            end;

            item_count := 0;
            token_start := 1;
            for scan_idx := 1 to Length(comment_text) + 1 do
            begin
                if (scan_idx > Length(comment_text)) or
                    (comment_text[scan_idx] = ' ') then
                begin
                    token_text := Trim(Copy(comment_text, token_start,
                        scan_idx - token_start));
                    append_token_local(token_text);
                    token_start := scan_idx + 1;
                end;
            end;

            Result := Length(out_syllables) > 1;
        end;
    begin
        if (m_dictionary = nil) or (max_count < 0) then
        begin
            Exit;
        end;

        if fallback_comment <> '' then
        begin
            try_build_syllables_from_comment_local(fallback_comment, syllables);
        end;
        if Length(syllables) <= 1 then
        begin
            syllables := get_effective_compact_pinyin_syllables(m_composition_text,
                allow_relaxed_missing_apostrophe);
        end;
        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        first_syllable := syllables[0].text;
        if first_syllable = '' then
        begin
            Exit;
        end;

        remaining_pinyin := '';
        for idx := 1 to High(syllables) do
        begin
            remaining_pinyin := remaining_pinyin + syllables[idx].text;
        end;
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(first_syllable, fallback_lookup) then
        begin
            Exit;
        end;

        trailing_count := Length(syllables) - 1;
        SetLength(forced_list, 0);
        forced_count := 0;
        for pass_idx := 0 to 1 do
        begin
            prefer_common_single_char := pass_idx = 0;
            for idx := 0 to High(fallback_lookup) do
            begin
                source_item := fallback_lookup[idx];
                if not is_single_text_unit(Trim(source_item.text)) then
                begin
                    Continue;
                end;
                if not is_bmp_cjk_single_char_candidate(source_item) then
                begin
                    Continue;
                end;
                if prefer_common_single_char then
                begin
                    if not is_preferred_partial_single_char_candidate(source_item) then
                    begin
                        Continue;
                    end;
                end
                else if is_preferred_partial_single_char_candidate(source_item) then
                begin
                    Continue;
                end;

                forced_item := source_item;
                forced_item.comment := remaining_pinyin;
                forced_item.score := source_item.score + c_fast_partial_prefix_bonus -
                    (trailing_count * c_fast_partial_penalty_per_syllable);
                SetLength(forced_list, forced_count + 1);
                forced_list[forced_count] := forced_item;
                Inc(forced_count);
                if (max_count > 0) and (forced_count >= max_count) then
                begin
                    Break;
                end;
            end;

            if (max_count > 0) and (forced_count >= max_count) then
            begin
                Break;
            end;
        end;

        if forced_count > 0 then
        begin
            candidates := merge_candidate_lists(candidates, forced_list, 0);
        end;
    end;

    procedure ensure_best_first_syllable_single_partial_visible(
        var candidates: TncCandidateList; const requested_target_index: Integer);
    const
        c_fast_partial_penalty_per_syllable = 120;
        c_fast_partial_prefix_bonus = 80;
    var
        syllables: TncPinyinParseResult;
        first_syllable: string;
        remaining_pinyin: string;
        fallback_lookup: TncCandidateList;
        best_candidate: TncCandidate;
        source_item: TncCandidate;
        partial_candidate: TncCandidate;
        idx: Integer;
        best_weight: Integer;
        source_weight: Integer;
        partial_index: Integer;
        target_index: Integer;
        trailing_count: Integer;
        preferred_strength: Integer;
        is_demonstrative_preferred: Boolean;

        function try_build_syllables_from_comment_local(
            const comment_text: string; out out_syllables: TncPinyinParseResult): Boolean;
        var
            scan_idx: Integer;
            token_start: Integer;
            token_text: string;
            item_count: Integer;

            procedure append_token_local(const value: string);
            begin
                if value = '' then
                begin
                    Exit;
                end;
                SetLength(out_syllables, item_count + 1);
                out_syllables[item_count].text := normalize_pinyin_text(value);
                out_syllables[item_count].start_index := 0;
                out_syllables[item_count].length := Length(value);
                Inc(item_count);
            end;
        begin
            SetLength(out_syllables, 0);
            Result := False;
            if Trim(comment_text) = '' then
            begin
                Exit;
            end;

            item_count := 0;
            token_start := 1;
            for scan_idx := 1 to Length(comment_text) + 1 do
            begin
                if (scan_idx > Length(comment_text)) or
                    (comment_text[scan_idx] = ' ') then
                begin
                    token_text := Trim(Copy(comment_text, token_start,
                        scan_idx - token_start));
                    append_token_local(token_text);
                    token_start := scan_idx + 1;
                end;
            end;

            Result := Length(out_syllables) > 1;
        end;
    begin
        if (m_dictionary = nil) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        if fallback_comment <> '' then
        begin
            try_build_syllables_from_comment_local(fallback_comment, syllables);
        end;
        if Length(syllables) <= 1 then
        begin
            syllables := get_effective_compact_pinyin_syllables(m_composition_text,
                allow_relaxed_missing_apostrophe);
        end;
        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        first_syllable := normalize_pinyin_text(syllables[0].text);
        if first_syllable = '' then
        begin
            Exit;
        end;

        remaining_pinyin := '';
        for idx := 1 to High(syllables) do
        begin
            remaining_pinyin := remaining_pinyin + normalize_pinyin_text(
                syllables[idx].text);
        end;
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        is_demonstrative_preferred :=
            try_get_preferred_demonstrative_head_single_char_local(
            syllables, 1, best_candidate, preferred_strength);
        if is_demonstrative_preferred then
        begin
            best_weight := preferred_strength;
        end
        else
        begin
            if not dictionary_lookup_cached(first_syllable, fallback_lookup) then
            begin
                Exit;
            end;

            best_weight := Low(Integer);
            FillChar(best_candidate, SizeOf(best_candidate), 0);
            for idx := 0 to High(fallback_lookup) do
            begin
                source_item := fallback_lookup[idx];
                if (Trim(source_item.comment) <> '') or
                    (not is_single_text_unit(Trim(source_item.text))) then
                begin
                    Continue;
                end;

                source_weight := source_item.score;
                if source_item.has_dict_weight and
                    (source_item.dict_weight > source_weight) then
                begin
                    source_weight := source_item.dict_weight;
                end;
                if source_weight > best_weight then
                begin
                    best_weight := source_weight;
                    best_candidate := source_item;
                end;
            end;
        end;

        if (best_weight = Low(Integer)) or (Trim(best_candidate.text) = '') then
        begin
            Exit;
        end;

        partial_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if SameText(Trim(candidates[idx].text), Trim(best_candidate.text)) and
                SameText(Trim(candidates[idx].comment), remaining_pinyin) then
            begin
                partial_index := idx;
                Break;
            end;
        end;

        if partial_index < 0 then
        begin
            partial_candidate := best_candidate;
            partial_candidate.comment := remaining_pinyin;
            trailing_count := Length(syllables) - 1;
            partial_candidate.score := best_candidate.score +
                c_fast_partial_prefix_bonus -
                (trailing_count * c_fast_partial_penalty_per_syllable);
            if best_candidate.has_dict_weight then
            begin
                partial_candidate.dict_weight := best_candidate.dict_weight;
            end;
            SetLength(candidates, Length(candidates) + 1);
            partial_index := High(candidates);
            candidates[partial_index] := partial_candidate;
        end;

        if is_demonstrative_preferred then
        begin
            target_index := 0;
        end
        else
        begin
            target_index := requested_target_index;
        end;
        if target_index < 0 then
        begin
            target_index := 0;
        end;
        if target_index >= get_candidate_limit then
        begin
            target_index := get_candidate_limit - 1;
        end;
        if target_index >= Length(candidates) then
        begin
            target_index := Length(candidates) - 1;
        end;
        if (target_index < 0) or (partial_index <= target_index) then
        begin
            Exit;
        end;

        partial_candidate := candidates[partial_index];
        for idx := partial_index downto target_index + 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[target_index] := partial_candidate;
    end;

    procedure promote_short_unsupported_complete_prefix_partial(
        var candidates: TncCandidateList);
    const
        c_max_short_complete_syllables = 5;
    var
        top_candidate: TncCandidate;
        top_text: string;
        top_path: string;
        path_parts: TArray<string>;
        syllables: TncPinyinParseResult;
        exact_results: TncCandidateList;
        prefix_results: TncCandidateList;
        tail_results: TncCandidateList;
        prefix_text: string;
        last_segment_text: string;
        prefix_key: string;
        tail_key: string;
        last_segment_units: Integer;
        prefix_units: Integer;
        idx: Integer;
        part_idx: Integer;
        target_idx: Integer;
        exact_idx: Integer;
        moved_candidate: TncCandidate;

        function build_query_key_local(const start_idx: Integer;
            const unit_count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (unit_count <= 0) or
                (start_idx + unit_count > Length(syllables)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + unit_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables[local_idx].text);
            end;
        end;

        function exact_full_query_has_complete_support_local: Boolean;
        var
            local_idx: Integer;
            exact_text: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (top_text = '') then
            begin
                Exit;
            end;

            if (not lookup_exact_full_pinyin_cached_local(lookup_text,
                exact_results)) and
                (not dictionary_lookup_cached(lookup_text, exact_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(exact_results) do
            begin
                exact_text := Trim(exact_results[local_idx].text);
                if (Trim(exact_results[local_idx].comment) = '') and
                    (get_candidate_text_unit_count(exact_text) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function prefix_text_has_exact_support_local: Boolean;
        var
            local_idx: Integer;
            exact_text: string;
        begin
            Result := False;
            if prefix_key = '' then
            begin
                Exit;
            end;

            if (not dictionary_exact_lookup_cached(prefix_key,
                prefix_results)) and
                (not dictionary_lookup_cached(prefix_key, prefix_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(prefix_results) do
            begin
                exact_text := Trim(prefix_results[local_idx].text);
                if (Trim(prefix_results[local_idx].comment) = '') and
                    SameText(exact_text, prefix_text) and
                    (get_candidate_text_unit_count(exact_text) =
                    prefix_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function tail_segment_has_weighted_support_local: Boolean;
        var
            local_idx: Integer;
            candidate_text: string;
        begin
            Result := False;
            if (tail_key = '') or (last_segment_text = '') then
            begin
                Exit;
            end;

            if (not dictionary_exact_lookup_cached(tail_key, tail_results)) and
                (not dictionary_lookup_cached(tail_key, tail_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(tail_results) do
            begin
                candidate_text := Trim(tail_results[local_idx].text);
                if (Trim(tail_results[local_idx].comment) = '') and
                    SameText(candidate_text, last_segment_text) and
                    (get_candidate_text_unit_count(candidate_text) =
                    last_segment_units) and
                    tail_results[local_idx].has_dict_weight then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (input_syllable_count < 4) or
            (input_syllable_count > c_max_short_complete_syllables) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        top_candidate := candidates[0];
        top_text := Trim(top_candidate.text);
        if (Trim(top_candidate.comment) <> '') or (top_text = '') or
            (top_candidate.source = cs_user) or
            (get_candidate_text_unit_count(top_text) <>
            input_syllable_count) then
        begin
            Exit;
        end;

        if exact_full_query_has_complete_support_local then
        begin
            Exit;
        end;

        top_path := get_segment_path_for_candidate(top_candidate);
        if (top_path = '') or (Pos(c_segment_path_separator, top_path) <= 0) then
        begin
            Exit;
        end;

        path_parts := top_path.Split([c_segment_path_separator]);
        if Length(path_parts) < 2 then
        begin
            Exit;
        end;

        last_segment_text := Trim(path_parts[High(path_parts)]);
        last_segment_units := get_candidate_text_unit_count(last_segment_text);
        if last_segment_units < 2 then
        begin
            Exit;
        end;

        prefix_units := input_syllable_count - last_segment_units;
        if prefix_units < 2 then
        begin
            Exit;
        end;

        syllables := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables) <> input_syllable_count then
        begin
            Exit;
        end;

        prefix_text := '';
        for part_idx := 0 to High(path_parts) - 1 do
        begin
            prefix_text := prefix_text + Trim(path_parts[part_idx]);
        end;
        if (prefix_text = '') or
            (get_candidate_text_unit_count(prefix_text) <> prefix_units) then
        begin
            Exit;
        end;

        prefix_key := build_query_key_local(0, prefix_units);
        tail_key := build_query_key_local(prefix_units, last_segment_units);
        if (tail_key = '') or (not prefix_text_has_exact_support_local) then
        begin
            Exit;
        end;
        if tail_segment_has_weighted_support_local then
        begin
            Exit;
        end;

        target_idx := -1;
        for idx := 1 to High(candidates) do
        begin
            if SameText(Trim(candidates[idx].text), prefix_text) and
                SameText(Trim(candidates[idx].comment), tail_key) then
            begin
                target_idx := idx;
                Break;
            end;
        end;
        if target_idx <= 0 then
        begin
            Exit;
        end;

        moved_candidate := candidates[target_idx];
        for exact_idx := target_idx downto 1 do
        begin
            candidates[exact_idx] := candidates[exact_idx - 1];
        end;
        candidates[0] := moved_candidate;
    end;

    procedure ensure_bounded_single_char_partial_pool(
        var candidates: TncCandidateList);
    var
        max_single_partials: Integer;
    begin
        if input_syllable_count >= 2 then
        begin
            if input_syllable_count <= 3 then
            begin
                max_single_partials := 0;
            end
            else if input_syllable_count <= 6 then
            begin
                max_single_partials := get_candidate_limit * 2;
            end
            else
            begin
                max_single_partials := get_candidate_limit + 3;
            end;
            ensure_fast_prefix_single_char_partials_visible(candidates,
                max_single_partials);
            Exit;
        end;

        ensure_forced_single_char_partial(candidates);
    end;

    procedure ensure_top_complete_candidate_prefix_partials_visible(
        var candidates: TncCandidateList);
    const
        c_max_query_syllables = 8;
        c_max_added_partials = 3;
        c_score_penalty_per_remaining_syllable = 80;
    var
        syllables_local: TncPinyinParseResult;
        anchor_idx: Integer;
        prefix_len: Integer;
        added_count: Integer;
        out_idx: Integer;
        source_idx: Integer;
        partial_idx: Integer;
        compressed_anchor_idx: Integer;
        units: TArray<string>;
        prefix_text: string;
        tail_comment: string;
        partials: TncCandidateList;

        function candidate_text_unit_count_local(const candidate: TncCandidate): Integer;
        begin
            Result := get_candidate_text_unit_count(Trim(candidate.text));
        end;

        function build_prefix_text_local(const local_prefix_len: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            for unit_idx := 0 to local_prefix_len - 1 do
            begin
                Result := Result + units[unit_idx];
            end;
        end;

        function build_tail_comment_local(const local_prefix_len: Integer): string;
        var
            syllable_idx: Integer;
        begin
            Result := '';
            for syllable_idx := local_prefix_len to High(syllables_local) do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[syllable_idx].text);
            end;
        end;

        function find_candidate_index_local(const local_text: string;
            const local_comment: string): Integer;
        var
            local_idx: Integer;
        begin
            Result := -1;
            for local_idx := 0 to High(candidates) do
            begin
                if SameText(Trim(candidates[local_idx].text), local_text) and
                    SameText(Trim(candidates[local_idx].comment),
                    local_comment) then
                begin
                    Exit(local_idx);
                end;
            end;
        end;

        function has_partial_local(const local_text: string;
            const local_comment: string): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := 0 to High(partials) do
            begin
                if SameText(Trim(partials[local_idx].text), local_text) and
                    SameText(Trim(partials[local_idx].comment),
                    local_comment) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure append_partial_local(const anchor_candidate: TncCandidate;
            const local_prefix_len: Integer);
        var
            local_partial: TncCandidate;
            local_remaining_count: Integer;
            existing_idx: Integer;
        begin
            if added_count >= c_max_added_partials then
            begin
                Exit;
            end;

            prefix_text := build_prefix_text_local(local_prefix_len);
            tail_comment := build_tail_comment_local(local_prefix_len);
            if (prefix_text = '') or (tail_comment = '') or
                has_partial_local(prefix_text, tail_comment) then
            begin
                Exit;
            end;

            local_remaining_count := Length(syllables_local) - local_prefix_len;
            existing_idx := find_candidate_index_local(prefix_text, tail_comment);
            if existing_idx >= 0 then
            begin
                local_partial := candidates[existing_idx];
            end
            else
            begin
                local_partial := anchor_candidate;
                local_partial.text := prefix_text;
                local_partial.comment := tail_comment;
                local_partial.score := Max(1, anchor_candidate.score -
                    (local_remaining_count *
                    c_score_penalty_per_remaining_syllable));
                if local_partial.has_dict_weight then
                begin
                    local_partial.dict_weight := Max(1,
                        local_partial.dict_weight -
                        (local_remaining_count *
                        c_score_penalty_per_remaining_syllable));
                end;
            end;

            SetLength(partials, Length(partials) + 1);
            partials[High(partials)] := local_partial;
            Inc(added_count);
        end;

        function is_promoted_partial_local(const candidate: TncCandidate): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := 0 to High(partials) do
            begin
                if SameText(Trim(partials[local_idx].text),
                    Trim(candidate.text)) and
                    SameText(Trim(partials[local_idx].comment),
                    Trim(candidate.comment)) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 2) or
            (input_syllable_count > c_max_query_syllables) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            m_composition_text, allow_relaxed_missing_apostrophe);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        anchor_idx := -1;
        for source_idx := 0 to Min(High(candidates), 2) do
        begin
            if (Trim(candidates[source_idx].comment) = '') and
                (candidate_text_unit_count_local(candidates[source_idx]) =
                input_syllable_count) then
            begin
                anchor_idx := source_idx;
                Break;
            end;
        end;
        if anchor_idx < 0 then
        begin
            Exit;
        end;

        units := split_text_units(Trim(candidates[anchor_idx].text));
        if Length(units) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(partials, 0);
        added_count := 0;
        for prefix_len := Length(units) - 1 downto 1 do
        begin
            append_partial_local(candidates[anchor_idx], prefix_len);
            if added_count >= c_max_added_partials then
            begin
                Break;
            end;
        end;
        if added_count <= 0 then
        begin
            Exit;
        end;

        SetLength(partials, added_count);
        out_idx := 0;
        compressed_anchor_idx := -1;
        for source_idx := 0 to High(candidates) do
        begin
            if (source_idx <> anchor_idx) and
                is_promoted_partial_local(candidates[source_idx]) then
            begin
                Continue;
            end;
            if source_idx = anchor_idx then
            begin
                compressed_anchor_idx := out_idx;
            end;
            candidates[out_idx] := candidates[source_idx];
            Inc(out_idx);
        end;
        if compressed_anchor_idx < 0 then
        begin
            Exit;
        end;
        SetLength(candidates, out_idx + added_count);
        for source_idx := High(candidates) - added_count downto
            compressed_anchor_idx + 1 do
        begin
            candidates[source_idx + added_count] := candidates[source_idx];
        end;
        out_idx := compressed_anchor_idx + 1;
        for partial_idx := 0 to High(partials) do
        begin
            candidates[out_idx] := partials[partial_idx];
            Inc(out_idx);
        end;
    end;

    function is_backward_attaching_boundary_unit_for_partial(const text_value: string): Boolean;
    var
        normalized_text_value: string;
    begin
        Result := False;
        normalized_text_value := Trim(text_value);
        if get_text_unit_count(normalized_text_value) <> 1 then
        begin
            Exit;
        end;

        Result :=
            (normalized_text_value = string(Char($4E2A))) or
            (normalized_text_value = string(Char($4F4D))) or
            (normalized_text_value = string(Char($6B21))) or
            (normalized_text_value = string(Char($70B9))) or
            (normalized_text_value = string(Char($4E9B))) or
            (normalized_text_value = string(Char($79CD))) or
            (normalized_text_value = string(Char($5929))) or
            (normalized_text_value = string(Char($5E74))) or
            (normalized_text_value = string(Char($6708))) or
            (normalized_text_value = string(Char($91CC))) or
            (normalized_text_value = string(Char($4E0B))) or
            (normalized_text_value = string(Char($56DE))) or
            (normalized_text_value = string(Char($904D))) or
            (normalized_text_value = string(Char($58F0))) or
            (normalized_text_value = string(Char($9762))) or
            (normalized_text_value = string(Char($773C))) or
            (normalized_text_value = string(Char($8FB9)));
    end;

    function is_quantity_like_prefix_for_boundary_unit_partial(const text_value: string): Boolean;
    var
        normalized_text_value: string;
    begin
        Result := False;
        normalized_text_value := Trim(text_value);
        if get_text_unit_count(normalized_text_value) <> 1 then
        begin
            Exit;
        end;

        Result :=
            (normalized_text_value = string(Char($4E00))) or
            (normalized_text_value = string(Char($4E8C))) or
            (normalized_text_value = string(Char($4E09))) or
            (normalized_text_value = string(Char($56DB))) or
            (normalized_text_value = string(Char($4E94))) or
            (normalized_text_value = string(Char($516D))) or
            (normalized_text_value = string(Char($4E03))) or
            (normalized_text_value = string(Char($516B))) or
            (normalized_text_value = string(Char($4E5D))) or
            (normalized_text_value = string(Char($5341))) or
            (normalized_text_value = string(Char($767E))) or
            (normalized_text_value = string(Char($5343))) or
            (normalized_text_value = string(Char($4E07))) or
            (normalized_text_value = string(Char($4E24))) or
            (normalized_text_value = string(Char($51E0))) or
            (normalized_text_value = string(Char($8FD9))) or
            (normalized_text_value = string(Char($90A3))) or
            (normalized_text_value = string(Char($54EA))) or
            (normalized_text_value = string(Char($6BCF))) or
            (normalized_text_value = string(Char($5404))) or
            (normalized_text_value = string(Char($534A))) or
            (normalized_text_value = string(Char($591A))) or
            (normalized_text_value = string(Char($6574))) or
            (normalized_text_value = string(Char($5355))) or
            (normalized_text_value = string(Char($53CC))) or
            (normalized_text_value = string(Char($4FE9))) or
            (normalized_text_value = string(Char($4EE8)));
    end;

    function count_single_char_partial_candidates(const candidates: TncCandidateList): Integer;
    var
        idx: Integer;
    begin
        Result := 0;
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment <> '') and is_bmp_cjk_single_char_candidate(candidates[idx]) then
            begin
                Inc(Result);
            end;
        end;
    end;

    procedure filter_non_bmp_single_char_candidates(var candidates: TncCandidateList);
    var
        idx: Integer;
        out_idx: Integer;
        trimmed_text: string;
    begin
        out_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            trimmed_text := Trim(candidates[idx].text);
            if (Length(trimmed_text) = 2) and
                (Ord(trimmed_text[1]) >= $D800) and (Ord(trimmed_text[1]) <= $DBFF) and
                (Ord(trimmed_text[2]) >= $DC00) and (Ord(trimmed_text[2]) <= $DFFF) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                candidates[out_idx] := candidates[idx];
            end;
            Inc(out_idx);
        end;
        SetLength(candidates, out_idx);
    end;

    function try_build_primary_single_char_partial(out out_candidate: TncCandidate): Boolean;
    const
        c_forced_partial_penalty_per_syllable = 120;
        c_forced_partial_prefix_bonus = 80;
    var
        syllables: TncPinyinParseResult;
        first_syllable: string;
        remaining_pinyin: string;
        fallback_lookup: TncCandidateList;
        source_item: TncCandidate;
        idx: Integer;
        pass_idx: Integer;
        best_index: Integer;
        best_rank: Integer;
        rank_score: Integer;
        trailing_count: Integer;
        prefer_common_single_char: Boolean;
        preferred_item: TncCandidate;
        preferred_strength: Integer;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;

        if m_dictionary = nil then
        begin
            Exit;
        end;

        syllables := get_effective_compact_pinyin_syllables(m_composition_text);

        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        first_syllable := syllables[0].text;
        if first_syllable = '' then
        begin
            Exit;
        end;

        remaining_pinyin := '';
        for idx := 1 to High(syllables) do
        begin
            remaining_pinyin := remaining_pinyin + syllables[idx].text;
        end;
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(first_syllable, fallback_lookup) then
        begin
            Exit;
        end;

        if try_get_preferred_demonstrative_head_single_char_local(
            syllables, 1, preferred_item, preferred_strength) then
        begin
            trailing_count := Length(syllables) - 1;
            out_candidate := preferred_item;
            out_candidate.comment := remaining_pinyin;
            out_candidate.score := preferred_item.score + c_forced_partial_prefix_bonus -
                (trailing_count * c_forced_partial_penalty_per_syllable);
            Inc(out_candidate.score, get_demonstrative_head_partial_score_adjustment_local(
                syllables, out_candidate.text, 1));
            Exit(True);
        end;

        for pass_idx := 0 to 1 do
        begin
            prefer_common_single_char := pass_idx = 0;
            best_index := -1;
            best_rank := Low(Integer);
            for idx := 0 to High(fallback_lookup) do
            begin
                source_item := fallback_lookup[idx];
                if not is_single_text_unit(Trim(source_item.text)) then
                begin
                    Continue;
                end;
                if not is_bmp_cjk_single_char_candidate(source_item) then
                begin
                    Continue;
                end;
                if prefer_common_single_char and
                    (not is_preferred_partial_single_char_candidate(source_item)) then
                begin
                    Continue;
                end;

                rank_score := source_item.score;
                if source_item.source = cs_user then
                begin
                    Inc(rank_score, c_user_score_bonus);
                end;
                if rank_score > best_rank then
                begin
                    best_rank := rank_score;
                    best_index := idx;
                end;
            end;

            if best_index >= 0 then
            begin
                Break;
            end;
        end;

        if best_index < 0 then
        begin
            Exit;
        end;

        trailing_count := Length(syllables) - 1;
        out_candidate := fallback_lookup[best_index];
        out_candidate.comment := remaining_pinyin;
        out_candidate.score := fallback_lookup[best_index].score + c_forced_partial_prefix_bonus -
            (trailing_count * c_forced_partial_penalty_per_syllable);
        Inc(out_candidate.score, get_demonstrative_head_partial_score_adjustment_local(
            syllables, out_candidate.text, 1));
        Result := True;
    end;

    function try_build_phrase_anchored_single_char_partial(const candidates: TncCandidateList;
        out out_candidate: TncCandidate): Boolean;
    const
        c_phrase_anchor_follow_penalty = 48;
        c_phrase_anchor_redup_bonus = 4096;
        c_forced_partial_penalty_per_syllable = 120;
        c_forced_partial_prefix_bonus = 80;
    var
        parser: TncPinyinParser;
        syllables: TncPinyinParseResult;
        first_syllable: string;
        remaining_pinyin: string;
        fallback_lookup: TncCandidateList;
        visible_partials: TDictionary<string, Byte>;
        fallback_idx: Integer;
        candidate_idx: Integer;
        leading_units: TArray<string>;
        leading_text: string;
        source_item: TncCandidate;
        trailing_count: Integer;
        fallback_item: TncCandidate;
        best_source_item: TncCandidate;
        best_fallback_item: TncCandidate;
        preferred_item: TncCandidate;
        preferred_strength: Integer;
        anchor_rank: Integer;
        best_anchor_rank: Integer;
        has_redup_anchor: Boolean;
        visible_limit: Integer;
        best_anchor_uses_confirmed_boundary: Boolean;
        confirmed_boundary_support: Integer;
        boundary_unit_idx: Integer;
        best_boundary_unit_idx: Integer;
        best_boundary_unit_score: Integer;
        confirmed_prefix_tail_text: string;

        function try_get_confirmed_prefix_tail_text(out out_text: string): Boolean;
        var
            confirmed_units: TArray<string>;
        begin
            out_text := '';
            if is_single_text_unit(Trim(m_recent_partial_prefix_text)) then
            begin
                out_text := Trim(m_recent_partial_prefix_text);
                Exit(True);
            end;
            if (m_confirmed_segments <> nil) and (m_confirmed_segments.Count > 0) then
            begin
                out_text := Trim(m_confirmed_segments[m_confirmed_segments.Count - 1].text);
                if is_single_text_unit(out_text) then
                begin
                    Exit(True);
                end;
            end;

            if Trim(m_confirmed_text) = '' then
            begin
                Exit(False);
            end;

            confirmed_units := split_text_units(Trim(m_confirmed_text));
            if Length(confirmed_units) <= 0 then
            begin
                Exit(False);
            end;

            out_text := Trim(confirmed_units[High(confirmed_units)]);
            Result := is_single_text_unit(out_text);
        end;

        function get_confirmed_prefix_boundary_support(const leading_text: string): Integer;
        var
            prefix_lookup_key: string;
            prefix_lookup: TncCandidateList;
            prefix_idx: Integer;
            prefix_candidate: TncCandidate;
            prefix_units: TArray<string>;
            confirmed_segment: TncConfirmedSegment;
            best_match_score: Integer;
            confirmed_text_for_boundary: string;
        begin
            Result := 0;
            if not try_get_confirmed_prefix_tail_text(confirmed_text_for_boundary) then
            begin
                Exit;
            end;
            if not is_backward_attaching_boundary_unit_for_partial(leading_text) then
            begin
                Exit;
            end;

            if (m_confirmed_segments = nil) or (m_confirmed_segments.Count <= 0) then
            begin
                confirmed_segment.text := confirmed_text_for_boundary;
                confirmed_segment.pinyin := '';
            end
            else
            begin
                confirmed_segment := m_confirmed_segments[m_confirmed_segments.Count - 1];
            end;

            if (Trim(confirmed_segment.text) = '') or
                (not is_single_text_unit(Trim(confirmed_segment.text))) then
            begin
                Exit;
            end;

            prefix_lookup_key := normalize_pinyin_text(confirmed_segment.pinyin) + first_syllable;
            if (confirmed_segment.pinyin <> '') and (prefix_lookup_key = '') then
            begin
                Exit;
            end;

            if (confirmed_segment.pinyin <> '') and
                (not dictionary_lookup_cached(prefix_lookup_key, prefix_lookup)) then
            begin
                Exit;
            end;

            for prefix_idx := 0 to High(prefix_lookup) do
            begin
                prefix_candidate := prefix_lookup[prefix_idx];
                if prefix_candidate.comment <> '' then
                begin
                    Continue;
                end;
                prefix_units := split_text_units(Trim(prefix_candidate.text));
                if Length(prefix_units) <> 2 then
                begin
                    Continue;
                end;
                if (not SameText(prefix_units[0], Trim(confirmed_segment.text))) or
                    (not SameText(prefix_units[1], Trim(leading_text))) then
                begin
                    Continue;
                end;
                if (prefix_candidate.source <> cs_user) and (not prefix_candidate.has_dict_weight) then
                begin
                    Continue;
                end;

                best_match_score := prefix_candidate.score;
                if prefix_candidate.has_dict_weight and (prefix_candidate.dict_weight > best_match_score) then
                begin
                    best_match_score := prefix_candidate.dict_weight;
                end;
                if prefix_candidate.source = cs_user then
                begin
                    Inc(best_match_score, c_user_score_bonus);
                end;
                if best_match_score > Result then
                begin
                    Result := best_match_score;
                end;
            end;

            if Result >= 720 then
            begin
                Result := 520;
            end
            else if Result >= 560 then
            begin
                Result := 420;
            end
            else if Result >= 420 then
            begin
                Result := 320;
            end
            else if Result > 0 then
            begin
                Result := 220;
            end;

            if (Result <= 0) and
                is_quantity_like_prefix_for_boundary_unit_partial(confirmed_text_for_boundary) then
            begin
                Result := 260;
            end;
        end;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        confirmed_prefix_boundary_partial_preferred := False;

        if m_dictionary = nil then
        begin
            Exit;
        end;

        parser := TncPinyinParser.create;
        try
            syllables := get_effective_compact_pinyin_syllables(m_composition_text);
        finally
            parser.Free;
        end;

        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        first_syllable := syllables[0].text;
        if first_syllable = '' then
        begin
            Exit;
        end;

        remaining_pinyin := '';
        for candidate_idx := 1 to High(syllables) do
        begin
            remaining_pinyin := remaining_pinyin + syllables[candidate_idx].text;
        end;
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        if not dictionary_lookup_cached(first_syllable, fallback_lookup) then
        begin
            Exit;
        end;

        trailing_count := Length(syllables) - 1;

        confirmed_prefix_tail_text := '';
        if try_get_confirmed_prefix_tail_text(confirmed_prefix_tail_text) and
            is_quantity_like_prefix_for_boundary_unit_partial(confirmed_prefix_tail_text) then
        begin
            best_boundary_unit_idx := -1;
            best_boundary_unit_score := Low(Integer);
            for boundary_unit_idx := 0 to High(fallback_lookup) do
            begin
                if (not is_bmp_cjk_single_char_candidate(fallback_lookup[boundary_unit_idx])) or
                    (not is_backward_attaching_boundary_unit_for_partial(
                    Trim(fallback_lookup[boundary_unit_idx].text))) then
                begin
                    Continue;
                end;

                if (best_boundary_unit_idx < 0) or
                    (fallback_lookup[boundary_unit_idx].score > best_boundary_unit_score) then
                begin
                    best_boundary_unit_idx := boundary_unit_idx;
                    best_boundary_unit_score := fallback_lookup[boundary_unit_idx].score;
                end;
            end;

            if best_boundary_unit_idx >= 0 then
            begin
                out_candidate := fallback_lookup[best_boundary_unit_idx];
                out_candidate.comment := remaining_pinyin;
                out_candidate.score := fallback_lookup[best_boundary_unit_idx].score +
                    c_forced_partial_prefix_bonus + 200;
                confirmed_prefix_boundary_partial_preferred := True;
                Result := True;
                Exit;
            end;
        end;

        visible_partials := TDictionary<string, Byte>.Create;
        try
            best_anchor_rank := Low(Integer);
            best_anchor_uses_confirmed_boundary := False;
            visible_limit := get_candidate_limit;
            if (visible_limit <= 0) or (visible_limit > Length(candidates)) then
            begin
                visible_limit := Length(candidates);
            end;
            for candidate_idx := 0 to visible_limit - 1 do
            begin
                if (candidates[candidate_idx].comment = remaining_pinyin) and
                    is_single_text_unit(Trim(candidates[candidate_idx].text)) then
                begin
                    visible_partials.AddOrSetValue(Trim(candidates[candidate_idx].text), 1);
                end;
            end;

            for candidate_idx := 0 to High(candidates) do
            begin
                source_item := candidates[candidate_idx];
                if source_item.comment <> '' then
                begin
                    Continue;
                end;
                leading_units := split_text_units(Trim(source_item.text));
                if Length(leading_units) < 2 then
                begin
                    Continue;
                end;

                leading_text := leading_units[0];
                if not is_single_text_unit(leading_text) then
                begin
                    Continue;
                end;
                confirmed_boundary_support := get_confirmed_prefix_boundary_support(leading_text);
                if visible_partials.ContainsKey(leading_text) and (confirmed_boundary_support <= 0) then
                begin
                    Continue;
                end;
                has_redup_anchor := repeated_two_syllable_query and
                    is_two_unit_redup_text(source_item.text);
                if (source_item.source <> cs_user) and
                    (not has_redup_anchor) and
                    (not is_runtime_chain_candidate(source_item)) and
                    (not is_runtime_common_pattern_candidate(source_item)) and
                    (not is_runtime_redup_candidate(source_item)) and
                    (confirmed_boundary_support <= 0) then
                begin
                    Continue;
                end;

                for fallback_idx := 0 to High(fallback_lookup) do
                begin
                    fallback_item := fallback_lookup[fallback_idx];
                    if not SameText(Trim(fallback_item.text), leading_text) then
                    begin
                        Continue;
                    end;
                    if not is_bmp_cjk_single_char_candidate(fallback_item) then
                    begin
                        Continue;
                    end;
                    anchor_rank := source_item.score;
                    if source_item.source = cs_user then
                    begin
                        Inc(anchor_rank, c_user_score_bonus);
                    end;
                    Inc(anchor_rank, confirmed_boundary_support);
                    if has_redup_anchor then
                    begin
                        Inc(anchor_rank, c_phrase_anchor_redup_bonus);
                    end;

                    if anchor_rank > best_anchor_rank then
                    begin
                        best_anchor_rank := anchor_rank;
                        best_anchor_uses_confirmed_boundary := confirmed_boundary_support > 0;
                        best_source_item := source_item;
                        best_fallback_item := fallback_item;
                    end;
                    Break;
                end;
            end;

            if best_anchor_rank <= Low(Integer) then
            begin
                Exit;
            end;

            out_candidate := best_fallback_item;
            if try_get_preferred_demonstrative_head_single_char_local(
                syllables, 1, preferred_item, preferred_strength) then
            begin
                out_candidate := preferred_item;
            end;
            out_candidate.comment := remaining_pinyin;
            out_candidate.score := Max(
                best_fallback_item.score + c_forced_partial_prefix_bonus -
                    (trailing_count * c_forced_partial_penalty_per_syllable),
                best_source_item.score - c_phrase_anchor_follow_penalty -
                    (trailing_count * c_forced_partial_penalty_per_syllable));
            Inc(out_candidate.score, get_demonstrative_head_partial_score_adjustment_local(
                syllables, out_candidate.text, 1));
            if best_anchor_uses_confirmed_boundary then
            begin
                Inc(out_candidate.score, 180);
                confirmed_prefix_boundary_partial_preferred := True;
            end;
            Result := True;
        finally
            visible_partials.Free;
        end;
    end;

    procedure ensure_hard_single_char_partial_visible(var candidates: TncCandidateList);
    var
        visible_limit: Integer;
        i: Integer;
        partial_index: Integer;
        target_index: Integer;
        partial_candidate: TncCandidate;
        phrase_anchored_candidate: TncCandidate;
        best_score: Integer;
        best_index: Integer;
        has_complete_phrase: Boolean;
    begin
        if not has_multi_syllable_input then
        begin
            Exit;
        end;

        has_complete_phrase := False;
        if input_syllable_count >= 3 then
        begin
            for i := 0 to High(candidates) do
            begin
                if (candidates[i].comment = '') and
                    (get_text_unit_count(Trim(candidates[i].text)) >= 2) then
                begin
                    has_complete_phrase := True;
                    Break;
                end;
            end;
        end;

        visible_limit := get_candidate_limit;
        if (visible_limit <= 0) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        if try_build_phrase_anchored_single_char_partial(candidates, phrase_anchored_candidate) then
        begin
            partial_index := -1;
            for i := 0 to High(candidates) do
            begin
                if (candidates[i].comment = phrase_anchored_candidate.comment) and
                    SameText(Trim(candidates[i].text), Trim(phrase_anchored_candidate.text)) then
                begin
                    partial_index := i;
                    Break;
                end;
            end;
            if partial_index < 0 then
            begin
                SetLength(candidates, Length(candidates) + 1);
                candidates[High(candidates)] := phrase_anchored_candidate;
                partial_index := High(candidates);
            end
            else
            begin
                candidates[partial_index] := phrase_anchored_candidate;
            end;
        end
        else
        begin
        best_index := -1;
        best_score := Low(Integer);
        for i := 0 to High(candidates) do
        begin
            if (candidates[i].comment <> '') and is_bmp_cjk_single_char_candidate(candidates[i]) then
            begin
                if (best_index < 0) or (candidates[i].score > best_score) then
                begin
                    best_index := i;
                    best_score := candidates[i].score;
                end;
            end;
        end;

        partial_index := best_index;
        if partial_index < 0 then
        begin
            if not try_build_primary_single_char_partial(partial_candidate) then
            begin
                Exit;
            end;

            SetLength(candidates, Length(candidates) + 1);
            candidates[High(candidates)] := partial_candidate;
            partial_index := High(candidates);
        end;

        if partial_index < 0 then
        begin
            Exit;
        end;
        end;

        // Keep a practical single-char continuation visible, but avoid occupying
        // the very front on longer queries where phrase intent is stronger.
        if confirmed_prefix_boundary_partial_preferred and (input_syllable_count = 2) then
        begin
            target_index := 0;
        end
        else if has_complete_phrase and (input_syllable_count >= 6) then
        begin
            target_index := 5;
        end
        else if has_complete_phrase and (input_syllable_count >= 4) then
        begin
            target_index := 4;
        end
        else if input_syllable_count >= 4 then
        begin
            target_index := 2;
        end
        else if input_syllable_count >= 3 then
        begin
            target_index := 4;
        end
        else
        begin
            target_index := 2;
        end;
        if (input_syllable_count >= 3) and (not has_complete_phrase) and (target_index > 2) then
        begin
            target_index := 2;
        end;
        if target_index >= visible_limit then
        begin
            target_index := visible_limit - 1;
        end;
        if target_index >= Length(candidates) then
        begin
            target_index := Length(candidates) - 1;
        end;
        if target_index < 0 then
        begin
            Exit;
        end;

        if partial_index > target_index then
        begin
            partial_candidate := candidates[partial_index];
            for i := partial_index downto target_index + 1 do
            begin
                candidates[i] := candidates[i - 1];
            end;
            candidates[target_index] := partial_candidate;
        end;
    end;

    procedure ensure_two_syllable_exact_phrase_precedes_matching_single_char_partial(
        var candidates: TncCandidateList);
    var
        idx: Integer;
        partial_index: Integer;
        exact_index: Integer;
        partial_text: string;
        exact_units: TArray<string>;
        exact_candidate: TncCandidate;
    begin
        if (input_syllable_count <> 2) or (Length(candidates) <= 1) then
        begin
            Exit;
        end;

        partial_index := -1;
        partial_text := '';
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment <> '') and is_single_text_unit(Trim(candidates[idx].text)) then
            begin
                partial_index := idx;
                partial_text := Trim(candidates[idx].text);
                Break;
            end;
        end;
        if (partial_index < 0) or (partial_text = '') then
        begin
            Exit;
        end;

        exact_index := -1;
        for idx := 0 to High(candidates) do
        begin
            if (candidates[idx].comment <> '') or
                (not (candidates[idx].has_dict_weight or (candidates[idx].source = cs_user))) then
            begin
                Continue;
            end;

            exact_units := split_text_units(Trim(candidates[idx].text));
            if (Length(exact_units) < 2) or (exact_units[0] <> partial_text) then
            begin
                Continue;
            end;

            exact_index := idx;
            Break;
        end;

        if (exact_index < 0) or (exact_index < partial_index) then
        begin
            Exit;
        end;

        exact_candidate := candidates[exact_index];
        for idx := exact_index downto partial_index + 1 do
        begin
            candidates[idx] := candidates[idx - 1];
        end;
        candidates[partial_index] := exact_candidate;
    end;

    procedure apply_user_penalties(const pinyin_key: string; var candidates: TncCandidateList);
    var
        idx: Integer;
        write_idx: Integer;
        penalty: Integer;
    begin
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit;
        end;

        write_idx := 0;
        for idx := 0 to High(candidates) do
        begin
            if candidates[idx].text = '' then
            begin
                Continue;
            end;

            penalty := m_dictionary.get_candidate_penalty(pinyin_key, candidates[idx].text);
            if penalty > 0 then
            begin
                if (candidates[idx].comment = '') and
                    ((candidates[idx].source = cs_user) or
                    (not m_dictionary.is_base_entry(pinyin_key, candidates[idx].text))) then
                begin
                    Continue;
                end;
                Dec(candidates[idx].score, penalty);
            end;
            if write_idx <> idx then
            begin
                candidates[write_idx] := candidates[idx];
            end;
            Inc(write_idx);
        end;

        if write_idx < Length(candidates) then
        begin
            SetLength(candidates, write_idx);
        end;
    end;

    procedure merge_confirmed_prefix_user_extensions(var candidates: TncCandidateList);
    const
        c_confirmed_user_extension_bonus = 480;
    var
        normalized_current: string;
        confirmed_pinyin: string;
        full_query: string;
        full_candidates: TncCandidateList;
        confirmed_prefix_text: string;
        idx: Integer;
        seg_idx: Integer;
        existing_idx: Integer;
        candidate: TncCandidate;
        extension_text: string;
    begin
        if (m_dictionary = nil) or (m_composition_text = '') then
        begin
            Exit;
        end;
        if (m_confirmed_text = '') or (m_confirmed_segments = nil) or (m_confirmed_segments.Count = 0) then
        begin
            Exit;
        end;

        normalized_current := normalize_pinyin_text(m_composition_text);
        if normalized_current = '' then
        begin
            Exit;
        end;

        confirmed_pinyin := '';
        for seg_idx := 0 to m_confirmed_segments.Count - 1 do
        begin
            if m_confirmed_segments[seg_idx].pinyin <> '' then
            begin
                confirmed_pinyin := confirmed_pinyin + normalize_pinyin_text(m_confirmed_segments[seg_idx].pinyin);
            end;
        end;
        if confirmed_pinyin = '' then
        begin
            Exit;
        end;

        full_query := confirmed_pinyin + normalized_current;
        if not dictionary_lookup_cached(full_query, full_candidates) then
        begin
            Exit;
        end;
        if Length(full_candidates) = 0 then
        begin
            Exit;
        end;

        confirmed_prefix_text := m_confirmed_text;
        for idx := 0 to High(full_candidates) do
        begin
            candidate := full_candidates[idx];
            if candidate.source <> cs_user then
            begin
                Continue;
            end;

            if (candidate.text = '') or (Length(candidate.text) <= Length(confirmed_prefix_text)) then
            begin
                Continue;
            end;
            if Copy(candidate.text, 1, Length(confirmed_prefix_text)) <> confirmed_prefix_text then
            begin
                Continue;
            end;

            extension_text := Copy(candidate.text, Length(confirmed_prefix_text) + 1, Length(candidate.text));
            extension_text := Trim(extension_text);
            if extension_text = '' then
            begin
                Continue;
            end;

            existing_idx := -1;
            for seg_idx := 0 to High(candidates) do
            begin
                if SameText(candidates[seg_idx].text, extension_text) then
                begin
                    existing_idx := seg_idx;
                    Break;
                end;
            end;

            if existing_idx >= 0 then
            begin
                if candidates[existing_idx].comment <> '' then
                begin
                    candidates[existing_idx].comment := '';
                end;
                if candidates[existing_idx].score < candidate.score + c_confirmed_user_extension_bonus then
                begin
                    candidates[existing_idx].score := candidate.score + c_confirmed_user_extension_bonus;
                end;
                candidates[existing_idx].source := cs_user;
                candidates[existing_idx].has_dict_weight := False;
                candidates[existing_idx].dict_weight := 0;
            end
            else
            begin
                candidate.text := extension_text;
                candidate.comment := '';
                candidate.source := cs_user;
                candidate.has_dict_weight := False;
                candidate.dict_weight := 0;
                Inc(candidate.score, c_confirmed_user_extension_bonus);
                SetLength(candidates, Length(candidates) + 1);
                candidates[High(candidates)] := candidate;
            end;
        end;
    end;

    procedure prioritize_complete_phrase_matches(var candidates: TncCandidateList);
    const
        c_complete_phrase_bonus = 220;
        c_complete_phrase_unit_bonus = 36;
        c_partial_with_complete_penalty = 620;
        c_exact_length_bonus = 260;
        c_near_length_bonus = 80;
        c_exact_dict_complete_bonus = 360;
        c_near_dict_complete_bonus = 96;
        c_non_dict_complete_with_exact_dict_penalty = 320;
        c_partial_with_exact_dict_penalty = 180;
        c_short_complete_gap_penalty = 180;
        c_single_char_complete_penalty = 520;
        c_short_partial_penalty = 180;
        c_single_char_partial_penalty = 360;
        c_strong_exact_dict_complete_weight = 700;
        c_weak_complete_against_strong_exact_penalty = 4200;
        c_overlong_complete_full_key_penalty = 5200;
    var
        idx: Integer;
        input_syllables: Integer;
        text_units: Integer;
        effective_units: Integer;
        has_complete_phrase: Boolean;
        has_exact_dict_complete: Boolean;
        has_near_dict_complete: Boolean;
        syllable_gap: Integer;
        strongest_exact_dict_complete_weight: Integer;
        dict_weight_value: Integer;
        function get_dict_complete_confidence_bonus(const candidate: TncCandidate): Integer;
        var
            dict_weight_value: Integer;
            candidate_units: Integer;
        begin
            Result := 0;
            if (not candidate.has_dict_weight) or (input_syllables < 3) then
            begin
                Exit;
            end;

            candidate_units := get_text_unit_count(Trim(candidate.text));
            if candidate_units < 3 then
            begin
                Exit;
            end;

            dict_weight_value := candidate.dict_weight;
            if candidate_units = input_syllables then
            begin
                if dict_weight_value >= 760 then
                begin
                    Result := 180;
                end
                else if dict_weight_value >= 620 then
                begin
                    Result := 128;
                end
                else if dict_weight_value >= 480 then
                begin
                    Result := 84;
                end
                else if dict_weight_value >= 360 then
                begin
                    Result := 40;
                end;
            end
            else if (input_syllables >= 4) and (candidate_units + 1 = input_syllables) then
            begin
                if dict_weight_value >= 720 then
                begin
                    Result := 72;
                end
                else if dict_weight_value >= 560 then
                begin
                    Result := 48;
                end
                else if dict_weight_value >= 420 then
                begin
                    Result := 24;
                end;
            end;
        end;
    begin
        if (not has_multi_syllable_input) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        input_syllables := get_input_syllable_count;
        if input_syllables < 2 then
        begin
            Exit;
        end;

        has_complete_phrase := False;
        has_exact_dict_complete := False;
        has_near_dict_complete := False;
        strongest_exact_dict_complete_weight := 0;
        for idx := 0 to High(candidates) do
        begin
            if candidates[idx].comment <> '' then
            begin
                Continue;
            end;

            text_units := get_text_unit_count(Trim(candidates[idx].text));
            if text_units >= 2 then
            begin
                has_complete_phrase := True;
                if candidates[idx].has_dict_weight then
                begin
                    dict_weight_value := candidates[idx].dict_weight;
                    syllable_gap := input_syllables - text_units;
                    if syllable_gap = 0 then
                    begin
                        has_exact_dict_complete := True;
                        if dict_weight_value > strongest_exact_dict_complete_weight then
                        begin
                            strongest_exact_dict_complete_weight := dict_weight_value;
                        end;
                    end
                    else if (input_syllables >= 4) and (syllable_gap = 1) then
                    begin
                        has_near_dict_complete := True;
                    end;
                end;
            end;
        end;

        if not has_complete_phrase then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            text_units := get_text_unit_count(Trim(candidates[idx].text));
            if text_units <= 0 then
            begin
                Continue;
            end;

            if (candidates[idx].comment = '') and (text_units >= 2) then
            begin
                effective_units := text_units;
                if effective_units > input_syllables then
                begin
                    effective_units := input_syllables;
                end;
                Inc(candidates[idx].score,
                    c_complete_phrase_bonus + (effective_units * c_complete_phrase_unit_bonus));
                if input_syllables >= 3 then
                begin
                    syllable_gap := input_syllables - text_units;
                    if syllable_gap = 0 then
                    begin
                        Inc(candidates[idx].score, c_exact_length_bonus);
                    end
                    else if syllable_gap = 1 then
                    begin
                        Inc(candidates[idx].score, c_near_length_bonus);
                    end
                    else if syllable_gap > 1 then
                    begin
                        Dec(candidates[idx].score, syllable_gap * c_short_complete_gap_penalty);
                    end
                    else
                    begin
                        Dec(candidates[idx].score, Abs(syllable_gap) * (c_short_complete_gap_penalty div 2));
                    end;

                    if (syllable_gap < 0) and is_full_pinyin_key(lookup_text) then
                    begin
                        Dec(candidates[idx].score,
                            c_overlong_complete_full_key_penalty +
                            (Abs(syllable_gap) * c_short_complete_gap_penalty));
                    end;
                end;

                if input_syllables >= 3 then
                begin
                    if candidates[idx].has_dict_weight then
                    begin
                        if (strongest_exact_dict_complete_weight >=
                            c_strong_exact_dict_complete_weight) and
                            (text_units = input_syllables) and
                            (candidates[idx].dict_weight <
                            strongest_exact_dict_complete_weight - 300) then
                        begin
                            Dec(candidates[idx].score,
                                c_weak_complete_against_strong_exact_penalty);
                        end;
                        if text_units = input_syllables then
                        begin
                            Inc(candidates[idx].score, c_exact_dict_complete_bonus);
                            if input_syllables <= 4 then
                            begin
                                Inc(candidates[idx].score, 220);
                            end;
                        end
                        else if (input_syllables >= 4) and (text_units + 1 = input_syllables) then
                        begin
                            Inc(candidates[idx].score, c_near_dict_complete_bonus);
                            if input_syllables <= 4 then
                            begin
                                Inc(candidates[idx].score, 80);
                            end;
                        end;
                        Inc(candidates[idx].score, get_dict_complete_confidence_bonus(candidates[idx]));
                    end
                    else if has_exact_dict_complete then
                    begin
                        if text_units = input_syllables then
                        begin
                            Dec(candidates[idx].score, c_non_dict_complete_with_exact_dict_penalty);
                            if input_syllables <= 4 then
                            begin
                                Dec(candidates[idx].score, 220);
                            end;
                        end
                        else if (input_syllables >= 4) and (text_units + 1 = input_syllables) then
                        begin
                            Dec(candidates[idx].score,
                                c_non_dict_complete_with_exact_dict_penalty div 2);
                            if input_syllables <= 4 then
                            begin
                                Dec(candidates[idx].score, 80);
                            end;
                        end;
                    end
                    else if has_near_dict_complete and (input_syllables >= 4) and
                        (text_units + 1 >= input_syllables) then
                    begin
                        Dec(candidates[idx].score, c_near_dict_complete_bonus div 2);
                    end;
                end;
            end
            else if (candidates[idx].comment = '') and (input_syllables >= 3) and (text_units = 1) then
            begin
                Dec(candidates[idx].score, c_single_char_complete_penalty);
            end
            else if candidates[idx].comment <> '' then
            begin
                Dec(candidates[idx].score, c_partial_with_complete_penalty);
                if has_exact_dict_complete and (input_syllables >= 3) then
                begin
                    Dec(candidates[idx].score, c_partial_with_exact_dict_penalty);
                end;
                if input_syllables >= 3 then
                begin
                    if text_units <= 1 then
                    begin
                        Dec(candidates[idx].score, c_single_char_partial_penalty);
                    end
                    else if text_units + 1 < input_syllables then
                    begin
                        Dec(candidates[idx].score, c_short_partial_penalty);
                    end;
                end;
            end;
        end;
    end;

    procedure apply_syllable_single_char_alignment_bonus(var candidates: TncCandidateList);
    const
        c_rank_window = 10;
        c_rank_step = 10;
        c_missing_rank_penalty = 48;
        c_alignment_average_divisor = 1;
        c_alignment_adjust_cap = 120;
        c_alignment_score_gap_limit = 140;
    var
        syllables: TncPinyinParseResult;
        unit_rank_maps: TArray<TDictionary<string, Integer>>;
        lookup_results: TncCandidateList;
        candidate_units: TArray<string>;
        candidate_text: string;
        unit_text: string;
        syllable_idx: Integer;
        idx: Integer;
        rank_idx: Integer;
        found_rank: Integer;
        adjustment: Integer;
        rank_data_count: Integer;
        applied_count: Integer;
        top_complete_score: Integer;
        has_top_complete: Boolean;

        function is_valid_full_syllable(const syllable: string): Boolean;
        var
            ch: Char;
        begin
            Result := False;
            if syllable = '' then
            begin
                Exit;
            end;

            if Length(syllable) = 1 then
            begin
                Result := CharInSet(syllable[1], ['a', 'e', 'o']);
                Exit;
            end;

            for ch in syllable do
            begin
                if CharInSet(ch, ['a', 'e', 'i', 'o', 'u', 'v']) then
                begin
                    Result := True;
                    Exit;
                end;
            end;
        end;
    begin
        if (not has_multi_syllable_input) or (m_dictionary = nil) or (Length(candidates) = 0) then
        begin
            Exit;
        end;

        syllables := get_effective_compact_pinyin_syllables(m_composition_text);

        if Length(syllables) < 2 then
        begin
            Exit;
        end;
        if Length(syllables) > 2 then
        begin
            // Keep long phrases governed by phrase weight; use this only for
            // short same-pinyin disambiguation where common single-char choice matters.
            Exit;
        end;

        // Only apply this calibration to complete full-syllable queries.
        for idx := 0 to High(syllables) do
        begin
            if not is_valid_full_syllable(syllables[idx].text) then
            begin
                Exit;
            end;
        end;

        SetLength(unit_rank_maps, Length(syllables));
        try
            rank_data_count := 0;
            for syllable_idx := 0 to High(syllables) do
            begin
                unit_rank_maps[syllable_idx] := TDictionary<string, Integer>.Create;

                if not dictionary_lookup_cached(syllables[syllable_idx].text, lookup_results) then
                begin
                    unit_rank_maps[syllable_idx].Free;
                    unit_rank_maps[syllable_idx] := nil;
                    Continue;
                end;

                rank_idx := 0;
                for idx := 0 to High(lookup_results) do
                begin
                    unit_text := Trim(lookup_results[idx].text);
                    if not is_single_text_unit(unit_text) then
                    begin
                        Continue;
                    end;

                    if not unit_rank_maps[syllable_idx].ContainsKey(unit_text) then
                    begin
                        unit_rank_maps[syllable_idx].Add(unit_text, rank_idx);
                        Inc(rank_idx);
                        if rank_idx >= c_rank_window then
                        begin
                            Break;
                        end;
                    end;
                end;

                if unit_rank_maps[syllable_idx].Count = 0 then
                begin
                    unit_rank_maps[syllable_idx].Free;
                    unit_rank_maps[syllable_idx] := nil;
                    Continue;
                end;

                Inc(rank_data_count);
            end;

            if rank_data_count <= 0 then
            begin
                Exit;
            end;

            has_top_complete := False;
            top_complete_score := 0;
            for idx := 0 to High(candidates) do
            begin
                if candidates[idx].comment <> '' then
                begin
                    Continue;
                end;

                if (not has_top_complete) or (candidates[idx].score > top_complete_score) then
                begin
                    top_complete_score := candidates[idx].score;
                    has_top_complete := True;
                end;
            end;

            for idx := 0 to High(candidates) do
            begin
                if candidates[idx].comment <> '' then
                begin
                    Continue;
                end;
                if candidates[idx].source = cs_user then
                begin
                    Continue;
                end;
                if has_top_complete and
                    (candidates[idx].score < (top_complete_score - c_alignment_score_gap_limit)) then
                begin
                    // Keep lexical phrase weight as the primary signal; alignment only
                    // acts as a mild tie-breaker among near-head same-pinyin candidates.
                    Continue;
                end;

                candidate_text := Trim(candidates[idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;

                candidate_units := split_text_units(candidate_text);
                if Length(candidate_units) <> Length(syllables) then
                begin
                    Continue;
                end;

                adjustment := 0;
                applied_count := 0;
                for syllable_idx := 0 to High(syllables) do
                begin
                    if unit_rank_maps[syllable_idx] = nil then
                    begin
                        Continue;
                    end;

                    Inc(applied_count);
                    if unit_rank_maps[syllable_idx].TryGetValue(candidate_units[syllable_idx], found_rank) then
                    begin
                        Inc(adjustment, (c_rank_window - found_rank) * c_rank_step);
                    end
                    else
                    begin
                        Dec(adjustment, c_missing_rank_penalty);
                    end;
                end;

                if applied_count <= 0 then
                begin
                    Continue;
                end;

                adjustment := adjustment div applied_count;
                adjustment := adjustment div c_alignment_average_divisor;
                if adjustment > c_alignment_adjust_cap then
                begin
                    adjustment := c_alignment_adjust_cap;
                end
                else if adjustment < -c_alignment_adjust_cap then
                begin
                    adjustment := -c_alignment_adjust_cap;
                end;

                Inc(candidates[idx].score, adjustment);
            end;
        finally
            for syllable_idx := 0 to High(unit_rank_maps) do
            begin
                if unit_rank_maps[syllable_idx] <> nil then
                begin
                    unit_rank_maps[syllable_idx].Free;
                end;
            end;
        end;
    end;

    procedure apply_two_single_exact_merge_penalty(var candidates: TncCandidateList);
    const
        c_exact_probe_limit = 6;
        c_exact_min_weight = 420;
        c_two_single_exact_penalty = 920;
    var
        syllables_local: TncPinyinParseResult;
        idx: Integer;
        text_units: TArray<string>;
        unit_idx: Integer;
        current_text: string;
        next_text: string;
        combined_text: string;
        exact_text: string;
        exact_weight: Integer;
        exact_text_by_unit: TArray<string>;
        exact_weight_by_unit: TArray<Integer>;
        has_exact_by_unit: TArray<Boolean>;
        unit_matches_syllable: TArray<Boolean>;
        has_tracked_segment_path: Boolean;
        adjustment: Integer;
        preferred: Boolean;

        function get_fixed_bridge_single_char_local(
            const syllable_text: string): string;
        var
            normalized_syllable: string;
        begin
            Result := '';
            normalized_syllable := normalize_pinyin_text(syllable_text);
            if (normalized_syllable = 'ta') or
                (normalized_syllable = 'qing') or
                (normalized_syllable = 'bei') then
            begin
                Exit;
            end;
            if normalized_syllable = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable = 'ba' then
            begin
                Result := string(Char($628A));
            end
            else if normalized_syllable = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable = 'zhe' then
            begin
                Result := string(Char($8FD9));
            end
            else if normalized_syllable = 'que' then
            begin
                Result := string(Char($5374));
            end
            else if normalized_syllable = 'bing' then
            begin
                Result := string(Char($5E76));
            end
            else if normalized_syllable = 'qie' then
            begin
                Result := string(Char($4E14));
            end
            else if normalized_syllable = 'qing' then
            begin
                Result := string(Char($8BF7));
            end;
        end;

        function try_get_best_two_syllable_exact_text(const start_idx: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        var
            query_key: string;
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_units: Integer;
            weight_value: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (start_idx < 0) or (start_idx + 1 > High(syllables_local)) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(syllables_local[start_idx].text) +
                normalize_pinyin_text(syllables_local[start_idx + 1].text);
            if (query_key = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key,
                exact_results)) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_results) do
            begin
                if exact_idx >= c_exact_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results[exact_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text := Trim(exact_results[exact_idx].text);
                candidate_units := get_candidate_text_unit_count(candidate_text);
                if candidate_units <> 2 then
                begin
                    Continue;
                end;

                if exact_results[exact_idx].has_dict_weight then
                begin
                    weight_value := exact_results[exact_idx].dict_weight;
                end
                else
                begin
                    weight_value := exact_results[exact_idx].score;
                end;

                if weight_value > out_weight then
                begin
                    out_text := candidate_text;
                    out_weight := weight_value;
                    Result := True;
                end;
            end;

        end;

    begin
        if (Length(candidates) = 0) or (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 4) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(m_last_lookup_key));
        if Length(syllables_local) <> m_last_lookup_syllable_count then
        begin
            Exit;
        end;

        SetLength(exact_text_by_unit, Max(0, Length(syllables_local) - 1));
        SetLength(exact_weight_by_unit, Length(exact_text_by_unit));
        SetLength(has_exact_by_unit, Length(exact_text_by_unit));
        for unit_idx := 0 to High(exact_text_by_unit) do
        begin
            has_exact_by_unit[unit_idx] :=
                try_get_best_two_syllable_exact_text(unit_idx, exact_text,
                exact_weight);
            if has_exact_by_unit[unit_idx] then
            begin
                exact_text_by_unit[unit_idx] := exact_text;
                exact_weight_by_unit[unit_idx] := exact_weight;
            end;
        end;

        for idx := 0 to High(candidates) do
        begin
            if Trim(candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            text_units := split_text_units(Trim(candidates[idx].text));
            if Length(text_units) <> Length(syllables_local) then
            begin
                Continue;
            end;

            // A tracked path was built from query-aligned dictionary edges, so
            // revalidating every character against the same syllable is redundant.
            has_tracked_segment_path := get_segment_path_for_candidate(
                candidates[idx], idx) <> '';
            SetLength(unit_matches_syllable, Length(text_units));
            for unit_idx := 0 to High(text_units) do
            begin
                unit_matches_syllable[unit_idx] :=
                    (get_fixed_bridge_single_char_local(
                    syllables_local[unit_idx].text) = '') and
                    (has_tracked_segment_path or
                    match_single_char_candidate_for_syllable(
                    syllables_local[unit_idx].text,
                    Trim(text_units[unit_idx]), preferred));
            end;

            adjustment := 0;
            for unit_idx := 0 to High(text_units) - 1 do
            begin
                if unit_idx + 1 > High(text_units) then
                begin
                    Break;
                end;

                current_text := Trim(text_units[unit_idx]);
                next_text := Trim(text_units[unit_idx + 1]);
                if (not unit_matches_syllable[unit_idx]) or
                    (not unit_matches_syllable[unit_idx + 1]) then
                begin
                    Continue;
                end;

                if has_exact_by_unit[unit_idx] and
                    (exact_weight_by_unit[unit_idx] >= c_exact_min_weight) then
                begin
                    combined_text := current_text + next_text;
                    if not SameText(combined_text,
                        exact_text_by_unit[unit_idx]) then
                    begin
                        Dec(adjustment, c_two_single_exact_penalty);
                    end;
                end;
            end;

            if adjustment <> 0 then
            begin
                Inc(candidates[idx].score, adjustment);
            end;
        end;
    end;

    function get_complete_exact_tail_phrase_weight_local(
        const tail_syllable_count: Integer): Integer;
    var
        syllables_local: TncPinyinParseResult;
        tail_key_local: string;
        idx_local: Integer;
        exact_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
        candidate_weight_local: Integer;
    begin
        Result := 0;
        if (m_dictionary = nil) or (tail_syllable_count < 2) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < tail_syllable_count then
        begin
            Exit;
        end;

        tail_key_local := '';
        for idx_local := Length(syllables_local) - tail_syllable_count to
            High(syllables_local) do
        begin
            tail_key_local := tail_key_local +
                normalize_pinyin_text(syllables_local[idx_local].text);
        end;
        if tail_key_local = '' then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(tail_key_local,
            exact_results_local) then
        begin
            Exit;
        end;

        for result_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(exact_results_local[result_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                tail_syllable_count) then
            begin
                Continue;
            end;
            candidate_weight_local := exact_results_local[result_idx_local].score;
            if exact_results_local[result_idx_local].has_dict_weight and
                (exact_results_local[result_idx_local].dict_weight >
                candidate_weight_local) then
            begin
                candidate_weight_local :=
                    exact_results_local[result_idx_local].dict_weight;
            end;
            if candidate_weight_local > Result then
            begin
                Result := candidate_weight_local;
            end;
        end;
    end;

    function has_complete_exact_phrase_before_tail_local(
        const phrase_syllable_count: Integer): Boolean;
    var
        syllables_local: TncPinyinParseResult;
        phrase_key_local: string;
        start_idx_local: Integer;
        idx_local: Integer;
        exact_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (phrase_syllable_count < 2) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <= phrase_syllable_count then
        begin
            Exit;
        end;

        start_idx_local := Length(syllables_local) - phrase_syllable_count - 1;
        if start_idx_local < 0 then
        begin
            Exit;
        end;

        phrase_key_local := '';
        for idx_local := start_idx_local to
            start_idx_local + phrase_syllable_count - 1 do
        begin
            phrase_key_local := phrase_key_local +
                normalize_pinyin_text(syllables_local[idx_local].text);
        end;
        if phrase_key_local = '' then
        begin
            Exit;
        end;

        if not lookup_exact_full_pinyin_cached_local(phrase_key_local,
            exact_results_local) then
        begin
            Exit;
        end;

        for result_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text_local := Trim(exact_results_local[result_idx_local].text);
            if (candidate_text_local <> '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                phrase_syllable_count) then
            begin
                Exit(True);
            end;
        end;
    end;

    function query_has_modal_tail_suffix_local(const query_text: string): Boolean;
    var
        parser_local: TncPinyinParser;
        parts_local: TncPinyinParseResult;
        tail_text_local: string;
    begin
        Result := False;
        parser_local := TncPinyinParser.Create;
        try
            parts_local := parser_local.parse(normalize_pinyin_text(query_text));
        finally
            parser_local.Free;
        end;
        if Length(parts_local) = 0 then
        begin
            Exit;
        end;

        tail_text_local := normalize_pinyin_text(parts_local[High(parts_local)].text);
        Result := (tail_text_local = 'ba') or
            (tail_text_local = 'ma') or
            (tail_text_local = 'ne') or
            (tail_text_local = 'a') or
            (tail_text_local = 'ya') or
            (tail_text_local = 'la') or
            (tail_text_local = 'le') or
            (tail_text_local = 'wa');
    end;

    function try_get_modal_tail_info_for_query_local(const query_text: string;
        out out_tail_key: string; out out_tail_text: string): Boolean;
    var
        parser_local: TncPinyinParser;
        parts_local: TncPinyinParseResult;
    begin
        Result := False;
        out_tail_key := '';
        out_tail_text := '';
        parser_local := TncPinyinParser.Create;
        try
            parts_local := parser_local.parse(normalize_pinyin_text(query_text));
        finally
            parser_local.Free;
        end;
        if Length(parts_local) = 0 then
        begin
            Exit;
        end;

        out_tail_key := normalize_pinyin_text(parts_local[High(parts_local)].text);
        if out_tail_key = 'ba' then
        begin
            out_tail_text := string(Char($5427));
        end
        else if out_tail_key = 'ma' then
        begin
            out_tail_text := string(Char($5417));
        end
        else if out_tail_key = 'ne' then
        begin
            out_tail_text := string(Char($5462));
        end
        else if out_tail_key = 'a' then
        begin
            out_tail_text := string(Char($554A));
        end
        else if out_tail_key = 'ya' then
        begin
            out_tail_text := string(Char($5440));
        end
        else if out_tail_key = 'la' then
        begin
            out_tail_text := string(Char($5566));
        end
        else if out_tail_key = 'le' then
        begin
            out_tail_text := string(Char($4E86));
        end
        else if out_tail_key = 'wa' then
        begin
            out_tail_text := string(Char($54C7));
        end;
        Result := out_tail_text <> '';
    end;

    procedure ensure_fast_modal_tail_candidate_visible_local(
        var candidates: TncCandidateList);
    const
        c_fast_modal_tail_bonus = 1800;
    var
        tail_key_local: string;
        expected_tail_text_local: string;
        idx_local: Integer;
        move_idx_local: Integer;
        existing_idx_local: Integer;
        best_idx_local: Integer;
        best_score_local: Integer;
        candidate_units_local: TArray<string>;
        unit_idx_local: Integer;
        corrected_head_text_local: string;
        corrected_text_local: string;
        corrected_path_local: string;
        candidate_local: TncCandidate;
        partial_candidate_local: TncCandidate;
        partial_exists_local: Boolean;
    begin
        if (Length(candidates) = 0) or (input_syllable_count < 4) or
            (not is_full_pinyin_key(lookup_text)) or
            (not try_get_modal_tail_info_for_query_local(lookup_text,
            tail_key_local, expected_tail_text_local)) then
        begin
            Exit;
        end;

        existing_idx_local := -1;
        best_idx_local := -1;
        best_score_local := Low(Integer);
        corrected_head_text_local := '';
        corrected_text_local := '';
        for idx_local := 0 to High(candidates) do
        begin
            if Trim(candidates[idx_local].comment) = '' then
            begin
                if get_candidate_text_unit_count(Trim(candidates[idx_local].text)) <>
                    input_syllable_count then
                begin
                    Continue;
                end;

                candidate_units_local := split_text_units(Trim(candidates[idx_local].text));
                if Length(candidate_units_local) <> input_syllable_count then
                begin
                    Continue;
                end;

                corrected_head_text_local := '';
                for unit_idx_local := 0 to High(candidate_units_local) - 1 do
                begin
                    corrected_head_text_local := corrected_head_text_local +
                        candidate_units_local[unit_idx_local];
                end;
                corrected_text_local := corrected_head_text_local +
                    expected_tail_text_local;
                if SameText(candidate_units_local[High(candidate_units_local)],
                    expected_tail_text_local) then
                begin
                    existing_idx_local := idx_local;
                    Break;
                end;
            end
            else if SameText(normalize_pinyin_text(Trim(candidates[idx_local].comment)),
                tail_key_local) and
                (get_candidate_text_unit_count(Trim(candidates[idx_local].text)) =
                input_syllable_count - 1) then
            begin
                corrected_head_text_local := Trim(candidates[idx_local].text);
                corrected_text_local := corrected_head_text_local +
                    expected_tail_text_local;
            end
            else
            begin
                Continue;
            end;

            if (corrected_text_local <> '') and
                (candidates[idx_local].score > best_score_local) then
            begin
                best_score_local := candidates[idx_local].score;
                best_idx_local := idx_local;
            end;
        end;

        if existing_idx_local > 0 then
        begin
            candidate_local := candidates[existing_idx_local];
            candidate_local.source := cs_rule;
            Inc(candidate_local.score, c_fast_modal_tail_bonus);
            candidate_local.has_dict_weight := True;
            candidate_local.dict_weight := Max(candidate_local.dict_weight,
                candidate_local.score);
            for move_idx_local := existing_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := candidate_local;
            Exit;
        end
        else if existing_idx_local = 0 then
        begin
            candidates[0].source := cs_rule;
            Inc(candidates[0].score, c_fast_modal_tail_bonus);
            candidates[0].has_dict_weight := True;
            candidates[0].dict_weight := Max(candidates[0].dict_weight,
                candidates[0].score);
            Exit;
        end;

        if (best_idx_local < 0) or (corrected_head_text_local = '') or
            (corrected_text_local = '') then
        begin
            Exit;
        end;

        partial_exists_local := False;
        for idx_local := 0 to High(candidates) do
        begin
            if SameText(Trim(candidates[idx_local].text), corrected_head_text_local) and
                SameText(normalize_pinyin_text(Trim(candidates[idx_local].comment)),
                tail_key_local) then
            begin
                partial_exists_local := True;
                Break;
            end;
        end;

        candidate_local := candidates[best_idx_local];
        candidate_local.text := corrected_text_local;
        candidate_local.comment := '';
        candidate_local.source := cs_rule;
        candidate_local.score := Max(candidate_local.score + c_fast_modal_tail_bonus,
            candidates[0].score + c_fast_modal_tail_bonus);
        candidate_local.has_dict_weight := True;
        candidate_local.dict_weight := candidate_local.score;
        corrected_path_local := corrected_head_text_local + c_segment_path_separator +
            expected_tail_text_local;
        remember_segment_path_for_candidate(candidate_local.text, '',
            corrected_path_local, candidate_local.score);

        if not partial_exists_local then
        begin
            partial_candidate_local := candidate_local;
            partial_candidate_local.text := corrected_head_text_local;
            partial_candidate_local.comment := tail_key_local;
            Dec(partial_candidate_local.score, 320);
            remember_segment_path_for_candidate(partial_candidate_local.text,
                partial_candidate_local.comment, corrected_head_text_local,
                partial_candidate_local.score);
            SetLength(candidates, Length(candidates) + 1);
            candidates[High(candidates)] := partial_candidate_local;
        end;

        SetLength(candidates, Length(candidates) + 1);
        for move_idx_local := High(candidates) downto 1 do
        begin
            candidates[move_idx_local] := candidates[move_idx_local - 1];
        end;
        candidates[0] := candidate_local;
    end;

    procedure refresh_debug_top_candidate_summary_local;
    var
        marker_pos: Integer;
        scan_pos: Integer;
        delete_len: Integer;
        replacement_text: string;
    begin
        if (not m_config.debug_mode) or (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        replacement_text := ' ' + get_candidate_debug_summary(m_candidates[0]);
        marker_pos := Pos(' top=[', m_last_lookup_debug_extra);
        if marker_pos <= 0 then
        begin
            m_last_lookup_debug_extra := m_last_lookup_debug_extra + replacement_text;
            Exit;
        end;

        scan_pos := marker_pos + Length(' top=[');
        while (scan_pos <= Length(m_last_lookup_debug_extra)) and
            (m_last_lookup_debug_extra[scan_pos] <> ']') do
        begin
            Inc(scan_pos);
        end;
        if scan_pos > Length(m_last_lookup_debug_extra) then
        begin
            Exit;
        end;

        delete_len := scan_pos - marker_pos + 1;
        Delete(m_last_lookup_debug_extra, marker_pos, delete_len);
        Insert(replacement_text, m_last_lookup_debug_extra, marker_pos);
    end;

    function try_build_exact_two_syllable_head_partial_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    const
        c_head_partial_base_score = 36000;
        c_head_partial_bonus = 1800;
        c_probe_limit = 6;
    var
        syllables_local: TncPinyinParseResult;
        syllable_texts_local: TArray<string>;
        comment_parts_local: TArray<string>;
        head_key_local: string;
        tail_comment_local: string;
        head_results_local: TncCandidateList;
        result_idx_local: Integer;
        best_idx_local: Integer;
        best_score_local: Integer;
        candidate_text_local: string;
        candidate_score_local: Integer;
        idx_local: Integer;
        normalized_part_local: string;
        normalized_comment_local: string;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := 0;

        if (m_dictionary = nil) or
            ((not is_full_pinyin_key(lookup_text)) and
            (not has_safe_trailing_initial_typing_state)) or
            (input_syllable_count < 3) or (input_syllable_count > 4) or
            query_has_modal_tail_suffix_local(lookup_text) or
            ((not query_has_modal_tail_suffix_local(lookup_text)) and
            has_full_query_exact_complete_candidate_local) then
        begin
            Exit;
        end;

        SetLength(syllable_texts_local, 0);
        if Trim(fallback_comment) <> '' then
        begin
            normalized_comment_local := Trim(fallback_comment);
            comment_parts_local := normalized_comment_local.Split([' ']);
            for idx_local := 0 to High(comment_parts_local) do
            begin
                normalized_part_local := normalize_pinyin_text(
                    comment_parts_local[idx_local]);
                if normalized_part_local = '' then
                begin
                    Continue;
                end;
                SetLength(syllable_texts_local, Length(syllable_texts_local) + 1);
                syllable_texts_local[High(syllable_texts_local)] :=
                    normalized_part_local;
            end;
        end;

        if Length(syllable_texts_local) <> input_syllable_count then
        begin
            syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
            SetLength(syllable_texts_local, Length(syllables_local));
            for idx_local := 0 to High(syllables_local) do
            begin
                syllable_texts_local[idx_local] :=
                    normalize_pinyin_text(syllables_local[idx_local].text);
            end;
        end;

        if Length(syllable_texts_local) <> input_syllable_count then
        begin
            Exit;
        end;

        head_key_local := syllable_texts_local[0] + syllable_texts_local[1];
        tail_comment_local := '';
        for idx_local := 2 to High(syllable_texts_local) do
        begin
            tail_comment_local := tail_comment_local + syllable_texts_local[idx_local];
        end;
        if (head_key_local = '') or (tail_comment_local = '') then
        begin
            Exit;
        end;

        if not dictionary_exact_lookup_cached(head_key_local, head_results_local) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_score_local := Low(Integer);
        for result_idx_local := 0 to High(head_results_local) do
        begin
            if result_idx_local >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(head_results_local[result_idx_local].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(head_results_local[result_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <> 2) then
            begin
                Continue;
            end;

            candidate_score_local := head_results_local[result_idx_local].score;
            if head_results_local[result_idx_local].has_dict_weight and
                (head_results_local[result_idx_local].dict_weight >
                candidate_score_local) then
            begin
                candidate_score_local := head_results_local[result_idx_local].dict_weight;
            end;
            if candidate_score_local > best_score_local then
            begin
                best_score_local := candidate_score_local;
                best_idx_local := result_idx_local;
            end;
        end;

        if best_idx_local < 0 then
        begin
            Exit;
        end;

        out_candidate := head_results_local[best_idx_local];
        out_candidate.comment := tail_comment_local;
        out_candidate.score := c_head_partial_base_score + best_score_local +
            c_head_partial_bonus - ((input_syllable_count - 2) * 80);
        out_candidate.dict_weight := out_candidate.score;
        out_candidate.has_dict_weight := True;
        Result := True;
    end;

    function try_build_fragment_prefix_partial_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    const
        c_max_span = 3;
        c_dict_span_probe_limit = 12;
        c_dict_span_min_weight = 420;
        c_prefix_base_score = 18000;
        c_phrase_span_bonus = 1450;
        c_dict_span_bonus = 950;
        c_single_bonus = 220;
    var
        syllables_local: TncPinyinParseResult;
        prefix_count_local: Integer;
        pos_local: Integer;
        span_len_local: Integer;
        tail_idx_local: Integer;
        phrase_count_local: Integer;
        last_span_len_local: Integer;
        text_local: string;
        tail_comment_local: string;
        span_text_local: string;
        span_bonus_local: Integer;
        span_is_phrase_local: Boolean;
        single_text_local: string;
        single_score_local: Integer;
        last_single_total_score_local: Integer;
        tail_is_fragment_local: Boolean;
        text_units_local: TArray<string>;
        unit_idx_local: Integer;

        function build_query_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to local_start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function is_stable_boundary_single_tail_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'ta') or
                (normalized_syllable_local = 'he') or
                (normalized_syllable_local = 'qu') or
                (normalized_syllable_local = 'shi') or
                (normalized_syllable_local = 'de') or
                (normalized_syllable_local = 'zai') or
                (normalized_syllable_local = 'ye') or
                (normalized_syllable_local = 'mei') or
                (normalized_syllable_local = 'le') or
                (normalized_syllable_local = 'yi');
        end;

        function get_fixed_single_text_local(const syllable_text: string): string;
        var
            normalized_syllable_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if normalized_syllable_local = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable_local = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable_local = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable_local = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable_local = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable_local = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable_local = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable_local = 'bu' then
            begin
                Result := string(Char($4E0D));
            end
            else if normalized_syllable_local = 'hen' then
            begin
                Result := string(Char($5F88));
            end
            else if normalized_syllable_local = 'geng' then
            begin
                Result := string(Char($66F4));
            end
            else if normalized_syllable_local = 'zui' then
            begin
                Result := string(Char($6700));
            end
            else if normalized_syllable_local = 'tai' then
            begin
                Result := string(Char($592A));
            end
            else if normalized_syllable_local = 'yao' then
            begin
                Result := string(Char($8981));
            end
            else if normalized_syllable_local = 'neng' then
            begin
                Result := string(Char($80FD));
            end
            else if normalized_syllable_local = 'ke' then
            begin
                Result := string(Char($53EF));
            end
            else if normalized_syllable_local = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable_local = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable_local = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable_local = 'dou' then
            begin
                Result := string(Char($90FD));
            end
            else if normalized_syllable_local = 'sha' then
            begin
                Result := string(Char($5565));
            end
            else if normalized_syllable_local = 'mei' then
            begin
                Result := string(Char($6CA1));
            end
            else if normalized_syllable_local = 'ying' then
            begin
                Result := string(Char($5E94));
            end
            else if normalized_syllable_local = 'jiang' then
            begin
                Result := string(Char($5C06));
            end
            else if normalized_syllable_local = 'zheng' then
            begin
                Result := string(Char($6B63));
            end
            else if normalized_syllable_local = 'you' then
            begin
                Result := string(Char($53C8));
            end
            else if normalized_syllable_local = 'hai' then
            begin
                Result := string(Char($8FD8));
            end
            else if normalized_syllable_local = 'dou' then
            begin
                Result := string(Char($90FD));
            end
            else if normalized_syllable_local = 'jiu' then
            begin
                Result := string(Char($5C31));
            end
            else if normalized_syllable_local = 'bie' then
            begin
                Result := string(Char($522B));
            end
            else if normalized_syllable_local = 'le' then
            begin
                Result := string(Char($4E86));
            end;
        end;

        function prefix_uses_only_fixed_single_local: Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if prefix_count_local < 2 then
            begin
                Exit;
            end;

            for local_idx := 0 to prefix_count_local - 1 do
            begin
                if get_fixed_single_text_local(syllables_local[local_idx].text) = '' then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function try_get_dictionary_span_text_local(
            const query_key: string; const expected_units: Integer;
            out out_text: string; out out_bonus: Integer): Boolean;
        var
            results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_bonus := 0;
            if (query_key = '') or (expected_units <= 1) then
            begin
                Exit;
            end;
            if (expected_units >= 3) or
                ((expected_units >= 2) and
                (m_dictionary is TncSqliteDictionary)) then
            begin
                if not dictionary_exact_lookup_cached(query_key,
                    results_local) then
                begin
                    Exit;
                end;
            end
            else if not dictionary_lookup_cached(query_key, results_local) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(results_local) do
            begin
                if result_idx_local >= c_dict_span_probe_limit then
                begin
                    Break;
                end;
                if Trim(results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(results_local[result_idx_local].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) then
                begin
                    Continue;
                end;

                candidate_weight_local := results_local[result_idx_local].score;
                if results_local[result_idx_local].has_dict_weight and
                    (results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_weight_local,
                    get_directional_locative_span_bonus_local(
                    candidate_text_local));
                Inc(candidate_weight_local, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text_local));
                if candidate_weight_local < c_dict_span_min_weight then
                begin
                    Continue;
                end;

                if (not Result) or (candidate_weight_local > out_bonus) then
                begin
                    Result := True;
                    out_text := candidate_text_local;
                    out_bonus := candidate_weight_local;
                end;
            end;
        end;

        function try_get_span_text_local(const local_start_idx: Integer;
            const local_span_len: Integer; out out_text: string;
            out out_bonus: Integer; out out_is_phrase: Boolean): Boolean;
        var
            query_key_local: string;
            dict_text_local: string;
            dict_bonus_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_bonus := 0;
            out_is_phrase := False;
            query_key_local := build_query_key_local(local_start_idx,
                local_span_len);
            if query_key_local = '' then
            begin
                Exit;
            end;

            if (local_span_len <= 4) and
                try_get_dictionary_span_text_local(query_key_local,
                local_span_len, dict_text_local, dict_bonus_local) then
            begin
                out_text := dict_text_local;
                out_bonus := dict_bonus_local + c_dict_span_bonus +
                    local_span_len * 120;
                out_is_phrase := True;
                Exit(True);
            end;
        end;

        function has_ambiguous_three_syllable_head_span_local: Boolean;
        const
            c_probe_limit = 8;
        var
            head_results_local: TncCandidateList;
            idx_local: Integer;
            candidate_text_local: string;
            candidate_count_local: Integer;
        begin
            Result := False;
            if input_syllable_count <> 3 then
            begin
                Exit;
            end;

            if not dictionary_lookup_cached(build_query_key_local(0, 2),
                head_results_local) then
            begin
                Exit;
            end;

            candidate_count_local := 0;
            for idx_local := 0 to High(head_results_local) do
            begin
                if idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(head_results_local[idx_local].text);
                if get_candidate_text_unit_count(candidate_text_local) <> 2 then
                begin
                    Continue;
                end;
                Inc(candidate_count_local);
                if candidate_count_local > 1 then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_build_two_span_tail_prefix_completion_local: Boolean;
        const
            c_tail_prefix_probe_limit = 8;
            c_tail_prefix_ambiguity_margin = 160;
        var
            head_text_local: string;
            head_bonus_local: Integer;
            head_is_phrase_local: Boolean;
            tail_prefix_key_local: string;
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
            tail_text_local: string;
            tail_score_local: Integer;
            best_tail_text_local: string;
            best_tail_score_local: Integer;
            second_tail_score_local: Integer;
            fixed_head_text_local: string;
            fixed_head_score_local: Integer;
            middle_phrase_text_local: string;
            middle_phrase_bonus_local: Integer;
            middle_phrase_is_phrase_local: Boolean;
            residual_tail_comment_local: string;
        begin
            Result := False;
            if input_syllable_count <> 4 then
            begin
                Exit;
            end;

            residual_tail_comment_local := normalize_pinyin_text(
                syllables_local[3].text);
            if (residual_tail_comment_local <> '') and
                (not is_full_pinyin_key(residual_tail_comment_local)) and
                (get_fixed_single_text_local(syllables_local[0].text) <> '') and
                try_get_span_text_local(1, 2, middle_phrase_text_local,
                middle_phrase_bonus_local, middle_phrase_is_phrase_local) and
                middle_phrase_is_phrase_local then
            begin
                fixed_head_text_local := get_fixed_single_text_local(
                    syllables_local[0].text);
                fixed_head_score_local := c_single_bonus + 900;
                out_candidate.text := fixed_head_text_local +
                    middle_phrase_text_local;
                out_candidate.comment := residual_tail_comment_local;
                out_candidate.source := cs_rule;
                out_candidate.score := c_prefix_base_score +
                    fixed_head_score_local + middle_phrase_bonus_local +
                    c_phrase_span_bonus;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := out_candidate.score;
                Result := True;
                Exit;
            end;

            if not try_get_span_text_local(0, 2, head_text_local,
                head_bonus_local, head_is_phrase_local) then
            begin
                Exit;
            end;

            tail_prefix_key_local := build_query_key_local(2, 2);
            if (tail_prefix_key_local = '') or
                (not dictionary_prefix_lookup_cached(tail_prefix_key_local,
                tail_results_local)) then
            begin
                Exit;
            end;

            best_tail_text_local := '';
            best_tail_score_local := Low(Integer);
            second_tail_score_local := Low(Integer);
            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if tail_idx_local >= c_tail_prefix_probe_limit then
                begin
                    Break;
                end;
                if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                if get_candidate_text_unit_count(tail_text_local) <> 2 then
                begin
                    Continue;
                end;

                tail_score_local := tail_results_local[tail_idx_local].score;
                if tail_results_local[tail_idx_local].has_dict_weight and
                    (tail_results_local[tail_idx_local].dict_weight >
                    tail_score_local) then
                begin
                    tail_score_local := tail_results_local[tail_idx_local].dict_weight;
                end;
                if tail_score_local < c_dict_span_min_weight then
                begin
                    Continue;
                end;

                if tail_score_local > best_tail_score_local then
                begin
                    second_tail_score_local := best_tail_score_local;
                    best_tail_score_local := tail_score_local;
                    best_tail_text_local := tail_text_local;
                end
                else if tail_score_local > second_tail_score_local then
                begin
                    second_tail_score_local := tail_score_local;
                end;
            end;

            if best_tail_text_local = '' then
            begin
                Exit;
            end;
            if (second_tail_score_local > Low(Integer) div 2) and
                (second_tail_score_local >= best_tail_score_local -
                c_tail_prefix_ambiguity_margin) then
            begin
                Exit;
            end;

            out_candidate.text := head_text_local + best_tail_text_local;
            out_candidate.comment := '';
            out_candidate.source := cs_rule;
            out_candidate.score := c_prefix_base_score + head_bonus_local +
                best_tail_score_local + c_phrase_span_bonus * 2;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
            Result := True;
        end;

        function try_get_single_text_local(const local_pos: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
        begin
            Result := False;
            out_text := get_fixed_single_text_local(
                syllables_local[local_pos].text);
            if out_text <> '' then
            begin
                out_score := 900;
                Exit(True);
            end;

            out_score := 0;
            if m_dictionary = nil then
            begin
                Exit;
            end;
            if m_dictionary is TncSqliteDictionary then
            begin
                if not dictionary_exact_lookup_cached(normalize_pinyin_text(
                    syllables_local[local_pos].text), local_results) then
                begin
                    Exit;
                end;
            end
            else if not dictionary_lookup_cached(normalize_pinyin_text(
                syllables_local[local_pos].text), local_results) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                candidate_text_local := Trim(local_results[local_idx].text);
                if get_candidate_text_unit_count(candidate_text_local) <> 1 then
                begin
                    Continue;
                end;
                if not is_preferred_partial_single_char_candidate(
                    local_results[local_idx]) then
                begin
                    Continue;
                end;

                candidate_score_local := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight >
                    candidate_score_local) then
                begin
                    candidate_score_local := local_results[local_idx].dict_weight;
                end;
                if (not Result) or (candidate_score_local > out_score) then
                begin
                    Result := True;
                    out_text := candidate_text_local;
                    out_score := candidate_score_local;
                end;
            end;
        end;

        function try_build_two_syllable_head_single_tail_completion_local: Boolean;
        const
            c_min_head_weight = 420;
        var
            head_key_local: string;
            head_text_local: string;
            head_weight_local: Integer;
            tail_text_local: string;
            tail_score_local: Integer;
        begin
            Result := False;
            if input_syllable_count <> 3 then
            begin
                Exit;
            end;
            if normalize_pinyin_text(syllables_local[2].text) <> 'he' then
            begin
                Exit;
            end;

            head_key_local := build_query_key_local(0, 2);
            if (head_key_local = '') or
                (not try_get_dictionary_span_text_local(head_key_local, 2,
                head_text_local, head_weight_local)) or
                (head_weight_local < c_min_head_weight) then
            begin
                Exit;
            end;

            if (not try_get_single_text_local(2, tail_text_local,
                tail_score_local)) or (tail_text_local = '') then
            begin
                Exit;
            end;

            out_candidate.text := head_text_local + tail_text_local;
            out_candidate.comment := '';
            out_candidate.source := cs_rule;
            out_candidate.score := c_prefix_base_score + head_weight_local +
                tail_score_local + c_phrase_span_bonus;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
            Result := True;
        end;

        function try_build_fixed_single_before_phrase_prefix_local(
            const local_pos: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_span_len: Integer;
            local_span_text: string;
            local_span_bonus: Integer;
            local_span_is_phrase: Boolean;
            local_single_text: string;
            local_single_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (local_pos < 0) or
                (local_pos + 2 > prefix_count_local) or
                (get_fixed_single_text_local(syllables_local[local_pos].text) = '') then
            begin
                Exit;
            end;

            local_span_len := Min(c_max_span, prefix_count_local - local_pos - 1);
            while local_span_len >= 2 do
            begin
                if try_get_span_text_local(local_pos + 1, local_span_len,
                    local_span_text, local_span_bonus, local_span_is_phrase) and
                    local_span_is_phrase then
                begin
                    if not try_get_single_text_local(local_pos, local_single_text,
                        local_single_score) then
                    begin
                        Exit;
                    end;
                    out_text := local_single_text;
                    out_score := c_single_bonus + local_single_score;
                    Result := True;
                    Exit;
                end;
                Dec(local_span_len);
            end;
        end;

        function try_build_complete_fixed_single_chain_local: Boolean;
        var
            local_idx: Integer;
            local_syllable: string;
            local_text: string;
            local_single_text: string;
        begin
            Result := False;
            if input_syllable_count < 3 then
            begin
                Exit;
            end;

            local_text := '';
            for local_idx := 0 to High(syllables_local) do
            begin
                local_syllable := normalize_pinyin_text(
                    syllables_local[local_idx].text);
                if (local_syllable = '') or
                    (not is_full_pinyin_key(local_syllable)) then
                begin
                    Exit;
                end;

                local_single_text := get_fixed_single_text_local(local_syllable);
                if local_single_text = '' then
                begin
                    Exit;
                end;
                local_text := local_text + local_single_text;
            end;

            out_candidate.text := local_text;
            out_candidate.comment := '';
            out_candidate.source := cs_rule;
            out_candidate.score := c_prefix_base_score +
                input_syllable_count * (c_single_bonus + 980);
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
            Result := True;
        end;
    begin
        Result := False;
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := 0;

        if (m_dictionary = nil) or (lookup_text = '') or
            (input_syllable_count < 3) or
            ((not query_has_modal_tail_suffix_local(lookup_text)) and
            has_full_query_exact_complete_candidate_local) or
            (query_has_modal_tail_suffix_local(lookup_text) and
            (not has_precise_extendable_tail_syllable_local(lookup_text))) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        tail_is_fragment_local := has_safe_trailing_initial_typing_state or
            ((has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            (not is_stable_boundary_single_tail_local(
            syllables_local[High(syllables_local)].text))) or
            ((input_syllable_count = 3) and
            (not has_strong_full_query_exact_complete_candidate_local)) or
            ((input_syllable_count >= 8) and
            (not has_complete_exact_tail_phrase_local(3)) and
            (((not has_complete_exact_tail_phrase_local(2)) and
            (not is_stable_boundary_single_tail_local(
            syllables_local[High(syllables_local)].text))) or
            ((get_complete_exact_tail_phrase_weight_local(2) > 0) and
            (get_complete_exact_tail_phrase_weight_local(2) < 500)) or
            has_complete_exact_phrase_before_tail_local(2)));
        if not tail_is_fragment_local then
        begin
            Exit;
        end;
        if try_build_complete_fixed_single_chain_local then
        begin
            Result := True;
            Exit;
        end;
        if try_build_two_syllable_head_single_tail_completion_local then
        begin
            Result := True;
            Exit;
        end;
        if (input_syllable_count < 5) and
            ((input_syllable_count <> 3) or
            has_safe_trailing_initial_typing_state or
            (get_fixed_single_text_local(syllables_local[0].text) <> '') or
            has_ambiguous_three_syllable_head_span_local) then
        begin
            if not try_build_two_span_tail_prefix_completion_local then
            begin
                if get_fixed_single_text_local(syllables_local[0].text) = '' then
                begin
                    Exit;
                end;
            end
            else
            begin
                Result := True;
                Exit;
            end;
        end;

        prefix_count_local := Length(syllables_local) - 1;
        if prefix_count_local < 2 then
        begin
            Exit;
        end;

        tail_comment_local := '';
        for tail_idx_local := prefix_count_local to High(syllables_local) do
        begin
            tail_comment_local := tail_comment_local +
                normalize_pinyin_text(syllables_local[tail_idx_local].text);
        end;
        if tail_comment_local = '' then
        begin
            Exit;
        end;

        pos_local := 0;
        text_local := '';
        phrase_count_local := 0;
        last_span_len_local := 0;
        last_single_total_score_local := 0;
        out_candidate.score := c_prefix_base_score;
        while pos_local < prefix_count_local do
        begin
            if try_build_fixed_single_before_phrase_prefix_local(pos_local,
                single_text_local, single_score_local) then
            begin
                text_local := text_local + single_text_local;
                last_single_total_score_local := single_score_local;
                Inc(out_candidate.score, last_single_total_score_local);
                Inc(pos_local);
                last_span_len_local := 1;
                Continue;
            end;

            span_len_local := Min(c_max_span, prefix_count_local - pos_local);
            while span_len_local >= 2 do
            begin
                if try_get_span_text_local(pos_local, span_len_local,
                    span_text_local, span_bonus_local,
                    span_is_phrase_local) then
                begin
                    text_local := text_local + span_text_local;
                    Inc(out_candidate.score, span_bonus_local);
                    Inc(pos_local, span_len_local);
                    Inc(phrase_count_local);
                    last_span_len_local := span_len_local;
                    last_single_total_score_local := 0;
                    Break;
                end;
                Dec(span_len_local);
            end;

            if span_len_local >= 2 then
            begin
                Continue;
            end;

            if not try_get_single_text_local(pos_local, single_text_local,
                single_score_local) then
            begin
                Exit;
            end;
            text_local := text_local + single_text_local;
            last_single_total_score_local := c_single_bonus + single_score_local;
            Inc(out_candidate.score, last_single_total_score_local);
            Inc(pos_local);
            last_span_len_local := 1;
        end;

        if (last_span_len_local = 1) and (prefix_count_local > 2) and
            (((not has_safe_trailing_initial_typing_state) or
            (input_syllable_count >= 8)) or
            (not is_stable_boundary_single_tail_local(
            syllables_local[prefix_count_local - 1].text))) then
        begin
            text_units_local := split_text_units(text_local);
            if Length(text_units_local) = prefix_count_local then
            begin
                text_local := '';
                for unit_idx_local := 0 to High(text_units_local) - 1 do
                begin
                    text_local := text_local + text_units_local[unit_idx_local];
                end;
                tail_comment_local := normalize_pinyin_text(
                    syllables_local[prefix_count_local - 1].text) +
                    tail_comment_local;
                Dec(prefix_count_local);
                Dec(out_candidate.score, last_single_total_score_local);
            end;
        end;

        if get_candidate_text_unit_count(text_local) <> prefix_count_local then
        begin
            Exit;
        end;
        if (phrase_count_local <= 0) and
            (not prefix_uses_only_fixed_single_local) then
        begin
            Exit;
        end;

        out_candidate.text := text_local;
        out_candidate.comment := tail_comment_local;
        Inc(out_candidate.score, prefix_count_local * 260);
        out_candidate.dict_weight := out_candidate.score;
        Result := True;
    end;

    function try_build_exact_span_chain_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_min_syllables = 3;
        c_max_span = 5;
        c_span_probe_limit = 12;
        c_min_span_weight = 480;
        c_min_long_span_weight = 180;
        c_min_long_two_syllable_span_weight = 300;
        c_ambiguous_span_margin = 320;
        c_ambiguity_min_competitor_weight = 300;
        c_weak_fixed_head_phrase_weight = 700;
        c_base_score = 70000;
        c_span_score_bonus = 900;
        c_fixed_single_bonus = 180;
        c_bridge_single_before_phrase_bonus = 3200;
        c_pronoun_before_phrase_bonus = 6400;
        c_quantity_tail_bonus = 4200;
    type
        TExactChainStateLocal = record
            text: string;
            path_text: string;
            prev_text: string;
            prev_prev_text: string;
            score: Integer;
            path_preference_score: Integer;
            segment_count: Integer;
            phrase_count: Integer;
            single_count: Integer;
        end;
        TExactChainStateListLocal = array of TExactChainStateLocal;
        TExactChainStateMatrixLocal = array of TExactChainStateListLocal;
    var
        syllables_local: TncPinyinParseResult;
        phrase_count_local: Integer;
        text_local: string;
        encoded_path_local: string;
        chain_score_local: Integer;
        memo_known_local: TArray<Boolean>;
        memo_success_local: TArray<Boolean>;
        memo_text_local: TArray<string>;
        memo_path_local: TArray<string>;
        memo_score_local: TArray<Integer>;
        memo_phrase_count_local: TArray<Integer>;

        function build_query_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to local_start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_effective_max_span_local: Integer;
        begin
            Result := c_max_span;
            if input_syllable_count >= 14 then
            begin
                Result := 3;
            end
            else if input_syllable_count >= 10 then
            begin
                Result := 4;
            end;
        end;

        function get_span_probe_limit_local: Integer;
        begin
            Result := c_span_probe_limit;
            if input_syllable_count >= 10 then
            begin
                Result := 8;
            end
            else if input_syllable_count >= 8 then
            begin
                Result := 10;
            end;
        end;

        function get_fixed_single_text_local(const syllable_text: string): string;
        var
            normalized_syllable_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if normalized_syllable_local = 'wo' then
            begin
                Result := string(Char($6211));
            end
            else if normalized_syllable_local = 'ni' then
            begin
                Result := string(Char($4F60));
            end
            else if normalized_syllable_local = 'ta' then
            begin
                Result := string(Char($4ED6));
            end
            else if normalized_syllable_local = 'he' then
            begin
                Result := string(Char($548C));
            end
            else if normalized_syllable_local = 'qu' then
            begin
                Result := string(Char($53BB));
            end
            else if normalized_syllable_local = 'shi' then
            begin
                Result := string(Char($662F));
            end
            else if normalized_syllable_local = 'yi' then
            begin
                Result := string(Char($4E00));
            end
            else if normalized_syllable_local = 'zai' then
            begin
                Result := string(Char($5728));
            end
            else if normalized_syllable_local = 'ye' then
            begin
                Result := string(Char($4E5F));
            end
            else if normalized_syllable_local = 'hui' then
            begin
                Result := string(Char($4F1A));
            end
            else if normalized_syllable_local = 'de' then
            begin
                Result := string(Char($7684));
            end
            else if normalized_syllable_local = 'dou' then
            begin
                Result := string(Char($90FD));
            end
            else if normalized_syllable_local = 'sha' then
            begin
                Result := string(Char($5565));
            end
            else if normalized_syllable_local = 'mei' then
            begin
                Result := string(Char($6CA1));
            end
            else if normalized_syllable_local = 'le' then
            begin
                Result := string(Char($4E86));
            end
            else if normalized_syllable_local = 'wan' then
            begin
                Result := string(Char($5B8C));
            end
            else if normalized_syllable_local = 'ba' then
            begin
                Result := string(Char($628A));
            end;
        end;

        function is_pronoun_fixed_single_syllable_local(
            const syllable_text: string): Boolean;
        var
            normalized_syllable_local: string;
        begin
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_syllable_local = 'wo') or
                (normalized_syllable_local = 'ni') or
                (normalized_syllable_local = 'ta');
        end;

        function is_pronoun_fixed_single_text_local(
            const text_value: string): Boolean;
        begin
            Result := (text_value = string(Char($6211))) or
                (text_value = string(Char($4F60))) or
                (text_value = string(Char($4ED6)));
        end;

        function is_stable_fixed_single_text_local(
            const text_value: string): Boolean;
        begin
            Result := (text_value = string(Char($6211))) or
                (text_value = string(Char($4F60))) or
                (text_value = string(Char($4ED6))) or
                (text_value = string(Char($548C))) or
                (text_value = string(Char($53BB))) or
                (text_value = string(Char($662F))) or
                (text_value = string(Char($628A))) or
                (text_value = string(Char($4F1A))) or
                (text_value = string(Char($4E0D))) or
                (text_value = string(Char($5F88))) or
                (text_value = string(Char($66F4))) or
                (text_value = string(Char($6700))) or
                (text_value = string(Char($592A))) or
                (text_value = string(Char($8981))) or
                (text_value = string(Char($80FD))) or
                (text_value = string(Char($53EF))) or
                (text_value = string(Char($5E94))) or
                (text_value = string(Char($5C06))) or
                (text_value = string(Char($6B63))) or
                (text_value = string(Char($53C8))) or
                (text_value = string(Char($8FD8))) or
                (text_value = string(Char($90FD))) or
                (text_value = string(Char($5C31))) or
                (text_value = string(Char($522B))) or
                (text_value = string(Char($4E5F))) or
                (text_value = string(Char($5728))) or
                (text_value = string(Char($6CA1))) or
                (text_value = string(Char($90FD))) or
                (text_value = string(Char($5565))) or
                (text_value = string(Char($7684))) or
                (text_value = string(Char($4E86))) or
                (text_value = string(Char($4E00))) or
                (text_value = string(Char($5427))) or
                (text_value = string(Char($5B8C)));
        end;

        function is_bridge_fixed_single_text_local(
            const text_value: string): Boolean;
        begin
            Result := (text_value = string(Char($662F))) or
                (text_value = string(Char($7684))) or
                (text_value = string(Char($628A))) or
                (text_value = string(Char($4F1A)));
        end;

        function is_place_noun_unit_local(const unit_value: string): Boolean;
        begin
            Result := (unit_value = string(Char($5BB6))) or
                (unit_value = string(Char($5E97))) or
                (unit_value = string(Char($5C4B))) or
                (unit_value = string(Char($697C))) or
                (unit_value = string(Char($57CE))) or
                (unit_value = string(Char($6751))) or
                (unit_value = string(Char($53BF))) or
                (unit_value = string(Char($5E02))) or
                (unit_value = string(Char($6821))) or
                (unit_value = string(Char($9662))) or
                (unit_value = string(Char($5382))) or
                (unit_value = string(Char($9986))) or
                (unit_value = string(Char($7AD9))) or
                (unit_value = string(Char($5C40))) or
                (unit_value = string(Char($6240))) or
                (unit_value = string(Char($56ED))) or
                (unit_value = string(Char($533A))) or
                (unit_value = string(Char($5708))) or
                (unit_value = string(Char($8DEF))) or
                (unit_value = string(Char($8857))) or
                (unit_value = string(Char($95E8))) or
                (unit_value = string(Char($5BA4))) or
                (unit_value = string(Char($623F))) or
                (unit_value = string(Char($5385))) or
                (unit_value = string(Char($573A))) or
                (unit_value = string(Char($53F8)));
        end;

        function is_quantity_tail_unit_local(const unit_text: string): Boolean;
        var
            codepoint: Integer;
        begin
            Result := False;
            if unit_text = '' then
            begin
                Exit;
            end;
            if Length(unit_text) = 1 then
            begin
                codepoint := Ord(unit_text[1]);
            end
            else if (Length(unit_text) = 2) and
                (Ord(unit_text[1]) >= $D800) and (Ord(unit_text[1]) <= $DBFF) and
                (Ord(unit_text[2]) >= $DC00) and (Ord(unit_text[2]) <= $DFFF) then
            begin
                codepoint := ((Ord(unit_text[1]) - $D800) shl 10) +
                    (Ord(unit_text[2]) - $DC00) + $10000;
            end
            else
            begin
                Exit;
            end;

            case codepoint of
                $4E2A, $4E9B, $70B9, $4E0B, $6B21, $56DE, $904D,
                $5929, $5E74, $8F6E, $6B65, $6761, $6839, $6279,
                $5339, $90E8, $53F0, $628A, $526F, $5BF9, $53CC,
                $74F6, $6876, $680B, $5E62, $7247, $723F, $76D8,
                $53EA:
                    Result := True;
            end;
        end;

        function get_min_span_weight_local(const local_span_len: Integer): Integer;
        begin
            if local_span_len >= 3 then
            begin
                Result := c_min_long_span_weight;
            end
            else if input_syllable_count >= 7 then
            begin
                Result := c_min_long_two_syllable_span_weight;
            end
            else
            begin
                Result := c_min_span_weight;
            end;
        end;

        function is_directional_locative_text_local(const text_value: string): Boolean;
        begin
            Result := get_directional_locative_span_bonus_local(text_value) > 0;
        end;

        function is_temporal_process_span_text_local(
            const text_value_local: string): Boolean;
        begin
            Result :=
                SameText(text_value_local, string(Char($65F6)) +
                string(Char($95F4))) or
                SameText(text_value_local, string(Char($65F6)) +
                string(Char($5019))) or
                SameText(text_value_local, string(Char($671F)) +
                string(Char($95F4))) or
                SameText(text_value_local, string(Char($8FC7)) +
                string(Char($7A0B))) or
                SameText(text_value_local, string(Char($9636)) +
                string(Char($6BB5))) or
                SameText(text_value_local, string(Char($65F6)) +
                string(Char($523B))) or
                SameText(text_value_local, string(Char($77AC)) +
                string(Char($95F4))) or
                SameText(text_value_local, string(Char($65E9)) +
                string(Char($6668))) or
                SameText(text_value_local, string(Char($591C)) +
                string(Char($665A))) or
                SameText(text_value_local, string(Char($4E0A)) +
                string(Char($5348))) or
                SameText(text_value_local, string(Char($4E0B)) +
                string(Char($5348))) or
                SameText(text_value_local, string(Char($4E2D)) +
                string(Char($5348))) or
                SameText(text_value_local, string(Char($5E74)) +
                string(Char($4EE3))) or
                SameText(text_value_local, string(Char($65F6)) +
                string(Char($4EE3)));
        end;

        function has_function_particle_before_span_local(
            const local_pos: Integer): Boolean;
        var
            previous_key_local: string;
        begin
            Result := False;
            if local_pos <= 0 then
            begin
                Exit;
            end;

            previous_key_local := normalize_pinyin_text(
                syllables_local[local_pos - 1].text);
            Result := SameText(previous_key_local, 'de') or
                SameText(previous_key_local, 'zhi');
        end;

        function get_temporal_process_context_bonus_local(
            const candidate_text: string; const local_pos: Integer): Integer;
        begin
            Result := 0;
            if has_function_particle_before_span_local(local_pos) and
                is_temporal_process_span_text_local(candidate_text) then
            begin
                Result := 4200;
            end;
        end;

        function has_place_context_before_span_local(const local_pos: Integer): Boolean;
        const
            c_context_probe_limit = 8;
        var
            back_len_local: Integer;
            context_key_local: string;
            context_results_local: TncCandidateList;
            result_idx_local: Integer;
            context_text_local: string;
            context_units_local: TArray<string>;
        begin
            Result := False;
            if local_pos <= 0 then
            begin
                Exit;
            end;

            for back_len_local := 1 to Min(4, local_pos) do
            begin
                context_key_local := build_query_key_local(
                    local_pos - back_len_local, back_len_local);
                if (context_key_local = '') or
                    (not dictionary_exact_lookup_cached(context_key_local,
                    context_results_local)) then
                begin
                    Continue;
                end;

                for result_idx_local := 0 to High(context_results_local) do
                begin
                    if result_idx_local >= c_context_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(context_results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    context_text_local := Trim(context_results_local[result_idx_local].text);
                    context_units_local := split_text_units(context_text_local);
                    if (Length(context_units_local) > 0) and
                        is_place_noun_unit_local(
                        context_units_local[High(context_units_local)]) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function has_directional_locative_context_for_span_local(
            const local_pos: Integer; const expected_units: Integer): Boolean;
        var
            next_key_local: string;
        begin
            Result := False;
            if (expected_units <> 2) or
                (not has_place_context_before_span_local(local_pos)) then
            begin
                Exit;
            end;

            if local_pos + expected_units >= Length(syllables_local) then
            begin
                Exit(True);
            end;

            next_key_local := normalize_pinyin_text(
                syllables_local[local_pos + expected_units].text);
            if next_key_local = 'de' then
            begin
                Result := True;
            end;
        end;

        function get_directional_locative_context_bonus_local(
            const candidate_text: string; const local_pos: Integer;
            const expected_units: Integer): Integer;
        const
            c_place_context_directional_bonus = 12000;
        begin
            Result := 0;
            if is_directional_locative_text_local(candidate_text) and
                has_directional_locative_context_for_span_local(local_pos,
                expected_units) then
            begin
                Result := c_place_context_directional_bonus;
            end;
        end;

        function get_crossing_previous_exact_phrase_penalty_local(
            const local_pos: Integer; const current_span_len: Integer): Integer;
        const
            c_cross_boundary_min_weight = 420;
            c_cross_boundary_base_penalty = 4200;
            c_cross_boundary_len_penalty = 220;
        var
            back_len_local: Integer;
            phrase_key_local: string;
            phrase_results_local: TncCandidateList;
            phrase_idx_local: Integer;
            phrase_text_local: string;
            phrase_weight_local: Integer;
            best_weight_local: Integer;
        begin
            Result := 0;
            if (local_pos <= 0) or (current_span_len < 2) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            best_weight_local := 0;
            for back_len_local := 2 to Min(5, local_pos + 1) do
            begin
                phrase_key_local := build_query_key_local(
                    local_pos - back_len_local + 1, back_len_local);
                if (phrase_key_local = '') or
                    (not dictionary_exact_lookup_cached(phrase_key_local,
                    phrase_results_local)) then
                begin
                    Continue;
                end;

                for phrase_idx_local := 0 to High(phrase_results_local) do
                begin
                    if phrase_idx_local >= get_span_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(phrase_results_local[phrase_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    phrase_text_local := Trim(phrase_results_local[phrase_idx_local].text);
                    if (phrase_text_local = '') or
                        (get_candidate_text_unit_count(phrase_text_local) <>
                        back_len_local) then
                    begin
                        Continue;
                    end;

                    phrase_weight_local := phrase_results_local[phrase_idx_local].score;
                    if phrase_results_local[phrase_idx_local].has_dict_weight and
                        (phrase_results_local[phrase_idx_local].dict_weight >
                        phrase_weight_local) then
                    begin
                        phrase_weight_local :=
                            phrase_results_local[phrase_idx_local].dict_weight;
                    end;
                    if phrase_weight_local > best_weight_local then
                    begin
                        best_weight_local := phrase_weight_local;
                    end;
                end;
            end;

            if best_weight_local >= c_cross_boundary_min_weight then
            begin
                Result := c_cross_boundary_base_penalty +
                    Min(1600, best_weight_local) +
                    current_span_len * c_cross_boundary_len_penalty;
            end;
        end;

        function get_best_split_piece_weight_local(const local_pos: Integer;
            const local_len: Integer; out out_weight: Integer): Boolean;
        var
            piece_key_local: string;
            piece_results_local: TncCandidateList;
            piece_idx_local: Integer;
            piece_text_local: string;
            piece_weight_local: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (local_len <= 0) or
                (local_pos < 0) or
                (local_pos + local_len > Length(syllables_local)) then
            begin
                Exit;
            end;

            piece_key_local := build_query_key_local(local_pos, local_len);
            if (piece_key_local = '') or
                (not dictionary_exact_lookup_cached(piece_key_local,
                piece_results_local)) then
            begin
                Exit;
            end;

            for piece_idx_local := 0 to High(piece_results_local) do
            begin
                if piece_idx_local >= get_span_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(piece_results_local[piece_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                piece_text_local := Trim(piece_results_local[piece_idx_local].text);
                if (piece_text_local = '') or
                    (get_candidate_text_unit_count(piece_text_local) <>
                    local_len) then
                begin
                    Continue;
                end;

                piece_weight_local := piece_results_local[piece_idx_local].score;
                if piece_results_local[piece_idx_local].has_dict_weight and
                    (piece_results_local[piece_idx_local].dict_weight >
                    piece_weight_local) then
                begin
                    piece_weight_local :=
                        piece_results_local[piece_idx_local].dict_weight;
                end;

                if piece_weight_local > out_weight then
                begin
                    out_weight := piece_weight_local;
                    Result := True;
                end;
            end;
        end;

        function get_stronger_split_span_penalty_local(
            const local_pos: Integer; const current_span_len: Integer;
            const current_weight: Integer): Integer;
        const
            c_weak_span_weight = 700;
            c_split_strong_piece_weight = 760;
            c_split_margin = 220;
            c_split_penalty = 4800;
        var
            split_pos_local: Integer;
            left_weight_local: Integer;
            right_weight_local: Integer;
            split_weight_local: Integer;
        begin
            Result := 0;
            if (current_span_len < 2) or (current_weight >= c_weak_span_weight) then
            begin
                Exit;
            end;

            for split_pos_local := 1 to current_span_len - 1 do
            begin
                if (not get_best_split_piece_weight_local(local_pos,
                    split_pos_local, left_weight_local)) or
                    (not get_best_split_piece_weight_local(local_pos +
                    split_pos_local, current_span_len - split_pos_local,
                    right_weight_local)) then
                begin
                    Continue;
                end;

                if Max(left_weight_local, right_weight_local) <
                    c_split_strong_piece_weight then
                begin
                    Continue;
                end;

                split_weight_local := left_weight_local + right_weight_local;
                if split_weight_local >= current_weight + c_split_margin then
                begin
                    Exit(c_split_penalty);
                end;
            end;
        end;

        function dictionary_lookup_has_span_text_local(const local_query_key: string;
            const local_text: string; const local_expected_units: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            if (local_query_key = '') or (local_text = '') or
                (local_expected_units <= 0) then
            begin
                Exit;
            end;
            if m_dictionary is TncSqliteDictionary then
            begin
                if not dictionary_exact_lookup_cached(local_query_key,
                    local_results) then
                begin
                    Exit;
                end;
            end
            else if not dictionary_lookup_cached(local_query_key,
                local_results) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= get_span_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    local_results[local_idx].dict_weight,
                    local_results[local_idx].has_dict_weight) then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(local_results[local_idx].text);
                if SameText(candidate_text_local, local_text) and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    local_expected_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_nonbase_two_plus_single_dictionary_span_local(
            const local_query_key: string; const local_expected_units: Integer;
            const local_text: string): Boolean;
        var
            local_syllables: TncPinyinParseResult;
            local_units: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            tail_syllable_local: string;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (local_expected_units <> 3) then
            begin
                Exit;
            end;
            if m_dictionary.is_base_entry(local_query_key, local_text) or
                m_dictionary.is_user_entry(local_query_key, local_text) then
            begin
                Exit;
            end;

            local_syllables := get_effective_compact_pinyin_syllables(
                local_query_key);
            local_units := split_text_units(Trim(local_text));
            if (Length(local_syllables) <> 3) or (Length(local_units) <> 3) then
            begin
                Exit;
            end;

            tail_syllable_local := normalize_pinyin_text(
                local_syllables[2].text);
            if (tail_syllable_local = 'de') or
                (tail_syllable_local = 'le') or
                (tail_syllable_local = 'la') or
                (tail_syllable_local = 'ba') or
                (tail_syllable_local = 'ma') then
            begin
                Exit;
            end;

            head_key_local := normalize_pinyin_text(local_syllables[0].text) +
                normalize_pinyin_text(local_syllables[1].text);
            tail_key_local := tail_syllable_local;
            head_text_local := local_units[0] + local_units[1];
            tail_text_local := local_units[2];

            Result := dictionary_lookup_has_span_text_local(head_key_local,
                head_text_local, 2) and
                (dictionary_lookup_has_span_text_local(tail_key_local,
                tail_text_local, 1) or
                m_dictionary.single_char_matches_pinyin(tail_key_local,
                tail_text_local));
        end;

        function try_get_dictionary_span_text_local(
            const query_key: string; const expected_units: Integer;
            const required_tail_text: string; const local_pos: Integer;
            out out_text: string;
            out out_weight: Integer; out out_is_ambiguous: Boolean): Boolean;
        var
            results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            candidate_base_weight_local: Integer;
            candidate_preferred_bonus_local: Integer;
            direction_bonus_local: Integer;
            direction_context_bonus_local: Integer;
            has_directional_context_local: Boolean;
            has_directional_candidate_local: Boolean;
            candidate_units_local: TArray<string>;
            second_weight_local: Integer;
            valid_count_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_is_ambiguous := False;
            second_weight_local := Low(Integer);
            valid_count_local := 0;
            if (query_key = '') or (expected_units <= 1) then
            begin
                Exit;
            end;
            if (expected_units >= 3) and
                (m_dictionary is TncSqliteDictionary) then
            begin
                if (not dictionary_exact_lookup_cached(query_key,
                    results_local)) and
                    (not dictionary_lookup_cached(query_key,
                    results_local)) then
                begin
                    Exit;
                end;
            end
            else if not dictionary_lookup_cached(query_key, results_local) then
            begin
                Exit;
            end;

            has_directional_context_local :=
                has_directional_locative_context_for_span_local(local_pos,
                expected_units);
            has_directional_candidate_local := False;
            if has_directional_context_local then
            begin
                for result_idx_local := 0 to High(results_local) do
                begin
                    if result_idx_local >= get_span_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        results_local[result_idx_local].dict_weight,
                        results_local[result_idx_local].has_dict_weight) then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(results_local[result_idx_local].text);
                    if (candidate_text_local <> '') and
                        (get_candidate_text_unit_count(candidate_text_local) =
                        expected_units) and
                        is_directional_locative_text_local(candidate_text_local) then
                    begin
                        has_directional_candidate_local := True;
                        Break;
                    end;
                end;
            end;

            for result_idx_local := 0 to High(results_local) do
            begin
                if result_idx_local >= get_span_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                if not long_exact_chunk_weight_is_eligible(
                    results_local[result_idx_local].dict_weight,
                    results_local[result_idx_local].has_dict_weight) then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(results_local[result_idx_local].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) then
                begin
                    Continue;
                end;
                if is_nonbase_two_plus_single_dictionary_span_local(query_key,
                    expected_units, candidate_text_local) then
                begin
                    Continue;
                end;
                candidate_base_weight_local := results_local[result_idx_local].score;
                if results_local[result_idx_local].has_dict_weight and
                    (results_local[result_idx_local].dict_weight >
                    candidate_base_weight_local) then
                begin
                    candidate_base_weight_local :=
                        results_local[result_idx_local].dict_weight;
                end;
                candidate_preferred_bonus_local :=
                    get_preferred_query_phrase_bonus_local(query_key,
                    candidate_text_local);
                if has_directional_context_local and
                    has_directional_candidate_local and
                    (not is_directional_locative_text_local(candidate_text_local)) then
                begin
                    candidate_preferred_bonus_local := 0;
                end;
                if required_tail_text <> '' then
                begin
                    candidate_units_local := split_text_units(candidate_text_local);
                    if (Length(candidate_units_local) <> expected_units) or
                        (candidate_units_local[High(candidate_units_local)] <>
                        required_tail_text) then
                    begin
                        if candidate_preferred_bonus_local <= 0 then
                        begin
                            Continue;
                        end;
                        if (not results_local[result_idx_local].has_dict_weight) or
                            (results_local[result_idx_local].dict_weight <
                            c_weak_fixed_head_phrase_weight) then
                        begin
                            Continue;
                        end;
                    end;
                end;

                candidate_weight_local := candidate_base_weight_local;
                direction_bonus_local :=
                    get_directional_locative_span_bonus_local(
                    candidate_text_local);
                direction_context_bonus_local :=
                    get_directional_locative_context_bonus_local(
                    candidate_text_local, local_pos, expected_units);
                Inc(candidate_weight_local, direction_bonus_local);
                Inc(candidate_weight_local,
                    direction_context_bonus_local);
                Inc(candidate_weight_local, candidate_preferred_bonus_local);
                Inc(candidate_weight_local,
                    get_temporal_process_context_bonus_local(
                    candidate_text_local, local_pos));
                Dec(candidate_weight_local,
                    get_crossing_previous_exact_phrase_penalty_local(
                    local_pos, expected_units));
                Dec(candidate_weight_local,
                    get_stronger_split_span_penalty_local(local_pos,
                    expected_units, candidate_base_weight_local));
                if (expected_units = 2) and
                    (normalize_pinyin_text(syllables_local[local_pos].text) = 'yi') then
                begin
                    candidate_units_local := split_text_units(candidate_text_local);
                    if (Length(candidate_units_local) = 2) and
                        (candidate_units_local[0] = string(Char($4E00))) and
                        is_quantity_tail_unit_local(candidate_units_local[1]) then
                    begin
                        Inc(candidate_weight_local, c_quantity_tail_bonus);
                    end;
                end;
                if candidate_weight_local >= c_ambiguity_min_competitor_weight then
                begin
                    Inc(valid_count_local);
                end;
                if candidate_weight_local <
                    get_min_span_weight_local(expected_units) then
                begin
                    second_weight_local := Max(second_weight_local,
                        candidate_weight_local);
                    Continue;
                end;

                if (not Result) or (candidate_weight_local > out_weight) then
                begin
                    if Result then
                    begin
                        second_weight_local := Max(second_weight_local,
                            out_weight);
                    end;
                    Result := True;
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                end
                else
                begin
                    second_weight_local := Max(second_weight_local,
                        candidate_weight_local);
                end;
            end;

            if Result and (valid_count_local > 1) and
                (second_weight_local >= out_weight - c_ambiguous_span_margin) then
            begin
                out_is_ambiguous := True;
            end;
        end;

        function combine_path_segments_local(const head_path: string;
            const tail_path: string): string;
        begin
            Result := head_path;
            if tail_path = '' then
            begin
                Exit;
            end;
            if Result <> '' then
            begin
                Result := Result + string(Char($0003));
            end;
            Result := Result + tail_path;
        end;

        function get_first_path_segment_local(const encoded_path: string): string;
        var
            separator_pos_local: Integer;
        begin
            Result := Trim(encoded_path);
            if Result = '' then
            begin
                Exit;
            end;
            separator_pos_local := Pos(c_segment_path_separator, Result);
            if separator_pos_local > 0 then
            begin
                Result := Copy(Result, 1, separator_pos_local - 1);
            end;
        end;

        function get_last_path_segment_local(const encoded_path: string): string;
        var
            scan_idx_local: Integer;
        begin
            Result := Trim(encoded_path);
            for scan_idx_local := Length(Result) downto 1 do
            begin
                if Result[scan_idx_local] = c_segment_path_separator then
                begin
                    Result := Copy(Result, scan_idx_local + 1, MaxInt);
                    Exit;
                end;
            end;
        end;

        function stable_fixed_single_chain_with_phrase_local(
            const encoded_path: string): Boolean;
        var
            segment_parts_local: TArray<string>;
            segment_idx_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            has_phrase_segment_local: Boolean;
        begin
            Result := False;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            segment_parts_local := encoded_path.Split([c_segment_path_separator]);
            if Length(segment_parts_local) < 2 then
            begin
                Exit;
            end;

            has_phrase_segment_local := False;
            for segment_idx_local := 0 to High(segment_parts_local) do
            begin
                segment_text_local := Trim(segment_parts_local[segment_idx_local]);
                if segment_text_local = '' then
                begin
                    Exit;
                end;

                segment_units_local :=
                    get_candidate_text_unit_count(segment_text_local);
                if segment_units_local >= 2 then
                begin
                    has_phrase_segment_local := True;
                    Continue;
                end;

                if (segment_units_local <> 1) or
                    (not is_stable_fixed_single_text_local(segment_text_local)) then
                begin
                    Exit;
                end;
            end;

            Result := has_phrase_segment_local;
        end;

        function get_following_context_bonus_local(const head_text: string;
            const tail_path: string): Integer;
        var
            tail_head_text_local: string;
        begin
            Result := 0;
            if (m_dictionary = nil) or (Trim(head_text) = '') or
                (Trim(tail_path) = '') then
            begin
                Exit;
            end;

            tail_head_text_local := get_first_path_segment_local(tail_path);
            if tail_head_text_local <> '' then
            begin
                Result := m_dictionary.get_context_bonus(head_text,
                    tail_head_text_local);
            end;
        end;

        function try_get_dictionary_single_text_local(const query_key: string;
            out out_text: string; out out_weight: Integer): Boolean;
        var
            results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (query_key = '') or
                (not dictionary_exact_lookup_cached(query_key, results_local)) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(results_local) do
            begin
                if result_idx_local >= get_span_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(results_local[result_idx_local].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <> 1) then
                begin
                    Continue;
                end;

                candidate_weight_local := results_local[result_idx_local].score;
                if results_local[result_idx_local].has_dict_weight and
                    (results_local[result_idx_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        results_local[result_idx_local].dict_weight;
                end;
                Inc(candidate_weight_local, get_preferred_query_phrase_bonus_local(
                    query_key, candidate_text_local));
                if (not Result) or (candidate_weight_local > out_weight) then
                begin
                    Result := True;
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                end;
            end;
        end;

        function is_motion_tail_syllable_local(const syllable_text: string): Boolean;
        var
            normalized_local: string;
        begin
            normalized_local := normalize_pinyin_text(syllable_text);
            Result := (normalized_local = 'qu') or (normalized_local = 'lai') or
                (normalized_local = 'dao');
        end;

        function try_get_locative_li_span_text_local(const local_pos: Integer;
            out out_text: string; out out_weight: Integer): Boolean;
        const
            c_locative_li_motion_bonus = 3600;
        var
            head_key_local: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (local_pos < 0) or (local_pos + 2 > High(syllables_local)) or
                (normalize_pinyin_text(syllables_local[local_pos + 1].text) <> 'li') or
                (not is_motion_tail_syllable_local(
                syllables_local[local_pos + 2].text)) then
            begin
                Exit;
            end;

            head_key_local := normalize_pinyin_text(syllables_local[local_pos].text);
            if (head_key_local = '') or
                (not dictionary_exact_lookup_cached(head_key_local, local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= get_span_probe_limit_local then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(local_results[local_idx].text);
                if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                    (not is_place_noun_unit_local(candidate_text_local)) then
                begin
                    Continue;
                end;

                candidate_weight_local := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > candidate_weight_local) then
                begin
                    candidate_weight_local := local_results[local_idx].dict_weight;
                end;
                Inc(candidate_weight_local, c_locative_li_motion_bonus);
                if (not Result) or (candidate_weight_local > out_weight) then
                begin
                    Result := True;
                    out_text := candidate_text_local + string(Char($91CC));
                    out_weight := candidate_weight_local;
                end;
            end;
        end;

        function try_get_locative_li_motion_span_text_local(const local_pos: Integer;
            out out_text: string; out out_path: string; out out_weight: Integer): Boolean;
        const
            c_locative_motion_span_bonus = 480;
        var
            locative_text_local: string;
            locative_weight_local: Integer;
            tail_key_local: string;
            previous_key_local: string;
            preferred_tail_text_local: string;
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            tail_text_local: string;
            tail_weight_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_weight := 0;
            if (local_pos < 0) or (local_pos + 2 > High(syllables_local)) or
                (not try_get_locative_li_span_text_local(local_pos,
                locative_text_local, locative_weight_local)) then
            begin
                Exit;
            end;

            tail_key_local := normalize_pinyin_text(
                syllables_local[local_pos + 2].text);
            previous_key_local := '';
            if local_pos > 0 then
            begin
                previous_key_local := normalize_pinyin_text(
                    syllables_local[local_pos - 1].text);
            end;

            preferred_tail_text_local := get_fixed_single_text_local(
                tail_key_local);
            if (tail_key_local = 'qu') and (previous_key_local = 'qu') then
            begin
                preferred_tail_text_local := string(Char($53D6));
            end;

            tail_text_local := '';
            tail_weight_local := Low(Integer);
            if (preferred_tail_text_local <> '') and
                dictionary_exact_lookup_cached(tail_key_local,
                tail_results_local) then
            begin
                for tail_idx_local := 0 to High(tail_results_local) do
                begin
                    if tail_idx_local >= get_span_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(tail_results_local[tail_idx_local].text);
                    if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                        (candidate_text_local <> preferred_tail_text_local) then
                    begin
                        Continue;
                    end;

                    candidate_weight_local := tail_results_local[tail_idx_local].score;
                    if tail_results_local[tail_idx_local].has_dict_weight and
                        (tail_results_local[tail_idx_local].dict_weight >
                        candidate_weight_local) then
                    begin
                        candidate_weight_local :=
                            tail_results_local[tail_idx_local].dict_weight;
                    end;
                    tail_text_local := candidate_text_local;
                    tail_weight_local := candidate_weight_local;
                    Break;
                end;
            end;

            if tail_text_local = '' then
            begin
                if not try_get_dictionary_single_text_local(tail_key_local,
                    tail_text_local, tail_weight_local) then
                begin
                    Exit;
                end;
            end;

            out_text := locative_text_local + tail_text_local;
            out_path := combine_path_segments_local(locative_text_local,
                tail_text_local);
            out_weight := locative_weight_local + tail_weight_local +
                c_locative_motion_span_bonus;
            Inc(out_weight, get_following_context_bonus_local(locative_text_local,
                tail_text_local));
            Result := True;
        end;

        function try_get_result_wan_span_text_local(const local_pos: Integer;
            out out_text: string; out out_path: string; out out_weight: Integer): Boolean;
        const
            c_result_wan_span_bonus = 3600;
        var
            head_key_local: string;
            head_text_local: string;
            head_weight_local: Integer;
            wan_text_local: string;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_weight := 0;
            if (local_pos < 0) or (local_pos + 1 > High(syllables_local)) or
                (normalize_pinyin_text(syllables_local[local_pos + 1].text) <>
                'wan') then
            begin
                Exit;
            end;

            head_key_local := normalize_pinyin_text(syllables_local[local_pos].text);
            if (head_key_local = '') or
                (not try_get_dictionary_single_text_local(head_key_local,
                head_text_local, head_weight_local)) then
            begin
                Exit;
            end;

            wan_text_local := string(Char($5B8C));
            out_text := head_text_local + wan_text_local;
            out_path := combine_path_segments_local(head_text_local, wan_text_local);
            out_weight := head_weight_local + c_result_wan_span_bonus;
            Inc(out_weight, get_following_context_bonus_local(head_text_local,
                wan_text_local));
            Result := True;
        end;

        function try_resolve_final_three_syllable_span_text_local(
            const local_start: Integer; out out_text: string; out out_path: string;
            out out_weight: Integer): Boolean;
        var
            left_key_local: string;
            right_key_local: string;
            left_text_local: string;
            right_text_local: string;
            left_weight_local: Integer;
            right_weight_local: Integer;
            left_ambiguous_local: Boolean;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_weight := 0;
            if (input_syllable_count > 8) or (local_start < 0) or
                (local_start + 2 <> High(syllables_local)) then
            begin
                Exit;
            end;

            left_key_local := build_query_key_local(local_start, 2);
            right_key_local := build_query_key_local(local_start + 2, 1);
            if (not try_get_dictionary_span_text_local(left_key_local, 2, '',
                local_start,
                left_text_local, left_weight_local, left_ambiguous_local)) or
                (left_ambiguous_local and (input_syllable_count < 7)) or
                (not try_get_dictionary_single_text_local(right_key_local,
                right_text_local, right_weight_local)) then
            begin
                Exit;
            end;

            out_text := left_text_local + right_text_local;
            out_path := combine_path_segments_local(left_text_local,
                right_text_local);
            out_weight := left_weight_local + right_weight_local;
            if m_dictionary <> nil then
            begin
                Inc(out_weight, m_dictionary.get_context_bonus(left_text_local,
                    right_text_local));
            end;
            Result := True;
        end;

        function has_resolved_final_tail_three_syllable_span_local: Boolean;
        var
            dummy_text_local: string;
            dummy_path_local: string;
            dummy_weight_local: Integer;
        begin
            Result := (Length(syllables_local) >= 3) and
                try_resolve_final_three_syllable_span_text_local(
                Length(syllables_local) - 3, dummy_text_local, dummy_path_local,
                dummy_weight_local);
        end;

        function has_exact_phrase_starting_at_local(const local_pos: Integer): Boolean;
        var
            local_span_len: Integer;
            local_key: string;
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            if (local_pos < 0) or (local_pos >= Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_span_len := 2 to Min(get_effective_max_span_local,
                Length(syllables_local) - local_pos) do
            begin
                local_key := build_query_key_local(local_pos, local_span_len);
                if (local_key = '') or
                    (not dictionary_exact_lookup_cached(local_key,
                    local_results)) then
                begin
                    Continue;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    local_text := Trim(local_results[local_idx].text);
                    if (local_text <> '') and
                        (get_candidate_text_unit_count(local_text) =
                        local_span_len) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function should_skip_fixed_single_chain_step_local(
            const local_pos: Integer; const local_single_text: string): Boolean;
        begin
            Result := (input_syllable_count >= 4) and
                (Trim(local_single_text) <> '') and
                (not is_pronoun_fixed_single_text_local(local_single_text)) and
                has_exact_phrase_starting_at_local(local_pos);
        end;

        function get_chain_first_unit_local(const text_value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(text_value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[1];
            end;
        end;

        function get_chain_last_unit_local(const text_value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(text_value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[Length(trimmed_value)];
            end;
        end;

        function is_chain_unit_code_local(const text_value: string;
            const code_value: Integer): Boolean;
        begin
            Result := (Length(text_value) = 1) and (Ord(text_value[1]) = code_value);
        end;

        function is_chain_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $505A, $5230, $53BB, $544A, $547C, $56DE, $5BFB, $5F00,
                $60F3, $6210, $6253, $627E, $63A5, $63D0, $6539, $653E,
                $6765, $7528, $770B, $7814, $7ED9, $80FD, $8981, $89C1,
                $8BF4, $8D70, $8F93, $9001, $95EE, $8868:
                    Result := True;
            end;
        end;

        function is_chain_location_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4E0A, $4E0B, $4E2D, $4E9B, $4F4D, $5185, $524D, $540E,
                $56FD, $5730, $57CE, $5B66, $5BB6, $5C42, $5E02, $65B9,
                $65F6, $672C, $6761, $6B21, $79CD, $7C7B, $9879:
                    Result := True;
            end;
        end;

        function is_chain_bound_yu_left_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4F4D, $5173, $5BF9, $7528, $5C5E, $7B49, $6E90, $57FA,
                $5904, $51FA, $6025, $7126, $9274, $81F3, $4FBF, $5229,
                $7531, $9650:
                    Result := True;
            end;
        end;

        function is_chain_predicate_yu_left_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4E50, $4FBF, $5229, $5584, $5FD9, $6025, $75B2:
                    Result := True;
            end;
        end;

        function get_chain_yu_context_adjustment_local(
            const segment_last_syllable: string; const segment_text: string;
            const tail_first_segment_text: string): Integer;
        begin
            Result := 0;
        end;

        function try_build_from_pos_local(const local_pos: Integer;
            out out_text: string; out out_path: string; out out_score: Integer;
            out out_phrase_count: Integer): Boolean;
        var
            local_span_len: Integer;
            local_span_text: string;
            local_span_weight: Integer;
            local_span_ambiguous: Boolean;
            local_span_path: string;
            locative_span_text: string;
            locative_span_path: string;
            locative_span_weight: Integer;
            result_wan_span_text: string;
            result_wan_span_path: string;
            result_wan_span_weight: Integer;
            local_single_text: string;
            local_tail_text: string;
            local_tail_path: string;
            local_tail_score: Integer;
            local_tail_phrase_count: Integer;
            local_candidate_text: string;
            local_candidate_path: string;
            local_candidate_score: Integer;
            local_candidate_phrase_count: Integer;
            tail_first_segment: string;
            local_best_text: string;
            local_best_path: string;
            local_best_score: Integer;
            local_best_phrase_count: Integer;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := 0;
            out_phrase_count := 0;

            if local_pos >= Length(syllables_local) then
            begin
                Result := True;
                Exit;
            end;

            if memo_known_local[local_pos] then
            begin
                Result := memo_success_local[local_pos];
                if Result then
                begin
                    out_text := memo_text_local[local_pos];
                    out_path := memo_path_local[local_pos];
                    out_score := memo_score_local[local_pos];
                    out_phrase_count := memo_phrase_count_local[local_pos];
                end;
                Exit;
            end;

            memo_known_local[local_pos] := True;
            local_best_text := '';
            local_best_path := '';
            local_best_score := Low(Integer);
            local_best_phrase_count := -1;

            for local_span_len := Min(get_effective_max_span_local,
                Length(syllables_local) - local_pos) downto 2 do
            begin
                if local_span_len > 2 then
                begin
                    local_single_text := get_fixed_single_text_local(
                        syllables_local[local_pos + local_span_len - 1].text);
                end
                else
                begin
                    local_single_text := '';
                end;

                if try_get_dictionary_span_text_local(
                    build_query_key_local(local_pos, local_span_len),
                    local_span_len, local_single_text, local_pos, local_span_text,
                    local_span_weight, local_span_ambiguous) then
                begin
                    local_span_path := local_span_text;
                    if (local_span_len = 3) and
                        try_get_locative_li_motion_span_text_local(local_pos,
                        locative_span_text, locative_span_path,
                        locative_span_weight) and
                        (locative_span_weight > local_span_weight) then
                    begin
                        local_span_text := locative_span_text;
                        local_span_path := locative_span_path;
                        local_span_weight := locative_span_weight;
                        local_span_ambiguous := False;
                    end;
                end
                else if (local_span_len = 3) and
                    try_get_locative_li_motion_span_text_local(local_pos,
                    local_span_text, local_span_path, local_span_weight) then
                begin
                    local_span_ambiguous := False;
                end
                else if (local_span_len = 3) and
                    try_resolve_final_three_syllable_span_text_local(local_pos,
                    local_span_text, local_span_path, local_span_weight) then
                begin
                    local_span_ambiguous := False;
                end
                else
                begin
                    Continue;
                end;

                if (local_span_len = 2) and
                    try_get_locative_li_span_text_local(local_pos,
                    locative_span_text, locative_span_weight) and
                    (locative_span_weight > local_span_weight) then
                begin
                    local_span_text := locative_span_text;
                    local_span_path := locative_span_text;
                    local_span_weight := locative_span_weight;
                    local_span_ambiguous := False;
                end;
                if (local_span_len = 2) and
                    try_get_result_wan_span_text_local(local_pos,
                    result_wan_span_text, result_wan_span_path,
                    result_wan_span_weight) and
                    ((local_span_weight < c_weak_fixed_head_phrase_weight) or
                    (result_wan_span_weight > local_span_weight)) then
                begin
                    local_span_text := result_wan_span_text;
                    local_span_path := result_wan_span_path;
                    local_span_weight := result_wan_span_weight;
                    local_span_ambiguous := False;
                end;

                if (local_pos = 0) and (local_span_len = 2) and
                    (local_span_weight < c_weak_fixed_head_phrase_weight) and
                    (get_fixed_single_text_local(syllables_local[local_pos].text) <> '') then
                begin
                    Continue;
                end;

                if try_build_from_pos_local(local_pos + local_span_len,
                    local_tail_text, local_tail_path, local_tail_score,
                    local_tail_phrase_count) then
                begin
                    if local_span_ambiguous and (input_syllable_count < 7) and
                        (not ((local_pos > 0) and
                        (local_pos + local_span_len = Length(syllables_local)))) and
                        (local_tail_phrase_count <= 0) then
                    begin
                        Continue;
                    end;
                    local_candidate_text := local_span_text + local_tail_text;
                    local_candidate_path := combine_path_segments_local(
                        local_span_path, local_tail_path);
                    local_candidate_score := local_span_weight +
                        local_span_len * c_span_score_bonus + local_tail_score +
                        get_following_context_bonus_local(local_span_text,
                        local_tail_path);
                    tail_first_segment := get_first_path_segment_local(
                        local_tail_path);
                    Inc(local_candidate_score,
                        get_chain_yu_context_adjustment_local(
                        syllables_local[local_pos + local_span_len - 1].text,
                        local_span_text, tail_first_segment));
                    if (m_dictionary <> nil) and (local_pos = 0) and
                        (local_candidate_path <> '') then
                    begin
                        Inc(local_candidate_score,
                            get_cached_query_segment_path_bonus(
                            lookup_text, local_candidate_path));
                        Dec(local_candidate_score,
                            get_cached_query_segment_path_penalty(
                            lookup_text, local_candidate_path));
                    end;
                    local_candidate_phrase_count := 1 + local_tail_phrase_count;
                    if (local_candidate_score > local_best_score) or
                        ((local_candidate_score = local_best_score) and
                        (local_candidate_phrase_count > local_best_phrase_count)) then
                    begin
                        local_best_text := local_candidate_text;
                        local_best_path := local_candidate_path;
                        local_best_score := local_candidate_score;
                        local_best_phrase_count := local_candidate_phrase_count;
                    end;
                end;
            end;

            local_single_text := get_fixed_single_text_local(
                syllables_local[local_pos].text);
            if local_single_text <> '' then
            begin
                if should_skip_fixed_single_chain_step_local(local_pos,
                    local_single_text) then
                begin
                    memo_success_local[local_pos] := local_best_text <> '';
                    if memo_success_local[local_pos] then
                    begin
                        memo_text_local[local_pos] := local_best_text;
                        memo_path_local[local_pos] := local_best_path;
                        memo_score_local[local_pos] := local_best_score;
                        memo_phrase_count_local[local_pos] :=
                            local_best_phrase_count;
                        out_text := local_best_text;
                        out_path := local_best_path;
                        out_score := local_best_score;
                        out_phrase_count := local_best_phrase_count;
                    end;
                    Exit(memo_success_local[local_pos]);
                end;

                if try_build_from_pos_local(local_pos + 1, local_tail_text,
                    local_tail_path, local_tail_score, local_tail_phrase_count) then
                begin
                    local_candidate_text := local_single_text + local_tail_text;
                    local_candidate_path := combine_path_segments_local(
                        local_single_text, local_tail_path);
                    local_candidate_score := c_fixed_single_bonus +
                        local_tail_score + get_following_context_bonus_local(
                        local_single_text, local_tail_path);
                    tail_first_segment := get_first_path_segment_local(
                        local_tail_path);
                    if is_pronoun_fixed_single_syllable_local(
                        syllables_local[local_pos].text) and
                        (get_candidate_text_unit_count(tail_first_segment) >= 2) then
                    begin
                        Inc(local_candidate_score,
                            c_pronoun_before_phrase_bonus);
                    end
                    else if (local_pos > 0) and
                        is_bridge_fixed_single_text_local(local_single_text) and
                        (get_candidate_text_unit_count(tail_first_segment) >= 2) then
                    begin
                        Inc(local_candidate_score,
                            c_bridge_single_before_phrase_bonus);
                    end;
                    local_candidate_phrase_count := local_tail_phrase_count;
                    if (local_candidate_score > local_best_score) or
                        ((local_candidate_score = local_best_score) and
                        (local_candidate_phrase_count > local_best_phrase_count)) then
                    begin
                        local_best_text := local_candidate_text;
                        local_best_path := local_candidate_path;
                        local_best_score := local_candidate_score;
                        local_best_phrase_count := local_candidate_phrase_count;
                    end;
                end;
            end;

            if local_best_text = '' then
            begin
                memo_success_local[local_pos] := False;
                Exit;
            end;

            memo_success_local[local_pos] := True;
            memo_text_local[local_pos] := local_best_text;
            memo_path_local[local_pos] := local_best_path;
            memo_score_local[local_pos] := local_best_score;
            memo_phrase_count_local[local_pos] := local_best_phrase_count;
            out_text := local_best_text;
            out_path := local_best_path;
            out_score := local_best_score;
            out_phrase_count := local_best_phrase_count;
            Result := True;
        end;

        function try_build_generic_exact_chain_fast_local(
            out out_text: string; out out_path: string; out out_score: Integer;
            out out_phrase_count: Integer): Boolean;
        const
            c_state_keep_limit_local = 10;
            c_single_candidate_limit_local = 8;
            c_multi_candidate_limit_local = 4;
            c_weight_scale_local = 6;
            c_segment_penalty_local = 720;
            c_single_unit_penalty_local = 3200;
            c_stable_single_bonus_local = 4800;
            c_two_unit_bonus_local = 1400;
            c_three_unit_bonus_local = 4200;
            c_long_unit_bonus_local = 11200;
            c_per_extra_unit_bonus_local = 1600;
            c_full_query_exact_bonus_local = 9000;
            c_trusted_chain_bonus_local = 24000;
            c_transition_bonus_scale_local = 4;
            c_candidate_rank_penalty_local = 96;
            c_no_phrase_penalty_local = 12000;
            c_completion_bonus_local = 2200;
        var
            best_states_local: TExactChainStateMatrixLocal;
            transition_bonus_cache_local: TDictionary<string, Integer>;
            suffix_first_unit_cache_local: TDictionary<Integer, string>;
            pos_local: Integer;
            span_len_local: Integer;
            option_idx_local: Integer;
            state_idx_local: Integer;
            next_pos_local: Integer;
            candidate_limit_local: Integer;
            span_key_local: string;
            exact_results_local: TncCandidateList;
            option_candidate_local: TncCandidate;
            option_text_local: string;
            option_units_local: Integer;
            option_score_local: Integer;
            option_is_preferred_local: Boolean;
            state_keep_limit_local: Integer;
            multi_candidate_limit_local: Integer;
            transition_bonus_local: Integer;
            direction_bonus_local: Integer;
            direction_context_bonus_local: Integer;
            base_state_local: TExactChainStateLocal;
            combined_state_local: TExactChainStateLocal;
            best_state_local: TExactChainStateLocal;
            fixed_single_text_local: string;
            fixed_single_candidate_local: TncCandidate;
            candidate_units_local: TArray<string>;

            function get_effective_candidate_weight_local(
                const candidate_local: TncCandidate): Integer;
            begin
                Result := candidate_local.score;
                if candidate_local.has_dict_weight and
                    (candidate_local.dict_weight > Result) then
                begin
                    Result := candidate_local.dict_weight;
                end;
                if Result < 0 then
                begin
                    Result := 0;
                end;
            end;

            function get_generic_first_text_unit_local(const value: string): string;
            var
                trimmed_local: string;
            begin
                Result := '';
                trimmed_local := Trim(value);
                if Length(trimmed_local) > 0 then
                begin
                    Result := trimmed_local[1];
                end;
            end;

            function get_generic_last_text_unit_local(const value: string): string;
            var
                trimmed_local: string;
            begin
                Result := '';
                trimmed_local := Trim(value);
                if Length(trimmed_local) > 0 then
                begin
                    Result := trimmed_local[Length(trimmed_local)];
                end;
            end;

            function is_generic_context_unit_code_local(const value: string;
                const code_local: Integer): Boolean;
            begin
                Result := (Length(value) = 1) and (Ord(value[1]) = code_local);
            end;

            function is_generic_predicate_head_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $505A, $5230, $53BB, $544A, $547C, $56DE, $5BFB, $5F00,
                    $60F3, $6210, $6253, $627E, $62FF, $6301, $63A5, $63D0,
                    $6539, $653E, $6765, $7528, $770B, $7740, $7814, $79BB,
                    $7EE7, $7ED9, $80E1, $89C1, $8BA9, $8BB2, $8BB0, $8BF4,
                    $8D70, $8DD1, $8F93, $8F6C, $8FC7, $9001, $9009, $95EE,
                    $5199, $5C06, $964D, $9610, $9996, $6572, $653B, $878D,
                    $642C, $4FEE, $963B, $8054, $88AB, $5FD0, $76B1, $8D62,
                    $9501, $5229, $7A33, $8131, $6563, $5BA3, $5E94, $79F0,
                    $8EBA, $70B9, $8FC5, $9010, $6DF1, $8DF3, $63E1, $6D12,
                    $8FFD:
                        Result := True;
                end;
            end;

            function is_generic_location_head_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4E0A, $4E0B, $4E2D, $4E2A, $4E9B, $4EF6, $4EEC, $4F4D,
                    $5143, $5165, $5185, $524D, $540E, $56FD, $5730, $573A,
                    $57CE, $5B66, $5BB6, $5C42, $5C4B, $5DE5, $5E02,
                    $5E74, $65B9, $65F6, $6708, $672C, $6761, $679A, $6B21,
                    $7247, $79CD, $7C7B, $7EC4, $8005, $91CC, $95F4, $9879:
                        Result := True;
                end;
            end;

            function is_generic_bound_yu_left_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4F4D, $5173, $5BF9, $7528, $5C5E, $7B49, $6E90, $57FA,
                    $5904, $51FA, $6025, $7126, $9274, $81F3, $4FBF, $5229,
                    $7531, $9650:
                        Result := True;
                end;
            end;

            function is_generic_predicate_yu_left_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4E50, $4FBF, $5229, $5584, $5FD9, $6025, $75B2:
                        Result := True;
                end;
            end;

            function get_suffix_first_unit_for_context_local(
                const local_pos_value: Integer): string;
            const
                c_context_probe_limit_local = 4;
            var
                cached_value_local: string;
                local_span_len_value: Integer;
                local_key_value: string;
                local_results_value: TncCandidateList;
                local_idx_value: Integer;
                local_text_value: string;
            begin
                Result := '';
                if (local_pos_value < 0) or
                    (local_pos_value >= Length(syllables_local)) then
                begin
                    Exit;
                end;
                if (suffix_first_unit_cache_local <> nil) and
                    suffix_first_unit_cache_local.TryGetValue(local_pos_value,
                    cached_value_local) then
                begin
                    Exit(cached_value_local);
                end;

                for local_span_len_value :=
                    Min(4, Length(syllables_local) - local_pos_value) downto 2 do
                begin
                    local_key_value := build_query_key_local(local_pos_value,
                        local_span_len_value);
                    if (local_key_value = '') or
                        (not lookup_exact_full_pinyin_cached_local(
                        local_key_value, local_results_value)) then
                    begin
                        Continue;
                    end;
                    for local_idx_value := 0 to High(local_results_value) do
                    begin
                        if local_idx_value >= c_context_probe_limit_local then
                        begin
                            Break;
                        end;
                        if Trim(local_results_value[local_idx_value].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not long_exact_chunk_weight_is_eligible(
                            local_results_value[local_idx_value].dict_weight,
                            local_results_value[local_idx_value].has_dict_weight) then
                        begin
                            Continue;
                        end;
                        if is_runtime_chain_candidate(
                            local_results_value[local_idx_value]) or
                            is_runtime_common_pattern_candidate(
                            local_results_value[local_idx_value]) or
                            is_runtime_redup_candidate(
                            local_results_value[local_idx_value]) then
                        begin
                            Continue;
                        end;
                        local_text_value := Trim(
                            local_results_value[local_idx_value].text);
                        if get_candidate_text_unit_count(local_text_value) <>
                            local_span_len_value then
                        begin
                            Continue;
                        end;
                        Result :=
                            get_generic_first_text_unit_local(local_text_value);
                        Break;
                    end;
                    if Result <> '' then
                    begin
                        Break;
                    end;
                end;

                if Result = '' then
                begin
                    Result := get_fixed_single_text_local(
                        syllables_local[local_pos_value].text);
                end;

                if suffix_first_unit_cache_local <> nil then
                begin
                    suffix_first_unit_cache_local.AddOrSetValue(local_pos_value,
                        Result);
                end;
            end;

            function get_yu_following_context_adjustment_local(
                const local_last_syllable_value: string;
                const local_option_text_value: string;
                const local_next_pos_value: Integer): Integer;
            begin
                Result := 0;
            end;

            function try_get_protected_span_text_local(
                const local_start_value: Integer; const local_len_value: Integer;
                out out_text_value: string; out out_weight_value: Integer;
                out out_rank_value: Integer): Boolean;
            const
                c_protected_probe_limit_local = 8;
                c_protected_min_weight_local = 260;
            var
                local_key_value: string;
                local_results_value: TncCandidateList;
                local_idx_value: Integer;
                local_text_value: string;
                local_weight_value: Integer;
                best_text_value: string;
                best_weight_value: Integer;
                best_rank_value: Integer;
            begin
                Result := False;
                out_text_value := '';
                out_weight_value := 0;
                out_rank_value := 0;
                if (local_len_value < 2) or (local_len_value > 4) or
                    (local_start_value < 0) or
                    (local_start_value + local_len_value >
                    Length(syllables_local)) then
                begin
                    Exit;
                end;

                local_key_value := build_query_key_local(local_start_value,
                    local_len_value);
                if (local_key_value = '') or
                    (not lookup_exact_full_pinyin_cached_local(local_key_value,
                    local_results_value)) then
                begin
                    Exit;
                end;

                best_text_value := '';
                best_weight_value := Low(Integer);
                best_rank_value := 0;
                for local_idx_value := 0 to High(local_results_value) do
                begin
                    if local_idx_value >= c_protected_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(local_results_value[local_idx_value].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results_value[local_idx_value].dict_weight,
                        local_results_value[local_idx_value].has_dict_weight) then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(local_results_value[local_idx_value]) or
                        is_runtime_common_pattern_candidate(
                        local_results_value[local_idx_value]) or
                        is_runtime_redup_candidate(
                        local_results_value[local_idx_value]) then
                    begin
                        Continue;
                    end;

                    local_text_value := Trim(local_results_value[local_idx_value].text);
                    if (local_text_value = '') or
                        (get_candidate_text_unit_count(local_text_value) <>
                        local_len_value) then
                    begin
                        Continue;
                    end;

                    local_weight_value :=
                        get_effective_candidate_weight_local(
                        local_results_value[local_idx_value]);
                    if local_weight_value < c_protected_min_weight_local then
                    begin
                        Continue;
                    end;

                    if local_weight_value > best_weight_value then
                    begin
                        best_text_value := local_text_value;
                        best_weight_value := local_weight_value;
                        best_rank_value := local_idx_value + 1;
                    end;
                end;
                if best_text_value <> '' then
                begin
                    out_text_value := best_text_value;
                    out_weight_value := best_weight_value;
                    out_rank_value := best_rank_value;
                    Result := True;
                end;
            end;

            function get_protected_span_overlap_adjustment_local(
                const local_pos_value: Integer; const local_span_len_value: Integer;
                const local_text_value: string): Integer;
            var
                span_start_value: Integer;
                span_len_value: Integer;
                protected_text_value: string;
                protected_weight_value: Integer;
                protected_rank_value: Integer;
                local_units_value: TArray<string>;
                protected_units_value: TArray<string>;
                overlap_start_value: Integer;
                overlap_end_value: Integer;
                unit_pos_value: Integer;
                mismatch_value: Boolean;
                penalty_value: Integer;
                exact_bonus_value: Integer;

                function is_confident_protected_span_local(
                    const span_len_local: Integer; const span_weight_local: Integer;
                    const span_rank_local: Integer): Boolean;
                begin
                    Result := False;
                    if span_rank_local <= 0 then
                    begin
                        Exit;
                    end;

                    if span_rank_local = 1 then
                    begin
                        case span_len_local of
                            2:
                                Result := span_weight_local >= 650;
                            3:
                                Result := span_weight_local >= 420;
                            4:
                                Result := span_weight_local >= 360;
                        end;
                    end
                    else if (span_rank_local = 2) and (span_len_local >= 3) then
                    begin
                        Result := span_weight_local >= 900;
                    end
                    else if (span_rank_local <= 4) then
                    begin
                        case span_len_local of
                            2:
                                Result := span_weight_local >= 900;
                            3:
                                Result := span_weight_local >= 760;
                            4:
                                Result := span_weight_local >= 680;
                        end;
                    end;
                end;
            begin
                Result := 0;
                if (local_text_value = '') or (local_span_len_value <= 0) then
                begin
                    Exit;
                end;

                if (local_span_len_value >= 2) and
                    try_get_protected_span_text_local(local_pos_value,
                    local_span_len_value, protected_text_value,
                    protected_weight_value, protected_rank_value) and
                    is_confident_protected_span_local(local_span_len_value,
                    protected_weight_value, protected_rank_value) and
                    SameText(protected_text_value, local_text_value) then
                begin
                    exact_bonus_value := 180 + (local_span_len_value * 120) +
                        Min(520, protected_weight_value div 3);
                    if protected_rank_value > 1 then
                    begin
                        Dec(exact_bonus_value, (protected_rank_value - 1) * 160);
                    end;
                    if exact_bonus_value > 0 then
                    begin
                        Inc(Result, exact_bonus_value);
                    end;
                end;

                local_units_value := split_text_units(Trim(local_text_value));
                if Length(local_units_value) <> local_span_len_value then
                begin
                    Exit;
                end;

                for span_start_value := Max(0, local_pos_value - 3) to
                    Min(local_pos_value + local_span_len_value - 1,
                    Length(syllables_local) - 1) do
                begin
                    for span_len_value := 2 to 4 do
                    begin
                        if span_start_value + span_len_value >
                            Length(syllables_local) then
                        begin
                            Break;
                        end;
                        if (span_start_value = local_pos_value) and
                            (span_len_value = local_span_len_value) then
                        begin
                            Continue;
                        end;
                        if (span_start_value >= local_pos_value +
                            local_span_len_value) or
                            (local_pos_value >= span_start_value +
                            span_len_value) then
                        begin
                            Continue;
                        end;
                        if not try_get_protected_span_text_local(
                            span_start_value, span_len_value,
                            protected_text_value, protected_weight_value,
                            protected_rank_value) then
                        begin
                            Continue;
                        end;
                        if not is_confident_protected_span_local(span_len_value,
                            protected_weight_value, protected_rank_value) then
                        begin
                            Continue;
                        end;

                        protected_units_value :=
                            split_text_units(Trim(protected_text_value));
                        if Length(protected_units_value) <> span_len_value then
                        begin
                            Continue;
                        end;

                        overlap_start_value := Max(local_pos_value,
                            span_start_value);
                        overlap_end_value := Min(local_pos_value +
                            local_span_len_value, span_start_value + span_len_value);
                        if (overlap_end_value - overlap_start_value) <= 0 then
                        begin
                            Continue;
                        end;
                        mismatch_value := False;
                        for unit_pos_value := overlap_start_value to
                            overlap_end_value - 1 do
                        begin
                            if not SameText(
                                local_units_value[unit_pos_value -
                                local_pos_value],
                                protected_units_value[unit_pos_value -
                                span_start_value]) then
                            begin
                                mismatch_value := True;
                                Break;
                            end;
                        end;
                        if not mismatch_value then
                        begin
                            Continue;
                        end;

                        penalty_value := 360 + (span_len_value * 160) +
                            ((overlap_end_value - overlap_start_value) * 220) +
                            Min(1800, protected_weight_value);
                        if protected_rank_value > 1 then
                        begin
                            Dec(penalty_value, (protected_rank_value - 1) * 260);
                        end;
                        if penalty_value > 0 then
                        begin
                            Dec(Result, penalty_value);
                        end;
                    end;
                end;
            end;

            function build_span_score_local(const candidate_local: TncCandidate;
                const local_pos_value: Integer; const local_span_len_value: Integer;
                const local_rank_value: Integer): Integer;
            begin
                Result := get_effective_candidate_weight_local(candidate_local) *
                    c_weight_scale_local;
                case local_span_len_value of
                    1:
                        Dec(Result, c_single_unit_penalty_local);
                    2:
                        Inc(Result, c_two_unit_bonus_local);
                    3:
                        Inc(Result, c_three_unit_bonus_local);
                else
                    Inc(Result, c_long_unit_bonus_local +
                        Max(0, local_span_len_value - 4) *
                        c_per_extra_unit_bonus_local);
                end;
                if (local_pos_value = 0) and
                    (local_span_len_value = input_syllable_count) and
                    (input_syllable_count >= 4) then
                begin
                    Inc(Result, c_full_query_exact_bonus_local);
                end;
                if candidate_local.source = cs_user then
                begin
                    Inc(Result, 1200);
                end;
                Inc(Result, get_temporal_process_context_bonus_local(
                    Trim(candidate_local.text), local_pos_value));
                Inc(Result, get_protected_span_overlap_adjustment_local(
                    local_pos_value, local_span_len_value,
                    Trim(candidate_local.text)));
                Dec(Result, local_rank_value * c_candidate_rank_penalty_local);
            end;

            function take_trailing_text_units_local(const value: string;
                const max_units: Integer): string;
            var
                units_local: TArray<string>;
                unit_idx_local: Integer;
                start_idx_local: Integer;
            begin
                Result := '';
                if (Trim(value) = '') or (max_units <= 0) then
                begin
                    Exit;
                end;

                units_local := split_text_units(Trim(value));
                if Length(units_local) <= 0 then
                begin
                    Exit;
                end;

                start_idx_local := Length(units_local) - max_units;
                if start_idx_local < 0 then
                begin
                    start_idx_local := 0;
                end;

                for unit_idx_local := start_idx_local to High(units_local) do
                begin
                    Result := Result + units_local[unit_idx_local];
                end;
            end;

            function get_exact_base_prefix_transition_bonus_local(
                const prev_prev_text: string; const prev_text: string;
                const candidate_text: string): Integer;
            const
                c_prefix_bonus_cap = 2200;
            var
                combined_text_local: string;
                raw_bonus_local: Integer;
                best_bonus_local: Integer;
            begin
                Result := 0;
                if (m_dictionary = nil) or (Trim(prev_text) = '') or
                    (Trim(candidate_text) = '') then
                begin
                    Exit;
                end;

                best_bonus_local := 0;

                combined_text_local := take_trailing_text_units_local(
                    Trim(prev_text) + Trim(candidate_text), 6);
                if get_candidate_text_unit_count(combined_text_local) >= 2 then
                begin
                    raw_bonus_local :=
                        get_sentence_text_prefix_bonus_local(
                        combined_text_local);
                    if raw_bonus_local > best_bonus_local then
                    begin
                        best_bonus_local := raw_bonus_local;
                    end;
                end;

                if Trim(prev_prev_text) <> '' then
                begin
                    combined_text_local := take_trailing_text_units_local(
                        Trim(prev_prev_text) + Trim(prev_text) +
                        Trim(candidate_text), 6);
                    if get_candidate_text_unit_count(
                        combined_text_local) >= 2 then
                    begin
                        raw_bonus_local :=
                            get_sentence_text_prefix_bonus_local(
                            combined_text_local);
                        if raw_bonus_local > best_bonus_local then
                        begin
                            best_bonus_local := raw_bonus_local;
                        end;
                    end;
                end;

                if best_bonus_local > 0 then
                begin
                    Result := Min(c_prefix_bonus_cap,
                        (best_bonus_local * 3) div 4);
                end;
            end;

            function get_transition_bonus_local(
                const prev_prev_text: string; const prev_text: string;
                const candidate_text: string): Integer;
            const
                c_temporal_process_transition_bonus = 12000;
            var
                cache_key_local: string;
                pair_bonus_local: Integer;
                trigram_bonus_local: Integer;
                prefix_bonus_local: Integer;
            begin
                Result := 0;
                if (Trim(prev_text) = '') or (Trim(candidate_text) = '') then
                begin
                    Exit;
                end;

                cache_key_local := LowerCase(Trim(prev_prev_text)) + #1 +
                    LowerCase(Trim(prev_text)) + #1 +
                    LowerCase(Trim(candidate_text));
                if (transition_bonus_cache_local <> nil) and
                    transition_bonus_cache_local.TryGetValue(cache_key_local,
                    Result) then
                begin
                    Exit;
                end;

                pair_bonus_local := 0;
                trigram_bonus_local := 0;
                prefix_bonus_local := 0;
                if m_dictionary <> nil then
                begin
                    pair_bonus_local := m_dictionary.get_context_bonus(
                        prev_text, candidate_text);
                    if Trim(prev_prev_text) <> '' then
                    begin
                        trigram_bonus_local :=
                            m_dictionary.get_context_trigram_bonus(
                            prev_prev_text, prev_text, candidate_text);
                    end;
                    prefix_bonus_local :=
                        get_exact_base_prefix_transition_bonus_local(
                        prev_prev_text, prev_text, candidate_text);
                end;

                if trigram_bonus_local > 0 then
                begin
                    Result := trigram_bonus_local + (pair_bonus_local div 2);
                end
                else
                begin
                    Result := pair_bonus_local;
                end;

                if prefix_bonus_local > 0 then
                begin
                    if Result > 0 then
                    begin
                        Inc(Result, prefix_bonus_local div 2);
                    end
                    else
                    begin
                        Result := prefix_bonus_local;
                    end;
                end;

                if Result > 0 then
                begin
                    Result := Result * c_transition_bonus_scale_local;
                end;

                if (SameText(Trim(prev_text), string(Char($7684))) or
                    SameText(Trim(prev_text), string(Char($5730))) or
                    SameText(Trim(prev_text), string(Char($5F97))) or
                    SameText(Trim(prev_text), string(Char($4E4B)))) and
                    is_temporal_process_span_text_local(Trim(candidate_text)) then
                begin
                    Inc(Result, c_temporal_process_transition_bonus);
                end;

                if transition_bonus_cache_local <> nil then
                begin
                    transition_bonus_cache_local.AddOrSetValue(cache_key_local,
                        Result);
                end;
            end;

            function has_exact_phrase_span_local(const local_pos_value: Integer;
                const local_span_len_value: Integer): Boolean;
            var
                local_key_value: string;
                local_results_value: TncCandidateList;
                local_idx_value: Integer;
            begin
                Result := False;
                if local_span_len_value < 2 then
                begin
                    Exit;
                end;

                local_key_value := build_query_key_local(local_pos_value,
                    local_span_len_value);
                if not lookup_exact_full_pinyin_cached_local(local_key_value,
                    local_results_value) then
                begin
                    Exit;
                end;

                for local_idx_value := 0 to High(local_results_value) do
                begin
                    if Trim(local_results_value[local_idx_value].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results_value[local_idx_value].dict_weight,
                        local_results_value[local_idx_value].has_dict_weight) then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(
                        Trim(local_results_value[local_idx_value].text)) =
                        local_span_len_value then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function has_exact_phrase_ending_at_local(
                const local_end_idx_value: Integer): Boolean;
            var
                local_start_idx_value: Integer;
                local_span_len_value: Integer;
            begin
                Result := False;
                for local_start_idx_value := Max(0,
                    local_end_idx_value - c_max_span + 1) to
                    local_end_idx_value - 1 do
                begin
                    local_span_len_value := local_end_idx_value -
                        local_start_idx_value + 1;
                    if has_exact_phrase_span_local(local_start_idx_value,
                        local_span_len_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function candidate_breaks_stable_boundary_local(
                const local_text_value: string; const local_pos_value: Integer;
                const local_span_len_value: Integer;
                const local_candidate_weight_value: Integer): Boolean;
            var
                local_units_value: TArray<string>;
                local_split_idx_value: Integer;
                local_stable_text_value: string;
                protected_span_len_value: Integer;
                protected_text_value: string;
                protected_weight_value: Integer;
                protected_rank_value: Integer;
                protected_units_value: TArray<string>;
                overlap_len_value: Integer;
                overlap_idx_value: Integer;
                mismatch_value: Boolean;

                function is_confident_following_span_value(
                    const span_len_value: Integer;
                    const span_weight_value: Integer;
                    const span_rank_value: Integer): Boolean;
                begin
                    Result := False;
                    if span_rank_value <= 0 then
                    begin
                        Exit;
                    end;
                    if span_rank_value = 1 then
                    begin
                        case span_len_value of
                            2:
                                Result := span_weight_value >= 900;
                            3:
                                Result := span_weight_value >= 520;
                            4:
                                Result := span_weight_value >= 420;
                        end;
                    end
                    else if span_rank_value <= 4 then
                    begin
                        case span_len_value of
                            2:
                                Result := span_weight_value >= 900;
                            3:
                                Result := span_weight_value >= 760;
                            4:
                                Result := span_weight_value >= 680;
                        end;
                    end;
                end;
            begin
                Result := False;
                if local_span_len_value < 2 then
                begin
                    Exit;
                end;

                local_units_value := split_text_units(Trim(local_text_value));
                if Length(local_units_value) <> local_span_len_value then
                begin
                    Exit;
                end;

                for local_split_idx_value := 1 to local_span_len_value - 1 do
                begin
                    for protected_span_len_value := Min(4,
                        Length(syllables_local) - local_pos_value -
                        local_split_idx_value) downto 2 do
                    begin
                        if not try_get_protected_span_text_local(
                            local_pos_value + local_split_idx_value,
                            protected_span_len_value, protected_text_value,
                            protected_weight_value, protected_rank_value) then
                        begin
                            Continue;
                        end;
                        if not is_confident_following_span_value(
                            protected_span_len_value, protected_weight_value,
                            protected_rank_value) then
                        begin
                            Continue;
                        end;

                        protected_units_value := split_text_units(
                            Trim(protected_text_value));
                        if Length(protected_units_value) <>
                            protected_span_len_value then
                        begin
                            Continue;
                        end;
                        overlap_len_value := Min(local_span_len_value -
                            local_split_idx_value, protected_span_len_value);
                        mismatch_value := False;
                        for overlap_idx_value := 0 to overlap_len_value - 1 do
                        begin
                            if not SameText(local_units_value[
                                local_split_idx_value + overlap_idx_value],
                                protected_units_value[overlap_idx_value]) then
                            begin
                                mismatch_value := True;
                                Break;
                            end;
                        end;
                        if mismatch_value and
                            (local_candidate_weight_value <=
                            protected_weight_value + 220) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                local_stable_text_value := get_fixed_single_text_local(
                    syllables_local[local_pos_value].text);
                if (local_stable_text_value <> '') and
                    (not SameText(local_units_value[0], local_stable_text_value)) and
                    (local_span_len_value = 2) and
                    (local_candidate_weight_value <
                    c_weak_fixed_head_phrase_weight) then
                begin
                    Exit(True);
                end;
                if (local_stable_text_value <> '') and
                    (not SameText(local_units_value[0], local_stable_text_value)) and
                    has_exact_phrase_span_local(local_pos_value + 1,
                    local_span_len_value - 1) then
                begin
                    Exit(True);
                end;

                local_stable_text_value := get_fixed_single_text_local(
                    syllables_local[local_pos_value + local_span_len_value - 1].text);
                if (local_stable_text_value <> '') and
                    (not SameText(local_units_value[local_span_len_value - 1],
                    local_stable_text_value)) and
                    has_exact_phrase_ending_at_local(
                    local_pos_value + local_span_len_value - 2) then
                begin
                    Exit(True);
                end;

                for local_split_idx_value := 1 to local_span_len_value - 1 do
                begin
                    local_stable_text_value := get_fixed_single_text_local(
                        syllables_local[local_pos_value + local_split_idx_value].text);
                    if local_stable_text_value = '' then
                    begin
                        Continue;
                    end;
                    if SameText(local_units_value[local_split_idx_value],
                        local_stable_text_value) then
                    begin
                        Continue;
                    end;
                    if has_exact_phrase_span_local(local_pos_value,
                        local_split_idx_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function chain_option_is_nonbase_two_plus_single_local(
                const local_query_key_value: string;
                const local_span_len_value: Integer;
                const local_text_value: string): Boolean;
            var
                local_syllables_value: TncPinyinParseResult;
                local_units_value: TArray<string>;
                local_head_key_value: string;
                local_tail_key_value: string;
                local_tail_syllable_value: string;
                local_head_text_value: string;
                local_tail_text_value: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (local_span_len_value <> 3) then
                begin
                    Exit;
                end;
                if m_dictionary.is_base_entry(local_query_key_value,
                    local_text_value) or
                    m_dictionary.is_user_entry(local_query_key_value,
                    local_text_value) then
                begin
                    Exit;
                end;

                local_syllables_value := get_effective_compact_pinyin_syllables(
                    local_query_key_value);
                local_units_value := split_text_units(Trim(local_text_value));
                if (Length(local_syllables_value) <> 3) or
                    (Length(local_units_value) <> 3) then
                begin
                    Exit;
                end;

                local_tail_syllable_value := normalize_pinyin_text(
                    local_syllables_value[2].text);
                if (local_tail_syllable_value = 'de') or
                    (local_tail_syllable_value = 'le') or
                    (local_tail_syllable_value = 'la') or
                    (local_tail_syllable_value = 'ba') or
                    (local_tail_syllable_value = 'ma') then
                begin
                    Exit;
                end;

                local_head_key_value := normalize_pinyin_text(
                    local_syllables_value[0].text) +
                    normalize_pinyin_text(local_syllables_value[1].text);
                local_tail_key_value := local_tail_syllable_value;
                local_head_text_value := local_units_value[0] +
                    local_units_value[1];
                local_tail_text_value := local_units_value[2];

                Result := direct_exact_lookup_has_text_local(local_head_key_value,
                    local_head_text_value, 2) and
                    (direct_exact_lookup_has_text_local(local_tail_key_value,
                    local_tail_text_value, 1) or
                    m_dictionary.single_char_matches_pinyin(local_tail_key_value,
                    local_tail_text_value));
            end;

            function compare_chain_state_local(
                const left_local: TExactChainStateLocal;
                const right_local: TExactChainStateLocal): Integer;
            begin
                Result := left_local.score - right_local.score;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := left_local.path_preference_score -
                    right_local.path_preference_score;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := right_local.single_count - left_local.single_count;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := right_local.segment_count - left_local.segment_count;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := left_local.phrase_count - right_local.phrase_count;
                if Result <> 0 then
                begin
                    Exit;
                end;
                Result := Length(right_local.path_text) -
                    Length(left_local.path_text);
            end;

            procedure sort_chain_states_local(
                var states_local: TExactChainStateListLocal);
            var
                left_idx_local: Integer;
                right_idx_local: Integer;
                tmp_state_local: TExactChainStateLocal;
            begin
                for left_idx_local := 0 to High(states_local) do
                begin
                    for right_idx_local := left_idx_local + 1 to High(states_local) do
                    begin
                        if compare_chain_state_local(states_local[right_idx_local],
                            states_local[left_idx_local]) > 0 then
                        begin
                            tmp_state_local := states_local[left_idx_local];
                            states_local[left_idx_local] := states_local[right_idx_local];
                            states_local[right_idx_local] := tmp_state_local;
                        end;
                    end;
                end;
            end;

            procedure remember_chain_state_local(
                var states_local: TExactChainStateListLocal;
                const state_local: TExactChainStateLocal);
            var
                idx_local: Integer;
                state_key_local: string;
                existing_key_local: string;
            begin
                if Trim(state_local.text) = '' then
                begin
                    Exit;
                end;

                state_key_local := LowerCase(Trim(state_local.prev_prev_text)) +
                    #1 + LowerCase(Trim(state_local.prev_text)) + #1 +
                    IntToStr(Min(state_local.single_count, 4)) + #1 +
                    IntToStr(Min(state_local.phrase_count, 4)) + #1 +
                    IntToStr(Min(state_local.segment_count, 8));

                for idx_local := 0 to High(states_local) do
                begin
                    existing_key_local :=
                        LowerCase(Trim(states_local[idx_local].prev_prev_text)) +
                        #1 + LowerCase(Trim(states_local[idx_local].prev_text)) +
                        #1 + IntToStr(Min(states_local[idx_local].single_count, 4)) +
                        #1 + IntToStr(Min(states_local[idx_local].phrase_count, 4)) +
                        #1 + IntToStr(Min(states_local[idx_local].segment_count, 8));
                    if SameText(existing_key_local, state_key_local) then
                    begin
                        if compare_chain_state_local(state_local,
                            states_local[idx_local]) > 0 then
                        begin
                            states_local[idx_local] := state_local;
                            sort_chain_states_local(states_local);
                        end;
                        Exit;
                    end;
                end;

                SetLength(states_local, Length(states_local) + 1);
                states_local[High(states_local)] := state_local;
                sort_chain_states_local(states_local);
                if Length(states_local) > state_keep_limit_local then
                begin
                    SetLength(states_local, state_keep_limit_local);
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := 0;
            out_phrase_count := 0;
            transition_bonus_cache_local := TDictionary<string, Integer>.Create;
            suffix_first_unit_cache_local := TDictionary<Integer, string>.Create;
            try
                state_keep_limit_local := c_state_keep_limit_local;
                multi_candidate_limit_local := c_multi_candidate_limit_local;
                if Length(syllables_local) >= 12 then
                begin
                    state_keep_limit_local := 5;
                    multi_candidate_limit_local := 3;
                end
                else if Length(syllables_local) >= 8 then
                begin
                    state_keep_limit_local := 6;
                    multi_candidate_limit_local := 3;
                end
                else if Length(syllables_local) >= 6 then
                begin
                    state_keep_limit_local := 7;
                    multi_candidate_limit_local := 3;
                end;

                SetLength(best_states_local, Length(syllables_local) + 1);
                SetLength(best_states_local[0], 1);
                best_states_local[0][0].text := '';
                best_states_local[0][0].path_text := '';
                best_states_local[0][0].prev_text := '';
                best_states_local[0][0].prev_prev_text := '';
                best_states_local[0][0].score := 0;
                best_states_local[0][0].path_preference_score := 0;
                best_states_local[0][0].segment_count := 0;
                best_states_local[0][0].phrase_count := 0;
                best_states_local[0][0].single_count := 0;

                for pos_local := 0 to Length(syllables_local) - 1 do
                begin
                    if Length(best_states_local[pos_local]) = 0 then
                    begin
                        Continue;
                    end;

                    for span_len_local := 1 to Min(get_effective_max_span_local,
                        Length(syllables_local) - pos_local) do
                    begin
                        span_key_local := build_query_key_local(pos_local,
                            span_len_local);
                        SetLength(exact_results_local, 0);
                        if not lookup_exact_full_pinyin_cached_local(
                            span_key_local, exact_results_local) and
                            (span_len_local = 1) and (m_dictionary <> nil) then
                        begin
                            m_dictionary.lookup(span_key_local,
                                exact_results_local);
                        end;

                        if span_len_local = 1 then
                        begin
                            candidate_limit_local :=
                                c_single_candidate_limit_local;
                        end
                        else
                        begin
                            candidate_limit_local :=
                                multi_candidate_limit_local;
                        end;
                        next_pos_local := pos_local + span_len_local;

                        for state_idx_local := 0 to
                            High(best_states_local[pos_local]) do
                        begin
                            base_state_local :=
                                best_states_local[pos_local][state_idx_local];
                            for option_idx_local := 0 to High(exact_results_local) do
                            begin
                                if option_idx_local >= candidate_limit_local then
                                begin
                                    Break;
                                end;

                                option_candidate_local :=
                                    exact_results_local[option_idx_local];
                                if Trim(option_candidate_local.comment) <> '' then
                                begin
                                    Continue;
                                end;
                                if (span_len_local >= 2) and
                                    (not long_exact_chunk_weight_is_eligible(
                                    option_candidate_local.dict_weight,
                                    option_candidate_local.has_dict_weight)) then
                                begin
                                    Continue;
                                end;

                                option_text_local := Trim(
                                    option_candidate_local.text);
                                option_units_local :=
                                    get_candidate_text_unit_count(
                                    option_text_local);
                                if (option_text_local = '') or
                                    (option_units_local <> span_len_local) then
                                begin
                                    Continue;
                                end;
                                if chain_option_is_nonbase_two_plus_single_local(
                                    span_key_local, span_len_local,
                                    option_text_local) then
                                begin
                                    Continue;
                                end;
                                if candidate_breaks_stable_boundary_local(
                                    option_text_local, pos_local,
                                    span_len_local,
                                    get_effective_candidate_weight_local(
                                    option_candidate_local)) then
                                begin
                                    Continue;
                                end;
                                if span_len_local = 1 then
                                begin
                                    option_is_preferred_local := False;
                                    if not match_single_char_candidate_for_syllable(
                                        syllables_local[pos_local].text,
                                        option_text_local,
                                        option_is_preferred_local) or
                                        (not option_is_preferred_local) then
                                    begin
                                        Continue;
                                    end;
                                    if not SameText(option_text_local,
                                        get_fixed_single_text_local(
                                        syllables_local[pos_local].text)) then
                                    begin
                                        Continue;
                                    end;
                                end;

                                option_score_local := build_span_score_local(
                                    option_candidate_local, pos_local,
                                    span_len_local, option_idx_local);
                                Inc(option_score_local,
                                    get_yu_following_context_adjustment_local(
                                    syllables_local[next_pos_local - 1].text,
                                    option_text_local, next_pos_local));
                                direction_bonus_local :=
                                    get_directional_locative_span_bonus_local(
                                    option_text_local);
                                direction_context_bonus_local :=
                                    get_directional_locative_context_bonus_local(
                                    option_text_local, pos_local,
                                    span_len_local);
                                Inc(option_score_local,
                                    direction_bonus_local);
                                Inc(option_score_local,
                                    direction_context_bonus_local);
                                Dec(option_score_local,
                                    get_crossing_previous_exact_phrase_penalty_local(
                                    pos_local, span_len_local));
                                Dec(option_score_local,
                                    get_stronger_split_span_penalty_local(
                                    pos_local, span_len_local,
                                    get_effective_candidate_weight_local(
                                    option_candidate_local)));
                                if (span_len_local = 2) and
                                    (normalize_pinyin_text(
                                    syllables_local[pos_local].text) = 'yi') then
                                begin
                                    candidate_units_local :=
                                        split_text_units(option_text_local);
                                    if (Length(candidate_units_local) = 2) and
                                        (candidate_units_local[0] =
                                        string(Char($4E00))) and
                                        is_quantity_tail_unit_local(
                                        candidate_units_local[1]) then
                                    begin
                                        Inc(option_score_local,
                                            c_quantity_tail_bonus);
                                    end;
                                end;
                                transition_bonus_local :=
                                    get_transition_bonus_local(
                                    base_state_local.prev_prev_text,
                                    base_state_local.prev_text,
                                    option_text_local);

                                combined_state_local.text :=
                                    base_state_local.text + option_text_local;
                                combined_state_local.path_text :=
                                    combine_path_segments_local(
                                    base_state_local.path_text,
                                    option_text_local);
                                combined_state_local.prev_prev_text :=
                                    base_state_local.prev_text;
                                combined_state_local.prev_text :=
                                    option_text_local;
                                combined_state_local.segment_count :=
                                    base_state_local.segment_count + 1;
                                combined_state_local.phrase_count :=
                                    base_state_local.phrase_count;
                                if span_len_local >= 2 then
                                begin
                                    Inc(combined_state_local.phrase_count);
                                end;
                                combined_state_local.single_count :=
                                    base_state_local.single_count;
                                if span_len_local = 1 then
                                begin
                                    Inc(combined_state_local.single_count);
                                end;
                                combined_state_local.path_preference_score :=
                                    base_state_local.path_preference_score +
                                    transition_bonus_local;
                                combined_state_local.score :=
                                    base_state_local.score + option_score_local -
                                    c_segment_penalty_local +
                                    transition_bonus_local;
                                if next_pos_local = Length(syllables_local) then
                                begin
                                    Inc(combined_state_local.score,
                                        c_completion_bonus_local);
                                end;
                                remember_chain_state_local(
                                    best_states_local[next_pos_local],
                                    combined_state_local);
                            end;

                            if span_len_local <> 1 then
                            begin
                                Continue;
                            end;

                            fixed_single_text_local := get_fixed_single_text_local(
                                syllables_local[pos_local].text);
                            if fixed_single_text_local = '' then
                            begin
                                Continue;
                            end;
                            if should_skip_fixed_single_chain_step_local(pos_local,
                                fixed_single_text_local) then
                            begin
                                Continue;
                            end;

                            fixed_single_candidate_local.text :=
                                fixed_single_text_local;
                            fixed_single_candidate_local.comment := '';
                            fixed_single_candidate_local.score := 420;
                            fixed_single_candidate_local.source := cs_rule;
                            fixed_single_candidate_local.has_dict_weight := False;
                            fixed_single_candidate_local.dict_weight := 0;

                            option_score_local := build_span_score_local(
                                fixed_single_candidate_local, pos_local, 1, 0) +
                                c_stable_single_bonus_local;
                            transition_bonus_local :=
                                get_transition_bonus_local(
                                base_state_local.prev_prev_text,
                                base_state_local.prev_text,
                                fixed_single_candidate_local.text);
                            combined_state_local.text := base_state_local.text +
                                fixed_single_candidate_local.text;
                            combined_state_local.path_text :=
                                combine_path_segments_local(
                                base_state_local.path_text,
                                fixed_single_candidate_local.text);
                            combined_state_local.prev_prev_text :=
                                base_state_local.prev_text;
                            combined_state_local.prev_text :=
                                fixed_single_candidate_local.text;
                            combined_state_local.segment_count :=
                                base_state_local.segment_count + 1;
                            combined_state_local.phrase_count :=
                                base_state_local.phrase_count;
                            combined_state_local.single_count :=
                                base_state_local.single_count + 1;
                            combined_state_local.path_preference_score :=
                                base_state_local.path_preference_score +
                                transition_bonus_local;
                            combined_state_local.score :=
                                base_state_local.score + option_score_local -
                                c_segment_penalty_local +
                                transition_bonus_local;
                            if next_pos_local = Length(syllables_local) then
                            begin
                                Inc(combined_state_local.score,
                                    c_completion_bonus_local);
                            end;
                            remember_chain_state_local(
                                best_states_local[next_pos_local],
                                combined_state_local);
                        end;
                    end;
                end;

                if Length(best_states_local[Length(syllables_local)]) = 0 then
                begin
                    Exit;
                end;

                best_state_local :=
                    best_states_local[Length(syllables_local)][0];
                if (best_state_local.phrase_count <= 0) and
                    (input_syllable_count >= 4) then
                begin
                    Dec(best_state_local.score, c_no_phrase_penalty_local);
                end;
                if best_state_local.single_count >=
                    best_state_local.segment_count then
                begin
                    Dec(best_state_local.score, c_no_phrase_penalty_local);
                end;
                if (best_state_local.path_text <> '') and
                    (m_dictionary <> nil) then
                begin
                    Inc(best_state_local.score,
                        get_cached_query_segment_path_bonus(lookup_text,
                        best_state_local.path_text));
                    Dec(best_state_local.score,
                        get_cached_query_segment_path_penalty(
                        lookup_text, best_state_local.path_text));
                end;
                if get_candidate_text_unit_count(best_state_local.text) <>
                    input_syllable_count then
                begin
                    Exit;
                end;
                if (input_syllable_count >= 6) and
                    (best_state_local.phrase_count >= 2) and
                    (best_state_local.single_count * 2 <=
                    best_state_local.segment_count) then
                begin
                    Inc(best_state_local.score, c_trusted_chain_bonus_local);
                end;

                out_text := best_state_local.text;
                out_path := best_state_local.path_text;
                out_score := best_state_local.score;
                out_phrase_count := best_state_local.phrase_count;
                Result := True;
            finally
                suffix_first_unit_cache_local.Free;
                transition_bonus_cache_local.Free;
            end;
        end;

        function try_build_compact_exact_phrase_chain_fast_local(
            out out_text: string; out out_path: string; out out_score: Integer;
            out out_phrase_count: Integer): Boolean;
        const
            c_low_score_local = Low(Integer) div 4;
            c_span_probe_limit_local = 8;
            c_weight_scale_local = 8;
            c_segment_penalty_local = 520;
            c_min_phrase_count_local = 2;
        var
            best_scores_local: TArray<Integer>;
            best_texts_local: TArray<string>;
            best_paths_local: TArray<string>;
            best_phrase_counts_local: TArray<Integer>;
            best_single_counts_local: TArray<Integer>;
            pos_local: Integer;
            span_len_local: Integer;
            next_pos_local: Integer;
            span_text_local: string;
            span_score_local: Integer;
            combined_score_local: Integer;
            suffix_adjustment_local: Integer;
            combined_path_local: string;
            compact_protected_span_text_cache_local: TDictionary<string, string>;
            compact_protected_span_weight_cache_local: TDictionary<string, Integer>;
            compact_protected_span_rank_cache_local: TDictionary<string, Integer>;

            function get_compact_first_text_unit_local(const value: string): string;
            var
                trimmed_local: string;
            begin
                Result := '';
                trimmed_local := Trim(value);
                if Length(trimmed_local) > 0 then
                begin
                    Result := trimmed_local[1];
                end;
            end;

            function get_compact_last_text_unit_local(const value: string): string;
            var
                trimmed_local: string;
            begin
                Result := '';
                trimmed_local := Trim(value);
                if Length(trimmed_local) > 0 then
                begin
                    Result := trimmed_local[Length(trimmed_local)];
                end;
            end;

            function is_compact_context_unit_code_local(const value: string;
                const code_local: Integer): Boolean;
            begin
                Result := (Length(value) = 1) and (Ord(value[1]) = code_local);
            end;

            function is_compact_predicate_head_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $505A, $5230, $53BB, $544A, $547C, $56DE, $5BFB, $5F00,
                    $60F3, $6210, $6253, $627E, $63A5, $63D0, $6539, $653E,
                    $6765, $7528, $770B, $7814, $7ED9, $80FD, $8981, $89C1,
                    $8BF4, $8D70, $8F93, $9001, $95EE, $8868:
                        Result := True;
                end;
            end;

            function is_compact_location_head_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4E0A, $4E0B, $4E2D, $4E9B, $4F4D, $5185, $524D, $540E,
                    $56FD, $5730, $57CE, $5B66, $5BB6, $5C42, $5E02, $65B9,
                    $65F6, $672C, $6761, $6B21, $79CD, $7C7B, $9879:
                        Result := True;
                end;
            end;

            function is_compact_bound_yu_left_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4F4D, $5173, $5BF9, $7528, $5C5E, $7B49, $6E90, $57FA,
                    $5904, $51FA, $6025, $7126, $9274, $81F3, $4FBF, $5229,
                    $7531, $9650:
                        Result := True;
                end;
            end;

            function is_compact_predicate_yu_left_unit_local(
                const unit_local: string): Boolean;
            begin
                Result := False;
                if Length(unit_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(unit_local[1]) of
                    $4E50, $4FBF, $5229, $5584, $5FD9, $6025, $75B2:
                        Result := True;
                end;
            end;

            function get_compact_suffix_context_adjustment_local(
                const chunk_last_syllable_local: string;
                const chunk_text_local: string;
                const suffix_text_local: string): Integer;
            begin
                Result := 0;
            end;

            function try_get_compact_protected_span_text_local(
                const local_start_idx: Integer; const local_span_len: Integer;
                out out_text: string; out out_weight: Integer;
                out out_rank: Integer): Boolean;
            const
                c_protected_probe_limit_local = 8;
                c_protected_min_weight_local = 260;
            var
                cache_key_local: string;
                cached_weight_local: Integer;
                local_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                local_text: string;
                local_weight: Integer;
            begin
                Result := False;
                out_text := '';
                out_weight := 0;
                out_rank := 0;
                if (local_span_len < 2) or (local_span_len > 4) or
                    (local_start_idx < 0) or
                    (local_start_idx + local_span_len >
                    Length(syllables_local)) then
                begin
                    Exit;
                end;

                cache_key_local := IntToStr(local_start_idx) + ':' +
                    IntToStr(local_span_len);
                if (compact_protected_span_weight_cache_local <> nil) and
                    compact_protected_span_weight_cache_local.TryGetValue(
                    cache_key_local, cached_weight_local) then
                begin
                    if cached_weight_local <= 0 then
                    begin
                        Exit(False);
                    end;
                    out_weight := cached_weight_local;
                    if compact_protected_span_text_cache_local <> nil then
                    begin
                        compact_protected_span_text_cache_local.TryGetValue(
                            cache_key_local, out_text);
                    end;
                    if compact_protected_span_rank_cache_local <> nil then
                    begin
                        compact_protected_span_rank_cache_local.TryGetValue(
                            cache_key_local, out_rank);
                    end;
                    Exit(out_text <> '');
                end;

                local_key := build_query_key_local(local_start_idx,
                    local_span_len);
                if (local_key = '') or
                    (not lookup_exact_full_pinyin_cached_local(local_key,
                    local_results)) then
                begin
                    if compact_protected_span_weight_cache_local <> nil then
                    begin
                        compact_protected_span_weight_cache_local.AddOrSetValue(
                            cache_key_local, -1);
                    end;
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_protected_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results[local_idx].dict_weight,
                        local_results[local_idx].has_dict_weight) then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(local_results[local_idx]) or
                        is_runtime_common_pattern_candidate(local_results[local_idx]) or
                        is_runtime_redup_candidate(local_results[local_idx]) then
                    begin
                        Continue;
                    end;

                    local_text := Trim(local_results[local_idx].text);
                    if (local_text = '') or
                        (get_candidate_text_unit_count(local_text) <>
                        local_span_len) then
                    begin
                        Continue;
                    end;

                    local_weight := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight > local_weight) then
                    begin
                        local_weight := local_results[local_idx].dict_weight;
                    end;
                    if local_weight < c_protected_min_weight_local then
                    begin
                        Continue;
                    end;
                    if not (((local_idx = 0) and
                        (((local_span_len = 2) and (local_weight >= 650)) or
                        ((local_span_len = 3) and (local_weight >= 420)) or
                        ((local_span_len >= 4) and (local_weight >= 360)))) or
                        ((local_idx = 1) and (local_span_len >= 3) and
                        (local_weight >= 900))) then
                    begin
                        Continue;
                    end;

                    out_text := local_text;
                    out_weight := local_weight;
                    out_rank := local_idx + 1;
                    if compact_protected_span_weight_cache_local <> nil then
                    begin
                        compact_protected_span_weight_cache_local.AddOrSetValue(
                            cache_key_local, out_weight);
                    end;
                    if compact_protected_span_text_cache_local <> nil then
                    begin
                        compact_protected_span_text_cache_local.AddOrSetValue(
                            cache_key_local, out_text);
                    end;
                    if compact_protected_span_rank_cache_local <> nil then
                    begin
                        compact_protected_span_rank_cache_local.AddOrSetValue(
                            cache_key_local, out_rank);
                    end;
                    Exit(True);
                end;

                if compact_protected_span_weight_cache_local <> nil then
                begin
                    compact_protected_span_weight_cache_local.AddOrSetValue(
                        cache_key_local, -1);
                end;
            end;

            function get_compact_protected_span_adjustment_local(
                const local_start_idx: Integer; const local_span_len: Integer;
                const local_text: string): Integer;
            var
                span_start: Integer;
                span_len: Integer;
                protected_text: string;
                protected_weight: Integer;
                protected_rank: Integer;
                local_units: TArray<string>;
                protected_units: TArray<string>;
                overlap_start: Integer;
                overlap_end: Integer;
                unit_pos: Integer;
                mismatch: Boolean;
                penalty: Integer;
                bonus: Integer;
            begin
                Result := 0;
                if (local_text = '') or (local_span_len <= 0) then
                begin
                    Exit;
                end;

                if (local_span_len >= 2) and
                    try_get_compact_protected_span_text_local(local_start_idx,
                    local_span_len, protected_text, protected_weight,
                    protected_rank) and SameText(protected_text, local_text) then
                begin
                    bonus := 220 + (local_span_len * 80) +
                        Min(360, protected_weight div 4);
                    if (local_span_len >= 3) and (protected_rank = 1) and
                        (protected_weight >= 900) then
                    begin
                        Inc(bonus, Min(9000, protected_weight * 6) +
                            Max(0, local_span_len - 2) * 1800);
                    end;
                    if protected_rank > 1 then
                    begin
                        Dec(bonus, (protected_rank - 1) * 50);
                    end;
                    if bonus > 0 then
                    begin
                        Inc(Result, bonus);
                    end;
                end;

                local_units := split_text_units(Trim(local_text));
                if Length(local_units) <> local_span_len then
                begin
                    Exit;
                end;

                for span_start := Max(0, local_start_idx - 3) to
                    Min(local_start_idx + local_span_len - 1,
                    Length(syllables_local) - 1) do
                begin
                    for span_len := 2 to 4 do
                    begin
                        if span_start + span_len > Length(syllables_local) then
                        begin
                            Break;
                        end;
                        if (span_start = local_start_idx) and
                            (span_len = local_span_len) then
                        begin
                            Continue;
                        end;
                        if (span_start >= local_start_idx + local_span_len) or
                            (local_start_idx >= span_start + span_len) then
                        begin
                            Continue;
                        end;
                        if not try_get_compact_protected_span_text_local(
                            span_start, span_len, protected_text,
                            protected_weight, protected_rank) then
                        begin
                            Continue;
                        end;

                        protected_units := split_text_units(Trim(protected_text));
                        if Length(protected_units) <> span_len then
                        begin
                            Continue;
                        end;

                        overlap_start := Max(local_start_idx, span_start);
                        overlap_end := Min(local_start_idx + local_span_len,
                            span_start + span_len);
                        if (overlap_end - overlap_start) < 2 then
                        begin
                            Continue;
                        end;
                        mismatch := False;
                        for unit_pos := overlap_start to overlap_end - 1 do
                        begin
                            if not SameText(local_units[unit_pos - local_start_idx],
                                protected_units[unit_pos - span_start]) then
                            begin
                                mismatch := True;
                                Break;
                            end;
                        end;
                        if not mismatch then
                        begin
                            Continue;
                        end;

                        penalty := 360 + (span_len * 160) +
                            ((overlap_end - overlap_start) * 220) +
                            Min(1800, protected_weight);
                        if (span_len >= 3) and (protected_rank = 1) and
                            (protected_weight >= 900) then
                        begin
                            Inc(penalty, Min(9000, protected_weight * 6) +
                                Max(0, span_len - 2) * 1800);
                        end;
                        if protected_rank > 1 then
                        begin
                            Dec(penalty, (protected_rank - 1) * 100);
                        end;
                        if penalty > 0 then
                        begin
                            Dec(Result, penalty);
                        end;
                    end;
                end;
            end;

            function compact_direct_exact_lookup_has_text_local(
                const local_query_key: string; const local_text: string;
                const local_expected_units: Integer): Boolean;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                candidate_text_local: string;
            begin
                Result := False;
                if (local_query_key = '') or (local_text = '') or
                    (local_expected_units <= 0) then
                begin
                    Exit;
                end;
                if not lookup_exact_full_pinyin_cached_local(local_query_key,
                    local_results) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_span_probe_limit_local then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if not long_exact_chunk_weight_is_eligible(
                        local_results[local_idx].dict_weight,
                        local_results[local_idx].has_dict_weight) then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(local_results[local_idx]) or
                        is_runtime_common_pattern_candidate(
                        local_results[local_idx]) or
                        is_runtime_redup_candidate(local_results[local_idx]) then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(local_results[local_idx].text);
                    if SameText(candidate_text_local, local_text) and
                        (get_candidate_text_unit_count(candidate_text_local) =
                        local_expected_units) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function compact_nonbase_two_plus_single_exact_chunk_local(
                const local_query_key: string; const local_chunk_len: Integer;
                const local_text: string): Boolean;
            var
                local_syllables: TncPinyinParseResult;
                local_units: TArray<string>;
                head_key_local: string;
                tail_key_local: string;
                tail_syllable_local: string;
                head_text_local: string;
                tail_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (local_chunk_len <> 3) then
                begin
                    Exit;
                end;
                if m_dictionary.is_base_entry(local_query_key, local_text) or
                    m_dictionary.is_user_entry(local_query_key, local_text) then
                begin
                    Exit;
                end;

                local_syllables := get_effective_compact_pinyin_syllables(
                    local_query_key);
                local_units := split_text_units(Trim(local_text));
                if (Length(local_syllables) <> 3) or
                    (Length(local_units) <> 3) then
                begin
                    Exit;
                end;

                tail_syllable_local := normalize_pinyin_text(
                    local_syllables[2].text);
                if (tail_syllable_local = 'de') or
                    (tail_syllable_local = 'le') or
                    (tail_syllable_local = 'la') or
                    (tail_syllable_local = 'ba') or
                    (tail_syllable_local = 'ma') then
                begin
                    Exit;
                end;

                head_key_local := normalize_pinyin_text(local_syllables[0].text) +
                    normalize_pinyin_text(local_syllables[1].text);
                tail_key_local := tail_syllable_local;
                head_text_local := local_units[0] + local_units[1];
                tail_text_local := local_units[2];

                Result := compact_direct_exact_lookup_has_text_local(
                    head_key_local, head_text_local, 2) and
                    (compact_direct_exact_lookup_has_text_local(tail_key_local,
                    tail_text_local, 1) or
                    m_dictionary.single_char_matches_pinyin(tail_key_local,
                    tail_text_local));
            end;

            function compact_allows_fixed_single_before_phrase_local(
                const local_fixed_text: string): Boolean;
            begin
                Result := is_pronoun_fixed_single_text_local(local_fixed_text) or
                    is_bridge_fixed_single_text_local(local_fixed_text) or
                    (local_fixed_text = string(Char($6CA1))) or
                    (local_fixed_text = string(Char($4E00))) or

                    (local_fixed_text = string(Char($4E5F))) or
                    (local_fixed_text = string(Char($90FD))) or
                    (local_fixed_text = string(Char($5728))) or
                    (local_fixed_text = string(Char($53BB))) or
                    (local_fixed_text = string(Char($548C)));
            end;

            function try_get_best_exact_phrase_span_local(
                const local_start_idx: Integer; const local_span_len: Integer;
                out out_span_text: string; out out_span_score: Integer): Boolean;
            var
                span_key_local: string;
                span_results_local: TncCandidateList;
                span_idx_local: Integer;
                candidate_text_local: string;
                candidate_units_local: Integer;
                candidate_score_local: Integer;
                subphrase_text_local: string;
                subphrase_score_local: Integer;
                fixed_single_text_local: string;
                combined_text_local: string;
                combined_score_local: Integer;
                has_full_exact_span_local: Boolean;

                function try_get_exact_subphrase_local(
                    const local_sub_start_idx: Integer;
                    const local_sub_len: Integer; out out_sub_text: string;
                    out out_sub_score: Integer): Boolean;
                var
                    sub_key_local: string;
                    sub_results_local: TncCandidateList;
                    sub_idx_local: Integer;
                    sub_text_local: string;
                    sub_units_local: Integer;
                    sub_score_local: Integer;
                begin
                    Result := False;
                    out_sub_text := '';
                    out_sub_score := 0;
                    if (local_sub_len < 2) or (local_sub_len > 4) or
                        (local_sub_start_idx < 0) or
                        (local_sub_start_idx + local_sub_len >
                        Length(syllables_local)) then
                    begin
                        Exit;
                    end;

                    sub_key_local := build_query_key_local(local_sub_start_idx,
                        local_sub_len);
                    if (sub_key_local = '') or
                        (not lookup_exact_full_pinyin_cached_local(sub_key_local,
                        sub_results_local)) then
                    begin
                        Exit;
                    end;

                    for sub_idx_local := 0 to High(sub_results_local) do
                    begin
                        if sub_idx_local >= c_span_probe_limit_local then
                        begin
                            Break;
                        end;
                        if Trim(sub_results_local[sub_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not long_exact_chunk_weight_is_eligible(
                            sub_results_local[sub_idx_local].dict_weight,
                            sub_results_local[sub_idx_local].has_dict_weight) then
                        begin
                            Continue;
                        end;
                        if is_runtime_chain_candidate(sub_results_local[sub_idx_local]) or
                            is_runtime_common_pattern_candidate(
                            sub_results_local[sub_idx_local]) or
                            is_runtime_redup_candidate(sub_results_local[sub_idx_local]) then
                        begin
                            Continue;
                        end;

                        sub_text_local := Trim(sub_results_local[sub_idx_local].text);
                        sub_units_local := get_candidate_text_unit_count(
                            sub_text_local);
                        if (sub_text_local = '') or
                            (sub_units_local <> local_sub_len) then
                        begin
                            Continue;
                        end;
                        if compact_nonbase_two_plus_single_exact_chunk_local(
                            sub_key_local, local_sub_len, sub_text_local) then
                        begin
                            Continue;
                        end;

                        sub_score_local := sub_results_local[sub_idx_local].score;
                        if sub_results_local[sub_idx_local].has_dict_weight and
                            (sub_results_local[sub_idx_local].dict_weight >
                            sub_score_local) then
                        begin
                            sub_score_local :=
                                sub_results_local[sub_idx_local].dict_weight;
                        end;
                        if sub_score_local < 0 then
                        begin
                            sub_score_local := 0;
                        end;
                        sub_score_local := sub_score_local * c_weight_scale_local +
                            local_sub_len * 2600 - sub_idx_local * 48;
                        Inc(sub_score_local,
                            get_temporal_process_context_bonus_local(
                            sub_text_local, local_sub_start_idx));
                        if local_sub_len >= 3 then
                        begin
                            Inc(sub_score_local, 900);
                        end;

                        if (not Result) or (sub_score_local > out_sub_score) then
                        begin
                            Result := True;
                            out_sub_text := sub_text_local;
                            out_sub_score := sub_score_local;
                        end;
                    end;
                end;

                procedure consider_span_result_local(const local_text: string;
                    const local_score: Integer);
                begin
                    if (local_text = '') or
                        (get_candidate_text_unit_count(local_text) <>
                        local_span_len) then
                    begin
                        Exit;
                    end;
                    if (not Result) or (local_score > out_span_score) then
                    begin
                        Result := True;
                        out_span_text := local_text;
                        out_span_score := local_score;
                    end;
                end;
            begin
                Result := False;
                out_span_text := '';
                out_span_score := 0;
                has_full_exact_span_local := False;
                if (local_span_len < 2) or (local_span_len > 4) or
                    (local_start_idx < 0) or
                    (local_start_idx + local_span_len > Length(syllables_local)) then
                begin
                    Exit;
                end;

                span_key_local := build_query_key_local(local_start_idx,
                    local_span_len);
                if span_key_local = '' then
                begin
                    Exit;
                end;

                SetLength(span_results_local, 0);
                if lookup_exact_full_pinyin_cached_local(span_key_local,
                    span_results_local) then
                begin
                    for span_idx_local := 0 to High(span_results_local) do
                    begin
                        if span_idx_local >= c_span_probe_limit_local then
                        begin
                            Break;
                        end;
                        if Trim(span_results_local[span_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        if not long_exact_chunk_weight_is_eligible(
                            span_results_local[span_idx_local].dict_weight,
                            span_results_local[span_idx_local].has_dict_weight) then
                        begin
                            Continue;
                        end;
                        if is_runtime_chain_candidate(span_results_local[span_idx_local]) or
                            is_runtime_common_pattern_candidate(
                            span_results_local[span_idx_local]) or
                            is_runtime_redup_candidate(span_results_local[span_idx_local]) then
                        begin
                            Continue;
                        end;

                        candidate_text_local := Trim(
                            span_results_local[span_idx_local].text);
                        candidate_units_local :=
                            get_candidate_text_unit_count(candidate_text_local);
                        if (candidate_text_local = '') or
                            (candidate_units_local <> local_span_len) then
                        begin
                            Continue;
                        end;
                        if compact_nonbase_two_plus_single_exact_chunk_local(
                            span_key_local, local_span_len,
                            candidate_text_local) then
                        begin
                            Continue;
                        end;

                        candidate_score_local := span_results_local[span_idx_local].score;
                        if span_results_local[span_idx_local].has_dict_weight and
                            (span_results_local[span_idx_local].dict_weight >
                            candidate_score_local) then
                        begin
                            candidate_score_local :=
                                span_results_local[span_idx_local].dict_weight;
                        end;
                        if candidate_score_local < 0 then
                        begin
                            candidate_score_local := 0;
                        end;
                        fixed_single_text_local := get_fixed_single_text_local(
                            syllables_local[local_start_idx].text);
                        if (local_span_len = 2) and
                            (fixed_single_text_local <> '') and
                            (candidate_score_local <
                            c_weak_fixed_head_phrase_weight) and
                            (not SameText(
                            get_compact_first_text_unit_local(candidate_text_local),
                            fixed_single_text_local)) then
                        begin
                            Continue;
                        end;
                        candidate_score_local := candidate_score_local *
                            c_weight_scale_local + local_span_len * 2600 -
                            span_idx_local * 48;
                        Inc(candidate_score_local,
                            get_directional_locative_span_bonus_local(
                            candidate_text_local));
                        Inc(candidate_score_local,
                            get_directional_locative_context_bonus_local(
                            candidate_text_local, local_start_idx, local_span_len));
                        Inc(candidate_score_local,
                            get_temporal_process_context_bonus_local(
                            candidate_text_local, local_start_idx));
                        if local_span_len >= 3 then
                        begin
                            Inc(candidate_score_local, 900);
                        end;
                        Inc(candidate_score_local,
                            get_compact_protected_span_adjustment_local(
                            local_start_idx, local_span_len, candidate_text_local));

                        consider_span_result_local(candidate_text_local,
                            candidate_score_local);
                        if Result then
                        begin
                            has_full_exact_span_local := True;
                        end;
                    end;
                end;

                if (local_span_len >= 3) and (not has_full_exact_span_local) then
                begin
                    fixed_single_text_local := get_fixed_single_text_local(
                        syllables_local[local_start_idx].text);
                    if (fixed_single_text_local <> '') and
                        (not should_skip_fixed_single_chain_step_local(
                        local_start_idx, fixed_single_text_local)) and
                        try_get_exact_subphrase_local(local_start_idx + 1,
                        local_span_len - 1, subphrase_text_local,
                        subphrase_score_local) then
                    begin
                        combined_text_local := fixed_single_text_local +
                            subphrase_text_local;
                        combined_score_local := subphrase_score_local +
                            52000 + local_span_len * 2200;
                        if (local_span_len <> 3) or
                            compact_allows_fixed_single_before_phrase_local(
                            fixed_single_text_local) or
                            m_dictionary.is_base_entry(span_key_local,
                            combined_text_local) or
                            m_dictionary.is_user_entry(span_key_local,
                            combined_text_local) then
                        begin
                            if is_bridge_fixed_single_text_local(
                                fixed_single_text_local) then
                            begin
                                Inc(combined_score_local,
                                    c_bridge_single_before_phrase_bonus);
                            end;
                            if is_pronoun_fixed_single_text_local(
                                fixed_single_text_local) then
                            begin
                                Inc(combined_score_local,
                                    c_pronoun_before_phrase_bonus);
                            end;
                            Inc(combined_score_local,
                                get_compact_protected_span_adjustment_local(
                                local_start_idx, local_span_len,
                                combined_text_local));
                            consider_span_result_local(combined_text_local,
                                combined_score_local);
                        end;
                    end;

                    fixed_single_text_local := get_fixed_single_text_local(
                        syllables_local[local_start_idx + local_span_len - 1].text);
                    if (fixed_single_text_local <> '') and
                        try_get_exact_subphrase_local(local_start_idx,
                        local_span_len - 1, subphrase_text_local,
                        subphrase_score_local) then
                    begin
                        combined_text_local := subphrase_text_local +
                            fixed_single_text_local;
                        combined_score_local := subphrase_score_local +
                            36000 + local_span_len * 1800;
                        if not compact_nonbase_two_plus_single_exact_chunk_local(
                            span_key_local, local_span_len,
                            combined_text_local) then
                        begin
                            if is_bridge_fixed_single_text_local(
                                fixed_single_text_local) then
                            begin
                                Inc(combined_score_local,
                                    c_bridge_single_before_phrase_bonus div 2);
                            end;
                            if is_pronoun_fixed_single_text_local(
                                fixed_single_text_local) then
                            begin
                                Inc(combined_score_local,
                                    c_pronoun_before_phrase_bonus div 2);
                            end;
                            Inc(combined_score_local,
                                get_compact_protected_span_adjustment_local(
                                local_start_idx, local_span_len,
                                combined_text_local));
                            consider_span_result_local(combined_text_local,
                                combined_score_local);
                        end;
                    end;
                end;
            end;

            function try_get_fixed_single_span_local(
                const local_start_idx: Integer;
                out out_span_text: string; out out_span_score: Integer): Boolean;
            begin
                Result := False;
                out_span_text := '';
                out_span_score := 0;
                if (local_start_idx < 0) or
                    (local_start_idx >= Length(syllables_local)) then
                begin
                    Exit;
                end;

                out_span_text := get_fixed_single_text_local(
                    syllables_local[local_start_idx].text);
                if out_span_text = '' then
                begin
                    Exit;
                end;
                if should_skip_fixed_single_chain_step_local(local_start_idx,
                    out_span_text) then
                begin
                    Exit;
                end;

                out_span_score := 3600;
                if is_bridge_fixed_single_text_local(out_span_text) then
                begin
                    Inc(out_span_score, c_bridge_single_before_phrase_bonus);
                end;
                if is_pronoun_fixed_single_text_local(out_span_text) then
                begin
                    Inc(out_span_score, c_pronoun_before_phrase_bonus);
                end;
                Result := True;
            end;
        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := 0;
            out_phrase_count := 0;

            if (input_syllable_count < 5) or (input_syllable_count > 8) then
            begin
                Exit;
            end;

            compact_protected_span_text_cache_local :=
                TDictionary<string, string>.Create;
            compact_protected_span_weight_cache_local :=
                TDictionary<string, Integer>.Create;
            compact_protected_span_rank_cache_local :=
                TDictionary<string, Integer>.Create;
            try
                SetLength(best_scores_local, Length(syllables_local) + 1);
                SetLength(best_texts_local, Length(syllables_local) + 1);
                SetLength(best_paths_local, Length(syllables_local) + 1);
                SetLength(best_phrase_counts_local, Length(syllables_local) + 1);
                SetLength(best_single_counts_local, Length(syllables_local) + 1);
                for pos_local := 0 to High(best_scores_local) do
                begin
                    best_scores_local[pos_local] := c_low_score_local;
                    best_texts_local[pos_local] := '';
                    best_paths_local[pos_local] := '';
                    best_phrase_counts_local[pos_local] := 0;
                    best_single_counts_local[pos_local] := 0;
                end;
                best_scores_local[Length(syllables_local)] := 0;

                for pos_local := Length(syllables_local) - 1 downto 0 do
                begin
                    for span_len_local := Min(4, Length(syllables_local) -
                        pos_local) downto 1 do
                    begin
                        next_pos_local := pos_local + span_len_local;
                        if best_scores_local[next_pos_local] <=
                            c_low_score_local div 2 then
                        begin
                            Continue;
                        end;
                        if span_len_local >= 2 then
                        begin
                            if not try_get_best_exact_phrase_span_local(pos_local,
                                span_len_local, span_text_local,
                                span_score_local) then
                            begin
                                Continue;
                            end;
                        end
                        else if not try_get_fixed_single_span_local(pos_local,
                            span_text_local, span_score_local) then
                        begin
                            Continue;
                        end;

                        combined_score_local := span_score_local +
                            best_scores_local[next_pos_local] -
                            c_segment_penalty_local;
                        if best_texts_local[next_pos_local] <> '' then
                        begin
                            suffix_adjustment_local :=
                                get_compact_suffix_context_adjustment_local(
                                syllables_local[next_pos_local - 1].text,
                                span_text_local, best_texts_local[next_pos_local]);
                            Inc(combined_score_local, suffix_adjustment_local);
                        end;
                        if combined_score_local <= best_scores_local[pos_local] then
                        begin
                            Continue;
                        end;

                        best_scores_local[pos_local] := combined_score_local;
                        best_texts_local[pos_local] := span_text_local +
                            best_texts_local[next_pos_local];
                        combined_path_local := span_text_local;
                        if best_paths_local[next_pos_local] <> '' then
                        begin
                            combined_path_local := combined_path_local +
                                c_segment_path_separator +
                                best_paths_local[next_pos_local];
                        end;
                        best_paths_local[pos_local] := combined_path_local;
                        best_phrase_counts_local[pos_local] :=
                            best_phrase_counts_local[next_pos_local];
                        best_single_counts_local[pos_local] :=
                            best_single_counts_local[next_pos_local];
                        if span_len_local >= 2 then
                        begin
                            Inc(best_phrase_counts_local[pos_local]);
                        end
                        else
                        begin
                            Inc(best_single_counts_local[pos_local]);
                        end;
                    end;
                end;

                if (best_scores_local[0] <= c_low_score_local div 2) or
                    (best_texts_local[0] = '') or
                    (get_candidate_text_unit_count(best_texts_local[0]) <>
                    input_syllable_count) or
                    (best_phrase_counts_local[0] < c_min_phrase_count_local) or
                    (best_single_counts_local[0] > best_phrase_counts_local[0] + 1) then
                begin
                    Exit;
                end;

                out_text := best_texts_local[0];
                out_path := best_paths_local[0];
                out_score := best_scores_local[0] + 18000;
                out_phrase_count := best_phrase_counts_local[0];
                Result := True;
            finally
                compact_protected_span_rank_cache_local.Free;
                compact_protected_span_weight_cache_local.Free;
                compact_protected_span_text_cache_local.Free;
            end;
        end;
    begin
        Result := False;
        out_encoded_path := '';
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := 0;

        if (m_dictionary = nil) or (lookup_text = '') or
            (input_syllable_count < c_min_syllables) or
            (not is_full_pinyin_key(lookup_text)) or
            all_initial_compact_query or has_internal_dangling_initial or
            has_explicit_apostrophe_input then
        begin
            Exit;
        end;
        if has_strong_full_query_exact_complete_candidate_local then
        begin
            // Exact span chains are fallback composition. Do not build a
            // competing full-length chain when the full query already has a
            // strong exact dictionary/user hit.
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        if has_precise_extendable_tail_syllable_local(lookup_text) and
            (get_fixed_single_text_local(
            syllables_local[High(syllables_local)].text) = '') and
            (not has_complete_exact_tail_phrase_local(4)) and
            (not has_complete_exact_tail_phrase_local(3)) and
            (not has_resolved_final_tail_three_syllable_span_local) and
            (not (((input_syllable_count >= 6) or
            ((Length(syllables_local) >= 3) and
            (get_fixed_single_text_local(
            syllables_local[High(syllables_local) - 2].text) <> ''))) and
            has_complete_exact_tail_phrase_local(2))) then
        begin
            Exit;
        end;

        if try_build_compact_exact_phrase_chain_fast_local(text_local,
            encoded_path_local, chain_score_local, phrase_count_local) then
        begin
            out_candidate.score := c_base_score + chain_score_local;
            out_candidate.text := text_local;
            out_candidate.comment := '';
            out_candidate.dict_weight := out_candidate.score;
            out_encoded_path := encoded_path_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' dchain=[%s/%s phr=%d score=%d path=%s]',
                    [out_candidate.text, out_candidate.comment, phrase_count_local,
                    out_candidate.score, out_encoded_path]);
            end;
            Result := True;
            Exit;
        end;

        if try_build_generic_exact_chain_fast_local(text_local,
            encoded_path_local, chain_score_local, phrase_count_local) then
        begin
            out_candidate.score := c_base_score + chain_score_local;
            out_candidate.text := text_local;
            out_candidate.comment := '';
            out_candidate.dict_weight := out_candidate.score;
            out_encoded_path := encoded_path_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' dchain=[%s/%s phr=%d score=%d path=%s]',
                    [out_candidate.text, out_candidate.comment, phrase_count_local,
                    out_candidate.score, out_encoded_path]);
            end;
            Result := True;
            Exit;
        end;

        SetLength(memo_known_local, Length(syllables_local) + 1);
        SetLength(memo_success_local, Length(syllables_local) + 1);
        SetLength(memo_text_local, Length(syllables_local) + 1);
        SetLength(memo_path_local, Length(syllables_local) + 1);
        SetLength(memo_score_local, Length(syllables_local) + 1);
        SetLength(memo_phrase_count_local, Length(syllables_local) + 1);

        if not try_build_from_pos_local(0, text_local, encoded_path_local,
            chain_score_local, phrase_count_local) then
        begin
            Exit;
        end;
        out_candidate.score := c_base_score + chain_score_local;

        if get_candidate_text_unit_count(text_local) <> input_syllable_count then
        begin
            Exit;
        end;

        if (phrase_count_local < 2) and
            (not ((input_syllable_count = 3) and (phrase_count_local >= 1) and
            (Pos(c_segment_path_separator, encoded_path_local) > 0) and
            (is_pronoun_fixed_single_text_local(
            get_first_path_segment_local(encoded_path_local)) or
            is_stable_fixed_single_text_local(
            get_last_path_segment_local(encoded_path_local))))) and
            (not ((phrase_count_local >= 1) and
            stable_fixed_single_chain_with_phrase_local(encoded_path_local))) then
        begin
            Exit;
        end;

        out_candidate.text := text_local;
        out_candidate.comment := '';
        out_candidate.dict_weight := out_candidate.score;
        out_encoded_path := encoded_path_local;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' dchain=[%s/%s phr=%d score=%d path=%s]',
                [out_candidate.text, out_candidate.comment, phrase_count_local,
                out_candidate.score, out_encoded_path]);
        end;
        Result := True;
    end;

    function try_build_stable_leading_prefix_partial_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    const
        c_probe_window = 4;
        c_probe_limit = 16;
        c_prefix_bonus = 3600;
    var
        syllables_local: TncPinyinParseResult;
        prefix_count_local: Integer;
        idx_local: Integer;
        result_idx_local: Integer;
        prefix_key_local: string;
        tail_comment_local: string;
        prefix_results_local: TncCandidateList;
        candidate_text_local: string;
        candidate_units_local: Integer;
        candidate_score_local: Integer;
        best_prefix_idx_local: Integer;
        best_prefix_score_local: Integer;
        ranked_path_local: string;
        path_parts_local: TArray<string>;
        path_text_local: string;
        path_units_local: Integer;
        path_prefix_key_local: string;
        path_tail_comment_local: string;
        best_candidate_local: TncCandidate;
        best_candidate_score_local: Integer;
        best_candidate_units_local: Integer;

        function build_syllable_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to local_start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_stable_single_text_local(const syllable_text: string): string;
        var
            normalized_syllable_local: string;
            fixed_text_local: string;
        begin
            Result := '';
            normalized_syllable_local := normalize_pinyin_text(syllable_text);
            if normalized_syllable_local = '' then
            begin
                Exit;
            end;

            fixed_text_local := get_fixed_resolved_boundary_char_local(
                normalized_syllable_local);
            if fixed_text_local <> '' then
            begin
                Exit(fixed_text_local);
            end;

            Exit;
        end;

        function has_strong_phrase_for_span_local(const local_start_idx: Integer;
            const local_len: Integer): Boolean;
        var
            local_key: string;
            local_text: string;
            local_bonus: Integer;
            local_results: TncCandidateList;
            local_idx: Integer;
            local_candidate_text: string;
        begin
            Result := False;
            if local_len < 2 then
            begin
                Exit;
            end;

            local_key := build_syllable_key_local(local_start_idx, local_len);
            if local_key = '' then
            begin
                Exit;
            end;

            if try_get_preferred_query_phrase_text_local(local_key, local_text,
                local_bonus) and
                (get_candidate_text_unit_count(Trim(local_text)) = local_len) then
            begin
                Exit(True);
            end;

            // Broad lookup only adds prefix/prediction candidates here. For
            // longer path spans an exact miss cannot produce a usable phrase.
            if dictionary_exact_lookup_cached(local_key, local_results) or
                ((local_len <= 2) and
                dictionary_lookup_cached(local_key, local_results)) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    local_candidate_text := Trim(local_results[local_idx].text);
                    if get_candidate_text_unit_count(local_candidate_text) =
                        local_len then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function candidate_breaks_preferred_anchor_local(
            const local_text: string; const local_start_idx: Integer;
            const local_len: Integer): Boolean;
        var
            candidate_units_local: TArray<string>;
            anchor_len_local: Integer;
            span_start_local: Integer;
            anchor_key_local: string;
            anchor_text_local: string;
            anchor_bonus_local: Integer;
            anchor_units_local: TArray<string>;
            unit_idx_local: Integer;
            mismatch_local: Boolean;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
            exact_score_local: Integer;
            best_exact_score_local: Integer;
            second_exact_score_local: Integer;
        begin
            Result := False;
            candidate_units_local := split_text_units(Trim(local_text));
            if Length(candidate_units_local) <> local_len then
            begin
                Exit;
            end;

            for anchor_len_local := Min(4, local_len) downto 2 do
            begin
                for span_start_local := 0 to local_len - anchor_len_local do
                begin
                    anchor_key_local := build_syllable_key_local(
                        local_start_idx + span_start_local, anchor_len_local);
                    if anchor_key_local = '' then
                    begin
                        Continue;
                    end;

                    anchor_text_local := '';
                    anchor_bonus_local := 0;
                    if not try_get_preferred_query_phrase_text_local(
                        anchor_key_local, anchor_text_local,
                        anchor_bonus_local) then
                    begin
                        best_exact_score_local := Low(Integer);
                        second_exact_score_local := Low(Integer);
                        if dictionary_exact_lookup_cached(anchor_key_local,
                            exact_results_local) then
                        begin
                            for exact_idx_local := 0 to
                                High(exact_results_local) do
                            begin
                                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                                begin
                                    Continue;
                                end;
                                exact_text_local := Trim(
                                    exact_results_local[exact_idx_local].text);
                                if (exact_text_local = '') or
                                    (get_candidate_text_unit_count(
                                    exact_text_local) <> anchor_len_local) then
                                begin
                                    Continue;
                                end;

                                exact_score_local :=
                                    exact_results_local[exact_idx_local].score;
                                if exact_results_local[exact_idx_local].has_dict_weight and
                                    (exact_results_local[exact_idx_local].dict_weight >
                                    exact_score_local) then
                                begin
                                    exact_score_local :=
                                        exact_results_local[exact_idx_local].dict_weight;
                                end;

                                if exact_score_local > best_exact_score_local then
                                begin
                                    second_exact_score_local :=
                                        best_exact_score_local;
                                    best_exact_score_local := exact_score_local;
                                    anchor_text_local := exact_text_local;
                                end
                                else if exact_score_local >
                                    second_exact_score_local then
                                begin
                                    second_exact_score_local :=
                                        exact_score_local;
                                end;
                            end;
                        end;

                        if (anchor_text_local <> '') and
                            ((anchor_len_local >= 3) or
                            (best_exact_score_local >= 4096)) then
                        begin
                            if not (
                                (second_exact_score_local = Low(Integer)) or
                                (best_exact_score_local >=
                                second_exact_score_local + 6000) or
                                ((best_exact_score_local * 100) >=
                                (second_exact_score_local * 165))) then
                            begin
                                anchor_text_local := '';
                            end;
                        end
                        else
                        begin
                            anchor_text_local := '';
                        end;
                    end;

                    if anchor_text_local = '' then
                    begin
                        Continue;
                    end;

                    anchor_units_local := split_text_units(Trim(anchor_text_local));
                    if Length(anchor_units_local) <> anchor_len_local then
                    begin
                        Continue;
                    end;

                    mismatch_local := False;
                    for unit_idx_local := 0 to anchor_len_local - 1 do
                    begin
                        if not SameText(candidate_units_local[span_start_local +
                            unit_idx_local], anchor_units_local[unit_idx_local]) then
                        begin
                            mismatch_local := True;
                            Break;
                        end;
                    end;
                    if mismatch_local then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function candidate_breaks_stable_boundary_local(
            const local_text: string; const local_start_idx: Integer;
            const local_len: Integer): Boolean;
        var
            candidate_units_local: TArray<string>;
            split_idx_local: Integer;
            stable_text_local: string;
        begin
            Result := False;
            if local_len < 3 then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(local_text));
            if Length(candidate_units_local) <> local_len then
            begin
                Exit;
            end;

            for split_idx_local := 1 to local_len - 1 do
            begin
                stable_text_local := get_stable_single_text_local(
                    syllables_local[local_start_idx + split_idx_local].text);
                if stable_text_local = '' then
                begin
                    Continue;
                end;
                if not has_strong_phrase_for_span_local(local_start_idx,
                    split_idx_local) then
                begin
                    Continue;
                end;
                if not SameText(candidate_units_local[split_idx_local],
                    stable_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function try_build_single_text_local(const local_pos: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (m_dictionary = nil) or
                (not m_dictionary.lookup(normalize_pinyin_text(
                syllables_local[local_pos].text), local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                local_text := Trim(local_results[local_idx].text);
                if get_candidate_text_unit_count(local_text) <> 1 then
                begin
                    Continue;
                end;

                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end;
                if (not Result) or (local_score > out_score) then
                begin
                    Result := True;
                    out_text := local_text;
                    out_score := local_score;
                end;
            end;
        end;

        function try_build_preferred_prefix_text_local(
            const local_prefix_count: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        const
            c_max_span = 4;
            c_span_probe_limit = 12;
        var
            local_pos: Integer;
            local_span_len: Integer;
            local_query_key: string;
            local_text: string;
            local_bonus: Integer;
            local_units: TArray<string>;
            local_single_score: Integer;
            phrase_count_local: Integer;
            matched_local: Boolean;

            function try_get_dictionary_span_text_local(
                const local_query_key: string; const local_unit_count: Integer;
                out out_span_text: string; out out_span_score: Integer): Boolean;
            var
                local_results: TncCandidateList;
                local_idx: Integer;
                current_text: string;
                current_score: Integer;
            begin
                Result := False;
                out_span_text := '';
                out_span_score := 0;
                // Keep the broad fallback for one/two-syllable spans only;
                // longer full-pinyin spans must be backed by an exact entry.
                if (local_query_key = '') or
                    ((not dictionary_exact_lookup_cached(local_query_key,
                    local_results)) and
                    ((local_unit_count >= 3) or
                    (not dictionary_lookup_cached(local_query_key,
                    local_results)))) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= c_span_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;

                    current_text := Trim(local_results[local_idx].text);
                    if (current_text = '') or
                        (get_candidate_text_unit_count(current_text) <>
                        local_unit_count) then
                    begin
                        Continue;
                    end;
                    if candidate_breaks_preferred_anchor_local(current_text,
                        local_pos, local_unit_count) or
                        candidate_breaks_stable_boundary_local(current_text,
                        local_pos, local_unit_count) then
                    begin
                        Continue;
                    end;

                    current_score := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight > current_score) then
                    begin
                        current_score := local_results[local_idx].dict_weight;
                    end;

                    if (not Result) or (current_score > out_span_score) then
                    begin
                        Result := True;
                        out_span_text := current_text;
                        out_span_score := current_score;
                    end;
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            local_pos := 0;
            phrase_count_local := 0;
            while local_pos < local_prefix_count do
            begin
                matched_local := False;
                local_span_len := Min(c_max_span,
                    local_prefix_count - local_pos);
                while local_span_len >= 2 do
                begin
                    local_query_key := build_syllable_key_local(local_pos,
                        local_span_len);
                    if try_get_preferred_query_phrase_text_local(
                        local_query_key, local_text, local_bonus) then
                    begin
                        local_units := split_text_units(local_text);
                        if Length(local_units) = local_span_len then
                        begin
                            out_text := out_text + local_text;
                            Inc(out_score, local_bonus +
                                local_span_len * 260);
                            Inc(local_pos, local_span_len);
                            Inc(phrase_count_local);
                            matched_local := True;
                            Break;
                        end;
                    end;
                    if (local_span_len <= 3) and
                        try_get_dictionary_span_text_local(local_query_key,
                        local_span_len, local_text, local_bonus) then
                    begin
                        out_text := out_text + local_text;
                        Inc(out_score, 1200 + local_bonus +
                            local_span_len * 220);
                        Inc(local_pos, local_span_len);
                        Inc(phrase_count_local);
                        matched_local := True;
                        Break;
                    end;
                    Dec(local_span_len);
                end;

                if matched_local then
                begin
                    Continue;
                end;

                if (phrase_count_local <= 0) or
                    (not try_build_single_text_local(local_pos, local_text,
                    local_single_score)) then
                begin
                    Exit;
                end;
                out_text := out_text + local_text;
                Inc(out_score, 500 + local_single_score);
                Inc(local_pos);
            end;

            Result := (phrase_count_local > 0) and
                (get_candidate_text_unit_count(out_text) = local_prefix_count);
        end;

        function try_build_lightweight_prefix_text_local(
            const local_prefix_key: string; const local_prefix_count: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (local_prefix_key = '') or
                (not build_lightweight_sentence_candidates(local_prefix_key,
                local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if (local_text = '') or
                    (get_candidate_text_unit_count(local_text) <>
                    local_prefix_count) then
                begin
                    Continue;
                end;
                if candidate_breaks_preferred_anchor_local(local_text, 0,
                    local_prefix_count) or
                    candidate_breaks_stable_boundary_local(local_text, 0,
                    local_prefix_count) then
                begin
                    Continue;
                end;

                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end;

                if (not Result) or (local_score > out_score) then
                begin
                    Result := True;
                    out_text := local_text;
                    out_score := local_score;
                end;
            end;
        end;

        procedure normalize_candidate_to_prefix_partial_local(
            var io_candidate: TncCandidate; const local_prefix_count: Integer;
            const local_tail_comment: string);
        var
            local_units: TArray<string>;
            local_idx: Integer;
            local_text: string;
        begin
            local_units := split_text_units(Trim(io_candidate.text));
            if (local_prefix_count <= 0) or
                (Length(local_units) < local_prefix_count) then
            begin
                Exit;
            end;

            local_text := '';
            for local_idx := 0 to local_prefix_count - 1 do
            begin
                local_text := local_text + local_units[local_idx];
            end;
            io_candidate.text := local_text;
            io_candidate.comment := local_tail_comment;
        end;

        function try_build_from_last_ranked_path_local(
            out local_candidate: TncCandidate): Boolean;
        var
            local_path_part_count: Integer;
            local_path_part_idx: Integer;
        begin
            Result := False;
            ranked_path_local := Trim(m_last_ranked_top_path);
            if get_encoded_path_segment_count_local(ranked_path_local) <= 1 then
            begin
                Exit;
            end;

            path_parts_local := ranked_path_local.Split(
                [c_segment_path_separator]);
            for local_path_part_count := Length(path_parts_local) downto 1 do
            begin
                path_text_local := '';
                for local_path_part_idx := 0 to local_path_part_count - 1 do
                begin
                    path_text_local := path_text_local +
                        Trim(path_parts_local[local_path_part_idx]);
                end;

                path_units_local := get_candidate_text_unit_count(
                    path_text_local);
                if (path_text_local = '') or (path_units_local <= 0) or
                    (path_units_local >= input_syllable_count) then
                begin
                    Continue;
                end;
                if (input_syllable_count >= 10) and
                    (path_units_local < input_syllable_count - 4) then
                begin
                    Continue;
                end;

                path_prefix_key_local := build_syllable_key_local(0,
                    path_units_local);
                if (path_prefix_key_local = '') or
                    (Copy(normalize_pinyin_text(lookup_text), 1,
                    Length(path_prefix_key_local)) <> path_prefix_key_local) then
                begin
                    Continue;
                end;

                path_tail_comment_local := build_syllable_key_local(
                    path_units_local,
                    input_syllable_count - path_units_local);
                if path_tail_comment_local = '' then
                begin
                    Continue;
                end;

                FillChar(local_candidate, SizeOf(local_candidate), 0);
                local_candidate.text := path_text_local;
                local_candidate.comment := path_tail_comment_local;
                local_candidate.score := 68000 + path_units_local * 256;
                local_candidate.source := cs_rule;
                local_candidate.has_dict_weight := True;
                local_candidate.dict_weight := local_candidate.score;
                Exit(True);
            end;
        end;

        procedure consider_prefix_candidate_local(
            const local_candidate: TncCandidate; const local_method_bonus: Integer);
        var
            local_adjusted_score: Integer;
            local_units: Integer;
            local_text: string;
            local_prefix_key: string;
            local_support_score: Integer;
            local_support_segments: Integer;
            local_support_path: string;
        begin
            local_text := Trim(local_candidate.text);
            if (local_text = '') or (Trim(local_candidate.comment) = '') then
            begin
                Exit;
            end;

            local_units := get_candidate_text_unit_count(local_text);
            if (local_units < 2) or (local_units >= input_syllable_count) then
            begin
                Exit;
            end;

            local_adjusted_score := local_candidate.score + local_method_bonus;
            local_prefix_key := build_syllable_key_local(0, local_units);
            if local_prefix_key <> '' then
            begin
                local_support_path := infer_segment_path_for_query_text_with_score(
                    local_prefix_key, local_text, local_support_score,
                    local_support_segments);
                if local_support_path <> '' then
                begin
                    Inc(local_adjusted_score, Min(2400, Max(0,
                        local_support_score div 8)));
                    if local_support_segments > 1 then
                    begin
                        Inc(local_adjusted_score,
                            900 + (local_support_segments * 120));
                    end;
                end;
            end;
            if (best_candidate_score_local <> Low(Integer)) and
                (local_adjusted_score < best_candidate_score_local - 1024) then
            begin
                Exit;
            end;

            if (best_candidate_score_local = Low(Integer)) or
                (local_adjusted_score > best_candidate_score_local) or
                ((local_adjusted_score = best_candidate_score_local) and
                (local_units > best_candidate_units_local)) then
            begin
                best_candidate_local := local_candidate;
                best_candidate_score_local := local_adjusted_score;
                best_candidate_units_local := local_units;
            end;
        end;
    begin
        out_candidate.text := '';
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        Result := False;
        FillChar(best_candidate_local, SizeOf(best_candidate_local), 0);
        best_candidate_score_local := Low(Integer);
        best_candidate_units_local := 0;
        if (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 3) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        if try_build_from_last_ranked_path_local(out_candidate) and
            ((input_syllable_count < 10) or
            (Length(split_text_units(Trim(out_candidate.text))) >=
            input_syllable_count - 4)) then
        begin
            candidate_units_local := get_candidate_text_unit_count(
                Trim(out_candidate.text));
            if (candidate_units_local >= 2) and
                (not candidate_breaks_preferred_anchor_local(
                out_candidate.text, 0, candidate_units_local)) and
                (not candidate_breaks_stable_boundary_local(
                out_candidate.text, 0, candidate_units_local)) then
            begin
                consider_prefix_candidate_local(out_candidate, 2400);
            end;
        end;
        for prefix_count_local := input_syllable_count - 1 downto
            Max(2, input_syllable_count - c_probe_window) do
        begin
            prefix_key_local := '';
            for idx_local := 0 to prefix_count_local - 1 do
            begin
                prefix_key_local := prefix_key_local +
                    normalize_pinyin_text(syllables_local[idx_local].text);
            end;

            tail_comment_local := '';
            for idx_local := prefix_count_local to High(syllables_local) do
            begin
                tail_comment_local := tail_comment_local +
                    normalize_pinyin_text(syllables_local[idx_local].text);
            end;

            if (prefix_key_local = '') or (tail_comment_local = '') or
                (tail_comment_local = '') then
            begin
                Continue;
            end;

            if try_build_preferred_prefix_text_local(prefix_count_local,
                candidate_text_local, candidate_score_local) then
            begin
                out_candidate.text := candidate_text_local;
                out_candidate.comment := tail_comment_local;
                out_candidate.score := candidate_score_local + c_prefix_bonus +
                    (prefix_count_local * 220) -
                    ((input_syllable_count - prefix_count_local) * 30);
                out_candidate.source := cs_rule;
                out_candidate.dict_weight := out_candidate.score;
                out_candidate.has_dict_weight := True;
                normalize_candidate_to_prefix_partial_local(out_candidate,
                    prefix_count_local, tail_comment_local);
                consider_prefix_candidate_local(out_candidate, 2200);
            end;

            if (input_syllable_count <= 8) and
                (prefix_count_local <= 5) and
                (not delayed_long_decode_mode) and
                (prefix_count_local >= 3) and
                try_build_lightweight_prefix_text_local(prefix_key_local,
                prefix_count_local, candidate_text_local,
                candidate_score_local) then
            begin
                out_candidate.text := candidate_text_local;
                out_candidate.comment := tail_comment_local;
                out_candidate.score := candidate_score_local + c_prefix_bonus +
                    1200 + (prefix_count_local * 220) -
                    ((input_syllable_count - prefix_count_local) * 30);
                out_candidate.source := cs_rule;
                out_candidate.dict_weight := out_candidate.score;
                out_candidate.has_dict_weight := True;
                normalize_candidate_to_prefix_partial_local(out_candidate,
                    prefix_count_local, tail_comment_local);
                consider_prefix_candidate_local(out_candidate, 1600);
            end;

            // Long prefix phrases are exact-only. Running the generic lookup
            // after an exact miss repeats expensive prediction work.
            if ((prefix_count_local >= 3) and
                (not dictionary_exact_lookup_cached(prefix_key_local,
                prefix_results_local))) or
                ((prefix_count_local <= 2) and
                (not dictionary_lookup_cached(prefix_key_local,
                prefix_results_local))) then
            begin
                Continue;
            end;

            best_prefix_idx_local := -1;
            best_prefix_score_local := Low(Integer);
            for result_idx_local := 0 to High(prefix_results_local) do
            begin
                if result_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(prefix_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(prefix_results_local[result_idx_local].text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if (candidate_text_local = '') or
                    (candidate_units_local <> prefix_count_local) then
                begin
                    Continue;
                end;
                if candidate_breaks_preferred_anchor_local(
                    candidate_text_local, 0, prefix_count_local) or
                    candidate_breaks_stable_boundary_local(
                    candidate_text_local, 0, prefix_count_local) then
                begin
                    Continue;
                end;

                candidate_score_local := prefix_results_local[result_idx_local].score;
                if prefix_results_local[result_idx_local].has_dict_weight and
                    (prefix_results_local[result_idx_local].dict_weight >
                    candidate_score_local) then
                begin
                    candidate_score_local :=
                        prefix_results_local[result_idx_local].dict_weight;
                end;

                if candidate_score_local > best_prefix_score_local then
                begin
                    best_prefix_score_local := candidate_score_local;
                    best_prefix_idx_local := result_idx_local;
                end;
            end;

            if best_prefix_idx_local >= 0 then
            begin
                out_candidate := prefix_results_local[best_prefix_idx_local];
                out_candidate.comment := tail_comment_local;
                out_candidate.score := best_prefix_score_local + c_prefix_bonus +
                    (prefix_count_local * 160) -
                    ((input_syllable_count - prefix_count_local) * 30);
                out_candidate.dict_weight := out_candidate.score;
                out_candidate.has_dict_weight := True;
                normalize_candidate_to_prefix_partial_local(out_candidate,
                    prefix_count_local, tail_comment_local);
                consider_prefix_candidate_local(out_candidate, 0);
            end;
        end;

        if best_candidate_score_local = Low(Integer) then
        begin
            Exit(False);
        end;

        out_candidate := best_candidate_local;
        Result := True;
    end;

    function try_build_internal_dangling_prefix_partial_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    var
        parser_local: TncPinyinParser;
        raw_parts_local: TncPinyinParseResult;
        part_idx_local: Integer;
        dangling_idx_local: Integer;
        prefix_key_local: string;
        tail_comment_local: string;
        prefix_candidates_local: TncCandidateList;
        candidate_text_local: string;
        candidate_units_local: Integer;
        best_idx_local: Integer;
        best_score_local: Integer;
        rank_score_local: Integer;
        dp_scores_local: TArray<Integer>;
        dp_texts_local: TArray<string>;
        dp_segments_local: TArray<Integer>;

        function pick_best_prefix_candidate_local: Boolean;
        var
            candidate_idx_local: Integer;
        begin
            best_idx_local := -1;
            best_score_local := Low(Integer);
            for candidate_idx_local := 0 to High(prefix_candidates_local) do
            begin
                if Trim(prefix_candidates_local[candidate_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(prefix_candidates_local[candidate_idx_local].text);
                candidate_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                if (candidate_text_local = '') or
                    (candidate_units_local <> dangling_idx_local) then
                begin
                    Continue;
                end;

                rank_score_local := prefix_candidates_local[candidate_idx_local].score;
                if prefix_candidates_local[candidate_idx_local].has_dict_weight and
                    (prefix_candidates_local[candidate_idx_local].dict_weight >
                    rank_score_local) then
                begin
                    rank_score_local :=
                        prefix_candidates_local[candidate_idx_local].dict_weight;
                end;
                if (best_idx_local < 0) or (rank_score_local > best_score_local) then
                begin
                    best_idx_local := candidate_idx_local;
                    best_score_local := rank_score_local;
                end;
            end;

            Result := best_idx_local >= 0;
        end;

        function build_part_span_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_len <= 0) or
                (local_start_idx + local_len > dangling_idx_local) then
            begin
                Exit;
            end;
            for local_idx := local_start_idx to local_start_idx + local_len - 1 do
            begin
                Result := Result +
                    normalize_pinyin_text(raw_parts_local[local_idx].text);
            end;
        end;

        function try_pick_span_candidate_local(const local_start_idx: Integer;
            const local_len: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        const
            c_lookup_top_span_bonus = 3200;
        var
            local_key: string;
            local_results: TncCandidateList;
            local_lookup_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_lookup_top_text: string;
            local_units: Integer;
            local_score: Integer;
            local_best_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            local_key := build_part_span_key_local(local_start_idx, local_len);
            if local_key = '' then
            begin
                Exit;
            end;

            SetLength(local_results, 0);
            local_lookup_top_text := '';
            if dictionary_exact_lookup_cached(local_key,
                local_lookup_results) then
            begin
                local_results := merge_candidate_lists(local_results,
                    local_lookup_results, 0);
            end;
            // A broad lookup remains useful for short spans, but not for a
            // three-or-more-syllable exact miss in a dictionary-only path.
            if (Length(local_results) = 0) and (local_len >= 3) then
            begin
                Exit;
            end;
            if dictionary_lookup_cached(local_key, local_lookup_results) then
            begin
                if Length(local_lookup_results) > 0 then
                begin
                    local_lookup_top_text := Trim(local_lookup_results[0].text);
                end;
                local_results := merge_candidate_lists(local_results,
                    local_lookup_results, 0);
            end;
            if Length(local_results) = 0 then
            begin
                Exit;
            end;

            local_best_score := Low(Integer);
            for local_idx := 0 to High(local_results) do
            begin
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;
                local_text := Trim(local_results[local_idx].text);
                local_units := get_candidate_text_unit_count(local_text);
                if (local_text = '') or (local_units <> local_len) then
                begin
                    Continue;
                end;

                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end;
                if local_results[local_idx].source = cs_user then
                begin
                    Inc(local_score, c_user_score_bonus);
                end;
                if (local_lookup_top_text <> '') and
                    SameText(local_text, local_lookup_top_text) then
                begin
                    Inc(local_score, c_lookup_top_span_bonus);
                end;
                if (not Result) or (local_score > local_best_score) then
                begin
                    Result := True;
                    local_best_score := local_score;
                    out_text := local_text;
                    out_score := local_score;
                end;
            end;
        end;

        function try_build_natural_prefix_candidate_local(
            out local_candidate: TncCandidate): Boolean;
        const
            c_max_span_syllables = 4;
        var
            local_idx: Integer;
            local_len: Integer;
            local_text: string;
            local_score: Integer;
            local_total_score: Integer;
            local_next_idx: Integer;
            local_segment_bonus: Integer;
            local_has_multi_span: Boolean;

            procedure consider_span_local(const span_len: Integer;
                const span_text: string; const span_score: Integer);
            begin
                if span_len >= 2 then
                begin
                    local_segment_bonus := 760 + (span_len * 90);
                end
                else
                begin
                    local_segment_bonus := -80;
                end;

                local_next_idx := local_idx + span_len;
                local_total_score := dp_scores_local[local_idx] +
                    span_score + local_segment_bonus;
                if (local_total_score > dp_scores_local[local_next_idx]) or
                    ((local_total_score = dp_scores_local[local_next_idx]) and
                    (dp_segments_local[local_idx] + 1 <
                    dp_segments_local[local_next_idx])) then
                begin
                    dp_scores_local[local_next_idx] := local_total_score;
                    dp_texts_local[local_next_idx] :=
                        dp_texts_local[local_idx] + span_text;
                    dp_segments_local[local_next_idx] :=
                        dp_segments_local[local_idx] + 1;
                end;
            end;
        begin
            FillChar(local_candidate, SizeOf(local_candidate), 0);
            Result := False;
            if dangling_idx_local < 2 then
            begin
                Exit;
            end;

            SetLength(dp_scores_local, dangling_idx_local + 1);
            SetLength(dp_texts_local, dangling_idx_local + 1);
            SetLength(dp_segments_local, dangling_idx_local + 1);
            for local_idx := 0 to dangling_idx_local do
            begin
                dp_scores_local[local_idx] := Low(Integer);
                dp_texts_local[local_idx] := '';
                dp_segments_local[local_idx] := MaxInt;
            end;
            dp_scores_local[0] := 0;
            dp_segments_local[0] := 0;

            for local_idx := 0 to dangling_idx_local - 1 do
            begin
                if dp_scores_local[local_idx] = Low(Integer) then
                begin
                    Continue;
                end;
                local_has_multi_span := False;
                for local_len := Min(c_max_span_syllables,
                    dangling_idx_local - local_idx) downto 2 do
                begin
                    if not try_pick_span_candidate_local(local_idx, local_len,
                        local_text, local_score) then
                    begin
                        Continue;
                    end;
                    local_has_multi_span := True;
                    consider_span_local(local_len, local_text, local_score);
                end;

                // If a real multi-syllable word starts here, do not build the
                // same span from single characters.
                if (not local_has_multi_span) and
                    try_pick_span_candidate_local(local_idx, 1, local_text,
                    local_score) then
                begin
                    consider_span_local(1, local_text, local_score);
                end;
            end;

            if (dp_scores_local[dangling_idx_local] = Low(Integer)) or
                (dp_texts_local[dangling_idx_local] = '') then
            begin
                Exit;
            end;

            local_candidate := Default(TncCandidate);
            local_candidate.text := dp_texts_local[dangling_idx_local];
            local_candidate.comment := tail_comment_local;
            local_candidate.source := cs_rule;
            local_candidate.score := dp_scores_local[dangling_idx_local] +
                7200 + (dangling_idx_local * 160) -
                ((Length(raw_parts_local) - dangling_idx_local) * 45);
            local_candidate.dict_weight := dp_scores_local[dangling_idx_local];
            local_candidate.has_dict_weight := True;
            Result := True;
        end;

        function split_compact_syllables_for_dangling_local(
            const local_text: string; out out_parts: TncPinyinParseResult): Boolean;
        const
            c_max_syllable_len = 6;
            c_initials_local: array[0..22] of string = (
                'zh', 'ch', 'sh',
                'b', 'p', 'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h',
                'j', 'q', 'x', 'r', 'z', 'c', 's', 'y', 'w'
            );
            c_finals_local: array[0..35] of string = (
                'iang', 'iong', 'uang',
                'uai', 'uan', 'iao', 'ian', 'ing', 'ang', 'eng', 'ong',
                'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
                'ia', 'ie', 'in', 'iu', 'ua', 'ui', 'un', 'uo',
                've', 'van', 'vn', 'ue',
                'a', 'e', 'i', 'o', 'u', 'v'
            );
            c_finals_no_initial_local: array[0..11] of string = (
                'ang', 'eng',
                'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
                'a', 'e', 'o'
            );
        var
            normalized_local: string;
            pos_local: Integer;
            probe_len_local: Integer;
            token_local: string;
            best_token_local: string;
            out_idx_local: Integer;

            function is_initial_final_compatible_local(
                const initial_value: string; const final_value: string): Boolean;
            begin
                if not nc_is_pinyin_spelling_helper_compatible(initial_value,
                    final_value) then
                begin
                    Exit(False);
                end;

                if (initial_value = 'zh') or (initial_value = 'ch') or
                    (initial_value = 'sh') or (initial_value = 'r') or
                    (initial_value = 'z') or (initial_value = 'c') or
                    (initial_value = 's') then
                begin
                    if (final_value = 'ia') or (final_value = 'in') or
                        (final_value = 'ing') or (final_value = 'iu') or
                        (final_value = 'ie') or (final_value = 'ian') or
                        (final_value = 'iang') or (final_value = 'iao') or
                        (final_value = 'iong') or (final_value = 'ue') or
                        (final_value = 've') or (final_value = 'van') or
                        (final_value = 'vn') then
                    begin
                        Exit(False);
                    end
                end;
                Result := True;
            end;

            function is_valid_single_syllable_local(
                const value: string): Boolean;
            var
                initial_idx_local: Integer;
                final_idx_local: Integer;
                final_value_local: string;
            begin
                Result := False;
                if value = '' then
                begin
                    Exit;
                end;

                for final_idx_local := Low(c_finals_no_initial_local) to
                    High(c_finals_no_initial_local) do
                begin
                    if value = c_finals_no_initial_local[final_idx_local] then
                    begin
                        Exit(True);
                    end;
                end;

                for initial_idx_local := Low(c_initials_local) to
                    High(c_initials_local) do
                begin
                    if Copy(value, 1, Length(c_initials_local[initial_idx_local])) <>
                        c_initials_local[initial_idx_local] then
                    begin
                        Continue;
                    end;
                    final_value_local := Copy(value,
                        Length(c_initials_local[initial_idx_local]) + 1,
                        MaxInt);
                    if final_value_local = '' then
                    begin
                        Continue;
                    end;
                    for final_idx_local := Low(c_finals_local) to
                        High(c_finals_local) do
                    begin
                        if (final_value_local = c_finals_local[final_idx_local]) and
                            is_initial_final_compatible_local(
                            c_initials_local[initial_idx_local],
                            final_value_local) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;

            procedure append_token_local(const append_text: string;
                const append_pos: Integer);
            begin
                SetLength(out_parts, out_idx_local + 1);
                out_parts[out_idx_local].text := append_text;
                out_parts[out_idx_local].start_index := append_pos;
                out_parts[out_idx_local].length := Length(append_text);
                Inc(out_idx_local);
            end;
        begin
            SetLength(out_parts, 0);
            Result := False;
            normalized_local := normalize_pinyin_text(local_text);
            if normalized_local = '' then
            begin
                Exit;
            end;

            pos_local := 1;
            out_idx_local := 0;
            while pos_local <= Length(normalized_local) do
            begin
                best_token_local := '';
                for probe_len_local := Min(c_max_syllable_len,
                    Length(normalized_local) - pos_local + 1) downto 1 do
                begin
                    token_local := Copy(normalized_local, pos_local,
                        probe_len_local);
                    if is_valid_single_syllable_local(token_local) then
                    begin
                        best_token_local := token_local;
                        Break;
                    end;
                end;

                if best_token_local = '' then
                begin
                    token_local := Copy(normalized_local, pos_local, 1);
                    if not is_single_initial_token_local(token_local) then
                    begin
                        SetLength(out_parts, 0);
                        Exit;
                    end;
                    best_token_local := token_local;
                end;

                append_token_local(best_token_local, pos_local);
                Inc(pos_local, Length(best_token_local));
            end;

            Result := Length(out_parts) > 0;
        end;
    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        Result := False;
        if (m_dictionary = nil) or (Trim(m_composition_text) = '') then
        begin
            Exit;
        end;

        if (not split_compact_syllables_for_dangling_local(m_composition_text,
            raw_parts_local)) or (Length(raw_parts_local) < 3) then
        begin
            parser_local := TncPinyinParser.Create;
            try
                raw_parts_local := parser_local.parse(m_composition_text);
            finally
                parser_local.Free;
            end;
        end;
        if Length(raw_parts_local) < 3 then
        begin
            Exit;
        end;

        dangling_idx_local := -1;
        for part_idx_local := 0 to High(raw_parts_local) - 1 do
        begin
            if is_single_initial_token_local(raw_parts_local[part_idx_local].text) then
            begin
                dangling_idx_local := part_idx_local;
                Break;
            end;
        end;
        if dangling_idx_local < 1 then
        begin
            Exit;
        end;

        prefix_key_local := '';
        for part_idx_local := 0 to dangling_idx_local - 1 do
        begin
            prefix_key_local := prefix_key_local +
                normalize_pinyin_text(raw_parts_local[part_idx_local].text);
        end;
        tail_comment_local := '';
        for part_idx_local := dangling_idx_local to High(raw_parts_local) do
        begin
            tail_comment_local := tail_comment_local +
                normalize_pinyin_text(raw_parts_local[part_idx_local].text);
        end;
        if (prefix_key_local = '') or (tail_comment_local = '') then
        begin
            Exit;
        end;

        if try_build_natural_prefix_candidate_local(out_candidate) then
        begin
            Exit(True);
        end;

        if not (dictionary_exact_lookup_cached(prefix_key_local,
            prefix_candidates_local) or
            dictionary_lookup_cached(prefix_key_local,
            prefix_candidates_local)) then
        begin
            Exit;
        end;
        if not pick_best_prefix_candidate_local then
        begin
            Exit;
        end;

        out_candidate := prefix_candidates_local[best_idx_local];
        out_candidate.comment := tail_comment_local;
        out_candidate.score := best_score_local + 7200 +
            (dangling_idx_local * 160) -
            ((Length(raw_parts_local) - dangling_idx_local) * 45);
        out_candidate.dict_weight := best_score_local;
        out_candidate.has_dict_weight := True;
        Result := Trim(out_candidate.text) <> '';
    end;

    function has_incremental_composition_local: Boolean;
    begin
        Result := m_composition_built_incrementally;
    end;

    function get_cached_stable_prefix_candidate_local(
        out out_candidate: TncCandidate): Boolean;
    begin
        if not stable_prefix_candidate_evaluated then
        begin
            stable_prefix_candidate_evaluated := True;
            has_stable_prefix_candidate := False;
            FillChar(stable_prefix_candidate, SizeOf(stable_prefix_candidate), 0);

            if (m_dictionary <> nil) and has_multi_syllable_input and
                is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and
                (not all_initial_compact_query) and
                ((not has_internal_dangling_initial) or
                normalized_typo_long_query_fast_path) then
            begin
                has_stable_prefix_candidate :=
                    try_build_stable_leading_prefix_partial_candidate_local(
                    stable_prefix_candidate) and
                    (Trim(stable_prefix_candidate.text) <> '') and
                    (Trim(stable_prefix_candidate.comment) <> '');
            end;
        end;

        if has_stable_prefix_candidate then
        begin
            out_candidate := stable_prefix_candidate;
        end
        else
        begin
            FillChar(out_candidate, SizeOf(out_candidate), 0);
        end;
        Result := has_stable_prefix_candidate;
    end;

    function should_skip_expensive_sentence_generation_local: Boolean;
    var
        local_candidate: TncCandidate;
        tail_units_local: Integer;
    begin
        Result := False;
        if (not has_incremental_composition_local) or
            (not delayed_long_decode_mode) or
            (not is_full_pinyin_key(lookup_text)) or
            (input_syllable_count < 7) or
            all_initial_compact_query or has_internal_dangling_initial then
        begin
            Exit;
        end;

        if not (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) then
        begin
            Exit;
        end;

        if has_preferred_tail_phrase_local(2) or
            has_complete_exact_tail_phrase_local(2) or
            has_preferred_tail_phrase_local(3) or
            has_complete_exact_tail_phrase_local(3) then
        begin
            Exit;
        end;

        if not get_cached_stable_prefix_candidate_local(local_candidate) then
        begin
            Exit;
        end;

        tail_units_local := get_effective_compact_pinyin_unit_count(
            normalize_pinyin_text(Trim(local_candidate.comment)));
        if (tail_units_local <= 0) or (tail_units_local > 2) then
        begin
            Exit;
        end;

        Result := candidate_has_preferred_prefix_coverage_for_fast_exit_local(
            local_candidate);
    end;

    function try_build_typo_normalized_prefix_candidate_local(
        out out_candidate: TncCandidate;
        const require_typo_normalized_query: Boolean = True;
        const allow_stable_single_units: Boolean = False): Boolean;
    const
        c_max_span = 4;
        c_probe_limit = 8;
        c_base_score = 64000;
    var
        syllables_local: TncPinyinParseResult;
        pos_local: Integer;
        prefix_limit_local: Integer;
        span_len_local: Integer;
        text_local: string;
        tail_comment_local: string;
        score_local: Integer;
        phrase_count_local: Integer;

        function build_syllable_key_local(const local_start_idx: Integer;
            const local_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to
                local_start_idx + local_len - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function build_tail_comment_local(const local_start_idx: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := local_start_idx to High(syllables_local) do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function has_strong_phrase_for_span_local(const local_start_idx: Integer;
            const local_len: Integer): Boolean;
        var
            local_key: string;
            local_text: string;
            local_bonus: Integer;
            local_results: TncCandidateList;
            local_idx: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            if local_len < 2 then
            begin
                Exit;
            end;

            local_key := build_syllable_key_local(local_start_idx, local_len);
            if local_key = '' then
            begin
                Exit;
            end;

            if try_get_preferred_query_phrase_text_local(local_key, local_text,
                local_bonus) and
                (get_candidate_text_unit_count(Trim(local_text)) = local_len) then
            begin
                Exit(True);
            end;

            if dictionary_exact_lookup_cached(local_key, local_results) or
                dictionary_lookup_cached(local_key, local_results) then
            begin
                for local_idx := 0 to High(local_results) do
                begin
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(local_results[local_idx].text);
                    if get_candidate_text_unit_count(candidate_text_local) =
                        local_len then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function candidate_breaks_preferred_anchor_local(
            const local_text: string; const local_start_idx: Integer;
            const local_len: Integer): Boolean;
        var
            candidate_units_local: TArray<string>;
            anchor_len_local: Integer;
            anchor_key_local: string;
            anchor_text_local: string;
            anchor_bonus_local: Integer;
            anchor_units_local: TArray<string>;
            unit_idx_local: Integer;
            mismatch_local: Boolean;
        begin
            Result := False;
            candidate_units_local := split_text_units(Trim(local_text));
            if Length(candidate_units_local) <> local_len then
            begin
                Exit;
            end;

            for anchor_len_local := Min(4, local_len) downto 2 do
            begin
                anchor_key_local := build_syllable_key_local(local_start_idx,
                    anchor_len_local);
                if anchor_key_local = '' then
                begin
                    Continue;
                end;
                if not try_get_preferred_query_phrase_text_local(anchor_key_local,
                    anchor_text_local, anchor_bonus_local) then
                begin
                    Continue;
                end;
                anchor_units_local := split_text_units(Trim(anchor_text_local));
                if Length(anchor_units_local) <> anchor_len_local then
                begin
                    Continue;
                end;

                mismatch_local := False;
                for unit_idx_local := 0 to anchor_len_local - 1 do
                begin
                    if not SameText(candidate_units_local[unit_idx_local],
                        anchor_units_local[unit_idx_local]) then
                    begin
                        mismatch_local := True;
                        Break;
                    end;
                end;
                Exit(mismatch_local);
            end;
        end;

        function candidate_breaks_stable_boundary_local(
            const local_text: string; const local_start_idx: Integer;
            const local_len: Integer;
            const local_candidate_weight: Integer): Boolean; forward;

        function try_get_span_text_local(const local_start_idx: Integer;
            const local_span_len: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        var
            local_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                local_text: string;
                local_score: Integer;
                local_candidate_weight: Integer;
                preferred_bonus_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            local_key := build_syllable_key_local(local_start_idx,
                local_span_len);
            if local_key = '' then
            begin
                Exit;
            end;

            if try_get_preferred_query_phrase_text_local(local_key, local_text,
                preferred_bonus_local) and
                (get_candidate_text_unit_count(local_text) = local_span_len) then
            begin
                out_text := local_text;
                out_score := preferred_bonus_local + local_span_len * 360;
                Exit(True);
            end;

            if not dictionary_exact_lookup_cached(local_key, local_results) then
            begin
                Exit;
            end;
            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if (local_text = '') or
                    (get_candidate_text_unit_count(local_text) <> local_span_len) then
                begin
                    Continue;
                end;
                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end;
                local_candidate_weight := local_score;
                if candidate_breaks_preferred_anchor_local(local_text,
                    local_start_idx, local_span_len) or
                    candidate_breaks_stable_boundary_local(local_text,
                    local_start_idx, local_span_len,
                    local_candidate_weight) then
                begin
                    Continue;
                end;
                out_text := local_text;
                out_score := local_score + local_span_len * 240;
                Exit(True);
            end;
        end;

        function try_get_stable_single_text_local(const local_start_idx: Integer;
            out out_text: string; out out_score: Integer): Boolean;
        var
            local_syllable: string;
            fixed_text_local: string;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (local_start_idx < 0) or
                (local_start_idx >= Length(syllables_local)) then
            begin
                Exit;
            end;
            local_syllable := normalize_pinyin_text(
                syllables_local[local_start_idx].text);
            if local_syllable = '' then
            begin
                Exit;
            end;

            fixed_text_local := get_fixed_resolved_boundary_char_local(
                local_syllable);
            if fixed_text_local = '' then
            begin
                Exit;
            end;

            out_text := fixed_text_local;
            out_score := 900;
            Result := True;
        end;

        function candidate_breaks_stable_boundary_local(
            const local_text: string; const local_start_idx: Integer;
            const local_len: Integer;
            const local_candidate_weight: Integer): Boolean;
        var
            candidate_units_local: TArray<string>;
            split_idx_local: Integer;
            stable_single_text_local: string;
            stable_single_score_local: Integer;
            protected_span_len_local: Integer;
            protected_text_local: string;
            protected_weight_local: Integer;
            protected_rank_local: Integer;
            protected_units_local: TArray<string>;
            overlap_len_local: Integer;
            overlap_idx_local: Integer;
            mismatch_local: Boolean;

            function is_confident_following_span_local(
                const local_span_len: Integer;
                const local_rank: Integer;
                const local_weight: Integer): Boolean;
            begin
                Result := False;
                if local_rank <> 0 then
                begin
                    Exit;
                end;
                if ((local_span_len = 2) and (local_weight >= 900)) or
                    ((local_span_len = 3) and (local_weight >= 520)) or
                    ((local_span_len >= 4) and (local_weight >= 420)) then
                begin
                    Result := True;
                end;
            end;

            function try_get_confident_following_span_local(
                const local_start: Integer;
                const local_span_len: Integer;
                out out_text: string;
                out out_weight: Integer;
                out out_rank: Integer): Boolean;
            var
                local_key: string;
                local_results: TncCandidateList;
                local_idx: Integer;
                local_result_text: string;
                local_result_weight: Integer;
            begin
                Result := False;
                out_text := '';
                out_weight := 0;
                out_rank := -1;
                if (local_start < 0) or (local_span_len < 2) or
                    (local_start + local_span_len > Length(syllables_local)) then
                begin
                    Exit;
                end;

                local_key := build_syllable_key_local(local_start,
                    local_span_len);
                if (local_key = '') or
                    (not dictionary_exact_lookup_cached(local_key,
                    local_results)) then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(local_results) do
                begin
                    if local_idx >= 4 then
                    begin
                        Break;
                    end;
                    if Trim(local_results[local_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(local_results[local_idx]) or
                        is_runtime_common_pattern_candidate(
                        local_results[local_idx]) or
                        is_runtime_redup_candidate(local_results[local_idx]) then
                    begin
                        Continue;
                    end;
                    local_result_text := Trim(local_results[local_idx].text);
                    if (local_result_text = '') or
                        (get_candidate_text_unit_count(local_result_text) <>
                        local_span_len) then
                    begin
                        Continue;
                    end;
                    local_result_weight := local_results[local_idx].score;
                    if local_results[local_idx].has_dict_weight and
                        (local_results[local_idx].dict_weight >
                        local_result_weight) then
                    begin
                        local_result_weight :=
                            local_results[local_idx].dict_weight;
                    end;
                    if not is_confident_following_span_local(local_span_len,
                        local_idx, local_result_weight) then
                    begin
                        Continue;
                    end;
                    out_text := local_result_text;
                    out_weight := local_result_weight;
                    out_rank := local_idx;
                    Exit(True);
                end;
            end;
        begin
            Result := False;
            candidate_units_local := split_text_units(Trim(local_text));
            if Length(candidate_units_local) <> local_len then
            begin
                Exit;
            end;

            for split_idx_local := 1 to local_len - 1 do
            begin
                for protected_span_len_local := Min(4,
                    Length(syllables_local) - local_start_idx -
                    split_idx_local) downto 2 do
                begin
                    if not try_get_confident_following_span_local(
                        local_start_idx + split_idx_local,
                        protected_span_len_local, protected_text_local,
                        protected_weight_local, protected_rank_local) then
                    begin
                        Continue;
                    end;
                    protected_units_local := split_text_units(
                        protected_text_local);
                    if Length(protected_units_local) <>
                        protected_span_len_local then
                    begin
                        Continue;
                    end;

                    overlap_len_local := Min(local_len - split_idx_local,
                        protected_span_len_local);
                    mismatch_local := False;
                    for overlap_idx_local := 0 to overlap_len_local - 1 do
                    begin
                        if not SameText(candidate_units_local[split_idx_local +
                            overlap_idx_local],
                            protected_units_local[overlap_idx_local]) then
                        begin
                            mismatch_local := True;
                            Break;
                        end;
                    end;
                    if mismatch_local and
                        (local_candidate_weight <= protected_weight_local +
                        220) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            if local_len < 3 then
            begin
                Exit;
            end;

            for split_idx_local := 1 to local_len - 1 do
            begin
                if not try_get_stable_single_text_local(
                    local_start_idx + split_idx_local, stable_single_text_local,
                    stable_single_score_local) then
                begin
                    Continue;
                end;
                if not has_strong_phrase_for_span_local(local_start_idx,
                    split_idx_local) then
                begin
                    Continue;
                end;
                if not SameText(candidate_units_local[split_idx_local],
                    stable_single_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function should_leave_stable_single_boundary_open_local(
            const local_start_idx: Integer): Boolean;
        var
            stable_single_text_local: string;
            stable_single_score_local: Integer;
            next_span_len_local: Integer;
            next_text_local: string;
            next_score_local: Integer;
            current_syllable_local: string;
            next_syllable_local: string;
            next_next_syllable_local: string;
        begin
            Result := False;
            if (not allow_stable_single_units) or
                (local_start_idx >= prefix_limit_local - 1) then
            begin
                Exit;
            end;
            if not try_get_stable_single_text_local(local_start_idx,
                stable_single_text_local, stable_single_score_local) then
            begin
                Exit;
            end;

            current_syllable_local := normalize_pinyin_text(
                syllables_local[local_start_idx].text);
            if local_start_idx + 2 < Length(syllables_local) then
            begin
                next_syllable_local := normalize_pinyin_text(
                    syllables_local[local_start_idx + 1].text);
                next_next_syllable_local := normalize_pinyin_text(
                    syllables_local[local_start_idx + 2].text);
                if (current_syllable_local = 'qian') and
                    (next_syllable_local = 'liang') and
                    (next_next_syllable_local = 'nian') then
                begin
                    Exit(True);
                end;
            end;

            next_span_len_local := Min(c_max_span,
                prefix_limit_local - local_start_idx - 1);
            while next_span_len_local >= 2 do
            begin
                if try_get_span_text_local(local_start_idx + 1,
                    next_span_len_local, next_text_local,
                    next_score_local) then
                begin
                    Exit(True);
                end;
                Dec(next_span_len_local);
            end;
        end;
    var
        span_text_local: string;
        span_score_local: Integer;
    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;
        if require_typo_normalized_query and
            (m_last_lookup_normalized_from = '') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;

        prefix_limit_local := Length(syllables_local) - 1;
        pos_local := 0;
        text_local := '';
        score_local := c_base_score;
        phrase_count_local := 0;
        while pos_local < prefix_limit_local do
        begin
            span_len_local := Min(c_max_span, prefix_limit_local - pos_local);
            while span_len_local >= 2 do
            begin
                if try_get_span_text_local(pos_local, span_len_local,
                    span_text_local, span_score_local) then
                begin
                    if should_leave_stable_single_boundary_open_local(
                        pos_local) then
                    begin
                        Dec(span_len_local);
                        Continue;
                    end;
                    text_local := text_local + span_text_local;
                    Inc(score_local, span_score_local);
                    Inc(pos_local, span_len_local);
                    Inc(phrase_count_local);
                    Break;
                end;
                Dec(span_len_local);
            end;

            if span_len_local < 2 then
            begin
                if allow_stable_single_units and
                    try_get_stable_single_text_local(pos_local,
                    span_text_local, span_score_local) then
                begin
                    text_local := text_local + span_text_local;
                    Inc(score_local, span_score_local);
                    Inc(pos_local);
                    Continue;
                end;
                Break;
            end;
        end;

        tail_comment_local := build_tail_comment_local(pos_local);
        if (phrase_count_local <= 0) or (text_local = '') or
            (tail_comment_local = '') or
            (get_candidate_text_unit_count(text_local) < 2) then
        begin
            Exit;
        end;

        out_candidate.text := text_local;
        out_candidate.comment := tail_comment_local;
        out_candidate.score := score_local + get_candidate_text_unit_count(
            text_local) * 220;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' typoprefix=[%s/%s pos=%d len=%d]',
                [text_local, tail_comment_local, pos_local,
                Length(syllables_local)]);
        end;
        Result := True;
    end;

    function try_build_relaxed_no_initial_tail_phrase_prefix_candidate_local(
        const query_text_local: string; out out_candidate: TncCandidate): Boolean;
    const
        c_probe_limit = 6;
        c_base_score = 58000;
    var
        normalized_local: string;
        split_idx_local: Integer;
        head_key_local: string;
        tail_key_local: string;
        parser_local: TncPinyinParser;
        head_parts_local: TncPinyinParseResult;
        tail_parts_local: TncPinyinParseResult;
        head_units_local: Integer;
        head_results_local: TncCandidateList;
        result_idx_local: Integer;
        candidate_text_local: string;
        candidate_score_local: Integer;
        best_text_local: string;
        best_score_local: Integer;
        preferred_text_local: string;
        preferred_bonus_local: Integer;

        function build_parts_key_local(
            const parts_local: TncPinyinParseResult): string;
        var
            part_idx_local: Integer;
        begin
            Result := '';
            for part_idx_local := 0 to High(parts_local) do
            begin
                Result := Result + normalize_pinyin_text(
                    parts_local[part_idx_local].text);
            end;
        end;

        function load_head_results_local: Boolean;
        begin
            Result := lookup_exact_full_pinyin_cached_local(head_key_local,
                head_results_local);
            if not Result then
            begin
                Result := dictionary_exact_lookup_cached(head_key_local,
                    head_results_local);
            end;
            if not Result then
            begin
                Result := dictionary_lookup_cached(head_key_local,
                    head_results_local);
            end;
        end;
    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        Result := False;
        if (m_dictionary = nil) or (Trim(query_text_local) = '') then
        begin
            Exit;
        end;

        normalized_local := normalize_pinyin_text(query_text_local);
        if Length(normalized_local) < 4 then
        begin
            Exit;
        end;

        parser_local := TncPinyinParser.Create;
        try
            for split_idx_local := Length(normalized_local) - 1 downto 2 do
            begin
                head_key_local := Copy(normalized_local, 1, split_idx_local);
                tail_key_local := Copy(normalized_local, split_idx_local + 1,
                    MaxInt);
                if not is_relaxed_no_initial_syllable_text(tail_key_local) then
                begin
                    Continue;
                end;

                head_parts_local := parser_local.parse(head_key_local);
                if Length(head_parts_local) < 2 then
                begin
                    Continue;
                end;
                if not SameText(build_parts_key_local(head_parts_local),
                    head_key_local) then
                begin
                    Continue;
                end;

                tail_parts_local := parser_local.parse(tail_key_local);
                if (Length(tail_parts_local) <> 1) or
                    (not SameText(normalize_pinyin_text(
                    tail_parts_local[0].text), tail_key_local)) then
                begin
                    Continue;
                end;

                head_units_local := Length(head_parts_local);
                best_text_local := '';
                best_score_local := Low(Integer);
                if try_get_preferred_query_phrase_text_local(head_key_local,
                    preferred_text_local, preferred_bonus_local) and
                    (get_candidate_text_unit_count(Trim(preferred_text_local)) =
                    head_units_local) then
                begin
                    best_text_local := Trim(preferred_text_local);
                    best_score_local := preferred_bonus_local +
                        head_units_local * 360;
                end;

                if load_head_results_local then
                begin
                    for result_idx_local := 0 to High(head_results_local) do
                    begin
                        if result_idx_local >= c_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(head_results_local[result_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;

                        candidate_text_local :=
                            Trim(head_results_local[result_idx_local].text);
                        if (candidate_text_local = '') or
                            (get_candidate_text_unit_count(candidate_text_local) <>
                            head_units_local) then
                        begin
                            Continue;
                        end;

                        candidate_score_local :=
                            head_results_local[result_idx_local].score;
                        if head_results_local[result_idx_local].has_dict_weight and
                            (head_results_local[result_idx_local].dict_weight >
                            candidate_score_local) then
                        begin
                            candidate_score_local :=
                                head_results_local[result_idx_local].dict_weight;
                        end;
                        candidate_score_local := candidate_score_local +
                            head_units_local * 280;
                        if candidate_score_local > best_score_local then
                        begin
                            best_text_local := candidate_text_local;
                            best_score_local := candidate_score_local;
                        end;
                    end;
                end;

                if best_text_local = '' then
                begin
                    Continue;
                end;

                out_candidate.text := best_text_local;
                out_candidate.comment := tail_key_local;
                out_candidate.score := c_base_score + best_score_local +
                    head_units_local * 260;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := out_candidate.score;
                Result := True;
                Exit;
            end;
        finally
            parser_local.Free;
        end;
    end;

    function try_build_ranked_path_prefix_continuation_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    var
        previous_query_local: string;
        normalized_query_local: string;
        path_prefix_query_local: string;
        suffix_key_local: string;
        suffix_text_local: string;
        suffix_path_local: string;
        suffix_score_local: Integer;
        ranked_path_local: string;
        path_parts_local: TArray<string>;
        syllables_local: TncPinyinParseResult;
        part_idx_local: Integer;
        combined_text_local: string;
        last_path_segment_local: string;
        prefix_syllable_count_local: Integer;
        suffix_syllable_count_local: Integer;
        rollback_units_local: Integer;
        rollback_candidate_prefix_count_local: Integer;
        rollback_suffix_count_local: Integer;
        rollback_suffix_key_local: string;
        rollback_prefix_text_local: string;
        rollback_prefix_path_local: string;
        rollback_part_idx_local: Integer;
        rollback_part_text_local: string;
        rollback_part_units_local: Integer;
        active_path_part_high_local: Integer;
        same_syllable_tail_completion_local: Boolean;

        function build_query_key_from_syllables_local(
            const local_start_idx: Integer; const local_count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_count <= 0) or
                (local_start_idx + local_count > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function is_stable_sentence_tail_suffix_key_local(
            const local_suffix_key: string): Boolean;
        var
            normalized_suffix_key_local: string;
        begin
            normalized_suffix_key_local := normalize_pinyin_text(local_suffix_key);
            Result := (normalized_suffix_key_local = 'le') or
                (normalized_suffix_key_local = 'ba') or
                (normalized_suffix_key_local = 'ma') or
                (normalized_suffix_key_local = 'a') or
                (normalized_suffix_key_local = 'la') or
                (normalized_suffix_key_local = 'ne') or
                (normalized_suffix_key_local = 'ya') or
                (normalized_suffix_key_local = 'wa');
        end;

        function try_get_best_suffix_text_local(const local_suffix_key: string;
            const local_expected_units: Integer; out out_text: string;
            out out_score: Integer): Boolean;
        const
            c_suffix_probe_limit = 10;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
            local_score: Integer;
        begin
            Result := False;
            out_text := '';
            out_score := 0;
            if (local_suffix_key = '') or (local_expected_units <= 0) or
                ((not dictionary_exact_lookup_cached(local_suffix_key,
                local_results)) and
                (not dictionary_lookup_cached(local_suffix_key,
                local_results))) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_suffix_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if (local_text = '') or
                    (get_candidate_text_unit_count(local_text) <>
                    local_expected_units) then
                begin
                    Continue;
                end;

                local_score := local_results[local_idx].score;
                if local_results[local_idx].has_dict_weight and
                    (local_results[local_idx].dict_weight > local_score) then
                begin
                    local_score := local_results[local_idx].dict_weight;
                end;

                if (not Result) or (local_score > out_score) then
                begin
                    Result := True;
                    out_text := local_text;
                    out_score := local_score;
                end;
            end;
        end;

        function try_get_best_suffix_chain_text_local(
            const local_start_idx: Integer; const local_expected_units: Integer;
            out out_text: string; out out_path: string;
            out out_score: Integer): Boolean;
        const
            c_span_probe_limit = 10;
            c_low_score = Low(Integer) div 4;
        var
            best_scores_local: TArray<Integer>;
            best_texts_local: TArray<string>;
            best_paths_local: TArray<string>;
            pos_local: Integer;
            span_units_local: Integer;
            next_pos_local: Integer;
            span_text_local: string;
            span_score_local: Integer;
            total_score_local: Integer;
            path_value_local: string;

            function try_get_best_exact_span_text_local(
                const span_start_idx: Integer; const span_units: Integer;
                out span_text: string; out span_score: Integer): Boolean;
            var
                span_key_local: string;
                span_results_local: TncCandidateList;
                span_idx_local: Integer;
                candidate_text_local: string;
                candidate_score_local: Integer;
            begin
                Result := False;
                span_text := '';
                span_score := 0;
                if (span_start_idx < 0) or (span_units <= 0) or
                    (span_units > 4) or
                    (span_start_idx + span_units >
                    Length(syllables_local)) then
                begin
                    Exit;
                end;

                span_key_local := build_query_key_from_syllables_local(
                    span_start_idx, span_units);
                if (span_key_local = '') or
                    (not dictionary_exact_lookup_cached(span_key_local,
                    span_results_local)) then
                begin
                    Exit;
                end;

                for span_idx_local := 0 to High(span_results_local) do
                begin
                    if span_idx_local >= c_span_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(span_results_local[span_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(span_results_local[span_idx_local]) or
                        is_runtime_common_pattern_candidate(
                        span_results_local[span_idx_local]) or
                        is_runtime_redup_candidate(span_results_local[span_idx_local]) then
                    begin
                        Continue;
                    end;

                    candidate_text_local := Trim(
                        span_results_local[span_idx_local].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        span_units) then
                    begin
                        Continue;
                    end;

                    candidate_score_local := span_results_local[span_idx_local].score;
                    if span_results_local[span_idx_local].has_dict_weight and
                        (span_results_local[span_idx_local].dict_weight >
                        candidate_score_local) then
                    begin
                        candidate_score_local :=
                            span_results_local[span_idx_local].dict_weight;
                    end;

                    if span_units > 1 then
                    begin
                        Inc(candidate_score_local, 3200 + span_units * 720);
                    end
                    else
                    begin
                        Dec(candidate_score_local, 240);
                    end;

                    Inc(candidate_score_local,
                        (c_span_probe_limit - span_idx_local) * 24);
                    if (not Result) or
                        (candidate_score_local > span_score) then
                    begin
                        Result := True;
                        span_text := candidate_text_local;
                        span_score := candidate_score_local;
                    end;
                end;
            end;

        begin
            Result := False;
            out_text := '';
            out_path := '';
            out_score := 0;
            if (local_start_idx < 0) or (local_expected_units <= 0) or
                (local_expected_units > 4) or
                (local_start_idx + local_expected_units >
                Length(syllables_local)) then
            begin
                Exit;
            end;

            SetLength(best_scores_local, local_expected_units + 1);
            SetLength(best_texts_local, local_expected_units + 1);
            SetLength(best_paths_local, local_expected_units + 1);
            for pos_local := 0 to local_expected_units do
            begin
                best_scores_local[pos_local] := c_low_score;
                best_texts_local[pos_local] := '';
                best_paths_local[pos_local] := '';
            end;
            best_scores_local[local_expected_units] := 0;

            for pos_local := local_expected_units - 1 downto 0 do
            begin
                for span_units_local := Min(4, local_expected_units - pos_local)
                    downto 1 do
                begin
                    next_pos_local := pos_local + span_units_local;
                    if best_scores_local[next_pos_local] <= c_low_score div 2 then
                    begin
                        Continue;
                    end;
                    if not try_get_best_exact_span_text_local(
                        local_start_idx + pos_local, span_units_local,
                        span_text_local, span_score_local) then
                    begin
                        Continue;
                    end;

                    total_score_local := span_score_local +
                        best_scores_local[next_pos_local];
                    if span_units_local > 1 then
                    begin
                        Inc(total_score_local, span_units_local * 320);
                    end;

                    if total_score_local > best_scores_local[pos_local] then
                    begin
                        best_scores_local[pos_local] := total_score_local;
                        best_texts_local[pos_local] := span_text_local +
                            best_texts_local[next_pos_local];
                        path_value_local := span_text_local;
                        if best_paths_local[next_pos_local] <> '' then
                        begin
                            path_value_local := path_value_local +
                                c_segment_path_separator +
                                best_paths_local[next_pos_local];
                        end;
                        best_paths_local[pos_local] := path_value_local;
                    end;
                end;
            end;

            if (best_scores_local[0] <= c_low_score div 2) or
                (best_texts_local[0] = '') or
                (get_candidate_text_unit_count(best_texts_local[0]) <>
                local_expected_units) then
            begin
                Exit;
            end;

            out_text := best_texts_local[0];
            out_path := best_paths_local[0];
            out_score := best_scores_local[0];
            Result := True;
        end;

        function suffix_is_precisely_extendable_local(
            const local_suffix_key: string): Boolean;
        var
            suffix_ch_local: Char;
            extended_suffix_key_local: string;
        begin
            Result := False;
            if (local_suffix_key = '') or
                (not is_full_pinyin_key(local_suffix_key)) then
            begin
                Exit;
            end;

            for suffix_ch_local := 'a' to 'z' do
            begin
                extended_suffix_key_local := local_suffix_key +
                    suffix_ch_local;
                if is_full_pinyin_key(extended_suffix_key_local) and
                    (get_effective_compact_pinyin_unit_count(
                    extended_suffix_key_local) = 1) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_complete_full_query_candidate_local: Boolean;
        const
            c_full_probe_limit = 16;
        var
            local_results: TncCandidateList;
            local_idx: Integer;
            local_text: string;
        begin
            Result := False;
            if (not is_full_pinyin_key(normalized_query_local)) or
                (not dictionary_exact_lookup_cached(normalized_query_local,
                local_results)) then
            begin
                Exit;
            end;

            for local_idx := 0 to High(local_results) do
            begin
                if local_idx >= c_full_probe_limit then
                begin
                    Break;
                end;
                if Trim(local_results[local_idx].comment) <> '' then
                begin
                    Continue;
                end;

                local_text := Trim(local_results[local_idx].text);
                if (local_text <> '') and
                    (get_candidate_text_unit_count(local_text) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        Result := False;

        previous_query_local := normalize_pinyin_text(m_last_ranked_query_key);
        normalized_query_local := normalize_pinyin_text(lookup_text);
        ranked_path_local := Trim(m_last_ranked_top_path);
        same_syllable_tail_completion_local := False;
        if (previous_query_local = '') or (normalized_query_local = '') or
            (ranked_path_local = '') or
            (Length(normalized_query_local) <= Length(previous_query_local)) or
            (Copy(normalized_query_local, 1, Length(previous_query_local)) <>
            previous_query_local) then
        begin
            Exit;
        end;

        if has_complete_full_query_candidate_local then
        begin
            Exit;
        end;

        if get_encoded_path_segment_count_local(ranked_path_local) <= 1 then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        path_parts_local := ranked_path_local.Split([c_segment_path_separator]);
        active_path_part_high_local := High(path_parts_local);
        combined_text_local := '';
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            combined_text_local := combined_text_local + Trim(path_parts_local[part_idx_local]);
        end;
        prefix_syllable_count_local := get_candidate_text_unit_count(
            combined_text_local);
        if (combined_text_local = '') or (prefix_syllable_count_local <= 0) or
            (get_candidate_text_unit_count(combined_text_local) <>
            prefix_syllable_count_local) then
        begin
            Exit;
        end;

        if prefix_syllable_count_local >= Length(syllables_local) then
        begin
            if (prefix_syllable_count_local <> Length(syllables_local)) or
                (prefix_syllable_count_local <= 1) or
                (Length(path_parts_local) <= 1) then
            begin
                Exit;
            end;

            last_path_segment_local := Trim(path_parts_local[High(path_parts_local)]);
            if (get_candidate_text_unit_count(last_path_segment_local) < 1) or
                (get_candidate_text_unit_count(last_path_segment_local) > 2) then
            begin
                Exit;
            end;
            same_syllable_tail_completion_local := True;

            Dec(prefix_syllable_count_local,
                get_candidate_text_unit_count(last_path_segment_local));
            combined_text_local := '';
            ranked_path_local := '';
            active_path_part_high_local := High(path_parts_local) - 1;
            for part_idx_local := 0 to active_path_part_high_local do
            begin
                combined_text_local := combined_text_local +
                    Trim(path_parts_local[part_idx_local]);
                if ranked_path_local <> '' then
                begin
                    ranked_path_local := ranked_path_local +
                        c_segment_path_separator;
                end;
                ranked_path_local := ranked_path_local +
                    Trim(path_parts_local[part_idx_local]);
            end;

            if (combined_text_local = '') or
                (get_encoded_path_segment_count_local(ranked_path_local) <= 1) then
            begin
                Exit;
            end;
        end;

        path_prefix_query_local := build_query_key_from_syllables_local(0,
            prefix_syllable_count_local);
        if (path_prefix_query_local = '') or
            (Copy(normalized_query_local, 1, Length(path_prefix_query_local)) <>
            path_prefix_query_local) then
        begin
            Exit;
        end;
        if (input_syllable_count <= 4) and (m_dictionary <> nil) and
            (not m_dictionary.is_base_entry(path_prefix_query_local,
            combined_text_local)) then
        begin
            Exit;
        end;

        suffix_syllable_count_local := Length(syllables_local) -
            prefix_syllable_count_local;
        if (suffix_syllable_count_local < 1) or
            (suffix_syllable_count_local > 4) then
        begin
            Exit;
        end;

        if is_full_pinyin_key(normalized_query_local) and
            (active_path_part_high_local > 0) then
        begin
            for rollback_units_local := Min(2, active_path_part_high_local + 1) downto 1 do
            begin
                rollback_candidate_prefix_count_local :=
                    prefix_syllable_count_local;
                for rollback_part_idx_local := active_path_part_high_local -
                    rollback_units_local + 1 to active_path_part_high_local do
                begin
                    rollback_part_text_local := Trim(
                        path_parts_local[rollback_part_idx_local]);
                    rollback_part_units_local := get_candidate_text_unit_count(
                        rollback_part_text_local);
                    if rollback_part_units_local <> 1 then
                    begin
                        rollback_candidate_prefix_count_local := -1;
                        Break;
                    end;
                    Dec(rollback_candidate_prefix_count_local,
                        rollback_part_units_local);
                end;

                if rollback_candidate_prefix_count_local <= 0 then
                begin
                    Continue;
                end;

                rollback_suffix_count_local := Length(syllables_local) -
                    rollback_candidate_prefix_count_local;
                if ((rollback_suffix_count_local < 2) and
                    (not same_syllable_tail_completion_local)) or
                    (rollback_suffix_count_local < 1) or
                    (rollback_suffix_count_local > 4) then
                begin
                    Continue;
                end;

                rollback_suffix_key_local := build_query_key_from_syllables_local(
                    rollback_candidate_prefix_count_local,
                    rollback_suffix_count_local);
                if (rollback_suffix_key_local = '') or
                    (Length(rollback_suffix_key_local) > 24) then
                begin
                    Continue;
                end;

                suffix_path_local := '';
                if not try_get_best_suffix_chain_text_local(
                    rollback_candidate_prefix_count_local,
                    rollback_suffix_count_local, suffix_text_local,
                    suffix_path_local, suffix_score_local) then
                begin
                    if not try_get_best_suffix_text_local(rollback_suffix_key_local,
                        rollback_suffix_count_local, suffix_text_local,
                        suffix_score_local) then
                    begin
                        Continue;
                    end;
                    suffix_path_local := suffix_text_local;
                end;

                rollback_prefix_text_local := '';
                rollback_prefix_path_local := '';
                for rollback_part_idx_local := 0 to
                    active_path_part_high_local - rollback_units_local do
                begin
                    rollback_part_text_local := Trim(
                        path_parts_local[rollback_part_idx_local]);
                    rollback_prefix_text_local := rollback_prefix_text_local +
                        rollback_part_text_local;
                    if rollback_prefix_path_local <> '' then
                    begin
                        rollback_prefix_path_local := rollback_prefix_path_local +
                            c_segment_path_separator;
                    end;
                    rollback_prefix_path_local := rollback_prefix_path_local +
                        rollback_part_text_local;
                end;

                if (rollback_prefix_text_local = '') or
                    (get_candidate_text_unit_count(rollback_prefix_text_local) <>
                    rollback_candidate_prefix_count_local) then
                begin
                    Continue;
                end;

                out_candidate.text := rollback_prefix_text_local +
                    suffix_text_local;
                out_candidate.comment := '';
                out_candidate.score := 66000 +
                    rollback_candidate_prefix_count_local * 256 +
                    Max(0, suffix_score_local) + rollback_suffix_count_local * 512;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := out_candidate.score;
                out_encoded_path := rollback_prefix_path_local +
                    c_segment_path_separator + suffix_path_local;
                Exit(True);
            end;
        end;

        suffix_key_local := build_query_key_from_syllables_local(
            prefix_syllable_count_local, suffix_syllable_count_local);
        if (suffix_key_local = '') or (Length(suffix_key_local) > 24) then
        begin
            Exit;
        end;

        out_candidate.text := combined_text_local;
        out_candidate.comment := suffix_key_local;
        out_candidate.score := 64000 + prefix_syllable_count_local * 256;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;
        out_encoded_path := ranked_path_local;

        if is_full_pinyin_key(suffix_key_local) and
            ((not suffix_is_precisely_extendable_local(suffix_key_local)) or
            is_stable_sentence_tail_suffix_key_local(suffix_key_local)) then
        begin
            if not try_get_best_suffix_chain_text_local(
                prefix_syllable_count_local, suffix_syllable_count_local,
                suffix_text_local, suffix_path_local, suffix_score_local) then
            begin
                if not try_get_best_suffix_text_local(suffix_key_local,
                    suffix_syllable_count_local, suffix_text_local,
                    suffix_score_local) then
                begin
                    Exit;
                end;
                suffix_path_local := suffix_text_local;
            end;

            out_candidate.text := combined_text_local + suffix_text_local;
            out_candidate.comment := '';
            Inc(out_candidate.score, Max(0, suffix_score_local) + 512);
            out_candidate.dict_weight := out_candidate.score;
            out_encoded_path := ranked_path_local +
                c_segment_path_separator + suffix_path_local;
        end;

        Result := True;
    end;

    function try_build_incremental_fixed_single_tail_completion_local(
        out out_candidate: TncCandidate;
        out out_encoded_path: string): Boolean;
    const
        c_previous_probe_limit = 8;
    var
        syllables_local: TncPinyinParseResult;
        tail_syllable_local: string;
        fixed_tail_text_local: string;
        previous_idx_local: Integer;
        previous_text_local: string;
        previous_comment_local: string;
        previous_units_local: TArray<string>;
        previous_score_local: Integer;
        full_score_local: Integer;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (Length(previous_candidates) <= 0) or
            (previous_lookup_key = '') or
            (Length(lookup_text) <> Length(previous_lookup_key) + 1) or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 2 then
        begin
            Exit;
        end;
        tail_syllable_local := normalize_pinyin_text(
            syllables_local[High(syllables_local)].text);
        if tail_syllable_local <> 'yi' then
        begin
            Exit;
        end;
        fixed_tail_text_local := get_fixed_resolved_boundary_char_local(
            tail_syllable_local);
        if (tail_syllable_local = '') or (fixed_tail_text_local = '') then
        begin
            Exit;
        end;

        for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
            High(previous_candidates)) do
        begin
            previous_text_local := Trim(
                previous_candidates[previous_idx_local].text);
            previous_comment_local := normalize_pinyin_text(Trim(
                previous_candidates[previous_idx_local].comment));
            previous_units_local := split_text_units(previous_text_local);
            if (previous_text_local = '') or (previous_comment_local = '') or
                (Length(previous_units_local) <> Length(syllables_local) - 1) or
                (Copy(tail_syllable_local, 1,
                Length(previous_comment_local)) <> previous_comment_local) then
            begin
                Continue;
            end;
            previous_score_local := previous_candidates[previous_idx_local].score;
            if previous_candidates[previous_idx_local].has_dict_weight and
                (previous_candidates[previous_idx_local].dict_weight >
                previous_score_local) then
            begin
                previous_score_local :=
                    previous_candidates[previous_idx_local].dict_weight;
            end;
            full_score_local := Max(0, previous_score_local) + 1400;
            if (not Result) or (full_score_local > out_candidate.score) then
            begin
                out_candidate.text := previous_text_local + fixed_tail_text_local;
                out_candidate.comment := '';
                out_candidate.score := full_score_local;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := False;
                out_candidate.dict_weight := 0;
                out_encoded_path := previous_text_local +
                    c_segment_path_separator + fixed_tail_text_local;
                Result := True;
            end;
        end;
    end;

    function try_build_incremental_exact_tail_completion_local(
        const tail_syllable_count: Integer; out out_candidate: TncCandidate;
        out out_encoded_path: string): Boolean;
    const
        c_tail_probe_limit = 8;
        c_min_tail_weight = 560;
    var
        syllables_local: TncPinyinParseResult;
        tail_query_local: string;
        last_syllable_local: string;
        tail_results_local: TncCandidateList;
        previous_idx_local: Integer;
        previous_text_local: string;
        previous_comment_local: string;
        previous_units_local: TArray<string>;
        previous_score_local: Integer;
        tail_idx_local: Integer;
        tail_text_local: string;
        tail_units_local: TArray<string>;
        tail_weight_local: Integer;
        overlap_idx_local: Integer;
        prefix_unit_count_local: Integer;
        prefix_text_local: string;
        full_score_local: Integer;
        syllable_idx_local: Integer;
        unit_idx_local: Integer;
        overlap_matches_local: Boolean;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (tail_syllable_count < 2) or
            (Length(previous_candidates) <= 0) or
            (previous_lookup_key = '') or
            (Length(lookup_text) <> Length(previous_lookup_key) + 1) or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <= tail_syllable_count then
        begin
            Exit;
        end;
        tail_query_local := '';
        for syllable_idx_local := Length(syllables_local) -
            tail_syllable_count to High(syllables_local) do
        begin
            tail_query_local := tail_query_local + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
        end;
        last_syllable_local := normalize_pinyin_text(
            syllables_local[High(syllables_local)].text);
        if (tail_query_local = '') or (last_syllable_local = '') or
            (not lookup_exact_full_pinyin_cached_local(tail_query_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        for previous_idx_local := 0 to High(previous_candidates) do
        begin
            previous_text_local := Trim(
                previous_candidates[previous_idx_local].text);
            previous_comment_local := normalize_pinyin_text(Trim(
                previous_candidates[previous_idx_local].comment));
            previous_units_local := split_text_units(previous_text_local);
            if (previous_text_local = '') or (previous_comment_local = '') or
                (Length(previous_units_local) <> Length(syllables_local) - 1) or
                (Copy(last_syllable_local, 1,
                Length(previous_comment_local)) <> previous_comment_local) then
            begin
                Continue;
            end;
            previous_score_local := previous_candidates[previous_idx_local].score;
            if previous_candidates[previous_idx_local].has_dict_weight and
                (previous_candidates[previous_idx_local].dict_weight >
                previous_score_local) then
            begin
                previous_score_local :=
                    previous_candidates[previous_idx_local].dict_weight;
            end;

            for tail_idx_local := 0 to Min(c_tail_probe_limit - 1,
                High(tail_results_local)) do
            begin
                if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                tail_units_local := split_text_units(tail_text_local);
                if Length(tail_units_local) <> tail_syllable_count then
                begin
                    Continue;
                end;
                tail_weight_local := tail_results_local[tail_idx_local].score;
                if tail_results_local[tail_idx_local].has_dict_weight and
                    (tail_results_local[tail_idx_local].dict_weight >
                    tail_weight_local) then
                begin
                    tail_weight_local :=
                        tail_results_local[tail_idx_local].dict_weight;
                end;
                if tail_weight_local < c_min_tail_weight then
                begin
                    Continue;
                end;

                prefix_unit_count_local := Length(previous_units_local) -
                    (tail_syllable_count - 1);
                if prefix_unit_count_local <= 0 then
                begin
                    Continue;
                end;
                overlap_matches_local := True;
                for overlap_idx_local := 0 to tail_syllable_count - 2 do
                begin
                    if not SameText(previous_units_local[
                        prefix_unit_count_local + overlap_idx_local],
                        tail_units_local[overlap_idx_local]) then
                    begin
                        overlap_matches_local := False;
                        Break;
                    end;
                end;
                if not overlap_matches_local then
                begin
                    Continue;
                end;

                prefix_text_local := '';
                for unit_idx_local := 0 to prefix_unit_count_local - 1 do
                begin
                    prefix_text_local := prefix_text_local +
                        previous_units_local[unit_idx_local];
                end;
                full_score_local := Max(0, previous_score_local) +
                    tail_weight_local + tail_syllable_count * 512;
                if (not Result) or (full_score_local > out_candidate.score) then
                begin
                    out_candidate.text := prefix_text_local + tail_text_local;
                    out_candidate.comment := '';
                    out_candidate.score := full_score_local;
                    out_candidate.source := cs_rule;
                    out_candidate.has_dict_weight := True;
                    out_candidate.dict_weight := full_score_local;
                    out_encoded_path := prefix_text_local +
                        c_segment_path_separator + tail_text_local;
                    Result := True;
                end;
            end;
        end;
    end;

    function candidate_list_has_complete_current_query_local(
        const candidates_local: TncCandidateList): Boolean;
    var
        candidate_idx_local: Integer;
    begin
        Result := False;
        for candidate_idx_local := 0 to High(candidates_local) do
        begin
            if (Trim(candidates_local[candidate_idx_local].comment) = '') and
                (get_candidate_text_unit_count(Trim(
                candidates_local[candidate_idx_local].text)) =
                input_syllable_count) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure append_current_leading_single_fallbacks_local(
        var candidates_local: TncCandidateList);
    var
        syllables_local: TncPinyinParseResult;
        single_results_local: TncCandidateList;
        first_key_local: string;
        remainder_key_local: string;
        single_idx_local: Integer;
        syllable_idx_local: Integer;
        duplicate_idx_local: Integer;
        single_candidate_local: TncCandidate;
        duplicate_found_local: Boolean;
    begin
        if (m_dictionary = nil) or (input_syllable_count < 2) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        first_key_local := normalize_pinyin_text(syllables_local[0].text);
        remainder_key_local := '';
        for syllable_idx_local := 1 to High(syllables_local) do
        begin
            remainder_key_local := remainder_key_local + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
        end;
        if (first_key_local = '') or (remainder_key_local = '') or
            (not lookup_exact_full_pinyin_cached_local(first_key_local,
            single_results_local)) then
        begin
            Exit;
        end;

        for single_idx_local := 0 to High(single_results_local) do
        begin
            if (Trim(single_results_local[single_idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(
                single_results_local[single_idx_local].text)) <> 1) then
            begin
                Continue;
            end;
            duplicate_found_local := False;
            for duplicate_idx_local := 0 to High(candidates_local) do
            begin
                if SameText(Trim(candidates_local[duplicate_idx_local].text),
                    Trim(single_results_local[single_idx_local].text)) and
                    SameText(normalize_pinyin_text(Trim(
                    candidates_local[duplicate_idx_local].comment)),
                    remainder_key_local) then
                begin
                    duplicate_found_local := True;
                    Break;
                end;
            end;
            if duplicate_found_local then
            begin
                Continue;
            end;

            single_candidate_local := single_results_local[single_idx_local];
            single_candidate_local.comment := remainder_key_local;
            SetLength(candidates_local, Length(candidates_local) + 1);
            candidates_local[High(candidates_local)] := single_candidate_local;
        end;
    end;

    function try_reuse_incremental_partial_tail_candidates_local(
        out out_candidates: TncCandidateList;
        const allow_complete_exact_tail: Boolean;
        const min_text_unit_count: Integer = 2): Boolean;
    const
        c_previous_probe_limit = 64;
    var
        current_syllables_local: TncPinyinParseResult;
        previous_syllables_local: TncPinyinParseResult;
        previous_idx_local: Integer;
        duplicate_idx_local: Integer;
        text_unit_count_local: Integer;
        previous_comment_unit_count_local: Integer;
        syllable_idx_local: Integer;
        previous_text_local: string;
        previous_comment_local: string;
        current_comment_local: string;
        candidate_local: TncCandidate;
        duplicate_found_local: Boolean;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (m_dictionary = nil) or (Length(previous_candidates) <= 0) or
            (previous_lookup_key = '') or
            (Length(lookup_text) <> Length(previous_lookup_key) + 1) or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or (input_syllable_count < 3) or
            has_explicit_apostrophe_input or all_initial_compact_query or
            has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') or
            ((not allow_complete_exact_tail) and
            (has_complete_exact_tail_phrase_local(2) or
            has_complete_exact_tail_phrase_local(3))) then
        begin
            Exit;
        end;

        current_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        previous_syllables_local := get_effective_compact_pinyin_syllables(
            previous_lookup_key);
        if (Length(current_syllables_local) < 3) or
            (Length(previous_syllables_local) < 2) or
            ((Length(current_syllables_local) <>
            Length(previous_syllables_local) + 1) and
            (Length(current_syllables_local) <>
            Length(previous_syllables_local))) then
        begin
            Exit;
        end;

        for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
            High(previous_candidates)) do
        begin
            previous_text_local := Trim(previous_candidates[previous_idx_local].text);
            previous_comment_local := normalize_pinyin_text(Trim(
                previous_candidates[previous_idx_local].comment));
            text_unit_count_local := get_candidate_text_unit_count(
                previous_text_local);
            if (previous_text_local = '') or
                (text_unit_count_local < min_text_unit_count) or
                (text_unit_count_local >= Length(current_syllables_local)) then
            begin
                Continue;
            end;

            if previous_comment_local <> '' then
            begin
                previous_comment_unit_count_local :=
                    get_effective_compact_pinyin_unit_count(
                    previous_comment_local);
                if (previous_comment_unit_count_local <= 0) or
                    (text_unit_count_local + previous_comment_unit_count_local <>
                    Length(previous_syllables_local)) then
                begin
                    Continue;
                end;
            end
            else if text_unit_count_local <> Length(previous_syllables_local) then
            begin
                Continue;
            end;

            current_comment_local := '';
            for syllable_idx_local := text_unit_count_local to
                High(current_syllables_local) do
            begin
                current_comment_local := current_comment_local +
                    normalize_pinyin_text(
                    current_syllables_local[syllable_idx_local].text);
            end;
            if (current_comment_local = '') or
                ((previous_comment_local <> '') and
                (not SameText(Copy(current_comment_local, 1,
                Length(previous_comment_local)), previous_comment_local))) then
            begin
                Continue;
            end;

            duplicate_found_local := False;
            for duplicate_idx_local := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[duplicate_idx_local].text),
                    previous_text_local) and
                    SameText(Trim(out_candidates[duplicate_idx_local].comment),
                    current_comment_local) then
                begin
                    duplicate_found_local := True;
                    Break;
                end;
            end;
            if duplicate_found_local then
            begin
                Continue;
            end;

            candidate_local := previous_candidates[previous_idx_local];
            candidate_local.text := previous_text_local;
            candidate_local.comment := current_comment_local;
            if not candidate_local.has_dict_weight then
            begin
                candidate_local.has_dict_weight := True;
                candidate_local.dict_weight := Max(300, candidate_local.score);
            end;
            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate_local;
        end;

        Result := Length(out_candidates) > 0;
    end;

    function try_preserve_short_lm_prefix_from_previous_local(
        out out_candidate: TncCandidate): Boolean;
    const
        c_previous_probe_limit = 12;
        c_preserved_generated_score = 16000;
    var
        current_syllables_local: TncPinyinParseResult;
        previous_syllables_local: TncPinyinParseResult;
        current_tail_local: string;
        previous_tail_local: string;
        current_four_tail_local: string;
        previous_four_tail_local: string;
        four_prefix_key_local: string;
        normalized_previous_key_local: string;
        previous_idx_local: Integer;
        syllable_idx_local: Integer;
        appending_local: Boolean;
        deleting_local: Boolean;

        function try_take_four_unit_candidate_local(
            const candidate_local: TncCandidate;
            const expected_tail_local: string;
            const allow_complete_local: Boolean): Boolean;
        var
            normalized_comment_local: string;
        begin
            Result := False;
            normalized_comment_local := normalize_pinyin_text(Trim(
                candidate_local.comment));
            if (candidate_local.display_kind <> cdk_lm_compound) or
                (get_candidate_text_unit_count(Trim(candidate_local.text)) <> 4)
            then
            begin
                Exit;
            end;
            if allow_complete_local then
            begin
                if normalized_comment_local <> '' then
                begin
                    Exit;
                end;
            end
            else if (normalized_comment_local = '') or
                (not SameText(normalized_comment_local, expected_tail_local)) then
            begin
                Exit;
            end;

            out_candidate := candidate_local;
            out_candidate.comment := current_four_tail_local;
            Result := True;
        end;

        function try_take_candidate_local(const candidate_local: TncCandidate;
            const expected_tail_local: string;
            const allow_complete_local: Boolean): Boolean;
        var
            normalized_comment_local: string;
        begin
            Result := False;
            normalized_comment_local := normalize_pinyin_text(Trim(
                candidate_local.comment));
            if (candidate_local.display_kind <> cdk_lm_compound) or
                (get_candidate_text_unit_count(Trim(candidate_local.text)) <> 3)
            then
            begin
                Exit;
            end;
            if allow_complete_local then
            begin
                if normalized_comment_local <> '' then
                begin
                    Exit;
                end;
            end
            else if (normalized_comment_local = '') or
                (not SameText(normalized_comment_local, expected_tail_local)) then
            begin
                Exit;
            end;

            out_candidate := candidate_local;
            out_candidate.comment := current_tail_local;
            Result := True;
        end;

        function previous_forced_candidate_matches_local(
            const expected_tail_local: string): Boolean;
        begin
            Result := m_has_forced_visible_top_candidate and
                SameText(m_forced_visible_top_lookup_key,
                previous_lookup_key) and
                try_take_candidate_local(m_forced_visible_top_candidate,
                expected_tail_local, False);
        end;

        function try_take_previous_supported_pair_local: Boolean;
        var
            accepted_text_local: string;
            candidate_idx_local: Integer;
            pair_idx_local: Integer;
        begin
            Result := False;
            if not SameText(previous_short_three_exact_pair_query,
                previous_lookup_key) then
            begin
                Exit;
            end;

            for pair_idx_local := 0 to
                High(previous_short_three_exact_pair_texts) do
            begin
                accepted_text_local := Trim(
                    previous_short_three_exact_pair_texts[pair_idx_local]);
                if get_candidate_text_unit_count(accepted_text_local) <> 3 then
                begin
                    Continue;
                end;

                for candidate_idx_local := 0 to High(previous_candidates) do
                begin
                    if (Trim(previous_candidates[candidate_idx_local].comment) = '') and
                        SameText(Trim(previous_candidates[candidate_idx_local].text),
                        accepted_text_local) then
                    begin
                        out_candidate := previous_candidates[candidate_idx_local];
                        out_candidate.comment := current_tail_local;
                        out_candidate.source := cs_rule;
                        out_candidate.display_kind := cdk_lm_compound;
                        Exit(True);
                    end;
                end;

                FillChar(out_candidate, SizeOf(out_candidate), 0);
                out_candidate.text := accepted_text_local;
                out_candidate.comment := current_tail_local;
                out_candidate.score := c_preserved_generated_score;
                out_candidate.source := cs_rule;
                out_candidate.display_kind := cdk_lm_compound;
                Exit(True);
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        appending_local := (Length(lookup_text) =
            Length(previous_lookup_key) + 1) and
            SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key);
        deleting_local := (Length(previous_lookup_key) =
            Length(lookup_text) + 1) and
            SameText(Copy(previous_lookup_key, 1, Length(lookup_text)),
            lookup_text);
        if (input_syllable_count < 4) or (input_syllable_count > 8) or
            ((Length(previous_candidates) = 0) and
            (Length(previous_visible_candidates) = 0) and
            (Length(previous_short_three_exact_pair_texts) = 0)) or
            (previous_lookup_key = '') or
            (not (appending_local or deleting_local)) or
            has_explicit_apostrophe_input or
            all_initial_compact_query or has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') then
        begin
            Exit;
        end;

        current_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        previous_syllables_local := get_effective_compact_pinyin_syllables(
            previous_lookup_key);
        if (Length(current_syllables_local) <> input_syllable_count) or
            (Length(previous_syllables_local) < 3) or
            (Length(previous_syllables_local) > 8) or
            ((Length(current_syllables_local) <>
            Length(previous_syllables_local) + 1) and
            (Length(current_syllables_local) <>
            Length(previous_syllables_local)) and
            (Length(previous_syllables_local) <>
            Length(current_syllables_local) + 1)) then
        begin
            Exit;
        end;

        // Preserve a validated 2+2 LM prefix while the user continues typing
        // its tail. The original three-unit preservation path cannot carry a
        // four-unit candidate such as 可以|取代 across d -> de.
        current_four_tail_local := '';
        if Length(current_syllables_local) > 4 then
        begin
            for syllable_idx_local := 4 to High(current_syllables_local) do
            begin
                current_four_tail_local := current_four_tail_local +
                    normalize_pinyin_text(
                    current_syllables_local[syllable_idx_local].text);
            end;
        end;
        if (current_four_tail_local <> '') and
            (Length(previous_syllables_local) >= 4) then
        begin
            four_prefix_key_local := '';
            for syllable_idx_local := 0 to 3 do
            begin
                four_prefix_key_local := four_prefix_key_local +
                    normalize_pinyin_text(
                    current_syllables_local[syllable_idx_local].text);
            end;
            previous_four_tail_local := '';
            if Length(previous_syllables_local) > 4 then
            begin
                for syllable_idx_local := 4 to High(previous_syllables_local) do
                begin
                    previous_four_tail_local := previous_four_tail_local +
                        normalize_pinyin_text(
                        previous_syllables_local[syllable_idx_local].text);
                end;
            end;
            normalized_previous_key_local := normalize_pinyin_text(
                previous_lookup_key);
            if (previous_four_tail_local = '') and
                (four_prefix_key_local <> '') and
                (Length(normalized_previous_key_local) >
                Length(four_prefix_key_local)) and
                SameText(Copy(normalized_previous_key_local, 1,
                Length(four_prefix_key_local)), four_prefix_key_local) then
            begin
                // A trailing initial such as the d in keyiqudaid is not a
                // parsed syllable yet, but it is already the visible comment.
                previous_four_tail_local := Copy(normalized_previous_key_local,
                    Length(four_prefix_key_local) + 1, MaxInt);
            end;

            if (Length(previous_syllables_local) = 4) and
                (previous_four_tail_local = '') and appending_local then
            begin
                for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                    High(previous_visible_candidates)) do
                begin
                    if try_take_four_unit_candidate_local(
                        previous_visible_candidates[previous_idx_local], '',
                        True) then
                    begin
                        Exit(True);
                    end;
                end;
                for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                    High(previous_candidates)) do
                begin
                    if try_take_four_unit_candidate_local(
                        previous_candidates[previous_idx_local], '', True) then
                    begin
                        Exit(True);
                    end;
                end;
            end
            else if (previous_four_tail_local <> '') and
                (not SameText(current_four_tail_local,
                previous_four_tail_local)) and
                ((appending_local and SameText(Copy(current_four_tail_local, 1,
                Length(previous_four_tail_local)),
                previous_four_tail_local)) or
                (deleting_local and SameText(Copy(previous_four_tail_local, 1,
                Length(current_four_tail_local)), current_four_tail_local))) then
            begin
                for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                    High(previous_visible_candidates)) do
                begin
                    if try_take_four_unit_candidate_local(
                        previous_visible_candidates[previous_idx_local],
                        previous_four_tail_local, False) then
                    begin
                        Exit(True);
                    end;
                end;
                for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                    High(previous_candidates)) do
                begin
                    if try_take_four_unit_candidate_local(
                        previous_candidates[previous_idx_local],
                        previous_four_tail_local, False) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        current_tail_local := '';
        for syllable_idx_local := 3 to High(current_syllables_local) do
        begin
            current_tail_local := current_tail_local + normalize_pinyin_text(
                current_syllables_local[syllable_idx_local].text);
        end;
        if current_tail_local = '' then
        begin
            Exit;
        end;

        if Length(previous_syllables_local) = 3 then
        begin
            if not appending_local then
            begin
                Exit;
            end;
            for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                High(previous_visible_candidates)) do
            begin
                if try_take_candidate_local(
                    previous_visible_candidates[previous_idx_local], '', True)
                then
                begin
                    Exit(True);
                end;
            end;
            for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
                High(previous_candidates)) do
            begin
                if try_take_candidate_local(
                    previous_candidates[previous_idx_local], '', True) then
                begin
                    Exit(True);
                end;
            end;
            Exit(try_take_previous_supported_pair_local);
        end;

        previous_tail_local := '';
        for syllable_idx_local := 3 to High(previous_syllables_local) do
        begin
            previous_tail_local := previous_tail_local + normalize_pinyin_text(
                previous_syllables_local[syllable_idx_local].text);
        end;
        if (previous_tail_local = '') or
            SameText(current_tail_local, previous_tail_local) or
            (appending_local and
            (not SameText(Copy(current_tail_local, 1,
            Length(previous_tail_local)), previous_tail_local))) or
            (deleting_local and
            (not SameText(Copy(previous_tail_local, 1,
            Length(current_tail_local)), current_tail_local))) then
        begin
            Exit;
        end;

        for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
            High(previous_visible_candidates)) do
        begin
            if try_take_candidate_local(
                previous_visible_candidates[previous_idx_local],
                previous_tail_local, False) then
            begin
                Exit(True);
            end;
        end;
        for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
            High(previous_candidates)) do
        begin
            if try_take_candidate_local(previous_candidates[previous_idx_local],
                previous_tail_local, False) then
            begin
                Exit(True);
            end;
        end;
        Result := previous_forced_candidate_matches_local(previous_tail_local);
    end;

    function try_reuse_long_incomplete_tail_candidates_local(
        out out_candidates: TncCandidateList): Boolean;
    const
        c_tail_exact_probe_max_syllables = 5;
    var
        current_syllables_local: TncPinyinParseResult;
        previous_syllables_local: TncPinyinParseResult;
        last_syllable_local: string;

        function has_any_complete_exact_tail_edge_local: Boolean;
        var
            span_len_local: Integer;
            start_idx_local: Integer;
            syllable_idx_local: Integer;
            result_idx_local: Integer;
            span_key_local: string;
            exact_results_local: TncCandidateList;
        begin
            Result := False;
            for span_len_local := 1 to Min(
                c_tail_exact_probe_max_syllables,
                Length(current_syllables_local)) do
            begin
                start_idx_local := Length(current_syllables_local) -
                    span_len_local;
                span_key_local := '';
                for syllable_idx_local := start_idx_local to
                    High(current_syllables_local) do
                begin
                    span_key_local := span_key_local + normalize_pinyin_text(
                        current_syllables_local[syllable_idx_local].text);
                end;
                if (span_key_local = '') or
                    (not dictionary_exact_lookup_cached(span_key_local,
                    exact_results_local)) then
                begin
                    Continue;
                end;

                for result_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[result_idx_local].comment) = '') and
                        (get_candidate_text_unit_count(Trim(
                        exact_results_local[result_idx_local].text)) =
                        span_len_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            if Length(current_syllables_local) <=
                c_tail_exact_probe_max_syllables then
            begin
                Exit;
            end;
            if not dictionary_exact_lookup_cached(lookup_text,
                exact_results_local) then
            begin
                Exit;
            end;
            for result_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[result_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(
                    exact_results_local[result_idx_local].text)) =
                    Length(current_syllables_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (input_syllable_count < c_long_sentence_full_path_min_syllables) or
            (input_syllable_count > 40) or
            (not has_incremental_composition_local) or
            has_explicit_apostrophe_input or all_initial_compact_query or
            has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') then
        begin
            Exit;
        end;

        current_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        previous_syllables_local := get_effective_compact_pinyin_syllables(
            previous_lookup_key);
        if (Length(current_syllables_local) <> input_syllable_count) or
            (Length(current_syllables_local) <>
            Length(previous_syllables_local)) then
        begin
            Exit;
        end;
        last_syllable_local := normalize_pinyin_text(
            current_syllables_local[High(current_syllables_local)].text);
        if Length(last_syllable_local) <= 1 then
        begin
            Exit;
        end;
        // Some parser-accepted forms have no exact lexical edge. Only those
        // are safe to carry forward; any exact tail edge must run full ranking.
        if is_full_pinyin_key(last_syllable_local) and
            ((not (m_dictionary is TncSqliteDictionary)) or
            has_any_complete_exact_tail_edge_local) then
        begin
            Exit;
        end;

        Result := try_reuse_incremental_partial_tail_candidates_local(
            out_candidates, True, 1);
    end;

    function try_reuse_incremental_raw_comment_candidates_local(
        out out_candidates: TncCandidateList;
        const min_text_unit_count: Integer): Boolean;
    const
        c_previous_probe_limit = 64;
    var
        previous_idx_local: Integer;
        duplicate_idx_local: Integer;
        previous_text_local: string;
        previous_comment_local: string;
        current_comment_local: string;
        candidate_local: TncCandidate;
        duplicate_found_local: Boolean;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (Length(previous_candidates) = 0) or
            (previous_lookup_key = '') or
            (Length(lookup_text) <> Length(previous_lookup_key) + 1) or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or has_explicit_apostrophe_input or
            all_initial_compact_query or has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') then
        begin
            Exit;
        end;

        for previous_idx_local := 0 to Min(c_previous_probe_limit - 1,
            High(previous_candidates)) do
        begin
            previous_text_local := Trim(
                previous_candidates[previous_idx_local].text);
            previous_comment_local := normalize_pinyin_text(Trim(
                previous_candidates[previous_idx_local].comment));
            if (previous_text_local = '') or
                (previous_comment_local = '') or
                (get_candidate_text_unit_count(previous_text_local) <
                min_text_unit_count) or
                (Length(previous_comment_local) > Length(previous_lookup_key)) or
                (not SameText(Copy(previous_lookup_key,
                Length(previous_lookup_key) - Length(previous_comment_local) + 1,
                Length(previous_comment_local)), previous_comment_local)) then
            begin
                Continue;
            end;

            current_comment_local := Copy(lookup_text,
                Length(lookup_text) - Length(previous_comment_local),
                Length(previous_comment_local) + 1);
            if current_comment_local = '' then
            begin
                Continue;
            end;

            duplicate_found_local := False;
            for duplicate_idx_local := 0 to High(out_candidates) do
            begin
                if SameText(Trim(out_candidates[duplicate_idx_local].text),
                    previous_text_local) and
                    SameText(normalize_pinyin_text(Trim(
                    out_candidates[duplicate_idx_local].comment)),
                    normalize_pinyin_text(current_comment_local)) then
                begin
                    duplicate_found_local := True;
                    Break;
                end;
            end;
            if duplicate_found_local then
            begin
                Continue;
            end;

            candidate_local := previous_candidates[previous_idx_local];
            candidate_local.text := previous_text_local;
            candidate_local.comment := current_comment_local;
            SetLength(out_candidates, Length(out_candidates) + 1);
            out_candidates[High(out_candidates)] := candidate_local;
        end;

        Result := Length(out_candidates) > 0;
    end;

    function try_reuse_stable_multi_prefix_before_lookup_local(
        out out_candidates: TncCandidateList): Boolean;
    var
        current_syllables_local: TncPinyinParseResult;
        last_syllable_local: string;
        first_syllable_local: string;
        second_syllable_local: string;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (input_syllable_count < 5) or (input_syllable_count > 8) or
            (Length(previous_candidates) <= 0) or
            (get_candidate_text_unit_count(Trim(
            previous_candidates[0].text)) < 3) or
            (not has_extendable_trailing_prefix_query_text(lookup_text)) then
        begin
            Exit;
        end;

        current_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(current_syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        first_syllable_local := normalize_pinyin_text(
            current_syllables_local[0].text);
        second_syllable_local := normalize_pinyin_text(
            current_syllables_local[1].text);
        if (not ((first_syllable_local = 'wo') or
            (first_syllable_local = 'ni') or
            (first_syllable_local = 'nin') or
            (first_syllable_local = 'ta') or
            (first_syllable_local = 'zan') or
            (first_syllable_local = 'an'))) or
            (not ((second_syllable_local = 'gen') or
            (second_syllable_local = 'he') or
            (second_syllable_local = 'yu') or
            (second_syllable_local = 'tong'))) then
        begin
            Exit;
        end;
        last_syllable_local := normalize_pinyin_text(
            current_syllables_local[High(current_syllables_local)].text);
        if Length(last_syllable_local) <> 1 then
        begin
            Exit;
        end;

        Result := try_reuse_incremental_partial_tail_candidates_local(
            out_candidates, False) and (Length(out_candidates) > 0) and
            (get_candidate_text_unit_count(Trim(out_candidates[0].text)) >= 3);
    end;

    function try_build_pronoun_connector_tail_incremental_fast_local(
        out out_candidates: TncCandidateList): Boolean;
    const
        c_stable_partial_margin = 2048;
    var
        candidate_idx_local: Integer;
        complete_idx_local: Integer;
        best_partial_idx_local: Integer;
        complete_score_local: Integer;
        best_partial_score_local: Integer;
        picked_local: TncCandidate;
        reused_previous_local: Boolean;
        syllables_local: TncPinyinParseResult;
        first_syllable_local: string;
        second_syllable_local: string;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (input_syllable_count < 4) or (input_syllable_count > 6) or
            (Length(previous_candidates) = 0) or
            (not is_full_pinyin_key(lookup_text)) or
            has_explicit_apostrophe_input or all_initial_compact_query or
            has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        first_syllable_local := normalize_pinyin_text(
            syllables_local[0].text);
        second_syllable_local := normalize_pinyin_text(
            syllables_local[1].text);
        if (not ((first_syllable_local = 'wo') or
            (first_syllable_local = 'ni') or
            (first_syllable_local = 'nin') or
            (first_syllable_local = 'ta') or
            (first_syllable_local = 'zan') or
            (first_syllable_local = 'an'))) or
            (not ((second_syllable_local = 'gen') or
            (second_syllable_local = 'he') or
            (second_syllable_local = 'yu') or
            (second_syllable_local = 'tong'))) then
        begin
            Exit;
        end;

        reused_previous_local :=
            try_reuse_incremental_partial_tail_candidates_local(
            out_candidates, True);
        if (not reused_previous_local) and
            (Length(previous_lookup_key) + 1 = Length(lookup_text)) and
            SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key) and
            (Trim(previous_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(
            previous_candidates[0].text)) = input_syllable_count) then
        begin
            SetLength(out_candidates, 1);
            out_candidates[0] := previous_candidates[0];
        end;
        if Length(out_candidates) = 0 then
        begin
            Exit;
        end;

        ensure_best_short_exact_prefix_tail_candidate_visible(out_candidates);
        complete_idx_local := -1;
        best_partial_idx_local := -1;
        complete_score_local := Low(Integer);
        best_partial_score_local := Low(Integer);
        for candidate_idx_local := 0 to High(out_candidates) do
        begin
            if (Trim(out_candidates[candidate_idx_local].comment) = '') and
                (get_candidate_text_unit_count(Trim(
                out_candidates[candidate_idx_local].text)) =
                input_syllable_count) then
            begin
                if (complete_idx_local < 0) or
                    (out_candidates[candidate_idx_local].score >
                    complete_score_local) then
                begin
                    complete_idx_local := candidate_idx_local;
                    complete_score_local :=
                        out_candidates[candidate_idx_local].score;
                end;
            end
            else if (Trim(out_candidates[candidate_idx_local].comment) <> '') and
                ((best_partial_idx_local < 0) or
                (out_candidates[candidate_idx_local].score >
                best_partial_score_local)) then
            begin
                best_partial_idx_local := candidate_idx_local;
                best_partial_score_local :=
                    out_candidates[candidate_idx_local].score;
            end;
        end;
        Result := complete_idx_local >= 0;
        if Result and (best_partial_idx_local > 0) and
            (best_partial_score_local >= complete_score_local +
            c_stable_partial_margin) then
        begin
            picked_local := out_candidates[best_partial_idx_local];
            for candidate_idx_local := best_partial_idx_local downto 1 do
            begin
                out_candidates[candidate_idx_local] :=
                    out_candidates[candidate_idx_local - 1];
            end;
            out_candidates[0] := picked_local;
        end;
        if Result then
        begin
            append_current_leading_single_fallbacks_local(out_candidates);
        end;
    end;

    function try_build_pronoun_connector_predictive_tail_fast_local(
        out out_candidates: TncCandidateList): Boolean;
    const
        c_role_probe_limit = 32;
        c_tail_probe_limit = 24;
        c_min_tail_weight = 300;
        c_predictive_complete_bonus = 24576;
    var
        syllables_local: TncPinyinParseResult;
        pronoun_results_local: TncCandidateList;
        connector_results_local: TncCandidateList;
        tail_results_local: TncCandidateList;
        pronoun_text_local: string;
        connector_text_local: string;
        tail_text_local: string;
        tail_key_local: string;
        candidate_text_local: string;
        candidate_path_local: string;
        pronoun_score_local: Integer;
        connector_score_local: Integer;
        tail_score_local: Integer;
        candidate_score_local: Integer;
        result_idx_local: Integer;
        picked_local: TncCandidate;

        function is_pronoun_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1, $4FFA:
                    Result := True;
            end;
        end;

        function is_connector_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $8DDF, $548C, $4E0E, $540C:
                    Result := True;
            end;
        end;

        function pick_role_local(const results_local: TncCandidateList;
            const require_pronoun_local: Boolean; out out_text_local: string;
            out out_score_local: Integer): Boolean;
        var
            role_idx_local: Integer;
            role_text_local: string;
            role_score_local: Integer;
        begin
            Result := False;
            out_text_local := '';
            out_score_local := Low(Integer);
            for role_idx_local := 0 to Min(c_role_probe_limit - 1,
                High(results_local)) do
            begin
                if Trim(results_local[role_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                role_text_local := Trim(results_local[role_idx_local].text);
                if (require_pronoun_local and
                    (not is_pronoun_local(role_text_local))) or
                    ((not require_pronoun_local) and
                    (not is_connector_local(role_text_local))) then
                begin
                    Continue;
                end;
                role_score_local := results_local[role_idx_local].score;
                if results_local[role_idx_local].has_dict_weight and
                    (results_local[role_idx_local].dict_weight >
                    role_score_local) then
                begin
                    role_score_local :=
                        results_local[role_idx_local].dict_weight;
                end;
                if (not Result) or (role_score_local > out_score_local) then
                begin
                    Result := True;
                    out_text_local := role_text_local;
                    out_score_local := role_score_local;
                end;
            end;
        end;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        if (m_dictionary = nil) or (input_syllable_count < 4) or
            (input_syllable_count > 6) or
            (Length(previous_candidates) = 0) or
            (Length(previous_lookup_key) + 1 <> Length(lookup_text)) or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or is_full_pinyin_key(lookup_text) or
            (not has_extendable_trailing_prefix_query_text(lookup_text)) or
            has_explicit_apostrophe_input or all_initial_compact_query or
            has_internal_dangling_initial or
            (m_last_lookup_normalized_from <> '') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;
        if (not lookup_exact_full_pinyin_cached_local(normalize_pinyin_text(
            syllables_local[0].text), pronoun_results_local)) or
            (not pick_role_local(pronoun_results_local, True,
            pronoun_text_local, pronoun_score_local)) or
            (not lookup_exact_full_pinyin_cached_local(normalize_pinyin_text(
            syllables_local[1].text), connector_results_local)) or
            (not pick_role_local(connector_results_local, False,
            connector_text_local, connector_score_local)) then
        begin
            Exit;
        end;

        tail_key_local := '';
        for result_idx_local := 2 to High(syllables_local) do
        begin
            tail_key_local := tail_key_local + normalize_pinyin_text(
                syllables_local[result_idx_local].text);
        end;
        if (tail_key_local = '') or
            (not dictionary_lookup_cached(tail_key_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        tail_text_local := '';
        tail_score_local := Low(Integer);
        for result_idx_local := 0 to Min(c_tail_probe_limit - 1,
            High(tail_results_local)) do
        begin
            candidate_text_local := Trim(
                tail_results_local[result_idx_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count - 2) then
            begin
                Continue;
            end;
            candidate_score_local := tail_results_local[result_idx_local].score;
            if tail_results_local[result_idx_local].has_dict_weight and
                (tail_results_local[result_idx_local].dict_weight >
                candidate_score_local) then
            begin
                candidate_score_local :=
                    tail_results_local[result_idx_local].dict_weight;
            end;
            if candidate_score_local < c_min_tail_weight then
            begin
                Continue;
            end;
            if (tail_text_local = '') or
                (candidate_score_local > tail_score_local) then
            begin
                tail_text_local := candidate_text_local;
                tail_score_local := candidate_score_local;
            end;
        end;
        if tail_text_local = '' then
        begin
            Exit;
        end;

        picked_local.text := pronoun_text_local + connector_text_local +
            tail_text_local;
        picked_local.comment := '';
        picked_local.score := c_predictive_complete_bonus +
            pronoun_score_local + connector_score_local + tail_score_local;
        picked_local.source := cs_rule;
        picked_local.has_dict_weight := False;
        picked_local.dict_weight := 0;
        candidate_path_local := pronoun_text_local +
            c_segment_path_separator + connector_text_local +
            c_segment_path_separator + tail_text_local;
        remember_segment_path_for_candidate(picked_local.text, '',
            candidate_path_local, picked_local.score);

        SetLength(out_candidates, 1);
        out_candidates[0] := picked_local;
        append_current_leading_single_fallbacks_local(out_candidates);
        Result := True;
    end;

    function try_build_stable_prefix_exact_tail_completion_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_tail_probe_limit = 8;
        c_max_tail_units = 4;
        c_max_rollback_units = 3;
        c_min_tail_weight = 560;
        c_place_direction_bonus = 12000;
    var
        prefix_candidate_local: TncCandidate;
        syllables_local: TncPinyinParseResult;
        unresolved_syllables_local: TncPinyinParseResult;
        prefix_units_local: TArray<string>;
        prefix_unit_count_local: Integer;
        unresolved_unit_count_local: Integer;
        rollback_units_local: Integer;
        suffix_unit_count_local: Integer;
        suffix_start_local: Integer;
        suffix_key_local: string;
        suffix_results_local: TncCandidateList;
        suffix_idx_local: Integer;
        suffix_text_local: string;
        suffix_weight_local: Integer;
        remaining_prefix_text_local: string;
        remaining_prefix_path_local: string;
        full_path_local: string;
        full_text_local: string;
        full_score_local: Integer;
        prefix_query_local: string;
        cached_prefix_path_local: string;
        inferred_prefix_path_local: string;
        inferred_prefix_score_local: Integer;
        inferred_prefix_segments_local: Integer;
        previous_unit_local: string;

        function build_query_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;
            for syllable_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[syllable_idx_local].text);
            end;
        end;

        function join_prefix_units_local(const count_local: Integer): string;
        var
            unit_idx_local: Integer;
        begin
            Result := '';
            for unit_idx_local := 0 to Min(count_local - 1,
                High(prefix_units_local)) do
            begin
                Result := Result + prefix_units_local[unit_idx_local];
            end;
        end;

        function trim_path_tail_units_local(const path_value_local: string;
            const remove_units_local: Integer;
            const fallback_text_local: string): string;
        var
            parts_local: TArray<string>;
            part_units_local: Integer;
            remaining_remove_local: Integer;
            last_idx_local: Integer;
            part_idx_local: Integer;
            part_text_local: string;
        begin
            Result := '';
            if remove_units_local <= 0 then
            begin
                Exit(path_value_local);
            end;
            parts_local := path_value_local.Split([c_segment_path_separator]);
            remaining_remove_local := remove_units_local;
            last_idx_local := High(parts_local);
            while (last_idx_local >= 0) and (remaining_remove_local > 0) do
            begin
                part_text_local := Trim(parts_local[last_idx_local]);
                part_units_local := get_candidate_text_unit_count(part_text_local);
                if (part_units_local <= 0) or
                    (part_units_local > remaining_remove_local) then
                begin
                    Exit(fallback_text_local);
                end;
                Dec(remaining_remove_local, part_units_local);
                Dec(last_idx_local);
            end;
            if remaining_remove_local <> 0 then
            begin
                Exit(fallback_text_local);
            end;
            for part_idx_local := 0 to last_idx_local do
            begin
                part_text_local := Trim(parts_local[part_idx_local]);
                if part_text_local = '' then
                begin
                    Continue;
                end;
                if Result <> '' then
                begin
                    Result := Result + c_segment_path_separator;
                end;
                Result := Result + part_text_local;
            end;
            if Result = '' then
            begin
                Result := fallback_text_local;
            end;
        end;

        function get_path_adjustment_local(const path_value_local: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (Trim(path_value_local) = '') then
            begin
                Exit;
            end;
            Inc(Result, get_cached_query_segment_path_bonus(lookup_text,
                path_value_local));
            Dec(Result, get_cached_query_segment_path_penalty(lookup_text,
                path_value_local));
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        cached_prefix_path_local := '';
        if (not try_build_ranked_path_prefix_continuation_candidate_local(
            prefix_candidate_local, cached_prefix_path_local)) or
            (Trim(prefix_candidate_local.comment) = '') then
        begin
            cached_prefix_path_local := '';
            if not get_cached_stable_prefix_candidate_local(
                prefix_candidate_local) then
            begin
                Exit;
            end;
        end;

        unresolved_syllables_local := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(Trim(prefix_candidate_local.comment)));
        unresolved_unit_count_local := Length(unresolved_syllables_local);
        if (unresolved_unit_count_local <= 0) or
            (unresolved_unit_count_local > c_max_tail_units) or
            (not is_full_pinyin_key(normalize_pinyin_text(
            Trim(prefix_candidate_local.comment)))) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        prefix_units_local := split_text_units(Trim(prefix_candidate_local.text));
        prefix_unit_count_local := Length(prefix_units_local);
        if (prefix_unit_count_local <= 0) or
            (prefix_unit_count_local + unresolved_unit_count_local <>
            Length(syllables_local)) then
        begin
            Exit;
        end;

        prefix_query_local := build_query_key_local(0,
            prefix_unit_count_local);
        if (Trim(cached_prefix_path_local) = '') or
            (get_encoded_path_segment_count_local(cached_prefix_path_local) <= 1) then
        begin
            if (m_session_ranked_query_paths <> nil) and
                m_session_ranked_query_paths.TryGetValue(prefix_query_local,
                inferred_prefix_path_local) and
                (get_encoded_path_segment_count_local(
                inferred_prefix_path_local) > 1) then
            begin
                cached_prefix_path_local := inferred_prefix_path_local;
            end;
        end;
        if (Trim(cached_prefix_path_local) = '') or
            (get_encoded_path_segment_count_local(cached_prefix_path_local) <= 1) then
        begin
            inferred_prefix_score_local := 0;
            inferred_prefix_segments_local := 0;
            inferred_prefix_path_local :=
                infer_segment_path_for_query_text_with_score(
                prefix_query_local, Trim(prefix_candidate_local.text),
                inferred_prefix_score_local,
                inferred_prefix_segments_local);
            if (Trim(inferred_prefix_path_local) <> '') and
                (inferred_prefix_segments_local > 1) then
            begin
                cached_prefix_path_local := inferred_prefix_path_local;
            end;
        end;
        if Trim(cached_prefix_path_local) = '' then
        begin
            cached_prefix_path_local := get_segment_path_for_candidate(
                prefix_candidate_local);
        end;
        if Trim(cached_prefix_path_local) = '' then
        begin
            cached_prefix_path_local := Trim(prefix_candidate_local.text);
        end;

        for rollback_units_local := 0 to Min(c_max_rollback_units,
            prefix_unit_count_local - 1) do
        begin
            suffix_unit_count_local := unresolved_unit_count_local +
                rollback_units_local;
            if suffix_unit_count_local > c_max_tail_units then
            begin
                Break;
            end;
            suffix_start_local := Length(syllables_local) -
                suffix_unit_count_local;
            suffix_key_local := build_query_key_local(suffix_start_local,
                suffix_unit_count_local);
            if (suffix_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(suffix_key_local,
                suffix_results_local)) then
            begin
                Continue;
            end;

            remaining_prefix_text_local := join_prefix_units_local(
                prefix_unit_count_local - rollback_units_local);
            remaining_prefix_path_local := trim_path_tail_units_local(
                cached_prefix_path_local, rollback_units_local,
                remaining_prefix_text_local);
            previous_unit_local := '';
            if prefix_unit_count_local - rollback_units_local > 0 then
            begin
                previous_unit_local := prefix_units_local[
                    prefix_unit_count_local - rollback_units_local - 1];
            end;

            for suffix_idx_local := 0 to Min(c_tail_probe_limit - 1,
                High(suffix_results_local)) do
            begin
                if Trim(suffix_results_local[suffix_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                suffix_text_local := Trim(
                    suffix_results_local[suffix_idx_local].text);
                if (suffix_text_local = '') or
                    (get_candidate_text_unit_count(suffix_text_local) <>
                    suffix_unit_count_local) then
                begin
                    Continue;
                end;
                suffix_weight_local := suffix_results_local[suffix_idx_local].score;
                if suffix_results_local[suffix_idx_local].has_dict_weight and
                    (suffix_results_local[suffix_idx_local].dict_weight >
                    suffix_weight_local) then
                begin
                    suffix_weight_local :=
                        suffix_results_local[suffix_idx_local].dict_weight;
                end;
                if suffix_weight_local < c_min_tail_weight then
                begin
                    Continue;
                end;

                full_text_local := remaining_prefix_text_local + suffix_text_local;
                if get_candidate_text_unit_count(full_text_local) <>
                    Length(syllables_local) then
                begin
                    Continue;
                end;
                full_path_local := remaining_prefix_path_local;
                if full_path_local <> '' then
                begin
                    full_path_local := full_path_local +
                        c_segment_path_separator;
                end;
                full_path_local := full_path_local + suffix_text_local;
                full_score_local := 64000 +
                    (prefix_unit_count_local - rollback_units_local) * 256 +
                    suffix_weight_local + suffix_unit_count_local * 512 +
                    get_path_adjustment_local(full_path_local);
                if is_place_context_tail_unit_local(previous_unit_local) and
                    (get_directional_locative_span_bonus_local(
                    suffix_text_local) > 0) then
                begin
                    Inc(full_score_local, c_place_direction_bonus);
                end;

                if (not Result) or (full_score_local > out_candidate.score) then
                begin
                    out_candidate.text := full_text_local;
                    out_candidate.comment := '';
                    out_candidate.score := full_score_local;
                    out_candidate.source := cs_rule;
                    out_candidate.has_dict_weight := True;
                    out_candidate.dict_weight := full_score_local;
                    out_encoded_path := full_path_local;
                    Result := True;
                end;
            end;
        end;
    end;

    function try_build_contextual_directional_tail_from_ranked_prefix_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_tail_probe_limit = 8;
        c_context_bonus = 12000;
    var
        syllables_local: TncPinyinParseResult;
        prefix_query_local: string;
        tail_query_local: string;
        prefix_path_local: string;
        path_parts_local: TArray<string>;
        prefix_text_local: string;
        prefix_units_local: TArray<string>;
        tail_results_local: TncCandidateList;
        tail_idx_local: Integer;
        tail_text_local: string;
        tail_weight_local: Integer;
        full_score_local: Integer;
        idx_local: Integer;
        prefix_candidates_local: TncCandidateList;
        raw_prefix_candidates_local: TncCandidateList;
        prefix_idx_local: Integer;
        prefix_score_local: Integer;
        best_prefix_score_local: Integer;

        function build_query_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            for syllable_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
            end;
        end;

    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (m_session_ranked_query_paths = nil) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;

        prefix_query_local := build_query_key_local(0,
            Length(syllables_local) - 2);
        tail_query_local := build_query_key_local(
            Length(syllables_local) - 2, 2);
        if (prefix_query_local = '') or (tail_query_local = '') or
            (not lookup_exact_full_pinyin_cached_local(tail_query_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        prefix_text_local := '';
        prefix_path_local := '';
        if m_session_ranked_query_paths.TryGetValue(prefix_query_local,
            prefix_path_local) and (Trim(prefix_path_local) <> '') then
        begin
            path_parts_local := prefix_path_local.Split(
                [c_segment_path_separator]);
            for idx_local := 0 to High(path_parts_local) do
            begin
                prefix_text_local := prefix_text_local +
                    Trim(path_parts_local[idx_local]);
            end;
        end
        else
        begin
            best_prefix_score_local := Low(Integer);
            SetLength(prefix_candidates_local, 0);
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                raw_prefix_candidates_local) then
            begin
                prefix_candidates_local := raw_prefix_candidates_local;
            end;
            if build_raw_leading_prefix_partial_candidates(lookup_text,
                raw_prefix_candidates_local) then
            begin
                prefix_candidates_local := merge_candidate_lists(
                    prefix_candidates_local, raw_prefix_candidates_local, 0);
            end;
            if Length(prefix_candidates_local) <= 0 then
            begin
                Exit;
            end;
            for prefix_idx_local := 0 to High(prefix_candidates_local) do
            begin
                if not SameText(normalize_pinyin_text(
                    Trim(prefix_candidates_local[prefix_idx_local].comment)),
                    tail_query_local) then
                begin
                    Continue;
                end;
                if get_candidate_text_unit_count(Trim(
                    prefix_candidates_local[prefix_idx_local].text)) <>
                    Length(syllables_local) - 2 then
                begin
                    Continue;
                end;
                prefix_score_local :=
                    prefix_candidates_local[prefix_idx_local].score;
                if prefix_candidates_local[prefix_idx_local].has_dict_weight and
                    (prefix_candidates_local[prefix_idx_local].dict_weight >
                    prefix_score_local) then
                begin
                    prefix_score_local :=
                        prefix_candidates_local[prefix_idx_local].dict_weight;
                end;
                if prefix_score_local > best_prefix_score_local then
                begin
                    best_prefix_score_local := prefix_score_local;
                    prefix_text_local := Trim(
                        prefix_candidates_local[prefix_idx_local].text);
                    prefix_path_local := get_segment_path_for_candidate(
                        prefix_candidates_local[prefix_idx_local]);
                end;
            end;
            if prefix_text_local = '' then
            begin
                Exit;
            end;
            if Trim(prefix_path_local) = '' then
            begin
                prefix_path_local := prefix_text_local;
            end;
        end;
        prefix_units_local := split_text_units(prefix_text_local);
        if (Length(prefix_units_local) <> Length(syllables_local) - 2) or
            (not is_place_context_tail_unit_local(
            prefix_units_local[High(prefix_units_local)])) then
        begin
            Exit;
        end;

        for tail_idx_local := 0 to Min(c_tail_probe_limit - 1,
            High(tail_results_local)) do
        begin
            if Trim(tail_results_local[tail_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            tail_text_local := Trim(tail_results_local[tail_idx_local].text);
            if (get_candidate_text_unit_count(tail_text_local) <> 2) or
                (get_directional_locative_span_bonus_local(tail_text_local) <= 0) then
            begin
                Continue;
            end;
            tail_weight_local := tail_results_local[tail_idx_local].score;
            if tail_results_local[tail_idx_local].has_dict_weight and
                (tail_results_local[tail_idx_local].dict_weight >
                tail_weight_local) then
            begin
                tail_weight_local := tail_results_local[tail_idx_local].dict_weight;
            end;
            full_score_local := 64000 + Length(prefix_units_local) * 256 +
                tail_weight_local + c_context_bonus;
            if (not Result) or (full_score_local > out_candidate.score) then
            begin
                out_candidate.text := prefix_text_local + tail_text_local;
                out_candidate.comment := '';
                out_candidate.score := full_score_local;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := full_score_local;
                out_encoded_path := prefix_path_local +
                    c_segment_path_separator + tail_text_local;
                Result := True;
            end;
        end;
    end;

    function try_build_contextual_directional_tail_from_previous_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_prefix_probe_limit = 32;
        c_tail_probe_limit = 8;
        c_context_bonus = 12000;
    var
        syllables_local: TncPinyinParseResult;
        tail_query_local: string;
        prefix_idx_local: Integer;
        prefix_text_local: string;
        prefix_units_local: TArray<string>;
        prefix_comment_local: string;
        prefix_score_local: Integer;
        resolved_prefix_text_local: string;
        consumed_directional_head_local: string;
        best_prefix_text_local: string;
        best_directional_head_local: string;
        best_prefix_score_local: Integer;
        tail_results_local: TncCandidateList;
        tail_idx_local: Integer;
        tail_text_local: string;
        tail_units_local: TArray<string>;
        tail_weight_local: Integer;
        full_score_local: Integer;
        syllable_idx_local: Integer;

        function directional_tail_starts_with_unit_local(
            const unit_value_local: string): Boolean;
        var
            result_idx_local: Integer;
            result_text_local: string;
            result_units_local: TArray<string>;
        begin
            Result := False;
            if unit_value_local = '' then
            begin
                Exit;
            end;
            for result_idx_local := 0 to Min(c_tail_probe_limit - 1,
                High(tail_results_local)) do
            begin
                if Trim(tail_results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                result_text_local := Trim(
                    tail_results_local[result_idx_local].text);
                result_units_local := split_text_units(result_text_local);
                if (Length(result_units_local) = 2) and
                    SameText(result_units_local[0], unit_value_local) and
                    (get_directional_locative_span_bonus_local(
                    result_text_local) > 0) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function take_prefix_units_local(const units_local: TArray<string>;
            const unit_count_local: Integer): string;
        var
            unit_idx_local: Integer;
        begin
            Result := '';
            for unit_idx_local := 0 to Min(unit_count_local,
                Length(units_local)) - 1 do
            begin
                Result := Result + units_local[unit_idx_local];
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (Length(previous_candidates) <= 0) or
            (previous_lookup_key = '') or
            (not SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
            previous_lookup_key)) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;
        tail_query_local := '';
        for syllable_idx_local := Length(syllables_local) - 2 to
            High(syllables_local) do
        begin
            tail_query_local := tail_query_local + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
        end;
        if (tail_query_local = '') or
            (not lookup_exact_full_pinyin_cached_local(tail_query_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        best_prefix_text_local := '';
        best_directional_head_local := '';
        best_prefix_score_local := Low(Integer);
        for prefix_idx_local := 0 to Min(c_prefix_probe_limit - 1,
            High(previous_candidates)) do
        begin
            prefix_text_local := Trim(previous_candidates[prefix_idx_local].text);
            prefix_comment_local := normalize_pinyin_text(
                Trim(previous_candidates[prefix_idx_local].comment));
            prefix_units_local := split_text_units(prefix_text_local);
            if (prefix_text_local = '') or (prefix_comment_local = '') then
            begin
                Continue;
            end;
            resolved_prefix_text_local := '';
            consumed_directional_head_local := '';
            if (Length(prefix_units_local) = Length(syllables_local) - 2) and
                is_place_context_tail_unit_local(
                prefix_units_local[High(prefix_units_local)]) and
                (Copy(tail_query_local, 1, Length(prefix_comment_local)) =
                prefix_comment_local) then
            begin
                resolved_prefix_text_local := prefix_text_local;
            end
            else if (Length(prefix_units_local) = Length(syllables_local) - 1) and
                (Length(prefix_units_local) >= 2) and
                is_place_context_tail_unit_local(
                prefix_units_local[High(prefix_units_local) - 1]) and
                directional_tail_starts_with_unit_local(
                prefix_units_local[High(prefix_units_local)]) and
                (Copy(normalize_pinyin_text(
                syllables_local[High(syllables_local)].text), 1,
                Length(prefix_comment_local)) = prefix_comment_local) then
            begin
                consumed_directional_head_local :=
                    prefix_units_local[High(prefix_units_local)];
                resolved_prefix_text_local := take_prefix_units_local(
                    prefix_units_local, Length(prefix_units_local) - 1);
            end;
            if resolved_prefix_text_local = '' then
            begin
                Continue;
            end;
            prefix_score_local := previous_candidates[prefix_idx_local].score;
            if previous_candidates[prefix_idx_local].has_dict_weight and
                (previous_candidates[prefix_idx_local].dict_weight >
                prefix_score_local) then
            begin
                prefix_score_local :=
                    previous_candidates[prefix_idx_local].dict_weight;
            end;
            if prefix_score_local > best_prefix_score_local then
            begin
                best_prefix_score_local := prefix_score_local;
                best_prefix_text_local := resolved_prefix_text_local;
                best_directional_head_local :=
                    consumed_directional_head_local;
            end;
        end;
        if best_prefix_text_local = '' then
        begin
            Exit;
        end;

        for tail_idx_local := 0 to Min(c_tail_probe_limit - 1,
            High(tail_results_local)) do
        begin
            if Trim(tail_results_local[tail_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            tail_text_local := Trim(tail_results_local[tail_idx_local].text);
            tail_units_local := split_text_units(tail_text_local);
            if (Length(tail_units_local) <> 2) or
                (get_directional_locative_span_bonus_local(tail_text_local) <= 0) then
            begin
                Continue;
            end;
            if (best_directional_head_local <> '') and
                (not SameText(tail_units_local[0],
                best_directional_head_local)) then
            begin
                Continue;
            end;
            tail_weight_local := tail_results_local[tail_idx_local].score;
            if tail_results_local[tail_idx_local].has_dict_weight and
                (tail_results_local[tail_idx_local].dict_weight >
                tail_weight_local) then
            begin
                tail_weight_local := tail_results_local[tail_idx_local].dict_weight;
            end;
            full_score_local := Max(0, best_prefix_score_local) +
                tail_weight_local + c_context_bonus;
            if (not Result) or (full_score_local > out_candidate.score) then
            begin
                out_candidate.text := best_prefix_text_local + tail_text_local;
                out_candidate.comment := '';
                out_candidate.score := full_score_local;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := full_score_local;
                out_encoded_path := best_prefix_text_local +
                    c_segment_path_separator + tail_text_local;
                Result := True;
            end;
        end;
    end;

    function try_complete_contextual_directional_partial_local(
        const partial_candidate: TncCandidate; const partial_encoded_path: string;
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_tail_probe_limit = 8;
        c_context_bonus = 12000;
    var
        syllables_local: TncPinyinParseResult;
        tail_query_local: string;
        partial_text_local: string;
        partial_comment_local: string;
        partial_units_local: TArray<string>;
        tail_results_local: TncCandidateList;
        tail_idx_local: Integer;
        tail_text_local: string;
        tail_weight_local: Integer;
        prefix_score_local: Integer;
        syllable_idx_local: Integer;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        partial_text_local := Trim(partial_candidate.text);
        partial_comment_local := normalize_pinyin_text(
            Trim(partial_candidate.comment));
        if (partial_text_local = '') or (partial_comment_local = '') then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) < 3 then
        begin
            Exit;
        end;
        tail_query_local := '';
        for syllable_idx_local := Length(syllables_local) - 2 to
            High(syllables_local) do
        begin
            tail_query_local := tail_query_local + normalize_pinyin_text(
                syllables_local[syllable_idx_local].text);
        end;
        partial_units_local := split_text_units(partial_text_local);
        if (tail_query_local = '') or
            (not SameText(partial_comment_local, tail_query_local)) or
            (Length(partial_units_local) <> Length(syllables_local) - 2) or
            (not is_place_context_tail_unit_local(
            partial_units_local[High(partial_units_local)])) or
            (not lookup_exact_full_pinyin_cached_local(tail_query_local,
            tail_results_local)) then
        begin
            Exit;
        end;

        prefix_score_local := partial_candidate.score;
        if partial_candidate.has_dict_weight and
            (partial_candidate.dict_weight > prefix_score_local) then
        begin
            prefix_score_local := partial_candidate.dict_weight;
        end;
        for tail_idx_local := 0 to Min(c_tail_probe_limit - 1,
            High(tail_results_local)) do
        begin
            if Trim(tail_results_local[tail_idx_local].comment) <> '' then
            begin
                Continue;
            end;
            tail_text_local := Trim(tail_results_local[tail_idx_local].text);
            if (get_candidate_text_unit_count(tail_text_local) <> 2) or
                (get_directional_locative_span_bonus_local(tail_text_local) <= 0) then
            begin
                Continue;
            end;
            tail_weight_local := tail_results_local[tail_idx_local].score;
            if tail_results_local[tail_idx_local].has_dict_weight and
                (tail_results_local[tail_idx_local].dict_weight >
                tail_weight_local) then
            begin
                tail_weight_local :=
                    tail_results_local[tail_idx_local].dict_weight;
            end;
            if (not Result) or
                (tail_weight_local > out_candidate.dict_weight) then
            begin
                out_candidate.text := partial_text_local + tail_text_local;
                out_candidate.comment := '';
                out_candidate.score := Max(0, prefix_score_local) +
                    tail_weight_local + c_context_bonus;
                out_candidate.source := cs_rule;
                out_candidate.has_dict_weight := True;
                out_candidate.dict_weight := tail_weight_local;
                out_encoded_path := Trim(partial_encoded_path);
                if out_encoded_path = '' then
                begin
                    out_encoded_path := partial_text_local;
                end;
                out_encoded_path := out_encoded_path +
                    c_segment_path_separator + tail_text_local;
                Result := True;
            end;
        end;
    end;

    function ranked_path_requires_exact_competition_validation_local(
        const stable_text: string; const encoded_path: string): Boolean;
    const
        c_exact_probe_limit = 8;
        c_crossing_exact_min_weight = 560;
        c_max_crossing_span_units = 4;
        c_close_exact_min_weight = 700;
        c_close_exact_raw_gap = 96;
    var
        syllables_local: TncPinyinParseResult;
        path_parts_local: TArray<string>;
        path_starts_local: TArray<Integer>;
        path_lengths_local: TArray<Integer>;
        stable_units_local: TArray<string>;
        total_units_local: Integer;
        part_idx_local: Integer;
        part_text_local: string;
        part_units_local: TArray<string>;
        part_key_local: string;
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
        alternative_path_local: string;
        stable_path_adjustment_local: Integer;
        alternative_path_adjustment_local: Integer;
        previous_unit_local: string;
        span_start_local: Integer;
        span_len_local: Integer;
        span_text_local: string;
        unit_idx_local: Integer;
        refine_part_idx_local: Integer;
        boundary_mask_local: Integer;
        max_boundary_mask_local: Integer;
        stable_part_weight_local: Integer;
        alternative_weight_local: Integer;
        crossing_breaks_strong_pair_local: Boolean;

        function build_query_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;
            for idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[idx_local].text);
            end;
        end;

        function get_path_adjustment_local(const path_value_local: string): Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (Trim(path_value_local) = '') then
            begin
                Exit;
            end;
            Inc(Result, get_cached_query_segment_path_bonus(lookup_text,
                path_value_local));
            Dec(Result, get_cached_query_segment_path_penalty(lookup_text,
                path_value_local));
        end;

        function build_replaced_path_local(const replace_idx_local: Integer;
            const replacement_text_local: string): string;
        var
            idx_local: Integer;
            segment_local: string;
        begin
            Result := '';
            for idx_local := 0 to High(path_parts_local) do
            begin
                if idx_local = replace_idx_local then
                begin
                    segment_local := replacement_text_local;
                end
                else
                begin
                    segment_local := Trim(path_parts_local[idx_local]);
                end;
                if segment_local = '' then
                begin
                    Continue;
                end;
                if Result <> '' then
                begin
                    Result := Result + c_segment_path_separator;
                end;
                Result := Result + segment_local;
            end;
        end;

        function build_replaced_path_with_refined_part_local(
            const replace_idx_local: Integer;
            const replacement_text_local: string;
            const refine_idx_local: Integer;
            const boundary_mask_value_local: Integer): string;
        var
            idx_local: Integer;
            segment_local: string;
            refined_units_local: TArray<string>;
            unit_idx_value_local: Integer;

            procedure append_segment_local(const value_local: string);
            var
                normalized_value_local: string;
            begin
                normalized_value_local := Trim(value_local);
                if normalized_value_local = '' then
                begin
                    Exit;
                end;
                if Result <> '' then
                begin
                    Result := Result + c_segment_path_separator;
                end;
                Result := Result + normalized_value_local;
            end;
        begin
            Result := '';
            if (replace_idx_local < 0) or
                (replace_idx_local > High(path_parts_local)) or
                (refine_idx_local < 0) or
                (refine_idx_local >= replace_idx_local) then
            begin
                Exit;
            end;
            refined_units_local := split_text_units(
                Trim(path_parts_local[refine_idx_local]));
            if (Length(refined_units_local) < 2) or
                (Length(refined_units_local) > 6) or
                (boundary_mask_value_local <= 0) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(path_parts_local) do
            begin
                if idx_local = refine_idx_local then
                begin
                    segment_local := '';
                    for unit_idx_value_local := 0 to
                        High(refined_units_local) do
                    begin
                        segment_local := segment_local +
                            refined_units_local[unit_idx_value_local];
                        if (unit_idx_value_local <
                            High(refined_units_local)) and
                            ((boundary_mask_value_local and
                            (1 shl unit_idx_value_local)) <> 0) then
                        begin
                            append_segment_local(segment_local);
                            segment_local := '';
                        end;
                    end;
                    append_segment_local(segment_local);
                end
                else if idx_local = replace_idx_local then
                begin
                    append_segment_local(replacement_text_local);
                end
                else
                begin
                    append_segment_local(path_parts_local[idx_local]);
                end;
            end;
        end;

        function span_crosses_path_boundary_local(const start_idx_local: Integer;
            const count_local: Integer): Boolean;
        var
            idx_local: Integer;
            boundary_local: Integer;
            end_idx_local: Integer;
        begin
            Result := False;
            end_idx_local := start_idx_local + count_local;
            for idx_local := 1 to High(path_starts_local) do
            begin
                boundary_local := path_starts_local[idx_local];
                if (boundary_local > start_idx_local) and
                    (boundary_local < end_idx_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function exact_candidate_is_reliable_local(
            const query_key_local: string; const candidate_local: TncCandidate;
            const expected_units_local: Integer): Boolean;
        var
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            if Trim(candidate_local.comment) <> '' then
            begin
                Exit;
            end;
            candidate_text_local := Trim(candidate_local.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units_local) then
            begin
                Exit;
            end;
            candidate_weight_local := candidate_local.score;
            if candidate_local.has_dict_weight and
                (candidate_local.dict_weight > candidate_weight_local) then
            begin
                candidate_weight_local := candidate_local.dict_weight;
            end;
            Inc(candidate_weight_local, get_preferred_query_phrase_bonus_local(
                query_key_local, candidate_text_local));
            Result := candidate_weight_local >= c_crossing_exact_min_weight;
        end;

        function get_path_part_exact_weight_local(
            const part_idx_value_local: Integer): Integer;
        var
            query_key_local: string;
            results_local: TncCandidateList;
            result_idx_value_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := 0;
            if (part_idx_value_local < 0) or
                (part_idx_value_local > High(path_parts_local)) then
            begin
                Exit;
            end;
            query_key_local := build_query_key_local(
                path_starts_local[part_idx_value_local],
                path_lengths_local[part_idx_value_local]);
            if (query_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key_local,
                results_local)) then
            begin
                Exit;
            end;
            for result_idx_value_local := 0 to Min(c_exact_probe_limit - 1,
                High(results_local)) do
            begin
                if Trim(results_local[result_idx_value_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    results_local[result_idx_value_local].text);
                if not SameText(candidate_text_local,
                    Trim(path_parts_local[part_idx_value_local])) then
                begin
                    Continue;
                end;
                candidate_weight_local :=
                    results_local[result_idx_value_local].score;
                if results_local[result_idx_value_local].has_dict_weight and
                    (results_local[result_idx_value_local].dict_weight >
                    candidate_weight_local) then
                begin
                    candidate_weight_local :=
                        results_local[result_idx_value_local].dict_weight;
                end;
                Exit(candidate_weight_local);
            end;
        end;

        function get_path_part_exact_raw_weight_local(
            const part_idx_value_local: Integer): Integer;
        var
            query_key_local: string;
            results_local: TncCandidateList;
            result_idx_value_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := 0;
            if (part_idx_value_local < 0) or
                (part_idx_value_local > High(path_parts_local)) then
            begin
                Exit;
            end;
            query_key_local := build_query_key_local(
                path_starts_local[part_idx_value_local],
                path_lengths_local[part_idx_value_local]);
            if (query_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key_local,
                results_local)) then
            begin
                Exit;
            end;
            for result_idx_value_local := 0 to Min(c_exact_probe_limit - 1,
                High(results_local)) do
            begin
                if Trim(results_local[result_idx_value_local].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    results_local[result_idx_value_local].text);
                if not SameText(candidate_text_local,
                    Trim(path_parts_local[part_idx_value_local])) then
                begin
                    Continue;
                end;
                candidate_weight_local :=
                    results_local[result_idx_value_local].score;
                if results_local[result_idx_value_local].has_dict_weight and
                    (results_local[result_idx_value_local].dict_weight > 0) then
                begin
                    candidate_weight_local :=
                        results_local[result_idx_value_local].dict_weight;
                end;
                Exit(candidate_weight_local);
            end;
        end;

        function crossing_span_breaks_strong_word_pair_local(
            const start_idx_local: Integer; const count_local: Integer): Boolean;
        const
            c_strong_part_weight = 700;
        var
            part_idx_value_local: Integer;
            boundary_local: Integer;
            end_idx_local: Integer;
        begin
            Result := False;
            end_idx_local := start_idx_local + count_local;
            for part_idx_value_local := 1 to High(path_parts_local) do
            begin
                boundary_local := path_starts_local[part_idx_value_local];
                if (boundary_local <= start_idx_local) or
                    (boundary_local >= end_idx_local) then
                begin
                    Continue;
                end;
                if (path_lengths_local[part_idx_value_local - 1] >= 2) and
                    (path_lengths_local[part_idx_value_local] >= 2) and
                    (get_path_part_exact_weight_local(
                    part_idx_value_local - 1) >= c_strong_part_weight) and
                    (get_path_part_exact_weight_local(
                    part_idx_value_local) >= c_strong_part_weight) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (Trim(stable_text) = '') or
            (Trim(encoded_path) = '') or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        stable_units_local := split_text_units(Trim(stable_text));
        if (Length(syllables_local) < 2) or
            (Length(stable_units_local) <> Length(syllables_local)) then
        begin
            Exit;
        end;

        path_parts_local := encoded_path.Split([c_segment_path_separator]);
        SetLength(path_starts_local, Length(path_parts_local));
        SetLength(path_lengths_local, Length(path_parts_local));
        total_units_local := 0;
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            part_text_local := Trim(path_parts_local[part_idx_local]);
            part_units_local := split_text_units(part_text_local);
            if (part_text_local = '') or (Length(part_units_local) <= 0) then
            begin
                Exit;
            end;
            path_starts_local[part_idx_local] := total_units_local;
            path_lengths_local[part_idx_local] := Length(part_units_local);
            Inc(total_units_local, Length(part_units_local));
        end;
        if total_units_local <> Length(syllables_local) then
        begin
            Exit;
        end;

        stable_path_adjustment_local := get_path_adjustment_local(encoded_path);
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            part_text_local := Trim(path_parts_local[part_idx_local]);
            part_key_local := build_query_key_local(
                path_starts_local[part_idx_local],
                path_lengths_local[part_idx_local]);
            if (part_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(part_key_local,
                exact_results_local)) then
            begin
                Continue;
            end;
            previous_unit_local := '';
            if path_starts_local[part_idx_local] > 0 then
            begin
                previous_unit_local := stable_units_local[
                    path_starts_local[part_idx_local] - 1];
            end;
            for exact_idx_local := 0 to Min(c_exact_probe_limit - 1,
                High(exact_results_local)) do
            begin
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    (exact_text_local = '') or
                    SameText(exact_text_local, part_text_local) or
                    (get_candidate_text_unit_count(exact_text_local) <>
                    path_lengths_local[part_idx_local]) then
                begin
                    Continue;
                end;

                stable_part_weight_local :=
                    get_path_part_exact_raw_weight_local(part_idx_local);
                alternative_weight_local :=
                    exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight > 0) then
                begin
                    alternative_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end;
                if (path_lengths_local[part_idx_local] <= 3) and
                    (stable_part_weight_local >= c_close_exact_min_weight) and
                    (alternative_weight_local >= c_close_exact_min_weight) and
                    (Abs(stable_part_weight_local - alternative_weight_local) <=
                    c_close_exact_raw_gap) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info + Format(
                            ' rvclose=[part=%d from=%s to=%s gap=%d]',
                            [part_idx_local, part_text_local, exact_text_local,
                            Abs(stable_part_weight_local -
                            alternative_weight_local)]);
                    end;
                    Exit(True);
                end;

                alternative_path_local := build_replaced_path_local(
                    part_idx_local, exact_text_local);
                alternative_path_adjustment_local := get_path_adjustment_local(
                    alternative_path_local);
                if alternative_path_adjustment_local >
                    stable_path_adjustment_local then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info + Format(
                            ' rvpath=[part=%d from=%s to=%s delta=%d]',
                            [part_idx_local, part_text_local, exact_text_local,
                            alternative_path_adjustment_local -
                            stable_path_adjustment_local]);
                    end;
                    Exit(True);
                end;

                if part_idx_local > 0 then
                begin
                    for refine_part_idx_local := 0 to
                        part_idx_local - 1 do
                    begin
                        if (path_lengths_local[refine_part_idx_local] < 2) or
                            (path_lengths_local[refine_part_idx_local] > 6) then
                        begin
                            Continue;
                        end;
                        max_boundary_mask_local :=
                            (1 shl (path_lengths_local[
                            refine_part_idx_local] - 1)) - 1;
                        for boundary_mask_local := 1 to
                            max_boundary_mask_local do
                        begin
                            alternative_path_local :=
                                build_replaced_path_with_refined_part_local(
                                part_idx_local, exact_text_local,
                                refine_part_idx_local,
                                boundary_mask_local);
                            alternative_path_adjustment_local :=
                                get_path_adjustment_local(
                                alternative_path_local);
                            if alternative_path_adjustment_local >
                                stable_path_adjustment_local then
                            begin
                                if m_config.debug_mode then
                                begin
                                    m_last_full_path_debug_info :=
                                        m_last_full_path_debug_info + Format(
                                        ' rvpathrefine=[part=%d refine=%d mask=%d from=%s to=%s delta=%d]',
                                        [part_idx_local,
                                        refine_part_idx_local,
                                        boundary_mask_local,
                                        part_text_local, exact_text_local,
                                        alternative_path_adjustment_local -
                                        stable_path_adjustment_local]);
                                end;
                                Exit(True);
                            end;
                        end;
                    end;
                end;

                if (path_lengths_local[part_idx_local] = 2) and
                    is_place_context_tail_unit_local(previous_unit_local) and
                    (get_directional_locative_span_bonus_local(
                    exact_text_local) > 0) and
                    (get_directional_locative_span_bonus_local(
                    part_text_local) <= 0) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info + Format(
                            ' rvdir=[part=%d from=%s to=%s]',
                            [part_idx_local, part_text_local, exact_text_local]);
                    end;
                    Exit(True);
                end;
            end;
        end;

        if Length(syllables_local) > 8 then
        begin
            Exit;
        end;
        for span_start_local := 0 to Length(syllables_local) - 2 do
        begin
            for span_len_local := 2 to Min(c_max_crossing_span_units,
                Length(syllables_local) - span_start_local) do
            begin
                if span_start_local + span_len_local >
                    Length(syllables_local) - 2 then
                begin
                    Continue;
                end;
                if not span_crosses_path_boundary_local(span_start_local,
                    span_len_local) then
                begin
                    Continue;
                end;
                crossing_breaks_strong_pair_local :=
                    crossing_span_breaks_strong_word_pair_local(
                    span_start_local, span_len_local);
                if crossing_breaks_strong_pair_local and
                    ((span_start_local <> 0) or (span_len_local < 3)) then
                begin
                    Continue;
                end;

                part_key_local := build_query_key_local(span_start_local,
                    span_len_local);
                if (part_key_local = '') or
                    (not lookup_exact_full_pinyin_cached_local(part_key_local,
                    exact_results_local)) then
                begin
                    Continue;
                end;
                span_text_local := '';
                for unit_idx_local := span_start_local to
                    span_start_local + span_len_local - 1 do
                begin
                    span_text_local := span_text_local +
                        stable_units_local[unit_idx_local];
                end;
                for exact_idx_local := 0 to Min(c_exact_probe_limit - 1,
                    High(exact_results_local)) do
                begin
                    exact_text_local := Trim(
                        exact_results_local[exact_idx_local].text);
                    if (not SameText(exact_text_local, span_text_local)) and
                        exact_candidate_is_reliable_local(part_key_local,
                        exact_results_local[exact_idx_local], span_len_local) and
                        ((not crossing_breaks_strong_pair_local) or
                        (exact_results_local[exact_idx_local].score >= 900) or
                        (exact_results_local[exact_idx_local].has_dict_weight and
                        (exact_results_local[exact_idx_local].dict_weight >=
                        900))) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info + Format(
                                ' rvcross=[start=%d len=%d key=%s from=%s to=%s]',
                                [span_start_local, span_len_local,
                                part_key_local, span_text_local,
                                exact_text_local]);
                        end;
                        Exit(True);
                    end;
                end;
            end;
        end;
    end;

    function try_repair_ranked_directional_context_local(
        const stable_candidate: TncCandidate; const encoded_path: string;
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_exact_probe_limit = 8;
        c_directional_context_bonus = 12000;
    var
        syllables_local: TncPinyinParseResult;
        stable_units_local: TArray<string>;
        path_parts_local: TArray<string>;
        path_starts_local: TArray<Integer>;
        path_lengths_local: TArray<Integer>;
        total_units_local: Integer;
        part_idx_local: Integer;
        part_units_local: TArray<string>;
        part_text_local: string;
        query_key_local: string;
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
        exact_units_local: TArray<string>;
        exact_weight_local: Integer;
        best_weight_local: Integer;
        best_part_idx_local: Integer;
        best_text_local: string;
        unit_idx_local: Integer;

        function build_query_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            syllable_idx_local: Integer;
        begin
            Result := '';
            for syllable_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[syllable_idx_local].text);
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        if (m_dictionary = nil) or (Trim(stable_candidate.comment) <> '') or
            (Trim(stable_candidate.text) = '') or (Trim(encoded_path) = '') or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        stable_units_local := split_text_units(Trim(stable_candidate.text));
        if Length(stable_units_local) <> Length(syllables_local) then
        begin
            Exit;
        end;

        path_parts_local := encoded_path.Split([c_segment_path_separator]);
        SetLength(path_starts_local, Length(path_parts_local));
        SetLength(path_lengths_local, Length(path_parts_local));
        total_units_local := 0;
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            part_text_local := Trim(path_parts_local[part_idx_local]);
            part_units_local := split_text_units(part_text_local);
            if (part_text_local = '') or (Length(part_units_local) <= 0) then
            begin
                Exit;
            end;
            path_starts_local[part_idx_local] := total_units_local;
            path_lengths_local[part_idx_local] := Length(part_units_local);
            Inc(total_units_local, Length(part_units_local));
        end;
        if total_units_local <> Length(syllables_local) then
        begin
            Exit;
        end;

        best_weight_local := Low(Integer);
        best_part_idx_local := -1;
        best_text_local := '';
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            if (path_lengths_local[part_idx_local] <> 2) or
                (path_starts_local[part_idx_local] <= 0) or
                (not is_place_context_tail_unit_local(stable_units_local[
                path_starts_local[part_idx_local] - 1])) or
                (get_directional_locative_span_bonus_local(
                Trim(path_parts_local[part_idx_local])) > 0) then
            begin
                Continue;
            end;
            query_key_local := build_query_key_local(
                path_starts_local[part_idx_local], 2);
            if (query_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(query_key_local,
                exact_results_local)) then
            begin
                Continue;
            end;
            for exact_idx_local := 0 to Min(c_exact_probe_limit - 1,
                High(exact_results_local)) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (get_candidate_text_unit_count(exact_text_local) <> 2) or
                    (get_directional_locative_span_bonus_local(
                    exact_text_local) <= 0) then
                begin
                    Continue;
                end;
                exact_weight_local := exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight >
                    exact_weight_local) then
                begin
                    exact_weight_local :=
                        exact_results_local[exact_idx_local].dict_weight;
                end;
                if exact_weight_local > best_weight_local then
                begin
                    best_weight_local := exact_weight_local;
                    best_part_idx_local := part_idx_local;
                    best_text_local := exact_text_local;
                end;
            end;
        end;
        if best_part_idx_local < 0 then
        begin
            Exit;
        end;

        out_candidate := stable_candidate;
        exact_units_local := split_text_units(best_text_local);
        stable_units_local[path_starts_local[best_part_idx_local]] :=
            exact_units_local[0];
        stable_units_local[path_starts_local[best_part_idx_local] + 1] :=
            exact_units_local[1];
        out_candidate.text := '';
        for unit_idx_local := 0 to High(stable_units_local) do
        begin
            out_candidate.text := out_candidate.text + stable_units_local[unit_idx_local];
        end;
        Inc(out_candidate.score, c_directional_context_bonus +
            Max(0, best_weight_local));
        out_candidate.has_dict_weight := True;
        out_candidate.dict_weight := out_candidate.score;

        path_parts_local[best_part_idx_local] := best_text_local;
        out_encoded_path := '';
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            if out_encoded_path <> '' then
            begin
                out_encoded_path := out_encoded_path + c_segment_path_separator;
            end;
            out_encoded_path := out_encoded_path + Trim(path_parts_local[part_idx_local]);
        end;
        Result := True;
    end;

    function try_build_light_stable_phrase_chain_candidate_local(
        out out_candidate: TncCandidate; out out_encoded_path: string): Boolean;
    const
        c_max_span = 5;
        c_probe_limit = 6;
    var
        syllables_local: TncPinyinParseResult;
        pos_local: Integer;
        text_local: string;
        path_local: string;
        score_local: Integer;
        phrase_count_local: Integer;

        function build_syllable_key_local(const local_start_idx: Integer;
            const local_count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_count <= 0) or
                (local_start_idx + local_count > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function build_tail_comment_local(const local_start_idx: Integer): string;
        begin
            Result := build_syllable_key_local(local_start_idx,
                Length(syllables_local) - local_start_idx);
        end;

        function is_user_exact_full_text_local(
            const text_value_local: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (Trim(text_value_local) = '') or
                (not dictionary_exact_lookup_cached(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (exact_results_local[exact_idx_local].source = cs_user) and
                    (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    Trim(text_value_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_exact_full_query_text_support_local(
            const text_value_local: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if (Trim(text_value_local) = '') or
                (not dictionary_exact_lookup_cached(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(exact_text_local, Trim(text_value_local)) and
                    (get_candidate_text_unit_count(exact_text_local) =
                    input_syllable_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function stable_single_text_local(const syllable_text: string): string;
        var
            normalized_local: string;
            fixed_text_local: string;
        begin
            Result := '';
            normalized_local := normalize_pinyin_text(syllable_text);
            if (normalized_local = '') or
                (not is_full_pinyin_key(normalized_local)) then
            begin
                Exit;
            end;

            fixed_text_local := get_fixed_resolved_boundary_char_local(
                normalized_local);
            if fixed_text_local <> '' then
            begin
                Exit(fixed_text_local);
            end;

            Exit;
        end;

        procedure append_segment_local(const segment_text: string;
            const segment_score: Integer; const is_phrase: Boolean);
        begin
            text_local := text_local + segment_text;
            if path_local <> '' then
            begin
                path_local := path_local + c_segment_path_separator;
            end;
            path_local := path_local + segment_text;
            Inc(score_local, segment_score);
            if is_phrase then
            begin
                Inc(phrase_count_local);
            end;
        end;

        function try_append_two_plus_one_split_local(
            const split_pos_local: Integer;
            const current_segment_score_local: Integer): Boolean;
        var
            prefix_key_local: string;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_text_local: string;
            prefix_weight_local: Integer;
            best_prefix_text_local: string;
            best_prefix_weight_local: Integer;
            suffix_text_local: string;
            split_score_local: Integer;
        begin
            Result := False;
            if (split_pos_local < 0) or
                (split_pos_local + 2 >= Length(syllables_local)) then
            begin
                Exit;
            end;

            prefix_key_local := build_syllable_key_local(split_pos_local, 2);
            if (prefix_key_local = '') or
                (not dictionary_exact_lookup_cached(prefix_key_local,
                prefix_results_local)) then
            begin
                Exit;
            end;

            best_prefix_text_local := '';
            best_prefix_weight_local := Low(Integer);
            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if prefix_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                prefix_text_local := Trim(
                    prefix_results_local[prefix_idx_local].text);
                if (prefix_text_local = '') or
                    (get_candidate_text_unit_count(prefix_text_local) <> 2) then
                begin
                    Continue;
                end;
                if prefix_results_local[prefix_idx_local].has_dict_weight then
                begin
                    prefix_weight_local :=
                        prefix_results_local[prefix_idx_local].dict_weight;
                end
                else
                begin
                    prefix_weight_local :=
                        prefix_results_local[prefix_idx_local].score;
                end;
                if prefix_weight_local > best_prefix_weight_local then
                begin
                    best_prefix_weight_local := prefix_weight_local;
                    best_prefix_text_local := prefix_text_local;
                end;
            end;

            if (best_prefix_text_local = '') or
                (best_prefix_weight_local < 700) then
            begin
                Exit;
            end;

            suffix_text_local := stable_single_text_local(
                syllables_local[split_pos_local + 2].text);
            if suffix_text_local = '' then
            begin
                Exit;
            end;

            split_score_local := best_prefix_weight_local + 2800;
            if split_score_local <= current_segment_score_local + 260 then
            begin
                Exit;
            end;

            append_segment_local(best_prefix_text_local,
                best_prefix_weight_local + 1200, True);
            append_segment_local(suffix_text_local, 900, False);
            Result := True;
        end;

        function tail_key_is_precisely_extendable_local(
            const local_tail_key: string): Boolean;
        var
            suffix_ch_local: Char;
            extended_tail_key_local: string;
        begin
            Result := False;
            if (local_tail_key = '') or (not is_full_pinyin_key(local_tail_key)) then
            begin
                Exit;
            end;

            for suffix_ch_local := 'a' to 'z' do
            begin
                extended_tail_key_local := local_tail_key + suffix_ch_local;
                if is_full_pinyin_key(extended_tail_key_local) and
                    (get_effective_compact_pinyin_unit_count(
                    extended_tail_key_local) = 1) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function should_keep_tail_syllable_partial_local(
            const local_pos: Integer): Boolean;
        var
            tail_key_local: string;
        begin
            Result := False;
            if (local_pos < 0) or (local_pos <> High(syllables_local)) then
            begin
                Exit;
            end;

            tail_key_local := normalize_pinyin_text(
                syllables_local[local_pos].text);
            Result := (tail_key_local = '') or
                (not is_full_pinyin_key(tail_key_local)) or
                tail_key_is_precisely_extendable_local(tail_key_local);
        end;

        function equals_cjk2_chars_local(const value: string;
            const first_code: Word; const second_code: Word): Boolean;
        begin
            Result := (Length(value) = 2) and
                (value[1] = WideChar(first_code)) and
                (value[2] = WideChar(second_code));
        end;

        function ends_with_cjk1_char_local(const value: string;
            const code: Word): Boolean;
        begin
            Result := (Length(value) >= 1) and
                (value[Length(value)] = WideChar(code));
        end;

        function ends_with_cjk2_chars_local(const value: string;
            const first_code: Word; const second_code: Word): Boolean;
        begin
            Result := (Length(value) >= 2) and
                (value[Length(value) - 1] = WideChar(first_code)) and
                (value[Length(value)] = WideChar(second_code));
        end;

        function is_location_direction_phrase_local(
            const phrase_text: string): Boolean;
        begin
            Result := equals_cjk2_chars_local(phrase_text, $5317, $9762) or
                equals_cjk2_chars_local(phrase_text, $5357, $9762) or
                equals_cjk2_chars_local(phrase_text, $4E1C, $9762) or
                equals_cjk2_chars_local(phrase_text, $897F, $9762) or
                equals_cjk2_chars_local(phrase_text, $524D, $9762) or
                equals_cjk2_chars_local(phrase_text, $540E, $9762) or
                equals_cjk2_chars_local(phrase_text, $4E0A, $9762) or
                equals_cjk2_chars_local(phrase_text, $4E0B, $9762) or
                equals_cjk2_chars_local(phrase_text, $91CC, $9762) or
                equals_cjk2_chars_local(phrase_text, $5916, $9762) or
                equals_cjk2_chars_local(phrase_text, $9644, $8FD1) or
                equals_cjk2_chars_local(phrase_text, $65C1, $8FB9);
        end;

        function looks_like_location_context_local(
            const context_text: string): Boolean;
        var
            trimmed_context_local: string;
        begin
            trimmed_context_local := Trim(context_text);
            Result := (trimmed_context_local <> '') and
                (ends_with_cjk2_chars_local(trimmed_context_local, $5B66,
                $6821) or ends_with_cjk2_chars_local(trimmed_context_local,
                $5927, $5B66) or ends_with_cjk2_chars_local(
                trimmed_context_local, $5B66, $9662) or
                ends_with_cjk2_chars_local(trimmed_context_local, $5C0F,
                $533A) or ends_with_cjk1_char_local(trimmed_context_local,
                $6821) or ends_with_cjk1_char_local(trimmed_context_local,
                $9662) or ends_with_cjk1_char_local(trimmed_context_local,
                $56ED) or ends_with_cjk1_char_local(trimmed_context_local,
                $8DEF) or ends_with_cjk1_char_local(trimmed_context_local,
                $8857) or ends_with_cjk1_char_local(trimmed_context_local,
                $533A) or ends_with_cjk1_char_local(trimmed_context_local,
                $53BF) or ends_with_cjk1_char_local(trimmed_context_local,
                $5E02) or ends_with_cjk1_char_local(trimmed_context_local,
                $7701) or ends_with_cjk1_char_local(trimmed_context_local,
                $9547) or ends_with_cjk1_char_local(trimmed_context_local,
                $4E61) or ends_with_cjk1_char_local(trimmed_context_local,
                $6751) or ends_with_cjk1_char_local(trimmed_context_local,
                $5E97) or ends_with_cjk1_char_local(trimmed_context_local,
                $697C) or ends_with_cjk1_char_local(trimmed_context_local,
                $7AD9) or ends_with_cjk1_char_local(trimmed_context_local,
                $95E8) or ends_with_cjk1_char_local(trimmed_context_local,
                $9986) or ends_with_cjk1_char_local(trimmed_context_local,
                $573A) or ends_with_cjk1_char_local(trimmed_context_local,
                $57CE) or ends_with_cjk2_chars_local(trimmed_context_local,
                $516C, $53F8) or ends_with_cjk2_chars_local(
                trimmed_context_local, $5546, $5708));
        end;

        function get_previous_context_bonus_local(
            const current_text_local: string): Integer;
        begin
            Result := 0;
            if is_location_direction_phrase_local(Trim(current_text_local)) and
                looks_like_location_context_local(text_local) then
            begin
                Result := 1200;
            end;
        end;

        function try_get_best_exact_phrase_local(const local_pos: Integer;
            out out_text: string; out out_score: Integer;
            out out_units: Integer): Boolean;
        var
            span_len_local: Integer;
            key_local: string;
            results_local: TncCandidateList;
            lookup_results_local: TncCandidateList;
            result_idx_local: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
            candidate_base_score_local: Integer;
            best_span_base_score_local: Integer;
            same_span_quality_gap_local: Integer;
            split_check_score_local: Integer;
            internal_alignment_bonus_local: Integer;
            internal_matched_score_local: Integer;
            primary_match_bonus_local: Integer;

            function has_better_prefix_single_split_local(
                const split_span_len_local: Integer;
                const current_score_local: Integer): Boolean;
            const
                c_split_margin = 480;
            var
                prefix_len_local: Integer;
                prefix_key_local: string;
                suffix_key_local: string;
                prefix_results_local: TncCandidateList;
                suffix_results_local: TncCandidateList;
                prefix_idx_local: Integer;
                suffix_idx_local: Integer;
                current_base_score_local: Integer;
                prefix_base_score_local: Integer;
                prefix_score_local: Integer;
                suffix_score_local: Integer;
                split_score_local: Integer;
                prefix_text_local: string;
                suffix_text_local: string;
                stable_suffix_text_local: string;
            begin
                Result := False;
                if split_span_len_local < 3 then
                begin
                    Exit;
                end;

                prefix_len_local := split_span_len_local - 1;
                prefix_key_local := build_syllable_key_local(local_pos,
                    prefix_len_local);
                suffix_key_local := build_syllable_key_local(local_pos +
                    prefix_len_local, 1);
                if (prefix_key_local = '') or (suffix_key_local = '') then
                begin
                    Exit;
                end;

                if not dictionary_exact_lookup_cached(prefix_key_local,
                    prefix_results_local) then
                begin
                    Exit;
                end;
                if not dictionary_lookup_cached(suffix_key_local,
                    suffix_results_local) then
                begin
                    Exit;
                end;

                current_base_score_local := current_score_local -
                    (split_span_len_local * 600);
                if current_base_score_local < 50 then
                begin
                    Exit;
                end;
                if (split_span_len_local = 3) and
                    (current_base_score_local >= 1200) then
                begin
                    Exit;
                end;
                prefix_score_local := Low(Integer);
                prefix_base_score_local := Low(Integer);
                for prefix_idx_local := 0 to High(prefix_results_local) do
                begin
                    if prefix_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                    if (prefix_text_local = '') or
                        (get_candidate_text_unit_count(prefix_text_local) <>
                        prefix_len_local) then
                    begin
                        Continue;
                    end;
                    if prefix_results_local[prefix_idx_local].has_dict_weight then
                    begin
                        prefix_base_score_local :=
                            prefix_results_local[prefix_idx_local].dict_weight;
                    end
                    else
                    begin
                        prefix_base_score_local :=
                            prefix_results_local[prefix_idx_local].score;
                    end;
                    prefix_score_local := prefix_base_score_local;
                    Inc(prefix_score_local, prefix_len_local * 600);
                    Break;
                end;
                if prefix_score_local = Low(Integer) then
                begin
                    Exit;
                end;
                if prefix_base_score_local < Max(360,
                    current_base_score_local + 80) then
                begin
                    Exit;
                end;

                suffix_score_local := Low(Integer);
                for suffix_idx_local := 0 to High(suffix_results_local) do
                begin
                    if suffix_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(suffix_results_local[suffix_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    suffix_text_local := Trim(suffix_results_local[suffix_idx_local].text);
                    if (suffix_text_local = '') or
                        (get_candidate_text_unit_count(suffix_text_local) <> 1) or
                        (not is_bmp_cjk_single_char_candidate(
                        suffix_results_local[suffix_idx_local])) then
                    begin
                        Continue;
                    end;
                    if suffix_results_local[suffix_idx_local].has_dict_weight then
                    begin
                        suffix_score_local :=
                            suffix_results_local[suffix_idx_local].dict_weight;
                    end
                    else
                    begin
                        suffix_score_local := suffix_results_local[suffix_idx_local].score;
                    end;
                    Inc(suffix_score_local, 900);
                    Break;
                end;
                if suffix_score_local = Low(Integer) then
                begin
                    stable_suffix_text_local := stable_single_text_local(
                        syllables_local[local_pos + prefix_len_local].text);
                    if stable_suffix_text_local = '' then
                    begin
                        Exit;
                    end;
                    suffix_score_local := 1600;
                end;

                split_score_local := prefix_score_local + suffix_score_local;
                Result := split_score_local > current_score_local + c_split_margin;
            end;

            function has_better_two_single_split_local(
                const split_span_len_local: Integer;
                const current_base_score_local: Integer;
                const current_score_local: Integer): Boolean;
            const
                c_split_margin = 360;
                c_weak_two_char_phrase_limit = 520;
                c_keep_phrase_with_following_min_weight = 400;
                c_reliable_following_phrase_min_weight = 700;
            var
                left_text_local: string;
                right_text_local: string;
                split_score_local: Integer;

                function has_reliable_following_phrase_local: Boolean;
                var
                    next_pos_local: Integer;
                    next_len_local: Integer;
                    next_key_local: string;
                    next_results_local: TncCandidateList;
                    next_idx_local: Integer;
                    next_text_local: string;
                    next_weight_local: Integer;
                begin
                    Result := False;
                    next_pos_local := local_pos + split_span_len_local;
                    if next_pos_local >= Length(syllables_local) then
                    begin
                        Exit;
                    end;

                    for next_len_local := Min(c_max_span,
                        Length(syllables_local) - next_pos_local) downto 2 do
                    begin
                        next_key_local := build_syllable_key_local(
                            next_pos_local, next_len_local);
                        if (next_key_local = '') or
                            (not dictionary_exact_lookup_cached(next_key_local,
                            next_results_local)) then
                        begin
                            Continue;
                        end;

                        for next_idx_local := 0 to High(next_results_local) do
                        begin
                            if next_idx_local >= c_probe_limit then
                            begin
                                Break;
                            end;
                            if Trim(next_results_local[next_idx_local].comment) <> '' then
                            begin
                                Continue;
                            end;
                            next_text_local :=
                                Trim(next_results_local[next_idx_local].text);
                            if (next_text_local = '') or
                                (get_candidate_text_unit_count(
                                next_text_local) <> next_len_local) then
                            begin
                                Continue;
                            end;
                            next_weight_local :=
                                next_results_local[next_idx_local].score;
                            if next_results_local[next_idx_local].has_dict_weight and
                                (next_results_local[next_idx_local].dict_weight >
                                next_weight_local) then
                            begin
                                next_weight_local :=
                                    next_results_local[next_idx_local].dict_weight;
                            end;
                            if next_weight_local >=
                                c_reliable_following_phrase_min_weight then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;
            begin
                Result := False;
                if (split_span_len_local <> 2) or
                    (current_base_score_local >= c_weak_two_char_phrase_limit) or
                    (local_pos + 1 >= Length(syllables_local)) then
                begin
                    Exit;
                end;

                if (current_base_score_local >=
                    c_keep_phrase_with_following_min_weight) and
                    has_reliable_following_phrase_local then
                begin
                    Exit;
                end;

                left_text_local := stable_single_text_local(
                    syllables_local[local_pos].text);
                right_text_local := stable_single_text_local(
                    syllables_local[local_pos + 1].text);
                if (left_text_local = '') or (right_text_local = '') then
                begin
                    Exit;
                end;

                split_score_local := 3000;
                if m_dictionary <> nil then
                begin
                    Inc(split_score_local,
                        m_dictionary.get_context_bonus(left_text_local,
                        right_text_local) * 4);
                end;
                Result := split_score_local > current_score_local + c_split_margin;
            end;

            function get_lookup_rank_score_for_text_local(
                const lookup_text_local: string;
                const fallback_score_local: Integer): Integer;
            const
                c_lookup_rank_bonus = 384;
            var
                lookup_idx_local: Integer;
                lookup_candidate_text_local: string;
            begin
                Result := fallback_score_local;
                for lookup_idx_local := 0 to High(lookup_results_local) do
                begin
                    if lookup_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results_local[lookup_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    lookup_candidate_text_local := Trim(
                        lookup_results_local[lookup_idx_local].text);
                    if (lookup_candidate_text_local <> '') and
                        SameText(lookup_candidate_text_local,
                        lookup_text_local) then
                    begin
                        Result := lookup_results_local[lookup_idx_local].score;
                        if lookup_results_local[lookup_idx_local].has_dict_weight and
                            (lookup_results_local[lookup_idx_local].dict_weight >
                            Result) then
                        begin
                            Result :=
                                lookup_results_local[lookup_idx_local].dict_weight;
                        end;
                        Inc(Result, (c_probe_limit - lookup_idx_local) *
                            c_lookup_rank_bonus);
                        Exit;
                    end;
                end;
            end;

            function get_lookup_primary_match_bonus_for_text_local(
                const lookup_text_local: string;
                const lookup_units_local: Integer): Integer;
            const
                c_primary_lookup_bonus = 720;
            var
                lookup_idx_local: Integer;
                lookup_candidate_text_local: string;
            begin
                Result := 0;
                for lookup_idx_local := 0 to High(lookup_results_local) do
                begin
                    if lookup_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(lookup_results_local[lookup_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    lookup_candidate_text_local := Trim(
                        lookup_results_local[lookup_idx_local].text);
                    if (lookup_candidate_text_local = '') or
                        (get_candidate_text_unit_count(
                        lookup_candidate_text_local) <> lookup_units_local) then
                    begin
                        Continue;
                    end;
                    if SameText(lookup_candidate_text_local,
                        lookup_text_local) then
                    begin
                        Result := c_primary_lookup_bonus;
                    end;
                    Exit;
                end;
            end;

            function try_get_best_calibrated_exact_span_score_local(
                const score_pos_local: Integer; const score_units_local: Integer;
                out out_score_local: Integer; out out_text_local: string): Boolean;
            var
                score_key_local: string;
                exact_local: TncCandidateList;
                lookup_local: TncCandidateList;
                exact_idx_local: Integer;
                lookup_idx_local: Integer;
                exact_text_local: string;
                lookup_text_local: string;
                base_score_local: Integer;
                calibrated_score_local: Integer;
            begin
                Result := False;
                out_score_local := Low(Integer);
                out_text_local := '';
                score_key_local := build_syllable_key_local(score_pos_local,
                    score_units_local);
                if (score_key_local = '') or
                    (not dictionary_exact_lookup_cached(score_key_local,
                    exact_local)) then
                begin
                    Exit;
                end;
                if not dictionary_lookup_cached(score_key_local, lookup_local) then
                begin
                    lookup_local := nil;
                end;

                for exact_idx_local := 0 to High(exact_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_local := Trim(exact_local[exact_idx_local].text);
                    if (exact_text_local = '') or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        score_units_local) then
                    begin
                        Continue;
                    end;

                    if exact_local[exact_idx_local].has_dict_weight then
                    begin
                        base_score_local := exact_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        base_score_local := exact_local[exact_idx_local].score;
                    end;
                    calibrated_score_local := base_score_local;
                    for lookup_idx_local := 0 to High(lookup_local) do
                    begin
                        if lookup_idx_local >= c_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(lookup_local[lookup_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        lookup_text_local := Trim(
                            lookup_local[lookup_idx_local].text);
                        if SameText(lookup_text_local, exact_text_local) then
                        begin
                            calibrated_score_local :=
                                lookup_local[lookup_idx_local].score;
                            if lookup_local[lookup_idx_local].has_dict_weight and
                                (lookup_local[lookup_idx_local].dict_weight >
                                calibrated_score_local) then
                            begin
                                calibrated_score_local :=
                                    lookup_local[lookup_idx_local].dict_weight;
                            end;
                            Break;
                        end;
                    end;
                    Inc(calibrated_score_local, score_units_local * 600);
                    if (not Result) or (calibrated_score_local >
                        out_score_local) then
                    begin
                        Result := True;
                        out_score_local := calibrated_score_local;
                        out_text_local := exact_text_local;
                    end;
                end;
            end;

            function has_better_single_prefix_phrase_split_local(
                const split_span_len_local: Integer;
                const current_base_score_local: Integer;
                const current_score_local: Integer): Boolean;
            const
                c_split_margin = 220;
                c_fixed_prefix_score = 1900;
                c_min_tail_phrase_score = 900;
                c_function_split_bonus = 1400;
            var
                prefix_text_local: string;
                tail_score_local: Integer;
                tail_units_local: Integer;
                remaining_units_local: Integer;
                split_score_local: Integer;
                tail_text_local: string;
                single_key_local: string;
                single_results_local: TncCandidateList;
                single_idx_local: Integer;
                single_text_local: string;

                function is_extended_function_prefix_text_local(
                    const text_value_local: string): Boolean;
                begin
                    Result := (text_value_local = string(Char($5BF9))) or
                        (text_value_local = string(Char($4ECE))) or
                        (text_value_local = string(Char($5411))) or
                        (text_value_local = string(Char($4E0E))) or
                        (text_value_local = string(Char($90FD))) or
                        (text_value_local = string(Char($8FD8))) or
                        (text_value_local = string(Char($624D))) or
                        (text_value_local = string(Char($5C31))) or
                        (text_value_local = string(Char($53EA))) or
                        (text_value_local = string(Char($522B))) or
                        (text_value_local = string(Char($5DF2))) or
                        (text_value_local = string(Char($5C06))) or
                        (text_value_local = string(Char($53EF))) or
                        (text_value_local = string(Char($60F3))) or
                        (text_value_local = string(Char($65E0))) or
                        (text_value_local = string(Char($975E))) or
                        (text_value_local = string(Char($672A))) or
                        (text_value_local = string(Char($4ECD))) or
                        (text_value_local = string(Char($591A))) or
                        (text_value_local = string(Char($5C11))) or
                        (text_value_local = string(Char($65B0))) or
                        (text_value_local = string(Char($8001))) or
                        (text_value_local = string(Char($5C0F))) or
                        (text_value_local = string(Char($5927))) or
                        (text_value_local = string(Char($5168))) or
                        (text_value_local = string(Char($8D85))) or
                        (text_value_local = string(Char($771F))) or
                        (text_value_local = string(Char($597D))) or
                        (text_value_local = string(Char($5982))) or
                        (text_value_local = string(Char($633A))) or
                        (text_value_local = string(Char($8F83))) or
                        (text_value_local = string(Char($6781)));
                end;

                function is_function_prefix_text_local(
                    const text_value_local: string): Boolean;
                begin
                    Result := (text_value_local = string(Char($662F))) or
                        (text_value_local = string(Char($7684))) or
                        (text_value_local = string(Char($4E86))) or
                        (text_value_local = string(Char($5728))) or
                        (text_value_local = string(Char($4E8E))) or
                        (text_value_local = string(Char($4E5F))) or
                        (text_value_local = string(Char($548C))) or
                        (text_value_local = string(Char($4E0D))) or
                        (text_value_local = string(Char($518D))) or
                        (text_value_local = string(Char($9AD8))) or
                        (text_value_local = string(Char($5F88))) or
                        (text_value_local = string(Char($592A))) or
                        (text_value_local = string(Char($6700))) or

                        (text_value_local = string(Char($66F4))) or
                        (text_value_local = string(Char($6CA1))) or
                        (text_value_local = string(Char($8981))) or
                        (text_value_local = string(Char($4F1A))) or
                        (text_value_local = string(Char($80FD))) or
                        (text_value_local = string(Char($628A))) or
                        (text_value_local = string(Char($88AB))) or
                        (text_value_local = string(Char($8BA9))) or
                        (text_value_local = string(Char($7ED9)));
                end;
            begin
                Result := False;
                if (split_span_len_local < 2) or
                    (local_pos + split_span_len_local > Length(syllables_local)) then
                begin
                    Exit;
                end;
                if split_span_len_local >= 3 then
                begin
                    Exit;
                end;

                prefix_text_local := stable_single_text_local(
                    syllables_local[local_pos].text);
                if (prefix_text_local = '') or
                    ((not is_function_prefix_text_local(prefix_text_local)) and
                    (not is_extended_function_prefix_text_local(prefix_text_local))) then
                begin
                    prefix_text_local := '';
                    single_key_local := normalize_pinyin_text(
                        syllables_local[local_pos].text);
                    if (single_key_local <> '') and
                        dictionary_exact_lookup_cached(single_key_local,
                        single_results_local) then
                    begin
                        for single_idx_local := 0 to High(single_results_local) do
                        begin
                            if single_idx_local >= 12 then
                            begin
                                Break;
                            end;
                            single_text_local := Trim(
                                single_results_local[single_idx_local].text);
                            if (single_text_local <> '') and
                                (get_candidate_text_unit_count(single_text_local) = 1) and
                                (is_function_prefix_text_local(single_text_local) or
                                is_extended_function_prefix_text_local(single_text_local)) then
                            begin
                                prefix_text_local := single_text_local;
                                Break;
                            end;
                        end;
                    end;
                end;
                if prefix_text_local = '' then
                begin
                    Exit;
                end;

                remaining_units_local := Length(syllables_local) - local_pos - 1;
                for tail_units_local := Min(c_max_span,
                    remaining_units_local) downto 2 do
                begin
                    if (1 + tail_units_local < split_span_len_local) then
                    begin
                        Continue;
                    end;
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos + 1, tail_units_local, tail_score_local,
                        tail_text_local) then
                    begin
                        Continue;
                    end;
                    if tail_score_local < c_min_tail_phrase_score then
                    begin
                        Continue;
                    end;

                    split_score_local := c_fixed_prefix_score +
                        tail_score_local + c_function_split_bonus;
                    if m_dictionary <> nil then
                    begin
                        Inc(split_score_local,
                            m_dictionary.get_context_bonus(prefix_text_local,
                            tail_text_local) * 4);
                    end;
                    if split_score_local > current_score_local + c_split_margin then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function has_better_internal_phrase_split_local(
                const split_span_len_local: Integer;
                const current_score_local: Integer): Boolean;
            const
                c_internal_split_margin = 480;
            var
                left_units_local: Integer;
                left_score_local: Integer;
                right_score_local: Integer;
                left_text_local: string;
                right_text_local: string;
            begin
                Result := False;
                if split_span_len_local < 4 then
                begin
                    Exit;
                end;

                for left_units_local := 2 to split_span_len_local - 2 do
                begin
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos, left_units_local, left_score_local,
                        left_text_local) then
                    begin
                        Continue;
                    end;
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos + left_units_local,
                        split_span_len_local - left_units_local,
                        right_score_local, right_text_local) then
                    begin
                        Continue;
                    end;
                    if left_score_local + right_score_local >
                        current_score_local + c_internal_split_margin then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function has_better_prefix_following_phrase_split_local(
                const split_span_len_local: Integer;
                const current_score_local: Integer): Boolean;
            const
                c_split_margin = 420;
                c_min_following_phrase_score = 1500;
            var
                left_units_local: Integer;
                right_start_local: Integer;
                right_units_local: Integer;
                remaining_units_local: Integer;
                left_score_local: Integer;
                right_score_local: Integer;
                combined_score_local: Integer;
                left_text_local: string;
                right_text_local: string;
            begin
                Result := False;
                if (split_span_len_local < 3) or
                    (local_pos + split_span_len_local >= Length(syllables_local)) then
                begin
                    Exit;
                end;

                for left_units_local := 2 to split_span_len_local - 1 do
                begin
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos, left_units_local, left_score_local,
                        left_text_local) then
                    begin
                        Continue;
                    end;

                    right_start_local := local_pos + left_units_local;
                    remaining_units_local := Length(syllables_local) -
                        right_start_local;
                    for right_units_local := Min(c_max_span,
                        remaining_units_local) downto 2 do
                    begin
                        if left_units_local + right_units_local <=
                            split_span_len_local then
                        begin
                            Continue;
                        end;
                        if not try_get_best_calibrated_exact_span_score_local(
                            right_start_local, right_units_local,
                            right_score_local, right_text_local) then
                        begin
                            Continue;
                        end;
                        if right_score_local < c_min_following_phrase_score then
                        begin
                            Continue;
                        end;

                        combined_score_local := left_score_local +
                            right_score_local;
                        if m_dictionary <> nil then
                        begin
                            Inc(combined_score_local,
                                m_dictionary.get_context_bonus(left_text_local,
                                right_text_local) * 4);
                        end;
                        if combined_score_local > current_score_local +
                            c_split_margin then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;

            function build_units_slice_text_local(const units_local: TArray<string>;
                const start_idx_local: Integer; const count_local: Integer): string;
            var
                unit_idx_local: Integer;
            begin
                Result := '';
                if (count_local <= 0) or (start_idx_local < 0) or
                    (start_idx_local + count_local > Length(units_local)) then
                begin
                    Exit;
                end;
                for unit_idx_local := start_idx_local to
                    start_idx_local + count_local - 1 do
                begin
                    Result := Result + units_local[unit_idx_local];
                end;
            end;

            function is_confident_chain_exact_span_local(
                const protected_span_len_local: Integer;
                const protected_rank_local: Integer;
                const protected_weight_local: Integer): Boolean;
            begin
                Result := False;
                if protected_rank_local = 0 then
                begin
                    if ((protected_span_len_local = 2) and
                        (protected_weight_local >= 650)) or
                        ((protected_span_len_local = 3) and
                        (protected_weight_local >= 420)) or
                        ((protected_span_len_local >= 4) and
                        (protected_weight_local >= 360)) then
                    begin
                        Result := True;
                    end;
                end
                else if (protected_rank_local = 1) and
                    (protected_span_len_local >= 3) and
                    (protected_weight_local >= 900) then
                begin
                    Result := True;
                end;
            end;

            function try_get_confident_chain_exact_span_local(
                const protected_start_local: Integer;
                const protected_span_len_local: Integer;
                out out_protected_text_local: string;
                out out_protected_weight_local: Integer;
                out out_protected_rank_local: Integer): Boolean;
            var
                protected_key_local: string;
                protected_results_local: TncCandidateList;
                protected_idx_local: Integer;
                protected_text_local: string;
                protected_weight_local: Integer;
            begin
                Result := False;
                out_protected_text_local := '';
                out_protected_weight_local := 0;
                out_protected_rank_local := -1;
                if (protected_start_local < 0) or
                    (protected_span_len_local < 2) or
                    (protected_start_local + protected_span_len_local >
                    Length(syllables_local)) then
                begin
                    Exit;
                end;

                protected_key_local := build_syllable_key_local(
                    protected_start_local, protected_span_len_local);
                if (protected_key_local = '') or
                    (not dictionary_exact_lookup_cached(protected_key_local,
                    protected_results_local)) then
                begin
                    Exit;
                end;

                for protected_idx_local := 0 to High(protected_results_local) do
                begin
                    if protected_idx_local >= 4 then
                    begin
                        Break;
                    end;
                    if Trim(protected_results_local[protected_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(
                        protected_results_local[protected_idx_local]) or
                        is_runtime_common_pattern_candidate(
                        protected_results_local[protected_idx_local]) or
                        is_runtime_redup_candidate(
                        protected_results_local[protected_idx_local]) then
                    begin
                        Continue;
                    end;
                    protected_text_local := Trim(
                        protected_results_local[protected_idx_local].text);
                    if (protected_text_local = '') or
                        (get_candidate_text_unit_count(protected_text_local) <>
                        protected_span_len_local) then
                    begin
                        Continue;
                    end;
                    if protected_results_local[protected_idx_local].has_dict_weight then
                    begin
                        protected_weight_local :=
                            protected_results_local[protected_idx_local].dict_weight;
                    end
                    else
                    begin
                        protected_weight_local :=
                            protected_results_local[protected_idx_local].score;
                    end;
                    if not is_confident_chain_exact_span_local(
                        protected_span_len_local, protected_idx_local,
                        protected_weight_local) then
                    begin
                        Continue;
                    end;

                    out_protected_text_local := protected_text_local;
                    out_protected_weight_local := protected_weight_local;
                    out_protected_rank_local := protected_idx_local;
                    Exit(True);
                end;
            end;

            function candidate_cuts_confident_chain_span_local(
                const candidate_text_value_local: string;
                const candidate_start_local: Integer;
                const candidate_span_len_local: Integer;
                const candidate_base_weight_local: Integer): Boolean;
            const
                c_overlap_mismatch_margin = 220;
                c_boundary_cut_margin = 520;
            var
                candidate_units_local: TArray<string>;
                protected_units_local: TArray<string>;
                protected_start_local: Integer;
                protected_span_len_local: Integer;
                protected_text_local: string;
                protected_weight_local: Integer;
                protected_rank_local: Integer;
                scan_start_local: Integer;
                scan_end_local: Integer;
                overlap_start_local: Integer;
                overlap_end_local: Integer;
                overlap_len_local: Integer;
                candidate_overlap_local: string;
                protected_overlap_local: string;
            begin
                Result := False;
                if candidate_span_len_local < 2 then
                begin
                    Exit;
                end;
                candidate_units_local := split_text_units(Trim(
                    candidate_text_value_local));
                if Length(candidate_units_local) <> candidate_span_len_local then
                begin
                    Exit;
                end;

                scan_start_local := Max(0, candidate_start_local - 3);
                scan_end_local := Min(Length(syllables_local) - 2,
                    candidate_start_local + candidate_span_len_local - 1);
                for protected_start_local := scan_start_local to scan_end_local do
                begin
                    for protected_span_len_local := Min(4,
                        Length(syllables_local) - protected_start_local) downto 2 do
                    begin
                        if (protected_start_local = candidate_start_local) and
                            (protected_span_len_local = candidate_span_len_local) then
                        begin
                            Continue;
                        end;
                        if not try_get_confident_chain_exact_span_local(
                            protected_start_local, protected_span_len_local,
                            protected_text_local, protected_weight_local,
                            protected_rank_local) then
                        begin
                            Continue;
                        end;
                        protected_units_local := split_text_units(
                            protected_text_local);
                        if Length(protected_units_local) <>
                            protected_span_len_local then
                        begin
                            Continue;
                        end;

                        overlap_start_local := Max(candidate_start_local,
                            protected_start_local);
                        overlap_end_local := Min(candidate_start_local +
                            candidate_span_len_local, protected_start_local +
                            protected_span_len_local);
                        overlap_len_local := overlap_end_local -
                            overlap_start_local;
                        if overlap_len_local <= 0 then
                        begin
                            Continue;
                        end;

                        candidate_overlap_local := build_units_slice_text_local(
                            candidate_units_local,
                            overlap_start_local - candidate_start_local,
                            overlap_len_local);
                        protected_overlap_local := build_units_slice_text_local(
                            protected_units_local,
                            overlap_start_local - protected_start_local,
                            overlap_len_local);
                        if (candidate_overlap_local = '') or
                            (protected_overlap_local = '') then
                        begin
                            Continue;
                        end;

                        if not SameText(candidate_overlap_local,
                            protected_overlap_local) then
                        begin
                            if ((overlap_len_local >= 2) and
                                (candidate_base_weight_local <=
                                protected_weight_local +
                                c_overlap_mismatch_margin)) or
                                ((overlap_len_local = 1) and
                                (protected_rank_local = 0) and
                                (protected_weight_local >= 900) and
                                (candidate_base_weight_local <=
                                protected_weight_local +
                                c_overlap_mismatch_margin)) then
                            begin
                                Exit(True);
                            end;
                            Continue;
                        end;

                        if (protected_start_local > candidate_start_local) and
                            (protected_start_local < candidate_start_local +
                            candidate_span_len_local) and
                            (protected_start_local >= candidate_start_local + 2) and
                            (overlap_start_local = protected_start_local) and
                            (overlap_len_local < protected_span_len_local) and
                            (protected_span_len_local >= 3) and
                            (protected_weight_local >= 900) and
                            (candidate_base_weight_local <=
                            protected_weight_local + c_boundary_cut_margin) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;

            function try_get_calibrated_exact_text_score_local(
                const score_pos_local: Integer; const score_units_local: Integer;
                const expected_text_local: string;
                out out_score_local: Integer): Boolean;
            var
                score_key_local: string;
                exact_local: TncCandidateList;
                lookup_local: TncCandidateList;
                exact_idx_local: Integer;
                lookup_idx_local: Integer;
                exact_text_local: string;
                lookup_text_local: string;
                base_score_local: Integer;
            begin
                Result := False;
                out_score_local := Low(Integer);
                if Trim(expected_text_local) = '' then
                begin
                    Exit;
                end;
                score_key_local := build_syllable_key_local(score_pos_local,
                    score_units_local);
                if (score_key_local = '') or
                    (not dictionary_exact_lookup_cached(score_key_local,
                    exact_local)) then
                begin
                    Exit;
                end;
                if not dictionary_lookup_cached(score_key_local, lookup_local) then
                begin
                    lookup_local := nil;
                end;

                for exact_idx_local := 0 to High(exact_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(exact_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_local := Trim(exact_local[exact_idx_local].text);
                    if (not SameText(exact_text_local, expected_text_local)) or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        score_units_local) then
                    begin
                        Continue;
                    end;
                    if exact_local[exact_idx_local].has_dict_weight then
                    begin
                        base_score_local := exact_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        base_score_local := exact_local[exact_idx_local].score;
                    end;
                    out_score_local := base_score_local;
                    for lookup_idx_local := 0 to High(lookup_local) do
                    begin
                        if lookup_idx_local >= c_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(lookup_local[lookup_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        lookup_text_local := Trim(
                            lookup_local[lookup_idx_local].text);
                        if SameText(lookup_text_local, exact_text_local) then
                        begin
                            out_score_local := lookup_local[lookup_idx_local].score;
                            if lookup_local[lookup_idx_local].has_dict_weight and
                                (lookup_local[lookup_idx_local].dict_weight >
                                out_score_local) then
                            begin
                                out_score_local :=
                                    lookup_local[lookup_idx_local].dict_weight;
                            end;
                            Break;
                        end;
                    end;
                    Inc(out_score_local, score_units_local * 600);
                    Exit(True);
                end;
            end;

            function try_get_internal_phrase_split_matched_score_local(
                const split_text_local: string;
                const split_span_len_local: Integer;
                out out_score_local: Integer): Boolean;
            var
                units_local: TArray<string>;
                left_units_local: Integer;
                left_score_local: Integer;
                right_score_local: Integer;
                left_slice_local: string;
                right_slice_local: string;
                combined_score_local: Integer;
            begin
                Result := False;
                out_score_local := Low(Integer);
                if split_span_len_local < 4 then
                begin
                    Exit;
                end;
                units_local := split_text_units(Trim(split_text_local));
                if Length(units_local) <> split_span_len_local then
                begin
                    Exit;
                end;

                for left_units_local := 2 to split_span_len_local - 2 do
                begin
                    left_slice_local := build_units_slice_text_local(units_local,
                        0, left_units_local);
                    right_slice_local := build_units_slice_text_local(units_local,
                        left_units_local,
                        split_span_len_local - left_units_local);
                    if (left_slice_local = '') or (right_slice_local = '') then
                    begin
                        Continue;
                    end;
                    if not try_get_calibrated_exact_text_score_local(local_pos,
                        left_units_local, left_slice_local,
                        left_score_local) then
                    begin
                        Continue;
                    end;
                    if not try_get_calibrated_exact_text_score_local(
                        local_pos + left_units_local,
                        split_span_len_local - left_units_local,
                        right_slice_local, right_score_local) then
                    begin
                        Continue;
                    end;
                    combined_score_local := left_score_local + right_score_local;
                    if (not Result) or (combined_score_local > out_score_local) then
                    begin
                        Result := True;
                        out_score_local := combined_score_local;
                    end;
                end;
            end;

            function get_internal_phrase_split_alignment_bonus_local(
                const split_text_local: string;
                const split_span_len_local: Integer): Integer;
            const
                c_alignment_bonus = 3600;
            var
                units_local: TArray<string>;
                left_units_local: Integer;
                left_score_local: Integer;
                right_score_local: Integer;
                left_text_local: string;
                right_text_local: string;
                left_slice_local: string;
                right_slice_local: string;
            begin
                Result := 0;
                if split_span_len_local < 4 then
                begin
                    Exit;
                end;
                units_local := split_text_units(Trim(split_text_local));
                if Length(units_local) <> split_span_len_local then
                begin
                    Exit;
                end;

                for left_units_local := 2 to split_span_len_local - 2 do
                begin
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos, left_units_local, left_score_local,
                        left_text_local) then
                    begin
                        Continue;
                    end;
                    if not try_get_best_calibrated_exact_span_score_local(
                        local_pos + left_units_local,
                        split_span_len_local - left_units_local,
                        right_score_local, right_text_local) then
                    begin
                        Continue;
                    end;
                    left_slice_local := build_units_slice_text_local(units_local,
                        0, left_units_local);
                    right_slice_local := build_units_slice_text_local(units_local,
                        left_units_local,
                        split_span_len_local - left_units_local);
                    if SameText(left_slice_local, left_text_local) and
                        SameText(right_slice_local, right_text_local) then
                    begin
                        Exit(c_alignment_bonus);
                    end;
                end;
            end;

            function get_following_context_bonus_local(
                const current_text_local: string;
                const next_pos_local: Integer;
                const span_gap_for_context_local: Integer): Integer;
            const
                c_max_following_context_bonus = 2400;
            var
                next_span_len_local: Integer;
                next_key_local: string;
                next_results_local: TncCandidateList;
                next_idx_local: Integer;
                next_text_local: string;
                raw_context_bonus_local: Integer;
                prefix_bonus_local: Integer;
            begin
                Result := 0;
                if (m_dictionary = nil) or (Trim(current_text_local) = '') or
                    (next_pos_local >= Length(syllables_local)) then
                begin
                    Exit;
                end;

                for next_span_len_local := Min(c_max_span,
                    Length(syllables_local) - next_pos_local) downto 1 do
                begin
                    next_key_local := build_syllable_key_local(next_pos_local,
                        next_span_len_local);
                    if next_key_local = '' then
                    begin
                        Continue;
                    end;
                    if not dictionary_lookup_cached(next_key_local,
                        next_results_local) then
                    begin
                        Continue;
                    end;

                    for next_idx_local := 0 to High(next_results_local) do
                    begin
                        if next_idx_local >= c_probe_limit then
                        begin
                            Break;
                        end;
                        if Trim(next_results_local[next_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        next_text_local := Trim(next_results_local[next_idx_local].text);
                        if (next_text_local = '') or
                            (get_candidate_text_unit_count(next_text_local) <>
                            next_span_len_local) then
                        begin
                            Continue;
                        end;

                        raw_context_bonus_local :=
                            m_dictionary.get_context_bonus(
                            Trim(current_text_local), next_text_local);
                        if span_gap_for_context_local >= 240 then
                        begin
                            raw_context_bonus_local :=
                                Min(raw_context_bonus_local, 240);
                        end;
                        prefix_bonus_local :=
                            get_sentence_text_prefix_bonus_local(
                            Trim(current_text_local) + next_text_local);

                        Result := Min(c_max_following_context_bonus,
                            raw_context_bonus_local * 4 +
                            prefix_bonus_local * 2);
                        Exit;
                    end;
                end;
            end;
        begin
            Result := False;
            out_text := '';
            out_score := Low(Integer);
            out_units := 0;

            for span_len_local := Min(c_max_span,
                Length(syllables_local) - local_pos) downto 2 do
            begin
                key_local := build_syllable_key_local(local_pos,
                    span_len_local);
                if key_local = '' then
                begin
                    Continue;
                end;

                if not dictionary_exact_lookup_cached(key_local, results_local) then
                begin
                    Continue;
                end;
                if not dictionary_lookup_cached(key_local,
                    lookup_results_local) then
                begin
                    lookup_results_local := nil;
                end;

                best_span_base_score_local := Low(Integer);
                for result_idx_local := 0 to High(results_local) do
                begin
                    if result_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(results_local[result_idx_local]) or
                        is_runtime_common_pattern_candidate(
                        results_local[result_idx_local]) or
                        is_runtime_redup_candidate(
                        results_local[result_idx_local]) then
                    begin
                        Continue;
                    end;

                    candidate_text_local :=
                        Trim(results_local[result_idx_local].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        span_len_local) then
                    begin
                        Continue;
                    end;

                    if results_local[result_idx_local].has_dict_weight then
                    begin
                        candidate_base_score_local :=
                            results_local[result_idx_local].dict_weight;
                    end
                    else
                    begin
                        candidate_base_score_local :=
                            results_local[result_idx_local].score;
                    end;
                    if (results_local[result_idx_local].source = cs_rule) and
                        (candidate_base_score_local >= 50000) then
                    begin
                        Continue;
                    end;
                    if candidate_base_score_local > best_span_base_score_local then
                    begin
                        best_span_base_score_local := candidate_base_score_local;
                    end;
                end;

                for result_idx_local := 0 to High(results_local) do
                begin
                    if result_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if Trim(results_local[result_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    if is_runtime_chain_candidate(results_local[result_idx_local]) or
                        is_runtime_common_pattern_candidate(
                        results_local[result_idx_local]) or
                        is_runtime_redup_candidate(
                        results_local[result_idx_local]) then
                    begin
                        Continue;
                    end;

                    candidate_text_local :=
                        Trim(results_local[result_idx_local].text);
                    if (candidate_text_local = '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        span_len_local) then
                    begin
                        Continue;
                    end;

                    if results_local[result_idx_local].has_dict_weight then
                    begin
                        candidate_base_score_local :=
                            results_local[result_idx_local].dict_weight;
                    end
                    else
                    begin
                        candidate_base_score_local :=
                            results_local[result_idx_local].score;
                    end;
                    if (results_local[result_idx_local].source = cs_rule) and
                        (candidate_base_score_local >= 50000) then
                    begin
                        Continue;
                    end;
                    candidate_score_local := get_lookup_rank_score_for_text_local(
                        candidate_text_local, candidate_base_score_local);
                    primary_match_bonus_local :=
                        get_lookup_primary_match_bonus_for_text_local(
                        candidate_text_local, span_len_local);
                    Inc(candidate_score_local, primary_match_bonus_local);
                    Inc(candidate_score_local, span_len_local * 600);

                    internal_alignment_bonus_local :=
                        get_internal_phrase_split_alignment_bonus_local(
                        candidate_text_local, span_len_local);
                    Inc(candidate_score_local, internal_alignment_bonus_local);
                    if try_get_internal_phrase_split_matched_score_local(
                        candidate_text_local, span_len_local,
                        internal_matched_score_local) and
                        (internal_matched_score_local > candidate_score_local) then
                    begin
                        candidate_score_local := internal_matched_score_local;
                    end;
                    same_span_quality_gap_local := Max(0,
                        best_span_base_score_local - candidate_base_score_local);
                    if primary_match_bonus_local > 0 then
                    begin
                        same_span_quality_gap_local := 0;
                    end;
                    if same_span_quality_gap_local >= 240 then
                    begin
                        Dec(candidate_score_local, Min(80000, 12000 +
                            same_span_quality_gap_local * 80));
                    end;
                    if candidate_cuts_confident_chain_span_local(
                        candidate_text_local, local_pos, span_len_local,
                        candidate_base_score_local) then
                    begin
                        Continue;
                    end;
                    if (candidate_base_score_local <= 80) and
                        (internal_alignment_bonus_local <= 0) and
                        has_better_internal_phrase_split_local(span_len_local,
                        candidate_score_local) then
                    begin
                        Continue;
                    end;

                    if not ((span_len_local >= 4) and
                        (candidate_base_score_local >= 600)) then
                    begin
                        split_check_score_local := candidate_base_score_local +
                            span_len_local * 600;
                        if has_better_prefix_single_split_local(span_len_local,
                            split_check_score_local) then
                        begin
                            Continue;
                        end;
                        if has_better_two_single_split_local(span_len_local,
                            candidate_base_score_local, candidate_score_local) then
                        begin
                            Continue;
                        end;
                        if has_better_single_prefix_phrase_split_local(
                            span_len_local, candidate_base_score_local,
                            split_check_score_local) then
                        begin
                            Continue;
                        end;
                        if not ((span_len_local = 3) and
                            (candidate_base_score_local >= 520)) and
                            has_better_prefix_following_phrase_split_local(
                            span_len_local, split_check_score_local) then
                        begin
                            Continue;
                        end;
                    end;
                    Inc(candidate_score_local, get_previous_context_bonus_local(
                        candidate_text_local));
                    Inc(candidate_score_local, get_following_context_bonus_local(
                        candidate_text_local, local_pos + span_len_local,
                        same_span_quality_gap_local));
                    if (not Result) or (candidate_score_local > out_score) then
                    begin
                        Result := True;
                        out_text := candidate_text_local;
                        out_score := candidate_score_local;
                        out_units := span_len_local;
                    end;
                end;

                if Result then
                begin
                    Exit;
                end;
            end;
        end;

        procedure prefer_exact_three_syllable_head_local;
        const
            c_min_head_weight = 1500;
        var
            head_key_local: string;
            head_results_local: TncCandidateList;
            head_idx_local: Integer;
            head_text_local: string;
            current_head_local: string;
            suffix_text_local: string;
            units_local: TArray<string>;
            unit_idx_local: Integer;
            head_weight_local: Integer;
        begin
            if (Length(syllables_local) < 4) or (Trim(text_local) = '') then
            begin
                Exit;
            end;

            units_local := split_text_units(text_local);
            if Length(units_local) <> Length(syllables_local) then
            begin
                Exit;
            end;
            current_head_local := units_local[0] + units_local[1] + units_local[2];

            head_key_local := build_syllable_key_local(0, 3);
            if (head_key_local = '') or
                (not dictionary_exact_lookup_cached(head_key_local,
                head_results_local)) then
            begin
                Exit;
            end;

            for head_idx_local := 0 to High(head_results_local) do
            begin
                if head_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results_local[head_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                head_text_local := Trim(head_results_local[head_idx_local].text);
                if (head_text_local = '') or
                    (get_candidate_text_unit_count(head_text_local) <> 3) then
                begin
                    Continue;
                end;
                if head_results_local[head_idx_local].has_dict_weight then
                begin
                    head_weight_local := head_results_local[head_idx_local].dict_weight;
                end
                else
                begin
                    head_weight_local := head_results_local[head_idx_local].score;
                end;
                if SameText(head_text_local, current_head_local) and
                    (head_weight_local >= c_min_head_weight) then
                begin
                    Exit;
                end;
            end;

            for head_idx_local := 0 to High(head_results_local) do
            begin
                if head_idx_local >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results_local[head_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                head_text_local := Trim(head_results_local[head_idx_local].text);
                if (head_text_local = '') or
                    SameText(head_text_local, current_head_local) or
                    (get_candidate_text_unit_count(head_text_local) <> 3) then
                begin
                    Continue;
                end;
                if head_results_local[head_idx_local].has_dict_weight then
                begin
                    head_weight_local := head_results_local[head_idx_local].dict_weight;
                end
                else
                begin
                    head_weight_local := head_results_local[head_idx_local].score;
                end;
                if head_weight_local < c_min_head_weight then
                begin
                    Continue;
                end;

                suffix_text_local := '';
                for unit_idx_local := 3 to High(units_local) do
                begin
                    suffix_text_local := suffix_text_local + units_local[unit_idx_local];
                end;
                text_local := head_text_local + suffix_text_local;
                path_local := head_text_local + c_segment_path_separator +
                    suffix_text_local;
                Inc(score_local, 2400 + head_weight_local);
                Inc(phrase_count_local);
                Exit;
            end;
        end;

        function try_reduce_short_composed_complete_to_prefix_partial_local(
            out out_tail_comment: string): Boolean;
        var
            path_parts_local: TArray<string>;
            units_local: TArray<string>;
            last_segment_units_local: Integer;
            prefix_units_local: Integer;
            unit_idx_local: Integer;
            path_idx_local: Integer;
            prefix_text_local: string;
            prefix_path_local: string;
            prefix_key_local: string;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_supported_local: Boolean;
        begin
            Result := False;
            out_tail_comment := '';
            if (input_syllable_count > 5) or (text_local = '') or
                (path_local = '') or
                (Pos(c_segment_path_separator, path_local) <= 0) or
                has_exact_full_query_text_support_local(text_local) then
            begin
                Exit;
            end;

            path_parts_local := path_local.Split([c_segment_path_separator]);
            if Length(path_parts_local) < 2 then
            begin
                Exit;
            end;

            last_segment_units_local := get_candidate_text_unit_count(
                Trim(path_parts_local[High(path_parts_local)]));
            if last_segment_units_local < 2 then
            begin
                Exit;
            end;

            prefix_units_local := input_syllable_count - last_segment_units_local;
            if prefix_units_local < 2 then
            begin
                Exit;
            end;

            units_local := split_text_units(text_local);
            if Length(units_local) <> input_syllable_count then
            begin
                Exit;
            end;

            prefix_text_local := '';
            for unit_idx_local := 0 to prefix_units_local - 1 do
            begin
                prefix_text_local := prefix_text_local + units_local[unit_idx_local];
            end;
            if get_candidate_text_unit_count(prefix_text_local) <> prefix_units_local then
            begin
                Exit;
            end;

            prefix_key_local := build_syllable_key_local(0, prefix_units_local);
            if (prefix_key_local = '') or
                (not dictionary_exact_lookup_cached(prefix_key_local,
                prefix_results_local)) then
            begin
                Exit;
            end;

            prefix_supported_local := False;
            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                if (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                    SameText(Trim(prefix_results_local[prefix_idx_local].text),
                    prefix_text_local) and
                    (get_candidate_text_unit_count(
                    Trim(prefix_results_local[prefix_idx_local].text)) =
                    prefix_units_local) then
                begin
                    prefix_supported_local := True;
                    Break;
                end;
            end;
            if not prefix_supported_local then
            begin
                Exit;
            end;

            prefix_path_local := '';
            for path_idx_local := 0 to High(path_parts_local) - 1 do
            begin
                if prefix_path_local <> '' then
                begin
                    prefix_path_local := prefix_path_local +
                        c_segment_path_separator;
                end;
                prefix_path_local := prefix_path_local +
                    Trim(path_parts_local[path_idx_local]);
            end;

            out_tail_comment := build_tail_comment_local(prefix_units_local);
            if out_tail_comment = '' then
            begin
                Exit;
            end;

            text_local := prefix_text_local;
            path_local := prefix_path_local;
            Dec(score_local, last_segment_units_local * 520);
            Result := True;
        end;
    var
        segment_text_local: string;
        segment_score_local: Integer;
        segment_units_local: Integer;
        single_text_local: string;
        tail_comment_local: string;
    begin
        FillChar(out_candidate, SizeOf(out_candidate), 0);
        out_encoded_path := '';
        Result := False;
        if (m_dictionary = nil) or (lookup_text = '') or
            ((not is_full_pinyin_key(lookup_text)) and
            (not has_extendable_trailing_prefix_query_text(lookup_text))) or
            (not has_multi_syllable_input) or (input_syllable_count < 3) or
            (input_syllable_count > 40) or all_initial_compact_query or
            has_internal_dangling_initial or has_explicit_apostrophe_input then
        begin
            Exit;
        end;
        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        pos_local := 0;
        text_local := '';
        path_local := '';
        score_local := 76000;
        phrase_count_local := 0;
        while pos_local < Length(syllables_local) do
        begin
            if try_get_best_exact_phrase_local(pos_local, segment_text_local,
                segment_score_local, segment_units_local) then
            begin
                if (segment_units_local = 3) and
                    try_append_two_plus_one_split_local(pos_local,
                    segment_score_local) then
                begin
                    Inc(pos_local, segment_units_local);
                    Continue;
                end;
                append_segment_local(segment_text_local, segment_score_local,
                    True);
                Inc(pos_local, segment_units_local);
                Continue;
            end;

            if should_keep_tail_syllable_partial_local(pos_local) then
            begin
                Break;
            end;

            single_text_local := stable_single_text_local(
                syllables_local[pos_local].text);
            if single_text_local = '' then
            begin
                Break;
            end;

            append_segment_local(single_text_local, 900, False);
            Inc(pos_local);
        end;

        prefer_exact_three_syllable_head_local;

        if (text_local = '') or (phrase_count_local <= 0) or
            (get_candidate_text_unit_count(text_local) < 2) then
        begin
            Exit;
        end;

        tail_comment_local := build_tail_comment_local(pos_local);
        if (tail_comment_local = '') and
            try_reduce_short_composed_complete_to_prefix_partial_local(
            tail_comment_local) then
        begin
        end;
        out_candidate.text := text_local;
        out_candidate.comment := tail_comment_local;
        out_candidate.score := score_local + get_candidate_text_unit_count(
            text_local) * 256 - get_effective_compact_pinyin_unit_count(
            tail_comment_local) * 90;
        if (tail_comment_local = '') and is_user_exact_full_text_local(text_local) then
        begin
            out_candidate.source := cs_user;
            out_candidate.has_dict_weight := False;
            out_candidate.dict_weight := 0;
        end
        else
        begin
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := out_candidate.score;
        end;
        out_encoded_path := path_local;
        Result := True;
    end;

    function try_build_top_complete_phrase_chain_candidates_local(
        out out_candidates: TncCandidateList;
        out out_encoded_paths: TArray<string>): Boolean;
    const
        c_max_input_syllables = 8;
        c_max_span = 5;
        c_min_segment_units = 2;
        c_min_segment_weight = 300;
        c_segment_probe_limit = 6;
        c_max_states_per_pos = 10;
        c_output_limit = 2;
        c_path_bonus_cap = 640;
        c_path_penalty_cap = 240;
    type
        TLocalSegmentOption = record
            text: string;
            units: Integer;
            weight: Integer;
            quality_gap: Integer;
        end;
        TLocalSegmentOptionArray = TArray<TLocalSegmentOption>;
        TLocalChainState = record
            text: string;
            path: string;
            last_segment_text: string;
            last_segment_quality_gap: Integer;
            score: Integer;
            segment_count: Integer;
        end;
        TLocalChainStateArray = TArray<TLocalChainState>;
    var
        syllables_local: TncPinyinParseResult;
        state_map_local: TArray<TLocalChainStateArray>;
        pos_local: Integer;
        state_idx_local: Integer;
        span_len_local: Integer;
        option_idx_local: Integer;
        current_state_local: TLocalChainState;
        next_state_local: TLocalChainState;
        options_local: TLocalSegmentOptionArray;
        final_states_local: TLocalChainStateArray;
        output_idx_local: Integer;
        context_bonus_local: Integer;
        context_quality_gap_local: Integer;
        same_span_penalty_local: Integer;

        function build_syllable_key_local(const local_start_idx: Integer;
            const local_count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_count <= 0) or
                (local_start_idx + local_count > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function get_candidate_effective_weight_local(
            const candidate_local: TncCandidate): Integer;
        begin
            Result := candidate_local.score;
            if ((not is_fuzzy_pinyin_active) or
                (candidate_local.fuzzy_cost <= 0)) and
                candidate_local.has_dict_weight and
                (candidate_local.dict_weight > Result) then
            begin
                Result := candidate_local.dict_weight;
            end;
        end;

        procedure append_segment_option_local(var io_options:
            TLocalSegmentOptionArray; const option_local: TLocalSegmentOption);
        var
            existing_idx_local: Integer;
        begin
            if option_local.text = '' then
            begin
                Exit;
            end;

            for existing_idx_local := 0 to High(io_options) do
            begin
                if SameText(io_options[existing_idx_local].text,
                    option_local.text) then
                begin
                    if option_local.weight >
                        io_options[existing_idx_local].weight then
                    begin
                        io_options[existing_idx_local] := option_local;
                    end;
                    Exit;
                end;
            end;

            SetLength(io_options, Length(io_options) + 1);
            io_options[High(io_options)] := option_local;
        end;

        function collect_segment_options_local(const local_pos: Integer;
            const local_units: Integer;
            out out_options: TLocalSegmentOptionArray): Boolean;
        var
            key_local: string;
            results_local: TncCandidateList;
            result_idx_local: Integer;
            option_local: TLocalSegmentOption;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_weight_local: Integer;
        begin
            Result := False;
            SetLength(out_options, 0);
            if local_units < c_min_segment_units then
            begin
                Exit;
            end;

            key_local := build_syllable_key_local(local_pos, local_units);
            if (key_local = '') or
                (not dictionary_exact_lookup_cached(key_local, results_local)) then
            begin
                Exit;
            end;

            best_weight_local := Low(Integer);
            for result_idx_local := 0 to High(results_local) do
            begin
                if result_idx_local >= c_segment_probe_limit then
                begin
                    Break;
                end;
                if Trim(results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(results_local[result_idx_local].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    local_units) then
                begin
                    Continue;
                end;

                candidate_weight_local := get_candidate_effective_weight_local(
                    results_local[result_idx_local]);
                if candidate_weight_local > best_weight_local then
                begin
                    best_weight_local := candidate_weight_local;
                end;
            end;

            if best_weight_local = Low(Integer) then
            begin
                Exit;
            end;

            for result_idx_local := 0 to High(results_local) do
            begin
                if result_idx_local >= c_segment_probe_limit then
                begin
                    Break;
                end;
                if Trim(results_local[result_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(results_local[result_idx_local].text);
                if (candidate_text_local = '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    local_units) then
                begin
                    Continue;
                end;

                candidate_weight_local := get_candidate_effective_weight_local(
                    results_local[result_idx_local]);
                if candidate_weight_local < c_min_segment_weight then
                begin
                    Continue;
                end;

                option_local.text := candidate_text_local;
                option_local.units := local_units;
                option_local.weight := candidate_weight_local;
                option_local.quality_gap := Max(0,
                    best_weight_local - candidate_weight_local);
                append_segment_option_local(out_options, option_local);
            end;

            Result := Length(out_options) > 0;
        end;

        function get_path_adjustment_local(const encoded_path_local: string): Integer;
        var
            bonus_local: Integer;
            expanded_bonus_local: Integer;
            penalty_local: Integer;
            expanded_path_local: string;

            function is_splittable_function_tail_local(
                const text_value_local: string): Boolean;
            begin
                Result := (text_value_local = string(Char($53BB))) or
                    (text_value_local = string(Char($7684))) or
                    (text_value_local = string(Char($4E86))) or
                    (text_value_local = string(Char($5427))) or
                    (text_value_local = string(Char($5417)));
            end;

            function expand_function_tail_path_local(
                const path_value_local: string): string;
            var
                parts_local: TArray<string>;
                units_local: TArray<string>;
                part_idx_local: Integer;
                unit_idx_local: Integer;
                segment_text_local: string;
                head_text_local: string;
                tail_text_local: string;
            begin
                Result := '';
                if Trim(path_value_local) = '' then
                begin
                    Exit;
                end;

                parts_local := path_value_local.Split(
                    [c_segment_path_separator]);
                for part_idx_local := 0 to High(parts_local) do
                begin
                    segment_text_local := Trim(parts_local[part_idx_local]);
                    if segment_text_local = '' then
                    begin
                        Continue;
                    end;

                    units_local := split_text_units(segment_text_local);
                    if (Length(units_local) >= 3) and
                        is_splittable_function_tail_local(
                        units_local[High(units_local)]) then
                    begin
                        head_text_local := '';
                        for unit_idx_local := 0 to High(units_local) - 1 do
                        begin
                            head_text_local := head_text_local +
                                units_local[unit_idx_local];
                        end;
                        tail_text_local := units_local[High(units_local)];
                        if Result <> '' then
                        begin
                            Result := Result + c_segment_path_separator;
                        end;
                        Result := Result + head_text_local +
                            c_segment_path_separator + tail_text_local;
                    end
                    else
                    begin
                        if Result <> '' then
                        begin
                            Result := Result + c_segment_path_separator;
                        end;
                        Result := Result + segment_text_local;
                    end;
                end;
            end;
        begin
            Result := 0;
            if (m_dictionary = nil) or (lookup_text = '') or
                (encoded_path_local = '') then
            begin
                Exit;
            end;

            bonus_local := Max(0, get_cached_query_segment_path_bonus(
                lookup_text, encoded_path_local));
            Inc(bonus_local, Max(0, get_session_query_path_bonus(lookup_text,
                encoded_path_local)));
            Inc(bonus_local, Max(0, get_session_ranked_query_path_bonus(
                encoded_path_local)));
            expanded_path_local := expand_function_tail_path_local(
                encoded_path_local);
            if (expanded_path_local <> '') and
                (not SameText(expanded_path_local, encoded_path_local)) then
            begin
                expanded_bonus_local := Max(0,
                    get_cached_query_segment_path_bonus(lookup_text,
                    expanded_path_local));
                Inc(expanded_bonus_local, Max(0, get_session_query_path_bonus(
                    lookup_text, expanded_path_local)));
                Inc(expanded_bonus_local, Max(0,
                    get_session_ranked_query_path_bonus(
                    expanded_path_local)));
                if expanded_bonus_local > bonus_local then
                begin
                    bonus_local := expanded_bonus_local;
                end;
            end;
            penalty_local := Max(0, get_cached_query_segment_path_penalty(
                lookup_text, encoded_path_local));
            Inc(penalty_local, Max(0, get_session_query_path_penalty(lookup_text,
                encoded_path_local)));

            Result := Min(c_path_bonus_cap, bonus_local) -
                Min(c_path_penalty_cap, penalty_local);
        end;

        function is_user_exact_full_text_local(
            const text_value_local: string): Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (Trim(text_value_local) = '') or
                (not dictionary_exact_lookup_cached(lookup_text,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if (exact_results_local[exact_idx_local].source = cs_user) and
                    (Trim(exact_results_local[exact_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_idx_local].text),
                    Trim(text_value_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure prune_states_local(var io_states: TLocalChainStateArray);
        var
            left_idx_local: Integer;
            right_idx_local: Integer;
            tmp_state_local: TLocalChainState;
        begin
            for left_idx_local := 0 to High(io_states) - 1 do
            begin
                for right_idx_local := left_idx_local + 1 to High(io_states) do
                begin
                    if (io_states[right_idx_local].score >
                        io_states[left_idx_local].score) or
                        ((io_states[right_idx_local].score =
                        io_states[left_idx_local].score) and
                        (io_states[right_idx_local].segment_count <
                        io_states[left_idx_local].segment_count)) or
                        ((io_states[right_idx_local].score =
                        io_states[left_idx_local].score) and
                        (io_states[right_idx_local].segment_count =
                        io_states[left_idx_local].segment_count) and
                        (Length(io_states[right_idx_local].path) <
                        Length(io_states[left_idx_local].path))) then
                    begin
                        tmp_state_local := io_states[left_idx_local];
                        io_states[left_idx_local] := io_states[right_idx_local];
                        io_states[right_idx_local] := tmp_state_local;
                    end;
                end;
            end;

            if Length(io_states) > c_max_states_per_pos then
            begin
                SetLength(io_states, c_max_states_per_pos);
            end;
        end;

        procedure append_state_local(var io_states: TLocalChainStateArray;
            const state_local: TLocalChainState);
        var
            existing_idx_local: Integer;
        begin
            if (state_local.text = '') or (state_local.path = '') then
            begin
                Exit;
            end;

            for existing_idx_local := 0 to High(io_states) do
            begin
                if SameText(io_states[existing_idx_local].text,
                    state_local.text) then
                begin
                    if state_local.score > io_states[existing_idx_local].score then
                    begin
                        io_states[existing_idx_local] := state_local;
                    end;
                    prune_states_local(io_states);
                    Exit;
                end;
            end;

            SetLength(io_states, Length(io_states) + 1);
            io_states[High(io_states)] := state_local;
            prune_states_local(io_states);
        end;

        procedure append_candidate_output_local(
            const state_local: TLocalChainState);
        var
            candidate_local: TncCandidate;
            existing_idx_local: Integer;
            adjusted_score_local: Integer;
        begin
            if (state_local.text = '') or (state_local.path = '') or
                (state_local.segment_count < 2) or
                (get_candidate_text_unit_count(state_local.text) <>
                input_syllable_count) then
            begin
                Exit;
            end;

            adjusted_score_local := state_local.score +
                get_path_adjustment_local(state_local.path);

            for existing_idx_local := 0 to High(out_candidates) do
            begin
                if SameText(out_candidates[existing_idx_local].text,
                    state_local.text) then
                begin
                    if adjusted_score_local >
                        out_candidates[existing_idx_local].score then
                    begin
                        out_candidates[existing_idx_local].score :=
                            adjusted_score_local;
                        out_candidates[existing_idx_local].dict_weight :=
                            adjusted_score_local;
                        out_encoded_paths[existing_idx_local] :=
                            state_local.path;
                    end;
                    Exit;
                end;
            end;

            if Length(out_candidates) >= c_output_limit then
            begin
                Exit;
            end;

            FillChar(candidate_local, SizeOf(candidate_local), 0);
            candidate_local.text := state_local.text;
            candidate_local.comment := '';
            candidate_local.score := adjusted_score_local;
            if is_user_exact_full_text_local(state_local.text) then
            begin
                candidate_local.source := cs_user;
                candidate_local.has_dict_weight := False;
                candidate_local.dict_weight := 0;
            end
            else
            begin
                candidate_local.source := cs_rule;
                candidate_local.has_dict_weight := True;
                candidate_local.dict_weight := adjusted_score_local;
            end;

            SetLength(out_candidates, Length(out_candidates) + 1);
            SetLength(out_encoded_paths, Length(out_encoded_paths) + 1);
            out_candidates[High(out_candidates)] := candidate_local;
            out_encoded_paths[High(out_encoded_paths)] := state_local.path;
        end;

        procedure sort_outputs_local;
        var
            left_idx_local: Integer;
            right_idx_local: Integer;
            tmp_candidate_local: TncCandidate;
            tmp_path_local: string;
        begin
            for left_idx_local := 0 to High(out_candidates) - 1 do
            begin
                for right_idx_local := left_idx_local + 1 to High(out_candidates) do
                begin
                    if (out_candidates[right_idx_local].score >
                        out_candidates[left_idx_local].score) or
                        ((out_candidates[right_idx_local].score =
                        out_candidates[left_idx_local].score) and
                        (Length(out_encoded_paths[right_idx_local]) <
                        Length(out_encoded_paths[left_idx_local]))) then
                    begin
                        tmp_candidate_local := out_candidates[left_idx_local];
                        out_candidates[left_idx_local] :=
                            out_candidates[right_idx_local];
                        out_candidates[right_idx_local] := tmp_candidate_local;

                        tmp_path_local := out_encoded_paths[left_idx_local];
                        out_encoded_paths[left_idx_local] :=
                            out_encoded_paths[right_idx_local];
                        out_encoded_paths[right_idx_local] := tmp_path_local;
                    end;
                end;
            end;
        end;
    begin
        Result := False;
        SetLength(out_candidates, 0);
        SetLength(out_encoded_paths, 0);
        if (m_dictionary = nil) or (lookup_text = '') or
            (not is_full_pinyin_key(lookup_text)) or
            (not has_multi_syllable_input) or
            (input_syllable_count < 3) or
            (input_syllable_count > c_max_input_syllables) or
            all_initial_compact_query or has_internal_dangling_initial or
            has_explicit_apostrophe_input then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(lookup_text);
        if Length(syllables_local) <> input_syllable_count then
        begin
            Exit;
        end;

        SetLength(state_map_local, input_syllable_count + 1);
        SetLength(state_map_local[0], 1);
        state_map_local[0][0].text := '';
        state_map_local[0][0].path := '';
        state_map_local[0][0].last_segment_text := '';
        state_map_local[0][0].last_segment_quality_gap := 0;
        state_map_local[0][0].score := 120000;
        state_map_local[0][0].segment_count := 0;

        for pos_local := 0 to input_syllable_count - 1 do
        begin
            if Length(state_map_local[pos_local]) <= 0 then
            begin
                Continue;
            end;

            for state_idx_local := 0 to High(state_map_local[pos_local]) do
            begin
                current_state_local := state_map_local[pos_local][state_idx_local];
                for span_len_local := Min(c_max_span,
                    input_syllable_count - pos_local) downto c_min_segment_units do
                begin
                    if not collect_segment_options_local(pos_local,
                        span_len_local, options_local) then
                    begin
                        Continue;
                    end;

                    for option_idx_local := 0 to High(options_local) do
                    begin
                        next_state_local.text := current_state_local.text +
                            options_local[option_idx_local].text;
                        if current_state_local.path = '' then
                        begin
                            next_state_local.path :=
                                options_local[option_idx_local].text;
                        end
                        else
                        begin
                            next_state_local.path := current_state_local.path +
                                c_segment_path_separator +
                                options_local[option_idx_local].text;
                        end;
                        next_state_local.last_segment_text :=
                            options_local[option_idx_local].text;
                        next_state_local.last_segment_quality_gap :=
                            options_local[option_idx_local].quality_gap;
                        next_state_local.segment_count :=
                            current_state_local.segment_count + 1;
                        next_state_local.score := current_state_local.score +
                            options_local[option_idx_local].weight * 4 +
                            span_len_local * 600;
                        if options_local[option_idx_local].quality_gap >= 240 then
                        begin
                            same_span_penalty_local := Min(80000, 12000 +
                                options_local[option_idx_local].quality_gap * 80);
                            Dec(next_state_local.score, same_span_penalty_local);
                        end;
                        if (m_dictionary <> nil) and
                            (current_state_local.last_segment_text <> '') then
                        begin
                            context_bonus_local :=
                                m_dictionary.get_context_bonus(
                                current_state_local.last_segment_text,
                                options_local[option_idx_local].text);
                            context_quality_gap_local := Max(
                                current_state_local.last_segment_quality_gap,
                                options_local[option_idx_local].quality_gap);
                            if context_quality_gap_local >= 240 then
                            begin
                                context_bonus_local := Min(context_bonus_local, 240);
                            end;
                            Inc(next_state_local.score,
                                context_bonus_local * 4);
                        end;
                        append_state_local(
                            state_map_local[pos_local + span_len_local],
                            next_state_local);
                    end;
                end;
            end;
        end;

        final_states_local := state_map_local[input_syllable_count];
        if Length(final_states_local) <= 0 then
        begin
            Exit;
        end;
        prune_states_local(final_states_local);

        for output_idx_local := 0 to High(final_states_local) do
        begin
            append_candidate_output_local(final_states_local[output_idx_local]);
            if Length(out_candidates) >= c_output_limit then
            begin
                Break;
            end;
        end;
        sort_outputs_local;
        Result := Length(out_candidates) > 0;
    end;

    function try_build_exact_head_tail_complete_candidate_local(
        const head_candidates_local: TncCandidateList;
        const baseline_candidate_local: TncCandidate;
        out out_candidate_local: TncCandidate;
        out out_encoded_path_local: string): Boolean;
    const
        c_head_probe_limit = 32;
        c_tail_probe_limit = 3;
        c_min_head_units = 2;
        c_base_replacement_bonus = 8200;
        c_mismatched_head_bonus = 3600;
        c_long_head_bonus = 1800;
    var
        head_idx_local: Integer;
        tail_idx_local: Integer;
        head_text_local: string;
        tail_query_local: string;
        tail_text_local: string;
        baseline_text_local: string;
        combined_text_local: string;
        head_units_local: Integer;
        tail_units_local: Integer;
        combined_units_local: Integer;
        combined_score_local: Integer;
        tail_candidate_text_local: string;
        baseline_units_local: TArray<string>;
        baseline_unit_idx_local: Integer;
        complete_support_score_local: Integer;
        complete_support_segments_local: Integer;
        complete_support_path_local: string;
        head_weight_local: Integer;
        raw_head_weight_local: Integer;

        function get_candidate_effective_weight_local(
            const candidate_local: TncCandidate): Integer;
        begin
            Result := candidate_local.score;
            if ((not is_fuzzy_pinyin_active) or
                (candidate_local.fuzzy_cost <= 0)) and
                candidate_local.has_dict_weight and
                (candidate_local.dict_weight > Result) then
            begin
                Result := candidate_local.dict_weight;
            end;
        end;

        function get_raw_exact_head_weight_local(const head_text_value: string;
            const tail_query_value: string): Integer;
        var
            head_key_local: string;
            raw_results_local: TncCandidateList;
            raw_idx_local: Integer;
            raw_text_local: string;
        begin
            Result := Low(Integer);
            if (head_text_value = '') or (tail_query_value = '') or
                (Length(tail_query_value) >= Length(lookup_text)) or
                (Copy(lookup_text, Length(lookup_text) -
                Length(tail_query_value) + 1, Length(tail_query_value)) <>
                tail_query_value) then
            begin
                Exit;
            end;

            head_key_local := Copy(lookup_text, 1,
                Length(lookup_text) - Length(tail_query_value));
            if (head_key_local = '') or
                (not lookup_exact_full_pinyin_cached_local(head_key_local,
                raw_results_local)) then
            begin
                Exit;
            end;

            for raw_idx_local := 0 to High(raw_results_local) do
            begin
                if raw_idx_local >= c_head_probe_limit then
                begin
                    Break;
                end;
                if Trim(raw_results_local[raw_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                raw_text_local := Trim(raw_results_local[raw_idx_local].text);
                if SameText(raw_text_local, head_text_value) then
                begin
                    Result := raw_results_local[raw_idx_local].score;
                    if raw_results_local[raw_idx_local].has_dict_weight and
                        (raw_results_local[raw_idx_local].dict_weight > Result) then
                    begin
                        Result := raw_results_local[raw_idx_local].dict_weight;
                    end;
                    Exit;
                end;
            end;
        end;

        function build_tail_syllable_key_local(
            const tail_syllables_local: TncPinyinParseResult;
            const start_idx_local: Integer; const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(tail_syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    tail_syllables_local[key_idx_local].text);
            end;
        end;

        function get_tail_single_text_local(const syllable_text_local: string): string;
        var
            single_key_local: string;
            single_results_local: TncCandidateList;
            single_idx_local: Integer;
            single_text_local: string;
        begin
            Result := '';
            single_key_local := normalize_pinyin_text(syllable_text_local);
            if single_key_local = '' then
            begin
                Exit;
            end;

            Result := get_fixed_resolved_boundary_char_local(single_key_local);
            if Result <> '' then
            begin
                Exit;
            end;

            if not dictionary_exact_lookup_cached(single_key_local,
                single_results_local) then
            begin
                if not dictionary_lookup_cached(single_key_local,
                    single_results_local) then
                begin
                    Exit;
                end;
            end;
            for single_idx_local := 0 to High(single_results_local) do
            begin
                if single_idx_local >= 12 then
                begin
                    Break;
                end;
                if Trim(single_results_local[single_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                single_text_local := Trim(single_results_local[single_idx_local].text);
                if (single_text_local <> '') and
                    (get_candidate_text_unit_count(single_text_local) = 1) and
                    is_bmp_cjk_single_char_candidate(
                    single_results_local[single_idx_local]) then
                begin
                    Exit(single_text_local);
                end;
            end;
        end;

        function try_get_tail_exact_phrase_local(
            const tail_syllables_local: TncPinyinParseResult;
            const start_idx_local: Integer;
            out out_text_local: string; out out_units_local: Integer): Boolean;
        const
            c_tail_max_span = 5;
        var
            span_len_local: Integer;
            exact_key_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            out_text_local := '';
            out_units_local := 0;
            for span_len_local := Min(c_tail_max_span,
                Length(tail_syllables_local) - start_idx_local) downto 2 do
            begin
                exact_key_local := build_tail_syllable_key_local(
                    tail_syllables_local, start_idx_local, span_len_local);
                if (exact_key_local = '') or
                    (not dictionary_exact_lookup_cached(exact_key_local,
                    exact_results_local)) then
                begin
                    Continue;
                end;
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= 12 then
                    begin
                        Break;
                    end;
                    if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (exact_text_local <> '') and
                        (get_candidate_text_unit_count(exact_text_local) =
                        span_len_local) then
                    begin
                        out_text_local := exact_text_local;
                        out_units_local := span_len_local;
                        Exit(True);
                    end;
                end;
            end;
        end;

        function try_build_greedy_tail_text_local(
            const tail_query_value_local: string;
            out out_tail_text_local: string;
            out out_tail_units_local: Integer): Boolean;
        var
            tail_syllables_local: TncPinyinParseResult;
            tail_pos_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            single_text_local: string;
        begin
            Result := False;
            out_tail_text_local := '';
            out_tail_units_local := 0;
            tail_syllables_local :=
                get_effective_compact_pinyin_syllables(tail_query_value_local);
            if Length(tail_syllables_local) <= 0 then
            begin
                Exit;
            end;

            tail_pos_local := 0;
            while tail_pos_local < Length(tail_syllables_local) do
            begin
                if try_get_tail_exact_phrase_local(tail_syllables_local,
                    tail_pos_local, segment_text_local, segment_units_local) then
                begin
                    out_tail_text_local := out_tail_text_local + segment_text_local;
                    Inc(out_tail_units_local, segment_units_local);
                    Inc(tail_pos_local, segment_units_local);
                    Continue;
                end;

                single_text_local := get_tail_single_text_local(
                    tail_syllables_local[tail_pos_local].text);
                if single_text_local = '' then
                begin
                    Exit;
                end;
                out_tail_text_local := out_tail_text_local + single_text_local;
                Inc(out_tail_units_local);
                Inc(tail_pos_local);
            end;
            Result := out_tail_units_local = Length(tail_syllables_local);
        end;

        function try_build_supported_full_head_path_local(
            const full_text_local: string; out out_path_local: string;
            out out_score_local: Integer; out out_segments_local: Integer): Boolean;
        var
            full_units_local: TArray<string>;
            full_syllables_local: TncPinyinParseResult;
            prefix_len_local: Integer;
            prefix_key_local: string;
            prefix_text_local: string;
            suffix_text_local: string;
            tail_key_local: string;
            tail_text_local: string;
            tail_units_local: Integer;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;

            function join_full_units_local(const start_idx_local: Integer;
                const unit_count_local: Integer): string;
            var
                unit_idx_local: Integer;
            begin
                Result := '';
                if (unit_count_local <= 0) or (start_idx_local < 0) or
                    (start_idx_local + unit_count_local > Length(full_units_local)) then
                begin
                    Exit;
                end;

                for unit_idx_local := start_idx_local to
                    start_idx_local + unit_count_local - 1 do
                begin
                    Result := Result + full_units_local[unit_idx_local];
                end;
            end;
        begin
            Result := False;
            out_path_local := '';
            out_score_local := Low(Integer);
            out_segments_local := 0;
            full_units_local := split_text_units(Trim(full_text_local));
            full_syllables_local := get_effective_compact_pinyin_syllables(
                lookup_text);
            if (Length(full_units_local) <> input_syllable_count) or
                (Length(full_syllables_local) <> input_syllable_count) or
                (input_syllable_count < 3) then
            begin
                Exit;
            end;

            for prefix_len_local := Min(3, input_syllable_count - 1) downto 2 do
            begin
                prefix_key_local := build_tail_syllable_key_local(
                    full_syllables_local, 0, prefix_len_local);
                if (prefix_key_local = '') or
                    (not dictionary_exact_lookup_cached(prefix_key_local,
                    prefix_results_local)) then
                begin
                    Continue;
                end;

                prefix_text_local := join_full_units_local(0,
                    prefix_len_local);
                suffix_text_local := join_full_units_local(prefix_len_local,
                    input_syllable_count - prefix_len_local);
                if (prefix_text_local = '') or (suffix_text_local = '') then
                begin
                    Continue;
                end;

                candidate_score_local := Low(Integer);
                for prefix_idx_local := 0 to High(prefix_results_local) do
                begin
                    if prefix_idx_local >= 12 then
                    begin
                        Break;
                    end;
                    if Trim(prefix_results_local[prefix_idx_local].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(
                        prefix_results_local[prefix_idx_local].text);
                    if (get_candidate_text_unit_count(candidate_text_local) =
                        prefix_len_local) and
                        SameText(candidate_text_local, prefix_text_local) then
                    begin
                        candidate_score_local :=
                            prefix_results_local[prefix_idx_local].score;
                        if prefix_results_local[prefix_idx_local].has_dict_weight and
                            (prefix_results_local[prefix_idx_local].dict_weight >
                            candidate_score_local) then
                        begin
                            candidate_score_local :=
                                prefix_results_local[prefix_idx_local].dict_weight;
                        end;
                        Break;
                    end;
                end;
                if candidate_score_local = Low(Integer) then
                begin
                    Continue;
                end;

                tail_key_local := build_tail_syllable_key_local(
                    full_syllables_local, prefix_len_local,
                    input_syllable_count - prefix_len_local);
                if (tail_key_local = '') or
                    (not try_build_greedy_tail_text_local(tail_key_local,
                    tail_text_local, tail_units_local)) or
                    (tail_units_local <>
                    input_syllable_count - prefix_len_local) or
                    (not SameText(tail_text_local, suffix_text_local)) then
                begin
                    Continue;
                end;

                out_path_local := prefix_text_local +
                    c_segment_path_separator + tail_text_local;
                out_score_local := candidate_score_local +
                    prefix_len_local * 1200 + tail_units_local * 900;
                out_segments_local := 2;
                Exit(True);
            end;
        end;
    begin
        Result := False;
        FillChar(out_candidate_local, SizeOf(out_candidate_local), 0);
        out_encoded_path_local := '';
        baseline_text_local := Trim(baseline_candidate_local.text);
        baseline_units_local := split_text_units(baseline_text_local);
        if (Length(head_candidates_local) <= 0) or
            (baseline_text_local = '') or (input_syllable_count <= 3) then
        begin
            Exit;
        end;

        for head_idx_local := 0 to High(head_candidates_local) do
        begin
            if head_idx_local >= c_head_probe_limit then
            begin
                Break;
            end;

            head_text_local := Trim(head_candidates_local[head_idx_local].text);
            tail_query_local := Trim(head_candidates_local[head_idx_local].comment);
            head_units_local := get_candidate_text_unit_count(head_text_local);
            head_weight_local := get_candidate_effective_weight_local(
                head_candidates_local[head_idx_local]);
            raw_head_weight_local := get_raw_exact_head_weight_local(
                head_text_local, tail_query_local);
            if (head_units_local >= 3) and
                (((raw_head_weight_local <> Low(Integer)) and
                (raw_head_weight_local < 512)) or
                ((raw_head_weight_local = Low(Integer)) and
                (head_weight_local < 512))) then
            begin
                Continue;
            end;
            if (head_text_local <> '') and (tail_query_local = '') and
                (head_units_local = input_syllable_count) then
            begin
                if SameText(head_text_local, baseline_text_local) then
                begin
                    Continue;
                end;

                complete_support_score_local := Low(Integer);
                complete_support_segments_local := MaxInt;
                complete_support_path_local :=
                    infer_segment_path_for_query_text_with_score(lookup_text,
                    head_text_local, complete_support_score_local,
                    complete_support_segments_local);
                if (complete_support_path_local = '') or
                    (complete_support_segments_local <= 1) then
                begin
                    try_build_supported_full_head_path_local(head_text_local,
                        complete_support_path_local,
                        complete_support_score_local,
                        complete_support_segments_local);
                end;
                if (complete_support_path_local = '') or
                    (complete_support_segments_local <= 1) then
                begin
                    Continue;
                end;

                combined_score_local := baseline_candidate_local.score +
                    c_base_replacement_bonus + head_units_local * 420 +
                    Min(12000, Max(0, complete_support_score_local div 4)) -
                    head_idx_local * 120;
                if head_units_local >= 3 then
                begin
                    Inc(combined_score_local, c_long_head_bonus);
                end;

                if (not Result) or (combined_score_local >
                    out_candidate_local.score) then
                begin
                    FillChar(out_candidate_local, SizeOf(out_candidate_local), 0);
                    out_candidate_local.text := head_text_local;
                    out_candidate_local.comment := '';
                    out_candidate_local.score := combined_score_local;
                    out_candidate_local.source := cs_rule;
                    out_candidate_local.has_dict_weight := True;
                    out_candidate_local.dict_weight := combined_score_local;
                    out_encoded_path_local := complete_support_path_local;
                    Result := True;
                end;
                Continue;
            end;
            if (head_text_local = '') or (tail_query_local = '') or
                (head_units_local < c_min_head_units) or
                (not is_full_pinyin_key(tail_query_local)) then
            begin
                Continue;
            end;
            if not try_build_greedy_tail_text_local(tail_query_local,
                tail_candidate_text_local, tail_units_local) then
            begin
                if (Length(baseline_units_local) <> input_syllable_count) or
                    (head_units_local >= Length(baseline_units_local)) then
                begin
                    Continue;
                end;
                tail_candidate_text_local := '';
                for baseline_unit_idx_local := head_units_local to
                    High(baseline_units_local) do
                begin
                    tail_candidate_text_local := tail_candidate_text_local +
                        baseline_units_local[baseline_unit_idx_local];
                end;
                tail_units_local := Length(baseline_units_local) -
                    head_units_local;
            end;

            for tail_idx_local := 0 to 0 do
            begin
                tail_text_local := tail_candidate_text_local;
                combined_units_local := head_units_local + tail_units_local;
                if (tail_text_local = '') or
                    (combined_units_local <> input_syllable_count) then
                begin
                    Continue;
                end;

                combined_text_local := head_text_local + tail_text_local;
                if SameText(combined_text_local, baseline_text_local) then
                begin
                    Continue;
                end;

                combined_score_local := baseline_candidate_local.score +
                    c_base_replacement_bonus + head_units_local * 420 -
                    tail_idx_local * 180 - head_idx_local * 120;
                Inc(combined_score_local,
                    Min(7200, Max(0, (head_weight_local - 200) * 6)));
                if head_units_local >= 3 then
                begin
                    if head_weight_local >= 512 then
                    begin
                        Inc(combined_score_local, c_long_head_bonus);
                    end
                    else
                    begin
                        Dec(combined_score_local, 2600);
                    end;
                end;
                if not SameText(Copy(baseline_text_local, 1,
                    Length(head_text_local)), head_text_local) then
                begin
                    Inc(combined_score_local, c_mismatched_head_bonus);
                end;
                complete_support_score_local := 0;
                complete_support_segments_local := 0;
                complete_support_path_local :=
                    infer_segment_path_for_query_text_with_score(lookup_text,
                    combined_text_local, complete_support_score_local,
                    complete_support_segments_local);
                if (complete_support_path_local <> '') and
                    (complete_support_segments_local > 0) then
                begin
                    Inc(combined_score_local,
                        Min(36000, complete_support_score_local));
                    Inc(combined_score_local,
                        (input_syllable_count - complete_support_segments_local) * 240);
                end;

                if (not Result) or (combined_score_local >
                    out_candidate_local.score) then
                begin
                    FillChar(out_candidate_local, SizeOf(out_candidate_local), 0);
                    out_candidate_local.text := combined_text_local;
                    out_candidate_local.comment := '';
                    out_candidate_local.score := combined_score_local;
                    out_candidate_local.source := cs_rule;
                    out_candidate_local.has_dict_weight := True;
                    out_candidate_local.dict_weight := combined_score_local;
                    if complete_support_path_local <> '' then
                    begin
                        out_encoded_path_local := complete_support_path_local;
                    end
                    else
                    begin
                        out_encoded_path_local := head_text_local +
                            c_segment_path_separator + tail_text_local;
                    end;
                    Result := True;
                end;
            end;
        end;
    end;

    function try_pick_supported_complete_candidate_local(
        const candidates_local: TncCandidateList;
        const baseline_candidate_local: TncCandidate;
        const baseline_encoded_path_local: string;
        out out_candidate_local: TncCandidate;
        out out_encoded_path_local: string): Boolean;
    const
        c_probe_limit = 18;
        c_score_margin = 300;
        c_strong_segment_gap = 2;
    var
        idx_local: Integer;
        probe_limit_local: Integer;
        candidate_text_local: string;
        baseline_text_local: string;
        candidate_path_local: string;
        baseline_path_local: string;
        candidate_support_score_local: Integer;
        baseline_support_score_local: Integer;
        candidate_segment_count_local: Integer;
        baseline_segment_count_local: Integer;
        candidate_units_local: Integer;
        baseline_units_local: Integer;
        candidate_rank_local: Integer;
        baseline_rank_local: Integer;
        best_rank_local: Integer;
        candidate_local: TncCandidate;
        path_syllables_local: TncPinyinParseResult;
        candidate_identity_key_local: string;

        function is_function_or_modifier_text_local(
            const value_local: string): Boolean;
        begin
            Result := False;
            if Length(value_local) <> 1 then
            begin
                Exit;
            end;

            case Ord(value_local[1]) of
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168, $8D85,
                $771F, $597D, $5982, $633A, $8F83, $6781:
                    Result := True;
            end;
        end;

        function build_path_syllable_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(path_syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    path_syllables_local[key_idx_local].text);
            end;
        end;

        function get_exact_text_weight_local(const start_idx_local: Integer;
            const count_local: Integer; const expected_text_local: string;
            out out_weight_local: Integer): Boolean;
        var
            key_local: string;
            results_local: TncCandidateList;
            idx_local: Integer;
            text_local: string;
            weight_local: Integer;
        begin
            Result := False;
            out_weight_local := 0;
            key_local := build_path_syllable_key_local(start_idx_local,
                count_local);
            if (key_local = '') or
                (not dictionary_exact_lookup_cached(key_local, results_local)) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(results_local) do
            begin
                if idx_local >= 12 then
                begin
                    Break;
                end;
                if Trim(results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                text_local := Trim(results_local[idx_local].text);
                if (text_local = '') or
                    (get_candidate_text_unit_count(text_local) <> count_local) or
                    ((expected_text_local <> '') and
                    (not SameText(text_local, expected_text_local))) then
                begin
                    Continue;
                end;
                weight_local := results_local[idx_local].score;
                if results_local[idx_local].has_dict_weight and
                    (results_local[idx_local].dict_weight > weight_local) then
                begin
                    weight_local := results_local[idx_local].dict_weight;
                end;
                if (not Result) or (weight_local > out_weight_local) then
                begin
                    Result := True;
                    out_weight_local := weight_local;
                end;
            end;
        end;

        function get_structural_path_quality_local(
            const encoded_path_local: string): Integer;
        var
            parts_local: TArray<string>;
            part_idx_local: Integer;
            pos_local: Integer;
            units_local: TArray<string>;
            unit_count_local: Integer;
            segment_text_local: string;
            segment_weight_local: Integer;
            best_segment_weight_local: Integer;
            left_weight_local: Integer;
            right_weight_local: Integer;
        begin
            Result := 0;
            if (Trim(encoded_path_local) = '') or
                (Length(path_syllables_local) = 0) then
            begin
                Exit;
            end;

            parts_local := encoded_path_local.Split([c_segment_path_separator]);
            pos_local := 0;
            for part_idx_local := 0 to High(parts_local) do
            begin
                segment_text_local := Trim(parts_local[part_idx_local]);
                if segment_text_local = '' then
                begin
                    Continue;
                end;
                units_local := split_text_units(segment_text_local);
                unit_count_local := Length(units_local);
                if (unit_count_local <= 0) or
                    (pos_local + unit_count_local > Length(path_syllables_local)) then
                begin
                    Exit;
                end;

                if get_exact_text_weight_local(pos_local, unit_count_local,
                    segment_text_local, segment_weight_local) then
                begin
                    if get_exact_text_weight_local(pos_local, unit_count_local, '',
                        best_segment_weight_local) and
                        (best_segment_weight_local >=
                        segment_weight_local + 240) then
                    begin
                        Dec(Result, Min(80000, 12000 +
                            (best_segment_weight_local -
                            segment_weight_local) * 80));
                    end;

                    if unit_count_local >= 4 then
                    begin
                        Inc(Result, 20000 + (unit_count_local - 4) * 3000);
                    end
                    else if (unit_count_local = 3) and
                        (segment_weight_local >= 1000) then
                    begin
                        Inc(Result, 1200);
                    end;

                    if (unit_count_local = 3) and
                        (segment_weight_local < 1000) then
                    begin
                        if get_exact_text_weight_local(pos_local, 2, '',
                            left_weight_local) and
                            (left_weight_local >= Max(700,
                            segment_weight_local + 180)) then
                        begin
                            Dec(Result, 20000);
                        end
                        else if is_function_or_modifier_text_local(
                            units_local[0]) and
                            get_exact_text_weight_local(pos_local + 1, 2, '',
                            right_weight_local) and
                            (right_weight_local >= Max(700,
                            segment_weight_local + 120)) then
                        begin
                            Dec(Result, 20000);
                        end;
                    end;
                end;

                Inc(pos_local, unit_count_local);
            end;
        end;

        function support_rank_local(const support_score_local: Integer;
            const segment_count_local: Integer;
            const candidate_score_local: Integer;
            const encoded_path_local: string): Integer;
        begin
            Result := support_score_local + candidate_score_local div 24;
            if segment_count_local > 0 then
            begin
                Inc(Result, (input_syllable_count - segment_count_local) * 500);
            end;
            Inc(Result, get_structural_path_quality_local(encoded_path_local));
        end;

        function is_promotable_complete_local(
            const candidate_value_local: TncCandidate;
            const candidate_text_value_local: string): Boolean;
        begin
            Result := False;
            if (Trim(candidate_value_local.comment) <> '') or
                (candidate_text_value_local = '') or
                SameText(candidate_text_value_local, baseline_text_local) then
            begin
                Exit;
            end;
            if is_runtime_chain_candidate(candidate_value_local) or
                is_runtime_common_pattern_candidate(candidate_value_local) or
                is_runtime_redup_candidate(candidate_value_local) then
            begin
                Exit;
            end;
            Result := get_candidate_text_unit_count(candidate_text_value_local) =
                input_syllable_count;
        end;
    begin
        Result := False;
        FillChar(out_candidate_local, SizeOf(out_candidate_local), 0);
        out_encoded_path_local := '';

        if (Length(candidates_local) <= 0) or (input_syllable_count < 4) or
            (input_syllable_count > 24) then
        begin
            Exit;
        end;

        baseline_text_local := Trim(baseline_candidate_local.text);
        baseline_units_local := get_candidate_text_unit_count(baseline_text_local);
        if (baseline_text_local = '') or
            (baseline_units_local <> input_syllable_count) then
        begin
            Exit;
        end;
        path_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);

        baseline_path_local := infer_segment_path_for_query_text_with_score(
            lookup_text, baseline_text_local, baseline_support_score_local,
            baseline_segment_count_local);
        if (baseline_path_local = '') and
            (m_current_segment_path_map <> nil) then
        begin
            candidate_identity_key_local := build_candidate_identity_key(
                baseline_candidate_local.text, baseline_candidate_local.comment);
            if candidate_identity_key_local <> '' then
            begin
                m_current_segment_path_map.TryGetValue(
                    candidate_identity_key_local, baseline_path_local);
            end;
        end;
        if baseline_path_local = '' then
        begin
            baseline_path_local := baseline_encoded_path_local;
            baseline_segment_count_local :=
                get_encoded_path_segment_count_local(baseline_path_local);
            baseline_support_score_local := baseline_candidate_local.score;
        end;
        if baseline_segment_count_local <= 0 then
        begin
            Exit;
        end;
        baseline_rank_local := support_rank_local(baseline_support_score_local,
            baseline_segment_count_local, baseline_candidate_local.score,
            baseline_path_local);

        best_rank_local := Low(Integer);
        probe_limit_local := Min(High(candidates_local), c_probe_limit - 1);
        for idx_local := 0 to probe_limit_local do
        begin
            candidate_local := candidates_local[idx_local];
            candidate_text_local := Trim(candidate_local.text);
            if not is_promotable_complete_local(candidate_local,
                candidate_text_local) then
            begin
                Continue;
            end;

            candidate_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text_local, candidate_support_score_local,
                candidate_segment_count_local);
            if (candidate_path_local = '') and
                (m_current_segment_path_map <> nil) then
            begin
                candidate_identity_key_local := build_candidate_identity_key(
                    candidate_local.text, candidate_local.comment);
                if candidate_identity_key_local <> '' then
                begin
                    m_current_segment_path_map.TryGetValue(
                        candidate_identity_key_local, candidate_path_local);
                    if candidate_path_local <> '' then
                    begin
                        candidate_segment_count_local :=
                            get_encoded_path_segment_count_local(
                            candidate_path_local);
                        candidate_support_score_local := candidate_local.score;
                    end;
                end;
            end;
            if (candidate_path_local = '') or
                (candidate_segment_count_local <= 0) then
            begin
                Continue;
            end;

            candidate_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            if candidate_units_local <> input_syllable_count then
            begin
                Continue;
            end;

            candidate_rank_local := support_rank_local(
                candidate_support_score_local, candidate_segment_count_local,
                candidate_local.score, candidate_path_local);
            if (candidate_segment_count_local <= baseline_segment_count_local -
                c_strong_segment_gap) then
            begin
                Inc(candidate_rank_local, 6000);
            end
            else if candidate_segment_count_local < baseline_segment_count_local then
            begin
                Inc(candidate_rank_local, 3200);
            end
            else if candidate_segment_count_local >
                baseline_segment_count_local + 1 then
            begin
                Dec(candidate_rank_local, 2800);
            end;

            if (candidate_rank_local <= baseline_rank_local + c_score_margin) and
                (candidate_segment_count_local >= baseline_segment_count_local) then
            begin
                Continue;
            end;

            if (not Result) or (candidate_rank_local > best_rank_local) then
            begin
                Result := True;
                best_rank_local := candidate_rank_local;
                out_candidate_local := candidate_local;
                out_candidate_local.comment := '';
                if out_candidate_local.score <= baseline_candidate_local.score then
                begin
                    out_candidate_local.score := baseline_candidate_local.score +
                        Min(12000, Max(1600, candidate_rank_local -
                        baseline_rank_local));
                end;
                out_candidate_local.has_dict_weight := True;
                out_candidate_local.dict_weight := out_candidate_local.score;
                out_encoded_path_local := candidate_path_local;
            end;
        end;
    end;

    procedure ensure_fast_tail_exact_completion_visible_local(
        var candidates: TncCandidateList);
    const
        c_candidate_probe_limit = 18;
        c_tail_probe_limit = 12;
        c_tail_max_syllables = 4;
    var
        idx_local: Integer;
        tail_idx_local: Integer;
        existing_idx_local: Integer;
        move_idx_local: Integer;
        prefix_text_local: string;
        tail_key_local: string;
        tail_units_local: Integer;
        prefix_units_local: Integer;
        tail_results_local: TncCandidateList;
        tail_text_local: string;
        tail_weight_local: Integer;
        best_text_local: string;
        best_path_local: string;
        best_score_local: Integer;
        candidate_score_local: Integer;
        candidate_path_local: string;
        completion_text_local: string;
        completion_candidate_local: TncCandidate;
        support_path_local: string;
        support_score_local: Integer;
        support_segments_local: Integer;
    begin
        if (Length(candidates) = 0) or (m_dictionary = nil) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_text_local := '';
        best_path_local := '';
        best_score_local := Low(Integer);
        for idx_local := 0 to Min(High(candidates),
            c_candidate_probe_limit - 1) do
        begin
            prefix_text_local := Trim(candidates[idx_local].text);
            tail_key_local := normalize_pinyin_text(
                Trim(candidates[idx_local].comment));
            if (prefix_text_local = '') or (tail_key_local = '') or
                (not is_full_pinyin_key(tail_key_local)) then
            begin
                Continue;
            end;

            prefix_units_local := get_candidate_text_unit_count(
                prefix_text_local);
            tail_units_local := get_effective_compact_pinyin_unit_count(
                tail_key_local);
            if (prefix_units_local < 2) or (tail_units_local < 1) or
                (tail_units_local > c_tail_max_syllables) or
                (prefix_units_local + tail_units_local <> input_syllable_count) then
            begin
                Continue;
            end;

            if not lookup_exact_full_pinyin_cached_local(tail_key_local,
                tail_results_local) then
            begin
                Continue;
            end;

            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if tail_idx_local >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(tail_results_local[tail_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                if (tail_text_local = '') or
                    (get_candidate_text_unit_count(tail_text_local) <>
                    tail_units_local) then
                begin
                    Continue;
                end;

                tail_weight_local := tail_results_local[tail_idx_local].score;
                if tail_results_local[tail_idx_local].has_dict_weight and
                    (tail_results_local[tail_idx_local].dict_weight >
                    tail_weight_local) then
                begin
                    tail_weight_local :=
                        tail_results_local[tail_idx_local].dict_weight;
                end;
                if tail_weight_local < 260 then
                begin
                    Continue;
                end;

                completion_text_local := prefix_text_local + tail_text_local;
                if (Trim(candidates[0].comment) = '') and
                    SameText(Trim(candidates[0].text), completion_text_local) then
                begin
                    Continue;
                end;

                candidate_score_local := prefix_units_local * 800 +
                    tail_units_local * 3000 + tail_weight_local * 4 +
                    Min(8000, Max(0, candidates[idx_local].score div 4));
                support_path_local := infer_segment_path_for_query_text_with_score(
                    lookup_text, completion_text_local, support_score_local,
                    support_segments_local);
                if (support_path_local <> '') and
                    (support_segments_local > 0) then
                begin
                    Inc(candidate_score_local, support_score_local);
                    Inc(candidate_score_local,
                        (input_syllable_count - support_segments_local) * 1200);
                end
                else
                begin
                    Dec(candidate_score_local, 2400);
                end;
                if candidate_score_local > best_score_local then
                begin
                    best_score_local := candidate_score_local;
                    best_text_local := completion_text_local;
                    if support_path_local <> '' then
                    begin
                        best_path_local := support_path_local;
                    end
                    else
                    begin
                        candidate_path_local := get_segment_path_for_candidate(
                            candidates[idx_local]);
                        if candidate_path_local = '' then
                        begin
                            candidate_path_local := prefix_text_local;
                        end;
                        best_path_local := candidate_path_local +
                            c_segment_path_separator + tail_text_local;
                    end;
                end;
            end;
        end;

        if best_text_local = '' then
        begin
            Exit;
        end;

        existing_idx_local := -1;
        for idx_local := 0 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) = '') and
                SameText(Trim(candidates[idx_local].text), best_text_local) then
            begin
                existing_idx_local := idx_local;
                Break;
            end;
        end;

        if existing_idx_local >= 0 then
        begin
            completion_candidate_local := candidates[existing_idx_local];
        end
        else
        begin
            FillChar(completion_candidate_local,
                SizeOf(completion_candidate_local), 0);
            completion_candidate_local.text := best_text_local;
            completion_candidate_local.comment := '';
            completion_candidate_local.source := cs_rule;
        end;

        completion_candidate_local.score := Max(candidates[0].score + 2048,
            best_score_local);
        completion_candidate_local.has_dict_weight := True;
        completion_candidate_local.dict_weight := completion_candidate_local.score;
        remember_segment_path_for_candidate(completion_candidate_local.text, '',
            best_path_local, completion_candidate_local.score);

        if existing_idx_local >= 0 then
        begin
            for move_idx_local := existing_idx_local downto 1 do
            begin
                candidates[move_idx_local] := candidates[move_idx_local - 1];
            end;
            candidates[0] := completion_candidate_local;
            Exit;
        end;

        SetLength(candidates, Length(candidates) + 1);
        for move_idx_local := High(candidates) downto 1 do
        begin
            candidates[move_idx_local] := candidates[move_idx_local - 1];
        end;
        candidates[0] := completion_candidate_local;
    end;

    procedure promote_best_supported_complete_candidate_visible_local(
        var candidates: TncCandidateList);
    const
        c_candidate_probe_limit = 12;
        c_promote_margin = 240;
    var
        idx_local: Integer;
        move_idx_local: Integer;
        best_idx_local: Integer;
        top_rank_local: Integer;
        top_segments_local: Integer;
        best_rank_local: Integer;
        support_score_local: Integer;
        support_segments_local: Integer;
        candidate_rank_local: Integer;
        candidate_text_local: string;
        support_path_local: string;
        path_hint_local: string;
        support_bonus_local: Integer;
        hint_bonus_local: Integer;
        best_candidate_local: TncCandidate;
        path_syllables_local: TncPinyinParseResult;

        function is_modifier_single_text_local(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $662F, $7684, $4E86, $5728, $548C, $4E0D, $518D, $9AD8,
                $5F88, $592A, $6700, $66F4, $6CA1, $8981, $4F1A, $80FD,
                $628A, $88AB, $8BA9, $7ED9, $5982, $8001, $5C0F, $5927,
                $5168, $8D85, $771F, $597D, $53C8, $4E5F, $5DF2, $5C06,
                $53EF, $60F3, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $65B0, $633A,
                $8F83, $9887, $6781, $5411, $5BF9, $4ECE, $4E8E, $4E0E,
                $800C, $5E76:
                    Result := True;
            end;
        end;

        function get_modifier_head_tail_path_bonus_local(
            const encoded_path: string): Integer;
        var
            path_parts_local: TArray<string>;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := 0;
            if encoded_path = '' then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            if Length(path_parts_local) <> 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(path_parts_local[0]);
            tail_text_local := Trim(path_parts_local[1]);
            if is_modifier_single_text_local(head_text_local) and
                (get_candidate_text_unit_count(tail_text_local) >= 2) then
            begin
                Result := 3500;
            end;
        end;

        function get_long_exact_segment_path_bonus_local(
            const encoded_path: string): Integer;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            unit_count_local: Integer;
        begin
            Result := 0;
            if encoded_path = '' then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                unit_count_local := get_candidate_text_unit_count(
                    Trim(path_parts_local[part_idx_local]));
                if unit_count_local >= 4 then
                begin
                    Inc(Result, 50000 + (unit_count_local - 4) * 8000);
                end;
            end;
        end;

        function build_path_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(path_syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    path_syllables_local[key_idx_local].text);
            end;
        end;

        function get_best_exact_weight_for_path_span_local(
            const start_idx_local: Integer; const count_local: Integer;
            const expected_text_local: string): Integer;
        var
            key_local: string;
            results_local: TncCandidateList;
            idx_local: Integer;
            text_local: string;
            weight_local: Integer;
        begin
            Result := Low(Integer);
            key_local := build_path_key_local(start_idx_local, count_local);
            if (key_local = '') or
                (not dictionary_exact_lookup_cached(key_local, results_local)) then
            begin
                Exit;
            end;
            for idx_local := 0 to High(results_local) do
            begin
                if idx_local >= 12 then
                begin
                    Break;
                end;
                if Trim(results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                text_local := Trim(results_local[idx_local].text);
                if (text_local = '') or
                    (get_candidate_text_unit_count(text_local) <> count_local) or
                    ((expected_text_local <> '') and
                    (not SameText(text_local, expected_text_local))) then
                begin
                    Continue;
                end;
                weight_local := results_local[idx_local].score;
                if results_local[idx_local].has_dict_weight and
                    (results_local[idx_local].dict_weight > weight_local) then
                begin
                    weight_local := results_local[idx_local].dict_weight;
                end;
                if weight_local > Result then
                begin
                    Result := weight_local;
                end;
            end;
        end;

        function get_suspicious_three_segment_path_penalty_local(
            const encoded_path: string): Integer;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            pos_local: Integer;
            segment_text_local: string;
            unit_count_local: Integer;
            segment_weight_local: Integer;
            split_weight_local: Integer;
        begin
            Result := 0;
            if (encoded_path = '') or (Length(path_syllables_local) = 0) then
            begin
                Exit;
            end;
            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            pos_local := 0;
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                unit_count_local := get_candidate_text_unit_count(segment_text_local);
                if (unit_count_local <= 0) or
                    (pos_local + unit_count_local > Length(path_syllables_local)) then
                begin
                    Exit;
                end;
                if unit_count_local = 3 then
                begin
                    segment_weight_local :=
                        get_best_exact_weight_for_path_span_local(pos_local, 3,
                        segment_text_local);
                    split_weight_local :=
                        get_best_exact_weight_for_path_span_local(pos_local, 2, '');
                    if (segment_weight_local < 700) and
                        (split_weight_local >= Max(700,
                        segment_weight_local + 180)) then
                    begin
                        Inc(Result, 50000);
                    end;
                end;
                Inc(pos_local, unit_count_local);
            end;
        end;

        function get_same_span_competitor_path_penalty_local(
            const encoded_path: string): Integer;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            pos_local: Integer;
            segment_text_local: string;
            unit_count_local: Integer;
            segment_weight_local: Integer;
            best_segment_weight_local: Integer;
            weight_gap_local: Integer;
        begin
            Result := 0;
            if (encoded_path = '') or (Length(path_syllables_local) = 0) then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            pos_local := 0;
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                unit_count_local := get_candidate_text_unit_count(segment_text_local);
                if (unit_count_local <= 0) or
                    (pos_local + unit_count_local > Length(path_syllables_local)) then
                begin
                    Exit;
                end;

                if get_best_exact_weight_for_path_span_local(pos_local,
                    unit_count_local, segment_text_local) <>
                    Low(Integer) then
                begin
                    segment_weight_local :=
                        get_best_exact_weight_for_path_span_local(pos_local,
                        unit_count_local, segment_text_local);
                    best_segment_weight_local :=
                        get_best_exact_weight_for_path_span_local(pos_local,
                        unit_count_local, '');
                    if best_segment_weight_local <> Low(Integer) then
                    begin
                        weight_gap_local := best_segment_weight_local -
                            segment_weight_local;
                        if weight_gap_local >= 240 then
                        begin
                            Inc(Result, Min(80000, 12000 +
                                weight_gap_local * 80));
                        end;
                    end;
                end;

                Inc(pos_local, unit_count_local);
            end;
        end;
    begin
        if (Length(candidates) <= 1) or (m_dictionary = nil) or
            (input_syllable_count < 3) or (input_syllable_count > 24) or
            (not is_full_pinyin_key(lookup_text)) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        top_rank_local := Low(Integer);
        top_segments_local := 0;
        path_syllables_local := get_effective_compact_pinyin_syllables(
            lookup_text);
        for idx_local := 0 to Min(High(candidates),
            c_candidate_probe_limit - 1) do
        begin
            candidate_text_local := Trim(candidates[idx_local].text);
            if (candidate_text_local = '') or
                (Trim(candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                input_syllable_count) then
            begin
                Continue;
            end;
            if (candidates[idx_local].source <> cs_user) and
                is_unsupported_short_four_ab_cd_text_local(
                candidate_text_local) then
            begin
                Continue;
            end;

            support_path_local := infer_segment_path_for_query_text_with_score(
                lookup_text, candidate_text_local, support_score_local,
                support_segments_local);
            path_hint_local := get_segment_path_for_candidate(
                candidates[idx_local], idx_local);
            if (support_path_local = '') or (support_segments_local <= 0) then
            begin
                support_path_local := path_hint_local;
                support_segments_local :=
                    get_encoded_path_segment_count_local(support_path_local);
                support_score_local := candidates[idx_local].score;
                if (support_path_local = '') or (support_segments_local <= 0) then
                begin
                    Continue;
                end;
            end;

            candidate_rank_local := support_score_local +
                candidates[idx_local].score div 32 - support_segments_local * 120;
            support_bonus_local :=
                get_modifier_head_tail_path_bonus_local(support_path_local);
            hint_bonus_local :=
                get_modifier_head_tail_path_bonus_local(path_hint_local);
            Inc(candidate_rank_local, Max(support_bonus_local, hint_bonus_local));
            Inc(candidate_rank_local, Max(
                get_long_exact_segment_path_bonus_local(support_path_local),
                get_long_exact_segment_path_bonus_local(path_hint_local)));
            Dec(candidate_rank_local, Max(
                get_suspicious_three_segment_path_penalty_local(support_path_local),
                get_suspicious_three_segment_path_penalty_local(path_hint_local)));
            Dec(candidate_rank_local, Max(
                get_same_span_competitor_path_penalty_local(support_path_local),
                get_same_span_competitor_path_penalty_local(path_hint_local)));
            if idx_local = 0 then
            begin
                top_rank_local := candidate_rank_local;
                top_segments_local := support_segments_local;
            end
            else if (top_segments_local > 0) and
                (support_segments_local < top_segments_local) then
            begin
                Inc(candidate_rank_local,
                    (top_segments_local - support_segments_local) * 1600);
            end;
            if (best_idx_local < 0) or
                (candidate_rank_local > best_rank_local) then
            begin
                best_idx_local := idx_local;
                best_rank_local := candidate_rank_local;
                remember_segment_path_for_candidate(candidates[idx_local].text,
                    candidates[idx_local].comment, support_path_local,
                    candidates[idx_local].score);
            end;
        end;

        if (best_idx_local <= 0) or
            (best_rank_local <= top_rank_local + c_promote_margin) then
        begin
            Exit;
        end;

        best_candidate_local := candidates[best_idx_local];
        if best_candidate_local.score <= candidates[0].score then
        begin
            best_candidate_local.score := candidates[0].score + 1024;
            best_candidate_local.has_dict_weight := True;
            best_candidate_local.dict_weight := best_candidate_local.score;
        end;
        for move_idx_local := best_idx_local downto 1 do
        begin
            candidates[move_idx_local] := candidates[move_idx_local - 1];
        end;
        candidates[0] := best_candidate_local;
    end;

    procedure demote_low_evidence_admin_place_alias_top_local(
        var candidates: TncCandidateList);
    var
        top_rank_local: Integer;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
        best_idx_local: Integer;
        idx_local: Integer;
        move_idx_local: Integer;
        moved_candidate_local: TncCandidate;
        moved_path_local: string;

        function is_low_evidence_admin_alias_candidate_local(
            const candidate: TncCandidate): Boolean;
        begin
            Result := (m_dictionary <> nil) and (lookup_text <> '') and
                (Trim(candidate.comment) = '') and
                is_low_evidence_admin_place_alias_cached(
                lookup_text, Trim(candidate.text));
        end;
    begin
        if (Length(candidates) <= 1) or
            (not is_low_evidence_admin_alias_candidate_local(candidates[0])) then
        begin
            Exit;
        end;

        top_rank_local := get_rank_score(candidates[0]);
        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx_local := 1 to High(candidates) do
        begin
            if (Trim(candidates[idx_local].comment) <> '') or
                is_low_evidence_admin_alias_candidate_local(candidates[idx_local]) then
            begin
                Continue;
            end;
            candidate_rank_local := get_rank_score(candidates[idx_local]);
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx_local;
            end;
        end;

        if (best_idx_local <= 0) or (best_rank_local <= top_rank_local) then
        begin
            Exit;
        end;

        moved_candidate_local := candidates[best_idx_local];
        moved_path_local := '';
        if best_idx_local < Length(m_candidate_segment_paths) then
        begin
            moved_path_local := m_candidate_segment_paths[best_idx_local];
        end;
        for move_idx_local := best_idx_local downto 1 do
        begin
            candidates[move_idx_local] := candidates[move_idx_local - 1];
            if move_idx_local < Length(m_candidate_segment_paths) then
            begin
                if (move_idx_local - 1) < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx_local] :=
                        m_candidate_segment_paths[move_idx_local - 1];
                end
                else
                begin
                    m_candidate_segment_paths[move_idx_local] := '';
                end;
            end;
        end;
        candidates[0] := moved_candidate_local;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := moved_path_local;
        end;
    end;

    procedure ensure_short_lexicon_prefix_completions_visible_local(
        var candidates: TncCandidateList);
    var
        evidence_local: TncOneKeyCompletionList;
        ranks_local: TArray<Int64>;
        distant_flags_local: TArray<Boolean>;
        exact_flags_local: TArray<Boolean>;
        item_local: TncOneKeyCompletion;
        candidate_local: TncCandidate;
        units_local: TArray<string>;
        prefix_local: string;
        anchored_local, distant_local, exact_local: Boolean;
        rank_local: Int64;
        idx_local, evidence_idx_local, unit_idx_local, insert_idx_local, move_idx_local: Integer;
        count_local, keep_local, distant_count_local, predicted_count_local: Integer;

        function has_visible_exact_prefix_local(const text_value: string): Boolean;
        var visible_idx: Integer;
        begin
            Result := False;
            for visible_idx := 0 to High(candidates) do
                if (Trim(candidates[visible_idx].comment) = '') and
                    (get_candidate_text_unit_count(candidates[visible_idx].text) =
                    input_syllable_count) and
                    SameText(Trim(candidates[visible_idx].text), text_value) then
                    Exit(True);
        end;

        function already_collected_local(const text_value: string): Boolean;
        var collected_idx: Integer;
        begin
            Result := False;
            for collected_idx := 0 to
                High(m_short_exact_lexicon_completion_candidates) do
                if SameText(m_short_exact_lexicon_completion_candidates[
                    collected_idx].text, text_value) then Exit(True);
        end;
    begin
        if (m_dictionary = nil) or (lookup_text = '') or
            (input_syllable_count < 2) then Exit;
        // An empty admitted pool is still authoritative. Later display passes
        // must not refill it from the old, unfiltered prefix probe.
        m_short_exact_lexicon_completion_query := normalize_pinyin_text(lookup_text);
        SetLength(m_short_exact_lexicon_completion_candidates, 0);
        SetLength(m_short_exact_lexicon_completion_texts, 0);
        if not m_dictionary.lookup_candidate_prefix_completions(lookup_text,
            evidence_local) then Exit;
        for evidence_idx_local := 0 to High(evidence_local) do
        begin
            item_local := evidence_local[evidence_idx_local];
            if item_local.source <> okcs_base_exact then Continue;
            item_local.text := Trim(item_local.text);
            exact_local := (item_local.full_pinyin <> '') and
                SameText(item_local.full_pinyin, lookup_text);
            units_local := split_text_units(item_local.text);
            if (Length(units_local) < input_syllable_count) and
                (not exact_local) then Continue;
            if (Length(units_local) = input_syllable_count) and
                has_visible_exact_prefix_local(item_local.text) then Continue;
            if already_collected_local(item_local.text) then Continue;
            prefix_local := '';
            for unit_idx_local := 0 to Min(input_syllable_count,
                Length(units_local)) - 1 do
                prefix_local := prefix_local + units_local[unit_idx_local];
            anchored_local := has_visible_exact_prefix_local(prefix_local);
            distant_local :=
                (not exact_local) and
                (input_syllable_count < c_long_sentence_full_path_min_syllables) and
                (Length(units_local) >= c_long_sentence_full_path_min_syllables);
            // A compact exact may use a different syllable split from the
            // primary parser. Restore it independently of prediction budgets.
            if exact_local then rank_local := Int64(High(Integer)) * 1000000 + item_local.weight
            else if not nc_prefix_completion_rank(item_local, input_syllable_count,
                Length(units_local), anchored_local, distant_local,
                rank_local) then Continue;

            insert_idx_local := 0;
            count_local := Length(m_short_exact_lexicon_completion_candidates);
            while insert_idx_local < count_local do
            begin
                if distant_flags_local[insert_idx_local] <> distant_local then
                begin
                    if not distant_local then Break;
                end
                else if rank_local > ranks_local[insert_idx_local] then Break;
                Inc(insert_idx_local);
            end;
            SetLength(m_short_exact_lexicon_completion_candidates, count_local + 1);
            SetLength(ranks_local, count_local + 1);
            SetLength(distant_flags_local, count_local + 1);
            SetLength(exact_flags_local, count_local + 1);
            for move_idx_local := count_local downto insert_idx_local + 1 do
            begin
                m_short_exact_lexicon_completion_candidates[move_idx_local] :=
                    m_short_exact_lexicon_completion_candidates[move_idx_local - 1];
                ranks_local[move_idx_local] := ranks_local[move_idx_local - 1];
                distant_flags_local[move_idx_local] := distant_flags_local[move_idx_local - 1];
                exact_flags_local[move_idx_local] := exact_flags_local[move_idx_local - 1];
            end;
            candidate_local := Default(TncCandidate);
            candidate_local.text := item_local.text;
            candidate_local.source := cs_rule;
            candidate_local.has_dict_weight := True;
            candidate_local.dict_weight := item_local.weight;
            candidate_local.score := item_local.weight;
            m_short_exact_lexicon_completion_candidates[insert_idx_local] := candidate_local;
            ranks_local[insert_idx_local] := rank_local;
            distant_flags_local[insert_idx_local] := distant_local;
            exact_flags_local[insert_idx_local] := exact_local;
        end;

        keep_local := 0;
        distant_count_local := 0;
        predicted_count_local := 0;
        for idx_local := 0 to High(m_short_exact_lexicon_completion_candidates) do
        begin
            if (not exact_flags_local[idx_local]) and
                (predicted_count_local >= c_short_prefix_completion_limit) then Continue;
            if distant_flags_local[idx_local] then
            begin
                if distant_count_local >= c_short_long_prefix_completion_limit then Continue;
                Inc(distant_count_local);
            end;
            m_short_exact_lexicon_completion_candidates[keep_local] :=
                m_short_exact_lexicon_completion_candidates[idx_local];
            if not exact_flags_local[idx_local] then Inc(predicted_count_local);
            Inc(keep_local);
        end;
        SetLength(m_short_exact_lexicon_completion_candidates, keep_local);
        SetLength(m_short_exact_lexicon_completion_texts, keep_local);
        for idx_local := 0 to keep_local - 1 do
            m_short_exact_lexicon_completion_texts[idx_local] :=
                m_short_exact_lexicon_completion_candidates[idx_local].text;
    end;

    procedure load_literal_user_candidates_local;
    var
        phase_tick_local: UInt64;
    begin
        SetLength(literal_user_candidates, 0);
        if (m_dictionary = nil) or (lookup_text = '') then
        begin
            Exit;
        end;

        phase_tick_local := nc_monotonic_tick_ms;
        m_dictionary.lookup_literal_user_words(lookup_text,
            literal_user_candidates);
        Inc(lookup_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_tick_local));
    end;

    procedure inject_literal_user_candidates_local;
    var
        total_limit_local: Integer;
    begin
        if Length(literal_user_candidates) = 0 then
        begin
            Exit;
        end;

        m_candidates := merge_candidate_lists(literal_user_candidates,
            m_candidates, 0);
        total_limit_local := get_total_candidate_limit;
        if (total_limit_local > 0) and
            (Length(m_candidates) > total_limit_local) then
        begin
            SetLength(m_candidates, total_limit_local);
        end;

        m_last_lookup_literal_user_fast := True;
        SetLength(m_candidate_segment_paths, 0);
        refresh_candidate_segment_paths;
        m_visible_candidates_cache_valid := False;
        SetLength(m_visible_candidates_cache, 0);
        SetLength(m_visible_candidate_source_indices_cache, 0);
        m_page_index := 0;
        m_selected_index := 0;
        m_last_candidates_incrementally_reusable := False;
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := Trim(m_last_lookup_debug_extra +
                ' literaluser=1');
        end;
    end;

    procedure reconcile_short_model_candidates_local;
    var
        syllables_local: TncPinyinParseResult;
        admitted_local, exact_local: TncCandidateList;
        evidence_local: TncPairPathEvidenceList;
        admitted_idx_local, candidate_idx_local, recovered_local: Integer;
        found_local, new_admission_local, release_partial_lock_local: Boolean;
    begin
        if (m_dictionary = nil) or (m_composition_text = '') or
            (input_syllable_count < 2) or (input_syllable_count > 4) or
            has_explicit_apostrophe_input or
            (not is_full_pinyin_key(lookup_text)) then Exit;

        new_admission_local := False;
        case input_syllable_count of
            2:
                begin
                    new_admission_local := not short_two_single_pair_prepared;
                    prepare_short_two_single_pair_candidates_local;
                    admitted_local := short_two_single_pair_candidates;
                end;
            3:
                begin
                    new_admission_local := not short_three_exact_pair_prepared;
                    prepare_short_three_exact_pair_candidates_local;
                    admitted_local := short_three_exact_pair_candidates;
                end;
            4:
                begin
                    // Only recorded model paths belong to this common exit.
                    // Existing grammatical fallbacks keep their own admission.
                    if (not m_dictionary.get_exact_pair_path_evidence(lookup_text,
                        evidence_local)) or (Length(evidence_local) = 0) then Exit;
                    if lookup_exact_full_pinyin_cached_local(lookup_text,
                        exact_local) and (Length(exact_local) > 0) then Exit;
                    syllables_local := get_effective_compact_pinyin_syllables(
                        lookup_text);
                    new_admission_local := not short_four_model_evidence_applied;
                    ensure_supported_short_four_two_exact_path_visible_local(
                        syllables_local, admitted_local);
                end;
        end;
        if Length(admitted_local) = 0 then Exit;

        recovered_local := 0;
        for admitted_idx_local := 0 to High(admitted_local) do
        begin
            found_local := False;
            for candidate_idx_local := 0 to High(m_candidates) do
                if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                    SameText(Trim(m_candidates[candidate_idx_local].text),
                    Trim(admitted_local[admitted_idx_local].text)) then
                begin
                    found_local := True;
                    Break;
                end;
            if not found_local then Inc(recovered_local);
        end;

        // Apply newly admitted evidence once, but do not repeat a decision that
        // downstream context ranking has already seen. Missing paths are restored.
        if new_admission_local or (recovered_local > 0) then
            case input_syllable_count of
                2: ensure_short_two_single_pair_candidates_visible_local(m_candidates);
                3: ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
                4: m_candidates := merge_candidate_lists(admitted_local, m_candidates, 0);
            end;

        // Fast-display flags describe the resulting pool, not the shortcut that
        // produced its seed. A validated complete path is no longer prefix-only.
        m_last_lookup_prefix_partial_fast := False;
        m_last_lookup_short312_head_fast := False;

        release_partial_lock_local := m_has_forced_visible_top_candidate and
            SameText(m_forced_visible_top_composition_text, m_composition_text) and
            SameText(m_forced_visible_top_lookup_key, m_last_lookup_key) and
            (Trim(m_forced_visible_top_candidate.comment) <> '') and
            (m_forced_visible_top_candidate.source <> cs_user);
        if release_partial_lock_local then
        begin
            m_has_forced_visible_top_candidate := False;
            m_forced_visible_top_from_incremental_lm_prefix := False;
        end;
        if new_admission_local or (recovered_local > 0) or release_partial_lock_local then
            m_visible_candidates_cache_valid := False;
        if m_config.debug_mode then
            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                Format(' short-evidence-contract=[units=%d admitted=%d recovered=%d new=%d unlock=%d]',
                [input_syllable_count, Length(admitted_local), recovered_local,
                Ord(new_admission_local), Ord(release_partial_lock_local)]);
    end;

    procedure generate_candidates_local;
    var
        i, interactive_prefix_len: Integer;
    begin
        if m_lookup_context_query_bonus_cache <> nil then
        begin
            m_lookup_context_query_bonus_cache.Clear;
        end;
        if m_lookup_context_query_latest_bonus_cache <> nil then
        begin
            m_lookup_context_query_latest_bonus_cache.Clear;
        end;
        total_start_tick := nc_monotonic_tick_ms;
        lookup_cache_hits := 0;
        lookup_cache_misses := 0;
        lookup_elapsed_ms := 0;
        segment_elapsed_ms := 0;
        runtime_elapsed_ms := 0;
        post_elapsed_ms := 0;
        sort_elapsed_ms := 0;
        path_search_elapsed_ms := 0;
        extendable_same_tail_cache_valid := False;
        extendable_same_tail_cache_key := '';
        extendable_same_tail_cache_expected_count := -1;
        extendable_same_tail_cache_value := False;
        short_extendable_tail_cache_valid := False;
        short_extendable_tail_cache_value := False;
        skip_three_syllable_contextual_completion := False;
        short_two_single_pair_prepared := False;
        short_four_model_evidence_applied := False;
        SetLength(short_two_single_pair_candidates, 0);
        SetLength(short_two_single_pair_paths, 0);
        SetLength(short_two_single_pair_rank_scores, 0);
        m_short_two_single_pair_query := '';
        SetLength(m_short_two_single_pair_texts, 0);
        previous_short_three_exact_pair_query :=
            m_short_three_exact_pair_query;
        previous_short_three_exact_pair_texts :=
            m_short_three_exact_pair_texts;
        if m_visible_candidates_cache_valid then
        begin
            previous_visible_candidates := m_visible_candidates_cache;
        end
        else
        begin
            SetLength(previous_visible_candidates, 0);
        end;
        short_three_exact_pair_prepared := False;
        SetLength(short_three_exact_pair_candidates, 0);
        SetLength(short_three_exact_pair_paths, 0);
        SetLength(short_three_exact_pair_rank_scores, 0);
        m_short_three_exact_pair_query := '';
        SetLength(m_short_three_exact_pair_texts, 0);
        m_short_three_char_pair_path := '';
        m_short_three_char_pair_evidence := 0;
        m_short_exact_lexicon_completion_query := '';
        SetLength(m_short_exact_lexicon_completion_texts, 0);
        SetLength(m_short_exact_lexicon_completion_candidates, 0);
        long_path_exact_lookup_filter_depth := 0;
        SetLength(explicit_apostrophe_query_syllables, 0);
        explicit_apostrophe_query_parsed := False;
        SetLength(explicit_apostrophe_exact_candidates, 0);
        explicit_apostrophe_exact_candidates_loaded := False;
        previous_lookup_key := m_last_lookup_key;
        if m_last_candidates_incrementally_reusable then
        begin
            previous_candidates := m_candidates;
        end
        else
        begin
            previous_lookup_key := '';
            SetLength(previous_candidates, 0);
        end;
        m_last_candidates_incrementally_reusable := True;
        // A model handoff can rebuild the same pinyin. Preserve the snapshots
        // captured above, but never page new sources through an old display pool.
        m_visible_candidates_cache_valid := False;
        m_long_visible_candidate_pool_cache_valid := False;
        SetLength(m_candidates, 0);
        m_page_index := 0;
        m_selected_index := 0;
        m_candidate_navigation_started := False;
        confirmed_prefix_boundary_partial_preferred := False;
        m_last_lookup_key := '';
        m_last_lookup_normalized_from := '';
        m_last_lookup_syllable_count := 0;
        m_last_three_syllable_partial_preference_kind := 0;
        m_last_three_syllable_head_exact_text := '';
        m_last_three_syllable_head_strength := 0;
        m_last_three_syllable_tail_strength := 0;
        m_last_three_syllable_first_single_strength := 0;
        m_last_three_syllable_last_single_strength := 0;
        m_last_three_syllable_head_path_bonus := 0;
        m_last_three_syllable_tail_path_bonus := 0;
        m_last_three_syllable_partial_debug_info := '';
        m_last_full_path_debug_info := '';
        m_last_lookup_debug_extra := '';
        m_last_lookup_timing_info := '';
        m_last_lookup_all_initial_fast := False;
        m_last_lookup_literal_user_fast := False;
        m_last_lookup_prefix_partial_fast := False;
        m_last_lookup_short312_head_fast := False;
        m_last_lookup_incremental_partial_reuse := False;
        m_runtime_chain_text := '';
        m_runtime_common_pattern_text := '';
        m_runtime_redup_text := '';
        SetLength(m_runtime_long_chain_candidates, 0);
        SetLength(m_runtime_long_complete_pool_candidates, 0);
        SetLength(m_runtime_long_retained_exact_edges, 0);
        m_long_complete_pool_pairwise_text := '';
        m_long_complete_pool_pairwise_insert_rank := -1;
        clear_segment_path_tracking;
        clear_lookup_bonus_caches;
        if m_composition_text = '' then
        begin
            Exit;
        end;

        has_raw_candidates := False;
        has_segment_candidates := False;
        raw_from_dictionary := False;
        lookup_text := normalize_pinyin_text(m_composition_text);
        m_last_lookup_key := lookup_text;
        load_literal_user_candidates_local;
        has_safe_trailing_initial_typing_state :=
            detect_safe_trailing_initial_typing_state(m_composition_text);
        if (not has_safe_trailing_initial_typing_state) and (not is_full_pinyin_key(lookup_text)) then
        begin
            normalized_lookup_text := normalize_adjacent_swap_typo(lookup_text);
            if (normalized_lookup_text <> '') and (not SameText(normalized_lookup_text, lookup_text)) then
            begin
                m_last_lookup_normalized_from := lookup_text;
                lookup_text := normalized_lookup_text;
            end;
        end;
        m_last_lookup_key := lookup_text;
        if is_compact_all_single_initial_query_text(lookup_text, 8) then
        begin
            if (previous_lookup_key <> '') and
                (Length(previous_candidates) > 0) and
                SameText(Copy(lookup_text, 1, Length(previous_lookup_key)),
                previous_lookup_key) then
            begin
                m_candidates := previous_candidates;
                all_initial_tail := Copy(lookup_text,
                    Length(previous_lookup_key) + 1, MaxInt);
                if all_initial_tail <> '' then
                begin
                    for i := 0 to High(m_candidates) do
                    begin
                        if Trim(m_candidates[i].comment) <> '' then
                        begin
                            m_candidates[i].comment :=
                                m_candidates[i].comment + all_initial_tail;
                        end
                        else if get_candidate_text_unit_count(
                            Trim(m_candidates[i].text)) < Length(lookup_text) then
                        begin
                            m_candidates[i].comment := all_initial_tail;
                        end;
                    end;
                end;
            end
            else
            begin
                SetLength(m_candidates, 0);
            end;
            if (Length(m_candidates) = 0) and (m_dictionary <> nil) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                if m_dictionary.lookup_full_pinyin_prefix(lookup_text,
                    raw_candidates) and
                    try_pick_predictive_prefix_candidate_from_list_local(
                    raw_candidates, direct_preferred_candidate) then
                begin
                    Inc(lookup_elapsed_ms,
                        Int64(nc_monotonic_tick_ms - phase_start_tick));
                    SetLength(m_candidates, 1);
                    m_candidates[0] := direct_preferred_candidate;
                end
                else
                begin
                    Inc(lookup_elapsed_ms,
                        Int64(nc_monotonic_tick_ms - phase_start_tick));
                end;
            end;
            if (Length(m_candidates) = 0) and (m_dictionary <> nil) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                if m_dictionary.lookup(Copy(lookup_text, 1, 1),
                    raw_candidates) then
                begin
                    Inc(lookup_elapsed_ms,
                        Int64(nc_monotonic_tick_ms - phase_start_tick));
                    m_candidates := raw_candidates;
                    all_initial_tail := Copy(lookup_text, 2, MaxInt);
                    if all_initial_tail <> '' then
                    begin
                        for i := 0 to High(m_candidates) do
                        begin
                            if Trim(m_candidates[i].comment) = '' then
                            begin
                                m_candidates[i].comment := all_initial_tail;
                            end;
                        end;
                    end;
                end
                else
                begin
                    Inc(lookup_elapsed_ms,
                        Int64(nc_monotonic_tick_ms - phase_start_tick));
                    SetLength(m_candidates, 0);
                end;
                if Length(m_candidates) = 0 then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    if m_dictionary.lookup_full_pinyin_prefix(Copy(lookup_text, 1,
                        1), raw_candidates) then
                    begin
                        Inc(lookup_elapsed_ms,
                            Int64(nc_monotonic_tick_ms - phase_start_tick));
                        m_candidates := raw_candidates;
                        all_initial_tail := Copy(lookup_text, 2, MaxInt);
                        if all_initial_tail <> '' then
                        begin
                            for i := 0 to High(m_candidates) do
                            begin
                                if Trim(m_candidates[i].comment) = '' then
                                begin
                                    m_candidates[i].comment := all_initial_tail;
                                end;
                            end;
                        end;
                    end
                    else
                    begin
                        Inc(lookup_elapsed_ms,
                            Int64(nc_monotonic_tick_ms - phase_start_tick));
                        SetLength(m_candidates, 0);
                    end;
                end;
            end;
            if (Length(m_candidates) = 0) or
                ((Length(m_candidates) > 0) and
                (Trim(m_candidates[0].text) = '')) then
            begin
                SetLength(m_candidates, 1);
                m_candidates[0].text := lookup_text;
                m_candidates[0].comment := '';
                m_candidates[0].score := 0;
                m_candidates[0].source := cs_rule;
                m_candidates[0].has_dict_weight := False;
                m_candidates[0].dict_weight := 0;
            end;
            input_syllable_count := Length(lookup_text);
            m_last_lookup_syllable_count := input_syllable_count;
            finalize_all_initial_compact_candidates_and_prepare_exit(Format(
                'multi=1 seg=0 dangling=0 head_only=1 runtime=0 redup=0 allinitfast=1 allearly=1 allinit=1 len=%d',
                [input_syllable_count]));
            Exit;
        end;
        has_explicit_apostrophe_input := Pos('''', m_composition_text) > 0;
        explicit_apostrophe_lookup_text := '';
        if has_explicit_apostrophe_input then
        begin
            explicit_apostrophe_lookup_text := LowerCase(
                nc_normalize_umlaut_spelling(Trim(m_composition_text)));
        end;
        if m_last_lookup_normalized_from <> '' then
        begin
            fallback_comment := build_pinyin_comment(lookup_text);
        end
        else
        begin
            fallback_comment := build_pinyin_comment(m_composition_text);
            if fallback_comment = '' then
            begin
                fallback_comment := build_pinyin_comment(lookup_text);
            end;
        end;
        allow_relaxed_missing_apostrophe := False;
        has_multi_syllable_input := fallback_comment <> '';
        if m_last_lookup_normalized_from <> '' then
        begin
            relaxed_missing_apostrophe_comment := build_pinyin_comment(lookup_text, True);
        end
        else
        begin
            relaxed_missing_apostrophe_comment := build_pinyin_comment(m_composition_text, True);
            if relaxed_missing_apostrophe_comment = '' then
            begin
                relaxed_missing_apostrophe_comment := build_pinyin_comment(lookup_text, True);
            end;
        end;
        has_relaxed_missing_apostrophe_partial :=
            (relaxed_missing_apostrophe_comment <> '') and
            (not SameText(relaxed_missing_apostrophe_comment, fallback_comment));
        has_relaxed_missing_apostrophe_boundary_shift := False;
        if has_relaxed_missing_apostrophe_partial then
        begin
            has_relaxed_missing_apostrophe_boundary_shift :=
                detect_relaxed_missing_apostrophe_boundary_shift(m_composition_text);
            if (not has_relaxed_missing_apostrophe_boundary_shift) and
                (lookup_text <> m_composition_text) then
            begin
                has_relaxed_missing_apostrophe_boundary_shift :=
                    detect_relaxed_missing_apostrophe_boundary_shift(lookup_text);
            end;
        end;
        has_internal_dangling_initial := detect_internal_dangling_initial(m_composition_text);
        if has_safe_trailing_initial_typing_state then
        begin
            has_internal_dangling_initial := False;
        end;
        all_initial_compact_query := detect_all_initial_compact_query(m_composition_text);
        if m_last_lookup_normalized_from <> '' then
        begin
            // If lookup key is typo-normalized (e.g. chagn->chang), segmenting by raw input
            // is usually noisy; force dangling-initial guard to suppress that path.
            if not has_safe_trailing_initial_typing_state then
            begin
                has_internal_dangling_initial := True;
            end;
        end;
        if m_last_lookup_normalized_from <> '' then
        begin
            input_syllable_count := get_input_syllable_count_for_text(lookup_text);
        end
        else
        begin
            input_syllable_count := get_input_syllable_count_for_text(m_composition_text);
            if input_syllable_count <= 0 then
            begin
                input_syllable_count := get_input_syllable_count_for_text(lookup_text);
            end;
        end;
        has_raw_safe_trailing_initial_typing_state :=
            has_safe_trailing_initial_typing_state or
            detect_raw_safe_trailing_initial_typing_state(lookup_text,
            input_syllable_count);
        if has_raw_safe_trailing_initial_typing_state then
        begin
            has_internal_dangling_initial := False;
        end;
        if (m_last_lookup_normalized_from <> '') and
            (not has_raw_safe_trailing_initial_typing_state) and
            is_full_pinyin_key(lookup_text) then
        begin
            // For malformed compact typos like "chagn" -> "chang", prefer the
            // normalized full-pinyin lookup intent over raw pseudo-segmentation.
            input_syllable_count := get_input_syllable_count_for_text(lookup_text);
            if input_syllable_count <= 1 then
            begin
                has_multi_syllable_input := False;
                fallback_comment := '';
                relaxed_missing_apostrophe_comment := '';
            end;
        end;
        subspan_oracle_budget_remaining := 0;
        m_last_lookup_syllable_count := input_syllable_count;
        if try_preserve_short_lm_prefix_from_previous_local(
            short_four_preserved_prefix_candidate) then
        begin
            m_has_forced_visible_top_candidate := True;
            m_forced_visible_top_from_incremental_lm_prefix := True;
            m_forced_visible_top_candidate :=
                short_four_preserved_prefix_candidate;
            m_forced_visible_top_composition_text := m_composition_text;
            m_forced_visible_top_lookup_key := m_last_lookup_key;
        end;
        if try_reuse_long_incomplete_tail_candidates_local(
            incremental_partial_candidates) then
        begin
            m_candidates := incremental_partial_candidates;
            m_last_lookup_incremental_partial_reuse := True;
            m_last_lookup_prefix_partial_fast := True;
            SetLength(m_candidate_segment_paths, 0);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=0 path=0 rt=0 post=0 sort=0 cache=%d/%d total=%d]',
                [lookup_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                Int64(nc_monotonic_tick_ms - total_start_tick)]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=0 dangling=0 head_only=0 runtime=0 redup=0 longincompletetailreuse=1 allinit=%d %s',
                    [Ord(has_multi_syllable_input),
                    Ord(all_initial_compact_query),
                    get_candidate_debug_summary(m_candidates[0])]);
            end;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        short4_standalone_particle_tail := False;
        if input_syllable_count = 4 then
        begin
            short4_tail_syllables := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(lookup_text));
            if Length(short4_tail_syllables) = 4 then
            begin
                short4_tail_unit_key := normalize_pinyin_text(
                    short4_tail_syllables[High(short4_tail_syllables)].text);
                short4_standalone_particle_tail :=
                    (short4_tail_unit_key = 'de') or
                    (short4_tail_unit_key = 'le') or
                    (short4_tail_unit_key = 'zhe') or
                    (short4_tail_unit_key = 'la') or
                    (short4_tail_unit_key = 'ba') or
                    (short4_tail_unit_key = 'ma') or
                    (short4_tail_unit_key = 'a');
            end;
        end;
        short_dangling_prefix_query := has_multi_syllable_input and
            has_internal_dangling_initial and
            (input_syllable_count <= 8) and
            (not has_raw_safe_trailing_initial_typing_state) and
            ((m_last_lookup_normalized_from <> '') or
            (not is_full_pinyin_key(lookup_text)));
        short_full_exact_query := has_multi_syllable_input and
            (input_syllable_count >= 2) and (input_syllable_count <= 8) and
            is_full_pinyin_key(lookup_text) and
            (not short_dangling_prefix_query);
        long_full_exact_query := has_multi_syllable_input and
            (input_syllable_count >= 12) and
            is_full_pinyin_key(lookup_text);
        if (m_dictionary <> nil) and has_multi_syllable_input and
            has_raw_safe_trailing_initial_typing_state and
            (input_syllable_count >= 7) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if try_build_fragment_prefix_partial_candidate_local(
                fragment_prefix_candidate) then
            begin
                note_debug_helper_elapsed('tearlysafeinitfrag',
                    phase_start_tick);
                SetLength(m_candidates, 1);
                m_candidates[0] := fragment_prefix_candidate;
                finalize_prefix_partial_candidates_and_prepare_exit(Format(
                    'multi=%d seg=0 dangling=0 head_only=0 runtime=0 redup=0 earlysafeinitialfrag=1 lighttail=1 allinit=%d',
                    [Ord(has_multi_syllable_input),
                    Ord(all_initial_compact_query)]), False, False);
                Exit;
            end;
            note_debug_helper_elapsed('tearlysafeinitfrag', phase_start_tick);
        end;
        if has_incremental_composition_local and
            try_reuse_stable_multi_prefix_before_lookup_local(
            incremental_partial_candidates) then
        begin
            m_candidates := incremental_partial_candidates;
            m_last_lookup_incremental_partial_reuse := True;
            m_last_lookup_prefix_partial_fast := True;
            SetLength(m_candidate_segment_paths, 0);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=0 path=0 rt=0 post=0 sort=0 cache=%d/%d total=%d]',
                [lookup_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                Int64(nc_monotonic_tick_ms - total_start_tick)]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=0 dangling=0 head_only=0 runtime=0 redup=0 incrementalpartialearly=1 allinit=%d %s',
                    [Ord(has_multi_syllable_input), Ord(all_initial_compact_query),
                    get_candidate_debug_summary(m_candidates[0])]);
            end;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if has_incremental_composition_local and
            try_build_pronoun_connector_predictive_tail_fast_local(
            incremental_partial_candidates) then
        begin
            m_candidates := incremental_partial_candidates;
            m_last_lookup_prefix_partial_fast := True;
            SetLength(m_candidate_segment_paths, 0);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=0 path=0 rt=0 post=0 sort=0 cache=%d/%d total=%d]',
                [lookup_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                Int64(nc_monotonic_tick_ms - total_start_tick)]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=0 dangling=0 head_only=0 runtime=0 redup=0 pronounconnectorpredictivefast=1 allinit=%d %s',
                    [Ord(has_multi_syllable_input), Ord(all_initial_compact_query),
                    get_candidate_debug_summary(m_candidates[0])]);
            end;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if has_incremental_composition_local and
            try_build_pronoun_connector_tail_incremental_fast_local(
            incremental_partial_candidates) then
        begin
            m_candidates := incremental_partial_candidates;
            m_last_lookup_incremental_partial_reuse := True;
            m_last_lookup_prefix_partial_fast := True;
            SetLength(m_candidate_segment_paths, 0);
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=0 path=0 rt=0 post=0 sort=0 cache=%d/%d total=%d]',
                [lookup_elapsed_ms, lookup_cache_hits, lookup_cache_misses,
                Int64(nc_monotonic_tick_ms - total_start_tick)]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=0 dangling=0 head_only=0 runtime=0 redup=0 pronounconnectortailfast=1 allinit=%d %s',
                    [Ord(has_multi_syllable_input), Ord(all_initial_compact_query),
                    get_candidate_debug_summary(m_candidates[0])]);
            end;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        // Full exact short queries should not enter prefix repair or long-sentence search.
        if short_full_exact_query and (input_syllable_count >= 2) and
            (input_syllable_count <= 5) and (not has_explicit_apostrophe_input) and
            lookup_exact_full_pinyin_cached_local(lookup_text, raw_candidates) and
            has_short_exact_complete_anchor(raw_candidates) then
        begin
            m_candidates := raw_candidates;
            phase_start_tick := nc_monotonic_tick_ms;
            if m_dictionary <> nil then
            begin
                latest_query_choice_text := get_cached_query_latest_choice_text(
                    normalize_pinyin_text(lookup_text));
            end;
            for i := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[i].comment) <> '' then
                begin
                    Continue;
                end;
                if is_latest_session_query_choice(m_candidates[i].text) then
                begin
                    Inc(m_candidates[i].score, 820);
                end;
                if (latest_query_choice_text <> '') and
                    SameText(Trim(m_candidates[i].text),
                    latest_query_choice_text) then
                begin
                    Inc(m_candidates[i].score, 760);
                end;
                if m_dictionary <> nil then
                begin
                    Inc(m_candidates[i].score,
                        m_dictionary.get_query_choice_bonus(
                        normalize_pinyin_text(lookup_text),
                        m_candidates[i].text));
                end;
            end;
            apply_user_penalties(lookup_text, m_candidates);
            filter_non_bmp_single_char_candidates(m_candidates);
            sort_candidates_lightweight(m_candidates);
            promote_short_exact_raw_weight_leader_local(m_candidates);
            ensure_short_two_single_pair_candidates_visible_local(m_candidates);
            // Confirmed tails and literal user words can bypass short-exact
            // display ranking. Their prefix recall must not depend on it.
            if ((m_confirmed_text <> '') or
                (Length(literal_user_candidates) > 0)) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                ensure_multi_char_partial_precedes_single_char_partial(
                    m_candidates);
            end;
            ensure_short_lexicon_prefix_completions_visible_local(
                m_candidates);
            Inc(sort_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            m_last_lookup_timing_info := Format(
                'perf=[lk=%d seg=%d path=%d rt=%d post=%d sort=%d cache=%d/%d total=%d]',
                [lookup_elapsed_ms, segment_elapsed_ms, path_search_elapsed_ms,
                runtime_elapsed_ms, post_elapsed_ms, sort_elapsed_ms,
                lookup_cache_hits, lookup_cache_misses,
                Int64(nc_monotonic_tick_ms - total_start_tick)]);
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=%d dangling=%d head_only=0 runtime=0 redup=0 shortexactfast=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(all_initial_compact_query)]);
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
            end;
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_strong_full_query_exact_complete_candidate_local) and
            (not has_complete_exact_tail_phrase_local(2)) and
            (not has_complete_exact_tail_phrase_local(3)) and
            try_build_incremental_fixed_single_tail_completion_local(
            head_tail_complete_candidate,
            head_tail_complete_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := head_tail_complete_candidate;
            remember_segment_path_for_candidate(
                head_tail_complete_candidate.text,
                head_tail_complete_candidate.comment,
                head_tail_complete_encoded_path,
                0);
            if (input_syllable_count < 9) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                promote_candidate_to_front_local(m_candidates,
                    head_tail_complete_candidate);
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d incrementalsingletailfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added),
                Ord(runtime_redup_added), Ord(all_initial_compact_query)]),
                head_tail_complete_candidate, False);
            m_last_candidates_incrementally_reusable := False;
            m_last_ranked_query_key := '';
            m_last_ranked_top_path := '';
            Exit;
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            has_contextual_directional_exact_tail_candidate_local and
            (try_build_contextual_directional_tail_from_previous_local(
            head_tail_complete_candidate, head_tail_complete_encoded_path) or
            try_build_contextual_directional_tail_from_ranked_prefix_local(
            head_tail_complete_candidate,
            head_tail_complete_encoded_path)) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := head_tail_complete_candidate;
            remember_segment_path_for_candidate(
                head_tail_complete_candidate.text,
                head_tail_complete_candidate.comment,
                head_tail_complete_encoded_path,
                head_tail_complete_candidate.score);
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d directionalpreviousfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added),
                Ord(runtime_redup_added), Ord(all_initial_compact_query)]),
                head_tail_complete_candidate, False);
            Exit;
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (try_build_incremental_exact_tail_completion_local(4,
            head_tail_complete_candidate,
            head_tail_complete_encoded_path) or
            try_build_incremental_exact_tail_completion_local(3,
            head_tail_complete_candidate,
            head_tail_complete_encoded_path)) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := head_tail_complete_candidate;
            remember_segment_path_for_candidate(
                head_tail_complete_candidate.text,
                head_tail_complete_candidate.comment,
                head_tail_complete_encoded_path,
                head_tail_complete_candidate.score);
            if (input_syllable_count < 9) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                promote_candidate_to_front_local(m_candidates,
                    head_tail_complete_candidate);
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d incrementaltailfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added),
                Ord(runtime_redup_added), Ord(all_initial_compact_query)]),
                head_tail_complete_candidate, False);
            Exit;
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            has_reliable_exact_tail_candidate_local(4) and
            try_build_stable_prefix_exact_tail_completion_local(
            head_tail_complete_candidate, head_tail_complete_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := head_tail_complete_candidate;
            remember_segment_path_for_candidate(
                head_tail_complete_candidate.text,
                head_tail_complete_candidate.comment,
                head_tail_complete_encoded_path,
                head_tail_complete_candidate.score);
            if (input_syllable_count < 9) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d stabletailfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added),
                Ord(runtime_redup_added), Ord(all_initial_compact_query)]),
                head_tail_complete_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 4) and (not has_explicit_apostrophe_input) and
            (not all_initial_compact_query) then
        begin
            interactive_query_text := normalize_pinyin_text(lookup_text);
            for interactive_prefix_len := Max(1, Length(interactive_query_text) - 2) to
                Length(interactive_query_text) - 1 do
            begin
                interactive_head_key := Copy(interactive_query_text, 1,
                    interactive_prefix_len);
                interactive_tail_key := Copy(interactive_query_text,
                    interactive_prefix_len + 1,
                    Length(interactive_query_text) - interactive_prefix_len);
                if (interactive_tail_key <> '') and (Length(interactive_tail_key) <= 2) then
                begin
                    if (not is_full_pinyin_key(interactive_head_key)) or
                        (get_input_syllable_count_for_text(interactive_head_key) <> 3) then
                    begin
                        Continue;
                    end;
                    interactive_best_idx := -1;
                    interactive_best_score := Low(Integer);
                    interactive_best_tail_text := '';
                    if lookup_exact_full_pinyin_cached_local(interactive_head_key,
                        interactive_head_candidates) then
                    begin
                        for i := 0 to High(interactive_head_candidates) do
                        begin
                            if (Trim(interactive_head_candidates[i].comment) <> '') or
                                (Trim(interactive_head_candidates[i].text) = '') or
                                (get_candidate_text_unit_count(Trim(
                                interactive_head_candidates[i].text)) <> 3) then
                            begin
                                Continue;
                            end;
                            interactive_score := interactive_head_candidates[i].score;
                            if interactive_head_candidates[i].has_dict_weight and
                                (interactive_head_candidates[i].dict_weight >
                                interactive_score) then
                            begin
                                interactive_score := interactive_head_candidates[i].dict_weight;
                            end;
                            if interactive_head_candidates[i].source = cs_user then
                            begin
                                Inc(interactive_score, c_user_score_bonus);
                            end;
                            if (interactive_best_idx < 0) or
                                (interactive_score > interactive_best_score) then
                            begin
                                interactive_best_idx := i;
                                interactive_best_score := interactive_score;
                            end;
                        end;
                    end;
                    if (interactive_best_idx < 0) and
                        try_get_fixed_pronoun_tail_query_parts(
                        interactive_head_key, 3, interactive_fixed_head_pinyin,
                        interactive_fixed_head_text, interactive_fixed_tail_key,
                        interactive_fixed_tail_units) and
                        (interactive_fixed_tail_units = 2) and
                        dictionary_exact_lookup_cached(interactive_fixed_tail_key,
                        interactive_tail_candidates) then
                    begin
                        for i := 0 to High(interactive_tail_candidates) do
                        begin
                            if (Trim(interactive_tail_candidates[i].comment) <> '') or
                                (Trim(interactive_tail_candidates[i].text) = '') or
                                (get_candidate_text_unit_count(Trim(
                                interactive_tail_candidates[i].text)) <>
                                interactive_fixed_tail_units) then
                            begin
                                Continue;
                            end;
                            interactive_tail_text := Trim(
                                interactive_tail_candidates[i].text);
                            if get_candidate_text_unit_count(
                                interactive_fixed_head_text + interactive_tail_text) <> 3 then
                            begin
                                Continue;
                            end;
                            if (not m_dictionary.is_base_entry(
                                interactive_fixed_tail_key, interactive_tail_text)) and
                                (not m_dictionary.is_user_entry(
                                interactive_fixed_tail_key, interactive_tail_text)) then
                            begin
                                Continue;
                            end;
                            if interactive_tail_candidates[i].has_dict_weight then
                            begin
                                interactive_tail_score :=
                                    interactive_tail_candidates[i].dict_weight;
                            end
                            else
                            begin
                                interactive_tail_score :=
                                    interactive_tail_candidates[i].score;
                            end;
                            if interactive_tail_candidates[i].source = cs_user then
                            begin
                                Inc(interactive_tail_score, c_user_score_bonus);
                            end;
                            if (interactive_best_idx <> -2) or
                                (interactive_tail_score > interactive_best_score) then
                            begin
                                interactive_best_idx := -2;
                                interactive_best_score := interactive_tail_score;
                                interactive_best_tail_text := interactive_tail_text;
                            end;
                        end;
                    end;
                    if interactive_best_idx >= 0 then
                    begin
                        SetLength(m_candidates, 1);
                        m_candidates[0] := interactive_head_candidates[interactive_best_idx];
                        m_candidates[0].comment := interactive_tail_key;
                        interactive_preserved_candidate := m_candidates[0];
                        ensure_rich_head_single_char_partials_visible(m_candidates);
                        promote_candidate_to_front_local(m_candidates,
                            interactive_preserved_candidate);
                        m_has_forced_visible_top_candidate := True;
                        m_forced_visible_top_from_incremental_lm_prefix := False;
                        m_forced_visible_top_candidate := interactive_preserved_candidate;
                        m_forced_visible_top_composition_text := m_composition_text;
                        m_forced_visible_top_lookup_key := m_last_lookup_key;
                        finalize_lookup_timing_info;
                        if m_config.debug_mode then
                        begin
                            m_last_lookup_debug_extra := Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=0 redup=0 short3tailreuse=1 allinit=%d',
                                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                                Ord(all_initial_compact_query)]);
                            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                                ' ' + get_candidate_debug_summary(m_candidates[0]);
                        end;
                        refresh_candidate_segment_paths;
                        note_ranked_top_candidate;
                        m_page_index := 0;
                        m_selected_index := 0;
                        Exit;
                    end;
                    if interactive_best_idx = -2 then
                    begin
                        SetLength(m_candidates, 1);
                        FillChar(m_candidates[0], SizeOf(m_candidates[0]), 0);
                        m_candidates[0].text := interactive_fixed_head_text +
                            interactive_best_tail_text;
                        m_candidates[0].comment := interactive_tail_key;
                        m_candidates[0].source := cs_rule;
                        m_candidates[0].score := interactive_best_score + 6400 +
                            (interactive_fixed_tail_units * 920);
                        m_candidates[0].has_dict_weight := True;
                        m_candidates[0].dict_weight := m_candidates[0].score;
                        if build_exact_leading_prefix_partial_candidates(
                            lookup_text, exact_head_partial_candidates) then
                        begin
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                        end
                        else if build_raw_leading_prefix_partial_candidates(
                            lookup_text, exact_head_partial_candidates) then
                        begin
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                        end;
                        finalize_lookup_timing_info;
                        if m_config.debug_mode then
                        begin
                            m_last_lookup_debug_extra := Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=0 redup=0 short3fixedtailreuse=1 allinit=%d',
                                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                                Ord(all_initial_compact_query)]);
                            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                                ' ' + get_candidate_debug_summary(m_candidates[0]);
                        end;
                        refresh_candidate_segment_paths;
                        note_ranked_top_candidate;
                        m_page_index := 0;
                        m_selected_index := 0;
                        Exit;
                    end;
                end;
            end;
        end;
        if (not has_explicit_apostrophe_input) and
            (get_effective_compact_pinyin_unit_count(lookup_text) >= 2) and
            (get_effective_compact_pinyin_unit_count(lookup_text) <= 4) and
            (m_dictionary <> nil) and
            m_dictionary.lookup_full_pinyin_prefix(lookup_text, raw_candidates) and
            try_pick_predictive_prefix_candidate_from_list_local(raw_candidates,
            direct_preferred_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := direct_preferred_candidate;
            if input_syllable_count = 4 then
            begin
                ensure_best_short_exact_prefix_tail_candidate_visible(
                    m_candidates);
            end;
            finalize_direct_preferred_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=0 runtime=0 redup=0 predictprefix=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if build_single_incomplete_prefix_lookup_candidates_local(raw_candidates) then
        begin
            m_candidates := raw_candidates;
            phase_start_tick := nc_monotonic_tick_ms;
            sort_candidates_lightweight(m_candidates);
            Inc(sort_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            finalize_fast_prefix_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=0 runtime=0 redup=0 singleprefix=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if (not has_explicit_apostrophe_input) and
            (not all_initial_compact_query) and
            build_two_syllable_single_head_prefix_partial_candidates_local(
            lookup_text, raw_candidates) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, 1);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=0 runtime=0 redup=0 short2singleprefix=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(all_initial_compact_query)]), False, False, False);
            Exit;
        end;
        delayed_long_decode_mode := should_use_delayed_long_decode_mode_local;
        normalized_typo_long_query_fast_path := has_multi_syllable_input and
            (m_last_lookup_normalized_from <> '') and
            has_internal_dangling_initial and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= c_long_sentence_full_path_min_syllables);
        ultra_long_partial_shortfast_applied := False;
        ultra_long_exact_chunk_chain_miss_cached := False;
        repeated_two_syllable_query := is_repeated_two_syllable_query_text(lookup_text);
        single_char_partial_min_count := 1;
        if repeated_two_syllable_query then
        begin
            // For repeated two-syllable input like "shishi", keep extra single-char
            // continuation options visible to reduce phrase-only crowding.
            single_char_partial_min_count := 2;
        end;
        if has_multi_syllable_input and is_full_pinyin_key(lookup_text) then
        begin
            if input_syllable_count >= 6 then
            begin
                single_char_partial_min_count := Max(single_char_partial_min_count, 3);
            end
            else if input_syllable_count >= 4 then
            begin
                single_char_partial_min_count := Max(single_char_partial_min_count, 2);
            end;
        end;
        note_debug_helper_elapsed('tinit', total_start_tick);
        runtime_phrase_added := False;
        runtime_redup_added := False;
        full_query_exact_promoted := False;
        has_long_direct_preferred_candidate := False;
        has_long_direct_chain_candidate := False;
        has_stable_prefix_candidate := False;
        stable_prefix_candidate_evaluated := False;
        FillChar(stable_prefix_candidate, SizeOf(stable_prefix_candidate), 0);
        FillChar(head_tail_complete_candidate, SizeOf(head_tail_complete_candidate), 0);
        head_tail_complete_encoded_path := '';
        raw_candidates_seeded_from_runtime_only := False;
        trailing_prefix_rescue_applied := False;
        has_strong_long_sentence_prediction := False;
        strict_oracle_sentence_built := False;
        strict_oracle_sentence_ok := False;
        resolved_sentence_candidate_built := False;
        resolved_sentence_candidate_ok := False;
        FillChar(resolved_sentence_candidate_cached,
            SizeOf(resolved_sentence_candidate_cached), 0);
        resolved_sentence_path_cached := '';
        subquery_sentence_promoted := False;
        subquery_sentence_missed := False;
        ctxalt_miss_debug_count := 0;
        latest_query_choice_text := '';
        SetLength(relaxed_segment_candidates, 0);
        SetLength(explicit_apostrophe_aligned_candidates, 0);
        SetLength(lightweight_sentence_candidates, 0);
        SetLength(strict_oracle_sentence_candidates, 0);
        lightweight_sentence_applied := False;
        head_only_multi_syllable := m_config.enable_segment_candidates and
            has_multi_syllable_input and
            (not short_full_exact_query) and
            (all_initial_compact_query or
                (m_config.segment_head_only_multi_syllable and
                    (not has_internal_dangling_initial) and
                    (input_syllable_count < c_long_sentence_exact_min_syllables)));
        if (m_dictionary <> nil) and has_multi_syllable_input and
            is_short_repeated_initial_compact_query_text_local(lookup_text) and
            dictionary_lookup_cached(lookup_text, raw_candidates) then
        begin
            m_candidates := raw_candidates;
            phase_start_tick := nc_monotonic_tick_ms;
            sort_candidates_lightweight(m_candidates);
            Inc(sort_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d repeatedinit=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), m_candidates[0], False);
            if Length(m_candidates) > 0 then
            begin
                m_has_forced_visible_top_candidate := True;
                m_forced_visible_top_from_incremental_lm_prefix := False;
                m_forced_visible_top_candidate := m_candidates[0];
                m_forced_visible_top_composition_text := m_composition_text;
                m_forced_visible_top_lookup_key := m_last_lookup_key;
            end;
            Exit;
        end;
        if try_build_explicit_apostrophe_lookup_seed_local(raw_candidates) then
        begin
            m_candidates := raw_candidates;
            sort_candidates_lightweight(m_candidates);
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d explicitapostrophe=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            if Length(m_candidates) > 0 then
            begin
                { The strict explicit-boundary exact hit is the authoritative
                  first candidate. Keep display-time partial supplementation
                  from replacing it after the rebuild has finished. }
                m_has_forced_visible_top_candidate := True;
                m_forced_visible_top_from_incremental_lm_prefix := False;
                m_forced_visible_top_candidate := m_candidates[0];
                m_forced_visible_top_composition_text := m_composition_text;
                m_forced_visible_top_lookup_key := m_last_lookup_key;
            end;
            Exit;
        end;
        if (m_dictionary <> nil) and (input_syllable_count <= 4) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_strong_short_exact_complete_lookup_local(input_syllable_count)) and
            build_fixed_wo_tail_dictionary_candidates(lookup_text,
            fixed_wo_tail_candidates) then
        begin
            m_candidates := fixed_wo_tail_candidates;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end
            else if build_raw_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            remove_partials_shadowed_by_complete_text_local(m_candidates);
            sort_candidates_lightweight(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            ensure_short_two_single_pair_candidates_visible_local(m_candidates);
            ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d fixedheadtailprefast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), m_candidates[0], False);
            Exit;
        end;
        if (m_dictionary <> nil) and (m_last_lookup_normalized_from <> '') and
            has_multi_syllable_input and
            try_build_relaxed_no_initial_tail_phrase_prefix_candidate_local(
            lookup_text, stable_prefix_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typorelaxedprefix=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False);
            Exit;
        end;
        if (m_dictionary <> nil) and short_dangling_prefix_query and
            (not all_initial_compact_query) and
            (build_string_prefix_exact_partial_candidates(
            m_last_lookup_normalized_from, raw_candidates) or
            build_exact_leading_prefix_partial_candidates(
            m_last_lookup_normalized_from, raw_candidates) or
            build_string_prefix_exact_partial_candidates(lookup_text,
            raw_candidates) or build_exact_leading_prefix_partial_candidates(
            lookup_text, raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typoshortprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and (m_last_lookup_normalized_from <> '') and
            has_multi_syllable_input and (input_syllable_count <= 4) and
            (not short_dangling_prefix_query) and
            lookup_exact_full_pinyin_cached_local(lookup_text, raw_candidates) and
            try_pick_complete_candidate_from_list_local(raw_candidates,
            typo_exact_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := typo_exact_candidate;
            finalize_direct_preferred_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typoexact=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if (m_dictionary <> nil) and normalized_typo_long_query_fast_path and
            (not all_initial_compact_query) and
            try_build_direct_preferred_subspan_sentence_candidate_local(
            direct_preferred_candidate) then
        begin
            has_long_direct_preferred_candidate := True;
        end;
        if (m_dictionary <> nil) and (m_last_lookup_normalized_from <> '') and
            has_internal_dangling_initial and
            (input_syllable_count >= 4) and (not all_initial_compact_query) and
            try_build_typo_normalized_prefix_candidate_local(
            stable_prefix_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typostable=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False);
            Exit;
        end;
        if (m_dictionary <> nil) and (m_last_lookup_normalized_from <> '') and
            has_internal_dangling_initial and
            (input_syllable_count >= 4) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (build_string_prefix_exact_partial_candidates(lookup_text,
            raw_candidates) or build_exact_leading_prefix_partial_candidates(
            lookup_text, raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typoprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and (m_last_lookup_normalized_from <> '') and
            has_multi_syllable_input and (input_syllable_count >= 4) and
            (input_syllable_count <= 8) and (not all_initial_compact_query) and
            build_adjacent_swap_prefix_partial_candidates(
            m_last_lookup_normalized_from, raw_candidates) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d typoswapprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and (input_syllable_count <= 4) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_strong_short_exact_complete_lookup_local(input_syllable_count)) and
            build_fixed_wo_tail_dictionary_candidates(lookup_text,
            fixed_wo_tail_candidates) then
        begin
            m_candidates := fixed_wo_tail_candidates;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end
            else if build_raw_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            remove_partials_shadowed_by_complete_text_local(m_candidates);
            sort_candidates_lightweight(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d fixedheadtail=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), m_candidates[0], False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            has_raw_safe_trailing_initial_typing_state and
            (input_syllable_count >= 3) and (input_syllable_count <= 6) and
            (not all_initial_compact_query) and
            (build_string_prefix_exact_partial_candidates(lookup_text,
            raw_candidates) or build_exact_leading_prefix_partial_candidates(
            lookup_text, raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            if input_syllable_count >= 4 then
            begin
                SetLength(fixed_wo_tail_candidates, 0);
                if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                    fixed_wo_tail_candidates) then
                begin
                    m_candidates := merge_candidate_lists(
                        fixed_wo_tail_candidates, m_candidates, 0);
                    suppress_non_fixed_wo_single_head_partials_local(m_candidates);
                end;
                incremental_partial_reuse_applied := False;
                if input_syllable_count = 4 then
                begin
                    ensure_best_short_exact_prefix_tail_candidate_visible(
                        m_candidates);
                end;
                if (not candidate_list_has_complete_current_query_local(
                    m_candidates)) and
                    try_reuse_incremental_partial_tail_candidates_local(
                    incremental_partial_candidates, False) then
                begin
                    m_candidates := merge_candidate_lists(
                        incremental_partial_candidates, m_candidates, 0);
                    incremental_partial_reuse_applied := True;
                end;
                ensure_supported_short_four_two_prefix_partial_visible_local(
                    get_effective_compact_pinyin_syllables(lookup_text),
                    m_candidates);
                sort_candidates_lightweight(m_candidates);
                if incremental_partial_reuse_applied and
                    (Length(incremental_partial_candidates) > 0) and
                    (get_candidate_text_unit_count(Trim(
                    incremental_partial_candidates[0].text)) >= 2) then
                begin
                    promote_candidate_to_front_local(m_candidates,
                        incremental_partial_candidates[0]);
                end;
                if incremental_partial_reuse_applied then
                begin
                    m_last_lookup_incremental_partial_reuse := True;
                end;
                if Length(fixed_wo_tail_candidates) = 0 then
                begin
                    promote_longest_partial_prefix_candidate_local(m_candidates);
                end;
                finalize_lookup_timing_info;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d trailingprefixfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]);
                    if Length(m_candidates) > 0 then
                    begin
                        if incremental_partial_reuse_applied then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra +
                                ' incrementalpartialreuse=1';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            ' ' + get_candidate_debug_summary(m_candidates[0]);
                    end;
                end;
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d trailingprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and
            (not all_initial_compact_query) and
            (has_internal_dangling_initial or
            (not is_full_pinyin_key(lookup_text))) then
        begin
            SetLength(m_candidates, 0);
            if try_build_internal_dangling_prefix_partial_candidate_local(
                stable_prefix_candidate) and
                (Trim(stable_prefix_candidate.text) <> '') and
                (Trim(stable_prefix_candidate.comment) <> '') then
            begin
                SetLength(m_candidates, 1);
                m_candidates[0] := stable_prefix_candidate;
            end;
            if Length(m_candidates) > 0 then
            begin
                sort_candidates_lightweight(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                i := 0;
                while i <= High(m_candidates) do
                begin
                    if SameText(Trim(m_candidates[i].text),
                        Trim(stable_prefix_candidate.text)) and
                        SameText(Trim(m_candidates[i].comment),
                        Trim(stable_prefix_candidate.comment)) then
                    begin
                        stable_prefix_candidate := m_candidates[i];
                        while i > 0 do
                        begin
                            m_candidates[i] := m_candidates[i - 1];
                            Dec(i);
                        end;
                        m_candidates[0] := stable_prefix_candidate;
                        Break;
                    end;
                    Inc(i);
                end;
                finalize_lookup_timing_info;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d danglingprefixfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]);
                    if Length(m_candidates) > 0 then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            ' ' + get_candidate_debug_summary(m_candidates[0]);
                    end;
                end;
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            has_internal_dangling_initial and (input_syllable_count <= 4) and
            (not all_initial_compact_query) then
        begin
            SetLength(m_candidates, 0);
            if try_build_internal_dangling_prefix_partial_candidate_local(
                stable_prefix_candidate) and
                (Trim(stable_prefix_candidate.text) <> '') and
                (Trim(stable_prefix_candidate.comment) <> '') then
            begin
                SetLength(m_candidates, 1);
                m_candidates[0] := stable_prefix_candidate;
                finalize_lookup_timing_info;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortdanglingfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]);
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        ' ' + get_candidate_debug_summary(m_candidates[0]);
                end;
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 4) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not is_full_pinyin_key(lookup_text)) and
            build_adjacent_swap_prefix_partial_candidates(m_composition_text,
            raw_candidates) then
        begin
            m_candidates := raw_candidates;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d swapprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if try_build_three_syllable_pronoun_one_plus_two_complete_candidate_local(
            direct_three_syllable_one_plus_two_candidate,
            direct_three_syllable_one_plus_two_encoded_path) then
        begin
            m_last_lookup_short312_head_fast := True;
            SetLength(m_candidates, 1);
            m_candidates[0] := direct_three_syllable_one_plus_two_candidate;
            remember_segment_path_for_candidate(
                direct_three_syllable_one_plus_two_candidate.text, '',
                direct_three_syllable_one_plus_two_encoded_path,
                direct_three_syllable_one_plus_two_candidate.score);
            if dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    raw_candidates, 0);
            end;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
                suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
                sort_candidates_lightweight(m_candidates);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            if (Length(fixed_wo_tail_candidates) > 0) and
                (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) then
            begin
                direct_three_syllable_one_plus_two_candidate := m_candidates[0];
            end
            else
            begin
                m_candidates[0] := direct_three_syllable_one_plus_two_candidate;
            end;
            ensure_short_three_exact_pair_candidates_visible_local(m_candidates);
            if direct_three_syllable_one_plus_two_candidate.display_kind =
                cdk_lm_compound then
            begin
                if not SameText(m_short_three_exact_pair_query,
                    normalize_pinyin_text(lookup_text)) then
                begin
                    m_short_three_exact_pair_query :=
                        normalize_pinyin_text(lookup_text);
                    SetLength(m_short_three_exact_pair_texts, 0);
                end;
                direct_three_syllable_one_plus_two_registered := False;
                for i := 0 to High(m_short_three_exact_pair_texts) do
                begin
                    if SameText(Trim(m_short_three_exact_pair_texts[i]),
                        Trim(direct_three_syllable_one_plus_two_candidate.text)) then
                    begin
                        direct_three_syllable_one_plus_two_registered := True;
                        Break;
                    end;
                end;
                if not direct_three_syllable_one_plus_two_registered then
                begin
                    SetLength(m_short_three_exact_pair_texts,
                        Length(m_short_three_exact_pair_texts) + 1);
                    m_short_three_exact_pair_texts[
                        High(m_short_three_exact_pair_texts)] := Trim(
                        direct_three_syllable_one_plus_two_candidate.text);
                end;
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short312head=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]),
                direct_three_syllable_one_plus_two_candidate, False);
            Exit;
        end;
        if dictionary_exact_lookup_cached(lookup_text, raw_candidates) and
            try_build_three_syllable_two_plus_one_complete_candidate_local(
            direct_three_syllable_two_plus_one_candidate,
            direct_three_syllable_two_plus_one_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := direct_three_syllable_two_plus_one_candidate;
            remember_segment_path_for_candidate(
                direct_three_syllable_two_plus_one_candidate.text, '',
                direct_three_syllable_two_plus_one_encoded_path,
                direct_three_syllable_two_plus_one_candidate.score);
            m_candidates := merge_candidate_lists(m_candidates,
                raw_candidates, 0);
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            m_candidates[0] := direct_three_syllable_two_plus_one_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short321fast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]),
                direct_three_syllable_two_plus_one_candidate, False);
            Exit;
        end;
        if try_build_five_syllable_strong_exact_partition_candidate_local(
            direct_chain_candidate, direct_chain_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := direct_chain_candidate;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            promote_candidate_to_front_local(m_candidates,
                direct_chain_candidate);
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d strongexact5=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added),
                Ord(runtime_redup_added), Ord(all_initial_compact_query)]),
                direct_chain_candidate, False);
            // Preserved finalization may collapse this path with an inferred
            // duplicate. Keep the generated-path marker used by rendering and
            // by the user-dictionary learning guard.
            for i := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[i].text),
                    Trim(direct_chain_candidate.text)) and
                    SameText(Trim(m_candidates[i].comment),
                    Trim(direct_chain_candidate.comment)) then
                begin
                    m_candidates[i].display_kind := cdk_lm_compound;
                    Break;
                end;
            end;
            // This candidate is a local five-syllable recovery, not a stable
            // incremental seed. Reusing it on the next key can lock a long
            // sentence onto this temporary segmentation before the wider
            // lattice has a chance to reconsider the boundary.
            m_last_candidates_incrementally_reusable := False;
            m_last_ranked_query_key := '';
            m_last_ranked_top_path := '';
            Exit;
        end;
        if (not has_strong_exact_complete_lookup_weight_local(512)) and
            try_build_three_syllable_two_plus_one_prefix_continuation_candidate_local(
            direct_three_syllable_two_plus_one_candidate,
            direct_three_syllable_two_plus_one_encoded_path) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' [s321post]';
            end;
            SetLength(m_candidates, 1);
            m_candidates[0] := direct_three_syllable_two_plus_one_candidate;
            remember_segment_path_for_candidate(
                direct_three_syllable_two_plus_one_candidate.text, '',
                direct_three_syllable_two_plus_one_encoded_path,
                direct_three_syllable_two_plus_one_candidate.score);
            if dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    raw_candidates, 0);
            end;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' [s321head]';
            end;
            incremental_partial_reuse_applied :=
                has_incremental_composition_local and
                (try_reuse_incremental_raw_comment_candidates_local(
                incremental_partial_candidates, 1) or
                try_reuse_incremental_partial_tail_candidates_local(
                incremental_partial_candidates, True, 1));
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' [s321reuse=%d prev=%s n=%d]',
                    [Ord(incremental_partial_reuse_applied),
                    previous_lookup_key, Length(previous_candidates)]);
            end;
            if incremental_partial_reuse_applied then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    incremental_partial_candidates, 0);
                m_last_lookup_incremental_partial_reuse := True;
            end
            else if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' [s321single]';
            end;
            if not incremental_partial_reuse_applied then
            begin
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                ensure_rich_head_single_char_partials_visible(m_candidates);
            end;
            m_candidates[0] := direct_three_syllable_two_plus_one_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short321tailfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]),
                direct_three_syllable_two_plus_one_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 3) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            build_exact_leading_prefix_partial_candidates(lookup_text,
            exact_head_partial_candidates) and
            build_short_particle_tail_from_prefix_partials_fast_local(
            lookup_text, exact_head_partial_candidates,
            short_particle_tail_candidates) then
        begin
            m_candidates := short_particle_tail_candidates;
            m_candidates := merge_candidate_lists(m_candidates,
                exact_head_partial_candidates, 0);
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 6);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            sort_candidates_lightweight(m_candidates);
            filter_non_bmp_single_char_candidates(m_candidates);
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortparticletailfast=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]);
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            finalize_lookup_timing_info;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 4) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            short4_standalone_particle_tail and
            build_short_particle_tail_dictionary_candidates(lookup_text,
            short_particle_tail_candidates) then
        begin
            m_candidates := short_particle_tail_candidates;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 6);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
            sort_candidates_lightweight(m_candidates);
            filter_non_bmp_single_char_candidates(m_candidates);
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short4particledictfast=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]);
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
            end;
            finalize_lookup_timing_info;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 3) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_strong_short_exact_complete_lookup_local(1)) and
            ((is_full_pinyin_key(lookup_text) and
            build_exact_leading_prefix_partial_candidates(lookup_text,
            raw_candidates)) or
            build_raw_leading_prefix_partial_candidates(lookup_text,
            raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            if try_build_compact_interrogative_location_candidate(
                compact_runtime_candidate) then
            begin
                SetLength(compact_runtime_candidates, 1);
                compact_runtime_candidates[0] := compact_runtime_candidate;
                m_candidates := merge_candidate_lists(compact_runtime_candidates,
                    m_candidates, 0);
                runtime_phrase_added := True;
                m_runtime_common_pattern_text := compact_runtime_candidate.text;
            end;
            if build_explicit_apostrophe_prefix_partial_candidates(
                explicit_apostrophe_prefix_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    explicit_apostrophe_prefix_candidates, m_candidates, 0);
            end;
            if build_short_particle_tail_from_prefix_partials_fast_local(
                lookup_text, m_candidates, short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    short_particle_tail_candidates, m_candidates, 0);
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 6);
                ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
                sort_candidates_lightweight(m_candidates);
                filter_non_bmp_single_char_candidates(m_candidates);
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rawshortparticletailfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]);
                    if Length(m_candidates) > 0 then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                            get_candidate_debug_summary(m_candidates[0]);
                    end;
                end;
                finalize_lookup_timing_info;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
            if build_lexicon_longer_prefix_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 6);
                ensure_best_first_syllable_single_partial_visible(m_candidates, 2);
                sort_candidates_lightweight(m_candidates);
                filter_non_bmp_single_char_candidates(m_candidates);
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rawshortparticledictfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]);
                    if Length(m_candidates) > 0 then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                            get_candidate_debug_summary(m_candidates[0]);
                    end;
                end;
                finalize_lookup_timing_info;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            promote_best_supported_complete_candidate_visible_local(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortprefixraw=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 4) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not (has_raw_safe_trailing_initial_typing_state and
            SameText(Copy(lookup_text, 1, 2), 'yi') and
            ((Length(lookup_text) < 3) or (lookup_text[3] <> 'n')))) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            short4_has_strong_exact :=
                has_strong_short_exact_complete_lookup_local(1);
            note_debug_helper_elapsed('s4strong', phase_start_tick);
            if not short4_has_strong_exact then
            begin
                short4_is_particle_tail := short4_standalone_particle_tail and
                    try_get_short_particle_tail_query_parts(lookup_text,
                    short4_tail_key, short4_tail_text, short4_head_key);
                if short4_is_particle_tail then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    short4_prefix_candidates_ready :=
                        build_exact_leading_prefix_partial_candidates(lookup_text,
                        raw_candidates);
                    note_debug_helper_elapsed('s4exactprefix', phase_start_tick);
                end
                else
                begin
                    short4_prefix_candidates_ready := False;
                end;
                if not short4_prefix_candidates_ready then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    short4_prefix_candidates_ready :=
                        build_raw_leading_prefix_partial_candidates(lookup_text,
                        raw_candidates);
                    note_debug_helper_elapsed('s4rawprefix', phase_start_tick);
                end;
            end
            else
            begin
                short4_prefix_candidates_ready := False;
            end;
            if short4_prefix_candidates_ready then
            begin
                m_candidates := raw_candidates;
            phase_start_tick := nc_monotonic_tick_ms;
            if build_explicit_apostrophe_prefix_partial_candidates(
                explicit_apostrophe_prefix_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    explicit_apostrophe_prefix_candidates, m_candidates, 0);
            end;
            note_debug_helper_elapsed('s4apost', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if build_short_pinyin_prefix_completion_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
                remove_partials_shadowed_by_complete_text_local(m_candidates);
            end;
            note_debug_helper_elapsed('s4pred', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
            end;
            note_debug_helper_elapsed('s4fixed', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
            end;
            if build_short_particle_prefix_partial_candidates_local(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
            end;
            note_debug_helper_elapsed('s4part', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            note_debug_helper_elapsed('s4visible', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
            filter_unsupported_short_four_ab_cd_complete_candidates_local(
                m_candidates);
            filter_unsupported_short_four_ab_c_partial_candidates_local(
                m_candidates);
            promote_best_supported_complete_candidate_visible_local(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            note_debug_helper_elapsed('s4post', phase_start_tick);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short4prefixraw=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
                Exit;
            end;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count = 2) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_strong_short_exact_complete_lookup_local(input_syllable_count)) and
            (not has_strong_short_exact_complete_lookup_local(1)) and
            (((is_full_pinyin_key(lookup_text)) and
            build_exact_leading_prefix_partial_candidates(lookup_text,
            raw_candidates)) or
            build_raw_leading_prefix_partial_candidates(lookup_text,
            raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            if (m_confirmed_text <> '') and
                m_dictionary.lookup(lookup_text,
                exact_head_partial_candidates) then
            begin
                // A selected prefix makes the remaining shorthand an
                // independent lookup. Retain its complete dictionary matches.
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
            end;
            if build_short_pinyin_prefix_completion_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
                remove_partials_shadowed_by_complete_text_local(m_candidates);
            end;
            if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
            end;
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            promote_best_supported_complete_candidate_visible_local(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d short2prefixraw=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count <= 3) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not is_short_mixed_head_only_fast_anchor_query) and
            (not has_strong_short_exact_complete_lookup_local(1)) and
            build_segment_candidates_timed(raw_candidates, False,
            allow_relaxed_missing_apostrophe) then
        begin
            m_candidates := raw_candidates;
            if build_lexicon_longer_prefix_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if build_short_pinyin_prefix_completion_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
                remove_partials_shadowed_by_complete_text_local(m_candidates);
            end;
            if build_fixed_wo_tail_dictionary_candidates(lookup_text,
                fixed_wo_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(fixed_wo_tail_candidates,
                    m_candidates, 0);
            end;
            if build_short_particle_tail_dictionary_candidates(lookup_text,
                short_particle_tail_candidates) then
            begin
                m_candidates := merge_candidate_lists(short_particle_tail_candidates,
                    m_candidates, 0);
            end;
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            promote_best_supported_complete_candidate_visible_local(m_candidates);
            suppress_non_fixed_wo_single_head_partials_local(m_candidates);
            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortpartial=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), False, False);
            Exit;
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 8) and
            is_full_pinyin_key(lookup_text) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not should_defer_exact_chain_for_extendable_tail_local) and
            try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) and
            (Trim(stable_prefix_candidate.comment) = '') and
            (get_candidate_text_unit_count(Trim(stable_prefix_candidate.text)) =
            input_syllable_count) and
            (get_encoded_path_segment_count_local(
            direct_chain_encoded_path) > 1) and
            (not is_problematic_single_char_chain_candidate_for_query(
            lookup_text, stable_prefix_candidate)) and
            (not ranked_path_requires_exact_competition_validation_local(
            stable_prefix_candidate.text, direct_chain_encoded_path)) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            remember_segment_path_for_candidate(
                stable_prefix_candidate.text,
                stable_prefix_candidate.comment,
                direct_chain_encoded_path,
                stable_prefix_candidate.score);
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                promote_candidate_to_front_local(m_candidates,
                    stable_prefix_candidate);
            end;
            finalize_direct_preferred_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankedearlysafefast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        // Do not early-return a full-length long sentence by merely extending
        // the previous top path. That path can already contain a wrong single
        // character tail (for example shang/quan), and accepting it here skips
        // the exact-chunk lattice that can recover an existing word such as
        // shangquan. Prefix queries still use the fast continuation path below.
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 8) and
            ((input_syllable_count = 5) or
            (not has_incremental_composition_local)) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            is_full_pinyin_key(lookup_text) and
            (not should_defer_exact_chain_for_extendable_tail_local) and
            ((not has_precise_extendable_tail_syllable_local(lookup_text)) or
            has_complete_exact_tail_phrase_local(2) or
            has_complete_exact_tail_phrase_local(3)) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if try_build_exact_span_chain_candidate_local(
                direct_chain_candidate, direct_chain_encoded_path) and
                (Trim(direct_chain_candidate.comment) = '') and
                (get_candidate_text_unit_count(
                Trim(direct_chain_candidate.text)) = input_syllable_count) and
                (get_encoded_path_segment_count_local(
                direct_chain_encoded_path) > 1) and
                (not is_problematic_single_char_chain_candidate_for_query(
                lookup_text, direct_chain_candidate)) then
            begin
                note_debug_helper_elapsed('tdchainpre', phase_start_tick);
                SetLength(m_candidates, 1);
                m_candidates[0] := direct_chain_candidate;
                remember_segment_path_for_candidate(
                    direct_chain_candidate.text,
                    direct_chain_candidate.comment,
                    direct_chain_encoded_path,
                    direct_chain_candidate.dict_weight);
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                promote_long_full_query_exact_over_chain_candidate_local(
                    m_candidates, direct_chain_candidate.score);
                if Length(m_candidates) > 0 then
                begin
                    direct_chain_candidate := m_candidates[0];
                end;
                ensure_bounded_single_char_partial_pool(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                promote_candidate_to_front_local(m_candidates,
                    direct_chain_candidate);
                finalize_modal_tail_sentence_candidate(m_candidates);
                SetLength(prefast_char_lm_texts, 1);
                prefast_char_lm_texts[0] := Trim(m_candidates[0].text);
                if get_cached_char_lm_scores(prefast_char_lm_texts,
                    prefast_char_lm_scores, clsm_full, '') and
                    (Length(prefast_char_lm_scores) = 1) and
                    (prefast_char_lm_scores[0] <
                    c_long_prefast_catchup_char_lm_threshold) then
                begin
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates, True);
                    mark_debug_target_candidate_stage_local(2048,
                        m_candidates);
                end;
                if Length(m_candidates) > 0 then
                begin
                    direct_chain_candidate := m_candidates[0];
                end;
                finalize_preserved_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainprefast=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]),
                    direct_chain_candidate, False);
                Exit;
            end;
            note_debug_helper_elapsed('tdchainpre', phase_start_tick);
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 6) and (input_syllable_count <= 40) and
            is_full_pinyin_key(lookup_text) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) and
            (Trim(stable_prefix_candidate.comment) = '') and
            (get_candidate_text_unit_count(Trim(stable_prefix_candidate.text)) =
            input_syllable_count) and
            (not is_problematic_single_char_chain_candidate_for_query(
            lookup_text, stable_prefix_candidate)) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            if (Trim(stable_prefix_candidate.comment) = '') and
                (direct_chain_encoded_path <> '') then
            begin
                remember_segment_path_for_candidate(
                    stable_prefix_candidate.text,
                    stable_prefix_candidate.comment,
                    direct_chain_encoded_path,
                    stable_prefix_candidate.score);
            end;

            ranked_directional_repair_applied :=
                try_repair_ranked_directional_context_local(
                stable_prefix_candidate, direct_chain_encoded_path,
                head_tail_complete_candidate,
                head_tail_complete_encoded_path);
            if ranked_directional_repair_applied then
            begin
                stable_prefix_candidate := head_tail_complete_candidate;
                direct_chain_encoded_path := head_tail_complete_encoded_path;
                m_candidates[0] := stable_prefix_candidate;
                remember_segment_path_for_candidate(
                    stable_prefix_candidate.text,
                    stable_prefix_candidate.comment,
                    direct_chain_encoded_path,
                    stable_prefix_candidate.score);
            end;

            phase_start_tick := nc_monotonic_tick_ms;
            if (not ranked_directional_repair_applied) and
                ranked_path_requires_exact_competition_validation_local(
                stable_prefix_candidate.text, direct_chain_encoded_path) then
            begin
                ensure_best_exact_chunk_chain_complete_candidate_visible(
                    m_candidates, True, False);
            end;
            note_debug_helper_elapsed('trankedvalidate', phase_start_tick);
            if (input_syllable_count < 9) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) and
                (not is_problematic_single_char_chain_candidate_for_query(
                lookup_text, m_candidates[0])) then
            begin
                if ranked_directional_repair_applied then
                begin
                    finalize_preserved_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankeddirectionfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]),
                        stable_prefix_candidate, False);
                end
                else
                begin
                    finalize_direct_preferred_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankedvalidatedfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                end;
                Exit;
            end;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (delayed_long_decode_mode or (input_syllable_count >= 6) or
            (has_raw_safe_trailing_initial_typing_state and
            (input_syllable_count >= 5))) and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 5) and (input_syllable_count <= 40) and
            ((not m_composition_built_incrementally) or
            (not should_defer_exact_chain_for_extendable_tail_local) or
            has_reliable_exact_tail_candidate_local(4)) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            FillChar(fast_chain_seed_candidate, SizeOf(fast_chain_seed_candidate), 0);
            fast_chain_seed_candidate.comment := fallback_comment;
            SetLength(m_candidates, 1);
            m_candidates[0] := fast_chain_seed_candidate;
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_best_exact_chunk_chain_complete_candidate_visible(
                m_candidates, False, not m_composition_built_incrementally,
                m_composition_built_incrementally);
            note_debug_helper_elapsed('tpldp', phase_start_tick);
            if (Length(m_candidates) > 0) and
                (m_candidates[0].source = cs_rule) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) then
            begin
                fast_chain_complete_candidate := m_candidates[0];
                phase_start_tick := nc_monotonic_tick_ms;
                if (input_syllable_count < 9) and
                    build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                note_debug_helper_elapsed('tplprefix', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                if input_syllable_count < 6 then
                begin
                    ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                    ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                end;
                note_debug_helper_elapsed('tplsingle', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                if (input_syllable_count >= 6) and
                    (Trim(fast_chain_complete_candidate.comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(fast_chain_complete_candidate.text)) =
                    input_syllable_count) and
                    (get_segment_path_for_candidate(
                    fast_chain_complete_candidate) <> '') and
                    (not is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, fast_chain_complete_candidate)) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info + ' tplfixedskip=1';
                    end;
                end
                else
                begin
                    ensure_best_fixed_boundary_exact_chain_candidate_visible(
                        m_candidates);
                end;
                note_debug_helper_elapsed('tplfixed', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_simple_fixed_boundary_chain_candidate_visible(
                    m_candidates);
                note_debug_helper_elapsed('tplsimple', phase_start_tick);
                if (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                    input_syllable_count) then
                begin
                    if (Trim(fast_chain_complete_candidate.text) = '') or
                        (m_candidates[0].score > fast_chain_complete_candidate.score +
                        12000) then
                    begin
                        fast_chain_complete_candidate := m_candidates[0];
                    end;
                end;
                if has_raw_safe_trailing_initial_typing_state then
                begin
                    promote_stable_prefix_partial_candidate_local(m_candidates, 2);
                end
                else
                begin
                    m_candidates[0] := fast_chain_complete_candidate;
                end;
                ensure_complete_long_top_tail_partial_second_local;
                phase_start_tick := nc_monotonic_tick_ms;
                finalize_direct_preferred_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d prelightfastdp=1 fastfinal=1 typofast=%d fqexact=%d allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted),
                    Ord(all_initial_compact_query)]));
                note_debug_helper_elapsed('tplfinal', phase_start_tick);
                Exit;
            end;
            if has_exact_complete_phrase_candidate_for_syllable_count(
                m_candidates, input_syllable_count) or
                has_natural_complete_segment_path_candidate_for_syllable_count(
                m_candidates, input_syllable_count) then
            begin
                finalize_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d prelightfastmiss=1 typofast=%d fqexact=%d allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted), Ord(all_initial_compact_query)]));
                Exit;
            end;
            SetLength(m_candidates, 0);
        end;

        SetLength(top_complete_chain_candidates, 0);
        SetLength(top_complete_chain_encoded_paths, 0);
        if (m_dictionary <> nil) and has_multi_syllable_input and
            has_raw_safe_trailing_initial_typing_state and
            (input_syllable_count >= 4) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            get_cached_stable_prefix_candidate_local(stable_prefix_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                filter_single_char_partial_candidates_for_current_query(
                    exact_head_partial_candidates);
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d safeinitialfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            has_raw_safe_trailing_initial_typing_state and
            (input_syllable_count >= 4) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if try_build_fragment_prefix_partial_candidate_local(
                fragment_prefix_candidate) then
            begin
                note_debug_helper_elapsed('tsafeinitfrag', phase_start_tick);
                SetLength(m_candidates, 1);
                m_candidates[0] := fragment_prefix_candidate;
                if input_syllable_count >= 7 then
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d safeinitialfrag=1 lighttail=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]), False, False);
                    Exit;
                end;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    filter_single_char_partial_candidates_for_current_query(
                        exact_head_partial_candidates);
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                m_candidates[0] := fragment_prefix_candidate;
                finalize_prefix_partial_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d safeinitialfrag=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]));
                Exit;
            end;
            note_debug_helper_elapsed('tsafeinitfrag', phase_start_tick);
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 6) and (input_syllable_count <= 40) and
            has_incremental_composition_local and
            (not is_full_pinyin_key(lookup_text)) and
            has_extendable_trailing_prefix_query_text(lookup_text) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            remember_segment_path_for_candidate(stable_prefix_candidate.text,
                stable_prefix_candidate.comment, direct_chain_encoded_path,
                stable_prefix_candidate.score);
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankedtailprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 7) and (input_syllable_count <= 40) and
            has_incremental_composition_local and
            is_full_pinyin_key(lookup_text) and
            should_defer_exact_chain_for_extendable_tail_local and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) or
            get_cached_stable_prefix_candidate_local(stable_prefix_candidate)) then
        begin
            if try_complete_contextual_directional_partial_local(
                stable_prefix_candidate, direct_chain_encoded_path,
                head_tail_complete_candidate,
                head_tail_complete_encoded_path) then
            begin
                stable_prefix_candidate := head_tail_complete_candidate;
                direct_chain_encoded_path := head_tail_complete_encoded_path;
            end;
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            if (Trim(stable_prefix_candidate.comment) = '') and
                (direct_chain_encoded_path <> '') then
            begin
                remember_segment_path_for_candidate(stable_prefix_candidate.text,
                    stable_prefix_candidate.comment, direct_chain_encoded_path,
                    stable_prefix_candidate.score);
            end;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                filter_single_char_partial_candidates_for_current_query(
                    exact_head_partial_candidates);
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d extendabletailcachefast=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 7) and (input_syllable_count <= 40) and
            has_incremental_composition_local and
            is_full_pinyin_key(lookup_text) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not (m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []))) and
            (not has_strong_full_query_exact_complete_candidate_local) and
            (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            (not has_complete_exact_tail_phrase_local(2)) and
            (not has_complete_exact_tail_phrase_local(3)) and
            get_cached_stable_prefix_candidate_local(stable_prefix_candidate) then
        begin
            if try_complete_contextual_directional_partial_local(
                stable_prefix_candidate, '', head_tail_complete_candidate,
                head_tail_complete_encoded_path) then
            begin
                stable_prefix_candidate := head_tail_complete_candidate;
                direct_chain_encoded_path := head_tail_complete_encoded_path;
            end;
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            if (Trim(stable_prefix_candidate.comment) = '') and
                (direct_chain_encoded_path <> '') then
            begin
                remember_segment_path_for_candidate(stable_prefix_candidate.text,
                    stable_prefix_candidate.comment, direct_chain_encoded_path,
                    stable_prefix_candidate.score);
            end;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                filter_single_char_partial_candidates_for_current_query(
                    exact_head_partial_candidates);
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d extendabletailstablefast=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 3) and (input_syllable_count <= 40) and
            has_incremental_composition_local and
            (is_full_pinyin_key(lookup_text) or
            has_extendable_trailing_prefix_query_text(lookup_text)) and
            (not has_strong_full_query_exact_complete_candidate_local) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            if is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and
                (input_syllable_count <= 8) and
                (has_short_extendable_tail_prefix_local or
                has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count)) and
                (has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count) or
                ((not has_complete_exact_tail_phrase_local(2)) and
                (not has_complete_exact_tail_phrase_local(3)))) and
                build_exact_leading_prefix_partial_candidates(lookup_text,
                raw_candidates) then
            begin
                m_candidates := raw_candidates;
                if m_config.fuzzy_pinyin_enabled and
                    (m_config.fuzzy_pinyin_rules <> []) and
                    build_lightweight_sentence_candidates(lookup_text,
                    lightweight_sentence_candidates) then
                begin
                    m_candidates := merge_candidate_lists(
                        lightweight_sentence_candidates, m_candidates, 0);
                end;
                filter_single_char_partial_candidates_for_current_query(
                    m_candidates);
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                ensure_fast_modal_tail_candidate_visible_local(m_candidates);
                if input_syllable_count = 4 then
                begin
                    ensure_best_short_exact_prefix_tail_candidate_visible(
                        m_candidates);
                end;
                incremental_partial_reuse_applied := False;
                if (not candidate_list_has_complete_current_query_local(
                    m_candidates)) and
                    try_reuse_incremental_partial_tail_candidates_local(
                    incremental_partial_candidates, False) then
                begin
                    m_candidates := merge_candidate_lists(
                        incremental_partial_candidates, m_candidates, 0);
                    if (Length(incremental_partial_candidates) > 0) and
                        (get_candidate_text_unit_count(Trim(
                        incremental_partial_candidates[0].text)) >= 2) then
                    begin
                        promote_candidate_to_front_local(m_candidates,
                            incremental_partial_candidates[0]);
                    end;
                    incremental_partial_reuse_applied := True;
                    m_last_lookup_incremental_partial_reuse := True;
                end;
                ensure_supported_short_four_two_prefix_partial_visible_local(
                    get_effective_compact_pinyin_syllables(lookup_text),
                    m_candidates);
                if incremental_partial_reuse_applied then
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d extendabletailprefixfast=1 incrementalpartialreuse=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]), False, False);
                end
                else
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d extendabletailprefixfast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]), False, False);
                end;
                Exit;
            end;
            top_complete_chain_applied := False;
            if False and not should_defer_exact_chain_for_extendable_tail_local then
            begin
                top_complete_chain_applied :=
                    try_build_top_complete_phrase_chain_candidates_local(
                    top_complete_chain_candidates,
                    top_complete_chain_encoded_paths);
            end;
            if (not top_complete_chain_applied) and
                (not try_build_light_stable_phrase_chain_candidate_local(
                stable_prefix_candidate, direct_chain_encoded_path)) then
            begin
                SetLength(m_candidates, 0);
            end
            else
            begin
                note_debug_helper_elapsed('tlchain', total_start_tick);
                if top_complete_chain_applied then
                begin
                    m_candidates := top_complete_chain_candidates;
                    stable_prefix_candidate := m_candidates[0];
                    direct_chain_encoded_path :=
                        top_complete_chain_encoded_paths[0];
                    for i := 0 to High(top_complete_chain_encoded_paths) do
                    begin
                        remember_segment_path_for_candidate(
                            top_complete_chain_candidates[i].text,
                            top_complete_chain_candidates[i].comment,
                            top_complete_chain_encoded_paths[i],
                            top_complete_chain_candidates[i].score);
                    end;
                end
                else
                begin
                    SetLength(m_candidates, 1);
                    m_candidates[0] := stable_prefix_candidate;
                    remember_segment_path_for_candidate(
                        stable_prefix_candidate.text,
                        stable_prefix_candidate.comment,
                        direct_chain_encoded_path,
                        stable_prefix_candidate.score);
                end;
            phase_start_tick := nc_monotonic_tick_ms;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                note_debug_helper_elapsed('tlprefix', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                filter_single_char_partial_candidates_for_current_query(
                    exact_head_partial_candidates);
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                note_debug_helper_elapsed('tlprefixpost', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                if try_build_exact_head_tail_complete_candidate_local(
                    m_candidates, stable_prefix_candidate,
                    head_tail_complete_candidate,
                    head_tail_complete_encoded_path) then
                begin
                    remember_segment_path_for_candidate(
                        head_tail_complete_candidate.text,
                        head_tail_complete_candidate.comment,
                        head_tail_complete_encoded_path,
                        head_tail_complete_candidate.score);
                end;
                note_debug_helper_elapsed('tlheadtail', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                if try_pick_supported_complete_candidate_local(m_candidates,
                    stable_prefix_candidate, direct_chain_encoded_path,
                    supported_complete_candidate,
                    supported_complete_encoded_path) then
                begin
                    head_tail_complete_candidate := supported_complete_candidate;
                    head_tail_complete_encoded_path :=
                        supported_complete_encoded_path;
                    remember_segment_path_for_candidate(
                        head_tail_complete_candidate.text,
                        head_tail_complete_candidate.comment,
                        head_tail_complete_encoded_path,
                        head_tail_complete_candidate.score);
                end;
                note_debug_helper_elapsed('tlsupport1', phase_start_tick);
            end;
            note_debug_helper_elapsed('tlprefix', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            note_debug_helper_elapsed('tlfastsingle', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if (not has_raw_safe_trailing_initial_typing_state) and
                is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 7) and (input_syllable_count <= 18) and
                (not should_defer_exact_chain_for_extendable_tail_local) and
                build_lightweight_sentence_candidates(lookup_text,
                lightweight_sentence_candidates) then
            begin
                note_debug_helper_elapsed('tllight', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                m_candidates := merge_candidate_lists(lightweight_sentence_candidates,
                    m_candidates, 0);
                if (Trim(head_tail_complete_candidate.text) <> '') then
                begin
                    stable_prefix_candidate := head_tail_complete_candidate;
                    direct_chain_encoded_path := head_tail_complete_encoded_path;
                end;
                if try_pick_supported_complete_candidate_local(m_candidates,
                    stable_prefix_candidate, direct_chain_encoded_path,
                    supported_complete_candidate,
                    supported_complete_encoded_path) then
                begin
                    head_tail_complete_candidate := supported_complete_candidate;
                    head_tail_complete_encoded_path :=
                        supported_complete_encoded_path;
                    remember_segment_path_for_candidate(
                        head_tail_complete_candidate.text,
                        head_tail_complete_candidate.comment,
                        head_tail_complete_encoded_path,
                        head_tail_complete_candidate.score);
                end;
                note_debug_helper_elapsed('tllightpost', phase_start_tick);
            end;
            note_debug_helper_elapsed('tllight', phase_start_tick);
            if (Trim(head_tail_complete_candidate.text) <> '') and
                (head_tail_complete_candidate.score >=
                stable_prefix_candidate.score - 600) then
            begin
                m_candidates[0] := head_tail_complete_candidate;
                stable_prefix_candidate := head_tail_complete_candidate;
            end
            else
            begin
                m_candidates[0] := stable_prefix_candidate;
            end;
            phase_start_tick := nc_monotonic_tick_ms;
            if (Length(m_candidates) = 0) or
                (Trim(m_candidates[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
                input_syllable_count) then
            begin
                ensure_fast_tail_exact_completion_visible_local(m_candidates);
            end;
            note_debug_helper_elapsed('tltail', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            promote_best_supported_complete_candidate_visible_local(m_candidates);
            note_debug_helper_elapsed('tlsupport2', phase_start_tick);
            if should_defer_exact_chain_for_extendable_tail_local and
                (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) <> '') then
            begin
                finalize_preserved_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightchainextendabletail=1 allinit=%d',
                    [Ord(has_multi_syllable_input),
                    Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
                Exit;
            end;
            if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 4) and
                (input_syllable_count <= 8) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
                ensure_fast_modal_tail_candidate_visible_local(m_candidates);
                note_debug_helper_elapsed('tlshorttail', phase_start_tick);
            end;
            if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 6) and
                ((Length(m_candidates) = 0) or
                (Trim(m_candidates[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
                input_syllable_count)) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                note_debug_helper_elapsed('tlvrs', phase_start_tick);
            end;
            if top_complete_chain_applied and
                (Length(top_complete_chain_candidates) > 1) then
            begin
                m_candidates := merge_candidate_lists(top_complete_chain_candidates,
                    m_candidates, 0);
                for i := 0 to High(top_complete_chain_encoded_paths) do
                begin
                    remember_segment_path_for_candidate(
                        top_complete_chain_candidates[i].text,
                        top_complete_chain_candidates[i].comment,
                        top_complete_chain_encoded_paths[i],
                        top_complete_chain_candidates[i].score);
                end;
            end;
            if is_full_pinyin_key(lookup_text) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                if (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(m_candidates[0].text)) = input_syllable_count) and
                    (get_segment_path_for_candidate(m_candidates[0]) <> '') and
                    ((input_syllable_count >= 8) or
                    (not is_problematic_single_char_chain_candidate_for_query(
                    lookup_text, m_candidates[0]))) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info + ' tlfixedskip=1';
                    end;
                end
                else
                begin
                    ensure_best_fixed_boundary_exact_chain_candidate_visible(
                        m_candidates);
                    ensure_simple_fixed_boundary_chain_candidate_visible(
                        m_candidates);
                end;
                note_debug_helper_elapsed('tlfixed', phase_start_tick);
            end;
            if (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) then
            begin
                stable_prefix_candidate := m_candidates[0];
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightchain=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate,
                not ((input_syllable_count <= 6) and
                (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count)));
            Exit;
            end;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 4) and (input_syllable_count <= 8) and
            is_full_pinyin_key(lookup_text) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not (m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []))) and
            (not has_strong_full_query_exact_complete_candidate_local) and
            (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            get_cached_stable_prefix_candidate_local(stable_prefix_candidate) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                filter_single_char_partial_candidates_for_current_query(
                    exact_head_partial_candidates);
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            ensure_fast_modal_tail_candidate_visible_local(m_candidates);
            if (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) then
            begin
                stable_prefix_candidate := m_candidates[0];
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d stableprefixearly=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count <= 6) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if not try_build_fragment_prefix_partial_candidate_local(
                fragment_prefix_candidate) then
            begin
                note_debug_helper_elapsed('tfrag', phase_start_tick);
            end
            else
            begin
                note_debug_helper_elapsed('tfrag', phase_start_tick);
                SetLength(m_candidates, 1);
                m_candidates[0] := fragment_prefix_candidate;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
                m_candidates[0] := fragment_prefix_candidate;
                ensure_fast_modal_tail_candidate_visible_local(m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 5) then
                begin
                    finalize_modal_tail_sentence_candidate(m_candidates);
                    finalize_le_plus_modal_particle_sentence_candidate(
                        m_candidates);
                    if input_syllable_count >= 6 then
                    begin
                        ensure_best_exact_chunk_chain_complete_candidate_visible(
                            m_candidates);
                    end;
                    if input_syllable_count >= 10 then
                    begin
                        ensure_strict_oracle_complete_candidate_visible(
                            m_candidates);
                    end;
                    ensure_best_oracle_backed_exact_complete_candidate_visible(
                        m_candidates);
                    prepare_visible_complete_candidates_for_fast_exit_local(
                        m_candidates);
                    if (Length(m_candidates) > 0) and
                        (Trim(m_candidates[0].comment) = '') and
                        (get_candidate_text_unit_count(
                        Trim(m_candidates[0].text)) = input_syllable_count) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                ' ffrag=[defer-complete]';
                        end;
                        raw_candidates := merge_candidate_lists(raw_candidates,
                            m_candidates, 0);
                        has_raw_candidates := Length(raw_candidates) > 0;
                        raw_from_dictionary := False;
                    end
                    else
                    begin
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d fragmentprefix=1 fast=1 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]));
                        ensure_fast_modal_tail_candidate_visible_local(m_candidates);
                        refresh_candidate_segment_paths;
                        note_ranked_top_candidate;
                        refresh_debug_top_candidate_summary_local;
                        Exit;
                    end;
                end;
                if (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) <> '') then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d fragmentprefix=1 fast=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    ensure_fast_modal_tail_candidate_visible_local(m_candidates);
                    refresh_candidate_segment_paths;
                    note_ranked_top_candidate;
                    refresh_debug_top_candidate_summary_local;
                    Exit;
                end;
            end;
        end;
        // Exact-chunk and lightweight DPs supersede this older chain on
        // very long input; running all three only repeats the same search.
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not has_full_query_exact_complete_candidate_local) and
            (input_syllable_count <= 6) and
            (not is_full_pinyin_key(lookup_text)) and
            (((input_syllable_count >= 3) and
            get_cached_stable_prefix_candidate_local(
            stable_prefix_candidate) or
            try_build_exact_two_syllable_head_partial_candidate_local(
            head_partial_candidate))) then
        begin
            if not ((Trim(head_partial_candidate.text) <> '') and
                (Trim(head_partial_candidate.comment) <> '')) then
            begin
                try_build_exact_two_syllable_head_partial_candidate_local(
                    head_partial_candidate);
            end;

            SetLength(m_candidates, 0);
            if has_stable_prefix_candidate then
            begin
                SetLength(m_candidates, Length(m_candidates) + 1);
                m_candidates[High(m_candidates)] := stable_prefix_candidate;
            end;
            if (Trim(head_partial_candidate.text) <> '') and
                (Trim(head_partial_candidate.comment) <> '') then
            begin
                SetLength(m_candidates, Length(m_candidates) + 1);
                m_candidates[High(m_candidates)] := head_partial_candidate;
            end;
            if build_exact_head_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            if has_stable_prefix_candidate then
            begin
                m_candidates[0] := stable_prefix_candidate;
            end
            else
            begin
                m_candidates[0] := head_partial_candidate;
            end;
            if try_finalize_fast_prefix_candidates_and_prepare_exit_local(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d headpartialfast=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)])) then
            begin
                Exit;
            end;
            raw_candidates := merge_candidate_lists(raw_candidates,
                m_candidates, 0);
            has_raw_candidates := Length(raw_candidates) > 0;
            raw_from_dictionary := False;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count >= 5) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            is_full_pinyin_key(lookup_text) and
            try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) and
            (not ranked_path_requires_exact_competition_validation_local(
            stable_prefix_candidate.text, direct_chain_encoded_path)) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            remember_segment_path_for_candidate(stable_prefix_candidate.text,
                stable_prefix_candidate.comment, direct_chain_encoded_path,
                stable_prefix_candidate.score);
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            if input_syllable_count >= 7 then
            begin
                trim_candidates_for_delayed_long_decode(m_candidates);
                m_candidates[0] := stable_prefix_candidate;
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankedprefixearly=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if (m_dictionary <> nil) and has_multi_syllable_input and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (input_syllable_count < 20) and
            (not should_defer_exact_chain_for_extendable_tail_local) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if try_build_exact_span_chain_candidate_local(
                direct_chain_candidate, direct_chain_encoded_path) then
            begin
                note_debug_helper_elapsed('tdchain', phase_start_tick);
                SetLength(m_candidates, 1);
                m_candidates[0] := direct_chain_candidate;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                ensure_bounded_single_char_partial_pool(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                if input_syllable_count <= 6 then
                begin
                    m_candidates[0] := direct_chain_candidate;
                    remember_segment_path_for_candidate(
                        direct_chain_candidate.text,
                        direct_chain_candidate.comment,
                        direct_chain_encoded_path,
                        direct_chain_candidate.dict_weight);
                    if ((input_syllable_count > 6) or
                        query_has_modal_tail_suffix_local(lookup_text)) and
                        (not (direct_chain_candidate.has_dict_weight and
                        (direct_chain_candidate.dict_weight >= 50000) and
                        (Trim(direct_chain_candidate.comment) = '') and
                        (get_candidate_text_unit_count(
                        Trim(direct_chain_candidate.text)) =
                        input_syllable_count))) then
                    begin
                        ensure_best_short_exact_prefix_tail_candidate_visible(
                            m_candidates);
                    end;
                    finalize_modal_tail_sentence_candidate(m_candidates);
                    finalize_le_plus_modal_particle_sentence_candidate(
                        m_candidates);
                    if is_full_pinyin_key(lookup_text) and
                        (input_syllable_count >= 3) and
                        has_precise_extendable_tail_syllable_local(lookup_text) then
                    begin
                        if build_exact_head_partial_candidates(lookup_text,
                            exact_head_partial_candidates) then
                        begin
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                        end;
                        if build_lookup_leading_prefix_partial_candidates(
                            lookup_text, exact_head_partial_candidates) then
                        begin
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                        end;
                        sort_candidates_lightweight(m_candidates);
                        ensure_partial_fallback_visible(m_candidates,
                            get_candidate_limit);
                        ensure_single_char_partial_visible(m_candidates,
                            get_candidate_limit, single_char_partial_min_count);
                        ensure_hard_single_char_partial_visible(m_candidates);
                        ensure_multi_char_partial_precedes_single_char_partial(
                            m_candidates);
                        ensure_rich_head_single_char_partials_visible(
                            m_candidates);
                        ensure_best_natural_prefix_candidate_visible_local(
                            m_candidates);
                        if prepare_prefix_partial_fast_exit_local(m_candidates,
                            Max(2, input_syllable_count - 2)) and
                            top_candidate_is_stable_non_modal_partial_local(
                            m_candidates) then
                        begin
                            finalize_prefix_partial_candidates_and_prepare_exit(
                                Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainprefixfast=1 allinit=%d',
                                [Ord(has_multi_syllable_input),
                                Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial),
                                Ord(head_only_multi_syllable),
                                Ord(runtime_phrase_added),
                                Ord(runtime_redup_added),
                                Ord(all_initial_compact_query)]));
                            Exit;
                        end;
                    end;
                    if (Trim(direct_chain_candidate.comment) = '') and
                        (get_candidate_text_unit_count(
                        Trim(direct_chain_candidate.text)) =
                        input_syllable_count) and
                        (get_encoded_path_segment_count_local(
                        direct_chain_encoded_path) > 1) then
                    begin
                        promote_candidate_to_front_local(m_candidates,
                            direct_chain_candidate);
                        filter_self_composed_complete_candidates_when_normal_complete_exists;
                        if is_full_pinyin_key(lookup_text) and
                            (input_syllable_count >= 3) then
                        begin
                            ensure_incomplete_tail_partial_precedes_completed_extension(
                                m_candidates);
                            ensure_best_natural_prefix_candidate_visible_local(
                                m_candidates);
                            if has_precise_extendable_tail_syllable_local(
                                lookup_text) and (Length(m_candidates) > 1) and
                                (m_dictionary <> nil) and
                                (not m_dictionary.is_base_entry(lookup_text,
                                Trim(direct_chain_candidate.text))) and
                                (Trim(m_candidates[1].comment) <> '') and
                                (get_candidate_text_unit_count(
                                Trim(m_candidates[1].text)) >=
                                Max(2, input_syllable_count - 1)) and
                                (get_effective_compact_pinyin_unit_count(
                                normalize_pinyin_text(
                                Trim(m_candidates[1].comment))) <= 1) then
                            begin
                                m_candidates[0] := m_candidates[1];
                                trim_sorted_candidates_for_strong_long_sentence(
                                    m_candidates);
                                finalize_preserved_candidates_and_prepare_exit(
                                    Format(
                                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainprefixprefer=1 allinit=%d',
                                    [Ord(has_multi_syllable_input),
                                    Ord(has_segment_candidates),
                                    Ord(has_internal_dangling_initial),
                                    Ord(head_only_multi_syllable),
                                    Ord(runtime_phrase_added),
                                    Ord(runtime_redup_added),
                                    Ord(all_initial_compact_query)]),
                                    m_candidates[1], False);
                                Exit;
                            end;
                            if has_precise_extendable_tail_syllable_local(
                                lookup_text) and (Length(m_candidates) > 0) and
                                (Trim(m_candidates[0].comment) <> '') and
                                (get_candidate_text_unit_count(
                                Trim(m_candidates[0].text)) >=
                                Max(2, input_syllable_count - 1)) and
                                (get_effective_compact_pinyin_unit_count(
                                normalize_pinyin_text(
                                Trim(m_candidates[0].comment))) <= 1) then
                            begin
                                finalize_preserved_candidates_and_prepare_exit(
                                    Format(
                                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainpartial=1 allinit=%d',
                                    [Ord(has_multi_syllable_input),
                                    Ord(has_segment_candidates),
                                    Ord(has_internal_dangling_initial),
                                    Ord(head_only_multi_syllable),
                                    Ord(runtime_phrase_added),
                                    Ord(runtime_redup_added),
                                    Ord(all_initial_compact_query)]),
                                    m_candidates[0], False);
                                Exit;
                            end;
                            if (Length(m_candidates) > 0) and
                                (Trim(m_candidates[0].comment) = '') then
                            begin
                                promote_candidate_to_front_local(m_candidates,
                                    direct_chain_candidate);
                            end;
                        end;
                        trim_sorted_candidates_for_strong_long_sentence(
                            m_candidates);
                        if (Length(m_candidates) > 0) and
                            (Trim(m_candidates[0].comment) <> '') and
                            (get_candidate_text_unit_count(
                            Trim(m_candidates[0].text)) >=
                            Max(2, input_syllable_count - 1)) and
                            (get_effective_compact_pinyin_unit_count(
                            normalize_pinyin_text(
                            Trim(m_candidates[0].comment))) <= 1) then
                        begin
                            finalize_preserved_candidates_and_prepare_exit(
                                Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainpartialtrim=1 allinit=%d',
                                [Ord(has_multi_syllable_input),
                                Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial),
                                Ord(head_only_multi_syllable),
                                Ord(runtime_phrase_added),
                                Ord(runtime_redup_added),
                                Ord(all_initial_compact_query)]),
                                m_candidates[0], False);
                            Exit;
                        end;
                        promote_candidate_to_front_local(m_candidates,
                            direct_chain_candidate);
                        finalize_preserved_candidates_and_prepare_exit(
                            Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchainfast=1 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]),
                            direct_chain_candidate, False);
                        Exit;
                    end;
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d exactchain=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;

                has_long_direct_chain_candidate := True;
            end;
            note_debug_helper_elapsed('tdchain', phase_start_tick);
        end;
        if has_incremental_composition_local and
            (m_dictionary <> nil) and has_multi_syllable_input and
            (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            is_full_pinyin_key(lookup_text) and
            try_build_ranked_path_prefix_continuation_candidate_local(
            stable_prefix_candidate, direct_chain_encoded_path) then
        begin
            SetLength(m_candidates, 1);
            m_candidates[0] := stable_prefix_candidate;
            remember_segment_path_for_candidate(stable_prefix_candidate.text,
                stable_prefix_candidate.comment, direct_chain_encoded_path,
                stable_prefix_candidate.score);
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            m_candidates[0] := stable_prefix_candidate;
            if input_syllable_count >= 7 then
            begin
                trim_candidates_for_delayed_long_decode(m_candidates);
                m_candidates[0] := stable_prefix_candidate;
            end;
            finalize_preserved_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rankedprefixfast=1 allinit=%d',
                [Ord(has_multi_syllable_input),
                Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]), stable_prefix_candidate, False);
            Exit;
        end;
        if m_dictionary <> nil then
        begin
            if has_multi_syllable_input and (input_syllable_count >= 5) and
                (input_syllable_count <= 6) and (not all_initial_compact_query) and
                (not has_internal_dangling_initial) and
                (not query_has_modal_tail_suffix_local(lookup_text)) and
                (not has_preferred_tail_phrase_local(2)) and
                build_lookup_leading_prefix_partial_candidates(lookup_text,
                raw_candidates) then
            begin
                has_raw_candidates := True;
                raw_from_dictionary := True;
                m_candidates := raw_candidates;
                if get_cached_stable_prefix_candidate_local(
                    stable_prefix_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := stable_prefix_candidate;
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                sort_candidates_lightweight(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                ensure_rich_head_single_char_partials_visible(m_candidates);
                ensure_best_natural_prefix_candidate_visible_local(m_candidates);
                if prepare_prefix_partial_fast_exit_local(m_candidates, 2) then
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortprefixseed=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end
            else if has_multi_syllable_input and (input_syllable_count >= 7) and
                is_full_pinyin_key(lookup_text) and
                (not all_initial_compact_query) and
                (not has_internal_dangling_initial) and
                (not query_has_modal_tail_suffix_local(lookup_text)) and
                (has_short_extendable_tail_prefix_local or
                has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count)) and
                (not has_preferred_tail_phrase_local(2)) and
                (not has_complete_exact_tail_phrase_local(2)) then
            begin
                if get_cached_stable_prefix_candidate_local(
                    stable_prefix_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := stable_prefix_candidate;
                    raw_candidates := merge_candidate_lists(raw_candidates,
                        exact_head_partial_candidates, 0);
                    has_raw_candidates := True;
                end;

                if query_has_preferred_subspan_coverage_for_light_path_local(True) and
                    try_build_direct_preferred_subspan_sentence_candidate_local(
                    direct_preferred_candidate) then
                begin
                    has_long_direct_preferred_candidate := True;
                end;

                if get_cached_stable_prefix_candidate_local(
                    stable_prefix_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := stable_prefix_candidate;
                    raw_candidates := merge_candidate_lists(raw_candidates,
                        exact_head_partial_candidates, 0);
                    has_raw_candidates := True;
                end;

                SetLength(raw_candidates, 0);
                if build_long_exact_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    raw_candidates := merge_candidate_lists(raw_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    raw_candidates := merge_candidate_lists(raw_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if Length(raw_candidates) > 0 then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                    m_candidates := raw_candidates;
                    if get_cached_stable_prefix_candidate_local(
                        stable_prefix_candidate) then
                    begin
                        if (input_syllable_count < 9) and
                            (not query_has_modal_tail_suffix_local(lookup_text)) and
                            (has_short_extendable_tail_prefix_local or
                            has_extendable_same_syllable_tail_local(lookup_text,
                            input_syllable_count)) then
                        begin
                            SetLength(m_candidates, 1);
                            m_candidates[0] := stable_prefix_candidate;
                            ensure_bounded_single_char_partial_pool(m_candidates);
                            ensure_partial_fallback_visible(m_candidates,
                                get_candidate_limit);
                            ensure_single_char_partial_visible(m_candidates,
                                get_candidate_limit, single_char_partial_min_count);
                            ensure_hard_single_char_partial_visible(m_candidates);
                            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                            ensure_rich_head_single_char_partials_visible(m_candidates);
                            m_candidates[0] := stable_prefix_candidate;
                            if m_config.debug_mode then
                            begin
                                m_last_full_path_debug_info :=
                                    m_last_full_path_debug_info + Format(
                                    ' stable=[%s/%s]',
                                    [stable_prefix_candidate.text,
                                    stable_prefix_candidate.comment]);
                            end;
                            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortstableprefix=1 allinit=%d',
                                [Ord(has_multi_syllable_input),
                                Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial),
                                Ord(head_only_multi_syllable),
                                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                                Ord(all_initial_compact_query)]));
                            Exit;
                        end;
                        SetLength(exact_head_partial_candidates, 1);
                        exact_head_partial_candidates[0] := stable_prefix_candidate;
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    sort_candidates_lightweight(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                    ensure_best_natural_prefix_candidate_visible_local(m_candidates);
                    if prepare_prefix_partial_fast_exit_local(m_candidates, 2) then
                    begin
                        finalize_prefix_partial_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d longprefixseed=1 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
            end
            else if head_only_multi_syllable then
            begin
                if all_initial_compact_query and (input_syllable_count >= 5) then
                begin
                    if dictionary_lookup_cached(lookup_text, raw_candidates) then
                    begin
                        has_raw_candidates := True;
                        raw_from_dictionary := True;
                    end;
                end
                else if is_short_mixed_head_only_fast_anchor_query and
                    dictionary_lookup_cached(lookup_text, raw_candidates) then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                end;

                if (not (is_short_mixed_head_only_fast_anchor_query and
                    has_raw_candidates)) and
                    (not (all_initial_compact_query and
                    (input_syllable_count >= 5))) and
                    (not (has_raw_candidates and
                    has_short_exact_complete_anchor(raw_candidates))) and
                    build_segment_candidates_timed(segment_candidates, False) then
                begin
                    has_raw_candidates := True;
                    has_segment_candidates := True;
                    if Length(raw_candidates) > 0 then
                    begin
                        raw_candidates := merge_candidate_lists(raw_candidates,
                            segment_candidates, 0);
                    end
                    else
                    begin
                        raw_candidates := segment_candidates;
                    end;
                end;

                if has_relaxed_missing_apostrophe_partial and
                    has_relaxed_missing_apostrophe_boundary_shift and
                    build_segment_candidates_timed(relaxed_segment_candidates, False, True) then
                begin
                    has_raw_candidates := True;
                    has_segment_candidates := True;
                    if Length(raw_candidates) > 0 then
                    begin
                        raw_candidates := merge_candidate_lists(raw_candidates, relaxed_segment_candidates, 0);
                    end
                    else
                    begin
                        raw_candidates := relaxed_segment_candidates;
                    end;
                end;

                if has_raw_candidates then
                begin
                    merge_head_only_full_lookup_candidates(raw_candidates, lookup_text);
                end;
            end
            else if has_multi_syllable_input and
                merge_incomplete_trailing_prefix_full_lookup_candidates(raw_candidates,
                    lookup_text) then
            begin
                has_raw_candidates := True;
                raw_from_dictionary := True;
                trailing_prefix_rescue_applied := True;
            end
            else if has_explicit_apostrophe_input and
                has_multi_syllable_input and
                (explicit_apostrophe_lookup_text <> '') and
                dictionary_lookup_cached(explicit_apostrophe_lookup_text,
                raw_candidates) then
            begin
                filter_complete_candidates_for_explicit_apostrophe_boundary(
                    raw_candidates, raw_candidates);
                filter_short_dictionary_hits_for_explicit_apostrophe(
                    raw_candidates, input_syllable_count);
                if Length(raw_candidates) > 0 then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                end
                else
                begin
                    SetLength(raw_candidates, 0);
                end;
            end
            else if short_full_exact_query and
                dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
            begin
                has_raw_candidates := True;
                raw_from_dictionary := True;
            end
            else if long_full_exact_query and
                dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
            begin
                has_raw_candidates := True;
                raw_from_dictionary := True;
                lightweight_sentence_applied := True;
                full_query_exact_promoted := True;
            end
            else if delayed_long_decode_mode then
            begin
                // In predictive long-sentence mode, do not seed from the generic
                // full-query dictionary bucket. Let lightweight prefix prediction
                // and narrow exact/prefix probes drive the first candidate set.
            end
            else if dictionary_lookup_cached(lookup_text, raw_candidates) then
            begin
                if has_explicit_apostrophe_input and
                    has_multi_syllable_input then
                begin
                    filter_complete_candidates_for_explicit_apostrophe_boundary(
                        raw_candidates, raw_candidates);
                    filter_short_dictionary_hits_for_explicit_apostrophe(
                        raw_candidates, input_syllable_count);
                end;
                if Length(raw_candidates) > 0 then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                end;
            end
            else
            begin
                if (m_last_lookup_normalized_from = '') and
                    has_relaxed_missing_apostrophe_partial and
                    ((not has_multi_syllable_input) or
                    has_relaxed_missing_apostrophe_boundary_shift or
                    ((input_syllable_count <= 4) and
                    (get_input_syllable_count_for_text(m_composition_text, True) >
                    input_syllable_count))) then
                begin
                    fallback_comment := relaxed_missing_apostrophe_comment;
                    has_multi_syllable_input := True;
                    allow_relaxed_missing_apostrophe := True;
                    input_syllable_count := get_input_syllable_count_for_text(m_composition_text, True);
                    if input_syllable_count <= 1 then
                    begin
                        input_syllable_count := get_input_syllable_count_for_text(lookup_text, True);
                    end;
                    if input_syllable_count <= 1 then
                    begin
                        input_syllable_count := 2;
                    end;
                    m_last_lookup_syllable_count := input_syllable_count;
                end;

                SetLength(raw_candidates, 0);
                if has_multi_syllable_input and
                    merge_incomplete_trailing_prefix_full_lookup_candidates(raw_candidates,
                        lookup_text) then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                    trailing_prefix_rescue_applied := True;
                end
                else if has_explicit_apostrophe_input and
                    has_multi_syllable_input and
                    (explicit_apostrophe_lookup_text <> '') and
                    dictionary_lookup_cached(explicit_apostrophe_lookup_text,
                    raw_candidates) then
                begin
                    filter_complete_candidates_for_explicit_apostrophe_boundary(
                        raw_candidates, raw_candidates);
                    filter_short_dictionary_hits_for_explicit_apostrophe(
                        raw_candidates, input_syllable_count);
                    if Length(raw_candidates) > 0 then
                    begin
                        has_raw_candidates := True;
                        raw_from_dictionary := True;
                    end
                    else
                    begin
                        SetLength(raw_candidates, 0);
                    end;
                end
                else if short_full_exact_query and
                    dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                end
                else if long_full_exact_query and
                    dictionary_exact_lookup_cached(lookup_text, raw_candidates) then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                    lightweight_sentence_applied := True;
                    full_query_exact_promoted := True;
                end
                else if delayed_long_decode_mode then
                begin
                    // Predictive long-sentence mode keeps the generic full-query
                    // dictionary bucket out of the primary path. Heavy decode can
                    // still run later if lightweight candidates are not strong enough.
                end
                else if has_explicit_apostrophe_input and
                    has_multi_syllable_input and
                    (explicit_apostrophe_lookup_text <> '') and
                    dictionary_lookup_cached(explicit_apostrophe_lookup_text,
                    raw_candidates) then
                begin
                    filter_complete_candidates_for_explicit_apostrophe_boundary(
                        raw_candidates, raw_candidates);
                    filter_short_dictionary_hits_for_explicit_apostrophe(
                        raw_candidates, input_syllable_count);
                    if Length(raw_candidates) > 0 then
                    begin
                        has_raw_candidates := True;
                        raw_from_dictionary := True;
                    end
                    else
                    begin
                        SetLength(raw_candidates, 0);
                    end;
                end
                else if has_multi_syllable_input and
                    (input_syllable_count >= 3) and
                    (input_syllable_count <= 4) and
                    (has_safe_trailing_initial_typing_state or
                    has_short_extendable_tail_prefix_local or
                    has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count)) and
                    (build_exact_leading_prefix_partial_candidates(lookup_text,
                    raw_candidates) or
                    build_raw_leading_prefix_partial_candidates(lookup_text,
                    raw_candidates)) then
                begin
                    has_raw_candidates := True;
                    raw_from_dictionary := True;
                    trailing_prefix_rescue_applied := True;
                end
                else if has_multi_syllable_input and
                    should_enable_long_sentence_exact_search_local and
                    ((input_syllable_count >= 5) or
                    (short_full_exact_query and
                    (input_syllable_count >= 4))) then
                begin
                    // For longer in-progress sentences, let the exact-path composer
                    // drive prediction instead of paying an expensive segment build
                    // before we even have a stable dictionary seed.
                end
                else if m_config.enable_segment_candidates and
                    build_segment_candidates_timed(segment_candidates,
                        (not all_initial_compact_query) and
                        ((not c_suppress_nonlexicon_complete_long_candidates) or
                        should_enable_long_sentence_full_path_search_local),
                        allow_relaxed_missing_apostrophe) then
                begin
                    has_raw_candidates := True;
                    has_segment_candidates := True;
                    raw_candidates := segment_candidates;
                end;
            end;
        end;

        if has_multi_syllable_input and (input_syllable_count <= 4) and
            (not (has_raw_candidates and
            has_short_exact_complete_anchor(raw_candidates))) and
            build_short_pinyin_prefix_completion_candidates(lookup_text,
            exact_head_partial_candidates) then
        begin
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, raw_candidates, 0);
                remove_partials_shadowed_by_complete_text_local(raw_candidates);
            end
            else
            begin
                raw_candidates := exact_head_partial_candidates;
                has_raw_candidates := True;
            end;
            raw_from_dictionary := True;
            trailing_prefix_rescue_applied := True;
        end;

        if has_raw_candidates then
        begin
            filter_overlong_partial_candidates_for_current_query(raw_candidates);
            if Length(raw_candidates) = 0 then
            begin
                has_raw_candidates := False;
                raw_from_dictionary := False;
                trailing_prefix_rescue_applied := False;
            end;
        end;
        if short_full_exact_query and
            (not (has_raw_candidates and raw_from_dictionary and
            has_short_exact_complete_anchor(raw_candidates))) and
            seed_short_full_exact_resolved_candidate_local(raw_candidates) then
        begin
            has_raw_candidates := True;
            raw_from_dictionary := True;
        end;
        if has_raw_candidates then
        begin
            filter_overlong_partial_candidates_for_current_query(raw_candidates);
            if Length(raw_candidates) = 0 then
            begin
                has_raw_candidates := False;
                raw_from_dictionary := False;
                trailing_prefix_rescue_applied := False;
            end;
        end;

        if all_initial_compact_query and (input_syllable_count >= 5) and
            has_raw_candidates then
        begin
            m_candidates := raw_candidates;
            sort_candidates_lightweight(m_candidates);
            finalize_all_initial_compact_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d allinitfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;

        if short_full_exact_query and has_raw_candidates and
            has_multi_syllable_input and (input_syllable_count >= 3) and
            (input_syllable_count <= 6) and
            (not query_has_modal_tail_suffix_local(lookup_text)) and
            (has_safe_trailing_initial_typing_state or
            has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) then
        begin
            m_candidates := raw_candidates;
            sort_candidates_lightweight(m_candidates);
            filter_overlong_partial_candidates_for_current_query(m_candidates);
            ensure_fast_prefix_single_char_partials_visible(m_candidates, 0);
            if top_candidate_is_stable_non_modal_partial_local(m_candidates) then
            begin
                if try_finalize_fast_prefix_candidates_and_prepare_exit_local(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d rawprefixpartialfast=1 allinit=%d',
                    [Ord(has_multi_syllable_input),
                    Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)])) then
                begin
                    Exit;
                end;
            end;
        end;

        if trailing_prefix_rescue_applied and has_raw_candidates and
            has_reliable_complete_phrase_candidate_for_syllable_count(raw_candidates,
            input_syllable_count) then
        begin
            m_candidates := raw_candidates;
            sort_candidates_lightweight(m_candidates);
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d trailingprefix=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;

        if short_full_exact_query and (input_syllable_count >= 4) and
            (not has_raw_candidates) then
        begin
            if build_exact_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) or
                build_raw_leading_prefix_partial_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                raw_candidates := exact_head_partial_candidates;
                has_raw_candidates := True;
                raw_from_dictionary := True;
            end;
        end;

        if try_build_compact_interrogative_location_candidate(compact_runtime_candidate) then
        begin
            SetLength(compact_runtime_candidates, 1);
            compact_runtime_candidates[0] := compact_runtime_candidate;
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(raw_candidates, compact_runtime_candidates, 0);
            end
            else
            begin
                raw_candidates := compact_runtime_candidates;
                has_raw_candidates := True;
                raw_candidates_seeded_from_runtime_only := True;
            end;
            runtime_phrase_added := True;
            m_runtime_common_pattern_text := compact_runtime_candidate.text;
        end;

        if has_raw_candidates and (not has_multi_syllable_input) then
        begin
            m_candidates := raw_candidates;
            phase_start_tick := nc_monotonic_tick_ms;
            filter_non_bmp_single_char_candidates(m_candidates);
            Inc(post_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            phase_start_tick := nc_monotonic_tick_ms;
            apply_user_penalties(lookup_text, m_candidates);
            Inc(post_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            phase_start_tick := nc_monotonic_tick_ms;
            sort_candidates_lightweight(m_candidates);
            apply_selected_prefix_single_context_lm_local(m_candidates);
            Inc(sort_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            limit := get_total_candidate_limit;
            if limit > c_candidate_total_limit_max then
            begin
                limit := c_candidate_total_limit_max;
            end;
            if (limit > 0) and (Length(m_candidates) > limit) then
            begin
                SetLength(m_candidates, limit);
            end;
            if (Length(m_candidates) > 0) and
                is_short_repeated_initial_compact_query_text_local(lookup_text) then
            begin
                finalize_preserved_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d repeatedinitraw=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]), m_candidates[0], False);
                if Length(m_candidates) > 0 then
                begin
                    m_has_forced_visible_top_candidate := True;
                    m_forced_visible_top_from_incremental_lm_prefix := False;
                    m_forced_visible_top_candidate := m_candidates[0];
                    m_forced_visible_top_composition_text := m_composition_text;
                    m_forced_visible_top_lookup_key := m_last_lookup_key;
                end;
                Exit;
            end;
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d singlefast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;

        if has_multi_syllable_input and has_internal_dangling_initial and
            (input_syllable_count >= c_long_sentence_exact_min_syllables) and
            build_dangling_exact_head_chain_sentence_candidates(lookup_text,
            exact_head_partial_candidates) then
        begin
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(exact_head_partial_candidates,
                    raw_candidates, 0);
            end
            else
            begin
                raw_candidates := exact_head_partial_candidates;
                has_raw_candidates := True;
            end;
            raw_from_dictionary := True;
        end;

        if normalized_typo_long_query_fast_path and has_raw_candidates then
        begin
            // Adjacent-swap typo normalization (e.g. "chagn" -> "chang") already
            // gives us a good exact lookup seed. Re-entering the heavy long-sentence
            // post chain here mostly produces lag and noisy alternates.
            lightweight_sentence_applied := True;
        end;

        if has_raw_candidates and raw_from_dictionary and
            has_short_exact_complete_anchor(raw_candidates) then
        begin
            if has_explicit_apostrophe_input and (input_syllable_count > 1) then
            begin
                filter_complete_candidates_for_explicit_apostrophe_boundary(
                    raw_candidates, raw_candidates);
                filter_short_dictionary_hits_for_explicit_apostrophe(
                    raw_candidates, input_syllable_count);
            end;
            if m_dictionary <> nil then
            begin
                latest_query_choice_text := get_cached_query_latest_choice_text(
                    normalize_pinyin_text(lookup_text));
            end;

            phase_start_tick := nc_monotonic_tick_ms;
            apply_preferred_query_phrase_bonus_for_query(lookup_text, raw_candidates);
            apply_preferred_sentence_phrase_bonus(raw_candidates);
            note_debug_helper_elapsed('trawbonus', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if short_full_exact_query and (input_syllable_count = 2) then
            begin
                // Exact dictionary buckets are already ranked. Avoid a full
                // lightweight rerank on every two-syllable keystroke; learned
                // choices are promoted in the short finalization path below.
            end
            else if is_short_mixed_head_only_fast_anchor_query then
            begin
                sort_candidates_lightweight(raw_candidates);
            end
            else
            begin
                sort_candidates_lightweight(raw_candidates);
            end;
            filter_non_bmp_single_char_candidates(raw_candidates);
            note_debug_helper_elapsed('trawsort', phase_start_tick);

            phase_start_tick := nc_monotonic_tick_ms;
            limit := get_total_candidate_limit;
            multi_syllable_cap_limit := get_candidate_limit * 3;
            if count_single_char_partial_candidates(raw_candidates) +
                get_candidate_limit > multi_syllable_cap_limit then
            begin
                multi_syllable_cap_limit :=
                    count_single_char_partial_candidates(raw_candidates) +
                    get_candidate_limit;
            end;
            if multi_syllable_cap_limit > c_candidate_total_limit_max then
            begin
                multi_syllable_cap_limit := c_candidate_total_limit_max;
            end;
            if limit > multi_syllable_cap_limit then
            begin
                limit := multi_syllable_cap_limit;
            end;
            if limit > c_candidate_total_limit_max then
            begin
                limit := c_candidate_total_limit_max;
            end;
            note_debug_helper_elapsed('trawlimit', phase_start_tick);

            if Length(raw_candidates) > limit then
            begin
                SetLength(m_candidates, limit);
                for i := 0 to limit - 1 do
                begin
                    m_candidates[i] := raw_candidates[i];
                end;
                raw_candidates := m_candidates;
            end
            else
            begin
                m_candidates := raw_candidates;
            end;

            if has_relaxed_missing_apostrophe_partial and
                has_relaxed_missing_apostrophe_boundary_shift and
                build_segment_candidates_timed(relaxed_segment_candidates, False, True) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    relaxed_segment_candidates, 0);
            end;

            phase_start_tick := nc_monotonic_tick_ms;
            merge_confirmed_prefix_user_extensions(m_candidates);
            for i := 0 to High(m_candidates) do
            begin
                if Trim(m_candidates[i].comment) <> '' then
                begin
                    Continue;
                end;
                if is_latest_session_query_choice(m_candidates[i].text) then
                begin
                    Inc(m_candidates[i].score, 820);
                end;
                if (latest_query_choice_text <> '') and
                    SameText(Trim(m_candidates[i].text), latest_query_choice_text) then
                begin
                    Inc(m_candidates[i].score, 760);
                end;
                if m_dictionary <> nil then
                begin
                    Inc(m_candidates[i].score,
                        m_dictionary.get_query_choice_bonus(
                        normalize_pinyin_text(lookup_text), m_candidates[i].text));
                end;
            end;
            note_debug_helper_elapsed('tshortchoice', phase_start_tick);

            phase_start_tick := nc_monotonic_tick_ms;
            ensure_bounded_single_char_partial_pool(m_candidates);
            prioritize_complete_phrase_matches(m_candidates);
            apply_user_penalties(lookup_text, m_candidates);
            apply_shifted_boundary_longer_exact_penalty(m_candidates);
            apply_compact_long_phrase_bonus(m_candidates);
            apply_preferred_query_phrase_bonus_for_query(lookup_text, m_candidates);
            apply_preferred_sentence_phrase_bonus(m_candidates);
            note_debug_helper_elapsed('tshortscore', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if build_lexicon_longer_prefix_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            note_debug_helper_elapsed('tshortlexprefix', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if short_full_exact_query and (input_syllable_count = 2) then
            begin
                // Keep exact bucket order for the second-stage candidate list too.
            end
            else if is_short_mixed_head_only_fast_anchor_query then
            begin
                sort_candidates_lightweight(m_candidates);
            end
            else
            begin
                sort_candidates_lightweight(m_candidates);
            end;
            note_debug_helper_elapsed('tshortsort2', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates, get_candidate_limit,
                single_char_partial_min_count);
            note_debug_helper_elapsed('tshortsingle', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_two_syllable_exact_phrase_precedes_matching_single_char_partial(m_candidates);
            note_debug_helper_elapsed('tshortfilter2', phase_start_tick);
            if (not head_only_multi_syllable) and is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and (input_syllable_count <= 6) and
                (not top_partial_tail_has_multi_syllable_local(m_candidates)) then
            begin
                ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortsimple=1 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            if (input_syllable_count >= 4) and
                (input_syllable_count <= 6) and
                has_weak_user_complete_phrase_candidate_for_syllable_count(
                m_candidates, input_syllable_count) then
            begin
                ensure_best_short_natural_split_candidate_visible(m_candidates);
            end;
            if (not head_only_multi_syllable) and has_multi_syllable_input and
                is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 5) and (input_syllable_count <= 6) and
                try_build_direct_preferred_subspan_sentence_candidate_local(
                direct_preferred_candidate) then
            begin
                SetLength(m_candidates, 1);
                m_candidates[0] := direct_preferred_candidate;
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                ensure_bounded_single_char_partial_pool(m_candidates);
                ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                finalize_modal_tail_sentence_candidate(m_candidates);
                finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
                finalize_direct_preferred_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortprefdirect=1 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]));
                Exit;
            end;

            if short_full_exact_query and (input_syllable_count <= 3) then
            begin
                if top_candidate_is_stable_non_modal_partial_local(m_candidates) then
                begin
                    if try_finalize_fast_prefix_candidates_and_prepare_exit_local(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartialfast=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)])) then
                    begin
                        Exit;
                    end;
                end;
                if input_syllable_count >= 3 then
                begin
                    ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
                    if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                    begin
                        has_short_exact_preserved_candidate :=
                            try_get_best_complete_exact_candidate_local(
                            m_candidates, short_exact_preserved_candidate);
                        if build_exact_head_partial_candidates(lookup_text,
                            exact_head_partial_candidates) then
                        begin
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                            sort_candidates_lightweight(m_candidates);
                            if has_short_exact_preserved_candidate then
                            begin
                                promote_candidate_to_front_local(m_candidates,
                                    short_exact_preserved_candidate);
                            end;
                            ensure_partial_fallback_visible(m_candidates,
                                get_candidate_limit);
                            ensure_single_char_partial_visible(m_candidates,
                                get_candidate_limit,
                                single_char_partial_min_count);
                        end;
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortptail=1 prefqv=0 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                ensure_exact_dict_complete_candidate_visible(m_candidates);
                if (input_syllable_count = 2) and
                    (not has_explicit_apostrophe_input) then
                begin
                    ensure_two_syllable_quantity_phrase_candidate_visible(m_candidates);
                    ensure_relaxed_missing_apostrophe_head_partial_visible(
                        m_candidates);
                    prefer_relaxed_missing_apostrophe_boundary_partial(
                        m_candidates);
                end;
                if (input_syllable_count = 2) and
                    has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 short2completefast=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                    m_candidates);
                ensure_best_user_complete_candidate_visible(m_candidates);
                ensure_best_short_natural_split_candidate_visible(m_candidates);
                promote_preferred_query_phrase_candidate_for_query(lookup_text,
                    m_candidates);
                ensure_short_full_query_exact_cluster_visible(m_candidates);
                if not head_only_multi_syllable then
                begin
                    ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
                end;
                filter_self_composed_complete_candidates_when_normal_complete_exists;
                if (input_syllable_count = 2) and
                    (not has_explicit_apostrophe_input) then
                begin
                    ensure_relaxed_missing_apostrophe_head_partial_visible(
                        m_candidates);
                    prefer_relaxed_missing_apostrophe_boundary_partial(
                        m_candidates);
                end;
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortfast=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if input_syllable_count = 2 then
                begin
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 short2partialfast=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            if short_full_exact_query and (input_syllable_count >= 4) and
                (input_syllable_count <= 6) then
            begin
                ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
                ensure_best_short_natural_split_candidate_visible(m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) and
                    (not (has_short_extendable_tail_prefix_local or
                    has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count))) and
                    ((Length(m_candidates) = 0) or
                    (m_candidates[0].source <> cs_user)) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortptail=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                begin
                    if build_exact_head_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if build_exact_leading_prefix_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if build_lookup_leading_prefix_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if get_cached_stable_prefix_candidate_local(
                        stable_prefix_candidate) then
                    begin
                        if (input_syllable_count < 9) and
                            (not query_has_modal_tail_suffix_local(lookup_text)) and
                            (has_short_extendable_tail_prefix_local or
                            has_extendable_same_syllable_tail_local(lookup_text,
                            input_syllable_count)) then
                        begin
                            SetLength(m_candidates, 1);
                            m_candidates[0] := stable_prefix_candidate;
                            ensure_bounded_single_char_partial_pool(m_candidates);
                            ensure_partial_fallback_visible(m_candidates,
                                get_candidate_limit);
                            ensure_single_char_partial_visible(m_candidates,
                                get_candidate_limit, single_char_partial_min_count);
                            ensure_hard_single_char_partial_visible(m_candidates);
                            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                            ensure_rich_head_single_char_partials_visible(m_candidates);
                            m_candidates[0] := stable_prefix_candidate;
                            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortstableprefix=1 allinit=%d',
                                [Ord(has_multi_syllable_input),
                                Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial),
                                Ord(head_only_multi_syllable),
                                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                                Ord(all_initial_compact_query)]));
                            Exit;
                        end;
                        SetLength(exact_head_partial_candidates, 1);
                        exact_head_partial_candidates[0] := stable_prefix_candidate;
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    sort_candidates_lightweight(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                    if (not query_has_modal_tail_suffix_local(lookup_text)) and
                        prepare_prefix_partial_fast_exit_local(m_candidates, 2) then
                    begin
                        finalize_prefix_partial_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortptailprefix=1 prefqv=0 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                ensure_best_short_natural_split_candidate_visible(m_candidates);
                ensure_best_fixed_boundary_exact_chain_candidate_visible(m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(m_candidates);
                ensure_best_fixed_boundary_exact_chain_candidate_visible(m_candidates);
                if not head_only_multi_syllable then
                begin
                    ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
                end;
                ensure_best_user_complete_candidate_visible(m_candidates);
                filter_self_composed_complete_candidates_when_normal_complete_exists;
                promote_preferred_query_phrase_candidate_for_query(lookup_text,
                    m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortchain=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            infer_paths_for_front_full_length_complete_candidates(m_candidates);
            ensure_best_inferred_full_length_candidate_visible(m_candidates);
            ensure_best_full_length_pathless_candidate_visible(m_candidates);
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            finalize_modal_tail_sentence_candidate(m_candidates);
            finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
            ensure_exact_dict_complete_candidate_visible(m_candidates);
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            ensure_best_user_complete_candidate_visible(m_candidates);
            promote_preferred_query_phrase_candidate_for_query(lookup_text,
                m_candidates);
            if not head_only_multi_syllable then
            begin
                ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
            end;
            filter_self_composed_complete_candidates_when_normal_complete_exists;
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            sort_candidates_lightweight(m_candidates);
            ensure_short_full_query_exact_cluster_visible(m_candidates);
            finalize_lookup_timing_info;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortfast=1',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added)]) +
                    Format(' allinit=%d', [Ord(all_initial_compact_query)]);
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
            end;
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;

        if short_full_exact_query and has_raw_candidates and raw_from_dictionary and
            (input_syllable_count >= 4) and (input_syllable_count <= 6) and
            (not (m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []))) and
            (not has_short_exact_complete_anchor(raw_candidates)) then
        begin
            m_candidates := raw_candidates;
            if build_exact_head_modal_tail_complete_candidates(lookup_text,
                exact_head_partial_candidates) then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            merge_confirmed_prefix_user_extensions(m_candidates);
            apply_user_penalties(lookup_text, m_candidates);
            apply_shifted_boundary_longer_exact_penalty(m_candidates);
            apply_compact_long_phrase_bonus(m_candidates);
            apply_preferred_query_phrase_bonus_for_query(lookup_text, m_candidates);
            apply_preferred_sentence_phrase_bonus(m_candidates);
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates, get_candidate_limit,
                single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            ensure_modal_tail_sentence_candidate_visible(m_candidates);
            finalize_modal_tail_sentence_candidate(m_candidates);
            finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
            if (not head_only_multi_syllable) and is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and (input_syllable_count <= 6) and
                (not top_partial_tail_has_multi_syllable_local(m_candidates)) then
            begin
                ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartialsimple=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            if (input_syllable_count >= 4) and
                (input_syllable_count <= 6) and
                has_weak_user_complete_phrase_candidate_for_syllable_count(
                m_candidates, input_syllable_count) then
            begin
                ensure_best_short_natural_split_candidate_visible(m_candidates);
            end;
            if input_syllable_count > 6 then
            begin
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
            end;
            if is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and
                (not top_partial_tail_has_multi_syllable_local(m_candidates)) then
            begin
                ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) and
                    (not (has_short_extendable_tail_prefix_local or
                    has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count))) and
                    ((Length(m_candidates) = 0) or
                    (m_candidates[0].source <> cs_user)) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartialptail=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                begin
                    if build_exact_head_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if build_exact_leading_prefix_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if build_lookup_leading_prefix_partial_candidates(lookup_text,
                        exact_head_partial_candidates) then
                    begin
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    if get_cached_stable_prefix_candidate_local(
                        stable_prefix_candidate) then
                    begin
                        if (input_syllable_count < 9) and
                            (not query_has_modal_tail_suffix_local(lookup_text)) and
                            (has_short_extendable_tail_prefix_local or
                            has_extendable_same_syllable_tail_local(lookup_text,
                            input_syllable_count)) then
                        begin
                            SetLength(m_candidates, 1);
                            m_candidates[0] := stable_prefix_candidate;
                            ensure_bounded_single_char_partial_pool(m_candidates);
                            ensure_partial_fallback_visible(m_candidates,
                                get_candidate_limit);
                            ensure_single_char_partial_visible(m_candidates,
                                get_candidate_limit, single_char_partial_min_count);
                            ensure_hard_single_char_partial_visible(m_candidates);
                            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                            ensure_rich_head_single_char_partials_visible(m_candidates);
                            m_candidates[0] := stable_prefix_candidate;
                            if m_config.debug_mode then
                            begin
                                m_last_full_path_debug_info :=
                                    m_last_full_path_debug_info + Format(
                                    ' stable=[%s/%s]',
                                    [stable_prefix_candidate.text,
                                    stable_prefix_candidate.comment]);
                            end;
                            finalize_prefix_partial_candidates_and_prepare_exit(Format(
                                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortstableprefix=1 allinit=%d',
                                [Ord(has_multi_syllable_input),
                                Ord(has_segment_candidates),
                                Ord(has_internal_dangling_initial),
                                Ord(head_only_multi_syllable),
                                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                                Ord(all_initial_compact_query)]));
                            Exit;
                        end;
                        SetLength(exact_head_partial_candidates, 1);
                        exact_head_partial_candidates[0] := stable_prefix_candidate;
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                    end;
                    sort_candidates_lightweight(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                    if (not query_has_modal_tail_suffix_local(lookup_text)) and
                        prepare_prefix_partial_fast_exit_local(m_candidates, 2) then
                    begin
                        finalize_prefix_partial_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartialprefix=1 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                ensure_best_fixed_boundary_exact_chain_candidate_visible(
                    m_candidates);
                ensure_best_supported_complete_candidate_visible(
                    m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                ensure_best_fixed_boundary_exact_chain_candidate_visible(
                    m_candidates);
                if not head_only_multi_syllable then
                begin
                    ensure_simple_fixed_boundary_chain_candidate_visible(
                        m_candidates);
                end;
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartialchain=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            if is_full_pinyin_key(lookup_text) and
                (input_syllable_count > 6) then
            begin
                if not query_has_preferred_subspan_coverage_for_light_path_local(False) then
                begin
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates);
                end;
                ensure_best_supported_complete_candidate_visible(
                    m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
            end;
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
            finalize_modal_tail_sentence_candidate(m_candidates);
            finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
            ensure_preferred_subspan_repair_candidate_visible(m_candidates);
            promote_preferred_query_phrase_candidate_for_query(lookup_text,
                m_candidates);
            promote_full_query_exact_complete_phrase_local(m_candidates);
            promote_multi_syllable_partial_without_full_exact_local(m_candidates);
            if (not head_only_multi_syllable) and is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 4) and
                (input_syllable_count <= 6) then
            begin
                ensure_simple_fixed_boundary_chain_candidate_visible(m_candidates);
            end;
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=0 shortpartial=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if has_multi_syllable_input and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 7) and
            (not all_initial_compact_query) and
            ((not has_internal_dangling_initial) or
            normalized_typo_long_query_fast_path) and
            (not query_has_modal_tail_suffix_local(lookup_text)) and
            (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            try_build_typo_normalized_prefix_candidate_local(
            stable_prefix_candidate, False, True) then
        begin
            SetLength(exact_head_partial_candidates, 1);
            exact_head_partial_candidates[0] := stable_prefix_candidate;
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(raw_candidates,
                    exact_head_partial_candidates, 0);
            end
            else
            begin
                raw_candidates := exact_head_partial_candidates;
                has_raw_candidates := True;
            end;
        end;

        if has_multi_syllable_input and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 7) and
            (not all_initial_compact_query) and
            ((not has_internal_dangling_initial) or
            normalized_typo_long_query_fast_path) and
            (not should_skip_expensive_sentence_generation_local) and
            query_has_modal_tail_suffix_local(lookup_text) and
            query_has_preferred_subspan_coverage_for_light_path_local(True) then
        begin
            if try_build_direct_preferred_subspan_sentence_candidate_local(
                direct_preferred_candidate) then
            begin
                has_long_direct_preferred_candidate := True;
            end;
        end;

        if has_multi_syllable_input and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 7) and
            (not all_initial_compact_query) and
            ((not has_internal_dangling_initial) or
            normalized_typo_long_query_fast_path) and
            (not should_defer_exact_chain_for_extendable_tail_local) and
            (not should_skip_expensive_sentence_generation_local) and
            query_has_preferred_subspan_coverage_for_light_path_local(True) then
        begin
            if try_build_direct_preferred_subspan_sentence_candidate_local(
                direct_preferred_candidate) then
            begin
                has_long_direct_preferred_candidate := True;
            end;
        end;

        if delayed_long_decode_mode and has_raw_candidates and
            has_multi_syllable_input and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 7) and (input_syllable_count <= 40) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) then
        begin
            m_candidates := raw_candidates;
            ensure_best_exact_chunk_chain_complete_candidate_visible(
                m_candidates);
            if (Length(m_candidates) > 0) and
                (m_candidates[0].source = cs_rule) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                input_syllable_count) then
            begin
                finalize_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 prefastdp=1 typofast=%d fqexact=%d allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted), Ord(all_initial_compact_query)]));
                Exit;
            end;
            if top_candidate_has_natural_complete_segment_path_local(m_candidates) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        ' preexactchain=1';
                end;
            end;
            if m_config.debug_mode and (input_syllable_count > 8) then
            begin
                finalize_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 prefastmissdebug=1 typofast=%d fqexact=%d allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted), Ord(all_initial_compact_query)]));
                Exit;
            end;
        end;

        phase_start_tick := nc_monotonic_tick_ms;
        if has_multi_syllable_input and (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not (has_raw_candidates and raw_from_dictionary and
            has_short_exact_complete_anchor(raw_candidates))) and
            (not should_skip_expensive_sentence_generation_local) and
            build_lightweight_sentence_candidates(lookup_text, lightweight_sentence_candidates) then
        begin
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(lightweight_sentence_candidates, raw_candidates, 0);
            end
            else
            begin
                raw_candidates := lightweight_sentence_candidates;
                has_raw_candidates := True;
            end;
            lightweight_sentence_applied := True;
        end;
        note_debug_helper_elapsed('tlite', phase_start_tick);

        if has_multi_syllable_input and is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 6) and
            build_long_exact_prefix_partial_candidates(lookup_text,
            exact_head_partial_candidates) then
        begin
            if has_raw_candidates then
            begin
                raw_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, raw_candidates, 0);
            end
            else
            begin
                raw_candidates := exact_head_partial_candidates;
                has_raw_candidates := True;
            end;
            raw_from_dictionary := True;
            lightweight_sentence_applied := True;
        end;

        if (not has_raw_candidates) and has_multi_syllable_input then
        begin
            if build_exact_head_partial_candidates(lookup_text, raw_candidates) then
            begin
                has_raw_candidates := True;
            end;
        end;

        if (not has_raw_candidates) and has_multi_syllable_input then
        begin
            if build_short_pinyin_prefix_completion_candidates(lookup_text,
                raw_candidates) then
            begin
                has_raw_candidates := True;
                raw_from_dictionary := True;
                trailing_prefix_rescue_applied := True;
            end;
        end;

        if (not has_raw_candidates) and has_multi_syllable_input then
        begin
            if build_exact_leading_prefix_partial_candidates(lookup_text, raw_candidates) then
            begin
                has_raw_candidates := True;
            end;
        end;

        if (not has_raw_candidates) and has_multi_syllable_input then
        begin
            SetLength(raw_candidates, 0);
            ensure_bounded_single_char_partial_pool(raw_candidates);
            phase_start_tick := nc_monotonic_tick_ms;
            merge_runtime_constructed_candidates(raw_candidates, False);
            Inc(runtime_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            if Length(raw_candidates) > 0 then
            begin
                has_raw_candidates := True;
            end;
            if all_initial_compact_query and (Length(raw_candidates) > 0) then
            begin
                // All-initial queries already prefer lightweight prefix fallbacks.
            end
            else if (Length(raw_candidates) > 0) and (runtime_phrase_added or runtime_redup_added) then
            begin
                has_raw_candidates := True;
                raw_candidates_seeded_from_runtime_only := True;
            end;
        end;

        if has_long_direct_chain_candidate then
        begin
            remember_segment_path_for_candidate(direct_chain_candidate.text,
                direct_chain_candidate.comment, direct_chain_encoded_path,
                direct_chain_candidate.dict_weight);
            if not has_raw_candidates then
            begin
                SetLength(raw_candidates, 1);
                raw_candidates[0] := direct_chain_candidate;
                has_raw_candidates := True;
            end
            else
            begin
                SetLength(exact_head_partial_candidates, 1);
                exact_head_partial_candidates[0] := direct_chain_candidate;
                raw_candidates := merge_candidate_lists(raw_candidates,
                    exact_head_partial_candidates, 0);
            end;
        end;

        if has_long_direct_preferred_candidate and (not has_raw_candidates) then
        begin
            SetLength(raw_candidates, 1);
            raw_candidates[0] := direct_preferred_candidate;
            has_raw_candidates := True;
        end;

        if has_raw_candidates then
        begin
            ultra_long_partial_shortfast_applied :=
                should_short_circuit_ultra_long_partial_query_local(raw_candidates);
        end;

        if has_raw_candidates and has_multi_syllable_input and
            (input_syllable_count >= 5) and (not all_initial_compact_query) and
            (has_safe_trailing_initial_typing_state or
            (is_full_pinyin_key(lookup_text) and (input_syllable_count >= 7) and
            (has_short_extendable_tail_prefix_local or
            has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count)) and
            (not has_preferred_tail_phrase_local(2)) and
            (not has_complete_exact_tail_phrase_local(2)))) then
        begin
            if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 7) and
                ((not has_internal_dangling_initial) or
                normalized_typo_long_query_fast_path) and
                query_has_preferred_subspan_coverage_for_light_path_local(True) and
                try_build_direct_preferred_subspan_sentence_candidate_local(
                direct_preferred_candidate) then
            begin
            end;

            m_candidates := raw_candidates;
            if get_cached_stable_prefix_candidate_local(
                stable_prefix_candidate) then
            begin
                SetLength(exact_head_partial_candidates, 1);
                exact_head_partial_candidates[0] := stable_prefix_candidate;
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            ensure_best_natural_prefix_candidate_visible_local(m_candidates);
            if (not query_has_modal_tail_suffix_local(lookup_text)) and
                prepare_prefix_partial_fast_exit_local(m_candidates, 2) then
            begin
                finalize_prefix_partial_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d prefixpartialfast=1 allinit=%d',
                    [Ord(has_multi_syllable_input),
                    Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]));
                Exit;
            end;
        end;

        if has_raw_candidates and has_multi_syllable_input and
            is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 7) and (input_syllable_count <= 8) and
            (not all_initial_compact_query) and
            (not has_internal_dangling_initial) and
            (not query_has_modal_tail_suffix_local(lookup_text)) and
            get_cached_stable_prefix_candidate_local(
            stable_prefix_candidate) then
        begin
            m_candidates := raw_candidates;
            SetLength(exact_head_partial_candidates, 1);
            exact_head_partial_candidates[0] := stable_prefix_candidate;
            m_candidates := merge_candidate_lists(exact_head_partial_candidates,
                m_candidates, 0);
            sort_candidates_lightweight(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates,
                get_candidate_limit, single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            ensure_best_natural_prefix_candidate_visible_local(m_candidates);
            if (not has_trusted_complete_top_candidate_local(m_candidates)) and
                (not has_complete_phrase_candidate_for_syllable_count(
                m_candidates, input_syllable_count)) and
                (get_candidate_text_unit_count(Trim(stable_prefix_candidate.text)) >=
                Max(3, input_syllable_count - 4)) and
                (Trim(stable_prefix_candidate.comment) <> '') then
            begin
                promote_candidate_to_front_local(m_candidates,
                    stable_prefix_candidate);
                finalize_prefix_partial_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d stableprefixmid=1 allinit=%d',
                    [Ord(has_multi_syllable_input),
                    Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]));
                Exit;
            end;
        end;

        if all_initial_compact_query and (input_syllable_count >= 5) and
            has_raw_candidates then
        begin
            m_candidates := raw_candidates;
            sort_candidates_lightweight(m_candidates);
            finalize_all_initial_compact_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d allinitfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;

        if delayed_long_decode_mode and has_raw_candidates and
            (not ultra_long_partial_shortfast_applied) and
            (not has_fuzzy_complete_candidate_for_syllable_count(
            raw_candidates, input_syllable_count)) and
            (not (has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count) and
            query_has_preferred_subspan_coverage_for_light_path_local(False))) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if not has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count) then
            begin
                merge_exact_long_full_lookup_candidates(raw_candidates,
                    lookup_text);
            end;
            merge_incomplete_trailing_prefix_full_lookup_candidates(raw_candidates,
                lookup_text);
            note_debug_helper_elapsed('tdmerge', phase_start_tick);
            if has_strong_delayed_long_decode_candidate(raw_candidates) and
                ((not is_full_pinyin_key(lookup_text)) or
                has_exact_complete_phrase_candidate_for_syllable_count(
                    raw_candidates, input_syllable_count) or
                has_natural_complete_segment_path_candidate_for_syllable_count(
                    raw_candidates, input_syllable_count)) then
            begin
                trim_candidates_for_delayed_long_decode(raw_candidates);
                m_candidates := raw_candidates;

                if get_cached_stable_prefix_candidate_local(
                    stable_prefix_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := stable_prefix_candidate;
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                sort_candidates_lightweight(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                ensure_multi_char_partial_precedes_single_char_partial(
                    m_candidates);
                ensure_rich_head_single_char_partials_visible(m_candidates);
                ensure_best_natural_prefix_candidate_visible_local(m_candidates);
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);

                if (not query_has_modal_tail_suffix_local(lookup_text)) and
                    prepare_prefix_partial_fast_exit_local(m_candidates, 2) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) <> '') then
                begin
                    trim_candidates_for_delayed_long_decode(m_candidates);
                    finalize_prefix_partial_candidates_and_prepare_exit(
                        Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 rawprefixfast=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added),
                        Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;

                ensure_best_exact_chunk_chain_complete_candidate_visible(
                    m_candidates);
                if top_candidate_has_natural_complete_segment_path_local(
                    m_candidates) then
                begin
                    trim_candidates_for_delayed_long_decode(m_candidates);
                    finalize_candidates_and_prepare_exit(
                        Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 rawcompletefast=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added),
                        Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;

                if get_cached_stable_prefix_candidate_local(
                    stable_prefix_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := stable_prefix_candidate;
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                    if candidate_has_preferred_prefix_coverage_for_fast_exit_local(
                        stable_prefix_candidate) then
                    begin
                        sort_candidates_lightweight(m_candidates);
                        ensure_partial_fallback_visible(m_candidates,
                            get_candidate_limit);
                        ensure_single_char_partial_visible(m_candidates,
                            get_candidate_limit, single_char_partial_min_count);
                        ensure_hard_single_char_partial_visible(m_candidates);
                        ensure_multi_char_partial_precedes_single_char_partial(
                            m_candidates);
                        ensure_rich_head_single_char_partials_visible(
                            m_candidates);
                        ensure_best_natural_prefix_candidate_visible_local(
                            m_candidates);
                        promote_candidate_to_front_local(m_candidates,
                            stable_prefix_candidate);
                        ensure_incomplete_tail_partial_precedes_completed_extension(
                            m_candidates);
                        finalize_preserved_candidates_and_prepare_exit(
                            Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 stableprefixseed=1 typofast=%d fqexact=%d allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]),
                            m_candidates[0], False);
                        Exit;
                    end;
                end;

                phase_start_tick := nc_monotonic_tick_ms;
                if build_exact_head_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_head_modal_tail_complete_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_long_exact_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                note_debug_helper_elapsed('tdhead', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_modal_tail_sentence_candidate_visible(m_candidates);
                note_debug_helper_elapsed('tdmodal', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                merge_confirmed_prefix_user_extensions(m_candidates);
                note_debug_helper_elapsed('tduserext', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                apply_user_penalties(lookup_text, m_candidates);
                apply_shifted_boundary_longer_exact_penalty(m_candidates);
                apply_compact_long_phrase_bonus(m_candidates);
                apply_preferred_query_phrase_bonus_for_query(lookup_text,
                    m_candidates);
                apply_preferred_sentence_phrase_bonus(m_candidates);
                note_debug_helper_elapsed('tdbonus', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                sort_candidates_lightweight(m_candidates);
                note_debug_helper_elapsed('tdsort', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates, get_candidate_limit,
                    single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
                ensure_rich_head_single_char_partials_visible(m_candidates);
                note_debug_helper_elapsed('tdpartial', phase_start_tick);
                if (input_syllable_count > 10) and (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                    input_syllable_count) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 completefast=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_natural_prefix_candidate_visible_local(m_candidates);
                note_debug_helper_elapsed('tdnatprefix', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);
                note_debug_helper_elapsed('tdincomplete', phase_start_tick);
                if top_partial_candidate_has_preferred_prefix_coverage_for_fast_exit_local(
                    m_candidates) then
                begin
                    finalize_prefix_partial_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 naturalprefixfast=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                phase_start_tick := nc_monotonic_tick_ms;
                if not has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    ensure_long_sentence_exact_candidate_visible(m_candidates);
                    ensure_long_sentence_partial_exact_candidate_visible(
                        m_candidates);
                    ensure_long_sentence_prefix_partial_visible(m_candidates,
                        get_candidate_limit);
                end;
                ensure_strong_two_plus_one_partial_visible(m_candidates,
                    get_candidate_limit);
                note_debug_helper_elapsed('tdexact', phase_start_tick);
                note_debug_helper_elapsed('tabs_after_tdexact',
                    total_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                note_debug_helper_elapsed('tfiltershort', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_lightweight_sentence_candidate_visible(m_candidates);
                note_debug_helper_elapsed('tbestlite', phase_start_tick);
                if not has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    if input_syllable_count <= 10 then
                    begin
                        phase_start_tick := nc_monotonic_tick_ms;
                        prepare_visible_complete_candidates_for_fast_exit_local(
                            m_candidates);
                        note_debug_helper_elapsed('tprep_previs',
                            phase_start_tick);
                        phase_start_tick := nc_monotonic_tick_ms;
                        if try_finalize_locked_long_postprocess_candidate_local(
                            'previsible') then
                        begin
                            Exit;
                        end;
                        note_debug_helper_elapsed('tlock_previs',
                            phase_start_tick);
                        phase_start_tick := nc_monotonic_tick_ms;
                        if try_finalize_stable_long_candidate_local(
                            'previsible', False) then
                        begin
                            Exit;
                        end;
                        note_debug_helper_elapsed('tstable_previs',
                            phase_start_tick);
                    end
                    else if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            ' tprep_previs=[skip-long]';
                    end;
                end;
                if has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) and
                    (not has_long_direct_chain_candidate) then
                begin
                    if input_syllable_count >= 12 then
                    begin
                        if try_build_typo_normalized_prefix_candidate_local(
                            stable_prefix_candidate, False, True) then
                        begin
                            SetLength(exact_head_partial_candidates, 1);
                            exact_head_partial_candidates[0] :=
                                stable_prefix_candidate;
                            m_candidates := merge_candidate_lists(m_candidates,
                                exact_head_partial_candidates, 0);
                        end;
                        ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                    end;
                    finalize_modal_tail_sentence_candidate(m_candidates);
                    finalize_le_plus_modal_particle_sentence_candidate(
                        m_candidates);
                    promote_preferred_query_phrase_candidate_for_query(
                        lookup_text, m_candidates);
                    if is_full_pinyin_key(lookup_text) and
                        (input_syllable_count >= 12) then
                    begin
                        if not has_strong_top_complete_candidate_for_postprocess_lock_local(
                            m_candidates) then
                        begin
                            ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                        end;
                    end
                    else
                    begin
                        ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                    end;
                    if has_long_direct_chain_candidate then
                    begin
                        SetLength(exact_head_partial_candidates, 1);
                        exact_head_partial_candidates[0] := direct_chain_candidate;
                        m_candidates := merge_candidate_lists(m_candidates,
                            exact_head_partial_candidates, 0);
                        promote_candidate_to_front_local(m_candidates,
                            direct_chain_candidate);
                        ensure_best_fixed_boundary_exact_chain_candidate_visible(
                            m_candidates);
                        ensure_full_query_exact_complete_candidate_visible(
                            m_candidates);
                        promote_full_query_exact_complete_phrase_local(
                            m_candidates);
                    end;
                    ensure_incomplete_tail_partial_precedes_completed_extension(
                        m_candidates);
                    ensure_complete_extension_precedes_prefix_partial(
                        m_candidates);
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 partialfinal=1 xtail=%d typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(has_extendable_same_syllable_tail_local(lookup_text,
                        input_syllable_count)),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if input_syllable_count <= 8 then
                begin
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates);
                    if m_config.debug_mode and (Length(m_candidates) > 0) then
                    begin
                        m_last_full_path_debug_info := m_last_full_path_debug_info +
                            Format(' afterxfast=[top=%s src=%d units=%d]',
                            [Trim(m_candidates[0].text), Ord(m_candidates[0].source),
                            get_candidate_text_unit_count(Trim(m_candidates[0].text))]);
                    end;
                    if (Length(m_candidates) > 0) and
                        (m_candidates[0].source = cs_rule) and
                        (Trim(m_candidates[0].comment) = '') and
                        (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                        input_syllable_count) then
                    begin
                        prepare_visible_complete_candidates_for_fast_exit_local(
                            m_candidates);
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 fastdp=1 typofast=%d fqexact=%d allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                    promote_full_query_exact_complete_phrase_local(m_candidates);
                    if is_full_pinyin_key(lookup_text) and
                        (input_syllable_count >= 6) and
                        (not has_strong_near_complete_prefix_partial_candidate_local(
                        m_candidates)) and
                        top_candidate_has_natural_complete_segment_path_local(
                        m_candidates) then
                    begin
                        if input_syllable_count >= 10 then
                        begin
                            ensure_strict_oracle_complete_candidate_visible(
                                m_candidates);
                        end;
                        ensure_best_oracle_backed_exact_complete_candidate_visible(
                            m_candidates);
                        prepare_visible_complete_candidates_for_fast_exit_local(
                            m_candidates);
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 exactchainfast=1 typofast=%d fqexact=%d allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                note_debug_helper_elapsed('tabs_before_tail',
                    total_start_tick);
                if try_finalize_budgeted_long_postprocess_candidate_local(
                    'beforetail') then
                begin
                    Exit;
                end;
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                note_debug_helper_elapsed('tdtail_fast', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                note_debug_helper_elapsed('tpref_fast', phase_start_tick);
                if try_finalize_budgeted_long_postprocess_candidate_local(
                    'aftertail') then
                begin
                    Exit;
                end;
                if input_syllable_count > 8 then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates);
                    note_debug_helper_elapsed('txfastdp', phase_start_tick);
                    phase_start_tick := nc_monotonic_tick_ms;
                    if not has_complete_top_candidate_for_fast_exit_local(
                        m_candidates) then
                    begin
                        promote_best_supported_complete_candidate_visible_local(
                            m_candidates);
                    end;
                    note_debug_helper_elapsed('txfastpromote',
                        phase_start_tick);
                    if has_complete_top_candidate_for_fast_exit_local(
                        m_candidates) then
                    begin
                        if input_syllable_count <= 20 then
                        begin
                            phase_start_tick := nc_monotonic_tick_ms;
                            prepare_visible_complete_candidates_for_fast_exit_local(
                                m_candidates);
                            note_debug_helper_elapsed('tprep_fast',
                                phase_start_tick);
                        end
                        else if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                ' tprep_fast=[skip-ultra-long]';
                        end;
                        note_debug_helper_elapsed('tabs_fastdone',
                            total_start_tick);
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 fastdp=1 typofast=%d fqexact=%d allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                promote_full_query_exact_complete_phrase_local(m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 6) and
                    (not has_strong_near_complete_prefix_partial_candidate_local(
                    m_candidates)) and
                    top_candidate_has_natural_complete_segment_path_local(
                    m_candidates) then
                begin
                    if input_syllable_count >= 10 then
                    begin
                        ensure_strict_oracle_complete_candidate_visible(
                            m_candidates);
                    end;
                    ensure_best_oracle_backed_exact_complete_candidate_visible(
                        m_candidates);
                    prepare_visible_complete_candidates_for_fast_exit_local(
                        m_candidates);
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 exactchainfast=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                prepare_visible_complete_candidates_for_fast_exit_local(
                    m_candidates);
                infer_paths_for_front_full_length_complete_candidates(m_candidates);
                ensure_best_inferred_full_length_candidate_visible(m_candidates);
                ensure_best_full_length_pathless_candidate_visible(m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_exact_dict_complete_candidate_visible(m_candidates);
                ensure_best_user_complete_candidate_visible(m_candidates);
                ensure_incomplete_tail_partial_precedes_completed_extension(m_candidates);
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                if try_finalize_stable_long_candidate_local(
                    'predelay', True) then
                begin
                    Exit;
                end;
                ensure_best_exact_window_consensus_complete_candidate_visible(
                    m_candidates);
                if input_syllable_count >= 10 then
                begin
                    ensure_strict_oracle_complete_candidate_visible(
                        m_candidates);
                end;
                ensure_best_oracle_backed_exact_complete_candidate_visible(
                    m_candidates);
                prepare_visible_complete_candidates_for_fast_exit_local(
                    m_candidates);
                if has_strong_top_complete_candidate_for_postprocess_lock_local(
                    m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 faststrong=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 8) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(m_candidates[0].text)) = input_syllable_count) then
                begin
                    finalize_stable_consensus_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 earlycons=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 9) then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    ensure_best_fixed_boundary_exact_chain_candidate_visible(
                        m_candidates);
                    note_debug_helper_elapsed('tfxe', phase_start_tick);
                    phase_start_tick := nc_monotonic_tick_ms;
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates);
                    note_debug_helper_elapsed('txcce', phase_start_tick);
                    if input_syllable_count >= 10 then
                    begin
                        ensure_strict_oracle_complete_candidate_visible(
                            m_candidates);
                    end;
                    prepare_visible_complete_candidates_for_fast_exit_local(
                        m_candidates);
                    ensure_best_exact_window_consensus_complete_candidate_visible(
                        m_candidates);
                    if (Length(m_candidates) > 0) and
                        (Trim(m_candidates[0].comment) = '') and
                        (get_candidate_text_unit_count(
                        Trim(m_candidates[0].text)) = input_syllable_count) then
                    begin
                        finalize_stable_consensus_candidates_and_prepare_exit(
                            Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 earlychain=1 typofast=%d fqexact=%d allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                end;
                if top_candidate_has_preferred_subspan_coverage_for_fast_exit_local(
                    m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 fastrepair=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if full_query_exact_promoted and is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 8) and
                    has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d fastfqv=1 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                ensure_strict_oracle_complete_candidate_visible(m_candidates);
                phase_start_tick := nc_monotonic_tick_ms;
                if should_skip_expensive_subquery_sentence_solver_local(
                    m_candidates) then
                begin
                    subquery_sentence_promoted := False;
                    subquery_sentence_missed := True;
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            ' tss=[skip-long-partial]';
                    end;
                end
                else
                begin
                    ensure_best_subquery_sentence_candidate_visible(m_candidates,
                        subquery_sentence_promoted);
                end;
                note_debug_helper_elapsed('tss', phase_start_tick);
                if (not subquery_sentence_promoted) and
                    should_record_subquery_sentence_miss_local(m_candidates) then
                begin
                    subquery_sentence_missed := True;
                end;
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                ensure_strict_oracle_complete_candidate_visible(m_candidates);
                if subquery_sentence_promoted and
                    (has_complete_top_candidate_for_fast_exit_local(
                    m_candidates) or
                    is_trusted_top_complete_candidate_for_early_exit(
                    m_candidates)) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                if not (subquery_sentence_promoted and
                    is_trusted_top_complete_candidate_for_early_exit(
                    m_candidates)) then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    if should_skip_expensive_long_partial_repair_local(
                        m_candidates) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                ' tfx=[skip-long-partial]';
                        end;
                    end
                    else
                    begin
                        ensure_best_fixed_boundary_exact_chain_candidate_visible(
                            m_candidates);
                    end;
                    note_debug_helper_elapsed('tfx', phase_start_tick);
                    phase_start_tick := nc_monotonic_tick_ms;
                    ensure_best_exact_chunk_chain_complete_candidate_visible(m_candidates);
                    note_debug_helper_elapsed('txcc', phase_start_tick);
                    ensure_full_query_exact_complete_candidate_visible(
                        m_candidates);
                    promote_full_query_exact_complete_phrase_local(m_candidates);
                    if input_syllable_count >= 10 then
                    begin
                        ensure_strict_oracle_complete_candidate_visible(
                            m_candidates);
                    end;
                    ensure_best_oracle_backed_exact_complete_candidate_visible(
                        m_candidates);
                    prepare_visible_complete_candidates_for_fast_exit_local(
                        m_candidates);
                    if has_strong_top_complete_candidate_for_postprocess_lock_local(
                        m_candidates) or
                        is_trusted_top_complete_candidate_for_early_exit(
                        m_candidates) then
                    begin
                        finalize_candidates_and_prepare_exit(Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d postrepairfast=1 allinit=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added), Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted),
                            Ord(all_initial_compact_query)]));
                        Exit;
                    end;
                    if not (is_full_pinyin_key(lookup_text) and
                        (has_exact_complete_phrase_candidate_for_syllable_count(
                        m_candidates, input_syllable_count) or
                        has_natural_complete_segment_path_candidate_for_syllable_count(
                        m_candidates, input_syllable_count))) then
                    begin
                        phase_start_tick := nc_monotonic_tick_ms;
                        ensure_best_exact_chunk_chain_partial_candidate_visible(
                            m_candidates);
                        note_debug_helper_elapsed('txcp', phase_start_tick);
                        phase_start_tick := nc_monotonic_tick_ms;
                        ensure_best_visible_partial_completion_candidate_visible(
                            m_candidates);
                        note_debug_helper_elapsed('tvpc', phase_start_tick);
                    end;

                    if try_finalize_stable_long_candidate_local(
                        'postchain', True) then
                    begin
                        Exit;
                    end;
                end;
                ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(m_candidates);
                ensure_best_dict_exact_prefix_partial_visible(m_candidates);
                ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(m_candidates);
                ensure_strong_long_dict_prefix_partial_visible(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(m_candidates);
                if ((Length(m_candidates) = 0) or
                    (Trim(m_candidates[0].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
                    input_syllable_count)) and
                    (not has_exact_complete_phrase_candidate_for_syllable_count(
                    m_candidates, input_syllable_count)) and
                    (not has_natural_complete_segment_path_candidate_for_syllable_count(
                    m_candidates, input_syllable_count)) then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    if should_skip_expensive_long_partial_repair_local(
                        m_candidates) then
                    begin
                        if m_config.debug_mode then
                        begin
                            m_last_full_path_debug_info :=
                                m_last_full_path_debug_info +
                                ' tfx2=[skip-long-partial]';
                        end;
                    end
                    else
                    begin
                        ensure_best_fixed_boundary_exact_chain_candidate_visible(
                            m_candidates);
                    end;
                    note_debug_helper_elapsed('tfx2', phase_start_tick);
                end;
                ensure_complete_extension_precedes_prefix_partial(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
                finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
                promote_preferred_query_phrase_candidate_for_query(lookup_text,
                    m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                ensure_best_exact_window_repair_complete_candidate_visible(
                    m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                ensure_best_exact_window_consensus_complete_candidate_visible(
                    m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 8) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(m_candidates[0].text)) = input_syllable_count) then
                begin
                    finalize_stable_consensus_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d stablecons=1 allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
                finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                ensure_best_oracle_backed_exact_complete_candidate_visible(
                    m_candidates);
                if (ensure_visible_strict_oracle_top_complete_candidate_visible(
                    m_candidates) or
                    ensure_strict_oracle_complete_candidate_visible(
                    m_candidates)) and is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 10) and
                    is_trusted_top_complete_candidate_for_early_exit(
                    m_candidates) then
                begin
                    finalize_lookup_timing_info;
                    if m_config.debug_mode then
                    begin
                        m_last_lookup_debug_extra := Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d fastoracle=1',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted)]) +
                            Format(' allinit=%d',
                            [Ord(all_initial_compact_query)]);
                        if Length(m_candidates) > 0 then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ' +
                                get_candidate_debug_summary(m_candidates[0]);
                        end;
                        if m_last_full_path_debug_info <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra +
                                m_last_full_path_debug_info;
                        end;
                    end;
                    refresh_candidate_segment_paths;
                    note_ranked_top_candidate;
                    m_page_index := 0;
                    m_selected_index := 0;
                    Exit;
                end;
                ensure_best_long_exact_subspan_sentence_candidate_visible(
                    m_candidates);
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_best_exact_window_consensus_complete_candidate_visible(
                    m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 8) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(m_candidates[0].text)) = input_syllable_count) then
                begin
                    finalize_stable_consensus_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 latecons=1 typofast=%d fqexact=%d allinit=%d',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);
                ensure_exact_backed_same_comment_partial_visible(m_candidates);
                ensure_best_oracle_backed_exact_complete_candidate_visible(
                    m_candidates);
                ensure_strict_oracle_exact_top_complete_candidate_visible(
                    m_candidates);
                ensure_best_late_exact_chunk_consensus_complete_candidate_visible(
                    m_candidates);
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                ensure_best_resolved_sentence_candidate_visible_aggressive(
                    m_candidates);
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                ensure_strict_oracle_complete_candidate_visible(m_candidates);
                finalize_lookup_timing_info;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=1 delayed=1 typofast=%d fqexact=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]) +
                        Format(' allinit=%d', [Ord(all_initial_compact_query)]);
                    if Length(m_candidates) > 0 then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                            get_candidate_debug_summary(m_candidates[0]);
                    end;
                    if m_last_full_path_debug_info <> '' then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            m_last_full_path_debug_info;
                    end;
                end;
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;
        end;

        if has_raw_candidates and
            (not has_fuzzy_complete_candidate_for_syllable_count(
            raw_candidates, input_syllable_count)) and
            (((lightweight_sentence_applied) and
            ((not delayed_long_decode_mode) or
            (not is_full_pinyin_key(lookup_text)) or
            has_complete_phrase_candidate_for_syllable_count(
                raw_candidates, input_syllable_count) or
            has_exact_complete_phrase_candidate_for_syllable_count(
                raw_candidates, input_syllable_count) or
            has_natural_complete_segment_path_candidate_for_syllable_count(
                raw_candidates, input_syllable_count))) or
            ultra_long_partial_shortfast_applied) then
        begin
            sort_candidates_lightweight(raw_candidates);
            reserve_fuzzy_complete_candidates_local(raw_candidates,
                get_candidate_limit);
            filter_non_bmp_single_char_candidates(raw_candidates);

            limit := get_total_candidate_limit;
            multi_syllable_cap_limit := get_candidate_limit * 3;
            if count_single_char_partial_candidates(raw_candidates) +
                get_candidate_limit > multi_syllable_cap_limit then
            begin
                multi_syllable_cap_limit :=
                    count_single_char_partial_candidates(raw_candidates) +
                    get_candidate_limit;
            end;
            if multi_syllable_cap_limit > c_candidate_total_limit_max then
            begin
                multi_syllable_cap_limit := c_candidate_total_limit_max;
            end;
            if limit > multi_syllable_cap_limit then
            begin
                limit := multi_syllable_cap_limit;
            end;
            if limit > c_candidate_total_limit_max then
            begin
                limit := c_candidate_total_limit_max;
            end;

            if Length(raw_candidates) > limit then
            begin
                SetLength(m_candidates, limit);
                for i := 0 to limit - 1 do
                begin
                    m_candidates[i] := raw_candidates[i];
                end;
            end
            else
            begin
                m_candidates := raw_candidates;
            end;

            if is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 7) and
                lightweight_sentence_applied and
                (not all_initial_compact_query) and
                ((not has_internal_dangling_initial) or
                normalized_typo_long_query_fast_path) and
                try_build_direct_preferred_subspan_sentence_candidate_local(
                direct_preferred_candidate) then
            begin
                SetLength(exact_head_partial_candidates, 1);
                exact_head_partial_candidates[0] := direct_preferred_candidate;
                m_candidates := merge_candidate_lists(
                    exact_head_partial_candidates, m_candidates, 0);
                ensure_bounded_single_char_partial_pool(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                if input_syllable_count < 9 then
                begin
                    finalize_modal_tail_sentence_candidate(m_candidates);
                    finalize_le_plus_modal_particle_sentence_candidate(
                        m_candidates);
                    finalize_direct_preferred_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d prefdirect=1 fastlight=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]) +
                        Format(' allinit=%d ultrafast=%d',
                        [Ord(all_initial_compact_query),
                        Ord(ultra_long_partial_shortfast_applied)]));
                    Exit;
                end;
                if (Trim(direct_preferred_candidate.comment) <> '') and
                    (top_candidate_is_stable_non_modal_partial_local(m_candidates) or
                    prepare_prefix_partial_fast_exit_local(m_candidates, 3)) then
                begin
                    finalize_direct_preferred_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d prefdirect=1 longpartial=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]) +
                        Format(' allinit=%d ultrafast=%d',
                        [Ord(all_initial_compact_query),
                        Ord(ultra_long_partial_shortfast_applied)]));
                    Exit;
                end;
            end;

            if normalized_typo_long_query_fast_path and
                is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 6) and
                lightweight_sentence_applied then
            begin
                if try_build_direct_preferred_subspan_sentence_candidate_local(
                    direct_preferred_candidate) then
                begin
                    SetLength(exact_head_partial_candidates, 1);
                    exact_head_partial_candidates[0] := direct_preferred_candidate;
                    m_candidates := merge_candidate_lists(
                        exact_head_partial_candidates, m_candidates, 0);
                    ensure_bounded_single_char_partial_pool(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    finalize_modal_tail_sentence_candidate(m_candidates);
                    finalize_le_plus_modal_particle_sentence_candidate(
                        m_candidates);
                    finalize_direct_preferred_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d fasttypo=1 prefdirect=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]) +
                        Format(' allinit=%d ultrafast=%d',
                        [Ord(all_initial_compact_query),
                        Ord(ultra_long_partial_shortfast_applied)]));
                    Exit;
                end;
                merge_confirmed_prefix_user_extensions(m_candidates);
                apply_user_penalties(lookup_text, m_candidates);
                apply_shifted_boundary_longer_exact_penalty(m_candidates);
                apply_compact_long_phrase_bonus(m_candidates);
                apply_preferred_query_phrase_bonus_for_query(lookup_text,
                    m_candidates);
                apply_preferred_sentence_phrase_bonus(m_candidates);
                sort_candidates_lightweight(m_candidates);
                ensure_partial_fallback_visible(m_candidates,
                    get_candidate_limit);
                ensure_single_char_partial_visible(m_candidates,
                    get_candidate_limit, single_char_partial_min_count);
                ensure_hard_single_char_partial_visible(m_candidates);
                ensure_multi_char_partial_precedes_single_char_partial(
                    m_candidates);
                ensure_rich_head_single_char_partials_visible(m_candidates);
                ensure_best_lightweight_sentence_candidate_visible(m_candidates);
                ensure_best_full_length_pathless_candidate_visible(m_candidates);
                ensure_exact_dict_complete_candidate_visible(m_candidates);
                ensure_best_user_complete_candidate_visible(m_candidates);
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
                finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
                if (Length(m_candidates) = 0) or
                    (Trim(m_candidates[0].comment) <> '') then
                begin
                    ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(
                        m_candidates);
                    ensure_best_dict_exact_prefix_partial_visible(m_candidates);
                    ensure_strong_long_dict_prefix_partial_visible(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(
                        m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                end;
                finalize_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d fasttypo=1',
                    [Ord(has_multi_syllable_input),
                    Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial),
                    Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(lightweight_sentence_applied),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted)]) +
                    Format(' allinit=%d ultrafast=%d',
                    [Ord(all_initial_compact_query),
                    Ord(ultra_long_partial_shortfast_applied)]));
                Exit;
            end;

            if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 8) and
                lightweight_sentence_applied then
            begin
                if build_exact_head_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_leading_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_exact_head_modal_tail_complete_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                if build_long_exact_prefix_partial_candidates(lookup_text,
                    exact_head_partial_candidates) then
                begin
                    m_candidates := merge_candidate_lists(m_candidates,
                        exact_head_partial_candidates, 0);
                end;
                ensure_modal_tail_sentence_candidate_visible(m_candidates);
                merge_confirmed_prefix_user_extensions(m_candidates);
                apply_user_penalties(lookup_text, m_candidates);
                apply_shifted_boundary_longer_exact_penalty(m_candidates);
                apply_compact_long_phrase_bonus(m_candidates);
                apply_preferred_query_phrase_bonus_for_query(lookup_text,
                    m_candidates);
                apply_preferred_sentence_phrase_bonus(m_candidates);
                sort_candidates_lightweight(m_candidates);
                ensure_best_lightweight_sentence_candidate_visible(m_candidates);
                ensure_best_exact_tail_complete_candidate_visible(m_candidates);
                finalize_modal_tail_sentence_candidate(m_candidates);
                finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
                if not (is_full_pinyin_key(lookup_text) and
                    has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) and
                    (not has_exact_complete_phrase_candidate_for_syllable_count(
                    m_candidates, input_syllable_count)) and
                    (not has_natural_complete_segment_path_candidate_for_syllable_count(
                    m_candidates, input_syllable_count))) then
                begin
                    ensure_best_resolved_sentence_candidate_visible(m_candidates);
                    ensure_best_resolved_sentence_candidate_visible_aggressive(
                        m_candidates);
                    ensure_best_fixed_boundary_exact_chain_candidate_visible(
                        m_candidates);
                    ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                    ensure_best_exact_chunk_chain_complete_candidate_visible(
                        m_candidates);
                    ensure_full_query_exact_complete_candidate_visible(
                        m_candidates);
                    ensure_exact_dict_complete_candidate_visible(m_candidates);
                    ensure_best_user_complete_candidate_visible(m_candidates);
                    ensure_best_supported_complete_candidate_visible(m_candidates);
                    ensure_strong_supported_complete_candidate_locked_visible(
                        m_candidates);
                    promote_full_query_exact_complete_phrase_local(m_candidates);
                end;
                if (input_syllable_count >= 10) and
                    top_candidate_has_natural_complete_segment_path_local(
                    m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d stablefinal=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]));
                    Exit;
                end;
                if has_strong_top_complete_candidate_for_postprocess_lock_local(
                    m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d fastfinal=1 fastresolved=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]));
                    Exit;
                end;
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_strong_supported_complete_candidate_locked_visible(
                    m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 10) and
                    has_exact_complete_phrase_candidate_for_syllable_count(m_candidates,
                    input_syllable_count) and
                    top_candidate_has_natural_complete_segment_path_local(
                    m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d stablemid=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]));
                    Exit;
                end;
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_best_exact_window_consensus_complete_candidate_visible(
                    m_candidates);
                if is_full_pinyin_key(lookup_text) and
                    (input_syllable_count >= 8) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(
                    Trim(m_candidates[0].text)) = input_syllable_count) then
                begin
                    finalize_stable_consensus_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d stablecons=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]));
                    Exit;
                end;
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_best_oracle_backed_exact_complete_candidate_visible(
                    m_candidates);
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                if ensure_visible_strict_oracle_top_complete_candidate_visible(
                    m_candidates) and long_full_exact_query and
                    (input_syllable_count >= 10) then
                begin
                    ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                    finalize_lookup_timing_info;
                    if m_config.debug_mode then
                    begin
                        m_last_lookup_debug_extra := Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d fastfinal=1 fastoracle=1',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(lightweight_sentence_applied),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted)]) +
                            Format(' allinit=%d',
                            [Ord(all_initial_compact_query)]);
                        if Length(m_candidates) > 0 then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ' +
                                get_candidate_debug_summary(m_candidates[0]);
                        end;
                        if m_last_full_path_debug_info <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra +
                                m_last_full_path_debug_info;
                        end;
                    end;
                    refresh_candidate_segment_paths;
                    note_ranked_top_candidate;
                    m_page_index := 0;
                    m_selected_index := 0;
                    Exit;
                end;
                ensure_best_long_exact_subspan_sentence_candidate_visible(
                    m_candidates);
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);
                ensure_exact_backed_same_comment_partial_visible(m_candidates);
                ensure_complete_extension_precedes_prefix_partial(m_candidates);
                if (Length(m_candidates) = 0) or
                    (Trim(m_candidates[0].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
                    input_syllable_count) then
                begin
                    ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(
                        m_candidates);
                    ensure_best_dict_exact_prefix_partial_visible(m_candidates);
                    ensure_strong_long_dict_prefix_partial_visible(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(
                        m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                end;
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
                finalize_lookup_timing_info;
                if m_config.debug_mode then
                begin
                    m_last_lookup_debug_extra := Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d fastfinal=1',
                        [Ord(has_multi_syllable_input),
                        Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial),
                        Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(lightweight_sentence_applied),
                        Ord(normalized_typo_long_query_fast_path),
                        Ord(full_query_exact_promoted)]) +
                        Format(' allinit=%d ultrafast=%d',
                        [Ord(all_initial_compact_query),
                        Ord(ultra_long_partial_shortfast_applied)]);
                    if Length(m_candidates) > 0 then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            ' ' + get_candidate_debug_summary(m_candidates[0]);
                    end;
                    if m_last_full_path_debug_info <> '' then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            m_last_full_path_debug_info;
                    end;
                end;
                refresh_candidate_segment_paths;
                note_ranked_top_candidate;
                m_page_index := 0;
                m_selected_index := 0;
                Exit;
            end;

            if is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 6) and
                lightweight_sentence_applied then
            begin
                apply_preferred_query_phrase_bonus_for_query(lookup_text,
                    m_candidates);
                apply_preferred_sentence_phrase_bonus(m_candidates);
                sort_candidates_lightweight(m_candidates);
                if has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    ensure_best_lightweight_sentence_candidate_visible(m_candidates);
                    ensure_incomplete_tail_partial_precedes_completed_extension(
                        m_candidates);
                    ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(
                        m_candidates);
                    ensure_best_dict_exact_prefix_partial_visible(m_candidates);
                    ensure_strong_long_dict_prefix_partial_visible(m_candidates);
                    ensure_partial_fallback_visible(m_candidates,
                        get_candidate_limit);
                    ensure_single_char_partial_visible(m_candidates,
                        get_candidate_limit, single_char_partial_min_count);
                    ensure_hard_single_char_partial_visible(m_candidates);
                    ensure_multi_char_partial_precedes_single_char_partial(
                        m_candidates);
                    ensure_rich_head_single_char_partials_visible(m_candidates);
                    finalize_lookup_timing_info;
                    if m_config.debug_mode then
                    begin
                        m_last_lookup_debug_extra := Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d xtailfast=1 typofast=%d fqexact=%d',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(lightweight_sentence_applied),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted)]) +
                            Format(' allinit=%d ultrafast=%d',
                            [Ord(all_initial_compact_query),
                            Ord(ultra_long_partial_shortfast_applied)]);
                        if Length(m_candidates) > 0 then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ' +
                                get_candidate_debug_summary(m_candidates[0]);
                        end;
                        if m_last_full_path_debug_info <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra +
                                m_last_full_path_debug_info;
                        end;
                    end;
                    refresh_candidate_segment_paths;
                    note_ranked_top_candidate;
                    m_page_index := 0;
                    m_selected_index := 0;
                    Exit;
                end;
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                promote_full_query_exact_complete_phrase_local(m_candidates);
                if has_strong_top_complete_candidate_for_postprocess_lock_local(
                    m_candidates) then
                begin
                    finalize_lookup_timing_info;
                    if m_config.debug_mode then
                    begin
                        m_last_lookup_debug_extra := Format(
                            'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d postlockfast=1',
                            [Ord(has_multi_syllable_input),
                            Ord(has_segment_candidates),
                            Ord(has_internal_dangling_initial),
                            Ord(head_only_multi_syllable),
                            Ord(runtime_phrase_added),
                            Ord(runtime_redup_added),
                            Ord(lightweight_sentence_applied),
                            Ord(normalized_typo_long_query_fast_path),
                            Ord(full_query_exact_promoted)]) +
                            Format(' allinit=%d ultrafast=%d',
                            [Ord(all_initial_compact_query),
                            Ord(ultra_long_partial_shortfast_applied)]);
                        if Length(m_candidates) > 0 then
                        begin
                            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                                ' ' + get_candidate_debug_summary(m_candidates[0]);
                        end;
                        if m_last_full_path_debug_info <> '' then
                        begin
                            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                                m_last_full_path_debug_info;
                        end;
                    end;
                    refresh_candidate_segment_paths;
                    note_ranked_top_candidate;
                    m_page_index := 0;
                    m_selected_index := 0;
                    Exit;
                end;
                ensure_best_resolved_sentence_candidate_visible(m_candidates);
                ensure_best_supported_complete_candidate_visible(m_candidates);
                ensure_best_visible_complete_sentence_candidate_visible(
                    m_candidates);
                ensure_best_oracle_backed_exact_complete_candidate_visible(
                    m_candidates);
                ensure_strict_oracle_exact_top_complete_candidate_visible(
                    m_candidates);
                ensure_best_late_exact_chunk_consensus_complete_candidate_visible(
                    m_candidates);
                ensure_best_resolved_sentence_candidate_visible_aggressive(
                    m_candidates);
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
            end;

            finalize_lookup_timing_info;
            if m_config.debug_mode then
            begin
                m_last_lookup_debug_extra := Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d typofast=%d fqexact=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(lightweight_sentence_applied),
                    Ord(normalized_typo_long_query_fast_path),
                    Ord(full_query_exact_promoted)]) +
                    Format(' allinit=%d ultrafast=%d',
                    [Ord(all_initial_compact_query),
                    Ord(ultra_long_partial_shortfast_applied)]);
                if Length(m_candidates) > 0 then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                        get_candidate_debug_summary(m_candidates[0]);
                end;
                if m_last_full_path_debug_info <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        m_last_full_path_debug_info;
                end;
            end;
            refresh_candidate_segment_paths;
            note_ranked_top_candidate;
            m_page_index := 0;
            m_selected_index := 0;
            Exit;
        end;

        if has_raw_candidates then
        begin
            if raw_from_dictionary and has_explicit_apostrophe_input and (input_syllable_count > 1) then
            begin
                if m_config.enable_segment_candidates then
                begin
                    build_segment_candidates_timed(explicit_apostrophe_aligned_candidates,
                        (not c_suppress_nonlexicon_complete_long_candidates) or
                        should_enable_long_sentence_full_path_search_local);
                end;
                filter_complete_candidates_for_explicit_apostrophe_boundary(raw_candidates,
                    explicit_apostrophe_aligned_candidates);
                filter_short_dictionary_hits_for_explicit_apostrophe(raw_candidates,
                    input_syllable_count);
                has_raw_candidates := Length(raw_candidates) > 0;
            end;

            if not has_raw_candidates then
            begin
                raw_from_dictionary := False;
            end;

        if has_raw_candidates then
        begin
            if raw_from_dictionary then
            begin
                clear_candidate_comments(raw_candidates);
            end;

            // Long queries are fully sorted again after segment and exact-path
            // candidates are merged. Keep this preliminary ordering inexpensive.
            if input_syllable_count >= 10 then
            begin
                sort_candidates_lightweight(raw_candidates);
            end
            else
            begin
                sort_candidates_timed(raw_candidates);
            end;
            if raw_from_dictionary and (m_last_lookup_normalized_from = '') and
                has_relaxed_missing_apostrophe_partial and
                has_multi_char_dictionary_anchor(raw_candidates) then
            begin
                allow_relaxed_missing_apostrophe := True;
                ensure_bounded_single_char_partial_pool(raw_candidates);
                sort_candidates_timed(raw_candidates);
                ensure_partial_fallback_visible(raw_candidates, get_candidate_limit);
                ensure_single_char_partial_visible(raw_candidates, get_candidate_limit,
                    single_char_partial_min_count);
                ensure_multi_char_partial_precedes_single_char_partial(raw_candidates);
                ensure_rich_head_single_char_partials_visible(raw_candidates);
                allow_relaxed_missing_apostrophe := False;
            end;
            if m_config.enable_segment_candidates and raw_from_dictionary and
                (not has_internal_dangling_initial) and (not all_initial_compact_query) then
            begin
                if trailing_prefix_rescue_applied then
                begin
                    has_segment_candidates := False;
                end
                else if (input_syllable_count >= 4) and
                    has_complete_phrase_candidate_for_syllable_count(raw_candidates,
                        input_syllable_count) then
                begin
                    has_segment_candidates := False;
                end
                else if should_enable_long_sentence_exact_search_local and
                    (input_syllable_count >= 5) and
                    has_extendable_trailing_prefix_query_text(lookup_text) then
                begin
                    // For long in-progress sentences with an unfinished trailing syllable,
                    // the exact-path composer already provides the useful prediction.
                    // A second segment pass is expensive here and mostly adds noisy variants.
                    has_segment_candidates := False;
                end
                else if not has_segment_candidates then
                begin
                    // When exact lexicon hits already exist, keep the extra segment pass cheap.
                    // The current product strategy prefers direct lexicon matches and only needs
                    // lightweight partial/anchor assistance here, not a second full-path search.
                    if c_suppress_nonlexicon_complete_long_candidates and
                        (not should_enable_long_sentence_full_path_search_local) then
                    begin
                        has_segment_candidates := build_segment_candidates_timed(segment_candidates, False);
                    end
                    else
                    begin
                        has_segment_candidates := build_segment_candidates_timed(segment_candidates, True);
                    end;
                end;

                if has_segment_candidates then
                begin
                    raw_candidates := merge_candidate_lists(raw_candidates, segment_candidates, 0);
                    if not (should_enable_long_sentence_exact_search_local and
                        has_strong_long_sentence_prediction and
                        (input_syllable_count >= 10)) then
                    begin
                        ensure_partial_fallback_visible(raw_candidates, get_candidate_limit);
                        ensure_single_char_partial_visible(raw_candidates, get_candidate_limit,
                            single_char_partial_min_count);
                        ensure_multi_char_partial_precedes_single_char_partial(raw_candidates);
                        ensure_rich_head_single_char_partials_visible(raw_candidates);
                        ensure_long_sentence_prefix_partial_visible(raw_candidates,
                            get_candidate_limit);
                        ensure_strong_two_plus_one_partial_visible(raw_candidates,
                            get_candidate_limit);
                    end;
                end;
            end;
        end;

        // Even when segment candidates are disabled or fail to build, multi-syllable input
        // must keep a single-char partial fallback (e.g. "hai" + "budaxing").
        if has_multi_syllable_input then
        begin
            if not raw_candidates_seeded_from_runtime_only then
            begin
                ensure_bounded_single_char_partial_pool(raw_candidates);
                phase_start_tick := nc_monotonic_tick_ms;
                merge_runtime_constructed_candidates(raw_candidates);
                Inc(runtime_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
            end;
            if should_enable_long_sentence_exact_search_local then
            begin
                if not (is_full_pinyin_key(lookup_text) and
                    has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count)) then
                begin
                    phase_start_tick := nc_monotonic_tick_ms;
                    merge_long_sentence_exact_candidates(raw_candidates, lookup_text);
                    Inc(path_search_elapsed_ms, Int64(nc_monotonic_tick_ms - phase_start_tick));
                end;

                if m_config.enable_segment_candidates and
                    (input_syllable_count >= 5) and
                    (not has_internal_dangling_initial) and
                    (not all_initial_compact_query) and
                    (not trailing_prefix_rescue_applied) and
                    (not has_strong_long_sentence_prediction_local(raw_candidates)) and
                    build_segment_candidates_timed(segment_candidates, False) then
                begin
                    raw_candidates := merge_candidate_lists(raw_candidates, segment_candidates, 0);
                end;

                has_strong_long_sentence_prediction :=
                    has_strong_long_sentence_prediction_local(raw_candidates);
            end;
            filter_long_query_nonlexicon_complete_candidates(raw_candidates);
            if not (should_enable_long_sentence_exact_search_local and
                has_strong_long_sentence_prediction and
                (input_syllable_count >= 10)) then
            begin
                if not has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    merge_exact_long_full_lookup_candidates(raw_candidates,
                        lookup_text);
                end;
                merge_incomplete_trailing_prefix_full_lookup_candidates(raw_candidates,
                    lookup_text);
            end;
            apply_preferred_query_phrase_bonus_for_query(lookup_text, raw_candidates);
            apply_preferred_sentence_phrase_bonus(raw_candidates);
            if input_syllable_count >= c_long_sentence_full_path_min_syllables then
            begin
                sort_candidates_lightweight(raw_candidates);
            end
            else
            begin
                sort_candidates_timed(raw_candidates);
            end;
            reserve_fuzzy_complete_candidates_local(raw_candidates,
                get_candidate_limit);
            if should_enable_long_sentence_exact_search_local and
                has_strong_long_sentence_prediction and
                (input_syllable_count >= 10) then
            begin
                if not has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    ensure_long_sentence_exact_candidate_visible(raw_candidates);
                    ensure_long_sentence_partial_exact_candidate_visible(
                        raw_candidates);
                end;
            end
            else
            begin
                ensure_partial_fallback_visible(raw_candidates, get_candidate_limit);
                ensure_single_char_partial_visible(raw_candidates, get_candidate_limit,
                    single_char_partial_min_count);
                ensure_multi_char_partial_precedes_single_char_partial(raw_candidates);
                ensure_rich_head_single_char_partials_visible(raw_candidates);
                if not has_extendable_same_syllable_tail_local(lookup_text,
                    input_syllable_count) then
                begin
                    ensure_long_sentence_exact_candidate_visible(raw_candidates);
                    ensure_long_sentence_partial_exact_candidate_visible(
                        raw_candidates);
                    ensure_long_sentence_prefix_partial_visible(raw_candidates,
                        get_candidate_limit);
                end;
                ensure_strong_two_plus_one_partial_visible(raw_candidates,
                    get_candidate_limit);
                if not has_explicit_apostrophe_input then
                begin
                    ensure_relaxed_missing_apostrophe_head_partial_visible(
                        raw_candidates);
                    prefer_relaxed_missing_apostrophe_boundary_partial(raw_candidates);
                end;
            end;
            if should_trim_strong_long_sentence_candidates_local(raw_candidates) then
            begin
                trim_sorted_candidates_for_strong_long_sentence(raw_candidates);
            end;
        end;

        if has_explicit_apostrophe_input and (input_syllable_count > 1) then
        begin
            filter_explicit_apostrophe_single_char_complete_candidates(raw_candidates);
        end;
        filter_non_bmp_single_char_candidates(raw_candidates);

        limit := get_total_candidate_limit;
        if m_config.enable_segment_candidates then
        begin
            if get_candidate_limit * c_candidate_total_expand_factor > limit then
            begin
                limit := get_candidate_limit * c_candidate_total_expand_factor;
            end;
            if limit > c_candidate_total_limit_max then
            begin
                limit := c_candidate_total_limit_max;
            end;
        end;
        if raw_from_dictionary and has_multi_syllable_input then
        begin
            multi_syllable_cap_limit := get_candidate_limit * 3;
            if count_single_char_partial_candidates(raw_candidates) + get_candidate_limit >
                multi_syllable_cap_limit then
            begin
                multi_syllable_cap_limit :=
                    count_single_char_partial_candidates(raw_candidates) + get_candidate_limit;
            end;
            if multi_syllable_cap_limit > c_candidate_total_limit_max then
            begin
                multi_syllable_cap_limit := c_candidate_total_limit_max;
            end;
            if (multi_syllable_cap_limit > 0) and (limit > multi_syllable_cap_limit) then
            begin
                limit := multi_syllable_cap_limit;
            end;
        end;

        ensure_redup_complete_candidate_visible(raw_candidates, limit);
        reserve_fuzzy_complete_candidates_local(raw_candidates,
            get_candidate_limit);

        if Length(raw_candidates) > limit then
        begin
            SetLength(m_candidates, limit);
            for i := 0 to limit - 1 do
            begin
                m_candidates[i] := raw_candidates[i];
            end
        end
        else
        begin
            m_candidates := raw_candidates;
        end;

        merge_confirmed_prefix_user_extensions(m_candidates);
        if should_trim_strong_long_sentence_candidates_local(m_candidates) then
        begin
            trim_sorted_candidates_for_strong_long_sentence(m_candidates);
        end;
        final_has_strong_long_sentence_prediction :=
            has_strong_long_sentence_prediction_local(m_candidates);
        if (input_syllable_count > 8) and
            (not final_has_strong_long_sentence_prediction) then
        begin
            if not has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count) then
            begin
                merge_exact_long_full_lookup_candidates(m_candidates,
                    lookup_text);
            end;
            merge_incomplete_trailing_prefix_full_lookup_candidates(m_candidates,
                lookup_text);
        end;
        apply_user_penalties(lookup_text, m_candidates);
        prioritize_complete_phrase_matches(m_candidates);
        phase_start_tick := nc_monotonic_tick_ms;
        apply_two_single_exact_merge_penalty(m_candidates);
        apply_syllable_single_char_alignment_bonus(m_candidates);
        note_post_phase_elapsed(phase_start_tick);
        apply_shifted_boundary_longer_exact_penalty(m_candidates);
        apply_compact_long_phrase_bonus(m_candidates);
        apply_preferred_query_phrase_bonus_for_query(lookup_text, m_candidates);
        apply_preferred_sentence_phrase_bonus(m_candidates);
        if build_lexicon_longer_prefix_candidates(lookup_text,
            exact_head_partial_candidates) then
        begin
            m_candidates := merge_candidate_lists(m_candidates,
                exact_head_partial_candidates, 0);
        end;
        if final_has_strong_long_sentence_prediction then
        begin
            sort_candidates_lightweight(m_candidates);
        end
        else
        begin
            sort_candidates_timed(m_candidates);
        end;
        if has_explicit_apostrophe_input and (input_syllable_count > 1) then
        begin
            filter_complete_candidates_for_explicit_apostrophe_boundary(m_candidates, m_candidates);
        end;
        if final_has_strong_long_sentence_prediction then
        begin
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates, get_candidate_limit,
                single_char_partial_min_count);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            if input_syllable_count > 8 then
            begin
                ensure_long_sentence_exact_candidate_visible(m_candidates);
                ensure_long_sentence_partial_exact_candidate_visible(m_candidates);
                ensure_long_sentence_prefix_partial_visible(m_candidates, get_candidate_limit);
            end;
            ensure_short_exact_tail_completion_visible(m_candidates);
            ensure_strong_two_plus_one_partial_visible(m_candidates, get_candidate_limit);
        end
        else
        begin
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates, get_candidate_limit, single_char_partial_min_count);
            ensure_short_exact_tail_completion_visible(m_candidates);
            if input_syllable_count > 8 then
            begin
                ensure_long_sentence_exact_candidate_visible(m_candidates);
                ensure_long_sentence_partial_exact_candidate_visible(m_candidates);
                ensure_long_sentence_prefix_partial_visible(m_candidates, get_candidate_limit);
            end;
            ensure_strong_two_plus_one_partial_visible(m_candidates, get_candidate_limit);
            ensure_redup_complete_candidate_visible(m_candidates, get_candidate_limit);
            ensure_hard_single_char_partial_visible(m_candidates);
            ensure_multi_char_partial_precedes_single_char_partial(m_candidates);
            ensure_rich_head_single_char_partials_visible(m_candidates);
            if not has_explicit_apostrophe_input then
            begin
                ensure_relaxed_missing_apostrophe_head_partial_visible(m_candidates);
                prefer_relaxed_missing_apostrophe_boundary_partial(m_candidates);
            end;
            ensure_two_syllable_exact_phrase_precedes_matching_single_char_partial(m_candidates);
        end;
        ensure_best_lightweight_sentence_candidate_visible(m_candidates);
        ensure_short_exact_tail_completion_visible(m_candidates);
        if has_extendable_same_syllable_tail_local(lookup_text,
            input_syllable_count) and
            (not has_long_direct_chain_candidate) then
        begin
            if (input_syllable_count >= 10) and
                try_build_typo_normalized_prefix_candidate_local(
                stable_prefix_candidate, False, True) then
            begin
                SetLength(exact_head_partial_candidates, 1);
                exact_head_partial_candidates[0] := stable_prefix_candidate;
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
            end;
            finalize_modal_tail_sentence_candidate(m_candidates);
            finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
            promote_preferred_query_phrase_candidate_for_query(lookup_text,
                m_candidates);
            if not is_full_pinyin_key(lookup_text) then
            begin
                ensure_incomplete_tail_partial_precedes_completed_extension(
                    m_candidates);
            end;
            if not (is_full_pinyin_key(lookup_text) and
                (input_syllable_count >= 10)) then
            begin
                ensure_preferred_subspan_repair_candidate_visible(m_candidates);
            end;
            if has_long_direct_chain_candidate then
            begin
                SetLength(exact_head_partial_candidates, 1);
                exact_head_partial_candidates[0] := direct_chain_candidate;
                m_candidates := merge_candidate_lists(m_candidates,
                    exact_head_partial_candidates, 0);
                promote_candidate_to_front_local(m_candidates,
                    direct_chain_candidate);
                ensure_best_fixed_boundary_exact_chain_candidate_visible(
                    m_candidates);
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
                promote_full_query_exact_complete_phrase_local(m_candidates);
            end;
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d partialfinal=1 xtail=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(has_extendable_same_syllable_tail_local(lookup_text,
                input_syllable_count)),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if short_full_exact_query and (input_syllable_count <= 3) then
        begin
            if top_candidate_is_stable_non_modal_partial_local(m_candidates) then
            begin
                if try_finalize_fast_prefix_candidates_and_prepare_exit_local(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortpartialfast=1 prefqv=0 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)])) then
                begin
                    Exit;
                end;
            end;
            if input_syllable_count >= 3 then
            begin
                ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
                if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
                begin
                    finalize_candidates_and_prepare_exit(Format(
                        'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortptail=1 prefqv=0 allinit=%d',
                        [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                        Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                        Ord(runtime_phrase_added), Ord(runtime_redup_added),
                        Ord(all_initial_compact_query)]));
                    Exit;
                end;
            end;
            ensure_exact_dict_complete_candidate_visible(m_candidates);
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            ensure_best_user_complete_candidate_visible(m_candidates);
            ensure_best_exact_tail_complete_candidate_visible(m_candidates);
            ensure_preferred_subspan_repair_candidate_visible(m_candidates);
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            sort_candidates_lightweight(m_candidates);
            if has_complete_top_candidate_for_fast_exit_local(m_candidates) then
            begin
                finalize_candidates_and_prepare_exit(Format(
                    'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortfast=1 prefqv=0 allinit=%d',
                    [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                    Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                    Ord(runtime_phrase_added), Ord(runtime_redup_added),
                    Ord(all_initial_compact_query)]));
                Exit;
            end;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        infer_paths_for_front_full_length_complete_candidates(m_candidates);
        note_debug_helper_elapsed('tinfpath0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_inferred_full_length_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tbestinf0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_full_length_pathless_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tpathless0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tfqv0=[skip-long-partial]';
            end;
        end
        else
        begin
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
        end;
        note_debug_helper_elapsed('tfqv0', phase_start_tick);
        if try_finalize_stable_long_candidate_local('fqv0', False) then
        begin
            Exit;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_exact_dict_complete_candidate_visible(m_candidates);
        note_debug_helper_elapsed('texactd0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tb3210', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_user_complete_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tuser0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_best_exact_tail_complete_candidate_visible(m_candidates);
        end;
        note_debug_helper_elapsed('ttail0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            if should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info :=
                        m_last_full_path_debug_info +
                        ' tvisprep0=[skip-long-partial]';
                end;
            end
            else
            begin
                prepare_visible_complete_candidates_for_fast_exit_local(
                    m_candidates);
            end;
        end;
        note_debug_helper_elapsed('tvisprep0', phase_start_tick);
        if try_finalize_locked_long_postprocess_candidate_local(
            'prepostvis') then
        begin
            Exit;
        end;
        if try_finalize_stable_long_candidate_local('prepost', False) then
        begin
            Exit;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_preferred_subspan_repair_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tpref0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tb3211', phase_start_tick);
        sort_candidates_lightweight(m_candidates);
        if short_full_exact_query and (input_syllable_count <= 3) and
            has_complete_top_candidate_for_fast_exit_local(m_candidates) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d shortfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if top_candidate_has_preferred_subspan_coverage_for_fast_exit_local(
            m_candidates) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d fastrepair=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            has_complete_top_candidate_for_fast_exit_local(m_candidates) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d midfast=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        if should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
        begin
            subquery_sentence_promoted := False;
            subquery_sentence_missed := True;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tss=[skip-long-partial]';
            end;
        end
        else
        begin
            ensure_best_subquery_sentence_candidate_visible(m_candidates,
                subquery_sentence_promoted);
        end;
        note_debug_helper_elapsed('tss', phase_start_tick);
        if (not subquery_sentence_promoted) and
            should_record_subquery_sentence_miss_local(m_candidates) then
        begin
            subquery_sentence_missed := True;
        end;
        ensure_preferred_subspan_repair_candidate_visible(m_candidates);
        if subquery_sentence_promoted and
            (has_complete_top_candidate_for_fast_exit_local(m_candidates) or
            is_trusted_top_complete_candidate_for_early_exit(
            m_candidates)) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added),
                Ord(all_initial_compact_query)]));
            Exit;
        end;
        if not (subquery_sentence_promoted and
            is_trusted_top_complete_candidate_for_early_exit(
            m_candidates)) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_best_visible_complete_sentence_candidate_visible(m_candidates);
            note_debug_helper_elapsed('tvfull', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            if not (is_full_pinyin_key(lookup_text) and
                (input_syllable_count <= 8) and
                (not has_internal_dangling_initial) and
                (not has_explicit_apostrophe_input)) then
            begin
                if should_skip_expensive_long_partial_repair_local(
                    m_candidates) then
                begin
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            ' tfx=[skip-long-partial]';
                    end;
                end
                else
                begin
                    ensure_best_fixed_boundary_exact_chain_candidate_visible(
                        m_candidates);
                end;
            end;
            note_debug_helper_elapsed('tfx', phase_start_tick);
            phase_start_tick := nc_monotonic_tick_ms;
            ensure_best_exact_chunk_chain_complete_candidate_visible(m_candidates);
            mark_debug_target_candidate_stage_local(2048, m_candidates);
            note_debug_helper_elapsed('txcc', phase_start_tick);
            if not (is_full_pinyin_key(lookup_text) and
                (has_exact_complete_phrase_candidate_for_syllable_count(
                m_candidates, input_syllable_count) or
                has_natural_complete_segment_path_candidate_for_syllable_count(
                m_candidates, input_syllable_count))) then
            begin
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_exact_chunk_chain_partial_candidate_visible(m_candidates);
                mark_debug_target_candidate_stage_local(4096, m_candidates);
                note_debug_helper_elapsed('txcp', phase_start_tick);
                phase_start_tick := nc_monotonic_tick_ms;
                ensure_best_visible_partial_completion_candidate_visible(m_candidates);
                mark_debug_target_candidate_stage_local(8192, m_candidates);
                note_debug_helper_elapsed('tvpc', phase_start_tick);
            end;

            mark_debug_target_candidate_stage_local(16384, m_candidates);
            if try_finalize_stable_long_candidate_local('postchain', True) then
            begin
                Exit;
            end;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_exact_prefix_partial_precedes_suspicious_long_candidate(m_candidates);
        note_debug_helper_elapsed('tprefix0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_dict_exact_prefix_partial_visible(m_candidates);
        note_debug_helper_elapsed('tdictprefix0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(m_candidates);
        note_debug_helper_elapsed('tstrongprefix0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        finalize_modal_tail_sentence_candidate(m_candidates);
        note_debug_helper_elapsed('tmodal0', phase_start_tick);
        if is_full_pinyin_key(lookup_text) and (input_syllable_count >= 4) and
            (input_syllable_count <= 6) then
        begin
            ensure_best_short_exact_prefix_tail_candidate_visible(m_candidates);
            ensure_best_short_natural_split_candidate_visible(m_candidates);
            ensure_best_exact_chunk_chain_complete_candidate_visible(
                m_candidates);
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            if should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        ' tfqv1=[skip-long-partial]';
                end;
            end
            else
            begin
                ensure_full_query_exact_complete_candidate_visible(m_candidates);
            end;
        end;
        note_debug_helper_elapsed('tfqv1', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tb3212', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_strong_complete_candidate_precedes_long_exact_prefix_partial(m_candidates);
        note_debug_helper_elapsed('tstrongprefix1', phase_start_tick);
        if ((Length(m_candidates) = 0) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            input_syllable_count)) and
            (not has_exact_complete_phrase_candidate_for_syllable_count(
            m_candidates, input_syllable_count)) and
            (not has_natural_complete_segment_path_candidate_for_syllable_count(
            m_candidates, input_syllable_count)) then
        begin
            phase_start_tick := nc_monotonic_tick_ms;
            if should_skip_expensive_long_partial_repair_local(m_candidates) then
            begin
                if m_config.debug_mode then
                begin
                    m_last_full_path_debug_info := m_last_full_path_debug_info +
                        ' tfx2=[skip-long-partial]';
                end;
            end
            else
            begin
                ensure_best_fixed_boundary_exact_chain_candidate_visible(
                    m_candidates);
            end;
            note_debug_helper_elapsed('tfx2', phase_start_tick);
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_complete_extension_precedes_prefix_partial(m_candidates);
        note_debug_helper_elapsed('textend0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        finalize_modal_tail_sentence_candidate(m_candidates);
        note_debug_helper_elapsed('tmodal1', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
        note_debug_helper_elapsed('tlemodal0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        promote_preferred_query_phrase_candidate_for_query(lookup_text,
            m_candidates);
        note_debug_helper_elapsed('tprefq0', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
        end;
        note_debug_helper_elapsed('tfqv2', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tb3213', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if should_skip_repeated_subquery_sentence_solver_local(m_candidates) or
            should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
        begin
            subquery_sentence_promoted := False;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tss2=[skip-prev-miss]';
            end;
        end
        else
        begin
            ensure_best_subquery_sentence_candidate_visible(m_candidates,
                subquery_sentence_promoted);
        end;
        note_debug_helper_elapsed('tss2', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_best_visible_complete_sentence_candidate_visible(
                m_candidates);
        end;
        note_debug_helper_elapsed('tvfull3', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_best_supported_complete_candidate_visible(m_candidates);
        end;
        note_debug_helper_elapsed('tvsup3', phase_start_tick);
        ensure_strong_supported_complete_candidate_locked_visible(
            m_candidates);
        if try_finalize_stable_long_candidate_local('prewindow', True) then
        begin
            Exit;
        end;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_best_exact_window_repair_complete_candidate_visible(
                m_candidates);
            ensure_best_supported_complete_candidate_visible(m_candidates);
            ensure_strong_supported_complete_candidate_locked_visible(
                m_candidates);
            ensure_best_visible_complete_sentence_candidate_visible(
                m_candidates);
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
            ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
                m_candidates);
            finalize_modal_tail_sentence_candidate(m_candidates);
            finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
            ensure_best_supported_complete_candidate_visible(m_candidates);
            ensure_strong_supported_complete_candidate_locked_visible(
                m_candidates);
        end;
        if is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 10) and
            has_exact_complete_phrase_candidate_for_syllable_count(m_candidates,
            input_syllable_count) and
            top_candidate_has_natural_complete_segment_path_local(m_candidates) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d typofast=%d fqexact=%d stablepost=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added), 1,
                Ord(delayed_long_decode_mode),
                Ord(normalized_typo_long_query_fast_path),
                Ord(long_full_exact_query), Ord(all_initial_compact_query)]));
            Exit;
        end;
        if not (is_full_pinyin_key(lookup_text) and
            (input_syllable_count <= 8) and
            (not has_internal_dangling_initial) and
            (not has_explicit_apostrophe_input)) then
        begin
            ensure_best_exact_window_consensus_complete_candidate_visible(
                m_candidates);
        end;
        if is_full_pinyin_key(lookup_text) and
            (input_syllable_count >= 8) and
            (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            input_syllable_count) then
        begin
            finalize_stable_consensus_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d typofast=%d fqexact=%d stablecons=1 allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added), 1,
                Ord(delayed_long_decode_mode),
                Ord(normalized_typo_long_query_fast_path),
                Ord(long_full_exact_query), Ord(all_initial_compact_query)]));
            Exit;
        end;
        finalize_modal_tail_sentence_candidate(m_candidates);
        finalize_le_plus_modal_particle_sentence_candidate(m_candidates);
        ensure_best_oracle_backed_exact_complete_candidate_visible(
            m_candidates);
        if ensure_visible_strict_oracle_top_complete_candidate_visible(
            m_candidates) and long_full_exact_query and
            (input_syllable_count >= 10) and
            is_trusted_top_complete_candidate_for_early_exit(m_candidates) then
        begin
            finalize_candidates_and_prepare_exit(Format(
                'multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d lightweight=%d delayed=%d typofast=%d fqexact=%d allinit=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates),
                Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added), 1,
                Ord(delayed_long_decode_mode),
                Ord(normalized_typo_long_query_fast_path),
                Ord(long_full_exact_query), Ord(all_initial_compact_query)]));
            Exit;
        end;
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_long_exact_subspan_sentence_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tlexsub', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_resolved_sentence_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tvrs2', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_supported_complete_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tvsup2', phase_start_tick);
        ensure_strong_supported_complete_candidate_locked_visible(
            m_candidates);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_visible_complete_sentence_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tvfull2', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_full_query_exact_complete_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tfqv2', phase_start_tick);
        ensure_strong_supported_complete_candidate_locked_visible(
            m_candidates);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_three_syllable_contextual_two_plus_one_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tb321', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_exact_window_consensus_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tvcons2', phase_start_tick);
        ensure_incomplete_tail_partial_precedes_completed_extension(m_candidates);
        ensure_exact_backed_same_comment_partial_visible(m_candidates);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_oracle_backed_exact_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('toracle2', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_strict_oracle_exact_top_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tstricto', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_late_exact_chunk_consensus_complete_candidate_visible(
            m_candidates);
        note_debug_helper_elapsed('tlatexc', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_resolved_sentence_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tvrs3', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_best_resolved_sentence_candidate_visible_aggressive(
            m_candidates);
        note_debug_helper_elapsed('tvrsa', phase_start_tick);
        phase_start_tick := nc_monotonic_tick_ms;
        ensure_preferred_subspan_repair_candidate_visible(m_candidates);
        note_debug_helper_elapsed('tpref2', phase_start_tick);
        if should_skip_expensive_subquery_sentence_solver_local(m_candidates) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' tfqv3=[skip-long-partial]';
            end;
        end
        else
        begin
            ensure_full_query_exact_complete_candidate_visible(m_candidates);
        end;
        promote_full_query_exact_complete_phrase_local(m_candidates);
        ensure_high_quality_complete_candidate_in_top2(m_candidates);
        ensure_short_full_query_exact_cluster_visible(m_candidates);
        demote_low_evidence_admin_place_alias_top_local(m_candidates);
        finalize_lookup_timing_info;
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := Format('multi=%d seg=%d dangling=%d head_only=%d runtime=%d redup=%d',
                [Ord(has_multi_syllable_input), Ord(has_segment_candidates), Ord(has_internal_dangling_initial),
                Ord(head_only_multi_syllable), Ord(runtime_phrase_added), Ord(runtime_redup_added)]) +
                Format(' allinit=%d', [Ord(all_initial_compact_query)]);
            if m_last_three_syllable_partial_preference_kind > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' b3pref=%d', [m_last_three_syllable_partial_preference_kind]);
            end;
            if m_last_three_syllable_partial_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_three_syllable_partial_debug_info;
            end;
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
        end;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
        Exit;
    end;

        SetLength(m_candidates, 1);
        if m_composition_display_text <> '' then
        begin
            m_candidates[0].text := m_composition_display_text;
        end
        else
        begin
            m_candidates[0].text := m_composition_text;
        end;
        m_candidates[0].comment := fallback_comment;
        m_candidates[0].score := 0;
        m_candidates[0].source := cs_rule;
        m_candidates[0].has_dict_weight := False;
        m_candidates[0].dict_weight := 0;
        if has_multi_syllable_input then
        begin
            ensure_bounded_single_char_partial_pool(m_candidates);
            ensure_partial_fallback_visible(m_candidates, get_candidate_limit);
            ensure_single_char_partial_visible(m_candidates, get_candidate_limit,
                single_char_partial_min_count);
            ensure_best_first_syllable_single_partial_visible(m_candidates, 1);
        end;
        finalize_lookup_timing_info;
        if m_config.debug_mode then
        begin
            m_last_lookup_debug_extra := Format('multi=%d seg=0 dangling=%d head_only=%d runtime=%d redup=%d fallback=1',
                [Ord(has_multi_syllable_input), Ord(has_internal_dangling_initial), Ord(head_only_multi_syllable),
                Ord(runtime_phrase_added), Ord(runtime_redup_added)]) +
                Format(' allinit=%d', [Ord(all_initial_compact_query)]);
            if m_last_full_path_debug_info <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    m_last_full_path_debug_info;
            end;
            if Length(m_candidates) > 0 then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ' +
                    get_candidate_debug_summary(m_candidates[0]);
            end;
        end;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    end;

begin
    if (m_short_exact_pair_tail_query <> '') and
        (not SameText(normalize_pinyin_text(m_composition_text),
        m_short_exact_pair_tail_query)) then
    begin
        clear_short_exact_pair_tail_handoff;
    end;
    reset_debug_target_recall_metrics;
    { Nested prefix/chain helpers may consult this cache on an early exit path. }
    exact_subspan_cache_prepared := False;
    head_only_multi_syllable := False;
    subspan_oracle_states := nil;
    subspan_oracle_texts := nil;
    subspan_oracle_scores := nil;
    subspan_oracle_engine := nil;
    subspan_oracle_budget_remaining := 0;
    input_syllable_count := 0;
    SetLength(literal_user_candidates, 0);
    SetLength(incremental_partial_candidates, 0);
    preferred_exact_candidate_cache := nil;
    preferred_exact_syllable_count_cache := nil;
    preferred_exact_state_cache := nil;
    preferred_query_phrase_bonus_cache := nil;
    preferred_query_phrase_text_cache := nil;
    preferred_query_phrase_text_bonus_cache := nil;
    learned_query_choice_override_cache := nil;
    stronger_query_choice_competitor_cache := nil;
    lookup_cache := m_build_lookup_cache;
    if lookup_cache = nil then
    begin
        lookup_cache := TDictionary<string, TncCandidateList>.Create;
        m_build_lookup_cache := lookup_cache;
    end;
    try
        preferred_exact_candidate_cache := TDictionary<string, TncCandidate>.Create;
        preferred_exact_syllable_count_cache := TDictionary<string, Integer>.Create;
        preferred_exact_state_cache := TDictionary<string, Byte>.Create;
        preferred_query_phrase_bonus_cache := TDictionary<string, Integer>.Create;
        preferred_query_phrase_text_cache := TDictionary<string, string>.Create;
        preferred_query_phrase_text_bonus_cache := TDictionary<string, Integer>.Create;
        learned_query_choice_override_cache := TDictionary<string, Byte>.Create;
        stronger_query_choice_competitor_cache := TDictionary<string, Byte>.Create;
        // Early returns stay inside generation. All successful paths share the
        // same evidence/ownership boundary before the display stages see them.
        generate_candidates_local;
        reconcile_short_model_candidates_local;
        inject_literal_user_candidates_local;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
        m_page_index := 0;
        m_selected_index := 0;
    finally
        stronger_query_choice_competitor_cache.Free;
        learned_query_choice_override_cache.Free;
        preferred_query_phrase_text_bonus_cache.Free;
        preferred_query_phrase_text_cache.Free;
        preferred_query_phrase_bonus_cache.Free;
        preferred_exact_state_cache.Free;
        preferred_exact_syllable_count_cache.Free;
        preferred_exact_candidate_cache.Free;
        subspan_oracle_engine.Free;
        subspan_oracle_scores.Free;
        subspan_oracle_texts.Free;
        subspan_oracle_states.Free;
    end;
end;

{$IFDEF FPC}
{$OPTIMIZATION CSE}
{$ENDIF}
procedure TncEngine.apply_long_complete_candidate_pool(
    var candidates: TncCandidateList;
    var source_indices: TArray<Integer>;
    const normalized_query: string;
    const query_syllables: TncPinyinParseResult;
    const expected_units: Integer);
const
    c_pool_limit = 32;
    c_raw_pool_limit = 192;
    c_existing_probe_limit = 64;
    c_pair_seed_limit = 6;
    c_pair_candidate_limit = 24;
    c_exact_lattice_beam_width = 8;
    c_exact_lattice_candidate_limit = 8;
    c_exact_lattice_word_option_limit = 6;
    c_exact_lattice_single_option_limit = 8;
    c_exact_lattice_max_segment_units = 5;
    c_exact_lattice_signature_limit = 3;
    c_exact_edge_model_lattice_beam_width = 12;
    c_exact_edge_model_lattice_candidate_limit = 16;
    c_exact_edge_model_lattice_option_limit = 10;
    c_exact_edge_model_lattice_single_option_limit = 4;
    c_exact_edge_model_min_anchor_units = 3;
    c_exact_edge_model_min_anchor_score = 2.5;
    c_retained_exact_lattice_probe_limit = 16;
    c_exact_anchor_candidate_limit = 32;
    c_exact_anchor_best_candidate_limit = 24;
    c_retained_exact_anchor_candidate_limit = 12;
    c_exact_anchor_seed_limit = 2;
    c_exact_anchor_options_per_position = 4;
    c_exact_anchor_word_option_limit = 4;
    c_exact_dual_anchor_candidate_limit = 12;
    c_exact_dual_anchor_pair_pool_limit = 36;
    c_retained_exact_subpath_width = 3;
    c_retained_exact_subpath_option_limit = 4;
    c_retained_exact_anchor_paths_per_edge = 3;
    c_single_option_limit = 6;
    c_signature_soft_limit = 4;
    c_chain_reserved = 6;
    c_pair_reserved = 6;
    c_local_reserved = 12;
    c_proper_name_reserved = 2;
    c_exact_lattice_reserved = 8;
    c_exact_edge_chain_reserved = 2;
    c_visible_complete_limit = 2;
    c_min_lm_pair_weight = 390;
    c_min_query_pair_weight = 420;
    c_min_corroborated_pair_weight = 360;
    c_min_strong_word_lm_bonus = 360;
    c_query_path_bonus_cap = 420;
type
    TncCompletePoolLatticeState = record
        score: Int64;
        text: string;
        signature_low: UInt64;
        signature_high: UInt64;
        predecessor_position: Integer;
        predecessor_state_index: Integer;
        segment_text: string;
        previous_previous_text: string;
        previous_text: string;
        previous_previous_edge_id: UInt32;
        previous_edge_id: UInt32;
        previous_previous_units: Integer;
        previous_units: Integer;
        segment_count: Integer;
        transition_support: Integer;
        retained_edge_count: Integer;
        retained_bonus_total: Int64;
        retained_anchor_start: Integer;
        retained_anchor_units: Integer;
        retained_anchor_exact_rank: Integer;
        retained_anchor_top_weight: Integer;
        model_anchor_count: Integer;
        model_anchor_score_total: Double;
        model_anchor_score_max: Double;
        model_word_count: Integer;
        model_word_score_total: Double;
        model_word_score_min: Double;
        model_word_score_max: Double;
    end;
    TncCompletePoolLatticeStateArray =
        TArray<TncCompletePoolLatticeState>;
    TncCompletePoolAnchorProposal = record
        valid: Boolean;
        span_units: Integer;
        exact_rank: Integer;
        source_weight: Integer;
        top_weight: Integer;
        confidence: Int64;
        candidate: TncCandidate;
    end;
    TncCompletePoolAnchorProposalArray =
        TArray<TncCompletePoolAnchorProposal>;
    TncCompletePoolAnchorProposalMatrix =
        TArray<TncCompletePoolAnchorProposalArray>;
    TncCompletePoolDualAnchorProposal = record
        valid: Boolean;
        left_position: Integer;
        right_position: Integer;
        confidence: Int64;
        left_anchor: TncCompletePoolAnchorProposal;
        right_anchor: TncCompletePoolAnchorProposal;
    end;
    TncCompletePoolDualAnchorProposalArray =
        TArray<TncCompletePoolDualAnchorProposal>;
    TncExactSubpathState = record
        valid: Boolean;
        score: Int64;
        segment_count: Integer;
        text: string;
        path_text: string;
        first_segment_text: string;
        last_segment_text: string;
        first_segment_units: Integer;
        last_segment_units: Integer;
        transition_support: Integer;
    end;
    TncExactSubpathStateArray = TArray<TncExactSubpathState>;
    TncExactSubpathStateMatrix = TArray<TncExactSubpathStateArray>;
    TncBooleanArray = TArray<Boolean>;
    TncBooleanMatrix = TArray<TncBooleanArray>;
var
    raw_pool: TncLongCompletePoolRuntimeCandidateArray;
    baseline_raw_pool: TncLongCompletePoolRuntimeCandidateArray;
    full_raw_pool: TncLongCompletePoolRuntimeCandidateArray;
    raw_index_by_text: TDictionary<string, Integer>;
    baseline_index_by_text: TDictionary<string, Integer>;
    selected_indices: TArray<Integer>;
    selected_texts: TDictionary<string, Boolean>;
    selected_signature_counts: TDictionary<string, Integer>;
    signature_totals: TDictionary<string, Integer>;
    exact_cache: TDictionary<string, TncCandidateList>;
    word_lm_cache: TDictionary<string, Integer>;
    lattice_pair_transition_cache: TDictionary<UInt64, Integer>;
    lattice_trigram_transition_cache: TDictionary<UInt64, Integer>;
    exact_edge_model_score_cache: TDictionary<UInt64, Double>;
    span_key_cache: TArray<TArray<string>>;
    original_candidates: TncCandidateList;
    original_source_indices: TArray<Integer>;
    internal_candidates: TncCandidateList;
    internal_source_indices: TArray<Integer>;
    baseline_candidates: TncCandidateList;
    baseline_source_indices: TArray<Integer>;
    baseline_top1: string;
    baseline_top2: string;
    original_visible_count: Integer;
    raw_original_count: Integer;
    pair_candidate_count: Integer;
    idx: Integer;
    raw_idx: Integer;
    chain_idx: Integer;
    source_idx: Integer;
    candidate_value: TncCandidate;
    char_texts: TArray<string>;
    char_scores: TArray<Integer>;
    char_suffix_scores: TArray<Integer>;
    char_context_scores: TArray<Integer>;
    missing_char_raw_indices: TArray<Integer>;
    missing_char_texts: TArray<string>;
    missing_char_scores: TArray<Integer>;
    missing_char_suffix_scores: TArray<Integer>;
    missing_char_context_scores: TArray<Integer>;
    missing_char_count: Integer;
    missing_char_idx: Integer;
    char_scores_ok: Boolean;
    previous_previous_text: string;
    previous_text: string;
    left_context: string;
    has_left_context: Boolean;
    sorted_indices: TArray<Integer>;
    merged_candidates: TncCandidateList;
    merged_sources: TArray<Integer>;
    merged_count: Integer;
    complete_count: Integer;
    emitted_candidate_identities: TDictionary<string, Boolean>;
    pairwise_best_idx: Integer;
    pairwise_best_score: Double;
    pairwise_insert_rank: Integer;
    pool_phase_tick: Int64;
    pool_phase_frequency: Int64;
    chain_source_kind: TncLongCompletePoolSource;

    procedure note_pool_phase(const phase_name: string);
    var
        current_tick: Int64;
        elapsed_us: Int64;
    begin
        if not m_config.debug_mode then
        begin
            Exit;
        end;
        QueryPerformanceCounter(current_tick);
        elapsed_us := ((current_tick - pool_phase_tick) * 1000000) div
            pool_phase_frequency;
        pool_phase_tick := current_tick;
        if elapsed_us <= 0 then
        begin
            Exit;
        end;
        if m_last_lookup_debug_extra <> '' then
        begin
            m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
        end;
        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
            Format('lcpus_%s=%d', [phase_name, elapsed_us]);
    end;

    function clamp_int64_to_integer(const value: Int64): Integer;
    begin
        if value > High(Integer) then
        begin
            Exit(High(Integer));
        end;
        if value < Low(Integer) then
        begin
            Exit(Low(Integer));
        end;
        Result := value;
    end;

    function candidate_raw_weight(const value: TncCandidate): Integer;
    begin
        Result := value.score;
        if value.has_dict_weight then
        begin
            Result := Max(Result, value.dict_weight);
        end;
        if value.source = cs_user then
        begin
            Result := Max(Result, 4096);
        end;
    end;

    function candidate_is_complete(const value: TncCandidate): Boolean;
    begin
        Result := (Trim(value.text) <> '') and
            (Trim(value.comment) = '') and
            (get_candidate_text_unit_count(Trim(value.text)) = expected_units);
    end;

    function build_span_key(const start_index: Integer;
        const span_length: Integer): string;
    var
        syllable_idx: Integer;
    begin
        Result := '';
        if (start_index < 0) or (span_length <= 0) or
            (start_index + span_length > Length(query_syllables)) then
        begin
            Exit;
        end;
        if Length(span_key_cache) <> Length(query_syllables) then
        begin
            SetLength(span_key_cache, Length(query_syllables));
            for syllable_idx := 0 to High(span_key_cache) do
            begin
                SetLength(span_key_cache[syllable_idx],
                    Length(query_syllables) - syllable_idx + 1);
            end;
        end;
        if span_key_cache[start_index][span_length] <> '' then
        begin
            Exit(span_key_cache[start_index][span_length]);
        end;
        for syllable_idx := start_index to
            start_index + span_length - 1 do
        begin
            Result := Result + normalize_pinyin_text(
                query_syllables[syllable_idx].text);
        end;
        span_key_cache[start_index][span_length] := Result;
    end;

    function load_exact_candidates(const query_key: string;
        out values: TncCandidateList): Boolean;
    var
        cache_key: string;
        shared_cache_key: string;
        cached_values: TncCandidateList;
    begin
        SetLength(values, 0);
        cache_key := query_key;
        if exact_cache.TryGetValue(cache_key, values) then
        begin
            Exit(Length(values) > 0);
        end;
        shared_cache_key := '#exact#' + query_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(shared_cache_key,
            cached_values) then
        begin
            values := cached_values;
        end
        else if not m_dictionary.lookup_exact_full_pinyin(query_key, values) then
        begin
            SetLength(values, 0);
        end;
        if (m_build_lookup_cache <> nil) and
            (not m_build_lookup_cache.ContainsKey(shared_cache_key)) then
        begin
            m_build_lookup_cache.Add(shared_cache_key,
                values);
        end;
        exact_cache.AddOrSetValue(cache_key,
            values);
        Result := Length(values) > 0;
    end;

    function find_exact_candidate(const query_key: string;
        const text_value: string; const unit_count: Integer;
        out exact_candidate: TncCandidate): Boolean;
    var
        values: TncCandidateList;
        value_idx: Integer;
    begin
        Result := False;
        exact_candidate := Default(TncCandidate);
        if not load_exact_candidates(query_key, values) then
        begin
            Exit;
        end;
        for value_idx := 0 to High(values) do
        begin
            if not long_exact_chunk_weight_is_eligible(
                values[value_idx].dict_weight,
                values[value_idx].has_dict_weight) then
            begin
                Continue;
            end;
            if (Trim(values[value_idx].comment) = '') and
                (get_candidate_text_unit_count(
                Trim(values[value_idx].text)) = unit_count) and
                SameText(Trim(values[value_idx].text), Trim(text_value)) then
            begin
                exact_candidate := values[value_idx];
                Exit(True);
            end;
        end;
    end;

    function infer_complete_pool_exact_path(const candidate_text: string;
        out encoded_path: string): Boolean;
    type
        TncCompletePoolExactPathState = record
            valid: Boolean;
            score: Int64;
            segment_count: Integer;
            path_text: string;
        end;
    const
        c_max_exact_segment_units = 5;
    var
        units: TArray<string>;
        states: TArray<TncCompletePoolExactPathState>;
        candidate_state: TncCompletePoolExactPathState;
        exact_candidate: TncCandidate;
        segment_text: string;
        query_key: string;
        unit_idx: Integer;
        segment_idx: Integer;
        segment_units: Integer;
        next_idx: Integer;
        exact_weight: Integer;
    begin
        Result := False;
        encoded_path := '';
        units := split_text_units(Trim(candidate_text));
        if Length(units) <> expected_units then
        begin
            Exit;
        end;

        SetLength(states, expected_units + 1);
        states[expected_units].valid := True;
        states[expected_units].score := 0;
        states[expected_units].segment_count := 0;
        states[expected_units].path_text := '';
        for unit_idx := expected_units - 1 downto 0 do
        begin
            states[unit_idx].valid := False;
            for segment_units := 1 to Min(c_max_exact_segment_units,
                expected_units - unit_idx) do
            begin
                next_idx := unit_idx + segment_units;
                if not states[next_idx].valid then
                begin
                    Continue;
                end;
                segment_text := '';
                for segment_idx := unit_idx to next_idx - 1 do
                begin
                    segment_text := segment_text + units[segment_idx];
                end;
                query_key := build_span_key(unit_idx, segment_units);
                if (query_key = '') or (not find_exact_candidate(query_key,
                    segment_text, segment_units, exact_candidate)) then
                begin
                    Continue;
                end;

                exact_weight := candidate_raw_weight(exact_candidate);
                candidate_state.valid := True;
                candidate_state.score := states[next_idx].score +
                    exact_weight + Int64(segment_units * segment_units) * 1200;
                candidate_state.segment_count :=
                    states[next_idx].segment_count + 1;
                candidate_state.path_text := segment_text;
                if states[next_idx].path_text <> '' then
                begin
                    candidate_state.path_text := candidate_state.path_text +
                        c_segment_path_separator + states[next_idx].path_text;
                end;
                if (not states[unit_idx].valid) or
                    (candidate_state.score > states[unit_idx].score) or
                    ((candidate_state.score = states[unit_idx].score) and
                    (candidate_state.segment_count <
                    states[unit_idx].segment_count)) then
                begin
                    states[unit_idx] := candidate_state;
                end;
            end;
        end;
        if states[0].valid then
        begin
            encoded_path := states[0].path_text;
            Result := encoded_path <> '';
        end;
    end;

    function decode_and_validate_path(const candidate_text: string;
        const encoded_path: string; out signature: string;
        out name_confidence: Integer): Boolean;
    var
        parts: TArray<string>;
        part_units: TArray<string>;
        exact_candidate: TncCandidate;
        reconstructed: string;
        query_key: string;
        part_idx: Integer;
        unit_cursor: Integer;
        part_length: Integer;
        first_unit: string;
    begin
        Result := False;
        signature := '';
        name_confidence := 0;
        reconstructed := '';
        unit_cursor := 0;
        if Trim(encoded_path) = '' then
        begin
            Exit;
        end;
        parts := encoded_path.Split([c_segment_path_separator]);
        if Length(parts) <= 0 then
        begin
            Exit;
        end;
        for part_idx := 0 to High(parts) do
        begin
            parts[part_idx] := Trim(parts[part_idx]);
            part_units := split_text_units(parts[part_idx]);
            part_length := Length(part_units);
            if (part_length <= 0) or
                (unit_cursor + part_length > expected_units) then
            begin
                Exit;
            end;
            query_key := build_span_key(unit_cursor, part_length);
            if (query_key = '') or
                (not find_exact_candidate(query_key, parts[part_idx],
                part_length, exact_candidate)) then
            begin
                Exit;
            end;
            if signature <> '' then
            begin
                signature := signature + '-';
            end;
            signature := signature + IntToStr(part_length);
            reconstructed := reconstructed + parts[part_idx];
            if (part_length in [2, 3]) and
                (Length(part_units) > 0) then
            begin
                first_unit := part_units[0];
                if is_common_surname_text(first_unit) then
                begin
                    name_confidence := Max(name_confidence,
                        620 + part_length * 80);
                end;
            end;
            Inc(unit_cursor, part_length);
        end;
        Result := (unit_cursor = expected_units) and
            SameText(reconstructed, Trim(candidate_text));
    end;

    function decode_trusted_exact_path(const candidate_text: string;
        const encoded_path: string; out signature: string;
        out name_confidence: Integer): Boolean;
    var
        parts: TArray<string>;
        part_units: TArray<string>;
        reconstructed: string;
        part_idx: Integer;
        unit_cursor: Integer;
        part_length: Integer;
        first_unit: string;
    begin
        Result := False;
        signature := '';
        name_confidence := 0;
        reconstructed := '';
        unit_cursor := 0;
        if Trim(encoded_path) = '' then
        begin
            Exit;
        end;
        parts := encoded_path.Split([c_segment_path_separator]);
        if Length(parts) <= 0 then
        begin
            Exit;
        end;
        for part_idx := 0 to High(parts) do
        begin
            parts[part_idx] := Trim(parts[part_idx]);
            part_units := split_text_units(parts[part_idx]);
            part_length := Length(part_units);
            if (part_length <= 0) or
                (unit_cursor + part_length > expected_units) then
            begin
                Exit;
            end;
            if signature <> '' then
            begin
                signature := signature + '-';
            end;
            signature := signature + IntToStr(part_length);
            reconstructed := reconstructed + parts[part_idx];
            if part_length in [2, 3] then
            begin
                first_unit := part_units[0];
                if is_common_surname_text(first_unit) then
                begin
                    name_confidence := Max(name_confidence,
                        620 + part_length * 80);
                end;
            end;
            Inc(unit_cursor, part_length);
        end;
        Result := (unit_cursor = expected_units) and
            SameText(reconstructed, Trim(candidate_text));
    end;

    function resolve_candidate_path(const value: TncCandidate;
        const candidate_source_index: Integer; const preferred_path: string;
        out resolved_path: string; out signature: string;
        out name_confidence: Integer): Boolean;
    var
        inferred_score: Integer;
        inferred_segments: Integer;
    begin
        resolved_path := Trim(preferred_path);
        if (resolved_path = '') and (candidate_source_index >= 0) and
            (candidate_source_index < Length(m_candidates)) then
        begin
            resolved_path := Trim(get_segment_path_for_candidate(
                m_candidates[candidate_source_index], candidate_source_index));
        end;
        if resolved_path = '' then
        begin
            resolved_path := Trim(get_segment_path_for_candidate(value,
                candidate_source_index));
        end;
        if not decode_and_validate_path(Trim(value.text), resolved_path,
            signature, name_confidence) then
        begin
            resolved_path := Trim(infer_segment_path_for_query_text_with_score(
                normalized_query, Trim(value.text), inferred_score,
                inferred_segments, True));
        end;
        if not decode_and_validate_path(Trim(value.text), resolved_path,
            signature, name_confidence) then
        begin
            infer_complete_pool_exact_path(Trim(value.text), resolved_path);
        end;
        Result := decode_and_validate_path(Trim(value.text), resolved_path,
            signature, name_confidence);
    end;

    function source_priority(const value: TncLongCompletePoolSource): Integer;
    begin
        case value of
            lcps_visible: Result := 6;
            lcps_proper_name: Result := 5;
            lcps_chain: Result := 4;
            lcps_exact_lattice: Result := 4;
            lcps_exact_edge_chain: Result := 4;
            lcps_exact_pair: Result := 3;
            lcps_existing: Result := 2;
            lcps_local_repair: Result := 1;
        else
            Result := 0;
        end;
    end;

    procedure add_candidate(const value: TncCandidate;
        const candidate_source_index: Integer; const preferred_path: string;
        const source_kind: TncLongCompletePoolSource;
        const seed_rank: Integer; const seed_visible_index: Integer;
        const original: Boolean; const substitutions: Integer;
        const changed_position: Integer; const source_char_weight: Integer;
        const replacement_char_weight: Integer;
        const pair_evidence: Integer; const anchor_start: Integer;
        const anchor_units: Integer;
        const anchor_exact_rank: Integer;
        const anchor_top_weight: Integer;
        const baseline_eligible: Boolean); forward;

    function candidate_text_is_edge_only(const candidate_text: string): Boolean;
    var
        chain_candidate_idx: Integer;
        retained_match: Boolean;
    begin
        retained_match := False;
        for chain_candidate_idx := 0 to
            High(m_runtime_long_chain_candidates) do
        begin
            if not SameText(Trim(m_runtime_long_chain_candidates[
                chain_candidate_idx].text), Trim(candidate_text)) then
            begin
                Continue;
            end;
            if m_runtime_long_chain_candidates[
                chain_candidate_idx].retained_exact_edge_count <= 0 then
            begin
                Exit(False);
            end;
            retained_match := True;
        end;
        Result := retained_match;
    end;

    function cached_lm_transition_bonus(const query_value: string;
        const path_value: string): Integer;
    var
        cache_key: string;
    begin
        Result := 0;
        if (query_value = '') or (path_value = '') then
        begin
            Exit;
        end;
        cache_key := '#lm#' + query_value + #1 + path_value;
        if word_lm_cache.TryGetValue(cache_key, Result) then
        begin
            Exit;
        end;
        Result := m_dictionary.get_lm_transition_bonus(query_value,
            path_value);
        word_lm_cache.Add(cache_key, Result);
    end;

    function cached_path_transition_bonus(const query_value: string;
        const path_value: string): Integer;
    var
        cache_key: string;
        query_path_bonus: Integer;
    begin
        Result := 0;
        if (query_value = '') or (path_value = '') then
        begin
            Exit;
        end;
        cache_key := '#transition#' + query_value + #1 + path_value;
        if word_lm_cache.TryGetValue(cache_key, Result) then
        begin
            Exit;
        end;
        Result := cached_lm_transition_bonus(query_value, path_value);
        if Result < c_query_path_bonus_cap then
        begin
            query_path_bonus := m_dictionary.get_long_query_segment_path_bonus(
                query_value, path_value);
            Result := Max(Result, query_path_bonus);
        end;
        word_lm_cache.Add(cache_key, Result);
    end;

    function exact_edge_model_cache_key(const position_value,
        span_units_value, exact_rank_value: Integer): UInt64;
    begin
        Result := (UInt64(Cardinal(position_value)) shl 32) or
            (UInt64(Cardinal(span_units_value)) shl 24) or
            UInt64(Cardinal(exact_rank_value));
    end;

    procedure collect_model_retained_exact_edges;
    const
        c_edge_limit = 4;
        c_option_limit = 16;
        c_min_model_score = 2.5;
        c_min_model_margin = 0.0;
    type
        TEdgeProposal = record
            valid: Boolean;
            model_score: Double;
            pair_rate: Double;
            edge: TncLongRetainedExactEdge;
        end;
        TEdgeProposalArray = TArray<TEdgeProposal>;
    var
        proposals: TEdgeProposalArray;
        best: TEdgeProposal;
        second_score: Double;
        swap_value: TEdgeProposal;
        exact_candidates: TncCandidateList;
        candidate_value_local: TncCandidate;
        candidate_text_local: string;
        query_key: string;
        position: Integer;
        span_units: Integer;
        candidate_idx: Integer;
        option_count: Integer;
        raw_weight: Integer;
        top_weight: Integer;
        retention_score: Int64;
        features: TncLongExactEdgeLatticeFeatures;
        model_score: Double;
        proposal_idx: Integer;
        insert_idx: Integer;
        model_cache_key: UInt64;
    begin
        SetLength(proposals, 0);
        for position := 0 to expected_units - 2 do
        begin
            best := Default(TEdgeProposal);
            best.model_score := -MaxDouble;
            second_score := -MaxDouble;
            for span_units := 3 to Min(c_exact_lattice_max_segment_units,
                expected_units - position) do
            begin
                query_key := build_span_key(position, span_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key,
                    exact_candidates)) then
                begin
                    Continue;
                end;
                option_count := 0;
                top_weight := 0;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    candidate_value_local := exact_candidates[candidate_idx];
                    candidate_text_local := Trim(
                        candidate_value_local.text);
                    if (Trim(candidate_value_local.comment) <> '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        span_units) then
                    begin
                        Continue;
                    end;
                    Inc(option_count);
                    raw_weight := candidate_raw_weight(
                        candidate_value_local);
                    if option_count = 1 then
                    begin
                        top_weight := raw_weight;
                    end;
                    if option_count > c_option_limit then
                    begin
                        Break;
                    end;
                    retention_score := long_edge_retention_score(
                        option_count, raw_weight, top_weight,
                        candidate_value_local.score, span_units, position,
                        expected_units,
                        candidate_value_local.source = cs_user);
                    build_long_exact_edge_lattice_features(query_key,
                        candidate_text_local, span_units, option_count,
                        raw_weight, top_weight,
                        candidate_value_local.score,
                        clamp_int64_to_integer(retention_score), position,
                        expected_units,
                        option_count <= c_exact_lattice_word_option_limit,
                        features);
                    model_score := long_exact_edge_lattice_ranker_score(
                        features);
                    model_cache_key := exact_edge_model_cache_key(position,
                        span_units, option_count);
                    exact_edge_model_score_cache.AddOrSetValue(
                        model_cache_key, model_score);
                    if model_score > best.model_score then
                    begin
                        second_score := best.model_score;
                        best.valid := True;
                        best.model_score := model_score;
                        best.pair_rate := features[11];
                        best.edge.position := position;
                        best.edge.chunk_units := span_units;
                        best.edge.exact_rank := option_count;
                        best.edge.raw_weight := raw_weight;
                        best.edge.top_weight := top_weight;
                        best.edge.confidence := Round(model_score * 100000.0) +
                            Int64(span_units) * 1000 - option_count * 100;
                        best.edge.query_key := query_key;
                        best.edge.text := candidate_text_local;
                    end
                    else if model_score > second_score then
                    begin
                        second_score := model_score;
                    end;
                end;
            end;
            if best.valid and (best.model_score >= c_min_model_score) and
                ((best.model_score - second_score) >=
                c_min_model_margin) then
            begin
                SetLength(proposals, Length(proposals) + 1);
                proposals[High(proposals)] := best;
            end;
        end;

        for proposal_idx := 1 to High(proposals) do
        begin
            swap_value := proposals[proposal_idx];
            insert_idx := proposal_idx - 1;
            while (insert_idx >= 0) and
                (swap_value.edge.confidence >
                proposals[insert_idx].edge.confidence) do
            begin
                proposals[insert_idx + 1] := proposals[insert_idx];
                Dec(insert_idx);
            end;
            proposals[insert_idx + 1] := swap_value;
        end;
        if Length(proposals) > c_edge_limit then
        begin
            SetLength(proposals, c_edge_limit);
        end;
        SetLength(m_runtime_long_retained_exact_edges,
            Length(proposals));
        for proposal_idx := 0 to High(proposals) do
        begin
            m_runtime_long_retained_exact_edges[proposal_idx] :=
                proposals[proposal_idx].edge;
        end;
    end;

    procedure generate_exact_lattice_candidates(
        const use_retained_exact_edges: Boolean;
        const use_edge_model: Boolean);
    var
        states_by_position: TArray<TncCompletePoolLatticeStateArray>;
        state_counts_by_position: TArray<Integer>;
        source_states: TncCompletePoolLatticeStateArray;
        destination_states: TncCompletePoolLatticeStateArray;
        retained_states: TncCompletePoolLatticeStateArray;
        exact_candidates: TncCandidateList;
        state_value: TncCompletePoolLatticeState;
        next_state: TncCompletePoolLatticeState;
        candidate_value_local: TncCandidate;
        generated: TncCandidate;
        position: Integer;
        span_units: Integer;
        destination: Integer;
        state_idx: Integer;
        candidate_idx: Integer;
        option_count: Integer;
        option_limit: Integer;
        baseline_option_limit: Integer;
        capacity_option_limit: Integer;
        lattice_beam_width: Integer;
        lattice_candidate_limit: Integer;
        insert_idx: Integer;
        retained_count: Integer;
        destination_count: Integer;
        destination_capacity: Integer;
        query_key: string;
        transition_query: string;
        transition_path: string;
        transition_bonus: Integer;
        trigram_bonus: Integer;
        candidate_text_local: string;
        candidate_key: string;
        retained_edge: TncLongRetainedExactEdge;
        retained_edge_match: Boolean;
        retained_search_bonus: Int64;
        edge_model_features: TncLongExactEdgeLatticeFeatures;
        edge_model_score: Double;
        edge_model_adjustment: Int64;
        edge_model_anchor: Boolean;
        edge_model_raw_weight: Integer;
        edge_model_top_weight: Integer;
        edge_model_retention_score: Int64;
        edge_model_cache_key: UInt64;
        current_edge_id: UInt32;
        transition_cache_key: UInt64;
        sorted_state_indices: TArray<Integer>;
        sort_buffer: TArray<Integer>;

        function lattice_edge_id(const position_value, span_value,
            option_value: Integer): UInt32;
        begin
            Result := 0;
            if (position_value < 0) or (position_value >= 255) or
                (span_value <= 0) or (span_value > 7) or
                (option_value <= 0) or (option_value > 31) then
            begin
                Exit;
            end;
            Result := (UInt32(position_value + 1) shl 8) or
                (UInt32(span_value) shl 5) or UInt32(option_value);
        end;

        function rebuild_state_path(const final_position,
            final_state_index: Integer): string;
        var
            path_segments: TArray<string>;
            current_position: Integer;
            current_state_index: Integer;
            path_segment_index: Integer;
            path_state: TncCompletePoolLatticeState;
        begin
            Result := '';
            if (final_position <= 0) or
                (final_state_index < 0) or
                (final_state_index >= Length(
                states_by_position[final_position])) then
            begin
                Exit;
            end;
            path_state := states_by_position[final_position][
                final_state_index];
            if path_state.segment_count <= 0 then
            begin
                Exit;
            end;
            SetLength(path_segments, path_state.segment_count);
            current_position := final_position;
            current_state_index := final_state_index;
            for path_segment_index := High(path_segments) downto 0 do
            begin
                if (current_position <= 0) or
                    (current_state_index < 0) or
                    (current_state_index >= Length(
                    states_by_position[current_position])) then
                begin
                    Exit('');
                end;
                path_state := states_by_position[current_position][
                    current_state_index];
                path_segments[path_segment_index] :=
                    path_state.segment_text;
                current_state_index :=
                    path_state.predecessor_state_index;
                current_position := path_state.predecessor_position;
            end;
            if current_position <> 0 then
            begin
                Exit('');
            end;
            Result := path_segments[0];
            for path_segment_index := 1 to High(path_segments) do
            begin
                Result := Result + c_segment_path_separator +
                    path_segments[path_segment_index];
            end;
        end;

        function find_retained_edge(const position_value: Integer;
            const span_value: Integer; const query_value: string;
            const text_value: string;
            out edge_value: TncLongRetainedExactEdge): Boolean;
        var
            edge_idx_local: Integer;
        begin
            Result := False;
            edge_value := Default(TncLongRetainedExactEdge);
            if not use_retained_exact_edges then
            begin
                Exit;
            end;
            for edge_idx_local := 0 to
                High(m_runtime_long_retained_exact_edges) do
            begin
                if (m_runtime_long_retained_exact_edges[
                    edge_idx_local].position = position_value) and
                    (m_runtime_long_retained_exact_edges[
                    edge_idx_local].chunk_units = span_value) and
                    SameText(m_runtime_long_retained_exact_edges[
                    edge_idx_local].query_key, query_value) and
                    SameText(Trim(m_runtime_long_retained_exact_edges[
                    edge_idx_local].text), Trim(text_value)) then
                begin
                    edge_value := m_runtime_long_retained_exact_edges[
                        edge_idx_local];
                    Exit(True);
                end;
            end;
        end;

        procedure prune_position(const position_value: Integer);
        var
            state_idx_local: Integer;
            sorted_pos: Integer;
            pass: Integer;
            retained_idx: Integer;
            retained_count_local: Integer;
            signature_count: Integer;
            duplicate_text: Boolean;

            function state_index_is_better(const left_idx,
                right_idx: Integer): Boolean;
            var
                text_compare: Integer;
            begin
                if destination_states[left_idx].score <>
                    destination_states[right_idx].score then
                begin
                    Exit(destination_states[left_idx].score >
                        destination_states[right_idx].score);
                end;
                if destination_states[left_idx].segment_count <>
                    destination_states[right_idx].segment_count then
                begin
                    Exit(destination_states[left_idx].segment_count <
                        destination_states[right_idx].segment_count);
                end;
                text_compare := CompareText(destination_states[left_idx].text,
                    destination_states[right_idx].text);
                if text_compare <> 0 then
                begin
                    Exit(text_compare < 0);
                end;
                Result := left_idx < right_idx;
            end;

            procedure stable_sort_state_indices(const left_pos,
                right_pos: Integer);
            var
                middle_pos: Integer;
                left_cursor: Integer;
                right_cursor: Integer;
                output_cursor: Integer;
                copy_cursor: Integer;
            begin
                if left_pos >= right_pos then
                begin
                    Exit;
                end;
                middle_pos := (left_pos + right_pos) div 2;
                stable_sort_state_indices(left_pos, middle_pos);
                stable_sort_state_indices(middle_pos + 1, right_pos);
                left_cursor := left_pos;
                right_cursor := middle_pos + 1;
                output_cursor := left_pos;
                while (left_cursor <= middle_pos) and
                    (right_cursor <= right_pos) do
                begin
                    if state_index_is_better(
                        sorted_state_indices[left_cursor],
                        sorted_state_indices[right_cursor]) then
                    begin
                        sort_buffer[output_cursor] :=
                            sorted_state_indices[left_cursor];
                        Inc(left_cursor);
                    end
                    else
                    begin
                        sort_buffer[output_cursor] :=
                            sorted_state_indices[right_cursor];
                        Inc(right_cursor);
                    end;
                    Inc(output_cursor);
                end;
                while left_cursor <= middle_pos do
                begin
                    sort_buffer[output_cursor] :=
                        sorted_state_indices[left_cursor];
                    Inc(left_cursor);
                    Inc(output_cursor);
                end;
                while right_cursor <= right_pos do
                begin
                    sort_buffer[output_cursor] :=
                        sorted_state_indices[right_cursor];
                    Inc(right_cursor);
                    Inc(output_cursor);
                end;
                for copy_cursor := left_pos to right_pos do
                begin
                    sorted_state_indices[copy_cursor] :=
                        sort_buffer[copy_cursor];
                end;
            end;
        begin
            destination_states := states_by_position[position_value];
            if Length(destination_states) <
                state_counts_by_position[position_value] then
            begin
                Exit;
            end;
            SetLength(destination_states,
                state_counts_by_position[position_value]);
            states_by_position[position_value] := destination_states;
            if state_counts_by_position[position_value] <= 1 then
            begin
                Exit;
            end;
            if Length(sorted_state_indices) < Length(destination_states) then
            begin
                SetLength(sorted_state_indices, Max(
                    Length(destination_states),
                    Length(sorted_state_indices) * 2));
                SetLength(sort_buffer, Length(sorted_state_indices));
            end;
            for state_idx_local := 0 to High(destination_states) do
            begin
                sorted_state_indices[state_idx_local] := state_idx_local;
            end;
            stable_sort_state_indices(0, High(destination_states));
            SetLength(retained_states, Min(lattice_beam_width,
                Length(destination_states)));
            retained_count_local := 0;
            for pass := 0 to 1 do
            begin
                for sorted_pos := 0 to High(destination_states) do
                begin
                    if retained_count_local >= lattice_beam_width then
                    begin
                        Break;
                    end;
                    state_idx_local := sorted_state_indices[sorted_pos];
                    candidate_key := destination_states[
                        state_idx_local].text;
                    if candidate_key = '' then
                    begin
                        Continue;
                    end;
                    duplicate_text := False;
                    signature_count := 0;
                    for retained_idx := 0 to retained_count_local - 1 do
                    begin
                        if SameText(retained_states[retained_idx].text,
                            candidate_key) then
                        begin
                            duplicate_text := True;
                            Break;
                        end;
                        if (retained_states[retained_idx].signature_low =
                            destination_states[
                            state_idx_local].signature_low) and
                            (retained_states[retained_idx].signature_high =
                            destination_states[
                            state_idx_local].signature_high) then
                        begin
                            Inc(signature_count);
                        end;
                    end;
                    if duplicate_text or ((pass = 0) and
                        (signature_count >=
                        c_exact_lattice_signature_limit)) then
                    begin
                        Continue;
                    end;
                    retained_states[retained_count_local] :=
                        destination_states[state_idx_local];
                    Inc(retained_count_local);
                end;
                if retained_count_local >= lattice_beam_width then
                begin
                    Break;
                end;
            end;
            SetLength(retained_states, retained_count_local);
            states_by_position[position_value] := retained_states;
            state_counts_by_position[position_value] := retained_count_local;
        end;
    begin
        if use_retained_exact_edges and use_edge_model then
        begin
            Exit;
        end;
        if use_retained_exact_edges and
            (Length(m_runtime_long_retained_exact_edges) <= 0) then
        begin
            Exit;
        end;
        lattice_beam_width := c_exact_lattice_beam_width;
        lattice_candidate_limit := c_exact_lattice_candidate_limit;
        if use_edge_model then
        begin
            lattice_beam_width := c_exact_edge_model_lattice_beam_width;
            lattice_candidate_limit :=
                c_exact_edge_model_lattice_candidate_limit;
        end;
        SetLength(states_by_position, expected_units + 1);
        SetLength(state_counts_by_position, expected_units + 1);
        SetLength(states_by_position[0], 1);
        state_counts_by_position[0] := 1;
        states_by_position[0][0] := Default(TncCompletePoolLatticeState);
        states_by_position[0][0].predecessor_position := -1;
        states_by_position[0][0].predecessor_state_index := -1;
        for position := 0 to expected_units - 1 do
        begin
            { All predecessor positions have contributed by this point.  Prune
              once here instead of repeatedly sorting the same destination
              after every incoming span. }
            prune_position(position);
            source_states := states_by_position[position];
            if Length(source_states) <= 0 then
            begin
                Continue;
            end;
            for span_units := 1 to Min(c_exact_lattice_max_segment_units,
                expected_units - position) do
            begin
                query_key := build_span_key(position, span_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key,
                    exact_candidates)) then
                begin
                    Continue;
                end;
                if span_units = 1 then
                begin
                    baseline_option_limit :=
                        c_exact_lattice_single_option_limit;
                end
                else
                begin
                    baseline_option_limit :=
                        c_exact_lattice_word_option_limit;
                end;
                option_limit := baseline_option_limit;
                if use_edge_model then
                begin
                    if span_units = 1 then
                    begin
                        { The learned lane retains exact word edges.  Single
                          characters remain a bounded connector and must not
                          consume the wider word-edge probe budget. }
                        option_limit :=
                            c_exact_edge_model_lattice_single_option_limit;
                    end
                    else
                    begin
                        option_limit :=
                            c_exact_edge_model_lattice_option_limit;
                    end;
                end;
                option_count := 0;
                edge_model_top_weight := 0;
                destination := position + span_units;
                destination_count :=
                    state_counts_by_position[destination];
                capacity_option_limit := option_limit;
                if use_retained_exact_edges then
                begin
                    capacity_option_limit := Max(capacity_option_limit,
                        c_retained_exact_lattice_probe_limit);
                end;
                destination_capacity := destination_count +
                    Length(source_states) * Min(capacity_option_limit,
                    Length(exact_candidates));
                if Length(states_by_position[destination]) <
                    destination_capacity then
                begin
                    SetLength(states_by_position[destination], Max(
                        destination_capacity,
                        Length(states_by_position[destination]) * 2));
                end;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    candidate_value_local := exact_candidates[candidate_idx];
                    candidate_text_local := Trim(candidate_value_local.text);
                    if (Trim(candidate_value_local.comment) <> '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        span_units) then
                    begin
                        Continue;
                    end;
                    retained_edge_match := find_retained_edge(position,
                        span_units, query_key, candidate_text_local,
                        retained_edge);
                    Inc(option_count);
                    edge_model_raw_weight := candidate_raw_weight(
                        candidate_value_local);
                    if option_count = 1 then
                    begin
                        edge_model_top_weight := edge_model_raw_weight;
                    end;
                    if option_count > option_limit then
                    begin
                        if not use_retained_exact_edges then
                        begin
                            Break;
                        end;
                        if not retained_edge_match then
                        begin
                            if candidate_idx + 1 >=
                                c_retained_exact_lattice_probe_limit then
                            begin
                                Break;
                            end;
                            Continue;
                        end;
                    end;
                    retained_search_bonus := 0;
                    if retained_edge_match then
                    begin
                        retained_search_bonus := 180000 +
                            Int64(span_units) * 12000 -
                            Int64(Max(0, retained_edge.exact_rank - 1)) *
                            6000;
                    end;
                    edge_model_adjustment := 0;
                    edge_model_score := 0.0;
                    edge_model_anchor := False;
                    if use_edge_model then
                    begin
                        edge_model_retention_score :=
                            long_edge_retention_score(option_count,
                            edge_model_raw_weight, edge_model_top_weight,
                            candidate_value_local.score, span_units,
                            position, expected_units,
                            candidate_value_local.source = cs_user);
                        edge_model_cache_key := exact_edge_model_cache_key(
                            position, span_units, option_count);
                        if not exact_edge_model_score_cache.TryGetValue(
                            edge_model_cache_key, edge_model_score) then
                        begin
                            build_long_exact_edge_lattice_features(query_key,
                                candidate_text_local, span_units, option_count,
                                edge_model_raw_weight, edge_model_top_weight,
                                candidate_value_local.score,
                                clamp_int64_to_integer(
                                edge_model_retention_score), position,
                                expected_units,
                                option_count <= baseline_option_limit,
                                edge_model_features);
                            edge_model_score :=
                                long_exact_edge_lattice_ranker_score(
                                edge_model_features);
                            exact_edge_model_score_cache.AddOrSetValue(
                                edge_model_cache_key, edge_model_score);
                        end;
                        edge_model_score := Max(-8.0,
                            Min(8.0, edge_model_score));
                        edge_model_adjustment := Round(
                            edge_model_score *
                            c_long_exact_edge_lattice_model_scale +
                            c_long_exact_edge_lattice_segment_penalty);
                        edge_model_anchor :=
                            (span_units >=
                            c_exact_edge_model_min_anchor_units) and
                            (edge_model_score >=
                            c_exact_edge_model_min_anchor_score);
                    end;
                    current_edge_id := lattice_edge_id(position,
                        span_units, option_count);
                    for state_idx := 0 to High(source_states) do
                    begin
                        state_value := source_states[state_idx];
                        transition_bonus := 0;
                        if (state_value.previous_text <> '') and
                            (state_value.previous_units > 0) then
                        begin
                            transition_cache_key :=
                                (UInt64(state_value.previous_edge_id) shl 32) or
                                UInt64(current_edge_id);
                            if (state_value.previous_edge_id = 0) or
                                (current_edge_id = 0) or
                                (not lattice_pair_transition_cache.TryGetValue(
                                transition_cache_key, transition_bonus)) then
                            begin
                                transition_query := build_span_key(position -
                                    state_value.previous_units,
                                    state_value.previous_units + span_units);
                                transition_path := state_value.previous_text +
                                    c_segment_path_separator +
                                    candidate_text_local;
                                transition_bonus := cached_path_transition_bonus(
                                    transition_query, transition_path);
                                if (state_value.previous_edge_id <> 0) and
                                    (current_edge_id <> 0) then
                                begin
                                    lattice_pair_transition_cache.AddOrSetValue(
                                        transition_cache_key,
                                        transition_bonus);
                                end;
                            end;
                        end;
                        if (state_value.previous_previous_text <> '') and
                            (state_value.previous_previous_units > 0) and
                            (state_value.previous_units > 0) then
                        begin
                            transition_cache_key :=
                                (UInt64(state_value.
                                previous_previous_edge_id) shl 32) or
                                (UInt64(state_value.previous_edge_id) shl 16) or
                                UInt64(current_edge_id);
                            if (state_value.previous_previous_edge_id = 0) or
                                (state_value.previous_edge_id = 0) or
                                (current_edge_id = 0) or
                                (not lattice_trigram_transition_cache.TryGetValue(
                                transition_cache_key, trigram_bonus)) then
                            begin
                                transition_query := build_span_key(position -
                                    state_value.previous_previous_units -
                                    state_value.previous_units,
                                    state_value.previous_previous_units +
                                    state_value.previous_units + span_units);
                                transition_path :=
                                    state_value.previous_previous_text +
                                    c_segment_path_separator +
                                    state_value.previous_text +
                                    c_segment_path_separator +
                                    candidate_text_local;
                                trigram_bonus := cached_path_transition_bonus(
                                    transition_query, transition_path);
                                if (state_value.previous_previous_edge_id <> 0) and
                                    (state_value.previous_edge_id <> 0) and
                                    (current_edge_id <> 0) then
                                begin
                                    lattice_trigram_transition_cache.
                                        AddOrSetValue(transition_cache_key,
                                        trigram_bonus);
                                end;
                            end;
                            transition_bonus := Max(transition_bonus,
                                trigram_bonus);
                        end;

                        next_state.score := state_value.score +
                            edge_model_raw_weight +
                            Int64(span_units * span_units) * 1200 +
                            Int64(transition_bonus) * 4 +
                            retained_search_bonus +
                            edge_model_adjustment;
                        next_state.text := state_value.text +
                            candidate_text_local;
                        next_state.signature_low :=
                            state_value.signature_low;
                        next_state.signature_high :=
                            state_value.signature_high;
                        next_state.predecessor_position := position;
                        next_state.predecessor_state_index := state_idx;
                        next_state.segment_text := candidate_text_local;
                        if state_value.segment_count < 21 then
                        begin
                            next_state.signature_low :=
                                state_value.signature_low or
                                (UInt64(span_units) shl
                                (state_value.segment_count * 3));
                        end
                        else
                        begin
                            next_state.signature_high :=
                                state_value.signature_high or
                                (UInt64(span_units) shl
                                ((state_value.segment_count - 21) * 3));
                        end;
                        next_state.previous_previous_text :=
                            state_value.previous_text;
                        next_state.previous_previous_edge_id :=
                            state_value.previous_edge_id;
                        next_state.previous_previous_units :=
                            state_value.previous_units;
                        next_state.previous_text := candidate_text_local;
                        next_state.previous_edge_id := current_edge_id;
                        next_state.previous_units := span_units;
                        next_state.segment_count :=
                            state_value.segment_count + 1;
                        next_state.transition_support :=
                            state_value.transition_support +
                            transition_bonus;
                        next_state.retained_edge_count :=
                            state_value.retained_edge_count;
                        next_state.retained_bonus_total :=
                            state_value.retained_bonus_total;
                        next_state.retained_anchor_start :=
                            state_value.retained_anchor_start;
                        next_state.retained_anchor_units :=
                            state_value.retained_anchor_units;
                        next_state.retained_anchor_exact_rank :=
                            state_value.retained_anchor_exact_rank;
                        next_state.retained_anchor_top_weight :=
                            state_value.retained_anchor_top_weight;
                        if retained_edge_match then
                        begin
                            next_state.retained_edge_count :=
                                state_value.retained_edge_count + 1;
                            next_state.retained_bonus_total :=
                                state_value.retained_bonus_total +
                                retained_search_bonus;
                            next_state.retained_anchor_start := position;
                            next_state.retained_anchor_units := span_units;
                            next_state.retained_anchor_exact_rank :=
                                retained_edge.exact_rank;
                            next_state.retained_anchor_top_weight :=
                                retained_edge.top_weight;
                        end;
                        next_state.model_anchor_count :=
                            state_value.model_anchor_count;
                        next_state.model_anchor_score_total :=
                            state_value.model_anchor_score_total;
                        next_state.model_anchor_score_max :=
                            state_value.model_anchor_score_max;
                        if edge_model_anchor then
                        begin
                            next_state.model_anchor_count :=
                                state_value.model_anchor_count + 1;
                            next_state.model_anchor_score_total :=
                                state_value.model_anchor_score_total +
                                edge_model_score;
                            next_state.model_anchor_score_max := Max(
                                state_value.model_anchor_score_max,
                                edge_model_score);
                        end;
                        next_state.model_word_count :=
                            state_value.model_word_count;
                        next_state.model_word_score_total :=
                            state_value.model_word_score_total;
                        next_state.model_word_score_min :=
                            state_value.model_word_score_min;
                        next_state.model_word_score_max :=
                            state_value.model_word_score_max;
                        if use_edge_model and (span_units >= 2) then
                        begin
                            next_state.model_word_count :=
                                state_value.model_word_count + 1;
                            next_state.model_word_score_total :=
                                state_value.model_word_score_total +
                                edge_model_score;
                            if state_value.model_word_count = 0 then
                            begin
                                next_state.model_word_score_min :=
                                    edge_model_score;
                                next_state.model_word_score_max :=
                                    edge_model_score;
                            end
                            else
                            begin
                                next_state.model_word_score_min := Min(
                                    state_value.model_word_score_min,
                                    edge_model_score);
                                next_state.model_word_score_max := Max(
                                    state_value.model_word_score_max,
                                    edge_model_score);
                            end;
                        end;
                        states_by_position[destination][destination_count] :=
                            next_state;
                        Inc(destination_count);
                    end;
                end;
                state_counts_by_position[destination] := destination_count;
            end;
        end;

        prune_position(expected_units);
        source_states := states_by_position[expected_units];
        retained_count := Min(lattice_candidate_limit,
            Length(source_states));
        for state_idx := 0 to retained_count - 1 do
        begin
            if use_retained_exact_edges and
                (source_states[state_idx].retained_edge_count <= 0) then
            begin
                Continue;
            end;
            generated := Default(TncCandidate);
            generated.text := source_states[state_idx].text;
            generated.comment := '';
            generated.source := cs_rule;
            generated.score := clamp_int64_to_integer(
                source_states[state_idx].score -
                source_states[state_idx].retained_bonus_total);
            generated.has_dict_weight := False;
            generated.dict_weight := 0;
            if use_retained_exact_edges then
            begin
                add_candidate(generated, -1,
                    rebuild_state_path(expected_units, state_idx),
                    lcps_exact_edge_chain, state_idx + 1, -1, False, 0, -1,
                    0, 0, source_states[state_idx].transition_support,
                    source_states[state_idx].retained_anchor_start,
                    source_states[state_idx].retained_anchor_units,
                    source_states[state_idx].retained_anchor_exact_rank,
                    source_states[state_idx].retained_anchor_top_weight,
                    False);
            end
            else
            begin
                add_candidate(generated, -1,
                    rebuild_state_path(expected_units, state_idx),
                    lcps_exact_lattice, state_idx + 1, -1, False, 0, -1,
                    0, 0, source_states[state_idx].transition_support,
                    IfThen(use_edge_model, -2, -1), 0, 0, 0,
                    not use_edge_model);
            end;
            if use_edge_model then
            begin
                candidate_key := LowerCase(Trim(generated.text));
                if raw_index_by_text.TryGetValue(candidate_key,
                    insert_idx) then
                begin
                    raw_pool[insert_idx].edge_model_anchor_count := Max(
                        raw_pool[insert_idx].edge_model_anchor_count,
                        source_states[state_idx].model_anchor_count);
                    raw_pool[insert_idx].edge_model_score_total := Max(
                        raw_pool[insert_idx].edge_model_score_total,
                        Round(source_states[state_idx].
                        model_anchor_score_total * 1000.0));
                    raw_pool[insert_idx].edge_model_score_max := Max(
                        raw_pool[insert_idx].edge_model_score_max,
                        Round(source_states[state_idx].
                        model_anchor_score_max * 1000.0));
                    if (source_states[state_idx].model_word_count > 0) and
                        ((raw_pool[insert_idx].edge_model_word_count <= 0) or
                        (source_states[state_idx].model_word_score_total >
                        raw_pool[insert_idx].edge_model_word_score_total /
                        1000.0)) then
                    begin
                        raw_pool[insert_idx].edge_model_word_count :=
                            source_states[state_idx].model_word_count;
                        raw_pool[insert_idx].edge_model_word_score_total :=
                            Round(source_states[state_idx].
                            model_word_score_total * 1000.0);
                        raw_pool[insert_idx].edge_model_word_score_min :=
                            Round(source_states[state_idx].
                            model_word_score_min * 1000.0);
                        raw_pool[insert_idx].edge_model_word_score_max :=
                            Round(source_states[state_idx].
                            model_word_score_max * 1000.0);
                        raw_pool[insert_idx].edge_model_word_score_mean :=
                            Round(source_states[state_idx].
                            model_word_score_total * 1000.0 /
                            source_states[state_idx].model_word_count);
                    end;
                end;
            end;
        end;
    end;

    function provenance_is_better(
        const candidate_item: TncLongCompletePoolRuntimeCandidate;
        const current_item: TncLongCompletePoolRuntimeCandidate): Boolean;
    begin
        if source_priority(candidate_item.source_kind) <>
            source_priority(current_item.source_kind) then
        begin
            Exit(source_priority(candidate_item.source_kind) >
                source_priority(current_item.source_kind));
        end;
        if candidate_item.pair_evidence <> current_item.pair_evidence then
        begin
            Exit(candidate_item.pair_evidence > current_item.pair_evidence);
        end;
        if candidate_item.substitutions <> current_item.substitutions then
        begin
            Exit(candidate_item.substitutions < current_item.substitutions);
        end;
        Result := candidate_item.seed_rank < current_item.seed_rank;
    end;

    procedure retain_candidate_in_pool(
        var target_pool: TncLongCompletePoolRuntimeCandidateArray;
        const target_index: TDictionary<string, Integer>;
        const value: TncLongCompletePoolRuntimeCandidate);
    var
        key: string;
        existing_idx: Integer;
        item: TncLongCompletePoolRuntimeCandidate;
    begin
        key := LowerCase(Trim(value.candidate.text));
        if key = '' then
        begin
            Exit;
        end;
        if target_index.TryGetValue(key, existing_idx) then
        begin
            item := target_pool[existing_idx];
            item.pair_evidence := Max(item.pair_evidence,
                value.pair_evidence);
            item.edge_model_anchor_count := Max(
                item.edge_model_anchor_count,
                value.edge_model_anchor_count);
            item.edge_model_score_total := Max(
                item.edge_model_score_total,
                value.edge_model_score_total);
            item.edge_model_score_max := Max(
                item.edge_model_score_max,
                value.edge_model_score_max);
            if (value.edge_model_word_count > 0) and
                ((item.edge_model_word_count <= 0) or
                (value.edge_model_word_score_total >
                item.edge_model_word_score_total)) then
            begin
                item.edge_model_word_count := value.edge_model_word_count;
                item.edge_model_word_score_total :=
                    value.edge_model_word_score_total;
                item.edge_model_word_score_min :=
                    value.edge_model_word_score_min;
                item.edge_model_word_score_max :=
                    value.edge_model_word_score_max;
                item.edge_model_word_score_mean :=
                    value.edge_model_word_score_mean;
            end;
            item.proper_name_confidence := Max(
                item.proper_name_confidence,
                value.proper_name_confidence);
            if provenance_is_better(value, item) then
            begin
                item.candidate := value.candidate;
                item.source_index := value.source_index;
                item.segment_path := value.segment_path;
                item.segmentation_signature :=
                    value.segmentation_signature;
                item.source_kind := value.source_kind;
                item.seed_rank := value.seed_rank;
                item.seed_visible_index := value.seed_visible_index;
                item.original := value.original;
                item.substitutions := value.substitutions;
                item.changed_position := value.changed_position;
                item.source_char_weight := value.source_char_weight;
                item.replacement_char_weight :=
                    value.replacement_char_weight;
                item.anchor_start := value.anchor_start;
                item.anchor_units := value.anchor_units;
                item.anchor_exact_rank := value.anchor_exact_rank;
                item.anchor_top_weight := value.anchor_top_weight;
                item.edge_retention_score := value.edge_retention_score;
            end;
            if value.candidate.display_kind = cdk_lm_compound then
            begin
                item.candidate.display_kind := cdk_lm_compound;
            end;
            target_pool[existing_idx] := item;
            Exit;
        end;
        if Length(target_pool) >= c_raw_pool_limit then
        begin
            Exit;
        end;
        existing_idx := Length(target_pool);
        SetLength(target_pool, existing_idx + 1);
        target_pool[existing_idx] := value;
        target_index.Add(key, existing_idx);
    end;

    procedure retain_raw_candidate(
        const value: TncLongCompletePoolRuntimeCandidate);
    begin
        retain_candidate_in_pool(raw_pool, raw_index_by_text, value);
    end;

    procedure retain_baseline_candidate(
        const value: TncLongCompletePoolRuntimeCandidate);
    begin
        retain_candidate_in_pool(baseline_raw_pool,
            baseline_index_by_text, value);
    end;

    procedure add_candidate(const value: TncCandidate;
        const candidate_source_index: Integer; const preferred_path: string;
        const source_kind: TncLongCompletePoolSource;
        const seed_rank: Integer; const seed_visible_index: Integer;
        const original: Boolean; const substitutions: Integer;
        const changed_position: Integer; const source_char_weight: Integer;
        const replacement_char_weight: Integer;
        const pair_evidence: Integer; const anchor_start: Integer;
        const anchor_units: Integer;
        const anchor_exact_rank: Integer;
        const anchor_top_weight: Integer;
        const baseline_eligible: Boolean);
    var
        item: TncLongCompletePoolRuntimeCandidate;
        resolved_path: string;
        signature: string;
        name_confidence: Integer;
    begin
        if not candidate_is_complete(value) then
        begin
            Exit;
        end;
        if ((source_kind = lcps_exact_lattice) or
            (source_kind = lcps_exact_edge_chain)) and
            (anchor_units > 0) and
            decode_trusted_exact_path(Trim(value.text), preferred_path,
            signature, name_confidence) then
        begin
            resolved_path := Trim(preferred_path);
        end
        else if not resolve_candidate_path(value, candidate_source_index,
            preferred_path, resolved_path, signature, name_confidence) then
        begin
            Exit;
        end;
        item := Default(TncLongCompletePoolRuntimeCandidate);
        item.candidate := value;
        item.candidate.comment := '';
        item.source_index := candidate_source_index;
        item.segment_path := resolved_path;
        item.segmentation_signature := signature;
        item.source_kind := source_kind;
        item.seed_rank := Max(1, seed_rank);
        item.seed_visible_index := seed_visible_index;
        item.original := original;
        item.substitutions := substitutions;
        item.changed_position := changed_position;
        item.source_char_weight := source_char_weight;
        item.replacement_char_weight := replacement_char_weight;
        item.anchor_start := anchor_start;
        item.anchor_units := anchor_units;
        item.anchor_exact_rank := anchor_exact_rank;
        item.anchor_top_weight := anchor_top_weight;
        item.pair_evidence := pair_evidence;
        item.proper_name_confidence := name_confidence;
        item.local_pairwise_insert_rank := -1;
        retain_raw_candidate(item);
        if baseline_eligible then
        begin
            retain_baseline_candidate(item);
        end;
    end;

    function build_text_from_path_parts(const parts: TArray<string>): string;
    var
        part_idx: Integer;
    begin
        Result := '';
        for part_idx := 0 to High(parts) do
        begin
            Result := Result + Trim(parts[part_idx]);
        end;
    end;

    function build_encoded_path(const parts: TArray<string>): string;
    var
        part_idx: Integer;
    begin
        Result := '';
        for part_idx := 0 to High(parts) do
        begin
            if Result <> '' then
            begin
                Result := Result + c_segment_path_separator;
            end;
            Result := Result + Trim(parts[part_idx]);
        end;
    end;

    procedure build_best_exact_anchor_subpaths(
        out prefix_states: TncExactSubpathStateArray;
        out suffix_states: TncExactSubpathStateArray);
    var
        boundary_idx: Integer;
        unit_count: Integer;
        candidate_state: TncExactSubpathState;
        exact_candidates: TncCandidateList;
        segment_text: string;
        query_key: string;
        candidate_idx: Integer;
        segment_units: Integer;
        source_idx: Integer;
        next_idx: Integer;

        function candidate_is_usable(const value: TncCandidate;
            const expected_segment_units: Integer): Boolean;
        begin
            Result := (Trim(value.comment) = '') and
                (get_candidate_text_unit_count(Trim(value.text)) =
                expected_segment_units) and
                long_exact_chunk_weight_is_eligible(value.dict_weight,
                value.has_dict_weight);
        end;

        function state_is_better(const candidate_value: TncExactSubpathState;
            const current_value: TncExactSubpathState): Boolean;
        begin
            Result := (not current_value.valid) or
                (candidate_value.score > current_value.score) or
                ((candidate_value.score = current_value.score) and
                (candidate_value.segment_count <
                current_value.segment_count));
        end;

    begin
        unit_count := expected_units;
        SetLength(prefix_states, unit_count + 1);
        SetLength(suffix_states, unit_count + 1);

        prefix_states[0].valid := True;
        for boundary_idx := 1 to unit_count do
        begin
            prefix_states[boundary_idx].valid := False;
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units, boundary_idx) do
            begin
                source_idx := boundary_idx - segment_units;
                if not prefix_states[source_idx].valid then
                begin
                    Continue;
                end;
                query_key := build_span_key(source_idx, segment_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key, exact_candidates)) then
                begin
                    Continue;
                end;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if not candidate_is_usable(exact_candidates[candidate_idx],
                        segment_units) then
                    begin
                        Continue;
                    end;
                    segment_text := Trim(exact_candidates[candidate_idx].text);
                    candidate_state.valid := True;
                    candidate_state.score := prefix_states[source_idx].score +
                        candidate_raw_weight(exact_candidates[candidate_idx]) +
                        Int64(segment_units * segment_units) * 1200;
                    candidate_state.segment_count :=
                        prefix_states[source_idx].segment_count + 1;
                    candidate_state.text := prefix_states[source_idx].text +
                        segment_text;
                    candidate_state.path_text :=
                        prefix_states[source_idx].path_text;
                    if candidate_state.path_text <> '' then
                    begin
                        candidate_state.path_text :=
                            candidate_state.path_text +
                            c_segment_path_separator;
                    end;
                    candidate_state.path_text := candidate_state.path_text +
                        segment_text;
                    if state_is_better(candidate_state,
                        prefix_states[boundary_idx]) then
                    begin
                        prefix_states[boundary_idx] := candidate_state;
                    end;
                    Break;
                end;
            end;
        end;

        suffix_states[unit_count].valid := True;
        for boundary_idx := unit_count - 1 downto 0 do
        begin
            suffix_states[boundary_idx].valid := False;
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units,
                unit_count - boundary_idx) do
            begin
                next_idx := boundary_idx + segment_units;
                if not suffix_states[next_idx].valid then
                begin
                    Continue;
                end;
                query_key := build_span_key(boundary_idx, segment_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key, exact_candidates)) then
                begin
                    Continue;
                end;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if not candidate_is_usable(exact_candidates[candidate_idx],
                        segment_units) then
                    begin
                        Continue;
                    end;
                    segment_text := Trim(exact_candidates[candidate_idx].text);
                    candidate_state.valid := True;
                    candidate_state.score := suffix_states[next_idx].score +
                        candidate_raw_weight(exact_candidates[candidate_idx]) +
                        Int64(segment_units * segment_units) * 1200;
                    candidate_state.segment_count :=
                        suffix_states[next_idx].segment_count + 1;
                    candidate_state.text := segment_text +
                        suffix_states[next_idx].text;
                    candidate_state.path_text := segment_text;
                    if suffix_states[next_idx].path_text <> '' then
                    begin
                        candidate_state.path_text :=
                            candidate_state.path_text +
                            c_segment_path_separator +
                            suffix_states[next_idx].path_text;
                    end;
                    if state_is_better(candidate_state,
                        suffix_states[boundary_idx]) then
                    begin
                        suffix_states[boundary_idx] := candidate_state;
                    end;
                    Break;
                end;
            end;
        end;
    end;

    procedure build_ranked_exact_anchor_subpaths(
        out prefix_states: TncExactSubpathStateMatrix;
        out suffix_states: TncExactSubpathStateMatrix);
    var
        boundary_idx: Integer;
        unit_count: Integer;
        candidate_state: TncExactSubpathState;
        source_state: TncExactSubpathState;
        exact_candidates: TncCandidateList;
        segment_text: string;
        query_key: string;
        transition_query: string;
        transition_path: string;
        transition_bonus: Integer;
        candidate_idx: Integer;
        usable_count: Integer;
        source_state_idx: Integer;
        segment_units: Integer;
        source_idx: Integer;
        next_idx: Integer;

        function candidate_is_usable(const value: TncCandidate;
            const expected_segment_units: Integer): Boolean;
        begin
            Result := (Trim(value.comment) = '') and
                (get_candidate_text_unit_count(Trim(value.text)) =
                expected_segment_units) and
                long_exact_chunk_weight_is_eligible(value.dict_weight,
                value.has_dict_weight);
        end;

        function state_is_better(const candidate_value: TncExactSubpathState;
            const current_value: TncExactSubpathState): Boolean;
        begin
            if candidate_value.score <> current_value.score then
            begin
                Exit(candidate_value.score > current_value.score);
            end;
            if candidate_value.segment_count <> current_value.segment_count then
            begin
                Exit(candidate_value.segment_count <
                    current_value.segment_count);
            end;
            Result := CompareText(candidate_value.path_text,
                current_value.path_text) < 0;
        end;

        procedure retain_state(var values: TncExactSubpathStateArray;
            const value: TncExactSubpathState);
        var
            value_idx: Integer;
            insert_idx: Integer;
            swap_value: TncExactSubpathState;
        begin
            for value_idx := 0 to High(values) do
            begin
                if SameText(values[value_idx].path_text,
                    value.path_text) then
                begin
                    if state_is_better(value, values[value_idx]) then
                    begin
                        values[value_idx] := value;
                    end;
                    Exit;
                end;
            end;
            SetLength(values, Length(values) + 1);
            values[High(values)] := value;
            insert_idx := High(values);
            while (insert_idx > 0) and state_is_better(
                values[insert_idx], values[insert_idx - 1]) do
            begin
                swap_value := values[insert_idx - 1];
                values[insert_idx - 1] := values[insert_idx];
                values[insert_idx] := swap_value;
                Dec(insert_idx);
            end;
            if Length(values) > c_retained_exact_subpath_width then
            begin
                SetLength(values, c_retained_exact_subpath_width);
            end;
        end;

    begin
        unit_count := expected_units;
        SetLength(prefix_states, unit_count + 1);
        SetLength(suffix_states, unit_count + 1);
        SetLength(prefix_states[0], 1);
        prefix_states[0][0] := Default(TncExactSubpathState);
        prefix_states[0][0].valid := True;

        for boundary_idx := 1 to unit_count do
        begin
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units, boundary_idx) do
            begin
                source_idx := boundary_idx - segment_units;
                if Length(prefix_states[source_idx]) <= 0 then
                begin
                    Continue;
                end;
                query_key := build_span_key(source_idx, segment_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key, exact_candidates)) then
                begin
                    Continue;
                end;
                usable_count := 0;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if not candidate_is_usable(
                        exact_candidates[candidate_idx], segment_units) then
                    begin
                        Continue;
                    end;
                    Inc(usable_count);
                    if usable_count > c_retained_exact_subpath_option_limit then
                    begin
                        Break;
                    end;
                    segment_text := Trim(
                        exact_candidates[candidate_idx].text);
                    for source_state_idx := 0 to
                        High(prefix_states[source_idx]) do
                    begin
                        source_state := prefix_states[source_idx][
                            source_state_idx];
                        transition_bonus := 0;
                        if (source_state.last_segment_text <> '') and
                            (source_state.last_segment_units > 0) then
                        begin
                            transition_query := build_span_key(source_idx -
                                source_state.last_segment_units,
                                source_state.last_segment_units +
                                segment_units);
                            transition_path :=
                                source_state.last_segment_text +
                                c_segment_path_separator + segment_text;
                            transition_bonus := cached_path_transition_bonus(
                                transition_query, transition_path);
                        end;
                        candidate_state := Default(TncExactSubpathState);
                        candidate_state.valid := True;
                        candidate_state.score := source_state.score +
                            candidate_raw_weight(
                            exact_candidates[candidate_idx]) +
                            Int64(segment_units * segment_units) * 1200 +
                            Int64(transition_bonus) * 4;
                        candidate_state.segment_count :=
                            source_state.segment_count + 1;
                        candidate_state.text := source_state.text +
                            segment_text;
                        candidate_state.path_text :=
                            source_state.path_text;
                        if candidate_state.path_text <> '' then
                        begin
                            candidate_state.path_text :=
                                candidate_state.path_text +
                                c_segment_path_separator;
                        end;
                        candidate_state.path_text :=
                            candidate_state.path_text + segment_text;
                        candidate_state.first_segment_text :=
                            source_state.first_segment_text;
                        candidate_state.first_segment_units :=
                            source_state.first_segment_units;
                        if candidate_state.first_segment_text = '' then
                        begin
                            candidate_state.first_segment_text := segment_text;
                            candidate_state.first_segment_units :=
                                segment_units;
                        end;
                        candidate_state.last_segment_text := segment_text;
                        candidate_state.last_segment_units := segment_units;
                        candidate_state.transition_support :=
                            source_state.transition_support + transition_bonus;
                        retain_state(prefix_states[boundary_idx],
                            candidate_state);
                    end;
                end;
            end;
        end;

        SetLength(suffix_states[unit_count], 1);
        suffix_states[unit_count][0] := Default(TncExactSubpathState);
        suffix_states[unit_count][0].valid := True;
        for boundary_idx := unit_count - 1 downto 0 do
        begin
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units,
                unit_count - boundary_idx) do
            begin
                next_idx := boundary_idx + segment_units;
                if Length(suffix_states[next_idx]) <= 0 then
                begin
                    Continue;
                end;
                query_key := build_span_key(boundary_idx, segment_units);
                if (query_key = '') or
                    (not load_exact_candidates(query_key, exact_candidates)) then
                begin
                    Continue;
                end;
                usable_count := 0;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if not candidate_is_usable(
                        exact_candidates[candidate_idx], segment_units) then
                    begin
                        Continue;
                    end;
                    Inc(usable_count);
                    if usable_count > c_retained_exact_subpath_option_limit then
                    begin
                        Break;
                    end;
                    segment_text := Trim(
                        exact_candidates[candidate_idx].text);
                    for source_state_idx := 0 to
                        High(suffix_states[next_idx]) do
                    begin
                        source_state := suffix_states[next_idx][
                            source_state_idx];
                        transition_bonus := 0;
                        if (source_state.first_segment_text <> '') and
                            (source_state.first_segment_units > 0) then
                        begin
                            transition_query := build_span_key(boundary_idx,
                                segment_units +
                                source_state.first_segment_units);
                            transition_path := segment_text +
                                c_segment_path_separator +
                                source_state.first_segment_text;
                            transition_bonus := cached_path_transition_bonus(
                                transition_query, transition_path);
                        end;
                        candidate_state := Default(TncExactSubpathState);
                        candidate_state.valid := True;
                        candidate_state.score := source_state.score +
                            candidate_raw_weight(
                            exact_candidates[candidate_idx]) +
                            Int64(segment_units * segment_units) * 1200 +
                            Int64(transition_bonus) * 4;
                        candidate_state.segment_count :=
                            source_state.segment_count + 1;
                        candidate_state.text := segment_text +
                            source_state.text;
                        candidate_state.path_text := segment_text;
                        if source_state.path_text <> '' then
                        begin
                            candidate_state.path_text :=
                                candidate_state.path_text +
                                c_segment_path_separator +
                                source_state.path_text;
                        end;
                        candidate_state.first_segment_text := segment_text;
                        candidate_state.first_segment_units := segment_units;
                        candidate_state.last_segment_text :=
                            source_state.last_segment_text;
                        candidate_state.last_segment_units :=
                            source_state.last_segment_units;
                        if candidate_state.last_segment_text = '' then
                        begin
                            candidate_state.last_segment_text := segment_text;
                            candidate_state.last_segment_units := segment_units;
                        end;
                        candidate_state.transition_support :=
                            source_state.transition_support + transition_bonus;
                        retain_state(suffix_states[boundary_idx],
                            candidate_state);
                    end;
                end;
            end;
        end;
    end;

    procedure build_exact_anchor_subpaths(const units: TArray<string>;
        out prefix_states: TncExactSubpathStateArray;
        out suffix_states: TncExactSubpathStateArray);
    var
        boundary_idx: Integer;
        unit_count: Integer;
        candidate_state: TncExactSubpathState;
        exact_candidate: TncCandidate;
        segment_text: string;
        query_key: string;
        text_idx: Integer;
        segment_units: Integer;
        source_idx: Integer;
        next_idx: Integer;
    begin
        unit_count := Length(units);
        SetLength(prefix_states, unit_count + 1);
        SetLength(suffix_states, unit_count + 1);

        prefix_states[0].valid := True;
        for boundary_idx := 1 to unit_count do
        begin
            prefix_states[boundary_idx].valid := False;
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units, boundary_idx) do
            begin
                source_idx := boundary_idx - segment_units;
                if not prefix_states[source_idx].valid then
                begin
                    Continue;
                end;
                segment_text := '';
                for text_idx := source_idx to boundary_idx - 1 do
                begin
                    segment_text := segment_text + units[text_idx];
                end;
                query_key := build_span_key(source_idx, segment_units);
                if (query_key = '') or (not find_exact_candidate(query_key,
                    segment_text, segment_units, exact_candidate)) then
                begin
                    Continue;
                end;
                candidate_state.valid := True;
                candidate_state.score := prefix_states[source_idx].score +
                    candidate_raw_weight(exact_candidate) +
                    Int64(segment_units * segment_units) * 1200;
                candidate_state.segment_count :=
                    prefix_states[source_idx].segment_count + 1;
                candidate_state.text := prefix_states[source_idx].text +
                    segment_text;
                candidate_state.path_text :=
                    prefix_states[source_idx].path_text;
                if candidate_state.path_text <> '' then
                begin
                    candidate_state.path_text := candidate_state.path_text +
                        c_segment_path_separator;
                end;
                candidate_state.path_text := candidate_state.path_text +
                    segment_text;
                if (not prefix_states[boundary_idx].valid) or
                    (candidate_state.score >
                    prefix_states[boundary_idx].score) or
                    ((candidate_state.score =
                    prefix_states[boundary_idx].score) and
                    (candidate_state.segment_count <
                    prefix_states[boundary_idx].segment_count)) then
                begin
                    prefix_states[boundary_idx] := candidate_state;
                end;
            end;
        end;

        suffix_states[unit_count].valid := True;
        for boundary_idx := unit_count - 1 downto 0 do
        begin
            suffix_states[boundary_idx].valid := False;
            for segment_units := 1 to Min(
                c_exact_lattice_max_segment_units,
                unit_count - boundary_idx) do
            begin
                next_idx := boundary_idx + segment_units;
                if not suffix_states[next_idx].valid then
                begin
                    Continue;
                end;
                segment_text := '';
                for text_idx := boundary_idx to next_idx - 1 do
                begin
                    segment_text := segment_text + units[text_idx];
                end;
                query_key := build_span_key(boundary_idx, segment_units);
                if (query_key = '') or (not find_exact_candidate(query_key,
                    segment_text, segment_units, exact_candidate)) then
                begin
                    Continue;
                end;
                candidate_state.valid := True;
                candidate_state.score := suffix_states[next_idx].score +
                    candidate_raw_weight(exact_candidate) +
                    Int64(segment_units * segment_units) * 1200;
                candidate_state.segment_count :=
                    suffix_states[next_idx].segment_count + 1;
                candidate_state.text := segment_text +
                    suffix_states[next_idx].text;
                candidate_state.path_text := segment_text;
                if suffix_states[next_idx].path_text <> '' then
                begin
                    candidate_state.path_text := candidate_state.path_text +
                        c_segment_path_separator +
                        suffix_states[next_idx].path_text;
                end;
                if (not suffix_states[boundary_idx].valid) or
                    (candidate_state.score >
                    suffix_states[boundary_idx].score) or
                    ((candidate_state.score =
                    suffix_states[boundary_idx].score) and
                    (candidate_state.segment_count <
                    suffix_states[boundary_idx].segment_count)) then
                begin
                    suffix_states[boundary_idx] := candidate_state;
                end;
            end;
        end;
    end;

    procedure generate_retained_exact_edge_anchor_candidates(
        const candidate_limit: Integer);
    type
        TAnchorCombination = record
            score: Int64;
            transition_support: Integer;
            prefix_state: TncExactSubpathState;
            suffix_state: TncExactSubpathState;
        end;
        TAnchorCombinationArray = TArray<TAnchorCombination>;
    var
        prefix_states: TncExactSubpathStateMatrix;
        suffix_states: TncExactSubpathStateMatrix;
        prefix_state: TncExactSubpathState;
        suffix_state: TncExactSubpathState;
        combination: TAnchorCombination;
        combinations: TAnchorCombinationArray;
        seed_item: TncLongCompletePoolRuntimeCandidate;
        seed_units: TArray<string>;
        generated_units: TArray<string>;
        retained_units: TArray<string>;
        retained_edge: TncLongRetainedExactEdge;
        exact_candidate: TncCandidate;
        seed_span_candidate: TncCandidate;
        generated: TncCandidate;
        generated_text: string;
        generated_path: string;
        prefix_path: string;
        suffix_path: string;
        seed_span_text: string;
        query_key: string;
        edge_idx: Integer;
        seed_idx: Integer;
        unit_idx: Integer;
        prefix_idx: Integer;
        suffix_idx: Integer;
        combination_idx: Integer;
        insert_idx: Integer;
        destination: Integer;
        difference_count: Integer;
        changed_position: Integer;
        generated_count: Integer;
        raw_count_before: Integer;
        seed_span_weight: Integer;
        anchor_confidence: Int64;
        left_transition_bonus: Integer;
        right_transition_bonus: Integer;
        transition_query: string;
        transition_path: string;

        procedure retain_combination(const value: TAnchorCombination);
        var
            swap_value: TAnchorCombination;
        begin
            SetLength(combinations, Length(combinations) + 1);
            combinations[High(combinations)] := value;
            insert_idx := High(combinations);
            while (insert_idx > 0) and
                (combinations[insert_idx].score >
                combinations[insert_idx - 1].score) do
            begin
                swap_value := combinations[insert_idx - 1];
                combinations[insert_idx - 1] := combinations[insert_idx];
                combinations[insert_idx] := swap_value;
                Dec(insert_idx);
            end;
            if Length(combinations) >
                c_retained_exact_anchor_paths_per_edge then
            begin
                SetLength(combinations,
                    c_retained_exact_anchor_paths_per_edge);
            end;
        end;
    begin
        if (candidate_limit <= 0) or (raw_original_count <= 0) or
            (Length(m_runtime_long_retained_exact_edges) <= 0) then
        begin
            Exit;
        end;
        { Keep a small, fixed-width exact-path beam on both sides. A single
          best subpath preserves the same local ambiguity that originally
          pushed the exact edge out of the beam. }
        build_ranked_exact_anchor_subpaths(prefix_states, suffix_states);
        generated_count := 0;
        seed_idx := 0;
        seed_item := raw_pool[seed_idx];
        seed_units := split_text_units(Trim(seed_item.candidate.text));
        if Length(seed_units) <> expected_units then
        begin
            Exit;
        end;
        for edge_idx := 0 to
            High(m_runtime_long_retained_exact_edges) do
        begin
            if generated_count >= candidate_limit then
            begin
                Exit;
            end;
            retained_edge :=
                m_runtime_long_retained_exact_edges[edge_idx];
            destination := retained_edge.position +
                retained_edge.chunk_units;
            if (retained_edge.position < 0) or
                (retained_edge.chunk_units < 2) or
                (destination > expected_units) or
                (Length(prefix_states[retained_edge.position]) <= 0) or
                (Length(suffix_states[destination]) <= 0) then
            begin
                Continue;
            end;
            query_key := build_span_key(retained_edge.position,
                retained_edge.chunk_units);
            if (query_key = '') or
                (not SameText(query_key, retained_edge.query_key)) or
                (not find_exact_candidate(query_key,
                retained_edge.text, retained_edge.chunk_units,
                exact_candidate)) then
            begin
                Continue;
            end;
            retained_units := split_text_units(
                Trim(retained_edge.text));
            if Length(retained_units) <>
                retained_edge.chunk_units then
            begin
                Continue;
            end;
            seed_span_text := '';
            for unit_idx := retained_edge.position to
                destination - 1 do
            begin
                seed_span_text := seed_span_text + seed_units[unit_idx];
            end;
            seed_span_weight := 0;
            if find_exact_candidate(query_key, seed_span_text,
                retained_edge.chunk_units, seed_span_candidate) then
            begin
                seed_span_weight := candidate_raw_weight(
                    seed_span_candidate);
            end;
            anchor_confidence :=
                Int64(retained_edge.chunk_units) * 10000 +
                Int64(retained_edge.raw_weight) * 10 -
                Int64(Max(0, retained_edge.exact_rank - 1)) * 2500;
            SetLength(combinations, 0);
            for prefix_idx := 0 to High(prefix_states[
                retained_edge.position]) do
            begin
                prefix_state := prefix_states[
                    retained_edge.position][prefix_idx];
                for suffix_idx := 0 to High(suffix_states[destination]) do
                begin
                    suffix_state := suffix_states[destination][suffix_idx];
                    left_transition_bonus := 0;
                    if (prefix_state.last_segment_text <> '') and
                        (prefix_state.last_segment_units > 0) then
                    begin
                        transition_query := build_span_key(
                            retained_edge.position -
                            prefix_state.last_segment_units,
                            prefix_state.last_segment_units +
                            retained_edge.chunk_units);
                        transition_path :=
                            prefix_state.last_segment_text +
                            c_segment_path_separator +
                            Trim(retained_edge.text);
                        left_transition_bonus :=
                            cached_path_transition_bonus(transition_query,
                            transition_path);
                    end;
                    right_transition_bonus := 0;
                    if (suffix_state.first_segment_text <> '') and
                        (suffix_state.first_segment_units > 0) then
                    begin
                        transition_query := build_span_key(
                            retained_edge.position,
                            retained_edge.chunk_units +
                            suffix_state.first_segment_units);
                        transition_path := Trim(retained_edge.text) +
                            c_segment_path_separator +
                            suffix_state.first_segment_text;
                        right_transition_bonus :=
                            cached_path_transition_bonus(transition_query,
                            transition_path);
                    end;
                    combination := Default(TAnchorCombination);
                    combination.prefix_state := prefix_state;
                    combination.suffix_state := suffix_state;
                    combination.transition_support :=
                        prefix_state.transition_support +
                        suffix_state.transition_support +
                        left_transition_bonus + right_transition_bonus;
                    combination.score := prefix_state.score +
                        suffix_state.score +
                        candidate_raw_weight(exact_candidate) +
                        Int64(retained_edge.chunk_units *
                        retained_edge.chunk_units) * 1200 +
                        Int64(left_transition_bonus +
                        right_transition_bonus) * 4 + anchor_confidence;
                    retain_combination(combination);
                end;
            end;
            for combination_idx := 0 to High(combinations) do
            begin
                if generated_count >= candidate_limit then
                begin
                    Exit;
                end;
                prefix_state := combinations[
                    combination_idx].prefix_state;
                suffix_state := combinations[
                    combination_idx].suffix_state;
                generated_text := prefix_state.text +
                    Trim(retained_edge.text) + suffix_state.text;
                generated_units := split_text_units(generated_text);
                if Length(generated_units) <> expected_units then
                begin
                    Continue;
                end;
                difference_count := 0;
                changed_position := -1;
                for unit_idx := 0 to High(generated_units) do
                begin
                    if not SameText(generated_units[unit_idx],
                        seed_units[unit_idx]) then
                    begin
                        Inc(difference_count);
                        if changed_position < 0 then
                        begin
                            changed_position := unit_idx;
                        end;
                    end;
                end;
                if difference_count <= 0 then
                begin
                    Continue;
                end;
                prefix_path := prefix_state.path_text;
                suffix_path := suffix_state.path_text;
                generated_path := prefix_path;
                if generated_path <> '' then
                begin
                    generated_path := generated_path +
                        c_segment_path_separator;
                end;
                generated_path := generated_path +
                    Trim(retained_edge.text);
                if suffix_path <> '' then
                begin
                    generated_path := generated_path +
                        c_segment_path_separator + suffix_path;
                end;
                generated := seed_item.candidate;
                generated.text := generated_text;
                generated.comment := '';
                generated.source := cs_rule;
                generated.has_dict_weight := False;
                generated.dict_weight := 0;
                generated.score := clamp_int64_to_integer(
                    combinations[combination_idx].score);
                raw_count_before := Length(raw_pool);
                add_candidate(generated, -1, generated_path,
                    lcps_exact_edge_chain, seed_item.seed_rank,
                    seed_item.seed_visible_index, False, difference_count,
                    changed_position, seed_span_weight,
                    candidate_raw_weight(exact_candidate),
                    combinations[combination_idx].transition_support,
                    retained_edge.position, retained_edge.chunk_units,
                    retained_edge.exact_rank, retained_edge.top_weight,
                    False);
                if Length(raw_pool) > raw_count_before then
                begin
                    Inc(generated_count);
                end;
            end;
        end;
    end;

    procedure generate_exact_anchor_candidates(const use_best_subpaths: Boolean;
        const candidate_limit: Integer);
    var
        seed_item: TncLongCompletePoolRuntimeCandidate;
        seed_idx: Integer;
        seed_units: TArray<string>;
        generated_units: TArray<string>;
        anchor_units: TArray<string>;
        proposals: TncCompletePoolAnchorProposalMatrix;
        proposal: TncCompletePoolAnchorProposal;
        exact_candidates: TncCandidateList;
        generated: TncCandidate;
        prefix_path: string;
        suffix_path: string;
        prefix_states: TncExactSubpathStateArray;
        suffix_states: TncExactSubpathStateArray;
        generated_path: string;
        seed_span_text: string;
        generated_text: string;
        query_key: string;
        position: Integer;
        span_units: Integer;
        destination: Integer;
        candidate_idx: Integer;
        valid_rank: Integer;
        proposal_idx: Integer;
        unit_idx: Integer;
        anchor_idx: Integer;
        difference_count: Integer;
        changed_position: Integer;
        generated_count: Integer;
        raw_count_before: Integer;
        raw_weight: Integer;
        seed_span_weight: Integer;
        top_anchor_weight: Integer;
        prefix_score: Int64;
        suffix_score: Int64;

        function proposal_is_better(
            const left_value: TncCompletePoolAnchorProposal;
            const right_value: TncCompletePoolAnchorProposal): Boolean;
        begin
            if not right_value.valid then
            begin
                Exit(left_value.valid);
            end;
            if left_value.confidence <> right_value.confidence then
            begin
                Exit(left_value.confidence > right_value.confidence);
            end;
            if left_value.span_units <> right_value.span_units then
            begin
                Exit(left_value.span_units > right_value.span_units);
            end;
            Result := CompareText(Trim(left_value.candidate.text),
                Trim(right_value.candidate.text)) < 0;
        end;

        procedure retain_proposal(const position_value: Integer;
        const value: TncCompletePoolAnchorProposal);
        var
            existing_idx: Integer;
            proposal_insert_idx: Integer;
        begin
            for existing_idx := 0 to High(proposals[position_value]) do
            begin
                if proposals[position_value][existing_idx].valid and
                    (proposals[position_value][existing_idx].span_units =
                    value.span_units) and SameText(Trim(
                    proposals[position_value][existing_idx].candidate.text),
                    Trim(value.candidate.text)) then
                begin
                    Exit;
                end;
            end;
            for proposal_insert_idx := 0 to
                High(proposals[position_value]) do
            begin
                if proposal_is_better(value,
                    proposals[position_value][proposal_insert_idx]) then
                begin
                    for existing_idx := High(proposals[position_value])
                        downto proposal_insert_idx + 1 do
                    begin
                        proposals[position_value][existing_idx] :=
                            proposals[position_value][existing_idx - 1];
                    end;
                    proposals[position_value][proposal_insert_idx] := value;
                    Exit;
                end;
            end;
        end;
    begin
        if (raw_original_count <= 0) or (candidate_limit <= 0) then
        begin
            Exit;
        end;
        generated_count := 0;
        if use_best_subpaths then
        begin
            build_best_exact_anchor_subpaths(prefix_states, suffix_states);
        end;
        for seed_idx := 0 to Min(c_exact_anchor_seed_limit - 1,
            raw_original_count - 1) do
        begin
            seed_item := raw_pool[seed_idx];
            seed_units := split_text_units(Trim(seed_item.candidate.text));
            if Length(seed_units) <> expected_units then
            begin
                Continue;
            end;
            if not use_best_subpaths then
            begin
                build_exact_anchor_subpaths(seed_units, prefix_states,
                    suffix_states);
            end;
            SetLength(proposals, expected_units);
        for position := 0 to expected_units - 2 do
        begin
            SetLength(proposals[position],
                c_exact_anchor_options_per_position);
            for span_units := 2 to Min(
                c_exact_lattice_max_segment_units,
                expected_units - position) do
            begin
                query_key := build_span_key(position, span_units);
                if (query_key = '') or (not load_exact_candidates(query_key,
                    exact_candidates)) then
                begin
                    Continue;
                end;
                seed_span_text := '';
                for unit_idx := position to position + span_units - 1 do
                begin
                    seed_span_text := seed_span_text + seed_units[unit_idx];
                end;
                seed_span_weight := 0;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if (Trim(exact_candidates[candidate_idx].comment) = '') and
                        SameText(seed_span_text,
                        Trim(exact_candidates[candidate_idx].text)) then
                    begin
                        seed_span_weight := candidate_raw_weight(
                            exact_candidates[candidate_idx]);
                        Break;
                    end;
                end;
                valid_rank := 0;
                top_anchor_weight := 0;
                for candidate_idx := 0 to High(exact_candidates) do
                begin
                    if (Trim(exact_candidates[candidate_idx].comment) <> '') or
                        (get_candidate_text_unit_count(Trim(
                        exact_candidates[candidate_idx].text)) <> span_units) or
                        (not long_exact_chunk_weight_is_eligible(
                        exact_candidates[candidate_idx].dict_weight,
                        exact_candidates[candidate_idx].has_dict_weight)) then
                    begin
                        Continue;
                    end;
                    Inc(valid_rank);
                    if valid_rank = 1 then
                    begin
                        top_anchor_weight := candidate_raw_weight(
                            exact_candidates[candidate_idx]);
                    end;
                    if valid_rank > c_exact_anchor_word_option_limit then
                    begin
                        Break;
                    end;
                    if (not use_best_subpaths) and SameText(seed_span_text,
                        Trim(exact_candidates[candidate_idx].text)) then
                    begin
                        Continue;
                    end;
                    raw_weight := candidate_raw_weight(
                        exact_candidates[candidate_idx]);
                    if ((valid_rank = 2) and (raw_weight < 240)) or
                        ((valid_rank >= 3) and (raw_weight < 420)) then
                    begin
                        Continue;
                    end;
                    proposal := Default(TncCompletePoolAnchorProposal);
                    proposal.valid := True;
                    proposal.span_units := span_units;
                    proposal.exact_rank := valid_rank;
                    proposal.source_weight := seed_span_weight;
                    proposal.top_weight := top_anchor_weight;
                    proposal.candidate := exact_candidates[candidate_idx];
                    proposal.confidence := Int64(span_units) * 10000 +
                        Int64(raw_weight) * 10 -
                        Int64(valid_rank - 1) * 2500;
                    retain_proposal(position, proposal);
                end;
            end;
        end;

        for proposal_idx := 0 to
            c_exact_anchor_options_per_position - 1 do
        begin
            for position := 0 to expected_units - 2 do
            begin
                if generated_count >= candidate_limit then
                begin
                    Exit;
                end;
                proposal := proposals[position][proposal_idx];
                if not proposal.valid then
                begin
                    Continue;
                end;
                destination := position + proposal.span_units;
                anchor_units := split_text_units(
                    Trim(proposal.candidate.text));
                if Length(anchor_units) <> proposal.span_units then
                begin
                    Continue;
                end;
                if (not prefix_states[position].valid) or
                    (not suffix_states[destination].valid) then
                begin
                    Continue;
                end;
                    difference_count := 0;
                    changed_position := -1;
                    if use_best_subpaths then
                    begin
                        generated_text := prefix_states[position].text +
                            Trim(proposal.candidate.text) +
                            suffix_states[destination].text;
                        generated_units := split_text_units(generated_text);
                        if Length(generated_units) <> expected_units then
                        begin
                            Continue;
                        end;
                        for unit_idx := 0 to High(generated_units) do
                        begin
                            if not SameText(generated_units[unit_idx],
                                seed_units[unit_idx]) then
                            begin
                                Inc(difference_count);
                                if changed_position < 0 then
                                begin
                                    changed_position := unit_idx;
                                end;
                            end;
                        end;
                    end;
                    if not use_best_subpaths then
                    begin
                        generated_units := Copy(seed_units, 0,
                            Length(seed_units));
                        for anchor_idx := 0 to High(anchor_units) do
                        begin
                            if not SameText(
                                generated_units[position + anchor_idx],
                                anchor_units[anchor_idx]) then
                            begin
                                Inc(difference_count);
                                if changed_position < 0 then
                                begin
                                    changed_position := position + anchor_idx;
                                end;
                            end;
                            generated_units[position + anchor_idx] :=
                                anchor_units[anchor_idx];
                        end;
                        generated_text := '';
                        for unit_idx := 0 to High(generated_units) do
                        begin
                            generated_text := generated_text +
                                generated_units[unit_idx];
                        end;
                    end;
                    prefix_path := prefix_states[position].path_text;
                    prefix_score := prefix_states[position].score;
                    suffix_path := suffix_states[destination].path_text;
                    suffix_score := suffix_states[destination].score;
                    generated_path := prefix_path;
                    if generated_path <> '' then
                    begin
                        generated_path := generated_path +
                            c_segment_path_separator;
                    end;
                    generated_path := generated_path +
                        Trim(proposal.candidate.text);
                    if suffix_path <> '' then
                    begin
                        generated_path := generated_path +
                            c_segment_path_separator + suffix_path;
                    end;
                    if difference_count <= 0 then
                    begin
                        Continue;
                    end;
                    generated := seed_item.candidate;
                    generated.text := generated_text;
                    generated.comment := '';
                    generated.source := cs_rule;
                    generated.has_dict_weight := False;
                    generated.dict_weight := 0;
                    generated.score := clamp_int64_to_integer(prefix_score +
                        suffix_score + proposal.confidence);
                    raw_count_before := Length(raw_pool);
                    add_candidate(generated, -1, generated_path,
                        lcps_exact_lattice, seed_item.seed_rank,
                        seed_item.seed_visible_index, False, difference_count,
                        changed_position, proposal.source_weight,
                        candidate_raw_weight(proposal.candidate), 0, position,
                        proposal.span_units, proposal.exact_rank,
                        proposal.top_weight, True);
                    if Length(raw_pool) > raw_count_before then
                    begin
                        Inc(generated_count);
                    end;
            end;
        end;
        end;
    end;

    procedure generate_exact_dual_anchor_candidates;
    var
        seed_item: TncLongCompletePoolRuntimeCandidate;
        seed_units: TArray<string>;
        generated_units: TArray<string>;
        left_units: TArray<string>;
        right_units: TArray<string>;
        proposals: TncCompletePoolAnchorProposalMatrix;
        proposal: TncCompletePoolAnchorProposal;
        dual_pairs: TncCompletePoolDualAnchorProposalArray;
        dual_pair: TncCompletePoolDualAnchorProposal;
        exact_candidates: TncCandidateList;
        prefix_states: TncExactSubpathStateArray;
        suffix_states: TncExactSubpathStateArray;
        gap_states: TncExactSubpathStateMatrix;
        gap_computed: TncBooleanMatrix;
        gap_state: TncExactSubpathState;
        generated: TncCandidate;
        query_key: string;
        seed_span_text: string;
        generated_text: string;
        generated_path: string;
        segment_text: string;
        left_text: string;
        right_text: string;
        seed_idx: Integer;
        position: Integer;
        span_units: Integer;
        candidate_idx: Integer;
        valid_rank: Integer;
        proposal_idx: Integer;
        left_position: Integer;
        right_position: Integer;
        left_idx: Integer;
        right_idx: Integer;
        left_destination: Integer;
        right_destination: Integer;
        segment_idx: Integer;
        unit_idx: Integer;
        difference_count: Integer;
        changed_position: Integer;
        raw_count_before: Integer;
        raw_weight: Integer;
        seed_span_weight: Integer;
        top_anchor_weight: Integer;
        dual_generated_count: Integer;
        candidate_state: TncExactSubpathState;
        interval_states: TncExactSubpathStateArray;
        exact_candidate: TncCandidate;

        function proposal_is_better(
            const left_value: TncCompletePoolAnchorProposal;
            const right_value: TncCompletePoolAnchorProposal): Boolean;
        begin
            if not right_value.valid then
            begin
                Exit(left_value.valid);
            end;
            if left_value.confidence <> right_value.confidence then
            begin
                Exit(left_value.confidence > right_value.confidence);
            end;
            if left_value.span_units <> right_value.span_units then
            begin
                Exit(left_value.span_units > right_value.span_units);
            end;
            Result := CompareText(Trim(left_value.candidate.text),
                Trim(right_value.candidate.text)) < 0;
        end;

        procedure retain_proposal(const position_value: Integer;
            const value: TncCompletePoolAnchorProposal);
        var
            existing_idx: Integer;
            insert_idx: Integer;
        begin
            for existing_idx := 0 to High(proposals[position_value]) do
            begin
                if proposals[position_value][existing_idx].valid and
                    (proposals[position_value][existing_idx].span_units =
                    value.span_units) and SameText(Trim(
                    proposals[position_value][existing_idx].candidate.text),
                    Trim(value.candidate.text)) then
                begin
                    Exit;
                end;
            end;
            for insert_idx := 0 to High(proposals[position_value]) do
            begin
                if proposal_is_better(value,
                    proposals[position_value][insert_idx]) then
                begin
                    for existing_idx := High(proposals[position_value])
                        downto insert_idx + 1 do
                    begin
                        proposals[position_value][existing_idx] :=
                            proposals[position_value][existing_idx - 1];
                    end;
                    proposals[position_value][insert_idx] := value;
                    Exit;
                end;
            end;
        end;

        function dual_pair_is_better(
            const left_value: TncCompletePoolDualAnchorProposal;
            const right_value: TncCompletePoolDualAnchorProposal): Boolean;
        var
            left_rank_sum: Integer;
            right_rank_sum: Integer;
        begin
            if not right_value.valid then
            begin
                Exit(left_value.valid);
            end;
            if left_value.confidence <> right_value.confidence then
            begin
                Exit(left_value.confidence > right_value.confidence);
            end;
            left_rank_sum := left_value.left_anchor.exact_rank +
                left_value.right_anchor.exact_rank;
            right_rank_sum := right_value.left_anchor.exact_rank +
                right_value.right_anchor.exact_rank;
            if left_rank_sum <> right_rank_sum then
            begin
                Exit(left_rank_sum < right_rank_sum);
            end;
            if left_value.left_position <> right_value.left_position then
            begin
                Exit(left_value.left_position < right_value.left_position);
            end;
            Result := left_value.right_position < right_value.right_position;
        end;

        procedure retain_dual_pair(
            const value: TncCompletePoolDualAnchorProposal);
        var
            existing_idx: Integer;
            insert_idx: Integer;
        begin
            for insert_idx := 0 to High(dual_pairs) do
            begin
                if dual_pair_is_better(value, dual_pairs[insert_idx]) then
                begin
                    for existing_idx := High(dual_pairs) downto
                        insert_idx + 1 do
                    begin
                        dual_pairs[existing_idx] := dual_pairs[existing_idx - 1];
                    end;
                    dual_pairs[insert_idx] := value;
                    Exit;
                end;
            end;
        end;

        function seed_text_for_span(const start_position: Integer;
            const span_length: Integer): string;
        var
            text_idx: Integer;
        begin
            Result := '';
            for text_idx := start_position to
                start_position + span_length - 1 do
            begin
                Result := Result + seed_units[text_idx];
            end;
        end;

        function get_gap_state(const start_position: Integer;
            const end_position: Integer;
            out value: TncExactSubpathState): Boolean;
        var
            interval_length: Integer;
            interval_boundary_idx: Integer;
            interval_span_units: Integer;
            interval_segment_start: Integer;
        begin
            value := Default(TncExactSubpathState);
            if start_position = end_position then
            begin
                value.valid := True;
                Exit(True);
            end;
            if (start_position < 0) or (end_position > expected_units) or
                (start_position > end_position) then
            begin
                Exit(False);
            end;
            if gap_computed[start_position][end_position] then
            begin
                value := gap_states[start_position][end_position];
                Exit(value.valid);
            end;
            gap_computed[start_position][end_position] := True;
            interval_length := end_position - start_position;
            SetLength(interval_states, interval_length + 1);
            interval_states[0].valid := True;
            for interval_boundary_idx := 1 to interval_length do
            begin
                interval_states[interval_boundary_idx].valid := False;
                for interval_span_units := 1 to Min(
                    c_exact_lattice_max_segment_units,
                    interval_boundary_idx) do
                begin
                    interval_segment_start := start_position +
                        interval_boundary_idx - interval_span_units;
                    if not interval_states[interval_boundary_idx -
                        interval_span_units].valid then
                    begin
                        Continue;
                    end;
                    segment_text := seed_text_for_span(interval_segment_start,
                        interval_span_units);
                    query_key := build_span_key(interval_segment_start,
                        interval_span_units);
                    if (query_key = '') or (not find_exact_candidate(query_key,
                        segment_text, interval_span_units, exact_candidate)) then
                    begin
                        Continue;
                    end;
                    candidate_state.valid := True;
                    candidate_state.score :=
                        interval_states[interval_boundary_idx -
                        interval_span_units].score +
                        candidate_raw_weight(exact_candidate) +
                        Int64(interval_span_units * interval_span_units) * 1200;
                    candidate_state.segment_count :=
                        interval_states[interval_boundary_idx -
                        interval_span_units].segment_count + 1;
                    candidate_state.text :=
                        interval_states[interval_boundary_idx -
                        interval_span_units].text +
                        segment_text;
                    candidate_state.path_text :=
                        interval_states[interval_boundary_idx -
                        interval_span_units].path_text;
                    if candidate_state.path_text <> '' then
                    begin
                        candidate_state.path_text :=
                            candidate_state.path_text +
                            c_segment_path_separator;
                    end;
                    candidate_state.path_text := candidate_state.path_text +
                        segment_text;
                    if (not interval_states[interval_boundary_idx].valid) or
                        (candidate_state.score >
                        interval_states[interval_boundary_idx].score) or
                        ((candidate_state.score =
                        interval_states[interval_boundary_idx].score) and
                        (candidate_state.segment_count <
                        interval_states[
                        interval_boundary_idx].segment_count)) then
                    begin
                        interval_states[interval_boundary_idx] := candidate_state;
                    end;
                end;
            end;
            value := interval_states[interval_length];
            gap_states[start_position][end_position] := value;
            Result := value.valid;
        end;

        procedure append_path(var path_value: string;
            const segment_value: string);
        begin
            if segment_value = '' then
            begin
                Exit;
            end;
            if path_value <> '' then
            begin
                path_value := path_value + c_segment_path_separator;
            end;
            path_value := path_value + segment_value;
        end;
    begin
        if raw_original_count <= 0 then
        begin
            Exit;
        end;
        dual_generated_count := 0;
        for seed_idx := 0 to Min(c_exact_anchor_seed_limit - 1,
            raw_original_count - 1) do
        begin
            if dual_generated_count >= c_exact_dual_anchor_candidate_limit then
            begin
                Break;
            end;
            seed_item := raw_pool[seed_idx];
            seed_units := split_text_units(Trim(seed_item.candidate.text));
            if Length(seed_units) <> expected_units then
            begin
                Continue;
            end;
            build_exact_anchor_subpaths(seed_units, prefix_states,
                suffix_states);
            SetLength(proposals, expected_units);
            SetLength(gap_states, expected_units + 1);
            SetLength(gap_computed, expected_units + 1);
            for position := 0 to expected_units do
            begin
                SetLength(gap_states[position], expected_units + 1);
                SetLength(gap_computed[position], expected_units + 1);
            end;
            for position := 0 to expected_units - 2 do
            begin
                SetLength(proposals[position],
                    c_exact_anchor_options_per_position);
                for span_units := 2 to Min(
                    c_exact_lattice_max_segment_units,
                    expected_units - position) do
                begin
                    query_key := build_span_key(position, span_units);
                    if (query_key = '') or (not load_exact_candidates(query_key,
                        exact_candidates)) then
                    begin
                        Continue;
                    end;
                    seed_span_text := seed_text_for_span(position, span_units);
                    seed_span_weight := 0;
                    for candidate_idx := 0 to High(exact_candidates) do
                    begin
                        if (Trim(exact_candidates[candidate_idx].comment) = '') and
                            SameText(seed_span_text,
                            Trim(exact_candidates[candidate_idx].text)) then
                        begin
                            seed_span_weight := candidate_raw_weight(
                                exact_candidates[candidate_idx]);
                            Break;
                        end;
                    end;
                    valid_rank := 0;
                    top_anchor_weight := 0;
                    for candidate_idx := 0 to High(exact_candidates) do
                    begin
                        if (Trim(exact_candidates[candidate_idx].comment) <> '') or
                            (get_candidate_text_unit_count(Trim(
                            exact_candidates[candidate_idx].text)) <> span_units) or
                            (not long_exact_chunk_weight_is_eligible(
                            exact_candidates[candidate_idx].dict_weight,
                            exact_candidates[candidate_idx].has_dict_weight)) then
                        begin
                            Continue;
                        end;
                        Inc(valid_rank);
                        if valid_rank = 1 then
                        begin
                            top_anchor_weight := candidate_raw_weight(
                                exact_candidates[candidate_idx]);
                        end;
                        if valid_rank > c_exact_anchor_word_option_limit then
                        begin
                            Break;
                        end;
                        if SameText(seed_span_text,
                            Trim(exact_candidates[candidate_idx].text)) then
                        begin
                            Continue;
                        end;
                        raw_weight := candidate_raw_weight(
                            exact_candidates[candidate_idx]);
                        if ((valid_rank = 2) and (raw_weight < 240)) or
                            ((valid_rank >= 3) and (raw_weight < 420)) then
                        begin
                            Continue;
                        end;
                        proposal := Default(TncCompletePoolAnchorProposal);
                        proposal.valid := True;
                        proposal.span_units := span_units;
                        proposal.exact_rank := valid_rank;
                        proposal.source_weight := seed_span_weight;
                        proposal.top_weight := top_anchor_weight;
                        proposal.candidate := exact_candidates[candidate_idx];
                        proposal.confidence := Int64(span_units) * 10000 +
                            Int64(raw_weight) * 10 -
                            Int64(valid_rank - 1) * 2500;
                        retain_proposal(position, proposal);
                    end;
                end;
            end;

            SetLength(dual_pairs, c_exact_dual_anchor_pair_pool_limit);
            for left_position := 0 to expected_units - 2 do
            begin
                for left_idx := 0 to
                    c_exact_anchor_options_per_position - 1 do
                begin
                    if not proposals[left_position][left_idx].valid then
                    begin
                        Continue;
                    end;
                    left_destination := left_position +
                        proposals[left_position][left_idx].span_units;
                    for right_position := left_destination to
                        expected_units - 2 do
                    begin
                        for right_idx := 0 to
                            c_exact_anchor_options_per_position - 1 do
                        begin
                            if not proposals[right_position][right_idx].valid then
                            begin
                                Continue;
                            end;
                            right_destination := right_position +
                                proposals[right_position][right_idx].span_units;
                            if right_destination > expected_units then
                            begin
                                Continue;
                            end;
                            dual_pair := Default(
                                TncCompletePoolDualAnchorProposal);
                            dual_pair.valid := True;
                            dual_pair.left_position := left_position;
                            dual_pair.right_position := right_position;
                            dual_pair.left_anchor :=
                                proposals[left_position][left_idx];
                            dual_pair.right_anchor :=
                                proposals[right_position][right_idx];
                            dual_pair.confidence :=
                                dual_pair.left_anchor.confidence +
                                dual_pair.right_anchor.confidence;
                            retain_dual_pair(dual_pair);
                        end;
                    end;
                end;
            end;

            for proposal_idx := 0 to High(dual_pairs) do
            begin
                if dual_generated_count >=
                    c_exact_dual_anchor_candidate_limit then
                begin
                    Break;
                end;
                dual_pair := dual_pairs[proposal_idx];
                if not dual_pair.valid then
                begin
                    Break;
                end;
                left_destination := dual_pair.left_position +
                    dual_pair.left_anchor.span_units;
                right_destination := dual_pair.right_position +
                    dual_pair.right_anchor.span_units;
                if (not prefix_states[dual_pair.left_position].valid) or
                    (not suffix_states[right_destination].valid) or
                    (not get_gap_state(left_destination,
                    dual_pair.right_position, gap_state)) then
                begin
                    Continue;
                end;
                generated_units := Copy(seed_units, 0, Length(seed_units));
                left_text := Trim(dual_pair.left_anchor.candidate.text);
                right_text := Trim(dual_pair.right_anchor.candidate.text);
                left_units := split_text_units(left_text);
                right_units := split_text_units(right_text);
                if (Length(left_units) <>
                    dual_pair.left_anchor.span_units) or
                    (Length(right_units) <>
                    dual_pair.right_anchor.span_units) then
                begin
                    Continue;
                end;
                difference_count := 0;
                changed_position := -1;
                for segment_idx := 0 to
                    dual_pair.left_anchor.span_units - 1 do
                begin
                    if not SameText(generated_units[
                        dual_pair.left_position + segment_idx],
                        left_units[segment_idx]) then
                    begin
                        Inc(difference_count);
                        if changed_position < 0 then
                        begin
                            changed_position := dual_pair.left_position +
                                segment_idx;
                        end;
                    end;
                    generated_units[dual_pair.left_position + segment_idx] :=
                        left_units[segment_idx];
                end;
                for segment_idx := 0 to
                    dual_pair.right_anchor.span_units - 1 do
                begin
                    if not SameText(generated_units[
                        dual_pair.right_position + segment_idx],
                        right_units[segment_idx]) then
                    begin
                        Inc(difference_count);
                        if changed_position < 0 then
                        begin
                            changed_position := dual_pair.right_position +
                                segment_idx;
                        end;
                    end;
                    generated_units[dual_pair.right_position + segment_idx] :=
                        right_units[segment_idx];
                end;
                if difference_count <= 0 then
                begin
                    Continue;
                end;
                generated_text := '';
                for unit_idx := 0 to High(generated_units) do
                begin
                    generated_text := generated_text + generated_units[unit_idx];
                end;
                generated_path := '';
                append_path(generated_path,
                    prefix_states[dual_pair.left_position].path_text);
                append_path(generated_path, left_text);
                append_path(generated_path, gap_state.path_text);
                append_path(generated_path, right_text);
                append_path(generated_path,
                    suffix_states[right_destination].path_text);
                generated := seed_item.candidate;
                generated.text := generated_text;
                generated.comment := '';
                generated.source := cs_rule;
                generated.has_dict_weight := False;
                generated.dict_weight := 0;
                generated.score := clamp_int64_to_integer(
                    prefix_states[dual_pair.left_position].score +
                    gap_state.score + suffix_states[right_destination].score +
                    dual_pair.confidence);
                raw_count_before := Length(raw_pool);
                add_candidate(generated, -1, generated_path,
                    lcps_exact_lattice, seed_item.seed_rank,
                    seed_item.seed_visible_index, False, difference_count,
                    changed_position,
                    dual_pair.left_anchor.source_weight +
                    dual_pair.right_anchor.source_weight,
                    candidate_raw_weight(dual_pair.left_anchor.candidate) +
                    candidate_raw_weight(dual_pair.right_anchor.candidate),
                    0, dual_pair.left_position,
                    dual_pair.left_anchor.span_units +
                    dual_pair.right_anchor.span_units,
                    Max(dual_pair.left_anchor.exact_rank,
                    dual_pair.right_anchor.exact_rank),
                    Max(dual_pair.left_anchor.top_weight,
                    dual_pair.right_anchor.top_weight), True);
                if Length(raw_pool) > raw_count_before then
                begin
                    Inc(dual_generated_count);
                end;
            end;
        end;
    end;

    function pair_evidence_score(
        const evidence: TncPairPathEvidence): Integer;
    var
        query_value: Integer;
    begin
        Result := 0;
        if (evidence.lm_transition_weight >= c_min_lm_pair_weight) or
            ((evidence.lm_transition_weight >=
            c_min_corroborated_pair_weight) and
            (evidence.query_path_weight >=
            c_min_corroborated_pair_weight)) then
        begin
            Result := Min(1560, evidence.lm_transition_weight * 3);
        end;
        if evidence.query_path_weight >= c_min_query_pair_weight then
        begin
            query_value := Min(420,
                (evidence.query_path_weight * 3) div 5 + 28);
            Result := Max(Result, query_value);
        end;
    end;

    procedure generate_exact_pair_candidates;
    var
        seed_idx: Integer;
        seed_item: TncLongCompletePoolRuntimeCandidate;
        seed_parts: TArray<string>;
        seed_part_units: TArray<string>;
        part_starts: TArray<Integer>;
        part_lengths: TArray<Integer>;
        part_idx: Integer;
        cursor: Integer;
        window_units: Integer;
        window_start: Integer;
        window_key: string;
        evidence_paths: TncPairPathEvidenceList;
        evidence_idx: Integer;
        evidence_value: Integer;
        replacement_parts: TArray<string>;
        replacement_units: TArray<string>;
        replacement_head_units: Integer;
        replacement_tail_units: Integer;
        head_key: string;
        tail_key: string;
        exact_candidate: TncCandidate;
        new_parts: TArray<string>;
        new_part_idx: Integer;
        out_part_idx: Integer;
        generated: TncCandidate;
        generated_path: string;
    begin
        pair_candidate_count := 0;
        for seed_idx := 0 to Min(c_pair_seed_limit - 1,
            raw_original_count - 1) do
        begin
            if pair_candidate_count >= c_pair_candidate_limit then
            begin
                Break;
            end;
            seed_item := raw_pool[seed_idx];
            seed_parts := seed_item.segment_path.Split(
                [c_segment_path_separator]);
            if Length(seed_parts) < 2 then
            begin
                Continue;
            end;
            SetLength(part_starts, Length(seed_parts));
            SetLength(part_lengths, Length(seed_parts));
            cursor := 0;
            for part_idx := 0 to High(seed_parts) do
            begin
                seed_part_units := split_text_units(Trim(seed_parts[part_idx]));
                part_starts[part_idx] := cursor;
                part_lengths[part_idx] := Length(seed_part_units);
                Inc(cursor, part_lengths[part_idx]);
            end;
            if cursor <> expected_units then
            begin
                Continue;
            end;

            for part_idx := 0 to High(seed_parts) - 1 do
            begin
                if pair_candidate_count >= c_pair_candidate_limit then
                begin
                    Break;
                end;
                window_units := part_lengths[part_idx] +
                    part_lengths[part_idx + 1];
                if not (window_units in [4, 5]) then
                begin
                    Continue;
                end;
                window_start := part_starts[part_idx];
                window_key := build_span_key(window_start, window_units);
                if (window_key = '') or
                    (not m_dictionary.get_exact_pair_path_evidence(
                    window_key, evidence_paths)) then
                begin
                    Continue;
                end;
                for evidence_idx := 0 to High(evidence_paths) do
                begin
                    evidence_value := pair_evidence_score(
                        evidence_paths[evidence_idx]);
                    if evidence_value <= 0 then
                    begin
                        Continue;
                    end;
                    replacement_parts := evidence_paths[evidence_idx].encoded_path.Split(
                        [c_segment_path_separator]);
                    if Length(replacement_parts) <> 2 then
                    begin
                        Continue;
                    end;
                    replacement_units := split_text_units(
                        Trim(replacement_parts[0]));
                    replacement_head_units := Length(replacement_units);
                    replacement_units := split_text_units(
                        Trim(replacement_parts[1]));
                    replacement_tail_units := Length(replacement_units);
                    if ((window_units = 4) and
                        ((replacement_head_units <> 2) or
                        (replacement_tail_units <> 2))) or
                        ((window_units = 5) and
                        ((replacement_head_units <> 2) or
                        (replacement_tail_units <> 3))) then
                    begin
                        Continue;
                    end;
                    head_key := build_span_key(window_start,
                        replacement_head_units);
                    tail_key := build_span_key(window_start +
                        replacement_head_units, replacement_tail_units);
                    if (not find_exact_candidate(head_key,
                        Trim(replacement_parts[0]),
                        replacement_head_units, exact_candidate)) or
                        (not find_exact_candidate(tail_key,
                        Trim(replacement_parts[1]),
                        replacement_tail_units, exact_candidate)) then
                    begin
                        Continue;
                    end;

                    SetLength(new_parts, Length(seed_parts));
                    out_part_idx := 0;
                    for new_part_idx := 0 to High(seed_parts) do
                    begin
                        if new_part_idx = part_idx then
                        begin
                            new_parts[out_part_idx] :=
                                Trim(replacement_parts[0]);
                            Inc(out_part_idx);
                            new_parts[out_part_idx] :=
                                Trim(replacement_parts[1]);
                            Inc(out_part_idx);
                        end
                        else if new_part_idx = part_idx + 1 then
                        begin
                            Continue;
                        end
                        else
                        begin
                            new_parts[out_part_idx] :=
                                Trim(seed_parts[new_part_idx]);
                            Inc(out_part_idx);
                        end;
                    end;
                    SetLength(new_parts, out_part_idx);
                    generated_path := build_encoded_path(new_parts);
                    generated := seed_item.candidate;
                    generated.text := build_text_from_path_parts(new_parts);
                    generated.comment := '';
                    generated.source := cs_rule;
                    generated.has_dict_weight := False;
                    generated.dict_weight := 0;
                    generated.display_kind := cdk_lm_compound;
                    generated.score := Max(generated.score,
                        evidence_value);
                    add_candidate(generated, -1, generated_path,
                        lcps_exact_pair, seed_item.seed_rank,
                        seed_item.seed_visible_index, False, 1,
                        window_start, 0, 0, evidence_value, -1, 0, 0, 0,
                        True);
                    Inc(pair_candidate_count);
                    if pair_candidate_count >= c_pair_candidate_limit then
                    begin
                        Break;
                    end;
                end;
            end;
        end;
    end;

    procedure generate_local_repair_candidates;
    var
        seed_idx: Integer;
        seed_item: TncLongCompletePoolRuntimeCandidate;
        seed_units: TArray<string>;
        path_parts: TArray<string>;
        path_part_units: TArray<string>;
        part_starts: TArray<Integer>;
        part_lengths: TArray<Integer>;
        part_idx: Integer;
        unit_idx: Integer;
        part_unit_idx: Integer;
        cursor: Integer;
        source_part_idx: Integer;
        syllable_key: string;
        single_options: TncCandidateList;
        option_idx: Integer;
        source_weight: Integer;
        replacement_text: string;
        repaired_part_text: string;
        exact_candidate: TncCandidate;
        repaired_parts: TArray<string>;
        generated: TncCandidate;
        generated_path: string;
    begin
        if raw_original_count <= 0 then
        begin
            Exit;
        end;
        seed_idx := 0;
        seed_item := raw_pool[seed_idx];
        seed_units := split_text_units(Trim(seed_item.candidate.text));
        path_parts := seed_item.segment_path.Split(
            [c_segment_path_separator]);
        if (Length(seed_units) <> expected_units) or
            (Length(path_parts) <= 0) then
        begin
            Exit;
        end;
        SetLength(part_starts, Length(path_parts));
        SetLength(part_lengths, Length(path_parts));
        cursor := 0;
        for part_idx := 0 to High(path_parts) do
        begin
            path_part_units := split_text_units(Trim(path_parts[part_idx]));
            part_starts[part_idx] := cursor;
            part_lengths[part_idx] := Length(path_part_units);
            Inc(cursor, part_lengths[part_idx]);
        end;
        if cursor <> expected_units then
        begin
            Exit;
        end;

        for unit_idx := 0 to expected_units - 1 do
        begin
            source_part_idx := -1;
            for part_idx := 0 to High(path_parts) do
            begin
                if (unit_idx >= part_starts[part_idx]) and
                    (unit_idx < part_starts[part_idx] +
                    part_lengths[part_idx]) then
                begin
                    source_part_idx := part_idx;
                    Break;
                end;
            end;
            if source_part_idx < 0 then
            begin
                Continue;
            end;
            syllable_key := build_span_key(unit_idx, 1);
            if not load_exact_candidates(syllable_key, single_options) then
            begin
                Continue;
            end;
            source_weight := 0;
            for option_idx := 0 to High(single_options) do
            begin
                if (Trim(single_options[option_idx].comment) = '') and
                    SameText(Trim(single_options[option_idx].text),
                    seed_units[unit_idx]) then
                begin
                    source_weight := candidate_raw_weight(
                        single_options[option_idx]);
                    Break;
                end;
            end;
            for option_idx := 0 to Min(c_single_option_limit - 1,
                High(single_options)) do
            begin
                replacement_text := Trim(single_options[option_idx].text);
                if (Trim(single_options[option_idx].comment) <> '') or
                    (get_candidate_text_unit_count(replacement_text) <> 1) or
                    SameText(replacement_text, seed_units[unit_idx]) then
                begin
                    Continue;
                end;
                repaired_parts := Copy(path_parts, 0, Length(path_parts));
                path_part_units := split_text_units(
                    Trim(repaired_parts[source_part_idx]));
                part_unit_idx := unit_idx - part_starts[source_part_idx];
                if (part_unit_idx < 0) or
                    (part_unit_idx >= Length(path_part_units)) then
                begin
                    Continue;
                end;
                path_part_units[part_unit_idx] := replacement_text;
                repaired_part_text := '';
                for part_idx := 0 to High(path_part_units) do
                begin
                    repaired_part_text := repaired_part_text +
                        path_part_units[part_idx];
                end;
                if (part_lengths[source_part_idx] > 1) and
                    (not find_exact_candidate(build_span_key(
                    part_starts[source_part_idx],
                    part_lengths[source_part_idx]), repaired_part_text,
                    part_lengths[source_part_idx], exact_candidate)) then
                begin
                    Continue;
                end;
                repaired_parts[source_part_idx] := repaired_part_text;
                generated_path := build_encoded_path(repaired_parts);
                generated := seed_item.candidate;
                generated.text := build_text_from_path_parts(repaired_parts);
                generated.comment := '';
                generated.source := cs_rule;
                generated.has_dict_weight := False;
                generated.dict_weight := 0;
                add_candidate(generated, -1, generated_path,
                    lcps_local_repair, seed_item.seed_rank,
                    seed_item.seed_visible_index, False, 1, unit_idx,
                    source_weight,
                    candidate_raw_weight(single_options[option_idx]), 0,
                    -1, 0, 0, 0, True);
            end;
        end;
    end;

    function pool_sort_is_better(const left_idx: Integer;
        const right_idx: Integer): Boolean;
    var
        left_score: Int64;
        right_score: Int64;
    begin
        left_score := Int64(raw_pool[left_idx].char_lm_score) * 8 +
            Int64(raw_pool[left_idx].word_lm_bonus) * 4 +
            Int64(raw_pool[left_idx].pair_evidence) * 4 +
            raw_pool[left_idx].consensus_support +
            source_priority(raw_pool[left_idx].source_kind) * 40;
        right_score := Int64(raw_pool[right_idx].char_lm_score) * 8 +
            Int64(raw_pool[right_idx].word_lm_bonus) * 4 +
            Int64(raw_pool[right_idx].pair_evidence) * 4 +
            raw_pool[right_idx].consensus_support +
            source_priority(raw_pool[right_idx].source_kind) * 40;
        if left_score <> right_score then
        begin
            Exit(left_score > right_score);
        end;
        if raw_pool[left_idx].seed_rank <>
            raw_pool[right_idx].seed_rank then
        begin
            Exit(raw_pool[left_idx].seed_rank <
                raw_pool[right_idx].seed_rank);
        end;
        Result := CompareText(raw_pool[left_idx].candidate.text,
            raw_pool[right_idx].candidate.text) < 0;
    end;

    procedure select_raw_index(const value: Integer);
    var
        key: string;
        signature_count: Integer;
        append_idx: Integer;
    begin
        if (value < 0) or (value >= Length(raw_pool)) or
            (Length(selected_indices) >= c_pool_limit) then
        begin
            Exit;
        end;
        key := LowerCase(Trim(raw_pool[value].candidate.text));
        if (key = '') or selected_texts.ContainsKey(key) then
        begin
            Exit;
        end;
        append_idx := Length(selected_indices);
        SetLength(selected_indices, append_idx + 1);
        selected_indices[append_idx] := value;
        selected_texts.Add(key, True);
        signature_count := 0;
        selected_signature_counts.TryGetValue(
            raw_pool[value].segmentation_signature, signature_count);
        selected_signature_counts.AddOrSetValue(
            raw_pool[value].segmentation_signature, signature_count + 1);
    end;

    procedure reserve_source_lane(const source_kind:
        TncLongCompletePoolSource; const reserve_count: Integer;
        const require_name_confidence: Boolean = False);
    var
        order_idx: Integer;
        retained: Integer;
    begin
        retained := 0;
        for order_idx := 0 to High(sorted_indices) do
        begin
            raw_idx := sorted_indices[order_idx];
            if ((not require_name_confidence) and
                (raw_pool[raw_idx].source_kind <> source_kind)) or
                (require_name_confidence and
                (raw_pool[raw_idx].proper_name_confidence <= 0)) then
            begin
                Continue;
            end;
            if not selected_texts.ContainsKey(LowerCase(Trim(
                raw_pool[raw_idx].candidate.text))) then
            begin
                select_raw_index(raw_idx);
                Inc(retained);
                if retained >= reserve_count then
                begin
                    Break;
                end;
            end;
        end;
    end;

    procedure populate_consensus_features;
    var
        original_units: TArray<TArray<string>>;
        original_weights: TArray<Double>;
        candidate_units: TArray<string>;
        distances: TArray<Integer>;
        original_idx: Integer;
        candidate_idx: Integer;
        unit_idx: Integer;
        original_count: Integer;
        total_weight: Double;
        support: Double;
        distance_sum: Integer;
        top_units: TArray<string>;
        weight: Double;
    begin
        original_count := 0;
        total_weight := 0.0;
        SetLength(original_units, Min(8, raw_original_count));
        SetLength(original_weights, Length(original_units));
        SetLength(top_units, 0);
        for original_idx := 0 to raw_original_count - 1 do
        begin
            if original_count >= Length(original_units) then
            begin
                Break;
            end;
            candidate_units := split_text_units(
                raw_pool[original_idx].candidate.text);
            if Length(candidate_units) <> expected_units then
            begin
                Continue;
            end;
            original_units[original_count] := candidate_units;
            weight := 1.0 / Max(1, raw_pool[original_idx].seed_rank);
            original_weights[original_count] := weight;
            total_weight := total_weight + weight;
            if original_count = 0 then
            begin
                top_units := Copy(candidate_units, 0,
                    Length(candidate_units));
            end;
            Inc(original_count);
        end;
        SetLength(original_units, original_count);
        SetLength(original_weights, original_count);
        if (original_count <= 0) or (total_weight <= 0.0) then
        begin
            Exit;
        end;

        for candidate_idx := 0 to High(raw_pool) do
        begin
            candidate_units := split_text_units(
                raw_pool[candidate_idx].candidate.text);
            if Length(candidate_units) <> expected_units then
            begin
                Continue;
            end;
            SetLength(distances, original_count);
            raw_pool[candidate_idx].consensus_seed_count := original_count;
            raw_pool[candidate_idx].consensus_support_min := 1.0;
            raw_pool[candidate_idx].consensus_nearest_distance :=
                expected_units;
            for unit_idx := 0 to expected_units - 1 do
            begin
                support := 0.0;
                for original_idx := 0 to original_count - 1 do
                begin
                    if SameText(original_units[original_idx][unit_idx],
                        candidate_units[unit_idx]) then
                    begin
                        support := support + original_weights[original_idx];
                    end
                    else
                    begin
                        Inc(distances[original_idx]);
                    end;
                end;
                support := support / total_weight;
                raw_pool[candidate_idx].consensus_support_mean :=
                    raw_pool[candidate_idx].consensus_support_mean + support;
                raw_pool[candidate_idx].consensus_support_min := Min(
                    raw_pool[candidate_idx].consensus_support_min, support);
                if support >= 0.5 then
                begin
                    Inc(raw_pool[candidate_idx].consensus_majority_units);
                end;
                if support >= 0.999999 then
                begin
                    Inc(raw_pool[candidate_idx].consensus_unanimous_units);
                end;
                if unit_idx = raw_pool[candidate_idx].changed_position then
                begin
                    raw_pool[candidate_idx].consensus_changed_support :=
                        support;
                end;
            end;
            raw_pool[candidate_idx].consensus_support_mean :=
                raw_pool[candidate_idx].consensus_support_mean /
                expected_units;
            raw_pool[candidate_idx].consensus_support := Round(
                raw_pool[candidate_idx].consensus_support_mean * 1000.0);
            distance_sum := 0;
            for original_idx := 0 to original_count - 1 do
            begin
                raw_pool[candidate_idx].consensus_nearest_distance := Min(
                    raw_pool[candidate_idx].consensus_nearest_distance,
                    distances[original_idx]);
                Inc(distance_sum, distances[original_idx]);
            end;
            raw_pool[candidate_idx].consensus_mean_distance :=
                distance_sum / original_count;
            if (raw_pool[candidate_idx].changed_position >= 0) and
                (raw_pool[candidate_idx].changed_position <
                Length(top_units)) and
                SameText(top_units[raw_pool[candidate_idx].changed_position],
                candidate_units[raw_pool[candidate_idx].changed_position]) then
            begin
                raw_pool[candidate_idx].consensus_changed_top_match := 1;
            end;
        end;
    end;

    function find_reusable_chain_features(
        const item: TncLongCompletePoolRuntimeCandidate;
        const require_path: Boolean;
        const require_context: Boolean): Integer;
    var
        reusable_idx: Integer;
    begin
        Result := -1;
        for reusable_idx := 0 to High(m_runtime_long_chain_candidates) do
        begin
            if (m_runtime_long_chain_candidates[
                reusable_idx].retained_exact_edge_count > 0) or
                (not m_runtime_long_chain_candidates[reusable_idx].features_valid) or
                (not SameText(
                m_runtime_long_chain_candidates[reusable_idx].feature_query_key,
                normalized_query)) or
                (not SameText(Trim(
                m_runtime_long_chain_candidates[reusable_idx].text),
                Trim(item.candidate.text))) then
            begin
                Continue;
            end;
            if require_path and (not SameText(Trim(
                m_runtime_long_chain_candidates[reusable_idx].segment_path),
                Trim(item.segment_path))) then
            begin
                Continue;
            end;
            if require_context and (not SameText(
                m_runtime_long_chain_candidates[reusable_idx].feature_context_key,
                left_context)) then
            begin
                Continue;
            end;
            Exit(reusable_idx);
        end;
    end;

    procedure populate_word_lm_features;
    var
        candidate_idx: Integer;
        reusable_chain_idx: Integer;
        path_parts: TArray<string>;
        part_idx: Integer;
        query_offset: Integer;
        previous_text_value: string;
        current_text_value: string;
        next_text_value: string;
        previous_units: Integer;
        current_units: Integer;
        next_units: Integer;
        transition_query: string;
        transition_path: string;
        bigram_bonus: Integer;
        trigram_bonus: Integer;
        boundary_bonus: Integer;
        boundary_count: Integer;
        supported_count: Integer;
        strong_count: Integer;
        trigram_count: Integer;
        total_bonus: Int64;

    begin
        for candidate_idx := 0 to High(raw_pool) do
        begin
            reusable_chain_idx := find_reusable_chain_features(
                raw_pool[candidate_idx], True, False);
            if reusable_chain_idx >= 0 then
            begin
                raw_pool[candidate_idx].word_lm_bonus :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_bonus;
                raw_pool[candidate_idx].word_lm_boundary_count :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_boundary_count;
                raw_pool[candidate_idx].word_lm_boundary_min :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_boundary_min;
                raw_pool[candidate_idx].word_lm_boundary_max :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_boundary_max;
                raw_pool[candidate_idx].word_lm_boundary_first :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_boundary_first;
                raw_pool[candidate_idx].word_lm_boundary_last :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_boundary_last;
                raw_pool[candidate_idx].word_lm_supported_ratio :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_supported_ratio;
                raw_pool[candidate_idx].word_lm_strong_ratio :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_strong_ratio;
                raw_pool[candidate_idx].word_lm_trigram_ratio :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_trigram_ratio;
                raw_pool[candidate_idx].word_lm_zero_count :=
                    m_runtime_long_chain_candidates[reusable_chain_idx].word_lm_zero_count;
                Continue;
            end;
            path_parts := raw_pool[candidate_idx].segment_path.Split(
                [c_segment_path_separator]);
            if Length(path_parts) < 2 then
            begin
                Continue;
            end;
            query_offset := 0;
            previous_text_value := '';
            previous_units := 0;
            boundary_count := 0;
            supported_count := 0;
            strong_count := 0;
            trigram_count := 0;
            total_bonus := 0;
            raw_pool[candidate_idx].word_lm_boundary_min := MaxInt;
            for part_idx := 0 to High(path_parts) - 1 do
            begin
                current_text_value := Trim(path_parts[part_idx]);
                next_text_value := Trim(path_parts[part_idx + 1]);
                current_units := get_candidate_text_unit_count(
                    current_text_value);
                next_units := get_candidate_text_unit_count(next_text_value);
                if (current_text_value = '') or (next_text_value = '') or
                    (current_units <= 0) or (next_units <= 0) or
                    (query_offset + current_units + next_units >
                    expected_units) then
                begin
                    boundary_count := 0;
                    Break;
                end;
                transition_query := build_span_key(query_offset,
                    current_units + next_units);
                transition_path := current_text_value +
                    c_segment_path_separator + next_text_value;
                bigram_bonus := cached_lm_transition_bonus(transition_query,
                    transition_path);
                trigram_bonus := 0;
                if (part_idx > 0) and (previous_text_value <> '') and
                    (previous_units > 0) then
                begin
                    transition_query := build_span_key(
                        query_offset - previous_units,
                        previous_units + current_units + next_units);
                    transition_path := previous_text_value +
                        c_segment_path_separator + current_text_value +
                        c_segment_path_separator + next_text_value;
                    trigram_bonus := cached_lm_transition_bonus(
                        transition_query, transition_path);
                end;
                boundary_bonus := Max(bigram_bonus, trigram_bonus);
                if boundary_count = 0 then
                begin
                    raw_pool[candidate_idx].word_lm_boundary_first :=
                        boundary_bonus;
                end;
                raw_pool[candidate_idx].word_lm_boundary_last :=
                    boundary_bonus;
                raw_pool[candidate_idx].word_lm_boundary_min := Min(
                    raw_pool[candidate_idx].word_lm_boundary_min,
                    boundary_bonus);
                raw_pool[candidate_idx].word_lm_boundary_max := Max(
                    raw_pool[candidate_idx].word_lm_boundary_max,
                    boundary_bonus);
                if boundary_bonus > 0 then
                begin
                    Inc(supported_count);
                end;
                if boundary_bonus >= c_min_strong_word_lm_bonus then
                begin
                    Inc(strong_count);
                end;
                if trigram_bonus > 0 then
                begin
                    Inc(trigram_count);
                end;
                Inc(total_bonus, boundary_bonus);
                Inc(boundary_count);
                previous_text_value := current_text_value;
                previous_units := current_units;
                Inc(query_offset, current_units);
            end;
            if boundary_count <= 0 then
            begin
                raw_pool[candidate_idx].word_lm_boundary_min := 0;
                Continue;
            end;
            raw_pool[candidate_idx].word_lm_bonus :=
                total_bonus div boundary_count;
            raw_pool[candidate_idx].word_lm_boundary_count :=
                boundary_count;
            raw_pool[candidate_idx].word_lm_supported_ratio :=
                supported_count * 1000 div boundary_count;
            raw_pool[candidate_idx].word_lm_strong_ratio :=
                strong_count * 1000 div boundary_count;
            raw_pool[candidate_idx].word_lm_trigram_ratio :=
                trigram_count * 1000 div boundary_count;
            raw_pool[candidate_idx].word_lm_zero_count :=
                boundary_count - supported_count;
        end;
    end;

    procedure score_local_pairwise_candidates;
    var
        baseline_idx: Integer;
        challenger_indices: TArray<Integer>;
        challenger_count: Integer;
        challenger_idx: Integer;
        candidate_idx: Integer;
        baseline_units: TArray<string>;
        candidate_units: TArray<string>;
        window_texts: TArray<string>;
        window_scores: TArray<Integer>;
        window_offsets: TArray<Integer>;
        difference_starts: TArray<Integer>;
        difference_ends: TArray<Integer>;
        difference_counts: TArray<Integer>;
        difference_runs: TArray<Integer>;
        difference_max_runs: TArray<Integer>;
        window_count: Integer;
        radius: Integer;
        window_start: Integer;
        window_end: Integer;
        candidate_features: TncLongLocalPoolCandidateFeatures;
        baseline_features: TncLongLocalPoolCandidateFeatures;
        relation_features: TncLongLocalPoolRelationFeatures;
        top_lm: TncLongLocalPoolLmScores;
        candidate_lm: TncLongLocalPoolLmScores;
        model_features: TncLongLocalPairwisePoolFeatures;
        score: Double;

        procedure measure_relation(const left_units: TArray<string>;
            const right_units: TArray<string>; out first_difference: Integer;
            out difference_end: Integer; out different_count: Integer;
            out run_count: Integer; out max_run: Integer);
        var
            relation_idx: Integer;
            current_run: Integer;
        begin
            first_difference := Length(left_units);
            difference_end := Length(left_units);
            different_count := 0;
            run_count := 0;
            max_run := 0;
            current_run := 0;
            for relation_idx := 0 to High(left_units) do
            begin
                if not SameText(left_units[relation_idx],
                    right_units[relation_idx]) then
                begin
                    if different_count = 0 then
                    begin
                        first_difference := relation_idx;
                    end;
                    difference_end := relation_idx + 1;
                    Inc(different_count);
                    Inc(current_run);
                    if current_run = 1 then
                    begin
                        Inc(run_count);
                    end;
                    max_run := Max(max_run, current_run);
                end
                else
                begin
                    current_run := 0;
                end;
            end;
        end;

        procedure fill_candidate_features(const item:
            TncLongCompletePoolRuntimeCandidate;
            out features: TncLongLocalPoolCandidateFeatures);
        begin
            features := Default(TncLongLocalPoolCandidateFeatures);
            features.pool_rank := item.pool_rank;
            features.char_lm_score := item.char_lm_score;
            features.seed_rank := item.seed_rank;
            features.original := Ord(item.original);
            features.substitutions := item.substitutions;
            if item.original then
            begin
                features.changed_position_ratio := -1.0;
            end
            else
            begin
                features.changed_position_ratio :=
                    item.changed_position / Max(1, expected_units - 1);
            end;
            features.source_char_weight := item.source_char_weight;
            features.replacement_char_weight := item.replacement_char_weight;
            features.char_weight_delta := item.replacement_char_weight -
                item.source_char_weight;
            features.consensus_seed_count := item.consensus_seed_count;
            features.consensus_support_mean := item.consensus_support_mean;
            features.consensus_support_min := item.consensus_support_min;
            features.consensus_majority_units :=
                item.consensus_majority_units;
            features.consensus_unanimous_units :=
                item.consensus_unanimous_units;
            features.consensus_nearest_distance :=
                item.consensus_nearest_distance;
            features.consensus_mean_distance := item.consensus_mean_distance;
            features.consensus_changed_support :=
                item.consensus_changed_support;
            features.consensus_changed_top_match :=
                item.consensus_changed_top_match;
        end;

        function join_units(const units: TArray<string>;
            const first_index: Integer; const last_index: Integer): string;
        var
            join_idx: Integer;
        begin
            Result := '';
            for join_idx := first_index to last_index do
            begin
                Result := Result + units[join_idx];
            end;
        end;
    begin
        pairwise_best_idx := -1;
        pairwise_best_score := -MaxDouble;
        pairwise_insert_rank := -1;
        baseline_idx := -1;
        for candidate_idx := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            if m_runtime_long_complete_pool_candidates[candidate_idx].original and
                (m_runtime_long_complete_pool_candidates[candidate_idx].seed_rank = 1) then
            begin
                baseline_idx := candidate_idx;
                Break;
            end;
        end;
        if baseline_idx < 0 then
        begin
            Exit;
        end;
        SetLength(challenger_indices,
            c_long_local_pairwise_pool_max_challengers);
        challenger_count := 0;
        for candidate_idx := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            if (m_runtime_long_complete_pool_candidates[candidate_idx].source_kind <>
                lcps_local_repair) or
                (challenger_count >=
                c_long_local_pairwise_pool_max_challengers) then
            begin
                Continue;
            end;
            challenger_indices[challenger_count] := candidate_idx;
            Inc(challenger_count);
        end;
        if challenger_count <= 0 then
        begin
            Exit;
        end;
        SetLength(challenger_indices, challenger_count);
        baseline_units := split_text_units(
            m_runtime_long_complete_pool_candidates[baseline_idx].candidate.text);
        if Length(baseline_units) <> expected_units then
        begin
            Exit;
        end;
        SetLength(window_offsets, challenger_count);
        SetLength(difference_starts, challenger_count);
        SetLength(difference_ends, challenger_count);
        SetLength(difference_counts, challenger_count);
        SetLength(difference_runs, challenger_count);
        SetLength(difference_max_runs, challenger_count);
        SetLength(window_texts, challenger_count * 8);
        window_count := 0;
        for challenger_idx := 0 to challenger_count - 1 do
        begin
            candidate_idx := challenger_indices[challenger_idx];
            candidate_units := split_text_units(
                m_runtime_long_complete_pool_candidates[candidate_idx].candidate.text);
            if Length(candidate_units) <> expected_units then
            begin
                Exit;
            end;
            measure_relation(baseline_units, candidate_units,
                difference_starts[challenger_idx],
                difference_ends[challenger_idx],
                difference_counts[challenger_idx],
                difference_runs[challenger_idx],
                difference_max_runs[challenger_idx]);
            if difference_counts[challenger_idx] <= 0 then
            begin
                Continue;
            end;
            window_offsets[challenger_idx] := window_count;
            for radius := 0 to 3 do
            begin
                window_start := Max(0,
                    difference_starts[challenger_idx] - radius);
                window_end := Min(expected_units - 1,
                    difference_ends[challenger_idx] + radius - 1);
                window_texts[window_count] := join_units(baseline_units,
                    window_start, window_end);
                Inc(window_count);
                window_texts[window_count] := join_units(candidate_units,
                    window_start, window_end);
                Inc(window_count);
            end;
        end;
        SetLength(window_texts, window_count);
        if (window_count <= 0) or
            (not get_cached_char_lm_scores(window_texts, window_scores,
            clsm_suffix, '')) or (Length(window_scores) <> window_count) then
        begin
            Exit;
        end;
        fill_candidate_features(
            m_runtime_long_complete_pool_candidates[baseline_idx],
            baseline_features);
        for challenger_idx := 0 to challenger_count - 1 do
        begin
            candidate_idx := challenger_indices[challenger_idx];
            fill_candidate_features(
                m_runtime_long_complete_pool_candidates[candidate_idx],
                candidate_features);
            relation_features := Default(TncLongLocalPoolRelationFeatures);
            relation_features.different_units :=
                difference_counts[challenger_idx];
            relation_features.different_runs :=
                difference_runs[challenger_idx];
            relation_features.max_different_run :=
                difference_max_runs[challenger_idx];
            relation_features.same_prefix_units :=
                difference_starts[challenger_idx];
            relation_features.same_suffix_units := expected_units -
                difference_ends[challenger_idx];
            relation_features.difference_span_units :=
                difference_ends[challenger_idx] -
                difference_starts[challenger_idx];
            for radius := 0 to 3 do
            begin
                top_lm[radius] := window_scores[
                    window_offsets[challenger_idx] + radius * 2];
                candidate_lm[radius] := window_scores[
                    window_offsets[challenger_idx] + radius * 2 + 1];
            end;
            build_long_local_pairwise_pool_features(candidate_features,
                baseline_features, relation_features, top_lm, candidate_lm,
                model_features);
            score := long_local_pairwise_pool_score(model_features);
            m_runtime_long_complete_pool_candidates[candidate_idx].local_pairwise_score :=
                Round(score * 1000.0);
            if score > pairwise_best_score then
            begin
                pairwise_best_score := score;
                pairwise_best_idx := candidate_idx;
            end;
        end;
        if pairwise_best_idx < 0 then
        begin
            Exit;
        end;
        if pairwise_best_score >=
            c_long_local_pairwise_pool_top1_threshold then
        begin
            pairwise_insert_rank := 0;
        end
        else if pairwise_best_score >=
            c_long_local_pairwise_pool_top2_threshold then
        begin
            pairwise_insert_rank := 1;
        end
        else
        begin
            pairwise_best_idx := -1;
            Exit;
        end;
        m_runtime_long_complete_pool_candidates[pairwise_best_idx].local_pairwise_insert_rank :=
            pairwise_insert_rank;
        m_long_complete_pool_pairwise_text := Trim(
            m_runtime_long_complete_pool_candidates[pairwise_best_idx].candidate.text);
        m_long_complete_pool_pairwise_insert_rank := pairwise_insert_rank;
        m_long_local_rerank_generated_text :=
            m_long_complete_pool_pairwise_text;
        m_long_local_rerank_composition_text := m_composition_text;
        m_long_local_rerank_lookup_key := m_last_lookup_key;
    end;

    function generated_pool_candidate_has_visible_evidence(
        const candidate_text: string;
        const allow_primary_chain: Boolean): Boolean;
    var
        candidate_idx: Integer;
        item: TncLongCompletePoolRuntimeCandidate;
    begin
        Result := True;
        for candidate_idx := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            item := m_runtime_long_complete_pool_candidates[candidate_idx];
            if not SameText(Trim(item.candidate.text),
                Trim(candidate_text)) then
            begin
                Continue;
            end;
            if (item.source_kind <> lcps_exact_lattice) and
                (item.source_kind <> lcps_existing) and
                (item.source_kind <> lcps_chain) then
            begin
                Exit(True);
            end;

            if (item.source_kind = lcps_chain) and allow_primary_chain then
            begin
                Exit(True);
            end;

            { Keep zero-evidence lattice, pre-display and secondary chain paths
              in the internal pool, but do not expose them as complete choices. }
            Exit((item.pair_evidence > 0) or
                (item.word_lm_supported_ratio > 0) or
                (item.char_lm_score <> 0) or
                (item.char_lm_suffix_score <> 0) or
                (item.char_lm_context_score <> 0) or
                (item.proper_name_confidence > 0));
        end;
    end;

    function try_append_merged_candidate(const value: TncCandidate;
        const candidate_source_index: Integer): Boolean;
    var
        identity_key: string;
    begin
        Result := False;
        if merged_count >= Length(merged_candidates) then
        begin
            Exit;
        end;
        identity_key := build_candidate_identity_key(value.text,
            value.comment);
        if emitted_candidate_identities.ContainsKey(identity_key) then
        begin
            Exit;
        end;
        merged_candidates[merged_count] := value;
        merged_sources[merged_count] := candidate_source_index;
        emitted_candidate_identities.Add(identity_key, True);
        Inc(merged_count);
        Result := True;
    end;

    function is_retained_exact_edge_chain_text(
        const candidate_text: string): Boolean;
    var
        pool_idx: Integer;
    begin
        Result := False;
        for pool_idx := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            if (m_runtime_long_complete_pool_candidates[
                pool_idx].source_kind = lcps_exact_edge_chain) and
                SameText(Trim(m_runtime_long_complete_pool_candidates[
                pool_idx].candidate.text), Trim(candidate_text)) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure demote_unapproved_retained_exact_edge_chains;
    var
        stable_candidates: TncCandidateList;
        stable_sources: TArray<Integer>;
        deferred_candidates: TncCandidateList;
        deferred_sources: TArray<Integer>;
        candidate_idx: Integer;
        stable_count: Integer;
        deferred_count: Integer;
    begin
        stable_count := 0;
        deferred_count := 0;
        SetLength(stable_candidates, Length(internal_candidates));
        SetLength(stable_sources, Length(internal_source_indices));
        SetLength(deferred_candidates, Length(internal_candidates));
        SetLength(deferred_sources, Length(internal_source_indices));
        for candidate_idx := 0 to High(internal_candidates) do
        begin
            if is_retained_exact_edge_chain_text(
                internal_candidates[candidate_idx].text) then
            begin
                deferred_candidates[deferred_count] :=
                    internal_candidates[candidate_idx];
                if candidate_idx < Length(internal_source_indices) then
                begin
                    deferred_sources[deferred_count] :=
                        internal_source_indices[candidate_idx];
                end;
                Inc(deferred_count);
            end
            else
            begin
                stable_candidates[stable_count] :=
                    internal_candidates[candidate_idx];
                if candidate_idx < Length(internal_source_indices) then
                begin
                    stable_sources[stable_count] :=
                        internal_source_indices[candidate_idx];
                end;
                Inc(stable_count);
            end;
        end;
        SetLength(stable_candidates, stable_count + deferred_count);
        SetLength(stable_sources, stable_count + deferred_count);
        for candidate_idx := 0 to deferred_count - 1 do
        begin
            stable_candidates[stable_count + candidate_idx] :=
                deferred_candidates[candidate_idx];
            stable_sources[stable_count + candidate_idx] :=
                deferred_sources[candidate_idx];
        end;
        internal_candidates := stable_candidates;
        internal_source_indices := stable_sources;
    end;

    procedure sync_baseline_pool_features;
    var
        baseline_signature_counts: TDictionary<string, Integer>;
        baseline_idx: Integer;
        source_pool_idx: Integer;
        signature_count: Integer;
        key: string;
    begin
        baseline_signature_counts := TDictionary<string, Integer>.Create;
        try
            for baseline_idx := 0 to High(baseline_raw_pool) do
            begin
                signature_count := 0;
                baseline_signature_counts.TryGetValue(
                    baseline_raw_pool[baseline_idx].segmentation_signature,
                    signature_count);
                baseline_signature_counts.AddOrSetValue(
                    baseline_raw_pool[baseline_idx].segmentation_signature,
                    signature_count + 1);
            end;
            for baseline_idx := 0 to High(baseline_raw_pool) do
            begin
                key := LowerCase(Trim(
                    baseline_raw_pool[baseline_idx].candidate.text));
                if (key = '') or (not raw_index_by_text.TryGetValue(key,
                    source_pool_idx)) then
                begin
                    Continue;
                end;
                baseline_raw_pool[baseline_idx].char_lm_score :=
                    raw_pool[source_pool_idx].char_lm_score;
                baseline_raw_pool[baseline_idx].char_lm_suffix_score :=
                    raw_pool[source_pool_idx].char_lm_suffix_score;
                baseline_raw_pool[baseline_idx].char_lm_context_score :=
                    raw_pool[source_pool_idx].char_lm_context_score;
                baseline_raw_pool[baseline_idx].word_lm_bonus :=
                    raw_pool[source_pool_idx].word_lm_bonus;
                baseline_raw_pool[baseline_idx].word_lm_boundary_count :=
                    raw_pool[source_pool_idx].word_lm_boundary_count;
                baseline_raw_pool[baseline_idx].word_lm_boundary_min :=
                    raw_pool[source_pool_idx].word_lm_boundary_min;
                baseline_raw_pool[baseline_idx].word_lm_boundary_max :=
                    raw_pool[source_pool_idx].word_lm_boundary_max;
                baseline_raw_pool[baseline_idx].word_lm_boundary_first :=
                    raw_pool[source_pool_idx].word_lm_boundary_first;
                baseline_raw_pool[baseline_idx].word_lm_boundary_last :=
                    raw_pool[source_pool_idx].word_lm_boundary_last;
                baseline_raw_pool[baseline_idx].word_lm_supported_ratio :=
                    raw_pool[source_pool_idx].word_lm_supported_ratio;
                baseline_raw_pool[baseline_idx].word_lm_strong_ratio :=
                    raw_pool[source_pool_idx].word_lm_strong_ratio;
                baseline_raw_pool[baseline_idx].word_lm_trigram_ratio :=
                    raw_pool[source_pool_idx].word_lm_trigram_ratio;
                baseline_raw_pool[baseline_idx].word_lm_zero_count :=
                    raw_pool[source_pool_idx].word_lm_zero_count;
                baseline_raw_pool[baseline_idx].consensus_support :=
                    raw_pool[source_pool_idx].consensus_support;
                baseline_raw_pool[baseline_idx].consensus_seed_count :=
                    raw_pool[source_pool_idx].consensus_seed_count;
                baseline_raw_pool[baseline_idx].consensus_support_mean :=
                    raw_pool[source_pool_idx].consensus_support_mean;
                baseline_raw_pool[baseline_idx].consensus_support_min :=
                    raw_pool[source_pool_idx].consensus_support_min;
                baseline_raw_pool[baseline_idx].consensus_majority_units :=
                    raw_pool[source_pool_idx].consensus_majority_units;
                baseline_raw_pool[baseline_idx].consensus_unanimous_units :=
                    raw_pool[source_pool_idx].consensus_unanimous_units;
                baseline_raw_pool[baseline_idx].consensus_nearest_distance :=
                    raw_pool[source_pool_idx].consensus_nearest_distance;
                baseline_raw_pool[baseline_idx].consensus_mean_distance :=
                    raw_pool[source_pool_idx].consensus_mean_distance;
                baseline_raw_pool[baseline_idx].consensus_changed_support :=
                    raw_pool[source_pool_idx].consensus_changed_support;
                baseline_raw_pool[baseline_idx].consensus_changed_top_match :=
                    raw_pool[source_pool_idx].consensus_changed_top_match;
                signature_count := 0;
                baseline_signature_counts.TryGetValue(
                    baseline_raw_pool[baseline_idx].segmentation_signature,
                    signature_count);
                baseline_raw_pool[baseline_idx].signature_support :=
                    signature_count;
                { The shadow pool represents the pre-edge-recall pipeline. }
                baseline_raw_pool[baseline_idx].edge_model_anchor_count := 0;
                baseline_raw_pool[baseline_idx].edge_model_score_total := 0;
                baseline_raw_pool[baseline_idx].edge_model_score_max := 0;
                baseline_raw_pool[baseline_idx].edge_model_word_count := 0;
                baseline_raw_pool[baseline_idx].edge_model_word_score_total := 0;
                baseline_raw_pool[baseline_idx].edge_model_word_score_min := 0;
                baseline_raw_pool[baseline_idx].edge_model_word_score_max := 0;
                baseline_raw_pool[baseline_idx].edge_model_word_score_mean := 0;
            end;
        finally
            baseline_signature_counts.Free;
        end;
    end;

    procedure build_runtime_pool_from_raw(const mark_debug: Boolean;
        const preserve_shadow_top: Boolean = False);
    var
        local_raw_idx: Integer;
        local_sort_pos: Integer;
        local_sort_insert_pos: Integer;
        local_sort_value: Integer;
        local_selected_idx: Integer;
        local_signature_count: Integer;
    begin
        SetLength(sorted_indices, Length(raw_pool));
        for local_raw_idx := 0 to High(raw_pool) do
        begin
            sorted_indices[local_raw_idx] := local_raw_idx;
        end;
        { The raw pool is bounded. Stable insertion sort preserves source
          order for exact score ties without allocating a comparer. }
        for local_sort_pos := 1 to High(sorted_indices) do
        begin
            local_sort_value := sorted_indices[local_sort_pos];
            local_sort_insert_pos := local_sort_pos - 1;
            while (local_sort_insert_pos >= 0) and pool_sort_is_better(
                local_sort_value,
                sorted_indices[local_sort_insert_pos]) do
            begin
                sorted_indices[local_sort_insert_pos + 1] :=
                    sorted_indices[local_sort_insert_pos];
                Dec(local_sort_insert_pos);
            end;
            sorted_indices[local_sort_insert_pos + 1] := local_sort_value;
        end;

        SetLength(selected_indices, 0);
        selected_texts.Clear;
        selected_signature_counts.Clear;
        for local_raw_idx := 0 to High(raw_pool) do
        begin
            if raw_pool[local_raw_idx].source_kind = lcps_visible then
            begin
                select_raw_index(local_raw_idx);
            end;
        end;
        { The exact-edge lane is additive. Keep the settled pre-edge Top1/Top2
          available so the final auditor can always abstain without changing
          established output. }
        if preserve_shadow_top then
        begin
            if raw_index_by_text.TryGetValue(
                LowerCase(Trim(baseline_top1)), local_raw_idx) then
            begin
                select_raw_index(local_raw_idx);
            end;
            if raw_index_by_text.TryGetValue(
                LowerCase(Trim(baseline_top2)), local_raw_idx) then
            begin
                select_raw_index(local_raw_idx);
            end;
        end;
        reserve_source_lane(lcps_proper_name,
            c_proper_name_reserved, True);
        reserve_source_lane(lcps_chain, c_chain_reserved);
        reserve_source_lane(lcps_exact_lattice,
            c_exact_lattice_reserved);
        reserve_source_lane(lcps_exact_edge_chain,
            c_exact_edge_chain_reserved);
        reserve_source_lane(lcps_exact_pair, c_pair_reserved);
        reserve_source_lane(lcps_local_repair, c_local_reserved);

        for local_selected_idx := 0 to High(sorted_indices) do
        begin
            local_raw_idx := sorted_indices[local_selected_idx];
            local_signature_count := 0;
            selected_signature_counts.TryGetValue(
                raw_pool[local_raw_idx].segmentation_signature,
                local_signature_count);
            if local_signature_count >= c_signature_soft_limit then
            begin
                Continue;
            end;
            select_raw_index(local_raw_idx);
            if Length(selected_indices) >= c_pool_limit then
            begin
                Break;
            end;
        end;
        for local_selected_idx := 0 to High(sorted_indices) do
        begin
            if Length(selected_indices) >= c_pool_limit then
            begin
                Break;
            end;
            select_raw_index(sorted_indices[local_selected_idx]);
        end;

        SetLength(m_runtime_long_complete_pool_candidates,
            Length(selected_indices));
        for local_selected_idx := 0 to High(selected_indices) do
        begin
            local_raw_idx := selected_indices[local_selected_idx];
            raw_pool[local_raw_idx].pool_rank := local_selected_idx + 1;
            raw_pool[local_raw_idx].local_pairwise_score := 0;
            raw_pool[local_raw_idx].local_pairwise_insert_rank := -1;
            m_runtime_long_complete_pool_candidates[local_selected_idx] :=
                raw_pool[local_raw_idx];
            m_runtime_long_complete_pool_candidates[local_selected_idx].visible_evidence :=
                generated_pool_candidate_has_visible_evidence(
                raw_pool[local_raw_idx].candidate.text, False);
            remember_segment_path_for_candidate(
                raw_pool[local_raw_idx].candidate.text, '',
                raw_pool[local_raw_idx].segment_path,
                raw_pool[local_raw_idx].candidate.score);
            if mark_debug then
            begin
                debug_mark_long_exact_path_complete_pool(
                    raw_pool[local_raw_idx].segment_path);
            end;
        end;
    end;

    procedure copy_runtime_pool_to_internal(
        out target_candidates: TncCandidateList;
        out target_sources: TArray<Integer>);
    var
        local_selected_idx: Integer;
    begin
        SetLength(target_candidates,
            Length(m_runtime_long_complete_pool_candidates));
        SetLength(target_sources, Length(target_candidates));
        for local_selected_idx := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            target_candidates[local_selected_idx] :=
                m_runtime_long_complete_pool_candidates[local_selected_idx].candidate;
            target_sources[local_selected_idx] :=
                m_runtime_long_complete_pool_candidates[local_selected_idx].source_index;
        end;
    end;

begin
    pool_phase_tick := 0;
    pool_phase_frequency := 0;
    if m_config.debug_mode then
    begin
        QueryPerformanceFrequency(pool_phase_frequency);
        QueryPerformanceCounter(pool_phase_tick);
    end;
    SetLength(m_runtime_long_complete_pool_candidates, 0);
    m_long_complete_pool_pairwise_text := '';
    m_long_complete_pool_pairwise_insert_rank := -1;
    m_long_local_rerank_generated_text := '';
    m_long_local_rerank_composition_text := '';
    m_long_local_rerank_lookup_key := '';
    if (m_dictionary = nil) or (m_page_index <> 0) or
        (m_confirmed_text <> '') or (expected_units < 6) or
        (Length(query_syllables) <> expected_units) or
        (Length(candidates) < 2) or m_has_forced_visible_top_candidate then
    begin
        apply_long_final_visible_candidate_ranking(candidates,
            source_indices);
        Exit;
    end;
    if candidate_is_complete(candidates[0]) and
        ((candidates[0].source = cs_user) or
        m_dictionary.is_user_entry(normalized_query,
        Trim(candidates[0].text)) or
        (candidates[0].has_dict_weight and
        m_dictionary.is_base_entry(normalized_query,
        Trim(candidates[0].text)))) then
    begin
        apply_long_final_visible_candidate_ranking(candidates,
            source_indices);
        Exit;
    end;

    original_candidates := Copy(candidates, 0, Length(candidates));
    original_source_indices := Copy(source_indices, 0,
        Length(source_indices));
    original_visible_count := Length(original_candidates);
    SetLength(raw_pool, 0);
    SetLength(baseline_raw_pool, 0);
    raw_index_by_text := TDictionary<string, Integer>.Create;
    raw_index_by_text.Capacity := 128;
    baseline_index_by_text := TDictionary<string, Integer>.Create;
    baseline_index_by_text.Capacity := 64;
    selected_texts := TDictionary<string, Boolean>.Create;
    selected_texts.Capacity := 64;
    selected_signature_counts := TDictionary<string, Integer>.Create;
    selected_signature_counts.Capacity := 32;
    signature_totals := TDictionary<string, Integer>.Create;
    signature_totals.Capacity := 64;
    exact_cache := TDictionary<string, TncCandidateList>.Create;
    exact_cache.Capacity := 128;
    word_lm_cache := TDictionary<string, Integer>.Create;
    word_lm_cache.Capacity := 256;
    lattice_pair_transition_cache := TDictionary<UInt64, Integer>.Create;
    lattice_pair_transition_cache.Capacity := 512;
    lattice_trigram_transition_cache := TDictionary<UInt64, Integer>.Create;
    lattice_trigram_transition_cache.Capacity := 512;
    exact_edge_model_score_cache := TDictionary<UInt64, Double>.Create;
    exact_edge_model_score_cache.Capacity := 128;
    emitted_candidate_identities := TDictionary<string, Boolean>.Create;
    emitted_candidate_identities.Capacity := 32;
    try
        for idx := 0 to High(original_candidates) do
        begin
            source_idx := -1;
            if idx < Length(original_source_indices) then
            begin
                source_idx := original_source_indices[idx];
            end;
            add_candidate(original_candidates[idx], source_idx, '',
                lcps_visible, idx + 1, idx, True, 0, -1, 0, 0, 0,
                -1, 0, 0, 0, not candidate_text_is_edge_only(
                original_candidates[idx].text));
        end;

        for chain_idx := 0 to High(m_runtime_long_chain_candidates) do
        begin
            candidate_value := Default(TncCandidate);
            candidate_value.text :=
                m_runtime_long_chain_candidates[chain_idx].text;
            candidate_value.comment := '';
            if m_runtime_long_chain_candidates[chain_idx].second_stage_score >
                High(Integer) then
            begin
                candidate_value.score := High(Integer);
            end
            else if m_runtime_long_chain_candidates[chain_idx].second_stage_score <
                Low(Integer) then
            begin
                candidate_value.score := Low(Integer);
            end
            else
            begin
                candidate_value.score :=
                    m_runtime_long_chain_candidates[chain_idx].second_stage_score;
            end;
            candidate_value.source := cs_rule;
            candidate_value.has_dict_weight := False;
            candidate_value.dict_weight := 0;
            chain_source_kind := lcps_chain;
            if m_runtime_long_chain_candidates[
                chain_idx].retained_exact_edge_count > 0 then
            begin
                chain_source_kind := lcps_exact_edge_chain;
            end;
            add_candidate(candidate_value, -1,
                m_runtime_long_chain_candidates[chain_idx].segment_path,
                chain_source_kind,
                m_runtime_long_chain_candidates[chain_idx].rank,
                -1, True, 0, -1, 0, 0, 0, -1, 0, 0, 0,
                chain_source_kind <> lcps_exact_edge_chain);
        end;

        for idx := 0 to Min(c_existing_probe_limit - 1,
            High(m_candidates)) do
        begin
            add_candidate(m_candidates[idx], idx, '', lcps_existing,
                idx + 1, -1, True, 0, -1, 0, 0, 0, -1, 0, 0, 0,
                True);
        end;
        raw_original_count := Length(raw_pool);
        note_pool_phase('seed');
        if raw_original_count <= 0 then
        begin
            apply_long_final_visible_candidate_ranking(candidates,
                source_indices);
            Exit;
        end;

        collect_model_retained_exact_edges;
        note_pool_phase('edge_collect');
        generate_exact_lattice_candidates(False, False);
        note_pool_phase('lattice_base');
        generate_exact_lattice_candidates(True, False);
        note_pool_phase('lattice_retained');
        generate_exact_lattice_candidates(False, True);
        note_pool_phase('lattice_model');
        generate_exact_anchor_candidates(False,
            c_exact_anchor_candidate_limit);
        generate_exact_anchor_candidates(True,
            c_exact_anchor_best_candidate_limit);
        generate_retained_exact_edge_anchor_candidates(
            c_retained_exact_anchor_candidate_limit);
        note_pool_phase('anchor');
        generate_exact_pair_candidates;
        note_pool_phase('pair');
        generate_local_repair_candidates;
        note_pool_phase('repair');

        get_recent_path_context_seed(previous_previous_text, previous_text);
        left_context := Trim(previous_previous_text) + Trim(previous_text);
        has_left_context := left_context <> '';

        SetLength(char_texts, Length(raw_pool));
        SetLength(char_scores, Length(raw_pool));
        SetLength(char_suffix_scores, Length(raw_pool));
        SetLength(char_context_scores, Length(raw_pool));
        SetLength(missing_char_raw_indices, Length(raw_pool));
        SetLength(missing_char_texts, Length(raw_pool));
        missing_char_count := 0;
        for raw_idx := 0 to High(raw_pool) do
        begin
            char_texts[raw_idx] := Trim(raw_pool[raw_idx].candidate.text);
            chain_idx := find_reusable_chain_features(raw_pool[raw_idx],
                False, True);
            if chain_idx >= 0 then
            begin
                char_scores[raw_idx] :=
                    m_runtime_long_chain_candidates[chain_idx].char_lm_score;
                char_suffix_scores[raw_idx] :=
                    m_runtime_long_chain_candidates[chain_idx].char_lm_suffix_score;
                char_context_scores[raw_idx] :=
                    m_runtime_long_chain_candidates[chain_idx].char_lm_context_score;
            end
            else
            begin
                missing_char_raw_indices[missing_char_count] := raw_idx;
                missing_char_texts[missing_char_count] := char_texts[raw_idx];
                Inc(missing_char_count);
            end;
        end;
        SetLength(missing_char_raw_indices, missing_char_count);
        SetLength(missing_char_texts, missing_char_count);
        char_scores_ok := True;
        if missing_char_count > 0 then
        begin
            char_scores_ok := get_cached_char_lm_scores(missing_char_texts,
                missing_char_scores, clsm_full, '') and
                (Length(missing_char_scores) = missing_char_count);
            if char_scores_ok then
            begin
                if (not get_cached_char_lm_scores(missing_char_texts,
                    missing_char_suffix_scores, clsm_suffix, '')) or
                    (Length(missing_char_suffix_scores) <>
                    missing_char_count) then
                begin
                    missing_char_suffix_scores := Copy(missing_char_scores);
                end;
                if has_left_context and
                    get_cached_char_lm_scores(missing_char_texts,
                    missing_char_context_scores, clsm_context,
                    left_context) and
                    (Length(missing_char_context_scores) =
                    missing_char_count) then
                begin
                    { Keep the contextual scores returned by the dictionary. }
                end
                else
                begin
                    missing_char_context_scores :=
                        Copy(missing_char_suffix_scores);
                end;
                for missing_char_idx := 0 to missing_char_count - 1 do
                begin
                    raw_idx := missing_char_raw_indices[missing_char_idx];
                    char_scores[raw_idx] :=
                        missing_char_scores[missing_char_idx];
                    char_suffix_scores[raw_idx] :=
                        missing_char_suffix_scores[missing_char_idx];
                    char_context_scores[raw_idx] :=
                        missing_char_context_scores[missing_char_idx];
                end;
            end;
        end;
        if not char_scores_ok then
        begin
            if (not get_cached_char_lm_scores(char_texts, char_scores,
                clsm_full, '')) or
                (Length(char_scores) <> Length(raw_pool)) then
            begin
                SetLength(char_scores, Length(raw_pool));
            end;
            if (not get_cached_char_lm_scores(char_texts,
                char_suffix_scores, clsm_suffix, '')) or
                (Length(char_suffix_scores) <> Length(raw_pool)) then
            begin
                char_suffix_scores := Copy(char_scores);
            end;
            if has_left_context and get_cached_char_lm_scores(char_texts,
                char_context_scores, clsm_context, left_context) and
                (Length(char_context_scores) = Length(raw_pool)) then
            begin
                { Use the contextual batch. }
            end
            else
            begin
                char_context_scores := Copy(char_suffix_scores);
            end;
        end;
        for raw_idx := 0 to High(raw_pool) do
        begin
            raw_pool[raw_idx].char_lm_score := char_scores[raw_idx];
            raw_pool[raw_idx].char_lm_suffix_score :=
                char_suffix_scores[raw_idx];
            raw_pool[raw_idx].char_lm_context_score :=
                char_context_scores[raw_idx];
            { Rank 4+ exact anchors are isolated recall probes. Counting them
              as independent support changes the learned features of every
              established candidate that shares their segmentation, even
              when the probe itself never reaches the visible ranks. }
            if (raw_pool[raw_idx].source_kind <> lcps_exact_edge_chain) and
                (raw_pool[raw_idx].anchor_start <> -2) and
                ((raw_pool[raw_idx].anchor_units <= 0) or
                (raw_pool[raw_idx].anchor_exact_rank < 4)) then
            begin
                signature_totals.TryGetValue(
                    raw_pool[raw_idx].segmentation_signature, idx);
                signature_totals.AddOrSetValue(
                    raw_pool[raw_idx].segmentation_signature, idx + 1);
            end;
        end;
        note_pool_phase('charlm');
        populate_word_lm_features;
        note_pool_phase('wordlm');
        populate_consensus_features;
        note_pool_phase('consensus');
        for raw_idx := 0 to High(raw_pool) do
        begin
            signature_totals.TryGetValue(
                raw_pool[raw_idx].segmentation_signature, idx);
            raw_pool[raw_idx].signature_support := idx;
        end;

        sync_baseline_pool_features;
        full_raw_pool := raw_pool;

        { Rank the pre-edge-recall pool from the same cached feature batch.
          This shadow pass performs no lattice, exact or character-LM query. }
        raw_pool := baseline_raw_pool;
        build_runtime_pool_from_raw(False);
        score_local_pairwise_candidates;
        copy_runtime_pool_to_internal(baseline_candidates,
            baseline_source_indices);
        apply_long_final_visible_candidate_ranking(baseline_candidates,
            baseline_source_indices, True);
        baseline_top1 := '';
        baseline_top2 := '';
        if Length(baseline_candidates) > 0 then
        begin
            baseline_top1 := Trim(baseline_candidates[0].text);
        end;
        if Length(baseline_candidates) > 1 then
        begin
            baseline_top2 := Trim(baseline_candidates[1].text);
        end;
        note_pool_phase('shadow');

        raw_pool := full_raw_pool;
        build_runtime_pool_from_raw(True, True);
        note_pool_phase('select');
        score_local_pairwise_candidates;
        note_pool_phase('localpair');
        copy_runtime_pool_to_internal(internal_candidates,
            internal_source_indices);
        apply_long_final_visible_candidate_ranking(internal_candidates,
            internal_source_indices, True, baseline_top1, baseline_top2,
            True);
        note_pool_phase('rank');

        SetLength(merged_candidates, original_visible_count);
        SetLength(merged_sources, original_visible_count);
        merged_count := 0;
        complete_count := 0;
        for idx := 0 to High(internal_candidates) do
        begin
            if (complete_count >= c_visible_complete_limit) or
                (not candidate_is_complete(internal_candidates[idx])) then
            begin
                Continue;
            end;
            if not generated_pool_candidate_has_visible_evidence(
                internal_candidates[idx].text, complete_count = 0) then
            begin
                Continue;
            end;
            source_idx := -1;
            if idx < Length(internal_source_indices) then
            begin
                source_idx := internal_source_indices[idx];
            end;
            if not try_append_merged_candidate(internal_candidates[idx],
                source_idx) then
            begin
                Continue;
            end;
            Inc(complete_count);
            if merged_count >= original_visible_count then
            begin
                Break;
            end;
        end;
        for idx := 0 to High(original_candidates) do
        begin
            if (merged_count >= original_visible_count) or
                candidate_is_complete(original_candidates[idx]) then
            begin
                Continue;
            end;
            source_idx := -1;
            if idx < Length(original_source_indices) then
            begin
                source_idx := original_source_indices[idx];
            end;
            if not try_append_merged_candidate(original_candidates[idx],
                source_idx) then
            begin
                Continue;
            end;
        end;
        for idx := 0 to High(original_candidates) do
        begin
            if merged_count >= original_visible_count then
            begin
                Break;
            end;
            source_idx := -1;
            if idx < Length(original_source_indices) then
            begin
                source_idx := original_source_indices[idx];
            end;
            try_append_merged_candidate(original_candidates[idx],
                source_idx);
        end;
        SetLength(merged_candidates, merged_count);
        SetLength(merged_sources, merged_count);
        candidates := merged_candidates;
        source_indices := merged_sources;
        note_pool_phase('merge');
    finally
        emitted_candidate_identities.Free;
        exact_edge_model_score_cache.Free;
        lattice_trigram_transition_cache.Free;
        lattice_pair_transition_cache.Free;
        word_lm_cache.Free;
        exact_cache.Free;
        signature_totals.Free;
        selected_signature_counts.Free;
        selected_texts.Free;
        baseline_index_by_text.Free;
        raw_index_by_text.Free;
    end;
end;

procedure TncEngine.apply_long_final_visible_candidate_ranking(
    var candidates: TncCandidateList;
    var source_indices: TArray<Integer>;
    const unified_pool_final: Boolean;
    const exact_edge_baseline_top1: string;
    const exact_edge_baseline_top2: string;
    const run_host_neural_reranker: Boolean);
const
    c_ranking_stage_legacy_input = 0;
    c_ranking_stage_unified_ranker = 1;
    c_ranking_stage_anchor_guard = 2;
    c_ranking_stage_top2_pairwise = 3;
    c_ranking_stage_exact_anchor_pairwise = 4;
    c_ranking_stage_bidirectional_difference = 5;
    c_ranking_stage_second_slot_bidirectional = 6;
    c_ranking_stage_final_pairwise_residual = 7;
    c_ranking_stage_post_static_difference = 8;
    c_ranking_stage_ranker_second_slot_recovery = 9;
    c_ranking_stage_bidirectional_second_slot = 10;
    c_ranking_stage_post_static_bidirectional = 11;
    c_ranking_stage_settled_top2_residual = 12;
    c_ranking_stage_unified_top2_arbiter = 13;
    c_ranking_stage_deep_local_repair = 14;
    c_ranking_stage_count = 15;
    c_deep_local_repair_max_rank = 32;
    c_deep_local_repair_min_char_lm_gain = 500;
    c_deep_local_repair_min_suffix_lm_gain = 300;
type
    TncLongExactEdgeAuditorBaseFeatures = array[0..44] of Double;
    TncLongExactEdgeFallbackAuditorBaseFeatures = array[0..65] of Double;
var
    profile: Integer;
    candidate_count: Integer;
    candidate_idx: Integer;
    source_idx: Integer;
    insert_idx: Integer;
    current_idx: Integer;
    chain_idx: Integer;
    pool_idx: Integer;
    normalized_query: string;
    latest_choice_text: string;
    previous_previous_text: string;
    previous_text: string;
    left_context: string;
    has_left_context: Boolean;
    char_scores_ok: Boolean;
    candidate_texts: TArray<string>;
    char_scores: TArray<Integer>;
    char_suffix_scores: TArray<Integer>;
    char_context_scores: TArray<Integer>;
    candidate_chain_indices: TArray<Integer>;
    candidate_pool_indices: TArray<Integer>;
    missing_candidate_indices: TArray<Integer>;
    missing_candidate_texts: TArray<string>;
    missing_char_scores: TArray<Integer>;
    missing_char_suffix_scores: TArray<Integer>;
    missing_char_context_scores: TArray<Integer>;
    rank_features: TArray<TncLongFinalRankerFeatures>;
    rank_scores: TArray<Int64>;
    ordered_indices: TArray<Integer>;
    final_ranks: TArray<Integer>;
    legacy_candidates: TncCandidateList;
    legacy_paths: TArray<string>;
    ordered_candidates: TncCandidateList;
    legacy_source_indices: TArray<Integer>;
    ordered_source_indices: TArray<Integer>;
    neural_candidates: TncLongFinalCandidateDebugArray;
    neural_candidate_indices: TArray<Integer>;
    neural_selected_index: Integer;
    path_structure: TncLongPathStructureCacheValue;
    top_chain_score: Int64;
    abstain_features: TncLongFinalAbstainFeatures;
    abstain_score: Int64;
    apply_ranker: Boolean;
    complete_count: Integer;
    chain_count: Integer;
    top_idx: Integer;
    second_idx: Integer;
    third_idx: Integer;
    lowest_idx: Integer;
    missing_count: Integer;
    missing_idx: Integer;
    runtime_features_match: Boolean;
    runtime_path_features_match: Boolean;
    visible_pairwise_features: TncLongVisiblePairwiseResidualFeatures;
    visible_pairwise_score: Int64;
    best_visible_pairwise_score: Int64;
    best_visible_pairwise_position: Integer;
    visible_challenger_count: Integer;
    baseline_ranker_applied: Boolean;
    complete_pool_final_pairwise_debug_scores: TArray<Int64>;
    complete_pool_final_pairwise_current_rank_debug: TArray<Integer>;
    complete_pool_final_pairwise_promoted_index: Integer;
    complete_pool_post_static_difference_debug_scores: TArray<Int64>;
    complete_pool_post_static_difference_current_rank_debug: TArray<Integer>;
    complete_pool_post_static_difference_promoted_index: Integer;
    exact_edge_auditor_features: TncLongExactEdgeAuditorFeatures;
    exact_edge_auditor_score: Double;
    exact_edge_auditor_threshold: Double;
    exact_edge_baseline_top1_index: Integer;
    exact_edge_baseline_top2_index: Integer;
    exact_edge_current_top_index: Integer;
    exact_edge_auditor_source_kind: Integer;
    exact_edge_auditor_decision: Integer;
    exact_edge_fallback_auditor_features:
        TncLongExactEdgeFallbackAuditorFeatures;
    exact_edge_fallback_auditor_score: Double;
    exact_edge_fallback_auditor_threshold: Double;
    exact_edge_fallback_auditor_decision: Integer;
    exact_edge_pre_auditor_ranks: TArray<Integer>;
    local_difference_features: TncLongLocalDifferenceResidualFeatures;
    local_difference_score: Int64;
    best_local_difference_score: Int64;
    best_local_difference_position: Integer;
    local_difference_challenger_count: Integer;
    local_relation_valid: TArray<Boolean>;
    local_different_units: TArray<Integer>;
    local_different_runs: TArray<Integer>;
    local_max_different_run: TArray<Integer>;
    local_same_prefix_units: TArray<Integer>;
    local_same_suffix_units: TArray<Integer>;
    local_difference_span_units: TArray<Integer>;
    local_difference_start: TArray<Integer>;
    local_difference_end: TArray<Integer>;
    local_window_score_offset: TArray<Integer>;
    local_window_texts: TArray<string>;
    local_window_scores: TArray<Integer>;
    top_local_lm_scores: TArray<Integer>;
    candidate_local_lm_scores: TArray<Integer>;
    local_window_count: Integer;
    local_window_start: Integer;
    local_window_end: Integer;
    local_radius: Integer;
    local_char_idx: Integer;
    local_current_run: Integer;
    local_top_text: string;
    local_candidate_text: string;
    local_scores_ok: Boolean;
    local_difference_debug_scores: TArray<Int64>;
    local_difference_promoted_index: Integer;
    local_difference_current_rank_debug: TArray<Integer>;
    local_top_lm_debug: array[0..3] of TArray<Integer>;
    local_candidate_lm_debug: array[0..3] of TArray<Integer>;
    complete_pool_pairwise_position: Integer;
    complete_pool_pairwise_target_position: Integer;
    top2_pairwise_features: TncLongTop2PairwiseSwapFeatures;
    top2_pairwise_score: Int64;
    top2_pairwise_candidate_index: Integer;
    top2_pairwise_candidate_position: Integer;
    top2_pairwise_top_pool_index: Integer;
    top2_pairwise_candidate_pool_index: Integer;
    top2_pairwise_eligible: Boolean;
    top2_pairwise_swapped: Boolean;
    top2_different_units: Integer;
    top2_different_runs: Integer;
    top2_max_different_run: Integer;
    top2_same_prefix_units: Integer;
    top2_same_suffix_units: Integer;
    top2_difference_span_units: Integer;
    top2_top_local_lm_scores: TArray<Integer>;
    top2_candidate_local_lm_scores: TArray<Integer>;
    top2_query_parts: TncPinyinParseResult;
    top2_query_syllables: TncLongTop2DifferencePinyinSyllables;
    top2_query_idx: Integer;
    exact_anchor_pairwise_debug_scores: TArray<Int64>;
    exact_anchor_pairwise_current_rank_debug: TArray<Integer>;
    exact_anchor_top_local_word_lm_debug:
        TArray<TncLongExactAnchorLocalWordLmScores>;
    exact_anchor_candidate_local_word_lm_debug:
        TArray<TncLongExactAnchorLocalWordLmScores>;
    exact_anchor_pairwise_moved_index: Integer;
    exact_anchor_pairwise_target_rank: Integer;
    bidirectional_top1_swapped: Boolean;
    second_slot_selector_base_positions: TArray<Integer>;
    ranking_stage_capture_count: Integer;
    ranking_stage_ids: array[0..c_ranking_stage_count - 1] of Integer;
    ranking_stage_top_indices: array[0..c_ranking_stage_count - 1] of Integer;
    ranking_stage_second_indices: array[0..c_ranking_stage_count - 1] of Integer;
    ranking_top1_votes: TArray<Integer>;
    ranking_top2_votes: TArray<Integer>;
    ranking_first_top_stage: TArray<Integer>;
    ranking_last_top_stage: TArray<Integer>;
    ranking_top_entry_count: TArray<Integer>;
    settled_top2_feature_cache_valid: Boolean;
    settled_top2_feature_cache_top_index: Integer;
    settled_top2_feature_cache_second_index: Integer;
    settled_top2_feature_cache_forward:
        TncLongSettledTop2ResidualFeatures;
    settled_top2_feature_cache_reverse:
        TncLongSettledTop2ResidualFeatures;
    unified_top2_arbiter_score_value: Double;
    unified_top2_arbiter_family_value: Integer;
    unified_top2_arbiter_swapped: Boolean;
    unified_top2_arbiter_feature_values: TArray<Double>;
    ranking_phase_tick: Int64;
    ranking_phase_frequency: Int64;

    function build_text_unit_window(const units: TArray<string>;
        const start_index, end_index: Integer;
        const reverse_units: Boolean): string;
    var
        first_index: Integer;
        after_last_index: Integer;
        unit_index: Integer;
        unit_length: Integer;
        result_length: Integer;
        write_position: Integer;
    begin
        Result := '';
        first_index := Max(0, start_index);
        after_last_index := Min(Length(units), end_index);
        if first_index >= after_last_index then
        begin
            Exit;
        end;
        result_length := 0;
        for unit_index := first_index to after_last_index - 1 do
        begin
            Inc(result_length, Length(units[unit_index]));
        end;
        if result_length <= 0 then
        begin
            Exit;
        end;
        SetLength(Result, result_length);
        write_position := 1;
        if reverse_units then
        begin
            for unit_index := after_last_index - 1 downto first_index do
            begin
                unit_length := Length(units[unit_index]);
                if unit_length <= 0 then
                begin
                    Continue;
                end;
                Move(units[unit_index][1], Result[write_position],
                    unit_length * SizeOf(Char));
                Inc(write_position, unit_length);
            end;
        end
        else
        begin
            for unit_index := first_index to after_last_index - 1 do
            begin
                unit_length := Length(units[unit_index]);
                if unit_length <= 0 then
                begin
                    Continue;
                end;
                Move(units[unit_index][1], Result[write_position],
                    unit_length * SizeOf(Char));
                Inc(write_position, unit_length);
            end;
        end;
    end;

    function find_chain_candidate_index(const candidate_text: string): Integer;
    var
        idx_local: Integer;
    begin
        Result := -1;
        for idx_local := 0 to High(m_runtime_long_chain_candidates) do
        begin
            if m_runtime_long_chain_candidates[
                idx_local].retained_exact_edge_count > 0 then
            begin
                Continue;
            end;
            if SameText(Trim(m_runtime_long_chain_candidates[idx_local].text),
                Trim(candidate_text)) then
            begin
                Exit(idx_local);
            end;
        end;
    end;

    function find_complete_pool_candidate_index(
        const candidate_text: string): Integer;
    var
        idx_local: Integer;
    begin
        Result := -1;
        for idx_local := 0 to High(
            m_runtime_long_complete_pool_candidates) do
        begin
            if SameText(Trim(
                m_runtime_long_complete_pool_candidates[idx_local].candidate.text),
                Trim(candidate_text)) then
            begin
                Exit(idx_local);
            end;
        end;
    end;

    function is_local_rerank_candidate(const candidate_text: string): Boolean;
    begin
        Result := (m_long_local_rerank_generated_text <> '') and
            SameText(Trim(m_long_local_rerank_generated_text),
            Trim(candidate_text)) and
            SameText(m_long_local_rerank_composition_text,
            m_composition_text) and
            SameText(m_long_local_rerank_lookup_key, m_last_lookup_key);
    end;

    function index_is_better(const left_idx: Integer;
        const right_idx: Integer): Boolean;
    begin
        Result := rank_scores[left_idx] > rank_scores[right_idx];
        if rank_scores[left_idx] = rank_scores[right_idx] then
        begin
            Result := left_idx < right_idx;
        end;
    end;

    procedure get_text_relation(const candidate_index: Integer;
        const top_index: Integer; out different_units: Integer;
        out different_runs: Integer; out max_different_run: Integer;
        out same_prefix_units: Integer; out same_suffix_units: Integer;
        out difference_span_units: Integer);
    var
        candidate_units: TArray<string>;
        top_units: TArray<string>;
        shared_units: Integer;
        unit_idx: Integer;
        current_run: Integer;
    begin
        candidate_units := split_text_units(
            Trim(legacy_candidates[candidate_index].text));
        top_units := split_text_units(
            Trim(legacy_candidates[top_index].text));
        shared_units := Min(Length(candidate_units), Length(top_units));
        different_units := 0;
        different_runs := 0;
        max_different_run := 0;
        same_prefix_units := 0;
        same_suffix_units := 0;
        current_run := 0;

        while (same_prefix_units < shared_units) and
            SameText(candidate_units[same_prefix_units],
            top_units[same_prefix_units]) do
        begin
            Inc(same_prefix_units);
        end;
        while (same_suffix_units < shared_units - same_prefix_units) and
            SameText(candidate_units[High(candidate_units) -
            same_suffix_units], top_units[High(top_units) -
            same_suffix_units]) do
        begin
            Inc(same_suffix_units);
        end;

        for unit_idx := 0 to shared_units - 1 do
        begin
            if not SameText(candidate_units[unit_idx], top_units[unit_idx]) then
            begin
                Inc(different_units);
                Inc(current_run);
                if current_run = 1 then
                begin
                    Inc(different_runs);
                end;
                max_different_run := Max(max_different_run, current_run);
            end
            else
            begin
                current_run := 0;
            end;
        end;
        if Length(candidate_units) <> Length(top_units) then
        begin
            Inc(different_runs);
            Inc(different_units,
                Abs(Length(candidate_units) - Length(top_units)));
            max_different_run := Max(max_different_run,
                Abs(Length(candidate_units) - Length(top_units)));
        end;
        difference_span_units := Max(0,
            Max(Length(candidate_units), Length(top_units)) -
            same_prefix_units - same_suffix_units);
    end;

    procedure fill_exact_edge_auditor_base_features(
        const features: TncLongFinalRankerFeatures;
        out values: TncLongExactEdgeAuditorBaseFeatures);
    begin
        values[0] := features.candidate_score;
        values[1] := features.path_segments;
        values[2] := features.path_single_segments;
        values[3] := features.path_max_segment_units;
        values[4] := features.char_lm_score;
        values[5] := features.char_lm_suffix_score;
        values[6] := features.char_lm_context_score;
        values[7] := features.word_lm_bonus;
        values[8] := features.word_lm_boundary_count;
        values[9] := features.word_lm_boundary_min;
        values[10] := features.word_lm_boundary_max;
        values[11] := features.word_lm_boundary_first;
        values[12] := features.word_lm_boundary_last;
        values[13] := features.word_lm_supported_ratio;
        values[14] := features.word_lm_strong_ratio;
        values[15] := features.word_lm_trigram_ratio;
        values[16] := features.word_lm_zero_count;
        values[17] := features.score_per_unit;
        values[18] := features.complete_pool_rank;
        values[19] := features.complete_pool_seed_rank;
        values[20] := Ord(features.complete_pool_original);
        values[21] := features.complete_pool_substitutions;
        values[22] := Ord(features.complete_pool_anchor_present);
        values[23] := features.complete_pool_anchor_units;
        values[24] := features.complete_pool_anchor_exact_rank;
        values[25] := features.complete_pool_anchor_weight_gain;
        values[26] := features.complete_pool_pair_evidence;
        values[27] := features.complete_pool_proper_name_confidence;
        values[28] := features.complete_pool_signature_support;
        values[29] := features.complete_pool_consensus_support;
        values[30] := features.complete_pool_consensus_seed_count;
        values[31] := features.complete_pool_consensus_support_mean;
        values[32] := features.complete_pool_consensus_support_min;
        values[33] := features.complete_pool_consensus_majority_units;
        values[34] := features.complete_pool_consensus_unanimous_units;
        values[35] := features.complete_pool_consensus_nearest_distance;
        values[36] := features.complete_pool_consensus_mean_distance;
        values[37] := features.complete_pool_edge_model_anchor_count;
        values[38] := features.complete_pool_edge_model_score_total;
        values[39] := features.complete_pool_edge_model_score_max;
        values[40] := features.complete_pool_edge_model_word_count;
        values[41] := features.complete_pool_edge_model_word_score_total;
        values[42] := features.complete_pool_edge_model_word_score_min;
        values[43] := features.complete_pool_edge_model_word_score_max;
        values[44] := features.complete_pool_edge_model_word_score_mean;
    end;

    procedure fill_exact_edge_fallback_auditor_base_features(
        const features: TncLongFinalRankerFeatures;
        out values: TncLongExactEdgeFallbackAuditorBaseFeatures);
    var
        primary_values: TncLongExactEdgeAuditorBaseFeatures;
        primary_idx: Integer;
    begin
        fill_exact_edge_auditor_base_features(features, primary_values);
        for primary_idx := 0 to High(primary_values) do
        begin
            values[primary_idx] := primary_values[primary_idx];
        end;
        values[45] := features.dict_weight;
        values[46] := Ord(features.has_dict_weight);
        values[47] := Ord(features.source_chain);
        values[48] := Ord(features.source_pattern);
        values[49] := Ord(features.source_local_rerank);
        values[50] := features.legacy_rank;
        values[51] := features.chain_rank;
        values[52] := features.chain_score_gap;
        values[53] := features.path_confidence_score;
        values[54] := features.path_confidence_tier;
        values[55] := features.char_lm_context_gain;
        values[56] := features.query_path_bonus;
        values[57] := features.query_path_penalty;
        values[58] := features.dict_weight_per_unit;
        values[59] := Ord(features.complete_dictionary);
        values[60] := Ord(features.complete_chain);
        values[61] := features.complete_pool_source_kind;
        values[62] := features.complete_pool_changed_position;
        values[63] := features.complete_pool_consensus_changed_support;
        values[64] := Ord(
            features.complete_pool_consensus_changed_top_match);
        values[65] := features.complete_pool_local_pairwise_score;
    end;

    procedure build_exact_edge_auditor_features(
        const challenger_index: Integer;
        const baseline_index: Integer;
        out features: TncLongExactEdgeAuditorFeatures);
    var
        challenger_values: TncLongExactEdgeAuditorBaseFeatures;
        baseline_values: TncLongExactEdgeAuditorBaseFeatures;
        feature_idx: Integer;
        different_units: Integer;
        different_runs: Integer;
        max_different_run: Integer;
        same_prefix_units: Integer;
        same_suffix_units: Integer;
        difference_span_units: Integer;
    begin
        FillChar(features, SizeOf(features), 0);
        fill_exact_edge_auditor_base_features(
            rank_features[challenger_index], challenger_values);
        fill_exact_edge_auditor_base_features(
            rank_features[baseline_index], baseline_values);
        features[0] := rank_features[challenger_index].input_syllable_count;
        features[1] := 1.0;
        for feature_idx := 0 to High(challenger_values) do
        begin
            features[2 + feature_idx] := challenger_values[feature_idx];
            features[47 + feature_idx] := baseline_values[feature_idx];
            features[92 + feature_idx] := challenger_values[feature_idx] -
                baseline_values[feature_idx];
        end;
        get_text_relation(challenger_index, baseline_index, different_units,
            different_runs, max_different_run, same_prefix_units,
            same_suffix_units, difference_span_units);
        features[137] := different_units;
        features[138] := different_runs;
        features[139] := max_different_run;
        features[140] := same_prefix_units;
        features[141] := same_suffix_units;
    end;

    procedure build_exact_edge_fallback_auditor_features(
        const challenger_index: Integer;
        const baseline_index: Integer;
        out features: TncLongExactEdgeFallbackAuditorFeatures);
    var
        challenger_values: TncLongExactEdgeFallbackAuditorBaseFeatures;
        baseline_values: TncLongExactEdgeFallbackAuditorBaseFeatures;
        feature_idx: Integer;
        different_units: Integer;
        different_runs: Integer;
        max_different_run: Integer;
        same_prefix_units: Integer;
        same_suffix_units: Integer;
        difference_span_units: Integer;
    begin
        FillChar(features, SizeOf(features), 0);
        fill_exact_edge_fallback_auditor_base_features(
            rank_features[challenger_index], challenger_values);
        fill_exact_edge_fallback_auditor_base_features(
            rank_features[baseline_index], baseline_values);
        features[0] := rank_features[challenger_index].input_syllable_count;
        features[1] := 1.0;
        for feature_idx := 0 to High(challenger_values) do
        begin
            features[2 + feature_idx] := challenger_values[feature_idx];
            features[68 + feature_idx] := baseline_values[feature_idx];
            features[134 + feature_idx] := challenger_values[feature_idx] -
                baseline_values[feature_idx];
        end;
        get_text_relation(challenger_index, baseline_index, different_units,
            different_runs, max_different_run, same_prefix_units,
            same_suffix_units, difference_span_units);
        features[200] := different_units;
        features[201] := different_runs;
        features[202] := max_different_run;
        features[203] := same_prefix_units;
        features[204] := same_suffix_units;
    end;

    function find_legacy_candidate_index(const candidate_text: string): Integer;
    var
        legacy_idx: Integer;
    begin
        Result := -1;
        if Trim(candidate_text) = '' then
        begin
            Exit;
        end;
        for legacy_idx := 0 to High(legacy_candidates) do
        begin
            if SameText(Trim(legacy_candidates[legacy_idx].text),
                Trim(candidate_text)) then
            begin
                Exit(legacy_idx);
            end;
        end;
    end;

    procedure move_ordered_candidate_to_position(
        const candidate_index: Integer; const target_position: Integer);
    var
        current_position: Integer;
        move_idx: Integer;
    begin
        current_position := -1;
        for move_idx := 0 to High(ordered_indices) do
        begin
            if ordered_indices[move_idx] = candidate_index then
            begin
                current_position := move_idx;
                Break;
            end;
        end;
        if (current_position < 0) or (target_position < 0) or
            (target_position >= Length(ordered_indices)) or
            (current_position = target_position) then
        begin
            Exit;
        end;
        if current_position > target_position then
        begin
            for move_idx := current_position downto target_position + 1 do
            begin
                ordered_indices[move_idx] := ordered_indices[move_idx - 1];
            end;
        end
        else
        begin
            for move_idx := current_position to target_position - 1 do
            begin
                ordered_indices[move_idx] := ordered_indices[move_idx + 1];
            end;
        end;
        ordered_indices[target_position] := candidate_index;
    end;

    procedure apply_exact_edge_final_auditor;
    begin
        exact_edge_auditor_decision := -1;
        exact_edge_fallback_auditor_decision := -1;
        if (not unified_pool_final) or
            (Trim(exact_edge_baseline_top1) = '') or
            (Length(ordered_indices) <= 0) then
        begin
            Exit;
        end;
        exact_edge_current_top_index := ordered_indices[0];
        exact_edge_baseline_top1_index := find_legacy_candidate_index(
            exact_edge_baseline_top1);
        if (exact_edge_baseline_top1_index < 0) or
            (exact_edge_current_top_index = exact_edge_baseline_top1_index) or
            rank_features[exact_edge_current_top_index].source_user or
            rank_features[exact_edge_current_top_index].complete_user or
            rank_features[exact_edge_baseline_top1_index].source_user or
            rank_features[exact_edge_baseline_top1_index].complete_user then
        begin
            Exit;
        end;

        exact_edge_auditor_source_kind :=
            rank_features[exact_edge_current_top_index].
            complete_pool_source_kind;
        case exact_edge_auditor_source_kind of
            Ord(lcps_visible):
                exact_edge_auditor_threshold := -0.021090963316034224;
            Ord(lcps_local_repair):
                exact_edge_auditor_threshold := -0.27598497892068496;
            Ord(lcps_exact_lattice):
                exact_edge_auditor_threshold := 0.5126426148990523;
            Ord(lcps_exact_edge_chain):
                exact_edge_auditor_threshold := 0.3203492695358477;
        else
            Exit;
        end;
        build_exact_edge_auditor_features(exact_edge_current_top_index,
            exact_edge_baseline_top1_index, exact_edge_auditor_features);
        exact_edge_auditor_score := long_exact_edge_auditor_score(
            exact_edge_auditor_features);
        if exact_edge_auditor_score >= exact_edge_auditor_threshold then
        begin
            exact_edge_auditor_decision := 1;
            apply_ranker := True;
            Exit;
        end;

        exact_edge_auditor_decision := 0;
        if c_long_exact_edge_fallback_auditor_enabled and
            (exact_edge_auditor_source_kind = Ord(lcps_exact_lattice)) then
        begin
            build_exact_edge_fallback_auditor_features(
                exact_edge_current_top_index,
                exact_edge_baseline_top1_index,
                exact_edge_fallback_auditor_features);
            exact_edge_fallback_auditor_score :=
                long_exact_edge_fallback_auditor_score(
                exact_edge_fallback_auditor_features);
            exact_edge_fallback_auditor_threshold :=
                c_long_exact_edge_fallback_auditor_threshold;
            if exact_edge_fallback_auditor_score >=
                exact_edge_fallback_auditor_threshold then
            begin
                exact_edge_fallback_auditor_decision := 1;
                exact_edge_auditor_decision := 2;
                apply_ranker := True;
                Exit;
            end;
            exact_edge_fallback_auditor_decision := 0;
        end;

        move_ordered_candidate_to_position(
            exact_edge_baseline_top1_index, 0);
        exact_edge_baseline_top2_index := find_legacy_candidate_index(
            exact_edge_baseline_top2);
        if (exact_edge_baseline_top2_index >= 0) and
            (exact_edge_baseline_top2_index <>
            exact_edge_baseline_top1_index) and
            (Length(ordered_indices) > 1) then
        begin
            move_ordered_candidate_to_position(
                exact_edge_baseline_top2_index, 1);
        end;
    end;

    function get_top2_cached_span_lm_scores(const top_index: Integer;
        const candidate_index: Integer; out top_scores: TArray<Integer>;
        out candidate_scores: TArray<Integer>): Boolean;
    var
        top_units: TArray<string>;
        candidate_units: TArray<string>;
        window_texts: TArray<string>;
        window_scores: TArray<Integer>;
        first_difference: Integer;
        difference_end: Integer;
        window_start: Integer;
        window_end: Integer;
        radius: Integer;
        unit_idx: Integer;
        top_window: string;
        candidate_window: string;
    begin
        Result := False;
        SetLength(top_scores, 4);
        SetLength(candidate_scores, 4);
        top_units := split_text_units(
            Trim(legacy_candidates[top_index].text));
        candidate_units := split_text_units(
            Trim(legacy_candidates[candidate_index].text));
        if (Length(top_units) = 0) or
            (Length(top_units) <> Length(candidate_units)) then
        begin
            Exit;
        end;

        first_difference := 0;
        while (first_difference < Length(top_units)) and
            SameText(top_units[first_difference],
            candidate_units[first_difference]) do
        begin
            Inc(first_difference);
        end;
        if first_difference >= Length(top_units) then
        begin
            Exit;
        end;
        difference_end := Length(top_units);
        while (difference_end > first_difference) and
            SameText(top_units[difference_end - 1],
            candidate_units[difference_end - 1]) do
        begin
            Dec(difference_end);
        end;

        SetLength(window_texts, 8);
        for radius := 0 to 3 do
        begin
            window_start := Max(0, first_difference - radius);
            window_end := Min(Length(top_units), difference_end + radius);
            top_window := '';
            candidate_window := '';
            for unit_idx := window_start to window_end - 1 do
            begin
                top_window := top_window + top_units[unit_idx];
                candidate_window := candidate_window +
                    candidate_units[unit_idx];
            end;
            window_texts[radius * 2] := top_window;
            window_texts[radius * 2 + 1] := candidate_window;
        end;

        Result := (m_dictionary <> nil) and
            get_cached_char_lm_scores(window_texts, window_scores,
            clsm_span, '') and (Length(window_scores) = 8);
        if not Result then
        begin
            Exit;
        end;
        for radius := 0 to 3 do
        begin
            top_scores[radius] := window_scores[radius * 2];
            candidate_scores[radius] := window_scores[radius * 2 + 1];
        end;
    end;

    procedure apply_exact_anchor_pairwise_ranking;
    var
        top_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        best_position_local: Integer;
        best_score_local: Double;
        score_local: Double;
        strict_threshold_local: Double;
        move_result_local: Integer;
        relation_local: TncLongExactAnchorRelationFeatures;
        relations_local: TArray<TncLongExactAnchorRelationFeatures>;
        relation_valid_local: TArray<Boolean>;
        window_score_offsets_local: TArray<Integer>;
        window_texts_local: TArray<string>;
        window_scores_local: TArray<Integer>;
        top_scores_local: TArray<Integer>;
        candidate_scores_local: TArray<Integer>;
        top_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        candidate_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        query_parts_local: TncPinyinParseResult;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        window_count_local: Integer;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_local: Integer;
        top_window_local: string;
        candidate_window_local: string;
        features_local: TncLongExactAnchorPairwiseFeatures;

        procedure get_local_word_lm_scores(const encoded_path: string;
            const relation: TncLongExactAnchorRelationFeatures;
            out scores: TncLongExactAnchorLocalWordLmScores);
        const
            c_strong_word_lm_bonus = 360;
        var
            parts_local: TArray<string>;
            part_starts_local: TArray<Integer>;
            part_units_local: TArray<Integer>;
            boundary_idx_local: Integer;
            part_idx_local: Integer;
            syllable_idx_local: Integer;
            cursor_local: Integer;
            left_start_local: Integer;
            left_end_local: Integer;
            right_start_local: Integer;
            right_end_local: Integer;
            difference_start_local: Integer;
            difference_end_local: Integer;
            query_local: string;
            path_local: string;
            bigram_local: Integer;
            trigram_local: Integer;
            value_local: Integer;
        begin
            FillChar(scores, SizeOf(scores), 0);
            if (m_dictionary = nil) or (Trim(encoded_path) = '') or
                (Length(query_parts_local) = 0) then
            begin
                Exit;
            end;
            parts_local := encoded_path.Split([c_segment_path_separator]);
            if Length(parts_local) < 2 then
            begin
                Exit;
            end;
            SetLength(part_starts_local, Length(parts_local));
            SetLength(part_units_local, Length(parts_local));
            cursor_local := 0;
            for part_idx_local := 0 to High(parts_local) do
            begin
                parts_local[part_idx_local] := Trim(parts_local[part_idx_local]);
                part_starts_local[part_idx_local] := cursor_local;
                part_units_local[part_idx_local] :=
                    get_candidate_text_unit_count(parts_local[part_idx_local]);
                if part_units_local[part_idx_local] <= 0 then
                begin
                    Exit;
                end;
                Inc(cursor_local, part_units_local[part_idx_local]);
            end;
            if cursor_local <> Length(query_parts_local) then
            begin
                Exit;
            end;

            difference_start_local := relation.same_prefix_units;
            difference_end_local := Length(query_parts_local) -
                relation.same_suffix_units;
            scores[2] := MaxInt;
            for boundary_idx_local := 1 to High(parts_local) do
            begin
                left_start_local := part_starts_local[boundary_idx_local - 1];
                left_end_local := part_starts_local[boundary_idx_local];
                right_start_local := left_end_local;
                right_end_local := right_start_local +
                    part_units_local[boundary_idx_local];
                if not (((left_start_local < difference_end_local) and
                    (left_end_local > difference_start_local)) or
                    ((right_start_local < difference_end_local) and
                    (right_end_local > difference_start_local))) then
                begin
                    Continue;
                end;

                query_local := '';
                for syllable_idx_local := left_start_local to
                    right_end_local - 1 do
                begin
                    query_local := query_local + normalize_pinyin_text(
                        query_parts_local[syllable_idx_local].text);
                end;
                path_local := parts_local[boundary_idx_local - 1] +
                    c_segment_path_separator + parts_local[boundary_idx_local];
                bigram_local := m_dictionary.get_lm_transition_bonus(
                    query_local, path_local);
                trigram_local := 0;
                if boundary_idx_local >= 2 then
                begin
                    left_start_local := part_starts_local[
                        boundary_idx_local - 2];
                    query_local := '';
                    for syllable_idx_local := left_start_local to
                        right_end_local - 1 do
                    begin
                        query_local := query_local + normalize_pinyin_text(
                            query_parts_local[syllable_idx_local].text);
                    end;
                    path_local := parts_local[boundary_idx_local - 2] +
                        c_segment_path_separator +
                        parts_local[boundary_idx_local - 1] +
                        c_segment_path_separator +
                        parts_local[boundary_idx_local];
                    trigram_local := m_dictionary.get_lm_transition_bonus(
                        query_local, path_local);
                end;
                value_local := Max(bigram_local, trigram_local);
                Inc(scores[0]);
                Inc(scores[1], value_local);
                scores[2] := Min(scores[2], value_local);
                scores[3] := Max(scores[3], value_local);
                if scores[0] = 1 then
                begin
                    scores[4] := value_local;
                end;
                scores[5] := value_local;
                if value_local > 0 then
                begin
                    Inc(scores[6]);
                end;
                if value_local >= c_strong_word_lm_bonus then
                begin
                    Inc(scores[7]);
                end;
                if (trigram_local > 0) and
                    (trigram_local >= bigram_local) then
                begin
                    Inc(scores[8]);
                end;
            end;
            if scores[0] = 0 then
            begin
                scores[2] := 0;
            end;
        end;
    begin
        exact_anchor_pairwise_moved_index := -1;
        exact_anchor_pairwise_target_rank := 0;
        SetLength(exact_anchor_pairwise_debug_scores, candidate_count);
        SetLength(exact_anchor_pairwise_current_rank_debug,
            candidate_count);
        SetLength(exact_anchor_top_local_word_lm_debug, candidate_count);
        SetLength(exact_anchor_candidate_local_word_lm_debug,
            candidate_count);
        for candidate_position_local := 0 to candidate_count - 1 do
        begin
            candidate_index_local :=
                ordered_indices[candidate_position_local];
            exact_anchor_pairwise_debug_scores[candidate_index_local] :=
                Low(Int64);
            exact_anchor_pairwise_current_rank_debug[
                candidate_index_local] := candidate_position_local + 1;
        end;
        if (not unified_pool_final) or (candidate_count < 2) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        if (not rank_features[top_index_local].complete_match) or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;
        top_units_local := split_text_units(
            Trim(legacy_candidates[top_index_local].text));
        if Length(top_units_local) = 0 then
        begin
            Exit;
        end;
        SetLength(relations_local, candidate_count);
        SetLength(relation_valid_local, candidate_count);
        SetLength(window_score_offsets_local, candidate_count);
        for candidate_index_local := 0 to candidate_count - 1 do
        begin
            window_score_offsets_local[candidate_index_local] := -1;
        end;
        SetLength(window_texts_local, (candidate_count - 1) * 8);
        window_count_local := 0;
        for candidate_position_local := 1 to candidate_count - 1 do
        begin
            candidate_index_local :=
                ordered_indices[candidate_position_local];
            if (not rank_features[candidate_index_local].complete_match) or
                (not rank_features[candidate_index_local].complete_pool_anchor_present) or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) then
            begin
                Continue;
            end;
            candidate_units_local := split_text_units(
                Trim(legacy_candidates[candidate_index_local].text));
            if Length(candidate_units_local) <> Length(top_units_local) then
            begin
                Continue;
            end;
            get_text_relation(candidate_index_local, top_index_local,
                relation_local.different_units,
                relation_local.different_runs,
                relation_local.max_different_run,
                relation_local.same_prefix_units,
                relation_local.same_suffix_units,
                relation_local.difference_span_units);
            if relation_local.different_units <= 0 then
            begin
                Continue;
            end;
            relations_local[candidate_index_local] := relation_local;
            relation_valid_local[candidate_index_local] := True;
            window_score_offsets_local[candidate_index_local] :=
                window_count_local;
            for radius_local := 0 to 3 do
            begin
                window_start_local := Max(0,
                    relation_local.same_prefix_units - radius_local);
                window_end_local := Min(Length(top_units_local),
                    Length(top_units_local) -
                    relation_local.same_suffix_units + radius_local);
                top_window_local := build_text_unit_window(
                    top_units_local, window_start_local,
                    window_end_local, False);
                candidate_window_local := build_text_unit_window(
                    candidate_units_local, window_start_local,
                    window_end_local, False);
                window_texts_local[window_count_local] :=
                    top_window_local;
                Inc(window_count_local);
                window_texts_local[window_count_local] :=
                    candidate_window_local;
                Inc(window_count_local);
            end;
        end;
        SetLength(window_texts_local, window_count_local);
        if (window_count_local = 0) or
            (not get_cached_char_lm_scores(window_texts_local,
            window_scores_local, clsm_suffix, '')) or
            (Length(window_scores_local) <> window_count_local) then
        begin
            Exit;
        end;

        SetLength(top_scores_local, 4);
        SetLength(candidate_scores_local, 4);
        best_position_local := -1;
        best_score_local := -1.0E308;
        for candidate_position_local := 1 to candidate_count - 1 do
        begin
            candidate_index_local :=
                ordered_indices[candidate_position_local];
            if (not relation_valid_local[candidate_index_local]) or
                (window_score_offsets_local[candidate_index_local] < 0) then
            begin
                Continue;
            end;
            for radius_local := 0 to 3 do
            begin
                top_scores_local[radius_local] := window_scores_local[
                    window_score_offsets_local[candidate_index_local] +
                    radius_local * 2];
                candidate_scores_local[radius_local] :=
                    window_scores_local[
                    window_score_offsets_local[candidate_index_local] +
                    radius_local * 2 + 1];
            end;
            { The independent corpus found no positive exact-anchor examples
              supported by these sparse local word transitions. Keep the
              feature slots stable without paying two dictionary queries for
              every challenger. }
            FillChar(top_word_scores_local,
                SizeOf(top_word_scores_local), 0);
            FillChar(candidate_word_scores_local,
                SizeOf(candidate_word_scores_local), 0);
            exact_anchor_top_local_word_lm_debug[candidate_index_local] :=
                top_word_scores_local;
            exact_anchor_candidate_local_word_lm_debug[
                candidate_index_local] := candidate_word_scores_local;
            build_long_exact_anchor_pairwise_features(
                rank_features[candidate_index_local],
                rank_features[top_index_local],
                candidate_position_local + 1, 1,
                rank_scores[candidate_index_local],
                rank_scores[top_index_local],
                relations_local[candidate_index_local],
                top_scores_local, candidate_scores_local,
                top_word_scores_local, candidate_word_scores_local,
                features_local);
            score_local := long_exact_anchor_pairwise_score(features_local);
            exact_anchor_pairwise_debug_scores[candidate_index_local] :=
                Round(score_local * 1000000.0);
            if score_local > best_score_local then
            begin
                best_score_local := score_local;
                best_position_local := candidate_position_local;
            end;
        end;
        if best_position_local <= 0 then
        begin
            Exit;
        end;
        candidate_index_local := ordered_indices[best_position_local];
        { Stage tracing on the current runtime distribution shows that this
          narrow exact-anchor model is useful as a recall/second-slot lane but
          loses Top1 accuracy when it makes the final decision itself. Keep the
          exact path for the later bidirectional and unified arbiters instead. }
        strict_threshold_local := 1.0E308;
        move_result_local := apply_long_exact_anchor_pairwise_index_move(
            ordered_indices, best_position_local, best_score_local,
            strict_threshold_local,
            c_long_exact_anchor_pairwise_second_threshold);
        if move_result_local >= 0 then
        begin
            exact_anchor_pairwise_moved_index := candidate_index_local;
            exact_anchor_pairwise_target_rank := move_result_local + 1;
            apply_ranker := True;
        end;
    end;

    procedure apply_bidirectional_difference_ranking;
    const
        c_reverse_radii: array[0..4] of Integer = (0, 1, 2, 3, 5);
    var
        top_index_local: Integer;
        candidate_index_local: Integer;
        relation_local: TncLongExactAnchorRelationFeatures;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        forward_top_scores_local: TArray<Integer>;
        forward_candidate_scores_local: TArray<Integer>;
        reverse_window_texts_local: TArray<string>;
        reverse_window_scores_local: TArray<Integer>;
        reverse_top_scores_local: TArray<Integer>;
        reverse_candidate_scores_local: TArray<Integer>;
        top_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        candidate_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        base_features_local: TncLongExactAnchorPairwiseFeatures;
        features_local: TncLongBidirectionalDifferenceFeatures;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_index_local: Integer;
        top_window_local: string;
        candidate_window_local: string;
        score_local: Double;
        swap_index_local: Integer;
    begin
        if (not unified_pool_final) or (candidate_count < 2) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;
        top_index_local := ordered_indices[0];
        candidate_index_local := ordered_indices[1];
        if (not rank_features[top_index_local].complete_match) or
            (not rank_features[candidate_index_local].complete_match) or
            rank_features[top_index_local].source_user or
            rank_features[candidate_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[candidate_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            rank_features[candidate_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) or
            (rank_features[candidate_index_local].query_choice_bonus > 0) or
            SameText(Trim(legacy_candidates[top_index_local].text),
            Trim(legacy_candidates[candidate_index_local].text)) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(
            Trim(legacy_candidates[top_index_local].text));
        candidate_units_local := split_text_units(
            Trim(legacy_candidates[candidate_index_local].text));
        if (Length(top_units_local) = 0) or
            (Length(top_units_local) <> Length(candidate_units_local)) then
        begin
            Exit;
        end;
        get_text_relation(candidate_index_local, top_index_local,
            relation_local.different_units,
            relation_local.different_runs,
            relation_local.max_different_run,
            relation_local.same_prefix_units,
            relation_local.same_suffix_units,
            relation_local.difference_span_units);
        if relation_local.different_units <= 0 then
        begin
            Exit;
        end;
        if not get_top2_cached_span_lm_scores(
            top_index_local, candidate_index_local,
            forward_top_scores_local, forward_candidate_scores_local) then
        begin
            Exit;
        end;

        SetLength(reverse_window_texts_local,
            Length(c_reverse_radii) * 2);
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            window_start_local := Max(0,
                relation_local.same_prefix_units -
                c_reverse_radii[radius_index_local]);
            window_end_local := Min(Length(top_units_local),
                Length(top_units_local) - relation_local.same_suffix_units +
                c_reverse_radii[radius_index_local]);
            top_window_local := build_text_unit_window(top_units_local,
                window_start_local, window_end_local, True);
            candidate_window_local := build_text_unit_window(
                candidate_units_local, window_start_local,
                window_end_local, True);
            reverse_window_texts_local[radius_index_local * 2] :=
                top_window_local;
            reverse_window_texts_local[radius_index_local * 2 + 1] :=
                candidate_window_local;
        end;
        if (not get_cached_char_lm_scores(reverse_window_texts_local,
            reverse_window_scores_local, clsm_reverse, '')) or
            (Length(reverse_window_scores_local) <
            Length(reverse_window_texts_local)) then
        begin
            Exit;
        end;

        SetLength(reverse_top_scores_local, Length(c_reverse_radii));
        SetLength(reverse_candidate_scores_local, Length(c_reverse_radii));
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            reverse_top_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2];
            reverse_candidate_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2 + 1];
        end;
        FillChar(top_word_scores_local, SizeOf(top_word_scores_local), 0);
        FillChar(candidate_word_scores_local,
            SizeOf(candidate_word_scores_local), 0);
        build_long_exact_anchor_pairwise_features(
            rank_features[candidate_index_local],
            rank_features[top_index_local], 2, 1,
            rank_scores[candidate_index_local], rank_scores[top_index_local],
            relation_local, forward_top_scores_local,
            forward_candidate_scores_local, top_word_scores_local,
            candidate_word_scores_local, base_features_local);
        build_long_bidirectional_difference_features(base_features_local,
            reverse_top_scores_local, reverse_candidate_scores_local,
            features_local);
        score_local := long_bidirectional_difference_score(features_local);
        if score_local < c_long_bidirectional_threshold then
        begin
            Exit;
        end;

        swap_index_local := ordered_indices[0];
        ordered_indices[0] := ordered_indices[1];
        ordered_indices[1] := swap_index_local;
        bidirectional_top1_swapped := True;
        apply_ranker := True;
    end;

    procedure apply_second_slot_bidirectional_ranking;
    const
        c_reverse_radii: array[0..4] of Integer = (0, 1, 2, 3, 5);
    var
        baseline_index_local: Integer;
        candidate_index_local: Integer;
        relation_local: TncLongExactAnchorRelationFeatures;
        baseline_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        forward_baseline_scores_local: TArray<Integer>;
        forward_candidate_scores_local: TArray<Integer>;
        reverse_window_texts_local: TArray<string>;
        reverse_window_scores_local: TArray<Integer>;
        reverse_baseline_scores_local: TArray<Integer>;
        reverse_candidate_scores_local: TArray<Integer>;
        baseline_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        candidate_word_scores_local: TncLongExactAnchorLocalWordLmScores;
        base_features_local: TncLongExactAnchorPairwiseFeatures;
        features_local: TncLongSecondSlotBidirectionalFeatures;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_index_local: Integer;
        baseline_window_local: string;
        candidate_window_local: string;
        score_local: Double;
        swap_index_local: Integer;
    begin
        { The independent model was trained against the established rank-2
          candidate. Skip cases where the Top1 model changed that reference. }
        if bidirectional_top1_swapped or (not unified_pool_final) or
            (candidate_count < 3) or (m_dictionary = nil) then
        begin
            Exit;
        end;
        baseline_index_local := ordered_indices[1];
        candidate_index_local := ordered_indices[2];
        if (not rank_features[baseline_index_local].complete_match) or
            (not rank_features[candidate_index_local].complete_match) or
            rank_features[baseline_index_local].source_user or
            rank_features[candidate_index_local].source_user or
            rank_features[baseline_index_local].complete_user or
            rank_features[candidate_index_local].complete_user or
            rank_features[baseline_index_local].latest_query_choice or
            rank_features[candidate_index_local].latest_query_choice or
            (rank_features[baseline_index_local].query_choice_bonus > 0) or
            (rank_features[candidate_index_local].query_choice_bonus > 0) or
            SameText(Trim(legacy_candidates[baseline_index_local].text),
            Trim(legacy_candidates[candidate_index_local].text)) then
        begin
            Exit;
        end;

        baseline_units_local := split_text_units(
            Trim(legacy_candidates[baseline_index_local].text));
        candidate_units_local := split_text_units(
            Trim(legacy_candidates[candidate_index_local].text));
        if (Length(baseline_units_local) = 0) or
            (Length(baseline_units_local) <> Length(candidate_units_local)) then
        begin
            Exit;
        end;
        get_text_relation(candidate_index_local, baseline_index_local,
            relation_local.different_units,
            relation_local.different_runs,
            relation_local.max_different_run,
            relation_local.same_prefix_units,
            relation_local.same_suffix_units,
            relation_local.difference_span_units);
        if relation_local.different_units <= 0 then
        begin
            Exit;
        end;
        if not get_top2_cached_span_lm_scores(
            baseline_index_local, candidate_index_local,
            forward_baseline_scores_local,
            forward_candidate_scores_local) then
        begin
            Exit;
        end;

        SetLength(reverse_window_texts_local,
            Length(c_reverse_radii) * 2);
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            window_start_local := Max(0,
                relation_local.same_prefix_units -
                c_reverse_radii[radius_index_local]);
            window_end_local := Min(Length(baseline_units_local),
                Length(baseline_units_local) - relation_local.same_suffix_units +
                c_reverse_radii[radius_index_local]);
            baseline_window_local := build_text_unit_window(
                baseline_units_local, window_start_local,
                window_end_local, True);
            candidate_window_local := build_text_unit_window(
                candidate_units_local, window_start_local,
                window_end_local, True);
            reverse_window_texts_local[radius_index_local * 2] :=
                baseline_window_local;
            reverse_window_texts_local[radius_index_local * 2 + 1] :=
                candidate_window_local;
        end;
        if (not get_cached_char_lm_scores(reverse_window_texts_local,
            reverse_window_scores_local, clsm_reverse, '')) or
            (Length(reverse_window_scores_local) <
            Length(reverse_window_texts_local)) then
        begin
            Exit;
        end;

        SetLength(reverse_baseline_scores_local, Length(c_reverse_radii));
        SetLength(reverse_candidate_scores_local, Length(c_reverse_radii));
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            reverse_baseline_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2];
            reverse_candidate_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2 + 1];
        end;
        FillChar(baseline_word_scores_local,
            SizeOf(baseline_word_scores_local), 0);
        FillChar(candidate_word_scores_local,
            SizeOf(candidate_word_scores_local), 0);
        build_long_exact_anchor_pairwise_features(
            rank_features[candidate_index_local],
            rank_features[baseline_index_local], 3, 2,
            rank_scores[candidate_index_local],
            rank_scores[baseline_index_local], relation_local,
            forward_baseline_scores_local, forward_candidate_scores_local,
            baseline_word_scores_local, candidate_word_scores_local,
            base_features_local);
        build_long_second_slot_bidirectional_features(base_features_local,
            reverse_baseline_scores_local, reverse_candidate_scores_local,
            features_local);
        score_local := long_second_slot_bidirectional_score(features_local);
        if score_local < c_long_second_slot_bidirectional_threshold then
        begin
            Exit;
        end;

        swap_index_local := ordered_indices[1];
        ordered_indices[1] := ordered_indices[2];
        ordered_indices[2] := swap_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_final_pairwise_residual;
    var
        top_index_local: Integer;
        top_position_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        challenger_limit_local: Integer;
        challenger_rank_local: Integer;
        best_position_local: Integer;
        best_score_local: Int64;
        score_local: Int64;
        features_local: TncLongVisiblePairwiseResidualFeatures;
        function can_be_primary_visible(
            const candidate_index_value: Integer): Boolean;
        var
            pool_index_local: Integer;
            pool_item_local: TncLongCompletePoolRuntimeCandidate;
        begin
            Result := False;
            if (candidate_index_value < 0) or
                (candidate_index_value >= candidate_count) or
                (not rank_features[candidate_index_value].complete_match) then
            begin
                Exit;
            end;
            pool_index_local := candidate_pool_indices[candidate_index_value];
            if (pool_index_local < 0) or
                (pool_index_local >= Length(
                m_runtime_long_complete_pool_candidates)) then
            begin
                Exit;
            end;
            pool_item_local :=
                m_runtime_long_complete_pool_candidates[pool_index_local];
            case pool_item_local.source_kind of
                lcps_exact_lattice, lcps_existing:
                    Result := pool_item_local.visible_evidence;
                lcps_chain:
                    Result := True;
            else
                Result := True;
            end;
        end;
    begin
        if (not unified_pool_final) or (candidate_count < 2) then
        begin
            Exit;
        end;

        top_position_local := -1;
        for candidate_position_local := 0 to candidate_count - 1 do
        begin
            if can_be_primary_visible(
                ordered_indices[candidate_position_local]) then
            begin
                top_position_local := candidate_position_local;
                Break;
            end;
        end;
        if top_position_local < 0 then
        begin
            Exit;
        end;
        top_index_local := ordered_indices[top_position_local];
        complete_pool_final_pairwise_current_rank_debug[
            top_index_local] := 1;
        if (not rank_features[top_index_local].complete_match) or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;

        challenger_limit_local :=
            c_long_visible_pairwise_residual_max_challenger_rank;
        best_position_local := -1;
        best_score_local := Low(Int64);
        challenger_rank_local := 1;
        for candidate_position_local := top_position_local + 1 to
            candidate_count - 1 do
        begin
            candidate_index_local := ordered_indices[candidate_position_local];
            if not can_be_primary_visible(candidate_index_local) then
            begin
                Continue;
            end;
            Inc(challenger_rank_local);
            if (challenger_limit_local > 0) and
                (challenger_rank_local > challenger_limit_local) then
            begin
                Break;
            end;
            if (not rank_features[candidate_index_local].complete_match) or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) then
            begin
                Continue;
            end;
            build_long_visible_pairwise_residual_features(
                rank_features[candidate_index_local],
                rank_features[top_index_local], challenger_rank_local,
                rank_scores[candidate_index_local], rank_scores[top_index_local],
                apply_ranker, abstain_score, features_local);
            score_local := long_visible_pairwise_residual_score(features_local);
            complete_pool_final_pairwise_debug_scores[
                candidate_index_local] := score_local;
            complete_pool_final_pairwise_current_rank_debug[
                candidate_index_local] := challenger_rank_local;
            if score_local > best_score_local then
            begin
                best_score_local := score_local;
                best_position_local := candidate_position_local;
            end;
        end;
        if (best_position_local <= 0) or
            (best_score_local <
            c_long_visible_pairwise_residual_promotion_threshold) then
        begin
            Exit;
        end;

        candidate_index_local := ordered_indices[best_position_local];
        for candidate_position_local := best_position_local downto
            top_position_local + 1 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[top_position_local] := candidate_index_local;
        complete_pool_final_pairwise_promoted_index := candidate_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_post_static_difference_residual;
    var
        top_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        challenger_limit_local: Integer;
        best_position_local: Integer;
        best_score_local: Int64;
        score_local: Int64;
        different_units_local: Integer;
        different_runs_local: Integer;
        max_different_run_local: Integer;
        same_prefix_units_local: Integer;
        same_suffix_units_local: Integer;
        difference_span_units_local: Integer;
        top_local_lm_scores_local: TArray<Integer>;
        candidate_local_lm_scores_local: TArray<Integer>;
        features_local: TncLongCompletePoolDifferenceFeatures;
    begin
        if (not unified_pool_final) or (candidate_count < 2) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        complete_pool_post_static_difference_current_rank_debug[
            top_index_local] := 1;
        if (not rank_features[top_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;

        challenger_limit_local := Min(candidate_count,
            c_long_complete_pool_difference_max_challenger_rank);
        best_position_local := -1;
        best_score_local := Low(Int64);
        for candidate_position_local := 1 to challenger_limit_local - 1 do
        begin
            candidate_index_local := ordered_indices[candidate_position_local];
            complete_pool_post_static_difference_current_rank_debug[
                candidate_index_local] := candidate_position_local + 1;
            if (not rank_features[candidate_index_local].complete_match) or
                (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) or
                (rank_features[candidate_index_local].text_units <>
                rank_features[top_index_local].text_units) then
            begin
                Continue;
            end;

            get_text_relation(candidate_index_local, top_index_local,
                different_units_local, different_runs_local,
                max_different_run_local, same_prefix_units_local,
                same_suffix_units_local, difference_span_units_local);
            if (different_units_local <= 0) or
                (not get_top2_cached_span_lm_scores(
                top_index_local, candidate_index_local,
                top_local_lm_scores_local,
                candidate_local_lm_scores_local)) then
            begin
                Continue;
            end;

            build_long_complete_pool_difference_features(
                rank_features[candidate_index_local],
                rank_features[top_index_local], candidate_position_local + 1,
                rank_scores[candidate_index_local], rank_scores[top_index_local],
                apply_ranker, abstain_score, different_units_local,
                different_runs_local, max_different_run_local,
                same_prefix_units_local, same_suffix_units_local,
                difference_span_units_local, top_local_lm_scores_local,
                candidate_local_lm_scores_local, features_local);
            score_local := long_complete_pool_difference_score(features_local);
            complete_pool_post_static_difference_debug_scores[
                candidate_index_local] := score_local;
            if score_local > best_score_local then
            begin
                best_score_local := score_local;
                best_position_local := candidate_position_local;
            end;
        end;
        if (best_position_local <= 0) or
            (best_score_local <
            c_long_complete_pool_difference_promotion_threshold) then
        begin
            Exit;
        end;

        candidate_index_local := ordered_indices[best_position_local];
        for candidate_position_local := best_position_local downto 1 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[0] := candidate_index_local;
        complete_pool_post_static_difference_promoted_index :=
            candidate_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_ranker_second_slot_recovery;
    var
        top_index_local: Integer;
        second_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        candidate_limit_local: Integer;
        best_position_local: Integer;
        best_score_local: Int64;
        promoted_index_local: Integer;
        different_units_local: Integer;
        different_runs_local: Integer;
        max_different_run_local: Integer;
        same_prefix_units_local: Integer;
        same_suffix_units_local: Integer;
        difference_span_units_local: Integer;
        gate_score_local: Int64;
        gate_features_local: TncLongSecondSlotRecoveryGateFeatures;
    begin
        if (not unified_pool_final) or (candidate_count < 3) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        second_index_local := ordered_indices[1];
        if (not rank_features[top_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) or
            rank_features[second_index_local].source_user or
            rank_features[second_index_local].complete_user or
            rank_features[second_index_local].latest_query_choice or
            (rank_features[second_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;

        candidate_limit_local := Min(candidate_count,
            c_long_second_slot_recovery_gate_max_candidate_rank);
        best_position_local := -1;
        best_score_local := rank_scores[second_index_local];
        for candidate_position_local := 2 to candidate_limit_local - 1 do
        begin
            candidate_index_local := ordered_indices[candidate_position_local];
            if (not rank_features[candidate_index_local].complete_match) or
                (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) or
                (rank_features[candidate_index_local].text_units <>
                rank_features[top_index_local].text_units) or
                SameText(Trim(legacy_candidates[candidate_index_local].text),
                Trim(legacy_candidates[second_index_local].text)) then
            begin
                Continue;
            end;
            if rank_scores[candidate_index_local] > best_score_local then
            begin
                best_score_local := rank_scores[candidate_index_local];
                best_position_local := candidate_position_local;
            end;
        end;
        if (best_position_local < 2) or
            (best_score_local - rank_scores[second_index_local] <
            c_long_second_slot_recovery_gate_static_gap) then
        begin
            Exit;
        end;

        promoted_index_local := ordered_indices[best_position_local];
        get_text_relation(promoted_index_local, second_index_local,
            different_units_local, different_runs_local,
            max_different_run_local, same_prefix_units_local,
            same_suffix_units_local, difference_span_units_local);
        if different_units_local <= 0 then
        begin
            Exit;
        end;
        build_long_second_slot_recovery_gate_features(
            rank_features[promoted_index_local],
            rank_features[second_index_local],
            rank_features[top_index_local], best_position_local + 1,
            rank_scores[promoted_index_local],
            rank_scores[second_index_local], rank_scores[top_index_local],
            different_units_local, different_runs_local,
            max_different_run_local, same_prefix_units_local,
            same_suffix_units_local, difference_span_units_local,
            gate_features_local);
        gate_score_local := long_second_slot_recovery_gate_score(
            gate_features_local);
        if gate_score_local < c_long_second_slot_recovery_gate_threshold then
        begin
            Exit;
        end;
        for candidate_position_local := best_position_local downto 2 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[1] := promoted_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_bidirectional_second_slot_selector;
    const
        c_forward_radii: array[0..3] of Integer = (0, 1, 2, 3);
        c_reverse_radii: array[0..4] of Integer = (0, 1, 2, 3, 5);
    var
        top_index_local: Integer;
        current_second_index_local: Integer;
        original_second_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        candidate_limit_local: Integer;
        deep_indices_local: TArray<Integer>;
        deep_positions_local: TArray<Integer>;
        deep_count_local: Integer;
        sort_left_local: Integer;
        sort_right_local: Integer;
        swap_index_local: Integer;
        swap_position_local: Integer;
        option_indices_local: TArray<Integer>;
        option_positions_local: TArray<Integer>;
        option_count_local: Integer;
        option_index_local: Integer;
        best_option_local: Integer;
        best_position_local: Integer;
        relation_local: TncLongExactAnchorRelationFeatures;
        relations_local: TArray<TncLongExactAnchorRelationFeatures>;
        current_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        forward_window_texts_local: TArray<string>;
        forward_window_scores_local: TArray<Integer>;
        reverse_window_texts_local: TArray<string>;
        reverse_window_scores_local: TArray<Integer>;
        current_forward_scores_local: TArray<Integer>;
        candidate_forward_scores_local: TArray<Integer>;
        current_reverse_scores_local: TArray<Integer>;
        candidate_reverse_scores_local: TArray<Integer>;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_index_local: Integer;
        text_offset_local: Integer;
        current_window_local: string;
        candidate_window_local: string;
        base_features_local: TncLongSecondSlotRecoveryGateFeatures;
        selector_features_local: TncLongSecondSlotSelectorFeatures;
        baseline_score_local: Double;
        best_score_local: Double;
        score_local: Double;

    begin
        if (not unified_pool_final) or (candidate_count < 3) or
            (m_dictionary = nil) or
            (Length(second_slot_selector_base_positions) <> candidate_count) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        current_second_index_local := ordered_indices[1];
        if (not rank_features[top_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) or
            rank_features[current_second_index_local].source_user or
            rank_features[current_second_index_local].complete_user or
            rank_features[current_second_index_local].latest_query_choice or
            (rank_features[current_second_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;

        original_second_index_local := -1;
        for candidate_index_local := 0 to candidate_count - 1 do
        begin
            if second_slot_selector_base_positions[candidate_index_local] = 1 then
            begin
                original_second_index_local := candidate_index_local;
                Break;
            end;
        end;
        if original_second_index_local < 0 then
        begin
            Exit;
        end;

        candidate_limit_local := Min(candidate_count,
            c_long_second_slot_selector_max_candidate_rank);
        { Base positions are expected to be unique, but the selector must not
          trust that invariant while collecting from the complete pool. A
          duplicate position can otherwise write past candidate_limit_local
          and corrupt the heap before the model-side alternative cap applies. }
        SetLength(deep_indices_local, candidate_count);
        SetLength(deep_positions_local, candidate_count);
        deep_count_local := 0;
        for candidate_index_local := 0 to candidate_count - 1 do
        begin
            candidate_position_local :=
                second_slot_selector_base_positions[candidate_index_local];
            if (candidate_position_local < 2) or
                (candidate_position_local >= candidate_limit_local) or
                (not rank_features[candidate_index_local].complete_match) or
                (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) or
                (rank_features[candidate_index_local].text_units <>
                rank_features[top_index_local].text_units) or
                SameText(Trim(legacy_candidates[candidate_index_local].text),
                Trim(legacy_candidates[original_second_index_local].text)) then
            begin
                Continue;
            end;
            deep_indices_local[deep_count_local] := candidate_index_local;
            deep_positions_local[deep_count_local] := candidate_position_local;
            Inc(deep_count_local);
        end;
        if deep_count_local = 0 then
        begin
            Exit;
        end;
        SetLength(deep_indices_local, deep_count_local);
        SetLength(deep_positions_local, deep_count_local);

        for sort_left_local := 0 to deep_count_local - 2 do
        begin
            for sort_right_local := sort_left_local + 1 to
                deep_count_local - 1 do
            begin
                if (rank_scores[deep_indices_local[sort_right_local]] >
                    rank_scores[deep_indices_local[sort_left_local]]) or
                    ((rank_scores[deep_indices_local[sort_right_local]] =
                    rank_scores[deep_indices_local[sort_left_local]]) and
                    (deep_positions_local[sort_right_local] <
                    deep_positions_local[sort_left_local])) then
                begin
                    swap_index_local := deep_indices_local[sort_left_local];
                    deep_indices_local[sort_left_local] :=
                        deep_indices_local[sort_right_local];
                    deep_indices_local[sort_right_local] := swap_index_local;
                    swap_position_local := deep_positions_local[sort_left_local];
                    deep_positions_local[sort_left_local] :=
                        deep_positions_local[sort_right_local];
                    deep_positions_local[sort_right_local] := swap_position_local;
                end;
            end;
        end;
        deep_count_local := Min(deep_count_local,
            c_long_second_slot_selector_max_alternatives);
        SetLength(deep_indices_local, deep_count_local);
        SetLength(deep_positions_local, deep_count_local);

        { Reconstruct the training option order: current settled second first,
          then the original second and ranker-sorted deep alternatives. }
        { The option set can contain the current second, the original second,
          and every deep alternative. The current second is not guaranteed to
          be present in the filtered deep list, so reserve both leading slots. }
        SetLength(option_indices_local, deep_count_local + 2);
        SetLength(option_positions_local, deep_count_local + 2);
        option_count_local := 1;
        option_indices_local[0] := current_second_index_local;
        option_positions_local[0] :=
            second_slot_selector_base_positions[current_second_index_local];
        if original_second_index_local <> current_second_index_local then
        begin
            option_indices_local[option_count_local] :=
                original_second_index_local;
            option_positions_local[option_count_local] := 1;
            Inc(option_count_local);
        end;
        for option_index_local := 0 to deep_count_local - 1 do
        begin
            if deep_indices_local[option_index_local] =
                current_second_index_local then
            begin
                Continue;
            end;
            option_indices_local[option_count_local] :=
                deep_indices_local[option_index_local];
            option_positions_local[option_count_local] :=
                deep_positions_local[option_index_local];
            Inc(option_count_local);
        end;
        SetLength(option_indices_local, option_count_local);
        SetLength(option_positions_local, option_count_local);
        if option_count_local < 2 then
        begin
            Exit;
        end;

        current_units_local := split_text_units(
            Trim(legacy_candidates[current_second_index_local].text));
        SetLength(relations_local, option_count_local);
        SetLength(forward_window_texts_local,
            (option_count_local - 1) * Length(c_forward_radii) * 2);
        SetLength(reverse_window_texts_local,
            (option_count_local - 1) * Length(c_reverse_radii) * 2);
        for option_index_local := 1 to option_count_local - 1 do
        begin
            candidate_index_local := option_indices_local[option_index_local];
            candidate_units_local := split_text_units(
                Trim(legacy_candidates[candidate_index_local].text));
            get_text_relation(candidate_index_local,
                current_second_index_local,
                relation_local.different_units,
                relation_local.different_runs,
                relation_local.max_different_run,
                relation_local.same_prefix_units,
                relation_local.same_suffix_units,
                relation_local.difference_span_units);
            relations_local[option_index_local] := relation_local;
            for radius_index_local := 0 to High(c_forward_radii) do
            begin
                window_start_local := Max(0,
                    relation_local.same_prefix_units -
                    c_forward_radii[radius_index_local]);
                window_end_local := Min(Length(current_units_local),
                    Length(current_units_local) -
                    relation_local.same_suffix_units +
                    c_forward_radii[radius_index_local]);
                current_window_local := build_text_unit_window(
                    current_units_local, window_start_local,
                    window_end_local, False);
                candidate_window_local := build_text_unit_window(
                    candidate_units_local, window_start_local,
                    window_end_local, False);
                text_offset_local := (option_index_local - 1) *
                    Length(c_forward_radii) * 2 + radius_index_local * 2;
                forward_window_texts_local[text_offset_local] :=
                    current_window_local;
                forward_window_texts_local[text_offset_local + 1] :=
                    candidate_window_local;
            end;
            for radius_index_local := 0 to High(c_reverse_radii) do
            begin
                window_start_local := Max(0,
                    relation_local.same_prefix_units -
                    c_reverse_radii[radius_index_local]);
                window_end_local := Min(Length(current_units_local),
                    Length(current_units_local) -
                    relation_local.same_suffix_units +
                    c_reverse_radii[radius_index_local]);
                current_window_local := build_text_unit_window(
                    current_units_local, window_start_local,
                    window_end_local, True);
                candidate_window_local := build_text_unit_window(
                    candidate_units_local, window_start_local,
                    window_end_local, True);
                text_offset_local := (option_index_local - 1) *
                    Length(c_reverse_radii) * 2 + radius_index_local * 2;
                reverse_window_texts_local[text_offset_local] :=
                    current_window_local;
                reverse_window_texts_local[text_offset_local + 1] :=
                    candidate_window_local;
            end;
        end;
        if (not get_cached_char_lm_scores(forward_window_texts_local,
            forward_window_scores_local, clsm_span, '')) or
            (Length(forward_window_scores_local) <
            Length(forward_window_texts_local)) or
            (not get_cached_char_lm_scores(reverse_window_texts_local,
            reverse_window_scores_local, clsm_reverse, '')) or
            (Length(reverse_window_scores_local) <
            Length(reverse_window_texts_local)) then
        begin
            Exit;
        end;

        SetLength(current_forward_scores_local, Length(c_forward_radii));
        SetLength(candidate_forward_scores_local, Length(c_forward_radii));
        SetLength(current_reverse_scores_local, Length(c_reverse_radii));
        SetLength(candidate_reverse_scores_local, Length(c_reverse_radii));
        FillChar(relations_local[0], SizeOf(relations_local[0]), 0);
        relations_local[0].same_prefix_units := Length(current_units_local);
        relations_local[0].same_suffix_units := Length(current_units_local);
        build_long_second_slot_recovery_gate_features(
            rank_features[current_second_index_local],
            rank_features[current_second_index_local],
            rank_features[top_index_local], option_positions_local[0] + 1,
            rank_scores[current_second_index_local],
            rank_scores[current_second_index_local],
            rank_scores[top_index_local], 0, 0, 0,
            Length(current_units_local), Length(current_units_local), 0,
            base_features_local);
        build_long_second_slot_selector_features(base_features_local,
            current_forward_scores_local, candidate_forward_scores_local,
            current_reverse_scores_local, candidate_reverse_scores_local,
            selector_features_local);
        baseline_score_local := long_second_slot_selector_score(
            selector_features_local);
        best_score_local := baseline_score_local;
        best_option_local := 0;

        for option_index_local := 1 to option_count_local - 1 do
        begin
            for radius_index_local := 0 to High(c_forward_radii) do
            begin
                text_offset_local := (option_index_local - 1) *
                    Length(c_forward_radii) * 2 + radius_index_local * 2;
                current_forward_scores_local[radius_index_local] :=
                    forward_window_scores_local[text_offset_local];
                candidate_forward_scores_local[radius_index_local] :=
                    forward_window_scores_local[text_offset_local + 1];
            end;
            for radius_index_local := 0 to High(c_reverse_radii) do
            begin
                text_offset_local := (option_index_local - 1) *
                    Length(c_reverse_radii) * 2 + radius_index_local * 2;
                current_reverse_scores_local[radius_index_local] :=
                    reverse_window_scores_local[text_offset_local];
                candidate_reverse_scores_local[radius_index_local] :=
                    reverse_window_scores_local[text_offset_local + 1];
            end;
            candidate_index_local := option_indices_local[option_index_local];
            relation_local := relations_local[option_index_local];
            build_long_second_slot_recovery_gate_features(
                rank_features[candidate_index_local],
                rank_features[current_second_index_local],
                rank_features[top_index_local],
                option_positions_local[option_index_local] + 1,
                rank_scores[candidate_index_local],
                rank_scores[current_second_index_local],
                rank_scores[top_index_local], relation_local.different_units,
                relation_local.different_runs,
                relation_local.max_different_run,
                relation_local.same_prefix_units,
                relation_local.same_suffix_units,
                relation_local.difference_span_units, base_features_local);
            build_long_second_slot_selector_features(base_features_local,
                current_forward_scores_local, candidate_forward_scores_local,
                current_reverse_scores_local, candidate_reverse_scores_local,
                selector_features_local);
            score_local := long_second_slot_selector_score(
                selector_features_local);
            if score_local > best_score_local then
            begin
                best_score_local := score_local;
                best_option_local := option_index_local;
            end;
        end;
        if (best_option_local = 0) or
            (best_score_local - baseline_score_local <
            c_long_second_slot_selector_margin_threshold) then
        begin
            Exit;
        end;

        candidate_index_local := option_indices_local[best_option_local];
        best_position_local := -1;
        for candidate_position_local := 2 to candidate_count - 1 do
        begin
            if ordered_indices[candidate_position_local] =
                candidate_index_local then
            begin
                best_position_local := candidate_position_local;
                Break;
            end;
        end;
        if best_position_local < 2 then
        begin
            Exit;
        end;
        for candidate_position_local := best_position_local downto 2 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[1] := candidate_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_post_static_bidirectional_residual;
    const
        c_forward_radii: array[0..3] of Integer = (0, 1, 2, 3);
        c_reverse_radii: array[0..4] of Integer = (0, 1, 2, 3, 5);
    var
        top_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        challenger_limit_local: Integer;
        eligible_count_local: Integer;
        eligible_index_local: Integer;
        best_position_local: Integer;
        best_score_local: Double;
        score_local: Double;
        relation_local: TncLongExactAnchorRelationFeatures;
        relations_local: TArray<TncLongExactAnchorRelationFeatures>;
        candidate_indices_local: TArray<Integer>;
        candidate_positions_local: TArray<Integer>;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        forward_window_texts_local: TArray<string>;
        forward_window_scores_local: TArray<Integer>;
        reverse_window_texts_local: TArray<string>;
        reverse_window_scores_local: TArray<Integer>;
        forward_top_scores_local: TArray<Integer>;
        forward_candidate_scores_local: TArray<Integer>;
        reverse_top_scores_local: TArray<Integer>;
        reverse_candidate_scores_local: TArray<Integer>;
        base_features_local: TncLongCompletePoolDifferenceFeatures;
        gate_features_local: TncLongPostSecondSlotGateFeatures;
        features_local: TncLongCompletePoolBidirectionalFeatures;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_index_local: Integer;
        text_offset_local: Integer;
        top_window_local: string;
        candidate_window_local: string;
    begin
        if (not unified_pool_final) or (candidate_count < 2) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        if (not rank_features[top_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) then
        begin
            Exit;
        end;
        top_units_local := split_text_units(
            Trim(legacy_candidates[top_index_local].text));
        if Length(top_units_local) = 0 then
        begin
            Exit;
        end;

        challenger_limit_local := Min(candidate_count,
            c_long_complete_pool_bidirectional_max_challenger_rank);
        SetLength(relations_local, challenger_limit_local - 1);
        SetLength(candidate_indices_local, challenger_limit_local - 1);
        SetLength(candidate_positions_local, challenger_limit_local - 1);
        SetLength(forward_window_texts_local,
            (challenger_limit_local - 1) * Length(c_forward_radii) * 2);
        SetLength(reverse_window_texts_local,
            (challenger_limit_local - 1) * Length(c_reverse_radii) * 2);
        eligible_count_local := 0;
        for candidate_position_local := 1 to challenger_limit_local - 1 do
        begin
            candidate_index_local := ordered_indices[candidate_position_local];
            if (not rank_features[candidate_index_local].complete_match) or
                (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) then
            begin
                Continue;
            end;
            candidate_units_local := split_text_units(
                Trim(legacy_candidates[candidate_index_local].text));
            if Length(candidate_units_local) <> Length(top_units_local) then
            begin
                Continue;
            end;

            get_text_relation(candidate_index_local, top_index_local,
                relation_local.different_units,
                relation_local.different_runs,
                relation_local.max_different_run,
                relation_local.same_prefix_units,
                relation_local.same_suffix_units,
                relation_local.difference_span_units);
            if relation_local.different_units <= 0 then
            begin
                Continue;
            end;

            relations_local[eligible_count_local] := relation_local;
            candidate_indices_local[eligible_count_local] :=
                candidate_index_local;
            candidate_positions_local[eligible_count_local] :=
                candidate_position_local;

            for radius_index_local := 0 to High(c_forward_radii) do
            begin
                window_start_local := Max(0,
                    relation_local.same_prefix_units -
                    c_forward_radii[radius_index_local]);
                window_end_local := Min(Length(top_units_local),
                    Length(top_units_local) -
                    relation_local.same_suffix_units +
                    c_forward_radii[radius_index_local]);
                top_window_local := build_text_unit_window(
                    top_units_local, window_start_local,
                    window_end_local, False);
                candidate_window_local := build_text_unit_window(
                    candidate_units_local, window_start_local,
                    window_end_local, False);
                text_offset_local := eligible_count_local *
                    Length(c_forward_radii) * 2 + radius_index_local * 2;
                forward_window_texts_local[text_offset_local] :=
                    top_window_local;
                forward_window_texts_local[text_offset_local + 1] :=
                    candidate_window_local;
            end;

            for radius_index_local := 0 to High(c_reverse_radii) do
            begin
                window_start_local := Max(0,
                    relation_local.same_prefix_units -
                    c_reverse_radii[radius_index_local]);
                window_end_local := Min(Length(top_units_local),
                    Length(top_units_local) -
                    relation_local.same_suffix_units +
                    c_reverse_radii[radius_index_local]);
                top_window_local := build_text_unit_window(
                    top_units_local, window_start_local,
                    window_end_local, True);
                candidate_window_local := build_text_unit_window(
                    candidate_units_local, window_start_local,
                    window_end_local, True);
                text_offset_local := eligible_count_local *
                    Length(c_reverse_radii) * 2 + radius_index_local * 2;
                reverse_window_texts_local[text_offset_local] :=
                    top_window_local;
                reverse_window_texts_local[text_offset_local + 1] :=
                    candidate_window_local;
            end;
            Inc(eligible_count_local);
        end;
        if eligible_count_local = 0 then
        begin
            Exit;
        end;

        SetLength(relations_local, eligible_count_local);
        SetLength(candidate_indices_local, eligible_count_local);
        SetLength(candidate_positions_local, eligible_count_local);
        SetLength(forward_window_texts_local, eligible_count_local *
            Length(c_forward_radii) * 2);
        SetLength(reverse_window_texts_local, eligible_count_local *
            Length(c_reverse_radii) * 2);
        if (not get_cached_char_lm_scores(forward_window_texts_local,
            forward_window_scores_local, clsm_span, '')) or
            (Length(forward_window_scores_local) <
            Length(forward_window_texts_local)) then
        begin
            Exit;
        end;

        SetLength(forward_top_scores_local, Length(c_forward_radii));
        SetLength(forward_candidate_scores_local, Length(c_forward_radii));
        SetLength(reverse_top_scores_local, Length(c_reverse_radii));
        SetLength(reverse_candidate_scores_local, Length(c_reverse_radii));

        { The deployed bidirectional model compares only Top1 and Top2, so
          eligible_count_local is at most one. A forward-only teacher gate,
          calibrated to retain every independent-set promotion, avoids the
          reverse-LM query for most candidate pairs. }
        eligible_index_local := 0;
        candidate_index_local := candidate_indices_local[eligible_index_local];
        candidate_position_local :=
            candidate_positions_local[eligible_index_local];
        for radius_index_local := 0 to High(c_forward_radii) do
        begin
            text_offset_local := eligible_index_local *
                Length(c_forward_radii) * 2 + radius_index_local * 2;
            forward_top_scores_local[radius_index_local] :=
                forward_window_scores_local[text_offset_local];
            forward_candidate_scores_local[radius_index_local] :=
                forward_window_scores_local[text_offset_local + 1];
        end;
        relation_local := relations_local[eligible_index_local];
        build_long_complete_pool_difference_features(
            rank_features[candidate_index_local],
            rank_features[top_index_local], candidate_position_local + 1,
            rank_scores[candidate_index_local], rank_scores[top_index_local],
            apply_ranker, abstain_score, relation_local.different_units,
            relation_local.different_runs, relation_local.max_different_run,
            relation_local.same_prefix_units, relation_local.same_suffix_units,
            relation_local.difference_span_units, forward_top_scores_local,
            forward_candidate_scores_local, base_features_local);
        build_long_post_second_slot_gate_features(
            rank_features[candidate_index_local],
            rank_features[top_index_local], candidate_position_local + 1,
            rank_scores[candidate_index_local], rank_scores[top_index_local],
            apply_ranker, abstain_score, relation_local.different_units,
            relation_local.different_runs, relation_local.max_different_run,
            relation_local.same_prefix_units, relation_local.same_suffix_units,
            relation_local.difference_span_units, forward_top_scores_local,
            forward_candidate_scores_local, gate_features_local);
        if long_post_second_slot_gate_score(gate_features_local) <
            c_long_post_second_slot_gate_promotion_threshold then
        begin
            Exit;
        end;

        if (not get_cached_char_lm_scores(reverse_window_texts_local,
            reverse_window_scores_local, clsm_reverse, '')) or
            (Length(reverse_window_scores_local) <
            Length(reverse_window_texts_local)) then
        begin
            Exit;
        end;

        best_position_local := -1;
        best_score_local := -1.0E308;
        for eligible_index_local := 0 to eligible_count_local - 1 do
        begin
            candidate_index_local :=
                candidate_indices_local[eligible_index_local];
            candidate_position_local :=
                candidate_positions_local[eligible_index_local];
            for radius_index_local := 0 to High(c_forward_radii) do
            begin
                text_offset_local := eligible_index_local *
                    Length(c_forward_radii) * 2 + radius_index_local * 2;
                forward_top_scores_local[radius_index_local] :=
                    forward_window_scores_local[text_offset_local];
                forward_candidate_scores_local[radius_index_local] :=
                    forward_window_scores_local[text_offset_local + 1];
            end;
            for radius_index_local := 0 to High(c_reverse_radii) do
            begin
                text_offset_local := eligible_index_local *
                    Length(c_reverse_radii) * 2 + radius_index_local * 2;
                reverse_top_scores_local[radius_index_local] :=
                    reverse_window_scores_local[text_offset_local];
                reverse_candidate_scores_local[radius_index_local] :=
                    reverse_window_scores_local[text_offset_local + 1];
            end;

            relation_local := relations_local[eligible_index_local];
            build_long_complete_pool_difference_features(
                rank_features[candidate_index_local],
                rank_features[top_index_local], candidate_position_local + 1,
                rank_scores[candidate_index_local], rank_scores[top_index_local],
                apply_ranker, abstain_score, relation_local.different_units,
                relation_local.different_runs,
                relation_local.max_different_run,
                relation_local.same_prefix_units,
                relation_local.same_suffix_units,
                relation_local.difference_span_units,
                forward_top_scores_local, forward_candidate_scores_local,
                base_features_local);
            build_long_complete_pool_bidirectional_features(
                base_features_local, reverse_top_scores_local,
                reverse_candidate_scores_local, features_local);
            score_local := long_complete_pool_bidirectional_score(
                features_local);
            if score_local > best_score_local then
            begin
                best_score_local := score_local;
                best_position_local := candidate_position_local;
            end;
        end;
        if (best_position_local <= 0) or
            (best_score_local <
            c_long_complete_pool_bidirectional_threshold) then
        begin
            Exit;
        end;

        candidate_index_local := ordered_indices[best_position_local];
        for candidate_position_local := best_position_local downto 1 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[0] := candidate_index_local;
        apply_ranker := True;
    end;

    procedure apply_complete_pool_settled_top2_residual;
    const
        c_reverse_radii: array[0..4] of Integer = (0, 1, 2, 3, 5);
    var
        top_index_local: Integer;
        second_index_local: Integer;
        different_units_local: Integer;
        different_runs_local: Integer;
        max_different_run_local: Integer;
        same_prefix_units_local: Integer;
        same_suffix_units_local: Integer;
        difference_span_units_local: Integer;
        top_units_local: TArray<string>;
        second_units_local: TArray<string>;
        forward_top_scores_local: TArray<Integer>;
        forward_second_scores_local: TArray<Integer>;
        reverse_window_texts_local: TArray<string>;
        reverse_window_scores_local: TArray<Integer>;
        reverse_top_scores_local: TArray<Integer>;
        reverse_second_scores_local: TArray<Integer>;
        window_start_local: Integer;
        window_end_local: Integer;
        radius_index_local: Integer;
        top_window_local: string;
        second_window_local: string;
        base_features_local: TncLongCompletePoolDifferenceFeatures;
        reverse_base_features_local: TncLongCompletePoolDifferenceFeatures;
        features_local: TncLongSettledTop2ResidualFeatures;
        reverse_features_local: TncLongSettledTop2ResidualFeatures;
        score_local: Double;
    begin
        if (not unified_pool_final) or (candidate_count < 2) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;
        top_index_local := ordered_indices[0];
        second_index_local := ordered_indices[1];
        if (not rank_features[top_index_local].complete_match) or
            (not rank_features[second_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            (Trim(legacy_candidates[second_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) or
            rank_features[second_index_local].source_user or
            rank_features[second_index_local].complete_user or
            rank_features[second_index_local].latest_query_choice or
            (rank_features[second_index_local].query_choice_bonus > 0) or
            (rank_features[top_index_local].text_units <>
            rank_features[second_index_local].text_units) then
        begin
            Exit;
        end;

        get_text_relation(second_index_local, top_index_local,
            different_units_local, different_runs_local,
            max_different_run_local, same_prefix_units_local,
            same_suffix_units_local, difference_span_units_local);
        if (different_units_local <= 0) or
            (not get_top2_cached_span_lm_scores(
            top_index_local, second_index_local,
            forward_top_scores_local, forward_second_scores_local)) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(
            Trim(legacy_candidates[top_index_local].text));
        second_units_local := split_text_units(
            Trim(legacy_candidates[second_index_local].text));
        SetLength(reverse_window_texts_local, Length(c_reverse_radii) * 2);
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            window_start_local := Max(0, same_prefix_units_local -
                c_reverse_radii[radius_index_local]);
            window_end_local := Min(Length(top_units_local),
                Length(top_units_local) - same_suffix_units_local +
                c_reverse_radii[radius_index_local]);
            top_window_local := build_text_unit_window(top_units_local,
                window_start_local, window_end_local, True);
            second_window_local := build_text_unit_window(
                second_units_local, window_start_local,
                window_end_local, True);
            reverse_window_texts_local[radius_index_local * 2] :=
                top_window_local;
            reverse_window_texts_local[radius_index_local * 2 + 1] :=
                second_window_local;
        end;
        if (not get_cached_char_lm_scores(reverse_window_texts_local,
            reverse_window_scores_local, clsm_reverse, '')) or
            (Length(reverse_window_scores_local) <
            Length(reverse_window_texts_local)) then
        begin
            Exit;
        end;
        SetLength(reverse_top_scores_local, Length(c_reverse_radii));
        SetLength(reverse_second_scores_local, Length(c_reverse_radii));
        for radius_index_local := 0 to High(c_reverse_radii) do
        begin
            reverse_top_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2];
            reverse_second_scores_local[radius_index_local] :=
                reverse_window_scores_local[radius_index_local * 2 + 1];
        end;

        build_long_complete_pool_difference_features(
            rank_features[second_index_local], rank_features[top_index_local],
            2, rank_scores[second_index_local], rank_scores[top_index_local],
            apply_ranker, abstain_score, different_units_local,
            different_runs_local, max_different_run_local,
            same_prefix_units_local, same_suffix_units_local,
            difference_span_units_local, forward_top_scores_local,
            forward_second_scores_local, base_features_local);
        build_long_settled_top2_residual_features(base_features_local,
            reverse_top_scores_local, reverse_second_scores_local,
            features_local);
        build_long_complete_pool_difference_features(
            rank_features[top_index_local], rank_features[second_index_local],
            2, rank_scores[top_index_local], rank_scores[second_index_local],
            apply_ranker, abstain_score, different_units_local,
            different_runs_local, max_different_run_local,
            same_prefix_units_local, same_suffix_units_local,
            difference_span_units_local, forward_second_scores_local,
            forward_top_scores_local, reverse_base_features_local);
        build_long_settled_top2_residual_features(reverse_base_features_local,
            reverse_second_scores_local, reverse_top_scores_local,
            reverse_features_local);
        settled_top2_feature_cache_valid := True;
        settled_top2_feature_cache_top_index := top_index_local;
        settled_top2_feature_cache_second_index := second_index_local;
        settled_top2_feature_cache_forward := features_local;
        settled_top2_feature_cache_reverse := reverse_features_local;
        score_local := long_settled_top2_residual_score(features_local);
        if score_local < c_long_settled_top2_residual_threshold then
        begin
            Exit;
        end;
        ordered_indices[0] := second_index_local;
        ordered_indices[1] := top_index_local;
        apply_ranker := True;
    end;

    procedure fill_unified_top2_meta(const candidate_index_local: Integer;
        out meta_local: TncLongUnifiedTop2ArbiterMetaFeatures);
    begin
        FillChar(meta_local, SizeOf(meta_local), 0);
        if (candidate_index_local < 0) or
            (candidate_index_local >= candidate_count) then
        begin
            Exit;
        end;

        if candidate_index_local = top2_pairwise_candidate_index then
        begin
            meta_local[c_long_unified_top2_meta_top2_pairwise_score] :=
                top2_pairwise_score;
            meta_local[c_long_unified_top2_meta_top2_pairwise_eligible] :=
                Ord(top2_pairwise_eligible);
            meta_local[c_long_unified_top2_meta_top2_pairwise_swapped] :=
                Ord(top2_pairwise_swapped);
        end;
        if exact_anchor_pairwise_debug_scores[candidate_index_local] <>
            Low(Int64) then
        begin
            meta_local[
                c_long_unified_top2_meta_exact_anchor_pairwise_score] :=
                exact_anchor_pairwise_debug_scores[candidate_index_local];
            meta_local[
                c_long_unified_top2_meta_exact_anchor_pairwise_eligible] := 1;
        end;
        meta_local[c_long_unified_top2_meta_exact_anchor_pairwise_moved] :=
            Ord(candidate_index_local = exact_anchor_pairwise_moved_index);
        if candidate_index_local = exact_anchor_pairwise_moved_index then
        begin
            meta_local[
                c_long_unified_top2_meta_exact_anchor_pairwise_target_rank] :=
                exact_anchor_pairwise_target_rank;
        end;
        if complete_pool_final_pairwise_debug_scores[candidate_index_local] <>
            Low(Int64) then
        begin
            meta_local[
                c_long_unified_top2_meta_complete_pool_final_pairwise_score] :=
                complete_pool_final_pairwise_debug_scores[candidate_index_local];
            meta_local[
                c_long_unified_top2_meta_complete_pool_final_pairwise_eligible] :=
                1;
        end;
        meta_local[
            c_long_unified_top2_meta_complete_pool_final_pairwise_promoted] :=
            Ord(candidate_index_local =
            complete_pool_final_pairwise_promoted_index);
        meta_local[
            c_long_unified_top2_meta_complete_pool_final_pairwise_current_rank] :=
            complete_pool_final_pairwise_current_rank_debug[
            candidate_index_local];
        if complete_pool_post_static_difference_debug_scores[
            candidate_index_local] <> Low(Int64) then
        begin
            meta_local[
                c_long_unified_top2_meta_complete_pool_post_static_difference_score] :=
                complete_pool_post_static_difference_debug_scores[
                candidate_index_local];
            meta_local[
                c_long_unified_top2_meta_complete_pool_post_static_difference_eligible] :=
                1;
        end;
        meta_local[
            c_long_unified_top2_meta_complete_pool_post_static_difference_promoted] :=
            Ord(candidate_index_local =
            complete_pool_post_static_difference_promoted_index);
        meta_local[
            c_long_unified_top2_meta_complete_pool_post_static_difference_current_rank] :=
            complete_pool_post_static_difference_current_rank_debug[
            candidate_index_local];
        if local_difference_debug_scores[candidate_index_local] <>
            Low(Int64) then
        begin
            meta_local[c_long_unified_top2_meta_local_difference_score] :=
                local_difference_debug_scores[candidate_index_local];
        end;
        meta_local[c_long_unified_top2_meta_local_difference_promoted] :=
            Ord(candidate_index_local = local_difference_promoted_index);
        meta_local[c_long_unified_top2_meta_local_difference_current_rank] :=
            local_difference_current_rank_debug[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_top1_votes] :=
            ranking_top1_votes[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_top2_votes] :=
            ranking_top2_votes[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_first_top_stage] :=
            ranking_first_top_stage[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_last_top_stage] :=
            ranking_last_top_stage[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_top_entry_count] :=
            ranking_top_entry_count[candidate_index_local];
        meta_local[c_long_unified_top2_meta_ranking_pre_settled_top] := Ord(
            (ranking_stage_capture_count >
            c_ranking_stage_post_static_bidirectional) and
            (ranking_stage_top_indices[
            c_ranking_stage_post_static_bidirectional] =
            candidate_index_local));
    end;

    procedure apply_long_unified_top2_arbiter;
    var
        top_index_local: Integer;
        second_index_local: Integer;
        different_units_local: Integer;
        different_runs_local: Integer;
        max_different_run_local: Integer;
        same_prefix_units_local: Integer;
        same_suffix_units_local: Integer;
        difference_span_units_local: Integer;
        base_features_local: TncLongSettledTop2ResidualFeatures;
        top_meta_local: TncLongUnifiedTop2ArbiterMetaFeatures;
        second_meta_local: TncLongUnifiedTop2ArbiterMetaFeatures;
        features_local: TncLongUnifiedTop2ArbiterFeatures;
        query_parts_local: TncPinyinParseResult;
        query_syllables_local: TncLongTop2DifferencePinyinSyllables;
        expanded_query_syllables_local:
            TncLongUnifiedTop2TextPinyinSyllables;
        evidence_features_local: TncLongUnifiedTop2EvidenceFeatures;
        query_index_local: Integer;
        aligned_score_local: Int64;
        expanded_difference_score_local: Int64;
    begin
        if (not unified_pool_final) or (candidate_count < 2) or
            (not settled_top2_feature_cache_valid) then
        begin
            Exit;
        end;
        top_index_local := ordered_indices[0];
        second_index_local := ordered_indices[1];
        if (top_index_local = settled_top2_feature_cache_top_index) and
            (second_index_local = settled_top2_feature_cache_second_index) then
        begin
            base_features_local := settled_top2_feature_cache_forward;
        end
        else if (top_index_local = settled_top2_feature_cache_second_index) and
            (second_index_local = settled_top2_feature_cache_top_index) then
        begin
            base_features_local := settled_top2_feature_cache_reverse;
        end
        else
        begin
            Exit;
        end;

        get_text_relation(second_index_local, top_index_local,
            different_units_local,
            different_runs_local, max_different_run_local,
            same_prefix_units_local, same_suffix_units_local,
            difference_span_units_local);
        if different_units_local <= 0 then
        begin
            Exit;
        end;
        if Max(rank_features[top_index_local].
            complete_pool_proper_name_confidence,
            rank_features[second_index_local].
            complete_pool_proper_name_confidence) > 0 then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_proper_name;
        end
        else if different_units_local = 1 then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_single_char;
        end
        else if (different_runs_local = 1) and
            (different_units_local = 2) then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_contiguous_2;
        end
        else if (different_runs_local = 1) and
            (different_units_local <= 4) then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_contiguous_3_4;
        end
        else if different_runs_local = 1 then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_contiguous_long;
        end
        else if different_runs_local > 1 then
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_sparse;
        end
        else
        begin
            unified_top2_arbiter_family_value :=
                c_long_unified_top2_family_other;
        end;

        query_parts_local := get_effective_compact_pinyin_syllables(
            normalized_query);
        SetLength(query_syllables_local, Length(query_parts_local));
        SetLength(expanded_query_syllables_local, Length(query_parts_local));
        for query_index_local := 0 to High(query_parts_local) do
        begin
            query_syllables_local[query_index_local] :=
                normalize_pinyin_text(query_parts_local[query_index_local].text);
            expanded_query_syllables_local[query_index_local] :=
                query_syllables_local[query_index_local];
        end;
        aligned_score_local := long_top2_pairwise_difference_score(
            query_syllables_local,
            legacy_candidates[second_index_local].text,
            legacy_candidates[top_index_local].text,
            legacy_paths[second_index_local], legacy_paths[top_index_local]);
        expanded_difference_score_local := long_unified_top2_text_score(
            expanded_query_syllables_local,
            legacy_candidates[second_index_local].text,
            legacy_candidates[top_index_local].text,
            legacy_paths[second_index_local], legacy_paths[top_index_local]);
        build_long_unified_top2_evidence_features(
            expanded_query_syllables_local,
            legacy_candidates[top_index_local].text,
            legacy_candidates[second_index_local].text,
            evidence_features_local);
        fill_unified_top2_meta(top_index_local, top_meta_local);
        fill_unified_top2_meta(second_index_local, second_meta_local);
        build_long_unified_top2_arbiter_features(base_features_local,
            top_meta_local, second_meta_local, aligned_score_local,
            expanded_difference_score_local,
            unified_top2_arbiter_family_value, evidence_features_local,
            features_local);
        // Training stores the dense matrix as float32. Quantize only this
        // final arbiter input so native tree comparisons use the same values.
        for query_index_local := 0 to
            c_long_unified_top2_arbiter_feature_count - 1 do
        begin
            features_local[query_index_local] :=
                nc_quantize_single(features_local[query_index_local]);
        end;
        unified_top2_arbiter_score_value :=
            long_unified_top2_arbiter_score(features_local);
        if m_debug_target_recall_text <> '' then
        begin
            SetLength(unified_top2_arbiter_feature_values,
                c_long_unified_top2_arbiter_feature_count);
            for query_index_local := 0 to
                c_long_unified_top2_arbiter_feature_count - 1 do
            begin
                unified_top2_arbiter_feature_values[query_index_local] :=
                    features_local[query_index_local];
            end;
        end;
        if unified_top2_arbiter_score_value <
            long_unified_top2_arbiter_threshold(
            unified_top2_arbiter_family_value) then
        begin
            Exit;
        end;

        ordered_indices[0] := second_index_local;
        ordered_indices[1] := top_index_local;
        unified_top2_arbiter_swapped := True;
        apply_ranker := True;
    end;

    procedure apply_deep_local_repair_fallback;
    var
        top_index_local: Integer;
        candidate_index_local: Integer;
        candidate_position_local: Integer;
        best_position_local: Integer;
        best_gain_local: Integer;
        gain_local: Integer;
        different_units_local: Integer;
        top_text_local: string;
        candidate_text_local: string;
        char_index_local: Integer;
    begin
        if (not unified_pool_final) or (candidate_count < 2) then
        begin
            Exit;
        end;

        top_index_local := ordered_indices[0];
        top_text_local := Trim(legacy_candidates[top_index_local].text);
        if (not rank_features[top_index_local].complete_match) or
            (Trim(legacy_candidates[top_index_local].comment) <> '') or
            rank_features[top_index_local].source_user or
            rank_features[top_index_local].complete_user or
            rank_features[top_index_local].latest_query_choice or
            (rank_features[top_index_local].query_choice_bonus > 0) or
            (rank_features[top_index_local].text_units <>
            m_last_lookup_syllable_count) then
        begin
            Exit;
        end;

        best_position_local := -1;
        best_gain_local := Low(Integer);
        for candidate_position_local := 1 to Min(candidate_count - 1,
            c_deep_local_repair_max_rank - 1) do
        begin
            candidate_index_local :=
                ordered_indices[candidate_position_local];
            if (rank_features[candidate_index_local].
                complete_pool_source_kind <> Ord(lcps_local_repair)) or
                (not rank_features[candidate_index_local].complete_match) or
                rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user or
                rank_features[candidate_index_local].latest_query_choice or
                (rank_features[candidate_index_local].query_choice_bonus > 0) or
                (rank_features[candidate_index_local].path_segments -
                rank_features[top_index_local].path_segments > 1) or
                (rank_features[candidate_index_local].word_lm_bonus <
                rank_features[top_index_local].word_lm_bonus) or
                (rank_features[candidate_index_local].char_lm_score -
                rank_features[top_index_local].char_lm_score <
                c_deep_local_repair_min_char_lm_gain) or
                (rank_features[candidate_index_local].char_lm_suffix_score -
                rank_features[top_index_local].char_lm_suffix_score <
                c_deep_local_repair_min_suffix_lm_gain) then
            begin
                Continue;
            end;

            candidate_text_local := Trim(
                legacy_candidates[candidate_index_local].text);
            if (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                (Length(candidate_text_local) <> Length(top_text_local)) then
            begin
                Continue;
            end;

            different_units_local := 0;
            for char_index_local := 1 to Length(top_text_local) do
            begin
                if top_text_local[char_index_local] <>
                    candidate_text_local[char_index_local] then
                begin
                    Inc(different_units_local);
                    if different_units_local > 1 then
                    begin
                        Break;
                    end;
                end;
            end;
            if different_units_local <> 1 then
            begin
                Continue;
            end;

            gain_local :=
                rank_features[candidate_index_local].char_lm_score -
                rank_features[top_index_local].char_lm_score +
                rank_features[candidate_index_local].char_lm_suffix_score -
                rank_features[top_index_local].char_lm_suffix_score;
            if gain_local > best_gain_local then
            begin
                best_gain_local := gain_local;
                best_position_local := candidate_position_local;
            end;
        end;
        if best_position_local <= 0 then
        begin
            Exit;
        end;

        candidate_index_local := ordered_indices[best_position_local];
        for candidate_position_local := best_position_local downto 1 do
        begin
            ordered_indices[candidate_position_local] :=
                ordered_indices[candidate_position_local - 1];
        end;
        ordered_indices[0] := candidate_index_local;
        apply_ranker := True;
    end;

    function ranking_stage_name(const stage_id: Integer): string;
    begin
        case stage_id of
            c_ranking_stage_legacy_input:
                Result := 'legacy_input';
            c_ranking_stage_unified_ranker:
                Result := 'unified_ranker';
            c_ranking_stage_anchor_guard:
                Result := 'anchor_guard';
            c_ranking_stage_top2_pairwise:
                Result := 'top2_pairwise';
            c_ranking_stage_exact_anchor_pairwise:
                Result := 'exact_anchor_pairwise';
            c_ranking_stage_bidirectional_difference:
                Result := 'bidirectional_difference';
            c_ranking_stage_second_slot_bidirectional:
                Result := 'second_slot_bidirectional';
            c_ranking_stage_final_pairwise_residual:
                Result := 'final_pairwise_residual';
            c_ranking_stage_post_static_difference:
                Result := 'post_static_difference';
            c_ranking_stage_ranker_second_slot_recovery:
                Result := 'ranker_second_slot_recovery';
            c_ranking_stage_bidirectional_second_slot:
                Result := 'bidirectional_second_slot';
            c_ranking_stage_post_static_bidirectional:
                Result := 'post_static_bidirectional';
            c_ranking_stage_settled_top2_residual:
                Result := 'settled_top2_residual';
            c_ranking_stage_unified_top2_arbiter:
                Result := 'unified_top2_arbiter';
            c_ranking_stage_deep_local_repair:
                Result := 'deep_local_repair';
        else
            Result := 'unknown';
        end;
    end;

    procedure capture_ranking_stage(const stage_id: Integer);
    var
        stage_index_local: Integer;
        current_tick_local: Int64;
        elapsed_us_local: Int64;
    begin
        if m_config.debug_mode and (ranking_phase_frequency > 0) then
        begin
            QueryPerformanceCounter(current_tick_local);
            elapsed_us_local := ((current_tick_local - ranking_phase_tick) *
                1000000) div ranking_phase_frequency;
            ranking_phase_tick := current_tick_local;
            if elapsed_us_local > 0 then
            begin
                if m_last_lookup_debug_extra <> '' then
                begin
                    m_last_lookup_debug_extra :=
                        m_last_lookup_debug_extra + ' ';
                end;
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format('lrkus_%s=%d', [ranking_stage_name(stage_id),
                    elapsed_us_local]);
            end;
        end;
        if (not unified_pool_final) or (candidate_count <= 0) or
            (Length(ordered_indices) < candidate_count) or
            (ranking_stage_capture_count >= c_ranking_stage_count) then
        begin
            Exit;
        end;

        stage_index_local := ranking_stage_capture_count;
        ranking_stage_ids[stage_index_local] := stage_id;
        ranking_stage_top_indices[stage_index_local] := ordered_indices[0];
        ranking_stage_second_indices[stage_index_local] := -1;
        if candidate_count >= 2 then
        begin
            ranking_stage_second_indices[stage_index_local] :=
                ordered_indices[1];
        end;
        Inc(ranking_stage_capture_count);
    end;

    procedure build_ranking_stage_vote_features;
    var
        stage_index_local: Integer;
        candidate_index_local: Integer;
        previous_top_index_local: Integer;
    begin
        SetLength(ranking_top1_votes, candidate_count);
        SetLength(ranking_top2_votes, candidate_count);
        SetLength(ranking_first_top_stage, candidate_count);
        SetLength(ranking_last_top_stage, candidate_count);
        SetLength(ranking_top_entry_count, candidate_count);
        for candidate_index_local := 0 to candidate_count - 1 do
        begin
            ranking_first_top_stage[candidate_index_local] := -1;
            ranking_last_top_stage[candidate_index_local] := -1;
        end;

        previous_top_index_local := -1;
        for stage_index_local := 0 to ranking_stage_capture_count - 1 do
        begin
            candidate_index_local := ranking_stage_top_indices[
                stage_index_local];
            if (candidate_index_local >= 0) and
                (candidate_index_local < candidate_count) then
            begin
                Inc(ranking_top1_votes[candidate_index_local]);
                if ranking_first_top_stage[candidate_index_local] < 0 then
                begin
                    ranking_first_top_stage[candidate_index_local] :=
                        stage_index_local;
                end;
                ranking_last_top_stage[candidate_index_local] :=
                    stage_index_local;
                if candidate_index_local <> previous_top_index_local then
                begin
                    Inc(ranking_top_entry_count[candidate_index_local]);
                end;
            end;
            candidate_index_local := ranking_stage_second_indices[
                stage_index_local];
            if (candidate_index_local >= 0) and
                (candidate_index_local < candidate_count) then
            begin
                Inc(ranking_top2_votes[candidate_index_local]);
            end;
            previous_top_index_local := ranking_stage_top_indices[
                stage_index_local];
        end;
    end;

    procedure restore_pre_settled_top2_order;
    var
        top_index_local: Integer;
        second_index_local: Integer;
    begin
        if (ranking_stage_capture_count <=
            c_ranking_stage_post_static_bidirectional) or
            (candidate_count < 2) then
        begin
            Exit;
        end;

        top_index_local := ranking_stage_top_indices[
            c_ranking_stage_post_static_bidirectional];
        second_index_local := ranking_stage_second_indices[
            c_ranking_stage_post_static_bidirectional];
        if (top_index_local < 0) or (top_index_local >= candidate_count) or
            (second_index_local < 0) or
            (second_index_local >= candidate_count) or
            (top_index_local = second_index_local) then
        begin
            Exit;
        end;

        { The settled residual remains a proposal feature. Restore the order
          it received so one authoritative arbiter makes the final swap. }
        ordered_indices[0] := top_index_local;
        ordered_indices[1] := second_index_local;
    end;

    procedure flush_ranking_stages;
    var
        stage_index_local: Integer;
        candidate_index_local: Integer;
    begin
        if (not m_debug_capture_long_ranking_stages) or
            (ranking_stage_capture_count <= 0) then
        begin
            Exit;
        end;

        SetLength(m_debug_long_ranking_stages, ranking_stage_capture_count);
        for stage_index_local := 0 to ranking_stage_capture_count - 1 do
        begin
            m_debug_long_ranking_stages[stage_index_local].stage_name :=
                ranking_stage_name(ranking_stage_ids[stage_index_local]);
            m_debug_long_ranking_stages[stage_index_local].top1_text := '';
            candidate_index_local :=
                ranking_stage_top_indices[stage_index_local];
            if (candidate_index_local >= 0) and
                (candidate_index_local < Length(legacy_candidates)) then
            begin
                m_debug_long_ranking_stages[stage_index_local].top1_text :=
                    Trim(legacy_candidates[candidate_index_local].text);
            end;
            m_debug_long_ranking_stages[stage_index_local].top2_text := '';
            candidate_index_local :=
                ranking_stage_second_indices[stage_index_local];
            if (candidate_index_local >= 0) and
                (candidate_index_local < Length(legacy_candidates)) then
            begin
                m_debug_long_ranking_stages[stage_index_local].top2_text :=
                    Trim(legacy_candidates[candidate_index_local].text);
            end;
        end;
    end;

    procedure copy_features_to_debug(const features:
        TncLongFinalRankerFeatures; var debug_item:
        TncLongFinalCandidateDebug);
    begin
        debug_item.candidate_score := features.candidate_score;
        debug_item.dict_weight := features.dict_weight;
        debug_item.has_dict_weight := features.has_dict_weight;
        debug_item.source_user := features.source_user;
        debug_item.source_chain := features.source_chain;
        debug_item.source_pattern := features.source_pattern;
        debug_item.source_redup := features.source_redup;
        debug_item.source_local_rerank := features.source_local_rerank;
        debug_item.source_rule_fallback := features.source_rule_fallback;
        debug_item.legacy_top := features.legacy_top;
        debug_item.chain_rank := features.chain_rank;
        debug_item.chain_present := features.chain_present;
        debug_item.chain_first_stage_score :=
            features.chain_first_stage_score;
        debug_item.chain_second_stage_score :=
            features.chain_second_stage_score;
        debug_item.chain_score_gap := features.chain_score_gap;
        debug_item.complete_match := features.complete_match;
        debug_item.partial_match := features.partial_match;
        debug_item.text_units := features.text_units;
        debug_item.comment_length := features.comment_length;
        debug_item.unit_delta := features.unit_delta;
        debug_item.path_available := features.path_available;
        debug_item.path_confidence_score :=
            features.path_confidence_score;
        debug_item.path_confidence_tier := features.path_confidence_tier;
        debug_item.path_segments := features.path_segments;
        debug_item.path_single_segments := features.path_single_segments;
        debug_item.path_max_segment_units :=
            features.path_max_segment_units;
        debug_item.char_lm_score := features.char_lm_score;
        debug_item.char_lm_suffix_score := features.char_lm_suffix_score;
        debug_item.char_lm_context_score := features.char_lm_context_score;
        debug_item.char_lm_context_gain := features.char_lm_context_gain;
        debug_item.has_left_context := features.has_left_context;
        debug_item.query_choice_bonus := features.query_choice_bonus;
        debug_item.latest_query_choice := features.latest_query_choice;
        debug_item.query_path_bonus := features.query_path_bonus;
        debug_item.query_path_penalty := features.query_path_penalty;
        debug_item.word_lm_bonus := features.word_lm_bonus;
        debug_item.word_lm_boundary_count := features.word_lm_boundary_count;
        debug_item.word_lm_boundary_min := features.word_lm_boundary_min;
        debug_item.word_lm_boundary_max := features.word_lm_boundary_max;
        debug_item.word_lm_boundary_first := features.word_lm_boundary_first;
        debug_item.word_lm_boundary_last := features.word_lm_boundary_last;
        debug_item.word_lm_supported_ratio := features.word_lm_supported_ratio;
        debug_item.word_lm_strong_ratio := features.word_lm_strong_ratio;
        debug_item.word_lm_trigram_ratio := features.word_lm_trigram_ratio;
        debug_item.word_lm_zero_count := features.word_lm_zero_count;
        debug_item.input_syllable_count := features.input_syllable_count;
        debug_item.score_per_unit := features.score_per_unit;
        debug_item.dict_weight_per_unit := features.dict_weight_per_unit;
        debug_item.complete_user := features.complete_user;
        debug_item.complete_dictionary := features.complete_dictionary;
        debug_item.complete_chain := features.complete_chain;
        debug_item.complete_pool_present := features.complete_pool_present;
        debug_item.complete_pool_source_kind :=
            features.complete_pool_source_kind;
        debug_item.complete_pool_rank := features.complete_pool_rank;
        debug_item.complete_pool_seed_rank :=
            features.complete_pool_seed_rank;
        debug_item.complete_pool_original :=
            features.complete_pool_original;
        debug_item.complete_pool_substitutions :=
            features.complete_pool_substitutions;
        debug_item.complete_pool_changed_position :=
            features.complete_pool_changed_position;
        debug_item.complete_pool_anchor_present :=
            features.complete_pool_anchor_present;
        debug_item.complete_pool_anchor_start :=
            features.complete_pool_anchor_start;
        debug_item.complete_pool_anchor_units :=
            features.complete_pool_anchor_units;
        debug_item.complete_pool_anchor_exact_rank :=
            features.complete_pool_anchor_exact_rank;
        debug_item.complete_pool_anchor_source_weight :=
            features.complete_pool_anchor_source_weight;
        debug_item.complete_pool_anchor_replacement_weight :=
            features.complete_pool_anchor_replacement_weight;
        debug_item.complete_pool_anchor_top_weight :=
            features.complete_pool_anchor_top_weight;
        debug_item.complete_pool_anchor_weight_gain :=
            features.complete_pool_anchor_weight_gain;
        debug_item.complete_pool_pair_evidence :=
            features.complete_pool_pair_evidence;
        debug_item.complete_pool_proper_name_confidence :=
            features.complete_pool_proper_name_confidence;
        debug_item.complete_pool_signature_support :=
            features.complete_pool_signature_support;
        debug_item.complete_pool_consensus_support :=
            features.complete_pool_consensus_support;
        debug_item.complete_pool_consensus_seed_count :=
            features.complete_pool_consensus_seed_count;
        debug_item.complete_pool_consensus_support_mean :=
            features.complete_pool_consensus_support_mean;
        debug_item.complete_pool_consensus_support_min :=
            features.complete_pool_consensus_support_min;
        debug_item.complete_pool_consensus_majority_units :=
            features.complete_pool_consensus_majority_units;
        debug_item.complete_pool_consensus_unanimous_units :=
            features.complete_pool_consensus_unanimous_units;
        debug_item.complete_pool_consensus_nearest_distance :=
            features.complete_pool_consensus_nearest_distance;
        debug_item.complete_pool_consensus_mean_distance :=
            features.complete_pool_consensus_mean_distance;
        debug_item.complete_pool_consensus_changed_support :=
            features.complete_pool_consensus_changed_support;
        debug_item.complete_pool_consensus_changed_top_match :=
            features.complete_pool_consensus_changed_top_match;
        debug_item.complete_pool_local_pairwise_score :=
            features.complete_pool_local_pairwise_score;
        debug_item.complete_pool_edge_model_anchor_count :=
            features.complete_pool_edge_model_anchor_count;
        debug_item.complete_pool_edge_model_score_total :=
            features.complete_pool_edge_model_score_total;
        debug_item.complete_pool_edge_model_score_max :=
            features.complete_pool_edge_model_score_max;
        debug_item.complete_pool_edge_model_word_count :=
            features.complete_pool_edge_model_word_count;
        debug_item.complete_pool_edge_model_word_score_total :=
            features.complete_pool_edge_model_word_score_total;
        debug_item.complete_pool_edge_model_word_score_min :=
            features.complete_pool_edge_model_word_score_min;
        debug_item.complete_pool_edge_model_word_score_max :=
            features.complete_pool_edge_model_word_score_max;
        debug_item.complete_pool_edge_model_word_score_mean :=
            features.complete_pool_edge_model_word_score_mean;
    end;

    procedure apply_host_neural_reranker;
    const
        c_neural_candidate_limit = 16;
        c_direct_generation_limit = 4;
        c_direct_generation_word_max_syllables = 5;
    var
        ordered_position_local: Integer;
        candidate_index_local: Integer;
        neural_index_local: Integer;
        duplicate_index_local: Integer;
        generated_index_local: Integer;
        generated_existing_neural_local: Integer;
        generated_existing_candidate_local: Integer;
        generated_count_local: Integer;
        replacement_candidate_index_local: Integer;
        path_score_local: Integer;
        path_segment_count_local: Integer;
        selected_candidate_index_local: Integer;
        candidate_text_local: string;
        generated_path_local: string;
        generator_query_local: string;
        generated_top_score_local: Single;
        duplicate_local: Boolean;
        generator_syllables_local: TncPinyinParseResult;
        generated_candidates_local: TncLongGeneratedCandidateArray;
        generated_candidate_local: TncLongFinalCandidateDebug;
        replacement_candidate_local: TncCandidate;
        function is_allowed_generated_single_char_local(
            const candidate_text: string): Boolean;
        begin
            Result := False;
            if Length(candidate_text) <> 1 then
            begin
                Exit;
            end;

            case Ord(candidate_text[1]) of
                $6211, $4F60, $4ED6, $5979, $5B83, $4EEC, $548C, $53BB, $6765,
                $5728, $662F, $6709, $8981, $60F3, $4F1A, $80FD, $53EA, $628A, $88AB,
                $8BA9, $7ED9, $5411, $5BF9, $8DDF, $540C, $4ECE, $4E0E, $4E8E, $5C31,
                $4E5F, $90FD, $8FD8, $53C8, $518D, $4E0D, $5F88, $592A, $6700, $5DF2,
                $5C06, $5230, $7684, $4E86, $7740, $8FC7, $4F46, $867D, $800C, $5E76,
                $5374, $4E14:
                    Result := True;
            end;
        end;

        function infer_generated_exact_path_local(const candidate_text: string;
            out out_segment_count: Integer): string;
        var
            units_local: TArray<string>;
            valid_local: TArray<Boolean>;
            paths_local: TArray<string>;
            segment_counts_local: TArray<Integer>;
            position_local: Integer;
            segment_length_local: Integer;
            syllable_index_local: Integer;
            signature_index_local: Integer;
            segment_pinyin_local: string;
            segment_text_local: string;

            function try_signature_path_local(const signature_path: string;
                out signature_segment_count: Integer): string;
            var
                signature_parts_local: TArray<string>;
                signature_part_local: string;
                signature_units_local: TArray<string>;
                signature_cursor_local: Integer;
                signature_length_local: Integer;
                signature_syllable_local: Integer;
                signature_pinyin_local: string;
                signature_text_local: string;
            begin
                Result := '';
                signature_segment_count := 0;
                signature_parts_local :=
                    signature_path.Split([c_segment_path_separator]);
                signature_cursor_local := 0;
                for signature_part_local in signature_parts_local do
                begin
                    signature_units_local :=
                        split_text_units(Trim(signature_part_local));
                    signature_length_local := Length(signature_units_local);
                    if (signature_length_local <= 0) or
                        (signature_cursor_local + signature_length_local >
                        Length(units_local)) then
                    begin
                        Exit('');
                    end;
                    signature_pinyin_local := '';
                    signature_text_local := '';
                    for signature_syllable_local := signature_cursor_local to
                        signature_cursor_local + signature_length_local - 1 do
                    begin
                        signature_pinyin_local := signature_pinyin_local +
                            LowerCase(Trim(generator_syllables_local[
                            signature_syllable_local].text));
                        signature_text_local := signature_text_local +
                            units_local[signature_syllable_local];
                    end;
                    if signature_length_local = 1 then
                    begin
                        if not is_allowed_generated_single_char_local(
                            signature_text_local) then
                        begin
                            Exit('');
                        end;
                    end
                    else if not m_dictionary.is_base_entry(
                        signature_pinyin_local, signature_text_local) then
                    begin
                        Exit('');
                    end;
                    if Result <> '' then
                    begin
                        Result := Result + c_segment_path_separator;
                    end;
                    Result := Result + signature_text_local;
                    Inc(signature_segment_count);
                    Inc(signature_cursor_local, signature_length_local);
                end;
                if (signature_cursor_local <> Length(units_local)) or
                    (signature_segment_count <= 1) then
                begin
                    Result := '';
                    signature_segment_count := 0;
                end;
            end;
        begin
            Result := '';
            out_segment_count := 0;
            if (m_dictionary = nil) or
                (Length(generator_syllables_local) < 2) then
            begin
                Exit;
            end;
            units_local := split_text_units(Trim(candidate_text));
            if Length(units_local) <> Length(generator_syllables_local) then
            begin
                Exit;
            end;

            for signature_index_local := 0 to
                Min(neural_index_local - 1, 3) do
            begin
                Result := try_signature_path_local(
                    neural_candidates[signature_index_local].segment_path,
                    out_segment_count);
                if Result <> '' then
                begin
                    Exit;
                end;
            end;

            SetLength(valid_local, Length(units_local) + 1);
            SetLength(paths_local, Length(units_local) + 1);
            SetLength(segment_counts_local, Length(units_local) + 1);
            valid_local[Length(units_local)] := True;
            for position_local := High(units_local) downto 0 do
            begin
                for segment_length_local :=
                    Min(c_direct_generation_word_max_syllables,
                    Length(units_local) - position_local) downto 2 do
                begin
                    if not valid_local[position_local + segment_length_local] then
                    begin
                        Continue;
                    end;
                    segment_pinyin_local := '';
                    segment_text_local := '';
                    for syllable_index_local := position_local to
                        position_local + segment_length_local - 1 do
                    begin
                        segment_pinyin_local := segment_pinyin_local +
                            LowerCase(Trim(generator_syllables_local[
                            syllable_index_local].text));
                        segment_text_local := segment_text_local +
                            units_local[syllable_index_local];
                    end;
                    if not m_dictionary.is_base_entry(segment_pinyin_local,
                        segment_text_local) then
                    begin
                        Continue;
                    end;
                    valid_local[position_local] := True;
                    segment_counts_local[position_local] :=
                        segment_counts_local[position_local +
                        segment_length_local] + 1;
                    paths_local[position_local] := segment_text_local;
                    if paths_local[position_local + segment_length_local] <> '' then
                    begin
                        paths_local[position_local] :=
                            paths_local[position_local] +
                            c_segment_path_separator +
                            paths_local[position_local + segment_length_local];
                    end;
                    Break;
                end;
                if valid_local[position_local] then
                begin
                    Continue;
                end;
                if valid_local[position_local + 1] and
                    is_allowed_generated_single_char_local(
                    units_local[position_local]) then
                begin
                    valid_local[position_local] := True;
                    segment_counts_local[position_local] :=
                        segment_counts_local[position_local + 1] + 1;
                    paths_local[position_local] := units_local[position_local];
                    if paths_local[position_local + 1] <> '' then
                    begin
                        paths_local[position_local] :=
                            paths_local[position_local] +
                            c_segment_path_separator +
                            paths_local[position_local + 1];
                    end;
                end;
            end;
            if valid_local[0] and (segment_counts_local[0] > 1) then
            begin
                Result := paths_local[0];
                out_segment_count := segment_counts_local[0];
            end;
        end;
    begin
        if (not run_host_neural_reranker) or
            (m_long_neural_reranker = nil) or (not unified_pool_final) or
            (candidate_count < 2) or (m_last_lookup_syllable_count < 6) or
            (m_last_lookup_syllable_count > 40) or
            (m_config.pinyin_input_scheme <> pis_full_pinyin) or
            is_fuzzy_pinyin_active or (not is_full_pinyin_key(
            normalized_query)) then
        begin
            Exit;
        end;

        SetLength(neural_candidates, Min(candidate_count,
            c_neural_candidate_limit));
        SetLength(neural_candidate_indices, Length(neural_candidates));
        neural_index_local := 0;
        for ordered_position_local := 0 to candidate_count - 1 do
        begin
            candidate_index_local := ordered_indices[ordered_position_local];
            if (not rank_features[candidate_index_local].complete_match) or
                (Trim(legacy_candidates[candidate_index_local].comment) <> '') or
                (rank_features[candidate_index_local].text_units <>
                m_last_lookup_syllable_count) then
            begin
                Continue;
            end;
            if rank_features[candidate_index_local].source_user or
                rank_features[candidate_index_local].complete_user then
            begin
                { User choices remain authoritative and are never submitted to
                  the corpus-trained model. }
                SetLength(neural_candidates, 0);
                SetLength(neural_candidate_indices, 0);
                Exit;
            end;
            candidate_text_local := Trim(
                legacy_candidates[candidate_index_local].text);
            if candidate_text_local = '' then
            begin
                Continue;
            end;
            duplicate_local := False;
            for duplicate_index_local := 0 to neural_index_local - 1 do
            begin
                if SameText(neural_candidates[duplicate_index_local].text,
                    candidate_text_local) then
                begin
                    duplicate_local := True;
                    Break;
                end;
            end;
            if duplicate_local then
            begin
                Continue;
            end;

            neural_candidates[neural_index_local] :=
                Default(TncLongFinalCandidateDebug);
            neural_candidates[neural_index_local].legacy_rank :=
                candidate_index_local + 1;
            neural_candidates[neural_index_local].final_rank :=
                neural_index_local + 1;
            copy_features_to_debug(rank_features[candidate_index_local],
                neural_candidates[neural_index_local]);
            if SameText(candidate_text_local,
                Trim(exact_edge_baseline_top1)) then
            begin
                neural_candidates[neural_index_local].exact_edge_shadow_rank := 1;
            end
            else if SameText(candidate_text_local,
                Trim(exact_edge_baseline_top2)) then
            begin
                neural_candidates[neural_index_local].exact_edge_shadow_rank := 2;
            end;
            neural_candidates[neural_index_local].exact_edge_auditor_decision :=
                exact_edge_auditor_decision;
            neural_candidates[neural_index_local].exact_edge_pre_auditor_rank :=
                exact_edge_pre_auditor_ranks[candidate_index_local];
            if exact_edge_auditor_decision >= 0 then
            begin
                neural_candidates[neural_index_local].exact_edge_auditor_score :=
                    Round(exact_edge_auditor_score * 1000000.0);
                neural_candidates[neural_index_local].exact_edge_auditor_threshold :=
                    Round(exact_edge_auditor_threshold * 1000000.0);
            end;
            neural_candidates[neural_index_local].ranker_score :=
                rank_scores[candidate_index_local];
            neural_candidates[neural_index_local].abstain_score := abstain_score;
            neural_candidates[neural_index_local].ranker_applied := apply_ranker;
            neural_candidates[neural_index_local].text :=
                legacy_candidates[candidate_index_local].text;
            neural_candidates[neural_index_local].comment :=
                legacy_candidates[candidate_index_local].comment;
            neural_candidates[neural_index_local].segment_path :=
                legacy_paths[candidate_index_local];
            neural_candidate_indices[neural_index_local] :=
                candidate_index_local;
            Inc(neural_index_local);
            if neural_index_local >= c_neural_candidate_limit then
            begin
                Break;
            end;
        end;
        SetLength(neural_candidates, neural_index_local);
        SetLength(neural_candidate_indices, neural_index_local);
        if neural_index_local < 2 then
        begin
            Exit;
        end;

        generator_syllables_local :=
            get_effective_compact_pinyin_syllables(normalized_query);
        generator_query_local := '';
        if Length(generator_syllables_local) =
            m_last_lookup_syllable_count then
        begin
            for generated_index_local := 0 to
                High(generator_syllables_local) do
            begin
                if generator_query_local <> '' then
                begin
                    generator_query_local := generator_query_local + '''';
                end;
                generator_query_local := generator_query_local +
                    LowerCase(Trim(
                    generator_syllables_local[generated_index_local].text));
            end;
        end;
        SetLength(generated_candidates_local, 0);
        if generator_query_local <> '' then
        begin
            try
                if m_long_neural_reranker.should_generate(
                    generator_query_local, neural_candidates) then
                begin
                    m_long_neural_reranker.try_generate(generator_query_local,
                        generated_candidates_local);
                end;
            except
                SetLength(generated_candidates_local, 0);
            end;
        end;
        if Length(generated_candidates_local) >
            c_direct_generation_limit then
        begin
            SetLength(generated_candidates_local,
                c_direct_generation_limit);
        end;
        generated_count_local := 0;
        generated_top_score_local := 0.0;
        if Length(generated_candidates_local) > 0 then
        begin
            generated_top_score_local :=
                generated_candidates_local[0].normalized_score;
        end;
        for generated_index_local := 0 to
            High(generated_candidates_local) do
        begin
            candidate_text_local := Trim(
                generated_candidates_local[generated_index_local].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                m_last_lookup_syllable_count) then
            begin
                Continue;
            end;
            generated_existing_neural_local := -1;
            for duplicate_index_local := 0 to neural_index_local - 1 do
            begin
                if SameText(Trim(
                    neural_candidates[duplicate_index_local].text),
                    candidate_text_local) then
                begin
                    generated_existing_neural_local :=
                        duplicate_index_local;
                    Break;
                end;
            end;
            if generated_existing_neural_local = 0 then
            begin
                { The generator agreeing with the settled Top1 is not a
                  challenge and does not need a duplicate model row. }
                Continue;
            end;
            duplicate_local := False;
            for duplicate_index_local := neural_index_local to
                neural_index_local + generated_count_local - 1 do
            begin
                if SameText(Trim(
                    neural_candidates[duplicate_index_local].text),
                    candidate_text_local) then
                begin
                    duplicate_local := True;
                    Break;
                end;
            end;
            if duplicate_local then
            begin
                Continue;
            end;

            path_score_local := 0;
            path_segment_count_local := 0;
            if generated_existing_neural_local > 0 then
            begin
                generated_path_local := Trim(
                    neural_candidates[generated_existing_neural_local].segment_path);
                path_score_local := neural_candidates[
                    generated_existing_neural_local].path_confidence_score;
                path_segment_count_local := neural_candidates[
                    generated_existing_neural_local].path_segments;
            end
            else
            begin
                generated_path_local := infer_generated_exact_path_local(
                    candidate_text_local, path_segment_count_local);
            end;
            if generated_path_local = '' then
            begin
                Continue;
            end;

            generated_existing_candidate_local := -1;
            for candidate_index_local := 0 to candidate_count - 1 do
            begin
                if SameText(Trim(
                    legacy_candidates[candidate_index_local].text),
                    candidate_text_local) and
                    (Trim(legacy_candidates[candidate_index_local].comment) = '') then
                begin
                    generated_existing_candidate_local :=
                        candidate_index_local;
                    Break;
                end;
            end;
            if generated_existing_neural_local > 0 then
            begin
                generated_candidate_local :=
                    neural_candidates[generated_existing_neural_local];
            end
            else
            begin
                generated_candidate_local :=
                    Default(TncLongFinalCandidateDebug);
                generated_candidate_local.input_syllable_count :=
                    m_last_lookup_syllable_count;
                generated_candidate_local.text_units :=
                    m_last_lookup_syllable_count;
                generated_candidate_local.complete_match := True;
                generated_candidate_local.path_available := True;
                generated_candidate_local.path_segments :=
                    path_segment_count_local;
                generated_candidate_local.path_confidence_score :=
                    path_score_local;
                generated_candidate_local.segment_path :=
                    generated_path_local;
                generated_candidate_local.text := candidate_text_local;
            end;
            generated_candidate_local.source_direct_generation := True;
            generated_candidate_local.direct_generation_rank :=
                generated_index_local + 1;
            generated_candidate_local.direct_generation_score :=
                generated_candidates_local[
                generated_index_local].normalized_score;
            generated_candidate_local.direct_generation_score_delta :=
                generated_candidate_local.direct_generation_score -
                generated_top_score_local;
            generated_candidate_local.direct_generation_existing_rank := 0;
            if generated_existing_neural_local > 0 then
            begin
                generated_candidate_local.direct_generation_existing_rank :=
                    generated_existing_neural_local + 1;
            end;
            generated_candidate_local.text := candidate_text_local;
            generated_candidate_local.comment := '';
            generated_candidate_local.segment_path := generated_path_local;
            SetLength(neural_candidates, Length(neural_candidates) + 1);
            SetLength(neural_candidate_indices,
                Length(neural_candidate_indices) + 1);
            neural_candidates[High(neural_candidates)] :=
                generated_candidate_local;
            neural_candidate_indices[High(neural_candidate_indices)] :=
                generated_existing_candidate_local;
            Inc(generated_count_local);
        end;

        neural_selected_index := 0;
        try
            if (not m_long_neural_reranker.try_select(normalized_query,
                neural_candidates, neural_selected_index)) or
                (neural_selected_index <= 0) or
                (neural_selected_index >= Length(neural_candidates)) then
            begin
                Exit;
            end;
        except
            { A missing or unhealthy optional model must never break input. }
            Exit;
        end;

        selected_candidate_index_local :=
            neural_candidate_indices[neural_selected_index];
        if selected_candidate_index_local < 0 then
        begin
            replacement_candidate_index_local :=
                ordered_indices[candidate_count - 1];
            replacement_candidate_local := Default(TncCandidate);
            replacement_candidate_local.text :=
                neural_candidates[neural_selected_index].text;
            replacement_candidate_local.comment := '';
            replacement_candidate_local.score :=
                legacy_candidates[ordered_indices[0]].score;
            replacement_candidate_local.source := cs_rule;
            replacement_candidate_local.has_dict_weight := False;
            replacement_candidate_local.dict_weight := 0;
            replacement_candidate_local.display_kind := cdk_default;
            legacy_candidates[replacement_candidate_index_local] :=
                replacement_candidate_local;
            legacy_paths[replacement_candidate_index_local] :=
                neural_candidates[neural_selected_index].segment_path;
            legacy_source_indices[replacement_candidate_index_local] := -1;
            FillChar(rank_features[replacement_candidate_index_local],
                SizeOf(rank_features[replacement_candidate_index_local]), 0);
            rank_features[replacement_candidate_index_local].complete_match :=
                True;
            rank_features[replacement_candidate_index_local].text_units :=
                m_last_lookup_syllable_count;
            rank_features[replacement_candidate_index_local].path_available :=
                True;
            rank_features[replacement_candidate_index_local].path_segments :=
                neural_candidates[neural_selected_index].path_segments;
            rank_features[replacement_candidate_index_local].
                path_confidence_score :=
                neural_candidates[neural_selected_index].
                path_confidence_score;
            remember_segment_path_for_candidate(
                replacement_candidate_local.text, '',
                legacy_paths[replacement_candidate_index_local]);
            selected_candidate_index_local :=
                replacement_candidate_index_local;
        end;
        for ordered_position_local := 0 to candidate_count - 1 do
        begin
            if ordered_indices[ordered_position_local] =
                selected_candidate_index_local then
            begin
                for candidate_index_local := ordered_position_local downto 1 do
                begin
                    ordered_indices[candidate_index_local] :=
                        ordered_indices[candidate_index_local - 1];
                end;
                ordered_indices[0] := selected_candidate_index_local;
                apply_ranker := True;
                Break;
            end;
        end;
    end;

begin
    ranking_phase_tick := 0;
    ranking_phase_frequency := 0;
    if m_config.debug_mode then
    begin
        QueryPerformanceFrequency(ranking_phase_frequency);
        QueryPerformanceCounter(ranking_phase_tick);
    end;
    SetLength(m_debug_long_final_candidates, 0);
    SetLength(m_debug_long_ranking_stages, 0);
    ranking_stage_capture_count := 0;
    bidirectional_top1_swapped := False;
    settled_top2_feature_cache_valid := False;
    settled_top2_feature_cache_top_index := -1;
    settled_top2_feature_cache_second_index := -1;
    unified_top2_arbiter_score_value := -1.0E300;
    unified_top2_arbiter_family_value :=
        c_long_unified_top2_family_other;
    unified_top2_arbiter_swapped := False;
    SetLength(unified_top2_arbiter_feature_values, 0);
    exact_edge_auditor_score := -MaxDouble;
    exact_edge_auditor_threshold := MaxDouble;
    exact_edge_auditor_decision := -1;
    exact_edge_fallback_auditor_score := -MaxDouble;
    exact_edge_fallback_auditor_threshold := MaxDouble;
    exact_edge_fallback_auditor_decision := -1;
    exact_edge_baseline_top1_index := -1;
    exact_edge_baseline_top2_index := -1;
    if unified_pool_final then
    begin
        profile := c_long_complete_pool_ranker_default_profile;
    end
    else
    begin
        profile := c_long_final_ranker_default_profile;
    end;
    if m_debug_long_final_ranker_profile >= 0 then
    begin
        profile := m_debug_long_final_ranker_profile;
    end;

    candidate_count := Length(candidates);
    if (m_last_lookup_syllable_count <= 5) or (candidate_count < 2) or
        (m_dictionary = nil) or
        ((profile <= 0) and (m_debug_target_recall_text = '')) then
    begin
        Exit;
    end;

    SetLength(complete_pool_final_pairwise_debug_scores, candidate_count);
    SetLength(complete_pool_final_pairwise_current_rank_debug,
        candidate_count);
    complete_pool_final_pairwise_promoted_index := -1;
    SetLength(complete_pool_post_static_difference_debug_scores,
        candidate_count);
    SetLength(complete_pool_post_static_difference_current_rank_debug,
        candidate_count);
    complete_pool_post_static_difference_promoted_index := -1;
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        complete_pool_final_pairwise_debug_scores[candidate_idx] :=
            Low(Int64);
        complete_pool_post_static_difference_debug_scores[candidate_idx] :=
            Low(Int64);
    end;

    legacy_candidates := Copy(candidates);
    legacy_source_indices := Copy(source_indices);
    if Length(legacy_source_indices) < candidate_count then
    begin
        source_idx := Length(legacy_source_indices);
        SetLength(legacy_source_indices, candidate_count);
        while source_idx < candidate_count do
        begin
            legacy_source_indices[source_idx] := -1;
            Inc(source_idx);
        end;
    end;
    SetLength(legacy_paths, candidate_count);
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        source_idx := legacy_source_indices[candidate_idx];
        if (source_idx >= 0) and (source_idx < Length(m_candidates)) then
        begin
            legacy_paths[candidate_idx] := get_segment_path_for_candidate(
                m_candidates[source_idx], source_idx);
        end
        else
        begin
            legacy_paths[candidate_idx] := get_segment_path_for_candidate(
                legacy_candidates[candidate_idx]);
        end;
    end;

    normalized_query := normalize_pinyin_text(m_last_lookup_key);
    latest_choice_text := get_cached_query_latest_choice_text(normalized_query);
    get_recent_path_context_seed(previous_previous_text, previous_text);
    left_context := Trim(previous_previous_text) + Trim(previous_text);
    has_left_context := left_context <> '';

    SetLength(candidate_texts, candidate_count);
    SetLength(candidate_chain_indices, candidate_count);
    SetLength(candidate_pool_indices, candidate_count);
    SetLength(char_scores, candidate_count);
    SetLength(char_suffix_scores, candidate_count);
    SetLength(char_context_scores, candidate_count);
    SetLength(missing_candidate_indices, candidate_count);
    SetLength(missing_candidate_texts, candidate_count);
    missing_count := 0;
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        candidate_texts[candidate_idx] :=
            Trim(legacy_candidates[candidate_idx].text);
        if unified_pool_final and
            (candidate_idx < Length(
            m_runtime_long_complete_pool_candidates)) and
            SameText(candidate_texts[candidate_idx], Trim(
            m_runtime_long_complete_pool_candidates[
            candidate_idx].candidate.text)) then
        begin
            { copy_runtime_pool_to_internal preserves this index mapping. }
            pool_idx := candidate_idx;
        end
        else
        begin
            pool_idx := find_complete_pool_candidate_index(
                candidate_texts[candidate_idx]);
        end;
        candidate_pool_indices[candidate_idx] := pool_idx;
        chain_idx := find_chain_candidate_index(candidate_texts[candidate_idx]);
        candidate_chain_indices[candidate_idx] := chain_idx;
        if pool_idx >= 0 then
        begin
            char_scores[candidate_idx] :=
                m_runtime_long_complete_pool_candidates[pool_idx].char_lm_score;
            char_suffix_scores[candidate_idx] :=
                m_runtime_long_complete_pool_candidates[pool_idx].char_lm_suffix_score;
            char_context_scores[candidate_idx] :=
                m_runtime_long_complete_pool_candidates[pool_idx].char_lm_context_score;
            Continue;
        end;
        runtime_features_match := (chain_idx >= 0) and
            m_runtime_long_chain_candidates[chain_idx].features_valid and
            SameText(m_runtime_long_chain_candidates[chain_idx].feature_query_key,
            normalized_query) and
            SameText(m_runtime_long_chain_candidates[chain_idx].feature_context_key,
            left_context);
        if runtime_features_match then
        begin
            char_scores[candidate_idx] :=
                m_runtime_long_chain_candidates[chain_idx].char_lm_score;
            char_suffix_scores[candidate_idx] :=
                m_runtime_long_chain_candidates[chain_idx].char_lm_suffix_score;
            char_context_scores[candidate_idx] :=
                m_runtime_long_chain_candidates[chain_idx].char_lm_context_score;
        end
        else
        begin
            missing_candidate_indices[missing_count] := candidate_idx;
            missing_candidate_texts[missing_count] :=
                candidate_texts[candidate_idx];
            Inc(missing_count);
        end;
    end;
    SetLength(missing_candidate_indices, missing_count);
    SetLength(missing_candidate_texts, missing_count);
    char_scores_ok := True;
    if missing_count > 0 then
    begin
        char_scores_ok := get_cached_char_lm_scores(missing_candidate_texts,
            missing_char_scores, clsm_full, '') and
            (Length(missing_char_scores) = missing_count);
        if char_scores_ok then
        begin
            if (not get_cached_char_lm_scores(missing_candidate_texts,
                missing_char_suffix_scores, clsm_suffix, '')) or
                (Length(missing_char_suffix_scores) <> missing_count) then
            begin
                missing_char_suffix_scores := Copy(missing_char_scores);
            end;
            if has_left_context and
                get_cached_char_lm_scores(missing_candidate_texts,
                missing_char_context_scores, clsm_context, left_context) and
                (Length(missing_char_context_scores) = missing_count) then
            begin
                { Keep the contextual scores returned by the dictionary. }
            end
            else
            begin
                missing_char_context_scores :=
                    Copy(missing_char_suffix_scores);
            end;
            for missing_idx := 0 to missing_count - 1 do
            begin
                candidate_idx := missing_candidate_indices[missing_idx];
                char_scores[candidate_idx] :=
                    missing_char_scores[missing_idx];
                char_suffix_scores[candidate_idx] :=
                    missing_char_suffix_scores[missing_idx];
                char_context_scores[candidate_idx] :=
                    missing_char_context_scores[missing_idx];
            end;
        end;
    end;
    if not char_scores_ok then
    begin
        { Preserve the legacy all-candidate fallback if a partial batch fails. }
        char_scores_ok := get_cached_char_lm_scores(candidate_texts,
            char_scores, clsm_full, '') and
            (Length(char_scores) = candidate_count);
        if not char_scores_ok then
        begin
            SetLength(char_scores, candidate_count);
        end;
        if (not get_cached_char_lm_scores(candidate_texts,
            char_suffix_scores, clsm_suffix, '')) or
            (Length(char_suffix_scores) <> candidate_count) then
        begin
            char_suffix_scores := Copy(char_scores);
        end;
        if has_left_context and
            get_cached_char_lm_scores(candidate_texts, char_context_scores,
            clsm_context, left_context) and
            (Length(char_context_scores) = candidate_count) then
        begin
            { Keep the contextual scores returned by the dictionary. }
        end
        else
        begin
            char_context_scores := Copy(char_suffix_scores);
        end;
    end;

    top_chain_score := 0;
    if Length(m_runtime_long_chain_candidates) > 0 then
    begin
        top_chain_score :=
            m_runtime_long_chain_candidates[0].second_stage_score;
    end;

    SetLength(rank_features, candidate_count);
    SetLength(rank_scores, candidate_count);
    SetLength(ordered_indices, candidate_count);
    SetLength(final_ranks, candidate_count);
    complete_count := 0;
    chain_count := 0;
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        FillChar(rank_features[candidate_idx],
            SizeOf(rank_features[candidate_idx]), 0);
        with rank_features[candidate_idx] do
        begin
            candidate_score := legacy_candidates[candidate_idx].score;
            dict_weight := legacy_candidates[candidate_idx].dict_weight;
            has_dict_weight :=
                legacy_candidates[candidate_idx].has_dict_weight;
            source_user := legacy_candidates[candidate_idx].source = cs_user;
            source_pattern := is_runtime_common_pattern_candidate(
                legacy_candidates[candidate_idx]);
            source_redup := is_runtime_redup_candidate(
                legacy_candidates[candidate_idx]);
            source_local_rerank := is_local_rerank_candidate(
                legacy_candidates[candidate_idx].text);
            legacy_rank := candidate_idx + 1;
            legacy_top := candidate_idx = 0;
            chain_idx := candidate_chain_indices[candidate_idx];
            source_chain := chain_idx >= 0;
            chain_present := source_chain;
            if chain_present then
            begin
                chain_rank :=
                    m_runtime_long_chain_candidates[chain_idx].rank;
                chain_first_stage_score :=
                    m_runtime_long_chain_candidates[chain_idx].first_stage_score;
                chain_second_stage_score :=
                    m_runtime_long_chain_candidates[chain_idx].second_stage_score;
                chain_score_gap := chain_second_stage_score - top_chain_score;
                Inc(chain_count);
            end;
            source_rule_fallback :=
                (legacy_candidates[candidate_idx].source = cs_rule) and
                (not has_dict_weight) and (not source_chain) and
                (not source_pattern) and (not source_redup) and
                (not source_local_rerank);
            text_units := get_candidate_text_unit_count(
                legacy_candidates[candidate_idx].text);
            comment_length := Length(Trim(
                legacy_candidates[candidate_idx].comment));
            partial_match := comment_length > 0;
            unit_delta := text_units - m_last_lookup_syllable_count;
            complete_match := (not partial_match) and (unit_delta = 0);
            if complete_match then
            begin
                Inc(complete_count);
            end;
            path_available := legacy_paths[candidate_idx] <> '';
            path_confidence_score := get_candidate_path_confidence_score(
                legacy_candidates[candidate_idx]);
            path_confidence_tier := get_candidate_path_confidence_tier(
                legacy_candidates[candidate_idx]);
            runtime_path_features_match := chain_present and
                m_runtime_long_chain_candidates[chain_idx].features_valid and
                SameText(m_runtime_long_chain_candidates[chain_idx].feature_query_key,
                normalized_query) and
                SameText(m_runtime_long_chain_candidates[chain_idx].segment_path,
                legacy_paths[candidate_idx]);
            if path_available and runtime_path_features_match then
            begin
                path_segments :=
                    m_runtime_long_chain_candidates[chain_idx].path_segments;
                path_single_segments :=
                    m_runtime_long_chain_candidates[chain_idx].path_single_segments;
                path_max_segment_units :=
                    m_runtime_long_chain_candidates[chain_idx].path_max_segment_units;
            end
            else if path_available and get_cached_long_path_structure(
                legacy_paths[candidate_idx], path_structure) then
            begin
                path_segments := path_structure.segment_count;
                path_single_segments :=
                    path_structure.single_segment_count;
                path_max_segment_units :=
                    path_structure.max_segment_units;
            end;
            char_lm_score := char_scores[candidate_idx];
            char_lm_suffix_score := char_suffix_scores[candidate_idx];
            char_lm_context_score := char_context_scores[candidate_idx];
            char_lm_context_gain :=
                char_lm_context_score - char_lm_suffix_score;
            has_left_context := Length(left_context) > 0;
            query_choice_bonus := m_dictionary.get_query_choice_bonus(
                normalized_query, legacy_candidates[candidate_idx].text);
            latest_query_choice := (latest_choice_text <> '') and
                SameText(latest_choice_text,
                Trim(legacy_candidates[candidate_idx].text));
            if path_available and runtime_path_features_match then
            begin
                query_path_bonus :=
                    m_runtime_long_chain_candidates[chain_idx].query_path_bonus;
                query_path_penalty :=
                    m_runtime_long_chain_candidates[chain_idx].query_path_penalty;
                word_lm_bonus :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_bonus;
                word_lm_boundary_count :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_boundary_count;
                word_lm_boundary_min :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_boundary_min;
                word_lm_boundary_max :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_boundary_max;
                word_lm_boundary_first :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_boundary_first;
                word_lm_boundary_last :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_boundary_last;
                word_lm_supported_ratio :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_supported_ratio;
                word_lm_strong_ratio :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_strong_ratio;
                word_lm_trigram_ratio :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_trigram_ratio;
                word_lm_zero_count :=
                    m_runtime_long_chain_candidates[chain_idx].word_lm_zero_count;
            end
            else if path_available then
            begin
                query_path_bonus := get_cached_query_segment_path_bonus(
                    normalized_query, legacy_paths[candidate_idx]);
                query_path_penalty :=
                    get_cached_query_segment_path_penalty(
                    normalized_query, legacy_paths[candidate_idx]);
            end;
            input_syllable_count := m_last_lookup_syllable_count;
            if text_units > 0 then
            begin
                score_per_unit := candidate_score div text_units;
                dict_weight_per_unit := dict_weight div text_units;
            end;
            complete_user := complete_match and source_user;
            complete_dictionary := complete_match and has_dict_weight;
            complete_chain := complete_match and source_chain;
            pool_idx := candidate_pool_indices[candidate_idx];
            complete_pool_present := pool_idx >= 0;
            if complete_pool_present then
            begin
                complete_pool_source_kind := Ord(
                    m_runtime_long_complete_pool_candidates[pool_idx].source_kind);
                complete_pool_rank :=
                    m_runtime_long_complete_pool_candidates[pool_idx].pool_rank;
                complete_pool_seed_rank :=
                    m_runtime_long_complete_pool_candidates[pool_idx].seed_rank;
                complete_pool_original :=
                    m_runtime_long_complete_pool_candidates[pool_idx].original;
                complete_pool_substitutions :=
                    m_runtime_long_complete_pool_candidates[pool_idx].substitutions;
                complete_pool_changed_position :=
                    m_runtime_long_complete_pool_candidates[pool_idx].changed_position;
                complete_pool_anchor_present :=
                    m_runtime_long_complete_pool_candidates[pool_idx].anchor_units > 0;
                complete_pool_anchor_start :=
                    m_runtime_long_complete_pool_candidates[pool_idx].anchor_start;
                complete_pool_anchor_units :=
                    m_runtime_long_complete_pool_candidates[pool_idx].anchor_units;
                complete_pool_anchor_exact_rank :=
                    m_runtime_long_complete_pool_candidates[pool_idx].anchor_exact_rank;
                complete_pool_anchor_source_weight :=
                    m_runtime_long_complete_pool_candidates[pool_idx].source_char_weight;
                complete_pool_anchor_replacement_weight :=
                    m_runtime_long_complete_pool_candidates[pool_idx].replacement_char_weight;
                complete_pool_anchor_top_weight :=
                    m_runtime_long_complete_pool_candidates[pool_idx].anchor_top_weight;
                complete_pool_anchor_weight_gain :=
                    m_runtime_long_complete_pool_candidates[pool_idx].replacement_char_weight -
                    m_runtime_long_complete_pool_candidates[pool_idx].source_char_weight;
                complete_pool_pair_evidence :=
                    m_runtime_long_complete_pool_candidates[pool_idx].pair_evidence;
                complete_pool_proper_name_confidence :=
                    m_runtime_long_complete_pool_candidates[pool_idx].proper_name_confidence;
                complete_pool_signature_support :=
                    m_runtime_long_complete_pool_candidates[pool_idx].signature_support;
                complete_pool_consensus_support :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_support;
                complete_pool_consensus_seed_count :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_seed_count;
                complete_pool_consensus_support_mean := Round(
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_support_mean *
                    1000.0);
                complete_pool_consensus_support_min := Round(
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_support_min *
                    1000.0);
                complete_pool_consensus_majority_units :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_majority_units;
                complete_pool_consensus_unanimous_units :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_unanimous_units;
                complete_pool_consensus_nearest_distance :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_nearest_distance;
                complete_pool_consensus_mean_distance := Round(
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_mean_distance *
                    1000.0);
                complete_pool_consensus_changed_support := Round(
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_changed_support *
                    1000.0);
                complete_pool_consensus_changed_top_match :=
                    m_runtime_long_complete_pool_candidates[pool_idx].consensus_changed_top_match > 0;
                complete_pool_local_pairwise_score :=
                    m_runtime_long_complete_pool_candidates[pool_idx].local_pairwise_score;
                complete_pool_edge_model_anchor_count :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_anchor_count;
                complete_pool_edge_model_score_total :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_score_total;
                complete_pool_edge_model_score_max :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_score_max;
                complete_pool_edge_model_word_count :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_word_count;
                complete_pool_edge_model_word_score_total :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_word_score_total;
                complete_pool_edge_model_word_score_min :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_word_score_min;
                complete_pool_edge_model_word_score_max :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_word_score_max;
                complete_pool_edge_model_word_score_mean :=
                    m_runtime_long_complete_pool_candidates[pool_idx].
                    edge_model_word_score_mean;
                word_lm_bonus :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_bonus;
                word_lm_boundary_count :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_boundary_count;
                word_lm_boundary_min :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_boundary_min;
                word_lm_boundary_max :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_boundary_max;
                word_lm_boundary_first :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_boundary_first;
                word_lm_boundary_last :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_boundary_last;
                word_lm_supported_ratio :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_supported_ratio;
                word_lm_strong_ratio :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_strong_ratio;
                word_lm_trigram_ratio :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_trigram_ratio;
                word_lm_zero_count :=
                    m_runtime_long_complete_pool_candidates[pool_idx].word_lm_zero_count;
            end;
        end;
        if profile > 0 then
        begin
            if unified_pool_final then
            begin
                rank_scores[candidate_idx] :=
                    long_complete_pool_ranker_score(
                    rank_features[candidate_idx]);
            end
            else
            begin
                rank_scores[candidate_idx] := long_final_ranker_score(
                    rank_features[candidate_idx]);
            end;
        end
        else
        begin
            rank_scores[candidate_idx] := 0;
        end;
        ordered_indices[candidate_idx] := candidate_idx;
    end;

    capture_ranking_stage(c_ranking_stage_legacy_input);

    if profile > 0 then
    begin
        for candidate_idx := 1 to candidate_count - 1 do
        begin
            current_idx := ordered_indices[candidate_idx];
            insert_idx := candidate_idx - 1;
            while (insert_idx >= 0) and index_is_better(current_idx,
                ordered_indices[insert_idx]) do
            begin
                ordered_indices[insert_idx + 1] :=
                    ordered_indices[insert_idx];
                Dec(insert_idx);
            end;
            ordered_indices[insert_idx + 1] := current_idx;
        end;
    end;

    for candidate_idx := 0 to candidate_count - 1 do
    begin
        final_ranks[ordered_indices[candidate_idx]] := candidate_idx + 1;
    end;

    abstain_score := 0;
    apply_ranker := profile = 1;
    if profile >= 2 then
    begin
        top_idx := ordered_indices[0];
        second_idx := ordered_indices[Min(1, candidate_count - 1)];
        third_idx := ordered_indices[Min(2, candidate_count - 1)];
        lowest_idx := ordered_indices[candidate_count - 1];
        if top_idx <> 0 then
        begin
            FillChar(abstain_features, SizeOf(abstain_features), 0);
            abstain_features.candidate_count := candidate_count;
            abstain_features.complete_count := complete_count;
            abstain_features.chain_count := chain_count;
            abstain_features.input_syllable_count :=
                m_last_lookup_syllable_count;
            abstain_features.has_left_context := has_left_context;
            abstain_features.ranker_top_score := rank_scores[top_idx];
            abstain_features.ranker_second_score := rank_scores[second_idx];
            abstain_features.ranker_third_score := rank_scores[third_idx];
            abstain_features.ranker_top_margin := rank_scores[top_idx] -
                rank_scores[second_idx];
            abstain_features.ranker_second_margin := rank_scores[second_idx] -
                rank_scores[third_idx];
            abstain_features.ranker_score_range := rank_scores[top_idx] -
                rank_scores[lowest_idx];
            abstain_features.ranker_top_legacy_rank := top_idx + 1;
            abstain_features.ranker_top_chain_rank :=
                rank_features[top_idx].chain_rank;
            abstain_features.ranker_top_complete :=
                rank_features[top_idx].complete_match;
            abstain_features.ranker_top_user :=
                rank_features[top_idx].source_user;
            abstain_features.ranker_top_dictionary :=
                rank_features[top_idx].has_dict_weight;
            abstain_features.ranker_top_chain :=
                rank_features[top_idx].source_chain;
            abstain_features.legacy_top_ranker_score := rank_scores[0];
            abstain_features.ranker_top_over_legacy_margin :=
                rank_scores[top_idx] - rank_scores[0];
            abstain_features.ranker_disagrees := top_idx <> 0;
            abstain_features.legacy_top_complete :=
                rank_features[0].complete_match;
            abstain_features.legacy_top_user := rank_features[0].source_user;
            abstain_features.legacy_top_dictionary :=
                rank_features[0].has_dict_weight;
            abstain_features.legacy_top_chain :=
                rank_features[0].source_chain;
            abstain_features.legacy_top_chain_rank :=
                rank_features[0].chain_rank;
            abstain_features.ranker_top_char_lm_score :=
                rank_features[top_idx].char_lm_score;
            abstain_features.legacy_top_char_lm_score :=
                rank_features[0].char_lm_score;
            abstain_features.ranker_top_char_lm_gain :=
                rank_features[top_idx].char_lm_score -
                rank_features[0].char_lm_score;
            abstain_features.ranker_top_path_confidence :=
                rank_features[top_idx].path_confidence_score;
            abstain_features.legacy_top_path_confidence :=
                rank_features[0].path_confidence_score;
            abstain_features.ranker_top_path_confidence_gain :=
                rank_features[top_idx].path_confidence_score -
                rank_features[0].path_confidence_score;
            abstain_features.ranker_top_query_choice_bonus :=
                rank_features[top_idx].query_choice_bonus;
            abstain_features.legacy_top_query_choice_bonus :=
                rank_features[0].query_choice_bonus;
            abstain_features.ranker_top_pool_source_kind :=
                rank_features[top_idx].complete_pool_source_kind;
            abstain_features.legacy_top_pool_source_kind :=
                rank_features[0].complete_pool_source_kind;
            abstain_features.ranker_top_pool_rank :=
                rank_features[top_idx].complete_pool_rank;
            abstain_features.legacy_top_pool_rank :=
                rank_features[0].complete_pool_rank;
            abstain_features.ranker_top_pair_evidence :=
                rank_features[top_idx].complete_pool_pair_evidence;
            abstain_features.legacy_top_pair_evidence :=
                rank_features[0].complete_pool_pair_evidence;
            abstain_features.ranker_top_word_lm_bonus :=
                rank_features[top_idx].word_lm_bonus;
            abstain_features.legacy_top_word_lm_bonus :=
                rank_features[0].word_lm_bonus;
            abstain_features.ranker_top_word_lm_gain :=
                rank_features[top_idx].word_lm_bonus -
                rank_features[0].word_lm_bonus;
            abstain_features.ranker_top_consensus_support :=
                rank_features[top_idx].complete_pool_consensus_support;
            abstain_features.legacy_top_consensus_support :=
                rank_features[0].complete_pool_consensus_support;
            abstain_features.ranker_top_consensus_gain :=
                rank_features[top_idx].complete_pool_consensus_support -
                rank_features[0].complete_pool_consensus_support;
            abstain_features.ranker_top_proper_name_confidence :=
                rank_features[top_idx].complete_pool_proper_name_confidence;
            abstain_features.legacy_top_proper_name_confidence :=
                rank_features[0].complete_pool_proper_name_confidence;
            abstain_features.ranker_top_local_pairwise_score :=
                rank_features[top_idx].complete_pool_local_pairwise_score;
            abstain_features.legacy_top_local_pairwise_score :=
                rank_features[0].complete_pool_local_pairwise_score;
            abstain_features.ranker_top_edge_model_anchor_count :=
                rank_features[top_idx].complete_pool_edge_model_anchor_count;
            abstain_features.legacy_top_edge_model_anchor_count :=
                rank_features[0].complete_pool_edge_model_anchor_count;
            abstain_features.ranker_top_edge_model_anchor_gain :=
                rank_features[top_idx].complete_pool_edge_model_anchor_count -
                rank_features[0].complete_pool_edge_model_anchor_count;
            abstain_features.ranker_top_edge_model_score_total :=
                rank_features[top_idx].complete_pool_edge_model_score_total;
            abstain_features.legacy_top_edge_model_score_total :=
                rank_features[0].complete_pool_edge_model_score_total;
            abstain_features.ranker_top_edge_model_score_total_gain :=
                rank_features[top_idx].complete_pool_edge_model_score_total -
                rank_features[0].complete_pool_edge_model_score_total;
            abstain_features.ranker_top_edge_model_score_max :=
                rank_features[top_idx].complete_pool_edge_model_score_max;
            abstain_features.legacy_top_edge_model_score_max :=
                rank_features[0].complete_pool_edge_model_score_max;
            abstain_features.ranker_top_edge_model_score_max_gain :=
                rank_features[top_idx].complete_pool_edge_model_score_max -
                rank_features[0].complete_pool_edge_model_score_max;
            abstain_features.ranker_top_edge_model_word_count :=
                rank_features[top_idx].complete_pool_edge_model_word_count;
            abstain_features.legacy_top_edge_model_word_count :=
                rank_features[0].complete_pool_edge_model_word_count;
            abstain_features.ranker_top_edge_model_word_count_gain :=
                rank_features[top_idx].complete_pool_edge_model_word_count -
                rank_features[0].complete_pool_edge_model_word_count;
            abstain_features.ranker_top_edge_model_word_score_mean :=
                rank_features[top_idx].complete_pool_edge_model_word_score_mean;
            abstain_features.legacy_top_edge_model_word_score_mean :=
                rank_features[0].complete_pool_edge_model_word_score_mean;
            abstain_features.ranker_top_edge_model_word_score_mean_gain :=
                rank_features[top_idx].complete_pool_edge_model_word_score_mean -
                rank_features[0].complete_pool_edge_model_word_score_mean;
            abstain_features.ranker_top_edge_model_word_score_min :=
                rank_features[top_idx].complete_pool_edge_model_word_score_min;
            abstain_features.legacy_top_edge_model_word_score_min :=
                rank_features[0].complete_pool_edge_model_word_score_min;
            abstain_features.ranker_top_edge_model_word_score_min_gain :=
                rank_features[top_idx].complete_pool_edge_model_word_score_min -
                rank_features[0].complete_pool_edge_model_word_score_min;
            if unified_pool_final then
            begin
                abstain_score := long_complete_pool_abstain_score(
                    abstain_features);
            end
            else
            begin
                abstain_score := long_final_abstain_score(abstain_features);
            end;
            apply_ranker := abstain_score > 0;
        end;
    end;

    if not unified_pool_final then
    begin
    { Compare the current visible Top1 with each challenger only after the
      existing ranker and abstain decision. This stage reuses all calculated
      features, does not query the dictionary, and can promote one item at
      most. }
    baseline_ranker_applied := apply_ranker;
    if not apply_ranker then
    begin
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            ordered_indices[candidate_idx] := candidate_idx;
        end;
    end;
    best_visible_pairwise_position := -1;
    best_visible_pairwise_score := Low(Int64);
    top_idx := ordered_indices[0];
    if c_long_visible_pairwise_residual_max_challenger_rank > 0 then
    begin
        visible_challenger_count := Min(candidate_count - 1,
            c_long_visible_pairwise_residual_max_challenger_rank - 1);
    end
    else
    begin
        visible_challenger_count := candidate_count - 1;
    end;
    for candidate_idx := 1 to visible_challenger_count do
    begin
        current_idx := ordered_indices[candidate_idx];
        build_long_visible_pairwise_residual_features(
            rank_features[current_idx], rank_features[top_idx],
            candidate_idx + 1, rank_scores[current_idx],
            rank_scores[top_idx], baseline_ranker_applied,
            abstain_score, visible_pairwise_features);
        visible_pairwise_score := long_visible_pairwise_residual_score(
            visible_pairwise_features);
        if visible_pairwise_score > best_visible_pairwise_score then
        begin
            best_visible_pairwise_score := visible_pairwise_score;
            best_visible_pairwise_position := candidate_idx;
        end;
    end;
    if (best_visible_pairwise_position > 0) and
        (best_visible_pairwise_score >=
        c_long_visible_pairwise_residual_promotion_threshold) then
    begin
        current_idx := ordered_indices[best_visible_pairwise_position];
        for candidate_idx := best_visible_pairwise_position downto 1 do
        begin
            ordered_indices[candidate_idx] :=
                ordered_indices[candidate_idx - 1];
        end;
        ordered_indices[0] := current_idx;
        apply_ranker := True;
    end;

    { A whole-sentence average can hide a one- or two-character error in a
      long candidate. Compare small windows around each differing span only
      after the established visible order is final. All windows are scored in
      one cached LM batch; short/exact input never enters this procedure. }
    best_local_difference_position := -1;
    best_local_difference_score := Low(Int64);
    local_difference_promoted_index := -1;
    SetLength(local_difference_debug_scores, candidate_count);
    SetLength(local_difference_current_rank_debug, candidate_count);
    for local_radius := 0 to 3 do
    begin
        SetLength(local_top_lm_debug[local_radius], candidate_count);
        SetLength(local_candidate_lm_debug[local_radius], candidate_count);
    end;
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        local_difference_debug_scores[candidate_idx] := Low(Int64);
    end;
    top_idx := ordered_indices[0];
    local_top_text := Trim(legacy_candidates[top_idx].text);
    if c_long_local_difference_residual_max_challenger_rank > 0 then
    begin
        local_difference_challenger_count := Min(candidate_count - 1,
            c_long_local_difference_residual_max_challenger_rank - 1);
    end
    else
    begin
        local_difference_challenger_count := candidate_count - 1;
    end;
    SetLength(local_relation_valid, candidate_count);
    SetLength(local_different_units, candidate_count);
    SetLength(local_different_runs, candidate_count);
    SetLength(local_max_different_run, candidate_count);
    SetLength(local_same_prefix_units, candidate_count);
    SetLength(local_same_suffix_units, candidate_count);
    SetLength(local_difference_span_units, candidate_count);
    SetLength(local_difference_start, candidate_count);
    SetLength(local_difference_end, candidate_count);
    SetLength(local_window_score_offset, candidate_count);
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        local_window_score_offset[candidate_idx] := -1;
    end;
    local_window_count := 0;
    SetLength(local_window_texts,
        local_difference_challenger_count * 8);
    if rank_features[top_idx].complete_match then
    begin
        for candidate_idx := 1 to local_difference_challenger_count do
        begin
            current_idx := ordered_indices[candidate_idx];
            local_difference_current_rank_debug[current_idx] :=
                candidate_idx + 1;
            local_candidate_text := Trim(legacy_candidates[current_idx].text);
            if (not rank_features[current_idx].complete_match) or
                (Length(local_candidate_text) <> Length(local_top_text)) then
            begin
                Continue;
            end;

            local_current_run := 0;
            for local_char_idx := 1 to Length(local_top_text) do
            begin
                if local_top_text[local_char_idx] <>
                    local_candidate_text[local_char_idx] then
                begin
                    Inc(local_different_units[current_idx]);
                    Inc(local_current_run);
                    if local_current_run = 1 then
                    begin
                        Inc(local_different_runs[current_idx]);
                    end;
                    local_max_different_run[current_idx] := Max(
                        local_max_different_run[current_idx],
                        local_current_run);
                    if local_difference_start[current_idx] = 0 then
                    begin
                        local_difference_start[current_idx] := local_char_idx;
                    end;
                    local_difference_end[current_idx] := local_char_idx;
                end
                else
                begin
                    local_current_run := 0;
                end;
            end;
            if local_different_units[current_idx] <= 0 then
            begin
                Continue;
            end;

            local_same_prefix_units[current_idx] :=
                local_difference_start[current_idx] - 1;
            local_same_suffix_units[current_idx] := Length(local_top_text) -
                local_difference_end[current_idx];
            local_difference_span_units[current_idx] :=
                local_difference_end[current_idx] -
                local_difference_start[current_idx] + 1;
            local_relation_valid[current_idx] := True;
            local_window_score_offset[current_idx] := local_window_count;
            for local_radius := 0 to 3 do
            begin
                local_window_start := Max(1,
                    local_difference_start[current_idx] - local_radius);
                local_window_end := Min(Length(local_top_text),
                    local_difference_end[current_idx] + local_radius);
                local_window_texts[local_window_count] := Copy(
                    local_top_text, local_window_start,
                    local_window_end - local_window_start + 1);
                Inc(local_window_count);
                local_window_texts[local_window_count] := Copy(
                    local_candidate_text, local_window_start,
                    local_window_end - local_window_start + 1);
                Inc(local_window_count);
            end;
        end;
    end;
    SetLength(local_window_texts, local_window_count);
    local_scores_ok := (local_window_count > 0) and
        get_cached_char_lm_scores(local_window_texts,
        local_window_scores, clsm_suffix, '') and
        (Length(local_window_scores) = local_window_count);
    if local_scores_ok then
    begin
        SetLength(top_local_lm_scores, 4);
        SetLength(candidate_local_lm_scores, 4);
        for candidate_idx := 1 to local_difference_challenger_count do
        begin
            current_idx := ordered_indices[candidate_idx];
            if (not local_relation_valid[current_idx]) or
                (local_window_score_offset[current_idx] < 0) then
            begin
                Continue;
            end;
            for local_radius := 0 to 3 do
            begin
                top_local_lm_scores[local_radius] := local_window_scores[
                    local_window_score_offset[current_idx] +
                    local_radius * 2];
                candidate_local_lm_scores[local_radius] :=
                    local_window_scores[
                    local_window_score_offset[current_idx] +
                    local_radius * 2 + 1];
                local_top_lm_debug[local_radius][current_idx] :=
                    top_local_lm_scores[local_radius];
                local_candidate_lm_debug[local_radius][current_idx] :=
                    candidate_local_lm_scores[local_radius];
            end;
            build_long_local_difference_residual_features(
                rank_features[current_idx], rank_features[top_idx],
                candidate_idx + 1, rank_scores[current_idx],
                rank_scores[top_idx], apply_ranker, abstain_score,
                local_different_units[current_idx],
                local_different_runs[current_idx],
                local_max_different_run[current_idx],
                local_same_prefix_units[current_idx],
                local_same_suffix_units[current_idx],
                local_difference_span_units[current_idx],
                top_local_lm_scores, candidate_local_lm_scores,
                local_difference_features);
            { Do not let a local character preference replace a materially
              cleaner word path. Multi-character replacements also need at
              least non-decreasing word-transition evidence. }
            if (local_difference_features.delta_path_segments >= 3.0) or
                ((local_different_units[current_idx] > 1) and
                (local_difference_features.delta_word_lm_bonus < 0.0)) then
            begin
                Continue;
            end;
            local_difference_score :=
                long_local_difference_residual_score(
                local_difference_features);
            local_difference_debug_scores[current_idx] :=
                local_difference_score;
            if local_difference_score > best_local_difference_score then
            begin
                best_local_difference_score := local_difference_score;
                best_local_difference_position := candidate_idx;
            end;
        end;
    end;
    if (best_local_difference_position > 0) and
        (best_local_difference_score >=
        c_long_local_difference_residual_promotion_threshold) then
    begin
        current_idx := ordered_indices[best_local_difference_position];
        for candidate_idx := best_local_difference_position downto 1 do
        begin
            ordered_indices[candidate_idx] :=
                ordered_indices[candidate_idx - 1];
        end;
        ordered_indices[0] := current_idx;
        local_difference_promoted_index := current_idx;
        apply_ranker := True;
    end;

    { The local one-character repair model is now a single confidence-gated
      fallback after the unified complete-candidate ranking. It no longer
      runs in several visible-candidate passes that can repeatedly change
      Top1. }
    complete_pool_pairwise_position := -1;
    if (m_long_complete_pool_pairwise_text <> '') and
        (m_long_complete_pool_pairwise_insert_rank in [0, 1]) then
    begin
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            current_idx := ordered_indices[candidate_idx];
            if SameText(Trim(legacy_candidates[current_idx].text),
                Trim(m_long_complete_pool_pairwise_text)) then
            begin
                complete_pool_pairwise_position := candidate_idx;
                Break;
            end;
        end;
        complete_pool_pairwise_target_position := Min(
            m_long_complete_pool_pairwise_insert_rank,
            candidate_count - 1);
        if complete_pool_pairwise_position >
            complete_pool_pairwise_target_position then
        begin
            current_idx := ordered_indices[complete_pool_pairwise_position];
            for candidate_idx := complete_pool_pairwise_position downto
                complete_pool_pairwise_target_position + 1 do
            begin
                ordered_indices[candidate_idx] :=
                    ordered_indices[candidate_idx - 1];
            end;
            ordered_indices[complete_pool_pairwise_target_position] :=
                current_idx;
            apply_ranker := True;
        end;
    end;
    end
    else
    begin
        { The complete pool has one owner for final ordering. Legacy residual
          scores remain ranker features, but cannot mutate Top1 afterwards. }
        if not apply_ranker then
        begin
            for candidate_idx := 0 to candidate_count - 1 do
            begin
                ordered_indices[candidate_idx] := candidate_idx;
            end;
        end;
        local_difference_promoted_index := -1;
        SetLength(local_difference_debug_scores, candidate_count);
        SetLength(local_difference_current_rank_debug, candidate_count);
        SetLength(local_different_units, candidate_count);
        SetLength(local_different_runs, candidate_count);
        SetLength(local_max_different_run, candidate_count);
        SetLength(local_same_prefix_units, candidate_count);
        SetLength(local_same_suffix_units, candidate_count);
        SetLength(local_difference_span_units, candidate_count);
        for local_radius := 0 to 3 do
        begin
            SetLength(local_top_lm_debug[local_radius], candidate_count);
            SetLength(local_candidate_lm_debug[local_radius], candidate_count);
        end;
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            local_difference_debug_scores[candidate_idx] := Low(Int64);
        end;
    end;

    capture_ranking_stage(c_ranking_stage_unified_ranker);

    { Rank 4+ exact anchors are a recall channel, not a trained Top1
      channel. The unified pool model predates these candidates, so keep the
      first established/rank 1-3 complete candidate ahead of them. }
    if unified_pool_final and (candidate_count >= 2) then
    begin
        top_idx := ordered_indices[0];
        if rank_features[top_idx].complete_pool_anchor_present and
            (rank_features[top_idx].complete_pool_anchor_exact_rank >= 4) then
        begin
            for candidate_idx := 1 to candidate_count - 1 do
            begin
                current_idx := ordered_indices[candidate_idx];
                if (not rank_features[current_idx].complete_pool_anchor_present) or
                    (rank_features[current_idx].complete_pool_anchor_exact_rank < 4) then
                begin
                    for top_idx := candidate_idx downto 1 do
                    begin
                        ordered_indices[top_idx] := ordered_indices[top_idx - 1];
                    end;
                    ordered_indices[0] := current_idx;
                    apply_ranker := True;
                    Break;
                end;
            end;
        end;
    end;

    capture_ranking_stage(c_ranking_stage_anchor_guard);

    top2_pairwise_score := 0;
    top2_pairwise_candidate_index := -1;
    top2_pairwise_candidate_position := -1;
    top2_pairwise_eligible := False;
    top2_pairwise_swapped := False;
    if unified_pool_final and
        (not m_debug_disable_long_top2_pairwise_swap) and
        (candidate_count >= 2) then
    begin
        top_idx := ordered_indices[0];
        top2_pairwise_top_pool_index := candidate_pool_indices[top_idx];
        if rank_features[top_idx].complete_match and
            (top2_pairwise_top_pool_index >= 0) and
            m_runtime_long_complete_pool_candidates[
            top2_pairwise_top_pool_index].visible_evidence then
        begin
            for candidate_idx := 1 to candidate_count - 1 do
            begin
                current_idx := ordered_indices[candidate_idx];
                top2_pairwise_candidate_pool_index :=
                    candidate_pool_indices[current_idx];
                if rank_features[current_idx].complete_match and
                    (top2_pairwise_candidate_pool_index >= 0) and
                    m_runtime_long_complete_pool_candidates[
                    top2_pairwise_candidate_pool_index].visible_evidence then
                begin
                    top2_pairwise_candidate_position := candidate_idx;
                    top2_pairwise_candidate_index := current_idx;
                    Break;
                end;
            end;
        end;

        if top2_pairwise_candidate_index >= 0 then
        begin
            current_idx := top2_pairwise_candidate_index;
            top2_pairwise_eligible :=
                (not rank_features[top_idx].source_user) and
                (not rank_features[current_idx].source_user) and
                (not rank_features[top_idx].complete_user) and
                (not rank_features[current_idx].complete_user) and
                (not rank_features[top_idx].latest_query_choice) and
                (not rank_features[current_idx].latest_query_choice) and
                (rank_features[top_idx].query_choice_bonus <= 0) and
                (rank_features[current_idx].query_choice_bonus <= 0) and
                (not SameText(Trim(legacy_candidates[top_idx].text),
                Trim(legacy_candidates[current_idx].text)));
            if top2_pairwise_eligible then
            begin
                get_text_relation(current_idx, top_idx,
                    top2_different_units, top2_different_runs,
                    top2_max_different_run, top2_same_prefix_units,
                    top2_same_suffix_units, top2_difference_span_units);
                top2_pairwise_eligible := get_top2_cached_span_lm_scores(
                    top_idx, current_idx,
                    top2_top_local_lm_scores,
                    top2_candidate_local_lm_scores);
            end;
            if top2_pairwise_eligible then
            begin
                top2_query_parts := get_effective_compact_pinyin_syllables(
                    normalized_query);
                SetLength(top2_query_syllables, Length(top2_query_parts));
                for top2_query_idx := 0 to High(top2_query_parts) do
                begin
                    top2_query_syllables[top2_query_idx] :=
                        normalize_pinyin_text(
                        top2_query_parts[top2_query_idx].text);
                end;
                build_long_top2_pairwise_swap_features(
                    rank_features[current_idx], rank_features[top_idx],
                    rank_scores[current_idx], rank_scores[top_idx],
                    apply_ranker, abstain_score, top2_different_units,
                    top2_different_runs, top2_max_different_run,
                    top2_same_prefix_units, top2_same_suffix_units,
                    top2_difference_span_units,
                    SameText(legacy_paths[current_idx],
                    legacy_paths[top_idx]), top2_top_local_lm_scores,
                    top2_candidate_local_lm_scores,
                    top2_pairwise_features);
                top2_pairwise_score := long_top2_pairwise_swap_score(
                    top2_pairwise_features);
                top2_pairwise_score := long_top2_pairwise_combined_score(
                    top2_pairwise_score,
                    legacy_candidates[current_idx].text,
                    legacy_candidates[top_idx].text);
                top2_pairwise_score :=
                    long_top2_pairwise_difference_combined_score(
                    top2_pairwise_score, top2_query_syllables,
                    legacy_candidates[current_idx].text,
                    legacy_candidates[top_idx].text,
                    legacy_paths[current_idx], legacy_paths[top_idx]);
                top2_pairwise_swapped :=
                    apply_long_top2_pairwise_index_swap(ordered_indices,
                    top2_pairwise_candidate_position, True,
                    top2_pairwise_score,
                    c_long_top2_pairwise_difference_combined_threshold);
                if top2_pairwise_swapped then
                begin
                    apply_ranker := True;
                end;
            end;
        end;
    end;

    capture_ranking_stage(c_ranking_stage_top2_pairwise);

    { Recover exact-word anchored paths only after the established complete
      pool and Top2 policies have settled. The model may move one existing
      complete candidate to rank 1 or 2; it never changes candidate recall. }
    apply_exact_anchor_pairwise_ranking;
    capture_ranking_stage(c_ranking_stage_exact_anchor_pairwise);

    { The final pairwise stage only compares the two already selected complete
      long candidates. It cannot change Top2 recall or short-word ordering. }
    apply_bidirectional_difference_ranking;
    capture_ranking_stage(c_ranking_stage_bidirectional_difference);

    { Recover a high-confidence rank-3 complete path into the second slot.
      The model cannot change Top1 and is isolated from short-word ranking. }
    apply_second_slot_bidirectional_ranking;
    capture_ranking_stage(c_ranking_stage_second_slot_bidirectional);

    { This final residual is trained against the captured order after every
      established complete-pool policy. It can promote one already recalled,
      visible complete path and performs no additional dictionary or LM work. }
    apply_complete_pool_final_pairwise_residual;
    capture_ranking_stage(c_ranking_stage_final_pairwise_residual);

    { The static final residual remains the conservative first pass. This
      difference-aware pass is trained on the real post-static order and may
      recover one complete internal-pool path without changing recall. }
    apply_complete_pool_post_static_difference_residual;
    capture_ranking_stage(c_ranking_stage_post_static_difference);

    SetLength(second_slot_selector_base_positions, candidate_count);
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        second_slot_selector_base_positions[ordered_indices[candidate_idx]] :=
            candidate_idx;
    end;

    { Reuse the unified ranker's existing score to recover one deep complete
      path into rank 2. Top1 and every short-word path remain untouched. }
    apply_complete_pool_ranker_second_slot_recovery;
    capture_ranking_stage(c_ranking_stage_ranker_second_slot_recovery);

    { Re-evaluate the settled second slot against a bounded deep-candidate
      pool using local forward and reverse LM evidence. Top1 and short-word
      ranking are unchanged. }
    apply_complete_pool_bidirectional_second_slot_selector;
    capture_ranking_stage(c_ranking_stage_bidirectional_second_slot);

    { The forward gate skips most reverse-LM lookups without changing the
      high-confidence bidirectional Top1/Top2 decision. }
    apply_complete_pool_post_static_bidirectional_residual;
    capture_ranking_stage(c_ranking_stage_post_static_bidirectional);

    { The settled-order residual is trained after every preceding complete
      pool stage. It only swaps the two existing complete long candidates. }
    apply_complete_pool_settled_top2_residual;
    capture_ranking_stage(c_ranking_stage_settled_top2_residual);
    build_ranking_stage_vote_features;
    restore_pre_settled_top2_order;
    apply_long_unified_top2_arbiter;
    capture_ranking_stage(c_ranking_stage_unified_top2_arbiter);
    apply_deep_local_repair_fallback;
    capture_ranking_stage(c_ranking_stage_deep_local_repair);
    { The exact-edge lane may change the settled Top1 only when its
      independent-corpus auditor prefers it to the shadow baseline. }
    SetLength(exact_edge_pre_auditor_ranks, candidate_count);
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        exact_edge_pre_auditor_ranks[ordered_indices[candidate_idx]] :=
            candidate_idx + 1;
    end;
    exact_edge_current_top_index := ordered_indices[0];
    apply_exact_edge_final_auditor;
    { The optional host-only Transformer sees the settled complete Top16 and
      may make one confidence-gated promotion. A nil/not-ready model is an
      exact no-op, so TSF and cold-start paths retain the native ordering. }
    apply_host_neural_reranker;

    if apply_ranker then
    begin
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            final_ranks[ordered_indices[candidate_idx]] := candidate_idx + 1;
        end;
    end;

    if apply_ranker then
    begin
        SetLength(ordered_candidates, candidate_count);
        SetLength(ordered_source_indices, candidate_count);
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            current_idx := ordered_indices[candidate_idx];
            ordered_candidates[candidate_idx] :=
                legacy_candidates[current_idx];
            ordered_source_indices[candidate_idx] :=
                legacy_source_indices[current_idx];
        end;
        candidates := ordered_candidates;
        source_indices := ordered_source_indices;
    end
    else
    begin
        for candidate_idx := 0 to candidate_count - 1 do
        begin
            final_ranks[candidate_idx] := candidate_idx + 1;
        end;
    end;

    flush_ranking_stages;

    { Final-state snapshots are an audit/training surface only. Building the
      large per-candidate records in normal input duplicates both the shadow
      and final ranking work without affecting candidate order. }
    if (not m_config.debug_mode) and
        (not m_debug_capture_long_ranking_stages) and
        (not m_debug_capture_long_beam_states) and
        (not m_debug_capture_long_final_candidates) then
    begin
        Exit;
    end;

    SetLength(m_debug_long_final_candidates, candidate_count);
    for candidate_idx := 0 to candidate_count - 1 do
    begin
        FillChar(m_debug_long_final_candidates[candidate_idx],
            SizeOf(m_debug_long_final_candidates[candidate_idx]), 0);
        m_debug_long_final_candidates[candidate_idx].legacy_rank :=
            candidate_idx + 1;
        m_debug_long_final_candidates[candidate_idx].final_rank :=
            final_ranks[candidate_idx];
        copy_features_to_debug(rank_features[candidate_idx],
            m_debug_long_final_candidates[candidate_idx]);
        if SameText(Trim(legacy_candidates[candidate_idx].text),
            Trim(exact_edge_baseline_top1)) then
        begin
            m_debug_long_final_candidates[candidate_idx].exact_edge_shadow_rank := 1;
        end
        else if SameText(Trim(legacy_candidates[candidate_idx].text),
            Trim(exact_edge_baseline_top2)) then
        begin
            m_debug_long_final_candidates[candidate_idx].exact_edge_shadow_rank := 2;
        end;
        m_debug_long_final_candidates[candidate_idx].exact_edge_auditor_decision :=
            exact_edge_auditor_decision;
        m_debug_long_final_candidates[candidate_idx].exact_edge_fallback_auditor_decision :=
            exact_edge_fallback_auditor_decision;
        m_debug_long_final_candidates[candidate_idx].exact_edge_pre_auditor_rank :=
            exact_edge_pre_auditor_ranks[candidate_idx];
        if exact_edge_auditor_decision >= 0 then
        begin
            m_debug_long_final_candidates[candidate_idx].exact_edge_auditor_score :=
                Round(exact_edge_auditor_score * 1000000.0);
            m_debug_long_final_candidates[candidate_idx].exact_edge_auditor_threshold :=
                Round(exact_edge_auditor_threshold * 1000000.0);
        end;
        if exact_edge_fallback_auditor_decision >= 0 then
        begin
            m_debug_long_final_candidates[candidate_idx].exact_edge_fallback_auditor_score :=
                Round(exact_edge_fallback_auditor_score * 1000000.0);
            m_debug_long_final_candidates[candidate_idx].exact_edge_fallback_auditor_threshold :=
                Round(exact_edge_fallback_auditor_threshold * 1000000.0);
        end;
        m_debug_long_final_candidates[candidate_idx].ranker_score :=
            rank_scores[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].abstain_score :=
            abstain_score;
        m_debug_long_final_candidates[candidate_idx].ranker_applied :=
            apply_ranker;
        if candidate_idx = top2_pairwise_candidate_index then
        begin
            m_debug_long_final_candidates[candidate_idx].top2_pairwise_score :=
                top2_pairwise_score;
            m_debug_long_final_candidates[candidate_idx].top2_pairwise_eligible :=
                top2_pairwise_eligible;
            m_debug_long_final_candidates[candidate_idx].top2_pairwise_swapped :=
                top2_pairwise_swapped;
        end;
        m_debug_long_final_candidates[candidate_idx].exact_anchor_pairwise_score :=
            exact_anchor_pairwise_debug_scores[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].exact_anchor_pairwise_eligible :=
            exact_anchor_pairwise_debug_scores[candidate_idx] <> Low(Int64);
        m_debug_long_final_candidates[candidate_idx].exact_anchor_pairwise_moved :=
            candidate_idx = exact_anchor_pairwise_moved_index;
        if candidate_idx = exact_anchor_pairwise_moved_index then
        begin
            m_debug_long_final_candidates[candidate_idx].exact_anchor_pairwise_target_rank :=
                exact_anchor_pairwise_target_rank;
        end;
        for local_radius := 0 to 8 do
        begin
            m_debug_long_final_candidates[candidate_idx].
                exact_anchor_top_local_word_lm[local_radius] :=
                exact_anchor_top_local_word_lm_debug[
                candidate_idx][local_radius];
            m_debug_long_final_candidates[candidate_idx].
                exact_anchor_candidate_local_word_lm[local_radius] :=
                exact_anchor_candidate_local_word_lm_debug[
                candidate_idx][local_radius];
        end;
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_final_pairwise_score :=
            complete_pool_final_pairwise_debug_scores[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_final_pairwise_eligible :=
            complete_pool_final_pairwise_debug_scores[candidate_idx] <>
            Low(Int64);
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_final_pairwise_promoted :=
            candidate_idx = complete_pool_final_pairwise_promoted_index;
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_final_pairwise_current_rank :=
            complete_pool_final_pairwise_current_rank_debug[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_post_static_difference_score :=
            complete_pool_post_static_difference_debug_scores[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_post_static_difference_eligible :=
            complete_pool_post_static_difference_debug_scores[candidate_idx] <>
            Low(Int64);
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_post_static_difference_promoted :=
            candidate_idx = complete_pool_post_static_difference_promoted_index;
        m_debug_long_final_candidates[candidate_idx].
            complete_pool_post_static_difference_current_rank :=
            complete_pool_post_static_difference_current_rank_debug[
            candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_difference_score :=
            local_difference_debug_scores[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_difference_promoted :=
            candidate_idx = local_difference_promoted_index;
        m_debug_long_final_candidates[candidate_idx].local_difference_current_rank :=
            local_difference_current_rank_debug[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_different_units :=
            local_different_units[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_different_runs :=
            local_different_runs[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_max_different_run :=
            local_max_different_run[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_same_prefix_units :=
            local_same_prefix_units[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_same_suffix_units :=
            local_same_suffix_units[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_difference_span_units :=
            local_difference_span_units[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_top_lm_r0 :=
            local_top_lm_debug[0][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_top_lm_r1 :=
            local_top_lm_debug[1][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_top_lm_r2 :=
            local_top_lm_debug[2][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_top_lm_r3 :=
            local_top_lm_debug[3][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_candidate_lm_r0 :=
            local_candidate_lm_debug[0][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_candidate_lm_r1 :=
            local_candidate_lm_debug[1][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_candidate_lm_r2 :=
            local_candidate_lm_debug[2][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].local_candidate_lm_r3 :=
            local_candidate_lm_debug[3][candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_top1_votes :=
            ranking_top1_votes[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_top2_votes :=
            ranking_top2_votes[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_first_top_stage :=
            ranking_first_top_stage[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_last_top_stage :=
            ranking_last_top_stage[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_top_entry_count :=
            ranking_top_entry_count[candidate_idx];
        m_debug_long_final_candidates[candidate_idx].ranking_pre_settled_top :=
            (ranking_stage_capture_count >
            c_ranking_stage_post_static_bidirectional) and
            (ranking_stage_top_indices[
            c_ranking_stage_post_static_bidirectional] = candidate_idx);
        m_debug_long_final_candidates[candidate_idx].
            unified_top2_arbiter_score := unified_top2_arbiter_score_value;
        m_debug_long_final_candidates[candidate_idx].
            unified_top2_arbiter_family := unified_top2_arbiter_family_value;
        m_debug_long_final_candidates[candidate_idx].
            unified_top2_arbiter_swapped := unified_top2_arbiter_swapped;
        m_debug_long_final_candidates[candidate_idx].
            unified_top2_arbiter_features :=
            Copy(unified_top2_arbiter_feature_values);
        m_debug_long_final_candidates[candidate_idx].target :=
            (m_debug_target_recall_text <> '') and
            SameText(Trim(legacy_candidates[candidate_idx].text),
            Trim(m_debug_target_recall_text));
        m_debug_long_final_candidates[candidate_idx].text :=
            legacy_candidates[candidate_idx].text;
        m_debug_long_final_candidates[candidate_idx].comment :=
            legacy_candidates[candidate_idx].comment;
        m_debug_long_final_candidates[candidate_idx].segment_path :=
            legacy_paths[candidate_idx];
    end;
end;

function TncEngine.visible_candidates_cache_is_current(
    const page_size: Integer): Boolean;
begin
    Result := m_visible_candidates_cache_valid and
        SameText(m_visible_candidates_cache_composition_text,
        m_composition_text) and
        SameText(m_visible_candidates_cache_lookup_key, m_last_lookup_key) and
        (m_visible_candidates_cache_page_index = m_page_index) and
        (m_visible_candidates_cache_page_size = page_size);
end;

procedure TncEngine.cache_visible_candidate_page(const candidates: TncCandidateList;
    const source_indices: TArray<Integer>; const page_size: Integer);
begin
    if Length(candidates) <> Length(source_indices) then
        raise EArgumentException.Create('Visible candidate/source count mismatch');
    m_visible_candidates_cache := Copy(candidates);
    m_visible_candidate_source_indices_cache := Copy(source_indices);
    m_visible_candidates_cache_composition_text := m_composition_text;
    m_visible_candidates_cache_lookup_key := m_last_lookup_key;
    m_visible_candidates_cache_page_index := m_page_index;
    m_visible_candidates_cache_page_size := page_size;
    m_visible_candidates_cache_valid := True;
    if (Length(candidates) = 0) or (m_selected_index < 0) then
        m_selected_index := 0
    else if m_selected_index >= Length(candidates) then
        m_selected_index := High(candidates);
end;

function TncEngine.get_candidate_state_signature: UInt64;
var
    signature: UInt64;
    candidate_idx: Integer;

    procedure mix_value(const value: UInt64);
    begin
        signature := ((signature shl 7) or (signature shr 57)) xor value;
    end;

    procedure mix_text(const value: string);
    var
        char_idx: Integer;
    begin
        mix_value(Length(value));
        for char_idx := 1 to Length(value) do
        begin
            mix_value(Ord(value[char_idx]));
        end;
    end;
begin
    signature := $6A09E667F3BCC909;
    mix_value(Length(m_candidates));
    for candidate_idx := 0 to High(m_candidates) do
    begin
        mix_text(m_candidates[candidate_idx].text);
        mix_text(m_candidates[candidate_idx].comment);
        mix_value(UInt64(Cardinal(m_candidates[candidate_idx].score)));
        mix_value(Ord(m_candidates[candidate_idx].source));
        mix_value(Ord(m_candidates[candidate_idx].display_kind));
        mix_value(Ord(m_candidates[candidate_idx].has_dict_weight));
        mix_value(UInt64(Cardinal(m_candidates[candidate_idx].dict_weight)));
        if candidate_idx < Length(m_candidate_segment_paths) then
        begin
            mix_text(m_candidate_segment_paths[candidate_idx]);
        end
        else
        begin
            mix_value(0);
        end;
    end;
    Result := signature;
end;

function TncEngine.get_long_visible_candidate_pool_cache_key(
    const page_size: Integer): string;
begin
    Result := m_composition_text + #0 + m_composition_display_text + #0 +
        m_last_lookup_key + #0 + m_confirmed_text + #0 +
        m_external_left_context + #0 + m_segment_left_context + #0 +
        IntToStr(page_size);
end;

function TncEngine.long_visible_candidate_pool_cache_is_current(
    const page_size: Integer): Boolean;
begin
    Result := m_long_visible_candidate_pool_cache_valid and
        SameText(m_long_visible_candidate_pool_cache_key,
        get_long_visible_candidate_pool_cache_key(page_size)) and
        (m_long_visible_candidate_pool_source_signature =
        get_candidate_state_signature);
end;

function TncEngine.get_current_page_candidate_count(
    const page_size: Integer): Integer;
begin
    if visible_candidates_cache_is_current(page_size) then
    begin
        Exit(Length(m_visible_candidates_cache));
    end;

    if long_visible_candidate_pool_cache_is_current(page_size) then
    begin
        Result := nc_candidate_page_items(Length(m_long_visible_candidate_pool_cache),
            m_page_index, page_size);
        Exit;
    end;

    Result := nc_candidate_page_items(Length(m_candidates), m_page_index, page_size);
end;

procedure TncEngine.apply_visible_local_repair(var candidates: TncCandidateList;
    var source_indices: TArray<Integer>; const expected_units: Integer);
var
    text, path, key, segment, replacement, original_path: string;
    document_key, preceding_text, aligned_pinyin, repair_query: string;
    refined_text, refined_pinyin, joint_second: string;
    refinement_override: string;
    minimum_word_ratio, refined_word_ratio: Double;
    index, existing, saved_source, position, unit_index: Integer;
    original_segments: TArray<string>;
    candidate, saved: TncCandidate;
    repair_accepted, joint_available: Boolean;
    boundary_enabled, path_validation_enabled: Boolean;
    validated, refined_path, joint_path: TncValidatedRepairPath;
    procedure remember_validated_path(const source_index: Integer);
    begin
        if not m_local_repair_validated.exact_path or
            (m_local_repair_validated.text <> text) or (source_index < 0) or
            (source_index >= Length(m_candidates)) or
            (m_candidates[source_index].text <> text) then Exit;
        m_candidate_segment_paths := Copy(m_candidate_segment_paths);
        if Length(m_candidate_segment_paths) <= source_index then
            SetLength(m_candidate_segment_paths, source_index + 1);
        m_candidate_segment_paths[source_index] := m_local_repair_validated.segment_path;
        remember_segment_path_for_candidate(text, '', m_local_repair_validated.segment_path);
    end;
    procedure sync_paging_pool;
    var
        pool: TncCandidateList;
        indices: TArray<Integer>;
        seen: TDictionary<string, Byte>;
        i, count: Integer;
        item_key: string;
    begin
        if not m_long_visible_candidate_pool_cache_valid then Exit;
        if m_long_visible_candidate_pool_cache_key <>
            get_long_visible_candidate_pool_cache_key(get_candidate_page_size) then Exit;
        pool := Copy(m_long_visible_candidate_pool_cache);
        indices := Copy(m_long_visible_candidate_pool_source_indices_cache);
        if (Length(pool) < Length(candidates)) or
            (Length(pool) <> Length(indices)) then Exit;
        for i := 0 to High(candidates) do
        begin
            pool[i] := candidates[i];
            indices[i] := source_indices[i];
        end;
        seen := TDictionary<string, Byte>.Create;
        try
            count := 0;
            for i := 0 to High(pool) do
            begin
                item_key := nc_visible_candidate_key(pool[i],
                    normalize_pinyin_text(Trim(pool[i].comment)));
                if seen.ContainsKey(item_key) then Continue;
                seen.Add(item_key, 0);
                pool[count] := pool[i];
                indices[count] := indices[i];
                Inc(count);
            end;
            SetLength(pool, count);
            SetLength(indices, count);
        finally
            seen.Free;
        end;
        m_long_visible_candidate_pool_cache := pool;
        m_long_visible_candidate_pool_source_indices_cache := indices;
        m_long_visible_candidate_pool_source_signature := get_candidate_state_signature;
    end;
begin
    if (m_long_local_repair = nil) or (m_dictionary = nil) or (m_page_index <> 0) or
        (Length(candidates) = 0) or (Length(source_indices) <> Length(candidates)) or
        (expected_units < 6) or (expected_units > 40) or
        (m_config.pinyin_input_scheme <> pis_full_pinyin) or
        (m_config.dictionary_variant <> dv_simplified) or
        m_config.fuzzy_pinyin_enabled or (m_confirmed_text <> '') or
        (candidates[0].source = cs_user) or
        (candidates[0].comment <> '') or
        (Length(candidates[0].text) <> expected_units) then Exit;
    // Some generated paths inherit a display weight from their components.
    // Only a real full-query dictionary entry is an immutable exact candidate.
    if candidates[0].has_dict_weight and m_dictionary.is_base_entry(
        normalize_pinyin_text(m_composition_text), candidates[0].text) then Exit;
    document_key := '';
    preceding_text := '';
    if m_document_context_model <> nil then
    begin
        document_key := m_document_context_model.document_key;
        preceding_text := m_document_context_model.semantic_tail;
    end;
    key := m_composition_text + #0 + m_last_lookup_key;
    key := key + #0 + document_key + #0 + preceding_text;
    text := '';
    if (key = m_local_repair_query_key) and (m_local_repair_text <> '') and
        ((candidates[0].text = m_local_repair_draft) or
        (candidates[0].text = m_local_repair_text)) then
        text := m_local_repair_text
    else
    begin
        m_local_repair_validated := Default(TncValidatedRepairPath);
        if m_debug_capture_local_repair then
        begin
            m_debug_local_repair_guard := Default(TncLocalRepairGuardDebug);
            m_debug_local_repair_guard.query_text := m_composition_text;
            m_debug_local_repair_guard.draft := candidates[0].text;
            m_debug_local_repair_guard.segment_path :=
                get_segment_path_for_candidate(candidates[0], source_indices[0]);
            m_debug_local_repair_guard.invoked := True;
        end;
        // Model vocabularies use canonical syllables, with explicit boundaries intact.
        repair_query := nc_normalize_umlaut_spelling(m_composition_text);
        joint_available := (preceding_text = '') and
            (m_long_joint_repair <> nil) and m_long_joint_repair.joint_ready;
        try
            repair_accepted := m_long_local_repair.try_repair(repair_query,
                candidates[0].text, document_key, preceding_text, text,
                aligned_pinyin, minimum_word_ratio);
            if m_debug_capture_local_repair then
            begin
                m_debug_local_repair_guard.proposal := text;
                m_debug_local_repair_guard.aligned_pinyin := aligned_pinyin;
                m_debug_local_repair_guard.accepted := repair_accepted;
            end;
            if not repair_accepted then
            begin
                if not joint_available or (aligned_pinyin = '') then Exit;
                text := candidates[0].text;
            end;
        except
            Exit;
        end;
        if (text = '') or (Length(text) <> expected_units) then Exit;
        original_path := get_segment_path_for_candidate(candidates[0], source_indices[0]);
        boundary_enabled := GetEnvironmentVariable('CASSOTIS_LOCAL_REPAIR_BOUNDARIES') <> '0';
        path_validation_enabled := boundary_enabled or
            (GetEnvironmentVariable('CASSOTIS_TAB_REPAIRED_PREFIX') = '1');
        validated := Default(TncValidatedRepairPath);
        if path_validation_enabled and repair_accepted then
        begin
            validated := validate_local_repair_path(m_dictionary, candidates[0].text,
                text, original_path, aligned_pinyin, minimum_word_ratio, boundary_enabled);
            text := validated.text;
        end
        else if repair_accepted then
            text := guard_local_repair_words(m_dictionary, candidates[0].text,
                text, original_path, aligned_pinyin, minimum_word_ratio);
        if m_debug_capture_local_repair then
            m_debug_local_repair_guard.guarded := text;
        // Reuse the guarded first pass as context, without changing its accepted
        // positions or increasing the total edit budget. Contextual refinement
        // has not passed a separate gate and remains on the original single pass.
        refinement_override := GetEnvironmentVariable('CASSOTIS_LOCAL_REPAIR_REFINE');
        if ((refinement_override = '1') or
            ((refinement_override <> '0') and (m_long_local_repair_policy <> nil) and
            m_long_local_repair_policy.allows_no_context_refinement)) and
            (preceding_text = '') and (text <> candidates[0].text) then
        begin
            try
                if m_long_local_repair.try_repair(repair_query, text,
                    document_key, preceding_text, refined_text, refined_pinyin,
                    refined_word_ratio) and (refined_pinyin = aligned_pinyin) and
                    (refined_word_ratio = minimum_word_ratio) and
                    valid_local_repair_refinement(candidates[0].text, text, refined_text) then
                begin
                    if path_validation_enabled then
                    begin
                        refined_path := validate_local_repair_path(m_dictionary,
                            candidates[0].text, refined_text, original_path,
                            aligned_pinyin, minimum_word_ratio, boundary_enabled);
                        refined_text := refined_path.text;
                    end
                    else refined_text := guard_local_repair_words(m_dictionary,
                        candidates[0].text, refined_text, original_path,
                        aligned_pinyin, minimum_word_ratio);
                    if valid_local_repair_refinement(candidates[0].text, text, refined_text) then
                    begin
                        text := refined_text;
                        if path_validation_enabled then validated := refined_path;
                    end;
                end;
            except
                // A failed optional pass must retain the valid first result.
            end;
        end;
        m_local_repair_baseline_text := text;
        if joint_available and path_validation_enabled then
        begin
            joint_second := '';
            if Length(candidates) > 1 then
            begin
                joint_second := candidates[1].text;
                if candidates[1].comment <> '' then
                    joint_second := joint_second + '/' + candidates[1].comment
                else if text = joint_second then joint_second := candidates[0].text;
            end;
            // The KEEP here is the settled, guarded/refined baseline result.
            // Only this last stage may replace it; the complete path travels with it.
            try
                if m_long_joint_repair.try_finalize(m_dictionary, repair_query,
                    candidates[0].text, original_path, text, joint_second,
                    aligned_pinyin, document_key, preceding_text, joint_path) and
                    joint_path.exact_path and (Length(joint_path.text) = expected_units) and
                    (joint_path.aligned_pinyin = aligned_pinyin) and
                    (StringReplace(joint_path.segment_path, #3, '', [rfReplaceAll]) =
                    joint_path.text) then
                begin
                    text := joint_path.text;
                    validated := joint_path;
                end;
            except
                // Optional adjudication failure never discards the baseline.
            end;
        end;
        m_local_repair_query_key := key;
        m_local_repair_text := text;
        m_local_repair_draft := candidates[0].text;
        m_local_repair_original_path := original_path;
        m_local_repair_validated := validated;
    end;
    if text = candidates[0].text then Exit;
    // Reproduce the baseline's swap before applying a different final winner.
    // Otherwise replacing a repaired second item would silently change Top2.
    if (Length(candidates) > 1) and
        (m_local_repair_baseline_text = candidates[1].text) and
        (candidates[1].comment = '') and
        (text <> m_local_repair_baseline_text) then
    begin
        saved := candidates[0]; saved_source := source_indices[0];
        candidates[0] := candidates[1]; source_indices[0] := source_indices[1];
        candidates[1] := saved; source_indices[1] := saved_source;
    end;
    // If repair selects an existing visible item, swap the records and their
    // source indices together. Otherwise replace only the first visible slot.
    for index := 1 to High(candidates) do
        if (candidates[index].text = text) and (candidates[index].comment = '') then
        begin
            saved := candidates[0];
            saved_source := source_indices[0];
            candidates[0] := candidates[index];
            source_indices[0] := source_indices[index];
            candidates[index] := saved;
            source_indices[index] := saved_source;
            remember_validated_path(source_indices[0]);
            sync_paging_pool;
            Exit;
        end;
    existing := -1;
    for index := 0 to High(m_candidates) do
        if (m_candidates[index].text = text) and (m_candidates[index].comment = '') then
        begin
            existing := index;
            Break;
        end;
    if existing < 0 then
    begin
        candidate := Default(TncCandidate);
        candidate.text := text;
        candidate.score := candidates[0].score;
        candidate.source := cs_rule;
        candidate.display_kind := cdk_default;
        existing := Length(m_candidates);
        SetLength(m_candidates, existing + 1);
        m_candidates[existing] := candidate;
        path := '';
        if m_local_repair_validated.exact_path and
            (m_local_repair_validated.text = text) then
            path := m_local_repair_validated.segment_path;
        original_path := get_segment_path_for_candidate(candidates[0], source_indices[0]);
        original_segments := original_path.Split([#3], TStringSplitOptions.ExcludeEmpty);
        if (path = '') and
            (StringReplace(original_path, #3, '', [rfReplaceAll]) = candidates[0].text) then
        begin
            position := 1;
            for segment in original_segments do
            begin
                replacement := Copy(text, position, Length(segment));
                if path <> '' then path := path + #3;
                if replacement = segment then path := path + segment
                else
                    for unit_index := 1 to Length(replacement) do
                    begin
                        if unit_index > 1 then path := path + #3;
                        path := path + replacement[unit_index];
                    end;
                Inc(position, Length(segment));
            end;
        end;
        if path = '' then
            for index := 1 to Length(text) do
            begin
                if path <> '' then path := path + #3;
                path := path + text[index];
            end;
        remember_segment_path_for_candidate(text, '', path);
    end;
    candidates[0] := m_candidates[existing];
    source_indices[0] := existing;
    remember_validated_path(existing);
    sync_paging_pool;
end;

procedure TncEngine.normalize_page_and_selection;
var
    page_size: Integer;
    page_count: Integer;
    page_items: Integer;
begin
    page_size := get_candidate_page_size;
    page_count := get_page_count_internal(page_size);
    if page_count <= 0 then
    begin
        m_page_index := 0;
        m_selected_index := 0;
        Exit;
    end;

    if m_page_index < 0 then
    begin
        m_page_index := 0;
    end
    else if m_page_index >= page_count then
    begin
        m_page_index := page_count - 1;
    end;

    page_items := get_current_page_candidate_count(page_size);

    if page_items <= 0 then
    begin
        m_selected_index := 0;
        Exit;
    end;

    if m_selected_index < 0 then
    begin
        m_selected_index := 0;
    end
    else if m_selected_index >= page_items then
    begin
        m_selected_index := page_items - 1;
    end;
end;

function TncEngine.get_source_rank(const source: TncCandidateSource): Integer;
begin
    case source of
        cs_user:
            Result := 0;
        cs_rule:
            Result := 1;
    else
        Result := 1;
    end;
end;

function TncEngine.get_context_variants(const context_text: string): TArray<string>;
var
    context_units: TArray<string>;
    seen: TDictionary<string, Boolean>;
    variant_text: string;
    idx: Integer;
    start_idx: Integer;
    min_start_idx: Integer;
begin
    SetLength(Result, 0);
    variant_text := Trim(context_text);
    if variant_text = '' then
    begin
        Exit;
    end;

    seen := TDictionary<string, Boolean>.Create;
    try
        SetLength(Result, 1);
        Result[0] := variant_text;
        seen.Add(variant_text, True);

        context_units := split_text_units(variant_text);
        if Length(context_units) <= 1 then
        begin
            Exit;
        end;

        min_start_idx := Length(context_units) - 4;
        if min_start_idx < 0 then
        begin
            min_start_idx := 0;
        end;

        for start_idx := min_start_idx to Length(context_units) - 1 do
        begin
            if start_idx < 0 then
            begin
                Continue;
            end;
            variant_text := '';
            for idx := start_idx to High(context_units) do
            begin
                variant_text := variant_text + context_units[idx];
            end;
            variant_text := Trim(variant_text);
            if (variant_text = '') or seen.ContainsKey(variant_text) then
            begin
                Continue;
            end;
            seen.Add(variant_text, True);
            SetLength(Result, Length(Result) + 1);
            Result[High(Result)] := variant_text;
        end;
    finally
        seen.Free;
    end;
end;

function TncEngine.get_session_text_bonus(const candidate_text: string): Integer;
const
    c_multi_text_step = 110;
    c_multi_text_base = 70;
    c_multi_text_cap = 640;
    c_single_text_step = 48;
    c_single_text_base = 24;
    c_single_text_cap = 220;
    c_recent_phrase_bonus_top = 240;
    c_recent_phrase_bonus_mid = 150;
    c_recent_phrase_bonus_tail = 70;
    c_recent_single_bonus_top = 90;
    c_recent_single_bonus_mid = 56;
    c_recent_single_bonus_tail = 28;
var
    unit_count: Integer;
    count: Integer;
    last_seen_serial: Int64;
    serial_gap: Int64;
    recent_bonus: Integer;
    text_key: string;
begin
    text_key := Trim(candidate_text);
    if (text_key <> '') and (m_lookup_session_bonus_cache <> nil) and
        m_lookup_session_bonus_cache.TryGetValue(text_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if m_session_text_counts = nil then
    begin
        Exit;
    end;

    if (text_key = '') or (not m_session_text_counts.TryGetValue(text_key, count)) or
        (count <= 0) then
    begin
        Exit;
    end;

    unit_count := get_candidate_text_unit_count(text_key);
    last_seen_serial := 0;
    serial_gap := High(Int64);
    if (m_session_text_last_seen <> nil) and
        m_session_text_last_seen.TryGetValue(text_key, last_seen_serial) and
        (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
    begin
        serial_gap := m_session_commit_serial - last_seen_serial;
    end;

    if unit_count <= 1 then
    begin
        Result := c_single_text_base + ((count - 1) * c_single_text_step);
        recent_bonus := 0;
        if serial_gap <= 1 then
        begin
            recent_bonus := c_recent_single_bonus_top;
        end
        else if serial_gap <= 3 then
        begin
            recent_bonus := c_recent_single_bonus_mid;
        end
        else if serial_gap <= 6 then
        begin
            recent_bonus := c_recent_single_bonus_tail;
        end;
        Inc(Result, recent_bonus);
        if (count >= 3) and (serial_gap <= 2) then
        begin
            Inc(Result, 24);
        end;
        if Result > c_single_text_cap then
        begin
            Result := c_single_text_cap;
        end;
        Exit;
    end;

    Result := c_multi_text_base + ((count - 1) * c_multi_text_step);
    if count >= 3 then
    begin
        Inc(Result, 40);
    end;
    recent_bonus := 0;
    if serial_gap <= 1 then
    begin
        recent_bonus := c_recent_phrase_bonus_top;
    end
    else if serial_gap <= 3 then
    begin
        recent_bonus := c_recent_phrase_bonus_mid;
    end
    else if serial_gap <= 6 then
    begin
        recent_bonus := c_recent_phrase_bonus_tail;
    end;
    Inc(Result, recent_bonus);
    if (count >= 2) and (serial_gap <= 2) then
    begin
        Inc(Result, 96);
    end;
    if (count >= 3) and (serial_gap <= 4) then
    begin
        Inc(Result, 72);
    end;
    if Result > c_multi_text_cap then
    begin
        Result := c_multi_text_cap;
    end;

    if (text_key <> '') and (m_lookup_session_bonus_cache <> nil) then
    begin
        m_lookup_session_bonus_cache.AddOrSetValue(text_key, Result);
    end;
end;

function TncEngine.build_session_query_choice_key(const query_key: string; const candidate_text: string): string;
var
    text_key: string;
begin
    text_key := Trim(candidate_text);
    if (query_key = '') or (text_key = '') then
    begin
        Result := '';
        Exit;
    end;

    Result := query_key + #1 + text_key;
end;

function TncEngine.build_session_query_path_choice_key(const query_key: string; const encoded_path: string): string;
var
    path_key: string;
begin
    path_key := Trim(encoded_path);
    if (query_key = '') or (path_key = '') or (get_encoded_path_segment_count_local(path_key) <= 1) then
    begin
        Result := '';
        Exit;
    end;

    Result := query_key + c_session_query_path_separator + path_key;
end;

function TncEngine.build_context_query_scope_key(const context_text: string; const query_key: string): string;
var
    context_key: string;
begin
    context_key := Trim(context_text);
    if (context_key = '') or (query_key = '') then
    begin
        Result := '';
        Exit;
    end;

    Result := context_key + #2 + query_key;
end;

function TncEngine.build_context_query_choice_key(const context_text: string; const query_key: string;
    const candidate_text: string): string;
var
    context_key: string;
    text_key: string;
begin
    context_key := Trim(context_text);
    text_key := Trim(candidate_text);
    if (context_key = '') or (query_key = '') or (text_key = '') then
    begin
        Result := '';
        Exit;
    end;

    Result := context_key + #2 + query_key + #1 + text_key;
end;

function TncEngine.get_session_query_bonus(const candidate_text: string): Integer;
const
    c_single_query_base = 44;
    c_single_query_step = 34;
    c_single_query_cap = 176;
    c_multi_query_base = 96;
    c_multi_query_step = 56;
    c_multi_query_cap = 980;
    c_query_recent_latest = 900;
    c_query_recent_top = 180;
    c_query_recent_mid = 96;
    c_query_recent_tail = 52;
var
    key: string;
    count: Integer;
    last_seen_serial: Int64;
    serial_gap: Int64;
    text_units: Integer;
    recent_bonus: Integer;

    function is_current_latest_query_choice_local: Boolean;
    var
        cached_latest_text_local: string;
        latest_text_local: string;
    begin
        Result := False;
        if (m_last_lookup_key = '') or (Trim(candidate_text) = '') then
        begin
            Exit;
        end;

        if (m_session_query_latest_text <> nil) and
            m_session_query_latest_text.TryGetValue(m_last_lookup_key,
            latest_text_local) then
        begin
            Exit(SameText(Trim(candidate_text), latest_text_local));
        end;

        if m_dictionary = nil then
        begin
            Exit;
        end;

        latest_text_local := '';
        if (m_lookup_query_latest_text_cache <> nil) and
            m_lookup_query_latest_text_cache.TryGetValue(m_last_lookup_key,
            cached_latest_text_local) then
        begin
            latest_text_local := cached_latest_text_local;
        end
        else
        begin
            latest_text_local := get_cached_query_latest_choice_text(
                m_last_lookup_key);
            if m_lookup_query_latest_text_cache <> nil then
            begin
                m_lookup_query_latest_text_cache.AddOrSetValue(
                    m_last_lookup_key, latest_text_local);
            end;
        end;

        Result := SameText(Trim(candidate_text), latest_text_local);
    end;

    function weak_choice_blocked_by_strong_exact_local: Boolean;
    const
        c_strong_exact_protection_min = 260;
        c_strong_exact_protection_gap = 700;
        c_weak_choice_exact_max = 80;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
        exact_weight_local: Integer;
        candidate_weight_local: Integer;
        competing_weight_local: Integer;
        exact_cache_key_local: string;
        exact_lookup_ok_local: Boolean;
    begin
        Result := False;
        candidate_weight_local := -1;
        competing_weight_local := -1;
        if (m_dictionary = nil) or (m_last_lookup_key = '') or
            (not is_full_pinyin_key(m_last_lookup_key)) or
            (Trim(candidate_text) = '') then
        begin
            Exit;
        end;

        exact_cache_key_local := '#exact#' + m_last_lookup_key;
        exact_lookup_ok_local := (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(exact_cache_key_local,
            exact_results_local);
        if not exact_lookup_ok_local then
        begin
            if not m_dictionary.lookup_exact_full_pinyin(m_last_lookup_key,
                exact_results_local) then
            begin
                SetLength(exact_results_local, 0);
            end;
            if m_build_lookup_cache <> nil then
            begin
                m_build_lookup_cache.AddOrSetValue(exact_cache_key_local,
                    Copy(exact_results_local, 0, Length(exact_results_local)));
            end;
        end;
        if Length(exact_results_local) = 0 then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if Trim(exact_results_local[exact_idx_local].comment) <> '' then
            begin
                Continue;
            end;

            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (exact_text_local = '') or
                (get_candidate_text_unit_count(exact_text_local) <>
                get_candidate_text_unit_count(Trim(candidate_text))) then
            begin
                Continue;
            end;

            exact_weight_local := exact_results_local[exact_idx_local].score;
            if exact_results_local[exact_idx_local].has_dict_weight and
                (exact_results_local[exact_idx_local].dict_weight >
                exact_weight_local) then
            begin
                exact_weight_local :=
                    exact_results_local[exact_idx_local].dict_weight;
            end;

            if SameText(exact_text_local, Trim(candidate_text)) then
            begin
                candidate_weight_local := Max(candidate_weight_local,
                    exact_weight_local);
            end
            else
            begin
                competing_weight_local := Max(competing_weight_local,
                    exact_weight_local);
            end;
        end;

        Result := (candidate_weight_local >= 0) and
            (candidate_weight_local <= c_weak_choice_exact_max) and
            (competing_weight_local >= c_strong_exact_protection_min) and
            (competing_weight_local >= candidate_weight_local +
            c_strong_exact_protection_gap);
    end;
begin
    key := build_session_query_choice_key(m_last_lookup_key, candidate_text);
    if (key <> '') and (m_lookup_query_bonus_cache <> nil) and
        m_lookup_query_bonus_cache.TryGetValue(key, Result) then
    begin
        Exit;
    end;
    if (m_dictionary <> nil) and (m_last_lookup_key <> '') and
        is_low_evidence_admin_place_alias_cached(
        m_last_lookup_key, candidate_text) then
    begin
        Result := 0;
        if (key <> '') and (m_lookup_query_bonus_cache <> nil) then
        begin
            m_lookup_query_bonus_cache.AddOrSetValue(key, Result);
        end;
        Exit;
    end;
    if (m_last_lookup_syllable_count >= 2) and
        (not is_current_latest_query_choice_local) and
        weak_choice_blocked_by_strong_exact_local then
    begin
        Result := 0;
        if (key <> '') and (m_lookup_query_bonus_cache <> nil) then
        begin
            m_lookup_query_bonus_cache.AddOrSetValue(key, Result);
        end;
        Exit;
    end;
    Result := 0;
    if (key = '') or (m_session_query_choice_counts = nil) then
    begin
        if (m_dictionary <> nil) and (m_last_lookup_key <> '') then
        begin
            Result := m_dictionary.get_query_choice_bonus(m_last_lookup_key,
                candidate_text);
            if (key <> '') and (m_lookup_query_bonus_cache <> nil) then
            begin
                m_lookup_query_bonus_cache.AddOrSetValue(key, Result);
            end;
        end;
        Exit;
    end;
    if (not m_session_query_choice_counts.TryGetValue(key, count)) or (count <= 0) then
    begin
        if m_dictionary <> nil then
        begin
            Result := m_dictionary.get_query_choice_bonus(m_last_lookup_key,
                candidate_text);
            if (key <> '') and (m_lookup_query_bonus_cache <> nil) then
            begin
                m_lookup_query_bonus_cache.AddOrSetValue(key, Result);
            end;
        end;
        Exit;
    end;

    last_seen_serial := 0;
    serial_gap := High(Int64);
    if (m_session_query_choice_last_seen <> nil) and
        m_session_query_choice_last_seen.TryGetValue(key, last_seen_serial) and
        (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
    begin
        serial_gap := m_session_commit_serial - last_seen_serial;
    end;

    recent_bonus := 0;
    if serial_gap = 0 then
    begin
        recent_bonus := c_query_recent_latest;
    end
    else if serial_gap <= 1 then
    begin
        recent_bonus := c_query_recent_top;
    end
    else if serial_gap <= 3 then
    begin
        recent_bonus := c_query_recent_mid;
    end
    else if serial_gap <= 6 then
    begin
        recent_bonus := c_query_recent_tail;
    end;

    text_units := get_candidate_text_unit_count(Trim(candidate_text));
    if text_units <= 1 then
    begin
        Result := c_single_query_base + ((count - 1) * c_single_query_step) + (recent_bonus div 3);
        if count >= 3 then
        begin
            Inc(Result, 18);
        end;
        if Result > c_single_query_cap then
        begin
            Result := c_single_query_cap;
        end;
    end
    else
    begin
        Result := c_multi_query_base + ((count - 1) * c_multi_query_step) + recent_bonus;
        if (count >= 2) and (serial_gap <= 2) then
        begin
            Inc(Result, 56);
        end;
        if (count >= 3) and (serial_gap <= 4) then
        begin
            Inc(Result, 32);
        end;
        if Result > c_multi_query_cap then
        begin
            Result := c_multi_query_cap;
        end;
    end;

    if (key <> '') and (m_lookup_query_bonus_cache <> nil) then
    begin
        m_lookup_query_bonus_cache.AddOrSetValue(key, Result);
    end;
end;

function TncEngine.get_session_query_path_bonus(const query_key: string; const encoded_path: string): Integer;
const
    c_query_path_base = 88;
    c_query_path_step = 72;
    c_query_path_cap = 760;
    c_query_path_recent_latest = 168;
    c_query_path_recent_top = 112;
    c_query_path_recent_mid = 68;
    c_query_path_recent_tail = 36;
var
    cache_key: string;
    count: Integer;
    key: string;
    last_seen_serial: Int64;
    normalized_query: string;
    normalized_path: string;
    recent_bonus: Integer;
    serial_gap: Int64;
begin
    normalized_query := normalize_pinyin_text(query_key);
    normalized_path := Trim(encoded_path);
    key := build_session_query_path_choice_key(normalized_query, normalized_path);
    cache_key := 'E' + #1 + key;
    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (key = '') or (m_session_query_path_choice_counts = nil) then
    begin
        Exit;
    end;
    if (not m_session_query_path_choice_counts.TryGetValue(key, count)) or (count <= 0) then
    begin
        Exit;
    end;

    last_seen_serial := 0;
    serial_gap := High(Int64);
    if (m_session_query_path_choice_last_seen <> nil) and
        m_session_query_path_choice_last_seen.TryGetValue(key, last_seen_serial) and
        (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
    begin
        serial_gap := m_session_commit_serial - last_seen_serial;
    end;

    recent_bonus := 0;
    if serial_gap = 0 then
    begin
        recent_bonus := c_query_path_recent_latest;
    end
    else if serial_gap <= 1 then
    begin
        recent_bonus := c_query_path_recent_top;
    end
    else if serial_gap <= 3 then
    begin
        recent_bonus := c_query_path_recent_mid;
    end
    else if serial_gap <= 6 then
    begin
        recent_bonus := c_query_path_recent_tail;
    end;

    Result := c_query_path_base + (count * c_query_path_step) + recent_bonus;
    if count >= 2 then
    begin
        Inc(Result, 52);
    end;
    if count >= 4 then
    begin
        Inc(Result, 36);
    end;

    if Result > c_query_path_cap then
    begin
        Result := c_query_path_cap;
    end;

    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_session_query_path_penalty(const query_key: string; const encoded_path: string): Integer;
const
    c_query_path_penalty_base = 68;
    c_query_path_penalty_step = 52;
    c_query_path_penalty_cap = 520;
    c_query_path_penalty_recent_latest = 132;
    c_query_path_penalty_recent_top = 88;
    c_query_path_penalty_recent_mid = 52;
    c_query_path_penalty_recent_tail = 24;
var
    cache_key: string;
    count: Integer;
    key: string;
    last_seen_serial: Int64;
    normalized_query: string;
    normalized_path: string;
    recent_bonus: Integer;
    serial_gap: Int64;
begin
    normalized_query := normalize_pinyin_text(query_key);
    normalized_path := Trim(encoded_path);
    key := build_session_query_path_choice_key(normalized_query, normalized_path);
    cache_key := 'EN' + #1 + key;
    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (key = '') or (m_session_query_path_penalty_counts = nil) then
    begin
        Exit;
    end;
    if (not m_session_query_path_penalty_counts.TryGetValue(key, count)) or (count <= 0) then
    begin
        Exit;
    end;

    last_seen_serial := 0;
    serial_gap := High(Int64);
    if (m_session_query_path_penalty_last_seen <> nil) and
        m_session_query_path_penalty_last_seen.TryGetValue(key, last_seen_serial) and
        (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
    begin
        serial_gap := m_session_commit_serial - last_seen_serial;
    end;

    recent_bonus := 0;
    if serial_gap = 0 then
    begin
        recent_bonus := c_query_path_penalty_recent_latest;
    end
    else if serial_gap <= 1 then
    begin
        recent_bonus := c_query_path_penalty_recent_top;
    end
    else if serial_gap <= 3 then
    begin
        recent_bonus := c_query_path_penalty_recent_mid;
    end
    else if serial_gap <= 6 then
    begin
        recent_bonus := c_query_path_penalty_recent_tail;
    end;

    Result := c_query_path_penalty_base + (count * c_query_path_penalty_step) + recent_bonus;
    if count >= 2 then
    begin
        Inc(Result, 44);
    end;
    if count >= 4 then
    begin
        Inc(Result, 28);
    end;

    if Result > c_query_path_penalty_cap then
    begin
        Result := c_query_path_penalty_cap;
    end;

    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_session_query_path_prefix_bonus(const query_key: string; const encoded_path: string): Integer;
const
    c_query_path_prefix_base = 44;
    c_query_path_prefix_step = 36;
    c_query_path_prefix_cap = 260;
    c_query_path_prefix_recent_top = 54;
    c_query_path_prefix_recent_mid = 28;
    c_query_path_prefix_recent_tail = 12;
var
    cache_key: string;
    count: Integer;
    current_key: string;
    current_serial: Int64;
    key_pair: TPair<string, Integer>;
    last_seen_serial: Int64;
    local_bonus: Integer;
    normalized_query: string;
    normalized_path: string;
    prefix_with_separator: string;
    recent_bonus: Integer;
    serial_gap: Int64;
    session_query_prefix: string;
    stored_path: string;
begin
    normalized_query := normalize_pinyin_text(query_key);
    normalized_path := Trim(encoded_path);
    if (normalized_query = '') or (normalized_path = '') or
        (get_encoded_path_segment_count_local(normalized_path) <= 1) then
    begin
        Exit(0);
    end;

    cache_key := 'P' + #1 + build_session_query_path_choice_key(normalized_query, normalized_path);
    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (m_session_query_path_choice_counts = nil) or (m_session_query_path_choice_counts.Count <= 0) then
    begin
        Exit;
    end;

    prefix_with_separator := normalized_path + c_segment_path_separator;
    session_query_prefix := normalized_query + c_session_query_path_separator;
    current_serial := m_session_commit_serial;
    for key_pair in m_session_query_path_choice_counts do
    begin
        current_key := key_pair.Key;
        if (Length(current_key) <= Length(session_query_prefix)) or
            (Copy(current_key, 1, Length(session_query_prefix)) <> session_query_prefix) then
        begin
            Continue;
        end;

        stored_path := Copy(current_key, Length(session_query_prefix) + 1, MaxInt);
        if (stored_path <> normalized_path) and
            (Copy(stored_path, 1, Length(prefix_with_separator)) <> prefix_with_separator) then
        begin
            Continue;
        end;

        count := key_pair.Value;
        if count <= 0 then
        begin
            Continue;
        end;

        last_seen_serial := 0;
        serial_gap := High(Int64);
        if (m_session_query_path_choice_last_seen <> nil) and
            m_session_query_path_choice_last_seen.TryGetValue(current_key, last_seen_serial) and
            (last_seen_serial > 0) and (current_serial >= last_seen_serial) then
        begin
            serial_gap := current_serial - last_seen_serial;
        end;

        recent_bonus := 0;
        if serial_gap <= 1 then
        begin
            recent_bonus := c_query_path_prefix_recent_top;
        end
        else if serial_gap <= 3 then
        begin
            recent_bonus := c_query_path_prefix_recent_mid;
        end
        else if serial_gap <= 6 then
        begin
            recent_bonus := c_query_path_prefix_recent_tail;
        end;

        local_bonus := c_query_path_prefix_base + (count * c_query_path_prefix_step) + recent_bonus +
            ((get_encoded_path_segment_count_local(stored_path) -
            get_encoded_path_segment_count_local(normalized_path)) * 12);
        if local_bonus > c_query_path_prefix_cap then
        begin
            local_bonus := c_query_path_prefix_cap;
        end;
        if local_bonus > Result then
        begin
            Result := local_bonus;
        end;
    end;

    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_session_query_path_prefix_penalty(const query_key: string;
    const encoded_path: string): Integer;
const
    c_query_path_prefix_penalty_base = 30;
    c_query_path_prefix_penalty_step = 24;
    c_query_path_prefix_penalty_cap = 220;
    c_query_path_prefix_penalty_recent_top = 40;
    c_query_path_prefix_penalty_recent_mid = 20;
    c_query_path_prefix_penalty_recent_tail = 8;
var
    cache_key: string;
    count: Integer;
    current_key: string;
    current_serial: Int64;
    key_pair: TPair<string, Integer>;
    last_seen_serial: Int64;
    local_penalty: Integer;
    normalized_query: string;
    normalized_path: string;
    prefix_with_separator: string;
    recent_bonus: Integer;
    serial_gap: Int64;
    session_query_prefix: string;
    stored_path: string;
begin
    normalized_query := normalize_pinyin_text(query_key);
    normalized_path := Trim(encoded_path);
    if (normalized_query = '') or (normalized_path = '') or
        (get_encoded_path_segment_count_local(normalized_path) <= 1) then
    begin
        Exit(0);
    end;

    cache_key := 'PN' + #1 + build_session_query_path_choice_key(normalized_query, normalized_path);
    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (m_session_query_path_penalty_counts = nil) or
        (m_session_query_path_penalty_counts.Count <= 0) then
    begin
        Exit;
    end;

    prefix_with_separator := normalized_path + c_segment_path_separator;
    session_query_prefix := normalized_query + c_session_query_path_separator;
    current_serial := m_session_commit_serial;
    for key_pair in m_session_query_path_penalty_counts do
    begin
        current_key := key_pair.Key;
        if (Length(current_key) <= Length(session_query_prefix)) or
            (Copy(current_key, 1, Length(session_query_prefix)) <> session_query_prefix) then
        begin
            Continue;
        end;

        stored_path := Copy(current_key, Length(session_query_prefix) + 1, MaxInt);
        if (stored_path <> normalized_path) and
            (Copy(stored_path, 1, Length(prefix_with_separator)) <> prefix_with_separator) then
        begin
            Continue;
        end;

        count := key_pair.Value;
        if count <= 0 then
        begin
            Continue;
        end;

        last_seen_serial := 0;
        serial_gap := High(Int64);
        if (m_session_query_path_penalty_last_seen <> nil) and
            m_session_query_path_penalty_last_seen.TryGetValue(current_key, last_seen_serial) and
            (last_seen_serial > 0) and (current_serial >= last_seen_serial) then
        begin
            serial_gap := current_serial - last_seen_serial;
        end;

        recent_bonus := 0;
        if serial_gap <= 1 then
        begin
            recent_bonus := c_query_path_prefix_penalty_recent_top;
        end
        else if serial_gap <= 3 then
        begin
            recent_bonus := c_query_path_prefix_penalty_recent_mid;
        end
        else if serial_gap <= 6 then
        begin
            recent_bonus := c_query_path_prefix_penalty_recent_tail;
        end;

        local_penalty := c_query_path_prefix_penalty_base +
            (count * c_query_path_prefix_penalty_step) + recent_bonus +
            ((get_encoded_path_segment_count_local(stored_path) -
            get_encoded_path_segment_count_local(normalized_path)) * 8);
        if local_penalty > c_query_path_prefix_penalty_cap then
        begin
            local_penalty := c_query_path_prefix_penalty_cap;
        end;
        if local_penalty > Result then
        begin
            Result := local_penalty;
        end;
    end;

    if (cache_key <> '') and (m_lookup_query_path_bonus_cache <> nil) then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_session_ranked_query_path_bonus(const encoded_path: string): Integer;
const
    c_recent_ranked_same_path_base = 86;
    c_recent_ranked_extended_path_base = 168;
    c_recent_ranked_same_query_extra = 36;
    c_recent_ranked_long_prefix_step = 18;
    c_recent_ranked_bonus_cap = 320;
var
    best_bonus: Integer;
    cache_key: string;
    current_query: string;
    current_score: Integer;
    current_stored_path: string;
    key_pair: TPair<string, string>;
    local_bonus: Integer;
    normalized_path: string;
    prefix_query: string;
begin
    Result := 0;
    normalized_path := Trim(encoded_path);
    current_query := normalize_pinyin_text(m_last_lookup_key);
    if (normalized_path = '') or (current_query = '') or
        (get_encoded_path_segment_count_local(normalized_path) <= 1) or
        (m_session_ranked_query_paths = nil) or
        (m_session_ranked_query_paths.Count <= 0) then
    begin
        Exit;
    end;

    cache_key := 'SR' + #1 + current_query + #1 + normalized_path;
    if (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    best_bonus := 0;
    for key_pair in m_session_ranked_query_paths do
    begin
        prefix_query := key_pair.Key;
        current_stored_path := Trim(key_pair.Value);
        if (prefix_query = '') or (current_stored_path = '') then
        begin
            Continue;
        end;
        if (Length(prefix_query) > Length(current_query)) or
            (Copy(current_query, 1, Length(prefix_query)) <> prefix_query) then
        begin
            Continue;
        end;

        if not m_session_ranked_query_path_scores.TryGetValue(prefix_query, current_score) then
        begin
            current_score := 0;
        end;

        local_bonus := 0;
        if normalized_path = current_stored_path then
        begin
            local_bonus := c_recent_ranked_same_path_base;
        end
        else if Copy(normalized_path, 1, Length(current_stored_path) + 1) =
            (current_stored_path + c_segment_path_separator) then
        begin
            local_bonus := c_recent_ranked_extended_path_base;
        end;

        if local_bonus <= 0 then
        begin
            Continue;
        end;

        if SameText(prefix_query, current_query) then
        begin
            Inc(local_bonus, c_recent_ranked_same_query_extra);
        end;

        if Length(prefix_query) >= 4 then
        begin
            Inc(local_bonus, Min(72, (Length(prefix_query) - 3) * c_recent_ranked_long_prefix_step));
        end;

        if current_score > 0 then
        begin
            Inc(local_bonus, Min(84, current_score div 8));
        end;

        if local_bonus > best_bonus then
        begin
            best_bonus := local_bonus;
        end;
    end;

    if best_bonus > c_recent_ranked_bonus_cap then
    begin
        best_bonus := c_recent_ranked_bonus_cap;
    end;
    Result := best_bonus;

    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_persistent_query_path_prefix_support(const encoded_path: string): Integer;
const
    c_query_path_prefix_support_cap = 420;
    c_query_path_prefix_penalty_cap = 260;
var
    cache_key: string;
    normalized_path: string;
    segment_text: string;
    prefix_path: string;
    prefix_query: string;
    normalized_lookup_query: string;
    bonus_value: Integer;
    penalty_value: Integer;
    positive_total: Integer;
    negative_total: Integer;
    idx: Integer;
    path_start: Integer;
begin
    Result := 0;
    normalized_path := Trim(encoded_path);
    if (normalized_path = '') or (get_encoded_path_segment_count_local(normalized_path) <= 1) or
        (m_dictionary = nil) then
    begin
        Exit;
    end;

    normalized_lookup_query := normalize_pinyin_text(m_last_lookup_key);
    if normalized_lookup_query = '' then
    begin
        Exit;
    end;

    cache_key := 'DP' + #1 + normalized_path;
    if (m_lookup_query_path_bonus_cache <> nil) and
        m_lookup_query_path_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    prefix_path := '';
    positive_total := 0;
    negative_total := 0;
    path_start := 1;
    for idx := 1 to Length(normalized_path) + 1 do
    begin
        if (idx <= Length(normalized_path)) and (normalized_path[idx] <> c_segment_path_separator) then
        begin
            Continue;
        end;

        segment_text := Trim(Copy(normalized_path, path_start, idx - path_start));
        path_start := idx + 1;
        if segment_text = '' then
        begin
            Continue;
        end;

        if prefix_path <> '' then
        begin
            prefix_path := prefix_path + c_segment_path_separator;
        end;
        prefix_path := prefix_path + segment_text;

        if (prefix_path = normalized_path) or
            (get_encoded_path_segment_count_local(prefix_path) <= 1) then
        begin
            Continue;
        end;

        prefix_query := get_query_prefix_for_segment_path(prefix_path);
        if (prefix_query = '') or (prefix_query = normalized_lookup_query) then
        begin
            Continue;
        end;

        bonus_value := get_cached_query_segment_path_bonus(prefix_query,
            prefix_path);
        penalty_value := get_cached_query_segment_path_penalty(prefix_query,
            prefix_path);
        if bonus_value > 0 then
        begin
            Inc(positive_total, bonus_value);
        end;
        if penalty_value > 0 then
        begin
            Inc(negative_total, penalty_value);
        end;
    end;

    if positive_total > c_query_path_prefix_support_cap then
    begin
        positive_total := c_query_path_prefix_support_cap;
    end;
    if negative_total > c_query_path_prefix_penalty_cap then
    begin
        negative_total := c_query_path_prefix_penalty_cap;
    end;
    Result := positive_total - negative_total;

    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_context_query_bonus(const candidate_text: string): Integer;
const
    c_context_combined_cap = 720;
    c_multi_context_query_base = 118;
    c_multi_context_query_step = 62;
    c_multi_context_query_cap = 520;
    c_single_context_query_base = 40;
    c_single_context_query_step = 26;
    c_single_context_query_cap = 180;
    c_recent_top = 150;
    c_recent_mid = 88;
    c_recent_tail = 46;
var
    context_value: string;
    context_variants: TArray<string>;
    variant_idx: Integer;
    variant_weight: Integer;
    key: string;
    text_key: string;
    count: Integer;
    last_seen_serial: Int64;
    serial_gap: Int64;
    text_units: Integer;
    context_units: Integer;
    variant_bonus: Integer;
    persistent_bonus: Integer;
    session_found: Boolean;

    function get_variant_weight(const variant_index: Integer): Integer;
    begin
        case variant_index of
            0:
                Result := 100;
            1:
                Result := 88;
            2:
                Result := 72;
            3:
                Result := 58;
        else
            Result := 42;
        end;
    end;

    function merge_variant_bonus(const current_bonus: Integer; const weighted_bonus: Integer): Integer;
    begin
        Result := current_bonus;
        if weighted_bonus <= 0 then
        begin
            Exit;
        end;

        if Result <= 0 then
        begin
            Result := weighted_bonus;
            Exit;
        end;

        if weighted_bonus > Result then
        begin
            Result := weighted_bonus + (Result div 3);
        end
        else
        begin
            Result := Result + (weighted_bonus div 3);
        end;

        if Result > c_context_combined_cap then
        begin
            Result := c_context_combined_cap;
        end;
    end;
begin
    text_key := Trim(candidate_text);
    if (text_key <> '') and (m_lookup_context_query_bonus_cache <> nil) and
        m_lookup_context_query_bonus_cache.TryGetValue(text_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (text_key = '') or (m_last_lookup_key = '') then
    begin
        Exit;
    end;

    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end;
    if (m_segment_left_context = '') and (m_external_left_context <> '') then
    begin
        context_value := m_external_left_context;
    end;
    if context_value = '' then
    begin
        Exit;
    end;

    context_variants := get_context_variants(context_value);
    if Length(context_variants) = 0 then
    begin
        Exit;
    end;

    text_units := get_candidate_text_unit_count(text_key);
    for variant_idx := 0 to High(context_variants) do
    begin
        key := build_context_query_choice_key(context_variants[variant_idx], m_last_lookup_key, text_key);
        count := 0;
        session_found := (key <> '') and
            (m_session_context_query_choice_counts <> nil) and
            m_session_context_query_choice_counts.TryGetValue(key, count) and
            (count > 0);
        variant_bonus := 0;
        if session_found then
        begin
            if text_units <= 1 then
            begin
                variant_bonus := c_single_context_query_base +
                    ((count - 1) * c_single_context_query_step);
                if variant_bonus > c_single_context_query_cap then
                begin
                    variant_bonus := c_single_context_query_cap;
                end;
            end
            else
            begin
                variant_bonus := c_multi_context_query_base +
                    ((count - 1) * c_multi_context_query_step);
                if count >= 3 then
                begin
                    Inc(variant_bonus, 36);
                end;
                if variant_bonus > c_multi_context_query_cap then
                begin
                    variant_bonus := c_multi_context_query_cap;
                end;
            end;
        end;

        context_units := get_candidate_text_unit_count(
            context_variants[variant_idx]);
        persistent_bonus := 0;
        if (m_dictionary <> nil) and (context_units >= 2) and
            (context_units <= 4) then
        begin
            persistent_bonus := m_dictionary.get_context_query_choice_bonus(
                context_variants[variant_idx], m_last_lookup_key, text_key);
        end;
        if persistent_bonus > variant_bonus then
        begin
            persistent_bonus := persistent_bonus + (variant_bonus div 3);
            variant_bonus := persistent_bonus;
        end
        else if persistent_bonus > 0 then
        begin
            Inc(variant_bonus, persistent_bonus div 3);
        end;
        if variant_bonus <= 0 then
        begin
            Continue;
        end;

        if session_found and
            (m_session_context_query_choice_last_seen <> nil) and
            m_session_context_query_choice_last_seen.TryGetValue(key,
            last_seen_serial) and (last_seen_serial > 0) and
            (m_session_commit_serial >= last_seen_serial) then
        begin
            serial_gap := m_session_commit_serial - last_seen_serial;
            if serial_gap <= 1 then
            begin
                Inc(variant_bonus, c_recent_top);
            end
            else if serial_gap <= 3 then
            begin
                Inc(variant_bonus, c_recent_mid);
            end
            else if serial_gap <= 6 then
            begin
                Inc(variant_bonus, c_recent_tail);
            end;
        end;

        variant_weight := get_variant_weight(variant_idx);
        variant_bonus := (variant_bonus * variant_weight) div 100;
        Result := merge_variant_bonus(Result, variant_bonus);
    end;

    if (text_key <> '') and (m_lookup_context_query_bonus_cache <> nil) then
    begin
        m_lookup_context_query_bonus_cache.AddOrSetValue(text_key, Result);
    end;
end;

function TncEngine.get_context_query_latest_bonus(const candidate_text: string): Integer;
const
    c_context_latest_cap = 320;
    c_phrase_latest_bonus = 228;
    c_single_latest_bonus = 92;
var
    context_value: string;
    context_variants: TArray<string>;
    variant_idx: Integer;
    variant_weight: Integer;
    key: string;
    text_key: string;
    latest_text: string;
    text_units: Integer;
    variant_bonus: Integer;

    function get_variant_weight(const variant_index: Integer): Integer;
    begin
        case variant_index of
            0:
                Result := 100;
            1:
                Result := 88;
            2:
                Result := 72;
            3:
                Result := 58;
        else
            Result := 42;
        end;
    end;

    function merge_variant_bonus(const current_bonus: Integer; const weighted_bonus: Integer): Integer;
    begin
        Result := current_bonus;
        if weighted_bonus <= 0 then
        begin
            Exit;
        end;

        if Result <= 0 then
        begin
            Result := weighted_bonus;
            Exit;
        end;

        if weighted_bonus > Result then
        begin
            Result := weighted_bonus + (Result div 3);
        end
        else
        begin
            Result := Result + (weighted_bonus div 3);
        end;

        if Result > c_context_latest_cap then
        begin
            Result := c_context_latest_cap;
        end;
    end;
begin
    text_key := Trim(candidate_text);
    if (text_key <> '') and (m_lookup_context_query_latest_bonus_cache <> nil) and
        m_lookup_context_query_latest_bonus_cache.TryGetValue(text_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if (text_key = '') or (m_last_lookup_key = '') or (m_session_context_query_latest_text = nil) then
    begin
        Exit;
    end;

    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end;
    if (m_segment_left_context = '') and (m_external_left_context <> '') then
    begin
        context_value := m_external_left_context;
    end;
    if context_value = '' then
    begin
        Exit;
    end;

    context_variants := get_context_variants(context_value);
    if Length(context_variants) = 0 then
    begin
        Exit;
    end;

    text_units := get_candidate_text_unit_count(text_key);
    for variant_idx := 0 to High(context_variants) do
    begin
        key := build_context_query_scope_key(context_variants[variant_idx], m_last_lookup_key);
        if (key = '') or (not m_session_context_query_latest_text.TryGetValue(key, latest_text)) then
        begin
            Continue;
        end;
        if not SameText(text_key, latest_text) then
        begin
            Continue;
        end;
        key := build_context_query_choice_key(context_variants[variant_idx], m_last_lookup_key, text_key);
        if (key = '') or ((m_session_context_query_choice_last_seen <> nil) and
            (not m_session_context_query_choice_last_seen.ContainsKey(key))) then
        begin
            Continue;
        end;

        if text_units <= 1 then
        begin
            variant_bonus := c_single_latest_bonus;
        end
        else
        begin
            variant_bonus := c_phrase_latest_bonus;
            if (m_last_lookup_syllable_count >= 3) and (text_units + 1 >= m_last_lookup_syllable_count) then
            begin
                Inc(variant_bonus, 32);
            end;
        end;

        variant_weight := get_variant_weight(variant_idx);
        variant_bonus := (variant_bonus * variant_weight) div 100;
        Result := merge_variant_bonus(Result, variant_bonus);
    end;

    if (text_key <> '') and (m_lookup_context_query_latest_bonus_cache <> nil) then
    begin
        m_lookup_context_query_latest_bonus_cache.AddOrSetValue(text_key, Result);
    end;
end;

function TncEngine.is_latest_session_query_choice(const candidate_text: string): Boolean;
var
    cached_latest_text: string;
    latest_text: string;
    text_key: string;
begin
    Result := False;
    text_key := Trim(candidate_text);
    if (m_last_lookup_key = '') or (text_key = '') then
    begin
        Exit;
    end;

    if (m_session_query_latest_text <> nil) and
        m_session_query_latest_text.TryGetValue(m_last_lookup_key, latest_text) then
    begin
        Exit(SameText(text_key, latest_text));
    end;

    if m_dictionary = nil then
    begin
        Exit(False);
    end;

    latest_text := '';
    if (m_lookup_query_latest_text_cache <> nil) and
        m_lookup_query_latest_text_cache.TryGetValue(m_last_lookup_key, cached_latest_text) then
    begin
        latest_text := cached_latest_text;
    end
    else
    begin
        latest_text := get_cached_query_latest_choice_text(m_last_lookup_key);
        if m_lookup_query_latest_text_cache <> nil then
        begin
            m_lookup_query_latest_text_cache.AddOrSetValue(m_last_lookup_key, latest_text);
        end;
    end;

    Result := SameText(text_key, latest_text);
end;

function TncEngine.get_phrase_context_bonus(const candidate_text: string): Integer;
const
    c_phrase_pair_step = 120;
    c_phrase_pair_cap = 360;
    c_phrase_trigram_step = 150;
    c_phrase_trigram_cap = 460;
    c_phrase_context_cap = 620;
    c_phrase_pair_recent_top = 160;
    c_phrase_pair_recent_mid = 92;
    c_phrase_pair_recent_tail = 44;
    c_phrase_trigram_recent_top = 220;
    c_phrase_trigram_recent_mid = 132;
    c_phrase_trigram_recent_tail = 64;
    c_context_variant_pair_cap = 420;
var
    pair_count: Integer;
    pair_bonus: Integer;
    trigram_count: Integer;
    trigram_bonus: Integer;
    key: string;
    text_key: string;
    last_seen_serial: Int64;
    serial_gap: Int64;
    pair_recent_bonus: Integer;
    trigram_recent_bonus: Integer;
    context_value: string;
    context_variants: TArray<string>;
    variant_idx: Integer;
    variant_weight: Integer;
    variant_bonus: Integer;

    function get_variant_weight(const variant_index: Integer): Integer;
    begin
        case variant_index of
            0:
                Result := 100;
            1:
                Result := 88;
            2:
                Result := 72;
            3:
                Result := 58;
        else
            Result := 42;
        end;
    end;

    function merge_bonus(const current_bonus: Integer; const next_bonus: Integer): Integer;
    begin
        Result := current_bonus;
        if next_bonus <= 0 then
        begin
            Exit;
        end;

        if Result <= 0 then
        begin
            Result := next_bonus;
            Exit;
        end;

        if next_bonus > Result then
        begin
            Result := next_bonus + (Result div 3);
        end
        else
        begin
            Result := Result + (next_bonus div 3);
        end;

        if Result > c_phrase_context_cap then
        begin
            Result := c_phrase_context_cap;
        end;
    end;

    function get_recent_phrase_bonus(
        const phrase_key: string;
        const top_bonus: Integer;
        const mid_bonus: Integer;
        const tail_bonus: Integer
    ): Integer;
    begin
        Result := 0;
        if (phrase_key = '') or (m_phrase_context_last_seen = nil) then
        begin
            Exit;
        end;
        if (not m_phrase_context_last_seen.TryGetValue(phrase_key, last_seen_serial)) or
            (last_seen_serial <= 0) or (m_session_commit_serial < last_seen_serial) then
        begin
            Exit;
        end;

        serial_gap := m_session_commit_serial - last_seen_serial;
        if serial_gap <= 1 then
        begin
            Result := top_bonus;
        end
        else if serial_gap <= 3 then
        begin
            Result := mid_bonus;
        end
        else if serial_gap <= 6 then
        begin
            Result := tail_bonus;
        end;
    end;
begin
    text_key := Trim(candidate_text);
    if (text_key <> '') and (m_lookup_phrase_context_bonus_cache <> nil) and
        m_lookup_phrase_context_bonus_cache.TryGetValue(text_key, Result) then
    begin
        Exit;
    end;

    Result := 0;
    if m_phrase_context_pairs = nil then
    begin
        Exit;
    end;

    if (text_key = '') or (m_last_output_commit_text = '') then
    begin
        Exit;
    end;

    pair_bonus := 0;
    trigram_bonus := 0;
    pair_recent_bonus := 0;
    trigram_recent_bonus := 0;

    key := m_last_output_commit_text + #1 + text_key;
    if m_phrase_context_pairs.TryGetValue(key, pair_count) and (pair_count > 0) then
    begin
        pair_bonus := pair_count * c_phrase_pair_step;
        if pair_bonus > c_phrase_pair_cap then
        begin
            pair_bonus := c_phrase_pair_cap;
        end;
        pair_recent_bonus := get_recent_phrase_bonus(
            key,
            c_phrase_pair_recent_top,
            c_phrase_pair_recent_mid,
            c_phrase_pair_recent_tail);
    end;

    if m_prev_output_commit_text <> '' then
    begin
        key := m_prev_output_commit_text + #2 + m_last_output_commit_text + #1 + text_key;
        if m_phrase_context_pairs.TryGetValue(key, trigram_count) and (trigram_count > 0) then
        begin
            trigram_bonus := trigram_count * c_phrase_trigram_step;
            if trigram_bonus > c_phrase_trigram_cap then
            begin
                trigram_bonus := c_phrase_trigram_cap;
            end;
            trigram_recent_bonus := get_recent_phrase_bonus(
                key,
                c_phrase_trigram_recent_top,
                c_phrase_trigram_recent_mid,
                c_phrase_trigram_recent_tail);
        end;
    end;

    if trigram_bonus > 0 then
    begin
        Result := trigram_bonus + (pair_bonus div 2) + trigram_recent_bonus + (pair_recent_bonus div 2);
    end
    else
    begin
        Result := pair_bonus + pair_recent_bonus;
    end;

    if Result > c_phrase_context_cap then
    begin
        Result := c_phrase_context_cap;
    end;

    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end;
    if (m_segment_left_context = '') and (m_external_left_context <> '') then
    begin
        context_value := m_external_left_context;
    end;
    if context_value <> '' then
    begin
        context_variants := get_context_variants(context_value);
        for variant_idx := 0 to High(context_variants) do
        begin
            key := Trim(context_variants[variant_idx]) + #1 + text_key;
            if (key = '') or (not m_phrase_context_pairs.TryGetValue(key, pair_count)) or (pair_count <= 0) then
            begin
                Continue;
            end;

            variant_bonus := pair_count * c_phrase_pair_step;
            if variant_bonus > c_context_variant_pair_cap then
            begin
                variant_bonus := c_context_variant_pair_cap;
            end;
            Inc(variant_bonus, get_recent_phrase_bonus(
                key,
                c_phrase_pair_recent_top,
                c_phrase_pair_recent_mid,
                c_phrase_pair_recent_tail));

            variant_weight := get_variant_weight(variant_idx);
            variant_bonus := (variant_bonus * variant_weight) div 100;
            Result := merge_bonus(Result, variant_bonus);
        end;
    end;

    if (text_key <> '') and (m_lookup_phrase_context_bonus_cache <> nil) then
    begin
        m_lookup_phrase_context_bonus_cache.AddOrSetValue(text_key, Result);
    end;
end;

function TncEngine.get_candidate_text_unit_count(const text: string): Integer;
const
    c_text_unit_count_cache_limit = 8192;
var
    text_idx: Integer;
    text_char: Char;
begin
    if (text <> '') and (m_lookup_text_unit_count_cache <> nil) and
        m_lookup_text_unit_count_cache.TryGetValue(text, Result) then
    begin
        Exit;
    end;

    Result := 0;
    text_idx := 1;
    while text_idx <= Length(text) do
    begin
        text_char := text[text_idx];
        if (Ord(text_char) >= $D800) and (Ord(text_char) <= $DBFF) and
            (text_idx < Length(text)) and
            (Ord(text[text_idx + 1]) >= $DC00) and
            (Ord(text[text_idx + 1]) <= $DFFF) then
        begin
            Inc(text_idx, 2);
        end
        else
        begin
            Inc(text_idx);
        end;
        Inc(Result);
    end;
    if (text <> '') and (m_lookup_text_unit_count_cache <> nil) then
    begin
        if m_lookup_text_unit_count_cache.Count >=
            c_text_unit_count_cache_limit then
        begin
            m_lookup_text_unit_count_cache.Clear;
        end;
        m_lookup_text_unit_count_cache.AddOrSetValue(text, Result);
    end;
end;

function TncEngine.try_get_fixed_pronoun_tail_query_parts(
    const query_text: string; const expected_unit_count: Integer;
    out head_pinyin: string; out head_text: string; out tail_key: string;
    out tail_units: Integer): Boolean;
var
    normalized_query: string;
    head_query_length: Integer;
    query_syllables: TncPinyinParseResult;
    tail_syllables: TncPinyinParseResult;
begin
    Result := False;
    head_pinyin := '';
    head_text := '';
    tail_key := '';
    tail_units := 0;

    normalized_query := normalize_pinyin_text(query_text);
    if (normalized_query = '') or (expected_unit_count < 1) then
    begin
        Exit;
    end;

    if Copy(normalized_query, 1, 2) = 'wo' then
    begin
        head_pinyin := 'wo';
        head_text := string(Char($6211));
        head_query_length := 2;
    end
    else if Copy(normalized_query, 1, 2) = 'ni' then
    begin
        head_pinyin := 'ni';
        head_text := string(Char($4F60));
        head_query_length := 2;
    end
    else if Copy(normalized_query, 1, 3) = 'zui' then
    begin
        head_pinyin := 'zui';
        head_text := string(Char($6700));
        head_query_length := 3;
    end
    else if Copy(normalized_query, 1, 2) = 'bu' then
    begin
        head_pinyin := 'bu';
        head_text := string(Char($4E0D));
        head_query_length := 2;
    end
    else if Copy(normalized_query, 1, 3) = 'hen' then
    begin
        head_pinyin := 'hen';
        head_text := string(Char($5F88));
        head_query_length := 3;
    end
    else if Copy(normalized_query, 1, 3) = 'jiu' then
    begin
        head_pinyin := 'jiu';
        head_text := string(Char($5C31));
        head_query_length := 3;
    end
    else if Copy(normalized_query, 1, 4) = 'qing' then
    begin
        head_pinyin := 'qing';
        head_text := string(Char($8BF7));
        head_query_length := 4;
    end
    else
    begin
        Exit;
    end;

    query_syllables := get_effective_compact_pinyin_syllables(
        normalized_query);
    if (Length(query_syllables) = 0) or
        (not SameText(normalize_pinyin_text(query_syllables[0].text),
        head_pinyin)) then
    begin
        Exit;
    end;

    tail_key := Copy(normalized_query, head_query_length + 1, MaxInt);
    tail_syllables := get_effective_compact_pinyin_syllables(tail_key);
    tail_units := Length(tail_syllables);
    if tail_units <= 0 then
    begin
        tail_units := expected_unit_count - 1;
    end;
    Result := tail_key <> '';
end;

function TncEngine.try_get_short_particle_tail_query_parts(
    const query_text: string; out tail_key: string; out tail_text: string;
    out head_key: string): Boolean;
var
    normalized_query: string;

    function try_tail_local(const candidate_tail_key: string;
        const candidate_tail_text: string): Boolean;
    begin
        Result := False;
        if (candidate_tail_key = '') or (candidate_tail_text = '') or
            (Length(normalized_query) <= Length(candidate_tail_key)) or
            (not SameText(Copy(normalized_query,
            Length(normalized_query) - Length(candidate_tail_key) + 1,
            Length(candidate_tail_key)), candidate_tail_key)) then
        begin
            Exit;
        end;

        head_key := Copy(normalized_query, 1,
            Length(normalized_query) - Length(candidate_tail_key));
        if (head_key = '') or (not is_full_pinyin_key(head_key)) then
        begin
            head_key := '';
            Exit;
        end;

        tail_key := candidate_tail_key;
        tail_text := candidate_tail_text;
        Result := True;
    end;
begin
    Result := False;
    tail_key := '';
    tail_text := '';
    head_key := '';

    normalized_query := normalize_pinyin_text(query_text);
    if normalized_query = '' then
    begin
        Exit;
    end;

    Result := try_tail_local('zhe', string(Char($7740))) or
        try_tail_local('de', string(Char($7684))) or
        try_tail_local('le', string(Char($4E86))) or
        try_tail_local('la', string(Char($5566))) or
        try_tail_local('ba', string(Char($5427))) or
        try_tail_local('ma', string(Char($5417))) or
        try_tail_local('a', string(Char($554A)));
end;

function TncEngine.is_runtime_chain_candidate(const candidate: TncCandidate): Boolean;
begin
    Result := (m_runtime_chain_text <> '') and (candidate.source = cs_rule) and
        (candidate.comment = '') and (not candidate.has_dict_weight) and
        SameText(candidate.text, m_runtime_chain_text);
end;

function TncEngine.is_runtime_common_pattern_candidate(const candidate: TncCandidate): Boolean;
begin
    Result := (m_runtime_common_pattern_text <> '') and (candidate.source = cs_rule) and
        (candidate.comment = '') and (not candidate.has_dict_weight) and
        SameText(candidate.text, m_runtime_common_pattern_text);
end;

function TncEngine.is_runtime_redup_candidate(const candidate: TncCandidate): Boolean;
begin
    Result := (m_runtime_redup_text <> '') and (candidate.source = cs_rule) and
        (candidate.comment = '') and (not candidate.has_dict_weight) and
        SameText(candidate.text, m_runtime_redup_text);
end;

function TncEngine.is_nonlexicon_long_segment_path_candidate(
    const candidate: TncCandidate): Boolean;
var
    encoded_path: string;
begin
    Result := False;
    if (candidate.comment <> '') or candidate.has_dict_weight or
        (candidate.source = cs_user) or is_runtime_chain_candidate(candidate) or
        is_runtime_common_pattern_candidate(candidate) or
        is_runtime_redup_candidate(candidate) or
        (get_candidate_text_unit_count(candidate.text) < 5) then
    begin
        Exit;
    end;

    encoded_path := get_segment_path_for_candidate(candidate);
    Result := get_encoded_path_segment_count_local(encoded_path) > 1;
end;

function TncEngine.get_runtime_candidate_kind(const candidate: TncCandidate): string;
begin
    Result := '';
    if is_runtime_chain_candidate(candidate) then
    begin
        Result := 'chain';
    end
    else if is_runtime_common_pattern_candidate(candidate) then
    begin
        Result := 'pattern';
    end
    else if is_runtime_redup_candidate(candidate) then
    begin
        Result := 'redup';
    end;
end;

function TncEngine.is_runtime_constructed_phrase_friendly(const text: string): Boolean;
var
    units: TArray<string>;
    tail_text: string;
begin
    Result := False;
    units := split_text_units(Trim(text));
    if Length(units) <> 2 then
    begin
        Exit;
    end;

    if units[0] = units[1] then
    begin
        Result := True;
        Exit;
    end;

    tail_text := units[1];
    if tail_text = '' then
    begin
        Exit;
    end;

    case Ord(tail_text[1]) of
        $5427, $5417, $5462, $4E2A, $4F4D, $6B21, $70B9,
        $4E9B, $79CD, $5929, $5E74, $6708, $91CC, $4E0B,
        $56DE, $904D, $58F0, $9762, $773C, $8FB9:
            Result := True;
    end;
end;

function TncEngine.get_candidate_path_confidence_score(const candidate: TncCandidate): Integer;
var
    encoded_path: string;
    text_units: Integer;
    cache_key: string;
    damp_long_exact_dict_local: Boolean;
begin
    Result := 0;
    encoded_path := get_segment_path_for_candidate(candidate);
    if get_encoded_path_segment_count_local(encoded_path) <= 1 then
    begin
        Exit;
    end;

    damp_long_exact_dict_local := (m_last_lookup_syllable_count >= 5) and
        (candidate.comment = '') and candidate.has_dict_weight;

    if is_nonlexicon_long_segment_path_candidate(candidate) then
    begin
        Exit;
    end;

    cache_key := build_candidate_identity_key(candidate.text, candidate.comment) + #1 + encoded_path;
    if (m_lookup_candidate_path_confidence_cache <> nil) and
        m_lookup_candidate_path_confidence_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    Result := get_segment_path_support_score(candidate);
    if Result < 0 then
    begin
        Result := 0;
    end;

    if damp_long_exact_dict_local and (Result > 0) then
    begin
        // Long exact dict phrases still need path evidence to influence final
        // ranking, but keep it damped so weak inferred paths do not dominate
        // lexical score by themselves.
        Result := (Result * 3) div 4;
    end;

    if (Result > 0) and (candidate.comment <> '') then
    begin
        text_units := get_candidate_text_unit_count(candidate.text);
        if is_runtime_chain_candidate(candidate) then
        begin
            Result := Result div 4;
        end
        else
        begin
            Result := (Result * 3) div 5;
            if (text_units >= 2) and (m_last_lookup_syllable_count >= 3) then
            begin
                Inc(Result, Min(88, Result div 6));
            end;
        end;
    end;

    if m_lookup_candidate_path_confidence_cache <> nil then
    begin
        m_lookup_candidate_path_confidence_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_candidate_path_confidence_tier(const candidate: TncCandidate): Integer;
var
    path_confidence_score: Integer;
begin
    Result := 0;
    path_confidence_score := get_candidate_path_confidence_score(candidate);
    if path_confidence_score >= 880 then
    begin
        Result := 2;
    end
    else if path_confidence_score >= 420 then
    begin
        Result := 1;
    end;
end;

function TncEngine.get_candidate_segment_path_score_hint(const candidate: TncCandidate): Integer;
var
    key: string;
begin
    Result := Low(Integer);
    if m_current_segment_path_score_map = nil then
    begin
        Exit;
    end;

    key := build_candidate_identity_key(candidate.text, candidate.comment);
    if key = '' then
    begin
        Exit;
    end;

    if not m_current_segment_path_score_map.TryGetValue(key, Result) then
    begin
        Result := Low(Integer);
    end;
end;

function TncEngine.get_candidate_confidence_rank(const candidate: TncCandidate): Integer;
var
    layer_value: Integer;
    text_units: Integer;
    context_bonus: Integer;
    query_bonus: Integer;
    session_bonus: Integer;
    path_confidence_tier: Integer;
    path_confidence_score: Integer;
begin
    layer_value := get_multi_syllable_intent_layer(candidate);
    text_units := get_candidate_text_unit_count(candidate.text);
    path_confidence_score := get_candidate_path_confidence_score(candidate);
    path_confidence_tier := get_candidate_path_confidence_tier(candidate);

    if candidate.comment <> '' then
    begin
        if (text_units >= 2) and (path_confidence_score >= 720) and
            (not is_runtime_chain_candidate(candidate)) then
        begin
            Result := 5;
        end
        else if text_units >= 2 then
        begin
            Result := 6;
        end
        else
        begin
            Result := 8;
        end;
        Exit;
    end;

    if candidate.source = cs_user then
    begin
        Result := 0;
        Exit;
    end;

    if candidate.has_dict_weight then
    begin
        if layer_value = 0 then
        begin
            Result := 0;
        end
        else if layer_value = 1 then
        begin
            Result := 1;
        end
        else
        begin
            Result := 2;
        end;
    end
    else if is_runtime_common_pattern_candidate(candidate) then
    begin
        Result := 3;
    end
    else if is_runtime_redup_candidate(candidate) then
    begin
        Result := 4;
    end
    else if is_runtime_chain_candidate(candidate) then
    begin
        Result := 8;
    end
    else if (candidate.source = cs_rule) and (text_units >= 2) then
    begin
        Result := 5;
    end
    else
    begin
        Result := 7;
    end;

    if Result <= 0 then
    begin
        Exit;
    end;

    context_bonus := get_context_bonus(candidate.text);
    query_bonus := get_session_query_bonus(candidate.text);
    session_bonus := get_session_text_bonus(candidate.text);
    if candidate.comment <> '' then
    begin
        query_bonus := query_bonus div 2;
        session_bonus := session_bonus div 2;
    end;

    if (text_units >= 2) and ((context_bonus >= 320) or (query_bonus >= 320) or (session_bonus >= 320)) then
    begin
        Dec(Result);
    end;
    if path_confidence_tier >= 2 then
    begin
        Dec(Result, 2);
    end
    else if path_confidence_tier = 1 then
    begin
        Dec(Result);
    end;
    if path_confidence_score >= 1280 then
    begin
        Dec(Result);
    end;
    if Result < 0 then
    begin
        Result := 0;
    end;
end;

function TncEngine.get_front_row_confidence_bonus(const candidate: TncCandidate): Integer;
var
    text_units: Integer;
    layer_value: Integer;
    path_confidence_tier: Integer;
    path_confidence_score: Integer;
begin
    Result := 0;
    text_units := get_candidate_text_unit_count(candidate.text);
    layer_value := get_multi_syllable_intent_layer(candidate);
    path_confidence_score := get_candidate_path_confidence_score(candidate);
    path_confidence_tier := get_candidate_path_confidence_tier(candidate);

    if candidate.comment <> '' then
    begin
        if m_last_lookup_syllable_count >= 3 then
        begin
            Dec(Result, 60 + (layer_value * 18));
        end;
        if (text_units >= 2) and (path_confidence_score > 0) and
            (not is_runtime_chain_candidate(candidate)) then
        begin
            case path_confidence_tier of
                1: Inc(Result, 24);
                2: Inc(Result, 56);
            end;
            Inc(Result, Min(84, path_confidence_score div 18));
        end;
        Exit;
    end;

    if candidate.has_dict_weight then
    begin
        if layer_value = 0 then
        begin
            Inc(Result, 140);
            if (m_last_lookup_syllable_count >= 4) and (text_units >= 3) then
            begin
                Inc(Result, 36);
            end;
        end
        else if layer_value = 1 then
        begin
            Inc(Result, 68);
        end;
    end;
    if (not candidate.has_dict_weight) and is_runtime_common_pattern_candidate(candidate) then
    begin
        Inc(Result, 96);
    end;
    if (not candidate.has_dict_weight) and is_runtime_redup_candidate(candidate) then
    begin
        Inc(Result, 72);
    end;

    if is_runtime_chain_candidate(candidate) then
    begin
        Dec(Result, 320);
        Exit;
    end;

    if (candidate.source = cs_rule) and (text_units >= 2) then
    begin
        Dec(Result, 120);
    end;

    case path_confidence_tier of
        1: Inc(Result, 28);
        2: Inc(Result, 72);
    end;
    if path_confidence_score > 0 then
    begin
        Inc(Result, Min(48, path_confidence_score div 40));
    end;
end;

function TncEngine.get_multi_syllable_intent_layer(const candidate: TncCandidate): Integer;
var
    text_units: Integer;
    syllable_gap: Integer;
begin
    Result := 0;
    if m_last_lookup_syllable_count < 3 then
    begin
        Exit;
    end;

    text_units := get_candidate_text_unit_count(candidate.text);
    syllable_gap := m_last_lookup_syllable_count - text_units;

    if candidate.comment = '' then
    begin
        if text_units <= 0 then
        begin
            Result := 8;
        end
        else if text_units = 1 then
        begin
            Result := 6;
        end
        else if syllable_gap = 0 then
        begin
            Result := 0;
        end
        else if syllable_gap = 1 then
        begin
            Result := 1;
        end
        else if syllable_gap > 1 then
        begin
            Result := 2;
        end
        else
        begin
            Result := 5;
        end;
        Exit;
    end;

    if text_units <= 0 then
    begin
        Result := 9;
    end
    else if text_units = 1 then
    begin
        Result := 7;
    end
    else if text_units + 1 >= m_last_lookup_syllable_count then
    begin
        Result := 3;
    end
    else
    begin
        Result := 4;
    end;
end;

function TncEngine.get_text_context_bonus(const candidate_text: string): Integer;
const
    c_context_combined_cap = 620;
var
    context_value: string;
    count: Integer;
    local_bonus: Integer;
    persistent_bonus: Integer;
    context_variants: TArray<string>;
    variant_idx: Integer;
    variant_weight: Integer;
    variant_key: string;
    variant_bonus: Integer;
    secondary_bonus: Integer;

    function get_variant_weight(const variant_index: Integer): Integer;
    begin
        case variant_index of
            0:
                Result := 100;
            1:
                Result := 88;
            2:
                Result := 72;
            3:
                Result := 58;
        else
            Result := 42;
        end;
    end;

    function merge_variant_bonus(const current_bonus: Integer; const weighted_bonus: Integer): Integer;
    begin
        Result := current_bonus;
        if weighted_bonus <= 0 then
        begin
            Exit;
        end;

        if Result <= 0 then
        begin
            Result := weighted_bonus;
            Exit;
        end;

        if weighted_bonus > Result then
        begin
            Result := weighted_bonus + (Result div 3);
        end
        else
        begin
            Result := Result + (weighted_bonus div 3);
        end;

        if Result > c_context_combined_cap then
        begin
            Result := c_context_combined_cap;
        end;
    end;
begin
    if (candidate_text <> '') and (m_lookup_text_context_bonus_cache <> nil) and
        m_lookup_text_context_bonus_cache.TryGetValue(candidate_text, Result) then
    begin
        Exit;
    end;

    Result := 0;
    local_bonus := 0;
    persistent_bonus := 0;
    context_value := m_left_context;
    if m_segment_left_context <> '' then
    begin
        context_value := m_segment_left_context;
    end;
    if (m_segment_left_context = '') and (m_external_left_context <> '') then
    begin
        context_value := m_external_left_context;
    end;

    if (candidate_text = '') or (context_value = '') then
    begin
        Exit;
    end;

    context_variants := get_context_variants(context_value);
    if Length(context_variants) = 0 then
    begin
        Exit;
    end;

    if m_context_pairs <> nil then
    begin
        for variant_idx := 0 to High(context_variants) do
        begin
            variant_weight := get_variant_weight(variant_idx);

            variant_key := context_variants[variant_idx] + #1 + candidate_text;
            if not m_context_pairs.TryGetValue(variant_key, count) then
            begin
                Continue;
            end;

            variant_bonus := count * c_context_score_bonus;
            if variant_bonus > c_context_score_bonus_max then
            begin
                variant_bonus := c_context_score_bonus_max;
            end;
            variant_bonus := (variant_bonus * variant_weight) div 100;
            local_bonus := merge_variant_bonus(local_bonus, variant_bonus);
        end;
    end;

    if (m_dictionary <> nil) and (m_context_db_bonus_cache <> nil) then
    begin
        if m_context_db_bonus_cache_key <> context_value then
        begin
            m_context_db_bonus_cache.Clear;
            m_context_db_bonus_cache_key := context_value;
        end;

        for variant_idx := 0 to High(context_variants) do
        begin
            variant_weight := get_variant_weight(variant_idx);

            variant_key := context_variants[variant_idx] + #1 + candidate_text;
            if not m_context_db_bonus_cache.TryGetValue(variant_key, variant_bonus) then
            begin
                variant_bonus := m_dictionary.get_context_bonus(context_variants[variant_idx], candidate_text);
                m_context_db_bonus_cache.AddOrSetValue(variant_key, variant_bonus);
            end;

            variant_bonus := (variant_bonus * variant_weight) div 100;
            persistent_bonus := merge_variant_bonus(persistent_bonus, variant_bonus);
        end;
    end;

    if local_bonus >= persistent_bonus then
    begin
        Result := local_bonus;
        secondary_bonus := persistent_bonus;
    end
    else
    begin
        Result := persistent_bonus;
        secondary_bonus := local_bonus;
    end;

    if secondary_bonus > 0 then
    begin
        Inc(Result, secondary_bonus div 2);
    end;
    if Result > c_context_combined_cap then
    begin
        Result := c_context_combined_cap;
    end;

    if (candidate_text <> '') and (m_lookup_text_context_bonus_cache <> nil) then
    begin
        m_lookup_text_context_bonus_cache.AddOrSetValue(candidate_text, Result);
    end;
end;

function TncEngine.get_context_bonus(const candidate_text: string): Integer;
const
    c_context_total_cap = 760;
    c_context_with_document_total_cap = 1480;
var
    text_context_bonus: Integer;
    phrase_context_bonus: Integer;
    context_query_bonus: Integer;
    context_query_latest_bonus: Integer;
    document_context_bonus: Integer;
    text_units: Integer;
begin
    if (candidate_text <> '') and (m_lookup_context_bonus_cache <> nil) and
        m_lookup_context_bonus_cache.TryGetValue(candidate_text, Result) then
    begin
        Exit;
    end;

    text_context_bonus := get_text_context_bonus(candidate_text);
    phrase_context_bonus := get_phrase_context_bonus(candidate_text);
    context_query_bonus := get_context_query_bonus(candidate_text);
    context_query_latest_bonus := get_context_query_latest_bonus(candidate_text);
    document_context_bonus := 0;
    if (m_document_context_model <> nil) and
        m_document_context_model.has_context then
    begin
        document_context_bonus := m_document_context_model.score_text(
            candidate_text);
    end;

    if text_context_bonus >= phrase_context_bonus then
    begin
        Result := text_context_bonus;
        if phrase_context_bonus > 0 then
        begin
            Inc(Result, phrase_context_bonus div 2);
        end;
    end
    else
    begin
        Result := phrase_context_bonus;
        if text_context_bonus > 0 then
        begin
            Inc(Result, text_context_bonus div 2);
        end;
    end;

    if context_query_bonus > 0 then
    begin
        Inc(Result, context_query_bonus);
        text_units := get_candidate_text_unit_count(candidate_text);
        if (m_last_lookup_syllable_count >= 2) and (text_units >= 2) then
        begin
            Inc(Result, Min(160, context_query_bonus div 3));
        end;
    end;
    if context_query_latest_bonus > 0 then
    begin
        Inc(Result, context_query_latest_bonus);
        if context_query_bonus > 0 then
        begin
            Inc(Result, Min(96, context_query_latest_bonus div 4));
        end;
    end;
    if document_context_bonus > 0 then
    begin
        Inc(Result, document_context_bonus);
    end;

    if (m_document_context_model <> nil) and
        m_document_context_model.has_context then
    begin
        if Result > c_context_with_document_total_cap then
        begin
            Result := c_context_with_document_total_cap;
        end;
    end
    else if Result > c_context_total_cap then
    begin
        Result := c_context_total_cap;
    end;

    if (candidate_text <> '') and (m_lookup_context_bonus_cache <> nil) then
    begin
        m_lookup_context_bonus_cache.AddOrSetValue(candidate_text, Result);
    end;
end;

function TncEngine.get_segment_path_preference_score(const encoded_path: string): Integer;
var
    normalized_path: string;
    path_penalty: Integer;
    prefix_penalty: Integer;
    recent_ranked_bonus: Integer;
    session_path_penalty: Integer;
    prefix_support: Integer;
    session_prefix_support: Integer;
    cached_result: Integer;
begin
    normalized_path := Trim(encoded_path);
    if get_encoded_path_segment_count_local(normalized_path) <= 1 then
    begin
        Exit(0);
    end;

    if (m_lookup_segment_path_preference_cache <> nil) and
        m_lookup_segment_path_preference_cache.TryGetValue(normalized_path, cached_result) then
    begin
        Exit(cached_result);
    end;

    Result := -(get_encoded_path_segment_count_local(normalized_path) * 18);
    if m_last_lookup_key <> '' then
    begin
        Inc(Result, get_session_query_path_bonus(m_last_lookup_key, normalized_path));
        session_path_penalty := get_session_query_path_penalty(m_last_lookup_key, normalized_path);
        if session_path_penalty > 0 then
        begin
            Dec(Result, session_path_penalty);
        end;
        session_prefix_support := get_session_query_path_prefix_bonus(m_last_lookup_key, normalized_path);
        if session_prefix_support > 0 then
        begin
            Inc(Result, session_prefix_support);
        end;
        recent_ranked_bonus := get_session_ranked_query_path_bonus(normalized_path);
        if recent_ranked_bonus > 0 then
        begin
            Inc(Result, recent_ranked_bonus);
        end;
        prefix_penalty := get_session_query_path_prefix_penalty(m_last_lookup_key, normalized_path);
        if prefix_penalty > 0 then
        begin
            Dec(Result, prefix_penalty);
        end;
        if m_dictionary <> nil then
        begin
            Inc(Result, get_cached_query_segment_path_bonus(
                m_last_lookup_key, normalized_path));
            path_penalty := get_cached_query_segment_path_penalty(
                m_last_lookup_key, normalized_path);
            if path_penalty > 0 then
            begin
                Dec(Result, path_penalty);
            end;
        end;
        prefix_support := get_persistent_query_path_prefix_support(normalized_path);
        if prefix_support <> 0 then
        begin
            Inc(Result, prefix_support);
        end;
    end;

    if m_lookup_segment_path_preference_cache <> nil then
    begin
        m_lookup_segment_path_preference_cache.AddOrSetValue(normalized_path, Result);
    end;
end;

function TncEngine.get_incremental_path_stability_bonus_for_path(const encoded_path: string): Integer;
const
    c_same_path_bonus = 96;
    c_extended_path_bonus = 240;
    c_single_step_extension_bonus = 40;
var
    current_query: string;
    previous_query: string;
begin
    Result := 0;
    current_query := normalize_pinyin_text(m_last_lookup_key);
    previous_query := normalize_pinyin_text(m_last_ranked_query_key);
    if (current_query = '') or (previous_query = '') or
        (current_query = previous_query) or
        (Length(current_query) <= Length(previous_query)) or
        (m_last_ranked_top_path = '') then
    begin
        Exit;
    end;

    if Copy(current_query, 1, Length(previous_query)) <> previous_query then
    begin
        Exit;
    end;

    if Length(current_query) - Length(previous_query) > 8 then
    begin
        Exit;
    end;

    if get_encoded_path_segment_count_local(encoded_path) <= 1 then
    begin
        Exit;
    end;

    if encoded_path = m_last_ranked_top_path then
    begin
        Result := c_same_path_bonus;
    end
    else if Copy(encoded_path, 1, Length(m_last_ranked_top_path) + 1) =
        (m_last_ranked_top_path + c_segment_path_separator) then
    begin
        Result := c_extended_path_bonus;
    end;

    if (Result > 0) and (Length(current_query) = Length(previous_query) + 1) then
    begin
        Inc(Result, c_single_step_extension_bonus);
    end;
end;

function TncEngine.get_incremental_path_stability_bonus(const candidate: TncCandidate): Integer;
var
    candidate_path: string;
begin
    Result := 0;
    if candidate.comment <> '' then
    begin
        Exit;
    end;

    candidate_path := get_segment_path_for_candidate(candidate);
    Result := get_incremental_path_stability_bonus_for_path(candidate_path);
end;

function TncEngine.get_segment_path_support_score(const candidate: TncCandidate): Integer;
var
    encoded_path: string;
    preference_score: Integer;
    stability_bonus: Integer;
begin
    Result := get_segment_path_context_bonus(candidate);
    encoded_path := get_segment_path_for_candidate(candidate);
    if encoded_path = '' then
    begin
        Exit;
    end;

    preference_score := get_segment_path_preference_score(encoded_path);
    if preference_score > 0 then
    begin
        Inc(Result, Min(260, preference_score div 2));
    end
    else if preference_score < 0 then
    begin
        Dec(Result, Min(180, Abs(preference_score) div 3));
    end;

    stability_bonus := get_incremental_path_stability_bonus(candidate);
    if stability_bonus > 0 then
    begin
        Inc(Result, stability_bonus);
    end;
end;

procedure TncEngine.get_recent_path_context_seed(out prev_prev_text: string; out prev_text: string);
var
    context_value: string;
    segment_count: Integer;
begin
    prev_prev_text := '';
    prev_text := '';

    context_value := Trim(m_segment_left_context);
    if context_value = '' then
    begin
        context_value := Trim(m_external_left_context);
    end;
    if context_value = '' then
    begin
        context_value := Trim(m_left_context);
    end;

    if (m_confirmed_segments <> nil) and (m_confirmed_segments.Count > 0) then
    begin
        segment_count := m_confirmed_segments.Count;
        prev_text := Trim(m_confirmed_segments[segment_count - 1].text);
        if segment_count >= 2 then
        begin
            prev_prev_text := Trim(m_confirmed_segments[segment_count - 2].text);
        end
        else if context_value <> '' then
        begin
            prev_prev_text := context_value;
        end;
        Exit;
    end;

    if context_value <> '' then
    begin
        prev_text := context_value;
    end;
end;

function TncEngine.get_segment_path_context_bonus(const candidate: TncCandidate): Integer;
var
    cache_key: string;
    encoded_path: string;
    path_segments: TArray<string>;
    segment_start: Integer;
    idx: Integer;
    segment_count: Integer;
    seed_prev_prev_text: string;
    seed_prev_text: string;
    current_prev_prev_text: string;
    current_prev_text: string;
    transition_bonus: Integer;
    path_penalty: Integer;
    session_path_penalty: Integer;
    prefix_support: Integer;
    session_prefix_support: Integer;
    session_prefix_penalty: Integer;
    document_path_bonus: Integer;

    function get_exact_context_pair_bonus(const left_text: string; const candidate_text: string): Integer;
    const
        c_segment_pair_context_cap = 520;
        c_segment_pair_bonus_scale = 75;
    var
        pair_key: string;
        local_bonus: Integer;
        persistent_bonus: Integer;
        count: Integer;
        secondary_bonus: Integer;
    begin
        Result := 0;
        if (left_text = '') or (candidate_text = '') then
        begin
            Exit;
        end;

        local_bonus := 0;
        persistent_bonus := 0;
        if m_context_pairs <> nil then
        begin
            pair_key := left_text + #1 + candidate_text;
            if m_context_pairs.TryGetValue(pair_key, count) and (count > 0) then
            begin
                local_bonus := count * c_context_score_bonus;
                if local_bonus > c_context_score_bonus_max then
                begin
                    local_bonus := c_context_score_bonus_max;
                end;
            end;
        end;

        if m_dictionary <> nil then
        begin
            persistent_bonus := m_dictionary.get_context_bonus(left_text, candidate_text);
        end;

        if local_bonus >= persistent_bonus then
        begin
            Result := local_bonus;
            secondary_bonus := persistent_bonus;
        end
        else
        begin
            Result := persistent_bonus;
            secondary_bonus := local_bonus;
        end;

        if secondary_bonus > 0 then
        begin
            Inc(Result, secondary_bonus div 2);
        end;
        if Result > c_segment_pair_context_cap then
        begin
            Result := c_segment_pair_context_cap;
        end;
        Result := (Result * c_segment_pair_bonus_scale) div 100;
    end;

    function get_phrase_trigram_transition_bonus(const prev_prev_text: string;
        const prev_text: string; const candidate_text: string): Integer;
    const
        c_segment_trigram_step = 96;
        c_segment_trigram_cap = 300;
        c_segment_trigram_recent_top = 140;
        c_segment_trigram_recent_mid = 84;
        c_segment_trigram_recent_tail = 40;
    var
        trigram_key: string;
        trigram_count: Integer;
        last_seen_serial: Int64;
        serial_gap: Int64;
    begin
        Result := 0;
        if (prev_prev_text = '') or (prev_text = '') or (candidate_text = '') or
            (m_phrase_context_pairs = nil) then
        begin
            Exit;
        end;

        trigram_key := prev_prev_text + #2 + prev_text + #1 + candidate_text;
        if (not m_phrase_context_pairs.TryGetValue(trigram_key, trigram_count)) or (trigram_count <= 0) then
        begin
            Exit;
        end;

        Result := trigram_count * c_segment_trigram_step;
        if Result > c_segment_trigram_cap then
        begin
            Result := c_segment_trigram_cap;
        end;

        if (m_phrase_context_last_seen <> nil) and
            m_phrase_context_last_seen.TryGetValue(trigram_key, last_seen_serial) and
            (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
        begin
            serial_gap := m_session_commit_serial - last_seen_serial;
            if serial_gap <= 1 then
            begin
                Inc(Result, c_segment_trigram_recent_top);
            end
            else if serial_gap <= 3 then
            begin
                Inc(Result, c_segment_trigram_recent_mid);
            end
            else if serial_gap <= 6 then
            begin
                Inc(Result, c_segment_trigram_recent_tail);
            end;
        end;
    end;

    function get_compound_prev_trigram_bonus(const combined_prev_text: string;
        const candidate_text: string): Integer;
    var
        combined_units: TArray<string>;
        split_idx: Integer;
        unit_idx: Integer;
        left_part: string;
        right_part: string;
        local_session_bonus: Integer;
        local_persistent_bonus: Integer;
        local_secondary_bonus: Integer;
        local_bonus: Integer;
    begin
        Result := 0;
        if (combined_prev_text = '') or (candidate_text = '') then
        begin
            Exit;
        end;

        combined_units := split_text_units(combined_prev_text);
        if Length(combined_units) < 2 then
        begin
            Exit;
        end;

        for split_idx := 1 to High(combined_units) do
        begin
            left_part := '';
            for unit_idx := 0 to split_idx - 1 do
            begin
                left_part := left_part + combined_units[unit_idx];
            end;

            right_part := '';
            for unit_idx := split_idx to High(combined_units) do
            begin
                right_part := right_part + combined_units[unit_idx];
            end;

            if (left_part = '') or (right_part = '') then
            begin
                Continue;
            end;

            local_session_bonus := get_phrase_trigram_transition_bonus(
                left_part, right_part, candidate_text);
            local_persistent_bonus := 0;
            if m_dictionary <> nil then
            begin
                local_persistent_bonus := m_dictionary.get_context_trigram_bonus(
                    left_part, right_part, candidate_text);
            end;

            if local_session_bonus >= local_persistent_bonus then
            begin
                local_bonus := local_session_bonus;
                local_secondary_bonus := local_persistent_bonus;
            end
            else
            begin
                local_bonus := local_persistent_bonus;
                local_secondary_bonus := local_session_bonus;
            end;

            if local_secondary_bonus > 0 then
            begin
                Inc(local_bonus, local_secondary_bonus div 2);
            end;

            if local_bonus > Result then
            begin
                Result := local_bonus;
            end;
        end;
    end;

    function get_path_transition_bonus(const prev_prev_text: string; const prev_text: string;
        const candidate_text: string): Integer;
    var
        pair_bonus: Integer;
        trigram_bonus: Integer;
        persistent_trigram_bonus: Integer;
        compound_prev_trigram_bonus: Integer;
        secondary_bonus: Integer;
    begin
        Result := 0;
        if (prev_text = '') or (candidate_text = '') then
        begin
            Exit;
        end;

        pair_bonus := get_exact_context_pair_bonus(prev_text, candidate_text);
        trigram_bonus := get_phrase_trigram_transition_bonus(prev_prev_text, prev_text, candidate_text);
        persistent_trigram_bonus := 0;
        if (prev_prev_text <> '') and (m_dictionary <> nil) then
        begin
            persistent_trigram_bonus := m_dictionary.get_context_trigram_bonus(
                prev_prev_text, prev_text, candidate_text);
        end;
        compound_prev_trigram_bonus := 0;
        if prev_prev_text = '' then
        begin
            compound_prev_trigram_bonus := get_compound_prev_trigram_bonus(prev_text, candidate_text);
        end;

        if trigram_bonus >= persistent_trigram_bonus then
        begin
            Result := trigram_bonus;
            secondary_bonus := persistent_trigram_bonus;
        end
        else
        begin
            Result := persistent_trigram_bonus;
            secondary_bonus := trigram_bonus;
        end;

        if secondary_bonus > 0 then
        begin
            Inc(Result, secondary_bonus div 2);
        end;
        if compound_prev_trigram_bonus > Result then
        begin
            Result := compound_prev_trigram_bonus;
        end;

        if Result > 0 then
        begin
            Inc(Result, pair_bonus div 2);
        end
        else
        begin
            Result := pair_bonus;
        end;
    end;
begin
    Result := 0;
    if m_dictionary = nil then
    begin
        Exit;
    end;

    if (m_last_lookup_syllable_count >= 5) and (candidate.comment = '') and
        candidate.has_dict_weight then
    begin
        Exit;
    end;

    if is_nonlexicon_long_segment_path_candidate(candidate) then
    begin
        Exit;
    end;

    encoded_path := get_segment_path_for_candidate(candidate);
    if (encoded_path = '') or (get_encoded_path_segment_count_local(encoded_path) <= 1) then
    begin
        Exit;
    end;

    cache_key := encoded_path;
    if candidate.comment <> '' then
    begin
        cache_key := 'P' + #1 + cache_key;
        if is_runtime_chain_candidate(candidate) then
        begin
            cache_key := cache_key + #1 + 'C';
        end;
    end;

    if (m_lookup_segment_path_context_bonus_cache <> nil) and
        m_lookup_segment_path_context_bonus_cache.TryGetValue(cache_key, Result) then
    begin
        Exit;
    end;

    SetLength(path_segments, 0);
    segment_start := 1;
    for idx := 1 to Length(encoded_path) + 1 do
    begin
        if (idx <= Length(encoded_path)) and (encoded_path[idx] <> c_segment_path_separator) then
        begin
            Continue;
        end;

        if idx > segment_start then
        begin
            segment_count := Length(path_segments);
            SetLength(path_segments, segment_count + 1);
            path_segments[segment_count] := Copy(encoded_path, segment_start, idx - segment_start);
        end;
        segment_start := idx + 1;
    end;

    if Length(path_segments) <= 0 then
    begin
        Exit;
    end;

    get_recent_path_context_seed(seed_prev_prev_text, seed_prev_text);
    current_prev_prev_text := seed_prev_prev_text;
    current_prev_text := seed_prev_text;

    for idx := 0 to High(path_segments) do
    begin
        transition_bonus := get_path_transition_bonus(current_prev_prev_text, current_prev_text, path_segments[idx]);
        if transition_bonus > 0 then
        begin
            Inc(Result, transition_bonus);
        end;
        current_prev_prev_text := current_prev_text;
        current_prev_text := path_segments[idx];
    end;

    if m_last_lookup_key <> '' then
    begin
        transition_bonus := get_session_query_path_bonus(m_last_lookup_key, encoded_path);
        if transition_bonus > 0 then
        begin
            Inc(Result, transition_bonus);
        end;
        session_path_penalty := get_session_query_path_penalty(m_last_lookup_key, encoded_path);
        if session_path_penalty > 0 then
        begin
            Dec(Result, session_path_penalty);
        end;
        session_prefix_support := get_session_query_path_prefix_bonus(m_last_lookup_key, encoded_path);
        if session_prefix_support > 0 then
        begin
            Inc(Result, session_prefix_support);
        end;
        session_prefix_penalty := get_session_query_path_prefix_penalty(m_last_lookup_key, encoded_path);
        if session_prefix_penalty > 0 then
        begin
            Dec(Result, session_prefix_penalty);
        end;
        if m_dictionary <> nil then
        begin
            transition_bonus := get_cached_query_segment_path_bonus(
                m_last_lookup_key, encoded_path);
            if transition_bonus > 0 then
            begin
                Inc(Result, transition_bonus);
            end;
            path_penalty := get_cached_query_segment_path_penalty(
                m_last_lookup_key, encoded_path);
            if path_penalty > 0 then
            begin
                Dec(Result, path_penalty);
            end;
        end;
    end;
        prefix_support := get_persistent_query_path_prefix_support(encoded_path);
        if prefix_support <> 0 then
        begin
            Inc(Result, prefix_support);
        end;

    document_path_bonus := 0;
    if (m_document_context_model <> nil) and
        m_document_context_model.has_context then
    begin
        document_path_bonus := m_document_context_model.score_path(
            encoded_path, c_segment_path_separator);
    end;
    if document_path_bonus > 0 then
    begin
        Inc(Result, document_path_bonus);
    end;

    if candidate.comment <> '' then
    begin
        if is_runtime_chain_candidate(candidate) then
        begin
            Result := Result div 4;
        end
        else
        begin
            Result := (Result * 5) div 8;
            if get_candidate_text_unit_count(candidate.text) >= 2 then
            begin
                Inc(Result, Min(72, Result div 7));
            end;
        end;
    end;

    if m_lookup_segment_path_context_bonus_cache <> nil then
    begin
        m_lookup_segment_path_context_bonus_cache.AddOrSetValue(cache_key, Result);
    end;
end;

function TncEngine.get_punctuation_char(const key_code: Word; const key_state: TncKeyState; out out_char: Char): Boolean;
begin
    Result := True;
    case key_code of
        Ord('1'):
            if key_state.shift_down then
            begin
                out_char := '!';
            end
            else
            begin
                Result := False;
            end;
        Ord('2'):
            if key_state.shift_down then
            begin
                out_char := '@';
            end
            else
            begin
                Result := False;
            end;
        Ord('3'):
            if key_state.shift_down then
            begin
                out_char := '#';
            end
            else
            begin
                Result := False;
            end;
        Ord('4'):
            if key_state.shift_down then
            begin
                out_char := '$';
            end
            else
            begin
                Result := False;
            end;
        Ord('5'):
            if key_state.shift_down then
            begin
                out_char := '%';
            end
            else
            begin
                Result := False;
            end;
        Ord('6'):
            if key_state.shift_down then
            begin
                out_char := '^';
            end
            else
            begin
                Result := False;
            end;
        Ord('7'):
            if key_state.shift_down then
            begin
                out_char := '&';
            end
            else
            begin
                Result := False;
            end;
        Ord('8'):
            if key_state.shift_down then
            begin
                out_char := '*';
            end
            else
            begin
                Result := False;
            end;
        Ord('9'):
            if key_state.shift_down then
            begin
                out_char := '(';
            end
            else
            begin
                Result := False;
            end;
        Ord('0'):
            if key_state.shift_down then
            begin
                out_char := ')';
            end
            else
            begin
                Result := False;
            end;
        VK_OEM_COMMA:
            if key_state.shift_down then
            begin
                out_char := '<';
            end
            else
            begin
                out_char := ',';
            end;
        VK_OEM_PERIOD:
            if key_state.shift_down then
            begin
                out_char := '>';
            end
            else
            begin
                out_char := '.';
            end;
        VK_OEM_1:
            if key_state.shift_down then
            begin
                out_char := ':';
            end
            else
            begin
                out_char := ';';
            end;
        VK_OEM_2:
            if key_state.shift_down then
            begin
                out_char := '?';
            end
            else
            begin
                out_char := '/';
            end;
        VK_OEM_3:
            if key_state.shift_down then
            begin
                out_char := '~';
            end
            else
            begin
                out_char := '`';
            end;
        VK_OEM_4:
            if key_state.shift_down then
            begin
                out_char := '{';
            end
            else
            begin
                out_char := '[';
            end;
        VK_OEM_5:
            if key_state.shift_down then
            begin
                out_char := '|';
            end
            else
            begin
                out_char := '\';
            end;
        VK_OEM_6:
            if key_state.shift_down then
            begin
                out_char := '}';
            end
            else
            begin
                out_char := ']';
            end;
        VK_OEM_7:
            if key_state.shift_down then
            begin
                out_char := '"';
            end
            else
            begin
                out_char := '''';
            end;
        VK_OEM_MINUS:
            if key_state.shift_down then
            begin
                out_char := '_';
            end
            else
            begin
                out_char := '-';
            end;
        VK_OEM_PLUS:
            if key_state.shift_down then
            begin
                out_char := '+';
            end
            else
            begin
                out_char := '=';
            end;
        VK_ADD:
            out_char := '+';
        VK_SUBTRACT:
            out_char := '-';
        VK_MULTIPLY:
            out_char := '*';
    else
        Result := False;
    end;
end;

function TncEngine.map_full_width_char(const input_char: Char): string;
begin
    if input_char = ' ' then
    begin
        Result := Char($3000);
        Exit;
    end;

    if (input_char >= '!') and (input_char <= '~') then
    begin
        Result := Char(Ord(input_char) + c_full_width_offset);
        Exit;
    end;

    Result := input_char;
end;

function TncEngine.map_punctuation_char(const input_char: Char): string;
begin
    if get_effective_punctuation_full_width then
    begin
        case input_char of
            ',':
                Result := Char($FF0C);
            '.':
                Result := Char($3002);
            '?':
                Result := Char($FF1F);
            '!':
                Result := Char($FF01);
            '@':
                Result := Char($FF20);
            '#':
                Result := Char($FF03);
            '$':
                Result := Char($FFE5);
            '%':
                Result := Char($FF05);
            '&':
                Result := Char($FF06);
            '*':
                Result := Char($FF0A);
            ':':
                Result := Char($FF1A);
            ';':
                Result := Char($FF1B);
            '/':
                Result := input_char;
            '\':
                Result := Char($3001);
            '(':
                Result := Char($FF08);
            ')':
                Result := Char($FF09);
            '[':
                Result := Char($3010);
            ']':
                Result := Char($3011);
            '{':
                Result := Char($300E);
            '}':
                Result := Char($300F);
            '<':
                Result := Char($300A);
            '>':
                Result := Char($300B);
            '''':
                begin
                    if m_single_quote_open then
                    begin
                        Result := Char($2019);
                    end
                    else
                    begin
                        Result := Char($2018);
                    end;
                    m_single_quote_open := not m_single_quote_open;
                end;
            '"':
                begin
                    if m_double_quote_open then
                    begin
                        Result := Char($201D);
                    end
                    else
                    begin
                        Result := Char($201C);
                    end;
                    m_double_quote_open := not m_double_quote_open;
                end;
            '`':
                Result := Char($00B7);
            '~':
                Result := Char($FF5E);
            '^':
                Result := Char($2026) + Char($2026);
            '-':
                Result := Char($FF0D);
            '_':
                Result := Char($2014) + Char($2014);
            '=':
                Result := Char($FF1D);
            '+':
                Result := Char($FF0B);
        else
            if m_config.full_width_mode then
            begin
                Result := map_full_width_char(input_char);
            end
            else
            begin
                Result := input_char;
            end;
        end;
        Exit;
    end;

    if m_config.full_width_mode then
    begin
        Result := map_full_width_char(input_char);
        Exit;
    end;

    Result := input_char;
end;

function TncEngine.get_effective_punctuation_full_width: Boolean;
begin
    Result := (m_config.input_mode <> im_english) and m_config.punctuation_full_width;
end;

function TncEngine.get_direct_ascii_commit_text(const key_code: Word; const key_state: TncKeyState;
    out out_text: string): Boolean;
var
    key_char: Char;
    display_key_char: Char;
    punct_char: Char;
begin
    out_text := '';

    if m_config.full_width_mode and is_alpha_key(key_code, key_state, key_char, display_key_char) then
    begin
        out_text := map_full_width_char(display_key_char);
        Exit(True);
    end;

    if m_config.full_width_mode and (key_code >= Ord('0')) and (key_code <= Ord('9')) and
        (not key_state.shift_down) then
    begin
        out_text := map_full_width_char(Char(key_code));
        Exit(True);
    end;

    if m_config.full_width_mode and (key_code = VK_SPACE) then
    begin
        out_text := map_full_width_char(' ');
        Exit(True);
    end;

    if (get_effective_punctuation_full_width or m_config.full_width_mode) and
        get_punctuation_char(key_code, key_state, punct_char) then
    begin
        if (key_code = VK_ADD) or (key_code = VK_SUBTRACT) or
            (key_code = VK_MULTIPLY) then
        begin
            out_text := punct_char;
        end
        else
        begin
            out_text := map_punctuation_char(punct_char);
        end;
        Exit(True);
    end;

    Result := False;
end;

function TncEngine.get_raw_composition_commit_text: string;
var
    source_text: string;
    idx: Integer;
begin
    if m_composition_display_text <> '' then
    begin
        source_text := m_composition_display_text;
    end
    else
    begin
        source_text := m_composition_text;
    end;

    if (not m_config.full_width_mode) or (source_text = '') then
    begin
        Exit(source_text);
    end;

    Result := '';
    SetLength(Result, 0);
    for idx := 1 to Length(source_text) do
    begin
        Result := Result + map_full_width_char(source_text[idx]);
    end;
end;

function TncEngine.get_rank_score(const candidate: TncCandidate): Integer;
var
    context_bonus: Integer;
    segment_path_context_bonus: Integer;
    session_bonus: Integer;
    query_bonus: Integer;
    text_units: Integer;
    syllable_gap: Integer;
    is_single_syllable_single_char_lookup: Boolean;

    function get_same_span_competitor_path_penalty_local: Integer;
        var
            encoded_path_local: string;
            syllables_local: TncPinyinParseResult;
            parts_local: TArray<string>;
            part_idx_local: Integer;
            pos_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            segment_weight_local: Integer;
            best_weight_local: Integer;
            weight_gap_local: Integer;

        function build_span_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            key_idx_local: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;
            for key_idx_local := start_idx_local to
                start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[key_idx_local].text);
            end;
        end;

        function get_exact_weight_local(const start_idx_local: Integer;
            const count_local: Integer; const expected_text_local: string;
            out out_weight_local: Integer): Boolean;
        var
            key_local: string;
            results_local: TncCandidateList;
            idx_local: Integer;
            text_local: string;
            weight_local: Integer;
        begin
            Result := False;
            out_weight_local := Low(Integer);
            key_local := build_span_key_local(start_idx_local, count_local);
            if (key_local = '') or
                (not m_dictionary.lookup(key_local, results_local)) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(results_local) do
            begin
                if idx_local >= 12 then
                begin
                    Break;
                end;
                if Trim(results_local[idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                text_local := Trim(results_local[idx_local].text);
                if (text_local = '') or
                    (get_candidate_text_unit_count(text_local) <> count_local) or
                    ((expected_text_local <> '') and
                    (not SameText(text_local, expected_text_local))) then
                begin
                    Continue;
                end;
                weight_local := results_local[idx_local].score;
                if (not Result) or (weight_local > out_weight_local) then
                begin
                    Result := True;
                    out_weight_local := weight_local;
                end;
            end;
        end;

    begin
        Result := 0;
        if (m_dictionary = nil) or (m_last_lookup_syllable_count < 3) or
            (m_last_lookup_syllable_count > 12) or
            (candidate.comment <> '') then
        begin
            Exit;
        end;

        encoded_path_local := get_segment_path_for_candidate(candidate);
        if encoded_path_local = '' then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            m_last_lookup_key);
        if Length(syllables_local) <> m_last_lookup_syllable_count then
        begin
            Exit;
        end;

        parts_local := encoded_path_local.Split([c_segment_path_separator]);
        pos_local := 0;
        for part_idx_local := 0 to High(parts_local) do
        begin
            segment_text_local := Trim(parts_local[part_idx_local]);
            segment_units_local := get_candidate_text_unit_count(
                segment_text_local);
            if (segment_units_local <= 0) or
                (pos_local + segment_units_local > Length(syllables_local)) then
            begin
                Exit;
            end;

            if get_exact_weight_local(pos_local, segment_units_local,
                segment_text_local, segment_weight_local) and
                get_exact_weight_local(pos_local, segment_units_local, '',
                best_weight_local) then
            begin
                weight_gap_local := best_weight_local - segment_weight_local;
                if weight_gap_local >= 240 then
                begin
                    Inc(Result, Min(220000, 20000 + weight_gap_local * 120));
                end;
            end;

            Inc(pos_local, segment_units_local);
        end;
    end;
begin
    Result := candidate.score;
    if (m_dictionary <> nil) and (m_last_lookup_key <> '') and
        (candidate.comment = '') and
        is_low_evidence_admin_place_alias_cached(
        m_last_lookup_key, candidate.text) then
    begin
        Dec(Result, 3000);
    end;
    text_units := get_candidate_text_unit_count(candidate.text);
    is_single_syllable_single_char_lookup := (m_last_lookup_syllable_count = 1) and
        (candidate.comment = '') and (text_units = 1);
    context_bonus := get_context_bonus(candidate.text);
    if m_last_lookup_normalized_from <> '' then
    begin
        // When we auto-correct a likely adjacent-swap typo (e.g. chagn->chang),
        // reduce context influence so lexical score dominates.
        context_bonus := context_bonus div 4;
    end;
    if is_single_syllable_single_char_lookup and (context_bonus < 160) then
    begin
        context_bonus := 0;
    end;
    Inc(Result, context_bonus);
    segment_path_context_bonus := get_segment_path_context_bonus(candidate);
    if is_single_syllable_single_char_lookup and (segment_path_context_bonus < 200) then
    begin
        segment_path_context_bonus := 0;
    end;
    Inc(Result, segment_path_context_bonus);
    query_bonus := get_session_query_bonus(candidate.text);
    if candidate.comment <> '' then
    begin
        query_bonus := query_bonus div 2;
    end;
    Inc(Result, query_bonus);
    session_bonus := get_session_text_bonus(candidate.text);
    if candidate.comment <> '' then
    begin
        session_bonus := session_bonus div 2;
    end;
    if is_single_syllable_single_char_lookup and (session_bonus < 200) then
    begin
        session_bonus := 0;
    end;
    Inc(Result, session_bonus);

    // For one-syllable full-pinyin lookups, keep single-char candidates ahead.
    if (m_last_lookup_syllable_count = 1) and (candidate.comment = '') then
    begin
        if text_units > 1 then
        begin
            Dec(Result, (text_units - 1) * 180);
        end;
    end;

    // For multi-syllable lookups, rank candidates by intent layers:
    // complete phrases > near-complete phrase chunks > shorter chunks >
    // single-char fallbacks. This keeps long input usable even when the raw
    // lexicon contains high-weight noisy short heads.
    if (m_last_lookup_syllable_count >= 3) and (candidate.comment = '') then
    begin
        text_units := get_candidate_text_unit_count(candidate.text);
        if text_units >= 2 then
        begin
            syllable_gap := m_last_lookup_syllable_count - text_units;
            if syllable_gap = 0 then
            begin
                Inc(Result, 520 + (text_units * 24));
            end;
            if syllable_gap = 1 then
            begin
                Inc(Result, 260);
            end
            else if syllable_gap < 0 then
            begin
                Dec(Result, Abs(syllable_gap) * 120);
            end
            else
            begin
                Dec(Result, syllable_gap * 220);
            end;

            if (m_last_lookup_syllable_count >= 4) and (text_units >= 3) and
                (text_units + 1 >= m_last_lookup_syllable_count) then
            begin
                Inc(Result, 80);
            end;
        end
        else
        begin
            Dec(Result, 620);
        end;

        if is_runtime_chain_candidate(candidate) then
        begin
            if (context_bonus + session_bonus) <= 0 then
            begin
                Dec(Result, 160);
            end;
            if m_last_lookup_syllable_count >= 4 then
            begin
                Dec(Result, (m_last_lookup_syllable_count - 3) * 90);
            end;
        end
        else if (candidate.source = cs_rule) and (not candidate.has_dict_weight) and
            (not is_runtime_common_pattern_candidate(candidate)) and
            (not is_runtime_redup_candidate(candidate)) then
        begin
            if (context_bonus + session_bonus) <= 0 then
            begin
                Dec(Result, 120);
            end;
            if text_units <= 2 then
            begin
                Dec(Result, 160);
            end;
        end;
    end;

    if candidate.comment <> '' then
    begin
        if m_last_lookup_syllable_count >= 3 then
        begin
            text_units := get_candidate_text_unit_count(candidate.text);
            if text_units >= 2 then
            begin
                if text_units + 1 >= m_last_lookup_syllable_count then
                begin
                    Inc(Result, 340);
                end
                else if text_units + 2 >= m_last_lookup_syllable_count then
                begin
                    Inc(Result, 120);
                end
                else
                begin
                    Dec(Result, 180 + ((m_last_lookup_syllable_count - text_units - 2) * 140));
                end;
            end
            else
            begin
                Dec(Result, 760);
            end;
        end;

        // Segment fallback candidates with remaining pinyin (e.g. "... ti")
        // should stay available but rank below complete phrase candidates.
        Dec(Result, c_partial_candidate_score_penalty);
    end;

    // Generic runtime chains with productive/friendly tails (e.g. ...锟?锟?锟? are
    // useful fallback phrases, but when an exact lexicon phrase already competes for
    // the same query they should stop behaving like the default top result.
    Dec(Result, get_same_span_competitor_path_penalty_local);
    Inc(Result, get_front_row_confidence_bonus(candidate));

    case candidate.source of
        cs_user:
            Inc(Result, c_user_score_bonus);
    end;
end;

function TncEngine.match_single_char_candidate_for_syllable(const syllable_text: string;
    const unit_text: string; out out_preferred: Boolean): Boolean;
const
    c_partial_preferred_min_weight = 120;
    c_partial_preferred_max_rank = 4;
    c_partial_preferred_top_ratio_pct = 85;
var
    results: TncCandidateList;
    idx: Integer;
    candidate_text: string;
    normalized_syllable: string;
    normalized_unit: string;
    weight_value: Integer;
    top_weight_value: Integer;
    match_rank: Integer;
    single_char_rank: Integer;
    cache_key: string;
    lookup_cache_key: string;
    cached_match_value: Integer;

    function finish_with_cache(const matched: Boolean;
        const preferred: Boolean): Boolean;
    var
        cache_value: Integer;
    begin
        out_preferred := preferred;
        if matched then
        begin
            if preferred then
            begin
                cache_value := 2;
            end
            else
            begin
                cache_value := 1;
            end;
        end
        else
        begin
            cache_value := 0;
        end;
        if (cache_key <> '') and (m_lookup_single_char_match_cache <> nil) then
        begin
            m_lookup_single_char_match_cache.AddOrSetValue(cache_key,
                cache_value);
        end;
        Result := matched;
    end;
begin
    Result := False;
    out_preferred := False;
    normalized_syllable := Trim(syllable_text);
    normalized_unit := Trim(unit_text);
    if (m_dictionary = nil) or (normalized_syllable = '') or (normalized_unit = '') then
    begin
        Exit;
    end;
    if get_candidate_text_unit_count(unit_text) <> 1 then
    begin
        Exit;
    end;

    cache_key := normalize_pinyin_text(normalized_syllable) + #9 + normalized_unit;
    if (m_lookup_single_char_match_cache <> nil) and
        m_lookup_single_char_match_cache.TryGetValue(cache_key,
        cached_match_value) then
    begin
        out_preferred := cached_match_value = 2;
        Exit(cached_match_value > 0);
    end;

    // For explicit apostrophe alignment, exact single-char existence must not
    // depend on whether the char happens to appear inside the short lookup
    // shortlist for this syllable. Queries like "ji'an"/"ji'e" should still
    // preserve complete phrases such as "瀛ｈ兒/鍚夊畨/楗ラタ" even when "锟?锟?锟?锟?
    // are not near the top of the bare single-char bucket.
    if not m_dictionary.single_char_matches_pinyin(normalized_syllable, normalized_unit) then
    begin
        Exit(finish_with_cache(False, False));
    end;

    lookup_cache_key := '#singlelookup#' + normalize_pinyin_text(normalized_syllable);
    if (m_build_lookup_cache <> nil) and
        m_build_lookup_cache.TryGetValue(lookup_cache_key, results) then
    begin
        if Length(results) = 0 then
        begin
            Exit(finish_with_cache(True, False));
        end;
    end
    else
    begin
        if not m_dictionary.lookup(normalized_syllable, results) then
        begin
            SetLength(results, 0);
            if m_build_lookup_cache <> nil then
            begin
                m_build_lookup_cache.AddOrSetValue(lookup_cache_key, results);
            end;
            Exit(finish_with_cache(True, False));
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(lookup_cache_key, results);
        end;
    end;
    if Length(results) = 0 then
    begin
        Exit(finish_with_cache(True, False));
    end;

    top_weight_value := 0;
    match_rank := 0;
    single_char_rank := 0;
    for idx := 0 to High(results) do
    begin
        candidate_text := Trim(results[idx].text);
        if (candidate_text = '') or (get_candidate_text_unit_count(candidate_text) <> 1) then
        begin
            Continue;
        end;

        Inc(single_char_rank);
        if results[idx].has_dict_weight then
        begin
            weight_value := results[idx].dict_weight;
        end
        else
        begin
            weight_value := results[idx].score;
        end;
        if top_weight_value <= 0 then
        begin
            top_weight_value := weight_value;
        end;
        if not SameText(candidate_text, normalized_unit) then
        begin
            Continue;
        end;

        if match_rank = 0 then
        begin
            match_rank := single_char_rank;
        end;
        out_preferred := (weight_value >= c_partial_preferred_min_weight) and
            ((match_rank <= c_partial_preferred_max_rank) or
            (top_weight_value <= 0) or
            ((weight_value * 100) >= (top_weight_value * c_partial_preferred_top_ratio_pct)));
        Exit(finish_with_cache(True, out_preferred));
    end;

    Result := finish_with_cache(True, False);
end;

function TncEngine.is_weak_single_char_chain_candidate_for_query(const query_key: string;
    const candidate: TncCandidate): Boolean;
var
    syllables: TncPinyinParseResult;
    text_units: TArray<string>;
    normalized_query: string;
    idx: Integer;
    is_preferred: Boolean;
    has_weak_unit: Boolean;
begin
    Result := False;
    if candidate.comment <> '' then
    begin
        Exit;
    end;

    normalized_query := normalize_pinyin_text(query_key);
    if normalized_query = '' then
    begin
        normalized_query := normalize_pinyin_text(m_composition_text);
    end;
    if normalized_query = '' then
    begin
        Exit;
    end;

    syllables := get_effective_compact_pinyin_syllables(normalized_query);

    if Length(syllables) < 2 then
    begin
        Exit;
    end;

    text_units := split_text_units(Trim(candidate.text));
    if Length(text_units) <> Length(syllables) then
    begin
        Exit;
    end;

    has_weak_unit := False;
    for idx := 0 to High(text_units) do
    begin
        if not match_single_char_candidate_for_syllable(syllables[idx].text, text_units[idx], is_preferred) then
        begin
            Exit(False);
        end;
        if not is_preferred then
        begin
            has_weak_unit := True;
        end;
    end;

    Result := has_weak_unit;
end;

function TncEngine.is_problematic_single_char_chain_candidate_for_query(const query_key: string;
    const candidate: TncCandidate): Boolean;
const
    c_low_evidence_user_chain_weight = 8;
    c_explicit_query_choice_min_bonus = 200;
var
    syllables: TncPinyinParseResult;
    text_units: TArray<string>;
    path_text: string;
    path_segments: TArray<string>;
    normalized_query: string;
    idx: Integer;
    is_preferred: Boolean;
    is_low_evidence_user_chain: Boolean;
    function build_span_query_key_local(const start_idx: Integer;
        const span_len: Integer): string;
    var
        local_idx: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (span_len <= 0) or
            (start_idx + span_len - 1 > High(syllables)) then
        begin
            Exit;
        end;

        for local_idx := start_idx to start_idx + span_len - 1 do
        begin
            Result := Result + normalize_pinyin_text(syllables[local_idx].text);
        end;
    end;
    function get_candidate_lookup_weight_local(const value: TncCandidate): Integer;
    begin
        Result := value.score;
        if value.has_dict_weight and (value.dict_weight > Result) then
        begin
            Result := value.dict_weight;
        end;
        if Result < 0 then
        begin
            Result := 0;
        end;
    end;
    function try_get_best_exact_span_text_local(const start_idx: Integer;
        const span_len: Integer; out out_text: string; out out_weight: Integer): Boolean;
    const
        c_probe_limit = 8;
    var
        query_key: string;
        local_results: TncCandidateList;
        local_idx: Integer;
        candidate_text: string;
        candidate_weight: Integer;
    begin
        Result := False;
        out_text := '';
        out_weight := 0;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        query_key := build_span_query_key_local(start_idx, span_len);
        if (query_key = '') or
            (not m_dictionary.lookup_exact_full_pinyin(query_key, local_results)) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(local_results[local_idx].comment) <> '' then
            begin
                Continue;
            end;
            if (local_results[local_idx].source = cs_user) or
                (not local_results[local_idx].has_dict_weight) or
                is_runtime_chain_candidate(local_results[local_idx]) or
                is_runtime_common_pattern_candidate(local_results[local_idx]) or
                is_runtime_redup_candidate(local_results[local_idx]) then
            begin
                Continue;
            end;

            candidate_text := Trim(local_results[local_idx].text);
            if get_candidate_text_unit_count(candidate_text) <> span_len then
            begin
                Continue;
            end;

            candidate_weight := get_candidate_lookup_weight_local(local_results[local_idx]);
            if candidate_weight > out_weight then
            begin
                out_text := candidate_text;
                out_weight := candidate_weight;
                Result := True;
            end;
        end;
    end;
    function try_get_exact_span_text_weight_local(const start_idx: Integer;
        const span_len: Integer; const text_value: string; out out_weight: Integer): Boolean;
    const
        c_probe_limit = 16;
    var
        query_key: string;
        local_results: TncCandidateList;
        local_idx: Integer;
        candidate_text: string;
    begin
        Result := False;
        out_weight := 0;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        query_key := build_span_query_key_local(start_idx, span_len);
        if (query_key = '') or
            (not m_dictionary.lookup_exact_full_pinyin(query_key, local_results)) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(local_results[local_idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate_text := Trim(local_results[local_idx].text);
            if (candidate_text <> text_value) or
                (get_candidate_text_unit_count(candidate_text) <> span_len) then
            begin
                Continue;
            end;

            out_weight := get_candidate_lookup_weight_local(local_results[local_idx]);
            Exit(True);
        end;
    end;
    function path_crosses_stronger_exact_span_local: Boolean;
    const
        c_min_query_units = 6;
        c_min_best_weight = 700;
        c_exact_weight_margin = 240;
    var
        local_path: string;
        local_segments: TArray<string>;
        segment_idx: Integer;
        syllable_idx: Integer;
        current_units: TArray<string>;
        next_units: TArray<string>;
        current_unit_count: Integer;
        next_unit_count: Integer;
        boundary_start_idx: Integer;
        actual_pair_text: string;
        best_pair_text: string;
        best_pair_weight: Integer;
        actual_pair_weight: Integer;
    begin
        Result := False;
        if (m_dictionary = nil) or (Length(syllables) < c_min_query_units) then
        begin
            Exit;
        end;

        local_path := get_segment_path_for_candidate(candidate);
        if (local_path = '') or (Pos(c_segment_path_separator, local_path) <= 0) then
        begin
            Exit;
        end;

        local_segments := local_path.Split([c_segment_path_separator]);
        if Length(local_segments) < 2 then
        begin
            Exit;
        end;

        syllable_idx := 0;
        for segment_idx := 0 to High(local_segments) do
        begin
            current_units := split_text_units(Trim(local_segments[segment_idx]));
            current_unit_count := Length(current_units);
            if (current_unit_count <= 0) or
                (syllable_idx + current_unit_count > Length(syllables)) then
            begin
                Exit(False);
            end;

            if segment_idx < High(local_segments) then
            begin
                next_units := split_text_units(Trim(local_segments[segment_idx + 1]));
                next_unit_count := Length(next_units);
                if next_unit_count <= 0 then
                begin
                    Exit(False);
                end;

                boundary_start_idx := syllable_idx + current_unit_count - 1;
                if boundary_start_idx + 1 <= High(syllables) then
                begin
                    actual_pair_text := current_units[High(current_units)] + next_units[0];
                    if try_get_best_exact_span_text_local(boundary_start_idx, 2,
                        best_pair_text, best_pair_weight) and
                        (not SameText(best_pair_text, actual_pair_text)) and
                        (best_pair_weight >= c_min_best_weight) then
                    begin
                        actual_pair_weight := 0;
                        try_get_exact_span_text_weight_local(boundary_start_idx, 2,
                            actual_pair_text, actual_pair_weight);
                        if (actual_pair_weight <= 0) or
                            (best_pair_weight >= actual_pair_weight +
                            c_exact_weight_margin) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;

            Inc(syllable_idx, current_unit_count);
        end;
    end;
begin
    Result := False;
    if candidate.comment <> '' then
    begin
        Exit;
    end;

    normalized_query := normalize_pinyin_text(query_key);
    if normalized_query = '' then
    begin
        normalized_query := normalize_pinyin_text(m_composition_text);
    end;
    if normalized_query = '' then
    begin
        Exit;
    end;

    syllables := get_effective_compact_pinyin_syllables(normalized_query);

    if Length(syllables) < 2 then
    begin
        Exit;
    end;

    // A persisted same-query choice is explicit user intent. It may still look
    // like a per-syllable chain (for example qiantao -> 宓屽), but it must not
    // be demoted as runtime pollution against a competing base phrase.
    if (candidate.source = cs_user) and (m_dictionary <> nil) and
        ((is_latest_session_query_choice(candidate.text)) or
        (m_dictionary.get_query_choice_bonus(normalized_query, candidate.text) >=
        c_explicit_query_choice_min_bonus)) then
    begin
        Exit(False);
    end;

    for idx := 0 to High(syllables) do
    begin
        // Skip all-initial / dangling-initial queries; weak-chain heuristics are
        // only meaningful for fully specified multi-syllable input.
        if Length(Trim(syllables[idx].text)) <= 1 then
        begin
            Exit(False);
        end;
    end;

    is_low_evidence_user_chain := (candidate.source = cs_user) and
        (((candidate.has_dict_weight) and
        (candidate.dict_weight <= c_low_evidence_user_chain_weight)) or
        ((not candidate.has_dict_weight) and
        (candidate.score <= c_low_evidence_user_chain_weight)));

    // User-learned one-character-per-syllable chains are allowed as fallback,
    // but they should not outrank a real complete lexicon phrase for the same
    // query. Exact lexicon phrases must still stay protected.
    if is_low_evidence_user_chain then
    begin
        text_units := split_text_units(Trim(candidate.text));
        if Length(text_units) = Length(syllables) then
        begin
            for idx := 0 to High(text_units) do
            begin
                if (get_candidate_text_unit_count(text_units[idx]) <> 1) or
                    (not match_single_char_candidate_for_syllable(
                    syllables[idx].text, text_units[idx], is_preferred)) then
                begin
                    Exit(False);
                end;
            end;
            Exit(True);
        end;
    end;

    // Only suppress low-evidence user chains and runtime-composed single-char
    // chains. Exact lexicon phrases must not be treated as problematic just
    // because they also align with per-syllable single chars.
    if candidate.has_dict_weight then
    begin
        Exit;
    end;

    if is_weak_single_char_chain_candidate_for_query(normalized_query, candidate) then
    begin
        Exit(True);
    end;

    if not is_runtime_chain_candidate(candidate) then
    begin
        Exit(False);
    end;

    path_text := get_segment_path_for_candidate(candidate);
    if path_text <> '' then
    begin
        path_segments := path_text.Split([c_segment_path_separator]);
        if Length(path_segments) <> Length(syllables) then
        begin
            Exit(False);
        end;

        SetLength(text_units, Length(path_segments));
        for idx := 0 to High(path_segments) do
        begin
            text_units[idx] := Trim(path_segments[idx]);
            if get_candidate_text_unit_count(text_units[idx]) <> 1 then
            begin
                Exit(False);
            end;
        end;
    end
    else
    begin
        text_units := split_text_units(Trim(candidate.text));
        if Length(text_units) <> Length(syllables) then
        begin
            Exit(False);
        end;
    end;

    for idx := 0 to High(text_units) do
    begin
        if not match_single_char_candidate_for_syllable(syllables[idx].text, text_units[idx], is_preferred) then
        begin
            Exit(False);
        end;
    end;

    Result := is_runtime_constructed_phrase_friendly(candidate.text);
end;

function TncEngine.has_competing_exact_phrase_candidate(const selected_text: string): Boolean;
var
    idx: Integer;
    candidate: TncCandidate;
begin
    Result := False;
    for idx := 0 to High(m_candidates) do
    begin
        candidate := m_candidates[idx];
        if (candidate.comment <> '') or (candidate.text = '') or SameText(candidate.text, selected_text) then
        begin
            Continue;
        end;
        if get_candidate_text_unit_count(candidate.text) < 2 then
        begin
            Continue;
        end;
        if candidate.has_dict_weight or
            ((candidate.source = cs_rule) and
            (not is_runtime_chain_candidate(candidate)) and
            (not is_runtime_common_pattern_candidate(candidate)) and
            (not is_runtime_redup_candidate(candidate))) then
        begin
            Exit(True);
        end;
    end;
end;

function TncEngine.get_candidate_debug_summary(const candidate: TncCandidate): string;
var
    text_context_bonus: Integer;
    phrase_context_bonus: Integer;
    context_query_bonus: Integer;
    context_query_latest_bonus: Integer;
    context_bonus: Integer;
    segment_path_context_bonus: Integer;
    query_bonus: Integer;
    session_bonus: Integer;
    rank_score: Integer;
    layer_value: Integer;
    runtime_kind: string;
    path_confidence_tier: Integer;
    path_confidence_score: Integer;
begin
    text_context_bonus := get_text_context_bonus(candidate.text);
    phrase_context_bonus := get_phrase_context_bonus(candidate.text);
    context_query_bonus := get_context_query_bonus(candidate.text);
    context_query_latest_bonus := get_context_query_latest_bonus(candidate.text);
    context_bonus := get_context_bonus(candidate.text);
    segment_path_context_bonus := get_segment_path_context_bonus(candidate);
    query_bonus := get_session_query_bonus(candidate.text);
    if candidate.comment <> '' then
    begin
        query_bonus := query_bonus div 2;
    end;
    session_bonus := get_session_text_bonus(candidate.text);
    if candidate.comment <> '' then
    begin
        session_bonus := session_bonus div 2;
    end;
    rank_score := get_rank_score(candidate);
    layer_value := get_multi_syllable_intent_layer(candidate);
    runtime_kind := get_runtime_candidate_kind(candidate);
    path_confidence_score := get_candidate_path_confidence_score(candidate);
    path_confidence_tier := get_candidate_path_confidence_tier(candidate);

    Result := Format(
        'top=[%s src=%d score=%d rank=%d ctx=%d text_ctx=%d phr_ctx=%d spath_ctx=%d qctx=%d qctxl=%d qsess=%d sess=%d layer=%d path_conf=%d path_score=%d partial=%d dw=%d rt=%s]',
        [candidate.text, Ord(candidate.source), candidate.score, rank_score, context_bonus, text_context_bonus,
        phrase_context_bonus, segment_path_context_bonus, context_query_bonus, context_query_latest_bonus, query_bonus,
        session_bonus, layer_value, path_confidence_tier, path_confidence_score,
        Ord(candidate.comment <> ''), candidate.dict_weight, runtime_kind]);
end;

procedure TncEngine.sort_candidates(var candidates: TncCandidateList);
type
    TncCandidateSortItem = record
        candidate: TncCandidate;
        rank_score: Integer;
        layer: Integer;
        confidence_rank: Integer;
        path_confidence_score: Integer;
        conservative_margin: Integer;
        source_rank: Integer;
        text_length: Integer;
        text_units: Integer;
        is_latest_context_query_choice: Boolean;
        is_latest_query_choice: Boolean;
        is_weak_single_char_chain: Boolean;
        is_problematic_single_char_chain: Boolean;
        is_weighted_complete_phrase: Boolean;
        is_runtime_constructed_complete_phrase: Boolean;
        is_prioritized_user_phrase: Boolean;
        is_boundary_anchor_partial: Boolean;
        is_weighted_partial_phrase: Boolean;
        is_complete_phrase_prefix_partial: Boolean;
        is_supported_head_phrase_partial: Boolean;
        is_base_exact_prefix_partial: Boolean;
        is_user_prefix_partial: Boolean;
        partial_comment_syllables: Integer;
        is_one_plus_two_partial: Boolean;
        is_two_plus_one_partial: Boolean;
        is_demonstrative_head_friendly_partial: Boolean;
        is_fixed_top_single_char: Boolean;
        is_full_query_exact_dict_text: Boolean;
        full_query_exact_weight: Integer;
        segment_path_count: Integer;
        is_sentence_particle_tail_split: Boolean;
        short_particle_complete_text: string;
        short_particle_partial_complete_text: string;
    end;
var
    list: TList<TncCandidateSortItem>;
    item: TncCandidateSortItem;
    i: Integer;
    use_intent_layers: Boolean;
    has_strong_complete_phrase: Boolean;
    has_competing_exact_phrase: Boolean;
    has_weighted_complete_phrase: Boolean;
    weak_chain_query_syllables: TncPinyinParseResult;
    weak_chain_preference_maps: TArray<TDictionary<string, Byte>>;
    weak_chain_query_prepared: Boolean;
    weak_chain_normalized_query: string;
    candidate_path_score_hint: Integer;
    partial_comment_syllable_cache: TDictionary<string, Integer>;
    best_one_plus_two_partial_score: Integer;
    best_two_plus_one_partial_score: Integer;
    has_boundary_anchor_one_plus_two: Boolean;
    has_boundary_anchor_two_plus_one: Boolean;
    preferred_three_syllable_partial_kind: Integer;
    fixed_top_single_char_text: string;
    best_sentence_single_char_texts: TArray<string>;
    best_sentence_single_char_weights: TArray<Integer>;
    best_sentence_single_char_known: TArray<Boolean>;
    best_two_syllable_exact_texts: TArray<string>;
    best_two_syllable_exact_weights: TArray<Integer>;
    best_two_syllable_exact_known: TArray<Boolean>;
    best_three_syllable_exact_texts: TArray<string>;
    best_three_syllable_exact_weights: TArray<Integer>;
    best_three_syllable_exact_known: TArray<Boolean>;
    best_four_syllable_exact_texts: TArray<string>;
    best_four_syllable_exact_weights: TArray<Integer>;
    best_four_syllable_exact_known: TArray<Boolean>;
    full_query_exact_text_weights: TDictionary<string, Integer>;
const
    c_boundary_anchor_partial_score_hint_flag = 1000000;
    c_boundary_anchor_partial_shape_kind_scale = 10000;
    c_boundary_anchor_partial_shape_one_plus_two = 1;
    c_boundary_anchor_partial_shape_two_plus_one = 2;
    c_three_syllable_boundary_ratio_pct = 100;
    c_three_syllable_partial_shape_margin = 24;
    c_demonstrative_head_friendly_partial_rank_bonus = 220;
    c_strong_exact_partial_shape_penalty = 6000;
    c_complete_phrase_prefix_partial_rank_bonus = 420;
    c_complete_phrase_prefix_partial_override_gap = 120;
    c_supported_head_phrase_partial_rank_bonus = 420;
    c_supported_head_phrase_partial_override_gap = 0;
    c_supported_head_phrase_first_single_margin = 180;
    c_supported_head_phrase_tail_ratio_pct = 80;
    c_overlong_complete_full_key_sort_penalty = 5200;
    function get_display_text_unit_count(const text: string): Integer;
    var
        idx: Integer;
        trimmed: string;
        codepoint: Integer;
    begin
        Result := 0;
        trimmed := Trim(text);
        idx := 1;
        while idx <= Length(trimmed) do
        begin
            codepoint := Ord(trimmed[idx]);
            if (codepoint >= $D800) and (codepoint <= $DBFF) and
                (idx < Length(trimmed)) and
                (Ord(trimmed[idx + 1]) >= $DC00) and
                (Ord(trimmed[idx + 1]) <= $DFFF) then
            begin
                Inc(idx);
            end;
            Inc(Result);
            Inc(idx);
        end;
    end;
    function get_fixed_top_single_char_for_query_local: string;
    var
        normalized_query: string;
    begin
        Result := '';
        if m_last_lookup_syllable_count <> 1 then
        begin
            Exit;
        end;

        normalized_query := normalize_pinyin_text(m_last_lookup_key);
        if normalized_query = 'en' then
        begin
            Result := string(Char($55EF));
        end
        else if normalized_query = 'ba' then
        begin
            Result := string(Char($5427));
        end
        else if normalized_query = 'e' then
        begin
            Result := string(Char($5443));
        end
        else if normalized_query = 'o' then
        begin
            Result := string(Char($54E6));
        end
        else if normalized_query = 'ai' then
        begin
            Result := string(Char($5509));
        end
        else if normalized_query = 'ha' then
        begin
            Result := string(Char($54C8));
        end
        else if normalized_query = 'xi' then
        begin
            Result := string(Char($563B));
        end
        else if normalized_query = 'xing' then
        begin
            Result := string(Char($884C));
        end
        else if normalized_query = 'hao' then
        begin
            Result := string(Char($597D));
        end
        else if normalized_query = 'bang' then
        begin
            Result := string(Char($68D2));
        end
        else if normalized_query = 'ya' then
        begin
            Result := string(Char($5440));
        end
        else if normalized_query = 'qie' then
        begin
            Result := string(Char($5207));
        end
        else if normalized_query = 'ca' then
        begin
            Result := string(Char($64E6));
        end
        else if normalized_query = 'gun' then
        begin
            Result := string(Char($6EDA));
        end
        else if normalized_query = 'hei' then
        begin
            Result := string(Char($563F));
        end
        else if normalized_query = 'pi' then
        begin
            Result := string(Char($5C41));
        end
        else if normalized_query = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_query = 'zhe' then
        begin
            Result := string(Char($8FD9));
        end
        else if normalized_query = 'er' then
        begin
            Result := string(Char($800C));
        end
        else if normalized_query = 'he' then
        begin
            Result := string(Char($548C));
        end
        else if normalized_query = 'you' then
        begin
            Result := string(Char($6709));
        end
        else if normalized_query = 'shi' then
        begin
            Result := string(Char($662F));
        end
        else if normalized_query = 'qing' then
        begin
            Result := string(Char($8BF7));
        end
    end;
    function is_overlong_complete_full_key_item(
        const sort_item: TncCandidateSortItem): Boolean;
    begin
        Result := (m_last_lookup_syllable_count >= 2) and
            is_full_pinyin_key(m_last_lookup_key) and
            (Trim(sort_item.candidate.comment) = '') and
            (sort_item.text_units > m_last_lookup_syllable_count);
    end;
    function is_sentence_particle_unit_for_sort(const unit_value: string): Boolean;
    begin
        Result := (unit_value = string(Char($4E86))) or
            (unit_value = string(Char($7740))) or

            (unit_value = string(Char($8FC7))) or
            (unit_value = string(Char($5417))) or
            (unit_value = string(Char($561B))) or
            (unit_value = string(Char($5427))) or
            (unit_value = string(Char($5462))) or
            (unit_value = string(Char($554A))) or
            (unit_value = string(Char($5440))) or

            (unit_value = string(Char($5566))) or
            (unit_value = string(Char($7684))) or
            (unit_value = string(Char($5730))) or
            (unit_value = string(Char($5F97)));
    end;
    function is_sentence_particle_tail_split_for_sort(
        const candidate: TncCandidate; const segment_path_count: Integer): Boolean;
    var
        encoded_path: string;
        segment_parts: TArray<string>;
        tail_units: TArray<string>;
    begin
        Result := False;
        if (m_last_lookup_syllable_count <> 3) or
            (Trim(candidate.comment) <> '') or (segment_path_count <> 2) then
        begin
            Exit;
        end;

        encoded_path := get_segment_path_for_candidate(candidate);
        if encoded_path = '' then
        begin
            Exit;
        end;
        segment_parts := encoded_path.Split([c_segment_path_separator]);
        if Length(segment_parts) <> 2 then
        begin
            Exit;
        end;
        if get_candidate_text_unit_count(Trim(segment_parts[0])) <> 2 then
        begin
            Exit;
        end;
        tail_units := split_text_units(Trim(segment_parts[1]));
        Result := (Length(tail_units) = 1) and
            is_sentence_particle_unit_for_sort(tail_units[0]);
    end;
    function get_short_particle_tail_text_for_sort(
        const tail_key_value: string): string;
    var
        normalized_tail_key_local: string;
    begin
        Result := '';
        normalized_tail_key_local := normalize_pinyin_text(Trim(tail_key_value));
        if normalized_tail_key_local = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_tail_key_local = 'le' then
        begin
            Result := string(Char($4E86));
        end
        else if normalized_tail_key_local = 'zhe' then
        begin
            Result := string(Char($7740));
        end
        else if normalized_tail_key_local = 'la' then
        begin
            Result := string(Char($5566));
        end
        else if normalized_tail_key_local = 'ba' then
        begin
            Result := string(Char($5427));
        end
        else if normalized_tail_key_local = 'ma' then
        begin
            Result := string(Char($5417));
        end
        else if normalized_tail_key_local = 'a' then
        begin
            Result := string(Char($554A));
        end;
    end;
    function dictionary_lookup_cached_for_sort(const pinyin_key: string;
        const exact_only: Boolean; out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
        cached_results: TncCandidateList;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        if exact_only then
        begin
            cache_key := '#exact#' + pinyin_key;
        end
        else
        begin
            cache_key := '#lookup#' + pinyin_key;
        end;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, cached_results) then
        begin
            out_results := cached_results;
            Exit(Length(out_results) > 0);
        end;

        if exact_only then
        begin
            Result := m_dictionary.lookup_exact_full_pinyin(pinyin_key,
                out_results);
        end
        else
        begin
            Result := m_dictionary.lookup(pinyin_key, out_results);
        end;
        if not Result then
        begin
            SetLength(out_results, 0);
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key,
                Copy(out_results, 0, Length(out_results)));
        end;
        Result := Length(out_results) > 0;
    end;
    procedure prepare_full_query_exact_text_weights;
    var
        exact_results: TncCandidateList;
        exact_idx: Integer;
        exact_text: string;
        exact_weight: Integer;
    begin
        if full_query_exact_text_weights <> nil then
        begin
            Exit;
        end;

        full_query_exact_text_weights := TDictionary<string, Integer>.Create;
        if (m_dictionary = nil) or (m_last_lookup_syllable_count < 2) or
            (not is_full_pinyin_key(m_last_lookup_key)) then
        begin
            Exit;
        end;
        if not dictionary_lookup_cached_for_sort(m_last_lookup_key, True,
            exact_results) then
        begin
            Exit;
        end;

        for exact_idx := 0 to High(exact_results) do
        begin
            if Trim(exact_results[exact_idx].comment) <> '' then
            begin
                Continue;
            end;
            exact_text := Trim(exact_results[exact_idx].text);
            if (exact_text = '') or
                (get_candidate_text_unit_count(exact_text) <>
                m_last_lookup_syllable_count) then
            begin
                Continue;
            end;

            exact_weight := exact_results[exact_idx].score;
            if exact_results[exact_idx].has_dict_weight and
                (exact_results[exact_idx].dict_weight > exact_weight) then
            begin
                exact_weight := exact_results[exact_idx].dict_weight;
            end;
            if exact_weight < 0 then
            begin
                exact_weight := 0;
            end;
            full_query_exact_text_weights.AddOrSetValue(exact_text,
                exact_weight);
        end;
    end;
    function try_get_full_query_exact_weight_local(const text_value: string;
        out out_weight: Integer): Boolean;
    begin
        Result := False;
        out_weight := 0;
        prepare_full_query_exact_text_weights;
        if full_query_exact_text_weights = nil then
        begin
            Exit;
        end;
        Result := full_query_exact_text_weights.TryGetValue(Trim(text_value),
            out_weight);
    end;
    function get_partial_comment_syllable_count(const comment_text: string): Integer;
    var
        normalized_comment: string;
    begin
        Result := 0;
        normalized_comment := normalize_pinyin_text(comment_text);
        if normalized_comment = '' then
        begin
            Exit;
        end;
        if partial_comment_syllable_cache.TryGetValue(normalized_comment, Result) then
        begin
            Exit;
        end;
        Result := get_effective_compact_pinyin_unit_count(normalized_comment);
        partial_comment_syllable_cache.AddOrSetValue(normalized_comment, Result);
    end;
    function is_exact_phrase_candidate_for_ranking(const candidate: TncCandidate): Boolean;
    begin
        Result := (candidate.comment = '') and
            (get_candidate_text_unit_count(candidate.text) >= 2) and
            (candidate.has_dict_weight or (candidate.source = cs_user));
    end;
    function is_complete_phrase_prefix_partial_candidate(const partial_candidate: TncCandidate;
        const partial_units: Integer; const partial_comment_syllables: Integer): Boolean;
    var
        idx: Integer;
        unit_idx: Integer;
        partial_text_units: TArray<string>;
        complete_text_units: TArray<string>;
        matches_prefix: Boolean;
    begin
        Result := False;
        if (partial_candidate.comment = '') or (partial_units < 2) or
            (partial_comment_syllables <= 0) or
            (partial_units + partial_comment_syllables <> m_last_lookup_syllable_count) then
        begin
            Exit;
        end;

        partial_text_units := split_text_units(Trim(partial_candidate.text));
        if Length(partial_text_units) <> partial_units then
        begin
            Exit;
        end;

        for idx := 0 to High(candidates) do
        begin
            if not is_exact_phrase_candidate_for_ranking(candidates[idx]) then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(candidates[idx].text) <>
                m_last_lookup_syllable_count then
            begin
                Continue;
            end;

            complete_text_units := split_text_units(Trim(candidates[idx].text));
            if Length(complete_text_units) <> m_last_lookup_syllable_count then
            begin
                Continue;
            end;

            matches_prefix := True;
            for unit_idx := 0 to partial_units - 1 do
            begin
                if complete_text_units[unit_idx] <> partial_text_units[unit_idx] then
                begin
                    matches_prefix := False;
                    Break;
                end;
            end;
            if matches_prefix then
            begin
                Exit(True);
            end;
        end;
    end;
    function is_base_exact_prefix_partial_candidate(const partial_candidate: TncCandidate;
        const partial_units: Integer; const partial_comment_syllables: Integer): Boolean;
    var
        prefix_key: string;
        syllable_idx: Integer;
        parser_local: TncPinyinParser;
        syllables_local: TncPinyinParseResult;
    begin
        Result := False;
        if (m_dictionary = nil) or (partial_candidate.comment = '') or
            (partial_units <= 0) or (partial_comment_syllables <= 0) or
            (partial_units + partial_comment_syllables <> m_last_lookup_syllable_count) then
        begin
            Exit;
        end;

        parser_local := TncPinyinParser.Create;
        try
            syllables_local := parser_local.parse(normalize_pinyin_text(m_last_lookup_key));
        finally
            parser_local.Free;
        end;
        if Length(syllables_local) < partial_units then
        begin
            Exit;
        end;

        prefix_key := '';
        for syllable_idx := 0 to partial_units - 1 do
        begin
            prefix_key := prefix_key +
                normalize_pinyin_text(syllables_local[syllable_idx].text);
        end;

        Result := (prefix_key <> '') and
            m_dictionary.is_base_entry(prefix_key, Trim(partial_candidate.text));
    end;
    function is_supported_head_phrase_partial_candidate(const sort_item: TncCandidateSortItem): Boolean;
    begin
        Result := False;
        if (m_last_lookup_syllable_count <> 3) or
            (preferred_three_syllable_partial_kind <> 2) or
            (not sort_item.is_two_plus_one_partial) then
        begin
            Exit;
        end;
        if (m_last_three_syllable_head_exact_text = '') or
            (Trim(sort_item.candidate.text) <> m_last_three_syllable_head_exact_text) then
        begin
            Exit;
        end;

        if m_last_three_syllable_head_strength <= 0 then
        begin
            Exit;
        end;
        if m_last_three_syllable_head_strength <
            (m_last_three_syllable_first_single_strength +
            c_supported_head_phrase_first_single_margin) then
        begin
            Exit;
        end;

        if m_last_three_syllable_head_path_bonus > 0 then
        begin
            Exit(True);
        end;

        if m_last_three_syllable_tail_strength <= 0 then
        begin
            Exit(True);
        end;

        Result := (m_last_three_syllable_head_strength * 100) >=
            (m_last_three_syllable_tail_strength *
            c_supported_head_phrase_tail_ratio_pct);
    end;
    function get_cached_single_char_preference(const syllable_index: Integer;
        const unit_text: string; out out_matched: Boolean): Boolean;
    const
        c_partial_preferred_min_weight = 120;
        c_partial_preferred_max_rank = 4;
        c_partial_preferred_top_ratio_pct = 85;
    var
        cache_map: TDictionary<string, Byte>;
        results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        trimmed_unit_text: string;
        stored_state: Byte;
        weight_value: Integer;
        top_weight_value: Integer;
        single_char_rank: Integer;
        preferred: Boolean;
    begin
        Result := False;
        out_matched := False;
        if (syllable_index < 0) or (syllable_index > High(weak_chain_query_syllables)) then
        begin
            Exit;
        end;

        trimmed_unit_text := Trim(unit_text);
        if trimmed_unit_text = '' then
        begin
            Exit;
        end;

        cache_map := weak_chain_preference_maps[syllable_index];
        if cache_map = nil then
        begin
            cache_map := TDictionary<string, Byte>.Create;
            weak_chain_preference_maps[syllable_index] := cache_map;
            if (m_dictionary <> nil) and dictionary_lookup_cached_for_sort(
                Trim(weak_chain_query_syllables[syllable_index].text), False,
                results) then
            begin
                top_weight_value := 0;
                single_char_rank := 0;
                for idx := 0 to High(results) do
                begin
                    candidate_text := Trim(results[idx].text);
                    if (candidate_text = '') or (get_candidate_text_unit_count(candidate_text) <> 1) then
                    begin
                        Continue;
                    end;

                    Inc(single_char_rank);
                    if results[idx].has_dict_weight then
                    begin
                        weight_value := results[idx].dict_weight;
                    end
                    else
                    begin
                        weight_value := results[idx].score;
                    end;
                    if top_weight_value <= 0 then
                    begin
                        top_weight_value := weight_value;
                    end;

                    preferred := (weight_value >= c_partial_preferred_min_weight) and
                        ((single_char_rank <= c_partial_preferred_max_rank) or
                        (top_weight_value <= 0) or
                        ((weight_value * 100) >= (top_weight_value * c_partial_preferred_top_ratio_pct)));
                    if preferred then
                    begin
                        stored_state := 2;
                    end
                    else
                    begin
                        stored_state := 1;
                    end;
                    if not cache_map.ContainsKey(candidate_text) then
                    begin
                        cache_map.Add(candidate_text, stored_state);
                    end;
                end;
            end;
        end;

        if cache_map.TryGetValue(trimmed_unit_text, stored_state) then
        begin
            out_matched := True;
            Result := stored_state = 2;
        end;
    end;
    function get_fixed_sentence_single_char_for_sort(
        const syllable_text: string): string;
    var
        normalized_syllable: string;
    begin
        Result := '';
        normalized_syllable := normalize_pinyin_text(syllable_text);
        if normalized_syllable = 'wo' then
        begin
            Result := string(Char($6211));
        end
        else if normalized_syllable = 'ni' then
        begin
            Result := string(Char($4F60));
        end
        else if normalized_syllable = 'ta' then
        begin
            Result := string(Char($4ED6));
        end
        else if normalized_syllable = 'he' then
        begin
            Result := string(Char($548C));
        end
        else if normalized_syllable = 'qu' then
        begin
            Result := string(Char($53BB));
        end
        else if normalized_syllable = 'shi' then
        begin
            Result := string(Char($662F));
        end
        else if normalized_syllable = 'ye' then
        begin
            Result := string(Char($4E5F));
        end
        else if normalized_syllable = 'yu' then
        begin
            Result := string(Char($4E0E));
        end
        else if normalized_syllable = 'hui' then
        begin
            Result := string(Char($4F1A));
        end
        else if normalized_syllable = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_syllable = 'zhe' then
        begin
            Result := string(Char($8FD9));
        end
        else if normalized_syllable = 'qing' then
        begin
            Result := string(Char($8BF7));
        end;
    end;
    procedure prepare_weak_chain_query_cache;
    var
        parser: TncPinyinParser;
        idx: Integer;
    begin
        if weak_chain_query_prepared then
        begin
            Exit;
        end;

        weak_chain_query_prepared := True;
        weak_chain_normalized_query := normalize_pinyin_text(m_last_lookup_key);
        if weak_chain_normalized_query = '' then
        begin
            weak_chain_normalized_query := normalize_pinyin_text(m_composition_text);
        end;
        if weak_chain_normalized_query = '' then
        begin
            Exit;
        end;

        parser := TncPinyinParser.Create;
        try
            weak_chain_query_syllables := parser.parse(weak_chain_normalized_query);
        finally
            parser.Free;
        end;

        if Length(weak_chain_query_syllables) < 2 then
        begin
            SetLength(weak_chain_query_syllables, 0);
            Exit;
        end;

        for idx := 0 to High(weak_chain_query_syllables) do
        begin
            if Length(Trim(weak_chain_query_syllables[idx].text)) <= 1 then
            begin
                SetLength(weak_chain_query_syllables, 0);
                Exit;
            end;
        end;

        SetLength(weak_chain_preference_maps, Length(weak_chain_query_syllables));
        SetLength(best_sentence_single_char_texts, Length(weak_chain_query_syllables));
        SetLength(best_sentence_single_char_weights, Length(weak_chain_query_syllables));
        SetLength(best_sentence_single_char_known, Length(weak_chain_query_syllables));
        SetLength(best_two_syllable_exact_texts, Length(weak_chain_query_syllables) - 1);
        SetLength(best_two_syllable_exact_weights, Length(weak_chain_query_syllables) - 1);
        SetLength(best_two_syllable_exact_known, Length(weak_chain_query_syllables) - 1);
        if Length(weak_chain_query_syllables) >= 3 then
        begin
            SetLength(best_three_syllable_exact_texts, Length(weak_chain_query_syllables) - 2);
            SetLength(best_three_syllable_exact_weights, Length(weak_chain_query_syllables) - 2);
            SetLength(best_three_syllable_exact_known, Length(weak_chain_query_syllables) - 2);
        end
        else
        begin
            SetLength(best_three_syllable_exact_texts, 0);
            SetLength(best_three_syllable_exact_weights, 0);
            SetLength(best_three_syllable_exact_known, 0);
        end;
        if Length(weak_chain_query_syllables) >= 4 then
        begin
            SetLength(best_four_syllable_exact_texts, Length(weak_chain_query_syllables) - 3);
            SetLength(best_four_syllable_exact_weights, Length(weak_chain_query_syllables) - 3);
            SetLength(best_four_syllable_exact_known, Length(weak_chain_query_syllables) - 3);
        end
        else
        begin
            SetLength(best_four_syllable_exact_texts, 0);
            SetLength(best_four_syllable_exact_weights, 0);
            SetLength(best_four_syllable_exact_known, 0);
        end;
    end;
    function try_get_best_sentence_single_char_for_sort(const syllable_index: Integer;
        out out_text: string; out out_weight: Integer): Boolean;
    const
        c_probe_limit = 6;
    var
        local_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        is_preferred: Boolean;
        weight_value: Integer;
        fixed_text: string;
    begin
        Result := False;
        out_text := '';
        out_weight := 0;
        prepare_weak_chain_query_cache;
        if (syllable_index < 0) or (syllable_index > High(weak_chain_query_syllables)) then
        begin
            Exit;
        end;

        if best_sentence_single_char_known[syllable_index] then
        begin
            out_text := best_sentence_single_char_texts[syllable_index];
            out_weight := best_sentence_single_char_weights[syllable_index];
            Exit(out_text <> '');
        end;

        best_sentence_single_char_known[syllable_index] := True;
        fixed_text := get_fixed_sentence_single_char_for_sort(
            weak_chain_query_syllables[syllable_index].text);
        if fixed_text <> '' then
        begin
            best_sentence_single_char_texts[syllable_index] := fixed_text;
            best_sentence_single_char_weights[syllable_index] := 960;
            out_text := fixed_text;
            out_weight := 960;
            Exit(True);
        end;

        if (m_dictionary = nil) or
            (not dictionary_lookup_cached_for_sort(
            Trim(weak_chain_query_syllables[syllable_index].text), False,
            local_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(local_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            candidate_text := Trim(local_results[idx].text);
            if (get_candidate_text_unit_count(candidate_text) <> 1) or
                (not match_single_char_candidate_for_syllable(
                weak_chain_query_syllables[syllable_index].text, candidate_text, is_preferred)) or
                (not is_preferred) then
            begin
                Continue;
            end;

            if local_results[idx].has_dict_weight then
            begin
                weight_value := local_results[idx].dict_weight;
            end
            else
            begin
                weight_value := local_results[idx].score;
            end;

            if weight_value > out_weight then
            begin
                out_text := candidate_text;
                out_weight := weight_value;
                Result := True;
            end;
        end;

        if Result then
        begin
            best_sentence_single_char_texts[syllable_index] := out_text;
            best_sentence_single_char_weights[syllable_index] := out_weight;
        end;
    end;
    function build_sort_query_key(const start_idx: Integer;
        const syllable_count_local: Integer): string;
    var
        idx: Integer;
    begin
        Result := '';
        prepare_weak_chain_query_cache;
        if (start_idx < 0) or (syllable_count_local <= 0) or
            (start_idx + syllable_count_local - 1 > High(weak_chain_query_syllables)) then
        begin
            Exit;
        end;
        for idx := start_idx to start_idx + syllable_count_local - 1 do
        begin
            Result := Result + normalize_pinyin_text(weak_chain_query_syllables[idx].text);
        end;
    end;
    function try_get_best_exact_phrase_for_cached_span(const start_idx: Integer;
        const syllable_count_local: Integer; var text_cache: TArray<string>;
        var weight_cache: TArray<Integer>; var known_cache: TArray<Boolean>;
        out out_text: string; out out_weight: Integer): Boolean;
    const
        c_probe_limit = 8;
    var
        query_key: string;
        local_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        candidate_units: Integer;
        weight_value: Integer;
    begin
        Result := False;
        out_text := '';
        out_weight := 0;
        prepare_weak_chain_query_cache;
        if (start_idx < 0) or (start_idx > High(known_cache)) then
        begin
            Exit;
        end;

        if known_cache[start_idx] then
        begin
            out_text := text_cache[start_idx];
            out_weight := weight_cache[start_idx];
            Exit(out_text <> '');
        end;

        known_cache[start_idx] := True;
        query_key := build_sort_query_key(start_idx, syllable_count_local);
        if (query_key = '') or (m_dictionary = nil) or
            (not dictionary_lookup_cached_for_sort(query_key, True,
            local_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(local_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(local_results[idx].comment) <> '' then
            begin
                Continue;
            end;
            if (local_results[idx].source = cs_user) or
                (not local_results[idx].has_dict_weight) or
                is_runtime_chain_candidate(local_results[idx]) or
                is_runtime_common_pattern_candidate(local_results[idx]) or
                is_runtime_redup_candidate(local_results[idx]) then
            begin
                Continue;
            end;

            candidate_text := Trim(local_results[idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if candidate_units <> syllable_count_local then
            begin
                Continue;
            end;

            if local_results[idx].has_dict_weight then
            begin
                weight_value := local_results[idx].dict_weight;
            end
            else
            begin
                weight_value := local_results[idx].score;
            end;

            if weight_value > out_weight then
            begin
                out_text := candidate_text;
                out_weight := weight_value;
                Result := True;
            end;
        end;

        if Result then
        begin
            text_cache[start_idx] := out_text;
            weight_cache[start_idx] := out_weight;
        end;
    end;
    function try_get_best_exact_phrase_for_two_syllable_span(const start_idx: Integer;
        out out_text: string; out out_weight: Integer): Boolean;
    begin
        Result := try_get_best_exact_phrase_for_cached_span(start_idx, 2,
            best_two_syllable_exact_texts, best_two_syllable_exact_weights,
            best_two_syllable_exact_known, out_text, out_weight);
    end;
    function try_get_best_exact_phrase_for_three_syllable_span(const start_idx: Integer;
        out out_text: string; out out_weight: Integer): Boolean;
    begin
        Result := try_get_best_exact_phrase_for_cached_span(start_idx, 3,
            best_three_syllable_exact_texts, best_three_syllable_exact_weights,
            best_three_syllable_exact_known, out_text, out_weight);
    end;
    function try_get_best_exact_phrase_for_four_syllable_span(const start_idx: Integer;
        out out_text: string; out out_weight: Integer): Boolean;
    begin
        Result := try_get_best_exact_phrase_for_cached_span(start_idx, 4,
            best_four_syllable_exact_texts, best_four_syllable_exact_weights,
            best_four_syllable_exact_known, out_text, out_weight);
    end;
    function get_sentence_path_bridge_adjustment(const candidate: TncCandidate): Integer;
    const
        c_exact_span_min_weight = 1200;
        c_exact_two_span_match_bonus = 320;
        c_exact_three_span_match_bonus = 480;
        c_exact_four_span_match_bonus = 640;
        c_exact_two_span_mismatch_penalty = 720;
        c_exact_three_span_mismatch_penalty = 1040;
        c_exact_four_span_mismatch_penalty = 1520;
    var
        encoded_path: string;
        segment_parts: TArray<string>;
        segment_idx: Integer;
        syllable_idx: Integer;
        segment_text: string;
        next_segment_text: string;
        segment_units: Integer;
        next_segment_units: Integer;
        left_text: string;
        right_text: string;
        left_weight: Integer;
        right_weight: Integer;
        fixed_left: string;
        fixed_right: string;
        fixed_third: string;
        combined_text: string;
        candidate_units: TArray<string>;
        segment_units_texts: TArray<string>;
        unit_idx: Integer;
        append_idx: Integer;
        window_start: Integer;
        window_len: Integer;
        window_text: string;
        best_exact_span_text: string;
        best_exact_span_weight: Integer;
        best_exact_two_text: string;
        best_exact_two_weight: Integer;
        best_exact_three_text: string;
        best_exact_three_weight: Integer;
        bridge_cache_key: string;
        cached_bridge_adjustment: Integer;
        function try_get_best_exact_phrase_for_span_local(const start_idx: Integer;
            const span_len: Integer; out out_text: string;
            out out_weight: Integer): Boolean;
        begin
            Result := False;
            case span_len of
                2:
                    Result := try_get_best_exact_phrase_for_two_syllable_span(
                        start_idx, out_text, out_weight);
                3:
                    Result := try_get_best_exact_phrase_for_three_syllable_span(
                        start_idx, out_text, out_weight);
                4:
                    Result := try_get_best_exact_phrase_for_four_syllable_span(
                        start_idx, out_text, out_weight);
            end;
        end;
    begin
        Result := 0;
        if (candidate.comment <> '') or (candidate.source = cs_user) or
            (get_candidate_text_unit_count(candidate.text) < 2) then
        begin
            Exit;
        end;

        prepare_weak_chain_query_cache;
        if Length(weak_chain_query_syllables) < 2 then
        begin
            Exit;
        end;

        encoded_path := get_segment_path_for_candidate(candidate);
        if get_encoded_path_segment_count_local(encoded_path) <= 1 then
        begin
            Exit;
        end;
        bridge_cache_key := 'sortbridge' + #1 + m_last_lookup_key + #1 +
            Trim(candidate.text) + #1 + encoded_path;
        if (m_lookup_display_feature_cache <> nil) and
            m_lookup_display_feature_cache.TryGetValue(bridge_cache_key,
            cached_bridge_adjustment) then
        begin
            Exit(cached_bridge_adjustment);
        end;

        segment_parts := encoded_path.Split([c_segment_path_separator]);
        SetLength(candidate_units, 0);
        for segment_idx := 0 to High(segment_parts) do
        begin
            segment_units_texts := split_text_units(Trim(segment_parts[segment_idx]));
            for unit_idx := 0 to High(segment_units_texts) do
            begin
                append_idx := Length(candidate_units);
                SetLength(candidate_units, append_idx + 1);
                candidate_units[append_idx] := segment_units_texts[unit_idx];
            end;
        end;

        syllable_idx := 0;
        for segment_idx := 0 to High(segment_parts) do
        begin
            segment_text := Trim(segment_parts[segment_idx]);
            segment_units := get_candidate_text_unit_count(segment_text);
            if (segment_units = 2) and (syllable_idx + 1 <= High(weak_chain_query_syllables)) then
            begin
                fixed_left := get_fixed_sentence_single_char_for_sort(
                    weak_chain_query_syllables[syllable_idx].text);
                fixed_right := get_fixed_sentence_single_char_for_sort(
                    weak_chain_query_syllables[syllable_idx + 1].text);
                best_exact_two_text := '';
                best_exact_two_weight := 0;
                if try_get_best_exact_phrase_for_two_syllable_span(
                    syllable_idx, best_exact_two_text, best_exact_two_weight) then
                begin
                    if SameText(segment_text, best_exact_two_text) then
                    begin
                        Inc(Result, 720);
                    end
                    else if (fixed_left <> '') and (fixed_right <> '') and
                        SameText(segment_text, fixed_left + fixed_right) then
                    begin
                        Dec(Result, 1600);
                    end;
                end;

                if try_get_best_sentence_single_char_for_sort(
                    syllable_idx, left_text, left_weight) and
                    try_get_best_sentence_single_char_for_sort(
                    syllable_idx + 1, right_text, right_weight) and
                    (segment_text <> left_text + right_text) then
                begin
                    if (left_weight + right_weight) >= 1500 then
                    begin
                        Dec(Result, 1100);
                    end
                    else if (left_weight + right_weight) >= 1200 then
                    begin
                        Dec(Result, 780);
                    end;
                end;

                if (segment_idx < High(segment_parts)) and
                    (syllable_idx + 2 <= High(weak_chain_query_syllables)) then
                begin
                    next_segment_text := Trim(segment_parts[segment_idx + 1]);
                    next_segment_units := get_candidate_text_unit_count(next_segment_text);
                    if next_segment_units >= 1 then
                    begin
                        combined_text := segment_text + split_text_units(next_segment_text)[0];
                        fixed_third := get_fixed_sentence_single_char_for_sort(
                            weak_chain_query_syllables[syllable_idx + 2].text);
                        best_exact_three_text := '';
                        best_exact_three_weight := 0;
                        if try_get_best_exact_phrase_for_three_syllable_span(
                            syllable_idx, best_exact_three_text, best_exact_three_weight) then
                        begin
                            if combined_text = best_exact_three_text then
                            begin
                                Inc(Result, 520);
                            end
                            else if (fixed_left <> '') and (fixed_right <> '') and
                                (fixed_third <> '') and
                                SameText(combined_text,
                                fixed_left + fixed_right + fixed_third) then
                            begin
                                Dec(Result, 880);
                            end;
                        end;
                    end;
                end;
            end;
            Inc(syllable_idx, segment_units);
        end;

        if Length(candidate_units) = Length(weak_chain_query_syllables) then
        begin
            for window_len := 2 to 4 do
            begin
                for window_start := 0 to Length(candidate_units) - window_len do
                begin
                    best_exact_span_text := '';
                    best_exact_span_weight := 0;
                    if not try_get_best_exact_phrase_for_span_local(window_start,
                        window_len, best_exact_span_text, best_exact_span_weight) then
                    begin
                        Continue;
                    end;
                    if best_exact_span_weight < c_exact_span_min_weight then
                    begin
                        Continue;
                    end;

                    window_text := '';
                    for unit_idx := window_start to window_start + window_len - 1 do
                    begin
                        window_text := window_text + candidate_units[unit_idx];
                    end;

                    if SameText(window_text, best_exact_span_text) then
                    begin
                        case window_len of
                            2:
                                Inc(Result, c_exact_two_span_match_bonus);
                            3:
                                Inc(Result, c_exact_three_span_match_bonus);
                            4:
                                Inc(Result, c_exact_four_span_match_bonus);
                        end;
                    end
                    else
                    begin
                        case window_len of
                            2:
                                Dec(Result, c_exact_two_span_mismatch_penalty);
                            3:
                                Dec(Result, c_exact_three_span_mismatch_penalty);
                            4:
                                Dec(Result, c_exact_four_span_mismatch_penalty);
                        end;
                    end;
                end;
            end;
        end;
        if m_lookup_display_feature_cache <> nil then
        begin
            m_lookup_display_feature_cache.AddOrSetValue(bridge_cache_key,
                Result);
        end;
    end;
    function is_weighted_complete_phrase_candidate(const candidate: TncCandidate): Boolean;
    var
        encoded_path: string;
    begin
        Result := (candidate.comment = '') and candidate.has_dict_weight and
            (get_candidate_text_unit_count(candidate.text) >= 2);
        if Result then
        begin
            Exit;
        end;

        if (candidate.comment <> '') or
            (m_last_lookup_syllable_count < c_long_sentence_full_path_min_syllables) or
            (get_candidate_text_unit_count(candidate.text) < 2) or
            is_runtime_chain_candidate(candidate) or
            is_runtime_common_pattern_candidate(candidate) or
            is_runtime_redup_candidate(candidate) then
        begin
            Exit(False);
        end;

        encoded_path := get_segment_path_for_candidate(candidate);
        Result := get_encoded_path_segment_count_local(encoded_path) > 1;
    end;
    function has_multi_char_segment_path_candidate(const candidate: TncCandidate): Boolean;
    var
        encoded_path: string;
        segment_parts: TArray<string>;
        part: string;
    begin
        Result := False;
        if candidate.comment <> '' then
        begin
            Exit;
        end;

        encoded_path := get_segment_path_for_candidate(candidate);
        if get_encoded_path_segment_count_local(encoded_path) <= 1 then
        begin
            Exit;
        end;

        segment_parts := encoded_path.Split([c_segment_path_separator]);
        for part in segment_parts do
        begin
            if get_candidate_text_unit_count(Trim(part)) >= 2 then
            begin
                Exit(True);
            end;
        end;
    end;
    function is_runtime_constructed_complete_phrase_candidate(const candidate: TncCandidate): Boolean;
    begin
        Result := (candidate.comment = '') and (get_candidate_text_unit_count(candidate.text) >= 2) and
            (is_runtime_chain_candidate(candidate) or
            is_runtime_common_pattern_candidate(candidate) or
            is_runtime_redup_candidate(candidate));
    end;
    function is_weak_single_char_chain_candidate_cached(const candidate: TncCandidate): Boolean;
    var
        text_units: TArray<string>;
        idx: Integer;
        is_preferred: Boolean;
        is_matched: Boolean;
        has_weak_unit: Boolean;
    begin
        Result := False;
        if candidate.comment <> '' then
        begin
            Exit;
        end;

        if (candidate.source = cs_user) and
            (get_candidate_text_unit_count(candidate.text) >= 2) and
            (candidate.has_dict_weight or (candidate.score > 8)) then
        begin
            Exit(False);
        end;

        if has_multi_char_segment_path_candidate(candidate) then
        begin
            Exit(False);
        end;

        prepare_weak_chain_query_cache;
        if Length(weak_chain_query_syllables) < 2 then
        begin
            Exit;
        end;

        text_units := split_text_units(Trim(candidate.text));
        if Length(text_units) <> Length(weak_chain_query_syllables) then
        begin
            Exit;
        end;

        has_weak_unit := False;
        for idx := 0 to High(text_units) do
        begin
            is_preferred := get_cached_single_char_preference(idx, text_units[idx], is_matched);
            if not is_matched then
            begin
                Exit(False);
            end;
            if not is_preferred then
            begin
                has_weak_unit := True;
            end;
        end;

        Result := has_weak_unit;
    end;
    function is_problematic_single_char_chain_candidate_cached(const candidate: TncCandidate): Boolean;
    var
        text_units: TArray<string>;
        idx: Integer;
        is_matched: Boolean;
    begin
        Result := False;
        if candidate.comment <> '' then
        begin
            Exit;
        end;

        if (candidate.source = cs_user) and
            (get_candidate_text_unit_count(candidate.text) >= 2) and
            (candidate.has_dict_weight or (candidate.score > 8)) then
        begin
            Exit(False);
        end;

        if has_multi_char_segment_path_candidate(candidate) then
        begin
            Exit(False);
        end;

        prepare_weak_chain_query_cache;
        if Length(weak_chain_query_syllables) < 2 then
        begin
            Exit;
        end;

        if is_weak_single_char_chain_candidate_cached(candidate) then
        begin
            Exit(True);
        end;

        if (not is_runtime_chain_candidate(candidate)) and
            (candidate.source <> cs_user) then
        begin
            Exit(False);
        end;

        if not is_runtime_chain_candidate(candidate) then
        begin
            Exit(False);
        end;

        text_units := split_text_units(Trim(candidate.text));
        if Length(text_units) <> Length(weak_chain_query_syllables) then
        begin
            Exit;
        end;

        for idx := 0 to High(text_units) do
        begin
            get_cached_single_char_preference(idx, text_units[idx], is_matched);
            if not is_matched then
            begin
                Exit(False);
            end;
        end;

        Result := is_runtime_constructed_phrase_friendly(candidate.text);
    end;

    function is_demonstrative_head_friendly_partial_candidate_cached(
        const candidate: TncCandidate): Boolean;
    var
        expected_head_text: string;
        normalized_comment: string;
        expected_comment: string;
        tail_syllable_text: string;
    begin
        Result := False;
        if candidate.comment = '' then
        begin
            Exit;
        end;

        prepare_weak_chain_query_cache;
        if Length(weak_chain_query_syllables) < 2 then
        begin
            Exit;
        end;

        expected_head_text := '';
        if SameText(weak_chain_query_syllables[0].text, 'zhe') then
        begin
            expected_head_text := string(Char($8FD9));
        end
        else if SameText(weak_chain_query_syllables[0].text, 'na') or
            SameText(weak_chain_query_syllables[0].text, 'nei') then
        begin
            expected_head_text := string(Char($90A3));
        end;
        if expected_head_text = '' then
        begin
            Exit;
        end;
        if Trim(candidate.text) <> expected_head_text then
        begin
            Exit;
        end;

        if Length(weak_chain_query_syllables) = 2 then
        begin
            tail_syllable_text := weak_chain_query_syllables[1].text;
        end
        else if (Length(weak_chain_query_syllables) = 3) and
            SameText(weak_chain_query_syllables[1].text, 'yi') then
        begin
            tail_syllable_text := weak_chain_query_syllables[2].text;
        end
        else
        begin
            tail_syllable_text := '';
        end;
        if (tail_syllable_text = '') or
            (not SameText(tail_syllable_text, 'ge')) and
            (not SameText(tail_syllable_text, 'xie')) and
            (not SameText(tail_syllable_text, 'zhong')) and
            (not SameText(tail_syllable_text, 'li')) and
            (not SameText(tail_syllable_text, 'yang')) and
            (not SameText(tail_syllable_text, 'me')) and
            (not SameText(tail_syllable_text, 'ci')) and
            (not SameText(tail_syllable_text, 'hui')) and
            (not SameText(tail_syllable_text, 'lun')) and
            (not SameText(tail_syllable_text, 'lei')) and
            (not SameText(tail_syllable_text, 'bian')) and
            (not SameText(tail_syllable_text, 'mian')) and
            (not SameText(tail_syllable_text, 'yan')) and
            (not SameText(tail_syllable_text, 'tian')) and
            (not SameText(tail_syllable_text, 'nian')) and
            (not SameText(tail_syllable_text, 'yue')) and
            (not SameText(tail_syllable_text, 'xia')) and
            (not SameText(tail_syllable_text, 'sheng')) and
            (not SameText(tail_syllable_text, 'dian')) and
            (not SameText(tail_syllable_text, 'wei')) then
        begin
            Exit;
        end;

        normalized_comment := normalize_pinyin_text(candidate.comment);
        expected_comment := '';
        if Length(weak_chain_query_syllables) >= 2 then
        begin
            expected_comment := Trim(weak_chain_query_syllables[1].text);
            if Length(weak_chain_query_syllables) >= 3 then
            begin
                expected_comment := expected_comment + Trim(weak_chain_query_syllables[2].text);
            end;
        end;
        Result := (normalized_comment <> '') and (normalized_comment = expected_comment);
    end;

    procedure swap_items(const left_index: Integer; const right_index: Integer);
    var
        tmp: TncCandidateSortItem;
    begin
        tmp := list[left_index];
        list[left_index] := list[right_index];
        list[right_index] := tmp;
    end;

    procedure apply_front_row_conservative_guard;
    var
        top_item: TncCandidateSortItem;
        alt_item: TncCandidateSortItem;
        alt_idx: Integer;
        margin: Integer;
        score_gap: Integer;
        path_gap: Integer;
        function is_nonlex_full_complete_item(const sort_item: TncCandidateSortItem): Boolean;
        begin
            Result := (sort_item.candidate.comment = '') and
                (sort_item.text_units = m_last_lookup_syllable_count) and
                (sort_item.candidate.source <> cs_user) and
                (not sort_item.candidate.has_dict_weight);
        end;
    begin
        if list.Count < 2 then
        begin
            Exit;
        end;

        top_item := list[0];
        if is_nonlex_full_complete_item(top_item) then
        begin
            Exit;
        end;
        if top_item.is_prioritized_user_phrase or
            (top_item.candidate.source = cs_user) or
            top_item.is_latest_context_query_choice or top_item.is_latest_query_choice or
            (top_item.candidate.comment <> '') or
            (top_item.confidence_rank <= 1) then
        begin
            Exit;
        end;

        for alt_idx := 1 to Min(list.Count - 1, 4) do
        begin
            alt_item := list[alt_idx];
            if is_nonlex_full_complete_item(alt_item) then
            begin
                Continue;
            end;
            if alt_item.candidate.comment <> '' then
            begin
                Continue;
            end;
            if top_item.is_full_query_exact_dict_text and
                (alt_item.segment_path_count > 1) and
                (not (alt_item.is_sentence_particle_tail_split and
                (top_item.candidate.dict_weight < 1600))) then
            begin
                Continue;
            end;
            if alt_item.confidence_rank > top_item.confidence_rank then
            begin
                Continue;
            end;
            if (alt_item.confidence_rank = top_item.confidence_rank) and
                (alt_item.path_confidence_score <= top_item.path_confidence_score + 160) then
            begin
                Continue;
            end;
            if use_intent_layers and (alt_item.layer > top_item.layer + 1) then
            begin
                Continue;
            end;

            score_gap := top_item.rank_score - alt_item.rank_score;
            margin := Max(top_item.conservative_margin, alt_item.conservative_margin);
            path_gap := alt_item.path_confidence_score - top_item.path_confidence_score;
            if path_gap > 0 then
            begin
                Inc(margin, Min(96, path_gap div 3));
            end;
            if score_gap <= margin then
            begin
                swap_items(0, alt_idx);
                Break;
            end;
        end;
    end;

    procedure promote_short_exact_complete_score_leader;
    const
        c_short_exact_score_margin = 96;
    var
        idx: Integer;
        best_idx: Integer;
        best_score: Integer;
        item_score: Integer;
        top_score: Integer;
        picked: TncCandidateSortItem;

        function is_short_exact_complete_item(
            const sort_item: TncCandidateSortItem): Boolean;
        begin
            Result := (m_last_lookup_syllable_count >= 2) and
                (m_last_lookup_syllable_count <= 3) and
                (sort_item.candidate.comment = '') and
                (sort_item.text_units = m_last_lookup_syllable_count) and
                (sort_item.candidate.has_dict_weight or
                (sort_item.candidate.source = cs_user));
        end;

        function get_short_exact_order_score(
            const sort_item: TncCandidateSortItem): Integer;
        begin
            Result := sort_item.candidate.score;
            if sort_item.candidate.has_dict_weight then
            begin
                Result := sort_item.candidate.dict_weight;
            end;
            if sort_item.candidate.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;
    begin
        if (list.Count < 2) or (m_last_lookup_syllable_count < 2) or
            (m_last_lookup_syllable_count > 3) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_score := Low(Integer);
        for idx := 0 to list.Count - 1 do
        begin
            if not is_short_exact_complete_item(list[idx]) then
            begin
                Continue;
            end;

            item_score := get_short_exact_order_score(list[idx]);
            if (best_idx < 0) or (item_score > best_score) or
                ((item_score = best_score) and
                (list[idx].rank_score > list[best_idx].rank_score)) then
            begin
                best_idx := idx;
                best_score := item_score;
            end;
        end;

        if best_idx <= 0 then
        begin
            Exit;
        end;

        if (list[0].candidate.source = cs_user) and
            (list[best_idx].candidate.source <> cs_user) then
        begin
            Exit;
        end;

        top_score := get_short_exact_order_score(list[0]);
        if is_short_exact_complete_item(list[0]) and
            (best_score <= top_score + c_short_exact_score_margin) then
        begin
            Exit;
        end;

        picked := list[best_idx];
        for idx := best_idx downto 1 do
        begin
            list[idx] := list[idx - 1];
        end;
        list[0] := picked;
    end;
    function compare_candidate_sort_items(const left,
        right: TncCandidateSortItem): Integer;
    var
        left_protected_complete: Boolean;
        right_protected_complete: Boolean;
        left_exact_long_complete: Boolean;
        right_exact_long_complete: Boolean;
        left_exact_dict_complete: Boolean;
        right_exact_dict_complete: Boolean;
        left_nonlex_complete: Boolean;
        right_nonlex_complete: Boolean;
        left_nonlex_full_complete: Boolean;
        right_nonlex_full_complete: Boolean;
        left_generated_segment_complete: Boolean;
        right_generated_segment_complete: Boolean;
        left_path_segment_count: Integer;
        right_path_segment_count: Integer;
    begin
        if left.is_fixed_top_single_char and (not right.is_fixed_top_single_char) then
        begin
            Result := -1;
            Exit;
        end;
        if right.is_fixed_top_single_char and (not left.is_fixed_top_single_char) then
        begin
            Result := 1;
            Exit;
        end;

        if (left.short_particle_complete_text <> '') and
            SameText(left.short_particle_complete_text,
            right.short_particle_partial_complete_text) then
        begin
            Result := -1;
            Exit;
        end;
        if (right.short_particle_complete_text <> '') and
            SameText(right.short_particle_complete_text,
            left.short_particle_partial_complete_text) then
        begin
            Result := 1;
            Exit;
        end;

        left_protected_complete := (left.candidate.comment = '') and
            ((left.candidate.source = cs_user) or left.is_weighted_complete_phrase);
        right_protected_complete := (right.candidate.comment = '') and
            ((right.candidate.source = cs_user) or right.is_weighted_complete_phrase);
        left_exact_long_complete := (m_last_lookup_syllable_count >= 4) and
            (left.candidate.comment = '') and
            (left.text_units = m_last_lookup_syllable_count) and
            ((left.candidate.source = cs_user) or left.is_weighted_complete_phrase);
        right_exact_long_complete := (m_last_lookup_syllable_count >= 4) and
            (right.candidate.comment = '') and
            (right.text_units = m_last_lookup_syllable_count) and
            ((right.candidate.source = cs_user) or right.is_weighted_complete_phrase);
        left_exact_dict_complete := (left.candidate.comment = '') and
            left.candidate.has_dict_weight and
            (left.text_units = m_last_lookup_syllable_count);
        right_exact_dict_complete := (right.candidate.comment = '') and
            right.candidate.has_dict_weight and
            (right.text_units = m_last_lookup_syllable_count);
        left_nonlex_complete := (left.candidate.comment = '') and
            (left.text_units >= 2) and
            (left.candidate.source <> cs_user) and
            (not left.candidate.has_dict_weight);
        right_nonlex_complete := (right.candidate.comment = '') and
            (right.text_units >= 2) and
            (right.candidate.source <> cs_user) and
            (not right.candidate.has_dict_weight);
        left_nonlex_full_complete := (left.candidate.comment = '') and
            (left.text_units = m_last_lookup_syllable_count) and
            (left.candidate.source <> cs_user) and
            (not left.candidate.has_dict_weight);
        right_nonlex_full_complete := (right.candidate.comment = '') and
            (right.text_units = m_last_lookup_syllable_count) and
            (right.candidate.source <> cs_user) and
            (not right.candidate.has_dict_weight);
        left_generated_segment_complete := (left.candidate.comment = '') and
            (left.text_units = m_last_lookup_syllable_count) and
            (left.candidate.source <> cs_user) and
            (not left.is_full_query_exact_dict_text) and
            (left.segment_path_count > 1);
        right_generated_segment_complete := (right.candidate.comment = '') and
            (right.text_units = m_last_lookup_syllable_count) and
            (right.candidate.source <> cs_user) and
            (not right.is_full_query_exact_dict_text) and
            (right.segment_path_count > 1);

        if left_nonlex_full_complete and right_nonlex_full_complete then
        begin
            left_path_segment_count := left.segment_path_count;
            right_path_segment_count := right.segment_path_count;

            if (left_path_segment_count <> right_path_segment_count) and
                (Abs(left.candidate.score - right.candidate.score) >= 64) then
            begin
                if (left.candidate.score > right.candidate.score) and
                    (left_path_segment_count < right_path_segment_count) then
                begin
                    Result := -1;
                    Exit;
                end;
                if (right.candidate.score > left.candidate.score) and
                    (right_path_segment_count < left_path_segment_count) then
                begin
                    Result := 1;
                    Exit;
                end;
            end;
        end;

        if (m_last_lookup_syllable_count = 3) then
        begin
            if left.is_supported_head_phrase_partial and
                right.is_two_plus_one_partial and
                (not right.is_supported_head_phrase_partial) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_supported_head_phrase_partial and
                left.is_two_plus_one_partial and
                (not left.is_supported_head_phrase_partial) then
            begin
                Result := 1;
                Exit;
            end;
            if left.is_base_exact_prefix_partial and right.is_user_prefix_partial then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_base_exact_prefix_partial and left.is_user_prefix_partial then
            begin
                Result := 1;
                Exit;
            end;
            if left.is_supported_head_phrase_partial and
                right_nonlex_full_complete and
                (not right_protected_complete) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_supported_head_phrase_partial and
                left_nonlex_full_complete and
                (not left_protected_complete) then
            begin
                Result := 1;
                Exit;
            end;
            if left.is_two_plus_one_partial and
                left.is_weighted_partial_phrase and
                right_nonlex_full_complete and
                (not right_protected_complete) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_two_plus_one_partial and
                right.is_weighted_partial_phrase and
                left_nonlex_full_complete and
                (not left_protected_complete) then
            begin
                Result := 1;
                Exit;
            end;
            if left.is_two_plus_one_partial and
                left.is_weighted_partial_phrase and
                right_generated_segment_complete and
                (not right.is_sentence_particle_tail_split) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_two_plus_one_partial and
                right.is_weighted_partial_phrase and
                left_generated_segment_complete and
                (not left.is_sentence_particle_tail_split) then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if m_last_lookup_syllable_count >= 2 then
        begin
            if left.is_full_query_exact_dict_text and
                (right.candidate.comment <> '') then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_full_query_exact_dict_text and
                (left.candidate.comment <> '') then
            begin
                Result := 1;
                Exit;
            end;

            if left_exact_dict_complete and
                (right.candidate.source = cs_user) and
                (not right.candidate.has_dict_weight) and
                (right.candidate.score <= 8) then
            begin
                Result := -1;
                Exit;
            end;
            if right_exact_dict_complete and
                (left.candidate.source = cs_user) and
                (not left.candidate.has_dict_weight) and
                (left.candidate.score <= 8) then
            begin
                Result := 1;
                Exit;
            end;

            if (left.candidate.comment = '') and
                (right.candidate.comment = '') and
                (left.candidate.source = cs_user) and
                (right.candidate.source <> cs_user) and
                (not left.is_problematic_single_char_chain) then
            begin
                Result := -1;
                Exit;
            end;
            if (left.candidate.comment = '') and
                (right.candidate.comment = '') and
                (right.candidate.source = cs_user) and
                (left.candidate.source <> cs_user) and
                (not right.is_problematic_single_char_chain) then
            begin
                Result := 1;
                Exit;
            end;

            if left.is_full_query_exact_dict_text and
                (not right.is_full_query_exact_dict_text) and
                (right.candidate.comment = '') and
                (right.text_units = m_last_lookup_syllable_count) and
                (right.candidate.source <> cs_user) and
                (not (right.is_sentence_particle_tail_split and
                (left.full_query_exact_weight < 1600))) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_full_query_exact_dict_text and
                (not left.is_full_query_exact_dict_text) and
                (left.candidate.comment = '') and
                (left.text_units = m_last_lookup_syllable_count) and
                (left.candidate.source <> cs_user) and
                (not (left.is_sentence_particle_tail_split and
                (right.full_query_exact_weight < 1600))) then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if has_competing_exact_phrase and (m_last_lookup_syllable_count <= 6) then
        begin
            if left_exact_dict_complete and right_nonlex_complete then
            begin
                Result := -1;
                Exit;
            end;
            if right_exact_dict_complete and left_nonlex_complete then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if c_suppress_nonlexicon_complete_long_candidates and
            (m_last_lookup_syllable_count >= 4) then
        begin
            if left_exact_long_complete and
                ((right.candidate.comment <> '') or (not right_exact_long_complete)) then
            begin
                Result := -1;
                Exit;
            end;
            if right_exact_long_complete and
                ((left.candidate.comment <> '') or (not left_exact_long_complete)) then
            begin
                Result := 1;
                Exit;
            end;

            if (left.candidate.comment <> '') and (right.candidate.comment <> '') then
            begin
                if (left.text_units >= 2) and (left.partial_comment_syllables > 0) and
                    (left.text_units + left.partial_comment_syllables =
                    m_last_lookup_syllable_count) and
                    (right.text_units = 1) and
                    (right.partial_comment_syllables = m_last_lookup_syllable_count - 1) then
                begin
                    Result := -1;
                    Exit;
                end;
                if (right.text_units >= 2) and (right.partial_comment_syllables > 0) and
                    (right.text_units + right.partial_comment_syllables =
                    m_last_lookup_syllable_count) and
                    (left.text_units = 1) and
                    (left.partial_comment_syllables = m_last_lookup_syllable_count - 1) then
                begin
                    Result := 1;
                    Exit;
                end;
            end;
        end;

        if has_weighted_complete_phrase and
            (left.candidate.comment = '') and (right.candidate.comment = '') then
        begin
            if left.is_weighted_complete_phrase and right.is_problematic_single_char_chain then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_weighted_complete_phrase and left.is_problematic_single_char_chain then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if (left.candidate.comment = '') and (right.candidate.comment = '') then
        begin
            if left.is_latest_context_query_choice and
                (not right.is_latest_context_query_choice) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_latest_context_query_choice and
                (not left.is_latest_context_query_choice) then
            begin
                Result := 1;
                Exit;
            end;
            if left.is_latest_query_choice and (not right.is_latest_query_choice) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_latest_query_choice and (not left.is_latest_query_choice) then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if left.is_prioritized_user_phrase and (not right.is_prioritized_user_phrase) and
            (not (has_competing_exact_phrase and left.is_problematic_single_char_chain)) then
        begin
            Result := -1;
            Exit;
        end;
        if right.is_prioritized_user_phrase and (not left.is_prioritized_user_phrase) and
            (not (has_competing_exact_phrase and right.is_problematic_single_char_chain)) then
        begin
            Result := 1;
            Exit;
        end;

        if left.is_demonstrative_head_friendly_partial and
            right.is_weighted_complete_phrase and
            (left.rank_score >= right.rank_score + 80) then
        begin
            Result := -1;
            Exit;
        end;
        if right.is_demonstrative_head_friendly_partial and
            left.is_weighted_complete_phrase and
            (right.rank_score >= left.rank_score + 80) then
        begin
            Result := 1;
            Exit;
        end;

        if c_suppress_nonlexicon_complete_long_candidates and
            (m_last_lookup_syllable_count >= 3) then
        begin
            if preferred_three_syllable_partial_kind = 1 then
            begin
                if left.is_one_plus_two_partial and
                    right.is_supported_head_phrase_partial and
                    right.is_two_plus_one_partial and
                    (right.rank_score >= left.rank_score +
                    c_supported_head_phrase_partial_override_gap) then
                begin
                    Result := 1;
                    Exit;
                end;
                if right.is_one_plus_two_partial and
                    left.is_supported_head_phrase_partial and
                    left.is_two_plus_one_partial and
                    (left.rank_score >= right.rank_score +
                    c_supported_head_phrase_partial_override_gap) then
                begin
                    Result := -1;
                    Exit;
                end;

                if left.is_one_plus_two_partial and
                    (((not right.is_one_plus_two_partial) and
                    (not right_protected_complete)) or
                    ((right.is_one_plus_two_partial) and
                    (left.rank_score > right.rank_score + c_three_syllable_partial_shape_margin))) then
                begin
                    Result := -1;
                    Exit;
                end;
                if right.is_one_plus_two_partial and
                    (((not left.is_one_plus_two_partial) and
                    (not left_protected_complete)) or
                    ((left.is_one_plus_two_partial) and
                    (right.rank_score > left.rank_score + c_three_syllable_partial_shape_margin))) then
                begin
                    Result := 1;
                    Exit;
                end;
            end
            else if preferred_three_syllable_partial_kind = 2 then
            begin
                if left.is_two_plus_one_partial and
                    (((not right.is_two_plus_one_partial) and
                    (not right_protected_complete)) or
                    ((right.is_two_plus_one_partial) and
                    (left.rank_score > right.rank_score + c_three_syllable_partial_shape_margin))) then
                begin
                    Result := -1;
                    Exit;
                end;
                if right.is_two_plus_one_partial and
                    (((not left.is_two_plus_one_partial) and
                    (not left_protected_complete)) or
                    ((left.is_two_plus_one_partial) and
                    (right.rank_score > left.rank_score + c_three_syllable_partial_shape_margin))) then
                begin
                    Result := 1;
                    Exit;
                end;
            end;

            if left.is_boundary_anchor_partial and (not right.is_boundary_anchor_partial) and
                (((right.candidate.comment <> '')) or
                ((right.candidate.comment = '') and
                (right.candidate.source <> cs_user) and
                (not right.is_weighted_complete_phrase))) then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_boundary_anchor_partial and (not left.is_boundary_anchor_partial) and
                (((left.candidate.comment <> '')) or
                ((left.candidate.comment = '') and
                (left.candidate.source <> cs_user) and
                (not left.is_weighted_complete_phrase))) then
            begin
                Result := 1;
                Exit;
            end;
        end;

        // Learned user candidates should take priority over rule
        // candidates when both are complete commit candidates.
        if (left.candidate.comment = '') and (right.candidate.comment = '') then
        begin
        if (left.candidate.source = cs_user) and (right.candidate.source <> cs_user) and
            (not (has_competing_exact_phrase and left.is_problematic_single_char_chain)) then
        begin
            Result := -1;
            Exit;
        end;
        if (right.candidate.source = cs_user) and (left.candidate.source <> cs_user) and
            (not (has_competing_exact_phrase and right.is_problematic_single_char_chain)) then
        begin
            Result := 1;
            Exit;
        end;
    end;

        if has_weighted_complete_phrase and
            (left.candidate.comment = '') and (right.candidate.comment = '') then
        begin
            if left.is_weighted_complete_phrase and right.is_problematic_single_char_chain then
            begin
                Result := -1;
                Exit;
            end;
            if right.is_weighted_complete_phrase and left.is_problematic_single_char_chain then
            begin
                Result := 1;
                Exit;
            end;
        end;

        if (left.candidate.comment = '') and (right.candidate.comment = '') then
        begin
            if left.is_problematic_single_char_chain and
                (left.candidate.source <> cs_user) and
                (not right.is_problematic_single_char_chain) and
                (right.text_units >= 2) then
            begin
                Result := 1;
                Exit;
            end;
            if right.is_problematic_single_char_chain and
                (right.candidate.source <> cs_user) and
                (not left.is_problematic_single_char_chain) and
                (left.text_units >= 2) then
            begin
                Result := -1;
                Exit;
            end;
        end;

        if use_intent_layers then
        begin
            Result := left.layer - right.layer;
            if Result <> 0 then
            begin
                Exit;
            end;
        end;

        if (left.candidate.comment = '') and (right.candidate.comment = '') and
            (left.confidence_rank <> right.confidence_rank) and
            (Abs(left.rank_score - right.rank_score) <=
            Max(left.conservative_margin, right.conservative_margin)) then
        begin
            Result := left.confidence_rank - right.confidence_rank;
            if Result <> 0 then
            begin
                Exit;
            end;
        end;
        if (left.candidate.comment = '') and (right.candidate.comment = '') and
            (left.path_confidence_score <> right.path_confidence_score) and
            (Abs(left.rank_score - right.rank_score) <=
            Max(left.conservative_margin, right.conservative_margin)) and
            (Abs(left.path_confidence_score - right.path_confidence_score) >= 120) then
        begin
            Result := right.path_confidence_score - left.path_confidence_score;
            if Result <> 0 then
            begin
                Exit;
            end;
        end;

        Result := right.rank_score - left.rank_score;
        if Result = 0 then
        begin
            Result := left.source_rank - right.source_rank;
            if Result = 0 then
            begin
                Result := left.text_length - right.text_length;
                if Result = 0 then
                begin
                    Result := CompareText(left.candidate.text, right.candidate.text);
                end;
            end;
        end;
    end;

    procedure stable_sort_candidate_items;
    var
        item_index: Integer;
        insert_index: Integer;
        pending_item: TncCandidateSortItem;
    begin
        for item_index := 1 to list.Count - 1 do
        begin
            pending_item := list[item_index];
            insert_index := item_index - 1;
            while (insert_index >= 0) and
                (compare_candidate_sort_items(list[insert_index],
                pending_item) > 0) do
            begin
                list[insert_index + 1] := list[insert_index];
                Dec(insert_index);
            end;
            list[insert_index + 1] := pending_item;
        end;
    end;
begin
    if Length(candidates) <= 1 then
    begin
        Exit;
    end;

    use_intent_layers := m_last_lookup_syllable_count >= 3;
    has_strong_complete_phrase := False;
    has_competing_exact_phrase := False;
    has_weighted_complete_phrase := False;
    weak_chain_query_prepared := False;
    SetLength(weak_chain_query_syllables, 0);
    SetLength(weak_chain_preference_maps, 0);
    partial_comment_syllable_cache := TDictionary<string, Integer>.Create;
    best_one_plus_two_partial_score := Low(Integer);
    best_two_plus_one_partial_score := Low(Integer);
    has_boundary_anchor_one_plus_two := False;
    has_boundary_anchor_two_plus_one := False;
    preferred_three_syllable_partial_kind := m_last_three_syllable_partial_preference_kind;
    fixed_top_single_char_text := get_fixed_top_single_char_for_query_local;
    full_query_exact_text_weights := nil;

    list := TList<TncCandidateSortItem>.Create;
    try
        list.Capacity := Length(candidates);
        for i := 0 to High(candidates) do
        begin
            item.candidate := candidates[i];
            item.rank_score := get_rank_score(candidates[i]);
            if use_intent_layers then
            begin
                item.layer := get_multi_syllable_intent_layer(candidates[i]);
            end
            else
            begin
                item.layer := 0;
            end;
            item.confidence_rank := get_candidate_confidence_rank(candidates[i]);
            item.path_confidence_score := get_candidate_path_confidence_score(candidates[i]);
            if is_runtime_chain_candidate(candidates[i]) then
            begin
                item.conservative_margin := 280;
            end
            else if is_runtime_common_pattern_candidate(candidates[i]) then
            begin
                item.conservative_margin := 190;
            end
            else if is_runtime_redup_candidate(candidates[i]) then
            begin
                item.conservative_margin := 160;
            end
            else if (candidates[i].source = cs_rule) and (not candidates[i].has_dict_weight) then
            begin
                item.conservative_margin := 140;
            end
            else
            begin
                item.conservative_margin := 96;
            end;
            item.source_rank := get_source_rank(candidates[i].source);
            item.text_length := Length(candidates[i].text);
            item.text_units := get_display_text_unit_count(candidates[i].text);
            item.is_latest_context_query_choice := get_context_query_latest_bonus(candidates[i].text) > 0;
            item.is_latest_query_choice := is_latest_session_query_choice(candidates[i].text);
            item.is_weak_single_char_chain := is_weak_single_char_chain_candidate_cached(candidates[i]);
            item.is_problematic_single_char_chain :=
                is_problematic_single_char_chain_candidate_cached(candidates[i]);
            item.is_weighted_complete_phrase := is_weighted_complete_phrase_candidate(candidates[i]);
            item.is_runtime_constructed_complete_phrase :=
                is_runtime_constructed_complete_phrase_candidate(candidates[i]);
            item.is_prioritized_user_phrase := (candidates[i].comment = '') and
                (candidates[i].source = cs_user) and (item.text_units >= 2);
            item.is_one_plus_two_partial := False;
            item.is_two_plus_one_partial := False;
            item.is_weighted_partial_phrase := False;
            item.is_complete_phrase_prefix_partial := False;
            item.is_supported_head_phrase_partial := False;
            item.is_base_exact_prefix_partial := False;
            item.is_user_prefix_partial := False;
            candidate_path_score_hint := get_candidate_segment_path_score_hint(candidates[i]);
            item.is_boundary_anchor_partial := (candidates[i].comment <> '') and
                (candidate_path_score_hint >= c_boundary_anchor_partial_score_hint_flag);
            if item.is_boundary_anchor_partial then
            begin
                case ((candidate_path_score_hint - c_boundary_anchor_partial_score_hint_flag) div
                    c_boundary_anchor_partial_shape_kind_scale) of
                    c_boundary_anchor_partial_shape_one_plus_two:
                    begin
                        item.is_one_plus_two_partial := True;
                        item.is_two_plus_one_partial := False;
                    end;
                    c_boundary_anchor_partial_shape_two_plus_one:
                    begin
                        item.is_one_plus_two_partial := False;
                        item.is_two_plus_one_partial := True;
                    end;
                end;
            end;
            if candidates[i].comment <> '' then
            begin
                item.partial_comment_syllables := get_partial_comment_syllable_count(candidates[i].comment);
            end
            else
            begin
                item.partial_comment_syllables := 0;
            end;
            item.is_one_plus_two_partial := item.is_one_plus_two_partial or
                ((m_last_lookup_syllable_count = 3) and
                (item.partial_comment_syllables = 2) and (item.text_units = 1));
            item.is_two_plus_one_partial := item.is_two_plus_one_partial or
                ((m_last_lookup_syllable_count = 3) and
                (item.partial_comment_syllables = 1) and (item.text_units >= 2));
            item.is_weighted_partial_phrase := (candidates[i].comment <> '') and
                (item.text_units >= 2) and
                (candidates[i].has_dict_weight or (candidates[i].source = cs_user) or
                ((candidates[i].source = cs_rule) and
                (not is_runtime_chain_candidate(candidates[i])) and
                (not is_runtime_common_pattern_candidate(candidates[i])) and
                (not is_runtime_redup_candidate(candidates[i]))));
            item.is_complete_phrase_prefix_partial :=
                is_complete_phrase_prefix_partial_candidate(candidates[i],
                item.text_units, item.partial_comment_syllables);
            item.is_base_exact_prefix_partial :=
                is_base_exact_prefix_partial_candidate(candidates[i],
                item.text_units, item.partial_comment_syllables);
            item.is_user_prefix_partial := (candidates[i].comment <> '') and
                (candidates[i].source = cs_user) and
                (not item.is_base_exact_prefix_partial);
            if item.is_complete_phrase_prefix_partial then
            begin
                Inc(item.rank_score, c_complete_phrase_prefix_partial_rank_bonus);
            end;
            item.is_supported_head_phrase_partial :=
                is_supported_head_phrase_partial_candidate(item);
            if item.is_supported_head_phrase_partial then
            begin
                Inc(item.rank_score, c_supported_head_phrase_partial_rank_bonus);
            end;
            item.is_demonstrative_head_friendly_partial :=
                is_demonstrative_head_friendly_partial_candidate_cached(candidates[i]);
            if item.is_demonstrative_head_friendly_partial then
            begin
                Inc(item.rank_score, c_demonstrative_head_friendly_partial_rank_bonus);
            end;
            Inc(item.rank_score,
                get_sentence_path_bridge_adjustment(candidates[i]));
            item.is_fixed_top_single_char := (fixed_top_single_char_text <> '') and
                (item.candidate.comment = '') and (item.text_units = 1) and
                (Trim(item.candidate.text) = fixed_top_single_char_text);
            item.segment_path_count := get_encoded_path_segment_count_local(
                get_segment_path_for_candidate(candidates[i]));
            item.is_full_query_exact_dict_text :=
                (item.candidate.comment = '') and
                (item.text_units = m_last_lookup_syllable_count) and
                try_get_full_query_exact_weight_local(item.candidate.text,
                item.full_query_exact_weight);
            if not item.is_full_query_exact_dict_text then
            begin
                item.full_query_exact_weight := 0;
            end;
            item.is_sentence_particle_tail_split :=
                is_sentence_particle_tail_split_for_sort(candidates[i],
                item.segment_path_count);
            item.short_particle_complete_text := '';
            item.short_particle_partial_complete_text := '';
            if is_full_pinyin_key(m_last_lookup_key) then
            begin
                if (Trim(candidates[i].comment) = '') and
                    (item.text_units >= 3) and (item.text_units <= 4) then
                begin
                    item.short_particle_complete_text := Trim(candidates[i].text);
                end
                else if (Trim(candidates[i].comment) <> '') and
                    (item.partial_comment_syllables = 1) and
                    (item.text_units >= 2) and (item.text_units <= 3) and
                    (get_short_particle_tail_text_for_sort(
                    candidates[i].comment) <> '') then
                begin
                    item.short_particle_partial_complete_text :=
                        Trim(candidates[i].text) +
                        get_short_particle_tail_text_for_sort(
                        candidates[i].comment);
                end;
            end;
            if is_overlong_complete_full_key_item(item) then
            begin
                Dec(item.rank_score,
                    c_overlong_complete_full_key_sort_penalty +
                    ((item.text_units - m_last_lookup_syllable_count) * 180));
                item.is_latest_context_query_choice := False;
                item.is_latest_query_choice := False;
                item.is_prioritized_user_phrase := False;
            end;
            if use_intent_layers and (item.candidate.comment = '') and
                (item.layer <= 1) and (get_candidate_text_unit_count(item.candidate.text) >= 2) and
                (item.candidate.has_dict_weight or (item.candidate.source = cs_user)) then
            begin
                has_strong_complete_phrase := True;
            end;
            if (item.candidate.comment = '') and (get_candidate_text_unit_count(item.candidate.text) >= 2) and
                (item.candidate.has_dict_weight or (item.candidate.source = cs_user)) then
            begin
                has_competing_exact_phrase := True;
            end;
            if item.is_weighted_complete_phrase then
            begin
                has_weighted_complete_phrase := True;
            end;
            if item.is_one_plus_two_partial and (item.rank_score > best_one_plus_two_partial_score) then
            begin
                best_one_plus_two_partial_score := item.rank_score;
            end;
            if item.is_two_plus_one_partial and (item.rank_score > best_two_plus_one_partial_score) then
            begin
                best_two_plus_one_partial_score := item.rank_score;
            end;
            if item.is_boundary_anchor_partial and item.is_one_plus_two_partial then
            begin
                has_boundary_anchor_one_plus_two := True;
            end;
            if item.is_boundary_anchor_partial and item.is_two_plus_one_partial then
            begin
                has_boundary_anchor_two_plus_one := True;
            end;
            list.Add(item);
        end;

        if (preferred_three_syllable_partial_kind = 0) and
            c_suppress_nonlexicon_complete_long_candidates and
            (m_last_lookup_syllable_count >= 3) then
        begin
            if has_boundary_anchor_one_plus_two and (not has_boundary_anchor_two_plus_one) then
            begin
                preferred_three_syllable_partial_kind := 1;
            end
            else if has_boundary_anchor_two_plus_one and (not has_boundary_anchor_one_plus_two) then
            begin
                preferred_three_syllable_partial_kind := 2;
            end;
        end;

        if (preferred_three_syllable_partial_kind = 0) and
            c_suppress_nonlexicon_complete_long_candidates and
            (m_last_lookup_syllable_count >= 3) then
        begin
            if (best_one_plus_two_partial_score > Low(Integer)) and
                ((best_two_plus_one_partial_score <= Low(Integer)) or
                ((best_two_plus_one_partial_score * 100) <=
                (best_one_plus_two_partial_score * c_three_syllable_boundary_ratio_pct))) then
            begin
                preferred_three_syllable_partial_kind := 1;
            end
            else if best_two_plus_one_partial_score > Low(Integer) then
            begin
                preferred_three_syllable_partial_kind := 2;
            end;
        end;

        if c_suppress_nonlexicon_complete_long_candidates and
            (m_last_lookup_syllable_count >= 3) and
            (preferred_three_syllable_partial_kind <> 0) then
        begin
            for i := 0 to list.Count - 1 do
            begin
                item := list[i];
                if (preferred_three_syllable_partial_kind = 1) and item.is_two_plus_one_partial then
                begin
                    if item.is_complete_phrase_prefix_partial then
                    begin
                    end
                    else if item.is_weighted_partial_phrase then
                    begin
                        Dec(item.rank_score, c_strong_exact_partial_shape_penalty);
                    end
                    else
                    begin
                        Dec(item.rank_score, 880);
                    end;
                end
                else if (preferred_three_syllable_partial_kind = 2) and item.is_one_plus_two_partial then
                begin
                    Dec(item.rank_score, 880);
                end;
                list[i] := item;
            end;
        end;

        if use_intent_layers and has_strong_complete_phrase then
        begin
            for i := 0 to list.Count - 1 do
            begin
                item := list[i];
                if is_runtime_chain_candidate(item.candidate) then
                begin
                    Dec(item.rank_score, 220);
                end
                else if is_runtime_common_pattern_candidate(item.candidate) then
                begin
                    Dec(item.rank_score, 140);
                end
                else if is_runtime_redup_candidate(item.candidate) then
                begin
                    Dec(item.rank_score, 96);
                end;
                list[i] := item;
            end;
        end;

        if has_competing_exact_phrase then
        begin
            for i := 0 to list.Count - 1 do
            begin
                item := list[i];
                if item.is_weak_single_char_chain then
                begin
                    if (item.candidate.source = cs_user) and (item.candidate.score <= 1) then
                    begin
                        Dec(item.rank_score, c_user_score_bonus + 240);
                    end
                    else if is_runtime_chain_candidate(item.candidate) then
                    begin
                        Dec(item.rank_score, 520);
                    end
                    else
                    begin
                        Dec(item.rank_score, 360);
                    end;
                    item.path_confidence_score := 0;
                end
                else if item.is_problematic_single_char_chain then
                begin
                    Dec(item.rank_score, 680);
                    item.path_confidence_score := 0;
                end;
                list[i] := item;
            end;
        end;

        // Precompute heavy ranking keys once per candidate to avoid repeatedly
        // recomputing context/session bonuses during every comparison.
        stable_sort_candidate_items;

        apply_front_row_conservative_guard;
        promote_short_exact_complete_score_leader;

        for i := 0 to High(candidates) do
        begin
            candidates[i] := list[i].candidate;
        end;
    finally
        for i := 0 to High(weak_chain_preference_maps) do
        begin
            if weak_chain_preference_maps[i] <> nil then
            begin
                weak_chain_preference_maps[i].Free;
                weak_chain_preference_maps[i] := nil;
            end;
        end;
        partial_comment_syllable_cache.Free;
        if full_query_exact_text_weights <> nil then
        begin
            full_query_exact_text_weights.Free;
        end;
        list.Free;
    end;
end;

function TncEngine.normalize_pinyin_text(const input_text: string): string;
var
    i: Integer;
    ch: Char;
    spelling: string;
begin
    Result := '';
    if input_text = '' then
    begin
        Exit;
    end;

    spelling := nc_normalize_umlaut_spelling(input_text);
    SetLength(Result, Length(spelling));
    for i := 1 to Length(spelling) do
    begin
        ch := spelling[i];
        if ch = '''' then
        begin
            Result[i] := #0;
        end
        else if (ch >= 'A') and (ch <= 'Z') then
        begin
            Result[i] := Char(Ord(ch) + Ord('a') - Ord('A'));
        end
        else
        begin
            Result[i] := ch;
        end;
    end;

    Result := StringReplace(Result, #0, '', [rfReplaceAll]);
end;

function TncEngine.is_shuangpin_input: Boolean;
begin
    Result := nc_is_shuangpin_scheme(m_config.pinyin_input_scheme);
end;

procedure TncEngine.rebuild_shuangpin_composition_from_input_code;
var
    decoded: TncShuangpinDecodeResult;
begin
    if not is_shuangpin_input then
    begin
        Exit;
    end;
    decoded := nc_decode_shuangpin(m_config.pinyin_input_scheme,
        m_composition_display_text);
    m_composition_text := decoded.canonical_text;
end;

function TncEngine.get_shuangpin_remaining_input_code_for_partial(
    const selected_text: string): string;
var
    decoded: TncShuangpinDecodeResult;
    consumed_units: Integer;
begin
    Result := '';
    if (not is_shuangpin_input) or (m_composition_display_text = '') then
    begin
        Exit;
    end;

    consumed_units := get_candidate_text_unit_count(Trim(selected_text));
    if consumed_units <= 0 then
    begin
        Exit;
    end;
    decoded := nc_decode_shuangpin(m_config.pinyin_input_scheme,
        m_composition_display_text);
    Result := nc_shuangpin_raw_suffix_after_units(decoded, consumed_units);
end;

function TncEngine.is_valid_pinyin_syllable(const syllable: string): Boolean;
var
    ch: Char;
begin
    Result := False;
    if syllable = '' then
    begin
        Exit;
    end;

    // Single-letter syllables are only valid for standalone finals.
    if Length(syllable) = 1 then
    begin
        Result := CharInSet(syllable[1], ['a', 'e', 'o']);
        Exit;
    end;

    for ch in syllable do
    begin
        if CharInSet(ch, ['a', 'e', 'i', 'o', 'u', 'v']) then
        begin
            Result := True;
            Exit;
        end;
    end;
end;

function TncEngine.is_full_pinyin_key(const value: string): Boolean;
const
    c_full_pinyin_key_cache_limit = 4096;
var
    parser: TncPinyinParser;
    syllables: TncPinyinParseResult;
    idx: Integer;
    reconstructed: string;
    normalized_value: string;
    cached_value: Boolean;
begin
    Result := False;
    normalized_value := LowerCase(Trim(value));
    if normalized_value = '' then
    begin
        Exit;
    end;

    if (m_full_pinyin_key_cache <> nil) and
        m_full_pinyin_key_cache.TryGetValue(normalized_value, cached_value) then
    begin
        Exit(cached_value);
    end;

    parser := TncPinyinParser.create;
    try
        syllables := parser.parse(normalized_value);
    finally
        parser.Free;
    end;

    if Length(syllables) <= 0 then
    begin
        Result := False;
        if m_full_pinyin_key_cache <> nil then
        begin
            if m_full_pinyin_key_cache.Count >= c_full_pinyin_key_cache_limit then
            begin
                m_full_pinyin_key_cache.Clear;
            end;
            m_full_pinyin_key_cache.AddOrSetValue(normalized_value, Result);
        end;
        Exit;
    end;

    reconstructed := '';
    for idx := 0 to High(syllables) do
    begin
        if not is_valid_pinyin_syllable(syllables[idx].text) then
        begin
            Result := False;
            if m_full_pinyin_key_cache <> nil then
            begin
                if m_full_pinyin_key_cache.Count >= c_full_pinyin_key_cache_limit then
                begin
                    m_full_pinyin_key_cache.Clear;
                end;
                m_full_pinyin_key_cache.AddOrSetValue(normalized_value, Result);
            end;
            Exit;
        end;
        reconstructed := reconstructed + syllables[idx].text;
    end;

    Result := SameText(reconstructed, normalized_value);
    if m_full_pinyin_key_cache <> nil then
    begin
        if m_full_pinyin_key_cache.Count >= c_full_pinyin_key_cache_limit then
        begin
            m_full_pinyin_key_cache.Clear;
        end;
        m_full_pinyin_key_cache.AddOrSetValue(normalized_value, Result);
    end;
end;

function TncEngine.get_effective_compact_pinyin_syllables(const input_text: string;
    const allow_relaxed_split: Boolean): TncPinyinParseResult;
const
    c_effective_pinyin_parse_cache_limit = 8192;
    c_initials_local: array[0..22] of string = (
        'zh', 'ch', 'sh',
        'b', 'p', 'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h',
        'j', 'q', 'x', 'r', 'z', 'c', 's', 'y', 'w'
    );
    c_finals_local: array[0..35] of string = (
        'iang', 'iong', 'uang',
        'uai', 'uan', 'iao', 'ian', 'ing', 'ang', 'eng', 'ong',
        'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
        'ia', 'ie', 'in', 'iu', 'ua', 'ui', 'un', 'uo',
        've', 'van', 'vn', 'ue',
        'a', 'e', 'i', 'o', 'u', 'v'
    );
    c_finals_no_initial_local: array[0..11] of string = (
        'ang', 'eng',
        'ai', 'an', 'ao', 'ei', 'en', 'er', 'ou',
        'a', 'e', 'o'
    );
var
    parser: TncPinyinParser;
    syllables: TncPinyinParseResult;
    idx: Integer;
    tail_text: string;
    merged_count: Integer;
    tail_idx: Integer;
    cache_key: string;
    cached_result: TncPinyinParseResult;
    current_composition_query: Boolean;

    function is_initial_final_compatible_local(const initial_value: string;
        const final_value: string): Boolean;
    begin
        if not nc_is_pinyin_spelling_helper_compatible(initial_value,
            final_value) then
        begin
            Exit(False);
        end;

        if (initial_value = 'zh') or (initial_value = 'ch') or (initial_value = 'sh') or
            (initial_value = 'r') or (initial_value = 'z') or (initial_value = 'c') or
            (initial_value = 's') then
        begin
            if (final_value = 'ia') or (final_value = 'in') or (final_value = 'ing') or (final_value = 'iu') or
                (final_value = 'ie') or (final_value = 'ian') or (final_value = 'iang') or
                (final_value = 'iao') or (final_value = 'iong') or
                (final_value = 'ue') or (final_value = 've') or (final_value = 'van') or
                (final_value = 'vn') then
            begin
                Exit(False);
            end;
        end;

        Result := True;
    end;

    function is_single_syllable_prefix_text(const value: string): Boolean;
    var
        lower_value: string;
        initial_idx: Integer;
        final_idx: Integer;
        full_syllable: string;
    begin
        Result := False;
        lower_value := LowerCase(Trim(value));
        if lower_value = '' then
        begin
            Exit;
        end;

        for initial_idx := Low(c_initials_local) to High(c_initials_local) do
        begin
            for final_idx := Low(c_finals_local) to High(c_finals_local) do
            begin
                if not is_initial_final_compatible_local(c_initials_local[initial_idx],
                    c_finals_local[final_idx]) then
                begin
                    Continue;
                end;

                full_syllable := c_initials_local[initial_idx] + c_finals_local[final_idx];
                if Copy(full_syllable, 1, Length(lower_value)) = lower_value then
                begin
                    Exit(True);
                end;
            end;
        end;

        for final_idx := Low(c_finals_no_initial_local) to High(c_finals_no_initial_local) do
        begin
            full_syllable := c_finals_no_initial_local[final_idx];
            if Copy(full_syllable, 1, Length(lower_value)) = lower_value then
            begin
                Exit(True);
            end;
        end;
    end;

    function build_tail_text(const start_index: Integer): string;
    var
        tail_idx: Integer;
    begin
        Result := '';
        if (start_index < 0) or (start_index > High(syllables)) then
        begin
            Exit;
        end;

        for tail_idx := start_index to High(syllables) do
        begin
            Result := Result + syllables[tail_idx].text;
        end;
    end;

    function get_best_exact_phrase_weight_local(const key: string;
        const expected_units: Integer): Integer;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_text: string;
        local_units: Integer;
        weight_value: Integer;
    begin
        Result := 0;
        if (m_dictionary = nil) or (key = '') or (expected_units <= 0) then
        begin
            Exit;
        end;

        if not m_dictionary.lookup_exact_full_pinyin(key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_idx >= 6 then
            begin
                Break;
            end;
            if Trim(local_results[local_idx].comment) <> '' then
            begin
                Continue;
            end;

            local_text := Trim(local_results[local_idx].text);
            local_units := get_candidate_text_unit_count(local_text);
            if local_units <> expected_units then
            begin
                Continue;
            end;

            if local_results[local_idx].has_dict_weight then
            begin
                weight_value := local_results[local_idx].dict_weight;
            end
            else
            begin
                weight_value := local_results[local_idx].score;
            end;

            if weight_value > Result then
            begin
                Result := weight_value;
            end;
        end;
    end;

    procedure apply_dictionary_supported_ng_boundary_shift_local(
        var values: TncPinyinParseResult);
    const
        c_shift_min_left_weight = 500;
        c_shift_min_right_weight = 420;
        c_shift_margin = 320;
    var
        local_idx: Integer;
        left_text: string;
        right_text: string;
        shifted_left_text: string;
        shifted_right_text: string;
        prev_text: string;
        next_text: string;
        next_next_text: string;
        current_support: Integer;
        current_left_support: Integer;
        current_right_support: Integer;
        shifted_left_support: Integer;
        shifted_right_support: Integer;
        shifted_support: Integer;

        function get_best_following_support_local(const head_text_value: string;
            const next_text_value: string; const next_next_text_value: string): Integer;
        var
            key_value: string;
            weight_value: Integer;
        begin
            Result := 0;
            if (head_text_value = '') or (next_text_value = '') then
            begin
                Exit;
            end;

            key_value := head_text_value + next_text_value;
            weight_value := get_best_exact_phrase_weight_local(key_value, 2);
            if weight_value > Result then
            begin
                Result := weight_value;
            end;

            if next_next_text_value <> '' then
            begin
                key_value := head_text_value + next_text_value +
                    next_next_text_value;
                weight_value := get_best_exact_phrase_weight_local(key_value, 3);
                if weight_value > Result then
                begin
                    Result := weight_value;
                end;
            end;
        end;
    begin
        if (m_dictionary = nil) or (Pos('''', input_text) > 0) or
            (Length(values) < 4) or
            (not SameText(normalize_pinyin_text(input_text),
            normalize_pinyin_text(m_composition_text))) then
        begin
            Exit;
        end;

        local_idx := 1;
        while local_idx < High(values) do
        begin
            left_text := LowerCase(Trim(values[local_idx].text));
            right_text := LowerCase(Trim(values[local_idx + 1].text));
            if (Length(left_text) <= 2) or
                (Copy(left_text, Length(left_text) - 1, 2) <> 'ng') or
                (not is_relaxed_no_initial_syllable_text(right_text)) or
                (local_idx + 2 > High(values)) then
            begin
                Inc(local_idx);
                Continue;
            end;

            shifted_left_text := Copy(left_text, 1, Length(left_text) - 1);
            shifted_right_text := 'g' + right_text;
            if (not is_full_pinyin_key(shifted_left_text)) or
                (not is_full_pinyin_key(shifted_right_text)) then
            begin
                Inc(local_idx);
                Continue;
            end;

            prev_text := LowerCase(Trim(values[local_idx - 1].text));
            next_text := LowerCase(Trim(values[local_idx + 2].text));
            if local_idx + 3 <= High(values) then
            begin
                next_next_text := LowerCase(Trim(values[local_idx + 3].text));
            end
            else
            begin
                next_next_text := '';
            end;

            shifted_left_support := Max(
                get_best_exact_phrase_weight_local(
                prev_text + shifted_left_text, 2),
                get_best_exact_phrase_weight_local(shifted_left_text, 1));
            shifted_right_support := Max(
                get_best_exact_phrase_weight_local(
                shifted_right_text + next_text, 2),
                get_best_following_support_local(shifted_right_text,
                next_text, next_next_text));
            if (shifted_left_support < c_shift_min_left_weight) or
                (shifted_right_support < c_shift_min_right_weight) then
            begin
                Inc(local_idx);
                Continue;
            end;

            current_left_support := Max(
                get_best_exact_phrase_weight_local(prev_text + left_text, 2),
                get_best_exact_phrase_weight_local(left_text, 1));
            current_right_support := Max(
                get_best_exact_phrase_weight_local(right_text + next_text, 2),
                get_best_following_support_local(right_text, next_text,
                next_next_text));
            current_support := current_left_support + current_right_support;
            shifted_support := shifted_left_support + shifted_right_support;
            if shifted_support < current_support + c_shift_margin then
            begin
                Inc(local_idx);
                Continue;
            end;

            values[local_idx].text := shifted_left_text;
            values[local_idx].length := Length(shifted_left_text);
            values[local_idx + 1].text := shifted_right_text;
            values[local_idx + 1].start_index :=
                values[local_idx].start_index + Length(shifted_left_text);
            values[local_idx + 1].length := Length(shifted_right_text);
            Inc(local_idx, 2);
        end;
    end;
begin
    SetLength(Result, 0);
    if input_text = '' then
    begin
        Exit;
    end;

    current_composition_query := SameText(normalize_pinyin_text(input_text),
        normalize_pinyin_text(m_composition_text));
    if allow_relaxed_split then
    begin
        cache_key := 'r';
    end
    else
    begin
        cache_key := 's';
    end;
    if current_composition_query then
    begin
        cache_key := cache_key + 'c';
    end
    else
    begin
        cache_key := cache_key + 'p';
    end;
    cache_key := cache_key + #1 + LowerCase(Trim(input_text));
    if (m_effective_pinyin_parse_cache <> nil) and
        m_effective_pinyin_parse_cache.TryGetValue(cache_key, cached_result) then
    begin
        Result := Copy(cached_result, 0, Length(cached_result));
        Exit;
    end;

    try
        parser := TncPinyinParser.create;
        try
            if allow_relaxed_split then
            begin
                syllables := parse_pinyin_with_relaxed_missing_apostrophe(input_text);
            end
            else
            begin
                syllables := parser.parse(input_text);
            end;
        finally
            parser.Free;
        end;

        nc_merge_abbreviated_retroflex_initials(syllables);
        Result := syllables;
        apply_dictionary_supported_ng_boundary_shift_local(Result);
        // Explicit apostrophe input must preserve the user's syllable boundary.
        // Queries like "ji'an" / "ji'e" should stay two-syllable here instead of
        // being merged back into compact forms such as "jian" / "jie".
        if Pos('''', input_text) > 0 then
        begin
            Exit;
        end;

        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        for idx := 0 to High(syllables) do
        begin
            tail_text := build_tail_text(idx);
            if is_single_syllable_prefix_text(tail_text) then
            begin
                if idx = High(syllables) then
                begin
                    Exit;
                end;

                merged_count := idx + 1;
                SetLength(Result, merged_count);
                for tail_idx := 0 to idx - 1 do
                begin
                    Result[tail_idx] := syllables[tail_idx];
                end;

                Result[idx].text := tail_text;
                Result[idx].start_index := syllables[idx].start_index;
                Result[idx].length := 0;
                for tail_idx := idx to High(syllables) do
                begin
                    Inc(Result[idx].length, syllables[tail_idx].length);
                end;
                Exit;
            end;
        end;
    finally
        if m_effective_pinyin_parse_cache <> nil then
        begin
            if m_effective_pinyin_parse_cache.Count >=
                c_effective_pinyin_parse_cache_limit then
            begin
                m_effective_pinyin_parse_cache.Clear;
            end;
            m_effective_pinyin_parse_cache.AddOrSetValue(cache_key,
                Copy(Result, 0, Length(Result)));
        end
    end;
end;

function TncEngine.get_effective_compact_pinyin_unit_count(const input_text: string;
    const allow_relaxed_split: Boolean): Integer;
var
    syllables: TncPinyinParseResult;
begin
    syllables := get_effective_compact_pinyin_syllables(input_text, allow_relaxed_split);
    Result := Length(syllables);
end;

function TncEngine.normalize_adjacent_swap_typo(const value: string): string;
const
    c_adjacent_swap_typo_min_query_len = 5;
var
    swap_idx: Integer;
    swap_value: string;
    swap_char: Char;
    best_value: string;
    best_unit_count: Integer;
    swap_unit_count: Integer;
begin
    Result := '';
    if (value = '') or (Length(value) < c_adjacent_swap_typo_min_query_len) then
    begin
        Exit;
    end;

    // "gn" is not a valid compact pinyin boundary and is the dominant adjacent
    // typo for "-ng" finals. Fix it directly instead of repeatedly parsing the
    // whole long query.
    for swap_idx := Length(value) - 1 downto 1 do
    begin
        if (value[swap_idx] = 'g') and (value[swap_idx + 1] = 'n') then
        begin
            Result := value;
            Result[swap_idx] := 'n';
            Result[swap_idx + 1] := 'g';
            Exit;
        end;
    end;

    if Length(value) > 8 then
    begin
        Exit;
    end;

    best_value := '';
    best_unit_count := MaxInt;
    for swap_idx := Length(value) - 1 downto 1 do
    begin
        if value[swap_idx] = value[swap_idx + 1] then
        begin
            Continue;
        end;

        swap_value := value;
        swap_char := swap_value[swap_idx];
        swap_value[swap_idx] := swap_value[swap_idx + 1];
        swap_value[swap_idx + 1] := swap_char;
        if is_full_pinyin_key(swap_value) then
        begin
            swap_unit_count := get_effective_compact_pinyin_unit_count(
                swap_value);
            if (swap_unit_count > 0) and (swap_unit_count < best_unit_count) then
            begin
                best_value := swap_value;
                best_unit_count := swap_unit_count;
            end;
        end;
    end;
    Result := best_value;
end;

function TncEngine.split_text_units(const input_text: string): TArray<string>;
const
    c_text_units_cache_limit = 8192;
var
    list: TList<string>;
    cached_units: TArray<string>;
    i: Integer;
    ch: Char;
begin
    SetLength(Result, 0);
    if input_text = '' then
    begin
        Exit;
    end;

    if (m_lookup_text_units_cache <> nil) and
        m_lookup_text_units_cache.TryGetValue(input_text, cached_units) then
    begin
        // Some ranking helpers rearrange their local unit array. Keep the
        // persistent cache immutable so one lookup cannot change the next.
        Result := Copy(cached_units);
        Exit;
    end;

    list := TList<string>.Create;
    try
        i := 1;
        while i <= Length(input_text) do
        begin
            ch := input_text[i];
            if (Ord(ch) >= $D800) and (Ord(ch) <= $DBFF) and (i < Length(input_text)) and
                (Ord(input_text[i + 1]) >= $DC00) and (Ord(input_text[i + 1]) <= $DFFF) then
            begin
                list.Add(Copy(input_text, i, 2));
                Inc(i, 2);
            end
            else
            begin
                list.Add(Copy(input_text, i, 1));
                Inc(i);
            end;
        end;

        Result := list.ToArray;
        if m_lookup_text_units_cache <> nil then
        begin
            if m_lookup_text_units_cache.Count >= c_text_units_cache_limit then
            begin
                m_lookup_text_units_cache.Clear;
            end;
            m_lookup_text_units_cache.AddOrSetValue(input_text, Copy(Result));
        end;
    finally
        list.Free;
    end;
end;

function TncEngine.is_common_surname_text(const value: string): Boolean;
var
    trimmed: string;
    codepoint: Integer;
begin
    Result := False;
    trimmed := Trim(value);
    if trimmed = '' then
    begin
        Exit;
    end;

    if Length(trimmed) = 1 then
    begin
        codepoint := Ord(trimmed[1]);
    end
    else if (Length(trimmed) = 2) and
        (Ord(trimmed[1]) >= $D800) and (Ord(trimmed[1]) <= $DBFF) and
        (Ord(trimmed[2]) >= $DC00) and (Ord(trimmed[2]) <= $DFFF) then
    begin
        codepoint := ((Ord(trimmed[1]) - $D800) shl 10) + (Ord(trimmed[2]) - $DC00) + $10000;
    end
    else
    begin
        Exit;
    end;

    case codepoint of
        $8D75, $94B1, $5B59, $674E, $5468, $5434, $90D1, $738B, $51AF, $9648,
        $891A, $536B, $848B, $6C88, $97E9, $6768, $6731, $79E6, $8BB8, $4F55,
        $5415, $65BD, $5F20, $5B54, $66F9, $4E25, $534E, $91D1, $9B4F, $9676,
        $59DC, $621A, $8C22, $90B9, $55BB, $82CF, $6F58, $845B, $8303, $5F6D,
        $90CE, $9C81, $97E6, $9A6C, $82D7, $51E4, $65B9, $4FDE, $4EFB, $8881,
        $67F3, $9C8D, $53F2, $5510, $8D39, $5EC9, $5C91, $859B, $96F7, $8D3A,
        $502A, $6C64, $6ED5, $6BB7, $7F57, $6BD5, $90DD, $90AC, $5B89, $5E38,
        $4E50, $4E8E, $5085, $76AE, $9F50, $5EB7, $4F0D, $4F59, $5143, $987E,
        $5B5F, $5E73, $9EC4, $548C, $7A46, $8427, $5C39, $59DA, $90B5, $6E5B,
        $6C6A, $7941, $6BDB, $79B9, $72C4, $7C73, $660E, $81E7, $8BA1, $4F0F,
        $6210, $6234, $5B8B, $8305, $5E9E, $718A, $7EAA, $8212, $5C48, $9879,
        $795D, $8463, $6881, $675C, $962E, $84DD, $95F5, $5E2D, $5B63, $9EBB,
        $5F3A, $8D3E, $8DEF, $5A04, $5371, $6C5F, $7AE5, $989C, $90ED, $6885,
        $76DB, $6797, $949F, $5F90, $90B1, $9A86, $9AD8, $590F, $8521, $7530,
        $6A0A, $80E1, $51CC, $970D, $865E, $4E07, $67EF, $5362, $83AB, $623F,
        $7F2A, $89E3, $5E94, $4E01, $9093, $90C1, $5D14, $9F9A, $7A0B, $90A2,
        $88F4, $9646, $8363, $7FC1, $8340, $7F8A, $60E0, $7504, $5C01, $82AE,
        $50A8, $9773, $6BB5, $7126, $5DF4, $5F13, $7267, $8F66, $4FAF, $5B93,
        $84EC, $5168:
            Result := True;
    end;
end;

function TncEngine.merge_candidate_lists(const primary_candidates: TncCandidateList;
    const secondary_candidates: TncCandidateList; const max_candidates: Integer): TncCandidateList;
var
    seen: TDictionary<string, Integer>;
    list: TList<TncCandidate>;
    i: Integer;
    existing_index: Integer;
    key: string;
    limit: Integer;
    candidate: TncCandidate;
    function build_merge_key(const candidate_value: TncCandidate): string;
    begin
        Result := LowerCase(Trim(candidate_value.text)) + #1 +
            LowerCase(Trim(candidate_value.comment));
    end;
begin
    seen := TDictionary<string, Integer>.Create;
    list := TList<TncCandidate>.Create;
    try
        for i := 0 to High(primary_candidates) do
        begin
            key := build_merge_key(primary_candidates[i]);
            if not seen.ContainsKey(key) then
            begin
                list.Add(primary_candidates[i]);
                seen.Add(key, list.Count - 1);
            end;
        end;

        for i := 0 to High(secondary_candidates) do
        begin
            key := build_merge_key(secondary_candidates[i]);
            if not seen.TryGetValue(key, existing_index) then
            begin
                list.Add(secondary_candidates[i]);
                seen.Add(key, list.Count - 1);
            end
            else
            begin
                if (existing_index >= 0) and (existing_index < list.Count) then
                begin
                    candidate := list[existing_index];
                    if secondary_candidates[i].has_dict_weight and
                        (not candidate.has_dict_weight) and
                        (candidate.source <> cs_user) then
                    begin
                        candidate.score := secondary_candidates[i].score;
                    end
                    else if secondary_candidates[i].score > candidate.score then
                    begin
                        candidate.score := secondary_candidates[i].score;
                    end;

                    if get_source_rank(secondary_candidates[i].source) <
                        get_source_rank(candidate.source) then
                    begin
                        candidate.source := secondary_candidates[i].source;
                    end;

                    // Preserve raw lexicon weight for debug rendering when duplicates collapse by text.
                    if secondary_candidates[i].has_dict_weight then
                    begin
                        if (not candidate.has_dict_weight) or
                            (secondary_candidates[i].dict_weight > candidate.dict_weight) then
                        begin
                            candidate.has_dict_weight := True;
                            candidate.dict_weight := secondary_candidates[i].dict_weight;
                        end;
                    end;

                    { A raw pronunciation always owns a duplicate candidate.  If
                      both paths are fuzzy, retain the least costly spelling so
                      selection learning records the actual recall path. }
                    if is_fuzzy_pinyin_active then
                    begin
                        if (secondary_candidates[i].fuzzy_cost = 0) and
                            (secondary_candidates[i].has_dict_weight or
                            (secondary_candidates[i].source = cs_user)) then
                        begin
                            candidate.fuzzy_cost := 0;
                            candidate.fuzzy_rules := [];
                        end
                        else if (candidate.fuzzy_cost > 0) and
                            (secondary_candidates[i].fuzzy_cost < candidate.fuzzy_cost) then
                        begin
                            candidate.fuzzy_cost := secondary_candidates[i].fuzzy_cost;
                            candidate.fuzzy_rules := secondary_candidates[i].fuzzy_rules;
                        end;
                        if (candidate.fuzzy_cost = 0) and
                            (secondary_candidates[i].fuzzy_cost > 0) and
                            (not candidate.has_dict_weight) and
                            (candidate.source <> cs_user) then
                        begin
                            candidate.fuzzy_cost := secondary_candidates[i].fuzzy_cost;
                            candidate.fuzzy_rules := secondary_candidates[i].fuzzy_rules;
                        end;
                    end;

                    list[existing_index] := candidate;
                end;
            end;
        end;

        limit := list.Count;
        if (max_candidates > 0) and (limit > max_candidates) then
        begin
            limit := max_candidates;
        end;

        SetLength(Result, limit);
        for i := 0 to limit - 1 do
        begin
            Result[i] := list[i];
        end;
    finally
        list.Free;
        seen.Free;
    end;
end;

function TncEngine.build_segment_candidates(out out_candidates: TncCandidateList;
    const include_full_path: Boolean; out out_path_search_elapsed_ms: Int64;
    const allow_relaxed_missing_apostrophe: Boolean): Boolean;
const
    c_segment_max_per_segment = 256;
    c_segment_max_syllables = 24;
    c_segment_word_max_syllables = 4;
    c_segment_partial_penalty = 180;
    c_segment_partial_quadratic_penalty = 160;
    c_segment_prefix_bonus = 80;
    c_segment_long_prefix_bonus = 220;
    c_segment_page_expand_factor = 16;
    c_segment_full_state_limit = 128;
    c_segment_full_completion_bonus = 160;
    c_segment_full_transition_penalty = 6;
    c_segment_full_leading_single_bonus = 36;
    c_segment_full_single_top_n = 1;
    c_segment_full_non_leading_single_penalty = 72;
    c_segment_full_preferred_single_penalty = 24;
    c_segment_full_leading_multi_penalty = 42;
    c_segment_full_path_budget_ms = 18;
    c_segment_full_path_budget_ms_long = 120;
    c_segment_full_path_budget_probe_interval = 64;
    c_segment_full_path_work_limit = 8192;
    c_segment_full_path_work_limit_long = 32768;
    c_segment_full_path_non_user_limit = 4;
    c_segment_full_path_non_user_limit_long = 4;
    c_segment_full_state_limit_long = 384;
    c_segment_full_partial_remaining_limit = 2;
    c_segment_full_partial_non_user_limit = 3;
    c_segment_full_partial_penalty = 90;
    c_segment_full_partial_quadratic_penalty = 80;
    c_segment_partial_single_top_n = 6;
    c_segment_text_unit_mismatch_penalty = 100;
    c_segment_text_unit_overflow_penalty = 60;
    c_segment_alignment_rank_window = 6;
    c_segment_alignment_rank_step = 24;
    c_segment_alignment_missing_penalty = 60;
    c_segment_alignment_adjust_cap = 140;
var
    syllables: TncPinyinParseResult;
    lookup_cache: TDictionary<string, TncCandidateList>;
    owns_lookup_cache: Boolean;
    list: TList<TncCandidate>;
    dedup: TDictionary<string, Integer>;
    total_limit: Integer;
    per_limit: Integer;
    max_word_len: Integer;
    segment_len: Integer;
    segment_text: string;
    remaining_syllables: Integer;
    remaining_pinyin: string;
    i: Integer;
    lookup_results: TncCandidateList;
    candidate: TncCandidate;
    item: TncCandidate;
    score_value: Integer;
    dedup_key: string;
    existing_index: Integer;
    partial_single_limit: Integer;
    is_first_overall_segment: Boolean;
    candidate_text_units: Integer;
    path_search_start_tick: UInt64;

    function is_single_text_unit(const value: string): Boolean;
    begin
        if Length(value) = 1 then
        begin
            Result := True;
            Exit;
        end;

        Result := (Length(value) = 2) and
            (Ord(value[1]) >= $D800) and (Ord(value[1]) <= $DBFF) and
            (Ord(value[2]) >= $DC00) and (Ord(value[2]) <= $DFFF);
    end;

    function is_multi_char_word(const text: string): Boolean;
    var
        trimmed_text: string;
    begin
        trimmed_text := Trim(text);
        Result := (trimmed_text <> '') and (not is_single_text_unit(trimmed_text));
    end;

    function contains_non_ascii(const text: string): Boolean;
    var
        ch: Char;
    begin
        Result := False;
        for ch in text do
        begin
            if Ord(ch) > $7F then
            begin
                Result := True;
                Exit;
            end;
        end;
    end;

    function is_single_initial_token(const token_text: string): Boolean;
    var
        ch: Char;
    begin
        Result := False;
        if Length(token_text) <> 1 then
        begin
            Exit;
        end;

        ch := token_text[1];
        if (ch >= 'A') and (ch <= 'Z') then
        begin
            ch := Chr(Ord(ch) + 32);
        end;
        Result := CharInSet(ch, ['b', 'p', 'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h', 'j', 'q', 'x',
            'r', 'z', 'c', 's', 'y', 'w']);
    end;

    function get_text_unit_count(const text: string): Integer;
    var
        idx: Integer;
        codepoint: Integer;
    begin
        Result := 0;
        if text = '' then
        begin
            Exit;
        end;

        idx := 1;
        while idx <= Length(text) do
        begin
            codepoint := Ord(text[idx]);
            if (codepoint >= $D800) and (codepoint <= $DBFF) and (idx < Length(text)) then
            begin
                if (Ord(text[idx + 1]) >= $DC00) and (Ord(text[idx + 1]) <= $DFFF) then
                begin
                    Inc(idx);
                end;
            end;
            Inc(Result);
            Inc(idx);
        end;
    end;

    function build_syllable_text(const start_index: Integer; const syllable_count: Integer): string;
    var
        idx: Integer;
        end_index: Integer;
    begin
        Result := '';
        if (syllable_count <= 0) or (start_index < 0) or (start_index > High(syllables)) then
        begin
            Exit;
        end;

        end_index := start_index + syllable_count - 1;
        if end_index > High(syllables) then
        begin
            end_index := High(syllables);
        end;

        for idx := start_index to end_index do
        begin
            Result := Result + syllables[idx].text;
        end;
    end;

    function dictionary_lookup_cached(const pinyin_key: string; out out_results: TncCandidateList): Boolean;
    var
        build_cache_key: string;
        local_cache_key: string;
        fuzzy_results: TncCandidateList;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (pinyin_key = '') then
        begin
            Exit(False);
        end;

        local_cache_key := '#fuzzy#' + pinyin_key;
        if lookup_cache.TryGetValue(local_cache_key, out_results) then
        begin
            Exit(Length(out_results) > 0);
        end;

        build_cache_key := '#fuzzylookup#' + pinyin_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(build_cache_key,
            out_results) then
        begin
            lookup_cache.AddOrSetValue(local_cache_key, out_results);
            Exit(Length(out_results) > 0);
        end;

        if not m_dictionary.lookup(pinyin_key, out_results) then
        begin
            SetLength(out_results, 0);
        end;
        if m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []) and
            m_dictionary.lookup_fuzzy_full_pinyin_bounded(pinyin_key,
            fuzzy_results, 1, 6, 4, 16) then
        begin
            if Length(fuzzy_results) > 16 then
            begin
                SetLength(fuzzy_results, 16);
            end;
            out_results := merge_candidate_lists(out_results,
                fuzzy_results, 0);
        end;
        lookup_cache.AddOrSetValue(local_cache_key, out_results);
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(build_cache_key,
                out_results);
        end;
        Result := Length(out_results) > 0;
    end;

    function get_candidate_effective_weight_local(const local_candidate: TncCandidate): Integer;
    var
        local_units: Integer;
    begin
        if is_fuzzy_pinyin_active and
            (local_candidate.fuzzy_cost > 0) then
        begin
            Result := local_candidate.score;
        end
        else if local_candidate.has_dict_weight then
        begin
            Result := local_candidate.dict_weight;
        end
        else
        begin
            Result := local_candidate.score;
        end;

        local_units := get_text_unit_count(Trim(local_candidate.text));
        case local_units of
            1:
                if Result > 180 then
                begin
                    Result := 180 + ((Result - 180) div 12);
                end;
            2:
                if Result > 240 then
                begin
                    Result := 240 + ((Result - 240) div 10);
                end;
            3:
                if Result > 300 then
                begin
                    Result := 300 + ((Result - 300) div 24);
                end;
            4:
                if Result > 360 then
                begin
                    Result := 360 + ((Result - 360) div 32);
                end;
        else
            if Result > 420 then
            begin
                Result := 420 + ((Result - 420) div 40);
            end;
        end;
    end;

    function get_best_exact_phrase_strength_local(const pinyin_key: string): Integer;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
    begin
        Result := 0;
        if not dictionary_lookup_cached(pinyin_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units < 2 then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > Result then
            begin
                Result := local_weight;
            end;
        end;
    end;

    function get_best_single_char_strength_for_syllable_local(const syllable_index: Integer): Integer;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
        local_key: string;
    begin
        Result := 0;
        if (syllable_index < 0) or (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        local_key := syllables[syllable_index].text;
        if not dictionary_lookup_cached(local_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > Result then
            begin
                Result := local_weight;
            end;
        end;
    end;

    function try_get_best_single_char_candidate_for_syllable_local(const syllable_index: Integer;
        out out_text: string; out out_strength: Integer): Boolean;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
        local_key: string;
    begin
        Result := False;
        out_text := '';
        out_strength := 0;
        if (syllable_index < 0) or (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        local_key := syllables[syllable_index].text;
        if not dictionary_lookup_cached(local_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > out_strength then
            begin
                out_text := local_results[local_idx].text;
                out_strength := local_weight;
                Result := True;
            end;
        end;
    end;

    function is_boundary_unit_single_char_text_local(const text_value: string): Boolean;
    var
        normalized_text_value: string;
    begin
        Result := False;
        normalized_text_value := Trim(text_value);
        if get_text_unit_count(normalized_text_value) <> 1 then
        begin
            Exit;
        end;

        Result :=
            (normalized_text_value = string(Char($4E2A))) or
            (normalized_text_value = string(Char($4F4D))) or
            (normalized_text_value = string(Char($6B21))) or
            (normalized_text_value = string(Char($70B9))) or
            (normalized_text_value = string(Char($4E9B))) or
            (normalized_text_value = string(Char($79CD))) or
            (normalized_text_value = string(Char($5929))) or
            (normalized_text_value = string(Char($5E74))) or
            (normalized_text_value = string(Char($6708))) or
            (normalized_text_value = string(Char($91CC))) or
            (normalized_text_value = string(Char($4E0B))) or
            (normalized_text_value = string(Char($56DE))) or
            (normalized_text_value = string(Char($904D))) or
            (normalized_text_value = string(Char($58F0))) or
            (normalized_text_value = string(Char($9762))) or
            (normalized_text_value = string(Char($773C))) or
            (normalized_text_value = string(Char($8FB9)));
    end;

    function try_get_best_boundary_unit_single_char_for_pinyin_key_local(const pinyin_key: string;
        out out_text: string; out out_strength: Integer): Boolean;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
        local_text: string;
    begin
        Result := False;
        out_text := '';
        out_strength := 0;
        if not dictionary_lookup_cached(Trim(pinyin_key), local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            local_text := Trim(local_results[local_idx].text);
            if not is_boundary_unit_single_char_text_local(local_text) then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > out_strength then
            begin
                out_text := local_text;
                out_strength := local_weight;
                Result := True;
            end;
        end;
    end;

    function try_get_shifted_boundary_unit_split_local(out out_head_two_key: string;
        out out_shifted_middle_key: string; out out_shifted_tail_key: string;
        out out_boundary_unit_text: string; out out_boundary_unit_strength: Integer): Boolean;
    var
        middle_key: string;
        tail_key: string;
        shifted_char: string;
    begin
        Result := False;
        out_head_two_key := '';
        out_shifted_middle_key := '';
        out_shifted_tail_key := '';
        out_boundary_unit_text := '';
        out_boundary_unit_strength := 0;
        if Length(syllables) < 3 then
        begin
            Exit;
        end;

        middle_key := Trim(syllables[1].text);
        tail_key := Trim(syllables[2].text);
        if (middle_key = '') or (tail_key = '') or (Length(middle_key) <= 1) then
        begin
            Exit;
        end;

        shifted_char := Copy(middle_key, Length(middle_key), 1);
        out_shifted_middle_key := Copy(middle_key, 1, Length(middle_key) - 1);
        out_shifted_tail_key := shifted_char + tail_key;
        if (out_shifted_middle_key = '') or (out_shifted_tail_key = '') then
        begin
            Exit;
        end;

        if (not is_full_pinyin_key(out_shifted_middle_key)) or
            (not is_full_pinyin_key(out_shifted_tail_key)) then
        begin
            Exit;
        end;

        if not try_get_best_boundary_unit_single_char_for_pinyin_key_local(
            out_shifted_middle_key, out_boundary_unit_text, out_boundary_unit_strength) then
        begin
            Exit;
        end;

        out_head_two_key := Trim(syllables[0].text) + out_shifted_middle_key;
        Result := out_head_two_key <> '';
    end;

    function is_quantity_like_prefix_text_local(const text_value: string): Boolean;
    var
        normalized_text_value: string;
    begin
        Result := False;
        normalized_text_value := Trim(text_value);
        if get_text_unit_count(normalized_text_value) <> 1 then
        begin
            Exit;
        end;

        Result :=
            (normalized_text_value = string(Char($4E00))) or
            (normalized_text_value = string(Char($4E8C))) or
            (normalized_text_value = string(Char($4E09))) or
            (normalized_text_value = string(Char($56DB))) or
            (normalized_text_value = string(Char($4E94))) or
            (normalized_text_value = string(Char($516D))) or
            (normalized_text_value = string(Char($4E03))) or
            (normalized_text_value = string(Char($516B))) or
            (normalized_text_value = string(Char($4E5D))) or
            (normalized_text_value = string(Char($5341))) or
            (normalized_text_value = string(Char($767E))) or
            (normalized_text_value = string(Char($5343))) or
            (normalized_text_value = string(Char($4E07))) or
            (normalized_text_value = string(Char($4E24))) or
            (normalized_text_value = string(Char($51E0))) or
            (normalized_text_value = string(Char($8FD9))) or
            (normalized_text_value = string(Char($90A3))) or
            (normalized_text_value = string(Char($54EA))) or
            (normalized_text_value = string(Char($6BCF))) or
            (normalized_text_value = string(Char($5404))) or
            (normalized_text_value = string(Char($534A))) or
            (normalized_text_value = string(Char($591A))) or
            (normalized_text_value = string(Char($6574))) or
            (normalized_text_value = string(Char($5355))) or
            (normalized_text_value = string(Char($53CC))) or
            (normalized_text_value = string(Char($4FE9))) or
            (normalized_text_value = string(Char($4EE8)));
    end;

    function try_get_best_boundary_unit_single_char_for_syllable_local(const syllable_index: Integer;
        out out_text: string; out out_strength: Integer): Boolean;
    begin
        Result := False;
        out_text := '';
        out_strength := 0;
        if (syllable_index < 0) or (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        Result := try_get_best_boundary_unit_single_char_for_pinyin_key_local(
            syllables[syllable_index].text, out_text, out_strength);
    end;

    function get_single_char_strength_for_text_at_syllable_local(const syllable_index: Integer;
        const text_value: string): Integer;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
        local_key: string;
        normalized_text_value: string;
    begin
        Result := 0;
        normalized_text_value := Trim(text_value);
        if (normalized_text_value = '') or (syllable_index < 0) or
            (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        local_key := syllables[syllable_index].text;
        if not dictionary_lookup_cached(local_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            if Trim(local_results[local_idx].text) <> normalized_text_value then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > Result then
            begin
                Result := local_weight;
            end;
        end;
    end;

    function get_single_char_rank_for_text_at_syllable_local(const syllable_index: Integer;
        const text_value: string): Integer;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_rank: Integer;
        local_key: string;
        normalized_text_value: string;
    begin
        Result := 0;
        normalized_text_value := Trim(text_value);
        if (normalized_text_value = '') or (syllable_index < 0) or
            (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        local_key := syllables[syllable_index].text;
        if not dictionary_lookup_cached(local_key, local_results) then
        begin
            Exit;
        end;

        local_rank := 0;
        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units <> 1 then
            begin
                Continue;
            end;

            Inc(local_rank);
            if Trim(local_results[local_idx].text) = normalized_text_value then
            begin
                Result := local_rank;
                Exit;
            end;
        end;
    end;

    function get_suffix_tail_bias_base_for_pinyin_key_local(const pinyin_key: string): Integer;
    var
        normalized_pinyin_key: string;
    begin
        Result := 0;
        normalized_pinyin_key := LowerCase(Trim(pinyin_key));
        if normalized_pinyin_key = '' then
        begin
            Exit;
        end;

        if normalized_pinyin_key = 'de' then
        begin
            Exit(520);
        end;

        if (normalized_pinyin_key = 'le') or (normalized_pinyin_key = 'zhe') or
            (normalized_pinyin_key = 'guo') then
        begin
            Exit(420);
        end;

        if (normalized_pinyin_key = 'ma') or (normalized_pinyin_key = 'ne') or
            (normalized_pinyin_key = 'ba') then
        begin
            Exit(360);
        end;

        if (normalized_pinyin_key = 'la') or (normalized_pinyin_key = 'a') or
            (normalized_pinyin_key = 'ya') or (normalized_pinyin_key = 'wa') or
            (normalized_pinyin_key = 'ha') then
        begin
            Exit(300);
        end;
    end;

    function get_suffix_tail_head_preference_ratio_pct_local(
        const pinyin_key: string): Integer;
    var
        normalized_pinyin_key: string;
    begin
        Result := 100;
        normalized_pinyin_key := LowerCase(Trim(pinyin_key));
        if normalized_pinyin_key = 'de' then
        begin
            Exit(170);
        end;

        if (normalized_pinyin_key = 'le') or (normalized_pinyin_key = 'zhe') or
            (normalized_pinyin_key = 'guo') then
        begin
            Exit(118);
        end;

        if (normalized_pinyin_key = 'ma') or (normalized_pinyin_key = 'ne') or
            (normalized_pinyin_key = 'ba') or (normalized_pinyin_key = 'la') or
            (normalized_pinyin_key = 'a') or (normalized_pinyin_key = 'ya') or
            (normalized_pinyin_key = 'wa') or (normalized_pinyin_key = 'ha') then
        begin
            Exit(112);
        end;
    end;

    function get_suffix_tail_head_retention_bonus_local(
        const pinyin_key: string): Integer;
    var
        normalized_pinyin_key: string;
    begin
        Result := 0;
        normalized_pinyin_key := LowerCase(Trim(pinyin_key));
        if normalized_pinyin_key = 'de' then
        begin
            Exit(420);
        end;

        if (normalized_pinyin_key = 'le') or (normalized_pinyin_key = 'zhe') or
            (normalized_pinyin_key = 'guo') then
        begin
            Exit(200);
        end;

        if (normalized_pinyin_key = 'ma') or (normalized_pinyin_key = 'ne') or
            (normalized_pinyin_key = 'ba') or (normalized_pinyin_key = 'la') or
            (normalized_pinyin_key = 'a') or (normalized_pinyin_key = 'ya') or
            (normalized_pinyin_key = 'wa') or (normalized_pinyin_key = 'ha') then
        begin
            Exit(120);
        end;
    end;

    function is_suffix_tail_expected_text_local(const pinyin_key: string;
        const text_value: string): Boolean;
    var
        normalized_pinyin_key: string;
        normalized_text_value: string;
    begin
        Result := False;
        normalized_pinyin_key := LowerCase(Trim(pinyin_key));
        normalized_text_value := Trim(text_value);
        if (normalized_pinyin_key = '') or (normalized_text_value = '') or
            (get_text_unit_count(normalized_text_value) <> 1) then
        begin
            Exit;
        end;

        if normalized_pinyin_key = 'de' then
        begin
            Result := (normalized_text_value = string(Char($7684))) or
                (normalized_text_value = string(Char($5F97))) or
                (normalized_text_value = string(Char($5730)));
            Exit;
        end;

        if normalized_pinyin_key = 'le' then
        begin
            Result := normalized_text_value = string(Char($4E86));
            Exit;
        end;

        if normalized_pinyin_key = 'zhe' then
        begin
            Result := (normalized_text_value = string(Char($7740))) or
                (normalized_text_value = string(Char($8457)));
            Exit;
        end;

        if normalized_pinyin_key = 'guo' then
        begin
            Result := normalized_text_value = string(Char($8FC7));
            Exit;
        end;

        if normalized_pinyin_key = 'ma' then
        begin
            Result := (normalized_text_value = string(Char($5417))) or
                (normalized_text_value = string(Char($561B)));
            Exit;
        end;

        if normalized_pinyin_key = 'ne' then
        begin
            Result := normalized_text_value = string(Char($5462));
            Exit;
        end;

        if normalized_pinyin_key = 'ba' then
        begin
            Result := (normalized_text_value = string(Char($5427))) or
                (normalized_text_value = string(Char($7F62)));
            Exit;
        end;

        if normalized_pinyin_key = 'la' then
        begin
            Result := normalized_text_value = string(Char($5566));
            Exit;
        end;

        if normalized_pinyin_key = 'a' then
        begin
            Result := normalized_text_value = string(Char($554A));
            Exit;
        end;

        if normalized_pinyin_key = 'ya' then
        begin
            Result := normalized_text_value = string(Char($5440));
            Exit;
        end;

        if normalized_pinyin_key = 'wa' then
        begin
            Result := normalized_text_value = string(Char($54C7));
            Exit;
        end;

        if normalized_pinyin_key = 'ha' then
        begin
            Result := normalized_text_value = string(Char($54C8));
            Exit;
        end;
    end;

    function get_suffix_tail_expected_text_support_strength_local(
        const syllable_index: Integer; const pinyin_key: string;
        const best_single_text: string; const best_single_strength: Integer;
        out out_matched_text: string): Integer;
    var
        normalized_best_single_text: string;

        procedure consider_expected_text_local(const expected_text: string);
        var
            local_strength: Integer;
        begin
            if expected_text = '' then
            begin
                Exit;
            end;

            if (normalized_best_single_text <> '') and
                SameText(normalized_best_single_text, expected_text) and
                (best_single_strength > 0) then
            begin
                local_strength := best_single_strength;
            end
            else
            begin
                local_strength := get_single_char_strength_for_text_at_syllable_local(
                    syllable_index, expected_text);
            end;

            if local_strength > Result then
            begin
                Result := local_strength;
                out_matched_text := expected_text;
            end;
        end;
    begin
        Result := 0;
        out_matched_text := '';
        if (syllable_index < 0) or (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        normalized_best_single_text := Trim(best_single_text);
        if not is_full_pinyin_key(Trim(syllables[syllable_index].text)) then
        begin
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'de') then
        begin
            consider_expected_text_local(string(Char($7684)));
            consider_expected_text_local(string(Char($5F97)));
            consider_expected_text_local(string(Char($5730)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'le') then
        begin
            consider_expected_text_local(string(Char($4E86)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'zhe') then
        begin
            consider_expected_text_local(string(Char($7740)));
            consider_expected_text_local(string(Char($8457)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'guo') then
        begin
            consider_expected_text_local(string(Char($8FC7)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'ma') then
        begin
            consider_expected_text_local(string(Char($5417)));
            consider_expected_text_local(string(Char($561B)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'ne') then
        begin
            consider_expected_text_local(string(Char($5462)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'ba') then
        begin
            consider_expected_text_local(string(Char($5427)));
            consider_expected_text_local(string(Char($7F62)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'la') then
        begin
            consider_expected_text_local(string(Char($5566)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'a') then
        begin
            consider_expected_text_local(string(Char($554A)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'ya') then
        begin
            consider_expected_text_local(string(Char($5440)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'wa') then
        begin
            consider_expected_text_local(string(Char($54C7)));
            Exit;
        end;

        if SameText(Trim(pinyin_key), 'ha') then
        begin
            consider_expected_text_local(string(Char($54C8)));
            Exit;
        end;
    end;

    function get_suffix_tail_bias_adjustment_for_syllable_local(
        const syllable_index: Integer; const best_single_text: string;
        const best_single_strength: Integer; out out_matched_text: string;
        out out_support_strength: Integer): Integer;
    var
        base_bias: Integer;
        pinyin_key: string;
    begin
        Result := 0;
        out_matched_text := '';
        out_support_strength := 0;
        if (syllable_index < 0) or (syllable_index > High(syllables)) then
        begin
            Exit;
        end;

        pinyin_key := Trim(syllables[syllable_index].text);
        base_bias := get_suffix_tail_bias_base_for_pinyin_key_local(pinyin_key);
        if base_bias <= 0 then
        begin
            Exit;
        end;

        out_support_strength := get_suffix_tail_expected_text_support_strength_local(
            syllable_index, pinyin_key, best_single_text, best_single_strength, out_matched_text);
        if out_support_strength <= 0 then
        begin
            Exit;
        end;

        Result := base_bias + Min(base_bias div 2, out_support_strength div 2);
    end;

    function try_get_best_exact_phrase_candidate_local(const pinyin_key: string;
        out out_text: string; out out_strength: Integer; out out_source: TncCandidateSource;
        out out_has_dict_weight: Boolean; out out_dict_weight: Integer): Boolean;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_weight: Integer;
    begin
        Result := False;
        out_text := '';
        out_strength := 0;
        out_source := cs_rule;
        out_has_dict_weight := False;
        out_dict_weight := 0;
        if not dictionary_lookup_cached(pinyin_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units < 2 then
            begin
                Continue;
            end;

            local_weight := get_candidate_effective_weight_local(local_results[local_idx]);
            if local_weight > out_strength then
            begin
                out_text := local_results[local_idx].text;
                out_strength := local_weight;
                out_source := local_results[local_idx].source;
                out_has_dict_weight := local_results[local_idx].has_dict_weight;
                out_dict_weight := local_results[local_idx].dict_weight;
                Result := True;
            end;
        end;
    end;

    function try_get_best_scored_exact_phrase_candidate_local(const pinyin_key: string;
        out out_text: string; out out_strength: Integer; out out_source: TncCandidateSource;
        out out_has_dict_weight: Boolean; out out_dict_weight: Integer): Boolean;
    var
        local_results: TncCandidateList;
        local_idx: Integer;
        local_units: Integer;
        local_score: Integer;
        local_dict_weight: Integer;
    begin
        Result := False;
        out_text := '';
        out_strength := 0;
        out_source := cs_rule;
        out_has_dict_weight := False;
        out_dict_weight := 0;
        if not dictionary_lookup_cached(pinyin_key, local_results) then
        begin
            Exit;
        end;

        for local_idx := 0 to High(local_results) do
        begin
            if local_results[local_idx].comment <> '' then
            begin
                Continue;
            end;

            local_units := get_text_unit_count(Trim(local_results[local_idx].text));
            if local_units < 2 then
            begin
                Continue;
            end;

            // Once ab|c or a|bc is already decided, pick the phrase that would
            // win as a standalone two-syllable query. Provider score reflects
            // lexicon rerank and user recency better than raw dict weight.
            local_score := local_results[local_idx].score;
            local_dict_weight := 0;
            if local_results[local_idx].has_dict_weight then
            begin
                local_dict_weight := local_results[local_idx].dict_weight;
                if local_score <= 0 then
                begin
                    local_score := local_dict_weight;
                end;
            end;

            if (local_score > out_strength) or
                ((local_score = out_strength) and (local_dict_weight > out_dict_weight)) then
            begin
                out_text := local_results[local_idx].text;
                out_strength := local_score;
                out_source := local_results[local_idx].source;
                out_has_dict_weight := local_results[local_idx].has_dict_weight;
                out_dict_weight := local_results[local_idx].dict_weight;
                Result := True;
            end;
        end;
    end;

    procedure update_three_syllable_partial_preference_kind_local;
    const
        c_three_syllable_boundary_ratio_pct_local = 100;
        c_first_single_tail_bias_cap_local = 520;
        c_last_single_head_bias_cap_local = 620;
        c_boundary_alignment_bonus_cap_local = 620;
        c_boundary_alignment_penalty_cap_local = 260;
        c_boundary_alignment_unique_best_bonus_local = 260;
        c_boundary_alignment_mismatch_penalty_base_local = 120;
        c_boundary_alignment_mismatch_penalty_cap_local = 480;
        c_shared_tail_friendly_boundary_base_bias_local = 360;
        c_shared_tail_friendly_boundary_bonus_cap_local = 360;
        c_quantity_boundary_head_bias_base_local = 960;
        c_quantity_boundary_head_bias_cap_local = 420;
        c_quantity_boundary_tail_penalty_local = 280;
        c_suffix_tail_conflict_penalty_cap_local = 480;
        c_segmented_prefix_strength_penalty_local = 520;
        c_head_phrase_first_unit_mismatch_penalty_local = 560;
        c_head_phrase_first_unit_near_top_ratio_pct_local = 88;
        c_head_phrase_first_unit_near_top_gap_local = 64;
        c_head_phrase_first_unit_top_rank_window_local = 5;
        c_safe_trailing_initial_head_margin_local = 700;
    var
        first_single_text: string;
        first_single_strength: Integer;
        middle_single_text: string;
        middle_single_strength: Integer;
        last_single_text: string;
        last_single_strength: Integer;
        head_two_key: string;
        tail_two_key: string;
        head_two_exact_text: string;
        head_two_exact_strength: Integer;
        head_two_strength: Integer;
        head_two_source: TncCandidateSource;
        head_two_has_dict_weight: Boolean;
        head_two_dict_weight: Integer;
        tail_two_exact_text: string;
        tail_two_exact_strength: Integer;
        tail_two_source: TncCandidateSource;
        tail_two_has_dict_weight: Boolean;
        tail_two_dict_weight: Integer;
        tail_two_strength: Integer;
        segmented_head_strength: Integer;
        head_two_units: TArray<string>;
        tail_two_units: TArray<string>;
        head_two_first_unit_strength: Integer;
        head_two_first_unit_rank: Integer;
        head_two_first_unit_near_top: Boolean;
        head_two_first_unit_matches_best: Boolean;
        head_two_boundary_unit_strength: Integer;
        head_two_boundary_unit_rank: Integer;
        head_two_boundary_unit_near_top: Boolean;
        head_two_boundary_unit_matches_best: Boolean;
        tail_two_boundary_unit_strength: Integer;
        tail_two_boundary_unit_rank: Integer;
        tail_two_boundary_unit_near_top: Boolean;
        tail_two_boundary_unit_matches_best: Boolean;
        shared_tail_friendly_boundary_bias: Integer;
        shared_tail_friendly_unit_strength: Integer;
        preferred_boundary_unit_text: string;
        preferred_boundary_unit_strength: Integer;
        head_two_query_path_bonus: Integer;
        tail_two_query_path_bonus: Integer;
        head_two_lookup_results: TncCandidateList;
        head_two_lookup_idx: Integer;
        has_quantity_boundary_head_partial: Boolean;
        has_safe_trailing_initial_state: Boolean;
        normalized_text_local: string;
        prefix_text_local: string;
        tail_cluster_local: string;
        shifted_head_two_key_local: string;
        shifted_middle_key_local: string;
        shifted_tail_key_local: string;
        shifted_boundary_unit_text_local: string;
        shifted_boundary_unit_strength_local: Integer;
        suffix_tail_matched_text_local: string;
        suffix_tail_support_strength_local: Integer;
        suffix_tail_bias_local: Integer;
        suffix_tail_boundary_ratio_pct_local: Integer;
        suffix_tail_head_retention_bonus_local: Integer;
        tail_two_suffix_conflict_penalty_local: Integer;
        head_two_first_unit_penalty_local: Integer;

        function is_backward_attaching_boundary_unit_local(const text_value: string): Boolean;
        var
            normalized_text_value: string;
        begin
            Result := False;
            normalized_text_value := Trim(text_value);
            if get_text_unit_count(normalized_text_value) <> 1 then
            begin
                Exit;
            end;
            Result :=
                (normalized_text_value = string(Char($4E2A))) or
                (normalized_text_value = string(Char($4F4D))) or
                (normalized_text_value = string(Char($6B21))) or
                (normalized_text_value = string(Char($70B9))) or
                (normalized_text_value = string(Char($4E9B))) or
                (normalized_text_value = string(Char($79CD))) or
                (normalized_text_value = string(Char($5929))) or
                (normalized_text_value = string(Char($5E74))) or
                (normalized_text_value = string(Char($6708))) or
                (normalized_text_value = string(Char($91CC))) or
                (normalized_text_value = string(Char($4E0B))) or
                (normalized_text_value = string(Char($56DE))) or
                (normalized_text_value = string(Char($904D))) or
                (normalized_text_value = string(Char($58F0))) or
                (normalized_text_value = string(Char($9762))) or
                (normalized_text_value = string(Char($773C))) or
                (normalized_text_value = string(Char($8FB9)));
        end;
        function get_query_path_support_adjustment_local(const left_text: string;
            const right_text: string): Integer;
        var
            encoded_path: string;
            bonus_value: Integer;
            penalty_value: Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (Trim(left_text) = '') or (Trim(right_text) = '') or
                (Trim(m_last_lookup_key) = '') then
            begin
                Exit;
            end;

            encoded_path := Trim(left_text) + c_segment_path_separator + Trim(right_text);
            bonus_value := get_cached_query_segment_path_bonus(
                m_last_lookup_key, encoded_path);
            penalty_value := get_cached_query_segment_path_penalty(
                m_last_lookup_key, encoded_path);
            Result := bonus_value - penalty_value;
        end;
    begin
        m_last_three_syllable_partial_preference_kind := 0;
        m_last_three_syllable_partial_debug_info := '';
        m_last_three_syllable_head_exact_text := '';
        m_last_three_syllable_head_strength := 0;
        m_last_three_syllable_tail_strength := 0;
        m_last_three_syllable_first_single_strength := 0;
        m_last_three_syllable_last_single_strength := 0;
        m_last_three_syllable_head_path_bonus := 0;
        m_last_three_syllable_tail_path_bonus := 0;
        if (not c_suppress_nonlexicon_complete_long_candidates) or
            (Length(syllables) < 3) then
        begin
            Exit;
        end;

        head_two_key := build_syllable_text(0, 2);
        tail_two_key := build_syllable_text(1, 2);
        head_two_exact_text := '';
        head_two_exact_strength := 0;
        head_two_source := cs_rule;
        head_two_has_dict_weight := False;
        head_two_dict_weight := 0;
        if not try_get_best_exact_phrase_candidate_local(head_two_key, head_two_exact_text,
            head_two_exact_strength, head_two_source, head_two_has_dict_weight,
            head_two_dict_weight) then
        begin
            head_two_exact_text := '';
            head_two_exact_strength := 0;
        end;
        head_two_strength := head_two_exact_strength;
        tail_two_exact_text := '';
        tail_two_exact_strength := 0;
        tail_two_source := cs_rule;
        tail_two_has_dict_weight := False;
        tail_two_dict_weight := 0;
        if not try_get_best_exact_phrase_candidate_local(tail_two_key, tail_two_exact_text,
            tail_two_exact_strength, tail_two_source, tail_two_has_dict_weight,
            tail_two_dict_weight) then
        begin
            tail_two_exact_text := '';
            tail_two_exact_strength := 0;
        end;
        tail_two_strength := tail_two_exact_strength;
        first_single_text := '';
        first_single_strength := 0;
        middle_single_text := '';
        middle_single_strength := 0;
        last_single_text := '';
        last_single_strength := 0;
        head_two_first_unit_near_top := False;
        head_two_first_unit_matches_best := False;
        head_two_boundary_unit_strength := 0;
        head_two_boundary_unit_near_top := False;
        head_two_boundary_unit_matches_best := False;
        tail_two_boundary_unit_strength := 0;
        tail_two_boundary_unit_rank := 0;
        tail_two_boundary_unit_near_top := False;
        tail_two_boundary_unit_matches_best := False;
        shared_tail_friendly_boundary_bias := 0;
        shared_tail_friendly_unit_strength := 0;
        preferred_boundary_unit_text := '';
        preferred_boundary_unit_strength := 0;
        head_two_query_path_bonus := 0;
        tail_two_query_path_bonus := 0;
        has_quantity_boundary_head_partial := False;
        shifted_head_two_key_local := '';
        shifted_middle_key_local := '';
        shifted_tail_key_local := '';
        shifted_boundary_unit_text_local := '';
        shifted_boundary_unit_strength_local := 0;
        suffix_tail_matched_text_local := '';
        suffix_tail_support_strength_local := 0;
        tail_two_suffix_conflict_penalty_local := 0;
        try_get_best_single_char_candidate_for_syllable_local(0, first_single_text,
            first_single_strength);
        try_get_best_single_char_candidate_for_syllable_local(1, middle_single_text,
            middle_single_strength);
        try_get_best_single_char_candidate_for_syllable_local(2, last_single_text,
            last_single_strength);
        suffix_tail_bias_local := get_suffix_tail_bias_adjustment_for_syllable_local(
            2, last_single_text, last_single_strength, suffix_tail_matched_text_local,
            suffix_tail_support_strength_local);
        suffix_tail_boundary_ratio_pct_local := c_three_syllable_boundary_ratio_pct_local;
        suffix_tail_head_retention_bonus_local := 0;
        if (suffix_tail_bias_local > 0) and (suffix_tail_support_strength_local > 0) and
            (suffix_tail_matched_text_local <> '') and (head_two_exact_strength > 0) then
        begin
            suffix_tail_boundary_ratio_pct_local :=
                get_suffix_tail_head_preference_ratio_pct_local(syllables[2].text);
            suffix_tail_head_retention_bonus_local :=
                get_suffix_tail_head_retention_bonus_local(syllables[2].text);
        end;
        try_get_best_boundary_unit_single_char_for_syllable_local(1, preferred_boundary_unit_text,
            preferred_boundary_unit_strength);
        if (preferred_boundary_unit_strength <= 0) and
            is_quantity_like_prefix_text_local(first_single_text) and
            try_get_shifted_boundary_unit_split_local(shifted_head_two_key_local,
            shifted_middle_key_local, shifted_tail_key_local,
            shifted_boundary_unit_text_local, shifted_boundary_unit_strength_local) then
        begin
            preferred_boundary_unit_text := shifted_boundary_unit_text_local;
            preferred_boundary_unit_strength := shifted_boundary_unit_strength_local;
            head_two_key := shifted_head_two_key_local;
            head_two_exact_text := '';
            head_two_exact_strength := 0;
            head_two_source := cs_rule;
            head_two_has_dict_weight := False;
            head_two_dict_weight := 0;
            if not try_get_best_exact_phrase_candidate_local(head_two_key, head_two_exact_text,
                head_two_exact_strength, head_two_source, head_two_has_dict_weight,
                head_two_dict_weight) then
            begin
                head_two_exact_text := '';
                head_two_exact_strength := 0;
            end;
            head_two_strength := head_two_exact_strength;
        end;
        if is_quantity_like_prefix_text_local(first_single_text) and
            (preferred_boundary_unit_strength > 0) then
        begin
            has_quantity_boundary_head_partial := True;
        end;
        if dictionary_lookup_cached(head_two_key, head_two_lookup_results) then
        begin
            for head_two_lookup_idx := 0 to High(head_two_lookup_results) do
            begin
                if (Trim(head_two_lookup_results[head_two_lookup_idx].comment) <> syllables[1].text) or
                    (not is_single_text_unit(Trim(head_two_lookup_results[head_two_lookup_idx].text))) or
                    (not is_quantity_like_prefix_text_local(
                    Trim(head_two_lookup_results[head_two_lookup_idx].text))) then
                begin
                    Continue;
                end;

                has_quantity_boundary_head_partial := True;
                Break;
            end;
        end;
        has_safe_trailing_initial_state := False;
        normalized_text_local := normalize_pinyin_text(m_composition_text);
        if Length(normalized_text_local) >= 3 then
        begin
            prefix_text_local := Copy(normalized_text_local, 1, Length(normalized_text_local) - 1);
            if is_single_initial_token(Copy(normalized_text_local, Length(normalized_text_local), 1)) and
                is_full_pinyin_key(prefix_text_local) then
            begin
                has_safe_trailing_initial_state := True;
            end;
        end;
        if (not has_safe_trailing_initial_state) and (Length(normalized_text_local) >= 4) then
        begin
            tail_cluster_local := Copy(normalized_text_local, Length(normalized_text_local) - 1, 2);
            prefix_text_local := Copy(normalized_text_local, 1, Length(normalized_text_local) - 2);
            if ((tail_cluster_local = 'zh') or (tail_cluster_local = 'ch') or
                (tail_cluster_local = 'sh')) and is_full_pinyin_key(prefix_text_local) then
            begin
                has_safe_trailing_initial_state := True;
            end;
        end;

        if head_two_exact_text <> '' then
        begin
            head_two_units := split_text_units(head_two_exact_text);
            if Length(head_two_units) > 0 then
            begin
                head_two_first_unit_strength := get_single_char_strength_for_text_at_syllable_local(0,
                    head_two_units[0]);
                head_two_first_unit_rank := get_single_char_rank_for_text_at_syllable_local(0,
                    head_two_units[0]);
                head_two_first_unit_matches_best := SameText(head_two_units[0], first_single_text);
                if (first_single_strength > 0) and (head_two_first_unit_strength > 0) then
                begin
                    head_two_first_unit_near_top :=
                        ((head_two_first_unit_strength * 100) >=
                        (first_single_strength * c_head_phrase_first_unit_near_top_ratio_pct_local)) or
                        ((first_single_strength - head_two_first_unit_strength) <=
                        c_head_phrase_first_unit_near_top_gap_local) or
                        ((head_two_first_unit_rank > 0) and
                        (head_two_first_unit_rank <= c_head_phrase_first_unit_top_rank_window_local));
                end;
            end;
            if Length(head_two_units) > 1 then
            begin
                head_two_boundary_unit_strength :=
                    get_single_char_strength_for_text_at_syllable_local(1, head_two_units[1]);
                head_two_boundary_unit_rank :=
                    get_single_char_rank_for_text_at_syllable_local(1, head_two_units[1]);
                head_two_boundary_unit_matches_best := SameText(head_two_units[1], middle_single_text);
                if (middle_single_strength > 0) and (head_two_boundary_unit_strength > 0) then
                begin
                    head_two_boundary_unit_near_top :=
                        ((head_two_boundary_unit_strength * 100) >=
                        (middle_single_strength * c_head_phrase_first_unit_near_top_ratio_pct_local)) or
                        ((middle_single_strength - head_two_boundary_unit_strength) <=
                        c_head_phrase_first_unit_near_top_gap_local) or
                        ((head_two_boundary_unit_rank > 0) and
                        (head_two_boundary_unit_rank <= c_head_phrase_first_unit_top_rank_window_local));
                end;
            end;
        end;

        if tail_two_exact_text <> '' then
        begin
            tail_two_units := split_text_units(tail_two_exact_text);
            if Length(tail_two_units) > 0 then
            begin
                tail_two_boundary_unit_strength :=
                    get_single_char_strength_for_text_at_syllable_local(1, tail_two_units[0]);
                tail_two_boundary_unit_rank :=
                    get_single_char_rank_for_text_at_syllable_local(1, tail_two_units[0]);
                tail_two_boundary_unit_matches_best := SameText(tail_two_units[0], middle_single_text);
                if (middle_single_strength > 0) and (tail_two_boundary_unit_strength > 0) then
                begin
                    tail_two_boundary_unit_near_top :=
                        ((tail_two_boundary_unit_strength * 100) >=
                        (middle_single_strength * c_head_phrase_first_unit_near_top_ratio_pct_local)) or
                        ((middle_single_strength - tail_two_boundary_unit_strength) <=
                        c_head_phrase_first_unit_near_top_gap_local) or
                        ((tail_two_boundary_unit_rank > 0) and
                        (tail_two_boundary_unit_rank <= c_head_phrase_first_unit_top_rank_window_local));
                end;
            end;
        end;

        if (tail_two_strength > 0) and (first_single_strength > 0) and
            (not head_two_first_unit_matches_best) then
        begin
            if not head_two_first_unit_near_top then
            begin
                Inc(tail_two_strength, Min(c_first_single_tail_bias_cap_local, first_single_strength));
            end;
        end;

        if (head_two_strength > 0) and (first_single_text <> '') then
        begin
            if (Length(head_two_units) > 0) and (head_two_units[0] <> first_single_text) then
            begin
                if not head_two_first_unit_near_top then
                begin
                    head_two_first_unit_penalty_local :=
                        c_head_phrase_first_unit_mismatch_penalty_local +
                        Min(180, first_single_strength div 2);
                    if suffix_tail_head_retention_bonus_local > 0 then
                    begin
                        head_two_first_unit_penalty_local := Min(
                            head_two_first_unit_penalty_local,
                            220 + Min(120, first_single_strength div 4));
                    end;
                    Dec(head_two_strength, head_two_first_unit_penalty_local);
                    if head_two_strength < 0 then
                    begin
                        head_two_strength := 0;
                    end;
                end;
            end;
        end;

        if (head_two_exact_strength > 0) and (not head_two_has_dict_weight) and
            (Length(head_two_units) > 0) and (head_two_units[0] <> first_single_text) and
            (not head_two_first_unit_near_top) then
        begin
            Dec(head_two_strength, Max(head_two_exact_strength,
                c_head_phrase_first_unit_mismatch_penalty_local));
            if head_two_strength < 0 then
            begin
                head_two_strength := 0;
            end;
        end;

        if (head_two_exact_strength > 0) and (tail_two_exact_strength > 0) and
            (not head_two_has_dict_weight) and tail_two_boundary_unit_near_top and
            ((tail_two_exact_strength - head_two_exact_strength) >= 360) then
        begin
            Inc(tail_two_strength, Min(c_boundary_alignment_bonus_cap_local,
                tail_two_boundary_unit_strength));
            Dec(head_two_strength, Min(c_boundary_alignment_mismatch_penalty_cap_local,
                tail_two_exact_strength - head_two_exact_strength));
            if head_two_strength < 0 then
            begin
                head_two_strength := 0;
            end;
        end;

        segmented_head_strength := get_best_single_char_strength_for_syllable_local(0) +
            get_best_single_char_strength_for_syllable_local(1);
        if (segmented_head_strength > 0) and (head_two_exact_strength <= 0) then
        begin
            Dec(segmented_head_strength, c_segmented_prefix_strength_penalty_local);
            if segmented_head_strength > head_two_strength then
            begin
                head_two_strength := segmented_head_strength;
            end;
        end;

        if (head_two_exact_strength > 0) and (last_single_strength > 0) then
        begin
            Inc(head_two_strength, Min(c_last_single_head_bias_cap_local,
                last_single_strength + Min(160, last_single_strength div 2)));
        end;

        if (head_two_exact_text <> '') and (last_single_text <> '') then
        begin
            head_two_query_path_bonus := get_query_path_support_adjustment_local(
                head_two_exact_text, last_single_text);
            if head_two_query_path_bonus <> 0 then
            begin
                Inc(head_two_strength, head_two_query_path_bonus);
            end;
        end;

        if (tail_two_exact_text <> '') and (first_single_text <> '') then
        begin
            tail_two_query_path_bonus := get_query_path_support_adjustment_local(
                first_single_text, tail_two_exact_text);
            if tail_two_query_path_bonus <> 0 then
            begin
                Inc(tail_two_strength, tail_two_query_path_bonus);
            end;
        end;

        if (suffix_tail_bias_local > 0) and (suffix_tail_support_strength_local > 0) and
            (suffix_tail_matched_text_local <> '') and (head_two_exact_strength > 0) then
        begin
            if (tail_two_exact_strength <= 0) or
                ((head_two_exact_strength * 100) >= (tail_two_exact_strength * 55)) then
            begin
                Inc(head_two_strength, suffix_tail_bias_local);
            end
            else
            begin
                Inc(head_two_strength, Max(180, suffix_tail_bias_local div 2));
            end;
            if suffix_tail_head_retention_bonus_local > 0 then
            begin
                Inc(head_two_strength, suffix_tail_head_retention_bonus_local);
            end;

            if (tail_two_exact_text <> '') and (Length(tail_two_units) > 1) and
                (not is_suffix_tail_expected_text_local(syllables[2].text, tail_two_units[1])) then
            begin
                tail_two_suffix_conflict_penalty_local := Min(
                    c_suffix_tail_conflict_penalty_cap_local,
                    Max(suffix_tail_support_strength_local div 2,
                    (suffix_tail_bias_local * 2) div 3));
                Dec(tail_two_strength, tail_two_suffix_conflict_penalty_local);
                if tail_two_strength < 0 then
                begin
                    tail_two_strength := 0;
                end;
            end;
        end;

        if (head_two_exact_strength > 0) and (tail_two_exact_strength <= 0) and
            (not has_quantity_boundary_head_partial) then
        begin
            m_last_three_syllable_partial_preference_kind := 2;
            m_last_three_syllable_head_exact_text := Trim(head_two_exact_text);
            m_last_three_syllable_head_strength := head_two_strength;
            m_last_three_syllable_tail_strength := tail_two_strength;
            m_last_three_syllable_first_single_strength := first_single_strength;
            m_last_three_syllable_last_single_strength := last_single_strength;
            m_last_three_syllable_head_path_bonus := head_two_query_path_bonus;
            m_last_three_syllable_tail_path_bonus := tail_two_query_path_bonus;
            Exit;
        end;

        if (tail_two_exact_strength > 0) and (head_two_exact_strength <= 0) and
            (not has_quantity_boundary_head_partial) then
        begin
            m_last_three_syllable_partial_preference_kind := 1;
            m_last_three_syllable_head_exact_text := Trim(head_two_exact_text);
            m_last_three_syllable_head_strength := head_two_strength;
            m_last_three_syllable_tail_strength := tail_two_strength;
            m_last_three_syllable_first_single_strength := first_single_strength;
            m_last_three_syllable_last_single_strength := last_single_strength;
            m_last_three_syllable_head_path_bonus := head_two_query_path_bonus;
            m_last_three_syllable_tail_path_bonus := tail_two_query_path_bonus;
            Exit;
        end;

        if (head_two_exact_strength > 0) and (tail_two_exact_strength > 0) then
        begin
            if (head_two_query_path_bonus <= 0) and
                ((suffix_tail_bias_local <= 0) or (suffix_tail_support_strength_local <= 0) or
                (suffix_tail_matched_text_local = '')) and
                ((tail_two_exact_strength - head_two_exact_strength) >= 220) then
            begin
                m_last_three_syllable_partial_preference_kind := 1;
                m_last_three_syllable_head_exact_text := Trim(head_two_exact_text);
                m_last_three_syllable_head_strength := head_two_strength;
                m_last_three_syllable_tail_strength := tail_two_strength;
                m_last_three_syllable_first_single_strength := first_single_strength;
                m_last_three_syllable_last_single_strength := last_single_strength;
                m_last_three_syllable_head_path_bonus := head_two_query_path_bonus;
                m_last_three_syllable_tail_path_bonus := tail_two_query_path_bonus;
                Exit;
            end;
        end;

        if has_quantity_boundary_head_partial and (preferred_boundary_unit_strength > 0) then
        begin
            Inc(head_two_strength, c_quantity_boundary_head_bias_base_local +
                Min(c_quantity_boundary_head_bias_cap_local,
                (first_single_strength + preferred_boundary_unit_strength) div 2));
            if tail_two_strength > 0 then
            begin
                Dec(tail_two_strength, c_quantity_boundary_tail_penalty_local);
                if tail_two_strength < 0 then
                begin
                    tail_two_strength := 0;
                end;
            end;
        end;

        if (head_two_exact_strength > 0) and head_two_boundary_unit_matches_best then
        begin
            Inc(head_two_strength, Min(c_boundary_alignment_bonus_cap_local, middle_single_strength));
            if (tail_two_exact_strength > 0) and (not tail_two_boundary_unit_near_top) then
            begin
                Dec(tail_two_strength, Min(c_boundary_alignment_penalty_cap_local,
                    middle_single_strength div 2));
                if tail_two_strength < 0 then
                begin
                    tail_two_strength := 0;
                end;
            end;
        end
        else if (tail_two_exact_strength > 0) and tail_two_boundary_unit_matches_best then
        begin
            Inc(tail_two_strength, Min(c_boundary_alignment_bonus_cap_local, middle_single_strength));
            if (head_two_exact_strength > 0) and (not head_two_boundary_unit_near_top) then
            begin
                Dec(head_two_strength, Min(c_boundary_alignment_penalty_cap_local,
                    middle_single_strength div 2));
                if head_two_strength < 0 then
                begin
                    head_two_strength := 0;
                end;
            end;
        end;

        if (head_two_exact_strength > 0) and head_two_boundary_unit_matches_best and
            (tail_two_exact_strength > 0) and (not tail_two_boundary_unit_matches_best) then
        begin
            Inc(head_two_strength, c_boundary_alignment_unique_best_bonus_local);
        end
        else if (tail_two_exact_strength > 0) and tail_two_boundary_unit_matches_best and
            (head_two_exact_strength > 0) and (not head_two_boundary_unit_matches_best) then
        begin
            Inc(tail_two_strength, c_boundary_alignment_unique_best_bonus_local);
        end;

        if (head_two_exact_strength > 0) and (middle_single_text <> '') and
            (Length(head_two_units) > 1) and (head_two_units[1] <> middle_single_text) and
            (not head_two_boundary_unit_near_top) then
        begin
            Dec(head_two_strength, Min(c_boundary_alignment_mismatch_penalty_cap_local,
                c_boundary_alignment_mismatch_penalty_base_local +
                Max(0, middle_single_strength - head_two_boundary_unit_strength)));
            if head_two_strength < 0 then
            begin
                head_two_strength := 0;
            end;
        end;

        if (tail_two_exact_strength > 0) and (middle_single_text <> '') and
            (Length(tail_two_units) > 0) and (tail_two_units[0] <> middle_single_text) and
            (not tail_two_boundary_unit_near_top) then
        begin
            Dec(tail_two_strength, Min(c_boundary_alignment_mismatch_penalty_cap_local,
                c_boundary_alignment_mismatch_penalty_base_local +
                Max(0, middle_single_strength - tail_two_boundary_unit_strength)));
            if tail_two_strength < 0 then
            begin
                tail_two_strength := 0;
            end;
        end;

        if (head_two_exact_text <> '') and (tail_two_exact_text <> '') then
        begin
            head_two_units := split_text_units(head_two_exact_text);
            tail_two_units := split_text_units(tail_two_exact_text);
            if (Length(head_two_units) = 2) and (Length(tail_two_units) = 2) and
                (head_two_units[1] = tail_two_units[0]) and
                is_backward_attaching_boundary_unit_local(head_two_units[1]) then
            begin
                shared_tail_friendly_unit_strength :=
                    get_single_char_strength_for_text_at_syllable_local(1, head_two_units[1]);
                shared_tail_friendly_boundary_bias :=
                    c_shared_tail_friendly_boundary_base_bias_local +
                    Min(c_shared_tail_friendly_boundary_bonus_cap_local,
                    shared_tail_friendly_unit_strength);
                Inc(head_two_strength, shared_tail_friendly_boundary_bias);
                Dec(tail_two_strength, shared_tail_friendly_boundary_bias div 2);
                if tail_two_strength < 0 then
                begin
                    tail_two_strength := 0;
                end;
            end;
        end;

        m_last_three_syllable_head_exact_text := Trim(head_two_exact_text);
        m_last_three_syllable_head_strength := head_two_strength;
        m_last_three_syllable_tail_strength := tail_two_strength;
        m_last_three_syllable_first_single_strength := first_single_strength;
        m_last_three_syllable_last_single_strength := last_single_strength;
        m_last_three_syllable_head_path_bonus := head_two_query_path_bonus;
        m_last_three_syllable_tail_path_bonus := tail_two_query_path_bonus;

        if m_config.debug_mode then
        begin
            m_last_three_syllable_partial_debug_info := Format(
                ' b3head=%d b3tail=%d b3first=%d b3last=%d b3link=%d b3qty=%d b3unit=%d b3sfx=%d b3hpath=%d b3tpath=%d headkey=%s tailkey=%s',
                [head_two_strength, tail_two_strength, first_single_strength, last_single_strength,
                shared_tail_friendly_boundary_bias, Ord(has_quantity_boundary_head_partial),
                preferred_boundary_unit_strength, suffix_tail_bias_local, head_two_query_path_bonus,
                tail_two_query_path_bonus, head_two_key, tail_two_key]);
        end;

        if has_quantity_boundary_head_partial and (preferred_boundary_unit_strength > 0) then
        begin
            m_last_three_syllable_partial_preference_kind := 2;
            Exit;
        end;

        if has_safe_trailing_initial_state and (tail_two_strength <= 0) and
            (first_single_strength > 0) then
        begin
            if (head_two_strength <= 0) or
                ((head_two_strength - first_single_strength) <=
                c_safe_trailing_initial_head_margin_local) then
            begin
                m_last_three_syllable_partial_preference_kind := 1;
            end
            else
            begin
                m_last_three_syllable_partial_preference_kind := 2;
            end;
            Exit;
        end;

        if (tail_two_strength > 0) and
            ((head_two_strength <= 0) or
            ((tail_two_strength * 100) >=
            (head_two_strength * suffix_tail_boundary_ratio_pct_local))) then
        begin
            m_last_three_syllable_partial_preference_kind := 1;
        end
        else if head_two_strength > 0 then
        begin
            m_last_three_syllable_partial_preference_kind := 2;
        end;
    end;

    procedure append_three_syllable_head_only_anchor_partial_candidates; forward;

    function merge_source_rank(const left: TncCandidateSource; const right: TncCandidateSource): TncCandidateSource;
    begin
        if (left = cs_user) or (right = cs_user) then
        begin
            Result := cs_user;
        end
        else
        begin
            Result := cs_rule;
        end;
    end;

    procedure append_candidate(const text: string; const score: Integer;
        const source: TncCandidateSource;
        const comment_override: string;
        const has_dict_weight: Boolean = False;
        const dict_weight: Integer = 0;
        const fuzzy_cost: Integer = 0;
        const fuzzy_rules: TncFuzzyPinyinRules = []);
    begin
        if text = '' then
        begin
            Exit;
        end;

        dedup_key := LowerCase(Trim(text)) + #1 + comment_override;
        if dedup.TryGetValue(dedup_key, existing_index) then
        begin
            if (existing_index >= 0) and (existing_index < list.Count) then
            begin
                item := list[existing_index];
                if score > item.score then
                begin
                    item.score := score;
                end;
                item.source := merge_source_rank(item.source, source);
                if has_dict_weight then
                begin
                    if (not item.has_dict_weight) or (dict_weight > item.dict_weight) then
                    begin
                        item.has_dict_weight := True;
                        item.dict_weight := dict_weight;
                    end;
                end;
                if fuzzy_cost = 0 then
                begin
                    item.fuzzy_cost := 0;
                    item.fuzzy_rules := [];
                end
                else if (item.fuzzy_cost > 0) and
                    (fuzzy_cost < item.fuzzy_cost) then
                begin
                    item.fuzzy_cost := fuzzy_cost;
                    item.fuzzy_rules := fuzzy_rules;
                end;
                list[existing_index] := item;
            end;
            Exit;
        end;

        item.text := text;
        item.comment := comment_override;
        item.score := score;
        item.source := source;
        item.has_dict_weight := (source = cs_rule) and has_dict_weight;
        if item.has_dict_weight then
        begin
            item.dict_weight := dict_weight;
        end
        else
        begin
            item.dict_weight := 0;
        end;
        item.fuzzy_cost := fuzzy_cost;
        item.fuzzy_rules := fuzzy_rules;
        list.Add(item);
        dedup.Add(dedup_key, list.Count - 1);
    end;

    procedure append_three_syllable_head_only_anchor_partial_candidates;
    const
        c_anchor_partial_bonus_local = 420;
        c_anchor_partial_hint_kind_scale_local = 10000;
        c_anchor_partial_hint_one_plus_two_local = 1;
        c_anchor_partial_hint_two_plus_one_local = 2;
        c_anchor_partial_tail_bonus_scale_local = 4;
        c_anchor_partial_remaining_penalty_local = 36;
        c_anchor_partial_non_preferred_penalty_local = 220;
        c_anchor_partial_non_preferred_gap_local = 48;
        c_boundary_anchor_partial_score_hint_flag_local = 1000000;
        c_segmented_prefix_strength_penalty_local = 520;
    var
        anchor_hint_local: Integer;
        remaining_pinyin: string;
        boundary_tail_key: string;
        anchor_text: string;
        anchor_strength: Integer;
        anchor_source: TncCandidateSource;
        anchor_has_dict_weight: Boolean;
        anchor_dict_weight: Integer;
        tail_strength: Integer;
        first_unit_text: string;
        first_unit_strength: Integer;
        second_unit_text: string;
        second_unit_strength: Integer;
        exact_head_key: string;
        score_value_local: Integer;
        remaining_single_text_local: string;
        remaining_single_strength_local: Integer;
        shifted_head_two_key_local: string;
        shifted_middle_key_local: string;
        shifted_tail_key_local: string;
        shifted_boundary_unit_text_local: string;
        shifted_boundary_unit_strength_local: Integer;
        use_shifted_boundary_local: Boolean;

        procedure append_exact_head_fallback_candidate(
            const preferred_score_value_local: Integer;
            const tail_support_strength_local: Integer);
        var
            fallback_text_local: string;
            fallback_strength_local: Integer;
            fallback_source_local: TncCandidateSource;
            fallback_has_dict_weight_local: Boolean;
            fallback_dict_weight_local: Integer;
            fallback_remaining_pinyin_local: string;
            fallback_last_single_text_local: string;
            fallback_last_single_strength_local: Integer;
            fallback_gap_penalty_local: Integer;
            fallback_score_local: Integer;
            fallback_hint_local: Integer;
        begin
            fallback_remaining_pinyin_local := build_syllable_text(2, Length(syllables) - 2);
            if fallback_remaining_pinyin_local = '' then
            begin
                Exit;
            end;

            if not try_get_best_scored_exact_phrase_candidate_local(build_syllable_text(0, 2),
                fallback_text_local, fallback_strength_local, fallback_source_local,
                fallback_has_dict_weight_local, fallback_dict_weight_local) then
            begin
                Exit;
            end;

            if get_candidate_text_unit_count(fallback_text_local) < 2 then
            begin
                Exit;
            end;

            fallback_last_single_text_local := '';
            fallback_last_single_strength_local := 0;
            try_get_best_single_char_candidate_for_syllable_local(2,
                fallback_last_single_text_local, fallback_last_single_strength_local);

            fallback_gap_penalty_local := 0;
            if tail_support_strength_local > fallback_strength_local then
            begin
                fallback_gap_penalty_local := Min(220,
                    (tail_support_strength_local - fallback_strength_local) div 3);
            end;

            fallback_score_local := fallback_strength_local + c_anchor_partial_bonus_local +
                Min(220, (fallback_strength_local + fallback_last_single_strength_local) div
                c_anchor_partial_tail_bonus_scale_local) -
                c_anchor_partial_remaining_penalty_local -
                c_anchor_partial_non_preferred_penalty_local -
                fallback_gap_penalty_local;
            if (preferred_score_value_local > 0) and
                (fallback_score_local >= preferred_score_value_local) then
            begin
                fallback_score_local := preferred_score_value_local -
                    c_anchor_partial_non_preferred_gap_local;
            end;

            if fallback_score_local <= 0 then
            begin
                Exit;
            end;

            append_candidate(fallback_text_local, fallback_score_local, fallback_source_local,
                fallback_remaining_pinyin_local, fallback_has_dict_weight_local,
                fallback_dict_weight_local);
            fallback_hint_local := c_boundary_anchor_partial_score_hint_flag_local +
                (c_anchor_partial_hint_two_plus_one_local *
                c_anchor_partial_hint_kind_scale_local) + fallback_score_local;
            remember_segment_path_score_hint_for_candidate(fallback_text_local,
                fallback_remaining_pinyin_local, fallback_hint_local);
        end;
    begin
        if include_full_path or (Length(syllables) < 3) or
            (not c_suppress_nonlexicon_complete_long_candidates) then
        begin
            Exit;
        end;

        if m_last_three_syllable_partial_preference_kind = 1 then
        begin
            score_value_local := 0;
            remaining_pinyin := build_syllable_text(1, Length(syllables) - 1);
            boundary_tail_key := build_syllable_text(1, 2);
            tail_strength := get_best_exact_phrase_strength_local(boundary_tail_key);
            if (remaining_pinyin <> '') and
                (not try_get_best_single_char_candidate_for_syllable_local(0, anchor_text,
                anchor_strength)) then
            begin
                anchor_text := '';
                anchor_strength := 0;
            end;
            if (remaining_pinyin <> '') and (anchor_text <> '') then
            begin
                score_value_local := anchor_strength + c_anchor_partial_bonus_local +
                    Min(220, tail_strength div c_anchor_partial_tail_bonus_scale_local) -
                    (2 * c_anchor_partial_remaining_penalty_local);
                append_candidate(anchor_text, score_value_local, cs_rule, remaining_pinyin);
                anchor_hint_local := c_boundary_anchor_partial_score_hint_flag_local +
                    (c_anchor_partial_hint_one_plus_two_local * c_anchor_partial_hint_kind_scale_local) +
                    score_value_local;
                remember_segment_path_score_hint_for_candidate(anchor_text, remaining_pinyin,
                    anchor_hint_local);
            end;

            append_exact_head_fallback_candidate(score_value_local, tail_strength);
        end
        else if m_last_three_syllable_partial_preference_kind = 2 then
        begin
            shifted_head_two_key_local := '';
            shifted_middle_key_local := '';
            shifted_tail_key_local := '';
            shifted_boundary_unit_text_local := '';
            shifted_boundary_unit_strength_local := 0;
            use_shifted_boundary_local := False;
            first_unit_text := '';
            first_unit_strength := 0;
            try_get_best_single_char_candidate_for_syllable_local(0, first_unit_text,
                first_unit_strength);
            if is_quantity_like_prefix_text_local(first_unit_text) and
                try_get_shifted_boundary_unit_split_local(shifted_head_two_key_local,
                shifted_middle_key_local, shifted_tail_key_local,
                shifted_boundary_unit_text_local, shifted_boundary_unit_strength_local) then
            begin
                use_shifted_boundary_local := True;
                remaining_pinyin := shifted_tail_key_local;
                exact_head_key := shifted_head_two_key_local;
            end
            else
            begin
                remaining_pinyin := build_syllable_text(2, Length(syllables) - 2);
                exact_head_key := build_syllable_text(0, 2);
            end;
            anchor_text := '';
            anchor_strength := 0;
            anchor_source := cs_rule;
            anchor_has_dict_weight := False;
            anchor_dict_weight := 0;
            if not try_get_best_scored_exact_phrase_candidate_local(exact_head_key, anchor_text, anchor_strength,
                anchor_source, anchor_has_dict_weight, anchor_dict_weight) then
            begin
                if (first_unit_text <> '') and
                    (((is_quantity_like_prefix_text_local(first_unit_text) and
                    (use_shifted_boundary_local or
                    try_get_best_boundary_unit_single_char_for_syllable_local(1, second_unit_text,
                    second_unit_strength))) or
                    try_get_best_single_char_candidate_for_syllable_local(1, second_unit_text,
                    second_unit_strength))) then
                begin
                    if use_shifted_boundary_local then
                    begin
                        second_unit_text := shifted_boundary_unit_text_local;
                        second_unit_strength := shifted_boundary_unit_strength_local;
                    end;
                    anchor_text := first_unit_text + second_unit_text;
                    anchor_strength := first_unit_strength + second_unit_strength -
                        c_segmented_prefix_strength_penalty_local;
                    if anchor_strength < 0 then
                    begin
                        anchor_strength := 0;
                    end;
                    anchor_source := cs_rule;
                    anchor_has_dict_weight := False;
                    anchor_dict_weight := 0;
                end;
            end;

            if (remaining_pinyin <> '') and (anchor_text <> '') then
            begin
                remaining_single_text_local := '';
                remaining_single_strength_local := 0;
                try_get_best_single_char_candidate_for_syllable_local(2, remaining_single_text_local,
                    remaining_single_strength_local);
                score_value_local := anchor_strength + c_anchor_partial_bonus_local +
                    Min(220, (anchor_strength + remaining_single_strength_local) div
                    c_anchor_partial_tail_bonus_scale_local) -
                    c_anchor_partial_remaining_penalty_local;
                append_candidate(anchor_text, score_value_local, anchor_source, remaining_pinyin,
                    anchor_has_dict_weight, anchor_dict_weight);
                anchor_hint_local := c_boundary_anchor_partial_score_hint_flag_local +
                    (c_anchor_partial_hint_two_plus_one_local * c_anchor_partial_hint_kind_scale_local) +
                    score_value_local;
                remember_segment_path_score_hint_for_candidate(anchor_text, remaining_pinyin,
                    anchor_hint_local);
            end;
        end;
    end;

    procedure append_long_prefix_phrase_anchor_partial_candidates;
    const
        c_long_prefix_anchor_bonus_local = 560;
        c_long_prefix_strength_scale_local = 4;
        c_long_prefix_remaining_penalty_local = 64;
    var
        prefix_key: string;
        remaining_pinyin: string;
        anchor_text: string;
        anchor_strength: Integer;
        anchor_source: TncCandidateSource;
        anchor_has_dict_weight: Boolean;
        anchor_dict_weight: Integer;
        remaining_syllables_local: Integer;
        score_value_local: Integer;
    begin
        if include_full_path or (Length(syllables) < 4) or
            (not c_suppress_nonlexicon_complete_long_candidates) then
        begin
            Exit;
        end;

        prefix_key := build_syllable_text(0, 3);
        remaining_syllables_local := Length(syllables) - 3;
        if remaining_syllables_local <= 0 then
        begin
            Exit;
        end;
        remaining_pinyin := build_syllable_text(3, remaining_syllables_local);
        if remaining_pinyin = '' then
        begin
            Exit;
        end;

        anchor_text := '';
        anchor_strength := 0;
        anchor_source := cs_rule;
        anchor_has_dict_weight := False;
        anchor_dict_weight := 0;
        if not try_get_best_scored_exact_phrase_candidate_local(prefix_key, anchor_text, anchor_strength,
            anchor_source, anchor_has_dict_weight, anchor_dict_weight) then
        begin
            Exit;
        end;

        if get_candidate_text_unit_count(anchor_text) < 2 then
        begin
            Exit;
        end;

        score_value_local := anchor_strength + c_long_prefix_anchor_bonus_local +
            Min(220, anchor_strength div c_long_prefix_strength_scale_local) -
            (remaining_syllables_local * c_long_prefix_remaining_penalty_local);
        append_candidate(anchor_text, score_value_local, anchor_source, remaining_pinyin,
            anchor_has_dict_weight, anchor_dict_weight);
    end;

    procedure append_full_path_candidates;
    var
        states: TArray<TList<TncSegmentPathState>>;
        state_dedup: TArray<TDictionary<string, Integer>>;
        transition_bonus_cache: TDictionary<string, Integer>;
        state_pos: Integer;
        next_pos: Integer;
        local_segment_len: Integer;
        local_segment_text: string;
        local_lookup_results: TncCandidateList;
        state_index: Integer;
        candidate_index: Integer;
        final_index: Integer;
        existing_state_index: Integer;
        keep_count: Integer;
        full_non_user_added: Integer;
        full_head_top_n: Integer;
        full_path_non_user_limit: Integer;
        full_path_budget_ms_limit: Integer;
        full_path_work_limit_limit: Int64;
        full_state_limit_limit: Integer;
        long_sentence_full_path_mode: Boolean;
        local_state: TncSegmentPathState;
        local_candidate: TncCandidate;
        local_new_state: TncSegmentPathState;
        local_existing_state: TncSegmentPathState;
        sorted_states: TArray<TncSegmentPathState>;
        local_key: string;
        allow_leading_single_char: Boolean;
        allow_single_char_path: Boolean;
        preferred_phrase_flags: TArray<Boolean>;
        preferred_phrase_max_len: TArray<Integer>;
        preferred_phrase_strength: TArray<Integer>;
        preferred_two_syllable_phrase_strength: TArray<Integer>;
        preferred_single_strength: TArray<Integer>;
        single_char_rank_maps: TArray<TDictionary<string, Integer>>;
        segment_units: TArray<string>;
        text_unit_mismatch: Integer;
        candidate_has_non_ascii: Boolean;
        local_candidate_text: string;
        local_incremental_path_bonus: Integer;
        local_rank_map: TDictionary<string, Integer>;
        local_rank: Integer;
        local_path_confidence_score: Integer;
        local_path_penalty_value: Integer;
        local_path_preference_score: Integer;
        local_recent_ranked_bonus: Integer;
        local_path_transition_bonus: Integer;
        local_query_path_bonus: Integer;
        local_segment_count: Integer;
        local_multi_char_segment_count: Integer;
        local_segment_quality_bonus: Integer;
        local_chain_shape_bonus: Integer;
        full_path_budget_start_tick: UInt64;
        full_path_budget_work_counter: Integer;
        full_path_total_work_counter: Int64;
        full_path_budget_exhausted: Boolean;
        final_complete_state_count: Integer;
        final_complete_multiseg_state_count: Integer;
        final_complete_debug_paths: string;
        final_position_debug_counts: string;

        function build_state_key(const value: TncSegmentPathState): string;
        begin
            Result := LowerCase(Trim(value.text)) + #1 + LowerCase(Trim(value.prev_text)) +
                #1 + LowerCase(Trim(value.prev_pinyin_text)) + #1 +
                LowerCase(Trim(value.prev_prev_text)) + #1 +
                IntToStr(Ord(value.has_multi_segment));
        end;

        function compare_state(const left: TncSegmentPathState; const right: TncSegmentPathState): Integer;
        const
            c_state_path_preference_margin = 72;
            c_state_path_confidence_margin = 96;
            c_state_score_close_gap = 144;
        var
            score_gap: Integer;
            left_units: Integer;
            left_segment_count: Integer;
            path_confidence_gap: Integer;
            path_preference_gap: Integer;
            right_units: Integer;
            right_segment_count: Integer;
            source_compare: Integer;
            text_compare: Integer;
            function get_segment_count(const encoded_path: string): Integer;
            var
                idx: Integer;
            begin
                Result := 0;
                if Trim(encoded_path) = '' then
                begin
                    Exit;
                end;

                Result := 1;
                for idx := 1 to Length(encoded_path) do
                begin
                    if encoded_path[idx] = c_segment_path_separator then
                    begin
                        Inc(Result);
                    end;
                end;
            end;
        begin
            text_compare := CompareText(left.text, right.text);
            if text_compare = 0 then
            begin
                path_confidence_gap := left.path_confidence_score - right.path_confidence_score;
                if Abs(path_confidence_gap) >= c_state_path_confidence_margin then
                begin
                    if path_confidence_gap > 0 then
                    begin
                        Result := -1;
                    end
                    else
                    begin
                        Result := 1;
                    end;
                    Exit;
                end;

                path_preference_gap := left.path_preference_score - right.path_preference_score;
                if Abs(path_preference_gap) >= c_state_path_preference_margin then
                begin
                    if path_preference_gap > 0 then
                    begin
                        Result := -1;
                    end
                    else
                    begin
                        Result := 1;
                    end;
                    Exit;
                end;

                left_segment_count := get_segment_count(left.path_text);
                right_segment_count := get_segment_count(right.path_text);
                if left_segment_count < right_segment_count then
                begin
                    Result := -1;
                    Exit;
                end;
                if left_segment_count > right_segment_count then
                begin
                    Result := 1;
                    Exit;
                end;
            end;

            score_gap := Abs(left.score - right.score);
            if score_gap <= c_state_score_close_gap then
            begin
                path_confidence_gap := left.path_confidence_score - right.path_confidence_score;
                if Abs(path_confidence_gap) >= c_state_path_confidence_margin then
                begin
                    if path_confidence_gap > 0 then
                    begin
                        Result := -1;
                    end
                    else
                    begin
                        Result := 1;
                    end;
                    Exit;
                end;

                path_preference_gap := left.path_preference_score - right.path_preference_score;
                if Abs(path_preference_gap) >= c_state_path_preference_margin then
                begin
                    if path_preference_gap > 0 then
                    begin
                        Result := -1;
                    end
                    else
                    begin
                        Result := 1;
                    end;
                    Exit;
                end;
            end;

            if left.score > right.score then
            begin
                Result := -1;
                Exit;
            end;
            if left.score < right.score then
            begin
                Result := 1;
                Exit;
            end;

            source_compare := get_source_rank(left.source) - get_source_rank(right.source);
            if source_compare <> 0 then
            begin
                Result := source_compare;
                Exit;
            end;

            if left.has_multi_segment <> right.has_multi_segment then
            begin
                if left.has_multi_segment then
                begin
                    Result := -1;
                end
                else
                begin
                    Result := 1;
                end;
                Exit;
            end;

            left_units := get_text_unit_count(Trim(left.text));
            right_units := get_text_unit_count(Trim(right.text));
            if left_units > right_units then
            begin
                Result := -1;
                Exit;
            end;
            if left_units < right_units then
            begin
                Result := 1;
                Exit;
            end;

            Result := text_compare;
        end;

        function get_multi_char_segment_count(const encoded_path: string): Integer;
        var
            segment_parts: TArray<string>;
            part: string;
        begin
            Result := 0;
            if Trim(encoded_path) = '' then
            begin
                Exit;
            end;

            segment_parts := encoded_path.Split([c_segment_path_separator]);
            for part in segment_parts do
            begin
                if get_candidate_text_unit_count(Trim(part)) >= 2 then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function is_debug_target_prefix(const value: string): Boolean;
        begin
            Result := is_debug_target_recall_prefix(value);
        end;

        procedure sort_state_array(var values: TArray<TncSegmentPathState>);
        var
            left_idx: Integer;
            right_idx: Integer;
            temp_state: TncSegmentPathState;
        begin
            if Length(values) <= 1 then
            begin
                Exit;
            end;

            for left_idx := 0 to High(values) - 1 do
            begin
                for right_idx := left_idx + 1 to High(values) do
                begin
                    if compare_state(values[left_idx], values[right_idx]) > 0 then
                    begin
                        temp_state := values[left_idx];
                        values[left_idx] := values[right_idx];
                        values[right_idx] := temp_state;
                    end;
                end;
            end;
        end;

        function get_exact_context_pair_bonus(const left_text: string; const candidate_text: string): Integer;
        const
            c_segment_pair_context_cap = 520;
            c_segment_pair_bonus_scale = 75;
        var
            pair_key: string;
            local_bonus: Integer;
            persistent_bonus: Integer;
            count: Integer;
            secondary_bonus: Integer;
        begin
            Result := 0;
            if (left_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            local_bonus := 0;
            persistent_bonus := 0;
            if m_context_pairs <> nil then
            begin
                pair_key := left_text + #1 + candidate_text;
                if m_context_pairs.TryGetValue(pair_key, count) and (count > 0) then
                begin
                    local_bonus := count * c_context_score_bonus;
                    if local_bonus > c_context_score_bonus_max then
                    begin
                        local_bonus := c_context_score_bonus_max;
                    end;
                end;
            end;

            if m_dictionary <> nil then
            begin
                persistent_bonus := m_dictionary.get_context_bonus(left_text, candidate_text);
            end;

            if local_bonus >= persistent_bonus then
            begin
                Result := local_bonus;
                secondary_bonus := persistent_bonus;
            end
            else
            begin
                Result := persistent_bonus;
                secondary_bonus := local_bonus;
            end;

            if secondary_bonus > 0 then
            begin
                Inc(Result, secondary_bonus div 2);
            end;
            if Result > c_segment_pair_context_cap then
            begin
                Result := c_segment_pair_context_cap;
            end;
            Result := (Result * c_segment_pair_bonus_scale) div 100;
        end;

        function get_phrase_trigram_transition_bonus(const prev_prev_text: string;
            const prev_text: string; const candidate_text: string): Integer;
        const
            c_segment_trigram_step = 96;
            c_segment_trigram_cap = 300;
            c_segment_trigram_recent_top = 140;
            c_segment_trigram_recent_mid = 84;
            c_segment_trigram_recent_tail = 40;
        var
            trigram_key: string;
            trigram_count: Integer;
            last_seen_serial: Int64;
            serial_gap: Int64;
        begin
            Result := 0;
            if (prev_prev_text = '') or (prev_text = '') or (candidate_text = '') or
                (m_phrase_context_pairs = nil) then
            begin
                Exit;
            end;

            trigram_key := prev_prev_text + #2 + prev_text + #1 + candidate_text;
            if (not m_phrase_context_pairs.TryGetValue(trigram_key, trigram_count)) or (trigram_count <= 0) then
            begin
                Exit;
            end;

            Result := trigram_count * c_segment_trigram_step;
            if Result > c_segment_trigram_cap then
            begin
                Result := c_segment_trigram_cap;
            end;

            if (m_phrase_context_last_seen <> nil) and
                m_phrase_context_last_seen.TryGetValue(trigram_key, last_seen_serial) and
                (last_seen_serial > 0) and (m_session_commit_serial >= last_seen_serial) then
            begin
                serial_gap := m_session_commit_serial - last_seen_serial;
                if serial_gap <= 1 then
                begin
                    Inc(Result, c_segment_trigram_recent_top);
                end
                else if serial_gap <= 3 then
                begin
                    Inc(Result, c_segment_trigram_recent_mid);
                end
                else if serial_gap <= 6 then
                begin
                    Inc(Result, c_segment_trigram_recent_tail);
                end;
            end;
        end;

        function get_compound_prev_trigram_bonus(const combined_prev_text: string;
            const candidate_text: string): Integer;
        var
            combined_units: TArray<string>;
            split_idx: Integer;
            unit_idx: Integer;
            left_part: string;
            right_part: string;
            local_session_bonus: Integer;
            local_persistent_bonus: Integer;
            local_secondary_bonus: Integer;
            local_bonus: Integer;
        begin
            Result := 0;
            if (combined_prev_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            combined_units := split_text_units(combined_prev_text);
            if Length(combined_units) < 2 then
            begin
                Exit;
            end;

            for split_idx := 1 to High(combined_units) do
            begin
                left_part := '';
                for unit_idx := 0 to split_idx - 1 do
                begin
                    left_part := left_part + combined_units[unit_idx];
                end;

                right_part := '';
                for unit_idx := split_idx to High(combined_units) do
                begin
                    right_part := right_part + combined_units[unit_idx];
                end;

                if (left_part = '') or (right_part = '') then
                begin
                    Continue;
                end;

                local_session_bonus := get_phrase_trigram_transition_bonus(
                    left_part, right_part, candidate_text);
                local_persistent_bonus := 0;
                if m_dictionary <> nil then
                begin
                    local_persistent_bonus := m_dictionary.get_context_trigram_bonus(
                        left_part, right_part, candidate_text);
                end;

                if local_session_bonus >= local_persistent_bonus then
                begin
                    local_bonus := local_session_bonus;
                    local_secondary_bonus := local_persistent_bonus;
                end
                else
                begin
                    local_bonus := local_persistent_bonus;
                    local_secondary_bonus := local_session_bonus;
                end;

                if local_secondary_bonus > 0 then
                begin
                    Inc(local_bonus, local_secondary_bonus div 2);
                end;

                if local_bonus > Result then
                begin
                    Result := local_bonus;
                end;
            end;
        end;

        function get_first_text_unit_for_transition_local(const value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[1];
            end;
        end;

        function get_last_text_unit_for_transition_local(const value: string): string;
        var
            trimmed_value: string;
        begin
            Result := '';
            trimmed_value := Trim(value);
            if Length(trimmed_value) > 0 then
            begin
                Result := trimmed_value[Length(trimmed_value)];
            end;
        end;

        function is_transition_unit_code_local(const value: string;
            const code_value: Integer): Boolean;
        begin
            Result := (Length(value) = 1) and (Ord(value[1]) = code_value);
        end;

        function is_transition_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $505A, $5230, $53BB, $56DE, $5F00, $60F3, $6210, $6253,
                $627E, $62FF, $6301, $63A5, $63D0, $6539, $653E, $6765,
                $7528, $770B, $7740, $7814, $7EE7, $7ED9, $80E1, $89C1,
                $8BA9, $8BB2, $8BB0, $8BF4, $8D70, $8DD1, $8F93, $8F6C,
                $8FC7, $9001, $9009, $95EE, $5199, $5229:
                    Result := True;
            end;
        end;

        function is_transition_location_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4E0A, $4E0B, $4E2D, $4E1C, $5317, $5357, $56FD, $5730,
                $573A, $57CE, $5916, $5B66, $5BB6, $5C42, $5C4B, $5DE5,
                $5E02, $5E74, $65B9, $65F6, $6708, $672C, $91CC, $897F,
                $95F4:
                    Result := True;
            end;
        end;

        function is_transition_nominal_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;
            case Ord(unit_value[1]) of
                $4EBA, $4E8B, $4EF6, $516C, $5173, $5185, $5206, $529F,
                $56FE, $5730, $57CE, $58F0, $5B66, $5DE5, $5E02, $5E74,
                $5F62, $60C5, $6587, $65B9, $6570, $670D, $672C, $673A,
                $6A21, $6B21, $6CD5, $7406, $7528, $76EE, $7CFB, $7ED3,
                $7F51, $80FD, $89C4, $8BA1, $8BED, $95EE, $9762, $9879:
                    Result := True;
            end;
        end;

        function get_structural_transition_bonus_local(const prev_text: string;
            const candidate_text: string): Integer;
        var
            prev_last_unit: string;
            candidate_first_unit: string;
            candidate_is_predicate: Boolean;
            candidate_is_location: Boolean;
            candidate_is_nominal: Boolean;
        begin
            Result := 0;
            prev_last_unit := get_last_text_unit_for_transition_local(prev_text);
            candidate_first_unit := get_first_text_unit_for_transition_local(
                candidate_text);
            if (prev_last_unit = '') or (candidate_first_unit = '') then
            begin
                Exit;
            end;

            candidate_is_predicate := is_transition_predicate_head_unit_local(
                candidate_first_unit);
            candidate_is_location := is_transition_location_head_unit_local(
                candidate_first_unit);
            candidate_is_nominal := candidate_is_location or
                is_transition_nominal_head_unit_local(candidate_first_unit);

            if is_transition_unit_code_local(prev_last_unit, $4E8E) then
            begin
                if candidate_is_location or candidate_is_nominal then
                begin
                    Inc(Result, 5200);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $4E0E) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 2600);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $5728) then
            begin
                if candidate_is_location or candidate_is_nominal then
                begin
                    Inc(Result, 2800);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $518D) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 2400);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $5730) then
            begin
                if candidate_is_predicate then
                begin
                    Inc(Result, 4200);
                end
                else if candidate_is_nominal then
                begin
                    Dec(Result, 3600);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $7684) then
            begin
                if candidate_is_predicate then
                begin
                    Dec(Result, 1800);
                end
                else if candidate_is_nominal then
                begin
                    Inc(Result, 2600);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $5230) then
            begin
                if candidate_is_location or candidate_is_predicate then
                begin
                    Inc(Result, 2400);
                end;
            end
            else if is_transition_unit_code_local(prev_last_unit, $9053) then
            begin
                if candidate_is_location then
                begin
                    Dec(Result, 1800);
                end;
            end;
        end;

        function get_path_transition_bonus(const prev_prev_text: string; const prev_text: string;
            const candidate_text: string): Integer;
        var
            cache_key: string;
            pair_bonus: Integer;
            trigram_bonus: Integer;
            persistent_trigram_bonus: Integer;
            compound_prev_trigram_bonus: Integer;
            structural_bonus: Integer;
            secondary_bonus: Integer;
        begin
            Result := 0;
            if (prev_text = '') or (candidate_text = '') then
            begin
                Exit;
            end;

            cache_key := prev_prev_text + #2 + prev_text + #1 + candidate_text;
            if (transition_bonus_cache <> nil) and transition_bonus_cache.TryGetValue(cache_key, Result) then
            begin
                Exit;
            end;

            pair_bonus := get_exact_context_pair_bonus(prev_text, candidate_text);
            trigram_bonus := get_phrase_trigram_transition_bonus(prev_prev_text, prev_text, candidate_text);
            persistent_trigram_bonus := 0;
            if (prev_prev_text <> '') and (m_dictionary <> nil) then
            begin
                persistent_trigram_bonus := m_dictionary.get_context_trigram_bonus(
                    prev_prev_text, prev_text, candidate_text);
            end;
            compound_prev_trigram_bonus := 0;
            if prev_prev_text = '' then
            begin
                compound_prev_trigram_bonus := get_compound_prev_trigram_bonus(
                    prev_text, candidate_text);
            end;

            if trigram_bonus >= persistent_trigram_bonus then
            begin
                Result := trigram_bonus;
                secondary_bonus := persistent_trigram_bonus;
            end
            else
            begin
                Result := persistent_trigram_bonus;
                secondary_bonus := trigram_bonus;
            end;

            if secondary_bonus > 0 then
            begin
                Inc(Result, secondary_bonus div 2);
            end;
            if compound_prev_trigram_bonus > Result then
            begin
                Result := compound_prev_trigram_bonus;
            end;

            if Result > 0 then
            begin
                Inc(Result, pair_bonus div 2);
            end
            else
            begin
                Result := pair_bonus;
            end;

            structural_bonus := get_structural_transition_bonus_local(
                prev_text, candidate_text);
            if structural_bonus <> 0 then
            begin
                Inc(Result, structural_bonus);
            end;

            if transition_bonus_cache <> nil then
            begin
                transition_bonus_cache.AddOrSetValue(cache_key, Result);
            end;
        end;

        function get_local_query_path_transition_bonus(const prev_pinyin_text: string;
            const candidate_pinyin_text: string; const prev_text: string;
            const candidate_text: string): Integer;
        const
            c_local_query_path_bonus_cap = 520;
            c_local_query_path_penalty_cap = 520;
        var
            cache_key: string;
            local_query: string;
            local_path: string;
            bonus_value: Integer;
            penalty_value: Integer;
        begin
            Result := 0;
            if (m_dictionary = nil) or (prev_pinyin_text = '') or
                (candidate_pinyin_text = '') or (prev_text = '') or
                (candidate_text = '') then
            begin
                Exit;
            end;

            local_query := prev_pinyin_text + candidate_pinyin_text;
            local_path := prev_text + c_segment_path_separator + candidate_text;
            cache_key := #4 + local_query + #1 + local_path;
            if (transition_bonus_cache <> nil) and
                transition_bonus_cache.TryGetValue(cache_key, Result) then
            begin
                Exit;
            end;

            bonus_value := Max(0, get_cached_query_segment_path_bonus(
                local_query, local_path));
            penalty_value := Max(0, get_cached_query_segment_path_penalty(
                local_query, local_path));
            Result := Min(c_local_query_path_bonus_cap, bonus_value) -
                Min(c_local_query_path_penalty_cap, penalty_value);
            if transition_bonus_cache <> nil then
            begin
                transition_bonus_cache.AddOrSetValue(cache_key, Result);
            end;
        end;

        function get_segment_lexical_quality_bonus(const candidate: TncCandidate;
            const segment_len: Integer; const state_pos: Integer; const next_pos: Integer): Integer;
        var
            dict_weight_value: Integer;
            remaining_syllables: Integer;
            text_units: Integer;
        begin
            Result := 0;
            text_units := get_text_unit_count(Trim(candidate.text));
            if text_units <= 0 then
            begin
                Exit;
            end;

            remaining_syllables := Length(syllables) - next_pos;
            if candidate.source = cs_user then
            begin
                if (text_units = segment_len) and (segment_len >= 2) then
                begin
                    Result := 84 + (segment_len * 12);
                end;
                Exit;
            end;

            if candidate.has_dict_weight then
            begin
                dict_weight_value := candidate.dict_weight;
            end
            else
            begin
                dict_weight_value := candidate.score;
            end;

            if (text_units = segment_len) and (segment_len >= 2) then
            begin
                if dict_weight_value >= 760 then
                begin
                    Inc(Result, 180);
                end
                else if dict_weight_value >= 620 then
                begin
                    Inc(Result, 128);
                end
                else if dict_weight_value >= 480 then
                begin
                    Inc(Result, 84);
                end
                else if dict_weight_value >= 340 then
                begin
                    Inc(Result, 40);
                end;

                if (segment_len >= 3) and (dict_weight_value >= 420) then
                begin
                    Inc(Result, 28 + ((segment_len - 3) * 10));
                end;

                if next_pos = Length(syllables) then
                begin
                    if state_pos = 0 then
                    begin
                        Inc(Result, 96);
                    end
                    else
                    begin
                        Inc(Result, 44);
                    end;
                end
                else if remaining_syllables <= 2 then
                begin
                    Inc(Result, 20);
                end;
                Exit;
            end;

            if (segment_len = 1) and (remaining_syllables > 0) then
            begin
                if dict_weight_value < 260 then
                begin
                    Dec(Result, 72);
                end
                else if dict_weight_value < 360 then
                begin
                    Dec(Result, 44);
                end
                else
                begin
                    Dec(Result, 18);
                end;
                Exit;
            end;

            if text_units <> segment_len then
            begin
                Dec(Result, Abs(text_units - segment_len) * 36);
            end;
        end;

        function get_segment_chain_shape_bonus(const current_state: TncSegmentPathState;
            const segment_text_units: Integer; const segment_len: Integer;
            const state_pos: Integer; const next_pos: Integer): Integer;
        var
            previous_segment_count: Integer;
            previous_text_units: Integer;
            remaining_syllables: Integer;
        begin
            Result := 0;
            if segment_text_units <= 0 then
            begin
                Exit;
            end;

            previous_segment_count := get_encoded_path_segment_count_local(current_state.path_text);
            if Trim(current_state.path_text) = '' then
            begin
                previous_segment_count := 0;
            end;
            previous_text_units := get_text_unit_count(Trim(current_state.text));
            remaining_syllables := Length(syllables) - next_pos;

            if segment_text_units = segment_len then
            begin
                if segment_len >= 2 then
                begin
                    if previous_segment_count >= 1 then
                    begin
                        Inc(Result, 54);
                    end;
                    if (segment_len = 2) and (remaining_syllables = 2) then
                    begin
                        Inc(Result, 86);
                    end;
                    if (state_pos = 0) and (remaining_syllables >= 1) and (remaining_syllables <= 3) then
                    begin
                        Inc(Result, 44);
                    end;
                    if (remaining_syllables = 0) and (previous_text_units >= 2) then
                    begin
                        Inc(Result, 112);
                    end;
                end
                else if (segment_len = 1) and (previous_segment_count >= 1) then
                begin
                    if remaining_syllables > 0 then
                    begin
                        Dec(Result, 72);
                    end
                    else if previous_text_units >= 2 then
                    begin
                        Dec(Result, 36);
                    end;
                end;
                Exit;
            end;

            if (segment_len >= 2) and (Abs(segment_text_units - segment_len) = 1) and
                (previous_segment_count >= 1) then
            begin
                Dec(Result, 24);
            end;
        end;

        procedure append_state(const position: Integer; const value: TncSegmentPathState);
        begin
            if is_debug_target_prefix(value.text) and
                (position > m_debug_target_max_generated_syllable) then
            begin
                m_debug_target_max_generated_syllable := position;
            end;
            if is_debug_target_prefix(value.text) and
                (position > m_debug_target_full_path_generated_syllable) then
            begin
                m_debug_target_full_path_generated_syllable := position;
            end;

            local_key := build_state_key(value);
            if state_dedup[position].TryGetValue(local_key, existing_state_index) then
            begin
                if (existing_state_index >= 0) and (existing_state_index < states[position].Count) then
                begin
                    local_existing_state := states[position][existing_state_index];
                    if compare_state(value, local_existing_state) < 0 then
                    begin
                        local_existing_state := value;
                    end;
                    local_existing_state.source := merge_source_rank(local_existing_state.source, value.source);
                    states[position][existing_state_index] := local_existing_state;
                end;
                Exit;
            end;

            states[position].Add(value);
            state_dedup[position].Add(local_key, states[position].Count - 1);
        end;

        function full_path_budget_reached(const force_tick_check: Boolean = False): Boolean;
        var
            timed_budget_ms_local: UInt64;
            emergency_budget_ms_local: UInt64;
        begin
            Result := full_path_budget_exhausted;
            if Result then
            begin
                Exit;
            end;

            if not force_tick_check then
            begin
                Inc(full_path_total_work_counter);
                Inc(full_path_budget_work_counter);
                if (full_path_total_work_counter < full_path_work_limit_limit) and
                    (full_path_budget_work_counter <
                    c_segment_full_path_budget_probe_interval) then
                begin
                    Exit(False);
                end;
            end;

            full_path_budget_work_counter := 0;
            if m_composition_built_incrementally then
            begin
                timed_budget_ms_local := full_path_budget_ms_limit;
                emergency_budget_ms_local := full_path_budget_ms_limit * 4;
            end
            else
            begin
                timed_budget_ms_local := 0;
                emergency_budget_ms_local := 0;
            end;
            full_path_budget_exhausted := search_budget_should_stop(
                full_path_budget_start_tick, full_path_total_work_counter,
                full_path_work_limit_limit, timed_budget_ms_local,
                emergency_budget_ms_local, long_sentence_full_path_mode);
            Result := full_path_budget_exhausted;
        end;

        procedure record_final_debug_path(const value: string);
        begin
            if Trim(value) = '' then
            begin
                Exit;
            end;

            if final_complete_debug_paths = '' then
            begin
                final_complete_debug_paths := value;
            end
            else if Pos(value, final_complete_debug_paths) <= 0 then
            begin
                final_complete_debug_paths := final_complete_debug_paths + ' || ' + value;
            end;
        end;

        procedure append_position_debug_count(const position: Integer; const count: Integer);
        begin
            if final_position_debug_counts <> '' then
            begin
                final_position_debug_counts := final_position_debug_counts + ',';
            end;
            final_position_debug_counts := final_position_debug_counts + IntToStr(position) + ':' + IntToStr(count);
        end;

        procedure trim_state(const position: Integer);
        const
            c_segment_full_state_confidence_gap_limit = 320;
            c_segment_full_state_min_keep = 48;
            c_segment_full_state_score_gap_limit = 720;
        var
            best_path_confidence: Integer;
            best_score: Integer;
            dynamic_keep_count: Integer;
            idx: Integer;
        begin
            if states[position].Count <= full_state_limit_limit then
            begin
                Exit;
            end;

            SetLength(sorted_states, states[position].Count);
            for idx := 0 to states[position].Count - 1 do
            begin
                sorted_states[idx] := states[position][idx];
            end;
            sort_state_array(sorted_states);
            keep_count := full_state_limit_limit;
            if (not long_sentence_full_path_mode) and (keep_count > c_segment_full_state_min_keep) then
            begin
                best_score := sorted_states[0].score;
                best_path_confidence := sorted_states[0].path_confidence_score;
                dynamic_keep_count := c_segment_full_state_min_keep;
                for idx := c_segment_full_state_min_keep to keep_count - 1 do
                begin
                    if ((best_score - sorted_states[idx].score) > c_segment_full_state_score_gap_limit) and
                        ((best_path_confidence - sorted_states[idx].path_confidence_score) >
                        c_segment_full_state_confidence_gap_limit) then
                    begin
                        Break;
                    end;
                    dynamic_keep_count := idx + 1;
                end;
                keep_count := dynamic_keep_count;
            end;

            states[position].Clear;
            state_dedup[position].Clear;
            for idx := 0 to keep_count - 1 do
            begin
                states[position].Add(sorted_states[idx]);
                state_dedup[position].Add(build_state_key(sorted_states[idx]), idx);
            end;
        end;

        procedure capture_debug_target_state_summary;
        var
            debug_final_states: TArray<TncSegmentPathState>;
            debug_position: Integer;
            debug_state_index: Integer;
            debug_eligible_rank: Integer;
            debug_state: TncSegmentPathState;
        begin
            if m_debug_target_recall_text = '' then
            begin
                Exit;
            end;

            if full_path_budget_exhausted then
            begin
                m_debug_target_full_path_budget_exhausted := True;
            end;

            for debug_position := 0 to High(states) do
            begin
                for debug_state_index := 0 to states[debug_position].Count - 1 do
                begin
                    if is_debug_target_prefix(states[debug_position][debug_state_index].text) and
                        (debug_position > m_debug_target_max_retained_syllable) then
                    begin
                        m_debug_target_max_retained_syllable := debug_position;
                    end;
                    if is_debug_target_prefix(states[debug_position][debug_state_index].text) and
                        (debug_position > m_debug_target_full_path_retained_syllable) then
                    begin
                        m_debug_target_full_path_retained_syllable := debug_position;
                    end;
                end;
            end;

            if states[Length(syllables)].Count <= 0 then
            begin
                Exit;
            end;

            SetLength(debug_final_states, states[Length(syllables)].Count);
            for debug_state_index := 0 to states[Length(syllables)].Count - 1 do
            begin
                debug_final_states[debug_state_index] :=
                    states[Length(syllables)][debug_state_index];
            end;
            sort_state_array(debug_final_states);

            debug_eligible_rank := 0;
            for debug_state_index := 0 to High(debug_final_states) do
            begin
                debug_state := debug_final_states[debug_state_index];
                if (not debug_state.has_multi_segment) and
                    (not ((Pos('''', m_composition_text) > 0) and
                    (get_encoded_path_segment_count_local(debug_state.path_text) > 1))) then
                begin
                    Continue;
                end;
                if get_multi_char_segment_count(debug_state.path_text) <= 0 then
                begin
                    Continue;
                end;

                Inc(debug_eligible_rank);
                if SameText(Trim(debug_state.text), m_debug_target_recall_text) then
                begin
                    m_debug_target_final_state_source_mask :=
                        m_debug_target_final_state_source_mask or 16;
                    if (m_debug_target_full_final_state_rank = 0) or
                        (debug_eligible_rank <
                        m_debug_target_full_final_state_rank) then
                    begin
                        m_debug_target_full_final_state_rank := debug_eligible_rank;
                    end;
                    if (not m_debug_target_final_state_found) or
                        (debug_eligible_rank < m_debug_target_final_state_rank) then
                    begin
                        m_debug_target_final_state_found := True;
                        m_debug_target_final_state_rank := debug_eligible_rank;
                    end;
                end;
            end;
            if debug_eligible_rank > m_debug_target_final_state_count then
            begin
                m_debug_target_final_state_count := debug_eligible_rank;
            end;
        end;

        procedure append_long_sentence_exact_aligned_complete_candidates;
        const
            c_exact_state_limit = 64;
            c_exact_single_top_n = 2;
            c_exact_multi_top_n = 4;
            c_exact_complete_non_user_limit = 6;
            c_exact_single_penalty = 220;
            c_exact_multi_bonus = 260;
            c_exact_completion_bonus = 420;
            c_exact_continuation_bonus = 24;
        var
            exact_states: TArray<TList<TncSegmentPathState>>;
            exact_dedup: TArray<TDictionary<string, Integer>>;
            exact_sorted_states: TArray<TncSegmentPathState>;
            exact_state: TncSegmentPathState;
            exact_new_state: TncSegmentPathState;
            exact_existing_state: TncSegmentPathState;
            exact_candidate: TncCandidate;
            exact_lookup_results: TncCandidateList;
            exact_state_pos: Integer;
            exact_next_pos: Integer;
            exact_segment_len: Integer;
            exact_state_index: Integer;
            exact_candidate_index: Integer;
            exact_existing_state_index: Integer;
            exact_keep_count: Integer;
            exact_non_user_added: Integer;
            exact_segment_text: string;
            exact_candidate_text: string;
            exact_key: string;
            exact_units: Integer;
            exact_segment_count: Integer;
            exact_effective_weight: Integer;
            exact_transition_bonus: Integer;
            exact_pair_bonus: Integer;
            exact_query_path_bonus: Integer;
            exact_candidate_limit: Integer;
            exact_is_preferred: Boolean;
            exact_multi_char_segment_count: Integer;

            function get_multi_char_segment_count(const encoded_path: string): Integer;
            var
                segment_parts: TArray<string>;
                part: string;
            begin
                Result := 0;
                if Trim(encoded_path) = '' then
                begin
                    Exit;
                end;

                segment_parts := encoded_path.Split([c_segment_path_separator]);
                for part in segment_parts do
                begin
                    if get_text_unit_count(Trim(part)) >= 2 then
                    begin
                        Inc(Result);
                    end;
                end;
            end;

            function is_long_sentence_allowed_single_char_text(const value: string): Boolean;
            begin
                Result := (value = string(Char($6211))) or
                    (value = string(Char($4F60))) or
                    (value = string(Char($4ED6))) or
                    (value = string(Char($5979))) or
                    (value = string(Char($5B83))) or
                    (value = string(Char($548C))) or
                    (value = string(Char($53BB))) or
                    (value = string(Char($6765))) or
                    (value = string(Char($5728))) or
                    (value = string(Char($662F))) or
                    (value = string(Char($6709))) or
                    (value = string(Char($8981))) or
                    (value = string(Char($60F3))) or
                    (value = string(Char($4F1A))) or
                    (value = string(Char($80FD))) or
                    (value = string(Char($53EA))) or
                    (value = string(Char($628A))) or
                    (value = string(Char($88AB))) or
                    (value = string(Char($8BA9))) or
                    (value = string(Char($7ED9))) or
                    (value = string(Char($5411))) or
                    (value = string(Char($5BF9))) or
                    (value = string(Char($4E8E))) or
                    (value = string(Char($8DDF))) or
                    (value = string(Char($540C))) or
                    (value = string(Char($4ECE))) or
                    (value = string(Char($4E0E))) or
                    (value = string(Char($800C))) or
                    (value = string(Char($5E76))) or
                    (value = string(Char($5C31))) or
                    (value = string(Char($4E5F))) or
                    (value = string(Char($90FD))) or
                    (value = string(Char($8FD8))) or
                    (value = string(Char($53C8))) or
                    (value = string(Char($518D))) or
                    (value = string(Char($4E0D))) or
                    (value = string(Char($5F88))) or
                    (value = string(Char($592A))) or
                    (value = string(Char($7684))) or
                    (value = string(Char($4EBA))) or
                    (value = string(Char($4EEC))) or
                    (value = string(Char($822C))) or
                    (value = string(Char($529B))) or
                    (value = string(Char($65F6))) or
                    (value = string(Char($4F4D))) or
                    (value = string(Char($7740))) or
                    (value = string(Char($8FD9)));
            end;
            procedure append_exact_state(const position: Integer; const value: TncSegmentPathState);
            begin
                exact_key := build_state_key(value);
                if exact_dedup[position].TryGetValue(exact_key, exact_existing_state_index) then
                begin
                    if (exact_existing_state_index >= 0) and
                        (exact_existing_state_index < exact_states[position].Count) then
                    begin
                        exact_existing_state := exact_states[position][exact_existing_state_index];
                        if compare_state(value, exact_existing_state) < 0 then
                        begin
                            exact_existing_state := value;
                        end;
                        exact_existing_state.source := merge_source_rank(
                            exact_existing_state.source, value.source);
                        exact_states[position][exact_existing_state_index] := exact_existing_state;
                    end;
                    Exit;
                end;

                exact_states[position].Add(value);
                exact_dedup[position].Add(exact_key, exact_states[position].Count - 1);
            end;

            procedure trim_exact_state(const position: Integer);
            var
                idx: Integer;
            begin
                if exact_states[position].Count <= c_exact_state_limit then
                begin
                    Exit;
                end;

                SetLength(exact_sorted_states, exact_states[position].Count);
                for idx := 0 to exact_states[position].Count - 1 do
                begin
                    exact_sorted_states[idx] := exact_states[position][idx];
                end;
                sort_state_array(exact_sorted_states);
                exact_keep_count := c_exact_state_limit;

                exact_states[position].Clear;
                exact_dedup[position].Clear;
                for idx := 0 to exact_keep_count - 1 do
                begin
                    exact_states[position].Add(exact_sorted_states[idx]);
                    exact_dedup[position].Add(build_state_key(exact_sorted_states[idx]), idx);
                end;
            end;
        begin
            SetLength(exact_states, Length(syllables) + 1);
            SetLength(exact_dedup, Length(syllables) + 1);
            try
                for exact_state_pos := 0 to High(exact_states) do
                begin
                    exact_states[exact_state_pos] := TList<TncSegmentPathState>.Create;
                    exact_dedup[exact_state_pos] := TDictionary<string, Integer>.Create;
                end;

                exact_state.text := '';
                exact_state.score := 0;
                exact_state.path_preference_score := 0;
                exact_state.path_confidence_score := 0;
                exact_state.source := cs_rule;
                exact_state.has_multi_segment := False;
                exact_state.uses_prefix_completion := False;
                get_recent_path_context_seed(exact_state.prev_prev_text, exact_state.prev_text);
                exact_state.prev_pinyin_text := '';
                exact_state.path_text := '';
                exact_state.fuzzy_cost := 0;
                exact_state.fuzzy_rules := [];
                exact_states[0].Add(exact_state);
                exact_dedup[0].Add(build_state_key(exact_state), 0);

                for exact_state_pos := 0 to High(syllables) do
                begin
                    if exact_states[exact_state_pos].Count = 0 then
                    begin
                        Continue;
                    end;

                    for exact_segment_len := 1 to Min(4, max_word_len) do
                    begin
                        if (exact_state_pos = 0) and (exact_segment_len = 1) then
                        begin
                            Continue;
                        end;
                        exact_next_pos := exact_state_pos + exact_segment_len;
                        if exact_next_pos > Length(syllables) then
                        begin
                            Break;
                        end;

                        exact_segment_text := build_syllable_text(exact_state_pos, exact_segment_len);
                        if (exact_segment_text = '') or
                            (not dictionary_lookup_cached(exact_segment_text, exact_lookup_results)) then
                        begin
                            Continue;
                        end;

                        if exact_segment_len = 1 then
                        begin
                            exact_candidate_limit := c_exact_single_top_n;
                        end
                        else
                        begin
                            exact_candidate_limit := c_exact_multi_top_n;
                        end;

                        for exact_state_index := 0 to exact_states[exact_state_pos].Count - 1 do
                        begin
                            exact_state := exact_states[exact_state_pos][exact_state_index];
                            for exact_candidate_index := 0 to High(exact_lookup_results) do
                            begin
                                if exact_candidate_index >= exact_candidate_limit then
                                begin
                                    Break;
                                end;

                                exact_candidate := exact_lookup_results[exact_candidate_index];
                                exact_candidate_text := Trim(exact_candidate.text);
                                if (exact_candidate_text = '') or
                                    (not contains_non_ascii(exact_candidate_text)) then
                                begin
                                    Continue;
                                end;

                                exact_units := get_text_unit_count(exact_candidate_text);
                                if exact_units <= 0 then
                                begin
                                    Continue;
                                end;

                                if exact_segment_len = 1 then
                                begin
                                    if (not is_single_text_unit(exact_candidate_text)) or
                                        (not is_long_sentence_allowed_single_char_text(exact_candidate_text)) or
                                        (not match_single_char_candidate_for_syllable(
                                        syllables[exact_state_pos].text, exact_candidate_text,
                                        exact_is_preferred)) or
                                        (not exact_is_preferred) then
                                    begin
                                        Continue;
                                    end;
                                end
                                else
                                begin
                                    if (not is_multi_char_word(exact_candidate_text)) or
                                        (exact_units <> exact_segment_len) then
                                    begin
                                        Continue;
                                    end;
                                end;

                                exact_new_state.text := exact_state.text + exact_candidate_text;
                                exact_new_state.score := exact_state.score;
                                exact_effective_weight := get_candidate_effective_weight_local(exact_candidate);
                                Inc(exact_new_state.score, exact_effective_weight);
                                exact_transition_bonus := get_path_transition_bonus(
                                    exact_state.prev_prev_text, exact_state.prev_text,
                                    exact_candidate_text);
                                if exact_transition_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_transition_bonus);
                                end;
                                exact_pair_bonus := get_exact_context_pair_bonus(
                                    exact_state.prev_text, exact_candidate_text);
                                if exact_pair_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_pair_bonus);
                                end;
                                exact_query_path_bonus := get_local_query_path_transition_bonus(
                                    exact_state.prev_pinyin_text, exact_segment_text,
                                    exact_state.prev_text, exact_candidate_text);
                                if exact_query_path_bonus <> 0 then
                                begin
                                    Inc(exact_new_state.score, exact_query_path_bonus);
                                end;

                                if exact_segment_len = 1 then
                                begin
                                    Dec(exact_new_state.score, c_exact_single_penalty);
                                end
                                else
                                begin
                                    Inc(exact_new_state.score,
                                        c_exact_multi_bonus + (exact_segment_len * 24));
                                end;

                                if exact_next_pos = Length(syllables) then
                                begin
                                    Inc(exact_new_state.score, c_exact_completion_bonus);
                                end
                                else
                                begin
                                    Inc(exact_new_state.score, c_exact_continuation_bonus);
                                end;

                                exact_new_state.source := merge_source_rank(
                                    exact_state.source, exact_candidate.source);
                                exact_new_state.fuzzy_cost := exact_state.fuzzy_cost +
                                    exact_candidate.fuzzy_cost;
                                exact_new_state.fuzzy_rules := exact_state.fuzzy_rules +
                                    exact_candidate.fuzzy_rules;
                                exact_new_state.prev_prev_text := exact_state.prev_text;
                                exact_new_state.prev_text := exact_candidate_text;
                                exact_new_state.prev_pinyin_text := exact_segment_text;
                                exact_new_state.path_text := exact_state.path_text;
                                if exact_new_state.path_text <> '' then
                                begin
                                    exact_new_state.path_text := exact_new_state.path_text +
                                        c_segment_path_separator;
                                end;
                                exact_new_state.path_text := exact_new_state.path_text +
                                    exact_candidate_text;
                                exact_new_state.has_multi_segment :=
                                    get_encoded_path_segment_count_local(exact_new_state.path_text) > 1;
                                exact_new_state.uses_prefix_completion :=
                                    exact_state.uses_prefix_completion;
                                exact_new_state.path_preference_score := exact_transition_bonus +
                                    exact_pair_bonus + exact_query_path_bonus;
                                exact_new_state.path_confidence_score := exact_new_state.path_preference_score;
                                if exact_new_state.path_confidence_score < 0 then
                                begin
                                    exact_new_state.path_confidence_score := 0;
                                end;
                                append_exact_state(exact_next_pos, exact_new_state);
                            end;
                        end;

                        trim_exact_state(exact_next_pos);
                    end;
                end;

                if exact_states[Length(syllables)].Count <= 0 then
                begin
                    Exit;
                end;

                SetLength(exact_sorted_states, exact_states[Length(syllables)].Count);
                for exact_state_pos := 0 to exact_states[Length(syllables)].Count - 1 do
                begin
                    exact_sorted_states[exact_state_pos] := exact_states[Length(syllables)][exact_state_pos];
                end;
                sort_state_array(exact_sorted_states);

                exact_non_user_added := 0;
                for exact_state_pos := 0 to High(exact_sorted_states) do
                begin
                    exact_state := exact_sorted_states[exact_state_pos];
                    exact_segment_count := get_encoded_path_segment_count_local(exact_state.path_text);
                    exact_multi_char_segment_count :=
                        get_multi_char_segment_count(exact_state.path_text);
                    if (exact_segment_count <= 1) or
                        (get_candidate_text_unit_count(exact_state.text) < 4) then
                    begin
                        Continue;
                    end;
                    if exact_multi_char_segment_count < 3 then
                    begin
                        Continue;
                    end;
                    if (exact_multi_char_segment_count * 2) < exact_segment_count then
                    begin
                        Continue;
                    end;
                    if (exact_state.source <> cs_user) and
                        (exact_non_user_added >= c_exact_complete_non_user_limit) then
                    begin
                        Continue;
                    end;

                    append_candidate(exact_state.text, exact_state.score + 1200,
                        exact_state.source, '', False, 0,
                        exact_state.fuzzy_cost, exact_state.fuzzy_rules);
                    remember_segment_path_for_candidate(exact_state.text, '',
                        exact_state.path_text, exact_state.score + 1200);
                    if exact_state.source <> cs_user then
                    begin
                        Inc(exact_non_user_added);
                    end;
                end;
            finally
                for exact_state_pos := 0 to High(exact_states) do
                begin
                    if exact_states[exact_state_pos] <> nil then
                    begin
                        exact_states[exact_state_pos].Free;
                    end;
                    if exact_dedup[exact_state_pos] <> nil then
                    begin
                        exact_dedup[exact_state_pos].Free;
                    end;
                end;
            end;
        end;

        procedure append_long_sentence_best_exact_candidate;
        type
            TLongSentenceOption = record
                text: string;
                path_text: string;
                score: Integer;
                source: TncCandidateSource;
                segment_count: Integer;
                multi_segment_count: Integer;
                fuzzy_cost: Integer;
                fuzzy_rules: TncFuzzyPinyinRules;
                found: Boolean;
            end;
        const
            c_search_multi_top_n = 3;
            c_search_single_top_n = 1;
            c_search_max_segment_len = 4;
            c_search_single_penalty = 120;
            c_search_multi_bonus = 260;
            c_search_completion_bonus = 420;
        var
            result_cache: TDictionary<string, TLongSentenceOption>;
            seed_prev_prev_text: string;
            seed_prev_text: string;

            function is_long_sentence_allowed_single_char_text_local(const value: string): Boolean;
            begin
                Result := (value = string(Char($6211))) or
                    (value = string(Char($4F60))) or
                    (value = string(Char($4ED6))) or
                    (value = string(Char($5979))) or
                    (value = string(Char($5B83))) or
                    (value = string(Char($548C))) or
                    (value = string(Char($53BB))) or
                    (value = string(Char($6765))) or
                    (value = string(Char($5728))) or
                    (value = string(Char($662F))) or
                    (value = string(Char($6709))) or
                    (value = string(Char($8981))) or
                    (value = string(Char($60F3))) or
                    (value = string(Char($4F1A))) or
                    (value = string(Char($80FD))) or
                    (value = string(Char($53EA))) or
                    (value = string(Char($628A))) or
                    (value = string(Char($88AB))) or
                    (value = string(Char($8BA9))) or
                    (value = string(Char($7ED9))) or
                    (value = string(Char($5411))) or
                    (value = string(Char($5BF9))) or
                    (value = string(Char($4E8E))) or
                    (value = string(Char($8DDF))) or
                    (value = string(Char($540C))) or
                    (value = string(Char($4ECE))) or
                    (value = string(Char($4E0E))) or
                    (value = string(Char($800C))) or
                    (value = string(Char($5E76))) or
                    (value = string(Char($5C31))) or
                    (value = string(Char($4E5F))) or
                    (value = string(Char($90FD))) or
                    (value = string(Char($8FD8))) or
                    (value = string(Char($53C8))) or
                    (value = string(Char($518D))) or
                    (value = string(Char($4E0D))) or
                    (value = string(Char($5F88))) or
                    (value = string(Char($592A))) or
                    (value = string(Char($7684))) or
                    (value = string(Char($4EBA))) or
                    (value = string(Char($4EEC))) or
                    (value = string(Char($822C))) or
                    (value = string(Char($529B))) or
                    (value = string(Char($65F6))) or
                    (value = string(Char($4F4D))) or
                    (value = string(Char($7740))) or
                    (value = string(Char($8FD9)));
            end;
            function is_better_long_sentence_option(const left: TLongSentenceOption;
                const right: TLongSentenceOption): Boolean;
            begin
                if not right.found then
                begin
                    Result := left.found;
                    Exit;
                end;
                if not left.found then
                begin
                    Result := False;
                    Exit;
                end;

                if left.score <> right.score then
                begin
                    Result := left.score > right.score;
                    Exit;
                end;
                if left.multi_segment_count <> right.multi_segment_count then
                begin
                    Result := left.multi_segment_count > right.multi_segment_count;
                    Exit;
                end;
                if left.segment_count <> right.segment_count then
                begin
                    Result := left.segment_count < right.segment_count;
                    Exit;
                end;
                Result := Length(left.text) > Length(right.text);
            end;

            function solve_long_sentence(const position: Integer; const prev_prev_text: string;
                const prev_text: string; const prev_pinyin_text: string): TLongSentenceOption;
            var
                cache_key: string;
                best_option: TLongSentenceOption;
                suffix_option: TLongSentenceOption;
                candidate_option: TLongSentenceOption;
                lookup_results_local: TncCandidateList;
                candidate_local: TncCandidate;
                candidate_text_local: string;
                segment_text_local: string;
                segment_len_local: Integer;
                next_pos_local: Integer;
                candidate_index_local: Integer;
                candidate_limit_local: Integer;
                candidate_units_local: Integer;
                is_preferred_local: Boolean;
                candidate_weight_local: Integer;
                transition_bonus_local: Integer;
                pair_bonus_local: Integer;
                query_path_bonus_local: Integer;
            begin
                cache_key := IntToStr(position) + #1 + prev_prev_text + #1 + prev_text +
                    #1 + prev_pinyin_text;
                if result_cache.TryGetValue(cache_key, Result) then
                begin
                    Exit;
                end;

                FillChar(best_option, SizeOf(best_option), 0);
                best_option.found := False;

                if position >= Length(syllables) then
                begin
                    best_option.found := True;
                    result_cache.AddOrSetValue(cache_key, best_option);
                    Result := best_option;
                    Exit;
                end;

                for segment_len_local := 1 to Min(c_search_max_segment_len, max_word_len) do
                begin
                    if (position = 0) and (segment_len_local = 1) then
                    begin
                        Continue;
                    end;

                    next_pos_local := position + segment_len_local;
                    if next_pos_local > Length(syllables) then
                    begin
                        Break;
                    end;

                    segment_text_local := build_syllable_text(position, segment_len_local);
                    if (segment_text_local = '') or
                        (not dictionary_lookup_cached(segment_text_local, lookup_results_local)) then
                    begin
                        Continue;
                    end;

                    if segment_len_local = 1 then
                    begin
                        candidate_limit_local := c_search_single_top_n;
                    end
                    else
                    begin
                        candidate_limit_local := c_search_multi_top_n;
                    end;

                    for candidate_index_local := 0 to High(lookup_results_local) do
                    begin
                        if candidate_index_local >= candidate_limit_local then
                        begin
                            Break;
                        end;

                        candidate_local := lookup_results_local[candidate_index_local];
                        candidate_text_local := Trim(candidate_local.text);
                        if (candidate_text_local = '') or
                            (not contains_non_ascii(candidate_text_local)) then
                        begin
                            Continue;
                        end;

                        candidate_units_local := get_text_unit_count(candidate_text_local);
                        if candidate_units_local <= 0 then
                        begin
                            Continue;
                        end;

                        if segment_len_local = 1 then
                        begin
                            if (not is_single_text_unit(candidate_text_local)) or
                                (not is_long_sentence_allowed_single_char_text_local(candidate_text_local)) or
                                (not match_single_char_candidate_for_syllable(
                                syllables[position].text, candidate_text_local, is_preferred_local)) or
                                (not is_preferred_local) then
                            begin
                                Continue;
                            end;
                        end
                        else
                        begin
                            if (not is_multi_char_word(candidate_text_local)) or
                                (candidate_units_local <> segment_len_local) then
                            begin
                                Continue;
                            end;
                        end;

                        suffix_option := solve_long_sentence(next_pos_local, prev_text,
                            candidate_text_local, segment_text_local);
                        if not suffix_option.found then
                        begin
                            Continue;
                        end;

                        candidate_option.found := True;
                        candidate_option.text := candidate_text_local + suffix_option.text;
                        candidate_option.path_text := candidate_text_local;
                        if suffix_option.path_text <> '' then
                        begin
                            candidate_option.path_text := candidate_option.path_text +
                                c_segment_path_separator + suffix_option.path_text;
                        end;
                        candidate_option.source := merge_source_rank(candidate_local.source,
                            suffix_option.source);
                        candidate_option.fuzzy_cost := candidate_local.fuzzy_cost +
                            suffix_option.fuzzy_cost;
                        candidate_option.fuzzy_rules := candidate_local.fuzzy_rules +
                            suffix_option.fuzzy_rules;
                        candidate_option.segment_count := suffix_option.segment_count + 1;
                        candidate_option.multi_segment_count := suffix_option.multi_segment_count;
                        if segment_len_local >= 2 then
                        begin
                            Inc(candidate_option.multi_segment_count);
                        end;

                        candidate_weight_local := get_candidate_effective_weight_local(candidate_local);
                        transition_bonus_local := get_path_transition_bonus(prev_prev_text, prev_text,
                            candidate_text_local);
                        pair_bonus_local := get_exact_context_pair_bonus(prev_text, candidate_text_local);
                        query_path_bonus_local := get_local_query_path_transition_bonus(
                            prev_pinyin_text, segment_text_local, prev_text,
                            candidate_text_local);

                        candidate_option.score := suffix_option.score + candidate_weight_local +
                            transition_bonus_local + pair_bonus_local + query_path_bonus_local;
                        if segment_len_local = 1 then
                        begin
                            Dec(candidate_option.score, c_search_single_penalty);
                        end
                        else
                        begin
                            Inc(candidate_option.score, c_search_multi_bonus + (segment_len_local * 24));
                        end;
                        if next_pos_local = Length(syllables) then
                        begin
                            Inc(candidate_option.score, c_search_completion_bonus);
                            if (m_dictionary <> nil) and
                                (candidate_option.path_text <> '') then
                            begin
                                Inc(candidate_option.score,
                                    get_cached_query_segment_path_bonus(
                                    m_last_lookup_key, candidate_option.path_text));
                            end;
                        end;

                        if is_better_long_sentence_option(candidate_option, best_option) then
                        begin
                            best_option := candidate_option;
                        end;
                    end;
                end;

                result_cache.AddOrSetValue(cache_key, best_option);
                Result := best_option;
            end;
        var
            best_option: TLongSentenceOption;
        begin
            result_cache := TDictionary<string, TLongSentenceOption>.Create;
            try
                get_recent_path_context_seed(seed_prev_prev_text, seed_prev_text);
                best_option := solve_long_sentence(0, seed_prev_prev_text, seed_prev_text, '');
                m_last_full_path_debug_info := Format(
                    ' longsent_best=[found=%d score=%d seg=%d multi=%d path=%s text=%s]',
                    [Ord(best_option.found), best_option.score, best_option.segment_count,
                    best_option.multi_segment_count, best_option.path_text, best_option.text]);
                if (not best_option.found) or
                    (best_option.multi_segment_count < 3) or
                    ((best_option.multi_segment_count * 2) < best_option.segment_count) or
                    (Trim(best_option.text) = '') then
                begin
                    Exit;
                end;

                append_candidate(best_option.text, best_option.score + 1200,
                    best_option.source, '', False, 0, best_option.fuzzy_cost,
                    best_option.fuzzy_rules);
                remember_segment_path_for_candidate(best_option.text, '', best_option.path_text,
                    best_option.score + 1200);
            finally
                result_cache.Free;
            end;
        end;

        function detect_preferred_phrase_at_position(const start_pos: Integer): Boolean;
        var
            probe_segment_len: Integer;
            probe_segment_text: string;
            probe_lookup_results: TncCandidateList;
            probe_idx: Integer;
            probe_text: string;
            local_rank: Integer;
        begin
            Result := False;
            if (Length(syllables) <= 1) or (start_pos < 0) or (start_pos >= Length(syllables)) then
            begin
                Exit;
            end;

            for probe_segment_len := 2 to max_word_len do
            begin
                if start_pos + probe_segment_len > Length(syllables) then
                begin
                    Break;
                end;

                probe_segment_text := build_syllable_text(start_pos, probe_segment_len);
                if probe_segment_text = '' then
                begin
                    Continue;
                end;

                if not dictionary_lookup_cached(probe_segment_text, probe_lookup_results) then
                begin
                    Continue;
                end;

                if (per_limit > 0) and (Length(probe_lookup_results) > per_limit) then
                begin
                    SetLength(probe_lookup_results, per_limit);
                end;

                for probe_idx := 0 to High(probe_lookup_results) do
                begin
                    probe_text := Trim(probe_lookup_results[probe_idx].text);
                    if (probe_text = '') or (not contains_non_ascii(probe_text)) then
                    begin
                        Continue;
                    end;

                    if get_text_unit_count(probe_text) > 1 then
                    begin
                        if probe_lookup_results[probe_idx].has_dict_weight then
                        begin
                            local_rank := probe_lookup_results[probe_idx].dict_weight;
                        end
                        else
                        begin
                            local_rank := probe_lookup_results[probe_idx].score;
                        end;
                        if local_rank > preferred_phrase_strength[start_pos] then
                        begin
                            preferred_phrase_strength[start_pos] := local_rank;
                        end;
                        if (probe_segment_len = 2) and
                            (local_rank > preferred_two_syllable_phrase_strength[start_pos]) then
                        begin
                            preferred_two_syllable_phrase_strength[start_pos] := local_rank;
                        end;
                        if probe_segment_len > preferred_phrase_max_len[start_pos] then
                        begin
                            preferred_phrase_max_len[start_pos] := probe_segment_len;
                        end;
                        Result := True;
                        Exit;
                    end;
                end;
            end;
        end;

        function detect_preferred_single_strength_at_position(const start_pos: Integer): Integer;
        var
            probe_lookup_results: TncCandidateList;
            probe_idx: Integer;
            probe_text: string;
            probe_rank: Integer;
            is_preferred: Boolean;
        begin
            Result := 0;
            if (start_pos < 0) or (start_pos >= Length(syllables)) then
            begin
                Exit;
            end;

            if not dictionary_lookup_cached(syllables[start_pos].text, probe_lookup_results) then
            begin
                Exit;
            end;

            if (per_limit > 0) and (Length(probe_lookup_results) > per_limit) then
            begin
                SetLength(probe_lookup_results, per_limit);
            end;

            for probe_idx := 0 to High(probe_lookup_results) do
            begin
                probe_text := Trim(probe_lookup_results[probe_idx].text);
                if (probe_text = '') or
                    (not is_single_text_unit(probe_text)) or
                    (not contains_non_ascii(probe_text)) then
                begin
                    Continue;
                end;
                if not match_single_char_candidate_for_syllable(
                    syllables[start_pos].text, probe_text, is_preferred) or
                    (not is_preferred) then
                begin
                    Continue;
                end;

                if probe_lookup_results[probe_idx].has_dict_weight then
                begin
                    probe_rank := probe_lookup_results[probe_idx].dict_weight;
                end
                else
                begin
                    probe_rank := probe_lookup_results[probe_idx].score;
                end;
                if probe_rank > Result then
                begin
                    Result := probe_rank;
                end;
            end;
        end;

        function get_segment_boundary_cohesion_bonus(const current_state: TncSegmentPathState;
            const candidate: TncCandidate; const segment_text_units: Integer;
            const segment_len: Integer; const state_pos: Integer; const next_pos: Integer): Integer;
        const
            c_boundary_strength_bonus_cap = 132;
            c_boundary_follow_scale = 7;
            c_boundary_prefix_scale = 6;
            c_boundary_weak_single_penalty = 24;
            c_boundary_gap_penalty = 42;
            c_three_syllable_tail_phrase_bonus = 132;
            c_three_syllable_tail_phrase_penalty = 284;
            c_three_syllable_tail_phrase_soft_penalty = 112;
            c_three_syllable_weak_tail_phrase_penalty = 420;
            c_suffix_tail_phrase_bonus_cap = 420;
        var
            remaining_syllables: Integer;
            current_phrase_strength: Integer;
            next_phrase_strength: Integer;
            next_single_chain_strength: Integer;
            overlapping_phrase_strength: Integer;
            previous_text_units: Integer;
            suffix_tail_matched_text: string;
            suffix_tail_support_strength: Integer;
            suffix_tail_bias: Integer;
        begin
            Result := 0;
            if not c_suppress_nonlexicon_complete_long_candidates then
            begin
                Exit;
            end;

            remaining_syllables := Length(syllables) - next_pos;
            previous_text_units := get_text_unit_count(Trim(current_state.text));

            current_phrase_strength := 0;
            if (segment_text_units = segment_len) and (segment_len >= 2) and
                contains_non_ascii(Trim(candidate.text)) then
            begin
                if candidate.has_dict_weight then
                begin
                    current_phrase_strength := candidate.dict_weight;
                end
                else
                begin
                    current_phrase_strength := candidate.score;
                end;
            end;

            next_phrase_strength := 0;
            if (next_pos >= 0) and (next_pos < Length(preferred_phrase_strength)) then
            begin
                next_phrase_strength := preferred_phrase_strength[next_pos];
            end;

            overlapping_phrase_strength := 0;
            if (state_pos = 0) and (Length(syllables) = 3) and
                (Length(preferred_phrase_strength) > 1) then
            begin
                overlapping_phrase_strength := preferred_phrase_strength[1];
            end;

            next_single_chain_strength := 0;
            if (state_pos = 0) and (segment_len = 1) and (remaining_syllables = 2) and
                (next_pos >= 0) and (next_pos + 1 < Length(preferred_single_strength)) then
            begin
                next_single_chain_strength := preferred_single_strength[next_pos] +
                    preferred_single_strength[next_pos + 1];
            end;

            suffix_tail_matched_text := '';
            suffix_tail_support_strength := 0;
            suffix_tail_bias := 0;
            if (segment_len >= 2) and (remaining_syllables = 1) and
                (next_pos >= 0) and (next_pos < Length(syllables)) then
            begin
                suffix_tail_bias := get_suffix_tail_bias_adjustment_for_syllable_local(
                    next_pos, '', 0, suffix_tail_matched_text,
                    suffix_tail_support_strength);
            end;

            if (state_pos = 0) and (remaining_syllables > 0) then
            begin
                if segment_len = 1 then
                begin
                    if next_phrase_strength > 0 then
                    begin
                        Inc(Result, Min(c_boundary_strength_bonus_cap,
                            next_phrase_strength div c_boundary_follow_scale));
                    end
                    else if remaining_syllables >= 2 then
                    begin
                        Dec(Result, c_boundary_weak_single_penalty);
                    end;
                    if (Length(syllables) = 3) and (remaining_syllables = 2) and
                        (next_phrase_strength > 0) then
                    begin
                        if (next_single_chain_strength > 0) and
                            ((next_phrase_strength * 100) < (next_single_chain_strength * 85)) then
                        begin
                            Dec(Result, c_three_syllable_weak_tail_phrase_penalty +
                                Min(180, (next_single_chain_strength - next_phrase_strength) div 2));
                        end
                        else
                        begin
                            Inc(Result, c_three_syllable_tail_phrase_bonus +
                                Min(164, next_phrase_strength div 4));
                        end;
                    end;
                end
                else if current_phrase_strength > 0 then
                begin
                    if remaining_syllables = 1 then
                    begin
                        Inc(Result, Min(c_boundary_strength_bonus_cap,
                            current_phrase_strength div c_boundary_prefix_scale));
                        if (Length(syllables) = 3) and (overlapping_phrase_strength > 0) then
                        begin
                            if (current_phrase_strength * 100) <=
                                (overlapping_phrase_strength * 230) then
                            begin
                                Dec(Result, c_three_syllable_tail_phrase_penalty +
                                    Min(156, overlapping_phrase_strength div 6));
                            end
                            else if (current_phrase_strength * 100) <=
                                (overlapping_phrase_strength * 280) then
                            begin
                                Dec(Result, c_three_syllable_tail_phrase_soft_penalty +
                                    Min(92, overlapping_phrase_strength div 10));
                            end;
                        end;
                        if (suffix_tail_bias > 0) and (suffix_tail_support_strength > 0) and
                            (suffix_tail_matched_text <> '') then
                        begin
                            Inc(Result, Min(c_suffix_tail_phrase_bonus_cap,
                                (suffix_tail_bias * 2) div 3));
                        end;
                    end
                    else if next_phrase_strength > current_phrase_strength + 120 then
                    begin
                        Dec(Result, c_boundary_gap_penalty);
                    end;
                end;
            end
            else if (remaining_syllables = 0) and (previous_text_units = 1) and
                (current_phrase_strength > 0) then
            begin
                Inc(Result, Min(64, current_phrase_strength div 10));
            end;
        end;

        function try_get_best_anchor_state(const position: Integer;
            const minimum_text_units: Integer; out out_state: TncSegmentPathState): Boolean;
        var
            local_sorted_states: TArray<TncSegmentPathState>;
            local_idx: Integer;
            local_units: Integer;
        begin
            Result := False;
            out_state.text := '';
            out_state.score := 0;
            out_state.path_preference_score := 0;
            out_state.path_confidence_score := 0;
            out_state.source := cs_rule;
            out_state.has_multi_segment := False;
            out_state.uses_prefix_completion := False;
            out_state.prev_prev_text := '';
            out_state.prev_text := '';
            out_state.prev_pinyin_text := '';
            out_state.path_text := '';
            if (position < 0) or (position > High(states)) or (states[position].Count <= 0) then
            begin
                Exit;
            end;

            SetLength(local_sorted_states, states[position].Count);
            for local_idx := 0 to states[position].Count - 1 do
            begin
                local_sorted_states[local_idx] := states[position][local_idx];
            end;
            sort_state_array(local_sorted_states);

            for local_idx := 0 to High(local_sorted_states) do
            begin
                if Trim(local_sorted_states[local_idx].text) = '' then
                begin
                    Continue;
                end;
                local_units := get_candidate_text_unit_count(local_sorted_states[local_idx].text);
                if local_units < minimum_text_units then
                begin
                    Continue;
                end;
                if (minimum_text_units = 1) and
                    (not is_single_text_unit(Trim(local_sorted_states[local_idx].text))) then
                begin
                    Continue;
                end;
                out_state := local_sorted_states[local_idx];
                Result := True;
                Break;
            end;
        end;

        procedure append_three_syllable_anchor_partial_candidates;
        const
            c_three_syllable_boundary_ratio_pct = 100;
            c_anchor_partial_bonus = 420;
            c_anchor_partial_tail_bonus_scale = 4;
            c_anchor_partial_remaining_penalty = 36;
            c_anchor_partial_non_preferred_penalty = 220;
            c_anchor_partial_non_preferred_gap = 48;
            c_anchor_partial_score_hint_flag = 1000000;
            c_anchor_partial_hint_kind_scale = 10000;
            c_anchor_partial_hint_one_plus_two = 1;
            c_anchor_partial_hint_two_plus_one = 2;
            c_segmented_prefix_strength_penalty = 520;
            c_first_single_tail_bias_cap = 520;
            c_last_single_head_bias_cap = 620;
        var
            head_two_strength: Integer;
            head_two_effective_strength: Integer;
            head_two_supported_strength: Integer;
            tail_two_strength: Integer;
            tail_two_supported_strength: Integer;
            first_single_strength: Integer;
            last_single_text: string;
            last_single_strength: Integer;
            detected_last_single_strength: Integer;
            suffix_tail_matched_text: string;
            suffix_tail_support_strength: Integer;
            suffix_tail_bias: Integer;
            boundary_ratio_pct: Integer;
            head_retention_bonus: Integer;
            remaining_pinyin: string;
            anchor_state: TncSegmentPathState;
            prefix_two_state: TncSegmentPathState;
            anchor_score: Integer;
            local_units: Integer;
            is_preferred: Boolean;
            anchor_hint: Integer;
            derived_head_strength: Integer;
            preferred_one_plus_two_score: Integer;

            procedure append_non_preferred_head_two_anchor_candidate(
                const preferred_score_value: Integer;
                const tail_support_strength_value: Integer);
            var
                fallback_state: TncSegmentPathState;
                fallback_score: Integer;
                fallback_hint: Integer;
                fallback_remaining_pinyin: string;
                fallback_units: Integer;
                fallback_gap_penalty: Integer;
            begin
                fallback_remaining_pinyin := build_syllable_text(2, Length(syllables) - 2);
                if fallback_remaining_pinyin = '' then
                begin
                    Exit;
                end;

                if not try_get_best_anchor_state(2, 2, fallback_state) then
                begin
                    Exit;
                end;

                fallback_units := get_candidate_text_unit_count(fallback_state.text);
                if fallback_units < 2 then
                begin
                    Exit;
                end;

                fallback_gap_penalty := 0;
                if tail_support_strength_value > head_two_supported_strength then
                begin
                    fallback_gap_penalty := Min(220,
                        (tail_support_strength_value - head_two_supported_strength) div 3);
                end;

                fallback_score := fallback_state.score + c_anchor_partial_bonus +
                    Min(220, head_two_supported_strength div
                    c_anchor_partial_tail_bonus_scale) -
                    c_anchor_partial_remaining_penalty -
                    c_anchor_partial_non_preferred_penalty -
                    fallback_gap_penalty;
                if (preferred_score_value > 0) and
                    (fallback_score >= preferred_score_value) then
                begin
                    fallback_score := preferred_score_value -
                        c_anchor_partial_non_preferred_gap;
                end;

                if fallback_score <= 0 then
                begin
                    Exit;
                end;

                append_candidate(fallback_state.text, fallback_score,
                    fallback_state.source, fallback_remaining_pinyin, False, 0,
                    fallback_state.fuzzy_cost, fallback_state.fuzzy_rules);
                fallback_hint := c_anchor_partial_score_hint_flag +
                    (c_anchor_partial_hint_two_plus_one *
                    c_anchor_partial_hint_kind_scale) + fallback_score;
                remember_segment_path_for_candidate(fallback_state.text,
                    fallback_remaining_pinyin, fallback_state.path_text, fallback_hint);
            end;
        begin
            if (not c_suppress_nonlexicon_complete_long_candidates) or
                (Length(syllables) < 3) then
            begin
                Exit;
            end;

            head_two_strength := 0;
            if Length(preferred_two_syllable_phrase_strength) > 0 then
            begin
                head_two_strength := preferred_two_syllable_phrase_strength[0];
            end;
            tail_two_strength := 0;
            if Length(preferred_two_syllable_phrase_strength) > 1 then
            begin
                tail_two_strength := preferred_two_syllable_phrase_strength[1];
            end;
            first_single_strength := 0;
            if Length(preferred_single_strength) > 0 then
            begin
                first_single_strength := preferred_single_strength[0];
            end;
            last_single_text := '';
            last_single_strength := 0;
            detected_last_single_strength := 0;
            if Length(preferred_single_strength) > 2 then
            begin
                last_single_strength := preferred_single_strength[2];
            end;
            try_get_best_single_char_candidate_for_syllable_local(2, last_single_text,
                detected_last_single_strength);
            if last_single_strength <= 0 then
            begin
                last_single_strength := detected_last_single_strength;
            end;
            suffix_tail_matched_text := '';
            suffix_tail_support_strength := 0;
            suffix_tail_bias := get_suffix_tail_bias_adjustment_for_syllable_local(
                2, last_single_text, last_single_strength, suffix_tail_matched_text,
                suffix_tail_support_strength);
            boundary_ratio_pct := c_three_syllable_boundary_ratio_pct;
            head_retention_bonus := 0;
            if (suffix_tail_bias > 0) and (suffix_tail_support_strength > 0) and
                (suffix_tail_matched_text <> '') and (head_two_strength > 0) then
            begin
                boundary_ratio_pct :=
                    get_suffix_tail_head_preference_ratio_pct_local(syllables[2].text);
                head_retention_bonus :=
                    get_suffix_tail_head_retention_bonus_local(syllables[2].text);
            end;

            head_two_effective_strength := head_two_strength;
            if try_get_best_anchor_state(2, 2, prefix_two_state) then
            begin
                derived_head_strength := prefix_two_state.score;
                if prefix_two_state.has_multi_segment then
                begin
                    Dec(derived_head_strength, c_segmented_prefix_strength_penalty);
                end;
                if derived_head_strength > head_two_effective_strength then
                begin
                    head_two_effective_strength := derived_head_strength;
                end;
            end;

            head_two_supported_strength := head_two_effective_strength;
            if (head_two_effective_strength > 0) and (last_single_strength > 0) then
            begin
                Inc(head_two_supported_strength, Min(c_last_single_head_bias_cap,
                    last_single_strength + Min(160, last_single_strength div 2)));
            end;
            if (head_two_effective_strength > 0) and (suffix_tail_bias > 0) and
                (suffix_tail_support_strength > 0) and (suffix_tail_matched_text <> '') then
            begin
                Inc(head_two_supported_strength, suffix_tail_bias);
                if head_retention_bonus > 0 then
                begin
                    Inc(head_two_supported_strength, head_retention_bonus);
                end;
            end;

            tail_two_supported_strength := tail_two_strength;
            if (tail_two_strength > 0) and (first_single_strength > 0) then
            begin
                Inc(tail_two_supported_strength, Min(c_first_single_tail_bias_cap, first_single_strength));
            end;

            if (head_two_supported_strength <= 0) and (tail_two_supported_strength <= 0) then
            begin
                Exit;
            end;

            preferred_one_plus_two_score := 0;
            if (tail_two_supported_strength > 0) and
                ((head_two_supported_strength <= 0) or
                ((tail_two_supported_strength * 100) >=
                (head_two_supported_strength * boundary_ratio_pct))) then
            begin
                remaining_pinyin := build_syllable_text(1, Length(syllables) - 1);
                if (remaining_pinyin <> '') and
                    try_get_best_anchor_state(1, 1, anchor_state) then
                begin
                    if match_single_char_candidate_for_syllable(
                        syllables[0].text, anchor_state.text, is_preferred) and is_preferred then
                    begin
                        anchor_score := anchor_state.score + c_anchor_partial_bonus +
                            Min(220, tail_two_supported_strength div c_anchor_partial_tail_bonus_scale) -
                            (2 * c_anchor_partial_remaining_penalty);
                        preferred_one_plus_two_score := anchor_score;
                        append_candidate(anchor_state.text, anchor_score,
                            anchor_state.source, remaining_pinyin, False, 0,
                            anchor_state.fuzzy_cost, anchor_state.fuzzy_rules);
                        anchor_hint := c_anchor_partial_score_hint_flag +
                            (c_anchor_partial_hint_one_plus_two * c_anchor_partial_hint_kind_scale) +
                            anchor_score;
                        remember_segment_path_for_candidate(anchor_state.text, remaining_pinyin,
                            anchor_state.path_text, anchor_hint);
                    end;
                end;

                append_non_preferred_head_two_anchor_candidate(
                    preferred_one_plus_two_score, tail_two_supported_strength);
            end;

            if (head_two_supported_strength > 0) and
                ((tail_two_supported_strength <= 0) or
                ((tail_two_supported_strength * 100) <
                (head_two_supported_strength * boundary_ratio_pct))) then
            begin
                remaining_pinyin := build_syllable_text(2, Length(syllables) - 2);
                if (remaining_pinyin <> '') and
                    try_get_best_anchor_state(2, 2, anchor_state) then
                begin
                    local_units := get_candidate_text_unit_count(anchor_state.text);
                    if local_units >= 2 then
                    begin
                        anchor_score := anchor_state.score + c_anchor_partial_bonus +
                            Min(220, head_two_supported_strength div c_anchor_partial_tail_bonus_scale) -
                            c_anchor_partial_remaining_penalty;
                        append_candidate(anchor_state.text, anchor_score, anchor_state.source,
                            remaining_pinyin, False, 0, anchor_state.fuzzy_cost,
                            anchor_state.fuzzy_rules);
                        anchor_hint := c_anchor_partial_score_hint_flag +
                            (c_anchor_partial_hint_two_plus_one * c_anchor_partial_hint_kind_scale) +
                            anchor_score;
                        remember_segment_path_for_candidate(anchor_state.text, remaining_pinyin,
                            anchor_state.path_text, anchor_hint);
                    end;
                end;
            end;
        end;

        function get_segment_alignment_adjustment(const start_pos: Integer; const syllable_count: Integer;
            const candidate_text_value: string): Integer;
        var
            unit_idx: Integer;
            local_adjustment: Integer;
            local_applied_count: Integer;
            unit_text: string;
        begin
            Result := 0;
            if (start_pos < 0) or (syllable_count <= 0) then
            begin
                Exit;
            end;

            segment_units := split_text_units(candidate_text_value);
            if Length(segment_units) <> syllable_count then
            begin
                Exit;
            end;

            local_adjustment := 0;
            local_applied_count := 0;
            for unit_idx := 0 to syllable_count - 1 do
            begin
                if (start_pos + unit_idx < 0) or (start_pos + unit_idx >= Length(single_char_rank_maps)) then
                begin
                    Continue;
                end;

                local_rank_map := single_char_rank_maps[start_pos + unit_idx];
                if (local_rank_map = nil) or (local_rank_map.Count = 0) then
                begin
                    Continue;
                end;

                Inc(local_applied_count);
                unit_text := segment_units[unit_idx];
                if local_rank_map.TryGetValue(unit_text, local_rank) then
                begin
                    Inc(local_adjustment, (c_segment_alignment_rank_window - local_rank) *
                        c_segment_alignment_rank_step);
                end
                else
                begin
                    Dec(local_adjustment, c_segment_alignment_missing_penalty);
                end;
            end;

            if local_applied_count <= 0 then
            begin
                Exit;
            end;

            Result := local_adjustment div local_applied_count;
            if Result > c_segment_alignment_adjust_cap then
            begin
                Result := c_segment_alignment_adjust_cap;
            end
            else if Result < -c_segment_alignment_adjust_cap then
            begin
                Result := -c_segment_alignment_adjust_cap;
            end;
        end;
        begin
            if Length(syllables) <= 1 then
            begin
                Exit;
            end;

        long_sentence_full_path_mode := Length(syllables) >= c_long_sentence_full_path_min_syllables;
        full_path_budget_ms_limit := c_segment_full_path_budget_ms;
        full_path_work_limit_limit := c_segment_full_path_work_limit;
        full_state_limit_limit := c_segment_full_state_limit;
        if long_sentence_full_path_mode then
        begin
            full_head_top_n := 4;
            full_path_non_user_limit := c_segment_full_path_non_user_limit_long;
            full_path_budget_ms_limit := c_segment_full_path_budget_ms_long;
            full_path_work_limit_limit := c_segment_full_path_work_limit_long;
            full_state_limit_limit := c_segment_full_state_limit_long;
        end
        else if Length(syllables) >= 6 then
        begin
            full_head_top_n := 2;
            full_path_non_user_limit := 3;
        end
        else if Length(syllables) >= 4 then
        begin
            full_head_top_n := 3;
            full_path_non_user_limit := 4;
        end
        else
        begin
            full_head_top_n := 4;
            full_path_non_user_limit := c_segment_full_path_non_user_limit;
        end;

        SetLength(states, Length(syllables) + 1);
        SetLength(state_dedup, Length(syllables) + 1);
        transition_bonus_cache := TDictionary<string, Integer>.Create;
        for state_pos := 0 to High(states) do
        begin
            states[state_pos] := TList<TncSegmentPathState>.Create;
            state_dedup[state_pos] := TDictionary<string, Integer>.Create;
        end;

        try
            local_state.text := '';
            local_state.score := 0;
            local_state.path_preference_score := 0;
            local_state.path_confidence_score := 0;
            local_state.source := cs_rule;
            local_state.has_multi_segment := False;
            local_state.uses_prefix_completion := False;
            get_recent_path_context_seed(local_state.prev_prev_text, local_state.prev_text);
            local_state.prev_pinyin_text := '';
            local_state.path_text := '';
            local_state.fuzzy_cost := 0;
            local_state.fuzzy_rules := [];
            states[0].Add(local_state);
            state_dedup[0].Add(build_state_key(local_state), 0);
            SetLength(preferred_phrase_flags, Length(syllables));
            SetLength(preferred_phrase_max_len, Length(syllables));
            SetLength(preferred_phrase_strength, Length(syllables));
            SetLength(preferred_two_syllable_phrase_strength, Length(syllables));
            SetLength(preferred_single_strength, Length(syllables));
            SetLength(single_char_rank_maps, Length(syllables));
            full_path_budget_start_tick := nc_monotonic_tick_ms;
            full_path_budget_work_counter := 0;
            full_path_total_work_counter := 0;
            full_path_budget_exhausted := False;
            final_complete_state_count := 0;
            final_complete_multiseg_state_count := 0;
            final_complete_debug_paths := '';
            final_position_debug_counts := '';
            if Length(syllables) >= c_long_sentence_head_only_bypass_min_syllables then
            begin
                m_last_full_path_debug_info := ' longsent_skip_legacy=1';
                Exit;
            end;
            for state_pos := 0 to High(syllables) do
            begin
                if full_path_budget_reached(True) then
                begin
                    Break;
                end;

                single_char_rank_maps[state_pos] := TDictionary<string, Integer>.Create;
                if dictionary_lookup_cached(syllables[state_pos].text, local_lookup_results) then
                begin
                    local_rank := 0;
                    for candidate_index := 0 to High(local_lookup_results) do
                    begin
                        if full_path_budget_reached(False) then
                        begin
                            Break;
                        end;

                        local_candidate_text := Trim(local_lookup_results[candidate_index].text);
                        if (local_candidate_text = '') or
                            (not is_single_text_unit(local_candidate_text)) or
                            (not contains_non_ascii(local_candidate_text)) then
                        begin
                            Continue;
                        end;

                        if not single_char_rank_maps[state_pos].ContainsKey(local_candidate_text) then
                        begin
                            single_char_rank_maps[state_pos].Add(local_candidate_text, local_rank);
                            Inc(local_rank);
                            if local_rank >= c_segment_alignment_rank_window then
                            begin
                                Break;
                            end;
                        end;
                    end;
                end;
                preferred_phrase_flags[state_pos] := detect_preferred_phrase_at_position(state_pos);
                preferred_single_strength[state_pos] := detect_preferred_single_strength_at_position(state_pos);
            end;

            for state_pos := 0 to High(syllables) do
            begin
                if full_path_budget_reached(True) then
                begin
                    Break;
                end;

                if states[state_pos].Count = 0 then
                begin
                    Continue;
                end;

                for local_segment_len := 1 to max_word_len do
                begin
                    if full_path_budget_exhausted then
                    begin
                        Break;
                    end;

                    if state_pos + local_segment_len > Length(syllables) then
                    begin
                        Break;
                    end;

                    local_segment_text := build_syllable_text(state_pos, local_segment_len);
                    if local_segment_text = '' then
                    begin
                        Continue;
                    end;

                    if not dictionary_lookup_cached(local_segment_text, local_lookup_results) then
                    begin
                        Continue;
                    end;

                    if (per_limit > 0) and (Length(local_lookup_results) > per_limit) then
                    begin
                        SetLength(local_lookup_results, per_limit);
                    end;

                    next_pos := state_pos + local_segment_len;
                    for state_index := 0 to states[state_pos].Count - 1 do
                    begin
                        if full_path_budget_reached(False) then
                        begin
                            Break;
                        end;

                        local_state := states[state_pos][state_index];
                        for candidate_index := 0 to High(local_lookup_results) do
                        begin
                            if full_path_budget_reached(False) then
                            begin
                                Break;
                            end;

                            // Keep full-path expansion anchored by high-confidence head chunks.
                            // Low-ranked leading chunks (for example rare/unnatural head words)
                            // should not keep expanding into long noisy phrases.
                            if (state_pos = 0) and (local_segment_len > 1) and
                                (next_pos < Length(syllables)) and
                                (candidate_index >= full_head_top_n) then
                            begin
                                Continue;
                            end;

                            local_candidate := local_lookup_results[candidate_index];
                            local_candidate_text := Trim(local_candidate.text);
                            candidate_has_non_ascii := contains_non_ascii(local_candidate_text);
                            candidate_text_units := get_text_unit_count(local_candidate_text);
                            if candidate_text_units <= 0 then
                            begin
                                Continue;
                            end;
                            if not candidate_has_non_ascii then
                            begin
                                Continue;
                            end;
                            // For one-syllable segment expansion, multi-char words are typically noisy bridges
                            // (e.g. "xian" -> "閻熸鐏濋悾?) and hurt full-path quality.
                            if candidate_has_non_ascii and (local_segment_len = 1) and
                                (candidate_text_units > 1) then
                            begin
                                Continue;
                            end;
                            allow_leading_single_char := False;
                            allow_single_char_path := False;
                            local_path_transition_bonus := 0;
                            if not is_multi_char_word(local_candidate.text) then
                            begin
                                // Allow only the top leading single-char candidate so cases like
                                // "wo + faxian" can become "闁瑰瓨鍨佃ぐ鍌炴偝?, while still blocking noisy
                                // single-char full-path combinations in general.
                                if candidate_has_non_ascii and (local_segment_len = 1) and
                                    ((candidate_index < c_segment_full_single_top_n) or
                                    ((local_state.prev_text <> '') and
                                    (get_path_transition_bonus(
                                        local_state.prev_prev_text,
                                        local_state.prev_text,
                                        local_candidate_text) > 0))) then
                                begin
                                    if (candidate_index >= c_segment_full_single_top_n) and
                                        (local_state.prev_text <> '') then
                                    begin
                                        local_path_transition_bonus := get_path_transition_bonus(
                                            local_state.prev_prev_text,
                                            local_state.prev_text,
                                            local_candidate_text);
                                    end;
                                    allow_single_char_path := True;
                                    allow_leading_single_char := state_pos = 0;
                                    if (not allow_leading_single_char) and (Length(local_candidate.text) = 2) and
                                        (Ord(local_candidate.text[1]) >= $D800) and (Ord(local_candidate.text[1]) <= $DBFF) and
                                        (Ord(local_candidate.text[2]) >= $DC00) and (Ord(local_candidate.text[2]) <= $DFFF) then
                                    begin
                                        Continue;
                                    end;
                                end
                                else
                                begin
                                    Continue;
                                end;
                            end;

                            local_new_state.text := local_state.text + local_candidate.text;
                            local_new_state.score := local_state.score + local_candidate.score +
                                (local_segment_len * c_segment_prefix_bonus);
                            if local_path_transition_bonus = 0 then
                            begin
                                local_path_transition_bonus := get_path_transition_bonus(
                                    local_state.prev_prev_text,
                                    local_state.prev_text,
                                    local_candidate_text);
                            end;
                            Inc(local_new_state.score, local_path_transition_bonus);
                            local_query_path_bonus := get_local_query_path_transition_bonus(
                                local_state.prev_pinyin_text, local_segment_text,
                                local_state.prev_text, local_candidate_text);
                            if local_query_path_bonus <> 0 then
                            begin
                                Inc(local_new_state.score, local_query_path_bonus);
                            end;
                            local_segment_quality_bonus := get_segment_lexical_quality_bonus(
                                local_candidate, local_segment_len, state_pos, next_pos);
                            Inc(local_new_state.score, local_segment_quality_bonus);
                            local_chain_shape_bonus := get_segment_chain_shape_bonus(
                                local_state, candidate_text_units, local_segment_len, state_pos, next_pos);
                            if local_chain_shape_bonus <> 0 then
                            begin
                                Inc(local_new_state.score, local_chain_shape_bonus);
                            end;
                            Inc(local_new_state.score, get_segment_boundary_cohesion_bonus(
                                local_state, local_candidate, candidate_text_units,
                                local_segment_len, state_pos, next_pos));
                            if (state_pos = 0) and (local_segment_len > 1) and
                                (next_pos < Length(syllables)) then
                            begin
                                Dec(local_new_state.score, c_segment_full_leading_multi_penalty);
                            end;
                            if allow_single_char_path then
                            begin
                                if allow_leading_single_char and (next_pos < Length(syllables)) and
                                    (state_pos >= 0) and (state_pos < Length(preferred_phrase_max_len)) and
                                    (preferred_phrase_max_len[state_pos] >= 3) then
                                begin
                                    Continue;
                                end;
                                if (next_pos < Length(syllables)) and (state_pos >= 0) and
                                    (state_pos < Length(preferred_phrase_flags)) and
                                    preferred_phrase_flags[state_pos] then
                                begin
                                    if not allow_leading_single_char then
                                    begin
                                        Continue;
                                    end;
                                    Dec(local_new_state.score, c_segment_full_preferred_single_penalty);
                                end;
                                if allow_leading_single_char then
                                begin
                                    if Length(syllables) >= 3 then
                                    begin
                                        Inc(local_new_state.score, c_segment_full_leading_single_bonus);
                                    end
                                    else
                                    begin
                                        Dec(local_new_state.score, c_segment_full_preferred_single_penalty);
                                    end;
                                end
                                else
                                begin
                                    Dec(local_new_state.score, c_segment_full_non_leading_single_penalty);
                                end;
                            end;
                            if (next_pos = Length(syllables)) then
                            begin
                                Inc(local_new_state.score, c_segment_full_completion_bonus);
                            end
                            else
                            begin
                                Dec(local_new_state.score, c_segment_full_transition_penalty);
                            end;

                            if candidate_has_non_ascii and (candidate_text_units <> local_segment_len) then
                            begin
                                text_unit_mismatch := Abs(candidate_text_units - local_segment_len);
                                Dec(local_new_state.score, text_unit_mismatch * c_segment_text_unit_mismatch_penalty);
                                if candidate_text_units > local_segment_len then
                                begin
                                    Dec(local_new_state.score,
                                        (candidate_text_units - local_segment_len) * c_segment_text_unit_overflow_penalty);
                                end;
                            end;
                            if (Length(syllables) >= 4) and (state_pos = 1) and
                                (next_pos = Length(syllables)) and
                                (local_segment_len >= Length(syllables) - 1) and
                                (get_text_unit_count(local_state.text) = 1) then
                            begin
                                Dec(local_new_state.score, 140);
                            end;
                            if candidate_has_non_ascii and (candidate_text_units = local_segment_len) and
                                (local_segment_len >= 2) then
                            begin
                                Inc(local_new_state.score,
                                    get_segment_alignment_adjustment(state_pos, local_segment_len, local_candidate_text));
                            end;

                            if is_first_overall_segment and (state_pos = 0) and (local_segment_len = 1) and
                                (next_pos < Length(syllables)) and (Length(local_candidate.text) = 1) and
                                is_common_surname_text(local_candidate.text) then
                            begin
                                Inc(local_new_state.score, c_segment_surname_bonus);
                            end;

                            local_new_state.source := merge_source_rank(local_state.source, local_candidate.source);
                            local_new_state.fuzzy_cost := local_state.fuzzy_cost +
                                local_candidate.fuzzy_cost;
                            local_new_state.fuzzy_rules := local_state.fuzzy_rules +
                                local_candidate.fuzzy_rules;
                            local_new_state.prev_prev_text := local_state.prev_text;
                            local_new_state.prev_text := local_candidate_text;
                            local_new_state.prev_pinyin_text := local_segment_text;
                            local_new_state.path_text := local_state.path_text;
                            if local_candidate_text <> '' then
                            begin
                                if local_new_state.path_text <> '' then
                                begin
                                    local_new_state.path_text := local_new_state.path_text + c_segment_path_separator;
                                end;
                                local_new_state.path_text := local_new_state.path_text + local_candidate_text;
                            end;
                            local_new_state.has_multi_segment :=
                                get_encoded_path_segment_count_local(local_new_state.path_text) > 1;
                            local_new_state.uses_prefix_completion :=
                                local_state.uses_prefix_completion;
                            local_new_state.path_preference_score := 0;
                            local_new_state.path_confidence_score := 0;
                            if get_encoded_path_segment_count_local(local_new_state.path_text) > 1 then
                            begin
                                local_segment_count := get_encoded_path_segment_count_local(local_new_state.path_text);
                                local_path_preference_score := -(local_segment_count * 18);
                                if local_segment_quality_bonus <> 0 then
                                begin
                                    Inc(local_path_preference_score, local_segment_quality_bonus div 2);
                                end;
                                if local_chain_shape_bonus <> 0 then
                                begin
                                    Inc(local_path_preference_score, local_chain_shape_bonus div 2);
                                end;
                                if local_path_transition_bonus > 0 then
                                begin
                                    Inc(local_path_preference_score, Min(220, local_path_transition_bonus div 2));
                                end;
                                if local_query_path_bonus <> 0 then
                                begin
                                    Inc(local_path_preference_score, local_query_path_bonus);
                                end;
                                local_incremental_path_bonus := get_incremental_path_stability_bonus_for_path(
                                    local_new_state.path_text);
                                if local_incremental_path_bonus > 0 then
                                begin
                                    Inc(local_path_preference_score, local_incremental_path_bonus);
                                end;
                                remember_segment_path_query_prefix(
                                    local_new_state.path_text,
                                    build_syllable_text(0, next_pos));
                                if m_last_lookup_key <> '' then
                                begin
                                    local_query_path_bonus := get_session_query_path_prefix_bonus(
                                        m_last_lookup_key, local_new_state.path_text);
                                    if local_query_path_bonus > 0 then
                                    begin
                                        Inc(local_new_state.score, local_query_path_bonus);
                                        Inc(local_path_preference_score, local_query_path_bonus);
                                    end;
                                    local_recent_ranked_bonus := get_session_ranked_query_path_bonus(
                                        local_new_state.path_text);
                                    if local_recent_ranked_bonus > 0 then
                                    begin
                                        Inc(local_new_state.score, local_recent_ranked_bonus);
                                        Inc(local_path_preference_score, local_recent_ranked_bonus);
                                    end;
                                    local_path_penalty_value := get_session_query_path_prefix_penalty(
                                        m_last_lookup_key, local_new_state.path_text);
                                    if local_path_penalty_value > 0 then
                                    begin
                                        Dec(local_new_state.score, local_path_penalty_value);
                                        Dec(local_path_preference_score, local_path_penalty_value);
                                    end;
                                    local_query_path_bonus := get_persistent_query_path_prefix_support(
                                        local_new_state.path_text);
                                    if local_query_path_bonus <> 0 then
                                    begin
                                        Inc(local_new_state.score, local_query_path_bonus);
                                        Inc(local_path_preference_score, local_query_path_bonus);
                                    end;

                                    if next_pos = Length(syllables) then
                                    begin
                                        local_query_path_bonus := get_session_query_path_bonus(
                                            m_last_lookup_key, local_new_state.path_text);
                                        if local_query_path_bonus > 0 then
                                        begin
                                            Inc(local_new_state.score, local_query_path_bonus div 2);
                                            Inc(local_path_preference_score, local_query_path_bonus);
                                        end;
                                        local_path_penalty_value := get_session_query_path_penalty(
                                            m_last_lookup_key, local_new_state.path_text);
                                        if local_path_penalty_value > 0 then
                                        begin
                                            Dec(local_new_state.score, local_path_penalty_value);
                                            Dec(local_path_preference_score, local_path_penalty_value);
                                        end;
                                        if m_dictionary <> nil then
                                        begin
                                            local_query_path_bonus := get_cached_query_segment_path_bonus(
                                                m_last_lookup_key, local_new_state.path_text);
                                            if local_query_path_bonus > 0 then
                                            begin
                                                Inc(local_new_state.score, local_query_path_bonus div 2);
                                                Inc(local_path_preference_score, local_query_path_bonus);
                                            end;
                                            local_path_penalty_value := get_cached_query_segment_path_penalty(
                                                m_last_lookup_key, local_new_state.path_text);
                                            if local_path_penalty_value > 0 then
                                            begin
                                                Dec(local_new_state.score, local_path_penalty_value);
                                                Dec(local_path_preference_score, local_path_penalty_value);
                                            end;
                                        end;
                                    end;
                                end;
                                local_new_state.path_preference_score := local_path_preference_score;
                                local_path_confidence_score := local_path_preference_score;
                                if local_path_confidence_score < 0 then
                                begin
                                    local_path_confidence_score := 0;
                                end;
                                local_new_state.path_confidence_score := local_path_confidence_score;
                            end;
                            append_state(next_pos, local_new_state);
                        end;

                        if full_path_budget_exhausted then
                        begin
                            Break;
                        end;
                    end;

                    if full_path_budget_exhausted then
                    begin
                        Break;
                    end;

                    trim_state(next_pos);
                end;
                if long_sentence_full_path_mode then
                begin
                    append_position_debug_count(state_pos, states[state_pos].Count);
                end;
            end;

            if states[Length(syllables)].Count > 0 then
            begin
                final_complete_state_count := states[Length(syllables)].Count;
                SetLength(sorted_states, states[Length(syllables)].Count);
                for final_index := 0 to states[Length(syllables)].Count - 1 do
                begin
                    sorted_states[final_index] := states[Length(syllables)][final_index];
                end;
                sort_state_array(sorted_states);

                for final_index := 0 to High(sorted_states) do
                begin
                    if get_encoded_path_segment_count_local(sorted_states[final_index].path_text) > 1 then
                    begin
                        Inc(final_complete_multiseg_state_count);
                        if final_complete_multiseg_state_count <= 5 then
                        begin
                            record_final_debug_path(sorted_states[final_index].path_text);
                        end;
                    end;
                end;

                full_non_user_added := 0;
                for final_index := 0 to High(sorted_states) do
                begin
                    local_state := sorted_states[final_index];
                    if (not local_state.has_multi_segment) and
                        (not ((Pos('''', m_composition_text) > 0) and
                        (get_encoded_path_segment_count_local(local_state.path_text) > 1))) then
                    begin
                        Continue;
                    end;
                    local_multi_char_segment_count :=
                        get_multi_char_segment_count(local_state.path_text);
                    if local_multi_char_segment_count <= 0 then
                    begin
                        Continue;
                    end;
                    if (local_state.source <> cs_user) and (full_non_user_added >= full_path_non_user_limit) then
                    begin
                        Continue;
                    end;
                    append_candidate(local_state.text, local_state.score,
                        local_state.source, '', False, 0,
                        local_state.fuzzy_cost, local_state.fuzzy_rules);
                    remember_segment_path_for_candidate(local_state.text, '', local_state.path_text,
                        local_state.score);
                    if local_state.source <> cs_user then
                    begin
                        Inc(full_non_user_added);
                    end;
                end;
            end;

            // Surface high-quality multi-segment prefix paths as partial candidates,
            // e.g. "womenjintianquz" => "鎴戜滑浠婂ぉ鍘粅z". This keeps multi-word
            // prefixes visible before the tail is fully typed, even when the
            // consumed prefix itself is not a monolithic dictionary phrase.
            for state_pos := Length(syllables) - 1 downto 2 do
            begin
                remaining_syllables := Length(syllables) - state_pos;
                if (remaining_syllables <= 0) or
                    (remaining_syllables > c_segment_full_partial_remaining_limit) or
                    (states[state_pos].Count = 0) then
                begin
                    Continue;
                end;

                remaining_pinyin := build_syllable_text(state_pos, remaining_syllables);
                if remaining_pinyin = '' then
                begin
                    Continue;
                end;

                SetLength(sorted_states, states[state_pos].Count);
                for final_index := 0 to states[state_pos].Count - 1 do
                begin
                    sorted_states[final_index] := states[state_pos][final_index];
                end;
                sort_state_array(sorted_states);

                full_non_user_added := 0;
                for final_index := 0 to High(sorted_states) do
                begin
                    local_state := sorted_states[final_index];
                    if not local_state.has_multi_segment then
                    begin
                        Continue;
                    end;
                    if get_encoded_path_segment_count_local(local_state.path_text) <= 1 then
                    begin
                        Continue;
                    end;
                    local_multi_char_segment_count :=
                        get_multi_char_segment_count(local_state.path_text);
                    if local_multi_char_segment_count <= 0 then
                    begin
                        Continue;
                    end;
                    if get_candidate_text_unit_count(local_state.text) < 2 then
                    begin
                        Continue;
                    end;
                    if (local_state.source <> cs_user) and
                        (full_non_user_added >= c_segment_full_partial_non_user_limit) then
                    begin
                        Continue;
                    end;

                    score_value := local_state.score;
                    Dec(score_value, c_segment_full_partial_penalty * remaining_syllables);
                    Dec(score_value, c_segment_full_partial_quadratic_penalty *
                        remaining_syllables * (remaining_syllables - 1));
                    append_candidate(local_state.text, score_value,
                        local_state.source, remaining_pinyin, False, 0,
                        local_state.fuzzy_cost, local_state.fuzzy_rules);
                    remember_segment_path_for_candidate(local_state.text, remaining_pinyin,
                        local_state.path_text, score_value);

                    if local_state.source <> cs_user then
                    begin
                        Inc(full_non_user_added);
                    end;
                end;
            end;

            append_three_syllable_anchor_partial_candidates;
        finally
            capture_debug_target_state_summary;
            if final_complete_state_count > 0 then
            begin
                m_last_full_path_debug_info := Format(
                    ' fullpath_end=[states=%d multiseg=%d pos=%s sample=%s]',
                    [final_complete_state_count, final_complete_multiseg_state_count,
                    Copy(final_position_debug_counts, 1, 180),
                    Copy(Trim(final_complete_debug_paths), 1, 180)]);
            end
            else
            begin
                m_last_full_path_debug_info := Format(' fullpath_end=[states=0 multiseg=0 pos=%s]',
                    [Copy(final_position_debug_counts, 1, 180)]);
            end;
            for state_pos := 0 to High(states) do
            begin
                if states[state_pos] <> nil then
                begin
                    states[state_pos].Free;
                    states[state_pos] := nil;
                end;
                if state_dedup[state_pos] <> nil then
                begin
                    state_dedup[state_pos].Free;
                    state_dedup[state_pos] := nil;
                end;
                if (state_pos <= High(single_char_rank_maps)) and (single_char_rank_maps[state_pos] <> nil) then
                begin
                    single_char_rank_maps[state_pos].Free;
                    single_char_rank_maps[state_pos] := nil;
                end;
            end;
            if transition_bonus_cache <> nil then
            begin
                transition_bonus_cache.Free;
            end;
        end;
    end;
begin
    SetLength(out_candidates, 0);
    out_path_search_elapsed_ms := 0;
    Result := False;
    if (m_dictionary = nil) or (m_composition_text = '') then
    begin
        Exit;
    end;

    lookup_cache := m_build_lookup_cache;
    owns_lookup_cache := lookup_cache = nil;
    if owns_lookup_cache then
    begin
        lookup_cache := TDictionary<string, TncCandidateList>.Create;
    end;
    try
        syllables := get_effective_compact_pinyin_syllables(m_composition_text,
            allow_relaxed_missing_apostrophe);
        if Length(syllables) <= 1 then
        begin
            Exit;
        end;

        if Length(syllables) > c_segment_max_syllables then
        begin
            Exit;
        end;

        is_first_overall_segment := (m_confirmed_segments = nil) or (m_confirmed_segments.Count = 0);

        max_word_len := c_segment_word_max_syllables;
        if (not include_full_path) and
            (Length(syllables) >= c_long_sentence_head_only_bypass_min_syllables) then
        begin
            max_word_len := Min(max_word_len, 2);
        end;
        if max_word_len > Length(syllables) then
        begin
            max_word_len := Length(syllables);
        end;

        total_limit := get_total_candidate_limit;
        if total_limit <= 0 then
        begin
            Exit;
        end;

        per_limit := total_limit;
        if get_candidate_limit > 0 then
        begin
            if get_candidate_limit * c_segment_page_expand_factor > per_limit then
            begin
                per_limit := get_candidate_limit * c_segment_page_expand_factor;
            end;
        end;
        if per_limit > c_segment_max_per_segment then
        begin
            per_limit := c_segment_max_per_segment;
        end;

        list := TList<TncCandidate>.Create;
        dedup := TDictionary<string, Integer>.Create;
        try
            if include_full_path then
            begin
                // First, build full-path phrase candidates (for example women+jintian -> full phrase).
                path_search_start_tick := nc_monotonic_tick_ms;
                append_full_path_candidates;
                Inc(out_path_search_elapsed_ms, Int64(nc_monotonic_tick_ms - path_search_start_tick));
            end;

            // Keep prefix segment candidates as fallback/partial-commit choices.
            for segment_len := 1 to max_word_len do
            begin
                // In head-only mode, keep chunk conversion behavior for long input:
                // only expose prefix chunks and leave remaining pinyin for next commit.
                if (not include_full_path) and (Length(syllables) >= 3) and
                    (segment_len >= Length(syllables)) then
                begin
                    Break;
                end;

                segment_text := build_syllable_text(0, segment_len);
                if segment_text = '' then
                begin
                    Continue;
                end;

                if not dictionary_lookup_cached(segment_text, lookup_results) then
                begin
                    Continue;
                end;

                // Keep one-syllable segment lookups untrimmed here: we filter to single-char
                // candidates below, and early truncation can starve useful fallbacks.
                if (segment_len > 1) and (per_limit > 0) and (Length(lookup_results) > per_limit) then
                begin
                    SetLength(lookup_results, per_limit);
                end;

                remaining_syllables := Length(syllables) - segment_len;
                if remaining_syllables > 0 then
                begin
                    remaining_pinyin := build_syllable_text(segment_len, remaining_syllables);
                end
                else
                begin
                    remaining_pinyin := '';
                end;

                if (segment_len = 1) and (remaining_syllables > 0) and
                    (Length(lookup_results) > c_segment_partial_single_top_n) then
                begin
                    partial_single_limit := c_segment_partial_single_top_n;
                    if get_candidate_limit > 0 then
                    begin
                        partial_single_limit := Max(partial_single_limit,
                            get_candidate_limit * 2);
                    end;
                    partial_single_limit := Min(partial_single_limit, 24);
                    if Length(lookup_results) > partial_single_limit then
                    begin
                        SetLength(lookup_results, partial_single_limit);
                    end;
                end;

                for i := 0 to High(lookup_results) do
                begin
                    candidate := lookup_results[i];
                    candidate_text_units := get_text_unit_count(Trim(candidate.text));
                    if candidate_text_units <= 0 then
                    begin
                        Continue;
                    end;
                    // Avoid over-complete long phrase suggestions on incomplete trailing initials,
                    // e.g. "jibuzh" => "閻犱線顣︾粭澶嬫媴? + dangling "h".
                    if (segment_len >= 3) and (remaining_syllables = 1) and
                        (High(syllables) >= 0) and
                        is_single_initial_token(syllables[High(syllables)].text) and
                        (candidate_text_units > 1) and (candidate_text_units >= segment_len) then
                    begin
                        Continue;
                    end;
                    if (segment_len = 1) and (candidate_text_units > 1) then
                    begin
                        Continue;
                    end;
                    if (Length(syllables) >= 3) and (segment_len = 1) and
                        (remaining_syllables >= 2) and (i >= c_segment_partial_single_top_n) then
                    begin
                        Continue;
                    end;
                    score_value := candidate.score + (segment_len * c_segment_prefix_bonus);
                    if (remaining_syllables > 0) and (segment_len > 1) then
                    begin
                        Inc(score_value, (segment_len - 1) * c_segment_long_prefix_bonus);
                    end;
                    if remaining_syllables > 0 then
                    begin
                        Dec(score_value, c_segment_partial_penalty * remaining_syllables);
                        Dec(score_value, c_segment_partial_quadratic_penalty *
                            remaining_syllables * remaining_syllables);
                    end;

                    if candidate_text_units <> segment_len then
                    begin
                        Dec(score_value, Abs(candidate_text_units - segment_len) * c_segment_text_unit_mismatch_penalty);
                        if candidate_text_units > segment_len then
                        begin
                            Dec(score_value,
                                (candidate_text_units - segment_len) * c_segment_text_unit_overflow_penalty);
                        end;
                    end;

                    if is_first_overall_segment and (segment_len = 1) and (remaining_syllables > 0) and
                        (Length(candidate.text) = 1) and is_common_surname_text(candidate.text) then
                    begin
                        Inc(score_value, c_segment_surname_bonus);
                    end;

                    append_candidate(candidate.text, score_value, candidate.source, remaining_pinyin,
                        candidate.has_dict_weight, candidate.dict_weight,
                        candidate.fuzzy_cost, candidate.fuzzy_rules);
                end;
            end;

            if Length(syllables) < c_long_sentence_head_only_bypass_min_syllables then
            begin
                update_three_syllable_partial_preference_kind_local;
                append_three_syllable_head_only_anchor_partial_candidates;
                append_long_prefix_phrase_anchor_partial_candidates;
            end;

            if list.Count = 0 then
            begin
                Exit;
            end;

            SetLength(out_candidates, list.Count);
            for i := 0 to list.Count - 1 do
            begin
                out_candidates[i] := list[i];
            end;

            sort_candidates(out_candidates);
            // Guarantee at least one partial-commit fallback candidate is visible after truncation.
            if (total_limit > 0) and (Length(out_candidates) > total_limit) then
            begin
                existing_index := -1;
                for i := 0 to High(out_candidates) do
                begin
                    if out_candidates[i].comment <> '' then
                    begin
                        existing_index := i;
                        Break;
                    end;
                end;

                if existing_index >= total_limit then
                begin
                    candidate := out_candidates[existing_index];
                    for i := existing_index downto total_limit do
                    begin
                        out_candidates[i] := out_candidates[i - 1];
                    end;
                    out_candidates[total_limit - 1] := candidate;
                end;
            end;

            if Length(out_candidates) > total_limit then
            begin
                SetLength(out_candidates, total_limit);
            end;
            Result := Length(out_candidates) > 0;
        finally
            dedup.Free;
            list.Free;
        end;
    finally
        if owns_lookup_cache then
        begin
            lookup_cache.Free;
        end;
    end;
end;

function TncEngine.build_pinyin_comment(const input_text: string;
    const allow_relaxed_missing_apostrophe: Boolean): string;
var
    parser: TncPinyinParser;
    parts: TncPinyinParseResult;
    i: Integer;
begin
    Result := '';
    if input_text = '' then
    begin
        Exit;
    end;

    parser := TncPinyinParser.create;
    try
        if allow_relaxed_missing_apostrophe then
        begin
            parts := parse_pinyin_with_relaxed_missing_apostrophe(input_text);
        end
        else
        begin
            parts := parser.parse(input_text);
        end;
        if Length(parts) <= 1 then
        begin
            Exit;
        end;

        for i := 0 to High(parts) do
        begin
            if Result <> '' then
            begin
                Result := Result + ' ';
            end;
            Result := Result + parts[i].text;
        end;
    finally
        parser.Free;
    end;
end;

procedure TncEngine.update_segment_left_context;
var
    segment_context: string;
begin
    segment_context := trim_left_context_to_sentence(m_confirmed_text,
        c_left_context_max_len);
    m_segment_left_context := segment_context;
end;

procedure TncEngine.push_confirmed_segment(const text: string; const pinyin: string;
    const input_code: string; const canonical_pinyin: string;
    const explicit_choice: Boolean; const literal_choice: Boolean;
    const fuzzy_choice: Boolean);
var
    item: TncConfirmedSegment;
begin
    if (text = '') or (pinyin = '') then
    begin
        Exit;
    end;

    if m_confirmed_segments = nil then
    begin
        Exit;
    end;

    item.text := text;
    item.pinyin := pinyin;
    item.input_code := input_code;
    item.canonical_pinyin := canonical_pinyin;
    item.explicit_choice := explicit_choice;
    item.literal_choice := literal_choice;
    item.fuzzy_choice := fuzzy_choice;
    m_confirmed_segments.Add(item);
    m_confirmed_text := m_confirmed_text + text;
    if Length(split_text_units(Trim(text))) = 1 then
    begin
        m_recent_partial_prefix_text := Trim(text);
    end
    else
    begin
        m_recent_partial_prefix_text := '';
    end;
    update_segment_left_context;
end;

procedure TncEngine.rebuild_confirmed_text;
var
    i: Integer;
begin
    m_confirmed_text := '';
    m_recent_partial_prefix_text := '';
    if m_confirmed_segments = nil then
    begin
        Exit;
    end;

    for i := 0 to m_confirmed_segments.Count - 1 do
    begin
        m_confirmed_text := m_confirmed_text + m_confirmed_segments[i].text;
    end;
    if (m_confirmed_segments.Count > 0) and
        (Length(split_text_units(Trim(m_confirmed_segments[m_confirmed_segments.Count - 1].text))) = 1) then
    begin
        m_recent_partial_prefix_text := Trim(m_confirmed_segments[m_confirmed_segments.Count - 1].text);
    end;
end;

function TncEngine.pop_confirmed_segment(out out_segment: TncConfirmedSegment): Boolean;
begin
    Result := False;
    if (m_confirmed_segments = nil) or (m_confirmed_segments.Count = 0) then
    begin
        Exit;
    end;

    out_segment := m_confirmed_segments[m_confirmed_segments.Count - 1];
    m_confirmed_segments.Delete(m_confirmed_segments.Count - 1);
    rebuild_confirmed_text;
    update_segment_left_context;
    Result := True;
end;

function TncEngine.rollback_last_segment: Boolean;
var
    segment: TncConfirmedSegment;
    remaining_segment: TncConfirmedSegment;
begin
    Result := False;
    if not pop_confirmed_segment(segment) then
    begin
        Exit;
    end;
    clear_short_exact_pair_tail_handoff;

    if is_shuangpin_input then
    begin
        if segment.input_code <> '' then
        begin
            m_composition_display_text := segment.input_code +
                m_composition_display_text;
        end
        else
        begin
            m_composition_display_text := segment.pinyin +
                m_composition_display_text;
        end;
        rebuild_shuangpin_composition_from_input_code;
    end
    else
    begin
        m_composition_text := segment.pinyin + m_composition_text;
        if segment.input_code <> '' then
        begin
            m_composition_display_text := segment.input_code +
                m_composition_display_text;
        end
        else if m_composition_display_text <> '' then
        begin
            m_composition_display_text := segment.pinyin +
                m_composition_display_text;
        end
        else
        begin
            m_composition_display_text := m_composition_text;
        end;
    end;
    m_confirmed_explicit_choice := False;
    if m_confirmed_segments <> nil then
    begin
        for remaining_segment in m_confirmed_segments do
        begin
            m_confirmed_explicit_choice := m_confirmed_explicit_choice or
                remaining_segment.explicit_choice;
        end;
    end;
    clear_pending_commit;
    m_page_index := 0;
    build_candidates;
    Result := True;
end;

procedure TncEngine.apply_partial_commit(const selected_text: string; const remaining_pinyin: string;
    const segment_path: string = ''; const explicit_choice: Boolean = False;
    const remaining_input_code: string = '';
    const literal_choice: Boolean = False;
    const fuzzy_choice: Boolean = False);
var
    normalized_pinyin: string;
    prefix_pinyin: string;
    normalized_remaining_pinyin: string;
    full_input_code: string;
    prefix_input_code: string;
    effective_remaining_input_code: string;
    effective_segment_path: string;
    prev_left_context: string;
    canonical_prefix_pinyin: string;
    literal_composition_choice: Boolean;

    procedure record_query_path_prefixes(const tail_full_path: string; const final_query: string);
    var
        idx: Integer;
        prefix_start: Integer;
        segment_piece: string;
        prefix_path: string;
        prefix_query: string;
    begin
        if (m_dictionary = nil) or (tail_full_path = '') then
        begin
            Exit;
        end;

        prefix_path := '';
        prefix_start := 1;
        for idx := 1 to Length(tail_full_path) + 1 do
        begin
            if (idx <= Length(tail_full_path)) and (tail_full_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_piece := Trim(Copy(tail_full_path, prefix_start, idx - prefix_start));
            prefix_start := idx + 1;
            if segment_piece = '' then
            begin
                Continue;
            end;

            if prefix_path <> '' then
            begin
                prefix_path := prefix_path + c_segment_path_separator;
            end;
            prefix_path := prefix_path + segment_piece;
            if get_encoded_path_segment_count_local(prefix_path) <= 1 then
            begin
                Continue;
            end;

            prefix_query := get_query_prefix_for_segment_path(prefix_path);
            if (prefix_query = '') or (prefix_query = final_query) then
            begin
                Continue;
            end;

            m_dictionary.record_query_segment_path(prefix_query, prefix_path);
        end;
    end;

    procedure note_session_query_path_prefixes(const tail_full_path: string; const final_query: string);
    var
        idx: Integer;
        prefix_start: Integer;
        segment_piece: string;
        prefix_path: string;
        prefix_query: string;
    begin
        if tail_full_path = '' then
        begin
            Exit;
        end;

        prefix_path := '';
        prefix_start := 1;
        for idx := 1 to Length(tail_full_path) + 1 do
        begin
            if (idx <= Length(tail_full_path)) and (tail_full_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_piece := Trim(Copy(tail_full_path, prefix_start, idx - prefix_start));
            prefix_start := idx + 1;
            if segment_piece = '' then
            begin
                Continue;
            end;

            if prefix_path <> '' then
            begin
                prefix_path := prefix_path + c_segment_path_separator;
            end;
            prefix_path := prefix_path + segment_piece;
            if get_encoded_path_segment_count_local(prefix_path) <= 1 then
            begin
                Continue;
            end;

            prefix_query := get_query_prefix_for_segment_path(prefix_path);
            if (prefix_query = '') or (prefix_query = final_query) then
            begin
                Continue;
            end;

            note_session_query_path_choice(prefix_query, prefix_path);
        end;
    end;
begin
    if (selected_text = '') or (remaining_pinyin = '') then
    begin
        Exit;
    end;

    normalized_pinyin := normalize_pinyin_text(m_composition_text);
    prefix_pinyin := normalized_pinyin;
    normalized_remaining_pinyin := normalize_pinyin_text(remaining_pinyin);
    full_input_code := m_composition_display_text;
    if full_input_code = '' then
    begin
        full_input_code := m_composition_text;
    end;
    effective_remaining_input_code := remaining_input_code;
    if effective_remaining_input_code = '' then
    begin
        effective_remaining_input_code := remaining_pinyin;
    end;
    prefix_input_code := full_input_code;
    if (effective_remaining_input_code <> '') and
        (Length(effective_remaining_input_code) <= Length(prefix_input_code)) and
        SameText(Copy(prefix_input_code,
        Length(prefix_input_code) - Length(effective_remaining_input_code) + 1,
        Length(effective_remaining_input_code)), effective_remaining_input_code) then
    begin
        prefix_input_code := Copy(prefix_input_code, 1,
            Length(prefix_input_code) - Length(effective_remaining_input_code));
    end;
    if (normalized_remaining_pinyin <> '') and
        (Length(normalized_remaining_pinyin) <= Length(prefix_pinyin)) then
    begin
        if Copy(prefix_pinyin, Length(prefix_pinyin) - Length(normalized_remaining_pinyin) + 1,
            Length(normalized_remaining_pinyin)) = normalized_remaining_pinyin then
        begin
            prefix_pinyin := Copy(prefix_pinyin, 1,
                Length(prefix_pinyin) - Length(normalized_remaining_pinyin));
        end;
    end;

    if prefix_pinyin = '' then
    begin
        prefix_pinyin := normalized_pinyin;
    end;
    if prefix_input_code = '' then
    begin
        prefix_input_code := prefix_pinyin;
    end;

    effective_segment_path := Trim(segment_path);
    if (effective_segment_path = '') and (selected_text <> '') and (prefix_pinyin <> '') then
    begin
        effective_segment_path := infer_segment_path_for_selected_text(selected_text);
    end;

    prev_left_context := m_left_context;
    if m_segment_left_context <> '' then
    begin
        prev_left_context := m_segment_left_context;
    end
    else if m_external_left_context <> '' then
    begin
        prev_left_context := m_external_left_context;
    end;

    canonical_prefix_pinyin := '';
    literal_composition_choice := literal_choice and
        (not is_full_pinyin_key(normalized_pinyin)) and
        (m_dictionary <> nil) and
        m_dictionary.resolve_literal_user_word_pinyin(prefix_pinyin,
        selected_text, canonical_prefix_pinyin);

    prepare_short_exact_pair_tail_handoff(normalized_pinyin,
        selected_text, normalized_remaining_pinyin);

    // Partial selections are only anchors for the current composition. Persisting
    // them as complete user words pollutes the user dictionary with fragments
    // such as "suoweikai -> 鎵€璋撳紑" from a "鎵€璋撳紑/fa" candidate.
    if (not literal_composition_choice) and (not fuzzy_choice) then
    begin
        note_session_commit(selected_text);
        if prefix_pinyin <> '' then
        begin
            note_session_query_choice(prefix_pinyin, selected_text);
            note_session_context_query_choice(prev_left_context, prefix_pinyin,
                selected_text);
            if get_encoded_path_segment_count_local(effective_segment_path) > 1 then
            begin
                note_session_query_path_prefixes(effective_segment_path,
                    prefix_pinyin);
                note_session_query_path_choice(prefix_pinyin,
                    effective_segment_path);
            end;
        end;
    end;

    if Length(split_text_units(Trim(selected_text))) = 1 then
    begin
        m_recent_partial_prefix_text := Trim(selected_text);
    end
    else
    begin
        m_recent_partial_prefix_text := '';
    end;
    if (canonical_prefix_pinyin = '') and literal_choice and
        (m_dictionary <> nil) then
    begin
        m_dictionary.resolve_literal_user_word_pinyin(prefix_pinyin,
            selected_text, canonical_prefix_pinyin);
    end;
    push_confirmed_segment(selected_text, prefix_pinyin, prefix_input_code,
        canonical_prefix_pinyin, explicit_choice, literal_choice,
        fuzzy_choice);
    m_confirmed_explicit_choice := m_confirmed_explicit_choice or explicit_choice;

    m_composition_text := remaining_pinyin;
    m_composition_display_text := effective_remaining_input_code;
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_segment_path := '';
    m_page_index := 0;
    build_candidates;
    apply_short_exact_pair_tail_handoff;
end;

procedure TncEngine.set_pending_commit(const text: string; const remaining_pinyin: string = '';
    const allow_learning: Boolean = True; const segment_path: string = '';
    const explicit_choice: Boolean = False; const remaining_input_code: string = '';
    const literal_choice: Boolean = False;
    const fuzzy_choice: Boolean = False;
    const fuzzy_choice_text: string = '';
    const learning_locked: Boolean = False);
var
    query_key: string;
    normalized_remaining: string;
begin
    m_pending_commit_text := text;
    m_pending_commit_remaining := remaining_pinyin;
    m_pending_commit_remaining_input_code := remaining_input_code;
    if (m_pending_commit_remaining_input_code = '') and
        (remaining_pinyin <> '') then
    begin
        m_pending_commit_remaining_input_code := m_composition_display_text;
    end;
    m_has_pending_commit := True;
    normalized_remaining := normalize_pinyin_text(remaining_pinyin);
    m_pending_commit_allow_learning := allow_learning and
        (not fuzzy_choice) and (normalized_remaining = '');
    m_pending_commit_learning_locked := learning_locked;
    m_pending_commit_explicit_choice := explicit_choice;
    m_pending_commit_literal_choice := literal_choice;
    m_pending_commit_fuzzy_choice := fuzzy_choice;
    m_pending_commit_fuzzy_text := Trim(fuzzy_choice_text);
    m_pending_commit_segment_path := segment_path;
    query_key := normalize_pinyin_text(m_last_lookup_key);
    if query_key = '' then
    begin
        query_key := normalize_pinyin_text(m_composition_text);
    end;
    m_pending_commit_query_key := query_key;
end;

procedure TncEngine.clear_pending_commit;
begin
    if not m_has_pending_commit then
    begin
        Exit;
    end;

    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_segment_path := '';
    m_pending_commit_query_key := '';
end;

procedure TncEngine.update_left_context(const committed_text: string);
var
    next_context: string;
begin
    next_context := trim_left_context_to_sentence(m_left_context +
        committed_text, c_left_context_max_len);
    if next_context = '' then
    begin
        m_left_context := '';
        Exit;
    end;

    m_left_context := next_context;
end;

procedure TncEngine.note_session_commit(const text: string);
var
    count: Integer;
    evict_text: string;
    text_key: string;
begin
    text_key := Trim(text);
    if (text_key = '') or (m_session_text_counts = nil) or (m_session_text_order = nil) then
    begin
        Exit;
    end;

    Inc(m_session_commit_serial);

    if m_session_text_counts.TryGetValue(text_key, count) then
    begin
        Inc(count);
        m_session_text_counts.AddOrSetValue(text_key, count);
    end
    else
    begin
        m_session_text_counts.Add(text_key, 1);
    end;
    if m_session_text_last_seen <> nil then
    begin
        m_session_text_last_seen.AddOrSetValue(text_key, m_session_commit_serial);
    end;

    m_session_text_order.Enqueue(text_key);
    while m_session_text_order.Count > c_session_text_history_limit do
    begin
        evict_text := m_session_text_order.Dequeue;
        if not m_session_text_counts.TryGetValue(evict_text, count) then
        begin
            Continue;
        end;
        Dec(count);
        if count <= 0 then
        begin
            m_session_text_counts.Remove(evict_text);
            if m_session_text_last_seen <> nil then
            begin
                m_session_text_last_seen.Remove(evict_text);
            end;
        end
        else
        begin
            m_session_text_counts.AddOrSetValue(evict_text, count);
        end;
    end;
end;

procedure TncEngine.note_session_query_choice(const query_key: string; const text: string);
var
    count: Integer;
    evict_key: string;
    key: string;
    current_serial: Int64;
begin
    key := build_session_query_choice_key(normalize_pinyin_text(query_key), text);
    if (key = '') or (m_session_query_choice_counts = nil) or (m_session_query_choice_order = nil) then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;

    if m_session_query_choice_counts.TryGetValue(key, count) then
    begin
        Inc(count);
        m_session_query_choice_counts.AddOrSetValue(key, count);
    end
    else
    begin
        m_session_query_choice_counts.Add(key, 1);
    end;
    if m_session_query_choice_last_seen <> nil then
    begin
        m_session_query_choice_last_seen.AddOrSetValue(key, current_serial);
    end;
    if m_session_query_latest_text <> nil then
    begin
        m_session_query_latest_text.AddOrSetValue(normalize_pinyin_text(query_key), Trim(text));
    end;

    m_session_query_choice_order.Enqueue(key);
    while m_session_query_choice_order.Count > c_session_query_history_limit do
    begin
        evict_key := m_session_query_choice_order.Dequeue;
        if not m_session_query_choice_counts.TryGetValue(evict_key, count) then
        begin
            Continue;
        end;

        Dec(count);
        if count <= 0 then
        begin
            m_session_query_choice_counts.Remove(evict_key);
            if m_session_query_choice_last_seen <> nil then
            begin
                m_session_query_choice_last_seen.Remove(evict_key);
            end;
        end
        else
        begin
            m_session_query_choice_counts.AddOrSetValue(evict_key, count);
        end;
    end;
end;

procedure TncEngine.note_session_query_path_choice(const query_key: string; const encoded_path: string);
var
    count: Integer;
    current_serial: Int64;
    evict_key: string;
    key: string;
begin
    key := build_session_query_path_choice_key(normalize_pinyin_text(query_key), encoded_path);
    if (key = '') or (m_session_query_path_choice_counts = nil) or
        (m_session_query_path_choice_order = nil) then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;

    if m_session_query_path_choice_counts.TryGetValue(key, count) then
    begin
        Inc(count);
        m_session_query_path_choice_counts.AddOrSetValue(key, count);
    end
    else
    begin
        m_session_query_path_choice_counts.Add(key, 1);
    end;

    if m_session_query_path_choice_last_seen <> nil then
    begin
        m_session_query_path_choice_last_seen.AddOrSetValue(key, current_serial);
    end;

    // Keep immediate follow-up lookups in sync with the just-recorded choice.
    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.Clear;
    end;

    m_session_query_path_choice_order.Enqueue(key);
    while m_session_query_path_choice_order.Count > c_session_query_path_history_limit do
    begin
        evict_key := m_session_query_path_choice_order.Dequeue;
        if not m_session_query_path_choice_counts.TryGetValue(evict_key, count) then
        begin
            Continue;
        end;

        Dec(count);
        if count <= 0 then
        begin
            m_session_query_path_choice_counts.Remove(evict_key);
            if m_session_query_path_choice_last_seen <> nil then
            begin
                m_session_query_path_choice_last_seen.Remove(evict_key);
            end;
        end
        else
        begin
            m_session_query_path_choice_counts.AddOrSetValue(evict_key, count);
        end;
    end;
end;

procedure TncEngine.note_session_query_path_penalty(const query_key: string; const encoded_path: string);
var
    count: Integer;
    current_serial: Int64;
    evict_key: string;
    key: string;
begin
    key := build_session_query_path_choice_key(normalize_pinyin_text(query_key), encoded_path);
    if (key = '') or (m_session_query_path_penalty_counts = nil) or
        (m_session_query_path_penalty_order = nil) then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;

    if m_session_query_path_penalty_counts.TryGetValue(key, count) then
    begin
        Inc(count);
        m_session_query_path_penalty_counts.AddOrSetValue(key, count);
    end
    else
    begin
        m_session_query_path_penalty_counts.Add(key, 1);
    end;

    if m_session_query_path_penalty_last_seen <> nil then
    begin
        m_session_query_path_penalty_last_seen.AddOrSetValue(key, current_serial);
    end;

    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.Clear;
    end;

    m_session_query_path_penalty_order.Enqueue(key);
    while m_session_query_path_penalty_order.Count > c_session_query_path_history_limit do
    begin
        evict_key := m_session_query_path_penalty_order.Dequeue;
        if not m_session_query_path_penalty_counts.TryGetValue(evict_key, count) then
        begin
            Continue;
        end;

        Dec(count);
        if count <= 0 then
        begin
            m_session_query_path_penalty_counts.Remove(evict_key);
            if m_session_query_path_penalty_last_seen <> nil then
            begin
                m_session_query_path_penalty_last_seen.Remove(evict_key);
            end;
        end
        else
        begin
            m_session_query_path_penalty_counts.AddOrSetValue(evict_key, count);
        end;
    end;
end;

procedure TncEngine.note_session_ranked_query_path(const query_key: string; const encoded_path: string;
    const path_confidence_score: Integer);
var
    normalized_query: string;
    normalized_path: string;
    evict_key: string;
begin
    normalized_query := normalize_pinyin_text(query_key);
    normalized_path := Trim(encoded_path);
    if (normalized_query = '') or (normalized_path = '') or
        (get_encoded_path_segment_count_local(normalized_path) <= 1) or
        (m_session_ranked_query_paths = nil) or (m_session_ranked_query_path_scores = nil) or
        (m_session_ranked_query_path_order = nil) then
    begin
        Exit;
    end;

    if not m_session_ranked_query_paths.ContainsKey(normalized_query) then
    begin
        m_session_ranked_query_path_order.Enqueue(normalized_query);
    end;
    m_session_ranked_query_paths.AddOrSetValue(normalized_query, normalized_path);
    m_session_ranked_query_path_scores.AddOrSetValue(normalized_query,
        Max(0, path_confidence_score));

    if m_lookup_query_path_bonus_cache <> nil then
    begin
        m_lookup_query_path_bonus_cache.Clear;
    end;

    while m_session_ranked_query_path_order.Count > c_session_ranked_query_path_history_limit do
    begin
        evict_key := m_session_ranked_query_path_order.Dequeue;
        m_session_ranked_query_paths.Remove(evict_key);
        m_session_ranked_query_path_scores.Remove(evict_key);
    end;
end;

procedure TncEngine.note_session_context_query_choice(const context_text: string; const query_key: string;
    const text: string);
var
    context_variants: TArray<string>;
    variant_idx: Integer;
    key: string;
    count: Integer;
    evict_key: string;
    current_serial: Int64;
    normalized_query: string;
    context_units: Integer;
    persist_choice: Boolean;
begin
    if (Trim(context_text) = '') or (Trim(query_key) = '') or (Trim(text) = '') or
        (m_session_context_query_choice_counts = nil) or (m_session_context_query_choice_order = nil) then
    begin
        Exit;
    end;

    normalized_query := normalize_pinyin_text(query_key);
    context_variants := get_context_variants(context_text);
    if Length(context_variants) = 0 then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;
    persist_choice := (m_dictionary <> nil) and
        (m_dictionary.is_base_entry(normalized_query, Trim(text)) or
        m_dictionary.is_user_entry(normalized_query, Trim(text)));

    if persist_choice then
    begin
        m_dictionary.begin_learning_batch;
    end;
    try
        for variant_idx := 0 to High(context_variants) do
        begin
            key := build_context_query_choice_key(context_variants[variant_idx],
                normalized_query, text);
            if key = '' then
            begin
                Continue;
            end;

            if m_session_context_query_choice_counts.TryGetValue(key, count) then
            begin
                Inc(count);
                m_session_context_query_choice_counts.AddOrSetValue(key, count);
            end
            else
            begin
                m_session_context_query_choice_counts.Add(key, 1);
            end;

            if m_session_context_query_choice_last_seen <> nil then
            begin
                m_session_context_query_choice_last_seen.AddOrSetValue(key,
                    current_serial);
            end;
            if m_session_context_query_latest_text <> nil then
            begin
                m_session_context_query_latest_text.AddOrSetValue(
                    build_context_query_scope_key(
                    context_variants[variant_idx], normalized_query),
                    Trim(text));
            end;

            context_units := get_candidate_text_unit_count(
                context_variants[variant_idx]);
            if persist_choice and (context_units >= 2) and
                (context_units <= 4) then
            begin
                m_dictionary.record_context_query_choice(
                    context_variants[variant_idx], normalized_query,
                    Trim(text));
            end;

            m_session_context_query_choice_order.Enqueue(key);
        end;
        if persist_choice then
        begin
            m_dictionary.commit_learning_batch;
        end;
    except
        if persist_choice then
        begin
            m_dictionary.rollback_learning_batch;
        end;
        raise;
    end;

    while m_session_context_query_choice_order.Count > c_session_context_query_history_limit do
    begin
        evict_key := m_session_context_query_choice_order.Dequeue;
        if not m_session_context_query_choice_counts.TryGetValue(evict_key, count) then
        begin
            Continue;
        end;

        Dec(count);
        if count <= 0 then
        begin
            m_session_context_query_choice_counts.Remove(evict_key);
            if m_session_context_query_choice_last_seen <> nil then
            begin
                m_session_context_query_choice_last_seen.Remove(evict_key);
            end;
        end
        else
        begin
            m_session_context_query_choice_counts.AddOrSetValue(evict_key, count);
        end;
    end;
end;

procedure TncEngine.track_phrase_context_key(const phrase_key: string; const current_serial: Int64);
var
    count: Integer;
    evict_key: string;
begin
    if (phrase_key = '') or (m_phrase_context_pairs = nil) or (m_phrase_context_order = nil) then
    begin
        Exit;
    end;

    if m_phrase_context_pairs.TryGetValue(phrase_key, count) then
    begin
        Inc(count);
        m_phrase_context_pairs.AddOrSetValue(phrase_key, count);
    end
    else
    begin
        m_phrase_context_pairs.Add(phrase_key, 1);
    end;
    m_phrase_context_order.Enqueue(phrase_key);
    if m_phrase_context_last_seen <> nil then
    begin
        m_phrase_context_last_seen.AddOrSetValue(phrase_key, current_serial);
    end;

    while m_phrase_context_order.Count > c_phrase_context_history_limit do
    begin
        evict_key := m_phrase_context_order.Dequeue;
        if not m_phrase_context_pairs.TryGetValue(evict_key, count) then
        begin
            Continue;
        end;

        Dec(count);
        if count <= 0 then
        begin
            m_phrase_context_pairs.Remove(evict_key);
            if m_phrase_context_last_seen <> nil then
            begin
                m_phrase_context_last_seen.Remove(evict_key);
            end;
        end
        else
        begin
            m_phrase_context_pairs.AddOrSetValue(evict_key, count);
        end;
    end;
end;

procedure TncEngine.note_output_phrase_context(const committed_text: string);
var
    text_key: string;
    current_serial: Int64;
begin
    text_key := Trim(committed_text);
    if text_key = '' then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;

    if m_last_output_commit_text <> '' then
    begin
        track_phrase_context_key(m_last_output_commit_text + #1 + text_key, current_serial);
    end;

    if (m_prev_output_commit_text <> '') and (m_last_output_commit_text <> '') then
    begin
        track_phrase_context_key(
            m_prev_output_commit_text + #2 + m_last_output_commit_text + #1 + text_key,
            current_serial);
    end;

    m_prev_output_commit_text := m_last_output_commit_text;
    m_last_output_commit_text := text_key;
end;

procedure TncEngine.note_segment_path_context(const encoded_path: string);
var
    segments: TArray<string>;
    segment_text: string;
    current_serial: Int64;
    idx: Integer;
    segment_start: Integer;
    segment_idx: Integer;
begin
    if (encoded_path = '') or (m_phrase_context_pairs = nil) or (m_phrase_context_order = nil) then
    begin
        Exit;
    end;

    current_serial := m_session_commit_serial;
    if current_serial <= 0 then
    begin
        current_serial := 1;
    end;

    SetLength(segments, 0);
    segment_start := 1;
    for idx := 1 to Length(encoded_path) + 1 do
    begin
        if (idx <= Length(encoded_path)) and (encoded_path[idx] <> c_segment_path_separator) then
        begin
            Continue;
        end;

        segment_text := Copy(encoded_path, segment_start, idx - segment_start);
        segment_text := Trim(segment_text);
        if segment_text <> '' then
        begin
            segment_idx := Length(segments);
            SetLength(segments, segment_idx + 1);
            segments[segment_idx] := segment_text;
        end;
        segment_start := idx + 1;
    end;

    if Length(segments) <= 1 then
    begin
        Exit;
    end;

    for idx := 1 to High(segments) do
    begin
        track_phrase_context_key(segments[idx - 1] + #1 + segments[idx], current_serial);
        if idx >= 2 then
        begin
            track_phrase_context_key(
                segments[idx - 2] + #2 + segments[idx - 1] + #1 + segments[idx],
                current_serial);
        end;
    end;
end;

procedure TncEngine.record_context_pair(const left_text: string; const committed_text: string);
var
    context_variants: TArray<string>;
    evict_key: string;
    count: Integer;
    key: string;
    variant_idx: Integer;
begin
    if (left_text = '') or (committed_text = '') then
    begin
        Exit;
    end;

    context_variants := get_context_variants(left_text);
    if Length(context_variants) = 0 then
    begin
        Exit;
    end;

    if m_context_db_bonus_cache <> nil then
    begin
        m_context_db_bonus_cache.Clear;
        m_context_db_bonus_cache_key := '';
    end;

    if m_context_pairs = nil then
    begin
        Exit;
    end;

    if m_context_order = nil then
    begin
        Exit;
    end;

    for variant_idx := 0 to High(context_variants) do
    begin
        key := context_variants[variant_idx] + #1 + committed_text;
        if m_context_pairs.TryGetValue(key, count) then
        begin
            Inc(count);
            m_context_pairs.AddOrSetValue(key, count);
        end
        else
        begin
            m_context_pairs.Add(key, 1);
        end;

        m_context_order.Enqueue(key);
        while m_context_order.Count > c_context_history_limit do
        begin
            evict_key := m_context_order.Dequeue;
            if m_context_pairs.TryGetValue(evict_key, count) then
            begin
                Dec(count);
                if count <= 0 then
                begin
                    m_context_pairs.Remove(evict_key);
                end
                else
                begin
                    m_context_pairs.AddOrSetValue(evict_key, count);
                end;
            end;
        end;
    end;
end;

procedure TncEngine.toggle_input_mode;
begin
    if m_config.input_mode = im_chinese then
    begin
        m_config.input_mode := im_english;
    end
    else
    begin
        m_config.input_mode := im_chinese;
    end;

    reset;
end;

function TncEngine.process_key(const key_code: Word; const key_state: TncKeyState): Boolean;
var
    key_char: Char;
    display_key_char: Char;
    index: Integer;
    commit_text: string;
    page_size: Integer;
    page_offset: Integer;
    punct_char: Char;
    punct_text: string;
    candidate: TncCandidate;
    remaining_pinyin: string;
    selected_segment_path: string;

    procedure demote_weak_user_single_chain_before_selection;
    const
        c_low_evidence_user_weight = 8;
    var
        query_units: TncPinyinParseResult;
        query_key: string;
        expected_units: Integer;
        top_candidate_local: TncCandidate;
        best_idx_local: Integer;
        best_score_local: Integer;
        idx: Integer;
        picked_candidate_local: TncCandidate;
        candidate_units_local: Integer;
        comment_units_local: Integer;

        function is_low_evidence_user_single_chain_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            unit_key_local: string;
        begin
            Result := False;
            if (expected_units < 3) or (m_dictionary = nil) or
                (candidate_value.source <> cs_user) or
                (Trim(candidate_value.comment) <> '') or
                (Trim(candidate_value.text) = '') then
            begin
                Exit;
            end;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > c_low_evidence_user_weight) then
            begin
                Exit;
            end;
            if (m_dictionary <> nil) and
                m_dictionary.is_user_entry(query_key,
                Trim(candidate_value.text)) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_value.text));
            if Length(text_units_local) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(text_units_local) do
            begin
                if get_candidate_text_unit_count(text_units_local[unit_idx_local]) <> 1 then
                begin
                    Exit;
                end;
                unit_key_local := normalize_pinyin_text(query_units[unit_idx_local].text);
                if (unit_key_local = '') or
                    (not m_dictionary.single_char_matches_pinyin(unit_key_local,
                    Trim(text_units_local[unit_idx_local]))) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function is_strong_prefix_partial_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := False;
            if (Trim(candidate_value.comment) = '') or
                (not candidate_value.has_dict_weight) then
            begin
                Exit;
            end;
            candidate_units_local := get_candidate_text_unit_count(
                Trim(candidate_value.text));
            if candidate_units_local < 2 then
            begin
                Exit;
            end;
            comment_units_local := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(Trim(candidate_value.comment)));
            Result := (comment_units_local > 0) and
                (candidate_units_local + comment_units_local = expected_units);
        end;
    begin
        if Length(m_candidates) <= 1 then
        begin
            Exit;
        end;

        query_key := normalize_pinyin_text(m_composition_text);
        query_units := get_effective_compact_pinyin_syllables(query_key, True);
        expected_units := Length(query_units);
        top_candidate_local := m_candidates[0];
        if not is_low_evidence_user_single_chain_local(top_candidate_local) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_score_local := top_candidate_local.score;
        for idx := 1 to High(m_candidates) do
        begin
            if is_strong_prefix_partial_local(m_candidates[idx]) and
                (m_candidates[idx].score > best_score_local) then
            begin
                best_idx_local := idx;
                best_score_local := m_candidates[idx].score;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        picked_candidate_local := m_candidates[best_idx_local];
        for idx := best_idx_local downto 1 do
        begin
            m_candidates[idx] := m_candidates[idx - 1];
        end;
        m_candidates[0] := picked_candidate_local;
    end;

    function get_selected_candidate(out out_candidate: TncCandidate;
        const use_visible_selection: Boolean = False): Boolean;
    var
        visible_candidate: TncCandidate;
        visible_candidates: TncCandidateList;
        visible_index: Integer;
        mapped_index: Integer;

        function try_use_visible_prefix_partial_for_default_selection(
            const raw_candidate: TncCandidate; out out_visible_candidate: TncCandidate): Boolean;
        var
            visible_candidates: TncCandidateList;
            visible_candidate: TncCandidate;
            query_key: string;
            query_units: TncPinyinParseResult;
            prefix_units: Integer;
            tail_units: Integer;
            prefix_key: string;
            unit_idx: Integer;
            exact_results: TncCandidateList;

            function exact_text_exists_local(const key_value: string;
                const text_value: string; const expected_units: Integer): Boolean;
            var
                exact_text: string;
                exact_idx: Integer;
            begin
                Result := False;
                if (m_dictionary = nil) or (key_value = '') or
                    (text_value = '') then
                begin
                    Exit;
                end;

                if m_dictionary.is_base_entry(key_value, text_value) or
                    m_dictionary.is_user_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;

                if not m_dictionary.lookup_exact_full_pinyin(key_value,
                    exact_results) then
                begin
                    Exit;
                end;

                for exact_idx := 0 to High(exact_results) do
                begin
                    exact_text := Trim(exact_results[exact_idx].text);
                    if (Trim(exact_results[exact_idx].comment) = '') and
                        SameText(exact_text, text_value) and
                        (get_candidate_text_unit_count(exact_text) =
                        expected_units) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            FillChar(out_visible_candidate, SizeOf(out_visible_candidate), 0);
            if (m_dictionary = nil) or (m_page_index <> 0) or
                (m_selected_index <> 0) or (Trim(raw_candidate.comment) <> '') or
                (Trim(raw_candidate.text) = '') or
                (Pos('''', m_composition_text) > 0) then
            begin
                Exit;
            end;

            query_key := normalize_pinyin_text(m_composition_text);
            if (query_key = '') or (not is_full_pinyin_key(query_key)) then
            begin
                Exit;
            end;

            query_units := get_effective_compact_pinyin_syllables(query_key);
            if (Length(query_units) < 3) or (Length(query_units) > 5) or
                (get_candidate_text_unit_count(Trim(raw_candidate.text)) <>
                Length(query_units)) or
                exact_text_exists_local(query_key, Trim(raw_candidate.text),
                Length(query_units)) then
            begin
                Exit;
            end;

            visible_candidates := get_candidates;
            if Length(visible_candidates) = 0 then
            begin
                Exit;
            end;

            visible_candidate := visible_candidates[0];
            if Trim(visible_candidate.comment) = '' then
            begin
                Exit;
            end;

            prefix_units := get_candidate_text_unit_count(
                Trim(visible_candidate.text));
            tail_units := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(Trim(visible_candidate.comment)));
            if (prefix_units < 2) or (tail_units <= 0) or
                (prefix_units + tail_units <> Length(query_units)) then
            begin
                Exit;
            end;

            prefix_key := '';
            for unit_idx := 0 to prefix_units - 1 do
            begin
                prefix_key := prefix_key + normalize_pinyin_text(
                    query_units[unit_idx].text);
            end;
            if not exact_text_exists_local(prefix_key,
                Trim(visible_candidate.text), prefix_units) then
            begin
                Exit;
            end;

            out_visible_candidate := visible_candidate;
            Result := True;
        end;

        function visible_selection_cache_is_current: Boolean;
        begin
            Result := visible_candidates_cache_is_current(page_size);
        end;

        function get_visible_candidate_source_index(
            const visible_candidate_index: Integer): Integer;
        begin
            Result := -1;
            if (visible_candidate_index < 0) or
                (not visible_selection_cache_is_current) or
                (visible_candidate_index >=
                Length(m_visible_candidate_source_indices_cache)) then
            begin
                Exit;
            end;

            Result :=
                m_visible_candidate_source_indices_cache[visible_candidate_index];
            if (Result < 0) or (Result >= Length(m_candidates)) then
            begin
                Result := -1;
            end;
        end;

    begin
        normalize_page_and_selection;
        page_size := get_candidate_page_size;
        if use_visible_selection and visible_selection_cache_is_current then
        begin
            visible_index := m_selected_index;
            if (visible_index >= 0) and
                (visible_index < Length(m_visible_candidates_cache)) then
            begin
                out_candidate := m_visible_candidates_cache[visible_index];
                mapped_index := get_visible_candidate_source_index(visible_index);
                if mapped_index >= 0 then
                begin
                    index := mapped_index;
                end
                else
                begin
                    index := -1;
                end;
                Result := True;
                Exit;
            end;

            index := -1;
            Result := False;
            Exit;
        end;

        demote_weak_user_single_chain_before_selection;
        page_offset := m_page_index * page_size;
        index := page_offset + m_selected_index;
        if (index >= 0) and (index < Length(m_candidates)) then
        begin
            out_candidate := m_candidates[index];
            visible_index := m_selected_index;
            if use_visible_selection then
            begin
                visible_candidates := get_candidates;
            end
            else
            begin
                SetLength(visible_candidates, 0);
            end;
            if use_visible_selection and (visible_index >= 0) and
                (visible_index < Length(visible_candidates)) then
            begin
                visible_candidate := visible_candidates[visible_index];
                mapped_index := get_visible_candidate_source_index(visible_index);
                if mapped_index >= 0 then
                begin
                    index := mapped_index;
                end;
                if (not SameText(Trim(visible_candidate.text),
                    Trim(out_candidate.text))) or
                    (not SameText(Trim(visible_candidate.comment),
                    Trim(out_candidate.comment))) then
                begin
                    out_candidate := visible_candidate;
                    if mapped_index < 0 then
                    begin
                        index := -1;
                    end;
                    Result := True;
                    Exit;
                end;
            end;
            if try_use_visible_prefix_partial_for_default_selection(
                out_candidate, visible_candidate) then
            begin
                out_candidate := visible_candidate;
                mapped_index := get_visible_candidate_source_index(0);
                if mapped_index >= 0 then
                begin
                    index := mapped_index;
                end
                else
                begin
                    index := -1;
                end;
            end;
            Result := True;
            Exit;
        end;

        index := -1;
        if m_composition_display_text <> '' then
        begin
            out_candidate.text := m_composition_display_text;
        end
        else
        begin
            out_candidate.text := m_composition_text;
        end;
        out_candidate.comment := '';
        out_candidate.score := 0;
        out_candidate.source := cs_rule;
        out_candidate.has_dict_weight := False;
        out_candidate.dict_weight := 0;
        Result := True;
    end;

    procedure prefer_longer_default_partial_for_punctuation(
        var io_candidate: TncCandidate; var io_segment_path: string);
    var
        selected_text: string;
        selected_tail: string;
        selected_text_units: Integer;
        selected_tail_units: Integer;
        expected_total_units: Integer;
        idx: Integer;
        probe_limit: Integer;
        candidate_text: string;
        candidate_tail: string;
        candidate_text_units: Integer;
        candidate_tail_units: Integer;
        best_idx: Integer;
        best_score: Integer;
        lookup_results: TncCandidateList;
        lookup_idx: Integer;
        query_key: string;
    begin
        if (m_page_index <> 0) or (m_selected_index <> 0) or
            (Trim(io_candidate.comment) = '') then
        begin
            Exit;
        end;

        selected_text := Trim(io_candidate.text);
        selected_tail := normalize_pinyin_text(Trim(io_candidate.comment));
        selected_text_units := get_candidate_text_unit_count(selected_text);
        selected_tail_units :=
            get_effective_compact_pinyin_unit_count(selected_tail);
        if (selected_text = '') or (selected_text_units <= 0) or
            (selected_tail_units <= 0) then
        begin
            Exit;
        end;
        expected_total_units := selected_text_units + selected_tail_units;

        best_idx := -1;
        best_score := Low(Integer);
        probe_limit := Min(12, Length(m_candidates));
        for idx := 0 to probe_limit - 1 do
        begin
            candidate_tail :=
                normalize_pinyin_text(Trim(m_candidates[idx].comment));
            if candidate_tail = '' then
            begin
                Continue;
            end;

            candidate_text := Trim(m_candidates[idx].text);
            candidate_text_units := get_candidate_text_unit_count(candidate_text);
            candidate_tail_units :=
                get_effective_compact_pinyin_unit_count(candidate_tail);
            if (candidate_text_units <= selected_text_units) or
                (candidate_tail_units <= 0) or
                (candidate_text_units + candidate_tail_units <>
                expected_total_units) or
                (Copy(candidate_text, 1, Length(selected_text)) <>
                selected_text) then
            begin
                Continue;
            end;

            if (best_idx < 0) or
                (candidate_text_units > get_candidate_text_unit_count(
                Trim(m_candidates[best_idx].text))) or
                ((candidate_text_units = get_candidate_text_unit_count(
                Trim(m_candidates[best_idx].text))) and
                (m_candidates[idx].score > best_score)) then
            begin
                best_idx := idx;
                best_score := m_candidates[idx].score;
            end;
        end;

        if best_idx >= 0 then
        begin
            io_candidate := m_candidates[best_idx];
            io_segment_path := get_segment_path_for_candidate(io_candidate,
                best_idx);
            Exit;
        end;

        query_key := normalize_pinyin_text(m_composition_text);
        if (m_dictionary = nil) or (query_key = '') or
            (not m_dictionary.lookup(query_key, lookup_results)) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_score := Low(Integer);
        for lookup_idx := 0 to High(lookup_results) do
        begin
            candidate_tail :=
                normalize_pinyin_text(Trim(lookup_results[lookup_idx].comment));
            if candidate_tail = '' then
            begin
                Continue;
            end;

            candidate_text := Trim(lookup_results[lookup_idx].text);
            candidate_text_units := get_candidate_text_unit_count(candidate_text);
            candidate_tail_units :=
                get_effective_compact_pinyin_unit_count(candidate_tail);
            if (candidate_text_units <= selected_text_units) or
                (candidate_tail_units <= 0) or
                (candidate_text_units + candidate_tail_units <>
                expected_total_units) or
                (Copy(candidate_text, 1, Length(selected_text)) <>
                selected_text) then
            begin
                Continue;
            end;

            if (best_idx < 0) or
                (candidate_text_units > get_candidate_text_unit_count(
                Trim(lookup_results[best_idx].text))) or
                ((candidate_text_units = get_candidate_text_unit_count(
                Trim(lookup_results[best_idx].text))) and
                (lookup_results[lookup_idx].score > best_score)) then
            begin
                best_idx := lookup_idx;
                best_score := lookup_results[lookup_idx].score;
            end;
        end;

        if best_idx >= 0 then
        begin
            io_candidate := lookup_results[best_idx];
            io_segment_path := '';
        end;
    end;

    function is_generic_runtime_chain_selection(const selected: TncCandidate): Boolean;
    begin
        Result := False;
        if (m_runtime_chain_text = '') or selected.has_dict_weight or
            (selected.source <> cs_rule) or (selected.comment <> '') then
        begin
            Exit;
        end;
        Result := SameText(m_runtime_chain_text, selected.text);
    end;

    function is_generated_long_local_rerank_selection(
        const selected: TncCandidate): Boolean;
    begin
        Result := (m_long_local_rerank_generated_text <> '') and
            SameText(m_long_local_rerank_composition_text,
            m_composition_text) and
            SameText(m_long_local_rerank_lookup_key, m_last_lookup_key) and
            SameText(m_long_local_rerank_generated_text,
            Trim(selected.text)) and (Trim(selected.comment) = '');
    end;

    function is_nonlearnable_generated_selection(const selected: TncCandidate): Boolean;
    var
        text_units: Integer;
        idx: Integer;
        full_text: string;
        segment_path: string;
        unit_text: string;
        units: TArray<string>;

        function is_generated_short_exact_pair_selection_local: Boolean;
        var
            query_key_local: string;
            query_syllables_local: TncPinyinParseResult;
            path_local: string;
            parts_local: TArray<string>;
            first_units_local: Integer;
            second_units_local: Integer;
            first_key_local: string;
            second_key_local: string;
            syllable_idx_local: Integer;

            function segment_has_exact_entry_local(const key_value: string;
                const text_value: string;
                const expected_units_value: Integer): Boolean;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or (key_value = '') or
                    (text_value = '') or
                    (get_candidate_text_unit_count(text_value) <>
                    expected_units_value) then
                begin
                    Exit;
                end;
                if m_dictionary.is_base_entry(key_value, text_value) or
                    m_dictionary.is_user_entry(key_value, text_value) then
                begin
                    Exit(True);
                end;
                if not m_dictionary.lookup_exact_full_pinyin(key_value,
                    exact_results_local) then
                begin
                    Exit;
                end;
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    exact_text_local := Trim(
                        exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(exact_text_local, Trim(text_value)) and
                        (get_candidate_text_unit_count(exact_text_local) =
                        expected_units_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (selected.source <> cs_rule) or
                (Trim(selected.comment) <> '') or
                (get_candidate_text_unit_count(Trim(selected.text)) <> 3) then
            begin
                Exit;
            end;

            query_key_local := normalize_pinyin_text(m_last_lookup_key);
            query_syllables_local := get_effective_compact_pinyin_syllables(
                query_key_local);
            if (query_key_local = '') or (Length(query_syllables_local) <> 3) or
                segment_has_exact_entry_local(query_key_local,
                Trim(selected.text), 3) then
            begin
                Exit;
            end;

            path_local := get_segment_path_for_candidate(selected);
            if get_encoded_path_segment_count_local(path_local) <> 2 then
            begin
                Exit;
            end;
            parts_local := path_local.Split([c_segment_path_separator]);
            if Length(parts_local) <> 2 then
            begin
                Exit;
            end;
            first_units_local := get_candidate_text_unit_count(
                Trim(parts_local[0]));
            second_units_local := get_candidate_text_unit_count(
                Trim(parts_local[1]));
            if (first_units_local < 1) or (first_units_local > 2) or
                (second_units_local < 1) or (second_units_local > 2) or
                (first_units_local + second_units_local <> 3) or
                (not SameText(Trim(parts_local[0]) + Trim(parts_local[1]),
                Trim(selected.text))) then
            begin
                Exit;
            end;

            first_key_local := '';
            for syllable_idx_local := 0 to first_units_local - 1 do
            begin
                first_key_local := first_key_local + normalize_pinyin_text(
                    query_syllables_local[syllable_idx_local].text);
            end;
            second_key_local := '';
            for syllable_idx_local := first_units_local to 2 do
            begin
                second_key_local := second_key_local + normalize_pinyin_text(
                    query_syllables_local[syllable_idx_local].text);
            end;
            Result := segment_has_exact_entry_local(first_key_local,
                Trim(parts_local[0]), first_units_local) and
                segment_has_exact_entry_local(second_key_local,
                Trim(parts_local[1]), second_units_local);
        end;
    begin
        Result := False;
        if (selected.display_kind = cdk_lm_compound) and
            (Trim(selected.comment) = '') then
        begin
            Exit(True);
        end;
        if is_generated_short_exact_pair_selection_local then
        begin
            Exit(True);
        end;
        if not is_generic_runtime_chain_selection(selected) then
        begin
            Exit;
        end;

        segment_path := get_segment_path_for_candidate(selected);
        if segment_path <> '' then
        begin
            Exit(False);
        end;

        full_text := selected.text;
        if m_confirmed_text <> '' then
        begin
            full_text := m_confirmed_text + full_text;
        end;

        text_units := get_candidate_text_unit_count(full_text);
        if text_units >= 4 then
        begin
            units := split_text_units(full_text);
            Result := Length(units) = text_units;
            if Result then
            begin
                for idx := 0 to High(units) do
                begin
                    unit_text := Trim(units[idx]);
                    if (unit_text = '') or (Length(unit_text) <> 1) or (Ord(unit_text[1]) <= $7F) then
                    begin
                        Result := False;
                        Break;
                    end;
                end;
            end;
            if Result then
            begin
                Exit(False);
            end;
        end;

        Result := True;
    end;

    function is_compact_ascii_pinyin(const value: string): Boolean;
    var
        idx: Integer;
        ch: Char;
    begin
        Result := value <> '';
        if not Result then
        begin
            Exit;
        end;

        for idx := 1 to Length(value) do
        begin
            ch := value[idx];
            if not (((ch >= 'a') and (ch <= 'z')) or ((ch >= 'A') and (ch <= 'Z'))) then
            begin
                Result := False;
                Exit;
            end;
        end;
    end;

    function get_display_remaining_pinyin_for_partial(const selected_text: string;
        const remaining_pinyin_value: string): string;
    var
        display_text: string;
        normalized_remaining: string;
        suffix_text: string;
        parser: TncPinyinParser;
        display_syllables: TncPinyinParseResult;
        consumed_units: Integer;
        suffix_start: Integer;
        composition_key: string;
        candidate_tail: string;
        candidate_prefix: string;
        aligned_tail: string;
        total_units: Integer;
        prefix_units: Integer;
        tail_units: Integer;
        candidate_idx: Integer;
    begin
        Result := remaining_pinyin_value;
        normalized_remaining := normalize_pinyin_text(remaining_pinyin_value);
        if normalized_remaining = '' then
        begin
            Exit;
        end;

        consumed_units := get_candidate_text_unit_count(Trim(selected_text));
        composition_key := normalize_pinyin_text(m_composition_text);
        if (not is_shuangpin_input) and
            (Pos('''', m_composition_display_text) <= 0) and
            (composition_key <> '') and (consumed_units > 0) then
        begin
            total_units := get_effective_compact_pinyin_unit_count(
                composition_key);
            aligned_tail := '';
            for candidate_idx := 0 to High(m_candidates) do
            begin
                if not SameText(Trim(m_candidates[candidate_idx].text),
                    Trim(selected_text)) then
                begin
                    Continue;
                end;
                candidate_tail := normalize_pinyin_text(
                    Trim(m_candidates[candidate_idx].comment));
                if (candidate_tail = '') or
                    (Length(candidate_tail) >= Length(composition_key)) or
                    (not SameText(Copy(composition_key,
                    Length(composition_key) - Length(candidate_tail) + 1,
                    Length(candidate_tail)), candidate_tail)) then
                begin
                    Continue;
                end;
                candidate_prefix := Copy(composition_key, 1,
                    Length(composition_key) - Length(candidate_tail));
                prefix_units := get_effective_compact_pinyin_unit_count(
                    candidate_prefix);
                tail_units := get_effective_compact_pinyin_unit_count(
                    candidate_tail);
                if (prefix_units = consumed_units) and
                    (tail_units = total_units - consumed_units) and
                    (Length(candidate_tail) > Length(aligned_tail)) then
                begin
                    aligned_tail := candidate_tail;
                end;
            end;
            if aligned_tail <> '' then
            begin
                Result := aligned_tail;
                normalized_remaining := aligned_tail;
            end;
        end;

        display_text := m_composition_display_text;
        if display_text = '' then
        begin
            display_text := m_composition_text;
        end;
        if display_text = '' then
        begin
            Exit;
        end;

        if Pos('''', display_text) <= 0 then
        begin
            // Lookup uses canonical spelling; partial selection must retain
            // the user's equal-length alias in the unconsumed input.
            if (not is_shuangpin_input) and
                (Length(normalized_remaining) < Length(display_text)) then
            begin
                suffix_text := Copy(display_text,
                    Length(display_text) - Length(normalized_remaining) + 1,
                    Length(normalized_remaining));
                if SameText(normalize_pinyin_text(suffix_text), normalized_remaining) then
                    Result := suffix_text;
            end;
            Exit;
        end;

        if consumed_units <= 0 then
        begin
            Exit;
        end;

        parser := TncPinyinParser.Create;
        try
            display_syllables := parser.parse(display_text);
        finally
            parser.Free;
        end;

        if consumed_units >= Length(display_syllables) then
        begin
            Exit;
        end;

        suffix_start := display_syllables[consumed_units].start_index + 1;
        if (suffix_start < 1) or (suffix_start > Length(display_text)) then
        begin
            Exit;
        end;

        suffix_text := Copy(display_text, suffix_start,
            Length(display_text) - suffix_start + 1);
        if SameText(normalize_pinyin_text(suffix_text), normalized_remaining) then
        begin
            Result := suffix_text;
        end;
    end;

    function try_apply_trailing_pinyin_candidate(const candidate_text: string): Boolean;
    var
        head_text: string;
        tail_pinyin: string;
        normalized_pinyin: string;
        idx: Integer;
        tail_len: Integer;
        ch: Char;
        head_has_non_ascii: Boolean;
        function is_ascii_letter(const value: Char): Boolean;
        begin
            Result := (value >= 'a') and (value <= 'z') or (value >= 'A') and (value <= 'Z');
        end;
    begin
        Result := False;
        head_text := '';
        tail_pinyin := '';
        if candidate_text = '' then
        begin
            Exit;
        end;

        idx := Length(candidate_text);
        while idx > 0 do
        begin
            ch := candidate_text[idx];
            if not is_ascii_letter(ch) then
            begin
                Break;
            end;
            Dec(idx);
        end;

        if idx = Length(candidate_text) then
        begin
            Exit;
        end;

        head_text := Copy(candidate_text, 1, idx);
        tail_pinyin := Copy(candidate_text, idx + 1, Length(candidate_text) - idx);
        if (head_text = '') or (tail_pinyin = '') then
        begin
            Exit;
        end;

        head_has_non_ascii := False;
        for idx := 1 to Length(head_text) do
        begin
            if Ord(head_text[idx]) > $7F then
            begin
                head_has_non_ascii := True;
                Break;
            end;
        end;
        if not head_has_non_ascii then
        begin
            Exit;
        end;

        normalized_pinyin := normalize_pinyin_text(m_composition_text);
        tail_len := Length(tail_pinyin);
        if (tail_len > 0) and (tail_len <= Length(normalized_pinyin)) then
        begin
            if SameText(Copy(normalized_pinyin, Length(normalized_pinyin) - tail_len + 1, tail_len), tail_pinyin) then
            begin
                tail_pinyin := get_display_remaining_pinyin_for_partial(
                    head_text, tail_pinyin);
                apply_partial_commit(head_text, tail_pinyin, '', False,
                    get_shuangpin_remaining_input_code_for_partial(head_text));
                Result := True;
            end;
        end;
    end;

    function apply_candidate_selection(const selected: TncCandidate;
        const selected_candidate_index: Integer = -1;
        const explicit_selection: Boolean = False;
        const manual_selection: Boolean = False): Boolean;
    var
        allow_learning: Boolean;
        segment_path: string;
        top_candidate: TncCandidate;
        top_score: Integer;
        selected_score: Integer;
        top_confidence_rank: Integer;
        selected_confidence_rank: Integer;
        top_segment_path: string;
        partial_remaining_pinyin: string;
        generated_long_local_rerank_selection: Boolean;
        nonlearnable_generated_selection: Boolean;
        short_exact_lexicon_completion_selection: Boolean;

        function split_encoded_path(const encoded_path: string): TArray<string>;
        var
            idx: Integer;
            start_idx: Integer;
            segment_text: string;
            count: Integer;
        begin
            SetLength(Result, 0);
            start_idx := 1;
            for idx := 1 to Length(encoded_path) + 1 do
            begin
                if (idx <= Length(encoded_path)) and (encoded_path[idx] <> c_segment_path_separator) then
                begin
                    Continue;
                end;

                segment_text := Trim(Copy(encoded_path, start_idx, idx - start_idx));
                if segment_text <> '' then
                begin
                    count := Length(Result);
                    SetLength(Result, count + 1);
                    Result[count] := segment_text;
                end;
                start_idx := idx + 1;
            end;
        end;

        procedure record_divergent_query_path_prefix_penalties(const selected_path: string;
            const wrong_path: string);
        var
            selected_segments: TArray<string>;
            wrong_segments: TArray<string>;
            shared_count: Integer;
            prefix_idx: Integer;
            prefix_path: string;
            prefix_query: string;
        begin
            if (m_dictionary = nil) or (selected_path = '') or (wrong_path = '') then
            begin
                Exit;
            end;
            if (get_encoded_path_segment_count_local(selected_path) <= 1) or
                (get_encoded_path_segment_count_local(wrong_path) <= 1) then
            begin
                Exit;
            end;

            selected_segments := split_encoded_path(selected_path);
            wrong_segments := split_encoded_path(wrong_path);
            if (Length(selected_segments) = 0) or (Length(wrong_segments) <= 2) then
            begin
                Exit;
            end;

            shared_count := 0;
            while (shared_count < Length(selected_segments)) and (shared_count < Length(wrong_segments)) and
                SameText(Trim(selected_segments[shared_count]), Trim(wrong_segments[shared_count])) do
            begin
                Inc(shared_count);
            end;

            prefix_path := '';
            for prefix_idx := 0 to High(wrong_segments) - 1 do
            begin
                if prefix_path <> '' then
                begin
                    prefix_path := prefix_path + c_segment_path_separator;
                end;
                prefix_path := prefix_path + wrong_segments[prefix_idx];
                if (prefix_idx + 1 < 2) or (prefix_idx + 1 <= shared_count) then
                begin
                    Continue;
                end;

                prefix_query := get_query_prefix_for_segment_path(prefix_path);
                if prefix_query <> '' then
                begin
                    note_session_query_path_penalty(prefix_query, prefix_path);
                    m_dictionary.record_query_segment_path_penalty(prefix_query, prefix_path);
                end;
            end;
        end;

        function is_generated_short_particle_tail_selection(
            const selected_value: TncCandidate): Boolean;
        var
            query_key_local: string;
            text_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_key_local: string;
            head_text_local: string;
            text_units_local: TArray<string>;
            head_unit_count_local: Integer;
            unit_idx_local: Integer;

            function try_get_tail_local: Boolean;
            begin
                Result := try_get_short_particle_tail_query_parts(
                    query_key_local, tail_key_local, tail_text_local,
                    head_key_local);
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (selected_value.source <> cs_rule) or
                (Trim(selected_value.comment) <> '') then
            begin
                Exit;
            end;

            query_key_local := normalize_pinyin_text(m_last_lookup_key);
            text_key_local := Trim(selected_value.text);
            if (query_key_local = '') or (text_key_local = '') or
                (not is_full_pinyin_key(query_key_local)) or
                m_dictionary.is_base_entry(query_key_local, text_key_local) or
                m_dictionary.is_user_entry(query_key_local, text_key_local) or
                (not try_get_tail_local) then
            begin
                Exit;
            end;

            head_unit_count_local :=
                get_effective_compact_pinyin_unit_count(head_key_local);
            if (head_key_local = '') or (head_unit_count_local < 2) or
                (head_unit_count_local > 3) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(text_key_local);
            if (Length(text_units_local) <> head_unit_count_local + 1) or
                (not SameText(Trim(text_units_local[High(text_units_local)]),
                tail_text_local)) then
            begin
                Exit;
            end;

            head_text_local := '';
            for unit_idx_local := 0 to High(text_units_local) - 1 do
            begin
                head_text_local := head_text_local + text_units_local[unit_idx_local];
            end;

            Result := (head_text_local <> '') and
                (m_dictionary.is_base_entry(head_key_local, head_text_local) or
                m_dictionary.is_user_entry(head_key_local, head_text_local));
        end;
    begin
        segment_path := get_segment_path_for_candidate(selected, selected_candidate_index);
        if (selected.comment <> '') and is_compact_ascii_pinyin(selected.comment) then
        begin
            if segment_path = '' then
            begin
                segment_path := infer_segment_path_for_selected_text(selected.text);
            end;
            partial_remaining_pinyin := get_display_remaining_pinyin_for_partial(
                selected.text, selected.comment);
            apply_partial_commit(selected.text, partial_remaining_pinyin, segment_path,
                explicit_selection,
                get_shuangpin_remaining_input_code_for_partial(selected.text),
                manual_selection and ((not is_fuzzy_pinyin_active) or
                (selected.fuzzy_cost <= 0)),
                is_fuzzy_pinyin_active and (selected.fuzzy_cost > 0));
            Result := True;
            Exit;
        end;

        if try_apply_trailing_pinyin_candidate(selected.text) then
        begin
            Result := True;
            Exit;
        end;

        generated_long_local_rerank_selection :=
            is_generated_long_local_rerank_selection(selected);
        nonlearnable_generated_selection :=
            is_nonlearnable_generated_selection(selected);
        short_exact_lexicon_completion_selection :=
            is_current_short_exact_lexicon_completion(m_last_lookup_key,
            selected.text) and
            (Trim(selected.comment) = '');
        if (segment_path = '') and (selected.comment = '') and
            (not generated_long_local_rerank_selection) then
        begin
            segment_path := infer_segment_path_for_selected_text(selected.text);
        end;

        allow_learning := ((not is_fuzzy_pinyin_active) or
            (selected.fuzzy_cost <= 0)) and
            (not generated_long_local_rerank_selection) and
            (not nonlearnable_generated_selection) and
            (not short_exact_lexicon_completion_selection);
        if allow_learning and is_generated_short_particle_tail_selection(selected) then
        begin
            allow_learning := False;
        end;
        if allow_learning and has_competing_exact_phrase_candidate(selected.text) and
            is_problematic_single_char_chain_candidate_for_query(m_last_lookup_key, selected) then
        begin
            allow_learning := False;
        end;
        if ((not is_fuzzy_pinyin_active) or
            (selected.fuzzy_cost <= 0)) and (not allow_learning) and
            explicit_selection and (m_dictionary <> nil) and
            (Trim(selected.comment) = '') and (get_candidate_text_unit_count(selected.text) = 1) and
            (m_last_lookup_key <> '') and m_dictionary.single_char_matches_pinyin(
            m_last_lookup_key, selected.text) then
        begin
            // A user explicitly picking a matching single character for an
            // exact pinyin query is intentional and must survive host restarts.
            allow_learning := True;
        end;

        if ((not is_fuzzy_pinyin_active) or
            (selected.fuzzy_cost <= 0)) and (m_last_lookup_key <> '') and
            (selected_candidate_index > 0) and
            (Length(m_candidates) > 0) and
            (not short_exact_lexicon_completion_selection) then
        begin
            top_candidate := m_candidates[0];
            if (top_candidate.comment = '') and (selected.comment = '') and
                    (top_candidate.text <> '') and (selected.text <> '') and
                    (top_candidate.text <> selected.text) and
                    (not generated_long_local_rerank_selection) and
                    (not nonlearnable_generated_selection) then
            begin
                top_score := get_rank_score(top_candidate);
                selected_score := get_rank_score(selected);
                top_confidence_rank := get_candidate_confidence_rank(top_candidate);
                selected_confidence_rank := get_candidate_confidence_rank(selected);
                if (selected_confidence_rank < top_confidence_rank) or
                    ((top_score - selected_score) <= 160) or
                    is_runtime_chain_candidate(top_candidate) or
                    ((top_candidate.source = cs_rule) and (not top_candidate.has_dict_weight) and
                    (not is_runtime_common_pattern_candidate(top_candidate)) and
                    (not is_runtime_redup_candidate(top_candidate))) then
                begin
                    if m_dictionary <> nil then
                    begin
                        m_dictionary.record_candidate_penalty(m_last_lookup_key, top_candidate.text);
                        top_segment_path := get_segment_path_for_candidate(top_candidate, 0);
                        if (top_segment_path = '') and (top_candidate.comment = '') then
                        begin
                            top_segment_path := infer_segment_path_for_selected_text(top_candidate.text);
                        end;
                        if (segment_path <> '') and (top_segment_path <> '') and
                            (segment_path <> top_segment_path) and
                            (get_encoded_path_segment_count_local(segment_path) > 1) and
                            (get_encoded_path_segment_count_local(top_segment_path) > 1) then
                        begin
                            record_divergent_query_path_prefix_penalties(segment_path, top_segment_path);
                            note_session_query_path_penalty(m_last_lookup_key, top_segment_path);
                            m_dictionary.record_query_segment_path_penalty(m_last_lookup_key, top_segment_path);
                        end;
                    end;
                end;
            end;
        end;

        set_pending_commit(selected.text, '', allow_learning,
            segment_path, explicit_selection, '',
            manual_selection and ((not is_fuzzy_pinyin_active) or
            (selected.fuzzy_cost <= 0)),
            is_fuzzy_pinyin_active and (selected.fuzzy_cost > 0), '',
            nonlearnable_generated_selection or
            short_exact_lexicon_completion_selection);
        Result := True;
    end;
begin
    Result := False;
    m_composition_built_incrementally := True;
    if nc_shortcut_matches(m_config.shortcuts.input_mode_toggle, key_code, key_state) then
    begin
        toggle_input_mode;
        Result := True;
        Exit;
    end;

    if nc_shortcut_matches(m_config.shortcuts.full_width_toggle, key_code, key_state) then
    begin
        m_config.full_width_mode := not m_config.full_width_mode;
        Result := True;
        Exit;
    end;

    if nc_shortcut_matches(m_config.shortcuts.punctuation_toggle, key_code, key_state) then
    begin
        m_config.punctuation_full_width := not m_config.punctuation_full_width;
        Result := True;
        Exit;
    end;

    if key_state.ctrl_down or key_state.alt_down then
    begin
        Exit(False);
    end;

    if m_config.input_mode = im_english then
    begin
        if get_direct_ascii_commit_text(key_code, key_state, commit_text) then
        begin
            set_pending_commit(commit_text);
            Exit(True);
        end;
        Exit(False);
    end;

    if m_composition_text <> '' then
    begin
        normalize_page_and_selection;
    end;

    if is_alpha_key(key_code, key_state, key_char, display_key_char) then
    begin
        clear_pending_commit;
        if is_shuangpin_input then
        begin
            m_composition_display_text := m_composition_display_text + display_key_char;
            rebuild_shuangpin_composition_from_input_code;
        end
        else
        begin
            m_composition_text := m_composition_text + key_char;
            m_composition_display_text := m_composition_display_text + display_key_char;
        end;
        build_candidates;
        Result := True;
        Exit;
    end;

    if (key_code = VK_OEM_1) and (not key_state.shift_down) and
        (not key_state.ctrl_down) and (not key_state.alt_down) and
        nc_shuangpin_accepts_semicolon(m_config.pinyin_input_scheme,
        m_composition_display_text) then
    begin
        clear_pending_commit;
        m_composition_display_text := m_composition_display_text + ';';
        rebuild_shuangpin_composition_from_input_code;
        build_candidates;
        Result := True;
        Exit;
    end;

    if (m_one_key_completion.text <> '') and
        nc_one_key_completion_matches(m_config.one_key_completion_key,
        key_code, key_state) then
    begin
        Result := accept_one_key_completion;
        Exit;
    end;

    if (m_config.one_key_completion_key = ock_backtick) and
        (m_composition_text <> '') and
        nc_one_key_completion_matches(m_config.one_key_completion_key,
        key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if (key_code = VK_OEM_3) and (m_composition_text <> '') and (not key_state.shift_down) and
        (not key_state.ctrl_down) and (not key_state.alt_down) then
    begin
        reset;
        Result := True;
        Exit;
    end;

    if ((m_composition_text <> '') or (m_confirmed_text <> '') or
        (Length(m_candidates) > 0)) and (not key_state.ctrl_down) and
        (not key_state.alt_down) then
    begin
        if nc_candidate_page_key_matches_next(
            m_config.candidate_page_key_scheme, key_code, key_state) then
        begin
            next_page;
            Result := True;
            Exit;
        end;

        if nc_candidate_page_key_matches_previous(
            m_config.candidate_page_key_scheme, key_code, key_state) then
        begin
            prev_page;
            Result := True;
            Exit;
        end;
    end;

    if get_punctuation_char(key_code, key_state, punct_char) then
    begin
        if (key_code <> VK_OEM_7) or (m_composition_text = '') then
        begin
            if (key_code = VK_ADD) or (key_code = VK_SUBTRACT) or
                (key_code = VK_MULTIPLY) then
            begin
                punct_text := punct_char;
            end
            else
            begin
                punct_text := map_punctuation_char(punct_char);
            end;
            if (m_composition_text <> '') or (m_confirmed_text <> '') then
            begin
                commit_text := punct_text;
                if m_composition_text <> '' then
                begin
                    get_selected_candidate(candidate);
                    selected_segment_path := get_segment_path_for_candidate(candidate,
                        index);
                    prefer_longer_default_partial_for_punctuation(candidate,
                        selected_segment_path);
                    if candidate.comment <> '' then
                    begin
                        remaining_pinyin := get_display_remaining_pinyin_for_partial(
                            candidate.text, candidate.comment);
                        apply_partial_commit(candidate.text, remaining_pinyin,
                            selected_segment_path, False,
                            get_shuangpin_remaining_input_code_for_partial(
                            candidate.text), False,
                            is_fuzzy_pinyin_active and
                            (candidate.fuzzy_cost > 0));
                        set_pending_commit(punct_text, remaining_pinyin, False);
                    end
                    else
                    begin
                        commit_text := candidate.text + punct_text;
                        set_pending_commit(commit_text, '',
                            ((not is_fuzzy_pinyin_active) or
                            (candidate.fuzzy_cost <= 0)) and
                            (not is_nonlearnable_generated_selection(candidate)),
                            selected_segment_path, False, '', False,
                            is_fuzzy_pinyin_active and
                            (candidate.fuzzy_cost > 0), candidate.text,
                            is_nonlearnable_generated_selection(candidate));
                    end;
                    Result := True;
                    Exit;
                end;

                set_pending_commit(commit_text);
                Result := True;
                Exit;
            end;

            if get_effective_punctuation_full_width or m_config.full_width_mode then
            begin
                set_pending_commit(punct_text);
                Result := True;
                Exit;
            end;

            Exit(False);
        end;
    end;

    case key_code of
        VK_BACK:
            begin
                // When there is confirmed segmented prefix, Backspace should first
                // rollback that prefix to pinyin so users can reselect without
                // deleting the entire remaining tail.
                if (m_confirmed_segments <> nil) and (m_confirmed_segments.Count > 0) then
                begin
                    Result := rollback_last_segment;
                end;
                if (not Result) and (m_composition_text <> '') then
                begin
                    clear_pending_commit;
                    if is_shuangpin_input then
                    begin
                        if m_composition_display_text <> '' then
                        begin
                            Delete(m_composition_display_text,
                                Length(m_composition_display_text), 1);
                        end;
                        rebuild_shuangpin_composition_from_input_code;
                    end
                    else
                    begin
                        Delete(m_composition_text, Length(m_composition_text), 1);
                        if m_composition_display_text <> '' then
                        begin
                            Delete(m_composition_display_text,
                                Length(m_composition_display_text), 1);
                        end;
                    end;
                    build_candidates;
                    Result := True;
                end;
            end;
        VK_OEM_7:
            begin
                if m_composition_text <> '' then
                begin
                    clear_pending_commit;
                    if is_shuangpin_input then
                    begin
                        if (m_composition_display_text <> '') and
                            (m_composition_display_text[Length(m_composition_display_text)] <> '''') then
                        begin
                            m_composition_display_text := m_composition_display_text + '''';
                            rebuild_shuangpin_composition_from_input_code;
                            build_candidates;
                        end;
                    end
                    else if m_composition_text[Length(m_composition_text)] <> '''' then
                    begin
                        m_composition_text := m_composition_text + '''';
                        m_composition_display_text := m_composition_display_text + '''';
                        build_candidates;
                    end;

                    Result := True;
                end;
            end;
        VK_ESCAPE:
            begin
                if (m_composition_text <> '') or (m_confirmed_text <> '') or (Length(m_candidates) > 0) or
                    m_has_pending_commit then
                begin
                    reset;
                    Result := True;
                end;
            end;
        VK_SPACE:
            begin
                if m_composition_text <> '' then
                begin
                    if get_selected_candidate(candidate, True) then
                    begin
                        // Space confirms the selected candidate with the same
                        // learning semantics as its numeric selection key.
                        Result := apply_candidate_selection(candidate, index,
                            True, True);
                    end;
                end
                else if m_confirmed_text <> '' then
                begin
                    set_pending_commit('');
                    Result := True;
                end;
            end;
        VK_RETURN:
            begin
                if m_composition_text <> '' then
                begin
                    // Enter should commit raw input text instead of selecting candidate.
                    set_pending_commit(get_raw_composition_commit_text);
                    Result := True;
                end
                else if m_confirmed_text <> '' then
                begin
                    set_pending_commit('');
                    Result := True;
                end;
            end;
        VK_PRIOR:
            begin
                if (m_composition_text <> '') or (m_confirmed_text <> '') or
                    (Length(m_candidates) > 0) then
                begin
                    if (m_composition_text <> '') or (Length(m_candidates) > 0) then
                    begin
                        prev_page;
                    end;
                    Result := True;
                end;
            end;
        VK_NEXT:
            begin
                if (m_composition_text <> '') or (m_confirmed_text <> '') or
                    (Length(m_candidates) > 0) then
                begin
                    if (m_composition_text <> '') or (Length(m_candidates) > 0) then
                    begin
                        next_page;
                    end;
                    Result := True;
                end;
            end;
        VK_LEFT, VK_UP:
            begin
                if (m_composition_text <> '') and (not key_state.shift_down) and (not key_state.ctrl_down) and
                    (not key_state.alt_down) then
                begin
                    page_size := get_candidate_page_size;
                    if page_size <= 0 then
                    begin
                        page_size := c_default_page_size;
                    end;

                    normalize_page_and_selection;
                    if m_selected_index > 0 then
                    begin
                        m_candidate_navigation_started := True;
                        Dec(m_selected_index);
                    end
                    else if m_page_index > 0 then
                    begin
                        m_candidate_navigation_started := True;
                        Dec(m_page_index);
                        index := get_current_page_candidate_count(page_size);
                        if index > 0 then
                        begin
                            m_selected_index := index - 1;
                        end
                        else
                        begin
                            m_selected_index := 0;
                        end;
                    end;
                    normalize_page_and_selection;
                    Result := True;
                end;
            end;
        VK_RIGHT, VK_DOWN:
            begin
                if (m_composition_text <> '') and (not key_state.shift_down) and (not key_state.ctrl_down) and
                    (not key_state.alt_down) then
                begin
                    page_size := get_candidate_page_size;
                    if page_size <= 0 then
                    begin
                        page_size := c_default_page_size;
                    end;

                    normalize_page_and_selection;
                    index := get_current_page_candidate_count(page_size);

                    if (index > 0) and (m_selected_index < index - 1) then
                    begin
                        m_candidate_navigation_started := True;
                        Inc(m_selected_index);
                    end
                    else if m_page_index < get_page_count_internal(page_size) - 1 then
                    begin
                        m_candidate_navigation_started := True;
                        Inc(m_page_index);
                        m_selected_index := 0;
                        normalize_page_and_selection;
                    end;
                    Result := True;
                end;
            end;
        Ord('1')..Ord('9'):
            begin
                if m_composition_text <> '' then
                begin
                    page_size := get_candidate_page_size;
                    index := key_code - Ord('1');
                    if (index < page_size) and
                        (index < get_current_page_candidate_count(page_size)) then
                    begin
                        m_selected_index := index;
                        if get_selected_candidate(candidate, True) then
                        begin
                            Result := apply_candidate_selection(candidate, index,
                                True, True);
                        end;
                    end
                    else
                    begin
                        set_pending_commit(get_raw_composition_commit_text);
                        Result := True;
                    end;
                end
                else if m_confirmed_text <> '' then
                begin
                    set_pending_commit(Char(key_code));
                    Result := True;
                end;
            end;
    end;
end;

function TncEngine.get_candidates: TncCandidateList;
var
    page_size: Integer;
    visible_page_size: Integer;
    page_count: Integer;
    start_index: Integer;
    end_index: Integer;
    normalized_pinyin: string;
    candidate: TncCandidate;
    head_text: string;
    tail_pinyin: string;
    user_entry_query: string;
    syllables: TncPinyinParseResult;
    relaxed_single_syllables: TncPinyinParseResult;
    expected_units: Integer;
    entry_top_candidate: TncCandidate;
    entry_top_path: string;
    has_entry_pathful_complete_top: Boolean;
    display_phase_tick: UInt64;
    long_display_fast_path: Boolean;
    short_exact_query_mode: Boolean;
    short_exact_predictive_prefix_only_mode: Boolean;
    short_nocontext_promoted_exact_text: string;
    short_nocontext_promoted_exact_lead: Integer;
    short_context_promoted_exact_text: string;
    short_context_promoted_exact_lead: Integer;
    promoted_repeated_initial_count: Integer;
    repeated_initial_display_source_candidates: TncCandidateList;
    explicit_apostrophe_entry_top_partial_candidate: TncCandidate;
    has_explicit_apostrophe_entry_top_partial_candidate: Boolean;
    visible_ranked_top_path: string;
    visible_ranked_source_index: Integer;
    relaxed_boundary_cache_ready: Boolean;
    relaxed_boundary_cache_found: Boolean;
    relaxed_boundary_cache_head_key: string;
    relaxed_boundary_cache_tail_key: string;
    relaxed_boundary_cache_head_units: Integer;
    relaxed_boundary_cache_tail_units: Integer;
    display_query_key_cache: TArray<string>;
    display_normalized_syllable_cache: TArray<string>;
    candidate_idx: Integer;
    cached_page_sources: TArray<Integer>;
    umlaut_raw_prefixes_ready: Boolean;
    umlaut_raw_prefixes: TncCandidateList;

    function is_short_repeated_initial_display_query_local: Boolean;
    var
        normalized_query_local: string;
        idx_local: Integer;
    begin
        Result := False;
        normalized_query_local := normalize_pinyin_text(normalized_pinyin);
        if (Length(normalized_query_local) < 2) or
            (Length(normalized_query_local) > 3) then
        begin
            Exit;
        end;

        for idx_local := 1 to Length(normalized_query_local) do
        begin
            if (not CharInSet(normalized_query_local[idx_local], ['b', 'p',
                'm', 'f', 'd', 't', 'n', 'l', 'g', 'k', 'h', 'j', 'q',
                'x', 'z', 'c', 's', 'r', 'y', 'w'])) or
                ((idx_local > 1) and
                (normalized_query_local[idx_local] <> normalized_query_local[1])) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure capture_repeated_initial_display_source_candidates;
    begin
        SetLength(repeated_initial_display_source_candidates, 0);
        if (Length(m_candidates) <= 0) or
            (not is_short_repeated_initial_display_query_local) then
        begin
            Exit;
        end;

        repeated_initial_display_source_candidates :=
            Copy(m_candidates, 0, Length(m_candidates));
    end;

    function get_display_raw_query_text_local: string;
    begin
        if m_composition_display_text <> '' then
        begin
            Result := m_composition_display_text;
        end
        else
        begin
            Result := m_composition_text;
        end;
    end;

    procedure capture_explicit_apostrophe_entry_top_partial_candidate;
    begin
        has_explicit_apostrophe_entry_top_partial_candidate := False;
        FillChar(explicit_apostrophe_entry_top_partial_candidate,
            SizeOf(explicit_apostrophe_entry_top_partial_candidate), 0);
        if (Pos('''', get_display_raw_query_text_local) <= 0) or
            (Length(m_candidates) <= 0) or
            (Trim(m_candidates[0].comment) = '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) < 2) then
        begin
            Exit;
        end;

        explicit_apostrophe_entry_top_partial_candidate := m_candidates[0];
        has_explicit_apostrophe_entry_top_partial_candidate := True;
    end;

    function get_display_query_syllables_local(
        const raw_query_text: string): TncPinyinParseResult;
    var
        parser_local: TncPinyinParser;
        tokens_local: TArray<string>;
        token_idx_local: Integer;
        part_idx_local: Integer;
        out_idx_local: Integer;
        compact_offset_local: Integer;
        token_text_local: string;
        token_parts_local: TncPinyinParseResult;
    begin
        if Pos('''', raw_query_text) <= 0 then
        begin
            Exit(get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(raw_query_text)));
        end;

        SetLength(Result, 0);
        parser_local := TncPinyinParser.Create;
        try
            tokens_local := raw_query_text.Split(['''']);
            compact_offset_local := 0;
            for token_idx_local := 0 to High(tokens_local) do
            begin
                token_text_local := normalize_pinyin_text(tokens_local[token_idx_local]);
                if token_text_local = '' then
                begin
                    SetLength(Result, 0);
                    Exit;
                end;

                token_parts_local := parser_local.parse(token_text_local);
                if Length(token_parts_local) = 0 then
                begin
                    SetLength(Result, 0);
                    Exit;
                end;

                out_idx_local := Length(Result);
                SetLength(Result, out_idx_local + Length(token_parts_local));
                for part_idx_local := 0 to High(token_parts_local) do
                begin
                    Result[out_idx_local + part_idx_local] :=
                        token_parts_local[part_idx_local];
                    Result[out_idx_local + part_idx_local].text :=
                        normalize_pinyin_text(token_parts_local[part_idx_local].text);
                    Result[out_idx_local + part_idx_local].start_index :=
                        compact_offset_local +
                        token_parts_local[part_idx_local].start_index;
                end;
                Inc(compact_offset_local, Length(token_text_local));
            end;
        finally
            parser_local.Free;
        end;
    end;

    procedure initialize_display_query_context;
    var
        raw_query_text_local: string;
        decoded_query_local: TncShuangpinDecodeResult;
        decoded_unit_idx_local: Integer;
        canonical_offset_local: Integer;
    begin
        normalized_pinyin := normalize_pinyin_text(m_composition_text);
        if is_shuangpin_input then
        begin
            decoded_query_local := nc_decode_shuangpin(
                m_config.pinyin_input_scheme, m_composition_display_text);
            normalized_pinyin := normalize_pinyin_text(
                decoded_query_local.compact_pinyin);
            if (normalized_pinyin = '') and (m_last_lookup_key <> '') then
            begin
                normalized_pinyin := normalize_pinyin_text(m_last_lookup_key);
            end;
        end
        else if (m_last_lookup_key <> '') and
            is_full_pinyin_key(normalize_pinyin_text(m_last_lookup_key)) and
            (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            normalized_pinyin := normalize_pinyin_text(m_last_lookup_key);
        end;

        user_entry_query := normalize_pinyin_text(m_last_lookup_key);
        if user_entry_query = '' then
        begin
            user_entry_query := normalized_pinyin;
        end;

        raw_query_text_local := get_display_raw_query_text_local;
        if is_shuangpin_input then
        begin
            SetLength(syllables, Length(decoded_query_local.units));
            canonical_offset_local := 1;
            for decoded_unit_idx_local := 0 to
                High(decoded_query_local.units) do
            begin
                syllables[decoded_unit_idx_local].text :=
                    decoded_query_local.units[decoded_unit_idx_local].pinyin;
                syllables[decoded_unit_idx_local].start_index :=
                    canonical_offset_local;
                syllables[decoded_unit_idx_local].length := Length(
                    decoded_query_local.units[decoded_unit_idx_local].pinyin);
                Inc(canonical_offset_local,
                    syllables[decoded_unit_idx_local].length);
            end;
        end
        else if Pos('''', raw_query_text_local) > 0 then
        begin
            syllables := get_display_query_syllables_local(raw_query_text_local);
        end
        else
        begin
            syllables := get_effective_compact_pinyin_syllables(normalized_pinyin);
        end;
        if Length(syllables) = 0 then
        begin
            syllables := get_effective_compact_pinyin_syllables(normalized_pinyin);
        end;
        expected_units := Length(syllables);
    end;

    function can_use_display_trailing_prefix_fast_path: Boolean;
    var
        trailing_syllables_local: TncPinyinParseResult;
        last_trailing_text_local: string;

        function top_candidate_is_covering_prefix_partial_local: Boolean;
        var
            top_candidate_local: TncCandidate;
            top_text_local: string;
            top_tail_local: string;
            top_text_units_local: Integer;
            head_key_local: string;
            expected_tail_key_local: string;
            unit_idx_local: Integer;

            function is_closed_modal_tail_pinyin_local(
                const value: string): Boolean;
            begin
                Result := SameText(value, 'de') or SameText(value, 'le') or
                    SameText(value, 'la') or SameText(value, 'ma') or
                    SameText(value, 'ba') or SameText(value, 'ne');
            end;

            function has_internal_initial_fragment_local: Boolean;
            var
                local_idx: Integer;
                local_text: string;
            begin
                Result := False;
                if Length(syllables) <= 2 then
                begin
                    Exit;
                end;

                for local_idx := 0 to High(syllables) - 1 do
                begin
                    local_text := normalize_pinyin_text(syllables[local_idx].text);
                    if (Length(local_text) = 1) and
                        (not is_full_pinyin_key(local_text)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            if (Length(m_candidates) = 0) or (m_dictionary = nil) then
            begin
                Exit;
            end;
            if has_internal_initial_fragment_local then
            begin
                Exit;
            end;

            top_candidate_local := m_candidates[0];
            top_text_local := Trim(top_candidate_local.text);
            top_tail_local := normalize_pinyin_text(Trim(top_candidate_local.comment));
            if (top_text_local = '') or (top_tail_local = '') or
                (top_tail_local <> Trim(top_candidate_local.comment)) then
            begin
                Exit;
            end;

            top_text_units_local := get_candidate_text_unit_count(top_text_local);
            if (top_text_units_local <= 0) or
                (top_text_units_local >= expected_units) then
            begin
                Exit;
            end;
            if (top_text_units_local < 2) and
                (not m_last_lookup_incremental_partial_reuse) then
            begin
                Exit;
            end;

            expected_tail_key_local := '';
            if top_text_units_local < Length(syllables) then
            begin
                for unit_idx_local := top_text_units_local to High(syllables) do
                begin
                    expected_tail_key_local := expected_tail_key_local +
                        normalize_pinyin_text(syllables[unit_idx_local].text);
                end;
            end;
            if (expected_tail_key_local = '') or
                (not SameText(top_tail_local, expected_tail_key_local)) then
            begin
                Exit;
            end;

            trailing_syllables_local :=
                get_effective_compact_pinyin_syllables(expected_tail_key_local);
            if (Length(trailing_syllables_local) <= 0) or
                is_closed_modal_tail_pinyin_local(normalize_pinyin_text(
                trailing_syllables_local[High(trailing_syllables_local)].text)) then
            begin
                Exit;
            end;

            head_key_local := '';
            if top_text_units_local <= Length(syllables) then
            begin
                for unit_idx_local := 0 to top_text_units_local - 1 do
                begin
                    head_key_local := head_key_local +
                        normalize_pinyin_text(syllables[unit_idx_local].text);
                end;
            end;
            if head_key_local = '' then
            begin
                Exit;
            end;

            if top_text_units_local = 1 then
            begin
                Result := m_dictionary.single_char_matches_pinyin(
                    head_key_local, top_text_local);
            end
            else
            begin
                Result := m_dictionary.is_base_entry(head_key_local,
                    top_text_local) or
                    m_dictionary.is_user_entry(head_key_local, top_text_local) or
                    ((top_candidate_local.has_dict_weight or
                    (top_candidate_local.dict_weight > 0)) and
                    (top_candidate_local.dict_weight >= 300));
            end;
        end;
    begin
        Result := False;
        if short_exact_query_mode then
        begin
            Exit;
        end;

        if expected_units < 9 then
        begin
            if (expected_units < 4) or
                (not top_candidate_is_covering_prefix_partial_local) then
            begin
                Exit;
            end;

            Exit(True);
        end;

        if (Pos('trailingprefixfast=1', m_last_lookup_debug_extra) <= 0) and
            (Pos('extendabletailprefixfast=1', m_last_lookup_debug_extra) <= 0) and
            is_full_pinyin_key(normalized_pinyin) then
        begin
            Exit;
        end;

        if expected_units = 9 then
        begin
            trailing_syllables_local :=
                get_effective_compact_pinyin_syllables(normalized_pinyin);
            if Length(trailing_syllables_local) <= 0 then
            begin
                Exit;
            end;
            last_trailing_text_local := Trim(
                trailing_syllables_local[High(trailing_syllables_local)].text);
            if SameText(last_trailing_text_local, 'q') then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure filter_explicit_apostrophe_display_candidates;
    var
        explicit_syllables: TncPinyinParseResult;
        explicit_exact_candidates: TncCandidateList;
        idx: Integer;
        out_idx: Integer;
        text_units: TArray<string>;
        expected_tail: string;

        function contains_non_ascii_text(const value: string): Boolean;
        var
            ch: Char;
        begin
            Result := False;
            for ch in value do
            begin
                if Ord(ch) > $7F then
                begin
                    Exit(True);
                end;
            end;
        end;

        function complete_candidate_matches_boundary(const text_value: string): Boolean;
        var
            unit_idx: Integer;
            exact_idx: Integer;
        begin
            Result := False;
            for exact_idx := 0 to High(explicit_exact_candidates) do
            begin
                if (Trim(explicit_exact_candidates[exact_idx].comment) = '') and
                    SameText(Trim(explicit_exact_candidates[exact_idx].text),
                    Trim(text_value)) then
                begin
                    Exit(True);
                end;
            end;

            text_units := split_text_units(Trim(text_value));
            if Length(text_units) <> Length(explicit_syllables) then
            begin
                Exit;
            end;

            for unit_idx := 0 to High(text_units) do
            begin
                if not m_dictionary.single_char_matches_pinyin(
                    normalize_pinyin_text(explicit_syllables[unit_idx].text),
                    text_units[unit_idx]) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function partial_candidate_matches_boundary(
            const candidate: TncCandidate): Boolean;
        var
            unit_idx: Integer;
            span_idx: Integer;
            span_key: string;
            span_lookup: TncCandidateList;
            span_text: string;

            function lookup_has_complete_text(const lookup_key: string;
                const expected_text: string): Boolean;
            var
                lookup_idx: Integer;
            begin
                Result := False;
                if (lookup_key = '') or (expected_text = '') then
                begin
                    Exit;
                end;
                if (not m_dictionary.lookup_exact_full_pinyin(lookup_key,
                    span_lookup)) and
                    (not m_dictionary.lookup(lookup_key, span_lookup)) then
                begin
                    Exit;
                end;
                for lookup_idx := 0 to High(span_lookup) do
                begin
                    if (Trim(span_lookup[lookup_idx].comment) = '') and
                        SameText(Trim(span_lookup[lookup_idx].text),
                        expected_text) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            text_units := split_text_units(Trim(candidate.text));
            if (Length(text_units) <= 0) or
                (Length(text_units) >= Length(explicit_syllables)) then
            begin
                Exit;
            end;

            span_text := Trim(candidate.text);
            if Length(text_units) = 1 then
            begin
                if not m_dictionary.single_char_matches_pinyin(
                    normalize_pinyin_text(explicit_syllables[0].text),
                    text_units[0]) then
                begin
                    Exit;
                end;
            end
            else
            begin
                span_key := '';
                for span_idx := 0 to High(text_units) do
                begin
                    span_key := span_key +
                        normalize_pinyin_text(explicit_syllables[span_idx].text);
                end;
                if not lookup_has_complete_text(span_key, span_text) then
                begin
                    Exit;
                end;
            end;

            expected_tail := '';
            for unit_idx := Length(text_units) to High(explicit_syllables) do
            begin
                expected_tail := expected_tail +
                    normalize_pinyin_text(explicit_syllables[unit_idx].text);
            end;

            Result := (expected_tail <> '') and
                SameText(normalize_pinyin_text(Trim(candidate.comment)),
                expected_tail);
        end;
    begin
        if (m_dictionary = nil) or
            (Pos('''', get_display_raw_query_text_local) <= 0) then
        begin
            Exit;
        end;

        explicit_syllables := get_display_query_syllables_local(
            get_display_raw_query_text_local);

        if Length(explicit_syllables) <= 1 then
        begin
            Exit;
        end;

        { Keep strict explicit-boundary exact words even when a character's
          contextual pronunciation is absent from the single-character table. }
        if not m_dictionary.lookup(get_display_raw_query_text_local,
            explicit_exact_candidates) then
        begin
            SetLength(explicit_exact_candidates, 0);
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) =
                Length(explicit_syllables)) and
                (not complete_candidate_matches_boundary(m_candidates[idx].text)) then
            begin
                Continue;
            end;

            if (Trim(m_candidates[idx].comment) <> '') and
                contains_non_ascii_text(Trim(m_candidates[idx].text)) and
                (not partial_candidate_matches_boundary(m_candidates[idx])) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
            end;
            Inc(out_idx);
        end;
        SetLength(m_candidates, out_idx);
    end;

    function build_display_query_key(const start_idx: Integer;
        const syllable_count: Integer): string;
    var
        idx: Integer;
        cache_idx: Integer;
        syllable_total: Integer;
    begin
        Result := '';
        syllable_total := Length(syllables);
        if (start_idx < 0) or (syllable_count <= 0) or
            (start_idx + syllable_count > syllable_total) then
        begin
            Exit;
        end;

        if Length(display_query_key_cache) <> syllable_total * syllable_total then
        begin
            SetLength(display_query_key_cache, syllable_total * syllable_total);
            SetLength(display_normalized_syllable_cache, syllable_total);
        end;
        cache_idx := start_idx * syllable_total + syllable_count - 1;
        if display_query_key_cache[cache_idx] <> '' then
        begin
            Exit(display_query_key_cache[cache_idx]);
        end;

        for idx := start_idx to start_idx + syllable_count - 1 do
        begin
            if display_normalized_syllable_cache[idx] = '' then
            begin
                display_normalized_syllable_cache[idx] :=
                    normalize_pinyin_text(syllables[idx].text);
            end;
            Result := Result + display_normalized_syllable_cache[idx];
        end;
        display_query_key_cache[cache_idx] := Result;
    end;

    function display_last_syllable_is_short_particle_tail: Boolean;
    var
        tail_key_local: string;
    begin
        Result := False;
        if expected_units <= 0 then
        begin
            Exit;
        end;

        tail_key_local := build_display_query_key(expected_units - 1, 1);
        Result := SameText(tail_key_local, 'de') or
            SameText(tail_key_local, 'le') or
            SameText(tail_key_local, 'zhe') or
            SameText(tail_key_local, 'la') or
            SameText(tail_key_local, 'ba') or
            SameText(tail_key_local, 'ma') or
            SameText(tail_key_local, 'a');
    end;

    function is_display_function_or_modifier_single(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit;
        end;

        case Ord(text_value[1]) of
            $6211, $4F60, $4ED6, $5979,
            $662F, $7684, $5730, $4E86, $5728, $4E8E, $4E5F, $548C,
            $4E0D, $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1,
            $8981, $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9,
            $4ECE, $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA,
            $522B, $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A,
            $4ECD, $591A, $5C11, $65B0, $8001, $5C0F, $5927, $5168,
            $8D85, $771F, $597D, $5982, $633A, $8F83, $6781, $800C,
            $5E76, $6240, $4E0A, $4E0B, $4E00, $4E14, $8FD9,
            $54EA, $53BB, $5148:
                Result := True;
        end;
    end;

    function is_display_one_plus_two_modifier_head_single(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit;
        end;

        case Ord(text_value[1]) of
            $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
            $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
            $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
            $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
            $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
            $5BF9, $4ECE, $4E8E, $548C, $4E0E, $662F,
            $7B2C, $5148:
                Result := True;
        end;
    end;

    function is_display_one_plus_two_pronoun_head_single(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit;
        end;

        case Ord(text_value[1]) of
            $6211, $4F60, $60A8:
                Result := True;
        end;
    end;

    function is_display_one_plus_two_direct_complete_head_single(
        const text_value: string): Boolean;
    begin
        Result := is_display_one_plus_two_pronoun_head_single(text_value);
        if Result then
        begin
            Exit;
        end;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit(False);
        end;

        Result := Ord(text_value[1]) = $6700; // 鏈€
    end;

    function is_display_one_plus_two_supported_head_single(
        const text_value: string): Boolean;
    begin
        Result := is_display_one_plus_two_modifier_head_single(text_value) or
            is_display_one_plus_two_pronoun_head_single(text_value);
    end;

    function try_get_display_function_single(const syllable_key: string;
        out out_text: string; out out_weight: Integer): Boolean;
    const
        c_probe_limit = 12;
    var
        exact_results: TncCandidateList;
        idx: Integer;
        text_value: string;
        weight_value: Integer;
    begin
        Result := False;
        out_text := '';
        out_weight := 0;
        if (m_dictionary = nil) or (syllable_key = '') or
            (not m_dictionary.lookup_exact_full_pinyin(syllable_key,
            exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(exact_results[idx].comment) <> '' then
            begin
                Continue;
            end;

            text_value := Trim(exact_results[idx].text);
            if not is_display_function_or_modifier_single(text_value) then
            begin
                Continue;
            end;

            if exact_results[idx].has_dict_weight then
            begin
                weight_value := exact_results[idx].dict_weight;
            end
            else
            begin
                weight_value := exact_results[idx].score;
            end;

            if weight_value > out_weight then
            begin
                out_text := text_value;
                out_weight := weight_value;
                Result := True;
            end;
        end;
    end;

    function lookup_display_exact_cached(const query_key: string;
        out out_results: TncCandidateList): Boolean; forward;

    function try_get_display_exact_phrase(const start_idx: Integer;
        const syllable_count: Integer; out out_text: string;
        out out_weight: Integer): Boolean;
    const
        c_probe_limit = 8;
    var
        query_key: string;
        exact_results: TncCandidateList;
        idx: Integer;
        text_value: string;
        weight_value: Integer;
    begin
        Result := False;
        out_text := '';
        out_weight := 0;
        query_key := build_display_query_key(start_idx, syllable_count);
        if (m_dictionary = nil) or (query_key = '') or
            (not lookup_display_exact_cached(query_key, exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(exact_results[idx].comment) <> '' then
            begin
                Continue;
            end;

            text_value := Trim(exact_results[idx].text);
            if get_candidate_text_unit_count(text_value) <> syllable_count then
            begin
                Continue;
            end;

            if exact_results[idx].has_dict_weight then
            begin
                weight_value := exact_results[idx].dict_weight;
            end
            else
            begin
                weight_value := exact_results[idx].score;
            end;

            if weight_value > out_weight then
            begin
                out_text := text_value;
                out_weight := weight_value;
                Result := True;
            end;
        end;
    end;

    function lookup_display_exact_cached(const query_key: string;
        out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
    begin
        SetLength(out_results, 0);
        if (m_dictionary = nil) or (query_key = '') then
        begin
            Exit(False);
        end;

        cache_key := '#exact#' + query_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, out_results) then
        begin
            Exit(Length(out_results) > 0);
        end;

        Result := m_dictionary.lookup_exact_full_pinyin(query_key, out_results);
        if not Result then
        begin
            SetLength(out_results, 0);
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key, out_results);
        end;
        Result := Length(out_results) > 0;
    end;

    procedure prepare_umlaut_raw_prefixes;
    var
        raw_query: string;
        canonical_query: string;
        prefix_key: string;
        tail_key: string;
        offset: Integer;
        idx: Integer;
        out_idx: Integer;
        exact_results: TncCandidateList;
    begin
        if umlaut_raw_prefixes_ready then Exit;
        umlaut_raw_prefixes_ready := True;
        if (m_dictionary = nil) or is_shuangpin_input then Exit;
        raw_query := m_composition_text;
        canonical_query := nc_normalize_umlaut_spelling(raw_query);
        if raw_query = canonical_query then Exit;
        raw_query := LowerCase(raw_query);
        canonical_query := LowerCase(canonical_query);

        // The alias and the original spelling are parallel lookup paths:
        // hu/lue can match hu/lve, but hulu/e must remain selectable too.
        // Query only the additional u/e boundaries, not all input substrings.
        for offset := Length(raw_query) downto 1 do
        begin
            if (raw_query[offset] <> 'u') or
                (canonical_query[offset] <> 'v') then Continue;
            prefix_key := Copy(raw_query, 1, offset);
            tail_key := Copy(raw_query, offset + 1, MaxInt);
            if not lookup_display_exact_cached(prefix_key, exact_results) then Continue;
            for idx := 0 to High(exact_results) do
            begin
                if (Trim(exact_results[idx].text) = '') or
                    (Trim(exact_results[idx].comment) <> '') then Continue;
                out_idx := Length(umlaut_raw_prefixes);
                SetLength(umlaut_raw_prefixes, out_idx + 1);
                umlaut_raw_prefixes[out_idx] := exact_results[idx];
                umlaut_raw_prefixes[out_idx].comment := tail_key;
            end;
        end;
    end;

    function umlaut_raw_prefix_units(const value: TncCandidate): Integer;
    var
        idx: Integer;
    begin
        Result := 0;
        if value.comment = '' then Exit;
        for idx := 0 to High(umlaut_raw_prefixes) do
            if SameText(value.text, umlaut_raw_prefixes[idx].text) and
                SameText(value.comment, umlaut_raw_prefixes[idx].comment) then
                Exit(get_candidate_text_unit_count(value.text));
    end;

    function alternative_exact_prefix_units(const value: TncCandidate): Integer;
    var
        split_pos, boundary: Integer;
        head_key, tail_key: string;
    begin
        Result := 0;
        if (expected_units <> 4) or (Length(syllables) <> 4) or
            (m_dictionary = nil) or (value.comment = '') or
            (Pos('''', m_composition_text) > 0) or is_shuangpin_input or
            (get_candidate_text_unit_count(value.text) <> 2) then Exit;
        tail_key := normalize_pinyin_text(value.comment);
        split_pos := Length(normalized_pinyin) - Length(tail_key);
        boundary := Length(syllables[0].text) + Length(syllables[1].text);
        if (split_pos < 2) or (split_pos >= Length(normalized_pinyin)) or
            (Abs(split_pos - boundary) <> 1) or
            (not CharInSet(normalized_pinyin[Min(split_pos, boundary) + 1], ['n', 'g'])) or
            (Copy(normalized_pinyin, split_pos + 1, MaxInt) <> tail_key) then Exit;
        head_key := Copy(normalized_pinyin, 1, split_pos);
        if (Length(get_effective_compact_pinyin_syllables(head_key)) = 2) and
            (Length(get_effective_compact_pinyin_syllables(tail_key)) = 2) and
            (m_dictionary.is_base_entry(head_key, value.text) or
            m_dictionary.is_user_entry(head_key, value.text)) then Result := 2;
    end;

    function lookup_display_complete_cached(const query_key: string;
        out out_results: TncCandidateList): Boolean;
    var
        cache_key: string;
    begin
        Result := lookup_display_exact_cached(query_key, out_results);
        if Result then
        begin
            Exit;
        end;

        if (m_dictionary = nil) or (query_key = '') then
        begin
            SetLength(out_results, 0);
            Exit(False);
        end;

        cache_key := '#lookup#' + query_key;
        if (m_build_lookup_cache <> nil) and
            m_build_lookup_cache.TryGetValue(cache_key, out_results) then
        begin
            Exit(Length(out_results) > 0);
        end;

        if not m_dictionary.lookup(query_key, out_results) then
        begin
            SetLength(out_results, 0);
        end;
        if m_build_lookup_cache <> nil then
        begin
            m_build_lookup_cache.AddOrSetValue(cache_key, out_results);
        end;
        Result := Length(out_results) > 0;
    end;

    function display_exact_key_has_text(const query_key: string;
        const expected_text: string): Boolean;
    const
        c_probe_limit = 16;
    var
        exact_results: TncCandidateList;
        idx: Integer;
    begin
        Result := False;
        if (query_key = '') or (expected_text = '') or
            (not lookup_display_exact_cached(query_key, exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            if (Trim(exact_results[idx].comment) = '') and
                SameText(Trim(exact_results[idx].text), expected_text) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_exact_primary_has_text(const query_key: string;
        const expected_text: string; const expected_units_local: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (query_key = '') or (expected_text = '') or
            (expected_units_local <= 0) or
            (not lookup_display_exact_cached(query_key, exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                (exact_text_local <> '') and
                (get_candidate_text_unit_count(exact_text_local) =
                expected_units_local) then
            begin
                Exit(SameText(exact_text_local, expected_text));
            end;
        end;
    end;

    function display_productive_short_function_head_text(
        const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 2 then
        begin
            Exit;
        end;

        Result := SameText(text_value, string(Char($6709)) + string(Char($70B9))) or
            SameText(text_value, string(Char($4E00)) + string(Char($70B9))) or
            SameText(text_value, string(Char($4E0D)) + string(Char($662F))) or
            SameText(text_value, string(Char($4E0D)) + string(Char($4F1A))) or
            SameText(text_value, string(Char($4E0D)) + string(Char($80FD))) or
            SameText(text_value, string(Char($4E0D)) + string(Char($8981))) or
            SameText(text_value, string(Char($6CA1)) + string(Char($6709))) or
            SameText(text_value, string(Char($53EF)) + string(Char($4EE5))) or
            SameText(text_value, string(Char($5E94)) + string(Char($8BE5))) or
            SameText(text_value, string(Char($9700)) + string(Char($8981)));
    end;

    function display_candidate_is_supported_pronoun_connector_tail_complete_local(
        const candidate_value: TncCandidate): Boolean;
    var
        units_local: TArray<string>;
        tail_key_local: string;
        tail_text_local: string;
        tail_unit_idx_local: Integer;

        function is_pronoun_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1, $4FFA:
                    Result := True;
            end;
        end;

        function is_connector_local(const value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(value) <> 1 then
            begin
                Exit;
            end;
            case Ord(value[1]) of
                $8DDF, $548C, $4E0E, $540C:
                    Result := True;
            end;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (expected_units < 4) or
            (expected_units > 6) or (Length(syllables) <> expected_units) or
            (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        units_local := split_text_units(Trim(candidate_value.text));
        if (Length(units_local) <> expected_units) or
            (not is_pronoun_local(units_local[0])) or
            (not is_connector_local(units_local[1])) or
            (not m_dictionary.single_char_matches_pinyin(
            normalize_pinyin_text(syllables[0].text), units_local[0])) or
            (not m_dictionary.single_char_matches_pinyin(
            normalize_pinyin_text(syllables[1].text), units_local[1])) then
        begin
            Exit;
        end;

        tail_key_local := build_display_query_key(2, expected_units - 2);
        tail_text_local := '';
        for tail_unit_idx_local := 2 to High(units_local) do
        begin
            tail_text_local := tail_text_local + units_local[tail_unit_idx_local];
        end;
        Result := display_exact_key_has_text(tail_key_local, tail_text_local) or
            m_dictionary.is_base_entry(tail_key_local, tail_text_local) or
            m_dictionary.is_user_entry(tail_key_local, tail_text_local);
    end;

    function display_short_four_two_exact_path_is_supported_local(
        const candidate_value: TncCandidate): Boolean;
    var
        encoded_path_local: string;
        evidence_score_local: Integer;
    begin
        Result := (expected_units = 4) and (Length(syllables) = 4) and
            (Trim(candidate_value.comment) = '') and
            get_short_four_two_exact_path_evidence(normalized_pinyin,
            Trim(candidate_value.text), encoded_path_local,
            evidence_score_local);
    end;

    function display_candidate_is_unsupported_short_four_ab_cd_complete(
        const candidate_value: TncCandidate): Boolean;
    var
        candidate_text_local: string;
        units_local: TArray<string>;
        first_unit_local: string;
        head_text_local: string;
        head_three_text_local: string;
        middle_text_local: string;
        tail_text_local: string;
        head_key_local: string;
        head_three_key_local: string;
        middle_key_local: string;
        tail_key_local: string;
        transition_particle_path_local: string;
        transition_particle_evidence_local: Integer;

        function is_display_short_particle_tail_unit_local(
            const pinyin_key_value: string; const text_value: string): Boolean;
        begin
            Result := ((pinyin_key_value = 'de') and
                SameText(text_value, string(Char($7684)))) or
                ((pinyin_key_value = 'le') and
                SameText(text_value, string(Char($4E86)))) or
                ((pinyin_key_value = 'zhe') and
                SameText(text_value, string(Char($7740)))) or
                ((pinyin_key_value = 'la') and
                SameText(text_value, string(Char($5566)))) or
                ((pinyin_key_value = 'ba') and
                SameText(text_value, string(Char($5427)))) or
                ((pinyin_key_value = 'ma') and
                SameText(text_value, string(Char($5417)))) or
                ((pinyin_key_value = 'a') and
                SameText(text_value, string(Char($554A))));
        end;

        function is_display_short_pronoun_unit_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $6211, $4F60, $60A8, $4ED6, $5979, $5B83, $54B1:
                    Result := True;
            end;
        end;

        function is_display_short_connector_unit_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $548C, $4E0E, $8DDF, $540C:
                    Result := True;
            end;
        end;

        function display_prefix_lookup_has_complete_text_local: Boolean;
        var
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or
                (not m_dictionary.lookup_full_pinyin_prefix(normalized_pinyin,
                prefix_results_local)) then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                    prefix_results_local[prefix_idx_local].has_dict_weight and
                    (prefix_results_local[prefix_idx_local].dict_weight > 0) and
                    SameText(prefix_text_local, candidate_text_local) and
                    (get_candidate_text_unit_count(prefix_text_local) =
                    expected_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        Result := False;
        if (m_dictionary = nil) or (expected_units <> 4) or
            (Length(syllables) <> 4) or
            (not is_full_pinyin_key(normalized_pinyin)) or
            (Trim(candidate_value.comment) <> '') or
            (candidate_value.source = cs_user) then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        if (candidate_text_local = '') or
            display_exact_key_has_text(normalized_pinyin, candidate_text_local) or
            m_dictionary.is_user_entry(normalized_pinyin, candidate_text_local) then
        begin
            Exit;
        end;

        if display_prefix_lookup_has_complete_text_local then
        begin
            Exit;
        end;

        units_local := split_text_units(candidate_text_local);
        if Length(units_local) <> 4 then
        begin
            Exit;
        end;

        head_text_local := units_local[0] + units_local[1];
        tail_text_local := units_local[2] + units_local[3];

        head_key_local := build_display_query_key(0, 2);
        tail_key_local := build_display_query_key(2, 2);
        if display_short_four_two_exact_path_is_supported_local(
            candidate_value) then
        begin
            Exit;
        end;

        if get_short_four_transition_particle_path_evidence(
            normalized_pinyin, candidate_text_local,
            transition_particle_path_local,
            transition_particle_evidence_local) then
        begin
            Exit;
        end;

        if display_productive_short_function_head_text(head_text_local) and
            display_exact_key_has_text(tail_key_local, tail_text_local) then
        begin
            Exit;
        end;

        if display_exact_key_has_text(head_key_local, head_text_local) and
            is_display_short_pronoun_unit_local(units_local[2]) and
            is_display_short_connector_unit_local(units_local[3]) then
        begin
            Exit;
        end;

        if is_display_short_pronoun_unit_local(units_local[0]) and
            is_display_short_connector_unit_local(units_local[1]) and
            display_exact_key_has_text(tail_key_local, tail_text_local) then
        begin
            Exit;
        end;

        first_unit_local := units_local[0];
        middle_text_local := units_local[1] + units_local[2];
        middle_key_local := build_display_query_key(1, 2);
        if is_display_one_plus_two_supported_head_single(first_unit_local) and
            display_exact_key_has_text(middle_key_local, middle_text_local) and
            is_display_short_particle_tail_unit_local(syllables[3].text,
            units_local[3]) then
        begin
            Exit;
        end;

        tail_text_local := units_local[1] + units_local[2] + units_local[3];
        tail_key_local := build_display_query_key(1, 3);
        if is_display_one_plus_two_supported_head_single(first_unit_local) and
            display_exact_key_has_text(tail_key_local, tail_text_local) then
        begin
            Exit;
        end;

        head_three_text_local := units_local[0] + units_local[1] +
            units_local[2];
        head_three_key_local := build_display_query_key(0, 3);
        if display_exact_key_has_text(head_three_key_local,
            head_three_text_local) and
            is_display_short_particle_tail_unit_local(syllables[3].text,
            units_local[3]) then
        begin
            Exit;
        end;

        Result := True;
    end;

    procedure filter_display_unsupported_short_four_ab_cd_complete_candidates;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        path_local: string;
    begin
        if (Length(m_candidates) = 0) or (expected_units <> 4) or
            (m_dictionary = nil) or (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(m_candidates) do
        begin
            path_local := '';
            if in_idx_local < Length(m_candidate_segment_paths) then
            begin
                path_local := m_candidate_segment_paths[in_idx_local];
            end;

            if (not (m_candidates[in_idx_local].has_dict_weight and
                (m_candidates[in_idx_local].dict_weight > 0))) and
                display_candidate_is_unsupported_short_four_ab_cd_complete(
                m_candidates[in_idx_local]) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                m_candidates[out_idx_local] := m_candidates[in_idx_local];
                if out_idx_local < Length(m_candidate_segment_paths) then
                begin
                    path_local := '';
                    if in_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        path_local := m_candidate_segment_paths[in_idx_local];
                    end;
                    m_candidate_segment_paths[out_idx_local] := path_local;
                end;
            end;
            Inc(out_idx_local);
        end;

        SetLength(m_candidates, out_idx_local);
        if Length(m_candidate_segment_paths) > out_idx_local then
        begin
            SetLength(m_candidate_segment_paths, out_idx_local);
        end;
    end;

    function display_base_exact_key_has_text(const query_key: string;
        const target_text: string; const target_units: Integer): Boolean;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (query_key = '') or (target_text = '') or
            (not lookup_display_exact_cached(query_key, exact_results_local)) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (exact_results_local[exact_idx_local].source = cs_rule) and
                (Trim(exact_results_local[exact_idx_local].comment) = '') and
                SameText(exact_text_local, target_text) and
                (get_candidate_text_unit_count(exact_text_local) =
                target_units) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_candidate_is_unsupported_short_four_ab_c_partial(
        const candidate_value: TncCandidate): Boolean;
    var
        candidate_text_local: string;
        units_local: TArray<string>;
        head_text_local: string;
        tail_text_local: string;
        tail_key_local: string;
        prefix_key_local: string;
        prefix_text_local: string;
        normalized_comment_local: string;
        comment_unit_count_local: Integer;
        unit_idx_local: Integer;

        function candidate_has_allowed_one_plus_two_prefix_support_local:
            Boolean;
        begin
            Result := False;
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            head_text_local := Trim(units_local[0]);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            tail_key_local := build_display_query_key(1, 2);
            Result := (head_text_local <> '') and (tail_text_local <> '') and
                (tail_key_local <> '') and
                is_display_one_plus_two_supported_head_single(head_text_local) and
                display_exact_key_has_text(tail_key_local, tail_text_local);
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (expected_units <> 4) or
            (Length(syllables) <> 4) or
            (not is_full_pinyin_key(normalized_pinyin)) or
            (Trim(candidate_value.comment) = '') then
        begin
            Exit;
        end;

        normalized_comment_local := normalize_pinyin_text(
            Trim(candidate_value.comment));
        comment_unit_count_local := get_effective_compact_pinyin_unit_count(
            normalized_comment_local);
        if comment_unit_count_local <= 0 then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        units_local := split_text_units(candidate_text_local);
        if (Length(units_local) < 2) or (Length(units_local) > 3) then
        begin
            Exit;
        end;
        if Length(units_local) + comment_unit_count_local <> expected_units then
        begin
            Exit;
        end;

        prefix_text_local := '';
        for unit_idx_local := 0 to High(units_local) do
        begin
            prefix_text_local := prefix_text_local + units_local[unit_idx_local];
        end;
        prefix_key_local := build_display_query_key(0, Length(units_local));
        if display_exact_key_has_text(prefix_key_local, prefix_text_local) then
        begin
            Exit;
        end;

        if Length(units_local) <> 3 then
        begin
            Exit(True);
        end;

        if candidate_has_allowed_one_plus_two_prefix_support_local then
        begin
            Exit(False);
        end;

        Exit(True);
    end;

    procedure filter_display_unsupported_short_four_ab_c_partial_candidates;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        path_local: string;
    begin
        if (Length(m_candidates) = 0) or (expected_units <> 4) or
            (m_dictionary = nil) or (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(m_candidates) do
        begin
            if display_candidate_is_unsupported_short_four_ab_c_partial(
                m_candidates[in_idx_local]) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                m_candidates[out_idx_local] := m_candidates[in_idx_local];
                if out_idx_local < Length(m_candidate_segment_paths) then
                begin
                    path_local := '';
                    if in_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        path_local := m_candidate_segment_paths[in_idx_local];
                    end;
                    m_candidate_segment_paths[out_idx_local] := path_local;
                end;
            end;
            Inc(out_idx_local);
        end;

        SetLength(m_candidates, out_idx_local);
        if Length(m_candidate_segment_paths) > out_idx_local then
        begin
            SetLength(m_candidate_segment_paths, out_idx_local);
        end;
    end;

    function display_candidate_is_exact_full_dictionary_entry(
        const query_key: string; const candidate_value: TncCandidate): Boolean;
    var
        candidate_text_local: string;
    begin
        Result := False;
        if (query_key = '') or (Trim(candidate_value.comment) <> '') or
            (candidate_value.source <> cs_rule) or
            (not candidate_value.has_dict_weight) then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        if (candidate_text_local = '') or
            (get_candidate_text_unit_count(candidate_text_local) <> expected_units) then
        begin
            Exit;
        end;

        Result := display_exact_key_has_text(query_key, candidate_text_local);
    end;

    function display_candidate_has_supported_full_entry(
        const query_key: string; const candidate_value: TncCandidate): Boolean;
    const
        c_min_supported_exact_dict_weight = 160;
    var
        candidate_text_local: string;
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (query_key = '') or
            (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        if candidate_text_local = '' then
        begin
            Exit;
        end;

        if m_dictionary.is_base_entry(query_key, candidate_text_local) then
        begin
            Exit(True);
        end;

        if not lookup_display_exact_cached(query_key, exact_results_local) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                (not SameText(exact_text_local, candidate_text_local)) or
                (get_candidate_text_unit_count(exact_text_local) <>
                expected_units) then
            begin
                Continue;
            end;

            if (exact_results_local[exact_idx_local].source = cs_rule) and
                exact_results_local[exact_idx_local].has_dict_weight and
                (exact_results_local[exact_idx_local].dict_weight >=
                c_min_supported_exact_dict_weight) then
            begin
                Exit(True);
            end;

            if (exact_results_local[exact_idx_local].source = cs_user) and
                m_dictionary.is_user_entry(query_key, candidate_text_local) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_candidate_has_base_full_entry(
        const query_key: string; const candidate_value: TncCandidate): Boolean;
    const
        c_min_supported_exact_dict_weight = 160;
    var
        candidate_text_local: string;
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        exact_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (query_key = '') or
            (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        if candidate_text_local = '' then
        begin
            Exit;
        end;

        if m_dictionary.is_base_entry(query_key, candidate_text_local) then
        begin
            Exit(True);
        end;

        if not lookup_display_exact_cached(query_key, exact_results_local) then
        begin
            Exit;
        end;

        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            exact_text_local := Trim(exact_results_local[exact_idx_local].text);
            if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                (not SameText(exact_text_local, candidate_text_local)) or
                (get_candidate_text_unit_count(exact_text_local) <>
                expected_units) then
            begin
                Continue;
            end;

            if (exact_results_local[exact_idx_local].source = cs_rule) and
                exact_results_local[exact_idx_local].has_dict_weight and
                (exact_results_local[exact_idx_local].dict_weight >=
                c_min_supported_exact_dict_weight) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_candidate_is_persistent_latest_choice(
        const query_key: string; const candidate_text_value: string): Boolean;
    var
        latest_text_local: string;
    begin
        Result := False;
        if (m_dictionary = nil) or (query_key = '') or
            (candidate_text_value = '') then
        begin
            Exit;
        end;

        latest_text_local := get_cached_query_latest_choice_text(query_key);
        Result := (latest_text_local <> '') and
            SameText(latest_text_local, candidate_text_value);
    end;

    procedure filter_display_rejected_user_candidate_texts;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[in_idx_local].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[in_idx_local].comment) = '') and
                (m_dictionary.get_candidate_penalty(normalized_pinyin,
                candidate_text_local) > 0) and
                (not m_dictionary.is_base_entry(normalized_pinyin,
                candidate_text_local)) then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                m_candidates[out_idx_local] := m_candidates[in_idx_local];
            end;
            Inc(out_idx_local);
        end;

        if out_idx_local <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx_local);
        end;
    end;

    function display_exact_key_has_any_complete_text(const query_key: string;
        const expected_units_local: Integer): Boolean;
    const
        c_probe_limit = 12;
    var
        exact_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
    begin
        Result := False;
        if (query_key = '') or (expected_units_local <= 0) or
            (not lookup_display_exact_cached(query_key, exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            candidate_text := Trim(exact_results[idx].text);
            if (Trim(exact_results[idx].comment) = '') and
                (candidate_text <> '') and
                (get_candidate_text_unit_count(candidate_text) =
                expected_units_local) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_exact_key_has_stronger_complete_text(
        const query_key: string; const current_text: string;
        const expected_units_local: Integer; const min_weight_gap: Integer): Boolean;
    const
        c_probe_limit = 12;
    var
        exact_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        candidate_weight: Integer;
        current_weight: Integer;
        best_weight: Integer;
    begin
        Result := False;
        current_weight := Low(Integer);
        best_weight := Low(Integer);
        if (query_key = '') or (current_text = '') or
            (expected_units_local <= 0) or
            (not lookup_display_exact_cached(query_key, exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            candidate_text := Trim(exact_results[idx].text);
            if (Trim(exact_results[idx].comment) <> '') or
                (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <>
                expected_units_local) then
            begin
                Continue;
            end;

            if exact_results[idx].has_dict_weight then
            begin
                candidate_weight := exact_results[idx].dict_weight;
            end
            else
            begin
                candidate_weight := exact_results[idx].score;
            end;
            if candidate_weight > best_weight then
            begin
                best_weight := candidate_weight;
            end;
            if SameText(candidate_text, current_text) and
                (candidate_weight > current_weight) then
            begin
                current_weight := candidate_weight;
            end;
        end;

        Result := (current_weight > Low(Integer)) and
            (best_weight > current_weight + min_weight_gap);
    end;

    function get_display_exact_text_weight(const query_key: string;
        const expected_text: string; const expected_units_local: Integer;
        out out_weight: Integer): Boolean;
    const
        c_probe_limit = 16;
    var
        exact_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        candidate_weight: Integer;
    begin
        Result := False;
        out_weight := 0;
        if (query_key = '') or (expected_text = '') or
            (expected_units_local <= 0) or
            (not lookup_display_exact_cached(query_key, exact_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(exact_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            candidate_text := Trim(exact_results[idx].text);
            if (Trim(exact_results[idx].comment) <> '') or
                (candidate_text = '') or
                (not SameText(candidate_text, expected_text)) or
                (get_candidate_text_unit_count(candidate_text) <>
                expected_units_local) then
            begin
                Continue;
            end;

            if exact_results[idx].has_dict_weight then
            begin
                candidate_weight := exact_results[idx].dict_weight;
            end
            else
            begin
                candidate_weight := exact_results[idx].score;
            end;
            if (not Result) or (candidate_weight > out_weight) then
            begin
                out_weight := candidate_weight;
                Result := True;
            end;
        end;
    end;

    function is_display_chinese_number_text(const text_value: string): Boolean;
    var
        units: TArray<string>;
        idx: Integer;
        ch: Char;
    begin
        Result := False;
        units := split_text_units(Trim(text_value));
        if Length(units) = 0 then
        begin
            Exit;
        end;

        for idx := 0 to High(units) do
        begin
            if Length(units[idx]) <> 1 then
            begin
                Exit(False);
            end;
            ch := units[idx][1];
            case Ord(ch) of
                $96F6, $4E00, $4E8C, $4E09, $56DB, $4E94, $516D, $4E03,
                $516B, $4E5D, $5341, $767E, $5343, $4E07, $4EBF,
                $5146, $4E24:
                    ;
            else
                Exit(False);
            end;
        end;
        Result := True;
    end;

    function is_display_repeated_unit_text(const text_value: string): Boolean;
    var
        units: TArray<string>;
        idx: Integer;
    begin
        Result := False;
        units := split_text_units(Trim(text_value));
        if Length(units) < 2 then
        begin
            Exit;
        end;

        for idx := 1 to High(units) do
        begin
            if not SameText(units[idx], units[0]) then
            begin
                Exit(False);
            end;
        end;
        Result := True;
    end;

    function display_candidate_effective_weight(
        const candidate_value: TncCandidate): Integer;
    begin
        Result := candidate_value.score;
        if ((not is_fuzzy_pinyin_active) or
            (candidate_value.fuzzy_cost <= 0)) and
            candidate_value.has_dict_weight and
            (candidate_value.dict_weight > Result) then
        begin
            Result := candidate_value.dict_weight;
        end;
        if candidate_value.source = cs_user then
        begin
            Inc(Result, c_user_score_bonus);
        end;
    end;

    procedure filter_display_constructed_multi_char_partial_candidates;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
        candidate_head_local: string;
        candidate_tail_local: string;
        candidate_units_local: Integer;
        single_multi_tail_candidate_count_local: Integer;
        kept_single_multi_tail_candidate_count_local: Integer;
        base_entry_cache_local: TDictionary<string, Boolean>;
        user_entry_cache_local: TDictionary<string, Boolean>;
        partial_filter_tick_local: UInt64;

        procedure note_partial_filter_phase_local(const phase_name: string);
        var
            elapsed_ms_local: UInt64;
        begin
            if not m_config.debug_mode then
            begin
                partial_filter_tick_local := nc_monotonic_tick_ms;
                Exit;
            end;

            elapsed_ms_local := nc_monotonic_tick_ms - partial_filter_tick_local;
            partial_filter_tick_local := nc_monotonic_tick_ms;
            if elapsed_ms_local = 0 then
            begin
                Exit;
            end;
            if m_last_lookup_debug_extra <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
            end;
            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                Format('gcf_%s=%d', [phase_name, elapsed_ms_local]);
        end;

        function is_ascii_letter_local(const value: Char): Boolean;
        begin
            Result := ((value >= 'a') and (value <= 'z')) or
                ((value >= 'A') and (value <= 'Z'));
        end;

        function split_trailing_ascii_candidate_local(
            const candidate_text: string; out head: string;
            out tail: string): Boolean;
        var
            idx: Integer;
            tail_len: Integer;
        begin
            Result := False;
            head := '';
            tail := '';
            idx := Length(candidate_text);
            while (idx > 0) and is_ascii_letter_local(candidate_text[idx]) do
            begin
                Dec(idx);
            end;

            if (idx = Length(candidate_text)) or (idx <= 0) then
            begin
                Exit;
            end;

            head := Copy(candidate_text, 1, idx);
            tail_len := Length(candidate_text) - idx;
            tail := Copy(candidate_text, idx + 1, tail_len);
            Result := (head <> '') and (tail <> '');
        end;

        function cached_base_entry_exists_local(const query_key: string;
            const text_value: string): Boolean;
        var
            cache_key_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key = '') or (text_value = '') then
            begin
                Exit;
            end;

            cache_key_local := query_key + #1 + text_value;
            if base_entry_cache_local.TryGetValue(cache_key_local, Result) then
            begin
                Exit;
            end;

            Result := m_dictionary.is_base_entry(query_key, text_value);
            base_entry_cache_local.AddOrSetValue(cache_key_local, Result);
        end;

        function cached_user_entry_exists_local(const query_key: string;
            const text_value: string): Boolean;
        var
            cache_key_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (query_key = '') or (text_value = '') then
            begin
                Exit;
            end;

            cache_key_local := query_key + #1 + text_value;
            if user_entry_cache_local.TryGetValue(cache_key_local, Result) then
            begin
                Exit;
            end;

            Result := m_dictionary.is_user_entry(query_key, text_value);
            user_entry_cache_local.AddOrSetValue(cache_key_local, Result);
        end;

        function has_explicit_head_phrase_entry_local(
            const text_value: string; const unit_count: Integer): Boolean;
        var
            head_key_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (text_value = '') or
                (unit_count < 2) then
            begin
                Exit;
            end;

            head_key_local := build_display_query_key(0, unit_count);
            if head_key_local = '' then
            begin
                Exit;
            end;

            if display_exact_key_has_text(head_key_local, text_value) then
            begin
                Exit(True);
            end;

            if cached_base_entry_exists_local(head_key_local, text_value) then
            begin
                Exit(True);
            end;

            Result := cached_user_entry_exists_local(head_key_local, text_value);
        end;

        function is_unsupported_two_char_quantity_partial_local(
            const text_value: string): Boolean;
        var
            units_local: TArray<string>;
            quantity_unit_local: string;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <> 2 then
            begin
                Exit;
            end;

            quantity_unit_local := units_local[1];
            if quantity_unit_local = '' then
            begin
                Exit;
            end;

            case Ord(quantity_unit_local[1]) of
                $96F6, $4E00, $4E8C, $4E24, $4FE9, $4E09, $56DB, $4E94,
                $516D, $4E03, $516B, $4E5D, $5341, $767E, $5343, $4E07,
                $4EBF, $5146:
                    Result := True;
            end;
        end;

        function is_number_unit_for_quantity_prefix_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if (unit_value = '') or (get_candidate_text_unit_count(unit_value) <> 1) then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4E00, $4E8C, $4E24, $4FE9, $4E09, $56DB, $4E94, $516D,
                $4E03, $516B, $4E5D, $5341, $51E0, $534A:
                    Result := True;
            end;
        end;

        function is_classifier_unit_for_quantity_prefix_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if (unit_value = '') or (get_candidate_text_unit_count(unit_value) <> 1) then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4E2A, $6761, $53EA, $672C, $53F0, $90E8, $6279, $5339,
                $6839, $7247, $9875, $4EF6, $9879, $6B21, $6B65, $8F6E,
                $628A, $526F, $5BF9, $53CC, $74F6, $6876, $76D8, $6247,
                $95E8, $5B57, $884C, $5217, $5C42, $7AE0, $8282, $6BB5,
                $680B, $5E62:
                    Result := True;
            end;
        end;

        function is_non_nominal_tail_key_for_quantity_prefix_local(
            const tail_key_value: string): Boolean;
        var
            normalized_tail_local: string;
        begin
            normalized_tail_local := normalize_pinyin_text(tail_key_value);
            Result := (normalized_tail_local = 'dao') or
                (normalized_tail_local = 'da') or
                (normalized_tail_local = 'd') or
                (normalized_tail_local = 'le') or
                (normalized_tail_local = 'de') or
                (normalized_tail_local = 'guo') or
                (normalized_tail_local = 'zhe') or
                (normalized_tail_local = 'ma') or
                (normalized_tail_local = 'ba') or
                (normalized_tail_local = 'ne') or
                (normalized_tail_local = 'a') or
                (normalized_tail_local = 'ya') or
                (normalized_tail_local = 'la');
        end;

        function is_quantity_prefix_before_non_nominal_tail_local(
            const text_value: string; const tail_key_value: string): Boolean;
        var
            units_local: TArray<string>;
        begin
            Result := False;
            if not is_non_nominal_tail_key_for_quantity_prefix_local(
                tail_key_value) then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(text_value));
            Result := (Length(units_local) = 2) and
                is_number_unit_for_quantity_prefix_local(units_local[0]) and
                is_classifier_unit_for_quantity_prefix_local(units_local[1]);
        end;

        function is_single_prefix_before_multi_syllable_tail_local(
            const head_value: string; const tail_key_value: string): Boolean;
        begin
            Result := (expected_units >= 2) and (Length(tail_key_value) >= 2) and
                (get_candidate_text_unit_count(head_value) = 1);
        end;

        function partial_path_is_single_char_chain_local(
            const candidate_index: Integer; const expected_units: Integer): Boolean;
        var
            path_local: string;
            parts_local: TArray<string>;
            part_idx_local: Integer;
            part_text_local: string;
        begin
            Result := False;
            if (candidate_index < 0) or
                (candidate_index >= Length(m_candidate_segment_paths)) or
                (expected_units < 2) then
            begin
                Exit;
            end;

            path_local := Trim(m_candidate_segment_paths[candidate_index]);
            if path_local = '' then
            begin
                Exit;
            end;

            parts_local := path_local.Split([c_segment_path_separator]);
            if Length(parts_local) <> expected_units then
            begin
                Exit;
            end;

            for part_idx_local := 0 to High(parts_local) do
            begin
                part_text_local := Trim(parts_local[part_idx_local]);
                if get_candidate_text_unit_count(part_text_local) <> 1 then
                begin
                    Exit(False);
                end;
            end;

            Result := True;
        end;

        function complete_path_starts_with_ordinal_single_local(
            const candidate_index: Integer): Boolean;
        var
            path_local: string;
            parts_local: TArray<string>;
            first_part_local: string;
        begin
            Result := False;
            if (candidate_index < 0) or
                (candidate_index >= Length(m_candidate_segment_paths)) then
            begin
                Exit;
            end;

            path_local := Trim(m_candidate_segment_paths[candidate_index]);
            if path_local = '' then
            begin
                Exit;
            end;

            parts_local := path_local.Split([c_segment_path_separator]);
            if Length(parts_local) <= 1 then
            begin
                Exit;
            end;

            first_part_local := Trim(parts_local[0]);
            Result := (get_candidate_text_unit_count(first_part_local) = 1) and
                (Length(first_part_local) > 0) and (Ord(first_part_local[1]) = $7B2C);
        end;

    begin
        if Length(m_candidates) = 0 then
        begin
            Exit;
        end;

        base_entry_cache_local := TDictionary<string, Boolean>.Create;
        user_entry_cache_local := TDictionary<string, Boolean>.Create;
        try
            partial_filter_tick_local := nc_monotonic_tick_ms;
            single_multi_tail_candidate_count_local := 0;
            for in_idx_local := 0 to High(m_candidates) do
            begin
                candidate_text_local := Trim(m_candidates[in_idx_local].text);
                candidate_head_local := candidate_text_local;
                candidate_tail_local := Trim(m_candidates[in_idx_local].comment);
                if (candidate_tail_local = '') and
                    split_trailing_ascii_candidate_local(candidate_text_local,
                    candidate_head_local, candidate_tail_local) then
                begin
                    candidate_head_local := Trim(candidate_head_local);
                    candidate_tail_local := Trim(candidate_tail_local);
                end;
                if is_single_prefix_before_multi_syllable_tail_local(
                    candidate_head_local, candidate_tail_local) then
                begin
                    Inc(single_multi_tail_candidate_count_local);
                end;
            end;
            note_partial_filter_phase_local('count');

            out_idx_local := 0;
            kept_single_multi_tail_candidate_count_local := 0;
            for in_idx_local := 0 to High(m_candidates) do
            begin
                candidate_text_local := Trim(m_candidates[in_idx_local].text);
                candidate_head_local := candidate_text_local;
                candidate_tail_local := Trim(m_candidates[in_idx_local].comment);
                if (candidate_tail_local = '') and
                    split_trailing_ascii_candidate_local(candidate_text_local,
                    candidate_head_local, candidate_tail_local) then
                begin
                    candidate_head_local := Trim(candidate_head_local);
                    candidate_tail_local := Trim(candidate_tail_local);
                end;

                candidate_units_local := get_candidate_text_unit_count(
                    candidate_head_local);
                if is_single_prefix_before_multi_syllable_tail_local(
                    candidate_head_local, candidate_tail_local) then
                begin
                    Inc(kept_single_multi_tail_candidate_count_local);
                    if (single_multi_tail_candidate_count_local >
                        c_candidate_total_limit_max) and
                        (kept_single_multi_tail_candidate_count_local >
                        c_candidate_total_limit_max) then
                    begin
                        Continue;
                    end;
                end;

                if (candidate_tail_local <> '') and
                    is_quantity_prefix_before_non_nominal_tail_local(
                    candidate_head_local, candidate_tail_local) then
                begin
                    Continue;
                end;

                if (candidate_tail_local <> '') and
                    (not has_explicit_head_phrase_entry_local(candidate_head_local,
                    candidate_units_local)) and
                    (is_unsupported_two_char_quantity_partial_local(
                    candidate_head_local) or
                    partial_path_is_single_char_chain_local(in_idx_local,
                    candidate_units_local)) then
                begin
                    Continue;
                end;

                if (candidate_tail_local = '') and (expected_units <= 3) and
                    complete_path_starts_with_ordinal_single_local(in_idx_local) and
                    (m_dictionary <> nil) and
                    (not cached_base_entry_exists_local(normalized_pinyin,
                    candidate_text_local)) and
                    (not cached_user_entry_exists_local(normalized_pinyin,
                    candidate_text_local)) then
                begin
                    Continue;
                end;

                if out_idx_local <> in_idx_local then
                begin
                    m_candidates[out_idx_local] := m_candidates[in_idx_local];
                    if out_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        if in_idx_local < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[out_idx_local] :=
                                m_candidate_segment_paths[in_idx_local];
                        end
                        else
                        begin
                            m_candidate_segment_paths[out_idx_local] := '';
                        end;
                    end;
                end;
                Inc(out_idx_local);
            end;
            note_partial_filter_phase_local('loop');

            if out_idx_local < Length(m_candidates) then
            begin
                SetLength(m_candidates, out_idx_local);
                if Length(m_candidate_segment_paths) > out_idx_local then
                begin
                    SetLength(m_candidate_segment_paths, out_idx_local);
                end;
            end;
        finally
            user_entry_cache_local.Free;
            base_entry_cache_local.Free;
        end;
    end;

    function apply_short_query_exact_display_ranking: Boolean;
    const
        c_max_base_exact_units = 5;
        c_max_user_exact_units = 6;
        c_low_frequency_medical_exact_weight = 520;
        c_decisive_transition_min_full_lm_lead = 1400;
        c_decisive_transition_min_suffix_lm_lead = 1000;
    type
        TShortExactRankItem = record
            candidate: TncCandidate;
            path: string;
            category: Integer;
            prefix_units: Integer;
            rank_score: Integer;
            original_index: Integer;
            text_length: Integer;
            actual_full_exact: Boolean;
            user_full_exact: Boolean;
            decisive_transition_override: Boolean;
            transition_evidence: Integer;
            low_frequency_medical_exact: Boolean;
            nocontext_exact_priority: Integer;
            context_exact_priority: Integer;
        end;
    var
        exact_results: TncCandidateList;
        fuzzy_exact_results: TncCandidateList;
        predictive_prefix_results: TncCandidateList;
        exact_weights: TDictionary<string, Integer>;
        exact_is_user: TDictionary<string, Boolean>;
        exact_candidates: TDictionary<string, TncCandidate>;
        prefix_candidate_keys: TDictionary<string, Boolean>;
        predictive_prefix_texts: TDictionary<string, Boolean>;
        particle_tail_texts: TDictionary<string, Boolean>;
        list: TList<TShortExactRankItem>;
        idx: Integer;
        exact_idx: Integer;
        exact_text: string;
        exact_weight: Integer;
        existing_weight: Integer;
        existing_is_user: Boolean;
        candidate_item: TncCandidate;
        closed_particle_candidate: TncCandidate;
        item: TShortExactRankItem;
        path_value: string;
        has_user_exact: Boolean;
        has_closed_particle_candidate: Boolean;
        has_exact_lookup: Boolean;
        has_fuzzy_exact_lookup: Boolean;
        has_predictive_prefix_lookup: Boolean;
        has_particle_tail_lookup: Boolean;
        has_query_prefix_lookup: Boolean;
        predictive_prefix_results_loaded: Boolean;
        pair_evidence_loaded: Boolean;
        pair_evidence_results: TncPairPathEvidenceList;
        max_base_exact_competing_weight: Integer;
        max_base_exact_competing_text: string;
        short_exact_phase_start: UInt64;

        procedure note_short_exact_phase_local(const phase_name: string);
        var
            elapsed_ms: UInt64;
        begin
            if not m_config.debug_mode then
            begin
                Exit;
            end;

            elapsed_ms := nc_monotonic_tick_ms - short_exact_phase_start;
            if m_last_lookup_debug_extra <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
            end;
            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                Format('se_%s=%d', [phase_name, elapsed_ms]);
            short_exact_phase_start := nc_monotonic_tick_ms;
        end;

        function text_has_low_frequency_medical_unit_local(
            const text_value: string): Boolean;
        var
            char_idx_local: Integer;
        begin
            Result := False;
            for char_idx_local := 1 to Length(text_value) do
            begin
                case Ord(text_value[char_idx_local]) of
                    $6C28, // 氨
                    $708E, // 炎
                    $75C9, // 痉
                    $7624, // 瘤
                    $764C, // 癌
                    $80BC, // 肼
                    $80FA, // 胺
                    $8148, // 腈
                    $817A, // 腺
                    $82C4, // 苄
                    $916E, // 酮
                    $916F, // 酯
                    $9176, // 酶
                    $9187, // 醇
                    $919B, // 醛
                    $94F5: // 铵
                        Exit(True);
                end;
            end;
        end;

        function query_choice_rank_bonus_local(const text_value: string): Integer;
        var
            query_bonus_local: Integer;
            context_bonus_local: Integer;
            latest_text_local: string;
        begin
            Result := 0;
            if text_value = '' then
            begin
                Exit;
            end;
            if not is_full_pinyin_key(normalized_pinyin) then
            begin
                Exit;
            end;

            if ((m_session_query_latest_text <> nil) and
                m_session_query_latest_text.TryGetValue(normalized_pinyin,
                latest_text_local) and SameText(Trim(latest_text_local),
                text_value)) or is_latest_session_query_choice(text_value) then
            begin
                Inc(Result, 200000000);
            end;

            context_bonus_local := get_context_query_latest_bonus(text_value);
            if context_bonus_local > 0 then
            begin
                Inc(Result, 100000000 + (context_bonus_local * 1000));
            end;

            if m_dictionary <> nil then
            begin
                query_bonus_local := m_dictionary.get_query_choice_bonus(
                    normalized_pinyin, text_value);
                if query_bonus_local > 0 then
                begin
                    Inc(Result, 50000000 + (query_bonus_local * 1000));
                end;
            end;
        end;

        function has_effective_short_context_local: Boolean;
        begin
            Result := (Trim(m_segment_left_context) <> '') or
                (Trim(m_external_left_context) <> '') or
                (Trim(m_left_context) <> '');
        end;

        function transition_path_has_supported_function_head_local(
            const encoded_path: string): Boolean;
        var
            parts_local: TArray<string>;
            head_text_local: string;
            normalized_path_local: string;
        begin
            Result := False;
            normalized_path_local := Trim(encoded_path);
            parts_local := normalized_path_local.Split(
                [c_segment_path_separator]);
            if Length(parts_local) <> 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(parts_local[0]);
            if get_candidate_text_unit_count(head_text_local) <> 1 then
            begin
                Exit;
            end;
            { Limit exact overrides to productive grammar heads. The broader
              transition candidate remains visible, but nominal fragments such
              as "文+道" or "行+和" cannot displace a real dictionary word. }
            case Ord(head_text_local[1]) of
                $4E0D, $518D, $5728, $9AD8, $5F88, $592A, $6700, $66F4,
                $53C8, $4E5F, $5C06, $53EF, $4F1A, $80FD, $8981,
                $60F3, $6CA1, $65E0, $975E, $672A, $4ECD, $90FD, $8FD8,
                $624D, $5C31, $53EA, $522B, $591A, $5C11, $5168, $8D85,
                $771F, $633A, $8F83, $628A, $88AB, $8BA9, $7ED9, $5411,
                $5BF9, $4ECE, $4E8E, $662F,
                $7B2C, $5148, $6211, $4F60, $60A8,
                $8FD9, $90A3, $54EA, // 这、那、哪
                $4ED6, $5979, $5B83, $54B1: // 他、她、它、咱
                    Result := True;
            end;
        end;

        function transition_has_decisive_char_lm_lead_local(
            const transition_text, base_exact_text: string;
            out full_lm_lead, suffix_lm_lead: Integer): Boolean;
        var
            texts_local: TArray<string>;
            full_scores_local: TArray<Integer>;
            suffix_scores_local: TArray<Integer>;
        begin
            Result := False;
            full_lm_lead := 0;
            suffix_lm_lead := 0;
            if has_effective_short_context_local or
                (Trim(transition_text) = '') or
                (Trim(base_exact_text) = '') or (m_dictionary = nil) then
            begin
                Exit;
            end;

            SetLength(texts_local, 2);
            texts_local[0] := Trim(base_exact_text);
            texts_local[1] := Trim(transition_text);
            if (not get_cached_char_lm_scores(texts_local, full_scores_local,
                clsm_full, '')) or (Length(full_scores_local) <> 2) or
                (not get_cached_char_lm_scores(texts_local,
                suffix_scores_local, clsm_suffix, '')) or
                (Length(suffix_scores_local) <> 2) then
            begin
                Exit;
            end;

            full_lm_lead := full_scores_local[1] - full_scores_local[0];
            suffix_lm_lead := suffix_scores_local[1] - suffix_scores_local[0];
            Result := (full_lm_lead >=
                c_decisive_transition_min_full_lm_lead) and
                (suffix_lm_lead >=
                c_decisive_transition_min_suffix_lm_lead);
        end;

        function get_supported_pair_evidence_local(const text_value: string;
            out encoded_path: string): Integer;
        var
            evidence_idx_local: Integer;
            parts_local: TArray<string>;
            candidate_text_local: string;
            raw_evidence_local: Integer;
            normalized_path_local: string;
        begin
            Result := 0;
            encoded_path := '';
            if (text_value = '') or
                ((not is_current_short_two_single_pair_candidate_supported(
                normalized_pinyin, text_value)) and
                (not is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, text_value))) then
            begin
                Exit;
            end;

            if SameText(normalized_pinyin, m_short_three_exact_pair_query) and
                (m_short_three_char_pair_evidence > 0) then
            begin
                parts_local := m_short_three_char_pair_path.Split(
                    [c_segment_path_separator]);
                if (Length(parts_local) = 2) and
                    SameText(parts_local[0] + parts_local[1], text_value) then
                begin
                    encoded_path := m_short_three_char_pair_path;
                    Exit(m_short_three_char_pair_evidence);
                end;
            end;

            if not pair_evidence_loaded then
            begin
                pair_evidence_loaded := True;
                SetLength(pair_evidence_results, 0);
                m_dictionary.get_exact_pair_path_evidence(normalized_pinyin,
                    pair_evidence_results);
            end;

            for evidence_idx_local := 0 to High(pair_evidence_results) do
            begin
                normalized_path_local := Trim(pair_evidence_results[
                    evidence_idx_local].encoded_path);
                parts_local := normalized_path_local.Split(
                    [c_segment_path_separator]);
                if Length(parts_local) <> 2 then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(parts_local[0]) +
                    Trim(parts_local[1]);
                if not SameText(candidate_text_local, text_value) then
                begin
                    Continue;
                end;

                raw_evidence_local := Max(pair_evidence_results[
                    evidence_idx_local].lm_transition_weight,
                    pair_evidence_results[
                    evidence_idx_local].query_path_weight);
                if raw_evidence_local > Result then
                begin
                    Result := raw_evidence_local;
                    encoded_path := Trim(pair_evidence_results[
                        evidence_idx_local].encoded_path);
                end;
            end;
        end;

        function short_base_exact_dynamic_weight_local(
            const text_value: string): Integer;
        const
            c_min_repeated_choice_evidence = 320;
        begin
            Result := 0;
            if (text_value = '') or (m_dictionary = nil) or
                (not is_full_pinyin_key(normalized_pinyin)) then
            begin
                Exit;
            end;

            { The persisted score belongs to this exact pinyin/text pair, so it
              remains valid even when the engine has left context. Session-only
              recency, language models and scores learned for another candidate
              remain excluded. }
            Result := m_dictionary.get_query_choice_bonus(normalized_pinyin,
                text_value);
            if Result < c_min_repeated_choice_evidence then
            begin
                Result := 0;
            end;
        end;

        function short_exact_effective_weight_local(
            const candidate_value: TncCandidate): Integer;
        begin
            Result := display_candidate_effective_weight(candidate_value);
            Inc(Result, query_choice_rank_bonus_local(Trim(candidate_value.text)));
        end;

        function ensure_predictive_prefix_results_local: Boolean;
        begin
            if not predictive_prefix_results_loaded then
            begin
                predictive_prefix_results_loaded := True;
                if SameText(normalized_pinyin,
                    m_short_exact_lexicon_completion_query) then
                    predictive_prefix_results :=
                        Copy(m_short_exact_lexicon_completion_candidates)
                else if (m_dictionary = nil) or
                    (not m_dictionary.lookup_full_pinyin_prefix(normalized_pinyin,
                    predictive_prefix_results)) then
                begin
                    SetLength(predictive_prefix_results, 0);
                end;
            end;

            Result := Length(predictive_prefix_results) > 0;
        end;

        function short_base_exact_choice_rank_bonus_local(
            const text_value: string; const exact_weight_value: Integer;
            const text_units_value: Integer): Integer;
        const
            c_multi_base_choice_bonus_cap = 240;
            c_multi_base_choice_bonus_floor = 48;
            c_rare_exact_phrase_weight = 300;
            c_rare_unit_weight = 80;
            c_multi_base_strong_choice_min = 520;
        var
            raw_bonus_local: Integer;
            capped_bonus_local: Integer;
            latest_text_local: string;

            function has_rare_low_weight_unit_local: Boolean;
            var
                units_local: TArray<string>;
                unit_idx_local: Integer;
                unit_key_local: string;
                unit_weight_local: Integer;
                unit_results_local: TncCandidateList;
                unit_result_idx_local: Integer;
                unit_candidate_text_local: string;
            begin
                Result := False;
                if (m_dictionary = nil) or
                    (exact_weight_value >= c_rare_exact_phrase_weight) then
                begin
                    Exit;
                end;

                units_local := split_text_units(text_value);
                if Length(units_local) <> text_units_value then
                begin
                    Exit;
                end;

                for unit_idx_local := 0 to High(units_local) do
                begin
                    unit_key_local := build_display_query_key(unit_idx_local, 1);
                    if (unit_key_local = '') or
                        (not lookup_display_exact_cached(unit_key_local,
                        unit_results_local)) then
                    begin
                        Continue;
                    end;

                    unit_weight_local := 0;
                    for unit_result_idx_local := 0 to High(unit_results_local) do
                    begin
                        unit_candidate_text_local := Trim(
                            unit_results_local[unit_result_idx_local].text);
                        if (Trim(unit_results_local[unit_result_idx_local].comment) <> '') or
                            (not SameText(unit_candidate_text_local,
                            units_local[unit_idx_local])) or
                            (get_candidate_text_unit_count(unit_candidate_text_local) <> 1) then
                        begin
                            Continue;
                        end;

                        if unit_results_local[unit_result_idx_local].has_dict_weight then
                        begin
                            unit_weight_local :=
                                unit_results_local[unit_result_idx_local].dict_weight;
                        end
                        else
                        begin
                            unit_weight_local :=
                                unit_results_local[unit_result_idx_local].score;
                        end;
                        Break;
                    end;

                    if (unit_weight_local > 0) and
                        (unit_weight_local < c_rare_unit_weight) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := 0;
            if text_value = '' then
            begin
                Exit;
            end;
            if (exact_weight_value <= c_low_frequency_medical_exact_weight) and
                text_has_low_frequency_medical_unit_local(text_value) then
            begin
                Exit;
            end;

            // Single-character query learning must stay strong and persistent.
            // For multi-character base exact entries, choice history is only a
            // tie-breaker; it must not let a low-frequency base word outrank
            // much stronger exact dictionary matches after an accidental pick.
            if text_units_value <= 1 then
            begin
                Exit(query_choice_rank_bonus_local(text_value));
            end;

            if not is_full_pinyin_key(normalized_pinyin) then
            begin
                Exit;
            end;

            if ((m_session_query_latest_text <> nil) and
                m_session_query_latest_text.TryGetValue(normalized_pinyin,
                latest_text_local) and SameText(Trim(latest_text_local),
                text_value)) or is_latest_session_query_choice(text_value) then
            begin
                if not has_rare_low_weight_unit_local then
                begin
                    Exit(query_choice_rank_bonus_local(text_value));
                end;
            end;

            if m_dictionary = nil then
            begin
                Exit;
            end;

            raw_bonus_local := m_dictionary.get_query_choice_bonus(
                normalized_pinyin, text_value);
            if raw_bonus_local <= 0 then
            begin
                Exit;
            end;

            capped_bonus_local := exact_weight_value div 2;
            if capped_bonus_local < c_multi_base_choice_bonus_floor then
            begin
                capped_bonus_local := c_multi_base_choice_bonus_floor;
            end;
            if capped_bonus_local > c_multi_base_choice_bonus_cap then
            begin
                capped_bonus_local := c_multi_base_choice_bonus_cap;
            end;

            if (raw_bonus_local >= c_multi_base_strong_choice_min) and
                (not has_rare_low_weight_unit_local) then
            begin
                Exit(50000000 + (raw_bonus_local * 1000));
            end;

            Result := Min(raw_bonus_local, capped_bonus_local);
        end;

        function exact_candidate_is_user_local(
            const candidate_value: TncCandidate; const text_value: string): Boolean;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (text_value = '') then
            begin
                Exit;
            end;

            Result := m_dictionary.is_user_entry(normalized_pinyin, text_value);
        end;

        function exact_candidate_is_reliable_complete_local(
            const candidate_value: TncCandidate; const text_value: string): Boolean;
        const
            c_min_supported_exact_dict_weight = 160;
        var
            latest_text_local: string;
        begin
            Result := exact_candidate_is_user_local(candidate_value, text_value);
            if Result then
            begin
                Exit;
            end;

            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (text_value = '') then
            begin
                Exit(False);
            end;

            if (m_session_query_latest_text <> nil) and
                m_session_query_latest_text.TryGetValue(normalized_pinyin,
                latest_text_local) and SameText(Trim(latest_text_local),
                text_value) then
            begin
                Exit(True);
            end;

            if is_latest_session_query_choice(text_value) or
                (get_context_query_latest_bonus(text_value) > 0) or
                (m_dictionary.get_query_choice_bonus(normalized_pinyin,
                text_value) > 0) then
            begin
                Exit(True);
            end;

            Result := m_dictionary.is_base_entry(normalized_pinyin,
                text_value);
            if Result then
            begin
                Exit;
            end;

            Result := (candidate_value.source = cs_rule) and
                candidate_value.has_dict_weight and
                (candidate_value.dict_weight >= c_min_supported_exact_dict_weight);
        end;

        procedure apply_short_nocontext_reranker_local;
        const
            c_weak_residual_score_margin = 20000000;
            c_weak_residual_min_full_lm_gain = 600;
        var
            context_value_local: string;
            candidate_texts_local: TArray<string>;
            candidate_weights_local: TArray<Integer>;
            candidate_display_scores_local: TArray<Integer>;
            candidate_full_lm_scores_local: TArray<Integer>;
            candidate_suffix_lm_scores_local: TArray<Integer>;
            candidate_list_indices_local: TArray<Integer>;
            seen_texts_local: TDictionary<string, Boolean>;
            list_idx_local: Integer;
            candidate_idx_local: Integer;
            best_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            best_full_lm_local: Integer;
            best_suffix_lm_local: Integer;
            best_score_local: Int64;
            candidate_score_local: Int64;
            features_local: TncShortNoContextFeatures;
            rank_item_local: TShortExactRankItem;

            function effective_left_context_local: string;
            begin
                Result := Trim(m_left_context);
                if Trim(m_segment_left_context) <> '' then
                begin
                    Result := Trim(m_segment_left_context);
                end;
                if (Trim(m_segment_left_context) = '') and
                    (Trim(m_external_left_context) <> '') then
                begin
                    Result := Trim(m_external_left_context);
                end;
                Result := context_model_tail(Result);
            end;

            procedure build_features_local(const candidate_index_local: Integer;
                out output_local: TncShortNoContextFeatures);
            var
                candidate_units_local: TArray<string>;
                top_units_local: TArray<string>;
                compare_idx_local: Integer;
                compare_limit_local: Integer;
                different_local: Integer;
                same_prefix_local: Integer;
                same_suffix_local: Integer;
                candidate_log_weight_local: Double;
                candidate_log_display_local: Double;
                top_log_weight_local: Double;
                top_log_display_local: Double;
            begin
                FillChar(output_local, SizeOf(output_local), 0);
                candidate_units_local := split_text_units(
                    candidate_texts_local[candidate_index_local]);
                top_units_local := split_text_units(candidate_texts_local[0]);
                compare_limit_local := Min(Length(candidate_units_local),
                    Length(top_units_local));
                different_local := Abs(Length(candidate_units_local) -
                    Length(top_units_local));
                for compare_idx_local := 0 to compare_limit_local - 1 do
                begin
                    if candidate_units_local[compare_idx_local] <>
                        top_units_local[compare_idx_local] then
                    begin
                        Inc(different_local);
                    end;
                end;
                same_prefix_local := 0;
                while (same_prefix_local < compare_limit_local) and
                    (candidate_units_local[same_prefix_local] =
                    top_units_local[same_prefix_local]) do
                begin
                    Inc(same_prefix_local);
                end;
                same_suffix_local := 0;
                while (same_suffix_local <
                    compare_limit_local - same_prefix_local) and
                    (candidate_units_local[High(candidate_units_local) -
                    same_suffix_local] = top_units_local[High(top_units_local) -
                    same_suffix_local]) do
                begin
                    Inc(same_suffix_local);
                end;

                candidate_log_weight_local := Ln(1.0 + Max(0,
                    candidate_weights_local[candidate_index_local]));
                candidate_log_display_local := Ln(1.0 + Max(0,
                    candidate_display_scores_local[candidate_index_local]));
                top_log_weight_local := Ln(1.0 + Max(0,
                    candidate_weights_local[0]));
                top_log_display_local := Ln(1.0 + Max(0,
                    candidate_display_scores_local[0]));

                output_local.candidate_rank := candidate_index_local + 1;
                output_local.candidate_full_lm :=
                    candidate_full_lm_scores_local[candidate_index_local];
                output_local.candidate_suffix_lm :=
                    candidate_suffix_lm_scores_local[candidate_index_local];
                output_local.candidate_log_dict_weight :=
                    candidate_log_weight_local;
                output_local.candidate_log_display_score :=
                    candidate_log_display_local;
                output_local.candidate_text_units :=
                    Length(candidate_units_local);
                output_local.query_units := expected_units;
                output_local.candidate_count := Length(candidate_texts_local);
                output_local.delta_full_lm_from_top :=
                    candidate_full_lm_scores_local[candidate_index_local] -
                    candidate_full_lm_scores_local[0];
                output_local.delta_suffix_lm_from_top :=
                    candidate_suffix_lm_scores_local[candidate_index_local] -
                    candidate_suffix_lm_scores_local[0];
                output_local.delta_dict_weight_from_top :=
                    candidate_weights_local[candidate_index_local] -
                    candidate_weights_local[0];
                output_local.delta_display_score_from_top :=
                    candidate_display_scores_local[candidate_index_local] -
                    candidate_display_scores_local[0];
                output_local.delta_log_dict_weight_from_top :=
                    candidate_log_weight_local - top_log_weight_local;
                output_local.delta_log_display_score_from_top :=
                    candidate_log_display_local - top_log_display_local;
                output_local.delta_full_lm_from_best :=
                    candidate_full_lm_scores_local[candidate_index_local] -
                    best_full_lm_local;
                output_local.delta_suffix_lm_from_best :=
                    candidate_suffix_lm_scores_local[candidate_index_local] -
                    best_suffix_lm_local;
                output_local.candidate_is_full_lm_best := Ord(
                    candidate_full_lm_scores_local[candidate_index_local] =
                    best_full_lm_local);
                output_local.candidate_is_suffix_lm_best := Ord(
                    candidate_suffix_lm_scores_local[candidate_index_local] =
                    best_suffix_lm_local);
                output_local.different_units_from_top := different_local;
                output_local.same_prefix_units := same_prefix_local;
                output_local.same_suffix_units := same_suffix_local;
                output_local.top_full_lm := candidate_full_lm_scores_local[0];
                output_local.top_suffix_lm := candidate_suffix_lm_scores_local[0];
                output_local.top_log_dict_weight := top_log_weight_local;
                output_local.top_log_display_score := top_log_display_local;
            end;

            function baseline_has_protected_name_shape_local(
                const challenger_index_local: Integer): Boolean;
            var
                baseline_units_local: TArray<string>;
                challenger_units_local: TArray<string>;
            begin
                Result := False;
                if (challenger_index_local <= 0) or
                    (challenger_index_local > High(candidate_texts_local)) then
                begin
                    Exit;
                end;
                baseline_units_local := split_text_units(candidate_texts_local[0]);
                challenger_units_local := split_text_units(
                    candidate_texts_local[challenger_index_local]);
                Result := (Length(baseline_units_local) = 2) and
                    (Length(challenger_units_local) = 2) and
                    (baseline_units_local[0] = challenger_units_local[0]) and
                    is_common_surname_text(baseline_units_local[0]);
            end;

        begin
            short_nocontext_promoted_exact_text := '';
            short_nocontext_promoted_exact_lead := 0;
            if (m_dictionary = nil) or has_user_exact or
                (expected_units < 2) or (expected_units > 4) or
                (Length(syllables) <> expected_units) or
                (not is_full_pinyin_key(normalized_pinyin)) then
            begin
                Exit;
            end;
            context_value_local := effective_left_context_local;
            if context_value_local <> '' then
            begin
                Exit;
            end;

            seen_texts_local := TDictionary<string, Boolean>.Create;
            try
                SetLength(candidate_texts_local, 0);
                SetLength(candidate_weights_local, 0);
                SetLength(candidate_display_scores_local, 0);
                SetLength(candidate_list_indices_local, 0);
                for list_idx_local := 0 to list.Count - 1 do
                begin
                    rank_item_local := list[list_idx_local];
                    if rank_item_local.category <> 1 then
                    begin
                        Continue;
                    end;
                    if Length(candidate_texts_local) >=
                        c_short_nocontext_max_candidate_rank then
                    begin
                        Break;
                    end;
                    if rank_item_local.low_frequency_medical_exact then
                    begin
                        Continue;
                    end;
                    candidate_text_local := Trim(rank_item_local.candidate.text);
                    if (candidate_text_local = '') or
                        seen_texts_local.ContainsKey(candidate_text_local) or
                        (not exact_weights.TryGetValue(candidate_text_local,
                        candidate_weight_local)) then
                    begin
                        Continue;
                    end;
                    if not short_residual_exact_weight_is_eligible(
                        candidate_weight_local,
                        Length(candidate_texts_local) = 0) then
                    begin
                        Continue;
                    end;
                    if query_choice_rank_bonus_local(candidate_text_local) > 0 then
                    begin
                        Exit;
                    end;

                    seen_texts_local.Add(candidate_text_local, True);
                    candidate_idx_local := Length(candidate_texts_local);
                    SetLength(candidate_texts_local, candidate_idx_local + 1);
                    SetLength(candidate_weights_local, candidate_idx_local + 1);
                    SetLength(candidate_display_scores_local,
                        candidate_idx_local + 1);
                    SetLength(candidate_list_indices_local,
                        candidate_idx_local + 1);
                    candidate_texts_local[candidate_idx_local] :=
                        candidate_text_local;
                    candidate_weights_local[candidate_idx_local] :=
                        candidate_weight_local;
                    candidate_display_scores_local[candidate_idx_local] :=
                        rank_item_local.candidate.score;
                    candidate_list_indices_local[candidate_idx_local] :=
                        list_idx_local;
                end;
                if Length(candidate_texts_local) < 2 then
                begin
                    Exit;
                end;
                if (not get_cached_char_lm_scores(candidate_texts_local,
                    candidate_full_lm_scores_local, clsm_full, '')) or
                    (Length(candidate_full_lm_scores_local) <>
                    Length(candidate_texts_local)) then
                begin
                    Exit;
                end;
                if (not get_cached_char_lm_scores(candidate_texts_local,
                    candidate_suffix_lm_scores_local, clsm_suffix, '')) or
                    (Length(candidate_suffix_lm_scores_local) <>
                    Length(candidate_texts_local)) then
                begin
                    Exit;
                end;

                best_full_lm_local := candidate_full_lm_scores_local[0];
                best_suffix_lm_local := candidate_suffix_lm_scores_local[0];
                for candidate_idx_local := 1 to High(candidate_texts_local) do
                begin
                    best_full_lm_local := Max(best_full_lm_local,
                        candidate_full_lm_scores_local[candidate_idx_local]);
                    best_suffix_lm_local := Max(best_suffix_lm_local,
                        candidate_suffix_lm_scores_local[candidate_idx_local]);
                end;

                best_idx_local := 0;
                best_score_local := Low(Int64);
                for candidate_idx_local := 1 to High(candidate_texts_local) do
                begin
                    build_features_local(candidate_idx_local, features_local);
                    candidate_score_local := short_nocontext_residual_score(
                        features_local);
                    if candidate_score_local > best_score_local then
                    begin
                        best_score_local := candidate_score_local;
                        best_idx_local := candidate_idx_local;
                    end;
                end;
                if (best_idx_local <= 0) or (best_score_local <
                    c_short_nocontext_promotion_threshold) then
                begin
                    Exit;
                end;
                { Without user or context evidence, preserve a decisive
                  dictionary gap only when the baseline also has a same-surname
                  two-character name shape. Raw weights alone remain too noisy
                  to veto the residual model. }
                if short_nocontext_large_weight_gap_blocks_promotion(
                    candidate_weights_local[0],
                    candidate_weights_local[best_idx_local],
                    baseline_has_protected_name_shape_local(best_idx_local)) then
                begin
                    Exit;
                end;
                { A weak residual alone must not displace a high-confidence
                  base exact with a lower-weight candidate. Require an
                  independent full-text LM gain for that override. }
                if (candidate_weights_local[0] >=
                    c_short_nocontext_protected_baseline_min_weight) and
                    (candidate_weights_local[best_idx_local] <
                    candidate_weights_local[0]) and
                    (best_score_local - c_short_nocontext_promotion_threshold <
                    c_weak_residual_score_margin) and
                    (Int64(candidate_full_lm_scores_local[best_idx_local]) -
                    candidate_full_lm_scores_local[0] <
                    c_weak_residual_min_full_lm_gain) then
                begin
                    Exit;
                end;

                short_nocontext_promoted_exact_text :=
                    candidate_texts_local[best_idx_local];
                short_nocontext_promoted_exact_lead := EnsureRange(
                    (best_score_local - c_short_nocontext_promotion_threshold)
                    div 100000, Low(Integer), High(Integer));
                rank_item_local := list[
                    candidate_list_indices_local[best_idx_local]];
                rank_item_local.nocontext_exact_priority := 1;
                list[candidate_list_indices_local[best_idx_local]] :=
                    rank_item_local;
            finally
                seen_texts_local.Free;
            end;
        end;

        procedure apply_short_context_reranker_local;
        const
            c_max_context_candidates = 9;
        var
            context_value_local: string;
            candidate_texts_local: TArray<string>;
            candidate_weights_local: TArray<Integer>;
            candidate_display_scores_local: TArray<Integer>;
            candidate_lm_scores_local: TArray<Integer>;
            candidate_list_indices_local: TArray<Integer>;
            seen_texts_local: TDictionary<string, Boolean>;
            list_idx_local: Integer;
            candidate_idx_local: Integer;
            baseline_idx_local: Integer;
            best_idx_local: Integer;
            first_stage_idx_local: Integer;
            residual_best_idx_local: Integer;
            final_idx_local: Integer;
            best_lm_idx_local: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            baseline_model_score_local: Double;
            candidate_model_score_local: Double;
            best_model_score_local: Double;
            model_lead_local: Double;
            residual_baseline_score_local: Double;
            residual_candidate_score_local: Double;
            residual_best_score_local: Double;
            residual_lead_local: Double;
            first_stage_promoted_local: Boolean;
            residual_promoted_local: Boolean;
            boundary_prefix_texts_local: TArray<string>;
            boundary_prefix_scores_local: TArray<Integer>;
            boundary_prefix1_scores_local: TArray<Integer>;
            boundary_prefix2_scores_local: TArray<Integer>;
            boundary_features_local: TncShortContextBoundaryFeatures;
            boundary_best_idx_local: Integer;
            boundary_idx_local: Integer;
            boundary_rank_local: Integer;
            boundary_best_score_local: Int64;
            boundary_score_local: Int64;
            boundary_lead_local: Int64;
            boundary_full_best_local: Integer;
            boundary_prefix1_best_local: Integer;
            boundary_prefix2_best_local: Integer;
            boundary_different_local: Integer;
            boundary_same_prefix_local: Integer;
            boundary_same_suffix_local: Integer;
            boundary_compare_limit_local: Integer;
            boundary_promoted_local: Boolean;
            difference_features_local: TncShortContextDifferenceFeatures;
            difference_stage1_features_local:
                TncShortContextDifferenceFeatures;
            difference_top_idx_local: Integer;
            difference_second_idx_local: Integer;
            difference_nocontext_top_idx_local: Integer;
            difference_stage1_top_idx_local: Integer;
            difference_stage1_second_idx_local: Integer;
            difference_best_idx_local: Integer;
            difference_idx_local: Integer;
            difference_rank_local: Integer;
            difference_score_local: Int64;
            difference_best_score_local: Int64;
            difference_runner_up_score_local: Int64;
            difference_best_probability_local: Double;
            difference_runner_up_probability_local: Double;
            difference_stage1_promoted_local: Boolean;
            difference_stage2_promoted_local: Boolean;
            difference_stage1_features_valid_local: Boolean;
            top2_difference_score_local: Int64;
            top2_difference_threshold_local: Int64;
            top2_difference_promoted_local: Boolean;
            rank_item_local: TShortExactRankItem;

            function effective_left_context_local: string;
            begin
                Result := Trim(m_left_context);
                if Trim(m_segment_left_context) <> '' then
                begin
                    Result := Trim(m_segment_left_context);
                end;
                if (Trim(m_segment_left_context) = '') and
                    (Trim(m_external_left_context) <> '') then
                begin
                    Result := Trim(m_external_left_context);
                end;
                Result := context_model_tail(Result);
            end;

            function rank_after_context_local(const candidate_index_local,
                promoted_index_local: Integer): Integer;
            begin
                if candidate_index_local = promoted_index_local then
                begin
                    Exit(1);
                end;
                Result := candidate_index_local + 1;
                if (promoted_index_local > 0) and
                    (candidate_index_local < promoted_index_local) then
                begin
                    Inc(Result);
                end;
            end;

            procedure build_boundary_features_local(
                const candidate_index_local, top_index_local: Integer;
                out features_local: TncShortContextBoundaryFeatures);
            var
                candidate_text_inner: string;
                top_text_inner: string;
                compare_idx_inner: Integer;
                compare_limit_inner: Integer;
            begin
                FillChar(features_local, SizeOf(features_local), 0);
                candidate_text_inner := candidate_texts_local[
                    candidate_index_local];
                top_text_inner := candidate_texts_local[top_index_local];
                boundary_different_local := Abs(Length(candidate_text_inner) -
                    Length(top_text_inner));
                compare_limit_inner := Min(Length(candidate_text_inner),
                    Length(top_text_inner));
                for compare_idx_inner := 1 to compare_limit_inner do
                begin
                    if candidate_text_inner[compare_idx_inner] <>
                        top_text_inner[compare_idx_inner] then
                    begin
                        Inc(boundary_different_local);
                    end;
                end;
                boundary_same_prefix_local := 0;
                while (boundary_same_prefix_local < compare_limit_inner) and
                    (candidate_text_inner[boundary_same_prefix_local + 1] =
                    top_text_inner[boundary_same_prefix_local + 1]) do
                begin
                    Inc(boundary_same_prefix_local);
                end;
                boundary_same_suffix_local := 0;
                while (boundary_same_suffix_local <
                    compare_limit_inner - boundary_same_prefix_local) and
                    (candidate_text_inner[Length(candidate_text_inner) -
                    boundary_same_suffix_local] = top_text_inner[
                    Length(top_text_inner) - boundary_same_suffix_local]) do
                begin
                    Inc(boundary_same_suffix_local);
                end;

                boundary_rank_local := rank_after_context_local(
                    candidate_index_local, top_index_local);
                features_local.candidate_rank_context := boundary_rank_local;
                features_local.candidate_rank_nocontext :=
                    candidate_index_local + 1;
                features_local.candidate_full_lm :=
                    candidate_lm_scores_local[candidate_index_local];
                features_local.candidate_prefix1_lm :=
                    boundary_prefix1_scores_local[candidate_index_local];
                features_local.candidate_prefix2_lm :=
                    boundary_prefix2_scores_local[candidate_index_local];
                features_local.candidate_log_dict_weight := Ln(1.0 + Max(0,
                    candidate_weights_local[candidate_index_local]));
                features_local.candidate_log_display_score := Ln(1.0 + Max(0,
                    candidate_display_scores_local[candidate_index_local]));
                features_local.candidate_text_units :=
                    Length(candidate_text_inner);
                features_local.context_units := Length(context_value_local);
                features_local.candidate_count := Length(candidate_texts_local);
                features_local.candidate_is_current_top := 0.0;
                features_local.delta_full_lm_from_top :=
                    candidate_lm_scores_local[candidate_index_local] -
                    candidate_lm_scores_local[top_index_local];
                features_local.delta_prefix1_lm_from_top :=
                    boundary_prefix1_scores_local[candidate_index_local] -
                    boundary_prefix1_scores_local[top_index_local];
                features_local.delta_prefix2_lm_from_top :=
                    boundary_prefix2_scores_local[candidate_index_local] -
                    boundary_prefix2_scores_local[top_index_local];
                features_local.delta_dict_weight_from_top :=
                    candidate_weights_local[candidate_index_local] -
                    candidate_weights_local[top_index_local];
                features_local.delta_display_score_from_top :=
                    candidate_display_scores_local[candidate_index_local] -
                    candidate_display_scores_local[top_index_local];
                features_local.delta_full_lm_from_best :=
                    candidate_lm_scores_local[candidate_index_local] -
                    boundary_full_best_local;
                features_local.delta_prefix1_lm_from_best :=
                    boundary_prefix1_scores_local[candidate_index_local] -
                    boundary_prefix1_best_local;
                features_local.delta_prefix2_lm_from_best :=
                    boundary_prefix2_scores_local[candidate_index_local] -
                    boundary_prefix2_best_local;
                features_local.candidate_is_full_lm_best := Ord(
                    candidate_lm_scores_local[candidate_index_local] =
                    boundary_full_best_local);
                features_local.candidate_is_prefix1_lm_best := Ord(
                    boundary_prefix1_scores_local[candidate_index_local] =
                    boundary_prefix1_best_local);
                features_local.candidate_is_prefix2_lm_best := Ord(
                    boundary_prefix2_scores_local[candidate_index_local] =
                    boundary_prefix2_best_local);
                features_local.candidate_context_rank_gain :=
                    candidate_index_local + 1 - boundary_rank_local;
                features_local.different_units_from_top :=
                    boundary_different_local;
                features_local.same_prefix_units :=
                    boundary_same_prefix_local;
                features_local.same_suffix_units :=
                    boundary_same_suffix_local;
            end;

            function difference_pair_eligible_local(
                const top_index_local,
                challenger_index_local: Integer): Boolean;
            begin
                Result :=
                    (Length(candidate_texts_local[top_index_local]) =
                    Length(candidate_texts_local[challenger_index_local])) and
                    m_dictionary.is_base_entry(normalized_pinyin,
                    candidate_texts_local[top_index_local]) and
                    m_dictionary.is_base_entry(normalized_pinyin,
                    candidate_texts_local[challenger_index_local]);
            end;

            function difference_candidate_precedes_local(
                const left_index_local,
                right_index_local: Integer): Boolean;
            var
                left_item_local: TShortExactRankItem;
                right_item_local: TShortExactRankItem;
            begin
                left_item_local := list[
                    candidate_list_indices_local[left_index_local]];
                right_item_local := list[
                    candidate_list_indices_local[right_index_local]];
                if left_item_local.low_frequency_medical_exact <>
                    right_item_local.low_frequency_medical_exact then
                begin
                    Exit(not left_item_local.low_frequency_medical_exact);
                end;
                if left_item_local.nocontext_exact_priority <>
                    right_item_local.nocontext_exact_priority then
                begin
                    Exit(left_item_local.nocontext_exact_priority >
                        right_item_local.nocontext_exact_priority);
                end;
                if left_item_local.rank_score <> right_item_local.rank_score then
                begin
                    Exit(left_item_local.rank_score > right_item_local.rank_score);
                end;
                if left_item_local.original_index <>
                    right_item_local.original_index then
                begin
                    Exit(left_item_local.original_index <
                        right_item_local.original_index);
                end;
                if left_item_local.text_length <> right_item_local.text_length then
                begin
                    Exit(left_item_local.text_length < right_item_local.text_length);
                end;
                Result := CompareText(left_item_local.candidate.text,
                    right_item_local.candidate.text) < 0;
            end;

            procedure build_difference_features_local(
                const top_index_local, challenger_index_local,
                challenger_rank_local: Integer;
                const use_stage2_evidence_local: Boolean;
                out features_local: TncShortContextDifferenceFeatures);
            var
                top_text_inner: string;
                challenger_text_inner: string;
                compare_index_inner: Integer;
                compare_limit_inner: Integer;
                different_inner: Integer;
                prefix_inner: Integer;
                suffix_inner: Integer;
            begin
                FillChar(features_local, SizeOf(features_local), 0);
                top_text_inner := candidate_texts_local[top_index_local];
                challenger_text_inner := candidate_texts_local[
                    challenger_index_local];
                compare_limit_inner := Min(Length(top_text_inner),
                    Length(challenger_text_inner));
                different_inner := Abs(Length(top_text_inner) -
                    Length(challenger_text_inner));
                for compare_index_inner := 1 to compare_limit_inner do
                begin
                    if top_text_inner[compare_index_inner] <>
                        challenger_text_inner[compare_index_inner] then
                    begin
                        Inc(different_inner);
                    end;
                end;
                prefix_inner := 0;
                while (prefix_inner < compare_limit_inner) and
                    (top_text_inner[prefix_inner + 1] =
                    challenger_text_inner[prefix_inner + 1]) do
                begin
                    Inc(prefix_inner);
                end;
                suffix_inner := 0;
                while (suffix_inner < compare_limit_inner - prefix_inner) and
                    (top_text_inner[Length(top_text_inner) - suffix_inner] =
                    challenger_text_inner[Length(challenger_text_inner) -
                    suffix_inner]) do
                begin
                    Inc(suffix_inner);
                end;

                features_local.challenger_rank := challenger_rank_local;
                features_local.delta_full_lm :=
                    candidate_lm_scores_local[challenger_index_local] -
                    candidate_lm_scores_local[top_index_local];
                features_local.delta_prefix1_lm :=
                    boundary_prefix1_scores_local[challenger_index_local] -
                    boundary_prefix1_scores_local[top_index_local];
                features_local.delta_prefix2_lm :=
                    boundary_prefix2_scores_local[challenger_index_local] -
                    boundary_prefix2_scores_local[top_index_local];
                features_local.delta_dict_weight :=
                    candidate_weights_local[challenger_index_local] -
                    candidate_weights_local[top_index_local];
                features_local.delta_display_score :=
                    candidate_display_scores_local[challenger_index_local] -
                    candidate_display_scores_local[top_index_local];
                features_local.top_full_lm :=
                    candidate_lm_scores_local[top_index_local];
                features_local.challenger_full_lm :=
                    candidate_lm_scores_local[challenger_index_local];
                features_local.top_prefix1_lm :=
                    boundary_prefix1_scores_local[top_index_local];
                features_local.challenger_prefix1_lm :=
                    boundary_prefix1_scores_local[challenger_index_local];
                features_local.top_prefix2_lm :=
                    boundary_prefix2_scores_local[top_index_local];
                features_local.challenger_prefix2_lm :=
                    boundary_prefix2_scores_local[challenger_index_local];
                features_local.top_log_dict_weight := Ln(1.0 + Max(0,
                    candidate_weights_local[top_index_local]));
                features_local.challenger_log_dict_weight := Ln(1.0 + Max(0,
                    candidate_weights_local[challenger_index_local]));
                features_local.context_units := Min(12,
                    Length(context_value_local));
                features_local.query_units := Length(normalized_pinyin);
                features_local.top_text_units := Length(top_text_inner);
                features_local.challenger_text_units :=
                    Length(challenger_text_inner);
                features_local.different_units := different_inner;
                features_local.same_prefix_units := prefix_inner;
                features_local.same_suffix_units := suffix_inner;
                features_local.candidate_count :=
                    Length(candidate_texts_local);
                if use_stage2_evidence_local then
                begin
                    short_context_difference_fill_stage2_evidence(
                        context_value_local, top_text_inner,
                        challenger_text_inner, features_local);
                end
                else
                begin
                    short_context_difference_fill_stage1_evidence(
                        context_value_local, top_text_inner,
                        challenger_text_inner, features_local);
                end;
            end;

            function difference_probability_local(
                const score_local: Int64): Double;
            var
                scaled_score_local: Double;
                exponential_local: Double;
            begin
                scaled_score_local := score_local /
                    c_short_context_difference_score_scale;
                if scaled_score_local >= 0.0 then
                begin
                    Result := 1.0 / (1.0 + Exp(-scaled_score_local));
                end
                else
                begin
                    exponential_local := Exp(scaled_score_local);
                    Result := exponential_local / (1.0 + exponential_local);
                end;
            end;

        begin
            short_context_promoted_exact_text := '';
            short_context_promoted_exact_lead := 0;
            context_value_local := effective_left_context_local;
            if (context_value_local = '') or (m_dictionary = nil) or
                has_user_exact or (list.Count < 2) then
            begin
                Exit;
            end;

            seen_texts_local := TDictionary<string, Boolean>.Create;
            try
                SetLength(candidate_texts_local, 0);
                SetLength(candidate_weights_local, 0);
                SetLength(candidate_display_scores_local, 0);
                SetLength(candidate_list_indices_local, 0);
                for list_idx_local := 0 to list.Count - 1 do
                begin
                    rank_item_local := list[list_idx_local];
                    if rank_item_local.category <> 1 then
                    begin
                        Continue;
                    end;
                    if Length(candidate_texts_local) >= c_max_context_candidates then
                    begin
                        Break;
                    end;

                    candidate_text_local := Trim(rank_item_local.candidate.text);
                    if (candidate_text_local = '') or
                        seen_texts_local.ContainsKey(candidate_text_local) or
                        (not exact_weights.TryGetValue(candidate_text_local,
                        candidate_weight_local)) then
                    begin
                        Continue;
                    end;

                    if not short_residual_exact_weight_is_eligible(
                        candidate_weight_local,
                        Length(candidate_texts_local) = 0) then
                    begin
                        Continue;
                    end;

                    if query_choice_rank_bonus_local(candidate_text_local) > 0 then
                    begin
                        Exit;
                    end;

                    seen_texts_local.Add(candidate_text_local, True);
                    candidate_idx_local := Length(candidate_texts_local);
                    SetLength(candidate_texts_local, candidate_idx_local + 1);
                    SetLength(candidate_weights_local, candidate_idx_local + 1);
                    SetLength(candidate_display_scores_local,
                        candidate_idx_local + 1);
                    SetLength(candidate_list_indices_local,
                        candidate_idx_local + 1);
                    candidate_texts_local[candidate_idx_local] :=
                        candidate_text_local;
                    candidate_weights_local[candidate_idx_local] :=
                        candidate_weight_local;
                    candidate_display_scores_local[candidate_idx_local] :=
                        rank_item_local.candidate.score;
                    candidate_list_indices_local[candidate_idx_local] :=
                        list_idx_local;
                end;

                if Length(candidate_texts_local) < 2 then
                begin
                    Exit;
                end;
                if (not get_cached_char_lm_scores(candidate_texts_local,
                    candidate_lm_scores_local, clsm_short_context,
                    context_value_local)) or
                    (Length(candidate_lm_scores_local) <>
                    Length(candidate_texts_local)) then
                begin
                    Exit;
                end;

                baseline_idx_local := 0;
                best_lm_idx_local := 0;
                for candidate_idx_local := 1 to High(candidate_texts_local) do
                begin
                    if candidate_lm_scores_local[candidate_idx_local] >
                        candidate_lm_scores_local[best_lm_idx_local] then
                    begin
                        best_lm_idx_local := candidate_idx_local;
                    end;
                end;

                best_idx_local := baseline_idx_local;
                baseline_model_score_local := short_context_reranker_score(
                    context_value_local,
                    candidate_texts_local[baseline_idx_local], 1,
                    candidate_lm_scores_local[baseline_idx_local],
                    candidate_weights_local[baseline_idx_local],
                    candidate_display_scores_local[baseline_idx_local], True, 0,
                    0, candidate_lm_scores_local[baseline_idx_local] -
                    candidate_lm_scores_local[best_lm_idx_local],
                    baseline_idx_local = best_lm_idx_local,
                    Length(candidate_texts_local), Length(context_value_local),
                    Length(candidate_texts_local[baseline_idx_local]));
                best_model_score_local := baseline_model_score_local;
                for candidate_idx_local := 1 to High(candidate_texts_local) do
                begin
                    candidate_model_score_local := short_context_reranker_score(
                        context_value_local,
                        candidate_texts_local[candidate_idx_local],
                        candidate_idx_local + 1,
                        candidate_lm_scores_local[candidate_idx_local],
                        candidate_weights_local[candidate_idx_local],
                        candidate_display_scores_local[candidate_idx_local], False,
                        candidate_lm_scores_local[candidate_idx_local] -
                        candidate_lm_scores_local[baseline_idx_local],
                        candidate_weights_local[candidate_idx_local] -
                        candidate_weights_local[baseline_idx_local],
                        candidate_lm_scores_local[candidate_idx_local] -
                        candidate_lm_scores_local[best_lm_idx_local],
                        candidate_idx_local = best_lm_idx_local,
                        Length(candidate_texts_local), Length(context_value_local),
                        Length(candidate_texts_local[candidate_idx_local]));
                    if candidate_model_score_local > best_model_score_local then
                    begin
                        best_idx_local := candidate_idx_local;
                        best_model_score_local := candidate_model_score_local;
                    end;
                end;

                model_lead_local := best_model_score_local -
                    baseline_model_score_local;
                first_stage_promoted_local :=
                    (best_idx_local <> baseline_idx_local) and
                    (model_lead_local >=
                    c_short_context_reranker_promotion_margin);
                first_stage_idx_local := baseline_idx_local;
                if first_stage_promoted_local then
                begin
                    first_stage_idx_local := best_idx_local;
                end;

                residual_baseline_score_local := short_context_residual_score(
                    context_value_local,
                    candidate_texts_local[first_stage_idx_local],
                    first_stage_idx_local + 1,
                    candidate_lm_scores_local[first_stage_idx_local],
                    candidate_weights_local[first_stage_idx_local],
                    candidate_display_scores_local[first_stage_idx_local], True,
                    0, 0, candidate_lm_scores_local[first_stage_idx_local] -
                    candidate_lm_scores_local[best_lm_idx_local],
                    first_stage_idx_local = best_lm_idx_local,
                    Length(candidate_texts_local), Length(context_value_local),
                    Length(candidate_texts_local[first_stage_idx_local]));
                residual_best_idx_local := first_stage_idx_local;
                residual_best_score_local := residual_baseline_score_local;
                for candidate_idx_local := 0 to High(candidate_texts_local) do
                begin
                    if candidate_idx_local = first_stage_idx_local then
                    begin
                        Continue;
                    end;
                    residual_candidate_score_local :=
                        short_context_residual_score(context_value_local,
                        candidate_texts_local[candidate_idx_local],
                        candidate_idx_local + 1,
                        candidate_lm_scores_local[candidate_idx_local],
                        candidate_weights_local[candidate_idx_local],
                        candidate_display_scores_local[candidate_idx_local], False,
                        candidate_lm_scores_local[candidate_idx_local] -
                        candidate_lm_scores_local[first_stage_idx_local],
                        candidate_weights_local[candidate_idx_local] -
                        candidate_weights_local[first_stage_idx_local],
                        candidate_lm_scores_local[candidate_idx_local] -
                        candidate_lm_scores_local[best_lm_idx_local],
                        candidate_idx_local = best_lm_idx_local,
                        Length(candidate_texts_local), Length(context_value_local),
                        Length(candidate_texts_local[candidate_idx_local]));
                    if residual_candidate_score_local >
                        residual_best_score_local then
                    begin
                        residual_best_idx_local := candidate_idx_local;
                        residual_best_score_local :=
                            residual_candidate_score_local;
                    end;
                end;

                residual_lead_local := residual_best_score_local -
                    residual_baseline_score_local;
                residual_promoted_local :=
                    (residual_best_idx_local <> first_stage_idx_local) and
                    (residual_lead_local >=
                    c_short_context_residual_promotion_margin);
                final_idx_local := first_stage_idx_local;
                if residual_promoted_local then
                begin
                    final_idx_local := residual_best_idx_local;
                end;

                { The whole-word continuation average can hide a strong
                  context boundary. A final conservative residual compares
                  the first character and first two characters of existing
                  exact candidates; it never creates candidates. }
                boundary_promoted_local := False;
                difference_stage1_promoted_local := False;
                difference_stage2_promoted_local := False;
                difference_stage1_features_valid_local := False;
                top2_difference_promoted_local := False;
                difference_top_idx_local := final_idx_local;
                boundary_best_idx_local := final_idx_local;
                boundary_best_score_local := Low(Int64);
                boundary_lead_local := 0;
                SetLength(boundary_prefix_texts_local,
                    Length(candidate_texts_local) * 2);
                for candidate_idx_local := 0 to High(candidate_texts_local) do
                begin
                    boundary_prefix_texts_local[candidate_idx_local * 2] :=
                        Copy(candidate_texts_local[candidate_idx_local], 1, 1);
                    boundary_prefix_texts_local[candidate_idx_local * 2 + 1] :=
                        Copy(candidate_texts_local[candidate_idx_local], 1, 2);
                end;
                if get_cached_char_lm_scores(boundary_prefix_texts_local,
                    boundary_prefix_scores_local, clsm_short_context,
                    context_value_local) and
                    (Length(boundary_prefix_scores_local) =
                    Length(boundary_prefix_texts_local)) then
                begin
                    SetLength(boundary_prefix1_scores_local,
                        Length(candidate_texts_local));
                    SetLength(boundary_prefix2_scores_local,
                        Length(candidate_texts_local));
                    boundary_full_best_local := Low(Integer);
                    boundary_prefix1_best_local := Low(Integer);
                    boundary_prefix2_best_local := Low(Integer);
                    for candidate_idx_local := 0 to High(candidate_texts_local) do
                    begin
                        boundary_prefix1_scores_local[candidate_idx_local] :=
                            boundary_prefix_scores_local[
                            candidate_idx_local * 2];
                        boundary_prefix2_scores_local[candidate_idx_local] :=
                            boundary_prefix_scores_local[
                            candidate_idx_local * 2 + 1];
                        boundary_full_best_local := Max(
                            boundary_full_best_local,
                            candidate_lm_scores_local[candidate_idx_local]);
                        boundary_prefix1_best_local := Max(
                            boundary_prefix1_best_local,
                            boundary_prefix1_scores_local[candidate_idx_local]);
                        boundary_prefix2_best_local := Max(
                            boundary_prefix2_best_local,
                            boundary_prefix2_scores_local[candidate_idx_local]);
                    end;
                    boundary_compare_limit_local := Min(
                        Length(candidate_texts_local),
                        c_short_context_boundary_max_candidate_rank);
                    for boundary_idx_local := 0 to
                        boundary_compare_limit_local - 1 do
                    begin
                        if boundary_idx_local = final_idx_local then
                        begin
                            Continue;
                        end;
                        build_boundary_features_local(boundary_idx_local,
                            final_idx_local, boundary_features_local);
                        boundary_score_local :=
                            short_context_boundary_residual_score(
                            boundary_features_local);
                        if boundary_score_local > boundary_best_score_local then
                        begin
                            boundary_best_score_local := boundary_score_local;
                            boundary_best_idx_local := boundary_idx_local;
                        end;
                    end;
                    if (boundary_best_idx_local <> final_idx_local) and
                        (boundary_best_score_local >=
                        c_short_context_boundary_promotion_threshold) then
                    begin
                        final_idx_local := boundary_best_idx_local;
                        boundary_promoted_local := True;
                        boundary_lead_local := boundary_best_score_local -
                            c_short_context_boundary_promotion_threshold;
                    end;

                    { Compare the final current winner against existing exact
                      alternatives. The first pass only considers the logical
                      second candidate. A stricter second pass considers the
                      remaining candidates only when the first pass abstains. }
                    difference_top_idx_local := final_idx_local;
                    difference_nocontext_top_idx_local := 0;
                    for difference_idx_local := 1 to
                        High(candidate_texts_local) do
                    begin
                        if difference_candidate_precedes_local(
                            difference_idx_local,
                            difference_nocontext_top_idx_local) then
                        begin
                            difference_nocontext_top_idx_local :=
                                difference_idx_local;
                        end;
                    end;
                    difference_second_idx_local := -1;
                    for difference_idx_local := 0 to
                        High(candidate_texts_local) do
                    begin
                        if difference_idx_local <>
                            difference_top_idx_local then
                        begin
                            difference_second_idx_local :=
                                difference_idx_local;
                            Break;
                        end;
                    end;
                    difference_score_local := Low(Int64);
                    if (difference_second_idx_local >= 0) and
                        difference_pair_eligible_local(
                        difference_top_idx_local,
                        difference_second_idx_local) and
                        short_context_difference_has_positive_signal(
                        context_value_local,
                        candidate_texts_local[difference_top_idx_local],
                        candidate_texts_local[
                        difference_second_idx_local]) then
                    begin
                        build_difference_features_local(
                            difference_top_idx_local,
                            difference_second_idx_local, 2, False,
                            difference_features_local);
                        difference_stage1_features_local :=
                            difference_features_local;
                        difference_stage1_top_idx_local :=
                            difference_top_idx_local;
                        difference_stage1_second_idx_local :=
                            difference_second_idx_local;
                        difference_stage1_features_valid_local := True;
                        difference_score_local :=
                            short_context_difference_stage1_score(
                            difference_features_local);
                        if difference_score_local >=
                            c_short_context_difference_stage1_threshold then
                        begin
                            final_idx_local := difference_second_idx_local;
                            difference_stage1_promoted_local := True;
                        end;
                    end;

                    if not difference_stage1_promoted_local then
                    begin
                        difference_best_idx_local := -1;
                        difference_best_score_local := Low(Int64);
                        difference_runner_up_score_local := Low(Int64);
                        difference_rank_local := 1;
                        for difference_idx_local := 0 to
                            High(candidate_texts_local) do
                        begin
                            if difference_idx_local =
                                difference_top_idx_local then
                            begin
                                Continue;
                            end;
                            Inc(difference_rank_local);
                            if difference_idx_local =
                                difference_second_idx_local then
                            begin
                                Continue;
                            end;
                            if not difference_pair_eligible_local(
                                difference_top_idx_local,
                                difference_idx_local) then
                            begin
                                Continue;
                            end;
                            if not
                                short_context_difference_has_positive_mined_evidence(
                                context_value_local,
                                candidate_texts_local[
                                difference_top_idx_local],
                                candidate_texts_local[difference_idx_local]) then
                            begin
                                Continue;
                            end;
                            build_difference_features_local(
                                difference_top_idx_local,
                                difference_idx_local,
                                difference_rank_local, True,
                                difference_features_local);
                            difference_score_local :=
                                short_context_difference_stage2_score(
                                difference_features_local);
                            if difference_score_local >
                                difference_best_score_local then
                            begin
                                difference_runner_up_score_local :=
                                    difference_best_score_local;
                                difference_best_score_local :=
                                    difference_score_local;
                                difference_best_idx_local :=
                                    difference_idx_local;
                            end
                            else if difference_score_local >
                                difference_runner_up_score_local then
                            begin
                                difference_runner_up_score_local :=
                                    difference_score_local;
                            end;
                        end;
                        if (difference_best_idx_local >= 0) and
                            (difference_best_score_local >=
                            c_short_context_difference_stage2_threshold) then
                        begin
                            difference_best_probability_local :=
                                difference_probability_local(
                                difference_best_score_local);
                            if difference_runner_up_score_local =
                                Low(Int64) then
                            begin
                                difference_runner_up_probability_local := 0.0;
                            end
                            else
                            begin
                                difference_runner_up_probability_local :=
                                    difference_probability_local(
                                    difference_runner_up_score_local);
                            end;
                            if difference_best_probability_local -
                                difference_runner_up_probability_local >=
                                c_short_context_difference_stage2_lead_threshold then
                            begin
                                final_idx_local := difference_best_idx_local;
                                difference_stage2_promoted_local := True;
                                difference_score_local :=
                                    difference_best_score_local;
                            end;
                        end;
                    end;

                    { Preserve all existing context stages, then calibrate only
                      the final first and second exact candidates. This pass
                      cannot add, remove, or promote a deeper candidate. }
                    difference_top_idx_local := final_idx_local;
                    difference_second_idx_local := -1;
                    for difference_idx_local := 0 to
                        High(candidate_texts_local) do
                    begin
                        if (difference_idx_local <>
                            difference_top_idx_local) and
                            ((difference_second_idx_local < 0) or
                            difference_candidate_precedes_local(
                            difference_idx_local,
                            difference_second_idx_local)) then
                        begin
                            difference_second_idx_local :=
                                difference_idx_local;
                        end;
                    end;
                    top2_difference_score_local := Low(Int64);
                    top2_difference_threshold_local :=
                        c_short_context_top2_override_threshold;
                    if (difference_second_idx_local >= 0) and
                        difference_pair_eligible_local(
                        difference_top_idx_local,
                        difference_second_idx_local) then
                    begin
                        if (difference_second_idx_local =
                            difference_nocontext_top_idx_local) and
                            (difference_top_idx_local <>
                            difference_nocontext_top_idx_local) then
                        begin
                            top2_difference_threshold_local :=
                                c_short_context_top2_restore_threshold;
                        end
                        else
                        begin
                            { Replacing the original no-context winner needs
                              stronger evidence than restoring it. }
                            top2_difference_threshold_local :=
                                c_short_context_top2_override_threshold;
                        end;
                        if difference_stage1_features_valid_local and
                            (difference_top_idx_local =
                            difference_stage1_top_idx_local) and
                            (difference_second_idx_local =
                            difference_stage1_second_idx_local) then
                        begin
                            difference_features_local :=
                                difference_stage1_features_local;
                        end
                        else
                        begin
                            build_difference_features_local(
                                difference_top_idx_local,
                                difference_second_idx_local, 2, False,
                                difference_features_local);
                        end;
                        short_context_top2_overlay_evidence(
                            context_value_local,
                            candidate_texts_local[
                            difference_top_idx_local],
                            candidate_texts_local[
                            difference_second_idx_local],
                            difference_features_local);
                        { Long-sentence search reuses this ranker for internal
                          chunks, so lower-support evidence is short-only. }
                        if (expected_units >= 2) and (expected_units <= 4) then
                        begin
                            short_context_top2_overlay_expanded_evidence(
                                context_value_local,
                                candidate_texts_local[
                                difference_top_idx_local],
                                candidate_texts_local[
                                difference_second_idx_local],
                                difference_features_local);
                        end;
                        if short_context_top2_passes(
                            difference_features_local,
                            top2_difference_threshold_local,
                            top2_difference_score_local) then
                        begin
                            final_idx_local :=
                                difference_second_idx_local;
                            top2_difference_promoted_local := True;
                        end;
                    end;

                end;

                if (final_idx_local <> baseline_idx_local) or
                    top2_difference_promoted_local then
                begin
                    short_context_promoted_exact_text :=
                        candidate_texts_local[final_idx_local];
                    if top2_difference_promoted_local then
                    begin
                        short_context_promoted_exact_lead := EnsureRange(
                            (top2_difference_score_local -
                            top2_difference_threshold_local) div
                            100000, Low(Integer), High(Integer));
                    end
                    else if difference_stage1_promoted_local then
                    begin
                        short_context_promoted_exact_lead := EnsureRange(
                            (difference_score_local -
                            c_short_context_difference_stage1_threshold) div
                            100000, Low(Integer), High(Integer));
                    end
                    else if difference_stage2_promoted_local then
                    begin
                        short_context_promoted_exact_lead := EnsureRange(
                            (difference_score_local -
                            c_short_context_difference_stage2_threshold) div
                            100000, Low(Integer), High(Integer));
                    end
                    else if boundary_promoted_local then
                    begin
                        short_context_promoted_exact_lead := EnsureRange(
                            boundary_lead_local div 100000, Low(Integer),
                            High(Integer));
                    end
                    else if residual_promoted_local then
                    begin
                        short_context_promoted_exact_lead := Round(
                            residual_lead_local * 1000.0);
                    end
                    else
                    begin
                        short_context_promoted_exact_lead := Round(
                            model_lead_local * 1000.0);
                    end;
                    rank_item_local := list[
                        candidate_list_indices_local[final_idx_local]];
                    if top2_difference_promoted_local then
                    begin
                        rank_item_local.context_exact_priority := 3;
                    end
                    else
                    begin
                        rank_item_local.context_exact_priority := 1;
                    end;
                    list[candidate_list_indices_local[final_idx_local]] :=
                        rank_item_local;
                    if top2_difference_promoted_local then
                    begin
                        rank_item_local := list[
                            candidate_list_indices_local[
                            difference_top_idx_local]];
                        rank_item_local.context_exact_priority := 2;
                        list[candidate_list_indices_local[
                            difference_top_idx_local]] := rank_item_local;
                    end;
                end;

                if m_config.debug_mode then
                begin
                    if first_stage_promoted_local then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('shortctxrr=%s:%.3f>%s:%.3f',
                            [candidate_texts_local[baseline_idx_local],
                            baseline_model_score_local,
                            candidate_texts_local[first_stage_idx_local],
                            best_model_score_local]);
                    end;
                    if residual_promoted_local then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('shortctxres=%s:%.3f>%s:%.3f',
                            [candidate_texts_local[first_stage_idx_local],
                            residual_baseline_score_local,
                            candidate_texts_local[residual_best_idx_local],
                            residual_best_score_local]);
                    end;
                    if boundary_promoted_local and
                        (not difference_stage1_promoted_local) and
                        (not difference_stage2_promoted_local) then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('shortctxboundary=%s:%d',
                            [candidate_texts_local[final_idx_local],
                            short_context_promoted_exact_lead]);
                    end;
                    if difference_stage1_promoted_local or
                        difference_stage2_promoted_local then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('shortctxdiff%d=%s:%d',
                            [1 + Ord(difference_stage2_promoted_local),
                            candidate_texts_local[final_idx_local],
                            short_context_promoted_exact_lead]);
                    end;
                    if top2_difference_promoted_local then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('shortctxtop2=%s:%d',
                            [candidate_texts_local[final_idx_local],
                            short_context_promoted_exact_lead]);
                    end;
                end;
            finally
                seen_texts_local.Free;
            end;
        end;

        function try_get_closed_particle_combo_candidate_local(
            out out_candidate: TncCandidate): Boolean;
        var
            de_text: string;
            dehao_text: string;
            le_text: string;
            ba_text: string;
            ma_text: string;
            candidate_text_local: string;
            candidate_weight_local: Integer;
        begin
            Result := False;
            FillChar(out_candidate, SizeOf(out_candidate), 0);
            if (expected_units <> 2) or (Length(syllables) <> 2) or
                (not is_full_pinyin_key(normalized_pinyin)) then
            begin
                Exit;
            end;

            de_text := string(Char($7684));
            dehao_text := string(Char($5F97)) + string(Char($597D));
            le_text := string(Char($4E86));
            ba_text := string(Char($5427));
            if m_config.dictionary_variant = dv_traditional then
            begin
                ma_text := string(Char($55CE));
            end
            else
            begin
                ma_text := string(Char($5417));
            end;

            candidate_text_local := '';
            candidate_weight_local := 760;
            if SameText(normalized_pinyin, 'dema') then
            begin
                candidate_text_local := de_text + ma_text;
                candidate_weight_local := 860;
            end
            else if SameText(normalized_pinyin, 'deba') then
            begin
                candidate_text_local := de_text + ba_text;
                candidate_weight_local := 760;
            end
            else if SameText(normalized_pinyin, 'dele') then
            begin
                candidate_text_local := de_text + le_text;
                candidate_weight_local := 240;
            end
            else if SameText(normalized_pinyin, 'dehao') then
            begin
                candidate_text_local := dehao_text;
                candidate_weight_local := 760;
            end
            else if SameText(normalized_pinyin, 'leba') then
            begin
                candidate_text_local := le_text + ba_text;
                candidate_weight_local := 760;
            end
            else if SameText(normalized_pinyin, 'lema') then
            begin
                candidate_text_local := le_text + ma_text;
                candidate_weight_local := 860;
            end;

            if candidate_text_local = '' then
            begin
                Exit;
            end;

            out_candidate.text := candidate_text_local;
            out_candidate.comment := '';
            out_candidate.score := candidate_weight_local;
            out_candidate.source := cs_rule;
            out_candidate.has_dict_weight := True;
            out_candidate.dict_weight := candidate_weight_local;
            Result := True;
        end;

        function short_complete_rank_score_local(
            const candidate_value: TncCandidate; const text_value: string;
            const persistent_user: Boolean): Integer;
        begin
            Result := candidate_value.score;
            if is_full_pinyin_key(normalized_pinyin) and
                candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;

            if persistent_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
            Inc(Result, query_choice_rank_bonus_local(text_value));
        end;

        function prefix_candidate_cache_key_local(const text_value: string;
            const tail_key: string): string;
        begin
            Result := Trim(text_value) + #9 +
                normalize_pinyin_text(Trim(tail_key));
        end;

        function prefix_candidate_matches_query_local(
            const text_value: string; const text_units: Integer;
            const tail_key: string): Boolean;
        var
            prefix_key: string;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            prefix_text_local: string;
        begin
            Result := False;
            if (text_value = '') or (text_units <= 0) or
                (text_units >= expected_units) or (tail_key = '') or
                (text_units + get_effective_compact_pinyin_unit_count(tail_key) <>
                expected_units) then
            begin
                Exit;
            end;

            prefix_key := build_display_query_key(0, text_units);
            if prefix_key = '' then
            begin
                Exit;
            end;

            if text_units = 1 then
            begin
                if lookup_display_complete_cached(prefix_key,
                    prefix_results_local) then
                begin
                    for prefix_idx_local := 0 to High(prefix_results_local) do
                    begin
                        prefix_text_local := Trim(
                            prefix_results_local[prefix_idx_local].text);
                        if (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                            (get_candidate_text_unit_count(prefix_text_local) = 1) and
                            SameText(prefix_text_local, text_value) then
                        begin
                            Exit(True);
                        end;
                    end;
                    Exit(False);
                end;

                if m_dictionary.single_char_matches_pinyin(prefix_key,
                    text_value) then
                begin
                    Exit(True);
                end;

                Exit(False);
            end;

            Result := display_exact_key_has_text(prefix_key, text_value);
        end;

        function find_existing_display_candidate_local(
            const text_value: string; const comment_value: string): Integer;
        var
            find_idx: Integer;
        begin
            Result := -1;
            for find_idx := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[find_idx].text), text_value) and
                    SameText(Trim(m_candidates[find_idx].comment), comment_value) then
                begin
                    Exit(find_idx);
                end;
            end;
        end;

        function predictive_prefix_units_allowed_local(
            const units_value: Integer): Boolean;
        begin
            Result := False;
            if (units_value <= 0) or (expected_units <= 0) then
            begin
                Exit;
            end;

            if (expected_units <= 3) and (units_value = expected_units) then
            begin
                Exit(True);
            end;

            if (expected_units = 4) and (units_value = 3) then
            begin
                Exit(True);
            end;
        end;

        procedure append_missing_exact_candidates_local;
        var
            map_pair: TPair<string, TncCandidate>;
            existing_idx_local: Integer;
            append_idx_local: Integer;
            exact_candidate_local: TncCandidate;
        begin
            for map_pair in exact_candidates do
            begin
                existing_idx_local := find_existing_display_candidate_local(
                    map_pair.Key, '');
                if existing_idx_local >= 0 then
                begin
                    if exact_is_user.ContainsKey(map_pair.Key) and
                        exact_is_user[map_pair.Key] then
                    begin
                        m_candidates[existing_idx_local].source := cs_user;
                        m_candidates[existing_idx_local].has_dict_weight := False;
                        m_candidates[existing_idx_local].dict_weight := 0;
                    end;
                    Continue;
                end;

                exact_candidate_local := map_pair.Value;
                append_idx_local := Length(m_candidates);
                SetLength(m_candidates, append_idx_local + 1);
                m_candidates[append_idx_local] := exact_candidate_local;
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(m_candidates));
                end;
                m_candidate_segment_paths[append_idx_local] := '';
            end;
        end;

        procedure append_query_prefix_candidates_local;
        const
            c_prefix_phrase_probe_limit = 12;
            c_prefix_single_probe_limit = c_candidate_total_limit_max;
        var
            prefix_units_local: Integer;
            prefix_idx_local: Integer;
            prefix_probe_limit_local: Integer;
            append_idx_local: Integer;
            existing_idx_local: Integer;
            prefix_key_local: string;
            tail_key_local: string;
            prefix_text_local: string;
            prefix_results_local: TncCandidateList;
            prefix_candidate_local: TncCandidate;
        begin
            for prefix_units_local := 1 to expected_units - 1 do
            begin
                prefix_key_local := build_display_query_key(0,
                    prefix_units_local);
                tail_key_local := build_display_query_key(prefix_units_local,
                    expected_units - prefix_units_local);
                if (prefix_key_local = '') or (tail_key_local = '') then
                begin
                    Continue;
                end;

                if prefix_units_local = 1 then
                begin
                    if not lookup_display_complete_cached(prefix_key_local,
                        prefix_results_local) then
                    begin
                        Continue;
                    end;
                end
                else if not lookup_display_complete_cached(prefix_key_local,
                    prefix_results_local) then
                begin
                    Continue;
                end;

                if (prefix_units_local = 1) and (expected_units = 2) then
                begin
                    prefix_probe_limit_local := c_prefix_single_probe_limit;
                end
                else
                begin
                    prefix_probe_limit_local := c_prefix_phrase_probe_limit;
                end;
                for prefix_idx_local := 0 to High(prefix_results_local) do
                begin
                    if prefix_idx_local >= prefix_probe_limit_local then
                    begin
                        Break;
                    end;

                    prefix_text_local := Trim(
                        prefix_results_local[prefix_idx_local].text);
                    if (prefix_text_local = '') or
                        (Trim(prefix_results_local[prefix_idx_local].comment) <> '') or
                        (get_candidate_text_unit_count(prefix_text_local) <>
                        prefix_units_local) then
                    begin
                        Continue;
                    end;

                    existing_idx_local := find_existing_display_candidate_local(
                        prefix_text_local, tail_key_local);
                    if existing_idx_local >= 0 then
                    begin
                        Continue;
                    end;

                    prefix_candidate_keys.AddOrSetValue(
                        prefix_candidate_cache_key_local(prefix_text_local,
                        tail_key_local), True);

                    prefix_candidate_local :=
                        prefix_results_local[prefix_idx_local];
                    prefix_candidate_local.text := prefix_text_local;
                    prefix_candidate_local.comment := tail_key_local;
                    append_idx_local := Length(m_candidates);
                    SetLength(m_candidates, append_idx_local + 1);
                    m_candidates[append_idx_local] := prefix_candidate_local;
                    if Length(m_candidate_segment_paths) < Length(m_candidates) then
                    begin
                        SetLength(m_candidate_segment_paths,
                            Length(m_candidates));
                    end;
                    m_candidate_segment_paths[append_idx_local] := '';
                end;
            end;
        end;

        procedure append_predictive_pinyin_prefix_candidates_local;
        const
            c_predictive_probe_limit = 12;
        var
            prefix_idx_local: Integer;
            accepted_count_local: Integer;
            append_idx_local: Integer;
            existing_idx_local: Integer;
            prefix_text_local: string;
            prefix_units_local: Integer;
            prefix_candidate_local: TncCandidate;
        begin
            if not ensure_predictive_prefix_results_local then
            begin
                Exit;
            end;

            accepted_count_local := 0;
            for prefix_idx_local := 0 to High(predictive_prefix_results) do
            begin
                if accepted_count_local >= c_predictive_probe_limit then
                begin
                    Break;
                end;

                prefix_text_local := Trim(
                    predictive_prefix_results[prefix_idx_local].text);
                if (prefix_text_local = '') or
                    (Trim(predictive_prefix_results[prefix_idx_local].comment) <> '') or
                    exact_weights.ContainsKey(prefix_text_local) then
                begin
                    Continue;
                end;

                prefix_units_local :=
                    get_candidate_text_unit_count(prefix_text_local);
                if not predictive_prefix_units_allowed_local(prefix_units_local) then
                begin
                    Continue;
                end;

                predictive_prefix_texts.AddOrSetValue(prefix_text_local, True);
                existing_idx_local := find_existing_display_candidate_local(
                    prefix_text_local, '');
                if existing_idx_local >= 0 then
                begin
                    Continue;
                end;

                prefix_candidate_local := predictive_prefix_results[prefix_idx_local];
                prefix_candidate_local.text := prefix_text_local;
                prefix_candidate_local.comment := '';
                append_idx_local := Length(m_candidates);
                SetLength(m_candidates, append_idx_local + 1);
                m_candidates[append_idx_local] := prefix_candidate_local;
                Inc(accepted_count_local);
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths,
                        Length(m_candidates));
                end;
                m_candidate_segment_paths[append_idx_local] := '';
            end;
        end;

        procedure append_particle_tail_completion_candidates_local;
        const
            c_head_probe_limit = 8;
            c_particle_tail_min_head_score = 80;
            c_particle_tail_visible_bonus = 160;
            c_particle_tail_unit_bonus = 24;
            c_particle_tail_weak_bonus = 8;
        var
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_units_local: Integer;
            head_results_local: TncCandidateList;
            head_idx_local: Integer;
            append_idx_local: Integer;
            existing_idx_local: Integer;
            generated_text_local: string;
            head_text_local: string;
            head_score_local: Integer;
            head_dict_weight_local: Integer;
            generated_candidate_local: TncCandidate;

            function derived_particle_tail_score_local(
                const base_score: Integer): Integer;
            begin
                Result := base_score;
                if Result < 0 then
                begin
                    Result := 0;
                end;
                if Result < c_particle_tail_min_head_score then
                begin
                    Inc(Result, c_particle_tail_weak_bonus);
                    Exit;
                end;

                Inc(Result, c_particle_tail_visible_bonus +
                    (head_units_local * c_particle_tail_unit_bonus));
            end;
        begin
            if (not try_get_short_particle_tail_query_parts(normalized_pinyin,
                tail_key_local, tail_text_local, head_key_local)) or
                (not SameText(tail_key_local, 'a')) then
            begin
                Exit;
            end;

            if head_key_local = '' then
            begin
                Exit;
            end;

            head_units_local :=
                get_effective_compact_pinyin_unit_count(head_key_local);
            if (head_units_local < 2) or (head_units_local > 3) or
                (not lookup_display_exact_cached(head_key_local,
                head_results_local)) then
            begin
                Exit;
            end;

            for head_idx_local := 0 to High(head_results_local) do
            begin
                if head_idx_local >= c_head_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results_local[head_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                head_text_local := Trim(head_results_local[head_idx_local].text);
                if (head_text_local = '') or
                    (get_candidate_text_unit_count(head_text_local) <>
                    head_units_local) or
                    SameText(Copy(head_text_local, Length(head_text_local), 1),
                    tail_text_local) then
                begin
                    Continue;
                end;

                generated_text_local := head_text_local + tail_text_local;
                if exact_weights.ContainsKey(generated_text_local) then
                begin
                    Continue;
                end;
                particle_tail_texts.AddOrSetValue(generated_text_local, True);
                existing_idx_local := find_existing_display_candidate_local(
                    generated_text_local, '');
                if existing_idx_local >= 0 then
                begin
                    Continue;
                end;

                head_score_local := head_results_local[head_idx_local].score;
                head_dict_weight_local := head_score_local;
                if head_results_local[head_idx_local].has_dict_weight and
                    (head_results_local[head_idx_local].dict_weight >
                    head_score_local) then
                begin
                    head_score_local :=
                        head_results_local[head_idx_local].dict_weight;
                    head_dict_weight_local :=
                        head_results_local[head_idx_local].dict_weight;
                end;
                if head_results_local[head_idx_local].source = cs_user then
                begin
                    Inc(head_score_local, c_user_score_bonus);
                end;

                FillChar(generated_candidate_local,
                    SizeOf(generated_candidate_local), 0);
                generated_candidate_local.text := generated_text_local;
                generated_candidate_local.comment := '';
                generated_candidate_local.source := cs_rule;
                generated_candidate_local.score :=
                    derived_particle_tail_score_local(head_score_local);
                generated_candidate_local.has_dict_weight := True;
                generated_candidate_local.dict_weight :=
                    head_dict_weight_local;

                append_idx_local := Length(m_candidates);
                SetLength(m_candidates, append_idx_local + 1);
                m_candidates[append_idx_local] := generated_candidate_local;
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths,
                        Length(m_candidates));
                end;
                m_candidate_segment_paths[append_idx_local] := '';
            end;
        end;

        function has_predictive_pinyin_prefix_candidate_local: Boolean;
        var
            prefix_idx_local: Integer;
            prefix_text_local: string;
        begin
            Result := False;
            if expected_units > 3 then
            begin
                Exit;
            end;

            if not ensure_predictive_prefix_results_local then
            begin
                Exit;
            end;

            for prefix_idx_local := 0 to High(predictive_prefix_results) do
            begin
                prefix_text_local := Trim(
                    predictive_prefix_results[prefix_idx_local].text);
                if (prefix_text_local <> '') and
                    (Trim(predictive_prefix_results[prefix_idx_local].comment) = '') and
                    predictive_prefix_units_allowed_local(
                    get_candidate_text_unit_count(prefix_text_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_query_prefix_exact_candidate_local: Boolean;
        var
            prefix_units_local: Integer;
            prefix_key_local: string;
            prefix_idx_local: Integer;
            prefix_text_local: string;
            prefix_results_local: TncCandidateList;
            fixed_head_pinyin_local: string;
            fixed_head_text_local: string;
            fixed_tail_key_local: string;
            fixed_tail_units_local: Integer;
        begin
            Result := False;
            // This is only a visibility fallback for a complete three-syllable
            // query such as nianqing/shi. Existing fixed-head and particle-tail
            // routes already provide stronger, purpose-built completions.
            if (expected_units <> 3) or
                (not is_full_pinyin_key(normalized_pinyin)) or
                has_particle_tail_lookup or
                ((Length(normalized_pinyin) > Length('dehao')) and
                SameText(Copy(normalized_pinyin,
                Length(normalized_pinyin) - Length('dehao') + 1,
                Length('dehao')), 'dehao')) or
                try_get_fixed_pronoun_tail_query_parts(normalized_pinyin,
                expected_units, fixed_head_pinyin_local,
                fixed_head_text_local, fixed_tail_key_local,
                fixed_tail_units_local) then
            begin
                Exit;
            end;

            for prefix_units_local := expected_units - 1 downto 2 do
            begin
                prefix_key_local := build_display_query_key(0,
                    prefix_units_local);
                if (prefix_key_local = '') or
                    (not lookup_display_complete_cached(prefix_key_local,
                    prefix_results_local)) then
                begin
                    Continue;
                end;

                for prefix_idx_local := 0 to High(prefix_results_local) do
                begin
                    prefix_text_local := Trim(
                        prefix_results_local[prefix_idx_local].text);
                    if (prefix_text_local <> '') and
                        (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                        (get_candidate_text_unit_count(prefix_text_local) =
                        prefix_units_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function has_particle_tail_completion_candidate_local: Boolean;
        const
            c_head_probe_limit = 8;
        var
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_units_local: Integer;
            head_results_local: TncCandidateList;
            head_idx_local: Integer;
            head_text_local: string;
        begin
            Result := False;
            if (not try_get_short_particle_tail_query_parts(normalized_pinyin,
                tail_key_local, tail_text_local, head_key_local)) or
                (not SameText(tail_key_local, 'a')) then
            begin
                Exit;
            end;

            if head_key_local = '' then
            begin
                Exit;
            end;

            head_units_local :=
                get_effective_compact_pinyin_unit_count(head_key_local);
            if (head_units_local < 2) or (head_units_local > 3) or
                (not lookup_display_exact_cached(head_key_local,
                head_results_local)) then
            begin
                Exit;
            end;

            for head_idx_local := 0 to High(head_results_local) do
            begin
                if head_idx_local >= c_head_probe_limit then
                begin
                    Break;
                end;
                if Trim(head_results_local[head_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                head_text_local := Trim(head_results_local[head_idx_local].text);
                if (head_text_local <> '') and
                    (get_candidate_text_unit_count(head_text_local) =
                    head_units_local) and
                    (not SameText(Copy(head_text_local,
                    Length(head_text_local), 1), tail_text_local)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_is_actual_full_exact_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_value: string;
        begin
            Result := False;
            text_value := Trim(candidate_value.text);
            if (text_value = '') or (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(text_value) <> expected_units) or
                (m_dictionary = nil) then
            begin
                Exit;
            end;

            Result := m_dictionary.is_base_entry(normalized_pinyin, text_value) or
                m_dictionary.is_user_entry(normalized_pinyin, text_value);
        end;

        procedure add_rank_item_local(const candidate_value: TncCandidate;
            const path_text: string; const original_idx: Integer);
        var
            local_text: string;
            local_comment: string;
            local_units: Integer;
            local_comment_units: Integer;
            local_exact_weight: Integer;
            local_exact_user: Boolean;
            local_tail_key: string;
            local_full_complete: Boolean;
            local_direct_exact: Boolean;
            local_supported_exact_pair: Boolean;
            local_preserved_lm_prefix: Boolean;
            local_predictive_prefix: Boolean;
            local_lexicon_completion: Boolean;
            local_particle_tail_rule: Boolean;
            local_fuzzy_exact: Boolean;
            local_direct_exact_rank: Integer;
            local_transition_path: string;
            local_transition_evidence: Integer;
            local_transition_full_lm_lead: Integer;
            local_transition_suffix_lm_lead: Integer;

            function is_number_unit_local(const unit_value: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(unit_value) <> 1 then
                begin
                    Exit;
                end;

                case Ord(unit_value[1]) of
                    $4E00, $4E8C, $4E24, $4E09, $56DB, $4E94, $516D,
                    $4E03, $516B, $4E5D, $5341:
                        Result := True;
                end;
            end;

            function is_quantity_or_unit_tail_local(
                const unit_value: string): Boolean;
            begin
                Result := False;
                if get_candidate_text_unit_count(unit_value) <> 1 then
                begin
                    Exit;
                end;

                case Ord(unit_value[1]) of
                    $4E2A, $500B, $6B21, $6761, $689D, $5F20, $5F35,
                    $672C, $7247, $6B65, $8F6E, $8F2A, $56DE, $4E0B,
                    $9879, $9805, $5B57, $53F0, $81FA, $95F4, $9593,
                    $5957, $623F, $5385, $5EF3, $536B, $885B, $95E8,
                    $9580, $5B54, $5F00, $958B, $80A1, $9875, $9801,
                    $5E45, $526F, $5757, $584A, $6BDB, $5143, $4E07,
                    $680B, $68DF:
                        Result := True;
                end;
            end;

            function is_numeric_quantity_partial_noise_local: Boolean;
            var
                units_local: TArray<string>;
            begin
                Result := False;
                if (local_comment = '') or (local_units <> 2) or
                    (local_comment_units <> 1) or
                    is_full_pinyin_key(local_tail_key) then
                begin
                    Exit;
                end;

                units_local := split_text_units(local_text);
                if Length(units_local) <> 2 then
                begin
                    Exit;
                end;

                Result := is_number_unit_local(units_local[0]) and
                    is_quantity_or_unit_tail_local(units_local[1]);
            end;
        begin
            local_text := Trim(candidate_value.text);
            local_comment := Trim(candidate_value.comment);
            if local_text = '' then
            begin
                Exit;
            end;

            local_units := get_candidate_text_unit_count(local_text);
            local_tail_key := normalize_pinyin_text(local_comment);
            local_comment_units := 0;
            if local_tail_key <> '' then
            begin
                local_comment_units :=
                    get_effective_compact_pinyin_unit_count(local_tail_key);
            end;

            item.candidate := candidate_value;
            item.candidate.text := local_text;
            item.candidate.comment := local_comment;
            item.path := path_text;
            item.original_index := original_idx;
            item.text_length := Length(local_text);
            item.actual_full_exact := candidate_is_actual_full_exact_local(
                candidate_value);
            item.user_full_exact := False;
            item.decisive_transition_override := False;
            item.transition_evidence := 0;
            item.low_frequency_medical_exact := False;
            item.nocontext_exact_priority := 0;
            item.context_exact_priority := 0;
            item.prefix_units := 0;
            item.rank_score := display_candidate_effective_weight(
                candidate_value);
            item.category := 6;

            local_predictive_prefix :=
                predictive_prefix_texts.ContainsKey(local_text);
            local_lexicon_completion :=
                is_current_short_exact_lexicon_completion(normalized_pinyin,
                local_text);
            local_particle_tail_rule :=
                particle_tail_texts.ContainsKey(local_text);
            local_fuzzy_exact := is_fuzzy_pinyin_active and
                (candidate_value.fuzzy_cost > 0) and
                (local_comment = '') and (local_units = expected_units);
            local_full_complete := (local_comment = '') and
                ((local_units = expected_units) or
                local_particle_tail_rule or
                local_lexicon_completion or
                (local_predictive_prefix and
                predictive_prefix_units_allowed_local(local_units)));
            local_preserved_lm_prefix := (local_comment <> '') and
                m_last_lookup_incremental_partial_reuse and
                (candidate_value.source = cs_rule) and
                (candidate_value.display_kind = cdk_lm_compound) and
                (local_units >= 3) and
                (local_units + local_comment_units = expected_units);
            if local_full_complete then
            begin
                local_direct_exact := exact_weights.ContainsKey(local_text);
                local_supported_exact_pair :=
                    is_current_short_two_single_pair_candidate_supported(
                    normalized_pinyin, local_text) or
                    is_current_short_three_exact_pair_candidate_supported(
                    normalized_pinyin, local_text);
                local_exact_user := False;
                exact_is_user.TryGetValue(local_text, local_exact_user);
                if not local_exact_user then
                begin
                    local_exact_user := exact_candidate_is_user_local(
                        candidate_value, local_text);
                end;
                if local_exact_user then
                begin
                    item.low_frequency_medical_exact := False;
                end;
                item.user_full_exact := local_direct_exact and local_exact_user;

                if local_fuzzy_exact then
                begin
                    item.category := 2;
                    item.rank_score := display_candidate_effective_weight(
                        candidate_value);
                end
                else if local_supported_exact_pair then
                begin
                    local_transition_evidence :=
                        get_supported_pair_evidence_local(local_text,
                        local_transition_path);
                    item.transition_evidence := local_transition_evidence;
                    local_transition_full_lm_lead := 0;
                    local_transition_suffix_lm_lead := 0;
                    item.decisive_transition_override :=
                        (not has_user_exact) and
                        (local_transition_evidence >= 410) and
                        ((max_base_exact_competing_weight < 0) or
                        ((local_transition_evidence >=
                        max_base_exact_competing_weight + 96) and
                        transition_path_has_supported_function_head_local(
                        local_transition_path) and
                        transition_has_decisive_char_lm_lead_local(local_text,
                        max_base_exact_competing_text,
                        local_transition_full_lm_lead,
                        local_transition_suffix_lm_lead)));
                    if m_config.debug_mode then
                    begin
                        if m_last_lookup_debug_extra <> '' then
                        begin
                            m_last_lookup_debug_extra :=
                                m_last_lookup_debug_extra + ' ';
                        end;
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('sepair=%s:%d/%d/%d/%d/%d', [local_text,
                            local_transition_evidence,
                            max_base_exact_competing_weight,
                            local_transition_full_lm_lead,
                            local_transition_suffix_lm_lead,
                            Ord(item.decisive_transition_override)]);
                    end;
                    if item.decisive_transition_override then
                    begin
                        item.category := 1;
                        item.rank_score := local_transition_evidence;
                    end
                    else
                    begin
                        item.category := 2;
                        item.rank_score := display_candidate_effective_weight(
                            candidate_value);
                    end;
                end
                else if (local_predictive_prefix or local_lexicon_completion) and
                    (not local_direct_exact) then
                begin
                    item.category := 4;
                    item.prefix_units := local_units;
                    item.rank_score := short_exact_effective_weight_local(
                        candidate_value);
                end
                else if local_particle_tail_rule and (not local_direct_exact) then
                begin
                    item.category := 2;
                    item.rank_score := short_exact_effective_weight_local(
                        candidate_value);
                end
                else if (candidate_value.source = cs_user) and
                    (not local_exact_user) then
                begin
                    Exit;
                end;

                if (not local_predictive_prefix) and
                    (not local_lexicon_completion) and
                    (not local_particle_tail_rule) and
                    (not local_direct_exact) and
                    (not local_supported_exact_pair) and
                    (not local_fuzzy_exact) then
                begin
                    Exit;
                end;

                if local_direct_exact then
                begin
                    if (not local_exact_user) and
                        has_effective_short_context_local and
                        (m_dictionary <> nil) and
                        is_low_evidence_admin_place_alias_cached(
                        normalized_pinyin, local_text) then
                    begin
                        item.category := 4;
                        item.prefix_units := local_units;
                        item.rank_score := short_exact_effective_weight_local(
                            candidate_value);
                        local_direct_exact := False;
                    end;
                end;

                if local_direct_exact then
                begin
                    item.rank_score := short_complete_rank_score_local(
                        candidate_value, local_text, local_exact_user);
                    if exact_weights.TryGetValue(local_text, local_exact_weight) then
                    begin
                        item.low_frequency_medical_exact :=
                            has_effective_short_context_local and
                            (not local_exact_user) and
                            (local_exact_weight <=
                            c_low_frequency_medical_exact_weight) and
                            text_has_low_frequency_medical_unit_local(
                            local_text);
                        { Without context, base exact candidates retain raw
                          dictionary-weight order unless this exact pinyin/text
                          pair has durable user-choice evidence. }
                        local_direct_exact_rank := local_exact_weight;
                        if local_exact_user then
                        begin
                            Inc(local_direct_exact_rank,
                                query_choice_rank_bonus_local(local_text));
                            Inc(local_direct_exact_rank, c_user_score_bonus);
                        end;
                        if not local_exact_user then
                        begin
                            Inc(local_direct_exact_rank,
                                short_base_exact_dynamic_weight_local(
                                local_text));
                        end;
                        item.rank_score := local_direct_exact_rank;
                    end;

                    if local_exact_user then
                    begin
                        item.category := 0;
                        item.candidate.source := cs_user;
                        item.candidate.has_dict_weight := False;
                        item.candidate.dict_weight := 0;
                        Inc(item.rank_score, c_user_score_bonus);
                    end
                    else if candidate_value.source = cs_user then
                    begin
                        item.category := 2;
                    end
                    else
                    begin
                        item.category := 1;
                    end;
                end;
            end
            else if (local_comment <> '') and
                (local_units + local_comment_units = expected_units) and
                (local_preserved_lm_prefix or (local_units = 1) or
                prefix_candidate_keys.ContainsKey(
                prefix_candidate_cache_key_local(local_text, local_tail_key)) or
                prefix_candidate_matches_query_local(local_text, local_units,
                local_tail_key)) then
            begin
                if is_numeric_quantity_partial_noise_local then
                begin
                    Exit;
                end;

                if local_units >= 2 then
                begin
                    item.category := 3;
                end
                else
                begin
                    item.category := 5;
                end;
                item.prefix_units := local_units;
            end
            else if local_comment = '' then
            begin
                Exit;
            end
            else
            begin
                Exit;
            end;

            list.Add(item);
        end;

        procedure sort_short_exact_rank_items_local;
        var
            item_index: Integer;
            insert_index: Integer;
            pending_item: TShortExactRankItem;

            function compare_items(const left,
                right: TShortExactRankItem): Integer;
    begin
        if left.user_full_exact <> right.user_full_exact then
        begin
            if left.user_full_exact then
            begin
                Exit(-1);
            end;
            Exit(1);
        end;

        if left.decisive_transition_override and
            right.actual_full_exact then
        begin
            Exit(-1);
        end;
        if right.decisive_transition_override and
            left.actual_full_exact then
        begin
            Exit(1);
        end;

        if left.actual_full_exact <> right.actual_full_exact then
        begin
            if left.actual_full_exact then
            begin
                Exit(-1);
            end;
            Exit(1);
        end;

        if (left.category >= 3) and (left.category <= 5) and
            (right.category >= 3) and (right.category <= 5) then
        begin
            Result := right.prefix_units - left.prefix_units;
            if Result <> 0 then
            begin
                Exit;
            end;
        end;

        Result := left.category - right.category;
        if Result <> 0 then
        begin
            Exit;
        end;

        if (left.low_frequency_medical_exact <>
            right.low_frequency_medical_exact) then
        begin
            if left.low_frequency_medical_exact then
            begin
                Exit(1);
            end;
            Exit(-1);
        end;

        Result := right.context_exact_priority -
            left.context_exact_priority;
        if Result <> 0 then
        begin
            Exit;
        end;

        Result := right.nocontext_exact_priority -
            left.nocontext_exact_priority;
        if Result <> 0 then
        begin
            Exit;
        end;

        Result := right.rank_score - left.rank_score;
        if Result <> 0 then
        begin
            Exit;
        end;

        Result := left.original_index - right.original_index;
        if Result <> 0 then
        begin
            Exit;
        end;

        Result := left.text_length - right.text_length;
        if Result <> 0 then
        begin
            Exit;
        end;

        Result := CompareText(left.candidate.text,
            right.candidate.text);
            end;
        begin
            for item_index := 1 to list.Count - 1 do
            begin
                pending_item := list[item_index];
                insert_index := item_index - 1;
                while (insert_index >= 0) and
                    (compare_items(list[insert_index], pending_item) > 0) do
                begin
                    list[insert_index + 1] := list[insert_index];
                    Dec(insert_index);
                end;
                list[insert_index + 1] := pending_item;
            end;
        end;

        function apply_context_supported_pair_ranking_local: Boolean;
        const
            c_min_exact_competitors = 2;
            c_max_exact_competitors = 9;
            c_max_pair_competitors = 2;
            c_min_context_score = -6000;
        var
            context_local: string;
            context_units_local: TArray<string>;
            base_units_local, pair_units_local: TArray<string>;
            texts_local, ngrams_local: TArray<string>;
            indices_local, ngram_owners_local: TArray<Integer>;
            context_scores_local, neutral_scores_local: TArray<Integer>;
            attested_scores_local: TArray<Integer>;
            prefix_scores_local: TArray<Integer>;
            observed_local: TArray<Boolean>;
            list_idx_local, idx_local, pair_count_local, exact_count_local: Integer;
            best_idx_local, runner_score_local, ngram_idx_local: Integer;
            lead_local, context_gain_local: Int64;
            rank_item_local: TShortExactRankItem;
            pair_local: Boolean;
            base_prefix_local, pair_prefix_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or has_user_exact or
                (expected_units < 2) or (expected_units > 3) or
                (Length(syllables) <> expected_units) or
                (not is_full_pinyin_key(normalized_pinyin)) or
                is_fuzzy_pinyin_active or (list.Count < 2) or
                (not list[0].actual_full_exact) or
                (list[0].context_exact_priority > 0) then Exit;

            context_local := Trim(m_left_context);
            if Trim(m_segment_left_context) <> '' then
                context_local := Trim(m_segment_left_context)
            else if Trim(m_external_left_context) <> '' then
                context_local := Trim(m_external_left_context);
            context_local := context_model_tail(context_local);
            if context_local = '' then Exit;
            context_units_local := split_text_units(context_local);
            if Length(context_units_local) = 0 then Exit;

            pair_count_local := 0;
            exact_count_local := 0;
            for list_idx_local := 0 to list.Count - 1 do
            begin
                rank_item_local := list[list_idx_local];
                if (rank_item_local.candidate.comment <> '') or
                    (get_candidate_text_unit_count(rank_item_local.candidate.text) <>
                    expected_units) then Continue;
                pair_local := (rank_item_local.transition_evidence > 0) and
                    (rank_item_local.candidate.display_kind = cdk_lm_compound) and
                    (not rank_item_local.actual_full_exact);
                if not pair_local and not rank_item_local.actual_full_exact then
                    Continue;
                if query_choice_rank_bonus_local(rank_item_local.candidate.text) > 0 then
                    Exit;
                if pair_local then Inc(pair_count_local)
                else Inc(exact_count_local);
                if (pair_count_local > c_max_pair_competitors) or
                    (exact_count_local > c_max_exact_competitors) then Exit;

                idx_local := Length(texts_local);
                SetLength(texts_local, idx_local + 1);
                SetLength(indices_local, idx_local + 1);
                texts_local[idx_local] := rank_item_local.candidate.text;
                indices_local[idx_local] := list_idx_local;
                if pair_local then
                begin
                    ngram_idx_local := Length(ngrams_local);
                    SetLength(ngrams_local, ngram_idx_local + 1);
                    SetLength(ngram_owners_local, ngram_idx_local + 1);
                    ngrams_local[ngram_idx_local] :=
                        context_units_local[High(context_units_local)] +
                        rank_item_local.candidate.text;
                    ngram_owners_local[ngram_idx_local] := idx_local;
                    if (expected_units = 2) and (Length(context_units_local) > 1) then
                    begin
                        ngrams_local[ngram_idx_local] :=
                            context_units_local[High(context_units_local) - 1] +
                            ngrams_local[ngram_idx_local];
                    end;
                end;
            end;
            // A unique dictionary exact is not an ambiguous exact group. Leave
            // that decision to the trained rankers instead of a local LM margin.
            if (pair_count_local = 0) or
                (exact_count_local < c_min_exact_competitors) or
                (Length(indices_local) < 2) or (indices_local[0] <> 0) then Exit;

            // Admission is unchanged. A backoff guess or a blue display marker
            // alone cannot override an exact. Use the longest available context
            // window, not a shorter common function-word fragment as fallback.
            if (not m_dictionary.get_char_lm_attested_scores(ngrams_local,
                attested_scores_local)) or
                (Length(attested_scores_local) <> Length(ngrams_local)) then Exit;
            SetLength(observed_local, Length(texts_local));
            for idx_local := 0 to High(attested_scores_local) do
                if attested_scores_local[idx_local] >= c_min_context_score then
                    observed_local[ngram_owners_local[idx_local]] := True;
            if (not get_cached_char_lm_scores(texts_local, context_scores_local,
                clsm_short_context, context_local)) or
                (Length(context_scores_local) <> Length(texts_local)) then Exit;

            best_idx_local := 0;
            for idx_local := 0 to High(context_scores_local) do
            begin
                if context_scores_local[idx_local] = Low(Integer) then Exit;
                if context_scores_local[idx_local] > context_scores_local[best_idx_local] then
                    best_idx_local := idx_local;
            end;
            if (best_idx_local = 0) or (not observed_local[best_idx_local]) or
                (context_scores_local[best_idx_local] < c_min_context_score) then Exit;
            runner_score_local := Low(Integer);
            for idx_local := 0 to High(context_scores_local) do
                if idx_local <> best_idx_local then
                    runner_score_local := Max(runner_score_local, context_scores_local[idx_local]);
            lead_local := Int64(context_scores_local[best_idx_local]) - runner_score_local;
            if lead_local < c_decisive_transition_min_full_lm_lead then Exit;

            // A frequent tail must not compensate for making the first changed
            // prefix less compatible with the actual left context.
            base_units_local := split_text_units(texts_local[0]);
            pair_units_local := split_text_units(texts_local[best_idx_local]);
            base_prefix_local := '';
            pair_prefix_local := '';
            for idx_local := 0 to expected_units - 2 do
            begin
                base_prefix_local := base_prefix_local + base_units_local[idx_local];
                pair_prefix_local := pair_prefix_local + pair_units_local[idx_local];
                if base_units_local[idx_local] = pair_units_local[idx_local] then Continue;
                if (not get_cached_char_lm_scores(TArray<string>.Create(
                    base_prefix_local, pair_prefix_local), prefix_scores_local,
                    clsm_short_context, context_local)) or
                    (Length(prefix_scores_local) <> 2) or
                    (prefix_scores_local[0] = Low(Integer)) or
                    (prefix_scores_local[1] < prefix_scores_local[0]) then Exit;
                Break;
            end;

            // Context must add discriminating evidence, not just preserve a
            // phrase's context-free advantage. Reuse the ordinary scoring cache.
            if (not get_cached_char_lm_scores(TArray<string>.Create(texts_local[0],
                texts_local[best_idx_local]), neutral_scores_local,
                clsm_short_context, '')) or (Length(neutral_scores_local) <> 2) or
                (neutral_scores_local[0] = Low(Integer)) or
                (neutral_scores_local[1] = Low(Integer)) then Exit;
            context_gain_local := Int64(context_scores_local[best_idx_local]) -
                context_scores_local[0] -
                (Int64(neutral_scores_local[1]) - neutral_scores_local[0]);
            if context_gain_local < c_decisive_transition_min_suffix_lm_lead then Exit;

            rank_item_local := list[indices_local[best_idx_local]];
            rank_item_local.decisive_transition_override := True;
            rank_item_local.category := 1;
            list[indices_local[best_idx_local]] := rank_item_local;
            if m_config.debug_mode then
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format(' shortctxpair=%s:lead=%d/gain=%d',
                    [texts_local[best_idx_local], lead_local, context_gain_local]);
            Result := True;
        end;

        function apply_document_context_exact_reranker_local: Boolean;
        const
            c_document_exact_min_score = 280;
            c_document_exact_min_baseline_lead = 240;
            c_document_exact_min_runner_up_lead = 96;
            c_document_exact_max_candidates = 9;
        var
            candidate_idx_local: Integer;
            candidate_limit_local: Integer;
            candidate_score_local: Integer;
            baseline_score_local: Integer;
            best_idx_local: Integer;
            best_score_local: Integer;
            runner_up_score_local: Integer;
            rank_item_local: TShortExactRankItem;
        begin
            Result := False;
            if (m_document_context_model = nil) or
                (not m_document_context_model.has_context) or
                (list.Count < 2) or list[0].user_full_exact then
            begin
                Exit;
            end;

            baseline_score_local := m_document_context_model.score_text(
                list[0].candidate.text);
            best_idx_local := 0;
            best_score_local := baseline_score_local;
            runner_up_score_local := Low(Integer);
            candidate_limit_local := Min(list.Count,
                c_document_exact_max_candidates);
            for candidate_idx_local := 1 to candidate_limit_local - 1 do
            begin
                rank_item_local := list[candidate_idx_local];
                if (rank_item_local.category <> 1) or
                    (not rank_item_local.actual_full_exact) or
                    rank_item_local.user_full_exact or
                    rank_item_local.low_frequency_medical_exact then
                begin
                    Continue;
                end;
                candidate_score_local :=
                    m_document_context_model.score_text(
                    rank_item_local.candidate.text);
                if candidate_score_local > best_score_local then
                begin
                    runner_up_score_local := best_score_local;
                    best_score_local := candidate_score_local;
                    best_idx_local := candidate_idx_local;
                end
                else if candidate_score_local > runner_up_score_local then
                begin
                    runner_up_score_local := candidate_score_local;
                end;
            end;

            if (best_idx_local <= 0) or
                (best_score_local < c_document_exact_min_score) or
                (best_score_local - baseline_score_local <
                c_document_exact_min_baseline_lead) or
                ((runner_up_score_local <> Low(Integer)) and
                (best_score_local - runner_up_score_local <
                c_document_exact_min_runner_up_lead)) then
            begin
                Exit;
            end;

            rank_item_local := list[best_idx_local];
            rank_item_local.context_exact_priority := Max(
                rank_item_local.context_exact_priority, 4);
            list[best_idx_local] := rank_item_local;
            Result := True;
        end;

        procedure ensure_prefix_visible_on_first_page_local;
        var
            visible_limit_local: Integer;
            visible_idx_local: Integer;
            insert_idx_local: Integer;
            exact_count_local: Integer;
            prefix_item_local: TShortExactRankItem;
        begin
            visible_limit_local := get_candidate_limit;
            if (visible_limit_local <= 0) or (list.Count <= visible_limit_local) then
            begin
                Exit;
            end;

            exact_count_local := 0;
            while (exact_count_local < list.Count) and
                (list[exact_count_local].category <= 2) do
            begin
                Inc(exact_count_local);
            end;
            if exact_count_local >= visible_limit_local then
            begin
                Exit;
            end;

            for visible_idx_local := 0 to visible_limit_local - 1 do
            begin
                if (list[visible_idx_local].category = 3) or
                    (list[visible_idx_local].category = 4) or
                    (list[visible_idx_local].category = 5) then
                begin
                    Exit;
                end;
            end;

            for visible_idx_local := visible_limit_local to list.Count - 1 do
            begin
                if (list[visible_idx_local].category <> 3) and
                    (list[visible_idx_local].category <> 4) and
                    (list[visible_idx_local].category <> 5) then
                begin
                    Continue;
                end;

                prefix_item_local := list[visible_idx_local];
                list.Delete(visible_idx_local);
                insert_idx_local := visible_limit_local - 1;
                if insert_idx_local < exact_count_local then
                begin
                    insert_idx_local := exact_count_local;
                end;
                if insert_idx_local < 0 then
                begin
                    insert_idx_local := 0;
                end;
                list.Insert(insert_idx_local, prefix_item_local);
                Exit;
            end;
        end;

        procedure append_missing_supported_pair_candidates_local;
        var
            pair_texts_local: TArray<string>;
            pair_idx_local: Integer;
            candidate_idx_local: Integer;
            append_idx_local: Integer;
            pair_text_local: string;
            pair_path_local: string;
            pair_evidence_local: Integer;
            already_present_local: Boolean;
        begin
            SetLength(pair_texts_local, 0);
            if SameText(normalized_pinyin, m_short_two_single_pair_query) then
            begin
                pair_texts_local := Copy(m_short_two_single_pair_texts, 0,
                    Length(m_short_two_single_pair_texts));
            end;
            if SameText(normalized_pinyin, m_short_three_exact_pair_query) then
            begin
                pair_texts_local := Copy(m_short_three_exact_pair_texts, 0,
                    Length(m_short_three_exact_pair_texts));
            end;

            for pair_idx_local := 0 to High(pair_texts_local) do
            begin
                pair_text_local := Trim(pair_texts_local[pair_idx_local]);
                if pair_text_local = '' then
                begin
                    Continue;
                end;

                already_present_local := False;
                for candidate_idx_local := 0 to High(m_candidates) do
                begin
                    if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                        SameText(Trim(m_candidates[candidate_idx_local].text),
                        pair_text_local) then
                    begin
                        already_present_local := True;
                        Break;
                    end;
                end;
                if already_present_local then
                begin
                    Continue;
                end;

                pair_evidence_local := get_supported_pair_evidence_local(
                    pair_text_local, pair_path_local);
                if (pair_evidence_local <= 0) or (pair_path_local = '') then
                begin
                    Continue;
                end;

                append_idx_local := Length(m_candidates);
                SetLength(m_candidates, append_idx_local + 1);
                SetLength(m_candidate_segment_paths, append_idx_local + 1);
                m_candidates[append_idx_local] := Default(TncCandidate);
                m_candidates[append_idx_local].text := pair_text_local;
                m_candidates[append_idx_local].comment := '';
                m_candidates[append_idx_local].score := 16000 +
                    pair_evidence_local;
                m_candidates[append_idx_local].source := cs_rule;
                m_candidates[append_idx_local].has_dict_weight := False;
                m_candidates[append_idx_local].dict_weight := 0;
                m_candidates[append_idx_local].display_kind := cdk_lm_compound;
                m_candidate_segment_paths[append_idx_local] := pair_path_local;
            end;
        end;

        function confirmed_prefix_allows_short_exact_local: Boolean;
        const
            c_min_confirmed_tail_single_count = 12;
        var
            first_key_local: string;
            prefix_results_local: TncCandidateList;
            prefix_idx_local: Integer;
            single_count_local: Integer;
            prefix_text_local: string;
        begin
            Result := True;
            if (Trim(m_confirmed_text) = '') and
                ((m_confirmed_segments = nil) or (m_confirmed_segments.Count = 0)) then
            begin
                Exit;
            end;

            if is_fuzzy_pinyin_active and (expected_units = 1) and
                (m_last_lookup_syllable_count = 1) then
            begin
                Exit(True);
            end;

            Result := False;
            if expected_units <> 2 then
            begin
                Exit;
            end;

            first_key_local := build_display_query_key(0, 1);
            if (first_key_local = '') or
                (not lookup_display_complete_cached(first_key_local,
                prefix_results_local)) then
            begin
                Exit;
            end;

            single_count_local := 0;
            for prefix_idx_local := 0 to High(prefix_results_local) do
            begin
                prefix_text_local := Trim(prefix_results_local[prefix_idx_local].text);
                if (prefix_text_local <> '') and
                    (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(prefix_text_local) = 1) then
                begin
                    Inc(single_count_local);
                    if single_count_local >= c_min_confirmed_tail_single_count then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (expected_units < 1) or (expected_units > c_max_user_exact_units) or
            (not confirmed_prefix_allows_short_exact_local) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_key) > 0) or
            (Pos('''', m_last_lookup_normalized_from) > 0) then
        begin
            Exit;
        end;

        short_exact_phase_start := nc_monotonic_tick_ms;
        predictive_prefix_results_loaded := False;
        pair_evidence_loaded := False;
        SetLength(pair_evidence_results, 0);
        max_base_exact_competing_weight := -1;
        max_base_exact_competing_text := '';
        SetLength(predictive_prefix_results, 0);
        has_closed_particle_candidate :=
            try_get_closed_particle_combo_candidate_local(
            closed_particle_candidate);
        note_short_exact_phase_local('closed');
        has_exact_lookup := lookup_display_exact_cached(normalized_pinyin,
            exact_results);
        if not has_exact_lookup then
        begin
            SetLength(exact_results, 0);
        end;
        has_fuzzy_exact_lookup := False;
        SetLength(fuzzy_exact_results, 0);
        if m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []) and (m_dictionary <> nil) then
        begin
            if is_shuangpin_input then
            begin
                has_fuzzy_exact_lookup :=
                    m_dictionary.lookup_fuzzy_full_pinyin(normalized_pinyin,
                    fuzzy_exact_results);
            end
            else
            begin
                has_fuzzy_exact_lookup :=
                    m_dictionary.lookup_fuzzy_full_pinyin(
                    get_display_raw_query_text_local, fuzzy_exact_results);
            end;
            if has_fuzzy_exact_lookup then
            begin
                m_candidates := merge_candidate_lists(m_candidates,
                    fuzzy_exact_results, 0);
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(m_candidates));
                end;
            end;
        end;
        note_short_exact_phase_local('exact');
        has_predictive_prefix_lookup :=
            has_predictive_pinyin_prefix_candidate_local;
        note_short_exact_phase_local('pred');
        has_particle_tail_lookup :=
            has_particle_tail_completion_candidate_local;
        note_short_exact_phase_local('tail');
        has_query_prefix_lookup := has_query_prefix_exact_candidate_local;
        short_exact_predictive_prefix_only_mode := False;
        if (not has_exact_lookup) and (not has_fuzzy_exact_lookup) and
            (not has_closed_particle_candidate) and
            (not has_predictive_prefix_lookup) and
            (not has_particle_tail_lookup) and
            (not has_query_prefix_lookup) then
        begin
            Exit;
        end;

            exact_weights := TDictionary<string, Integer>.Create;
            exact_is_user := TDictionary<string, Boolean>.Create;
            exact_candidates := TDictionary<string, TncCandidate>.Create;
            prefix_candidate_keys := TDictionary<string, Boolean>.Create;
            predictive_prefix_texts := TDictionary<string, Boolean>.Create;
            particle_tail_texts := TDictionary<string, Boolean>.Create;
            list := TList<TShortExactRankItem>.Create;
            try
                has_user_exact := False;
                if has_closed_particle_candidate then
                begin
                    exact_text := Trim(closed_particle_candidate.text);
                    exact_weight := closed_particle_candidate.dict_weight;
                    exact_weights.AddOrSetValue(exact_text, exact_weight);
                    exact_candidates.AddOrSetValue(exact_text,
                        closed_particle_candidate);
                    exact_is_user.AddOrSetValue(exact_text, False);
                end;
                for exact_idx := 0 to High(exact_results) do
            begin
                exact_text := Trim(exact_results[exact_idx].text);
                if (exact_text = '') or (Trim(exact_results[exact_idx].comment) <> '') or
                    (get_candidate_text_unit_count(exact_text) <>
                    expected_units) then
                begin
                    Continue;
                end;

                if not exact_candidate_is_reliable_complete_local(
                    exact_results[exact_idx], exact_text) then
                begin
                    Continue;
                end;

                exact_weight := exact_results[exact_idx].score;
                if is_full_pinyin_key(normalized_pinyin) and
                    exact_results[exact_idx].has_dict_weight then
                begin
                    exact_weight := exact_results[exact_idx].dict_weight;
                end;
                if not exact_candidate_is_user_local(exact_results[exact_idx],
                    exact_text) then
                begin
                    existing_weight := exact_weight +
                        short_base_exact_dynamic_weight_local(exact_text);
                    if existing_weight > max_base_exact_competing_weight then
                    begin
                        max_base_exact_competing_weight := existing_weight;
                        max_base_exact_competing_text := exact_text;
                    end;
                end;
                existing_weight := Low(Integer);
                exact_weights.TryGetValue(exact_text, existing_weight);
                if (not exact_weights.ContainsKey(exact_text)) or
                    (exact_weight > existing_weight) then
                begin
                    exact_weights.AddOrSetValue(exact_text, exact_weight);
                    candidate_item := exact_results[exact_idx];
                    candidate_item.text := exact_text;
                    candidate_item.comment := '';
                    exact_candidates.AddOrSetValue(exact_text, candidate_item);
                end;

                existing_is_user := False;
                exact_is_user.TryGetValue(exact_text, existing_is_user);
                if exact_candidate_is_user_local(exact_results[exact_idx],
                    exact_text) then
                begin
                    exact_is_user.AddOrSetValue(exact_text, True);
                    has_user_exact := True;
                end
                else if not existing_is_user then
                begin
                    exact_is_user.AddOrSetValue(exact_text, False);
                end;
            end;
            note_short_exact_phase_local('collect');
            short_exact_predictive_prefix_only_mode :=
                (not is_full_pinyin_key(normalized_pinyin)) and
                has_predictive_prefix_lookup and
                (not has_closed_particle_candidate) and
                (not has_particle_tail_lookup);

            if ((exact_weights.Count = 0) and
                (not has_fuzzy_exact_lookup) and
                (not has_predictive_prefix_lookup) and
                (not has_particle_tail_lookup) and
                (not has_query_prefix_lookup)) or
                ((expected_units > c_max_base_exact_units) and
                (not has_user_exact)) then
            begin
                Exit;
            end;

            append_missing_exact_candidates_local;
            append_particle_tail_completion_candidates_local;
            append_query_prefix_candidates_local;
            append_predictive_pinyin_prefix_candidates_local;
            append_missing_supported_pair_candidates_local;
            note_short_exact_phase_local('append');

            for idx := 0 to High(m_candidates) do
            begin
                if short_exact_predictive_prefix_only_mode and
                    (idx >= c_default_page_size * 10) and
                    (not predictive_prefix_texts.ContainsKey(
                    Trim(m_candidates[idx].text))) then
                begin
                    Continue;
                end;

                path_value := '';
                if idx < Length(m_candidate_segment_paths) then
                begin
                    path_value := m_candidate_segment_paths[idx];
                end;
                add_rank_item_local(m_candidates[idx], path_value, idx);
            end;
            note_short_exact_phase_local('rank');

            sort_short_exact_rank_items_local;
            note_short_exact_phase_local('sort');
            { Keep dictionary weight as the baseline, then allow the dedicated
              residual model to correct high-confidence no-context errors. }
            apply_short_nocontext_reranker_local;
            if short_nocontext_promoted_exact_text <> '' then
            begin
                sort_short_exact_rank_items_local;
            end;
            note_short_exact_phase_local('nocontext');
            apply_short_context_reranker_local;
            if short_context_promoted_exact_text <> '' then
            begin
                sort_short_exact_rank_items_local;
            end;
            note_short_exact_phase_local('context');
            if apply_document_context_exact_reranker_local then
            begin
                sort_short_exact_rank_items_local;
            end;
            note_short_exact_phase_local('document');
            if apply_context_supported_pair_ranking_local then
            begin
                sort_short_exact_rank_items_local;
            end;
            note_short_exact_phase_local('contextpair');
            ensure_prefix_visible_on_first_page_local;
            note_short_exact_phase_local('visible');
            if short_exact_predictive_prefix_only_mode then
            begin
                while list.Count > c_default_page_size * 10 do
                begin
                    list.Delete(list.Count - 1);
                end;
            end;

            SetLength(m_candidates, list.Count);
            SetLength(m_candidate_segment_paths, list.Count);
            for idx := 0 to list.Count - 1 do
            begin
                m_candidates[idx] := list[idx].candidate;
                m_candidate_segment_paths[idx] := list[idx].path;
            end;

            if m_config.debug_mode and (list.Count > 0) then
            begin
                if m_last_lookup_debug_extra <> '' then
                begin
                    m_last_lookup_debug_extra :=
                        m_last_lookup_debug_extra + ' ';
                end;
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    Format('seranktop=%s/%d/%d/%d',
                    [Trim(list[0].candidate.text), list[0].category,
                    Ord(list[0].actual_full_exact),
                    Ord(list[0].decisive_transition_override)]);
            end;

            if m_config.debug_mode then
            begin
                if m_last_lookup_debug_extra <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
                end;
                m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                    'shortexact=1';
                if short_nocontext_promoted_exact_text <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' shortnoctx=%s:%d',
                        [short_nocontext_promoted_exact_text,
                        short_nocontext_promoted_exact_lead]);
                end;
                if short_context_promoted_exact_text <> '' then
                begin
                    m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                        Format(' shortctx=%s:%d',
                        [short_context_promoted_exact_text,
                        short_context_promoted_exact_lead]);
                end;
            end;

            Result := True;
        finally
            particle_tail_texts.Free;
            predictive_prefix_texts.Free;
            prefix_candidate_keys.Free;
            list.Free;
            exact_candidates.Free;
            exact_is_user.Free;
            exact_weights.Free;
        end;
    end;

    function should_keep_display_top_before_candidate(
        const candidate_value: TncCandidate): Boolean;
    const
        c_pathful_top_min_gap = 2400;
    var
        top_text_local: string;
        top_path_local: string;
        top_weight_local: Integer;
        candidate_weight_local: Integer;
        candidate_text_local: string;
        top_exact_weight_local: Integer;
        candidate_exact_weight_local: Integer;
        top_query_choice_bonus_local: Integer;
        candidate_query_choice_bonus_local: Integer;

        function fixed_tail_alignment_score_local(
            const candidate_local: TncCandidate): Integer;
        var
            candidate_units_local: TArray<string>;
            tail_key_local: string;
            expected_tail_text_local: string;
        begin
            Result := 0;
            if (expected_units < 3) or (Length(syllables) <> expected_units) or
                (Trim(candidate_local.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate_local.text)) <>
                expected_units) then
            begin
                Exit;
            end;

            tail_key_local := normalize_pinyin_text(
                syllables[expected_units - 1].text);
            expected_tail_text_local := '';
            if tail_key_local = 'yi' then
            begin
                expected_tail_text_local := string(Char($4E00));
            end
            else if tail_key_local = 'ba' then
            begin
                expected_tail_text_local := string(Char($5427));
            end
            else if tail_key_local = 'ma' then
            begin
                expected_tail_text_local := string(Char($5417));
            end
            else if tail_key_local = 'le' then
            begin
                expected_tail_text_local := string(Char($4E86));
            end
            else if tail_key_local = 'zhe' then
            begin
                expected_tail_text_local := string(Char($7740));
            end
            else if tail_key_local = 'de' then
            begin
                expected_tail_text_local := string(Char($7684));
            end
            else if tail_key_local = 'he' then
            begin
                expected_tail_text_local := string(Char($548C));
            end
            else if tail_key_local = 'wo' then
            begin
                expected_tail_text_local := string(Char($6211));
            end;
            if expected_tail_text_local = '' then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate_local.text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;
            if SameText(Trim(candidate_units_local[expected_units - 1]),
                expected_tail_text_local) then
            begin
                Result := 1;
            end
            else
            begin
                Result := -1;
            end;
        end;
    begin
        Result := False;
        if (Length(m_candidates) = 0) or (expected_units < 2) then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        if (top_text_local = '') or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) or
            SameText(top_text_local, Trim(candidate_value.text)) then
        begin
            Exit;
        end;

        candidate_text_local := Trim(candidate_value.text);
        if (candidate_value.source = cs_user) or
            is_latest_session_query_choice(candidate_text_local) or
            (get_context_query_latest_bonus(candidate_text_local) > 0) then
        begin
            Exit;
        end;

        top_path_local := '';
        if Length(m_candidate_segment_paths) > 0 then
        begin
            top_path_local := m_candidate_segment_paths[0];
        end;
        if top_path_local = '' then
        begin
            top_path_local := get_segment_path_for_candidate(m_candidates[0], 0);
        end;
        if get_candidate_text_unit_count(candidate_text_local) = expected_units then
        begin
            if (m_dictionary <> nil) and (normalized_pinyin <> '') then
            begin
                candidate_query_choice_bonus_local :=
                    m_dictionary.get_query_choice_bonus(normalized_pinyin,
                    candidate_text_local);
                top_query_choice_bonus_local :=
                    m_dictionary.get_query_choice_bonus(normalized_pinyin,
                    top_text_local);
                if candidate_query_choice_bonus_local >
                    top_query_choice_bonus_local then
                begin
                    Exit(False);
                end;
            end;
            if (normalized_pinyin <> '') and
                display_exact_key_has_text(normalized_pinyin,
                candidate_text_local) and
                display_exact_key_has_text(normalized_pinyin,
                top_text_local) and
                get_display_exact_text_weight(normalized_pinyin,
                candidate_text_local, expected_units,
                candidate_exact_weight_local) and
                get_display_exact_text_weight(normalized_pinyin,
                top_text_local, expected_units, top_exact_weight_local) then
            begin
                Exit(top_exact_weight_local > candidate_exact_weight_local);
            end;
            if (fixed_tail_alignment_score_local(candidate_value) > 0) and
                (fixed_tail_alignment_score_local(m_candidates[0]) < 0) then
            begin
                Exit(False);
            end;
            if (normalized_pinyin <> '') and
                display_candidate_has_supported_full_entry(normalized_pinyin,
                candidate_value) and
                (not display_candidate_has_supported_full_entry(
                normalized_pinyin, m_candidates[0])) then
            begin
                Exit(False);
            end;
            if (normalized_pinyin <> '') and
                display_exact_key_has_text(normalized_pinyin,
                candidate_text_local) and
                (not display_exact_key_has_text(normalized_pinyin,
                top_text_local)) then
            begin
                Exit(False);
            end;
            Exit(True);
        end;

        if get_encoded_path_segment_count_local(top_path_local) <= 1 then
        begin
            Exit;
        end;

        top_weight_local := display_candidate_effective_weight(m_candidates[0]);
        candidate_weight_local := display_candidate_effective_weight(
            candidate_value);
        Result := top_weight_local >= candidate_weight_local +
            c_pathful_top_min_gap;
    end;

    procedure normalize_display_full_query_exact_candidate_order;
    const
        c_visible_probe_limit = 24;
        c_latest_session_exact_rank_bonus = 200000000;
        c_context_exact_rank_bonus = 100000000;
        c_query_choice_exact_rank_bonus = 50000000;
        c_query_choice_exact_rank_scale = 1000;
    type
        TExactDisplayItem = record
            candidate: TncCandidate;
            path: string;
            rank: Integer;
        end;
    var
        slot_indices: TArray<Integer>;
        items: TArray<TExactDisplayItem>;
        probe_idx: Integer;
        item_count: Integer;
        insert_idx: Integer;
        temp_item: TExactDisplayItem;

        function try_get_full_query_exact_display_rank(
            const candidate_value: TncCandidate; out out_rank: Integer): Boolean;
        var
            candidate_text_local: string;
            exact_weight_local: Integer;
            query_choice_bonus_local: Integer;
            context_bonus_local: Integer;
        begin
            Result := False;
            out_rank := Low(Integer);

            candidate_text_local := Trim(candidate_value.text);
            if (normalized_pinyin = '') or (candidate_text_local = '') or
                (Trim(candidate_value.comment) <> '') or
                (expected_units < 2) or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units) or
                (not get_display_exact_text_weight(normalized_pinyin,
                candidate_text_local, expected_units, exact_weight_local)) then
            begin
                Exit;
            end;

            // Use the exact-query dictionary weight as the baseline.  A weak
            // user/runtime exact entry must not inherit the generic user source
            // bonus here; only explicit query-choice learning may lift it.
            out_rank := exact_weight_local;

            if is_latest_session_query_choice(candidate_text_local) then
            begin
                Inc(out_rank, c_latest_session_exact_rank_bonus);
            end;

            context_bonus_local := get_context_query_latest_bonus(
                candidate_text_local);
            if context_bonus_local > 0 then
            begin
                Inc(out_rank, c_context_exact_rank_bonus +
                    (context_bonus_local * c_query_choice_exact_rank_scale));
            end;

            if m_dictionary <> nil then
            begin
                query_choice_bonus_local := m_dictionary.get_query_choice_bonus(
                    normalized_pinyin, candidate_text_local);
                if query_choice_bonus_local > 0 then
                begin
                    Inc(out_rank, c_query_choice_exact_rank_bonus +
                        (query_choice_bonus_local *
                        c_query_choice_exact_rank_scale));
                end;
            end;

            Result := True;
        end;
    begin
        if (Length(m_candidates) < 2) or (normalized_pinyin = '') or
            (expected_units < 2) then
        begin
            Exit;
        end;

        item_count := 0;
        for probe_idx := 0 to Min(c_visible_probe_limit - 1,
            High(m_candidates)) do
        begin
            SetLength(slot_indices, item_count + 1);
            SetLength(items, item_count + 1);
            if not try_get_full_query_exact_display_rank(m_candidates[probe_idx],
                items[item_count].rank) then
            begin
                SetLength(slot_indices, item_count);
                SetLength(items, item_count);
                Continue;
            end;

            slot_indices[item_count] := probe_idx;
            items[item_count].candidate := m_candidates[probe_idx];
            if probe_idx < Length(m_candidate_segment_paths) then
            begin
                items[item_count].path := m_candidate_segment_paths[probe_idx];
            end
            else
            begin
                items[item_count].path := '';
            end;
            Inc(item_count);
        end;

        if item_count < 2 then
        begin
            Exit;
        end;

        // The display pipeline has many local promote/restore steps.  This
        // final stable pass makes full-query exact dictionary entries obey the
        // same quality order regardless of which local step inserted them.
        for probe_idx := 1 to item_count - 1 do
        begin
            temp_item := items[probe_idx];
            insert_idx := probe_idx - 1;
            while (insert_idx >= 0) and
                (items[insert_idx].rank < temp_item.rank) do
            begin
                items[insert_idx + 1] := items[insert_idx];
                Dec(insert_idx);
            end;
            items[insert_idx + 1] := temp_item;
        end;

        for probe_idx := 0 to item_count - 1 do
        begin
            m_candidates[slot_indices[probe_idx]] := items[probe_idx].candidate;
            if slot_indices[probe_idx] < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[slot_indices[probe_idx]] :=
                    items[probe_idx].path;
            end;
        end;
    end;

    procedure insert_display_candidate(const candidate_value: TncCandidate;
        const path_value: string; const target_idx: Integer);
    var
        idx: Integer;
        insert_idx: Integer;
        candidate_count: Integer;
    begin
        if (umlaut_raw_prefix_units(candidate_value) = 0) and
            display_candidate_is_unsupported_short_four_ab_cd_complete(candidate_value) then
        begin
            Exit;
        end;
        if (umlaut_raw_prefix_units(candidate_value) = 0) and
            display_candidate_is_unsupported_short_four_ab_c_partial(candidate_value) then
        begin
            Exit;
        end;

        candidate_count := Length(m_candidates);
        insert_idx := Max(0, Min(target_idx, candidate_count));
        if (insert_idx = 0) and
            should_keep_display_top_before_candidate(candidate_value) then
        begin
            insert_idx := Min(1, candidate_count);
        end;
        SetLength(m_candidates, candidate_count + 1);
        if Length(m_candidate_segment_paths) < candidate_count + 1 then
        begin
            SetLength(m_candidate_segment_paths, candidate_count + 1);
        end;
        for idx := candidate_count downto insert_idx + 1 do
        begin
            m_candidates[idx] := m_candidates[idx - 1];
            if idx < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[idx] := m_candidate_segment_paths[idx - 1];
            end;
        end;
        m_candidates[insert_idx] := candidate_value;
        if insert_idx < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[insert_idx] := path_value;
        end;
    end;

    function find_display_candidate(const text_value: string;
        const comment_value: string): Integer;
    var
        idx: Integer;
    begin
        Result := -1;
        for idx := 0 to High(m_candidates) do
        begin
            if SameText(Trim(m_candidates[idx].text), text_value) and
                SameText(Trim(m_candidates[idx].comment), comment_value) then
            begin
                Exit(idx);
            end;
        end;
    end;

    procedure move_display_candidate_to_index(const candidate_idx: Integer;
        const target_idx: Integer); forward;

    procedure ensure_best_full_query_exact_candidate_visible;
    const
        c_exact_probe_limit = 16;
        c_min_force_top_exact_rank = 500;
        c_latest_session_exact_rank_bonus = 200000000;
        c_context_exact_rank_bonus = 100000000;
        c_query_choice_exact_rank_bonus = 50000000;
        c_query_choice_exact_rank_scale = 1000;
    var
        exact_results_local: TncCandidateList;
        exact_idx_local: Integer;
        best_candidate_local: TncCandidate;
        best_rank_local: Integer;
        rank_local: Integer;
        best_text_local: string;
        visible_idx_local: Integer;
        target_idx_local: Integer;

        function full_query_exact_candidate_rank_local(
            const candidate_value: TncCandidate; out out_rank: Integer): Boolean;
        var
            candidate_text_local: string;
            candidate_weight_local: Integer;
            query_choice_bonus_local: Integer;
            context_bonus_local: Integer;
            is_persistent_user_entry_local: Boolean;
        begin
            Result := False;
            out_rank := Low(Integer);

            candidate_text_local := Trim(candidate_value.text);
            if (normalized_pinyin = '') or (candidate_text_local = '') or
                (Trim(candidate_value.comment) <> '') or
                (expected_units < 2) or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units) then
            begin
                Exit;
            end;

            query_choice_bonus_local := 0;
            context_bonus_local := get_context_query_latest_bonus(
                candidate_text_local);
            is_persistent_user_entry_local := False;
            if m_dictionary <> nil then
            begin
                query_choice_bonus_local := m_dictionary.get_query_choice_bonus(
                    normalized_pinyin, candidate_text_local);
                is_persistent_user_entry_local := m_dictionary.is_user_entry(
                    normalized_pinyin, candidate_text_local);
            end;

            if (candidate_value.source = cs_user) and
                (not is_persistent_user_entry_local) and
                (not is_latest_session_query_choice(candidate_text_local)) and
                (context_bonus_local <= 0) and
                (query_choice_bonus_local <= 0) then
            begin
                Exit;
            end;

            if candidate_value.has_dict_weight then
            begin
                candidate_weight_local := candidate_value.dict_weight;
            end
            else
            begin
                candidate_weight_local := candidate_value.score;
            end;
            out_rank := candidate_weight_local;

            if is_latest_session_query_choice(candidate_text_local) then
            begin
                Inc(out_rank, c_latest_session_exact_rank_bonus);
            end;

            if context_bonus_local > 0 then
            begin
                Inc(out_rank, c_context_exact_rank_bonus +
                    (context_bonus_local * c_query_choice_exact_rank_scale));
            end;

            if query_choice_bonus_local > 0 then
            begin
                Inc(out_rank, c_query_choice_exact_rank_bonus +
                    (query_choice_bonus_local *
                    c_query_choice_exact_rank_scale));
            end;

            Result := True;
        end;

        function current_top_should_stay_before_exact_local(
            const exact_text_value: string): Boolean;
        var
            top_text_local: string;
            top_query_choice_bonus_local: Integer;
            exact_query_choice_bonus_local: Integer;
        begin
            Result := False;
            if (Length(m_candidates) = 0) or (exact_text_value = '') then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            if (top_text_local = '') or SameText(top_text_local,
                exact_text_value) or (Trim(m_candidates[0].comment) <> '') or
                (get_candidate_text_unit_count(top_text_local) <>
                expected_units) then
            begin
                Exit;
            end;

            if is_latest_session_query_choice(top_text_local) or
                display_candidate_is_persistent_latest_choice(
                normalized_pinyin, top_text_local) or
                (get_context_query_latest_bonus(top_text_local) > 0) then
            begin
                Exit(True);
            end;

            if m_dictionary <> nil then
            begin
                top_query_choice_bonus_local :=
                    m_dictionary.get_query_choice_bonus(normalized_pinyin,
                    top_text_local);
                exact_query_choice_bonus_local :=
                    m_dictionary.get_query_choice_bonus(normalized_pinyin,
                    exact_text_value);
                if top_query_choice_bonus_local > exact_query_choice_bonus_local
                then
                begin
                    Exit(True);
                end;

                if (m_candidates[0].source = cs_user) and
                    m_dictionary.is_user_entry(normalized_pinyin,
                    top_text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function current_top_is_complete_display_candidate_local: Boolean;
        var
            top_text_local: string;
        begin
            Result := False;
            if Length(m_candidates) = 0 then
            begin
                Exit;
            end;

            top_text_local := Trim(m_candidates[0].text);
            Result := (top_text_local <> '') and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(top_text_local) =
                expected_units);
        end;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (expected_units < 2) or
            (Pos('''', normalized_pinyin) > 0) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_normalized_from) > 0) or
            (not m_dictionary.lookup_exact_full_pinyin(normalized_pinyin,
            exact_results_local)) then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    ' fqens=[lookup-miss]';
            end;
            Exit;
        end;

        best_rank_local := Low(Integer);
        best_text_local := '';
        for exact_idx_local := 0 to High(exact_results_local) do
        begin
            if exact_idx_local >= c_exact_probe_limit then
            begin
                Break;
            end;

            if not full_query_exact_candidate_rank_local(
                exact_results_local[exact_idx_local], rank_local) then
            begin
                Continue;
            end;

            if (best_text_local = '') or (rank_local > best_rank_local) then
            begin
                best_rank_local := rank_local;
                best_candidate_local := exact_results_local[exact_idx_local];
                best_text_local := Trim(best_candidate_local.text);
            end;
        end;

        if best_text_local = '' then
        begin
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqens=[no-best n=%d]', [Length(exact_results_local)]);
            end;
            Exit;
        end;

        target_idx_local := 0;
        if ((expected_units = 3) and
            (best_rank_local < c_min_force_top_exact_rank) and
            current_top_is_complete_display_candidate_local) or
            current_top_should_stay_before_exact_local(best_text_local) then
        begin
            target_idx_local := Min(1, Length(m_candidates));
        end;

        visible_idx_local := find_display_candidate(best_text_local, '');
        if visible_idx_local >= 0 then
        begin
            if visible_idx_local <> target_idx_local then
            begin
                move_display_candidate_to_index(visible_idx_local,
                    target_idx_local);
            end;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' fqens=[move %d->%d %s]', [visible_idx_local,
                    target_idx_local, best_text_local]);
            end;
            Exit;
        end;

        insert_display_candidate(best_candidate_local, best_text_local,
            target_idx_local);
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' fqens=[insert %d %s rank=%d]', [target_idx_local,
                best_text_local, best_rank_local]);
        end;
    end;

    procedure move_display_candidate_to_front(const candidate_idx: Integer);
    var
        idx: Integer;
        target_idx: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
    begin
        if (candidate_idx < 0) or (candidate_idx > High(m_candidates)) then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[candidate_idx];
        target_idx := 0;
        if should_keep_display_top_before_candidate(moved_candidate) then
        begin
            target_idx := Min(1, High(m_candidates));
        end;
        if candidate_idx = target_idx then
        begin
            Exit;
        end;

        moved_path := '';
        if candidate_idx < Length(m_candidate_segment_paths) then
        begin
            moved_path := m_candidate_segment_paths[candidate_idx];
        end;

        if candidate_idx > target_idx then
        begin
            for idx := candidate_idx downto target_idx + 1 do
            begin
                m_candidates[idx] := m_candidates[idx - 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx - 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx - 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end
        else
        begin
            for idx := candidate_idx to target_idx - 1 do
            begin
                m_candidates[idx] := m_candidates[idx + 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx + 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx + 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end;

        m_candidates[target_idx] := moved_candidate;
        if target_idx < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[target_idx] := moved_path;
        end;
    end;

    procedure move_display_candidate_to_index(const candidate_idx: Integer;
        const target_idx: Integer);
    var
        idx: Integer;
        normalized_target_idx: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
    begin
        if (candidate_idx < 0) or (candidate_idx > High(m_candidates)) then
        begin
            Exit;
        end;

        normalized_target_idx := Max(0, Min(target_idx, High(m_candidates)));
        moved_candidate := m_candidates[candidate_idx];
        if (normalized_target_idx = 0) and
            should_keep_display_top_before_candidate(moved_candidate) then
        begin
            normalized_target_idx := Min(1, High(m_candidates));
        end;
        if candidate_idx = normalized_target_idx then
        begin
            Exit;
        end;

        moved_path := '';
        if candidate_idx < Length(m_candidate_segment_paths) then
        begin
            moved_path := m_candidate_segment_paths[candidate_idx];
        end;

        if candidate_idx > normalized_target_idx then
        begin
            for idx := candidate_idx downto normalized_target_idx + 1 do
            begin
                m_candidates[idx] := m_candidates[idx - 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx - 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx - 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end
        else
        begin
            for idx := candidate_idx to normalized_target_idx - 1 do
            begin
                m_candidates[idx] := m_candidates[idx + 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx + 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx + 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end;

        m_candidates[normalized_target_idx] := moved_candidate;
        if normalized_target_idx < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[normalized_target_idx] := moved_path;
        end;
        m_visible_candidates_cache_valid := False;
    end;

    procedure force_move_display_candidate_to_index(const candidate_idx: Integer;
        const target_idx: Integer);
    var
        idx: Integer;
        normalized_target_idx: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
    begin
        if (candidate_idx < 0) or (candidate_idx > High(m_candidates)) then
        begin
            Exit;
        end;

        normalized_target_idx := Max(0, Min(target_idx, High(m_candidates)));
        if candidate_idx = normalized_target_idx then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[candidate_idx];
        moved_path := '';
        if candidate_idx < Length(m_candidate_segment_paths) then
        begin
            moved_path := m_candidate_segment_paths[candidate_idx];
        end;

        if candidate_idx > normalized_target_idx then
        begin
            for idx := candidate_idx downto normalized_target_idx + 1 do
            begin
                m_candidates[idx] := m_candidates[idx - 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx - 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx - 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end
        else
        begin
            for idx := candidate_idx to normalized_target_idx - 1 do
            begin
                m_candidates[idx] := m_candidates[idx + 1];
                if idx < Length(m_candidate_segment_paths) then
                begin
                    if idx + 1 < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[idx] :=
                            m_candidate_segment_paths[idx + 1];
                    end
                    else
                    begin
                        m_candidate_segment_paths[idx] := '';
                    end;
                end;
            end;
        end;

        m_candidates[normalized_target_idx] := moved_candidate;
        if normalized_target_idx < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[normalized_target_idx] := moved_path;
        end;
    end;

    function get_final_fixed_top_text: string;
    begin
        Result := '';
        if normalized_pinyin = '' then
        begin
            Exit;
        end;

        if expected_units <> 1 then
        begin
            if (expected_units = 2) and (normalized_pinyin = 'zheshi') then
            begin
                Result := string(Char($8FD9)) + string(Char($662F));
            end;
            Exit;
        end;

        if normalized_pinyin = 'en' then
        begin
            Result := string(Char($55EF));
        end
        else if normalized_pinyin = 'ba' then
        begin
            Result := string(Char($5427));
        end
        else if normalized_pinyin = 'e' then
        begin
            Result := string(Char($5443));
        end
        else if normalized_pinyin = 'o' then
        begin
            Result := string(Char($54E6));
        end
        else if normalized_pinyin = 'ai' then
        begin
            Result := string(Char($5509));
        end
        else if normalized_pinyin = 'ha' then
        begin
            Result := string(Char($54C8));
        end
        else if normalized_pinyin = 'xi' then
        begin
            Result := string(Char($563B));
        end
        else if normalized_pinyin = 'xing' then
        begin
            Result := string(Char($884C));
        end
        else if normalized_pinyin = 'hao' then
        begin
            Result := string(Char($597D));
        end
        else if normalized_pinyin = 'bang' then
        begin
            Result := string(Char($68D2));
        end
        else if normalized_pinyin = 'ya' then
        begin
            Result := string(Char($5440));
        end
        else if normalized_pinyin = 'qie' then
        begin
            Result := string(Char($5207));
        end
        else if normalized_pinyin = 'ca' then
        begin
            Result := string(Char($64E6));
        end
        else if normalized_pinyin = 'gun' then
        begin
            Result := string(Char($6EDA));
        end
        else if normalized_pinyin = 'hei' then
        begin
            Result := string(Char($563F));
        end
        else if normalized_pinyin = 'pi' then
        begin
            Result := string(Char($5C41));
        end
        else if normalized_pinyin = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_pinyin = 'zhe' then
        begin
            Result := string(Char($8FD9));
        end
        else if normalized_pinyin = 'er' then
        begin
            Result := string(Char($800C));
        end
        else if normalized_pinyin = 'he' then
        begin
            Result := string(Char($548C));
        end
        else if normalized_pinyin = 'you' then
        begin
            Result := string(Char($6709));
        end
        else if normalized_pinyin = 'shi' then
        begin
            Result := string(Char($662F));
        end
        else if normalized_pinyin = 'qing' then
        begin
            Result := string(Char($8BF7));
        end;
    end;

    procedure insert_final_fixed_top_candidate(const candidate_value: TncCandidate);
    var
        idx: Integer;
        candidate_count: Integer;
    begin
        candidate_count := Length(m_candidates);
        SetLength(m_candidates, candidate_count + 1);
        if Length(m_candidate_segment_paths) < candidate_count + 1 then
        begin
            SetLength(m_candidate_segment_paths, candidate_count + 1);
        end;
        for idx := candidate_count downto 1 do
        begin
            m_candidates[idx] := m_candidates[idx - 1];
            if idx < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[idx] := m_candidate_segment_paths[idx - 1];
            end;
        end;
        m_candidates[0] := candidate_value;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := '';
        end;
    end;

    procedure ensure_display_fixed_wo_tail_candidate_visible;
    const
        c_tail_probe_limit = 8;
        c_fixed_wo_tail_base_bonus = 6400;
        c_fixed_wo_tail_unit_bonus = 920;
    var
        head_pinyin: string;
        head_text: string;
        tail_key: string;
        tail_units: Integer;
        tail_results: TncCandidateList;
        candidate_idx: Integer;
        user_exact_idx: Integer;
        target_idx: Integer;
        fixed_candidate: TncCandidate;
        tail_text: string;
        tail_score: Integer;
        tail_dict_weight: Integer;
        out_idx: Integer;
        candidate_text: string;
        candidate_units: TArray<string>;

        function is_full_query_user_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') then
            begin
                Exit;
            end;

            text_local := Trim(candidate_value.text);
            Result := (text_local <> '') and
                (get_candidate_text_unit_count(text_local) = expected_units) and
                m_dictionary.is_user_entry(normalized_pinyin, text_local);
        end;

        function find_first_full_query_user_exact_candidate_local: Integer;
        var
            candidate_idx_local: Integer;
        begin
            Result := -1;
            if (m_dictionary = nil) or (normalized_pinyin = '') then
            begin
                Exit;
            end;

            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                if is_full_query_user_exact_candidate_local(
                    m_candidates[candidate_idx_local]) then
                begin
                    Exit(candidate_idx_local);
                end;
            end;
        end;

        function count_leading_full_query_user_exact_candidates_local: Integer;
        begin
            Result := 0;
            while (Result <= High(m_candidates)) and
                is_full_query_user_exact_candidate_local(m_candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;

        function is_full_query_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') then
            begin
                Exit;
            end;

            text_local := Trim(candidate_value.text);
            if (text_local = '') or
                (get_candidate_text_unit_count(text_local) <> expected_units) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(normalized_pinyin, text_local) or
                m_dictionary.is_user_entry(normalized_pinyin, text_local) then
            begin
                Exit(True);
            end;

            if lookup_display_exact_cached(normalized_pinyin,
                exact_results_local) then
            begin
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) =
                        '') and SameText(Trim(exact_results_local[
                        exact_idx_local].text), text_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function count_leading_full_query_exact_candidates_local: Integer;
        begin
            Result := 0;
            while (Result <= High(m_candidates)) and
                is_full_query_exact_candidate_local(m_candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;

        function try_find_tail_candidate(
            const candidates: TncCandidateList): Boolean;
        var
            tail_idx: Integer;
            candidate_tail_text: string;
            candidate_tail_score: Integer;
            candidate_tail_dict_weight: Integer;
            best_tail_score: Integer;
        begin
            Result := False;
            best_tail_score := Low(Integer);
            for tail_idx := 0 to High(candidates) do
            begin
                if tail_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                if Trim(candidates[tail_idx].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_tail_text := Trim(candidates[tail_idx].text);
                if (candidate_tail_text = '') or
                    (get_candidate_text_unit_count(candidate_tail_text) <> tail_units) then
                begin
                    Continue;
                end;
                if (not m_dictionary.is_base_entry(tail_key, candidate_tail_text)) and
                    (not m_dictionary.is_user_entry(tail_key, candidate_tail_text)) then
                begin
                    Continue;
                end;

                candidate_tail_score := candidates[tail_idx].score;
                candidate_tail_dict_weight := candidate_tail_score;
                if candidates[tail_idx].has_dict_weight then
                begin
                    candidate_tail_dict_weight := candidates[tail_idx].dict_weight;
                    candidate_tail_score := candidates[tail_idx].dict_weight;
                end;
                if candidates[tail_idx].source = cs_user then
                begin
                    Inc(candidate_tail_score, c_user_score_bonus);
                end;
                if Result and (candidate_tail_score <= best_tail_score) then
                begin
                    Continue;
                end;

                best_tail_score := candidate_tail_score;
                tail_text := candidate_tail_text;
                tail_score := candidate_tail_score;
                tail_dict_weight := candidate_tail_dict_weight;
                FillChar(fixed_candidate, SizeOf(fixed_candidate), 0);
                fixed_candidate.text := head_text + tail_text;
                fixed_candidate.comment := '';
                fixed_candidate.source := cs_rule;
                fixed_candidate.score := tail_score + c_fixed_wo_tail_base_bonus +
                    (tail_units * c_fixed_wo_tail_unit_bonus);
                fixed_candidate.has_dict_weight := True;
                fixed_candidate.dict_weight := tail_dict_weight;
                Result := True;
            end;
        end;

        function is_same_head_fixed_tail_complete_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            if Trim(candidate_value.comment) <> '' then
            begin
                Exit;
            end;

            text_local := Trim(candidate_value.text);
            if (text_local = '') or (Length(text_local) <= Length(head_text)) or
                (not SameText(Copy(text_local, 1, Length(head_text)),
                head_text)) or
                (get_candidate_text_unit_count(text_local) <> expected_units) then
            begin
                Exit;
            end;

            tail_text_local := Copy(text_local, Length(head_text) + 1, MaxInt);
            Result := (tail_text_local <> '') and
                (get_candidate_text_unit_count(tail_text_local) = tail_units);
        end;

        procedure keep_fixed_tail_completes_before_partials_local;
        var
            insert_idx_local: Integer;
            scan_idx_local: Integer;
        begin
            insert_idx_local := 0;
            while (insert_idx_local <= High(m_candidates)) and
                is_same_head_fixed_tail_complete_local(
                m_candidates[insert_idx_local]) do
            begin
                Inc(insert_idx_local);
            end;

            scan_idx_local := insert_idx_local + 1;
            while scan_idx_local <= High(m_candidates) do
            begin
                if is_same_head_fixed_tail_complete_local(
                    m_candidates[scan_idx_local]) then
                begin
                    force_move_display_candidate_to_index(scan_idx_local,
                        insert_idx_local);
                    Inc(insert_idx_local);
                    scan_idx_local := insert_idx_local + 1;
                    Continue;
                end;
                Inc(scan_idx_local);
            end;
        end;
    begin
        if (m_dictionary = nil) or
            (not try_get_fixed_pronoun_tail_query_parts(normalized_pinyin,
            expected_units, head_pinyin, head_text, tail_key, tail_units)) then
        begin
            Exit;
        end;

        if tail_key = '' then
        begin
            Exit;
        end;
        if tail_units < 2 then
        begin
            Exit;
        end;

        user_exact_idx := find_first_full_query_user_exact_candidate_local;
        if (not lookup_display_exact_cached(tail_key, tail_results)) or
            (not try_find_tail_candidate(tail_results)) then
        begin
            if (not m_dictionary.lookup(tail_key, tail_results)) or
                (not try_find_tail_candidate(tail_results)) then
            begin
                Exit;
            end;
        end;

        target_idx := count_leading_full_query_exact_candidates_local;
        if user_exact_idx > 0 then
        begin
            force_move_display_candidate_to_index(user_exact_idx, 0);
        end;
        if user_exact_idx >= 0 then
        begin
            target_idx := count_leading_full_query_user_exact_candidates_local;
        end;
        if target_idx > 0 then
        begin
            fixed_candidate.score := Min(fixed_candidate.score,
                Max(0, fixed_candidate.dict_weight) + 160 +
                (tail_units * 24));
        end;

        for candidate_idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[candidate_idx].comment) = '') and
                SameText(Trim(m_candidates[candidate_idx].text),
                fixed_candidate.text) then
            begin
                if is_full_query_user_exact_candidate_local(
                    m_candidates[candidate_idx]) then
                begin
                    Exit;
                end;
                if candidate_idx <> target_idx then
                begin
                    force_move_display_candidate_to_index(candidate_idx,
                        target_idx);
                end;
                Break;
            end;
        end;
        if (Length(m_candidates) = 0) or
            (target_idx > High(m_candidates)) or
            (not SameText(Trim(m_candidates[target_idx].text),
            fixed_candidate.text)) or
            (Trim(m_candidates[target_idx].comment) <> '') then
        begin
            insert_final_fixed_top_candidate(fixed_candidate);
            if target_idx > 0 then
            begin
                force_move_display_candidate_to_index(0, target_idx);
            end;
        end;
        keep_fixed_tail_completes_before_partials_local;

        out_idx := 0;
        for candidate_idx := 0 to High(m_candidates) do
        begin
            candidate_text := Trim(m_candidates[candidate_idx].text);
            candidate_units := split_text_units(candidate_text);
            if (Trim(m_candidates[candidate_idx].comment) <> '') and
                (Length(candidate_units) >= 2) and
                (Trim(candidate_units[0]) <> head_text) and
                m_dictionary.single_char_matches_pinyin(head_pinyin,
                Trim(candidate_units[0])) then
            begin
                Continue;
            end;

            if out_idx <> candidate_idx then
            begin
                m_candidates[out_idx] := m_candidates[candidate_idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if candidate_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[candidate_idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;
        if out_idx < Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure ensure_display_short_particle_tail_candidate_visible;
    const
        c_head_probe_limit = 8;
        c_particle_tail_min_head_score = 80;
        c_particle_tail_visible_bonus = 160;
        c_particle_tail_unit_bonus = 24;
        c_particle_tail_weak_bonus = 8;
    var
        syllables_local: TncPinyinParseResult;
        head_units: Integer;
        tail_units: Integer;
        part_idx: Integer;
        head_key: string;
        tail_key: string;
        tail_text: string;
        head_results: TncCandidateList;
        candidate_idx: Integer;
        user_exact_idx: Integer;
        target_idx: Integer;
        fixed_candidate: TncCandidate;
        fixed_candidates: TncCandidateList;
        fixed_idx: Integer;
        head_text: string;
        head_score: Integer;
        head_dict_weight: Integer;
        candidate_found: Boolean;
        candidate_is_full_query_exact: Boolean;
        require_dehao_head: Boolean;

        function derived_particle_tail_score_local(
            const base_score: Integer): Integer;
        begin
            Result := base_score;
            if Result < 0 then
            begin
                Result := 0;
            end;
            if Result < c_particle_tail_min_head_score then
            begin
                Inc(Result, c_particle_tail_weak_bonus);
                Exit;
            end;

            Inc(Result, c_particle_tail_visible_bonus +
                (head_units * c_particle_tail_unit_bonus));
        end;

        function text_ends_with_tail_particle_local(
            const text_value: string; const tail_text_value: string): Boolean;
        var
            trimmed_text_local: string;
            tail_start_local: Integer;
        begin
            Result := False;
            trimmed_text_local := Trim(text_value);
            if (trimmed_text_local = '') or (tail_text_value = '') then
            begin
                Exit;
            end;
            if Length(trimmed_text_local) < Length(tail_text_value) then
            begin
                Exit;
            end;

            tail_start_local := Length(trimmed_text_local) -
                Length(tail_text_value) + 1;
            Result := SameText(Copy(trimmed_text_local, tail_start_local,
                Length(tail_text_value)), tail_text_value);
        end;

        function is_full_query_user_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') then
            begin
                Exit;
            end;

            text_local := Trim(candidate_value.text);
            Result := (text_local <> '') and
                (get_candidate_text_unit_count(text_local) = expected_units) and
                m_dictionary.is_user_entry(normalized_pinyin, text_local);
        end;

        function is_full_query_exact_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') then
            begin
                Exit;
            end;

            text_local := Trim(candidate_value.text);
            if (text_local = '') or
                (get_candidate_text_unit_count(text_local) <> expected_units) then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(normalized_pinyin, text_local) or
                m_dictionary.is_user_entry(normalized_pinyin, text_local) then
            begin
                Exit(True);
            end;

            if lookup_display_exact_cached(normalized_pinyin,
                exact_results_local) then
            begin
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) =
                        '') and SameText(Trim(exact_results_local[
                        exact_idx_local].text), text_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function find_first_full_query_user_exact_candidate_local: Integer;
        var
            candidate_idx_local: Integer;
        begin
            Result := -1;
            if (m_dictionary = nil) or (normalized_pinyin = '') then
            begin
                Exit;
            end;

            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                if is_full_query_user_exact_candidate_local(
                    m_candidates[candidate_idx_local]) then
                begin
                    Exit(candidate_idx_local);
                end;
            end;
        end;

        function count_leading_full_query_user_exact_candidates_local: Integer;
        begin
            Result := 0;
            while (Result <= High(m_candidates)) and
                is_full_query_user_exact_candidate_local(m_candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;

        function count_leading_full_query_exact_candidates_local: Integer;
        begin
            Result := 0;
            while (Result <= High(m_candidates)) and
                is_full_query_exact_candidate_local(m_candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;

        function has_full_query_exact_candidate_local: Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if (normalized_pinyin = '') or
                (not lookup_display_exact_cached(normalized_pinyin,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (exact_text_local <> '') and
                    (get_candidate_text_unit_count(exact_text_local) =
                    expected_units) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function fixed_candidate_text_exists_local(
            const candidate_text_value: string): Boolean;
        var
            fixed_idx_local: Integer;
        begin
            Result := False;
            for fixed_idx_local := 0 to High(fixed_candidates) do
            begin
                if SameText(Trim(fixed_candidates[fixed_idx_local].text),
                    candidate_text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function should_keep_supported_pair_before_particle_local(
            const candidate_value: TncCandidate;
            const particle_candidate_value: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate_value.comment) = '') and
                is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, Trim(candidate_value.text)) and
                (display_candidate_effective_weight(candidate_value) >=
                display_candidate_effective_weight(particle_candidate_value));
        end;

        function append_head_candidates(
            const candidates: TncCandidateList): Boolean;
        var
            head_idx: Integer;
            candidate_local: TncCandidate;
            candidate_text_local: string;
        begin
            Result := False;
            for head_idx := 0 to High(candidates) do
            begin
                if head_idx >= c_head_probe_limit then
                begin
                    Break;
                end;
                if Trim(candidates[head_idx].comment) <> '' then
                begin
                    Continue;
                end;
                if is_runtime_chain_candidate(candidates[head_idx]) or
                    is_runtime_common_pattern_candidate(candidates[head_idx]) or
                    is_runtime_redup_candidate(candidates[head_idx]) then
                begin
                    Continue;
                end;

                head_text := Trim(candidates[head_idx].text);
                if (head_text = '') or
                    (get_candidate_text_unit_count(head_text) <> head_units) or
                    text_ends_with_tail_particle_local(head_text, tail_text) then
                begin
                    Continue;
                end;
                if require_dehao_head and
                    (not text_ends_with_tail_particle_local(head_text,
                    string(Char($5F97)))) then
                begin
                    Continue;
                end;

                head_score := candidates[head_idx].score;
                head_dict_weight := head_score;
                if candidates[head_idx].has_dict_weight and
                    (candidates[head_idx].dict_weight > head_score) then
                begin
                    head_score := candidates[head_idx].dict_weight;
                    head_dict_weight := candidates[head_idx].dict_weight;
                end;
                if candidates[head_idx].source = cs_user then
                begin
                    Inc(head_score, c_user_score_bonus);
                end;

                candidate_text_local := head_text + tail_text;
                if fixed_candidate_text_exists_local(candidate_text_local) then
                begin
                    Continue;
                end;

                FillChar(candidate_local, SizeOf(candidate_local), 0);
                candidate_local.text := candidate_text_local;
                candidate_local.comment := '';
                candidate_local.source := cs_rule;
                candidate_local.score :=
                    derived_particle_tail_score_local(head_score);
                candidate_local.has_dict_weight := True;
                candidate_local.dict_weight := head_dict_weight;

                SetLength(fixed_candidates, Length(fixed_candidates) + 1);
                fixed_candidates[High(fixed_candidates)] := candidate_local;
                Result := True;
            end;
        end;
    begin
        if (m_page_index <> 0) or (normalized_pinyin = '') or
            (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        if (expected_units = 2) and SameText(normalized_pinyin, 'dehao') then
        begin
            for candidate_idx := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[candidate_idx].comment) = '') and
                    SameText(Trim(m_candidates[candidate_idx].text),
                    string(Char($5F97)) + string(Char($597D))) then
                begin
                    if candidate_idx <> 0 then
                    begin
                        force_move_display_candidate_to_index(candidate_idx, 0);
                    end;
                    Exit;
                end;
            end;

            FillChar(fixed_candidate, SizeOf(fixed_candidate), 0);
            fixed_candidate.text := string(Char($5F97)) + string(Char($597D));
            fixed_candidate.comment := '';
            fixed_candidate.score := 760;
            fixed_candidate.source := cs_rule;
            fixed_candidate.has_dict_weight := True;
            fixed_candidate.dict_weight := 760;
            insert_final_fixed_top_candidate(fixed_candidate);
            Exit;
        end;

        if (m_dictionary = nil) or (expected_units < 3) or
            (expected_units > 4) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalized_pinyin);
        if Length(syllables_local) <> expected_units then
        begin
            Exit;
        end;

        tail_units := 1;
        require_dehao_head := False;
        tail_key := normalize_pinyin_text(syllables_local[High(syllables_local)].text);
        if (expected_units >= 3) and SameText(normalize_pinyin_text(
            syllables_local[High(syllables_local) - 1].text), 'de') and
            SameText(tail_key, 'hao') then
        begin
            tail_text := string(Char($597D));
            require_dehao_head := True;
        end
        else if SameText(tail_key, 'de') then
        begin
            tail_text := string(Char($7684));
        end
        else if SameText(tail_key, 'le') then
        begin
            tail_text := string(Char($4E86));
        end
        else if SameText(tail_key, 'zhe') then
        begin
            tail_text := string(Char($7740));
        end
        else if SameText(tail_key, 'la') then
        begin
            tail_text := string(Char($5566));
        end
        else if SameText(tail_key, 'ba') then
        begin
            tail_text := string(Char($5427));
        end
        else if SameText(tail_key, 'ma') then
        begin
            tail_text := string(Char($5417));
        end
        else if SameText(tail_key, 'a') then
        begin
            tail_text := string(Char($554A));
        end
        else
        begin
            Exit;
        end;

        if has_full_query_exact_candidate_local and
            (not SameText(tail_key, 'a')) then
        begin
            Exit;
        end;

        head_units := expected_units - tail_units;
        if (head_units > 3) or
            ((tail_units = 1) and (head_units < 2)) or
            ((tail_units = 2) and (head_units < 1)) then
        begin
            Exit;
        end;

        head_key := '';
        for part_idx := 0 to head_units - 1 do
        begin
            head_key := head_key + normalize_pinyin_text(
                syllables_local[part_idx].text);
        end;
        if head_key = '' then
        begin
            Exit;
        end;

        SetLength(fixed_candidates, 0);
        if (not lookup_display_exact_cached(head_key, head_results)) or
            (not append_head_candidates(head_results)) then
        begin
            Exit;
        end;

        user_exact_idx := find_first_full_query_user_exact_candidate_local;
        if user_exact_idx > 0 then
        begin
            force_move_display_candidate_to_index(user_exact_idx, 0);
        end;
        target_idx := count_leading_full_query_exact_candidates_local;
        if user_exact_idx >= 0 then
        begin
            target_idx := Max(target_idx,
                count_leading_full_query_user_exact_candidates_local);
        end;

        for fixed_idx := 0 to High(fixed_candidates) do
        begin
            fixed_candidate := fixed_candidates[fixed_idx];
            while (target_idx <= High(m_candidates)) and
                should_keep_supported_pair_before_particle_local(
                m_candidates[target_idx], fixed_candidate) do
            begin
                Inc(target_idx);
            end;
            candidate_found := False;
            candidate_is_full_query_exact := False;
            for candidate_idx := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[candidate_idx].comment) = '') and
                    SameText(Trim(m_candidates[candidate_idx].text),
                    fixed_candidate.text) then
                begin
                    candidate_found := True;
                    if is_full_query_exact_candidate_local(
                        m_candidates[candidate_idx]) then
                    begin
                        candidate_is_full_query_exact := True;
                        Break;
                    end;
                    if candidate_idx <> target_idx then
                    begin
                        force_move_display_candidate_to_index(candidate_idx,
                            target_idx);
                    end;
                    Break;
                end;
            end;

            if candidate_is_full_query_exact then
            begin
                Continue;
            end;

            if (not candidate_found) or (target_idx > High(m_candidates)) or
                (not ((target_idx >= 0) and
                (Trim(m_candidates[target_idx].comment) = '') and
                SameText(Trim(m_candidates[target_idx].text),
                fixed_candidate.text))) then
            begin
                insert_final_fixed_top_candidate(fixed_candidate);
                if target_idx > 0 then
                begin
                    force_move_display_candidate_to_index(0, target_idx);
                end;
            end;
            Inc(target_idx);
        end;
    end;

    procedure enforce_final_fixed_top_candidate;
    var
        expected_text: string;
        idx: Integer;
        exact_idx: Integer;
        exact_results: TncCandidateList;
        fixed_candidate: TncCandidate;
        found_candidate: Boolean;
    begin
        expected_text := get_final_fixed_top_text;
        if expected_text = '' then
        begin
            Exit;
        end;

        for idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[idx].comment) = '') and
                SameText(Trim(m_candidates[idx].text), expected_text) then
            begin
                if idx > 0 then
                begin
                    force_move_display_candidate_to_index(idx, 0);
                end;
                Exit;
            end;
        end;

        found_candidate := False;
        if lookup_display_exact_cached(normalized_pinyin, exact_results) then
        begin
            for exact_idx := 0 to High(exact_results) do
            begin
                if (Trim(exact_results[exact_idx].comment) = '') and
                    SameText(Trim(exact_results[exact_idx].text), expected_text) then
                begin
                    fixed_candidate := exact_results[exact_idx];
                    found_candidate := True;
                    Break;
                end;
            end;
        end;
        if not found_candidate then
        begin
            Exit;
        end;

        if Length(m_candidates) > 0 then
        begin
            fixed_candidate.score := Max(fixed_candidate.score,
                m_candidates[0].score + 512);
        end;
        fixed_candidate.has_dict_weight := True;
        fixed_candidate.dict_weight := Max(fixed_candidate.dict_weight,
            fixed_candidate.score);
        insert_final_fixed_top_candidate(fixed_candidate);
    end;

    procedure promote_complete_long_candidate_over_partial_top(
        const require_structural_signal: Boolean);
    const
        c_probe_limit = 8;
        c_min_long_units = 8;
        c_complete_over_complete_margin = 128;
        c_complete_over_partial_margin = -2200;
    var
        idx: Integer;
        probe_limit: Integer;
        best_idx: Integer;
        best_score: Integer;
        top_score: Integer;
        top_is_complete: Boolean;
        candidate_text: string;
        candidate_score: Integer;
        particle_exact_weight_cache: TDictionary<string, Integer>;

        function complete_candidate_rank_score(const candidate_value: TncCandidate): Integer;
        var
            raw_score: Integer;
        begin
            raw_score := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > raw_score) then
            begin
                raw_score := candidate_value.dict_weight;
            end;

            Result := raw_score;
            if (expected_units >= c_min_long_units) and (Result > 24000) then
            begin
                Result := 24000 + ((Result - 24000) div 32);
                if Result > 36000 then
                begin
                    Result := 36000;
                end;
            end;
        end;

        function is_complete_display_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate_value.comment) = '') and
                (Trim(candidate_value.text) <> '') and
                (get_candidate_text_unit_count(Trim(candidate_value.text)) =
                expected_units);
        end;

        function is_likely_verbal_unit_local(const unit_text: string): Boolean;
        begin
            Result := False;
            if unit_text = '' then
            begin
                Exit;
            end;

            case Ord(unit_text[1]) of
                $7EE7, $94BB, $5C55, $89E3, $53D1, $8FD0, $5904, $4F7F,
                $6267, $63D0, $6295, $9009, $6210, $8FDB, $6539, $8BAD,
                $62FF,
                $6D4B, $5B66, $5DE5, $505A, $5199, $770B, $8BF4,
                $8BFB, $95EE, $8D70, $53BB, $6765, $5750, $5E2E, $627E,
                $5E26, $4FDD, $53D8, $8F6C, $63A5, $89C2, $6CE8, $6D41,
                $8F93, $843D, $964D, $8BA9, $8BC6, $5E94, $7528, $5F00,
                $5173, $4EA7, $751F, $53D6, $83B7, $5305, $56F4, $63CF:
                    Result := True;
            end;
        end;

        function is_likely_result_complement_head_unit_local(
            const unit_text: string): Boolean;
        begin
            Result := False;
            if unit_text = '' then
            begin
                Exit;
            end;

            case Ord(unit_text[1]) of
                $4E0D, $4E86, $5230, $51FA, $597D, $5B8C, $5F00, $5F88,
                $5FEB, $6162, $660E, $6709, $6E05, $719F, $767D, $76F4,
                $771F, $7A33, $7EA2, $8001, $8D77, $8FDC, $8FD1, $900F,
                $9519, $9AD8:
                    Result := True;
            end;
        end;

        function get_first_text_unit_local(const text_value: string): string;
        var
            units_local: TArray<string>;
        begin
            Result := '';
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) > 0 then
            begin
                Result := Trim(units_local[0]);
            end;
        end;

        function get_last_text_unit_local(const text_value: string): string;
        var
            units_local: TArray<string>;
        begin
            Result := '';
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) > 0 then
            begin
                Result := Trim(units_local[High(units_local)]);
            end;
        end;

        function try_get_particle_exact_weight_local(const query_key: string;
            const text_value: string; const unit_count: Integer;
            out out_weight: Integer): Boolean;
        var
            cache_key_local: string;
            cached_weight_local: Integer;
        begin
            Result := False;
            out_weight := 0;
            if (query_key = '') or (text_value = '') or (unit_count <= 0) then
            begin
                Exit;
            end;

            cache_key_local := query_key + #1 + text_value + #1 +
                IntToStr(unit_count);
            if (particle_exact_weight_cache <> nil) and
                particle_exact_weight_cache.TryGetValue(cache_key_local,
                cached_weight_local) then
            begin
                if cached_weight_local > Low(Integer) then
                begin
                    out_weight := cached_weight_local;
                    Exit(True);
                end;
                Exit(False);
            end;

            Result := get_display_exact_text_weight(query_key, text_value,
                unit_count, out_weight);
            if particle_exact_weight_cache <> nil then
            begin
                if Result then
                begin
                    particle_exact_weight_cache.AddOrSetValue(cache_key_local,
                        out_weight);
                end
                else
                begin
                    particle_exact_weight_cache.AddOrSetValue(cache_key_local,
                        Low(Integer));
                end;
            end;
        end;

        function particle_text_context_score_local(
            const candidate_text_value: string): Integer;
        var
            units_local: TArray<string>;
            unit_idx: Integer;
            current_unit: string;
            next_unit: string;
            next_is_verbal: Boolean;
        begin
            Result := 0;
            units_local := split_text_units(Trim(candidate_text_value));
            for unit_idx := 0 to High(units_local) - 1 do
            begin
                current_unit := Trim(units_local[unit_idx]);
                next_unit := Trim(units_local[unit_idx + 1]);
                if (current_unit = '') or (next_unit = '') then
                begin
                    Continue;
                end;

                next_is_verbal := is_likely_verbal_unit_local(next_unit);
                if SameText(current_unit, string(Char($5730))) then
                begin
                    if next_is_verbal then
                    begin
                        Inc(Result, 5000);
                    end
                    else
                    begin
                        Dec(Result, 12000);
                    end;
                end
                else if SameText(current_unit, string(Char($7684))) then
                begin
                    if next_is_verbal then
                    begin
                        Dec(Result, 7000);
                    end
                    else
                    begin
                        Inc(Result, 3000);
                    end;
                end;
            end;
        end;

        function is_path_pronoun_unit_local(const unit_text: string): Boolean;
        begin
            Result := SameText(unit_text, string(Char($6211))) or
                SameText(unit_text, string(Char($4F60))) or
                SameText(unit_text, string(Char($4ED6))) or
                SameText(unit_text, string(Char($5979))) or
                SameText(unit_text, string(Char($5B83)));
        end;

        function is_path_predicate_or_modifier_head_local(
            const unit_text: string): Boolean;
        begin
            Result := SameText(unit_text, string(Char($4E0D))) or
                SameText(unit_text, string(Char($5E76))) or
                SameText(unit_text, string(Char($4F1A))) or
                SameText(unit_text, string(Char($80FD))) or
                SameText(unit_text, string(Char($60F3))) or
                SameText(unit_text, string(Char($8981))) or
                SameText(unit_text, string(Char($77E5))) or
                SameText(unit_text, string(Char($89C9))) or
                SameText(unit_text, string(Char($53EF))) or
                SameText(unit_text, string(Char($5E94))) or
                SameText(unit_text, string(Char($9700))) or
                SameText(unit_text, string(Char($5DF2))) or
                SameText(unit_text, string(Char($6B63))) or
                SameText(unit_text, string(Char($518D))) or
                SameText(unit_text, string(Char($8FD8))) or
                SameText(unit_text, string(Char($90FD))) or
                SameText(unit_text, string(Char($5C31))) or
                SameText(unit_text, string(Char($53EA))) or
                SameText(unit_text, string(Char($628A))) or
                SameText(unit_text, string(Char($88AB))) or
                SameText(unit_text, string(Char($7ED9))) or
                SameText(unit_text, string(Char($8BA9))) or
                SameText(unit_text, string(Char($5411))) or
                is_likely_verbal_unit_local(unit_text);
        end;

        function candidate_has_structural_path_signal_local(
            const candidate_idx: Integer): Boolean;
        var
            path_local: string;
            parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_text_local: string;
            next_segment_text_local: string;
            next_next_segment_text_local: string;
            segment_units_local: Integer;
            next_units_local: Integer;
            next_next_units_local: Integer;
            segment_last_unit_local: string;
            next_first_unit_local: string;
            inferred_score_local: Integer;
            inferred_count_local: Integer;
        begin
            Result := False;
            if (candidate_idx < 0) or
                (candidate_idx >= Length(m_candidates)) then
            begin
                Exit;
            end;

            path_local := '';
            if candidate_idx < Length(m_candidate_segment_paths) then
            begin
                path_local := Trim(m_candidate_segment_paths[candidate_idx]);
            end;
            if path_local = '' then
            begin
                path_local := Trim(get_segment_path_for_candidate(
                    m_candidates[candidate_idx], candidate_idx));
            end;
            if path_local = '' then
            begin
                path_local := infer_segment_path_for_query_text_with_score(
                    normalized_pinyin, Trim(m_candidates[candidate_idx].text),
                    inferred_score_local, inferred_count_local);
            end;
            if get_encoded_path_segment_count_local(path_local) <= 1 then
            begin
                Exit;
            end;

            parts_local := path_local.Split([c_segment_path_separator]);
            for part_idx_local := 0 to High(parts_local) - 1 do
            begin
                segment_text_local := Trim(parts_local[part_idx_local]);
                next_segment_text_local := Trim(parts_local[part_idx_local + 1]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                next_units_local := get_candidate_text_unit_count(
                    next_segment_text_local);
                if (segment_units_local <= 0) or (next_units_local <= 0) then
                begin
                    Continue;
                end;

                segment_last_unit_local := get_last_text_unit_local(
                    segment_text_local);
                next_first_unit_local := get_first_text_unit_local(
                    next_segment_text_local);

                if (segment_units_local = 1) and (next_units_local >= 2) then
                begin
                    if SameText(segment_last_unit_local, string(Char($5BF9))) or
                        (is_path_pronoun_unit_local(segment_last_unit_local) and
                        is_path_predicate_or_modifier_head_local(
                        next_first_unit_local)) or
                        (SameText(segment_last_unit_local, string(Char($66F4))) and
                        SameText(next_first_unit_local, string(Char($4E3A)))) then
                    begin
                        Exit(True);
                    end;
                end;

                if (segment_units_local = 1) and (next_units_local = 1) and
                    (part_idx_local + 1 < High(parts_local)) then
                begin
                    next_next_segment_text_local :=
                        Trim(parts_local[part_idx_local + 2]);
                    next_next_units_local := get_candidate_text_unit_count(
                        next_next_segment_text_local);
                    if (next_next_units_local >= 2) and
                        SameText(segment_last_unit_local, string(Char($5E76))) and
                        SameText(next_first_unit_local, string(Char($4E0D))) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function particle_path_context_score_local(const candidate_idx: Integer): Integer;
        var
            path_local: string;
            parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_text_local: string;
            next_segment_text_local: string;
            next_next_segment_text_local: string;
            segment_last_unit_local: string;
            next_first_unit_local: string;
            segment_units_local: Integer;
            next_units_local: Integer;
            next_next_units_local: Integer;
            segment_start_local: Integer;
            segment_first_unit_local: string;
            segment_key_local: string;
            next_last_unit_local: string;
            next_key_local: string;
            segment_weight_local: Integer;
            next_weight_local: Integer;
            segment_has_exact_weight_local: Boolean;
            next_has_exact_weight_local: Boolean;
            next_is_verbal_local: Boolean;
            low_multi_exact_run_local: Integer;
            low_run_after_high_multi_local: Boolean;
            previous_high_multi_local: Boolean;
            current_high_multi_local: Boolean;
            inferred_score_local: Integer;
            inferred_count_local: Integer;

            function is_directional_location_head_local(const unit_text: string): Boolean;
            begin
                Result := SameText(unit_text, string(Char($5317))) or
                    SameText(unit_text, string(Char($5357))) or
                    SameText(unit_text, string(Char($4E1C))) or
                    SameText(unit_text, string(Char($897F))) or
                    SameText(unit_text, string(Char($4E0A))) or
                    SameText(unit_text, string(Char($4E0B))) or
                    SameText(unit_text, string(Char($524D))) or
                    SameText(unit_text, string(Char($540E))) or
                    SameText(unit_text, string(Char($5DE6))) or
                    SameText(unit_text, string(Char($53F3))) or
                    SameText(unit_text, string(Char($91CC))) or
                    SameText(unit_text, string(Char($5916)));
            end;

            function is_place_like_tail_unit_local(const unit_text: string): Boolean;
            begin
                Result := SameText(unit_text, string(Char($56ED))) or
                    SameText(unit_text, string(Char($9986))) or
                    SameText(unit_text, string(Char($573A))) or
                    SameText(unit_text, string(Char($5E97))) or
                    SameText(unit_text, string(Char($6821))) or
                    SameText(unit_text, string(Char($9662))) or
                    SameText(unit_text, string(Char($697C))) or
                    SameText(unit_text, string(Char($7AD9))) or
                    SameText(unit_text, string(Char($57CE))) or
                    SameText(unit_text, string(Char($533A))) or
                    SameText(unit_text, string(Char($5E02))) or
                    SameText(unit_text, string(Char($6751))) or
                    SameText(unit_text, string(Char($9547))) or
                    SameText(unit_text, string(Char($53BF))) or
                    SameText(unit_text, string(Char($8DEF))) or
                    SameText(unit_text, string(Char($8857))) or
                    SameText(unit_text, string(Char($6865))) or
                    SameText(unit_text, string(Char($6CB3))) or
                    SameText(unit_text, string(Char($6D77))) or
                    SameText(unit_text, string(Char($5C71))) or
                    SameText(unit_text, string(Char($5C9B))) or
                    SameText(unit_text, string(Char($95E8))) or
                    SameText(unit_text, string(Char($5385))) or
                    SameText(unit_text, string(Char($5BA4))) or
                    SameText(unit_text, string(Char($623F))) or
                    SameText(unit_text, string(Char($5BB6))) or
                    SameText(unit_text, string(Char($516C))) or
                    SameText(unit_text, string(Char($5BD3)));
            end;
        begin
            Result := 0;
            if (candidate_idx < 0) or
                (candidate_idx >= Length(m_candidates)) then
            begin
                Exit;
            end;

            path_local := '';
            if candidate_idx < Length(m_candidate_segment_paths) then
            begin
                path_local := Trim(m_candidate_segment_paths[candidate_idx]);
            end;
            if path_local = '' then
            begin
                path_local := Trim(get_segment_path_for_candidate(
                    m_candidates[candidate_idx], candidate_idx));
            end;
            if path_local = '' then
            begin
                path_local := infer_segment_path_for_query_text_with_score(
                    normalized_pinyin, Trim(m_candidates[candidate_idx].text),
                    inferred_score_local, inferred_count_local);
            end;
            if get_encoded_path_segment_count_local(path_local) <= 1 then
            begin
                Exit;
            end;

            parts_local := path_local.Split([c_segment_path_separator]);
            segment_start_local := 0;
            low_multi_exact_run_local := 0;
            low_run_after_high_multi_local := False;
            previous_high_multi_local := False;
            for part_idx_local := 0 to High(parts_local) do
            begin
                segment_text_local := Trim(parts_local[part_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) then
                begin
                    Exit;
                end;

                next_segment_text_local := '';
                next_next_segment_text_local := '';
                next_units_local := 0;
                next_next_units_local := 0;
                next_first_unit_local := '';
                next_last_unit_local := '';
                next_is_verbal_local := False;
                if part_idx_local < High(parts_local) then
                begin
                    next_segment_text_local := Trim(parts_local[part_idx_local + 1]);
                    next_units_local := get_candidate_text_unit_count(
                        next_segment_text_local);
                    next_first_unit_local := get_first_text_unit_local(
                        next_segment_text_local);
                    next_last_unit_local := get_last_text_unit_local(
                        next_segment_text_local);
                    next_is_verbal_local :=
                        is_likely_verbal_unit_local(next_first_unit_local);
                end;
                if part_idx_local + 1 < High(parts_local) then
                begin
                    next_next_segment_text_local :=
                        Trim(parts_local[part_idx_local + 2]);
                    next_next_units_local := get_candidate_text_unit_count(
                        next_next_segment_text_local);
                end;

                segment_first_unit_local := get_first_text_unit_local(
                    segment_text_local);
                segment_last_unit_local := get_last_text_unit_local(
                    segment_text_local);
                segment_weight_local := 0;
                segment_has_exact_weight_local := False;
                current_high_multi_local := False;
                if segment_units_local >= 2 then
                begin
                    segment_key_local := build_display_query_key(
                        segment_start_local, segment_units_local);
                    segment_has_exact_weight_local :=
                        (segment_key_local <> '') and
                        try_get_particle_exact_weight_local(segment_key_local,
                        segment_text_local, segment_units_local,
                        segment_weight_local);
                    current_high_multi_local :=
                        ((segment_units_local >= 4) or
                        (segment_has_exact_weight_local and
                        (segment_units_local >= 3) and
                        (segment_weight_local >= 900)));
                    if (not segment_has_exact_weight_local) or
                        (segment_weight_local < 700) then
                    begin
                        if low_multi_exact_run_local = 0 then
                        begin
                            low_run_after_high_multi_local :=
                                previous_high_multi_local;
                        end;
                        Inc(low_multi_exact_run_local);
                        if low_run_after_high_multi_local and
                            (low_multi_exact_run_local >= 2) then
                        begin
                            Dec(Result, 26000 +
                                ((low_multi_exact_run_local - 2) * 8000));
                        end;
                    end
                    else
                    begin
                        low_multi_exact_run_local := 0;
                        low_run_after_high_multi_local := False;
                    end;
                    if current_high_multi_local then
                    begin
                        Inc(Result, 16000);
                    end;
                    if segment_has_exact_weight_local then
                    begin
                        if segment_units_local = 2 then
                        begin
                            if segment_weight_local >= 900 then
                            begin
                                Inc(Result, 11000);
                            end
                            else if segment_weight_local >= 700 then
                            begin
                                Inc(Result, 8000);
                            end
                            else if segment_weight_local >= 520 then
                            begin
                                Inc(Result, 4200);
                            end
                            else if segment_weight_local < 460 then
                            begin
                                Dec(Result, 6200);
                            end;
                        end
                        else if (segment_units_local >= 3) and
                            (segment_weight_local >= 700) then
                        begin
                            Inc(Result, 7000 + Min(5000, segment_weight_local * 2));
                        end;
                    end;
                    if (segment_units_local = 2) and
                        is_directional_location_head_local(segment_first_unit_local) and
                        SameText(segment_last_unit_local, string(Char($9762))) and
                        (next_units_local = 1) and
                        SameText(next_first_unit_local, string(Char($7684))) then
                    begin
                        Inc(Result, 18000);
                    end;
                end
                else
                begin
                    low_multi_exact_run_local := 0;
                    low_run_after_high_multi_local := False;
                end;

                if segment_units_local = 1 then
                begin
                    if (next_segment_text_local <> '') and (next_units_local >= 2) then
                    begin
                        if SameText(segment_last_unit_local, string(Char($5BF9))) then
                        begin
                            Inc(Result, 14000);
                        end
                        else if is_path_pronoun_unit_local(segment_last_unit_local) and
                            is_path_predicate_or_modifier_head_local(next_first_unit_local) then
                        begin
                            Inc(Result, 9000);
                        end
                        else if SameText(segment_last_unit_local, string(Char($66F4))) and
                            SameText(next_first_unit_local, string(Char($4E3A))) then
                        begin
                            Inc(Result, 12000);
                        end
                        else if SameText(segment_last_unit_local, string(Char($53BB))) and
                            is_place_like_tail_unit_local(next_last_unit_local) then
                        begin
                            Inc(Result, 14000);
                        end;
                    end
                    else if (next_units_local = 1) and
                        (next_next_segment_text_local <> '') and (next_next_units_local >= 2) then
                    begin
                        if SameText(segment_last_unit_local, string(Char($5E76))) and
                            SameText(next_first_unit_local, string(Char($4E0D))) then
                        begin
                            Inc(Result, 16000);
                        end;
                    end;
                end;

                if (segment_units_local > 1) and (next_units_local >= 2) and
                    SameText(segment_last_unit_local, string(Char($53BB))) and
                    is_place_like_tail_unit_local(next_last_unit_local) then
                begin
                    Inc(Result, 14000);
                end;

                if SameText(segment_last_unit_local, string(Char($5730))) then
                begin
                    if (segment_units_local >= 2) and
                        (next_units_local >= 2) and
                        ((not segment_has_exact_weight_local) or
                        (segment_weight_local < 700)) then
                    begin
                        Dec(Result, 18000);
                    end
                    else if next_is_verbal_local then
                    begin
                        Inc(Result, 6000);
                    end
                    else if next_segment_text_local <> '' then
                    begin
                        Dec(Result, 8000);
                    end;
                end
                else if SameText(segment_last_unit_local, string(Char($7684))) then
                begin
                    if segment_units_local = 1 then
                    begin
                        if next_units_local >= 2 then
                        begin
                            if (next_next_segment_text_local = '') or
                                (next_next_units_local >= 2) then
                            begin
                                Inc(Result, 12000);
                            end;
                            if (next_next_units_local = 1) and
                                SameText(get_first_text_unit_local(
                                next_next_segment_text_local),
                                string(Char($662F))) then
                            begin
                                Dec(Result, 12000);
                            end;
                            next_weight_local := 0;
                            next_key_local := build_display_query_key(
                                segment_start_local + segment_units_local,
                                next_units_local);
                            next_has_exact_weight_local := (next_key_local <> '') and
                                (next_segment_text_local <> '') and
                                try_get_particle_exact_weight_local(next_key_local,
                                next_segment_text_local, next_units_local,
                                next_weight_local);
                            if next_has_exact_weight_local and
                                (next_weight_local >= 900) and
                                ((next_next_segment_text_local = '') or
                                (next_next_units_local >= 2)) then
                            begin
                                Inc(Result, 14000);
                            end;
                        end;
                    end
                    else if next_is_verbal_local then
                    begin
                        Dec(Result, 10000);
                    end
                    else if next_segment_text_local <> '' then
                    begin
                        Inc(Result, 3000);
                    end;
                end
                else if SameText(segment_last_unit_local, string(Char($5F97))) then
                begin
                    if next_segment_text_local = '' then
                    begin
                        Dec(Result, 7000);
                    end
                    else if (segment_units_local >= 2) and
                        (next_units_local >= 2) and
                        is_likely_result_complement_head_unit_local(
                        next_first_unit_local) then
                    begin
                        Inc(Result, 14000);
                    end
                    else if (segment_units_local >= 2) and (next_units_local >= 2) then
                    begin
                        Dec(Result, 10000);
                    end;
                end;

                Inc(segment_start_local, segment_units_local);
                previous_high_multi_local := current_high_multi_local;
            end;
        end;

        function particle_candidate_context_score_local(
            const candidate_idx: Integer; const candidate_text_value: string): Integer;
        begin
            Result := particle_text_context_score_local(candidate_text_value) +
                particle_path_context_score_local(candidate_idx);
        end;
    begin
        if (expected_units < c_min_long_units) or (Length(m_candidates) <= 1) then
        begin
            Exit;
        end;

        particle_exact_weight_cache := TDictionary<string, Integer>.Create;
        try
            top_is_complete := is_complete_display_candidate_local(m_candidates[0]);
            top_score := complete_candidate_rank_score(m_candidates[0]) +
                particle_candidate_context_score_local(0,
                Trim(m_candidates[0].text));
            best_idx := -1;
            best_score := Low(Integer);
            probe_limit := Min(c_probe_limit - 1, High(m_candidates));
            for idx := 1 to probe_limit do
            begin
                if not is_complete_display_candidate_local(m_candidates[idx]) then
                begin
                    Continue;
                end;
                if require_structural_signal and
                    (not candidate_has_structural_path_signal_local(idx)) then
                begin
                    Continue;
                end;

                candidate_text := Trim(m_candidates[idx].text);
                if candidate_text = '' then
                begin
                    Continue;
                end;

                candidate_score := complete_candidate_rank_score(m_candidates[idx]) +
                    particle_candidate_context_score_local(idx, candidate_text);
                if (best_idx < 0) or (candidate_score > best_score) then
                begin
                    best_idx := idx;
                    best_score := candidate_score;
                end;
            end;

            if best_idx <= 0 then
            begin
                Exit;
            end;

            if top_is_complete then
            begin
                if best_score < top_score + c_complete_over_complete_margin then
                begin
                    Exit;
                end;
            end
            else if best_score < top_score + c_complete_over_partial_margin then
            begin
                Exit;
            end;

            force_move_display_candidate_to_index(best_idx, 0);
        finally
            particle_exact_weight_cache.Free;
        end;
    end;

    procedure promote_higher_rank_complete_long_candidate_final;
    const
        c_probe_limit = 8;
        c_min_units = 7;
        c_min_rank_margin = 128;
    var
        idx: Integer;
        probe_limit: Integer;
        top_units_local: Integer;
        top_rank_local: Integer;
        candidate_units_local: Integer;
        candidate_rank_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;

        function complete_candidate_rank_local(
            const candidate_value: TncCandidate): Integer;
        begin
            Result := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;
        end;
    begin
        if Length(m_candidates) <= 1 then
        begin
            Exit;
        end;

        if Trim(m_candidates[0].comment) <> '' then
        begin
            Exit;
        end;
        top_units_local := get_candidate_text_unit_count(
            Trim(m_candidates[0].text));
        if top_units_local < c_min_units then
        begin
            Exit;
        end;

        top_rank_local := complete_candidate_rank_local(m_candidates[0]);
        best_idx_local := -1;
        best_rank_local := top_rank_local;
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate_units_local := get_candidate_text_unit_count(
                Trim(m_candidates[idx].text));
            if candidate_units_local <> top_units_local then
            begin
                Continue;
            end;

            candidate_rank_local := complete_candidate_rank_local(
                m_candidates[idx]);
            if candidate_rank_local >= best_rank_local + c_min_rank_margin then
            begin
                best_idx_local := idx;
                best_rank_local := candidate_rank_local;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    function get_display_candidate_path_for_index(
        const candidate_value: TncCandidate; const candidate_idx: Integer): string;
    var
        inferred_score_local: Integer;
        inferred_count_local: Integer;
    begin
        Result := '';
        if (candidate_idx >= 0) and
            (candidate_idx < Length(m_candidate_segment_paths)) then
        begin
            Result := m_candidate_segment_paths[candidate_idx];
        end;
        if Result = '' then
        begin
            Result := get_segment_path_for_candidate(candidate_value,
                candidate_idx);
        end;
        if (Result = '') and (normalized_pinyin <> '') then
        begin
            Result := infer_segment_path_for_query_text_with_score(
                normalized_pinyin, Trim(candidate_value.text),
            inferred_score_local, inferred_count_local);
        end;
    end;

    function get_display_complete_exact_span_coverage_score(
        const candidate_value: TncCandidate): Integer;
    const
        c_max_span_units = 5;
        c_min_span_weight = 120;
    var
        cache_key_local: string;
        units_local: TArray<string>;
        occupied_local: TArray<Boolean>;
        span_len_local: Integer;
        start_idx_local: Integer;
        unit_idx_local: Integer;
        query_key_local: string;
        span_text_local: string;
        span_weight_local: Integer;
        overlaps_local: Boolean;
    begin
        Result := 0;
        if (expected_units < 6) or (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        units_local := split_text_units(Trim(candidate_value.text));
        if Length(units_local) <> expected_units then
        begin
            Exit;
        end;
        cache_key_local := 'span' + #1 + normalized_pinyin + #1 +
            Trim(candidate_value.text);
        if (m_lookup_display_feature_cache <> nil) and
            m_lookup_display_feature_cache.TryGetValue(cache_key_local,
            Result) then
        begin
            Exit;
        end;

        SetLength(occupied_local, Length(units_local));
        for span_len_local := Min(c_max_span_units, expected_units) downto 2 do
        begin
            for start_idx_local := 0 to expected_units - span_len_local do
            begin
                overlaps_local := False;
                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    if occupied_local[unit_idx_local] then
                    begin
                        overlaps_local := True;
                        Break;
                    end;
                end;
                if overlaps_local then
                begin
                    Continue;
                end;

                query_key_local := build_display_query_key(start_idx_local,
                    span_len_local);
                span_text_local := '';
                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    span_text_local := span_text_local + units_local[unit_idx_local];
                end;

                if (query_key_local <> '') and
                    get_display_exact_text_weight(query_key_local,
                    span_text_local, span_len_local, span_weight_local) and
                    (span_weight_local >= c_min_span_weight) then
                begin
                    Inc(Result, span_len_local * span_len_local * 800 +
                        span_weight_local * span_len_local);
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_local[unit_idx_local] := True;
                    end;
                end;
            end;
        end;
        if m_lookup_display_feature_cache <> nil then
        begin
            m_lookup_display_feature_cache.AddOrSetValue(cache_key_local,
                Result);
        end;
    end;

    function display_candidate_replaces_protected_conjunction_boundary(
        const candidate_value: TncCandidate): Boolean;
    const
        c_max_neighbor_units = 4;
    var
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        diff_idx_local: Integer;

        function is_protected_conjunction_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $548C, $4E0E, $800C, $53CA, $6216, $5E76, $4E14:
                    Result := True;
            end;
        end;

        function join_units_range_local(const units_value: TArray<string>;
            const local_start_idx: Integer; const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to local_start_idx + local_span_len - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function top_exact_neighbor_exists_local(const local_start_idx: Integer;
            const local_span_len: Integer): Boolean;
        var
            query_key_local: string;
            text_local: string;
            weight_local: Integer;
        begin
            Result := False;
            if (local_start_idx < 0) or (local_span_len < 2) or
                (local_start_idx + local_span_len > Length(top_units_local)) then
            begin
                Exit;
            end;

            query_key_local := build_display_query_key(local_start_idx,
                local_span_len);
            text_local := join_units_range_local(top_units_local,
                local_start_idx, local_span_len);
            Result := (query_key_local <> '') and (text_local <> '') and
                get_display_exact_text_weight(query_key_local, text_local,
                local_span_len, weight_local) and (weight_local > 0);
        end;

        function has_protected_neighbor_local(const local_idx: Integer): Boolean;
        var
            span_len_local: Integer;
        begin
            Result := False;
            for span_len_local := 2 to c_max_neighbor_units do
            begin
                if top_exact_neighbor_exists_local(local_idx + 1,
                    span_len_local) or
                    top_exact_neighbor_exists_local(local_idx - span_len_local,
                    span_len_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        Result := False;
        top_units_local := split_text_units(Trim(m_candidates[0].text));
        candidate_units_local := split_text_units(Trim(candidate_value.text));
        if (Length(top_units_local) <> expected_units) or
            (Length(candidate_units_local) <> expected_units) then
        begin
            Exit;
        end;

        for diff_idx_local := 0 to expected_units - 1 do
        begin
            if (diff_idx_local = 0) or
                SameText(top_units_local[diff_idx_local],
                candidate_units_local[diff_idx_local]) or
                (not is_protected_conjunction_unit_local(
                top_units_local[diff_idx_local])) or
                is_display_function_or_modifier_single(
                candidate_units_local[diff_idx_local]) then
            begin
                Continue;
            end;

            if has_protected_neighbor_local(diff_idx_local) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure promote_better_exact_span_covered_complete_candidate;
    const
        c_probe_limit = 5;
        c_min_coverage_gain = 1200;
        c_max_score_deficit = 120000;
        c_min_protected_exact_weight = 700;
        c_protected_exact_margin = 80;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        top_score_local: Integer;
        top_coverage_local: Integer;
        candidate_score_local: Integer;
        candidate_coverage_local: Integer;
        best_metric_local: Integer;
        candidate_metric_local: Integer;

        function candidate_replaces_stronger_same_position_exact_span(
            const candidate_value: TncCandidate): Boolean;
        const
            c_max_span_units = 5;
        var
            top_units_local: TArray<string>;
            candidate_units_local: TArray<string>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            top_span_text_local: string;
            candidate_span_text_local: string;
            top_span_weight_local: Integer;
            candidate_span_weight_local: Integer;
            span_touches_diff_local: Boolean;

            function join_units_range_local(const units_value: TArray<string>;
                const local_start_idx: Integer; const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (local_start_idx < 0) or (local_span_len <= 0) or
                    (local_start_idx + local_span_len > Length(units_value)) then
                begin
                    Exit;
                end;

                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;

        begin
            Result := False;
            top_units_local := split_text_units(Trim(m_candidates[0].text));
            candidate_units_local := split_text_units(Trim(candidate_value.text));
            if (Length(top_units_local) <> expected_units) or
                (Length(candidate_units_local) <> expected_units) then
            begin
                Exit;
            end;

            for span_len_local := Min(c_max_span_units, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    span_touches_diff_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if not SameText(top_units_local[unit_idx_local],
                            candidate_units_local[unit_idx_local]) then
                        begin
                            span_touches_diff_local := True;
                            Break;
                        end;
                    end;
                    if not span_touches_diff_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    top_span_text_local := join_units_range_local(top_units_local,
                        start_idx_local, span_len_local);
                    candidate_span_text_local := join_units_range_local(
                        candidate_units_local, start_idx_local, span_len_local);
                    if SameText(top_span_text_local, candidate_span_text_local) then
                    begin
                        Continue;
                    end;

                    top_span_weight_local := 0;
                    candidate_span_weight_local := 0;
                    if (query_key_local <> '') and
                        get_display_exact_text_weight(query_key_local,
                        top_span_text_local, span_len_local,
                        top_span_weight_local) and
                        get_display_exact_text_weight(query_key_local,
                        candidate_span_text_local, span_len_local,
                        candidate_span_weight_local) and
                        (top_span_weight_local >= c_min_protected_exact_weight) and
                        (top_span_weight_local >= candidate_span_weight_local +
                        c_protected_exact_margin) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;
    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 6) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_score_local := m_candidates[0].score;
        top_coverage_local :=
            get_display_complete_exact_span_coverage_score(m_candidates[0]);
        best_idx_local := -1;
        best_metric_local := top_coverage_local + top_score_local div 12;

        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_coverage_local :=
                get_display_complete_exact_span_coverage_score(
                m_candidates[idx_local]);
            if candidate_coverage_local < top_coverage_local +
                c_min_coverage_gain then
            begin
                Continue;
            end;
            if candidate_replaces_stronger_same_position_exact_span(
                m_candidates[idx_local]) then
            begin
                Continue;
            end;
            if display_candidate_replaces_protected_conjunction_boundary(
                m_candidates[idx_local]) then
            begin
                Continue;
            end;

            candidate_score_local := m_candidates[idx_local].score;
            if candidate_score_local + c_max_score_deficit < top_score_local then
            begin
                Continue;
            end;

            candidate_metric_local := candidate_coverage_local +
                candidate_score_local div 12;
            if candidate_metric_local > best_metric_local then
            begin
                best_metric_local := candidate_metric_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    function get_diff_exact_span_advantage_against_top(
        const candidate_value: TncCandidate): Integer;
    const
        c_max_span_units = 5;
        c_min_span_weight = 120;
    var
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        span_len_local: Integer;
        start_idx_local: Integer;
        unit_idx_local: Integer;
        query_key_local: string;
        top_span_text_local: string;
        candidate_span_text_local: string;
        top_span_weight_local: Integer;
        candidate_span_weight_local: Integer;
        span_touches_diff_local: Boolean;

        function join_units_range_local(const units_value: TArray<string>;
            const local_start_idx: Integer; const local_span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (local_start_idx < 0) or (local_span_len <= 0) or
                (local_start_idx + local_span_len > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function exact_span_weight_or_zero_local(const query_key_value: string;
            const text_value: string; const span_len_value: Integer): Integer;
        begin
            Result := 0;
            if (query_key_value = '') or (text_value = '') then
            begin
                Exit;
            end;

            if not get_display_exact_text_weight(query_key_value, text_value,
                span_len_value, Result) then
            begin
                Result := 0;
            end;
            if Result < c_min_span_weight then
            begin
                Result := 0;
            end;
        end;

        function top_neighbor_exact_phrase_exists_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            local_query_key: string;
            local_text: string;
        begin
            Result := False;
            if (local_start_idx < 0) or (local_span_len < 2) or
                (local_start_idx + local_span_len > Length(top_units_local)) then
            begin
                Exit;
            end;

            local_query_key := build_display_query_key(local_start_idx,
                local_span_len);
            local_text := join_units_range_local(top_units_local,
                local_start_idx, local_span_len);
            Result := exact_span_weight_or_zero_local(local_query_key,
                local_text, local_span_len) > 0;
        end;

        function top_has_protected_function_boundary_at_diff_local(
            const diff_idx_value: Integer): Boolean;
        var
            local_len: Integer;
        begin
            Result := False;
            if (diff_idx_value < 0) or
                (diff_idx_value >= Length(top_units_local)) or
                (not is_display_function_or_modifier_single(
                top_units_local[diff_idx_value])) then
            begin
                Exit;
            end;

            for local_len := 2 to 4 do
            begin
                if top_neighbor_exact_phrase_exists_local(diff_idx_value + 1,
                    local_len) or
                    top_neighbor_exact_phrase_exists_local(diff_idx_value -
                    local_len, local_len) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_span_crosses_protected_top_boundary_local(
            const local_start_idx: Integer; const local_span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            for local_idx := local_start_idx to
                local_start_idx + local_span_len - 1 do
            begin
                if (not SameText(top_units_local[local_idx],
                    candidate_units_local[local_idx])) and
                    top_has_protected_function_boundary_at_diff_local(
                    local_idx) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        Result := 0;
        if short_exact_query_mode or (expected_units < 6) or
            (Length(m_candidates) < 2) or (Trim(m_candidates[0].comment) <> '') or
            (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        candidate_units_local := split_text_units(Trim(candidate_value.text));
        if (Length(top_units_local) <> expected_units) or
            (Length(candidate_units_local) <> expected_units) then
        begin
            Exit;
        end;

        for span_len_local := Min(c_max_span_units, expected_units) downto 2 do
        begin
            for start_idx_local := 0 to expected_units - span_len_local do
            begin
                span_touches_diff_local := False;
                for unit_idx_local := start_idx_local to
                    start_idx_local + span_len_local - 1 do
                begin
                    if not SameText(top_units_local[unit_idx_local],
                        candidate_units_local[unit_idx_local]) then
                    begin
                        span_touches_diff_local := True;
                        Break;
                    end;
                end;
                if not span_touches_diff_local then
                begin
                    Continue;
                end;

                query_key_local := build_display_query_key(start_idx_local,
                    span_len_local);
                top_span_text_local := join_units_range_local(top_units_local,
                    start_idx_local, span_len_local);
                candidate_span_text_local := join_units_range_local(
                    candidate_units_local, start_idx_local, span_len_local);
                top_span_weight_local := exact_span_weight_or_zero_local(
                    query_key_local, top_span_text_local, span_len_local);
                candidate_span_weight_local := exact_span_weight_or_zero_local(
                    query_key_local, candidate_span_text_local, span_len_local);

                if (candidate_span_weight_local > 0) and
                    (top_span_weight_local = 0) then
                begin
                    if not candidate_span_crosses_protected_top_boundary_local(
                        start_idx_local, span_len_local) then
                    begin
                        Inc(Result, span_len_local * span_len_local * 2200 +
                            Min(10000, candidate_span_weight_local *
                            span_len_local));
                    end;
                end
                else if (candidate_span_weight_local = 0) and
                    (top_span_weight_local > 0) then
                begin
                    Dec(Result, span_len_local * span_len_local * 2200 +
                        Min(10000, top_span_weight_local * span_len_local));
                end
                else if (candidate_span_weight_local > 0) and
                    (top_span_weight_local > 0) and
                    (candidate_span_weight_local >= top_span_weight_local + 120) then
                begin
                    Inc(Result, span_len_local * span_len_local * 1800 +
                        Min(12000, (candidate_span_weight_local -
                        top_span_weight_local) * span_len_local * 6));
                end;
            end;
        end;
    end;

    procedure promote_complete_candidate_with_better_diff_exact_spans;
    const
        c_probe_limit = 6;
        c_min_advantage = 8000;
        c_strong_advantage_override = 30000;
        c_max_score_deficit = 120000;
        c_min_protected_exact_weight = 700;
        c_protected_exact_margin = 80;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_advantage_local: Integer;
        advantage_local: Integer;
        top_score_local: Integer;
        candidate_score_local: Integer;

        function candidate_replaces_stronger_same_position_exact_span(
            const candidate_value: TncCandidate): Boolean;
        const
            c_max_span_units = 5;
        var
            top_units_local: TArray<string>;
            candidate_units_local: TArray<string>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            top_span_text_local: string;
            candidate_span_text_local: string;
            top_span_weight_local: Integer;
            candidate_span_weight_local: Integer;
            span_touches_diff_local: Boolean;

            function join_units_range_local(const units_value: TArray<string>;
                const local_start_idx: Integer; const local_span_len: Integer): string;
            var
                local_idx: Integer;
            begin
                Result := '';
                if (local_start_idx < 0) or (local_span_len <= 0) or
                    (local_start_idx + local_span_len > Length(units_value)) then
                begin
                    Exit;
                end;

                for local_idx := local_start_idx to
                    local_start_idx + local_span_len - 1 do
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;

        begin
            Result := False;
            top_units_local := split_text_units(Trim(m_candidates[0].text));
            candidate_units_local := split_text_units(Trim(candidate_value.text));
            if (Length(top_units_local) <> expected_units) or
                (Length(candidate_units_local) <> expected_units) then
            begin
                Exit;
            end;

            for span_len_local := Min(c_max_span_units, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    span_touches_diff_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if not SameText(top_units_local[unit_idx_local],
                            candidate_units_local[unit_idx_local]) then
                        begin
                            span_touches_diff_local := True;
                            Break;
                        end;
                    end;
                    if not span_touches_diff_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    top_span_text_local := join_units_range_local(top_units_local,
                        start_idx_local, span_len_local);
                    candidate_span_text_local := join_units_range_local(
                        candidate_units_local, start_idx_local, span_len_local);
                    if SameText(top_span_text_local, candidate_span_text_local) then
                    begin
                        Continue;
                    end;

                    top_span_weight_local := 0;
                    candidate_span_weight_local := 0;
                    if (query_key_local <> '') and
                        get_display_exact_text_weight(query_key_local,
                        top_span_text_local, span_len_local,
                        top_span_weight_local) and
                        get_display_exact_text_weight(query_key_local,
                        candidate_span_text_local, span_len_local,
                        candidate_span_weight_local) and
                        (top_span_weight_local >= c_min_protected_exact_weight) and
                        (top_span_weight_local >= candidate_span_weight_local +
                        c_protected_exact_margin) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;
    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 6) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_score_local := m_candidates[0].score;
        best_idx_local := -1;
        best_advantage_local := c_min_advantage - 1;
        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_score_local := m_candidates[idx_local].score;
            if candidate_score_local + c_max_score_deficit < top_score_local then
            begin
                Continue;
            end;

            advantage_local :=
                get_diff_exact_span_advantage_against_top(m_candidates[idx_local]);
            if (advantage_local >= c_min_advantage) and
                (advantage_local < c_strong_advantage_override) and
                candidate_replaces_stronger_same_position_exact_span(
                m_candidates[idx_local]) then
            begin
                Continue;
            end;
            if (advantage_local >= c_min_advantage) and
                display_candidate_replaces_protected_conjunction_boundary(
                m_candidates[idx_local]) then
            begin
                Continue;
            end;
            if advantage_local > best_advantage_local then
            begin
                best_advantage_local := advantage_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_higher_score_complete_path_candidate;
    const
        c_probe_limit = 6;
        c_min_score_margin = 900;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        top_score_local: Integer;
        best_score_local: Integer;
        top_path_local: string;
        candidate_path_local: string;
    begin
        if (Length(m_candidates) < 2) or (expected_units < 3) or
            (expected_units > 4) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(m_candidates[0], 0);
        if get_encoded_path_segment_count_local(top_path_local) <= 1 then
        begin
            Exit;
        end;

        best_idx_local := -1;
        top_score_local := m_candidates[0].score;
        best_score_local := top_score_local;
        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx_local], idx_local);
            if get_encoded_path_segment_count_local(candidate_path_local) <= 1 then
            begin
                Continue;
            end;

            if m_candidates[idx_local].score > best_score_local then
            begin
                best_score_local := m_candidates[idx_local].score;
                best_idx_local := idx_local;
            end;
        end;

        if (best_idx_local > 0) and
            (best_score_local >= top_score_local + c_min_score_margin) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_pathful_complete_over_unbacked_generated_top;
    const
        c_probe_limit = 5;
        c_max_score_deficit = 150000;
        c_min_coverage_gain = 1800;
        c_max_function_unit_diffs = 2;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        top_score_local: Integer;
        top_coverage_local: Integer;
        candidate_coverage_local: Integer;
        top_path_local: string;
        candidate_path_local: string;
        candidate_segments_local: Integer;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        candidate_function_diff_local: Boolean;

        function is_common_function_confusion_pair_local(const left_value: string;
            const right_value: string): Boolean;
        begin
            if SameText(left_value, right_value) then
            begin
                Exit(True);
            end;

            Result :=
                (((left_value = string(Char($7684))) or
                (left_value = string(Char($5730))) or
                (left_value = string(Char($5F97)))) and
                ((right_value = string(Char($7684))) or
                (right_value = string(Char($5730))) or
                (right_value = string(Char($5F97))))) or
                (((left_value = string(Char($4ED6))) or
                (left_value = string(Char($5979))) or
                (left_value = string(Char($5B83)))) and
                ((right_value = string(Char($4ED6))) or
                (right_value = string(Char($5979))) or
                (right_value = string(Char($5B83))))) or
                (((left_value = string(Char($4E00))) or
                (left_value = string(Char($4EE5))) or
                (left_value = string(Char($5DF2)))) and
                ((right_value = string(Char($4E00))) or
                (right_value = string(Char($4EE5))) or
                (right_value = string(Char($5DF2))))) or
                (((left_value = string(Char($5728))) or
                (left_value = string(Char($518D)))) and
                ((right_value = string(Char($5728))) or
                (right_value = string(Char($518D))))) or
                (((left_value = string(Char($662F))) or
                (left_value = string(Char($65F6)))) and
                ((right_value = string(Char($662F))) or
                (right_value = string(Char($65F6)))));
        end;

        function differs_only_in_common_function_units_local(
            const left_units: TArray<string>;
            const right_units: TArray<string>): Boolean;
        var
            unit_idx_local: Integer;
            diff_count_local: Integer;
        begin
            Result := False;
            if (Length(left_units) <> expected_units) or
                (Length(right_units) <> expected_units) then
            begin
                Exit;
            end;

            diff_count_local := 0;
            for unit_idx_local := 0 to expected_units - 1 do
            begin
                if SameText(left_units[unit_idx_local],
                    right_units[unit_idx_local]) then
                begin
                    Continue;
                end;

                if not is_common_function_confusion_pair_local(
                    left_units[unit_idx_local], right_units[unit_idx_local]) then
                begin
                    Exit;
                end;

                Inc(diff_count_local);
                if diff_count_local > c_max_function_unit_diffs then
                begin
                    Exit;
                end;
            end;

            Result := diff_count_local > 0;
        end;
    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 6) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(m_candidates[0], 0);
        if get_encoded_path_segment_count_local(top_path_local) > 1 then
        begin
            Exit;
        end;
        if (normalized_pinyin <> '') and
            display_exact_key_has_text(normalized_pinyin, Trim(m_candidates[0].text)) then
        begin
            Exit;
        end;

        top_score_local := m_candidates[0].score;
        top_coverage_local :=
            get_display_complete_exact_span_coverage_score(m_candidates[0]);
        top_units_local := split_text_units(Trim(m_candidates[0].text));
        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) or
                (m_candidates[idx_local].score + c_max_score_deficit <
                top_score_local) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx_local], idx_local);
            candidate_segments_local :=
                get_encoded_path_segment_count_local(candidate_path_local);
            candidate_units_local := split_text_units(
                Trim(m_candidates[idx_local].text));
            candidate_function_diff_local :=
                differs_only_in_common_function_units_local(top_units_local,
                candidate_units_local);
            if (candidate_segments_local < 3) and
                (not candidate_function_diff_local) then
            begin
                Continue;
            end;

            candidate_coverage_local :=
                get_display_complete_exact_span_coverage_score(
                m_candidates[idx_local]);
            if (candidate_coverage_local < top_coverage_local +
                c_min_coverage_gain) and
                (not candidate_function_diff_local) then
            begin
                Continue;
            end;

            candidate_rank_local := candidate_coverage_local +
                candidate_segments_local * 320 + m_candidates[idx_local].score div 32;
            if candidate_function_diff_local then
            begin
                Inc(candidate_rank_local, c_min_coverage_gain);
            end;
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vunbacked=[idx=%d text=%s]',
                    [best_idx_local, Trim(m_candidates[0].text)]);
            end;
        end;
    end;

    procedure restore_fast_dp_highest_score_complete_candidate;
    const
        c_probe_limit = 6;
        c_min_score_margin = 96;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        top_score_local: Integer;
        best_score_local: Integer;
        candidate_path_local: string;
    begin
        if (Pos('prelightfastdp=1', m_last_lookup_debug_extra) <= 0) or
            (Length(m_candidates) < 2) or (expected_units < 6) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        top_score_local := m_candidates[0].score;
        best_score_local := top_score_local;
        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx_local], idx_local);
            if get_encoded_path_segment_count_local(candidate_path_local) <= 1 then
            begin
                Continue;
            end;

            if m_candidates[idx_local].score > best_score_local then
            begin
                best_score_local := m_candidates[idx_local].score;
                best_idx_local := idx_local;
            end;
        end;

        if (best_idx_local > 0) and
            (best_score_local >= top_score_local + c_min_score_margin) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure restore_final_fast_dp_higher_score_complete_candidate;
    const
        c_probe_limit = 6;
        c_min_score_margin = 96;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        top_score_local: Integer;
        best_score_local: Integer;
        candidate_path_local: string;
    begin
        if short_exact_query_mode or
            (Pos('prelightfastdp=1', m_last_lookup_debug_extra) <= 0) or
            (Length(m_candidates) < 2) or (expected_units < 6) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        top_score_local := m_candidates[0].score;
        best_score_local := top_score_local;
        for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx_local], idx_local);
            if get_encoded_path_segment_count_local(candidate_path_local) <= 1 then
            begin
                Continue;
            end;

            if m_candidates[idx_local].score > best_score_local then
            begin
                best_score_local := m_candidates[idx_local].score;
                best_idx_local := idx_local;
            end;
        end;

        if (best_idx_local > 0) and
            (best_score_local >= top_score_local + c_min_score_margin) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    function display_candidate_crosses_protected_exact_pair(
        const candidate_value: TncCandidate; const candidate_idx: Integer): Boolean;
    const
        c_min_protected_pair_weight = 700;
        c_pair_weight_margin = 400;
    var
        candidate_text_local: string;
        candidate_units_local: TArray<string>;
        path_local: string;
        path_parts_local: TArray<string>;
        part_idx_local: Integer;
        pair_pos_local: Integer;
        syllable_pos_local: Integer;
        part_units_local: Integer;
        next_part_units_local: Integer;
        pair_key_local: string;
        pair_text_local: string;
        pair_weight_local: Integer;
        best_pair_text_local: string;
        best_pair_weight_local: Integer;
        following_text_local: string;
        following_weight_local: Integer;

        function path_has_supported_tail_after_single_local(
            const single_pos_local: Integer;
            const competing_pair_weight_local: Integer): Boolean;
        var
            local_path: string;
            local_parts: TArray<string>;
            local_part_idx: Integer;
            local_syllable_pos: Integer;
            local_part_text: string;
            local_part_units: Integer;
            local_tail_key: string;
            local_tail_weight: Integer;
        begin
            Result := False;
            local_path := get_display_candidate_path_for_index(candidate_value,
                candidate_idx);
            if get_encoded_path_segment_count_local(local_path) <= 1 then
            begin
                Exit;
            end;

            local_parts := local_path.Split([c_segment_path_separator]);
            local_syllable_pos := 0;
            for local_part_idx := 0 to High(local_parts) do
            begin
                local_part_text := Trim(local_parts[local_part_idx]);
                local_part_units := get_candidate_text_unit_count(local_part_text);
                if local_part_units <= 0 then
                begin
                    Exit;
                end;

                if local_syllable_pos = single_pos_local + 1 then
                begin
                    if local_part_units < 2 then
                    begin
                        Exit;
                    end;
                    local_tail_key := build_display_query_key(local_syllable_pos,
                        local_part_units);
                    local_tail_weight := 0;
                    if (local_tail_key <> '') and
                        get_display_exact_text_weight(local_tail_key,
                        local_part_text, local_part_units, local_tail_weight) and
                        (local_tail_weight >= competing_pair_weight_local +
                        c_pair_weight_margin) then
                    begin
                        Exit(True);
                    end;
                    Exit;
                end;

                Inc(local_syllable_pos, local_part_units);
                if local_syllable_pos > single_pos_local + 1 then
                begin
                    Exit;
                end;
            end;
        end;

        function path_starts_segment_at_syllable_local(
            const syllable_pos_value: Integer): Boolean;
        var
            local_path: string;
            local_parts: TArray<string>;
            local_part_idx: Integer;
            local_syllable_pos: Integer;
            local_part_units: Integer;
        begin
            Result := True;
            local_path := get_display_candidate_path_for_index(candidate_value,
                candidate_idx);
            if get_encoded_path_segment_count_local(local_path) <= 1 then
            begin
                Exit;
            end;

            local_parts := local_path.Split([c_segment_path_separator]);
            local_syllable_pos := 0;
            for local_part_idx := 0 to High(local_parts) do
            begin
                if local_syllable_pos = syllable_pos_value then
                begin
                    Exit(True);
                end;
                local_part_units := get_candidate_text_unit_count(
                    Trim(local_parts[local_part_idx]));
                if local_part_units <= 0 then
                begin
                    Exit(True);
                end;
                Inc(local_syllable_pos, local_part_units);
                if local_syllable_pos > syllable_pos_value then
                begin
                    Exit(False);
                end;
            end;
        end;
    begin
        Result := False;
        candidate_text_local := Trim(candidate_value.text);
        if (candidate_text_local = '') or (Trim(candidate_value.comment) <> '') or
            (expected_units < 3) or
            (get_candidate_text_unit_count(candidate_text_local) <> expected_units) then
        begin
            Exit;
        end;

        candidate_units_local := split_text_units(candidate_text_local);
        if Length(candidate_units_local) <> expected_units then
        begin
            Exit;
        end;

        for pair_pos_local := 0 to expected_units - 2 do
        begin
            if not is_display_function_or_modifier_single(
                Trim(candidate_units_local[pair_pos_local])) then
            begin
                Continue;
            end;
            if not path_starts_segment_at_syllable_local(pair_pos_local) then
            begin
                Continue;
            end;
            if not try_get_display_exact_phrase(pair_pos_local, 2,
                best_pair_text_local, best_pair_weight_local) then
            begin
                Continue;
            end;
            if best_pair_weight_local < c_min_protected_pair_weight then
            begin
                Continue;
            end;

            pair_text_local := Trim(candidate_units_local[pair_pos_local]) +
                Trim(candidate_units_local[pair_pos_local + 1]);
            if SameText(pair_text_local, best_pair_text_local) then
            begin
                Continue;
            end;

            pair_weight_local := 0;
            pair_key_local := build_display_query_key(pair_pos_local, 2);
            get_display_exact_text_weight(pair_key_local, pair_text_local, 2,
                pair_weight_local);
            if (pair_weight_local <= 0) or
                (best_pair_weight_local >= pair_weight_local +
                c_pair_weight_margin) then
            begin
                if path_has_supported_tail_after_single_local(pair_pos_local,
                    best_pair_weight_local) then
                begin
                    Continue;
                end;
                Exit(True);
            end;
        end;

        path_local := get_display_candidate_path_for_index(candidate_value,
            candidate_idx);
        if get_encoded_path_segment_count_local(path_local) <= 1 then
        begin
            Exit;
        end;

        path_parts_local := path_local.Split([c_segment_path_separator]);
        syllable_pos_local := 0;
        for part_idx_local := 0 to High(path_parts_local) - 1 do
        begin
            part_units_local := get_candidate_text_unit_count(
                Trim(path_parts_local[part_idx_local]));
            next_part_units_local := get_candidate_text_unit_count(
                Trim(path_parts_local[part_idx_local + 1]));
            if part_units_local <= 0 then
            begin
                Exit;
            end;

            if (part_units_local = 1) and (next_part_units_local >= 2) and
                (syllable_pos_local + 1 < expected_units) and
                try_get_display_exact_phrase(syllable_pos_local, 2,
                best_pair_text_local, best_pair_weight_local) and
                (best_pair_weight_local >= c_min_protected_pair_weight) then
            begin
                pair_text_local := Trim(candidate_units_local[syllable_pos_local]) +
                    Trim(candidate_units_local[syllable_pos_local + 1]);
                if not SameText(pair_text_local, best_pair_text_local) then
                begin
                    following_weight_local := 0;
                    following_text_local := '';
                    try_get_display_exact_phrase(syllable_pos_local + 1,
                        next_part_units_local, following_text_local,
                        following_weight_local);
                    if (following_weight_local <= 0) or
                        (best_pair_weight_local >= following_weight_local +
                        c_pair_weight_margin) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            Inc(syllable_pos_local, part_units_local);
            if syllable_pos_local >= expected_units then
            begin
                Break;
            end;
        end;
    end;

    function display_path_has_one_plus_two_function_head(
        const path_value: string): Boolean;
    var
        parts_local: TArray<string>;
        head_text_local: string;
        tail_text_local: string;
        tail_key_local: string;
        tail_weight_local: Integer;
    begin
        Result := False;
        if get_encoded_path_segment_count_local(path_value) <> 2 then
        begin
            Exit;
        end;

        parts_local := path_value.Split([c_segment_path_separator]);
        if Length(parts_local) <> 2 then
        begin
            Exit;
        end;

        head_text_local := Trim(parts_local[0]);
        tail_text_local := Trim(parts_local[1]);
        Result :=
            (get_candidate_text_unit_count(head_text_local) = 1) and
            is_display_one_plus_two_supported_head_single(head_text_local) and
            (get_candidate_text_unit_count(tail_text_local) = 2);
        if Result and is_display_one_plus_two_pronoun_head_single(
            head_text_local) then
        begin
            tail_key_local := build_display_query_key(1, 2);
            Result := get_display_exact_text_weight(tail_key_local,
                tail_text_local, 2, tail_weight_local) and
                (tail_weight_local >= 500);
        end;
    end;

    function display_path_has_two_plus_one_non_particle_tail(
        const path_value: string): Boolean;
    var
        parts_local: TArray<string>;
        tail_text_local: string;
    begin
        Result := False;
        if get_encoded_path_segment_count_local(path_value) <> 2 then
        begin
            Exit;
        end;

        parts_local := path_value.Split([c_segment_path_separator]);
        if Length(parts_local) <> 2 then
        begin
            Exit;
        end;

        tail_text_local := Trim(parts_local[1]);
        if (get_candidate_text_unit_count(tail_text_local) = 1) then
        begin
            case Ord(tail_text_local[1]) of
                $4E86, $7740, $8FC7, $5417, $561B, $5427, $5462,
                $554A, $5440, $5566, $7684, $5730, $5F97:
                    Exit(False);
            end;
        end;
        Result :=
            (get_candidate_text_unit_count(Trim(parts_local[0])) = 2) and
            (get_candidate_text_unit_count(tail_text_local) = 1);
    end;

    function get_display_fixed_tail_text_outer(const syllable_text: string): string;
    var
        normalized_syllable_value: string;
    begin
        Result := '';
        normalized_syllable_value := normalize_pinyin_text(syllable_text);
        if normalized_syllable_value = 'yi' then
        begin
            Result := string(Char($4E00));
        end
        else if normalized_syllable_value = 'ba' then
        begin
            Result := string(Char($5427));
        end
        else if normalized_syllable_value = 'ma' then
        begin
            Result := string(Char($5417));
        end
        else if normalized_syllable_value = 'le' then
        begin
            Result := string(Char($4E86));
        end
        else if normalized_syllable_value = 'de' then
        begin
            Result := string(Char($7684));
        end
        else if normalized_syllable_value = 'he' then
        begin
            Result := string(Char($548C));
        end
        else if normalized_syllable_value = 'wo' then
        begin
            Result := string(Char($6211));
        end;
    end;

    function get_display_quantity_tail_text_outer(
        const syllable_text: string): string;
    var
        normalized_syllable_value: string;
    begin
        Result := '';
        normalized_syllable_value := normalize_pinyin_text(syllable_text);
        if normalized_syllable_value = 'ci' then
        begin
            Result := string(Char($6B21));
        end
        else if normalized_syllable_value = 'ge' then
        begin
            Result := string(Char($4E2A));
        end
        else if normalized_syllable_value = 'tiao' then
        begin
            Result := string(Char($6761));
        end
        else if normalized_syllable_value = 'zhang' then
        begin
            Result := string(Char($5F20));
        end
        else if normalized_syllable_value = 'ben' then
        begin
            Result := string(Char($672C));
        end
        else if normalized_syllable_value = 'pian' then
        begin
            Result := string(Char($7247));
        end
        else if normalized_syllable_value = 'bu' then
        begin
            Result := string(Char($6B65));
        end
        else if normalized_syllable_value = 'lun' then
        begin
            Result := string(Char($8F6E));
        end
        else if normalized_syllable_value = 'hui' then
        begin
            Result := string(Char($56DE));
        end
        else if normalized_syllable_value = 'xia' then
        begin
            Result := string(Char($4E0B));
        end;
    end;

    function get_display_number_text_outer(const syllable_text: string): string;
    var
        normalized_syllable_value: string;
    begin
        Result := '';
        normalized_syllable_value := normalize_pinyin_text(syllable_text);
        if normalized_syllable_value = 'yi' then
        begin
            Result := string(Char($4E00));
        end
        else if normalized_syllable_value = 'er' then
        begin
            Result := string(Char($4E8C));
        end
        else if normalized_syllable_value = 'liang' then
        begin
            Result := string(Char($4E24));
        end
        else if normalized_syllable_value = 'san' then
        begin
            Result := string(Char($4E09));
        end
        else if normalized_syllable_value = 'si' then
        begin
            Result := string(Char($56DB));
        end
        else if normalized_syllable_value = 'wu' then
        begin
            Result := string(Char($4E94));
        end
        else if normalized_syllable_value = 'liu' then
        begin
            Result := string(Char($516D));
        end
        else if normalized_syllable_value = 'qi' then
        begin
            Result := string(Char($4E03));
        end
        else if normalized_syllable_value = 'ba' then
        begin
            Result := string(Char($516B));
        end
        else if normalized_syllable_value = 'jiu' then
        begin
            Result := string(Char($4E5D));
        end
        else if normalized_syllable_value = 'shi' then
        begin
            Result := string(Char($5341));
        end;
    end;

    function get_display_structured_quantity_tail_text_outer(
        const syllable_text: string): string;
    var
        normalized_syllable_value: string;
    begin
        Result := '';
        normalized_syllable_value := normalize_pinyin_text(syllable_text);
        if normalized_syllable_value = 'zi' then
        begin
            Result := string(Char($5B57));
        end
        else if normalized_syllable_value = 'xiang' then
        begin
            Result := string(Char($9879));
        end
        else if normalized_syllable_value = 'ci' then
        begin
            Result := string(Char($6B21));
        end
        else if normalized_syllable_value = 'ge' then
        begin
            Result := string(Char($4E2A));
        end
        else if normalized_syllable_value = 'tiao' then
        begin
            Result := string(Char($6761));
        end
        else if normalized_syllable_value = 'zhang' then
        begin
            Result := string(Char($5F20));
        end
        else if normalized_syllable_value = 'ben' then
        begin
            Result := string(Char($672C));
        end
        else if normalized_syllable_value = 'pian' then
        begin
            Result := string(Char($7247));
        end
        else if normalized_syllable_value = 'dian' then
        begin
            Result := string(Char($70B9));
        end
        else if normalized_syllable_value = 'bu' then
        begin
            Result := string(Char($6B65));
        end
        else if normalized_syllable_value = 'lun' then
        begin
            Result := string(Char($8F6E));
        end
        else if normalized_syllable_value = 'jie' then
        begin
            Result := string(Char($8282));
        end
        else if normalized_syllable_value = 'ye' then
        begin
            Result := string(Char($9875));
        end
        else if normalized_syllable_value = 'duan' then
        begin
            Result := string(Char($6BB5));
        end
        else if normalized_syllable_value = 'hang' then
        begin
            Result := string(Char($884C));
        end
        else if normalized_syllable_value = 'lie' then
        begin
            Result := string(Char($5217));
        end;
    end;

    function display_candidate_has_exact_suffix_phrase_outer(
        const text_value: string; const min_suffix_units: Integer;
        const max_suffix_units: Integer): Boolean;
    var
        units_local: TArray<string>;
        suffix_units_local: Integer;
        start_idx_local: Integer;
        unit_idx_local: Integer;
        suffix_key_local: string;
        suffix_text_local: string;
    begin
        Result := False;
        units_local := split_text_units(Trim(text_value));
        if (Length(units_local) <> expected_units) or (expected_units < 2) then
        begin
            Exit;
        end;

        for suffix_units_local := Max(2, min_suffix_units) to
            Min(Max(2, max_suffix_units), expected_units - 1) do
        begin
            start_idx_local := expected_units - suffix_units_local;
            suffix_key_local := build_display_query_key(start_idx_local,
                suffix_units_local);
            suffix_text_local := '';
            for unit_idx_local := start_idx_local to expected_units - 1 do
            begin
                suffix_text_local := suffix_text_local +
                    Trim(units_local[unit_idx_local]);
            end;

            if (suffix_key_local <> '') and (suffix_text_local <> '') and
                display_exact_key_has_text(suffix_key_local,
                suffix_text_local) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_candidate_has_nonmatching_exact_suffix_phrase_outer(
        const text_value: string; const min_suffix_units: Integer;
        const max_suffix_units: Integer): Boolean;
    var
        units_local: TArray<string>;
        suffix_units_local: Integer;
        start_idx_local: Integer;
        unit_idx_local: Integer;
        suffix_key_local: string;
        suffix_text_local: string;
    begin
        Result := False;
        units_local := split_text_units(Trim(text_value));
        if (Length(units_local) <> expected_units) or (expected_units < 2) then
        begin
            Exit;
        end;

        for suffix_units_local := Max(2, min_suffix_units) to
            Min(Max(2, max_suffix_units), expected_units) do
        begin
            start_idx_local := expected_units - suffix_units_local;
            suffix_key_local := build_display_query_key(start_idx_local,
                suffix_units_local);
            suffix_text_local := '';
            for unit_idx_local := start_idx_local to expected_units - 1 do
            begin
                suffix_text_local := suffix_text_local +
                    Trim(units_local[unit_idx_local]);
            end;

            if (suffix_key_local <> '') and (suffix_text_local <> '') and
                display_exact_key_has_any_complete_text(suffix_key_local,
                suffix_units_local) and
                (not display_exact_key_has_text(suffix_key_local,
                suffix_text_local)) then
            begin
                Exit(True);
            end;
        end;
    end;

    function display_text_is_primary_for_query_key_outer(const query_key: string;
        const text_value: string; const text_units: Integer): Boolean;
    const
        c_probe_limit = 6;
    var
        ranked_results_local: TncCandidateList;
        ranked_idx_local: Integer;
        ranked_text_local: string;
        cache_key_local: string;
        lookup_cache_key_local: string;
        exact_cache_key_local: string;
        cached_state_local: Integer;
        build_hit_local: Boolean;

        procedure cache_result_local;
        begin
            if (m_lookup_display_feature_cache <> nil) and
                (cache_key_local <> '') then
            begin
                if Result then
                begin
                    cached_state_local := 1;
                end
                else
                begin
                    cached_state_local := 0;
                end;
                m_lookup_display_feature_cache.AddOrSetValue(cache_key_local,
                    cached_state_local);
            end;
        end;
    begin
        Result := True;
        cache_key_local := '';
        if (m_dictionary = nil) or (query_key = '') or (text_value = '') or
            (text_units <= 1) then
        begin
            Exit;
        end;

        cache_key_local := 'primary' + #1 + LowerCase(Trim(query_key)) + #1 +
            Trim(text_value) + #1 + IntToStr(text_units);
        if (m_lookup_display_feature_cache <> nil) and
            m_lookup_display_feature_cache.TryGetValue(cache_key_local,
            cached_state_local) then
        begin
            Exit(cached_state_local > 0);
        end;

        lookup_cache_key_local := '#lookup#' + query_key;
        build_hit_local := (m_build_lookup_cache <> nil) and
            (m_build_lookup_cache.TryGetValue(lookup_cache_key_local,
            ranked_results_local) or
            m_build_lookup_cache.TryGetValue(query_key,
            ranked_results_local));
        // Long-path subspan analysis normally populated the strict exact
        // bucket already. For an exact multi-character segment that bucket
        // has the same primary-word semantics and avoids another broad SQL
        // lookup for every segment in the path.
        exact_cache_key_local := '#exact#' + query_key;
        if (not build_hit_local) and (m_build_lookup_cache <> nil) then
        begin
            build_hit_local := m_build_lookup_cache.TryGetValue(
                exact_cache_key_local, ranked_results_local);
        end;
        if not build_hit_local then
        begin
            if not m_dictionary.lookup(query_key, ranked_results_local) then
            begin
                cache_result_local;
                Exit;
            end;
            if m_build_lookup_cache <> nil then
            begin
                m_build_lookup_cache.AddOrSetValue(lookup_cache_key_local,
                    ranked_results_local);
            end;
        end;
        for ranked_idx_local := 0 to
            Min(High(ranked_results_local), c_probe_limit - 1) do
        begin
            ranked_text_local := Trim(
                ranked_results_local[ranked_idx_local].text);
            if (Trim(ranked_results_local[ranked_idx_local].comment) <> '') or
                (ranked_text_local = '') or
                (get_candidate_text_unit_count(ranked_text_local) <>
                text_units) then
            begin
                Continue;
            end;

            Result := SameText(ranked_text_local, text_value);
            cache_result_local;
            Exit;
        end;
        cache_result_local;
    end;

    function display_path_has_nonprimary_multi_char_segment_outer(
        const path_value: string): Boolean;
    var
        cache_key_local: string;
        cache_value_local: TncLongPathStructureCacheValue;
        path_parts_local: TArray<string>;
        part_idx_local: Integer;
        segment_text_local: string;
        segment_units_local: Integer;
        segment_start_local: Integer;
        segment_key_local: string;

        procedure cache_result_local;
        begin
            if (m_lookup_long_path_structure_cache = nil) or
                (cache_key_local = '') then
            begin
                Exit;
            end;
            if not m_lookup_long_path_structure_cache.TryGetValue(
                cache_key_local, cache_value_local) then
            begin
                FillChar(cache_value_local, SizeOf(cache_value_local), 0);
            end;
            if Result then
            begin
                cache_value_local.nonprimary_multi_char_state := 2;
            end
            else
            begin
                cache_value_local.nonprimary_multi_char_state := 1;
            end;
            m_lookup_long_path_structure_cache.AddOrSetValue(cache_key_local,
                cache_value_local);
        end;
    begin
        Result := False;
        cache_key_local := Trim(path_value);
        if (m_dictionary = nil) or (path_value = '') or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;
        if (m_lookup_long_path_structure_cache <> nil) and
            m_lookup_long_path_structure_cache.TryGetValue(cache_key_local,
            cache_value_local) and
            (cache_value_local.nonprimary_multi_char_state <> 0) then
        begin
            Exit(cache_value_local.nonprimary_multi_char_state = 2);
        end;
        path_parts_local := path_value.Split([c_segment_path_separator]);
        if Length(path_parts_local) <= 1 then
        begin
            cache_result_local;
            Exit;
        end;

        segment_start_local := 0;
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            segment_text_local := Trim(path_parts_local[part_idx_local]);
            segment_units_local := get_candidate_text_unit_count(segment_text_local);
            if (segment_text_local = '') or (segment_units_local <= 0) or
                (segment_start_local + segment_units_local > expected_units) then
            begin
                Exit;
            end;

            if segment_units_local >= 2 then
            begin
                segment_key_local := build_display_query_key(segment_start_local,
                    segment_units_local);
                if not display_text_is_primary_for_query_key_outer(
                    segment_key_local, segment_text_local,
                    segment_units_local) then
                begin
                    Result := True;
                    cache_result_local;
                    Exit;
                end;
            end;

            Inc(segment_start_local, segment_units_local);
        end;
        cache_result_local;
    end;

    function display_path_multi_char_segment_count_outer(
        const path_value: string): Integer;
    var
        path_parts_local: TArray<string>;
        part_idx_local: Integer;
    begin
        Result := 0;
        if Trim(path_value) = '' then
        begin
            Exit;
        end;

        path_parts_local := path_value.Split([c_segment_path_separator]);
        for part_idx_local := 0 to High(path_parts_local) do
        begin
            if get_candidate_text_unit_count(Trim(path_parts_local[part_idx_local])) >= 2 then
            begin
                Inc(Result);
            end;
        end;
    end;

    function get_display_fixed_tail_alignment_score_outer(
        const candidate_value: TncCandidate): Integer;
    var
        cache_key_local: string;
        candidate_units: TArray<string>;
        expected_tail_text: string;

        procedure store_result_local;
        begin
            if (m_lookup_display_feature_cache <> nil) and
                (cache_key_local <> '') then
            begin
                m_lookup_display_feature_cache.AddOrSetValue(cache_key_local,
                    Result);
            end;
        end;
    begin
        Result := 0;
        cache_key_local := '';
        if (expected_units < 3) or (Length(syllables) <> expected_units) or
            (Trim(candidate_value.comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
            expected_units) then
        begin
            Exit;
        end;
        cache_key_local := 'tail' + #1 + normalized_pinyin + #1 +
            Trim(candidate_value.text);
        if (m_lookup_display_feature_cache <> nil) and
            m_lookup_display_feature_cache.TryGetValue(cache_key_local,
            Result) then
        begin
            Exit;
        end;

        expected_tail_text := get_display_fixed_tail_text_outer(
            syllables[expected_units - 1].text);
        if expected_tail_text = '' then
        begin
            store_result_local;
            Exit;
        end;

        candidate_units := split_text_units(Trim(candidate_value.text));
        if Length(candidate_units) <> expected_units then
        begin
            store_result_local;
            Exit;
        end;

        if display_candidate_has_nonmatching_exact_suffix_phrase_outer(
            Trim(candidate_value.text), 2, 4) then
        begin
            Dec(Result, 16000);
            store_result_local;
            Exit;
        end;
        if display_candidate_has_exact_suffix_phrase_outer(Trim(candidate_value.text),
            2, 4) then
        begin
            store_result_local;
            Exit;
        end;

        if SameText(Trim(candidate_units[High(candidate_units)]),
            expected_tail_text) then
        begin
            Inc(Result, 16000);
        end
        else
        begin
            Dec(Result, 16000);
        end;
        store_result_local;
    end;

    function get_display_fixed_quantity_alignment_score_outer(
        const candidate_value: TncCandidate): Integer;
    var
        candidate_units: TArray<string>;
        syllable_idx: Integer;
        expected_tail_text: string;
    begin
        Result := 0;
        if (expected_units < 4) or (Length(syllables) <> expected_units) or
            (Trim(candidate_value.comment) <> '') or
            (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
            expected_units) then
        begin
            Exit;
        end;

        candidate_units := split_text_units(Trim(candidate_value.text));
        if Length(candidate_units) <> expected_units then
        begin
            Exit;
        end;

        for syllable_idx := 0 to expected_units - 2 do
        begin
            if syllable_idx < expected_units - 3 then
            begin
                Continue;
            end;
            if normalize_pinyin_text(syllables[syllable_idx].text) <> 'yi' then
            begin
                Continue;
            end;

            expected_tail_text := get_display_quantity_tail_text_outer(
                syllables[syllable_idx + 1].text);
            if expected_tail_text = '' then
            begin
                Continue;
            end;

            if SameText(Trim(candidate_units[syllable_idx]),
                string(Char($4E00))) and
                SameText(Trim(candidate_units[syllable_idx + 1]),
                expected_tail_text) then
            begin
                Inc(Result, 22000);
            end
            else
            begin
                Dec(Result, 22000);
            end;
        end;
    end;

    procedure insert_fixed_tail_candidate_from_partial;
    var
        tail_key: string;
        tail_text: string;
        idx: Integer;
        duplicate_idx: Integer;
        found_duplicate: Boolean;
        head_text: string;
        head_key: string;
        head_units: Integer;
        repaired_text: string;
        repaired_candidate: TncCandidate;
        repaired_path: string;
        head_path: string;
        head_has_supported_exact_path: Boolean;

        function tail_accepts_supported_compound_head_local(
            const key_value: string): Boolean;
        var
            normalized_key_value: string;
        begin
            normalized_key_value := normalize_pinyin_text(key_value);
            Result := SameText(normalized_key_value, 'de') or
                SameText(normalized_key_value, 'le') or
                SameText(normalized_key_value, 'ba') or
                SameText(normalized_key_value, 'ma');
        end;

        function get_supported_exact_head_path_local(
            const candidate_idx: Integer; const local_head_units: Integer;
            out path_value: string): Boolean;
        var
            path_parts: TArray<string>;
            part_idx: Integer;
            segment_start: Integer;
            segment_text: string;
            segment_units: Integer;
            segment_key: string;
            head_query_key: string;
            evidence_path: string;
            evidence_score: Integer;
            has_supported_path: Boolean;
        begin
            Result := False;
            path_value := '';
            if (candidate_idx < 0) or (candidate_idx > High(m_candidates)) or
                (local_head_units < 2) then
            begin
                Exit;
            end;

            path_value := get_display_candidate_path_for_index(
                m_candidates[candidate_idx], candidate_idx);
            head_query_key := build_display_query_key(0, local_head_units);
            has_supported_path :=
                m_candidates[candidate_idx].display_kind = cdk_lm_compound;

            // Display styling is assigned late and may be absent after an
            // incremental reuse step. Validate supported compositions from
            // the same LM evidence that admitted the path instead of treating
            // the blue display marker as semantic state.
            if (local_head_units = 4) and (head_query_key <> '') and
                get_short_four_two_exact_path_evidence(head_query_key,
                Trim(m_candidates[candidate_idx].text), evidence_path,
                evidence_score) then
            begin
                path_value := evidence_path;
                has_supported_path := True;
            end
            else if (local_head_units = 3) and (head_query_key <> '') and
                is_current_short_three_exact_pair_candidate_supported(
                head_query_key, Trim(m_candidates[candidate_idx].text)) then
            begin
                has_supported_path := True;
            end
            else if (local_head_units = 2) and (head_query_key <> '') and
                is_current_short_two_single_pair_candidate_supported(
                head_query_key, Trim(m_candidates[candidate_idx].text)) then
            begin
                has_supported_path := True;
            end;
            if not has_supported_path then
            begin
                path_value := '';
                Exit;
            end;

            if get_encoded_path_segment_count_local(path_value) <= 1 then
            begin
                path_value := '';
                Exit;
            end;

            path_parts := path_value.Split([c_segment_path_separator]);
            segment_start := 0;
            for part_idx := 0 to High(path_parts) do
            begin
                segment_text := Trim(path_parts[part_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_text = '') or (segment_units <= 0) or
                    (segment_start + segment_units > local_head_units) then
                begin
                    path_value := '';
                    Exit;
                end;

                segment_key := build_display_query_key(segment_start,
                    segment_units);
                if (segment_key = '') or
                    (not (display_exact_key_has_text(segment_key,
                    segment_text) or m_dictionary.is_base_entry(segment_key,
                    segment_text) or m_dictionary.is_user_entry(segment_key,
                    segment_text))) then
                begin
                    path_value := '';
                    Exit;
                end;
                Inc(segment_start, segment_units);
            end;
            Result := segment_start = local_head_units;
            if not Result then
            begin
                path_value := '';
            end;
        end;

        procedure insert_forced_compound_tail_candidate_local;
        var
            candidate_local: TncCandidate;
            head_units_local: TArray<string>;
            left_text_local: string;
            right_text_local: string;
            left_key_local: string;
            right_key_local: string;
            inferred_path_local: string;
            existing_idx_local: Integer;
        begin
            if (expected_units <> 5) or
                (not tail_accepts_supported_compound_head_local(tail_key)) or
                (m_dictionary = nil) or
                (not m_has_forced_visible_top_candidate) or
                (not m_forced_visible_top_from_incremental_lm_prefix) or
                (m_forced_visible_top_candidate.display_kind <>
                cdk_lm_compound) or
                (not SameText(m_forced_visible_top_composition_text,
                m_composition_text)) or
                (not SameText(m_forced_visible_top_lookup_key,
                m_last_lookup_key)) or
                (not SameText(normalize_pinyin_text(Trim(
                m_forced_visible_top_candidate.comment)), tail_key)) or
                (get_candidate_text_unit_count(Trim(
                m_forced_visible_top_candidate.text)) <> 4) then
            begin
                Exit;
            end;

            candidate_local := m_forced_visible_top_candidate;
            head_units_local := split_text_units(Trim(candidate_local.text));
            if Length(head_units_local) <> 4 then
            begin
                Exit;
            end;
            left_text_local := head_units_local[0] + head_units_local[1];
            right_text_local := head_units_local[2] + head_units_local[3];
            left_key_local := build_display_query_key(0, 2);
            right_key_local := build_display_query_key(2, 2);
            if (left_key_local = '') or (right_key_local = '') or
                (not (display_exact_key_has_text(left_key_local,
                left_text_local) or m_dictionary.is_base_entry(left_key_local,
                left_text_local) or m_dictionary.is_user_entry(left_key_local,
                left_text_local))) or
                (not (display_exact_key_has_text(right_key_local,
                right_text_local) or m_dictionary.is_base_entry(right_key_local,
                right_text_local) or m_dictionary.is_user_entry(right_key_local,
                right_text_local))) then
            begin
                Exit;
            end;

            inferred_path_local := left_text_local +
                c_segment_path_separator + right_text_local +
                c_segment_path_separator + tail_text;
            candidate_local.text := Trim(candidate_local.text) + tail_text;
            candidate_local.comment := '';
            candidate_local.source := cs_rule;
            candidate_local.has_dict_weight := False;
            candidate_local.dict_weight := 0;
            candidate_local.display_kind := cdk_default;
            Inc(candidate_local.score, 20000);
            existing_idx_local := find_display_candidate(candidate_local.text,
                '');
            if existing_idx_local >= 0 then
            begin
                move_display_candidate_to_front(existing_idx_local);
                Exit;
            end;

            insert_display_candidate(candidate_local, inferred_path_local, 0);
            existing_idx_local := find_display_candidate(candidate_local.text,
                '');
            if existing_idx_local > 0 then
            begin
                move_display_candidate_to_front(existing_idx_local);
            end;
        end;

        function head_crosses_stronger_exact_pair_local(
            const candidate_idx: Integer; const local_head_text: string;
            const local_head_units: Integer): Boolean;
        const
            c_min_protected_pair_weight = 700;
            c_pair_weight_margin = 400;
        var
            local_units: TArray<string>;
            pair_pos: Integer;
            pair_key: string;
            pair_text: string;
            best_pair_text: string;
            best_pair_weight: Integer;
            current_pair_weight: Integer;

            function has_supported_exact_boundary_local(
                const local_pair_pos: Integer): Boolean;
            var
                local_path: string;
                local_parts: TArray<string>;
                local_part_idx: Integer;
                local_pos: Integer;
                left_text: string;
                right_text: string;
                left_units: Integer;
                right_units: Integer;
                left_key: string;
                right_key: string;
            begin
                Result := False;
                local_path := get_display_candidate_path_for_index(
                    m_candidates[candidate_idx], candidate_idx);
                if get_encoded_path_segment_count_local(local_path) <= 1 then
                begin
                    Exit;
                end;

                local_parts := local_path.Split([c_segment_path_separator]);
                local_pos := 0;
                for local_part_idx := 0 to High(local_parts) - 1 do
                begin
                    left_text := Trim(local_parts[local_part_idx]);
                    right_text := Trim(local_parts[local_part_idx + 1]);
                    left_units := get_candidate_text_unit_count(left_text);
                    right_units := get_candidate_text_unit_count(right_text);
                    if left_units <= 0 then
                    begin
                        Exit;
                    end;

                    if local_pos + left_units - 1 = local_pair_pos then
                    begin
                        // A true exact-word boundary such as 鎺ㄩ€?涓€娆?should not
                        // be overridden by an internal two-syllable alternative
                        // like songyi.  Single ordinary trailing characters do
                        // not provide the same evidence.
                        if (left_units >= 2) and (right_units >= 2) then
                        begin
                            left_key := build_display_query_key(local_pos,
                                left_units);
                            right_key := build_display_query_key(
                                local_pos + left_units, right_units);
                            Result := (left_key <> '') and (right_key <> '') and
                                display_exact_key_has_text(left_key,
                                left_text) and
                                display_exact_key_has_text(right_key,
                                right_text);
                        end;
                        Exit;
                    end;

                    Inc(local_pos, left_units);
                end;
            end;
        begin
            Result := False;
            if local_head_units < 3 then
            begin
                Exit;
            end;

            local_units := split_text_units(local_head_text);
            if Length(local_units) <> local_head_units then
            begin
                Exit;
            end;

            for pair_pos := 0 to local_head_units - 2 do
            begin
                pair_key := build_display_query_key(pair_pos, 2);
                if (pair_key = '') or
                    (not try_get_display_exact_phrase(pair_pos, 2,
                    best_pair_text, best_pair_weight)) or
                    (best_pair_weight < c_min_protected_pair_weight) then
                begin
                    Continue;
                end;

                pair_text := Trim(local_units[pair_pos]) +
                    Trim(local_units[pair_pos + 1]);
                if SameText(pair_text, best_pair_text) then
                begin
                    Continue;
                end;

                current_pair_weight := 0;
                get_display_exact_text_weight(pair_key, pair_text, 2,
                    current_pair_weight);
                if (current_pair_weight > 0) and
                    (best_pair_weight < current_pair_weight +
                    c_pair_weight_margin) then
                begin
                    Continue;
                end;

                if has_supported_exact_boundary_local(pair_pos) then
                begin
                    Continue;
                end;

                Exit(True);
            end;
        end;
    begin
        if (expected_units < 3) or (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables[expected_units - 1].text);
        tail_text := get_display_fixed_tail_text_outer(tail_key);
        if tail_text = '' then
        begin
            Exit;
        end;

        insert_forced_compound_tail_candidate_local;

        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if not SameText(Trim(m_candidates[idx].comment), tail_key) then
            begin
                Continue;
            end;

            head_text := Trim(m_candidates[idx].text);
            head_units := get_candidate_text_unit_count(head_text);
            if head_units <> expected_units - 1 then
            begin
                Continue;
            end;
            head_has_supported_exact_path :=
                tail_accepts_supported_compound_head_local(tail_key) and
                get_supported_exact_head_path_local(idx, head_units,
                head_path);
            head_key := build_display_query_key(0, head_units);
            if (expected_units = 4) and (head_key <> '') and
                (not display_exact_key_has_text(head_key, head_text)) then
            begin
                Continue;
            end;
            if (expected_units = 4) and
                head_crosses_stronger_exact_pair_local(idx, head_text,
                head_units) then
            begin
                Continue;
            end;
            if (head_key <> '') and
                display_exact_key_has_any_complete_text(head_key,
                head_units) and
                (not display_exact_key_has_text(head_key, head_text)) and
                (not head_has_supported_exact_path) then
            begin
                Continue;
            end;

            repaired_text := head_text + tail_text;
            found_duplicate := False;
            for duplicate_idx := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[duplicate_idx].text),
                    repaired_text) and
                    (Trim(m_candidates[duplicate_idx].comment) = '') then
                begin
                    found_duplicate := True;
                    Break;
                end;
            end;

            if found_duplicate then
            begin
                move_display_candidate_to_front(duplicate_idx);
                Exit;
            end;

            repaired_candidate := m_candidates[idx];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            repaired_candidate.has_dict_weight := False;
            repaired_candidate.dict_weight := 0;
            Inc(repaired_candidate.score, 20000);
            if head_has_supported_exact_path then
            begin
                repaired_path := head_path + c_segment_path_separator +
                    tail_text;
            end
            else
            begin
                repaired_path := head_text + c_segment_path_separator +
                    tail_text;
            end;
            insert_display_candidate(repaired_candidate, repaired_path, 0);
            Exit;
        end;
    end;

    procedure insert_fixed_leading_tail_exact_completion_from_partial;
    const
        c_default_probe_limit = 8;
        c_long_probe_limit = 24;
    var
        idx: Integer;
        probe_limit: Integer;
        duplicate_idx: Integer;
        result_idx: Integer;
        head_text: string;
        head_key: string;
        head_units: Integer;
        comment_key: string;
        tail_syllables: TncPinyinParseResult;
        first_tail_key: string;
        first_tail_text: string;
        remaining_tail_key: string;
        remaining_tail_text: string;
        repaired_text: string;
        repaired_path: string;
        repaired_candidate: TncCandidate;
        exact_results: TncCandidateList;
        syllable_idx: Integer;
        expected_remaining_units: Integer;
        found_duplicate: Boolean;
        inferred_path: string;
        inferred_score: Integer;
        inferred_segments: Integer;
        best_repaired_text: string;
        best_repaired_path: string;
        best_repaired_candidate: TncCandidate;
        best_inferred_score: Integer;
        best_inferred_segments: Integer;
        has_best_repair: Boolean;
    begin
        if (expected_units < 4) or (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        probe_limit := c_default_probe_limit;
        if expected_units >= 5 then
        begin
            probe_limit := c_long_probe_limit;
        end;
        has_best_repair := False;
        best_inferred_score := Low(Integer);
        best_inferred_segments := MaxInt;

        for idx := 0 to Min(probe_limit, High(m_candidates)) do
        begin
            head_text := Trim(m_candidates[idx].text);
            comment_key := normalize_pinyin_text(Trim(m_candidates[idx].comment));
            if (head_text = '') or (comment_key = '') then
            begin
                Continue;
            end;

            tail_syllables := get_effective_compact_pinyin_syllables(
                comment_key);
            if (Length(tail_syllables) < 2) or
                (get_candidate_text_unit_count(head_text) +
                Length(tail_syllables) <> expected_units) then
            begin
                Continue;
            end;
            head_units := get_candidate_text_unit_count(head_text);
            head_key := build_display_query_key(0, head_units);
            if (head_key <> '') and
                display_exact_key_has_any_complete_text(head_key,
                head_units) and
                (not display_exact_key_has_text(head_key, head_text)) then
            begin
                Continue;
            end;

            first_tail_key := normalize_pinyin_text(tail_syllables[0].text);
            first_tail_text := get_display_fixed_tail_text_outer(
                first_tail_key);
            if first_tail_text = '' then
            begin
                Continue;
            end;

            remaining_tail_key := '';
            for syllable_idx := 1 to High(tail_syllables) do
            begin
                remaining_tail_key := remaining_tail_key +
                    normalize_pinyin_text(tail_syllables[syllable_idx].text);
            end;
            expected_remaining_units := Length(tail_syllables) - 1;
            if (remaining_tail_key = '') or
                (not lookup_display_exact_cached(remaining_tail_key,
                exact_results)) then
            begin
                Continue;
            end;

            for result_idx := 0 to High(exact_results) do
            begin
                if result_idx >= 8 then
                begin
                    Break;
                end;
                remaining_tail_text := Trim(exact_results[result_idx].text);
                if (Trim(exact_results[result_idx].comment) <> '') or
                    (remaining_tail_text = '') or
                    (get_candidate_text_unit_count(remaining_tail_text) <>
                    expected_remaining_units) then
                begin
                    Continue;
                end;

                repaired_text := head_text + first_tail_text +
                    remaining_tail_text;
                if expected_units >= 5 then
                begin
                    inferred_path :=
                        infer_segment_path_for_query_text_with_score(
                        normalized_pinyin, repaired_text,
                        inferred_score, inferred_segments, False);
                    if inferred_path = '' then
                    begin
                        Continue;
                    end;
                    if (not has_best_repair) or
                        (inferred_score > best_inferred_score) or
                        ((inferred_score = best_inferred_score) and
                        (inferred_segments < best_inferred_segments)) or
                        ((inferred_score = best_inferred_score) and
                        (inferred_segments = best_inferred_segments) and
                        (m_candidates[idx].score >
                        best_repaired_candidate.score)) then
                    begin
                        has_best_repair := True;
                        best_inferred_score := inferred_score;
                        best_inferred_segments := inferred_segments;
                        best_repaired_text := repaired_text;
                        best_repaired_path := inferred_path;
                        best_repaired_candidate := m_candidates[idx];
                    end;
                    Continue;
                end;

                found_duplicate := False;
                for duplicate_idx := 0 to High(m_candidates) do
                begin
                    if SameText(Trim(m_candidates[duplicate_idx].text),
                        repaired_text) and
                        (Trim(m_candidates[duplicate_idx].comment) = '') then
                    begin
                        move_display_candidate_to_index(duplicate_idx, 0);
                        found_duplicate := True;
                        Break;
                    end;
                end;
                if found_duplicate then
                begin
                    Exit;
                end;

                repaired_candidate := m_candidates[idx];
                repaired_candidate.text := repaired_text;
                repaired_candidate.comment := '';
                repaired_candidate.source := cs_rule;
                repaired_candidate.has_dict_weight := False;
                repaired_candidate.dict_weight := 0;
                Inc(repaired_candidate.score, 22000);
                repaired_path := head_text + c_segment_path_separator +
                    first_tail_text + c_segment_path_separator +
                    remaining_tail_text;
                insert_display_candidate(repaired_candidate, repaired_path, 0);
                Exit;
            end;
        end;

        if not has_best_repair then
        begin
            Exit;
        end;
        for duplicate_idx := 0 to High(m_candidates) do
        begin
            if SameText(Trim(m_candidates[duplicate_idx].text),
                best_repaired_text) and
                (Trim(m_candidates[duplicate_idx].comment) = '') then
            begin
                move_display_candidate_to_index(duplicate_idx, 0);
                Exit;
            end;
        end;

        repaired_candidate := best_repaired_candidate;
        repaired_candidate.text := best_repaired_text;
        repaired_candidate.comment := '';
        repaired_candidate.source := cs_rule;
        repaired_candidate.has_dict_weight := False;
        repaired_candidate.dict_weight := 0;
        Inc(repaired_candidate.score, 22000);
        insert_display_candidate(repaired_candidate, best_repaired_path, 0);
    end;

    procedure insert_fixed_quantity_tail_candidate_from_partial;
    var
        tail_key: string;
        tail_text: string;
        quantity_text: string;
        fixed_head_text: string;
        idx: Integer;
        duplicate_idx: Integer;
        found_duplicate: Boolean;
        head_text: string;
        head_units: Integer;
        head_text_units: TArray<string>;
        fixed_head_units: TArray<string>;
        suffix_idx: Integer;
        unit_idx: Integer;
        suffix_matches: Boolean;
        repaired_text: string;
        repaired_candidate: TncCandidate;
        repaired_path: string;
    begin
        if (expected_units < 4) or (Length(syllables) <> expected_units) or
            (normalize_pinyin_text(syllables[expected_units - 3].text) <> 'yi') then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables[expected_units - 1].text);
        tail_text := get_display_fixed_tail_text_outer(tail_key);
        quantity_text := get_display_quantity_tail_text_outer(
            syllables[expected_units - 2].text);
        if (tail_key = '') or (tail_text = '') or (quantity_text = '') then
        begin
            Exit;
        end;

        fixed_head_text := string(Char($4E00)) + quantity_text;
        fixed_head_units := split_text_units(fixed_head_text);
        if Length(fixed_head_units) = 0 then
        begin
            Exit;
        end;

        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if not SameText(normalize_pinyin_text(Trim(m_candidates[idx].comment)),
                tail_key) then
            begin
                Continue;
            end;

            head_text := Trim(m_candidates[idx].text);
            head_units := get_candidate_text_unit_count(head_text);
            if head_units <> expected_units - 1 then
            begin
                Continue;
            end;

            head_text_units := split_text_units(head_text);
            if Length(head_text_units) < Length(fixed_head_units) then
            begin
                Continue;
            end;

            suffix_matches := True;
            for suffix_idx := 0 to High(fixed_head_units) do
            begin
                unit_idx := Length(head_text_units) - Length(fixed_head_units) +
                    suffix_idx;
                if not SameText(Trim(head_text_units[unit_idx]),
                    Trim(fixed_head_units[suffix_idx])) then
                begin
                    suffix_matches := False;
                    Break;
                end;
            end;
            if not suffix_matches then
            begin
                Continue;
            end;

            repaired_text := head_text + tail_text;
            found_duplicate := False;
            for duplicate_idx := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[duplicate_idx].text),
                    repaired_text) and
                    (Trim(m_candidates[duplicate_idx].comment) = '') then
                begin
                    move_display_candidate_to_front(duplicate_idx);
                    found_duplicate := True;
                    Break;
                end;
            end;
            if found_duplicate then
            begin
                Exit;
            end;

            repaired_candidate := m_candidates[idx];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            repaired_candidate.has_dict_weight := False;
            repaired_candidate.dict_weight := 0;
            Inc(repaired_candidate.score, 60000);
            repaired_path := head_text + c_segment_path_separator + tail_text;
            insert_display_candidate(repaired_candidate, repaired_path, 0);
            Exit;
        end;
    end;

    procedure remove_bad_fixed_tail_candidates;
    var
        idx: Integer;
        out_idx: Integer;
        has_good_fixed_tail: Boolean;
        alignment_score: Integer;
        quantity_score: Integer;
    begin
        if (expected_units < 4) or (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        has_good_fixed_tail := False;
        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if (get_display_fixed_tail_alignment_score_outer(m_candidates[idx]) > 0) or
                (get_display_fixed_quantity_alignment_score_outer(
                m_candidates[idx]) > 0) then
            begin
                has_good_fixed_tail := True;
                Break;
            end;
        end;
        if not has_good_fixed_tail then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            alignment_score := get_display_fixed_tail_alignment_score_outer(
                m_candidates[idx]);
            quantity_score := get_display_fixed_quantity_alignment_score_outer(
                m_candidates[idx]);
            if (quantity_score < 0) or
                ((quantity_score <= 0) and (alignment_score < 0) and
                (not display_candidate_is_exact_full_dictionary_entry(
                normalized_pinyin, m_candidates[idx]))) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        SetLength(m_candidates, out_idx);
        if Length(m_candidate_segment_paths) > out_idx then
        begin
            SetLength(m_candidate_segment_paths, out_idx);
        end;
    end;

    function is_all_initial_display_query: Boolean;
    var
        idx: Integer;
        ch: Char;
    begin
        Result := False;
        if Length(normalized_pinyin) < 5 then
        begin
            Exit;
        end;

        for idx := 1 to Length(normalized_pinyin) do
        begin
            ch := normalized_pinyin[idx];
            if Pos(string(ch), 'bpmfdtnlgkhjqxzcsryw') <= 0 then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure remove_complete_candidates_for_all_initial_query;
    var
        idx: Integer;
        out_idx: Integer;
    begin
        if (Length(m_candidates) = 0) or (not is_all_initial_display_query) then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            if Trim(m_candidates[idx].comment) = '' then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        SetLength(m_candidates, out_idx);
        if Length(m_candidate_segment_paths) > out_idx then
        begin
            SetLength(m_candidate_segment_paths, out_idx);
        end;
    end;

    procedure ensure_prefix_lookup_complete_visible;
    const
        c_max_units = 6;
        c_probe_limit = 4;
    var
        prefix_results: TncCandidateList;
        idx: Integer;
        candidate_text: string;
        existing_idx: Integer;
        prefix_candidate: TncCandidate;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (expected_units < 3) or (expected_units > c_max_units) or
            (Length(m_candidates) = 0) or
            (Trim(m_candidates[0].comment) = '') or
            (not m_dictionary.lookup_full_pinyin_prefix(normalized_pinyin,
            prefix_results)) then
        begin
            Exit;
        end;

        for idx := 0 to High(prefix_results) do
        begin
            if idx >= c_probe_limit then
            begin
                Break;
            end;
            if Trim(prefix_results[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text := Trim(prefix_results[idx].text);
            if get_candidate_text_unit_count(candidate_text) <> expected_units then
            begin
                Continue;
            end;

            existing_idx := find_display_candidate(candidate_text, '');
            if existing_idx >= 0 then
            begin
                move_display_candidate_to_front(existing_idx);
                Exit;
            end;

            prefix_candidate := prefix_results[idx];
            prefix_candidate.comment := '';
            prefix_candidate.source := cs_rule;
            Inc(prefix_candidate.score, 12000);
            insert_display_candidate(prefix_candidate, candidate_text, 0);
            Exit;
        end;
    end;

    procedure ensure_short_exact_full_candidates_before_partials;
    var
        exact_results: TncCandidateList;
        exact_candidates: TncCandidateList;
        idx: Integer;
        target_idx: Integer;
        candidate_text: string;
        candidate_units: Integer;
        has_exact_results: Boolean;
        existing_idx: Integer;
        picked: TncCandidate;

        function exact_effective_weight(const item: TncCandidate): Integer;
        begin
            Result := item.score;
            if item.has_dict_weight and (item.dict_weight > Result) then
            begin
                Result := item.dict_weight;
            end;
            if (item.source = cs_user) and
                display_candidate_has_supported_full_entry(normalized_pinyin,
                item) then
            begin
                Inc(Result, c_user_score_bonus);
            end;
            if display_candidate_has_supported_full_entry(normalized_pinyin,
                item) then
            begin
                Inc(Result, 1000000);
            end;
        end;

        function exact_text_already_selected(const text_value: string): Boolean;
        var
            exact_idx: Integer;
        begin
            Result := False;
            for exact_idx := 0 to High(exact_candidates) do
            begin
                if SameText(Trim(exact_candidates[exact_idx].text),
                    text_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure sort_exact_candidates_by_weight;
        var
            left_idx: Integer;
            right_idx: Integer;
            tmp: TncCandidate;
        begin
            for left_idx := 0 to High(exact_candidates) - 1 do
            begin
                for right_idx := left_idx + 1 to High(exact_candidates) do
                begin
                    if exact_effective_weight(exact_candidates[right_idx]) >
                        exact_effective_weight(exact_candidates[left_idx]) then
                    begin
                        tmp := exact_candidates[left_idx];
                        exact_candidates[left_idx] := exact_candidates[right_idx];
                        exact_candidates[right_idx] := tmp;
                    end;
                end;
            end;
        end;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (expected_units < 2) or (expected_units > 4) then
        begin
            Exit;
        end;

        has_exact_results := lookup_display_exact_cached(normalized_pinyin,
            exact_results);
        if not has_exact_results then
        begin
            Exit;
        end;

        SetLength(exact_candidates, 0);
        idx := 0;
        while idx <= High(exact_results) do
        begin
            if idx >= 16 then
            begin
                Break;
            end;
            candidate_text := Trim(exact_results[idx].text);
            candidate_units := get_candidate_text_unit_count(candidate_text);
            if (candidate_text = '') or (candidate_units <= 0) or
                (candidate_units <> expected_units) or
                (Trim(exact_results[idx].comment) <> '') or
                exact_text_already_selected(candidate_text) then
            begin
                Inc(idx);
                Continue;
            end;

            SetLength(exact_candidates, Length(exact_candidates) + 1);
            exact_candidates[High(exact_candidates)] := exact_results[idx];
            exact_candidates[High(exact_candidates)].text := candidate_text;
            exact_candidates[High(exact_candidates)].comment := '';
            Inc(idx);
        end;

        if Length(exact_candidates) = 0 then
        begin
            Exit;
        end;

        sort_exact_candidates_by_weight;
        if Length(exact_candidates) > 4 then
        begin
            SetLength(exact_candidates, 4);
        end;

        for idx := High(exact_candidates) downto 0 do
        begin
            picked := exact_candidates[idx];
            target_idx := 0;
            existing_idx := find_display_candidate(Trim(picked.text), '');
            if existing_idx >= 0 then
            begin
                move_display_candidate_to_index(existing_idx, target_idx);
            end
            else
            begin
                insert_display_candidate(picked, '', target_idx);
            end;
        end;
    end;

    procedure promote_recent_visible_choice_candidate;
    var
        idx: Integer;
        context_idx: Integer;
        context_bonus: Integer;
        best_context_bonus: Integer;
    begin
        if Length(m_candidates) = 0 then
        begin
            Exit;
        end;

        context_idx := -1;
        best_context_bonus := 0;
        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            context_bonus := get_context_query_latest_bonus(
                m_candidates[idx].text);
            if context_bonus > best_context_bonus then
            begin
                best_context_bonus := context_bonus;
                context_idx := idx;
            end;
        end;

        if context_idx >= 0 then
        begin
            move_display_candidate_to_front(context_idx);
            Exit;
        end;

        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx].comment) = '') and
                is_latest_session_query_choice(m_candidates[idx].text) then
            begin
                move_display_candidate_to_front(idx);
                Exit;
            end;
        end;
    end;

    procedure insert_tail_exact_completion_from_partial;
    const
        c_visible_probe_limit = 8;
        c_tail_probe_limit = 8;
    var
        idx: Integer;
        tail_key: string;
        tail_units: Integer;
        tail_results: TncCandidateList;
        tail_idx: Integer;
        head_text: string;
        head_units: Integer;
        tail_text: string;
        best_tail_text: string;
        best_tail_candidate: TncCandidate;
        best_tail_rank: Integer;
        tail_rank: Integer;
        repaired_text: string;
        existing_idx: Integer;
        target_idx: Integer;
        repaired_candidate: TncCandidate;
        repaired_path: string;
        repaired_rank: Integer;
        best_repaired_text: string;
        best_repaired_path: string;
        best_repaired_candidate: TncCandidate;
        best_repaired_rank: Integer;
        best_repaired_existing_idx: Integer;
        best_repaired_target_idx: Integer;

        function tail_key_is_controlled_modal_suffix_local(
            const key_value: string): Boolean;
        var
            normalized_key_value: string;
        begin
            normalized_key_value := normalize_pinyin_text(key_value);
            Result := SameText(normalized_key_value, 'de') or
                SameText(normalized_key_value, 'le') or
                SameText(normalized_key_value, 'la') or
                SameText(normalized_key_value, 'ba') or
                SameText(normalized_key_value, 'ma');
        end;

        function head_prefers_non_numeric_tail_local(
            const head_text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(head_text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(head_text_value[1]) of
                $4E0D, $6CA1, $65E0, $975E, $672A, $522B, $633A,
                $5F88, $592A, $6700, $66F4, $518D, $9AD8:
                    Result := True;
            end;
        end;

        function head_has_supported_segment_path_local(
            const candidate_idx: Integer): Boolean;
        var
            path_local: string;
            parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_pos_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            segment_key_local: string;
        begin
            Result := False;
            path_local := get_display_candidate_path_for_index(
                m_candidates[candidate_idx], candidate_idx);
            if get_encoded_path_segment_count_local(path_local) <= 1 then
            begin
                Exit;
            end;

            parts_local := path_local.Split([c_segment_path_separator]);
            segment_pos_local := 0;
            for part_idx_local := 0 to High(parts_local) do
            begin
                segment_text_local := Trim(parts_local[part_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) or
                    (segment_pos_local + segment_units_local > head_units) then
                begin
                    Exit(False);
                end;

                if segment_units_local >= 2 then
                begin
                    segment_key_local := build_display_query_key(
                        segment_pos_local, segment_units_local);
                    if display_exact_key_has_text(segment_key_local,
                        segment_text_local) then
                    begin
                        Exit(True);
                    end;
                end;
                Inc(segment_pos_local, segment_units_local);
            end;
        end;

        function head_matches_consumed_query_local(
            const candidate_idx: Integer): Boolean;
        var
            head_key_local: string;
        begin
            Result := False;
            if (head_units <= 0) or (head_units > expected_units) then
            begin
                Exit;
            end;

            if head_units = 1 then
            begin
                Exit(True);
            end;

            head_key_local := build_display_query_key(0, head_units);
            if (head_key_local <> '') and
                (m_dictionary.is_base_entry(head_key_local, head_text) or
                m_dictionary.is_user_entry(head_key_local, head_text)) then
            begin
                Exit(True);
            end;

            Result := head_has_supported_segment_path_local(candidate_idx);
        end;

        function is_productive_short_function_head_text_local(
            const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 2 then
            begin
                Exit;
            end;

            if SameText(text_value, string(Char($6709)) + string(Char($70B9))) or
                SameText(text_value, string(Char($4E00)) + string(Char($70B9))) or
                SameText(text_value, string(Char($4E0D)) + string(Char($662F))) or
                SameText(text_value, string(Char($4E0D)) + string(Char($4F1A))) or
                SameText(text_value, string(Char($4E0D)) + string(Char($80FD))) or
                SameText(text_value, string(Char($4E0D)) + string(Char($8981))) or
                SameText(text_value, string(Char($6CA1)) + string(Char($6709))) or
                SameText(text_value, string(Char($53EF)) + string(Char($4EE5))) or
                SameText(text_value, string(Char($5E94)) + string(Char($8BE5))) or
                SameText(text_value, string(Char($9700)) + string(Char($8981))) then
            begin
                Exit(True);
            end;
        end;

    begin
        if (m_dictionary = nil) or (expected_units < 3) or
            (expected_units > 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_repaired_text := '';
        best_repaired_path := '';
        best_repaired_rank := Low(Integer);
        best_repaired_existing_idx := -1;
        best_repaired_target_idx := 0;
        FillChar(best_repaired_candidate, SizeOf(best_repaired_candidate), 0);

        for idx := 0 to Min(c_visible_probe_limit - 1, High(m_candidates)) do
        begin
            tail_key := normalize_pinyin_text(Trim(m_candidates[idx].comment));
            if (tail_key = '') or (not is_full_pinyin_key(tail_key)) then
            begin
                Continue;
            end;

            head_text := Trim(m_candidates[idx].text);
            head_units := get_candidate_text_unit_count(head_text);
            tail_units := get_effective_compact_pinyin_unit_count(tail_key);
            if (tail_units < 2) or (tail_units > 4) or
                (Length(tail_key) > 16) or
                (head_units + tail_units <> expected_units) or
                (not lookup_display_complete_cached(tail_key,
                tail_results)) then
            begin
                Continue;
            end;
            if (head_units = 1) and (tail_units = 2) and
                (not is_display_one_plus_two_direct_complete_head_single(
                head_text)) then
            begin
                Continue;
            end;
            if (head_units >= 2) and
                (not head_matches_consumed_query_local(idx)) then
            begin
                Continue;
            end;

            best_tail_text := '';
            best_tail_rank := Low(Integer);
            FillChar(best_tail_candidate, SizeOf(best_tail_candidate), 0);
            for tail_idx := 0 to High(tail_results) do
            begin
                if tail_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                tail_text := Trim(tail_results[tail_idx].text);
                if (Trim(tail_results[tail_idx].comment) <> '') or
                    (get_candidate_text_unit_count(tail_text) <> tail_units) then
                begin
                    Continue;
                end;

                tail_rank := display_candidate_effective_weight(
                    tail_results[tail_idx]);
                if head_prefers_non_numeric_tail_local(head_text) and
                    is_display_chinese_number_text(tail_text) then
                begin
                    Dec(tail_rank, 1200);
                end;
                if head_prefers_non_numeric_tail_local(head_text) and
                    is_display_repeated_unit_text(tail_text) then
                begin
                    Dec(tail_rank, 700);
                end;
                if (best_tail_text = '') or (tail_rank > best_tail_rank) then
                begin
                    best_tail_text := tail_text;
                    best_tail_candidate := tail_results[tail_idx];
                    best_tail_rank := tail_rank;
                end;
            end;

            if best_tail_text = '' then
            begin
                Continue;
            end;
            if (not tail_key_is_controlled_modal_suffix_local(tail_key)) and
                (not m_dictionary.is_base_entry(tail_key, best_tail_text)) and
                (not m_dictionary.is_user_entry(tail_key, best_tail_text)) then
            begin
                Continue;
            end;

            repaired_text := head_text + best_tail_text;
            if (expected_units = 4) and (head_units = 2) and
                (tail_units = 2) and
                (not display_exact_key_has_text(normalized_pinyin,
                repaired_text)) and
                (not is_productive_short_function_head_text_local(head_text)) then
            begin
                Continue;
            end;
            target_idx := 0;
            if (Length(m_candidates) > 0) and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                expected_units) and
                (not SameText(Trim(m_candidates[0].text),
                repaired_text)) then
            begin
                target_idx := 1;
            end;
            existing_idx := find_display_candidate(repaired_text, '');

            repaired_candidate := m_candidates[idx];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            repaired_candidate.has_dict_weight := best_tail_candidate.has_dict_weight;
            repaired_candidate.dict_weight := best_tail_candidate.dict_weight;
            repaired_candidate.score := Max(m_candidates[idx].score,
                best_tail_candidate.score) + 16000;
            repaired_path := head_text + c_segment_path_separator +
                best_tail_text;
            repaired_rank := display_candidate_effective_weight(
                repaired_candidate) + best_tail_rank;
            if head_units = 1 then
            begin
                Inc(repaired_rank, 6000);
            end
            else if head_has_supported_segment_path_local(idx) then
            begin
                Inc(repaired_rank, 10000);
            end;
            Inc(repaired_rank, get_display_fixed_tail_alignment_score_outer(
                repaired_candidate));
            Inc(repaired_rank, get_display_fixed_quantity_alignment_score_outer(
                repaired_candidate));
            if (best_repaired_text = '') or
                (repaired_rank > best_repaired_rank) then
            begin
                best_repaired_text := repaired_text;
                best_repaired_path := repaired_path;
                best_repaired_candidate := repaired_candidate;
                best_repaired_rank := repaired_rank;
                best_repaired_existing_idx := existing_idx;
                best_repaired_target_idx := target_idx;
            end;
        end;

        if best_repaired_text <> '' then
        begin
            if best_repaired_existing_idx >= 0 then
            begin
                move_display_candidate_to_index(best_repaired_existing_idx,
                    best_repaired_target_idx);
            end
            else
            begin
                insert_display_candidate(best_repaired_candidate,
                    best_repaired_path, best_repaired_target_idx);
            end;
            if Pos('prefdirect=1', m_last_lookup_debug_extra) <= 0 then
            begin
                m_last_lookup_debug_extra :=
                    Trim(m_last_lookup_debug_extra + ' prefdirect=1');
            end;
        end;
    end;

    procedure insert_typo_tail_exact_completion_from_partial;
    const
        c_visible_probe_limit = 8;
        c_tail_probe_limit = 8;
    var
        idx: Integer;
        tail_key: string;
        tail_units: Integer;
        tail_results: TncCandidateList;
        tail_idx: Integer;
        head_text: string;
        head_units: Integer;
        head_path: string;
        tail_text: string;
        best_tail_text: string;
        best_tail_candidate: TncCandidate;
        best_tail_rank: Integer;
        tail_rank: Integer;
        repaired_text: string;
        repaired_candidate: TncCandidate;
        repaired_path: string;
        repaired_rank: Integer;
        best_repaired_text: string;
        best_repaired_path: string;
        best_repaired_candidate: TncCandidate;
        best_repaired_rank: Integer;
        best_repaired_existing_idx: Integer;

        function tail_key_is_controlled_modal_suffix_local(
            const key_value: string): Boolean;
        var
            normalized_key_value: string;
        begin
            normalized_key_value := normalize_pinyin_text(key_value);
            Result := SameText(normalized_key_value, 'de') or
                SameText(normalized_key_value, 'le') or
                SameText(normalized_key_value, 'la') or
                SameText(normalized_key_value, 'ba') or
                SameText(normalized_key_value, 'ma');
        end;

        function head_is_supported_typo_prefix_local(
            const candidate_idx: Integer): Boolean;
        var
            segment_count_local: Integer;
            head_key_local: string;
        begin
            Result := False;
            if (head_units < expected_units - 3) or (head_units < 3) then
            begin
                Exit;
            end;

            head_path := get_display_candidate_path_for_index(
                m_candidates[candidate_idx], candidate_idx);
            segment_count_local := get_encoded_path_segment_count_local(
                head_path);
            if segment_count_local > 1 then
            begin
                Exit(True);
            end;

            if m_candidates[candidate_idx].has_dict_weight and
                (m_candidates[candidate_idx].dict_weight >= 1000) then
            begin
                Exit(True);
            end;

            head_key_local := build_display_query_key(0, head_units);
            Result := (head_key_local <> '') and
                (display_exact_key_has_text(head_key_local, head_text));
        end;

    begin
        if (m_dictionary = nil) or (m_last_lookup_normalized_from = '') or
            (expected_units < 6) or (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_repaired_text := '';
        best_repaired_path := '';
        best_repaired_rank := Low(Integer);
        best_repaired_existing_idx := -1;
        FillChar(best_repaired_candidate, SizeOf(best_repaired_candidate), 0);

        for idx := 0 to Min(c_visible_probe_limit - 1, High(m_candidates)) do
        begin
            tail_key := normalize_pinyin_text(Trim(m_candidates[idx].comment));
            if (tail_key = '') or (not is_full_pinyin_key(tail_key)) then
            begin
                Continue;
            end;

            head_text := Trim(m_candidates[idx].text);
            head_units := get_candidate_text_unit_count(head_text);
            tail_units := get_effective_compact_pinyin_unit_count(tail_key);
            if (head_text = '') or (tail_units < 2) or (tail_units > 3) or
                (Length(tail_key) > 16) or
                (head_units + tail_units <> expected_units) or
                (not head_is_supported_typo_prefix_local(idx)) or
                (not lookup_display_complete_cached(tail_key,
                tail_results)) then
            begin
                Continue;
            end;

            best_tail_text := '';
            best_tail_rank := Low(Integer);
            FillChar(best_tail_candidate, SizeOf(best_tail_candidate), 0);
            for tail_idx := 0 to High(tail_results) do
            begin
                if tail_idx >= c_tail_probe_limit then
                begin
                    Break;
                end;
                tail_text := Trim(tail_results[tail_idx].text);
                if (tail_text = '') or (Trim(tail_results[tail_idx].comment) <> '') or
                    (get_candidate_text_unit_count(tail_text) <> tail_units) then
                begin
                    Continue;
                end;

                tail_rank := display_candidate_effective_weight(
                    tail_results[tail_idx]);
                if display_exact_key_has_text(tail_key, tail_text) then
                begin
                    Inc(tail_rank, 4096);
                end;
                if (best_tail_text = '') or (tail_rank > best_tail_rank) then
                begin
                    best_tail_text := tail_text;
                    best_tail_candidate := tail_results[tail_idx];
                    best_tail_rank := tail_rank;
                end;
            end;

            if best_tail_text = '' then
            begin
                Continue;
            end;
            if (not tail_key_is_controlled_modal_suffix_local(tail_key)) and
                (not m_dictionary.is_base_entry(tail_key, best_tail_text)) and
                (not m_dictionary.is_user_entry(tail_key, best_tail_text)) then
            begin
                Continue;
            end;

            repaired_text := head_text + best_tail_text;
            repaired_candidate := m_candidates[idx];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            repaired_candidate.has_dict_weight := best_tail_candidate.has_dict_weight;
            repaired_candidate.dict_weight := best_tail_candidate.dict_weight;
            repaired_candidate.score := Max(m_candidates[idx].score,
                best_tail_candidate.score) + 16000;
            repaired_path := head_text + c_segment_path_separator +
                best_tail_text;
            repaired_rank := display_candidate_effective_weight(
                repaired_candidate) + best_tail_rank;
            if get_encoded_path_segment_count_local(head_path) > 1 then
            begin
                Inc(repaired_rank, 12000);
            end;
            Inc(repaired_rank, get_display_fixed_tail_alignment_score_outer(
                repaired_candidate));

            if (best_repaired_text = '') or
                (repaired_rank > best_repaired_rank) then
            begin
                best_repaired_text := repaired_text;
                best_repaired_path := repaired_path;
                best_repaired_candidate := repaired_candidate;
                best_repaired_rank := repaired_rank;
                best_repaired_existing_idx := find_display_candidate(
                    repaired_text, '');
            end;
        end;

        if best_repaired_text <> '' then
        begin
            if best_repaired_existing_idx >= 0 then
            begin
                force_move_display_candidate_to_index(
                    best_repaired_existing_idx, 0);
            end
            else
            begin
                insert_display_candidate(best_repaired_candidate,
                    best_repaired_path, 0);
            end;
            if Pos('prefdirect=1', m_last_lookup_debug_extra) <= 0 then
            begin
                m_last_lookup_debug_extra :=
                    Trim(m_last_lookup_debug_extra + ' prefdirect=1');
            end;
        end;
    end;

    procedure insert_tail_split_prefix_partial_from_partial;
    const
        c_visible_probe_limit = 8;
        c_prefix_probe_limit = 8;
    var
        idx: Integer;
        split_units: Integer;
        prefix_idx: Integer;
        tail_key: string;
        tail_syllables: TncPinyinParseResult;
        tail_units: Integer;
        head_text: string;
        head_units: Integer;
        prefix_key: string;
        head_key_for_rank: string;
        remaining_key: string;
        prefix_results: TncCandidateList;
        prefix_text: string;
        repaired_text: string;
        existing_idx: Integer;
        repaired_candidate: TncCandidate;
        repaired_path: string;
        repaired_rank: Integer;
        best_repaired_text: string;
        best_repaired_path: string;
        best_repaired_candidate: TncCandidate;
        best_repaired_rank: Integer;
        best_existing_idx: Integer;
        head_rank: Integer;

        function join_tail_syllables_local(const start_idx: Integer;
            const count: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if ((start_idx < 0) or (count <= 0) or
                (start_idx + count > Length(tail_syllables))) then
            begin
                Exit;
            end;
            for local_idx := start_idx to start_idx + count - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    tail_syllables[local_idx].text);
            end;
        end;

        function lexical_candidate_rank_local(
            const candidate_value: TncCandidate): Integer;
        begin
            if candidate_value.has_dict_weight then
            begin
                Result := candidate_value.dict_weight;
            end
            else
            begin
                Result := candidate_value.score;
            end;

            if candidate_value.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function lookup_exact_text_rank_local(const exact_key: string;
            const exact_text: string; const expected_text_units: Integer;
            const fallback_rank: Integer): Integer;
        var
            exact_results: TncCandidateList;
            exact_idx: Integer;
            candidate_text: string;
            candidate_rank: Integer;
            found_rank: Boolean;
        begin
            Result := fallback_rank;
            found_rank := False;
            if (m_dictionary = nil) or (exact_key = '') or (exact_text = '') or
                (not m_dictionary.lookup_exact_full_pinyin(exact_key,
                exact_results)) then
            begin
                Exit;
            end;

            for exact_idx := 0 to High(exact_results) do
            begin
                candidate_text := Trim(exact_results[exact_idx].text);
                if (candidate_text = '') or
                    (Trim(exact_results[exact_idx].comment) <> '') or
                    (not SameText(candidate_text, exact_text)) or
                    (get_candidate_text_unit_count(candidate_text) <>
                    expected_text_units) then
                begin
                    Continue;
                end;

                candidate_rank := lexical_candidate_rank_local(
                    exact_results[exact_idx]);
                if (not found_rank) or (candidate_rank > Result) then
                begin
                    Result := candidate_rank;
                    found_rank := True;
                end;
            end;
        end;
    begin
        if (m_dictionary = nil) or (expected_units < 3) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_repaired_text := '';
        best_repaired_path := '';
        best_repaired_rank := Low(Integer);
        best_existing_idx := -1;
        FillChar(best_repaired_candidate, SizeOf(best_repaired_candidate), 0);

        for idx := 0 to Min(c_visible_probe_limit - 1, High(m_candidates)) do
        begin
            head_text := Trim(m_candidates[idx].text);
            tail_key := normalize_pinyin_text(Trim(m_candidates[idx].comment));
            if (head_text = '') or (tail_key = '') or (Length(tail_key) > 16) then
            begin
                Continue;
            end;

            tail_syllables := get_effective_compact_pinyin_syllables(tail_key);
            tail_units := Length(tail_syllables);
            head_units := get_candidate_text_unit_count(head_text);
            if (tail_units < 2) or (head_units + tail_units <> expected_units) then
            begin
                Continue;
            end;
            if (head_units = 1) and (tail_units = 2) and
                (not is_display_one_plus_two_direct_complete_head_single(
                head_text)) then
            begin
                Continue;
            end;

            head_key_for_rank := build_display_query_key(0, head_units);
            head_rank := lookup_exact_text_rank_local(head_key_for_rank,
                head_text, head_units,
                lexical_candidate_rank_local(m_candidates[idx]));

            for split_units := tail_units - 1 downto 1 do
            begin
                prefix_key := join_tail_syllables_local(0, split_units);
                remaining_key := join_tail_syllables_local(split_units,
                    tail_units - split_units);
                if (prefix_key = '') or (remaining_key = '') or
                    (not is_full_pinyin_key(prefix_key)) or
                    (not m_dictionary.lookup_exact_full_pinyin(prefix_key,
                    prefix_results)) then
                begin
                    Continue;
                end;

                for prefix_idx := 0 to High(prefix_results) do
                begin
                    if prefix_idx >= c_prefix_probe_limit then
                    begin
                        Break;
                    end;
                    prefix_text := Trim(prefix_results[prefix_idx].text);
                    if (Trim(prefix_results[prefix_idx].comment) <> '') or
                        (get_candidate_text_unit_count(prefix_text) <>
                        split_units) then
                    begin
                        Continue;
                    end;

                    repaired_text := head_text + prefix_text;
                    existing_idx := find_display_candidate(repaired_text,
                        remaining_key);

                    repaired_candidate := m_candidates[idx];
                    repaired_candidate.text := repaired_text;
                    repaired_candidate.comment := remaining_key;
                    repaired_candidate.source := cs_rule;
                    repaired_candidate.has_dict_weight :=
                        prefix_results[prefix_idx].has_dict_weight;
                    repaired_candidate.dict_weight :=
                        prefix_results[prefix_idx].dict_weight;
                    repaired_candidate.score := Max(m_candidates[idx].score,
                        prefix_results[prefix_idx].score) + 8000;
                    repaired_path := head_text + c_segment_path_separator +
                        prefix_text;
                    repaired_rank := head_rank +
                        lexical_candidate_rank_local(prefix_results[prefix_idx]) +
                        (head_units * 256) + (split_units * 512);
                    if (best_repaired_text = '') or
                        (repaired_rank > best_repaired_rank) then
                    begin
                        best_repaired_text := repaired_text;
                        best_repaired_path := repaired_path;
                        best_repaired_candidate := repaired_candidate;
                        best_repaired_rank := repaired_rank;
                        best_existing_idx := existing_idx;
                    end;
                end;
            end;
        end;

        if best_repaired_text = '' then
        begin
            Exit;
        end;

        if best_existing_idx >= 0 then
        begin
            move_display_candidate_to_index(best_existing_idx, 0);
        end
        else
        begin
            insert_display_candidate(best_repaired_candidate,
                best_repaired_path, 0);
        end;
    end;

    procedure insert_tail_prefix_completion_from_partial;
    const
        c_visible_probe_limit = 8;
        c_prefix_probe_limit = 6;
    var
        idx: Integer;
        tail_key: string;
        prefix_results: TncCandidateList;
        prefix_idx: Integer;
        prefix_text: string;
        prefix_units: Integer;
        head_text: string;
        repaired_text: string;
        existing_idx: Integer;
        repaired_candidate: TncCandidate;
        repaired_path: string;
        top_units: TArray<string>;
        prefix_candidate_units: TArray<string>;
    begin
        if (m_dictionary = nil) or (expected_units < 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables[expected_units - 1].text);
        if (tail_key = '') or (Length(tail_key) > 4) or
            (not m_dictionary.lookup_full_pinyin_prefix(tail_key,
            prefix_results)) then
        begin
            if (Length(tail_key) <= 1) or
                (not m_dictionary.lookup_full_pinyin_prefix(
                Copy(tail_key, 1, 1), prefix_results)) then
            begin
                Exit;
            end;
        end;

        head_text := '';
        for idx := 0 to Min(c_visible_probe_limit - 1, High(m_candidates)) do
        begin
            if not SameText(normalize_pinyin_text(Trim(m_candidates[idx].comment)),
                tail_key) then
            begin
                Continue;
            end;
            if get_candidate_text_unit_count(Trim(m_candidates[idx].text)) =
                expected_units - 1 then
            begin
                head_text := Trim(m_candidates[idx].text);
                Break;
            end;
        end;
        if head_text = '' then
        begin
            Exit;
        end;

        top_units := nil;
        if (Length(m_candidates) > 0) and
            (Trim(m_candidates[0].comment) = '') then
        begin
            top_units := split_text_units(Trim(m_candidates[0].text));
        end;

        for prefix_idx := 0 to High(prefix_results) do
        begin
            if prefix_idx >= c_prefix_probe_limit then
            begin
                Break;
            end;
            prefix_text := Trim(prefix_results[prefix_idx].text);
            prefix_units := get_candidate_text_unit_count(prefix_text);
            if (Trim(prefix_results[prefix_idx].comment) <> '') or
                (prefix_units <= 1) or (prefix_units > 3) then
            begin
                Continue;
            end;

            prefix_candidate_units := split_text_units(prefix_text);
            if (Length(top_units) = expected_units) and
                (Length(prefix_candidate_units) > 0) and
                (not SameText(Trim(top_units[High(top_units)]),
                Trim(prefix_candidate_units[0]))) then
            begin
                Continue;
            end;

            repaired_text := head_text + prefix_text;
            existing_idx := find_display_candidate(repaired_text, '');
            if existing_idx >= 0 then
            begin
                move_display_candidate_to_front(existing_idx);
                Exit;
            end;

            repaired_candidate := prefix_results[prefix_idx];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            Inc(repaired_candidate.score, 20000);
            repaired_path := head_text + c_segment_path_separator + prefix_text;
            insert_display_candidate(repaired_candidate, repaired_path, 0);
            Exit;
        end;
    end;

    procedure insert_fixed_tail_partial_from_complete;
    var
        tail_key: string;
        tail_text: string;
        idx: Integer;
        units: TArray<string>;
        head_text: string;
        partial_candidate: TncCandidate;
        existing_idx: Integer;
    begin
        if (expected_units < 4) or (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        tail_key := normalize_pinyin_text(syllables[expected_units - 1].text);
        tail_text := get_display_fixed_tail_text_outer(tail_key);
        if tail_text = '' then
        begin
            Exit;
        end;

        for idx := 0 to Min(5, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            units := split_text_units(Trim(m_candidates[idx].text));
            if (Length(units) <> expected_units) or
                (not SameText(Trim(units[High(units)]), tail_text)) then
            begin
                Continue;
            end;

            head_text := '';
            for existing_idx := 0 to High(units) - 1 do
            begin
                head_text := head_text + units[existing_idx];
            end;
            if head_text = '' then
            begin
                Continue;
            end;

            existing_idx := find_display_candidate(head_text, tail_key);
            if existing_idx >= 0 then
            begin
                Exit;
            end;

            partial_candidate := m_candidates[idx];
            partial_candidate.text := head_text;
            partial_candidate.comment := tail_key;
            Dec(partial_candidate.score, 64);
            insert_display_candidate(partial_candidate, '', Min(idx + 1, 2));
            Exit;
        end;
    end;

    procedure promote_demonstrative_head_partial;
    var
        tail_key: string;
        idx: Integer;
        desired_head: string;
        candidate_text: string;
        candidate_tail_key: string;
        candidate_text_units: Integer;
        candidate_tail_units: Integer;
    begin
        if (expected_units < 2) or (expected_units > 3) or
            (Length(syllables) <> expected_units) or
            (not SameText(normalize_pinyin_text(syllables[0].text), 'zhe')) then
        begin
            Exit;
        end;

        desired_head := string(Char($8FD9));
        tail_key := build_display_query_key(1, expected_units - 1);
        if tail_key = '' then
        begin
            Exit;
        end;

        if expected_units >= 3 then
        begin
            for idx := 0 to Min(8, High(m_candidates)) do
            begin
                candidate_text := Trim(m_candidates[idx].text);
                candidate_tail_key :=
                    normalize_pinyin_text(Trim(m_candidates[idx].comment));
                candidate_text_units :=
                    get_candidate_text_unit_count(candidate_text);
                candidate_tail_units :=
                    get_effective_compact_pinyin_unit_count(candidate_tail_key);
                if (candidate_text_units > 1) and (candidate_tail_units > 0) and
                    (candidate_text_units + candidate_tail_units =
                    expected_units) and
                    (Copy(candidate_text, 1, Length(desired_head)) =
                    desired_head) then
                begin
                    Exit;
                end;
            end;
        end;

        for idx := 0 to Min(8, High(m_candidates)) do
        begin
            if SameText(Trim(m_candidates[idx].text), desired_head) and
                SameText(normalize_pinyin_text(Trim(m_candidates[idx].comment)),
                tail_key) then
            begin
                move_display_candidate_to_front(idx);
                Exit;
            end;
        end;
    end;

    procedure insert_compact_interrogative_location_candidate;
    var
        expected_syllables_local: TArray<string>;
        expected_units_local: TArray<string>;
        idx: Integer;
        lookup_results: TncCandidateList;
        lookup_idx: Integer;
        found_unit: Boolean;
        candidate_value: TncCandidate;
        total_score: Integer;
        existing_idx: Integer;
    begin
        if m_dictionary = nil then
        begin
            Exit;
        end;

        if SameText(normalized_pinyin, 'naer') then
        begin
            expected_syllables_local := TArray<string>.Create('na', 'er');
            expected_units_local := TArray<string>.Create(
                string(Char($54EA)), string(Char($513F)));
        end
        else if SameText(normalized_pinyin, 'ner') then
        begin
            expected_syllables_local := TArray<string>.Create('na', 'er');
            expected_units_local := TArray<string>.Create(
                string(Char($54EA)), string(Char($513F)));
        end
        else if SameText(normalized_pinyin, 'qunaer') then
        begin
            expected_syllables_local := TArray<string>.Create('qu', 'na', 'er');
            expected_units_local := TArray<string>.Create(
                string(Char($53BB)), string(Char($54EA)),
                string(Char($513F)));
        end
        else if SameText(normalized_pinyin, 'qner') then
        begin
            expected_syllables_local := TArray<string>.Create('qu', 'na', 'er');
            expected_units_local := TArray<string>.Create(
                string(Char($53BB)), string(Char($54EA)),
                string(Char($513F)));
        end
        else
        begin
            Exit;
        end;

        total_score := 0;
        candidate_value.text := '';
        for idx := 0 to High(expected_units_local) do
        begin
            if not m_dictionary.lookup_exact_full_pinyin(
                expected_syllables_local[idx], lookup_results) then
            begin
                Exit;
            end;
            found_unit := False;
            for lookup_idx := 0 to High(lookup_results) do
            begin
                if SameText(Trim(lookup_results[lookup_idx].text),
                    expected_units_local[idx]) and
                    (Trim(lookup_results[lookup_idx].comment) = '') then
                begin
                    candidate_value.text := candidate_value.text +
                        expected_units_local[idx];
                    Inc(total_score, lookup_results[lookup_idx].score);
                    found_unit := True;
                    Break;
                end;
            end;
            if not found_unit then
            begin
                Exit;
            end;
        end;

        existing_idx := find_display_candidate(candidate_value.text, '');
        if existing_idx >= 0 then
        begin
            move_display_candidate_to_front(existing_idx);
            Exit;
        end;

        candidate_value.comment := '';
        candidate_value.score := total_score * 2 + 560 +
            Length(expected_units_local) * 40;
        candidate_value.source := cs_rule;
        candidate_value.has_dict_weight := True;
        candidate_value.dict_weight := total_score div
            Max(1, Length(expected_units_local));
        insert_display_candidate(candidate_value, candidate_value.text, 0);
    end;

    procedure insert_two_function_singles_before_phrase_repair;
    const
        c_visible_probe_limit = 6;
        c_strong_pair_protection_weight = 700;
        c_existing_repair_score_margin = 1024;
    var
        source_idx: Integer;
        start_idx: Integer;
        follow_len: Integer;
        units: TArray<string>;
        current_pair_text: string;
        current_pair_best_text: string;
        current_pair_best_weight: Integer;
        first_single_text: string;
        second_single_text: string;
        following_phrase_text: string;
        first_single_weight: Integer;
        second_single_weight: Integer;
        following_phrase_weight: Integer;
        phrase_units: TArray<string>;
        repaired_units: TArray<string>;
        unit_idx: Integer;
        repaired_text: string;
        repaired_path: string;
        existing_idx: Integer;
        repaired_candidate: TncCandidate;
        best_text: string;
        best_path: string;
        best_score: Integer;
        best_target: Integer;
    begin
        if (m_dictionary = nil) or (expected_units < 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_text := '';
        best_path := '';
        best_score := Low(Integer);
        best_target := 0;
        for source_idx := 0 to Min(c_visible_probe_limit - 1,
            High(m_candidates)) do
        begin
            if Trim(m_candidates[source_idx].comment) <> '' then
            begin
                Continue;
            end;

            units := split_text_units(Trim(m_candidates[source_idx].text));
            if Length(units) <> expected_units then
            begin
                Continue;
            end;

            for start_idx := 0 to expected_units - 4 do
            begin
                current_pair_text := units[start_idx] + units[start_idx + 1];
                if not display_exact_key_has_text(
                    build_display_query_key(start_idx, 2),
                    current_pair_text) then
                begin
                    Continue;
                end;
                if try_get_display_exact_phrase(start_idx, 2,
                    current_pair_best_text, current_pair_best_weight) and
                    SameText(Trim(current_pair_best_text),
                    current_pair_text) and
                    (current_pair_best_weight >=
                    c_strong_pair_protection_weight) then
                begin
                    Continue;
                end;
                if not try_get_display_function_single(
                    normalize_pinyin_text(syllables[start_idx].text),
                    first_single_text, first_single_weight) then
                begin
                    Continue;
                end;
                if not try_get_display_function_single(
                    normalize_pinyin_text(syllables[start_idx + 1].text),
                    second_single_text, second_single_weight) then
                begin
                    Continue;
                end;
                if SameText(current_pair_text,
                    first_single_text + second_single_text) then
                begin
                    Continue;
                end;

                for follow_len := 2 to Min(4, expected_units - start_idx - 2) do
                begin
                    if not try_get_display_exact_phrase(start_idx + 2,
                        follow_len, following_phrase_text,
                        following_phrase_weight) then
                    begin
                        Continue;
                    end;

                    phrase_units := split_text_units(following_phrase_text);
                    if Length(phrase_units) <> follow_len then
                    begin
                        Continue;
                    end;

                    for unit_idx := 0 to follow_len - 1 do
                    begin
                        if not SameText(Trim(units[start_idx + 2 + unit_idx]),
                            Trim(phrase_units[unit_idx])) then
                        begin
                            following_phrase_text := '';
                            Break;
                        end;
                    end;
                    if following_phrase_text = '' then
                    begin
                        Continue;
                    end;

                    repaired_units := Copy(units);
                    repaired_units[start_idx] := first_single_text;
                    repaired_units[start_idx + 1] := second_single_text;
                    repaired_text := '';
                    for unit_idx := 0 to High(repaired_units) do
                    begin
                        repaired_text := repaired_text + repaired_units[unit_idx];
                    end;
                    if SameText(repaired_text,
                        Trim(m_candidates[source_idx].text)) then
                    begin
                        Continue;
                    end;

                    existing_idx := find_display_candidate(repaired_text, '');
                    if existing_idx >= 0 then
                    begin
                        if m_candidates[existing_idx].score <
                            m_candidates[source_idx].score +
                            c_existing_repair_score_margin then
                        begin
                            Continue;
                        end;
                        move_display_candidate_to_front(existing_idx);
                        Exit;
                    end;

                    repaired_path := '';
                    for unit_idx := 0 to start_idx - 1 do
                    begin
                        if repaired_path <> '' then
                        begin
                            repaired_path := repaired_path +
                                c_segment_path_separator;
                        end;
                        repaired_path := repaired_path + repaired_units[unit_idx];
                    end;
                    if repaired_path <> '' then
                    begin
                        repaired_path := repaired_path + c_segment_path_separator;
                    end;
                    repaired_path := repaired_path + first_single_text +
                        c_segment_path_separator + second_single_text +
                        c_segment_path_separator + following_phrase_text;
                    for unit_idx := start_idx + 2 + follow_len to
                        High(repaired_units) do
                    begin
                        repaired_path := repaired_path + c_segment_path_separator +
                            repaired_units[unit_idx];
                    end;

                    if m_candidates[source_idx].score + first_single_weight +
                        second_single_weight + following_phrase_weight >
                        best_score then
                    begin
                        best_text := repaired_text;
                        best_path := repaired_path;
                        best_score := m_candidates[source_idx].score +
                            first_single_weight + second_single_weight +
                            following_phrase_weight;
                        best_target := Min(source_idx, 1);
                    end;
                end;
            end;
        end;

        if best_text = '' then
        begin
            Exit;
        end;

        repaired_candidate.text := best_text;
        repaired_candidate.comment := '';
        repaired_candidate.score := best_score + 4096;
        repaired_candidate.source := cs_rule;
        repaired_candidate.has_dict_weight := False;
        repaired_candidate.dict_weight := 0;
        insert_display_candidate(repaired_candidate, best_path, best_target);
    end;

    procedure ensure_missing_apostrophe_display_partial;
    const
        c_max_probe_chars = 8;
    var
        split_pos: Integer;
        left_key: string;
        right_key: string;
        left_results: TncCandidateList;
        full_results: TncCandidateList;
        result_idx: Integer;
        existing_idx: Integer;
        target_idx: Integer;
        insert_idx: Integer;
        move_idx: Integer;
        best_text: string;
        best_score: Integer;
        candidate_text: string;
        candidate_units: TArray<string>;
        partial_candidate: TncCandidate;
        has_full_multi_anchor: Boolean;
        has_boundary_shift: Boolean;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (Pos('''', m_composition_text) > 0) or
            (Length(normalized_pinyin) > c_max_probe_chars) or
            (expected_units > 3) then
        begin
            Exit;
        end;

        has_boundary_shift :=
            detect_relaxed_missing_apostrophe_boundary_shift(normalized_pinyin);
        has_full_multi_anchor := False;
        if m_dictionary.lookup(normalized_pinyin, full_results) then
        begin
            for result_idx := 0 to High(full_results) do
            begin
                if (Trim(full_results[result_idx].comment) = '') and
                    (get_candidate_text_unit_count(Trim(full_results[result_idx].text)) >
                    1) then
                begin
                    has_full_multi_anchor := True;
                    Break;
                end;
            end;
        end;

        if (not has_boundary_shift) and (not has_full_multi_anchor) then
        begin
            Exit;
        end;

        for split_pos := 1 to Length(normalized_pinyin) - 1 do
        begin
            left_key := Copy(normalized_pinyin, 1, split_pos);
            right_key := Copy(normalized_pinyin, split_pos + 1,
                Length(normalized_pinyin) - split_pos);
            if (left_key = '') or (right_key = '') or
                (not is_relaxed_no_initial_syllable_text(right_key)) then
            begin
                Continue;
            end;

            best_text := '';
            best_score := Low(Integer);
            if m_dictionary.lookup(left_key, left_results) then
            begin
                for result_idx := 0 to High(left_results) do
                begin
                    candidate_text := Trim(left_results[result_idx].text);
                    if (Trim(left_results[result_idx].comment) <> '') or
                        (get_candidate_text_unit_count(candidate_text) <= 0) then
                    begin
                        Continue;
                    end;
                    if left_results[result_idx].score > best_score then
                    begin
                        best_text := candidate_text;
                        best_score := left_results[result_idx].score;
                    end;
                end;
            end;

            if (best_text = '') and has_full_multi_anchor then
            begin
                for result_idx := 0 to High(full_results) do
                begin
                    candidate_text := Trim(full_results[result_idx].text);
                    if Trim(full_results[result_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    candidate_units := split_text_units(candidate_text);
                    if Length(candidate_units) <= 1 then
                    begin
                        Continue;
                    end;
                    best_text := candidate_units[0];
                    best_score := full_results[result_idx].score;
                    Break;
                end;
            end;

            if best_text = '' then
            begin
                Continue;
            end;

            existing_idx := -1;
            for result_idx := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[result_idx].text), best_text) and
                    SameText(Trim(m_candidates[result_idx].comment), right_key) then
                begin
                    existing_idx := result_idx;
                    Break;
                end;
            end;

            target_idx := 0;
            for result_idx := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[result_idx].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[result_idx].text)) >
                    1) then
                begin
                    target_idx := result_idx + 1;
                    Break;
                end;
            end;
            target_idx := Min(target_idx, 2);

            if existing_idx >= 0 then
            begin
                if existing_idx <= target_idx then
                begin
                    if get_candidate_text_unit_count(best_text) > 1 then
                    begin
                        Exit;
                    end;
                    Continue;
                end;
                partial_candidate := m_candidates[existing_idx];
                for move_idx := existing_idx downto target_idx + 1 do
                begin
                    m_candidates[move_idx] := m_candidates[move_idx - 1];
                    if move_idx < Length(m_candidate_segment_paths) then
                    begin
                        if move_idx - 1 < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[move_idx] :=
                                m_candidate_segment_paths[move_idx - 1];
                        end
                        else
                        begin
                            m_candidate_segment_paths[move_idx] := '';
                        end;
                    end;
                end;
                m_candidates[target_idx] := partial_candidate;
                if target_idx < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[target_idx] := '';
                end;
                if get_candidate_text_unit_count(best_text) > 1 then
                begin
                    Exit;
                end;
                Continue;
            end;

            partial_candidate.text := best_text;
            partial_candidate.comment := right_key;
            partial_candidate.score := best_score + 64;
            partial_candidate.source := cs_rule;
            partial_candidate.has_dict_weight := False;
            partial_candidate.dict_weight := 0;

            SetLength(m_candidates, Length(m_candidates) + 1);
            if Length(m_candidate_segment_paths) < Length(m_candidates) then
            begin
                SetLength(m_candidate_segment_paths, Length(m_candidates));
            end;
            insert_idx := Min(target_idx, High(m_candidates));
            for move_idx := High(m_candidates) downto insert_idx + 1 do
            begin
                m_candidates[move_idx] := m_candidates[move_idx - 1];
                if move_idx < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx] :=
                        m_candidate_segment_paths[move_idx - 1];
                end;
            end;
            m_candidates[insert_idx] := partial_candidate;
            if insert_idx < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[insert_idx] := '';
            end;
            if get_candidate_text_unit_count(best_text) > 1 then
            begin
                Exit;
            end;
        end;
    end;

    procedure prefer_display_relaxed_no_initial_tail_boundary;
    const
        c_min_base_exact_weight = 160;
    var
        relaxed_syllables: TncPinyinParseResult;
        head_key: string;
        tail_key: string;
        head_units: Integer;
        tail_units: Integer;
        out_idx: Integer;
        best_head_text: string;
        best_head_score: Integer;
        head_results: TncCandidateList;
        head_candidate_text: string;
        partial_idx: Integer;
        complete_idx: Integer;
        partial_candidate: TncCandidate;
        complete_candidate: TncCandidate;

        function build_relaxed_head_key_local: string;
        var
            part_idx: Integer;
        begin
            Result := '';
            for part_idx := 0 to High(relaxed_syllables) - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    Trim(relaxed_syllables[part_idx].text));
            end;
        end;

        function try_set_boundary_keys_local(const head_key_value: string;
            const tail_key_value: string): Boolean;
        var
            normalized_head_key: string;
            normalized_tail_key: string;
            calculated_head_units: Integer;
            calculated_tail_units: Integer;
        begin
            Result := False;
            normalized_head_key := normalize_pinyin_text(head_key_value);
            normalized_tail_key := normalize_pinyin_text(tail_key_value);
            if (normalized_head_key = '') or (normalized_tail_key = '') or
                (not is_relaxed_no_initial_syllable_text(
                normalized_tail_key)) then
            begin
                Exit;
            end;
            calculated_head_units :=
                get_effective_compact_pinyin_unit_count(normalized_head_key);
            calculated_tail_units :=
                get_effective_compact_pinyin_unit_count(normalized_tail_key);
            if (calculated_head_units <= 0) or
                (calculated_tail_units <= 0) or
                (calculated_head_units + calculated_tail_units <>
                expected_units) then
            begin
                Exit;
            end;

            head_key := normalized_head_key;
            tail_key := normalized_tail_key;
            head_units := calculated_head_units;
            tail_units := calculated_tail_units;
            Result := True;
        end;

        function load_explicit_apostrophe_boundary_local: Boolean;
        var
            raw_text_local: string;
            apostrophe_pos_local: Integer;
        begin
            Result := False;
            raw_text_local := Trim(m_composition_text);
            apostrophe_pos_local := LastDelimiter('''', raw_text_local);
            if (apostrophe_pos_local <= 1) or
                (apostrophe_pos_local >= Length(raw_text_local)) then
            begin
                Exit;
            end;

            Result := try_set_boundary_keys_local(
                Copy(raw_text_local, 1, apostrophe_pos_local - 1),
                Copy(raw_text_local, apostrophe_pos_local + 1, MaxInt));
        end;

        function load_relaxed_parts_boundary_local: Boolean;
        begin
            Result := False;
            relaxed_syllables := parse_pinyin_with_relaxed_missing_apostrophe(
                normalized_pinyin);
            if Length(relaxed_syllables) < 2 then
            begin
                Exit;
            end;

            Result := try_set_boundary_keys_local(build_relaxed_head_key_local,
                Trim(relaxed_syllables[High(relaxed_syllables)].text));
        end;

        function normalized_query_has_strong_base_complete_local: Boolean;
        var
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            exact_text_local: string;
        begin
            Result := False;
            if (m_last_lookup_normalized_from <> '') or
                (not lookup_display_exact_cached(normalized_pinyin,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(exact_text_local) <>
                    expected_units) then
                begin
                    Continue;
                end;

                if m_dictionary.is_base_entry(normalized_pinyin,
                    exact_text_local) then
                begin
                    if find_display_candidate(exact_text_local, '') >= 0 then
                    begin
                        Exit(True);
                    end;
                    Continue;
                end;

                if (exact_results_local[exact_idx_local].source = cs_rule) and
                    exact_results_local[exact_idx_local].has_dict_weight and
                    (exact_results_local[exact_idx_local].dict_weight >=
                    c_min_base_exact_weight) then
                begin
                    if find_display_candidate(exact_text_local, '') >= 0 then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function text_matches_head_key_local(const text_value: string): Boolean;
        begin
            Result := False;
            if (head_key = '') or (text_value = '') or
                (get_candidate_text_unit_count(text_value) <> head_units) then
            begin
                Exit;
            end;

            if display_exact_key_has_text(head_key, text_value) then
            begin
                Exit(True);
            end;

            Result := (head_units = 1) and
                m_dictionary.single_char_matches_pinyin(head_key, text_value);
        end;

        function text_matches_tail_key_local(const text_value: string): Boolean;
        begin
            Result := False;
            if (tail_key = '') or (text_value = '') or
                (get_candidate_text_unit_count(text_value) <> tail_units) then
            begin
                Exit;
            end;

            if display_exact_key_has_text(tail_key, text_value) then
            begin
                Exit(True);
            end;

            Result := (tail_units = 1) and
                m_dictionary.single_char_matches_pinyin(tail_key, text_value);
        end;

        function load_longest_supported_boundary_local: Boolean;
        var
            split_pos_local: Integer;
            head_results_local: TncCandidateList;
            head_idx_local: Integer;
        begin
            Result := False;
            for split_pos_local := Length(normalized_pinyin) - 1 downto 2 do
            begin
                if not try_set_boundary_keys_local(
                    Copy(normalized_pinyin, 1, split_pos_local),
                    Copy(normalized_pinyin, split_pos_local + 1, MaxInt)) then
                begin
                    Continue;
                end;

                if not lookup_display_complete_cached(head_key,
                    head_results_local) then
                begin
                    Continue;
                end;

                for head_idx_local := 0 to High(head_results_local) do
                begin
                    if (Trim(head_results_local[head_idx_local].comment) = '') and
                        text_matches_head_key_local(
                        Trim(head_results_local[head_idx_local].text)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function complete_matches_relaxed_boundary_local(
            const text_value: string): Boolean;
        var
            units_local: TArray<string>;
            unit_idx_local: Integer;
            head_text_local: string;
            tail_text_local: string;
        begin
            Result := False;
            units_local := split_text_units(Trim(text_value));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;

            head_text_local := '';
            for unit_idx_local := 0 to head_units - 1 do
            begin
                head_text_local := head_text_local + units_local[unit_idx_local];
            end;

            tail_text_local := '';
            for unit_idx_local := head_units to High(units_local) do
            begin
                tail_text_local := tail_text_local + units_local[unit_idx_local];
            end;

            Result := text_matches_head_key_local(head_text_local) and
                text_matches_tail_key_local(tail_text_local);
        end;

        function find_relaxed_boundary_partial_local: Integer;
        var
            idx_local: Integer;
        begin
            Result := -1;
            for idx_local := 0 to High(m_candidates) do
            begin
                if SameText(normalize_pinyin_text(Trim(m_candidates[idx_local].comment)),
                    tail_key) and
                    text_matches_head_key_local(Trim(m_candidates[idx_local].text)) then
                begin
                    Exit(idx_local);
                end;
            end;
        end;

        function find_relaxed_boundary_complete_local: Integer;
        var
            idx_local: Integer;
        begin
            Result := -1;
            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    complete_matches_relaxed_boundary_local(
                    Trim(m_candidates[idx_local].text)) then
                begin
                    Exit(idx_local);
                end;
            end;
        end;

        procedure remove_misaligned_complete_candidates_local;
        var
            idx_local: Integer;
        begin
            out_idx := 0;
            for idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) =
                    expected_units) and
                    (not complete_matches_relaxed_boundary_local(
                    Trim(m_candidates[idx_local].text))) and
                    (not display_candidate_has_base_full_entry(normalized_pinyin,
                    m_candidates[idx_local])) then
                begin
                    Continue;
                end;

                if out_idx <> idx_local then
                begin
                    m_candidates[out_idx] := m_candidates[idx_local];
                    if out_idx < Length(m_candidate_segment_paths) then
                    begin
                        if idx_local < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[out_idx] :=
                                m_candidate_segment_paths[idx_local];
                        end
                        else
                        begin
                            m_candidate_segment_paths[out_idx] := '';
                        end;
                    end;
                end;
                Inc(out_idx);
            end;

            if out_idx <> Length(m_candidates) then
            begin
                SetLength(m_candidates, out_idx);
                if Length(m_candidate_segment_paths) > out_idx then
                begin
                    SetLength(m_candidate_segment_paths, out_idx);
                end;
            end;
        end;

        function try_build_head_partial_candidate_local: Boolean;
        var
            head_result_idx_local: Integer;
        begin
            Result := False;
            best_head_text := '';
            best_head_score := Low(Integer);
            if not lookup_display_complete_cached(head_key, head_results) then
            begin
                Exit;
            end;

            for head_result_idx_local := 0 to High(head_results) do
            begin
                head_candidate_text := Trim(head_results[head_result_idx_local].text);
                if (Trim(head_results[head_result_idx_local].comment) <> '') or
                    (not text_matches_head_key_local(head_candidate_text)) then
                begin
                    Continue;
                end;

                if head_results[head_result_idx_local].score > best_head_score then
                begin
                    best_head_text := head_candidate_text;
                    best_head_score := head_results[head_result_idx_local].score;
                end;
            end;

            if best_head_text = '' then
            begin
                Exit;
            end;

            partial_candidate.text := best_head_text;
            partial_candidate.comment := tail_key;
            partial_candidate.score := best_head_score + 256;
            partial_candidate.source := cs_rule;
            partial_candidate.has_dict_weight := False;
            partial_candidate.dict_weight := 0;
            Result := True;
        end;

        function try_build_relaxed_boundary_complete_candidate_local(
            out out_candidate: TncCandidate): Boolean;
        var
            explicit_boundary_key: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            candidate_text_local: string;
            candidate_score_local: Integer;
            best_idx_local: Integer;
            best_score_local: Integer;
        begin
            Result := False;
            best_idx_local := -1;
            best_score_local := Low(Integer);
            explicit_boundary_key := head_key + '''' + tail_key;
            if (head_key = '') or (tail_key = '') or
                (not lookup_display_complete_cached(explicit_boundary_key,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if Trim(exact_results_local[exact_idx_local].comment) <> '' then
                begin
                    Continue;
                end;

                candidate_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) or
                    (not complete_matches_relaxed_boundary_local(
                    candidate_text_local)) then
                begin
                    Continue;
                end;

                candidate_score_local := exact_results_local[exact_idx_local].score;
                if exact_results_local[exact_idx_local].has_dict_weight then
                begin
                    Inc(candidate_score_local,
                        exact_results_local[exact_idx_local].dict_weight);
                end;

                if (best_idx_local < 0) or
                    (candidate_score_local > best_score_local) then
                begin
                    best_idx_local := exact_idx_local;
                    best_score_local := candidate_score_local;
                end;
            end;

            if best_idx_local < 0 then
            begin
                Exit;
            end;

            out_candidate := exact_results_local[best_idx_local];
            out_candidate.comment := '';
            Result := True;
        end;
    begin
        if (m_dictionary = nil) or (Length(m_candidates) = 0) or
            (normalized_pinyin = '') or (expected_units < 2) then
        begin
            Exit;
        end;

        if relaxed_boundary_cache_ready then
        begin
            if not relaxed_boundary_cache_found then
            begin
                Exit;
            end;
            head_key := relaxed_boundary_cache_head_key;
            tail_key := relaxed_boundary_cache_tail_key;
            head_units := relaxed_boundary_cache_head_units;
            tail_units := relaxed_boundary_cache_tail_units;
        end
        else
        begin
            relaxed_boundary_cache_ready := True;
            relaxed_boundary_cache_found :=
                load_explicit_apostrophe_boundary_local or
                load_longest_supported_boundary_local or
                load_relaxed_parts_boundary_local;
            if not relaxed_boundary_cache_found then
            begin
                Exit;
            end;
            relaxed_boundary_cache_head_key := head_key;
            relaxed_boundary_cache_tail_key := tail_key;
            relaxed_boundary_cache_head_units := head_units;
            relaxed_boundary_cache_tail_units := tail_units;
        end;

        if normalized_query_has_strong_base_complete_local then
        begin
            Exit;
        end;

        partial_idx := find_relaxed_boundary_partial_local;
        if (partial_idx < 0) and try_build_head_partial_candidate_local then
        begin
            insert_display_candidate(partial_candidate, '', 0);
            partial_idx := find_relaxed_boundary_partial_local;
        end;
        if partial_idx < 0 then
        begin
            Exit;
        end;

        remove_misaligned_complete_candidates_local;

        complete_idx := find_relaxed_boundary_complete_local;
        if complete_idx >= 0 then
        begin
            force_move_display_candidate_to_index(complete_idx, 0);
            Exit;
        end;

        if try_build_relaxed_boundary_complete_candidate_local(complete_candidate) then
        begin
            complete_idx := find_display_candidate(Trim(complete_candidate.text), '');
            if complete_idx >= 0 then
            begin
                force_move_display_candidate_to_index(complete_idx, 0);
            end
            else
            begin
                insert_display_candidate(complete_candidate, '', 0);
            end;
            Exit;
        end;

        partial_idx := find_relaxed_boundary_partial_local;
        if partial_idx >= 0 then
        begin
            force_move_display_candidate_to_index(partial_idx, 0);
        end;
    end;

    procedure insert_function_phrase_split_repairs;
    const
        c_source_probe_limit = 3;
        c_min_phrase_weight = 700;
    var
        source_idx: Integer;
        start_idx: Integer;
        follow_len: Integer;
        units: TArray<string>;
        repaired_units: TArray<string>;
        phrase_units: TArray<string>;
        unit_idx: Integer;
        single_text: string;
        single_weight: Integer;
        phrase_text: string;
        phrase_weight: Integer;
        current_two_text: string;
        repair_text: string;
        repair_path: string;
        duplicate_idx: Integer;
        duplicate_found: Boolean;
        repair_candidate: TncCandidate;
        best_text: string;
        best_path: string;
        best_score: Integer;
        best_target: Integer;
        current_two_best_text: string;
        current_two_weight: Integer;
    begin
        if (m_dictionary = nil) or (Length(m_candidates) <= 0) or
            (expected_units < 3) then
        begin
            Exit;
        end;

        best_text := '';
        best_path := '';
        best_score := Low(Integer);
        best_target := 0;
        for source_idx := 0 to Min(c_source_probe_limit - 1,
            High(m_candidates)) do
        begin
            if Trim(m_candidates[source_idx].comment) <> '' then
            begin
                Continue;
            end;

            units := split_text_units(Trim(m_candidates[source_idx].text));
            if Length(units) <> expected_units then
            begin
                Continue;
            end;

            for start_idx := 0 to expected_units - 3 do
            begin
                if not try_get_display_function_single(
                    normalize_pinyin_text(syllables[start_idx].text),
                    single_text, single_weight) then
                begin
                    Continue;
                end;

                for follow_len := 2 to Min(4, expected_units - start_idx - 1) do
                begin
                    current_two_text := units[start_idx] + units[start_idx + 1];
                    if (not try_get_display_exact_phrase(start_idx, 2,
                        current_two_best_text, current_two_weight)) or
                        (not SameText(current_two_best_text,
                        current_two_text)) then
                    begin
                        Continue;
                    end;
                    if not try_get_display_exact_phrase(start_idx + 1,
                        follow_len, phrase_text, phrase_weight) then
                    begin
                        Continue;
                    end;
                    if phrase_weight < c_min_phrase_weight then
                    begin
                        Continue;
                    end;
                    if current_two_weight >= phrase_weight + 400 then
                    begin
                        Continue;
                    end;

                    phrase_units := split_text_units(phrase_text);
                    if Length(phrase_units) <> follow_len then
                    begin
                        Continue;
                    end;

                    repaired_units := Copy(units);
                    repaired_units[start_idx] := single_text;
                    for unit_idx := 0 to High(phrase_units) do
                    begin
                        repaired_units[start_idx + 1 + unit_idx] :=
                            phrase_units[unit_idx];
                    end;

                    repair_text := '';
                    for unit_idx := 0 to High(repaired_units) do
                    begin
                        repair_text := repair_text + repaired_units[unit_idx];
                    end;

                    if SameText(repair_text, Trim(m_candidates[source_idx].text)) then
                    begin
                        Continue;
                    end;

                    duplicate_found := False;
                    for duplicate_idx := 0 to High(m_candidates) do
                    begin
                        if SameText(Trim(m_candidates[duplicate_idx].text),
                            repair_text) and
                            (Trim(m_candidates[duplicate_idx].comment) = '') then
                        begin
                            duplicate_found := True;
                            Break;
                        end;
                    end;
                    if duplicate_found then
                    begin
                        Continue;
                    end;

                    repair_path := '';
                    for unit_idx := 0 to start_idx - 1 do
                    begin
                        if repair_path <> '' then
                        begin
                            repair_path := repair_path + c_segment_path_separator;
                        end;
                        repair_path := repair_path + repaired_units[unit_idx];
                    end;
                    if repair_path <> '' then
                    begin
                        repair_path := repair_path + c_segment_path_separator;
                    end;
                    repair_path := repair_path + single_text +
                        c_segment_path_separator + phrase_text;
                    for unit_idx := start_idx + 1 + Length(phrase_units) to
                        High(repaired_units) do
                    begin
                        repair_path := repair_path + c_segment_path_separator +
                            repaired_units[unit_idx];
                    end;

                    if m_candidates[source_idx].score + single_weight +
                        phrase_weight > best_score then
                    begin
                        best_score := m_candidates[source_idx].score +
                            single_weight + phrase_weight;
                        best_text := repair_text;
                        best_path := repair_path;
                        best_target := Min(source_idx, 1);
                    end;
                end;
            end;
        end;

        if best_text = '' then
        begin
            Exit;
        end;

        repair_candidate.text := best_text;
        repair_candidate.comment := '';
        repair_candidate.score := best_score + 4096;
        repair_candidate.source := cs_rule;
        repair_candidate.has_dict_weight := False;
        repair_candidate.dict_weight := 0;
        insert_display_candidate(repair_candidate, best_path, best_target);
    end;

    procedure promote_pathful_high_score_complete_candidate;
    const
        c_visible_probe_limit = 6;
        c_quality_margin = 1800;
        c_score_margin = 1024;
        c_score_quality_floor = -9600;
    var
        idx: Integer;
        local_idx: Integer;
        probe_limit: Integer;
        best_idx: Integer;
        best_quality: Integer;
        best_rank: Integer;
        top_quality: Integer;
        candidate_quality: Integer;
        candidate_rank: Integer;
        top_exact_match: Boolean;
        score_order_complete_path_candidate: Boolean;
        top_effective_dict_weight: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
        top_path: string;
        candidate_path: string;

        function is_display_function_single(const text_value: string): Boolean;
        begin
            Result := False;
            if get_candidate_text_unit_count(text_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(text_value[1]) of
                $662F, $7684, $4E86, $5728, $4E8E, $4E5F, $548C, $4E0D,
                $518D, $9AD8, $5F88, $592A, $6700, $66F4, $6CA1, $8981,
                $4F1A, $80FD, $628A, $88AB, $8BA9, $7ED9, $5BF9, $4ECE,
                $5411, $4E0E, $90FD, $8FD8, $624D, $5C31, $53EA, $522B,
                $5DF2, $5C06, $53EF, $60F3, $65E0, $975E, $672A, $4ECD,
                $591A, $5C11, $5168, $8D85, $771F, $597D, $5982, $633A,
                $8F83, $6781, $800C, $5E76, $4E00, $4E14, $5427,
                $5417:
                    Result := True;
            end;
        end;

        function get_display_quantity_tail_text(
            const syllable_text: string): string;
        var
            normalized_syllable_value: string;
        begin
            Result := '';
            normalized_syllable_value := normalize_pinyin_text(syllable_text);
            if normalized_syllable_value = 'ci' then
            begin
                Result := string(Char($6B21));
            end
            else if normalized_syllable_value = 'ge' then
            begin
                Result := string(Char($4E2A));
            end
            else if normalized_syllable_value = 'tiao' then
            begin
                Result := string(Char($6761));
            end
            else if normalized_syllable_value = 'zhang' then
            begin
                Result := string(Char($5F20));
            end
            else if normalized_syllable_value = 'ben' then
            begin
                Result := string(Char($672C));
            end
            else if normalized_syllable_value = 'pian' then
            begin
                Result := string(Char($7247));
            end
            else if normalized_syllable_value = 'bu' then
            begin
                Result := string(Char($6B65));
            end
            else if normalized_syllable_value = 'lun' then
            begin
                Result := string(Char($8F6E));
            end
            else if normalized_syllable_value = 'hui' then
            begin
                Result := string(Char($56DE));
            end
            else if normalized_syllable_value = 'xia' then
            begin
                Result := string(Char($4E0B));
            end;
        end;

        function get_display_fixed_quantity_alignment_score(
            const candidate_value: TncCandidate): Integer;
        var
            candidate_units: TArray<string>;
            syllable_idx: Integer;
            expected_tail_text: string;
        begin
            Result := 0;
            if (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
                expected_units) then
            begin
                Exit;
            end;

            candidate_units := split_text_units(Trim(candidate_value.text));
            if Length(candidate_units) <> expected_units then
            begin
                Exit;
            end;

            for syllable_idx := 0 to expected_units - 2 do
            begin
                if syllable_idx < expected_units - 3 then
                begin
                    Continue;
                end;
                if normalize_pinyin_text(syllables[syllable_idx].text) <> 'yi' then
                begin
                    Continue;
                end;

                expected_tail_text := get_display_quantity_tail_text(
                    syllables[syllable_idx + 1].text);
                if expected_tail_text = '' then
                begin
                    Continue;
                end;

                if SameText(Trim(candidate_units[syllable_idx]),
                    string(Char($4E00))) and
                    SameText(Trim(candidate_units[syllable_idx + 1]),
                    expected_tail_text) then
                begin
                    Inc(Result, 22000);
                end
                else
                begin
                    Dec(Result, 22000);
                end;
            end;
        end;

        function get_display_path_quality(const candidate: TncCandidate;
            const path_value: string): Integer;
        var
            candidate_units: TArray<string>;
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            segment_start: Integer;
            query_key: string;
            previous_was_single: Boolean;
            boundary_start: Integer;
            span_units: Integer;
            span_start: Integer;
            span_start_min: Integer;
            span_start_max: Integer;
            span_text: string;
            span_key: string;
            function build_candidate_span_text(const start_idx: Integer;
                const unit_count: Integer): string;
            var
                unit_idx: Integer;
            begin
                Result := '';
                if (start_idx < 0) or (unit_count <= 0) or
                    (start_idx + unit_count > Length(candidate_units)) then
                begin
                    Exit;
                end;
                for unit_idx := start_idx to start_idx + unit_count - 1 do
                begin
                    Result := Result + candidate_units[unit_idx];
                end;
            end;
        begin
            Result := Low(Integer) div 4;
            if (Trim(candidate.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate.text)) <>
                expected_units) or
                (get_encoded_path_segment_count_local(path_value) <= 1) then
            begin
                Exit;
            end;

            Result := 0;
            candidate_units := split_text_units(Trim(candidate.text));
            if Length(candidate_units) <> expected_units then
            begin
                Exit(Low(Integer) div 4);
            end;
            Inc(Result, get_display_fixed_tail_alignment_score_outer(candidate));
            Inc(Result, get_display_fixed_quantity_alignment_score(candidate));
            parts := path_value.Split([c_segment_path_separator]);
            segment_start := 0;
            previous_was_single := False;
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_text = '') or (segment_units <= 0) or
                    (segment_start + segment_units > expected_units) then
                begin
                    Exit(Low(Integer) div 4);
                end;

                query_key := build_display_query_key(segment_start,
                    segment_units);
                boundary_start := segment_start;
                if boundary_start > 0 then
                begin
                    for span_units := 2 to 4 do
                    begin
                        span_start_min := Max(0, boundary_start - span_units + 1);
                        span_start_max := Min(boundary_start - 1,
                            expected_units - span_units);
                        for span_start := span_start_min to span_start_max do
                        begin
                            span_text := build_candidate_span_text(span_start,
                                span_units);
                            span_key := build_display_query_key(span_start,
                                span_units);
                            if (span_text <> '') and (span_key <> '') and
                                (not display_exact_key_has_text(span_key,
                                span_text)) and
                                display_exact_key_has_any_complete_text(span_key,
                                span_units) then
                            begin
                                Dec(Result, 4200 + span_units * 1200);
                            end
                            else if (span_units >= 3) and
                                display_exact_key_has_stronger_complete_text(
                                span_key, span_text, span_units, 5000) then
                            begin
                                Dec(Result, 9000 + span_units * 2600);
                            end
                            else if (span_units = 2) and
                                is_display_chinese_number_text(span_text) and
                                display_exact_key_has_stronger_complete_text(
                                span_key, span_text, span_units, 200) then
                            begin
                                Dec(Result, 9000);
                            end;
                        end;
                    end;
                end;
                if segment_units = 1 then
                begin
                    if is_display_function_single(segment_text) then
                    begin
                        Inc(Result, 240);
                    end
                    else
                    begin
                        Dec(Result, 2600);
                    end;
                    if previous_was_single then
                    begin
                        Dec(Result, 2200);
                    end;
                    previous_was_single := True;
                end
                else
                begin
                    Inc(Result, 900 + segment_units * 360);
                    if segment_units >= 4 then
                    begin
                        Inc(Result, 5000);
                    end;
                    if display_exact_key_has_text(query_key, segment_text) then
                    begin
                        Inc(Result, 2800 + segment_units * 520);
                    end
                    else if display_exact_key_has_any_complete_text(query_key,
                        segment_units) then
                    begin
                        Dec(Result, 4200 + segment_units * 700);
                    end
                    else
                    begin
                        Dec(Result, 900);
                    end;
                    previous_was_single := False;
                end;
                Inc(segment_start, segment_units);
            end;

            if segment_start <> expected_units then
            begin
                Exit(Low(Integer) div 4);
            end;
        end;

        function top_is_full_query_exact_dictionary_match: Boolean;
        var
            top_text: string;
        begin
            Result := False;
            if (Length(m_candidates) = 0) or (expected_units < 2) or
                (normalized_pinyin = '') or
                (Trim(m_candidates[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
                expected_units) then
            begin
                Exit;
            end;

            top_text := Trim(m_candidates[0].text);
            Result := display_exact_key_has_text(normalized_pinyin, top_text);
        end;

        function candidate_is_full_query_exact_dictionary_match(
            const candidate_value: TncCandidate): Boolean;
        var
            candidate_text: string;
        begin
            Result := False;
            if (expected_units < 2) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
                expected_units) then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate_value.text);
            Result := display_exact_key_has_text(normalized_pinyin,
                candidate_text);
        end;
    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 4) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_quality := Low(Integer);
        best_rank := Low(Integer);
        top_exact_match := top_is_full_query_exact_dictionary_match;
        top_quality := Low(Integer);
        top_path := get_display_candidate_path_for_index(m_candidates[0], 0);
        if top_path <> '' then
        begin
            top_quality := get_display_path_quality(m_candidates[0], top_path);
        end;
        probe_limit := Min(c_visible_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if get_encoded_path_segment_count_local(candidate_path) <= 1 then
            begin
                Continue;
            end;

            candidate_quality := get_display_path_quality(m_candidates[idx],
                candidate_path);
            candidate_rank := candidate_quality + Max(-24000, Min(24000,
                m_candidates[idx].score - m_candidates[0].score));
            if candidate_rank > best_rank then
            begin
                best_rank := candidate_rank;
                best_quality := candidate_quality;
                best_idx := idx;
            end;
        end;

        if m_candidates[0].has_dict_weight then
        begin
            top_effective_dict_weight := m_candidates[0].dict_weight;
        end
        else
        begin
            top_effective_dict_weight := m_candidates[0].score;
        end;
        score_order_complete_path_candidate := (expected_units >= 7) and
            (best_idx > 0) and m_candidates[best_idx].has_dict_weight and
            (best_quality >= top_quality - 1800) and
            (m_candidates[best_idx].dict_weight >
            top_effective_dict_weight + 128);

        if (best_idx <= 0) or
            (Trim(m_candidates[0].comment) <> '') or
            ((expected_units <= 4) and top_exact_match and
            candidate_is_full_query_exact_dictionary_match(
            m_candidates[best_idx]) and
            (m_candidates[best_idx].score < m_candidates[0].score)) or
            ((top_quality <= Low(Integer) div 8) and
            (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            expected_units) and
            (m_candidates[best_idx].score <
            m_candidates[0].score + c_score_margin)) or
            (top_exact_match and (not
            candidate_is_full_query_exact_dictionary_match(
            m_candidates[best_idx]))) or
            (top_exact_match and
            (m_candidates[best_idx].score <
            m_candidates[0].score + c_score_margin * 4) and
            (best_quality < top_quality + c_quality_margin)) or
            ((not score_order_complete_path_candidate) and
            (best_quality < top_quality + c_quality_margin) and
            ((m_candidates[best_idx].score <
            m_candidates[0].score + c_score_margin) or
            (best_quality < top_quality + c_score_quality_floor))) then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[best_idx];
        moved_path := '';
        if best_idx < Length(m_candidate_segment_paths) then
        begin
            moved_path := m_candidate_segment_paths[best_idx];
        end;
        if moved_path = '' then
        begin
            moved_path := get_display_candidate_path_for_index(moved_candidate,
                best_idx);
        end;
        for local_idx := best_idx downto 1 do
        begin
            m_candidates[local_idx] := m_candidates[local_idx - 1];
            if local_idx < Length(m_candidate_segment_paths) then
            begin
                if (local_idx - 1) < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[local_idx] :=
                        m_candidate_segment_paths[local_idx - 1];
                end
                else
                begin
                    m_candidate_segment_paths[local_idx] := '';
                end;
            end;
        end;
        m_candidates[0] := moved_candidate;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := moved_path;
        end;
    end;

    procedure promote_lower_cross_boundary_exact_penalty_candidate;
    const
        c_visible_probe_limit = 6;
        c_min_penalty_delta = 6000;
        c_max_score_lag = 20000;
    var
        idx: Integer;
        local_idx: Integer;
        probe_limit: Integer;
        top_penalty: Integer;
        candidate_penalty: Integer;
        candidate_rank: Integer;
        top_weight_score: Integer;
        best_idx: Integer;
        best_rank: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;

        function build_candidate_span_text_local(
            const candidate_units: TArray<string>; const start_idx: Integer;
            const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (unit_count <= 0) or
                (start_idx + unit_count > Length(candidate_units)) then
            begin
                Exit;
            end;

            for unit_idx := start_idx to start_idx + unit_count - 1 do
            begin
                Result := Result + candidate_units[unit_idx];
            end;
        end;

        function get_cross_boundary_exact_penalty(
            const candidate: TncCandidate; const path_value: string): Integer;
        var
            candidate_units: TArray<string>;
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            segment_start: Integer;
            boundary_start: Integer;
            span_units: Integer;
            span_start: Integer;
            span_start_min: Integer;
            span_start_max: Integer;
            span_text: string;
            span_key: string;
        begin
            Result := MaxInt div 4;
            if (Trim(candidate.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate.text)) <>
                expected_units) or
                (get_encoded_path_segment_count_local(path_value) <= 1) then
            begin
                Exit;
            end;

            candidate_units := split_text_units(Trim(candidate.text));
            if Length(candidate_units) <> expected_units then
            begin
                Exit;
            end;

            Result := 0;
            parts := path_value.Split([c_segment_path_separator]);
            segment_start := 0;
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_text = '') or (segment_units <= 0) or
                    (segment_start + segment_units > expected_units) then
                begin
                    Exit(MaxInt div 4);
                end;

                boundary_start := segment_start;
                if boundary_start > 0 then
                begin
                    for span_units := 2 to 4 do
                    begin
                        span_start_min := Max(0, boundary_start - span_units + 1);
                        span_start_max := Min(boundary_start - 1,
                            expected_units - span_units);
                        for span_start := span_start_min to span_start_max do
                        begin
                            span_text := build_candidate_span_text_local(
                                candidate_units, span_start, span_units);
                            span_key := build_display_query_key(span_start,
                                span_units);
                            if (span_text = '') or (span_key = '') then
                            begin
                                Continue;
                            end;
                            if span_units >= 3 then
                            begin
                                if (not display_exact_key_has_text(span_key,
                                    span_text)) and
                                    display_exact_key_has_any_complete_text(
                                    span_key, span_units) then
                                begin
                                    Inc(Result, 9000 + span_units * 2600);
                                end
                                else if
                                    display_exact_key_has_stronger_complete_text(
                                    span_key, span_text, span_units, 5000) then
                                begin
                                    Inc(Result, 9000 + span_units * 2600);
                                end;
                            end
                            else if is_display_chinese_number_text(span_text) and
                                display_exact_key_has_stronger_complete_text(
                                span_key, span_text, span_units, 200) then
                            begin
                                Inc(Result, 9000);
                            end;
                        end;
                    end;
                end;

                Inc(segment_start, segment_units);
            end;

            if segment_start <> expected_units then
            begin
                Result := MaxInt div 4;
            end;
        end;

        function get_cross_boundary_exact_weight_score(
            const candidate: TncCandidate; const path_value: string): Integer;
        var
            candidate_units: TArray<string>;
            parts: TArray<string>;
            part_idx: Integer;
            segment_text: string;
            segment_units: Integer;
            segment_start: Integer;
            boundary_start: Integer;
            span_units: Integer;
            span_start: Integer;
            span_start_min: Integer;
            span_start_max: Integer;
            span_text: string;
            span_key: string;
            exact_weight: Integer;
        begin
            Result := Low(Integer) div 4;
            if (Trim(candidate.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate.text)) <>
                expected_units) or
                (get_encoded_path_segment_count_local(path_value) <= 1) then
            begin
                Exit;
            end;

            candidate_units := split_text_units(Trim(candidate.text));
            if Length(candidate_units) <> expected_units then
            begin
                Exit;
            end;

            Result := 0;
            parts := path_value.Split([c_segment_path_separator]);
            segment_start := 0;
            for part_idx := 0 to High(parts) do
            begin
                segment_text := Trim(parts[part_idx]);
                segment_units := get_candidate_text_unit_count(segment_text);
                if (segment_text = '') or (segment_units <= 0) or
                    (segment_start + segment_units > expected_units) then
                begin
                    Exit(Low(Integer) div 4);
                end;

                boundary_start := segment_start;
                if boundary_start > 0 then
                begin
                    for span_units := 2 to 4 do
                    begin
                        span_start_min := Max(0, boundary_start - span_units + 1);
                        span_start_max := Min(boundary_start - 1,
                            expected_units - span_units);
                        for span_start := span_start_min to span_start_max do
                        begin
                            span_text := build_candidate_span_text_local(
                                candidate_units, span_start, span_units);
                            span_key := build_display_query_key(span_start,
                                span_units);
                            if (span_text = '') or (span_key = '') then
                            begin
                                Continue;
                            end;

                            if get_display_exact_text_weight(span_key, span_text,
                                span_units, exact_weight) then
                            begin
                                Inc(Result, exact_weight div 8);
                            end
                            else if (span_units >= 3) and
                                display_exact_key_has_any_complete_text(
                                span_key, span_units) then
                            begin
                                Dec(Result, 6000 + span_units * 1800);
                            end;
                        end;
                    end;
                end;

                Inc(segment_start, segment_units);
            end;

            if segment_start <> expected_units then
            begin
                Result := Low(Integer) div 4;
            end;
        end;
    begin
        if (Length(m_candidates) < 2) or (expected_units < 4) or
            (Length(m_candidate_segment_paths) = 0) then
        begin
            Exit;
        end;
        top_penalty := get_cross_boundary_exact_penalty(m_candidates[0],
            m_candidate_segment_paths[0]);
        if (top_penalty <= c_min_penalty_delta) or
            (top_penalty >= MaxInt div 8) then
        begin
            Exit;
        end;

        best_idx := -1;
        top_weight_score := get_cross_boundary_exact_weight_score(m_candidates[0],
            m_candidate_segment_paths[0]);
        best_rank := Low(Integer);
        probe_limit := Min(c_visible_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if idx >= Length(m_candidate_segment_paths) then
            begin
                Continue;
            end;

            candidate_penalty := get_cross_boundary_exact_penalty(
                m_candidates[idx], m_candidate_segment_paths[idx]);
            if (candidate_penalty >= top_penalty - c_min_penalty_delta) or
                (m_candidates[idx].score < m_candidates[0].score -
                c_max_score_lag) then
            begin
                Continue;
            end;

            candidate_rank := get_cross_boundary_exact_weight_score(
                m_candidates[idx], m_candidate_segment_paths[idx]) -
                top_weight_score + (top_penalty - candidate_penalty) * 4 +
                Max(-c_max_score_lag, Min(c_max_score_lag,
                m_candidates[idx].score - m_candidates[0].score));
            if (candidate_rank > c_min_penalty_delta) and
                (candidate_rank > best_rank) then
            begin
                best_rank := candidate_rank;
                best_idx := idx;
            end;
        end;

        if best_idx <= 0 then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[best_idx];
        moved_path := '';
        if best_idx < Length(m_candidate_segment_paths) then
        begin
            moved_path := m_candidate_segment_paths[best_idx];
        end;
        for local_idx := best_idx downto 1 do
        begin
            m_candidates[local_idx] := m_candidates[local_idx - 1];
            if local_idx < Length(m_candidate_segment_paths) then
            begin
                if local_idx - 1 < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[local_idx] :=
                        m_candidate_segment_paths[local_idx - 1];
                end
                else
                begin
                    m_candidate_segment_paths[local_idx] := '';
                end;
            end;
        end;
        m_candidates[0] := moved_candidate;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := moved_path;
        end;
    end;

    procedure promote_higher_scored_pathful_dict_complete_candidate;
    const
        c_visible_probe_limit = 6;
        c_min_score_delta = 128;
    var
        idx: Integer;
        local_idx: Integer;
        probe_limit: Integer;
        best_idx: Integer;
        best_score: Integer;
        top_weight: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
    begin
        if (Length(m_candidates) < 2) or (expected_units < 7) or
            (Length(m_candidate_segment_paths) = 0) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;
        best_idx := -1;
        if m_candidates[0].has_dict_weight then
        begin
            top_weight := m_candidates[0].dict_weight;
        end
        else
        begin
            top_weight := m_candidates[0].score;
        end;
        best_score := top_weight + c_min_score_delta;
        probe_limit := Min(c_visible_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if (idx >= Length(m_candidate_segment_paths)) or
                (not m_candidates[idx].has_dict_weight) or
                (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) or
                (get_encoded_path_segment_count_local(
                m_candidate_segment_paths[idx]) <= 1) then
            begin
                Continue;
            end;

            if m_candidates[idx].dict_weight > best_score then
            begin
                best_score := m_candidates[idx].dict_weight;
                best_idx := idx;
            end;
        end;

        if best_idx <= 0 then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[best_idx];
        moved_path := m_candidate_segment_paths[best_idx];
        for local_idx := best_idx downto 1 do
        begin
            m_candidates[local_idx] := m_candidates[local_idx - 1];
            if local_idx < Length(m_candidate_segment_paths) then
            begin
                if local_idx - 1 < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[local_idx] :=
                        m_candidate_segment_paths[local_idx - 1];
                end
                else
                begin
                    m_candidate_segment_paths[local_idx] := '';
                end;
            end;
        end;
        m_candidates[0] := moved_candidate;
        m_candidate_segment_paths[0] := moved_path;
    end;

    function is_ascii_letter(const value: Char): Boolean;
    begin
        Result := (value >= 'a') and (value <= 'z') or (value >= 'A') and (value <= 'Z');
    end;
    function split_trailing_ascii_candidate(const candidate_text: string; out head: string; out tail: string): Boolean;
    var
        idx: Integer;
        ch: Char;
        tail_len: Integer;
    begin
        Result := False;
        head := '';
        tail := '';
        if candidate_text = '' then
        begin
            Exit;
        end;

        idx := Length(candidate_text);
        while idx > 0 do
        begin
            ch := candidate_text[idx];
            if not is_ascii_letter(ch) then
            begin
                Break;
            end;
            Dec(idx);
        end;

        if idx = Length(candidate_text) then
        begin
            Exit;
        end;

        head := Copy(candidate_text, 1, idx);
        tail := Copy(candidate_text, idx + 1, Length(candidate_text) - idx);
        if (head = '') or (tail = '') then
        begin
            Exit;
        end;

        tail_len := Length(tail);
        if (tail_len <= 0) or (tail_len > Length(normalized_pinyin)) then
        begin
            Exit;
        end;

        if not SameText(Copy(normalized_pinyin, Length(normalized_pinyin) - tail_len + 1, tail_len), tail) then
        begin
            Exit;
        end;

        Result := True;
    end;

    function is_invisible_display_candidate(
        const candidate_value: TncCandidate): Boolean;
    var
        comment_key: string;
        idx: Integer;
        ch: Char;
        has_letter: Boolean;
    begin
        Result := False;
        if Trim(candidate_value.text) <> '' then
        begin
            Exit;
        end;

        comment_key := Trim(candidate_value.comment);
        if comment_key = '' then
        begin
            Result := True;
            Exit;
        end;

        has_letter := False;
        for idx := 1 to Length(comment_key) do
        begin
            ch := comment_key[idx];
            if ((ch >= 'a') and (ch <= 'z')) or
                ((ch >= 'A') and (ch <= 'Z')) then
            begin
                has_letter := True;
                Continue;
            end;
            if (ch = ' ') or (ch = #9) or (ch = '''') then
            begin
                Continue;
            end;
            Result := True;
            Exit;
        end;
        if not has_letter then
        begin
            Result := True;
            Exit;
        end;

        comment_key := normalize_pinyin_text(comment_key);
        comment_key := StringReplace(comment_key, ' ', '', [rfReplaceAll]);
        comment_key := StringReplace(comment_key, #9, '', [rfReplaceAll]);
        comment_key := StringReplace(comment_key, '''', '', [rfReplaceAll]);
        Result := (comment_key = '') or is_full_pinyin_key(comment_key);
    end;

    procedure remove_invisible_display_candidates;
    var
        in_idx: Integer;
        out_idx: Integer;
    begin
        if Length(m_candidates) = 0 then
        begin
            Exit;
        end;

        out_idx := 0;
        for in_idx := 0 to High(m_candidates) do
        begin
            if is_invisible_display_candidate(m_candidates[in_idx]) then
            begin
                Continue;
            end;

            if out_idx <> in_idx then
            begin
                m_candidates[out_idx] := m_candidates[in_idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if in_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[in_idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        SetLength(m_candidates, out_idx);
        if Length(m_candidate_segment_paths) > out_idx then
        begin
            SetLength(m_candidate_segment_paths, out_idx);
        end;
    end;

    procedure apply_single_syllable_fuzzy_exact_ranking;
    var
        original_candidates: TncCandidateList;
        original_paths: TArray<string>;
        seen_candidates: TDictionary<string, Boolean>;
        original_idx: Integer;
        candidate_idx_local: Integer;
        append_idx: Integer;
        relaxed_expected_units: Integer;
        candidate_key: string;
    begin
        original_candidates := Copy(m_candidates, 0, Length(m_candidates));
        original_paths := Copy(m_candidate_segment_paths, 0,
            Length(m_candidate_segment_paths));
        relaxed_expected_units := expected_units;

        { A valid raw single syllable can also have a relaxed missing-apostrophe
          split (for example le -> l/e). Rank fuzzy exacts against the raw
          syllable first, then restore those supplemental partial candidates. }
        expected_units := 1;
        try
            short_exact_query_mode :=
                apply_short_query_exact_display_ranking;
        finally
            expected_units := relaxed_expected_units;
        end;
        if not short_exact_query_mode then
        begin
            Exit;
        end;

        seen_candidates := TDictionary<string, Boolean>.Create;
        try
            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                candidate_key := build_candidate_identity_key(
                    m_candidates[candidate_idx_local].text,
                    m_candidates[candidate_idx_local].comment);
                if candidate_key <> '' then
                begin
                    seen_candidates.AddOrSetValue(candidate_key, True);
                end;
            end;

            for original_idx := 0 to High(original_candidates) do
            begin
                candidate_key := build_candidate_identity_key(
                    original_candidates[original_idx].text,
                    original_candidates[original_idx].comment);
                if (candidate_key = '') or
                    seen_candidates.ContainsKey(candidate_key) then
                begin
                    Continue;
                end;

                append_idx := Length(m_candidates);
                SetLength(m_candidates, append_idx + 1);
                SetLength(m_candidate_segment_paths, append_idx + 1);
                m_candidates[append_idx] := original_candidates[original_idx];
                if original_idx < Length(original_paths) then
                begin
                    m_candidate_segment_paths[append_idx] :=
                        original_paths[original_idx];
                end
                else
                begin
                    m_candidate_segment_paths[append_idx] := '';
                end;
                seen_candidates.AddOrSetValue(candidate_key, True);
            end;
        finally
            seen_candidates.Free;
        end;
    end;

    procedure filter_display_single_syllable_prefix_partials;
    var
        in_idx: Integer;
        out_idx: Integer;
        has_plain_single: Boolean;
        has_full_multi_anchor: Boolean;
        full_results: TncCandidateList;
        full_idx: Integer;

        function is_relaxed_missing_apostrophe_partial(
            const candidate: TncCandidate): Boolean;
        var
            relaxed_syllables: TncPinyinParseResult;
            part_idx: Integer;
            head_key: string;
            tail_key: string;
            candidate_text: string;
            candidate_comment: string;
            anchor_idx: Integer;
            anchor_units: TArray<string>;
        begin
            Result := False;
            if (m_dictionary = nil) or (normalized_pinyin = '') or
                (Pos('''', m_composition_text) > 0) then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate.text);
            candidate_comment := normalize_pinyin_text(Trim(candidate.comment));
            if (candidate_text = '') or (candidate_comment = '') or
                (not is_relaxed_no_initial_syllable_text(candidate_comment)) or
                (get_candidate_text_unit_count(candidate_text) <> 1) then
            begin
                Exit;
            end;

            relaxed_syllables := parse_pinyin_with_relaxed_missing_apostrophe(
                normalized_pinyin);
            if Length(relaxed_syllables) < 2 then
            begin
                Exit;
            end;

            head_key := '';
            for part_idx := 0 to High(relaxed_syllables) - 1 do
            begin
                head_key := head_key + normalize_pinyin_text(
                    Trim(relaxed_syllables[part_idx].text));
            end;
            tail_key := normalize_pinyin_text(
                Trim(relaxed_syllables[High(relaxed_syllables)].text));

            if (head_key = '') or (not SameText(tail_key, candidate_comment)) then
            begin
                Exit;
            end;

            if m_dictionary.single_char_matches_pinyin(head_key, candidate_text) then
            begin
                Exit(True);
            end;

            if has_full_multi_anchor then
            begin
                for anchor_idx := 0 to High(full_results) do
                begin
                    if Trim(full_results[anchor_idx].comment) <> '' then
                    begin
                        Continue;
                    end;
                    anchor_units := split_text_units(Trim(full_results[anchor_idx].text));
                    if (Length(anchor_units) > 1) and SameText(anchor_units[0],
                        candidate_text) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function is_full_multi_anchor_candidate(
            const candidate: TncCandidate): Boolean;
        var
            candidate_text: string;
            anchor_idx: Integer;
        begin
            Result := False;
            if (not has_full_multi_anchor) or (Trim(candidate.comment) <> '') then
            begin
                Exit;
            end;

            candidate_text := Trim(candidate.text);
            if (candidate_text = '') or
                (get_candidate_text_unit_count(candidate_text) <= 1) then
            begin
                Exit;
            end;

            for anchor_idx := 0 to High(full_results) do
            begin
                if (Trim(full_results[anchor_idx].comment) = '') and
                    SameText(Trim(full_results[anchor_idx].text), candidate_text) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(m_candidates) = 0) or (m_last_lookup_syllable_count <> 1) then
        begin
            Exit;
        end;

        has_plain_single := False;
        for in_idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[in_idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[in_idx].text)) =
                1) then
            begin
                has_plain_single := True;
                Break;
            end;
        end;
        has_full_multi_anchor := False;
        if (m_dictionary <> nil) and m_dictionary.lookup(normalized_pinyin,
            full_results) then
        begin
            for full_idx := 0 to High(full_results) do
            begin
                if (Trim(full_results[full_idx].comment) = '') and
                    (get_candidate_text_unit_count(Trim(full_results[full_idx].text)) >
                    1) then
                begin
                    has_full_multi_anchor := True;
                    Break;
                end;
            end;
        end;

        out_idx := 0;
        for in_idx := 0 to High(m_candidates) do
        begin
            if Trim(m_candidates[in_idx].comment) <> '' then
            begin
                if (has_plain_single and (not has_full_multi_anchor)) or
                    (not is_relaxed_missing_apostrophe_partial(
                    m_candidates[in_idx])) then
                begin
                    Continue;
                end;
            end
            else if get_candidate_text_unit_count(Trim(m_candidates[in_idx].text)) <>
                1 then
            begin
                if has_plain_single and
                    (not is_full_multi_anchor_candidate(m_candidates[in_idx])) then
                begin
                    Continue;
                end;
            end;

            if out_idx <> in_idx then
            begin
                m_candidates[out_idx] := m_candidates[in_idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if in_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[in_idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        SetLength(m_candidates, out_idx);
        if Length(m_candidate_segment_paths) > out_idx then
        begin
            SetLength(m_candidate_segment_paths, out_idx);
        end;
    end;

    function display_complete_text_matches_explicit_apostrophe_boundary(
        const text_value: string): Boolean;
    var
        parser_local: TncPinyinParser;
        apostrophe_syllables_local: TncPinyinParseResult;
        units_local: TArray<string>;
        idx_local: Integer;
    begin
        Result := False;
        if (Pos('''', m_composition_text) <= 0) or (expected_units <= 1) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        units_local := split_text_units(Trim(text_value));
        if Length(units_local) <> expected_units then
        begin
            Exit;
        end;

        parser_local := TncPinyinParser.Create;
        try
            apostrophe_syllables_local := parser_local.parse(m_composition_text);
        finally
            parser_local.Free;
        end;

        if Length(apostrophe_syllables_local) <> expected_units then
        begin
            Exit;
        end;

        for idx_local := 0 to expected_units - 1 do
        begin
            if not m_dictionary.single_char_matches_pinyin(
                normalize_pinyin_text(Trim(apostrophe_syllables_local[idx_local].text)),
                units_local[idx_local]) then
            begin
                Exit;
            end;
        end;

        Result := True;
    end;

    procedure filter_display_explicit_apostrophe_complete_candidates;
    var
        idx: Integer;
        out_idx: Integer;
    begin
        if (Pos('''', m_composition_text) <= 0) or (expected_units <= 1) then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) =
                expected_units) and
                (not display_complete_text_matches_explicit_apostrophe_boundary(
                m_candidates[idx].text)) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure capture_entry_pathful_complete_top;
    var
        entry_text: string;
    begin
        has_entry_pathful_complete_top := False;
        FillChar(entry_top_candidate, SizeOf(entry_top_candidate), 0);
        entry_top_path := '';
        if (Length(m_candidates) = 0) or (expected_units <= 0) then
        begin
            Exit;
        end;

        entry_text := Trim(m_candidates[0].text);
        if (entry_text = '') or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(entry_text) <> expected_units) then
        begin
            Exit;
        end;

        if Length(m_candidate_segment_paths) > 0 then
        begin
            entry_top_path := m_candidate_segment_paths[0];
        end;
        if entry_top_path = '' then
        begin
            entry_top_path := get_segment_path_for_candidate(m_candidates[0], 0);
        end;
        if get_encoded_path_segment_count_local(entry_top_path) <= 1 then
        begin
            Exit;
        end;

        entry_top_candidate := m_candidates[0];
        has_entry_pathful_complete_top := True;
    end;

    procedure restore_entry_pathful_complete_top;
    var
        current_text: string;
        entry_text: string;
        found_idx: Integer;
        idx: Integer;
        moved_candidate: TncCandidate;
        moved_path: string;
        current_path: string;
        current_exact_weight: Integer;
        entry_exact_weight: Integer;
        force_entry_over_unpathful_current: Boolean;

        function entry_path_rank_beats_current_local(
            const current_candidate_value: TncCandidate): Boolean;
        const
            c_entry_path_rank_margin = 1024;
        var
            current_rank_local: Integer;
            entry_rank_local: Integer;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(entry_top_path) <= 1 then
            begin
                Exit;
            end;

            current_rank_local := display_candidate_effective_weight(
                current_candidate_value) +
                Max(0, get_segment_path_support_score(current_candidate_value));
            entry_rank_local := display_candidate_effective_weight(
                entry_top_candidate) +
                Max(0, get_segment_path_support_score(entry_top_candidate));
            Result := entry_rank_local >= current_rank_local +
                c_entry_path_rank_margin;
        end;
    begin
        if not has_entry_pathful_complete_top then
        begin
            Exit;
        end;

        entry_text := Trim(entry_top_candidate.text);
        if entry_text = '' then
        begin
            Exit;
        end;

        if Length(m_candidates) > 0 then
        begin
            current_text := Trim(m_candidates[0].text);
            if SameText(current_text, entry_text) and
                SameText(Trim(m_candidates[0].comment),
                Trim(entry_top_candidate.comment)) then
            begin
                Exit;
            end;
            if (m_candidates[0].source = cs_user) or
                is_latest_session_query_choice(current_text) or
                (get_context_query_latest_bonus(current_text) > 0) then
            begin
                Exit;
            end;
            current_path := get_display_candidate_path_for_index(m_candidates[0], 0);
            force_entry_over_unpathful_current :=
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(current_text) = expected_units) and
                (get_encoded_path_segment_count_local(entry_top_path) > 1) and
                (get_encoded_path_segment_count_local(current_path) <= 1) and
                ((normalized_pinyin = '') or
                (not display_exact_key_has_text(normalized_pinyin, current_text)));
            if (normalized_pinyin <> '') and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(current_text) = expected_units) and
                display_exact_key_has_text(normalized_pinyin, current_text) and
                (not display_exact_key_has_text(normalized_pinyin,
                entry_text)) and
                (not force_entry_over_unpathful_current) and
                (not entry_path_rank_beats_current_local(m_candidates[0])) and
                (display_candidate_effective_weight(m_candidates[0]) +
                Max(0, get_segment_path_support_score(m_candidates[0])) >=
                display_candidate_effective_weight(entry_top_candidate) +
                Max(0, get_segment_path_support_score(entry_top_candidate)) -
                1024) then
            begin
                Exit;
            end;
            if (normalized_pinyin <> '') and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(current_text) = expected_units) and
                display_exact_key_has_text(normalized_pinyin, current_text) and
                display_exact_key_has_text(normalized_pinyin, entry_text) and
                get_display_exact_text_weight(normalized_pinyin, current_text,
                expected_units, current_exact_weight) and
                get_display_exact_text_weight(normalized_pinyin, entry_text,
                expected_units, entry_exact_weight) and
                (current_exact_weight > entry_exact_weight + 256) then
            begin
                Exit;
            end;
            if (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(current_text) = expected_units) and
                (display_candidate_effective_weight(m_candidates[0]) +
                Max(0, get_segment_path_support_score(m_candidates[0])) >
                display_candidate_effective_weight(entry_top_candidate) +
                Max(0, get_segment_path_support_score(entry_top_candidate)) +
                1024) and
                (normalized_pinyin <> '') and
                (not force_entry_over_unpathful_current) and
                display_exact_key_has_text(normalized_pinyin, current_text) then
            begin
                Exit;
            end;
            if (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(current_text) = expected_units) and
                (not SameText(current_text, entry_text)) and
                display_path_has_nonprimary_multi_char_segment_outer(
                entry_top_path) and
                (not entry_path_rank_beats_current_local(m_candidates[0])) and
                (not force_entry_over_unpathful_current) and
                (display_candidate_effective_weight(m_candidates[0]) +
                Max(0, get_segment_path_support_score(m_candidates[0])) >
                display_candidate_effective_weight(entry_top_candidate) +
                Max(0, get_segment_path_support_score(entry_top_candidate)) +
                1024) and
                (not display_candidate_crosses_protected_exact_pair(
                m_candidates[0], 0)) then
            begin
                Exit;
            end;
        end;

        found_idx := -1;
        for idx := 0 to High(m_candidates) do
        begin
            if SameText(Trim(m_candidates[idx].text), entry_text) and
                SameText(Trim(m_candidates[idx].comment),
                Trim(entry_top_candidate.comment)) then
            begin
                found_idx := idx;
                Break;
            end;
        end;

        if found_idx < 0 then
        begin
            insert_display_candidate(entry_top_candidate, entry_top_path, 0);
            m_visible_candidates_cache_valid := False;
            Exit;
        end;

        if found_idx = 0 then
        begin
            Exit;
        end;

        moved_candidate := m_candidates[found_idx];
        moved_path := entry_top_path;
        if found_idx < Length(m_candidate_segment_paths) then
        begin
            if m_candidate_segment_paths[found_idx] <> '' then
            begin
                moved_path := m_candidate_segment_paths[found_idx];
            end;
        end;
        for idx := found_idx downto 1 do
        begin
            m_candidates[idx] := m_candidates[idx - 1];
            if idx < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[idx] := m_candidate_segment_paths[idx - 1];
            end;
        end;
        m_candidates[0] := moved_candidate;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := moved_path;
        end;
        m_visible_candidates_cache_valid := False;
    end;

    procedure filter_display_protected_exact_pair_crossing_candidates;
    var
        idx: Integer;
        out_idx: Integer;
        candidate_text_local: string;
        has_safe_complete_local: Boolean;
        removed_count_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) then
        begin
            Exit;
        end;

        has_safe_complete_local := False;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                (not display_candidate_crosses_protected_exact_pair(
                m_candidates[idx], idx)) then
            begin
                has_safe_complete_local := True;
                Break;
            end;
        end;

        if not has_safe_complete_local then
        begin
            Exit;
        end;

        out_idx := 0;
        removed_count_local := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                (m_candidates[idx].source <> cs_user) and
                (not is_latest_session_query_choice(candidate_text_local)) and
                (get_context_query_latest_bonus(candidate_text_local) <= 0) and
                (not (has_entry_pathful_complete_top and
                (get_encoded_path_segment_count_local(entry_top_path) > 1) and
                (get_encoded_path_segment_count_local(
                get_display_candidate_path_for_index(m_candidates[idx], idx)) >
                1) and
                SameText(candidate_text_local, Trim(entry_top_candidate.text)) and
                SameText(Trim(m_candidates[idx].comment),
                Trim(entry_top_candidate.comment)))) and
                display_candidate_crosses_protected_exact_pair(
                m_candidates[idx], idx) then
            begin
                Inc(removed_count_local);
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
        if m_config.debug_mode and (removed_count_local > 0) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' disp_xpairdrop=[n=%d]', [removed_count_local]);
        end;
    end;

    procedure promote_display_full_query_exact_candidate;
    const
        c_probe_limit = 8;
        c_min_promotable_base_exact_weight = 160;
        c_min_promotable_short_exact_weight = 600;
    var
        idx: Integer;
        best_idx: Integer;
        probe_limit: Integer;
        min_promotable_exact_weight: Integer;
        candidate_text_local: string;
        exact_weight_local: Integer;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
        query_choice_bonus_local: Integer;
        has_supported_exact_candidate: Boolean;
        has_base_exact_candidate: Boolean;
        top_path_local: string;
        top_text_local: string;

        function candidate_is_recent_supported_user_exact_local(
            const candidate_value: TncCandidate; const candidate_text_value: string): Boolean;
        var
            latest_text_local: string;
            candidate_weight_local: Integer;
        begin
            latest_text_local := '';
            if m_dictionary <> nil then
            begin
                latest_text_local := get_cached_query_latest_choice_text(
                    normalized_pinyin);
            end;
            if (latest_text_local <> '') and
                SameText(latest_text_local, candidate_text_value) then
            begin
                Exit(True);
            end;
            Result := False;
            if (candidate_value.source <> cs_user) or
                (not (display_candidate_has_supported_full_entry(normalized_pinyin,
                candidate_value) or
                display_exact_key_has_text(normalized_pinyin,
                candidate_text_value) or
                ((m_dictionary <> nil) and
                m_dictionary.is_user_entry(normalized_pinyin,
                candidate_text_value)))) then
            begin
                Exit;
            end;

            if candidate_value.has_dict_weight then
            begin
                candidate_weight_local := candidate_value.dict_weight;
            end
            else
            begin
                candidate_weight_local := candidate_value.score;
            end;
            Result := is_latest_session_query_choice(candidate_text_value) or
                display_candidate_is_persistent_latest_choice(normalized_pinyin,
                candidate_text_value) or
                (get_context_query_latest_bonus(candidate_text_value) > 0) or
                (candidate_weight_local >= 256);
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 2) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank_local := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        if expected_units <= 3 then
        begin
            min_promotable_exact_weight := c_min_promotable_short_exact_weight;
        end
        else
        begin
            min_promotable_exact_weight := c_min_promotable_base_exact_weight;
        end;
        has_supported_exact_candidate := False;
        has_base_exact_candidate := False;
        for idx := 0 to probe_limit do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                display_candidate_has_supported_full_entry(normalized_pinyin,
                m_candidates[idx]) then
            begin
                has_supported_exact_candidate := True;
                if display_candidate_has_base_full_entry(normalized_pinyin,
                    m_candidates[idx]) then
                begin
                    has_base_exact_candidate := True;
                end;
            end;
        end;
        for idx := 0 to probe_limit do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local = '') or
                (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units) or
                (not display_exact_key_has_text(normalized_pinyin,
                candidate_text_local) and
                (not candidate_is_recent_supported_user_exact_local(
                m_candidates[idx], candidate_text_local))) then
            begin
                Continue;
            end;

            if (m_candidates[idx].source = cs_user) and
                (not candidate_is_recent_supported_user_exact_local(
                m_candidates[idx], candidate_text_local)) then
            begin
                Continue;
            end;

            if has_base_exact_candidate and
                (not display_candidate_has_base_full_entry(
                normalized_pinyin, m_candidates[idx])) and
                (not candidate_is_recent_supported_user_exact_local(
                m_candidates[idx], candidate_text_local)) then
            begin
                Continue;
            end;

            if has_supported_exact_candidate and
                (not display_candidate_has_supported_full_entry(
                normalized_pinyin, m_candidates[idx])) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]);
            if get_display_exact_text_weight(normalized_pinyin,
                candidate_text_local, expected_units, exact_weight_local) then
            begin
                Inc(candidate_rank_local, exact_weight_local * 2);
            end;
            if (m_dictionary <> nil) and is_full_pinyin_key(normalized_pinyin) then
            begin
                query_choice_bonus_local := m_dictionary.get_query_choice_bonus(
                    normalized_pinyin, candidate_text_local);
                if query_choice_bonus_local > 0 then
                begin
                    Inc(candidate_rank_local, query_choice_bonus_local * 1000);
                end;
            end;
            if (m_candidates[idx].source = cs_user) and
                candidate_is_recent_supported_user_exact_local(
                m_candidates[idx], candidate_text_local) then
            begin
                Inc(candidate_rank_local, 1000000);
            end;
            if is_latest_session_query_choice(candidate_text_local) or
                (get_context_query_latest_bonus(candidate_text_local) > 0) or
                ((m_dictionary <> nil) and
                SameText(get_cached_query_latest_choice_text(
                normalized_pinyin), candidate_text_local)) then
            begin
                Inc(candidate_rank_local, 2000000);
            end;

            if (best_idx < 0) or (candidate_rank_local > best_rank_local) then
            begin
                best_idx := idx;
                best_rank_local := candidate_rank_local;
            end;
        end;

        if best_idx > 0 then
        begin
            candidate_text_local := Trim(m_candidates[best_idx].text);
            top_text_local := '';
            top_path_local := '';
            if Length(m_candidates) > 0 then
            begin
                top_text_local := Trim(m_candidates[0].text);
                top_path_local := get_display_candidate_path_for_index(
                    m_candidates[0], 0);
            end;
            if (m_candidates[best_idx].source <> cs_user) and
                (not is_latest_session_query_choice(candidate_text_local)) and
                (get_context_query_latest_bonus(candidate_text_local) <= 0) and
                get_display_exact_text_weight(normalized_pinyin,
                candidate_text_local, expected_units, exact_weight_local) and
                (exact_weight_local < min_promotable_exact_weight) and
                (top_text_local <> '') and
                (Trim(m_candidates[0].comment) = '') and
                (get_candidate_text_unit_count(top_text_local) = expected_units) and
                (not display_exact_key_has_text(normalized_pinyin,
                top_text_local)) and
                (get_encoded_path_segment_count_local(top_path_local) > 1) and
                (not display_path_has_nonprimary_multi_char_segment_outer(
                top_path_local)) then
            begin
                Exit;
            end;
            if display_path_has_nonprimary_multi_char_segment_outer(
                top_path_local) then
            begin
                force_move_display_candidate_to_index(best_idx, 0);
            end
            else
            begin
                move_display_candidate_to_index(best_idx, 0);
            end;
        end;
    end;

    procedure promote_display_one_plus_two_function_complete_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        best_idx: Integer;
        probe_limit: Integer;
        path_local: string;
        top_path_local: string;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units <> 3) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        if (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            expected_units) and
            display_exact_key_has_text(normalized_pinyin,
            Trim(m_candidates[0].text)) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(
            m_candidates[0], 0);
        if not display_path_has_two_plus_one_non_particle_tail(
            top_path_local) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank_local := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if not display_path_has_one_plus_two_function_head(path_local) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]);
            if (m_candidates[idx].source = cs_user) and
                (((not m_candidates[idx].has_dict_weight) and
                (m_candidates[idx].score >= 4096)) or
                (m_candidates[idx].has_dict_weight and
                (m_candidates[idx].dict_weight >= 4096))) then
            begin
                Inc(candidate_rank_local, 1000000);
            end;
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx := idx;
            end;
        end;

        if best_idx > 0 then
        begin
            move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure promote_display_exact_two_syllable_head_path_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        best_idx: Integer;
        probe_limit: Integer;
        top_text_units_local: TArray<string>;
        top_head_text_local: string;
        head_key_local: string;
        path_local: string;
        parts_local: TArray<string>;
        head_segment_local: string;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        if (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        if (m_candidates[0].source = cs_user) and
            (((not m_candidates[0].has_dict_weight) and
            (m_candidates[0].score >= 4096)) or
            (m_candidates[0].has_dict_weight and
            (m_candidates[0].dict_weight >= 4096)) or
            is_latest_session_query_choice(Trim(m_candidates[0].text)) or
            (get_context_query_latest_bonus(Trim(m_candidates[0].text)) > 0)) then
        begin
            Exit;
        end;

        top_text_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_text_units_local) < 2 then
        begin
            Exit;
        end;
        top_head_text_local := top_text_units_local[0] +
            top_text_units_local[1];
        head_key_local := build_display_query_key(0, 2);
        if (head_key_local = '') or
            display_exact_key_has_text(head_key_local, top_head_text_local) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank_local := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if get_encoded_path_segment_count_local(path_local) <= 1 then
            begin
                Continue;
            end;
            parts_local := path_local.Split([c_segment_path_separator]);
            if Length(parts_local) < 2 then
            begin
                Continue;
            end;

            head_segment_local := Trim(parts_local[0]);
            if (get_candidate_text_unit_count(head_segment_local) <> 2) or
                (not display_exact_key_has_text(head_key_local,
                head_segment_local)) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]);
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx := idx;
            end;
        end;

        if best_idx > 0 then
        begin
            move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure filter_display_two_plus_one_noise_when_one_plus_two_function_exists;
    var
        idx: Integer;
        out_idx: Integer;
        path_local: string;
        has_one_plus_two_function_complete: Boolean;
    begin
        if (Length(m_candidates) <= 1) or (expected_units <> 3) then
        begin
            Exit;
        end;

        has_one_plus_two_function_complete := False;
        for idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if display_path_has_one_plus_two_function_head(path_local) then
            begin
                has_one_plus_two_function_complete := True;
                Break;
            end;
        end;

        if not has_one_plus_two_function_complete then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) =
                expected_units) and
                (not display_candidate_is_exact_full_dictionary_entry(
                normalized_pinyin, m_candidates[idx])) and
                display_path_has_two_plus_one_non_particle_tail(path_local) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure promote_display_fixed_tail_completion_over_matching_partial;
    var
        top_text_local: string;
        top_comment_key_local: string;
        tail_text_local: string;
        expected_text_local: string;
        idx: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Trim(m_candidates[0].comment) = '') then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        top_comment_key_local := normalize_pinyin_text(
            Trim(m_candidates[0].comment));
        tail_text_local := get_display_fixed_tail_text_outer(
            top_comment_key_local);
        if (top_text_local = '') or (tail_text_local = '') or
            (get_candidate_text_unit_count(top_text_local) + 1 <>
            expected_units) then
        begin
            Exit;
        end;

        expected_text_local := top_text_local + tail_text_local;
        for idx := 1 to Min(8, High(m_candidates)) do
        begin
            if SameText(Trim(m_candidates[idx].text), expected_text_local) and
                (Trim(m_candidates[idx].comment) = '') then
            begin
                move_display_candidate_to_index(idx, 0);
                Exit;
            end;
        end;
    end;

    procedure promote_and_filter_display_fixed_quantity_tail_candidate;
    const
        c_probe_limit = 10;
    var
        idx: Integer;
        out_idx: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        alignment_score_local: Integer;
    begin
        if (Length(m_candidates) = 0) or (expected_units < 4) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            alignment_score_local :=
                get_display_fixed_quantity_alignment_score_outer(
                m_candidates[idx]);
            if alignment_score_local <= 0 then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]) + alignment_score_local;
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local < 0 then
        begin
            Exit;
        end;

        if best_idx_local > 0 then
        begin
            move_display_candidate_to_index(best_idx_local, 0);
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            alignment_score_local :=
                get_display_fixed_quantity_alignment_score_outer(
                m_candidates[idx]);
            if alignment_score_local < 0 then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx < Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure promote_display_structured_quantity_candidate;
    var
        structured_query_local: string;
        structured_syllables_local: TncPinyinParseResult;
        actual_units_local: Integer;
        number_text_local: string;
        tail_text_local: string;
        structured_text_local: string;
        structured_path_local: string;
        existing_idx_local: Integer;
        candidate_local: TncCandidate;
        target_idx_local: Integer;
        is_ordinal_quantity_local: Boolean;

        function is_protected_complete_candidate_local(
            const candidate_value_local: TncCandidate): Boolean;
        var
            candidate_text_local: string;
        begin
            Result := False;
            candidate_text_local := Trim(candidate_value_local.text);
            if (candidate_text_local = '') or
                (Trim(candidate_value_local.comment) <> '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                actual_units_local) then
            begin
                Exit;
            end;

            if candidate_value_local.source = cs_user then
            begin
                Exit(True);
            end;
            if (m_dictionary <> nil) and
                m_dictionary.is_user_entry(structured_query_local,
                candidate_text_local) then
            begin
                Exit(True);
            end;

            if not is_ordinal_quantity_local then
            begin
                Exit(True);
            end;
        end;

        function get_structured_insert_index_local: Integer;
        begin
            Result := 0;
            while (Result <= High(m_candidates)) and
                is_protected_complete_candidate_local(m_candidates[Result]) do
            begin
                Inc(Result);
            end;
        end;

        procedure insert_structured_candidate_local(const target_idx: Integer);
        var
            idx: Integer;
            insert_idx: Integer;
            candidate_count: Integer;
        begin
            candidate_count := Length(m_candidates);
            insert_idx := Max(0, Min(target_idx, candidate_count));
            SetLength(m_candidates, candidate_count + 1);
            if Length(m_candidate_segment_paths) < candidate_count + 1 then
            begin
                SetLength(m_candidate_segment_paths, candidate_count + 1);
            end;
            for idx := candidate_count downto insert_idx + 1 do
            begin
                m_candidates[idx] := m_candidates[idx - 1];
                m_candidate_segment_paths[idx] := m_candidate_segment_paths[idx - 1];
            end;
            m_candidates[insert_idx] := candidate_local;
            m_candidate_segment_paths[insert_idx] := structured_path_local;
        end;

        procedure upsert_structured_candidate_local;
        begin
            if structured_text_local = '' then
            begin
                Exit;
            end;
            target_idx_local := get_structured_insert_index_local;

            existing_idx_local := find_display_candidate(
                structured_text_local, '');
            if existing_idx_local >= 0 then
            begin
                if existing_idx_local < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[existing_idx_local] :=
                        structured_path_local;
                end;
                if existing_idx_local > target_idx_local then
                begin
                    move_display_candidate_to_index(existing_idx_local,
                        target_idx_local);
                end;
                Exit;
            end;

            FillChar(candidate_local, SizeOf(candidate_local), 0);
            candidate_local.text := structured_text_local;
            candidate_local.comment := '';
            candidate_local.score := 0;
            candidate_local.source := cs_rule;
            candidate_local.has_dict_weight := False;
            candidate_local.dict_weight := 0;
            insert_structured_candidate_local(target_idx_local);
        end;
    begin
        is_ordinal_quantity_local := False;
        structured_query_local := normalized_pinyin;
        structured_syllables_local := syllables;
        actual_units_local := Length(structured_syllables_local);
        if ((structured_query_local = '') or
            ((actual_units_local <> 2) and (actual_units_local <> 3))) and
            (user_entry_query <> '') then
        begin
            structured_query_local := user_entry_query;
            structured_syllables_local :=
                get_effective_compact_pinyin_syllables(
                structured_query_local, True);
            actual_units_local := Length(structured_syllables_local);
        end;

        if (structured_query_local = '') or
            ((actual_units_local <> 2) and (actual_units_local <> 3)) then
        begin
            Exit;
        end;

        if actual_units_local = 2 then
        begin
            number_text_local := get_display_number_text_outer(
                structured_syllables_local[0].text);
            tail_text_local := get_display_structured_quantity_tail_text_outer(
                structured_syllables_local[1].text);
            if (number_text_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;

            structured_text_local := number_text_local + tail_text_local;
            structured_path_local := number_text_local +
                c_segment_path_separator + tail_text_local;
            upsert_structured_candidate_local;
            Exit;
        end;

        if normalize_pinyin_text(structured_syllables_local[0].text) <> 'di' then
        begin
            Exit;
        end;
        is_ordinal_quantity_local := True;

        number_text_local := get_display_number_text_outer(
            structured_syllables_local[1].text);
        tail_text_local := get_display_structured_quantity_tail_text_outer(
            structured_syllables_local[2].text);
        if (number_text_local = '') or (tail_text_local = '') then
        begin
            Exit;
        end;

        structured_text_local := string(Char($7B2C)) + number_text_local +
            tail_text_local;
        structured_path_local := string(Char($7B2C)) +
            c_segment_path_separator + number_text_local + tail_text_local;
        upsert_structured_candidate_local;
    end;

    procedure promote_display_longer_prefix_completion_candidate;
    const
        c_probe_limit = 8;
    var
        top_text_local: string;
        top_units_local: Integer;
        idx: Integer;
        candidate_text_local: string;
        candidate_units_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Trim(m_candidates[0].comment) <> '') then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        top_units_local := get_candidate_text_unit_count(top_text_local);
        if (top_text_local = '') or (top_units_local < 2) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            if (candidate_units_local <= top_units_local) or
                (candidate_units_local > expected_units + 3) or
                (Pos(top_text_local, candidate_text_local) <> 1) then
            begin
                Continue;
            end;

            candidate_rank_local := candidate_units_local * 4096 +
                display_candidate_effective_weight(m_candidates[idx]);
            if (idx < Length(m_candidate_segment_paths)) and
                (get_encoded_path_segment_count_local(
                m_candidate_segment_paths[idx]) > 1) then
            begin
                Inc(candidate_rank_local, 6000);
            end;

            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_more_resolved_partial_candidate;
    const
        c_probe_limit = 8;
    var
        top_text_local: string;
        top_comment_units_local: Integer;
        top_text_units_local: Integer;
        idx: Integer;
        candidate_text_local: string;
        candidate_comment_local: string;
        candidate_text_units_local: Integer;
        candidate_comment_units_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Trim(m_candidates[0].comment) = '') then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        top_text_units_local := get_candidate_text_unit_count(top_text_local);
        top_comment_units_local := get_effective_compact_pinyin_unit_count(
            normalize_pinyin_text(Trim(m_candidates[0].comment)));
        if (top_text_local = '') or (top_comment_units_local <= 1) or
            (top_text_units_local + top_comment_units_local <> expected_units) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_comment_local := normalize_pinyin_text(
                Trim(m_candidates[idx].comment));
            if (candidate_text_local = '') or (candidate_comment_local = '') or
                (Pos(top_text_local, candidate_text_local) <> 1) then
            begin
                Continue;
            end;

            candidate_text_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            candidate_comment_units_local :=
                get_effective_compact_pinyin_unit_count(candidate_comment_local);
            if (candidate_text_units_local <= top_text_units_local) or
                (candidate_text_units_local + candidate_comment_units_local <>
                expected_units) or
                (candidate_comment_units_local >= top_comment_units_local) then
            begin
                Continue;
            end;

            candidate_rank_local := candidate_text_units_local * 4096 +
                display_candidate_effective_weight(m_candidates[idx]);
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_fixed_alignment_complete_candidate;
    const
        c_probe_limit = 10;
        c_min_alignment_gain = 12000;
    var
        idx: Integer;
        best_idx_local: Integer;
        top_alignment_local: Integer;
        candidate_alignment_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;

        function get_alignment_local(const candidate_value: TncCandidate): Integer;
        begin
            Result := get_display_fixed_tail_alignment_score_outer(
                candidate_value) +
                get_display_fixed_quantity_alignment_score_outer(
                candidate_value);
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) then
        begin
            Exit;
        end;

        top_alignment_local := get_alignment_local(m_candidates[0]);
        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_alignment_local := get_alignment_local(m_candidates[idx]);
            if candidate_alignment_local < top_alignment_local +
                c_min_alignment_gain then
            begin
                Continue;
            end;

            candidate_rank_local := candidate_alignment_local +
                Max(-16000, Min(16000,
                display_candidate_effective_weight(m_candidates[idx]) -
                display_candidate_effective_weight(m_candidates[0])));
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_xiang_yiyang_grammar_candidate;
    const
        c_probe_limit = 8;
    var
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        idx: Integer;
        unit_idx: Integer;
        diff_idx_local: Integer;
        candidate_text_local: string;
        top_text_local: string;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 4) or
            (Length(syllables) <> expected_units) or
            (Trim(m_candidates[0].comment) <> '') then
        begin
            Exit;
        end;

        if (normalize_pinyin_text(syllables[expected_units - 2].text) <>
            'yi') or
            (normalize_pinyin_text(syllables[expected_units - 1].text) <>
            'yang') then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        top_units_local := split_text_units(top_text_local);
        if (Length(top_units_local) <> expected_units) or
            (not SameText(Trim(top_units_local[expected_units - 2]),
            string(Char($4E00)))) or
            (not SameText(Trim(top_units_local[expected_units - 1]),
            string(Char($6837)))) then
        begin
            Exit;
        end;

        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_units_local := split_text_units(candidate_text_local);
            if (Length(candidate_units_local) <> expected_units) or
                (not SameText(Trim(candidate_units_local[expected_units - 2]),
                string(Char($4E00)))) or
                (not SameText(Trim(candidate_units_local[expected_units - 1]),
                string(Char($6837)))) then
            begin
                Continue;
            end;

            diff_idx_local := -1;
            for unit_idx := 0 to expected_units - 1 do
            begin
                if SameText(Trim(candidate_units_local[unit_idx]),
                    Trim(top_units_local[unit_idx])) then
                begin
                    Continue;
                end;
                if diff_idx_local >= 0 then
                begin
                    diff_idx_local := -2;
                    Break;
                end;
                diff_idx_local := unit_idx;
            end;

            if (diff_idx_local < 0) or
                (normalize_pinyin_text(syllables[diff_idx_local].text) <>
                'xiang') then
            begin
                Continue;
            end;

            if SameText(Trim(top_units_local[diff_idx_local]),
                string(Char($60F3))) and
                SameText(Trim(candidate_units_local[diff_idx_local]),
                string(Char($50CF))) then
            begin
                force_move_display_candidate_to_index(idx, 0);
                Exit;
            end;
        end;
    end;

    procedure promote_display_contextual_modifier_repair_candidate;
    const
        c_probe_limit = 10;
        c_protected_exact_phrase_weight = 700;
    var
        top_units_local: TArray<string>;

        function build_units_text_local(const start_idx: Integer;
            const unit_count: Integer): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (unit_count <= 0) or
                (start_idx + unit_count > Length(top_units_local)) then
            begin
                Exit;
            end;
            for unit_idx := start_idx to start_idx + unit_count - 1 do
            begin
                Result := Result + top_units_local[unit_idx];
            end;
        end;

        function build_repaired_text_local(const start_idx: Integer;
            const unit_count: Integer; const replacement_text: string): string;
        var
            unit_idx: Integer;
        begin
            Result := '';
            for unit_idx := 0 to High(top_units_local) do
            begin
                if unit_idx = start_idx then
                begin
                    Result := Result + replacement_text;
                end;
                if (unit_idx < start_idx) or
                    (unit_idx >= start_idx + unit_count) then
                begin
                    Result := Result + top_units_local[unit_idx];
                end;
            end;
        end;

        function has_exact_phrase_local(const start_idx: Integer;
            const unit_count: Integer): Boolean;
        var
            query_key_local: string;
            text_local: string;
        begin
            Result := False;
            if (unit_count < 2) or
                (start_idx < 0) or
                (start_idx + unit_count > expected_units) then
            begin
                Exit;
            end;
            query_key_local := build_display_query_key(start_idx, unit_count);
            text_local := build_units_text_local(start_idx, unit_count);
            Result := display_exact_key_has_text(query_key_local, text_local);
        end;

        function has_following_phrase_local(const start_idx: Integer): Boolean;
        var
            unit_count: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (start_idx >= expected_units) then
            begin
                Exit;
            end;
            for unit_count := 2 to Min(4, expected_units - start_idx) do
            begin
                if has_exact_phrase_local(start_idx, unit_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_preceding_phrase_local(const end_idx: Integer): Boolean;
        var
            unit_count: Integer;
            start_idx: Integer;
        begin
            Result := False;
            if (end_idx <= 0) or (end_idx >= expected_units) then
            begin
                Exit;
            end;
            for unit_count := 2 to Min(4, end_idx + 1) do
            begin
                start_idx := end_idx - unit_count + 1;
                if has_exact_phrase_local(start_idx, unit_count) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function range_inside_strong_exact_phrase_local(const start_idx: Integer;
            const unit_count: Integer): Boolean;
        var
            span_len_local: Integer;
            span_start_local: Integer;
            span_end_local: Integer;
            query_key_local: string;
            span_text_local: string;
            span_weight_local: Integer;
        begin
            Result := False;
            if (unit_count <= 0) or (start_idx < 0) or
                (start_idx + unit_count > expected_units) then
            begin
                Exit;
            end;

            for span_len_local := unit_count + 1 to
                Min(4, expected_units) do
            begin
                for span_start_local := Max(0, start_idx - span_len_local + 1) to
                    Min(start_idx, expected_units - span_len_local) do
                begin
                    span_end_local := span_start_local + span_len_local;
                    if (span_start_local > start_idx) or
                        (span_end_local < start_idx + unit_count) then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(
                        span_start_local, span_len_local);
                    span_text_local := build_units_text_local(span_start_local,
                        span_len_local);
                    if (query_key_local <> '') and (span_text_local <> '') and
                        get_display_exact_text_weight(query_key_local,
                        span_text_local, span_len_local, span_weight_local) and
                        (span_weight_local >= c_protected_exact_phrase_weight) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        end;

        function try_pick_modifier_single_local(const unit_idx: Integer;
            const current_text: string; out out_text: string;
            out out_weight: Integer; const require_context: Boolean): Boolean;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            candidate_idx: Integer;
            candidate_text_local: string;
            candidate_weight_local: Integer;
            ranked_weight_local: Integer;
            between_phrases_local: Boolean;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            query_key_local := build_display_query_key(unit_idx, 1);
            if (query_key_local = '') or
                (not lookup_display_exact_cached(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            between_phrases_local := has_preceding_phrase_local(unit_idx - 1) and
                has_following_phrase_local(unit_idx + 1);
            if require_context and (not between_phrases_local) then
            begin
                Exit;
            end;

            for candidate_idx := 0 to High(exact_results_local) do
            begin
                if candidate_idx >= c_probe_limit then
                begin
                    Break;
                end;
                if Trim(exact_results_local[candidate_idx].comment) <> '' then
                begin
                    Continue;
                end;
                candidate_text_local := Trim(
                    exact_results_local[candidate_idx].text);
                if (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                    (not is_display_function_or_modifier_single(
                    candidate_text_local)) or
                    (not m_dictionary.single_char_matches_pinyin(
                    query_key_local, candidate_text_local)) then
                begin
                    Continue;
                end;

                candidate_weight_local := display_candidate_effective_weight(
                    exact_results_local[candidate_idx]);
                ranked_weight_local := candidate_weight_local +
                    ((c_probe_limit - candidate_idx) * 32);
                if between_phrases_local and
                    ((candidate_text_local = string(Char($4E0E))) or
                    (candidate_text_local = string(Char($548C)))) then
                begin
                    Inc(ranked_weight_local, 260);
                end;
                if (out_text = '') or (ranked_weight_local > out_weight) then
                begin
                    out_text := candidate_text_local;
                    out_weight := ranked_weight_local;
                    Result := True;
                end;
            end;
        end;

        function try_pick_modifier_pair_local(const start_idx: Integer;
            const current_text: string; out out_text: string;
            out out_weight: Integer): Boolean;
        var
            left_text: string;
            right_text: string;
            left_weight: Integer;
            right_weight: Integer;
            current_weight: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            if (start_idx < 0) or (start_idx + 2 > expected_units) or
                ((not has_following_phrase_local(start_idx + 2)) and
                (not has_preceding_phrase_local(start_idx - 1))) or
                (not get_display_exact_text_weight(
                build_display_query_key(start_idx, 2), current_text, 2,
                current_weight)) then
            begin
                Exit;
            end;
            if current_weight >= 1100 then
            begin
                Exit;
            end;

            if (not try_pick_modifier_single_local(start_idx,
                top_units_local[start_idx], left_text, left_weight, False)) or
                (not try_pick_modifier_single_local(start_idx + 1,
                top_units_local[start_idx + 1], right_text, right_weight,
                False)) then
            begin
                Exit;
            end;

            if (left_weight < 780) or (right_weight < 780) then
            begin
                Exit;
            end;

            out_text := left_text + right_text;
            out_weight := left_weight + right_weight;
            Result := (not SameText(out_text, current_text)) and
                (out_weight > current_weight + 280);
        end;

        procedure promote_or_insert_repair_local(const repaired_text: string;
            const repaired_weight: Integer);
        var
            existing_idx: Integer;
            repaired_candidate: TncCandidate;
        begin
            if (repaired_text = '') or SameText(repaired_text,
                Trim(m_candidates[0].text)) or
                (get_candidate_text_unit_count(repaired_text) <> expected_units) then
            begin
                Exit;
            end;

            existing_idx := find_display_candidate(repaired_text, '');
            if existing_idx >= 0 then
            begin
                force_move_display_candidate_to_index(existing_idx, 0);
                Exit;
            end;

            repaired_candidate := m_candidates[0];
            repaired_candidate.text := repaired_text;
            repaired_candidate.comment := '';
            repaired_candidate.source := cs_rule;
            repaired_candidate.has_dict_weight := True;
            repaired_candidate.score := Max(repaired_candidate.score,
                repaired_weight + 52000);
            repaired_candidate.dict_weight := repaired_candidate.score;
            insert_display_candidate(repaired_candidate, '', 0);
            existing_idx := find_display_candidate(repaired_text, '');
            if existing_idx > 0 then
            begin
                force_move_display_candidate_to_index(existing_idx, 0);
            end;
        end;

    var
        unit_idx: Integer;
        current_text_local: string;
        repair_text_local: string;
        repair_weight_local: Integer;
    begin
        if (Length(m_candidates) = 0) or (m_dictionary = nil) or
            (expected_units < 5) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;
        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;

        for unit_idx := 0 to expected_units - 2 do
        begin
            current_text_local := build_units_text_local(unit_idx, 2);
            if range_inside_strong_exact_phrase_local(unit_idx, 2) then
            begin
                Continue;
            end;
            if try_pick_modifier_pair_local(unit_idx, current_text_local,
                repair_text_local, repair_weight_local) then
            begin
                promote_or_insert_repair_local(build_repaired_text_local(
                    unit_idx, 2, repair_text_local), repair_weight_local);
                Exit;
            end;
        end;

        for unit_idx := 0 to expected_units - 1 do
        begin
            current_text_local := top_units_local[unit_idx];
            if range_inside_strong_exact_phrase_local(unit_idx, 1) then
            begin
                Continue;
            end;
            if try_pick_modifier_single_local(unit_idx, current_text_local,
                repair_text_local, repair_weight_local, True) and
                (not SameText(repair_text_local, current_text_local)) then
            begin
                promote_or_insert_repair_local(build_repaired_text_local(
                    unit_idx, 1, repair_text_local), repair_weight_local);
                Exit;
            end;
        end;
    end;

    procedure promote_display_better_exact_chunk_path_candidate;
    const
        c_probe_limit = 10;
        c_min_path_quality_gain = 1200;
    var
        top_path_score_local: Integer;
        best_idx_local: Integer;
        best_path_score_local: Integer;
        idx: Integer;
        candidate_path_score_local: Integer;

        function score_exact_chunk_path_local(const candidate_value: TncCandidate;
            const candidate_idx: Integer): Integer;
        var
            encoded_path_local: string;
            path_parts_local: TArray<string>;
            part_idx: Integer;
            part_text_local: string;
            part_units_local: Integer;
            query_key_local: string;
            syllable_pos_local: Integer;
            exact_weight_local: Integer;
            exact_units_local: Integer;
            multi_exact_units_local: Integer;
            non_function_exact_single_count_local: Integer;
            non_exact_units_local: Integer;
            prev_part_text_local: string;
            prev_part_units_local: Integer;
            pair_text_local: string;
            pair_key_local: string;
            combined_units_local: Integer;
            candidate_text_local: string;

            function segment_has_exact_prefix_fixed_tail_local(
                const start_pos_local: Integer; const unit_count_local: Integer;
                const text_local: string): Boolean;
            var
                segment_units_local: TArray<string>;
                prefix_text_local: string;
                prefix_key_local: string;
                unit_idx_local: Integer;
                expected_tail_text_local: string;
            begin
                Result := False;
                if (unit_count_local < 2) or
                    (start_pos_local + unit_count_local > expected_units) then
                begin
                    Exit;
                end;

                expected_tail_text_local := get_display_fixed_tail_text_outer(
                    syllables[start_pos_local + unit_count_local - 1].text);
                if expected_tail_text_local = '' then
                begin
                    Exit;
                end;

                segment_units_local := split_text_units(text_local);
                if (Length(segment_units_local) <> unit_count_local) or
                    (not SameText(Trim(segment_units_local[
                    High(segment_units_local)]), expected_tail_text_local)) then
                begin
                    Exit;
                end;

                prefix_text_local := '';
                for unit_idx_local := 0 to High(segment_units_local) - 1 do
                begin
                    prefix_text_local := prefix_text_local +
                        segment_units_local[unit_idx_local];
                end;
                prefix_key_local := build_display_query_key(start_pos_local,
                    unit_count_local - 1);
                Result := (prefix_key_local <> '') and
                    (prefix_text_local <> '') and
                    display_exact_key_has_text(prefix_key_local,
                    prefix_text_local);
            end;

            function is_function_bridge_single_text_local(
                const text_local: string): Boolean;
            begin
                Result := (text_local = string(Char($4E0D))) or
                    (text_local = string(Char($518D))) or
                    (text_local = string(Char($9AD8))) or
                    (text_local = string(Char($5F88))) or
                    (text_local = string(Char($66F4))) or
                    (text_local = string(Char($6700))) or
                    (text_local = string(Char($592A))) or
                    (text_local = string(Char($8981))) or
                    (text_local = string(Char($80FD))) or
                    (text_local = string(Char($6CA1))) or
                    (text_local = string(Char($65E0))) or
                    (text_local = string(Char($975E))) or
                    (text_local = string(Char($53EF))) or
                    (text_local = string(Char($4F1A))) or
                    (text_local = string(Char($5E94))) or
                    (text_local = string(Char($5C06))) or
                    (text_local = string(Char($672A))) or
                    (text_local = string(Char($5DF2))) or
                    (text_local = string(Char($6B63))) or
                    (text_local = string(Char($53C8))) or
                    (text_local = string(Char($4E5F))) or
                    (text_local = string(Char($8FD8))) or
                    (text_local = string(Char($90FD))) or
                    (text_local = string(Char($5C31))) or
                    (text_local = string(Char($522B))) or
                    (text_local = string(Char($6709))) or
                    (text_local = string(Char($5BF9))) or
                    (text_local = string(Char($5230))) or
                    (text_local = string(Char($4ECE))) or
                    (text_local = string(Char($548C))) or
                    (text_local = string(Char($4E0E))) or
                    (text_local = string(Char($4E8E))) or
                    (text_local = string(Char($628A))) or
                    (text_local = string(Char($88AB))) or
                    (text_local = string(Char($7ED9))) or
                    (text_local = string(Char($8BA9))) or
                    (text_local = string(Char($5728))) or
                    (text_local = string(Char($7684))) or
                    (text_local = string(Char($5730))) or
                    (text_local = string(Char($5F97))) or
                    (text_local = string(Char($6240))) or
                    (text_local = string(Char($662F))) or
                    (text_local = string(Char($53EA))) or
                    (text_local = string(Char($624D))) or
                    (text_local = string(Char($5E76))) or
                    (text_local = string(Char($4E14))) or
                    (text_local = string(Char($4E3A)));
            end;
        begin
            Result := Low(Integer) div 2;
            if (Trim(candidate_value.comment) <> '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
                expected_units) then
            begin
                Exit;
            end;

            encoded_path_local := get_display_candidate_path_for_index(
                candidate_value, candidate_idx);
            if get_encoded_path_segment_count_local(encoded_path_local) <= 1 then
            begin
                candidate_text_local := Trim(candidate_value.text);
                if (Trim(candidate_value.comment) = '') and
                    (candidate_text_local <> '') and
                    (get_candidate_text_unit_count(candidate_text_local) =
                    expected_units) and
                    display_exact_key_has_text(normalized_pinyin,
                    candidate_text_local) then
                begin
                    Result := expected_units * 700;
                    if get_display_exact_text_weight(normalized_pinyin,
                        candidate_text_local, expected_units,
                        exact_weight_local) then
                    begin
                        Inc(Result, Min(4800, Max(0,
                            exact_weight_local * 3)));
                    end;
                    if display_candidate_has_base_full_entry(normalized_pinyin,
                        candidate_value) then
                    begin
                        Inc(Result, 600);
                    end;
                    if display_candidate_is_persistent_latest_choice(
                        normalized_pinyin, candidate_text_local) or
                        is_latest_session_query_choice(candidate_text_local) then
                    begin
                        Inc(Result, 2000);
                    end;
                end;
                Exit;
            end;

            path_parts_local := encoded_path_local.Split(
                [c_segment_path_separator]);
            syllable_pos_local := 0;
            exact_units_local := 0;
            multi_exact_units_local := 0;
            non_function_exact_single_count_local := 0;
            non_exact_units_local := 0;
            prev_part_text_local := '';
            prev_part_units_local := 0;
            Result := 0;
            for part_idx := 0 to High(path_parts_local) do
            begin
                part_text_local := Trim(path_parts_local[part_idx]);
                part_units_local := get_candidate_text_unit_count(
                    part_text_local);
                if (part_text_local = '') or (part_units_local <= 0) or
                    (syllable_pos_local + part_units_local > expected_units) then
                begin
                    Result := Low(Integer) div 2;
                    Exit;
                end;

                query_key_local := build_display_query_key(
                    syllable_pos_local, part_units_local);
                if display_exact_key_has_text(query_key_local,
                    part_text_local) then
                begin
                    Inc(exact_units_local, part_units_local);
                    Inc(Result, part_units_local * 1000);
                    if part_units_local >= 2 then
                    begin
                        Inc(multi_exact_units_local, part_units_local);
                        Inc(Result, 360);
                    end;
                    if (part_units_local = 1) and
                        (not is_function_bridge_single_text_local(
                        part_text_local)) then
                    begin
                        Inc(non_function_exact_single_count_local);
                    end;
                    if get_display_exact_text_weight(query_key_local,
                        part_text_local, part_units_local,
                        exact_weight_local) then
                    begin
                        Inc(Result, Min(2200, Max(0,
                            exact_weight_local)) div 2);
                    end;
                end
                else if segment_has_exact_prefix_fixed_tail_local(
                    syllable_pos_local, part_units_local, part_text_local) then
                begin
                    Inc(exact_units_local, part_units_local);
                    Inc(Result, part_units_local * 900);
                    Inc(Result, 420);
                end
                else
                begin
                    Inc(non_exact_units_local, part_units_local);
                    if part_units_local >= 2 then
                    begin
                        Dec(Result, part_units_local * 1600 + 600);
                    end
                    else
                    begin
                        Dec(Result, 260);
                    end;
                end;

                if (prev_part_units_local > 0) and (part_units_local > 0) and
                    (syllable_pos_local > 0) then
                begin
                    combined_units_local := prev_part_units_local + part_units_local;
                    pair_text_local := prev_part_text_local + part_text_local;
                    pair_key_local := build_display_query_key(
                        syllable_pos_local - prev_part_units_local,
                        combined_units_local);
                    if (combined_units_local >= 2) and
                        (combined_units_local <= 4) and (pair_key_local <> '') and
                        (prev_part_units_local = 1) and
                        (part_units_local = 1) and
                        is_function_bridge_single_text_local(prev_part_text_local) and
                        is_function_bridge_single_text_local(part_text_local) and
                        display_exact_key_has_text(pair_key_local,
                        pair_text_local) then
                    begin
                        Inc(Result, 90000);
                    end
                    else if (combined_units_local >= 2) and
                        (combined_units_local <= 4) and (pair_key_local <> '') and
                        (not display_exact_key_has_text(pair_key_local,
                        pair_text_local)) and
                        display_exact_key_has_any_complete_text(pair_key_local,
                        combined_units_local) then
                    begin
                        Dec(Result, 60000 + combined_units_local * 4000);
                    end;
                end;
                Inc(syllable_pos_local, part_units_local);
                prev_part_text_local := part_text_local;
                prev_part_units_local := part_units_local;
            end;

            if syllable_pos_local <> expected_units then
            begin
                Result := Low(Integer) div 2;
                Exit;
            end;

            Inc(Result, get_display_fixed_tail_alignment_score_outer(
                candidate_value));
            Inc(Result, get_display_fixed_quantity_alignment_score_outer(
                candidate_value));
            Inc(Result, exact_units_local * 180);
            Dec(Result, non_exact_units_local * 220);
            if non_exact_units_local = 0 then
            begin
                Inc(Result, 1200);
            end;
            if (multi_exact_units_local >= 4) and
                (non_function_exact_single_count_local > 0) then
            begin
                Dec(Result, non_function_exact_single_count_local * 1400);
            end;
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (expected_units > 18) then
        begin
            Exit;
        end;

        top_path_score_local := score_exact_chunk_path_local(
            m_candidates[0], 0);
        best_idx_local := -1;
        best_path_score_local := top_path_score_local;
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_path_score_local := score_exact_chunk_path_local(
                m_candidates[idx], idx);
            if candidate_path_score_local > best_path_score_local then
            begin
                best_path_score_local := candidate_path_score_local;
                best_idx_local := idx;
            end;
        end;

        if (best_idx_local > 0) and
            (best_path_score_local > top_path_score_local +
            c_min_path_quality_gain) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_last_syllable_fixed_tail_candidate;
    const
        c_probe_limit = 10;
    var
        expected_tail_text_local: string;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        idx: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        expected_tail_text_local := get_display_fixed_tail_text_outer(
            syllables[expected_units - 1].text);
        if expected_tail_text_local = '' then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if (Trim(m_candidates[0].comment) = '') and
            (Length(top_units_local) = expected_units) and
            SameText(Trim(top_units_local[High(top_units_local)]),
            expected_tail_text_local) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;
            candidate_units_local := split_text_units(
                Trim(m_candidates[idx].text));
            if (Length(candidate_units_local) <> expected_units) or
                (not SameText(Trim(candidate_units_local[
                High(candidate_units_local)]), expected_tail_text_local)) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]) + Max(0,
                get_segment_path_support_score(m_candidates[idx]));
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_adverbial_de_particle_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        unit_idx: Integer;
        existing_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        candidate_units_local: TArray<string>;
        replacement_candidate_local: TncCandidate;
        replacement_text_local: string;
        best_replacement_text_local: string;
        best_replacement_candidate_local: TncCandidate;

        function is_display_de_adverbial_left_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $6025, $822C, $5F85, $987E, $7EC6, $5927, $5C0F, $9AD8,
                $4F4E, $5FEB, $6162, $6DF1, $6D45, $8F7B, $91CD, $9759,
                $9ED8, $6781, $529B, $5FC3, $610F, $7136, $65AD, $7EED,
                $76F4, $660E, $786E, $6E05, $695A, $6084, $5174,
                $597D, $4E3B, $529F, $82E6, $4E25, $5BC6, $5B9A, $7A33,
                $51C6, $6EE1, $575A, $51B3, $6027, $7ECF, $5916, $89C9,
                $51C0, $901F, $987A, $631A, $6FC0, $594B, $548C, $7981,
                $601D, $8BB6, $7545, $8352, $56F4, $6599, $8D45, $5947,
                $6B63:
                    Result := True;
            end;
        end;

        function is_display_de_adverbial_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $505A, $51FA, $5230, $53BB, $53D1, $53D8, $53D6, $547C,
                $542C, $56DE, $5B66, $5F00, $60F3, $6210, $6253, $627E,
                $62FF, $6301, $63A5, $63D0, $6539, $653E, $6765,
                $7528, $770B, $7740, $7814, $79BB, $7EE7, $7ED9, $80E1,
                $89C1, $8BA9, $8BB2, $8BB0, $8BF4, $8D70, $8DD1, $8F93,
                $8F6C, $8FC7, $9001, $9009, $95EE, $5199, $5C06, $964D,
                $9610, $9996, $6572, $653B, $878D, $642C, $4FEE, $963B,
                $8054, $88AB, $5FD0, $76B1, $8D62, $9501, $5229, $7A33,
                $8131, $6563, $5BA3, $5E94, $79F0, $8EBA, $70B9, $8FC5,
                $9010, $6DF1, $8DF3, $63E1, $6D12, $8FFD:
                    Result := True;
            end;
        end;

        function is_display_de_strong_adverbial_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $5B8C, $7EE7, $80E1, $6563, $8131, $94BB:
                    Result := True;
            end;
        end;

        function build_replaced_text_local(const units_value: TArray<string>;
            const replacement_idx: Integer; const replacement_text: string): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(units_value) do
            begin
                if local_idx = replacement_idx then
                begin
                    Result := Result + replacement_text;
                end
                else
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;
        end;

        function right_context_has_long_exact_phrase_local(
            const start_idx_value: Integer): Boolean;
        var
            span_len_local: Integer;
            local_idx: Integer;
            query_key_local: string;
            text_local: string;

            function exact_units_local(const exact_start_idx: Integer;
                const exact_span_len: Integer): Boolean;
            var
                exact_idx: Integer;
                exact_key_local: string;
                exact_text_local: string;
            begin
                Result := False;
                if (exact_start_idx < 0) or (exact_span_len <= 1) or
                    (exact_start_idx + exact_span_len > expected_units) then
                begin
                    Exit;
                end;
                exact_key_local := build_display_query_key(exact_start_idx,
                    exact_span_len);
                exact_text_local := '';
                for exact_idx := exact_start_idx to
                    exact_start_idx + exact_span_len - 1 do
                begin
                    exact_text_local := exact_text_local +
                        candidate_units_local[exact_idx];
                end;
                Result := (exact_key_local <> '') and
                    (exact_text_local <> '') and
                    display_exact_key_has_text(exact_key_local,
                    exact_text_local);
            end;
        begin
            Result := False;
            if (start_idx_value < 0) or
                (start_idx_value >= expected_units) then
            begin
                Exit;
            end;

            if exact_units_local(start_idx_value, 2) and
                exact_units_local(start_idx_value + 2, 2) then
            begin
                Exit(True);
            end;

            for span_len_local := Min(4, expected_units - start_idx_value)
                downto 3 do
            begin
                query_key_local := build_display_query_key(start_idx_value,
                    span_len_local);
                text_local := '';
                for local_idx := start_idx_value to
                    start_idx_value + span_len_local - 1 do
                begin
                    text_local := text_local + candidate_units_local[local_idx];
                end;
                if (query_key_local <> '') and (text_local <> '') and
                    display_exact_key_has_text(query_key_local, text_local) then
                begin
                    Exit(True);
                end;
            end;
        end;

    begin
        if (Length(m_candidates) = 0) or (expected_units < 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_rank_local := Low(Integer);
        best_replacement_text_local := '';
        best_replacement_candidate_local := Default(TncCandidate);

        for idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx].text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Continue;
            end;

            for unit_idx := 1 to High(candidate_units_local) - 1 do
            begin
                if (normalize_pinyin_text(syllables[unit_idx].text) = 'de') and
                    SameText(candidate_units_local[unit_idx], string(Char($7684))) and
                    is_display_de_adverbial_left_unit_local(
                    candidate_units_local[unit_idx - 1]) and
                    is_display_de_adverbial_predicate_head_unit_local(
                    candidate_units_local[unit_idx + 1]) and
                    ((not right_context_has_long_exact_phrase_local(unit_idx + 1)) or
                    is_display_de_strong_adverbial_predicate_head_unit_local(
                    candidate_units_local[unit_idx + 1])) then
                begin
                    replacement_text_local := build_replaced_text_local(
                        candidate_units_local, unit_idx, string(Char($5730)));
                    candidate_rank_local := m_candidates[idx].score;
                    if (idx < Length(m_candidate_segment_paths)) and
                        (Trim(m_candidate_segment_paths[idx]) <> '') then
                    begin
                        Inc(candidate_rank_local, 80000);
                    end;
                    Dec(candidate_rank_local, idx * 256);
                    if candidate_rank_local > best_rank_local then
                    begin
                        best_rank_local := candidate_rank_local;
                        best_replacement_text_local := replacement_text_local;
                        best_replacement_candidate_local := m_candidates[idx];
                        best_replacement_candidate_local.text :=
                            replacement_text_local;
                        best_replacement_candidate_local.comment := '';
                        Inc(best_replacement_candidate_local.score, 42000);
                        best_replacement_candidate_local.has_dict_weight := False;
                        best_replacement_candidate_local.dict_weight := 0;
                    end;
                end;
            end;
        end;

        if best_replacement_text_local = '' then
        begin
            Exit;
        end;

        existing_idx_local := find_display_candidate(best_replacement_text_local,
            '');
        if existing_idx_local < 0 then
        begin
            replacement_candidate_local := best_replacement_candidate_local;
            insert_display_candidate(replacement_candidate_local,
                best_replacement_text_local, 0);
            existing_idx_local := find_display_candidate(best_replacement_text_local,
                '');
        end;
        if existing_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(existing_idx_local, 0);
        end;
    end;

    procedure promote_display_nominal_de_particle_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        unit_idx: Integer;
        existing_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        candidate_units_local: TArray<string>;
        replacement_candidate_local: TncCandidate;
        replacement_text_local: string;
        best_replacement_text_local: string;
        best_replacement_candidate_local: TncCandidate;

        function unit_matches_local(const unit_value: string;
            const code_value: Word): Boolean;
        begin
            Result := (Length(unit_value) = 1) and
                (Ord(unit_value[1]) = code_value);
        end;

        function unit_pair_matches_local(const units_value: TArray<string>;
            const first_idx: Integer; const first_code: Word;
            const second_code: Word): Boolean;
        begin
            Result := (first_idx >= 0) and
                (first_idx + 1 <= High(units_value)) and
                unit_matches_local(units_value[first_idx], first_code) and
                unit_matches_local(units_value[first_idx + 1], second_code);
        end;

        function is_display_nominal_de_left_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $6027, $7EED, $5927, $5B9A, $6548, $5165, $529F, $6709,
                $63A5, $7136:
                    Result := True;
            end;
        end;

        function has_display_nominal_de_suffix_local(
            const units_value: TArray<string>; const de_idx: Integer): Boolean;
        begin
            Result := False;
            if (de_idx <= 0) or (de_idx + 1 > High(units_value)) or
                (not is_display_nominal_de_left_unit_local(
                units_value[de_idx - 1])) then
            begin
                Exit;
            end;

            Result :=
                unit_pair_matches_local(units_value, de_idx + 1, $63D0, $95EE) or
                unit_pair_matches_local(units_value, de_idx + 1, $76F4, $64AD) or
                unit_pair_matches_local(units_value, de_idx + 1, $7ADE, $4E89) or
                unit_pair_matches_local(units_value, de_idx + 1, $6253, $51FB) or
                unit_pair_matches_local(units_value, de_idx + 1, $5E2E, $52A9) or
                unit_pair_matches_local(units_value, de_idx + 1, $6210, $679C) or
                unit_pair_matches_local(units_value, de_idx + 1, $5F00, $53D1) or
                unit_pair_matches_local(units_value, de_idx + 1, $653B, $51FB) or
                unit_pair_matches_local(units_value, de_idx + 1, $5206, $6790) or
                unit_pair_matches_local(units_value, de_idx + 1, $63A2, $7D22) or
                unit_pair_matches_local(units_value, de_idx + 1, $8BDD, $9898) or
                unit_pair_matches_local(units_value, de_idx + 1, $80FD, $529B) or
                unit_pair_matches_local(units_value, de_idx + 1, $89C6, $9891) or
                unit_pair_matches_local(units_value, de_idx + 1, $98CE, $683C) or
                unit_pair_matches_local(units_value, de_idx + 1, $767D, $70ED);
        end;

        function build_replaced_text_local(const units_value: TArray<string>;
            const replacement_idx: Integer; const replacement_text: string): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(units_value) do
            begin
                if local_idx = replacement_idx then
                begin
                    Result := Result + replacement_text;
                end
                else
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;
        end;

    begin
        if (Length(m_candidates) = 0) or (expected_units < 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_rank_local := Low(Integer);
        best_replacement_text_local := '';
        best_replacement_candidate_local := Default(TncCandidate);

        for idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx].text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Continue;
            end;

            for unit_idx := 1 to High(candidate_units_local) - 1 do
            begin
                if (normalize_pinyin_text(syllables[unit_idx].text) = 'de') and
                    (SameText(candidate_units_local[unit_idx],
                    string(Char($5730))) or SameText(candidate_units_local[
                    unit_idx], string(Char($5F97)))) and
                    (has_display_nominal_de_suffix_local(candidate_units_local,
                    unit_idx)) then
                begin
                    replacement_text_local := build_replaced_text_local(
                        candidate_units_local, unit_idx, string(Char($7684)));
                    candidate_rank_local := m_candidates[idx].score;
                    if (idx < Length(m_candidate_segment_paths)) and
                        (Trim(m_candidate_segment_paths[idx]) <> '') then
                    begin
                        Inc(candidate_rank_local, 50000);
                    end;
                    Dec(candidate_rank_local, idx * 256);
                    if candidate_rank_local > best_rank_local then
                    begin
                        best_rank_local := candidate_rank_local;
                        best_replacement_text_local := replacement_text_local;
                        best_replacement_candidate_local := m_candidates[idx];
                        best_replacement_candidate_local.text :=
                            replacement_text_local;
                        best_replacement_candidate_local.comment := '';
                        Inc(best_replacement_candidate_local.score, 36000);
                        best_replacement_candidate_local.has_dict_weight := False;
                        best_replacement_candidate_local.dict_weight := 0;
                    end;
                end;
            end;
        end;

        if best_replacement_text_local = '' then
        begin
            Exit;
        end;

        existing_idx_local := find_display_candidate(best_replacement_text_local,
            '');
        if existing_idx_local < 0 then
        begin
            replacement_candidate_local := best_replacement_candidate_local;
            insert_display_candidate(replacement_candidate_local,
                best_replacement_text_local, 0);
            existing_idx_local := find_display_candidate(best_replacement_text_local,
                '');
        end;
        if existing_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(existing_idx_local, 0);
        end;
    end;

    procedure promote_display_modal_yu_particle_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        unit_idx: Integer;
        existing_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        candidate_units_local: TArray<string>;
        replacement_candidate_local: TncCandidate;
        replacement_text_local: string;
        best_replacement_text_local: string;
        best_replacement_candidate_local: TncCandidate;

        function should_replace_display_yu_with_conjunction_local(
            const units_value: TArray<string>; const yu_idx: Integer): Boolean;
        begin
            Result := False;
            if yu_idx <= 0 then
            begin
                Exit;
            end;

            if SameText(units_value[yu_idx - 1], string(Char($5426))) then
            begin
                Exit(True);
            end;

            Result := (yu_idx >= 2) and
                SameText(units_value[yu_idx - 2], string(Char($901A))) and
                SameText(units_value[yu_idx - 1], string(Char($8FC7)));
        end;

        function build_replaced_text_local(const units_value: TArray<string>;
            const replacement_idx: Integer; const replacement_text: string): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(units_value) do
            begin
                if local_idx = replacement_idx then
                begin
                    Result := Result + replacement_text;
                end
                else
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;
        end;
    begin
        if (Length(m_candidates) = 0) or (expected_units < 3) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_rank_local := Low(Integer);
        best_replacement_text_local := '';
        best_replacement_candidate_local := Default(TncCandidate);

        for idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx].text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Continue;
            end;

            for unit_idx := 1 to High(candidate_units_local) do
            begin
                if (normalize_pinyin_text(syllables[unit_idx].text) = 'yu') and
                    SameText(candidate_units_local[unit_idx], string(Char($4E8E))) and
                    should_replace_display_yu_with_conjunction_local(
                    candidate_units_local, unit_idx) then
                begin
                    replacement_text_local := build_replaced_text_local(
                        candidate_units_local, unit_idx, string(Char($4E0E)));
                    candidate_rank_local := m_candidates[idx].score;
                    if (idx < Length(m_candidate_segment_paths)) and
                        (Trim(m_candidate_segment_paths[idx]) <> '') then
                    begin
                        Inc(candidate_rank_local, 60000);
                    end;
                    Dec(candidate_rank_local, idx * 256);
                    if candidate_rank_local > best_rank_local then
                    begin
                        best_rank_local := candidate_rank_local;
                        best_replacement_text_local := replacement_text_local;
                        best_replacement_candidate_local := m_candidates[idx];
                        best_replacement_candidate_local.text :=
                            replacement_text_local;
                        best_replacement_candidate_local.comment := '';
                        Inc(best_replacement_candidate_local.score, 32000);
                        best_replacement_candidate_local.has_dict_weight := False;
                        best_replacement_candidate_local.dict_weight := 0;
                    end;
                end;
            end;
        end;

        if best_replacement_text_local = '' then
        begin
            Exit;
        end;

        existing_idx_local := find_display_candidate(best_replacement_text_local,
            '');
        if existing_idx_local < 0 then
        begin
            replacement_candidate_local := best_replacement_candidate_local;
            insert_display_candidate(replacement_candidate_local,
                best_replacement_text_local, 0);
            existing_idx_local := find_display_candidate(best_replacement_text_local,
                '');
        end;
        if existing_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(existing_idx_local, 0);
        end;
    end;

    procedure promote_display_shangwei_negative_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        unit_idx: Integer;
        existing_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        candidate_units_local: TArray<string>;
        replacement_candidate_local: TncCandidate;
        replacement_text_local: string;
        best_replacement_text_local: string;
        best_replacement_candidate_local: TncCandidate;

        function is_negative_following_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $5230, $80FD, $4F1A, $8981, $5C06, $53EF, $5F97, $51FA,
                $5F00, $5B8C, $8D77, $6210, $6709:
                    Result := True;
            end;
        end;

        function build_replaced_text_local(const units_value: TArray<string>;
            const replacement_idx: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(units_value) do
            begin
                if local_idx = replacement_idx then
                begin
                    Result := Result + string(Char($5C1A)) + string(Char($672A));
                end
                else if local_idx = replacement_idx + 1 then
                begin
                    Continue;
                end
                else
                begin
                    Result := Result + units_value[local_idx];
                end;
            end;
        end;
    begin
        if (Length(m_candidates) = 0) or (expected_units < 5) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        best_rank_local := Low(Integer);
        best_replacement_text_local := '';
        best_replacement_candidate_local := Default(TncCandidate);

        for idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx].comment) <> '' then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx].text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Continue;
            end;

            for unit_idx := 0 to High(candidate_units_local) - 2 do
            begin
                if (normalize_pinyin_text(syllables[unit_idx].text) = 'shang') and
                    (normalize_pinyin_text(syllables[unit_idx + 1].text) = 'wei') and
                    SameText(candidate_units_local[unit_idx], string(Char($4E0A))) and
                    SameText(candidate_units_local[unit_idx + 1],
                    string(Char($4F4D))) and
                    is_negative_following_unit_local(
                    candidate_units_local[unit_idx + 2]) then
                begin
                    replacement_text_local := build_replaced_text_local(
                        candidate_units_local, unit_idx);
                    candidate_rank_local := m_candidates[idx].score;
                    if (idx < Length(m_candidate_segment_paths)) and
                        (Trim(m_candidate_segment_paths[idx]) <> '') then
                    begin
                        Inc(candidate_rank_local, 50000);
                    end;
                    Dec(candidate_rank_local, idx * 256);
                    if candidate_rank_local > best_rank_local then
                    begin
                        best_rank_local := candidate_rank_local;
                        best_replacement_text_local := replacement_text_local;
                        best_replacement_candidate_local := m_candidates[idx];
                        best_replacement_candidate_local.text :=
                            replacement_text_local;
                        best_replacement_candidate_local.comment := '';
                        Inc(best_replacement_candidate_local.score, 32000);
                        best_replacement_candidate_local.has_dict_weight := False;
                        best_replacement_candidate_local.dict_weight := 0;
                    end;
                end;
            end;
        end;

        if best_replacement_text_local = '' then
        begin
            Exit;
        end;

        existing_idx_local := find_display_candidate(best_replacement_text_local,
            '');
        if existing_idx_local < 0 then
        begin
            replacement_candidate_local := best_replacement_candidate_local;
            insert_display_candidate(replacement_candidate_local,
                best_replacement_text_local, 0);
            existing_idx_local := find_display_candidate(best_replacement_text_local,
                '');
        end;
        if existing_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(existing_idx_local, 0);
        end;
    end;

    procedure promote_display_common_function_context_candidate;
    const
        c_probe_limit = 12;
        c_max_diff_units = 3;
    var
        idx: Integer;
        diff_count_local: Integer;
        top_units_local: TArray<string>;
        candidate_units_local: TArray<string>;

        function unit_matches_local(const unit_value: string;
            const code_value: Word): Boolean;
        begin
            Result := (Length(unit_value) = 1) and
                (Ord(unit_value[1]) = code_value);
        end;

        function is_zai_nominal_head_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4EBA, $4EEC, $4F60, $4ED6, $5979, $5B83, $5176, $8FD9,
                $90A3, $6B64, $5404, $6BCF, $5927, $5C0F, $4F18, $6797,
                $73B0, $7F51, $6570, $7535, $516C, $5BB6, $5B66, $57CE,
                $5730, $573A, $4E2D, $4E0A, $4E0B, $524D, $540E, $91CC,
                $5916, $5185, $4E1C, $897F, $5357, $5317:
                    Result := True;
            end;
        end;

        function is_zai_context_tail_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4E2D, $91CC, $4E0A, $524D, $540E, $65F6,
                $95F4, $671F, $9645, $5904, $5730, $65C1, $9762:
                    Result := True;
            end;
        end;

        function is_zai_context_trigger_local(const zai_idx_value: Integer): Boolean;
        const
            c_max_right_probe_units = 8;
        var
            probe_len_local: Integer;
            probe_idx_local: Integer;
        begin
            Result := False;
            if (zai_idx_value < 0) or
                (zai_idx_value + 1 > High(candidate_units_local)) then
            begin
                Exit;
            end;

            if is_zai_nominal_head_local(candidate_units_local[zai_idx_value + 1]) then
            begin
                Exit(True);
            end;

            for probe_len_local := 1 to c_max_right_probe_units do
            begin
                probe_idx_local := zai_idx_value + probe_len_local;
                if probe_idx_local > High(candidate_units_local) then
                begin
                    Break;
                end;
                if is_zai_context_tail_unit_local(
                    candidate_units_local[probe_idx_local]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_adverbial_di_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $505A, $51FA, $5230, $53BB, $53D1, $53D8, $53D6, $542C,
                $56DE, $5B66, $5F00, $60F3, $6210, $6253, $627E, $6301,
                $63A5, $63D0, $6539, $653E, $6765, $7528, $770B, $7740,
                $7814, $79BB, $7EE7, $7ED9, $80E1, $89C1, $8BA9,
                $8BB2, $8BB0, $8BF4, $8D70, $8DD1, $8F93, $8F6C, $8FC7,
                $9001, $9009, $95EE, $5199, $5C06, $964D, $653B, $878D,
                $642C, $4FEE, $8054, $88AB, $5FD0, $5229, $8EBA, $70B9,
                $8FFD, $5B8C, $5347:
                    Result := True;
            end;
        end;

        function candidate_has_adverbial_di_context_local(
            const di_idx_value: Integer): Boolean;
        begin
            Result := (di_idx_value >= 1) and
                (di_idx_value + 1 <= High(candidate_units_local)) and
                is_adverbial_di_predicate_head_unit_local(
                candidate_units_local[di_idx_value + 1]);
        end;

        function is_de_nominal_suffix_unit_local(const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4EBA, $6211, $8005, $5B50, $6CD5, $679C, $53F7, $7D22,
                $5883, $529B, $606F, $5FC6, $89C9, $5947, $578B, $7ECF,
                $65F6, $6210, $529F, $5C55, $503C, $7F6E, $6CE8, $5149,
                $9898, $6848, $70B9, $9762, $8DEF, $7EBF, $5C42, $7EA7,
                $7C7B, $6027, $6587, $4EF6, $7801, $5E93, $5668, $7A0B,
                $5E8F, $7CFB, $7EDF, $5DE5, $4F5C, $684C, $5C4B:
                    Result := True;
            end;
        end;

        function candidate_has_nominal_de_context_local(
            const de_idx_value: Integer): Boolean;
        const
            c_max_right_probe_units = 4;
        var
            probe_len_local: Integer;
            suffix_idx_local: Integer;
        begin
            Result := False;
            if (de_idx_value < 0) or
                (de_idx_value + 1 > High(candidate_units_local)) then
            begin
                Exit;
            end;
            if unit_matches_local(candidate_units_local[de_idx_value + 1],
                $70B9) then
            begin
                Exit;
            end;
            if is_adverbial_di_predicate_head_unit_local(
                candidate_units_local[de_idx_value + 1]) then
            begin
                Exit;
            end;

            for probe_len_local := 1 to c_max_right_probe_units do
            begin
                suffix_idx_local := de_idx_value + probe_len_local;
                if suffix_idx_local > High(candidate_units_local) then
                begin
                    Break;
                end;
                if is_de_nominal_suffix_unit_local(
                    candidate_units_local[suffix_idx_local]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function is_result_complement_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4E0D, $4E86, $5230, $51FA, $597D, $5B8C, $5F00, $5F88,
                $5FEB, $6162, $660E, $6709, $6E05, $719F, $767D, $76F4,
                $771F, $7A33, $7EA2, $8001, $8D77, $8FDC, $8FD1, $900F,
                $9519, $9AD8:
                    Result := True;
            end;
        end;

        function candidate_has_complement_de_context_local(
            const de_idx_value: Integer): Boolean;
        begin
            Result := (de_idx_value >= 1) and
                (de_idx_value + 1 <= High(candidate_units_local)) and
                is_adverbial_di_predicate_head_unit_local(
                candidate_units_local[de_idx_value - 1]) and
                is_result_complement_head_unit_local(
                candidate_units_local[de_idx_value + 1]);
        end;

        function candidate_has_supported_function_context_local: Boolean;
        var
            local_idx: Integer;
            local_supported: Boolean;
        begin
            local_supported := False;
            diff_count_local := 0;
            for local_idx := 0 to expected_units - 1 do
            begin
                if SameText(top_units_local[local_idx],
                    candidate_units_local[local_idx]) then
                begin
                    Continue;
                end;

                Inc(diff_count_local);
                if diff_count_local > c_max_diff_units then
                begin
                    Exit(False);
                end;

                if unit_matches_local(candidate_units_local[local_idx], $7684) and
                    (unit_matches_local(top_units_local[local_idx], $5730) or
                    unit_matches_local(top_units_local[local_idx], $5F97)) and
                    candidate_has_nominal_de_context_local(local_idx) then
                begin
                    local_supported := True;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[local_idx], $5F97) and
                    (unit_matches_local(top_units_local[local_idx], $7684) or
                    unit_matches_local(top_units_local[local_idx], $5730)) and
                    candidate_has_complement_de_context_local(local_idx) then
                begin
                    local_supported := True;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[local_idx], $5728) and
                    unit_matches_local(top_units_local[local_idx], $518D) and
                    is_zai_context_trigger_local(local_idx) then
                begin
                    local_supported := True;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[local_idx], $5730) and
                    (unit_matches_local(top_units_local[local_idx], $7684) or
                    unit_matches_local(top_units_local[local_idx], $5F97)) and
                    candidate_has_adverbial_di_context_local(local_idx) then
                begin
                    local_supported := True;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[local_idx], $662F) and
                    (unit_matches_local(top_units_local[local_idx], $89C6) or
                    unit_matches_local(top_units_local[local_idx], $5B9E)) and
                    (local_idx + 1 <= High(candidate_units_local)) and
                    unit_matches_local(candidate_units_local[local_idx + 1],
                    $4E3A) then
                begin
                    local_supported := True;
                    Continue;
                end;

                Exit(False);
            end;

            Result := local_supported;
        end;

    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 6) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;

        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx].text));
            if (Length(candidate_units_local) = expected_units) and
                candidate_has_supported_function_context_local then
            begin
                force_move_display_candidate_to_index(idx, 0);
                Exit;
            end;
        end;
    end;

    procedure promote_display_visible_complete_candidate_by_context_quality;
    const
        c_probe_limit = 12;
        c_min_quality_gain = 5000;
        c_max_diff_units = 6;
    var
        top_units_local: TArray<string>;
        best_idx_local: Integer;
        best_quality_local: Integer;
        top_quality_local: Integer;
        idx: Integer;
        candidate_quality_local: Integer;
        top_exact_quality_local: Integer;
        top2_exact_quality_local: Integer;
        top_exact_covered_units_local: Integer;
        top2_exact_covered_units_local: Integer;
        top_path_support_local: Integer;
        top2_path_support_local: Integer;
        top_path_segments_local: Integer;
        top2_path_segments_local: Integer;
        promoted_local: Boolean;

        function unit_matches_local(const unit_value: string;
            const code_value: Word): Boolean;
        begin
            Result := (Length(unit_value) = 1) and
                (Ord(unit_value[1]) = code_value);
        end;

        function units_equal_local(const units_a: TArray<string>;
            const units_b: TArray<string>; const unit_idx: Integer): Boolean;
        begin
            Result := (unit_idx >= 0) and (unit_idx < Length(units_a)) and
                (unit_idx < Length(units_b)) and
                SameText(units_a[unit_idx], units_b[unit_idx]);
        end;

        function is_likely_predicate_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $505A, $51FA, $5230, $53BB, $53D1, $53D8, $53D6, $542C,
                $56DE, $5B66, $5F00, $60F3, $6210, $6253, $627E, $6301,
                $63A5, $63D0, $6539, $653E, $6765, $7528, $770B, $7740,
                $7814, $79BB, $7EE7, $7ED9, $80E1, $89C1, $8BA9,
                $8BB2, $8BB0, $8BF4, $8D70, $8DD1, $8F93, $8F6C, $8FC7,
                $9001, $9009, $95EE, $5199, $5C06, $964D, $653B, $878D,
                $642C, $4FEE, $8054, $88AB, $5FD0, $5229, $8EBA, $70B9,
                $8FFD, $5B8C, $5347, $7ACB, $5207, $65AD, $8FDE, $7701:
                    Result := True;
            end;
        end;

        function is_likely_nominal_head_unit_local(
            const unit_value: string): Boolean;
        begin
            Result := False;
            if Length(unit_value) <> 1 then
            begin
                Exit;
            end;

            case Ord(unit_value[1]) of
                $4EBA, $4EEC, $6211, $4F60, $4ED6, $5979, $5B83, $5176,
                $8FD9, $90A3, $6B64, $5404, $6BCF, $5927, $5C0F, $4F18,
                $6797, $6570, $516C, $5BB6, $5B66, $57CE, $5730, $573A,
                $65F6, $95F4, $4E8B, $60C5, $95EE, $9898, $80FD, $529B,
                $4FE1, $606F, $636E, $4EE3, $7801, $6A21, $578B,
                $7CFB, $7EDF, $9879, $76EE, $77E5, $8BC6, $673A, $5668,
                $5DE5, $7A0B, $5E08, $90AE, $4EF6, $8BED, $8A00, $79D1,
                $6280, $98CE, $9669, $8D44, $6E90, $5E8F, $5E93:
                    Result := True;
            end;
        end;

        function is_zai_context_unit_local(const unit_value: string): Boolean;
        begin
            Result := is_likely_nominal_head_unit_local(unit_value) or
                unit_matches_local(unit_value, $4E2D) or
                unit_matches_local(unit_value, $91CC) or
                unit_matches_local(unit_value, $4E0A) or
                unit_matches_local(unit_value, $4E0B) or
                unit_matches_local(unit_value, $524D) or
                unit_matches_local(unit_value, $540E);
        end;

        function candidate_is_complete_local(const candidate_value: TncCandidate): Boolean;
        begin
            Result := (Trim(candidate_value.comment) = '') and
                (get_candidate_text_unit_count(Trim(candidate_value.text)) =
                expected_units);
        end;

        function build_units_range_text_local(const units_value: TArray<string>;
            const start_idx: Integer; const span_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx < 0) or (span_len <= 0) or
                (start_idx + span_len > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + span_len - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function score_exact_span_coverage_local(
            const units_value: TArray<string>): Integer;
        const
            c_max_span_len = 4;
        var
            occupied_local: TArray<Boolean>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            span_text_local: string;
            exact_weight_local: Integer;
            occupied_range_local: Boolean;
        begin
            Result := 0;
            if Length(units_value) <> expected_units then
            begin
                Exit;
            end;

            SetLength(occupied_local, expected_units);
            for span_len_local := Min(c_max_span_len, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    occupied_range_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if occupied_local[unit_idx_local] then
                        begin
                            occupied_range_local := True;
                            Break;
                        end;
                    end;
                    if occupied_range_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    span_text_local := build_units_range_text_local(units_value,
                        start_idx_local, span_len_local);
                    if (query_key_local = '') or (span_text_local = '') or
                        (not display_exact_key_has_text(query_key_local,
                        span_text_local)) then
                    begin
                        Continue;
                    end;

                    exact_weight_local := 0;
                    get_display_exact_text_weight(query_key_local, span_text_local,
                        span_len_local, exact_weight_local);
                    Inc(Result, span_len_local * span_len_local * 3000 +
                        Min(7000, Max(0, exact_weight_local) * 5));
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_local[unit_idx_local] := True;
                    end;
                end;
            end;
        end;

        function range_has_diff_local(const candidate_units_local:
            TArray<string>; const start_idx: Integer; const span_len: Integer):
            Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (span_len <= 0) or
                (start_idx + span_len > expected_units) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + span_len - 1 do
            begin
                if not units_equal_local(top_units_local, candidate_units_local,
                    local_idx) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function unit_is_context_function_diff_local(
            const candidate_unit_value: string; const top_unit_value: string):
            Boolean;

            function unit_is_common_function_unit_local(
                const unit_value: string): Boolean;
            begin
                Result := unit_matches_local(unit_value, $7684) or
                    unit_matches_local(unit_value, $5730) or
                    unit_matches_local(unit_value, $5F97) or
                    unit_matches_local(unit_value, $5728) or
                    unit_matches_local(unit_value, $518D) or
                    unit_matches_local(unit_value, $4E86) or
                    unit_matches_local(unit_value, $7740) or
                    unit_matches_local(unit_value, $8FC7) or
                    unit_matches_local(unit_value, $662F) or
                    unit_matches_local(unit_value, $548C) or
                    unit_matches_local(unit_value, $4E0E) or
                    unit_matches_local(unit_value, $4E8E) or
                    unit_matches_local(unit_value, $5BF9) or
                    unit_matches_local(unit_value, $6211) or
                    unit_matches_local(unit_value, $4F60) or
                    unit_matches_local(unit_value, $4ED6) or
                    unit_matches_local(unit_value, $5979) or
                    unit_matches_local(unit_value, $5B83) or
                    unit_matches_local(unit_value, $4E0D) or
                    unit_matches_local(unit_value, $5C31) or
                    unit_matches_local(unit_value, $4E5F) or
                    unit_matches_local(unit_value, $90FD) or
                    unit_matches_local(unit_value, $628A) or
                    unit_matches_local(unit_value, $88AB) or
                    unit_matches_local(unit_value, $4E3A) or
                    unit_matches_local(unit_value, $4EE5) or
                    unit_matches_local(unit_value, $4E00) or
                    unit_matches_local(unit_value, $624D) or
                    unit_matches_local(unit_value, $8FD9);
            end;
        begin
            Result := unit_is_common_function_unit_local(candidate_unit_value) or
                unit_is_common_function_unit_local(top_unit_value);
        end;

        function range_has_context_function_diff_local(
            const candidate_units_local: TArray<string>; const start_idx:
            Integer; const span_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (start_idx < 0) or (span_len <= 0) or
                (start_idx + span_len > expected_units) then
            begin
                Exit;
            end;

            for local_idx := start_idx to start_idx + span_len - 1 do
            begin
                if (not units_equal_local(top_units_local, candidate_units_local,
                    local_idx)) and unit_is_context_function_diff_local(
                    candidate_units_local[local_idx],
                    top_units_local[local_idx]) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function get_exact_span_support_local(const units_value: TArray<string>;
            const start_idx: Integer; const span_len: Integer;
            out exact_weight: Integer): Boolean;
        var
            query_key_local: string;
            span_text_local: string;
        begin
            Result := False;
            exact_weight := 0;
            query_key_local := build_display_query_key(start_idx, span_len);
            span_text_local := build_units_range_text_local(units_value,
                start_idx, span_len);
            if (query_key_local = '') or (span_text_local = '') or
                (not display_exact_key_has_text(query_key_local,
                span_text_local)) then
            begin
                Exit;
            end;

            get_display_exact_text_weight(query_key_local, span_text_local,
                span_len, exact_weight);
            Result := True;
        end;

        function score_diff_local_exact_support_local(
            const candidate_units_local: TArray<string>): Integer;
        const
            c_max_span_len = 5;
            c_min_span_len = 3;
            c_score_cap = 36000;
            c_two_char_score_cap = 24000;
        var
            span_len_local: Integer;
            start_idx_local: Integer;
            candidate_weight_local: Integer;
            top_weight_for_span_local: Integer;
            candidate_exact_local: Boolean;
            top_exact_local: Boolean;
            weight_delta_local: Integer;
            two_char_candidate_only_count_local: Integer;
            two_char_candidate_only_score_local: Integer;

            function candidate_span_replaces_stronger_top_two_char_local(
                const start_idx_value: Integer; const span_len_value: Integer): Boolean;
            var
                pair_start_local: Integer;
                candidate_pair_weight_local: Integer;
                top_pair_weight_local: Integer;
                candidate_pair_exact_local: Boolean;
                top_pair_exact_local: Boolean;
            begin
                Result := False;
                if span_len_value <= 2 then
                begin
                    Exit;
                end;

                for pair_start_local := start_idx_value to
                    start_idx_value + span_len_value - 2 do
                begin
                    if (not range_has_diff_local(candidate_units_local,
                        pair_start_local, 2)) then
                    begin
                        Continue;
                    end;

                    candidate_pair_exact_local := get_exact_span_support_local(
                        candidate_units_local, pair_start_local, 2,
                        candidate_pair_weight_local);
                    top_pair_exact_local := get_exact_span_support_local(
                        top_units_local, pair_start_local, 2,
                        top_pair_weight_local);
                    if candidate_pair_exact_local and top_pair_exact_local and
                        (top_pair_weight_local >= candidate_pair_weight_local + 500) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := 0;
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            for span_len_local := Min(c_max_span_len, expected_units) downto
                c_min_span_len do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    if not range_has_diff_local(candidate_units_local,
                        start_idx_local, span_len_local) then
                    begin
                        Continue;
                    end;
                    if range_has_context_function_diff_local(
                        candidate_units_local, start_idx_local,
                        span_len_local) then
                    begin
                        Continue;
                    end;

                    candidate_exact_local := get_exact_span_support_local(
                        candidate_units_local, start_idx_local, span_len_local,
                        candidate_weight_local);
                    if not candidate_exact_local then
                    begin
                        Continue;
                    end;

                    top_exact_local := get_exact_span_support_local(
                        top_units_local, start_idx_local, span_len_local,
                        top_weight_for_span_local);
                    if not top_exact_local then
                    begin
                        if not candidate_span_replaces_stronger_top_two_char_local(
                            start_idx_local, span_len_local) then
                        begin
                            Inc(Result, span_len_local * span_len_local * 4200 +
                                Min(9000, Max(0, candidate_weight_local) * 4));
                        end;
                    end
                    else
                    begin
                        weight_delta_local := candidate_weight_local -
                            top_weight_for_span_local;
                        if weight_delta_local >= 2000 then
                        begin
                            Inc(Result, Min(6000, weight_delta_local * 2));
                        end;
                    end;

                    if Result >= c_score_cap then
                    begin
                        Exit(c_score_cap);
                    end;
                end;
            end;

            two_char_candidate_only_count_local := 0;
            two_char_candidate_only_score_local := 0;
            for start_idx_local := 0 to expected_units - 2 do
            begin
                if (not range_has_diff_local(candidate_units_local,
                    start_idx_local, 2)) or
                    range_has_context_function_diff_local(candidate_units_local,
                    start_idx_local, 2) then
                begin
                    Continue;
                end;

                candidate_exact_local := get_exact_span_support_local(
                    candidate_units_local, start_idx_local, 2,
                    candidate_weight_local);
                if not candidate_exact_local then
                begin
                    Continue;
                end;

                top_exact_local := get_exact_span_support_local(top_units_local,
                    start_idx_local, 2, top_weight_for_span_local);
                if not top_exact_local then
                begin
                    Inc(two_char_candidate_only_count_local);
                    Inc(two_char_candidate_only_score_local, 9000 +
                        Min(5000, Max(0, candidate_weight_local) * 3));
                end;
            end;

            if two_char_candidate_only_count_local >= 999 then
            begin
                Inc(Result, Min(c_two_char_score_cap,
                    two_char_candidate_only_score_local));
            end;

            if Result > c_score_cap then
            begin
                Result := c_score_cap;
            end;
        end;

        function score_context_preference_local(const candidate_units_local:
            TArray<string>): Integer;
        var
            unit_idx: Integer;
            diff_count_local: Integer;
            prev_unit_local: string;
            next_unit_local: string;

            function previous_unit_matches_local(const code_value: Word): Boolean;
            begin
                Result := (prev_unit_local <> '') and
                    unit_matches_local(prev_unit_local, code_value);
            end;

            function next_unit_matches_local(const code_value: Word): Boolean;
            begin
                Result := (next_unit_local <> '') and
                    unit_matches_local(next_unit_local, code_value);
            end;

            function previous_top_unit_matches_local(const code_value: Word): Boolean;
            begin
                Result := (unit_idx > 0) and
                    unit_matches_local(top_units_local[unit_idx - 1], code_value);
            end;

            function next_top_unit_matches_local(const code_value: Word): Boolean;
            begin
                Result := (unit_idx + 1 < expected_units) and
                    unit_matches_local(top_units_local[unit_idx + 1], code_value);
            end;

            function candidate_pair_replaces_top_pair_local(
                const candidate_first: Word; const candidate_second: Word;
                const top_first: Word; const top_second: Word): Boolean;
            begin
                Result := False;
                if unit_matches_local(candidate_units_local[unit_idx],
                    candidate_first) and unit_matches_local(
                    top_units_local[unit_idx], top_first) and
                    next_unit_matches_local(candidate_second) and
                    next_top_unit_matches_local(top_second) then
                begin
                    Exit(True);
                end;

                if unit_matches_local(candidate_units_local[unit_idx],
                    candidate_second) and unit_matches_local(
                    top_units_local[unit_idx], top_second) and
                    previous_unit_matches_local(candidate_first) and
                    previous_top_unit_matches_local(top_first) then
                begin
                    Exit(True);
                end;
            end;

            function following_units_form_exact_phrase_local(
                const start_idx_value: Integer; const unit_count_value: Integer): Boolean;
            var
                local_idx: Integer;
                query_key_local: string;
                text_local: string;
            begin
                Result := False;
                if (start_idx_value < 0) or (unit_count_value <= 1) or
                    (start_idx_value + unit_count_value > expected_units) then
                begin
                    Exit;
                end;

                query_key_local := build_display_query_key(start_idx_value,
                    unit_count_value);
                text_local := '';
                for local_idx := start_idx_value to
                    start_idx_value + unit_count_value - 1 do
                begin
                    text_local := text_local + candidate_units_local[local_idx];
                end;
                Result := (query_key_local <> '') and (text_local <> '') and
                    display_exact_key_has_text(query_key_local, text_local);
            end;

            function next_context_is_exact_phrase_local: Boolean;
            begin
                Result := following_units_form_exact_phrase_local(unit_idx + 1, 2) or
                    following_units_form_exact_phrase_local(unit_idx + 1, 3);
            end;

        begin
            Result := 0;
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            diff_count_local := 0;
            for unit_idx := 0 to expected_units - 1 do
            begin
                if units_equal_local(top_units_local, candidate_units_local,
                    unit_idx) then
                begin
                    Continue;
                end;

                Inc(diff_count_local);
                if diff_count_local > c_max_diff_units then
                begin
                    Dec(Result, 32000);
                    Exit;
                end;

                prev_unit_local := '';
                next_unit_local := '';
                if unit_idx > 0 then
                begin
                    prev_unit_local := candidate_units_local[unit_idx - 1];
                end;
                if unit_idx + 1 < expected_units then
                begin
                    next_unit_local := candidate_units_local[unit_idx + 1];
                end;

                if candidate_pair_replaces_top_pair_local($624B, $673A,
                    $6536, $96C6) or
                    candidate_pair_replaces_top_pair_local($6536, $96C6,
                    $53D7, $63A5) or
                    candidate_pair_replaces_top_pair_local($963B, $6B62,
                    $7EC4, $7EC7) or
                    candidate_pair_replaces_top_pair_local($518D, $6B21,
                    $5728, $6B64) or
                    candidate_pair_replaces_top_pair_local($751F, $547D,
                    $58F0, $660E) or
                    candidate_pair_replaces_top_pair_local($663E, $5F97,
                    $95F2, $7684) or
                    candidate_pair_replaces_top_pair_local($7F13, $7F13,
                    $6362, $6362) or
                    candidate_pair_replaces_top_pair_local($5176, $5B9E,
                    $8D77, $59CB) or
                    candidate_pair_replaces_top_pair_local($5408, $4F19,
                    $548C, $6D3B) or
                    candidate_pair_replaces_top_pair_local($540C, $610F,
                    $7EDF, $4E00) or
                    candidate_pair_replaces_top_pair_local($79D1, $6280,
                    $53EF, $53CA) then
                begin
                    Inc(Result, 70000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7684) and
                    (unit_matches_local(top_units_local[unit_idx], $5730) or
                    unit_matches_local(top_units_local[unit_idx], $5F97)) then
                begin
                    if (next_unit_local <> '') and
                        ((not is_likely_predicate_head_unit_local(next_unit_local)) or
                        next_context_is_exact_phrase_local) then
                    begin
                        Inc(Result, 32000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5730) and
                    unit_matches_local(top_units_local[unit_idx], $7684) then
                begin
                    if (next_unit_local <> '') and
                        is_likely_predicate_head_unit_local(next_unit_local) and
                        (not next_context_is_exact_phrase_local) then
                    begin
                        Inc(Result, 30000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5F97) and
                    unit_matches_local(top_units_local[unit_idx], $7684) then
                begin
                    if previous_unit_matches_local($8BB0) then
                    begin
                        Inc(Result, 30000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5728) and
                    unit_matches_local(top_units_local[unit_idx], $518D) then
                begin
                    if (next_unit_local <> '') and
                        (is_zai_context_unit_local(next_unit_local) or
                        next_context_is_exact_phrase_local) then
                    begin
                        Inc(Result, 28000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $518D) and
                    unit_matches_local(top_units_local[unit_idx], $5728) then
                begin
                    if (next_unit_local <> '') and
                        (not is_zai_context_unit_local(next_unit_local)) and
                        (is_likely_predicate_head_unit_local(next_unit_local) or
                        next_unit_matches_local($6B21) or
                        next_unit_matches_local($900F) or
                        next_unit_matches_local($5904)) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E8E) and
                    unit_matches_local(top_units_local[unit_idx], $4E0E) then
                begin
                    if previous_unit_matches_local($5411) or
                        previous_unit_matches_local($503E) then
                    begin
                        Inc(Result, 30000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $548C) and
                    (unit_matches_local(top_units_local[unit_idx], $6838) or
                    unit_matches_local(top_units_local[unit_idx], $5408)) then
                begin
                    if (unit_idx > 0) and (unit_idx + 1 < expected_units) then
                    begin
                        Inc(Result, 32000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7684) and
                    unit_matches_local(top_units_local[unit_idx], $5FB7) then
                begin
                    if (next_unit_local <> '') and
                        (is_likely_nominal_head_unit_local(next_unit_local) or
                        next_context_is_exact_phrase_local) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6211) and
                    (unit_matches_local(top_units_local[unit_idx], $6DA1) or
                    unit_matches_local(top_units_local[unit_idx], $5367) or
                    unit_matches_local(top_units_local[unit_idx], $7A9D)) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F60) and
                    (unit_matches_local(top_units_local[unit_idx], $6CE5) or
                    unit_matches_local(top_units_local[unit_idx], $62DF) or
                    unit_matches_local(top_units_local[unit_idx], $5C3C)) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4ED6) and
                    (unit_matches_local(top_units_local[unit_idx], $8E0F) or
                    unit_matches_local(top_units_local[unit_idx], $5854)) then
                begin
                    Inc(Result, 20000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8FD9) and
                    (unit_matches_local(top_units_local[unit_idx], $7740) or
                    unit_matches_local(top_units_local[unit_idx], $8005)) then
                begin
                    if next_unit_matches_local($4E9B) or
                        next_unit_matches_local($4E2A) or
                        next_unit_matches_local($79CD) or
                        next_unit_matches_local($6837) or
                        next_unit_matches_local($91CC) then
                    begin
                        Inc(Result, 26000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5C31) and
                    (unit_matches_local(top_units_local[unit_idx], $9152) or
                    unit_matches_local(top_units_local[unit_idx], $65E7)) then
                begin
                    if (next_unit_local <> '') and
                        (is_likely_predicate_head_unit_local(next_unit_local) or
                        next_context_is_exact_phrase_local) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $9047) and
                    unit_matches_local(top_units_local[unit_idx], $4E8E) then
                begin
                    if next_unit_matches_local($5230) or
                        next_unit_matches_local($89C1) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $77E5) and
                    unit_matches_local(top_units_local[unit_idx], $6307) and
                    next_unit_matches_local($9053) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $9053) and
                    unit_matches_local(top_units_local[unit_idx], $5BFC) and
                    previous_unit_matches_local($77E5) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $60C5) and
                    unit_matches_local(top_units_local[unit_idx], $6E05) and
                    next_unit_matches_local($4E8B) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E8B) and
                    (unit_matches_local(top_units_local[unit_idx], $662F) or
                    unit_matches_local(top_units_local[unit_idx], $65F6)) and
                    previous_unit_matches_local($60C5) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5019) and
                    unit_matches_local(top_units_local[unit_idx], $540E) and
                    previous_unit_matches_local($65F6) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $523B) and
                    unit_matches_local(top_units_local[unit_idx], $53EF) then
                begin
                    if previous_unit_matches_local($7ACB) or
                        previous_unit_matches_local($65F6) then
                    begin
                        Inc(Result, 22000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F1A) and
                    unit_matches_local(top_units_local[unit_idx], $56DE) and
                    next_unit_matches_local($5BFC) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $81F4) and
                    unit_matches_local(top_units_local[unit_idx], $53EA) and
                    previous_unit_matches_local($5BFC) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8BB8) and
                    unit_matches_local(top_units_local[unit_idx], $865A) and
                    previous_unit_matches_local($6216) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7236) and
                    unit_matches_local(top_units_local[unit_idx], $592B) and
                    next_unit_matches_local($4EB2) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $76EE) and
                    unit_matches_local(top_units_local[unit_idx], $6BCD) and
                    next_unit_matches_local($524D) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6BCF) and
                    unit_matches_local(top_units_local[unit_idx], $59B9) then
                begin
                    if previous_unit_matches_local($6BCF) or
                        next_unit_matches_local($6BCF) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8D77) and
                    unit_matches_local(top_units_local[unit_idx], $5176) and
                    next_unit_matches_local($59CB) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $59CB) and
                    unit_matches_local(top_units_local[unit_idx], $5B9E) and
                    previous_unit_matches_local($8D77) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F7F) and
                    unit_matches_local(top_units_local[unit_idx], $662F) and
                    next_unit_matches_local($7528) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $73ED) and
                    unit_matches_local(top_units_local[unit_idx], $534A) and
                    previous_unit_matches_local($4E0B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $653E) and
                    unit_matches_local(top_units_local[unit_idx], $9632) and
                    next_unit_matches_local($7A7A) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5E08) and
                    unit_matches_local(top_units_local[unit_idx], $4F7F) and
                    previous_unit_matches_local($7A0B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E16) and
                    unit_matches_local(top_units_local[unit_idx], $662F) and
                    next_unit_matches_local($754C) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $754C) and
                    unit_matches_local(top_units_local[unit_idx], $89E3) and
                    previous_unit_matches_local($4E16) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5750) and
                    unit_matches_local(top_units_local[unit_idx], $5DE6) and
                    next_unit_matches_local($4E0B) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7F8E) and
                    unit_matches_local(top_units_local[unit_idx], $6BCF) and
                    next_unit_matches_local($5473) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5473) and
                    unit_matches_local(top_units_local[unit_idx], $4F4D) and
                    previous_unit_matches_local($7F8E) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5BFB) and
                    unit_matches_local(top_units_local[unit_idx], $900A) and
                    next_unit_matches_local($5E38) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5E38) and
                    unit_matches_local(top_units_local[unit_idx], $5531) and
                    previous_unit_matches_local($5BFB) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7C7B) and
                    unit_matches_local(top_units_local[unit_idx], $7D2F) and
                    next_unit_matches_local($4F3C) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F3C) and
                    unit_matches_local(top_units_local[unit_idx], $6B7B) and
                    previous_unit_matches_local($7C7B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8BED) and
                    unit_matches_local(top_units_local[unit_idx], $9884) and
                    next_unit_matches_local($6C14) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6C14) and
                    unit_matches_local(top_units_local[unit_idx], $671F) and
                    previous_unit_matches_local($8BED) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7ECF) and
                    unit_matches_local(top_units_local[unit_idx], $7CBE) and
                    next_unit_matches_local($7406) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7406) and
                    unit_matches_local(top_units_local[unit_idx], $529B) and
                    previous_unit_matches_local($7ECF) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $624D) and
                    unit_matches_local(top_units_local[unit_idx], $91C7) then
                begin
                    if next_unit_matches_local($53D1) or
                        next_unit_matches_local($52A8) or
                        next_unit_matches_local($662F) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E3B) and
                    unit_matches_local(top_units_local[unit_idx], $52A9) and
                    next_unit_matches_local($529B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $529B) and
                    unit_matches_local(top_units_local[unit_idx], $7406) and
                    previous_unit_matches_local($4E3B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7EDD) and
                    unit_matches_local(top_units_local[unit_idx], $51B3) and
                    next_unit_matches_local($5BF9) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $90A3) and
                    unit_matches_local(top_units_local[unit_idx], $54EA) and
                    next_unit_matches_local($79CD) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5FE7) and
                    unit_matches_local(top_units_local[unit_idx], $53F3) and
                    next_unit_matches_local($4F24) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5408) and
                    unit_matches_local(top_units_local[unit_idx], $6CB3) and
                    next_unit_matches_local($89C4) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6620) and
                    unit_matches_local(top_units_local[unit_idx], $5E94) and
                    next_unit_matches_local($5C04) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $673A) and
                    unit_matches_local(top_units_local[unit_idx], $96C6) and
                    previous_unit_matches_local($624B) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $53D7) and
                    unit_matches_local(top_units_local[unit_idx], $6536) and
                    previous_unit_matches_local($63A5) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $89C1) and
                    unit_matches_local(top_units_local[unit_idx], $4EF6) and
                    previous_unit_matches_local($610F) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6B65) and
                    unit_matches_local(top_units_local[unit_idx], $90E8) and
                    previous_unit_matches_local($811A) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $660E) and
                    unit_matches_local(top_units_local[unit_idx], $547D) and
                    previous_unit_matches_local($58F0) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $672A) and
                    unit_matches_local(top_units_local[unit_idx], $4F4D) and
                    next_unit_matches_local($77E5) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $77E5) and
                    unit_matches_local(top_units_local[unit_idx], $7F6E) and
                    previous_unit_matches_local($672A) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5931) and
                    unit_matches_local(top_units_local[unit_idx], $5B9E) and
                    next_unit_matches_local($6548) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $96BE) and
                    unit_matches_local(top_units_local[unit_idx], $6960) and
                    next_unit_matches_local($4F4F) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F4F) and
                    unit_matches_local(top_units_local[unit_idx], $7AF9) and
                    previous_unit_matches_local($96BE) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5BFC) and
                    unit_matches_local(top_units_local[unit_idx], $9053) and
                    next_unit_matches_local($5E08) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5E08) and
                    unit_matches_local(top_units_local[unit_idx], $58EB) and
                    previous_unit_matches_local($5BFC) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8D4B) and
                    unit_matches_local(top_units_local[unit_idx], $5BCC) and
                    next_unit_matches_local($4E88) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E88) and
                    unit_matches_local(top_units_local[unit_idx], $88D5) and
                    previous_unit_matches_local($8D4B) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E0D) and
                    unit_matches_local(top_units_local[unit_idx], $8865) and
                    next_unit_matches_local($53CA) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $53CA) and
                    unit_matches_local(top_units_local[unit_idx], $7ED9) and
                    previous_unit_matches_local($4E0D) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6562) and
                    unit_matches_local(top_units_local[unit_idx], $5E72) and
                    previous_unit_matches_local($4E0D) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6536) and
                    unit_matches_local(top_units_local[unit_idx], $624B) and
                    next_unit_matches_local($96C6) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $96C6) and
                    unit_matches_local(top_units_local[unit_idx], $673A) and
                    previous_unit_matches_local($6536) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8BBE) and
                    unit_matches_local(top_units_local[unit_idx], $6444) and
                    next_unit_matches_local($60F3) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $60F3) and
                    unit_matches_local(top_units_local[unit_idx], $50CF) and
                    previous_unit_matches_local($8BBE) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E2D) and
                    (unit_matches_local(top_units_local[unit_idx], $79CD) or
                    unit_matches_local(top_units_local[unit_idx], $91CD)) then
                begin
                    if next_unit_matches_local($51FA) or
                        next_unit_matches_local($53D1) or
                        next_unit_matches_local($73B0) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $51FA) and
                    unit_matches_local(top_units_local[unit_idx], $755C) and
                    previous_unit_matches_local($4E2D) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $73B0) and
                    unit_matches_local(top_units_local[unit_idx], $5148) then
                begin
                    if previous_unit_matches_local($51FA) or
                        previous_unit_matches_local($53D1) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $614C) and
                    unit_matches_local(top_units_local[unit_idx], $8352) and
                    next_unit_matches_local($5FD9) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5FD9) and
                    unit_matches_local(top_units_local[unit_idx], $8292) and
                    previous_unit_matches_local($614C) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7591) and
                    unit_matches_local(top_units_local[unit_idx], $4E00) and
                    next_unit_matches_local($8651) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $610F) and
                    unit_matches_local(top_units_local[unit_idx], $5DF2) and
                    next_unit_matches_local($89C1) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $661F) and
                    unit_matches_local(top_units_local[unit_idx], $5F62) and
                    next_unit_matches_local($5149) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5149) and
                    previous_unit_matches_local($661F) then
                begin
                    Inc(Result, 18000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $79D1) and
                    (unit_matches_local(top_units_local[unit_idx], $523B) or
                    unit_matches_local(top_units_local[unit_idx], $53EF)) and
                    next_unit_matches_local($6280) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6280) and
                    unit_matches_local(top_units_local[unit_idx], $673A) and
                    previous_unit_matches_local($79D1) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8D62) and
                    unit_matches_local(top_units_local[unit_idx], $5E94) and
                    next_unit_matches_local($5F97) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7EC4) and
                    unit_matches_local(top_units_local[unit_idx], $963B) and
                    next_unit_matches_local($7EC7) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7EC7) and
                    unit_matches_local(top_units_local[unit_idx], $6B62) and
                    previous_unit_matches_local($7EC4) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $53F0) and
                    unit_matches_local(top_units_local[unit_idx], $592A) and
                    previous_unit_matches_local($4F5C) then
                begin
                    Inc(Result, 20000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5F15) and
                    unit_matches_local(top_units_local[unit_idx], $996E) and
                    next_unit_matches_local($7528) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $56DE) and
                    unit_matches_local(top_units_local[unit_idx], $4F1A) and
                    next_unit_matches_local($56FD) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $56FD) and
                    unit_matches_local(top_units_local[unit_idx], $8FC7) and
                    previous_unit_matches_local($56DE) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E2D) and
                    unit_matches_local(top_units_local[unit_idx], $4F17) and
                    previous_unit_matches_local($53E3) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E0A) and
                    unit_matches_local(top_units_local[unit_idx], $4F24) and
                    next_unit_matches_local($6D77) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6D77) and
                    unit_matches_local(top_units_local[unit_idx], $5BB3) and
                    previous_unit_matches_local($4E0A) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $95F4) and
                    unit_matches_local(top_units_local[unit_idx], $8D31) and
                    previous_unit_matches_local($65F6) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $53CA) and
                    unit_matches_local(top_units_local[unit_idx], $5373) and
                    next_unit_matches_local($65F6) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $65F6) and
                    unit_matches_local(top_units_local[unit_idx], $4F7F) and
                    previous_unit_matches_local($53CA) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E0E) and
                    unit_matches_local(top_units_local[unit_idx], $4E8E) then
                begin
                    if (unit_idx > 0) and (unit_idx + 1 < expected_units) then
                    begin
                        Inc(Result, 18000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $56DE) and
                    (unit_matches_local(top_units_local[unit_idx], $4F1A) or
                    unit_matches_local(top_units_local[unit_idx], $6062)) then
                begin
                    if next_unit_matches_local($5230) or
                        next_unit_matches_local($5BB6) or
                        next_unit_matches_local($590D) or
                        next_unit_matches_local($7B54) then
                    begin
                        Inc(Result, 22000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4E0D) and
                    unit_matches_local(top_units_local[unit_idx], $6B65) then
                begin
                    if next_unit_matches_local($80FD) or
                        next_unit_matches_local($6562) or
                        next_unit_matches_local($5F97) or
                        next_unit_matches_local($662F) or
                        next_unit_matches_local($60F3) or
                        next_unit_matches_local($4F1A) then
                    begin
                        Inc(Result, 26000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $77E5) and
                    unit_matches_local(top_units_local[unit_idx], $804C) then
                begin
                    if next_unit_matches_local($8BC6) or
                        next_unit_matches_local($9053) or
                        next_unit_matches_local($6653) then
                    begin
                        Inc(Result, 26000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8BA4) and
                    unit_matches_local(top_units_local[unit_idx], $4EFB) then
                begin
                    if next_unit_matches_local($77E5) or
                        next_unit_matches_local($4E3A) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7EC8) and
                    unit_matches_local(top_units_local[unit_idx], $79CD) and
                    next_unit_matches_local($6B62) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $65F6) and
                    unit_matches_local(top_units_local[unit_idx], $5B9E) and
                    next_unit_matches_local($95F4) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $95F4) and
                    unit_matches_local(top_units_local[unit_idx], $8DF5) and
                    previous_unit_matches_local($65F6) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $4F9D) and
                    unit_matches_local(top_units_local[unit_idx], $4EE5) and
                    next_unit_matches_local($8D56) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8D56) and
                    unit_matches_local(top_units_local[unit_idx], $6765) and
                    previous_unit_matches_local($4F9D) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $8D77) and
                    unit_matches_local(top_units_local[unit_idx], $6C14) then
                begin
                    if previous_unit_matches_local($5347) or
                        previous_unit_matches_local($71C3) then
                    begin
                        Inc(Result, 22000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5931) and
                    unit_matches_local(top_units_local[unit_idx], $662F) then
                begin
                    if next_unit_matches_local($53BB) or
                        next_unit_matches_local($6548) or
                        next_unit_matches_local($843D) then
                    begin
                        Inc(Result, 24000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $5DF2) and
                    unit_matches_local(top_units_local[unit_idx], $4E00) and
                    next_unit_matches_local($7ECF) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $6B65) and
                    unit_matches_local(top_units_local[unit_idx], $4E0D) and
                    next_unit_matches_local($884C) then
                begin
                    if previous_unit_matches_local($5728) or
                        previous_unit_matches_local($4E0A) or
                        previous_unit_matches_local($8DEF) or
                        previous_unit_matches_local($5BB6) then
                    begin
                        Inc(Result, 26000);
                    end;
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $673A) and
                    unit_matches_local(top_units_local[unit_idx], $53CA) and
                    next_unit_matches_local($5668) then
                begin
                    Inc(Result, 26000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $77E5) and
                    (unit_matches_local(top_units_local[unit_idx], $53EA) or
                    unit_matches_local(top_units_local[unit_idx], $804C)) and
                    previous_unit_matches_local($7684) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $79D1) and
                    unit_matches_local(top_units_local[unit_idx], $53EF) and
                    next_unit_matches_local($6280) then
                begin
                    Inc(Result, 24000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $90AE) and
                    unit_matches_local(top_units_local[unit_idx], $53F3) and
                    next_unit_matches_local($4EF6) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $7761) and
                    unit_matches_local(top_units_local[unit_idx], $6C34) and
                    next_unit_matches_local($7720) then
                begin
                    Inc(Result, 20000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $79CD) and
                    unit_matches_local(top_units_local[unit_idx], $91CD) and
                    next_unit_matches_local($79CD) then
                begin
                    Inc(Result, 22000);
                    Continue;
                end;

                if unit_matches_local(candidate_units_local[unit_idx], $53CB) and
                    unit_matches_local(top_units_local[unit_idx], $6CB9) and
                    previous_unit_matches_local($8001) then
                begin
                    Inc(Result, 18000);
                    Continue;
                end;
            end;
        end;

        function is_common_single_path_segment_local(
            const segment_text: string): Boolean;
        begin
            Result := unit_matches_local(segment_text, $7684) or
                unit_matches_local(segment_text, $5F97) or
                unit_matches_local(segment_text, $5730) or
                unit_matches_local(segment_text, $4E86) or
                unit_matches_local(segment_text, $7740) or
                unit_matches_local(segment_text, $8FC7) or
                unit_matches_local(segment_text, $662F) or
                unit_matches_local(segment_text, $65F6) or
                unit_matches_local(segment_text, $6709) or
                unit_matches_local(segment_text, $5728) or
                unit_matches_local(segment_text, $518D) or
                unit_matches_local(segment_text, $548C) or
                unit_matches_local(segment_text, $4E0E) or
                unit_matches_local(segment_text, $4E5F) or
                unit_matches_local(segment_text, $90FD) or
                unit_matches_local(segment_text, $5C31) or
                unit_matches_local(segment_text, $8FD8) or
                unit_matches_local(segment_text, $53C8) or
                unit_matches_local(segment_text, $624D) or
                unit_matches_local(segment_text, $66F4) or
                unit_matches_local(segment_text, $6700) or
                unit_matches_local(segment_text, $4E0D) or
                unit_matches_local(segment_text, $6CA1) or
                unit_matches_local(segment_text, $628A) or
                unit_matches_local(segment_text, $88AB) or
                unit_matches_local(segment_text, $8BA9) or
                unit_matches_local(segment_text, $5C06) or
                unit_matches_local(segment_text, $6211) or
                unit_matches_local(segment_text, $4F60) or
                unit_matches_local(segment_text, $4ED6) or
                unit_matches_local(segment_text, $5979) or
                unit_matches_local(segment_text, $5B83);
        end;

        function score_segment_path_quality_local(
            const candidate_value: TncCandidate;
            const candidate_idx: Integer): Integer;
        var
            path_local: string;
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            segment_text_local: string;
            segment_units_local: Integer;
            segment_start_local: Integer;
            query_key_local: string;
            exact_weight_local: Integer;
            prev_segment_text_local: string;
            prev_prev_segment_text_local: string;
            context_bonus_local: Integer;
        begin
            Result := 0;
            path_local := get_display_candidate_path_for_index(candidate_value,
                candidate_idx);
            if Trim(path_local) = '' then
            begin
                Exit;
            end;

            path_parts_local := path_local.Split([c_segment_path_separator]);
            segment_start_local := 0;
            prev_segment_text_local := '';
            prev_prev_segment_text_local := '';
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                if segment_text_local = '' then
                begin
                    Continue;
                end;

                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if segment_units_local <= 0 then
                begin
                    Continue;
                end;

                if segment_units_local = 1 then
                begin
                    if is_common_single_path_segment_local(segment_text_local) then
                    begin
                        Inc(Result, 500);
                    end
                    else
                    begin
                        Dec(Result, 900);
                    end;
                end
                else
                begin
                    Inc(Result, segment_units_local * segment_units_local * 1300);
                    query_key_local := build_display_query_key(
                        segment_start_local, segment_units_local);
                    if (query_key_local <> '') and
                        display_exact_key_has_text(query_key_local,
                        segment_text_local) then
                    begin
                        exact_weight_local := 0;
                        get_display_exact_text_weight(query_key_local,
                            segment_text_local, segment_units_local,
                            exact_weight_local);
                        Inc(Result, segment_units_local * 900 +
                            Min(7000, Max(0, exact_weight_local) * 5));
                    end
                    else
                    begin
                        Dec(Result, 9000);
                    end;
                end;

                if (m_dictionary <> nil) and (prev_segment_text_local <> '') then
                begin
                    context_bonus_local := m_dictionary.get_context_bonus(
                        prev_segment_text_local, segment_text_local);
                    if context_bonus_local > 0 then
                    begin
                        Inc(Result, Min(9000, context_bonus_local * 8));
                    end;
                    if prev_prev_segment_text_local <> '' then
                    begin
                        context_bonus_local :=
                            m_dictionary.get_context_trigram_bonus(
                            prev_prev_segment_text_local,
                            prev_segment_text_local, segment_text_local);
                        if context_bonus_local > 0 then
                        begin
                            Inc(Result, Min(12000, context_bonus_local * 10));
                        end;
                    end;
                end;

                Inc(segment_start_local, segment_units_local);
                prev_prev_segment_text_local := prev_segment_text_local;
                prev_segment_text_local := segment_text_local;
            end;

            if segment_start_local <> expected_units then
            begin
                Dec(Result, 24000);
            end;
        end;

        function score_candidate_quality_local(const candidate_value: TncCandidate;
            const candidate_idx: Integer): Integer;
        var
            candidate_units_local: TArray<string>;
        begin
            Result := Low(Integer) div 4;
            if not candidate_is_complete_local(candidate_value) then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate_value.text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            Result := score_exact_span_coverage_local(candidate_units_local) +
                score_diff_local_exact_support_local(candidate_units_local) +
                score_context_preference_local(candidate_units_local);
            Dec(Result, candidate_idx * 512);
        end;

        function score_candidate_exact_span_quality_local(
            const candidate_value: TncCandidate): Integer;
        var
            candidate_units_local: TArray<string>;
        begin
            Result := Low(Integer) div 4;
            if not candidate_is_complete_local(candidate_value) then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate_value.text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            Result := score_exact_span_coverage_local(candidate_units_local) +
                score_diff_local_exact_support_local(candidate_units_local);
        end;

        function count_exact_span_covered_units_local(
            const candidate_value: TncCandidate): Integer;
        const
            c_max_span_len = 4;
        var
            candidate_units_local: TArray<string>;
            occupied_local: TArray<Boolean>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            span_text_local: string;
            occupied_range_local: Boolean;
        begin
            Result := 0;
            if not candidate_is_complete_local(candidate_value) then
            begin
                Exit;
            end;

            candidate_units_local := split_text_units(Trim(candidate_value.text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            SetLength(occupied_local, expected_units);
            for span_len_local := Min(c_max_span_len, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    occupied_range_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if occupied_local[unit_idx_local] then
                        begin
                            occupied_range_local := True;
                            Break;
                        end;
                    end;
                    if occupied_range_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    span_text_local := build_units_range_text_local(
                        candidate_units_local, start_idx_local, span_len_local);
                    if (query_key_local = '') or (span_text_local = '') or
                        (not display_exact_key_has_text(query_key_local,
                        span_text_local)) then
                    begin
                        Continue;
                    end;

                    Inc(Result, span_len_local);
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_local[unit_idx_local] := True;
                    end;
                end;
            end;
        end;

    begin
        if short_exact_query_mode or (Length(m_candidates) < 2) or
            (expected_units < 6) or (expected_units > 32) or
            (not candidate_is_complete_local(m_candidates[0])) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;

        top_quality_local := score_candidate_quality_local(m_candidates[0], 0);
        best_quality_local := top_quality_local;
        best_idx_local := -1;
        promoted_local := False;
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_quality_local :=
                score_candidate_quality_local(m_candidates[idx], idx);
            if candidate_quality_local > best_quality_local then
            begin
                best_quality_local := candidate_quality_local;
                best_idx_local := idx;
            end;
        end;

        if (best_idx_local > 0) and
            (best_quality_local >= top_quality_local + c_min_quality_gain) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
            promoted_local := True;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vctxq=[idx=%d gain=%d text=%s]',
                    [best_idx_local, best_quality_local - top_quality_local,
                    Trim(m_candidates[0].text)]);
            end;
        end;

        if (not promoted_local) and (Length(m_candidates) >= 2) and
            candidate_is_complete_local(m_candidates[0]) and
            candidate_is_complete_local(m_candidates[1]) then
        begin
            top_units_local := split_text_units(Trim(m_candidates[0].text));
            if Length(top_units_local) = expected_units then
            begin
                top_exact_quality_local :=
                    score_candidate_exact_span_quality_local(m_candidates[0]);
                top2_exact_quality_local :=
                    score_candidate_exact_span_quality_local(m_candidates[1]);
                top_exact_covered_units_local :=
                    count_exact_span_covered_units_local(m_candidates[0]);
                top2_exact_covered_units_local :=
                    count_exact_span_covered_units_local(m_candidates[1]);
                top_path_support_local := Max(0,
                    get_segment_path_support_score(m_candidates[0]));
                top2_path_support_local := Max(0,
                    get_segment_path_support_score(m_candidates[1]));
                top_path_segments_local := get_encoded_path_segment_count_local(
                    get_display_candidate_path_for_index(m_candidates[0], 0));
                top2_path_segments_local := get_encoded_path_segment_count_local(
                    get_display_candidate_path_for_index(m_candidates[1], 1));
                if (top2_exact_covered_units_local >
                    top_exact_covered_units_local) and
                    (top2_exact_quality_local > top_exact_quality_local) and
                    (top_path_support_local <= top2_path_support_local + 1000) and
                    ((top_path_segments_local <= top2_path_segments_local) or
                    (top2_exact_quality_local >= top_exact_quality_local + 12000)) then
                begin
                    force_move_display_candidate_to_index(1, 0);
                    if m_config.debug_mode then
                    begin
                        m_last_full_path_debug_info :=
                            m_last_full_path_debug_info +
                            Format(' vexact2=[gain=%d path=%d/%d text=%s]',
                            [top2_exact_quality_local -
                            top_exact_quality_local, top_path_support_local,
                            top2_path_support_local, Trim(m_candidates[0].text)]);
                    end;
                end;
            end;
        end;
    end;

    procedure insert_long_exact_span_repair_candidate;
    const
        c_max_span_len = 5;
        c_min_span_len = 2;
        c_exact_probe_limit = 16;
        c_two_char_min_weight = 900;
        c_three_char_min_weight = 260;
        c_longer_char_min_weight = 220;
        c_min_single_repair_gain = 12000;
    var
        top_units_local: TArray<string>;
        repaired_units_local: TArray<string>;
        occupied_local: TArray<Boolean>;
        span_len_local: Integer;
        start_idx_local: Integer;
        unit_idx_local: Integer;
        replacement_units_local: TArray<string>;
        replacement_text_local: string;
        replacement_weight_local: Integer;
        current_weight_local: Integer;
        current_text_local: string;
        repaired_text_local: string;
        repair_gain_local: Integer;
        total_gain_local: Integer;
        replaced_units_local: Integer;
        replacement_count_local: Integer;
        strongest_span_local: Integer;
        existing_idx_local: Integer;
        target_idx_local: Integer;
        repaired_candidate_local: TncCandidate;
        top_path_local: string;
        path_parts_local: TArray<string>;

        function build_units_text_local(const units_value: TArray<string>): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            for local_idx := 0 to High(units_value) do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function build_units_range_text_local(const units_value: TArray<string>;
            const range_start: Integer; const range_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (range_start < 0) or (range_len <= 0) or
                (range_start + range_len > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := range_start to range_start + range_len - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function range_is_occupied_local(const range_start: Integer;
            const range_len: Integer): Boolean;
        var
            local_idx: Integer;
        begin
            Result := True;
            if (range_start < 0) or (range_len <= 0) or
                (range_start + range_len > Length(occupied_local)) then
            begin
                Exit;
            end;

            for local_idx := range_start to range_start + range_len - 1 do
            begin
                if occupied_local[local_idx] then
                begin
                    Exit;
                end;
            end;
            Result := False;
        end;

        function range_crosses_final_fixed_tail_local(const range_start: Integer;
            const range_len: Integer): Boolean;
        var
            fixed_tail_text_local: string;
        begin
            Result := False;
            if (Length(syllables) <> expected_units) or
                (range_start < 0) or (range_len <= 0) or
                (range_start + range_len <> expected_units) or
                (expected_units <= 0) then
            begin
                Exit;
            end;

            fixed_tail_text_local := get_display_fixed_tail_text_outer(
                syllables[expected_units - 1].text);
            Result := (fixed_tail_text_local <> '') and
                SameText(Trim(repaired_units_local[expected_units - 1]),
                fixed_tail_text_local);
        end;

        procedure mark_range_occupied_local(const range_start: Integer;
            const range_len: Integer);
        var
            local_idx: Integer;
        begin
            if (range_start < 0) or (range_len <= 0) or
                (range_start + range_len > Length(occupied_local)) then
            begin
                Exit;
            end;

            for local_idx := range_start to range_start + range_len - 1 do
            begin
                occupied_local[local_idx] := True;
            end;
        end;

        procedure protect_existing_path_exact_spans_local;
        const
            c_min_protected_two_char_weight = 20000;
            c_min_protected_multi_char_weight = 420;
        var
            path_idx_local: Integer;
            segment_start_local: Integer;
            segment_units_local: Integer;
            segment_weight_local: Integer;
            segment_key_local: string;
            segment_text_local: string;
        begin
            top_path_local := get_display_candidate_path_for_index(
                m_candidates[0], 0);
            if (top_path_local = '') or
                (Pos(c_segment_path_separator, top_path_local) <= 0) then
            begin
                Exit;
            end;

            path_parts_local := top_path_local.Split([c_segment_path_separator]);
            segment_start_local := 0;
            for path_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[path_idx_local]);
                segment_units_local := get_candidate_text_unit_count(
                    segment_text_local);
                if (segment_text_local = '') or (segment_units_local <= 0) or
                    (segment_start_local + segment_units_local > expected_units) then
                begin
                    Exit;
                end;

                if segment_units_local >= 2 then
                begin
                    segment_key_local := build_display_query_key(
                        segment_start_local, segment_units_local);
                    segment_weight_local := 0;
                    if (segment_key_local <> '') and
                        get_display_exact_text_weight(segment_key_local,
                        segment_text_local, segment_units_local,
                        segment_weight_local) then
                    begin
                        if ((segment_units_local = 2) and
                            (segment_weight_local >=
                            c_min_protected_two_char_weight)) or
                            ((segment_units_local > 2) and
                            (segment_weight_local >=
                            c_min_protected_multi_char_weight)) then
                        begin
                            mark_range_occupied_local(segment_start_local,
                                segment_units_local);
                        end;
                    end;
                end;

                Inc(segment_start_local, segment_units_local);
            end;
        end;

        function min_replacement_weight_local(const span_len_value: Integer):
            Integer;
        begin
            if span_len_value <= 2 then
            begin
                Exit(c_two_char_min_weight);
            end;
            if span_len_value = 3 then
            begin
                Exit(c_three_char_min_weight);
            end;
            Result := c_longer_char_min_weight;
        end;

        function current_exact_delta_margin_local(const span_len_value: Integer):
            Integer;
        begin
            if span_len_value <= 2 then
            begin
                Exit(360);
            end;
            if span_len_value = 3 then
            begin
                Exit(220);
            end;
            Result := 120;
        end;

        function candidate_effective_exact_weight_local(
            const candidate_value: TncCandidate): Integer;
        begin
            if candidate_value.has_dict_weight then
            begin
                Result := candidate_value.dict_weight;
            end
            else
            begin
                Result := candidate_value.score;
            end;
            if candidate_value.source = cs_user then
            begin
                Inc(Result, 2400);
            end;
        end;

        function try_pick_exact_replacement_local(const range_start: Integer;
            const range_len: Integer; const current_text_value: string;
            out out_text: string; out out_weight: Integer;
            out out_current_weight: Integer): Boolean;
        var
            query_key_local: string;
            exact_results_local: TncCandidateList;
            exact_idx_local: Integer;
            candidate_text_local: string;
            candidate_units_local: TArray<string>;
            candidate_weight_local: Integer;
            best_rank_local: Integer;
            candidate_rank_local: Integer;
        begin
            Result := False;
            out_text := '';
            out_weight := 0;
            out_current_weight := 0;

            query_key_local := build_display_query_key(range_start, range_len);
            if (query_key_local = '') or
                (not lookup_display_exact_cached(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            get_display_exact_text_weight(query_key_local, current_text_value,
                range_len, out_current_weight);
            best_rank_local := Low(Integer);
            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                if exact_idx_local >= c_exact_probe_limit then
                begin
                    Break;
                end;

                candidate_text_local := Trim(exact_results_local[exact_idx_local].text);
                if (candidate_text_local = '') or
                    (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    SameText(candidate_text_local, current_text_value) then
                begin
                    Continue;
                end;

                candidate_units_local := split_text_units(candidate_text_local);
                if Length(candidate_units_local) <> range_len then
                begin
                    Continue;
                end;

                candidate_weight_local := candidate_effective_exact_weight_local(
                    exact_results_local[exact_idx_local]);
                if candidate_weight_local < min_replacement_weight_local(range_len) then
                begin
                    Continue;
                end;

                if (out_current_weight > 0) and
                    (candidate_weight_local < out_current_weight +
                    current_exact_delta_margin_local(range_len)) then
                begin
                    Continue;
                end;

                candidate_rank_local := candidate_weight_local +
                    range_len * range_len * 200;
                if candidate_rank_local > best_rank_local then
                begin
                    best_rank_local := candidate_rank_local;
                    out_text := candidate_text_local;
                    out_weight := candidate_weight_local;
                    Result := True;
                end;
            end;
        end;

    begin
        if short_exact_query_mode or (m_dictionary = nil) or
            (Length(m_candidates) = 0) or (expected_units < 6) or
            (expected_units > 40) or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;

        SetLength(repaired_units_local, Length(top_units_local));
        for unit_idx_local := 0 to High(top_units_local) do
        begin
            repaired_units_local[unit_idx_local] := top_units_local[unit_idx_local];
        end;
        SetLength(occupied_local, expected_units);
        protect_existing_path_exact_spans_local;

        total_gain_local := 0;
        replaced_units_local := 0;
        replacement_count_local := 0;
        strongest_span_local := 0;
        for span_len_local := Min(c_max_span_len, expected_units) downto
            c_min_span_len do
        begin
            for start_idx_local := 0 to expected_units - span_len_local do
            begin
                if range_is_occupied_local(start_idx_local, span_len_local) then
                begin
                    Continue;
                end;
                if range_crosses_final_fixed_tail_local(start_idx_local,
                    span_len_local) then
                begin
                    Continue;
                end;
                current_text_local := build_units_range_text_local(
                    repaired_units_local, start_idx_local, span_len_local);
                if (current_text_local = '') or
                    (not try_pick_exact_replacement_local(start_idx_local,
                    span_len_local, current_text_local, replacement_text_local,
                    replacement_weight_local, current_weight_local)) then
                begin
                    Continue;
                end;

                replacement_units_local := split_text_units(replacement_text_local);
                if Length(replacement_units_local) <> span_len_local then
                begin
                    Continue;
                end;

                for unit_idx_local := 0 to span_len_local - 1 do
                begin
                    repaired_units_local[start_idx_local + unit_idx_local] :=
                        replacement_units_local[unit_idx_local];
                end;
                mark_range_occupied_local(start_idx_local, span_len_local);

                repair_gain_local := span_len_local * span_len_local * 3000 +
                    Min(12000, replacement_weight_local * span_len_local);
                if current_weight_local > 0 then
                begin
                    Inc(repair_gain_local,
                        Min(8000, (replacement_weight_local -
                        current_weight_local) * 2));
                end;
                Inc(total_gain_local, repair_gain_local);
                Inc(replaced_units_local, span_len_local);
                Inc(replacement_count_local);
                if span_len_local > strongest_span_local then
                begin
                    strongest_span_local := span_len_local;
                end;
            end;
        end;

        if replacement_count_local = 0 then
        begin
            Exit;
        end;
        if (replacement_count_local = 1) and (strongest_span_local = 2) and
            (total_gain_local < c_min_single_repair_gain) then
        begin
            Exit;
        end;

        repaired_text_local := build_units_text_local(repaired_units_local);
        if (repaired_text_local = '') or
            SameText(repaired_text_local, Trim(m_candidates[0].text)) then
        begin
            Exit;
        end;

        target_idx_local := 1;
        if (strongest_span_local > 2) and
            ((replacement_count_local >= 2) or (strongest_span_local >= 4) or
            ((strongest_span_local >= 3) and (total_gain_local >= 24000)) or
            (total_gain_local >= 36000)) then
        begin
            target_idx_local := 0;
        end;
        existing_idx_local := find_display_candidate(repaired_text_local, '');
        if existing_idx_local >= 0 then
        begin
            if existing_idx_local <> target_idx_local then
            begin
                force_move_display_candidate_to_index(existing_idx_local,
                    target_idx_local);
            end;
            Exit;
        end;

        repaired_candidate_local := m_candidates[0];
        repaired_candidate_local.text := repaired_text_local;
        repaired_candidate_local.comment := '';
        repaired_candidate_local.source := cs_rule;
        repaired_candidate_local.has_dict_weight := False;
        repaired_candidate_local.dict_weight := 0;
        if strongest_span_local = 2 then
        begin
            repaired_candidate_local.score := m_candidates[0].score - 1;
        end
        else
        begin
            repaired_candidate_local.score := m_candidates[0].score +
                Min(90000, total_gain_local);
        end;
        insert_display_candidate(repaired_candidate_local, repaired_text_local,
            target_idx_local);

        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' spanrepair=[target=%d reps=%d units=%d gain=%d text=%s]',
                [target_idx_local, replacement_count_local,
                replaced_units_local, total_gain_local, repaired_text_local]);
        end;
    end;

    procedure promote_display_pathful_candidate_over_weak_user_top;
    const
        c_probe_limit = 10;
    var
        idx: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
        candidate_path_local: string;
        top_text_local: string;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Trim(m_candidates[0].comment) <> '') or
            (m_candidates[0].source <> cs_user) then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        if (top_text_local = '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) or
            is_latest_session_query_choice(top_text_local) or
            display_candidate_is_persistent_latest_choice(normalized_pinyin,
            top_text_local) or
            (get_context_query_latest_bonus(top_text_local) > 0) then
        begin
            Exit;
        end;
        if m_candidates[0].has_dict_weight and
            (m_candidates[0].dict_weight > 32) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if get_encoded_path_segment_count_local(candidate_path_local) <= 1 then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]) + Max(0,
                get_segment_path_support_score(m_candidates[idx]));
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure promote_display_supported_head_partial_candidate;
    const
        c_probe_limit = 8;
    var
        idx: Integer;
        best_idx: Integer;
        probe_limit: Integer;
        top_path_local: string;
        candidate_text_local: string;
        candidate_comment_local: string;
        candidate_text_units_local: Integer;
        candidate_comment_units_local: Integer;
        head_key_local: string;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units <> 3) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        if (Trim(m_candidates[0].comment) = '') and
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
            expected_units) and
            display_exact_key_has_text(normalized_pinyin,
            Trim(m_candidates[0].text)) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(
            m_candidates[0], 0);
        if display_path_has_one_plus_two_function_head(top_path_local) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank_local := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_comment_local := Trim(m_candidates[idx].comment);
            if (candidate_text_local = '') or (candidate_comment_local = '') then
            begin
                Continue;
            end;

            candidate_text_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            candidate_comment_units_local :=
                get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(candidate_comment_local));
            if (candidate_text_units_local < 2) or
                (candidate_text_units_local + candidate_comment_units_local <>
                expected_units) then
            begin
                Continue;
            end;

            head_key_local := build_display_query_key(0,
                candidate_text_units_local);
            if (head_key_local = '') or
                (not display_exact_key_has_text(head_key_local,
                candidate_text_local)) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]) + candidate_text_units_local * 2048;
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx := idx;
            end;
        end;

        if best_idx > 0 then
        begin
            move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure filter_display_invalid_partial_tail_alignment;
    var
        idx: Integer;
        out_idx: Integer;
        removed_count: Integer;
        candidate_text_local: string;
        candidate_comment_local: string;
        candidate_text_units_local: Integer;
        candidate_comment_units_local: Integer;
        head_key_local: string;
        tail_key_local: string;

        function text_matches_head_key_local(const head_key_value: string;
            const text_value: string; const text_units_value: Integer): Boolean;
        begin
            Result := False;
            if (head_key_value = '') or (text_value = '') then
            begin
                Exit;
            end;

            if display_exact_key_has_text(head_key_value, text_value) then
            begin
                Exit(True);
            end;

            Result := (text_units_value = 1) and (m_dictionary <> nil) and
                m_dictionary.single_char_matches_pinyin(head_key_value,
                text_value);
        end;
    begin
        if (Length(m_candidates) = 0) or (expected_units < 2) or
            (normalized_pinyin = '') or (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        out_idx := 0;
        removed_count := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_comment_local := normalize_pinyin_text(
                Trim(m_candidates[idx].comment));
            if candidate_comment_local <> '' then
            begin
                candidate_text_local := Trim(m_candidates[idx].text);
                candidate_text_units_local := get_candidate_text_unit_count(
                    candidate_text_local);
                candidate_comment_units_local :=
                    get_effective_compact_pinyin_unit_count(
                    candidate_comment_local);
                if (candidate_text_units_local > 0) and
                    (candidate_comment_units_local > 0) and
                    (candidate_text_units_local + candidate_comment_units_local =
                    expected_units) then
                begin
                    head_key_local := build_display_query_key(0,
                        candidate_text_units_local);
                    tail_key_local := build_display_query_key(
                        candidate_text_units_local,
                        candidate_comment_units_local);
                    if text_matches_head_key_local(head_key_local,
                        candidate_text_local, candidate_text_units_local) and
                        (tail_key_local <> '') and
                        (not SameText(candidate_comment_local,
                        tail_key_local)) and
                        (alternative_exact_prefix_units(m_candidates[idx]) = 0) then
                    begin
                        Inc(removed_count);
                        Continue;
                    end;
                end;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if removed_count > 0 then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure filter_display_nonbase_complete_when_base_exact_exists;
    const
        c_min_filtering_base_exact_weight = 500;
    var
        idx: Integer;
        out_idx: Integer;
        has_base_complete_local: Boolean;
        candidate_text_local: string;
        candidate_is_complete_local: Boolean;
        exact_weight_local: Integer;

        function candidate_has_non_function_one_plus_two_path_local(
            const candidate_idx: Integer): Boolean;
        var
            path_local: string;
            path_parts_local: TArray<string>;
            head_text_local: string;
        begin
            Result := False;
            path_local := get_display_candidate_path_for_index(
                m_candidates[candidate_idx], candidate_idx);
            if path_local = '' then
            begin
                Exit;
            end;

            path_parts_local := path_local.Split([c_segment_path_separator]);
            if Length(path_parts_local) <> 2 then
            begin
                Exit;
            end;

            head_text_local := Trim(path_parts_local[0]);
            Result := (get_candidate_text_unit_count(head_text_local) = 1) and
                (get_candidate_text_unit_count(Trim(path_parts_local[1])) = 2) and
                (not is_display_function_or_modifier_single(head_text_local));
        end;

        function candidate_has_non_function_one_plus_two_text_local(
            const candidate_text_value: string): Boolean;
        var
            units_local: TArray<string>;
            tail_key_local: string;
            tail_text_local: string;
            tail_results_local: TncCandidateList;
            tail_idx_local: Integer;
            tail_candidate_text_local: string;
        begin
            Result := False;
            if expected_units <> 3 then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate_text_value));
            if (Length(units_local) <> 3) or
                is_display_function_or_modifier_single(Trim(units_local[0])) then
            begin
                Exit;
            end;

            tail_key_local := build_display_query_key(1, 2);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            if (tail_key_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;

            if m_dictionary.is_base_entry(tail_key_local, tail_text_local) then
            begin
                Exit(True);
            end;

            if not lookup_display_exact_cached(tail_key_local,
                tail_results_local) then
            begin
                Exit;
            end;

            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                tail_candidate_text_local := Trim(
                    tail_results_local[tail_idx_local].text);
                if (Trim(tail_results_local[tail_idx_local].comment) = '') and
                    SameText(tail_candidate_text_local, tail_text_local) and
                    (tail_results_local[tail_idx_local].source = cs_rule) and
                    tail_results_local[tail_idx_local].has_dict_weight and
                    (tail_results_local[tail_idx_local].dict_weight >= 300) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function candidate_has_multi_segment_path_local(
            const candidate_idx: Integer): Boolean;
        begin
            Result := get_encoded_path_segment_count_local(
                get_display_candidate_path_for_index(
                m_candidates[candidate_idx], candidate_idx)) > 1;
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 2) or
            (expected_units > 5) or (normalized_pinyin = '') then
        begin
            Exit;
        end;

        has_base_complete_local := False;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                display_candidate_has_base_full_entry(normalized_pinyin,
                m_candidates[idx]) and
                get_display_exact_text_weight(normalized_pinyin,
                candidate_text_local, expected_units, exact_weight_local) and
                (exact_weight_local >= c_min_filtering_base_exact_weight) then
            begin
                has_base_complete_local := True;
                Break;
            end;
        end;
        if not has_base_complete_local then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_is_complete_local := (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units);
            if candidate_is_complete_local and
                (not display_candidate_has_base_full_entry(normalized_pinyin,
                m_candidates[idx])) and
                (not display_candidate_is_exact_full_dictionary_entry(
                normalized_pinyin, m_candidates[idx])) and
                ((not display_candidate_is_persistent_latest_choice(
                normalized_pinyin, candidate_text_local)) or
                candidate_has_non_function_one_plus_two_text_local(
                candidate_text_local) or
                candidate_has_multi_segment_path_local(idx)) and
                (not ((m_confirmed_text <> '') and
                (m_candidates[idx].source = cs_user))) then
            begin
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure filter_display_short_one_plus_two_self_composed_complete_candidates;
    var
        idx: Integer;
        out_idx: Integer;
        removed_count: Integer;
        candidate_text_local: string;
        candidate_units_local: TArray<string>;
        path_local: string;

        function candidate_has_one_plus_two_path_local(
            const path_value: string): Boolean;
        var
            path_parts_local: TArray<string>;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(path_value) <> 2 then
            begin
                Exit;
            end;

            path_parts_local := path_value.Split([c_segment_path_separator]);
            if Length(path_parts_local) <> 2 then
            begin
                Exit;
            end;

            Result :=
                (get_candidate_text_unit_count(Trim(path_parts_local[0])) = 1) and
                (get_candidate_text_unit_count(Trim(path_parts_local[1])) = 2);
        end;

        function complete_candidate_path_matches_text_local(
            const candidate_text_value: string; const path_value: string): Boolean;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            joined_text_local: string;
            segment_text_local: string;
        begin
            Result := True;
            if (Trim(path_value) = '') or
                (get_encoded_path_segment_count_local(path_value) <= 1) then
            begin
                Exit;
            end;

            path_parts_local := path_value.Split([c_segment_path_separator]);
            joined_text_local := '';
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                if segment_text_local = '' then
                begin
                    Exit(False);
                end;
                joined_text_local := joined_text_local + segment_text_local;
            end;

            Result := SameText(joined_text_local, Trim(candidate_text_value));
        end;

        function candidate_has_one_plus_two_tail_text_support_local(
            const candidate_text_value: string): Boolean;
        var
            tail_key_local: string;
            tail_text_local: string;
        begin
            Result := False;
            candidate_units_local := split_text_units(
                Trim(candidate_text_value));
            if Length(candidate_units_local) <> expected_units then
            begin
                Exit;
            end;

            tail_key_local := build_display_query_key(1, 2);
            tail_text_local := Trim(candidate_units_local[1]) +
                Trim(candidate_units_local[2]);
            if (tail_key_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;

            Result := display_exact_key_has_text(tail_key_local,
                tail_text_local);
        end;

        function candidate_has_function_head_one_plus_two_support_local(
            const candidate_text_value: string): Boolean;
        var
            units_local: TArray<string>;
            head_text_local: string;
            tail_key_local: string;
            tail_text_local: string;
            tail_weight_local: Integer;
        begin
            Result := False;
            units_local := split_text_units(Trim(candidate_text_value));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;

            head_text_local := Trim(units_local[0]);
            if not is_display_one_plus_two_supported_head_single(
                head_text_local) then
            begin
                Exit;
            end;

            tail_key_local := build_display_query_key(1, 2);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            if (tail_key_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;

            if is_display_one_plus_two_direct_complete_head_single(
                head_text_local) then
            begin
                Result := get_display_exact_text_weight(tail_key_local,
                    tail_text_local, 2, tail_weight_local) and
                    (tail_weight_local >= 500);
            end
            else
            begin
                Result := display_exact_key_has_text(tail_key_local,
                    tail_text_local);
            end;
        end;

        function candidate_has_direct_complete_one_plus_two_support_local(
            const candidate_text_value: string): Boolean;
        var
            units_local: TArray<string>;
            head_text_local: string;
            tail_key_local: string;
            tail_text_local: string;
            tail_weight_local: Integer;
        begin
            Result := False;
            if is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, candidate_text_value) then
            begin
                Exit(True);
            end;
            units_local := split_text_units(Trim(candidate_text_value));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;

            head_text_local := Trim(units_local[0]);
            if not is_display_one_plus_two_direct_complete_head_single(
                head_text_local) then
            begin
                Exit;
            end;

            tail_key_local := build_display_query_key(1, 2);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            if (tail_key_local = '') or (tail_text_local = '') then
            begin
                Exit;
            end;

            Result := get_display_exact_text_weight(tail_key_local,
                tail_text_local, 2, tail_weight_local) and
                (tail_weight_local >= 500);
        end;

        function candidate_has_allowed_two_plus_one_tail_local(
            const path_value: string): Boolean;
        var
            path_parts_local: TArray<string>;
            tail_text_local: string;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(path_value) <> 2 then
            begin
                Exit;
            end;

            path_parts_local := path_value.Split([c_segment_path_separator]);
            if Length(path_parts_local) <> 2 then
            begin
                Exit;
            end;

            tail_text_local := Trim(path_parts_local[1]);
            Result := (get_candidate_text_unit_count(tail_text_local) = 1) and
                is_display_function_or_modifier_single(tail_text_local);
        end;

        function candidate_has_strict_full_entry_local(
            const candidate_value: TncCandidate;
            const candidate_text_value: string): Boolean;
        begin
            Result := (m_dictionary <> nil) and
                (normalized_pinyin <> '') and
                (candidate_text_value <> '') and
                (m_dictionary.is_base_entry(normalized_pinyin,
                candidate_text_value) or
                ((candidate_value.source = cs_user) and
                m_dictionary.is_user_entry(normalized_pinyin,
                candidate_text_value)));
        end;

        function try_get_two_unit_head_weight_local(const candidate_text_value: string;
            out out_weight: Integer): Boolean;
        var
            units_local: TArray<string>;
            head_key_local: string;
            head_text_local: string;
        begin
            Result := False;
            out_weight := 0;
            units_local := split_text_units(Trim(candidate_text_value));
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            head_key_local := build_display_query_key(0, 2);
            head_text_local := Trim(units_local[0]) + Trim(units_local[1]);
            if (head_key_local = '') or (head_text_local = '') then
            begin
                Exit;
            end;

            Result := get_display_exact_text_weight(head_key_local,
                head_text_local, 2, out_weight);
        end;

        function candidate_has_stronger_same_tail_head_local(
            const candidate_index_value: Integer;
            const candidate_text_value: string): Boolean;
        const
            c_stronger_head_weight_margin = 240;
        var
            candidate_units_local: TArray<string>;
            candidate_head_local: string;
            candidate_tail_local: string;
            head_key_local: string;
            candidate_head_weight_local: Integer;
            other_idx_local: Integer;
            other_text_local: string;
            other_units_local: TArray<string>;
            other_head_weight_local: Integer;

            function head_key_has_stronger_exact_local(
                const local_head_key: string;
                const local_head_text: string;
                const local_head_weight: Integer): Boolean;
            const
                c_probe_limit = 16;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
            begin
                Result := False;
                if (local_head_key = '') or (local_head_text = '') or
                    (not lookup_display_exact_cached(local_head_key,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                        (exact_text_local = '') or
                        SameText(exact_text_local, local_head_text) or
                        (get_candidate_text_unit_count(exact_text_local) <> 2) then
                    begin
                        Continue;
                    end;

                    if exact_results_local[exact_idx_local].has_dict_weight then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        exact_weight_local := exact_results_local[exact_idx_local].score;
                    end;

                    if exact_weight_local >= local_head_weight +
                        c_stronger_head_weight_margin then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            Result := False;
            candidate_units_local := split_text_units(Trim(candidate_text_value));
            if Length(candidate_units_local) <> 3 then
            begin
                Exit;
            end;

            candidate_head_local := Trim(candidate_units_local[0]) +
                Trim(candidate_units_local[1]);
            candidate_tail_local := Trim(candidate_units_local[2]);
            if (candidate_head_local = '') or (candidate_tail_local = '') then
            begin
                Exit;
            end;
            if not try_get_two_unit_head_weight_local(candidate_text_value,
                candidate_head_weight_local) then
            begin
                candidate_head_weight_local := 0;
            end;

            head_key_local := build_display_query_key(0, 2);
            if is_display_function_or_modifier_single(candidate_tail_local) and
                head_key_has_stronger_exact_local(head_key_local,
                candidate_head_local, candidate_head_weight_local) then
            begin
                Exit(True);
            end;

            for other_idx_local := 0 to High(m_candidates) do
            begin
                if other_idx_local = candidate_index_value then
                begin
                    Continue;
                end;

                other_text_local := Trim(m_candidates[other_idx_local].text);
                if (other_text_local = '') or
                    SameText(other_text_local, candidate_text_value) or
                    (Trim(m_candidates[other_idx_local].comment) <> '') or
                    (display_candidate_has_supported_full_entry(normalized_pinyin,
                    m_candidates[other_idx_local])) or
                    (display_candidate_is_exact_full_dictionary_entry(
                    normalized_pinyin, m_candidates[other_idx_local])) or
                    display_candidate_is_persistent_latest_choice(
                    normalized_pinyin, other_text_local) or
                    ((m_candidates[other_idx_local].source = cs_user) and
                    m_dictionary.is_user_entry(normalized_pinyin,
                    other_text_local)) then
                begin
                    Continue;
                end;

                other_units_local := split_text_units(other_text_local);
                if (Length(other_units_local) <> 3) or
                    (not SameText(Trim(other_units_local[2]),
                    candidate_tail_local)) or
                    (not try_get_two_unit_head_weight_local(other_text_local,
                    other_head_weight_local)) then
                begin
                    Continue;
                end;

                if other_head_weight_local >= candidate_head_weight_local +
                    c_stronger_head_weight_margin then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units <> 3) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        out_idx := 0;
        removed_count := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                (not (is_fuzzy_pinyin_active and
                (m_candidates[idx].fuzzy_cost > 0) and
                ((m_candidates[idx].source = cs_user) or
                ((m_candidates[idx].source = cs_rule) and
                m_candidates[idx].has_dict_weight)))) and
                (not display_candidate_is_persistent_latest_choice(
                normalized_pinyin, candidate_text_local)) and
                (not ((m_confirmed_text <> '') and
                (m_candidates[idx].source = cs_user))) then
            begin
                path_local := get_display_candidate_path_for_index(
                    m_candidates[idx], idx);
                if not complete_candidate_path_matches_text_local(
                    candidate_text_local, path_local) then
                begin
                    Inc(removed_count);
                    Continue;
                end;
                if (not candidate_has_strict_full_entry_local(m_candidates[idx],
                    candidate_text_local)) and
                    candidate_has_one_plus_two_path_local(path_local) and
                    (not candidate_has_direct_complete_one_plus_two_support_local(
                    candidate_text_local)) then
                begin
                    Inc(removed_count);
                    Continue;
                end;

                if not candidate_has_strict_full_entry_local(m_candidates[idx],
                    candidate_text_local) then
                begin
                    if not candidate_has_direct_complete_one_plus_two_support_local(
                        candidate_text_local) then
                    begin
                        if candidate_has_stronger_same_tail_head_local(idx,
                            candidate_text_local) then
                        begin
                            Inc(removed_count);
                            Continue;
                        end;

                        if display_path_has_two_plus_one_non_particle_tail(path_local) and
                            (not candidate_has_allowed_two_plus_one_tail_local(
                            path_local)) then
                        begin
                            Inc(removed_count);
                            Continue;
                        end;
                        if candidate_has_one_plus_two_tail_text_support_local(
                            candidate_text_local) then
                        begin
                            Inc(removed_count);
                            Continue;
                        end;
                    end;
                end;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;

        if m_config.debug_mode and (removed_count > 0) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' disp_selfdrop=[n=%d]', [removed_count]);
        end;
    end;

    procedure filter_display_complete_single_char_chain_noise_candidates;
    var
        idx: Integer;
        out_idx: Integer;
        removed_count: Integer;
        candidate_text_local: string;

        function complete_candidate_is_unsupported_single_char_chain_local(
            const candidate_value: TncCandidate; const candidate_index: Integer): Boolean;

            function candidate_is_number_quantity_phrase_local: Boolean;
            var
                number_text_local: string;
                quantity_text_local: string;
                units_local: TArray<string>;
            begin
                Result := False;
                if (expected_units <> 2) or (Length(syllables) <> expected_units) then
                begin
                    Exit;
                end;

                number_text_local := get_display_number_text_outer(
                    syllables[0].text);
                quantity_text_local := get_display_quantity_tail_text_outer(
                    syllables[1].text);
                if quantity_text_local = '' then
                begin
                    quantity_text_local :=
                        get_display_structured_quantity_tail_text_outer(
                        syllables[1].text);
                end;
                if (number_text_local = '') or (quantity_text_local = '') then
                begin
                    Exit;
                end;

                units_local := split_text_units(Trim(candidate_value.text));
                Result := (Length(units_local) = 2) and
                    SameText(Trim(units_local[0]), number_text_local) and
                    SameText(Trim(units_local[1]), quantity_text_local);
            end;

            function has_supported_complete_competitor_local: Boolean;
            const
                c_probe_limit = 16;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
            begin
                Result := False;
                if not lookup_display_exact_cached(normalized_pinyin,
                    exact_results_local) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;

                    exact_text_local := Trim(
                        exact_results_local[exact_idx_local].text);
                    if (exact_text_local = '') or
                        SameText(exact_text_local, Trim(candidate_value.text)) or
                        (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        expected_units) then
                    begin
                        Continue;
                    end;

                    if m_dictionary.is_base_entry(normalized_pinyin,
                        exact_text_local) or
                        ((exact_results_local[exact_idx_local].source = cs_rule) and
                        exact_results_local[exact_idx_local].has_dict_weight and
                        (exact_results_local[exact_idx_local].dict_weight >= 160)) or
                        ((exact_results_local[exact_idx_local].source = cs_user) and
                        m_dictionary.is_user_entry(normalized_pinyin,
                        exact_text_local)) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function candidate_is_complete_single_char_chain_local: Boolean;
            var
                chain_units_local: TArray<string>;
                chain_path_local: string;
                chain_path_parts_local: TArray<string>;
                chain_unit_idx_local: Integer;
                chain_syllable_key_local: string;
                chain_path_part_local: string;
            begin
                Result := False;
                chain_path_local := get_display_candidate_path_for_index(candidate_value,
                    candidate_index);
                if chain_path_local <> '' then
                begin
                    if get_encoded_path_segment_count_local(chain_path_local) <>
                        expected_units then
                    begin
                        Exit;
                    end;

                    chain_path_parts_local := chain_path_local.Split([c_segment_path_separator]);
                    if Length(chain_path_parts_local) <> expected_units then
                    begin
                        Exit;
                    end;

                    for chain_unit_idx_local := 0 to High(chain_path_parts_local) do
                    begin
                        chain_path_part_local := Trim(chain_path_parts_local[chain_unit_idx_local]);
                        chain_syllable_key_local := normalize_pinyin_text(
                            syllables[chain_unit_idx_local].text);
                        if (chain_path_part_local = '') or (chain_syllable_key_local = '') or
                            (get_candidate_text_unit_count(chain_path_part_local) <> 1) or
                            (not m_dictionary.single_char_matches_pinyin(
                            chain_syllable_key_local, chain_path_part_local)) then
                        begin
                            Exit(False);
                        end;
                    end;
                    Exit(True);
                end;

                chain_units_local := split_text_units(
                    Trim(candidate_value.text));
                if Length(chain_units_local) <> expected_units then
                begin
                    Exit;
                end;

                for chain_unit_idx_local := 0 to High(chain_units_local) do
                begin
                    chain_syllable_key_local := normalize_pinyin_text(
                        syllables[chain_unit_idx_local].text);
                    if (chain_syllable_key_local = '') or
                        (get_candidate_text_unit_count(
                        chain_units_local[chain_unit_idx_local]) <> 1) or
                        (not m_dictionary.single_char_matches_pinyin(
                        chain_syllable_key_local,
                        chain_units_local[chain_unit_idx_local])) then
                    begin
                        Exit(False);
                    end;
                end;

                Result := True;
            end;

            function is_low_weight_supported_competitor_chain_local: Boolean;
            begin
                Result := (expected_units >= 3) and
                    (candidate_value.source = cs_rule) and
                    candidate_value.has_dict_weight and
                    (candidate_value.dict_weight < 160) and
                    has_supported_complete_competitor_local and
                    candidate_is_complete_single_char_chain_local;
            end;
        begin
            Result := False;
            if (normalized_pinyin = '') or (expected_units < 2) or
                (expected_units > 5) or (m_dictionary = nil) or
                (Trim(candidate_value.comment) <> '') or
                (Trim(candidate_value.text) = '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <>
                expected_units) or
                is_runtime_common_pattern_candidate(candidate_value) or
                candidate_is_number_quantity_phrase_local or
                (get_display_fixed_quantity_alignment_score_outer(
                candidate_value) > 0) or
                ((not is_low_weight_supported_competitor_chain_local) and
                m_dictionary.is_base_entry(normalized_pinyin,
                Trim(candidate_value.text))) or
                ((candidate_value.source = cs_rule) and
                candidate_value.has_dict_weight and
                (candidate_value.dict_weight >= 160) and
                display_candidate_has_supported_full_entry(normalized_pinyin,
                candidate_value)) or
                display_candidate_is_persistent_latest_choice(normalized_pinyin,
                Trim(candidate_value.text)) or
                is_latest_session_query_choice(Trim(candidate_value.text)) or
                ((candidate_value.source = cs_user) and
                m_dictionary.is_user_entry(normalized_pinyin,
                Trim(candidate_value.text))) then
            begin
                Exit;
            end;

            if is_low_weight_supported_competitor_chain_local then
            begin
                Exit(True);
            end;

            Result := candidate_is_complete_single_char_chain_local;
        end;
    begin
        if (Length(m_candidates) <= 1) or (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        out_idx := 0;
        removed_count := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                complete_candidate_is_unsupported_single_char_chain_local(
                m_candidates[idx], idx) then
            begin
                Inc(removed_count);
                Continue;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;

        if m_config.debug_mode and (removed_count > 0) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' disp_schaindrop=[n=%d]', [removed_count]);
        end;
    end;

    procedure ensure_display_head_single_partials_for_keys(
        const head_key_value: string; const tail_key_value: string);
    var
        head_key_local: string;
        tail_key_local: string;
        head_results_local: TncCandidateList;
        result_idx_local: Integer;
        append_idx_local: Integer;
        head_text_local: string;
        head_candidate_local: TncCandidate;
        existing_keys_local: TDictionary<string, Boolean>;

        function visible_key_local(const text_value: string;
            const comment_value: string): string;
        begin
            Result := LowerCase(Trim(text_value)) + #1 +
                LowerCase(Trim(comment_value));
        end;

        function head_single_partial_total_limit_local: Integer;
        begin
            Result := c_candidate_total_limit_max;
        end;
    begin
        if (m_dictionary = nil) or (normalized_pinyin = '') then
        begin
            Exit;
        end;

        head_key_local := normalize_pinyin_text(head_key_value);
        tail_key_local := normalize_pinyin_text(tail_key_value);
        if (head_key_local = '') or (tail_key_local = '') or
            (not m_dictionary.lookup(head_key_local, head_results_local)) then
        begin
            Exit;
        end;

        existing_keys_local := TDictionary<string, Boolean>.Create;
        try
            for append_idx_local := 0 to High(m_candidates) do
            begin
                existing_keys_local.AddOrSetValue(visible_key_local(
                    m_candidates[append_idx_local].text,
                    m_candidates[append_idx_local].comment), True);
            end;

            for result_idx_local := 0 to High(head_results_local) do
            begin
                if Length(m_candidates) >=
                    head_single_partial_total_limit_local then
                begin
                    Break;
                end;

                head_text_local := Trim(head_results_local[result_idx_local].text);
                if (head_text_local = '') or
                    (Trim(head_results_local[result_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(head_text_local) <> 1) then
                begin
                    Continue;
                end;

                if existing_keys_local.ContainsKey(visible_key_local(
                    head_text_local, tail_key_local)) then
                begin
                    Continue;
                end;

                head_candidate_local := head_results_local[result_idx_local];
                head_candidate_local.text := head_text_local;
                head_candidate_local.comment := tail_key_local;
                append_idx_local := Length(m_candidates);
                SetLength(m_candidates, append_idx_local + 1);
                m_candidates[append_idx_local] := head_candidate_local;
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(m_candidates));
                end;
                m_candidate_segment_paths[append_idx_local] := '';
                existing_keys_local.AddOrSetValue(visible_key_local(
                    head_text_local, tail_key_local), True);
            end;
        finally
            existing_keys_local.Free;
        end;
    end;

    procedure ensure_display_short_exact_head_single_partials_complete;
    begin
        if expected_units < 2 then
        begin
            Exit;
        end;

        ensure_display_head_single_partials_for_keys(
            build_display_query_key(0, 1),
            build_display_query_key(1, expected_units - 1));
    end;

    procedure filter_display_cross_boundary_single_noise_candidates;
    var
        idx: Integer;
        out_idx: Integer;
        candidate_text_local: string;
        path_local: string;

        function path_has_single_crossing_exact_boundary_local(
            const path_value: string): Boolean;
        var
            parts_local: TArray<string>;
            part_idx_local: Integer;
            part_text_local: string;
            next_text_local: string;
            part_units_list_local: TArray<string>;
            next_units_local: TArray<string>;
            part_units_local: Integer;
            syllable_pos_local: Integer;
            bridge_key_local: string;
            bridge_text_local: string;
        begin
            Result := False;
            if get_encoded_path_segment_count_local(path_value) < 2 then
            begin
                Exit;
            end;

            parts_local := path_value.Split([c_segment_path_separator]);
            syllable_pos_local := 0;
            for part_idx_local := 0 to High(parts_local) do
            begin
                part_text_local := Trim(parts_local[part_idx_local]);
                part_units_local := get_candidate_text_unit_count(
                    part_text_local);
                if (part_text_local = '') or (part_units_local <= 0) or
                    (syllable_pos_local + part_units_local > expected_units) then
                begin
                    Exit(False);
                end;

                if (part_idx_local < High(parts_local)) and
                    (part_units_local >= 1) then
                begin
                    next_text_local := Trim(parts_local[part_idx_local + 1]);
                    part_units_list_local := split_text_units(part_text_local);
                    next_units_local := split_text_units(next_text_local);
                    if (Length(part_units_list_local) >= part_units_local) and
                        (Length(next_units_local) >= 1) and
                        (not is_display_function_or_modifier_single(
                        Trim(part_units_list_local[part_units_local - 1]))) then
                    begin
                        bridge_key_local := build_display_query_key(
                            syllable_pos_local + part_units_local - 1, 2);
                        bridge_text_local :=
                            Trim(part_units_list_local[part_units_local - 1]) +
                            Trim(next_units_local[0]);
                        if (bridge_key_local <> '') and
                            display_exact_key_has_any_complete_text(
                            bridge_key_local, 2) and
                            (not display_exact_key_has_text(bridge_key_local,
                            bridge_text_local)) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;

                Inc(syllable_pos_local, part_units_local);
            end;
        end;

        function candidate_ends_with_expected_fixed_tail_local(
            const candidate_text_value: string): Boolean;
        var
            expected_tail_text_local: string;
            units_local: TArray<string>;
        begin
            Result := False;
            expected_tail_text_local := get_display_fixed_tail_text_outer(
                syllables[expected_units - 1].text);
            if expected_tail_text_local = '' then
            begin
                Exit;
            end;

            units_local := split_text_units(Trim(candidate_text_value));
            Result := (Length(units_local) = expected_units) and
                SameText(Trim(units_local[expected_units - 1]),
                expected_tail_text_local);
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 4) or
            (expected_units > 5) or (normalized_pinyin = '') then
        begin
            Exit;
        end;

        out_idx := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                (m_candidates[idx].source <> cs_user) and
                (not display_candidate_has_supported_full_entry(
                normalized_pinyin, m_candidates[idx])) and
                (not display_candidate_is_exact_full_dictionary_entry(
                normalized_pinyin, m_candidates[idx])) and
                (not display_candidate_is_persistent_latest_choice(
                normalized_pinyin, candidate_text_local)) and
                (not candidate_ends_with_expected_fixed_tail_local(
                candidate_text_local)) and
                (get_display_fixed_quantity_alignment_score_outer(
                m_candidates[idx]) <= 0) then
            begin
                path_local := get_display_candidate_path_for_index(
                    m_candidates[idx], idx);
                if path_has_single_crossing_exact_boundary_local(path_local) then
                begin
                    Continue;
                end;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;
    end;

    procedure promote_display_primary_path_complete_candidate;
    const
        c_probe_limit = 12;
    var
        idx: Integer;
        probe_limit: Integer;
        best_idx: Integer;
        best_rank: Integer;
        candidate_rank: Integer;
        top_text_local: string;
        top_path_local: string;
        candidate_text_local: string;
        candidate_path_local: string;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        if (top_text_local = '') or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) or
            ((m_candidates[0].source = cs_user) and
            (normalized_pinyin <> '') and
            (m_dictionary <> nil) and
            m_dictionary.is_user_entry(normalized_pinyin, top_text_local)) or
            is_latest_session_query_choice(top_text_local) or
            ((normalized_pinyin <> '') and
            display_candidate_is_persistent_latest_choice(normalized_pinyin,
            top_text_local)) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(m_candidates[0], 0);
        if (get_encoded_path_segment_count_local(top_path_local) > 1) and
            (not display_path_has_nonprimary_multi_char_segment_outer(
            top_path_local)) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local = '') or
                (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            if (get_encoded_path_segment_count_local(candidate_path_local) <= 1) or
                display_path_has_nonprimary_multi_char_segment_outer(
                candidate_path_local) then
            begin
                Continue;
            end;

            candidate_rank := display_candidate_effective_weight(
                m_candidates[idx]) + Max(0,
                get_segment_path_support_score(m_candidates[idx]));
            if candidate_rank > best_rank then
            begin
                best_rank := candidate_rank;
                best_idx := idx;
            end;
        end;

        if best_idx > 0 then
        begin
            force_move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure promote_display_richer_pathful_dict_complete_candidate;
    const
        c_probe_limit = 12;
        c_min_expected_units = 6;
        c_min_multi_char_segments = 3;
        c_min_rank_gap_when_top_has_dict = 512;
    var
        idx: Integer;
        probe_limit: Integer;
        best_idx: Integer;
        best_rank: Integer;
        candidate_rank: Integer;
        top_rank: Integer;
        top_text_local: string;
        top_path_local: string;
        top_multi_segments: Integer;
        candidate_text_local: string;
        candidate_path_local: string;
        candidate_multi_segments: Integer;
        entry_idx_local: Integer;
        top_is_entry_path_local: Boolean;
        candidate_has_stored_path_local: Boolean;
    begin
        if (Length(m_candidates) <= 1) or
            (expected_units < c_min_expected_units) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        if (top_text_local = '') or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) or
            ((m_candidates[0].source = cs_user) and
            (normalized_pinyin <> '') and
            (m_dictionary <> nil) and
            m_dictionary.is_user_entry(normalized_pinyin, top_text_local)) or
            is_latest_session_query_choice(top_text_local) or
            ((normalized_pinyin <> '') and
            display_candidate_is_persistent_latest_choice(normalized_pinyin,
            top_text_local)) then
        begin
            Exit;
        end;

        top_is_entry_path_local := has_entry_pathful_complete_top and
            SameText(top_text_local, Trim(entry_top_candidate.text));
        top_path_local := get_display_candidate_path_for_index(m_candidates[0], 0);
        if top_is_entry_path_local and (entry_top_path <> '') then
        begin
            top_path_local := entry_top_path;
        end;
        top_multi_segments := display_path_multi_char_segment_count_outer(
            top_path_local);
        top_rank := display_candidate_effective_weight(m_candidates[0]) +
            Max(0, get_segment_path_support_score(m_candidates[0])) +
            top_multi_segments * 800;

        if has_entry_pathful_complete_top and
            (Trim(entry_top_candidate.text) <> '') and
            (not SameText(top_text_local, Trim(entry_top_candidate.text))) and
            (not display_candidate_has_supported_full_entry(normalized_pinyin,
            m_candidates[0])) then
        begin
            entry_idx_local := find_display_candidate(
                Trim(entry_top_candidate.text), Trim(entry_top_candidate.comment));
            if entry_idx_local > 0 then
            begin
                force_move_display_candidate_to_index(entry_idx_local, 0);
                Exit;
            end;
        end;
        best_idx := -1;
        best_rank := Low(Integer);
        probe_limit := Min(c_probe_limit - 1, High(m_candidates));
        for idx := 1 to probe_limit do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local = '') or
                (Trim(m_candidates[idx].comment) <> '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                expected_units) or
                (not m_candidates[idx].has_dict_weight) then
            begin
                Continue;
            end;

            candidate_path_local := get_display_candidate_path_for_index(
                m_candidates[idx], idx);
            candidate_has_stored_path_local := (idx < Length(
                m_candidate_segment_paths)) and
                (Trim(m_candidate_segment_paths[idx]) <> '');
            if top_is_entry_path_local and (not candidate_has_stored_path_local) then
            begin
                Continue;
            end;
            if get_encoded_path_segment_count_local(candidate_path_local) <= 1 then
            begin
                Continue;
            end;

            candidate_multi_segments :=
                display_path_multi_char_segment_count_outer(candidate_path_local);
            if (candidate_multi_segments < c_min_multi_char_segments) or
                (candidate_multi_segments <= top_multi_segments) then
            begin
                Continue;
            end;

            candidate_rank := display_candidate_effective_weight(
                m_candidates[idx]) + Max(0,
                get_segment_path_support_score(m_candidates[idx])) +
                candidate_multi_segments * 800;
            if candidate_rank > best_rank then
            begin
                best_rank := candidate_rank;
                best_idx := idx;
            end;
        end;

        if best_idx <= 0 then
        begin
            Exit;
        end;

        if (not m_candidates[0].has_dict_weight) or
            (best_rank >= top_rank + c_min_rank_gap_when_top_has_dict) then
        begin
            force_move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure trim_display_long_complete_candidates_to_top2;
    var
        idx: Integer;
        out_idx: Integer;
        complete_count: Integer;
        removed_count: Integer;
        candidate_text_local: string;
    begin
        if (Length(m_candidates) <= c_long_sentence_complete_candidate_display_limit) or
            (expected_units < c_long_sentence_full_path_min_syllables) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        out_idx := 0;
        complete_count := 0;
        removed_count := 0;
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local <> '') and
                (Trim(m_candidates[idx].comment) = '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) then
            begin
                Inc(complete_count);
                if complete_count > c_long_sentence_complete_candidate_display_limit then
                begin
                    Inc(removed_count);
                    Continue;
                end;
            end;

            if out_idx <> idx then
            begin
                m_candidates[out_idx] := m_candidates[idx];
                if out_idx < Length(m_candidate_segment_paths) then
                begin
                    if idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx] :=
                            m_candidate_segment_paths[idx];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx] := '';
                    end;
                end;
            end;
            Inc(out_idx);
        end;

        if out_idx <> Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx);
            if Length(m_candidate_segment_paths) > out_idx then
            begin
                SetLength(m_candidate_segment_paths, out_idx);
            end;
        end;

        if m_config.debug_mode and (removed_count > 0) then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' disp_fulltrim=[n=%d]', [removed_count]);
        end;
    end;

    procedure promote_display_exact_over_unstable_complete_top_candidate;
    const
        c_probe_limit = 12;
        c_min_strong_base_exact_weight = 5000;
    var
        top_text_local: string;
        top_path_local: string;
        exact_idx_local: Integer;
        exact_text_local: string;

        function try_find_strong_base_exact_candidate_local(
            out out_idx: Integer): Boolean;
        var
            local_idx: Integer;
            local_text: string;
            local_weight: Integer;
            best_weight: Integer;
        begin
            Result := False;
            out_idx := -1;
            best_weight := Low(Integer);
            for local_idx := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
            begin
                local_text := Trim(m_candidates[local_idx].text);
                if (local_text = '') or
                    (Trim(m_candidates[local_idx].comment) <> '') or
                    (get_candidate_text_unit_count(local_text) <>
                    expected_units) or
                    (not display_candidate_has_base_full_entry(
                    normalized_pinyin, m_candidates[local_idx])) or
                    (not get_display_exact_text_weight(normalized_pinyin,
                    local_text, expected_units, local_weight)) or
                    (local_weight < c_min_strong_base_exact_weight) then
                begin
                    Continue;
                end;

                if (out_idx < 0) or (local_weight > best_weight) then
                begin
                    out_idx := local_idx;
                    best_weight := local_weight;
                    Result := True;
                end;
            end;
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 2) or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        if (top_text_local = '') or (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) then
        begin
            Exit;
        end;
        if display_candidate_has_base_full_entry(normalized_pinyin,
            m_candidates[0]) then
        begin
            Exit;
        end;

        if (m_candidates[0].source = cs_user) and
            (not is_latest_session_query_choice(top_text_local)) and
            (get_context_query_latest_bonus(top_text_local) <= 0) and
            try_find_strong_base_exact_candidate_local(exact_idx_local) and
            (exact_idx_local > 0) then
        begin
            force_move_display_candidate_to_index(exact_idx_local, 0);
            Exit;
        end;

        if display_candidate_has_supported_full_entry(normalized_pinyin,
            m_candidates[0]) then
        begin
            Exit;
        end;

        top_path_local := get_display_candidate_path_for_index(
            m_candidates[0], 0);
        if (not display_path_has_nonprimary_multi_char_segment_outer(
            top_path_local)) and
            (not is_problematic_single_char_chain_candidate_for_query(
            normalized_pinyin, m_candidates[0])) and
            display_exact_key_has_text(normalized_pinyin, top_text_local) then
        begin
            Exit;
        end;

        for exact_idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            exact_text_local := Trim(m_candidates[exact_idx_local].text);
            if (exact_text_local = '') or
                (Trim(m_candidates[exact_idx_local].comment) <> '') or
                (get_candidate_text_unit_count(exact_text_local) <>
                expected_units) or
                (not display_exact_key_has_text(normalized_pinyin,
                exact_text_local)) or
                (not display_candidate_has_supported_full_entry(
                normalized_pinyin, m_candidates[exact_idx_local])) then
            begin
                Continue;
            end;

            force_move_display_candidate_to_index(exact_idx_local, 0);
            Exit;
        end;
    end;

    procedure ensure_display_confirmed_prefix_user_extensions;
    const
        c_confirmed_user_extension_display_bonus = 480;
    var
        normalized_current_local: string;
        confirmed_pinyin_local: string;
        full_query_local: string;
        confirmed_prefix_text_local: string;
        full_candidates_local: TncCandidateList;
        seg_idx_local: Integer;
        cand_idx_local: Integer;
        existing_idx_local: Integer;
        extension_text_local: string;
        candidate_local: TncCandidate;
    begin
        if (m_dictionary = nil) or (m_composition_text = '') or
            (m_confirmed_text = '') or (m_confirmed_segments = nil) or
            (m_confirmed_segments.Count = 0) then
        begin
            Exit;
        end;

        normalized_current_local := normalize_pinyin_text(m_composition_text);
        if normalized_current_local = '' then
        begin
            Exit;
        end;

        confirmed_pinyin_local := '';
        for seg_idx_local := 0 to m_confirmed_segments.Count - 1 do
        begin
            if m_confirmed_segments[seg_idx_local].pinyin <> '' then
            begin
                confirmed_pinyin_local := confirmed_pinyin_local +
                    normalize_pinyin_text(m_confirmed_segments[seg_idx_local].pinyin);
            end;
        end;
        if confirmed_pinyin_local = '' then
        begin
            Exit;
        end;

        full_query_local := confirmed_pinyin_local + normalized_current_local;
        if (not m_dictionary.lookup(full_query_local, full_candidates_local)) or
            (Length(full_candidates_local) = 0) then
        begin
            Exit;
        end;

        confirmed_prefix_text_local := m_confirmed_text;
        for cand_idx_local := 0 to High(full_candidates_local) do
        begin
            candidate_local := full_candidates_local[cand_idx_local];
            if (candidate_local.source <> cs_user) or
                (candidate_local.text = '') or
                (Length(candidate_local.text) <= Length(confirmed_prefix_text_local)) or
                (Copy(candidate_local.text, 1,
                Length(confirmed_prefix_text_local)) <> confirmed_prefix_text_local) then
            begin
                Continue;
            end;

            extension_text_local := Trim(Copy(candidate_local.text,
                Length(confirmed_prefix_text_local) + 1,
                Length(candidate_local.text)));
            if extension_text_local = '' then
            begin
                Continue;
            end;

            existing_idx_local := find_display_candidate(extension_text_local, '');
            if existing_idx_local >= 0 then
            begin
                m_candidates[existing_idx_local].source := cs_user;
                m_candidates[existing_idx_local].has_dict_weight := False;
                m_candidates[existing_idx_local].dict_weight := 0;
                if m_candidates[existing_idx_local].score <
                    candidate_local.score + c_confirmed_user_extension_display_bonus then
                begin
                    m_candidates[existing_idx_local].score := candidate_local.score +
                        c_confirmed_user_extension_display_bonus;
                end;
                Continue;
            end;

            candidate_local.text := extension_text_local;
            candidate_local.comment := '';
            candidate_local.source := cs_user;
            candidate_local.has_dict_weight := False;
            candidate_local.dict_weight := 0;
            Inc(candidate_local.score, c_confirmed_user_extension_display_bonus);
            insert_display_candidate(candidate_local, '', Min(1, Length(m_candidates)));
        end;
    end;

    procedure promote_dangling_fast_best_partial_candidate;
    var
        idx: Integer;
        best_idx: Integer;
        candidate_text_local: string;
        candidate_comment_local: string;
        candidate_units_local: Integer;
        comment_units_local: Integer;
        candidate_rank_local: Integer;
        best_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Pos('danglingprefixfast=1', m_last_lookup_debug_extra) <= 0) then
        begin
            Exit;
        end;

        best_idx := -1;
        best_rank_local := Low(Integer);
        for idx := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[idx].text);
            candidate_comment_local := normalize_pinyin_text(
                Trim(m_candidates[idx].comment));
            if (candidate_text_local = '') or (candidate_comment_local = '') then
            begin
                Continue;
            end;

            candidate_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            comment_units_local := get_effective_compact_pinyin_unit_count(
                candidate_comment_local);
            if (candidate_units_local <= 0) or (comment_units_local <= 0) or
                (candidate_units_local + comment_units_local <> expected_units) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]) + (candidate_units_local * 512);
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx := idx;
            end;
        end;

        if best_idx > 0 then
        begin
            move_display_candidate_to_index(best_idx, 0);
        end;
    end;

    procedure promote_final_top2_exact_coverage_candidate;
    const
        c_max_span_len = 4;
        c_min_score_gap = 6000;
        c_min_equal_coverage_raw_score_gap = 24000;
    var
        top_units_local: TArray<string>;
        top2_units_local: TArray<string>;
        candidate_units_local: TArray<string>;
        top_score_local: Integer;
        top2_score_local: Integer;
        candidate_score_local: Integer;
        best_score_local: Integer;
        top_covered_local: Integer;
        top2_covered_local: Integer;
        candidate_covered_local: Integer;
        best_covered_local: Integer;
        idx_local: Integer;
        best_idx_local: Integer;
        top_raw_score_local: Integer;
        candidate_raw_score_local: Integer;
        best_raw_score_local: Integer;

        function differs_only_in_final_two_units_local: Boolean;
        var
            unit_idx_local: Integer;
            diff_count_local: Integer;

            function tail_unit_matches_local(const unit_value: string;
                const code_value: Word): Boolean;
            begin
                Result := (Length(unit_value) = 1) and
                    SameText(unit_value, string(WideChar(code_value)));
            end;
        begin
            Result := False;
            if (Length(top_units_local) <> expected_units) or
                (Length(top2_units_local) <> expected_units) then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to expected_units - 3 do
            begin
                if not SameText(top_units_local[unit_idx_local],
                    top2_units_local[unit_idx_local]) then
                begin
                    Exit;
                end;
            end;

            diff_count_local := 0;
            if not SameText(top_units_local[expected_units - 2],
                top2_units_local[expected_units - 2]) then
            begin
                Inc(diff_count_local);
            end;
            if not SameText(top_units_local[expected_units - 1],
                top2_units_local[expected_units - 1]) then
            begin
                Inc(diff_count_local);
            end;
            if diff_count_local <= 0 then
            begin
                Exit;
            end;

            if (diff_count_local = 1) and
                (not SameText(top_units_local[expected_units - 1],
                top2_units_local[expected_units - 1])) and
                ((tail_unit_matches_local(top_units_local[expected_units - 1], $7684) and
                (tail_unit_matches_local(top2_units_local[expected_units - 1], $5730) or
                tail_unit_matches_local(top2_units_local[expected_units - 1], $5F97))) or
                (tail_unit_matches_local(top_units_local[expected_units - 1], $5730) and
                (tail_unit_matches_local(top2_units_local[expected_units - 1], $7684) or
                tail_unit_matches_local(top2_units_local[expected_units - 1], $5F97))) or
                (tail_unit_matches_local(top_units_local[expected_units - 1], $5F97) and
                (tail_unit_matches_local(top2_units_local[expected_units - 1], $7684) or
                tail_unit_matches_local(top2_units_local[expected_units - 1], $5730)))) then
            begin
                Exit;
            end;

            if tail_unit_matches_local(top_units_local[expected_units - 1], $5417) and
                tail_unit_matches_local(top2_units_local[expected_units - 1], $5988) then
            begin
                Exit;
            end;

            if SameText(top_units_local[expected_units - 1],
                top2_units_local[expected_units - 1]) and
                (tail_unit_matches_local(top_units_local[expected_units - 1], $4E86) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $7684) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $5730) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $5F97) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $5417) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $5427) or
                tail_unit_matches_local(top_units_local[expected_units - 1], $5566)) then
            begin
                Exit;
            end;

            Result := True;
        end;

        function build_units_range_text_for_score_local(
            const units_value: TArray<string>; const range_start: Integer;
            const range_len: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (range_start < 0) or (range_len <= 0) or
                (range_start + range_len > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := range_start to range_start + range_len - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function score_non_overlapping_exact_spans_local(
            const units_value: TArray<string>; out covered_units: Integer):
            Integer;
        var
            occupied_local: TArray<Boolean>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            span_text_local: string;
            span_weight_local: Integer;
            range_occupied_local: Boolean;
        begin
            Result := 0;
            covered_units := 0;
            if Length(units_value) <> expected_units then
            begin
                Exit;
            end;

            SetLength(occupied_local, expected_units);
            for span_len_local := Min(c_max_span_len, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    range_occupied_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if occupied_local[unit_idx_local] then
                        begin
                            range_occupied_local := True;
                            Break;
                        end;
                    end;
                    if range_occupied_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    span_text_local := build_units_range_text_for_score_local(
                        units_value, start_idx_local, span_len_local);
                    if (query_key_local = '') or (span_text_local = '') or
                        (not display_exact_key_has_text(query_key_local,
                        span_text_local)) then
                    begin
                        Continue;
                    end;

                    span_weight_local := 0;
                    get_display_exact_text_weight(query_key_local, span_text_local,
                        span_len_local, span_weight_local);
                    Inc(Result, span_len_local * span_len_local * 2500 +
                        Min(12000, Max(0, span_weight_local) * 4));
                    Inc(covered_units, span_len_local);
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_local[unit_idx_local] := True;
                    end;
                end;
            end;
        end;

    begin
        if short_exact_query_mode or (expected_units < 6) or
            (Length(m_candidates) < 2) or
            (Trim(m_candidates[0].comment) <> '') or
            (get_candidate_text_unit_count(Trim(m_candidates[0].text)) <>
            expected_units) then
        begin
            Exit;
        end;

        top_units_local := split_text_units(Trim(m_candidates[0].text));
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;
        top_score_local := score_non_overlapping_exact_spans_local(
            top_units_local, top_covered_local);
        top_raw_score_local := m_candidates[0].score;

        best_idx_local := -1;
        best_score_local := top_score_local;
        best_covered_local := top_covered_local;
        best_raw_score_local := top_raw_score_local;
        for idx_local := 1 to Min(5, High(m_candidates)) do
        begin
            if (Trim(m_candidates[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(Trim(m_candidates[idx_local].text)) <>
                expected_units) then
            begin
                Continue;
            end;

            candidate_units_local := split_text_units(Trim(m_candidates[idx_local].text));
            if Length(candidate_units_local) <> expected_units then
            begin
                Continue;
            end;

            candidate_score_local := score_non_overlapping_exact_spans_local(
                candidate_units_local, candidate_covered_local);
            candidate_raw_score_local := m_candidates[idx_local].score;
            if ((candidate_covered_local <= top_covered_local) and
                (not ((candidate_covered_local = top_covered_local) and
                (((candidate_score_local >= top_score_local -
                c_min_score_gap) and
                (candidate_raw_score_local >= top_raw_score_local +
                c_min_equal_coverage_raw_score_gap)) or
                (candidate_score_local >= top_score_local +
                c_min_score_gap * 2))))) or
                ((candidate_covered_local > top_covered_local) and
                (candidate_score_local < top_score_local + c_min_score_gap)) then
            begin
                Continue;
            end;

            if (candidate_covered_local > best_covered_local) or
                ((candidate_covered_local = best_covered_local) and
                ((candidate_score_local > best_score_local) or
                ((candidate_score_local >= best_score_local - c_min_score_gap) and
                (candidate_raw_score_local > best_raw_score_local)))) then
            begin
                best_idx_local := idx_local;
                best_score_local := candidate_score_local;
                best_covered_local := candidate_covered_local;
                best_raw_score_local := candidate_raw_score_local;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        top2_units_local := split_text_units(Trim(m_candidates[best_idx_local].text));
        top2_covered_local := best_covered_local;
        top2_score_local := best_score_local;

        if differs_only_in_final_two_units_local or
            (top2_covered_local >= top_covered_local + 2) or
            (top2_score_local >= top_score_local + c_min_score_gap * 2) or
            ((top2_covered_local = top_covered_local) and
            (top2_score_local >= top_score_local - c_min_score_gap) and
            (best_raw_score_local >= top_raw_score_local +
            c_min_equal_coverage_raw_score_gap)) then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure keep_long_complete_candidates_before_partial_display;
    const
        c_probe_limit = 10;
    var
        first_partial_idx_local: Integer;
        next_complete_idx_local: Integer;
        complete_before_partial_local: Integer;
        scan_idx_local: Integer;

        function is_complete_long_display_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            text_local := Trim(candidate_value.text);
            Result := (text_local <> '') and
                (Trim(candidate_value.comment) = '') and
                (get_candidate_text_unit_count(text_local) = expected_units);
        end;

        function is_partial_long_display_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_units_local: Integer;
            tail_units_local: Integer;
        begin
            Result := False;
            if Trim(candidate_value.comment) = '' then
            begin
                Exit;
            end;

            text_units_local := get_candidate_text_unit_count(
                Trim(candidate_value.text));
            tail_units_local := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(Trim(candidate_value.comment)));
            Result := (text_units_local > 0) and (tail_units_local > 0) and
                (text_units_local + tail_units_local = expected_units);
        end;

        function find_first_partial_local: Integer;
        var
            find_idx_local: Integer;
        begin
            Result := -1;
            for find_idx_local := 0 to Min(c_probe_limit - 1,
                High(m_candidates)) do
            begin
                if is_partial_long_display_candidate_local(
                    m_candidates[find_idx_local]) then
                begin
                    Exit(find_idx_local);
                end;
            end;
        end;

        function count_complete_before_local(
            const boundary_idx_local: Integer): Integer;
        var
            count_idx_local: Integer;
        begin
            Result := 0;
            for count_idx_local := 0 to boundary_idx_local - 1 do
            begin
                if is_complete_long_display_candidate_local(
                    m_candidates[count_idx_local]) then
                begin
                    Inc(Result);
                end;
            end;
        end;

        function find_next_complete_after_local(
            const boundary_idx_local: Integer): Integer;
        var
            find_idx_local: Integer;
        begin
            Result := -1;
            for find_idx_local := boundary_idx_local + 1 to
                Min(c_probe_limit - 1, High(m_candidates)) do
            begin
                if is_complete_long_display_candidate_local(
                    m_candidates[find_idx_local]) then
                begin
                    Exit(find_idx_local);
                end;
            end;
        end;
    begin
        if short_exact_query_mode or
            (expected_units < c_long_sentence_full_path_min_syllables) or
            (Length(m_candidates) <= 1) then
        begin
            Exit;
        end;

        for scan_idx_local := 1 to c_long_sentence_complete_candidate_display_limit do
        begin
            first_partial_idx_local := find_first_partial_local;
            if first_partial_idx_local < 0 then
            begin
                Exit;
            end;

            complete_before_partial_local := count_complete_before_local(
                first_partial_idx_local);
            if complete_before_partial_local >=
                c_long_sentence_complete_candidate_display_limit then
            begin
                Exit;
            end;

            next_complete_idx_local := find_next_complete_after_local(
                first_partial_idx_local);
            if next_complete_idx_local < 0 then
            begin
                Exit;
            end;

            force_move_display_candidate_to_index(next_complete_idx_local,
                first_partial_idx_local);
        end;
    end;

    procedure promote_best_second_complete_long_candidate;
    const
        c_probe_limit = 8;
        c_max_span_len = 5;
        c_min_second_score_gain = 6000;
    var
        first_second_idx_local: Integer;
        best_idx_local: Integer;
        best_support_local: Integer;
        first_support_local: Integer;
        candidate_support_local: Integer;
        candidate_idx_local: Integer;

        function is_complete_long_display_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            text_local := Trim(candidate_value.text);
            Result := (text_local <> '') and
                (Trim(candidate_value.comment) = '') and
                (get_candidate_text_unit_count(text_local) = expected_units);
        end;

        function build_units_range_text_local(const units_value: TArray<string>;
            const start_idx_local: Integer; const span_len_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (span_len_local <= 0) or
                (start_idx_local + span_len_local > Length(units_value)) then
            begin
                Exit;
            end;

            for local_idx := start_idx_local to
                start_idx_local + span_len_local - 1 do
            begin
                Result := Result + units_value[local_idx];
            end;
        end;

        function score_candidate_exact_support_local(
            const candidate_value: TncCandidate): Integer;
        var
            units_local: TArray<string>;
            occupied_local: TArray<Boolean>;
            span_len_local: Integer;
            start_idx_local: Integer;
            unit_idx_local: Integer;
            query_key_local: string;
            span_text_local: string;
            span_weight_local: Integer;
            range_occupied_local: Boolean;
        begin
            Result := 0;
            units_local := split_text_units(Trim(candidate_value.text));
            if Length(units_local) <> expected_units then
            begin
                Exit;
            end;

            SetLength(occupied_local, expected_units);
            for span_len_local := Min(c_max_span_len, expected_units) downto 2 do
            begin
                for start_idx_local := 0 to expected_units - span_len_local do
                begin
                    range_occupied_local := False;
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        if occupied_local[unit_idx_local] then
                        begin
                            range_occupied_local := True;
                            Break;
                        end;
                    end;
                    if range_occupied_local then
                    begin
                        Continue;
                    end;

                    query_key_local := build_display_query_key(start_idx_local,
                        span_len_local);
                    span_text_local := build_units_range_text_local(units_local,
                        start_idx_local, span_len_local);
                    if (query_key_local = '') or (span_text_local = '') or
                        (not display_exact_key_has_text(query_key_local,
                        span_text_local)) then
                    begin
                        Continue;
                    end;

                    span_weight_local := 0;
                    get_display_exact_text_weight(query_key_local,
                        span_text_local, span_len_local, span_weight_local);
                    Inc(Result, span_len_local * span_len_local * 2200 +
                        Min(18000, Max(0, span_weight_local) * span_len_local *
                        4));
                    for unit_idx_local := start_idx_local to
                        start_idx_local + span_len_local - 1 do
                    begin
                        occupied_local[unit_idx_local] := True;
                    end;
                end;
            end;
        end;
    begin
        if short_exact_query_mode or
            (expected_units < c_long_sentence_full_path_min_syllables) or
            (Length(m_candidates) < 3) or
            (not is_complete_long_display_candidate_local(m_candidates[0])) then
        begin
            Exit;
        end;

        first_second_idx_local := -1;
        for candidate_idx_local := 1 to Min(c_probe_limit - 1,
            High(m_candidates)) do
        begin
            if is_complete_long_display_candidate_local(
                m_candidates[candidate_idx_local]) then
            begin
                first_second_idx_local := candidate_idx_local;
                Break;
            end;
        end;
        if first_second_idx_local < 0 then
        begin
            Exit;
        end;

        first_support_local := score_candidate_exact_support_local(
            m_candidates[first_second_idx_local]);
        best_support_local := first_support_local;
        best_idx_local := first_second_idx_local;

        for candidate_idx_local := first_second_idx_local + 1 to
            Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if not is_complete_long_display_candidate_local(
                m_candidates[candidate_idx_local]) then
            begin
                Continue;
            end;

            candidate_support_local := score_candidate_exact_support_local(
                m_candidates[candidate_idx_local]);
            if candidate_support_local > best_support_local then
            begin
                best_support_local := candidate_support_local;
                best_idx_local := candidate_idx_local;
            end;
        end;

        if (best_idx_local > first_second_idx_local) and
            (best_support_local >= first_support_local + c_min_second_score_gain) then
        begin
            force_move_display_candidate_to_index(best_idx_local,
                first_second_idx_local);
        end;
    end;

    procedure promote_higher_scored_complete_long_display_candidate;
    const
        c_probe_limit = 6;
        c_min_score_gain = 12000;
        c_min_pathless_over_pathful_score_gain = 40000;
        c_min_pathless_over_pathful_coverage_gain = 8000;
    var
        best_idx_local: Integer;
        best_score_local: Integer;
        candidate_idx_local: Integer;
        candidate_score_local: Integer;
        top_path_segments_local: Integer;
        candidate_path_segments_local: Integer;
        top_coverage_local: Integer;
        candidate_coverage_local: Integer;
        candidate_text_local: string;
        weak_pathless_over_pathful_local: Boolean;

        function is_complete_long_candidate_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_local: string;
        begin
            text_local := Trim(candidate_value.text);
            Result := (text_local <> '') and
                (Trim(candidate_value.comment) = '') and
                (get_candidate_text_unit_count(text_local) = expected_units);
        end;

        function effective_score_local(
            const candidate_value: TncCandidate): Integer;
        begin
            Result := candidate_value.score;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > Result) then
            begin
                Result := candidate_value.dict_weight;
            end;
            if candidate_value.source = cs_user then
            begin
                Inc(Result, c_user_score_bonus);
            end;
        end;

        function actual_path_segment_count_local(const candidate_value:
            TncCandidate; const candidate_idx_value: Integer): Integer;
        var
            path_local: string;
        begin
            path_local := '';
            if (candidate_idx_value >= 0) and
                (candidate_idx_value < Length(m_candidate_segment_paths)) then
            begin
                path_local := m_candidate_segment_paths[candidate_idx_value];
            end;
            if path_local = '' then
            begin
                path_local := get_segment_path_for_candidate(candidate_value,
                    candidate_idx_value);
            end;
            Result := get_encoded_path_segment_count_local(path_local);
        end;
    begin
        if short_exact_query_mode or
            (expected_units < c_long_sentence_full_path_min_syllables) or
            (Length(m_candidates) < 2) or (m_candidates[0].source = cs_user) or
            (not is_complete_long_candidate_local(m_candidates[0])) then
        begin
            Exit;
        end;

        best_idx_local := 0;
        best_score_local := effective_score_local(m_candidates[0]);
        top_path_segments_local := actual_path_segment_count_local(
            m_candidates[0], 0);
        top_coverage_local :=
            get_display_complete_exact_span_coverage_score(m_candidates[0]);
        for candidate_idx_local := 1 to Min(c_probe_limit - 1,
            High(m_candidates)) do
        begin
            if not is_complete_long_candidate_local(
                m_candidates[candidate_idx_local]) then
            begin
                Continue;
            end;

            candidate_score_local := effective_score_local(
                m_candidates[candidate_idx_local]);
            candidate_text_local := Trim(m_candidates[candidate_idx_local].text);
            candidate_path_segments_local := actual_path_segment_count_local(
                m_candidates[candidate_idx_local], candidate_idx_local);
            weak_pathless_over_pathful_local :=
                (top_path_segments_local > 1) and
                (candidate_path_segments_local <= 1) and
                ((normalized_pinyin = '') or
                (not display_exact_key_has_text(normalized_pinyin,
                candidate_text_local))) and
                (candidate_score_local < best_score_local +
                c_min_pathless_over_pathful_score_gain);
            if weak_pathless_over_pathful_local then
            begin
                candidate_coverage_local :=
                    get_display_complete_exact_span_coverage_score(
                    m_candidates[candidate_idx_local]);
                if candidate_coverage_local < top_coverage_local +
                    c_min_pathless_over_pathful_coverage_gain then
                begin
                    Continue;
                end;
            end;
            if candidate_score_local > best_score_local then
            begin
                best_score_local := candidate_score_local;
                best_idx_local := candidate_idx_local;
            end;
        end;

        if (best_idx_local <= 0) or
            (best_score_local < effective_score_local(m_candidates[0]) +
            c_min_score_gain) then
        begin
            Exit;
        end;

        force_move_display_candidate_to_index(best_idx_local, 0);
        if m_config.debug_mode then
        begin
            m_last_full_path_debug_info := m_last_full_path_debug_info +
                Format(' vhscore=[idx=%d gain=%d text=%s]',
                [best_idx_local,
                best_score_local - effective_score_local(m_candidates[1]),
                Trim(m_candidates[0].text)]);
        end;
    end;

    procedure note_display_phase(const phase_name: string);
    var
        elapsed_ms: UInt64;
    begin
        if not m_config.debug_mode then
        begin
            display_phase_tick := nc_monotonic_tick_ms;
            Exit;
        end;

        elapsed_ms := nc_monotonic_tick_ms - display_phase_tick;
        display_phase_tick := nc_monotonic_tick_ms;
        if elapsed_ms = 0 then
        begin
            Exit;
        end;

        if m_last_lookup_debug_extra <> '' then
        begin
            m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
        end;
        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
            Format('gc_%s=%d', [phase_name, elapsed_ms]);
    end;

    function can_use_display_typo_short_prefix_fast_path: Boolean;
    const
        c_min_supported_prefix_weight = 300;
        c_probe_limit = 8;
    var
        idx_local: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        split_head_local: string;
        split_tail_local: string;
        prefix_units_local: Integer;
        tail_units_local: Integer;
        prefix_key_local: string;
    begin
        Result := False;
        if (Pos('typoshortprefixfast=1', m_last_lookup_debug_extra) <= 0) or
            (expected_units < 3) or (expected_units > 4) or
            (Length(m_candidates) = 0) or (m_dictionary = nil) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_key) > 0) then
        begin
            Exit;
        end;

        for idx_local := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_text_local := Trim(m_candidates[idx_local].text);
            candidate_tail_local := Trim(m_candidates[idx_local].comment);
            if (candidate_tail_local = '') and
                split_trailing_ascii_candidate(candidate_text_local,
                split_head_local, split_tail_local) then
            begin
                candidate_text_local := Trim(split_head_local);
                candidate_tail_local := Trim(split_tail_local);
            end;

            candidate_tail_local := normalize_pinyin_text(candidate_tail_local);
            if (candidate_text_local = '') or (candidate_tail_local = '') or
                (Length(candidate_tail_local) > 10) then
            begin
                Continue;
            end;

            prefix_units_local :=
                get_candidate_text_unit_count(candidate_text_local);
            tail_units_local :=
                get_effective_compact_pinyin_unit_count(candidate_tail_local);
            if (prefix_units_local <= 0) or
                (prefix_units_local >= expected_units) or
                (tail_units_local <= 0) or
                (prefix_units_local + tail_units_local <> expected_units) then
            begin
                Continue;
            end;

            prefix_key_local := build_display_query_key(0,
                prefix_units_local);
            if prefix_key_local = '' then
            begin
                Continue;
            end;

            if prefix_units_local = 1 then
            begin
                if m_dictionary.single_char_matches_pinyin(prefix_key_local,
                    candidate_text_local) then
                begin
                    Exit(True);
                end;
            end
            else if display_exact_key_has_text(prefix_key_local,
                candidate_text_local) or
                m_dictionary.is_base_entry(prefix_key_local,
                candidate_text_local) or
                m_dictionary.is_user_entry(prefix_key_local,
                candidate_text_local) or
                (m_candidates[idx_local].dict_weight >=
                c_min_supported_prefix_weight) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure insert_typo_short_prefix_tail_completion_from_partial;
    const
        c_probe_limit = 8;
        c_tail_probe_limit = 8;
        c_min_supported_prefix_weight = 300;
    var
        idx_local: Integer;
        tail_idx_local: Integer;
        duplicate_idx_local: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        split_head_local: string;
        split_tail_local: string;
        prefix_units_local: Integer;
        tail_units_local: Integer;
        prefix_key_local: string;
        tail_results_local: TncCandidateList;
        tail_text_local: string;
        repaired_text_local: string;
        repaired_candidate_local: TncCandidate;
        repaired_path_local: string;
        prefix_supported_local: Boolean;
    begin
        if not can_use_display_typo_short_prefix_fast_path then
        begin
            Exit;
        end;

        for idx_local := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_text_local := Trim(m_candidates[idx_local].text);
            candidate_tail_local := Trim(m_candidates[idx_local].comment);
            if (candidate_tail_local = '') and
                split_trailing_ascii_candidate(candidate_text_local,
                split_head_local, split_tail_local) then
            begin
                candidate_text_local := Trim(split_head_local);
                candidate_tail_local := Trim(split_tail_local);
            end;

            candidate_tail_local := normalize_pinyin_text(candidate_tail_local);
            prefix_units_local :=
                get_candidate_text_unit_count(candidate_text_local);
            tail_units_local :=
                get_effective_compact_pinyin_unit_count(candidate_tail_local);
            if (candidate_text_local = '') or (candidate_tail_local = '') or
                (prefix_units_local <= 0) or (tail_units_local < 2) or
                (tail_units_local > 3) or
                (prefix_units_local + tail_units_local <> expected_units) then
            begin
                Continue;
            end;

            prefix_key_local := build_display_query_key(0,
                prefix_units_local);
            if prefix_key_local = '' then
            begin
                Continue;
            end;

            if prefix_units_local = 1 then
            begin
                prefix_supported_local :=
                    m_dictionary.single_char_matches_pinyin(prefix_key_local,
                    candidate_text_local);
            end
            else
            begin
                prefix_supported_local :=
                    display_exact_key_has_text(prefix_key_local,
                    candidate_text_local) or
                    m_dictionary.is_base_entry(prefix_key_local,
                    candidate_text_local) or
                    m_dictionary.is_user_entry(prefix_key_local,
                    candidate_text_local) or
                    (m_candidates[idx_local].dict_weight >=
                    c_min_supported_prefix_weight);
            end;
            if not prefix_supported_local then
            begin
                Continue;
            end;

            if not lookup_display_complete_cached(candidate_tail_local,
                tail_results_local) then
            begin
                Continue;
            end;

            for tail_idx_local := 0 to High(tail_results_local) do
            begin
                if tail_idx_local >= c_tail_probe_limit then
                begin
                    Break;
                end;

                tail_text_local := Trim(tail_results_local[tail_idx_local].text);
                if (Trim(tail_results_local[tail_idx_local].comment) <> '') or
                    (tail_text_local = '') or
                    (get_candidate_text_unit_count(tail_text_local) <>
                    tail_units_local) then
                begin
                    Continue;
                end;

                repaired_text_local := candidate_text_local + tail_text_local;
                duplicate_idx_local :=
                    find_display_candidate(repaired_text_local, '');
                if duplicate_idx_local >= 0 then
                begin
                    move_display_candidate_to_index(duplicate_idx_local, 0);
                    Exit;
                end;

                repaired_candidate_local := m_candidates[idx_local];
                repaired_candidate_local.text := repaired_text_local;
                repaired_candidate_local.comment := '';
                repaired_candidate_local.source := cs_rule;
                repaired_candidate_local.has_dict_weight :=
                    tail_results_local[tail_idx_local].has_dict_weight;
                repaired_candidate_local.dict_weight :=
                    tail_results_local[tail_idx_local].dict_weight;
                repaired_candidate_local.score := Max(
                    m_candidates[idx_local].score,
                    tail_results_local[tail_idx_local].score) + 16000;
                repaired_path_local := candidate_text_local +
                    c_segment_path_separator + tail_text_local;
                insert_display_candidate(repaired_candidate_local,
                    repaired_path_local, 0);
                Exit;
            end;
        end;
    end;

    function top_display_candidate_is_stable_complete_local: Boolean;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
        top_path_local: string;
        support_score_local: Integer;
        support_segments_local: Integer;
    begin
        Result := False;
        if (expected_units < 12) or
            (Length(m_candidates) = 0) or
            (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        if Trim(top_candidate_local.comment) <> '' then
        begin
            Exit;
        end;

        top_text_local := Trim(top_candidate_local.text);
        if (top_text_local = '') or
            (get_candidate_text_unit_count(top_text_local) <> expected_units) or
            is_problematic_single_char_chain_candidate_for_query(
            normalized_pinyin, top_candidate_local) then
        begin
            Exit;
        end;

        top_path_local := get_segment_path_for_candidate(top_candidate_local, 0);
        if top_path_local = '' then
        begin
            top_path_local := infer_segment_path_for_query_text_with_score(
                normalized_pinyin, top_text_local, support_score_local,
                support_segments_local);
        end;
        Result := top_path_local <> '';
    end;

    function can_use_display_short_complete_fast_path: Boolean;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
        short4_prefix_complete_local: Boolean;
        supported_pronoun_connector_complete_local: Boolean;
    begin
        Result := False;
        short4_prefix_complete_local :=
            Pos('short4prefixraw=1', m_last_lookup_debug_extra) > 0;
        if (expected_units < 4) or (expected_units > 6) or
            (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        supported_pronoun_connector_complete_local :=
            display_candidate_is_supported_pronoun_connector_tail_complete_local(
            top_candidate_local);
        if (Pos('shortcompletefast=1', m_last_lookup_debug_extra) <= 0) and
            (Pos('short321tailfast=1', m_last_lookup_debug_extra) <= 0) and
            (not short4_prefix_complete_local) and
            (not supported_pronoun_connector_complete_local) then
        begin
            Exit;
        end;
        if Trim(top_candidate_local.comment) <> '' then
        begin
            Exit;
        end;

        top_text_local := Trim(top_candidate_local.text);
        Result := (top_text_local <> '') and
            (get_candidate_text_unit_count(top_text_local) = expected_units) and
            ((not short4_prefix_complete_local) or
            (top_candidate_local.score >= 15000)) and
            ((not is_problematic_single_char_chain_candidate_for_query(
            normalized_pinyin, top_candidate_local)) or
            supported_pronoun_connector_complete_local);
    end;

    function can_use_display_fixed_head_tail_complete_fast_path: Boolean;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
    begin
        Result := False;
        if (expected_units < 3) or (expected_units > 6) or
            (Length(m_candidates) = 0) or
            (Pos('fixedheadtail=1', m_last_lookup_debug_extra) <= 0) or
            (not is_full_pinyin_key(normalized_pinyin)) then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        if Trim(top_candidate_local.comment) <> '' then
        begin
            Exit;
        end;

        top_text_local := Trim(top_candidate_local.text);
        Result := (top_text_local <> '') and
            (get_candidate_text_unit_count(top_text_local) = expected_units) and
            (not is_problematic_single_char_chain_candidate_for_query(
            normalized_pinyin, top_candidate_local));
    end;

    procedure promote_display_supported_partial_with_same_tail_local;
    const
        c_probe_limit = 8;
    var
        top_text_local: string;
        top_tail_local: string;
        top_units_local: Integer;
        head_key_local: string;
        idx: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_rank_local: Integer;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 3) or
            (Length(syllables) <> expected_units) then
        begin
            Exit;
        end;

        top_tail_local := normalize_pinyin_text(Trim(m_candidates[0].comment));
        if top_tail_local = '' then
        begin
            Exit;
        end;

        top_text_local := Trim(m_candidates[0].text);
        top_units_local := get_candidate_text_unit_count(top_text_local);
        if (top_units_local <= 0) or (top_units_local >= expected_units) then
        begin
            Exit;
        end;

        head_key_local := build_display_query_key(0, top_units_local);
        if head_key_local = '' then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_tail_local := normalize_pinyin_text(
                Trim(m_candidates[idx].comment));
            if candidate_tail_local <> top_tail_local then
            begin
                Continue;
            end;

            candidate_text_local := Trim(m_candidates[idx].text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <>
                top_units_local) or
                (not display_exact_key_has_text(head_key_local,
                candidate_text_local)) then
            begin
                Continue;
            end;

            candidate_rank_local := display_candidate_effective_weight(
                m_candidates[idx]);
            if candidate_rank_local > best_rank_local then
            begin
                best_rank_local := candidate_rank_local;
                best_idx_local := idx;
            end;
        end;

        if best_idx_local > 0 then
        begin
            force_move_display_candidate_to_index(best_idx_local, 0);
        end;
    end;

    procedure normalize_display_partial_prefix_order;
    const
        c_max_partial_order_candidates = 32;
    var
        block_start: Integer;
        block_end: Integer;
        original_block_end: Integer;
        local_idx: Integer;
        probe_idx: Integer;
        insert_idx: Integer;
        has_multi_char_partial: Boolean;
        picked_candidate: TncCandidate;
        picked_path: string;

        function partial_prefix_units_local(
            const candidate_value: TncCandidate): Integer;
        var
            tail_units_local: Integer;
        begin
            Result := 0;
            if Trim(candidate_value.comment) = '' then
            begin
                Exit;
            end;

            Result := get_candidate_text_unit_count(Trim(candidate_value.text));
            tail_units_local := get_effective_compact_pinyin_unit_count(
                candidate_value.comment);
            if (Result <= 0) or (tail_units_local <= 0) or
                (Result + tail_units_local <> expected_units) then
            begin
                Result := 0;
            end;
        end;

        function partial_prefix_weight_local(
            const candidate_value: TncCandidate): Integer;
        begin
            if candidate_value.has_dict_weight then
            begin
                Result := candidate_value.dict_weight;
            end
            else
            begin
                Result := candidate_value.score;
            end;
        end;

        function partial_prefix_is_exact_entry_local(
            const candidate_value: TncCandidate;
            const prefix_units: Integer): Boolean;
        var
            prefix_key_local: string;
        begin
            Result := False;
            if prefix_units <= 0 then
            begin
                Exit;
            end;

            prefix_key_local := build_display_query_key(0, prefix_units);
            Result := (prefix_key_local <> '') and
                display_exact_key_has_text(prefix_key_local,
                Trim(candidate_value.text));
        end;

        function partial_prefix_order_before_local(
            const left: TncCandidate; const right: TncCandidate): Boolean;
        var
            left_units: Integer;
            right_units: Integer;
            left_exact: Boolean;
            right_exact: Boolean;
            left_weight: Integer;
            right_weight: Integer;
        begin
            left_units := partial_prefix_units_local(left);
            right_units := partial_prefix_units_local(right);
            if (left_units <= 0) or (right_units <= 0) then
            begin
                Exit(False);
            end;

            if left_units <> right_units then
            begin
                Exit(left_units > right_units);
            end;
            if left_units = 1 then
            begin
                Exit(False);
            end;

            left_exact := partial_prefix_is_exact_entry_local(left, left_units);
            right_exact := partial_prefix_is_exact_entry_local(right, right_units);
            if left_exact <> right_exact then
            begin
                Exit(left_exact);
            end;

            if left.source <> right.source then
            begin
                if left.source = cs_user then
                begin
                    Exit(True);
                end;
                if right.source = cs_user then
                begin
                    Exit(False);
                end;
            end;

            left_weight := partial_prefix_weight_local(left);
            right_weight := partial_prefix_weight_local(right);
            if left_weight <> right_weight then
            begin
                Exit(left_weight > right_weight);
            end;

            Result := left.score > right.score;
        end;
    begin
        if (Length(m_candidates) <= 1) or (expected_units < 2) then
        begin
            Exit;
        end;

        has_multi_char_partial := False;
        for probe_idx := 0 to High(m_candidates) do
        begin
            if (Trim(m_candidates[probe_idx].comment) <> '') and
                (get_candidate_text_unit_count(
                Trim(m_candidates[probe_idx].text)) > 1) then
            begin
                has_multi_char_partial := True;
                Break;
            end;
        end;
        if not has_multi_char_partial then
        begin
            Exit;
        end;

        block_start := 0;
        while block_start <= High(m_candidates) do
        begin
            while (block_start <= High(m_candidates)) and
                (partial_prefix_units_local(m_candidates[block_start]) <= 0) do
            begin
                Inc(block_start);
            end;
            if block_start > High(m_candidates) then
            begin
                Break;
            end;

            block_end := block_start;
            while (block_end <= High(m_candidates)) and
                (partial_prefix_units_local(m_candidates[block_end]) > 0) do
            begin
                Inc(block_end);
            end;
            Dec(block_end);
            original_block_end := block_end;
            if block_end - block_start + 1 > c_max_partial_order_candidates then
            begin
                block_end := block_start + c_max_partial_order_candidates - 1;
            end;

            for local_idx := block_start + 1 to block_end do
            begin
                picked_candidate := m_candidates[local_idx];
                picked_path := '';
                if local_idx < Length(m_candidate_segment_paths) then
                begin
                    picked_path := m_candidate_segment_paths[local_idx];
                end;

                insert_idx := local_idx;
                while (insert_idx > block_start) and
                    partial_prefix_order_before_local(picked_candidate,
                    m_candidates[insert_idx - 1]) do
                begin
                    m_candidates[insert_idx] := m_candidates[insert_idx - 1];
                    if insert_idx < Length(m_candidate_segment_paths) then
                    begin
                        if insert_idx - 1 < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[insert_idx] :=
                                m_candidate_segment_paths[insert_idx - 1];
                        end
                        else
                        begin
                            m_candidate_segment_paths[insert_idx] := '';
                        end;
                    end;
                    Dec(insert_idx);
                end;

                m_candidates[insert_idx] := picked_candidate;
                if insert_idx < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[insert_idx] := picked_path;
                end;
            end;

            block_start := original_block_end + 1;
        end;
    end;

    procedure ensure_display_top_complete_prefix_partial_visible;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
        top_units_local: TArray<string>;
        syllables_local: TncPinyinParseResult;
        prefix_text_local: string;
        tail_key_local: string;
        unit_idx_local: Integer;
        candidate_idx_local: Integer;
        existing_idx_local: Integer;
        insert_idx_local: Integer;
        move_idx_local: Integer;
        best_anchor_idx_local: Integer;
        best_anchor_score_local: Integer;
        anchor_score_local: Integer;
        suffix_text_local: string;
        suffix_key_local: string;
        partial_candidate_local: TncCandidate;

        function build_syllable_key_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(syllables_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx_local to start_idx_local + count_local - 1 do
            begin
                Result := Result + normalize_pinyin_text(
                    syllables_local[local_idx].text);
            end;
        end;

        function build_unit_text_local(const start_idx_local: Integer;
            const count_local: Integer): string;
        var
            local_idx: Integer;
        begin
            Result := '';
            if (start_idx_local < 0) or (count_local <= 0) or
                (start_idx_local + count_local > Length(top_units_local)) then
            begin
                Exit;
            end;

            for local_idx := start_idx_local to start_idx_local + count_local - 1 do
            begin
                Result := Result + top_units_local[local_idx];
            end;
        end;

        function derived_prefix_has_exact_tail_support_local: Boolean;
        var
            local_suffix_len: Integer;
            local_suffix_start: Integer;
        begin
            Result := False;
            for local_suffix_len := Min(4, expected_units - 1) downto 2 do
            begin
                local_suffix_start := expected_units - 1 - local_suffix_len;
                suffix_text_local := build_unit_text_local(local_suffix_start,
                    local_suffix_len);
                suffix_key_local := build_syllable_key_local(local_suffix_start,
                    local_suffix_len);
                if (suffix_text_local <> '') and (suffix_key_local <> '') and
                    (display_exact_key_has_text(suffix_key_local,
                    suffix_text_local) or
                    ((m_dictionary <> nil) and
                    m_dictionary.is_base_entry(suffix_key_local,
                    suffix_text_local))) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function find_existing_prefix_partial_local: Integer;
        var
            find_idx_local: Integer;
        begin
            Result := -1;
            for find_idx_local := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[find_idx_local].text),
                    prefix_text_local) and
                    SameText(normalize_pinyin_text(Trim(
                    m_candidates[find_idx_local].comment)), tail_key_local) then
                begin
                    Exit(find_idx_local);
                end;
            end;
        end;

        function get_prefix_partial_insert_index_local: Integer;
        var
            scan_idx_local: Integer;
            complete_seen_local: Integer;
        begin
            Result := 1;
            complete_seen_local := 0;
            for scan_idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[scan_idx_local].comment) = '') and
                    (get_candidate_text_unit_count(Trim(
                    m_candidates[scan_idx_local].text)) = expected_units) then
                begin
                    Inc(complete_seen_local);
                    Result := scan_idx_local + 1;
                    if complete_seen_local >=
                        c_long_sentence_complete_candidate_display_limit then
                    begin
                        Break;
                    end;
                end;
            end;
            if Result < 1 then
            begin
                Result := 1;
            end;
        end;

        procedure move_prefix_partial_to_index_local(
            const candidate_idx_local: Integer; const target_idx_local: Integer);
        var
            local_move_idx: Integer;
            moved_candidate_local: TncCandidate;
            moved_path_local: string;
            normalized_target_idx_local: Integer;
        begin
            if (candidate_idx_local < 0) or
                (candidate_idx_local > High(m_candidates)) then
            begin
                Exit;
            end;

            normalized_target_idx_local := Max(1,
                Min(target_idx_local, High(m_candidates)));
            if candidate_idx_local < normalized_target_idx_local then
            begin
                Dec(normalized_target_idx_local);
            end;
            if candidate_idx_local = normalized_target_idx_local then
            begin
                Exit;
            end;

            moved_candidate_local := m_candidates[candidate_idx_local];
            moved_path_local := '';
            if candidate_idx_local < Length(m_candidate_segment_paths) then
            begin
                moved_path_local := m_candidate_segment_paths[candidate_idx_local];
            end;

            if candidate_idx_local > normalized_target_idx_local then
            begin
                for local_move_idx := candidate_idx_local downto
                    normalized_target_idx_local + 1 do
                begin
                    m_candidates[local_move_idx] := m_candidates[local_move_idx - 1];
                    if local_move_idx < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[local_move_idx] :=
                            m_candidate_segment_paths[local_move_idx - 1];
                    end;
                end;
            end
            else
            begin
                for local_move_idx := candidate_idx_local to
                    normalized_target_idx_local - 1 do
                begin
                    m_candidates[local_move_idx] := m_candidates[local_move_idx + 1];
                    if local_move_idx < Length(m_candidate_segment_paths) then
                    begin
                        if local_move_idx + 1 < Length(m_candidate_segment_paths) then
                        begin
                            m_candidate_segment_paths[local_move_idx] :=
                                m_candidate_segment_paths[local_move_idx + 1];
                        end
                        else
                        begin
                            m_candidate_segment_paths[local_move_idx] := '';
                        end;
                    end;
                end;
            end;
            m_candidates[normalized_target_idx_local] := moved_candidate_local;
            if normalized_target_idx_local < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[normalized_target_idx_local] :=
                    moved_path_local;
            end;
            m_visible_candidates_cache_valid := False;
        end;
    begin
        if (expected_units < 6) or (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        syllables_local := get_effective_compact_pinyin_syllables(
            normalized_pinyin);
        if Length(syllables_local) <> expected_units then
        begin
            Exit;
        end;

        best_anchor_idx_local := -1;
        best_anchor_score_local := Low(Integer);
        for candidate_idx_local := 0 to Min(High(m_candidates), 5) do
        begin
            top_candidate_local := m_candidates[candidate_idx_local];
            top_text_local := Trim(top_candidate_local.text);
            if (top_text_local = '') or
                (Trim(top_candidate_local.comment) <> '') or
                (get_candidate_text_unit_count(top_text_local) <>
                expected_units) then
            begin
                Continue;
            end;

            top_units_local := split_text_units(top_text_local);
            if Length(top_units_local) <> expected_units then
            begin
                Continue;
            end;

            anchor_score_local := top_candidate_local.score;
            if derived_prefix_has_exact_tail_support_local then
            begin
                Inc(anchor_score_local, 100000);
            end;
            if anchor_score_local > best_anchor_score_local then
            begin
                best_anchor_score_local := anchor_score_local;
                best_anchor_idx_local := candidate_idx_local;
            end;
        end;

        if best_anchor_idx_local < 0 then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[best_anchor_idx_local];
        top_text_local := Trim(top_candidate_local.text);
        top_units_local := split_text_units(top_text_local);
        if Length(top_units_local) <> expected_units then
        begin
            Exit;
        end;

        prefix_text_local := '';
        for unit_idx_local := 0 to High(top_units_local) - 1 do
        begin
            prefix_text_local := prefix_text_local + top_units_local[unit_idx_local];
        end;
        tail_key_local := normalize_pinyin_text(Trim(
            syllables_local[High(syllables_local)].text));
        if (prefix_text_local = '') or (tail_key_local = '') then
        begin
            Exit;
        end;

        existing_idx_local := find_existing_prefix_partial_local;
        insert_idx_local := get_prefix_partial_insert_index_local;
        if existing_idx_local = insert_idx_local then
        begin
            Exit;
        end;
        if existing_idx_local >= 0 then
        begin
            move_prefix_partial_to_index_local(existing_idx_local, insert_idx_local);
            Exit;
        end;

        partial_candidate_local := top_candidate_local;
        partial_candidate_local.text := prefix_text_local;
        partial_candidate_local.comment := tail_key_local;
        partial_candidate_local.score := Max(0, top_candidate_local.score - 80);
        partial_candidate_local.has_dict_weight := False;
        partial_candidate_local.dict_weight := 0;

        SetLength(m_candidates, Length(m_candidates) + 1);
        if Length(m_candidate_segment_paths) < Length(m_candidates) then
        begin
            SetLength(m_candidate_segment_paths, Length(m_candidates));
        end;
        for move_idx_local := High(m_candidates) downto insert_idx_local + 1 do
        begin
            m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
            if move_idx_local < Length(m_candidate_segment_paths) then
            begin
                m_candidate_segment_paths[move_idx_local] :=
                    m_candidate_segment_paths[move_idx_local - 1];
            end;
        end;
        m_candidates[insert_idx_local] := partial_candidate_local;
        if insert_idx_local < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[insert_idx_local] := '';
        end;
    end;

    procedure append_long_display_first_syllable_partials_to_fill_page;
    const
        c_partial_penalty_per_tail_syllable = 120;
        c_append_order_penalty = 4;
    var
        local_syllables: TncPinyinParseResult;
        first_key_local: string;
        tail_key_local: string;
        lookup_results_local: TncCandidateList;
        source_candidate_local: TncCandidate;
        partial_candidate_local: TncCandidate;
        lookup_idx_local: Integer;
        syllable_idx_local: Integer;
        appended_count_local: Integer;
        source_weight_local: Integer;

        function is_bmp_cjk_single_text_local(const text_value: string): Boolean;
        var
            normalized_text_local: string;
            codepoint_local: Integer;
        begin
            Result := False;
            normalized_text_local := Trim(text_value);
            if Length(normalized_text_local) <> 1 then
            begin
                Exit;
            end;

            codepoint_local := Ord(normalized_text_local[1]);
            Result := (codepoint_local >= $4E00) and (codepoint_local <= $9FFF);
        end;

        function display_candidate_exists_local(const text_value: string;
            const comment_value: string): Boolean;
        var
            scan_idx_local: Integer;
        begin
            Result := False;
            for scan_idx_local := 0 to High(m_candidates) do
            begin
                if SameText(Trim(m_candidates[scan_idx_local].text),
                    text_value) and
                    SameText(normalize_pinyin_text(Trim(
                    m_candidates[scan_idx_local].comment)), comment_value) then
                begin
                    Exit(True);
                end;
            end;
        end;

        function has_long_display_anchor_local: Boolean;
        var
            scan_idx_local: Integer;
            text_units_local: Integer;
            comment_units_local: Integer;
            comment_key_local: string;
        begin
            Result := False;
            for scan_idx_local := 0 to Min(High(m_candidates), 3) do
            begin
                text_units_local := get_candidate_text_unit_count(
                    Trim(m_candidates[scan_idx_local].text));
                comment_key_local := normalize_pinyin_text(Trim(
                    m_candidates[scan_idx_local].comment));
                comment_units_local :=
                    get_effective_compact_pinyin_unit_count(comment_key_local);
                if (text_units_local > 0) and
                    (text_units_local + comment_units_local >= expected_units - 1)
                then
                begin
                    Exit(True);
                end;
            end;
        end;

        function first_syllable_partial_total_limit_local: Integer;
        begin
            Result := c_candidate_total_limit_max;
        end;
    begin
        if (m_dictionary = nil) or (expected_units < 6) or
            (not is_full_pinyin_key(normalized_pinyin)) or
            (not has_long_display_anchor_local) then
        begin
            Exit;
        end;

        local_syllables := syllables;
        if Length(local_syllables) <> expected_units then
        begin
            local_syllables :=
                get_effective_compact_pinyin_syllables(normalized_pinyin);
        end;
        if Length(local_syllables) <> expected_units then
        begin
            Exit;
        end;

        first_key_local := normalize_pinyin_text(local_syllables[0].text);
        tail_key_local := '';
        for syllable_idx_local := 1 to High(local_syllables) do
        begin
            tail_key_local := tail_key_local + normalize_pinyin_text(
                local_syllables[syllable_idx_local].text);
        end;
        if (first_key_local = '') or (tail_key_local = '') or
            (not lookup_display_exact_cached(first_key_local,
            lookup_results_local)) then
        begin
            Exit;
        end;

        appended_count_local := 0;
        for lookup_idx_local := 0 to High(lookup_results_local) do
        begin
            if Length(m_candidates) >=
                first_syllable_partial_total_limit_local then
            begin
                Break;
            end;

            source_candidate_local := lookup_results_local[lookup_idx_local];
            if (Trim(source_candidate_local.comment) <> '') or
                (not is_bmp_cjk_single_text_local(source_candidate_local.text)) or
                display_candidate_exists_local(Trim(source_candidate_local.text),
                tail_key_local) then
            begin
                Continue;
            end;

            partial_candidate_local := source_candidate_local;
            partial_candidate_local.comment := tail_key_local;
            partial_candidate_local.has_dict_weight := False;
            partial_candidate_local.dict_weight := 0;
            source_weight_local :=
                display_candidate_effective_weight(source_candidate_local);
            partial_candidate_local.score := Max(1,
                source_weight_local -
                ((expected_units - 1) * c_partial_penalty_per_tail_syllable) -
                (appended_count_local * c_append_order_penalty));
            insert_display_candidate(partial_candidate_local, '',
                Length(m_candidates));
            Inc(appended_count_local);
        end;
    end;

    function top_display_candidate_can_use_long_fast_display_local: Boolean;
    var
        top_candidate_local: TncCandidate;
        top_text_local: string;
        top_path_local: string;
        tail_comment_local: string;
        tail_key_local: string;
        support_score_local: Integer;
        support_segments_local: Integer;
        text_units_local: Integer;
    begin
        Result := False;
        if (expected_units < 5) or
            (Length(m_candidates) = 0) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_key) > 0) then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        top_text_local := Trim(top_candidate_local.text);
        if top_text_local = '' then
        begin
            Exit;
        end;

        text_units_local := get_candidate_text_unit_count(top_text_local);
        if (text_units_local < expected_units - 2) or
            (text_units_local > expected_units) then
        begin
            Exit;
        end;

        if (Trim(top_candidate_local.comment) = '') and
            (text_units_local = expected_units) and
            is_problematic_single_char_chain_candidate_for_query(
            normalized_pinyin, top_candidate_local) then
        begin
            Exit;
        end;

        tail_comment_local := Trim(top_candidate_local.comment);
        tail_key_local := normalize_pinyin_text(tail_comment_local);
        if (tail_key_local <> '') and
            ((Length(tail_key_local) > 8) or
            (tail_key_local <> tail_comment_local)) then
        begin
            Exit;
        end;
        if tail_key_local <> '' then
        begin
            Exit(True);
        end;

        if (text_units_local = expected_units) and
            is_full_pinyin_key(normalized_pinyin) and
            (top_candidate_local.has_dict_weight or
            (top_candidate_local.score >= 20000)) then
        begin
            Exit(True);
        end;

        top_path_local := get_segment_path_for_candidate(top_candidate_local, 0);
        if top_path_local = '' then
        begin
            top_path_local := infer_segment_path_for_query_text_with_score(
                normalized_pinyin, top_text_local, support_score_local,
                support_segments_local);
        end;
        Result := top_path_local <> '';
    end;

    function has_admitted_short_complete_model_path_local: Boolean;
    var
        idx_local: Integer;
        text_local: string;
    begin
        Result := False;
        if (expected_units < 2) or (expected_units > 4) then Exit;
        for idx_local := 0 to Min(31, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx_local].comment) <> '' then Continue;
            text_local := Trim(m_candidates[idx_local].text);
            if get_candidate_text_unit_count(text_local) <> expected_units then
                Continue;
            case expected_units of
                2: Result := is_current_short_two_single_pair_candidate_supported(
                    normalized_pinyin, text_local);
                3: Result := is_current_short_three_exact_pair_candidate_supported(
                    normalized_pinyin, text_local);
                4: Result := (m_candidates[idx_local].display_kind = cdk_lm_compound) and
                    display_short_four_two_exact_path_is_supported_local(
                    m_candidates[idx_local]);
            end;
            if Result then Exit;
        end;
    end;

    function top_display_candidate_can_use_short_prefix_fast_display_local:
        Boolean;
    const
        c_min_short_prefix_fast_dict_weight = 300;
        c_probe_limit = 8;
    var
        idx_local: Integer;
        probe_candidate_local: TncCandidate;
        top_text_local: string;
        split_head_local: string;
        split_tail_local: string;
        tail_comment_local: string;
        tail_key_local: string;
        text_units_local: Integer;
        head_key_local: string;
        standalone_tail_key_local: string;
        saw_tail_candidate_local: Boolean;
        allow_lower_tail_probe_local: Boolean;

        procedure note_short_prefix_fast_reject_local(const reason: string);
        begin
            if not m_config.debug_mode then
            begin
                Exit;
            end;
            if m_last_lookup_debug_extra <> '' then
            begin
                m_last_lookup_debug_extra := m_last_lookup_debug_extra + ' ';
            end;
            m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                'spf=' + reason;
        end;

        function candidate_can_use_short_prefix_fast_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := False;
            top_text_local := Trim(candidate_value.text);
            tail_comment_local := Trim(candidate_value.comment);
            if (tail_comment_local = '') and
                split_trailing_ascii_candidate(top_text_local, split_head_local,
                split_tail_local) then
            begin
                top_text_local := Trim(split_head_local);
                tail_comment_local := Trim(split_tail_local);
            end;
            if (top_text_local = '') or (tail_comment_local = '') then
            begin
                Exit;
            end;
            saw_tail_candidate_local := True;

            tail_key_local := normalize_pinyin_text(tail_comment_local);
            if (tail_key_local = '') or (tail_key_local <> tail_comment_local) or
                (Length(tail_key_local) > 8) then
            begin
                Exit;
            end;

            text_units_local := get_candidate_text_unit_count(top_text_local);
            if (text_units_local <= 0) or (text_units_local >= expected_units) then
            begin
                Exit;
            end;

            head_key_local := build_display_query_key(0, text_units_local);
            if head_key_local = '' then
            begin
                Exit;
            end;

            if text_units_local = 1 then
            begin
                Result := m_dictionary.single_char_matches_pinyin(head_key_local,
                    top_text_local);
            end
            else
            begin
                Result := display_exact_key_has_text(head_key_local, top_text_local) or
                    m_dictionary.is_base_entry(head_key_local, top_text_local) or
                    m_dictionary.is_user_entry(head_key_local, top_text_local);
                if (not Result) and
                    (candidate_value.dict_weight >=
                    c_min_short_prefix_fast_dict_weight) then
                begin
                    Result := True;
                end;
            end;
        end;
    begin
        Result := False;
        if (expected_units < 2) or (expected_units > 4) or
            (Length(m_candidates) = 0) or (m_dictionary = nil) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_key) > 0) then
        begin
            note_short_prefix_fast_reject_local('guard');
            Exit;
        end;
        // A prefix-only display shortcut must not reinterpret an admitted model
        // path as untrusted composition, even if its seed used a prefix shortcut.
        if has_admitted_short_complete_model_path_local then
        begin
            note_short_prefix_fast_reject_local('modelpath');
            Exit;
        end;
        if (normalized_pinyin = 'naer') or (normalized_pinyin = 'ner') or
            (normalized_pinyin = 'qunaer') or (normalized_pinyin = 'qner') or
            (normalized_pinyin = 'nli') or (normalized_pinyin = 'qnli') then
        begin
            note_short_prefix_fast_reject_local('location');
            Exit;
        end;
        if (expected_units = 4) and (Length(syllables) = expected_units) then
        begin
            standalone_tail_key_local := normalize_pinyin_text(
                syllables[High(syllables)].text);
            if (standalone_tail_key_local = 'de') or
                (standalone_tail_key_local = 'le') or
                (standalone_tail_key_local = 'zhe') or
                (standalone_tail_key_local = 'la') or
                (standalone_tail_key_local = 'ba') or
                (standalone_tail_key_local = 'ma') or
                (standalone_tail_key_local = 'a') then
            begin
                note_short_prefix_fast_reject_local('particle');
                Exit;
            end;
        end;

        saw_tail_candidate_local := False;
        probe_candidate_local := m_candidates[0];
        if candidate_can_use_short_prefix_fast_local(probe_candidate_local) then
        begin
            Result := True;
            Exit;
        end;

        allow_lower_tail_probe_local := False;
        if (not saw_tail_candidate_local) and
            (m_last_lookup_prefix_partial_fast or
            (Pos('shortprefixraw=1', m_last_lookup_debug_extra) > 0) or
            (Pos('short4prefixraw=1', m_last_lookup_debug_extra) > 0) or
            (Pos('prefixpartialfast=1', m_last_lookup_debug_extra) > 0)) then
        begin
            top_text_local := Trim(probe_candidate_local.text);
            tail_comment_local := Trim(probe_candidate_local.comment);
            if (tail_comment_local = '') and
                (top_text_local <> '') and
                (get_candidate_text_unit_count(top_text_local) >=
                expected_units) then
            begin
                allow_lower_tail_probe_local := True;
            end;
        end;

        if allow_lower_tail_probe_local then
        begin
            for idx_local := 1 to Min(c_probe_limit - 1, High(m_candidates)) do
            begin
                probe_candidate_local := m_candidates[idx_local];
                if candidate_can_use_short_prefix_fast_local(
                    probe_candidate_local) then
                begin
                    Result := True;
                    Exit;
                end;
            end;
        end;

        if saw_tail_candidate_local then
        begin
            note_short_prefix_fast_reject_local('exact');
        end
        else
        begin
            note_short_prefix_fast_reject_local('notail');
        end;
    end;

    procedure promote_best_short_prefix_fast_candidate_local;
    var
        idx_local: Integer;
        best_idx_local: Integer;
        best_rank_local: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        split_head_local: string;
        split_tail_local: string;
        candidate_units_local: Integer;
        head_key_local: string;
        tail_key_local: string;
        rank_local: Integer;
        moved_candidate_local: TncCandidate;
        moved_path_local: string;
        move_idx_local: Integer;
    begin
        if (expected_units < 2) or (expected_units > 4) or
            (Length(m_candidates) <= 1) or (m_dictionary = nil) or
            (Pos('''', m_composition_text) > 0) or
            (Pos('''', m_last_lookup_key) > 0) then
        begin
            Exit;
        end;

        if has_admitted_short_complete_model_path_local then Exit;

        best_idx_local := -1;
        best_rank_local := Low(Integer);
        for idx_local := 0 to Min(31, High(m_candidates)) do
        begin
            candidate_text_local := Trim(m_candidates[idx_local].text);
            candidate_tail_local := Trim(m_candidates[idx_local].comment);
            if (candidate_tail_local = '') and
                split_trailing_ascii_candidate(candidate_text_local,
                split_head_local, split_tail_local) then
            begin
                candidate_text_local := Trim(split_head_local);
                candidate_tail_local := Trim(split_tail_local);
            end;
            if (candidate_text_local = '') or (candidate_tail_local = '') then
            begin
                Continue;
            end;

            tail_key_local := normalize_pinyin_text(candidate_tail_local);
            if (tail_key_local = '') or (tail_key_local <> candidate_tail_local) or
                (Length(tail_key_local) > 8) then
            begin
                Continue;
            end;

            candidate_units_local := get_candidate_text_unit_count(candidate_text_local);
            if (candidate_units_local < 2) or
                (candidate_units_local >= expected_units) then
            begin
                Continue;
            end;

            head_key_local := build_display_query_key(0, candidate_units_local);
            if (head_key_local = '') or
                (not (display_exact_key_has_text(head_key_local,
                candidate_text_local) or m_dictionary.is_base_entry(
                head_key_local, candidate_text_local) or
                m_dictionary.is_user_entry(head_key_local,
                candidate_text_local))) then
            begin
                Continue;
            end;

            rank_local := m_candidates[idx_local].dict_weight;
            if m_candidates[idx_local].source = cs_user then
            begin
                Inc(rank_local, 100000);
            end;
            if rank_local > best_rank_local then
            begin
                best_rank_local := rank_local;
                best_idx_local := idx_local;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        moved_candidate_local := m_candidates[best_idx_local];
        moved_path_local := '';
        if best_idx_local < Length(m_candidate_segment_paths) then
        begin
            moved_path_local := m_candidate_segment_paths[best_idx_local];
        end;

        for move_idx_local := best_idx_local downto 1 do
        begin
            m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
            if move_idx_local < Length(m_candidate_segment_paths) then
            begin
                if move_idx_local - 1 < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx_local] :=
                        m_candidate_segment_paths[move_idx_local - 1];
                end
                else
                begin
                    m_candidate_segment_paths[move_idx_local] := '';
                end;
            end;
        end;
        m_candidates[0] := moved_candidate_local;
        if Length(m_candidate_segment_paths) > 0 then
        begin
            m_candidate_segment_paths[0] := moved_path_local;
        end;
    end;

    function has_strong_short_complete_candidate_for_prefix_fast_local: Boolean;
    const
        c_probe_limit = 8;
        c_min_complete_score = 15000;
        c_min_override_prefix_rank = 1200;
    var
        idx_local: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        split_head_local: string;
        split_tail_local: string;
        candidate_units_local: Integer;
        head_key_local: string;
        tail_key_local: string;
        prefix_rank_local: Integer;
    begin
        Result := False;
        if (expected_units < 2) or (expected_units > 3) or
            (m_dictionary = nil) then
        begin
            Exit;
        end;

        for idx_local := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            candidate_text_local := Trim(m_candidates[idx_local].text);
            candidate_tail_local := Trim(m_candidates[idx_local].comment);
            if (candidate_tail_local = '') and
                split_trailing_ascii_candidate(candidate_text_local,
                split_head_local, split_tail_local) then
            begin
                candidate_text_local := Trim(split_head_local);
                candidate_tail_local := Trim(split_tail_local);
            end;

            if (candidate_text_local = '') or (candidate_tail_local = '') then
            begin
                Continue;
            end;

            tail_key_local := normalize_pinyin_text(candidate_tail_local);
            if (tail_key_local = '') or (tail_key_local <> candidate_tail_local) or
                (Length(tail_key_local) > 8) then
            begin
                Continue;
            end;

            candidate_units_local := get_candidate_text_unit_count(
                candidate_text_local);
            if (candidate_units_local < 2) or
                (candidate_units_local >= expected_units) then
            begin
                Continue;
            end;

            head_key_local := build_display_query_key(0, candidate_units_local);
            if (head_key_local = '') or
                (not (display_exact_key_has_text(head_key_local,
                candidate_text_local) or m_dictionary.is_base_entry(
                head_key_local, candidate_text_local) or
                m_dictionary.is_user_entry(head_key_local,
                candidate_text_local))) then
            begin
                Continue;
            end;

            prefix_rank_local := m_candidates[idx_local].dict_weight;
            if m_candidates[idx_local].source = cs_user then
            begin
                Inc(prefix_rank_local, 100000);
            end;
            if (prefix_rank_local >= c_min_override_prefix_rank) and
                ((tail_key_local = 'g') or (tail_key_local = 'gu') or
                (tail_key_local = 'guo')) then
            begin
                Exit(False);
            end;
        end;

        for idx_local := 0 to Min(c_probe_limit - 1, High(m_candidates)) do
        begin
            if Trim(m_candidates[idx_local].comment) <> '' then
            begin
                Continue;
            end;

            candidate_text_local := Trim(m_candidates[idx_local].text);
            if (candidate_text_local <> '') and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                (m_candidates[idx_local].score >= c_min_complete_score) then
            begin
                Exit(True);
            end;
        end;
    end;

    procedure filter_short_prefix_fast_constructed_candidates_local;
    var
        in_idx_local: Integer;
        out_idx_local: Integer;
        candidate_text_local: string;
        candidate_tail_local: string;
        split_head_local: string;
        split_tail_local: string;
        candidate_units_local: Integer;
        head_key_local: string;
        keep_candidate_local: Boolean;
    begin
        if (m_dictionary = nil) or (Length(m_candidates) = 0) then
        begin
            Exit;
        end;

        out_idx_local := 0;
        for in_idx_local := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[in_idx_local].text);
            candidate_tail_local := Trim(m_candidates[in_idx_local].comment);
            if (candidate_tail_local = '') and
                split_trailing_ascii_candidate(candidate_text_local,
                split_head_local, split_tail_local) then
            begin
                candidate_text_local := Trim(split_head_local);
                candidate_tail_local := Trim(split_tail_local);
            end;

            keep_candidate_local := True;
            candidate_units_local :=
                get_candidate_text_unit_count(candidate_text_local);
            if (candidate_tail_local <> '') and (candidate_units_local >= 2) and
                (candidate_units_local < expected_units) and
                (not (is_fuzzy_pinyin_active and
                (m_candidates[in_idx_local].fuzzy_cost > 0))) then
            begin
                head_key_local := build_display_query_key(0,
                    candidate_units_local);
                keep_candidate_local :=
                    (alternative_exact_prefix_units(m_candidates[in_idx_local]) > 0) or
                    display_exact_key_has_text(head_key_local,
                    candidate_text_local) or
                    m_dictionary.is_base_entry(head_key_local,
                    candidate_text_local) or
                    m_dictionary.is_user_entry(head_key_local,
                    candidate_text_local);

            end;

            if not keep_candidate_local then
            begin
                Continue;
            end;

            if out_idx_local <> in_idx_local then
            begin
                m_candidates[out_idx_local] := m_candidates[in_idx_local];
                if out_idx_local < Length(m_candidate_segment_paths) then
                begin
                    if in_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[out_idx_local] :=
                            m_candidate_segment_paths[in_idx_local];
                    end
                    else
                    begin
                        m_candidate_segment_paths[out_idx_local] := '';
                    end;
                end;
            end;
            Inc(out_idx_local);
        end;

        if out_idx_local < Length(m_candidates) then
        begin
            SetLength(m_candidates, out_idx_local);
        end;
        if Length(m_candidate_segment_paths) > out_idx_local then
        begin
            SetLength(m_candidate_segment_paths, out_idx_local);
        end;
    end;

    procedure ensure_fuzzy_short_single_prefixes_visible_local;
    const
        c_max_fuzzy_single_prefix_reserve = 3;
    var
        page_limit_local: Integer;
        reserve_limit_local: Integer;
        fuzzy_in_page_local: Integer;
        missing_local: Integer;
        source_idx_local: Integer;
        target_idx_local: Integer;
        scan_idx_local: Integer;
        candidate_tmp_local: TncCandidate;
        path_tmp_local: string;

        function is_fuzzy_single_prefix_local(
            const candidate_value: TncCandidate): Boolean;
        var
            tail_units_local: Integer;
        begin
            Result := False;
            if (candidate_value.fuzzy_cost <= 0) or
                (Trim(candidate_value.comment) = '') or
                (get_candidate_text_unit_count(Trim(candidate_value.text)) <> 1) then
            begin
                Exit;
            end;

            tail_units_local := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(Trim(candidate_value.comment)));
            Result := tail_units_local > 0;
            if Result and (expected_units > 0) then
            begin
                Result := 1 + tail_units_local = expected_units;
            end;
        end;

        function should_keep_page_slot_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := is_fuzzy_single_prefix_local(candidate_value) or
                ((candidate_value.fuzzy_cost > 0) and
                (Trim(candidate_value.comment) = ''));
        end;
    begin
        if (not is_fuzzy_pinyin_active) or (expected_units < 2) or
            (Length(m_candidates) <= 1) then
        begin
            Exit;
        end;

        page_limit_local := Min(get_candidate_page_size,
            Length(m_candidates));
        if page_limit_local <= 1 then
        begin
            Exit;
        end;
        reserve_limit_local := Min(c_max_fuzzy_single_prefix_reserve,
            page_limit_local - 1);

        fuzzy_in_page_local := 0;
        for scan_idx_local := 0 to page_limit_local - 1 do
        begin
            if is_fuzzy_single_prefix_local(m_candidates[scan_idx_local]) then
            begin
                Inc(fuzzy_in_page_local);
            end;
        end;
        missing_local := reserve_limit_local - fuzzy_in_page_local;
        if missing_local <= 0 then
        begin
            Exit;
        end;

        target_idx_local := page_limit_local - missing_local;
        while missing_local > 0 do
        begin
            while (target_idx_local < page_limit_local) and
                should_keep_page_slot_local(m_candidates[target_idx_local]) do
            begin
                Inc(target_idx_local);
            end;
            if target_idx_local >= page_limit_local then
            begin
                Break;
            end;

            source_idx_local := -1;
            for scan_idx_local := page_limit_local to High(m_candidates) do
            begin
                if is_fuzzy_single_prefix_local(m_candidates[scan_idx_local]) then
                begin
                    source_idx_local := scan_idx_local;
                    Break;
                end;
            end;
            if source_idx_local < 0 then
            begin
                Break;
            end;

            candidate_tmp_local := m_candidates[target_idx_local];
            m_candidates[target_idx_local] := m_candidates[source_idx_local];
            m_candidates[source_idx_local] := candidate_tmp_local;
            if (target_idx_local < Length(m_candidate_segment_paths)) and
                (source_idx_local < Length(m_candidate_segment_paths)) then
            begin
                path_tmp_local := m_candidate_segment_paths[target_idx_local];
                m_candidate_segment_paths[target_idx_local] :=
                    m_candidate_segment_paths[source_idx_local];
                m_candidate_segment_paths[source_idx_local] := path_tmp_local;
            end;
            Inc(target_idx_local);
            Dec(missing_local);
        end;
    end;

    procedure normalize_first_syllable_single_partial_order_local;
    var
        first_key_local: string;
        remaining_key_local: string;
        syllable_idx_local: Integer;
        candidate_idx_local: Integer;
        first_single_idx_local: Integer;
        best_single_idx_local: Integer;
        candidate_text_local: string;
        candidate_comment_local: string;
        candidate_weight_local: Integer;
        best_weight_local: Integer;
        moved_candidate_local: TncCandidate;
        moved_path_local: string;
        move_idx_local: Integer;
    begin
        if (m_page_index <> 0) or (m_dictionary = nil) or
            (expected_units < 2) or
            (expected_units > 3) or
            (Length(syllables) <> expected_units) or
            (Length(m_candidates) <= 1) then
        begin
            Exit;
        end;

        first_key_local := normalize_pinyin_text(syllables[0].text);
        if first_key_local = '' then
        begin
            Exit;
        end;

        remaining_key_local := '';
        for syllable_idx_local := 1 to High(syllables) do
        begin
            remaining_key_local := remaining_key_local +
                normalize_pinyin_text(syllables[syllable_idx_local].text);
        end;
        if remaining_key_local = '' then
        begin
            Exit;
        end;

        first_single_idx_local := -1;
        best_single_idx_local := -1;
        best_weight_local := Low(Integer);
        for candidate_idx_local := 0 to High(m_candidates) do
        begin
            candidate_text_local := Trim(m_candidates[candidate_idx_local].text);
            candidate_comment_local := normalize_pinyin_text(
                Trim(m_candidates[candidate_idx_local].comment));
            if (candidate_comment_local <> remaining_key_local) or
                (get_candidate_text_unit_count(candidate_text_local) <> 1) or
                (not m_dictionary.single_char_matches_pinyin(first_key_local,
                candidate_text_local)) then
            begin
                Continue;
            end;

            if first_single_idx_local < 0 then
            begin
                first_single_idx_local := candidate_idx_local;
            end;

            candidate_weight_local := m_candidates[candidate_idx_local].score;
            if m_candidates[candidate_idx_local].has_dict_weight and
                (m_candidates[candidate_idx_local].dict_weight >
                candidate_weight_local) then
            begin
                candidate_weight_local :=
                    m_candidates[candidate_idx_local].dict_weight;
            end;
            if m_candidates[candidate_idx_local].source = cs_user then
            begin
                Inc(candidate_weight_local, c_user_score_bonus);
            end;

            if candidate_weight_local > best_weight_local then
            begin
                best_weight_local := candidate_weight_local;
                best_single_idx_local := candidate_idx_local;
            end;
        end;

        if (first_single_idx_local < 0) or
            (best_single_idx_local <= first_single_idx_local) then
        begin
            Exit;
        end;

        moved_candidate_local := m_candidates[best_single_idx_local];
        moved_path_local := '';
        if best_single_idx_local < Length(m_candidate_segment_paths) then
        begin
            moved_path_local := m_candidate_segment_paths[best_single_idx_local];
        end;
        for move_idx_local := best_single_idx_local downto
            first_single_idx_local + 1 do
        begin
            m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
            if move_idx_local < Length(m_candidate_segment_paths) then
            begin
                if move_idx_local - 1 < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[move_idx_local] :=
                        m_candidate_segment_paths[move_idx_local - 1];
                end
                else
                begin
                    m_candidate_segment_paths[move_idx_local] := '';
                end;
            end;
        end;
        m_candidates[first_single_idx_local] := moved_candidate_local;
        if first_single_idx_local < Length(m_candidate_segment_paths) then
        begin
            m_candidate_segment_paths[first_single_idx_local] := moved_path_local;
        end;
    end;

    function display_short_complete_has_full_query_support(
        const candidate_value: TncCandidate; const text_value: string): Boolean;
    var
        prefix_results_local: TncCandidateList;
        prefix_idx_local: Integer;
        text_units_local: Integer;
        transition_particle_path_local: string;
        transition_particle_evidence_local: Integer;

        function predictive_prefix_units_allowed_local: Boolean;
        begin
            Result := False;
            if (text_units_local <= 0) or (expected_units <= 0) then
            begin
                Exit;
            end;

            if (expected_units <= 3) and (text_units_local = expected_units) then
            begin
                Exit(True);
            end;

            if (expected_units = 4) and (text_units_local = 3) then
            begin
                Exit(True);
            end;
        end;
    begin
        Result := False;
        if (m_dictionary = nil) or (normalized_pinyin = '') or
            (text_value = '') then
        begin
            Exit;
        end;

        if display_exact_key_has_text(normalized_pinyin, text_value) then
        begin
            Exit(True);
        end;

        if m_dictionary.is_user_entry(normalized_pinyin, text_value) then
        begin
            Exit(True);
        end;

        // Four-syllable short queries may have a reliable two-word path even
        // when the complete text is not itself a dictionary entry. Reuse the
        // same exact-pair gate as the candidate filter so paging cannot hide a
        // path that lookup already accepted.
        if (expected_units = 4) and (Length(syllables) = 4) and
            (Trim(candidate_value.comment) = '') and
            (get_candidate_text_unit_count(text_value) = expected_units) and
            display_short_four_two_exact_path_is_supported_local(
            candidate_value) then
        begin
            Exit(True);
        end;

        if (expected_units = 4) and (Length(syllables) = 4) and
            (Trim(candidate_value.comment) = '') and
            (get_candidate_text_unit_count(text_value) = expected_units) and
            get_short_four_transition_particle_path_evidence(
            normalized_pinyin, text_value, transition_particle_path_local,
            transition_particle_evidence_local) then
        begin
            Exit(True);
        end;

        text_units_local := get_candidate_text_unit_count(text_value);
        if (Trim(candidate_value.comment) <> '') or
            (not predictive_prefix_units_allowed_local) or
            (not m_dictionary.lookup_full_pinyin_prefix(normalized_pinyin,
            prefix_results_local)) then
        begin
            Exit;
        end;

        for prefix_idx_local := 0 to High(prefix_results_local) do
        begin
            if (Trim(prefix_results_local[prefix_idx_local].comment) = '') and
                SameText(Trim(prefix_results_local[prefix_idx_local].text),
                text_value) then
            begin
                Exit(True);
            end;
        end;
    end;

    function join_display_units_local(const units_value: TArray<string>;
        const start_idx: Integer; const end_idx: Integer): string;
    var
        unit_idx: Integer;
    begin
        Result := '';
        if (start_idx < 0) or (end_idx < start_idx) or
            (end_idx >= Length(units_value)) then
        begin
            Exit;
        end;
        for unit_idx := start_idx to end_idx do
        begin
            Result := Result + units_value[unit_idx];
        end;
    end;

    function display_short_complete_matches_fixed_prefix_rule(
        const query_prefix: string; const text_prefix: string;
        const text_value: string): Boolean;
    var
        text_units: TArray<string>;
        tail_key: string;
        tail_text: string;
    begin
        Result := False;
        if (normalized_pinyin = '') or
            (Length(normalized_pinyin) <= Length(query_prefix)) or
            (Copy(normalized_pinyin, 1, Length(query_prefix)) <>
            query_prefix) then
        begin
            Exit;
        end;

        text_units := split_text_units(text_value);
        if (Length(text_units) < 2) or
            (not SameText(text_units[0], text_prefix)) then
        begin
            Exit;
        end;

        tail_key := Copy(normalized_pinyin, Length(query_prefix) + 1, MaxInt);
        tail_text := join_display_units_local(text_units, 1, High(text_units));
        Result := display_exact_key_has_text(tail_key, tail_text) or
            ((m_dictionary <> nil) and
            m_dictionary.is_user_entry(tail_key, tail_text));
    end;

    function display_short_complete_matches_particle_suffix_rule(
        const query_suffix: string; const text_suffix: string;
        const text_value: string): Boolean;
    var
        text_units: TArray<string>;
        suffix_units: Integer;
        head_key: string;
        head_text: string;
    begin
        Result := False;
        if (normalized_pinyin = '') or
            (Length(normalized_pinyin) <= Length(query_suffix)) or
            (Copy(normalized_pinyin,
            Length(normalized_pinyin) - Length(query_suffix) + 1,
            Length(query_suffix)) <> query_suffix) then
        begin
            Exit;
        end;

        text_units := split_text_units(text_value);
        suffix_units := get_candidate_text_unit_count(text_suffix);
        if (suffix_units <= 0) or (Length(text_units) <= suffix_units) or
            (not SameText(join_display_units_local(text_units,
            Length(text_units) - suffix_units, High(text_units)), text_suffix)) then
        begin
            Exit;
        end;

        head_key := Copy(normalized_pinyin, 1,
            Length(normalized_pinyin) - Length(query_suffix));
        head_text := join_display_units_local(text_units, 0,
            High(text_units) - suffix_units);
        Result := display_exact_key_has_text(head_key, head_text) or
            ((m_dictionary <> nil) and
            m_dictionary.is_user_entry(head_key, head_text));
    end;

    function display_short_complete_matches_dehao_rule_text(
        const text_value: string): Boolean;
    var
        text_units: TArray<string>;
        head_key: string;
        head_text: string;
    begin
        Result := False;
        if (normalized_pinyin = '') or
            (Length(normalized_pinyin) <= Length('dehao')) or
            (not SameText(Copy(normalized_pinyin,
            Length(normalized_pinyin) - Length('dehao') + 1,
            Length('dehao')), 'dehao')) then
        begin
            Exit;
        end;

        text_units := split_text_units(text_value);
        if (Length(text_units) < 3) or
            (not SameText(text_units[High(text_units) - 1],
            string(Char($5F97)))) or
            (not SameText(text_units[High(text_units)],
            string(Char($597D)))) then
        begin
            Exit;
        end;

        head_key := Copy(normalized_pinyin, 1,
            Length(normalized_pinyin) - Length('hao'));
        head_text := join_display_units_local(text_units, 0,
            High(text_units) - 1);
        Result := display_exact_key_has_text(head_key, head_text) or
            ((m_dictionary <> nil) and
            m_dictionary.is_user_entry(head_key, head_text));
    end;

    function display_short_complete_is_allowed_rule_text(
        const text_value: string): Boolean;
    begin
        Result :=
            display_short_complete_matches_fixed_prefix_rule('wo',
            string(Char($6211)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('ni',
            string(Char($4F60)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('zui',
            string(Char($6700)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('bu',
            string(Char($4E0D)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('hen',
            string(Char($5F88)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('jiu',
            string(Char($5C31)), text_value) or
            display_short_complete_matches_fixed_prefix_rule('qing',
            string(Char($8BF7)), text_value) or
            display_short_complete_matches_particle_suffix_rule('de',
            string(Char($7684)), text_value) or
            display_short_complete_matches_particle_suffix_rule('le',
            string(Char($4E86)), text_value) or
            display_short_complete_matches_particle_suffix_rule('zhe',
            string(Char($7740)), text_value) or
            display_short_complete_matches_particle_suffix_rule('la',
            string(Char($5566)), text_value) or
            display_short_complete_matches_particle_suffix_rule('ma',
            string(Char($5417)), text_value) or
            display_short_complete_matches_particle_suffix_rule('ba',
            string(Char($5427)), text_value) or
            display_short_complete_matches_particle_suffix_rule('a',
            string(Char($554A)), text_value) or
            display_short_complete_matches_dehao_rule_text(text_value) or
            ((normalized_pinyin = 'dema') and SameText(text_value,
            string(Char($7684)) + string(Char($5417)))) or
            ((normalized_pinyin = 'deba') and SameText(text_value,
            string(Char($7684)) + string(Char($5427)))) or
            ((normalized_pinyin = 'dele') and SameText(text_value,
            string(Char($7684)) + string(Char($4E86)))) or
            ((normalized_pinyin = 'dehao') and SameText(text_value,
            string(Char($5F97)) + string(Char($597D)))) or
            ((normalized_pinyin = 'leba') and SameText(text_value,
            string(Char($4E86)) + string(Char($5427)))) or
            ((normalized_pinyin = 'lema') and SameText(text_value,
            string(Char($4E86)) + string(Char($5417)))) or
            (((normalized_pinyin = 'naer') or
            (normalized_pinyin = 'ner')) and SameText(text_value,
            string(Char($54EA)) + string(Char($513F)))) or
            (((normalized_pinyin = 'qunaer') or
            (normalized_pinyin = 'qner')) and SameText(text_value,
            string(Char($53BB)) + string(Char($54EA)) +
            string(Char($513F)))) or
            ((normalized_pinyin = 'nli') and SameText(text_value,
            string(Char($54EA)) + string(Char($91CC)))) or
            ((normalized_pinyin = 'qnli') and SameText(text_value,
            string(Char($53BB)) + string(Char($54EA)) +
            string(Char($91CC))));
    end;

    function display_short_complete_matches_tail_phrase_rule(
        const query_suffix: string; const text_suffix: string;
        const text_value: string): Boolean;
    var
        text_units: TArray<string>;
        head_key: string;
        head_text: string;
        suffix_units: Integer;
    begin
        Result := False;
        if (normalized_pinyin = '') or
            (Length(normalized_pinyin) <= Length(query_suffix)) or
            (Copy(normalized_pinyin,
            Length(normalized_pinyin) - Length(query_suffix) + 1,
            Length(query_suffix)) <> query_suffix) then
        begin
            Exit;
        end;

        text_units := split_text_units(text_value);
        suffix_units := get_candidate_text_unit_count(text_suffix);
        if (Length(text_units) <= suffix_units) or
            (not SameText(join_display_units_local(text_units,
            Length(text_units) - suffix_units, High(text_units)),
            text_suffix)) then
        begin
            Exit;
        end;

        head_key := Copy(normalized_pinyin, 1,
            Length(normalized_pinyin) - Length(query_suffix));
        head_text := join_display_units_local(text_units, 0,
            High(text_units) - suffix_units);
        Result := display_exact_key_has_text(head_key, head_text) or
            ((m_dictionary <> nil) and
            m_dictionary.is_user_entry(head_key, head_text));
    end;

    function display_candidate_should_drop_short_nonlexicon_complete(
        const candidate_value: TncCandidate; const source_index: Integer): Boolean;
    var
        text_value: string;
        text_units: Integer;
    begin
        Result := False;
        if (expected_units < 3) or (expected_units > 4) or
            (m_dictionary = nil) or (normalized_pinyin = '') or
            (not is_full_pinyin_key(normalized_pinyin)) or
            (Trim(candidate_value.comment) <> '') then
        begin
            Exit;
        end;

        text_value := Trim(candidate_value.text);
        text_units := get_candidate_text_unit_count(text_value);
        if text_value = '' then
        begin
            Exit;
        end;
        if is_current_short_exact_lexicon_completion(normalized_pinyin,
            text_value) then
        begin
            Exit;
        end;

        // This candidate was admitted from two exact dictionary segments by
        // the short three-syllable transition gate. Reuse that single verdict
        // instead of treating the same path as an arbitrary composition here.
        if is_current_short_three_exact_pair_candidate_supported(
            normalized_pinyin, text_value) then
        begin
            Exit;
        end;

        if display_short_complete_has_full_query_support(candidate_value,
            text_value) or
            display_short_complete_is_allowed_rule_text(text_value) or
            display_candidate_is_supported_pronoun_connector_tail_complete_local(
            candidate_value) then
        begin
            Exit;
        end;

        if text_units <> expected_units then
        begin
            Result := True;
            Exit;
        end;

        if display_short_complete_matches_tail_phrase_rule('dema',
            string(Char($7684)) + string(Char($5417)), text_value) or
            display_short_complete_matches_tail_phrase_rule('deba',
            string(Char($7684)) + string(Char($5427)), text_value) or
            display_short_complete_matches_tail_phrase_rule('dele',
            string(Char($7684)) + string(Char($4E86)), text_value) or
            display_short_complete_matches_tail_phrase_rule('dehao',
            string(Char($5F97)) + string(Char($597D)), text_value) or
            display_short_complete_matches_tail_phrase_rule('leba',
            string(Char($4E86)) + string(Char($5427)), text_value) or
            display_short_complete_matches_tail_phrase_rule('lema',
            string(Char($4E86)) + string(Char($5417)), text_value) then
        begin
            Exit;
        end;

        Result := True;
    end;

    function display_candidate_should_drop_short_invalid_partial(
        const candidate_value: TncCandidate): Boolean;
    var
        tail_syllables: TncPinyinParseResult;
        consumed_units: Integer;
        text_units: Integer;
    begin
        Result := False;
        if umlaut_raw_prefix_units(candidate_value) > 0 then Exit;
        if (expected_units < 3) or (expected_units > 6) or
            (normalized_pinyin = '') or
            (not is_full_pinyin_key(normalized_pinyin)) or
            (Trim(candidate_value.comment) = '') then
        begin
            Exit;
        end;

        tail_syllables := get_effective_compact_pinyin_syllables(
            normalize_pinyin_text(candidate_value.comment));
        if Length(tail_syllables) <= 0 then
        begin
            Exit;
        end;

        consumed_units := expected_units - Length(tail_syllables);
        text_units := get_candidate_text_unit_count(Trim(candidate_value.text));
        Result := (consumed_units <= 0) or (text_units > consumed_units);
    end;

    procedure promote_strong_short_four_two_exact_path_visible_local(
        var candidates_value: TncCandidateList;
        var source_indices_value: TArray<Integer>);
    const
        c_strong_path_top_evidence = 1000;
    var
        idx_local: Integer;
        move_idx_local: Integer;
        best_idx_local: Integer;
        best_evidence_local: Integer;
        evidence_local: Integer;
        encoded_path_local: string;
        picked_candidate_local: TncCandidate;
        picked_source_idx_local: Integer;
        exact_results_local: TncCandidateList;

        function is_full_query_exact_text_local(
            const text_value: string): Boolean;
        var
            exact_candidate_idx_local: Integer;
        begin
            Result := False;
            for exact_candidate_idx_local := 0 to High(exact_results_local) do
            begin
                if (Trim(exact_results_local[exact_candidate_idx_local].comment) = '') and
                    SameText(Trim(exact_results_local[exact_candidate_idx_local].text),
                    Trim(text_value)) then
                begin
                    Exit(True);
                end;
            end;
        end;
    begin
        if (expected_units <> 4) or (Length(candidates_value) <= 1) or
            (Length(source_indices_value) <> Length(candidates_value)) then
        begin
            Exit;
        end;

        // With full-query exacts, the model may only reorder another exact.
        // A composed non-exact path never crosses the exact boundary.
        SetLength(exact_results_local, 0);
        lookup_display_exact_cached(normalized_pinyin, exact_results_local);
        for idx_local := 0 to High(exact_results_local) do
        begin
            if (exact_results_local[idx_local].source = cs_user) and
                (Trim(exact_results_local[idx_local].comment) = '') then
            begin
                Exit;
            end;
        end;

        best_idx_local := -1;
        best_evidence_local := 0;
        for idx_local := 0 to High(candidates_value) do
        begin
            if (Trim(candidates_value[idx_local].comment) <> '') or
                (get_candidate_text_unit_count(
                Trim(candidates_value[idx_local].text)) <> 4) or
                ((Length(exact_results_local) > 0) and
                (not is_full_query_exact_text_local(
                Trim(candidates_value[idx_local].text)))) or
                (not get_short_four_two_exact_path_evidence(
                normalized_pinyin, Trim(candidates_value[idx_local].text),
                encoded_path_local, evidence_local)) then
            begin
                Continue;
            end;
            if (best_idx_local < 0) or
                (evidence_local > best_evidence_local) or
                ((evidence_local = best_evidence_local) and
                (candidates_value[idx_local].score >
                candidates_value[best_idx_local].score)) then
            begin
                best_idx_local := idx_local;
                best_evidence_local := evidence_local;
            end;
        end;
        if (best_idx_local <= 0) or
            ((Length(exact_results_local) > 0) and
            (best_evidence_local < c_strong_path_top_evidence)) then
        begin
            Exit;
        end;

        picked_candidate_local := candidates_value[best_idx_local];
        picked_source_idx_local := source_indices_value[best_idx_local];
        for move_idx_local := best_idx_local downto 1 do
        begin
            candidates_value[move_idx_local] :=
                candidates_value[move_idx_local - 1];
            source_indices_value[move_idx_local] :=
                source_indices_value[move_idx_local - 1];
        end;
        candidates_value[0] := picked_candidate_local;
        source_indices_value[0] := picked_source_idx_local;
    end;

    procedure build_current_page_result;
    var
        page_idx: Integer;
        source_idx: Integer;
        complete_visible_texts: TDictionary<string, Boolean>;
        emitted_visible_texts: TDictionary<string, Boolean>;
        candidate_key: string;
        visible_source_indices: TArray<Integer>;
        keep_explicit_prefix_partial: Boolean;
        keep_protected_full_exact: Boolean;
        protected_full_query_exacts: TncCandidateList;
        short_exact_ranked_texts: TArray<string>;
        has_supported_transition_top_local: Boolean;
        supported_transition_top_candidate_local: TncCandidate;
        supported_transition_top_path_local: string;

        procedure insert_umlaut_raw_prefixes_local;
        var
            idx: Integer;
            insert_idx: Integer;
            prefix_units: Integer;
            existing_units: Integer;
        begin
            prepare_umlaut_raw_prefixes;
            for idx := 0 to High(umlaut_raw_prefixes) do
            begin
                if find_display_candidate(umlaut_raw_prefixes[idx].text,
                    umlaut_raw_prefixes[idx].comment) >= 0 then Continue;
                prefix_units := get_candidate_text_unit_count(umlaut_raw_prefixes[idx].text);
                insert_idx := 0;
                while insert_idx < Length(m_candidates) do
                begin
                    if Trim(m_candidates[insert_idx].comment) <> '' then
                    begin
                        existing_units := get_candidate_text_unit_count(m_candidates[insert_idx].text);
                        if existing_units < prefix_units then Break;
                        if (existing_units = prefix_units) and
                            (umlaut_raw_prefixes[idx].source = m_candidates[insert_idx].source) and
                            (umlaut_raw_prefixes[idx].score > m_candidates[insert_idx].score) then Break;
                    end;
                    Inc(insert_idx);
                end;
                // Preserve complete candidates and the source-index/page mapping.
                insert_display_candidate(umlaut_raw_prefixes[idx], '', insert_idx);
            end;
        end;

        procedure insert_short_exact_lexicon_completion_local;
        var
            candidate_idx_local: Integer;
            completion_idx_local: Integer;
            insert_idx_local: Integer;
            move_idx_local: Integer;
            old_count_local: Integer;
            insert_count_local: Integer;
            total_limit_local: Integer;
            candidate_text_local: string;
            insert_candidates_local: TncCandidateList;

            function candidate_already_present_local(
                const text_value_local: string): Boolean;
            var
                existing_idx_local: Integer;
            begin
                Result := False;
                for existing_idx_local := 0 to High(m_candidates) do
                begin
                    if (Trim(m_candidates[existing_idx_local].comment) = '') and
                        SameText(Trim(m_candidates[existing_idx_local].text),
                        text_value_local) then
                    begin
                        Exit(True);
                    end;
                end;
                for existing_idx_local := 0 to High(insert_candidates_local) do
                begin
                    if SameText(Trim(insert_candidates_local[
                        existing_idx_local].text), text_value_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (not SameText(normalized_pinyin,
                m_short_exact_lexicon_completion_query)) or
                (Length(m_short_exact_lexicon_completion_candidates) = 0) then
            begin
                Exit;
            end;

            SetLength(insert_candidates_local, 0);
            for completion_idx_local := 0 to
                High(m_short_exact_lexicon_completion_candidates) do
            begin
                candidate_text_local := Trim(
                    m_short_exact_lexicon_completion_candidates[
                    completion_idx_local].text);
                if (candidate_text_local = '') or
                    (not is_current_short_exact_lexicon_completion(
                    normalized_pinyin, candidate_text_local)) or
                    candidate_already_present_local(candidate_text_local) then
                begin
                    Continue;
                end;
                candidate_idx_local := Length(insert_candidates_local);
                SetLength(insert_candidates_local, candidate_idx_local + 1);
                insert_candidates_local[candidate_idx_local] :=
                    m_short_exact_lexicon_completion_candidates[
                    completion_idx_local];
            end;
            insert_count_local := Length(insert_candidates_local);
            if insert_count_local = 0 then
            begin
                Exit;
            end;

            insert_idx_local := 0;
            // Predictive extensions follow every full exact match and precede
            // shorter partials, so they cannot alter the existing exact order.
            while (insert_idx_local < Length(m_candidates)) and
                (Trim(m_candidates[insert_idx_local].comment) = '') and
                (get_candidate_text_unit_count(
                Trim(m_candidates[insert_idx_local].text)) = expected_units) do
            begin
                Inc(insert_idx_local);
            end;

            old_count_local := Length(m_candidates);
            if Length(m_candidate_segment_paths) < old_count_local then
            begin
                SetLength(m_candidate_segment_paths, old_count_local);
            end;
            SetLength(m_candidates, old_count_local + insert_count_local);
            SetLength(m_candidate_segment_paths,
                old_count_local + insert_count_local);
            for move_idx_local := old_count_local - 1 downto insert_idx_local do
            begin
                m_candidates[move_idx_local + insert_count_local] :=
                    m_candidates[move_idx_local];
                m_candidate_segment_paths[
                    move_idx_local + insert_count_local] :=
                    m_candidate_segment_paths[move_idx_local];
            end;
            for completion_idx_local := 0 to insert_count_local - 1 do
            begin
                m_candidates[insert_idx_local + completion_idx_local] :=
                    insert_candidates_local[completion_idx_local];
                m_candidate_segment_paths[
                    insert_idx_local + completion_idx_local] := '';
            end;
            total_limit_local := get_total_candidate_limit;
            if (total_limit_local > 0) and
                (Length(m_candidates) > total_limit_local) then
            begin
                SetLength(m_candidates, total_limit_local);
                SetLength(m_candidate_segment_paths, total_limit_local);
            end;
        end;

        procedure capture_supported_transition_top_local;
        begin
            has_supported_transition_top_local := False;
            supported_transition_top_path_local := '';
            if (m_page_index <> 0) or (Length(m_candidates) <= 0) or
                (Trim(m_candidates[0].comment) <> '') or
                ((not is_current_short_two_single_pair_candidate_supported(
                normalized_pinyin, Trim(m_candidates[0].text))) and
                (not is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, Trim(m_candidates[0].text)))) then
            begin
                Exit;
            end;

            has_supported_transition_top_local := True;
            supported_transition_top_candidate_local := m_candidates[0];
            if Length(m_candidate_segment_paths) > 0 then
            begin
                supported_transition_top_path_local :=
                    m_candidate_segment_paths[0];
            end;
        end;

        procedure restore_supported_transition_top_local;
        var
            candidate_idx_local: Integer;
            move_idx_local: Integer;
        begin
            if not has_supported_transition_top_local then
            begin
                Exit;
            end;

            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                    SameText(Trim(m_candidates[candidate_idx_local].text),
                    Trim(supported_transition_top_candidate_local.text)) then
                begin
                    if candidate_idx_local > 0 then
                    begin
                        force_move_display_candidate_to_index(
                            candidate_idx_local, 0);
                    end;
                    m_candidates[0].display_kind := cdk_lm_compound;
                    Exit;
                end;
            end;

            SetLength(m_candidates, Length(m_candidates) + 1);
            SetLength(m_candidate_segment_paths, Length(m_candidates));
            for move_idx_local := High(m_candidates) downto 1 do
            begin
                m_candidates[move_idx_local] :=
                    m_candidates[move_idx_local - 1];
                m_candidate_segment_paths[move_idx_local] :=
                    m_candidate_segment_paths[move_idx_local - 1];
            end;
            m_candidates[0] := supported_transition_top_candidate_local;
            m_candidates[0].display_kind := cdk_lm_compound;
            m_candidate_segment_paths[0] :=
                supported_transition_top_path_local;
        end;

        procedure restore_visible_supported_transition_top_local;
        var
            candidate_idx_local: Integer;
            move_idx_local: Integer;
            source_idx_local: Integer;
        begin
            if (not has_supported_transition_top_local) or
                (m_page_index <> 0) then
            begin
                Exit;
            end;

            candidate_idx_local := -1;
            for move_idx_local := 0 to High(Result) do
            begin
                if (Trim(Result[move_idx_local].comment) = '') and
                    SameText(Trim(Result[move_idx_local].text),
                    Trim(supported_transition_top_candidate_local.text)) then
                begin
                    candidate_idx_local := move_idx_local;
                    Break;
                end;
            end;

            source_idx_local := -1;
            if (candidate_idx_local >= 0) and
                (candidate_idx_local < Length(visible_source_indices)) then
            begin
                source_idx_local := visible_source_indices[candidate_idx_local];
            end;
            if candidate_idx_local < 0 then
            begin
                for move_idx_local := 0 to High(m_candidates) do
                begin
                    if (Trim(m_candidates[move_idx_local].comment) = '') and
                        SameText(Trim(m_candidates[move_idx_local].text),
                        Trim(supported_transition_top_candidate_local.text)) then
                    begin
                        source_idx_local := move_idx_local;
                        Break;
                    end;
                end;
                if Length(Result) < visible_page_size then
                begin
                    SetLength(Result, Length(Result) + 1);
                    SetLength(visible_source_indices, Length(Result));
                end;
                candidate_idx_local := High(Result);
            end;

            for move_idx_local := candidate_idx_local downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[0] := supported_transition_top_candidate_local;
            Result[0].display_kind := cdk_lm_compound;
            visible_source_indices[0] := source_idx_local;
        end;

        procedure capture_short_exact_ranked_order_local;
        var
            candidate_idx_local: Integer;
            exact_idx_local: Integer;
            seen_idx_local: Integer;
            candidate_text_local: string;
            already_seen_local: Boolean;
            is_exact_local: Boolean;
            exact_results_local: TncCandidateList;
        begin
            SetLength(short_exact_ranked_texts, 0);
            if (not short_exact_query_mode) or (m_dictionary = nil) or
                (normalized_pinyin = '') or
                (not lookup_display_exact_cached(normalized_pinyin,
                exact_results_local)) then
            begin
                Exit;
            end;

            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                candidate_text_local := Trim(
                    m_candidates[candidate_idx_local].text);
                if (candidate_text_local = '') or
                    (Trim(m_candidates[candidate_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) then
                begin
                    Continue;
                end;

                { Preserve the complete order already decided by the short
                  ranker. Accepted LM compounds participate in that order even
                  though they are not base-dictionary exact rows. }
                is_exact_local :=
                    is_current_short_two_single_pair_candidate_supported(
                    normalized_pinyin, candidate_text_local) or
                    is_current_short_three_exact_pair_candidate_supported(
                    normalized_pinyin, candidate_text_local);
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[exact_idx_local].comment) = '') and
                        SameText(Trim(exact_results_local[exact_idx_local].text),
                        candidate_text_local) then
                    begin
                        is_exact_local := True;
                        Break;
                    end;
                end;
                if not is_exact_local then
                begin
                    Continue;
                end;

                already_seen_local := False;
                for seen_idx_local := 0 to High(short_exact_ranked_texts) do
                begin
                    if SameText(short_exact_ranked_texts[seen_idx_local],
                        candidate_text_local) then
                    begin
                        already_seen_local := True;
                        Break;
                    end;
                end;
                if already_seen_local then
                begin
                    Continue;
                end;

                SetLength(short_exact_ranked_texts,
                    Length(short_exact_ranked_texts) + 1);
                short_exact_ranked_texts[High(short_exact_ranked_texts)] :=
                    candidate_text_local;
            end;
        end;

        procedure restore_short_exact_ranked_order_local;
        var
            ranked_candidates_local: TncCandidateList;
            ranked_paths_local: TArray<string>;
            slot_indices_local: TArray<Integer>;
            ranked_idx_local: Integer;
            candidate_idx_local: Integer;
            found_idx_local: Integer;
            slot_idx_local: Integer;
            insert_idx_local: Integer;
            temp_slot_local: Integer;
        begin
            if Length(short_exact_ranked_texts) < 2 then
            begin
                Exit;
            end;

            SetLength(ranked_candidates_local,
                Length(short_exact_ranked_texts));
            SetLength(ranked_paths_local, Length(short_exact_ranked_texts));
            SetLength(slot_indices_local, Length(short_exact_ranked_texts));
            ranked_idx_local := 0;
            for slot_idx_local := 0 to High(short_exact_ranked_texts) do
            begin
                found_idx_local := -1;
                for candidate_idx_local := 0 to High(m_candidates) do
                begin
                    if (Trim(m_candidates[candidate_idx_local].comment) = '') and
                        SameText(Trim(m_candidates[candidate_idx_local].text),
                        short_exact_ranked_texts[slot_idx_local]) then
                    begin
                        found_idx_local := candidate_idx_local;
                        Break;
                    end;
                end;
                if found_idx_local < 0 then
                begin
                    Continue;
                end;

                ranked_candidates_local[ranked_idx_local] :=
                    m_candidates[found_idx_local];
                if found_idx_local < Length(m_candidate_segment_paths) then
                begin
                    ranked_paths_local[ranked_idx_local] :=
                        m_candidate_segment_paths[found_idx_local];
                end
                else
                begin
                    ranked_paths_local[ranked_idx_local] := '';
                end;
                slot_indices_local[ranked_idx_local] := found_idx_local;
                Inc(ranked_idx_local);
            end;

            if ranked_idx_local < 2 then
            begin
                Exit;
            end;
            SetLength(ranked_candidates_local, ranked_idx_local);
            SetLength(ranked_paths_local, ranked_idx_local);
            SetLength(slot_indices_local, ranked_idx_local);

            for slot_idx_local := 1 to High(slot_indices_local) do
            begin
                temp_slot_local := slot_indices_local[slot_idx_local];
                insert_idx_local := slot_idx_local - 1;
                while (insert_idx_local >= 0) and
                    (slot_indices_local[insert_idx_local] > temp_slot_local) do
                begin
                    slot_indices_local[insert_idx_local + 1] :=
                        slot_indices_local[insert_idx_local];
                    Dec(insert_idx_local);
                end;
                slot_indices_local[insert_idx_local + 1] := temp_slot_local;
            end;

            for ranked_idx_local := 0 to High(ranked_candidates_local) do
            begin
                candidate_idx_local := slot_indices_local[ranked_idx_local];
                m_candidates[candidate_idx_local] :=
                    ranked_candidates_local[ranked_idx_local];
                if candidate_idx_local < Length(m_candidate_segment_paths) then
                begin
                    m_candidate_segment_paths[candidate_idx_local] :=
                        ranked_paths_local[ranked_idx_local];
                end;
            end;
        end;

        function is_protected_full_query_exact_local(
            const candidate_value: TncCandidate): Boolean;
        var
            exact_idx_local: Integer;
            candidate_text_local: string;
        begin
            Result := False;
            if Trim(candidate_value.comment) <> '' then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate_value.text);
            if candidate_text_local = '' then
            begin
                Exit;
            end;

            // Literal entries arrive through a separate lookup, but belong
            // to the same leading exact cluster as base dictionary words.
            if m_last_lookup_literal_user_fast and
                (candidate_value.source = cs_user) and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and (m_dictionary <> nil) and
                m_dictionary.is_user_entry(normalized_pinyin,
                candidate_text_local) then
            begin
                Exit(True);
            end;

            if is_fuzzy_pinyin_active and
                (candidate_value.fuzzy_cost > 0) and
                (get_candidate_text_unit_count(candidate_text_local) =
                expected_units) and
                ((candidate_value.source = cs_user) or
                ((candidate_value.source = cs_rule) and
                candidate_value.has_dict_weight)) then
            begin
                Exit(True);
            end;

            for exact_idx_local := 0 to High(protected_full_query_exacts) do
            begin
                if SameText(candidate_text_local,
                    Trim(protected_full_query_exacts[exact_idx_local].text)) then
                begin
                    Exit(True);
                end;
            end;
        end;

        procedure retain_all_full_query_exact_candidates_local;
        var
            exact_results_local: TncCandidateList;
            rebuilt_candidates_local: TncCandidateList;
            rebuilt_paths_local: TArray<string>;
            query_key_local: string;
            raw_query_local: string;
            candidate_text_local: string;
            idx_local: Integer;
            exact_idx_local: Integer;
            write_idx_local: Integer;

            function protected_text_already_collected_local(
                const text_value: string): Boolean;
            var
                probe_idx_local: Integer;
            begin
                Result := False;
                for probe_idx_local := 0 to High(
                    protected_full_query_exacts) do
                begin
                    if SameText(Trim(
                        protected_full_query_exacts[probe_idx_local].text),
                        text_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function contains_non_ascii_candidate_text_local(
                const text_value: string): Boolean;
            var
                char_idx_local: Integer;
            begin
                Result := False;
                for char_idx_local := 1 to Length(text_value) do
                begin
                    if Ord(text_value[char_idx_local]) > 127 then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function rebuilt_exact_already_emitted_local(
                const text_value: string): Boolean;
            var
                probe_idx_local: Integer;
            begin
                Result := False;
                for probe_idx_local := 0 to write_idx_local - 1 do
                begin
                    if (Trim(rebuilt_candidates_local[probe_idx_local].comment) = '') and
                        SameText(Trim(
                        rebuilt_candidates_local[probe_idx_local].text),
                        text_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            procedure append_rebuilt_candidate_local(
                const candidate_value: TncCandidate;
                const path_value: string);
            begin
                rebuilt_candidates_local[write_idx_local] := candidate_value;
                rebuilt_paths_local[write_idx_local] := path_value;
                Inc(write_idx_local);
            end;
        begin
            SetLength(protected_full_query_exacts, 0);
            // Single-syllable candidates use their own complete paging and
            // learned-character ordering path. This guard is for multi-unit
            // exact words that can otherwise be lost in segmentation caps.
            if (m_dictionary = nil) or (expected_units < 2) then
            begin
                Exit;
            end;

            query_key_local := normalized_pinyin;
            raw_query_local := get_display_raw_query_text_local;
            if Pos('''', raw_query_local) > 0 then
            begin
                query_key_local := raw_query_local;
            end;
            if (query_key_local = '') or
                (not lookup_display_exact_cached(query_key_local,
                exact_results_local)) then
            begin
                Exit;
            end;

            for exact_idx_local := 0 to High(exact_results_local) do
            begin
                candidate_text_local := Trim(
                    exact_results_local[exact_idx_local].text);
                if (candidate_text_local = '') or
                    (not contains_non_ascii_candidate_text_local(
                    candidate_text_local)) or
                    (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) or
                    // Compact-key recovery must not undo explicit boundaries.
                    ((Pos('''', m_composition_text) > 0) and
                    (not display_complete_text_matches_explicit_apostrophe_boundary(
                    candidate_text_local))) or
                    (((exact_results_local[exact_idx_local].source = cs_user) and
                    (not m_dictionary.is_user_entry(query_key_local,
                    candidate_text_local))) or
                    ((exact_results_local[exact_idx_local].source <> cs_user) and
                    (not m_dictionary.is_base_entry(query_key_local,
                    candidate_text_local)))) or
                    protected_text_already_collected_local(
                    candidate_text_local) then
                begin
                    Continue;
                end;

                SetLength(protected_full_query_exacts,
                    Length(protected_full_query_exacts) + 1);
                protected_full_query_exacts[High(
                    protected_full_query_exacts)] :=
                    exact_results_local[exact_idx_local];
                protected_full_query_exacts[High(
                    protected_full_query_exacts)].text :=
                    candidate_text_local;
                protected_full_query_exacts[High(
                    protected_full_query_exacts)].comment := '';
            end;

            if Length(protected_full_query_exacts) = 0 then
            begin
                Exit;
            end;

            SetLength(rebuilt_candidates_local, Length(m_candidates) +
                Length(protected_full_query_exacts));
            SetLength(rebuilt_paths_local, Length(rebuilt_candidates_local));
            write_idx_local := 0;

            // Keep the ranking already established by the short-query path,
            // but gather every exact item into one lossless leading cluster.
            for idx_local := 0 to High(m_candidates) do
            begin
                if (not is_protected_full_query_exact_local(
                    m_candidates[idx_local])) or
                    rebuilt_exact_already_emitted_local(
                    Trim(m_candidates[idx_local].text)) then
                begin
                    Continue;
                end;

                if idx_local < Length(m_candidate_segment_paths) then
                begin
                    append_rebuilt_candidate_local(m_candidates[idx_local],
                        m_candidate_segment_paths[idx_local]);
                end
                else
                begin
                    append_rebuilt_candidate_local(m_candidates[idx_local], '');
                end;
            end;

            // Exact rows cut by an earlier candidate cap are restored in the
            // dictionary provider's stable order.
            for exact_idx_local := 0 to High(protected_full_query_exacts) do
            begin
                candidate_text_local := Trim(
                    protected_full_query_exacts[exact_idx_local].text);
                if rebuilt_exact_already_emitted_local(candidate_text_local) then
                begin
                    Continue;
                end;
                append_rebuilt_candidate_local(
                    protected_full_query_exacts[exact_idx_local], '');
            end;

            for idx_local := 0 to High(m_candidates) do
            begin
                if is_protected_full_query_exact_local(
                    m_candidates[idx_local]) then
                begin
                    Continue;
                end;

                if idx_local < Length(m_candidate_segment_paths) then
                begin
                    append_rebuilt_candidate_local(m_candidates[idx_local],
                        m_candidate_segment_paths[idx_local]);
                end
                else
                begin
                    append_rebuilt_candidate_local(m_candidates[idx_local], '');
                end;
            end;

            SetLength(rebuilt_candidates_local, write_idx_local);
            SetLength(rebuilt_paths_local, write_idx_local);
            m_candidates := rebuilt_candidates_local;
            m_candidate_segment_paths := rebuilt_paths_local;
        end;

        procedure clear_segment_path_for_identity(const candidate_value: TncCandidate);
        var
            identity_key_local: string;
        begin
            identity_key_local := build_candidate_identity_key(
                candidate_value.text, candidate_value.comment);
            if identity_key_local = '' then
            begin
                Exit;
            end;

            if m_current_segment_path_map <> nil then
            begin
                m_current_segment_path_map.Remove(identity_key_local);
            end;
            if m_current_segment_path_score_map <> nil then
            begin
                m_current_segment_path_score_map.Remove(identity_key_local);
            end;
        end;

        function segment_path_matches_visible_text(const encoded_path: string;
            const visible_text: string): Boolean;
        var
            path_parts_local: TArray<string>;
            part_idx_local: Integer;
            joined_text_local: string;
            segment_text_local: string;
        begin
            Result := True;
            if get_encoded_path_segment_count_local(encoded_path) <= 1 then
            begin
                Exit;
            end;

            path_parts_local := encoded_path.Split([c_segment_path_separator]);
            joined_text_local := '';
            for part_idx_local := 0 to High(path_parts_local) do
            begin
                segment_text_local := Trim(path_parts_local[part_idx_local]);
                if segment_text_local = '' then
                begin
                    Result := False;
                    Exit;
                end;
                joined_text_local := joined_text_local + segment_text_local;
            end;

            Result := SameText(joined_text_local, Trim(visible_text));
        end;

        procedure sanitize_short_partial_segment_paths;
        var
            idx_local: Integer;
            path_local: string;
        begin
            if (expected_units > 5) or
                (Length(m_candidate_segment_paths) = 0) then
            begin
                Exit;
            end;

            for idx_local := 0 to High(m_candidates) do
            begin
                if (idx_local >= Length(m_candidate_segment_paths)) or
                    (Trim(m_candidates[idx_local].comment) = '') then
                begin
                    Continue;
                end;

                path_local := Trim(m_candidate_segment_paths[idx_local]);
                if get_encoded_path_segment_count_local(path_local) <= 1 then
                begin
                    Continue;
                end;

                if not segment_path_matches_visible_text(path_local,
                    m_candidates[idx_local].text) then
                begin
                    m_candidate_segment_paths[idx_local] := '';
                    clear_segment_path_for_identity(m_candidates[idx_local]);
                end;
            end;
        end;

        procedure normalize_display_candidate(var candidate_value: TncCandidate);
        begin
            if (candidate_value.comment = '') and
                split_trailing_ascii_candidate(candidate_value.text, head_text,
                tail_pinyin) then
            begin
                candidate_value.text := head_text;
                candidate_value.comment := tail_pinyin;
            end;
            if Trim(candidate_value.comment) <> '' then
            begin
                candidate_value.display_kind := cdk_default;
            end;
            if (Trim(candidate_value.comment) = '') and
                (is_current_short_two_single_pair_candidate_supported(
                normalized_pinyin, Trim(candidate_value.text)) or
                is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, Trim(candidate_value.text))) then
            begin
                candidate_value.display_kind := cdk_lm_compound;
            end;
        end;

        function display_candidate_is_explicit_apostrophe_prefix_partial(
            const candidate_value: TncCandidate): Boolean;
        var
            tail_syllables_local: TncPinyinParseResult;
            query_syllables_local: TncPinyinParseResult;
            text_units_local: TArray<string>;
            prefix_key_local: string;
            query_unit_count_local: Integer;
            prefix_units_local: Integer;
            unit_idx_local: Integer;
            is_preferred_local: Boolean;
        begin
            Result := False;
            query_syllables_local := syllables;
            query_unit_count_local := expected_units;
            if Pos('''', get_display_raw_query_text_local) > 0 then
            begin
                query_syllables_local := get_display_query_syllables_local(
                    get_display_raw_query_text_local);
                query_unit_count_local := Length(query_syllables_local);
            end;
            if (Pos('''', get_display_raw_query_text_local) <= 0) or
                (Trim(candidate_value.comment) = '') or
                (query_unit_count_local < 3) or
                (Length(query_syllables_local) < query_unit_count_local) then
            begin
                Exit;
            end;

            tail_syllables_local := get_effective_compact_pinyin_syllables(
                normalize_pinyin_text(candidate_value.comment));
            if Length(tail_syllables_local) <= 0 then
            begin
                Exit;
            end;

            prefix_units_local := query_unit_count_local -
                Length(tail_syllables_local);
            if prefix_units_local < 2 then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_value.text));
            if Length(text_units_local) <> prefix_units_local then
            begin
                Exit;
            end;

            prefix_key_local := '';
            for unit_idx_local := 0 to prefix_units_local - 1 do
            begin
                prefix_key_local := prefix_key_local +
                    normalize_pinyin_text(query_syllables_local[unit_idx_local].text);
            end;
            if (prefix_key_local <> '') and
                ((display_exact_key_has_text(prefix_key_local,
                Trim(candidate_value.text))) or
                ((m_dictionary <> nil) and
                (m_dictionary.is_base_entry(prefix_key_local,
                Trim(candidate_value.text)) or
                m_dictionary.is_user_entry(prefix_key_local,
                Trim(candidate_value.text))))) then
            begin
                Exit(True);
            end;

            for unit_idx_local := 0 to prefix_units_local - 1 do
            begin
                if not match_single_char_candidate_for_syllable(
                    normalize_pinyin_text(query_syllables_local[unit_idx_local].text),
                    text_units_local[unit_idx_local], is_preferred_local) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function candidate_is_non_strict_one_plus_two_complete(
            const candidate_value: TncCandidate;
            const candidate_index_value: Integer): Boolean;
        var
            candidate_text_local: string;
            units_local: TArray<string>;
            path_local: string;
            path_parts_local: TArray<string>;
            head_key_local: string;
            tail_key_local: string;
            tail_text_local: string;
            fixed_head_text_local: string;
            fixed_tail_units_local: Integer;

            function candidate_is_short_particle_tail_completion_local: Boolean;
            var
                local_head_key: string;
                local_head_text: string;
                local_tail_key: string;
                local_tail_text: string;
                local_query_key: string;

                function try_get_compact_particle_tail_local(
                    out out_tail_key: string; out out_tail_text: string): Boolean;
                begin
                    out_tail_key := '';
                    out_tail_text := '';
                    local_head_key := '';
                    Result := try_get_short_particle_tail_query_parts(
                        local_query_key, out_tail_key, out_tail_text,
                        local_head_key);
                end;
            begin
                Result := False;
                if Length(units_local) <> 3 then
                begin
                    Exit;
                end;

                local_query_key := normalize_pinyin_text(normalized_pinyin);
                if try_get_compact_particle_tail_local(local_tail_key,
                    local_tail_text) then
                begin
                    local_head_text := Trim(units_local[0]) +
                        Trim(units_local[1]);
                    if (local_head_key <> '') and
                        SameText(Trim(units_local[2]), local_tail_text) and
                        (get_effective_compact_pinyin_unit_count(
                        local_head_key) = 2) and
                        (display_exact_key_has_text(local_head_key,
                        local_head_text) or
                        m_dictionary.is_base_entry(local_head_key,
                        local_head_text) or
                        m_dictionary.is_user_entry(local_head_key,
                        local_head_text)) then
                    begin
                        Exit(True);
                    end;
                end;

                if SameText(normalize_pinyin_text(syllables[1].text), 'de') and
                    SameText(normalize_pinyin_text(syllables[2].text), 'hao') and
                    SameText(Trim(units_local[1]) + Trim(units_local[2]),
                    string(Char($5F97)) + string(Char($597D))) then
                begin
                    local_head_key := normalize_pinyin_text(syllables[0].text) +
                        normalize_pinyin_text(syllables[1].text);
                    local_head_text := Trim(units_local[0]) +
                        Trim(units_local[1]);
                    Result := (local_head_key <> '') and
                        (local_head_text <> '') and
                        (display_exact_key_has_text(local_head_key,
                        local_head_text) or
                        m_dictionary.is_base_entry(local_head_key,
                        local_head_text) or
                        m_dictionary.is_user_entry(local_head_key,
                        local_head_text));
                    Exit;
                end;

                local_tail_key := normalize_pinyin_text(syllables[2].text);
                if SameText(local_tail_key, 'de') then
                begin
                    local_tail_text := string(Char($7684));
                end
                else if SameText(local_tail_key, 'le') then
                begin
                    local_tail_text := string(Char($4E86));
                end
                else if SameText(local_tail_key, 'zhe') then
                begin
                    local_tail_text := string(Char($7740));
                end
                else
                begin
                    Exit;
                end;

                if not SameText(Trim(units_local[2]), local_tail_text) then
                begin
                    Exit;
                end;

                local_head_key := normalize_pinyin_text(syllables[0].text) +
                    normalize_pinyin_text(syllables[1].text);
                local_head_text := Trim(units_local[0]) + Trim(units_local[1]);
                Result := (local_head_key <> '') and (local_head_text <> '') and
                    (display_exact_key_has_text(local_head_key,
                    local_head_text) or
                    m_dictionary.is_base_entry(local_head_key, local_head_text) or
                    m_dictionary.is_user_entry(local_head_key, local_head_text));
            end;

            function two_plus_one_head_has_stronger_exact_local(
                const local_head_key: string;
                const local_head_text: string): Boolean;
            const
                c_probe_limit = 16;
                c_min_weight_gap = 240;
            var
                exact_results_local: TncCandidateList;
                exact_idx_local: Integer;
                exact_text_local: string;
                exact_weight_local: Integer;
                current_weight_local: Integer;
                found_current_local: Boolean;
            begin
                Result := False;
                current_weight_local := 0;
                found_current_local := False;
                if (local_head_key = '') or (local_head_text = '') or
                    (not lookup_display_exact_cached(local_head_key,
                    exact_results_local)) then
                begin
                    Exit;
                end;

                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                        (exact_text_local = '') or
                        (get_candidate_text_unit_count(exact_text_local) <> 2) then
                    begin
                        Continue;
                    end;

                    if exact_results_local[exact_idx_local].has_dict_weight then
                    begin
                        exact_weight_local :=
                            exact_results_local[exact_idx_local].dict_weight;
                    end
                    else
                    begin
                        exact_weight_local := exact_results_local[exact_idx_local].score;
                    end;

                    if SameText(exact_text_local, local_head_text) then
                    begin
                        current_weight_local := Max(current_weight_local,
                            exact_weight_local);
                        found_current_local := True;
                        Continue;
                    end;

                    if exact_weight_local >= current_weight_local +
                        c_min_weight_gap then
                    begin
                        if (not found_current_local) or
                            (exact_weight_local >= current_weight_local +
                            c_min_weight_gap) then
                        begin
                            Exit(True);
                        end;
                    end;
                end;
            end;
        begin
            Result := False;
            if (m_dictionary = nil) or (expected_units <> 3) or
                (Length(syllables) <> 3) or (normalized_pinyin = '') or
                (Trim(candidate_value.comment) <> '') then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate_value.text);
            if (candidate_text_local = '') or
                (get_candidate_text_unit_count(candidate_text_local) <> 3) or
                m_dictionary.is_base_entry(normalized_pinyin,
                candidate_text_local) or
                ((candidate_value.source = cs_user) and
                m_dictionary.is_user_entry(normalized_pinyin,
                candidate_text_local)) then
            begin
                Exit;
            end;

            if is_current_short_three_exact_pair_candidate_supported(
                normalized_pinyin, candidate_text_local) then
            begin
                Exit(False);
            end;

            units_local := split_text_units(candidate_text_local);
            if Length(units_local) <> 3 then
            begin
                Exit;
            end;

            if try_get_fixed_pronoun_tail_query_parts(normalized_pinyin,
                expected_units, head_key_local, fixed_head_text_local,
                tail_key_local, fixed_tail_units_local) and
                SameText(Trim(units_local[0]), fixed_head_text_local) then
            begin
                tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
                if (tail_key_local <> '') and (tail_text_local <> '') and
                    (display_exact_key_has_text(tail_key_local,
                    tail_text_local) or
                    m_dictionary.is_base_entry(tail_key_local,
                    tail_text_local)) then
                begin
                    Exit(False);
                end;
            end;
            if candidate_is_short_particle_tail_completion_local then
            begin
                Exit(False);
            end;

            path_local := get_display_candidate_path_for_index(candidate_value,
                candidate_index_value);
            if get_encoded_path_segment_count_local(path_local) = 2 then
            begin
                path_parts_local := path_local.Split([c_segment_path_separator]);
                if (Length(path_parts_local) = 2) and
                    (get_candidate_text_unit_count(Trim(path_parts_local[0])) = 1) and
                    (get_candidate_text_unit_count(Trim(path_parts_local[1])) = 2) then
                begin
                    Exit(True);
                end;
            end;

            head_key_local := normalize_pinyin_text(syllables[0].text) +
                normalize_pinyin_text(syllables[1].text);
            tail_text_local := Trim(units_local[2]);
            if (head_key_local <> '') and
                is_display_function_or_modifier_single(tail_text_local) and
                two_plus_one_head_has_stronger_exact_local(head_key_local,
                Trim(units_local[0]) + Trim(units_local[1])) then
            begin
                Exit(True);
            end;

            head_key_local := normalize_pinyin_text(syllables[0].text);
            tail_key_local := normalize_pinyin_text(syllables[1].text) +
                normalize_pinyin_text(syllables[2].text);
            tail_text_local := Trim(units_local[1]) + Trim(units_local[2]);
            if (head_key_local = '') or (tail_key_local = '') or
                (tail_text_local = '') or
                (not m_dictionary.single_char_matches_pinyin(head_key_local,
                Trim(units_local[0]))) then
            begin
                Exit;
            end;

            Result := display_exact_key_has_text(tail_key_local,
                tail_text_local) or
                m_dictionary.is_base_entry(tail_key_local, tail_text_local);
        end;

        function forced_visible_top_candidate_applies: Boolean;
        begin
            Result := m_has_forced_visible_top_candidate and
                (m_page_index = 0) and
                (Trim(m_forced_visible_top_candidate.text) <> '') and
                SameText(m_forced_visible_top_composition_text,
                m_composition_text) and
                SameText(m_forced_visible_top_lookup_key, m_last_lookup_key);
        end;

        function same_visible_candidate_local(const left_candidate: TncCandidate;
            const right_candidate: TncCandidate): Boolean;
        begin
            Result := SameText(Trim(left_candidate.text), Trim(right_candidate.text)) and
                SameText(Trim(left_candidate.comment), Trim(right_candidate.comment));
        end;

        procedure ensure_explicit_apostrophe_prefix_partial_visible;
        var
            source_idx_local: Integer;
            existing_idx_local: Integer;
            move_idx_local: Integer;
            best_source_idx_local: Integer;
            candidate_local: TncCandidate;
            best_candidate_local: TncCandidate;
            explicit_syllables_local: TncPinyinParseResult;
            explicit_unit_count_local: Integer;

            procedure restore_explicit_candidate_to_internal_front_local;
            var
                move_idx_local: Integer;
            begin
                if best_source_idx_local = 0 then
                begin
                    m_candidates[0] := best_candidate_local;
                    Exit;
                end;

                if best_source_idx_local > 0 then
                begin
                    for move_idx_local := best_source_idx_local downto 1 do
                    begin
                        m_candidates[move_idx_local] :=
                            m_candidates[move_idx_local - 1];
                        if move_idx_local < Length(m_candidate_segment_paths) then
                        begin
                            if move_idx_local - 1 < Length(m_candidate_segment_paths) then
                            begin
                                m_candidate_segment_paths[move_idx_local] :=
                                    m_candidate_segment_paths[move_idx_local - 1];
                            end
                            else
                            begin
                                m_candidate_segment_paths[move_idx_local] := '';
                            end;
                        end;
                    end;
                    m_candidates[0] := best_candidate_local;
                    if Length(m_candidate_segment_paths) > 0 then
                    begin
                        m_candidate_segment_paths[0] := '';
                    end;
                    Exit;
                end;

                SetLength(m_candidates, Length(m_candidates) + 1);
                if Length(m_candidate_segment_paths) < Length(m_candidates) then
                begin
                    SetLength(m_candidate_segment_paths, Length(m_candidates));
                end;
                for move_idx_local := High(m_candidates) downto 1 do
                begin
                    m_candidates[move_idx_local] := m_candidates[move_idx_local - 1];
                    if move_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[move_idx_local] :=
                            m_candidate_segment_paths[move_idx_local - 1];
                    end;
                end;
                m_candidates[0] := best_candidate_local;
                if Length(m_candidate_segment_paths) > 0 then
                begin
                    m_candidate_segment_paths[0] := '';
                end;
            end;

            function try_build_display_explicit_prefix_candidate_local(
                out out_candidate: TncCandidate): Boolean;
            var
                prefix_count_local: Integer;
                unit_idx_local: Integer;
                prefix_key_local: string;
                raw_prefix_key_local: string;
                tail_comment_local: string;
                exact_results_local: TncCandidateList;
                candidate_text_local: string;
                raw_hit_local: Boolean;
                compact_hit_local: Boolean;

                function try_pick_candidate_from_results_local(
                    const source_results: TncCandidateList): Boolean;
                var
                    pick_idx_local: Integer;
                begin
                    Result := False;
                    for pick_idx_local := 0 to High(source_results) do
                    begin
                        if Trim(source_results[pick_idx_local].comment) <> '' then
                        begin
                            Continue;
                        end;
                        candidate_text_local :=
                            Trim(source_results[pick_idx_local].text);
                        if (candidate_text_local = '') or
                            (get_candidate_text_unit_count(candidate_text_local) <>
                            prefix_count_local) then
                        begin
                            Continue;
                        end;

                        out_candidate := source_results[pick_idx_local];
                        out_candidate.comment := tail_comment_local;
                        Exit(True);
                    end;
                end;

                function get_raw_prefix_key_for_count_local(
                    const target_prefix_count: Integer): string;
                var
                    target_compact_len_local: Integer;
                    local_unit_idx: Integer;
                    raw_idx_local: Integer;
                    compact_seen_local: Integer;
                    raw_char_local: Char;
                    raw_query_text_local: string;
                begin
                    Result := '';
                    raw_query_text_local := get_display_raw_query_text_local;
                    target_compact_len_local := 0;
                    for local_unit_idx := 0 to target_prefix_count - 1 do
                    begin
                        Inc(target_compact_len_local,
                            Length(normalize_pinyin_text(
                            explicit_syllables_local[local_unit_idx].text)));
                    end;
                    if target_compact_len_local <= 0 then
                    begin
                        Exit;
                    end;

                    compact_seen_local := 0;
                    for raw_idx_local := 1 to Length(raw_query_text_local) do
                    begin
                        raw_char_local := raw_query_text_local[raw_idx_local];
                        Result := Result + raw_char_local;
                        if raw_char_local <> '''' then
                        begin
                            Inc(compact_seen_local);
                            if compact_seen_local >= target_compact_len_local then
                            begin
                                Break;
                            end;
                        end;
                    end;
                    Result := LowerCase(Trim(Result));
                end;
            begin
                Result := False;
                FillChar(out_candidate, SizeOf(out_candidate), 0);
                if Length(explicit_syllables_local) < explicit_unit_count_local then
                begin
                    Exit;
                end;

                for prefix_count_local := explicit_unit_count_local - 1 downto 2 do
                begin
                    prefix_key_local := '';
                    for unit_idx_local := 0 to prefix_count_local - 1 do
                    begin
                        prefix_key_local := prefix_key_local +
                            normalize_pinyin_text(
                            explicit_syllables_local[unit_idx_local].text);
                    end;

                    tail_comment_local := '';
                    for unit_idx_local := prefix_count_local to
                        explicit_unit_count_local - 1 do
                    begin
                        tail_comment_local := tail_comment_local +
                            normalize_pinyin_text(
                            explicit_syllables_local[unit_idx_local].text);
                    end;

                    raw_prefix_key_local :=
                        get_raw_prefix_key_for_count_local(prefix_count_local);
                    if (prefix_key_local = '') or (tail_comment_local = '') then
                    begin
                        Continue;
                    end;
                    raw_hit_local := (raw_prefix_key_local <> '') and
                        (lookup_display_exact_cached(raw_prefix_key_local,
                        exact_results_local) or
                        m_dictionary.lookup(raw_prefix_key_local,
                        exact_results_local));
                    compact_hit_local := False;
                    if not raw_hit_local then
                    begin
                        compact_hit_local := lookup_display_exact_cached(
                            prefix_key_local, exact_results_local) or
                            m_dictionary.lookup(prefix_key_local,
                            exact_results_local);
                    end;
                    if (not raw_hit_local) and (not compact_hit_local) then
                    begin
                        Continue;
                    end;

                    if try_pick_candidate_from_results_local(
                        exact_results_local) then
                    begin
                        Exit(True);
                    end;
                    if raw_hit_local and
                        (lookup_display_exact_cached(prefix_key_local,
                        exact_results_local) or
                        m_dictionary.lookup(prefix_key_local,
                        exact_results_local)) and
                        try_pick_candidate_from_results_local(
                        exact_results_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;
        begin
            if (m_page_index <> 0) or (page_size <= 0) or
                (Pos('''', get_display_raw_query_text_local) <= 0) then
            begin
                Exit;
            end;
            explicit_syllables_local := get_display_query_syllables_local(
                get_display_raw_query_text_local);
            explicit_unit_count_local := Length(explicit_syllables_local);
            if explicit_unit_count_local < 3 then
            begin
                Exit;
            end;

            best_source_idx_local := -1;
            for source_idx_local := 0 to High(m_candidates) do
            begin
                candidate_local := m_candidates[source_idx_local];
                normalize_display_candidate(candidate_local);
                if display_candidate_is_explicit_apostrophe_prefix_partial(
                    candidate_local) then
                begin
                    best_candidate_local := candidate_local;
                    best_source_idx_local := source_idx_local;
                    Break;
                end;
            end;

            if best_source_idx_local < 0 then
            begin
                if has_explicit_apostrophe_entry_top_partial_candidate then
                begin
                    best_candidate_local :=
                        explicit_apostrophe_entry_top_partial_candidate;
                end
                else if not try_build_display_explicit_prefix_candidate_local(
                    best_candidate_local) then
                begin
                    Exit;
                end;
            end;

            restore_explicit_candidate_to_internal_front_local;

            for existing_idx_local := 0 to High(Result) do
            begin
                if same_visible_candidate_local(Result[existing_idx_local],
                    best_candidate_local) then
                begin
                    if existing_idx_local = 0 then
                    begin
                        Exit;
                    end;
                    for move_idx_local := existing_idx_local downto 1 do
                    begin
                        Result[move_idx_local] := Result[move_idx_local - 1];
                        if move_idx_local < Length(visible_source_indices) then
                        begin
                            visible_source_indices[move_idx_local] :=
                                visible_source_indices[move_idx_local - 1];
                        end;
                    end;
                    Result[0] := best_candidate_local;
                    if Length(visible_source_indices) > 0 then
                    begin
                        visible_source_indices[0] := best_source_idx_local;
                    end;
                    Exit;
                end;
            end;

            if Length(Result) < page_size then
            begin
                SetLength(Result, Length(Result) + 1);
                SetLength(visible_source_indices, Length(Result));
            end;
            if Length(Result) <= 0 then
            begin
                SetLength(Result, 1);
                SetLength(visible_source_indices, 1);
            end;

            for move_idx_local := High(Result) downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                if move_idx_local < Length(visible_source_indices) then
                begin
                    visible_source_indices[move_idx_local] :=
                        visible_source_indices[move_idx_local - 1];
                end;
            end;
            Result[0] := best_candidate_local;
            visible_source_indices[0] := best_source_idx_local;
        end;

        function find_forced_visible_top_source_index: Integer;
        var
            candidate_idx_local: Integer;
        begin
            Result := -1;
            for candidate_idx_local := 0 to High(m_candidates) do
            begin
                if same_visible_candidate_local(m_candidates[candidate_idx_local],
                    m_forced_visible_top_candidate) then
                begin
                    Result := candidate_idx_local;
                    Exit;
                end;
            end;
        end;

        procedure apply_forced_visible_top_candidate;
        var
            candidate_idx_local: Integer;
            forced_idx_local: Integer;
            forced_source_idx_local: Integer;
            superseding_idx_local: Integer;
            tmp_candidate_local: TncCandidate;
            tmp_source_idx_local: Integer;

            function current_top_supersedes_forced_partial_local(
                const current_top_candidate: TncCandidate;
                const current_candidate_idx: Integer): Boolean;
            var
                current_text_local: string;
                forced_text_local: string;
                current_prefix_key_local: string;
                current_text_units_local: Integer;
                current_tail_units_local: Integer;
                forced_text_units_local: Integer;
                forced_tail_units_local: Integer;

                function current_complete_has_backed_path_local: Boolean;
                var
                    current_path_local: string;
                    current_source_idx_local: Integer;
                begin
                    current_path_local := '';
                    current_source_idx_local := -1;
                    if (current_candidate_idx >= 0) and
                        (current_candidate_idx <
                        Length(visible_source_indices)) then
                    begin
                        current_source_idx_local :=
                            visible_source_indices[current_candidate_idx];
                    end;
                    if (current_source_idx_local >= 0) and
                        (current_source_idx_local < Length(m_candidates)) then
                    begin
                        current_path_local := get_segment_path_for_candidate(
                            m_candidates[current_source_idx_local],
                            current_source_idx_local);
                    end
                    else
                    begin
                        current_path_local := get_segment_path_for_candidate(
                            current_top_candidate);
                    end;
                    Result := get_encoded_path_segment_count_local(
                        current_path_local) > 1;
                end;
            begin
                Result := False;
                if Trim(m_forced_visible_top_candidate.comment) = '' then
                begin
                    Exit;
                end;

                current_text_local := Trim(current_top_candidate.text);
                forced_text_local := Trim(m_forced_visible_top_candidate.text);
                if (current_text_local = '') or (forced_text_local = '') then
                begin
                    Exit;
                end;

                current_text_units_local :=
                    get_candidate_text_unit_count(current_text_local);
                forced_text_units_local :=
                    get_candidate_text_unit_count(forced_text_local);
                forced_tail_units_local :=
                    get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(
                    Trim(m_forced_visible_top_candidate.comment)));
                if (forced_tail_units_local <= 0) or
                    (forced_text_units_local + forced_tail_units_local <>
                    expected_units) then
                begin
                    Exit;
                end;

                if Trim(current_top_candidate.comment) = '' then
                begin
                    Result := (current_text_units_local = expected_units) and
                        (
                            (Copy(current_text_local, 1,
                            Length(forced_text_local)) = forced_text_local) or
                            (
                                m_forced_visible_top_from_incremental_lm_prefix and
                                (display_exact_key_has_text(normalized_pinyin,
                                current_text_local) or
                                display_candidate_has_supported_full_entry(
                                normalized_pinyin, current_top_candidate) or
                                current_complete_has_backed_path_local)
                            )
                        );
                    Exit;
                end;

                current_tail_units_local :=
                    get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(
                    Trim(current_top_candidate.comment)));
                if (current_tail_units_local <= 0) or
                    (current_text_units_local + current_tail_units_local <>
                    expected_units) or
                    (current_text_units_local <= forced_text_units_local) then
                begin
                    Exit;
                end;

                current_prefix_key_local := build_display_query_key(0,
                    current_text_units_local);
                Result := (current_prefix_key_local <> '') and
                    display_exact_key_has_text(current_prefix_key_local,
                    current_text_local);
            end;
        begin
            if (page_size <= 0) or (not forced_visible_top_candidate_applies) then
            begin
                Exit;
            end;

            // A preserved partial must not override an explicit full user word.
            if (Length(Result) > 0) and (Result[0].source = cs_user) and
                is_protected_full_query_exact_local(Result[0]) then
            begin
                Exit;
            end;

            { A forced incremental prefix must not displace a full exact or a
              longer exact phrase prefix that explains more input syllables. }
            superseding_idx_local := -1;
            for candidate_idx_local := 0 to High(Result) do
            begin
                if ((Trim(Result[candidate_idx_local].comment) <> '') or
                    m_forced_visible_top_from_incremental_lm_prefix) and
                    current_top_supersedes_forced_partial_local(
                    Result[candidate_idx_local], candidate_idx_local) then
                begin
                    if superseding_idx_local < 0 then
                    begin
                        superseding_idx_local := candidate_idx_local;
                    end;
                    // A complete candidate that directly extends the
                    // preserved LM prefix is the authoritative repair. Only
                    // fall back to an unrelated backed path when no such
                    // extension exists.
                    if (Trim(Result[candidate_idx_local].comment) = '') and
                        (Copy(Trim(Result[candidate_idx_local].text), 1,
                        Length(Trim(m_forced_visible_top_candidate.text))) =
                        Trim(m_forced_visible_top_candidate.text)) then
                    begin
                        superseding_idx_local := candidate_idx_local;
                        Break;
                    end;
                end;
            end;
            if superseding_idx_local >= 0 then
            begin
                if superseding_idx_local > 0 then
                begin
                    tmp_candidate_local := Result[superseding_idx_local];
                    tmp_source_idx_local :=
                        visible_source_indices[superseding_idx_local];
                    for candidate_idx_local := superseding_idx_local downto 1 do
                    begin
                        Result[candidate_idx_local] :=
                            Result[candidate_idx_local - 1];
                        visible_source_indices[candidate_idx_local] :=
                            visible_source_indices[candidate_idx_local - 1];
                    end;
                    Result[0] := tmp_candidate_local;
                    visible_source_indices[0] := tmp_source_idx_local;
                end;

                { The complete path wins, but the preserved LM prefix remains
                  the longest partial and therefore belongs immediately after
                  it rather than disappearing from the visible page. }
                if (page_size > 1) and
                    m_forced_visible_top_from_incremental_lm_prefix then
                begin
                    forced_idx_local := -1;
                    for candidate_idx_local := 1 to High(Result) do
                    begin
                        if same_visible_candidate_local(
                            Result[candidate_idx_local],
                            m_forced_visible_top_candidate) then
                        begin
                            forced_idx_local := candidate_idx_local;
                            Break;
                        end;
                    end;
                    forced_source_idx_local :=
                        find_forced_visible_top_source_index;

                    if forced_idx_local < 0 then
                    begin
                        if Length(Result) < page_size then
                        begin
                            SetLength(Result, Length(Result) + 1);
                            SetLength(visible_source_indices,
                                Length(visible_source_indices) + 1);
                        end;
                        for candidate_idx_local := High(Result) downto 2 do
                        begin
                            Result[candidate_idx_local] :=
                                Result[candidate_idx_local - 1];
                            visible_source_indices[candidate_idx_local] :=
                                visible_source_indices[candidate_idx_local - 1];
                        end;
                        Result[1] := m_forced_visible_top_candidate;
                        visible_source_indices[1] := forced_source_idx_local;
                    end
                    else if forced_idx_local > 1 then
                    begin
                        tmp_candidate_local := Result[forced_idx_local];
                        tmp_source_idx_local :=
                            visible_source_indices[forced_idx_local];
                        for candidate_idx_local := forced_idx_local downto 2 do
                        begin
                            Result[candidate_idx_local] :=
                                Result[candidate_idx_local - 1];
                            visible_source_indices[candidate_idx_local] :=
                                visible_source_indices[candidate_idx_local - 1];
                        end;
                        Result[1] := tmp_candidate_local;
                        visible_source_indices[1] := tmp_source_idx_local;
                    end;

                    if m_forced_visible_top_candidate.display_kind =
                        cdk_lm_compound then
                    begin
                        Result[1].display_kind := cdk_lm_compound;
                    end;
                end;
                Exit;
            end;

            if (Length(Result) > 0) and
                current_top_supersedes_forced_partial_local(Result[0], 0) then
            begin
                Exit;
            end;

            forced_source_idx_local := find_forced_visible_top_source_index;
            if (Length(Result) > 0) and
                same_visible_candidate_local(Result[0],
                m_forced_visible_top_candidate) then
            begin
                if m_forced_visible_top_candidate.display_kind =
                    cdk_lm_compound then
                begin
                    Result[0].display_kind := cdk_lm_compound;
                end;
                if Length(visible_source_indices) > 0 then
                begin
                    visible_source_indices[0] := forced_source_idx_local;
                end;
                Exit;
            end;

            forced_idx_local := -1;
            for candidate_idx_local := 0 to High(Result) do
            begin
                if same_visible_candidate_local(Result[candidate_idx_local],
                    m_forced_visible_top_candidate) then
                begin
                    forced_idx_local := candidate_idx_local;
                    Break;
                end;
            end;

            if forced_idx_local > 0 then
            begin
                tmp_candidate_local := Result[forced_idx_local];
                if m_forced_visible_top_candidate.display_kind =
                    cdk_lm_compound then
                begin
                    tmp_candidate_local.display_kind := cdk_lm_compound;
                end;
                tmp_source_idx_local := -1;
                if forced_idx_local < Length(visible_source_indices) then
                begin
                    tmp_source_idx_local := visible_source_indices[forced_idx_local];
                end;
                for candidate_idx_local := forced_idx_local downto 1 do
                begin
                    Result[candidate_idx_local] := Result[candidate_idx_local - 1];
                    if candidate_idx_local < Length(visible_source_indices) then
                    begin
                        visible_source_indices[candidate_idx_local] :=
                            visible_source_indices[candidate_idx_local - 1];
                    end;
                end;
                Result[0] := tmp_candidate_local;
                if Length(visible_source_indices) > 0 then
                begin
                    visible_source_indices[0] := tmp_source_idx_local;
                end;
                Exit;
            end;

            if Length(Result) < page_size then
            begin
                SetLength(Result, Length(Result) + 1);
                SetLength(visible_source_indices, Length(Result));
            end;
            for candidate_idx_local := High(Result) downto 1 do
            begin
                Result[candidate_idx_local] := Result[candidate_idx_local - 1];
                visible_source_indices[candidate_idx_local] :=
                    visible_source_indices[candidate_idx_local - 1];
            end;
            if Length(Result) > 0 then
            begin
                Result[0] := m_forced_visible_top_candidate;
                visible_source_indices[0] := forced_source_idx_local;
            end;
        end;

        procedure demote_visible_long_sentence_partials_below_complete;
        var
            idx_local: Integer;
            write_idx_local: Integer;
            complete_count_local: Integer;
            partial_count_local: Integer;
            ordered_candidates_local: TArray<TncCandidate>;
            ordered_sources_local: TArray<Integer>;

            function is_visible_full_complete_local(
                const candidate_value: TncCandidate): Boolean;
            begin
                Result := (expected_units >= 6) and
                    (Trim(candidate_value.comment) = '') and
                    (get_candidate_text_unit_count(Trim(candidate_value.text)) =
                    expected_units);
            end;

            function is_visible_long_partial_local(
                const candidate_value: TncCandidate): Boolean;
            var
                text_units_local: Integer;
                comment_units_local: Integer;
            begin
                Result := False;
                if (expected_units < 6) or (Trim(candidate_value.comment) = '') then
                begin
                    Exit;
                end;

                text_units_local := get_candidate_text_unit_count(
                    Trim(candidate_value.text));
                comment_units_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(candidate_value.comment)));
                Result := (text_units_local >= 3) and (comment_units_local > 0) and
                    (text_units_local + comment_units_local = expected_units);
            end;
        begin
            if (expected_units < 6) or short_exact_query_mode or
                (Length(Result) < 3) then
            begin
                Exit;
            end;

            complete_count_local := 0;
            partial_count_local := 0;
            for idx_local := 0 to High(Result) do
            begin
                if is_visible_full_complete_local(Result[idx_local]) then
                begin
                    Inc(complete_count_local);
                    Continue;
                end;

                if is_visible_long_partial_local(Result[idx_local]) then
                begin
                    Inc(partial_count_local);
                    Continue;
                end;

            end;

            if (complete_count_local <= 0) or (partial_count_local = 0) or
                (Length(Result) <= 1) then
            begin
                Exit;
            end;

            SetLength(ordered_candidates_local, Length(Result));
            SetLength(ordered_sources_local, Length(Result));
            write_idx_local := 0;
            for idx_local := 0 to High(Result) do
            begin
                if is_visible_full_complete_local(Result[idx_local]) then
                begin
                    ordered_candidates_local[write_idx_local] := Result[idx_local];
                    ordered_sources_local[write_idx_local] :=
                        visible_source_indices[idx_local];
                    Inc(write_idx_local);
                end;
            end;
            for idx_local := 0 to High(Result) do
            begin
                if is_visible_long_partial_local(Result[idx_local]) then
                begin
                    ordered_candidates_local[write_idx_local] := Result[idx_local];
                    ordered_sources_local[write_idx_local] :=
                        visible_source_indices[idx_local];
                    Inc(write_idx_local);
                end;
            end;
            for idx_local := 0 to High(Result) do
            begin
                if (not is_visible_full_complete_local(Result[idx_local])) and
                    (not is_visible_long_partial_local(Result[idx_local])) then
                begin
                    ordered_candidates_local[write_idx_local] := Result[idx_local];
                    ordered_sources_local[write_idx_local] :=
                        visible_source_indices[idx_local];
                    Inc(write_idx_local);
                end;
            end;

            for idx_local := 0 to High(Result) do
            begin
                Result[idx_local] := ordered_candidates_local[idx_local];
                visible_source_indices[idx_local] := ordered_sources_local[idx_local];
            end;
        end;

        procedure promote_visible_pathful_complete_over_unbacked_generated_top;
        const
            c_probe_limit = 5;
            c_max_score_deficit = 150000;
            c_min_candidate_segments = 4;
        var
            idx_local: Integer;
            best_idx_local: Integer;
            best_rank_local: Integer;
            candidate_rank_local: Integer;
            top_path_local: string;
            candidate_path_local: string;
            candidate_segments_local: Integer;
            candidate_source_idx_local: Integer;
            move_idx_local: Integer;
            picked_candidate_local: TncCandidate;
            picked_source_idx_local: Integer;

            function visible_candidate_path_local(
                const candidate_value: TncCandidate;
                const source_idx_value: Integer): string;
            var
                inferred_score_local: Integer;
                inferred_count_local: Integer;
            begin
                Result := '';
                if source_idx_value >= 0 then
                begin
                    Result := get_display_candidate_path_for_index(
                        candidate_value, source_idx_value);
                end;
                if (Result = '') and (normalized_pinyin <> '') then
                begin
                    Result := infer_segment_path_for_query_text_with_score(
                        normalized_pinyin, Trim(candidate_value.text),
                        inferred_score_local, inferred_count_local);
                end;
            end;
        begin
            if short_exact_query_mode or (expected_units < 6) or
                (Length(Result) < 2) or (Trim(Result[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(Result[0].text)) <>
                expected_units) then
            begin
                Exit;
            end;

            top_path_local := visible_candidate_path_local(Result[0],
                visible_source_indices[0]);
            if get_encoded_path_segment_count_local(top_path_local) > 1 then
            begin
                Exit;
            end;
            if (normalized_pinyin <> '') and
                display_exact_key_has_text(normalized_pinyin, Trim(Result[0].text)) then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_rank_local := Low(Integer);
            for idx_local := 1 to Min(c_probe_limit - 1, High(Result)) do
            begin
                if (Trim(Result[idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(Result[idx_local].text)) <>
                    expected_units) or
                    (Result[idx_local].score + c_max_score_deficit <
                    Result[0].score) then
                begin
                    Continue;
                end;

                candidate_source_idx_local := -1;
                if idx_local < Length(visible_source_indices) then
                begin
                    candidate_source_idx_local := visible_source_indices[idx_local];
                end;
                candidate_path_local := visible_candidate_path_local(
                    Result[idx_local], candidate_source_idx_local);
                candidate_segments_local :=
                    get_encoded_path_segment_count_local(candidate_path_local);
                if candidate_segments_local < c_min_candidate_segments then
                begin
                    Continue;
                end;

                candidate_rank_local := candidate_segments_local * 1000 +
                    Result[idx_local].score div 64;
                if candidate_source_idx_local >= 0 then
                begin
                    Inc(candidate_rank_local, 100);
                end;

                if candidate_rank_local > best_rank_local then
                begin
                    best_rank_local := candidate_rank_local;
                    best_idx_local := idx_local;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            picked_candidate_local := Result[best_idx_local];
            picked_source_idx_local := visible_source_indices[best_idx_local];
            for move_idx_local := best_idx_local downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[0] := picked_candidate_local;
            visible_source_indices[0] := picked_source_idx_local;
        end;

        procedure promote_visible_higher_scored_complete_long_candidate;
        const
            c_probe_limit = 6;
            c_min_score_gain = 12000;
            c_min_coverage_gain = 1200;
            c_max_score_deficit = 120000;
            c_min_protected_exact_weight = 700;
            c_protected_exact_margin = 80;
            c_min_pathless_over_pathful_score_gain = 40000;
            c_min_pathless_over_pathful_coverage_gain = 8000;
        var
            best_idx_local: Integer;
            best_score_local: Integer;
            best_metric_local: Integer;
            score_best_idx_local: Integer;
            score_best_score_local: Integer;
            candidate_idx_local: Integer;
            candidate_score_local: Integer;
            top_coverage_local: Integer;
            candidate_coverage_local: Integer;
            candidate_metric_local: Integer;
            move_idx_local: Integer;
            picked_candidate_local: TncCandidate;
            picked_source_idx_local: Integer;
            top_path_segments_local: Integer;
            candidate_path_segments_local: Integer;
            top_effective_score_local: Integer;
            candidate_text_local: string;
            weak_pathless_over_pathful_local: Boolean;

            function is_complete_long_candidate_local(
                const candidate_value: TncCandidate): Boolean;
            var
                text_local: string;
            begin
                text_local := Trim(candidate_value.text);
                Result := (text_local <> '') and
                    (Trim(candidate_value.comment) = '') and
                    (get_candidate_text_unit_count(text_local) = expected_units);
            end;

            function effective_score_local(
                const candidate_value: TncCandidate): Integer;
            begin
                Result := candidate_value.score;
                if candidate_value.has_dict_weight and
                    (candidate_value.dict_weight > Result) then
                begin
                    Result := candidate_value.dict_weight;
                end;
                if candidate_value.source = cs_user then
                begin
                    Inc(Result, c_user_score_bonus);
                end;
            end;

            function candidate_replaces_stronger_same_position_exact_local(
                const top_candidate_local: TncCandidate;
                const candidate_value: TncCandidate): Boolean;
            const
                c_max_span_units = 4;
            var
                top_units_local: TArray<string>;
                candidate_units_local: TArray<string>;
                diff_idx_local: Integer;
                span_len_local: Integer;
                start_idx_local: Integer;
                query_key_local: string;
                top_span_text_local: string;
                candidate_span_text_local: string;
                top_span_weight_local: Integer;
                candidate_span_weight_local: Integer;

                function join_units_range_local(const units_value: TArray<string>;
                    const range_start_local: Integer;
                    const range_len_local: Integer): string;
                var
                    local_idx: Integer;
                begin
                    Result := '';
                    if (range_start_local < 0) or (range_len_local <= 0) or
                        (range_start_local + range_len_local > Length(units_value)) then
                    begin
                        Exit;
                    end;
                    for local_idx := range_start_local to
                        range_start_local + range_len_local - 1 do
                    begin
                        Result := Result + units_value[local_idx];
                    end;
                end;
            begin
                Result := False;
                top_units_local := split_text_units(Trim(top_candidate_local.text));
                candidate_units_local := split_text_units(Trim(candidate_value.text));
                if (Length(top_units_local) <> expected_units) or
                    (Length(candidate_units_local) <> expected_units) then
                begin
                    Exit;
                end;

                for diff_idx_local := 0 to expected_units - 1 do
                begin
                    if SameText(top_units_local[diff_idx_local],
                        candidate_units_local[diff_idx_local]) then
                    begin
                        Continue;
                    end;

                    for span_len_local := 2 to Min(c_max_span_units,
                        expected_units) do
                    begin
                        for start_idx_local := Max(0,
                            diff_idx_local - span_len_local + 1) to
                            Min(diff_idx_local,
                            expected_units - span_len_local) do
                        begin
                            query_key_local := build_display_query_key(
                                start_idx_local, span_len_local);
                            top_span_text_local := join_units_range_local(
                                top_units_local, start_idx_local,
                                span_len_local);
                            candidate_span_text_local := join_units_range_local(
                                candidate_units_local, start_idx_local,
                                span_len_local);
                            if SameText(top_span_text_local,
                                candidate_span_text_local) then
                            begin
                                Continue;
                            end;

                            top_span_weight_local := 0;
                            candidate_span_weight_local := 0;
                            if (query_key_local <> '') and
                                get_display_exact_text_weight(query_key_local,
                                top_span_text_local, span_len_local,
                                top_span_weight_local) and
                                get_display_exact_text_weight(query_key_local,
                                candidate_span_text_local, span_len_local,
                                candidate_span_weight_local) and
                                (top_span_weight_local >=
                                c_min_protected_exact_weight) and
                                (top_span_weight_local >=
                                candidate_span_weight_local +
                                c_protected_exact_margin) then
                            begin
                                Exit(True);
                            end;
                        end;
                    end;
                end;
            end;

            function visible_candidate_path_segment_count_local(
                const candidate_value: TncCandidate;
                const visible_idx_local: Integer): Integer;
            var
                source_idx_local: Integer;
                path_local: string;
            begin
                path_local := '';
                source_idx_local := -1;
                if visible_idx_local < Length(visible_source_indices) then
                begin
                    source_idx_local := visible_source_indices[visible_idx_local];
                end;
                if source_idx_local >= 0 then
                begin
                    if source_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        path_local := m_candidate_segment_paths[source_idx_local];
                    end;
                    if path_local = '' then
                    begin
                        path_local := get_segment_path_for_candidate(
                            candidate_value, source_idx_local);
                    end;
                end;
                if path_local = '' then
                begin
                    path_local := get_segment_path_for_candidate(
                        candidate_value, source_idx_local);
                end;
                Result := get_encoded_path_segment_count_local(path_local);
            end;
        begin
            if short_exact_query_mode or (expected_units < 6) or
                (Length(Result) < 2) or (Result[0].source = cs_user) or
                (not is_complete_long_candidate_local(Result[0])) then
            begin
                Exit;
            end;

            top_effective_score_local := effective_score_local(Result[0]);
            top_path_segments_local :=
                visible_candidate_path_segment_count_local(Result[0], 0);
            best_idx_local := 0;
            best_score_local := top_effective_score_local;
            score_best_idx_local := 0;
            score_best_score_local := best_score_local;
            top_coverage_local :=
                get_display_complete_exact_span_coverage_score(Result[0]);
            best_metric_local := top_coverage_local + best_score_local div 12;
            for candidate_idx_local := 1 to Min(c_probe_limit - 1,
                High(Result)) do
            begin
                if not is_complete_long_candidate_local(
                    Result[candidate_idx_local]) then
                begin
                    Continue;
                end;

                candidate_score_local := effective_score_local(
                    Result[candidate_idx_local]);
                candidate_text_local := Trim(Result[candidate_idx_local].text);
                candidate_path_segments_local :=
                    visible_candidate_path_segment_count_local(
                    Result[candidate_idx_local], candidate_idx_local);
                weak_pathless_over_pathful_local :=
                    (top_path_segments_local > 1) and
                    (candidate_path_segments_local <= 1) and
                    ((normalized_pinyin = '') or
                    (not display_exact_key_has_text(normalized_pinyin,
                    candidate_text_local))) and
                    (candidate_score_local < top_effective_score_local +
                    c_min_pathless_over_pathful_score_gain);

                if (not weak_pathless_over_pathful_local) and
                    (candidate_score_local > score_best_score_local) then
                begin
                    score_best_score_local := candidate_score_local;
                    score_best_idx_local := candidate_idx_local;
                end;
                candidate_coverage_local :=
                    get_display_complete_exact_span_coverage_score(
                    Result[candidate_idx_local]);
                candidate_metric_local := candidate_coverage_local +
                    candidate_score_local div 12;
                if (candidate_coverage_local >= top_coverage_local +
                    c_min_coverage_gain) and
                    ((not weak_pathless_over_pathful_local) or
                    (candidate_coverage_local >= top_coverage_local +
                    c_min_pathless_over_pathful_coverage_gain)) and
                    (candidate_score_local + c_max_score_deficit >=
                    best_score_local) and
                    (not candidate_replaces_stronger_same_position_exact_local(
                    Result[0], Result[candidate_idx_local])) and
                    (not display_candidate_replaces_protected_conjunction_boundary(
                    Result[candidate_idx_local])) and
                    (candidate_metric_local > best_metric_local) then
                begin
                    best_metric_local := candidate_metric_local;
                    best_score_local := candidate_score_local;
                    best_idx_local := candidate_idx_local;
                end;
            end;

            if (best_idx_local <= 0) and (score_best_idx_local > 0) and
                (score_best_score_local >= effective_score_local(Result[0]) +
                c_min_score_gain) then
            begin
                best_idx_local := score_best_idx_local;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            picked_candidate_local := Result[best_idx_local];
            picked_source_idx_local := visible_source_indices[best_idx_local];
            for move_idx_local := best_idx_local downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[0] := picked_candidate_local;
            visible_source_indices[0] := picked_source_idx_local;
            if m_config.debug_mode then
            begin
                m_last_full_path_debug_info := m_last_full_path_debug_info +
                    Format(' vhscore=[idx=%d text=%s]',
                    [best_idx_local, Trim(Result[0].text)]);
            end;
        end;

        procedure promote_visible_short_particle_tail_completion_over_partial;
        var
            partial_idx_local: Integer;
            complete_idx_local: Integer;
            move_idx_local: Integer;
            partial_text_local: string;
            tail_key_local: string;
            tail_text_local: string;
            complete_text_local: string;
            moved_candidate_local: TncCandidate;
            moved_source_idx_local: Integer;

            function particle_tail_text_local(const tail_key_value: string): string;
            begin
                Result := '';
                if tail_key_value = 'de' then
                begin
                    Result := string(Char($7684));
                end
                else if tail_key_value = 'le' then
                begin
                    Result := string(Char($4E86));
                end
                else if tail_key_value = 'zhe' then
                begin
                    Result := string(Char($7740));
                end
                else if tail_key_value = 'la' then
                begin
                    Result := string(Char($5566));
                end
                else if tail_key_value = 'ba' then
                begin
                    Result := string(Char($5427));
                end
                else if tail_key_value = 'ma' then
                begin
                    Result := string(Char($5417));
                end
                else if tail_key_value = 'a' then
                begin
                    Result := string(Char($554A));
                end
                else if tail_key_value = 'hao' then
                begin
                    Result := string(Char($597D));
                end;
            end;

            function text_ends_with_tail_particle_local(
                const text_value: string; const tail_text_value: string): Boolean;
            var
                trimmed_text_local: string;
                last_char_local: string;
            begin
                Result := False;
                trimmed_text_local := Trim(text_value);
                if (trimmed_text_local = '') or (tail_text_value = '') then
                begin
                    Exit;
                end;
                last_char_local := Copy(trimmed_text_local,
                    Length(trimmed_text_local), 1);
                Result := SameText(last_char_local, tail_text_value);
            end;
        begin
            for partial_idx_local := 0 to High(Result) do
            begin
                tail_key_local := normalize_pinyin_text(
                    Trim(Result[partial_idx_local].comment));
                if tail_key_local = '' then
                begin
                    Continue;
                end;

                tail_text_local := particle_tail_text_local(tail_key_local);
                if tail_text_local = '' then
                begin
                    Continue;
                end;

                partial_text_local := Trim(Result[partial_idx_local].text);
                if (get_candidate_text_unit_count(partial_text_local) < 2) or
                    (get_candidate_text_unit_count(partial_text_local) > 3) or
                    text_ends_with_tail_particle_local(partial_text_local,
                    tail_text_local) then
                begin
                    Continue;
                end;

                if SameText(tail_key_local, 'hao') and
                    (not text_ends_with_tail_particle_local(partial_text_local,
                    string(Char($5F97)))) then
                begin
                    Continue;
                end;

                complete_text_local := partial_text_local + tail_text_local;
                for complete_idx_local := partial_idx_local + 1 to High(Result) do
                begin
                    if (Trim(Result[complete_idx_local].comment) = '') and
                        SameText(Trim(Result[complete_idx_local].text),
                        complete_text_local) then
                    begin
                        moved_candidate_local := Result[complete_idx_local];
                        moved_source_idx_local := visible_source_indices[
                            complete_idx_local];
                        for move_idx_local := complete_idx_local downto
                            partial_idx_local + 1 do
                        begin
                            Result[move_idx_local] := Result[move_idx_local - 1];
                            visible_source_indices[move_idx_local] :=
                                visible_source_indices[move_idx_local - 1];
                        end;
                        Result[partial_idx_local] := moved_candidate_local;
                        visible_source_indices[partial_idx_local] :=
                            moved_source_idx_local;
                        Break;
                    end;
                end;
            end;
        end;

        procedure supplement_visible_short_prefix_exact_alternatives;
        const
            c_probe_limit = 8;
            c_max_added_per_prefix = 4;
        var
            base_idx_local: Integer;
            exact_idx_local: Integer;
            existing_idx_local: Integer;
            insert_pos_local: Integer;
            added_count_local: Integer;
            prefix_units_local: Integer;
            tail_units_local: Integer;
            prefix_key_local: string;
            tail_key_local: string;
            candidate_tail_local: string;
            suffix_key_local: string;
            exact_results_local: TncCandidateList;
            exact_text_local: string;
            new_candidate_local: TncCandidate;
            moved_candidate_local: TncCandidate;
            moved_source_local: Integer;
            base_exact_idx_local: Integer;

            procedure find_visible_candidate_index_local(const local_text: string;
                const local_comment: string; out out_idx: Integer);
            var
                local_idx: Integer;
            begin
                out_idx := -1;
                for local_idx := 0 to High(Result) do
                begin
                    if SameText(Trim(Result[local_idx].text), local_text) and
                        SameText(Trim(Result[local_idx].comment),
                        local_comment) then
                    begin
                        out_idx := local_idx;
                        Exit;
                    end;
                end;
            end;

            function exact_results_contain_text_local(
                const local_text: string): Boolean;
            var
                local_idx: Integer;
            begin
                Result := False;
                for local_idx := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[local_idx].comment) = '') and
                        SameText(Trim(exact_results_local[local_idx].text),
                        local_text) and
                        (get_candidate_text_unit_count(
                        Trim(exact_results_local[local_idx].text)) =
                        prefix_units_local) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function exact_text_index_local(const local_text: string): Integer;
            var
                local_idx: Integer;
            begin
                Result := -1;
                for local_idx := 0 to High(exact_results_local) do
                begin
                    if (Trim(exact_results_local[local_idx].comment) = '') and
                        SameText(Trim(exact_results_local[local_idx].text),
                        local_text) and
                        (get_candidate_text_unit_count(
                        Trim(exact_results_local[local_idx].text)) =
                        prefix_units_local) then
                    begin
                        Exit(local_idx);
                    end;
                end;
            end;

            function visible_candidate_is_same_prefix_partial_local(
                const local_candidate: TncCandidate): Boolean;
            var
                local_text: string;
                local_comment: string;
                local_units: Integer;
            begin
                local_text := Trim(local_candidate.text);
                local_comment := normalize_pinyin_text(
                    Trim(local_candidate.comment));
                local_units := get_candidate_text_unit_count(local_text);
                Result := (local_text <> '') and
                    SameText(local_comment, tail_key_local) and
                    (local_units = prefix_units_local) and
                    exact_results_contain_text_local(local_text);
            end;

            procedure remove_visible_candidate_local(const local_idx: Integer);
            var
                move_idx: Integer;
            begin
                if (local_idx < 0) or (local_idx > High(Result)) then
                begin
                    Exit;
                end;
                for move_idx := local_idx to High(Result) - 1 do
                begin
                    Result[move_idx] := Result[move_idx + 1];
                    visible_source_indices[move_idx] :=
                        visible_source_indices[move_idx + 1];
                end;
                SetLength(Result, Length(Result) - 1);
                SetLength(visible_source_indices, Length(visible_source_indices) - 1);
            end;

            procedure insert_visible_candidate_local(const local_idx: Integer;
                const local_candidate: TncCandidate;
                const local_source_idx: Integer);
            var
                old_len: Integer;
                new_len: Integer;
                insert_idx: Integer;
                move_idx: Integer;
            begin
                if visible_page_size <= 0 then
                begin
                    Exit;
                end;
                old_len := Length(Result);
                if old_len < visible_page_size then
                begin
                    new_len := old_len + 1;
                end
                else
                begin
                    new_len := old_len;
                end;
                if new_len <= 0 then
                begin
                    Exit;
                end;

                insert_idx := local_idx;
                if insert_idx < 0 then
                begin
                    insert_idx := 0;
                end
                else if insert_idx >= new_len then
                begin
                    insert_idx := new_len - 1;
                end;

                SetLength(Result, new_len);
                SetLength(visible_source_indices, new_len);
                for move_idx := new_len - 1 downto insert_idx + 1 do
                begin
                    Result[move_idx] := Result[move_idx - 1];
                    visible_source_indices[move_idx] :=
                        visible_source_indices[move_idx - 1];
                end;
                Result[insert_idx] := local_candidate;
                visible_source_indices[insert_idx] := local_source_idx;
            end;

            function is_number_unit_for_prefix_partial_local(
                const unit_value: string): Boolean;
            begin
                Result := False;
                if (unit_value = '') or
                    (get_candidate_text_unit_count(unit_value) <> 1) then
                begin
                    Exit;
                end;
                case Ord(unit_value[1]) of
                    $4E00, $4E8C, $4E24, $4FE9, $4E09, $56DB, $4E94,
                    $516D, $4E03, $516B, $4E5D, $5341, $51E0, $534A:
                        Result := True;
                end;
            end;

            function is_classifier_unit_for_prefix_partial_local(
                const unit_value: string): Boolean;
            begin
                Result := False;
                if (unit_value = '') or
                    (get_candidate_text_unit_count(unit_value) <> 1) then
                begin
                    Exit;
                end;
                case Ord(unit_value[1]) of
                    $4E2A, $6761, $53EA, $672C, $53F0, $90E8, $6279,
                    $5339, $6839, $7247, $9875, $4EF6, $9879, $6B21,
                    $6B65, $8F6E, $628A, $526F, $5BF9, $53CC, $74F6,
                    $6876, $76D8, $6247, $95E8, $5B57, $884C, $5217,
                    $5C42, $7AE0, $8282, $6BB5, $680B, $5E62:
                        Result := True;
                end;
            end;

            function is_rejected_quantity_partial_local(
                const text_value: string;
                const tail_key_value: string): Boolean;
            var
                units_local: TArray<string>;
                normalized_tail_local: string;
            begin
                normalized_tail_local := normalize_pinyin_text(tail_key_value);
                if (normalized_tail_local <> 'dao') and
                    (normalized_tail_local <> 'da') and
                    (normalized_tail_local <> 'd') and
                    (normalized_tail_local <> 'le') and
                    (normalized_tail_local <> 'de') and
                    (normalized_tail_local <> 'guo') and
                    (normalized_tail_local <> 'zhe') and
                    (normalized_tail_local <> 'ma') and
                    (normalized_tail_local <> 'ba') and
                    (normalized_tail_local <> 'ne') and
                    (normalized_tail_local <> 'a') and
                    (normalized_tail_local <> 'ya') and
                    (normalized_tail_local <> 'la') then
                begin
                    Exit(False);
                end;
                units_local := split_text_units(Trim(text_value));
                Result := (Length(units_local) = 2) and
                    is_number_unit_for_prefix_partial_local(units_local[0]) and
                    is_classifier_unit_for_prefix_partial_local(units_local[1]);
            end;

            procedure supplement_direct_exact_prefixes_local;
            const
                c_max_direct_added = 4;
            var
                direct_prefix_units_local: Integer;
                direct_tail_units_local: Integer;
                direct_exact_idx_local: Integer;
                direct_existing_idx_local: Integer;
                direct_source_idx_local: Integer;
                direct_scan_idx_local: Integer;
                direct_insert_pos_local: Integer;
                direct_added_local: Integer;
                direct_min_prefix_units_local: Integer;
                direct_visible_prefix_units_local: Integer;
                direct_visible_tail_units_local: Integer;
                direct_prefix_key_local: string;
                direct_tail_key_local: string;
                direct_exact_text_local: string;
                direct_exact_results_local: TncCandidateList;
                direct_candidate_local: TncCandidate;
                direct_moved_candidate_local: TncCandidate;
                direct_moved_source_local: Integer;
                direct_found_for_length_local: Boolean;

                function find_source_candidate_index_local(
                    const local_text: string;
                    const local_comment: string): Integer;
                var
                    local_idx: Integer;
                begin
                    Result := -1;
                    for local_idx := 0 to High(m_candidates) do
                    begin
                        if SameText(Trim(m_candidates[local_idx].text),
                            local_text) and
                            SameText(normalize_pinyin_text(
                            Trim(m_candidates[local_idx].comment)),
                            local_comment) then
                        begin
                            Exit(local_idx);
                        end;
                    end;
                end;

            begin
                direct_added_local := 0;
                direct_insert_pos_local := 0;
                direct_min_prefix_units_local := 2;
                for direct_scan_idx_local := 0 to High(Result) do
                begin
                    direct_visible_prefix_units_local :=
                        get_candidate_text_unit_count(
                        Trim(Result[direct_scan_idx_local].text));
                    direct_visible_tail_units_local :=
                        get_effective_compact_pinyin_unit_count(
                        normalize_pinyin_text(
                        Trim(Result[direct_scan_idx_local].comment)));
                    if (direct_visible_tail_units_local > 0) and
                        (direct_visible_prefix_units_local +
                        direct_visible_tail_units_local = expected_units) and
                        (direct_visible_prefix_units_local >
                        direct_min_prefix_units_local) then
                    begin
                        direct_min_prefix_units_local :=
                            direct_visible_prefix_units_local;
                    end;
                end;
                while (direct_insert_pos_local < Length(Result)) and
                    (Trim(Result[direct_insert_pos_local].comment) = '') do
                begin
                    Inc(direct_insert_pos_local);
                end;
                if direct_insert_pos_local >= visible_page_size then
                begin
                    direct_insert_pos_local := Max(1,
                        visible_page_size - c_max_direct_added);
                end;

                if expected_units - 1 < direct_min_prefix_units_local then
                begin
                    Exit;
                end;
                for direct_prefix_units_local := expected_units - 1 downto
                    direct_min_prefix_units_local do
                begin
                    if direct_added_local >= c_max_direct_added then
                    begin
                        Break;
                    end;
                    direct_tail_units_local := expected_units -
                        direct_prefix_units_local;
                    direct_prefix_key_local := build_display_query_key(0,
                        direct_prefix_units_local);
                    direct_tail_key_local := build_display_query_key(
                        direct_prefix_units_local, direct_tail_units_local);
                    if (direct_prefix_key_local = '') or
                        (direct_tail_key_local = '') or
                        (not lookup_display_exact_cached(
                        direct_prefix_key_local, direct_exact_results_local)) then
                    begin
                        Continue;
                    end;

                    direct_found_for_length_local := False;
                    for direct_exact_idx_local := 0 to
                        Min(c_probe_limit - 1,
                        High(direct_exact_results_local)) do
                    begin
                        direct_exact_text_local := Trim(
                            direct_exact_results_local[
                            direct_exact_idx_local].text);
                        if (Trim(direct_exact_results_local[
                            direct_exact_idx_local].comment) <> '') or
                            (direct_exact_text_local = '') or
                            (get_candidate_text_unit_count(
                            direct_exact_text_local) <>
                            direct_prefix_units_local) then
                        begin
                            Continue;
                        end;

                        if is_rejected_quantity_partial_local(
                            direct_exact_text_local,
                            direct_tail_key_local) then
                        begin
                            Continue;
                        end;

                        { Prefer an existing source candidate so selection keeps
                          its path metadata. Some exact prefixes are no longer
                          in the retained pool, so use the dictionary candidate
                          only after applying the same boundary rejection. }
                        direct_source_idx_local :=
                            find_source_candidate_index_local(
                            direct_exact_text_local, direct_tail_key_local);
                        if direct_source_idx_local >= 0 then
                        begin
                            direct_candidate_local :=
                                m_candidates[direct_source_idx_local];
                        end
                        else
                        begin
                            direct_candidate_local :=
                                direct_exact_results_local[
                                direct_exact_idx_local];
                            direct_candidate_local.comment :=
                                direct_tail_key_local;
                        end;

                        if display_candidate_should_drop_short_invalid_partial(
                            direct_candidate_local) then
                        begin
                            Continue;
                        end;
                        direct_found_for_length_local := True;
                        find_visible_candidate_index_local(
                            direct_exact_text_local, direct_tail_key_local,
                            direct_existing_idx_local);
                        if direct_existing_idx_local >= 0 then
                        begin
                            if direct_existing_idx_local >=
                                direct_insert_pos_local then
                            begin
                                direct_moved_candidate_local := Result[
                                    direct_existing_idx_local];
                                direct_moved_source_local :=
                                    visible_source_indices[
                                    direct_existing_idx_local];
                                remove_visible_candidate_local(
                                    direct_existing_idx_local);
                                insert_visible_candidate_local(
                                    direct_insert_pos_local,
                                    direct_moved_candidate_local,
                                    direct_moved_source_local);
                                Inc(direct_insert_pos_local);
                            end;
                            Continue;
                        end;

                        insert_visible_candidate_local(
                            direct_insert_pos_local, direct_candidate_local,
                            direct_source_idx_local);
                        Inc(direct_insert_pos_local);
                        Inc(direct_added_local);
                        if direct_added_local >= c_max_direct_added then
                        begin
                            Break;
                        end;
                    end;
                    if direct_found_for_length_local then
                    begin
                        Break;
                    end;
                end;
            end;
        begin
            if (m_page_index <> 0) or (m_dictionary = nil) or
                (expected_units < 3) or (expected_units > 5) or
                (not is_full_pinyin_key(normalized_pinyin)) or
                (Pos('''', m_last_lookup_key) > 0) or (Length(Result) = 0) then
            begin
                Exit;
            end;

            { Exact phrase prefixes must remain reachable even when an
              incremental fixed-head candidate and rich single-character
              fallbacks would otherwise occupy the whole first page. }
            supplement_direct_exact_prefixes_local;

            for base_idx_local := 0 to High(Result) do
            begin
                candidate_tail_local := Trim(Result[base_idx_local].comment);
                tail_key_local := normalize_pinyin_text(candidate_tail_local);
                if tail_key_local = '' then
                begin
                    Continue;
                end;

                prefix_units_local := get_candidate_text_unit_count(
                    Trim(Result[base_idx_local].text));
                tail_units_local := get_effective_compact_pinyin_unit_count(
                    tail_key_local);
                if (prefix_units_local < 2) or (tail_units_local <= 0) or
                    (prefix_units_local + tail_units_local <> expected_units) then
                begin
                    Continue;
                end;

                prefix_key_local := build_display_query_key(0,
                    prefix_units_local);
                suffix_key_local := build_display_query_key(prefix_units_local,
                    tail_units_local);
                if (prefix_key_local = '') or (suffix_key_local = '') or
                    (not SameText(suffix_key_local, tail_key_local)) or
                    (not lookup_display_exact_cached(prefix_key_local,
                    exact_results_local)) then
                begin
                    Continue;
                end;

                base_exact_idx_local := exact_text_index_local(
                    Trim(Result[base_idx_local].text));
                if base_exact_idx_local < 0 then
                begin
                    Continue;
                end;

                insert_pos_local := base_idx_local + 1;
                while (insert_pos_local <= High(Result)) and
                    visible_candidate_is_same_prefix_partial_local(
                    Result[insert_pos_local]) do
                begin
                    Inc(insert_pos_local);
                end;

                added_count_local := 0;
                for exact_idx_local := 0 to High(exact_results_local) do
                begin
                    if exact_idx_local >= c_probe_limit then
                    begin
                        Break;
                    end;
                    if exact_idx_local < base_exact_idx_local then
                    begin
                        Continue;
                    end;

                    exact_text_local := Trim(exact_results_local[exact_idx_local].text);
                    if (Trim(exact_results_local[exact_idx_local].comment) <> '') or
                        (exact_text_local = '') or
                        (get_candidate_text_unit_count(exact_text_local) <>
                        prefix_units_local) then
                    begin
                        Continue;
                    end;

                    new_candidate_local := exact_results_local[exact_idx_local];
                    new_candidate_local.comment := candidate_tail_local;
                    if is_rejected_quantity_partial_local(exact_text_local,
                        candidate_tail_local) or
                        display_candidate_should_drop_short_invalid_partial(
                        new_candidate_local) then
                    begin
                        Continue;
                    end;

                    find_visible_candidate_index_local(exact_text_local,
                        candidate_tail_local, existing_idx_local);
                    if existing_idx_local >= 0 then
                    begin
                        if existing_idx_local < insert_pos_local then
                        begin
                            Continue;
                        end;
                        moved_candidate_local := Result[existing_idx_local];
                        moved_source_local := visible_source_indices[existing_idx_local];
                        remove_visible_candidate_local(existing_idx_local);
                        insert_visible_candidate_local(insert_pos_local,
                            moved_candidate_local, moved_source_local);
                        Inc(insert_pos_local);
                        Continue;
                    end;

                    insert_visible_candidate_local(insert_pos_local,
                        new_candidate_local, -1);
                    Inc(insert_pos_local);
                    Inc(added_count_local);
                    if added_count_local >= c_max_added_per_prefix then
                    begin
                        Break;
                    end;
                end;
                Exit;
            end;
        end;

        procedure dedupe_visible_text_comment_candidates;
        var
            seen_keys_local: TDictionary<string, Boolean>;
            read_idx_local: Integer;
            write_idx_local: Integer;
            candidate_key_local: string;
        begin
            if Length(Result) <= 1 then
            begin
                Exit;
            end;

            seen_keys_local := TDictionary<string, Boolean>.Create;
            try
                write_idx_local := 0;
                for read_idx_local := 0 to High(Result) do
                begin
                    candidate_key_local := nc_visible_candidate_key(Result[read_idx_local],
                        normalize_pinyin_text(
                        Trim(Result[read_idx_local].comment)));
                    if (Trim(Result[read_idx_local].text) <> '') and
                        seen_keys_local.ContainsKey(candidate_key_local) then
                    begin
                        Continue;
                    end;

                    if Trim(Result[read_idx_local].text) <> '' then
                    begin
                        seen_keys_local.AddOrSetValue(candidate_key_local, True);
                    end;
                    if write_idx_local <> read_idx_local then
                    begin
                        Result[write_idx_local] := Result[read_idx_local];
                        visible_source_indices[write_idx_local] :=
                            visible_source_indices[read_idx_local];
                    end;
                    Inc(write_idx_local);
                end;
                if write_idx_local <> Length(Result) then
                begin
                    SetLength(Result, write_idx_local);
                    SetLength(visible_source_indices, write_idx_local);
                end;
            finally
                seen_keys_local.Free;
            end;
        end;

        procedure promote_visible_longest_exact_prefix_partial_local;
        var
            idx_local: Integer;
            move_idx_local: Integer;
            target_idx_local: Integer;
            best_idx_local: Integer;
            best_units_local: Integer;
            text_units_local: Integer;
            tail_units_local: Integer;
            prefix_key_local: string;
            support_path_local: string;
            support_score_local: Integer;
            support_segments_local: Integer;
            source_idx_local: Integer;
            picked_candidate_local: TncCandidate;
            picked_source_local: Integer;

            function candidate_has_supported_prefix_path_local(
                const candidate_idx: Integer;
                const query_key: string;
                const text_value: string): Boolean;
            begin
                Result := display_exact_key_has_text(query_key, text_value);
                if Result then
                begin
                    Exit;
                end;

                support_path_local := '';
                source_idx_local := -1;
                if candidate_idx < Length(visible_source_indices) then
                begin
                    source_idx_local := visible_source_indices[candidate_idx];
                end;
                if (source_idx_local >= 0) and
                    (source_idx_local < Length(m_candidates)) then
                begin
                    support_path_local := get_segment_path_for_candidate(
                        m_candidates[source_idx_local], source_idx_local);
                end;
                if get_encoded_path_segment_count_local(
                    support_path_local) > 1 then
                begin
                    Exit(True);
                end;

                support_path_local :=
                    infer_segment_path_for_query_text_with_score(query_key,
                    text_value, support_score_local,
                    support_segments_local, False);
                Result := (support_path_local <> '') and
                    (support_segments_local > 1);
            end;
        begin
            if (expected_units < 3) or (expected_units > 5) or
                (Length(Result) <= 1) or
                (not is_full_pinyin_key(normalized_pinyin)) then
            begin
                Exit;
            end;

            target_idx_local := 0;
            while (target_idx_local < Length(Result)) and
                (Trim(Result[target_idx_local].comment) = '') and
                (get_candidate_text_unit_count(
                Trim(Result[target_idx_local].text)) = expected_units) do
            begin
                Inc(target_idx_local);
            end;
            if target_idx_local >= Length(Result) then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_units_local := 1;
            for idx_local := target_idx_local to High(Result) do
            begin
                if Trim(Result[idx_local].comment) = '' then
                begin
                    Continue;
                end;
                text_units_local := get_candidate_text_unit_count(
                    Trim(Result[idx_local].text));
                tail_units_local := get_effective_compact_pinyin_unit_count(
                    normalize_pinyin_text(Trim(Result[idx_local].comment)));
                if (text_units_local <= best_units_local) or
                    (text_units_local + tail_units_local <> expected_units) then
                begin
                    Continue;
                end;
                prefix_key_local := build_display_query_key(0,
                    text_units_local);
                if (prefix_key_local = '') or
                    (not candidate_has_supported_prefix_path_local(idx_local,
                    prefix_key_local, Trim(Result[idx_local].text))) then
                begin
                    Continue;
                end;
                best_idx_local := idx_local;
                best_units_local := text_units_local;
            end;
            if best_idx_local <= target_idx_local then
            begin
                Exit;
            end;

            picked_candidate_local := Result[best_idx_local];
            picked_source_local := visible_source_indices[best_idx_local];
            for move_idx_local := best_idx_local downto target_idx_local + 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[target_idx_local] := picked_candidate_local;
            visible_source_indices[target_idx_local] := picked_source_local;
        end;

        procedure promote_visible_left_boundary_when_candidate_is_stronger;
        const
            c_probe_limit = 6;
            c_min_weight_slack = 16;
        var
            idx_local: Integer;
            boundary_idx_local: Integer;
            unit_idx_local: Integer;
            best_idx_local: Integer;
            best_rank_local: Integer;
            candidate_rank_local: Integer;
            top_units_local: TArray<string>;
            candidate_units_local: TArray<string>;
            top_right_text_local: string;
            candidate_left_text_local: string;
            top_right_key_local: string;
            candidate_left_key_local: string;
            top_right_weight_local: Integer;
            candidate_left_weight_local: Integer;
            move_idx_local: Integer;
            picked_candidate_local: TncCandidate;
            picked_source_idx_local: Integer;
            outside_same_local: Boolean;
        begin
            if short_exact_query_mode or (expected_units < 6) or
                (Length(Result) < 2) or (Trim(Result[0].comment) <> '') or
                (get_candidate_text_unit_count(Trim(Result[0].text)) <>
                expected_units) then
            begin
                Exit;
            end;

            top_units_local := split_text_units(Trim(Result[0].text));
            if Length(top_units_local) <> expected_units then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_rank_local := Low(Integer);
            for idx_local := 1 to Min(c_probe_limit - 1, High(Result)) do
            begin
                if (Trim(Result[idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(Trim(Result[idx_local].text)) <>
                    expected_units) or
                    (Result[idx_local].score < Result[0].score) then
                begin
                    Continue;
                end;

                candidate_units_local := split_text_units(Trim(Result[idx_local].text));
                if Length(candidate_units_local) <> expected_units then
                begin
                    Continue;
                end;

                for boundary_idx_local := 1 to expected_units - 2 do
                begin
                    if not SameText(top_units_local[boundary_idx_local - 1],
                        candidate_units_local[boundary_idx_local - 1]) then
                    begin
                        Continue;
                    end;

                    outside_same_local := True;
                    for unit_idx_local := 0 to expected_units - 1 do
                    begin
                        if (unit_idx_local >= boundary_idx_local - 1) and
                            (unit_idx_local <= boundary_idx_local + 1) then
                        begin
                            Continue;
                        end;
                        if not SameText(top_units_local[unit_idx_local],
                            candidate_units_local[unit_idx_local]) then
                        begin
                            outside_same_local := False;
                            Break;
                        end;
                    end;
                    if not outside_same_local then
                    begin
                        Continue;
                    end;

                    top_right_text_local := top_units_local[boundary_idx_local] +
                        top_units_local[boundary_idx_local + 1];
                    candidate_left_text_local :=
                        candidate_units_local[boundary_idx_local - 1] +
                        candidate_units_local[boundary_idx_local];
                    top_right_key_local := build_display_query_key(
                        boundary_idx_local, 2);
                    candidate_left_key_local := build_display_query_key(
                        boundary_idx_local - 1, 2);

                    if (top_right_key_local = '') or
                        (candidate_left_key_local = '') or
                        (not get_display_exact_text_weight(top_right_key_local,
                        top_right_text_local, 2, top_right_weight_local)) or
                        (not get_display_exact_text_weight(candidate_left_key_local,
                        candidate_left_text_local, 2, candidate_left_weight_local)) or
                        (candidate_left_weight_local + c_min_weight_slack <
                        top_right_weight_local) then
                    begin
                        Continue;
                    end;

                    candidate_rank_local := candidate_left_weight_local -
                        top_right_weight_local + Result[idx_local].score div 128;
                    if candidate_rank_local > best_rank_local then
                    begin
                        best_rank_local := candidate_rank_local;
                        best_idx_local := idx_local;
                    end;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            picked_candidate_local := Result[best_idx_local];
            picked_source_idx_local := visible_source_indices[best_idx_local];
            for move_idx_local := best_idx_local downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[0] := picked_candidate_local;
            visible_source_indices[0] := picked_source_idx_local;
        end;

        procedure restore_internal_supported_complete_top_over_visible_generated_top;
        var
            internal_top_local: TncCandidate;
            visible_idx_local: Integer;
            move_idx_local: Integer;
            source_idx_local: Integer;
            picked_candidate_local: TncCandidate;
            picked_source_idx_local: Integer;

            function is_complete_long_supported_candidate_local(
                const candidate_value: TncCandidate): Boolean;
            var
                text_local: string;
            begin
                text_local := Trim(candidate_value.text);
                Result := (expected_units >= 6) and
                    (not short_exact_query_mode) and
                    (text_local <> '') and
                    (Trim(candidate_value.comment) = '') and
                    (get_candidate_text_unit_count(text_local) = expected_units) and
                    (candidate_value.has_dict_weight or
                    (candidate_value.source = cs_user));
            end;

            function is_visible_unpathful_complete_local(
                const candidate_value: TncCandidate;
                const visible_index_value: Integer): Boolean;
            var
                text_local: string;
                path_local: string;
                source_idx_value: Integer;
            begin
                text_local := Trim(candidate_value.text);
                Result := False;
                if not ((expected_units >= 6) and
                    (not short_exact_query_mode) and
                    (text_local <> '') and
                    (Trim(candidate_value.comment) = '') and
                    (get_candidate_text_unit_count(text_local) = expected_units) and
                    (candidate_value.source <> cs_user)) then
                begin
                    Exit;
                end;

                path_local := '';
                if (visible_index_value >= 0) and
                    (visible_index_value < Length(visible_source_indices)) then
                begin
                    source_idx_value := visible_source_indices[visible_index_value];
                    if (source_idx_value >= 0) and
                        (source_idx_value < Length(m_candidate_segment_paths)) then
                    begin
                        path_local := Trim(m_candidate_segment_paths[source_idx_value]);
                    end;
                    if (path_local = '') and (source_idx_value >= 0) and
                        (source_idx_value <= High(m_candidates)) then
                    begin
                        path_local := Trim(get_segment_path_for_candidate(
                            m_candidates[source_idx_value], source_idx_value));
                    end;
                end;
                if path_local = '' then
                begin
                    path_local := Trim(get_segment_path_for_candidate(
                        candidate_value, -1));
                end;

                Result := (get_encoded_path_segment_count_local(path_local) <= 1) and
                    ((normalized_pinyin = '') or
                    (not display_exact_key_has_text(normalized_pinyin, text_local)));
            end;
        begin
            if (Length(Result) <= 0) or (Length(m_candidates) <= 0) then
            begin
                Exit;
            end;

            if has_entry_pathful_complete_top then
            begin
                internal_top_local := entry_top_candidate;
            end
            else
            begin
                internal_top_local := m_candidates[0];
            end;
            if (not is_complete_long_supported_candidate_local(
                internal_top_local)) or
                (not is_visible_unpathful_complete_local(Result[0], 0)) or
                SameText(Trim(Result[0].text), Trim(internal_top_local.text)) then
            begin
                Exit;
            end;

            visible_idx_local := -1;
            for source_idx_local := 0 to High(Result) do
            begin
                if same_visible_candidate_local(Result[source_idx_local],
                    internal_top_local) then
                begin
                    visible_idx_local := source_idx_local;
                    Break;
                end;
            end;

            if visible_idx_local > 0 then
            begin
                picked_candidate_local := Result[visible_idx_local];
                picked_source_idx_local := -1;
                if visible_idx_local < Length(visible_source_indices) then
                begin
                    picked_source_idx_local :=
                        visible_source_indices[visible_idx_local];
                end;
                for move_idx_local := visible_idx_local downto 1 do
                begin
                    Result[move_idx_local] := Result[move_idx_local - 1];
                    if move_idx_local < Length(visible_source_indices) then
                    begin
                        visible_source_indices[move_idx_local] :=
                            visible_source_indices[move_idx_local - 1];
                    end;
                end;
                Result[0] := picked_candidate_local;
                if Length(visible_source_indices) > 0 then
                begin
                    visible_source_indices[0] := picked_source_idx_local;
                end;
                Exit;
            end;

            if Length(Result) < page_size then
            begin
                SetLength(Result, Length(Result) + 1);
                SetLength(visible_source_indices, Length(Result));
            end;
            for move_idx_local := High(Result) downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                if move_idx_local < Length(visible_source_indices) then
                begin
                    visible_source_indices[move_idx_local] :=
                        visible_source_indices[move_idx_local - 1];
                end;
            end;
            Result[0] := internal_top_local;
            if Length(visible_source_indices) > 0 then
            begin
                visible_source_indices[0] := 0;
            end;
        end;

        procedure filter_visible_short_nonlexicon_complete_candidates;
        var
            read_idx_local: Integer;
            write_idx_local: Integer;
        begin
            if Length(Result) = 0 then
            begin
                Exit;
            end;

            write_idx_local := 0;
            for read_idx_local := 0 to High(Result) do
            begin
                keep_explicit_prefix_partial :=
                    display_candidate_is_explicit_apostrophe_prefix_partial(
                    Result[read_idx_local]);
                keep_protected_full_exact :=
                    is_protected_full_query_exact_local(
                    Result[read_idx_local]);
                if (not keep_protected_full_exact) and
                    (not keep_explicit_prefix_partial) and
                    (display_candidate_should_drop_short_invalid_partial(
                    Result[read_idx_local]) or
                    display_candidate_should_drop_short_nonlexicon_complete(
                    Result[read_idx_local],
                    visible_source_indices[read_idx_local])) then
                begin
                    Continue;
                end;

                if write_idx_local <> read_idx_local then
                begin
                    Result[write_idx_local] := Result[read_idx_local];
                    if (write_idx_local < Length(visible_source_indices)) and
                        (read_idx_local < Length(visible_source_indices)) then
                    begin
                        visible_source_indices[write_idx_local] :=
                            visible_source_indices[read_idx_local];
                    end;
                end;
                Inc(write_idx_local);
            end;

            if write_idx_local <> Length(Result) then
            begin
                SetLength(Result, write_idx_local);
                if Length(visible_source_indices) > write_idx_local then
                begin
                    SetLength(visible_source_indices, write_idx_local);
                end;
            end;
        end;

        procedure sanitize_visible_short_partial_source_paths;
        var
            idx_local: Integer;
            source_idx_local: Integer;
            path_local: string;
        begin
            if expected_units > 5 then
            begin
                Exit;
            end;

            for idx_local := 0 to High(Result) do
            begin
                if (idx_local >= Length(visible_source_indices)) or
                    (Trim(Result[idx_local].comment) = '') then
                begin
                    Continue;
                end;

                source_idx_local := visible_source_indices[idx_local];
                if (source_idx_local < 0) or
                    (source_idx_local > High(m_candidates)) then
                begin
                    Continue;
                end;

                path_local := '';
                if source_idx_local < Length(m_candidate_segment_paths) then
                begin
                    path_local := Trim(m_candidate_segment_paths[source_idx_local]);
                end;
                if path_local = '' then
                begin
                    path_local := Trim(get_segment_path_for_candidate(
                        m_candidates[source_idx_local], source_idx_local));
                end;
                if get_encoded_path_segment_count_local(path_local) <= 1 then
                begin
                    Continue;
                end;

                if not segment_path_matches_visible_text(path_local,
                    Result[idx_local].text) then
                begin
                    if source_idx_local < Length(m_candidate_segment_paths) then
                    begin
                        m_candidate_segment_paths[source_idx_local] := '';
                    end;
                    clear_segment_path_for_identity(m_candidates[source_idx_local]);
                    clear_segment_path_for_identity(Result[idx_local]);
                end;
            end;
        end;

        function candidate_is_repeated_particle_tail_complete_local(
            const candidate_value: TncCandidate): Boolean;
        var
            normalized_query_local: string;
            tail_key_local: string;
            tail_text_local: string;
            head_key_local: string;
            candidate_text_local: string;
            text_units_local: TArray<string>;
        begin
            Result := False;
            if Trim(candidate_value.comment) <> '' then
            begin
                Exit;
            end;

            normalized_query_local := normalize_pinyin_text(normalized_pinyin);
            if not try_get_short_particle_tail_query_parts(
                normalized_query_local, tail_key_local, tail_text_local,
                head_key_local) then
            begin
                Exit;
            end;

            candidate_text_local := Trim(candidate_value.text);
            text_units_local := split_text_units(candidate_text_local);
            Result := (Length(text_units_local) >= 2) and
                SameText(Trim(text_units_local[High(text_units_local)]),
                tail_text_local) and
                SameText(Trim(text_units_local[High(text_units_local) - 1]),
                tail_text_local);
        end;

        function is_short_repeated_initial_visible_query_local(
            const value: string): Boolean;
        var
            local_idx: Integer;
        begin
            Result := False;
            if (Length(value) < 2) or (Length(value) > 3) then
            begin
                Exit;
            end;
            for local_idx := 1 to Length(value) do
            begin
                if (not CharInSet(value[local_idx], ['b', 'p', 'm', 'f',
                    'd', 't', 'n', 'l', 'g', 'k', 'h', 'j', 'q', 'x',
                    'z', 'c', 's', 'r', 'y', 'w'])) or
                    ((local_idx > 1) and (value[local_idx] <> value[1])) then
                begin
                    Exit;
                end;
            end;
            Result := True;
        end;

        function candidate_is_visible_repeated_initial_reduplicated_local(
            const candidate_value: TncCandidate): Boolean;
        var
            normalized_query_local: string;
            text_units_local: TArray<string>;
            unit_count_local: Integer;
            idx_local: Integer;
            repeated_text_local: Boolean;
            repeated_tail_local: Boolean;
        begin
            Result := False;
            normalized_query_local := normalize_pinyin_text(normalized_pinyin);
            if (Trim(candidate_value.comment) <> '') or
                ((not candidate_value.has_dict_weight) and
                (candidate_value.source <> cs_user)) or
                (not is_short_repeated_initial_visible_query_local(
                normalized_query_local)) then
            begin
                Exit;
            end;

            unit_count_local := Length(normalized_query_local);
            text_units_local := split_text_units(Trim(candidate_value.text));
            if (unit_count_local < 2) or (unit_count_local > 3) or
                (Length(text_units_local) <> unit_count_local) then
            begin
                Exit;
            end;

            repeated_text_local := True;
            for idx_local := 1 to High(text_units_local) do
            begin
                if not SameText(text_units_local[idx_local], text_units_local[0]) then
                begin
                    repeated_text_local := False;
                    Break;
                end;
            end;
            repeated_tail_local := (unit_count_local = 3) and
                SameText(text_units_local[1], text_units_local[2]);
            Result := repeated_text_local or repeated_tail_local;
        end;

        function get_promoted_repeated_initial_candidate_count_local: Integer;
        var
            source_idx_local: Integer;
            candidate_local: TncCandidate;
            normalized_query_local: string;

            procedure try_count_candidate_local(const candidate_value: TncCandidate;
                const source_index_value: Integer);
            begin
                if Result >= page_size then
                begin
                    Exit;
                end;

                candidate_local := candidate_value;
                normalize_display_candidate(candidate_local);
                if candidate_is_visible_repeated_initial_reduplicated_local(
                    candidate_local) and (source_index_value >= page_size) then
                begin
                    Inc(Result);
                end;
            end;
        begin
            Result := 0;
            normalized_query_local := normalize_pinyin_text(normalized_pinyin);
            if (page_size <= 0) or
                (not is_short_repeated_initial_visible_query_local(
                normalized_query_local)) then
            begin
                Exit;
            end;

            for source_idx_local := 0 to
                High(repeated_initial_display_source_candidates) do
            begin
                try_count_candidate_local(
                    repeated_initial_display_source_candidates[source_idx_local],
                    source_idx_local);
                if Result >= page_size then
                begin
                    Exit;
                end;
            end;
            if Length(repeated_initial_display_source_candidates) > 0 then
            begin
                Exit;
            end;

            for source_idx_local := 0 to High(m_candidates) do
            begin
                try_count_candidate_local(m_candidates[source_idx_local],
                    source_idx_local);
                if Result >= page_size then
                begin
                    Exit;
                end;
            end;
        end;

        procedure promote_visible_repeated_initial_reduplicated_candidates;
        var
            ordered_candidates_local: TArray<TncCandidate>;
            ordered_sources_local: TArray<Integer>;
            idx_local: Integer;
            source_idx_local: Integer;
            write_idx_local: Integer;
            max_count_local: Integer;
            normalized_query_local: string;

            function same_display_candidate_local(const left_value,
                right_value: TncCandidate): Boolean;
            begin
                Result := SameText(Trim(left_value.text), Trim(right_value.text)) and
                    SameText(Trim(left_value.comment), Trim(right_value.comment));
            end;

            function ordered_contains_candidate_local(
                const candidate_value: TncCandidate): Boolean;
            var
                ordered_idx_local: Integer;
            begin
                Result := False;
                for ordered_idx_local := 0 to write_idx_local - 1 do
                begin
                    if same_display_candidate_local(
                        ordered_candidates_local[ordered_idx_local], candidate_value) then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            procedure append_ordered_candidate_local(
                const candidate_value: TncCandidate; const source_value: Integer);
            begin
                if (write_idx_local >= max_count_local) or
                    ordered_contains_candidate_local(candidate_value) then
                begin
                    Exit;
                end;
                ordered_candidates_local[write_idx_local] := candidate_value;
                ordered_sources_local[write_idx_local] := source_value;
                Inc(write_idx_local);
            end;
        begin
            normalized_query_local := normalize_pinyin_text(normalized_pinyin);
            if (page_size <= 0) or (m_page_index <> 0) or
                (not is_short_repeated_initial_visible_query_local(
                normalized_query_local)) then
            begin
                Exit;
            end;

            max_count_local := page_size;
            SetLength(ordered_candidates_local, max_count_local);
            SetLength(ordered_sources_local, max_count_local);
            write_idx_local := 0;
            for source_idx_local := 0 to
                High(repeated_initial_display_source_candidates) do
            begin
                candidate := repeated_initial_display_source_candidates[source_idx_local];
                normalize_display_candidate(candidate);
                if candidate_is_visible_repeated_initial_reduplicated_local(candidate) then
                begin
                    append_ordered_candidate_local(candidate, -1);
                end;
            end;
            for source_idx_local := 0 to High(m_candidates) do
            begin
                candidate := m_candidates[source_idx_local];
                normalize_display_candidate(candidate);
                if candidate_is_visible_repeated_initial_reduplicated_local(candidate) then
                begin
                    append_ordered_candidate_local(candidate, source_idx_local);
                end;
            end;
            if write_idx_local <= 0 then
            begin
                Exit;
            end;
            for idx_local := 0 to High(Result) do
            begin
                if not candidate_is_visible_repeated_initial_reduplicated_local(
                    Result[idx_local]) then
                begin
                    append_ordered_candidate_local(Result[idx_local],
                        visible_source_indices[idx_local]);
                end;
            end;
            SetLength(Result, write_idx_local);
            SetLength(visible_source_indices, write_idx_local);
            for idx_local := 0 to write_idx_local - 1 do
            begin
                Result[idx_local] := ordered_candidates_local[idx_local];
                visible_source_indices[idx_local] := ordered_sources_local[idx_local];
            end;
        end;

        procedure promote_visible_full_query_exact_over_generated_top;
        const
            c_visible_probe_limit = 9;
            c_latest_session_exact_rank_bonus = 200000000;
            c_context_exact_rank_bonus = 100000000;
            c_query_choice_exact_rank_bonus = 50000000;
            c_query_choice_exact_rank_scale = 1000;
        var
            idx_local: Integer;
            insert_idx_local: Integer;
            best_idx_local: Integer;
            best_rank_local: Integer;
            rank_local: Integer;
            top_text_local: string;
            moved_candidate_local: TncCandidate;
            moved_source_local: Integer;

            function visible_text_has_low_frequency_medical_unit_local(
                const text_value: string): Boolean;
            var
                char_idx_local: Integer;
            begin
                Result := False;
                for char_idx_local := 1 to Length(text_value) do
                begin
                    if (Ord(text_value[char_idx_local]) = $6C28) or
                        (Ord(text_value[char_idx_local]) = $708E) or
                        (Ord(text_value[char_idx_local]) = $75C9) or
                        (Ord(text_value[char_idx_local]) = $7624) or
                        (Ord(text_value[char_idx_local]) = $764C) or
                        (Ord(text_value[char_idx_local]) = $80BC) or
                        (Ord(text_value[char_idx_local]) = $80FA) or
                        (Ord(text_value[char_idx_local]) = $8148) or
                        (Ord(text_value[char_idx_local]) = $817A) or
                        (Ord(text_value[char_idx_local]) = $82C4) or
                        (Ord(text_value[char_idx_local]) = $916E) or
                        (Ord(text_value[char_idx_local]) = $916F) or
                        (Ord(text_value[char_idx_local]) = $9176) or
                        (Ord(text_value[char_idx_local]) = $9187) or
                        (Ord(text_value[char_idx_local]) = $919B) or
                        (Ord(text_value[char_idx_local]) = $94F5) then
                    begin
                        Exit(True);
                    end;
                    case Ord(text_value[char_idx_local]) of
                        $6C28, // 氨
                        $708E, // 炎
                        $75C9, // 痉
                        $7624, // 瘤
                        $764C, // 癌
                        $80BC, // 肼
                        $80FA, // 胺
                        $8148, // 腈
                        $817A, // 腺
                        $82C4, // 苄
                        $916E, // 酮
                        $916F, // 酯
                        $9176, // 酶
                        $9187, // 醇
                        $919B, // 醛
                        $94F5: // 铵
                            Exit(True);
                    end;
                end;
            end;

            function low_frequency_medical_top_has_stronger_exact_local(
                const visible_candidates: TncCandidateList): Boolean;
            const
                c_low_frequency_medical_exact_weight = 520;
                c_min_exact_weight_gap = 120;
            var
                top_weight_local: Integer;
                idx_local: Integer;
                candidate_text_local: string;
                candidate_weight_local: Integer;
            begin
                Result := False;
                if (Length(visible_candidates) <= 1) or
                    (not get_display_exact_text_weight(normalized_pinyin,
                    top_text_local, expected_units, top_weight_local)) or
                    (top_weight_local > c_low_frequency_medical_exact_weight) or
                    (not visible_text_has_low_frequency_medical_unit_local(
                    top_text_local)) then
                begin
                    Exit;
                end;

                for idx_local := 1 to Min(c_visible_probe_limit - 1,
                    High(visible_candidates)) do
                begin
                    candidate_text_local := Trim(visible_candidates[idx_local].text);
                    if (candidate_text_local = '') or
                        (Trim(visible_candidates[idx_local].comment) <> '') or
                        (get_candidate_text_unit_count(candidate_text_local) <>
                        expected_units) or
                        (not get_display_exact_text_weight(normalized_pinyin,
                        candidate_text_local, expected_units,
                        candidate_weight_local)) then
                    begin
                        Continue;
                    end;
                    if not visible_text_has_low_frequency_medical_unit_local(
                        candidate_text_local) then
                    begin
                        Exit(True);
                    end;
                    if candidate_weight_local >= top_weight_local +
                        c_min_exact_weight_gap then
                    begin
                        Exit(True);
                    end;
                end;
            end;

            function top_candidate_should_stay_local(
                const visible_candidates: TncCandidateList): Boolean;
            var
                top_query_choice_bonus_local: Integer;
            begin
                Result := True;
                if Length(visible_candidates) <= 0 then
                begin
                    Exit;
                end;

                top_text_local := Trim(visible_candidates[0].text);
                if (top_text_local = '') or
                    (Trim(visible_candidates[0].comment) <> '') or
                    (get_candidate_text_unit_count(top_text_local) <>
                    expected_units) then
                begin
                    Exit;
                end;

                if display_exact_key_has_text(normalized_pinyin,
                    top_text_local) then
                begin
                    if low_frequency_medical_top_has_stronger_exact_local(
                        visible_candidates) then
                    begin
                        Result := False;
                    end;
                    Exit;
                end;

                if is_latest_session_query_choice(top_text_local) or
                    display_candidate_is_persistent_latest_choice(
                    normalized_pinyin, top_text_local) or
                    (get_context_query_latest_bonus(top_text_local) > 0) then
                begin
                    Exit;
                end;

                if (m_dictionary <> nil) then
                begin
                    if m_dictionary.is_user_entry(normalized_pinyin,
                        top_text_local) then
                    begin
                        Exit;
                    end;

                    top_query_choice_bonus_local :=
                        m_dictionary.get_query_choice_bonus(normalized_pinyin,
                        top_text_local);
                    if top_query_choice_bonus_local > 0 then
                    begin
                        Exit;
                    end;
                end;

                Result := False;
            end;

            function full_query_exact_visible_rank_local(
                const candidate_value: TncCandidate; out out_rank: Integer): Boolean;
            var
                candidate_text_local: string;
                exact_weight_local: Integer;
                query_choice_bonus_local: Integer;
                context_bonus_local: Integer;
                suppress_choice_bonus_local: Boolean;
            begin
                Result := False;
                out_rank := Low(Integer);

                candidate_text_local := Trim(candidate_value.text);
                if (candidate_text_local = '') or
                    (Trim(candidate_value.comment) <> '') or
                    (get_candidate_text_unit_count(candidate_text_local) <>
                    expected_units) or
                    (not get_display_exact_text_weight(normalized_pinyin,
                    candidate_text_local, expected_units,
                    exact_weight_local)) then
                begin
                    Exit;
                end;

                out_rank := exact_weight_local;
                suppress_choice_bonus_local := (exact_weight_local <= 520) and
                    visible_text_has_low_frequency_medical_unit_local(
                    candidate_text_local);
                if (not suppress_choice_bonus_local) and
                    is_latest_session_query_choice(candidate_text_local) then
                begin
                    Inc(out_rank, c_latest_session_exact_rank_bonus);
                end;

                context_bonus_local := get_context_query_latest_bonus(
                    candidate_text_local);
                if (not suppress_choice_bonus_local) and
                    (context_bonus_local > 0) then
                begin
                    Inc(out_rank, c_context_exact_rank_bonus +
                        (context_bonus_local * c_query_choice_exact_rank_scale));
                end;

                if (not suppress_choice_bonus_local) and (m_dictionary <> nil) then
                begin
                    query_choice_bonus_local :=
                        m_dictionary.get_query_choice_bonus(normalized_pinyin,
                        candidate_text_local);
                    if query_choice_bonus_local > 0 then
                    begin
                        Inc(out_rank, c_query_choice_exact_rank_bonus +
                            (query_choice_bonus_local *
                            c_query_choice_exact_rank_scale));
                    end;
                end;

                Result := True;
            end;
        begin
            if (m_page_index <> 0) or (Length(Result) < 2) or
                (normalized_pinyin = '') or (expected_units < 2) or
                (expected_units > 4) or
                (not is_full_pinyin_key(normalized_pinyin)) or
                (Pos('''', normalized_pinyin) > 0) or
                top_candidate_should_stay_local(Result) then
            begin
                Exit;
            end;

            best_idx_local := -1;
            best_rank_local := Low(Integer);
            for idx_local := 1 to Min(c_visible_probe_limit - 1,
                High(Result)) do
            begin
                if full_query_exact_visible_rank_local(Result[idx_local],
                    rank_local) and
                    ((best_idx_local < 0) or (rank_local > best_rank_local)) then
                begin
                    best_idx_local := idx_local;
                    best_rank_local := rank_local;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            moved_candidate_local := Result[best_idx_local];
            moved_source_local := -1;
            if best_idx_local < Length(visible_source_indices) then
            begin
                moved_source_local := visible_source_indices[best_idx_local];
            end;

            for insert_idx_local := best_idx_local downto 1 do
            begin
                Result[insert_idx_local] := Result[insert_idx_local - 1];
                if insert_idx_local < Length(visible_source_indices) then
                begin
                    visible_source_indices[insert_idx_local] :=
                        visible_source_indices[insert_idx_local - 1];
                end;
            end;
            Result[0] := moved_candidate_local;
            if Length(visible_source_indices) > 0 then
            begin
                visible_source_indices[0] := moved_source_local;
            end;
        end;

        procedure rerank_visible_complete_long_candidates_by_lm;
        const
            c_visible_probe_limit = 3;
            c_min_average_lead = 320;
            c_min_supported_boundary_percent = 35;
            c_max_original_score_deficit = 20000;
        var
            top_average_local: Integer;
            ignored_support_local: Integer;
            ignored_boundary_local: Integer;
            top_score_local: Integer;
            idx_local: Integer;
            best_idx_local: Integer;
            average_local: Integer;
            support_local: Integer;
            boundary_local: Integer;
            score_local: Integer;
            best_average_local: Integer;
            best_support_local: Integer;
            best_boundary_local: Integer;
            best_score_local: Integer;
            move_idx_local: Integer;
            moved_candidate_local: TncCandidate;
            moved_source_local: Integer;
            top_text_local: string;
            stats_valid_local: Boolean;

            function get_visible_source_index_local(
                const visible_idx_local: Integer): Integer;
            begin
                Result := -1;
                if (visible_idx_local >= 0) and
                    (visible_idx_local < Length(visible_source_indices)) then
                begin
                    Result := visible_source_indices[visible_idx_local];
                end;
            end;

            function get_visible_original_score_local(
                const visible_idx_local: Integer): Integer;
            var
                source_idx_local: Integer;
            begin
                Result := 0;
                source_idx_local := get_visible_source_index_local(
                    visible_idx_local);
                if (source_idx_local >= 0) and
                    (source_idx_local < Length(m_candidates)) then
                begin
                    Result := m_candidates[source_idx_local].score;
                end;
            end;

            procedure get_visible_lm_stats_local(
                const visible_idx_local: Integer; out out_average: Integer;
                out out_support_count: Integer;
                out out_boundary_count: Integer; out out_valid: Boolean);
            var
                source_idx_local: Integer;
                path_local: string;
                parts_local: TArray<string>;
                part_idx_local: Integer;
                query_offset_local: Integer;
                previous_text_local: string;
                current_text_local: string;
                next_text_local: string;
                previous_units_local: Integer;
                current_units_local: Integer;
                next_units_local: Integer;
                transition_query_local: string;
                bigram_bonus_local: Integer;
                trigram_bonus_local: Integer;
                boundary_bonus_local: Integer;
                total_bonus_local: Integer;
            begin
                out_valid := False;
                out_average := 0;
                out_support_count := 0;
                out_boundary_count := 0;
                if (m_dictionary = nil) or (visible_idx_local < 0) or
                    (visible_idx_local >= Length(Result)) or
                    (Trim(Result[visible_idx_local].comment) <> '') or
                    (get_candidate_text_unit_count(
                    Trim(Result[visible_idx_local].text)) <> expected_units) then
                begin
                    Exit;
                end;

                source_idx_local := get_visible_source_index_local(
                    visible_idx_local);
                if (source_idx_local < 0) or
                    (source_idx_local >= Length(m_candidates)) then
                begin
                    Exit;
                end;

                path_local := '';
                if source_idx_local < Length(m_candidate_segment_paths) then
                begin
                    path_local := Trim(
                        m_candidate_segment_paths[source_idx_local]);
                end;
                if path_local = '' then
                begin
                    path_local := Trim(get_segment_path_for_candidate(
                        Result[visible_idx_local], source_idx_local));
                end;
                if (get_encoded_path_segment_count_local(path_local) <= 1) or
                    (not segment_path_matches_visible_text(path_local,
                    Result[visible_idx_local].text)) then
                begin
                    Exit;
                end;

                parts_local := path_local.Split([c_segment_path_separator]);
                if Length(parts_local) < 2 then
                begin
                    Exit;
                end;

                query_offset_local := 0;
                previous_text_local := '';
                previous_units_local := 0;
                total_bonus_local := 0;
                for part_idx_local := 0 to High(parts_local) - 1 do
                begin
                    current_text_local := Trim(parts_local[part_idx_local]);
                    next_text_local := Trim(parts_local[part_idx_local + 1]);
                    current_units_local := get_candidate_text_unit_count(
                        current_text_local);
                    next_units_local := get_candidate_text_unit_count(
                        next_text_local);
                    if (current_text_local = '') or (next_text_local = '') or
                        (current_units_local <= 0) or (next_units_local <= 0) or
                        (query_offset_local + current_units_local +
                        next_units_local > expected_units) then
                    begin
                        Exit;
                    end;

                    transition_query_local := build_display_query_key(
                        query_offset_local, current_units_local +
                        next_units_local);
                    bigram_bonus_local :=
                        m_dictionary.get_lm_transition_bonus(
                        transition_query_local, current_text_local +
                        c_segment_path_separator + next_text_local);
                    trigram_bonus_local := 0;
                    if (part_idx_local > 0) and
                        (previous_text_local <> '') and
                        (previous_units_local > 0) then
                    begin
                        transition_query_local := build_display_query_key(
                            query_offset_local - previous_units_local,
                            previous_units_local + current_units_local +
                            next_units_local);
                        trigram_bonus_local :=
                            m_dictionary.get_lm_transition_bonus(
                            transition_query_local, previous_text_local +
                            c_segment_path_separator + current_text_local +
                            c_segment_path_separator + next_text_local);
                    end;

                    boundary_bonus_local := Max(bigram_bonus_local,
                        trigram_bonus_local);
                    Inc(total_bonus_local, boundary_bonus_local);
                    if boundary_bonus_local > 0 then
                    begin
                        Inc(out_support_count);
                    end;
                    Inc(out_boundary_count);
                    previous_text_local := current_text_local;
                    previous_units_local := current_units_local;
                    Inc(query_offset_local, current_units_local);
                end;

                Inc(query_offset_local, get_candidate_text_unit_count(
                    Trim(parts_local[High(parts_local)])));
                if (query_offset_local <> expected_units) or
                    (out_boundary_count <= 0) then
                begin
                    Exit;
                end;

                out_average := total_bonus_local div out_boundary_count;
                out_valid := True;
            end;

            function candidate_is_better_local(const candidate_average: Integer;
                const candidate_support: Integer;
                const candidate_boundary: Integer;
                const candidate_score: Integer): Boolean;
            begin
                Result := (best_idx_local < 0) or
                    (candidate_average > best_average_local) or
                    ((candidate_average = best_average_local) and
                    (candidate_support * best_boundary_local >
                    best_support_local * candidate_boundary)) or
                    ((candidate_average = best_average_local) and
                    (candidate_support * best_boundary_local =
                    best_support_local * candidate_boundary) and
                    (candidate_support > best_support_local)) or
                    ((candidate_average = best_average_local) and
                    (candidate_support * best_boundary_local =
                    best_support_local * candidate_boundary) and
                    (candidate_support = best_support_local) and
                    (candidate_score > best_score_local));
            end;

            function candidates_differ_only_in_grammar_particles_local(
                const left_text: string; const right_text: string): Boolean;
            var
                left_units_local: TArray<string>;
                right_units_local: TArray<string>;
                unit_idx_local: Integer;
                has_difference_local: Boolean;

                function is_grammar_particle_local(
                    const unit_text: string): Boolean;
                begin
                    Result := SameText(unit_text, string(Char($7684))) or
                        SameText(unit_text, string(Char($5730))) or
                        SameText(unit_text, string(Char($5F97)));
                end;
            begin
                Result := False;
                left_units_local := split_text_units(Trim(left_text));
                right_units_local := split_text_units(Trim(right_text));
                if (Length(left_units_local) <= 0) or
                    (Length(left_units_local) <> Length(right_units_local)) then
                begin
                    Exit;
                end;

                has_difference_local := False;
                for unit_idx_local := 0 to High(left_units_local) do
                begin
                    if SameText(left_units_local[unit_idx_local],
                        right_units_local[unit_idx_local]) then
                    begin
                        Continue;
                    end;
                    if (not is_grammar_particle_local(
                        left_units_local[unit_idx_local])) or
                        (not is_grammar_particle_local(
                        right_units_local[unit_idx_local])) then
                    begin
                        Exit;
                    end;
                    has_difference_local := True;
                end;
                Result := has_difference_local;
            end;
        begin
            if (m_dictionary = nil) or short_exact_query_mode or
                (m_page_index <> 0) or (expected_units < 6) or
                (Length(Result) < 2) or forced_visible_top_candidate_applies then
            begin
                Exit;
            end;

            top_text_local := Trim(Result[0].text);
            if (top_text_local = '') or
                (Result[0].source = cs_user) or
                is_latest_session_query_choice(top_text_local) or
                display_candidate_is_persistent_latest_choice(
                normalized_pinyin, top_text_local) or
                ((m_dictionary <> nil) and
                m_dictionary.is_user_entry(normalized_pinyin,
                top_text_local)) then
            begin
                Exit;
            end;

            top_average_local := 0;
            ignored_support_local := 0;
            ignored_boundary_local := 0;
            get_visible_lm_stats_local(0, top_average_local,
                ignored_support_local, ignored_boundary_local,
                stats_valid_local);
            top_score_local := get_visible_original_score_local(0);
            best_idx_local := -1;
            best_average_local := 0;
            best_support_local := 0;
            best_boundary_local := 1;
            best_score_local := Low(Integer);

            for idx_local := 1 to Min(c_visible_probe_limit - 1,
                High(Result)) do
            begin
                get_visible_lm_stats_local(idx_local, average_local,
                    support_local, boundary_local, stats_valid_local);
                if not stats_valid_local then
                begin
                    Continue;
                end;
                score_local := get_visible_original_score_local(idx_local);
                if (average_local < top_average_local + c_min_average_lead) or
                    (support_local <= 0) or
                    (support_local * 100 < boundary_local *
                    c_min_supported_boundary_percent) or
                    (top_score_local - score_local >
                    c_max_original_score_deficit) then
                begin
                    Continue;
                end;
                if candidates_differ_only_in_grammar_particles_local(
                    Result[0].text, Result[idx_local].text) then
                begin
                    Continue;
                end;

                if candidate_is_better_local(average_local, support_local,
                    boundary_local, score_local) then
                begin
                    best_idx_local := idx_local;
                    best_average_local := average_local;
                    best_support_local := support_local;
                    best_boundary_local := boundary_local;
                    best_score_local := score_local;
                end;
            end;

            if best_idx_local <= 0 then
            begin
                Exit;
            end;

            moved_candidate_local := Result[best_idx_local];
            moved_source_local := visible_source_indices[best_idx_local];
            for move_idx_local := best_idx_local downto 1 do
            begin
                Result[move_idx_local] := Result[move_idx_local - 1];
                visible_source_indices[move_idx_local] :=
                    visible_source_indices[move_idx_local - 1];
            end;
            Result[0] := moved_candidate_local;
            visible_source_indices[0] := moved_source_local;
        end;

        procedure rerank_visible_complete_long_candidates_by_char_lm;
        const
            c_visible_probe_limit = 5;
            c_original_rank_penalty = 200;
        var
            eligible_indices_local: TArray<Integer>;
            candidate_texts_local: TArray<string>;
            lm_scores_local: TArray<Integer>;
            rerank_scores_local: TArray<Integer>;
            idx_local: Integer;
            eligible_count_local: Integer;
            rank_local: Integer;
            best_rank_local: Integer;
            scan_rank_local: Integer;
            visible_idx_local: Integer;
            best_visible_idx_local: Integer;
            temp_score_local: Integer;
            temp_candidate_local: TncCandidate;
            temp_source_local: Integer;
            top_text_local: string;
        begin
            if (m_dictionary = nil) or short_exact_query_mode or
                (m_page_index <> 0) or (expected_units < 6) or
                (Length(Result) < 2) or forced_visible_top_candidate_applies then
            begin
                Exit;
            end;

            top_text_local := Trim(Result[0].text);
            if (top_text_local = '') or (Result[0].source = cs_user) or
                is_latest_session_query_choice(top_text_local) or
                display_candidate_is_persistent_latest_choice(
                normalized_pinyin, top_text_local) or
                m_dictionary.is_user_entry(normalized_pinyin,
                top_text_local) then
            begin
                Exit;
            end;

            SetLength(eligible_indices_local, c_visible_probe_limit);
            SetLength(candidate_texts_local, c_visible_probe_limit);
            eligible_count_local := 0;
            for idx_local := 0 to Min(c_visible_probe_limit - 1,
                High(Result)) do
            begin
                if (Trim(Result[idx_local].comment) <> '') or
                    (Trim(Result[idx_local].text) = '') or
                    (Result[idx_local].source = cs_user) or
                    (get_candidate_text_unit_count(
                    Trim(Result[idx_local].text)) <> expected_units) then
                begin
                    Continue;
                end;
                eligible_indices_local[eligible_count_local] := idx_local;
                candidate_texts_local[eligible_count_local] :=
                    Trim(Result[idx_local].text);
                Inc(eligible_count_local);
            end;
            if (eligible_count_local < 2) or
                (eligible_indices_local[0] <> 0) then
            begin
                Exit;
            end;
            SetLength(eligible_indices_local, eligible_count_local);
            SetLength(candidate_texts_local, eligible_count_local);
            if not get_cached_char_lm_scores(candidate_texts_local,
                lm_scores_local, clsm_full, '') or
                (Length(lm_scores_local) <> eligible_count_local) then
            begin
                Exit;
            end;

            SetLength(rerank_scores_local, eligible_count_local);
            for rank_local := 0 to eligible_count_local - 1 do
            begin
                rerank_scores_local[rank_local] := lm_scores_local[rank_local] -
                    rank_local * c_original_rank_penalty;
            end;

            for rank_local := 0 to eligible_count_local - 2 do
            begin
                best_rank_local := rank_local;
                for scan_rank_local := rank_local + 1 to
                    eligible_count_local - 1 do
                begin
                    if rerank_scores_local[scan_rank_local] >
                        rerank_scores_local[best_rank_local] then
                    begin
                        best_rank_local := scan_rank_local;
                    end;
                end;
                if best_rank_local = rank_local then
                begin
                    Continue;
                end;

                visible_idx_local := eligible_indices_local[rank_local];
                best_visible_idx_local :=
                    eligible_indices_local[best_rank_local];
                temp_candidate_local := Result[visible_idx_local];
                Result[visible_idx_local] := Result[best_visible_idx_local];
                Result[best_visible_idx_local] := temp_candidate_local;
                temp_source_local := visible_source_indices[visible_idx_local];
                visible_source_indices[visible_idx_local] :=
                    visible_source_indices[best_visible_idx_local];
                visible_source_indices[best_visible_idx_local] :=
                    temp_source_local;
                temp_score_local := rerank_scores_local[rank_local];
                rerank_scores_local[rank_local] :=
                    rerank_scores_local[best_rank_local];
                rerank_scores_local[best_rank_local] := temp_score_local;
            end;
        end;

        procedure rerank_visible_complete_long_candidates_by_local_model_pass(
            const use_residual_model: Boolean;
            const use_pairwise_pool_model: Boolean);
        const
            c_seed_probe_limit = 5;
            c_generated_seed_limit = 1;
            c_single_option_limit = 3;
            c_pairwise_single_option_limit = 6;
            c_hidden_pool_limit = 50;
        var
            pool_local: TArray<TncLongLocalRerankPoolItem>;
            pool_index_by_text_local: TDictionary<string, Integer>;
            retained_texts_local: TDictionary<string, Boolean>;
            candidate_texts_local: TArray<string>;
            lm_scores_local: TArray<Integer>;
            order_local: TArray<Integer>;
            retained_local: TArray<TncLongLocalRerankPoolItem>;
            seed_units_local: TArray<string>;
            consensus_original_units_local: TArray<TArray<string>>;
            consensus_original_weights_local: TArray<Double>;
            consensus_top_units_local: TArray<string>;
            repair_segment_starts_local: TArray<Integer>;
            repair_segment_lengths_local: TArray<Integer>;
            single_options_local: TncCandidateList;
            item_local: TncLongLocalRerankPoolItem;
            previous_local: TncLongLocalRerankPoolItem;
            best_item_local: TncLongLocalRerankPoolItem;
            generated_candidate_local: TncCandidate;
            moved_candidate_local: TncCandidate;
            moved_source_local: Integer;
            top_text_local: string;
            syllable_key_local: string;
            replacement_text_local: string;
            variant_text_local: string;
            repaired_segment_text_local: string;
            repaired_segment_key_local: string;
            pool_key_local: string;
            source_weight_local: Integer;
            visible_idx_local: Integer;
            unit_idx_local: Integer;
            replacement_idx_local: Integer;
            pool_idx_local: Integer;
            order_idx_local: Integer;
            retained_idx_local: Integer;
            move_idx_local: Integer;
            existing_idx_local: Integer;
            baseline_idx_local: Integer;
            best_idx_local: Integer;
            original_count_local: Integer;
            retained_count_local: Integer;
            pairwise_challenger_count_local: Integer;
            pairwise_insert_rank_local: Integer;
            single_option_limit_local: Integer;
            consensus_original_count_local: Integer;
            consensus_total_weight_local: Double;
            repair_segment_start_local: Integer;
            repair_segment_length_local: Integer;
            repair_segment_unit_local: Integer;
            changed_ratio_local: Double;
            pairwise_score_local: Double;
            model_tick_local: UInt64;
            model_elapsed_local: UInt64;

            function compare_pool_order_local(const left_idx: Integer;
                const right_idx: Integer): Integer;
            var
                left_item_local: TncLongLocalRerankPoolItem;
                right_item_local: TncLongLocalRerankPoolItem;
                text_compare_local: Integer;
            begin
                left_item_local := pool_local[left_idx];
                right_item_local := pool_local[right_idx];
                if left_item_local.char_lm_score >
                    right_item_local.char_lm_score then
                begin
                    Exit(-1);
                end;
                if left_item_local.char_lm_score <
                    right_item_local.char_lm_score then
                begin
                    Exit(1);
                end;
                if left_item_local.substitutions <
                    right_item_local.substitutions then
                begin
                    Exit(-1);
                end;
                if left_item_local.substitutions >
                    right_item_local.substitutions then
                begin
                    Exit(1);
                end;
                if left_item_local.seed_rank < right_item_local.seed_rank then
                begin
                    Exit(-1);
                end;
                if left_item_local.seed_rank > right_item_local.seed_rank then
                begin
                    Exit(1);
                end;
                text_compare_local := CompareText(left_item_local.text,
                    right_item_local.text);
                if text_compare_local > 0 then
                begin
                    Exit(-1);
                end;
                if text_compare_local < 0 then
                begin
                    Exit(1);
                end;
                Result := 0;
            end;

            procedure sort_pool_order_local;
            var
                item_index_local: Integer;
                insert_index_local: Integer;
                pending_order_local: Integer;
            begin
                for item_index_local := 1 to High(order_local) do
                begin
                    pending_order_local := order_local[item_index_local];
                    insert_index_local := item_index_local - 1;
                    while (insert_index_local >= 0) and
                        (compare_pool_order_local(
                        order_local[insert_index_local],
                        pending_order_local) > 0) do
                    begin
                        order_local[insert_index_local + 1] :=
                            order_local[insert_index_local];
                        Dec(insert_index_local);
                    end;
                    order_local[insert_index_local + 1] := pending_order_local;
                end;
            end;

            function candidate_raw_weight_local(
                const value: TncCandidate): Integer;
            begin
                if value.has_dict_weight then
                begin
                    Result := value.dict_weight;
                end
                else
                begin
                    Result := value.score;
                end;
            end;

            procedure initialize_pool_item_local(
                var value: TncLongLocalRerankPoolItem);
            begin
                value.text := '';
                value.seed_rank := 0;
                value.seed_visible_index := -1;
                value.original := False;
                value.substitutions := 0;
                value.changed_position := -1;
                value.source_char_weight := 0;
                value.replacement_char_weight := 0;
                value.char_lm_score := 0;
                value.pool_rank := 0;
                value.consensus_seed_count := 0;
                value.consensus_support_mean := 0.0;
                value.consensus_support_min := 0.0;
                value.consensus_majority_units := 0;
                value.consensus_unanimous_units := 0;
                value.consensus_nearest_distance := 0;
                value.consensus_mean_distance := 0.0;
                value.consensus_changed_support := 0.0;
                value.consensus_changed_top_match := 0;
                value.model_score := 0.0;
            end;

            function get_single_options_local(const syllable_key: string;
                out options: TncCandidateList): Boolean;
            var
                raw_candidates_local: TncCandidateList;
                filtered_local: TList<TncCandidate>;
                seen_local: TDictionary<string, Boolean>;
                candidate_local: TncCandidate;
                sort_candidate_local: TncCandidate;
                text_local: string;
                idx_local: Integer;
                sort_idx_local: Integer;
                insert_idx_local: Integer;
                sort_weight_local: Integer;
            begin
                SetLength(options, 0);
                if (m_long_local_single_options_cache <> nil) and
                    m_long_local_single_options_cache.TryGetValue(
                    syllable_key, options) then
                begin
                    Exit(Length(options) > 0);
                end;
                if (syllable_key = '') or
                    (not m_dictionary.lookup_exact_full_pinyin(
                    syllable_key, raw_candidates_local)) then
                begin
                    if m_long_local_single_options_cache <> nil then
                    begin
                        m_long_local_single_options_cache.AddOrSetValue(
                            syllable_key, options);
                    end;
                    Exit(False);
                end;

                filtered_local := TList<TncCandidate>.Create;
                seen_local := TDictionary<string, Boolean>.Create;
                try
                    for idx_local := 0 to High(raw_candidates_local) do
                    begin
                        candidate_local := raw_candidates_local[idx_local];
                        text_local := Trim(candidate_local.text);
                        if (text_local = '') or
                            (Trim(candidate_local.comment) <> '') or
                            (candidate_local.source = cs_user) or
                            (get_candidate_text_unit_count(text_local) <> 1) or
                            seen_local.ContainsKey(text_local) then
                        begin
                            Continue;
                        end;
                        seen_local.Add(text_local, True);
                        filtered_local.Add(candidate_local);
                    end;
                    for sort_idx_local := 1 to filtered_local.Count - 1 do
                    begin
                        sort_candidate_local := filtered_local[sort_idx_local];
                        sort_weight_local :=
                            candidate_raw_weight_local(sort_candidate_local);
                        insert_idx_local := sort_idx_local - 1;
                        while (insert_idx_local >= 0) and
                            ((candidate_raw_weight_local(
                            filtered_local[insert_idx_local]) <
                            sort_weight_local) or
                            ((candidate_raw_weight_local(
                            filtered_local[insert_idx_local]) =
                            sort_weight_local) and
                            (CompareText(Trim(
                            filtered_local[insert_idx_local].text),
                            Trim(sort_candidate_local.text)) > 0))) do
                        begin
                            filtered_local[insert_idx_local + 1] :=
                                filtered_local[insert_idx_local];
                            Dec(insert_idx_local);
                        end;
                        filtered_local[insert_idx_local + 1] :=
                            sort_candidate_local;
                    end;
                    SetLength(options, filtered_local.Count);
                    for idx_local := 0 to filtered_local.Count - 1 do
                    begin
                        options[idx_local] := filtered_local[idx_local];
                    end;
                finally
                    seen_local.Free;
                    filtered_local.Free;
                end;
                if m_long_local_single_options_cache <> nil then
                begin
                    m_long_local_single_options_cache.AddOrSetValue(
                        syllable_key, Copy(options, 0, Length(options)));
                end;
                Result := Length(options) > 0;
            end;

            function provenance_is_better_local(
                const candidate_value: TncLongLocalRerankPoolItem;
                const previous_value: TncLongLocalRerankPoolItem): Boolean;
            begin
                if candidate_value.substitutions < previous_value.substitutions then
                begin
                    Exit(True);
                end;
                if candidate_value.substitutions > previous_value.substitutions then
                begin
                    Exit(False);
                end;
                if candidate_value.seed_rank < previous_value.seed_rank then
                begin
                    Exit(True);
                end;
                if candidate_value.seed_rank > previous_value.seed_rank then
                begin
                    Exit(False);
                end;
                Result := candidate_value.changed_position <
                    previous_value.changed_position;
            end;

            procedure retain_pool_item_local(
                const value: TncLongLocalRerankPoolItem);
            var
                new_idx_local: Integer;
            begin
                pool_key_local := LowerCase(Trim(value.text));
                if pool_key_local = '' then
                begin
                    Exit;
                end;
                if pool_index_by_text_local.TryGetValue(pool_key_local,
                    existing_idx_local) then
                begin
                    previous_local := pool_local[existing_idx_local];
                    if provenance_is_better_local(value, previous_local) then
                    begin
                        pool_local[existing_idx_local] := value;
                    end;
                    Exit;
                end;
                new_idx_local := Length(pool_local);
                SetLength(pool_local, new_idx_local + 1);
                pool_local[new_idx_local] := value;
                pool_index_by_text_local.Add(pool_key_local, new_idx_local);
            end;

            function build_variant_text_local(const units: TArray<string>;
                const changed_position: Integer;
                const replacement: string): string;
            var
                idx_local: Integer;
            begin
                Result := '';
                for idx_local := 0 to High(units) do
                begin
                    if idx_local = changed_position then
                    begin
                        Result := Result + replacement;
                    end
                    else
                    begin
                        Result := Result + units[idx_local];
                    end;
                end;
            end;

            function is_productive_particle_tail_segment_local(
                const units: TArray<string>; const segment_start: Integer;
                const segment_length: Integer;
                const changed_position: Integer): Boolean;
            var
                tail_text_local: string;
            begin
                Result := False;
                if (segment_length <> 2) or
                    (changed_position <> segment_start) or
                    (segment_start < 0) or
                    (segment_start + 1 > High(units)) then
                begin
                    Exit;
                end;
                tail_text_local := Trim(units[segment_start + 1]);
                if Length(tail_text_local) <> 1 then
                begin
                    Exit;
                end;
                case Ord(tail_text_local[1]) of
                    $7684, $5F97, $5730, $4E86, $7740,
                    $8FC7, $5427, $5417, $5566, $554A:
                        Result := True;
                end;
            end;

            procedure get_residual_repair_segments_local(
                const seed_candidate: TncCandidate;
                const visible_index: Integer;
                out segment_starts: TArray<Integer>;
                out segment_lengths: TArray<Integer>);
            var
                source_idx_local: Integer;
                inferred_score_local: Integer;
                inferred_segments_local: Integer;
                segment_idx_local: Integer;
                unit_idx_local: Integer;
                cursor_local: Integer;
                segment_units_local: TArray<string>;
                path_parts_local: TArray<string>;
                path_local: string;
                reconstructed_local: string;
            begin
                SetLength(segment_starts, expected_units);
                SetLength(segment_lengths, expected_units);
                for unit_idx_local := 0 to expected_units - 1 do
                begin
                    segment_starts[unit_idx_local] := -1;
                    segment_lengths[unit_idx_local] := 0;
                end;
                source_idx_local := -1;
                if (visible_index >= 0) and
                    (visible_index < Length(visible_source_indices)) then
                begin
                    source_idx_local := visible_source_indices[visible_index];
                end;
                path_local := '';
                if (source_idx_local >= 0) and
                    (source_idx_local < Length(m_candidate_segment_paths)) then
                begin
                    path_local := Trim(
                        m_candidate_segment_paths[source_idx_local]);
                end;
                if (path_local = '') and (source_idx_local >= 0) and
                    (source_idx_local <= High(m_candidates)) then
                begin
                    path_local := Trim(get_segment_path_for_candidate(
                        m_candidates[source_idx_local], source_idx_local));
                end;
                if path_local = '' then
                begin
                    path_local := Trim(get_segment_path_for_candidate(
                        seed_candidate, -1));
                end;

                // A one-piece path can be an unsegmented fallback. Re-infer it
                // before deciding which characters are safe local repairs.
                if get_encoded_path_segment_count_local(path_local) <= 1 then
                begin
                    path_local := Trim(
                        infer_segment_path_for_query_text_with_score(
                        normalized_pinyin, Trim(seed_candidate.text),
                        inferred_score_local, inferred_segments_local, True));
                end;
                if path_local = '' then
                begin
                    Exit;
                end;

                path_parts_local := path_local.Split(
                    [c_segment_path_separator]);
                cursor_local := 0;
                reconstructed_local := '';
                for segment_idx_local := 0 to High(path_parts_local) do
                begin
                    segment_units_local := split_text_units(
                        Trim(path_parts_local[segment_idx_local]));
                    if Length(segment_units_local) <= 0 then
                    begin
                        Exit;
                    end;
                    reconstructed_local := reconstructed_local +
                        Trim(path_parts_local[segment_idx_local]);
                    for unit_idx_local := 0 to
                        High(segment_units_local) do
                    begin
                        if cursor_local + unit_idx_local <
                            Length(segment_starts) then
                        begin
                            segment_starts[cursor_local + unit_idx_local] :=
                                cursor_local;
                            segment_lengths[cursor_local + unit_idx_local] :=
                                Length(segment_units_local);
                        end;
                    end;
                    Inc(cursor_local, Length(segment_units_local));
                end;
                if (cursor_local <> expected_units) or
                    (not SameText(reconstructed_local,
                    Trim(seed_candidate.text))) then
                begin
                    for unit_idx_local := 0 to expected_units - 1 do
                    begin
                        segment_starts[unit_idx_local] := -1;
                        segment_lengths[unit_idx_local] := 0;
                    end;
                end;
            end;

            function find_source_weight_local(
                const options: TncCandidateList;
                const source_text: string): Integer;
            var
                idx_local: Integer;
            begin
                Result := 0;
                for idx_local := 0 to High(options) do
                begin
                    if SameText(Trim(options[idx_local].text), source_text) then
                    begin
                        Exit(candidate_raw_weight_local(options[idx_local]));
                    end;
                end;
            end;

            function model_item_is_better_local(
                const candidate_value: TncLongLocalRerankPoolItem;
                const current_value: TncLongLocalRerankPoolItem): Boolean;
            begin
                if candidate_value.model_score > current_value.model_score then
                begin
                    Exit(True);
                end;
                if candidate_value.model_score < current_value.model_score then
                begin
                    Exit(False);
                end;
                Result := candidate_value.seed_rank < current_value.seed_rank;
            end;

            function join_units_local(const units: TArray<string>;
                const first_index: Integer;
                const last_index: Integer): string;
            var
                idx_local: Integer;
            begin
                Result := '';
                for idx_local := first_index to last_index do
                begin
                    Result := Result + units[idx_local];
                end;
            end;

            procedure prepare_consensus_originals_local;
            var
                original_units_local: TArray<string>;
                original_idx_local: Integer;
                weight_local: Double;
            begin
                consensus_original_count_local := 0;
                consensus_total_weight_local := 0.0;
                SetLength(consensus_original_units_local,
                    Length(pool_local));
                SetLength(consensus_original_weights_local,
                    Length(pool_local));
                SetLength(consensus_top_units_local, 0);
                for original_idx_local := 0 to High(pool_local) do
                begin
                    if not pool_local[original_idx_local].original then
                    begin
                        Continue;
                    end;
                    original_units_local := split_text_units(
                        pool_local[original_idx_local].text);
                    if Length(original_units_local) <> expected_units then
                    begin
                        Continue;
                    end;
                    consensus_original_units_local[
                        consensus_original_count_local] :=
                        original_units_local;
                    weight_local := 1.0 / Max(1,
                        pool_local[original_idx_local].seed_rank);
                    consensus_original_weights_local[
                        consensus_original_count_local] := weight_local;
                    consensus_total_weight_local :=
                        consensus_total_weight_local + weight_local;
                    if pool_local[original_idx_local].seed_rank = 1 then
                    begin
                        consensus_top_units_local :=
                            Copy(original_units_local, 0,
                            Length(original_units_local));
                    end;
                    Inc(consensus_original_count_local);
                end;
                SetLength(consensus_original_units_local,
                    consensus_original_count_local);
                SetLength(consensus_original_weights_local,
                    consensus_original_count_local);
            end;

            procedure populate_consensus_features_local(
                var value: TncLongLocalRerankPoolItem);
            var
                candidate_units_local: TArray<string>;
                distances_local: TArray<Integer>;
                compatible_idx_local: Integer;
                position_local: Integer;
                support_local: Double;
                distance_sum_local: Integer;
            begin
                candidate_units_local := split_text_units(value.text);
                if Length(candidate_units_local) <> expected_units then
                begin
                    Exit;
                end;
                if (consensus_original_count_local <= 0) or
                    (consensus_total_weight_local <= 0.0) then
                begin
                    value.consensus_nearest_distance := expected_units;
                    value.consensus_mean_distance := expected_units;
                    Exit;
                end;

                SetLength(distances_local,
                    consensus_original_count_local);
                value.consensus_seed_count :=
                    consensus_original_count_local;
                value.consensus_support_min := 1.0;
                value.consensus_nearest_distance := expected_units;
                for position_local := 0 to expected_units - 1 do
                begin
                    support_local := 0.0;
                    for compatible_idx_local := 0 to
                        consensus_original_count_local - 1 do
                    begin
                        if SameText(consensus_original_units_local[
                            compatible_idx_local][position_local],
                            candidate_units_local[position_local]) then
                        begin
                            support_local := support_local +
                                consensus_original_weights_local[
                                compatible_idx_local];
                        end
                        else
                        begin
                            Inc(distances_local[compatible_idx_local]);
                        end;
                    end;
                    support_local := support_local /
                        consensus_total_weight_local;
                    value.consensus_support_mean :=
                        value.consensus_support_mean + support_local;
                    value.consensus_support_min := Min(
                        value.consensus_support_min, support_local);
                    if support_local >= 0.5 then
                    begin
                        Inc(value.consensus_majority_units);
                    end;
                    if support_local >= 0.999999 then
                    begin
                        Inc(value.consensus_unanimous_units);
                    end;
                    if position_local = value.changed_position then
                    begin
                        value.consensus_changed_support := support_local;
                    end;
                end;
                value.consensus_support_mean :=
                    value.consensus_support_mean / expected_units;
                distance_sum_local := 0;
                for compatible_idx_local := 0 to
                    consensus_original_count_local - 1 do
                begin
                    value.consensus_nearest_distance := Min(
                        value.consensus_nearest_distance,
                        distances_local[compatible_idx_local]);
                    Inc(distance_sum_local,
                        distances_local[compatible_idx_local]);
                end;
                value.consensus_mean_distance :=
                    distance_sum_local /
                    consensus_original_count_local;
                if (value.changed_position >= 0) and
                    (value.changed_position <
                    Length(consensus_top_units_local)) and
                    SameText(consensus_top_units_local[
                    value.changed_position],
                    candidate_units_local[value.changed_position]) then
                begin
                    value.consensus_changed_top_match := 1;
                end;
            end;

            procedure populate_pair_candidate_features_local(
                const value: TncLongLocalRerankPoolItem;
                out features: TncLongLocalPoolCandidateFeatures);
            begin
                features := Default(TncLongLocalPoolCandidateFeatures);
                features.pool_rank := value.pool_rank;
                features.char_lm_score := value.char_lm_score;
                features.seed_rank := value.seed_rank;
                features.original := Ord(value.original);
                features.substitutions := value.substitutions;
                if value.original then
                begin
                    features.changed_position_ratio := -1.0;
                end
                else
                begin
                    features.changed_position_ratio :=
                        value.changed_position / Max(1, expected_units - 1);
                end;
                features.source_char_weight := value.source_char_weight;
                features.replacement_char_weight :=
                    value.replacement_char_weight;
                features.char_weight_delta :=
                    value.replacement_char_weight - value.source_char_weight;
                features.consensus_seed_count := value.consensus_seed_count;
                features.consensus_support_mean :=
                    value.consensus_support_mean;
                features.consensus_support_min := value.consensus_support_min;
                features.consensus_majority_units :=
                    value.consensus_majority_units;
                features.consensus_unanimous_units :=
                    value.consensus_unanimous_units;
                features.consensus_nearest_distance :=
                    value.consensus_nearest_distance;
                features.consensus_mean_distance :=
                    value.consensus_mean_distance;
                features.consensus_changed_support :=
                    value.consensus_changed_support;
                features.consensus_changed_top_match :=
                    value.consensus_changed_top_match;
            end;

            function score_pairwise_pool_local: Boolean;
            var
                challenger_indices_local: TArray<Integer>;
                window_offsets_local: TArray<Integer>;
                window_texts_local: TArray<string>;
                window_scores_local: TArray<Integer>;
                baseline_units_local: TArray<string>;
                candidate_units_local: TArray<string>;
                baseline_features_local: TncLongLocalPoolCandidateFeatures;
                candidate_features_local: TncLongLocalPoolCandidateFeatures;
                relation_features_local: TncLongLocalPoolRelationFeatures;
                top_lm_local: TncLongLocalPoolLmScores;
                candidate_lm_local: TncLongLocalPoolLmScores;
                model_features_local: TncLongLocalPairwisePoolFeatures;
                challenger_local: Integer;
                candidate_idx_local: Integer;
                radius_local: Integer;
                window_start_local: Integer;
                window_end_local: Integer;
                window_count_local: Integer;
            begin
                Result := False;
                pairwise_challenger_count_local := 0;
                pairwise_insert_rank_local := -1;
                SetLength(challenger_indices_local,
                    c_long_local_pairwise_pool_max_challengers);
                for candidate_idx_local := 0 to High(retained_local) do
                begin
                    if retained_local[candidate_idx_local].original then
                    begin
                        Continue;
                    end;
                    if pairwise_challenger_count_local >=
                        c_long_local_pairwise_pool_max_challengers then
                    begin
                        Continue;
                    end;
                    challenger_indices_local[pairwise_challenger_count_local] :=
                        candidate_idx_local;
                    Inc(pairwise_challenger_count_local);
                end;
                if (baseline_idx_local < 0) or
                    (pairwise_challenger_count_local <= 0) then
                begin
                    Exit;
                end;
                SetLength(challenger_indices_local,
                    pairwise_challenger_count_local);
                prepare_consensus_originals_local;
                populate_consensus_features_local(
                    retained_local[baseline_idx_local]);
                for challenger_local := 0 to
                    pairwise_challenger_count_local - 1 do
                begin
                    populate_consensus_features_local(retained_local[
                        challenger_indices_local[challenger_local]]);
                end;
                baseline_units_local := split_text_units(
                    retained_local[baseline_idx_local].text);
                if Length(baseline_units_local) <> expected_units then
                begin
                    Exit;
                end;

                SetLength(window_offsets_local,
                    pairwise_challenger_count_local);
                SetLength(window_texts_local,
                    pairwise_challenger_count_local * 8);
                window_count_local := 0;
                for challenger_local := 0 to
                    pairwise_challenger_count_local - 1 do
                begin
                    candidate_idx_local :=
                        challenger_indices_local[challenger_local];
                    candidate_units_local := split_text_units(
                        retained_local[candidate_idx_local].text);
                    if (Length(candidate_units_local) <> expected_units) or
                        (retained_local[candidate_idx_local].changed_position < 0) or
                        (retained_local[candidate_idx_local].changed_position >=
                        expected_units) then
                    begin
                        Exit;
                    end;
                    window_offsets_local[challenger_local] :=
                        window_count_local;
                    for radius_local := 0 to 3 do
                    begin
                        window_start_local := Max(0,
                            retained_local[candidate_idx_local].changed_position -
                            radius_local);
                        window_end_local := Min(expected_units - 1,
                            retained_local[candidate_idx_local].changed_position +
                            radius_local);
                        window_texts_local[window_count_local] :=
                            join_units_local(baseline_units_local,
                            window_start_local, window_end_local);
                        Inc(window_count_local);
                        window_texts_local[window_count_local] :=
                            join_units_local(candidate_units_local,
                            window_start_local, window_end_local);
                        Inc(window_count_local);
                    end;
                end;
                SetLength(window_texts_local, window_count_local);
                if (window_count_local <= 0) or
                    (not get_cached_char_lm_scores(window_texts_local,
                    window_scores_local, clsm_suffix, '')) or
                    (Length(window_scores_local) <> window_count_local) then
                begin
                    Exit;
                end;

                populate_pair_candidate_features_local(
                    retained_local[baseline_idx_local],
                    baseline_features_local);
                best_idx_local := -1;
                pairwise_score_local := -MaxDouble;
                for challenger_local := 0 to
                    pairwise_challenger_count_local - 1 do
                begin
                    candidate_idx_local :=
                        challenger_indices_local[challenger_local];
                    populate_pair_candidate_features_local(
                        retained_local[candidate_idx_local],
                        candidate_features_local);
                    relation_features_local :=
                        Default(TncLongLocalPoolRelationFeatures);
                    relation_features_local.different_units := 1.0;
                    relation_features_local.different_runs := 1.0;
                    relation_features_local.max_different_run := 1.0;
                    relation_features_local.same_prefix_units :=
                        retained_local[candidate_idx_local].changed_position;
                    relation_features_local.same_suffix_units :=
                        expected_units -
                        retained_local[candidate_idx_local].changed_position - 1;
                    relation_features_local.difference_span_units := 1.0;
                    for radius_local := 0 to 3 do
                    begin
                        top_lm_local[radius_local] := window_scores_local[
                            window_offsets_local[challenger_local] +
                            radius_local * 2];
                        candidate_lm_local[radius_local] := window_scores_local[
                            window_offsets_local[challenger_local] +
                            radius_local * 2 + 1];
                    end;
                    build_long_local_pairwise_pool_features(
                        candidate_features_local, baseline_features_local,
                        relation_features_local, top_lm_local,
                        candidate_lm_local, model_features_local);
                    retained_local[candidate_idx_local].model_score :=
                        long_local_pairwise_pool_score(model_features_local);
                    if (best_idx_local < 0) or
                        (retained_local[candidate_idx_local].model_score >
                        pairwise_score_local) then
                    begin
                        best_idx_local := candidate_idx_local;
                        pairwise_score_local :=
                            retained_local[candidate_idx_local].model_score;
                    end;
                end;
                if best_idx_local < 0 then
                begin
                    Exit;
                end;
                if pairwise_score_local >=
                    c_long_local_pairwise_pool_top1_threshold then
                begin
                    pairwise_insert_rank_local := 0;
                end
                else if pairwise_score_local >=
                    c_long_local_pairwise_pool_top2_threshold then
                begin
                    pairwise_insert_rank_local := 1;
                end
                else
                begin
                    Exit;
                end;
                Result := True;
            end;
        begin
            if use_pairwise_pool_model or (not use_residual_model) then
            begin
                m_long_local_rerank_generated_text := '';
                m_long_local_rerank_composition_text := '';
                m_long_local_rerank_lookup_key := '';
            end;
            // Do not cascade two independently generated substitutions.  The
            // residual pass may correct untouched engine output, but a first-
            // stage generated sentence is already the end of the repair path.
            if use_residual_model and (not use_pairwise_pool_model) and
                (m_long_local_rerank_generated_text <> '') then
            begin
                Exit;
            end;
            if (m_dictionary = nil) or short_exact_query_mode or
                (m_page_index <> 0) or (m_confirmed_text <> '') or
                (expected_units < 6) or (Length(Result) < 2) or
                forced_visible_top_candidate_applies or
                (Length(syllables) <> expected_units) then
            begin
                Exit;
            end;

            top_text_local := Trim(Result[0].text);
            if (top_text_local = '') or (Trim(Result[0].comment) <> '') or
                (get_candidate_text_unit_count(top_text_local) <> expected_units) or
                (Result[0].source = cs_user) or
                is_latest_session_query_choice(top_text_local) or
                display_candidate_is_persistent_latest_choice(
                normalized_pinyin, top_text_local) or
                m_dictionary.is_user_entry(normalized_pinyin,
                top_text_local) or
                (Result[0].has_dict_weight and
                m_dictionary.is_base_entry(normalized_pinyin,
                top_text_local)) then
            begin
                Exit;
            end;

            SetLength(pool_local, 0);
            single_option_limit_local := c_single_option_limit;
            if use_pairwise_pool_model then
            begin
                single_option_limit_local := c_pairwise_single_option_limit;
            end;
            model_tick_local := nc_monotonic_tick_ms;
            pool_index_by_text_local := TDictionary<string, Integer>.Create;
            retained_texts_local := TDictionary<string, Boolean>.Create;
            try
                original_count_local := 0;
                for visible_idx_local := 0 to Min(c_seed_probe_limit - 1,
                    High(Result)) do
                begin
                    if (Trim(Result[visible_idx_local].comment) <> '') or
                        (Trim(Result[visible_idx_local].text) = '') or
                        (Result[visible_idx_local].source = cs_user) or
                        (get_candidate_text_unit_count(
                        Trim(Result[visible_idx_local].text)) <> expected_units) then
                    begin
                        Continue;
                    end;
                    seed_units_local := split_text_units(
                        Trim(Result[visible_idx_local].text));
                    if Length(seed_units_local) <> expected_units then
                    begin
                        Continue;
                    end;
                    initialize_pool_item_local(item_local);
                    item_local.text := Trim(Result[visible_idx_local].text);
                    item_local.seed_rank := visible_idx_local + 1;
                    item_local.seed_visible_index := visible_idx_local;
                    item_local.original := True;
                    item_local.changed_position := -1;
                    retain_pool_item_local(item_local);
                    Inc(original_count_local);

                    if visible_idx_local >= c_generated_seed_limit then
                    begin
                        Continue;
                    end;

                    if use_residual_model then
                    begin
                        get_residual_repair_segments_local(
                            Result[visible_idx_local], visible_idx_local,
                            repair_segment_starts_local,
                            repair_segment_lengths_local);
                    end;

                    for unit_idx_local := 0 to expected_units - 1 do
                    begin
                        if use_residual_model and
                            ((unit_idx_local >=
                            Length(repair_segment_starts_local)) or
                            (unit_idx_local >=
                            Length(repair_segment_lengths_local)) or
                            (repair_segment_starts_local[unit_idx_local] < 0) or
                            (repair_segment_lengths_local[unit_idx_local] <= 0)) then
                        begin
                            Continue;
                        end;
                        syllable_key_local := normalize_pinyin_text(
                            syllables[unit_idx_local].text);
                        if not get_single_options_local(syllable_key_local,
                            single_options_local) then
                        begin
                            Continue;
                        end;
                        source_weight_local := find_source_weight_local(
                            single_options_local, seed_units_local[unit_idx_local]);
                        for replacement_idx_local := 0 to
                            Min(single_option_limit_local - 1,
                            High(single_options_local)) do
                        begin
                            replacement_text_local := Trim(
                                single_options_local[replacement_idx_local].text);
                            if (replacement_text_local = '') or
                                SameText(replacement_text_local,
                                seed_units_local[unit_idx_local]) then
                            begin
                                Continue;
                            end;
                            if use_residual_model then
                            begin
                                repair_segment_start_local :=
                                    repair_segment_starts_local[unit_idx_local];
                                repair_segment_length_local :=
                                    repair_segment_lengths_local[unit_idx_local];
                                if (repair_segment_length_local > 1) and
                                    (not is_productive_particle_tail_segment_local(
                                    seed_units_local,
                                    repair_segment_start_local,
                                    repair_segment_length_local,
                                    unit_idx_local)) then
                                begin
                                    repaired_segment_text_local := '';
                                    repaired_segment_key_local := '';
                                    for repair_segment_unit_local :=
                                        repair_segment_start_local to
                                        repair_segment_start_local +
                                        repair_segment_length_local - 1 do
                                    begin
                                        if repair_segment_unit_local =
                                            unit_idx_local then
                                        begin
                                            repaired_segment_text_local :=
                                                repaired_segment_text_local +
                                                replacement_text_local;
                                        end
                                        else
                                        begin
                                            repaired_segment_text_local :=
                                                repaired_segment_text_local +
                                                seed_units_local[
                                                repair_segment_unit_local];
                                        end;
                                        repaired_segment_key_local :=
                                            repaired_segment_key_local +
                                            normalize_pinyin_text(syllables[
                                            repair_segment_unit_local].text);
                                    end;
                                    if (repaired_segment_key_local = '') or
                                        (repaired_segment_text_local = '') or
                                        (not m_dictionary.is_base_entry(
                                        repaired_segment_key_local,
                                        repaired_segment_text_local)) then
                                    begin
                                        Continue;
                                    end;
                                end;
                            end;
                            variant_text_local := build_variant_text_local(
                                seed_units_local, unit_idx_local,
                                replacement_text_local);
                            initialize_pool_item_local(item_local);
                            item_local.text := variant_text_local;
                            item_local.seed_rank := visible_idx_local + 1;
                            item_local.seed_visible_index := visible_idx_local;
                            item_local.original := False;
                            item_local.substitutions := 1;
                            item_local.changed_position := unit_idx_local;
                            item_local.source_char_weight := source_weight_local;
                            item_local.replacement_char_weight :=
                                candidate_raw_weight_local(
                                single_options_local[replacement_idx_local]);
                            retain_pool_item_local(item_local);
                        end;
                    end;
                end;
                if (original_count_local <= 0) or (Length(pool_local) <= 1) then
                begin
                    Exit;
                end;

                SetLength(candidate_texts_local, Length(pool_local));
                for pool_idx_local := 0 to High(pool_local) do
                begin
                    candidate_texts_local[pool_idx_local] :=
                        pool_local[pool_idx_local].text;
                end;
                if not get_cached_char_lm_scores(candidate_texts_local,
                    lm_scores_local, clsm_full, '') or
                    (Length(lm_scores_local) <> Length(pool_local)) then
                begin
                    Exit;
                end;
                SetLength(order_local, Length(pool_local));
                for pool_idx_local := 0 to High(pool_local) do
                begin
                    pool_local[pool_idx_local].char_lm_score :=
                        lm_scores_local[pool_idx_local];
                    order_local[pool_idx_local] := pool_idx_local;
                end;
                sort_pool_order_local;

                retained_count_local := Min(c_hidden_pool_limit,
                    Length(order_local));
                SetLength(retained_local, retained_count_local);
                for order_idx_local := 0 to retained_count_local - 1 do
                begin
                    retained_local[order_idx_local] :=
                        pool_local[order_local[order_idx_local]];
                    retained_texts_local.AddOrSetValue(LowerCase(
                        Trim(retained_local[order_idx_local].text)), True);
                end;
                for pool_idx_local := 0 to High(pool_local) do
                begin
                    if (not pool_local[pool_idx_local].original) or
                        retained_texts_local.ContainsKey(LowerCase(
                        Trim(pool_local[pool_idx_local].text))) then
                    begin
                        Continue;
                    end;
                    retained_idx_local := Length(retained_local);
                    SetLength(retained_local, retained_idx_local + 1);
                    retained_local[retained_idx_local] := pool_local[pool_idx_local];
                    retained_texts_local.AddOrSetValue(LowerCase(
                        Trim(pool_local[pool_idx_local].text)), True);
                end;

                baseline_idx_local := -1;
                best_idx_local := -1;
                for retained_idx_local := 0 to High(retained_local) do
                begin
                    retained_local[retained_idx_local].pool_rank :=
                        retained_idx_local + 1;
                    if retained_local[retained_idx_local].original then
                    begin
                        changed_ratio_local := -1.0;
                    end
                    else
                    begin
                        changed_ratio_local :=
                            retained_local[retained_idx_local].changed_position /
                            Max(1, expected_units - 1);
                    end;
                    if use_pairwise_pool_model then
                    begin
                        retained_local[retained_idx_local].model_score := 0.0;
                    end
                    else if use_residual_model then
                    begin
                        retained_local[retained_idx_local].model_score :=
                            long_local_residual_score(
                            retained_local[retained_idx_local].pool_rank,
                            retained_local[retained_idx_local].char_lm_score,
                            retained_local[retained_idx_local].seed_rank,
                            retained_local[retained_idx_local].original,
                            retained_local[retained_idx_local].substitutions,
                            changed_ratio_local,
                            retained_local[retained_idx_local].source_char_weight,
                            retained_local[retained_idx_local].replacement_char_weight);
                    end
                    else
                    begin
                        retained_local[retained_idx_local].model_score :=
                            long_local_reranker_score(
                            retained_local[retained_idx_local].pool_rank,
                            retained_local[retained_idx_local].char_lm_score,
                            retained_local[retained_idx_local].seed_rank,
                            retained_local[retained_idx_local].original,
                            retained_local[retained_idx_local].substitutions,
                            changed_ratio_local,
                            retained_local[retained_idx_local].source_char_weight,
                            retained_local[retained_idx_local].replacement_char_weight);
                    end;
                    if retained_local[retained_idx_local].original and
                        (retained_local[retained_idx_local].seed_rank = 1) then
                    begin
                        baseline_idx_local := retained_idx_local;
                    end;
                    // The residual model is trained only against generated
                    // one-character repairs. Existing complete candidates
                    // retain their engine order and are not residual rivals.
                    if (not use_pairwise_pool_model) and
                        ((not use_residual_model) or
                        (not retained_local[retained_idx_local].original) or
                        (retained_idx_local = baseline_idx_local)) and
                        ((best_idx_local < 0) or model_item_is_better_local(
                        retained_local[retained_idx_local],
                        retained_local[best_idx_local])) then
                    begin
                        best_idx_local := retained_idx_local;
                    end;
                end;
                if use_pairwise_pool_model then
                begin
                    if not score_pairwise_pool_local then
                    begin
                        Exit;
                    end;
                end
                else if (baseline_idx_local < 0) or (best_idx_local < 0) or
                    (best_idx_local = baseline_idx_local) then
                begin
                    Exit;
                end;
                if use_pairwise_pool_model then
                begin
                    // The independently calibrated pairwise model already
                    // selected whether this repair belongs at rank 1 or 2.
                end
                else if use_residual_model then
                begin
                    if retained_local[best_idx_local].model_score <
                        retained_local[baseline_idx_local].model_score +
                        c_long_local_residual_promotion_margin then
                    begin
                        Exit;
                    end;
                end
                else if retained_local[best_idx_local].model_score <
                    retained_local[baseline_idx_local].model_score +
                    c_long_local_reranker_promotion_margin then
                begin
                    Exit;
                end;

                best_item_local := retained_local[best_idx_local];
                if best_item_local.original then
                begin
                    visible_idx_local := best_item_local.seed_visible_index;
                    if (visible_idx_local <= 0) or
                        (visible_idx_local >= Length(Result)) then
                    begin
                        Exit;
                    end;
                    moved_candidate_local := Result[visible_idx_local];
                    moved_source_local := visible_source_indices[visible_idx_local];
                    for move_idx_local := visible_idx_local downto 2 do
                    begin
                        Result[move_idx_local] := Result[move_idx_local - 1];
                        visible_source_indices[move_idx_local] :=
                            visible_source_indices[move_idx_local - 1];
                    end;
                    Result[1] := Result[0];
                    visible_source_indices[1] := visible_source_indices[0];
                    Result[0] := moved_candidate_local;
                    visible_source_indices[0] := moved_source_local;
                    if use_residual_model and
                        (m_long_local_rerank_generated_text <> '') and
                        (not SameText(m_long_local_rerank_generated_text,
                        Trim(moved_candidate_local.text))) then
                    begin
                        m_long_local_rerank_generated_text := '';
                        m_long_local_rerank_composition_text := '';
                        m_long_local_rerank_lookup_key := '';
                    end;
                    Exit;
                end;

                generated_candidate_local :=
                    Result[best_item_local.seed_visible_index];
                generated_candidate_local.text := best_item_local.text;
                generated_candidate_local.comment := '';
                generated_candidate_local.source := cs_rule;
                generated_candidate_local.has_dict_weight := False;
                generated_candidate_local.dict_weight := 0;
                if use_pairwise_pool_model and
                    (pairwise_insert_rank_local = 1) then
                begin
                    for move_idx_local := High(Result) downto 2 do
                    begin
                        Result[move_idx_local] := Result[move_idx_local - 1];
                        visible_source_indices[move_idx_local] :=
                            visible_source_indices[move_idx_local - 1];
                    end;
                    Result[1] := generated_candidate_local;
                    visible_source_indices[1] := -1;
                end
                else
                begin
                    for move_idx_local := High(Result) downto 2 do
                    begin
                        Result[move_idx_local] := Result[move_idx_local - 1];
                        visible_source_indices[move_idx_local] :=
                            visible_source_indices[move_idx_local - 1];
                    end;
                    Result[1] := Result[0];
                    visible_source_indices[1] := visible_source_indices[0];
                    Result[0] := generated_candidate_local;
                    visible_source_indices[0] := -1;
                end;
                m_long_local_rerank_generated_text :=
                    Trim(generated_candidate_local.text);
                m_long_local_rerank_composition_text := m_composition_text;
                m_long_local_rerank_lookup_key := m_last_lookup_key;
            finally
                if m_config.debug_mode then
                begin
                    model_elapsed_local := nc_monotonic_tick_ms - model_tick_local;
                    if m_last_lookup_debug_extra <> '' then
                    begin
                        m_last_lookup_debug_extra :=
                            m_last_lookup_debug_extra + ' ';
                    end;
                    if use_pairwise_pool_model then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('localpair=%d', [model_elapsed_local]);
                    end
                    else if use_residual_model then
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('localres=%d', [model_elapsed_local]);
                    end
                    else
                    begin
                        m_last_lookup_debug_extra := m_last_lookup_debug_extra +
                            Format('localrr=%d', [model_elapsed_local]);
                    end;
                end;
                retained_texts_local.Free;
                pool_index_by_text_local.Free;
            end;
        end;

        procedure build_visible_long_sentence_candidate_pool_local;
        const
            c_short_exact_prefix_max_units = 6;
            c_short_compound_prefix_max_units = 5;
        var
            pool_candidates_local: TncCandidateList;
            pool_source_indices_local: TArray<Integer>;
            seen_candidates_local: TDictionary<string, Boolean>;
            long_candidate_count_local: Integer;

            function get_partial_prefix_units_local(
                const candidate_value_local: TncCandidate;
                out prefix_units_local: Integer): Boolean;
            var
                tail_syllables_local: TncPinyinParseResult;
                text_units_local: Integer;
            begin
                Result := False;
                prefix_units_local := umlaut_raw_prefix_units(candidate_value_local);
                if prefix_units_local > 0 then Exit(True);
                prefix_units_local := 0;
                if Trim(candidate_value_local.comment) = '' then
                begin
                    Exit;
                end;

                tail_syllables_local := get_effective_compact_pinyin_syllables(
                    normalize_pinyin_text(candidate_value_local.comment));
                if Length(tail_syllables_local) <= 0 then
                begin
                    Exit;
                end;
                prefix_units_local := expected_units -
                    Length(tail_syllables_local);
                text_units_local := get_candidate_text_unit_count(
                    Trim(candidate_value_local.text));
                Result := (prefix_units_local > 0) and
                    (prefix_units_local < expected_units) and
                    (text_units_local = prefix_units_local);
            end;

            function candidate_is_complete_long_local(
                const candidate_value_local: TncCandidate): Boolean;
            var
                text_local: string;
            begin
                text_local := Trim(candidate_value_local.text);
                Result := (text_local <> '') and
                    (Trim(candidate_value_local.comment) = '');
            end;

            function candidate_has_exact_compound_path_local(
                const candidate_value_local: TncCandidate;
                const source_idx_local: Integer;
                const prefix_units_local: Integer): Boolean;
            var
                path_local: string;
                path_parts_local: TArray<string>;
                part_text_local: string;
                part_key_local: string;
                combined_text_local: string;
                part_units_local: Integer;
                consumed_units_local: Integer;
                part_idx_local: Integer;
                source_is_compound_local: Boolean;
            begin
                Result := False;
                source_is_compound_local :=
                    candidate_value_local.display_kind = cdk_lm_compound;
                if (source_idx_local >= 0) and
                    (source_idx_local < Length(m_candidates)) then
                begin
                    source_is_compound_local := source_is_compound_local or
                        (m_candidates[source_idx_local].display_kind =
                        cdk_lm_compound);
                end;
                if (not source_is_compound_local) or
                    (prefix_units_local > c_short_compound_prefix_max_units)
                then
                begin
                    Exit;
                end;

                path_local := get_segment_path_for_candidate(
                    candidate_value_local, source_idx_local);
                path_parts_local := path_local.Split(
                    [c_segment_path_separator]);
                if Length(path_parts_local) <> 2 then
                begin
                    Exit;
                end;

                consumed_units_local := 0;
                combined_text_local := '';
                for part_idx_local := 0 to High(path_parts_local) do
                begin
                    part_text_local := Trim(path_parts_local[part_idx_local]);
                    part_units_local := get_candidate_text_unit_count(
                        part_text_local);
                    if (part_text_local = '') or (part_units_local <= 0) or
                        (consumed_units_local + part_units_local >
                        prefix_units_local) then
                    begin
                        Exit;
                    end;
                    part_key_local := build_display_query_key(
                        consumed_units_local, part_units_local);
                    if (part_key_local = '') or
                        (not (display_exact_key_has_text(part_key_local,
                        part_text_local) or
                        ((m_dictionary <> nil) and
                        (m_dictionary.is_base_entry(part_key_local,
                        part_text_local) or
                        m_dictionary.is_user_entry(part_key_local,
                        part_text_local))))) then
                    begin
                        Exit;
                    end;
                    combined_text_local := combined_text_local +
                        part_text_local;
                    Inc(consumed_units_local, part_units_local);
                end;

                Result := (consumed_units_local = prefix_units_local) and
                    SameText(combined_text_local,
                    Trim(candidate_value_local.text));
            end;

            function candidate_is_short_prefix_local(
                const candidate_value_local: TncCandidate;
                const source_idx_local: Integer;
                out prefix_units_local: Integer;
                out highlight_compound_local: Boolean): Boolean;
            var
                prefix_key_local: string;
                prefix_text_local: string;
            begin
                Result := False;
                highlight_compound_local := False;
                if not get_partial_prefix_units_local(candidate_value_local,
                    prefix_units_local) then
                begin
                    Exit;
                end;

                if (prefix_units_local <= c_short_exact_prefix_max_units) and
                    ((umlaut_raw_prefix_units(candidate_value_local) > 0) or
                    (alternative_exact_prefix_units(candidate_value_local) > 0)) then Exit(True);

                prefix_key_local := build_display_query_key(0,
                    prefix_units_local);
                prefix_text_local := Trim(candidate_value_local.text);
                if (prefix_key_local = '') or (prefix_text_local = '') then
                begin
                    Exit;
                end;

                if prefix_units_local = 1 then
                begin
                    Exit((m_dictionary <> nil) and
                        (m_dictionary.single_char_matches_pinyin(
                        prefix_key_local, prefix_text_local) or
                        display_exact_key_has_text(prefix_key_local,
                        prefix_text_local)));
                end;

                if (prefix_units_local <=
                    c_short_exact_prefix_max_units) and
                    (display_exact_key_has_text(prefix_key_local,
                    prefix_text_local) or
                    ((m_dictionary <> nil) and
                    (m_dictionary.is_base_entry(prefix_key_local,
                    prefix_text_local) or
                    m_dictionary.is_user_entry(prefix_key_local,
                    prefix_text_local))) or
                    ((candidate_value_local.fuzzy_cost > 0) and
                    candidate_value_local.has_dict_weight)) then
                begin
                    Exit(True);
                end;

                highlight_compound_local :=
                    candidate_has_exact_compound_path_local(
                    candidate_value_local, source_idx_local,
                    prefix_units_local);
                Result := highlight_compound_local;
            end;

            function candidate_is_long_partial_local(
                const candidate_value_local: TncCandidate;
                const source_idx_local: Integer): Boolean;
            var
                prefix_units_local: Integer;
                highlight_compound_local: Boolean;
            begin
                Result := False;
                if not get_partial_prefix_units_local(candidate_value_local,
                    prefix_units_local) then
                begin
                    Exit;
                end;
                if candidate_is_short_prefix_local(candidate_value_local,
                    source_idx_local, prefix_units_local,
                    highlight_compound_local) then
                begin
                    Exit;
                end;
                Result := prefix_units_local >= 3;
            end;

            procedure append_pool_candidate_local(
                const candidate_value_local: TncCandidate;
                const source_idx_local: Integer;
                const is_long_candidate_local: Boolean;
                const highlight_compound_local: Boolean);
            var
                normalized_candidate_local: TncCandidate;
                candidate_key_local: string;
                out_idx_local: Integer;
            begin
                normalized_candidate_local := candidate_value_local;
                normalize_display_candidate(normalized_candidate_local);
                if is_long_candidate_local then
                begin
                    // The accent color denotes an explicit short transition
                    // compound, not a sentence that happened to use LM data.
                    normalized_candidate_local.display_kind := cdk_default;
                end
                else if highlight_compound_local then
                begin
                    normalized_candidate_local.display_kind :=
                        cdk_lm_compound;
                end;

                candidate_key_local := nc_visible_candidate_key(normalized_candidate_local,
                    normalize_pinyin_text(
                    Trim(normalized_candidate_local.comment)));
                if (Trim(normalized_candidate_local.text) = '') or
                    seen_candidates_local.ContainsKey(candidate_key_local) then
                begin
                    Exit;
                end;

                out_idx_local := Length(pool_candidates_local);
                SetLength(pool_candidates_local, out_idx_local + 1);
                SetLength(pool_source_indices_local, out_idx_local + 1);
                pool_candidates_local[out_idx_local] :=
                    normalized_candidate_local;
                pool_source_indices_local[out_idx_local] := source_idx_local;
                seen_candidates_local.Add(candidate_key_local, True);
            end;

            procedure append_complete_candidates_from_result_local;
            var
                idx_local: Integer;
            begin
                for idx_local := 0 to High(Result) do
                begin
                    if long_candidate_count_local >=
                        c_long_sentence_complete_candidate_display_limit then
                    begin
                        Exit;
                    end;
                    if not candidate_is_complete_long_local(Result[idx_local])
                    then
                    begin
                        Continue;
                    end;
                    append_pool_candidate_local(Result[idx_local],
                        visible_source_indices[idx_local], True, False);
                    long_candidate_count_local :=
                        Length(pool_candidates_local);
                end;
            end;

            procedure append_long_partials_from_list_local(
                const candidates_local: TncCandidateList;
                const source_indices_local: TArray<Integer>;
                const use_identity_source_local: Boolean);
            var
                idx_local: Integer;
                source_idx_local: Integer;
                before_count_local: Integer;
            begin
                for idx_local := 0 to High(candidates_local) do
                begin
                    if long_candidate_count_local >=
                        c_long_sentence_complete_candidate_display_limit then
                    begin
                        Exit;
                    end;
                    if use_identity_source_local then
                    begin
                        source_idx_local := idx_local;
                    end
                    else if idx_local < Length(source_indices_local) then
                    begin
                        source_idx_local := source_indices_local[idx_local];
                    end
                    else
                    begin
                        source_idx_local := -1;
                    end;
                    if not candidate_is_long_partial_local(
                        candidates_local[idx_local], source_idx_local) then
                    begin
                        Continue;
                    end;
                    before_count_local := Length(pool_candidates_local);
                    append_pool_candidate_local(candidates_local[idx_local],
                        source_idx_local, True, False);
                    if Length(pool_candidates_local) > before_count_local then
                    begin
                        Inc(long_candidate_count_local);
                    end;
                end;
            end;

            procedure append_short_prefixes_from_list_local(
                const candidates_local: TncCandidateList;
                const source_indices_local: TArray<Integer>;
                const use_identity_source_local: Boolean;
                const append_single_local: Boolean);
            var
                idx_local: Integer;
                source_idx_local: Integer;
                prefix_units_local: Integer;
                highlight_compound_local: Boolean;
            begin
                for idx_local := 0 to High(candidates_local) do
                begin
                    if use_identity_source_local then
                    begin
                        source_idx_local := idx_local;
                    end
                    else if idx_local < Length(source_indices_local) then
                    begin
                        source_idx_local := source_indices_local[idx_local];
                    end
                    else
                    begin
                        source_idx_local := -1;
                    end;
                    if not candidate_is_short_prefix_local(
                        candidates_local[idx_local], source_idx_local,
                        prefix_units_local, highlight_compound_local) then
                    begin
                        Continue;
                    end;
                    if (prefix_units_local = 1) <> append_single_local then
                    begin
                        Continue;
                    end;
                    append_pool_candidate_local(candidates_local[idx_local],
                        source_idx_local, False, highlight_compound_local);
                end;
            end;

            procedure append_all_first_syllable_singles_local;
            var
                first_key_local: string;
                tail_key_local: string;
                exact_singles_local: TncCandidateList;
                single_candidate_local: TncCandidate;
                idx_local: Integer;
            begin
                first_key_local := build_display_query_key(0, 1);
                tail_key_local := build_display_query_key(1,
                    expected_units - 1);
                if (first_key_local = '') or (tail_key_local = '') or
                    (not lookup_display_exact_cached(first_key_local,
                    exact_singles_local)) then
                begin
                    Exit;
                end;

                for idx_local := 0 to High(exact_singles_local) do
                begin
                    single_candidate_local := exact_singles_local[idx_local];
                    if (Trim(single_candidate_local.comment) <> '') or
                        (get_candidate_text_unit_count(
                        Trim(single_candidate_local.text)) <> 1) then
                    begin
                        Continue;
                    end;
                    single_candidate_local.comment := tail_key_local;
                    single_candidate_local.has_dict_weight := False;
                    single_candidate_local.dict_weight := 0;
                    single_candidate_local.display_kind := cdk_default;
                    append_pool_candidate_local(single_candidate_local, -1,
                        False, False);
                end;
            end;

            procedure append_nasal_boundary_singles_local;
            var
                first_key, head_key, tail_key: string;
                tail_parts: TncPinyinParseResult;
                singles: TncCandidateList;
                item, reserved: TncCandidate;
                idx, before_count, first_added, insert_at: Integer;
            begin
                if is_shuangpin_input or (Length(syllables) < 2) or
                    (Pos('''', m_composition_text) > 0) then Exit;
                first_key := normalize_pinyin_text(syllables[0].text);
                if (Length(first_key) < 3) or
                    (Copy(first_key, Length(first_key) - 1, 2) <> 'ng') or
                    (Copy(normalized_pinyin, 1, Length(first_key)) <> first_key) then Exit;
                head_key := Copy(first_key, 1, Length(first_key) - 1);
                if not nc_is_canonical_pinyin_syllable(head_key) then Exit;
                tail_key := Copy(normalized_pinyin, Length(head_key) + 1, MaxInt);
                tail_parts := get_effective_compact_pinyin_syllables(tail_key);
                if (Length(tail_parts) = 0) or (Length(tail_parts[0].text) < 2) or
                    (tail_parts[0].text[1] <> 'g') then Exit;
                for idx := 0 to High(tail_parts) do
                    if not nc_is_canonical_pinyin_syllable(tail_parts[idx].text) then Exit;
                if not lookup_display_exact_cached(head_key, singles) then Exit;

                // Keep the preferred ng parse. Alternative n/g characters are
                // selectable prefixes, not new paths or learned compounds.
                first_added := -1;
                for idx := 0 to High(singles) do
                begin
                    item := singles[idx];
                    if (item.comment <> '') or
                        (get_candidate_text_unit_count(item.text) <> 1) then Continue;
                    item.comment := tail_key;
                    item.display_kind := cdk_default;
                    before_count := Length(pool_candidates_local);
                    append_pool_candidate_local(item, -1, False, False);
                    if (first_added < 0) and
                        (Length(pool_candidates_local) > before_count) then
                        first_added := before_count;
                end;
                if first_added < 0 then Exit;

                // One alternative before the long tail of ng homophones; all
                // remaining characters stay pageable. Never displace Top1/2
                // or move a single character ahead of word/exact candidates.
                insert_at := 0;
                while (insert_at < first_added) and
                    ((get_candidate_text_unit_count(pool_candidates_local[insert_at].text) <> 1) or
                    (pool_candidates_local[insert_at].comment = '')) do Inc(insert_at);
                insert_at := Min(first_added, Max(2, insert_at + 1));
                reserved := pool_candidates_local[first_added];
                for idx := first_added downto insert_at + 1 do
                begin
                    pool_candidates_local[idx] := pool_candidates_local[idx - 1];
                    pool_source_indices_local[idx] := pool_source_indices_local[idx - 1];
                end;
                pool_candidates_local[insert_at] := reserved;
                pool_source_indices_local[insert_at] := -1;
                if expected_units < c_long_sentence_full_path_min_syllables then
                begin
                    Result := Copy(pool_candidates_local, 0, visible_page_size);
                    visible_source_indices := Copy(pool_source_indices_local, 0, visible_page_size);
                end;
            end;

            procedure order_short_pool_prefix_tiers_local;
            var
                prefix_units: TArray<Integer>;
                idx, units: Integer;
            begin
                SetLength(prefix_units, Length(pool_candidates_local));
                for idx := 0 to High(pool_candidates_local) do
                begin
                    if not get_partial_prefix_units_local(
                        pool_candidates_local[idx], units) then Continue;
                    prefix_units[idx] := units;
                end;
                nc_order_candidate_prefix_tiers(pool_candidates_local,
                    pool_source_indices_local, prefix_units, expected_units);
            end;

            procedure build_short_visible_candidate_pool_local;
            var
                candidate_idx_local: Integer;
                source_idx_local: Integer;
                candidate_local: TncCandidate;
                keep_full_exact_local: Boolean;
                keep_apostrophe_partial_local: Boolean;
            begin
                if (m_page_index <> 0) or (expected_units < 2) or
                    (expected_units >= c_long_sentence_full_path_min_syllables) or
                    (normalized_pinyin = '') then
                begin
                    Exit;
                end;

                SetLength(pool_candidates_local, 0);
                SetLength(pool_source_indices_local, 0);
                seen_candidates_local := TDictionary<string, Boolean>.Create;
                try
                    { Collect the processed first page and remaining source
                      candidates once. Merge scored word-prefix groups before
                      pagination, keeping singles last and complete slots fixed. }
                    for candidate_idx_local := 0 to High(Result) do
                    begin
                        if candidate_idx_local < Length(visible_source_indices) then
                        begin
                            source_idx_local :=
                                visible_source_indices[candidate_idx_local];
                        end
                        else
                        begin
                            source_idx_local := -1;
                        end;
                        append_pool_candidate_local(Result[candidate_idx_local],
                            source_idx_local, False,
                            Result[candidate_idx_local].display_kind =
                            cdk_lm_compound);
                    end;

                    for candidate_idx_local := 0 to High(m_candidates) do
                    begin
                        candidate_local := m_candidates[candidate_idx_local];
                        normalize_display_candidate(candidate_local);
                        keep_full_exact_local :=
                            is_protected_full_query_exact_local(candidate_local);
                        keep_apostrophe_partial_local :=
                            display_candidate_is_explicit_apostrophe_prefix_partial(
                            candidate_local);

                        if (not keep_full_exact_local) and
                            candidate_is_visible_repeated_initial_reduplicated_local(
                            candidate_local) then
                        begin
                            Continue;
                        end;
                        if (not keep_full_exact_local) and
                            (not keep_apostrophe_partial_local) and
                            display_candidate_should_drop_short_invalid_partial(
                            candidate_local) then
                        begin
                            Continue;
                        end;
                        if (not keep_full_exact_local) and
                            (not keep_apostrophe_partial_local) and
                            display_candidate_should_drop_short_nonlexicon_complete(
                            candidate_local, candidate_idx_local) then
                        begin
                            Continue;
                        end;
                        if (not keep_full_exact_local) and
                            (not keep_apostrophe_partial_local) and
                            candidate_is_repeated_particle_tail_complete_local(
                            candidate_local) then
                        begin
                            Continue;
                        end;
                        if (not keep_full_exact_local) and
                            (not keep_apostrophe_partial_local) and
                            candidate_is_non_strict_one_plus_two_complete(
                            candidate_local, candidate_idx_local) then
                        begin
                            Continue;
                        end;

                        append_pool_candidate_local(candidate_local,
                            candidate_idx_local, False,
                            candidate_local.display_kind = cdk_lm_compound);
                    end;

                    order_short_pool_prefix_tiers_local;
                    append_nasal_boundary_singles_local;
                    nc_copy_candidate_page(pool_candidates_local, pool_source_indices_local,
                        0, visible_page_size, Result, visible_source_indices);
                    m_long_visible_candidate_pool_cache := Copy(
                        pool_candidates_local, 0, Length(pool_candidates_local));
                    m_long_visible_candidate_pool_source_indices_cache := Copy(
                        pool_source_indices_local, 0,
                        Length(pool_source_indices_local));
                    m_long_visible_candidate_pool_cache_key :=
                        get_long_visible_candidate_pool_cache_key(
                        visible_page_size);
                    m_long_visible_candidate_pool_source_signature :=
                        get_candidate_state_signature;
                    m_long_visible_candidate_pool_cache_valid := True;
                finally
                    seen_candidates_local.Free;
                end;
            end;
        begin
            if expected_units < c_long_sentence_full_path_min_syllables then
            begin
                build_short_visible_candidate_pool_local;
                Exit;
            end;
            if short_exact_query_mode or
                (normalized_pinyin = '') or
                ((expected_units <= 6) and
                (Length(protected_full_query_exacts) > 0)) then
            begin
                Exit;
            end;

            SetLength(pool_candidates_local, 0);
            SetLength(pool_source_indices_local, 0);
            seen_candidates_local := TDictionary<string, Boolean>.Create;
            try
                long_candidate_count_local := 0;
                append_complete_candidates_from_result_local;
                if long_candidate_count_local <
                    c_long_sentence_complete_candidate_display_limit then
                begin
                    append_long_partials_from_list_local(Result,
                        visible_source_indices, False);
                end;
                if long_candidate_count_local <
                    c_long_sentence_complete_candidate_display_limit then
                begin
                    append_long_partials_from_list_local(m_candidates,
                        visible_source_indices, True);
                end;

                // Prefix words precede first-syllable singles, matching the
                // short-word display categories without exposing more
                // sentence-path variants.
                append_short_prefixes_from_list_local(Result,
                    visible_source_indices, False, False);
                append_short_prefixes_from_list_local(m_candidates,
                    visible_source_indices, True, False);
                append_short_prefixes_from_list_local(Result,
                    visible_source_indices, False, True);
                append_short_prefixes_from_list_local(m_candidates,
                    visible_source_indices, True, True);
                append_all_first_syllable_singles_local;
                append_nasal_boundary_singles_local;

                m_long_visible_candidate_pool_cache := Copy(
                    pool_candidates_local, 0, Length(pool_candidates_local));
                m_long_visible_candidate_pool_source_indices_cache := Copy(
                    pool_source_indices_local, 0,
                    Length(pool_source_indices_local));
                m_long_visible_candidate_pool_cache_key :=
                    get_long_visible_candidate_pool_cache_key(
                    visible_page_size);
                m_long_visible_candidate_pool_source_signature :=
                    get_candidate_state_signature;
                m_long_visible_candidate_pool_cache_valid := True;

                nc_copy_candidate_page(pool_candidates_local, pool_source_indices_local,
                    m_page_index, visible_page_size, Result, visible_source_indices);
            finally
                seen_candidates_local.Free;
            end;
        end;
    begin
        capture_supported_transition_top_local;
        capture_short_exact_ranked_order_local;
        retain_all_full_query_exact_candidates_local;
        normalize_first_syllable_single_partial_order_local;
        sanitize_short_partial_segment_paths;
        if not m_last_lookup_prefix_partial_fast then
        begin
            ensure_display_fixed_wo_tail_candidate_visible;
            ensure_display_short_particle_tail_candidate_visible;
            ensure_display_top_complete_prefix_partial_visible;
        end
        else if Pos('spf=particle', m_last_lookup_debug_extra) > 0 then
        begin
            ensure_display_short_particle_tail_candidate_visible;
        end;
        // Short exact candidates already have their final category and
        // dictionary-weight order. The generic promoter uses path/display
        // scores and must not run while assembling this page.
        if (not short_exact_predictive_prefix_only_mode) and
            (not m_last_lookup_prefix_partial_fast) then
        begin
            append_long_display_first_syllable_partials_to_fill_page;
        end;
        if expected_units >= 4 then
        begin
            insert_fixed_quantity_tail_candidate_from_partial;
            remove_bad_fixed_tail_candidates;
        end;
        restore_short_exact_ranked_order_local;
        restore_supported_transition_top_local;
        insert_short_exact_lexicon_completion_local;
        insert_umlaut_raw_prefixes_local;
        if m_last_lookup_prefix_partial_fast then
        begin
            ensure_fuzzy_short_single_prefixes_visible_local;
        end;
        normalize_page_and_selection;
        start_index := m_page_index * visible_page_size;
        promoted_repeated_initial_count := 0;
        if m_page_index <> 0 then
        begin
            promoted_repeated_initial_count :=
                get_promoted_repeated_initial_candidate_count_local;
            if promoted_repeated_initial_count > 0 then
            begin
                start_index := Max(0, start_index -
                    promoted_repeated_initial_count);
            end;
        end;
        end_index := start_index + visible_page_size - 1;
        if end_index > High(m_candidates) then
        begin
            end_index := High(m_candidates);
        end;

        complete_visible_texts := TDictionary<string, Boolean>.Create;
        emitted_visible_texts := TDictionary<string, Boolean>.Create;
        try
            if ((expected_units = 1) or (m_last_lookup_syllable_count = 1)) then
            begin
                for source_idx := 0 to High(m_candidates) do
                begin
                    candidate := m_candidates[source_idx];
                    normalize_display_candidate(candidate);
                    if (Trim(candidate.text) <> '') and
                        (Trim(candidate.comment) = '') then
                    begin
                        complete_visible_texts.AddOrSetValue(
                            LowerCase(Trim(candidate.text)), True);
                    end;
                end;
            end;

            SetLength(Result, visible_page_size);
            SetLength(visible_source_indices, visible_page_size);
            page_idx := 0;
            source_idx := start_index;
            while (source_idx <= High(m_candidates)) and
                (page_idx < visible_page_size) do
            begin
                candidate := m_candidates[source_idx];
                Inc(source_idx);
                if (m_dictionary <> nil) and (user_entry_query <> '') and
                    (Trim(candidate.comment) = '') and
                    m_dictionary.is_user_entry(user_entry_query,
                    Trim(candidate.text)) then
                begin
                    candidate.source := cs_user;
                    candidate.has_dict_weight := False;
                    candidate.dict_weight := 0;
                end;
                normalize_display_candidate(candidate);

                keep_protected_full_exact :=
                    is_protected_full_query_exact_local(candidate);

                if (not keep_protected_full_exact) and
                    (m_page_index <> 0) and
                    candidate_is_visible_repeated_initial_reduplicated_local(candidate) then
                begin
                    Continue;
                end;

                keep_explicit_prefix_partial :=
                    display_candidate_is_explicit_apostrophe_prefix_partial(candidate);

                if (not keep_protected_full_exact) and
                    (not keep_explicit_prefix_partial) and
                    display_candidate_should_drop_short_invalid_partial(candidate) then
                begin
                    Continue;
                end;

                if (not keep_protected_full_exact) and
                    (not keep_explicit_prefix_partial) and
                    display_candidate_should_drop_short_nonlexicon_complete(
                    candidate, source_idx - 1) then
                begin
                    Continue;
                end;

                if (not keep_protected_full_exact) and
                    (not keep_explicit_prefix_partial) and
                    candidate_is_repeated_particle_tail_complete_local(candidate) then
                begin
                    Continue;
                end;

                if (not keep_protected_full_exact) and
                    (not keep_explicit_prefix_partial) and
                    candidate_is_non_strict_one_plus_two_complete(candidate,
                    source_idx - 1) then
                begin
                    Continue;
                end;

                candidate_key := LowerCase(Trim(candidate.text));
                if (candidate_key <> '') and (Trim(candidate.comment) <> '') and
                    complete_visible_texts.ContainsKey(candidate_key) then
                begin
                    Continue;
                end;
                if (candidate_key <> '') and
                    emitted_visible_texts.ContainsKey(candidate_key) then
                begin
                    Continue;
                end;

                Result[page_idx] := candidate;
                visible_source_indices[page_idx] := source_idx - 1;
                if candidate_key <> '' then
                begin
                    emitted_visible_texts.AddOrSetValue(candidate_key, True);
                end;
                Inc(page_idx);
            end;

            if page_idx <> Length(Result) then
            begin
                SetLength(Result, page_idx);
                SetLength(visible_source_indices, page_idx);
            end;
            apply_forced_visible_top_candidate;
            promote_visible_short_particle_tail_completion_over_partial;
            supplement_visible_short_prefix_exact_alternatives;
            demote_visible_long_sentence_partials_below_complete;
            promote_visible_pathful_complete_over_unbacked_generated_top;
            sanitize_visible_short_partial_source_paths;
            filter_visible_short_nonlexicon_complete_candidates;
            promote_visible_pathful_complete_over_unbacked_generated_top;
            promote_visible_higher_scored_complete_long_candidate;
            demote_visible_long_sentence_partials_below_complete;
            promote_visible_left_boundary_when_candidate_is_stronger;
            promote_visible_repeated_initial_reduplicated_candidates;
            apply_forced_visible_top_candidate;
            dedupe_visible_text_comment_candidates;
            ensure_explicit_apostrophe_prefix_partial_visible;
            promote_visible_full_query_exact_over_generated_top;
            rerank_visible_complete_long_candidates_by_lm;
            rerank_visible_complete_long_candidates_by_char_lm;
            // Keep the proven local models as first-stage proposal generators.
            // Their output is folded into the internal complete pool below; only
            // the unified pool ranker is allowed to decide the visible order.
            rerank_visible_complete_long_candidates_by_local_model_pass(
                False, False);
            rerank_visible_complete_long_candidates_by_local_model_pass(
                True, False);
            // Preserve the established long-sentence pipeline as the
            // first-stage seed ranker. Its visible-looking result remains
            // internal: the complete pool below performs the only final
            // ordering that is exposed to the user.
            apply_long_final_visible_candidate_ranking(Result,
                visible_source_indices);
            rerank_visible_complete_long_candidates_by_local_model_pass(
                True, True);
            apply_long_complete_candidate_pool(Result,
                visible_source_indices, normalized_pinyin, syllables,
                expected_units);
            note_display_phase('longpool');
            promote_strong_short_four_two_exact_path_visible_local(Result,
                visible_source_indices);
            if expected_units >= c_long_sentence_full_path_min_syllables then
            begin
                build_visible_long_sentence_candidate_pool_local;
            end;
            promote_visible_longest_exact_prefix_partial_local;
            restore_visible_supported_transition_top_local;
            if expected_units < c_long_sentence_full_path_min_syllables then
            begin
                build_visible_long_sentence_candidate_pool_local;
            end;
            apply_visible_local_repair(Result, visible_source_indices, expected_units);
            if (Length(Result) > 0) and
                (Trim(Result[0].comment) = '') and
                (get_candidate_text_unit_count(Trim(Result[0].text)) =
                expected_units) then
            begin
                visible_ranked_source_index := visible_source_indices[0];
                if (visible_ranked_source_index >= 0) and
                    (visible_ranked_source_index < Length(m_candidates)) then
                begin
                    visible_ranked_top_path := get_segment_path_for_candidate(
                        m_candidates[visible_ranked_source_index],
                        visible_ranked_source_index);
                    if get_encoded_path_segment_count_local(
                        visible_ranked_top_path) > 1 then
                    begin
                        m_last_ranked_query_key := normalized_pinyin;
                        m_last_ranked_top_path := visible_ranked_top_path;
                    end;
                end;
            end;
            cache_visible_candidate_page(Result, visible_source_indices, visible_page_size);
        finally
            emitted_visible_texts.Free;
            complete_visible_texts.Free;
        end;
        note_display_phase('page');
    end;

    function infer_relaxed_missing_apostrophe_units_from_candidates: Integer;
    var
        idx_local: Integer;
        head_units_local: Integer;
        tail_units_local: Integer;
        tail_key_local: string;
    begin
        Result := 0;
        for idx_local := 0 to High(m_candidates) do
        begin
            tail_key_local := normalize_pinyin_text(
                Trim(m_candidates[idx_local].comment));
            if (tail_key_local = '') or
                (not is_relaxed_no_initial_syllable_text(tail_key_local)) then
            begin
                Continue;
            end;

            head_units_local := get_candidate_text_unit_count(
                Trim(m_candidates[idx_local].text));
            tail_units_local := get_effective_compact_pinyin_unit_count(
                tail_key_local);
            if (head_units_local > 0) and (tail_units_local > 0) and
                (head_units_local + tail_units_local > Result) then
            begin
                Result := head_units_local + tail_units_local;
            end;
        end;
    end;

    function try_get_short_exact_two_syllable_relaxed_tail_keys_local(
        out out_head_key: string; out out_tail_key: string): Boolean;
    var
        parser_local: TncPinyinParser;
        head_key_local: string;
        tail_key_local: string;
    begin
        Result := False;
        out_head_key := '';
        out_tail_key := '';
        if (expected_units <> 2) or (Pos('''', m_composition_text) > 0) then
        begin
            Exit;
        end;

        parser_local := TncPinyinParser.Create;
        try
            if not try_split_relaxed_missing_apostrophe_token(parser_local,
                normalized_pinyin, head_key_local, tail_key_local) then
            begin
                Exit;
            end;
        finally
            parser_local.Free;
        end;

        head_key_local := normalize_pinyin_text(head_key_local);
        tail_key_local := normalize_pinyin_text(tail_key_local);
        if (head_key_local = '') or (tail_key_local = '') or
            (not is_relaxed_no_initial_syllable_text(tail_key_local)) then
        begin
            Exit;
        end;

        out_head_key := head_key_local;
        out_tail_key := tail_key_local;
        Result := True;
    end;

    procedure ensure_short_exact_two_syllable_relaxed_head_partials_local;
    var
        head_key_local: string;
        tail_key_local: string;
        relaxed_syllables_local: TncPinyinParseResult;
    begin
        if try_get_short_exact_two_syllable_relaxed_tail_keys_local(
            head_key_local, tail_key_local) then
        begin
            relaxed_syllables_local :=
                parse_pinyin_with_relaxed_missing_apostrophe(normalized_pinyin);
            if Length(relaxed_syllables_local) = expected_units then
            begin
                syllables := relaxed_syllables_local;
            end;
            ensure_display_head_single_partials_for_keys(head_key_local,
                tail_key_local);
        end;
    end;

    procedure demote_weak_user_single_chain_below_strong_partial;
    const
        c_low_evidence_user_weight = 8;
    var
        top_candidate_local: TncCandidate;
        best_idx_local: Integer;
        best_score_local: Integer;
        idx_local: Integer;
        picked_candidate_local: TncCandidate;
        candidate_units_local: Integer;
        comment_units_local: Integer;

        function is_low_evidence_user_single_chain_local(
            const candidate_value: TncCandidate): Boolean;
        var
            text_units_local: TArray<string>;
            unit_idx_local: Integer;
            unit_key_local: string;
        begin
            Result := False;
            if (expected_units < 3) or (m_dictionary = nil) or
                (candidate_value.source <> cs_user) or
                (Trim(candidate_value.comment) <> '') or
                (Trim(candidate_value.text) = '') then
            begin
                Exit;
            end;
            if candidate_value.has_dict_weight and
                (candidate_value.dict_weight > c_low_evidence_user_weight) then
            begin
                Exit;
            end;
            if (m_dictionary <> nil) and
                m_dictionary.is_user_entry(normalized_pinyin,
                Trim(candidate_value.text)) then
            begin
                Exit;
            end;

            text_units_local := split_text_units(Trim(candidate_value.text));
            if Length(text_units_local) <> expected_units then
            begin
                Exit;
            end;

            for unit_idx_local := 0 to High(text_units_local) do
            begin
                if get_candidate_text_unit_count(text_units_local[unit_idx_local]) <> 1 then
                begin
                    Exit;
                end;
                unit_key_local := build_display_query_key(unit_idx_local, 1);
                if (unit_key_local = '') or
                    (not m_dictionary.single_char_matches_pinyin(unit_key_local,
                    Trim(text_units_local[unit_idx_local]))) then
                begin
                    Exit;
                end;
            end;

            Result := True;
        end;

        function is_strong_prefix_partial_local(
            const candidate_value: TncCandidate): Boolean;
        begin
            Result := False;
            if (Trim(candidate_value.comment) = '') or
                (not candidate_value.has_dict_weight) then
            begin
                Exit;
            end;
            candidate_units_local := get_candidate_text_unit_count(
                Trim(candidate_value.text));
            if candidate_units_local < 2 then
            begin
                Exit;
            end;
            comment_units_local := get_effective_compact_pinyin_unit_count(
                normalize_pinyin_text(Trim(candidate_value.comment)));
            Result := (comment_units_local > 0) and
                (candidate_units_local + comment_units_local = expected_units);
        end;
    begin
        if Length(m_candidates) <= 1 then
        begin
            Exit;
        end;

        top_candidate_local := m_candidates[0];
        if not is_low_evidence_user_single_chain_local(top_candidate_local) then
        begin
            Exit;
        end;

        best_idx_local := -1;
        best_score_local := top_candidate_local.score;
        for idx_local := 1 to High(m_candidates) do
        begin
            if is_strong_prefix_partial_local(m_candidates[idx_local]) and
                (m_candidates[idx_local].score > best_score_local) then
            begin
                best_idx_local := idx_local;
                best_score_local := m_candidates[idx_local].score;
            end;
        end;

        if best_idx_local <= 0 then
        begin
            Exit;
        end;

        picked_candidate_local := m_candidates[best_idx_local];
        for idx_local := best_idx_local downto 1 do
        begin
            m_candidates[idx_local] := m_candidates[idx_local - 1];
        end;
        m_candidates[0] := picked_candidate_local;
    end;

    function try_finalize_display_short_complete_fast_path_local: Boolean;
    begin
        Result := False;
        if not can_use_display_short_complete_fast_path then
        begin
            Exit;
        end;

        filter_display_rejected_user_candidate_texts;
        filter_display_unsupported_short_four_ab_cd_complete_candidates;
        filter_display_unsupported_short_four_ab_c_partial_candidates;
        if not can_use_display_short_complete_fast_path then
        begin
            Exit;
        end;

        restore_entry_pathful_complete_top;
        filter_display_unsupported_short_four_ab_cd_complete_candidates;
        filter_display_unsupported_short_four_ab_c_partial_candidates;
        if expected_units >= 4 then
        begin
            insert_fixed_tail_partial_from_complete;
            insert_fixed_tail_candidate_from_partial;
            insert_fixed_quantity_tail_candidate_from_partial;
            insert_fixed_leading_tail_exact_completion_from_partial;
            promote_and_filter_display_fixed_quantity_tail_candidate;
            insert_fixed_quantity_tail_candidate_from_partial;
            remove_bad_fixed_tail_candidates;
        end;
        note_display_phase('shortcomplete');
        build_current_page_result;
        Result := True;
    end;

begin
    display_phase_tick := nc_monotonic_tick_ms;
    umlaut_raw_prefixes_ready := False;
    relaxed_boundary_cache_ready := False;
    relaxed_boundary_cache_found := False;
    relaxed_boundary_cache_head_key := '';
    relaxed_boundary_cache_tail_key := '';
    relaxed_boundary_cache_head_units := 0;
    relaxed_boundary_cache_tail_units := 0;
    long_display_fast_path := False;
    short_exact_query_mode := False;
    short_exact_predictive_prefix_only_mode := False;
    short_nocontext_promoted_exact_text := '';
    short_nocontext_promoted_exact_lead := 0;
    short_context_promoted_exact_text := '';
    short_context_promoted_exact_lead := 0;
    promoted_repeated_initial_count := 0;
    if not is_fuzzy_pinyin_active then
    begin
        for candidate_idx := 0 to High(m_candidates) do
        begin
            m_candidates[candidate_idx].fuzzy_cost := 0;
            m_candidates[candidate_idx].fuzzy_rules := [];
        end;
        m_forced_visible_top_candidate.fuzzy_cost := 0;
        m_forced_visible_top_candidate.fuzzy_rules := [];
    end;
    SetLength(Result, 0);
    page_size := get_candidate_limit;
    visible_page_size := get_candidate_page_size;
    if visible_candidates_cache_is_current(visible_page_size) then
    begin
        Result := Copy(m_visible_candidates_cache, 0,
            Length(m_visible_candidates_cache));
        Exit;
    end;
    if long_visible_candidate_pool_cache_is_current(visible_page_size) then
    begin
        nc_copy_candidate_page(m_long_visible_candidate_pool_cache,
            m_long_visible_candidate_pool_source_indices_cache,
            m_page_index, visible_page_size, Result, cached_page_sources);
        cache_visible_candidate_page(Result, cached_page_sources, visible_page_size);
        note_display_phase('longvisiblecache');
        Exit;
    end;
    initialize_display_query_context;
    capture_repeated_initial_display_source_candidates;
    capture_explicit_apostrophe_entry_top_partial_candidate;
    if m_last_lookup_literal_user_fast then
    begin
        page_count := get_page_count_internal(page_size);
        if page_count <> 0 then
        begin
            note_display_phase('literaluser');
            build_current_page_result;
            Exit;
        end;
    end;
    if m_last_lookup_all_initial_fast then
    begin
        page_count := get_page_count_internal(page_size);
        if page_count <> 0 then
        begin
            note_display_phase('allinit');
            build_current_page_result;
            Exit;
        end;
    end;
    if is_short_repeated_initial_display_query_local and
        (Length(repeated_initial_display_source_candidates) > 0) then
    begin
        note_display_phase('repeatedinit');
        build_current_page_result;
        Exit;
    end;
    if short_exact_query_mode and (expected_units = 2) and
        (Pos('''', m_composition_text) <= 0) then
    begin
        filter_display_rejected_user_candidate_texts;
        ensure_short_exact_two_syllable_relaxed_head_partials_local;
        note_display_phase('shortexact2');
        build_current_page_result;
        Exit;
    end;

    filter_explicit_apostrophe_display_candidates;
    remove_invisible_display_candidates;
    note_display_phase('pre');

    initialize_display_query_context;
    ensure_display_short_particle_tail_candidate_visible;
    page_count := get_page_count_internal(page_size);
    if page_count = 0 then
    begin
        short_exact_query_mode := apply_short_query_exact_display_ranking;
        if short_exact_query_mode and (Length(m_candidates) > 0) then
        begin
            normalize_page_and_selection;
            note_display_phase('shortexact');
            build_current_page_result;
        end;
        Exit;
    end;

    normalize_page_and_selection;
    if m_last_lookup_syllable_count = 1 then
    begin
        expected_units := 1;
        if not is_shuangpin_input then
        begin
            ensure_missing_apostrophe_display_partial;
            relaxed_single_syllables :=
                parse_pinyin_with_relaxed_missing_apostrophe(
                normalized_pinyin);
            if Length(relaxed_single_syllables) > 1 then
            begin
                expected_units := Length(relaxed_single_syllables);
                prefer_display_relaxed_no_initial_tail_boundary;
            end
            else
            begin
                expected_units :=
                    infer_relaxed_missing_apostrophe_units_from_candidates;
                if expected_units > 1 then
                begin
                    prefer_display_relaxed_no_initial_tail_boundary;
                end
                else
                begin
                    expected_units := 1;
                end;
            end;
        end;
        if m_config.fuzzy_pinyin_enabled and
            (m_config.fuzzy_pinyin_rules <> []) then
        begin
            apply_single_syllable_fuzzy_exact_ranking;
            if short_exact_query_mode then
            begin
                enforce_final_fixed_top_candidate;
            end;
        end;
        filter_display_single_syllable_prefix_partials;
        note_display_phase('single');
        build_current_page_result;
        Exit;
    end;
    capture_entry_pathful_complete_top;
    short_exact_query_mode := apply_short_query_exact_display_ranking;
    if short_exact_query_mode then
    begin
        enforce_final_fixed_top_candidate;
    end;
    note_display_phase('init');
    if short_exact_query_mode then
    begin
        filter_display_rejected_user_candidate_texts;
        note_display_phase('reject');
        if short_exact_predictive_prefix_only_mode then
        begin
            ensure_missing_apostrophe_display_partial;
            prefer_display_relaxed_no_initial_tail_boundary;
            normalize_display_partial_prefix_order;
            note_display_phase('predictprefix');
            build_current_page_result;
            Exit;
        end;
        if not m_last_lookup_short312_head_fast then
        begin
            filter_display_constructed_multi_char_partial_candidates;
        end;
        note_display_phase('partial');
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        note_display_phase('short123');
        prefer_display_relaxed_no_initial_tail_boundary;
        demote_weak_user_single_chain_below_strong_partial;
        ensure_display_short_exact_head_single_partials_complete;
        ensure_short_exact_two_syllable_relaxed_head_partials_local;
        note_display_phase('repair');
        build_current_page_result;
        Exit;
    end;
    if m_last_lookup_short312_head_fast then
    begin
        filter_display_rejected_user_candidate_texts;
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        ensure_missing_apostrophe_display_partial;
        prefer_display_relaxed_no_initial_tail_boundary;
        normalize_display_partial_prefix_order;
        promote_display_full_query_exact_candidate;
        note_display_phase('short312head');
        build_current_page_result;
        Exit;
    end;
    if can_use_display_typo_short_prefix_fast_path then
    begin
        filter_display_rejected_user_candidate_texts;
        insert_typo_short_prefix_tail_completion_from_partial;
        normalize_display_partial_prefix_order;
        note_display_phase('typoshortprefix');
        build_current_page_result;
        Exit;
    end;
    if can_use_display_trailing_prefix_fast_path then
    begin
        filter_display_rejected_user_candidate_texts;
        filter_display_constructed_multi_char_partial_candidates;
        note_display_phase('trailingprefix');
        build_current_page_result;
        Exit;
    end;
    if try_finalize_display_short_complete_fast_path_local then
    begin
        Exit;
    end;
    if (not short_exact_query_mode) and m_last_lookup_prefix_partial_fast and
        top_display_candidate_can_use_short_prefix_fast_display_local then
    begin
        filter_display_rejected_user_candidate_texts;
        filter_short_prefix_fast_constructed_candidates_local;
        promote_best_short_prefix_fast_candidate_local;
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        normalize_display_partial_prefix_order;
        note_display_phase('shortprefixraw');
        build_current_page_result;
        Exit;
    end;
    if (not short_exact_query_mode) and
        m_last_lookup_prefix_partial_fast and (expected_units = 4) and
        (not display_last_syllable_is_short_particle_tail) and
        (Length(m_candidates) > 0) and (Trim(m_candidates[0].comment) <> '') then
    begin
        filter_display_rejected_user_candidate_texts;
        filter_short_prefix_fast_constructed_candidates_local;
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        normalize_display_partial_prefix_order;
        note_display_phase('short4prefixraw');
        build_current_page_result;
        Exit;
    end;
    if (not short_exact_query_mode) and
        can_use_display_fixed_head_tail_complete_fast_path then
    begin
        filter_display_rejected_user_candidate_texts;
        note_display_phase('fixedheadtail');
        build_current_page_result;
        Exit;
    end;
    if not short_exact_query_mode then
    begin
        if ((expected_units >= 4) or is_full_pinyin_key(normalized_pinyin)) and
            (expected_units > 0) then
        begin
            ensure_prefix_lookup_complete_visible;
            promote_display_supported_partial_with_same_tail_local;
            long_display_fast_path :=
                top_display_candidate_can_use_long_fast_display_local;
            if long_display_fast_path then
            begin
                trim_display_long_complete_candidates_to_top2;
                promote_lower_cross_boundary_exact_penalty_candidate;
                promote_higher_scored_pathful_dict_complete_candidate;
                restore_fast_dp_highest_score_complete_candidate;
                promote_display_better_exact_chunk_path_candidate;
                promote_better_exact_span_covered_complete_candidate;
                promote_pathful_high_score_complete_candidate;
                promote_pathful_complete_over_unbacked_generated_top;
                promote_complete_long_candidate_over_partial_top(True);
                promote_higher_scored_pathful_dict_complete_candidate;
                promote_display_better_exact_chunk_path_candidate;
                promote_better_exact_span_covered_complete_candidate;
                promote_higher_rank_complete_long_candidate_final;
                promote_display_better_exact_chunk_path_candidate;
                promote_complete_candidate_with_better_diff_exact_spans;
                remove_complete_candidates_for_all_initial_query;
                if (Pos('prelightfastdp=1', m_last_lookup_debug_extra) > 0) and
                    (Length(m_candidates) > 0) and
                    (Trim(m_candidates[0].comment) = '') and
                    (get_candidate_text_unit_count(Trim(m_candidates[0].text)) =
                    expected_units) and
                    (not is_problematic_single_char_chain_candidate_for_query(
                    normalized_pinyin, m_candidates[0])) then
                begin
                    filter_display_rejected_user_candidate_texts;
                    filter_display_constructed_multi_char_partial_candidates;
                    promote_better_exact_span_covered_complete_candidate;
                    promote_complete_candidate_with_better_diff_exact_spans;
                    promote_pathful_high_score_complete_candidate;
                    promote_pathful_complete_over_unbacked_generated_top;
                    promote_display_better_exact_chunk_path_candidate;
                    promote_display_common_function_context_candidate;
                    promote_display_visible_complete_candidate_by_context_quality;
                    promote_display_adverbial_de_particle_candidate;
                    promote_display_nominal_de_particle_candidate;
                    promote_display_modal_yu_particle_candidate;
                    restore_final_fast_dp_higher_score_complete_candidate;
                    ensure_best_full_query_exact_candidate_visible;
                    promote_display_full_query_exact_candidate;
                    insert_long_exact_span_repair_candidate;
                    promote_better_exact_span_covered_complete_candidate;
                    promote_complete_candidate_with_better_diff_exact_spans;
                    promote_pathful_complete_over_unbacked_generated_top;
                    ensure_display_top_complete_prefix_partial_visible;
                    ensure_best_full_query_exact_candidate_visible;
                    promote_display_full_query_exact_candidate;
                    restore_entry_pathful_complete_top;
                    promote_final_top2_exact_coverage_candidate;
                    keep_long_complete_candidates_before_partial_display;
                    promote_best_second_complete_long_candidate;
                    promote_pathful_high_score_complete_candidate;
                    promote_pathful_complete_over_unbacked_generated_top;
                    promote_higher_scored_complete_long_display_candidate;
                    note_display_phase('longfast');
                    build_current_page_result;
                    Exit;
                end;
            end
            else
            begin
                ensure_missing_apostrophe_display_partial;
                prefer_display_relaxed_no_initial_tail_boundary;
                insert_fixed_tail_candidate_from_partial;
                insert_fixed_quantity_tail_candidate_from_partial;
                insert_fixed_leading_tail_exact_completion_from_partial;
                insert_tail_split_prefix_partial_from_partial;
                promote_pathful_high_score_complete_candidate;
                insert_tail_exact_completion_from_partial;
                insert_typo_tail_exact_completion_from_partial;
                insert_tail_prefix_completion_from_partial;
                insert_two_function_singles_before_phrase_repair;
                promote_pathful_high_score_complete_candidate;
                insert_tail_split_prefix_partial_from_partial;
                insert_fixed_tail_partial_from_complete;
                insert_compact_interrogative_location_candidate;
                promote_demonstrative_head_partial;
                promote_recent_visible_choice_candidate;
                remove_bad_fixed_tail_candidates;
                remove_complete_candidates_for_all_initial_query;
            end;
        end
        else
        begin
            expected_units := 0;
            remove_bad_fixed_tail_candidates;
            remove_complete_candidates_for_all_initial_query;
        end;
    end;
    note_display_phase('first');
    if try_finalize_display_short_complete_fast_path_local then
    begin
        Exit;
    end;
    if (not short_exact_query_mode) and
        (not has_strong_short_complete_candidate_for_prefix_fast_local) then
    begin
        promote_best_short_prefix_fast_candidate_local;
    end;
    if (not short_exact_query_mode) and
        top_display_candidate_can_use_short_prefix_fast_display_local then
    begin
        filter_display_rejected_user_candidate_texts;
        filter_short_prefix_fast_constructed_candidates_local;
        promote_best_short_prefix_fast_candidate_local;
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        ensure_best_full_query_exact_candidate_visible;
        promote_display_full_query_exact_candidate;
        if expected_units >= 4 then
        begin
            insert_fixed_tail_candidate_from_partial;
            insert_fixed_quantity_tail_candidate_from_partial;
            promote_display_fixed_tail_completion_over_matching_partial;
        end;
        note_display_phase('shortprefix');
        build_current_page_result;
        Exit;
    end;
    if (not short_exact_query_mode) and (not long_display_fast_path) and
        (not top_display_candidate_is_stable_complete_local) then
    begin
        promote_recent_visible_choice_candidate;
        insert_tail_exact_completion_from_partial;
        insert_typo_tail_exact_completion_from_partial;
        insert_fixed_tail_candidate_from_partial;
        insert_fixed_quantity_tail_candidate_from_partial;
        promote_display_longer_prefix_completion_candidate;
        if expected_units >= 4 then
        begin
            promote_pathful_high_score_complete_candidate;
            promote_lower_cross_boundary_exact_penalty_candidate;
        end;
        restore_entry_pathful_complete_top;
        promote_display_full_query_exact_candidate;
        promote_display_exact_two_syllable_head_path_candidate;
        promote_display_one_plus_two_function_complete_candidate;
        promote_display_supported_head_partial_candidate;
        filter_display_two_plus_one_noise_when_one_plus_two_function_exists;
        promote_display_fixed_alignment_complete_candidate;
        promote_display_xiang_yiyang_grammar_candidate;
        promote_display_contextual_modifier_repair_candidate;
        promote_display_better_exact_chunk_path_candidate;
        promote_display_more_resolved_partial_candidate;
        promote_dangling_fast_best_partial_candidate;
        promote_display_fixed_tail_completion_over_matching_partial;
        promote_and_filter_display_fixed_quantity_tail_candidate;
        promote_display_longer_prefix_completion_candidate;
        filter_display_invalid_partial_tail_alignment;
        promote_display_full_query_exact_candidate;
        promote_and_filter_display_fixed_quantity_tail_candidate;
        filter_display_nonbase_complete_when_base_exact_exists;
        promote_display_primary_path_complete_candidate;
        promote_display_richer_pathful_dict_complete_candidate;
        ensure_display_confirmed_prefix_user_extensions;
        promote_display_full_query_exact_candidate;
        promote_and_filter_display_fixed_quantity_tail_candidate;
        filter_display_short_one_plus_two_self_composed_complete_candidates;
        filter_display_cross_boundary_single_noise_candidates;
        promote_display_primary_path_complete_candidate;
        promote_display_richer_pathful_dict_complete_candidate;
        filter_display_explicit_apostrophe_complete_candidates;
        prefer_display_relaxed_no_initial_tail_boundary;
        trim_display_long_complete_candidates_to_top2;
        promote_display_contextual_modifier_repair_candidate;
        promote_display_longer_prefix_completion_candidate;
        promote_display_better_exact_chunk_path_candidate;
        promote_display_exact_over_unstable_complete_top_candidate;
        promote_better_exact_span_covered_complete_candidate;
        if expected_units >= 4 then
        begin
            insert_fixed_tail_candidate_from_partial;
            insert_fixed_quantity_tail_candidate_from_partial;
        end;
        if expected_units <= 5 then
        begin
            insert_tail_exact_completion_from_partial;
            filter_display_cross_boundary_single_noise_candidates;
        end;
        promote_display_better_exact_chunk_path_candidate;
        promote_display_fixed_alignment_complete_candidate;
        promote_display_last_syllable_fixed_tail_candidate;
        promote_display_adverbial_de_particle_candidate;
        promote_display_nominal_de_particle_candidate;
        promote_display_modal_yu_particle_candidate;
        promote_display_fixed_tail_completion_over_matching_partial;
        promote_display_pathful_candidate_over_weak_user_top;
        promote_display_primary_path_complete_candidate;
        promote_display_richer_pathful_dict_complete_candidate;
        promote_higher_scored_pathful_dict_complete_candidate;
        promote_display_longer_prefix_completion_candidate;
        promote_display_structured_quantity_candidate;
        promote_display_pathful_candidate_over_weak_user_top;
        promote_display_full_query_exact_candidate;
        restore_entry_pathful_complete_top;
        filter_display_protected_exact_pair_crossing_candidates;
        promote_display_xiang_yiyang_grammar_candidate;
        promote_display_richer_pathful_dict_complete_candidate;
        promote_display_contextual_modifier_repair_candidate;
        promote_display_last_syllable_fixed_tail_candidate;
        promote_display_adverbial_de_particle_candidate;
        promote_display_nominal_de_particle_candidate;
        promote_display_modal_yu_particle_candidate;
        promote_and_filter_display_fixed_quantity_tail_candidate;
        promote_display_fixed_tail_completion_over_matching_partial;
        if expected_units >= 4 then
        begin
            insert_fixed_tail_candidate_from_partial;
            insert_fixed_quantity_tail_candidate_from_partial;
        end;
        promote_display_longer_prefix_completion_candidate;
        if expected_units >= 4 then
        begin
            promote_display_full_query_exact_candidate;
            promote_display_exact_over_unstable_complete_top_candidate;
        end;
        filter_display_nonbase_complete_when_base_exact_exists;
        ensure_best_full_query_exact_candidate_visible;
        prefer_display_relaxed_no_initial_tail_boundary;
        normalize_display_full_query_exact_candidate_order;
        enforce_final_fixed_top_candidate;
        promote_lower_cross_boundary_exact_penalty_candidate;
    end;
    filter_explicit_apostrophe_display_candidates;
    normalize_display_partial_prefix_order;
    promote_lower_cross_boundary_exact_penalty_candidate;
    filter_display_rejected_user_candidate_texts;
    insert_compact_interrogative_location_candidate;
    if not short_exact_query_mode then
    begin
        ensure_best_full_query_exact_candidate_visible;
        promote_display_full_query_exact_candidate;
    end;
    if (not short_exact_query_mode) or (expected_units >= 7) then
    begin
        promote_better_exact_span_covered_complete_candidate;
        promote_complete_candidate_with_better_diff_exact_spans;
        promote_pathful_complete_over_unbacked_generated_top;
        promote_higher_rank_complete_long_candidate_final;
        promote_complete_long_candidate_over_partial_top(False);
        promote_display_adverbial_de_particle_candidate;
        promote_display_nominal_de_particle_candidate;
        promote_display_modal_yu_particle_candidate;
        promote_display_shangwei_negative_candidate;
    end;
    filter_display_constructed_multi_char_partial_candidates;
    note_display_phase('partial');
    promote_higher_score_complete_path_candidate;
    promote_better_exact_span_covered_complete_candidate;
    promote_higher_rank_complete_long_candidate_final;
    promote_better_exact_span_covered_complete_candidate;
    promote_complete_candidate_with_better_diff_exact_spans;
    promote_display_better_exact_chunk_path_candidate;
    promote_display_adverbial_de_particle_candidate;
    promote_display_nominal_de_particle_candidate;
    promote_display_modal_yu_particle_candidate;
    promote_display_shangwei_negative_candidate;
    promote_display_common_function_context_candidate;
    promote_display_visible_complete_candidate_by_context_quality;
    promote_display_adverbial_de_particle_candidate;
    promote_display_nominal_de_particle_candidate;
    promote_display_modal_yu_particle_candidate;
    restore_final_fast_dp_higher_score_complete_candidate;
    insert_long_exact_span_repair_candidate;
    promote_better_exact_span_covered_complete_candidate;
    promote_complete_candidate_with_better_diff_exact_spans;
    ensure_display_top_complete_prefix_partial_visible;
    promote_complete_candidate_with_better_diff_exact_spans;
    if not short_exact_query_mode then
    begin
        ensure_best_full_query_exact_candidate_visible;
        promote_display_full_query_exact_candidate;
    end;
    restore_entry_pathful_complete_top;
    if expected_units >= 4 then
    begin
        insert_fixed_tail_candidate_from_partial;
        insert_fixed_quantity_tail_candidate_from_partial;
        promote_and_filter_display_fixed_quantity_tail_candidate;
        insert_fixed_quantity_tail_candidate_from_partial;
    end;
    keep_long_complete_candidates_before_partial_display;
    promote_final_top2_exact_coverage_candidate;
    keep_long_complete_candidates_before_partial_display;
    promote_best_second_complete_long_candidate;
    promote_higher_scored_complete_long_display_candidate;
    remove_bad_fixed_tail_candidates;
    note_display_phase('promote');
    note_display_phase('repair');
    build_current_page_result;
end;

function TncEngine.get_one_key_completion: TncOneKeyCompletion;
begin
    if has_validated_completion_prefix and
        (m_local_repair_query_key <> m_repaired_completion_query_key) then
    begin
        refresh_validated_prefix_completion;
        m_repaired_completion_query_key := m_local_repair_query_key;
    end;
    Result := project_validated_prefix_completion(m_one_key_completion);
    if Result.text <> m_one_key_completion.text then
    begin
        m_tab_projection_shown := Result;
        m_tab_projection_query := m_composition_text;
        m_tab_projection_document := '';
        if m_document_context_model <> nil then
            m_tab_projection_document := m_document_context_model.document_key;
        m_tab_projection_raw_text := m_one_key_completion.text;
        m_tab_projection_raw_path := m_one_key_completion.path_text;
    end
    else if m_tab_projection_shown.text <> '' then
        m_tab_projection_shown := Default(TncOneKeyCompletion);
end;

function TncEngine.get_long_neural_completion_request(
    out request: TncLongNeuralCompletionRequest): Boolean;
begin
    request := Default(TncLongNeuralCompletionRequest);
    Result := m_has_long_neural_completion_request and
        ((m_one_key_completion.text = '') or
        (m_one_key_completion.source = okcs_long_transition)) and
        (m_long_neural_completion_request.query_prefix <> '') and
        (m_long_neural_completion_request.query_syllables <> '') and
        (m_long_neural_completion_request.top1_text <> '') and
        (m_long_neural_completion_request.top1_anchor_path <> '');
    if Result then
    begin
        request := m_long_neural_completion_request;
    end;
end;

function TncEngine.get_prefetch_long_neural_completion_request(
    out request: TncLongNeuralCompletionRequest): Boolean;
begin
    request := Default(TncLongNeuralCompletionRequest);
    // Only overlap an already prepared fallback request. Experimental repaired
    // anchors must wait for visible repair; exact/static hints need no prefetch.
    Result := (TThread.ProcessorCount >= 8) and
        (m_page_index = 0) and (m_one_key_completion.text = '') and
        (GetEnvironmentVariable('CASSOTIS_TAB_REPAIRED_PREFIX') <> '1') and
        (GetEnvironmentVariable('CASSOTIS_TAB_REPAIRED_PREFIX') <> 'carry') and
        get_long_neural_completion_request(request);
end;

function TncEngine.apply_long_neural_completion(
    const request: TncLongNeuralCompletionRequest;
    const completion_result: TncLongNeuralCompletionResult): Boolean;
const
    c_static_challenge_min_confidence: Single = -2.5;
    c_static_single_character_min_confidence: Single = 0.0;
var
    suffix_segments: TArray<string>;
    pinyin_segments: TArray<string>;
    base_text: string;
    base_path: string;
    anchor_path: string;
    suffix_text: string;
    suffix_pinyin: string;
    continuation_pinyin: string;
    trimmed_base_text: string;
    trimmed_base_path: string;
    trimmed_anchor_path: string;
    segment_text: string;
    segment_pinyin: string;
    segment_idx: Integer;
    segment_units: Integer;
    repaired_units: Integer;
    syllable_count: Integer;

    function same_request(const left_value,
        right_value: TncLongNeuralCompletionRequest): Boolean;
    begin
        Result := SameText(left_value.query_prefix,
            right_value.query_prefix) and
            SameText(left_value.query_syllables,
            right_value.query_syllables) and
            SameText(left_value.context_text,
            right_value.context_text) and
            (left_value.phonetic_only = right_value.phonetic_only) and
            SameText(left_value.top1_text, right_value.top1_text) and
            SameText(left_value.top1_path, right_value.top1_path) and
            SameText(left_value.top1_anchor_path,
            right_value.top1_anchor_path) and
            SameText(left_value.top2_text, right_value.top2_text) and
            SameText(left_value.top2_path, right_value.top2_path) and
            SameText(left_value.top2_anchor_path,
            right_value.top2_anchor_path);
    end;

    function trim_path_tail(const path_value: string;
        const units: Integer): string;
    var
        parts: TArray<string>;
        part_idx: Integer;
        remaining: Integer;
        part_text: string;
    begin
        if units <= 0 then
        begin
            Result := path_value;
            Exit;
        end;
        parts := path_value.Split([#3],
            TStringSplitOptions.ExcludeEmpty);
        remaining := units;
        for part_idx := High(parts) downto 0 do
        begin
            part_text := Trim(parts[part_idx]);
            if Length(part_text) <= remaining then
            begin
                Dec(remaining, Length(part_text));
                parts[part_idx] := '';
            end
            else
            begin
                parts[part_idx] := Copy(part_text, 1,
                    Length(part_text) - remaining);
                remaining := 0;
            end;
            if remaining = 0 then
            begin
                Break;
            end;
        end;
        if remaining <> 0 then
        begin
            Result := '';
            Exit;
        end;
        Result := '';
        for part_idx := 0 to High(parts) do
        begin
            if parts[part_idx] = '' then
            begin
                Continue;
            end;
            if Result <> '' then
            begin
                Result := Result + #3;
            end;
            Result := Result + parts[part_idx];
        end;
    end;
begin
    Result := False;
    if (m_dictionary = nil) or
        ((m_one_key_completion.text <> '') and
        (m_one_key_completion.source <> okcs_long_transition)) or
        (not m_has_long_neural_completion_request) or
        (not same_request(request, m_long_neural_completion_request)) or
        (m_long_neural_completion_prefix_locked and
        ((completion_result.base_rank <> 1) or (completion_result.replace_units <> 0))) or
        (completion_result.base_rank < 1) or
        (completion_result.base_rank > 2) or
        (completion_result.replace_units < 0) or
        (completion_result.replace_units > 6) or
        (request.phonetic_only and
        (completion_result.replace_units = 0)) then
    begin
        Exit;
    end;
    // Static long-transition hints remain visible immediately. The async
    // model may replace one only inside the confidence range that improved
    // both calibration and held-out benchmark partitions.
    if (m_one_key_completion.source = okcs_long_transition) and
        (completion_result.confidence <
        c_static_challenge_min_confidence) then
    begin
        Exit;
    end;
    if completion_result.base_rank = 1 then
    begin
        base_text := request.top1_text;
        base_path := request.top1_path;
        anchor_path := request.top1_anchor_path;
    end
    else
    begin
        base_text := request.top2_text;
        base_path := request.top2_path;
        anchor_path := request.top2_anchor_path;
    end;
    if (base_text = '') or (base_path = '') or (anchor_path = '') then
    begin
        Exit;
    end;

    suffix_segments := completion_result.suffix_path.Split([#3],
        TStringSplitOptions.ExcludeEmpty);
    pinyin_segments := completion_result.suffix_pinyin_path.Split([#3],
        TStringSplitOptions.ExcludeEmpty);
    if (Length(suffix_segments) < 1) or (Length(suffix_segments) > 3) or
        (Length(pinyin_segments) <> Length(suffix_segments)) then
    begin
        Exit;
    end;
    suffix_text := '';
    suffix_pinyin := '';
    continuation_pinyin := '';
    syllable_count := 0;
    repaired_units := 0;
    for segment_idx := 0 to High(suffix_segments) do
    begin
        segment_text := Trim(suffix_segments[segment_idx]);
        segment_pinyin := normalize_pinyin_text(
            pinyin_segments[segment_idx]);
        if (segment_text = '') or (segment_pinyin = '') or
            (not m_dictionary.is_base_entry(segment_pinyin,
            segment_text)) then
        begin
            Exit;
        end;
        segment_units := get_effective_compact_pinyin_unit_count(
            segment_pinyin);
        Inc(syllable_count, segment_units);
        suffix_text := suffix_text + segment_text;
        suffix_pinyin := suffix_pinyin + segment_pinyin;
        if repaired_units < completion_result.replace_units then
        begin
            Inc(repaired_units, segment_units);
            if repaired_units > completion_result.replace_units then
            begin
                Exit;
            end;
        end
        else
        begin
            continuation_pinyin := continuation_pinyin + segment_pinyin;
        end;
    end;
    if (syllable_count < 1) or (syllable_count > 6) or
        (repaired_units <> completion_result.replace_units) or
        (continuation_pinyin = '') or
        (not SameText(suffix_text,
        Trim(completion_result.suffix_text))) then
    begin
        Exit;
    end;
    if (m_one_key_completion.source = okcs_long_transition) and
        (Length(suffix_text) = 1) and
        (completion_result.confidence <
        c_static_single_character_min_confidence) then
    begin
        // A one-character neural suffix saves little and was the only class
        // that regressed the medium-input validation folds. Require positive
        // confidence before replacing a longer, immediately available hint.
        Exit;
    end;

    if Length(base_text) < completion_result.replace_units then
    begin
        Exit;
    end;
    trimmed_base_text := Copy(base_text, 1,
        Length(base_text) - completion_result.replace_units);
    trimmed_base_path := trim_path_tail(base_path,
        completion_result.replace_units);
    trimmed_anchor_path := trim_path_tail(anchor_path,
        completion_result.replace_units);
    if (completion_result.replace_units > 0) and
        (trimmed_base_path = '') and (trimmed_base_text <> '') then
    begin
        Exit;
    end;
    if trimmed_anchor_path = '' then
    begin
        trimmed_anchor_path := trimmed_base_path;
    end;
    if SameText(m_one_key_completion.text,
        trimmed_base_text + suffix_text) and
        SameText(m_one_key_completion.full_pinyin,
        request.query_prefix + continuation_pinyin) then
    begin
        // Keep the already-visible static result and avoid an asynchronous
        // repaint when the model reaches the same completion.
        Exit;
    end;

    m_one_key_completion := Default(TncOneKeyCompletion);
    m_one_key_completion.anchor_text := trimmed_base_text;
    m_one_key_completion.suffix_text := suffix_text;
    m_one_key_completion.anchor_path := trimmed_anchor_path;
    m_one_key_completion.text := trimmed_base_text + suffix_text;
    m_one_key_completion.full_pinyin := request.query_prefix +
        continuation_pinyin;
    m_one_key_completion.path_text := trimmed_base_path;
    if m_one_key_completion.path_text <> '' then
    begin
        m_one_key_completion.path_text :=
            m_one_key_completion.path_text + #3;
    end;
    m_one_key_completion.path_text := m_one_key_completion.path_text +
        completion_result.suffix_path;
    m_one_key_completion.weight := Round(
        completion_result.confidence * 1000.0);
    m_one_key_completion.prefix_anchored := True;
    m_one_key_completion.source := okcs_long_neural;
    m_one_key_completion_query_prefix := request.query_prefix;
    m_one_key_completion_score := m_one_key_completion.weight;
    m_has_long_neural_completion_request := False;
    Result := True;
end;

function TncEngine.get_page_count_internal(const page_size: Integer): Integer;
var
    total_count: Integer;
begin
    if long_visible_candidate_pool_cache_is_current(page_size) then
    begin
        total_count := Length(m_long_visible_candidate_pool_cache);
    end
    else
    begin
        total_count := Length(m_candidates);
    end;
    Result := nc_candidate_page_count(total_count, page_size);
end;

function TncEngine.get_page_index: Integer;
begin
    if get_page_count_internal(get_candidate_page_size) = 0 then
    begin
        Result := 0;
        Exit;
    end;

    Result := m_page_index;
end;

function TncEngine.get_page_count: Integer;
begin
    Result := get_page_count_internal(get_candidate_page_size);
end;

function TncEngine.next_page: Boolean;
var
    page_count: Integer;
begin
    page_count := get_page_count;
    if page_count = 0 then
    begin
        Result := False;
        Exit;
    end;

    if m_page_index < page_count - 1 then
    begin
        m_candidate_navigation_started := True;
        Inc(m_page_index);
        m_selected_index := 0;
        Result := True;
    end
    else
    begin
        Result := False;
    end;
end;

function TncEngine.prev_page: Boolean;
begin
    if m_page_index > 0 then
    begin
        m_candidate_navigation_started := True;
        Dec(m_page_index);
        m_selected_index := 0;
        Result := True;
    end
    else
    begin
        Result := False;
    end;
end;

function TncEngine.get_composition_text: string;
begin
    if m_composition_display_text <> '' then
    begin
        Result := m_composition_display_text;
    end
    else
    begin
        Result := m_composition_text;
    end;
end;

function TncEngine.get_preedit_text: string;
begin
    // Keep confirmed segments visible while the remaining raw input is edited.
    // get_composition_text remains the replay/query input used by the core.
    Result := m_confirmed_text + get_composition_text;
end;

function TncEngine.get_last_lookup_key: string;
begin
    Result := m_last_lookup_key;
end;

function TncEngine.get_display_text: string;
var
    page_size: Integer;
    visible_index: Integer;
    display_text: string;
    visible_candidates: TncCandidateList;
begin
    if (m_composition_text = '') and (m_confirmed_text = '') then
    begin
        Result := '';
        Exit;
    end;

    display_text := '';
    if m_composition_text <> '' then
    begin
        normalize_page_and_selection;
        page_size := get_candidate_page_size;
        if visible_candidates_cache_is_current(page_size) then
        begin
            visible_candidates := m_visible_candidates_cache;
        end
        else
        begin
            visible_candidates := get_candidates;
        end;
        visible_index := m_selected_index;
        if (visible_index >= 0) and (visible_index < Length(visible_candidates)) then
        begin
            display_text := visible_candidates[visible_index].text;
        end
        else
        begin
            if m_composition_display_text <> '' then
            begin
                display_text := m_composition_display_text;
            end
            else
            begin
                display_text := m_composition_text;
            end;
        end;
    end;

    Result := m_confirmed_text + display_text;
end;

function TncEngine.get_confirmed_length: Integer;
begin
    Result := Length(m_confirmed_text);
end;

function TncEngine.get_lookup_perf_info: string;
var
    perf_parts: TStringList;
begin
    if m_last_lookup_key = '' then
    begin
        Result := '';
        Exit;
    end;

    perf_parts := TStringList.Create;
    try
        perf_parts.Delimiter := ' ';
        perf_parts.StrictDelimiter := True;
        if m_last_lookup_normalized_from <> '' then
        begin
            perf_parts.Add(Format('query_norm=[%s->%s]', [m_last_lookup_normalized_from, m_last_lookup_key]));
        end
        else
        begin
            perf_parts.Add(Format('query=[%s]', [m_last_lookup_key]));
        end;

        if m_last_lookup_syllable_count > 0 then
        begin
            perf_parts.Add(Format('syll=%d', [m_last_lookup_syllable_count]));
        end;

        if m_last_lookup_timing_info <> '' then
        begin
            perf_parts.Add(m_last_lookup_timing_info);
        end;

        Result := Trim(StringReplace(perf_parts.DelimitedText, '"', '', [rfReplaceAll]));
    finally
        perf_parts.Free;
    end;
end;

function TncEngine.get_lookup_debug_info: string;
var
    debug_parts: TStringList;
    sqlite_dict: TncSqliteDictionary;
    context_preview: string;
begin
    if m_last_lookup_key = '' then
    begin
        Result := '';
        Exit;
    end;

    debug_parts := TStringList.Create;
    try
        debug_parts.Delimiter := ' ';
        debug_parts.StrictDelimiter := True;
        if m_last_lookup_normalized_from <> '' then
        begin
            debug_parts.Add(Format('query_norm=[%s->%s]', [m_last_lookup_normalized_from, m_last_lookup_key]));
        end
        else
        begin
            debug_parts.Add(Format('query=[%s]', [m_last_lookup_key]));
        end;

        if not m_config.debug_mode then
        begin
            Result := Trim(StringReplace(debug_parts.DelimitedText, '"', '', [rfReplaceAll]));
            Exit;
        end;

        if m_last_lookup_syllable_count > 0 then
        begin
            debug_parts.Add(Format('syll=%d', [m_last_lookup_syllable_count]));
        end;

        context_preview := Trim(m_left_context);
        if context_preview <> '' then
        begin
            debug_parts.Add(Format('ctx=[%s]', [context_preview]));
        end;

        if m_last_lookup_debug_extra <> '' then
        begin
            debug_parts.Add(m_last_lookup_debug_extra);
        end;
        if m_last_lookup_timing_info <> '' then
        begin
            debug_parts.Add(m_last_lookup_timing_info);
        end;

        if m_dictionary is TncSqliteDictionary then
        begin
            sqlite_dict := TncSqliteDictionary(m_dictionary);
            if sqlite_dict.get_last_lookup_debug_hint <> '' then
            begin
                debug_parts.Add(sqlite_dict.get_last_lookup_debug_hint);
            end;
        end;

        Result := Trim(StringReplace(debug_parts.DelimitedText, '"', '', [rfReplaceAll]));
    finally
        debug_parts.Free;
    end;
end;

function TncEngine.get_selected_index: Integer;
begin
    if Length(m_candidates) = 0 then
    begin
        Result := 0;
        Exit;
    end;

    normalize_page_and_selection;
    Result := m_selected_index;
end;

function TncEngine.get_debug_last_output_commit_text: string;
begin
    Result := m_last_output_commit_text;
end;

function TncEngine.get_debug_phrase_context_pair_count(const left_text: string;
    const candidate_text: string): Integer;
var
    key: string;
begin
    Result := 0;
    if m_phrase_context_pairs = nil then
    begin
        Exit;
    end;

    if (Trim(left_text) = '') or (Trim(candidate_text) = '') then
    begin
        Exit;
    end;

    key := Trim(left_text) + #1 + Trim(candidate_text);
    if not m_phrase_context_pairs.TryGetValue(key, Result) then
    begin
        Result := 0;
    end;
end;

function TncEngine.get_debug_last_commit_segment_path: string;
begin
    Result := m_last_debug_commit_segment_path;
end;

procedure TncEngine.debug_enable_local_repair_capture(const enabled: Boolean);
begin
    m_debug_capture_local_repair := enabled;
    m_debug_local_repair_guard := Default(TncLocalRepairGuardDebug);
end;

function TncEngine.debug_get_local_repair_guard: TncLocalRepairGuardDebug;
begin
    Result := Default(TncLocalRepairGuardDebug);
    if m_debug_capture_local_repair and
        (m_debug_local_repair_guard.query_text = m_composition_text) then
        Result := m_debug_local_repair_guard;
end;

function TncEngine.get_debug_candidate_segment_path(const candidate_index: Integer): string;
var
    source_index: Integer;
begin
    Result := '';
    source_index := candidate_index;
    if (candidate_index >= 0) and
        (candidate_index < Length(m_visible_candidate_source_indices_cache)) then
    begin
        source_index := m_visible_candidate_source_indices_cache[candidate_index];
    end;

    if (source_index >= 0) and (source_index < Length(m_candidates)) then
    begin
        // Match the runtime guard: restored/generated candidates can keep their
        // path in the text-keyed cache rather than the parallel source array.
        Result := get_segment_path_for_candidate(m_candidates[source_index], source_index);
    end;
end;

function TncEngine.get_debug_target_recall(
    const target_text: string): TncTargetRecallDebug;
var
    idx: Integer;
    complete_rank: Integer;
    normalized_target: string;
begin
    Result.found := False;
    Result.raw_rank := 0;
    Result.complete_rank := 0;
    Result.raw_candidate_count := Length(m_candidates);
    Result.complete_candidate_count := 0;
    Result.score := 0;
    Result.segment_path := '';
    Result.final_state_found := m_debug_target_final_state_found;
    Result.final_state_rank := m_debug_target_final_state_rank;
    Result.final_state_count := m_debug_target_final_state_count;
    Result.max_generated_syllable := m_debug_target_max_generated_syllable;
    Result.max_retained_syllable := m_debug_target_max_retained_syllable;
    Result.chain_generated_syllable :=
        m_debug_target_chain_generated_syllable;
    Result.chain_prebeam_syllable :=
        m_debug_target_chain_prebeam_syllable;
    Result.chain_beam_syllable := m_debug_target_chain_beam_syllable;
    Result.full_path_generated_syllable :=
        m_debug_target_full_path_generated_syllable;
    Result.full_path_retained_syllable :=
        m_debug_target_full_path_retained_syllable;
    Result.final_state_source_mask := m_debug_target_final_state_source_mask;
    Result.chain_final_state_rank := m_debug_target_chain_final_state_rank;
    Result.light_final_state_rank := m_debug_target_light_final_state_rank;
    Result.lattice_final_state_rank := m_debug_target_lattice_final_state_rank;
    Result.oracle_final_state_rank := m_debug_target_oracle_final_state_rank;
    Result.full_final_state_rank := m_debug_target_full_final_state_rank;
    Result.chain_build_attempted := m_debug_target_chain_build_attempted;
    Result.chain_build_accepted := m_debug_target_chain_build_accepted;
    Result.chain_top_score := m_debug_target_chain_top_score;
    Result.chain_min_score := m_debug_target_chain_min_score;
    Result.chain_search_status := m_debug_target_chain_search_status;
    Result.candidate_stage_mask := m_debug_target_candidate_stage_mask;
    Result.input_syllable_count := m_last_lookup_syllable_count;
    Result.full_path_budget_exhausted :=
        m_debug_target_full_path_budget_exhausted;

    normalized_target := Trim(target_text);
    complete_rank := 0;
    for idx := 0 to High(m_candidates) do
    begin
        if (Trim(m_candidates[idx].text) = '') or
            (Trim(m_candidates[idx].comment) <> '') then
        begin
            Continue;
        end;

        Inc(complete_rank);
        if (not Result.found) and SameText(
            Trim(m_candidates[idx].text), normalized_target) then
        begin
            Result.found := True;
            Result.raw_rank := idx + 1;
            Result.complete_rank := complete_rank;
            Result.score := m_candidates[idx].score;
            Result.segment_path := get_segment_path_for_candidate(
                m_candidates[idx], idx);
        end;
    end;
    Result.complete_candidate_count := complete_rank;
end;

function TncEngine.get_debug_long_beam_states: TncLongBeamStateDebugArray;
begin
    Result := Copy(m_debug_long_beam_states);
end;

function TncEngine.get_debug_long_exact_edge_stages:
    TncLongExactEdgeStageDebugArray;
begin
    Result := Copy(m_debug_long_exact_edge_stages);
end;

function TncEngine.get_debug_long_retained_exact_edges:
    TncLongRetainedExactEdgeArray;
begin
    Result := Copy(m_runtime_long_retained_exact_edges);
end;

function TncEngine.get_debug_long_chain_candidates:
    TncLongChainCandidateDebugArray;
begin
    Result := Copy(m_debug_long_chain_candidates);
end;

function TncEngine.get_debug_long_final_candidates:
    TncLongFinalCandidateDebugArray;
begin
    Result := Copy(m_debug_long_final_candidates);
end;

function TncEngine.get_debug_long_ranking_stages:
    TncLongRankingStageDebugArray;
begin
    Result := Copy(m_debug_long_ranking_stages);
end;

function TncEngine.get_debug_pending_commit_segment_path: string;
begin
    Result := m_pending_commit_segment_path;
end;

function TncEngine.get_dictionary_debug_info: string;
var
    sqlite_dict: TncSqliteDictionary;
    provider_name: string;
    ready_value: Integer;
    base_ready_value: Integer;
    user_ready_value: Integer;
    path_value: string;
    path_exists: Integer;
    sc_path_value: string;
    sc_path_exists: Integer;
    tc_path_value: string;
    tc_path_exists: Integer;
    user_path_value: string;
    user_path_exists: Integer;
    variant_text: string;
    active_path: string;
begin
    provider_name := 'none';
    ready_value := 0;
    base_ready_value := 0;
    user_ready_value := 0;
    path_value := '';
    path_exists := 0;
    sc_path_value := '';
    sc_path_exists := 0;
    tc_path_value := '';
    tc_path_exists := 0;
    user_path_value := '';
    user_path_exists := 0;
    active_path := get_active_dictionary_path;
    if m_config.dictionary_variant = dv_traditional then
    begin
        variant_text := 'traditional';
    end
    else
    begin
        variant_text := 'simplified';
    end;

    if m_dictionary is TncSqliteDictionary then
    begin
        sqlite_dict := TncSqliteDictionary(m_dictionary);
        provider_name := 'sqlite';
        ready_value := Ord(sqlite_dict.ready);
        base_ready_value := Ord(sqlite_dict.base_ready);
        user_ready_value := Ord(sqlite_dict.user_ready);
        path_value := sqlite_dict.db_path;
        user_path_value := sqlite_dict.user_db_path;
    end
    else if m_dictionary is TncInMemoryDictionary then
    begin
        provider_name := 'memory';
    end;

    if path_value = '' then
        path_value := active_path;
    if (path_value <> '') and FileExists(path_value) then
    begin
        path_exists := 1;
    end;

    sc_path_value := get_dictionary_path_for_variant(dv_simplified);
    if (sc_path_value <> '') and FileExists(sc_path_value) then
    begin
        sc_path_exists := 1;
    end;

    tc_path_value := get_dictionary_path_for_variant(dv_traditional);
    if (tc_path_value <> '') and FileExists(tc_path_value) then
    begin
        tc_path_exists := 1;
    end;

    if not (m_dictionary is TncSqliteDictionary) then
        user_path_value := get_configured_user_dictionary_path;
    if (user_path_value <> '') and FileExists(user_path_value) then
    begin
        user_path_exists := 1;
    end;

    Result := Format('provider=%s ready=%d base_ready=%d user_ready=%d variant=%s dict_path=%s exists=%d dict_sc=%s sc_exists=%d dict_tc=%s tc_exists=%d user_path=%s user_exists=%d',
        [provider_name, ready_value, base_ready_value, user_ready_value, variant_text, path_value, path_exists,
        sc_path_value, sc_path_exists, tc_path_value, tc_path_exists, user_path_value, user_path_exists]);
end;

function TncEngine.should_handle_key(const key_code: Word; const key_state: TncKeyState): Boolean;
var
    key_char: Char;
    display_key_char: Char;
    punct_char: Char;
    has_candidates: Boolean;
    direct_commit_text: string;
begin
    Result := False;
    if nc_shortcut_matches(m_config.shortcuts.input_mode_toggle, key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if nc_shortcut_matches(m_config.shortcuts.full_width_toggle, key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if nc_shortcut_matches(m_config.shortcuts.punctuation_toggle, key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if key_state.ctrl_down or key_state.alt_down then
    begin
        Exit(False);
    end;

    if m_config.input_mode = im_english then
    begin
        Result := get_direct_ascii_commit_text(key_code, key_state, direct_commit_text);
        Exit;
    end;

    if is_alpha_key(key_code, key_state, key_char, display_key_char) then
    begin
        Result := True;
        Exit;
    end;

    if (key_code = VK_OEM_1) and (not key_state.shift_down) and
        nc_shuangpin_accepts_semicolon(m_config.pinyin_input_scheme,
        m_composition_display_text) then
    begin
        Result := True;
        Exit;
    end;

    if (m_one_key_completion.text <> '') and
        nc_one_key_completion_matches(m_config.one_key_completion_key,
        key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if (m_config.one_key_completion_key = ock_backtick) and
        (m_composition_text <> '') and
        nc_one_key_completion_matches(m_config.one_key_completion_key,
        key_code, key_state) then
    begin
        Result := True;
        Exit;
    end;

    if get_punctuation_char(key_code, key_state, punct_char) then
    begin
        if m_composition_text <> '' then
        begin
            Result := True;
        end
        else
        begin
            Result := get_effective_punctuation_full_width or m_config.full_width_mode;
        end;
        Exit;
    end;

    has_candidates := Length(m_candidates) > 0;
    if (m_composition_text = '') and (m_confirmed_text = '') and
        (not has_candidates) and (m_one_key_completion.text = '') and
        (not m_has_pending_commit) then
    begin
        Exit(False);
    end;

    if nc_candidate_page_key_matches_next(
        m_config.candidate_page_key_scheme, key_code, key_state) or
        nc_candidate_page_key_matches_previous(
        m_config.candidate_page_key_scheme, key_code, key_state) then
    begin
        if (m_composition_text <> '') or (m_confirmed_text <> '') or
            has_candidates then
        begin
            Result := True;
            Exit;
        end;
    end;

    if (m_composition_text <> '') and (not key_state.shift_down) and (not key_state.ctrl_down) and (not key_state.alt_down) then
    begin
        case key_code of
            VK_LEFT, VK_RIGHT, VK_UP, VK_DOWN:
                begin
                    Result := True;
                    Exit;
                end;
        end;
    end;

    case key_code of
        VK_BACK,
        VK_OEM_7,
        VK_ESCAPE,
        VK_SPACE,
        VK_RETURN,
        VK_PRIOR,
        VK_NEXT:
            Result := True;
        Ord('1')..Ord('9'):
            Result := True;
    end;
end;

function TncEngine.commit_text(out out_text: string): Boolean;
var
    normalized_pinyin: string;
    remaining_pinyin: string;
    remaining_input_code: string;
    full_pinyin: string;
    segment: TncConfirmedSegment;
    prev_left_context: string;
    commit_text: string;
    commit_segment_text: string;
    commit_segment_path: string;
    effective_segment_path: string;
    inferred_segment_path: string;
    allow_learning: Boolean;
    learning_locked: Boolean;
    path_segments: TArray<string>;
    path_idx: Integer;
    path_start: Integer;
    path_text: string;
    combined_segment_path: string;
    confirmed_pinyin_prefix: string;
    confirmed_segment_path_prefix: string;
    explicit_commit_choice: Boolean;
    literal_commit_choice: Boolean;
    pending_fuzzy_choice: Boolean;
    pending_fuzzy_choice_text: string;
    fuzzy_commit_choice: Boolean;
    prev_prev_committed_text: string;
    prev_prev_committed_pinyin: string;
    previous_committed_text: string;
    previous_committed_pinyin: string;
    literal_only_commit: Boolean;
    literal_word_to_record: Boolean;
    literal_full_pinyin: string;

    function is_boundary_learning_unit(const text_value: string): Boolean;
    begin
        Result := False;
        if get_candidate_text_unit_count(text_value) <> 1 then
        begin
            Exit;
        end;

        case Ord(text_value[1]) of
            $7684, $5730, $5F97, $4E86, $7740:
                Result := True;
        end;
    end;

    function is_trailing_boundary_learning_unit(const text_value: string): Boolean;
    begin
        Result := is_boundary_learning_unit(text_value);
        if Result then
        begin
            Exit;
        end;

        Result := (get_candidate_text_unit_count(text_value) = 1) and
            (Ord(text_value[1]) = $518D);
    end;

    function is_boundary_noise_learning_text(const text_value: string): Boolean;
    var
        text_units: TArray<string>;
    begin
        Result := False;
        text_units := split_text_units(Trim(text_value));
        if Length(text_units) < 2 then
        begin
            Exit;
        end;

        Result := is_boundary_learning_unit(text_units[0]) or
            is_trailing_boundary_learning_unit(text_units[High(text_units)]);
    end;

    function should_persist_adjacent_commit_learning(const left_text: string;
        const right_text: string): Boolean;
    var
        combined_text: string;
    begin
        combined_text := Trim(left_text) + Trim(right_text);
        Result := (combined_text <> '') and
            (not is_boundary_noise_learning_text(combined_text)) and
            (not is_boundary_learning_unit(Trim(left_text))) and
            (not is_trailing_boundary_learning_unit(Trim(right_text)));
    end;

    function is_generated_short_particle_tail_commit(const query_value: string;
        const text_value: string): Boolean;
    var
        query_key_local: string;
        text_key_local: string;
        tail_key_local: string;
        tail_text_local: string;
        head_key_local: string;
        head_text_local: string;
        text_units_local: TArray<string>;
        head_unit_count_local: Integer;
        unit_idx_local: Integer;

        function try_get_tail_local: Boolean;
        begin
            Result := try_get_short_particle_tail_query_parts(query_key_local,
                tail_key_local, tail_text_local, head_key_local);
        end;
    begin
        Result := False;
        if m_dictionary = nil then
        begin
            Exit;
        end;

        query_key_local := normalize_pinyin_text(query_value);
        text_key_local := Trim(text_value);
        if (query_key_local = '') or (text_key_local = '') or
            (not is_full_pinyin_key(query_key_local)) or
            m_dictionary.is_base_entry(query_key_local, text_key_local) or
            m_dictionary.is_user_entry(query_key_local, text_key_local) or
            (not try_get_tail_local) then
        begin
            Exit;
        end;

        head_unit_count_local :=
            get_effective_compact_pinyin_unit_count(head_key_local);
        if (head_key_local = '') or (head_unit_count_local < 2) or
            (head_unit_count_local > 3) then
        begin
            Exit;
        end;

        text_units_local := split_text_units(text_key_local);
        if (Length(text_units_local) <> head_unit_count_local + 1) or
            (not SameText(Trim(text_units_local[High(text_units_local)]),
            tail_text_local)) then
        begin
            Exit;
        end;

        head_text_local := '';
        for unit_idx_local := 0 to High(text_units_local) - 1 do
        begin
            head_text_local := head_text_local + text_units_local[unit_idx_local];
        end;

        Result := (head_text_local <> '') and
            (m_dictionary.is_base_entry(head_key_local, head_text_local) or
            m_dictionary.is_user_entry(head_key_local, head_text_local));
    end;

    function get_path_segment_count(const encoded_path: string): Integer;
    var
        idx: Integer;
        trimmed_path: string;
    begin
        trimmed_path := Trim(encoded_path);
        if trimmed_path = '' then
        begin
            Exit(0);
        end;

        Result := 1;
        for idx := 1 to Length(trimmed_path) do
        begin
            if trimmed_path[idx] = c_segment_path_separator then
            begin
                Inc(Result);
            end;
        end;
    end;

    function is_high_confidence_segment_path(const encoded_path: string): Boolean;
    var
        idx: Integer;
        segment_start: Integer;
        segment_text: string;
        has_multi_char_segment: Boolean;
    begin
        Result := False;
        if get_path_segment_count(encoded_path) <= 1 then
        begin
            Exit;
        end;

        has_multi_char_segment := False;
        segment_start := 1;
        for idx := 1 to Length(encoded_path) + 1 do
        begin
            if (idx <= Length(encoded_path)) and (encoded_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_text := Trim(Copy(encoded_path, segment_start, idx - segment_start));
            if segment_text <> '' then
            begin
                if get_candidate_text_unit_count(segment_text) >= 2 then
                begin
                    has_multi_char_segment := True;
                    Break;
                end;
            end;
            segment_start := idx + 1;
        end;

        Result := has_multi_char_segment;
    end;

    function get_path_text_unit_count(const encoded_path: string): Integer;
    var
        idx: Integer;
        segment_start: Integer;
        segment_text: string;
    begin
        Result := 0;
        segment_start := 1;
        for idx := 1 to Length(encoded_path) + 1 do
        begin
            if (idx <= Length(encoded_path)) and
                (encoded_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_text := Trim(Copy(encoded_path, segment_start, idx - segment_start));
            segment_start := idx + 1;
            if segment_text <> '' then
            begin
                Inc(Result, get_candidate_text_unit_count(segment_text));
            end;
        end;
    end;

    function should_persist_segment_path(const encoded_path: string): Boolean;
    const
        c_max_persistent_path_units = 10;
    begin
        Result := (get_path_segment_count(encoded_path) > 1) and
            (get_path_text_unit_count(encoded_path) <= c_max_persistent_path_units);
    end;

    function should_persist_path_prefixes(const encoded_path: string): Boolean;
    const
        c_max_persistent_prefix_segments = 4;
    begin
        Result := should_persist_segment_path(encoded_path) and
            (get_path_segment_count(encoded_path) <= c_max_persistent_prefix_segments);
    end;

    procedure append_path_segment(const segment_text: string);
    var
        segment_index: Integer;
    begin
        path_text := Trim(segment_text);
        if path_text = '' then
        begin
            Exit;
        end;
        segment_index := Length(path_segments);
        SetLength(path_segments, segment_index + 1);
        path_segments[segment_index] := path_text;
        if combined_segment_path <> '' then
        begin
            combined_segment_path := combined_segment_path + c_segment_path_separator;
        end;
        combined_segment_path := combined_segment_path + path_text;
    end;

    procedure record_query_path_prefixes(const base_query_prefix: string; const base_path_prefix: string;
        const tail_full_path: string; const final_query: string; const final_path: string);
    var
        idx: Integer;
        prefix_start: Integer;
        segment_piece: string;
        prefix_path: string;
        prefix_query: string;
        combined_query: string;
        combined_path: string;
    begin
        if (m_dictionary = nil) or (tail_full_path = '') then
        begin
            Exit;
        end;

        prefix_path := '';
        prefix_start := 1;
        for idx := 1 to Length(tail_full_path) + 1 do
        begin
            if (idx <= Length(tail_full_path)) and (tail_full_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_piece := Trim(Copy(tail_full_path, prefix_start, idx - prefix_start));
            prefix_start := idx + 1;
            if segment_piece = '' then
            begin
                Continue;
            end;

            if prefix_path <> '' then
            begin
                prefix_path := prefix_path + c_segment_path_separator;
            end;
            prefix_path := prefix_path + segment_piece;

            if get_path_segment_count(prefix_path) <= 1 then
            begin
                Continue;
            end;

            prefix_query := get_query_prefix_for_segment_path(prefix_path);
            if prefix_query = '' then
            begin
                Continue;
            end;

            combined_query := base_query_prefix + prefix_query;
            combined_path := prefix_path;
            if base_path_prefix <> '' then
            begin
                combined_path := base_path_prefix + c_segment_path_separator + combined_path;
            end;

            if (combined_query = '') or (combined_path = '') or
                ((combined_query = final_query) and (combined_path = final_path)) then
            begin
                Continue;
            end;

            m_dictionary.record_query_segment_path(combined_query, combined_path);
        end;
    end;

    procedure note_session_query_path_prefixes(const base_query_prefix: string; const base_path_prefix: string;
        const tail_full_path: string; const final_query: string; const final_path: string);
    var
        idx: Integer;
        prefix_start: Integer;
        segment_piece: string;
        prefix_path: string;
        prefix_query: string;
        combined_query: string;
        combined_path: string;
    begin
        if tail_full_path = '' then
        begin
            Exit;
        end;

        prefix_path := '';
        prefix_start := 1;
        for idx := 1 to Length(tail_full_path) + 1 do
        begin
            if (idx <= Length(tail_full_path)) and (tail_full_path[idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            segment_piece := Trim(Copy(tail_full_path, prefix_start, idx - prefix_start));
            prefix_start := idx + 1;
            if segment_piece = '' then
            begin
                Continue;
            end;

            if prefix_path <> '' then
            begin
                prefix_path := prefix_path + c_segment_path_separator;
            end;
            prefix_path := prefix_path + segment_piece;

            if get_path_segment_count(prefix_path) <= 1 then
            begin
                Continue;
            end;

            prefix_query := get_query_prefix_for_segment_path(prefix_path);
            if prefix_query = '' then
            begin
                Continue;
            end;

            combined_query := base_query_prefix + prefix_query;
            combined_path := prefix_path;
            if base_path_prefix <> '' then
            begin
                combined_path := base_path_prefix + c_segment_path_separator + combined_path;
            end;

            if (combined_query = '') or (combined_path = '') or
                ((combined_query = final_query) and (combined_path = final_path)) then
            begin
                Continue;
            end;

            note_session_query_path_choice(combined_query, combined_path);
        end;
    end;

    function prepare_literal_user_word(out canonical_full_pinyin: string): Boolean;
    var
        confirmed_segment: TncConfirmedSegment;
        current_canonical_pinyin: string;
        has_nonfull_input: Boolean;
        text_unit_count: Integer;

        procedure append_canonical_segment(const segment_pinyin: string);
        begin
            if segment_pinyin = '' then
            begin
                Exit;
            end;
            if canonical_full_pinyin <> '' then
            begin
                canonical_full_pinyin := canonical_full_pinyin + '''';
            end;
            canonical_full_pinyin := canonical_full_pinyin + segment_pinyin;
        end;
    begin
        Result := False;
        canonical_full_pinyin := '';
        if (m_dictionary = nil) or (m_confirmed_segments = nil) or
            (m_confirmed_segments.Count = 0) or
            (not literal_commit_choice) or (commit_segment_text = '') or
            (normalized_pinyin = '') then
        begin
            Exit;
        end;

        has_nonfull_input := not is_full_pinyin_key(normalized_pinyin);
        for confirmed_segment in m_confirmed_segments do
        begin
            if (not confirmed_segment.literal_choice) or
                (confirmed_segment.canonical_pinyin = '') then
            begin
                canonical_full_pinyin := '';
                Exit;
            end;
            append_canonical_segment(confirmed_segment.canonical_pinyin);
            has_nonfull_input := has_nonfull_input or
                (not is_full_pinyin_key(confirmed_segment.pinyin));
        end;

        if (not has_nonfull_input) or
            (not m_dictionary.resolve_literal_user_word_pinyin(
            normalized_pinyin, commit_segment_text,
            current_canonical_pinyin)) then
        begin
            canonical_full_pinyin := '';
            Exit;
        end;
        append_canonical_segment(current_canonical_pinyin);

        text_unit_count := get_candidate_text_unit_count(commit_text);
        Result := (text_unit_count >= 2) and (text_unit_count <= 4) and
            is_full_pinyin_key(normalize_pinyin_text(canonical_full_pinyin));
        if not Result then
        begin
            canonical_full_pinyin := '';
        end;
    end;

begin
    out_text := '';
    if not m_has_pending_commit then
    begin
        Result := False;
        Exit;
    end;

    commit_segment_text := m_pending_commit_text;
    commit_segment_path := m_pending_commit_segment_path;
    remaining_pinyin := m_pending_commit_remaining;
    remaining_input_code := m_pending_commit_remaining_input_code;
    prev_prev_committed_text := m_prev_committed_text;
    prev_prev_committed_pinyin := m_prev_committed_pinyin;
    previous_committed_text := m_last_committed_text;
    previous_committed_pinyin := m_last_committed_pinyin;
    commit_text := commit_segment_text;
    if m_confirmed_text <> '' then
    begin
        commit_text := m_confirmed_text + commit_text;
    end;

    out_text := commit_text;
    allow_learning := m_pending_commit_allow_learning;
    learning_locked := m_pending_commit_learning_locked;
    explicit_commit_choice := m_pending_commit_explicit_choice;
    literal_commit_choice := m_pending_commit_literal_choice;
    pending_fuzzy_choice := m_pending_commit_fuzzy_choice;
    pending_fuzzy_choice_text := m_pending_commit_fuzzy_text;
    fuzzy_commit_choice := pending_fuzzy_choice;
    literal_only_commit := False;
    literal_full_pinyin := '';
    m_pending_commit_text := '';
    m_pending_commit_remaining := '';
    m_pending_commit_remaining_input_code := '';
    m_has_pending_commit := False;
    m_pending_commit_allow_learning := True;
    m_pending_commit_learning_locked := False;
    m_pending_commit_explicit_choice := False;
    m_pending_commit_literal_choice := False;
    m_pending_commit_fuzzy_choice := False;
    m_pending_commit_fuzzy_text := '';
    m_pending_commit_segment_path := '';
    normalized_pinyin := m_pending_commit_query_key;
    m_pending_commit_query_key := '';
    prev_left_context := m_left_context;
    if m_segment_left_context <> '' then
    begin
        prev_left_context := m_segment_left_context;
    end
    else if m_external_left_context <> '' then
    begin
        prev_left_context := m_external_left_context;
    end;
    update_left_context(out_text);
    if normalized_pinyin = '' then
    begin
        normalized_pinyin := normalize_pinyin_text(m_last_lookup_key);
    end;
    if normalized_pinyin = '' then
    begin
        normalized_pinyin := normalize_pinyin_text(m_composition_text);
    end;
    full_pinyin := '';
    effective_segment_path := commit_segment_path;
    if allow_learning and (commit_segment_text <> '') and (normalized_pinyin <> '') and
        ((effective_segment_path = '') or (get_path_segment_count(effective_segment_path) <= 1)) then
    begin
        inferred_segment_path := infer_segment_path_for_selected_text(commit_segment_text);
        if inferred_segment_path <> '' then
        begin
            if (effective_segment_path = '') or
                ((get_path_segment_count(inferred_segment_path) > 1) and
                ((get_path_segment_count(effective_segment_path) <= 1) or
                (get_path_segment_count(inferred_segment_path) < get_path_segment_count(effective_segment_path)))) then
            begin
                effective_segment_path := inferred_segment_path;
            end;
        end;
    end;
    if (not fuzzy_commit_choice) and (not allow_learning) and
        (not learning_locked) and
        (commit_segment_text <> '') and
        (get_candidate_text_unit_count(commit_segment_text) >= 4) and
        is_high_confidence_segment_path(effective_segment_path) and
        (not is_generated_short_particle_tail_commit(normalized_pinyin,
        commit_segment_text)) then
    begin
        // Segment-decoded multi-phrase commits should still participate in learning
        // when the inferred path contains at least one real multi-char chunk.
        allow_learning := True;
    end;
    if m_confirmed_segments <> nil then
    begin
        for segment in m_confirmed_segments do
        begin
            fuzzy_commit_choice := fuzzy_commit_choice or segment.fuzzy_choice;
            full_pinyin := full_pinyin + segment.pinyin;
            append_path_segment(segment.text);
        end;
    end;
    confirmed_pinyin_prefix := full_pinyin;
    confirmed_segment_path_prefix := combined_segment_path;
    full_pinyin := full_pinyin + normalized_pinyin;
    if effective_segment_path <> '' then
    begin
        path_start := 1;
        for path_idx := 1 to Length(effective_segment_path) + 1 do
        begin
            if (path_idx <= Length(effective_segment_path)) and
                (effective_segment_path[path_idx] <> c_segment_path_separator) then
            begin
                Continue;
            end;

            append_path_segment(Copy(effective_segment_path, path_start, path_idx - path_start));
            path_start := path_idx + 1;
        end;
    end
    else if commit_segment_text <> '' then
    begin
        append_path_segment(commit_segment_text);
    end;
    if fuzzy_commit_choice then
    begin
        clear_one_key_completion_feedback_target;
    end
    else
    begin
        record_one_key_completion_feedback_target(full_pinyin, commit_text);
    end;
    if m_dictionary <> nil then
    begin
        literal_only_commit := (m_confirmed_segments <> nil) and
            (m_confirmed_segments.Count = 0) and
            m_dictionary.is_literal_user_entry(normalized_pinyin,
            commit_segment_text);
        literal_word_to_record := (not literal_only_commit) and
            prepare_literal_user_word(literal_full_pinyin);
        if literal_word_to_record then
        begin
            literal_only_commit := True;
            m_dictionary.record_literal_user_word(literal_full_pinyin,
                commit_text);
        end;
        if literal_only_commit then
        begin
            allow_learning := False;
        end;
    end;
    if fuzzy_commit_choice then
    begin
        allow_learning := False;
        literal_only_commit := False;
        if m_dictionary <> nil then
        begin
            if m_confirmed_segments <> nil then
            begin
                for segment in m_confirmed_segments do
                begin
                    if segment.fuzzy_choice then
                    begin
                        m_dictionary.record_fuzzy_choice(segment.pinyin,
                            segment.text);
                    end;
                end;
            end;
            if pending_fuzzy_choice and
                (normalized_pinyin <> '') and (commit_segment_text <> '') then
            begin
                if pending_fuzzy_choice_text = '' then
                begin
                    pending_fuzzy_choice_text := commit_segment_text;
                end;
                m_dictionary.record_fuzzy_choice(normalized_pinyin,
                    pending_fuzzy_choice_text);
            end;
        end;
    end;
    if (m_dictionary <> nil) and (not literal_only_commit) and
        (not fuzzy_commit_choice) then
    begin
        m_dictionary.begin_learning_batch;
        try
            if not literal_only_commit then
            begin
                record_context_pair(prev_left_context, out_text);
            end;
            if allow_learning then
            begin
                if Length(path_segments) > 1 then
                begin
                    for path_idx := 1 to High(path_segments) do
                    begin
                        record_context_pair(path_segments[path_idx - 1], path_segments[path_idx]);
                    end;
                end;

                if (normalized_pinyin <> '') and (commit_segment_text <> '') then
                begin
                    m_dictionary.record_commit(normalized_pinyin, commit_segment_text,
                        explicit_commit_choice);
                    if should_persist_segment_path(effective_segment_path) then
                    begin
                        if should_persist_path_prefixes(effective_segment_path) then
                        begin
                            record_query_path_prefixes('', '', effective_segment_path,
                                normalized_pinyin, effective_segment_path);
                        end;
                        m_dictionary.record_query_segment_path(normalized_pinyin, effective_segment_path);
                    end;
                end;

                if commit_text <> '' then
                begin
                    if (full_pinyin <> '') and
                        ((full_pinyin <> normalized_pinyin) or (commit_text <> commit_segment_text)) then
                    begin
                        // The dictionary suppresses unsafe standalone user rows
                        // while still preserving query-level latest/stats.
                        m_dictionary.record_commit(full_pinyin, commit_text,
                            explicit_commit_choice or m_confirmed_explicit_choice);
                        if should_persist_segment_path(combined_segment_path) then
                        begin
                            if should_persist_path_prefixes(combined_segment_path) then
                            begin
                                record_query_path_prefixes(
                                    confirmed_pinyin_prefix,
                                    confirmed_segment_path_prefix,
                                    effective_segment_path,
                                    full_pinyin,
                                    combined_segment_path);
                            end;
                            m_dictionary.record_query_segment_path(full_pinyin, combined_segment_path);
                        end;
                    end;

                end;
            end;
            m_dictionary.commit_learning_batch;
        except
            m_dictionary.rollback_learning_batch;
            raise;
        end;
    end
    else if (m_dictionary = nil) and (not literal_only_commit) and
        (not fuzzy_commit_choice) then
    begin
        record_context_pair(prev_left_context, out_text);
    end;
    if (not literal_only_commit) and (not fuzzy_commit_choice) and
        (commit_segment_text <> '') then
    begin
        note_session_commit(commit_segment_text);
    end;
    if (not literal_only_commit) and (not fuzzy_commit_choice) and
        (commit_text <> '') and
        (commit_text <> commit_segment_text) then
    begin
        note_session_commit(commit_text);
    end;
    if allow_learning then
    begin
        if (normalized_pinyin <> '') and (commit_segment_text <> '') then
        begin
            note_session_query_choice(normalized_pinyin, commit_segment_text);
            note_session_context_query_choice(prev_left_context, normalized_pinyin, commit_segment_text);
            if get_path_segment_count(effective_segment_path) > 1 then
            begin
                note_session_query_path_prefixes('', '', effective_segment_path,
                    normalized_pinyin, effective_segment_path);
                note_session_query_path_choice(normalized_pinyin, effective_segment_path);
            end;
        end;
        if (full_pinyin <> '') and ((full_pinyin <> normalized_pinyin) or (commit_text <> commit_segment_text)) and
            (commit_text <> '') then
        begin
            note_session_query_choice(full_pinyin, commit_text);
            note_session_context_query_choice(prev_left_context, full_pinyin, commit_text);
            if get_path_segment_count(combined_segment_path) > 1 then
            begin
                note_session_query_path_prefixes(
                    confirmed_pinyin_prefix,
                    confirmed_segment_path_prefix,
                    effective_segment_path,
                    full_pinyin,
                    combined_segment_path);
                note_session_query_path_choice(full_pinyin, combined_segment_path);
            end;
        end;
        if (previous_committed_text <> '') and (previous_committed_pinyin <> '') and
            (m_confirmed_text = '') and (commit_text = commit_segment_text) and
            (normalized_pinyin <> '') and (commit_text <> '') and
            (get_candidate_text_unit_count(previous_committed_text) +
            get_candidate_text_unit_count(commit_text) <= 4) and
            should_persist_adjacent_commit_learning(previous_committed_text,
            commit_text) then
        begin
            note_session_query_choice(previous_committed_pinyin + normalized_pinyin,
                previous_committed_text + commit_text);
            if (prev_prev_committed_text <> '') and (prev_prev_committed_pinyin <> '') and
                (get_candidate_text_unit_count(prev_prev_committed_text) +
                get_candidate_text_unit_count(previous_committed_text) +
                get_candidate_text_unit_count(commit_text) <= 4) and
                should_persist_adjacent_commit_learning(
                prev_prev_committed_text + previous_committed_text,
                commit_text) then
            begin
                note_session_query_choice(prev_prev_committed_pinyin +
                    previous_committed_pinyin + normalized_pinyin,
                    prev_prev_committed_text + previous_committed_text + commit_text);
            end;
        end;
    end;
    if (not literal_only_commit) and (not fuzzy_commit_choice) and
        (commit_text <> '') then
    begin
        note_output_phrase_context(commit_text);
    end;
    if allow_learning and (Length(path_segments) > 1) then
    begin
        note_segment_path_context(combined_segment_path);
    end;
    m_last_debug_commit_segment_path := combined_segment_path;
    if (not literal_only_commit) and (not fuzzy_commit_choice) and
        (normalized_pinyin <> '') and
        (commit_text <> '') and
        (m_confirmed_text = '') and (commit_text = commit_segment_text) and
        (get_candidate_text_unit_count(commit_text) <= 4) and
        (not is_boundary_learning_unit(commit_text)) then
    begin
        m_prev_committed_text := m_last_committed_text;
        m_prev_committed_pinyin := m_last_committed_pinyin;
        m_last_committed_text := commit_text;
        m_last_committed_pinyin := normalized_pinyin;
    end
    else
    begin
        m_prev_committed_text := '';
        m_prev_committed_pinyin := '';
        m_last_committed_text := '';
        m_last_committed_pinyin := '';
    end;
    m_confirmed_text := '';
    m_confirmed_explicit_choice := False;
    m_recent_partial_prefix_text := '';
    m_segment_left_context := '';
    if m_confirmed_segments <> nil then
    begin
        m_confirmed_segments.Clear;
    end;
    reset;
    if remaining_pinyin <> '' then
    begin
        m_composition_text := remaining_pinyin;
        if remaining_input_code <> '' then
        begin
            m_composition_display_text := remaining_input_code;
        end
        else
        begin
            m_composition_display_text := remaining_pinyin;
        end;
        if is_shuangpin_input then
        begin
            rebuild_shuangpin_composition_from_input_code;
        end;
        m_page_index := 0;
        build_candidates;
    end;
    Result := True;
end;

function TncEngine.remove_user_candidate(const pinyin: string; const text: string): Boolean;
var
    empty_candidate: TncCandidate;
begin
    empty_candidate.text := '';
    empty_candidate.comment := '';
    empty_candidate.score := 0;
    empty_candidate.source := cs_rule;
    empty_candidate.has_dict_weight := False;
    empty_candidate.dict_weight := 0;
    Result := remove_user_candidate(pinyin, text, empty_candidate, False);
end;

function TncEngine.remove_user_candidate(const pinyin: string; const text: string;
    const preserve_candidate: TncCandidate; const has_preserve_candidate: Boolean): Boolean;
var
    pinyin_key: string;
    candidate_text: string;
    previous_visible_candidates: TncCandidateList;
    preserved_visible_top_candidate: TncCandidate;
    confirmed_pinyin: string;
    full_query: string;
    full_candidates: TncCandidateList;
    full_text: string;
    extension_text: string;
    idx: Integer;
    seg_idx: Integer;
    removed_confirmed_extension: Boolean;
    preserve_visible_top_candidate: Boolean;

    function same_candidate_identity(const left_candidate: TncCandidate;
        const right_candidate: TncCandidate): Boolean;
    begin
        Result := SameText(Trim(left_candidate.text), Trim(right_candidate.text)) and
            SameText(Trim(left_candidate.comment), Trim(right_candidate.comment));
    end;

    procedure preserve_visible_top_after_rebuild;
    var
        candidate_idx: Integer;
        preserved_idx: Integer;
        tmp_candidate: TncCandidate;
    begin
        if not preserve_visible_top_candidate then
        begin
            Exit;
        end;

        m_has_forced_visible_top_candidate := True;
        m_forced_visible_top_from_incremental_lm_prefix := False;
        m_forced_visible_top_candidate := preserved_visible_top_candidate;
        m_forced_visible_top_composition_text := m_composition_text;
        m_forced_visible_top_lookup_key := m_last_lookup_key;

        if (Length(m_candidates) > 0) and
            same_candidate_identity(m_candidates[0], preserved_visible_top_candidate) then
        begin
            Exit;
        end;

        preserved_idx := -1;
        for candidate_idx := 0 to High(m_candidates) do
        begin
            if same_candidate_identity(m_candidates[candidate_idx],
                preserved_visible_top_candidate) then
            begin
                preserved_idx := candidate_idx;
                Break;
            end;
        end;

        if preserved_idx > 0 then
        begin
            tmp_candidate := m_candidates[preserved_idx];
            for candidate_idx := preserved_idx downto 1 do
            begin
                m_candidates[candidate_idx] := m_candidates[candidate_idx - 1];
            end;
            m_candidates[0] := tmp_candidate;
        end
        else if preserved_idx < 0 then
        begin
            SetLength(m_candidates, Length(m_candidates) + 1);
            for candidate_idx := High(m_candidates) downto 1 do
            begin
                m_candidates[candidate_idx] := m_candidates[candidate_idx - 1];
            end;
            m_candidates[0] := preserved_visible_top_candidate;
        end;

        m_page_index := 0;
        m_selected_index := 0;
        SetLength(m_visible_candidates_cache, 0);
        SetLength(m_visible_candidate_source_indices_cache, 0);
        m_visible_candidates_cache_valid := False;
        refresh_candidate_segment_paths;
        note_ranked_top_candidate;
    end;
begin
    Result := False;
    candidate_text := Trim(text);
    pinyin_key := normalize_pinyin_text(pinyin);
    if pinyin_key = '' then
    begin
        pinyin_key := normalize_pinyin_text(m_last_lookup_key);
    end;
    if pinyin_key = '' then
    begin
        pinyin_key := normalize_pinyin_text(m_composition_text);
    end;

    if (m_dictionary = nil) or (candidate_text = '') then
    begin
        Exit;
    end;

    preserve_visible_top_candidate := False;
    SetLength(previous_visible_candidates, 0);
    m_has_forced_visible_top_candidate := False;
    m_forced_visible_top_from_incremental_lm_prefix := False;
    m_forced_visible_top_composition_text := '';
    m_forced_visible_top_lookup_key := '';
    if has_preserve_candidate and (Trim(preserve_candidate.text) <> '') and
        (not SameText(Trim(preserve_candidate.text), candidate_text)) then
    begin
        preserved_visible_top_candidate := preserve_candidate;
        preserve_visible_top_candidate := True;
    end
    else if m_composition_text <> '' then
    begin
        previous_visible_candidates := get_candidates;
        if Length(previous_visible_candidates) > 0 then
        begin
            preserved_visible_top_candidate := previous_visible_candidates[0];
            preserve_visible_top_candidate :=
                (Trim(preserved_visible_top_candidate.text) <> '') and
                (not SameText(Trim(preserved_visible_top_candidate.text),
                candidate_text));
        end;
    end;

    removed_confirmed_extension := False;
    if (m_confirmed_text <> '') and (m_confirmed_segments <> nil) and (m_confirmed_segments.Count > 0) and
        (m_composition_text <> '') then
    begin
        confirmed_pinyin := '';
        for seg_idx := 0 to m_confirmed_segments.Count - 1 do
        begin
            if m_confirmed_segments[seg_idx].pinyin <> '' then
            begin
                confirmed_pinyin := confirmed_pinyin + normalize_pinyin_text(m_confirmed_segments[seg_idx].pinyin);
            end;
        end;

        if confirmed_pinyin <> '' then
        begin
            full_query := confirmed_pinyin + normalize_pinyin_text(m_composition_text);
            if (full_query <> '') and m_dictionary.lookup(full_query, full_candidates) then
            begin
                for idx := 0 to High(full_candidates) do
                begin
                    if full_candidates[idx].source <> cs_user then
                    begin
                        Continue;
                    end;

                    full_text := Trim(full_candidates[idx].text);
                    if (full_text = '') or (Length(full_text) <= Length(m_confirmed_text)) then
                    begin
                        Continue;
                    end;
                    if Copy(full_text, 1, Length(m_confirmed_text)) <> m_confirmed_text then
                    begin
                        Continue;
                    end;

                    extension_text := Trim(Copy(full_text, Length(m_confirmed_text) + 1, MaxInt));
                    if not SameText(extension_text, candidate_text) then
                    begin
                        Continue;
                    end;

                    m_dictionary.remove_user_entry(full_query, full_text);
                    removed_confirmed_extension := True;
                end;
            end;
        end;
    end;

    if not removed_confirmed_extension then
    begin
        m_dictionary.remove_user_entry(pinyin_key, candidate_text);
    end;

    if m_build_lookup_cache <> nil then
    begin
        m_build_lookup_cache.Clear;
    end;
    SetLength(m_visible_candidates_cache, 0);
    SetLength(m_visible_candidate_source_indices_cache, 0);
    m_visible_candidates_cache_valid := False;
    clear_session_learning_after_user_removal;
    clear_lookup_bonus_caches;

    if m_composition_text <> '' then
    begin
        build_candidates;
        preserve_visible_top_after_rebuild;
    end;

    Result := True;
end;

function TncEngine.clear_user_dictionary: Boolean;
begin
    Result := (m_dictionary <> nil) and m_dictionary.clear_user_dictionary;
    if Result then
    begin
        notify_user_dictionary_cleared;
    end;
end;

function TncEngine.dictionary_base_ready: Boolean;
begin
    Result := (m_dictionary is TncSqliteDictionary) and
        TncSqliteDictionary(m_dictionary).base_ready;
end;

function TncEngine.dictionary_user_ready: Boolean;
begin
    Result := (m_dictionary is TncSqliteDictionary) and
        TncSqliteDictionary(m_dictionary).user_ready;
end;

function TncEngine.is_user_or_literal_entry(const pinyin: string;
    const text: string): Boolean;
begin
    Result := (m_dictionary <> nil) and
        (m_dictionary.is_user_entry(pinyin, text) or
        m_dictionary.is_literal_user_entry(pinyin, text));
end;

procedure TncEngine.notify_user_dictionary_cleared;
begin
    clear_session_learning_after_user_removal;
    if m_build_lookup_cache <> nil then
    begin
        m_build_lookup_cache.Clear;
    end;
    SetLength(m_candidates, 0);
    SetLength(m_visible_candidates_cache, 0);
    SetLength(m_visible_candidate_source_indices_cache, 0);
    m_visible_candidates_cache_valid := False;
    m_long_local_rerank_generated_text := '';
    m_long_local_rerank_composition_text := '';
    m_long_local_rerank_lookup_key := '';
    m_has_forced_visible_top_candidate := False;
    m_forced_visible_top_from_incremental_lm_prefix := False;
    m_forced_visible_top_composition_text := '';
    m_forced_visible_top_lookup_key := '';
    clear_segment_path_tracking;
end;

end.
