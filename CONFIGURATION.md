# 使用与设置

从 macOS 输入菜单选择言泉输入法。输入菜单中的“设置…”可调整行为和外观，默认使用简体词库、全拼、九个候选和中文标点。

使用快捷键或输入菜单切换中英文时，光标旁会弹出“言泉 logo＋中／英”状态气泡，文字为 13 号，约一秒后淡出。从其他输入法切入言泉时，也会显示当前状态。气泡跟随候选窗配色，不抢焦点、不拦截点击；开始输入、切离言泉或进入密码框时收起。优先显示在光标上方，结合屏幕空间避开附近的系统光标标识和输入法切换列表；系统提示稍晚出现或展开时会调整位置，随后保持稳定。无法取得有效光标位置时不显示。开启 macOS“减少动态效果”后，仅保留淡入淡出。

| 按键 | 行为 |
| --- | --- |
| 字母、拼音分隔符 `'` | 全拼、简拼，或当前双拼方案 |
| Space、数字 1–9 | 确认首选或对应候选；支持分段选择 |
| Enter | 提交原文 |
| Escape | 取消本次组合 |
| Backspace / Delete、方向键 | 编辑组合、调整候选 |
| `-` / `=` | 默认上一页 / 下一页 |
| Tab | 接受独立显示的一键补全 |
| 单独按下并释放 Shift | 切换中英文 |
| Ctrl + `.` | 切换中英文标点 |
| Ctrl + Shift + T | 切换简体 / 繁体 |
| Shift + Space | 切换全角 / 半角 |
| Ctrl + Shift + F10 | 打开设置 |
| Ctrl + Delete（Mac 键盘通常为 Ctrl + Fn + Delete） | 删除当前选中的用户词语 |

可直接点击候选或补全行。可删除的用户词语右侧显示小红 ×，点击即可删除，也支持右键菜单；删除操作核对当前词语身份，不受快捷键改键影响，基础词库保持只读。点击其他应用或切换输入源时，会先结束当前组合。分段选择后的组合区显示已选汉字和剩余拼音。

支持微软、小鹤、自然码、搜狗、紫光和拼音加加六套双拼。设置中还可选择简繁、模糊音规则、每页 3–9 个候选、翻页键、补全键、五项快捷键及各自启用状态。Tab 翻页与 Tab 补全不可同时启用。

输入菜单按当前状态显示目标操作，例如中文模式显示“切换到英文”，英文模式显示“切换到中文”；简繁、全半角、标点和模糊拼音也显示相应目标。

中文标点与 Windows 版一致，包括 Shift+9 / Shift+0 的 `（）`、Shift+1 的 `！`、Shift+6 的 `……`、`【】`、`『』`、`《》`、成对弯引号和 `——`。英文模式使用英文标点；在中文模式切换到英文标点后，括号等保留英文形式。

全拼支持用 `v` 输入 ü：例如 `lv`、`nv`，也接受 `lue/nue`，以及 `jv/qv/xv` 和对应的 `ve/van/vn` 写法。候选、选词和学习按规范拼音处理；Enter 仍提交实际键入的原文。

在“快捷键”页点击录制框，然后直接按所需按键，例如 Ctrl + Shift + T、Option + T 或 F10。按住 Ctrl、Shift、Option 时，录制框立即显示当前修饰键；按 Esc 取消，按 Tab 移动焦点。支持方向键、Return、Delete、Page Up、F1–F20 和数字键盘；数字键盘的数字与主键区数字共用快捷键，运算符可分别配置。单独修饰键支持 Shift；其行为在释放时触发，与 Command 等组合使用不会误切换。Command、Ctrl+Space 和 Fn 系统切换键交由 macOS 处理。未配置为快捷键的 Option 字符和死键仍使用键盘布局的行为。

设置窗口标题为“Cassotis IME - 言泉输入法 (v版本号) - 设置”。左侧有常规、外观、模糊拼音、快捷键、日志、高级六个分类；点击分类会定位右侧内容，滚动时左侧选中项同步更新。确定选项后立即自动保存和生效，无需点击“应用”，关闭时不弹出保存提示。快捷键重复或翻页 / 补全冲突会在窗口底部显示原因，并恢复已保存的设置；未完成的快捷键录制可以按 Esc 取消。

候选窗固定两行：第一行最多九个候选，不换行，也不显示候选后继拼音；过长内容省略显示，悬停可查看完整候选。第二行始终保留一键补全区，没有补全时不显示 Tab / 反引号文字，区域留空并保持高度，右侧显示 logo 和版本号。选中候选铺圆角底色。用户词的小红 × 为 10 点标记，与词语相隔约 4 点，保留 22 点宽的点击区域，命中区不覆盖词语文字。

默认 14 点字号，提供 12–32 点的分档滑块，升级保留已保存的字号。配色包括跟随系统、晴白、深色、月白、青瓷、晴蓝、松墨、靛夜，六套命名配色使用 Windows 1.25.0 的 RGB 值。外观页按选择的每页 3–9 项实时预览，使用与 Windows 版相同的普通候选示例和补全句，不显示用户词或红 ×；确认字体、字号和配色后自动保存；“恢复默认外观”恢复 14 点、跟随系统、九项候选和 PingFang SC。

字体框支持输入名称并定位、补全匹配字体，不区分大小写；支持 Command+A 全选及系统常用的复制、粘贴、撤销按键。从下拉列表选择、按 Return、离开输入框或关闭设置时确认并保存；输入不对应任何可用字体时自动恢复 PingFang SC。编辑尚未确认的内容只用于预览。

候选窗不获取文本输入焦点。显示偏好保存在 `org.cassotis.inputmethod.Cassotis` 的 UserDefaults；引擎设置、用户词语和学习记录在 `~/Library/Application Support/CassotisIME/`，替换应用不会覆盖这些文件。

密码框使用 macOS 安全输入行为。普通文本框仅读取光标前最多 1,024 个 UTF-16 单元的上下文，并处理代理对边界；切换文本客户端时清理会话。所有推理和学习在本机执行，无云端输入服务。

引擎故障或请求超时会结束当前连接，保留并提交最后可见组合，当前按键由应用继续处理；旧回复不会在重连后上屏。启动时会暂存首批普通拼音按键并显示组合串，连接成功后继续转换；两秒仍未连接或切换文本框时，暂存的原文只提交一次。

## 拼音提示与简繁用户词

输入中的异常拼音会以红色文字和下划线颜色作保守提示。例如 `niiihao` 中无法解释的两个 `i` 会被标出；正常音节、输入中的未完成声母和合法双拼码不会因此改变候选或原始输入。修改或删除异常片段后提示自动消失。标记文本的最终颜色表现由接收输入的应用支持情况决定。

切换简繁模式后，用户词按当前模式显示，无需重新学习。学习仍归入原有记录，小红 × 删除同时覆盖该词的简繁显示别名；切换本身不会清除另一模式积累的用户词。
